---
phase: 01-deploy-skeleton-login
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/account-cli.ts
  - domain/auth/accounts.ts
  - domain/auth/lockout.ts
  - domain/auth/hooks.ts
  - db/schema/login-attempts.ts
  - db/schema/index.ts
  - db/migrations/0001_login_attempts.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0001_snapshot.json
  - repositories/login-attempts.ts
  - lib/auth.ts
  - test/unit/lockout.test.ts
  - test/unit/account-cli.test.ts
  - test/integration/accounts.test.ts
  - test/integration/lockout.test.ts
  - test/integration/rate-limit.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "pnpm account:create --email E --name N [--admin] 은 계정을 만들고 무작위 임시 비밀번호를 stdout에 정확히 한 번 출력한다 (AUTH-01, D-11)"
    - "pnpm account:reset --email E 는 새 임시 비밀번호를 출력하고 그 사용자의 모든 세션을 만료시키며 password_is_temporary=true로 만든다 (AUTH-03, D-10, D-08)"
    - "같은 이메일로 15분 창 안에 5회(LOCKOUT_THRESHOLD) 실패하면 6회째는 올바른 비밀번호여도 거부된다 — 카운트는 login_attempts 표(DB)에 있어 여러 인스턴스에서 동작한다 (AUTH-01, F3)"
    - "실패 5회 중 가장 오래된 것이 15분(LOCKOUT_WINDOW_MINUTES)보다 오래되면 창 밖이라 잠금이 풀린다; 로그인 성공은 열린 실패 기록을 resolved_reason='success'로 닫는다 (AUTH-01)"
    - "pnpm account:unlock --email E 는 열린 실패 기록을 resolved_reason='admin_unlock'으로 닫고 auth.unlock JSON 로그 이벤트를 남긴다; 잠금 발생 시 auth.lockout 이벤트가 남는다 (AUTH-01 행동 로그)"
    - "같은 IP에서 60초 안에 /api/auth/sign-in/email 을 RATE_LIMIT_LOGIN_MAX(10)회 넘게 부르면 429가 온다; rateLimit 저장소는 DB(rate_limits)이고 IP는 x-forwarded-for에서 읽는다 (AUTH-01)"
    - "잠금 임계와 창 길이는 설정 키 LOCKOUT_THRESHOLD·LOCKOUT_WINDOW_MINUTES(환경 변수, lib/env.ts)로 바뀐다 (Issue 5)"
  artifacts:
    - path: "db/schema/login-attempts.ts"
      provides: "login_attempts(id, email, success, ip, attempted_at, resolved_at, resolved_reason) + email/attempted_at 인덱스"
      contains: "loginAttempts"
    - path: "domain/auth/hooks.ts"
      provides: "createAuthMiddleware before(잠금 판정)·after(기록·초기화·lockout 이벤트)"
      exports: ["before", "after"]
    - path: "domain/auth/lockout.ts"
      provides: "isLocked(openFailures, threshold), windowStart(now, minutes), lockoutConfig()"
      exports: ["isLocked", "windowStart", "lockoutConfig"]
    - path: "repositories/login-attempts.ts"
      provides: "countOpenFailures / recordAttempt / resolveOpenFailures / backdateForTest(viewer 필수)"
      contains: "viewer"
    - path: "scripts/account-cli.ts"
      provides: "create|reset|unlock 서브커맨드 CLI (로컬 tsx, 컨테이너 번들)"
      contains: "unlock"
    - path: "db/migrations/0001_login_attempts.sql"
      provides: "확장 전용 마이그레이션 (CREATE TABLE만)"
      contains: "login_attempts"
  key_links:
    - from: "lib/auth.ts"
      to: "domain/auth/hooks.ts"
      via: "betterAuth({ hooks: { before, after } })"
      pattern: "hooks:"
    - from: "domain/auth/hooks.ts"
      to: "repositories/login-attempts.ts"
      via: "countOpenFailures(SYSTEM_VIEWER, email, since)"
      pattern: "countOpenFailures"
    - from: "scripts/account-cli.ts"
      to: "domain/auth/accounts.ts"
      via: "createAccount / resetPassword / unlockAccount"
      pattern: "resetPassword|unlockAccount"
    - from: "lib/auth.ts"
      to: "db/schema/auth.ts"
      via: "rateLimit: { storage: 'database', modelName 'rateLimit' → rate_limits }"
      pattern: "storage: \"database\"|storage: 'database'"
  prohibitions:
    - statement: "MUST NOT persist, email, or re-display the temporary password; the CLI prints it exactly once to stdout and never logs it (D-11, D-13)"
      status: resolved
      verification: judgment
---

<objective>
계정 발급 수단(CLI, D-11)과 무차별 대입 방어(계정 잠금 `login_attempts` + IP 속도 제한, Eng Issue 5)를 01-01의 골격 위에 얹는다. 관리자는 터미널에서 계정을 만들고 임시 비밀번호를 재발급하고 잠금을 풀 수 있고, 직원의 로그인 실패는 DB에 기록되어 여러 Cloud Run 인스턴스에서도 같은 잠금이 걸린다.

Purpose: AUTH-01의 잠금·속도 제한·행동 로그·관리자 해제와 AUTH-03의 관리자 재발급을 실제 동작으로 증명한다(통합 테스트 = ROADMAP 기준 2 "N회 실패 잠금·15분 해제·관리자 해제").
Output: `scripts/account-cli.ts`, `login_attempts` 마이그레이션 0001, 잠금 훅, rateLimit 설정, 통합 테스트 3개·단위 테스트 2개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-RESEARCH.md
@.planning/phases/01-deploy-skeleton-login/SKELETON.md
@.planning/phases/01-deploy-skeleton-login/01-01-SUMMARY.md
@lib/auth.ts
@lib/env.ts
@domain/auth/accounts.ts
@repositories/users.ts
@db/schema/auth.ts

<interfaces>
<!-- 01-01이 만든 계약(수정 없이 사용) -->
- `domain/viewer.ts`: `Viewer`, `SYSTEM_VIEWER`
- `lib/env.ts`: `env.LOCKOUT_THRESHOLD`(5), `env.LOCKOUT_WINDOW_MINUTES`(15), `env.RATE_LIMIT_LOGIN_MAX`(10)
- `lib/log.ts`: `log.info(event, fields)` — 이벤트 이름: `auth.lockout`, `auth.unlock`, `auth.password_reset`, `auth.account_created`
- `domain/auth/accounts.ts`: `createAccount(viewer, {email,name,isAdmin})`, `generateTempPassword()` — 이 플랜이 `resetPassword`·`unlockAccount`를 같은 파일에 추가한다
- `repositories/users.ts`: `findUserByEmail`, `setPasswordTemporary`
- `lib/auth.ts`: `auth`(betterAuth 인스턴스) — 이 플랜이 `hooks`·`rateLimit`을 추가한다

<!-- 이 플랜이 만드는 계약 -->
repositories/login-attempts.ts:
```typescript
export function countOpenFailures(viewer: Viewer, email: string, since: Date): Promise<number>; // success=false AND resolved_at IS NULL AND attempted_at >= since
export function recordAttempt(viewer: Viewer, row: { email: string; success: boolean; ip: string | null; attemptedAt: Date }): Promise<void>;
export function resolveOpenFailures(viewer: Viewer, email: string, reason: "success" | "admin_unlock"): Promise<number>; // 닫은 행 수
export function backdateOpenFailures(viewer: Viewer, email: string, to: Date): Promise<void>; // 테스트 전용(창 만료 시뮬레이션)
```
domain/auth/lockout.ts:
```typescript
export function lockoutConfig(): { threshold: number; windowMinutes: number }; // env에서
export function windowStart(now: Date, windowMinutes: number): Date;
export function isLocked(openFailures: number, threshold: number): boolean; // >= threshold
export const LOCKED_MESSAGE = "로그인 시도가 너무 많습니다. 15분 뒤 다시 시도하거나 관리자에게 문의하세요.";
```
domain/auth/accounts.ts (추가):
```typescript
export function resetPassword(viewer: Viewer, email: string): Promise<{ userId: string; tempPassword: string }>;
export function unlockAccount(viewer: Viewer, email: string): Promise<{ resolved: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 계정 CLI — create / reset / unlock (D-11, AUTH-03 관리자 재발급)</name>
  <precondition>dev DB가 떠 있다 — `pg_isready -h 127.0.0.1 -p 5432`가 exit 0 (아니면 `pnpm db:dev`)</precondition>
  <files>scripts/account-cli.ts, domain/auth/accounts.ts, test/unit/account-cli.test.ts, test/integration/accounts.test.ts</files>
  <read_first>
    - domain/auth/accounts.ts (01-01 — `createAccount`가 쓴 better-auth 내부 어댑터 호출 방식을 그대로 따른다)
    - .planning/phases/01-deploy-skeleton-login/01-01-SUMMARY.md (내부 어댑터 메서드 실제 이름)
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 3 (재발급 = setPassword + revoke sessions + password_is_temporary)
    - package.json (`account:create`/`account:reset`/`account:unlock` 스크립트가 이미 `scripts/account-cli.ts create|reset|unlock`를 가리킨다)
  </read_first>
  <behavior>
    - account-cli.test.ts(단위): `parseArgs(['create','--email','a@b.c','--name','홍길동','--admin'])` → `{ cmd:'create', email:'a@b.c', name:'홍길동', admin:true }`; `create`에 `--name` 없으면 usage 에러(exit 2); `reset`/`unlock`은 `--email`만 필수; 알 수 없는 서브커맨드는 exit 2; `--email`이 이메일 형식이 아니면 exit 2
    - accounts.test.ts(통합): `resetPassword(SYSTEM_VIEWER, email)` 뒤 옛 비밀번호 로그인은 throw, 새 임시 비밀번호는 성공, 기존 세션 토큰으로 `auth.api.getSession`은 null, `users.password_is_temporary`가 true; `resetPassword`를 비관리자 viewer로 부르면 throw; 없는 이메일이면 throw(메시지에 이메일 없음); `unlockAccount`는 열린 실패 기록 수를 반환(Task 2의 표를 쓰므로 Task 2 완료 후 통과 — 이 태스크에서는 `it.todo`로 두고 Task 2가 채운다)
  </behavior>
  <action>
    D-11·D-12·D-13·AUTH-03(관리자 재발급)을 구현한다.
    1. `domain/auth/accounts.ts`에 `resetPassword`: viewer.isAdmin 검사 → `findUserByEmail` → `generateTempPassword()` → `ctx.password.hash` → `ctx.internalAdapter.updatePassword(userId, hash)` → `ctx.internalAdapter.deleteSessions(userId)`(D-10 전 세션 만료) → `setPasswordTemporary(viewer, userId, true)`(D-08) → `log.info('auth.password_reset', { userId })` → 반환. `unlockAccount`: viewer.isAdmin 검사 → `resolveOpenFailures(SYSTEM_VIEWER, email, 'admin_unlock')` → `log.info('auth.unlock', { email, resolved })` → 반환(이 함수의 import 대상 `repositories/login-attempts.ts`는 Task 2가 만든다 — Task 1에서는 함수 서명과 호출만 쓰고 Task 2에서 컴파일이 맞는지 확인; 순서를 바꿔 Task 2를 먼저 해도 된다).
    2. `scripts/account-cli.ts`: 의존성 없는 인자 파서 `parseArgs(argv)`를 export(테스트 대상)하고 `main()`은 `process.argv.slice(2)`로 서브커맨드 분기: `create` → `createAccount(SYSTEM_VIEWER, …)`, `reset` → `resetPassword`, `unlock` → `unlockAccount`. 출력: 성공 시 stdout에 사람이 읽는 두 줄(`account created: <email> (admin=true|false)` / `temporary password: <pw>`) — 임시 비밀번호는 이 한 줄뿐이며 `log.*`로는 절대 내보내지 않는다(D-11 "한 번만 출력"). 끝에 `closeDb()` 후 exit 0; 오류는 stderr 한 줄 + exit 1(usage 오류는 exit 2). `import.meta.url === pathToFileURL(process.argv[1]).href`일 때만 `main()` 실행(테스트가 import해도 실행 안 됨). next 관련 import 금지(01-05가 esbuild로 번들해 Cloud Run Job이 실행).
    3. 실제 실행 확인: `pnpm account:create --email dev-admin@example.test --name 관리자 --admin` → 출력된 임시 비밀번호로 `pnpm dev` 앱에서 로그인되는지 브라우저 없이 `curl -X POST /api/auth/sign-in/email`로 확인(200). `pnpm account:reset --email dev-admin@example.test` → 새 비밀번호로만 로그인됨. 이 개발 계정 정보는 리포에 넣지 않는다(D-12).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/account-cli.test.ts && pnpm vitest run --project integration test/integration/accounts.test.ts</automated>
    <fails_when>non-zero exit, or a summary line containing "failed" (todo 항목은 허용)</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/account-cli.ts`가 `export function parseArgs`를 내보내고 서브커맨드 문자열 `create`·`reset`·`unlock`을 모두 처리한다
    - `grep -c "temporary password" scripts/account-cli.ts`가 1이고, `grep -n "log\.\(info\|warn\|error\)" scripts/account-cli.ts domain/auth/accounts.ts`의 어느 줄에도 `tempPassword`·`password:`가 없다
    - `domain/auth/accounts.ts`가 `resetPassword`·`unlockAccount`를 export하고 `resetPassword`가 `deleteSessions`(또는 동등한 전 세션 만료 호출)과 `setPasswordTemporary(` 호출을 포함한다
    - `pnpm account:create --email x@example.test --name x`를 두 번 실행하면 두 번째는 exit 1과 "already exists" 류 메시지
    - 위 verify 명령 통과
  </acceptance_criteria>
  <done>관리자가 CLI로 계정을 만들고 재발급·해제할 수 있으며, 재발급이 전 세션을 만료시키고 임시 표시를 켠다는 것이 통합 테스트로 증명됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: login_attempts 잠금 훅 + IP 속도 제한 + 행동 로그 이벤트</name>
  <precondition>dev DB가 떠 있다 — `pg_isready -h 127.0.0.1 -p 5432`가 exit 0</precondition>
  <files>db/schema/login-attempts.ts, db/schema/index.ts, db/migrations/0001_login_attempts.sql, db/migrations/meta/_journal.json, db/migrations/meta/0001_snapshot.json, repositories/login-attempts.ts, domain/auth/lockout.ts, domain/auth/hooks.ts, lib/auth.ts, test/unit/lockout.test.ts, test/integration/lockout.test.ts, test/integration/rate-limit.test.ts, test/integration/accounts.test.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 2 (before/after 훅 전체 코드, `ctx.path !== "/sign-in/email"` 첫 줄 규칙 — Pitfall 6), §Pattern 1 rateLimit 블록, §Open Questions 2 (초기화 = 삭제가 아니라 resolved_at 기록)
    - lib/auth.ts (01-01 — 기존 옵션을 유지하고 `hooks`·`rateLimit`만 더한다)
    - db/schema/auth.ts, drizzle.config.ts (컬럼 표기 관례 casing snake_case)
    - node_modules/better-auth 타입: `createAuthMiddleware`, `APIError`, `ctx.context.newSession`, `ctx.body`, rateLimit 옵션(`enabled`, `storage`, `modelName`, `customRules`, `window`, `max`)
  </read_first>
  <behavior>
    - lockout.test.ts(단위): `isLocked(4,5)` false, `isLocked(5,5)` true, `isLocked(6,5)` true; `windowStart(new Date('2026-09-18T10:00:00Z'), 15)` = 09:45:00Z; `lockoutConfig()`가 env 값(5, 15)을 반환
    - lockout.test.ts(통합): 계정 생성 후 틀린 비밀번호 5회 → 6회째 올바른 비밀번호도 `LOCKED_MESSAGE`로 거부(HTTP 403 또는 APIError FORBIDDEN); `backdateOpenFailures(…, 16분 전)` 뒤 올바른 비밀번호 성공(창 만료); 성공 후 `countOpenFailures`가 0(resolved_reason='success'); 다시 5회 실패 → 잠금 → `unlockAccount` → 즉시 로그인 성공(resolved_reason='admin_unlock'); 없는 이메일로 5회 실패해도 6회째 응답은 같은 잠금 문구(존재 여부 비노출); 5회째 실패 시 stdout JSON에 `"event":"auth.lockout"`가 정확히 1번(console.log spy)
    - rate-limit.test.ts(통합): `auth.handler`로 `x-forwarded-for: 203.0.113.9` 헤더를 붙여 60초 창 안에 sign-in 요청 11회 → 11번째 status 429; 다른 IP(203.0.113.10)의 1회는 429가 아니다
  </behavior>
  <action>
    Eng Issue 5·F3·AUTH-01을 구현한다.
    1. 스키마: `db/schema/login-attempts.ts`에 `loginAttempts = pgTable('login_attempts', { id uuid pk defaultRandom, email text notNull, success boolean notNull, ip text, attemptedAt timestamptz notNull defaultNow, resolvedAt timestamptz, resolvedReason text })` + `index('login_attempts_email_attempted_idx').on(email, attemptedAt)`. `db/schema/index.ts`에 `export * from './login-attempts'` 한 줄 추가. `pnpm db:generate --name login_attempts` → `0001_login_attempts.sql`(CREATE TABLE·CREATE INDEX만 — 확장 전용, Squawk 통과).
    2. `repositories/login-attempts.ts`: <interfaces> 서명 그대로(모든 export 첫 인자 viewer). `resolveOpenFailures`는 `UPDATE … SET resolved_at=now(), resolved_reason=$reason WHERE email=$e AND success=false AND resolved_at IS NULL` 후 rowCount 반환. `backdateOpenFailures`는 `attempted_at`을 주어진 시각으로 UPDATE(테스트 전용이지만 repositories에 두어 계층 규칙을 지킨다).
    3. `domain/auth/lockout.ts`: <interfaces>의 순수 함수 + `LOCKED_MESSAGE`(남은 시간은 표시하지 않는다 — Claude's Discretion: 창 길이 15분만 문구에 고정, 설정값이 바뀌면 문구도 `lockoutConfig().windowMinutes`로 조합).
    4. `domain/auth/hooks.ts`: RESEARCH Pattern 2를 기반으로 — `before`: 첫 줄 `if (ctx.path !== '/sign-in/email') return;`, `email = String(ctx.body?.email ?? '').toLowerCase()`, `count = countOpenFailures(SYSTEM_VIEWER, email, windowStart(new Date(), windowMinutes))`, `isLocked(count, threshold)`이면 `throw new APIError('FORBIDDEN', { message: LOCKED_MESSAGE })`. `after`: 같은 path 검사, `success = Boolean(ctx.context.newSession)`, `ip = ctx.headers?.get('x-forwarded-for')?.split(',')[0]?.trim() ?? null`, `recordAttempt(...)`; 성공이면 `resolveOpenFailures(…, 'success')`; 실패이면 다시 `countOpenFailures`를 세어 정확히 `threshold`가 된 순간 `log.info('auth.lockout', { email, threshold, windowMinutes })`(한 번만). 로그에 비밀번호·IP 외 개인 정보 금지. 사용자 존재 여부와 무관하게 동일하게 동작한다.
    5. `lib/auth.ts`에 `hooks: { before, after }`와 `rateLimit: { enabled: true, storage: 'database', modelName: 'rateLimit', window: 60, max: 100, customRules: { '/sign-in/email': { window: 60, max: env.RATE_LIMIT_LOGIN_MAX } } }`를 추가(개발 환경에서도 `enabled: true` — 통합 테스트로 검증하기 위해; better-auth 기본은 production만). `usePlural`로 모델 `rateLimit`이 `rateLimits` → 테이블 `rate_limits`에 매핑되는지 통합 테스트 실행으로 확인, 안 맞으면 `rateLimit.modelName`을 스키마 키에 맞춘다.
    6. Task 1의 `accounts.test.ts` `it.todo`(unlockAccount)를 실제 테스트로 바꾼다.
    7. 테스트를 먼저 실패시킨 뒤 구현(TDD). 통합 테스트는 `auth.api.signInEmail`(훅 실행 확인) 또는 `auth.handler(new Request(...))` 중 훅이 실제로 도는 쪽을 쓰고 SUMMARY에 어느 쪽이었는지 적는다(rate-limit 테스트는 반드시 `auth.handler` — IP는 HTTP 헤더에서만 온다).
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm vitest run --project unit test/unit/lockout.test.ts && pnpm vitest run --project integration test/integration/lockout.test.ts test/integration/rate-limit.test.ts test/integration/accounts.test.ts</automated>
    <fails_when>non-zero exit, or a summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `db/migrations/0001_login_attempts.sql`에 `CREATE TABLE "login_attempts"`와 `CREATE INDEX`가 있고 `DROP`·`ALTER`가 없다 (`grep -c -E 'DROP|ALTER' db/migrations/0001_login_attempts.sql`이 0)
    - `domain/auth/hooks.ts`의 `before`·`after` 각 첫 실행문이 `ctx.path !== "/sign-in/email"` 검사다
    - `lib/auth.ts`에 `hooks:`, `rateLimit:`, `storage: "database"`(또는 작은따옴표), `customRules`가 있다
    - `repositories/login-attempts.ts`의 모든 `export function`(또는 `export async function`) 첫 매개변수 이름이 `viewer`다
    - 통합 테스트가 다음을 단언한다: 6회째 거부, 창 만료 후 허용, 성공 시 resolved, admin_unlock, 존재하지 않는 이메일 동일 문구, `auth.lockout` 이벤트 1회, 429
    - 위 verify 명령 통과
  </acceptance_criteria>
  <done>N회 실패 잠금·15분 창 만료·성공 초기화·관리자 해제·IP 429가 실제 Postgres 위 통합 테스트로 증명되고 잠금·해제가 JSON 로그 이벤트로 남는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저/봇 → `/api/auth/sign-in/email` | 무차별 대입·크리덴셜 스터핑이 들어오는 경계 |
| 운영자 터미널/Cloud Run Job → 계정 CLI | 관리자 권한으로 계정·비밀번호를 만드는 경계 |
| Cloud Run 프록시 → 앱 (`x-forwarded-for`) | IP 식별의 신뢰 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-06 | Spoofing | 로그인 무차별 대입(계정 단위) | high | mitigate | `login_attempts` before 훅: 15분 창 실패 ≥5 거부, DB 저장(인스턴스 무관), 통합 테스트로 고정 |
| T-1-07 | Denial of Service | 로그인 무차별 대입(IP 단위) | high | mitigate | better-auth rateLimit DB 저장소, `/sign-in/email` 60초 10회, `x-forwarded-for` 첫 항목 사용(Cloud Run이 채움) |
| T-1-08 | Information Disclosure | 잠금 문구로 계정 존재 노출 | medium | mitigate | 존재하지 않는 이메일도 같은 카운트·같은 문구(통합 테스트), 로그인 실패 문구는 이메일/비밀번호 구분 없음 |
| T-1-09 | Information Disclosure | 임시 비밀번호가 로그·DB에 남음 | high | mitigate | CLI가 stdout에 한 번만 출력, `log.*` 호출에 비밀번호 필드 금지(acceptance grep), DB에는 해시만 |
| T-1-10 | Elevation of Privilege | CLI로 임의 관리자 생성 | medium | accept | Phase 1의 CLI 실행 주체는 리포·Cloud Shell·Cloud Run Job 권한을 가진 사용자 본인뿐이며 `createAccount`는 viewer.isAdmin(SYSTEM_VIEWER)을 요구; Phase 3 관리 화면이 권한표로 대체 |
| T-1-11 | Denial of Service | 공격자가 특정 직원 이메일을 반복 실패시켜 잠가버림(lockout DoS) | low | accept | 15분 자동 해제 + 관리자 즉시 해제(CLI) + IP 속도 제한으로 완화; 완전 차단은 D-09 마찰 최소화 원칙상 미도입 |
</threat_model>

## Flagged Assumptions (spec-less edge probe)

| requirement_id | category | status | 메모 |
|---|---|---|---|
| AUTH-01 | unclassified | unresolved | 프로브가 분류하지 못한 행. 플래너 검토: 경계는 5회째(잠금 발생)·6회째(거부)·창 만료 직후로 정하고 통합 테스트 `lockout.test.ts`가 세 지점을 모두 단언한다. 429 경계는 11번째 요청 |
| AUTH-03(관리자 재발급 부분) | unclassified | unresolved | 재발급 직후 옛 세션·옛 비밀번호가 즉시 무효인지가 경계 — `accounts.test.ts`가 단언 |

## Artifacts this phase produces (this plan)

- 새 파일: `scripts/account-cli.ts`, `db/schema/login-attempts.ts`, `db/migrations/0001_login_attempts.sql`(+meta), `repositories/login-attempts.ts`, `domain/auth/lockout.ts`, `domain/auth/hooks.ts`, `test/unit/{lockout,account-cli}.test.ts`, `test/integration/{accounts,lockout,rate-limit}.test.ts`
- 수정: `domain/auth/accounts.ts`(+`resetPassword`, `unlockAccount`), `lib/auth.ts`(+`hooks`, `rateLimit`), `db/schema/index.ts`
- 심볼: `loginAttempts`, `countOpenFailures`, `recordAttempt`, `resolveOpenFailures`, `backdateOpenFailures`, `lockoutConfig`, `windowStart`, `isLocked`, `LOCKED_MESSAGE`, `before`, `after`, `resetPassword`, `unlockAccount`, `parseArgs`, CLI 서브커맨드 `create`/`reset`/`unlock`, CLI 플래그 `--email`/`--name`/`--admin`
- 로그 이벤트: `auth.lockout`, `auth.unlock`, `auth.password_reset`
- DB 객체: 테이블 `login_attempts`, 인덱스 `login_attempts_email_attempted_idx`

<verification>
- `pnpm db:migrate && pnpm test:unit && pnpm test:integration` exit 0
- `pnpm account:create`·`account:reset`·`account:unlock`이 로컬에서 실제로 실행되어 기대 출력을 낸다(SUMMARY에 마스킹한 출력 기록)
</verification>

<success_criteria>
- ROADMAP 기준 2의 통합 테스트(N회 실패 잠금·15분 해제·관리자 해제)가 실제 Postgres에서 통과한다
- 관리자가 CLI로 직원 계정을 발급·재발급·해제할 수 있다(AUTH-01, AUTH-03)
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-02-SUMMARY.md` when done — 훅이 `auth.api` 호출에서도 실행됐는지, rateLimit 모델→테이블 매핑 결과를 기록.
</output>
