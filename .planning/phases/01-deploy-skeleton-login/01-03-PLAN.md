---
phase: 01-deploy-skeleton-login
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - lib/actions/client.ts
  - app/(app)/account/page.tsx
  - app/(app)/account/actions.ts
  - app/(app)/account/change-password-form.tsx
  - app/(auth)/login/page.tsx
  - domain/auth/password.ts
  - app/admin/system-status/page.tsx
  - domain/system-status/index.ts
  - repositories/system-status.ts
  - lib/gcp/cloud-sql-admin.ts
  - test/unit/password.test.ts
  - test/unit/auth-provider.test.ts
  - test/unit/system-status.test.ts
  - test/integration/system-status.test.ts
  - test/e2e/change-password.spec.ts
  - test/e2e/system-status.spec.ts
autonomous: true
requirements: [AUTH-03, AUTH-04, OPS-06]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "직원이 /account에서 현재 비밀번호와 8자 이상 새 비밀번호를 내면 비밀번호가 바뀌고, 그 사용자의 모든 세션(현재 기기 포함)이 만료되어 /login으로 안내되며 새 비밀번호로만 다시 로그인된다 (AUTH-03, D-10)"
    - "7자 비밀번호와 내장 흔한 비밀번호 목록(password, 12345678 등)은 거부되고 문자 조합 규칙은 없다 (D-09)"
    - "임시 비밀번호(password_is_temporary=true)로 로그인한 사용자는 /account에 '임시 비밀번호를 쓰고 있습니다 — 바꾸세요' 배너를 보고, 변경을 강제당하지 않으며, 본인이 바꾸면 배너가 사라진다 (D-08)"
    - "모든 Server Action은 lib/actions/client.ts의 authedActionClient(세션 확인 → viewer 주입 → zod)로만 만들어진다 (Issue 2)"
    - "AUTH_PROVIDER 환경 변수 하나로 로그인 방식이 정해지고(기본 email), google로 바꾸면 lib/auth.ts의 socialProviders 블록과 로그인 화면의 버튼 슬롯만 켜져 코드 구조가 바뀌지 않는다 (AUTH-04)"
    - "관리자만 /admin/system-status를 보고 직원은 404를 받는다; 화면은 배포 버전(git SHA·배포 시각·환경), DB 커넥션 수/max_connections, 마지막 백업(Cloud SQL Admin API, 조회 불가면 '확인 불가')을 로드 때마다 직접 조회하고 커넥션 비율이 80% 이상이면 배너를 띄운다 (OPS-06, D-17, D-18)"
  artifacts:
    - path: "lib/actions/client.ts"
      provides: "createSafeActionClient + authedActionClient(viewer ctx)"
      exports: ["authedActionClient"]
    - path: "app/(app)/account/actions.ts"
      provides: "changePasswordAction (use server, authedActionClient.schema(zod).action)"
      contains: "authedActionClient"
    - path: "domain/auth/password.ts"
      provides: "validateNewPassword(8자 이상 + COMMON_PASSWORDS 차단), revokeAllSessions, finalizePasswordChange(전 세션 만료 + 임시 플래그 해제 — 액션이 repositories를 직접 import하지 않도록 하는 domain 출구)"
      exports: ["validateNewPassword", "COMMON_PASSWORDS", "revokeAllSessions", "finalizePasswordChange"]
    - path: "domain/system-status/index.ts"
      provides: "getSystemStatus(viewer, deps) — 비관리자 throw, 커넥션·백업·버전 합성, 배너 판정"
      exports: ["getSystemStatus", "NotAdminError"]
    - path: "lib/gcp/cloud-sql-admin.ts"
      provides: "getLastBackup — @googleapis/sqladmin backupRuns.list(maxResults 1), 실패·미설정은 unavailable"
      contains: "backupRuns.list"
    - path: "app/admin/system-status/page.tsx"
      provides: "관리자 전용 상태 화면(notFound for non-admin)"
      contains: "notFound()"
  key_links:
    - from: "app/(app)/account/actions.ts"
      to: "lib/actions/client.ts"
      via: "authedActionClient.schema(...).action(...)"
      pattern: "authedActionClient\\s*\\.schema"
    - from: "lib/actions/client.ts"
      to: "lib/auth.ts"
      via: "auth.api.getSession({ headers })"
      pattern: "auth\\.api\\.getSession"
    - from: "app/(app)/account/actions.ts"
      to: "domain/auth/password.ts"
      via: "validateNewPassword → auth.api.changePassword → finalizePasswordChange(viewer, userId) (= revokeAllSessions + setPasswordTemporary(false) — repositories 호출은 domain 안에서만, Issue 1)"
      pattern: "finalizePasswordChange"
    - from: "app/admin/system-status/page.tsx"
      to: "domain/system-status/index.ts"
      via: "getSystemStatus(viewer)"
      pattern: "getSystemStatus"
    - from: "domain/system-status/index.ts"
      to: "repositories/system-status.ts"
      via: "countConnections(viewer) / maxConnections(viewer) — pg_stat_activity, SHOW max_connections"
      pattern: "countConnections"
  prohibitions:
    - statement: "MUST NOT force a password change or redirect-loop a user who holds a temporary password — banner only (D-08)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT cache or persist system-status screen data; every page load queries pg_stat_activity and the Cloud SQL Admin API live (D-18)"
      status: resolved
      verification: judgment
---

<objective>
내 계정 화면을 완성하고(비밀번호 변경 = 첫 Server Action, `authedActionClient` 도입, 임시 비밀번호 배너, 전 세션 만료), 로그인 방식 환경 변수 전환을 테스트로 고정하고, 관리자 시스템 상태 화면 뼈대(배포 버전·DB 커넥션·마지막 백업·한도 배너)를 세운다. 화면은 전부 무스타일 임시(Phase 2 교체).

Purpose: AUTH-03(본인 변경)·AUTH-04·OPS-06을 사용자 관점에서 끝내고, 이후 모든 페이즈가 쓸 Server Action 패턴(Issue 2)과 상태 화면 확장 지점(Phase 7·8·9가 항목 추가)을 만든다.
Output: `authedActionClient`, `changePasswordAction`, 상태 화면 + domain/repositories/lib 조회 경로, E2E 2개·통합 1개·단위 3개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-RESEARCH.md
@.planning/phases/01-deploy-skeleton-login/SKELETON.md
@.planning/phases/01-deploy-skeleton-login/01-01-SUMMARY.md
@lib/auth.ts
@lib/viewer.ts
@lib/env.ts
@domain/auth/provider.ts
@repositories/users.ts
@app/(app)/account/page.tsx
@app/(auth)/login/page.tsx

<interfaces>
<!-- 01-01 계약(사용만): Viewer, SYSTEM_VIEWER, env, log, auth, authClient, getSession, requireSession, SessionUser, db, findUserById, setPasswordTemporary, getAuthProvider, createFixtureUser -->

<!-- 이 플랜이 만드는 계약 -->
lib/actions/client.ts:
```typescript
export const actionClient: ReturnType<typeof createSafeActionClient>;   // handleServerError: Error면 message, 아니면 "서버 오류가 발생했습니다."
export const authedActionClient: /* actionClient.use(session 미들웨어) */; // ctx: { viewer: Viewer; user: SessionUser } — 세션 없으면 throw new Error("로그인이 필요합니다.")
```
domain/auth/password.ts:
```typescript
export const COMMON_PASSWORDS: ReadonlySet<string>; // 100개 안팎 내장 목록(password, 12345678, qwerty123, 11111111, iloveyou, plant8123 …), 소문자 비교
export class WeakPasswordError extends Error {}
export function validateNewPassword(pw: string): void; // 8자 미만 또는 목록 포함 → WeakPasswordError(한국어 메시지)
export function revokeAllSessions(viewer: Viewer, userId: string): Promise<void>; // 본인(viewer.id===userId) 또는 관리자만; auth.$context.internalAdapter.deleteSessions
export function finalizePasswordChange(viewer: Viewer, userId: string): Promise<void>; // revokeAllSessions(viewer, userId) → setPasswordTemporary(viewer, userId, false) → log.info("auth.password_changed", { userId }) — app 액션은 이 함수만 부른다(app→repositories 직접 import 금지, Issue 1)
```
domain/system-status/index.ts:
```typescript
export class NotAdminError extends Error {}
export type SystemStatus = {
  version: { sha: string; deployedAt: string | null; env: "local" | "staging" | "prod" };
  db: { connections: number; maxConnections: number; ratio: number; banner: boolean } | { unavailable: true };
  backup: { kind: "ok"; status: string; endTime: string | null } | { kind: "unavailable"; reason: string };
};
export type StatusDeps = { countConnections: typeof countConnections; maxConnections: typeof maxConnections; getLastBackup: typeof getLastBackup; now?: () => Date };
export function getSystemStatus(viewer: Viewer, deps?: Partial<StatusDeps>): Promise<SystemStatus>; // !viewer.isAdmin → NotAdminError
export function connectionBanner(connections: number, maxConnections: number, ratio: number): boolean; // connections / maxConnections >= ratio
```
repositories/system-status.ts:
```typescript
export function countConnections(viewer: Viewer): Promise<number>;   // SELECT count(*)::int FROM pg_stat_activity WHERE datname = current_database()
export function maxConnections(viewer: Viewer): Promise<number>;     // SHOW max_connections → parseInt
```
lib/gcp/cloud-sql-admin.ts:
```typescript
export function getLastBackup(params: { project?: string; instance?: string }, timeoutMs?: number): Promise<SystemStatus["backup"]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 비밀번호 변경(authedActionClient) + 임시 비밀번호 배너 + 로그인 방식 전환 고정</name>
  <precondition>dev DB가 떠 있다 — `pg_isready -h 127.0.0.1 -p 5432`가 exit 0 (아니면 `pnpm db:dev`)</precondition>
  <reversibility rating="reversible">D-10 문자 그대로(본인 변경 시 현재 세션 포함 전 세션 만료 → 재로그인)로 구현한다. 현재 세션을 남기는 쪽으로 바꾸려면 액션 한 곳의 revoke 호출만 바뀐다.</reversibility>
  <files>lib/actions/client.ts, app/(app)/account/actions.ts, app/(app)/account/change-password-form.tsx, app/(app)/account/page.tsx, app/(auth)/login/page.tsx, domain/auth/password.ts, test/unit/password.test.ts, test/unit/auth-provider.test.ts, test/e2e/change-password.spec.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 3 (changePassword revokeOtherSessions), §Pattern 4 (authedActionClient 전체 코드 — 이 플랜의 기준 구현)
    - app/(app)/account/page.tsx, app/(auth)/login/page.tsx (01-01 — 기존 구조를 유지하고 섹션만 추가)
    - lib/viewer.ts (SessionUser.passwordIsTemporary 필드), repositories/users.ts (setPasswordTemporary), domain/auth/provider.ts
    - node_modules/next-safe-action 타입: `createSafeActionClient({ handleServerError })`, `.use(async ({ next }) => next({ ctx }))`, `.schema()`, `.action()`, 클라이언트 `useAction`
    - node_modules/better-auth 타입: `auth.api.changePassword({ body: { currentPassword, newPassword, revokeOtherSessions }, headers })`, `auth.$context.internalAdapter.deleteSessions`
  </read_first>
  <behavior>
    - password.test.ts: `validateNewPassword('abcdefg')` throws WeakPasswordError(7자); `'abcdefgh'` 통과(8자, 조합 규칙 없음 — 소문자만도 OK); `'password'`·`'12345678'`·`'PASSWORD'`(대소문자 무시) throw; `COMMON_PASSWORDS.size >= 50`
    - auth-provider.test.ts: `AUTH_PROVIDER` 미설정 → `getAuthProvider()==='email'`; `'google'` + 클라이언트 ID/시크릿 → `'google'`; `'kakao'` → env 파싱 throw; 구조 불변 검사: `lib/auth.ts` 소스에 `socialProviders` 키가 정확히 1번 나오고 `getAuthProvider() === "google"` 조건이 있다(파일 읽기 단언)
    - change-password.spec.ts(E2E): 픽스처 계정(임시)으로 로그인 → /account에 배너 텍스트 "임시 비밀번호를 쓰고 있습니다" 보임 → 현재/새(8자 이상) 제출 → URL이 /login이고 "비밀번호가 바뀌었습니다. 다시 로그인하세요." 안내 → 옛 비밀번호 로그인 실패 문구 → 새 비밀번호 로그인 성공 → 배너 없음 → 틀린 현재 비밀번호로 변경 시도하면 "현재 비밀번호가 올바르지 않습니다." → 7자 새 비밀번호는 폼 오류 문구 → 로그아웃
  </behavior>
  <action>
    AUTH-03(본인 변경)·AUTH-04·D-08·D-09·D-10·Issue 2를 구현한다.
    1. `lib/actions/client.ts`: RESEARCH Pattern 4 그대로 — `actionClient = createSafeActionClient({ handleServerError(e) { return e instanceof Error ? e.message : "서버 오류가 발생했습니다." } })`, `authedActionClient = actionClient.use(async ({ next }) => { const s = await getSession(); if (!s) throw new Error("로그인이 필요합니다."); return next({ ctx: { viewer: s.viewer, user: s.user } }); })`. 이 파일이 이후 모든 페이즈의 유일한 액션 진입점이다(01-04 린트 규칙이 `use server` 파일에서 이 이름을 요구한다).
    2. `domain/auth/password.ts`: <interfaces>대로. `COMMON_PASSWORDS`는 파일 안 상수 배열(외부 파일·의존성 없음) — 흔한 목록 상위 항목 + 회사 이름 변형(`plant8`, `plant8123`, `plant8!`). `revokeAllSessions`는 `auth.$context`의 `internalAdapter.deleteSessions(userId)`. `finalizePasswordChange(viewer, userId)`는 `revokeAllSessions(viewer, userId)` → `setPasswordTemporary(viewer, userId, false)`(01-01 `repositories/users.ts`) → `log.info("auth.password_changed", { userId })` — repositories 호출을 domain 안에 가두는 출구다(Issue 1: app은 repositories/db를 import하지 않는다, 01-04 경계 규칙).
    3. `app/(app)/account/actions.ts`(`"use server"`): `changePasswordAction = authedActionClient.schema(z.object({ currentPassword: z.string().min(1, "현재 비밀번호를 입력하세요."), newPassword: z.string().min(8, "8자 이상이어야 합니다.") })).action(async ({ parsedInput, ctx }) => { validateNewPassword(newPassword); try { await auth.api.changePassword({ body: { currentPassword, newPassword, revokeOtherSessions: true }, headers: await headers() }); } catch (e) { APIError이면 throw new Error("현재 비밀번호가 올바르지 않습니다."); 아니면 rethrow } await finalizePasswordChange(ctx.viewer, ctx.viewer.id); redirect("/login?reason=password-changed"); })`. 이 파일의 import는 `@/lib/actions/client`·`@/lib/auth`·`@/domain/auth/password`·`next/headers`·`next/navigation`·`zod`뿐이다(repositories 직접 import 없음). D-10 문자 그대로: 본인 변경도 모든 세션 만료(현재 기기 포함) → 재로그인. 비밀번호 값은 어떤 로그에도 넣지 않는다.
    4. `app/(app)/account/change-password-form.tsx`(`'use client'`): `useAction(changePasswordAction)`, 입력 2개, 서버 오류(`result.serverError`)와 필드 오류(`validationErrors`)를 폼 아래 텍스트로 표시. `app/(app)/account/page.tsx`: `requireSession()` 결과의 `user.passwordIsTemporary`가 true면 `<p role="status">임시 비밀번호를 쓰고 있습니다 — 바꾸세요.</p>` 배너(D-08: 강제 없음, 리다이렉트 없음), 그 아래 `<ChangePasswordForm/>`, 기존 로그아웃 버튼 유지.
    5. `app/(auth)/login/page.tsx`: `searchParams.reason === 'password-changed'`면 안내 문구 "비밀번호가 바뀌었습니다. 다시 로그인하세요." 표시; `getAuthProvider() === 'google'`일 때만 `authClient.signIn.social({ provider: 'google' })`를 부르는 버튼(어댑터 자리, AUTH-04)을 렌더 — 기본(email)에서는 폼만. 구조 변경 없이 조건 한 줄로 켜진다.
    6. 테스트를 먼저 작성해 실패를 확인하고 구현(TDD). E2E 픽스처는 01-01의 `createFixtureUser`(생성 계정은 `passwordIsTemporary: true`이므로 배너 시나리오에 바로 쓸 수 있다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/password.test.ts test/unit/auth-provider.test.ts && pnpm playwright test test/e2e/change-password.spec.ts</automated>
    <fails_when>non-zero exit, or a summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/actions/client.ts`가 `export const authedActionClient`를 내보내고 `createSafeActionClient(`·`handleServerError`·`getSession`을 포함한다
    - `app/(app)/account/actions.ts` 첫 줄이 `"use server"`이고 모든 export가 `authedActionClient.schema(`로 시작하며 `finalizePasswordChange(`·`redirect("/login?reason=password-changed")` 호출이 있고 `grep -c -E 'from "@/(db|repositories)' 'app/(app)/account/actions.ts'`가 0이다
    - `domain/auth/password.ts`에 `revokeAllSessions(`·`setPasswordTemporary(`·`auth.password_changed`가 있다(repositories 호출은 여기에만)
    - `app/(app)/account/page.tsx`에 `passwordIsTemporary` 조건과 문자열 "임시 비밀번호를 쓰고 있습니다"가 있고 `redirect(`가 세션 없음 외의 조건으로 쓰이지 않는다
    - `app/(auth)/login/page.tsx`에 `getAuthProvider() === "google"`(또는 작은따옴표) 조건이 있다
    - `domain/auth/password.ts`의 `COMMON_PASSWORDS`에 `password`와 `12345678`이 들어 있다
    - 위 verify 명령 통과(E2E 단언 7개)
  </acceptance_criteria>
  <done>직원이 비밀번호를 바꾸면 전 세션이 끝나고 새 비밀번호로만 로그인되며, 임시 비밀번호 배너가 변경 전후로 정확히 나타나고 사라지고, 로그인 방식이 환경 변수 하나로 정해진다는 것이 테스트로 증명됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 관리자 시스템 상태 화면 뼈대 — 배포 버전·DB 커넥션·마지막 백업·한도 배너</name>
  <precondition>dev DB가 떠 있다 — `pg_isready -h 127.0.0.1 -p 5432`가 exit 0</precondition>
  <files>app/admin/system-status/page.tsx, domain/system-status/index.ts, repositories/system-status.ts, lib/gcp/cloud-sql-admin.ts, test/unit/system-status.test.ts, test/integration/system-status.test.ts, test/e2e/system-status.spec.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 11 (pg_stat_activity, backupRuns.list), §Pitfall 5 (roles/cloudsql.viewer 가정 A3), §Architectural Responsibility Map "시스템 상태 화면"
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-17·D-18, Claude's Discretion(배너 한도 80%, 배포 버전 = git SHA + 배포 시각)
    - lib/env.ts (`GCP_PROJECT_ID`, `CLOUD_SQL_INSTANCE_ID`, `APP_GIT_SHA`, `APP_DEPLOYED_AT`, `APP_ENV`, `STATUS_CONN_BANNER_RATIO`), lib/viewer.ts, domain/viewer.ts
    - node_modules/@googleapis/sqladmin 타입: `sqladmin({ version: 'v1', auth })`, `backupRuns.list({ project, instance, maxResults })`; node_modules/google-auth-library `GoogleAuth({ scopes })`
  </read_first>
  <behavior>
    - system-status.test.ts(단위): `connectionBanner(20, 25, 0.8)` true(정확히 80%), `connectionBanner(19, 25, 0.8)` false, `connectionBanner(25, 25, 0.8)` true; `getSystemStatus(adminViewer, { countConnections: async()=>3, maxConnections: async()=>25, getLastBackup: async()=>({ kind:'unavailable', reason:'not-configured' }) })`가 `db.ratio` 0.12·`banner` false·`backup.kind` 'unavailable'·`version.sha` 'local'을 반환; `getSystemStatus({ id:'e', isAdmin:false })`는 NotAdminError; `countConnections`가 throw하면 `db: { unavailable: true }`(화면은 계속 렌더); `getLastBackup({})`(project·instance 미설정)은 네트워크 없이 즉시 `{ kind:'unavailable', reason:'not-configured' }`
    - system-status.test.ts(통합): 실제 DB로 `getSystemStatus(adminViewer)` → `db.connections >= 1`, `db.maxConnections >= 1`, `backup.kind === 'unavailable'`(로컬)
    - system-status.spec.ts(E2E): 직원 픽스처 로그인 후 `/admin/system-status` 응답 status 404; 관리자 픽스처(`createFixtureUser({ isAdmin: true })`)는 200이며 "배포 버전", "DB 커넥션", "마지막 백업", "확인 불가" 텍스트가 보이고 배너(`role=alert`)는 없다(로컬 커넥션 소수)
  </behavior>
  <action>
    OPS-06·D-17·D-18을 구현한다. 캐시·별도 저장 없이 로드 때마다 직접 조회(`export const dynamic = 'force-dynamic'`).
    1. `repositories/system-status.ts`: <interfaces> 서명(첫 인자 viewer). `countConnections`는 `db.execute(sql\`select count(*)::int as count from pg_stat_activity where datname = current_database()\`)` — 비특권 사용자에게는 다른 세션의 state가 null로 보이므로 state 필터 없이 전체 연결 수를 센다(16A 공식이 세는 단위와 같다). `maxConnections`는 `show max_connections`의 문자열을 `Number.parseInt`.
    2. `lib/gcp/cloud-sql-admin.ts`: `project`·`instance` 중 하나라도 없으면 즉시 `{ kind:'unavailable', reason:'not-configured' }`; 있으면 `new GoogleAuth({ scopes: ['https://www.googleapis.com/auth/sqlservice.admin'] })` ADC로 `sqladmin.backupRuns.list({ project, instance, maxResults: 1 })`를 `AbortSignal.timeout(timeoutMs ?? 5000)` 또는 `Promise.race` 타임아웃과 함께 호출, `items[0]`의 `status`·`endTime`을 반환, 어떤 오류든 `{ kind:'unavailable', reason: 오류 클래스 이름 }` + `log.warn('status.backup_unavailable', { reason })`(D-18 "확인 불가"). 403이면 reason에 `permission`을 포함(A3 가정 검증용 — 실제 GCP에서는 01-07 스테이징·01-08 프로덕션 human-check가 확인).
    3. `domain/system-status/index.ts`: <interfaces>대로. `version = { sha: env.APP_GIT_SHA ?? 'local', deployedAt: env.APP_DEPLOYED_AT ?? null, env: env.APP_ENV }`; db 조회 실패는 `{ unavailable: true }`로 흡수; `banner = connectionBanner(connections, maxConnections, env.STATUS_CONN_BANNER_RATIO)`. deps 주입 기본값은 실제 함수.
    4. `app/admin/system-status/page.tsx`(서버 컴포넌트): `const s = await getSession(); if (!s) redirect('/login'); if (!s.viewer.isAdmin) notFound();` 뒤 `getSystemStatus(s.viewer)`(domain도 한 번 더 검사 — 이중 방어). 섹션: "배포 버전"(sha 앞 8자·deployedAt·env), "DB 커넥션"(`connections / maxConnections`, unavailable이면 "확인 불가"), "마지막 백업"(ok면 status·endTime, 아니면 "확인 불가"), 배너 `<p role="alert">DB 커넥션이 한도의 80%를 넘었습니다.</p>`(비율 문구는 `STATUS_CONN_BANNER_RATIO*100`으로 조합). 직원에게는 어떤 배너도 없다(404이므로 자연히). 이후 페이즈가 항목을 더할 수 있게 `SystemStatus` 타입에 필드를 추가하면 섹션이 늘어나는 단순 나열 구조.
    5. TDD: 단위 → 통합 → E2E 순으로 실패 확인 후 구현. E2E는 admin 픽스처를 위해 `createFixtureUser({ isAdmin: true })` 사용.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/system-status.test.ts && pnpm vitest run --project integration test/integration/system-status.test.ts && pnpm playwright test test/e2e/system-status.spec.ts</automated>
    <fails_when>non-zero exit, or a summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/admin/system-status/page.tsx`에 `notFound()`, `getSystemStatus(`, `export const dynamic = "force-dynamic"`(또는 작은따옴표)가 있고 `unstable_cache`·`revalidate`·`cache(`가 없다 (`grep -c -E 'unstable_cache|revalidate|\bcache\(' app/admin/system-status/page.tsx domain/system-status/index.ts`가 0)
    - `repositories/system-status.ts`에 `pg_stat_activity`와 `max_connections`가 있고 모든 export 첫 매개변수가 `viewer`다
    - `lib/gcp/cloud-sql-admin.ts`에 `backupRuns.list`와 `not-configured`가 있다
    - `domain/system-status/index.ts`가 `NotAdminError`·`connectionBanner`·`getSystemStatus`를 export한다
    - 위 verify 명령 통과(E2E: 직원 404, 관리자 200 + 항목 3개 + "확인 불가")
  </acceptance_criteria>
  <done>관리자만 보는 상태 화면이 배포 버전·커넥션·백업을 실시간 조회로 보여주고 한도 배너 판정이 단위 테스트로 고정됐으며, 직원은 404를 받는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Server Action(`changePasswordAction`) | 인증된 사용자의 상태 변경 입력 |
| 브라우저 → `/admin/system-status` | 관리자 전용 정보(인프라 상태) 노출 경계 |
| 앱 → Cloud SQL Admin API(서비스 계정 ADC) | 외부 API 호출·권한 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-12 | Elevation of Privilege | `changePasswordAction` 세션 없이/타인 대상 호출 | high | mitigate | `authedActionClient` 미들웨어가 세션을 요구하고 `ctx.viewer.id`만 대상으로 변경; `revokeAllSessions`는 본인 또는 관리자만 |
| T-1-13 | Tampering | Server Action CSRF | medium | mitigate | Next.js Server Action 내장 Origin/Host 검사(POST 전용) + better-auth `changePassword`가 현재 비밀번호를 요구 |
| T-1-14 | Spoofing | 탈취된 옛 세션으로 비밀번호 변경 후 계속 사용 | high | mitigate | 변경 시 전 세션 만료(D-10, `revokeAllSessions`) + E2E로 옛 세션 무효 확인 |
| T-1-15 | Elevation of Privilege | 직원이 URL로 상태 화면 접근 | high | mitigate | 서버 컴포넌트 `notFound()` + domain `NotAdminError` 이중 검사, E2E 404 단언(D-17) |
| T-1-16 | Information Disclosure | 상태 화면의 인프라 정보(인스턴스 ID·백업 시각) | low | accept | 관리자 1인만 열람, 인스턴스 ID는 화면에 표시하지 않고 백업 상태·시각만 |
| T-1-17 | Denial of Service | Cloud SQL Admin API 지연이 화면을 멈춤 | medium | mitigate | 5초 타임아웃 + 실패 시 "확인 불가"로 렌더 계속 |
| T-1-18 | Spoofing | 약한 비밀번호 | medium | mitigate | 8자 + 흔한 목록 차단(D-09), 무차별 대입은 01-02 잠금·429 |
</threat_model>

## Flagged Assumptions (spec-less edge probe)

| requirement_id | category | status | 메모 |
|---|---|---|---|
| AUTH-03(본인 변경) | unclassified | unresolved | 경계 = 7자 거부/8자 허용, 흔한 목록의 대소문자 — `password.test.ts`가 단언 |
| AUTH-04 | unclassified | unresolved | 경계 = 허용 값 두 개(`email`·`google`) 외는 부팅 실패; google인데 클라이언트 ID 없음 → 부팅 실패 — `auth-provider.test.ts`가 단언 |
| OPS-06 | unclassified | unresolved | 경계 = 커넥션 비율 정확히 80%는 배너(≥), 79%는 없음; 조회 불가는 "확인 불가"로 계속 렌더 — `system-status.test.ts`가 단언. Cloud SQL Admin API 권한(A3 `roles/cloudsql.viewer`)은 01-07·01-08에서 실제 확인 |

## Artifacts this phase produces (this plan)

- 새 파일: `lib/actions/client.ts`, `app/(app)/account/actions.ts`, `app/(app)/account/change-password-form.tsx`, `domain/auth/password.ts`, `app/admin/system-status/page.tsx`, `domain/system-status/index.ts`, `repositories/system-status.ts`, `lib/gcp/cloud-sql-admin.ts`, `test/unit/{password,auth-provider,system-status}.test.ts`, `test/integration/system-status.test.ts`, `test/e2e/{change-password,system-status}.spec.ts`
- 수정: `app/(app)/account/page.tsx`(배너·폼), `app/(auth)/login/page.tsx`(안내·provider 슬롯)
- 심볼: `actionClient`, `authedActionClient`, `changePasswordAction`, `ChangePasswordForm`, `COMMON_PASSWORDS`, `WeakPasswordError`, `validateNewPassword`, `revokeAllSessions`, `finalizePasswordChange`, `NotAdminError`, `SystemStatus`, `StatusDeps`, `getSystemStatus`, `connectionBanner`, `countConnections`, `maxConnections`, `getLastBackup`
- 라우트: `/admin/system-status`; 쿼리 파라미터 `/login?reason=password-changed`
- 로그 이벤트: `auth.password_changed`, `status.backup_unavailable`

<verification>
- `pnpm test:unit && pnpm test:integration && pnpm test:e2e` exit 0
- `pnpm typecheck` exit 0
</verification>

<success_criteria>
- 직원이 비밀번호를 바꾸고(AUTH-03) 임시 배너가 정확히 동작하며(D-08), 모든 Server Action이 `authedActionClient`를 지난다(Issue 2)
- 로그인 방식이 환경 변수로 결정되고 Google 어댑터 자리가 구조 변경 없이 켜진다(AUTH-04)
- 관리자 상태 화면 뼈대가 세 항목과 배너를 실시간 조회로 보여준다(OPS-06)
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-03-SUMMARY.md` when done — `auth.api.changePassword` 뒤 세션 상태(현재 세션 토큰이 살아 있었는지)와 `revokeAllSessions` 호출 결과, 상태 화면의 `SystemStatus` 필드 목록을 기록.
</output>
