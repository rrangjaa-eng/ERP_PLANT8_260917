---
phase: 01-deploy-skeleton-login
plan: 05
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-04"]
files_modified:
  - Dockerfile
  - .dockerignore
  - scripts/build-cli.mjs
  - scripts/db-bootstrap.ts
  - scripts/migrate-runner.ts
  - domain/ops/pool-rule.ts
  - test/unit/pool-rule.test.ts
  - test/unit/dockerfile.test.ts
  - test/unit/db-bootstrap-sql.test.ts
  - test/integration/db-bootstrap.test.ts
  - test/integration/migrate-runner.test.ts
autonomous: true
requirements: [OPS-01, OPS-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "같은 컨테이너 이미지가 Next.js standalone 서버와 CLI 번들 셋(migrate-runner·account-cli·db-bootstrap)을 담고, Cloud Run 서비스는 node server.js, Job은 node dist/cli/<name>.mjs 로 실행된다 (OPS-01, Issue 4, D-11)"
    - "migrate-runner는 마이그레이션 전에 실제 DB의 SHOW max_connections 를 읽어 MAX_INSTANCES × DB_POOL_MAX ≤ max_connections − 5 를 검사하고, 위반이면 exit 3으로 배포를 멈춘다 — 하드코딩된 25를 믿지 않는다 (OPS-01, 16A, Pitfall 3, A2)"
    - "커넥션 규칙은 max-instances × pool 이 정확히 max_connections − 5 와 같으면 허용하고 1 크면 거부한다 (OPS-01 boundary edge — resolved explicit)"
    - "커넥션 규칙은 정수 산술만 쓰고 비정수·비숫자 환경 값은 거부한다 — 반올림·부동소수 없음 (OPS-01 precision edge — resolved explicit)"
    - "db-bootstrap은 postgres 관리 사용자로 한 번 붙어 IAM 런타임 사용자에게 DB 소유권(erp 데이터베이스·public 스키마)을 주며 재실행해도 같은 결과다; 앱·migrate 경로는 IAM 인증만 쓴다 (OPS-01, 6A)"
    - "컨테이너는 min-instances 0 전제의 standalone 이미지이며 비루트 사용자로 실행된다 (OPS-02)"
  artifacts:
    - path: "Dockerfile"
      provides: "node:24-slim 멀티스테이지(deps → build → runtime), standalone + dist/cli + db/migrations, non-root"
      contains: "dist/cli"
    - path: "scripts/build-cli.mjs"
      provides: "esbuild ESM 번들 3개 (외부 pg-native, createRequire 배너)"
      contains: "createRequire"
    - path: "scripts/db-bootstrap.ts"
      provides: "buildBootstrapSql + main (DB_ADMIN_URL 또는 커넥터 PASSWORD 경로)"
      exports: ["buildBootstrapSql"]
    - path: "domain/ops/pool-rule.ts"
      provides: "checkPoolRule({maxInstances, poolMax, maxConnections})"
      exports: ["checkPoolRule", "PoolRuleInputError"]
    - path: "scripts/migrate-runner.ts"
      provides: "SHOW max_connections → checkPoolRule → migrate()"
      contains: "max_connections"
  key_links:
    - from: "scripts/migrate-runner.ts"
      to: "domain/ops/pool-rule.ts"
      via: "checkPoolRule 결과 ok=false → exit 3"
      pattern: "checkPoolRule"
    - from: "Dockerfile"
      to: "scripts/build-cli.mjs"
      via: "RUN pnpm build && pnpm build:cli"
      pattern: "build:cli"
    - from: "scripts/db-bootstrap.ts"
      to: "db/client.ts"
      via: "같은 Connector 사용, authType PASSWORD (관리 경로만)"
      pattern: "AuthTypes\\.PASSWORD"
  prohibitions:
    - statement: "MUST NOT assign a public IP to Cloud SQL instances; app and jobs reach the DB only through the Cloud SQL connector on private IP with IAM auth, the postgres password path exists only inside the db-bootstrap job (6A)"
      status: resolved
      verification: judgment
---

<objective>
배포 파이프라인의 "이미지 안" 부분을 만든다: 멀티스테이지 Dockerfile(standalone + CLI 번들), esbuild CLI 번들러, DB 소유권 부트스트랩 Job 스크립트, 그리고 migrate-runner에 16A 커넥션 규칙 검사(실제 `SHOW max_connections`)를 넣는다. 전부 로컬 Postgres와 단위·통합 테스트로 증명한다(Docker 데몬 없는 클라우드 세션에서도 번들·규칙·부트스트랩 SQL은 실행 검증 가능; 실제 이미지 빌드는 01-07의 GitHub Actions에서 처음 일어난다).

Purpose: 01-06의 deploy.sh가 부를 실행 단위(서비스·Job 3개)를 같은 이미지 하나로 고정하고(Issue 4), F10/Pitfall 11의 커넥션 폭증을 배포 시점에 거부한다(16A).
Output: `Dockerfile`, `.dockerignore`, `scripts/build-cli.mjs`, `scripts/db-bootstrap.ts`, `domain/ops/pool-rule.ts`, migrate-runner 확장, 테스트 5개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-RESEARCH.md
@.planning/phases/01-deploy-skeleton-login/SKELETON.md
@.planning/phases/01-deploy-skeleton-login/01-01-SUMMARY.md
@.planning/phases/01-deploy-skeleton-login/01-02-SUMMARY.md
@scripts/migrate-runner.ts
@scripts/account-cli.ts
@db/client.ts
@lib/env.ts
@next.config.ts
@package.json

<interfaces>
<!-- 01-01 계약: env.MAX_INSTANCES?, env.DB_POOL_MAX(5), env.DB_NAME(erp), env.DB_IAM_USER?, env.DB_ADMIN_PASSWORD?, env.DB_ADMIN_URL?, env.CLOUD_SQL_CONNECTION_NAME?; log; db/pool/closeDb; package.json `build:cli`=`node scripts/build-cli.mjs` -->

domain/ops/pool-rule.ts:
```typescript
export class PoolRuleInputError extends Error {}
export type PoolRuleInput = { maxInstances: number; poolMax: number; maxConnections: number };
export type PoolRuleResult =
  | { ok: true; used: number; limit: number; headroom: number }        // used = maxInstances*poolMax, limit = maxConnections-5
  | { ok: false; used: number; limit: number; reason: string };
export function checkPoolRule(input: PoolRuleInput): PoolRuleResult;  // 세 값 모두 Number.isInteger && > 0 아니면 PoolRuleInputError; ok ⇔ used <= limit
export function parsePoolEnv(env: { MAX_INSTANCES?: string | number; DB_POOL_MAX?: string | number }): { maxInstances: number; poolMax: number }; // 정수 문자열만 허용("3.5"·"abc" → PoolRuleInputError)
```
scripts/db-bootstrap.ts:
```typescript
export function buildBootstrapSql(p: { dbName: string; iamUser: string }): Array<{ database: string; statements: string[] }>;
// [ { database: "postgres", statements: [ role 존재 검사, GRANT "<iam>" TO CURRENT_USER, (CREATE DATABASE … OWNER | ALTER DATABASE … OWNER TO) ] },
//   { database: dbName,     statements: [ ALTER SCHEMA public OWNER TO "<iam>", GRANT ALL ON SCHEMA public TO "<iam>" ] } ]
// 식별자는 큰따옴표로 감싸고 내부 큰따옴표는 이스케이프(주입 방지)
```
컨테이너 실행 계약(01-06 deploy.sh가 사용):
- Job 공통 env(01-01 `lib/env.ts` 계약 — 세 번들 모두 lib/env.ts를 import하므로 `APP_ENV != local`이면 파싱 단계에서 요구된다): `APP_ENV`, `BETTER_AUTH_SECRET`(32자 이상, Secret Manager), `BETTER_AUTH_URL`(서비스 URL) — 01-06 `deploy_jobs`가 Job 3개 모두에 넣는다; 빠지면 Job이 부팅되지 않는다
- 서비스: `node server.js` (standalone), PORT 3000
- Job migrate: `node dist/cli/migrate-runner.mjs` (env MAX_INSTANCES, DB_POOL_MAX, CLOUD_SQL_CONNECTION_NAME, DB_IAM_USER, DB_NAME) — exit 0 성공 / 1 마이그레이션 실패 / 3 커넥션 규칙 위반
- Job db-bootstrap: `node dist/cli/db-bootstrap.mjs` (env DB_ADMIN_PASSWORD, CLOUD_SQL_CONNECTION_NAME, DB_IAM_USER, DB_NAME) — exit 0 / 1
- Job account: `node dist/cli/account-cli.mjs <create|reset|unlock> --email …` — exit 0 / 1 / 2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 컨테이너 이미지 — Dockerfile 멀티스테이지 + esbuild CLI 번들 + db-bootstrap Job 스크립트</name>
  <precondition>dev DB가 떠 있다 — `pg_isready -h 127.0.0.1 -p 5432`가 exit 0 (아니면 `pnpm db:dev`)</precondition>
  <files>Dockerfile, .dockerignore, scripts/build-cli.mjs, scripts/db-bootstrap.ts, test/unit/dockerfile.test.ts, test/unit/db-bootstrap-sql.test.ts, test/integration/db-bootstrap.test.ts</files>
  <read_first>
    - .planning/research/STACK.md §Development Tools (Node 24 `node:24-slim`, Docker 멀티스테이지 standalone)
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 6 (Connector 옵션), §Pattern 7 (Job이 같은 이미지로 migrate 실행)
    - next.config.ts (`output: 'standalone'`, `serverExternalPackages`), scripts/migrate-runner.ts, scripts/account-cli.ts (번들 진입점 — next import가 없어야 함), lib/env.ts, db/client.ts
    - node_modules/esbuild 문서 `build()` 옵션(`format: 'esm'`, `banner.js`, `external`, `tsconfig`), node_modules/@google-cloud/cloud-sql-connector README(`AuthTypes.PASSWORD`, `IpAddressTypes.PRIVATE`)
  </read_first>
  <behavior>
    - dockerfile.test.ts: Dockerfile 텍스트에 `FROM node:24-slim`이 2회 이상, 스테이지 이름 `deps`·`build`·`runtime`, `pnpm install --frozen-lockfile`, `pnpm build && pnpm build:cli`, `COPY --from=build /app/.next/standalone`, `COPY --from=build /app/dist/cli`, `COPY --from=build /app/db/migrations`, `USER`(비루트), `EXPOSE 3000`, `CMD ["node", "server.js"]`가 있고 `.dockerignore`에 `node_modules`, `.next`, `.env`, `.git`, `test`, `.planning`이 있다
    - db-bootstrap-sql.test.ts: `buildBootstrapSql({ dbName: 'erp', iamUser: 'erp-staging-runtime@proj.iam' })`의 첫 그룹 database가 `postgres`, 문장에 `GRANT "erp-staging-runtime@proj.iam" TO CURRENT_USER`와 `OWNER`가 있고, 둘째 그룹 database가 `erp`이며 `ALTER SCHEMA public OWNER TO "erp-staging-runtime@proj.iam"`이 있다; iamUser에 큰따옴표가 들어오면 이스케이프된다(`"a""b"`)
    - db-bootstrap.test.ts(통합, DB_ADMIN_URL=erp_test 슈퍼유저 경로): 테스트가 `CREATE ROLE "bootstrap_iam_test" LOGIN`을 먼저 만들고 dbName `erp_bootstrap_test`로 main 로직을 실행하면 `pg_database`에 그 DB가 소유자 `bootstrap_iam_test`로 존재; 두 번째 실행도 성공(멱등); 끝나면 테스트가 DB·롤을 정리
  </behavior>
  <action>
    OPS-01(같은 이미지의 Job)·OPS-02(스케일-투-제로 이미지)·6A를 구현한다.
    1. `Dockerfile`: `FROM node:24-slim AS base`(`corepack enable`, `WORKDIR /app`) → `deps`(`COPY package.json pnpm-lock.yaml ./`, `RUN pnpm install --frozen-lockfile`) → `build`(`COPY --from=deps /app/node_modules ./node_modules`, `COPY . .`, `ARG GIT_SHA`, 빌드 전용 더미 환경 `ENV APP_ENV=local DATABASE_URL=postgres://build:build@127.0.0.1:5432/build BETTER_AUTH_SECRET=build-time-only-secret-not-used-at-runtime BETTER_AUTH_URL=http://localhost:3000 NEXT_TELEMETRY_DISABLED=1`(lib/env.ts가 import 시 파싱하므로 `next build`에 값이 필요하다; 런타임은 Cloud Run 환경이 덮어쓴다 — 주석으로 명시), `RUN pnpm build && pnpm build:cli`) → `runtime`(`FROM node:24-slim`, `ENV NODE_ENV=production PORT=3000 HOSTNAME=0.0.0.0`, `RUN groupadd --system app && useradd --system --gid app app`, `COPY --from=build /app/.next/standalone ./`, `COPY --from=build /app/.next/static ./.next/static`, `COPY --from=build /app/public ./public`, `COPY --from=build /app/dist/cli ./dist/cli`, `COPY --from=build /app/db/migrations ./db/migrations`, `USER app`, `EXPOSE 3000`, `CMD ["node", "server.js"]`). `.dockerignore`: `node_modules`, `.next`, `dist`, `.env*`, `.git`, `.planning`, `docs`, `test`, `playwright-report`, `test-results`, `.claude`.
    2. `scripts/build-cli.mjs`: esbuild `build({ entryPoints: ['scripts/migrate-runner.ts', 'scripts/account-cli.ts', 'scripts/db-bootstrap.ts'], bundle: true, platform: 'node', target: 'node24', format: 'esm', outdir: 'dist/cli', outExtension: { '.js': '.mjs' }, external: ['pg-native'], banner: { js: "import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);" }, tsconfig: 'tsconfig.json', logLevel: 'info' })`. 번들 안에 `next`가 끌려오면(01-01·01-02 계약 위반) 실패시키는 검사: 빌드 후 `dist/cli/*.mjs`에 `from "next` 문자열이 없어야 한다.
    3. `scripts/db-bootstrap.ts`: <interfaces>의 `buildBootstrapSql` + `main()`: 연결은 `env.DB_ADMIN_URL`이 있으면 `new Pool({ connectionString })`(로컬·통합 테스트), 없으면 `Connector().getOptions({ instanceConnectionName: env.CLOUD_SQL_CONNECTION_NAME, authType: AuthTypes.PASSWORD, ipType: IpAddressTypes.PRIVATE })` + `user: 'postgres'`, `password: env.DB_ADMIN_PASSWORD`; `database`는 그룹마다 새 Pool(postgres → dbName). CREATE DATABASE는 트랜잭션 밖 단일 문장으로 실행하고 존재 여부는 `SELECT 1 FROM pg_database WHERE datname=$1`로 먼저 확인. 성공 `log.info('db.bootstrap', { dbName, iamUser })` exit 0, 실패 `log.error` exit 1. `import.meta.url` 가드로 테스트 import 시 실행 방지. 비밀번호는 로그 금지.
    4. 실행 확인: `pnpm build:cli` → `dist/cli/{migrate-runner,account-cli,db-bootstrap}.mjs` 생성 → `node --env-file=.env.local dist/cli/migrate-runner.mjs`가 로컬 DB에 exit 0(번들이 실제로 동작). Docker 데몬이 있으면(`docker info` 성공) `docker build -t erp:test --build-arg GIT_SHA=test .`까지 실행하고 결과를 SUMMARY에; 없으면 그 사실을 SUMMARY에 적는다(첫 실제 빌드는 01-07 GitHub Actions).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/dockerfile.test.ts test/unit/db-bootstrap-sql.test.ts && pnpm vitest run --project integration test/integration/db-bootstrap.test.ts && pnpm build:cli && node --env-file=.env.local dist/cli/migrate-runner.mjs</automated>
    <fails_when>non-zero exit, a vitest summary containing "failed", or dist/cli/db-bootstrap.mjs absent after build:cli</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls dist/cli/migrate-runner.mjs dist/cli/account-cli.mjs dist/cli/db-bootstrap.mjs` 셋 다 존재
    - `grep -c 'from "next' dist/cli/*.mjs`가 파일마다 0
    - `Dockerfile`에 `AS deps`, `AS build`, `AS runtime`, `USER app`, `CMD ["node", "server.js"]`가 있고 `--no-assign-ip` 같은 인프라 플래그는 없다(이미지는 인프라를 모른다)
    - `scripts/db-bootstrap.ts`에 `AuthTypes.PASSWORD`와 `DB_ADMIN_URL`이 있고 `password` 값이 `log.` 호출에 들어가지 않는다
    - 위 verify 명령 통과
  </acceptance_criteria>
  <done>같은 이미지가 서버와 CLI 번들 셋을 담는 Dockerfile이 정의되고, 번들이 로컬 DB에 실제로 실행되며, DB 소유권 부트스트랩이 멱등하게 동작함이 통합 테스트로 증명됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 16A 커넥션 규칙 — domain/ops/pool-rule + migrate-runner의 SHOW max_connections 검사</name>
  <precondition>dev DB가 떠 있다 — `pg_isready -h 127.0.0.1 -p 5432`가 exit 0</precondition>
  <files>domain/ops/pool-rule.ts, scripts/migrate-runner.ts, test/unit/pool-rule.test.ts, test/integration/migrate-runner.test.ts</files>
  <read_first>
    - docs/designs/plant8-erp-roadmap-ceo-review-260917.md 16A (공식 `max-instances × 풀 ≤ DB max_connections − 5`, 배포 거부), §Error & Rescue `deploy` 행(PoolRuleViolation)
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 6 커넥션 공식 문단, §Pitfall 3, §Assumptions A2 (25는 가정 — 실제 값을 읽어라)
    - scripts/migrate-runner.ts (01-01 — `main()` 구조 유지, 앞 단계로 검사 삽입), lib/env.ts (`MAX_INSTANCES`, `DB_POOL_MAX`)
  </read_first>
  <behavior>
    - pool-rule.test.ts: `checkPoolRule({maxInstances:3, poolMax:5, maxConnections:25})` → ok, used 15, limit 20, headroom 5; `{4,5,25}` → ok(20 ≤ 20, 경계 허용); `{5,5,25}` → ok false, reason에 "25 > 20"; `{1,20,25}` ok; `{3,5,24}` → ok false(15 > 19? 아니오 — 15 ≤ 19 ok) 대신 `{4,5,24}` → false(20 > 19); `maxInstances: 3.5` → PoolRuleInputError; `maxConnections: 0` → PoolRuleInputError; `parsePoolEnv({ MAX_INSTANCES: '3', DB_POOL_MAX: '5' })` → {3,5}; `'3.5'`·`'abc'`·`''` → PoolRuleInputError; `MAX_INSTANCES` 미설정 → PoolRuleInputError(호출자가 스킵 여부 결정)
    - migrate-runner.test.ts(통합, 자식 프로세스): `MAX_INSTANCES=1000 DB_POOL_MAX=5` + erp_test DATABASE_URL로 `node --import tsx scripts/migrate-runner.ts` 실행 → exit code 3, stdout JSON 줄에 `"event":"deploy.pool_rule_violation"`; `MAX_INSTANCES=1 DB_POOL_MAX=1` → exit 0, stdout에 `"event":"db.max_connections"`와 정수 `value`; `MAX_INSTANCES` 미설정 → exit 0, stdout에 `"event":"db.pool_rule_skipped"`(로컬 개발 경로)
  </behavior>
  <action>
    16A·F10·A2를 구현한다.
    1. `domain/ops/pool-rule.ts`를 <interfaces>대로 — 정수 검증(`Number.isInteger`, `> 0`), `used = maxInstances * poolMax`, `limit = maxConnections - 5`, `ok = used <= limit`(경계 포함), reason 문자열 `"${used} > ${limit} (max_instances ${maxInstances} × pool ${poolMax} > max_connections ${maxConnections} − 5)"`. 부동소수·반올림 없음.
    2. `scripts/migrate-runner.ts`의 `main()` 앞 단계: `MAX_INSTANCES`가 설정돼 있으면 `parsePoolEnv(process.env)` → `SHOW max_connections`(`db.execute(sql\`show max_connections\`)` → 첫 행의 문자열을 `Number.parseInt`) → `log.info('db.max_connections', { value })`(A2 가정 25를 배포마다 실측·기록) → `checkPoolRule` → `ok:false`면 `log.error('deploy.pool_rule_violation', { used, limit, reason })` + `closeDb()` + `process.exit(3)`; `PoolRuleInputError`면 `log.error('deploy.pool_rule_input_error')` + exit 3. `MAX_INSTANCES` 미설정이면 `log.warn('db.pool_rule_skipped', { reason: 'MAX_INSTANCES unset' })`. 통과하면 기존 migrate 진행. exit 코드 계약: 0 성공 / 1 마이그레이션 실패 / 3 규칙 위반(01-06 deploy.sh가 메시지로 구분).
    3. TDD: 단위 → 통합 순. 통합 테스트는 `child_process.spawnSync('node', ['--import', 'tsx', 'scripts/migrate-runner.ts'], { env })`로 exit code와 stdout을 단언한다.
    4. `pnpm build:cli`를 다시 돌려 번들에도 반영됐는지 `grep -c max_connections dist/cli/migrate-runner.mjs`로 확인.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/pool-rule.test.ts && pnpm vitest run --project integration test/integration/migrate-runner.test.ts && pnpm build:cli && grep -c max_connections dist/cli/migrate-runner.mjs</automated>
    <fails_when>non-zero exit, a vitest summary containing "failed", or the final grep printing 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/ops/pool-rule.ts`가 `checkPoolRule`·`parsePoolEnv`·`PoolRuleInputError`를 export하고 `Number.isInteger`를 사용한다
    - `scripts/migrate-runner.ts`에 `show max_connections`, `checkPoolRule(`, `process.exit(3)`, 이벤트 이름 `db.max_connections`·`deploy.pool_rule_violation`·`db.pool_rule_skipped`가 있다
    - 단위 테스트가 경계(20 ≤ 20 허용, 20 > 19 거부)와 비정수 거부를 단언한다
    - 위 verify 명령 통과
  </acceptance_criteria>
  <done>배포 시점에 실제 DB의 max_connections로 16A 규칙을 검사해 위반이면 exit 3으로 멈추는 migrate-runner가 로컬 Postgres에서 증명됐고, 규칙의 경계·정밀도가 단위 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 빌드 러너 → 베이스 이미지·npm | 공급망 |
| Cloud Run Job(db-bootstrap) → Cloud SQL `postgres` 관리 사용자 | 유일한 비밀번호 인증 경로 |
| Cloud Run 서비스/Job → Cloud SQL(IAM, 프라이빗 IP) | 앱 데이터 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-24 | Tampering | 베이스 이미지 `node:24-slim` 변조/드리프트 | medium | mitigate | 태그 고정 + 01-07 첫 빌드 뒤 `docker inspect`로 얻은 digest를 OPERATIONS.md에 기록해 이후 `@sha256` 고정 후보로 관리(01-08 문서 태스크) |
| T-1-25 | Elevation of Privilege | 컨테이너 루트 실행 | medium | mitigate | `USER app` 비루트, standalone 최소 파일만 복사 |
| T-1-26 | Information Disclosure | 빌드 스테이지 더미 시크릿이 런타임에 남음 | low | accept | 더미 값은 `build` 스테이지 ENV에만 있고 `runtime` 스테이지는 `FROM node:24-slim`으로 새로 시작해 상속되지 않음; 값 자체가 비밀이 아님 |
| T-1-27 | Spoofing | `DB_ADMIN_PASSWORD` 노출로 관리 사용자 탈취 | high | mitigate | Secret Manager 접미사 시크릿 → db-bootstrap Job에만 주입(서비스·migrate Job에는 없음, 01-06 deploy.sh), 프라이빗 IP라 외부 접근 불가, 로그 금지 |
| T-1-28 | Denial of Service | 커넥션 폭증(F10) | high | mitigate | migrate-runner 16A 검사 exit 3 → deploy.sh 중단; `DB_POOL_MAX` 5·`MAX_INSTANCES` 3 기본 |
| T-1-SC2 | Tampering | 이미지 빌드 공급망(npm postinstall) | high | mitigate | `pnpm install --frozen-lockfile` + `onlyBuiltDependencies` 2개만 + 01-01의 승인된 목록 |
</threat_model>

## Flagged Assumptions (spec-less edge probe)

| requirement_id | category | status | 메모 |
|---|---|---|---|
| OPS-01 | boundary | resolved (explicit) | `must_haves.truths`: 정확히 `max_connections − 5`는 허용, +1은 거부 — `pool-rule.test.ts` |
| OPS-01 | precision | resolved (explicit) | `must_haves.truths`: 정수 산술만, 비정수·비숫자 거부 — `pool-rule.test.ts` |
| OPS-02 | unclassified | unresolved | "비용 0에 가깝다"의 경계는 숫자로 검증할 수 없다(청구서 의존). 이 플랜은 min-instances 0 전제의 이미지(비루트·standalone)만 보장하고 실제 금액 확인은 01-08 문서 태스크와 사용자 청구서 확인(수동)에 둔다 |

## Artifacts this phase produces (this plan)

- 새 파일: `Dockerfile`, `.dockerignore`, `scripts/build-cli.mjs`, `scripts/db-bootstrap.ts`, `domain/ops/pool-rule.ts`, `test/unit/{pool-rule,dockerfile,db-bootstrap-sql}.test.ts`, `test/integration/{db-bootstrap,migrate-runner}.test.ts`
- 수정: `scripts/migrate-runner.ts`(16A 검사)
- 빌드 산출물(gitignored): `dist/cli/migrate-runner.mjs`, `dist/cli/account-cli.mjs`, `dist/cli/db-bootstrap.mjs`
- 심볼: `checkPoolRule`, `parsePoolEnv`, `PoolRuleInputError`, `PoolRuleInput`, `PoolRuleResult`, `buildBootstrapSql`; Docker 스테이지 `base`/`deps`/`build`/`runtime`, 빌드 인자 `GIT_SHA`; 종료 코드 계약 migrate-runner 0/1/3
- 로그 이벤트: `db.max_connections`, `deploy.pool_rule_violation`, `deploy.pool_rule_input_error`, `db.pool_rule_skipped`, `db.bootstrap`

<verification>
- `pnpm test:unit && pnpm test:integration && pnpm build:cli` exit 0
- `pnpm lint && pnpm typecheck` exit 0 (01-04 규칙 하에서 `domain/ops`·`scripts`가 경계를 지킨다)
</verification>

<success_criteria>
- 같은 이미지가 서비스와 Job 3개를 실행하는 컨테이너 정의가 있고 CLI 번들이 로컬 DB에서 실제로 돈다(OPS-01, Issue 4)
- 16A 검사가 실측값으로 동작하고 위반 시 exit 3으로 배포를 거부한다(OPS-01, Pitfall 3)
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-05-SUMMARY.md` when done — 로컬 Docker 빌드 실행 여부·결과, 번들 크기, `SHOW max_connections`가 로컬에서 반환한 값(참고)을 기록.
</output>
