---
phase: 01-deploy-skeleton-login
plan: 06
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-04"]
coupling_justified: ["01-05: 01-06 tests run only against fake gcloud/docker/curl shims and touch no DB, port, or file 01-05 uses; both plans only read package.json scripts — order irrelevant"]
files_modified:
  - infra/names.sh
  - infra/ar-cleanup-policy.json
  - scripts/deploy.sh
  - scripts/rollback.sh
  - scripts/bootstrap-gcp.sh
  - infra/monitoring/5xx-ratio.json.tpl
  - infra/monitoring/backup-failed.json.tpl
  - infra/monitoring/tick-stale.json.tpl
  - .github/workflows/deploy.yml
  - .github/workflows/account.yml
  - test/unit/deploy/fakebin/gcloud
  - test/unit/deploy/fakebin/docker
  - test/unit/deploy/fakebin/curl
  - test/unit/deploy/deploy-sh.test.ts
  - test/unit/deploy/rollback-sh.test.ts
  - test/unit/deploy/bootstrap-sh.test.ts
  - test/unit/deploy/workflows.test.ts
autonomous: true
requirements: [OPS-01, OPS-02]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "scripts/deploy.sh --env <staging|prod> --project <ID> --region <R> [--domain D] 한 번으로 Artifact Registry(서울)·Cloud SQL(서울, db-f1-micro, 공인 IP 없음, IAM 인증, 자동 백업)·Secret Manager(접미사 시크릿)·Cloud Run 서비스와 Job 3개가 없으면 만들어지고 있으면 갱신되며, 다른 프로젝트 ID로도 같은 결과를 낸다 (OPS-01, D-03, D-04)"
    - "deploy.sh는 더티 트리(git status --porcelain 비어 있지 않음)에서 아무 gcloud 명령도 실행하지 않고 exit 2로 거부한다 (Issue 4)"
    - "deploy.sh 순서는 이미지(git SHA 태그, 이미 있으면 빌드 생략) → db-bootstrap Job → migrate Job(실패·exit 3이면 중단) → 기존 서비스면 --no-traffic + rev-<sha8> 태그 리비전 → 스모크(/healthz ok:true, /login 200, 그리고 Origin=SERVICE_URL로 가짜 자격의 POST /api/auth/sign-in/email이 403이 아닌 4xx — BETTER_AUTH_URL이 실제 접속 URL과 같음을 배포마다 증명) → 경보 3개 upsert → 100% 승격 이며, 스모크·경보 실패 시 트래픽을 옮기지 않고 exit 1 이다 (OPS-01, 20A, Issue 4, 플랜 리뷰 Eng Issue 2·CEO OV-5·Eng OV-8)"
    - "SERVICE_URL은 결정적 형식 https://erp-<env>-<PROJECT_NUMBER>.<region>.run.app 하나이며 서비스 env BETTER_AUTH_URL·Job env·스모크·stdout 전부가 같은 값을 쓴다 — describe가 다른 호스트(레거시 *.a.run.app)를 돌려줘도 정보로만 남긴다; 두 주소가 다르면 better-auth Origin 검사 때문에 한쪽에서는 로그인이 403이 되므로 접속 주소는 하나로 고정한다 (플랜 리뷰 Eng Issue 2)"
    - "scripts/rollback.sh --env --project --region 은 현재 100% 서빙 중인 리비전보다 오래된 최신 리비전으로 트래픽 100%를 되돌린다 — 스모크에 실패해 0%로 남은 최신 리비전은 건너뛴다 — 서빙 리비전이 가장 오래된 것이면 거부한다 (OPS-01, 플랜 리뷰 Eng Issue 3)"
    - "deploy.sh는 어느 단계에서 실패하든 stderr 마지막 줄에 `deploy failed at <stage>`를 남긴다 — gcloud 오류가 어느 함수에서 났는지 워크플로 로그만으로 알 수 있다 (플랜 리뷰 CEO 5A)"
    - "deploy.sh는 Cloud Monitoring 이메일 채널(ALERT_EMAIL)과 정책 3개([env] 5xx ratio > 5% / [env] Cloud SQL backup failed / [env] notify tick stale 24h — 마지막은 enabled:false)를 displayName으로 찾아 있으면 update 없으면 create 한다 (D-16, 19A)"
    - "scripts/bootstrap-gcp.sh --project --github-repo owner/name [--region] 은 사용자가 Cloud Shell에서 1회 실행하는 부트스트랩으로, 먼저 프로젝트 존재·결제 연결을 검사해 둘 중 하나라도 아니면 아무 리소스도 만들지 않고 exit 1로 멈추며(프로젝트는 사용자가 콘솔에서 만든다 — 스크립트는 프로젝트를 만들지 않는다, D-03), 통과하면 API 활성화·WIF 풀(github)/프로바이더(erp-repo)·SA(gha-deployer, erp-staging-runtime, erp-prod-runtime)·역할·VPC 프라이빗 서비스 접근·조직 정책 확인을 멱등하게 수행하고 GitHub 변수 3개 값을 출력한다; 다른 파일을 source하지 않는 단일 파일이라 비공개 리포를 클론하지 않고 파일 하나만 Cloud Shell에 붙여넣어 실행할 수 있다 (D-02, D-03, OV-3, 플랜 리뷰 CEO OV-9)"
    - "Cloud SQL 인스턴스는 HDD 10GB로 시작하되 스토리지 자동 증가를 켜고 상한 20GB를 둔다 — login_attempts·rate_limits는 공개 URL의 봇 트래픽으로 자라고 디스크가 차면 인스턴스가 읽기 전용이 되어 5xx로만 드러난다; 상한이 비용을 묶는다 (D-06, 플랜 리뷰 CEO OV-6)"
    - "Artifact Registry 저장소에는 정리 정책(최근 20버전 유지, 그 외 60일 지나면 삭제)이 deploy.sh로 붙는다 — SHA 태그 이미지(~300MB)가 main 푸시마다 쌓여 $30 상한을 잠식하지 않게 (OPS-02, 플랜 리뷰 CEO OV-8)"
    - ".github/workflows/deploy.yml 은 main push 또는 workflow_dispatch(target=staging)에서 ci.yml(workflow_call) 통과 뒤 WIF로 인증해 스테이징에 deploy.sh를 실행한다; production 잡은 workflow_dispatch(target=production, 입력 sha — 비우면 스테이징이 현재 서빙 중인 이미지 태그)로만 실행되며 push로는 절대 실행되지 않고, 가드 스텝이 지정 SHA의 이미지가 Artifact Registry에 있고 스테이징 서빙 리비전이 같은 태그를 쓰는지 확인해 아니면 exit 1로 아무것도 배포하지 않으며, 통과하면 같은 SHA로 deploy.sh --env prod 를 실행한다; GitHub Environments·required reviewers는 쓰지 않고(무료 플랜 비공개 저장소) 변수는 저장소 수준이며 빌드는 한 번이다 (D-02, D-05)"
    - "Cloud Run 서비스는 min-instances 0, max-instances 3, DB_POOL_MAX 5 로 배포된다 (OPS-02, D-06, 16A 시작값)"
    - "deploy.sh는 Cloud Run Job 3개(db-bootstrap·migrate·account) 모두에 APP_ENV·BETTER_AUTH_URL(서비스 URL)·BETTER_AUTH_SECRET(Secret Manager, 32자 이상)을 넣어 lib/env.ts의 비로컬 refine(01-01 계약)을 만족시키고, DB_ADMIN_PASSWORD는 db-bootstrap Job에만 넣는다 — 가짜 gcloud 테스트가 세 `run jobs deploy` 줄에서 단언한다 (OPS-01, 01-01·01-05 계약, T-1-27)"
    - "deploy.sh의 --domain 은 Phase 1에서 받기만 하는 자리다 — stderr에 Phase 2~3 연기 안내를 내고 도메인 매핑 gcloud 명령은 실행하지 않는다; 리전 지원 확인(A1)은 bootstrap-gcp.sh 몫이다 (D-15, Deferred Ideas, COVERAGE.md OPT-OUT)"
    - ".github/workflows/deploy.yml 의 deploy.sh 스텝은 shell: bash(-eo pipefail)로 실행되어 `| tee deploy.log` 뒤에서도 deploy.sh 의 exit 1/2 가 잡 실패로 전파되고, 실패한 스테이징 실행이 success로 보이지 않는다 — 프로덕션 가드는 GitHub 실행 결과가 아니라 스테이징이 실제로 서빙 중인 리비전의 이미지 태그를 기준으로 삼으므로 스모크에 실패한 0% 리비전은 승격 대상이 되지 않는다 (D-05, OPS-01)"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "인자 파싱·더티 트리 거부·인프라 ensure·이미지·Job·리비전·스모크·승격·경보 (bash, set -euo pipefail)"
      min_lines: 200
    - path: "scripts/rollback.sh"
      provides: "직전 리비전 트래픽 복구"
      contains: "update-traffic"
    - path: "scripts/bootstrap-gcp.sh"
      provides: "WIF·SA·역할·VPC·조직 정책 확인 (Cloud Shell 1회)"
      contains: "workload-identity-pools"
    - path: "infra/names.sh"
      provides: "리소스 이름 규칙 함수와 상수(MAX_INSTANCES=3, DB_POOL_MAX=5, DB_TIER=db-f1-micro)"
      contains: "db-f1-micro"
    - path: "infra/ar-cleanup-policy.json"
      provides: "Artifact Registry 정리 정책(keep 최근 20버전, delete 60일 초과)"
      contains: "mostRecentVersions"
    - path: ".github/workflows/deploy.yml"
      provides: "ci → staging(push 자동) / production(workflow_dispatch + 스테이징 SHA 가드) 배포 워크플로 (WIF, GitHub Environments 없음)"
      contains: "workload_identity_provider"
    - path: ".github/workflows/account.yml"
      provides: "workflow_dispatch로 Cloud Run Job erp-{env}-account 실행 (create/reset/unlock)"
      contains: "erp-"
    - path: "infra/monitoring/tick-stale.json.tpl"
      provides: "notify tick 24h 부재 정책 (enabled false)"
      contains: "\"enabled\": false"
  key_links:
    - from: "scripts/deploy.sh"
      to: "infra/names.sh"
      via: "source infra/names.sh"
      pattern: "names\\.sh"
    - from: "scripts/deploy.sh"
      to: "dist/cli/migrate-runner.mjs (01-05)"
      via: "gcloud run jobs deploy … --command=node --args=dist/cli/migrate-runner.mjs; execute --wait; exit 3 → PoolRuleViolation 메시지"
      pattern: "migrate-runner\\.mjs"
    - from: ".github/workflows/deploy.yml"
      to: ".github/workflows/ci.yml"
      via: "uses: ./.github/workflows/ci.yml"
      pattern: "uses: \\./\\.github/workflows/ci\\.yml"
    - from: ".github/workflows/deploy.yml"
      to: "scripts/deploy.sh"
      via: "run: bash scripts/deploy.sh --env … --project ${{ vars.GCP_PROJECT_ID }}"
      pattern: "scripts/deploy\\.sh"
    - from: "scripts/deploy.sh"
      to: "infra/monitoring/*.json.tpl"
      via: "sed 치환 후 gcloud alpha monitoring policies create/update --policy-from-file"
      pattern: "policy-from-file"
  prohibitions:
    - statement: "MUST NOT write the real GCP project ID, project number, or alert email into the repo or docs; only the variable names GCP_PROJECT_ID / GCP_PROJECT_NUMBER / ALERT_EMAIL (D-03)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT run drizzle-kit's push subcommand anywhere in deploy.sh or the workflows; schema changes reach the DB only through the migrate Job (Issue 4)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT assign a public IP to Cloud SQL instances (deploy.sh creates them with --no-assign-ip on the private network) (6A)"
      status: resolved
      verification: judgment
---

<objective>
배포 파이프라인의 "이미지 밖" 전부를 스크립트와 워크플로로 쓴다: 이름 규칙, `deploy.sh`(인프라 ensure → 이미지 → Job → 0% 리비전 → 스모크 → 100% → 경보), `rollback.sh`, `bootstrap-gcp.sh`(사용자 Cloud Shell 1회), 경보 정책 템플릿 3개, `deploy.yml`(WIF, staging 자동 → production은 사용자 수동 실행 + 스테이징 SHA 가드)·`account.yml`. 실제 GCP 없이 가짜 `gcloud`/`docker`/`curl` 심으로 분기·순서·거부 조건을 단위 테스트한다. 실제 실행은 01-07(스테이징)·01-08(프로덕션).

Purpose: ROADMAP 기준 1·6과 D-02~D-06·D-15·D-16을 코드로 고정한다. 사용자는 배포마다 손이 가지 않아야 한다(스테이징은 자동, 프로덕션은 GitHub "Run workflow" 버튼 하나 — D-05).
Output: 스크립트 3개, 이름 파일, 정책 템플릿 3개, 워크플로 2개, 가짜 심 3개 + 테스트 4개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-RESEARCH.md
@.planning/phases/01-deploy-skeleton-login/SKELETON.md
@.planning/phases/01-deploy-skeleton-login/01-05-PLAN.md
@.planning/phases/01-deploy-skeleton-login/01-04-SUMMARY.md
@docs/designs/plant8-erp-roadmap-eng-review-260917.md
@docs/designs/plant8-erp-roadmap-ceo-review-260917.md
@scripts/install_pkgs.sh
@.github/workflows/ci.yml

<interfaces>
<!-- 01-05 계약: 이미지 안 명령 — 서비스 `node server.js`(3000), Job migrate `node dist/cli/migrate-runner.mjs`(exit 0/1/3, env MAX_INSTANCES·DB_POOL_MAX), Job db-bootstrap `node dist/cli/db-bootstrap.mjs`(env DB_ADMIN_PASSWORD), Job account `node dist/cli/account-cli.mjs <sub> --email …`; 세 번들 모두 lib/env.ts를 import한다 -->
<!-- 01-04 계약: `.github/workflows/ci.yml`의 `workflow_call` 트리거 -->
<!-- 01-01 계약: 앱 env 키 — APP_ENV, CLOUD_SQL_CONNECTION_NAME, DB_IAM_USER, DB_NAME, DB_POOL_MAX, BETTER_AUTH_SECRET, BETTER_AUTH_URL, AUTH_PROVIDER, APP_DATA_KEY_v1, SMTP_HOST/USER/PASSWORD/FROM, GCP_PROJECT_ID, CLOUD_SQL_INSTANCE_ID, APP_GIT_SHA, APP_DEPLOYED_AT. 비로컬 refine: APP_ENV != local 이면 BETTER_AUTH_SECRET(32자 이상)·BETTER_AUTH_URL 필수 — lib/env.ts를 import하는 모든 프로세스(서비스 + Job 3개)에 적용되므로 deploy_jobs는 Job 3개 모두에 APP_ENV·BETTER_AUTH_URL·BETTER_AUTH_SECRET을 넣는다 -->

infra/names.sh (bash, `source`용; 함수는 env 인자를 받는다):
```bash
REGION_DEFAULT=asia-northeast3; AR_REPO=erp; DB_NAME=erp; DB_TIER=db-f1-micro; DB_VERSION=POSTGRES_16; DB_STORAGE_GB=10
DB_STORAGE_LIMIT_GB=20   # 자동 증가 상한 — 디스크 참(읽기 전용 전환)과 비용 폭주 사이의 타협(플랜 리뷰 CEO OV-6)
MAX_INSTANCES=3; DB_POOL_MAX=5; NETWORK=default; VPC_RANGE=google-managed-services-default
WIF_POOL=github; WIF_PROVIDER=erp-repo; DEPLOYER_SA=gha-deployer
svc_name()        { echo "erp-$1"; }              # erp-staging / erp-prod
sql_instance()    { echo "erp-$1-db"; }
runtime_sa()      { echo "erp-$1-runtime"; }      # 이메일: erp-$1-runtime@$PROJECT.iam.gserviceaccount.com, IAM DB user: erp-$1-runtime@$PROJECT.iam
job_name()        { echo "erp-$1-$2"; }           # $2 ∈ migrate | db-bootstrap | account
secret_name()     { echo "$1-$2"; }               # base-env: better-auth-secret-staging, app-data-key-v1-staging, db-admin-password-staging, smtp-host-staging …
image_path()      { echo "$3-docker.pkg.dev/$2/$AR_REPO/app:$1"; }   # sha project region
alert_channel()   { echo "ERP Alerts ($1)"; }
policy_5xx()      { echo "[$1] 5xx ratio > 5%"; }
policy_backup()   { echo "[$1] Cloud SQL backup failed"; }
policy_tick()     { echo "[$1] notify tick stale 24h"; }
tick_metric()     { echo "notify_tick_success_$1"; }
```
scripts/deploy.sh 인자/환경/종료 코드:
- 인자: `--env staging|prod`(필수) `--project ID`(필수) `--region R`(기본 REGION_DEFAULT) `--sha S`(기본 `git rev-parse HEAD`; `--env prod`이면 그 SHA의 이미지가 Artifact Registry에 이미 있어야 하고 없으면 빌드하지 않고 exit 1 — 프로덕션은 스테이징이 만든 이미지만 쓴다, D-05) `--domain D`(선택; Phase 1은 자리만 — 받으면 stderr에 연기 안내를 내고 무시, D-15) `--allow-dirty`(선택, CI 미사용) `--dry-run`(선택: 명령을 `+ …`로 출력만)
- 환경: `ALERT_EMAIL`(필수), 테스트 훅 `DEPLOY_FAKE_STATE`(선택; 가짜 심이 상태를 저장하는 디렉터리 — 스크립트는 읽지 않는다)
- 종료: 0 성공 / 1 단계 실패(migrate·스모크·gcloud 오류; 메시지에 단계 이름, migrate exit 3이면 "PoolRuleViolation", `--env prod`에 이미지가 없으면 "ProdImageMissing") / 2 인자·더티 트리 거부
- `SERVICE_URL`: `resolve_project_number` 직후 결정적 형식 `https://$(svc_name $ENV)-$PROJECT_NUMBER.$REGION.run.app`으로 계산해 Job env(`BETTER_AUTH_URL`)·서비스 env(`BETTER_AUTH_URL`)·스모크·stdout **전부**에 이 값 하나를 쓴다. `deploy_service`가 describe `status.url`을 읽어 호스트가 다르면(Cloud Run이 함께 주는 레거시 `*.a.run.app` 주소) stderr에 `note: describe url differs (<url>); canonical is <SERVICE_URL>` 한 줄만 남기고 SERVICE_URL은 바꾸지 않는다 — better-auth는 POST의 Origin이 `BETTER_AUTH_URL`과 다르면 403을 내므로 접속 주소는 BETTER_AUTH_URL과 같은 하나여야 한다(플랜 리뷰 Eng Issue 2). 결정적 형식이 실제로 응답하는지는 스모크가 증명하고 01-07이 기록한다
- stdout 마지막 줄: `SERVICE_URL=https://…`(결정적 값)
- 실패 출력: 모든 함수는 첫 줄에서 `STAGE=<함수명>`을 갱신하고, `trap 'echo "deploy failed at $STAGE" >&2' ERR`가 `set -e`로 중단될 때 단계 이름을 stderr 마지막 줄에 남긴다(명시적 exit 경로의 메시지 — PoolRuleViolation·SmokeFailed·ProdImageMissing·dirty — 는 그대로 유지)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: infra/names.sh + scripts/deploy.sh + scripts/rollback.sh — 가짜 gcloud/docker/curl 심으로 순서·분기·거부 테스트</name>
  <files>infra/names.sh, scripts/deploy.sh, scripts/rollback.sh, test/unit/deploy/fakebin/gcloud, test/unit/deploy/fakebin/docker, test/unit/deploy/fakebin/curl, test/unit/deploy/deploy-sh.test.ts, test/unit/deploy/rollback-sh.test.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 7 (Job 실행 `--wait`), §Pattern 8 (`--no-traffic --tag`, 스모크, `update-traffic`, rollback), §Pattern 10 (채널·정책 upsert), §Pattern 6 커넥션 공식, §Pitfall 3·5, §Assumptions A2·A6
    - docs/designs/plant8-erp-roadmap-eng-review-260917.md Diagram 2, Issue 4(SHA 태그·더티 트리 거부·rollback.sh), F2·F10
    - docs/designs/plant8-erp-roadmap-ceo-review-260917.md 16A, 19A, 20A, §5 배포 순서·6 롤백, Error & Rescue `deploy` 행(SmokeFailed → 트래픽 0% 유지, PoolRuleViolation → 배포 거부)
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-03(프로젝트 ID는 인자), D-04(환경 2개, 접미사 시크릿, 인자 유지), D-06(최소 사양·백업 켬), D-15(`--domain` 자리), D-16(경보 채널·정책 이름에 환경, tick은 Phase 7 전까지 비발화)
    - .planning/phases/01-deploy-skeleton-login/01-05-PLAN.md <interfaces> (컨테이너 실행 계약·exit 코드)
    - scripts/install_pkgs.sh (셸 관례: shebang, 주석, `ROOT=` — 단 deploy.sh는 `set -euo pipefail`로 실패 시 반드시 중단)
  </read_first>
  <behavior>
    - deploy-sh.test.ts(가짜 심: `PATH=test/unit/deploy/fakebin:$PATH`, 심은 호출된 인자를 `$DEPLOY_FAKE_LOG`에 한 줄씩 append하고 `$DEPLOY_FAKE_STATE` 디렉터리의 파일로 상태(서비스 존재·이미지 존재·리비전 목록·정책 존재)를 흉내 낸다; 테스트는 임시 git 리포(`git init` + 커밋)에 `scripts/`·`infra/`를 복사해 실행):
      1) 새 프로젝트(상태 비어 있음), `ALERT_EMAIL=ops@example.test`: exit 0; 로그 순서가 `services enable` < `artifacts repositories create` < `artifacts repositories set-cleanup-policies` < `sql instances create` < `sql databases create` < `sql users create` < `secrets create` < `docker build` < `docker push` < `run jobs deploy erp-staging-db-bootstrap` < `run jobs execute erp-staging-db-bootstrap` < `run jobs execute erp-staging-migrate` < `run deploy erp-staging` < `curl …/healthz` < `curl …/api/auth/sign-in/email` < `monitoring policies create`; `run deploy` 줄에 `--no-traffic`이 없고 `--min-instances=0`·`--max-instances=3`이 있으며 `--set-secrets`에 `BETTER_AUTH_SECRET=better-auth-secret-staging:latest`가 있다; `sql instances create` 줄에 `--tier=db-f1-micro`·`--no-assign-ip`·`--database-flags=cloudsql.iam_authentication=on`·`--backup`·`--storage-auto-increase`·`--storage-auto-increase-limit=20`이 있고 `--no-storage-auto-increase`는 없다(`--min-instances`·`--max-instances`는 `run deploy` 줄에만, `--no-assign-ip`·`--tier`·`--backup`은 `sql instances create` 줄에만 — action의 `deploy_service`·`ensure_sql_instance`와 같은 배치); `set-cleanup-policies` 줄에 `--policy=`와 `infra/ar-cleanup-policy.json`이 있다; `curl …/sign-in/email` 줄에 `-X POST`와 `Origin: https://erp-staging-`가 있다; 마지막 stdout 줄이 `SERVICE_URL=https://erp-staging-`로 시작
      2) 서비스·이미지가 이미 있는 상태: `docker build` 없음; `run deploy` 줄에 `--no-traffic`과 `--tag=rev-01234567`(sha 앞 8자); 순서가 `curl` < `monitoring policies update` < `services update-traffic erp-staging … --to-latest`(경보 upsert가 승격보다 앞); `monitoring policies update`(create 아님); 상태 파일 `describe-url=https://erp-staging-abc123-du.a.run.app`(레거시 형식)을 두어도 stdout 마지막 줄은 여전히 결정적 `SERVICE_URL=https://erp-staging-123456789012.asia-northeast3.run.app`이고 stderr에 `describe url differs`가 있다
      3) 더티 트리(임시 리포에 untracked 파일): exit 2, 로그 파일 비어 있음, stderr에 `dirty`
      4) migrate Job 실패(상태 파일 `fail-migrate=3`): 가짜 `gcloud run jobs execute`는 실제 gcloud처럼 컨테이너 exit 코드를 전파하지 않고 exit 1만 돌려주고, 가짜 `gcloud logging read`가 `"event":"deploy.pool_rule_violation"` 줄을 출력한다 → 스크립트는 로그 조회로 원인을 구분해 exit 1, stderr에 `PoolRuleViolation`, 로그에 `run deploy` 없음; 상태 파일 `fail-migrate=1`(로그에 pool_rule 없음)이면 stderr에 `migration failed`
      5) 기존 서비스에서 스모크 실패(상태 파일 `healthz=503`): exit 1, stderr에 `SmokeFailed`, 로그에 `update-traffic` 없음; 상태 파일 `signin=403`(healthz·login은 정상)이면 exit 1, stderr에 `SmokeFailed(origin)`와 `BETTER_AUTH_URL`, 로그에 `update-traffic` 없음
      5b) 어떤 gcloud 호출이든 상태 파일 `fail-gcloud=<하위 명령>`(예: `sql instances create`)로 실패시키면 exit 1이고 stderr 마지막 줄이 `deploy failed at <그 함수명>`(예: `ensure_sql_instance`)이다
      6) `--dry-run`: exit 0, 로그 비어 있음, stdout에 `+ gcloud run deploy`
      7) `--env qa`·`--project` 누락·`ALERT_EMAIL` 미설정: exit 2
      8) `--project other-proj --region us-central1`: 모든 gcloud 줄에 `--project=other-proj`, 이미지 경로가 `us-central1-docker.pkg.dev/other-proj/erp/app:`
      9) Job 환경 계약(시나리오 1의 로그에서): `run jobs deploy` 줄이 정확히 3개(erp-staging-db-bootstrap·erp-staging-migrate·erp-staging-account)이고 세 줄 모두 `--set-env-vars`에 `APP_ENV=staging`과 `BETTER_AUTH_URL=https://erp-staging-`가, `--set-secrets`에 `BETTER_AUTH_SECRET=better-auth-secret-staging:latest`가 있다(01-01 lib/env.ts 비로컬 refine); `DB_ADMIN_PASSWORD=db-admin-password-staging:latest`는 db-bootstrap 줄에만 있고 migrate·account 줄에는 없다(T-1-27); migrate 줄에만 `MAX_INSTANCES=3`·`DB_POOL_MAX=5`; account 줄에는 `--command=node,dist/cli/account-cli.mjs`가 있고 `--args`가 없다(execute의 `--args`가 대체해도 스크립트 경로가 살아남게 — Eng OV-1), db-bootstrap·migrate 줄은 `--command=node`와 `--args=dist/cli/<name>.mjs`
      10) `--domain erp.example.test`: exit 0, 로그에 `domain-mappings` 문자열이 없고 stderr에 `deferred`와 `Phase 2`가 있다(D-15 자리만)
      11) `--env prod --sha 0123456789abcdef0123456789abcdef01234567`에 상태 파일 `image-exists`가 없음: exit 1, stderr에 `ProdImageMissing`, 로그에 `docker build`·`sql instances create`·`run jobs execute`·`run deploy`가 전혀 없다(프로덕션은 빌드하지 않고 어떤 ensure 단계보다 먼저 멈춘다 — D-05 빌드 1회; 스테이징 서빙 SHA 일치는 deploy.yml 가드 스텝이 확인한다); 같은 인자에 `image-exists`가 있으면 `docker build` 없이 `run deploy erp-prod`까지 진행
    - rollback-sh.test.ts(가짜 gcloud 상태: `revisions`는 생성 시각 내림차순 이름 목록, `serving`은 `services describe` `status.traffic`에서 percent 100인 리비전 이름): 리비전 `[v2, v1]`·serving v2 → `update-traffic erp-staging --to-revisions=v1=100`, exit 0; 리비전 `[v3, v2, v1]`·serving v2(v3는 스모크 실패로 0% 잔존) → `--to-revisions=v1=100`(v3가 아니라 v1 — "직전에 만든 것"이 아니라 "지금 서빙 중인 것보다 오래된 최신"); 리비전 `[v2, v1]`·serving v1 → exit 1, stderr에 `no previous revision`; 리비전 1개 → exit 1, `no previous revision`; serving을 못 찾으면(traffic에 100% 항목 없음) exit 1, stderr에 `no serving revision`
  </behavior>
  <action>
    OPS-01·Issue 4·16A·19A·20A·D-03·D-04·D-06·D-15·D-16을 구현한다. bash, `set -euo pipefail`, 함수 단위로 나누고 모든 외부 명령은 `run()` 래퍼를 지나 `--dry-run`이면 `+ …`만 출력한다. 프로젝트 ID·리전·이메일은 인자/환경에서만 온다(리포에 실제 값 금지, D-03). 파일 상단에 `STAGE=init; trap 'echo "deploy failed at $STAGE" >&2' ERR`를 두고 각 함수 첫 줄에서 `STAGE=<함수명>`을 갱신한다(어느 gcloud가 죽었는지 워크플로 로그의 마지막 줄로 알 수 있게 — 명시적 exit 경로는 자기 메시지를 먼저 낸다).
    1. `infra/names.sh`를 <interfaces>대로. `infra/ar-cleanup-policy.json`: `gcloud artifacts repositories set-cleanup-policies --policy=` 형식 — `[{ "name": "keep-recent", "action": {"type": "Keep"}, "mostRecentVersions": {"keepCount": 20} }, { "name": "delete-old", "action": {"type": "Delete"}, "condition": {"olderThan": "5184000s"} }]`(keep 규칙이 delete보다 우선한다 — 최근 20버전은 60일이 지나도 남는다; 프로덕션이 오래된 SHA를 서빙해도 Cloud Run 리비전은 이미지 사본을 갖고 있어 삭제에 영향받지 않지만, 21버전 전의 SHA를 production 가드에 넣으면 "image not found"로 거부되므로 그때는 스테이징에 다시 올린다 — OPERATIONS.md 승격 절에 한 줄).
    2. `scripts/deploy.sh` 단계(함수 이름 그대로):
       - `parse_args`(<interfaces> 인자·검증; 잘못되면 usage + exit 2) → `require_clean_tree`(`git status --porcelain`이 비어 있지 않고 `--allow-dirty` 없으면 stderr "refusing to deploy from a dirty tree" exit 2 — gcloud 호출 전) → `source infra/names.sh` → `resolve_project_number`(`gcloud projects describe $PROJECT --format='value(projectNumber)'` → 이어서 `SERVICE_URL=https://$(svc_name $ENV)-$PROJECT_NUMBER.$REGION.run.app`을 결정적 형식으로 계산 — Job env와 서비스 env가 같은 값을 쓴다) → `require_prod_image`(`--env prod`일 때만, 어떤 ensure 단계보다 앞: `gcloud artifacts docker images describe $(image_path $SHA $PROJECT $REGION) --project=$PROJECT`가 실패하면 stderr "ProdImageMissing: image for <sha> not in Artifact Registry — production never builds; deploy this SHA to staging first (D-05)" exit 1)
       - `ensure_apis`: `gcloud services enable run.googleapis.com sqladmin.googleapis.com secretmanager.googleapis.com artifactregistry.googleapis.com monitoring.googleapis.com logging.googleapis.com compute.googleapis.com servicenetworking.googleapis.com --project=$PROJECT`
       - `ensure_ar_repo`: `gcloud artifacts repositories describe $AR_REPO --location=$REGION` || `create --repository-format=docker --location=$REGION`; 이어서 매번 `gcloud artifacts repositories set-cleanup-policies $AR_REPO --location=$REGION --project=$PROJECT --policy=infra/ar-cleanup-policy.json --no-dry-run`(멱등 — 같은 정책을 다시 적용)
       - `ensure_network`: `gcloud services vpc-peerings list --network=$NETWORK --service=servicenetworking.googleapis.com --project` 출력에 `$VPC_RANGE`가 없으면 stderr "private services access missing — run scripts/bootstrap-gcp.sh first" exit 1
       - `ensure_sql_instance`: describe || `gcloud sql instances create $(sql_instance $ENV) --project --region --database-version=$DB_VERSION --tier=$DB_TIER --edition=ENTERPRISE --storage-type=HDD --storage-size=$DB_STORAGE_GB --storage-auto-increase --storage-auto-increase-limit=$DB_STORAGE_LIMIT_GB --availability-type=ZONAL --backup --backup-start-time=18:00 --retained-backups-count=7 --no-assign-ip --network=projects/$PROJECT/global/networks/$NETWORK --database-flags=cloudsql.iam_authentication=on --deletion-protection` 뒤 `describe --format='value(state)'`가 RUNNABLE 될 때까지 대기(최대 15분); `CONN_NAME=$(describe --format='value(connectionName)')`
       - `ensure_sql_db_users`: `gcloud sql databases describe $DB_NAME --instance` || create; IAM 사용자 `$(runtime_sa $ENV)@$PROJECT.iam`이 `gcloud sql users list --instance --format='value(name)'`에 없으면 `gcloud sql users create "$(runtime_sa $ENV)@$PROJECT.iam" --instance --type=cloud_iam_service_account`; 관리 비밀번호: 시크릿 `$(secret_name db-admin-password $ENV)`에 버전이 없으면 `openssl rand -base64 32`로 만들어 `gcloud secrets versions add`하고 같은 값으로 `gcloud sql users set-password postgres --instance --password=…`(새로 만들 때만; 값은 stdout·로그에 절대 출력하지 않는다 — `set -x` 금지)
       - `ensure_secrets`: 각 시크릿(`better-auth-secret`·`app-data-key-v1`: 없으면 `openssl rand -base64 48`; `smtp-host`·`smtp-user`·`smtp-password`·`smtp-from`: 없으면 센티널 `__unset__`; `db-admin-password`는 위) `gcloud secrets describe` || `create --replication-policy=user-managed --locations=$REGION`, 버전 없으면 `versions add --data-file=-`; 런타임 SA에 `roles/secretmanager.secretAccessor` 바인딩(`add-iam-policy-binding`)
       - `build_and_push_image`: `IMAGE=$(image_path $SHA $PROJECT $REGION)`; `gcloud artifacts docker images describe $IMAGE` 성공이면 건너뜀(D-05 빌드 1회; prod는 `require_prod_image`가 존재를 이미 보장했으므로 항상 건너뛴다), 아니면 `gcloud auth configure-docker $REGION-docker.pkg.dev --quiet` → `docker build --build-arg GIT_SHA=$SHA -t $IMAGE .` → `docker push $IMAGE`
       - `deploy_jobs`: 세 번들(01-05) 모두 `lib/env.ts`를 import하므로 `APP_ENV != local`의 refine(BETTER_AUTH_SECRET 32자 이상·BETTER_AUTH_URL 필수 — 01-01 계약)을 Job 3개 전부가 만족해야 한다(빠지면 첫 `run_db_bootstrap`에서 env 파싱 실패로 배포가 멈춘다). 공통 플래그 `--image=$IMAGE --region --project --service-account=$(runtime_sa $ENV)@$PROJECT.iam.gserviceaccount.com --network=$NETWORK --subnet=$NETWORK --vpc-egress=private-ranges-only --set-cloudsql-instances=$CONN_NAME --max-retries=0 --task-timeout=15m --set-env-vars=APP_ENV=$ENV,BETTER_AUTH_URL=$SERVICE_URL,CLOUD_SQL_CONNECTION_NAME=$CONN_NAME,DB_IAM_USER=$(runtime_sa $ENV)@$PROJECT.iam,DB_NAME=$DB_NAME` + 공통 시크릿 `BETTER_AUTH_SECRET=$(secret_name better-auth-secret $ENV):latest`. 개별: `gcloud run jobs deploy $(job_name $ENV db-bootstrap) --command=node --args=dist/cli/db-bootstrap.mjs --set-secrets=BETTER_AUTH_SECRET=…:latest,DB_ADMIN_PASSWORD=$(secret_name db-admin-password $ENV):latest`(DB_ADMIN_PASSWORD는 이 Job에만 — T-1-27); `… $(job_name $ENV migrate) --command=node --args=dist/cli/migrate-runner.mjs --set-env-vars=<공통>,MAX_INSTANCES=$MAX_INSTANCES,DB_POOL_MAX=$DB_POOL_MAX --set-secrets=BETTER_AUTH_SECRET=…:latest`; `… $(job_name $ENV account) --command=node,dist/cli/account-cli.mjs --set-secrets=BETTER_AUTH_SECRET=…:latest` — account Job만 스크립트 경로를 `--args`가 아니라 `--command`의 둘째 항목에 둔다: account.yml이 `gcloud run jobs execute … --args="create,--email,…"`로 실행할 때 `--args`는 배포 시 args를 **대체**하므로, 배포 args에 스크립트 경로가 있으면 컨테이너가 `node create --email …`를 실행해 "Cannot find module 'create'"로 죽는다(플랜 리뷰 Eng OV-1). db-bootstrap·migrate는 execute 때 `--args`를 주지 않으므로 그대로 둔다. Job의 `BETTER_AUTH_URL`은 env 계약 충족용이라 결정적 URL로 충분하다(Job은 HTTP를 받지 않는다; 서비스 describe 결과와 달라도 Job은 갱신하지 않는다). 공통 플래그를 변수 하나(`JOB_COMMON`)로 두어 세 호출이 같은 env·secret 집합을 쓰게 한다
       - `run_db_bootstrap`: `gcloud run jobs execute $(job_name $ENV db-bootstrap) --region --project --wait` 실패 → stderr "db-bootstrap failed" exit 1
       - `run_migrate`: `gcloud run jobs execute $(job_name $ENV migrate) … --wait`; 실패 시(실제 gcloud는 컨테이너 exit 코드 3을 전파하지 않고 exit 1만 돌려준다 — 그래서 로그 조회가 주 경로다) `gcloud logging read 'resource.type="cloud_run_job" AND resource.labels.job_name="<job>" AND jsonPayload.event="deploy.pool_rule_violation"' --project --freshness=10m --limit=5`에 결과가 있으면 stderr "PoolRuleViolation: max_instances × pool exceeds max_connections − 5" exit 1, 없으면 "migration failed" exit 1. 가짜 심도 같은 모양(execute → exit 1, logging read → 위반 줄)으로 흉내 내어 실제 경로를 테스트한다(behavior 4)
       - `deploy_service`: `SERVICE_URL`은 `resolve_project_number`에서 계산한 결정적 값 — 배포 뒤 `describe --format='value(status.url)'`을 읽어 호스트가 다르면 stderr에 `note: describe url differs (<url>); canonical is <SERVICE_URL>` 한 줄만 남기고 SERVICE_URL·스모크·stdout은 결정적 값을 유지한다(둘 다 응답하지만 better-auth Origin 검사는 BETTER_AUTH_URL과 같은 주소에서만 통과한다); `EXISTS`=describe 성공 여부; 공통 플래그 `--image --region --project --platform=managed --service-account=… --min-instances=0 --max-instances=$MAX_INSTANCES --concurrency=80 --cpu=1 --memory=512Mi --port=3000 --network --subnet --vpc-egress=private-ranges-only --set-cloudsql-instances=$CONN_NAME --allow-unauthenticated --set-env-vars=APP_ENV=$ENV,APP_GIT_SHA=$SHA,APP_DEPLOYED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ),CLOUD_SQL_CONNECTION_NAME=…,DB_IAM_USER=…,DB_NAME=$DB_NAME,DB_POOL_MAX=$DB_POOL_MAX,BETTER_AUTH_URL=$SERVICE_URL,AUTH_PROVIDER=email,GCP_PROJECT_ID=$PROJECT,CLOUD_SQL_INSTANCE_ID=$(sql_instance $ENV) --set-secrets=BETTER_AUTH_SECRET=…:latest,APP_DATA_KEY_v1=…:latest,SMTP_HOST=…:latest,SMTP_USER=…:latest,SMTP_PASSWORD=…:latest,SMTP_FROM=…:latest`; EXISTS면 `--no-traffic --tag=rev-${SHA:0:8}` 추가. `--allow-unauthenticated`가 조직 정책으로 실패하면 stderr "org policy blocks unauthenticated ingress (iam.allowedPolicyMemberDomains) — ask the GCP org admin" exit 1
       - `smoke`: 대상 URL = EXISTS면 `https://rev-${SHA:0:8}---$(svc_name $ENV)-$PROJECT_NUMBER.$REGION.run.app`(describe `status.traffic` 항목의 tag URL로 재확인) 아니면 `$SERVICE_URL`; `curl -fsS --retry 6 --retry-delay 5 --retry-all-errors $URL/healthz`가 `"ok":true`를 포함하고 `curl -s -o /dev/null -w '%{http_code}' $URL/login`이 200 — 아니면 stderr "SmokeFailed: traffic left unchanged" exit 1(리비전은 남겨 조사 가능, 트래픽은 옮기지 않음); 셋째로 `curl -s -o /dev/null -w '%{http_code}' -X POST -H "Origin: $SERVICE_URL" -H 'content-type: application/json' -d '{"email":"smoke@example.invalid","password":"smoke-not-a-password"}' $URL/api/auth/sign-in/email`이 403이면 stderr "SmokeFailed(origin): BETTER_AUTH_URL does not match the served origin" exit 1, 그 외 4xx(401·400 — 가짜 자격 거부)면 통과, 5xx면 SmokeFailed — GET만으로는 잡히지 않는 "화면은 뜨는데 로그인만 안 됨"을 배포 시점에 잡는다(Origin 헤더는 태그 URL에서 시험할 때도 SERVICE_URL로 보낸다 — 검사 대상은 리비전 env의 BETTER_AUTH_URL이다; 가짜 이메일 한 건이 `login_attempts`에 남는 것은 허용)
       - `ensure_alerts`(스모크 **뒤, promote 앞** — 경보 upsert가 실패하면 트래픽을 옮기기 전에 멈춘다; 앱이 이미 100% 서빙 중인데 워크플로만 빨간 상태를 피한다, Eng OV-8): 채널 `gcloud beta monitoring channels list --project --filter="displayName='$(alert_channel $ENV)'" --format='value(name)'` 없으면 `create --display-name --type=email --channel-labels=email_address=$ALERT_EMAIL`; 로그 기반 지표 `gcloud logging metrics describe $(tick_metric $ENV)` || `create --description --log-filter='resource.type="cloud_run_revision" AND resource.labels.service_name="'$(svc_name $ENV)'" AND jsonPayload.event="notify.tick" AND jsonPayload.ok=true'`; 정책 3개: 템플릿 `infra/monitoring/*.json.tpl`의 토큰 `__ENV__ __CHANNEL__ __SERVICE__ __PROJECT__ __INSTANCE__ __METRIC__`을 `sed`로 치환해 임시 파일로 만들고 `gcloud alpha monitoring policies list --project --filter="displayName='<name>'" --format='value(name)'`가 있으면 `update <name> --policy-from-file` 없으면 `create --policy-from-file`
       - `promote`: EXISTS면 `gcloud run services update-traffic $(svc_name $ENV) --region --project --to-latest`
       - `map_domain`(자리만, D-15·Deferred Ideas "deploy.sh 인자 자리만"·COVERAGE.md OPT-OUT): `--domain`이 있으면 stderr에 `--domain <D> accepted but deferred: custom domain mapping is Phase 2~3 (D-15); region support is checked by scripts/bootstrap-gcp.sh` 한 줄만 내고 아무 gcloud 명령도 호출하지 않는다(exit 코드 영향 없음). 리전 지원 확인(A1)은 Task 2의 bootstrap-gcp.sh가 `list`로 수행한다
       - 마지막: `echo "SERVICE_URL=$SERVICE_URL"`
    3. `scripts/rollback.sh --env --project --region`: (1) `SERVING=$(gcloud run services describe $(svc_name $ENV) --region --project --format=json | jq -r '.status.traffic[] | select(.percent == 100) | .revisionName' | head -n1)` — 비어 있으면 stderr "no serving revision" exit 1; (2) `gcloud run revisions list --service=$(svc_name $ENV) --region --project --sort-by='~metadata.creationTimestamp' --format='value(metadata.name)'`(전체)에서 `SERVING` 다음 줄(= 서빙 중인 것보다 오래된 최신 리비전)을 `PREV`로 — 스모크에 실패해 0%로 남은 최신 리비전은 `SERVING`보다 앞에 있으므로 자연히 건너뛴다; 다음 줄이 없으면 stderr "no previous revision" exit 1 → `update-traffic --to-revisions=$PREV=100` → 출력. ("생성 순서 둘째"를 고르면 실패한 0% 리비전이 있을 때 현재 서빙 중인 리비전을 다시 고르는 no-op 롤백이 된다 — 플랜 리뷰 Eng Issue 3)
    4. 가짜 심 3개(bash, 실행 비트): 호출 인자를 `$DEPLOY_FAKE_LOG`에 append; `gcloud`는 하위 명령별로 `$DEPLOY_FAKE_STATE`의 파일(예: `service-exists`, `image-exists`, `revisions`, `serving`, `policy-exists`, `fail-migrate`, `fail-gcloud`, `describe-url`, `signin`)을 읽어 성공/실패·출력(`projects describe` → `123456789012` — 상태 파일 `project-missing`이 있으면 stderr 한 줄 + exit 1, `billing projects describe` → `True` — 상태 파일 `billing-disabled`가 있으면 `False`, `describe --format=value(state)` → `RUNNABLE`, `connectionName` → `test-proj:asia-northeast3:erp-staging-db`, `vpc-peerings list` → `google-managed-services-default`, `services describe --format=json` → `serving` 파일의 리비전을 percent 100으로 담은 `status.traffic` JSON(+ `describe-url` 파일이 있으면 그 값을 `status.url`로), `run jobs execute` → `fail-migrate` 파일이 있으면 exit 1, `logging read` → `fail-migrate`가 `3`이면 `{"event":"deploy.pool_rule_violation"}` 한 줄, `fail-gcloud` 파일의 하위 명령과 일치하면 exit 1)을 흉내 낸다; `docker`는 항상 성공; `curl`은 `$DEPLOY_FAKE_STATE/healthz`가 `503`이면 exit 22, `-X POST`가 있고 `$DEPLOY_FAKE_STATE/signin`이 있으면 그 값을(기본 `401`) `%{http_code}`로 출력, 아니면 `{"ok":true}` 또는 `200` 출력. 테스트에서 `git config`로 임시 리포에 커밋을 만들고 `spawnSync('bash', ['scripts/deploy.sh', …], { cwd: tmpRepo, env: { ...process.env, PATH, DEPLOY_FAKE_LOG, DEPLOY_FAKE_STATE, ALERT_EMAIL } })`.
    5. TDD: 테스트 먼저 → 스크립트 → 통과. `bash -n scripts/deploy.sh`·`bash -n scripts/rollback.sh` 구문 확인. `shellcheck`가 있으면 실행(없으면 생략, SUMMARY에 기록).
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && bash -n scripts/rollback.sh && pnpm vitest run --project unit test/unit/deploy/deploy-sh.test.ts test/unit/deploy/rollback-sh.test.ts</automated>
    <fails_when>non-zero exit, or a vitest summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <!-- planner-discipline-allow: domain-mappings -->
  <!-- deploy.sh는 도메인 매핑 명령을 호출하지 않지만(아래 negative grep), 같은 플랜의 Task 2 bootstrap-gcp.sh는 리전 지원 확인용 list 호출을 정당하게 포함한다 -->
  <acceptance_criteria>
    - `scripts/deploy.sh` 첫 줄이 `#!/usr/bin/env bash`(또는 `#!/bin/bash`)이고 `set -euo pipefail`, 함수 `require_clean_tree`·`run_migrate`·`deploy_service`·`smoke`·`promote`·`ensure_alerts`가 정의돼 있다
    - `grep -c -- '--no-assign-ip' scripts/deploy.sh` ≥ 1, `grep -c -- '--allow-unauthenticated' scripts/deploy.sh` ≥ 1, `grep -c -- '--min-instances=0' scripts/deploy.sh` ≥ 1, `grep -c 'set -x' scripts/deploy.sh`가 0
    - `grep -c -E '[0-9]{12}|@gmail\.com|@plant8' scripts/deploy.sh infra/names.sh scripts/rollback.sh`가 0 (실제 식별자 없음)
    - `grep -c -E 'drizzle-kit\s+push' scripts/deploy.sh .github/workflows/*.yml`가 파일마다 0
    - `grep -v '^#' scripts/deploy.sh | grep -c 'BETTER_AUTH_URL='` ≥ 2 (Job 공통 + 서비스), `grep -v '^#' scripts/deploy.sh | grep -c 'DB_ADMIN_PASSWORD='` ≥ 1 (db-bootstrap Job에만 — 배타성은 시나리오 9가 단언), `grep -v '^#' scripts/deploy.sh | grep -c 'domain-mappings'`가 0
    - `grep -v '^#' scripts/deploy.sh | grep -c 'ProdImageMissing'` ≥ 1 이고 함수 `require_prod_image`가 정의돼 있다(prod는 빌드하지 않는다, D-05)
    - `grep -c "trap '.*deploy failed at" scripts/deploy.sh` ≥ 1, `grep -c 'STAGE=' scripts/deploy.sh` ≥ 10; `grep -c -- '--storage-auto-increase-limit' scripts/deploy.sh` ≥ 1이고 `grep -c -- '--no-storage-auto-increase' scripts/deploy.sh`가 0; `grep -c 'set-cleanup-policies' scripts/deploy.sh` ≥ 1이고 `infra/ar-cleanup-policy.json`에 `keepCount`가 있다; `grep -c 'sign-in/email' scripts/deploy.sh` ≥ 1(POST 스모크); `grep -c 'status.traffic' scripts/rollback.sh` ≥ 1(서빙 리비전 기준)
    - 단위 테스트가 <behavior>의 시나리오 1~11(5b 포함)과 rollback 5개를 모두 단언하고 통과한다
  </acceptance_criteria>
  <done>deploy.sh·rollback.sh가 가짜 gcloud 위에서 순서·분기·거부 조건 전부를 통과하고, 다른 프로젝트 ID·리전으로도 같은 명령 시퀀스를 낸다는 것이 테스트로 증명됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bootstrap-gcp.sh + 경보 정책 템플릿 3개 + deploy.yml·account.yml 워크플로</name>
  <files>scripts/bootstrap-gcp.sh, infra/monitoring/5xx-ratio.json.tpl, infra/monitoring/backup-failed.json.tpl, infra/monitoring/tick-stale.json.tpl, .github/workflows/deploy.yml, .github/workflows/account.yml, test/unit/deploy/bootstrap-sh.test.ts, test/unit/deploy/workflows.test.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 9 (WIF 풀·프로바이더·SA·attribute-condition, deploy job의 `permissions`·`google-github-actions/auth`), §Pattern 10 (정책 필터 예), §Open Questions 3 (역할은 넓게 → 좁히기), §Assumptions A1·A3·A6
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-02(부트스트랩은 사용자 Cloud Shell 1회), D-03(프로젝트는 사용자가 콘솔에서 만든다 — 스크립트는 존재·결제 연결만 검사하고 절대 만들지 않는다), D-05(승격: staging 자동 → 사용자가 workflow_dispatch로 production 수동 실행, 입력 sha 기본값 = 스테이징 서빙 SHA, 가드 통과 시 같은 SHA; GitHub Environments·required reviewers 없음 — 무료 플랜 비공개 저장소, 변수·시크릿은 저장소 수준), D-11(운영 계정 발급은 Cloud Run Job), D-16(tick 경보는 Phase 7 전 비발화), Claude's Discretion(WIF 이름·최소 권한)
    - PROJECT.md §Constraints "회사 GCP 조직 정책(비인증 ingress·외부 링크 허용)은 Phase 1에서 확인"
    - .github/workflows/ci.yml (01-04 — `workflow_call`), infra/names.sh (Task 1)
    - test/unit/ci-guard.test.ts (01-04 — 워크플로 전체에 대한 push 하위 명령 부재 단언이 deploy.yml·account.yml에도 자동 적용된다)
  </read_first>
  <behavior>
    - bootstrap-sh.test.ts(가짜 gcloud 심 재사용): `bash scripts/bootstrap-gcp.sh --project test-proj --github-repo rrangjaa-eng/ERP_PLANT8_260917` → exit 0; 로그의 첫 세 gcloud 호출이 순서대로 `projects describe test-proj` < `billing projects describe test-proj` < `services enable`(사전 검사가 어떤 리소스 생성보다 앞); 로그에 `services enable`(iam·iamcredentials·sts·cloudresourcemanager·orgpolicy·compute·servicenetworking 포함) < `iam workload-identity-pools create github` < `providers create-oidc erp-repo`(`--attribute-condition` 에 `assertion.repository == 'rrangjaa-eng/ERP_PLANT8_260917'`, `--issuer-uri=https://token.actions.githubusercontent.com`) < `iam service-accounts create gha-deployer` < `service-accounts create erp-staging-runtime` < `create erp-prod-runtime` < `projects add-iam-policy-binding`(roles/run.admin, roles/cloudsql.admin, roles/secretmanager.admin, roles/artifactregistry.admin, roles/monitoring.editor, roles/logging.admin, roles/serviceusage.serviceUsageAdmin, roles/compute.networkAdmin 각각 gha-deployer) < `service-accounts add-iam-policy-binding …workloadIdentityUser` < `compute addresses create google-managed-services-default` < `services vpc-peerings connect` < `org-policies describe`; 런타임 SA 역할 `roles/cloudsql.client`·`roles/cloudsql.instanceUser`·`roles/cloudsql.viewer`·`roles/logging.logWriter`·`roles/monitoring.metricWriter`가 두 환경 모두에 바인딩; stdout 마지막에 `GCP_PROJECT_ID=test-proj`, `GCP_PROJECT_NUMBER=123456789012`, `GCP_REGION=asia-northeast3`; `--github-repo` 누락 → exit 2; 두 번째 실행(상태: 이미 존재)도 exit 0; 상태 파일 `project-missing`이 있으면 exit 1, stderr에 `not found`와 `D-03`, 로그에 `services enable`·`create` 줄 없음(아무것도 만들지 않음); 상태 파일 `billing-disabled`가 있으면 exit 1, stderr에 `billing`, 로그에 `services enable` 없음
    - workflows.test.ts: `deploy.yml`에 `on:` 아래 `push:` — `branches: [main]`와 `paths-ignore`(`.planning/**`·`docs/**`) — 와 `workflow_dispatch`(`inputs.target` choice options `[staging, production]` 기본 `staging`; `inputs.sha` string 기본 빈 문자열), `permissions: id-token: write`, `uses: ./.github/workflows/ci.yml`, `google-github-actions/auth@v3`, `workload_identity_provider: projects/${{ vars.GCP_PROJECT_NUMBER }}/locations/global/workloadIdentityPools/github/providers/erp-repo`, `service_account: gha-deployer@${{ vars.GCP_PROJECT_ID }}.iam.gserviceaccount.com`; 잡 `staging`은 `needs: ci`이고 `if:`에 `github.event_name == 'push'`와 `inputs.target == 'staging'`이 있다; 잡 `production`은 `if:`에 `github.event_name == 'workflow_dispatch'`와 `inputs.target == 'production'`이 있고 `needs:`가 없으며 그 `if:` 줄에 `push` 문자열이 없다(push로는 실행 불가 — D-05); production 잡에 `id: guard` 스텝이 있고 그 `run:`에 `run services describe`, `run revisions describe`, `artifacts docker images describe`, `svc_name staging`, `git rev-parse --verify`, `exit 1`, `sha=`(`$GITHUB_OUTPUT`)가 있다; 둘 다 `bash scripts/deploy.sh --env` 실행, staging 줄에 `--sha "${{ github.sha }}"`, production 줄에 `--env prod`와 `--sha "${{ steps.guard.outputs.sha }}"`, `ALERT_EMAIL: ${{ vars.ALERT_EMAIL }}`; 세 워크플로 파일(`deploy.yml`·`account.yml`·`ci.yml`) 어디에도 `environment:` 키가 없다(주석 제외, 정규식 `^\s*environment:` — GitHub Environments 미사용, D-05); deploy.sh를 실행하는 두 스텝과 가드 스텝 모두 `shell: bash`가 있다(GitHub 기본 `bash -e {0}`에는 pipefail이 없어 `| tee`가 deploy.sh의 exit 1/2를 가린다 — `shell: bash`는 `-eo pipefail`); `setup-gcloud` 스텝에 `install_components: alpha,beta`(deploy.sh가 `gcloud alpha monitoring policies`·`gcloud beta monitoring channels`를 쓴다) — production 잡에도 같은 setup-gcloud 스텝; `account.yml`에 `workflow_dispatch` inputs `env`(staging|production) `action`(create|reset|unlock) `email` `name` `admin`, `permissions:` 아래 `id-token: write`와 `contents: read`(deploy.yml과 동일), `gcloud run jobs execute erp-` + `--args`, `--args` 조립 줄에 `--email,`(플래그 뒤 쉼표 — 플래그와 값이 별개 항목인 01-02 `parseArgs` 규약)와 `--name,`·`,--admin`이 있고 등호 결합 형태(`--email=`)는 없다, `gcloud logging read`가 `sleep`과 반복(`for`/`until`) 안에 있고 `temporary password` 문자열과 `do NOT re-run reset` 안내가 있다; 두 파일 모두 실제 프로젝트 ID 형태(12자리 숫자, 하드코딩 이메일) 없음; bootstrap-sh.test.ts가 `scripts/bootstrap-gcp.sh`에 `source`·`names.sh`가 없고 그 안의 `WIF_POOL`·`WIF_PROVIDER`·`DEPLOYER_SA`·`REGION_DEFAULT`·`VPC_RANGE`·`NETWORK` 값이 `infra/names.sh`의 값과 같음을 단언한다; 로그에 `org-policies describe iam.workloadIdentityPoolProviders`가 `workload-identity-pools create`보다 앞에 있다
  </behavior>
  <action>
    D-02·D-05·D-11·D-16·OV-3·19A를 구현한다.
    1. `scripts/bootstrap-gcp.sh`(사용자가 Cloud Shell에서 Owner로 1회 실행; 멱등; `set -euo pipefail`; `--project`·`--github-repo owner/name` 필수, `--region` 기본; **단일 파일** — `infra/names.sh`를 source하지 않고 필요한 이름 상수(`WIF_POOL`·`WIF_PROVIDER`·`DEPLOYER_SA`·런타임 SA 접두어 `erp-{env}-runtime`·`REGION_DEFAULT`·`VPC_RANGE`·`NETWORK`)를 파일 안에 복제한다 — 비공개 리포는 Cloud Shell에서 clone하려면 GitHub 토큰이 필요한데, 파일 하나를 붙여넣어 실행할 수 있으면 비개발자 사용자의 마찰이 없다(CEO OV-9); 값이 `infra/names.sh`와 같음을 `bootstrap-sh.test.ts`가 두 파일을 읽어 단언한다(DRY 예외의 근거는 파일 상단 주석에)): (0) 사전 검사 — fail fast(D-03: 프로젝트는 사용자가 콘솔에서 만든다, 이 스크립트는 프로젝트를 만들지 않는다): `gcloud projects describe "$PROJECT" --format='value(projectNumber)'`가 실패하면 stderr `project <ID> not found or no access — create it in the console under the company organization and link billing first (D-03); this script does not create projects` 후 exit 1; 이어서 `gcloud billing projects describe "$PROJECT" --format='value(billingEnabled)'`의 출력을 소문자로 비교해 `true`가 아니면 stderr `billing is not enabled on <ID> — link the company billing account in the console (D-03)` 후 exit 1. 두 검사는 `services enable`보다 앞에 두어 어떤 리소스도 만들기 전에 끝난다(PROJECT_NUMBER는 여기서 얻어 (e)와 (h)에 재사용) (a) `gcloud services enable` iam·iamcredentials·sts·cloudresourcemanager·orgpolicy·compute·servicenetworking·run·sqladmin·secretmanager·artifactregistry·monitoring·logging (b) WIF: `workload-identity-pools describe github --location=global` || create; `providers describe erp-repo` || `create-oidc erp-repo --location=global --workload-identity-pool=github --display-name="PLANT8 ERP repo" --attribute-mapping="google.subject=assertion.sub,attribute.actor=assertion.actor,attribute.repository=assertion.repository,attribute.repository_owner=assertion.repository_owner" --attribute-condition="assertion.repository == '$GITHUB_REPO'" --issuer-uri="https://token.actions.githubusercontent.com"` (c) SA 셋 describe || create (`gha-deployer`, `erp-staging-runtime`, `erp-prod-runtime`) (d) 배포자 프로젝트 역할(넓게 시작 — RESEARCH Open Question 3; 01-08 문서 태스크가 실제 사용 권한으로 좁히는 절차를 적는다): run.admin, cloudsql.admin, secretmanager.admin, artifactregistry.admin, monitoring.editor, logging.admin, serviceusage.serviceUsageAdmin, compute.networkAdmin; 런타임 SA 둘에 `iam.serviceAccountUser`(배포자가 사용); 런타임 SA 역할: cloudsql.client, cloudsql.instanceUser, cloudsql.viewer(D-18·A3), logging.logWriter, monitoring.metricWriter (e) WIF 바인딩: `gha-deployer`에 `roles/iam.workloadIdentityUser` member `principalSet://iam.googleapis.com/projects/$PROJECT_NUMBER/locations/global/workloadIdentityPools/github/attribute.repository/$GITHUB_REPO` (f) VPC: `compute networks describe default` 없으면 stderr 안내 후 `create default --subnet-mode=auto`; `compute addresses describe google-managed-services-default --global` || `create --global --purpose=VPC_PEERING --prefix-length=16 --network=default`; `services vpc-peerings list`에 없으면 `connect --service=servicenetworking.googleapis.com --ranges=google-managed-services-default --network=default` (g) 조직 정책 확인(실패해도 계속, 결과 출력): `gcloud org-policies describe iam.allowedPolicyMemberDomains --project --effective`, `run.allowedIngress`, `compute.restrictVpcPeering`, `iam.workloadIdentityPoolProviders`(외부 IdP 제한 — 걸려 있으면 (b)의 WIF 프로바이더 생성이 막히므로 (b) **앞**에서 확인해 막혀 있으면 어떤 리소스도 만들지 않고 "org policy iam.workloadIdentityPoolProviders blocks GitHub OIDC — ask the GCP org admin (예외 요청 목록에 추가)"로 exit 1; CEO OV 저확신 항목); 도메인 매핑 리전 확인(A1·D-15): `gcloud beta run domain-mappings list --region=$REGION --project`의 성공/오류 메시지를 그대로 출력 (h) 마지막 stdout: `GCP_PROJECT_ID=…`, `GCP_PROJECT_NUMBER=…`, `GCP_REGION=…` + GitHub 설정 안내(저장소 수준 Settings → Secrets and variables → Actions → Variables에 `GCP_PROJECT_ID`·`GCP_PROJECT_NUMBER`·`GCP_REGION`·`ALERT_EMAIL` 4개; Secrets 탭은 비워 둔다 — WIF라 키 파일이 없다; GitHub Environments는 만들지 않는다 — 무료 플랜 비공개 저장소, D-05).
    2. 정책 템플릿(JSON, gcloud `--policy-from-file` 형식; 토큰 `__ENV__ __CHANNEL__ __SERVICE__ __PROJECT__ __INSTANCE__ __METRIC__`): `5xx-ratio.json.tpl` — displayName `[__ENV__] 5xx ratio > 5%`, `conditionThreshold` filter `resource.type="cloud_run_revision" AND resource.labels.service_name="__SERVICE__" AND metric.type="run.googleapis.com/request_count" AND metric.labels.response_code_class="5xx"`, `denominatorFilter` 같은 필터에서 code_class 조건 제거, aggregations `alignmentPeriod 300s, perSeriesAligner ALIGN_RATE, crossSeriesReducer REDUCE_SUM`, **`denominatorAggregations`도 같은 값으로**(비율 조건은 분모 집계가 없으면 API가 거부한다 — Eng OV-8), `comparison COMPARISON_GT`, `thresholdValue 0.05`, `duration 300s`, `notificationChannels ["__CHANNEL__"]`, `combiner OR`, `enabled true`; `backup-failed.json.tpl` — displayName `[__ENV__] Cloud SQL backup failed`, `conditionMatchedLog` filter `resource.type="cloudsql_database" AND resource.labels.database_id="__PROJECT__:__INSTANCE__" AND (protoPayload.methodName=~"backup" OR jsonPayload.message=~"backup") AND severity>=ERROR`, `alertStrategy.notificationRateLimit.period 3600s`, `enabled true`([ASSUMED] 필터 — 01-07에서 실제 로그 항목으로 재확인); `tick-stale.json.tpl` — displayName `[__ENV__] notify tick stale 24h`, `conditionAbsent` filter `metric.type="logging.googleapis.com/user/__METRIC__" AND resource.type="cloud_run_revision"`, `duration 86400s`, `"enabled": false`(Phase 7이 tick을 만들며 true로 바꾼다 — D-16).
    3. `.github/workflows/deploy.yml`(D-05 — GitHub Environments 키는 어디에도 두지 않는다: 무료 플랜 비공개 저장소라 기능 자체가 없고 쓰지도 않는다; 변수는 전부 저장소 수준 `vars.*`): `name: deploy`; `on: push: { branches: [main], paths-ignore: ['.planning/**', 'docs/**'] }`(GSD 상태·문서만 바뀐 main 커밋마다 새 SHA로 풀빌드+스테이징 배포 ~10분을 쓰지 않는다 — 무료 플랜 Actions 2,000분/월, CEO 9A; 코드가 한 줄이라도 섞이면 배포된다); `workflow_dispatch: inputs: target: { type: choice, options: [staging, production], default: staging, description: "배포 대상" }, sha: { type: string, default: "", description: "production 전용 — 배포할 git SHA(7~40자 hex); 비우면 스테이징이 현재 서빙 중인 SHA" }`; `concurrency: { group: deploy, cancel-in-progress: false }`(스테이징 자동 배포와 프로덕션 수동 실행이 직렬화되어 가드가 읽은 스테이징 상태가 배포 도중 바뀌지 않는다); `permissions: { contents: read, id-token: write }`; 잡 `ci`(`if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.target == 'staging')`, `uses: ./.github/workflows/ci.yml`); 잡 `staging`(`needs: ci`, `ci`와 같은 `if:`, `env: { ALERT_EMAIL: ${{ vars.ALERT_EMAIL }} }`, 스텝: checkout(`fetch-depth: 0`) → `google-github-actions/auth@v3`(위 inputs) → `google-github-actions/setup-gcloud@v3`(`with: install_components: alpha,beta` — deploy.sh의 `alpha monitoring policies`·`beta monitoring channels`가 러너에서 비대화형 컴포넌트 설치를 유발하지 않도록) → `id: deploy` `shell: bash` `run: bash scripts/deploy.sh --env staging --project "${{ vars.GCP_PROJECT_ID }}" --region "${{ vars.GCP_REGION }}" --sha "${{ github.sha }}" | tee deploy.log; URL=$(grep '^SERVICE_URL=' deploy.log | cut -d= -f2-); echo "url=$URL" >> "$GITHUB_OUTPUT"; echo "staging: $URL (sha ${{ github.sha }})" >> "$GITHUB_STEP_SUMMARY"`(`shell: bash`는 `bash --noprofile --norc -eo pipefail {0}`이라 `| tee` 뒤에서도 deploy.sh의 exit 1/2가 스텝 실패가 된다 — 기본 셸은 pipefail이 없어 실패한 스테이징이 success로 보이는 문제를 막는다; 배포 URL은 Environment url 대신 스텝 요약에 적는다); 잡 `production`(`if: github.event_name == 'workflow_dispatch' && inputs.target == 'production'` — `needs` 없음, push로는 절대 실행되지 않는다; 같은 `env:`; 스텝: checkout(`fetch-depth: 0` — 모든 브랜치·태그를 가져와 입력 SHA를 검증한다) → auth → setup-gcloud(`install_components: alpha,beta`) → 가드 스텝 `id: guard` `shell: bash` `env: { SHA_INPUT: ${{ inputs.sha }}, PROJECT: ${{ vars.GCP_PROJECT_ID }}, REGION: ${{ vars.GCP_REGION }} }`(입력은 env로만 셸에 들어온다 — T-1-32와 같은 규칙) `run:` (1) `source infra/names.sh`; (2) 스테이징 서빙 리비전 `REV=$(gcloud run services describe "$(svc_name staging)" --region "$REGION" --project "$PROJECT" --format=json | jq -r '.status.traffic[] | select(.percent == 100) | .revisionName' | head -n1)`(ubuntu-latest 러너에 jq 기본 포함; `head -n1`은 100% 항목이 둘 이상 나오는 예외 형식에 대한 방어 — [ASSUMED] 절 참조; 비어 있으면 "staging is not serving any revision — deploy staging first (D-05)" exit 1) → `STAGING_IMAGE=$(gcloud run revisions describe "$REV" --region "$REGION" --project "$PROJECT" --format='value(spec.containers[0].image)')`, `STAGING_SHA=${STAGING_IMAGE##*:}`; (3) `SHA_INPUT`이 비어 있으면 `SHA=$STAGING_SHA`, 아니면 `[[ "$SHA_INPUT" =~ ^[0-9a-f]{7,40}$ ]]`가 아니면 "sha must be 7-40 hex chars" exit 1 후 `SHA=$(git rev-parse --verify "$SHA_INPUT^{commit}")`(짧은 SHA를 전체로 확장하고 리포에 없는 커밋은 거부); (4) `gcloud artifacts docker images describe "$(image_path "$SHA" "$PROJECT" "$REGION")" --project "$PROJECT"` 실패 → "image for $SHA not found in Artifact Registry — deploy it to staging first (D-05)" exit 1; (5) `[ "$SHA" = "$STAGING_SHA" ]`가 아니면 "staging is serving $STAGING_SHA, not $SHA — deploy $SHA to staging first (D-05)" exit 1; (6) `echo "sha=$SHA" >> "$GITHUB_OUTPUT"`와 `echo "promoting $SHA (staging serving $STAGING_SHA)" >> "$GITHUB_STEP_SUMMARY"` → 배포 스텝 `id: deploy` `shell: bash` `run: bash scripts/deploy.sh --env prod --project "${{ vars.GCP_PROJECT_ID }}" --region "${{ vars.GCP_REGION }}" --sha "${{ steps.guard.outputs.sha }}" | tee deploy.log; …`(staging과 같은 URL 출력·요약; 같은 SHA 이미지라 deploy.sh는 빌드하지 않는다 — Task 1 `require_prod_image`가 이중 방어). 가드가 exit 1이면 배포 스텝은 실행되지 않고(`if: success()` 기본) 아무것도 배포되지 않는다; 실행 기록(actor·SHA·시각·가드 출력)은 GitHub Actions 로그가 남긴다(D-05). `secrets: inherit` 불필요(키 파일 없음).
    4. `.github/workflows/account.yml`: `workflow_dispatch` inputs `env`(choice staging|production), `action`(choice create|reset|unlock), `email`(string), `name`(string, create만), `admin`(boolean, 기본 false); GitHub Environments 키 없음(D-05 — production 실행에도 승인 단계가 없다; 실행 권한은 비공개 저장소의 write 협업자(본인 1인)이고 실행 기록은 Actions 로그, T-1-45); `permissions: { contents: read, id-token: write }`(deploy.yml과 같음 — WIF OIDC 토큰에 필요한 최소 권한); WIF 인증 → `ENV_SHORT`(production→prod) → 인자 목록을 셸에서 조립: `ARGS="$ACTION,--email,$EMAIL"`, action이 create면 `ARGS="$ARGS,--name,$NAME"`, admin이 true면 `ARGS="$ARGS,--admin"` — 플래그와 값은 항상 별개 항목(쉼표 구분)이라 Job의 argv가 로컬 `pnpm account:create --email … --name … [--admin]`과 동일하고 01-02 `parseArgs`가 그대로 받는다(등호 결합 형태는 쓰지 않는다; 값에 쉼표가 들어오면 워크플로가 exit 1로 거부) → `gcloud run jobs execute erp-$ENV_SHORT-account --region --project --args="$ARGS" --wait`(배포된 Job의 `--command`가 `node,dist/cli/account-cli.mjs`라 이 `--args`가 그대로 argv가 된다 — Eng OV-1) → `gcloud logging read 'resource.type="cloud_run_job" AND resource.labels.job_name="erp-'$ENV_SHORT'-account"' --project --freshness=15m --limit=30 --format='value(textPayload)'`를 **10초 간격 최대 12회** 반복해 출력에 `temporary password`(create·reset) 또는 `unlocked`(unlock)가 나타날 때까지 기다린다 — Cloud Logging 인입 지연(수초~1분)으로 첫 조회가 비어 있는 경우가 흔하고, 빈 출력을 본 관리자가 `reset`을 다시 돌리면 전 세션 만료·새 비밀번호가 반복된다(CEO OV-4); 끝내 안 나오면 stderr "account job finished but its output is not in Cloud Logging yet — do NOT re-run reset; open Logging → job_name=erp-<env>-account in a minute" + exit 1. 나온 출력은 그대로 로그에 남긴다(임시 비밀번호는 여기 한 번 보인다 — 워크플로 요약에 "즉시 로그인해 변경" 경고 문구 `echo`). 입력은 따옴표로 감싸 인젝션 방지(`${{ }}`를 env로 넘겨 셸에서 `"$EMAIL"`로 참조).
    5. 테스트 먼저 → 파일 → 통과. `bash -n scripts/bootstrap-gcp.sh`.
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-gcp.sh && pnpm vitest run --project unit test/unit/deploy/bootstrap-sh.test.ts test/unit/deploy/workflows.test.ts test/unit/ci-guard.test.ts</automated>
    <fails_when>non-zero exit, or a vitest summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/bootstrap-gcp.sh`에 `workload-identity-pools`, `create-oidc`, `attribute-condition`, `roles/iam.workloadIdentityUser`, `vpc-peerings connect`, `org-policies describe`, `iam.workloadIdentityPoolProviders`, `domain-mappings list`가 있고 `GCP_PROJECT_NUMBER=` 출력이 있으며 `grep -c 'names.sh' scripts/bootstrap-gcp.sh`가 0(단일 파일)
    - `infra/monitoring/5xx-ratio.json.tpl`에 `denominatorAggregations`가 있다; `.github/workflows/account.yml`에 `sleep`이 있고 `do NOT re-run reset`이 있다
    - `grep -n -E 'projects describe|services enable' scripts/bootstrap-gcp.sh`의 첫 두 매치가 `projects describe`(일반·billing)이고 셋째가 `services enable`이다(사전 검사가 리소스 생성보다 앞); `grep -v '^#' scripts/bootstrap-gcp.sh | grep -c -E 'projects create'`가 0 (스크립트는 프로젝트를 만들지 않는다, D-03)
    - `infra/monitoring/tick-stale.json.tpl`에 `"enabled": false`, `5xx-ratio.json.tpl`에 `denominatorFilter`와 `0.05`, `backup-failed.json.tpl`에 `conditionMatchedLog`가 있다
    - `.github/workflows/deploy.yml`에 `--env prod`, `id: guard`, `steps.guard.outputs.sha`, `inputs.target == 'production'`, `artifacts docker images describe`, `run revisions describe`가 있고, `workflow_identity_provider`가 아닌 정확한 키 `workload_identity_provider`가 있다
    - `for f in .github/workflows/deploy.yml .github/workflows/account.yml .github/workflows/ci.yml; do grep -v '^\s*#' "$f" | grep -c -E '^\s*environment:'; done`이 세 파일 모두 0 (GitHub Environments 미사용, D-05); `grep -v '^\s*#' .github/workflows/deploy.yml | grep -c 'needs: staging'`이 0 (production은 push 체인에 매달리지 않는다)
    - `grep -c 'shell: bash' .github/workflows/deploy.yml` ≥ 3 (deploy.sh 스텝 둘 + 가드 스텝), `grep -c 'install_components: alpha,beta' .github/workflows/deploy.yml` ≥ 2
    - `.github/workflows/account.yml`에 `erp-`·`--args`·`--email,`·`gcloud logging read`가 있다(01-02 `parseArgs`와 같은 인자 규약: 플래그와 값은 별개 항목 `<action>,--email,<v>[,--name,<v>][,--admin]`); `grep -c 'id-token: write' .github/workflows/deploy.yml .github/workflows/account.yml`와 `grep -c 'contents: read' .github/workflows/deploy.yml .github/workflows/account.yml`가 파일마다 ≥ 1
    - `grep -c -E '[0-9]{12}' scripts/bootstrap-gcp.sh .github/workflows/deploy.yml .github/workflows/account.yml infra/monitoring/*.tpl`가 파일마다 0
    - 위 verify 명령 통과(ci-guard가 새 워크플로 2개에도 push 하위 명령 부재를 확인)
  </acceptance_criteria>
  <done>사용자가 Cloud Shell에서 한 번 돌릴 부트스트랩, 경보 정책 3개 정의, 그리고 "Run workflow" 버튼 하나로 스테이징이 서빙 중인 SHA만 프로덕션에 올리는(가드 포함, GitHub Environments 없음) 워크플로가 정의되고 가짜 심·텍스트 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions(OIDC 토큰) → GCP WIF → `gha-deployer` | 키 파일 없는 배포 인증 경계 |
| deploy.sh → GCP 리소스 생성/변경 | 인프라 변경 권한 |
| 인터넷 → Cloud Run(`--allow-unauthenticated`) | 앱 공개 경계(인증은 앱 안) |
| 워크플로 입력(account.yml) → 셸 | 사용자 입력 인젝션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-29 | Spoofing | 다른 리포/포크가 WIF로 배포자 SA를 빌림 | high | mitigate | 프로바이더 `attribute-condition`이 정확한 `assertion.repository == 'owner/name'`, `workloadIdentityUser` 바인딩도 `attribute.repository/<repo>`로 한정, 테스트 단언 |
| T-1-30 | Elevation of Privilege | 배포자 SA 과다 권한(A6) | medium | accept | 솔로 프로젝트에서 인프라를 스크립트가 만들므로 admin 계열이 필요; 01-08 문서에 Policy Analyzer로 좁히는 절차 기록(RESEARCH Open Q3) |
| T-1-31 | Tampering | 더티 트리·검증 안 된 코드 배포 | high | mitigate | `require_clean_tree` exit 2, 스테이징 잡은 `ci` 잡 성공을 `needs`로 요구, 이미지 태그 = git SHA; production 잡은 `workflow_dispatch`로만 실행되고 가드 스텝이 이미지 존재 + 스테이징 서빙 태그 일치를 확인하며 deploy.sh `require_prod_image`가 prod 빌드를 거부한다(D-05) |
| T-1-32 | Tampering | account.yml 입력으로 셸 인젝션 | medium | mitigate | `${{ inputs.* }}`를 `env:`로 넘겨 셸에서 따옴표 변수로만 사용, `--args`는 쉼표 분리 인자(플래그·값 별개 항목, 값의 쉼표는 거부) |
| T-1-33 | Information Disclosure | 워크플로 로그의 임시 비밀번호 | medium | accept | 비공개 리포·협업자 1인, 로그 보존 90일, D-08 배너와 문서로 즉시 변경 유도; Phase 3 관리 화면이 CLI를 대체 |
| T-1-34 | Information Disclosure | `set -x`·echo로 시크릿 값 노출 | high | mitigate | deploy.sh에 `set -x` 금지(acceptance grep), 시크릿 값은 `--data-file=-` 파이프로만 전달 |
| T-1-35 | Denial of Service | 스모크 실패 리비전에 트래픽 | high | mitigate | 기존 서비스는 `--no-traffic` 리비전 + 스모크 통과 뒤에만 `--to-latest`, 실패 시 exit 1·트래픽 불변, `rollback.sh` |
| T-1-36 | Elevation of Privilege | 조직 정책이 `allUsers`를 막는데 우회 시도 | low | accept | 우회하지 않고 명확한 오류로 중단, 조직 관리자에게 요청(PROJECT.md 제약) |
| T-1-45 | Elevation of Privilege | 승인 게이트 없는 production 수동 실행(deploy.yml target=production·account.yml env=production) | high | mitigate | GitHub Environments·required reviewers 없음(무료 플랜 비공개 저장소, D-05) 대신: workflow_dispatch 실행 권한은 비공개 저장소의 write 협업자뿐(본인 1인 — 01-07 Task 2가 협업자 목록으로 확인) + 가드 스텝이 스테이징 서빙 SHA만 통과 + WIF attribute-condition이 이 리포에 한정(T-1-29) + Actions 실행 로그가 actor·SHA·시각·가드 출력을 기록(01-08 DEPLOY-LOG에 실행 URL) |
</threat_model>

## Flagged Assumptions (spec-less edge probe)

| requirement_id | category | status | 메모 |
|---|---|---|---|
| OPS-02 | unclassified | unresolved | 이 플랜은 min-instances 0·max-instances 3·db-f1-micro·HDD 10GB를 고정한다. 실제 월 금액의 경계($30)는 01-08에서 청구서로 사용자가 확인(수동) |

### 실행 시 재검증할 [ASSUMED] 항목 (01-07이 실제 gcloud 출력으로 확인)
- Cloud Run 결정적 URL 형식 `https://<svc>-<project_number>.<region>.run.app` 및 태그 URL `rev-<sha8>---…` — deploy.sh는 describe 결과로 재확인한다(Job의 `BETTER_AUTH_URL`은 결정적 값 그대로 — env 계약 충족용이며 Job은 HTTP를 받지 않는다)
- 새 서비스에 `--no-traffic`이 허용되지 않는다는 전제로 첫 배포는 100%로 간다
- `backup-failed` 로그 필터, `roles/cloudsql.viewer`의 `backupRuns.list` 포함(A3), 프라이빗 IP + Direct VPC egress 조합, `iam_authentication` 플래그와 `--type=cloud_iam_service_account` 사용자 이름 형식
- deploy.yml 가드 스텝의 전제(D-05): `--to-latest` 승격 뒤 `gcloud run services describe --format=json`의 `status.traffic[]`에 `percent == 100`인 항목이 정확히 하나이고(태그 리비전 `rev-<sha8>`은 0%로 남을 수 있음), `gcloud run revisions describe`의 `spec.containers[0].image`가 배포 때 준 태그 문자열(`…/app:<sha>`)을 digest로 치환하지 않고 그대로 보존한다 — 01-07 DEPLOY-LOG에 실제 출력으로 기록하고, 가드의 jq 뒤 `| head -n1`은 그 전제가 깨질 때의 방어다

## Artifacts this phase produces (this plan)

- 새 파일: `infra/names.sh`, `scripts/deploy.sh`, `scripts/rollback.sh`, `scripts/bootstrap-gcp.sh`, `infra/monitoring/{5xx-ratio,backup-failed,tick-stale}.json.tpl`, `.github/workflows/deploy.yml`, `.github/workflows/account.yml`, `test/unit/deploy/fakebin/{gcloud,docker,curl}`, `test/unit/deploy/{deploy-sh,rollback-sh,bootstrap-sh,workflows}.test.ts`
- 심볼(bash 함수): `svc_name`, `sql_instance`, `runtime_sa`, `job_name`, `secret_name`, `image_path`, `alert_channel`, `policy_5xx`, `policy_backup`, `policy_tick`, `tick_metric`; deploy.sh 함수 `parse_args`, `require_clean_tree`, `resolve_project_number`, `require_prod_image`(prod 전용 — 이미지 없으면 exit 1, 빌드 안 함), `ensure_apis`, `ensure_ar_repo`, `ensure_network`, `ensure_sql_instance`, `ensure_sql_db_users`, `ensure_secrets`, `build_and_push_image`, `deploy_jobs`, `run_db_bootstrap`, `run_migrate`, `deploy_service`, `smoke`, `promote`, `ensure_alerts`, `map_domain`(자리만 — stderr 안내, gcloud 호출 없음)
- CLI 플래그: deploy.sh `--env --project --region --sha --domain --allow-dirty --dry-run`; rollback.sh `--env --project --region`; bootstrap-gcp.sh `--project --github-repo --region`
- 상수: `MAX_INSTANCES=3`, `DB_POOL_MAX=5`, `DB_TIER=db-f1-micro`, `DB_VERSION=POSTGRES_16`, `DB_STORAGE_GB=10`, `WIF_POOL=github`, `WIF_PROVIDER=erp-repo`, `DEPLOYER_SA=gha-deployer`
- GCP 리소스 이름 규칙: `erp-{env}`, `erp-{env}-db`, `erp-{env}-runtime`, `erp-{env}-{migrate|db-bootstrap|account}`, 시크릿 `{base}-{env}`, AR `erp`, 로그 지표 `notify_tick_success_{env}`, 정책 `[env] …`
- 워크플로: `deploy`(잡 ci/staging/production; inputs target(staging|production)/sha; production 잡 가드 스텝 `guard` 출력 `sha`), `account`(inputs env/action/email/name/admin); 저장소 수준 GitHub 변수 계약 `GCP_PROJECT_ID`, `GCP_PROJECT_NUMBER`, `GCP_REGION`, `ALERT_EMAIL`(Secrets 없음); GitHub Environments 없음(D-05)

<verification>
- `pnpm test:unit` exit 0 (deploy 디렉터리 테스트 4개 + ci-guard 포함)
- `bash -n` 스크립트 3개 exit 0
</verification>

<success_criteria>
- ROADMAP 기준 1·6의 deploy.sh 계약(인프라 ensure·SHA 이미지·더티 트리 거부·migrate Job·0%→스모크→100%·rollback·16A·경보 3개·프로젝트/리전 인자)이 스크립트로 존재하고 가짜 gcloud 테스트로 순서가 고정됐다(OPS-01)
- D-02·D-05의 GitHub Actions + WIF + 수동 승격(workflow_dispatch + 스테이징 SHA 가드, GitHub Environments 없음) 워크플로가 정의됐다
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-06-SUMMARY.md` when done — shellcheck 실행 여부, 가짜 심이 흉내 낸 gcloud 하위 명령 목록(실제 실행 시 차이가 날 수 있는 지점)을 기록.
</output>
