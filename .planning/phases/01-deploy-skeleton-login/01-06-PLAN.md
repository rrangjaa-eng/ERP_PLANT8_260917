---
phase: 01-deploy-skeleton-login
plan: 06
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-04"]
coupling_justified: ["01-05: 01-06 tests run only against fake gcloud/docker/curl shims and touch no DB, port, or file 01-05 uses; both plans only read package.json scripts — order irrelevant"]
files_modified:
  - infra/names.sh
  - scripts/deploy.sh
  - scripts/rollback.sh
  - scripts/bootstrap-gcp.sh
  - infra/monitoring/5xx-ratio.json.tpl
  - infra/monitoring/backup-failed.json.tpl
  - infra/monitoring/tick-stale.json.tpl
  - .github/workflows/deploy.yml
  - .github/workflows/account.yml
  - test/unit/deploy/fakebin/gcloud
  - test/unit/deploy/fakebin/docker
  - test/unit/deploy/fakebin/curl
  - test/unit/deploy/deploy-sh.test.ts
  - test/unit/deploy/rollback-sh.test.ts
  - test/unit/deploy/bootstrap-sh.test.ts
  - test/unit/deploy/workflows.test.ts
autonomous: true
requirements: [OPS-01, OPS-02]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "scripts/deploy.sh --env <staging|prod> --project <ID> --region <R> [--domain D] 한 번으로 Artifact Registry(서울)·Cloud SQL(서울, db-f1-micro, 공인 IP 없음, IAM 인증, 자동 백업)·Secret Manager(접미사 시크릿)·Cloud Run 서비스와 Job 3개가 없으면 만들어지고 있으면 갱신되며, 다른 프로젝트 ID로도 같은 결과를 낸다 (OPS-01, D-03, D-04)"
    - "deploy.sh는 더티 트리(git status --porcelain 비어 있지 않음)에서 아무 gcloud 명령도 실행하지 않고 exit 2로 거부한다 (Issue 4)"
    - "deploy.sh 순서는 이미지(git SHA 태그, 이미 있으면 빌드 생략) → db-bootstrap Job → migrate Job(실패·exit 3이면 중단) → 기존 서비스면 --no-traffic + rev-<sha8> 태그 리비전 → 스모크(/healthz ok:true, /login 200) → 100% 승격 → 경보 3개 upsert 이며, 스모크 실패 시 트래픽을 옮기지 않고 exit 1 이다 (OPS-01, 20A, Issue 4)"
    - "scripts/rollback.sh --env --project --region 은 직전 리비전으로 트래픽 100%를 되돌리고 리비전이 하나뿐이면 거부한다 (OPS-01)"
    - "deploy.sh는 Cloud Monitoring 이메일 채널(ALERT_EMAIL)과 정책 3개([env] 5xx ratio > 5% / [env] Cloud SQL backup failed / [env] notify tick stale 24h — 마지막은 enabled:false)를 displayName으로 찾아 있으면 update 없으면 create 한다 (D-16, 19A)"
    - "scripts/bootstrap-gcp.sh --project --github-repo owner/name [--region] 은 사용자가 Cloud Shell에서 1회 실행하는 부트스트랩으로 API 활성화·WIF 풀(github)/프로바이더(erp-repo)·SA(gha-deployer, erp-staging-runtime, erp-prod-runtime)·역할·VPC 프라이빗 서비스 접근·조직 정책 확인을 멱등하게 수행하고 GitHub 변수 3개 값을 출력한다 (D-02, OV-3)"
    - ".github/workflows/deploy.yml 은 main push 또는 workflow_dispatch(target=staging)에서 ci.yml(workflow_call) 통과 뒤 WIF로 인증해 스테이징에 deploy.sh를 실행하고, 이어서 environment: production(필수 검토자 승인) 잡이 같은 SHA로 deploy.sh --env prod 를 실행한다; 빌드는 한 번이다 (D-02, D-05)"
    - "Cloud Run 서비스는 min-instances 0, max-instances 3, DB_POOL_MAX 5 로 배포된다 (OPS-02, D-06, 16A 시작값)"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "인자 파싱·더티 트리 거부·인프라 ensure·이미지·Job·리비전·스모크·승격·경보 (bash, set -euo pipefail)"
      min_lines: 200
    - path: "scripts/rollback.sh"
      provides: "직전 리비전 트래픽 복구"
      contains: "update-traffic"
    - path: "scripts/bootstrap-gcp.sh"
      provides: "WIF·SA·역할·VPC·조직 정책 확인 (Cloud Shell 1회)"
      contains: "workload-identity-pools"
    - path: "infra/names.sh"
      provides: "리소스 이름 규칙 함수와 상수(MAX_INSTANCES=3, DB_POOL_MAX=5, DB_TIER=db-f1-micro)"
      contains: "db-f1-micro"
    - path: ".github/workflows/deploy.yml"
      provides: "ci → staging → production(environment gate) 배포 워크플로 (WIF)"
      contains: "workload_identity_provider"
    - path: ".github/workflows/account.yml"
      provides: "workflow_dispatch로 Cloud Run Job erp-{env}-account 실행 (create/reset/unlock)"
      contains: "erp-"
    - path: "infra/monitoring/tick-stale.json.tpl"
      provides: "notify tick 24h 부재 정책 (enabled false)"
      contains: "\"enabled\": false"
  key_links:
    - from: "scripts/deploy.sh"
      to: "infra/names.sh"
      via: "source infra/names.sh"
      pattern: "names\\.sh"
    - from: "scripts/deploy.sh"
      to: "dist/cli/migrate-runner.mjs (01-05)"
      via: "gcloud run jobs deploy … --command=node --args=dist/cli/migrate-runner.mjs; execute --wait; exit 3 → PoolRuleViolation 메시지"
      pattern: "migrate-runner\\.mjs"
    - from: ".github/workflows/deploy.yml"
      to: ".github/workflows/ci.yml"
      via: "uses: ./.github/workflows/ci.yml"
      pattern: "uses: \\./\\.github/workflows/ci\\.yml"
    - from: ".github/workflows/deploy.yml"
      to: "scripts/deploy.sh"
      via: "run: bash scripts/deploy.sh --env … --project ${{ vars.GCP_PROJECT_ID }}"
      pattern: "scripts/deploy\\.sh"
    - from: "scripts/deploy.sh"
      to: "infra/monitoring/*.json.tpl"
      via: "sed 치환 후 gcloud alpha monitoring policies create/update --policy-from-file"
      pattern: "policy-from-file"
  prohibitions:
    - statement: "MUST NOT write the real GCP project ID, project number, or alert email into the repo or docs; only the variable names GCP_PROJECT_ID / GCP_PROJECT_NUMBER / ALERT_EMAIL (D-03)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT run drizzle-kit's push subcommand anywhere in deploy.sh or the workflows; schema changes reach the DB only through the migrate Job (Issue 4)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT assign a public IP to Cloud SQL instances (deploy.sh creates them with --no-assign-ip on the private network) (6A)"
      status: resolved
      verification: judgment
---

<objective>
배포 파이프라인의 "이미지 밖" 전부를 스크립트와 워크플로로 쓴다: 이름 규칙, `deploy.sh`(인프라 ensure → 이미지 → Job → 0% 리비전 → 스모크 → 100% → 경보), `rollback.sh`, `bootstrap-gcp.sh`(사용자 Cloud Shell 1회), 경보 정책 템플릿 3개, `deploy.yml`(WIF, staging 자동 → production 승인)·`account.yml`. 실제 GCP 없이 가짜 `gcloud`/`docker`/`curl` 심으로 분기·순서·거부 조건을 단위 테스트한다. 실제 실행은 01-07(스테이징)·01-08(프로덕션).

Purpose: ROADMAP 기준 1·6과 D-02~D-06·D-15·D-16을 코드로 고정한다. 사용자는 배포마다 손이 가지 않아야 한다(GitHub 승인 버튼 하나).
Output: 스크립트 3개, 이름 파일, 정책 템플릿 3개, 워크플로 2개, 가짜 심 3개 + 테스트 4개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-RESEARCH.md
@.planning/phases/01-deploy-skeleton-login/SKELETON.md
@.planning/phases/01-deploy-skeleton-login/01-05-PLAN.md
@.planning/phases/01-deploy-skeleton-login/01-04-SUMMARY.md
@docs/designs/plant8-erp-roadmap-eng-review-260917.md
@docs/designs/plant8-erp-roadmap-ceo-review-260917.md
@scripts/install_pkgs.sh
@.github/workflows/ci.yml

<interfaces>
<!-- 01-05 계약: 이미지 안 명령 — 서비스 `node server.js`(3000), Job migrate `node dist/cli/migrate-runner.mjs`(exit 0/1/3, env MAX_INSTANCES·DB_POOL_MAX), Job db-bootstrap `node dist/cli/db-bootstrap.mjs`(env DB_ADMIN_PASSWORD), Job account `node dist/cli/account-cli.mjs <sub> --email …` -->
<!-- 01-04 계약: `.github/workflows/ci.yml`의 `workflow_call` 트리거 -->
<!-- 01-01 계약: 앱 env 키 — APP_ENV, CLOUD_SQL_CONNECTION_NAME, DB_IAM_USER, DB_NAME, DB_POOL_MAX, BETTER_AUTH_SECRET, BETTER_AUTH_URL, AUTH_PROVIDER, APP_DATA_KEY_v1, SMTP_HOST/USER/PASSWORD/FROM, GCP_PROJECT_ID, CLOUD_SQL_INSTANCE_ID, APP_GIT_SHA, APP_DEPLOYED_AT -->

infra/names.sh (bash, `source`용; 함수는 env 인자를 받는다):
```bash
REGION_DEFAULT=asia-northeast3; AR_REPO=erp; DB_NAME=erp; DB_TIER=db-f1-micro; DB_VERSION=POSTGRES_16; DB_STORAGE_GB=10
MAX_INSTANCES=3; DB_POOL_MAX=5; NETWORK=default; VPC_RANGE=google-managed-services-default
WIF_POOL=github; WIF_PROVIDER=erp-repo; DEPLOYER_SA=gha-deployer
svc_name()        { echo "erp-$1"; }              # erp-staging / erp-prod
sql_instance()    { echo "erp-$1-db"; }
runtime_sa()      { echo "erp-$1-runtime"; }      # 이메일: erp-$1-runtime@$PROJECT.iam.gserviceaccount.com, IAM DB user: erp-$1-runtime@$PROJECT.iam
job_name()        { echo "erp-$1-$2"; }           # $2 ∈ migrate | db-bootstrap | account
secret_name()     { echo "$1-$2"; }               # base-env: better-auth-secret-staging, app-data-key-v1-staging, db-admin-password-staging, smtp-host-staging …
image_path()      { echo "$3-docker.pkg.dev/$2/$AR_REPO/app:$1"; }   # sha project region
alert_channel()   { echo "ERP Alerts ($1)"; }
policy_5xx()      { echo "[$1] 5xx ratio > 5%"; }
policy_backup()   { echo "[$1] Cloud SQL backup failed"; }
policy_tick()     { echo "[$1] notify tick stale 24h"; }
tick_metric()     { echo "notify_tick_success_$1"; }
```
scripts/deploy.sh 인자/환경/종료 코드:
- 인자: `--env staging|prod`(필수) `--project ID`(필수) `--region R`(기본 REGION_DEFAULT) `--sha S`(기본 `git rev-parse HEAD`) `--domain D`(선택) `--allow-dirty`(선택, CI 미사용) `--dry-run`(선택: 명령을 `+ …`로 출력만)
- 환경: `ALERT_EMAIL`(필수), 테스트 훅 `DEPLOY_FAKE_STATE`(선택; 가짜 심이 상태를 저장하는 디렉터리 — 스크립트는 읽지 않는다)
- 종료: 0 성공 / 1 단계 실패(migrate·스모크·gcloud 오류; 메시지에 단계 이름, migrate exit 3이면 "PoolRuleViolation") / 2 인자·더티 트리 거부
- stdout 마지막 줄: `SERVICE_URL=https://…`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: infra/names.sh + scripts/deploy.sh + scripts/rollback.sh — 가짜 gcloud/docker/curl 심으로 순서·분기·거부 테스트</name>
  <files>infra/names.sh, scripts/deploy.sh, scripts/rollback.sh, test/unit/deploy/fakebin/gcloud, test/unit/deploy/fakebin/docker, test/unit/deploy/fakebin/curl, test/unit/deploy/deploy-sh.test.ts, test/unit/deploy/rollback-sh.test.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 7 (Job 실행 `--wait`), §Pattern 8 (`--no-traffic --tag`, 스모크, `update-traffic`, rollback), §Pattern 10 (채널·정책 upsert), §Pattern 6 커넥션 공식, §Pitfall 3·5, §Assumptions A2·A6
    - docs/designs/plant8-erp-roadmap-eng-review-260917.md Diagram 2, Issue 4(SHA 태그·더티 트리 거부·rollback.sh), F2·F10
    - docs/designs/plant8-erp-roadmap-ceo-review-260917.md 16A, 19A, 20A, §5 배포 순서·6 롤백, Error & Rescue `deploy` 행(SmokeFailed → 트래픽 0% 유지, PoolRuleViolation → 배포 거부)
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-03(프로젝트 ID는 인자), D-04(환경 2개, 접미사 시크릿, 인자 유지), D-06(최소 사양·백업 켬), D-15(`--domain` 자리), D-16(경보 채널·정책 이름에 환경, tick은 Phase 7 전까지 비발화)
    - .planning/phases/01-deploy-skeleton-login/01-05-PLAN.md <interfaces> (컨테이너 실행 계약·exit 코드)
    - scripts/install_pkgs.sh (셸 관례: shebang, 주석, `ROOT=` — 단 deploy.sh는 `set -euo pipefail`로 실패 시 반드시 중단)
  </read_first>
  <behavior>
    - deploy-sh.test.ts(가짜 심: `PATH=test/unit/deploy/fakebin:$PATH`, 심은 호출된 인자를 `$DEPLOY_FAKE_LOG`에 한 줄씩 append하고 `$DEPLOY_FAKE_STATE` 디렉터리의 파일로 상태(서비스 존재·이미지 존재·리비전 목록·정책 존재)를 흉내 낸다; 테스트는 임시 git 리포(`git init` + 커밋)에 `scripts/`·`infra/`를 복사해 실행):
      1) 새 프로젝트(상태 비어 있음), `ALERT_EMAIL=ops@example.test`: exit 0; 로그 순서가 `services enable` < `artifacts repositories create` < `sql instances create` < `sql databases create` < `sql users create` < `secrets create` < `docker build` < `docker push` < `run jobs deploy erp-staging-db-bootstrap` < `run jobs execute erp-staging-db-bootstrap` < `run jobs execute erp-staging-migrate` < `run deploy erp-staging` < `curl …/healthz` < `monitoring policies create`; `run deploy` 줄에 `--no-traffic`이 없고 `--min-instances=0`·`--max-instances=3`·`--no-assign-ip`는 `sql instances create` 줄에, `--set-secrets`에 `BETTER_AUTH_SECRET=better-auth-secret-staging:latest`가 있다; `sql instances create` 줄에 `--tier=db-f1-micro`·`--no-assign-ip`·`--database-flags=cloudsql.iam_authentication=on`·`--backup`이 있다; 마지막 stdout 줄이 `SERVICE_URL=https://erp-staging-`로 시작
      2) 서비스·이미지가 이미 있는 상태: `docker build` 없음; `run deploy` 줄에 `--no-traffic`과 `--tag=rev-01234567`(sha 앞 8자); `curl` 뒤에 `services update-traffic erp-staging … --to-latest`; `monitoring policies update`(create 아님)
      3) 더티 트리(임시 리포에 untracked 파일): exit 2, 로그 파일 비어 있음, stderr에 `dirty`
      4) migrate Job 실패(상태 파일 `fail-migrate=3`): exit 1, stderr에 `PoolRuleViolation`, 로그에 `run deploy` 없음
      5) 기존 서비스에서 스모크 실패(상태 파일 `healthz=503`): exit 1, stderr에 `SmokeFailed`, 로그에 `update-traffic` 없음
      6) `--dry-run`: exit 0, 로그 비어 있음, stdout에 `+ gcloud run deploy`
      7) `--env qa`·`--project` 누락·`ALERT_EMAIL` 미설정: exit 2
      8) `--project other-proj --region us-central1`: 모든 gcloud 줄에 `--project=other-proj`, 이미지 경로가 `us-central1-docker.pkg.dev/other-proj/erp/app:`
    - rollback-sh.test.ts: 리비전 2개 상태 → `update-traffic erp-staging --to-revisions=<두 번째>=100` 호출, exit 0; 리비전 1개 → exit 1, stderr에 `no previous revision`
  </behavior>
  <action>
    OPS-01·Issue 4·16A·19A·20A·D-03·D-04·D-06·D-15·D-16을 구현한다. bash, `set -euo pipefail`, 함수 단위로 나누고 모든 외부 명령은 `run()` 래퍼를 지나 `--dry-run`이면 `+ …`만 출력한다. 프로젝트 ID·리전·이메일은 인자/환경에서만 온다(리포에 실제 값 금지, D-03).
    1. `infra/names.sh`를 <interfaces>대로.
    2. `scripts/deploy.sh` 단계(함수 이름 그대로):
       - `parse_args`(<interfaces> 인자·검증; 잘못되면 usage + exit 2) → `require_clean_tree`(`git status --porcelain`이 비어 있지 않고 `--allow-dirty` 없으면 stderr "refusing to deploy from a dirty tree" exit 2 — gcloud 호출 전) → `source infra/names.sh` → `resolve_project_number`(`gcloud projects describe $PROJECT --format='value(projectNumber)'`)
       - `ensure_apis`: `gcloud services enable run.googleapis.com sqladmin.googleapis.com secretmanager.googleapis.com artifactregistry.googleapis.com monitoring.googleapis.com logging.googleapis.com compute.googleapis.com servicenetworking.googleapis.com --project=$PROJECT`
       - `ensure_ar_repo`: `gcloud artifacts repositories describe $AR_REPO --location=$REGION` || `create --repository-format=docker --location=$REGION`
       - `ensure_network`: `gcloud services vpc-peerings list --network=$NETWORK --service=servicenetworking.googleapis.com --project` 출력에 `$VPC_RANGE`가 없으면 stderr "private services access missing — run scripts/bootstrap-gcp.sh first" exit 1
       - `ensure_sql_instance`: describe || `gcloud sql instances create $(sql_instance $ENV) --project --region --database-version=$DB_VERSION --tier=$DB_TIER --edition=ENTERPRISE --storage-type=HDD --storage-size=$DB_STORAGE_GB --no-storage-auto-increase --availability-type=ZONAL --backup --backup-start-time=18:00 --retained-backups-count=7 --no-assign-ip --network=projects/$PROJECT/global/networks/$NETWORK --database-flags=cloudsql.iam_authentication=on --deletion-protection` 뒤 `describe --format='value(state)'`가 RUNNABLE 될 때까지 대기(최대 15분); `CONN_NAME=$(describe --format='value(connectionName)')`
       - `ensure_sql_db_users`: `gcloud sql databases describe $DB_NAME --instance` || create; IAM 사용자 `$(runtime_sa $ENV)@$PROJECT.iam`이 `gcloud sql users list --instance --format='value(name)'`에 없으면 `gcloud sql users create "$(runtime_sa $ENV)@$PROJECT.iam" --instance --type=cloud_iam_service_account`; 관리 비밀번호: 시크릿 `$(secret_name db-admin-password $ENV)`에 버전이 없으면 `openssl rand -base64 32`로 만들어 `gcloud secrets versions add`하고 같은 값으로 `gcloud sql users set-password postgres --instance --password=…`(새로 만들 때만; 값은 stdout·로그에 절대 출력하지 않는다 — `set -x` 금지)
       - `ensure_secrets`: 각 시크릿(`better-auth-secret`·`app-data-key-v1`: 없으면 `openssl rand -base64 48`; `smtp-host`·`smtp-user`·`smtp-password`·`smtp-from`: 없으면 센티널 `__unset__`; `db-admin-password`는 위) `gcloud secrets describe` || `create --replication-policy=user-managed --locations=$REGION`, 버전 없으면 `versions add --data-file=-`; 런타임 SA에 `roles/secretmanager.secretAccessor` 바인딩(`add-iam-policy-binding`)
       - `build_and_push_image`: `IMAGE=$(image_path $SHA $PROJECT $REGION)`; `gcloud artifacts docker images describe $IMAGE` 성공이면 건너뜀(D-05 빌드 1회), 아니면 `gcloud auth configure-docker $REGION-docker.pkg.dev --quiet` → `docker build --build-arg GIT_SHA=$SHA -t $IMAGE .` → `docker push $IMAGE`
       - `deploy_jobs`: 공통 플래그 `--image=$IMAGE --region --project --service-account=$(runtime_sa $ENV)@$PROJECT.iam.gserviceaccount.com --network=$NETWORK --subnet=$NETWORK --vpc-egress=private-ranges-only --set-cloudsql-instances=$CONN_NAME --max-retries=0 --task-timeout=15m --set-env-vars=APP_ENV=$ENV,CLOUD_SQL_CONNECTION_NAME=$CONN_NAME,DB_IAM_USER=$(runtime_sa $ENV)@$PROJECT.iam,DB_NAME=$DB_NAME`; `gcloud run jobs deploy $(job_name $ENV db-bootstrap) --command=node --args=dist/cli/db-bootstrap.mjs --set-secrets=DB_ADMIN_PASSWORD=$(secret_name db-admin-password $ENV):latest`; `… $(job_name $ENV migrate) --command=node --args=dist/cli/migrate-runner.mjs --set-env-vars=…,MAX_INSTANCES=$MAX_INSTANCES,DB_POOL_MAX=$DB_POOL_MAX,BETTER_AUTH_SECRET=…`(migrate·account Job은 `--set-secrets=BETTER_AUTH_SECRET=…:latest` — lib/env 파싱을 위해); `… $(job_name $ENV account) --command=node --args=dist/cli/account-cli.mjs`
       - `run_db_bootstrap`: `gcloud run jobs execute $(job_name $ENV db-bootstrap) --region --project --wait` 실패 → stderr "db-bootstrap failed" exit 1
       - `run_migrate`: `gcloud run jobs execute $(job_name $ENV migrate) … --wait`; 실패 시 `gcloud run jobs executions describe`(또는 `gcloud logging read … --limit=50`)로 `deploy.pool_rule_violation`이 보이면 stderr "PoolRuleViolation: max_instances × pool exceeds max_connections − 5" exit 1, 아니면 "migration failed" exit 1. 가짜 심 테스트에서는 실행 exit 코드 3을 그대로 PoolRuleViolation으로 매핑해도 된다(스크립트는 exit 코드 3 우선, 로그 조회는 보조)
       - `deploy_service`: `SERVICE_URL=https://$(svc_name $ENV)-$PROJECT_NUMBER.$REGION.run.app`(결정적 URL; 배포 뒤 `describe --format='value(status.url)'`로 재확인해 다르면 그 값을 쓴다); `EXISTS`=describe 성공 여부; 공통 플래그 `--image --region --project --platform=managed --service-account=… --min-instances=0 --max-instances=$MAX_INSTANCES --concurrency=80 --cpu=1 --memory=512Mi --port=3000 --network --subnet --vpc-egress=private-ranges-only --set-cloudsql-instances=$CONN_NAME --allow-unauthenticated --set-env-vars=APP_ENV=$ENV,APP_GIT_SHA=$SHA,APP_DEPLOYED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ),CLOUD_SQL_CONNECTION_NAME=…,DB_IAM_USER=…,DB_NAME=$DB_NAME,DB_POOL_MAX=$DB_POOL_MAX,BETTER_AUTH_URL=$SERVICE_URL,AUTH_PROVIDER=email,GCP_PROJECT_ID=$PROJECT,CLOUD_SQL_INSTANCE_ID=$(sql_instance $ENV) --set-secrets=BETTER_AUTH_SECRET=…:latest,APP_DATA_KEY_v1=…:latest,SMTP_HOST=…:latest,SMTP_USER=…:latest,SMTP_PASSWORD=…:latest,SMTP_FROM=…:latest`; EXISTS면 `--no-traffic --tag=rev-${SHA:0:8}` 추가. `--allow-unauthenticated`가 조직 정책으로 실패하면 stderr "org policy blocks unauthenticated ingress (iam.allowedPolicyMemberDomains) — ask the GCP org admin" exit 1
       - `smoke`: 대상 URL = EXISTS면 `https://rev-${SHA:0:8}---$(svc_name $ENV)-$PROJECT_NUMBER.$REGION.run.app`(describe `status.traffic` 항목의 tag URL로 재확인) 아니면 `$SERVICE_URL`; `curl -fsS --retry 6 --retry-delay 5 --retry-all-errors $URL/healthz`가 `"ok":true`를 포함하고 `curl -s -o /dev/null -w '%{http_code}' $URL/login`이 200 — 아니면 stderr "SmokeFailed: traffic left unchanged" exit 1(리비전은 남겨 조사 가능, 트래픽은 옮기지 않음)
       - `promote`: EXISTS면 `gcloud run services update-traffic $(svc_name $ENV) --region --project --to-latest`
       - `ensure_alerts`: 채널 `gcloud beta monitoring channels list --project --filter="displayName='$(alert_channel $ENV)'" --format='value(name)'` 없으면 `create --display-name --type=email --channel-labels=email_address=$ALERT_EMAIL`; 로그 기반 지표 `gcloud logging metrics describe $(tick_metric $ENV)` || `create --description --log-filter='resource.type="cloud_run_revision" AND resource.labels.service_name="'$(svc_name $ENV)'" AND jsonPayload.event="notify.tick" AND jsonPayload.ok=true'`; 정책 3개: 템플릿 `infra/monitoring/*.json.tpl`의 토큰 `__ENV__ __CHANNEL__ __SERVICE__ __PROJECT__ __INSTANCE__ __METRIC__`을 `sed`로 치환해 임시 파일로 만들고 `gcloud alpha monitoring policies list --project --filter="displayName='<name>'" --format='value(name)'`가 있으면 `update <name> --policy-from-file` 없으면 `create --policy-from-file`
       - `map_domain`(선택): `--domain`이 있으면 `gcloud beta run domain-mappings create --service --domain --region` 시도, 실패해도 exit 코드에 영향 없이 stderr에 결과(A1 확인용, D-15)
       - 마지막: `echo "SERVICE_URL=$SERVICE_URL"`
    3. `scripts/rollback.sh --env --project --region`: `gcloud run revisions list --service=$(svc_name $ENV) --region --project --sort-by='~metadata.creationTimestamp' --format='value(metadata.name)' --limit=2`에서 둘째 → 없으면 stderr "no previous revision" exit 1 → `update-traffic --to-revisions=$PREV=100` → 출력.
    4. 가짜 심 3개(bash, 실행 비트): 호출 인자를 `$DEPLOY_FAKE_LOG`에 append; `gcloud`는 하위 명령별로 `$DEPLOY_FAKE_STATE`의 파일(예: `service-exists`, `image-exists`, `revisions`, `policy-exists`, `fail-migrate`)을 읽어 성공/실패·출력(`projects describe` → `123456789012`, `describe --format=value(state)` → `RUNNABLE`, `connectionName` → `test-proj:asia-northeast3:erp-staging-db`, `vpc-peerings list` → `google-managed-services-default`)을 흉내 낸다; `docker`는 항상 성공; `curl`은 `$DEPLOY_FAKE_STATE/healthz`가 `503`이면 exit 22 아니면 `{"ok":true}` 또는 `200` 출력. 테스트에서 `git config`로 임시 리포에 커밋을 만들고 `spawnSync('bash', ['scripts/deploy.sh', …], { cwd: tmpRepo, env: { ...process.env, PATH, DEPLOY_FAKE_LOG, DEPLOY_FAKE_STATE, ALERT_EMAIL } })`.
    5. TDD: 테스트 먼저 → 스크립트 → 통과. `bash -n scripts/deploy.sh`·`bash -n scripts/rollback.sh` 구문 확인. `shellcheck`가 있으면 실행(없으면 생략, SUMMARY에 기록).
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && bash -n scripts/rollback.sh && pnpm vitest run --project unit test/unit/deploy/deploy-sh.test.ts test/unit/deploy/rollback-sh.test.ts</automated>
    <fails_when>non-zero exit, or a vitest summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy.sh` 첫 줄이 `#!/usr/bin/env bash`(또는 `#!/bin/bash`)이고 `set -euo pipefail`, 함수 `require_clean_tree`·`run_migrate`·`deploy_service`·`smoke`·`promote`·`ensure_alerts`가 정의돼 있다
    - `grep -c -- '--no-assign-ip' scripts/deploy.sh` ≥ 1, `grep -c -- '--allow-unauthenticated' scripts/deploy.sh` ≥ 1, `grep -c -- '--min-instances=0' scripts/deploy.sh` ≥ 1, `grep -c 'set -x' scripts/deploy.sh`가 0
    - `grep -c -E '[0-9]{12}|@gmail\.com|@plant8' scripts/deploy.sh infra/names.sh scripts/rollback.sh`가 0 (실제 식별자 없음)
    - `grep -c -E 'drizzle-kit\s+push' scripts/deploy.sh .github/workflows/*.yml`가 파일마다 0
    - 단위 테스트가 <behavior>의 시나리오 1~8과 rollback 2개를 모두 단언하고 통과한다
  </acceptance_criteria>
  <done>deploy.sh·rollback.sh가 가짜 gcloud 위에서 순서·분기·거부 조건 전부를 통과하고, 다른 프로젝트 ID·리전으로도 같은 명령 시퀀스를 낸다는 것이 테스트로 증명됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bootstrap-gcp.sh + 경보 정책 템플릿 3개 + deploy.yml·account.yml 워크플로</name>
  <files>scripts/bootstrap-gcp.sh, infra/monitoring/5xx-ratio.json.tpl, infra/monitoring/backup-failed.json.tpl, infra/monitoring/tick-stale.json.tpl, .github/workflows/deploy.yml, .github/workflows/account.yml, test/unit/deploy/bootstrap-sh.test.ts, test/unit/deploy/workflows.test.ts</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Pattern 9 (WIF 풀·프로바이더·SA·attribute-condition, deploy job의 `permissions`·`google-github-actions/auth`), §Pattern 10 (정책 필터 예), §Open Questions 3 (역할은 넓게 → 좁히기), §Assumptions A1·A3·A6
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-02(부트스트랩은 사용자 Cloud Shell 1회), D-05(승격: staging 자동 → production 승인 → 같은 SHA), D-11(운영 계정 발급은 Cloud Run Job), D-16(tick 경보는 Phase 7 전 비발화), Claude's Discretion(WIF 이름·최소 권한)
    - PROJECT.md §Constraints "회사 GCP 조직 정책(비인증 ingress·외부 링크 허용)은 Phase 1에서 확인"
    - .github/workflows/ci.yml (01-04 — `workflow_call`), infra/names.sh (Task 1)
    - test/unit/ci-guard.test.ts (01-04 — 워크플로 전체에 대한 push 하위 명령 부재 단언이 deploy.yml·account.yml에도 자동 적용된다)
  </read_first>
  <behavior>
    - bootstrap-sh.test.ts(가짜 gcloud 심 재사용): `bash scripts/bootstrap-gcp.sh --project test-proj --github-repo rrangjaa-eng/ERP_PLANT8_260917` → exit 0; 로그에 `services enable`(iam·iamcredentials·sts·cloudresourcemanager·orgpolicy·compute·servicenetworking 포함) < `iam workload-identity-pools create github` < `providers create-oidc erp-repo`(`--attribute-condition` 에 `assertion.repository == 'rrangjaa-eng/ERP_PLANT8_260917'`, `--issuer-uri=https://token.actions.githubusercontent.com`) < `iam service-accounts create gha-deployer` < `service-accounts create erp-staging-runtime` < `create erp-prod-runtime` < `projects add-iam-policy-binding`(roles/run.admin, roles/cloudsql.admin, roles/secretmanager.admin, roles/artifactregistry.admin, roles/monitoring.editor, roles/logging.admin, roles/serviceusage.serviceUsageAdmin, roles/compute.networkAdmin 각각 gha-deployer) < `service-accounts add-iam-policy-binding …workloadIdentityUser` < `compute addresses create google-managed-services-default` < `services vpc-peerings connect` < `org-policies describe`; 런타임 SA 역할 `roles/cloudsql.client`·`roles/cloudsql.instanceUser`·`roles/cloudsql.viewer`·`roles/logging.logWriter`·`roles/monitoring.metricWriter`가 두 환경 모두에 바인딩; stdout 마지막에 `GCP_PROJECT_ID=test-proj`, `GCP_PROJECT_NUMBER=123456789012`, `GCP_REGION=asia-northeast3`; `--github-repo` 누락 → exit 2; 두 번째 실행(상태: 이미 존재)도 exit 0
    - workflows.test.ts: `deploy.yml`에 `on:` 아래 `push: branches: [main]`와 `workflow_dispatch`(`inputs.target` choice `staging`만), `permissions: id-token: write`, `uses: ./.github/workflows/ci.yml`, `google-github-actions/auth@v3`, `workload_identity_provider: projects/${{ vars.GCP_PROJECT_NUMBER }}/locations/global/workloadIdentityPools/github/providers/erp-repo`, `service_account: gha-deployer@${{ vars.GCP_PROJECT_ID }}.iam.gserviceaccount.com`, 잡 `staging`(`environment: staging`)과 `production`(`environment: production`, `needs: staging`), 둘 다 `bash scripts/deploy.sh --env` 실행, production 줄에 `--env prod`와 `--sha ${{ github.sha }}`, `ALERT_EMAIL: ${{ vars.ALERT_EMAIL }}`; `account.yml`에 `workflow_dispatch` inputs `env`(staging|production) `action`(create|reset|unlock) `email` `name` `admin`, `gcloud run jobs execute erp-` + `--args`, `gcloud logging read`; 두 파일 모두 실제 프로젝트 ID 형태(12자리 숫자, 하드코딩 이메일) 없음
  </behavior>
  <action>
    D-02·D-05·D-11·D-16·OV-3·19A를 구현한다.
    1. `scripts/bootstrap-gcp.sh`(사용자가 Cloud Shell에서 Owner로 1회 실행; 멱등; `set -euo pipefail`; `--project`·`--github-repo owner/name` 필수, `--region` 기본): (a) `gcloud services enable` iam·iamcredentials·sts·cloudresourcemanager·orgpolicy·compute·servicenetworking·run·sqladmin·secretmanager·artifactregistry·monitoring·logging (b) WIF: `workload-identity-pools describe github --location=global` || create; `providers describe erp-repo` || `create-oidc erp-repo --location=global --workload-identity-pool=github --display-name="PLANT8 ERP repo" --attribute-mapping="google.subject=assertion.sub,attribute.actor=assertion.actor,attribute.repository=assertion.repository,attribute.repository_owner=assertion.repository_owner" --attribute-condition="assertion.repository == '$GITHUB_REPO'" --issuer-uri="https://token.actions.githubusercontent.com"` (c) SA 셋 describe || create (`gha-deployer`, `erp-staging-runtime`, `erp-prod-runtime`) (d) 배포자 프로젝트 역할(넓게 시작 — RESEARCH Open Question 3; 01-08 문서 태스크가 실제 사용 권한으로 좁히는 절차를 적는다): run.admin, cloudsql.admin, secretmanager.admin, artifactregistry.admin, monitoring.editor, logging.admin, serviceusage.serviceUsageAdmin, compute.networkAdmin; 런타임 SA 둘에 `iam.serviceAccountUser`(배포자가 사용); 런타임 SA 역할: cloudsql.client, cloudsql.instanceUser, cloudsql.viewer(D-18·A3), logging.logWriter, monitoring.metricWriter (e) WIF 바인딩: `gha-deployer`에 `roles/iam.workloadIdentityUser` member `principalSet://iam.googleapis.com/projects/$PROJECT_NUMBER/locations/global/workloadIdentityPools/github/attribute.repository/$GITHUB_REPO` (f) VPC: `compute networks describe default` 없으면 stderr 안내 후 `create default --subnet-mode=auto`; `compute addresses describe google-managed-services-default --global` || `create --global --purpose=VPC_PEERING --prefix-length=16 --network=default`; `services vpc-peerings list`에 없으면 `connect --service=servicenetworking.googleapis.com --ranges=google-managed-services-default --network=default` (g) 조직 정책 확인(실패해도 계속, 결과 출력): `gcloud org-policies describe iam.allowedPolicyMemberDomains --project --effective`, `run.allowedIngress`, `compute.restrictVpcPeering`; 도메인 매핑 리전 확인(A1·D-15): `gcloud beta run domain-mappings list --region=$REGION --project`의 성공/오류 메시지를 그대로 출력 (h) 마지막 stdout: `GCP_PROJECT_ID=…`, `GCP_PROJECT_NUMBER=…`, `GCP_REGION=…` + GitHub 설정 안내 3줄(Repository variables 3개 + `ALERT_EMAIL`, Environments `staging`(보호 없음)·`production`(Required reviewers = 본인)).
    2. 정책 템플릿(JSON, gcloud `--policy-from-file` 형식; 토큰 `__ENV__ __CHANNEL__ __SERVICE__ __PROJECT__ __INSTANCE__ __METRIC__`): `5xx-ratio.json.tpl` — displayName `[__ENV__] 5xx ratio > 5%`, `conditionThreshold` filter `resource.type="cloud_run_revision" AND resource.labels.service_name="__SERVICE__" AND metric.type="run.googleapis.com/request_count" AND metric.labels.response_code_class="5xx"`, `denominatorFilter` 같은 필터에서 code_class 조건 제거, aggregations `alignmentPeriod 300s, perSeriesAligner ALIGN_RATE, crossSeriesReducer REDUCE_SUM`, `comparison COMPARISON_GT`, `thresholdValue 0.05`, `duration 300s`, `notificationChannels ["__CHANNEL__"]`, `combiner OR`, `enabled true`; `backup-failed.json.tpl` — displayName `[__ENV__] Cloud SQL backup failed`, `conditionMatchedLog` filter `resource.type="cloudsql_database" AND resource.labels.database_id="__PROJECT__:__INSTANCE__" AND (protoPayload.methodName=~"backup" OR jsonPayload.message=~"backup") AND severity>=ERROR`, `alertStrategy.notificationRateLimit.period 3600s`, `enabled true`([ASSUMED] 필터 — 01-07에서 실제 로그 항목으로 재확인); `tick-stale.json.tpl` — displayName `[__ENV__] notify tick stale 24h`, `conditionAbsent` filter `metric.type="logging.googleapis.com/user/__METRIC__" AND resource.type="cloud_run_revision"`, `duration 86400s`, `"enabled": false`(Phase 7이 tick을 만들며 true로 바꾼다 — D-16).
    3. `.github/workflows/deploy.yml`: `name: deploy`; `on: push: branches: [main]; workflow_dispatch: inputs: target: { type: choice, options: [staging], default: staging }`; `concurrency: { group: deploy, cancel-in-progress: false }`; `permissions: { contents: read, id-token: write }`; 잡 `ci: uses: ./.github/workflows/ci.yml`; 잡 `staging`(`needs: ci`, `environment: { name: staging, url: ${{ steps.deploy.outputs.url }} }`, `env: { ALERT_EMAIL: ${{ vars.ALERT_EMAIL }} }`, 스텝: checkout(`fetch-depth: 0`) → `google-github-actions/auth@v3`(위 inputs) → `google-github-actions/setup-gcloud@v3` → `id: deploy` `run: bash scripts/deploy.sh --env staging --project "${{ vars.GCP_PROJECT_ID }}" --region "${{ vars.GCP_REGION }}" --sha "${{ github.sha }}" | tee deploy.log; echo "url=$(grep '^SERVICE_URL=' deploy.log | cut -d= -f2-)" >> "$GITHUB_OUTPUT"`); 잡 `production`(`needs: staging`, `environment: { name: production, url: … }`, 같은 스텝으로 `--env prod --sha ${{ github.sha }}` — 같은 SHA 이미지라 빌드 생략, D-05). `secrets: inherit` 불필요(키 파일 없음).
    4. `.github/workflows/account.yml`: `workflow_dispatch` inputs `env`(choice staging|production), `action`(choice create|reset|unlock), `email`(string), `name`(string, create만), `admin`(boolean, 기본 false); `environment: ${{ inputs.env }}`(production은 승인 필요); WIF 인증 → `ENV_SHORT`(production→prod) → `gcloud run jobs execute erp-$ENV_SHORT-account --region --project --args="${{ inputs.action }},--email=${{ inputs.email }}[,--name=…][,--admin]" --wait` → `gcloud logging read 'resource.type="cloud_run_job" AND resource.labels.job_name="erp-'$ENV_SHORT'-account"' --project --freshness=15m --limit=30 --format='value(textPayload)'`로 출력(임시 비밀번호는 여기 한 번 보인다 — 워크플로 요약에 "즉시 로그인해 변경" 경고 문구 `echo`). 입력은 따옴표로 감싸 인젝션 방지(`${{ }}`를 env로 넘겨 셸에서 `"$EMAIL"`로 참조).
    5. 테스트 먼저 → 파일 → 통과. `bash -n scripts/bootstrap-gcp.sh`.
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-gcp.sh && pnpm vitest run --project unit test/unit/deploy/bootstrap-sh.test.ts test/unit/deploy/workflows.test.ts test/unit/ci-guard.test.ts</automated>
    <fails_when>non-zero exit, or a vitest summary line containing "failed" or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/bootstrap-gcp.sh`에 `workload-identity-pools`, `create-oidc`, `attribute-condition`, `roles/iam.workloadIdentityUser`, `vpc-peerings connect`, `org-policies describe`, `domain-mappings list`가 있고 `GCP_PROJECT_NUMBER=` 출력이 있다
    - `infra/monitoring/tick-stale.json.tpl`에 `"enabled": false`, `5xx-ratio.json.tpl`에 `denominatorFilter`와 `0.05`, `backup-failed.json.tpl`에 `conditionMatchedLog`가 있다
    - `.github/workflows/deploy.yml`에 `environment:`가 2회(staging·production), `--env prod`, `workflow_identity_provider`가 아닌 정확한 키 `workload_identity_provider`가 있다
    - `.github/workflows/account.yml`에 `erp-`·`--args`·`gcloud logging read`가 있다
    - `grep -c -E '[0-9]{12}' scripts/bootstrap-gcp.sh .github/workflows/deploy.yml .github/workflows/account.yml infra/monitoring/*.tpl`가 파일마다 0
    - 위 verify 명령 통과(ci-guard가 새 워크플로 2개에도 push 하위 명령 부재를 확인)
  </acceptance_criteria>
  <done>사용자가 Cloud Shell에서 한 번 돌릴 부트스트랩, 경보 정책 3개 정의, 그리고 승인 버튼 하나로 스테이징→프로덕션이 이어지는 워크플로가 정의되고 가짜 심·텍스트 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions(OIDC 토큰) → GCP WIF → `gha-deployer` | 키 파일 없는 배포 인증 경계 |
| deploy.sh → GCP 리소스 생성/변경 | 인프라 변경 권한 |
| 인터넷 → Cloud Run(`--allow-unauthenticated`) | 앱 공개 경계(인증은 앱 안) |
| 워크플로 입력(account.yml) → 셸 | 사용자 입력 인젝션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-29 | Spoofing | 다른 리포/포크가 WIF로 배포자 SA를 빌림 | high | mitigate | 프로바이더 `attribute-condition`이 정확한 `assertion.repository == 'owner/name'`, `workloadIdentityUser` 바인딩도 `attribute.repository/<repo>`로 한정, 테스트 단언 |
| T-1-30 | Elevation of Privilege | 배포자 SA 과다 권한(A6) | medium | accept | 솔로 프로젝트에서 인프라를 스크립트가 만들므로 admin 계열이 필요; 01-08 문서에 Policy Analyzer로 좁히는 절차 기록(RESEARCH Open Q3) |
| T-1-31 | Tampering | 더티 트리·검증 안 된 코드 배포 | high | mitigate | `require_clean_tree` exit 2, deploy.yml은 `ci` 잡 성공을 `needs`로 요구, 이미지 태그 = git SHA |
| T-1-32 | Tampering | account.yml 입력으로 셸 인젝션 | medium | mitigate | `${{ inputs.* }}`를 `env:`로 넘겨 셸에서 따옴표 변수로만 사용, `--args`는 쉼표 분리 인자 |
| T-1-33 | Information Disclosure | 워크플로 로그의 임시 비밀번호 | medium | accept | 비공개 리포·협업자 1인, 로그 보존 90일, D-08 배너와 문서로 즉시 변경 유도; Phase 3 관리 화면이 CLI를 대체 |
| T-1-34 | Information Disclosure | `set -x`·echo로 시크릿 값 노출 | high | mitigate | deploy.sh에 `set -x` 금지(acceptance grep), 시크릿 값은 `--data-file=-` 파이프로만 전달 |
| T-1-35 | Denial of Service | 스모크 실패 리비전에 트래픽 | high | mitigate | 기존 서비스는 `--no-traffic` 리비전 + 스모크 통과 뒤에만 `--to-latest`, 실패 시 exit 1·트래픽 불변, `rollback.sh` |
| T-1-36 | Elevation of Privilege | 조직 정책이 `allUsers`를 막는데 우회 시도 | low | accept | 우회하지 않고 명확한 오류로 중단, 조직 관리자에게 요청(PROJECT.md 제약) |
</threat_model>

## Flagged Assumptions (spec-less edge probe)

| requirement_id | category | status | 메모 |
|---|---|---|---|
| OPS-02 | unclassified | unresolved | 이 플랜은 min-instances 0·max-instances 3·db-f1-micro·HDD 10GB를 고정한다. 실제 월 금액의 경계($30)는 01-08에서 청구서로 사용자가 확인(수동) |

### 실행 시 재검증할 [ASSUMED] 항목 (01-07이 실제 gcloud 출력으로 확인)
- Cloud Run 결정적 URL 형식 `https://<svc>-<project_number>.<region>.run.app` 및 태그 URL `rev-<sha8>---…` — deploy.sh는 describe 결과로 재확인한다
- 새 서비스에 `--no-traffic`이 허용되지 않는다는 전제로 첫 배포는 100%로 간다
- `backup-failed` 로그 필터, `roles/cloudsql.viewer`의 `backupRuns.list` 포함(A3), 프라이빗 IP + Direct VPC egress 조합, `iam_authentication` 플래그와 `--type=cloud_iam_service_account` 사용자 이름 형식

## Artifacts this phase produces (this plan)

- 새 파일: `infra/names.sh`, `scripts/deploy.sh`, `scripts/rollback.sh`, `scripts/bootstrap-gcp.sh`, `infra/monitoring/{5xx-ratio,backup-failed,tick-stale}.json.tpl`, `.github/workflows/deploy.yml`, `.github/workflows/account.yml`, `test/unit/deploy/fakebin/{gcloud,docker,curl}`, `test/unit/deploy/{deploy-sh,rollback-sh,bootstrap-sh,workflows}.test.ts`
- 심볼(bash 함수): `svc_name`, `sql_instance`, `runtime_sa`, `job_name`, `secret_name`, `image_path`, `alert_channel`, `policy_5xx`, `policy_backup`, `policy_tick`, `tick_metric`; deploy.sh 함수 `parse_args`, `require_clean_tree`, `resolve_project_number`, `ensure_apis`, `ensure_ar_repo`, `ensure_network`, `ensure_sql_instance`, `ensure_sql_db_users`, `ensure_secrets`, `build_and_push_image`, `deploy_jobs`, `run_db_bootstrap`, `run_migrate`, `deploy_service`, `smoke`, `promote`, `ensure_alerts`, `map_domain`
- CLI 플래그: deploy.sh `--env --project --region --sha --domain --allow-dirty --dry-run`; rollback.sh `--env --project --region`; bootstrap-gcp.sh `--project --github-repo --region`
- 상수: `MAX_INSTANCES=3`, `DB_POOL_MAX=5`, `DB_TIER=db-f1-micro`, `DB_VERSION=POSTGRES_16`, `DB_STORAGE_GB=10`, `WIF_POOL=github`, `WIF_PROVIDER=erp-repo`, `DEPLOYER_SA=gha-deployer`
- GCP 리소스 이름 규칙: `erp-{env}`, `erp-{env}-db`, `erp-{env}-runtime`, `erp-{env}-{migrate|db-bootstrap|account}`, 시크릿 `{base}-{env}`, AR `erp`, 로그 지표 `notify_tick_success_{env}`, 정책 `[env] …`
- 워크플로: `deploy`(잡 ci/staging/production), `account`(inputs env/action/email/name/admin); GitHub 변수 계약 `GCP_PROJECT_ID`, `GCP_PROJECT_NUMBER`, `GCP_REGION`, `ALERT_EMAIL`; 환경 `staging`, `production`

<verification>
- `pnpm test:unit` exit 0 (deploy 디렉터리 테스트 4개 + ci-guard 포함)
- `bash -n` 스크립트 3개 exit 0
</verification>

<success_criteria>
- ROADMAP 기준 1·6의 deploy.sh 계약(인프라 ensure·SHA 이미지·더티 트리 거부·migrate Job·0%→스모크→100%·rollback·16A·경보 3개·프로젝트/리전 인자)이 스크립트로 존재하고 가짜 gcloud 테스트로 순서가 고정됐다(OPS-01)
- D-02·D-05의 GitHub Actions + WIF + 승인 게이트 워크플로가 정의됐다
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-06-SUMMARY.md` when done — shellcheck 실행 여부, 가짜 심이 흉내 낸 gcloud 하위 명령 목록(실제 실행 시 차이가 날 수 있는 지점)을 기록.
</output>
