---
phase: 01-deploy-skeleton-login
plan: 07
type: execute
wave: 6
depends_on: ["01-03", "01-05", "01-06"]
files_modified:
  - infra/names.sh
  - .planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md
autonomous: false
requirements: [OPS-01, OPS-06]
user_setup:
  - service: gcp
    why: "회사 GCP 프로젝트에 WIF·서비스 계정·VPC 피어링을 만드는 최초 1회 부트스트랩은 GitHub Actions가 스스로 할 수 없다 (D-02)"
    env_vars:
      - name: GCP_PROJECT_ID
        source: "GitHub → Settings → Secrets and variables → Actions → Variables (bootstrap-gcp.sh 출력값)"
      - name: GCP_PROJECT_NUMBER
        source: "같은 곳 (bootstrap-gcp.sh 출력값)"
      - name: GCP_REGION
        source: "같은 곳, 값 asia-northeast3"
      - name: ALERT_EMAIL
        source: "같은 곳, 경보 수신 주소 (D-16)"
    dashboard_config:
      - task: "회사 조직 아래 GCP 프로젝트 1개 생성(스테이징·프로덕션은 그 안, D-04) + 회사 결제 계정 연결 + 본인 Owner 확인 — 스크립트는 프로젝트를 만들지 않는다 (D-03)"
        location: "console.cloud.google.com → IAM & Admin → Create Project / Billing → Link a billing account"
      - task: "Cloud Shell에서 scripts/bootstrap-gcp.sh 실행 (Owner 권한)"
        location: "console.cloud.google.com → Cloud Shell"
      - task: "저장소 수준 Actions Variables 4개(GCP_PROJECT_ID·GCP_PROJECT_NUMBER·GCP_REGION·ALERT_EMAIL) 등록; Secrets는 없음(WIF) — GitHub Environments는 만들지 않는다 (D-05, 무료 플랜 비공개 저장소)"
        location: "GitHub → Settings → Secrets and variables → Actions → Variables"
      - task: "write 이상 권한 협업자가 본인뿐인지 확인 — production 워크플로 수동 실행 권한의 전부 (D-05, T-1-45)"
        location: "GitHub → Settings → Collaborators and teams"
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GitHub Actions deploy 워크플로(스테이징)가 성공해 회사 GCP 프로젝트에 Artifact Registry·Cloud SQL erp-staging-db·Secret Manager 시크릿·Cloud Run erp-staging·Job 3개·경보 3개가 존재하고 https://erp-staging-<number>.asia-northeast3.run.app/healthz 가 ok:true 와 배포 SHA를 반환한다 (OPS-01, ROADMAP 기준 1·6, OV-6 게이트)"
    - "migrate Job 로그에 db.max_connections 실측값이 남고 16A 검사를 통과했다 (A2 검증)"
    - "account 워크플로로 만든 관리자 계정으로 스테이징에 로그인해 비밀번호를 바꾸고 로그아웃할 수 있다 (AUTH-01/02/03 on deployed URL, D-11, D-12)"
    - "스테이징 /admin/system-status 가 관리자에게 배포 SHA·DB 커넥션/max_connections·마지막 백업 상태(또는 첫 백업 전이면 '확인 불가'가 아닌 '백업 없음' — 01-03 `backup.kind === 'none'`)를 보이고 직원 계정은 404를 받는다 (OPS-06, D-17, D-18, A3 검증)"
    - "회사 GCP 조직 정책 확인 결과(비인증 ingress 허용 여부·외부 링크)가 기록됐다 (ROADMAP 기준 1, OV-3)"
  artifacts:
    - path: ".planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md"
      provides: "첫 스테이징 배포 기록: 워크플로 실행 URL, 서비스 URL 형식(숫자는 변수명으로), max_connections 실측, 조직 정책 결과, A1·A3 검증 결과, 실패·수정 이력"
      min_lines: 20
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GCP (WIF)"
      via: "google-github-actions/auth@v3 with vars.GCP_PROJECT_NUMBER / vars.GCP_PROJECT_ID"
      pattern: "vars\\.GCP_PROJECT_NUMBER"
  prohibitions:
    - statement: "MUST NOT write the real GCP project ID/number, service URL host, or admin email into any committed file including the deploy log — record them as variable names or masked (D-03, D-12)"
      status: resolved
      verification: judgment
---

<objective>
실제 회사 GCP에서 첫 배포를 성공시킨다: 리소스 이름 확정(one-way 결정) → 사용자가 Cloud Shell에서 `bootstrap-gcp.sh` 실행 + GitHub 변수·환경 설정 → deploy 워크플로(스테이징)를 실행해 통과시키고 → account 워크플로로 관리자·테스트 직원 계정을 만들고 → 배포된 URL에서 로그인·비밀번호 변경·로그아웃·상태 화면을 확인한다. 실패하면 스크립트를 고쳐 다시 푸시한다(이 플랜의 대부분은 이 루프다).

Purpose: ROADMAP 기준 1·6·7과 CEO OV-6 게이트("회사 GCP에서 deploy.sh 1회 성공")를 실제로 충족한다. 01-05·01-06의 [ASSUMED] 항목을 실측으로 바꾼다.
Output: 동작하는 `erp-staging`, 실측 기록 `01-07-DEPLOY-LOG.md`, 필요 시 스크립트 수정 커밋.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-RESEARCH.md
@.planning/phases/01-deploy-skeleton-login/01-05-SUMMARY.md
@.planning/phases/01-deploy-skeleton-login/01-06-SUMMARY.md
@infra/names.sh
@scripts/deploy.sh
@scripts/bootstrap-gcp.sh
@.github/workflows/deploy.yml
@.github/workflows/account.yml
@docs/OPERATIONS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: GCP 리소스 이름·사양 확정 (되돌릴 수 없는 문)</name>
  <read_first>
    - infra/names.sh (01-06 — 이름 규칙 함수·상수)
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-04(환경 2개, 예시 이름 `erp-staging`·`erp-prod`), D-06(최소 사양·월 $30)
  </read_first>
  <decision>회사 GCP에 처음 만들 리소스 이름과 사양을 확정한다 — Cloud SQL 인스턴스 이름은 만든 뒤 바꿀 수 없고(새 인스턴스 + 데이터 이전), 인스턴스 이름은 삭제 후 약 1주간 재사용 불가하다.</decision>
  <context>01-06 `infra/names.sh`의 기본값이 Option A다. 서비스·Job·시크릿·SA 이름은 다시 만들면 되지만 Cloud SQL 인스턴스 이름과 DB 이름(`erp`)은 데이터가 쌓인 뒤에는 이전 작업이 필요하다(one-way). 사양(db-f1-micro, HDD 10GB, ZONAL, 자동 백업 7개 보존)은 나중에 patch로 바꿀 수 있다(reversible). 예상 월 비용: 두 환경 합계 약 $20~25(RESEARCH 근거, [ASSUMED] — 첫 청구서에서 확인).</context>
  <options>
    <option id="option-a">
      <name>A. 접두어 `erp-` (기본, 권장) — 서비스 erp-staging/erp-prod, 인스턴스 erp-staging-db/erp-prod-db, DB `erp`, SA erp-{env}-runtime, AR 저장소 `erp`</name>
      <pros>짧고 D-04의 예시와 일치, 스크립트·문서·테스트가 이미 이 이름으로 작성됨</pros>
      <cons>같은 GCP 프로젝트에 다른 시스템이 들어오면 `erp`가 모호할 수 있음</cons>
    </option>
    <option id="option-b">
      <name>B. 접두어 `plant8-erp-` — plant8-erp-staging/plant8-erp-prod, plant8-erp-staging-db/…, DB `plant8_erp`</name>
      <pros>프로젝트에 다른 워크로드가 생겨도 명확</pros>
      <cons>`infra/names.sh`·테스트·OPERATIONS.md의 이름을 바꾸는 커밋이 하나 더 필요(실행자가 수행)</cons>
    </option>
  </options>
  <resume-signal>"A" 또는 "B"(또는 원하는 접두어와 DB 이름을 직접 적기). 사양 변경 요청이 있으면 함께 적기</resume-signal>
  <acceptance_criteria>
    - 사용자가 옵션을 골랐다; B 또는 커스텀이면 실행자가 `infra/names.sh`(+ 이름을 단언하는 테스트·docs/OPERATIONS.md의 환경 표)를 바꾸고 `pnpm test:unit`이 통과한 뒤 커밋했다
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 사용자가 Cloud Shell에서 부트스트랩 실행 + GitHub 저장소 변수 설정</name>
  <read_first>
    - scripts/bootstrap-gcp.sh (01-06 — 사용자가 실행할 스크립트; 출력 마지막의 GitHub 안내 3줄)
    - docs/OPERATIONS.md §부트스트랩 예외 (01-04)
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-02, D-03, D-05
  </read_first>
  <action>전제(D-03): 회사 GCP 프로젝트는 사용자가 콘솔에서 직접 만든다 — 스크립트는 프로젝트를 만들지 않는다. 그 위에서 GitHub Actions가 스스로 만들 수 없는 것(D-02): WIF 풀·프로바이더, 배포자·런타임 서비스 계정, VPC 프라이빗 서비스 접근, 조직 정책 확인. 그리고 GitHub 저장소 수준 Actions 변수 4개(GitHub Environments는 만들지 않는다 — 무료 플랜 비공개 저장소, D-05).</action>
  <instructions>
    1. 실행자가 먼저 현재 브랜치를 origin에 푸시했는지 확인한다(`git push -u origin HEAD`; force 금지). Cloud Shell은 리포를 클론하지 않고 스크립트 파일만 필요하므로, 실행자는 아래 명령을 안내한다.
    2. 전제 조건 체크리스트(D-03 — 부트스트랩을 실행하기 전에 사용자가 콘솔에서 끝내 둔다; 스크립트는 프로젝트를 만들지 않으며 하나라도 아니면 첫 단계에서 exit 1로 멈춘다):
       - [ ] 회사 GCP 조직 아래 프로젝트 1개가 존재한다(스테이징·프로덕션은 이 프로젝트 안에서 분리 — D-04; 프로젝트 2개 아님). 확인: 콘솔 프로젝트 선택기에 보이거나 Cloud Shell에서 `gcloud projects describe <프로젝트 ID>`가 성공
       - [ ] 회사 결제 계정이 연결돼 있다. 확인: `gcloud billing projects describe <프로젝트 ID>` 출력에 `billingEnabled: true`
       - [ ] 본인 계정이 그 프로젝트의 Owner다(조직 Owner 권한은 확보됨 — D-03). 확인: 콘솔 IAM 페이지에서 본인 계정의 역할이 Owner
       셋 다 확인되면 3으로 간다. 아니면 콘솔에서 프로젝트 생성·결제 연결·역할 부여를 먼저 하고 돌아온다(실행자에게 "프로젝트 없음"을 알리면 실행자는 대기한다 — 스크립트로 우회하지 않는다).
    3. https://console.cloud.google.com 에서 회사 프로젝트를 선택하고 Cloud Shell을 연다. `scripts/bootstrap-gcp.sh`는 다른 파일을 참조하지 않는 단일 파일이므로(01-06) 리포를 클론할 필요가 없다 — 실행자가 채팅에 파일 내용을 그대로 붙여 주면 Cloud Shell 편집기에 `bootstrap-gcp.sh`로 저장한다(비공개 리포 clone은 GitHub 토큰이 필요해 권하지 않는다; 원하면 `git clone https://github.com/rrangjaa-eng/ERP_PLANT8_260917.git && cd ERP_PLANT8_260917`도 된다).
    4. 실행: `bash scripts/bootstrap-gcp.sh --project <회사 프로젝트 ID> --github-repo rrangjaa-eng/ERP_PLANT8_260917`
       - 스크립트는 첫 단계에서 프로젝트 존재·결제 연결을 다시 검사하고(01-06), 실패하면 아무 리소스도 만들지 않고 `not found`/`billing` 메시지로 멈춘다 — 2의 체크리스트로 돌아간다.
       - 성공하면 마지막에 `GCP_PROJECT_ID=…`, `GCP_PROJECT_NUMBER=…`, `GCP_REGION=asia-northeast3`와 조직 정책 확인 결과, 도메인 매핑 리전 확인 결과가 출력된다. 이 출력 전체를 복사해 둔다(값은 채팅으로만 전달, 리포에 넣지 않음).
       - 실패하면 오류 메시지를 그대로 실행자에게 전달한다(권한·조직 정책·API 활성화 문제가 대부분; 실행자가 스크립트를 고치면 다시 실행).
    5. GitHub 리포 → Settings → Secrets and variables → Actions → **Variables**(Repository variables — 저장소 수준, Environment 변수가 아니다)에 4개 추가: `GCP_PROJECT_ID`, `GCP_PROJECT_NUMBER`, `GCP_REGION`(=asia-northeast3), `ALERT_EMAIL`(경보 받을 주소). 이름은 01-06 `deploy.yml`·`account.yml`의 `vars.*` 참조와 정확히 같아야 한다. **Secrets** 탭에는 넣을 것이 없다(WIF — 키 파일 없음, D-02).
    6. GitHub Environments는 만들지 않는다(D-05: 무료 플랜 비공개 저장소에는 기능이 없고, 프로덕션은 workflow_dispatch 수동 실행 + 스테이징 SHA 가드로 지킨다). 대신 Settings → Collaborators and teams에서 write 이상 권한을 가진 계정이 본인뿐인지 확인한다 — 이것이 production 워크플로 실행 권한의 전부다(T-1-45).
    7. 실행자에게 알릴 것: (a) 부트스트랩 출력 전문(조직 정책 결과 포함), (b) 변수 4개 설정 완료, (c) Secrets 비어 있음 + write 협업자가 본인뿐임, (d) 관리자 계정으로 쓸 이메일·표시 이름과 테스트 직원 계정의 이메일·표시 이름(D-12: 이 둘만 만든다), (e) 2의 체크리스트 셋을 확인했다는 한 줄(프로젝트 ID 값은 채팅으로만).
  </instructions>
  <verification>
    실행자는 사용자 보고를 받은 뒤(gcloud 없이) 다음을 확인한다: D-03 전제 조건 셋(프로젝트 존재·결제 연결 `billingEnabled: true`·Owner)을 확인했다는 보고가 있는지(부트스트랩 출력이 사전 검사를 지나 `GCP_PROJECT_NUMBER=`까지 갔다면 앞의 둘은 스크립트가 증명한 것); 보고된 부트스트랩 출력에 `GCP_PROJECT_ID=`·`GCP_PROJECT_NUMBER=`·`GCP_REGION=asia-northeast3` 세 줄과 `org-policies` 절이 있는지; 변수 4개 이름이 정확한지(`GCP_PROJECT_ID`·`GCP_PROJECT_NUMBER`·`GCP_REGION`·`ALERT_EMAIL`); `gh`가 있으면 `gh api repos/rrangjaa-eng/ERP_PLANT8_260917/actions/variables --jq '.variables[].name'`으로 변수 4개를, `gh api repos/rrangjaa-eng/ERP_PLANT8_260917/collaborators --jq '.[] | select(.permissions.push) | .login'`으로 write 협업자가 본인뿐임을 직접 확인(없으면 GitHub MCP 또는 사용자 보고로 갈음; Environments API는 호출하지 않는다 — 쓰지 않는다, D-05); 계정 정보 2건을 받았고 리포에 적지 않았는지.
  </verification>
  <resume-signal>"done" + 위 (a)~(d)</resume-signal>
  <acceptance_criteria>
    - 사용자가 D-03 전제 조건 셋(회사 조직 아래 프로젝트 1개 존재·회사 결제 계정 연결·본인 Owner)을 확인했다고 보고했다 — 프로젝트 생성은 사용자 몫이며 어떤 태스크·스크립트도 프로젝트를 만들지 않는다
    - 사용자가 부트스트랩 출력(GCP_PROJECT_NUMBER 포함)과 조직 정책 결과를 전달했다
    - 저장소 수준 변수 4개(`GCP_PROJECT_ID`·`GCP_PROJECT_NUMBER`·`GCP_REGION`·`ALERT_EMAIL`)가 설정됐고 Secrets는 비어 있으며 write 협업자가 본인뿐임을 확인했다 — GitHub Environment는 하나도 만들지 않았다(D-05)
    - 관리자·테스트 직원 계정 정보 2건을 받았다(리포에 기록하지 않는다)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 스테이징 첫 배포 실행 → 계정 발급 → 배포된 앱 검증 → 실측 기록</name>
  <precondition>GitHub 저장소 수준 Actions 변수 GCP_PROJECT_ID·GCP_PROJECT_NUMBER·GCP_REGION·ALERT_EMAIL이 설정돼 있다 (Task 2 완료 보고; GitHub Environments 없음 — D-05)</precondition>
  <files>.planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md, infra/names.sh</files>
  <read_first>
    - scripts/deploy.sh, .github/workflows/deploy.yml, .github/workflows/account.yml (01-06 — 실행 대상)
    - .planning/phases/01-deploy-skeleton-login/01-06-PLAN.md §실행 시 재검증할 [ASSUMED] 항목
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Assumptions Log A1·A2·A3·A6
    - docs/OPERATIONS.md §배포 런북 (01-04 — 실측과 다르면 01-08이 고친다; 이 태스크는 DEPLOY-LOG에 차이를 적는다)
  </read_first>
  <action>
    OPS-01·OPS-06·OV-6 게이트를 실제로 충족한다. 실행자는 이 세션에 gcloud가 없다(클라우드 세션) — 모든 GCP 조작은 GitHub Actions 워크플로를 통해서만 한다.
    1. 워크플로 실행: `gh`가 있고 인증돼 있으면 `gh workflow run deploy.yml --ref <현재 브랜치> -f target=staging` 후 `gh run watch`; `gh`가 없으면 GitHub MCP 도구(`run_workflow` → `get_workflow_run`/`get_job_logs`)로 같은 일을 한다; 둘 다 불가하면 `checkpoint:human-action`으로 사용자에게 Actions 탭에서 "deploy → Run workflow → target=staging"을 눌러 달라고 요청하고 실행 URL을 받는다.
    2. 실패 루프: 잡 로그를 읽어 실패 단계(`ci` / `staging`의 deploy.sh 단계 이름)를 특정하고, 스크립트·워크플로·코드를 고쳐 커밋(`fix(deploy): …` 제목 영어·본문 한국어)·푸시하고 다시 실행한다. 조직 정책이 비인증 ingress를 막으면(deploy.sh가 "org policy blocks unauthenticated ingress"로 중단) 사용자에게 알리고 조직 관리자 조치를 기다린다 — 우회 금지. 성공할 때까지 반복하되 매 시도의 원인·수정을 DEPLOY-LOG에 적는다.
    3. 스테이징 성공 후 검증(공개 URL은 이 세션에서 HTTPS로 접근 가능): `SERVICE_URL`(워크플로 출력, 결정적 형식)에 대해 `curl -fsS $SERVICE_URL/healthz`가 `"ok":true`와 `"sha":"<커밋 SHA>"`를 포함, `curl -s -o /dev/null -w '%{http_code}' $SERVICE_URL/login`이 200. 워크플로 로그에서 migrate Job 실행의 `db.max_connections` 값과 경보 3개 create/update 줄, `note: describe url differs` 줄 유무(레거시 URL이 따로 있는지)를 확인한다. 이어서 **클라이언트 IP 규칙 실측**(01-02 Eng Issue 1의 전제 — Cloud Run이 XFF를 뒤에 붙이는지): 같은 세션에서 `for i in $(seq 1 11); do curl -s -o /dev/null -w '%{http_code}\n' -X POST -H "Origin: $SERVICE_URL" -H "X-Forwarded-For: 10.0.0.$i" -H 'content-type: application/json' -d '{"email":"probe@example.invalid","password":"x"}' $SERVICE_URL/api/auth/sign-in/email; done` — 위조 XFF를 요청마다 바꿔도 11번째가 `429`여야 한다(가짜 첫 항목이 무시되고 실제 IP로 묶임). 429가 아니면 `lib/client-ip.ts`의 항목 선택(마지막 항목)이 Cloud Run의 실제 헤더 형태와 다른 것이다 — proxy.ts에 `log.info('auth.xff_probe', { xff })`를 임시로 넣어 한 번 배포하고 실제 `x-forwarded-for` 값을 DEPLOY-LOG에(IP는 마스킹) 기록한 뒤 규칙을 고치고 임시 로그를 지워 재배포한다. 또 Cloud Logging에 better-auth의 `no-trusted-ip` 경고가 없어야 한다(있으면 proxy.ts가 라우트에 적용되지 않은 것 — fail-closed 500이 함께 보일 것이다).
    4. 계정 발급(D-11·D-12): `account.yml`을 `env=staging action=create email=<관리자> name=<이름> admin=true`로 실행, 이어서 테스트 직원(`admin=false`). 워크플로 로그의 임시 비밀번호는 사용자에게만 전달한다(리포·DEPLOY-LOG 금지).
    5. `01-07-DEPLOY-LOG.md` 작성(값은 마스킹/변수명): 실행 URL·시도 횟수·원인·수정, 서비스 URL 형식(`https://erp-staging-<GCP_PROJECT_NUMBER>.asia-northeast3.run.app` 형식 확인 여부 — A 결정적 URL 검증), `max_connections` 실측(A2: 25인지), 새 서비스 첫 배포 방식(100%인지), 조직 정책 결과(비인증 ingress·외부 링크), 도메인 매핑 리전 확인 결과(A1), 프라이빗 IP·IAM 연결 성공 여부, `backup-failed` 정책이 생성됐는지(필터가 실제 로그 항목과 맞는지는 첫 백업 창(18:00 UTC) 뒤에만 볼 수 있으므로 여기서는 "미확인"으로 두고 01-08 human-check 4가 닫는다 — 실패 이벤트 없이는 검증할 수 없는 경보라는 점을 명시), XFF 실측 결과(11번째 429 여부, `describe url differs` 여부), 상태 화면 백업 절의 결과(A3: `permission` 이유가 나오면 역할 조정 필요 → 01-08에서 처리), deploy.yml 가드 전제(01-06 [ASSUMED]: 스테이징 `status.traffic[]`에 percent 100 항목이 하나인지, 서빙 리비전의 `spec.containers[0].image`가 `…/app:<sha>` 태그 문자열을 보존하는지 — GCP 콘솔 Cloud Run → erp-staging → 리비전/트래픽 탭에서 확인해 기록; 01-08 가드 스텝 요약이 최종 확정).
    6. 이 실행에서 `production` 잡은 실행되지 않는다(`workflow_dispatch` + `target=production` 전용, D-05) — 프로덕션 배포는 01-08에서 사용자가 스테이징 SHA로 수동 실행한다. DEPLOY-LOG에 스테이징이 서빙 중인 SHA(= 이 실행의 커밋 SHA)를 적어 01-08 Task 1의 입력으로 넘긴다.
  </action>
  <verify>
    <automated>curl -fsS "$SERVICE_URL/healthz" | grep -q '"ok":true' && curl -s -o /dev/null -w '%{http_code}' "$SERVICE_URL/login" | grep -q '^200$' && test -s .planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md</automated>
    <fails_when>non-zero exit (healthz without "ok":true, /login not 200, or an empty deploy log); SERVICE_URL은 워크플로 출력에서 export한 값</fails_when>
    <human-check>
      1. 브라우저에서 스테이징 URL `/login`에 관리자 임시 비밀번호로 로그인 → `/account`에 임시 비밀번호 배너가 보이는지
      2. 비밀번호 변경 → `/login`으로 돌아오고 새 비밀번호로만 로그인되는지(옛 비밀번호 실패)
      3. `/admin/system-status`에 배포 SHA(워크플로의 커밋과 일치)·DB 커넥션 `n / max_connections`·마지막 백업 절이 보이는지(첫 자동 백업 전이면 백업 목록이 비어 "확인 불가"가 아닌 "백업 없음"(01-03 `backup.kind === 'none'`)이 표시되는지, `permission` 이유가 아닌지)
      4. 테스트 직원 계정으로 로그인해 `/admin/system-status`가 404인지, 로그아웃이 되는지
      5. 브라우저를 완전히 닫았다 다시 열어 `/account`가 로그인 상태인지(D-07)
      6. GCP 콘솔 Monitoring → Alerting에 `[staging] 5xx ratio > 5%`, `[staging] Cloud SQL backup failed`(enabled), `[staging] notify tick stale 24h`(disabled) 3개와 이메일 채널이 있는지
    </human-check>
  </verify>
  <acceptance_criteria>
    - deploy 워크플로의 `staging` 잡이 success로 끝난 실행 URL이 DEPLOY-LOG에 있다
    - `curl $SERVICE_URL/healthz` 응답에 `"ok":true`와 배포 커밋 SHA가 있다
    - DEPLOY-LOG에 `max_connections` 실측값, 조직 정책 결과, A1·A3 확인 결과, 시도별 원인·수정이 있고 `grep -c -E '[0-9]{12}' 01-07-DEPLOY-LOG.md`가 0이다
    - account 워크플로 실행 2건(관리자·직원)이 success다
    - `git status --porcelain`이 비어 있다(수정은 전부 커밋·푸시됨)
  </acceptance_criteria>
  <done>회사 GCP 스테이징에 deploy.sh가 GitHub Actions에서 성공했고, 관리자·직원 계정이 발급됐으며, 배포된 URL에서 healthz·login이 응답하고 실측 기록이 남았다(OV-6 게이트 충족).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 Cloud Shell(Owner) → GCP | 최초 1회 부트스트랩 권한 |
| GitHub 변수/환경 → 워크플로 | 프로젝트 ID·번호·경보 주소(비밀 아님) |
| 채팅 → 실행자 | 임시 비밀번호·계정 이메일 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-37 | Information Disclosure | 부트스트랩 출력·계정 정보가 리포에 커밋됨 | high | mitigate | DEPLOY-LOG는 변수명·마스킹만(acceptance grep 12자리 숫자 0), 계정 정보는 채팅으로만 |
| T-1-38 | Elevation of Privilege | 잘못된 조직 정책 우회 시도 | medium | mitigate | deploy.sh 명확한 중단, 조직 관리자 요청 절차, 우회 금지 명시 |
| T-1-39 | Spoofing | 임시 비밀번호가 워크플로 로그에 남아 제3자 사용 | medium | mitigate | human-check 1~2에서 즉시 변경(D-08 배너), 로그 접근은 협업자 1인 |
| T-1-40 | Denial of Service | 첫 배포 실패 반복으로 리소스 반쯤 생성 | low | accept | deploy.sh 전 단계 멱등(describe || create), 재실행으로 수렴 |
</threat_model>

## Artifacts this phase produces (this plan)

- 새 파일: `.planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md`
- 수정 가능: `infra/names.sh`(Task 1 결정이 B/커스텀일 때), 실패 루프에서 고친 스크립트·워크플로(각각 커밋)
- GCP 리소스(코드 밖): Cloud Run `erp-staging` + Job 3개, Cloud SQL `erp-staging-db`, 시크릿 7개(`*-staging`), AR 저장소 `erp`, 경보 정책 3개·채널 1개, WIF 풀·프로바이더, SA 3개
- 계정: 관리자 1 + 테스트 직원 1 (스테이징)

<verification>
- deploy 워크플로 staging 잡 success, `$SERVICE_URL/healthz` ok, human-check 6항목
</verification>

<success_criteria>
- ROADMAP 기준 1(회사 GCP에 스크립트 한 번으로 기동·조직 정책 확인)·2(배포 URL 로그인·세션 유지·로그아웃)·6(migrate Job → 0% → 스모크 → 100%, 경보 3개)·7(상태 화면 실데이터)이 스테이징에서 사람 눈으로 확인됐다
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-07-SUMMARY.md` when done — DEPLOY-LOG 요약, [ASSUMED] 항목의 실측 결과, 01-08이 고쳐야 할 문서 차이.
</output>
