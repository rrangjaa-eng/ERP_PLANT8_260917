---
phase: 01-deploy-skeleton-login
plan: 08
type: execute
wave: 7
depends_on: ["01-07"]
files_modified:
  - docs/OPERATIONS.md
  - docs/ARCHITECTURE.md
  - CLAUDE.md
  - scripts/bootstrap-gcp.sh
  - .planning/phases/01-deploy-skeleton-login/01-08-DEPLOY-LOG.md
autonomous: false
requirements: [OPS-01, OPS-02, OPS-07]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "사용자가 deploy 워크플로를 workflow_dispatch(target=production, sha=스테이징 SHA 또는 빈 값)로 수동 실행하면 가드 스텝(이미지 존재 + 스테이징 서빙 태그 일치)을 지난 같은 git SHA 이미지가 erp-prod에 같은 순서(db-bootstrap → migrate → 리비전 → 스모크 → 100% → 경보)로 배포되고, 실행자·SHA·시각·가드 출력이 GitHub Actions 실행 로그에 남는다 — GitHub Environments·승인 버튼은 없다 (D-05, OPS-01)"
    - "프로덕션 URL의 /healthz 가 ok:true 와 스테이징과 같은 SHA를 반환하고, 관리자 계정으로 로그인·비밀번호 변경·로그아웃이 되며, 테스트 직원은 /admin/system-status 에서 404를 받는다 (ROADMAP 기준 2·7)"
    - "docs/OPERATIONS.md 가 실측(URL 형식·첫 배포 동작·max_connections·백업 시각·베이스 이미지 digest·배포자 역할 축소 절차·첫 청구서 확인일)으로 갱신되고 여전히 300줄 이하다 (OPS-07, OPS-02)"
    - "CLAUDE.md 명령 4개 자리(dev·test·lint·build)가 실제 pnpm 스크립트로 채워졌다 — Phase 1 완료 시점의 유일한 CLAUDE.md 수정 (STATE 블로커 해소)"
    - "월 비용 목표(두 환경 합계 $30 안팎)와 실제 과금 항목이 문서에 있고 첫 청구서 확인 절차가 적혀 있다 (OPS-02, D-06)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "명령 4개 확정"
      contains: "pnpm build"
    - path: "docs/OPERATIONS.md"
      provides: "실측 반영 런북 (≤300줄)"
      contains: "erp-prod"
    - path: ".planning/phases/01-deploy-skeleton-login/01-08-DEPLOY-LOG.md"
      provides: "프로덕션 수동 실행·가드·배포·검증 기록(마스킹)"
      min_lines: 15
  key_links:
    - from: "CLAUDE.md"
      to: "package.json"
      via: "명령 4개가 package.json scripts 이름과 일치"
      pattern: "pnpm (dev|test|lint|build)"
  prohibitions:
    - statement: "MUST NOT write the real GCP project ID/number, service URL host, or account emails into committed files; variable names or masked values only (D-03, D-12)"
      status: resolved
      verification: judgment
---

<objective>
프로덕션 승격을 실제로 한 번 수행하고("Run workflow" target=production → 가드 → 같은 SHA; GitHub Environments·승인 버튼 없음, D-05), 프로덕션에서 계정 발급·로그인·상태 화면을 확인한 뒤, 실측으로 운영 문서를 마무리하고 CLAUDE.md의 명령 4개를 채운다. 이 플랜이 끝나면 Phase 1의 모든 성공 기준이 두 환경에서 충족된다.

Purpose: D-05 승격 흐름의 실증, OPS-07 문서의 실측 반영, STATE 블로커(CLAUDE.md 명령 자리) 해소.
Output: 동작하는 `erp-prod`, 갱신된 `docs/OPERATIONS.md`(+ 필요 시 `docs/ARCHITECTURE.md`), 채워진 `CLAUDE.md`, `01-08-DEPLOY-LOG.md`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-skeleton-login/01-CONTEXT.md
@.planning/phases/01-deploy-skeleton-login/01-07-SUMMARY.md
@.planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md
@docs/OPERATIONS.md
@docs/ARCHITECTURE.md
@CLAUDE.md
@package.json
@.github/workflows/deploy.yml
@.github/workflows/account.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: GitHub에서 deploy 워크플로를 production으로 수동 실행 (같은 SHA 승격)</name>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md (스테이징 성공 실행 URL, 스테이징이 서빙 중인 SHA)
    - .planning/phases/01-deploy-skeleton-login/01-CONTEXT.md D-05
    - .github/workflows/deploy.yml (01-06 — `workflow_dispatch` 입력 `target`·`sha`, `production` 잡의 가드 스텝 메시지)
  </read_first>
  <action>D-05: 프로덕션은 사용자가 GitHub Actions에서 deploy 워크플로를 수동 실행할 때만 배포된다(GitHub Environments·승인 버튼 없음 — 무료 플랜 비공개 저장소). 가드 스텝이 지정 SHA의 이미지가 있고 스테이징이 그 SHA를 서빙 중인지 확인한 뒤 같은 이미지를 프로덕션에 배포한다(빌드 없음). 실행 기록은 GitHub Actions 로그가 남긴다.</action>
  <instructions>
    1. 01-07 human-check가 끝났고, 01-07 DEPLOY-LOG의 스테이징 서빙 SHA가 올리려는 커밋인지 확인한다.
    2. GitHub → Actions → 왼쪽 목록에서 `deploy` → 오른쪽 "Run workflow" 버튼 → Branch는 기본값(`main`; 아직 병합 전이면 스테이징을 올린 브랜치), `target`은 `production`, `sha`는 01-07의 스테이징 SHA를 붙여넣거나 비워 둔다(비우면 스테이징이 현재 서빙 중인 SHA가 기본값) → "Run workflow".
    3. 실행 페이지에서 `production` 잡의 가드 스텝 요약(`promoting <sha> (staging serving <sha>)`)이 보이고 배포 스텝이 끝날 때까지 기다린다. 가드가 exit 1로 멈추면(이미지 없음 / 스테이징이 다른 SHA를 서빙) 메시지를 실행자에게 그대로 전달한다 — 실행자가 먼저 그 SHA를 스테이징에 올린 뒤(`target=staging`) 다시 요청한다. 가드를 우회하는 경로는 없고 만들지 않는다.
    4. `production` 잡이 끝나면(success/failure) 결과와 실행 URL을 실행자에게 전달한다. 실패면 로그 링크도.
  </instructions>
  <verification>
    실행자는 보고된 실행 URL로 `production` 잡의 결론이 success인지, 실행 이벤트가 `workflow_dispatch`이고 실행자(triggering actor)가 사용자 계정인지, 가드 스텝 출력의 SHA가 01-07 스테이징 SHA와 같은지 확인한다 — `gh`가 있으면 `gh run view <run-id> --json conclusion,event,jobs --jq '.conclusion, .event, [.jobs[] | {name, conclusion}]'`와 `gh run view <run-id> --log | grep promoting`; 없으면 GitHub MCP `get_workflow_run`/`get_job_logs` 또는 사용자 보고. 이어서 production 잡 출력의 `SERVICE_URL`을 `PROD_URL`로 export해 `curl -fsS "$PROD_URL/healthz"`가 `"ok":true`와 같은 `sha`를 반환하는지 본다. 승인 기록은 없다 — 실행 로그가 그 자리를 대신한다(D-05).
  </verification>
  <resume-signal>"deployed" + production 잡 결과(success/failure)와 실행 URL</resume-signal>
  <acceptance_criteria>
    - `workflow_dispatch`(target=production)로 시작된 실행에서 `production` 잡이 success로 끝난 실행 URL을 받았다(실패면 실행자가 원인을 고쳐 스테이징부터 다시 올리고 수동 실행을 다시 요청한다)
    - 실행 페이지의 triggering actor가 사용자 본인이고, 가드 스텝 출력의 SHA가 01-07 스테이징 SHA와 같다 — GitHub Actions 실행 로그가 승인 기록을 대체한다(D-05)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 프로덕션 검증 — healthz·계정 발급·로그인·상태 화면·경보·백업·[ASSUMED] 마무리</name>
  <precondition>deploy 워크플로의 production 잡이 success로 끝났다 (Task 1 보고의 실행 URL)</precondition>
  <files>.planning/phases/01-deploy-skeleton-login/01-08-DEPLOY-LOG.md, scripts/bootstrap-gcp.sh</files>
  <read_first>
    - .planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md (스테이징에서 확인된 URL 형식·A1·A2·A3 결과)
    - .github/workflows/account.yml (production도 workflow_dispatch로 바로 실행 — 승인 단계 없음, D-05)
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Assumptions Log, §Pitfall 5 (A3 — cloudsql.viewer가 backupRuns.list를 포함하지 않으면 커스텀 역할)
  </read_first>
  <action>
    1. 프로덕션 URL(워크플로 `production` 잡 출력 `url`, 형식 `https://erp-prod-<GCP_PROJECT_NUMBER>.asia-northeast3.run.app`)에 `curl -fsS $PROD_URL/healthz` → `"ok":true` + 스테이징과 같은 `sha`; `/login` 200.
    2. `account.yml`을 `env=production`으로 관리자(admin=true)·테스트 직원 2건 실행 — 승인 단계는 없다(D-05; 실행 권한 = write 협업자 = 본인). 실행자가 `gh workflow run account.yml -f env=production …` 또는 GitHub MCP `run_workflow`로 실행하고, 둘 다 불가하면 사용자에게 Actions에서 Run workflow를 요청한다. 임시 비밀번호는 사용자에게만 전달(D-12·D-13).
    3. 경보·백업 확인: 워크플로 로그의 `ensure_alerts` 출력에서 `[prod]` 정책 3개·채널이 create/update 됐는지; Cloud SQL 자동 백업은 백업 창(18:00 UTC = 03:00 KST) 이후 첫 실행이 생기므로 시각을 DEPLOY-LOG에 적고 human-check로 다음날 확인을 요청한다.
    4. A3 처리: 스테이징·프로덕션 상태 화면의 "마지막 백업"이 `permission` 이유로 "확인 불가"면 `scripts/bootstrap-gcp.sh`의 런타임 SA 역할을 조정한다(우선 `roles/cloudsql.viewer` 유지 + 커스텀 역할 `erpBackupReader`(`cloudsql.backupRuns.list`, `cloudsql.instances.get`)를 만들어 바인딩하는 함수 추가) → 커밋 → 사용자가 Cloud Shell에서 `bash scripts/bootstrap-gcp.sh …`를 재실행(멱등)하도록 `checkpoint:human-action`을 동적으로 올린다 → 화면 재확인. 권한 문제가 없으면 A3를 "확인됨"으로 기록.
    5. `01-08-DEPLOY-LOG.md`: production 실행 URL·실행자(triggering actor = 본인)·가드 스텝 출력·SHA 일치 여부, 계정 발급 실행 2건, 경보 확인, 백업 창 시각, A3 결과, `docker inspect`가 불가하므로 워크플로 로그의 `docker build` 출력에서 베이스 이미지 digest(`node:24-slim@sha256:…`)를 옮겨 적는다(T-1-24 후속). 실제 프로젝트 ID·번호·이메일은 쓰지 않는다.
  </action>
  <verify>
    <automated>curl -fsS "$PROD_URL/healthz" | grep -q '"ok":true' && curl -s -o /dev/null -w '%{http_code}' "$PROD_URL/login" | grep -q '^200$' && test -s .planning/phases/01-deploy-skeleton-login/01-08-DEPLOY-LOG.md</automated>
    <fails_when>non-zero exit (healthz without "ok":true, /login not 200, or an empty deploy log); PROD_URL은 production 잡 출력에서 export한 값</fails_when>
    <human-check>
      1. 프로덕션 URL에 관리자 임시 비밀번호로 로그인 → 배너 → 비밀번호 변경 → 새 비밀번호로 재로그인 → 로그아웃
      2. 테스트 직원으로 로그인 → `/admin/system-status` 404 → 로그아웃
      3. 관리자로 `/admin/system-status`: 배포 SHA가 스테이징과 같고, DB 커넥션 `n / max_connections`가 보이며, 마지막 백업 절이 `permission` 이유가 아닌지
      4. 다음날(백업 창 이후) 같은 화면에서 마지막 백업이 성공 상태·시각으로 보이는지; GCP 콘솔 Cloud SQL → erp-prod-db → 백업 탭에 1건 이상
      5. GCP 콘솔 Monitoring → Alerting에 `[prod]` 정책 3개(tick만 disabled)와 이메일 채널; Billing → 보고서에서 프로젝트의 일별 비용이 두 환경 합계 월 $30 안팎으로 수렴하는지(첫 청구서 확인일을 OPERATIONS.md에 적음)
    </human-check>
  </verify>
  <acceptance_criteria>
    - `curl $PROD_URL/healthz`의 `sha`가 01-07 스테이징 SHA와 같다
    - account 워크플로 production 실행 2건이 success다
    - DEPLOY-LOG에 실행자(actor)·실행 URL·가드 출력·SHA 일치·경보 3개·백업 창 시각·A3 결과·베이스 이미지 digest가 있고 `grep -c -E '[0-9]{12}' 01-08-DEPLOY-LOG.md`가 0이다
    - A3가 `permission`이었으면 `scripts/bootstrap-gcp.sh`에 역할 조정이 커밋되고 재실행 뒤 화면이 백업 상태를 보인다
  </acceptance_criteria>
  <done>프로덕션이 사용자의 수동 실행(가드 통과)으로 같은 SHA로 배포되어 healthz·login이 응답하고, 관리자·직원 계정이 발급됐으며, 경보·백업·권한 가정이 실측으로 닫혔다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OPERATIONS.md 실측 반영 + ARCHITECTURE.md 점검 + CLAUDE.md 명령 4개 확정 (페이즈 마지막 커밋)</name>
  <files>docs/OPERATIONS.md, docs/ARCHITECTURE.md, CLAUDE.md</files>
  <read_first>
    - docs/OPERATIONS.md, docs/ARCHITECTURE.md (01-04 초판 — 실측과 다른 절만 고친다; 300줄 상한)
    - .planning/phases/01-deploy-skeleton-login/01-07-DEPLOY-LOG.md, 01-08-DEPLOY-LOG.md (실측)
    - CLAUDE.md ("명령: dev `[ ]` · test `[ ]` · lint `[ ]` · build `[ ]` — Phase 1(배포 스켈레톤) 완료 시 확정해 채운다" 줄; "이 파일과 @import 대상은 세션 중 수정 금지. 수정은 세션 끝에 몰아서" — 이 태스크가 그 시점이다)
    - package.json scripts (명령 4개의 실제 이름)
    - test/unit/docs-limits.test.ts (01-04 — 300줄 상한 테스트)
    - .planning/phases/01-deploy-skeleton-login/01-RESEARCH.md §Open Questions 3 (배포자 역할 축소 2단계 절차)
  </read_first>
  <behavior>
    - docs-limits.test.ts(기존)가 계속 통과: 두 문서 ≤ 300줄, 필수 문자열 유지
    - CLAUDE.md의 명령 줄이 정확히 `- 명령: dev \`pnpm db:dev && pnpm dev\` · test \`pnpm test\` · lint \`pnpm lint && pnpm typecheck && pnpm lint:sql\` · build \`pnpm build\``로 바뀌고 `[ ]` 자리가 남지 않는다
  </behavior>
  <action>
    OPS-07·OPS-02·D-06과 STATE 블로커(CLAUDE.md 명령)를 닫는다.
    1. `docs/OPERATIONS.md` 갱신(실측이 다른 곳만): 환경 표에 서비스 URL 형식(`https://erp-{env}-<GCP_PROJECT_NUMBER>.asia-northeast3.run.app`, 태그 리비전 URL 형식), 첫 배포는 100%·이후 0%→스모크→100%, migrate Job이 기록한 `max_connections` 실측과 16A 시작값(3×5 ≤ 실측−5) 재계산, 백업 창 시각(KST)과 확인 방법(상태 화면·콘솔), 경보 정책 이름 3개와 tick 비활성 사유(Phase 7에서 활성), 베이스 이미지 digest와 `@sha256` 고정을 검토할 시점, 배포자 SA 역할 축소 절차(Cloud Audit Logs Policy Analyzer로 실제 사용 권한 확인 → bootstrap-gcp.sh 역할 목록 축소 → 재실행; RESEARCH Open Q3), 계정 운영 절(account.yml 사용법·임시 비밀번호 즉시 변경·production도 승인 없음 — 실행 권한은 write 협업자), 승격 절(deploy 워크플로 Run workflow → target=production → sha 입력 규칙(빈 값 = 스테이징 서빙 SHA)·가드 메시지 2종과 대처·GitHub Environments 없음 — D-05), 비용 절에 첫 청구서 확인일(다음 달 초)과 $30 초과 시 스테이징 축소 순서(D-06), 도메인 매핑 리전 확인 결과(A1 — Phase 2~3 계획의 입력), SMTP 절에 TODOS 확인 결과 자리(미확인이면 "미확인, TODOS 참조").
    2. `docs/ARCHITECTURE.md`: 실측으로 틀린 서술(예: 결정적 URL·프라이빗 IP 경로·better-auth 내부 어댑터 메서드 이름)만 고친다. 300줄 이하 유지.
    3. `CLAUDE.md`: 명령 줄 하나만 교체 — `- 명령: dev \`pnpm db:dev && pnpm dev\` · test \`pnpm test\` · lint \`pnpm lint && pnpm typecheck && pnpm lint:sql\` · build \`pnpm build\``. 다른 줄은 손대지 않는다(진행 상황·날짜 추가 금지). 이 변경은 페이즈의 마지막 커밋(`docs: fill CLAUDE.md commands after Phase 1`)으로 따로 만든다.
    4. `pnpm vitest run --project unit test/unit/docs-limits.test.ts`와 `wc -l`로 상한 확인.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts && grep -c 'dev `pnpm db:dev && pnpm dev`' CLAUDE.md && test "$(grep -c '\[ \]' CLAUDE.md)" -eq 0</automated>
    <fails_when>non-zero exit, a vitest summary containing "failed", the CLAUDE.md grep printing 0, or any remaining "[ ]" placeholder token in CLAUDE.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md`에 `pnpm db:dev && pnpm dev`, `pnpm test`, `pnpm lint && pnpm typecheck && pnpm lint:sql`, `pnpm build`가 명령 줄 하나에 있고, CLAUDE.md 커밋의 SHA를 `<sha>`라 할 때 `git show --numstat --format= <sha> -- CLAUDE.md`의 출력이 `1	1	CLAUDE.md` 한 줄이다(한 줄 추가·한 줄 삭제; SUMMARY에 `<sha>` 기록)
    - `wc -l docs/OPERATIONS.md docs/ARCHITECTURE.md` 각각 ≤ 300
    - `docs/OPERATIONS.md`에 `erp-prod`, `max_connections`, `Policy Analyzer`, `sha256`, `청구서`가 있다
    - `grep -c -E '[0-9]{12}|@gmail\.com' docs/OPERATIONS.md docs/ARCHITECTURE.md CLAUDE.md`가 파일마다 0
  </acceptance_criteria>
  <done>운영 문서가 실측을 반영한 채 300줄 안에 있고, CLAUDE.md 명령 4개가 실제 스크립트로 확정되어 Phase 1이 문서 면에서도 닫혔다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub write 협업자(본인) → workflow_dispatch target=production | 프로덕션 변경 게이트(GitHub Environments 없음, D-05) |
| 인터넷 → `erp-prod` | 실제 직원이 쓰게 될 공개 경계 |
| 문서 → 리포 독자 | 실측 기록의 식별자 유출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-41 | Elevation of Privilege | 검토되지 않은 코드·제3자의 프로덕션 배포 | high | mitigate | production 잡은 `workflow_dispatch`(target=production)로만 실행 — 실행 권한은 비공개 저장소의 write 협업자(본인 1인, 01-07 Task 2 확인); 가드 스텝이 스테이징이 서빙 중인 SHA만 통과시키고 push로는 실행 불가(01-06 acceptance); Actions 실행 로그가 actor·SHA·시각 기록 (D-05, T-1-45) |
| T-1-42 | Information Disclosure | DEPLOY-LOG·문서에 프로젝트 번호·이메일 | medium | mitigate | 변수명·마스킹 규칙 + acceptance grep(12자리 숫자 0) |
| T-1-43 | Repudiation | 누가 언제 프로덕션에 올렸는지 불명 | low | mitigate | GitHub 실행 페이지의 triggering actor·SHA·시각·가드 출력이 기록(승인 기록 대체, D-05); DEPLOY-LOG에 실행 URL |
| T-1-44 | Tampering | 베이스 이미지 드리프트(T-1-24 후속) | medium | accept | digest를 문서에 기록하고 `@sha256` 고정은 첫 실운영 뒤 결정(문서 절차) |
</threat_model>

## Flagged Assumptions (spec-less edge probe)

| requirement_id | category | status | 메모 |
|---|---|---|---|
| OPS-02 | unclassified | unresolved | 월 $30 경계는 청구서로만 확인 가능 — human-check 5와 OPERATIONS.md 첫 청구서 확인일로 처리(수동, VALIDATION.md Manual-Only) |
| OPS-07 | unclassified | unresolved | 300줄 경계(300 허용·301 거부)는 01-04 `docs-limits.test.ts`가 고정; 이 플랜은 갱신 뒤 다시 실행 |

## Artifacts this phase produces (this plan)

- 새 파일: `.planning/phases/01-deploy-skeleton-login/01-08-DEPLOY-LOG.md`
- 수정: `docs/OPERATIONS.md`(실측 절), `docs/ARCHITECTURE.md`(필요 시), `CLAUDE.md`(명령 줄 1개), `scripts/bootstrap-gcp.sh`(A3 역할 조정 시)
- GCP 리소스(코드 밖): Cloud Run `erp-prod` + Job 3개, Cloud SQL `erp-prod-db`, 시크릿 7개(`*-prod`), `[prod]` 경보 3개; 계정: 관리자 1 + 테스트 직원 1 (프로덕션)

<verification>
- production 잡 success + `$PROD_URL/healthz` ok + human-check 5항목
- `pnpm test:unit`(docs-limits 포함) exit 0
</verification>

<success_criteria>
- D-05 승격 흐름이 실제로 한 번 수행됐고 프로덕션에서 ROADMAP 기준 2·7이 확인됐다
- OPS-07 문서 두 개가 실측을 담고 300줄 이하다; CLAUDE.md 명령 4개가 확정됐다
- Phase 1 완료 조건(deploy.sh 성공)이 두 환경에서 충족됐다
</success_criteria>

<output>
Create `.planning/phases/01-deploy-skeleton-login/01-08-SUMMARY.md` when done — 프로덕션 실행 URL(마스킹), A1·A2·A3 최종 상태, Phase 2·3이 이어받을 입력(도메인 매핑 가능 여부, 임시 화면 목록, CLI→관리 화면 교체 대상)을 기록.
</output>
