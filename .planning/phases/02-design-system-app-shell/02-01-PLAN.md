---
phase: 02-design-system-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/SYSTEM.md
  - docs/design/DECISIONS.md
  - docs/design/tokens.css
  - test/unit/design-system-docs.test.ts
autonomous: false
requirements: [UX-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SYSTEM.md §6 템플릿 목록에 로그인 화면이 있고, 셸이 없는 화면 구조·실패 문구 자리·계정 잠금 안내 자리·임시 비밀번호 배너 위치·다섯 상태가 모두 적혀 있다"
    - "SYSTEM.md §6 템플릿 목록에 시스템 상태 화면이 있고, 각 항목의 「확인 불가」 분기와 한도 배너 자리가 적혀 있다"
    - "SYSTEM.md §6 템플릿 목록에 오류 페이지(404 · 권한 없음 · 세션 만료)가 있고, 셋 모두 토큰 안에서 렌더된다는 것과 다음 한 수가 적혀 있다"
    - "SYSTEM.md §7에 배너 계약이 있고 §7-6 토스트(최대 1개·4초)와 어떻게 다른지가 적혀 있다"
    - "SYSTEM.md §7에 알림함·배지 계약이 있고 §7-4 「내 차례」·§7-5 상태 태그·§6-0 하단 탭 건수와의 관계가 적혀 있다"
    - "SYSTEM.md §6-0에 PC에서 내 계정·로그아웃에 닿는 경로가 적혀 있다 — 사용자 이름이 무엇을 여는지가 문장으로 있다(형태는 체크포인트 항목 G가 고른 것)"
    - "SYSTEM.md §6-0에 상단 바 사용자 표기가 이름만이고 소속은 Phase 3에서 붙는다는 것이 적혀 있다"
    - "SYSTEM.md §6-0에 관리자용 시스템 상태 화면 진입점이 적혀 있다"
    - "SYSTEM.md §7-1에 1차 버튼 위 kbd 테두리를 어떻게 처리하는지가 적혀 있고, 그 처리가 02-02의 stylelint 규칙을 **예외를 새로 만들지 않고** 통과한다 — stylelint.config.mjs는 wave 1의 02-02가 이미 확정했으므로 이 체크포인트의 답이 그 파일을 고치게 만들어서는 안 된다. 02-03 Task 1이 참조할 유일한 출처다"
    - "SYSTEM.md가 「더보기」 시트 계정 그룹에 남겨 둔 항목은 전부 갈 곳이 있다 — 「설정」이 목록에 남아 있다면 이 페이즈에 그 화면이 있고, 없다면 목록에서 빠져 있다"
    - "SYSTEM.md §6-0 폰 하단 탭 역할 표에 관리자·직원 두 행이 있다"
    - "SYSTEM.md §7-7 컴포넌트별 다섯 상태 표에 버튼·시트/모달·공통 셸·토스트·상태 태그·배너 행이 있다"
    - "SYSTEM.md와 tokens.css의 대비값 서술 2건이 실제 계산값과 일치한다"
    - "DECISIONS.md에 이번 페이즈의 의도적 이탈 5건이 이유와 함께 남아 있다"
    - "test/unit/design-system-docs.test.ts가 위 계약의 존재를 토큰 문자열로 고정하고, 이 파일이 있기 때문에 Wave 1 커밋이 docs 전용이 아니게 되어 CI가 돈다"
    - statement: "Windows Chrome/Edge에서 문서가 규정한 자릿수 정렬 규칙이 실제로 지켜지는지"
      verification: backstop
  prohibitions:
    - "SYSTEM.md에 실제로 결정되지 않은 제품 동작을 planner·executor가 지어내어 확정 문장으로 적어서는 안 된다 — 신설 절의 내용도, 항목 F~I의 답도, 체크포인트에서 사람이 고른 것만 들어간다"
    - "체크포인트에서 고른 답이 기존 E2E 스펙을 깨는 경우, 스펙 파일을 고쳐서 통과시켜서는 안 된다 — 그 경우는 재계획 신호이고 Task 2로 진행하지 않는다"
    - "코드를 먼저 쓰고 SYSTEM.md를 그것에 맞춰 사후 수정해, 결정한 적 없는 것을 결정된 것처럼 남겨서는 안 된다"
    - "시스템을 벗어나는 선택을 DECISIONS.md 기록 없이 화면 하나의 예외로 처리해서는 안 된다"
  artifacts:
    - path: "docs/design/SYSTEM.md"
      provides: "신설 3개 레이아웃 템플릿 + 신설 2개 컴포넌트 계약 + §6-0·§7-7 보강"
      contains: "로그인"
    - path: "docs/design/DECISIONS.md"
      provides: "2026-09-19 Phase 2 의도적 이탈 기록 5건"
      contains: "2026-09-19"
    - path: "docs/design/tokens.css"
      provides: "--viz-2 대비값 주석 정정 (+ 체크포인트 항목 I가 ②면 1차 버튼 위 kbd 테두리 토큰 1개)"
      contains: "--viz-2"
    - path: "test/unit/design-system-docs.test.ts"
      provides: "SYSTEM.md 신설 계약의 존재를 고정하는 회귀 테스트"
      min_lines: 30
  key_links:
    - from: "test/unit/design-system-docs.test.ts"
      to: "docs/design/SYSTEM.md"
      via: "readFileSync + toContain — docs-limits.test.ts와 같은 방식"
      pattern: "SYSTEM\\.md"
---

<objective>
SYSTEM.md가 Phase 2가 만들 화면들을 실제로 덮도록 문서를 먼저 고친다. 디자인 시스템은 원장(표·폼·대시보드·인쇄·외부수령자) 화면을 기준으로 지어졌고, Phase 2가 내보낼 화면(로그인 · 내 계정 · 시스템 상태 · 셸 · 오류 페이지) 중 문서가 제대로 덮는 것은 셸 뼈대뿐이다. 이 플랜은 그 여덟 개의 빈 곳을 메우고, 사실과 어긋난 대비값 서술 두 건을 고치고, 의도적 이탈을 DECISIONS.md에 남긴다.

**MVP 사용자 스토리** (ROADMAP Phase 2 Goal에서 옮김):
> **As a** PLANT8 직원, **I want to** 폰과 PC에서 `docs/design/SYSTEM.md` 기준으로 만들어진 앱 셸(로그인·내 계정·내비게이션)을 쓰고 싶다, **so that** 이후 모든 화면이 같은 토큰·컴포넌트만 써서 설명 없이도 같은 자리에서 같은 것을 찾을 수 있다.

Purpose: D-30의 원칙 「문서가 코드보다 먼저다」. §6 스스로가 "새 템플릿이 필요하면 먼저 여기에 추가한다"(SYSTEM.md L262)고 규정했고, UX-01이 "SYSTEM.md를 먼저 확정하고"라고 요구한다. 이 플랜이 끝나기 전에는 어떤 `ui/**` 파일도, 어떤 화면 코드도 쓰지 않는다.
Output: SYSTEM.md 신설 절 5개 + 기존 절 보강 4곳(§6-0 · §7-1 · §7-7 · §1-2), DECISIONS.md 기록 5건(체크포인트 항목 I가 ②면 6건), tokens.css 주석 1건 정정(+ I가 ②면 토큰 1개 신설), 회귀 테스트 1개.

그리고 이 플랜의 Task 1 체크포인트가 **이 페이즈 전체의 미결 제품·시각 결정을 한자리에서 닫는다** — 로그인 복귀 안내의 형태(02-03·02-06이 의존), PC 사용자 진입점의 형태(02-04가 의존), 「설정」 항목의 목적지(02-04·02-05가 의존), 1차 버튼 위 `kbd` 테두리 색(02-03이 의존). 이 넷은 어느 문서에도 결정이 없고 실물 HTML도 답을 주지 않으므로, 체크포인트를 우회하면 executor가 제품 결정을 대신 내리게 된다.

**이 체크포인트의 답을 받는 태스크는 전부 이 플랜보다 뒤 wave에 있다.** 항목 I의 선택지에서 `stylelint.config.mjs`를 고치는 안을 뺀 이유가 그것이다 — 그 파일은 같은 wave 1에서 병렬로 도는 02-02가 만들고, 02-02는 사람이 답하기 전에 끝난다. 답을 받을 수 있는 소유자가 없는 선택지는 내놓지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-app-shell/02-CONTEXT.md
@.planning/phases/02-design-system-app-shell/02-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: 신설 계약 5개 + 미결 제품·시각 결정 4건의 내용 결정 (백지 설계 — 지어내지 않는다)</name>
  <read_first>
    - docs/design/SYSTEM.md §6 전체(L260–520) — 기존 템플릿 7개가 무엇을 어떤 밀도로 적는지. §6-3 폼 화면(L366–400)이 신설 템플릿의 서술 밀도 기준이다
    - docs/design/SYSTEM.md §6-0 L300 — 「더보기」 시트의 계정 그룹이 이미 `설정 · 내 정보 · 로그아웃` 셋을 규정하고 있다(§7-8 L639가 같은 목록을 반복한다). 항목 H의 출처다
    - docs/design/SYSTEM.md §7-1 L531 — 단축키가 있는 버튼의 `kbd` 병기 규칙(`--fs-xs`, `1px --line`, `--faint`). 항목 I가 이 규칙의 1차 버튼 위 변형을 다룬다
    - docs/design/SYSTEM.md §7-5 상태 태그(L597) · §7-6 토스트(L603) · §7-7 다섯 상태(L609–629) — 신설 컴포넌트 계약의 서술 형식 기준
    - docs/design/DECISIONS.md — 이 주제들에 관한 기존 결정이 **하나도 없음**을 확인한다(검색어: 로그인 · 알림함 · 배너 · 404 · 세션 · kbd · 설정)
    - docs/design/system/preview.html L61–62 — `.btn kbd{border-color:currentColor;opacity:.8}` 바로 다음 줄에 1차 버튼용 덮어쓰기 한 줄이 있다. 항목 I가 결정하는 대상이 정확히 그 한 줄이다
    - app/(app)/account/page.tsx · app/admin/system-status/page.tsx — 배너가 실제로 어떤 두 화면에서 필요한지(임시 비밀번호 안내, DB 커넥션 한도 경고)
    - app/(auth)/login/page.tsx L14–17 — 현재 비밀번호 변경 뒤 돌아온 경우의 안내가 `role="status"` 문단 하나다. 항목 F가 이것의 운명을 정한다
    - test/e2e/change-password.spec.ts L25–27 — 그 안내의 URL 패턴과 문장을 E2E가 이미 단언한다. 항목 F의 제약이다
    - lib/viewer.ts L38–41 — `requireSession`이 이유 없이 `/login`으로 보낸다. 항목 F의 두 번째 제약이다
    - .planning/phases/02-design-system-app-shell/02-RESEARCH.md §Open Questions — Q1이 항목 I로 넘어왔다는 기록
    - .planning/phases/02-design-system-app-shell/02-CONTEXT.md §decisions D-30·D-31
  </read_first>
  <decision>SYSTEM.md에 신설할 다섯 계약의 제품 동작(A~E)과, 계획 전체가 의존하지만 아직 아무도 결정한 적 없는 제품·시각 결정 넷(F~I)을 확정한다: (1) §6-7 로그인 화면 템플릿 (2) §6-8 시스템 상태 화면 템플릿 (3) §6-9 오류 페이지 템플릿 (4) §7-11 배너 (5) §7-12 알림함·배지 (6) 로그인 복귀 안내의 형태 (7) PC 사용자 진입점의 형태 (8) 「설정」 항목의 목적지 (9) 1차 버튼 위 `kbd` 테두리 색</decision>
  <context>
이 다섯 가지는 실물 HTML도 없고 DECISIONS.md 언급도 0건이다. 포팅이 아니라 백지 설계다. 문서에 한번 적히면 §6 L262의 자기 규정에 따라 이후 모든 화면이 그것을 기준으로 삼으므로, executor가 추측으로 채우면 결정한 적 없는 제품 동작이 시스템 규칙이 된다. 그래서 작성 전에 사람이 고른다.

결정해야 하는 항목(각각 사용자가 고르거나 「추천안대로」라고 답한다):

A. **§6-7 로그인 화면** — ① 워드마크 위치(상단 좌측 고정 / 폼 위 가운데) ② 로그인 실패 문구의 자리(폼 위 한 줄 / 비밀번호 입력 아래) ③ 계정 잠금 안내가 실패 문구와 같은 자리인지 다른 자리인지 ④ 임시 비밀번호 배너가 로그인 직후 어디에 뜨는지(내 계정 화면 상단 / 셸 상단 전역) ⑤ 다섯 상태 중 EMPTY·PARTIAL이 이 화면에 해당하는지(해당 없음도 하나의 답이다)

B. **§6-8 시스템 상태 화면** — ① 세 항목(배포 버전 · DB 커넥션 · 마지막 백업)을 §7-3 표로 볼지, 라벨·값 목록(§7-8 시트 상세의 `dl` 모양)으로 볼지 ② 「확인 불가」를 값 자리에 글자로 둘지 §7-5 상태 태그로 둘지 ③ 한도 배너의 위치(화면 제목 위 / 해당 항목 바로 위)

C. **§6-9 오류 페이지** — ① 404 · 권한 없음 · 세션 만료 셋이 한 템플릿의 세 변종인지 각각 다른 템플릿인지 ② 셸 안에서 렌더되는지 밖인지(D-17은 직원에게 403이 아니라 404를 준다 — 이 404는 로그인한 사람이 보는 404다) ③ 각 경우의 「다음 한 수」가 무엇인지

D. **§7-11 배너** — ① 지속 시간(닫을 때까지 / 조건이 사라질 때까지) ② 한 화면에 몇 개까지 ③ 위치(셸 상단 바 바로 아래 전역 / 화면 제목 위 지역) ④ 등급(안내 · 경고 · 오류)과 각 등급의 ARIA 역할 ⑤ §7-6 토스트(최대 1개·4초·행동 결과)와의 경계 문장

E. **§7-12 알림함·배지** — ① 읽음/안 읽음 구분 방법 ② 목록 행이 §7-4 「내 차례」와 어떻게 다른지 ③ 진입점(PC 상단 바 / 폰 「더보기」 시트) ④ 건수 배지 규칙이 §7-5 상태 태그·§7-7 PARTIAL 배지·§6-0 하단 탭 건수와 충돌하지 않는 방법 ⑤ 다섯 상태

---

아래 F~I는 신설 절의 내용이 아니라, **이 페이즈의 코드 플랜들이 이미 전제하고 있지만 아무 문서에도 결정이 없는 것들**이다. 지금 고르지 않으면 executor가 대신 고르게 된다.

F. **로그인 복귀 안내의 형태 (§6-7이 정한다 · 02-03 T3 · 02-06 T2가 쓴다)**

F-1 — **비밀번호를 바꾼 뒤 돌아온 경우의 안내 형태.** 지금은 `app/(auth)/login/page.tsx`의 `role="status"` 문단 한 줄이다. §7-6은 「화면 이동이 따르는 행동의 결과」를 토스트로 보이라고 하고, 이 페이즈에서 그런 행동은 비밀번호 변경 하나뿐이다. 그런데 **`test/e2e/change-password.spec.ts`가 URL 패턴과 문장을 이미 단언하고 있고(L25–27), 02-06 Task 2의 acceptance는 그 스펙 파일을 고치지 않고 통과할 것을 요구한다.** 둘 중 하나는 양보해야 한다:
  - ① 지금의 status 한 줄을 §6-7의 정식 자리로 승격하고 문장을 그대로 둔다 → 스펙 무수정 통과. 토스트는 이 페이즈에서 「도착 화면의 결과 표시」로 쓰이지 않는다
  - ② 토스트로 바꾸되 **문장을 한 글자도 바꾸지 않는다** → 스펙의 텍스트 단언은 통과하지만, §7-6의 4초 자동 소멸 때문에 그 단언이 시간에 민감해진다(느린 CI에서 간헐 실패 가능)
  - ③ 토스트로 바꾸고 §8 규칙 2대로 버튼과 같은 단어의 새 문구를 쓴다 → **스펙 수정이 불가피하다.** 이 경우 `test/e2e/change-password.spec.ts`를 02-06의 files_modified에 넣고 02-06 Task 2의 「스펙 무수정」 acceptance를 함께 고쳐야 하므로, 이 플랜 세트의 재계획이 필요하다

F-2 — **세션 만료로 돌아온 경우의 안내를 Phase 2가 실제로 띄우는가.** 지금 `/login?reason=…`을 만드는 곳은 `app/(app)/account/actions.ts:43` 하나뿐이고 `lib/viewer.ts:41`의 `requireSession`은 이유 없이 `/login`으로 보낸다. 거기에 이유를 붙이면 **`test/e2e/login-logout.spec.ts:61`의 `toHaveURL(/\/login$/)`가 깨진다**(로그아웃 뒤 `/account` 재접근 케이스).
  - ① §6-7·§6-9는 세션 만료 안내의 **자리만 계약으로 적고**, Phase 2는 그 신호를 만들지 않는다 → `lib/viewer.ts`를 건드리지 않으므로 기존 E2E 전부 무수정 통과
  - ② Phase 2가 `requireSession`에 이유를 붙인다 → `lib/viewer.ts`가 어느 플랜의 files_modified에도 없고, `login-logout.spec.ts` 수정이 필요하며, 「최초 방문(로그인한 적 없음)」과 「세션 만료」를 그 리다이렉트가 구분하지 못한다는 문제가 남는다

G. **PC 사용자 진입점의 형태 (§6-0 (a) 보강이 정한다 · 02-04 T1이 만든다)** — §6-0은 상단 바 우측에 사용자를 그려 놓았지만 그것이 무엇을 여는지 한 번도 말하지 않는다. 폰은 「더보기」 시트의 계정 그룹이 덮지만 PC에는 정의된 경로가 없다. AUTH-02(「어느 화면에서든 로그아웃」)와 성공 기준 3(「키보드만으로 비밀번호 변경」)이 여기에 달려 있다:
  - ① 사용자 이름이 작은 메뉴를 연다(내 정보 · 로그아웃). Esc로 닫히고 포커스가 돌아온다 — §7-8 시트와 같은 결의 동작이 PC에 하나 더 생긴다
  - ② 사용자 이름이 곧 `/account` 링크 하나다. 로그아웃은 내 계정 화면 안에만 있다 — 만드는 것이 가장 적지만 「어느 화면에서든 로그아웃」이 한 번의 이동을 거친다
  - ③ 상단 바 우측에 이름 · 내 계정 링크 · 로그아웃 버튼을 나란히 둔다 — 열고 닫는 상태가 없어 가장 단순하지만 상단 바가 붐빈다

H. **「설정」 항목의 목적지 (§6-0 L300 · §7-8 L639가 이미 규정한 항목)** — SYSTEM.md가 「더보기」 시트 계정 그룹에 `설정`을 적어 두었는데 이 페이즈의 어느 플랜도 그 목적지를 만들지 않는다. 그대로 두면 02-05의 메뉴↔라우트 대조 검증이 실패하거나 사용자가 404를 만난다. 「내 정보」는 `/account`가 이미 있다:
  - ① D-22와 같은 방식으로 `/settings` 라우트를 만들고 §7-7 EMPTY 한 줄을 렌더한다 — 파일 하나, 「빈 자리를 두지 않는다」와 일관
  - ② 시트에서 비활성 + 이유로 둔다(§7-1) — 다만 UX-06의 비활성 패턴은 업무 흐름용이지 내비게이션용이 아니라고 D-22가 이미 거부한 논리다
  - ③ SYSTEM.md §6-0·§7-8에서 「설정」을 빼고 Phase 3(계정·권한)으로 미룬다 — 이 경우 그 두 줄의 편집이 Task 2 범위에 추가된다

I. **1차 버튼 위 `kbd` 테두리 색 (§7-1이 정한다 · 02-03 T1이 쓴다 — 02-02의 stylelint는 제약 조건일 뿐 이 답을 소비하지 않는다)** — `docs/design/system/preview.html:62`가 `.btn.p kbd{border-color:rgba(255,255,255,.5)}` 한 줄로 1차 버튼(`--accent` 면) 위의 `kbd` 테두리를 덮어쓴다. 02-02가 세우는 stylelint는 색 속성의 `rgba(...)` 리터럴을 금지하므로 02-03 Task 1이 이 줄을 그대로 옮기면 lint에 걸린다. `tokens.css`에 이 값에 대응하는 토큰이 없고 `DECISIONS.md`에 선례가 0건이다:
  - ① **기존 토큰 재사용** — `--bar-muted`(`--g-300`) 또는 `--bar-leaf`(`--g-400`)를 쓴다. 새 토큰 0개, 예외 0개. 다만 이 둘은 상단 바(`--bar`) 위 대비로 설계된 값이라 `--accent` 면 위에서는 다르게 보인다
  - ② **새 토큰 신설** — `docs/DESIGN.md` §4 절차대로 `tokens.css`에 `--on-accent-weak`를 더하고 값은 지금과 같게 둔다. 화면은 한 픽셀도 바뀌지 않고, stylelint 예외가 0개이며, 기록은 Task 3의 DECISIONS.md에 함께 남는다. `tokens.css`는 이미 이 플랜의 files_modified에 있다
  - ③ **덮어쓰기 제거** — `.btn.p kbd` 한 줄을 이관하지 않는다. 바로 윗줄 `.btn kbd{border-color:currentColor;opacity:.8}`가 그대로 적용되어 1차 버튼 위에서는 흰 글자색을 따른다. 토큰도 예외도 필요 없고, 테두리가 지금보다 조금 진해진다

  **네 번째 안(「이 한 선언에만 좁은 stylelint 예외를 둔다」 — 02-RESEARCH.md의 옛 권고)은 일부러 빼 두었다.** 세 가지 이유다. (1) **소유자가 없다** — 예외는 `stylelint.config.mjs`에 들어가는데 그 파일은 02-02 Task 2가 만들고, 02-02는 이 플랜과 같은 wave 1에서 `depends_on: []`로 병렬 실행되어 사람이 답하기 전에 끝난다. 뒤 wave의 어느 플랜도 그 파일을 `files_modified`에 갖고 있지 않으므로, 이 답을 고르면 그것을 실행할 태스크가 이 플랜 세트에 존재하지 않는다. (2) CLAUDE.md 프론트엔드 규칙의 「시스템을 벗어나야 하면 SYSTEM.md를 고친다. **화면 하나만 예외 금지**」와 정면으로 어긋난다 — 선언 하나를 위한 도구 예외가 정확히 그 패턴이다. (3) **결과를 잃지 않는다** — 위 셋이 결과 공간을 이미 덮는다(기존 토큰 재사용 · 새 토큰 추가 · 덮어쓰기 제거). ②는 현재 픽셀을 그대로 보존하므로, 「지금과 똑같이 보이게 하고 싶다」는 요구는 예외 없이 ②로 달성된다. 예외를 두어야만 얻을 수 있는 시각 결과는 없다.
  이 세 안 중 어느 것도 마음에 들지 않아 예외를 두고 싶다면 그렇게 답한다 — 그 경우 이 플랜 세트는 02-02에 `stylelint.config.mjs` 조건부 단계를 붙이는 재계획이 필요하고, 그것은 02-02를 `depends_on: ["02-01"]`로 바꿔 wave 1 병렬성을 잃는다는 뜻이다(F-1③과 같은 재계획 신호다).

**추천안(사용자가 「추천안대로」를 고르면 이대로 Task 2가 쓴다):** A ①폼 위 가운데 ②폼 위 한 줄 ③같은 자리, 문구만 다름 ④내 계정 화면 상단 ⑤EMPTY·PARTIAL 해당 없음 · B ①라벨·값 목록 ②상태 태그 ③화면 제목 위 · C ①한 템플릿의 세 변종 ②셸 안 ③각각 첫 화면으로 / 로그인으로 / 로그인으로 · D ①조건이 사라질 때까지 ②최대 1개 ③화면 제목 위 지역 ④안내·경고 둘, 안내는 status 역할 경고는 alert 역할 ⑤토스트는 행동의 결과, 배너는 화면에 걸린 조건 · E ①안 읽은 행만 좌측 인셋 선 ②「내 차례」는 내가 할 일, 알림함은 나에게 일어난 일 ③**PC는 상단 바 우측 사용자 진입점과 같은 자리 — 항목 G가 ①(메뉴)이면 그 메뉴 안의 한 항목, G가 ②·③(메뉴 없음)이면 그 진입점 옆에 나란한 항목 하나** · 폰 「더보기」 시트 ④건수 배지는 안 읽은 알림 수 하나만 세고 다른 셋과 자리를 나눠 쓴다 ⑤LOADING·EMPTY·ERROR만 정의, SUCCESS·PARTIAL 해당 없음 · **F** F-1 ① · F-2 ① · **G** ① · **H** ① · **I** ②

E③은 G의 답에 매여 있다 — G가 ②·③이면 열리는 메뉴 자체가 없으므로 「메뉴 안」이라는 자리가 존재하지 않는다. 위 문장은 G의 세 답 모두에 대해 자리가 정의되도록 쓴 것이고, §7-12는 **구현이 Phase 7인 계약 문서**이므로 이 페이즈가 그 진입점을 실제로 만들지는 않는다.

추천안을 그대로 고르면 이 플랜 세트는 파일 하나(`app/(app)/settings/page.tsx`)와 토큰 하나(`--on-accent-weak`)만 늘고 기존 E2E 세 스펙이 전부 무수정으로 통과한다 — 그 전제로 02-03·02-05·02-06이 이미 쓰여 있다.

**재계획 신호 — 아래 셋 중 하나를 고르면 Task 2로 진행하지 않는다.** F-1 ③(토스트 + 새 문구) · F-2 ②(`requireSession`에 이유 부착) · 항목 I에서 「좁은 stylelint 예외」. 앞의 둘은 E2E 스펙 수정이 필요하고, 셋째는 02-02의 의존 관계 변경이 필요하다. 셋 다 이 플랜 세트가 지금 실행할 수 있는 형태가 아니며, 알고 고르는 것은 괜찮다 — 다만 그 답은 문서 작성이 아니라 `/gsd-plan-phase`로 되돌아가는 것으로 이어진다. **A④(셸 상단 전역)와 C②(셸 밖)는 재계획 신호가 아니다** — 02-06이 두 갈래를 모두 실행할 수 있게 쓰여 있다.
  </context>
  <options>
    <option id="recommended">
      <name>추천안 그대로</name>
      <pros>위 다섯 묶음이 §6-3·§7-5·§7-6의 기존 서술 밀도·어휘와 맞춰 뽑은 안이다. Task 2가 바로 쓸 수 있다</pros>
      <cons>사용자가 실제로 원하는 동작과 다를 수 있고, 한번 문서가 되면 이후 모든 화면이 따른다</cons>
    </option>
    <option id="per-item">
      <name>항목별로 다르게 고른다</name>
      <pros>A~I 중 마음에 걸리는 것만 바꾸고 나머지는 추천안을 쓴다</pros>
      <cons>결정 항목이 24개라 응답이 길어진다</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 사용자 응답이 A~I 아홉 묶음 각각에 대해 확정 값을 남긴다(「추천안대로」도 확정 값이다)
    - Task 2가 문서를 쓰기 전에 이 체크포인트가 해소되어 있다 — 미해소 상태로 Task 2를 시작하지 않는다
    - 항목 I의 답이 세 선택지 ①②③ 중 하나다 — 셋 다 `stylelint.config.mjs`를 건드리지 않고 02-01(토큰 정의)과 02-03(CSS Module 작성)만으로 실행된다. 답이 02-03 Task 1에 전달되며 그 태스크의 `<precondition>`이 이 항목의 해소를 요구한다
    - 항목 F-1이 ③이거나 F-2가 ②이거나 항목 I에서 「좁은 stylelint 예외」를 고르면, 이 페이즈의 계획이 실행 가능한 형태가 아니다 — 이 경우 Task 2로 진행하지 않고 `/gsd-plan-phase`로 되돌아간다. F-1③·F-2②는 02-06(F-2면 02-03도)의 files_modified와 acceptance를, stylelint 예외는 02-02의 `depends_on`과 Task 2를 먼저 고쳐야 한다. **이 정지 조건은 Task 2의 `<precondition>`에 실행 가능한 형태로 다시 적혀 있다** — 여기 문장만으로는 executor가 넘어갈 수 있기 때문이다
  </acceptance_criteria>
  <resume-signal>「추천안대로」 또는 항목별 선택(예: 「A④는 셸 상단 전역, I는 ③, 나머지 추천안」)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: SYSTEM.md 신설 절 5개 + 기존 절 4곳 보강</name>
  <precondition>Task 1의 checkpoint:decision이 해소되어 A~I 아홉 묶음의 확정 값이 대화에 남아 있다</precondition>
  <precondition>그 확정 값이 재계획 신호 셋 중 어느 것도 아니다 — **F-1이 ③이 아니고, F-2가 ②가 아니고, 항목 I가 제시된 ①②③ 중 하나이며 「좁은 stylelint 예외」가 아니다.** 셋 중 하나라도 해당하면 이 태스크를 시작하지 않고 멈춘 뒤 `/gsd-plan-phase`로 되돌아간다. 이유: F-1③·F-2②는 이 플랜 세트의 어느 플랜도 `files_modified`에 갖고 있지 않은 E2E 스펙 파일 수정을 요구하고, stylelint 예외는 wave 1에서 이미 끝난 02-02의 `stylelint.config.mjs` 수정을 요구한다. 셋 다 실행할 소유자 태스크가 없으므로 문서를 먼저 쓰면 뒤 플랜이 확정적으로 실패한다</precondition>
  <files>docs/design/SYSTEM.md</files>
  <read_first>
    - docs/design/SYSTEM.md 전체 — 특히 §6-0(L264–300) · §6-1(L302–341) · §6-3(L366–400) · §7-5(L597) · §7-6(L603) · §7-7(L609–629) · §7-8(L631–640) · §10(L682–692)
    - Task 1의 사용자 응답 — 신설 절의 내용은 전부 여기서 온다
    - docs/design/system/preview.html L137(`header.bar`) · L263(`nav.tabs`) — §6-0 보강이 실물과 어긋나지 않는지 확인용
    - lib/viewer.ts — `SessionUser`에 소속/팀 필드가 없다는 사실(§6-0 사용자 표기 보강에 반영)
  </read_first>
  <action>
Task 1에서 확정된 값만 써서 SYSTEM.md를 편집한다. 확정되지 않은 항목이 하나라도 있으면 그 절을 쓰지 말고 멈춘다.

신설 절 5개(§6는 6-6 뒤, §7은 7-10 뒤에 번호를 이어 붙인다):
- §6-7 로그인 화면 — 셸이 없는 유일한 화면. 워드마크 + 폼 하나. 실패 문구 자리, 계정 잠금 안내 자리, 임시 비밀번호 배너가 로그인 직후 어디에 뜨는지, 다섯 상태.
- §6-8 시스템 상태 화면 — 관리자 전용(D-17: 직원은 404). 배포 버전 · DB 커넥션 · 마지막 백업 세 항목과 각각의 확인 불가 분기, 한도 경고 배너 자리, 다섯 상태.
- §6-9 오류 페이지 — 404 · 권한 없음 · 세션 만료. 셋 모두 tokens.css 토큰 안에서 렌더되고 프레임워크 기본 화면을 쓰지 않는다는 규정, 각각의 다음 한 수, 다섯 상태.
- §7-11 배너 — 지속 시간, 개수 상한, 위치, 등급과 ARIA 역할, §7-6 토스트와의 경계.
- §7-12 알림함 · 배지 — 읽음/안 읽음, 목록 행이 §7-4와 다른 점, 진입점, 건수 배지가 §7-5·§7-7·§6-0 건수와 자리를 나누는 규칙, 다섯 상태. 절 머리에 구현은 Phase 7이고 이 절은 계약만이라고 한 줄로 적는다.

기존 절 보강 4곳:
- §6-0 — (a) PC 상단 바 우측 사용자 이름이 무엇을 여는지 한 문장(내 계정 · 로그아웃 진입점). **형태는 Task 1 항목 G가 고른 것 하나만 쓴다** — 열리는 메뉴인지, 화면 링크 하나인지, 나란한 요소 셋인지. 이것이 없으면 성공 기준 3의 「키보드만으로 비밀번호 변경」과 AUTH-02의 「어느 화면에서든 로그아웃」이 셸 명세만으로는 도달 불가다. (b) 역할별 폰 하단 탭 표에 관리자 · 직원 두 행 추가 — D-14의 계급 모델이 둘뿐이고 D-23이 그 둘만 채우기 때문. 표 아래에 이 두 행은 Phase 3이 계급 5종으로 교체할 때 없어지는 임시 행이라고 한 줄. (c) 상단 바 사용자 표기는 이름만이고 소속은 Phase 3에서 붙는다는 한 줄 — `lib/viewer.ts`의 `SessionUser`에 소속 필드가 없다. (d) 관리자에게 시스템 상태 화면 진입점이 어디인지 한 줄. (e) **항목 H의 답이 ③이면** L300의 「더보기」 시트 계정 그룹에서 「설정」을 빼고 그것이 Phase 3으로 미뤄졌음을 한 줄로 적는다(§7-8 L639의 같은 목록도 함께). H가 ①·②면 이 편집은 하지 않는다.
- §7-1 버튼 위계 — 1차 버튼(`--accent` 면) 위 `kbd` 테두리 규칙 한 줄을 **Task 1 항목 I가 고른 대로** 적는다. 지금 §7-1 L531은 `1px --line` · `--faint`라는 기본 규칙만 있고 1차 버튼 위 변형은 실물 HTML에만 있다 — D-27상 실물이 아니라 이 문서가 기준이므로, 이관할 근거를 여기에 남긴다. I가 ③(덮어쓰기 제거)면 기본 규칙이 그대로 적용된다는 한 줄만 적는다. **어느 답이든 `stylelint.config.mjs` 예외를 전제하는 문장을 쓰지 않는다** — 그 파일은 wave 1의 02-02가 이미 확정했고 이 문서가 그것을 바꿀 수 없다. 새 토큰 이름을 쓸 경우 그 이름은 Task 3이 `tokens.css`에 실제로 정의한 것과 같아야 한다.
- §7-7 두 번째 표(컴포넌트별 다섯 상태) — 현재 7행(표·내 차례·폼·첨부·외부수령자·손익원장·인쇄)에 버튼(§7-1) · 시트/모달(§7-8) · 공통 셸(§6-0) · 토스트(§7-6) · 상태 태그(§7-5) · 배너(§7-11) 6행을 더한다. 각 행은 LOADING·EMPTY·ERROR·SUCCESS·PARTIAL 다섯 칸을 모두 채우되, 해당 없는 칸은 「해당 없음」과 그 이유를 짧게 적는다(기존 행들이 이미 쓰는 방식).
- §1-2 아래 비활성 텍스트 문장(L79) — `--faint` on `--surface`의 실제 WCAG 대비는 4.95이며 AA(4.5:1)를 통과한다. 현재 그 줄이 적은 대비 숫자와 거기서 끌어낸 「WCAG 예외 항목이다」라는 결론을 함께 정정한다. 뒤 문장(비활성 버튼은 이유를 옆에 글자로 쓴다)은 그대로 둔다. **정정 사유나 옛 숫자를 SYSTEM.md 안에 남기지 않는다** — 무엇이 왜 틀렸는지는 Task 3의 DECISIONS.md 기록에만 적는다.

문체는 기존 절과 같게 — 개조식 불릿, 명사형 종결, 사과·안내 문구 없음(§8). 새 색·서체·radius 값을 문서에 도입하지 않는다. 표기는 전부 기존 토큰 이름으로 한다.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/design/SYSTEM.md','utf8');for(const t of ['### 6-7','### 6-8','### 6-9','### 7-11','### 7-12','알림함','배너','세션 만료','권한 없음'])if(!s.includes(t)){console.error('MISSING '+t);process.exit(1)}const i=s.indexOf('### 6-0'),j=s.indexOf('### 6-1');if(!(j>i)){console.error('MISSING SECTION 6-0');process.exit(1)}const sec=s.slice(i,j),c=(re)=>(sec.match(re)||[]).length;if(!(c(/로그아웃/g)>1)){console.error('MISSING 6-0a logout-entry');process.exit(1)}if(!(c(/내 정보/g)+c(/내 계정/g)>1)){console.error('MISSING 6-0a account-entry');process.exit(1)}for(const t of ['소속','시스템 상태'])if(!sec.includes(t)){console.error('MISSING 6-0 '+t);process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 MISSING 으로 시작하는 줄이 나오고 종료 코드가 1이다. **이 명령은 편집 전 리포에서 반드시 실패한다** — 편집 전 값은 `### 6-7` 부재에서 이미 걸리고, 그것을 넘겨도 §6-0 구간의 `로그아웃`이 1회(L300 「더보기」 시트 줄 하나)뿐이라 `MISSING 6-0a logout-entry`에서 멈춘다. 문서 전체의 `로그아웃` 2회를 세는 검사였다면 편집 전에도 통과해 아무것도 증명하지 못한다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>tsc가 오류를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/SYSTEM.md`에 `### 6-7` · `### 6-8` · `### 6-9` · `### 7-11` · `### 7-12` 다섯 머리글이 존재한다
    - `grep -c '알림함' docs/design/SYSTEM.md`가 1 이상이다(편집 전 값은 0)
    - `grep -c '배너' docs/design/SYSTEM.md`가 1 이상이다(편집 전 값은 0)
    - `grep -c '세션 만료' docs/design/SYSTEM.md`가 1 이상이다(편집 전 값은 0)
    - §6-0의 폰 하단 탭 역할 표 행 수가 3에서 5로 늘고, 늘어난 두 행의 첫 칸이 각각 `관리자`·`직원`이다 — 보강 (b)
    - **보강 (a)** — `### 6-0`과 `### 6-1` 사이 구간의 `로그아웃` 출현 수가 편집 전 1에서 2 이상으로 늘고, 같은 구간의 `내 정보`와 `내 계정` 출현 수 합이 편집 전 1에서 2 이상으로 는다. 늘어난 문장이 상단 바 우측 사용자 진입점을 말하며 항목 G가 고른 형태와 일치한다
    - **보강 (c)** — 같은 §6-0 구간에 `소속`이 1회 이상 나온다(편집 전 문서 전체에서 0회). 그 문장이 상단 바 표기가 이름만이고 소속은 Phase 3에서 붙는다는 뜻이다
    - **보강 (d)** — 같은 §6-0 구간에 `시스템 상태`가 1회 이상 나온다(편집 전 문서 전체에서 0회). 그 문장이 관리자용 진입점의 위치를 말한다
    - **보강 (e)** — 항목 H의 답이 ③일 때만: `### 6-0` 구간과 §7-8 L639 줄에서 「설정」이 계정 그룹 목록에서 빠졌고 미루는 이유가 한 줄 붙었다. H가 ①·②면 두 줄이 편집 전과 동일하다
    - **§7-1 보강** — `### 7-1`과 `### 7-2` 사이 구간에 1차 버튼 위 `kbd` 테두리를 다루는 줄이 1개 있고, 항목 I가 ②를 골랐다면 그 줄이 참조하는 토큰 이름이 Task 3이 `tokens.css`에 정의한 이름과 문자열로 같다
    - §7-7의 두 번째 표 행 수가 7에서 13으로 늘고, 늘어난 행의 첫 칸에 버튼·시트·셸·토스트·상태 태그·배너가 각각 한 번씩 나온다
    - L79 부근의 비활성 텍스트 대비 서술에 `3.3`이 더는 없고 `4.95`가 있다
    - 신설 절 다섯 개 각각이 LOADING·EMPTY·ERROR·SUCCESS·PARTIAL 다섯 단어를 모두 포함한다
    - 위 자동 검사 명령이 편집 전 리포에서 실패하고 편집 후 통과한다 — 편집 전에도 통과하는 검사는 이 태스크의 완료 근거가 될 수 없다
  </acceptance_criteria>
  <done>SYSTEM.md가 Phase 2가 만들 다섯 화면 전부를 템플릿·컴포넌트 계약으로 덮고, 검증된 사실 오류 1건이 정정되어 있다</done>
</task>

<task type="auto">
  <name>Task 3: tokens.css 주석 정정 · DECISIONS.md 이탈 기록 5건 · 문서 회귀 테스트</name>
  <files>docs/design/tokens.css, docs/design/DECISIONS.md, test/unit/design-system-docs.test.ts</files>
  <read_first>
    - docs/design/tokens.css L62–70 — `--viz-2` 블록. `--bg`가 `#FFFFFF`로 정의된 줄도 함께 확인한다
    - docs/design/DECISIONS.md L1–30 — 기록 한 건의 형식(날짜 머리글 + 결정 + 이유 + 영향). 이 형식을 그대로 따른다
    - test/unit/docs-limits.test.ts 전체(81줄) — `readFileSync` + `it.each([...])("포함한다")` + `toContain` 패턴. 새 테스트는 이 구조를 그대로 복제한다
    - docs/design/SYSTEM.md — Task 2가 쓴 신설 절 머리글 문자열(테스트가 고정할 토큰)
    - .planning/phases/02-design-system-app-shell/02-CONTEXT.md D-20·D-25·D-27
  </read_first>
  <action>
(0) **Task 1 항목 I가 ②(새 토큰 신설)일 때만** `docs/design/tokens.css`에 토큰 하나를 더한다 — 1차 버튼(`--accent` 면) 위 `kbd` 테두리용. 값은 지금 실물이 쓰는 것과 같은 계산값이어야 한다(화면이 바뀌면 안 된다). 이름은 Task 2가 §7-1에 적은 것과 같은 문자열로 쓴다. 근거 한 줄을 아래 (2)의 DECISIONS.md에 여섯 번째 기록으로 남긴다. I가 ①(기존 토큰 재사용) 또는 ③(덮어쓰기 제거)이면 이 단계를 건너뛰고 기록도 다섯 건으로 둔다. 다른 토큰을 추가하지 않는다 — 이 페이즈가 `tokens.css`에 새 값을 넣는 것은 이 한 건뿐이고 그것도 사람이 고른 경우에만이다.

(1) `docs/design/tokens.css`의 `--viz-2` 주석을 고친다. 현재 주석은 두 개의 서로 다른 대비값을 적었는데 `--bg`가 `#FFFFFF`이므로 둘 다 같은 색 쌍(`#1F5FA8` 대 `#FFFFFF`)을 가리킨다. 실제 WCAG 대비는 양쪽 모두 6.44다. 하나의 값만 남기고, 무엇과 무엇의 대비인지 오해가 없도록 색 쌍을 명시한다. 값 자체(`#1F5FA8`)는 건드리지 않는다. **옛 숫자나 정정 사유를 tokens.css 안에 남기지 않는다** — 사유는 아래 (2)의 DECISIONS.md 기록에만 적는다.

(2) `docs/design/DECISIONS.md`에 2026-09-19 날짜로 다섯 건을 추가한다. CLAUDE.md 프론트엔드 규칙이 요구하는 「시스템을 벗어나면 이유 기록」이다.
- 표 컴포넌트를 Phase 2가 만들지 않는다(D-25) — 따라서 목록 화면 EMPTY는 §7-7 표 행이 전제하는 머리글·합계 뼈대 없이 한 줄만 렌더한다. ROADMAP 성공 기준 5가 구현 결정을 Phase 4로 미뤘다는 것이 이유. 표가 생기는 Phase 4에서 이 이탈이 해소된다는 것을 함께 적는다.
- §6-0 역할 표의 관리자·직원 두 행은 임시다 — D-14의 계급 모델이 둘뿐이고 원 표는 직능(기획 PM·경영관리·팀장·대표) 키다. Phase 3이 계급 5종·정보 노출표로 교체할 때 제거된다.
- 상단 바 사용자 표기는 이름만이다 — SYSTEM.md 목업의 소속 표기는 Phase 3 이후 상태를 그린 것이고 `lib/viewer.ts`의 `SessionUser`에 소속 필드가 없다. SYSTEM.md 수정 대상이 아니라 아직 데이터가 없는 경우라는 구분을 적는다.
- D-20의 리터럴 금지 범위를 색·서체·radius 셋으로 한정한다 — CLAUDE.md 원문이 「새 색·서체·radius 생성 금지」이고, 실물 HTML의 간격 선언 대부분이 tokens.css의 4px 계열 토큰에 대응하지 않아 간격까지 금지하면 SYSTEM.md가 이관을 지시한 마크업 자체가 거부된다. 간격 토큰을 새로 만들지 않는 이유도 함께.
- 대비값 서술 정정 2건 — SYSTEM.md 비활성 텍스트(4.95, AA 통과), tokens.css `--viz-2`(6.44). 값이 아니라 서술만 틀렸다는 것을 적는다.

(3) `test/unit/design-system-docs.test.ts`를 새로 만든다. `test/unit/docs-limits.test.ts`와 같은 구조(`readFileSync` + `describe` + `it.each` + `toContain`).
- `docs/design/SYSTEM.md`가 Task 2의 신설 머리글 5개와 핵심 계약 낱말을 포함하는지 고정한다.
- `docs/design/SYSTEM.md`의 §6-0 역할 표에 관리자·직원 행이 있는지 고정한다.
- **§6-0 보강 (a)(c)(d)를 구간 단위로 고정한다.** `### 6-0`과 `### 6-1` 사이만 잘라 낸 문자열에 대해: 로그아웃 진입점이 「더보기」 시트 줄 말고도 존재할 것(출현 수 2 이상), 내 계정·내 정보 언급 합이 2 이상일 것, 소속 표기 문장이 있을 것, 관리자용 시스템 상태 진입점 문장이 있을 것. 문서 전체를 대상으로 세지 않는다 — 전체 기준이면 다른 절이 같은 낱말을 쓰는 순간 §6-0이 비어도 통과한다.
- **§7-7 두 번째 표의 신설 6행을 고정한다.** 그 표 구간을 잘라 내고 버튼·시트·셸·토스트·상태 태그·배너 각각의 행이 있는지, 각 행이 다섯 상태 칸을 모두 채웠는지(파이프로 나눈 칸 수)를 단언한다. 행이 사라지면 이 테스트가 먼저 깨진다.
- `docs/design/tokens.css`가 정의하는 커스텀 속성 이름 중 SYSTEM.md가 참조하는 것이 전부 실재하는지 고정한다(속성 정의 `--x:`를 모아 집합을 만들고, SYSTEM.md 본문의 `--x` 참조가 그 집합에 있는지 검사). 이 검사는 이후 페이즈가 토큰 이름을 바꿀 때 문서가 먼저 깨지게 만든다.
- `docs/design/DECISIONS.md`가 2026-09-19 기록을 포함하는지 고정한다.
- 주석으로 이 파일의 존재 이유를 한 줄 남긴다: 두 워크플로의 경로 필터가 `docs/**`를 제외하므로(02-02가 `tokens.css` 하나만 예외로 되살린다) 디자인 문서만 바뀐 커밋은 CI가 돌지 않는다 — 그래서 디자인 문서의 계약은 테스트 파일과 함께 커밋되어야 검사된다. 필터의 구체적 형태(`paths-ignore`인지 `paths` + `!`인지)는 02-02가 정하므로 주석에 그 키 이름을 박지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>vitest 출력에 `Tests  ` 줄의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('docs/design/tokens.css','utf8');const m=s.split('\n').filter(l=>l.includes('--viz-2:'))[0]||'';if(m.includes('5.6')){console.error('STALE viz-2 comment');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 STALE viz-2 comment 가 나오고 종료 코드가 1이다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>eslint가 error 항목을 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/tokens.css`의 `--viz-2` 선언 줄 주석에 `5.6`이 더는 없다
    - `docs/design/DECISIONS.md`에 `2026-09-19`로 시작하는 `##` 머리글이 5개 있다(Task 1 항목 I가 ②면 6개)
    - 항목 I가 ②일 때만: `docs/design/tokens.css`에 새 커스텀 속성 정의가 정확히 1개 늘었고 그 이름이 SYSTEM.md §7-1이 참조하는 이름과 같다. I가 ①·③이면 `git diff docs/design/tokens.css`의 추가 줄에 새 `--` 정의가 0개다(주석 정정만)
    - `test/unit/design-system-docs.test.ts`가 존재하고 `pnpm vitest run --project unit test/unit/design-system-docs.test.ts`가 모두 통과한다
    - 그 테스트 파일이 `docs/design/SYSTEM.md`와 `docs/design/tokens.css`와 `docs/design/DECISIONS.md` 세 경로를 모두 읽는다
    - 그 테스트가 §6-0 구간(`### 6-0`~`### 6-1`)을 잘라 내어 단언하는 케이스를 3개 이상 갖는다 — 문서 전체가 아니라 구간이 대상임이 코드에서 보인다
    - 그 테스트가 §7-7 두 번째 표의 신설 6행(버튼·시트·셸·토스트·상태 태그·배너)을 각각 단언한다
    - 회귀 방향이 실제로 동작함을 증명한다: §6-0 구간의 보강 문장 한 줄을 일시적으로 지우면 이 테스트가 실패하고, 되돌리면 통과한다(실행 후 되돌릴 것)
    - 토큰 참조 검사가 실제로 동작함을 증명한다: SYSTEM.md에 존재하지 않는 커스텀 속성 참조를 일시적으로 넣으면 이 테스트가 실패하고, 되돌리면 통과한다(실행 후 되돌릴 것)
  </acceptance_criteria>
  <done>사실 오류 2건이 정정되고, 의도적 이탈 5건이 이유와 함께 남았고, 디자인 문서의 계약이 단위 테스트로 고정되어 docs 전용 커밋이 아닌 상태로 CI에 올라간다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 → 이후 모든 화면 | SYSTEM.md에 적힌 것은 §6 L262의 자기 규정에 따라 이후 페이즈가 그대로 따른다. 검증되지 않은 문장이 들어가면 그 오류가 화면으로 전파된다 |
| 사람 결정 → 문서 | Task 1 체크포인트가 유일한 진입점. 여기를 우회하면 에이전트가 만든 제품 동작이 시스템 규칙이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | docs/design/SYSTEM.md 신설 절 | medium | mitigate | Task 1의 `gate="blocking-human"` 체크포인트가 Task 2 앞에 있고, Task 2의 precondition이 그 해소를 요구한다 |
| T-02-02 | Repudiation | docs/design/DECISIONS.md | low | mitigate | 의도적 이탈 5건을 날짜·이유·해소 페이즈와 함께 기록. 이후 누가 왜 벗어났는지 추적 가능 |
| T-02-03 | Tampering | CI 사각지대(두 워크플로의 경로 필터가 `docs/**`를 제외) | medium | mitigate | `test/unit/design-system-docs.test.ts`가 같은 커밋에 포함되어 이 변경 묶음이 docs 전용이 아니게 된다. 필터 자체의 수정(문서화된 `paths` + `!` 형태로 교체, `tokens.css` 예외)은 02-02 Task 3이며 그 효과는 사람 체크로 확인된다 |
</threat_model>

<verification>
- `pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/docs-limits.test.ts` 전부 통과
- `pnpm lint` · `pnpm typecheck` 통과
- SYSTEM.md 신설 머리글 5개와 §6-0·§7-7 보강이 grep으로 확인된다
- DECISIONS.md에 2026-09-19 기록 5건
- `ui/` 아래 어떤 파일도 만들어지지 않았다(이 플랜의 files_modified에 `ui/`가 없다)
</verification>

<success_criteria>
- 검증된 SYSTEM.md 빈 곳 8개가 전부 문서로 메워졌다
- 검증된 사실 오류 2건이 정정되었다
- 백지 설계 다섯 가지의 내용이 전부 사람의 결정에서 왔고, 에이전트가 지어낸 제품 동작이 0건이다
- 이 플랜의 커밋 묶음에 코드 파일이 하나 이상 있어 CI가 실제로 돈다
</success_criteria>

## Artifacts this phase produces

이 플랜이 만드는 것(페이즈 전체 목록은 아래 「페이즈 전체 산출물 등록부」):

| 종류 | 이름 / 경로 |
|---|---|
| SYSTEM.md 신설 절 | `§6-7 로그인 화면` · `§6-8 시스템 상태 화면` · `§6-9 오류 페이지` · `§7-11 배너` · `§7-12 알림함 · 배지` |
| SYSTEM.md 보강 | `§6-0` 사용자 메뉴 진입점 · 관리자/직원 탭 행 · 소속 표기 주석 · 시스템 상태 진입점 (+ 항목 H가 ③이면 「설정」 제거) / `§7-1` 1차 버튼 위 `kbd` 테두리 규칙 / `§7-7` 두 번째 표 6행 추가 / `§1-2` 비활성 대비값 정정 |
| DECISIONS.md 기록 | 2026-09-19 × 5건 (표 미구현 이탈 · 임시 역할 행 · 소속 미표기 · D-20 범위 한정 · 대비값 정정) + 항목 I가 ②면 kbd 토큰 신설 1건 |
| tokens.css | `--viz-2` 주석 정정(값 불변) + 항목 I가 ②면 1차 버튼 위 `kbd` 테두리 토큰 1개 |
| 신규 파일 | `test/unit/design-system-docs.test.ts` |

### 페이즈 전체 산출물 등록부 (Phase 02)

| 플랜 | 종류 | 심볼 / 경로 |
|---|---|---|
| 02-01 | 문서 | SYSTEM.md §6-7 · §6-8 · §6-9 · §7-11 · §7-12, DECISIONS.md 5건, tokens.css 주석 |
| 02-01 | 테스트 | `test/unit/design-system-docs.test.ts` |
| 02-02 | 설정 | `eslint.config.mjs` boundaries element type `ui` (+ `app`·`test` allow 목록에 `ui`, 정책 `{ from: "ui", allow: ["ui","lib"] }`) |
| 02-02 | 신규 파일 | `stylelint.config.mjs` (규칙 `declaration-property-value-allowed-list` · `declaration-property-value-disallowed-list`) |
| 02-02 | 설정 | `package.json` 스크립트 `lint`에 stylelint 단계, devDependency `stylelint@17.15.0` |
| 02-02 | 설정 | `.dockerignore`에서 `docs` 제외 해제 · `ci.yml`/`deploy.yml` 트리거를 `paths-ignore`에서 문서화된 `paths` + `!` 형태로 교체(`**` → `!.planning/**` → `!docs/**` → `docs/design/tokens.css`) |
| 02-02 | 테스트 | `test/unit/stylelint-config.test.ts`, `test/unit/ci-guard.test.ts`·`test/unit/deploy/workflows.test.ts` 보강 |
| 02-02 | 문서 | `docs/ARCHITECTURE.md` §2에 `ui/` 계층 |
| 02-03 | 정적 자산 | `public/fonts/pretendard/` (woff2 92 + `pretendard-dynamic-subset.css` + `LICENSE.txt`) |
| 02-03 | 컴포넌트 | `ui/button/Button.tsx` + `Button.module.css` · `ui/input/TextField.tsx` + `TextField.module.css` · `ui/auth-frame/AuthFrame.tsx` + `AuthFrame.module.css` |
| 02-03 | 화면 | `app/layout.tsx`(토큰 import · 서체 link) · `app/globals.css` · `app/(auth)/login/page.tsx` · `login-form.tsx` |
| 02-04 | 데이터 | `ui/shell/role-menu.ts` — export `roleMenu(viewer)`, 타입 `NavItem`·`ShellMenu` |
| 02-04 | 컴포넌트 | `ui/shell/Shell.tsx` · `TopBar.tsx` · `BottomTabs.tsx` · `MoreSheet.tsx` (+ 각 `.module.css`) |
| 02-05 | 순수 함수 | `ui/next-turn/build-next-turn-view.ts` — export `buildNextTurnView`, 타입 `NextTurnItem`·`NextTurnView` |
| 02-05 | 컴포넌트 | `ui/next-turn/NextTurn.tsx` · `ui/list-empty/ListEmpty.tsx` · `ui/status-tag/StatusTag.tsx` (+ 각 `.module.css`) — 태그는 「내 차례」가 실제로 쓰는 자리이므로 같은 플랜에서 만들고 바로 배선한다 |
| 02-05 | 화면 | `app/(app)/page.tsx` · 빈 라우트 5개(`/projects` `/expenses` `/cards` `/approvals` `/pnl`) · `/settings`(체크포인트 항목 H가 ①일 때) |
| 02-05 | 테스트 | `test/unit/ui/next-turn.test.ts` |
| 02-06 | 컴포넌트 | `ui/banner/Banner.tsx` · `ui/toast/Toast.tsx` (+ 각 `.module.css`) |
| 02-06 | 화면 | `app/(app)/account/page.tsx` · `change-password-form.tsx` · `logout-button.tsx` · `app/(app)/admin/system-status/page.tsx` · `app/not-found.tsx` · `app/(app)/error.tsx` |
| 02-06 | 화면(조건부) | `app/(app)/not-found.tsx` — 항목 C②가 「셸 안」일 때만 |
| 02-06 | 배선(조건부) | `app/(app)/layout.tsx` 배너 한 줄(항목 A④가 「셸 상단 전역」일 때만) · `app/(auth)/login/page.tsx` 토스트(항목 F-1이 ②일 때만) |
| 02-07 | 설정 | `playwright.config.ts` projects `desktop` · `mobile-375` |
| 02-07 | 테스트 | `test/e2e/mobile-shell.spec.ts` · `test/e2e/keyboard-nav.spec.ts` · `test/e2e/a11y.spec.ts` |

<output>
Create `.planning/phases/02-design-system-app-shell/02-01-SUMMARY.md` when done
</output>
