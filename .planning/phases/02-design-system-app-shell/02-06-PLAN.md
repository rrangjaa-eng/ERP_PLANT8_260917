---
phase: 02-design-system-app-shell
plan: 06
type: execute
wave: 5
depends_on: ["02-04", "02-05"]
files_modified:
  - ui/banner/Banner.tsx
  - ui/banner/Banner.module.css
  - ui/toast/Toast.tsx
  - ui/toast/Toast.module.css
  - app/(app)/account/page.tsx
  - app/(app)/account/change-password-form.tsx
  - app/(app)/account/logout-button.tsx
  - app/(app)/layout.tsx        # 조건부 — 02-01 항목 A④가 「셸 상단 전역」일 때만 Task 2가 건드린다(02-04가 만든 파일, wave 3 < 5)
  - app/(auth)/login/page.tsx   # 조건부 — 02-01 항목 F-1이 ②(토스트)일 때만. ①이면 02-03 상태 그대로 둔다
  - app/(app)/admin/system-status/page.tsx
  - app/not-found.tsx
  - app/(app)/not-found.tsx     # 조건부 — 02-01 항목 C②가 「셸 안」일 때만 생긴다
  - app/(app)/error.tsx
autonomous: true
requirements: [UX-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "임시 비밀번호를 쓰는 직원이 SYSTEM.md §6-7이 정한 자리에서 그 사실을 알리는 배너를 본다 — 02-01 항목 A④가 「내 계정 화면 상단」이면 `app/(app)/account/page.tsx`가, 「셸 상단 전역」이면 `app/(app)/layout.tsx`가 그 배너를 렌더한다. 둘 중 정확히 하나이고, 배너 문구 문자열은 어느 쪽이든 Phase 1과 같다"
    - "직원이 현재 비밀번호를 틀리면 그 입력 아래에 서버가 준 이유가 보이고 입력값은 지워지지 않는다"
    - "직원이 비밀번호를 바꾸면 이동한 화면에서 결과가 보인다 — 버튼과 같은 단어로"
    - "직원이 어느 화면에서든 로그아웃할 수 있다"
    - "관리자가 시스템 상태 화면에서 배포 버전 · DB 커넥션 · 마지막 백업을 보고, 확인되지 않은 항목은 확인 불가로 표시된다"
    - "DB 커넥션이 한도를 넘으면 경고 배너가 보인다"
    - "직원이 관리자 화면 URL로 들어가면 404를 받고, 그 404 화면이 tokens.css 토큰 안에서 렌더된다 — 프레임워크 기본 화면이 아니다"
    - "존재하지 않는 URL도 토큰 안에서 렌더된 화면을 보여 주고 다음 한 수가 있다"
    - "화면에서 예외가 나도 토큰 안에서 렌더된 화면과 다시 시도가 보인다"
    - statement: "배너·토스트·상태 태그가 §7-7에 새로 추가된 다섯 상태 행과 어긋나지 않는다"
      verification: backstop
  prohibitions:
    - "관리자 전용 화면의 접근 제어 분기를 표현 교체 과정에서 약화하거나 우회 가능한 형태로 바꿔서는 안 된다"
    - "서버가 준 검증 메시지 문자열을 「더 나은 문구」로 바꿔서는 안 된다 — 그 문자열이 계약이고 E2E가 그것으로 찾는다"
    - "오류 화면에 사과 문구를 두어서는 안 된다(§8 규칙 4)"
  artifacts:
    - path: "ui/banner/Banner.tsx"
      provides: "§7-11 배너 — 화면에 걸린 조건을 알린다"
      exports: ["Banner"]
    - path: "ui/toast/Toast.tsx"
      provides: "§7-6 토스트 — 최대 1개, 화면 이동이 따르는 행동의 결과"
      exports: ["Toast"]
    - path: "app/(app)/admin/system-status/page.tsx"
      provides: "D-29 재구성 + D-17 접근 제어 보존"
      contains: "notFound"
    - path: "app/not-found.tsx"
      provides: "§6-9 오류 페이지 — 로그인 여부를 모르는 상태의 404. C②의 답과 무관하게 항상 만든다"
      min_lines: 10
    - path: "app/(app)/error.tsx"
      provides: "§6-9 오류 페이지 — 오류 경계. C②의 답과 무관하게 항상 만든다"
      min_lines: 10
    - path: "app/(app)/not-found.tsx"
      provides: "§6-9 오류 페이지 — 로그인한 사람이 보는 404. **02-01 항목 C②가 「셸 안」일 때만 만든다.** C②가 「셸 밖」이면 이 파일을 만들지 않고 관리자 게이트의 404도 루트 `app/not-found.tsx`로 떨어진다"
      min_lines: 10
      conditional: "02-01 항목 C② = 셸 안"
  key_links:
    - from: "app/(app)/account/change-password-form.tsx"
      to: "ui/input/TextField.tsx"
      via: "next-safe-action의 필드 오류를 TextField의 오류 prop에 그대로 넣는다"
      pattern: "validationErrors"
    - from: "app/(app)/admin/system-status/page.tsx"
      to: "ui/banner/Banner.tsx"
      via: "커넥션 한도 경고 배너"
      pattern: "Banner"
---

<objective>
Phase 1의 나머지 임시 화면 두 개(내 계정 · 관리자 시스템 상태)를 SYSTEM.md 기준으로 다시 만들고, 프레임워크 기본 화면이 튀어나오던 자리 세 곳(404 · 앱 안 404 · 예외)을 토큰 안으로 들여온다.

Purpose: 성공 기준 2가 「Phase 1의 임시 화면은 남아 있지 않다」이다. 그리고 관리자 화면은 이미 직원에게 404를 주고 있는데(D-17) 그 404가 Next.js 기본 화면이라 새 색·서체가 화면에 나타난다 — 기준 2의 「새 색·서체·radius가 없고」가 그 자리에서 깨진다. 배너는 SYSTEM.md에 한 번도 나온 적 없는 컴포넌트인데 이 두 화면이 실제로 요구한다(D-08 임시 비밀번호, D-17 한도 경고).
Output: `ui/` 컴포넌트 2개(배너 · 토스트) + 화면 재구성 2개 + 오류 화면 2~3개(§6-9의 C② 답에 따라). 상태 태그는 02-05가 만들어 「내 차례」에 배선했고 이 플랜은 그것을 가져다 쓴다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-app-shell/02-PATTERNS.md
@.planning/phases/02-design-system-app-shell/02-04-SUMMARY.md
@.planning/phases/02-design-system-app-shell/02-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 배너 · 토스트</name>
  <files>ui/banner/Banner.tsx, ui/banner/Banner.module.css, ui/toast/Toast.tsx, ui/toast/Toast.module.css</files>
  <read_first>
    - docs/design/SYSTEM.md §7-11 배너 — 02-01이 신설한 절. **이 절이 배너의 유일한 설계 출처다.** 지속 시간·개수 상한·위치·등급·ARIA 역할·토스트와의 경계가 거기 있다. 그 다섯은 02-01 체크포인트 **항목 D**의 다섯 답이 문장으로 들어온 것이다(①지속 ②한 화면의 개수 상한 ③위치 ④등급과 각 등급의 ARIA 역할 ⑤§7-6 토스트와의 경계) — 절에 그 문장이 없으면 항목 D가 소비되지 않은 것이므로 멈춘다
    - docs/design/SYSTEM.md §7-6 토스트(L603–607) — 위치(폰은 하단 탭 위), 면과 테두리, 최대 1개, 4초 뒤 사라짐(오류는 닫을 때까지), 내용은 버튼과 같은 단어 + 결과, 표 저장 결과는 토스트를 띄우지 않는다
    - docs/design/SYSTEM.md §7-7에 02-01이 추가한 토스트·배너 행 — 각 컴포넌트의 다섯 상태
    - docs/design/SYSTEM.md §10(L682–692) — 실시간 알림의 ARIA 규칙. 정보성 안내와 경고의 역할 구분
    - docs/design/system/preview.html L262(`div.toast`) — 실물 마크업
    - ui/status-tag/StatusTag.tsx — **02-05가 이미 만들어 「내 차례」에 배선한 §7-5 상태 태그다. 이 플랜은 다시 만들지 않고 Task 3에서 가져다 쓴다**
    - app/(app)/account/page.tsx L15 · app/admin/system-status/page.tsx L56 — 현재 두 배너가 각각 어떤 ARIA 역할을 쓰고 있는지. 새 컴포넌트가 그 역할을 그대로 낼 수 있어야 한다
  </read_first>
  <action>
`ui/banner/Banner.tsx` + CSS Module: §7-11이 규정한 대로 만든다. 등급 prop이 §7-11이 정한 등급 집합과 일치해야 하고, 각 등급의 ARIA 역할도 §7-11이 정한 대로다 — 현재 내 계정 화면의 임시 비밀번호 안내와 시스템 상태 화면의 한도 경고가 쓰는 두 역할이 각각 어느 등급에 대응하는지 SUMMARY에 적는다. 지속·개수·위치도 §7-11대로. §7-11에 없는 동작을 추가하지 않는다 — 없으면 02-01로 돌아가야 할 신호다.

**상태 태그는 만들지 않는다** — 02-05가 §7-5 계약대로 `ui/status-tag/StatusTag.tsx`를 이미 만들었고 「내 차례」 한 줄에 배선했다. 같은 것을 두 번 만들면 §7-5의 출처가 둘이 된다. Task 3이 그것을 import해서 쓴다.

`ui/toast/Toast.tsx` + CSS Module: §7-6 그대로. 최대 1개, 4초 뒤 사라짐(오류는 닫을 때까지), 하단 왼쪽(폰은 하단 탭 위), 실시간 알림 ARIA. 애니메이션은 §5 허용 목록 안에서만 쓰고 `prefers-reduced-motion`을 존중한다(tokens.css에 이미 그 미디어 쿼리가 있다).

두 CSS Module 모두 색·서체·radius가 전부 토큰 참조여야 한다. 실물 HTML의 흰색 리터럴은 같은 계산값의 배경 토큰 참조로 옮긴다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts</automated>
    <fails_when>vitest 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — §7-11 계약이 문서에서 사라진 경우다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ui/banner/Banner.tsx`가 `Banner`를, `ui/toast/Toast.tsx`가 `Toast`를 export한다
    - `Banner`의 등급 prop 타입이 §7-11이 정한 등급 집합과 원소 단위로 일치한다 — SUMMARY에 §7-11 원문과 타입의 대조표를 남긴다
    - `ui/banner/Banner.tsx`에 ARIA 역할을 지정하는 속성이 있고, 등급에 따라 값이 달라진다
    - `ui/toast/Toast.tsx`에 실시간 알림 ARIA 속성이 있다
    - `git status --porcelain ui/status-tag/`가 빈 출력이다 — 02-05가 만든 상태 태그를 다시 만들거나 고치지 않았다
    - `cat ui/banner/Banner.module.css ui/toast/Toast.module.css | grep -v '^\s*/\*' | grep -cE '#[0-9a-fA-F]{3,8}|rgba?\(|hsla?\('`가 0이다 — 파일별 개수가 아니라 합계 하나로 비교한다
    - 두 CSS Module에 `border-radius`가 있다면 값이 0 또는 토큰 참조다(`pnpm lint`가 강제)
    - 두 컴포넌트 어디에도 `domain`·`repositories`·`db` import가 없다
  </acceptance_criteria>
  <done>SYSTEM.md에 없던 배너가 계약과 함께 존재하고, 토스트가 §7-6대로 있다</done>
</task>

<task type="auto">
  <name>Task 2: 내 계정 화면 재구성 — §6-3 폼 템플릿 재사용</name>
  <files>app/(app)/account/page.tsx, app/(app)/account/change-password-form.tsx, app/(app)/account/logout-button.tsx, app/(app)/layout.tsx (A④가 「셸 상단 전역」일 때만), app/(auth)/login/page.tsx (F-1이 ②일 때만)</files>
  <read_first>
    - docs/design/SYSTEM.md §6-3 폼 화면(L366–400) — D-30이 「내 계정은 이 템플릿을 그대로 쓴다」고 정했다. 새 템플릿을 만들지 않는다
    - docs/design/SYSTEM.md §7-2(L541–550) · §7-1(L525–539) — 입력·오류·버튼 위계
    - docs/design/SYSTEM.md §7-11 · §7-6 — 배너와 토스트의 위치·지속
    - docs/design/SYSTEM.md §6-7 — 02-01이 정한 임시 비밀번호 배너의 위치와, 비밀번호 변경 뒤 도착하는 화면에서 결과가 어떻게 보이는지
    - app/(app)/account/page.tsx(21줄) · change-password-form.tsx(52줄) · logout-button.tsx(24줄) 전체 — 보존할 동작이 전부 여기 있다
    - .planning/phases/02-design-system-app-shell/02-PATTERNS.md §`app/(app)/account/...` — 보존할 동작 목록과 **바뀌면 안 되는 E2E 선택자 목록**이 그대로 적혀 있다
    - test/e2e/change-password.spec.ts L11–51 — 라벨 문자열 둘, 버튼 이름 둘, alert 역할, 그리고 바뀌면 안 되는 검증 메시지 문자열들
    - ui/input/TextField.tsx · ui/button/Button.tsx · ui/banner/Banner.tsx · ui/toast/Toast.tsx — 이미 있는 컴포넌트
    - app/(auth)/login/page.tsx — 02-03이 쓴 상태. 비밀번호 변경 뒤 돌아온 경우의 안내가 여기 있다
    - app/(app)/layout.tsx — 02-04 Task 3이 만든 상태. 세션을 읽어 `roleMenu`에 넘기고 `Shell`을 삽입한다. **§6-7이 임시 비밀번호 배너를 「셸 상단 전역」으로 정했다면 이 파일이 배너를 렌더하는 자리다** — 그 경우에만 건드린다
  </read_first>
  <action>
표현만 갈아끼운다. 보존할 동작:
- 미인증이면 로그인으로 보내는 세션 헬퍼 호출 — 어느 헬퍼를 쓰는지 바꾸지 않는다
- 임시 비밀번호 사용 여부에 따른 안내 — 문구 문자열을 그대로 두고, 감싸는 것만 배너 컴포넌트로 바꾼다. ARIA 역할이 전과 같은 값이어야 한다. **어느 화면에 놓을지는 §6-7이 정한 것 하나만 따른다**(바로 아래 참조)
- `next-safe-action` 훅 배선과 필드 오류·최상위 오류 추출 모양 — 그대로 둔다. 추출한 필드 오류 문자열을 `TextField`의 오류 prop에 그대로 넣는다
- 서버가 돌려주는 검증 메시지 문자열 — **한 글자도 바꾸지 않는다.** E2E가 그 문자열로 찾는다
- 최상위 오류의 alert 역할
- 로그아웃 버튼의 호출과 그 뒤 이동

**임시 비밀번호 배너의 자리는 §6-7이 정한 것 하나만 쓴다 — 두 갈래 중 하나다.** 02-01 체크포인트 항목 A④가 「내 계정 화면 상단」과 「셸 상단 전역」 중 하나를 골랐고 그 답이 §6-7에 문장으로 들어와 있다. §6-7을 읽고 아래 둘 중 정확히 하나를 실행한다:
- **내 계정 화면 상단이면** `app/(app)/account/page.tsx`가 `Banner`를 렌더한다. `app/(app)/layout.tsx`는 건드리지 않는다.
- **셸 상단 전역이면** `app/(app)/layout.tsx`가 `Banner`를 렌더한다 — 그 레이아웃은 이미 세션을 읽어 `roleMenu`에 넘기고 있으므로(02-04 Task 3) 임시 비밀번호 여부도 같은 세션 객체에서 온다. 배너는 `Shell`의 상단 바 바로 아래, 본문 위에 놓는다(§7-11의 「전역」 위치). 이 경우 `app/(app)/account/page.tsx`는 배너를 렌더하지 **않는다** — 같은 안내가 두 번 보이면 §7-11의 「한 화면에 몇 개까지」 규칙을 그 자리에서 깬다. 02-04가 만든 `app/(app)/layout.tsx`의 나머지(세션 읽기·`roleMenu` 호출·`Shell` 삽입)는 그대로 두고 배너 한 줄만 더한다.
어느 갈래든 **안내 문구 문자열과 ARIA 역할은 Phase 1과 같다.** §6-7이 둘 중 무엇을 적었는지 읽지 않은 채 한쪽을 가정하고 쓰지 않는다 — 추천안이 「내 계정 화면 상단」이라는 것은 사용자가 그것을 골랐다는 뜻이 아니다.

바꾸는 것: 라벨·입력·버튼을 `ui/` 컴포넌트로, 레이아웃을 §6-3 폼 템플릿으로. **보이는 라벨 문자열과 버튼 라벨 문자열을 바꾸지 않는다** — E2E가 라벨 텍스트와 버튼 이름으로 요소를 찾는다. 라벨과 입력의 id 연결도 유지한다. 버튼 위계는 §7-1대로 — 한 화면에 1차 버튼은 하나뿐이므로 비밀번호 변경이 1차, 로그아웃은 2차다. 진행 중에는 §7-1의 진행 중 규칙(누른 버튼 비활성 + 라벨 뒤 표시, 같은 폼의 다른 버튼도 비활성)을 쓴다.

**비밀번호 변경 성공의 결과 표시는 §6-7이 정한 형태 하나만 쓴다.** 02-01 체크포인트 항목 F-1이 이것을 이미 결정했고, 그 결정은 `test/e2e/change-password.spec.ts` L25–27이 URL 패턴 `/login?reason=password-changed$`와 문장 하나를 단언한다는 제약 위에서 내려졌다. §6-7이 「지금의 status 한 줄을 정식 자리로」라고 적었으면 `app/(auth)/login/page.tsx`는 02-03 상태 그대로 두고 토스트를 더하지 않는다. §6-7이 토스트 형태를 적었으면 그 토스트가 **기존 문장을 한 글자도 바꾸지 않은 채** 렌더되어야 한다.

**중단 조건:** §6-7이 정한 형태를 그대로 구현했을 때 `test/e2e/change-password.spec.ts`가 실패한다면, 스펙을 고치지 말고 멈춘다. 그 스펙 파일은 이 플랜의 files_modified에 없고, 「스펙 무수정 통과」가 아래 acceptance의 항목이다. 이것은 재계획 신호이지 executor가 풀 문제가 아니다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/change-password.spec.ts test/e2e/login-logout.spec.ts</automated>
    <fails_when>playwright 출력에 `failed` 개수가 1 이상이거나 종료 코드가 0이 아니다 — 라벨·버튼 선택자나 검증 메시지 문자열이 바뀐 경우다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/(app)/account/` 아래 세 파일에 원시 `<input`·`<button` 태그가 남아 있지 않다: `cat app/\(app\)/account/*.tsx | grep -cE '<input|<button'`가 0이다 — 글롭을 grep에 직접 넘기면 파일마다 한 줄씩 출력되어 비교할 값이 하나로 나오지 않는다(현재 리포에서 그 형태는 `3`·`1`·`0` 세 줄을 낸다)
    - `grep -c 'useAction' 'app/(app)/account/change-password-form.tsx'`가 1이다 — next-safe-action 배선이 그대로다
    - `grep -c 'validationErrors' 'app/(app)/account/change-password-form.tsx'`가 1 이상이다
    - 임시 비밀번호 배너를 렌더하는 파일이 §6-7이 정한 쪽 **하나뿐**이다: `node -e "const fs=require('fs');const n=['app/(app)/account/page.tsx','app/(app)/layout.tsx'].filter(p=>/Banner/.test(fs.readFileSync(p,'utf8'))).length;if(n!==1){console.error('BANNER PLACEMENT COUNT '+n);process.exit(1)}console.log('ok')"` — 0이면 배너가 어디에도 없고, 2면 같은 안내가 두 화면에 겹친다
    - 그 한 파일이 §6-7의 문장과 짝이 맞는다 — §6-7이 「내 계정 화면 상단」이면 `app/(app)/account/page.tsx`, 「셸 상단 전역」이면 `app/(app)/layout.tsx`다. SUMMARY에 §6-7 원문 한 줄과 실제 렌더 위치를 나란히 적는다
    - A④가 「내 계정 화면 상단」일 때: `git diff --stat 'app/(app)/layout.tsx'`가 빈 출력이다 — 02-04가 만든 레이아웃을 이 태스크가 건드리지 않았다
    - `test/e2e/change-password.spec.ts`가 스펙 파일 수정 없이 통과한다 — 스펙 파일의 git diff가 비어 있다
    - 임시 비밀번호 안내 문구 문자열이 재구성 전후로 동일하다(`git show HEAD:app/\(app\)/account/page.tsx`와 대조)
    - 한 화면의 1차 버튼이 정확히 1개다(§7-1)
  </acceptance_criteria>
  <done>내 계정 화면이 §6-3 폼 템플릿 그대로이고, Phase 1의 모든 동작과 모든 검증 메시지가 살아 있다</done>
</task>

<task type="auto">
  <name>Task 3: 시스템 상태 화면 재구성 + 오류 화면(§6-9가 정한 수만큼)</name>
  <files>app/(app)/admin/system-status/page.tsx, app/not-found.tsx, app/(app)/error.tsx, app/(app)/not-found.tsx (§6-9가 「셸 안」을 정했을 때만)</files>
  <read_first>
    - node_modules/next/dist/docs/ 의 not-found·error 파일 규약 문서 — **이 버전의 Next.js는 훈련 데이터와 다르다.** 루트 not-found와 세그먼트 not-found의 해석 차이, error 파일이 클라이언트 컴포넌트여야 하는지, 전역 오류 경계가 별도인지 코드 쓰기 전에 확인한다
    - docs/design/SYSTEM.md §6-8 시스템 상태 화면 — 02-01이 신설한 템플릿. **유일한 설계 출처다.** 02-01 체크포인트 **항목 B**의 세 답이 여기 문장으로 있다(①세 항목을 표로 볼지 라벨·값 목록으로 볼지 ②「확인 불가」를 글자로 둘지 상태 태그로 둘지 ③한도 배너의 위치) — 그 문장이 없으면 항목 B가 소비되지 않은 것이므로 멈춘다
    - docs/design/SYSTEM.md §6-9 오류 페이지 — 02-01이 신설한 템플릿. 세 변종과 각각의 다음 한 수
    - app/(app)/admin/system-status/page.tsx 전체(60줄, 02-04이 옮긴 위치) — 맨 위 세 줄(캐시 지시자 · 미인증 리다이렉트 · 관리자가 아니면 404)이 **건드리면 안 되는 부분**이고 그 아래 JSX가 교체 대상이다
    - test/e2e/system-status.spec.ts L9–37 — 바뀌면 안 되는 텍스트 선택자들과 한도 배너의 문구 패턴
    - .planning/phases/02-design-system-app-shell/02-PATTERNS.md §`app/admin/system-status/page.tsx — D-29 rewrite` — 보존 목록과 선택자 목록
    - ui/banner/Banner.tsx · ui/status-tag/StatusTag.tsx · ui/list-empty/ListEmpty.tsx — 이미 있는 컴포넌트. 오류 화면의 한 줄은 `ListEmpty`의 오류 톤을 쓴다(02-05가 그 prop을 두었다)
    - docs/design/SYSTEM.md §8 규칙 4 — 사과 문구 금지
  </read_first>
  <action>
(1) **시스템 상태 화면.** 맨 위 접근 제어 세 줄을 문자 그대로 보존한다 — 캐시 지시자, 미인증 리다이렉트, 관리자가 아니면 404. 이 아래의 JSX만 §6-8 템플릿으로 바꾼다. 세 항목의 라벨 문자열과 확인 불가 표시 문자열을 바꾸지 않는다 — E2E가 그 텍스트로 찾는다. 확인 불가는 §6-8이 정한 방식(상태 태그인지 글자인지)으로 표현하되 보이는 문자열은 그대로다. 커넥션 한도 경고는 배너 컴포넌트로 감싸되 **문구 패턴을 바꾸지 않는다** — E2E가 정규식으로 찾는다. 배너 위치는 §6-8이 정한 대로.

(2) **오류 화면.** §6-9 템플릿을 따른다. 전부 한 줄(무엇이 안 됐다 · 다음 행동) + 다음 한 수이고, `ListEmpty`의 오류 톤을 쓴다. 사과 문구를 두지 않는다. 그림·일러스트를 두지 않는다.

**몇 개를 만들지는 §6-9가 정한다 — 02-01 체크포인트 항목 C②(셸 안인지 밖인지)의 답이 §6-9에 문장으로 들어와 있다.** 읽고 아래 둘 중 하나를 실행한다:
- **§6-9가 「셸 안」이라고 적었으면 세 파일**: `app/not-found.tsx`(존재하지 않는 URL — 로그인 여부를 모르는 상태이므로 셸 밖) · `app/(app)/not-found.tsx`(로그인한 사람이 보는 404. 관리자 화면 접근 제어가 던지는 404가 여기로 오고, 셸 안에서 렌더되므로 직원이 자기 위치를 잃지 않는다) · `app/(app)/error.tsx`(오류 경계).
- **§6-9가 「셸 밖」이라고 적었으면 두 파일**: `app/(app)/not-found.tsx`를 **만들지 않는다.** 관리자 게이트가 던지는 404도 라우트 그룹을 벗어나 루트 `app/not-found.tsx`로 떨어지고, 그 화면 하나가 로그인 여부와 무관하게 모든 404를 덮는다. 이 경우 그 화면의 「다음 한 수」는 로그인 상태를 가정할 수 없으므로 §6-9가 적은 그대로만 쓴다. `app/(app)/error.tsx`는 이 답과 무관하게 만든다 — C②는 404의 자리를 묻는 항목이지 오류 경계의 자리를 묻는 항목이 아니다.
세그먼트별 `not-found` 해석 규칙(라우트 그룹 안의 `notFound()` 호출이 어느 파일로 가는지)은 이 버전 Next.js 문서로 먼저 확인한다 — 「셸 밖」을 고른 경우 `app/(app)/not-found.tsx`가 없어야 루트로 떨어지는지가 이 갈래의 유일한 기술적 전제다.

오류 경계 — 화면에서 예외가 났을 때. §7-7 ERROR 행대로 「다시 시도」 2차 버튼이 있다. Next.js 규약상 클라이언트 컴포넌트여야 하는지 문서로 확인하고 따른다.

만드는 파일 전부가 프레임워크 기본 화면을 대신하는 것이 목적이다 — 기본 화면은 자체 색·서체를 쓰므로 성공 기준 2의 「새 색·서체·radius가 없고」를 그 자리에서 깬다. **「셸 밖」을 골라 `app/(app)/not-found.tsx`를 만들지 않는 경우에도, 로그인한 사람이 만나는 404가 프레임워크 기본 화면이 아니라 루트 `app/not-found.tsx`로 간다는 것을 실행으로 확인한다** — 그 확인이 되지 않으면 이 갈래는 성립하지 않으므로 멈추고 SUMMARY에 올린다.

(3) 접근 제어가 살아 있는지 실행으로 확인한다 — 직원 계정으로 관리자 URL에 들어가면 404 화면이 나오고 상태 정보가 보이지 않아야 한다. E2E가 그것을 이미 검사하므로 스펙 파일을 고치지 않고 통과해야 한다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/system-status.spec.ts</automated>
    <fails_when>playwright 출력에 `failed` 개수가 1 이상이거나 종료 코드가 0이 아니다 — 직원 404 케이스가 깨지면 접근 제어가 손상된 것이다</fails_when>
    <automated>node -e "const cur=require('fs').readFileSync('app/(app)/admin/system-status/page.tsx','utf8');for(const t of ['force-dynamic','notFound()','redirect('])if(!cur.includes(t)){console.error('GATE LINE LOST: '+t);process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `GATE LINE LOST: ` 로 시작하는 줄이 나오고 종료 코드가 1이다</fails_when>
    <automated>pnpm build &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project unit</automated>
    <fails_when>next build·eslint·stylelint·tsc·vitest 중 하나라도 error나 failed를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/(app)/admin/system-status/page.tsx`에 캐시 지시자·미인증 리다이렉트·404 호출 세 요소가 모두 남아 있다
    - `test/e2e/system-status.spec.ts`가 스펙 파일 수정 없이 통과한다 — 그 파일의 git diff가 비어 있다
    - `app/not-found.tsx`와 `app/(app)/error.tsx`가 존재한다. `app/(app)/not-found.tsx`는 §6-9가 「셸 안」을 정했을 때만 존재하고, 「셸 밖」을 정했으면 존재하지 않는다 — 어느 쪽인지가 SUMMARY에 §6-9 원문 한 줄과 함께 적혀 있다
    - 만들어진 오류 화면 어디에도 사과 표현이 없다 — 파일 목록이 C②에 따라 달라지므로 실재하는 파일만 합쳐 **한 개의 수**로 센다: `cat app/not-found.tsx 'app/(app)/error.tsx' $(test -f 'app/(app)/not-found.tsx' && echo 'app/(app)/not-found.tsx') | grep -icE '죄송|미안|sorry'`가 0이다. 글롭이나 파일 목록을 grep에 직접 넘기면 파일마다 한 줄씩 나와 비교할 값이 하나로 모이지 않는다
    - 만들어진 오류 화면 각각에 다음 한 수에 해당하는 요소가 1개 이상 있다
    - 만들어진 오류 화면 어디에도 그림 요소가 없다: `cat app/not-found.tsx 'app/(app)/error.tsx' $(test -f 'app/(app)/not-found.tsx' && echo 'app/(app)/not-found.tsx') | grep -cE '<img|<svg'`가 0이다 — 같은 이유로 합쳐서 한 수로 센다
    - `pnpm build` 출력에 Next.js 기본 404 페이지가 아니라 프로젝트 404가 빌드되었음이 라우트 목록에 나타난다
  </acceptance_criteria>
  <done>Phase 1의 임시 화면이 하나도 남지 않았고, 프레임워크 기본 화면이 나오던 세 자리가 토큰 안으로 들어왔으며, 관리자 접근 제어가 그대로다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 직원 → 관리자 전용 화면 | D-17의 게이트가 유일한 방어선. 표현 교체가 이 게이트를 지나간다 |
| 서버 검증 → 화면 오류 표시 | 오류 표시 방식만 바뀌고 검증 자체는 불변이어야 한다 |
| 예외 → 오류 경계 | 오류 화면이 스택 트레이스나 내부 경로를 노출하면 정보가 샌다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Elevation of Privilege | 시스템 상태 화면 접근 제어 세 줄 | critical | mitigate | 세 요소의 존재를 자동 검증으로 고정하고, 직원 404 케이스를 담은 E2E를 스펙 수정 없이 통과시킨다 |
| T-02-18 | Information Disclosure | 오류 경계 화면 | medium | mitigate | 오류 화면이 보이는 것은 §6-9가 정한 한 줄과 다시 시도뿐이다 — 예외 메시지·스택·내부 경로를 렌더하지 않는다 |
| T-02-19 | Tampering | 서버 검증 메시지 문자열 | medium | mitigate | 메시지 문자열 불변을 prohibitions와 acceptance criteria로 고정하고 E2E가 그 문자열로 찾는다 |
| T-02-20 | Information Disclosure | 시스템 상태 값(배포 버전·DB 커넥션·백업) | medium | mitigate | 값을 읽는 로직과 접근 제어를 건드리지 않는다. 표현만 교체 |
</threat_model>

<verification>
- `pnpm build` · `pnpm lint` · `pnpm typecheck` · `pnpm test:unit` 통과
- `pnpm playwright test test/e2e/change-password.spec.ts test/e2e/login-logout.spec.ts test/e2e/system-status.spec.ts` 통과, 세 스펙 파일 모두 무수정
- 접근 제어 세 요소 존재 검증 통과
- `ui/**/*.module.css`에 색 리터럴 0개
</verification>

<success_criteria>
- Phase 1의 임시 화면이 남아 있지 않다(성공 기준 2)
- 새 색·서체·radius가 나타나던 프레임워크 기본 화면 세 곳이 토큰 안으로 들어왔다
- D-17 접근 제어가 재구성 후에도 동작한다
</success_criteria>

## Artifacts this phase produces

이 플랜이 만드는 것(페이즈 전체 등록부는 `02-01-PLAN.md`):

| 종류 | 이름 / 경로 |
|---|---|
| 컴포넌트 | `ui/banner/Banner.tsx` (export `Banner`, 등급 prop) + `Banner.module.css` |
| 컴포넌트 | `ui/toast/Toast.tsx` (export `Toast`) + `Toast.module.css` |
| (재사용) | `ui/status-tag/StatusTag.tsx` — 02-05가 만든 것을 Task 3이 import한다. 이 플랜은 만들지 않는다 |
| 화면 | `app/(app)/account/page.tsx` · `change-password-form.tsx` · `logout-button.tsx` |
| 화면 | `app/(app)/admin/system-status/page.tsx` |
| 화면 | `app/not-found.tsx` · `app/(app)/error.tsx` |
| 화면(조건부) | `app/(app)/not-found.tsx` — 02-01 항목 **C②가 「셸 안」일 때만**. 「셸 밖」이면 만들지 않고 모든 404가 루트 `app/not-found.tsx`로 떨어진다 |
| 배선(조건부) | `app/(app)/layout.tsx` — 02-01 항목 **A④가 「셸 상단 전역」일 때만** 임시 비밀번호 배너를 여기에 놓는다. 「내 계정 화면 상단」이면 이 파일을 건드리지 않고 `app/(app)/account/page.tsx`가 배너를 쓴다. 파일 자체는 02-04 Task 3이 만든다 |
| 배선(조건부) | `app/(auth)/login/page.tsx` — 02-01 항목 **F-1이 ②(토스트)일 때만** 비밀번호 변경 결과 토스트를 더한다. F-1이 ①(추천안, status 한 줄 승격)이면 02-03이 쓴 상태 그대로 두고 이 플랜은 이 파일을 건드리지 않는다. F-1③은 재계획 신호라 여기 도달하지 않는다 |

<output>
Create `.planning/phases/02-design-system-app-shell/02-06-SUMMARY.md` when done
</output>
