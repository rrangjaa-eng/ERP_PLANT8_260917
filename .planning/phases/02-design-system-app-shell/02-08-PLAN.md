---
phase: 02-design-system-app-shell
plan: 08
type: execute
wave: 7
depends_on: ["02-07"]
gap_closure: true
files_modified:
  - app/globals.css
  - ui/shell/TopBar.module.css
  - ui/form-alert/FormAlert.tsx
  - ui/form-alert/FormAlert.module.css
  - ui/kv-list/KvList.tsx
  - ui/kv-list/KvList.module.css
  - ui/page-header/PageHeader.tsx
  - ui/page-header/PageHeader.module.css
  - ui/shell/current-path.ts
  - ui/shell/TopBar.tsx
  - ui/shell/BottomTabs.tsx
  - ui/shell/BottomTabs.module.css
  - app/(auth)/login/login-form.tsx
  - app/(app)/account/change-password-form.tsx
  - app/(app)/account/page.tsx
  - app/(app)/admin/system-status/page.tsx
  - app/(app)/page.tsx
  - app/(app)/projects/page.tsx
  - app/(app)/expenses/page.tsx
  - app/(app)/cards/page.tsx
  - app/(app)/approvals/page.tsx
  - app/(app)/pnl/page.tsx
  - app/(app)/settings/page.tsx
  - app/(app)/not-found.tsx
  - app/(app)/error.tsx
  - app/not-found.tsx
  - test/e2e/page-chrome.spec.ts
  - test/e2e/mobile-page-chrome.spec.ts
  - test/unit/ui/current-path.test.ts
  - .planning/WINDOWS.md
autonomous: true
requirements: [UX-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "app/globals.css의 body 규칙이 실물(form-expense.html:18)의 아홉 선언을 전부 토큰 참조로 갖고(background·color·font-family·font-size·line-height·letter-spacing·word-break·overflow-wrap·-webkit-font-smoothing) UA 여백(margin)을 0으로 접는다 — 렌더된 body의 계산값이 color rgb(11, 21, 18) · font-size 14px(폰 375에서 15px) · line-height 22.4px · letter-spacing -0.21px · word-break keep-all · overflow-wrap anywhere · margin 0px다"
    - "§4-4 브라우저 기본 표면이 전부 토큰이다 — ::selection 배경 rgb(220, 232, 228)/글자 rgb(11, 21, 18), caret-color·accent-color rgb(0, 84, 70), scrollbar-color에 rgb(124, 138, 134)가 들어가고 scrollbar-width thin, 컴포넌트 포커스 스타일이 없는 요소(목록 EMPTY의 3차 링크)의 :focus-visible 링이 2px solid rgb(0, 84, 70) offset 2px, tab-size 4"
    - "button·input·select·textarea가 UA 컨트롤 서체(Arial)가 아니라 --font-sans(Pretendard Variable로 시작)로 렌더된다"
    - "app/**/*.tsx에 맨 h1 요소가 없다(로그인 화면의 sr-only 제목 하나만 예외) — 12개 화면(홈·1차 메뉴 5·설정·내 계정·시스템 상태·404×2·오류 경계)이 전부 ui/page-header/PageHeader를 쓰고, /projects의 h1 계산값이 18px · 700 · letter-spacing -0.36px · line-height 25.2px, 부제가 12px · rgb(78, 93, 89)다"
    - "오류 페이지 셋(404 루트·404 셸·오류 경계)의 제목은 §6-9대로 --fs-2xl — 계산값 32px · letter-spacing -0.64px · line-height 41.6px(크기 32px만은 UA 2em×16px와 우연히 같아 변경 전에도 참이므로 자간·행간이 판정 기준이다)"
    - "로그인 실패 문구(form 안의 role=alert 요소)가 ui/form-alert/FormAlert로 렌더되어 color rgb(155, 28, 28)다(§6-7 A②) — 비밀번호 변경 폼의 serverError도 같은 컴포넌트다"
    - "시스템 상태의 라벨·값 목록이 ui/kv-list/KvList로 렌더되어 dt 계산값이 12px · 600 · rgb(78, 93, 89) · 폭 96px(폰 375에서 84px) · 아래 점선 rgb(207, 219, 215)이고 dd의 UA 들여쓰기(margin-left 40px)가 0이다(§6-8 B①)"
    - "/projects에서 주 메뉴(nav[aria-label=\"주 메뉴\"])의 링크 정확히 하나에 aria-current=\"page\"가 붙고 그 텍스트가 「프로젝트」이며 계산값이 700 · rgb(220, 232, 228) · inset box-shadow다; /account에서는 0개다. 폰 375의 /에서는 하단 탭 정확히 하나(「내 차례」)가 aria-current=\"page\"이고 계산값이 rgb(0, 84, 70) · inset box-shadow다(WR-01, §6-0)"
    - "기존 E2E 스펙 6개(login-logout·change-password·system-status·keyboard-nav·mobile-shell·a11y)와 기존 단위 테스트 파일이 커밋 25aaa4f와 바이트 단위로 같고, pnpm lint(stylelint D-20 포함)·pnpm typecheck·pnpm test:unit·pnpm test:e2e가 전부 통과한다"
    - "docs/design/tokens.css·SYSTEM.md·DECISIONS.md가 커밋 25aaa4f와 같다 — 새 토큰 0개, 문서 이탈 기록 0건(필요 없음: 이 플랜은 SYSTEM.md를 벗어나지 않고 그대로 구현한다)"
    - ".planning/WINDOWS.md의 항목 5(TopBar aria-current 미배선)가 fixed다"
  prohibitions:
    - "기존 E2E 스펙 6개와 기존 단위 테스트를 한 글자도 고쳐서는 안 된다 — 통과시키려고 스펙을 손대는 것은 재계획 신호다"
    - "tokens.css에 토큰을 더하거나 DECISIONS.md·SYSTEM.md를 고쳐서는 안 된다 — 이 플랜의 모든 값은 이미 있는 토큰이고, design-system-docs.test.ts가 2026-09-19 기록 건수를 6으로 고정하고 있어 기록 1건 추가만으로 기존 테스트가 깨진다"
    - "런타임·개발 의존성을 추가해서는 안 된다(D-19)"
    - "CSS에 색·서체·radius 리터럴이나 font 축약을 써서는 안 된다(D-20) — 모든 값은 var(--…) 또는 색이 아닌 리터럴이다"
    - "ui/ 컴포넌트가 domain·repositories·db·app을 import해서는 안 된다(D-26); 셸 컴포넌트에 역할 분기를 두어서는 안 된다(D-23)"
    - "WR-02~WR-07, IN-01~IN-07을 이 플랜에서 손대서는 안 된다(사용자 범위 지정) — 특히 WR-07(레이아웃 단독 인증)은 Phase 4 전 별도 수정이다"
  artifacts:
    - path: "app/globals.css"
      provides: "페이지 층 — body 아홉 선언 + margin 0 · 컨트롤 서체 · §4-4 표면 · 전역 :focus-visible"
      contains: "word-break: keep-all"
    - path: "ui/page-header/PageHeader.tsx"
      provides: "§6-0 화면 제목(--fs-lg)+부제(--fs-sm --muted) · §6-9 오류 제목(--fs-2xl) 컴포넌트"
      exports: ["PageHeader"]
    - path: "ui/page-header/PageHeader.module.css"
      provides: "실물 .sec/.sec h1/.sec .sub 이관"
      contains: "font-size: var(--fs-lg)"
    - path: "ui/form-alert/FormAlert.tsx"
      provides: "폼 수준 실패 문구(role=alert, --danger)"
      exports: ["FormAlert"]
    - path: "ui/kv-list/KvList.tsx"
      provides: "§6-8 B① · §7-8 시트 상세와 같은 라벨·값 목록(dl) 골격"
      exports: ["KvList"]
    - path: "ui/kv-list/KvList.module.css"
      provides: "실물 sheet-modal.html .kv 이관(라벨 96/84, 점선 --line)"
      contains: "grid-template-columns: var(--label-w) 1fr"
    - path: "ui/shell/current-path.ts"
      provides: "pathname ↔ 메뉴 href 현재 여부 순수 함수"
      exports: ["isCurrentPath"]
    - path: "ui/shell/TopBar.tsx"
      provides: "주 메뉴 링크 aria-current 배선(usePathname)"
      contains: "aria-current"
    - path: "ui/shell/BottomTabs.module.css"
      provides: "폰 하단 탭 현재 표시(--inset-tab)"
      contains: "aria-current=\"page\""
    - path: "test/e2e/page-chrome.spec.ts"
      provides: "데스크톱 계산값 검증 — 본문·§4-4·컨트롤 서체·알림·dl·제목/부제·오류 제목·현재 메뉴"
      min_lines: 120
    - path: "test/e2e/mobile-page-chrome.spec.ts"
      provides: "폰 375 계산값 검증 — 본문 15px·dt 84px·하단 탭 현재 표시"
      min_lines: 40
    - path: "test/unit/ui/current-path.test.ts"
      provides: "isCurrentPath 경계 사례 고정"
      min_lines: 20
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "기존 import — 페이지 층 규칙이 모든 라우트(셸 안팎)에 적용되는 통로"
      pattern: "import \"./globals.css\""
    - from: "test/e2e/page-chrome.spec.ts"
      to: "app/globals.css"
      via: "getComputedStyle(document.body)로 렌더된 계산값을 잰다 — 파일 텍스트가 아니라 결과"
      pattern: "getComputedStyle"
    - from: "app/(app)/projects/page.tsx"
      to: "ui/page-header/PageHeader.tsx"
      via: "PageHeader import + 렌더(12개 화면 동일)"
      pattern: "PageHeader"
    - from: "app/(auth)/login/login-form.tsx"
      to: "ui/form-alert/FormAlert.tsx"
      via: "실패 문구 렌더"
      pattern: "FormAlert"
    - from: "app/(app)/admin/system-status/page.tsx"
      to: "ui/kv-list/KvList.tsx"
      via: "세 항목(배포 버전·DB 커넥션·마지막 백업)을 items로 넘긴다"
      pattern: "KvList"
    - from: "ui/shell/TopBar.tsx"
      to: "ui/shell/current-path.ts"
      via: "usePathname() 결과를 isCurrentPath(pathname, item.href)로 판정해 aria-current를 붙인다"
      pattern: "isCurrentPath"
    - from: "ui/shell/BottomTabs.tsx"
      to: "ui/shell/BottomTabs.module.css"
      via: "aria-current=\"page\" 속성 선택자가 --inset-tab을 켠다"
      pattern: "aria-current"
---

<objective>
`02-VERIFICATION.md`가 실패로 판정한 성공 기준 2의 유일한 갭 — **컴포넌트 층은 토큰만 쓰지만 그 아래 페이지 층이 UA 기본값으로 렌더된다** — 를 닫는다. 실물 `docs/design/system/form-expense.html:16-21`이 전역으로 정한 것(body 아홉 선언 · 컨트롤 서체 상속 · `::selection` · `:root` 표면 · `:focus-visible`)을 `app/globals.css`에 토큰 참조로 이관하고, SYSTEM.md가 화면 골격 단위로 정한 세 가지(§6-0 화면 제목+부제, §6-7 A② 로그인 실패 문구, §6-8 B① 라벨·값 목록)를 `ui/` 컴포넌트로 만들어 12개 화면에 적용한다. 같은 결의 §6-0 셸 계약 위반인 **WR-01(현재 메뉴 `aria-current` 미배선)** 을 함께 닫는다(`02-REVIEW.md`, `.planning/WINDOWS.md` 항목 5).

Purpose: 성공 기준 2 「SYSTEM.md 컴포넌트와 tokens.css 토큰만 쓴다」는 컴포넌트 층만 보면 참이지만 배포된 화면은 본문 검정 16px · 제목 UA 2em · `word-break` 없음(한글 단어 중간 줄바꿈) · Arial 컨트롤 · 8px UA 여백으로 렌더된다. 02-05-SUMMARY.md:59가 「무스타일 관례」라 적었으나 `DECISIONS.md`에 없으므로 결정된 이탈이 아니라 누락이고, 이후 어떤 페이즈도 기본 타이포그래피를 다루지 않는다. 검증자가 같은 기준 2로 재검증하므로 이 플랜의 인수 조건은 전부 **변경 전에는 실패하는 계산값 단언**이다(아래 「변경 전 실측」).

Output: `app/globals.css` 페이지 층 · `ui/page-header/PageHeader` · `ui/form-alert/FormAlert` · `ui/kv-list/KvList` · `ui/shell/current-path.ts` · 12개 화면 적용 · `TopBar`/`BottomTabs` `aria-current` 배선 · E2E 스펙 2개(신규) · 단위 테스트 1개(신규) · WINDOWS.md 항목 5 fixed.

**형태에 대한 메모.** 트레이서 슬라이스를 따로 두지 않는다 — 셸·토큰 import·CSS Modules·Playwright 두 프로젝트는 02-03~02-07이 이미 끝까지 증명한 구조이고, 이 플랜은 그 위에 규칙을 더하는 갭 클로저라 얇은 슬라이스가 새 정보를 주지 않는다(`--no-tracer` 근거). 대신 세 태스크 전부 **스펙 먼저 → 빨강 확인 → 구현 → 초록** 순서를 강제한다(`tdd="true"`). 태스크당 파일 수가 3-5개 지침을 넘는 이유: Task 2의 12개 화면 파일은 각각 30줄 이하이고 변경은 h1+p 두 줄을 PageHeader 한 줄로 바꾸는 기계적 치환이라 파일 수가 문맥 비용을 대표하지 않는다(플랜 전체 예상 80k 토큰).

**변경 전 실측(플래너가 2026-09-19 이 환경의 Playwright로 잰 값 — 스펙이 빨강임을 이것으로 안다).** 데스크톱 `/login` body: color `rgb(0, 0, 0)` · background `rgba(0, 0, 0, 0)` · font-size `16px` · line-height `normal` · letter-spacing `normal` · word-break `normal` · overflow-wrap `normal` · margin `8px` · tab-size `8`. `documentElement`: accent-color `auto` · caret-color `rgb(0, 0, 0)` · scrollbar-color `auto` · scrollbar-width `auto`. `::selection`(body): background `rgba(0, 0, 0, 0)`. 이메일 input font-family `Arial`, 로그인 button font-family `Arial`. 로그인 실패 문구(`form [role="alert"]`): color `rgb(0, 0, 0)` · 16px — **주의: `getByRole("alert")`는 실패 뒤 2개(Next 라우트 어나운서 + 폼 문구)를 잡으므로 스펙은 반드시 `form` 범위로 좁힌다.** `/projects` h1: `32px` · 700 · letter-spacing `normal` · line-height `normal`; 부제 p: `16px` · `rgb(0, 0, 0)`; 주 메뉴 `[aria-current="page"]` 0개; 목록 EMPTY 3차 링크 포커스 링: outline-style `auto` · 1px · `rgb(16, 16, 16)` · offset 1px. 루트 404 h1: `32px` · letter-spacing `normal` · line-height `normal`(크기 32px는 UA 2em×16px — 변경 후 `--fs-2xl` 32px와 같으므로 크기 단언은 공허하고 자간·행간만 빨강이다). `/admin/system-status` dt: `16px` · 400 · `rgb(0, 0, 0)` · 폭 `1224px` · border-bottom none; dd margin-left `40px`. 폰 375 `/login` body font-size `16px`; `/` 하단 탭 `[aria-current="page"]` 0개(첫 탭 color `rgb(78, 93, 89)`, box-shadow `none`); dt 폭 `331px`.

**결정 — 화면 제목·부제는 전역 요소 선택자가 아니라 `ui/page-header/PageHeader` 컴포넌트다.** 검증 보고서가 둘을 다 열어 두었는데, 컴포넌트를 고르는 근거: (1) 실물 자체가 `main > h1` 같은 요소 규칙이 아니라 `.sec{display:flex;align-items:baseline;gap;flex-wrap;padding-bottom}` + `.sec h1` + `.sec .sub`라는 **블록**으로 모델링했다(preview.html:48-50, form-expense.html:38-41). (2) 요소 인접 선택자(`h1 + p`)는 구조 우연에 기댄다 — 지금 코드에서도 내 계정 화면은 h1 다음 p가 이메일이고, 오류 페이지 셋은 h1 다음 p가 `ListEmpty`의 `<p>`라서 부제 규칙이 엉뚱한 요소를 물고, Phase 4가 제목과 부제 사이에 도구 줄을 끼우는 순간 조용히 풀린다. 11개 화면이 소비하는 규칙을 그런 결합에 두면 되돌리는 비용이 크다. (3) 이 페이즈의 관례가 `ui/<component>/<Name>.tsx + .module.css`이고(02-05 patterns-established) D-26 경계가 그 자리를 lint로 지킨다. (4) 실물 `.sec .right`(우측 행동 자리)는 Phase 4가 prop으로 확장하면 되고 전역 CSS를 건드릴 일이 없다 — 단, 지금은 소비자가 없으므로 그 슬롯을 **만들지 않는다**(D-25: 추측으로 만드는 것 없음). 같은 논리로 §6-7 A② 실패 문구는 `ui/form-alert/FormAlert`(소비자 2: 로그인 · 비밀번호 변경 serverError), §6-8 B① 목록은 `ui/kv-list/KvList`(소비자 1 지금, §7-8 시트 상세가 같은 골격을 쓴다고 SYSTEM.md가 명시)로 둔다. `globals.css`에 들어가는 것은 실물이 **요소 단위로 전역에** 정한 것뿐이다 — 02-03이 「컴포넌트 스타일은 globals.css에 넣지 않는다(D-19)」고 정한 것과 충돌하지 않는다.

**§6-9 오류 제목 크기에 대한 메모.** §6-9 도해는 오류 페이지 제목을 `--fs-2xl`로 적었고, §2-2 표는 `--fs-2xl`을 「손익 대시보드 KPI 타일 큰 숫자만」이라 적었다. SYSTEM.md 안의 긴장이지만 화면 전용 절(§6-9)이 더 구체적인 정본이므로 **이 플랜은 §6-9를 그대로 구현한다**(PageHeader `titleSize="2xl"`). SYSTEM.md 정정은 이 플랜의 일이 아니다 — `design-system-docs.test.ts`가 DECISIONS 2026-09-19 기록을 정확히 6건으로 고정하고 있어(WR-09 후속으로 완화 제안만 있고 미적용) 기록 1건 추가가 기존 테스트를 깨뜨린다. 사람 판단 항목으로 SUMMARY에 올린다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-app-shell/02-VERIFICATION.md
@.planning/phases/02-design-system-app-shell/02-CONTEXT.md
@CLAUDE.md
@docs/design/tokens.css
@app/globals.css
@app/layout.tsx
</context>

<scope>
**닫는 것:** VERIFICATION `gaps[0]`의 `missing` 1~4(body 이관 · §4-4 표면 · 제목/부제 규칙 + 11개 화면 적용 · 로그인 실패 문구 --danger + 시스템 상태 dl) + 플래너 실측이 추가로 드러낸 같은 층의 UA 기본값 둘(body margin 8px · 컨트롤 Arial) + WR-01(aria-current). `missing` 5(DECISIONS.md 이탈 기록 + override)는 갭을 **고치지 않을 때**의 대안 경로이므로 해당 없음.

**닫지 않는 것(의도적):** WR-02~WR-07 · IN-01~IN-07(사용자 범위 지정; WR-07은 Phase 4 전 별도) · 로그인 실패 문구의 카피가 better-auth 영문(`Invalid email or password`)인 것(§6-7 A②의 한국어 문장과 다르다 — 스타일이 아니라 카피 결함, SUMMARY에 보고) · 비밀번호 변경 폼의 h2(SYSTEM.md에 폼 안 소제목 규칙이 없다 — 규칙 없이 크기를 정하면 발명이다; body 토큰은 상속된다) · 「더보기」 시트 항목의 현재 표시(§6-0은 상단 바·하단 탭에만 현재 규칙을 둔다) · SYSTEM.md §2-2↔§6-9 정정 · UX-01 재마킹(이미 Complete — `requirements.mark-complete`를 다시 부르지 않는다).
</scope>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 페이지 층 이관 — body 아홉 선언 · 컨트롤 서체 · §4-4 표면 · 전역 포커스 링 + FormAlert(§6-7 A②) + KvList(§6-8 B①)</name>
  <files>app/globals.css, ui/shell/TopBar.module.css, ui/form-alert/FormAlert.tsx, ui/form-alert/FormAlert.module.css, app/(auth)/login/login-form.tsx, app/(app)/account/change-password-form.tsx, ui/kv-list/KvList.tsx, ui/kv-list/KvList.module.css, app/(app)/admin/system-status/page.tsx, test/e2e/page-chrome.spec.ts, test/e2e/mobile-page-chrome.spec.ts</files>
  <precondition>Postgres가 127.0.0.1:5432에서 응답한다(`pg_isready -h 127.0.0.1 -p 5432`; 아니면 `bash scripts/dev-db.sh`). Playwright Chromium은 `playwright.config.ts`가 클라우드 경로를 스스로 찾는다(`CLAUDE_CODE_REMOTE=true` 또는 `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH`).</precondition>
  <read_first>
    - docs/design/system/form-expense.html L16-L21 — 이관 원본: `*{}`(box-sizing은 이미 있음) · `html{font-size}` · `body{…아홉 선언}` · `button,input,select,textarea{font:inherit;color:inherit}` · `::selection{}` · `:root{caret/accent/scrollbar}` · `:focus-visible{}`
    - docs/design/SYSTEM.md §2-3(L149-155) 본문 행간·자간·keep-all/anywhere · §4-4(L221-231) 표면 7행 · §6-7(L526-556) A② 실패 문구 `--danger` · §6-8(L558-579) B① dl 규칙 · §7-8 L7 「시트 상세 = 라벨·값 목록(dl)」 같은 골격
    - docs/design/system/sheet-modal.html L102-L106 — `.kv` 실물(grid `var(--label-w) 1fr` · dt 7px 0 · dd 6px 0 · 점선 `var(--line)`)
    - docs/design/tokens.css — `--sel-bg`·`--sel-fg`·`--caret`·`--native-accent`·`--scrollbar`·`--focus`·`--focus-w`·`--focus-offset`·`--lh-body`·`--ls-body`·`--label-w`·`--fw-medium`(=600)·`--line-w`·`--line`·`--danger`가 전부 이미 있다
    - stylelint.config.mjs — font-family 값은 정확히 `var(--font-sans)`, font-size 값은 `var(--fs-…)`만 허용되고 `font` 축약은 금지다 → 실물의 `font:inherit`는 그대로 옮길 수 없다(아래 action)
    - app/globals.css(현재 25줄) · ui/shell/TopBar.module.css `.userTrigger:focus-visible`(상단 바 위 포커스 링을 `--bar-fg`로 두는 선례) · ui/list-empty/ListEmpty.module.css `.tertiary`(포커스 스타일이 없는 요소 — 전역 링의 검증 대상)
    - app/(auth)/login/login-form.tsx L66 · app/(app)/account/change-password-form.tsx L51 — role=alert p 요소 두 곳(FormAlert 소비자)
    - app/(app)/admin/system-status/page.tsx L14-L18 — D-17 접근 제어 세 줄은 손대지 않는다(02-06 read_first와 같은 제약); L31-L59가 교체 대상 dl 블록
    - test/e2e/a11y.spec.ts L22-L30 `loginAs` 헬퍼 모양 · test/e2e/fixtures.ts — 새 스펙은 같은 모양의 헬퍼를 **자기 파일 안에** 둔다(스펙끼리 import하지 않는다)
    - test/e2e/system-status.spec.ts L31-L37 — 보존해야 할 텍스트: `배포 버전`·`DB 커넥션`·`마지막 백업`·`확인 불가`
  </read_first>
  <behavior>
    새 파일 `test/e2e/page-chrome.spec.ts`(데스크톱 프로젝트 — 파일명이 `mobile-`로 시작하지 않으므로 자동으로 desktop만)와 `test/e2e/mobile-page-chrome.spec.ts`(mobile-375 프로젝트 — `mobile-*.spec.ts` 패턴)를 먼저 쓴다. 행간·자간은 `parseFloat` 뒤 `toBeCloseTo(값, 1)`로 비교한다(브라우저 부동소수 표기 차이 방지). 이 태스크 단계에서 두 파일이 담는 테스트:
    - [desktop · 인증 없음 · /login] body 계산값: color `rgb(11, 21, 18)` · backgroundColor `rgb(255, 255, 255)` · fontSize `14px` · lineHeight ≈22.4 · letterSpacing ≈-0.21 · wordBreak `keep-all` · overflowWrap `anywhere` · margin `0px` · tabSize `4`
    - [desktop · /login] §4-4: `document.documentElement` accentColor `rgb(0, 84, 70)` · scrollbarWidth `thin` · scrollbarColor가 `rgb(124, 138, 134)`를 포함; `getByLabel("이메일")`의 caretColor `rgb(0, 84, 70)`; `getComputedStyle(document.body, "::selection")` backgroundColor `rgb(220, 232, 228)` · color `rgb(11, 21, 18)`
    - [desktop · /login] 컨트롤 서체: `getByLabel("이메일")`·`getByRole("button",{name:"로그인"})`의 fontFamily가 `"Pretendard Variable"`로 시작
    - [desktop · /login] 실패 문구: 픽스처 계정 + 틀린 비밀번호로 제출 → `page.locator("form [role='alert']")`(전역 `getByRole("alert")` 금지 — 어나운서와 2개가 잡힌다) color `rgb(155, 28, 28)`. 텍스트는 단언하지 않는다(현재 영문 카피)
    - [desktop · 직원 로그인 · /projects] 전역 포커스 링: `getByRole("link",{name:"지출결의 보기"})`에 `.focus()` → outlineStyle `solid` · outlineWidth `2px` · outlineColor `rgb(0, 84, 70)` · outlineOffset `2px`
    - [desktop · 관리자 로그인 · /admin/system-status] 첫 `main dt`: fontSize `12px` · fontWeight `600` · color `rgb(78, 93, 89)` · `boundingBox().width`가 96±1 · borderBottomStyle `dotted` · borderBottomColor `rgb(207, 219, 215)`; 첫 `main dd` marginLeft `0px`; `getByText("배포 버전")`·`getByText("확인 불가")`는 그대로 보인다
    - [mobile-375 · /login] body fontSize `15px` · lineHeight ≈24
    - [mobile-375 · 관리자 · /admin/system-status] 첫 `main dt` `boundingBox().width`가 84±1
    먼저 두 스펙을 실행해 **위 단언이 전부 실패함을 확인한다**(objective의 실측값이 나온다). 그 다음 구현하고 다시 실행해 전부 통과시킨다.
  </behavior>
  <action>
    **(1) `app/globals.css` — 페이지 층.** 기존 box-sizing 블록과 `.sr-only`는 그대로 둔다. body 규칙을 실물의 아홉 선언으로 확장한다: `margin: 0`(실물의 `*{margin:0}` 리셋이 주던 것 — 컴포넌트 CSS는 자기 여백을 이미 명시적으로 0으로 두고 있어 body 수준만 빠져 있었다) · `background: var(--bg)` · `color: var(--fg)` · `font-family: var(--font-sans)`(기존) · `font-size: var(--fs-base)` · `line-height: var(--lh-body)` · `letter-spacing: var(--ls-body)` · `word-break: keep-all` · `overflow-wrap: anywhere` · `-webkit-font-smoothing: antialiased` · `tab-size: 4`(§4-4 마지막 행). `html { font-size: var(--fs-base) }`(실물 L17; 앱에 rem 사용은 없어 픽셀 영향 0, 실물과 같은 뿌리). 컨트롤 규칙 `button, input, select, textarea`: 실물의 `font:inherit`는 D-20 가드가 `font` 축약과 `inherit` 값(font-family/font-size 허용 목록 밖)을 거부하므로 **의도를 롱핸드로** 옮긴다 — `font-family: var(--font-sans)` · `font-size: var(--fs-base)` · `line-height: inherit` · `letter-spacing: inherit` · `color: inherit`(D-27: 실물은 참고, 규칙은 SYSTEM.md §2-1 Pretendard 전면 적용). 컴포넌트 클래스(`.btn` 12px · `.input` 14px · `.tab` 12px)가 요소 선택자보다 우선하므로 기존 크기는 바뀌지 않고, UA 컨트롤 서체(Arial)만 사라진다. §4-4 표면: `::selection { background: var(--sel-bg); color: var(--sel-fg); }` · `:root { caret-color: var(--caret); accent-color: var(--native-accent); scrollbar-color: var(--scrollbar) var(--bg); scrollbar-width: thin; }` · `:focus-visible { outline: var(--focus-w) solid var(--focus); outline-offset: var(--focus-offset); }`. 모든 색·서체 값은 `var(--…)`다(D-20) — `pnpm lint`의 stylelint가 판정한다. 주석에는 이관 원본(form-expense.html:16-21)과 `font` 축약을 롱핸드로 푼 이유를 한 줄씩 남긴다.

    **(2) `ui/shell/TopBar.module.css`.** 전역 포커스 링(`--focus` = g-700)은 딥그린 상단 바(`--bar` = g-900) 위에서 보이지 않는다. `.userTrigger:focus-visible`이 이미 `--bar-fg` 링을 쓰는 선례 그대로 `.navLink:focus-visible { outline: var(--focus-w) solid var(--bar-fg); outline-offset: var(--focus-offset); }`를 추가한다. 다른 선언은 건드리지 않는다.

    **(3) `ui/form-alert/FormAlert.tsx` + `.module.css`.** §6-7 A②(로그인 실패, `--danger`)와 §6-3/§6-8 ERROR 행(「한 줄 `--danger`」)이 공유하는 폼 수준 실패 문구. props `{ children: ReactNode }`. 렌더는 role이 alert인 p 요소 하나에 `styles.alert`를 붙인 것 — 요소·role·텍스트 노드 구조를 바꾸지 않아 `change-password.spec.ts`의 `getByRole("alert")`·`getByText("현재 비밀번호가 올바르지 않습니다.")`가 그대로 성립한다. CSS: `margin: var(--s-2) 0 var(--s-4)` · `color: var(--danger)`. 크기는 지정하지 않는다(§6-7이 크기를 정하지 않았다 — body `--fs-base` 상속; 정하지 않은 것을 정하면 발명이다). `ui`는 `ui`·`lib`만 import한다(D-26) — 이 파일은 react 타입 외 import가 없다.
    소비자 둘로 치환: `app/(auth)/login/login-form.tsx`의 `error` 렌더 줄과 `app/(app)/account/change-password-form.tsx`의 `result.serverError` 렌더 줄. 조건식·문자열·상태 로직은 한 글자도 바꾸지 않는다.

    **(4) `ui/kv-list/KvList.tsx` + `.module.css`.** §6-8 B① · §7-8 「시트 상세 = 라벨·값 목록(dl)」 골격. props `{ items: Array<{ label: string; value: ReactNode }> }`. 렌더: `dl.kv` 안에 항목마다 `dt`(label)·`dd`(value) 쌍을 `Fragment`(key=label)로 낸다 — dt/dd가 dl의 직계라 axe `definition-list` 규칙에 그대로 맞고, 시스템 상태 스펙의 텍스트 단언(`getByText("배포 버전")` 등)이 dt 텍스트로 성립한다. CSS(sheet-modal.html:102-106 이관): `.kv { display: grid; grid-template-columns: var(--label-w) 1fr; gap: 0 var(--s-3); margin: 0; }` · `.kv dt { margin: 0; padding: 7px 0; font-size: var(--fs-sm); font-weight: var(--fw-medium); color: var(--muted); border-bottom: var(--line-w) dotted var(--line); }` · `.kv dd { margin: 0; padding: 6px 0; min-width: 0; border-bottom: var(--line-w) dotted var(--line); }` · `@media (max-width: 699.98px) { .kv { grid-template-columns: 84px 1fr; } }`(§6-8 「폭 96(PC)/84(폰)」 — 84px는 간격/폭 리터럴이라 D-20 범위 밖이고 tokens.css에 폰용 라벨 폭 토큰이 없으므로 새 토큰 없이 리터럴). `dd`의 `margin: 0`이 UA 들여쓰기 40px를 없앤다.
    `app/(app)/admin/system-status/page.tsx`: L31-L59의 dl 블록을 `KvList`로 바꾼다 — items 세 개의 `value`에 현재 dd 안의 JSX(문자열 조합·`StatusTag`)를 **그대로** 옮긴다. import 추가 외에 L1-L29(캐시 지시자 · `getSession` · 미인증 redirect · `notFound` · 배너 · 제목)는 손대지 않는다(D-17·D-29). 제목 h1은 Task 2가 바꾼다.

    **(5) 스펙 두 파일**을 behavior대로 쓴다. 로그인 헬퍼(페이지 + 관리자 여부를 받는 a11y.spec.ts L22-L30과 같은 7줄)는 파일 안에 둔다. 실패 문구 테스트는 직원 픽스처 계정(`createFixtureUser`, 관리자 아님)의 이메일 + `"definitely-wrong-password"`로 제출한다(가짜 데이터 없음 — 실제 픽스처 계정, D-24 정신). 오늘 이 파일들에는 위 behavior 항목만 들어가고 Task 2·3이 describe 블록을 덧붙인다.

    순서: 스펙 작성 → `pnpm exec playwright test test/e2e/page-chrome.spec.ts test/e2e/mobile-page-chrome.spec.ts --reporter=line`으로 빨강 확인(실패 목록이 objective의 실측과 일치해야 한다) → (1)~(4) 구현 → 같은 명령으로 초록 → `pnpm lint && pnpm typecheck` → Phase-1 스펙 3개 + 02-07 스펙 3개 회귀(아래 verify) → 커밋 `feat(02-08): 페이지 층 토큰 이관 + FormAlert·KvList`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm exec playwright test test/e2e/page-chrome.spec.ts test/e2e/mobile-page-chrome.spec.ts test/e2e/login-logout.spec.ts test/e2e/change-password.spec.ts test/e2e/system-status.spec.ts test/e2e/a11y.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'word-break: keep-all' app/globals.css` ≥ 1 이고 `grep -c 'overflow-wrap: anywhere' app/globals.css` ≥ 1
    - `grep -Ec '^\s*(background: var\(--bg\)|color: var\(--fg\)|font-size: var\(--fs-base\)|line-height: var\(--lh-body\)|letter-spacing: var\(--ls-body\)|margin: 0);' app/globals.css` ≥ 6
    - `grep -c '::selection' app/globals.css` ≥ 1 · `grep -c 'caret-color: var(--caret)' app/globals.css` ≥ 1 · `grep -c 'accent-color: var(--native-accent)' app/globals.css` ≥ 1 · `grep -c 'scrollbar-color: var(--scrollbar) var(--bg)' app/globals.css` ≥ 1 · `grep -c 'scrollbar-width: thin' app/globals.css` ≥ 1 · `grep -c ':focus-visible' app/globals.css` ≥ 1
    - `grep -c 'button, input, select, textarea' app/globals.css` ≥ 1 이고 `grep -c 'box-sizing: border-box' app/globals.css` ≥ 1 이고 `grep -c '\.sr-only' app/globals.css` ≥ 1(기존 블록 보존)
    - `grep -c 'navLink:focus-visible' ui/shell/TopBar.module.css` ≥ 1
    - `test -f ui/form-alert/FormAlert.tsx && grep -c 'role="alert"' ui/form-alert/FormAlert.tsx` ≥ 1 · `grep -c 'color: var(--danger)' ui/form-alert/FormAlert.module.css` ≥ 1
    - `grep -c 'FormAlert' 'app/(auth)/login/login-form.tsx'` ≥ 2 이고 `grep -c 'FormAlert' 'app/(app)/account/change-password-form.tsx'` ≥ 2
    - `grep -c '<p role="alert">' 'app/(auth)/login/login-form.tsx'` == 0
    - `grep -c '<p role="alert">' 'app/(app)/account/change-password-form.tsx'` == 0
    - `test -f ui/kv-list/KvList.tsx && grep -c 'grid-template-columns: var(--label-w) 1fr' ui/kv-list/KvList.module.css` ≥ 1 · `grep -c '84px 1fr' ui/kv-list/KvList.module.css` ≥ 1 · `grep -c 'dotted var(--line)' ui/kv-list/KvList.module.css` ≥ 2
    - `grep -c 'KvList' 'app/(app)/admin/system-status/page.tsx'` ≥ 2 이고 `grep -c 'notFound()' 'app/(app)/admin/system-status/page.tsx'` ≥ 1(D-17 줄 보존)
    - `grep -c '<dl>' 'app/(app)/admin/system-status/page.tsx'` == 0
    - `git diff --quiet 25aaa4f91df30f84262b67cb54b5bb38b14ea9d1 -- docs/design/tokens.css docs/design/SYSTEM.md docs/design/DECISIONS.md test/e2e/login-logout.spec.ts test/e2e/change-password.spec.ts test/e2e/system-status.spec.ts test/e2e/a11y.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts` 가 0으로 끝난다
    - `git diff --quiet 25aaa4f91df30f84262b67cb54b5bb38b14ea9d1 -- package.json pnpm-lock.yaml` 가 0으로 끝난다(D-19)
    - verify 명령이 전부 통과하고, 새 스펙의 실패 목록이 구현 **전** 실행에서 비어 있지 않았다(SUMMARY에 빨강 실행의 실패 개수를 적는다)
  </acceptance_criteria>
  <done>로그인 화면을 열면 본문이 `--fg` 14px 1.6 keep-all로 렌더되고 컨트롤이 Pretendard이며 선택·캐럿·스크롤바·포커스 링이 토큰 색이다. 로그인 실패 문구가 `--danger`, 시스템 상태 목록이 라벨 96/84 + 점선 골격이다. 기존 30개 E2E + 새 스펙이 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 화면 제목·부제 컴포넌트(PageHeader) — 12개 화면 적용</name>
  <files>ui/page-header/PageHeader.tsx, ui/page-header/PageHeader.module.css, app/(app)/page.tsx, app/(app)/projects/page.tsx, app/(app)/expenses/page.tsx, app/(app)/cards/page.tsx, app/(app)/approvals/page.tsx, app/(app)/pnl/page.tsx, app/(app)/settings/page.tsx, app/(app)/account/page.tsx, app/(app)/admin/system-status/page.tsx, app/(app)/not-found.tsx, app/(app)/error.tsx, app/not-found.tsx, test/e2e/page-chrome.spec.ts, test/e2e/mobile-page-chrome.spec.ts</files>
  <precondition>Task 1이 커밋되어 있다(body 14px가 적용되지 않으면 아래 h1 단언의 「변경 전 32px」 전제가 달라진다). Postgres 127.0.0.1:5432 응답.</precondition>
  <read_first>
    - docs/design/SYSTEM.md §6-0 L290 「브레드크럼 없음. 화면 제목(`--fs-lg`) + 부제(`--fs-sm --muted`)가 위치를 말한다」 · §2-2 표(`--fs-lg` 18/1.4/700, `--fs-2xl` 32/1.3/700) · §2-3 「제목(`--fs-lg` 이상): letter-spacing -0.02em」 · §6-9 L581-L603 도해 「← --fs-2xl」
    - docs/design/system/preview.html L48-L50 · form-expense.html L38-L41 — `.sec` / `.sec h1` / `.sec .sub` 실물(우측 `.right`는 이 플랜에서 만들지 않는다)
    - 12개 대상 파일의 현재 h1/p 두 줄(`grep -rn "<h1" app --include=*.tsx`로 목록을 다시 뽑는다 — 로그인 화면의 `className="sr-only"` h1은 §6-7 워드마크 대체이므로 제외)
    - app/(app)/account/page.tsx — h1 다음 p가 이메일·이름 둘. login-logout.spec.ts가 `getByText(user.email)`를 단언하므로 이메일 텍스트는 남아야 한다
    - app/(app)/error.tsx — `"use client"` 파일. PageHeader는 훅·서버 전용 API가 없는 순수 표현 컴포넌트라 클라이언트 번들에서 그대로 import된다
    - test/e2e/keyboard-nav.spec.ts L55 `getByRole("heading", { name: "프로젝트" })` — h1 요소가 유지되어야 한다
    - ui/status-tag/StatusTag.tsx — 이 페이즈의 작은 `ui/` 컴포넌트 파일 모양(타입 export · 클래스 조합)
  </read_first>
  <behavior>
    `test/e2e/page-chrome.spec.ts`에 describe 「§6-0 화면 제목·부제 · §6-9 오류 제목」을 덧붙인다:
    - [직원 로그인 · /projects] `main h1`: fontSize `18px` · fontWeight `700` · letterSpacing ≈-0.36 · lineHeight ≈25.2; `getByText("진행 중인 프로젝트 원장")`: fontSize `12px` · color `rgb(78, 93, 89)`
    - [직원 · /account] `main h1` fontSize `18px`; `getByText(user.email)` fontSize `12px` · color `rgb(78, 93, 89)`(부제 = 이메일)
    - [인증 없음 · /e2e-page-chrome-nonexistent → 루트 404, 셸 밖] `main h1`: letterSpacing ≈-0.64 · lineHeight ≈41.6 · fontSize `32px`(크기는 변경 전에도 32px라 빨강 근거가 아니다 — 자간·행간이 근거)
    - [직원 · /admin/system-status → D-17 notFound → 셸 안 404] 응답 404이고 `main h1` letterSpacing ≈-0.64 · lineHeight ≈41.6
    구현 전 실행: 새 단언 전부 실패(h1 32px/normal/normal · 부제 16px 검정). 구현 후: 전부 통과. 오류 경계(`error.tsx`)는 E2E로 강제 발생시키지 않는다 — 아래 grep 기준이 PageHeader 사용을 고정한다.
  </behavior>
  <action>
    **(1) `ui/page-header/PageHeader.tsx` + `.module.css`.** props `{ title: string; subtitle?: string; titleSize?: "lg" | "2xl" }`(기본 `"lg"`). 렌더: `div.header` 안에 `h1.title`(+ `titleSize==="2xl"`이면 `.title2xl` 추가) 과, subtitle이 있을 때만 `p.subtitle`. `header` 요소는 쓰지 않는다(main 안 header는 banner 랜드마크가 아니지만 axe/스크린리더 해석 여지를 만들 이유가 없다). CSS(실물 `.sec` 이관): `.header { display: flex; align-items: baseline; gap: var(--s-3); flex-wrap: wrap; padding-bottom: var(--s-2); margin: 0 0 var(--s-4); }` · `.title { margin: 0; font-size: var(--fs-lg); font-weight: var(--fw-bold); letter-spacing: var(--ls-head); line-height: var(--lh-head); }` · `.title2xl { font-size: var(--fs-2xl); line-height: var(--lh-tight); }`(§6-9 도해 · §2-2 표의 2xl 행간 1.3 = `--lh-tight`) · `.subtitle { margin: 0; font-size: var(--fs-sm); color: var(--muted); }`. 우측 행동 슬롯(`.sec .right`)은 소비자가 없으므로 만들지 않는다(D-25). 파일 머리 주석에 objective의 결정(요소 선택자 대신 컴포넌트인 이유 세 줄)을 요약해 둔다 — 다음 페이즈가 전역 `h1` 규칙을 추가하려 할 때 읽을 자리다.

    **(2) 12개 화면 치환.** 각 파일에서 맨 h1 요소(+ 바로 뒤 부제 p)를 `<PageHeader title=… subtitle=… />` 한 줄로 바꾸고 import를 추가한다. 매핑: 홈 `내 차례`/`지금 처리할 항목` · 프로젝트 `프로젝트`/`진행 중인 프로젝트 원장` · 지출결의 `지출결의`/`지급요청·결재 진행 현황` · 법인카드 `법인카드`/`카드 사용 등록 내역` · 결재 `결재`/`내가 처리할 결재 문서` · 손익 `손익`/`프로젝트·팀 손익 원장` · 설정 `설정`/`운영 설정` · 내 계정 `내 계정`/subtitle=`user.email`(이름 p는 그대로 본문으로 남긴다) · 시스템 상태 `시스템 상태`(부제 없음; 배너는 그 위 그대로 — §6-8 B③) · 셸 404 `페이지를 찾을 수 없습니다` titleSize 2xl · 루트 404 같은 제목 titleSize 2xl(`main` 래퍼 유지) · 오류 경계 `문제가 생겼습니다` titleSize 2xl. 문구는 현재 파일의 문자열을 **그대로** 옮긴다(한 글자도 바꾸지 않는다 — a11y.spec·keyboard-nav.spec이 제목 텍스트로 요소를 찾는다). `ListEmpty`·`Banner`·`NextTurn`·폼 등 나머지 JSX는 손대지 않는다. 로그인 화면의 sr-only h1은 그대로 둔다.

    **(3) 스펙 덧붙이기**(behavior). 루트 404는 인증 없이 `page.goto("/e2e-page-chrome-nonexistent")`로 연다((app) 그룹 밖 URL은 리다이렉트 없이 `app/not-found.tsx`가 렌더된다). 셸 404는 직원으로 `/admin/system-status`를 열어 얻는다(system-status.spec.ts의 첫 테스트와 같은 경로 — 응답 404 단언 포함).

    순서: 스펙 추가 → 실행해 빨강 확인 → (1)(2) 구현 → 초록 → `pnpm lint && pnpm typecheck` → 회귀(verify) → 커밋 `feat(02-08): PageHeader로 12개 화면 제목·부제 통일`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm exec playwright test test/e2e/page-chrome.spec.ts test/e2e/mobile-page-chrome.spec.ts test/e2e/a11y.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/login-logout.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ui/page-header/PageHeader.tsx && test -f ui/page-header/PageHeader.module.css`
    - `grep -c 'font-size: var(--fs-lg)' ui/page-header/PageHeader.module.css` ≥ 1 · `grep -c 'font-size: var(--fs-2xl)' ui/page-header/PageHeader.module.css` ≥ 1 · `grep -c 'line-height: var(--lh-tight)' ui/page-header/PageHeader.module.css` ≥ 1 · `grep -c 'color: var(--muted)' ui/page-header/PageHeader.module.css` ≥ 1 · `grep -c 'letter-spacing: var(--ls-head)' ui/page-header/PageHeader.module.css` ≥ 1
    - 12개 파일 각각에 대해 `grep -c 'PageHeader' <file>` ≥ 2: `app/(app)/page.tsx` · `projects` · `expenses` · `cards` · `approvals` · `pnl` · `settings` · `account/page.tsx` · `admin/system-status/page.tsx` · `app/(app)/not-found.tsx` · `app/(app)/error.tsx` · `app/not-found.tsx`
    - `grep -rc '<h1>' app --include=*.tsx | awk -F: '{s+=$2} END {print s}'` 가 0을 출력한다(로그인의 sr-only h1은 이 리터럴에 걸리지 않는다)
    - `grep -c 'titleSize="2xl"' 'app/(app)/not-found.tsx' app/not-found.tsx 'app/(app)/error.tsx'` 각 1
    - `grep -c 'subtitle={user.email}' 'app/(app)/account/page.tsx'` ≥ 1 이고 `grep -c 'user.name' 'app/(app)/account/page.tsx'` ≥ 1
    - `grep -c 'margin-left: auto' ui/page-header/PageHeader.module.css` == 0(실물 우측 슬롯의 핵심 선언이 없다 = 슬롯을 만들지 않았다, D-25)
    - `git diff --quiet 25aaa4f91df30f84262b67cb54b5bb38b14ea9d1 -- test/e2e/login-logout.spec.ts test/e2e/change-password.spec.ts test/e2e/system-status.spec.ts test/e2e/a11y.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts docs/design` 가 0으로 끝난다
    - verify 명령이 전부 통과하고, 새 describe의 단언이 구현 전 실행에서 실패했다(SUMMARY에 개수 기록)
  </acceptance_criteria>
  <done>어느 화면을 열어도 제목이 18px 700(오류 페이지 셋은 32px 1.3) + 부제 12px muted로 같은 컴포넌트에서 나오고, app/ 아래에 맨 h1이 없다. 기존 스펙 30개 + 새 스펙이 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-01 — 현재 메뉴 aria-current 배선(상단 바 · 폰 하단 탭) + WINDOWS 항목 5 종결</name>
  <files>ui/shell/current-path.ts, test/unit/ui/current-path.test.ts, ui/shell/TopBar.tsx, ui/shell/BottomTabs.tsx, ui/shell/BottomTabs.module.css, test/e2e/page-chrome.spec.ts, test/e2e/mobile-page-chrome.spec.ts, .planning/WINDOWS.md</files>
  <precondition>Task 2 커밋. Postgres 127.0.0.1:5432 응답.</precondition>
  <read_first>
    - .planning/phases/02-design-system-app-shell/02-REVIEW.md WR-01 전문 — 대상 줄(TopBar.tsx:79-84 · TopBar.module.css:43-47 · BottomTabs.tsx:50-52)과 제안된 `isCurrent` 규칙(`/`는 정확 일치, 나머지는 일치 또는 `href + "/"` 접두)
    - docs/design/SYSTEM.md §6-0 L287 「현재 메뉴는 `--bar-fg` 700 + 아래 2px `--g-400` 밑줄」 · §1-3 포인트 색 사용처 「현재 위치(내비게이션 밑줄)」
    - docs/design/system/preview.html L27 `.bar nav a[aria-current]{…}` · L129 `.tabs a[aria-current]{color:var(--accent);box-shadow:var(--inset-tab)}` · docs/design/tokens.css `--inset-tab`
    - ui/shell/TopBar.tsx(이미 `"use client"`, `useRouter` import 있음) · ui/shell/BottomTabs.tsx(이미 `"use client"`) · ui/shell/TopBar.module.css L43-L47(선택자가 이미 있다 — CSS는 손대지 않는다) · ui/shell/BottomTabs.module.css(`.tab` 규칙 뒤에 현재 규칙 추가)
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-pathname.md — Next 16.3의 `usePathname`은 클라이언트 컴포넌트 훅이고 SSR에서 초기 HTML에 값이 들어간다. 하이드레이션 불일치 경고는 「정적 프리렌더 + rewrites/Proxy」 조합에만 해당: 이 리포의 `proxy.ts`는 `/api/auth/:path*`만 매치하고 경로를 바꾸지 않으며, `(app)` 라우트는 전부 레이아웃에서 세션을 읽어 동적이다 → 해당 없음
    - .planning/phases/02-design-system-app-shell/02-04-SUMMARY.md L201-L212 — 당시 배선을 미룬 이유(레이아웃이 pathname을 못 얻음). 이 태스크의 메커니즘은 레이아웃이 아니라 이미 클라이언트인 두 컴포넌트 안의 `usePathname()`이라 그 제약과 무관하다
    - .planning/WINDOWS.md 항목 5 · `node .claude/gsd-core/bin/gsd-tools.cjs windows` 사용법(하위 명령 status/append/waive/fixed)
    - test/unit/ui/role-menu.test.ts — 단위 테스트 파일 모양(vitest, 상대 import)
  </read_first>
  <behavior>
    단위(`test/unit/ui/current-path.test.ts`, `isCurrentPath(pathname, href)`): `("/", "/")` → true · `("/projects", "/")` → false · `("/projects", "/projects")` → true · `("/projects/123", "/projects")` → true · `("/projectsx", "/projects")` → false · `(null, "/")` → false · `("/account", "/projects")` → false. 구현 전 실행: 모듈이 없어 실패.
    E2E(`page-chrome.spec.ts` describe 「§6-0 현재 메뉴(WR-01)」, 직원 로그인): `/projects`에서 `nav[aria-label="주 메뉴"] a[aria-current="page"]` count 1 · 텍스트 `프로젝트` · 계산값 fontWeight `700` · color `rgb(220, 232, 228)` · boxShadow가 `inset`을 포함; `/account`에서 count 0.
    E2E(`mobile-page-chrome.spec.ts`, 직원 로그인): `/`에서 `nav[aria-label="하단 탭"] [aria-current="page"]` count 1 · 텍스트 `내 차례` · color `rgb(0, 84, 70)` · boxShadow가 `inset`을 포함; `/projects`에서 현재 탭 텍스트 `프로젝트`; `/account`에서 count 0. 구현 전 실행: count가 0이라 전부 실패(실측: 0개, 첫 탭 `rgb(78, 93, 89)`, box-shadow `none`).
  </behavior>
  <action>
    **(1) `ui/shell/current-path.ts`.** `export function isCurrentPath(pathname: string | null, href: string): boolean` — `pathname`이 null이면 false; `href === "/"`면 `pathname === "/"`; 아니면 `pathname === href || pathname.startsWith(href + "/")`. 순수 함수, import 없음. 역할 데이터(`role-menu.ts`)와 섞지 않는다 — 그 파일은 D-23의 「역할 → 메뉴 매핑 한 곳」이고 이것은 경로 판정이다. 단위 테스트를 behavior대로 쓴다.

    **(2) `ui/shell/TopBar.tsx`.** `usePathname`을 `next/navigation`에서 추가 import하고(같은 모듈에서 `useRouter`를 이미 가져온다), 컴포넌트 안에서 `const pathname = usePathname();`. 주 메뉴 `topBarMenu.map` 의 `a`에 `aria-current={isCurrentPath(pathname, item.href) ? "page" : undefined}`. CSS는 이미 `.navLink[aria-current="page"]`가 있으므로 손대지 않는다. 사용자 메뉴(`role="menu"`)의 항목에는 붙이지 않는다(§6-0 (a)는 팝업이고 「현재 메뉴」 규칙의 대상이 아니다). 역할 분기를 두지 않는다(D-23). 파일 머리 주석의 02-04 메모(pathname 소스 부재)를 이 메커니즘으로 갱신한다.

    **(3) `ui/shell/BottomTabs.tsx` + `.module.css`.** 같은 방식으로 `usePathname()`을 읽고 `tab.kind === "link"` 분기의 `a`에 `aria-current`를 붙인다. 「더보기」 버튼에는 붙이지 않는다(라우트가 아니다). CSS에 `.tab[aria-current="page"] { color: var(--accent); box-shadow: var(--inset-tab); }`(preview.html:129 그대로 — `--inset-tab`이 tokens.css의 「현재 탭 밑줄」 토큰). `.tab:focus-visible` 등 기존 규칙은 유지.

    **(4) 스펙 덧붙이기**(behavior). 상단 바 현재 링크의 계산값 단언은 `TopBar.module.css:43-47`이 죽은 CSS가 아니라 실제로 렌더됨을 증명하는 부분이다 — `count`만 세지 않는다.

    **(5) WINDOWS 항목 5 종결.** 초록 확인 뒤 `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 5`(또는 `gsd_run windows fixed 5`)를 실행한다 — `.planning/`을 손으로 편집하지 않는다(CLAUDE.md 금지 항목). 결과 파일의 항목 5 status가 `fixed`인지 grep으로 확인한다.

    순서: 단위 테스트 + E2E describe 추가 → 둘 다 빨강 확인 → (1)(2)(3) 구현 → `pnpm test:unit` 초록(285개 = 기존 284 + 이 파일) → E2E 초록 → `pnpm lint && pnpm typecheck` → (5) → 전체 회귀 `pnpm test:e2e` → 커밋 `fix(02-08): WR-01 현재 메뉴 aria-current 배선 (상단 바·하단 탭)`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ui/shell/current-path.ts && grep -c 'export function isCurrentPath' ui/shell/current-path.ts` ≥ 1
    - `grep -c '^import' ui/shell/current-path.ts` == 0
    - `pnpm exec vitest run --project unit test/unit/ui/current-path.test.ts` 통과(테스트 ≥ 6개)
    - `grep -c 'usePathname' ui/shell/TopBar.tsx` ≥ 2 이고 `grep -c 'isCurrentPath' ui/shell/TopBar.tsx` ≥ 2 이고 `grep -c 'aria-current' ui/shell/TopBar.tsx` ≥ 1
    - `grep -c 'usePathname' ui/shell/BottomTabs.tsx` ≥ 2 이고 `grep -c 'aria-current' ui/shell/BottomTabs.tsx` ≥ 1
    - `grep -c 'aria-current="page"' ui/shell/BottomTabs.module.css` ≥ 1 이고 `grep -c 'var(--inset-tab)' ui/shell/BottomTabs.module.css` ≥ 1
    - `grep -c 'isAdmin' ui/shell/TopBar.tsx` == 0
    - `grep -c 'isAdmin' ui/shell/BottomTabs.tsx` == 0
    - `grep -c 'isAdmin' ui/shell/current-path.ts` == 0
    - `grep -c 'navLink\[aria-current="page"\]' ui/shell/TopBar.module.css` ≥ 1(02-04가 준비한 선택자 유지 — Task 1이 같은 파일에 focus 규칙을 더했으므로 파일 자체는 25aaa4f와 다르다)
    - `grep -Ec '^\| 5 \| 02 \| deviation \| ui/shell/TopBar.tsx \|.*\| fixed \|' .planning/WINDOWS.md` ≥ 1
    - `git diff --quiet 25aaa4f91df30f84262b67cb54b5bb38b14ea9d1 --diff-filter=MD -- test/` 가 0으로 끝난다(기존 테스트 파일은 수정·삭제 없음 — 추가만)
    - `git diff --quiet 25aaa4f91df30f84262b67cb54b5bb38b14ea9d1 -- docs/design package.json pnpm-lock.yaml` 가 0으로 끝난다
    - `pnpm test:e2e`가 통과하고 총 개수가 30 + 새 스펙 개수(desktop ≥ 10 · mobile ≥ 5)다
  </acceptance_criteria>
  <done>PC에서 어느 1차 메뉴 화면을 열어도 그 메뉴가 `--bar-fg` 700 + 잎색 밑줄로 표시되고, 폰에서는 해당 하단 탭이 `--accent` + 인셋 밑줄이다. `/account`처럼 1차 메뉴 밖 화면에서는 아무것도 현재가 아니다. WINDOWS 항목 5가 fixed다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 URL → 클라이언트 컴포넌트 | `usePathname()`이 돌려주는 경로 문자열이 셸 컴포넌트의 속성 계산에 들어온다 |
| 전역 CSS → 모든 라우트 | `globals.css`의 요소 선택자가 셸 안팎 전 화면에 적용된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Information Disclosure / Tampering | `ui/shell/TopBar.tsx`·`BottomTabs.tsx` `usePathname` | low | accept | 경로 문자열은 `role-menu.ts`의 정적 href 목록과 **동등 비교만** 한다 — 텍스트·HTML·URL로 렌더되지 않고 CSS 변수에도 들어가지 않는다. `isCurrentPath`는 import 0의 순수 함수로 단위 테스트가 경계 사례(접두 오탐 `/projectsx`)를 고정한다 |
| T-02-08-02 | Tampering | `app/globals.css` 전역 선택자(`:root`·`::selection`·`:focus-visible`) | low | mitigate | 정적 CSS, 동적 값 없음. D-20 stylelint가 색·서체·radius 리터럴을 거부해 토큰 밖 값이 들어올 수 없다(`pnpm lint`가 각 태스크 verify에 포함) |
| T-02-08-03 | Denial of Service | E2E 픽스처(`createFixtureUser`, 실패 로그인 1회) | low | accept | 기존 02-07 패턴 재사용. 로그인 속도 제한 상향은 Playwright 프로세스에만 있고(`RATE_LIMIT_LOGIN_MAX`, 02-07) 이 플랜은 그 설정을 건드리지 않는다 |
| T-02-08-04 | Elevation of Privilege | `app/(app)/admin/system-status/page.tsx` 재구성 | medium | mitigate | D-17 접근 제어 세 줄(`getSession`·redirect·`notFound`)은 read_first에서 불변으로 못박고 acceptance가 `notFound()` 존재를 grep한다; system-status.spec.ts의 「직원 404」 테스트가 각 태스크 verify에서 돈다 |
| T-02-08-SC | Tampering | npm 설치 | low | accept | 이 플랜은 패키지를 설치하지 않는다(D-19; `package.json`·`pnpm-lock.yaml` 불변을 acceptance가 확인). 패키지 정당성 게이트 해당 없음 |
</threat_model>

<verification>
플랜 종료 시(Task 3 verify가 이미 포함):
- `pnpm lint && pnpm typecheck && pnpm lint:sql && pnpm test:unit` — CI quality 잡과 동일
- `pnpm test:e2e` — 기존 30개 + 새 스펙 전부 통과, 기존 스펙 6개는 커밋 25aaa4f와 동일
- `pnpm build` — 프로덕션 빌드 성공(`usePathname` 추가가 정적/동적 판정을 바꾸지 않았는지 — (app) 라우트는 원래 동적)
- `git diff --quiet 25aaa4f91df30f84262b67cb54b5bb38b14ea9d1 -- docs/design package.json pnpm-lock.yaml` — 새 토큰·문서 이탈·의존성 0
- 검증자가 성공 기준 2를 다시 볼 때 확인할 것: `app/globals.css`의 body가 실물 아홉 선언을 갖고, `app/**/*.tsx`에 맨 h1이 없으며, 렌더된 계산값이 두 스펙으로 고정되어 있다
</verification>

<success_criteria>
- ROADMAP Phase 2 성공 기준 2가 페이지 층까지 참이다: 본문·제목·부제·컨트롤·알림·목록·브라우저 표면이 전부 tokens.css 토큰으로 렌더되고 UA 기본값이 남은 곳이 없다(정확한 계산값은 must_haves.truths)
- WR-01이 닫혔다 — 현재 메뉴 표시가 PC·폰에서 실제로 렌더되고 E2E가 그것을 계산값으로 고정한다; WINDOWS 항목 5 fixed
- 기존 테스트 314개(284 단위 + 30 E2E)가 한 글자도 바뀌지 않은 채 통과하고, 새 테스트(단위 ≥ 6 · E2E ≥ 15)가 구현 전에는 실패했음이 SUMMARY에 기록되어 있다
- 새 토큰 0 · 새 의존성 0 · SYSTEM.md/DECISIONS.md 변경 0
- SUMMARY에 사람 판단 항목 2개를 올린다: (a) SYSTEM.md §2-2(`--fs-2xl` KPI 전용)와 §6-9(오류 제목 `--fs-2xl`)의 긴장 — 정정 시 DECISIONS.md 기록이 필요하고 그 전에 `design-system-docs.test.ts`의 `toBe(6)`을 `toBeGreaterThanOrEqual`로 완화해야 한다(WR-09 제안); (b) 로그인 실패 문구 카피가 §6-7의 한국어 문장이 아니라 better-auth 영문이다(`login-form.tsx`가 `result.error.message`를 우선한다)
</success_criteria>

<output>
Create `.planning/phases/02-design-system-app-shell/02-08-SUMMARY.md` when done. Do not call `requirements.mark-complete` — UX-01 is already Complete.
</output>
