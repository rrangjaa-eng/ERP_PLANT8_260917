# Phase 2: 디자인 시스템·앱 셸 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-19
**Phase:** 2-디자인 시스템·앱 셸
**Areas discussed:** 스타일링 기계장치, 셸의 메뉴·「내 차례」, 실물 → 컴포넌트 이관 범위, Phase 1 임시 화면 제거 + 루트 화면, 알림함·배지 계약

**논의 형태에 관한 기록:** 사용자가 회색지대 선택 두 번 모두에 "권장사항을 말해"로 답해, Claude가 8개 후보 중 5개를 골라 논의하고 3개(Pretendard 커밋·기준 3 증명 방법·회사 도메인)를 건너뛴 근거를 제시했다. 이후 모든 질문에서 사용자는 권장안을 택했다.

---

## (사전) 어느 회색지대를 논의할 것인가

| 후보 | 판단 |
|---|---|
| 스타일링 기계장치 | ✓ 논의 — 한방향 문, 이후 9개 페이즈가 따른다 |
| 셸의 메뉴·「내 차례」 | ✓ 논의 — 계급은 Phase 3, UX-02는 Phase 9, 데이터는 Phase 4+라 셸의 정의가 비어 있다 |
| 실물 → 컴포넌트 이관 범위 | ✓ 논의 — 페이즈 크기를 가른다 |
| Phase 1 임시 화면 제거 + 루트 화면 | ✓ 논의 — 성공 기준 2가 직접 요구 |
| 알림함·배지 계약 빈 곳 | ✓ 논의 — 성공 기준 4가 이름으로 요구하는데 SYSTEM.md에 없다 |
| Pretendard 커밋·검수 | ✗ 건너뜀 — 이미 사용자 결정이고 NEXT-SESSION.md에 명령까지 있다. 논의가 아니라 플랜 하나 |
| 기준 3 증명 방법 | ✗ 건너뜀 — Phase 1이 Playwright·CI를 세웠고 SYSTEM.md §11 검증 목록이 있다. planner 재량 |
| 회사 도메인 연결 | ✗ 건너뜀 — 직원 공개는 Phase 3(계정·권한) 뒤다. 이월로 다시 적는다 |

**사용자 응답:** "권장사항을 말해" / "이것도 권장사항 알려줘" — 선택을 Claude에 위임했고, 위 5/3 분할에 이견 없이 논의를 진행했다.

---

## 스타일링 기계장치

### 질문 1 — 앱이 tokens.css를 먹는 방식

| Option | Description | Selected |
|--------|-------------|----------|
| 전역 CSS + CSS Modules (권장) | Next.js 기본 지원, 새 의존성 0. 실물 HTML 10개가 이미 순수 CSS + var(--…)라 이관이 가장 곧다. 표·인쇄의 복잡한 선택자도 제약 없이 쓴다 | ✓ |
| 전역 CSS 하나만 | globals.css에 모든 컴포넌트 클래스. 실물과 1:1이지만 11개 페이즈 뒤 수천 줄이 되고 이름 충돌을 사람이 막아야 한다 | |
| Tailwind v4 @theme | tokens.css를 @theme으로 넘겨 유틸리티로. 실물 10개를 전부 다시 써야 하고 임의 값으로 토큰을 우회하기 쉽다 | |
| 정해줘 (당신이 판단) | researcher·planner가 Next.js 16 문서를 보고 결정 | |

**User's choice:** 전역 CSS + CSS Modules
**Notes:** → D-19. Reversibility costly — 나중에 바꾸면 이 페이즈의 모든 컴포넌트와 이후 화면을 다시 쓴다.

### 질문 2 — 토큰 강제 방법

| Option | Description | Selected |
|--------|-------------|----------|
| stylelint 규칙 + CI (권장) | 색·서체·radius·간격 리터럴 금지, var(--…)만 허용. Phase 1의 ESLint 커스텀 규칙 3개 + 규칙 테스트 전례를 따른다. 개발 의존성 1개 | ✓ |
| 테스트로만 막는다 | 의존성 0. Vitest가 CSS 파일을 읽어 금지 패턴을 찾는다. 거칠고 에디터에서 안 보인다 | |
| /design-review와 리뷰로만 | 자동 강제 없음. 11개 페이즈 동안 드리프트를 막을 장치가 사람뿐 | |

**User's choice:** stylelint 규칙 + CI
**Notes:** → D-20. 새 개발 의존성 stylelint 하나를 CLAUDE.md "새 의존성은 이유 한 줄 + 승인" 절차로 승인한 것으로 기록.

### 질문 3 — tokens.css 단일 출처

| Option | Description | Selected |
|--------|-------------|----------|
| 앱이 docs/를 직접 import (권장) | 복사본 0. 틀어질 수 없는 구조. Next.js 16 CSS 처리·ESLint boundaries와 부딪히는지는 researcher가 확인 | ✓ |
| app/으로 복사 + 바이트 동일 테스트 | 복사본을 두되 CI가 원본과 바이트 비교. 번들러·린트 제약을 안 받는다 | |
| 앱으로 완전히 옮긴다 | docs/design/은 문서만. 실물 10개가 ../tokens.css를 참조해 전부 고쳐야 하고 CLAUDE.md 문장도 바뀐다 | |

**User's choice:** 앱이 docs/를 직접 import
**Notes:** → D-21. 직접 import가 실무적으로 불가능하면 차선은 2번(복사 + 바이트 동일 테스트)이라고 CONTEXT.md에 명시했다. 3번은 거부.

---

## 셸의 메뉴·「내 차례」

### 질문 1 — 상단 바 메뉴 5개가 가리킬 곳

| Option | Description | Selected |
|--------|-------------|----------|
| 5개 전부 + 빈 라우트 EMPTY (권장) | §7-7 EMPTY("무엇이 없다 · 다음 한 수")를 렌더. 셸이 진짜 셸임을 즉시 증명하고 Phase 4+는 EMPTY 자리에 표를 꽂는다 | ✓ |
| 구현된 것만 내보낸다 | Phase 2에는 메뉴가 거의 없다. §6-0 "빈 자리를 두지 않는다"와는 맞지만 셸의 모양이 페이즈마다 바뀐다 | |
| 5개 전부 + 비활성 + 이유 | UX-06의 "선행 단계 전 비활성 + 이유"와 같은 결이지만 그 패턴은 업무 흐름용이지 내비게이션용이 아니다 | |

**User's choice:** 5개 전부 + 빈 라우트 EMPTY
**Notes:** → D-22. 다섯 상태 계약이 진짜 화면으로 검증된다는 점이 결정적.

### 질문 2 — 역할별 구성 처리

| Option | Description | Selected |
|--------|-------------|----------|
| 구성을 데이터로 빼낸다 (권장) | 셸은 "역할 → 메뉴 목록·탭 4개" 매핑을 받아 렌더링만. Phase 2는 관리자/직원 둘만 채우고 Phase 3은 그 매핑 한 곳만 교체 | ✓ |
| 모두에게 같은 메뉴 | 역할 분기 없이 5개를 똑같이. 단순하지만 Phase 3이 셸 컴포넌트를 다시 연다 | |
| 정해줘 | planner가 4계층 경계를 보고 결정 | |

**User's choice:** 구성을 데이터로 빼낸다
**Notes:** → D-23. 매핑을 어느 계층에 둘지는 Claude's Discretion으로 남겼다(D-26 경계 규칙만 지키면 된다).

### 질문 3 — 「내 차례」 블록

| Option | Description | Selected |
|--------|-------------|----------|
| 컴포넌트만 만들고 건수 0 (권장) | §7-4가 이미 "0이면 이 블록은 사라진다"를 규정. 단위 테스트가 6줄 상한·태그 순서·폰 두 줄을 고정 | ✓ |
| 이번 페이즈에서는 안 만든다 | 수요가 생기는 Phase 5에서. 다만 하단 탭 라벨의 "내 차례 3" 배지 자리는 §6-0 셸 규정이라 어찌 됐든 필요하다 | |
| 가짜 데이터로 채워 보인다 | 실물 preview.html처럼 샘플 3줄. 보기는 좋지만 배포된 앱에 가짜 숫자가 남을 위험 | |

**User's choice:** 컴포넌트만 만들고 건수 0
**Notes:** → D-24. "데이터가 없는 것과 화면이 미완성인 것은 다르다"가 D-22·D-28과 공유하는 논리.

---

## 실물 → 컴포넌트 이관 범위

**논의 전 확인한 사실:** 폰 하단 탭 4번 「더보기」 시트는 §6-0이 셸의 일부로 규정 → 시트(§7-8)는 셸 필수. 로그인·비밀번호 변경이 실제 폼 → §7-2 서버 검증 오류 폼도 이 페이즈에서 진짜로 쓰인다. 대시보드 3종은 Phase 9, 인쇄 2종은 Phase 5·11, 외부 수령자는 Phase 11.

### 질문 1 — 컴포넌트 인벤토리

| Option | Description | Selected |
|--------|-------------|----------|
| 셸 + 쓰이는 원자 컴포넌트 (권장) | 셸(§6-0·§7-8) + 버튼(§7-1) + 입력·오류(§7-2) + 상태 태그(§7-5) + 토스트(§7-6) + 내 차례(§7-4) + 목록 EMPTY(§6-1) + 다섯 상태(§7-7). 전부 이 페이즈에 실제로 쓰인다 | ✓ |
| 거기에 표(§7-3) 읽기 전용까지 | 빈 라우트가 표 뼈대를 보인다. 다만 기준 5가 구현 결정을 Phase 4로 미뤄 두 번 만들 위험 | |
| 최소 — 셸 + 로그인·내 계정에 필요한 것만 | 가장 작지만 §6-0이 「더보기」 시트를 셸로 규정해 셸이 미완성으로 남는다 | |

**User's choice:** 셸 + 쓰이는 원자 컴포넌트
**Notes:** → D-25. 표(§7-3)와 PC 480 모달은 제외. "추측으로 만드는 컴포넌트는 없다"가 기준.

### 질문 2 — 배치와 경계

| Option | Description | Selected |
|--------|-------------|----------|
| 최상위 ui/ + 새 경계 계층 (권장) | boundaries에 ui 타입 신설, from:ui → allow:[ui, lib]만. 컴포넌트가 domain·repositories를 못 부른다. 설정 한 번 바꾸면 끝 | ✓ |
| app/_components/ | app/** 패턴에 흡수돼 설정 변경 0. 하지만 컴포넌트가 domain을 직접 import하는 것을 막을 장치가 없다 | |
| 정해줘 | planner가 Next.js 16 관례와 기존 경계 설정을 보고 결정 | |

**User's choice:** 최상위 ui/ + 새 경계 계층
**Notes:** → D-26. Reversibility costly — 옮기려면 모든 import 경로와 경계 설정을 함께 바꾼다. 기존 8개 요소(app·domain·repositories·db·lib·scripts·test·eslint)에 ui가 아홉 번째로 들어간다.

### 질문 3 — 실물과 앱이 어긋날 때의 기준

| Option | Description | Selected |
|--------|-------------|----------|
| SYSTEM.md가 기준, 실물은 참고 (권장) | system/README.md가 스스로 "앱 코드가 아니다"라고 적었다. 실물이 맞으면 SYSTEM.md를 고친 뒤 따르고, 불일치는 DECISIONS.md에 기록 | ✓ |
| 실물이 기준 | 픽셀로 확정된 것이 진실. 다만 정적 HTML이라 상태·접근성 계약을 다 담지 못한다 | |
| 실물을 스크린샷 기준선으로 | system/shots/를 시각 회귀 기준으로. 강하지만 폰트 로딩·스크롤바 차이로 거짓 실패가 잦다 | |

**User's choice:** SYSTEM.md가 기준, 실물은 참고
**Notes:** → D-27. CLAUDE.md 프론트엔드 규칙("화면 하나만 예외 금지")과 일치.

---

## Phase 1 임시 화면 제거 + 루트 화면 · 알림함·배지 계약

**논의 전 발견:** `grep`으로 확인한 결과 SYSTEM.md에 「로그인」·「내 계정」·「비밀번호」가 한 번도 나오지 않는다(§6-5의 "로그인 없음" 한 번 제외). §6은 "새 템플릿이 필요하면 먼저 여기에 추가한다"고 스스로 규정했으나 템플릿 7개 중 로그인이 없다. 앞서 찾은 「알림함」 빈 곳과 함께 SYSTEM.md에 메울 곳이 둘.

### 질문 1 — 루트 `/`

| Option | Description | Selected |
|--------|-------------|----------|
| 「내 차례」 홈 화면 (권장) | §6-0 "폰 탭 1번이 곧 첫 화면" + §7-4 "역할별 첫 화면 최상단". 지금 구조를 만들면 Phase 9는 데이터만 꽂는다. 건수 0이면 블록이 사라지고 목록 EMPTY가 올라온다 | ✓ |
| 첫 메뉴로 리다이렉트 | 로그인 상태면 /projects, 아니면 /login. 간단하지만 폰 탭 1번이 가리킬 화면이 없어 셸이 미완성 | |
| 지금처럼 최소로 둔다 | Phase 9가 전부 정한다. 기준 2의 "임시 화면이 남아 있지 않다"에 걸린다 | |

**User's choice:** 「내 차례」 홈 화면
**Notes:** → D-28.

### 질문 2 — 관리자 시스템 상태 화면

| Option | Description | Selected |
|--------|-------------|----------|
| SYSTEM.md 기준으로 다시 만든다 (권장) | 01-CONTEXT.md가 이 화면을 "무스타일 임시 화면" 셋 중 하나로 적었고 기준 2가 그것들의 제거를 요구. D-17 관리자 전용·직원 404는 유지하고 셸 안으로 | ✓ |
| 토큰만 입히고 구조는 둔다 | 눈에 거슬리지 않을 만큼만. 제대로 된 재설계는 Phase 3 관리 콘솔에서 | |
| 그대로 두고 Phase 3으로 | 기준 2 판정에서 예외로 기록해야 한다 | |

**User's choice:** SYSTEM.md 기준으로 다시 만든다
**Notes:** → D-29.

### 질문 3 — SYSTEM.md의 로그인 템플릿 빈 곳

| Option | Description | Selected |
|--------|-------------|----------|
| §6에 로그인 템플릿 신설 (권장) | §6이 스스로 규정한 절차. 셸 없는 구조, 실패 문구 자리, 잠금 안내, 임시 비밀번호 배너(D-08), 다섯 상태를 계약으로 적고 그 다음 코드. 내 계정은 §6-3 폼 템플릿을 그대로 쓴다 | ✓ |
| 템플릿 없이 컴포넌트만으로 | 기준 2 문구는 "컴포넌트와 토큰만 쓴다"이지 "템플릿이 있어야 한다"가 아니다. §6의 자기 규정과는 어긋난다 | |
| 코드를 먼저 만들고 역으로 적는다 | 빠르지만 "시스템이 먼저"라는 UX-01의 전제와 순서가 뒤집힌다 | |

**User's choice:** §6에 로그인 템플릿 신설
**Notes:** → D-30. "문서가 코드보다 먼저"가 이 페이즈의 순서 원칙으로 확정됐다.

### 질문 4 — 「알림함·배지」 계약 빈 곳

| Option | Description | Selected |
|--------|-------------|----------|
| §7에 계약만 신설, 구현은 Phase 7 (권장) | 읽음/안 읽음, 목록 행 모양, 진입점, 배지 건수 규칙, 다섯 상태를 SYSTEM.md에 적는다. 기준 4의 목적이 "이후 페이즈가 그대로 가져다 쓰는 계약"이므로 Phase 7이 계약 없이 시작하면 지금 겪은 빈 곳이 반복된다. 코드는 안 쓴다 | ✓ |
| 계약 + 껍데기 컴포넌트까지 | 빈 알림함 컴포넌트(건수 0, EMPTY)까지. 「내 차례」와 같은 처리지만 Phase 2 작업량이 는다 | |
| 배지 규칙 정리로 갈음한다 | 기존 상태 태그·PARTIAL 배지·하단 탭 건수를 한 절로 모으고 알림함 화면 계약은 Phase 7로. ROADMAP 기준 4 문구를 고쳐야 한다 | |

**User's choice:** §7에 계약만 신설, 구현은 Phase 7
**Notes:** → D-31.

---

## 마무리 확인

| Option | Description | Selected |
|--------|-------------|----------|
| CONTEXT.md 쓰자 (권장) | 남은 것은 planner·researcher 재량이거나 이월 | ✓ |
| SYSTEM.md 수정 절차를 더 짚자 | D-30·D-31로 SYSTEM.md가 두 곳 바뀐다. REVIEW.md의 "다음 리뷰는 8점 미만 패스만 전체 검토" 방침에 따라 /plan-design-review를 다시 돌릴지 | |
| 다크 모드를 짚자 | tokens.css에 prefers-color-scheme이 없다. 의도된 것인지 | |
| 기준 3 증명 방법을 짚자 | Playwright 두 뷰포트·axe·시각 회귀 중 무엇을 쓸지 | |

**User's choice:** CONTEXT.md 쓰자
**Notes:** 고르지 않은 세 후보는 잃지 않았다 — 기준 3 증명 방법은 CONTEXT.md의 Claude's Discretion으로, 다크 모드는 Deferred Ideas로 넘겼다. SYSTEM.md 수정 뒤 `/plan-design-review` 재실행 여부는 planner가 정한다.

## Claude's Discretion

사용자가 명시적으로 "정해줘"를 고른 항목은 없다. CONTEXT.md의 Claude's Discretion 절에 적힌 것들은 애초에 논의 대상에서 제외한다고 Claude가 제안하고 사용자가 받아들인 것(기준 3 증명 방법)이거나, 결정의 결과로 자연히 planner 몫이 된 세부(파일 배치·선택자 이름·stylelint 규칙 구성·매핑의 계층·빈 라우트 URL 이름·「더보기」 검색 활성화 여부)다.

## Deferred Ideas

- 회사 도메인 연결 → Phase 3으로 확정(Phase 1 이월 항목이 "Phase 2~3"이었던 것을 좁혔다)
- 표(§7-3) 구현·라이브러리 선택 → Phase 4
- 알림함 구현 → Phase 7
- PC 480 모달 → Phase 5
- 손익 대시보드 → Phase 9 · 인쇄 라우트 → Phase 5·11 · 외부 수령자 → Phase 11
- 「더보기」 시트의 검색(⌘K) 실동작 → 검색 대상 데이터가 생기는 페이즈
- 다크 모드 → 미논의. 필요가 생기면 DECISIONS.md 기록 후 토큰 재정의로
