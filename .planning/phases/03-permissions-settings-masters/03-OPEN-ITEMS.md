# Phase 3 미해결 항목 통합 대장

오늘(2026-09-21) Phase 3에서 나온 지적이 여러 출처에 흩어져 있다 —
`03-REVIEW.md`(코드 리뷰 10건) · `03-SECURITY.md`(보안 보강 R1~R5) ·
`03-VERIFICATION.md`(요구사항 미달 4건) · 독립 DOM 감사(7건) · 사용자
스테이징 QA(3건). 처리 상태를 한 곳에서 보기 위한 대장이다.

**이 파일이 누락 방지 장치다.** 항목을 처리하면 여기 상태를 바꾸고,
새 항목이 나오면 여기 추가한다.

## 처리 완료 (커밋 있음)

| 출처 | 항목 | 커밋 |
|---|---|---|
| /review C-1 | 배포에 파생 시드 단계 없음 → 권한표 빈 채로 뜸 | `849c6a9` |
| 실행 검증 | 순환 import로 seed·account CLI 번들 교착(exit 13) | `9b35c32` |
| /review | 행동 로그 빈 검색 파라미터가 내보내기·정리 차단 | `890634a` |
| /cso T-03-30 | 계급 변경이 보관된 계급을 통과시킴 | `f8fdb44` |
| CI | db-bootstrap 정리가 남은 연결에 걸려 e2e 단계 skip | `75cb810` |
| /verify | 로그인이 행동 로그에 기록되지 않음(OPS-05) | `c8115ae` |
| /verify · R1 | 누수 스캔이 등록 파일 9개 중 7개만 봄(ADMN-03) | `4582307` |
| /verify | 설정 가져오기 진입점 없음(ADMN-06) | `3c6d3f9` |
| /verify | 거래처 수정 화면 없음(MAST-01) | `b8628fc` |
| /review M-5 | 빈 문자열 계좌번호가 암호문을 영구 삭제 | `45af478` |
| /review H-1 | 법인카드 소유자 변경 시 kind 미갱신 | `d9dde36` |
| /review L-2 | 계급 보관만 보관함 재검증 누락 | `25c5aa1` |
| /review M-3 | HistoryList 하드코딩 DOM id 충돌 | `e671ff4` |
| /review M-1 | 권한표 재시도 시 셀이 전부 해제됨 | `8a781f3` |
| DOM 감사 1 | 보관된 거래처가 ?editId=로 수정됨 | `2c8851e` |
| DOM 감사 4·5 | 거래처 폼 자리표시자·안내문·간격 SYSTEM.md 위반 | `2c8851e` |
| 사용자 QA | 코드표 선택 링크 간격 없음 + 폰 터치 목표 미달 | `7aa0536` |
| 사용자 QA | 관리자 화면 9개가 클릭으로 도달 불가(SYSTEM.md 573행) | `fa4a5a2` |
| 사용자 QA | 폼이 목록을 화면 밖으로 밀어냄(§6-1) — 거래처·코드표·사람·법인카드 | `94f9dbd` |
| 사용자 QA(CSV) | 상세 칸에 내부 사용자 ID 노출 · 대상 칸이 원시 UUID | `b0b8886` |
| DOM 감사 7 | 수정이 「문서 생성」으로 기록됨 | `b0b8886` |
| design-review H-1 | 빈 목록 「다음 한 수」가 폰에서 25px | `e19c30e` |
| design-review A-H1 | 조직·계급 화면이 §6-1 재구성에서 빠짐(폼 상시 렌더 · 1차 버튼 2개) | 이 커밋 |
| design-review A-H4 | 사람 목록 「상세」가 스타일 없는 기본 파랑(#0000EE) | 이 커밋 |

## 처리 중

없음 — design-review 둘(비관리자 11건 · 관리자 15건)이 모두 끝났고, 그 결과는
아래 두 절로 나뉘어 있다.

## Phase 3에서 고치기로 한 것 (사용자 승인 2026-09-21)

| 출처 | 항목 | 상태 |
|---|---|---|
| /review L-3 | 로그인 403 메시지를 그대로 노출(`Invalid origin` 등) | **GREEN** — `LOCKED_MESSAGE`를 import 없는 잎 모듈로 분리해 정확 대조. RED: `Invalid origin`이 그대로 반환됨 → 단위 9/9 통과 |
| /review M-2 | 보관과 세션 만료가 비원자적 · `getSession()`이 `archivedAt`을 안 봄 | **GREEN** — `lib/auth.ts`에 `archivedAt` 추가 필드 등록 + `lib/viewer.ts`가 fail-closed로 거부. 전제(better-auth가 요청마다 사용자 행을 다시 읽는다 — `cookieCache` 미사용)를 `test/e2e/archived-session.spec.ts`가 `CI=true` 프로덕션 빌드에서 실제로 확인했다(2026-09-21, 1/1 통과) |

## Phase 4로 미루기로 한 것 (사용자 승인 2026-09-21)

| 출처 | 항목 | 미루는 근거 |
|---|---|---|
| DOM 감사 2·6·7 | 폰 375px에서 관리자 표 가로 스크롤(481>375) · 이름이 1.3글자마다 줄바꿈 | `DECISIONS.md` 48행 위반이 맞다. 다만 §7-3 칸 접기 패턴이 코드베이스에 없어 새 UI 구조 작업(2~3시간)이다. SYSTEM.md 844행이 "관리자 콘솔은 PC 사용이 기본, 폰은 확인·긴급 수정용"이라고 규정해 미루는 근거가 문서에 있다 |
| /review M-4 | 이력 취소 실패가 아무 표시 없이 사라짐 | 사용자에게 보이지만 데이터 손상은 없음 |
| /review L-1 | 메뉴 보기 권한과 정보 노출이 어긋나면 500 오류 화면 | 404·빈 상태로 떨어뜨리면 되나 관리자 설정 실수에만 발생 |
| /cso R2 | 물리 삭제 불변식 범위 오류 | 문서 정합성, 동작 영향 없음 |
| /cso R3 | 자기 잠금 가드가 `view`를 안 막음 | 권한표 화면만 자기 자신에게 잠김 |
| /cso R5 | 법인카드 생성 경로에 owner XOR `superRefine` 누락 | 도메인·DB CHECK 2겹이 여전히 강제 |
| /cso R4 | 위협 항목의 마이그레이션 번호 표기 오차 | 문서 표기만 |

### design-review에서 나온 것 (2026-09-21, `03-DESIGN-REVIEW.md` 26건 중 23건)

**이 절은 사용자 승인 전이다** — 위 표와 달리 Claude 판단으로 미룬 것이고,
사용자가 「이것도 지금 고쳐」라고 하면 Phase 3으로 다시 올라온다. 누락 방지가
이 파일의 목적이므로 승인 여부와 무관하게 전부 적어 둔다.

비관리자 화면 11건 · 관리자 화면 15건 중, 위 「처리 완료」의 셋(H-1 · A-H1 ·
A-H4)을 뺀 나머지다. 공통 근거: **한 화면짜리 결함이 아니라 공용 컴포넌트·
템플릿을 바꿔야 하는 가로지르는 작업**이라 Phase 3 종료 시점에 손대면 이미
전부 GREEN인 화면들을 다시 흔든다.

| 번호 | 항목 | 미루는 근거 |
|---|---|---|
| A-H2 | 관리자 폼 7개의 PC 폼 폭·칸 폭·라벨 배치가 §6-3 밖(폼 1240, select만 전폭 라벨) | `ui/input/TextField`와 `select` 공용 스타일을 동시에 고쳐야 한다 — 비관리자 M-1(`/account`)과 같은 뿌리라 한 묶음으로 처리하는 것이 맞다 |
| A-H3 | 검증 오류 첫 관문이 브라우저 기본 영문 말풍선 · 서버 오류가 폼 맨 아래 14px 한 줄 | `TextField`의 `required` 위임을 걷어내고 `aria-invalid`·`aria-describedby` 배선을 공용 컴포넌트에 넣는 작업. 폼 7개 + `/account`가 한꺼번에 바뀐다 |
| H-2 | 폰 상단 바 사용자 버튼 19px · 드롭다운 항목 35px | 고치는 방법이 둘(폰에서 트리거를 감추거나 44로 키우거나)이고 §6-0 계약 해석이 갈린다 — SYSTEM.md 쪽 판단이 먼저다 |
| M-1 · M-2 | `/account`가 §6-3 폼 템플릿 밖 · 비밀번호 오류가 「원인 · 다음 행동」 아님 | A-H2·A-H3과 같은 뿌리 |
| M-3 | 「더보기」 시트의 Tab 순환이 로그아웃 다음 한 번 `body`를 거친다 | 네이티브 `<dialog>`의 기본 동작이다 — 포커스 트랩을 직접 구현해야 바뀐다 |
| A-M1 | 위험 행동 확인이 §7-8 모달·시트가 아니고 누른 순간 포커스가 사라짐 | `delete-to-archive`를 모달로 바꾸는 작업이 마스터 화면 6개에 동시에 걸린다. 지금 동작은 맞고(색으로 위험 표시 안 함) 그릇과 포커스 계약만 다르다 |
| A-M2 | 설정 체크박스 18개가 폰에서 26px | 한 줄 CSS지만 폰 관리자 화면 전반(위 DOM 감사 2·6·7)과 같은 묶음에서 한 번에 재는 편이 싸다 |
| A-M3 | 읽기용 표 6종에 `<caption>` 없음(코드표는 `th scope`도 없음) | 한 줄씩이지만 표 6개를 건드리므로 Phase 4 표 작업(칸 접기)과 같은 커밋에 넣는다 |
| A-M4 | 서버 오류 문구 3종이 §8-3 형식 밖 | 도메인·액션 세 층에 흩어져 있어 문구 규칙을 한 곳으로 모으는 작업이 먼저다 |
| A-M5 | 설정 체크박스 15개 · 행동 로그 「대상」 칸에 영문 원시 키 노출 | 행동 종류·엔티티 종류의 한국어 사전을 한 곳에 만드는 작업(행동 로그가 이미 절반을 갖고 있다) |
| A-M6 | 값을 바꾸는 표인데 머리글이 읽기용 표 모양(코드표·계급) | §7-3의 예시 목록에 관리자 마스터가 없어 계약 해석이 갈린다 — 판단이 먼저다 |
| A-M7 | 행 동작·패널 입력의 접근 가능한 이름이 행 맥락 없이 전부 같음 | `delete-to-archive` 등 공용 컴포넌트에 행 이름을 넘기는 배선 작업, 화면 6개에 걸친다 |
| A-H1 잔여 | 조직 화면이 `<ul>` 중첩 목록이라 §6-1 원장(`<table>`) 골격이 아니다 | 폼 상시 렌더(A-H1의 위반 본문)는 이번에 고쳤다. 남은 것은 **본부→팀 계층을 평평한 표로 어떻게 보일지**라는 새 설계 판단이고, `DECISIONS.md` 2026-09-21 항목이 다루지 않은 범위다 — 결정 없이 구조만 바꾸면 그 판단이 코드에 숨는다 |
| A-L1~A-L4 · L-1~L-6 | 힌트 길이 · 반복 안내문 · 3차 버튼 크기 두 갈래 · 수치 오차 2건 등 | 전부 Low. 화면이 무너지지 않는다 |

## 사람만 판정 가능한 것 (완료)

브라우저 QA 4건 전부 통과(2026-09-21). 권한 차단 404 · 코드표 비활성화 전파 ·
계좌번호 조회가 「마스킹 해제」로 기록 · Excel CSV가 실제 엑셀에서 한글 정상
(받은 파일 첫 3바이트 `ef bb bf` 확인).
