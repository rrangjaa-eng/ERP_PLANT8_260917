---
phase: 04-project-quotation-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - domain/vendors/index.ts
  - test/unit/dockerfile.test.ts
  - test/unit/vendors/reveal-archived.test.ts
autonomous: true
requirements: [MAST-01, ADMN-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`deploy.sh`를 거치지 않고 런타임 이미지를 실행해도 프로덕션 환경 검사가 돈다 — 짧은 인증 시크릿을 들고 있는 이미지가 조용히 뜨지 않는다"
    - "보관된 거래처의 계좌번호는 마스킹 해제 권한이 있는 계급에게도 평문으로 나오지 않고, 그 거부가 열람 기록보다 앞에 온다"
    - "보관된 거래처를 보관함에서 복원하는 동작이 이 수정 뒤에도 그대로 된다 — 마스킹 해제만 막히고 복원·행동 로그 이름 조회는 건드리지 않는다"
    - "이 플랜의 diff에 UI 컴포넌트·새 표·새 마이그레이션이 0개다 — D-59가 요구한 「UI 컴포넌트 작업보다 앞에, 별도 커밋」이 git 이력에서 보인다"
  prohibitions:
    - statement: "`repositories/vendors.ts:47` `findVendorById`에 `isNull(archivedAt)`를 걸어서는 안 된다. 호출처 셋이 보관된 행을 **보아야** 동작한다 — `repositories/archive.ts:169`가 이 함수를 거래처 항목의 `findById`로 넘기고 `domain/archive/index.ts:78-84` `restore()`가 보관된 행을 찾지 못하면 `ArchivableRowNotFoundError`를 던지며(복원 기능 정지), `domain/action-log/index.ts:188`이 이 함수를 행동 로그의 거래처 이름 해석기로 쓴다(보관 기록의 이름이 사라진다). D-59가 적은 것은 결함의 위치이고 수정의 위치가 아니다"
      status: resolved
      verification: automated
  artifacts:
    - path: "test/unit/vendors/reveal-archived.test.ts"
      provides: "보관된 거래처의 마스킹 해제 거부 + 복호화·기록 미호출을 고정하는 회귀 테스트. 같은 디렉터리 `update-archived.test.ts`가 형태의 선례다"
      min_lines: 30
    - path: "Dockerfile"
      provides: "런타임 스테이지가 APP_ENV를 명시해 zod 기본값 `\"local\"`로 떨어지지 않는다"
      contains: "APP_ENV"
    - path: "test/unit/dockerfile.test.ts"
      provides: "런타임 스테이지 구간을 잘라 APP_ENV 설정 여부를 단언하는 블록 추가"
      contains: "AS runtime"
  key_links:
    - from: "domain/vendors/index.ts"
      to: "domain/vendors/index.ts"
      via: "revealAccountNumber가 행을 읽은 직후 보관 상태를 판정한다 — updateVendor가 같은 파일에서 쓰는 판정과 같은 자리"
      pattern: "archivedAt"
    - from: "Dockerfile"
      to: "lib/env.ts"
      via: "런타임 스테이지의 APP_ENV가 superRefine의 프로덕션 분기를 켠다"
      pattern: "APP_ENV"
---

<objective>
**As a** 시스템 관리자, **I want to** 배포 스크립트를 거치지 않는 경로로도 이미지가 프로덕션 환경 검사를 통과해야 뜨고 보관된 거래처의 계좌번호가 어떤 경로로도 평문이 되지 않기를 원한다, **so that** Phase 3이 남긴 이월 결함 둘이 Phase 4의 새 코드에 섞이지 않고 각각 자기 커밋과 회귀 테스트를 갖는다.

D-59가 지정한 이월 보안 결함 둘(F1·F2)을 **UI 컴포넌트 작업보다 앞에, 각각 별도 커밋으로, 회귀 테스트와 함께** 닫는다. 이 플랜은 이 페이즈의 다른 어떤 플랜과도 파일을 공유하지 않는다.

Purpose: 04-RESEARCH.md Pitfall 4가 이유를 적었다 — 이 둘을 UI 커밋에 섞으면 테스트 실패의 원인이 새 컴포넌트 버그인지 보안 수정 버그인지 구분되지 않는다. 그리고 보안 수정은 되돌릴 때 단독으로 되돌려야 한다.

Output: `Dockerfile` 런타임 스테이지 한 줄, `revealAccountNumber`의 보관 판정 한 블록, 회귀 테스트 둘.

**F2의 수정 위치는 D-59가 적은 곳이 아니다 — 원문 확인으로 바뀌었다.** D-59는 결함을 `repositories/vendors.ts:47` `findVendorById`의 `archivedAt` 필터 누락으로 서술했다(서술 자체는 정확하다). 그러나 그 함수에 필터를 걸면 **호출처 셋 중 둘이 깨진다**(2026-09-21 원문 확인):

| 호출처 | 보관된 행을 보아야 하는 이유 | 필터를 걸면 |
|---|---|---|
| `repositories/archive.ts:169` → `domain/archive/index.ts:78-84` `restore()` | 복원 대상은 정의상 보관된 행이다 | `ArchivableRowNotFoundError` — 거래처 복원 기능 정지 |
| `domain/action-log/index.ts:188` `ENTITY_NAME_RESOLVERS.vendor` | 보관 기록의 대상 이름을 보여준다 | 보관·복원 로그 행의 거래처 이름이 사라진다 |
| `domain/vendors/index.ts:299` `updateVendor` | 보관된 행을 찾아 `ArchivedVendorError`로 거부한다 | `!existing` 갈래로 떨어져 우연히 같은 예외가 난다(깨지지는 않지만 「없음」과 「보관됨」이 합쳐진다) |

그래서 수정은 **실제로 새는 곳 하나**(`domain/vendors/index.ts:377 revealAccountNumber`)에서 한다. 같은 파일 `:295-302`의 `updateVendor`가 **이미 도메인에서 보관 판정을 하고 그 이유를 주석으로 적어 뒀다**(「판정은 화면이 아니라 여기서 한다」) — 선례가 같은 파일 안에 있고, D-59가 잠근 것은 「닫는다·맨 앞에서·별도 커밋·회귀 테스트」이지 파일 좌표가 아니다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: F1 — 런타임 이미지가 프로덕션 환경 검사를 건너뛰지 않게 한다</name>
  <files>Dockerfile, test/unit/dockerfile.test.ts</files>
  <read_first>
    - `Dockerfile` 39–44행(`FROM node:24-slim AS runtime` 직후) — 지금 `NODE_ENV`·`PORT`·`HOSTNAME` 셋만 `ENV`로 세운다. 바로 위 32–37행 build 스테이지는 더미값으로 `APP_ENV=local`을 명시하고 그 주석이 「런타임 값은 Cloud Run 환경 변수가 덮어쓴다」고 적어 뒀다 — 그 전제가 런타임 스테이지에서 기본값을 비워 둔 이유다
    - `lib/env.ts` 47–49행 — `APP_ENV`가 `z.enum(["local","staging","prod"]).optional()` + `.transform((v) => v ?? "local")`. **값이 없으면 `"local"`이 된다**
    - `lib/env.ts` 83–100행 — `rawSchema.superRefine`의 `if (data.APP_ENV !== "local")` 블록. 안에 `BETTER_AUTH_SECRET` 32자 검사와 `BETTER_AUTH_URL` 존재 검사가 있다. `APP_ENV`가 `"local"`이면 **블록 전체가 건너뛰어진다**
    - `scripts/deploy.sh` 315·440행 — 실제 배포는 `APP_ENV=${ENV}`를 Cloud Run 환경 변수로 주입한다. **그래서 배포 경로는 이 결함에 영향받지 않는다** — 남는 노출은 `deploy.sh`를 거치지 않는 모든 실행(로컬 `docker run`, 다른 오케스트레이터, 향후 Cloud Run Job 직접 기동)이다
    - `test/unit/dockerfile.test.ts` 전문(약 60행) — `readFileSync`로 Dockerfile 전문을 읽어 문자열·정규식으로 단언하는 형태. 스테이지 구간을 잘라 보는 블록은 아직 없다
    - `playwright.config.ts` 상단 — `process.env.APP_ENV ??= "local"`. E2E는 자기 값을 세우므로 이 변경의 영향을 받지 않는다
  </read_first>
  <behavior>
    - 런타임 스테이지 구간(`AS runtime` 이후)에 `APP_ENV`를 세우는 `ENV` 선언이 있다 → 지금은 없어서 RED
    - 런타임 스테이지가 세우는 `APP_ENV` 값이 `local`이 아니다 → 프로덕션 검사 분기가 켜진다
    - build 스테이지의 `APP_ENV=local` 더미값은 그대로 남아 있다 → `next build`가 여전히 돈다
  </behavior>
  <action>
`test/unit/dockerfile.test.ts`에 **먼저** 실패하는 단언 블록을 추가한다(RED 확인 후 Dockerfile을 고친다). 블록 이름은 런타임 스테이지의 환경 변수 계약임을 드러내고, 판정은 스테이지 구간을 잘라서 한다: `AS runtime` 문자열의 인덱스부터 문자열 끝까지를 잘라 그 구간 안에서 `APP_ENV`를 세우는 `ENV` 선언을 정규식으로 찾고, 찾은 값이 `local`이 **아님**을 단언한다. build 스테이지 구간(`AS build`부터 `AS runtime` 전까지)에는 `APP_ENV=local`이 여전히 있음을 별도 케이스로 단언해 더미값 제거를 사고로 허용하지 않는다.

`Dockerfile` 런타임 스테이지의 `ENV` 선언에 `APP_ENV=prod`를 더한다. 기존 세 줄의 백슬래시 이어 쓰기 형태를 그대로 유지하고 값은 `prod`로 한다 — 이 값을 고른 이유를 그 줄 옆이나 위에 한 문장으로 남긴다: 이미지의 기본값은 **fail-closed**여야 한다. `staging`을 기본값으로 두면 스테이징 시크릿 규칙이 프로덕션 실행에 적용되고, 비워 두면 지금처럼 검사가 통째로 꺼진다. `prod`가 기본이면 `deploy.sh`가 주입하는 실제 값(`staging`·`prod`)이 그것을 덮어쓰고, 주입 없이 실행하면 짧은 시크릿·URL 누락에서 **부팅이 거부된다**.

`lib/env.ts`를 고치지 않는다 — zod 기본값 `"local"`은 로컬 개발·단위 테스트·E2E가 의존하는 계약이다(`playwright.config.ts` 상단이 같은 값을 명시적으로 세운다). 이 결함은 이미지의 기본값 문제이고 스키마의 기본값 문제가 아니다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/dockerfile.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 새 블록이 RED로 남으면 런타임 스테이지 구간 잘라내기 정규식이 `ENV` 이어 쓰기(백슬래시 줄바꿈) 형태를 못 잡은 것이다 — 구간 전체 문자열에 대해 `/APP_ENV=(\S+)/`로 찾는 형태로 고친다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('Dockerfile','utf8');const i=s.indexOf('AS runtime');if(i<0){console.error('NO RUNTIME STAGE');process.exit(1)}const m=s.slice(i).match(/APP_ENV=(\S+)/);if(!m){console.error('RUNTIME APP_ENV UNSET');process.exit(1)}if(m[1].replace(/\\\\$/,'')==='local'){console.error('RUNTIME APP_ENV IS local');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `NO RUNTIME STAGE`·`RUNTIME APP_ENV UNSET`·`RUNTIME APP_ENV IS local` 중 하나가 나오고 종료 코드가 1이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `Dockerfile`의 `AS runtime` 이후 구간에 `APP_ENV`를 세우는 `ENV` 선언이 있고 그 값이 `local`이 아니다
    - `AS build`부터 `AS runtime` 전까지 구간에는 여전히 `APP_ENV=local` 더미값이 있다
    - 그 값을 고른 이유(fail-closed)가 Dockerfile 안에 한 문장으로 있다
    - `test/unit/dockerfile.test.ts`의 기존 케이스 전부가 그대로 통과한다 — 스테이지 이름·복사 경로·비루트 USER 단언을 건드리지 않았다
    - 이 태스크의 diff에 `lib/env.ts`가 없다
  </acceptance_criteria>
  <done>런타임 이미지가 `APP_ENV`를 명시하므로 `deploy.sh` 없이 실행해도 `BETTER_AUTH_SECRET` 길이 검사가 돌고, 그 계약이 단위 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F2 — 보관된 거래처의 계좌번호 마스킹 해제를 거부한다</name>
  <files>domain/vendors/index.ts, test/unit/vendors/reveal-archived.test.ts</files>
  <read_first>
    - `domain/vendors/index.ts` 368–390행 `revealAccountNumber` 전문 — 순서가 계약이다: 노출 판정(`visible(viewer, REVEAL_INFO_ITEM)`) → `repoFindVendorById` → 계좌번호 없으면 빈 문자열 → **`recordAction`으로 먼저 기록** → `decrypt`. 주석이 「기록이 실패하면 평문을 돌려주지 않는다 — 순서가 바뀌면 기록 없이 열람되는 창이 생긴다」고 그 순서의 이유를 적어 뒀다
    - `domain/vendors/index.ts` 295–302행 `updateVendor`의 보관 판정 — `const existing = await findVendorById(viewer, id); if (!existing || existing.archivedAt !== null) throw new ArchivedVendorError(...)`. 그 위 주석이 「목록이 보관된 행에 「수정」 링크를 감추는 것만으로는 부족하다 … 판정은 화면이 아니라 여기서 한다」. **이 판정이 이 태스크가 따를 선례이고 같은 파일 안에 있다**
    - `domain/vendors/index.ts`의 `ArchivedVendorError` 선언 위치와 부모 클래스 — `UserFacingError` 계열이어야 화면에 문장이 간다(`lib/actions/user-facing-error.ts`는 표식 클래스 하나이고 `lib/actions/client.ts`의 화이트리스트가 그 계열만 message를 내보낸다)
    - `repositories/vendors.ts` 47–50행 `findVendorById` — `where(eq(vendors.id, id))` 하나뿐. **이 함수를 고치지 않는다**(아래 금지 항목의 근거를 read_first로 직접 확인한다)
    - `repositories/archive.ts` 165–172행 — 거래처 항목의 `findById`가 `findVendorById`를 그대로 넘긴다
    - `domain/archive/index.ts` 70–86행 `restore()` — `entry.findById`가 null이면 `ArchivableRowNotFoundError`. 복원 대상은 정의상 보관된 행이다
    - `domain/action-log/index.ts` 187–195행 `ENTITY_NAME_RESOLVERS` — `vendor: { infoItem: "vendor.value", find: defaultFindVendorById }`
    - `test/unit/vendors/update-archived.test.ts` 전문(약 42행) — 이 태스크의 테스트가 그대로 따를 형태: `deps` 주입으로 `findVendorById`·`recordAction`을 가짜로 넣고 `rejects.toBeInstanceOf`로 예외 클래스를 단언하며 `expect(recordAction).not.toHaveBeenCalled()`로 부작용 없음을 단언한다
    - `test/integration/vendors.test.ts` — 거래처 통합 테스트. 복원 경로가 여기 있으면 이 태스크의 회귀 확인 대상이다
    - `app/(app)/admin/vendors/actions.ts` 60–70행 — `revealAccountNumberAction`이 이 도메인 함수의 유일한 호출자다
  </read_first>
  <behavior>
    - 보관된 거래처(`archivedAt`이 날짜)에 마스킹 해제를 호출하면 `UserFacingError` 계열 예외가 나고 `recordAction`도 `decrypt`도 호출되지 않는다 → 지금은 평문을 돌려주므로 RED
    - 보관되지 않은 거래처는 이전과 똑같이 기록 → 복호화 순서로 평문을 돌려준다
    - 계좌번호가 없는 보관된 거래처는 빈 문자열이 아니라 거부다 — 보관 판정이 「열 것이 없다」 판정보다 앞이다
    - 노출 권한이 없으면 보관 여부와 무관하게 기존 `ForbiddenError`가 먼저 난다 — 판정 순서가 바뀌지 않았다
  </behavior>
  <action>
`test/unit/vendors/reveal-archived.test.ts`를 **먼저** 만든다(RED 확인). `update-archived.test.ts`와 같은 구조로, `revealAccountNumber`에 `deps`를 주입해 네 케이스를 단언한다: ① 보관된 행 + 노출 권한 있음 → 거부 예외, `recordAction` 미호출 ② 보관된 행 + 계좌번호 없음 → 빈 문자열이 아니라 같은 거부 예외 ③ 보관되지 않은 행 → 기존 동작(기록 1회 + 평문) ④ 노출 권한 없음 → 기존 `ForbiddenError`가 먼저. `deps`에 `findVendorById`를 주입할 수 있어야 하므로 **`RevealAccountNumberDeps`에 그 필드를 더한다**(`UpdateVendorDeps`가 이미 같은 필드를 갖는 것이 선례다) — 기본값은 `repoFindVendorById`로 두어 호출자는 그대로 둔다.

`domain/vendors/index.ts`의 `revealAccountNumber`에서 행을 읽은 **직후**, 계좌번호 존재 판정보다 **앞에** 보관 판정을 넣는다: 행이 없거나 `archivedAt !== null`이면 거부한다. 거부 예외는 이 파일에 이미 있는 `ArchivedVendorError`를 재사용하고(새 클래스를 만들지 않는다) 메시지는 §8-3 오류 문구 형식(원인 · 다음 행동)에 맞는 한 줄로 쓴다 — 마스킹 해제 맥락이므로 「수정할 수 없습니다」가 아니라 계좌번호 열람이 막혔다는 원인과 보관함에서 복원하는 다음 행동을 담는다. 판정을 `recordAction` 앞에 두는 이유를 한 문장 주석으로 남긴다: 거부된 열람은 열람 기록이 아니다.

`repositories/vendors.ts`를 **건드리지 않는다.** `findVendorById`에 `isNull(vendors.archivedAt)`를 걸면 `repositories/archive.ts:169` → `domain/archive/index.ts` `restore()`가 복원 대상을 찾지 못해 거래처 복원이 정지하고, `domain/action-log/index.ts:188`의 거래처 이름 해석기가 보관 기록의 이름을 잃는다. 이 사실과 근거 좌표를 `revealAccountNumber`의 새 주석에 한 문장으로 남겨, 다음 사람이 「리포지토리에서 막는 게 깔끔하다」로 되돌리지 않게 한다.

회귀를 증명한다: 거래처 보관 → 복원이 여전히 되는 것과 보관 기록의 거래처 이름이 여전히 나오는 것을 **기존 테스트가 이미 덮고 있으면 그것으로 충분하다**. `test/integration/archive.test.ts`·`test/integration/action-log-query.test.ts`를 열어 덮는지 확인하고, 거래처 복원 케이스가 없으면 `test/integration/archive.test.ts`에 한 케이스를 더한다(보관 → 복원 → 목록에서 사라짐).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/vendors</automated>
    <fails_when>종료 코드가 0이 아니다. 새 스펙이 RED로 남으면 `RevealAccountNumberDeps`에 `findVendorById`를 더하지 않아 주입이 무시된 것이다</fails_when>
    <automated>pnpm test:integration</automated>
    <fails_when>종료 코드가 0이 아니다. `archive`·`action-log` describe 블록이 깨지면 `findVendorById`를 고친 것이다 — 금지 항목 위반이므로 즉시 되돌린다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('repositories/vendors.ts','utf8');const i=s.indexOf('export async function findVendorById'),j=s.indexOf('export async function findVendorsByNormalizedName');if(i<0||j<0){console.error('ANCHOR NOT FOUND');process.exit(1)}if(/archivedAt/.test(s.slice(i,j))){console.error('findVendorById WAS FILTERED');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `findVendorById WAS FILTERED` 또는 `ANCHOR NOT FOUND`가 나오고 종료 코드가 1이다 — 앞은 금지 항목 위반, 뒤는 함수 이름이 바뀐 것</fails_when>
  </verify>
  <acceptance_criteria>
    - 보관된 거래처에 마스킹 해제를 호출하면 `UserFacingError` 계열 예외가 나고 복호화도 열람 기록도 일어나지 않는다
    - 보관 판정이 계좌번호 존재 판정보다 앞이고 노출 권한 판정보다 뒤다 — 세 판정의 순서가 테스트로 고정됐다
    - `repositories/vendors.ts`가 이 태스크 diff에 없다
    - 거래처 보관 → 복원과 보관 기록의 거래처 이름 조회가 통합 테스트로 여전히 초록이다
    - 새 예외 클래스를 만들지 않았다 — 같은 파일의 `ArchivedVendorError`를 재사용했다
    - 되돌리지 말아야 하는 이유(호출처 셋)가 코드 주석에 좌표와 함께 남았다
  </acceptance_criteria>
  <done>보관된 거래처의 계좌번호가 어떤 권한으로도 평문이 되지 않고, 복원·행동 로그 이름 조회는 이전과 같이 동작한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 컨테이너 이미지 → 런타임 환경 | 이미지가 자기 기본값으로 뜨는 경로와 오케스트레이터가 값을 주입하는 경로가 다르다. 검사는 둘 다에서 돌아야 한다 |
| 정보 노출표 판정 → 평문 복호화 | 마스킹 해제는 이 시스템에서 유일하게 암호화 컬럼을 평문으로 꺼내는 경로다. 판정과 복호화 사이에 다른 상태 조건(보관)이 끼어든다 |
| 보관(삭제로 보이는 상태) → 데이터 접근 | 사용자에게 「삭제됨」인 행이 계속 읽히는 경로가 남아 있으면 삭제의 의미가 서지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | `Dockerfile` 런타임 스테이지 · `lib/env.ts` superRefine | high | mitigate | 런타임 스테이지가 `APP_ENV=prod`를 명시해 32자 미만 `BETTER_AUTH_SECRET`으로는 부팅이 거부된다. 단위 테스트가 스테이지 구간을 잘라 값과 「local 아님」을 단언한다 |
| T-04-02 | Information Disclosure | `domain/vendors.revealAccountNumber` | high | mitigate | 행을 읽은 직후 보관 판정으로 거부. 거부는 `recordAction`·`decrypt`보다 앞이라 열람 기록도 평문도 발생하지 않는다. 네 케이스 단위 테스트로 판정 순서를 고정한다 |
| T-04-03 | Denial of Service | `repositories/vendors.findVendorById`를 잘못 고쳐 복원이 정지 | high | mitigate | 금지 항목 + 정규식 게이트로 그 함수에 `archivedAt`이 들어가지 않음을 검사하고, 통합 테스트의 `archive`·`action-log` 블록이 두 번째 그물이다 |
| T-04-04 | Repudiation | 거부된 열람이 기록되지 않아 시도 흔적이 없다 | low | accept | 기존 계약이 「열람 성공만 기록」이고(권한 거부도 기록하지 않는다) 이 태스크가 그 계약을 바꾸지 않는다. 시도 기록은 행동 로그 설계 변경이며 이 페이즈 범위 밖이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음, D-45가 새 의존성 0을 요구). `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과 (`pnpm lint`를 grep에 파이프하지 않는다 — 종료 코드가 가려진다)
- `pnpm test:unit` · `pnpm test:integration` 통과
- `git diff --name-only`에 `ui/`·`app/`·`db/`·`scripts/migrate/` 파일이 0개
- 커밋이 둘이다 — F1과 F2가 각자 커밋을 갖는다(D-59)
- **웨이브 1 종료 게이트**: `/review` → **`/cso`**. `/cso`는 선택이 아니다 — 이 플랜은 `Dockerfile` 런타임 스테이지의 프로덕션 환경 검사(F1, `BETTER_AUTH_SECRET` 길이 검사가 도는지)와 계좌번호 **마스킹 해제 경로**(F2, 이 시스템에서 유일하게 암호화 컬럼을 평문으로 꺼내는 자리)를 건드리고, CLAUDE.md가 「페이즈가 인증·권한·암호화·외부 입력을 건드렸으면 `/cso`는 선택이 아니다」로 못박았다. **04-01은 웨이브 1의 단독 플랜이라 다른 플랜이 이 게이트를 대신 적어 주지 못한다** — 여기서 빠지면 Wave 1에 게이트가 아예 없다. UI 변경이 0이므로 `/design-review`는 대상이 아니다(W2 이후가 갖는다)
- 게이트를 페이즈 끝에 몰지 않는다(사용자 지시) — Phase 3에서 몰았다가 `/review` 14건 + `/design-review` 26건이 한꺼번에 나왔다
</verification>

<success_criteria>
- `deploy.sh`를 거치지 않는 이미지 실행에서도 프로덕션 환경 검사가 돈다
- 보관된 거래처의 계좌번호가 평문으로 나오지 않는다
- 거래처 복원과 행동 로그 이름 조회가 이전과 같다 — 결함을 닫으면서 다른 것을 깨지 않았다
- 두 수정이 UI 컴포넌트 작업보다 앞에, 각자 커밋으로 git 이력에 있다
- 웨이브 1 종료 게이트(`/review` · `/cso`)가 실제로 돌았고 결과가 SUMMARY에 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 테스트 | `test/unit/vendors/reveal-archived.test.ts` |
| 변경된 타입 | `RevealAccountNumberDeps`(`findVendorById` 필드 추가) |
| 변경된 함수 | `revealAccountNumber`(보관 판정 추가) |
| 변경된 설정 | `Dockerfile` 런타임 스테이지 `ENV APP_ENV` |
| 새 화면 · 새 표 · 새 마이그레이션 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-01-SUMMARY.md` when done
</output>
