---
phase: 04-project-quotation-ledger
plan: 03
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - domain/money/index.ts
  - test/unit/money/index.test.ts
  - repositories/code-tables.ts
  - test/unit/eslint-rules/fixtures/money-real-brand.ts
  - test/unit/eslint-rules/money-boundary.test.ts
  - test/unit/money/tax-rule.test.ts
autonomous: true
requirements: [FX-01, PROJ-03, PROJ-02]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "금액이 최소단위 정수로만 표현되고 스케일이 타입에 박혀 있다 — 원화 금액을 환율 자리에 넣는 코드는 컴파일되지 않는다(D-53)"
    - "반올림이 이 모듈의 한 함수에서만 일어난다 — 모듈 밖의 금액 산술은 이미 서 있던 린트가 즉시 막는다"
    - "절사가 절대값 기준 대칭이라 `round(x) + round(-x) === 0`이 성립한다 — 받았다가 환불하면 계정에 잔돈이 끼지 않는다(D-84)"
    - "브랜디드 타입 이름이 단독 토큰 `Money`라 `money-boundary` 정규식이 실제로 반응한다 — 그 사실이 린트 규칙 자신의 테스트로 고정됐다"
    - "합계에서 공급가액을 역산하면 「역산된 공급가 + 부가세 = 원래 합계」가 절사 이후에도 성립하고, 성립하지 않는 입력에서는 차이가 값으로 나온다"
    - "합계 13,640,000의 역산이 정확히 공급가 12,400,000 · 부가세 1,240,000이다 — 세율이 경계에서 최소단위 정수가 되어 역산 전체가 정수 산술이고, 부동소수 나눗셈이 만드는 1원 오차가 없다(D-53)"
    - "분할 금액의 합계가 원금과 정확히 같다 — 마지막 회차가 나머지를 흡수한다"
    - "세금 규칙 네 종류가 코드표의 규칙 정의와 이력형 설정의 세율을 각자의 정본에서 읽고, 기준일에 유효했던 세율로 계산한다"
    - "외화 금액과 환율에서 나온 원화 환산액이 서버 계산값이고 KRW는 환율 1이다"
  prohibitions:
    - statement: "타입 이름에 `Money`를 다른 단어 문자와 붙여 써서는 안 된다(`MoneyKRW` · `FxMoney` · `MoneyKrw`). `eslint/rules/money-boundary.mjs:3`의 `/\\bMoney\\b/`는 단어 경계로 판정하므로 그런 이름은 **매칭하지 않고** 그 타입의 산술이 모듈 밖에서 조용히 통과한다 — 탐지 구멍이다. 허용되는 이름은 `Money`와 `Money<...>`뿐이다"
      status: resolved
      verification: automated
    - statement: "부동소수점으로 금액을 담아서는 안 된다. 최소단위 정수 표현이 D-53의 본체이고 `round()`가 정본 하나인 이유다. 나눗셈 결과를 `Number`로 들고 다니지 않고 즉시 절사 규칙을 적용해 정수로 만든다"
      status: resolved
      verification: automated
    - statement: "공급가 역산을 **부동소수 비율로 나누어서는 안 된다.** `total / (1 + vatRate)` 형태는 실측으로 틀린 값을 낸다 — `13640000 / 1.1 = 12399999.999999998`이고 0 방향 절사가 **12,399,999**를 만들어 부가세가 1,240,001이 된다. `04-UI-SPEC.md:247`·`:572`와 이 플랜 자신이 적은 기대값은 12,400,000 / 1,240,000이다. 무작위 표본 20만 건에서 **5.5%**, 11,000의 배수(세금계산서 합계의 실제 모양) 20만 건에서 **59.7%**가 1원 어긋난다(둘 다 실측 2026-09-22). 세율은 `domain/settings/keys.ts:57`에서 **부동소수 `number`**(기본 0.1)로 들어오므로 경계에서 정수로 바꾼 뒤 정수 산술만 쓴다 — 그것이 D-53의 「산술이 전부 정수라 부동소수점 오차가 원리적으로 없다」를 실제로 성립시키는 유일한 형태다"
      status: resolved
      verification: automated
    - statement: "세율을 코드표의 `tax_rule` JSONB에 담아서는 안 된다. `domain/code-tables/tax-rule.ts` 머리 주석이 그 이유를 적어 뒀다 — 세율은 이력형 설정 키가 정본이고 담으면 정본이 둘이 된다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/money/index.ts"
      provides: "금액 표현·반올림·환산·분할·역산·세금 규칙의 단일 정본. `money-boundary` 린트가 예외로 두는 유일한 경로"
      exports: ["Money", "MoneyScale", "money", "rateToMinor", "RATE_ONE", "round", "toKrw", "splitWithRemainder", "grossFromTotal", "applyTaxRule"]
      min_lines: 150
    - path: "test/unit/money/index.test.ts"
      provides: "세금 규칙 4종 × 절사 표 기반 단위 테스트(ROADMAP 기준 5 원문 요구)"
      min_lines: 150
    - path: "test/unit/eslint-rules/fixtures/money-real-brand.ts"
      provides: "실제 `domain/money`의 `Money` 타입을 import해 모듈 밖 산술을 시도하는 픽스처 — A2 가정(`Money<Scale>`의 `typeToString`이 `Money<…>`로 나온다)을 규칙 테스트가 직접 증명한다"
      min_lines: 15
    - path: "repositories/code-tables.ts"
      provides: "table_key + value로 코드표 한 행을 찾는 함수 추가 — `applyTaxRule`이 평문 증빙 종류 문자열로 규칙을 찾는 경로"
      contains: "TableKeyAndValue"
  key_links:
    - from: "domain/money/index.ts"
      to: "domain/settings/registry.ts"
      via: "세율·절사 단위·기준일 종류를 이력형/단순형 설정에서 읽는다. 이력형은 기준일을 asOf로 넘긴다"
      pattern: "getSettingValue"
    - from: "domain/money/index.ts"
      to: "repositories/code-tables.ts"
      via: "증빙 종류 코드표 항목의 tax_rule JSONB에서 규칙 종류·절사·최소 징수액을 읽는다"
      pattern: "findCodeItemByTableKeyAndValue"
    - from: "eslint/rules/money-boundary.mjs"
      to: "domain/money/index.ts"
      via: "규칙이 먼저 서 있고 모듈이 비어 있었다 — 이 모듈이 생기는 순간 리포 전체의 금액 산술이 경계 안으로 들어온다"
      pattern: "Money"
---

<!-- planner-discipline-allow: MoneyKRW -->
<!-- planner-discipline-allow: FxMoney -->
<!-- planner-discipline-allow: MoneyKrw -->
<!-- planner-discipline-allow: vatRate -->

<objective>
**As a** 경영관리본부 담당자, **I want to** 어느 화면에서 본 금액이든 같은 한 함수가 반올림한 값이기를 원한다, **so that** 인트라넷이 남긴 「차익 불일치 66줄」 같은 오류가 구조적으로 불가능해진다.

`domain/money` 단일 모듈을 세운다(ROADMAP Phase 4 기준 5, Issue 8). 이 모듈이 생기는 순간 **이미 서 있던 린트**(`eslint/rules/money-boundary.mjs`, type-aware, `domain/money` 경로만 예외)가 리포 전체의 금액 산술을 이 모듈 안으로 가둔다.

Purpose: 04-RESEARCH.md § Don't Hand-Roll이 「hand-roll 하지 말라고 하는 대상은 전부 계산·판정 로직이지 UI 컴포넌트가 아니다」로 이 페이즈의 대칭을 적었다. 반올림은 정본이 하나가 아니면 반드시 갈라지고, 그 갈라짐의 실측 증거가 인트라넷 `quo_benefit` 컬럼의 66줄이다.

Output: `Money` 브랜디드 타입 + 여섯 함수, 세금 규칙 4종 × 절사 표 단위 테스트, 린트 규칙이 실제 타입에 반응함을 증명하는 픽스처, 코드표 조회 함수 하나.

**스케일을 타입에 박는다. 모양은 D-80이 이미 잠갔다 — 고르지 않고 받아 적는다.** D-53이 정한 것은 수치 표현(원화 1원 정수 · 외화 1/100 정수 · 환율 1/10000 정수)과 DB 타입(`bigint`)이고, **D-80이 그 위에 모양을 확정했다**: 「`Money`는 브랜디드 정수 스칼라 하나다 — `type Money = number & { __brand }`. 통화·환율·원화 환산액은 함수 인자와 행 컬럼으로 단다. 조립 헬퍼는 두지 않는다 — 단일 사용처 추상화 금지(CLAUDE.md 2번)」. 근거는 `money-boundary.mjs`가 TS 타입 문자열에 반응하므로 **스칼라여야 모든 산술 지점에서 자동으로 잡힌다**는 것이다(객체면 그 필드를 다시 `Money`로 타이핑해야 같은 효과다). 그래서 이 플랜의 모양은 **금액만 브랜디드 제네릭 스칼라이고 통화·환율은 같은 행의 별개 컬럼**이다. D-80과 같은 결인 이유 셋 — ① 04-RESEARCH Pattern 2가 `Money<Scale>` 제네릭을 권장하고 그 형태가 `typeToString`에서 `Money<…>`로 나와 린트 정규식이 반응한다 ② FX-01이 요구하는 네 값(통화·외화 금액·환율·원화 환산액)은 **컬럼 묶음**이라고 ROADMAP이 직접 적었다(「`domain/money`의 공용 컬럼 묶음」) ③ 단일 객체로 묶으면 `bigint` 컬럼 하나에 객체를 넣을 수 없어 어차피 컬럼으로 펼쳐야 하고, 그러면 같은 정보가 두 모양으로 존재한다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `Money` 브랜디드 타입 + `round()` + `toKrw()` — 린트가 실제로 반응하는지까지 증명한다</name>
  <files>domain/money/index.ts, test/unit/money/index.test.ts, test/unit/eslint-rules/fixtures/money-real-brand.ts, test/unit/eslint-rules/money-boundary.test.ts</files>
  <read_first>
    - `eslint/rules/money-boundary.mjs` 1–30행 — `MONEY_TYPE_PATTERN = /\bMoney\b/`, 산술 연산자 집합(`+ - * / %`)과 복합 대입(`+= -= *= /=`), `isExemptPath`가 경로 조각에 `domain` 다음 `money`가 오는지로 판정한다. **타입 정보가 없으면 규칙이 조용히 넘어가지 않고 파일마다 한 번 설정 오류를 보고한다**
    - `eslint.config.mjs` 75–92행 — `plant8/money-boundary": "error"`가 전체에 걸려 있고 `**/*.mjs`에서만 꺼진다
    - `test/unit/eslint-rules/money-boundary.test.ts` 전문 — `@typescript-eslint/rule-tester`로 규칙을 돌리는 형태와 픽스처를 어떻게 참조하는지
    - `test/unit/eslint-rules/fixtures/money.ts` — 지금 `export type Money = number & { readonly __brand: "Money" }`로 선언된 가짜 픽스처. 이 태스크는 **실제 모듈을 import하는 픽스처를 하나 더** 만든다(기존 픽스처는 건드리지 않는다)
    - `04-RESEARCH.md` Pattern 2 전문(294–324행) — `\b` 경계가 왜 `MoneyKRW`를 놓치는지의 문자 단위 추적과 권장 제네릭 선언. **A2 가정**(제네릭 인스턴스화가 구조적으로 펼쳐질 이론적 가능성)이 이 태스크의 검증 대상이다
    - `db/schema/action-log.ts` 19행 · `db/schema/auth.ts` 85행 — `bigint("…", { mode: "number" })` 선례. 04-05가 쓸 형태이고 이 모듈의 정수 표현이 그것과 맞아야 한다
    - `docs/design/SYSTEM.md` §2-4 — 표시 규칙(원화 정수 + 천 단위 쉼표 · 하이픈 마이너스 · 0은 `0` · 없음은 `—` · 외화 2행 · 계산 불가는 `계산 불가 · 환율 없음`). **표시는 이 모듈이 아니라 `ui/table`의 `kind` 축이 한다**(04-08) — 이 모듈은 값만 만든다
    - `domain/viewer.ts` — 이 모듈이 `viewer`를 받아야 하는지 판단하는 근거. 순수 계산은 받지 않고, 설정·코드표를 읽는 `applyTaxRule`만 읽기 경로를 쓴다(`domain/settings/registry.ts:78-85`가 `SYSTEM_VIEWER`로 권한 판정 없이 읽는 선례)
  </read_first>
  <behavior>
    - `Money<"krw">` 값을 `Money<"rate_minor">`를 받는 자리에 넘기면 타입 오류다 → 스케일 혼동이 컴파일에서 잡힌다
    - **절사는 절대값 기준 대칭이다(D-84).** 부호를 떼고 절대값에 규칙을 적용한 뒤 부호를 되붙인다. 「부호 2 × 방식 3」 여섯 칸의 기대값은 전부 아래에 박혀 있고 **실행자가 고르지 않는다**:
      · `round(1234, { unit: 10, method: "truncate" })` → `1230` / `round(-1234, …)` → `-1230`
      · `round(1234, { unit: 10, method: "round" })` → `1230` / `round(-1234, …)` → `-1230`
      · `round(1235, { unit: 10, method: "round" })` → `1240` / `round(-1235, …)` → `-1240` (절반은 절대값을 키우는 쪽)
      · `round(1234, { unit: 10, method: "ceil" })` → `1240` / `round(-1234, …)` → `-1240` (**0에서 먼 방향** — 수직선 위쪽이 아니다)
    - **구속 속성: `round(x, o) + round(-x, o) === 0`이 모든 `x`·모든 `o`에서 성립한다(D-84).** 대칭이 아니면 직접 받았다가 환불할 때 10원이 계정에 끼어 남아 회계가 안 맞고, EXP-14의 환불·할인이 음수를 실제로 만든다. **이 속성이 여섯 칸의 기대값보다 상위 계약이다** — 둘이 어긋나면 속성이 이긴다
    - `unit: 1`은 항등이다 — 이미 정수라 값이 그대로 나온다(양수·음수 모두)
    - `toKrw({ amountMinor, rateMinor })`가 외화 1/100 × 환율 1/10000에서 원화 정수를 만들고, 그 결과가 `round()`를 거친 값이다
    - 통화가 KRW면 환율은 1(=`10000` 최소단위)이고 `toKrw`가 입력 금액을 그대로 돌려준다
    - 환율이 없으면(`null`) `toKrw`가 던지지 않고 「계산 불가」를 뜻하는 값(`null`)을 돌려준다 — §7-7 PARTIAL이 그 상태를 렌더한다
    - `domain/money` 밖의 파일에서 `Money` 타입 값에 `+`를 쓰면 `pnpm lint`가 실패한다
  </behavior>
  <action>
`test/unit/money/index.test.ts`를 **먼저** 쓴다(RED 확인 — 모듈이 없어 import가 실패하는 것이 첫 RED다). behavior의 절사 표를 명시 기대값 표로 옮긴다: `unit ∈ {1, 10}` × `method ∈ {truncate, round, ceil}` × 부호 둘(양수·음수) × 경계 입력(정확히 나누어떨어지는 값 · 절반 값 · 절반 미만 · 절반 초과) 전체 조합. 조합을 `it.each` 표 하나로 쓴다. **behavior에 박힌 「부호 2 × 방식 3」 여섯 칸의 기대값을 그대로 옮겨 적는다 — 실행 시점에 다시 고르지 않는다(D-84).**

**대칭 속성을 별도 케이스로 둔다**: 표의 모든 `(값, 단위, 방식)` 조합에 대해 `round(x, o) + round(-x, o) === 0`을 단언한다. 여섯 칸의 기대값과 이 속성이 어긋나면 **속성이 이긴다** — 그 우선순위를 케이스 제목에 D-84 인용과 함께 적는다.

`domain/money/index.ts`를 만든다:
- `export type MoneyScale = "krw" | "fx_minor" | "rate_minor"`
- `export type Money<S extends MoneyScale = MoneyScale> = number & { readonly __brand: "Money"; readonly __scale: S }` — **이름은 정확히 `Money`다**(금지 항목). 선언 바로 위 주석에 `money-boundary` 정규식과 `\b` 경계 이유를 한 문장으로 남겨, 다음 사람이 `MoneyKrw` 같은 이름으로 「읽기 좋게」 바꾸지 않게 한다
- `export function money<S extends MoneyScale>(minor: number, scale: S): Money<S>` — 브랜딩 생성자 하나. 정수가 아니면 던진다(부동소수점이 들어오는 경로를 여기서 끊는다). **`as Money` 캐스팅을 이 모듈 밖에서 쓰지 못하게 하려면 생성자가 유일한 입구여야 한다** — 04-RESEARCH § Anti-Patterns가 이 우회를 지목했다
- `export const RATE_ONE = 10_000` — 환율·세율의 최소단위 「1.0」(D-53: 환율은 1/10000 단위 정수). `export function rateToMinor(rate: number): Money<"rate_minor">` — **부동소수 비율이 정수가 되는 단 하나의 지점**이다. `Math.round(rate * RATE_ONE)`로 만들고 결과가 정수 범위를 벗어나면 던진다. 설정 레지스트리의 세율 키 여섯(`tax.vat.rate` 등)이 전부 `number`(기본 0.1)라 **경계가 필요하고, 경계가 하나여야 각 호출처가 자기 방식으로 반올림하지 않는다.** 그 이유와 `domain/settings/keys.ts:57` 좌표를 함수 위 주석에 남긴다
- `export type RoundingUnit = 1 | 10` · `export type RoundingMethod = "truncate" | "round" | "ceil"` — 값은 새로 만들지 않고 `domain/code-tables/tax-rule.ts`의 `TAX_ROUNDING_METHOD_VALUES`·`roundingUnit` 리터럴 유니언과 **같은 집합**이어야 한다. 그 모듈에서 타입을 재수출할 수 있으면 재수출한다(정본 둘을 만들지 않는다)
- `export function round<S extends MoneyScale>(value: Money<S>, opts: { unit: RoundingUnit; method: RoundingMethod }): Money<S>` — **이 파일에서 유일하게 나눗셈·`Math.*`를 쓰는 함수**다. **구현 형태가 D-84로 확정돼 있다: 부호를 떼고 절대값에 규칙을 적용한 뒤 부호를 되붙인다.** 그래서 `truncate`는 0 방향, `round`는 절반에서 절대값을 키우는 방향, `ceil`은 **0에서 먼 방향**이다 — `Math.ceil`을 절대값이 아닌 원값에 직접 쓰면 음수에서 대칭이 깨진다. 구속 속성 `round(x) + round(-x) === 0`과 그 이유(환불 시 10원이 계정에 끼어 남으면 회계가 안 맞는다 · EXP-14가 음수를 실제로 만든다)를 함수 위 주석에 D-84 인용과 함께 남긴다
- `export function toKrw(input: { amountMinor: Money<"fx_minor"> | Money<"krw">; currency: string; rateMinor: Money<"rate_minor"> | null; rounding: { unit: RoundingUnit; method: RoundingMethod } }): Money<"krw"> | null` — KRW는 환율 판정 없이 통과, 환율 `null`이면 `null`, 그 외는 최소단위 스케일을 맞춘 뒤 `round()`를 거쳐 원화 정수를 만든다. `null`을 돌려주는 이유(§7-7 PARTIAL이 「계산 불가 · 환율 없음」으로 렌더한다)를 주석에 남긴다
- 파일 머리 주석에 ROADMAP 기준 5 인용 한 줄과 「이 모듈 밖의 금액 산술은 린트가 막는다」를 남긴다

**A2 가정을 검증한다(이 태스크의 tracer 지점).** `test/unit/eslint-rules/fixtures/money-real-brand.ts`를 만들어 `@/domain/money`의 실제 `Money`를 import하고 모듈 밖에서 `a + b` 산술을 한 줄 쓴다. `test/unit/eslint-rules/money-boundary.test.ts`에 그 픽스처를 쓰는 invalid 케이스를 더해 **규칙이 실제 제네릭 타입에도 반응함을 단언한다**. 기존 가짜 픽스처 케이스는 건드리지 않는다. 이 검증이 통과하지 않으면(TS가 제네릭을 구조적으로 펼쳐 `typeToString`이 `number & {…}`로 나오면) 타입 선언을 **비제네릭 `Money` + 스케일을 별도 phantom 필드**로 바꾸고 그 사실을 SUMMARY에 적는다 — 이름이 단독 토큰 `Money`인 것이 스케일 안전성보다 우선한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/money test/unit/eslint-rules</automated>
    <fails_when>종료 코드가 0이 아니다. 새 invalid 케이스가 통과하지 않으면 A2 가정이 틀린 것이고 action 마지막 단락의 대안으로 넘어간다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. `domain/money/index.ts` 안의 산술이 보고되면 `isExemptPath`가 경로를 못 잡은 것이다(경로 조각이 정확히 `domain/money`인지 확인). grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 타입 이름이 `Money`이고 제네릭 인자만 붙는다 — `grep -rn "Money[A-Za-z0-9_]" domain/ repositories/ app/ lib/` 결과에 식별자가 0개다
    - 절사 표 전체 조합(단위 2 × 방식 3 × 부호 2 × 경계 4)이 명시 기대값으로 고정됐다
    - 「부호 2 × 방식 3」 여섯 칸의 기대값이 behavior에 박힌 값 그대로다 — 실행자가 음수 방향을 고르지 않았다(D-84)
    - `round(x) + round(-x) === 0`이 표의 모든 조합에서 단언되고, 그 속성이 개별 기대값보다 상위임이 케이스 제목에 적혔다
    - `ceil`이 절대값 기준(0에서 먼 방향)이고 원값에 `Math.ceil`을 직접 쓰지 않았다
    - 나눗셈·`Math.*` 호출이 `round()` 안에만 있다
    - `money()` 생성자가 정수 아닌 입력을 던진다
    - 환율 없음이 예외가 아니라 `null`이고 그 이유가 주석에 있다
    - `money-boundary` 규칙 테스트에 **실제 모듈을 import하는** invalid 케이스가 있고 통과한다
    - `pnpm lint`가 전체에서 초록이다 — 기존 파일에서 새 위반이 생기지 않았다
  </acceptance_criteria>
  <done>금액이 스케일 박힌 정수 브랜디드 타입이고 반올림이 한 함수에만 있으며, 그 강제가 이미 서 있던 린트에서 실제로 작동함이 규칙 자신의 테스트로 증명됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `grossFromTotal()` · `splitWithRemainder()` — 절사 이후에도 합이 맞는다</name>
  <files>domain/money/index.ts, test/unit/money/index.test.ts</files>
  <read_first>
    - `04-RESEARCH.md` § Code Examples `grossFromTotal()` 스텁(640–656행) — 계약은 「합계 = 역산된 공급가 + 부가세가 `round()` 이후에도 성립해야 한다」다. **그 스텁의 나눗셈 형태는 따르지 않는다** — 부동소수 비율로 나누면 항등식은 성립하면서 공급가가 1원 낮아진다(금지 항목의 실측 수치). 이 태스크가 고정하는 것은 항등식 **하나가 아니라 둘**이다: 항등식 + 명시 기대값
    - `.planning/ROADMAP.md` Phase 4 기준 5 원문 — `splitWithRemainder()`의 계약이 「분할 시 합계가 원금과 같도록 마지막 회차 보정」이고, `grossFromTotal()`이 「합계 → 공급가 역산」이며 「통장에 실제 오간 돈 칸만 합계(부가세 포함)로 적고 계산값과 다르면 차이를 표시한다」
    - `.planning/REQUIREMENTS.md` PROJ-03(28행) · FX-01(76행) — 입금액은 통장 합계, 계약 금액·발행액은 공급가액. 분할 보정은 FX-01의 마지막 문장
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` 화면 계약 7 섹션 1 — 표시 문구가 정본이다: `부가세 10% 1,240,000 · 합계 13,640,000 · 서버 계산` / 입금액 역산은 `공급가액 12,400,000 역산 · 계산값과 30,000 차이 · 서버 계산`, **차이가 0이면 뒷 조각을 쓰지 않는다**. 이 함수는 그 세 값(공급가·부가세·차이)을 돌려주기만 하고 문구는 화면이 만든다
    - Task 1이 만든 `round()`의 정확한 시그니처와 음수 동작
    - `domain/settings/keys.ts` **56–66행 `TAX_VAT_RATE`** — `kind: "historized"` · `schema: z.coerce.number().min(0).max(1)` · `default: 0.1`. **값이 부동소수 `number`다**(실측 2026-09-22). 이것이 B2의 발원지다 — 이 값을 그대로 나눗셈에 쓰면 `13640000 / 1.1 = 12399999.999999998`이 된다. `TAX_ROUNDING_VAT_UNIT`(simple, default 1)도 같은 자리에 있다. `grossFromTotal`의 호출자가 넘길 값의 출처이고 **호출자는 Task 1의 `rateToMinor`로 감싸 넘긴다.** **이 함수 자신은 설정을 읽지 않고 인자로 받는다**(순수 함수로 남겨 단위 테스트가 DB 없이 돈다)
    - Task 1이 만든 `RATE_ONE`·`rateToMinor`의 정확한 형태 — 부동소수 비율이 정수가 되는 **유일한 지점**이다
  </read_first>
  <behavior>
    - `grossFromTotal(13_640_000, rateToMinor(0.1), {unit:1,method:"truncate"})` → 공급가 **정확히** `12_400_000` · 부가세 `1_240_000` · 차이 `0`. **이 값은 `04-UI-SPEC.md:247`·`:572`의 화면 문구 정본이고 PROJ-03의 「차이」 칸이 이 값을 기준으로 0이 된다 — 12,399,999가 나오면 기대값을 고치지 말고 산술을 고친다**
    - 항등식(`공급가 + 부가세 === 합계`)은 12,399,999에서도 성립하므로 **항등식만으로는 이 오차를 잡지 못한다.** 그래서 위 기대값이 별도 케이스로 박혀 있다
    - 세율이 **최소단위 정수**로 들어온다 — 역산이 정수 곱셈·정수 나눗셈으로만 이뤄지고 부동소수 비율이 중간에 생기지 않는다
    - 합계 × `RATE_ONE`이 안전 정수 범위를 넘으면 던진다 — 상한은 약 9,007억 원이고 D-53이 상정한 100억 원의 90배다
    - 역산이 정확히 나누어떨어지지 않는 합계(예: `13_640_001`)에서도 `공급가 + 부가세 === 입력 합계`가 성립한다 — 부가세를 합계에서 공급가를 뺀 값으로 확정해 반올림 잔차가 사라진다
    - 계산값과 사람이 적은 공급가액이 다를 때 그 차이가 부호 있는 값으로 나온다(같으면 0)
    - `vatRate === 0`이면 공급가 = 합계, 부가세 = 0
    - 음수 합계(EXP-14 환불)에서도 같은 항등식이 성립한다
    - `splitWithRemainder(10_000, 3)` → `[3334, 3333, 3333]` 또는 `[3333, 3333, 3334]` 중 **계약이 정한 한 모양**이고 합이 정확히 `10_000`이다
    - `splitWithRemainder`에 절사 단위를 주면 앞 회차들이 그 단위의 배수이고 **마지막 회차만** 나머지를 흡수한다
    - 회차 수가 0이거나 음수면 던진다
  </behavior>
  <action>
`test/unit/money/index.test.ts`에 두 함수의 케이스를 **먼저** 더한다(RED 확인). 단언이 **둘**이고 둘 다 필요하다:

1. **명시 기대값** — `grossFromTotal(13_640_000, rateToMinor(0.1), {unit:1,method:"truncate"})`가 공급가 `12_400_000` · 부가세 `1_240_000`임을 값으로 단언한다. 여기에 11,000의 배수 셋(예: `33_000` → 30,000 / `55_000` → 50,000 / `110_000` → 100,000)을 표로 더한다 — **이 모양이 실제 세금계산서 합계이고 실측에서 59.7%가 어긋난 구간이다**. 케이스 제목에 「항등식만으로는 잡히지 않는다」를 적는다
2. **항등식 속성** — `supply + vat === total`(`grossFromTotal`)과 `parts.reduce(sum) === principal`(`splitWithRemainder`)을 모든 케이스에서 단언하고, 무작위 입력 100건에 대해 항등식만 단언하는 케이스를 하나 둔다(속성 테스트 — 새 의존성 없이 `for` 루프와 결정적 시드로 쓴다)

두 단언의 관계를 파일 주석에 한 줄로 적는다: **항등식은 산술이 정수든 부동소수든 성립하므로 명시 기대값이 없으면 1원 오차가 조용히 통과한다.**

`domain/money/index.ts`에 더한다:
- `export function grossFromTotal(total: Money<"krw">, vatRateMinor: Money<"rate_minor">, rounding: { unit: RoundingUnit; method: RoundingMethod }, claimedSupply?: Money<"krw">): { supply: Money<"krw">; vat: Money<"krw">; diff: Money<"krw"> }` — **세율을 부동소수 `number`가 아니라 최소단위 정수로 받는다.** 정수화는 Task 1의 `rateToMinor` 한 곳에서만 일어나고 호출자(04-09·04-12)가 설정에서 읽은 값을 그것으로 감싸 넘긴다. 구현 순서를 주석으로 못박는다: ① `total × RATE_ONE`이 안전 정수 범위 안인지 확인하고 넘으면 던진다 ② 공급가 후보 = `total × RATE_ONE`을 `(RATE_ONE + vatRateMinor)`로 나눈 뒤 `round()`로 절사 — **분자·분모가 둘 다 정수이므로 중간에 부동소수 비율이 생기지 않는다** ③ **부가세는 다시 계산하지 않고 `total - supply`로 확정** — 이것이 항등식이 절사 이후에도 성립하는 이유다 ④ `diff`는 `claimedSupply`가 있을 때 `claimedSupply - supply`, 없으면 0. `claimedSupply`를 선택 인자로 둔 이유(입금액 칸은 사람이 공급가액도 적을 수 있고 적지 않을 수도 있다)를 주석에 남긴다.

  ②의 형태를 고른 이유를 함수 위 주석에 **실측 수치와 함께** 남긴다: 부동소수 비율로 나누면 `13640000`에서 `12399999.999999998`이 나와 0 방향 절사가 12,399,999를 만들고, 그 값이 `04-UI-SPEC.md:247`의 화면 문구(12,400,000)와 PROJ-03의 「차이」 칸을 조용히 틀리게 한다. **항등식은 그때도 성립하므로 항등식 테스트가 그것을 잡지 못한다** — 다음 사람이 「기대값이 틀렸네」로 테스트를 고치지 않도록 그 사실을 주석에 적는다
- `export function splitWithRemainder(principal: Money<"krw">, count: number, rounding: { unit: RoundingUnit; method: RoundingMethod }): Money<"krw">[]` — 앞 `count - 1`개를 `round(principal / count)`로 만들고 **마지막 하나를 `principal - 앞들의 합`으로** 확정한다. 보정을 마지막에 두는 것은 ROADMAP 원문(「마지막 회차에서 보정한다」)이므로 재량이 아니다. `count < 1`이면 던진다
- 두 함수 모두 설정을 읽지 않는다 — 세율·절사는 인자다. 그 이유(단위 테스트가 DB 없이 돌고 `applyTaxRule`이 유일한 조회 지점이 된다)를 주석에 남긴다
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/money/index.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 항등식 케이스가 무작위 입력에서 깨지면 부가세를 다시 계산한 것이다 — `total - supply`로 확정해야 한다. **명시 기대값 케이스가 12,399,999로 실패하면 기대값이 아니라 산술을 고친다** — 세율을 `rateToMinor`로 정수화해 정수 곱셈·나눗셈으로 역산한다</fails_when>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('domain/money/index.ts','utf8');const i=s.indexOf('export function grossFromTotal');if(i<0){console.error('grossFromTotal NOT FOUND');process.exit(1)}const rest=s.slice(i+20);const j=rest.indexOf('export function');const body=(j<0?rest:rest.slice(0,j)).replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');if(/\bvatRate\b/.test(body)){console.error('grossFromTotal STILL TAKES A FLOAT RATE');process.exit(1)}if(!/RATE_ONE/.test(body)){console.error('grossFromTotal DOES NOT USE THE INTEGER RATE SCALE');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `grossFromTotal STILL TAKES A FLOAT RATE`(세율이 부동소수로 남았다 — 1원 오차의 원인) 또는 `grossFromTotal DOES NOT USE THE INTEGER RATE SCALE`(정수 스케일을 안 썼다) 중 하나가 나오고 종료 코드가 1이다. 판정은 주석을 걷어낸 본문에서 한다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grossFromTotal(13_640_000, …)`이 공급가 **12,400,000** · 부가세 1,240,000을 값으로 단언한다 — 기대값이 12,399,999로 완화되지 않았다
    - 11,000의 배수 표(세금계산서 합계의 실제 모양) 케이스가 셋 이상 있다
    - 세율이 최소단위 정수로 들어오고 정수화가 `rateToMinor` 한 곳에서만 일어난다
    - `supply + vat === total`이 무작위 입력 100건 전부에서 성립한다
    - `parts` 합이 원금과 정확히 같고 보정이 **마지막** 회차에 있다
    - 부가세율 0과 음수 합계에서도 항등식이 성립한다
    - 두 함수가 설정 레지스트리를 읽지 않는다 — 세율·절사가 인자다
    - 표시 문구(「서버 계산」·「역산」·「차이」)가 이 모듈에 없다 — 화면이 만든다
  </acceptance_criteria>
  <done>합계에서 공급가를 역산해도, 금액을 나눠도, 절사 이후 합이 정확히 원래 값과 같다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `applyTaxRule()` — 코드표의 규칙 정의와 설정의 세율을 각자의 정본에서 읽는다</name>
  <files>domain/money/index.ts, repositories/code-tables.ts, test/unit/money/tax-rule.test.ts</files>
  <read_first>
    - `domain/code-tables/tax-rule.ts` 전문(약 55행) — `TAX_RULE_KIND_VALUES = ["none","vat_surcharge","withholding","company_borne"]`(ROADMAP 기준 5의 「세금 규칙 4종」과 정확히 일치) · `TAX_ROUNDING_METHOD_VALUES` · `TAX_RULE_BASIS_DATE_VALUES` 네 값 · `taxRuleSchema`가 `ruleKind !== "none"`일 때 절사 단위·방식·최소 징수액·기준일 종류를 전부 요구한다. **머리 주석이 「세율 값 자체는 이 스키마에 담지 않는다」를 이유와 함께 적어 뒀다**
    - `domain/seed/index.ts` 38–76행 `EVIDENCE_TYPE_CODES` — 증빙 종류 일곱과 각자의 `taxRule` 기본값. `tax_invoice`가 `{ ruleKind: "vat_surcharge", roundingUnit: 1, roundingMethod: "round", minWithholdingAmount: 0, basisDate: "evidence_date" }`
    - `domain/settings/registry.ts` 65–89행 `getSettingValue<T>(def, opts?, deps?)` 시그니처 + 78–85행 — 이력형 키에서 `opts.asOf`를 쓰고 **`SYSTEM_VIEWER`로 권한 판정 없이 읽는다**(계산 입력은 domain 전역에서 자유롭게 읽는다). 주석이 「`opts.asOf`는 이력형 키에서만 쓰이고 「어느 날짜를 넘길지」는 호출자의 책임」이라고 적어 뒀다
    - `domain/settings/keys.ts`의 세율 키 11종 전부 — `readBy: { phase: "4" }`가 붙은 키가 이 함수의 입력이다. 이 태스크가 그 표시를 **소비**하므로 `test/unit/settings/registry-coverage.test.ts`가 그 키들을 더 이상 「등록만 되고 안 읽히는 키」로 세지 않는다
    - `repositories/code-tables.ts` 전체 export 목록(8·12·30·48·57·64·78·87·94행) — `findCodeItemById(id)`만 있고 **table_key + value로 찾는 함수가 없다**(실측). `listCodeItems`는 scope 기반 목록만 반환한다
    - `repositories/code-tables.ts` `findCodeItemById`의 정확한 형태 — 새 함수가 `viewer` 첫 인자 규약(`eslint/rules/repository-viewer-param.mjs`)과 `db.select().from(codeItems).where(...).limit(1)` 형태를 그대로 따른다
    - `db/schema/code-items.ts`(또는 그에 해당하는 파일) — `tableKey`·`value`·`taxRule`·`active`·`archivedAt` 컬럼 이름 실측. 조회는 **비활성·보관된 항목을 제외**해야 하는지 판단한다: 세금 계산은 과거 문서에도 적용되므로 보관된 항목도 찾아야 한다 — 그 판단과 이유를 주석에 남긴다
    - `.planning/ROADMAP.md` Phase 4 기준 5의 세금 규칙 문구 — 「없음 / 부가세 가산 / 원천징수 수령자 부담 / 원천징수 회사 대납」. `withholding`이 수령자 부담이고 `company_borne`이 회사 대납이다
    - `04-RESEARCH.md` Pattern 5 전문(429–480행) — 이중 조회의 근거와 「FX·통화 키는 등록 0건」 실측
  </read_first>
  <behavior>
    - `ruleKind: "none"` → 세액 0, 지급액 = 원금, 절사 인자를 요구하지 않는다
    - `ruleKind: "vat_surcharge"` → 부가세 = `round(공급가 × tax.vat.rate)`, 합계 = 공급가 + 부가세. 지급액은 합계다
    - `ruleKind: "withholding"`(수령자 부담) → 원천징수액 = `round(공급가 × 해당 세율, 절사 단위·방식)`, 지급액 = 공급가 − 원천징수액. 회사 비용은 공급가다
    - `ruleKind: "company_borne"`(회사 대납) → 세액은 회사가 부담하므로 지급액 = 공급가, 회사 비용 = 공급가 + 세액. `tax.company_borne.method`가 `gross_up`이면 지급액을 그대로 두고 세액을 총액 환산으로 계산한다
    - 원천징수 세액이 `tax.rounding.min_withholding` 미만이면 0이다(최소 징수액)
    - 기타소득 면제 기준(`tax.withholding.other_income.exempt_threshold`, 기본 125000) 미만 지급액은 원천징수 0이다
    - 기준일 종류(`basisDate`)에 대응하는 실제 날짜를 **호출자가 넘기고**, 그 날짜가 이력형 세율 조회의 `asOf`가 된다 — 같은 규칙이 기준일에 따라 다른 세율을 쓴다
    - 등록되지 않은 증빙 종류 문자열이면 던진다 — 조용히 `none`으로 떨어지지 않는다
  </behavior>
  <action>
`repositories/code-tables.ts`에 `findCodeItemByTableKeyAndValue(viewer, tableKey, value)`를 더한다. `findCodeItemById`와 같은 형태(`db.select().from(codeItems).where(and(eq(tableKey), eq(value))).limit(1)`)이고 첫 인자는 `viewer`다. **보관·비활성 필터를 걸지 않는다** — 과거 문서의 세금 계산이 그때 쓰던 증빙 종류 규칙을 계속 찾아야 하기 때문이다. 그 이유를 함수 위 주석 한 문장으로 남긴다(04-01의 `findVendorById` 건이 보여 준 것처럼, 필터는 호출처마다 판단이 다르다).

`test/unit/money/tax-rule.test.ts`를 **먼저** 만든다(RED 확인). `applyTaxRule`에 `deps`로 코드표 조회와 설정 조회를 주입해 DB 없이 돌린다(`domain/archive/index.ts`의 `deps?.recordAction ?? defaultRecordAction` 주입 패턴이 이 저장소의 관례다). 테스트 표는 **규칙 4종 × 절사 단위 2 × 절사 방식 3**을 덮고, 여기에 최소 징수액 경계·면제 기준 경계·기준일에 따른 세율 변화(이력형 두 행) 케이스를 더한다. ROADMAP 기준 5가 「규칙 4종 × 절사 표 기반 단위 테스트가 있다」를 문자로 요구하므로 이 표가 그 요구의 이행이다.

`domain/money/index.ts`에 `applyTaxRule`을 더한다:
- 시그니처는 `applyTaxRule(input: { supply: Money<"krw">; evidenceTypeValue: string; basisDateValue: Date }, deps?: Partial<ApplyTaxRuleDeps>): Promise<{ ruleKind: TaxRuleKind; taxAmount: Money<"krw">; payableAmount: Money<"krw">; companyCost: Money<"krw"> }>` — 네 값을 돌려주는 이유: 지출결의(Phase 5)는 지급액을, 손익(Phase 9)은 회사 비용을 쓴다. 지금 한쪽만 돌려주면 그 페이즈가 같은 계산을 다시 한다
- `export type ApplyTaxRuleDeps = { findCodeItem: typeof findCodeItemByTableKeyAndValue; getSetting: typeof getSettingValue }` — 주입 가능한 두 의존성
- 절사 단위·방식·최소 징수액은 **코드표 항목의 `taxRule`**에서 읽고, 세율은 **이력형 설정 키**에서 `{ asOf: input.basisDateValue }`로 읽는다. 어느 값이 어느 정본에서 오는지를 표 형태 주석으로 남긴다 — 이 함수의 유일한 복잡성이 그 이중 조회이고 04-RESEARCH Pattern 5가 그 이유를 적었다
- `taxRuleSchema.parse`로 코드표 JSONB를 검증한 뒤 쓴다 — 관리자가 화면에서 고칠 수 있는 값이라 신뢰하지 않는다
- 원천징수의 세율 선택(기타소득 `0.088` vs 사업소득 `0.033`)이 어느 입력으로 갈리는지 실측으로 확인한다: `domain/seed/index.ts`의 증빙 종류 일곱 중 어느 값이 기타소득이고 어느 것이 사업소득인지 시드 블록을 읽어 매핑을 코드에 고정하고, 매핑을 코드표 `value` 문자열로 하드코딩하지 않을 방법이 없으면 그 사실을 주석에 남긴다(관리자가 `value`를 바꿀 수 있는지는 MAST-04 범위이고 이 페이즈가 결정하지 않는다)
- `TAX_COMPANY_BORNE_METHOD`가 `gross_up`인 경로를 구현한다 — 총액 환산은 `지급액 / (1 - 세율)`이고 반올림은 `round()` 한 곳만 거친다
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/money</automated>
    <fails_when>종료 코드가 0이 아니다. 기준일 케이스가 깨지면 `asOf`를 이력형 조회에 넘기지 않은 것이다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 이 태스크가 `readBy: { phase: "4" }` 키들을 실제로 읽기 시작하므로 이 테스트의 console.info 목록이 줄어야 한다 — 늘어나면 키를 안 읽고 상수를 하드코딩한 것이다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다 — `repository-viewer-param` 규칙이 새 리포지토리 함수의 첫 인자를 검사한다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 규칙 4종 × 절사 단위 2 × 절사 방식 3 조합이 단위 테스트 표로 덮였다(ROADMAP 기준 5의 문자 요구)
    - 최소 징수액 경계와 기타소득 면제 기준 경계가 각각 케이스로 있다
    - 같은 규칙이 기준일에 따라 다른 세율을 쓰는 케이스가 있다(이력형 두 행)
    - 세율이 코드표 JSONB에 없고 설정 키에서만 온다
    - 절사 단위·방식·최소 징수액이 코드표 항목에서만 온다
    - 등록되지 않은 증빙 종류 문자열이 던져진다
    - `findCodeItemByTableKeyAndValue`의 첫 인자가 `viewer`이고 보관·비활성 필터가 없으며 그 이유가 주석에 있다
    - `registry-coverage` 테스트의 `readBy` 예외 목록이 이 태스크 뒤에 줄었다
  </acceptance_criteria>
  <done>세금 규칙 네 종류가 코드표의 규칙 정의와 이력형 설정의 세율을 각자의 정본에서 읽고, 기준일에 유효했던 세율로 계산하며, 그 표 전체가 단위 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 계산값 → 저장 | PROJ-02가 「브라우저 계산값은 저장하지 않는다」를 요구한다. 이 모듈이 서버의 유일한 계산 지점이고, 클라이언트가 보낸 금액 파생값은 04-09가 전부 폐기한다 |
| 관리자 편집 가능한 코드표 → 세금 계산 | 증빙 종류의 `tax_rule` JSONB는 관리자가 화면에서 바꿀 수 있다. 그 값을 검증 없이 계산에 넣으면 잘못된 절사·최소 징수액이 조용히 적용된다 |
| 린트 경계 → 산술 위치 | `money-boundary`는 타입 문자열 매칭이다. 이름·캐스팅으로 우회할 수 있고 그 우회는 타입 검사에 걸리지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | `Money` 타입 이름이 린트 정규식을 벗어남 | high | mitigate | 이름을 단독 토큰 `Money`로 고정하고, **실제 모듈을 import하는 픽스처**로 규칙이 반응함을 규칙 자신의 테스트에서 단언한다(A2 가정 검증). `grep -rn "Money[A-Za-z0-9_]"`가 두 번째 그물 |
| T-04-11 | Tampering | `as Money` 캐스팅으로 원시 숫자 산술 후 브랜딩해 경계를 우회 | high | mitigate | 브랜딩 생성자 `money()`를 유일한 입구로 두고 정수 검사를 그 안에 넣는다. 04-09·04-12의 코드 리뷰 체크리스트에 캐스팅 위치 감사를 넣는다(04-RESEARCH § Anti-Patterns가 지목) |
| T-04-12 | Tampering | 관리자가 편집한 `tax_rule` JSONB가 검증 없이 계산에 들어감 | medium | mitigate | `taxRuleSchema.parse`를 계산 전에 통과시킨다. 스키마가 `ruleKind !== "none"`일 때 네 필드를 이미 요구한다 |
| T-04-13 | Tampering | 기준일을 넘기지 않아 항상 오늘 세율로 과거 문서를 계산 | medium | mitigate | `basisDateValue`를 필수 인자로 두어 호출자가 생략할 수 없게 하고, 이력형 두 행 케이스를 단위 테스트에 둔다 |
| T-04-84 | Tampering | 공급가 역산이 부동소수 비율 나눗셈이라 공급가가 1원 낮고 부가세가 1원 높다 — 항등식이 성립하므로 어떤 게이트도 잡지 못한다 | high | mitigate | 세율을 `rateToMinor`로 최소단위 정수화하는 지점을 하나 두고 역산을 정수 곱셈·나눗셈으로만 한다. 그물 셋: ① 12,400,000 명시 기대값 ② 11,000의 배수 표(실측 59.7% 어긋남 구간) ③ 본문에 부동소수 세율 인자가 남지 않았는지 보는 소스 게이트. 항등식 속성 테스트는 이 결함을 **잡지 못한다**는 사실을 테스트 파일 주석에 적는다 |
| T-04-14 | Information Disclosure | 세금 계산이 `SYSTEM_VIEWER`로 설정·코드표를 읽어 권한 판정을 건너뜀 | low | accept | 계산 입력(세율·절사)은 정보 노출 항목이 아니고, `domain/settings/registry.ts:78-85`가 같은 이유로 이미 그 경로를 쓴다. 노출 판정은 결과 DTO에서 `project()`가 한다(04-09) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). 속성 테스트도 새 라이브러리 없이 결정적 루프로 쓴다. `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` 통과. 통합·E2E는 이 플랜이 DB를 건드리지 않아 회귀 확인 목적으로만 돌린다
- `grep -rn "Money[A-Za-z0-9_]" domain/ repositories/ app/ lib/ scripts/` 결과가 비어 있다
- `test/unit/settings/registry-coverage.test.ts`의 `readBy` 예외 목록이 줄었다
- 커밋이 셋이다 — 타입·반올림 / 역산·분할 / 세금 규칙
- `node -e` 게이트(`grossFromTotal`이 정수 세율 스케일을 쓴다)가 초록이다
</verification>

<success_criteria>
- 금액 정본이 하나이고 그 밖의 산술이 린트로 막힌다 — 그 강제가 실제로 작동함이 증명됐다(D-80)
- 절사 이후에도 합계 항등식이 성립하고, **역산이 정수 산술이라 12,400,000이 정확히 나온다**(B2)
- 절사가 절대값 기준 대칭이라 `round(x) + round(-x) === 0`이다(D-84)
- 세금 규칙 4종이 두 정본에서 읽고 기준일을 존중한다
- ROADMAP 기준 5가 문자로 요구한 「규칙 4종 × 절사 표 기반 단위 테스트」가 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 모듈 | `domain/money/index.ts` — `Money` · `MoneyScale` · `money` · `RATE_ONE` · `rateToMinor` · `round` · `toKrw` · `grossFromTotal` · `splitWithRemainder` · `applyTaxRule` · `ApplyTaxRuleDeps` |
| 새 테스트 | `test/unit/money/index.test.ts` · `test/unit/money/tax-rule.test.ts` · `test/unit/eslint-rules/fixtures/money-real-brand.ts` |
| 변경된 테스트 | `test/unit/eslint-rules/money-boundary.test.ts`(실제 타입 invalid 케이스 추가) |
| 변경된 리포지토리 | `repositories/code-tables.ts`(`findCodeItemByTableKeyAndValue` 추가) |
| 새 화면 · 새 표 · 새 마이그레이션 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-03-SUMMARY.md` when done
</output>
