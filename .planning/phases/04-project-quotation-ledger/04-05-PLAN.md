---
phase: 04-project-quotation-ledger
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - db/schema/projects.ts
  - db/schema/quotes.ts
  - db/schema/quote-lines.ts
  - db/schema/exchange-rates.ts
  - db/schema/reserve-ledger.ts
  - db/schema/index.ts
  - db/migrations/0009_projects_quotes_reserve.sql
  - domain/seed/index.ts
  - docs/ARCHITECTURE.md
  - test/unit/source-column-coverage.test.ts
  - test/integration/project-status-check.test.ts
  - test/integration/reserve-balance-constraint.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-07, RSV-01, FX-01]

estimate:
  tokens: 86000
  raw_tokens: 86000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "프로젝트 상태 값이 DB 제약으로 네 개에 묶여 있다 — 애플리케이션을 우회한 직접 쓰기도 다섯 번째 값을 넣지 못한다(D-63)"
    - "상태 코드표가 그 네 값의 한글 라벨만 담고, 네 항목은 비활성화되지 않는다 — 관리자가 「완료(정산)」을 꺼서 완료 처리를 막는 경로가 없다"
    - "`pnpm db:seed`를 두 번 돌려도 결과가 같다 — 재시드가 멱등이다"
    - "모든 금액 컬럼이 최소단위 정수이고 드라이버가 문자열이 아니라 숫자를 돌려준다"
    - "연결 문서가 있는 견적 줄은 DB가 삭제를 거부한다 — FK가 RESTRICT다(PROJ-02)"
    - "리저브 잔액이 음수가 되는 쓰기를 DB가 거부한다(RSV-01)"
    - "이전 대상 업무 표마다 출처 컬럼 둘이 있고, 새 표가 그것을 빠뜨리면 테스트가 잡는다(D-58)"
    - "목록·검색이 쓰는 인덱스 목록과 `document_counters` 컬럼 매핑이 `docs/ARCHITECTURE.md`에 있다(ROADMAP 기준 1 · D-49)"
    - "요청 본문 1MB 한도가 프레임워크 기본값으로 충족된다는 사실과 근거가 문서에 남고, 그 한도를 올리는 설정이 들어오면 검사가 잡는다(ROADMAP 기준 1 · D-79)"
  prohibitions:
    - statement: "`CREATE INDEX CONCURRENTLY`를 써서는 안 된다. `.squawk.toml`이 `assume_in_transaction = true`이고 drizzle `migrate()`가 파일 전체를 트랜잭션으로 감싸므로 트랜잭션 안에서는 `CONCURRENTLY`가 **물리적으로 불가능**하다. `require-concurrent-index-creation`이 예외 목록에 있는 이유가 그것이지 「동시 인덱스를 써도 된다」가 아니다"
      status: resolved
      verification: automated
    - statement: "`bigint` 금액 컬럼에 `{ mode: \"number\" }`를 빠뜨려서는 안 된다. node-postgres가 정밀도 보존을 위해 `bigint`를 JS 문자열로 돌려주므로 금액 산술이 문자열 연결이 된다. 선례는 `db/schema/action-log.ts:19`·`db/schema/auth.ts:85`다"
      status: resolved
      verification: automated
    - statement: "마이그레이션 SQL을 손으로 쓰고 `SET LOCAL lock_timeout` 프리앰블을 수기로 옮겨 적어서는 안 된다. `pnpm db:generate`가 자동 삽입한다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/schema/projects.ts"
      provides: "프로젝트 원장 — 번호·클라이언트·담당 PM·팀·기간·상태(CHECK 4값)·매출 칸·custom_fields·보관·출처"
      exports: ["projects"]
      min_lines: 45
    - path: "db/schema/quote-lines.ts"
      provides: "견적 줄 — 차수 소속(D-55) · 견적가·실행가·차익 · 금액 공용 컬럼 묶음 · 취소 상태 · custom_fields"
      exports: ["quoteLines"]
      min_lines: 45
    - path: "db/schema/exchange-rates.ts"
      provides: "통화별 환율 이력 — (통화, 환율, 기록 시각, 기록한 사람) 행을 쌓고 기본값은 최신 행(D-54)"
      exports: ["exchangeRates"]
      min_lines: 20
    - path: "db/schema/reserve-ledger.ts"
      provides: "클라이언트별 리저브 대장 — 입금·출금·잔액 · 프로젝트 연결(선택) · 금액 공용 컬럼 묶음 · 음수 잔액 DB 제약"
      exports: ["reserveLedger"]
      min_lines: 30
    - path: "test/unit/source-column-coverage.test.ts"
      provides: "이전 대상 업무 표가 `source`·`source_id`를 빠뜨리면 잡는 메타 테스트(D-58). `registry-coverage`·`leak-scan-coverage`가 형태의 선례"
      min_lines: 40
    - path: "db/migrations/0009_projects_quotes_reserve.sql"
      provides: "표 다섯 + 제약 + 인덱스. `pnpm db:generate`로 생성하고 CHECK 제약만 수기로 덧붙인다"
  key_links:
    - from: "db/schema/projects.ts"
      to: "domain/rules/gate.ts"
      via: "CHECK 제약의 네 값이 게이트가 export하는 `ProjectStatus` 유니언과 같은 집합이어야 한다"
      pattern: "status"
    - from: "domain/seed/index.ts"
      to: "db/schema/projects.ts"
      via: "코드표는 CHECK 제약이 허용하는 네 값의 라벨만 시드한다 — 값 집합의 정본은 컬럼이다"
      pattern: "PROJECT_STATUS_CODES"
    - from: "db/schema/quote-lines.ts"
      to: "db/schema/quotes.ts"
      via: "줄이 차수에 속한다(D-55) — 이전 차수는 읽기 전용으로 남고 새 차수는 줄 전부의 복사본이다"
      pattern: "quoteId"
---

<objective>
**As a** 기획 PM, **I want to** 프로젝트·견적 차수·견적 줄·환율·리저브가 담길 표가 제약까지 갖춘 채로 서 있기를 원한다, **so that** 그 위에 올라갈 도메인·화면이 「이 값이 들어올 수 있나」를 화면마다 다시 판단하지 않는다.

이 페이즈의 **표 다섯과 마이그레이션 하나**를 세운다. 마이그레이션 번호가 하나라서 이 플랜이 이 페이즈에서 `db/migrations/`를 건드리는 **유일한 플랜**이다 — 같은 웨이브의 다른 플랜이 `pnpm db:generate`를 돌리면 파일 번호가 충돌한다.

Purpose: ROADMAP Phase 4 기준 1·2·4·6이 요구하는 구조 제약(번호 UNIQUE · 상태 고정 · FK RESTRICT · 음수 잔액 금지 · 음수 허용/금지의 구분)은 전부 **DB가 정본**이어야 한다. 애플리케이션 판정만 있으면 스크립트·직접 쓰기·다음 페이즈의 새 경로가 그것을 우회한다.

Output: 스키마 파일 다섯 + 배럴 갱신, 마이그레이션 하나, 상태 코드표 재시드, 출처 컬럼 커버리지 테스트, 제약을 증명하는 통합 테스트 둘, `docs/ARCHITECTURE.md` 표 둘(인덱스 목록 + D-49 컬럼 매핑).

**`project_status` 코드표 충돌을 이 플랜이 닫는다.** Phase 3이 `planning`·`in_progress`·`on_hold`·`done`·`cancelled` 다섯을 시드했고(`domain/seed/index.ts:19-25` 실측) D-41의 넷과 겹치는 것은 `in_progress` 하나뿐이다. D-63이 고른 해법은 **값 집합의 정본을 컬럼의 CHECK 제약에 두고 코드표는 라벨만 공급**하는 것이다 — `docs/inputs/README.md`가 40행에서 상태 **이름**을 마스터 데이터로, 42행에서 상태 **전이**를 「코드 수정 필요」 층으로 이미 갈라 놓았고, 두 줄을 모두 지키는 유일한 형태다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-RESEARCH.md
@.planning/phases/04-project-quotation-ledger/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `projects` 표 + 상태 CHECK 제약 + 코드표 재시드 — 한 표를 끝까지 세운다</name>
  <files>db/schema/projects.ts, db/schema/index.ts, db/migrations/0009_projects_quotes_reserve.sql, domain/seed/index.ts, test/integration/project-status-check.test.ts</files>
  <read_first>
    - `db/schema/vendors.ts` 전문(약 33행) — 이 저장소의 업무 표 규약 전체가 한 파일에 있다: `uuid("id").primaryKey().defaultRandom()` · `customFields: jsonb(...).notNull().default({})` · `archivedAt`/`archivedBy` · `createdAt`/`updatedAt` `defaultNow()` · `(table) => [index(...), index(...).using("gin", ...)]` 형태. **새 표 다섯이 전부 이 모양을 따른다**
    - `db/schema/index.ts` — 배럴. 새 표를 여기 붙여야 `@/db/schema`에서 import된다
    - `db/schema/action-log.ts` 19행 · `db/schema/auth.ts` 85행 — `bigint("…", { mode: "number" })` 선례 둘. 금액 컬럼 전부가 이 형태다
    - `db/migrations/0000_init.sql` 56–58행 — `SET LOCAL lock_timeout = '1s'` / `statement_timeout = '5s'` 프리앰블. **drizzle-kit이 자동 삽입한다 — 수기로 옮겨 적지 않는다**
    - `db/migrations/0007_vendors_crypto_conventions.sql` 전문 — 가장 최근의 업무 표 마이그레이션. CHECK 제약이나 수기 추가가 있으면 그 형태를 따른다
    - `.squawk.toml` 전문 — `assume_in_transaction = true`와 예외 규칙 목록. `adding-required-field`가 **기존 표에 NOT NULL 컬럼을 더할 때** 걸린다(신규 `CREATE TABLE`은 해당 없음). `pnpm lint:sql`이 이 설정으로 돈다
    - `domain/seed/index.ts` **19–25행** `PROJECT_STATUS_CODES` 전문 — 교체 대상. 그리고 **그 배열을 순회해 `seedCodeItem`을 부르는 루프**(같은 파일 안, 행 번호를 직접 찾는다 — 패턴 매퍼가 이 루프를 열지 못했다)
    - `repositories/code-tables.ts` 94행 `seedCodeItem`의 시그니처 — 멱등 시드의 형태. 재시드가 기존 다섯 행을 어떻게 처리해야 하는지(삭제·보관·무시)를 이 함수가 무엇을 할 수 있는지에서 판단한다
    - `repositories/code-tables.ts` 48행 `setCodeItemActive` — 네 항목의 비활성화를 막으려면 이 경로에 판정이 필요한지, 아니면 도메인 층(04-09)에 두어야 하는지 판단한다. **리포지토리는 판정하지 않는 것이 이 저장소의 계층 규약**이므로 기본은 도메인이다
    - `test/integration/mast-04-code-item-label.test.ts` — 코드표 관련 통합 테스트의 형태 선례
    - `test/integration/setup.ts` — 통합 테스트가 매 케이스 DB를 어떻게 준비하는지
    - `04-CONTEXT.md` D-63 전문 — 「`projects.status`는 CHECK 제약이 붙은 컬럼이고 값 네 개가 고정이다 … 네 항목은 비활성화되지 않게 막는다. 관리자는 「진행」을 「실행」으로 바꿀 수 있지만 상태를 늘리거나 없앨 수는 없다」
    - `.planning/ROADMAP.md` Phase 4 기준 1 — 「문서 표의 `UNIQUE(format_key, number)`가 중복을 막고」. 프로젝트 표의 번호 컬럼에 이 제약이 필요하다
    - `docs/inputs/phase-04-project-quote.md` §9 — PROJ-01의 필수 칸 목록(클라이언트 · 프로젝트명 · 담당 PM · 팀 · 시작일 · 종료일 · 상태, 사전 견적은 선택)
    - `04-CONTEXT.md` D-58 — `source NOT NULL DEFAULT 'demo'` + `source_id`. 이 페이즈부터 전환 전 입력은 전부 `demo`다(Eng OV-1)
    - `04-CONTEXT.md` **D-78 전문** — 저장 문자열 네 값이 **이미 확정됐다**: `pitching`(수주중) · `in_progress`(진행) · `settled`(완료(정산)) · `lost`(미수주). 04-04가 같은 넷을 `domain/rules/gate.ts`의 `ProjectStatus` 유니언에 쓴다. **어느 쪽도 고르지 않는다 — 양쪽이 D-78을 받아 적는다.** 04-04는 같은 웨이브라 그 파일이 이 시점에 존재하지 않을 수 있고, 그래서 **다른 플랜·다른 파일을 읽어 값을 맞추는 경로를 두지 않는다**. 값의 정본은 D-78 하나다. 두 산출물을 서로 대조하는 검사는 04-09 Task 1이 갖는다(그 플랜만 둘 다 볼 수 있다)
  </read_first>
  <action>
**상태 네 값의 저장 문자열은 D-78이 이미 확정했다 — 고르지 말고 받아 적는다.** 저장 값과 한글 라벨의 대응은 `pitching` 수주중 · `in_progress` 진행 · `settled` 완료(정산) · `lost` 미수주 넷이고, 이 넷이 전부다. 그 대응표를 `db/schema/projects.ts` 머리 주석과 `domain/seed/index.ts`의 새 배열 주석 **양쪽**에 남기고, 주석에 D-78을 근거로 인용한다. 04-04가 `domain/rules/gate.ts`의 `ProjectStatus` 유니언에 같은 넷을 쓴다 — **두 곳이 갈라지면 CHECK 제약이 도메인의 모든 상태 전이를 거부해 PROJ-04가 전멸하므로**, 이 값들을 이 태스크에서 재검토하지 않는다.

`db/schema/projects.ts`를 만든다. `vendors.ts`의 규약을 그대로 따르고 여기에 더한다:
- 번호 컬럼(`number`, text, `notNull`) + **`UNIQUE`**. ROADMAP 기준 1이 요구하는 이중 방어선이다. 프로젝트 번호는 재사용하지 않는다(`docs/inputs` §5)
- `status` text `notNull` + **CHECK 제약**. 허용 목록은 D-78의 넷 그대로다 — `CHECK (status IN ('pitching', 'in_progress', 'settled', 'lost'))`. drizzle의 `check()` 헬퍼가 이 버전에서 쓸 수 있으면 스키마에 선언하고, 없으면 마이그레이션 SQL에 `ALTER TABLE … ADD CONSTRAINT … CHECK (…)`를 수기로 덧붙인다. 어느 쪽인지 실측으로 확인하고 고른 방법을 주석에 남긴다. **목록에 다섯 번째 값을 넣지 않는다**
- PROJ-01 필수 칸: 클라이언트 · 프로젝트명 · 담당 PM · 팀 · 시작일 · 종료일. 인트라넷에 시작일이 없다는 사실(`fone_project`에 `project_edate` 하나뿐)은 **이전 시점의 문제**이고 새 표는 PROJ-01대로 `notNull`로 둔다 — 04-06의 transform이 유도하거나 기본값을 준다
- 사전 견적 금액(선택) — `bigint("…", { mode: "number" })`
- PROJ-03 매출 칸 다섯: 계약 금액 · 세금계산서 발행일 · 발행액 · 입금일 · 입금액. 금액 셋은 전부 `bigint({mode:"number"})`이고 **음수를 허용한다**(EXP-14 환불·할인). 지출결의·구매 요청의 양수 제약은 그 표가 생기는 Phase 5·6의 몫임을 주석에 적는다
- `customFields` jsonb + GIN 인덱스(Phase 3 규약)
- `source` text `notNull` `.default("demo")` + `sourceId` text(nullable) (D-58)
- 목록·검색 인덱스: 상태 · 팀 · 담당 PM · 번호 · 기간 — **ROADMAP 기준 1이 명시한 목록·검색 경로 다섯을 이 태스크가 전부 덮는다.** `db/migrations/`를 건드리는 플랜은 이 하나이므로(`04-VALIDATION.md:99`) 04-11은 인덱스를 더할 수 없다. 04-11이 실측 후 부족분을 발견하면 SUMMARY에 올려 후속 처리로 넘긴다는 것을 주석에 남긴다

`db/schema/index.ts` 배럴에 `projects`를 더한다. `pnpm db:generate`로 마이그레이션을 만든다(파일 이름은 drizzle-kit이 정하고, 이 플랜의 `files_modified`에 적은 이름과 다르면 실제 이름을 따른다). **프리앰블을 수기로 옮기지 않는다.** CHECK 제약을 스키마 선언으로 표현할 수 없었다면 그 문장만 생성된 파일에 덧붙인다.

`domain/seed/index.ts`의 `PROJECT_STATUS_CODES`를 네 값으로 **교체**한다. `value`·`label` 쌍은 D-78·D-41이 확정한 넷 그대로다 — `pitching`/수주중 · `in_progress`/진행 · `settled`/완료(정산) · `lost`/미수주. `sortOrder`는 그 순서(수주중 → 진행 → 완료(정산) → 미수주)이고, 04-11의 목록 그룹 머리글 순서가 이 값을 읽는다. 그 배열을 순회하는 `seedCodeItem` 호출부를 열어 네 값 기준으로 맞춘다. **기존 다섯 행을 어떻게 처리할지**를 결정한다: 겹치지 않는 넷(`planning`·`on_hold`·`done`·`cancelled`)을 마이그레이션에서 삭제할지, 보관 처리할지, 그대로 둘지. 판단 기준은 「그 값을 참조하는 행이 있는가」다 — 이 페이즈 전에는 `projects` 표 자체가 없었으므로 참조가 0이고, 남겨 두면 코드표 화면에 쓰이지 않는 상태가 넷 보이게 된다. 고른 방법과 이유를 주석과 SUMMARY에 남긴다. 배열 위 주석에 **이 시드는 라벨 전용이고 값 집합의 정본은 `projects.status`의 CHECK 제약**임을 못박는다(D-63) — 게이트 하드코딩과는 별개다.

`test/integration/project-status-check.test.ts`를 만든다: ① 네 값이 각각 삽입된다 ② 다섯 번째 값(예: 기존 `planning`)을 삽입하면 DB가 거부한다 ③ 번호 중복 삽입을 `UNIQUE`가 거부한다 ④ `source` 기본값이 `demo`다 ⑤ **`pnpm db:seed`를 두 번 돌린 뒤 코드표의 상태 항목이 정확히 넷이고 라벨이 D-41의 넷이다**(멱등성 — 기존 verify 명령이다).
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니다. `adding-required-field`가 걸리면 기존 표에 NOT NULL 컬럼을 더한 것이다(이 태스크는 신규 표만 만들어야 한다). `require-concurrent-index-creation` 관련 경고가 나오면 `.squawk.toml` 예외 목록을 다시 읽는다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/project-status-check.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. ②가 통과하지 못하면 CHECK 제약이 마이그레이션에 실제로 들어가지 않은 것이다 — 생성된 SQL 파일에서 `CHECK`를 직접 grep해 확인한다</fails_when>
    <automated>node -e "const fs=require('fs');const f=fs.readdirSync('db/migrations').filter(n=>n.endsWith('.sql')).sort().pop();const s=fs.readFileSync('db/migrations/'+f,'utf8');if(/CONCURRENTLY/i.test(s)){console.error('CONCURRENTLY IN MIGRATION '+f);process.exit(1)}if(!/CHECK/i.test(s)){console.error('NO CHECK CONSTRAINT IN '+f);process.exit(1)}if(!/lock_timeout/.test(s)){console.error('NO lock_timeout PREAMBLE IN '+f);process.exit(1)}console.log('ok '+f)"</automated>
    <fails_when>표준 출력에 ok 대신 `CONCURRENTLY IN MIGRATION …`(금지 항목 위반) · `NO CHECK CONSTRAINT IN …`(D-63 미구현) · `NO lock_timeout PREAMBLE IN …`(drizzle-kit 대신 손으로 썼다) 중 하나가 나오고 종료 코드가 1이다</fails_when>
    <automated>node -e "const fs=require('fs');const want=['pitching','in_progress','settled','lost'].sort().join(',');const f=fs.readdirSync('db/migrations').filter(n=>n.endsWith('.sql')).sort().pop();const sql=fs.readFileSync('db/migrations/'+f,'utf8');const m=sql.match(/CHECK\s*\(\s*\"?status\"?\s+IN\s*\(([^)]*)\)/i);if(!m){console.error('NO status IN LIST IN '+f);process.exit(1)}const got=[...new Set((m[1].match(/'[a-z_]+'/g)||[]).map(x=>x.slice(1,-1)))].sort().join(',');if(got!==want){console.error('MIGRATION CHECK LIST IS ['+got+'] BUT D-78 PINS ['+want+']');process.exit(1)}const seed=fs.readFileSync('domain/seed/index.ts','utf8');const missing=['pitching','in_progress','settled','lost'].filter(v=>!seed.includes(v));if(missing.length){console.error('SEED MISSING STATUS VALUES '+missing.join(','));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `MIGRATION CHECK LIST IS [...] BUT D-78 PINS [...]`(값을 다시 골랐다 — 04-04의 `ProjectStatus`와 갈라지면 Wave 3에서 원인과 떨어진 증상으로 나타난다) · `NO status IN LIST IN …`(CHECK를 IN 목록이 아닌 형태로 썼다) · `SEED MISSING STATUS VALUES …`(재시드가 D-78의 넷을 쓰지 않았다) 중 하나가 나오고 종료 코드가 1이다. 이 검사가 **Wave 2 안에서** D-78 이탈을 잡는 자리다 — 두 소스를 서로 대조하는 검사는 04-09 Task 1에 있다(같은 웨이브의 04-04 산출물은 이 시점에 아직 없다)</fails_when>
    <automated>pnpm db:seed &amp;&amp; pnpm db:seed</automated>
    <fails_when>두 번째 실행의 종료 코드가 0이 아니거나 코드표 상태 항목이 넷을 넘는다 — 재시드가 멱등이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 상태 네 값만 삽입되고 다섯 번째는 DB가 거부한다
    - CHECK 목록과 재시드 값이 D-78의 넷(`pitching` · `in_progress` · `settled` · `lost`)과 **집합으로 같다** — 실행자가 값을 고르지 않았다
    - 번호 컬럼에 `UNIQUE`가 있고 중복 삽입이 거부된다
    - 금액 컬럼이 전부 `bigint({ mode: "number" })`이고 매출 금액 셋이 음수를 허용한다
    - `source`가 `notNull` 기본값 `demo`이고 `sourceId`가 있다
    - `customFields` GIN 인덱스가 있다
    - 코드표 상태 항목이 정확히 넷이고 라벨이 D-41의 한글 넷이다
    - `pnpm db:seed`를 두 번 돌려도 결과가 같다
    - 시드 배열 주석에 「라벨 전용 · 값 집합의 정본은 CHECK 제약」이 적혔다
    - 마이그레이션에 `CONCURRENTLY`가 없고 `lock_timeout` 프리앰블이 drizzle-kit 생성물 그대로다
    - 기존 다섯 행의 처리 방법과 이유가 SUMMARY에 적혔다
  </acceptance_criteria>
  <done>프로젝트 표가 상태 CHECK 제약·번호 UNIQUE·출처 컬럼·custom_fields까지 갖추고 서 있고, 상태 코드표가 그 네 값의 라벨만 멱등하게 시드한다</done>
</task>

<task type="auto">
  <name>Task 2: 나머지 표 넷 — 차수·견적 줄·환율·리저브</name>
  <files>db/schema/quotes.ts, db/schema/quote-lines.ts, db/schema/exchange-rates.ts, db/schema/reserve-ledger.ts, db/schema/index.ts, db/migrations/0009_projects_quotes_reserve.sql, test/integration/reserve-balance-constraint.test.ts</files>
  <read_first>
    - Task 1이 만든 `db/schema/projects.ts` — 이 넷이 따를 같은 규약
    - `04-CONTEXT.md` D-55 전문 — 「견적 줄은 차수에 속하고, 새 차수는 줄 전부의 복사본이다. 이전 차수는 읽기 전용으로 남아 「그때 얼마로 잡았나」가 보존된다 … 지출결의가 붙은 줄은 **FK RESTRICT**로 원래 차수에 그대로 남고, 프로젝트 비용 합계는 차수가 아니라 지출 쪽에서 집계한다. 차수 기준으로 세는 것은 견적가 합계뿐이다」
    - `04-CONTEXT.md` D-51 — 차수는 **정수 컬럼**이고 사전 견적은 별도 값으로 구분한다. 「1차」는 표시 시점 조립(04-04의 `formatQuoteRound`)
    - `04-CONTEXT.md` D-53 — 원화 1원 정수 · 외화 1/100 정수 · 환율 1/10000 정수
    - `04-CONTEXT.md` D-54 전문 — `exchange_rates`가 설정 레지스트리 키가 아닌 이유(설정 쓰기는 `admin.settings` 권한 뒤에 있는데 환율을 적는 사람은 관리자가 아니다). 행 모양은 `(통화, 환율, 기록 시각, 기록한 사람)`이고 기본값은 최신 행
    - `.planning/REQUIREMENTS.md` FX-01(76행) — 「통화·외화 금액·환율·원화 환산액을 **함께 기록한다**」. 이것이 금액 공용 컬럼 묶음이고 `quote_lines`·`reserve_ledger`가 같은 묶음을 쓴다
    - `.planning/REQUIREMENTS.md` RSV-01(74행) — 「입금·출금·잔액을 날짜순으로 기록 … 각 줄은 프로젝트에 연결할 수 있고」. **연결은 선택**이다
    - `docs/inputs/phase-04-project-quote.md` §6 리저브 대장 — 음수 잔액 금지의 출처. 그 절을 열어 잔액이 **저장 컬럼인지 계산값인지**를 확인한다. 저장이면 CHECK 제약이 바로 걸리고, 계산값이면 제약을 어디에 걸지(트리거 vs 도메인) 판단해 이유와 함께 주석에 남긴다
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` 화면 계약 8 — 「합계/잔액: 마지막 행의 잔액을 합계 행에 `잔액 48,000,000`으로 둔다」. 화면이 마지막 행의 잔액을 읽는다는 것은 **행마다 잔액이 있다**는 뜻이다
    - `.planning/REQUIREMENTS.md` PROJ-02(27행) — 「연결 문서가 있는 견적 줄은 보관(삭제)하지 않고 '취소' 상태(견적가 0, 이력 유지)로만 바꾼다」
    - `.planning/ROADMAP.md` Phase 4 기준 4 마지막 문장 — 「지출결의·구매 요청은 음수 없이 양수만, 매출·조정 항목은 환불·할인을 위해 음수를 허용하는 규칙이 **스키마에 있다**」
    - `db/schema/code-tables.ts` — 견적 대·소분류가 참조할 코드표. 견적 줄이 그 항목을 FK로 참조할지 값 문자열로 담을지 판단한다(`vendors.defaultEvidenceType`가 **value 문자열**을 담는 선례가 있다)
    - `.squawk.toml` — FK·CHECK 추가에 걸리는 규칙이 있는지 재확인
  </read_first>
  <action>
`db/schema/quotes.ts` — 견적 차수. 프로젝트 FK · 차수 정수(`round`, nullable 또는 별도 플래그로 사전 견적 구분 — D-51이 「사전 견적은 별도 값으로 구분한다」고만 정했으므로 둘 중 하나를 고르고 이유를 주석에 남긴다) · 고객 승인 표시(`approvedAt`·`approvedBy` 또는 불리언 — 「언제 승인됐나」가 필요한지 판단해 고른다) · `source`/`sourceId` · 보관 · 타임스탬프. `(projectId, round)` UNIQUE로 같은 차수가 둘 생기지 않게 한다.

`db/schema/quote-lines.ts` — 견적 줄. **차수 FK**(D-55 — 프로젝트가 아니라 차수에 속한다) · 대분류·소분류 · 항목명 · 거래처(FK 또는 값 — 위 판단과 같은 결로 고른다) · **금액 공용 컬럼 묶음**(통화 · 외화 금액 `bigint` · 환율 `bigint` · 원화 환산액 `bigint`)을 견적가·실행가 각각에 대해 둘지, 아니면 통화·환율을 줄 단위로 하나만 둘지 판단한다 — §7-3이 「편집은 외화 금액·환율 두 칸에서 하고 원화는 계산값」이라 했고 한 줄에 견적가·실행가가 같은 통화인 것이 자연스러우므로 **줄 단위 통화·환율 + 금액별 외화·원화 컬럼**을 기본으로 고르고 이유를 주석에 남긴다 · 차익(`bigint`, 서버 계산 저장) · 상태(정상 / 견적 외 / 취소 — 견적 외 비용 줄은 견적가 0인 같은 표의 한 종류다) · `customFields` + GIN · `source`/`sourceId` · 보관 · 타임스탬프 · `updatedAt`(D-48 조건부 UPDATE가 이 값을 읽는다).

**FK RESTRICT를 이 표에 거는 것이 아니다** — 지출결의 표가 견적 줄을 참조할 때 그쪽 FK가 RESTRICT여야 한다(Phase 5). 이 페이즈가 할 수 있는 것은 ① 견적 줄의 보관 경로가 연결 문서를 확인하게 두는 것(04-09) ② 그 사실과 Phase 5가 걸 FK의 방향·동작을 `db/schema/quote-lines.ts` 주석에 계약으로 적어 두는 것이다. 주석에 그 계약을 적고, 이 페이즈에서 RESTRICT를 실제로 거는 FK가 있다면(차수 → 프로젝트 등) 어느 것인지 명시한다.

`db/schema/exchange-rates.ts` — `(currency, rateMinor bigint({mode:"number"}), recordedAt, recordedBy)` + id. 최신 행 조회가 빈번하므로 `(currency, recordedAt desc)` 인덱스를 둔다. **설정 레지스트리 키가 아닌 이유**(D-54)를 파일 머리 주석에 두 문장으로 남긴다.

`db/schema/reserve-ledger.ts` — 클라이언트 · 날짜 · 종류(입금/출금) · 금액 공용 컬럼 묶음 · **프로젝트 FK(nullable — 연결은 선택)** · 적요 · 잔액 · `source`/`sourceId` · 보관 · 타임스탬프. **음수 잔액 금지를 DB 제약으로 건다**(`docs/inputs` §6). 잔액이 저장 컬럼이면 `CHECK (balance >= 0)`이 바로 걸리고, 계산값이면 저장 컬럼으로 바꾸는 것이 제약을 DB에 둘 수 있는 유일한 형태다 — UI-SPEC이 「마지막 행의 잔액」을 화면 계약으로 적었으므로 **저장 컬럼**을 고르고 「행마다 잔액을 저장하고 쓰기 시점에 서버가 계산해 채운다」를 주석 계약으로 적는다.

`db/schema/index.ts` 배럴에 넷을 더하고 `pnpm db:generate`로 마이그레이션을 갱신한다(Task 1과 **같은 마이그레이션 파일**이 되도록 Task 1에서 생성한 파일을 지우고 다섯 표를 한 번에 생성하거나, drizzle-kit이 두 번째 파일을 만들면 그대로 둔다 — 어느 쪽이든 `pnpm db:reset:test`가 통과하면 된다. 고른 방법을 SUMMARY에 적는다).

`test/integration/reserve-balance-constraint.test.ts`를 만든다: ① 음수 잔액 쓰기를 DB가 거부한다 ② 잔액 0은 허용된다(§2-4 「0은 `0`」) ③ 프로젝트 연결 없이 줄을 만들 수 있다 ④ 매출 금액 컬럼에 음수를 넣을 수 있다(EXP-14) ⑤ `(projectId, round)` 중복 차수가 거부된다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/reserve-balance-constraint.test.ts test/integration/project-status-check.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. ①이 통과하지 못하면 잔액 CHECK 제약이 마이그레이션에 없다</fails_when>
    <automated>node -e "const fs=require('fs');let bad=[];for(const f of ['projects','quotes','quote-lines','exchange-rates','reserve-ledger']){const s=fs.readFileSync('db/schema/'+f+'.ts','utf8');const lines=s.split('\n').filter(l=>/bigint\(/.test(l)&&!/mode:\s*\"number\"/.test(l));if(lines.length)bad.push(f+': '+lines.length)}if(bad.length){console.error('BIGINT WITHOUT NUMBER MODE '+bad.join(' | '));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `BIGINT WITHOUT NUMBER MODE …`가 나오고 종료 코드가 1이다 — 드라이버가 그 컬럼에서 문자열을 돌려주므로 금액 산술이 문자열 연결이 된다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 견적 줄이 **차수**에 속한다(D-55) — 프로젝트를 직접 참조하지 않는다
    - 차수가 정수 컬럼이고 사전 견적이 별도 값으로 구분되며 `(projectId, round)`가 UNIQUE다
    - 금액 컬럼 전부가 `bigint({ mode: "number" })`다
    - 리저브 잔액 음수가 DB 제약으로 거부되고 0은 허용된다
    - 리저브 줄의 프로젝트 연결이 nullable이다
    - 표 다섯 전부에 `source`·`source_id`·`archivedAt`·`createdAt`/`updatedAt`이 있다
    - `exchange_rates` 파일 머리에 설정 레지스트리가 아닌 이유(D-54)가 적혔다
    - Phase 5가 걸 지출결의 → 견적 줄 FK의 RESTRICT 계약이 `quote-lines.ts` 주석에 적혔다
    - 통화·환율을 줄 단위로 둔 선택의 이유가 주석에 있다
  </acceptance_criteria>
  <done>차수·견적 줄·환율·리저브 네 표가 제약과 인덱스까지 갖추고 서 있고, 그 제약이 통합 테스트로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 출처 컬럼 커버리지 테스트 + 아키텍처 문서 표 둘(인덱스 목록 · D-49 컬럼 매핑)</name>
  <files>test/unit/source-column-coverage.test.ts, docs/ARCHITECTURE.md</files>
  <read_first>
    - `04-CONTEXT.md` D-58 전문 — 「`source`·`source_id` 컬럼을 이전 대상 업무 표마다 둔다. `source NOT NULL DEFAULT 'demo'`이고, **새 표가 빠뜨리면 잡는 테스트를 함께 둔다**. 근거: Phase 8의 `(source, source_id)` upsert 규약이 그 두 컬럼을 요구하고, `registry-coverage`·`leak-scan-coverage` 테스트가 같은 결의 선례다」
    - `test/unit/settings/registry-coverage.test.ts` 전문 — 소스 텍스트를 정규식으로 파싱해 목록을 만들고 위반을 실패 메시지로 내는 형태. **AST 파서를 새로 들이지 않는다**(그 파일 주석이 그 선택의 이유를 적었다)
    - `test/unit/leak-scan-coverage.test.ts` 전문 — 디렉터리 재귀 순회 + 「검출기 자체가 죽어있지 않다」 케이스. 이 테스트가 그 둘을 다 따른다
    - `db/schema/` 전체 파일 목록 — **어느 표가 「이전 대상 업무 표」인지**를 판정할 기준이 필요하다. 인프라·인증·권한 표(`auth.ts`·`login-attempts.ts`·`permissions.ts`·`roles.ts`·`settings.ts`·`document-counters.ts`·`action-log.ts`·`field-definitions.ts`)는 대상이 아니고, 마스터·업무 표(`vendors`·`org`·`corp-cards`·`code-tables` + 이 페이즈의 다섯)가 대상이다. **기존 마스터 표에 이 컬럼이 이미 있는지 실측한다** — 없으면 이 테스트가 즉시 RED가 되므로 허용 목록(명시적 예외)과 그 예외 사유를 테스트 안에 적어야 한다
    - `.planning/ROADMAP.md` Phase 4 기준 1 — 「목록·검색이 쓰는 인덱스 목록이 `docs/ARCHITECTURE.md`에 있으며」와 그 앞의 `(format_key, scope_key, next_no)` 표기. 이 태스크가 **둘 다** 이행한다
    - `db/schema/document-counters.ts` 전문 — `(counter_key, period, value)` 복합 PK와 `period`를 「순번 범위의 구분자」로 정의한 주석. D-49 매핑 표의 근거다. **이 파일을 고치지 않는다**(D-49)
    - `04-CONTEXT.md` D-49 전문 — 컬럼명 유지 결정과 매핑을 `docs/ARCHITECTURE.md`에 기록하라는 요구
    - `04-CONTEXT.md` **D-79 전문** — ROADMAP 기준 1의 「요청 본문 1MB 한도」는 **오늘 Next 16 기본값으로 이미 충족돼 있다**. 근거 둘을 직접 확인한다: `node_modules/next/dist/docs/01-app/02-guides/server-actions.md:83`(「Action requests are capped at 1MB by default」)과 `next.config.ts`에 `serverActions` 설정이 없다는 사실. **코드 변경이 아니라 기록과 되돌림 방지 검사**가 이 태스크의 몫이다
    - `docs/ARCHITECTURE.md` 전문 + `test/unit/docs-limits.test.ts` — **300행 상한**과 고정 토큰 목록. 표 둘을 더할 여유를 먼저 확인한다 — 넘칠 것 같으면 표를 더하지 말고 **다른 내용을 줄이지도 말고** 그 사실과 대안(별도 문서 분리)을 SUMMARY에 올려 사용자 판단을 받는다
  </read_first>
  <behavior>
    - 이 페이즈가 만든 표 다섯이 전부 `source`·`source_id`를 갖는다
    - 대상 표에서 그 컬럼을 지우면 테스트가 실패하고 실패 메시지에 **표 이름**이 나온다
    - 대상이 아닌 인프라 표는 검사되지 않는다 — 그 판정 기준이 테스트 안에 목록으로 있다
    - 검출기가 표를 하나 이상 찾았음을 단언하는 케이스가 있다(정규식이 죽으면 조용히 초록이 된다)
  </behavior>
  <action>
`test/unit/source-column-coverage.test.ts`를 **먼저** 만든다(RED 확인 — 아직 컬럼이 없는 표가 있으면 그 목록이 실패 메시지로 나온다). 구현:
- `db/schema/*.ts`를 읽어 `pgTable("…"` 호출의 표 이름을 정규식으로 뽑는다
- **대상 판정**은 파일 단위 허용 목록이 아니라 **제외 목록**으로 한다 — 새 업무 표가 생겼을 때 목록에 더하는 것을 잊어도 검사 대상이 되게(빠뜨리면 조용히 통과하는 방향이 아니라 시끄럽게 실패하는 방향). 제외 목록에 각 표의 제외 사유를 한 줄씩 적는다(인증·인프라·카운터·로그·권한 등)
- 대상 표마다 `source`와 `source_id` 컬럼 선언이 있는지, `source`에 `.notNull()`과 `.default("demo")`가 있는지 확인하고 위반을 `표이름: 빠진 것` 목록으로 모아 실패 메시지에 담는다
- **기존 마스터 표에 이 컬럼이 없어서 RED가 나면** 두 갈래 중 하나를 고른다: ⓐ 기존 표에도 컬럼을 더한다(이 태스크의 마이그레이션 범위가 커지고 `.squawk.toml`의 `adding-required-field`에 걸릴 수 있다) ⓑ 제외 목록에 「Phase 3 마스터 표 — 이전 대상 여부는 Phase 8이 판정한다」 사유로 넣는다. **ⓑ를 기본으로 고른다** — D-58이 요구한 것은 「새 표가 빠뜨리면 잡는」 것이고 기존 표를 소급 개조하는 것이 아니다. 고른 갈래와 이유를 SUMMARY에 적는다
- 「검출기 자체가 죽어있지 않다」 케이스: 찾은 표가 하나 이상이고, 이 페이즈의 표 다섯이 전부 그 목록에 있다

`docs/ARCHITECTURE.md`에 표 **둘**을 더한다. **웨이브 2에서 그 파일을 고치는 플랜은 이 하나다** — 04-04도 같은 웨이브에서 기록할 내용이 있었으나 파일 충돌을 피해 여기로 모았다. 표의 **구조와 행**은 이 플랜이 소유하고, 04-11(웨이브 4)이 인덱스 목록 표의 「어느 화면·쿼리가 쓰는가」 칸만 실측으로 채운다 — 웨이브가 둘 떨어져 있어 동시 편집이 아니다.

**표 1 — `document_counters` 컬럼 매핑(D-49).** ROADMAP Phase 4 기준 1이 적은 `(format_key, scope_key, next_no)`가 Phase 3이 만든 실제 컬럼 `(counter_key, period, value)`에 각각 대응한다. `db/schema/document-counters.ts`의 주석이 `period`를 「순번 범위의 구분자(연도 등 — 전사 범위는 고정 문자열)」로 정의해 둔 것이 정확히 `scope_key`다. 마이그레이션을 만들지 않는 이유도 한 줄: 이름만 다르고 구조가 같다(CLAUDE.md 3번 — 요청받지 않은 리팩터 금지).

**한 줄 — 요청 본문 1MB 한도(ROADMAP 기준 1 · D-79).** 표 둘과 같은 절에 문장 하나를 남긴다: 서버 액션 요청 본문은 Next 16 **프레임워크 기본값**으로 1MB에서 잘리고, 이 프로젝트는 그 기본값에 기대므로 `next.config.ts`에 `serverActions` 상향 설정을 두지 않는다. 근거 좌표(`node_modules/next/dist/docs/01-app/02-guides/server-actions.md:83`)를 같은 줄에 적는다 — 기준이 충족된 이유가 산출물에 없으면 나중에 아무도 그것이 의도였음을 알 수 없고, 상향 설정을 더하는 변경을 막을 근거도 없다. **`next.config.ts`를 고치지 않는다** — 이 플랜의 `files_modified`에 그 파일이 없다.

**표 2 — 목록·검색 인덱스 목록(ROADMAP 기준 1).** 각 행은 `표 · 인덱스 이름 · 컬럼 · 어느 화면·쿼리가 쓰는가`다. 이 페이즈가 만든 인덱스를 **행으로 전부** 적는다. 사용처 칸은 화면이 아직 없으므로 비워 두거나 예정 쿼리로 적고, **04-11이 그 칸을 실측으로 채운다**(행을 새로 만들지 않는다 — 인덱스를 더할 수 있는 플랜은 이 하나뿐이고, 04-11이 부족분을 발견하면 SUMMARY의 후속 처리 항목이 된다). 그 규약을 표 아래 한 문장으로 남긴다. **300행 상한을 넘기지 않는다** — 넘칠 것 같으면 표를 더하지 말고 그 사실과 대안(별도 문서 분리)을 SUMMARY에 올려 사용자 판단을 받는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/source-column-coverage.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 커버리지 테스트가 실패하면 메시지의 `표이름: 빠진 것` 목록이 남은 작업이다. `docs-limits`가 실패하면 `docs/ARCHITECTURE.md`가 300행을 넘었다</fails_when>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('next.config.ts','utf8');if(/bodySizeLimit/.test(c)){console.error('next.config.ts RAISES THE SERVER ACTION BODY LIMIT — ROADMAP CRITERION 1 RELIES ON THE 1MB FRAMEWORK DEFAULT (D-79)');process.exit(1)}const a=fs.readFileSync('docs/ARCHITECTURE.md','utf8');if(!/1MB/.test(a)){console.error('ARCHITECTURE.md MISSING THE 1MB REQUEST BODY RECORD (D-79)');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `next.config.ts RAISES THE SERVER ACTION BODY LIMIT …`(누군가 한도를 올렸다 — 기준 1이 무너진다) 또는 `ARCHITECTURE.md MISSING THE 1MB REQUEST BODY RECORD …`(기록을 빠뜨렸다)가 나오고 종료 코드가 1이다</fails_when>
    <automated>pnpm test:unit</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm test:integration</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 표·제약이 기존 통합 테스트(특히 `archive`·`leak-scan`)를 깨지 않았음을 확인한다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 페이즈의 표 다섯이 전부 `source`·`source_id`를 갖고 `source`가 `notNull` 기본값 `demo`다
    - 대상 판정이 제외 목록 방식이고 제외마다 사유가 한 줄씩 있다
    - 실패 메시지에 표 이름이 나온다
    - 검출기 살아있음 케이스가 이 페이즈의 표 다섯을 이름으로 확인한다
    - `docs/ARCHITECTURE.md`에 표 둘(D-49 컬럼 매핑 · 인덱스 목록)이 있고 파일이 300행 이하다
    - 같은 문서에 요청 본문 1MB 한도가 프레임워크 기본값으로 충족된다는 기록과 근거 좌표가 한 줄 있다(D-79)
    - `next.config.ts`가 이 플랜 diff에 없고 서버 액션 본문 한도 상향 설정이 그 파일에 없다(D-79)
    - `db/schema/document-counters.ts`가 이 플랜 diff에 없다 — 컬럼명 유지(D-49)
    - 기존 마스터 표 처리 갈래(ⓐ/ⓑ)와 이유가 SUMMARY에 적혔다
  </acceptance_criteria>
  <done>새 업무 표가 출처 컬럼을 빠뜨리면 단위 테스트가 표 이름과 함께 잡고, 인덱스 목록과 카운터 컬럼 매핑이 아키텍처 문서에 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 애플리케이션 우회 쓰기 → 데이터 무결성 | 스크립트·이전 도구·다음 페이즈의 새 경로가 도메인 판정을 거치지 않고 쓴다. 제약이 DB에 없으면 그 경로가 전부 구멍이다 |
| 관리자 편집 가능한 코드표 → 상태 값 집합 | 코드표에 값 집합을 두면 관리자가 항목을 지울 때 상태 자체가 사라진다 |
| 드라이버 타입 변환 → 금액 산술 | node-postgres가 `bigint`를 문자열로 돌려주면 금액이 조용히 문자열 연결이 된다 |
| 이전 데이터 → 업무 데이터 | 전환 전 입력과 이전된 행을 구분하지 못하면 전환일에 무엇을 지울지 알 수 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Tampering | `projects.status`에 정의되지 않은 값이 들어감 | high | mitigate | CHECK 제약으로 DB가 거부한다. 통합 테스트가 다섯 번째 값 삽입 거부를 단언하고, 게이트(04-04)가 export하는 유니언과 같은 집합임을 주석으로 묶는다 |
| T-04-23 | Tampering | 프로젝트 번호 중복 — 채번이 우회되거나 재시도로 같은 번호가 두 행에 들어감 | high | mitigate | 번호 컬럼 `UNIQUE`가 이중 방어선이다(04-04의 행 잠금이 첫 방어선). ROADMAP 기준 1 원문 |
| T-04-24 | Tampering | 리저브 잔액이 음수가 되는 쓰기 | high | mitigate | `CHECK (balance >= 0)` DB 제약. 통합 테스트가 거부를 단언한다 |
| T-04-25 | Denial of Service | 관리자가 코드표에서 「완료(정산)」을 비활성화해 완료 처리가 막힘 | medium | mitigate | 값 집합의 정본이 CHECK 제약이라 코드표 비활성화가 상태 자체를 없애지 못한다. 네 항목 비활성화 금지는 04-09의 도메인 판정이 더한다 |
| T-04-26 | Tampering | 연결 문서가 있는 견적 줄이 삭제되어 지출결의가 고아가 됨 | high | mitigate | Phase 5가 걸 FK의 RESTRICT 계약을 `quote-lines.ts` 주석에 못박고, 이 페이즈의 보관 경로는 04-09가 연결 확인 뒤에만 허용한다 |
| T-04-27 | Information Disclosure | 이전 데이터와 전환 전 입력이 섞여 전환일에 무엇을 지울지 모름 | medium | mitigate | `source NOT NULL DEFAULT 'demo'` + `source_id`, 그리고 새 표가 빠뜨리면 잡는 단위 테스트(D-58) |
| T-04-28 | Denial of Service | 마이그레이션이 운영 DB에서 오래 잠금을 잡음 | medium | mitigate | drizzle-kit이 삽입하는 `SET LOCAL lock_timeout '1s'` / `statement_timeout '5s'` 프리앰블. 신규 `CREATE TABLE`이라 기존 행 재작성이 없다. `pnpm lint:sql`이 squawk 규칙으로 검사한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). `pnpm db:generate`는 이미 설치된 drizzle-kit을 쓴다. `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm lint:sql` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm db:reset:test`가 새 마이그레이션까지 깨끗이 돈다
- `pnpm db:seed && pnpm db:seed` 멱등
- `pnpm test:unit` · `pnpm test:integration` 통과
- 이 페이즈에서 `db/migrations/`를 건드리는 플랜은 이 하나다 — 다른 플랜의 diff에 마이그레이션 파일이 있으면 웨이브 배치가 잘못된 것이다
- 커밋이 셋이다 — 프로젝트 표·재시드 / 나머지 표 넷 / 커버리지 테스트·문서
- 웨이브 종료 시 `/review`를 돌린다
</verification>

<success_criteria>
- 상태 네 값이 DB 제약으로 묶이고 코드표는 라벨만 공급한다(D-63)
- 견적 줄이 차수에 속하고 이전 차수가 읽기 전용으로 보존될 구조다(D-55)
- 금액이 전부 최소단위 정수이고 드라이버가 숫자를 돌려준다(D-53)
- 리저브 음수 잔액과 프로젝트 번호 중복을 DB가 거부한다
- 새 업무 표가 출처 컬럼을 빠뜨리면 테스트가 잡는다(D-58)
- 인덱스 목록과 카운터 컬럼 매핑이 아키텍처 문서에 있다(ROADMAP 기준 1 · D-49)
- 요청 본문 1MB 한도가 충족된 이유가 문서에 있고 상향 설정을 막는 검사가 있다(ROADMAP 기준 1 · D-79)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 표 | `projects` · `quotes` · `quote_lines` · `exchange_rates` · `reserve_ledger` |
| 새 마이그레이션 | `db/migrations/0009_*.sql`(drizzle-kit 생성, CHECK 제약 수기 보강) |
| 새 테스트 | `test/unit/source-column-coverage.test.ts` · `test/integration/project-status-check.test.ts` · `test/integration/reserve-balance-constraint.test.ts` |
| 변경된 시드 | `domain/seed/index.ts` `PROJECT_STATUS_CODES` 4값 교체 |
| 문서 | `docs/ARCHITECTURE.md` 표 둘 — `document_counters` 컬럼 매핑(D-49) · 목록·검색 인덱스 목록(ROADMAP 기준 1) + 요청 본문 1MB 한도 기록 한 줄(D-79) |
| 새 화면 · 새 컴포넌트 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-05-SUMMARY.md` when done
</output>
