---
phase: 04-project-quotation-ledger
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-05"]
files_modified:
  - scripts/migrate/extract.ts
  - scripts/migrate/transform.ts
  - scripts/migrate/parse-dump.ts
  - test/unit/migrate/extract-allowlist.test.ts
  - test/unit/migrate/transform.test.ts
  - test/unit/migrate/transform-schema-parity.test.ts
  - test/unit/migrate/planning-leak.test.ts
  - test/fixtures/migrate/synthetic-dump.sql
  - docs/OPERATIONS.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [PROJ-01, PROJ-02]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "허용목록 밖의 표나 DB를 만나면 추출이 **실패한다** — 개인정보가 담긴 표를 실수로 긁는 경로가 없다(D-57a)"
    - "이 저장소에 커밋되는 픽스처가 전부 합성이다 — 인트라넷에서 나온 값이 익명화된 형태로도 들어오지 않는다(D-57)"
    - "옛 차익을 믿지 않고 다시 계산한다 — 인트라넷의 66줄 불일치가 그대로 옮겨오지 않는다"
    - "자유 텍스트 금액 칸에서 앞 숫자만 취하고, 그 판정이 무엇을 버렸는지가 행마다 남는다"
    - "옛 금액이 공급가인지 합계인지를 행마다 `amount_basis`로 적고 불명은 「계산 불가」로 둔다(ROADMAP 기준 7)"
    - "원천에 없는 값(상태 4종 · 시작일 · 견적 차수)은 유도하거나 기본값을 주고, 어느 쪽인지가 변환 규칙 표에 적혀 있다"
    - "같은 덤프로 다시 돌리면 같은 번호가 나온다 — 옛 id에서 결정적으로 파생한다"
    - "변환 출력의 키가 04-05가 실제로 만든 표의 컬럼 이름 안에 들어 있다 — 「Phase 8에서 적재할 때 모양이 안 맞는다」가 **이 페이즈 안에서** 잡힌다(ROADMAP 기준 7의 「결과가 개발·테스트 픽스처로 쓰인다」)"
    - "이전 번호가 차지할 예약 범위와 Phase 8이 카운터를 그 위로 올린다는 계약이 문서에 있다 — 옛 id 파생 번호와 새 채번이 같은 값에서 부딪히지 않는다(ROADMAP 기준 7)"
    - "실데이터 수동 검증의 결과가 들어가는 `.planning/` 산출물에도 누출 게이트가 걸린다 — 이 저장소는 public이고 D-57이 집계값까지 막았다"
  prohibitions:
    - statement: "인트라넷에서 나온 값을 이 저장소에 커밋해서는 안 된다. 프로젝트명·클라이언트명은 물론 집계값(`quotationSum` 등)도 회사 매출 규모 공개라 제외다. 변환 결과 보고서도 커밋하지 않는다 — 이 저장소는 public이고 사용자가 그것을 의도했다(D-57)"
      status: resolved
      verification: automated
    - statement: "실데이터 검증 결과의 **값**을 `.planning/` 산출물에 옮겨 적어서는 안 된다. `04-VALIDATION.md`의 수동 검증 항목이 「로컬에서 실제 덤프로 돌리고 보고서를 눈으로 본다」인데, 그 결과가 들어가는 `04-06-SUMMARY.md`·`04-VERIFICATION.md`는 **커밋되고 이 저장소는 public이다**(D-57). 프로젝트명·클라이언트명은 물론 합계·건수 같은 집계도 회사 매출 규모 공개라 제외다 — 남길 수 있는 것은 **판정(맞다/틀리다)과 규칙 이름**뿐이고 값은 아니다"
      status: resolved
      verification: automated
    - statement: "허용목록을 DB 단위로 두어서는 안 된다. `PLANT8_INTRANET` **안에도** 개인정보 표가 있다 — `fone_member`(비밀번호·메일·이름) · `tb_admin_member`(비밀번호) · `fone_partners`(전화·평문 계좌번호) · `QUOTATION_PAYMENT`(이름·전화·계좌). 허용목록은 **표 단위**다(D-57a)"
      status: resolved
      verification: automated
    - statement: "`scripts/migrate/`를 `scripts/build-cli.mjs` 번들에 넣어서는 안 된다. 그 번들의 넷(`migrate-runner`·`seed-master`·`account-cli`·`db-bootstrap`)은 배포마다 자동으로 도는 프로비저닝 단계이고, 이것은 운영자가 필요할 때 손으로 한 번 돌리는 관리 작업이다(`scripts/settings-import.ts:6-13`이 같은 구분을 주석으로 적어 뒀다). 적재(load)가 Cloud Run Job을 필요로 하는 시점은 Phase 8이다"
      status: resolved
      verification: automated
  artifacts:
    - path: "scripts/migrate/extract.ts"
      provides: "mysqldump → 표별 JSONL. 허용목록 일곱 표 밖을 만나면 실패한다"
      exports: ["parseArgs", "extract"]
      min_lines: 80
    - path: "scripts/migrate/transform.ts"
      provides: "표별 JSONL → 새 표 구조. 차익 재계산 · `amount_basis` 판정 · 유도값 · 결정적 번호 파생"
      exports: ["parseArgs", "transform"]
      min_lines: 120
    - path: "test/fixtures/migrate/synthetic-dump.sql"
      provides: "합성 mysqldump 조각 — 실데이터를 대신해 파서·변환 규칙을 고정한다. 인트라넷 값 0개"
      min_lines: 40
    - path: "test/unit/migrate/extract-allowlist.test.ts"
      provides: "허용목록 밖 표·DB를 만나면 실패함을 고정하는 단위 테스트(D-57a)"
      min_lines: 40
    - path: "test/unit/migrate/transform-schema-parity.test.ts"
      provides: "변환 출력 키가 `db/schema/*.ts`의 컬럼 이름 집합의 **부분집합**임을 단언하는 메타 테스트. `registry-coverage`·`source-column-coverage`와 같은 형태(정규식 파싱, AST 파서 없음)"
      min_lines: 45
    - path: "test/unit/migrate/planning-leak.test.ts"
      provides: "`04-06-SUMMARY.md`·`04-VERIFICATION.md`에 실데이터 유래 **값**(천 단위 쉼표 수 · 7자리 이상 연속 숫자)이 없음을 단언하는 메타 테스트(D-57 · N5). 표 이름은 대상이 아니다 — D-57a가 이미 커밋한 공개 정보이고 금지하면 자기 무효화 게이트가 된다"
      min_lines: 40
  key_links:
    - from: "scripts/migrate/transform.ts"
      to: "domain/money/index.ts"
      via: "차익 재계산과 금액 파싱이 같은 반올림 정본을 쓴다 — 이전 데이터도 새 규칙으로 계산된다"
      pattern: "round"
    - from: "scripts/migrate/extract.ts"
      to: "scripts/migrate/parse-dump.ts"
      via: "mysqldump의 INSERT … VALUES를 직접 파싱한다(백업 저장소의 parse_dump.js가 뼈대)"
      pattern: "INSERT"
    - from: "test/unit/migrate/transform-schema-parity.test.ts"
      to: "db/schema/quote-lines.ts"
      via: "변환 출력 키를 04-05가 실제로 만든 컬럼 이름과 대조한다 — 어긋남이 Phase 8이 아니라 여기서 드러난다"
      pattern: "bigint"
---

<!-- planner-discipline-allow: 1000000 -->
<!-- planner-discipline-allow: 1234500 -->
<!-- 위 둘은 합성 픽스처(`test/fixtures/migrate/synthetic-dump.sql`)에 들어갈 **가짜** 금액이고,
     N5 누출 게이트의 감시 범위는 `04-06-SUMMARY.md`·`04-VERIFICATION.md` 두 문서뿐이다.
     즉 픽스처에 이 값을 쓰는 것은 정상이고, 같은 모양의 숫자를 그 두 문서에 적으면 게이트가 잡는다 —
     두 범위가 다르다는 것이 요점이다. -->

<objective>
**As a** 경영관리본부 담당자, **I want to** 인트라넷의 프로젝트·견적 줄이 새 표 구조로 옮겨지는 규칙이 지금 확정되기를 원한다, **so that** Phase 8의 실제 전환이 「어떻게 옮길지」를 그때 처음 고민하지 않고 이미 검증된 규칙을 돌리기만 한다.

ROADMAP Phase 4 기준 7의 **extract·transform까지**를 만든다. 적재(load)·검증·델타 이전·전환은 Phase 8이다.

Purpose: 기준 7이 「변환 결과가 개발·테스트 픽스처로 쓰인다」를 요구하는데, 실데이터를 이 저장소에 넣을 수 없으므로(D-57) 이 플랜의 산출물은 **합성 픽스처 + 검증된 변환 규칙**이다. 실데이터는 로컬에서 변환 규칙을 검증하는 데만 쓴다.

Output: 파서·추출·변환 세 모듈, 합성 덤프 픽스처, 허용목록·변환 규칙 단위 테스트, 운영 문서 한 줄.

**이 플랜은 UI와 완전히 독립이지만 웨이브는 3이다.** 건드리는 파일이 `scripts/migrate/**` · `test/**` · `docs/OPERATIONS.md` · `docs/ARCHITECTURE.md`뿐이라 **어느 플랜과도 파일을 공유하지 않는다**(`04-VALIDATION.md:99`의 제약은 `db/migrations/`에만 걸리고, 웨이브 3의 04-07·04-08·04-09는 이 넷 중 어느 것도 건드리지 않는다). 그런데도 웨이브 2가 아니라 3인 이유: **이 플랜의 변환 출력이 04-05가 만든 표의 컬럼 모양과 04-03이 만든 `domain/money`의 시그니처에 맞아야 한다.** 웨이브 2에 두면 그 산출물이 아직 없어 「플랜 마크다운을 읽어 맞춘다」가 되는데, `04-05-PLAN.md`의 Task 2가 바로 그 컬럼 모양들을 실행자 재량으로 남겼으므로(통화·환율을 줄 단위로 둘지 · 거래처가 FK인지 값인지 · 차수 구분을 nullable로 둘지) **읽어도 답이 없다.** 어긋나면 실제로 적재되는 Phase 8에서, 원인과 한 페이즈 떨어진 자리에서 드러난다 — D-78이 막으려 했던 것과 같은 양식이고 이번에는 페이즈 경계를 넘는다. 웨이브를 하나 미루는 비용은 0이다(파일 공유가 없고 웨이브 3의 폭이 셋에서 넷이 되어도 웨이브 2의 다섯을 넘지 않는다).

실데이터는 별도 private 저장소(`rrangjaa-eng/PLANT8_INTRANET_BACKUP_260915`)에 있고 **읽기 전용**이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-RESEARCH.md
@.planning/phases/04-project-quotation-ledger/04-03-SUMMARY.md
@.planning/phases/04-project-quotation-ledger/04-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 파서 + 추출 — 허용목록 일곱 표만, 밖을 만나면 실패한다</name>
  <files>scripts/migrate/parse-dump.ts, scripts/migrate/extract.ts, test/unit/migrate/extract-allowlist.test.ts, test/fixtures/migrate/synthetic-dump.sql, docs/OPERATIONS.md</files>
  <read_first>
    - `04-CONTEXT.md` **D-57a 전문** — 허용목록 일곱 표: `fone_project` · `QUOTATION_LINE` · `REPORT_CATEGORY1` · `REPORT_CATEGORY2` · `fone_client` · `fone_team` · `fone_card`. 그리고 `PLANT8_INTRANET` **안의** 개인정보 표 넷(`fone_member` · `tb_admin_member` · `fone_partners` · `QUOTATION_PAYMENT`)과 나머지 7개 DB(고객사 행사 참가자 실명·전화·이메일 약 3,800행)
    - `04-CONTEXT.md` **D-57 전문** — 이 저장소는 public이고 사용자가 그것을 의도했다. 익명화해도 커밋하지 않는다. 변환 결과 보고서도 커밋하지 않는다
    - `scripts/settings-import.ts` 6–13행 주석 — CLI 번들 구분의 정본. 「Cloud Run Job 번들에는 넣지 않는다 … 배포마다 자동으로 도는 프로비저닝 단계(migrate·seed·account·db-bootstrap, **이 넷만**)가 아니라, 운영자가 필요할 때 손으로 한 번 돌리는 관리 작업이다」
    - `scripts/account-cli.ts` 6–70행 — 인자 규약: **플래그와 값은 항상 별개 argv 원소, 등호 결합(`--flag=value`)은 거부**하고 `UsageError`를 던진다. `parseArgs(argv)`를 export해 단위 테스트가 직접 부른다
    - `scripts/build-cli.mjs` 8–13행 — 번들 엔트리포인트 넷의 목록. **이 파일을 건드리지 않는다**
    - `test/unit/settings-import-cli.test.ts` · `test/unit/account-cli.test.ts` — 로컬 운영자 스크립트의 단위 테스트 형태 선례
    - `package.json` scripts — `settings:import`가 `node --env-file-if-exists=.env.local --import tsx scripts/settings-import.ts` 형태다. 새 스크립트 둘도 같은 형태의 npm script가 필요한지 판단한다
    - `docs/OPERATIONS.md` + `test/unit/docs-limits.test.ts` — **300행 상한**과 고정 토큰 15개. 사용법 한 줄을 더할 여유가 있는지 확인한다
    - `.gitignore` — 덤프 파일·추출 산출물이 실수로 커밋되지 않게 무엇을 무시해야 하는지. 없으면 이 태스크가 규칙을 더한다
    - 백업 저장소의 `분석도구/parse_dump.js` — **재사용 가능한 파서**. mysqldump의 `INSERT … VALUES`를 직접 파싱해 표별 JSON을 낸다(46표). 같은 폴더 README가 「덤프 → JSONL 어댑터를 만들 때 이 파서를 뼈대로 쓸 수 있다」고 적었다. **로컬에서 읽되 코드를 그대로 붙여넣지 말고 TypeScript로 다시 쓴다** — 그 저장소는 이 저장소의 의존성이 아니다
    - 같은 폴더 README의 교훈 — 긁기 자료 JSONL의 id는 **문자열**, DB는 **숫자**라 대조 시 형변환이 필수다
    - Task 2의 누출 정규식 게이트 — `test/fixtures/migrate`와 `scripts/migrate` 아래 파일에서 개인정보 표 이름 넷(`fone_member` 등)의 출현을 금지한다(`extract.ts`만 예외). **그래서 합성 덤프의 「허용목록 밖 표」에 그 이름 넷 중 하나를 쓰면 게이트와 정면으로 부딪힌다** — 아래 action이 그 충돌을 이름 선택으로 없앤다(N11)
  </read_first>
  <behavior>
    - 허용목록 일곱 표의 `INSERT … VALUES`만 JSONL로 나온다
    - 허용목록 **밖의 표**를 만나면 추출이 0이 아닌 종료 코드로 실패하고 메시지에 그 표 이름이 있다 — 조용히 건너뛰지 않는다
    - 허용목록 밖의 **DB**(`USE` 문이나 덤프 헤더로 구분)를 만나면 같은 방식으로 실패한다
    - `--flag=value` 형태의 인자는 `UsageError`다
    - 같은 덤프를 두 번 추출하면 같은 JSONL이 나온다
    - 이스케이프된 문자열·NULL·따옴표 안 쉼표가 있는 행이 올바르게 쪼개진다
  </behavior>
  <action>
`test/fixtures/migrate/synthetic-dump.sql`을 **먼저** 만든다. **전부 합성이다** — 프로젝트명은 「합성 프로젝트 A」 류, 클라이언트는 「합성 클라이언트 1」 류, 금액은 눈에 띄게 가짜인 값(예: `1000000`·`1234500`)을 쓴다. 담을 것: 허용목록 표 중 최소 셋(`fone_project` · `QUOTATION_LINE` · `fone_client`)의 `CREATE TABLE` + `INSERT … VALUES` 조각 · **허용목록 밖 표 하나**의 조각 — 추출이 그것을 만나 실패하는 것을 테스트가 증명한다 · 파서를 괴롭히는 행 셋(문자열 안 쉼표 · 이스케이프된 따옴표 · `NULL`).

**허용목록 밖 표의 이름은 합성 이름이다(N11).** `synthetic_out_of_scope` 같은, 인트라넷에 실재하지 않는 이름을 쓴다. 실제 개인정보 표 이름(`fone_member` 등 넷)을 픽스처에 쓰지 않는다 — Task 2의 누출 게이트가 `test/fixtures/migrate` 아래에서 그 이름들을 금지하므로 **첫 실행부터 구조적으로 충돌**한다. 판정 대상은 「이 표가 `ALLOWED_TABLES`에 있는가」이지 「이 표가 개인정보 표인가」가 아니므로, 합성 이름으로도 계약이 **정확히 같은 강도로** 증명된다. 그 이유를 픽스처 머리 주석에 한 줄 남긴다.

실제 개인정보 표 넷이 각각 거부되는지는 **단위 테스트 파일에서** 확인한다 — `test/unit/migrate/**`는 누출 게이트의 순회 대상이 아니다(게이트가 `test/fixtures/migrate`와 `scripts/migrate`만 본다). `extract-allowlist.test.ts`에 그 넷을 `ALLOWED_TABLES`에 없는 이름으로 넣어 판정 함수가 전부 거부함을 단언하는 케이스를 하나 둔다.

`scripts/migrate/parse-dump.ts`를 만든다 — mysqldump 텍스트에서 `(표 이름, 컬럼 목록, 행 배열)`을 뽑는 순수 함수. 외부 의존성 0. 이 저장소가 처음 만드는 파서이므로 백업 저장소 파서의 **접근 방식만** 가져오고 코드는 새로 쓴다.

`scripts/migrate/extract.ts`를 만든다:
- `export const ALLOWED_TABLES` — 일곱 표 이름의 `readonly` 배열. 각 이름 옆에 **왜 이 표가 필요한지** 한 줄 주석(성공 기준 7이 요구하는 것은 프로젝트·견적 줄이고 나머지는 그 둘의 참조 대상이다)
- 허용목록 **밖**을 만나면 던지는 판정. 던지는 메시지에 표 이름과 「허용목록은 D-57a」를 담는다. **건너뛰기 옵션을 만들지 않는다** — 옵션이 있으면 언젠가 켜진다
- `export function parseArgs(argv)` — `account-cli.ts` 규약 그대로. 입력 덤프 경로와 출력 디렉터리를 받는다
- 출력은 표마다 JSONL 한 파일. 출력 디렉터리 기본값을 **`.gitignore`된 경로**로 둔다
- 파일 머리 주석에 ① 이 스크립트가 CLI 번들에 들어가지 않는 이유(`scripts/settings-import.ts:6-13` 인용) ② 산출물을 커밋하지 않는 이유(D-57) 두 문장

`.gitignore`에 추출·변환 산출물 경로와 덤프 파일 패턴을 더한다(이미 있으면 그대로 둔다).

`test/unit/migrate/extract-allowlist.test.ts`를 만든다(RED 먼저). 합성 덤프로 ① 허용목록 표가 JSONL로 나온다 ② 허용목록 밖 표를 만나면 던진다 ③ 던진 메시지에 그 표 이름이 있다 ④ `--flag=value`가 `UsageError`다 ⑤ 파서가 쉼표·따옴표·NULL 행을 올바르게 쪼갠다 ⑥ **`ALLOWED_TABLES`가 정확히 일곱이고 그 일곱이 D-57a의 목록과 같다**(목록이 조용히 늘어나는 것을 막는다).

`docs/OPERATIONS.md`에 사용법 한 줄을 더한다 — 300행 상한을 넘기면 더하지 말고 SUMMARY에 그 사실을 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/migrate</automated>
    <fails_when>종료 코드가 0이 아니다. ⑥이 실패하면 허용목록이 일곱이 아니다 — 늘렸다면 D-57a를 다시 읽는다</fails_when>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/build-cli.mjs','utf8');if(/migrate\/(extract|transform)/.test(s)){console.error('MIGRATE SCRIPTS IN CLI BUNDLE');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `MIGRATE SCRIPTS IN CLI BUNDLE`이 나오고 종료 코드가 1이다 — 금지 항목 위반</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ALLOWED_TABLES`가 정확히 일곱이고 D-57a의 목록과 같다
    - 허용목록 밖 표·DB를 만나면 실패하고 건너뛰기 옵션이 없다
    - 인자 규약이 `account-cli.ts`와 같다(등호 결합 거부)
    - 합성 픽스처에 인트라넷에서 나온 값이 0개다
    - 픽스처의 「허용목록 밖 표」 이름이 합성 이름이고 실제 개인정보 표 이름이 아니다 — Task 2의 누출 게이트와 충돌하지 않는다(N11)
    - 실제 개인정보 표 넷의 거부는 단위 테스트 파일에서 확인되고, 그 파일이 누출 게이트의 순회 대상이 아님이 주석에 적혔다
    - `scripts/build-cli.mjs`가 이 태스크 diff에 없다
    - 추출 산출물 경로가 `.gitignore`에 있다
    - `docs/OPERATIONS.md`가 300행 이하다
  </acceptance_criteria>
  <done>허용목록 일곱 표만 추출되고, 그 밖을 만나면 실패하며, 그 계약이 합성 픽스처 위 단위 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 변환 — 차익 재계산 · `amount_basis` 판정 · 원천에 없는 값의 유도</name>
  <files>scripts/migrate/transform.ts, test/unit/migrate/transform.test.ts, test/fixtures/migrate/synthetic-dump.sql</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 4 기준 7 원문 — 「변환은 옛 금액이 공급가인지 합계(부가세 포함)인지를 **표본 대조로 판정**해 행마다 `amount_basis`를 적고 불명은 '계산 불가'로 두며(Eng OV-7), 외화 건수를 보고서에 한 줄로 낸다(Eng OV-8, 정보). 이전 문서의 번호는 **옛 id에서 결정적으로 파생**(예약 범위)되어 재실행해도 같다」
    - `04-CONTEXT.md` § 인트라넷 원천 스키마 실측 전문 — 이 태스크의 변환 규칙이 전부 여기서 나온다:
      · `fone_project`에 **상태 컬럼이 없다**(있는 것은 `confirm_manager`·`confirm_kuckjang`·`confirm_admin`·`confirm_ceo` 네 장의 `char(1)` 결재 플래그뿐) → D-41/D-63의 상태 4종은 이전할 원천이 없다
      · **시작일이 없다** — `project_edate datetime` 하나와 자유 텍스트 `project_date varchar(500)`뿐인데 PROJ-01은 기간을 필수로 요구한다
      · `QUOTATION_LINE`에 **차수 컬럼이 없다** → 옛 줄은 전부 한 차수로 들어간다
      · `quo_benefit decimal(15,0)`이 저장돼 있고 **그것이 「차익 불일치 66줄」의 컬럼이다 — 믿지 말고 재계산한다**
      · `invoice_money varchar(500)` · `invoice_date varchar(1000)` · `QUOTATION_PAYMENT.pay_money varchar(45)` — **금액이 자유 텍스트**다. 백업 저장소 README가 「계산서 금액 칸에 `(카결 28,999,000)` 같은 주석이 섞여 있다 — **앞 숫자만 취해야** 앱 합계와 맞는다」고 경고했고 그것이 `invoiceSum: 4,887조`를 만든 원인이다. **`amount_basis` 판정이 다뤄야 할 대상이 바로 이 칸들이다**
      · **통화·환율 컬럼이 어디에도 없다** — 「옛 금액은 통화 KRW · 환율 1」(Phase 8 규약)이 실측으로 확인된다. `quo_price`·`quo_expect`·`quo_execute`가 전부 `decimal(15,0)` 정수라 D-53의 최소단위 정수와 맞는다
      · `pro_num`이 `AUTO_INCREMENT=363`인데 프로젝트는 125건 — **결번이 이미 있다.** 옛 id에서 결정적으로 파생하는 번호는 그 결번을 그대로 물려받는다
    - Task 1이 만든 `parse-dump.ts`·`extract.ts`의 출력 JSONL 모양
    - `domain/money/index.ts` — **04-03이 이미 만들어 둔 실제 파일을 읽는다**(이 플랜은 웨이브 3이고 `depends_on`에 04-03이 있다). `round()`·`Money`·`RATE_ONE`·`rateToMinor`의 실제 시그니처를 소스에서 확인해 맞춘다. 차익 재계산이 같은 정본을 쓴다 — 어댑터를 두지 않는다
    - **04-05가 만든 `db/schema/projects.ts` · `quotes.ts` · `quote-lines.ts` · `exchange-rates.ts` · `reserve-ledger.ts`의 실제 컬럼 이름** — 변환 출력이 그 모양이어야 한다. **플랜 마크다운이 아니라 소스 파일을 읽는다**(웨이브 3이므로 존재한다). 04-05 Task 2가 실행자 재량으로 남긴 것들(통화·환율을 줄 단위로 둘지 · 거래처가 FK인지 값인지 · 차수 구분을 nullable로 둘지 · 고객 승인이 타임스탬프인지 불리언인지)의 **실제 결정**이 그 파일들과 `04-05-SUMMARY.md`에 있다
    - `test/unit/settings/registry-coverage.test.ts` · `test/unit/source-column-coverage.test.ts`(04-05) — 소스 텍스트를 정규식으로 파싱해 두 정본의 어긋남을 잡는 메타 테스트 형태. 아래 스키마 대조 테스트가 그대로 따른다. **AST 파서를 새로 들이지 않는다**
    - `.planning/ROADMAP.md` Phase 4 기준 7의 「이전 문서의 번호는 옛 id에서 **결정적으로 파생(예약 범위)**되어 재실행해도 같다」 — 「예약 범위」라는 낱말이 여기 말고는 어느 산출물에도 없다. 아래 action이 그 계약을 만든다(N9)
    - `docs/ARCHITECTURE.md`의 `document_counters` 컬럼 매핑 표(04-05 Task 3이 만든다) — 예약 범위 한 줄이 그 표 **옆**에 붙어야 한다. `test/unit/docs-limits.test.ts`의 **300행 상한**을 먼저 확인하고, 여유가 없으면 한 줄도 더하지 말고 SUMMARY에 그 사실을 적는다
    - 백업 저장소 README의 교훈 — JSONL의 id는 **문자열**, DB는 **숫자**. 대조 시 형변환 필수
  </read_first>
  <behavior>
    - 자유 텍스트 금액 칸 `"28,999,000 (카결 28,999,000)"`에서 `28999000`이 나온다 — 앞 숫자만
    - `basis` 판정이 **같은 행의 다른 금액 칸을 실제로 본다** — 판정 함수가 행 단위 입력을 받으므로 「표본 대조」가 시그니처 안에서 가능하다
    - 부가세율이 인자로 들어온다 — 10%가 함수 안에 박혀 있지 않다
    - 버려진 꼬리 문자열이 행마다 보존되어 무엇을 버렸는지 알 수 있다
    - 숫자가 하나도 없는 자유 텍스트는 `null` + `amount_basis: "unknown"`이다
    - 차익이 `quo_benefit`이 아니라 `견적가 − 실행가`로 다시 계산되고, 옛 값과 다르면 그 행이 불일치로 표시된다
    - 상태는 결재 플래그 넷에서 **유도**되거나 기본값이 주어지고, 어느 쪽인지가 행마다 기록된다
    - 시작일이 없으므로 `project_date` 자유 텍스트에서 유도하거나 `project_edate`에서 기본값을 주고, 어느 쪽인지가 행마다 기록된다
    - 옛 줄이 전부 한 차수(사전 견적 또는 1차 — 고른 쪽을 규칙 표에 적는다)에 들어간다
    - 통화는 전부 KRW, 환율은 1이다
    - 같은 입력으로 두 번 돌리면 같은 번호·같은 출력이 나온다 — 결번을 물려받는다
    - 변환 요약(행 수 · 불일치 수 · `amount_basis` 분포 · 외화 건수)이 **표준 출력**으로 나오고 파일로 커밋되지 않는다
  </behavior>
  <action>
`test/fixtures/migrate/synthetic-dump.sql`에 변환을 괴롭히는 합성 행을 더한다: 자유 텍스트 금액 세 종류(앞 숫자 + 주석 · 숫자 없음 · 쉼표만) · `quo_benefit`이 `견적가 − 실행가`와 다른 줄 · 결재 플래그 조합 넷 · `project_date`가 빈 행. **전부 합성 값이다.**

`test/unit/migrate/transform.test.ts`를 **먼저** 만든다(RED 확인). behavior 열 줄을 케이스로 옮기고, **결정적 재실행 케이스**를 반드시 둔다 — 같은 입력으로 두 번 돌린 결과가 깊은 비교로 같다.

`scripts/migrate/transform.ts`를 만든다:
- **파싱과 판정을 두 함수로 가른다(N2).** `parseMoneyText(text: string): { amountMinor: number | null; discarded: string }` — 자유 텍스트에서 **앞 숫자만** 취하고 나머지를 `discarded`에 남긴다. 이 함수는 한 칸만 보므로 `basis`를 정할 수 없고, **정하려 들지 않는다**
- `export function resolveAmountBasis(row, opts: { vatRateMinor: Money<"rate_minor"> }): { basis: "supply" | "total" | "unknown"; comparedWith: string | null }` — **행 단위 입력을 받는다.** ROADMAP 기준 7의 「표본 대조」는 **같은 프로젝트의 다른 금액 칸과 비교하는 것**인데 칸 하나의 문자열만 받는 시그니처로는 그 비교가 물리적으로 불가능하다 — 그래서 이 함수가 행(또는 같은 프로젝트의 금액 칸 묶음)을 받고 어느 칸과 대조했는지를 `comparedWith`로 돌려준다. **부가세율을 인자로 받는다** — 10%를 함수 안에 박지 않는다. 세율은 설정 키(`tax.vat.rate`)가 정본이고 시점에 따라 바뀌며, 옛 데이터에 적용할 세율은 **운영자가 정해서 넘길 값**이다. 정수 비교를 위해 04-03의 `rateToMinor`로 최소단위 정수를 받는다
- 판정 규칙을 `resolveAmountBasis` 위 주석에 **표로** 남긴다: 어떤 조합에서 `total`, 어떤 조합에서 `supply`, 어떤 조합에서 `unknown`인지. Phase 8이 이 규칙을 실데이터로 검증하므로 규칙이 코드가 아니라 표로도 읽혀야 한다
- `export function deriveProjectStatus(row): { status: string; derivedFrom: "approval_flags" | "default" }` — 결재 플래그 넷에서 유도하는 규칙을 표로 적고, 유도할 수 없으면 기본값을 준다. **원천에 상태가 없다는 사실**(04-CONTEXT 실측)을 함수 위 주석 첫 문장으로 적는다
- `export function deriveProjectPeriod(row): { startDate; endDate; derivedFrom }` — 같은 형태
- `export function deriveDocumentNumber(oldId: number, year: number): string` — 옛 id에서 결정적으로 파생. 결번을 물려받는다는 사실과 그 이유(ROADMAP 기준 7 「재실행해도 같다」)를 주석에 적는다. 04-04의 `formatDocumentNumber`를 쓸 수 있으면 쓰고, 쓸 수 없으면 같은 서식을 만들되 그 중복을 SUMMARY에 올린다
- `export function reservedSequenceRange(rows): { year: number; maxSequence: number }[]` — **예약 범위를 값으로 낸다(N9).** ROADMAP 기준 7이 「옛 id에서 결정적으로 파생(**예약 범위**)」이라고 적었는데 그 낱말이 어느 산출물에도 계약으로 없다. 실측이 문제를 구체적으로 만든다: `fone_project`의 `pro_num`이 `AUTO_INCREMENT=363`인데 프로젝트는 125건이라 **파생 번호의 순번이 최대 362까지 올라가고**(3자리 순번이므로 `26362` 같은 값이 된다), 새 채번의 `document_counters.value`는 0에서 시작한다. Phase 8이 적재한 뒤 카운터를 올리지 않으면 **새 프로젝트의 첫 번호가 이미 쓰인 번호와 `UNIQUE`에서 부딪힌다.** 이 함수가 연도별 최대 순번을 내고, 변환 요약(표준 출력)이 그 값을 한 줄로 보고한다
- **계약을 두 곳에 남긴다**: ① `deriveDocumentNumber`·`reservedSequenceRange` 위 주석에 「이 범위는 Phase 8의 적재가 끝난 뒤 `document_counters`를 그 위로 올려야 한다 — 올리지 않으면 UNIQUE 충돌」 ② `docs/ARCHITECTURE.md`의 `document_counters` 컬럼 매핑 표(04-05 Task 3) **옆에 한 줄**로 같은 계약. 파생 규칙이 이 페이즈에만 있으므로 계약도 여기서 만든다. **`test/unit/docs-limits.test.ts`의 300행 상한을 먼저 확인**하고 여유가 없으면 문서를 건드리지 말고 SUMMARY에 그 사실과 문장을 적어 후속으로 넘긴다
- 차익은 `quo_benefit`을 **읽지 않고** `견적가 − 실행가`로 계산한다. 옛 값과의 불일치 건수를 요약에 센다
- 통화 KRW · 환율 1을 모든 행에 붙인다(실측으로 확인된 규약)
- 요약을 표준 출력으로만 낸다. **파일로 쓰지 않는다**(D-57 — 변환 결과 보고서도 커밋하지 않는다). 운영자가 필요하면 셸에서 리디렉션한다는 것을 사용법에 적는다
- 출력은 04-05의 표 다섯 모양에 맞춘 JSONL. 적재는 Phase 8이므로 **DB에 쓰지 않는다**

**`test/unit/migrate/transform-schema-parity.test.ts`를 만든다(B9의 강제 지점).** 04-06의 다른 검증은 전부 합성 픽스처 위에서 닫히고(`parse → transform → 단언`) **변환 출력의 키를 실제 표 모양과 대조하는 검사가 없다** — 어긋나면 적재가 일어나는 Phase 8에서, 원인과 한 페이즈 떨어진 자리에서 드러난다. 이 테스트가 그 고리를 여기서 건다:

- `db/schema/projects.ts` · `quotes.ts` · `quote-lines.ts` · `exchange-rates.ts` · `reserve-ledger.ts`를 읽어 **컬럼 이름 집합**을 정규식으로 뽑는다. 드리즐 선언의 컬럼 이름은 `text("project_name")` · `bigint("quote_price", …)` 형태이므로 **인용부호 안의 첫 인자**를 모으고, 동시에 **TS 속성 이름**(`projectName:`)도 모아 두 집합의 합집합을 만든다 — 변환 출력이 어느 표기를 쓰든 통과하게 하고, 고른 표기를 SUMMARY에 적는다
- 합성 픽스처를 변환해 나온 JSONL 행들의 **키 집합**을 모은다(표별로)
- **키 집합 ⊆ 컬럼 이름 집합**을 단언한다. 벗어난 키를 `표: 키1, 키2` 형태로 실패 메시지에 담는다 — 그 목록이 곧 고칠 대상이다
- 방향이 부분집합인 이유를 주석에 적는다: 변환이 모든 컬럼을 채울 필요는 없다(원천에 없는 값이 셋이다). 반대 방향(컬럼 ⊆ 키)을 단언하면 정상 상태가 실패한다
- **「검출기 자체가 죽어있지 않다」 케이스**: 뽑아낸 컬럼 이름 집합의 크기가 20 이상이고 변환 출력의 키 집합이 비어 있지 않다. 이 케이스가 없으면 정규식이 빈 집합을 낼 때 부분집합 단언이 조용히 통과한다

**`test/unit/migrate/planning-leak.test.ts`를 만든다(N5).** `04-VALIDATION.md`의 수동 검증 항목이 「로컬에서 실제 덤프로 돌리고 보고서를 눈으로 본다」인데 그 결과가 들어가는 `04-06-SUMMARY.md`·`04-VERIFICATION.md`는 **커밋되고 이 저장소는 public이다**(D-57). 지금 누출 게이트는 `test/fixtures/migrate`와 `scripts/migrate`의 표 이름만 본다 — 그 경로를 여기서 닫는다:

- 대상은 `.planning/phases/04-project-quotation-ledger/04-06-SUMMARY.md`와 `.planning/phases/04-project-quotation-ledger/04-VERIFICATION.md` 둘. **파일이 아직 없으면 그 케이스를 건너뛴다**(실행 중에 생긴다)
- 금지 ①: **금액 모양 숫자** — 천 단위 쉼표로 묶인 수와 7자리 이상 연속 숫자. 실데이터 유래 합계·행 금액이 들어오는 모양이 그것이고, D-57이 집계값까지 막았으므로 익명화가 아니라 **삭제**가 답이다
- 금지 ②: 변환 요약이 통째로 붙여넣어진 흔적 — 요약의 고정 문구(예: 불일치 건수 줄의 라벨)가 값과 함께 나타나는 형태
- **표 이름은 금지 대상이 아니다.** `fone_project` 같은 원천 표 이름은 `04-CONTEXT.md`의 D-57a가 이미 커밋해 둔 공개 정보이고, 그것을 금지하면 「픽스처에 어느 이름을 쓰지 않았는지」를 SUMMARY에 설명하는 것조차 게이트에 걸려 **자기 무효화 게이트**가 된다. 표 이름의 누출 방지는 `test/fixtures/migrate`·`scripts/migrate`를 보는 Task 2의 다른 게이트가 이미 맡는다 — 두 게이트의 역할 분담을 이 파일 주석에 한 줄로 적는다
- **허용**: 판정(맞다/틀리다) · 규칙 이름 · 검사한 케이스 수 · `04-06` 같은 식별자 · 날짜. 무엇을 적어도 되는지를 테스트 파일 머리 주석에 목록으로 적어, 실행자가 SUMMARY를 쓸 때 그 목록을 보고 쓰게 한다
- 위반을 `파일:줄` 목록으로 모아 실패 메시지에 담는다. **「검출기 살아있음」 케이스**: 금지 패턴을 담은 합성 문자열을 판정 함수에 넣으면 실제로 걸린다
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/migrate</automated>
    <fails_when>종료 코드가 0이 아니다. 결정적 재실행 케이스가 깨지면 번호 파생이나 정렬에 비결정적 요소(Map 순회·시각)가 섞인 것이다. **스키마 대조가 실패하면 벗어난 키 목록이 실패 메시지에 있다** — `db/schema/*.ts`를 열어 실제 컬럼 이름으로 맞춘다(플랜 마크다운이 아니라 소스가 정본이다). **누출 게이트가 실패하면 `파일:줄` 목록이 곧 지울 대상이다** — 값을 익명화하지 말고 지운다(D-57은 집계도 막는다)</fails_when>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/04-project-quotation-ledger/';let bad=[];for(const f of ['04-06-SUMMARY.md','04-VERIFICATION.md']){let s;try{s=fs.readFileSync(p+f,'utf8')}catch{continue}s.split('\n').forEach((l,i)=>{if(/\d{1,3}(,\d{3})+/.test(l)||/\d{7,}/.test(l))bad.push(f+':'+(i+1))})}if(bad.length){console.error('INTRANET-SHAPED VALUES IN COMMITTED PLANNING ARTIFACTS: '+bad.join(', '));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `INTRANET-SHAPED VALUES IN COMMITTED PLANNING ARTIFACTS: …`가 나오고 종료 코드가 1이다 — 실데이터 수동 검증의 결과를 값째로 옮겨 적었다(D-57 · N5). `파일:줄` 목록이 곧 지울 대상이고, 판정과 규칙 이름만 남긴다. 이미 공개된 ROADMAP 수치를 다시 적어야 하는 경우에도 적지 않는다 — 이 두 파일의 규칙은 「값 없음」이다. **게이트를 느슨하게 고쳐 통과시키지 않는다.** 표 이름은 이 게이트의 대상이 아니다(D-57a가 이미 커밋한 공개 정보) — 표 이름 누출은 `test/fixtures/migrate`·`scripts/migrate`를 보는 위 게이트가 맡는다</fails_when>
    <automated>node -e "const fs=require('fs'),path=require('path');function walk(d){let o=[];for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=path.join(d,e.name);if(e.isDirectory())o=o.concat(walk(f));else o.push(f)}return o}const files=walk('test/fixtures/migrate').concat(walk('scripts/migrate'));const banned=[/fone_member/,/tb_admin_member/,/fone_partners/,/QUOTATION_PAYMENT/];let bad=[];for(const f of files){if(f.endsWith('extract.ts'))continue;const s=fs.readFileSync(f,'utf8');for(const b of banned)if(b.test(s))bad.push(f+' :: '+b)}if(bad.length){console.error('PII TABLE REFERENCED OUTSIDE ALLOWLIST GUARD: '+bad.join(' | '));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `PII TABLE REFERENCED OUTSIDE ALLOWLIST GUARD …`가 나오고 종료 코드가 1이다. **예외 목록을 넓혀 통과시키지 않는다**(N11) — 합성 픽스처의 「허용목록 밖 표」는 Task 1이 정한 대로 합성 이름이어야 하고, 실제 개인정보 표 이름의 거부 케이스는 게이트의 순회 대상이 아닌 `test/unit/migrate/**`에 둔다. 예외는 `extract.ts` 하나뿐이다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다 — `money-boundary`가 `scripts/` 아래 금액 산술을 잡으면 `domain/money`를 거쳐야 한다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 자유 텍스트 금액에서 앞 숫자만 취하고 버린 꼬리가 보존된다
    - 차익이 `quo_benefit`을 읽지 않고 재계산되며 불일치 건수가 요약에 나온다
    - `amount_basis` 판정 규칙이 함수 주석에 표로 있고 불명이 `unknown`이다
    - 상태·시작일·차수 세 유도 규칙이 각각 함수로 있고 「원천에 없다」는 사실이 주석 첫 문장에 있다
    - 통화 KRW · 환율 1이 모든 행에 붙는다
    - 같은 입력으로 두 번 돌린 결과가 같다
    - 요약이 표준 출력으로만 나가고 파일로 쓰이지 않는다
    - 저장소에 커밋되는 픽스처에 인트라넷 값이 0개다
    - 변환 출력의 키 집합이 `db/schema/*.ts`의 컬럼 이름 집합의 **부분집합**임이 단언되고, 검출기 살아있음 케이스가 함께 있다(B9)
    - `basis` 판정 함수가 **행 단위 입력**을 받고 부가세율을 인자로 받는다 — 10%가 함수 안에 박혀 있지 않다(N2)
    - 예약 범위를 값으로 내는 함수가 있고, 「Phase 8이 카운터를 그 위로 올린다」 계약이 주석과 `docs/ARCHITECTURE.md` 한 줄에 있다(N9). 300행 상한 때문에 문서를 못 건드렸다면 그 사실과 문장이 SUMMARY에 있다
    - `04-06-SUMMARY.md`·`04-VERIFICATION.md`에 **값 모양 숫자**(천 단위 쉼표 · 7자리 이상 연속 숫자)가 0개다(N5). 표 이름은 이 게이트의 대상이 아니고 그 역할 분담이 테스트 주석에 적혔다
  </acceptance_criteria>
  <done>인트라넷 프로젝트·견적 줄을 새 표 구조로 옮기는 규칙이 확정되고, 차익 재계산과 자유 텍스트 금액 판정이 합성 픽스처 위 단위 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인트라넷 덤프 → 이 저장소 | 이 저장소는 public이다. 덤프에는 직원 비밀번호·평문 계좌번호·고객사 행사 참가자 실명 약 3,800행이 있다 |
| 허용목록 판정 → 추출 범위 | 판정이 느슨하거나 건너뛰기 옵션이 있으면 개인정보 표가 산출물에 들어간다 |
| 옛 저장값 → 새 계산값 | 인트라넷의 `quo_benefit`은 이미 66줄이 틀렸다. 그대로 옮기면 오류가 새 시스템의 정본이 된다 |
| 자유 텍스트 금액 → 집계 | 주석이 섞인 칸을 통째로 파싱하면 4,887조 같은 값이 나온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-29 | Information Disclosure | 개인정보 표가 추출 산출물에 들어감 | high | mitigate | 표 단위 허용목록 일곱(D-57a) + 밖을 만나면 **실패**(건너뛰기 옵션 없음) + 그 계약을 고정하는 단위 테스트 + 목록 길이 단언 |
| T-04-30 | Information Disclosure | 인트라넷에서 나온 값이 public 저장소에 커밋됨 | high | mitigate | 픽스처를 전부 합성으로 만들고, 산출물 경로를 `.gitignore`에 넣고, 요약을 파일이 아니라 표준 출력으로만 낸다. 정규식 게이트가 개인정보 표 이름의 저장소 내 출현을 검사한다 |
| T-04-31 | Tampering | 옛 차익을 그대로 옮겨 66줄의 오류가 새 시스템의 정본이 됨 | high | mitigate | `quo_benefit`을 읽지 않고 재계산하고, 불일치 건수를 요약에 센다. 단위 테스트가 불일치 행을 케이스로 갖는다 |
| T-04-32 | Tampering | 자유 텍스트 금액을 통째로 파싱해 집계가 폭주 | high | mitigate | 앞 숫자만 취하는 파서 + 버린 꼬리 보존 + 숫자 없음은 `null`/`unknown`. 세 종류 합성 행이 케이스다 |
| T-04-33 | Tampering | 재실행 시 번호가 달라져 같은 문서가 둘이 됨 | medium | mitigate | 옛 id에서 결정적으로 파생 + 두 번 돌린 결과 깊은 비교 케이스 |
| T-04-34 | Elevation of Privilege | 로컬 운영자 스크립트가 배포 번들에 들어가 자동 실행됨 | medium | mitigate | `scripts/build-cli.mjs`를 건드리지 않고, 정규식 게이트가 그 파일에 이 스크립트 이름이 없음을 검사한다. `scripts/settings-import.ts:6-13`이 같은 구분의 선례 |
| T-04-88 | Information Disclosure | 실데이터 수동 검증의 결과가 **커밋되는** `04-06-SUMMARY.md`·`04-VERIFICATION.md`에 값째로 들어가 public 저장소에 노출 | high | mitigate | 그 두 파일을 대상으로 하는 누출 단위 테스트(천 단위 쉼표 수 · 7자리 이상 연속 숫자 금지) + 「무엇은 적어도 되는가」 목록을 테스트 주석에 둔다. D-57이 집계값까지 막으므로 익명화가 아니라 **삭제**가 답이다. 표 이름은 대상에서 뺀다 — D-57a가 이미 커밋한 공개 정보라 금지하면 자기 무효화 게이트가 되고, 표 이름 누출은 `test/fixtures/migrate`·`scripts/migrate`를 보는 T-04-30의 게이트가 맡는다 |
| T-04-89 | Tampering | 이전 번호의 예약 범위가 어디에도 없어 Phase 8 적재 뒤 새 채번이 이미 쓰인 번호와 `UNIQUE`에서 부딪힌다 | medium | mitigate | 연도별 최대 순번을 값으로 내는 함수 + 변환 요약 한 줄 + 「Phase 8이 카운터를 그 위로 올린다」 계약을 주석과 `docs/ARCHITECTURE.md`에 남긴다. 실측 근거: `pro_num` AUTO_INCREMENT 363 대 프로젝트 125건이라 파생 순번이 362까지 올라가는데 `document_counters.value`는 0에서 시작한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). mysqldump 파서를 외부 라이브러리 없이 직접 쓴다. `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` 통과
- `git status`에 덤프 파일·추출 산출물이 없다
- `scripts/build-cli.mjs` 무변경
- 커밋이 둘이다 — 파서·추출 / 변환
- `pnpm vitest run --project unit test/unit/docs-limits.test.ts` 통과 — `docs/ARCHITECTURE.md`가 300행 이하다(N9 한 줄을 더했다면)
- **수동 검증(04-VALIDATION.md)**: 로컬에서 실제 덤프로 돌려 `amount_basis` 판정이 맞는지 눈으로 본다. 보고서는 커밋하지 않는다. **`04-06-SUMMARY.md`·`04-VERIFICATION.md`에는 판정과 규칙 이름만 적고 값을 적지 않는다** — 누출 단위 테스트가 그것을 강제한다(N5)
- 웨이브 3 종료 게이트는 04-07·04-08·04-09와 공유한다 — 이 플랜은 UI 산출물이 없으므로 `/design-review`·독립 DOM 감사의 대상이 아니다
</verification>

<success_criteria>
- 허용목록 일곱 표만 추출되고 밖을 만나면 실패한다(D-57a)
- 이 저장소에 인트라넷 값이 0개다(D-57)
- 차익이 재계산되고 자유 텍스트 금액이 앞 숫자만 취해진다
- 원천에 없는 값 셋(상태·시작일·차수)의 유도 규칙이 확정됐다
- 재실행해도 같은 번호가 나온다
- 변환 출력의 키가 04-05의 실제 컬럼 이름 안에 있다 — 모양 어긋남이 Phase 8이 아니라 여기서 잡힌다(B9)
- 이전 번호의 예약 범위와 Phase 8의 카운터 인상 계약이 문서에 있다(N9)
- 커밋되는 `.planning/` 산출물에 실데이터 유래 값이 0개다(N5)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 스크립트 | `scripts/migrate/parse-dump.ts` · `scripts/migrate/extract.ts` · `scripts/migrate/transform.ts` |
| 새 테스트 | `test/unit/migrate/extract-allowlist.test.ts` · `test/unit/migrate/transform.test.ts` · `test/unit/migrate/transform-schema-parity.test.ts`(B9) · `test/unit/migrate/planning-leak.test.ts`(N5) |
| 새 픽스처 | `test/fixtures/migrate/synthetic-dump.sql`(전부 합성) |
| 새 상수 | `ALLOWED_TABLES`(일곱) |
| 새 함수 | `parseMoneyText`(칸 하나) · `resolveAmountBasis`(행 단위 + 세율 인자, N2) · `reservedSequenceRange`(N9) |
| 문서 | `docs/OPERATIONS.md` 사용법 한 줄 · `docs/ARCHITECTURE.md` 예약 범위 계약 한 줄(N9 — 300행 상한에 걸리면 SUMMARY로) |
| 새 화면 · 새 표 · 새 마이그레이션 · CLI 번들 변경 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-06-SUMMARY.md` when done
</output>
