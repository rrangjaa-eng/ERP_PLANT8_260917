---
phase: 04-project-quotation-ledger
plan: 07
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - ui/form/Form.tsx
  - ui/form/Form.module.css
  - ui/form/FormRow.tsx
  - ui/form/SubmitRow.tsx
  - ui/select/Select.tsx
  - ui/select/Select.module.css
  - ui/input/Autocomplete.tsx
  - ui/input/Autocomplete.module.css
  - test/unit/design-tokens-usage.test.ts
  - test/unit/ui/form-select-contract.test.ts
  - test/unit/ui/no-raw-datalist.test.ts
autonomous: true
requirements: [UX-04, PROJ-01, RSV-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "폼 폭 720과 칸 폭 셋(200·280·480)이 컴포넌트가 강제하는 값이 되었다 — 화면 작성자가 기억하지 않는다(D-60)"
    - "네이티브 `required` 말풍선이 첫 관문이 아니다 — 빈 필수 칸은 제출 버튼이 이유를 말하고 그 이유가 버튼에 연결된다(A-H3 · F4)"
    - "`<select>`의 라벨 배치·오류 표시·aria 배선이 `TextField`와 정확히 같다 — 한 폼에 라벨 배치가 두 가지인 상태가 이 컴포넌트로 닫힌다(A-H2)"
    - "정의만 되고 아무도 안 쓰는 토큰을 단위 테스트가 잡는다 — 감시 범위가 출하 코드(`app/**`+`ui/**`)이고 목업은 제외다(D-61)"
    - "커스텀 드롭다운이 없다 — enum 선택은 네이티브 `<select>`, 자유 입력 자동완성은 네이티브 `<input list>`다(§7-2 · D-66)"
    - "자유 입력 자동완성에 **소유 컴포넌트**가 있다 — 네 사용처가 같은 라벨 배치·같은 aria 배선을 얻고, 화면 코드가 목록 요소를 직접 적지 않는다(D-83)"
    - "출하 화면 코드에 자동완성 목록 요소 리터럴이 0개다 — 그 사실이 단위 테스트로 고정됐다(D-83 · D-76과 같은 형태)"
  prohibitions:
    - statement: "커스텀 드롭다운을 만들어서는 안 된다. §7-2(`SYSTEM.md:633`)가 「네이티브 `<select>` 그대로. 커스텀 드롭다운 만들지 않는다」를 원문으로 정했다. 목록이 열려 있는 칸(클라이언트·거래처)은 그 규칙의 대상이 아니라 네이티브 `<input list>` + `<datalist>`다(D-66)"
      status: resolved
      verification: automated
    - statement: "`app/**`의 화면 코드가 자동완성 목록 요소를 **직접 적어서는 안 된다.** 이것이 정확히 A-H2를 만든 경로다 — `.selectLabel`이 6개 파일에 복제되어 한 폼 안에 라벨 배치가 두 가지가 됐다(`docs/HANDOFF.md` §함정 10 실측). D-66의 자유 입력 자동완성은 이 페이즈에서 **네 곳**(프로젝트 폼의 클라이언트 · 견적 줄의 거래처 셀 · 리저브 폼의 클라이언트와 프로젝트 연결)에서 쓰이므로, 소유 컴포넌트 없이 두면 같은 구멍을 **이 페이즈가 A-H2를 닫는다고 말하면서** 새로 연다(D-83)"
      status: resolved
      verification: automated
    - statement: "`aria-describedby` 배선을 `ui/form`에 두어서는 안 된다. `Button` 안에 있다(04-02 Task 3, F4) — 표 안 삭제 버튼·상세 화면 잠김 버튼은 폼 밖이라 폼에 두면 그 둘이 닫히지 않는다. `ui/form`은 이유 **문자열만** 넘긴다"
      status: resolved
      verification: automated
    - statement: "D-61 토큰 테스트의 감시 범위를 리포 전체로 넓혀서는 안 된다. `--form-max`는 리포 전체로는 `docs/design/system/*.html` 목업 4곳에서 쓰이고 있어 전체 범위 테스트는 오늘 바로 통과해 **아무것도 잡지 못한다**(실측). 출하 코드에서는 0이다"
      status: resolved
      verification: automated
    - statement: "새 색·서체·radius·간격 값을 만들어서는 안 된다. 04-UI-SPEC이 이 페이즈의 새 토큰을 0개로 확정했고 두 컴포넌트가 참조할 토큰은 전부 `tokens.css`에 있다"
      status: resolved
      verification: automated
  artifacts:
    - path: "ui/form/Form.tsx"
      provides: "§6-3 폼 골격 — `--form-max` 720 · 왼쪽 정렬 · 묶음/항목 간격 · `noValidate` · 제출 막힘 이유를 Button에 문자열로 넘김"
      exports: ["Form", "FormGroup"]
      min_lines: 60
    - path: "ui/select/Select.tsx"
      provides: "네이티브 `<select>` + `TextField`와 같은 라벨 배치·오류·aria 배선. 기본 폭 200"
      exports: ["Select"]
      min_lines: 50
    - path: "ui/input/Autocomplete.tsx"
      provides: "자유 입력 자동완성(D-66)의 소유 컴포넌트. `TextField`의 96px 라벨 칸·`aria-invalid`·`aria-describedby`를 상속하고 목록 요소를 `useId`로 내부 배선한다(D-83). 이 페이즈의 사용처 넷"
      exports: ["Autocomplete"]
      min_lines: 55
    - path: "test/unit/ui/no-raw-datalist.test.ts"
      provides: "`app/**`의 출하 화면 코드에 자동완성 목록 요소 리터럴이 없음을 단언하는 메타 테스트(D-83). `no-platform-key-literal.test.ts`(D-76)가 형태의 선례"
      min_lines: 35
    - path: "test/unit/design-tokens-usage.test.ts"
      provides: "사용처 0인 토큰을 잡는 메타 테스트. 감시 범위는 `app/**` + `ui/**`(D-61)"
      min_lines: 50
    - path: "ui/form/Form.module.css"
      provides: "폼 골격 CSS — `var(--form-max)`의 출하 코드 첫 사용처"
      contains: "--form-max"
  key_links:
    - from: "ui/form/Form.tsx"
      to: "ui/button/Button.tsx"
      via: "제출 막힘 이유를 `disabledReason` 문자열로 넘긴다 — 배선은 Button 안이다(F4)"
      pattern: "disabledReason"
    - from: "ui/form/Form.module.css"
      to: "docs/design/tokens.css"
      via: "`--form-max: 720px`의 출하 코드 첫 사용처 — 함정 10의 「정의만 되고 사용처 0」을 닫는다"
      pattern: "--form-max"
    - from: "ui/select/Select.module.css"
      to: "ui/input/TextField.module.css"
      via: "라벨 칸 96px 미디어쿼리 구조가 같아야 한다 — 다르면 A-H2가 다시 열린다"
      pattern: "--label-w"
    - from: "ui/input/Autocomplete.tsx"
      to: "ui/input/TextField.tsx"
      via: "같은 디렉터리에서 라벨 칸·오류·aria 배선을 상속한다 — 네 사용처가 그 하나를 공유한다(D-83)"
      pattern: "--label-w"
    - from: "ui/input/Autocomplete.tsx"
      to: "ui/grid/grid-keys.ts"
      via: "목록을 단 셀이 그리드 안에서 방향키 이동과 어떻게 어울리는지의 규칙이 04-10의 순수 모듈에 단위 테스트로 선다 — D-66이 「그리드 셀 안에서도 쓸 수 있다」를 근거로 삼았으므로 그 상호작용이 계약이다"
      pattern: "list"
---

<!-- planner-discipline-allow: datalist -->
<!-- planner-discipline-allow: <datalist> -->

<objective>
**As a** 프로젝트를 등록하는 기획 PM, **I want to** 필수 칸을 비웠을 때 영문 브라우저 말풍선이 아니라 제출 버튼 옆에서 무엇이 비었는지 한글로 듣고 싶다, **so that** 폼마다 다른 폭·다른 라벨 배치·다른 오류 관문을 만나지 않는다.

화면을 만들기 **전에** 폼 계약을 컴포넌트로 만든다(D-60, HANDOFF §함정 10). 이 플랜은 **화면을 하나도 만들지 않는다** — 04-11·04-12·04-13이 이 컴포넌트를 쓴다.

Purpose: `docs/HANDOFF.md` §함정 10의 실측 표가 이유다. 「폼 폭 720 · 칸 폭 280/480/200」의 강제 수단이 **없어서** 폼 7개 전부가 PC에서 1240px이 됐고, 「라벨 열 96px」은 `TextField` 컴포넌트가 강제해서 **전 폼에서 정확히 96px**이었다. 같은 문서의 결론이 「컴포넌트가 된 계약은 지켜졌고 산문으로 남은 계약은 전부 어긋났다 — 예외가 없다」다.

Output: `ui/form` 셋(골격·행·제출 줄), `ui/select`, **`ui/input/Autocomplete`**, D-61 토큰 감시 테스트, 컴포넌트 계약 소스 단언.

**여섯 번째 컴포넌트를 더한다(D-83).** D-68이 `ui/confirm`을 다섯 번째로 세웠고, 심층 검토가 **자유 입력 자동완성에 소유 컴포넌트가 없다**는 것을 찾았다. D-66이 고른 네이티브 `<input list>` + `<datalist>`가 이 페이즈에서 **네 곳**(04-11 Task 1의 클라이언트 · 04-12 Task 2의 거래처 셀 · 04-13 Task 2의 클라이언트와 프로젝트 연결)에서 쓰이는데 그 배선을 화면마다 적으면 **`.selectLabel` 6복제와 같은 양식**이 되고, 그것이 정확히 A-H2를 만든 경로다(`docs/HANDOFF.md` §함정 10). **이 페이즈가 A-H2를 닫는다고 하면서 새 화면에서 같은 구멍을 열 뻔했다.** HOLD SCOPE에서도 「스케치가 필요한 메커니즘을 빠뜨렸다면 그 작업은 범위 안」이므로 확장이 아니다(D-83).

**D-47 경계**: 이 페이즈는 컴포넌트만 만들고 **새 폼 둘**(프로젝트 등록·리저브 줄 등록)에만 쓴다. 관리자 폼 8개 이관은 Phase 7이다. 그래서 03-OPEN-ITEMS의 A-H2·A-H3은 이 페이즈에서 **절반만** 닫힌다 — 메커니즘 절반이고 이관 절반은 Phase 7이다.

**RTL이 없다.** `vitest.config.ts` unit project는 `environment: "node"`이고 `include`가 `test/unit/**/*.test.ts`뿐이며 `@testing-library/react`가 미설치다(D-45가 새 의존성 0). 그래서 이 플랜의 검증은 **① CSS·소스 단언**(`test/unit/ui/toast-timer.test.ts`·`system-md-compliance.test.ts` 선례) **② 실제 DOM은 04-11·04-12의 E2E** 둘로 가른다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quotation-ledger/04-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `ui/form` — 720 기둥과 막힘 이유를 컴포넌트가 강제한다</name>
  <files>ui/form/Form.tsx, ui/form/Form.module.css, ui/form/FormRow.tsx, ui/form/SubmitRow.tsx</files>
  <read_first>
    - `ui/input/TextField.tsx` + `ui/input/TextField.module.css` **전문** — 이 저장소에서 §6-3을 맞게 구현한 유일한 컴포넌트이고 `ui/form`·`ui/select`가 반드시 따라야 하는 모양이다. 특히 CSS의 **미디어쿼리 방향**: `.row`가 폰에서 세로 쌓기가 **기본값**이고 `@media (min-width: 700px)`에서만 flex 96px 라벨 칸으로 전환한다. **이 순서를 반대로(PC 기본 + 폰 오버라이드) 짜면 안 된다**(04-PATTERNS §6 핵심 계약 (1))
    - `docs/design/tokens.css` 110–120행 — `--form-max: 720px`(116행) · `--label-w: 96px` · `--modal-w: 480px` · `--control-h: 32px` · `--touch-min: 44px` · `--pad-page`
    - `docs/design/SYSTEM.md` §6-3 전문 — 폼 골격: 한 열 · `max-width: var(--form-max)` · **왼쪽 정렬**(「가운데 정렬하지 않는다」) · 머리 줄(제목·번호·상태 태그)도 같은 720 기둥 · 묶음 사이 `--s-6` + `1px --line` · 항목 사이 `--s-4` · 라벨-입력 세로 `--s-1` · 칸 폭 select 200 / 짧은 칸 280 / 긴 칸 480 / 폰 전폭 · **필수 표시 `*` 없음**
    - `docs/design/SYSTEM.md` §7-1 — 비활성 버튼은 이유를 옆에 글자로. 진행 중은 라벨 뒤 `…` + 같은 폼의 다른 버튼도 비활성, 300ms 넘으면 상단 2px 진행 바. **스피너 금지**(§5)
    - `docs/design/SYSTEM.md` §7-2 — 서버 검증 오류: 입력 테두리 `--danger` 1px + 입력 아래 `--fs-sm --danger` 한 줄 「원인 · 다음 행동」 + `aria-invalid` + `aria-describedby`. **입력값은 지우지 않는다**
    - `docs/design/SYSTEM.md` §10:911 — 「제출 불가 이유는 `role="alert"`가 아니라 버튼의 `aria-describedby`」
    - `docs/design/SYSTEM.md` §6-5 — 「막힘 = 1차 버튼 비활성 + 빈 칸 전부 나열」. 이유 문자열의 형태가 여기서 온다
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` § 컴포넌트 계약 1 전문 — 골격 · A-H3을 닫는 지점 · 자유 입력 자동완성(D-66) · 다섯 상태 표 · 폰 제출 줄 고정. 이유 문자열 실물 예시 둘: `등록할 수 없음 — 클라이언트 · 기간 2칸` / `등록하지 못했습니다 · 기간 1칸 · 값은 남아 있습니다`
    - `ui/button/Button.tsx`(04-02가 수선한 뒤) — `disabledReason`이 `aria-describedby`로 연결된다. `ui/form`은 문자열만 넘긴다
    - `ui/form-alert/FormAlert.tsx` — 폼 상단 서버 오류 컴포넌트. 이미 있으므로 `ui/form`이 다시 만들지 않고 자리만 둔다
    - `app/(app)/admin/vendors/vendor-form.tsx`(또는 그에 해당하는 파일) — 지금 화면이 `<form>`을 직접 쓰는 모양. **이 페이즈는 이관하지 않지만**(D-47) 새 컴포넌트가 그 자리에 들어갈 수 있는 API인지 확인한다
    - `ui/page-header/PageHeader.tsx` — 머리 줄이 같은 720 기둥에 맞아야 한다는 §6-3 요구를 `ui/form`이 어떻게 만족시킬지 판단하는 근거
    - `eslint.config.mjs` 68행 — `ui`는 `ui`와 `lib`만 import할 수 있다. `ui/form`이 domain을 import하지 않는다
    - `.stylelintrc`(또는 `pnpm lint`의 stylelint 설정) + `test/unit/stylelint-config.test.ts` — `ui/**/*.module.css`가 stylelint 검사 대상이다. 색·서체·radius 리터럴 금지 규칙이 있으면 그 규칙이 이 CSS에 걸린다
  </read_first>
  <behavior>
    - 폼이 `max-width: var(--form-max)`를 쓰고 왼쪽 정렬이다 — `margin: 0 auto`가 없다
    - `<form>`에 `noValidate`가 있다 — 네이티브 `required` 말풍선이 첫 관문이 아니다
    - 칸 폭 변형 셋(select 200 · 짧은 칸 280 · 긴 칸 480)이 선언으로 지정되고 폰에서는 전폭이다
    - 필수 표시 `*`를 렌더하지 않는다
    - 제출이 막히면 1차 버튼이 비활성이고 이유 문자열이 `Button`의 `disabledReason`으로 간다
    - 진행 중에는 같은 폼의 다른 버튼도 비활성이고 라벨 뒤에 `…`가 붙는다 — 스피너가 없다
    - 폰에서 제출 줄이 하단 탭 위에 고정되고 본문 아래 여백이 탭 44 + 제출 줄 높이다
    - CSS에 색·서체·radius 리터럴이 0개다 — 전부 `var(--토큰)`
  </behavior>
  <action>
**칸 폭 변형 API 형태를 고른다**(04-CONTEXT § Claude's Discretion). **변형 클래스가 아니라 prop**으로 간다 — 이유 둘: ① `TextField`가 이미 `numeric` 같은 불리언 prop으로 변형을 받고 있어 같은 결이다 ② 클래스 이름을 화면 코드가 적으면 그것이 다시 「산문으로 남은 계약」이 되어 함정 10으로 돌아간다. 고른 이유를 `FormRow.tsx` 머리 주석에 남긴다. prop 이름은 §6-3의 낱말을 그대로 쓴다(`width: "select" | "short" | "long"`).

`ui/form/Form.tsx` — `<form noValidate>` 래퍼. `max-width: var(--form-max)` + 왼쪽 정렬. props: `onSubmit`(또는 서버 액션 `action`) · `children` · 상단 오류(`FormAlert`를 렌더할 자리). `noValidate`를 **prop으로 끌 수 없게** 한다(`Omit`으로 막는다) — 끌 수 있으면 언젠가 켜진다.

`ui/form/FormGroup`(같은 파일 또는 별 파일) — 묶음. 사이 `--s-6` + `1px --line`.

`ui/form/FormRow.tsx` — 칸 하나의 그릇. `TextField`·`Select`·`<input list>` 어느 것이든 담고 폭 변형을 적용한다. **라벨 배치는 자식 컴포넌트가 이미 맞게 하고 있으므로 다시 구현하지 않는다** — `FormRow`는 폭과 세로 간격만 맡는다. 그 분담을 주석에 적는다.

`ui/form/SubmitRow.tsx` — 제출 줄. props: `submitLabel` · `pending` · `blockedReason?`(빈 칸이 있을 때의 이유 문자열) · `shortcut?`. 동작: `blockedReason`이 있으면 1차 버튼을 `disabled` + `disabledReason={blockedReason}`으로 렌더한다(배선은 Button 안 — F4). `pending`이면 같은 줄의 다른 버튼도 비활성. 폰에서 하단 고정.

`ui/form/Form.module.css` — 전부 `var(--토큰)`. **`var(--form-max)`가 여기 들어가는 것이 함정 10의 「사용처 0」을 닫는 지점**이고 Task 3의 테스트가 그것을 확인한다. 폰 제출 줄 고정의 아래 여백 계산(탭 44 + 제출 줄 높이)을 `calc()`와 토큰으로 쓴다.

**`ui/form`은 `aria-describedby`를 배선하지 않는다.** 문자열만 넘긴다(금지 항목). 그 이유(표 안 삭제 버튼·상세 화면 잠김 버튼은 폼 밖이다)를 `SubmitRow.tsx` 주석에 남긴다.
  </action>
  <verify>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. stylelint가 색·서체·radius 리터럴을 잡거나 boundaries가 `ui`의 domain import를 잡는다. grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('ui/form/Form.module.css','utf8');if(!/var\(--form-max\)/.test(css)){console.error('FORM MAX NOT USED');process.exit(1)}if(/margin:\s*0\s+auto/.test(css)){console.error('FORM IS CENTERED — SYSTEM.md 6-3 FORBIDS IT');process.exit(1)}const tsx=fs.readFileSync('ui/form/Form.tsx','utf8');if(!/noValidate/.test(tsx)){console.error('NO noValidate');process.exit(1)}if(/aria-describedby/.test(fs.readFileSync('ui/form/SubmitRow.tsx','utf8'))){console.error('FORM WIRES aria-describedby — BELONGS IN Button (F4)');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `FORM MAX NOT USED` · `FORM IS CENTERED …` · `NO noValidate` · `FORM WIRES aria-describedby …` 중 하나가 나오고 종료 코드가 1이다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `Form.module.css`가 `var(--form-max)`를 쓰고 가운데 정렬이 아니다
    - `<form>`에 `noValidate`가 있고 prop으로 끌 수 없다
    - 칸 폭 변형이 prop이고 이름이 §6-3의 낱말이며 고른 이유가 주석에 있다
    - 필수 표시 `*`를 렌더하지 않는다
    - `SubmitRow`가 이유 문자열만 넘기고 `aria-describedby`를 쓰지 않는다
    - 진행 중 표현이 라벨 뒤 `…`이고 스피너가 없다
    - CSS에 색·서체·radius 리터럴이 0개다
    - `ui/form`이 domain·repositories를 import하지 않는다
  </acceptance_criteria>
  <done>폼 폭·칸 폭·막힘 이유·네이티브 검증 차단이 산문이 아니라 컴포넌트의 강제가 되었고, `--form-max`가 출하 코드에서 처음 쓰인다</done>
</task>

<task type="auto">
  <name>Task 2: `ui/select` + `ui/input/Autocomplete` — 네이티브 입력 둘이 `TextField`와 같은 라벨 배치·오류·aria를 얻는다</name>
  <files>ui/select/Select.tsx, ui/select/Select.module.css, ui/input/Autocomplete.tsx, ui/input/Autocomplete.module.css, test/unit/ui/form-select-contract.test.ts, test/unit/ui/no-raw-datalist.test.ts</files>
  <read_first>
    - `ui/input/TextField.tsx` + `.module.css` 전문 — **`Select`는 이 파일의 구조를 그대로 복제한다.** `id`-`errorId` 쌍 · `aria-invalid` · `aria-describedby` · `.row`/`.label`/`.field`/`.error` 클래스 구조 · 미디어쿼리 방향
    - `docs/design/SYSTEM.md` §7-2 전문 — 「네이티브 `<select>` 그대로 + 시스템 테두리. 커스텀 드롭다운을 만들지 않는다」(633행) · 높이 `--control-h` 32(폰 40) · 테두리 `1px --line-ui` · 배경 `--bg` · `--fs-base` · radius 0 · focus `outline 2px --focus offset 2px`
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` § 컴포넌트 계약 2 전문 — 기본 폭 200 · 이 페이즈의 사용처 여섯(담당 PM · 팀 · 프로젝트 상태 · 통화 · 견적 차수 · 리저브 종류) · 「자유 입력 자동완성은 이 컴포넌트가 아니다」
    - `docs/HANDOFF.md` §함정 10의 복제 실측 — `.select` 6개 파일 · `.selectLabel` 6개 파일. 「`.selectLabel`은 화면마다 복사본이라 `<select>`가 전폭 블록 라벨을 쓴다. 같은 폼 안에서 `TextField`는 96px 왼쪽 라벨, `<select>`는 위 라벨 — **한 폼에 라벨 배치가 두 가지**다(A-H2)」. **이 컴포넌트가 닫는 것이 정확히 그것이다**
    - `app/(app)/admin/**/*.module.css`의 `.select`·`.selectLabel` 규칙 중 하나 — 지금 무엇이 복제돼 있는지 한 파일만 열어 확인한다(이관은 Phase 7이지만 새 컴포넌트가 그것을 대체할 수 있는 API여야 한다)
    - `docs/design/SYSTEM.md` §7-7의 「폼(지출결의)」 행 — 다섯 상태. `Select`는 EMPTY가 **해당 없음**이다(선택지 0개인 select가 이 페이즈에 없다 — 팀·계급·통화는 시드·고정 집합)
    - `04-CONTEXT.md` **D-66 전문** — 「클라이언트·거래처 자동완성은 `<input list>` + `<datalist>`. `SYSTEM.md:633`의 「네이티브 `<select>` 그대로. 커스텀 드롭다운 만들지 않는다」는 enum select 얘기지 자유 입력 자동완성을 다루지 않는다. 네이티브라 그 규칙과 충돌하지 않고, **키보드 흐름이 끊기지 않아 견적 줄 그리드 셀 안에서도 쓸 수 있다**(UX-05의 「키보드만으로 끝난다」)」
    - `04-CONTEXT.md` **D-83 전문** — 여섯 번째 컴포넌트를 세우는 결정과 그 근거(사용처 넷 · `.selectLabel` 6복제와 같은 양식 · A-H2를 만든 경로). 상속 대상 셋이 명시돼 있다: **`TextField`의 96px 라벨 칸 · `aria-invalid` · `aria-describedby`**. 배선 방식도 명시돼 있다: **목록 요소를 `useId`로 내부 배선**
    - `ui/input/TextField.tsx` 전문 — `Autocomplete`가 같은 디렉터리에 서고 같은 구조를 상속한다. `id`-`errorId` 쌍 형태(`${id}-error`)를 그대로 쓰고, **목록 id는 외부에서 받지 않으므로 `useId`로 만든다**(`Button`이 이유 span에 쓰는 방식과 같은 결 — 04-02 Task 3)
    - `test/unit/ui/no-platform-key-literal.test.ts`(04-02) — `app/**`+`ui/**`를 재귀 순회해 금지 리터럴의 출현을 `파일:줄`로 모으고 **주석을 걷어낸 뒤** 판정하며 「검출기 살아있음」 케이스를 함께 두는 형태. **D-83의 소스 단언이 이 형태를 그대로 복제한다**
    - 이 페이즈의 사용처 넷 — `04-11-PLAN.md` Task 1(프로젝트 폼의 클라이언트, 폭 480) · `04-12-PLAN.md` Task 2(견적 줄 그리드의 거래처 셀) · `04-13-PLAN.md` Task 2(리저브 폼의 클라이언트와 프로젝트 연결). **넷이 요구하는 것이 조금씩 다르다**: 폼 안 두 자리는 96px 라벨 칸이 필요하고 **그리드 셀 안 한 자리는 라벨이 없다.** API가 그 둘을 모두 담아야 한다
  </read_first>
  <behavior>
    - `Select`가 네이티브 `<select>`를 렌더한다 — `role="listbox"`·커스텀 팝업이 없다
    - 라벨이 PC에서 왼쪽 96px 칸, 폰에서 위로 간다 — `TextField`와 픽셀 단위로 같은 규칙이다
    - 오류가 있으면 테두리 `--danger` + 아래 한 줄 + `aria-invalid` + `aria-describedby`가 `${id}-error`를 가리킨다
    - 기본 폭이 200이고 폰에서 전폭이다
    - `id`를 필수 prop으로 받는다(`TextField`와 같다)
    - LOADING일 때 값 자리가 `—`다
    - `Autocomplete`가 네이티브 자유 입력 + 후보 목록을 렌더한다 — 커스텀 팝업·`role="combobox"` 손수 구현이 없다
    - 후보 목록의 id가 컴포넌트 안에서 만들어지고 입력과 연결된다 — 호출자가 그 id를 넘기지도, 목록 요소를 직접 적지도 않는다
    - 라벨이 있으면 `TextField`와 **같은 규칙**으로 PC 96px 왼쪽 · 폰 위이고, 라벨이 없으면(그리드 셀) 라벨 칸 없이 입력만 렌더된다
    - 오류가 있으면 `aria-invalid` + `aria-describedby`가 `TextField`와 같은 형태로 붙는다
    - 같은 화면에 이 컴포넌트가 여럿 있어도 목록 id가 충돌하지 않는다
    - 후보가 0건이면 입력은 그대로 쓸 수 있다 — 자유 입력이므로 목록이 비어도 막지 않는다
    - `app/**` 어디에도 자동완성 목록 요소 리터럴이 없다
  </behavior>
  <action>
`ui/select/Select.tsx`를 만든다. `TextField.tsx`의 구조를 그대로 옮기고 `<input>`을 `<select>`로 바꾼다. props: `id`(필수) · `label` · `error?` · `options: { value: string; label: string }[]` · 나머지는 `SelectHTMLAttributes`를 `Omit<..., "id" | "aria-invalid" | "aria-describedby">`로 받는다(`TextField`와 같은 Omit 집합).

`ui/select/Select.module.css`를 만든다. **`TextField.module.css`의 `.row`·`.label`·`.field`·`.error` 규칙과 미디어쿼리를 그대로** 쓰고 `.select` 규칙만 §7-2 값으로 새로 쓴다. 두 파일이 같은 규칙을 복제하는 것이 함정 10이 지목한 문제이므로, **공통 규칙을 한 곳으로 뽑을 수 있으면 뽑는다**(예: `ui/input/`의 CSS를 `composes`로 참조하거나 공용 `field.module.css`를 만든다). 뽑을 수 없으면(CSS Modules의 `composes`가 이 설정에서 동작하지 않으면) 복제하되 **Task 3의 계약 테스트가 두 파일의 라벨 규칙이 같음을 단언**하게 한다. 어느 쪽을 골랐는지와 이유를 주석에 남긴다.

`ui/input/Autocomplete.tsx`를 만든다(D-83 — 여섯 번째 컴포넌트). `TextField.tsx`와 **같은 디렉터리**에 두는 이유는 상속 대상이 그 파일이기 때문이다. 계약:
- props: `id`(필수 — `TextField`와 같다) · `label?`(**선택** — 그리드 셀에서는 라벨이 없다) · `error?` · `options: string[]`(자유 입력 자동완성의 후보는 값만 있으면 된다 — `Select`의 `{value,label}` 쌍과 다르다. 그 차이를 주석에 적는다) · 나머지는 `InputHTMLAttributes`를 `Omit<..., "id" | "list" | "aria-invalid" | "aria-describedby">`로 받는다. **`list`를 Omit에 넣는 것이 이 컴포넌트의 핵심**이다 — 호출자가 그 속성을 직접 넘길 수 있으면 배선이 조용히 덮인다(`Button`이 `aria-describedby`를 Omit하는 것과 같은 결, 04-02 Task 3)
- 목록 id를 `useId()`로 만들어 **입력의 목록 연결 속성과 후보 목록 요소의 `id` 양쪽에 같은 값**을 쓴다. 조건을 한 곳에 뽑아 두 자리가 갈리지 않게 한다(04-02 Task 3의 `showReason`과 같은 이유 — 참조만 남고 대상이 없는 상태를 만들지 않는다)
- 라벨·오류·aria 배선은 `TextField`의 구조를 그대로 상속한다. `label`이 없으면 라벨 칸 자체를 렌더하지 않고 입력만 낸다(그리드 셀 용례) — 두 모드를 주석에 적는다
- 후보 목록 요소는 이 컴포넌트가 **내부에서** 렌더한다. `options`가 빈 배열이면 목록을 비운 채 렌더하거나 아예 렌더하지 않는다(고른 쪽과 이유를 주석에) — 어느 쪽이든 자유 입력은 계속 된다
- 커스텀 팝업·`role="combobox"` 손수 구현을 하지 않는다(D-66이 네이티브를 고른 이유가 키보드 흐름이다)

`ui/input/Autocomplete.module.css`를 만든다. **`TextField.module.css`의 `.row`·`.label`·`.field`·`.error` 규칙과 미디어쿼리를 그대로 쓴다** — `Select`에 대해 내린 공통 CSS 판단(뽑을지 복제할지)을 여기에도 **같게** 적용한다. 세 파일이 같은 규칙을 각자 복제하는 것이 함정 10 그 자체이므로, 뽑을 수 있으면 세 파일이 하나를 참조하게 한다.

`test/unit/ui/no-raw-datalist.test.ts`를 만든다(D-83의 강제 지점 — `no-platform-key-literal.test.ts`와 **같은 형태**). `app/` 아래 `.ts`·`.tsx`를 재귀로 모아 **주석을 걷어낸 뒤** 자동완성 목록 요소 리터럴이 나오는 줄을 찾고, 위반을 `파일:줄` 목록으로 실패 메시지에 담는다. `ui/input/Autocomplete.tsx`는 감시 범위 밖이다(`app/`만 순회하므로 자연히 제외되고, 그 사실을 주석에 적는다). **「검출기 자체가 죽어있지 않다」 케이스**를 함께 둔다 — 모은 파일이 하나 이상이고, 금지 패턴을 담은 합성 문자열을 판정 함수에 넣으면 실제로 걸린다. 이 테스트는 **이 웨이브에서 첫 실행부터 초록**이다(`app/`에 아직 그 리터럴이 없다) — 초록인 것이 정상이고, 04-11·04-12·04-13이 화면을 만들 때 이 테스트가 RED를 낼 수 있다는 것이 이 테스트의 존재 이유다. 그 사실을 파일 머리 주석에 적는다.

`test/unit/ui/form-select-contract.test.ts`를 만든다(`toast-timer.test.ts` 형태의 소스·CSS 단언). 케이스: ① `Select.tsx`에 `role="listbox"`·`aria-haspopup`·커스텀 팝업 흔적이 없다(네이티브 유지) ② `Select.module.css`와 `TextField.module.css`의 라벨 규칙이 같다 — `--label-w`를 쓰는 미디어쿼리가 둘 다 `min-width: 700px`이고 폰 기본이 세로 쌓기다(정규식으로 `@media (min-width: 700px)` 블록 안에 `--label-w`가 있음을 확인) ③ `Select.tsx`의 Omit 집합이 `TextField.tsx`와 같다 ④ `Select.module.css`에 색·서체·radius 리터럴이 0개다(`#`으로 시작하는 색 · `px` 단위 `font-size` · `border-radius` 리터럴) ⑤ 기본 폭 200이 토큰 또는 §6-3 값으로 있다 ⑥ **`Autocomplete.tsx`가 `useId`를 호출하고 그 값을 목록 연결 속성과 목록 요소 `id` 양쪽에 쓴다**(같은 식별자가 두 자리에 나타난다) ⑦ **`Autocomplete.tsx`의 Omit 집합에 목록 연결 속성이 포함된다** — 호출자가 배선을 덮을 수 없다 ⑧ `Autocomplete.module.css`의 라벨 규칙이 `TextField.module.css`와 같다(②와 같은 판정) ⑨ `Autocomplete.tsx`에 커스텀 팝업 흔적(`role="combobox"`·`aria-expanded` 손수 구현)이 0개다. **이 테스트가 구현 뒤에 오는 이유**: 렌더 테스트 러너(`@testing-library/react`)가 없고 D-45가 그 추가를 금지하므로 컴포넌트 계약을 **소스·CSS 텍스트 단언**으로 검증한다 — 단언 대상 파일이 존재해야 읽을 수 있으므로 테스트 선행이 물리적으로 불가능하다(테스트 선행이 가능한 계층은 순수 모듈과 E2E이고, 이 페이즈는 그 둘을 실제로 선행한다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui</automated>
    <fails_when>종료 코드가 0이 아니다. ②·⑧이 실패하면 미디어쿼리 방향을 반대로 짠 것이다(PC 기본 + 폰 오버라이드) — 04-PATTERNS §6 핵심 계약 (1). ⑥이 실패하면 목록 id를 두 자리에 각각 만든 것이다</fails_when>
    <automated>node -e "const fs=require('fs'),path=require('path');function walk(d){let o=[];let es;try{es=fs.readdirSync(d,{withFileTypes:true})}catch{return o}for(const e of es){const f=path.join(d,e.name);if(e.isDirectory())o=o.concat(walk(f));else if(/\.tsx?$/.test(f))o.push(f)}return o}const bad=[];for(const f of walk('app')){const s=fs.readFileSync(f,'utf8').replace(/\/\*[\s\S]*?\*\//g,'').split('\n');s.forEach((l,i)=>{if(/^\s*\/\//.test(l))return;if(/<datalist/.test(l)||/\blist=\{?[\"']/.test(l))bad.push(f+':'+(i+1))})}if(bad.length){console.error('RAW AUTOCOMPLETE LIST WIRING IN app/**: '+bad.join(', ')+' — USE ui/input/Autocomplete (D-83)');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `RAW AUTOCOMPLETE LIST WIRING IN app/**: …`가 나오고 종료 코드가 1이다 — 화면이 목록 배선을 직접 적었다(D-83 금지 항목). 이 웨이브에서는 `app/`에 해당 화면이 없어 **첫 실행부터 초록**이고, 04-11·04-12·04-13이 화면을 만들 때 이 게이트가 실제로 일한다. 판정은 주석을 걷어낸 줄에서 한다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 네이티브 `<select>`이고 커스텀 팝업 흔적이 0개다
    - 라벨 배치 규칙이 `TextField`와 같고 미디어쿼리 방향이 폰 기본 + PC 전환이다
    - Omit 집합이 `TextField`와 같다
    - 오류 표시가 §7-2 그대로이고 `aria-invalid`/`aria-describedby`가 있다
    - CSS에 색·서체·radius 리터럴이 0개다
    - 공통 CSS를 뽑았는지 복제했는지가 주석에 이유와 함께 있다
    - `ui/input/Autocomplete.tsx`가 있고 `TextField`의 96px 라벨 칸·`aria-invalid`·`aria-describedby`를 상속한다(D-83)
    - 목록 id가 `useId`에서 나와 입력과 목록 요소 양쪽에 같은 값으로 쓰인다 — 조건이 한 곳에 있다
    - Omit 집합에 목록 연결 속성이 있어 호출자가 배선을 덮을 수 없다
    - `label`이 없는 모드(그리드 셀)가 지원되고 두 모드가 주석에 적혔다
    - `app/**`에 자동완성 목록 요소 리터럴이 0개이고 그 검사가 검출기 살아있음 케이스와 함께 있다
    - 커스텀 팝업·`role="combobox"` 손수 구현이 0개다
  </acceptance_criteria>
  <done>`<select>`와 자유 입력 자동완성의 라벨 배치·오류·aria가 `TextField`와 같아져, 한 폼에 라벨 배치가 두 가지인 상태를 컴포넌트가 구조적으로 막고, 자동완성 배선이 네 화면에 복제될 경로가 닫혔다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 사용처 0인 토큰을 잡는 단위 테스트 (D-61)</name>
  <files>test/unit/design-tokens-usage.test.ts</files>
  <read_first>
    - `04-CONTEXT.md` **D-61 전문** — 「사용처 0인 토큰을 잡는 단위 테스트의 감시 범위는 `app/**` + `ui/**`다. `docs/design/system/*.html` 목업은 제외한다 — 실측 결과 `--form-max`는 레포 전체로는 **4곳**(목업 `form-expense.html` 3 · `preview.html` 1)에서 쓰이고 있어, 레포 전체를 세는 테스트는 오늘 바로 통과해 아무것도 잡지 못한다. 출하 코드에서는 0이다」
    - `docs/HANDOFF.md` §함정 10 마지막 문단 — 「**검증도 같이**: `--form-max`처럼 정의만 되고 아무도 안 쓰는 토큰이 또 생기지 않게, 사용처 0인 토큰을 잡는 단위 테스트를 둬라」
    - `docs/design/tokens.css` **전문(196행)** — 감시 대상 토큰의 정본. 토큰 선언 정규식은 `test/unit/design-system-docs.test.ts` 122–136행이 이미 쓰는 `/--[a-z][a-z0-9-]*(?=\s*:)/gi`를 재사용한다
    - `test/unit/design-system-docs.test.ts` 122–136행 — 토큰 실재 검사(SYSTEM.md → tokens.css 방향). **이 테스트는 반대 방향**(tokens.css → 출하 코드)이다. 두 방향이 함께 있어야 정의·문서·사용이 맞물린다는 것을 주석에 적는다
    - `test/unit/leak-scan-coverage.test.ts` 전문 — 디렉터리 재귀 순회 + 위반 목록을 실패 메시지로 + 「검출기 자체가 죽어있지 않다」 케이스. 이 테스트가 그대로 따른다
    - `ui/auth-frame/AuthFrame.module.css` 3행 — `--form-max` **언급이 주석**이다(실측). 주석을 사용처로 세면 이 테스트가 오늘부터 잘못 통과한다 → 주석을 걷어내고 세야 한다
    - Task 1이 만든 `ui/form/Form.module.css` — `--form-max`의 첫 실제 사용처. **이 태스크는 Task 1 뒤에 와야 초록이 된다**
    - `docs/design/tokens.css`에서 **의도적으로 아직 안 쓰는 토큰이 있는지** 확인한다 — `--s-12`(04-UI-SPEC이 「이 페이즈 사용처 없음」이라고 적었다) · `--fs-xl`/`--fs-2xl`(인쇄·외부 수령자·대시보드 KPI 전용) · `--warning`/`--warning-weak`(이 페이즈 사용처 없음) 등. 그런 토큰은 **예외 목록**에 사유와 함께 넣어야 한다. 예외마다 「어느 페이즈가 쓸 것인지」를 한 줄로 적어 목록이 쓰레기통이 되지 않게 한다
  </read_first>
  <behavior>
    - `tokens.css`에 선언된 토큰마다 `app/**` + `ui/**`의 `.ts`·`.tsx`·`.css`에서 `var(--토큰)` 사용을 센다
    - 사용이 0이고 예외 목록에 없는 토큰이 있으면 실패하고 메시지에 토큰 이름 목록이 나온다
    - 주석 안의 언급은 사용으로 세지 않는다
    - `docs/design/system/*.html` 목업은 감시 범위 밖이다
    - 예외 목록의 각 항목에 사유와 예정 페이즈가 있다
    - 검출기가 토큰을 하나 이상 찾았음을 단언하는 케이스가 있다
  </behavior>
  <action>
`test/unit/design-tokens-usage.test.ts`를 만든다. **Task 1·2가 끝난 뒤에 실행해도 RED가 나오는 토큰이 있으면** 그것이 이 테스트의 첫 수확이다 — 그 목록을 보고 예외 목록에 넣을지(사유 + 예정 페이즈) 실제로 쓸지 판단하고, 판단 결과를 SUMMARY에 토큰별로 적는다.

구현:
- `tokens.css`에서 선언 토큰을 뽑는다(`design-system-docs.test.ts`와 같은 정규식)
- `app/`과 `ui/` 아래 `.ts`·`.tsx`·`.css`를 재귀로 모은다. **`docs/`는 순회하지 않는다**(D-61)
- 각 파일에서 **주석을 걷어낸 뒤** `var(--토큰)` 출현을 센다. CSS 주석(`/* */`)과 JS 주석(`//`·`/* */`)을 모두 처리한다 — `AuthFrame.module.css:3`의 주석 언급이 오탐의 실제 사례다
- **별칭 토큰**을 어떻게 셀지 정한다: `--accent: var(--g-700)`처럼 토큰이 토큰을 참조하는 경우 `--g-700`은 `tokens.css` 안에서만 쓰인다. `tokens.css` 자신을 사용처로 세면 명도 단계 전부가 통과해 버리고, 세지 않으면 전부 실패한다. **`tokens.css` 안의 참조는 「다른 토큰이 가리키는 값」으로 별도 집계**해 그런 토큰은 통과시키고, 그 판정 규칙을 주석에 적는다
- 예외 목록을 `const UNUSED_BY_DESIGN: { token: string; why: string; plannedPhase: string }[]`로 두고 각 항목에 사유와 예정 페이즈를 적는다
- 위반을 토큰 이름 배열로 모아 `expect(violations).toEqual([])`로 단언한다 — 실패 메시지가 곧 작업 목록이 된다
- 「검출기 자체가 죽어있지 않다」 케이스: 뽑은 토큰이 서른 개 이상이고 `--form-max`가 사용처 1 이상으로 집계된다(Task 1의 효과를 이 테스트가 확인한다)
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-tokens-usage.test.ts test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 실패 메시지의 토큰 목록이 남은 판단 대상이다 — 예외 목록에 사유와 함께 넣거나 실제로 쓴다. 검출기 케이스가 `--form-max` 사용 0을 보고하면 Task 1의 CSS가 아직 없거나 주석 제거 로직이 실제 사용까지 걷어낸 것이다</fails_when>
    <automated>pnpm test:unit</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('test/unit/design-tokens-usage.test.ts','utf8');if(/docs\/design\/system/.test(s)&&!/제외|exclude/.test(s)){console.error('MOCKUPS MAY BE IN SCAN SCOPE — D-61 EXCLUDES THEM');process.exit(1)}if(!/UNUSED_BY_DESIGN|예외/.test(s)){console.error('NO EXCEPTION LIST');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `MOCKUPS MAY BE IN SCAN SCOPE …` 또는 `NO EXCEPTION LIST`가 나오고 종료 코드가 1이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 감시 범위가 `app/**` + `ui/**`이고 `docs/design/system/*.html`이 제외다
    - 주석 안의 토큰 언급이 사용으로 세지지 않는다
    - `--form-max`가 사용처 1 이상으로 집계된다(Task 1의 효과)
    - 예외 목록의 각 항목에 사유와 예정 페이즈가 있다
    - 별칭 토큰(토큰이 토큰을 가리키는 경우)의 판정 규칙이 주석에 있다
    - 검출기 살아있음 케이스가 있다
    - RED로 나온 토큰마다 판단 결과가 SUMMARY에 적혔다
  </acceptance_criteria>
  <done>정의만 되고 출하 코드에서 안 쓰이는 토큰이 단위 테스트로 잡히고, 그 테스트가 목업을 세지 않아 실제로 무언가를 잡는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 네이티브 검증 → 서버 검증 | 네이티브 `required`가 첫 관문이면 사용자가 서버 검증 메시지를 볼 기회가 없고, 클라이언트만 믿는 코드가 자란다 |
| 컴포넌트 계약 → 화면 구현 | 계약이 prop·구조가 아니라 클래스 이름·산문이면 화면마다 다르게 구현된다(함정 10의 실측 결론) |
| 화면 코드 → 접근성 배선 | 배선이 컴포넌트마다 흩어지면 한 곳을 고쳐도 다른 곳이 안 닫힌다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-35 | Tampering | `noValidate`를 prop으로 끌 수 있어 네이티브 검증이 다시 첫 관문이 됨 | medium | mitigate | `Omit`으로 그 prop을 막고 정규식 게이트가 `noValidate` 존재를 검사한다 |
| T-04-36 | Information Disclosure | 제출 막힘 이유가 보조 기술에 닿지 않음 | medium | mitigate | 배선을 `Button` 안에 두고(04-02 F4) `ui/form`은 문자열만 넘긴다. 정규식 게이트가 폼에 `aria-describedby`가 없음을 검사한다 |
| T-04-37 | Tampering | 클라이언트 폼 검증만 믿고 서버 검증을 생략 | high | mitigate | `ui`는 domain을 import할 수 없어(boundaries) 이 컴포넌트가 판정을 못 한다. 실제 검증은 04-09의 zod 스키마이고 이 컴포넌트는 결과 표시만 한다 |
| T-04-38 | Denial of Service | 폼 제출이 중복 발사되어 같은 행이 둘 생김 | medium | mitigate | `pending`이면 같은 폼의 버튼 전부 비활성(§7-1). 서버 쪽 멱등은 04-09·04-11의 몫이고 이 컴포넌트는 첫 방어선이다 |
| T-04-39 | Tampering | 새 색·간격 리터럴이 CSS에 들어와 디자인 시스템이 갈라짐 | medium | mitigate | stylelint가 `ui/**/*.module.css`를 검사하고 계약 테스트가 리터럴 0을 단언한다. D-61 테스트가 반대 방향(정의만 된 토큰)을 잡는다 |
| T-04-90 | Information Disclosure | 자유 입력 자동완성 배선이 네 화면에 각자 복제되어 라벨 배치·`aria-invalid`·`aria-describedby`가 화면마다 갈리고 보조 기술에 이유가 닿지 않는다 | medium | mitigate | `ui/input/Autocomplete`가 소유 컴포넌트가 되어 `TextField`의 배선을 상속하고 목록 id를 `useId`로 내부에서 만든다. 목록 연결 속성을 `Omit`으로 막아 호출자가 덮지 못하게 하고, `app/**`의 리터럴 0을 단위 테스트가 단언한다(D-83 · D-76과 같은 형태). 실측 선례: `.selectLabel` 6파일 복제가 A-H2를 만들었다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45 새 의존성 0, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). RTL을 들이지 않고 CSS·소스 단언 + 04-11·04-12 E2E로 검증한다. `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint`(eslint + stylelint) · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` 통과
- `pnpm db:reset:test && CI=true pnpm test:e2e` — 이 플랜은 기존 화면을 건드리지 않지만 회귀 확인으로 돌린다
- `git diff docs/design/`가 비어 있다 — 새 토큰·문서 변경 0
- 커밋이 셋이다 — `ui/form` / `ui/select` / 토큰 감시 테스트
- **웨이브 종료 게이트 — 순서가 CLAUDE.md의 프론트엔드 규칙이다(N6): 싼 게이트(`pnpm lint` · `pnpm typecheck` · `pnpm build`) → 독립 DOM 감사 → 수정 → 전체 게이트 한 번.** 전체 게이트를 두 번 돌리지 않기 위한 순서다.
- **독립 DOM 감사(N6)** — 이 플랜의 산출물은 컴포넌트이고 이 웨이브에 **사용처가 0**이라 실측할 DOM이 아직 없다. 그래서 여기서는 **감사 항목을 등록한다**: 이 플랜이 세운 계약 중 실제 DOM으로만 판정 가능한 것(라벨 칸 96px의 실측 폭 · 폰 375에서의 세로 쌓기 전환 · 포커스 링 · 접근 가능한 이름)을 목록으로 만들어 **처음 렌더되는 웨이브(04-11·04-12·04-13)의 감사 목록에 올리고 SUMMARY에 그 목록을 적는다.** 등록하지 않으면 감사가 조용히 사라진다 — Phase 3의 DOM 감사가 결함 7건을 실제로 찾았다
- 웨이브 종료 시 `/review` · **(독립 DOM 감사)** · `/design-review`를 돌린다 — 감사가 `/design-review`보다 **앞**이다. 감사는 **실행자가 아닌 별도 에이전트**가 `CI=true`로 DOM을 실측 판정한다(스크린샷 육안 금지 — CLAUDE.md)
</verification>

<success_criteria>
- 폼 폭·칸 폭·라벨 배치·막힘 이유가 컴포넌트의 강제가 되었다(D-60 · 함정 10)
- 네이티브 `required` 말풍선이 첫 관문이 아니다(A-H3 메커니즘 절반)
- `<select>`의 라벨 배치가 `TextField`와 같다(A-H2 메커니즘 절반)
- 자유 입력 자동완성에 소유 컴포넌트가 생겼고 `app/**`에 그 배선 리터럴이 0개다(D-83)
- `--form-max`가 출하 코드에서 쓰이고, 사용처 0인 토큰을 잡는 테스트가 생겼다(D-61)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 컴포넌트 | `ui/form/Form` · `ui/form/FormGroup` · `ui/form/FormRow` · `ui/form/SubmitRow` · `ui/select/Select` · **`ui/input/Autocomplete`**(D-83) |
| 새 테스트 | `test/unit/design-tokens-usage.test.ts` · `test/unit/ui/form-select-contract.test.ts` · `test/unit/ui/no-raw-datalist.test.ts`(D-83) |
| 새 CSS | `ui/form/Form.module.css` · `ui/select/Select.module.css` · `ui/input/Autocomplete.module.css`(전부 `var(--토큰)`) |
| 새 화면 · 새 표 · 새 토큰 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-07-SUMMARY.md` when done
</output>
