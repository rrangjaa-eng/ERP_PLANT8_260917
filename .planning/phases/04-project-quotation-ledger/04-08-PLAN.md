---
phase: 04-project-quotation-ledger
plan: 08
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - ui/table/column-fold.ts
  - ui/table/Table.tsx
  - ui/table/Table.module.css
  - ui/confirm/Confirm.tsx
  - ui/confirm/Confirm.module.css
  - test/unit/ui/column-fold.test.ts
  - test/unit/ui/table-contract.test.ts
  - test/unit/ui/confirm-contract.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, UX-05, RSV-01]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "열이 P1을 넷 이상 선언하면 개발 모드에서 즉시 드러난다 — 산문 규칙이 아니라 코드가 막는다(D-72 F1)"
    - "열이 `kind: \"money\"`라고 선언하면 §2-4의 포맷·우측 정렬·`tabular-nums`·`nowrap`이 선언만으로 딸려 온다 — 화면 작성자가 열마다 기억하지 않는다(D-72 F3)"
    - "정렬 가능 머리글이 `<button>` + `aria-sort`를 선언만으로 얻는다 — 색이 의미의 유일한 매체가 아니다(D-74)"
    - "읽기용 표가 시각적으로 숨긴 `<caption>`과 `<th scope>`를 구조로 강제한다(A-M3 메커니즘)"
    - "행 안 동작의 접근 가능한 이름이 행 맥락을 담는다 — API가 그것을 요구하고 옵션이 아니다(A-M7 메커니즘)"
    - "읽기용 표 머리글이 흰 바탕이다 — `--g-100` 바탕은 편집 가능하다는 신호이므로 쓰지 않는다"
    - "확인 모달이 네이티브 `<dialog>.showModal()`로 포커스 트랩·가림막·Esc를 얻는다 — 새 런타임 의존성이 0이다(D-68)"
    - "확인 모달의 닫기 버튼이 `aria-label`을 요구한다 — API 필수 값이다(F2)"
  prohibitions:
    - statement: "`ui/table`에 셀 편집 기계장치를 넣어서는 안 된다. 읽기 표는 지금 당장 8곳이 쓰고(`.table` CSS가 8개 파일에 복제돼 있다) 편집 그리드는 견적 줄 한 곳이 쓴다 — 편집 기계장치를 8곳에 지우지 않는다(D-46). 두 컴포넌트가 공유하는 것은 `column-fold.ts` **하나뿐**이다"
      status: resolved
      verification: automated
    - statement: "읽기용 표 머리글에 `--g-100` 바탕을 쓰서는 안 된다. §7-3이 그 색을 **편집 가능하다는 신호**로 규정하므로, 편집할 수 없는 표에 쓰면 신호가 거짓이 된다. 이 신호 분리가 Phase 7의 이관을 CSS 재작성이 아니라 컴포넌트 선택으로 만드는 장치다(D-60 문구 정정)"
      status: resolved
      verification: automated
    - statement: "폰에서 표를 가로 스크롤하거나 카드로 바꾸어서는 안 된다(§7-3 원문). 칸 접기가 카드의 일을 한다"
      status: resolved
      verification: automated
    - statement: "포커스 트랩에 새 런타임 의존성을 두어서는 안 된다. `ui/shell/MoreSheet.tsx`의 주석이 그 확립된 패턴을 적어 뒀다 — 네이티브 `<dialog>`가 포커스 트랩·가림막·Esc를 브라우저 차원에서 제공한다. MoreSheet 자체는 단일 목적이라 재사용하지 않고 **패턴만** 가져온다(D-68)"
      status: resolved
      verification: automated
  artifacts:
    - path: "ui/table/column-fold.ts"
      provides: "열 선언 타입(`kind` 축 + P1/P2/P3 + `sortable`)과 폰 칸 접기 계산. `ui/table`·`ui/grid`가 공유하는 유일한 로직(D-46)"
      exports: ["ColumnDef", "ColumnKind", "Priority", "foldColumns", "assertColumnContract"]
      min_lines: 90
    - path: "ui/table/Table.tsx"
      provides: "§6-1 읽기용 원장 표 — 숨긴 caption · th scope · 흰 머리글 · 정렬 머리글 · 합계 행 · 그룹 머리글 · 다섯 상태"
      exports: ["Table"]
      min_lines: 110
    - path: "ui/confirm/Confirm.tsx"
      provides: "§7-8 확인 모달(PC 480) / 폰 시트. 네이티브 `<dialog>.showModal()` 기반, 닫기 `aria-label` 필수"
      exports: ["Confirm"]
      min_lines: 70
    - path: "test/unit/ui/column-fold.test.ts"
      provides: "P1 상한 dev 경고 · kind별 포맷 · 접기 결과를 고정하는 단위 테스트(순수 모듈이라 진짜 단위 테스트가 가능하다)"
      min_lines: 90
  key_links:
    - from: "ui/table/Table.tsx"
      to: "ui/table/column-fold.ts"
      via: "열 선언을 받아 접기·포맷·정렬 표시를 선언에서 끌어온다"
      pattern: "foldColumns"
    - from: "ui/confirm/Confirm.tsx"
      to: "ui/shell/MoreSheet.tsx"
      via: "네이티브 dialog 패턴(열기 전 활성 요소 저장 → 닫을 때 복귀, 첫 행동 요소 포커스, 명시적 Escape 처리)을 가져온다"
      pattern: "showModal"
    - from: "ui/table/column-fold.ts"
      to: "ui/button/Button.tsx"
      via: "P1 개수 초과 dev 경고가 `Button.tsx:34-39`의 dev warn 형태를 따른다"
      pattern: "NODE_ENV"
---

<objective>
**As a** 프로젝트 목록을 보는 팀장, **I want to** 375px 폰에서도 가로 스크롤 없이 항목·금액·상태 셋을 읽고 열 머리글을 눌러 정렬하고 싶다, **so that** 표 계약(칸 접기 · 숫자 형식 · 정렬 · 접근 가능한 이름)이 화면마다 다르게 구현되지 않는다.

읽기용 표와 확인 모달을 **화면보다 먼저** 만든다(D-60 · D-68). 편집용 그리드는 04-10이 이 플랜의 `column-fold.ts`를 상속해 만든다.

Purpose: `docs/HANDOFF.md` §함정 10의 복제 실측이 `.table` **8개 파일**을 세었고 「목록 우선(§6-1)」은 강제 수단이 없어서 마스터 화면 6개 전부가 어겼다. 그리고 04-UI-SPEC의 검수가 올린 F1·F3·D-74는 전부 「산문으로만 남은 계약」이고, 이 플랜이 그것들을 **열 선언 타입 하나**로 모은다.

Output: 순수 모듈 `column-fold.ts`, 읽기용 `ui/table`, 확인 모달 `ui/confirm`, 단위 테스트 셋.

**이 플랜의 검증 전략**: `column-fold.ts`는 순수 모듈이라 **진짜 단위 테스트**가 가능하다(`ui/permission-grid/PermissionGrid.tsx`가 `resyncCells`를 export해 `test/unit/ui/permission-grid-resync.test.ts`가 직접 부르는 선례). 컴포넌트 쪽은 RTL이 없으므로(D-45 새 의존성 0) **소스·CSS 단언 + 04-11·04-13의 E2E**로 나눈다. 그래서 **계약의 무게를 최대한 순수 모듈로 옮기는 것**이 이 플랜의 설계 원칙이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quotation-ledger/04-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `column-fold.ts` — 세 산문 계약을 열 선언 타입 하나로 (F1 · F3 · D-74)</name>
  <files>ui/table/column-fold.ts, test/unit/ui/column-fold.test.ts</files>
  <read_first>
    - `docs/design/SYSTEM.md` §7-3의 **「폰 전략 = 칸 접기」 블록 전문** — 「모든 열은 우선순위 P1/P2/P3을 선언한다. `<700px`에서 P1만 열로 남고 P2는 각 행 아래 **접힌 줄** 하나(`--fs-sm --muted`, `라벨 없이 값 · 값 · 값`, 순서는 열 순서)로 들어가고 P3은 숨는다 … P1은 **항목(문자 1개) + 금액(숫자 1개) + 상태 또는 행동(1개)**, 최대 3열. 그 이상 P1을 두면 시스템 위반」 · 「합계 행의 부제는 폰에서 숨긴다 … 그룹 머리글·합계 행은 폰에서도 그대로. 합계는 P1 금액 열만」 · 「표를 카드로 바꾸지 않는다」
    - `docs/design/SYSTEM.md` **§2-4 전문** — 숫자·날짜·상태 형식 전부. 원화 정수 + 천 단위 쉼표 · 단위 기호 없음 · 열 머리글에 「(원)」도 쓰지 않는다 · 음수는 하이픈 마이너스(**괄호 금지 · 붉은색 금지**) · 0은 `0`, 없음은 `—`, 계산 불가는 `계산 불가 · 환율 없음` · 외화 두 줄 병기 · 날짜 `2026-09-18`, **표 안 같은 해는 `09-18`**, 기간 `09-12 ~ 09-16` · 문서 번호는 서버 서식 그대로, 모노 아님, `tabular-nums` · 모든 숫자 칸 **우측 정렬 · `tabular-nums` · `white-space: nowrap`** · 합계 행만 700
    - `docs/design/SYSTEM.md` §7-5(`:707`·`:708`) — 상태 색 배정과 「표 상태 열은 테두리 태그가 아니라 **색 글자만**」
    - `docs/design/SYSTEM.md` §9:901 — 허용 아이콘 목록에 `arrow-up`/`arrow-down` 포함. §10:912 — 「색은 의미의 유일한 매체가 아니다」. §10:914 — 3차 버튼은 `<button>`
    - `docs/design/SYSTEM.md` §11:932 — `/design-review` 판정에 「P1 열 3개 이하」가 있다. **이 모듈이 첫 번째 그물이고 그 리뷰가 두 번째다**
    - `ui/button/Button.tsx` **34–39행** — dev warn 선례 전문. `process.env.NODE_ENV !== "production"` 조건 + `console.warn` + 주석이 「런타임 동작은 바꾸지 않고 개발 중에만 알린다」를 적었다. **F1이 따를 정확한 형태**
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` § 컴포넌트 계약 3의 「열 선언 타입」·「폰 칸 접기」 블록 전문 + § D-72/D-74 표 — F1·F3·D-74의 강제 지점 지정
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` 화면 계약 5·7·8의 열 목록 셋 — 이 모듈이 실제로 받을 선언의 실물이다. **리저브(화면 계약 8)가 P1 4열 후보로 걸린 사례**가 F1의 존재 이유이고, 그 해소(잔액을 P2로)가 이 모듈의 검사를 통과하는지 테스트로 확인해야 한다
    - `ui/permission-grid/PermissionGrid.tsx` + `test/unit/ui/permission-grid-resync.test.ts` — **`.tsx`에서 export한 순수 함수를 `test/unit/**/*.test.ts`가 직접 import해 부르는 선례.** 이 모듈은 `.ts`라 더 간단하다
    - `ui/next-turn/build-next-turn-view.ts` — 「뷰 데이터를 만드는 순수 모듈」 선례
    - `domain/money/index.ts`(04-03) — **이 모듈은 금액을 계산하지 않고 표시만 한다.** `ui`는 domain을 import할 수 없으므로(`eslint.config.mjs` 68행) 서버가 계산해 넘긴 숫자를 받아 문자열로 만든다. 그 경계를 파일 머리 주석에 적는다
  </read_first>
  <behavior>
    - P1 열이 넷 이상인 선언을 넘기면 개발 모드에서 경고가 나고 메시지에 P1 열 이름이 나온다 — 프로덕션에서는 동작이 바뀌지 않는다
    - P1 열이 셋 이하면 조용하다
    - `kind: "money"`인 열의 값 `12400000` → `12,400,000` · `0` → `0` · `null` → `—` · 음수 `-30000` → `-30,000`(괄호 아님)
    - `kind: "money"` 열이 우측 정렬 · `tabular-nums` · `nowrap` 속성을 딸려 온다
    - 외화가 있는 금액 값은 두 줄 구조(1행 원화 정수, 2행 `USD 4,400.00 @1,318.1818`)로 나온다
    - 환율이 없는 금액 값은 `계산 불가 · 환율 없음`이다
    - `kind: "date"` 값이 같은 해면 `09-18`, 다른 해면 `2026-09-18`
    - `kind: "status"`가 §7-5 색 글자 변형을 딸려 온다 — 테두리 태그가 아니다
    - `<700px`에서 P1만 열로 남고 P2가 접힌 줄 하나로, P3은 사라진다 — 접힌 줄의 값 순서가 **열 선언 순서**다
    - 700–1023에서는 P3만 숨는다
    - 서버가 보내지 않은 열(선언에 없는 열)은 애초에 계산에 들어오지 않는다 — 클라이언트가 숨기는 분기가 없다
    - `sortable: true`인 열이 현재 정렬 상태에 따라 `aria-sort` 값(`ascending`/`descending`/`none`)과 아이콘 방향을 딸려 온다
  </behavior>
  <action>
`test/unit/ui/column-fold.test.ts`를 **먼저** 쓴다(RED 확인). behavior 열두 줄 전부를 케이스로 옮긴다. 추가로 **04-UI-SPEC 화면 계약 5·7·8의 실제 열 선언 셋**을 픽스처로 넣어 ① 셋 다 P1이 셋 이하로 통과한다 ② 리저브 선언을 순진한 형태(잔액을 P1로)로 바꾸면 경고가 난다 — 그 케이스가 F1의 존재 이유를 테스트로 박는다.

`ui/table/column-fold.ts`를 만든다:
- `export type ColumnKind = "money" | "date" | "text" | "status"` — F3의 축
- `export type Priority = 1 | 2 | 3`
- `export type ColumnDef<Row> = { key: string; header: string; kind: ColumnKind; priority: Priority; sortable?: boolean; get(row: Row): unknown }` — `get`을 선언에 넣어 화면이 셀 렌더 함수를 따로 쓰지 않게 한다. 최종 형태는 실제 렌더 요구에 맞춰 조정하되 **`kind`·`priority`가 필수**인 것은 계약이다
- `export function assertColumnContract(columns): void` — P1 개수 > 3이면 `process.env.NODE_ENV !== "production"`에서 `console.warn`. 메시지에 P1 열 이름과 §7-3의 상한, 그리고 「그 이상 P1을 두면 시스템 위반」 인용을 담는다. **`throw`가 아니라 `warn`을 고른다** — `Button.tsx:34-39`가 같은 판단을 했고 런타임 동작을 바꾸지 않는 것이 그 선례의 핵심이다. 고른 이유를 함수 위 주석에 적는다
- `export function formatCell(kind, value, opts): CellView` — §2-4의 포맷 전부. `CellView`가 `{ text: string; secondLine?: string; align: "left" | "right"; numeric: boolean; nowrap: boolean }` 형태로, 정렬·`tabular-nums`·`nowrap`이 **`kind`에서 파생**되게 한다(F3). 날짜의 「같은 해」 판정에 쓸 기준 연도를 `opts`로 받는다 — 함수를 순수하게 유지하려면 `new Date()`를 안에서 부르지 않는다
- `export function foldColumns(columns, viewport: "phone" | "tablet" | "desktop"): { visible: ColumnDef[]; folded: ColumnDef[]; hidden: ColumnDef[] }` — 접기 계산. 브레이크포인트 숫자(700 · 1024)를 이 모듈의 상수로 두고 CSS와 어긋나지 않게 주석에 `tokens.css` 머리의 브레이크포인트 주석을 인용한다
- `export function sortHeaderView(column, current): { ariaSort: "ascending" | "descending" | "none"; icon: "arrow-up" | "arrow-down" | null }` — D-74. `aria-sort` 값과 §9 허용 아이콘 이름을 선언에서 만든다
- 파일 머리 주석에 ① 이 모듈이 `ui/table`·`ui/grid`가 공유하는 **유일한** 로직이라는 것(D-46) ② 금액을 **계산하지 않고 표시만** 한다는 경계 ③ F1·F3·D-74가 이 파일에 모인 이유(함정 10)를 적는다
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/column-fold.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 리저브 P1 4열 케이스가 경고를 내지 않으면 검사가 선언을 받는 지점에 없다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. `ui`가 domain을 import하면 boundaries가 잡고, `money-boundary`가 이 파일의 산술을 잡으면 금액을 계산하고 있는 것이다(표시만 해야 한다). grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - P1 넷 이상 선언이 개발 모드 경고를 내고 메시지에 열 이름이 있다
    - 경고가 `throw`가 아니고 프로덕션 동작을 바꾸지 않으며 고른 이유가 주석에 있다
    - `kind`에서 정렬·`tabular-nums`·`nowrap`·포맷이 파생된다 — 화면이 열마다 지정하지 않는다
    - 음수가 하이픈 마이너스이고 괄호·붉은색이 없다
    - 외화 두 줄과 「계산 불가 · 환율 없음」이 케이스로 있다
    - 표 안 같은 해 날짜가 `09-18`이다
    - 접힌 줄의 값 순서가 열 선언 순서다
    - `aria-sort` 값과 §9 아이콘 이름이 선언에서 나온다
    - 이 모듈이 `new Date()`를 안에서 부르지 않는다 — 순수 함수다
    - 04-UI-SPEC 화면 계약 5·7·8의 실제 열 선언이 픽스처로 있고 셋 다 통과한다
  </acceptance_criteria>
  <done>P1 상한·숫자 형식·정렬 표시 세 계약이 열 선언 타입 하나에 모여 선언만으로 딸려 오고, 그 전부가 순수 모듈 단위 테스트로 고정됐다</done>
</task>

<task type="auto">
  <name>Task 2: `ui/table` — 읽기용 원장 표 (§6-1 · A-M3 · A-M7 · D-74)</name>
  <files>ui/table/Table.tsx, ui/table/Table.module.css, test/unit/ui/table-contract.test.ts</files>
  <read_first>
    - Task 1이 만든 `column-fold.ts`의 최종 export 형태
    - `docs/design/SYSTEM.md` §7-3의 **「구조」 블록 전문**(읽기용·편집용 공통) — 머리글 `--fs-sm --muted` 600 + 아래 `1px --line-strong` + sticky · **읽기용 표(목록)는 흰 바탕** · 행 아래 `1px --line` · hover `--surface` · 높이 최소 36 PC / 44 폰, **고정 높이 금지** · 그룹 머리글 행 · 합계 행 위 `2px --line-strong` `--fs-base` 700 · **세로선 없음** · 첫 칸(행 번호) `--fs-xs --faint` 폭 28 · 열 폭(문자 `auto`, 숫자 `ch` 고정, 항목명 `keep-all` 줄바꿈, 거래처 한 줄 말줄임)
    - `docs/design/SYSTEM.md` §7-3의 「칸 수 가변(정보 노출표) 대응」 블록 — 「열은 서버가 계급에 따라 **아예 보내지 않는다**. 클라이언트에서 숨기지 않는다(누수 방지) … 「차익」「손익」 열이 빠져도 합계 행은 남은 열만으로 성립한다. 빈 칸 자리를 `—`로 채우지 않는다」
    - `docs/design/SYSTEM.md` §6-1 — 목록 = 원장 · 필터 줄 한 줄 · **페이지네이션 대신 `더 보기 50건` 3차 버튼** · 열 머리글 클릭 = 정렬 · 그룹 머리글
    - `docs/design/SYSTEM.md` §10 — 시각적으로 숨긴 `<caption>` · `<th scope="col">`/`<th scope="row">` · §7-13의 「좌표를 문장으로」 규약
    - `docs/design/SYSTEM.md` §7-7의 「표(목록·편집)」 행 — 다섯 상태. **읽기용 표는 SUCCESS가 해당 없음**이다(행동 결과를 표시할 자리가 없다)
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` § 컴포넌트 계약 3 전문 — 특히 「행 동작의 접근 가능한 이름 → A-M7을 닫는다」: `aria-label="{행 이름} · {동작}"`, 예 `aria-label="26001 아이오닉9 쇼케이스 · 견적 열기"`. **API가 이것을 요구한다(옵션이 아니다)**
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` § D-74 — 「열 선언에 `sortable` + 현재 정렬 상태를 두고 `<th>` 안을 `<button>`으로, `aria-sort` + §9 `arrow-up/down` 16px이 **선언만으로** 딸려 오게 한다」
    - `ui/list-empty/ListEmpty.tsx` — EMPTY 한 줄 + 다음 한 수. **표의 EMPTY가 이것을 재사용한다**(새로 만들지 않는다)
    - `ui/status-tag/StatusTag.tsx` — `kind` 5종 × `variant` `tag`/`text`. 상태 열은 전부 `variant="text"`다(§7-5:708)
    - `app/(app)/admin/**/*.module.css`의 `.table` 규칙 중 하나 — 지금 8개 파일에 복제된 것 중 하나를 열어 무엇이 복제됐는지 확인한다(이관은 Phase 7이지만 새 컴포넌트가 그것을 대체할 수 있는 API여야 한다)
    - `ui/permission-grid/PermissionGrid.tsx` — 이 저장소에서 표 모양 컴포넌트를 쓴 선례. **§7-13 체크박스 매트릭스는 §7-3과 분리된 별개 컴포넌트**(D-40)이므로 재사용하지 않고 형태만 본다
    - `docs/design/tokens.css` 110–120행 — `--row-min` 36 · `--cell-pad-y` 6px(폰 10px) · `--cell-pad-x` 8px · `--z-sticky`
  </read_first>
  <behavior>
    - `<table>`에 시각적으로 숨긴 `<caption>`이 있고 그 문구를 API가 필수로 받는다
    - 열 머리글이 `<th scope="col">`이고 행 머리글이 성립하면 `<th scope="row">`다
    - 머리글 바탕이 흰색이다 — `--g-100`이 아니다
    - `sortable` 열의 `<th>` 안이 `<button>`이고 `aria-sort`가 현재 상태를 담고 §9 화살표 아이콘이 붙는다
    - 행 안 3차 버튼·링크의 접근 가능한 이름이 `{행 이름} · {동작}` 형태이고, 그 이름을 만드는 값을 API가 필수로 받는다
    - 합계 행이 위 `2px --line-strong` + 700이고 `--g-100` 바탕이 없다
    - 서버가 보내지 않은 열은 렌더에서 아예 빠지고 그 자리를 `—`로 채우지 않는다
    - EMPTY가 `ListEmpty` 한 줄로 본문 자리를 대체한다 — 표 뼈대를 남기지 않는다
    - LOADING이 머리글·합계 뼈대 + `--surface` 행 3개이고 반짝임이 없다
    - SUCCESS 상태가 없다
    - CSS에 세로선·고정 행 높이·가로 스크롤·카드 전환이 없다
  </behavior>
  <action>
`ui/table/Table.tsx`를 만든다. props의 필수 항목이 계약이다: `caption`(필수 — A-M3) · `columns: ColumnDef<Row>[]` · `rows: Row[]` · `rowName(row): string`(필수 — A-M7의 `{행 이름}`을 만든다) · `sort?: { key: string; direction: "asc" | "desc" }` · `onSort?` · `groupBy?` · `totals?` · `state?: "loading" | "error" | "partial"` · `empty?: { message: string; action?: ... }` · `rowActions?`. `caption`과 `rowName`을 **선택 prop으로 두지 않는다** — 옵션이면 화면이 빠뜨리고 그것이 A-M3·A-M7의 원인이다.

렌더 시 `assertColumnContract(columns)`를 부른다(F1의 강제 지점이 실제로 호출되는 자리). 셀은 `formatCell(column.kind, value, opts)`의 결과를 쓰고 **화면이 포맷을 지정할 경로를 주지 않는다**(F3).

정렬 머리글은 `sortHeaderView`의 결과로 `<th aria-sort>` + 안쪽 `<button>` + §9 아이콘을 렌더한다. 아이콘은 `components/icons/`의 인라인 SVG 규약을 따른다 — 그 디렉터리에 `arrow-up`/`arrow-down`이 없으면 §9 허용 목록에서 가져와 더한다.

`ui/table/Table.module.css`를 만든다. §7-3 「구조」 블록의 모든 값을 토큰으로 쓴다. **세로선 없음 · 고정 행 높이 없음 · `overflow-x` 없음**. 폰 칸 접기는 `foldColumns`의 결과로 **렌더 단계에서** 하고 CSS의 `display: none`으로 숨기지 않는다 — 숨기면 접힌 줄의 값 순서를 보장할 수 없고 §7-3이 요구하는 「접힌 줄 하나」가 안 나온다. 그 판단을 주석에 적는다. 뷰포트 판정을 클라이언트에서 해야 하므로 `Table`이 클라이언트 컴포넌트가 되는지, 아니면 CSS 컨테이너 쿼리로 서버 렌더를 유지할 수 있는지 판단하고 고른 쪽과 이유를 주석에 남긴다 — **서버 렌더를 유지할 수 있으면 그쪽이 낫다**(목록은 RSC로 오는 데이터다).

`test/unit/ui/table-contract.test.ts`를 만든다(소스·CSS 단언). 케이스: ① `caption`·`rowName`이 필수 prop이다(타입 정의에 `?`가 없다) ② `Table.module.css`에 `--g-100`이 없다 ③ `overflow-x`·`border-left`/`border-right`(세로선)·`height:` 고정값이 없다 ④ `assertColumnContract` 호출이 렌더 경로에 있다 ⑤ `aria-sort`가 `<th>` 속성으로 있다 ⑥ `ListEmpty`를 import해 EMPTY에 쓴다(새로 만들지 않았다) ⑦ 색·서체·radius 리터럴이 0개다 ⑧ `role="grid"`·`gridcell`·`contentEditable`·키보드 이동 코드가 **없다**(편집 기계장치를 읽기 표에 넣지 않는다 — D-46 금지 항목). **이 테스트가 구현 뒤에 오는 이유**: 렌더 테스트 러너(`@testing-library/react`)가 없고 D-45가 그 추가를 금지하므로 컴포넌트 계약을 **소스·CSS 텍스트 단언**으로 검증한다 — 단언 대상 파일이 존재해야 읽을 수 있으므로 테스트 선행이 물리적으로 불가능하다(테스트 선행이 가능한 계층은 순수 모듈과 E2E이고, 이 페이즈는 그 둘을 실제로 선행한다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui</automated>
    <fails_when>종료 코드가 0이 아니다. ⑧이 실패하면 편집 기계장치가 읽기 표에 들어간 것이다 — 그것은 `ui/grid`(04-10)의 몫이다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `caption`·`rowName`이 필수 prop이다
    - 머리글이 흰 바탕이고 CSS에 `--g-100`이 없다
    - `sortable` 열이 `<th aria-sort>` + `<button>` + §9 화살표를 얻는다
    - 렌더 경로에서 `assertColumnContract`가 호출된다
    - 폰 접기가 렌더 단계 계산이고 CSS `display:none`이 아니며 그 판단이 주석에 있다
    - CSS에 세로선·고정 행 높이·가로 스크롤이 없다
    - EMPTY가 `ListEmpty` 재사용이다
    - SUCCESS 상태가 없다
    - 편집 기계장치(`role="grid"`·키보드 이동)가 0개다
    - 서버 렌더 유지 여부의 판단과 이유가 주석에 있다
  </acceptance_criteria>
  <done>읽기용 원장 표가 숨긴 caption·th scope·흰 머리글·정렬 머리글·행 맥락 이름을 구조로 강제하고, 편집 기계장치를 담지 않는다</done>
</task>

<task type="auto">
  <name>Task 3: `ui/confirm` — 네이티브 `<dialog>` 하나로 모달·시트 (D-68 · F2)</name>
  <files>ui/confirm/Confirm.tsx, ui/confirm/Confirm.module.css, test/unit/ui/confirm-contract.test.ts</files>
  <read_first>
    - `ui/shell/MoreSheet.tsx` **전문** — 특히 8–20행 주석: 「포커스 트랩에 새 런타임 의존성을 두지 않는다 … 네이티브 `<dialog>.showModal()`이 포커스 트랩 · 가림막(`::backdrop`) · Esc-닫힘을 브라우저 차원에서 제공한다. 여기서는 (1) 열기 전 활성 요소를 저장했다가 닫을 때 되돌리는 것과 (2) 열릴 때 첫 행동 요소에 포커스하는 것만 맡는다. 명시적으로도 Escape를 처리해 두어(멱등 — `HTMLDialogElement.close()`는 이미 닫힌 다이얼로그에 다시 호출해도 안전하다) 네이티브 동작에만 기대지 않는다」 — **이 세 가지를 그대로 가져온다.** `dialogRef`·`firstItemRef`·`previouslyFocusedRef` 세 ref와 `useEffect` 구조도 같다
    - `ui/shell/MoreSheet.module.css` — `<dialog>`·`::backdrop`·시트 올라옴 모션의 실제 CSS. `--scrim` 사용법과 `translateY` 200ms `--ease-sheet`
    - `docs/design/SYSTEM.md` **§7-8 전문** — 모달(PC): 폭 480(`--modal-w`) · `--bg` + `2px --line-strong` · 제목 `--fs-lg` = **실제 동작** · 부제 `--fs-sm --muted` 한 줄 · 하단 오른쪽 2차 `취소 Esc` + 1차 행동 · **폼을 넣지 않는다**. 시트(폰): 하단에서 올라옴 · 최대 85vh · 위 `2px --line-strong` · 머리·행동 줄 고정 본문만 스크롤 · 버튼 높이 44 · 1차가 2차의 2배 폭. 둘 다 `--scrim` 뒤 · Esc로 닫힘 · 포커스 트랩 · 열릴 때 첫 행동 요소에 포커스 · 닫힐 때 원래 요소로 복귀 · **아이콘은 닫기(x)만**
    - `docs/design/SYSTEM.md` §9:902 — 「아이콘 단독 버튼은 `aria-label` 필수」. **F2가 이것을 API 필수 값으로 만든다**
    - `docs/design/SYSTEM.md` §7-8:739 — 「목록형 시트가 비면 §7-7 EMPTY 문구를 본문에 그대로 적용한다」. 이 페이즈의 목록형 시트는 04-11의 「이전 프로젝트에서 복사」 하나이고, 그것도 이 컴포넌트의 골격을 쓴다
    - `docs/design/SYSTEM.md` §5 — 모션: 시트 `translateY` 200ms `--ease-sheet` / 모달 `opacity` 120ms. `prefers-reduced-motion` 처리
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 9 전문** + Copywriting Contract의 Destructive 세 행 — 사용처 셋의 실물 문구: 견적 줄 삭제(제목 `견적 줄 삭제` · 부제 `{번호} {항목명} · {거래처}` · 결과 한 줄 `이 줄이 사라집니다 · 되돌릴 수 없습니다` · 2차 `취소 Esc` + 1차 `삭제`) · 완료(정산) · 미수주로 닫기
    - `ui/kv-list/KvList.tsx` — §7-8 시트 상세 `dl`. **폰 행 시트가 이것을 재사용한다**(새로 만들지 않는다)
    - `ui/button/Button.tsx`(04-02 수선 뒤) — 모달의 2차·1차 버튼. `shortcut`에 `Esc`를 넘길 때 `format-shortcut.ts`를 거칠 필요가 있는지 판단한다(Esc는 플랫폼 공통이라 그대로 적는다 — 04-02 Task 1이 §7-9에 그 규칙을 적었다)
    - `03-OPEN-ITEMS.md`의 A-M1 — 「위험 행동 확인이 §7-8 모달·시트가 아님 + 포커스 소실」. 이 컴포넌트가 **메커니즘 절반**을 닫고 관리자 6화면 이관은 Phase 7이다
  </read_first>
  <behavior>
    - 열면 네이티브 `<dialog>.showModal()`이 호출되어 포커스 트랩·가림막·Esc가 브라우저에서 온다
    - 열릴 때 첫 행동 요소에 포커스가 가고, 닫힐 때 열기 전 활성 요소로 복귀한다
    - Esc를 명시적으로도 처리한다 — 네이티브 동작에만 기대지 않는다
    - 닫기 버튼의 `aria-label`이 **필수 prop**이다 — 없으면 컴파일되지 않는다
    - 아이콘이 닫기(x) 하나뿐이다
    - PC에서 폭 480(`--modal-w`), 폰에서 하단 시트 최대 85vh
    - 폼을 자식으로 받지 않는다 — 제목·부제·결과 한 줄·행동 둘만
    - 목록형 본문을 받을 수 있고, 본문이 비면 EMPTY 한 줄이 본문 자리를 대체한다
    - `prefers-reduced-motion`에서 모션이 없다
    - 이미 닫힌 상태에서 닫기를 다시 호출해도 안전하다(멱등)
  </behavior>
  <action>
`ui/confirm/Confirm.tsx`를 만든다. **MoreSheet의 패턴만 가져오고 MoreSheet 자체는 재사용하지 않는다**(그것은 `moreMenu`/`accountGroup`/`adminMenu`를 받는 단일 목적 컴포넌트다 — D-68). 세 ref와 `useEffect` 구조, 명시적 Escape 처리, 멱등 `close()`를 그대로 옮긴다.

props: `open` · `onClose` · `title`(실제 동작 — §7-8) · `subtitle?` · `consequence?`(결과 한 줄) · `closeLabel: string`(**필수** — F2) · `confirmLabel` · `onConfirm` · `cancelLabel?`(기본 `취소`) · `variant?: "modal" | "sheet" | "auto"` · `children?`(목록형 본문) · `emptyMessage?`(목록형 본문이 비었을 때 — §7-8:739). **`children`에 폼을 넣는 것을 타입으로 막을 수는 없으므로** 그 금지를 props JSDoc과 컴포넌트 머리 주석에 §7-8 원문 인용으로 적고, Task 3의 계약 테스트가 04-11·04-12·04-13의 사용처에서 `<form>`이 들어가지 않았는지 검사할 수 있게 그 검사를 **04-11·04-12·04-13의 verify에 넘긴다**(이 플랜 시점에는 사용처가 없다). **04-11이 목록에 있는 이유**: 위 read_first의 §7-8:739 항목이 적은 대로 이 페이즈의 목록형 시트는 04-11의 「이전 프로젝트에서 복사」 하나이고, 그것이 `emptyMessage` 계약의 유일한 실제 사용처다 — 그 플랜을 목록에서 빼면 이 컴포넌트에서 가장 먼저 쓰이는 계약이 아무 데서도 검사되지 않는다.

`ui/confirm/Confirm.module.css` — 모달·시트 두 변형. `--modal-w` · `--line-strong` · `--scrim` · `--ease-sheet` · `--touch-min`. 시트에서 1차가 2차의 2배 폭. `@media (prefers-reduced-motion: reduce)`에서 transition 제거. 색·서체·radius 리터럴 0개.

`variant: "auto"`의 판정(PC 모달 / 폰 시트)을 CSS 미디어쿼리로 할 수 있으면 그쪽으로 한다 — JS 뷰포트 판정을 쓰면 서버·클라이언트 첫 렌더가 갈릴 수 있다. 고른 방법과 이유를 주석에 남긴다.

`test/unit/ui/confirm-contract.test.ts`를 만든다(소스·CSS 단언). 케이스: ① `showModal`을 호출한다 ② `previouslyFocused` 저장·복귀 코드가 있다 ③ 명시적 `Escape` 처리가 있다 ④ `closeLabel`이 필수 prop이다(타입에 `?`가 없다) ⑤ `aria-label`이 닫기 버튼에 붙는다 ⑥ 새 런타임 의존성을 import하지 않는다(`focus-trap`·`react-modal` 류 0개) ⑦ CSS에 `--modal-w`가 쓰이고 색·서체·radius 리터럴이 0개다 ⑧ `prefers-reduced-motion` 블록이 있다 ⑨ 아이콘이 닫기 하나뿐이다(SVG·아이콘 import가 하나). **이 테스트가 구현 뒤에 오는 이유**: 렌더 테스트 러너(`@testing-library/react`)가 없고 D-45가 그 추가를 금지하므로 컴포넌트 계약을 **소스·CSS 텍스트 단언**으로 검증한다 — 단언 대상 파일이 존재해야 읽을 수 있으므로 테스트 선행이 물리적으로 불가능하다(테스트 선행이 가능한 계층은 순수 모듈과 E2E이고, 이 페이즈는 그 둘을 실제로 선행한다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui</automated>
    <fails_when>종료 코드가 0이 아니다. ⑥이 실패하면 포커스 트랩 라이브러리를 들인 것이다 — D-45 새 의존성 0 위반</fails_when>
    <automated>git diff --name-only package.json pnpm-lock.yaml</automated>
    <fails_when>출력이 비어 있지 않다 — 새 의존성이 들어왔다(D-45 위반)</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 네이티브 `<dialog>.showModal()` 기반이고 새 런타임 의존성이 0이다
    - 열기 전 활성 요소 저장·복귀와 첫 행동 요소 포커스가 있다
    - 명시적 Escape 처리가 있고 닫기가 멱등이다
    - `closeLabel`이 필수 prop이고 닫기 버튼 `aria-label`에 쓰인다
    - 아이콘이 닫기 하나뿐이다
    - PC 480 · 폰 시트 85vh이고 변형 판정 방법의 이유가 주석에 있다
    - 「폼을 넣지 않는다」 금지가 JSDoc과 머리 주석에 §7-8 인용으로 있다
    - 목록형 본문이 비면 EMPTY 한 줄이 본문 자리를 대체한다
    - `prefers-reduced-motion` 블록이 있다
    - 색·서체·radius 리터럴이 0개다
  </acceptance_criteria>
  <done>위험 행동 확인이 §7-8 골격(포커스 트랩·가림막·Esc·복귀)을 네이티브 `<dialog>` 하나로 얻고, 닫기 `aria-label`이 API 필수 값이 되었다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 열 선택 → 클라이언트 렌더 | 정보 노출은 서버가 열을 **아예 보내지 않는 것**으로 강제된다. 클라이언트가 숨기는 분기를 두면 응답에 값이 남아 누수 경로가 생긴다 |
| 열 선언 → 표시 형식 | 형식이 선언에서 파생되지 않으면 화면마다 다르게 포맷하고, 그것이 금액 표시가 갈라지는 경로다 |
| 시각 신호 → 편집 가능 여부 | `--g-100` 머리글이 「편집 가능」을 뜻하는 계약이므로 읽기 표에 쓰면 사용자가 편집을 시도한다 |
| 모달 열림 → 포커스 | 포커스가 트랩되지 않으면 배경 요소가 키보드로 조작되고, 닫힐 때 복귀하지 않으면 키보드 사용자가 위치를 잃는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-40 | Information Disclosure | 클라이언트가 열을 숨기는 방식으로 정보 노출을 구현 | high | mitigate | `foldColumns`가 **선언에 있는 열만** 계산한다. 서버가 보내지 않은 열은 선언에 없으므로 렌더 경로에 들어오지 않는다. 실제 열 선택은 04-09의 DTO 투영이고, 이 컴포넌트에 계급 분기가 없음을 계약 테스트가 확인한다 |
| T-04-41 | Spoofing | 읽기 전용 표가 편집 가능한 것처럼 보여 사용자가 편집을 시도 | medium | mitigate | 읽기 표 머리글 흰 바탕 강제 + CSS에 `--g-100` 0을 단언. 편집 기계장치가 이 컴포넌트에 없음도 단언한다 |
| T-04-42 | Information Disclosure | 행 동작의 접근 가능한 이름이 행 맥락 없이 전부 같아 보조 기술 사용자가 어느 행을 조작하는지 모름 | medium | mitigate | `rowName`을 필수 prop으로 두고 `{행 이름} · {동작}` 형태를 API가 요구한다(A-M7) |
| T-04-43 | Tampering | 위험 행동 확인 모달이 포커스를 트랩하지 않아 배경 버튼이 눌림 | medium | mitigate | 네이티브 `<dialog>.showModal()`이 브라우저 차원에서 트랩한다. 명시적 Escape 처리와 복귀 코드를 계약 테스트가 확인한다 |
| T-04-44 | Denial of Service | P1 열이 넷 이상이라 375px에서 가로 넘침으로 화면을 못 씀 | medium | mitigate | `assertColumnContract`의 dev 경고가 첫 그물, `/design-review`(§11:932)가 두 번째다. 리저브 선언이 실제로 걸린 사례가 테스트 케이스로 박혔다 |
| T-04-45 | Tampering | 새 런타임 의존성(포커스 트랩 라이브러리)이 들어와 D-45가 깨짐 | medium | mitigate | 계약 테스트가 import를 검사하고 `git diff package.json pnpm-lock.yaml`이 비어 있음을 verify가 확인한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45 새 의존성 0, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). 그리드도 확인 모달도 직접 만든다. `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint`(eslint + stylelint) · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` 통과
- `pnpm db:reset:test && CI=true pnpm test:e2e` — 기존 화면 회귀 확인
- `git diff --name-only package.json pnpm-lock.yaml`이 비어 있다
- `git diff docs/design/`가 비어 있다 — 새 토큰 0
- 커밋이 셋이다 — `column-fold` / `ui/table` / `ui/confirm`
- 웨이브 종료 시 `/review` · `/design-review`를 돌린다
</verification>

<success_criteria>
- P1 상한·숫자 형식·정렬 표시가 열 선언 하나에서 파생된다(F1 · F3 · D-74)
- 읽기용 표가 caption·th scope·흰 머리글·행 맥락 이름을 구조로 강제한다(A-M3 · A-M7 메커니즘)
- 편집 기계장치가 읽기 표에 없다(D-46)
- 확인 모달이 네이티브 `<dialog>` 하나로 §7-8 골격을 얻고 새 의존성이 0이다(D-68)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 모듈 | `ui/table/column-fold.ts` — `ColumnDef` · `ColumnKind` · `Priority` · `assertColumnContract` · `formatCell` · `foldColumns` · `sortHeaderView` |
| 새 컴포넌트 | `ui/table/Table` · `ui/confirm/Confirm` |
| 새 테스트 | `test/unit/ui/column-fold.test.ts` · `test/unit/ui/table-contract.test.ts` · `test/unit/ui/confirm-contract.test.ts` |
| 새 아이콘 | `components/icons/`의 `arrow-up`·`arrow-down`(§9 허용 목록, 없을 때만) |
| 새 화면 · 새 표 · 새 토큰 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-08-SUMMARY.md` when done
</output>
