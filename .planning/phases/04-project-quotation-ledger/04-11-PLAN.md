---
phase: 04-project-quotation-ledger
plan: 11
type: execute
wave: 4
depends_on: ["04-07", "04-08", "04-09"]
files_modified:
  - app/(app)/projects/page.tsx
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/project-form.tsx
  - app/(app)/projects/project-list.tsx
  - app/(app)/projects/copy-picker.tsx
  - app/(app)/projects/projects.module.css
  - test/integration/leak-scan.test.ts
  - test/e2e/projects.spec.ts
  - test/e2e/mobile-projects.spec.ts
autonomous: true
requirements: [PROJ-01, PROJ-05, UX-04, FX-01]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PM이 프로젝트를 등록하면 번호가 붙고 목록에 보인다 — 등록·목록·상세 진입이 한 화면에서 끝난다(PROJ-01)"
    - "등록 폼이 기본 진입에 없고 필터 줄의 등록 행동이 그것을 연다 — 목록이 화면이다(§6-1 · D-70)"
    - "필수 칸이 비면 네이티브 말풍선이 아니라 제출 버튼이 이유를 말하고 그 이유가 버튼에 연결된다"
    - "목록 합계가 집계 쿼리 1회로 나오고 차익 열이 빠진 계급에서도 합계 행이 남은 열만으로 성립한다"
    - "375px에서 P1 3열만 남고 가로 스크롤이 없다"
    - "이전 프로젝트에서 복사가 목록 골라내기로 동작하고, 복사할 건이 없으면 EMPTY 한 줄이 본문 자리에 들어간다(PROJ-05)"
    - "모든 서버 액션이 등록 파일에 한 줄씩 있어 누수 스캔이 덮는다"
    - "E2E 스펙이 만든 데이터를 스스로 치운다 — 다른 스펙의 폰 폭 단언을 깨지 않는다"
  prohibitions:
    - statement: "등록 폼을 목록 위에 상시 렌더해서는 안 된다. §6-1이 「목록이 화면이고 등록은 목록 머리글의 행동」을 규정하고, 관리자 마스터 화면 여섯이 이것을 어겼다가 `DECISIONS.md` 2026-09-21 기록으로 검색 파라미터 토글로 바뀌었다. 같은 메커니즘(`?new=1` · `?editId=`)을 쓴다(D-70)"
      status: resolved
      verification: automated
    - statement: "새 라우트(`/projects/new`)를 만들어서는 안 된다. 목록 필터 상태를 폼과 주고받는 배선이 생기고, 같은 라우트의 검색 파라미터 토글은 이미 그 상태를 쥐고 있다(`DECISIONS.md` 2026-09-21 기록의 버린 대안 3)"
      status: resolved
      verification: automated
    - statement: "E2E에서 변경 클릭 직후 `page.goto()`를 해서는 안 된다. 이동의 SELECT가 서버 액션의 UPDATE 커밋보다 먼저 읽는다 — 먼저 같은 화면에서 단언하고 그 다음 이동한다(`archive.spec.ts:33,42` · `vendor-edit.spec.ts:51`이 올바른 형태)"
      status: resolved
      verification: judgment
    - statement: "E2E가 만든 데이터를 남겨서는 안 된다. `mobile-vendors.spec.ts`가 계좌번호 있는 거래처를 남겨 다른 스펙의 375px 넘침 단언을 깼다(473>375, 여섯 번에 한 번 빨강). 스펙이 만든 것을 스펙이 치운다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/(app)/projects/page.tsx"
      provides: "§6-1 목록 화면 — 필터 줄 · 그룹 머리글 · 합계 행 · `?new=1`/`?editId=` 토글"
      contains: "searchParams"
      min_lines: 60
    - path: "app/(app)/projects/actions.ts"
      provides: "등록·수정 서버 액션. `authedActionClient` + zod + `revalidatePath`"
      exports: ["createProjectAction", "updateProjectAction"]
      min_lines: 50
    - path: "app/(app)/projects/actions.registry.ts"
      provides: "액션 등록 — 누수 스캔이 이 파일을 통해 액션을 본다"
      contains: "registerAction"
    - path: "test/e2e/projects.spec.ts"
      provides: "등록 → 번호 부여 → 목록 표시 종단 흐름 + 필수 칸 막힘 + 복사 골라내기"
      min_lines: 80
    - path: "test/e2e/mobile-projects.spec.ts"
      provides: "375px에서 P1 3열·가로 스크롤 없음·접힌 줄 DOM 실측"
      min_lines: 50
  key_links:
    - from: "app/(app)/projects/page.tsx"
      to: "ui/table/Table.tsx"
      via: "목록이 읽기용 표를 쓰고 열 선언에 kind·priority·sortable을 담는다"
      pattern: "Table"
    - from: "app/(app)/projects/project-form.tsx"
      to: "ui/form/Form.tsx"
      via: "폼이 720 기둥·칸 폭 변형·막힘 이유를 컴포넌트에서 얻는다"
      pattern: "SubmitRow"
    - from: "app/(app)/projects/actions.ts"
      to: "domain/projects/index.ts"
      via: "액션이 도메인을 부르고 리포지토리를 직접 부르지 않는다"
      pattern: "createProject"
---

<objective>
**As a** 기획 PM, **I want to** 프로젝트를 등록하면 번호가 붙어 목록에 바로 보이고 폰에서도 그 목록을 읽고 싶다, **so that** 이 시스템의 돈 뼈대가 실제로 쓸 수 있는 첫 화면을 갖는다.

이 페이즈의 **첫 화면**이다. 04-07·04-08의 컴포넌트와 04-09의 도메인을 배선한다.

Purpose: ROADMAP Phase 4 기준 1이 「PM이 프로젝트를 등록·목록·상세로 다루고, 번호는 설정된 서식으로 자동 부여된다 … 클라이언트·거래처는 자동완성되고, 이전 프로젝트·견적 줄 복사와 기본값 채움으로 반복 입력이 없다. 목록 합계는 집계 쿼리 1회로 계산되고 … 목록 응답은 p99 500ms 이내다」를 요구한다.

Output: 목록 화면 · 등록/수정 폼 · 복사 골라내기 · 서버 액션과 등록 · E2E 둘.

**상세 화면(`[id]`)은 04-12다.** 이 플랜은 목록 라우트 하나만 만들고 상세로 가는 링크만 둔다. 두 플랜이 `app/(app)/projects/actions.ts`를 공유하므로 웨이브를 갈랐다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quotation-ledger/04-07-SUMMARY.md
@.planning/phases/04-project-quotation-ledger/04-08-SUMMARY.md
@.planning/phases/04-project-quotation-ledger/04-09-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 등록 → 번호 → 목록 — 한 경로를 화면까지 뚫는다</name>
  <files>app/(app)/projects/page.tsx, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/project-form.tsx, app/(app)/projects/project-list.tsx, app/(app)/projects/projects.module.css, test/integration/leak-scan.test.ts, test/e2e/projects.spec.ts</files>
  <read_first>
    - `app/(app)/projects/page.tsx` **현재 전문(약 23행)** — `requireSession()` + `PageHeader` + `ListEmpty`. 주석이 「Phase 4가 이 라우트에 원장 데이터를 올린다」고 적어 뒀고 **WR-07**(인증 검사를 이 페이지가 직접 한다 — 레이아웃의 `requireSession()`에 기대지 않는다, 근거가 `next/dist/docs`)이 그 자리에 있다. **그 주석과 호출을 지우지 않는다**
    - `app/(app)/admin/vendors/page.tsx` — `?new=1`·`?editId=` 토글을 실제로 구현한 화면. `searchParams`를 어떻게 읽고 폼을 조건부로 렌더하는지 전문을 읽는다. **Next 16 App Router의 `searchParams`가 Promise인지 확인한다**(이 버전은 학습 데이터와 다르다 — `node_modules/next/dist/docs/`의 해당 가이드를 읽는다)
    - `app/(app)/admin/vendors/actions.ts` **1–78행 전문** — 서버 액션의 정본 형태: `"use server"` · `authedActionClient.schema(z.object({...})).action(async ({ parsedInput, ctx }) => {...})` · `revalidatePath` · **`import "./actions.registry"`**(03-03 선례 — 등록을 별도 파일로 분리해 누수 스캔이 이 파일을 직접 import하지 못하게 한다)
    - `app/(app)/admin/vendors/actions.registry.ts` 전문 — `registerAction({ name, menu, action, dtoName })`. `action`은 `"write"`/`"view"`, `dtoName: null` 허용
    - `test/unit/leak-scan-coverage.test.ts` + `test/integration/leak-scan.test.ts` — **등록 파일을 side-effect import로 나열하는 목록**이 `leak-scan.test.ts`에 있고 `leak-scan-coverage.test.ts`가 그 목록의 완전성을 강제한다. 실측 교훈: 등록 파일 9개 중 7개만 import돼 있었고 빠진 둘이 하필 권한표·노출표였다. **새 등록 파일을 그 목록에 더한다**
    - `app/(app)/admin/vendors/vendor-form.tsx` — 폼 화면의 현재 형태. `useAction`(next-safe-action) 사용법과 오류 표시 배선. **이 페이즈는 이관하지 않지만**(D-47) 새 폼이 같은 배선 관례를 따라야 한다
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 5 전문** — 필터 줄 구성 · 그룹 머리글(기본 상태) · **열과 우선순위 전체**(번호 P2 text · 프로젝트명 P1 text · 클라이언트 P2 text · 팀·담당 PM P3 text · 기간 P2 date · 견적 P1 money · 실행가 P3 money · 차익 P3 money(정보 노출표) · 상태 P1 status → **P1 3열**) · 합계 행 · **상태 4종 색 표(D-65)** · `?new=1` 토글(D-70) · 다섯 상태 · 성능 계약
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 6 전문** — 필수 칸 표 일곱(클라이언트 `<input list>` 480 · 프로젝트명 480 · 담당 PM select 200 · 팀 select 200 · 시작일·종료일 280 × 2 · 사전 견적 280 선택 · 상태 select 200 **수주중·진행 둘만**) · 자리표시자는 형식 예만 · 금액 입력 쉼표 자동 · 번호 입력 칸 없음(D-42) · 1차 버튼 라벨(등록 모드 `프로젝트 등록` / 수정 모드 `프로젝트 저장`)
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` Copywriting 표 전문 — 이 화면의 모든 문구. 특히 EMPTY(`등록된 프로젝트가 없습니다 · 프로젝트 등록`) · 필터 결과 0(`조건에 맞는 건이 없습니다 · 필터 지우기`) · 조회 실패 · 폼 SUCCESS(`등록됨 · 26001` + 3차 `프로젝트 열기` + 토스트)
    - `docs/design/DECISIONS.md`의 2026-09-21 「관리자 마스터 화면의 등록 폼을 검색 파라미터 토글로 전환」 기록 전문 — 이 화면이 따르는 메커니즘의 근거와 버린 대안 셋
    - `ui/status-tag/StatusTag.tsx` — 상태 열은 `variant="text"`, 제목 옆은 `variant="tag"`(§7-5:708)
    - `test/e2e/vendor-edit.spec.ts` **전문(약 60행)** — E2E의 정본 형태: `loginAsSysadmin` 헬퍼 + `createFixtureUser({ roleId })` + `getByLabel`/`getByRole` 셀렉터 + `Date.now() % 100000` 유니크 이름 + `page.locator("tr", { hasText: name })` 행 스코프. **51행이 「변경 클릭 뒤 같은 화면에서 먼저 단언」의 올바른 형태**다
    - `test/e2e/archive.spec.ts` 33·42행 — 같은 패턴의 두 번째 선례
    - `test/e2e/fixtures.ts` 전문 — `createFixtureUser`. PM 권한 픽스처가 필요하면 `SEED_ROLES`에서 기획 PM 계급 id를 쓴다
    - `docs/HANDOFF.md` §함정 10의 복제 실측 — `<form>` 직접 작성 9개 파일. **이 화면은 그 아홉에 열 번째를 더하지 않는다**
  </read_first>
  <behavior>
    - 기본 진입(`/projects`)에 폼이 없고 목록만 있다
    - 필터 줄의 등록 행동이 `?new=1`을 열고 폼이 나타난다
    - 필수 칸을 비우면 1차 버튼이 비활성이고 옆에 빈 칸 이름이 나열된다 — 브라우저 말풍선이 뜨지 않는다
    - 전부 채워 제출하면 번호가 부여되고 버튼 자리에 `등록됨 · {번호}`가 나온다
    - 목록에 그 프로젝트가 번호와 함께 보인다
    - 상태 열이 색 글자이고 테두리 태그가 아니다
    - 합계 행에 견적·실행가·차익이 있다
    - 차익 정보가 숨겨진 계급으로 보면 차익 열이 **응답에 없고** 합계 행이 남은 열만으로 성립한다
    - 열 머리글을 누르면 정렬되고 `aria-sort`가 바뀐다
    - 등록된 프로젝트가 0건이면 EMPTY 한 줄이 본문 자리를 대체한다
    - 필터 결과가 0건이면 `조건에 맞는 건이 없습니다 · 필터 지우기`다
  </behavior>
  <action>
`app/(app)/projects/page.tsx`를 고친다. **`requireSession()` 호출과 WR-07 주석을 그대로 둔다.** `searchParams`를 읽어 `?new=1`·`?editId=`에 따라 폼을 조건부 렌더하고, 기본 진입에는 목록만 둔다. Next 16의 `searchParams` 형태를 `node_modules/next/dist/docs/`에서 확인하고 그대로 쓴다.

`app/(app)/projects/project-list.tsx` — `ui/table`의 `Table`을 쓰는 목록. 열 선언을 화면 계약 5의 표 그대로 만들고 `kind`·`priority`·`sortable`을 담는다. `caption`과 `rowName`을 채운다(필수 prop). 행 안 3차 「견적 열기」의 접근 가능한 이름이 `{번호} {프로젝트명} · 견적 열기` 형태다. 필터 줄은 §6-1대로 **한 줄**이고 필터 패널·사이드바를 만들지 않는다. `더 보기 50건` 3차 버튼(페이지네이션 아님).

**차익 열은 서버가 안 보낸다.** 04-09의 DTO 투영이 `visible()` 판정으로 그 필드를 빼므로, 화면은 **DTO에 그 키가 있는지로 열 선언을 만든다** — `if (role === ...)` 분기를 화면에 두지 않는다(ADMN-03). 그 판정 코드를 한 곳에 두고 주석에 「클라이언트에서 숨기지 않는다」를 적는다.

`app/(app)/projects/project-form.tsx` — `ui/form`의 `Form`·`FormRow`·`SubmitRow` + `TextField`·`Select`·`<input list>`. 화면 계약 6의 칸 일곱을 폭 변형과 함께 배치한다. 클라이언트·거래처는 `<input list>` + `<datalist>`(D-66), 담당 PM·팀·상태는 `Select`. 상태 select의 선택지는 **수주중·진행 둘만**이다. 번호 입력 칸을 두지 않는다(D-42). 막힘 이유 문자열을 `SubmitRow`의 `blockedReason`으로 넘긴다 — 형태는 화면 계약 1의 실물 예시(`등록할 수 없음 — 클라이언트 · 기간 2칸`)를 따른다.

`app/(app)/projects/actions.ts` — `authedActionClient` + zod. `createProjectAction`·`updateProjectAction`이 **도메인 함수만** 부르고 리포지토리를 직접 부르지 않는다. `revalidatePath("/projects")`. `import "./actions.registry"`를 파일 머리에 둔다(선례 그대로).

`app/(app)/projects/actions.registry.ts` — 액션 둘을 `registerAction`으로 등록한다. `menu`는 `projects`, `action`은 `"write"`, `dtoName`은 `"ProjectDto"`.

`test/integration/leak-scan.test.ts`의 등록 파일 import 목록에 새 파일을 더한다. **`test/unit/leak-scan-coverage.test.ts`가 이것을 강제하므로 빠뜨리면 단위 단계에서 잡힌다.**

`test/e2e/projects.spec.ts`를 만든다. `vendor-edit.spec.ts`의 형태를 그대로 따른다. 시나리오: 로그인 → `/projects?new=1` → 칸 일부만 채우고 1차 버튼이 비활성이며 이유가 보임 → 전부 채워 제출 → **같은 화면에서 `등록됨 · {번호}`를 먼저 단언** → 그 다음 목록으로 이동해 행이 보임을 단언. **변경 클릭 직후 `page.goto()`를 하지 않는다**(금지 항목). 스펙 끝에 만든 프로젝트를 치우는 정리를 둔다 — 정리 방법(도메인 함수 직접 호출 또는 화면 경로)을 고르고 이유를 주석에 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 등록 파일이 `leak-scan.test.ts`의 import 목록에 없다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 액션의 DTO가 정보 노출 항목에 매핑되지 않았다(ADMN-03)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/projects.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. `CI=true`가 프로덕션 빌드를 쓴다 — 로컬 dev 통과는 완료 신호가 아니다(CLAUDE.md)</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. `require-action-client`가 서버 액션의 클라이언트 사용을 검사한다. grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 기본 진입에 폼이 없고 `?new=1`이 그것을 연다
    - 새 라우트를 만들지 않았다 — `app/(app)/projects/` 아래 `new/` 디렉터리가 없다
    - `requireSession()`과 WR-07 주석이 그대로 있다
    - 필수 칸 막힘이 브라우저 말풍선이 아니라 버튼 옆 이유다
    - 등록 성공이 버튼 자리에 `등록됨 · {번호}`로 보인다
    - 열 선언에 `kind`·`priority`·`sortable`이 있고 P1이 셋이다
    - 차익 열 판정이 DTO 키 존재 여부이고 계급 분기가 화면에 없다
    - `caption`·`rowName`이 채워졌다
    - 액션이 도메인만 부르고 등록 파일에 한 줄씩 있다
    - `leak-scan.test.ts` import 목록에 새 파일이 있다
    - E2E가 변경 클릭 뒤 같은 화면에서 먼저 단언하고 만든 데이터를 치운다
    - `<form>`을 직접 작성하지 않고 `ui/form`을 쓴다
  </acceptance_criteria>
  <done>PM이 프로젝트를 등록하면 번호가 붙어 목록에 보이고, 그 경로가 `CI=true` E2E로 증명된다</done>
</task>

<task type="auto">
  <name>Task 2: 필터·그룹·정렬·합계·폰 칸 접기 — 목록을 원장으로 완성한다</name>
  <files>app/(app)/projects/page.tsx, app/(app)/projects/project-list.tsx, app/(app)/projects/projects.module.css, test/e2e/mobile-projects.spec.ts</files>
  <read_first>
    - Task 1이 만든 목록의 최종 형태
    - `docs/design/SYSTEM.md` §6-1 전문 — 필터 줄 한 줄 · 그룹 머리글 · 정렬 · `더 보기 50건`
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` 화면 계약 5의 필터 줄 구성 — `프로젝트 125` · `[상태 ▾]` · `[내 것만]` · `[2026 ▾]` · 검색 · 1차 `프로젝트 등록`
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` 화면 계약 5의 **상태 4종 색 표(D-65)** — 수주중 `--muted` · 진행 `--accent` · 완료(정산) `--success` · 미수주 `--muted`. 「수주중과 같은 토큰이지만 §7-5:708이 표 상태 열에 **색 글자만** 쓰라고 해서 **글자가 구분한다**」
    - `docs/design/SYSTEM.md` §1-3 — accent 다섯 곳. 「진행 = `--accent`」가 여섯 번째 용처가 **아닌** 이유(§7-5가 상태 태그에 이미 배정했다 — F7)
    - `ui/table/column-fold.ts`(04-08) — `foldColumns`·`assertColumnContract`. 목록 열 선언이 **P1 3열 상한을 통과해야 한다**
    - `test/e2e/mobile-vendors.spec.ts` — **폰 스펙의 형태 선례이자 함정의 출처**다. 이 스펙이 계좌번호 있는 거래처를 남겨 다른 스펙의 375px 넘침 단언을 깼다(473>375). **정리를 반드시 둔다**
    - `test/e2e/mobile-admin-master-list-first.spec.ts` · `test/e2e/mobile-list-empty.spec.ts` — 폰 프로젝트가 잡는 파일 이름 패턴(`mobile-*.spec.ts`)과 DOM 실측 단언 형태
    - `playwright.config.ts`의 `MOBILE_SPEC_PATTERN`과 두 프로젝트 정의 — 폰 375 전용 스펙은 파일명 접두어로 구분되고 desktop 프로젝트가 그것을 제외한다. **새 파일 이름이 그 패턴에 맞아야** 별도 등록 없이 폰 프로젝트가 잡는다
    - `.planning/ROADMAP.md` Phase 4 기준 1의 성능 문구 — 「목록 응답은 p99 500ms 이내다」. **04-VALIDATION.md가 이것을 수동 검증으로 분류**했다(부하 특성이라 단위 테스트로 못 고정한다. 125건 규모에서는 자명하나 수치가 기준에 못박혀 있다) — 시드된 개발 DB에서 재고 결과를 `04-VERIFICATION.md`에 적는다
    - `04-CONTEXT.md` § Claude's Discretion의 「목록 p99 500ms를 어떻게 증명할지」 — 재량 항목이다
  </read_first>
  <behavior>
    - 필터 줄이 한 줄이고 필터 패널·사이드바가 없다
    - 기본 그룹이 상태이고 그룹 머리글이 네 상태로 나눈다
    - 열 머리글 클릭이 정렬을 바꾸고 `aria-sort`가 따라온다
    - 합계 행이 견적·실행가·차익이고 위 `2px --line-strong` + 700이다
    - 상태 색이 D-65 표대로이고 표 상태 열은 색 글자다
    - 375px에서 열이 셋(프로젝트명·견적·상태)만 남고 나머지가 접힌 줄 하나로 내려가거나 숨는다
    - 375px에서 가로 스크롤이 없다 — 문서 폭이 375를 넘지 않는다
    - 접힌 줄의 값 순서가 열 선언 순서다
    - 태블릿 700–1023에서 P3만 숨는다
  </behavior>
  <action>
필터 줄을 §6-1대로 **한 줄**로 만든다. 상태·내 것만·연도는 검색 파라미터로 다루고(폼 토글과 같은 메커니즘) 검색은 서버 쿼리로 간다. 건수(`프로젝트 125`)는 집계 쿼리의 결과를 쓴다 — 별도 COUNT 쿼리를 추가로 돌리지 않는다면 합계 집계에 함께 담는다.

그룹 머리글을 상태 기준으로 만든다. `Table`의 `groupBy`를 쓰고, 그룹 순서가 상태 진행 순서(수주중 → 진행 → 완료(정산) → 미수주)가 되게 한다 — 04-05의 `sortOrder`와 어긋나지 않게 한다.

정렬은 `Table`의 `sort`/`onSort`를 검색 파라미터로 왕복시킨다. 서버 정렬이므로 `orderBy`가 인덱스를 쓰는지 확인하고, 안 쓰면 04-05가 만든 인덱스 목록에 행을 더한다(`docs/ARCHITECTURE.md`도 함께 — 300행 상한 확인).

상태 색을 `StatusTag variant="text"`로 렌더한다. D-65 표의 매핑을 **한 곳**에 두고(화면마다 복제하지 않는다) 04-12가 같은 매핑을 쓰게 한다 — 그 자리를 `app/(app)/projects/` 안의 공용 모듈로 두거나 `ui/status-tag`의 `kind` 매핑으로 올릴지 판단하고 고른 쪽과 이유를 주석에 적는다.

`test/e2e/mobile-projects.spec.ts`를 만든다(파일 이름이 `mobile-*` 패턴이어야 폰 프로젝트가 잡는다). **DOM 실측**으로 단언한다 — 스크린샷 육안이 아니다(CLAUDE.md): ① 보이는 `<th>`가 셋(행 번호 열을 세는지 판단해 기대값을 정한다) ② `document.documentElement.scrollWidth <= 375` ③ 접힌 줄이 존재하고 그 텍스트의 값 순서가 열 선언 순서다 ④ 행 높이가 44 이상이다. **스펙이 만든 프로젝트를 스펙이 치운다**(금지 항목 — `mobile-vendors.spec.ts`의 실측 실패).

**p99 500ms**는 수동 검증이다(04-VALIDATION.md). 시드된 개발 DB에서 목록을 재는 절차를 SUMMARY에 적고 결과를 `04-VERIFICATION.md`에 남긴다 — 이 태스크에서 자동 게이트를 만들지 않는다. 125건 규모에서 자명하다는 판단과 근거를 함께 적는다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/mobile-projects.spec.ts test/e2e/projects.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. `scrollWidth > 375`가 나오면 열이 넷 이상 남았거나 숫자 칸이 꺾이지 않고 넘친 것이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test:e2e</automated>
    <fails_when>종료 코드가 0이 아니다. 다른 스펙의 375px 단언이 깨지면 이 스펙의 정리가 빠진 것이다(`mobile-vendors.spec.ts`가 남긴 것과 같은 증상)</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/column-fold.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 목록 열 선언이 P1 상한을 넘겼다면 dev 경고가 이미 났을 것이다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 필터 줄이 한 줄이고 패널·사이드바가 없다
    - 그룹 머리글이 상태 기준이고 순서가 상태 진행 순서다
    - 정렬이 서버 쿼리이고 `aria-sort`가 따라온다
    - 합계 행이 셋이고 차익 열 없는 계급에서도 성립한다
    - 상태 색 매핑이 한 곳에 있고 표 상태 열이 색 글자다
    - 375px에서 `scrollWidth <= 375`이고 열이 셋이다
    - 접힌 줄의 값 순서가 열 선언 순서다
    - 폰 스펙이 만든 데이터를 치운다
    - 전체 E2E 스위트가 통과한다
    - p99 측정 절차와 결과가 SUMMARY·`04-VERIFICATION.md`에 있다
  </acceptance_criteria>
  <done>목록이 필터·그룹·정렬·합계를 갖춘 원장이 되고, 375px에서 가로 스크롤 없이 P1 3열로 읽힌다</done>
</task>

<task type="auto">
  <name>Task 3: 이전 프로젝트에서 복사 — 골라내기와 확정적 EMPTY (PROJ-05)</name>
  <files>app/(app)/projects/copy-picker.tsx, app/(app)/projects/project-form.tsx, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/e2e/projects.spec.ts</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` PROJ-05(30행) — 「이전 프로젝트·견적 줄을 복사해 새로 만들 수 있고, 반복 입력 칸은 기본값이 채워진다」. **견적 줄 복사는 04-12의 차수 복사**이고 이 태스크는 프로젝트 칸 복사다
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 6의 「이전 프로젝트 복사」 블록 전문** — 폼 머리 줄의 3차 `이전 프로젝트에서 복사` · §6-3의 「바꾸기」와 같은 **목록 골라내기**(PC 모달 480 / 폰 시트, 검색 한 칸 첫 포커스, 목록 행 = 번호 `--fs-xs --faint` · 이름 · 클라이언트 부제) · 골격은 `ui/confirm`과 같은 §7-8
    - 같은 블록의 **EMPTY 계약** — §7-8(`SYSTEM.md:739`) 「목록형 시트가 비면 §7-7 EMPTY 문구를 본문에 그대로 적용」 → `복사할 이전 프로젝트가 없습니다 · 빈 폼으로 등록`. **첫 프로젝트에서는 0행이 확정적**이므로 선택 사항이 아니다. 시트·모달은 **닫히지 않고** 본문 자리에 이 한 줄이 들어가며, 「빈 폼으로 등록」은 시트를 닫고 빈 폼으로 돌아가는 3차 행동이다
    - 같은 블록의 검색 결과 0(`조건에 맞는 건이 없습니다 · 검색어 지우기`)과 ERROR(`프로젝트 목록을 불러오지 못했습니다 · 다시 시도` — 시트·모달은 닫히지 않는다)
    - `ui/confirm/Confirm.tsx`(04-08) — 목록형 본문과 `emptyMessage` prop. **`closeLabel`이 필수**다(F2). **「폼을 넣지 않는다」 금지**(§7-8) — 골라내기는 목록이지 폼이 아니므로 그 금지에 걸리지 않는다는 판단을 주석에 적는다
    - `domain/projects/index.ts`(04-09)의 `copyFromProject` — 입력 초안을 돌려주고 저장하지 않는다
    - `docs/design/SYSTEM.md` §7-8 — 모달·시트 골격과 「열릴 때 첫 행동 요소에 포커스(승인 / 사유 칸 / **검색**)」
    - `test/e2e/vendor-edit.spec.ts` — E2E 형태
  </read_first>
  <behavior>
    - 폼 머리 줄의 3차 행동이 골라내기를 연다
    - 열릴 때 검색 칸에 포커스가 간다
    - 목록 행을 고르면 폼의 반복 입력 칸이 채워지고 골라내기가 닫힌다
    - 복사된 값이 서버가 만든 초안이다 — 화면이 이전 프로젝트 행을 그대로 베끼지 않는다
    - 복사할 프로젝트가 0건이면 본문 자리에 EMPTY 한 줄이 들어가고 **모달·시트는 닫히지 않는다**
    - 「빈 폼으로 등록」이 골라내기를 닫고 빈 폼으로 돌아간다
    - 검색 결과가 0건이면 `조건에 맞는 건이 없습니다 · 검색어 지우기`다
    - 조회 실패면 본문에 오류 한 줄 + 2차 `다시 시도`이고 모달·시트가 닫히지 않는다
    - 번호는 복사되지 않는다 — 등록 시 새로 부여된다(D-42)
  </behavior>
  <action>
`app/(app)/projects/copy-picker.tsx`를 만든다. `ui/confirm`의 골격을 목록형으로 쓴다 — `closeLabel`을 채우고(F2) 본문에 검색 칸 + 목록을 둔다. 목록 행은 화면 계약 6대로 번호 `--fs-xs --faint` · 이름 · 클라이언트 부제. 열릴 때 검색 칸 포커스는 `ui/confirm`의 「첫 행동 요소에 포커스」가 이미 하므로 검색 칸이 첫 행동 요소가 되게 배치한다.

**EMPTY가 본문 자리를 대체하고 모달이 닫히지 않는 것**을 구현한다. `ui/confirm`의 `emptyMessage` prop을 쓰고, 「빈 폼으로 등록」을 3차 행동으로 둔다. **첫 프로젝트에서는 0행이 확정적**이므로 이 경로가 실제로 쓰인다 — E2E가 그것을 덮어야 한다.

복사 조회를 서버 액션(또는 RSC)으로 둔다. `copyFromProject`를 부르고 **초안만** 받는다 — 화면이 이전 프로젝트의 DTO를 그대로 폼에 붓지 않는다(번호·상태·기간 같은 칸은 복사 대상이 아니다). 어느 칸이 복사되고 어느 칸이 안 되는지 목록을 주석에 적고 그 판단 근거(PROJ-05 「반복 입력 칸」)를 함께 남긴다. 새 액션을 만들면 `actions.registry.ts`에 등록하고 `leak-scan.test.ts` 목록을 확인한다.

`test/e2e/projects.spec.ts`에 케이스를 더한다: ① **DB가 빈 상태에서** 골라내기를 열면 EMPTY 한 줄이 보이고 모달이 열린 채다 ② 「빈 폼으로 등록」이 모달을 닫고 폼이 그대로다 ③ 프로젝트를 하나 만든 뒤 골라내기를 열면 그 행이 보이고 고르면 칸이 채워진다 ④ 번호 칸이 채워지지 않는다(폼에 번호 입력 칸이 애초에 없다). ①이 **첫 프로젝트 시나리오**이므로 다른 케이스보다 먼저 와야 한다 — 테스트 순서를 그렇게 두고 그 이유를 주석에 적는다. 만든 데이터를 치운다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/projects.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. ①이 실패하면 0건에서 모달을 닫아 버린 것이다 — §7-8:739가 「본문에 그대로 적용」을 요구한다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 액션의 등록 파일이 목록에 없다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test:e2e</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 0건에서 EMPTY 한 줄이 본문을 대체하고 모달·시트가 닫히지 않는다
    - 「빈 폼으로 등록」이 닫고 빈 폼으로 돌아간다
    - 검색 결과 0과 조회 실패 문구가 계약 그대로다
    - 열릴 때 검색 칸에 포커스가 간다
    - 복사값이 서버 초안이고 복사되는·안 되는 칸 목록이 주석에 있다
    - 번호가 복사되지 않는다
    - 새 액션이 등록 파일에 있고 누수 스캔 목록에 반영됐다
    - 첫 프로젝트 시나리오 케이스가 다른 케이스보다 먼저 있고 이유가 주석에 있다
    - E2E가 만든 데이터를 치운다
  </acceptance_criteria>
  <done>이전 프로젝트에서 복사가 목록 골라내기로 동작하고, 복사할 건이 하나도 없는 첫 프로젝트에서도 사용자가 막히지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검색 파라미터 → 서버 쿼리 | 필터·정렬·검색이 URL에서 온다. 정렬 열 이름을 그대로 SQL에 넣으면 주입 경로가 된다 |
| 서버 액션 → 도메인 | 액션이 리포지토리를 직접 부르면 게이트·DTO 투영을 건너뛴다 |
| DTO 응답 → 화면 열 선택 | 화면이 계급으로 열을 숨기면 응답에 값이 남아 누수다 |
| E2E 데이터 → 다른 스펙 | 스펙이 남긴 데이터가 다른 스펙의 폭 단언을 깬다(실측 사례가 있다) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-61 | Tampering | 정렬 열 이름이 URL에서 와 SQL로 흘러감 | high | mitigate | 정렬 키를 **열 선언 목록의 허용 집합**으로 좁혀 검증하고 그 밖의 값은 기본 정렬로 떨어뜨린다. Drizzle의 컬럼 참조를 쓰고 문자열을 연결하지 않는다 |
| T-04-62 | Elevation of Privilege | 서버 액션이 도메인을 건너뛰고 리포지토리를 직접 호출 | high | mitigate | 액션이 도메인 함수만 import한다. `require-action-client` 린트 + 코드 리뷰 체크리스트. `revalidatePath` 뒤에도 게이트가 남는다 |
| T-04-63 | Information Disclosure | 화면이 계급 분기로 차익 열을 숨겨 응답에 값이 남음 | high | mitigate | 열 선언을 **DTO 키 존재 여부**로 만들고 화면에 계급 분기를 두지 않는다. `leak-scan` 통합 테스트가 DTO 매핑을 강제한다 |
| T-04-64 | Tampering | 등록 폼 중복 제출로 프로젝트가 둘 생김 | medium | mitigate | `SubmitRow`의 `pending`이 같은 폼 버튼 전부를 비활성화한다(04-07). 서버 쪽은 번호 `UNIQUE`(04-05)가 같은 번호를 막고, 같은 내용의 두 행 자체는 사용자가 의도한 것일 수 있어 막지 않는다 |
| T-04-65 | Information Disclosure | 자동완성 목록이 접근 권한 없는 클라이언트·거래처 이름을 노출 | medium | mitigate | `<datalist>` 후보를 서버가 `scopeFor` 판정을 거친 목록으로만 만든다. ADMN-03이 「자동완성·검색에 동일하게 적용된다」를 명시한다 |
| T-04-66 | Denial of Service | E2E가 남긴 데이터로 다른 스펙이 1/6 확률로 빨개짐 | medium | mitigate | 스펙이 만든 것을 스펙이 치운다. 전체 스위트 통과를 verify에 넣어 즉시 드러나게 한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint`(eslint + stylelint) · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` · `pnpm test:integration` 통과
- `pnpm db:reset:test && CI=true pnpm test:e2e` 전체 통과 — **로컬 dev 통과는 완료 신호가 아니다**
- `git diff --name-only`에 `db/migrations/`·`ui/` 파일이 0개
- 커밋이 셋이다 — 등록·목록 종단 / 필터·정렬·폰 / 복사 골라내기
- 웨이브 종료 시 `/review` · `/design-review` · `/qa`를 돌린다(사용자 지시: 게이트를 페이즈 끝에 몰지 않는다)
- p99 500ms 측정 결과를 `04-VERIFICATION.md`에 남긴다
</verification>

<success_criteria>
- PM이 프로젝트를 등록하고 목록·정렬·필터로 다룬다(PROJ-01)
- 등록 폼이 목록 위에 상시 렌더되지 않는다(§6-1 · D-70)
- 375px에서 가로 스크롤 없이 P1 3열로 읽힌다
- 이전 프로젝트 복사가 동작하고 0건에서도 막히지 않는다(PROJ-05)
- 차익 열이 서버 판정으로 빠지고 화면에 계급 분기가 없다(ADMN-03)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 화면 | `app/(app)/projects/page.tsx`(목록 + `?new=1`/`?editId=` 토글) · `project-list.tsx` · `project-form.tsx` · `copy-picker.tsx` |
| 새 서버 액션 | `createProjectAction` · `updateProjectAction` · 복사 초안 조회 |
| 새 등록 | `app/(app)/projects/actions.registry.ts` |
| 새 테스트 | `test/e2e/projects.spec.ts` · `test/e2e/mobile-projects.spec.ts` |
| 변경된 테스트 | `test/integration/leak-scan.test.ts`(등록 파일 import 추가) |
| 새 표 · 새 마이그레이션 · 새 컴포넌트 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-11-SUMMARY.md` when done
</output>
