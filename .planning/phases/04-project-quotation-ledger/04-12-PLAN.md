---
phase: 04-project-quotation-ledger
plan: 12
type: execute
wave: 5
depends_on: ["04-10", "04-11"]
files_modified:
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - app/(app)/projects/[id]/quote-section.tsx
  - app/(app)/projects/[id]/quote-grid-section.tsx
  - app/(app)/projects/[id]/status-section.tsx
  - app/(app)/projects/[id]/detail.module.css
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/quote-grid-keyboard.spec.ts
  - test/e2e/project-detail.spec.ts
  - test/e2e/mobile-quote-grid.spec.ts
autonomous: true
requirements: [PROJ-02, PROJ-03, PROJ-04, PROJ-07, UX-04, UX-05, FX-01]

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "매출 칸이 2차 버튼 「매출 저장」으로만 저장된다 — blur 자동저장이 없으므로 UX-04의 「중복 저장 없음」이 경로 수준에서 성립한다(D-85)"
    - "거래처 셀이 `ui/input/Autocomplete`를 거치고, 목록이 열린 셀의 키 양보가 `grid-keys.ts`의 24칸 표를 그대로 따른다(D-83)"
    - "계약 금액·발행액을 적으면 부가세·합계가 서버 계산으로 입력 바로 아래 한 줄에 보인다(PROJ-03)"
    - "입금액을 적으면 공급가액이 역산되고 계산값과 다르면 그 차이가 보인다 — 차이가 0이면 뒷 조각을 쓰지 않는다"
    - "매출 금액 칸이 음수를 받는다 — 표시가 하이픈 마이너스이고 괄호도 붉은색도 아니다(EXP-14 · §2-4)"
    - "견적 차수를 고르면 그 차수의 줄이 보이고 이전 차수는 읽기 전용 표로 렌더된다(D-55)"
    - "복사해 새 차수가 줄 전부를 복사하고 이전 차수를 건드리지 않는다"
    - "키보드만으로 견적 줄 입력이 끝난다 — Tab/Enter/방향키/범위 복사·붙여넣기/Esc/저장이 실제 브라우저에서 동작한다(UX-05)"
    - "여섯 줄 중 하나가 충돌하면 합계 행에 「오류 1칸 · 전부 거부」가 나오고 입력이 보존된다"
    - "상태 전이 넷이 화면에서 되고 완료(정산) 프로젝트는 견적 줄이 잠긴 읽기 표가 된다"
    - "「네 숫자 줄」을 렌더하지 않는다 — 확정 비용·수익이 없는 자리를 0이나 `—`로 채우지 않는다"
  prohibitions:
    - statement: "「네 숫자 줄」(PNL-01)을 이 페이즈에 렌더해서는 안 된다. 넷 중 확정 비용·수익은 지출이 생기는 Phase 5·6과 손익 Phase 9의 몫이고, 비어 있는 자리를 0이나 `—`로 채우는 것은 §7-3(「있는데 값이 없을 때만 `—`」)과 §7-7 PARTIAL(「빈 자리를 0으로 채우지 않는다」) 둘 다에 어긋난다. **줄 자체를 렌더하지 않는다**"
      status: resolved
      verification: automated
    - statement: "이전 차수를 `ui/grid`로 렌더해서는 안 된다. §7-3이 머리글 색(`--g-100`)을 **편집 가능하다는 신호**로 규정하므로 편집할 수 없는 차수를 편집용 표로 그리면 그 신호가 거짓이 된다. 완료(정산) 프로젝트의 견적 줄도 같다 — `ui/table`로 렌더한다"
      status: resolved
      verification: automated
    - statement: "`⌘E`(현재 줄로 지출결의)를 배선하거나 힌트 줄에 넣어서는 안 된다 — Phase 5다(§7-9 「그 화면에서 실제로 되는 것만」)"
      status: resolved
      verification: automated
    - statement: "상태 전이·저장에 토스트를 띄워서는 안 된다. §7-6이 「화면 이동이 따르는 행동만 토스트」이고 §7-3이 표 저장 결과를 합계 행에 두며, 상태 태그가 바뀌는 것 자체가 결과다(§7-7 SUCCESS)"
      status: resolved
      verification: automated
  artifacts:
    - path: "app/(app)/projects/[id]/page.tsx"
      provides: "§6-2 상세 = 견적 원장. 탭 없이 섹션 넷(매출 칸 · 차수·승인 · 견적 줄 표 · 상태 전환)"
      contains: "PageHeader"
      min_lines: 70
    - path: "app/(app)/projects/[id]/quote-grid-section.tsx"
      provides: "`ui/grid` 배선 + 일괄 저장 결과·충돌 셀 표시 + 단축키 힌트 줄"
      min_lines: 80
    - path: "test/e2e/quote-grid-keyboard.spec.ts"
      provides: "UX-05 키보드 전용 시나리오 — Tab/Enter/방향키/범위 복사·붙여넣기/Esc/저장. 04-VALIDATION.md가 지정한 파일 이름"
      min_lines: 90
    - path: "app/(app)/projects/[id]/status-section.tsx"
      provides: "상태 전이 넷 + 확인 모달 둘 + 완료 잠김 이유"
      min_lines: 60
  key_links:
    - from: "app/(app)/projects/[id]/quote-grid-section.tsx"
      to: "ui/grid/Grid.tsx"
      via: "편집용 표가 열 선언·키 처리·클립보드를 컴포넌트에서 얻고 저장은 서버 액션에 위임한다"
      pattern: "onSave"
    - from: "app/(app)/projects/[id]/revenue-section.tsx"
      to: "domain/money/index.ts"
      via: "부가세·합계·역산이 서버 계산이고 화면은 결과 문자열만 렌더한다"
      pattern: "grossFromTotal"
    - from: "app/(app)/projects/[id]/status-section.tsx"
      to: "domain/rules/gate.ts"
      via: "전이 가능 여부와 거부 사유가 게이트에서 온다 — 버튼 비활성 이유가 그 문장이다"
      pattern: "gate"
---

<!-- planner-discipline-allow: ⌘E -->
<!-- planner-discipline-allow: ⌘ -->

<objective>
**As a** 기획 PM, **I want to** 프로젝트 한 화면에서 매출 칸을 적고 견적 줄을 엑셀처럼 입력하고 상태를 넘기고 싶다, **so that** 이 페이즈가 세우려 한 「돈 뼈대」가 한 화면에서 실제로 돌아간다.

이 페이즈의 **마지막 프로젝트 화면**이다. 04-10의 그리드와 04-09의 도메인을 배선하고 상태 전이를 붙인다.

Purpose: ROADMAP Phase 4 기준 2·3·4·5가 전부 이 화면에서 만난다 — 키보드만으로 끝나는 표 입력 · 서버 계산 차익 · 차수와 고객 승인 · 매출 칸의 역산 · 상태 전이 넷.

Output: 상세 화면 섹션 넷, 서버 액션 추가, E2E 셋(키보드 · 상세 · 폰).

**이 플랜이 04-11과 파일을 공유한다.** `app/(app)/projects/actions.ts`와 `actions.registry.ts`에 액션을 더하므로 웨이브를 갈랐다(04-11이 wave 4, 이 플랜이 wave 5).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quotation-ledger/04-09-SUMMARY.md
@.planning/phases/04-project-quotation-ledger/04-10-SUMMARY.md
@.planning/phases/04-project-quotation-ledger/04-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 상세 화면 골격 + 매출 칸 — 서버 계산 한 줄까지 (PROJ-03)</name>
  <files>app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/detail.module.css, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/integration/leak-scan.test.ts, test/e2e/project-detail.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 7 전문** — §6-2 상세 = 견적 원장 · **탭 없음, 섹션(2px `--line-strong`)으로 아래로 이어진다** · 머리 줄(제목 = 프로젝트 이름 `--fs-lg` 700 · 부제 `{번호} · 상세 견적 2차 · 진행` · 오른쪽 2차 `복사해 새 차수` + 1차 `일괄 저장 N`) · **「네 숫자 줄」을 렌더하지 않는 이유** · 섹션 1 매출 칸 표 다섯 + 서버 계산 한 줄 둘 + 음수 허용 · 섹션 2 차수·승인 · 섹션 3 견적 줄 표 · 섹션 4 상태 전환
    - `docs/design/SYSTEM.md` §6-2 전문 — 상세 화면 템플릿
    - `docs/design/SYSTEM.md` §6-3의 「서버 계산 값은 입력 **바로 아래 한 줄**」 — `--fs-sm --muted`, 숫자만 700. **읽기 전용 행을 따로 두지 않는다**
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` Copywriting의 「서버 계산 한 줄(§6-3)」 행 — 문구 정본: `부가세 10% 1,240,000 · 합계 13,640,000 · 서버 계산` / 입금액 역산 `공급가액 12,400,000 역산 · 계산값과 30,000 차이 · 서버 계산`. **차이가 0이면 뒷 조각을 쓰지 않는다**
    - `.planning/REQUIREMENTS.md` PROJ-03(28행) — 계약 금액·발행액은 공급가액, 입금액은 통장 합계이고 시스템이 공급가액으로 역산한다. **별도 매출 모듈은 없다**
    - `docs/design/SYSTEM.md` §2-4 — 음수는 **하이픈 마이너스, 괄호 금지 · 붉은색 금지**. EXP-14의 환불·할인이 여기 걸린다
    - `domain/money/index.ts`(04-03) — `grossFromTotal(total, vatRateMinor, rounding, claimedSupply?)`가 `{ supply, vat, diff }`를 돌려준다. **세율을 부동소수가 아니라 최소단위 정수로 받는다** — 설정에서 읽은 `tax.vat.rate`를 같은 모듈의 `rateToMinor`로 감싸 넘긴다(B2: 부동소수 비율로 나누면 13,640,000의 공급가가 12,399,999가 되어 이 화면의 문구 정본 12,400,000과 어긋난다). 화면은 그 셋으로 문구를 만들 뿐 계산하지 않는다
    - `domain/settings/keys.ts` — `TAX_VAT_RATE`(historized) · `TAX_ROUNDING_VAT_UNIT`. **어느 날짜를 `asOf`로 넘길지는 호출자의 책임**(`domain/settings/registry.ts` 주석). 매출 칸의 기준일이 무엇인지 판단한다 — `tax.basis_date.vat = "evidence_date"`(세금계산서 발행일)가 기본값이므로 그 칸을 쓴다
    - `app/(app)/projects/page.tsx`(04-11) — 목록에서 상세로 가는 링크. 라우트 이름이 그것과 맞아야 한다
    - `app/(app)/admin/people/[id]/page.tsx` — 이 저장소의 동적 라우트 선례. **Next 16의 `params`가 Promise인지** `node_modules/next/dist/docs/`에서 확인한다
    - `app/(app)/projects/actions.ts`(04-11) — 이 태스크가 액션을 **더한다**. 04-11의 두 액션을 건드리지 않는다
    - `ui/page-header/PageHeader.tsx` · `ui/status-tag/StatusTag.tsx` — 머리 줄. 제목 옆 상태는 `variant="tag"`(§7-5)
    - `ui/form/Form.tsx`(04-07) — 매출 칸이 `ui/form` 골격을 상세 화면 안의 한 섹션으로 쓴다
    - 04-11이 만든 상태 색 매핑의 위치 — **같은 매핑을 쓴다**(복제하지 않는다)
  </read_first>
  <behavior>
    - 상세 화면에 탭이 없고 섹션이 `2px --line-strong`으로 갈린다
    - 머리 줄 제목이 프로젝트 이름이고 부제가 `{번호} · {차수 표시} · {상태}`다
    - 「네 숫자 줄」이 렌더되지 않는다 — 확정 비용·수익 자리가 0이나 `—`로 채워지지 않는다
    - 계약 금액을 적으면 그 아래 한 줄에 부가세·합계가 서버 계산으로 나온다
    - 입금액을 적으면 그 아래 한 줄에 역산 공급가액이 나오고, 계산값과 다르면 차이가 붙는다
    - 차이가 0이면 차이 조각이 없다
    - 매출 금액 칸에 음수를 넣을 수 있고 표시가 하이픈 마이너스다
    - 금액 입력에 타이핑 중 쉼표가 자동으로 붙고 우측 정렬 · `tabular-nums`다
    - 매출 칸은 **2차 버튼 「매출 저장」**을 눌러야 저장된다 — 칸에서 포커스가 빠지는 것만으로는 저장되지 않는다(D-85 · UX-04)
    - 칸을 고치고 저장하지 않은 채 다른 칸으로 옮겨도 **입력값이 남아 있다** — 자동 저장이 없으므로 중복 저장도 없다
    - 저장 중에는 그 버튼이 비활성이고 라벨 뒤에 `…`가 붙는다 — 연타로 두 번 저장되지 않는다(§7-1)
    - 접근 권한이 없는 프로젝트로 들어가면 404다
  </behavior>
  <action>
`app/(app)/projects/[id]/page.tsx`를 만든다. **04-11의 `page.tsx`가 쓴 WR-07 패턴을 따른다** — 이 페이지가 `requireSession()`을 직접 부르고 레이아웃에 기대지 않는다. 그 이유 주석을 04-11의 파일에서 그대로 가져온다. 접근 권한이 없으면 `notFound()`(관리자 화면들이 쓰는 형태).

섹션 넷을 위에서 아래로 배치하고 사이를 `2px --line-strong`으로 가른다. 탭을 만들지 않는다.

머리 줄: `PageHeader`의 제목·부제 + 오른쪽 2차 `복사해 새 차수` + 1차 `일괄 저장 N`(§7-1 1차는 한 화면에 1개 — **이 화면에서 1차는 그 하나뿐**이다).

**매출 칸은 2차 버튼 「매출 저장」으로 명시 저장한다(D-85 — 결정됐다. 고르지 않는다).** blur 자동저장을 쓰지 않는 이유가 잠겨 있다: 자동저장은 **중복 발화·이탈 경쟁**이라는 알려진 실패면을 갖는데 **UX-04가 「중복 저장이 없다」를 명시**한다 — 금지된 상황을 만드는 경로를 고를 이유가 없다. §7-1이 2차 버튼을 허용하므로 1차가 하나라는 규칙과도 충돌하지 않는다. 매출을 별도 화면으로 떼는 안은 PROJ-03의 「별도 매출 모듈은 없다」와 어긋나 역시 배제됐다. 이 근거를 `revenue-section.tsx` 주석에 D-85 인용과 함께 적는다 — **「고르고 주석에 적는다」가 아니라 「받아 적는다」다.**

**「네 숫자 줄」을 렌더하지 않는다.** 그 이유(§7-3 · §7-7의 두 규칙)를 `page.tsx` 주석에 남긴다 — 다음 페이즈가 「빈 자리를 0으로」 채우지 않게. Phase 5·6·9가 그 줄을 붙일 자리임도 적는다.

`revenue-section.tsx` — `ui/form` 골격 + 칸 다섯(계약 금액 · 세금계산서 발행일 · 발행액 · 입금일 · 입금액, 전부 280) + **2차 버튼 「매출 저장」**(D-85). 버튼은 `ui/form`의 `SubmitRow`가 아니라 섹션 안의 2차 버튼이다 — `SubmitRow`의 1차는 화면에 하나뿐인 `일괄 저장 N`이 쓴다. 저장 중 비활성과 라벨 뒤 `…`는 §7-1 그대로이고 `Button`이 이미 그것을 한다. blur·`onChange`에 저장을 걸지 않는다. 서버 계산 한 줄은 **서버에서 온 값**으로 렌더한다 — 화면이 `총액 × 0.1`을 계산하지 않는다(`money-boundary` 린트가 `app/`에서도 돌고, 무엇보다 PROJ-02의 원칙이다). 계산은 서버 액션 응답 또는 RSC에서 오고 화면은 문자열 조립만 한다. 문구는 Copywriting 정본 그대로이고 **차이가 0이면 뒷 조각을 쓰지 않는다**.

금액 입력의 쉼표 자동 삽입은 `TextField`의 `numeric` 변형이 이미 우측 정렬·`tabular-nums`를 하므로, 쉼표 삽입 로직만 더한다. **그 로직을 순수 함수로 뽑아** 단위 테스트를 붙일 수 있게 한다(RTL이 없으므로 그것이 유일한 검증 경로다).

서버 액션을 `actions.ts`에 더하고 `actions.registry.ts`에 등록하고 `leak-scan.test.ts` 목록을 확인한다.

`test/e2e/project-detail.spec.ts`를 만든다: ① 상세 진입 → 섹션 넷이 보이고 탭이 없다 ② 「네 숫자 줄」의 확정 비용·수익 라벨이 **DOM에 없다** ③ 계약 금액을 적으면 부가세·합계 한 줄이 나온다 ④ 입금액을 적으면 역산 한 줄이 나오고 차이가 0인 값에서는 차이 조각이 없다 ⑤ 음수를 적으면 하이픈 마이너스로 표시되고 괄호가 없다 ⑥ 권한 없는 계급으로 들어가면 404다. **변경 클릭 직후 `page.goto()`를 하지 않는다.** 만든 데이터를 치운다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/project-detail.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. ④의 차이 0 케이스가 실패하면 문구에서 뒷 조각을 무조건 붙인 것이다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 액션의 등록 파일이 누수 스캔 목록에 없다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('app/(app)/projects/[id]/revenue-section.tsx','utf8').replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');if(/onBlur\s*=/.test(s)){console.error('REVENUE FIELDS AUTOSAVE ON BLUR — UX-04 FORBIDS DUPLICATE SAVES (D-85)');process.exit(1)}if(!/매출 저장/.test(s)){console.error('NO EXPLICIT SAVE BUTTON IN REVENUE SECTION (D-85)');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `REVENUE FIELDS AUTOSAVE ON BLUR …`(D-85 위반 — blur 자동저장은 중복 발화·이탈 경쟁을 만들고 UX-04가 그것을 금지한다) 또는 `NO EXPLICIT SAVE BUTTON IN REVENUE SECTION`(2차 버튼이 없다) 중 하나가 나오고 종료 코드가 1이다. 판정은 주석을 걷어낸 본문에서 한다 — `onBlur`가 저장이 아닌 다른 목적(포맷 정리 등)으로 정말 필요하면 그 이유를 SUMMARY에 적고 게이트를 「저장 액션 호출이 `onBlur` 안에 없다」로 좁힌다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. `money-boundary`가 `app/` 아래 금액 산술을 잡으면 화면이 계산하고 있는 것이다 — 서버에서 받은 값을 렌더만 해야 한다. grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 탭이 없고 섹션이 `2px --line-strong`으로 갈린다
    - 「네 숫자 줄」이 DOM에 없고 그 이유가 주석에 있다
    - 서버 계산 한 줄이 입력 바로 아래이고 문구가 Copywriting 정본이다
    - 차이가 0이면 뒷 조각이 없다
    - 음수가 하이픈 마이너스이고 괄호·붉은색이 아니다
    - 화면이 금액을 계산하지 않는다 — `money-boundary`가 `app/`에서 초록이다
    - 1차 버튼이 한 화면에 하나이고, 매출 칸은 **2차 버튼 「매출 저장」**으로 명시 저장한다(D-85)
    - blur·`onChange`에 저장이 걸려 있지 않다 — 정규식 게이트가 확인한다
    - 저장하지 않은 입력값이 다른 칸으로 옮겨도 남아 있고, 저장 중 연타가 막힌다(UX-04)
    - D-85를 받아 적은 근거(UX-04의 「중복 저장 없음」 · §7-1의 2차 허용 · PROJ-03의 「별도 매출 모듈 없음」)가 주석에 있다
    - `requireSession()`을 페이지가 직접 부르고 WR-07 주석이 있다
    - 권한 없는 접근이 404다
    - 쉼표 삽입 로직이 순수 함수이고 단위 테스트가 있다
  </acceptance_criteria>
  <done>상세 화면이 섹션 넷 골격으로 서고, 매출 칸의 부가세·합계·역산이 서버 계산으로 입력 바로 아래에 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 차수·고객 승인 + 견적 줄 표 — 키보드만으로 끝난다 (PROJ-02 · PROJ-07 · UX-05)</name>
  <files>app/(app)/projects/[id]/quote-section.tsx, app/(app)/projects/[id]/quote-grid-section.tsx, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/e2e/quote-grid-keyboard.spec.ts, test/e2e/mobile-quote-grid.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 7의 섹션 2·3 전문** — 차수 선택 `ui/select` 200(`사전 견적` / `상세 견적 1차` / `상세 견적 2차`, D-51) · 고객 승인 표시 §7-5 색 글자 + 3차 `고객 승인 표시` · **이전 차수는 `ui/grid`가 아니라 `ui/table`로 렌더한다**(D-55, 근거는 머리글 색이 편집 가능 신호라는 §7-3) · 견적 줄 표의 **열과 우선순위**(행 번호 28px · 대분류/소분류 P3 text · 항목 P1 text · 거래처 P2 text · 견적가 P2 money · 실행가 P1 money · 차익 P3 money(서버가 안 보낼 수 있음) · 상태 P1 status → P1 3열) · 「견적 외 비용」 줄은 견적가 0인 같은 표의 한 종류 · 「취소」 상태 줄 · 거래처 셀 `<input list>` · 하단 §7-9 힌트 줄
    - `ui/grid/Grid.tsx`(04-10) — props와 `onSave` 위임 형태. `ui/grid/ShortcutHints.tsx`
    - `ui/table/Table.tsx`(04-08) — 이전 차수·완료 잠김 표가 이것을 쓴다
    - `domain/quote-lines/index.ts`(04-09) — `saveQuoteLines`의 충돌 예외와 그 필드. **화면이 그 필드를 읽어 셀마다 이유를 렌더한다**
    - `ui/input/Autocomplete.tsx`(04-07, D-83) — **거래처 셀이 쓰는 소유 컴포넌트**다. 라벨 없는 모드가 그리드 셀 용례이고, 04-07 Task 2가 `app/**`에 목록 요소 리터럴이 0개임을 단언하는 테스트를 세워 뒀으므로 **셀 안에서 직접 배선하면 그 테스트가 RED다**
    - `ui/grid/grid-keys.ts`(04-10, D-83) — **목록을 단 셀의 키 양보 규칙**이 그 모듈에 있다(위·아래와 `Enter`·`Esc`는 목록에, 좌우·`Tab`은 그리드에). 화면이 그 판정을 다시 쓰지 않고 모듈 결과를 그대로 따른다 — 셀 선언에 「목록을 달았다」와 「목록이 열려 있다」를 담아 넘기는 것이 화면의 몫이다
    - `test/e2e/fixtures.ts`(04-02가 확장) — 콘솔 오류·페이지 예외를 자동 수집해 0을 단언하는 확장 `test`. **이 플랜의 새 스펙 둘이 `@playwright/test`가 아니라 `./fixtures`에서 `test`·`expect`를 가져온다**(B7)
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` Copywriting의 표 저장 성공·실패·셀 충돌 세 행 — 문구 정본. 성공 `저장됨 6줄 14:02`(`--g-800`) · 실패 `오류 1칸 · 전부 거부`(`--danger`) · 충돌 `다른 사람이 14:01에 9,800,000으로 바꿈 · 덮어쓰기 / 그 값으로`
    - 같은 문서의 Destructive 「견적 줄 삭제」·「삭제 불가(연결 문서 있음)」 두 행 — 확인 모달 문구와 §7-1 이유 있는 비활성(`지출결의 {번호} 연결됨 · 취소 상태로 바꾸기`)
    - `.planning/ROADMAP.md` Phase 4 기준 2 — 「브라우저 계산값은 저장되지 않아 인트라넷의 차익 불일치(66줄) 같은 오류가 구조적으로 불가능하다 … **승인·삭제·상태 변경은 GET으로 되지 않는다**」
    - `.planning/REQUIREMENTS.md` UX-05(144행) 전문 — E2E가 덮어야 하는 키 전부
    - `04-VALIDATION.md`의 UX-05 행 — **파일 이름이 `test/e2e/quote-grid-keyboard.spec.ts`로 지정**됐다. 명령은 `CI=true pnpm playwright test test/e2e/quote-grid-keyboard.spec.ts`
    - `04-VALIDATION.md` § Manual-Only Verifications의 「엑셀 붙여넣기가 실제 스프레드시트에서 온 TSV로 동작」 행 — Playwright의 합성 클립보드가 실제 Excel·Google Sheets의 TSV와 다를 수 있어 **수동 검증으로 분류**됐다. 이 태스크는 합성 TSV로 자동 검증하고 실제 시트 확인은 수동으로 남긴다
    - `04-RESEARCH.md` Pitfall 5 — Playwright 클립보드의 두 방법(`page.evaluate`로 `ClipboardEvent` 직접 디스패치 vs `grantPermissions`). **네이티브 이벤트를 쓰므로 전자**다
    - `playwright.config.ts`의 `projects` 배열 — Chromium 계열 두 프로젝트(desktop · mobile)인지 실측 확인한다(04-RESEARCH A1 가정)
    - `test/e2e/keyboard-nav.spec.ts` — 이 저장소의 키보드 E2E 선례
  </read_first>
  <behavior>
    - 차수를 고르면 그 차수의 줄이 보인다
    - 현재 차수는 `ui/grid`(`--g-100` 머리글), 이전 차수는 `ui/table`(흰 머리글)로 렌더된다
    - 고객 승인 표시가 §7-5 색 글자이고 3차 버튼으로 바뀐다
    - 「복사해 새 차수」가 줄 전부를 복사하고 이전 차수가 그대로 남는다
    - 셀에 Enter로 편집을 시작하고 Tab이 오른쪽, Shift+Tab이 왼쪽, Enter가 아래로 간다
    - Esc가 값을 되돌린다
    - 범위를 복사해 붙여넣으면 값이 들어가고, 범위가 표를 넘으면 새 줄이 생긴다
    - 수정 키 + S가 일괄 저장하고 합계 행에 `저장됨 N줄 HH:mm`이 나온다
    - 한 줄이 충돌하면 합계 행에 `오류 1칸 · 전부 거부`가 나오고 **나머지 입력이 보존된다**
    - 충돌 셀에 이유가 붙고 그 이유에 바뀐 시각·값이 있다
    - 견적가·실행가를 넣으면 차익이 서버 계산값으로 보인다 — 브라우저가 계산한 값이 아니다
    - 견적가 0인 「견적 외」 줄이 같은 표에 있다
    - Delete가 확인 모달을 열고, 연결 문서가 있는 줄은 삭제 버튼이 이유와 함께 비활성이다
    - 하단 힌트 줄에 항목 6개가 있고 지출결의가 없다
    - 폰에서 셀 편집이 없고 행 탭이 시트를 연다
    - 승인·삭제·상태 변경이 GET으로 되지 않는다
  </behavior>
  <action>
**`test/e2e/quote-grid-keyboard.spec.ts`를 먼저 쓴다 — 화면 코드보다 앞에, RED를 눈으로 확인하고 커밋한다(W3).** 파일 이름은 `04-VALIDATION.md`가 지정했다. **`test`·`expect`를 `./fixtures`에서 가져온다**(04-02의 확장 — 콘솔 오류·페이지 예외가 자동으로 수집되고 0으로 단언된다, B7). 이 플랜의 폰 스펙도 같다. E2E는 이 페이즈에서 **유일하게 테스트 선행이 가능한 계층**이다: RTL 미설치는 컴포넌트 렌더 단언을 막을 뿐 실제 브라우저를 막지 않는다. 그리고 UX-05는 「키보드만으로 끝난다」가 요구사항 자체라서, 키 흐름을 먼저 적어 두지 않으면 구현이 자기가 만든 키 배치를 사후에 옮겨 적게 된다 — D-73(Tab = 오른쪽)이 산문으로 남았다가 어긋난 바로 그 부류다.

**RED 확인**: `pnpm db:reset:test && CI=true pnpm playwright test test/e2e/quote-grid-keyboard.spec.ts`가 **실패해야** 한다. 실패 메시지를 SUMMARY에 옮겨 적고 그 다음에야 아래 구현으로 넘어간다.

`quote-section.tsx` — 차수 선택 `Select` + 승인 표시 + 3차 `고객 승인 표시` + 2차 `복사해 새 차수`. **이전 차수는 `ui/table`로 렌더한다**(금지 항목). 어느 차수가 편집 가능한지의 판정은 **게이트에서 온다**(04-09) — 화면이 「마지막 차수면 편집 가능」 같은 규칙을 다시 쓰지 않는다.

`quote-grid-section.tsx` — `ui/grid` 배선. 열 선언을 화면 계약 7 섹션 3 그대로 만들고 `kind`·`priority`를 담는다. **거래처 셀은 `ui/input/Autocomplete`의 라벨 없는 모드를 쓴다**(D-83) — 목록 배선을 셀 안에서 직접 적지 않고, 셀 선언에 「목록을 달았다」·「목록이 열려 있다」를 담아 `grid-keys.ts`의 키 양보 규칙(04-10, D-83)이 그대로 적용되게 한다. 화면이 「목록이 열렸으면 아래 방향키를 삼킨다」 같은 판정을 **다시 쓰지 않는다** — 그 규칙의 정본은 순수 모듈이고 24칸 표로 고정돼 있다. 차익 열은 **DTO 키 존재 여부**로 선언에 넣는다(04-11과 같은 방식, 계급 분기 금지). `onSave`가 서버 액션을 부르고 응답을 `Grid`에 결과 prop으로 되돌린다 — 충돌이면 04-09의 예외 필드에서 셀별 이유를 만든다. 합계 행 문구는 Copywriting 정본이고 성공 색이 `--g-800`이다(컴포넌트가 이미 그 색을 쓰지만 화면이 `--success`를 덮어쓰지 않는지 확인한다).

**완료(정산) 프로젝트면 `ui/table`로 렌더한다**(금지 항목). 잠김 이유를 §7-1대로 버튼 옆 글자로 두고 `Button`의 `disabledReason`으로 넘긴다(`완료(정산) 프로젝트 · 잠김`). 판정은 게이트에서 온다.

`ShortcutHints`를 표 아래에 둔다. 항목이 6개이고 지출결의가 없다.

서버 액션(일괄 저장 · 승인 표시 · 새 차수 · 줄 삭제/취소)을 `actions.ts`에 더하고 등록·누수 스캔 목록을 갱신한다. **전부 서버 액션(POST)이다** — GET으로 되는 경로를 만들지 않는다(ROADMAP 기준 2).

(위에서 먼저 쓴 `test/e2e/quote-grid-keyboard.spec.ts`가 여기서 초록이 되어야 한다.) 그 스펙의 시나리오는 키보드만으로 진행한다: 프로젝트·차수 준비 → 첫 셀에 포커스 → Enter 편집 → 값 입력 → **Tab으로 오른쪽 이동 확인**(D-73 — 이 단언에 케이스 제목으로 D-73을 인용한다) → Shift+Tab으로 왼쪽 → Enter로 아래 → Esc로 되돌림 확인 → 범위 선택 → 복사 → 다른 위치에 붙여넣기 → **표를 넘는 범위 붙여넣기로 새 줄 생성 확인** → 수정 키 + S로 저장 → 합계 행 `저장됨 N줄` 확인. 클립보드는 `page.evaluate`로 `ClipboardEvent`를 디스패치하거나 `page.keyboard.press`가 네이티브 `copy`/`paste`를 일으키는지 실측으로 확인해 되는 쪽을 쓴다 — **`grantPermissions`를 쓰지 않는다**(네이티브 이벤트에는 필요 없다). 어느 방법이 실제로 동작했는지 SUMMARY에 적는다.

**거래처 셀(목록) 케이스를 키보드 스펙에 하나 둔다(D-83)**: 거래처 셀로 이동 → 글자 몇 개 입력 → 아래 방향키가 **그리드 행을 옮기지 않고** 후보를 옮긴다 → `Enter`로 후보 확정 → 한 번 더 `Enter`로 아래 칸 이동. `Tab`이 그 상태에서도 오른쪽으로 감을 함께 단언한다. 케이스 제목에 D-66의 근거 문장(「그리드 셀 안에서도 쓸 수 있다」)을 인용한다.

충돌 케이스를 별 테스트로 둔다: 두 컨텍스트(또는 한 컨텍스트 + 서버 쪽 직접 UPDATE)로 같은 줄을 바꾼 뒤 저장 → 합계 행 `오류 1칸 · 전부 거부` + **나머지 입력이 화면에 남아 있음**을 단언한다.

`test/e2e/mobile-quote-grid.spec.ts`(파일 이름이 `mobile-*` 패턴) — 375px에서 ① 셀 편집이 안 된다 ② 행 탭이 시트를 연다 ③ `scrollWidth <= 375` ④ 힌트 줄이 숨는다 ⑤ 접힌 줄에 외화 2행·거래처·날짜가 있다. **DOM 실측**이고 스크린샷 육안이 아니다.

두 스펙 모두 **만든 데이터를 치운다.**
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/quote-grid-keyboard.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. Tab 방향 단언이 실패하면 04-10의 `resolveKeyAction`이 아니라 화면이 키를 처리하고 있거나 D-73을 거슬렀다. 클립보드 단언이 실패하면 디스패치 방법을 바꿔 본다(`page.evaluate` ↔ `page.keyboard`)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/mobile-quote-grid.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. `scrollWidth > 375`면 P1이 셋을 넘었거나 힌트 줄이 폰에서 남은 것이다</fails_when>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/(app)/projects/[id]/quote-section.tsx','utf8')+fs.readFileSync('app/(app)/projects/[id]/quote-grid-section.tsx','utf8');if(!/Table/.test(s)){console.error('NO ui/table IMPORT — PRIOR ROUNDS AND LOCKED PROJECTS MUST USE ui/table (D-55)');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `NO ui/table IMPORT …`가 나오고 종료 코드가 1이다 — 이전 차수·완료 잠김을 편집용 표로 그렸다(금지 항목)</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/no-platform-key-literal.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 앞은 화면 코드에 플랫폼 기호 리터럴이 남은 것(D-76), 뒤는 새 액션의 등록 파일이 목록에 없는 것</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test:e2e</automated>
    <fails_when>종료 코드가 0이 아니다 — 다른 스펙이 깨지면 이 스펙의 정리가 빠졌다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 현재 차수가 `ui/grid`, 이전 차수·완료 잠김이 `ui/table`이다
    - 편집 가능 판정이 게이트에서 오고 화면이 규칙을 다시 쓰지 않는다
    - 키보드만으로 입력·이동·취소·복사·붙여넣기·저장이 되고 Tab이 오른쪽이다
    - 표를 넘는 붙여넣기가 새 줄을 만든다
    - 충돌 시 합계 행 문구가 정본이고 나머지 입력이 보존된다
    - 차익이 서버 계산값이다
    - 차익 열 판정이 DTO 키 존재 여부이고 계급 분기가 없다
    - 견적가 0인 줄이 같은 표에 있다
    - Delete가 확인 모달을 열고 연결 문서 있는 줄은 이유와 함께 비활성이다
    - 힌트 줄 항목이 6개이고 지출결의가 없다
    - 폰에서 셀 편집이 없고 `scrollWidth <= 375`다
    - 모든 변경이 서버 액션이고 GET 경로가 없다
    - 거래처 셀이 `ui/input/Autocomplete`를 쓰고 화면 코드에 목록 요소 리터럴이 0개다 — 04-07의 단위 테스트가 초록이다(D-83)
    - 목록이 열린 셀에서 위·아래·`Enter`·`Esc`의 동작이 `grid-keys.ts`의 판정을 그대로 따른다 — 화면에 같은 판정을 다시 쓴 분기가 없다
    - 새 E2E 스펙 둘이 `./fixtures`의 확장 `test`를 쓴다 — 콘솔 오류·페이지 예외 0이 자동으로 단언된다(B7)
    - 클립보드 디스패치 방법과 실측 결과가 SUMMARY에 있다
    - `test/e2e/quote-grid-keyboard.spec.ts`가 화면 코드보다 **앞선 커밋**에 있고 그 시점의 RED 실패 메시지가 SUMMARY에 있다(W3)
  </acceptance_criteria>
  <done>견적 줄 입력이 실제 브라우저에서 키보드만으로 끝나고, 일괄 저장이 전부 저장 또는 전부 거부로 동작하며, 이전 차수가 읽기 전용으로 보존된다</done>
</task>

<task type="auto">
  <name>Task 3: 상태 전환 넷 + 완료 잠김 (PROJ-04 · D-41 · D-44)</name>
  <files>app/(app)/projects/[id]/status-section.tsx, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/e2e/project-detail.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 7의 섹션 4 표 전문** — 전이 넷과 각자의 확인 여부: 수주중 → 진행(2차 `진행으로 전환`, **확인 없음**) · 수주중 → 미수주(2차 `미수주로 닫기`, **§7-8 확인 모달**) · 진행 → 완료(정산)(2차 `완료(정산)`, **확인 모달**) · 미수주 → 진행(2차 `되살리기`, **확인 없음**)
    - 같은 절의 결과 표현 — 「§7-7 SUCCESS 「행동한 자리에서 결과가 보인다」 — 머리 줄의 상태 태그가 바뀌는 것 자체가 결과이고 **토스트를 띄우지 않는다**(§7-6 — 화면 이동이 따르는 행동만 토스트)」
    - 같은 절의 완료(정산) 처리 — 「견적 줄 표가 잠긴다 → `ui/table`로 렌더 + 새 지출을 받지 않는 상태가 화면에 보인다(ROADMAP 기준 4). 잠김 이유는 §7-1 비활성 규칙대로 버튼 옆 글자로: `완료(정산) 프로젝트 · 잠김`, `Button`의 `aria-describedby`로 연결(F4)」
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` Copywriting의 Destructive 「완료(정산)」·「미수주로 닫기」 두 행 — 모달 제목·부제·결과 한 줄·1차 라벨 정본. 특히 미수주의 결과 한 줄 `그때까지 쌓인 비용이 팀 손익의 미수주 비용으로 내려갑니다`(D-41)
    - 같은 문서의 「비파괴 전환(확인 없음)」 행 — `진행으로 전환` · `되살리기`
    - `.planning/REQUIREMENTS.md` PROJ-04(29행) 전문 — 「수주중일 때 쌓인 비용은 진행으로 바뀌면 그대로 그 프로젝트의 비용이 되고, 미수주로 닫히면 팀 손익의 미수주 비용으로 내려간다. 뒤늦게 수주하면 미수주 → 진행으로 되살릴 수 있다」. **비용 귀속 자체는 Phase 5·6·10의 손익 계산이 하고 이 페이즈는 상태만 바꾼다** — 그 경계를 주석에 적는다
    - `domain/rules/gate.ts`(04-04)와 `domain/projects/index.ts`(04-09)의 `transitionProjectStatus` — 허용 판정과 거부 사유가 여기서 온다. **화면이 전이 표를 다시 쓰지 않는다**
    - `ui/confirm/Confirm.tsx`(04-08) — 확인 모달 둘. `closeLabel` 필수
    - `docs/design/SYSTEM.md` §7-1 — 「붉은 버튼 없음 — 위험 행동은 색이 아니라 확인 모달」. 1차는 한 화면에 1개(이 화면의 1차는 `일괄 저장 N`이므로 **상태 전환 버튼은 전부 2차**다. 모달 안의 1차는 그 모달의 1차이므로 별개다)
    - `domain/action-log/record.ts`(04-09가 더한 상태 전이 종류) — 전이가 로그에 남는다
    - `04-CONTEXT.md` D-44 — 「미수주 → 진행 되살리기를 허용한다. 상태 전이로 처리하고 행동 로그에 남긴다. 그때까지 쌓인 비용이 팀 손익의 미수주 비용에서 프로젝트 비용으로 자동으로 돌아온다 — **상태로 비용이 따라 움직이게 한다는 이 설계의 원래 목적 그대로다**」
  </read_first>
  <behavior>
    - 현재 상태에 따라 가능한 전이 버튼만 보인다 — 그 판정이 게이트에서 온다
    - 수주중 → 진행이 확인 없이 되고 머리 줄 상태 태그가 바뀐다
    - 수주중 → 미수주가 확인 모달을 거치고 모달 문구가 정본이다
    - 진행 → 완료(정산)가 확인 모달을 거친다
    - 미수주 → 진행(되살리기)이 확인 없이 된다
    - 전이 뒤 토스트가 뜨지 않는다
    - 전이가 행동 로그에 남는다
    - 완료(정산) 뒤 견적 줄 표가 `ui/table`로 렌더되고 편집 버튼이 이유와 함께 비활성이다
    - 게이트가 거부한 전이를 시도하면 그 이유가 화면에 보인다
    - 전이가 서버 액션이고 GET으로 되지 않는다
    - 붉은 버튼이 없다
  </behavior>
  <action>
`status-section.tsx`를 만든다. **가능한 전이 목록을 게이트에서 받는다** — 화면이 「수주중이면 진행·미수주 둘」 같은 표를 다시 쓰지 않는다(04-04의 게이트가 정본). 서버 컴포넌트에서 각 전이에 대해 게이트를 물어 `{ transition, allowed, reason }` 목록을 만들고, 허용된 것만 2차 버튼으로 렌더한다. 거부된 전이를 **아예 안 보일지 이유와 함께 비활성으로 보일지** 판단한다 — §7-1의 「이유 있는 비활성」 정신은 후자이지만 §6-0의 「빈 자리를 두지 않는다」는 전자에 가깝다. 고른 쪽과 근거를 주석에 적는다.

확인이 필요한 둘(미수주로 닫기 · 완료(정산))에 `ui/confirm`을 쓴다. 문구는 Copywriting 정본 그대로이고 `closeLabel`을 채운다. **모달에 폼을 넣지 않는다**(§7-8).

비파괴 둘(진행으로 전환 · 되살리기)은 확인 없이 바로 액션을 부른다. 결과는 머리 줄 상태 태그가 바뀌는 것이고 **토스트를 띄우지 않는다**.

완료(정산) 뒤 견적 줄 섹션이 `ui/table`로 바뀌게 한다 — Task 2의 섹션이 게이트 판정을 읽으므로 상태가 바뀌면 자동으로 따라온다. 잠김 이유를 `Button`의 `disabledReason`으로 넘긴다(`완료(정산) 프로젝트 · 잠김`).

**비용 귀속은 이 페이즈가 하지 않는다.** PROJ-04의 「미수주 비용으로 내려간다」는 손익 계산(Phase 10 PNL-07)의 몫이고 이 페이즈는 상태를 바꿀 뿐이다. 그 경계를 `status-section.tsx` 주석에 적어 다음 페이즈가 이 화면에서 비용을 찾지 않게 한다.

전이 액션을 `actions.ts`에 더하고 등록·누수 스캔 목록을 갱신한다. **전부 POST**다.

`test/e2e/project-detail.spec.ts`에 케이스를 더한다: ① 수주중 프로젝트에 `진행으로 전환` 버튼이 있고 `완료(정산)`은 없다(또는 비활성 — 고른 쪽대로) ② 누르면 **같은 화면에서** 상태 태그가 바뀌는 것을 먼저 단언하고 그 다음 필요하면 이동한다 ③ 토스트가 뜨지 않는다 ④ `미수주로 닫기`가 모달을 열고 결과 한 줄 문구가 보인다 ⑤ 미수주 프로젝트에 `되살리기`가 있다 ⑥ 완료(정산) 뒤 견적 줄 표의 머리글이 편집용이 **아니고** 편집 버튼이 이유와 함께 비활성이다 ⑦ 붉은 버튼이 없다(계산된 스타일로 `--danger` 배경 0). 만든 데이터를 치운다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/project-detail.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. ②가 실패하면 변경 클릭 직후 이동한 것이다 — 이동의 SELECT가 액션의 UPDATE 커밋보다 먼저 읽는다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/rules test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 게이트 단위 테스트가 깨지면 화면이 게이트를 바꾼 것이다(화면은 읽기만 해야 한다)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test:e2e</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; pnpm test:integration</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 전이 목록이 게이트에서 오고 화면에 전이 표가 없다
    - 확인이 필요한 둘만 모달을 거치고 문구가 정본이다
    - 비파괴 둘이 확인 없이 되고 토스트가 없다
    - 전이가 행동 로그에 남는다
    - 완료(정산) 뒤 견적 줄이 읽기용 표이고 편집 버튼이 이유와 함께 비활성이다
    - 붉은 버튼이 없다
    - 전이가 전부 POST다
    - 거부된 전이의 표현(숨김 vs 비활성) 판단과 근거가 주석에 있다
    - 비용 귀속이 이 페이즈 범위 밖이라는 경계가 주석에 있다
    - E2E가 변경 뒤 같은 화면에서 먼저 단언하고 만든 데이터를 치운다
  </acceptance_criteria>
  <done>상태 전이 넷이 화면에서 되고 위험한 둘만 확인을 거치며, 완료(정산) 프로젝트의 견적 줄이 잠긴 읽기 표가 된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 그리드 클라이언트 상태 → 저장 페이로드 | 화면이 계산한 차익·환산액이 페이로드에 실리면 PROJ-02가 깨진다 |
| URL·GET → 상태 변경 | 승인·삭제·상태 변경이 GET으로 되면 링크 하나로 남의 프로젝트가 바뀐다(ROADMAP 기준 2) |
| 게이트 판정 → 화면 표현 | 화면이 전이 규칙을 다시 쓰면 게이트와 두 진실이 공존한다 |
| 편집 신호 → 실제 편집 권한 | 완료 잠김·이전 차수를 편집용 표로 그리면 시각 신호가 거짓이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-67 | Elevation of Privilege | 승인·삭제·상태 변경이 GET으로 가능 | high | mitigate | 전부 서버 액션(POST)이고 `authedActionClient`를 지난다. ROADMAP 기준 2의 명시 요구이며 E2E가 화면 경로를 덮는다. GET 라우트 핸들러를 만들지 않는다 |
| T-04-68 | Tampering | 화면이 계산한 금액이 저장됨 | high | mitigate | `money-boundary` 린트가 `app/` 아래 금액 산술을 잡고, 서버가 요청의 파생값을 폐기한다(04-09). 화면은 서버에서 온 문자열만 렌더한다 |
| T-04-69 | Elevation of Privilege | 화면이 전이 규칙을 다시 써 게이트와 어긋남 | high | mitigate | 전이 목록을 게이트에서 받아 렌더한다. 게이트 단위 테스트가 정본이고 화면 테스트가 그것을 복제하지 않는다 |
| T-04-70 | Spoofing | 완료 잠김·이전 차수가 편집 가능한 것처럼 보임 | medium | mitigate | 그 경우 `ui/table`로 렌더한다(흰 머리글). 정규식 게이트가 `ui/table` import 존재를 검사하고 E2E가 머리글이 편집용이 아님을 DOM으로 단언한다 |
| T-04-71 | Information Disclosure | 차익 열이 응답에 남아 기획본부에 노출 | high | mitigate | 열 선언을 DTO 키 존재 여부로 만들고 계급 분기를 화면에 두지 않는다. `leak-scan`이 DTO 매핑을 강제한다 |
| T-04-72 | Tampering | 충돌 저장이 부분 반영되어 사용자가 어느 줄이 저장됐는지 모름 | high | mitigate | 서버가 전부 거부하고(04-09) 화면이 합계 행에 `오류 N칸 · 전부 거부`를 렌더하며 입력을 보존한다. E2E 충돌 케이스가 이것을 덮는다 |
| T-04-73 | Information Disclosure | 저장 결과·충돌 이유가 보조 기술에 전달되지 않음 | medium | mitigate | 합계 행 `aria-live="polite"`(04-10) + 오류 셀 `aria-invalid`/`aria-describedby`. 잠김 이유는 `Button`의 `aria-describedby`(04-02 F4) |
| T-04-74 | Denial of Service | 폰 375px에서 그리드가 넘쳐 화면을 못 씀 | medium | mitigate | 폰에서 셀 편집 없음 + P1 3열 + 힌트 줄 숨김. `mobile-quote-grid.spec.ts`가 `scrollWidth <= 375`를 DOM으로 실측한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). 클립보드 E2E도 `grantPermissions`·새 라이브러리 없이 네이티브 이벤트로 한다. `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint`(eslint + stylelint) · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` · `pnpm test:integration` 통과
- `pnpm db:reset:test && CI=true pnpm test:e2e` 전체 통과 — **로컬 dev 통과는 완료 신호가 아니다**
- `git diff --name-only`에 `db/migrations/`·`ui/`·`domain/`·`repositories/` 파일이 0개 — 화면만 만든다
- 커밋이 셋이다 — 골격·매출 칸 / 차수·견적 줄 표 / 상태 전환
- **웨이브 종료 게이트 — 순서가 CLAUDE.md의 프론트엔드 규칙이다(N6): 싼 게이트(`pnpm lint` · `pnpm typecheck` · `pnpm build`) → 독립 DOM 감사 → 수정 → 전체 게이트 한 번.** 전체 게이트를 두 번 돌리지 않기 위한 순서다
- **독립 DOM 감사(N6)** — **실행자가 아닌 별도 에이전트**가 `CI=true`로 이 플랜의 화면 DOM을 실측 판정한다. **스크린샷 육안 판정 금지**(CLAUDE.md). 감사 대상은 ① 이 플랜의 화면 계약(열 우선순위·375px 넘침 없음·접근 가능한 이름·라벨 칸 실측 폭·포커스 링·터치 44) ② 앞 웨이브의 컴포넌트 플랜(04-07·04-08·04-10)이 SUMMARY에 **등록해 넘긴 감사 항목** — 그 플랜들은 사용처가 0이라 실측할 DOM이 없었고 이 웨이브가 그것을 실제로 렌더한다. 근거: Phase 3의 DOM 감사가 결함 7건을 찾았고, 실행자가 자기 스펙에 감사 단언을 쓰면 자기가 만든 것을 자기가 통과시킨다
- 웨이브 종료 시 `/review` · **(독립 DOM 감사)** · `/design-review` · `/qa`를 돌린다 — 감사가 `/design-review`보다 **앞**이다. 이 플랜은 권한·외부 입력(클립보드)을 다루므로 `/cso`도 돌린다(CLAUDE.md)
- **수동 검증(04-VALIDATION.md)**: 실제 스프레드시트에서 범위를 복사해 견적 줄 표에 붙여넣어 TSV 형태가 맞는지 확인하고 결과를 `04-VERIFICATION.md`에 적는다
</verification>

<success_criteria>
- 매출 칸의 부가세·합계·역산이 서버 계산으로 보인다(PROJ-03)
- 견적 줄 입력이 키보드만으로 끝난다(UX-05)
- 일괄 저장이 전부 저장 또는 전부 거부이고 충돌이 셀에 표시된다(UX-04 · D-48)
- 차수와 고객 승인이 서고 이전 차수가 읽기 전용으로 보존된다(PROJ-07 · D-55)
- 상태 전이 넷이 되고 완료(정산)이 견적 줄을 잠근다(PROJ-04 · D-41 · D-44)
- 「네 숫자 줄」을 0이나 `—`로 채우지 않았다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 화면 | `app/(app)/projects/[id]/page.tsx` + 섹션 넷(`revenue-section` · `quote-section` · `quote-grid-section` · `status-section`) |
| 새 서버 액션 | 매출 칸 저장 · 견적 줄 일괄 저장 · 고객 승인 표시 · 복사해 새 차수 · 줄 삭제/취소 · 상태 전이 넷 |
| 새 테스트 | `test/e2e/quote-grid-keyboard.spec.ts` · `test/e2e/project-detail.spec.ts` · `test/e2e/mobile-quote-grid.spec.ts` |
| 변경된 테스트 | `test/integration/leak-scan.test.ts`(등록 목록 확인) |
| 새 표 · 새 마이그레이션 · 새 컴포넌트 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-12-SUMMARY.md` when done
</output>
