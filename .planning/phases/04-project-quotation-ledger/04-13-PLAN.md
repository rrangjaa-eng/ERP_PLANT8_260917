---
phase: 04-project-quotation-ledger
plan: 13
type: execute
wave: 6
depends_on: ["04-11", "04-12"]
files_modified:
  - repositories/reserve-ledger.ts
  - domain/reserve-ledger/index.ts
  - app/(app)/pnl/reserve/page.tsx
  - app/(app)/pnl/reserve/reserve-list.tsx
  - app/(app)/pnl/reserve/reserve-form.tsx
  - app/(app)/pnl/reserve/actions.ts
  - app/(app)/pnl/reserve/actions.registry.ts
  - app/(app)/pnl/reserve/reserve.module.css
  - app/(app)/pnl/page.tsx
  - app/(app)/layout.tsx
  - ui/page-header/PageHeader.tsx
  - ui/shell/role-menu.ts
  - ui/shell/MoreSheet.tsx
  - test/integration/reserve-ledger.test.ts
  - test/integration/leak-scan.test.ts
  - test/unit/ui/role-menu.test.ts
  - test/e2e/reserve.spec.ts
  - test/e2e/mobile-reserve.spec.ts
autonomous: true
requirements: [RSV-01, FX-01]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "동시에 들어온 출금 둘이 각자 잔액을 확인하고 각자 통과하는 창이 없다 — 한 클라이언트의 대장 쓰기가 행 잠금으로 **직렬화**되고, 그 사실이 `Promise.all` 통합 테스트로 증명된다(B4)"
    - "클라이언트·연결 프로젝트 칸이 `ui/input/Autocomplete`를 거친다 — 화면이 목록 배선을 직접 적지 않는다(D-83)"
    - "경영관리가 클라이언트별 입금·출금을 날짜순으로 기록하면 잔액이 서버 계산으로 보인다(RSV-01)"
    - "잔액이 음수가 되는 쓰기를 DB가 거부하고 화면이 그 이유를 원인 · 다음 행동 형식으로 말한다"
    - "각 줄을 프로젝트에 연결할 수 있고 연결이 선택이다"
    - "금액이 통화·환율·원화 환산액을 함께 담고 표시가 §2-4 외화 2행 규칙이다(FX-01)"
    - "리저브가 기획본부에게 기본 숨김이고, 숨겨지면 진입점 둘과 라우트 모두 렌더되지 않는다"
    - "1차 메뉴가 여전히 다섯이다 — 진입점이 손익 화면 부제 줄의 3차 링크와 폰 「더보기」 시트 한 줄뿐이다(D-64)"
    - "0건이면 EMPTY 한 줄이 본문 자리를 대체하고 합계 행이 없다(F9)"
    - "375px에서 P1이 셋이고 잔액이 접힌 줄로 내려간다 — 가로 스크롤이 없다"
  prohibitions:
    - statement: "**잔액 예측을 트랜잭션 밖에서, 행 잠금 없이 계산해서는 안 된다.** READ COMMITTED에서 두 출금 요청이 동시에 들어오면 둘 다 같은 「직전 잔액」을 읽고 각자 양수인 새 행을 계산해 **둘 다 도메인 판정을 통과하고 둘 다 `CHECK (balance >= 0)`을 통과한다** — 저장된 각 행의 잔액은 양수인데 실제 잔액만 음수가 된다. 04-05의 위협 표가 적은 「`CHECK (balance >= 0)` DB 제약」은 **단일 요청에서만** 참이다. 트랜잭션을 **먼저** 열고 그 클라이언트의 대장 행을 잠근 뒤에 예측·삽입·재계산을 한다"
      status: resolved
      verification: automated
    - statement: "1차 메뉴를 늘려서는 안 된다. `ui/shell/role-menu.ts:45-51`의 `TOP_BAR_MENU`가 정확히 다섯을 박아 뒀고 `test/e2e/keyboard-nav.spec.ts:134`가 「메뉴 5개」를 단언한다(실측). 늘리면 §6-0 · `role-menu.ts` · `DECISIONS.md` · 테스트 둘을 전부 고쳐야 한다(D-64)"
      status: resolved
      verification: automated
    - statement: "리저브 대장을 `ui/grid`로 렌더해서는 안 된다. §7-3이 편집용 표의 용례를 「견적·카드·목표」로 적고 리저브를 넣지 않았으며, 잔액이 **행 순서에 의존하는 누적 계산**이라 범위 붙여넣기·행 독립 일괄 저장 계약과 맞지 않는다. `ui/table` + `?new=1` 토글 + `ui/form`이다"
      status: resolved
      verification: automated
    - statement: "P1에 넷을 두어서는 안 된다. 순진하게 두면 종류·금액·잔액·연결 프로젝트가 넷이 된다 — 해소는 **잔액을 P2 접힌 줄로** 내리고 합계 행이 잔액을 대신 말하는 것이다(화면 계약 8). `column-fold.ts`의 dev 검사가 이 상한을 코드에서 잡는다"
      status: resolved
      verification: automated
    - statement: "0건일 때 합계 행에 `잔액 0`을 써서는 안 된다. §7-7 EMPTY 한 줄이 본문 자리를 **대체**하므로 합계 행을 놓을 자리 자체가 생기지 않는다(F9). §2-4의 「0은 `0`」은 **행이 있고 그 값이 0일 때**의 규칙이다"
      status: resolved
      verification: automated
  artifacts:
    - path: "repositories/reserve-ledger.ts"
      provides: "클라이언트별 대장 조회(날짜순) · 삽입 · 잔액 재계산. 잔액이 저장 컬럼이고 쓰기 시점에 서버가 채운다"
      exports: ["listReserveEntries", "findReserveEntryById", "insertReserveEntry", "recomputeBalancesFrom"]
      min_lines: 70
    - path: "domain/reserve-ledger/index.ts"
      provides: "DTO 명세 + registerDto + 잔액 계산 + 음수 거부 + 정보 노출 게이트"
      exports: ["RESERVE_LEDGER_DTO_SPEC", "listReserveLedger", "addReserveEntry"]
      min_lines: 80
    - path: "app/(app)/pnl/reserve/page.tsx"
      provides: "§6-1 목록 + `?new=1` 토글. 정보 노출이 막으면 라우트 자체가 404다"
      min_lines: 50
    - path: "test/integration/reserve-ledger.test.ts"
      provides: "입금·출금·잔액 계산 · 음수 DB 제약 거부 · 정보 노출 숨김(RSV-01)"
      min_lines: 80
  key_links:
    - from: "app/(app)/pnl/page.tsx"
      to: "app/(app)/pnl/reserve/page.tsx"
      via: "손익 화면 부제 줄의 3차 링크가 진입점이다 — 1차 메뉴를 늘리지 않는다(D-64)"
      pattern: "리저브 대장"
    - from: "ui/shell/role-menu.ts"
      to: "app/(app)/pnl/reserve/page.tsx"
      via: "폰 「더보기」 시트의 손익 그룹에 한 줄을 더한다 — TOP_BAR_MENU는 건드리지 않는다"
      pattern: "reserve"
    - from: "domain/reserve-ledger/index.ts"
      to: "domain/permissions/project.ts"
      via: "DTO 투영이 리저브 정보 항목으로 게이트한다 — 기획본부 기본 숨김"
      pattern: "project("
---

<objective>
**As a** 경영관리본부 담당자, **I want to** 클라이언트별로 받아 둔 리저브의 입금·출금을 날짜순으로 적고 잔액을 보고 싶다, **so that** 프로젝트 매출을 리저브에서 충당할 때(Phase 9 RSV-02) 쓸 잔액이 이미 정확하게 서 있다.

이 페이즈의 **마지막 화면**이다. 04-07·04-08의 컴포넌트와 04-09가 만든 권한·정보 노출 배선을 쓴다.

Purpose: ROADMAP Phase 4 기준 6이 「경영관리가 클라이언트별 리저브 대장에 입금·출금을 날짜순으로 기록하면 잔액이 서버 계산으로 보이고, 각 줄은 프로젝트에 연결할 수 있으며 5의 금액 모델로 적힌다. **리저브는 정보 노출표의 새 항목이라 기획본부에게는 기본 숨김이다**」를 요구한다.

Output: 리포지토리·도메인, 목록 화면 + 등록 폼, 진입점 둘, 통합 테스트, E2E 둘.

**이 플랜을 Phase 4에 남긴 판단(D-62).** ROADMAP Overview가 「5플랜을 넘기면 리저브 대장(RSV-01)을 뗀다」를 사전 승인했고 이 페이즈는 13플랜이 됐다. 그런데도 떼지 않은 이유 셋 — ① 떼면 13에서 12가 될 뿐이다. 이 페이즈가 큰 이유는 엑셀식 그리드·금액 모델·표 다섯·UI 표면 아홉이고 리저브가 아니다 ② 리저브는 이 페이즈에서 **가장 싼 표면**이다: `ui/table` + `ui/form` + `domain/money`를 그대로 쓰고 새로 만드는 것은 표 하나와 DB 제약 하나다 ③ Phase 9의 RSV-02가 이 대장을 필요로 하므로, 별도 페이즈로 떼면 Phase 4에 의존하는 페이즈가 하나 더 생겨 의존 그래프만 복잡해지고 Phase 4의 컨텍스트 비용은 줄지 않는다. D-62가 이 판단을 계획자에게 맡겼고 Phase 3이 같은 판단(D-33)으로 7플랜이 되어 무너지지 않았다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-project-quotation-ledger/04-CONTEXT.md
@.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quotation-ledger/04-09-SUMMARY.md
@.planning/phases/04-project-quotation-ledger/04-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 리저브 도메인 — 잔액 서버 계산 · 음수 거부 · 기본 숨김</name>
  <files>repositories/reserve-ledger.ts, domain/reserve-ledger/index.ts, test/integration/reserve-ledger.test.ts</files>
  <read_first>
    - 04-05가 만든 `db/schema/reserve-ledger.ts` — 실제 컬럼 이름과 **잔액 CHECK 제약**. 04-05 Task 2가 「잔액을 저장 컬럼으로 두고 쓰기 시점에 서버가 계산해 채운다」를 골랐는지 SUMMARY로 확인한다
    - `.planning/REQUIREMENTS.md` RSV-01(74행) 전문 — 「클라이언트별 리저브 대장: 입금·출금·잔액을 **날짜순으로** 기록하고 본다. 각 줄은 프로젝트에 연결할 수 있고, 통화·환율·원화 환산액을 함께 적는다」
    - `docs/inputs/phase-04-project-quote.md` §6 리저브 대장 전문 — 음수 잔액 금지의 출처와 실제 업무 형태
    - `.planning/ROADMAP.md` Phase 4 기준 6 — 「리저브는 정보 노출표의 새 항목이라 기획본부에게는 기본 숨김이다. 프로젝트 매출을 리저브에서 충당하는 동작은 매출 기준이 생기는 Phase 9(RSV-02)에서 붙는다」
    - 04-09가 등록한 리저브 정보 항목의 정확한 키 이름과 `staffDefault: false`
    - 04-09가 `ENTITY_MENUS`에 더한 `reserve_ledger → "pnl"` 줄
    - `domain/vendors/index.ts` 49–69행 — `DtoSpec` + `registerDto` 파생 형태. **04-PATTERNS의 지적**: 「거래처는 10개가 전부 `vendor.value` 하나지만, 리저브는 「정보 노출표의 새 항목이라 기획본부 기본 숨김」이므로 **금액 필드에 다른 `infoItem`을 물릴 수 있는 구조다**」 — 리저브에서 어느 필드가 어느 항목에 걸릴지 판단한다. 전부 한 항목으로 묶어도 되는지(대장 자체가 숨김이므로) 판단하고 이유를 적는다
    - `domain/money/index.ts`(04-03) — `Money`·`round`·`toKrw`. 잔액 누적이 이 모듈을 지난다
    - `domain/exchange-rates/index.ts`(04-09) — `latestRateFor`. 통화를 고르면 기본 환율이 여기서 온다
    - `repositories/settings.ts` 127–152행 — `db.transaction` 선례. 잔액 재계산이 삽입과 같은 트랜잭션에 있어야 한다. **그러나 이 선례에는 행 잠금이 없다** — 이 저장소에 `FOR UPDATE`·advisory lock 사용처가 **0건**이고(grep 확인) 04-04가 `UPDATE … RETURNING`의 암묵 행 잠금을 처음 도입한다. 리저브는 **읽고 계산한 뒤 쓰는** 형태라 그 암묵 잠금만으로는 부족하다
    - `repositories/document-counters.ts`(04-04) — 직렬화가 필요한 쓰기의 첫 선례와 그 주석. **같은 문제의 다른 형태**임을 확인한다: 카운터는 `UPDATE … SET value = value + 1`이 한 문장이라 잠금이 자동이고, 리저브는 `SELECT` → 계산 → `INSERT`라 **명시 잠금이 필요하다**
    - `test/integration/document-counters.test.ts`의 동시 증가 케이스(04-04) — `Promise.all`로 두 호출을 동시에 발사하고 결과 집합을 단언하는 형태. **이 태스크의 동시 출금 테스트가 그 형태를 그대로 쓴다**(B4)
    - `vitest.config.ts` integration project — `fileParallelism: false`는 **테스트 파일 간** 병렬만 막고 한 테스트 안의 `Promise.all`은 막지 않는다(04-RESEARCH Pattern 3 실측)
    - `04-05-PLAN.md`의 위협 표 `T-04-24` — mitigation이 「`CHECK (balance >= 0)` DB 제약」이고 **그것이 동시 경로에서 참이 아니라는 정정이 같은 행에 적혀 있다.** 직렬화는 이 태스크의 몫이다
    - `domain/archive/index.ts` 58–86행 + `repositories/archive.ts` `ARCHIVABLE_TABLES` — 리저브가 보관 대상인지 판단한다. ROADMAP이 「보관함·행동 로그·정보 노출표 규약을 그대로 따른다」고 적었으므로 **보관 대상이 기본**이다
    - `domain/action-log/record.ts` — 리저브 기록이 행동 로그에 남는지. 어느 종류를 쓸지 판단한다
    - `domain/rules/gate.ts`(04-04) — 음수 잔액 거부를 게이트로 둘지 DB 제약 + 도메인 판정으로 둘지 판단한다. **DB 제약이 정본**이고(04-05) 도메인은 사용자에게 §8-3 형식의 이유를 주기 위해 **먼저** 판정한다 — DB 예외 메시지를 화면에 그대로 내보내지 않는다
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` Copywriting의 「Error — 리저브 잔액」 행 — `잔액이 음수가 됩니다 · 출금액을 줄이거나 입금을 먼저 기록해 주세요`. **이 문구가 도메인 오류의 message다**
    - `lib/actions/user-facing-error.ts` — 그 message가 화면에 닿으려면 이 계열을 지나야 한다
  </read_first>
  <behavior>
    - 입금을 기록하면 잔액이 그만큼 늘고, 출금을 기록하면 줄어든다
    - 잔액이 서버 계산값이고 요청에 담긴 잔액은 무시된다
    - 날짜순 정렬이고 같은 날짜의 순서가 결정적이다(기록 시각 또는 id로 보조 정렬 — 고른 쪽을 고정한다)
    - 과거 날짜에 줄을 끼워 넣으면 그 뒤 줄들의 잔액이 다시 계산된다
    - 잔액이 음수가 되는 요청이 **도메인에서** 거부되고 그 이유가 §8-3 형식이며 `UserFacingError` 계열이다
    - **동시에 발사한 출금 둘 중 정확히 하나만 성공한다** — 둘 다 단독으로는 통과하지만 합치면 잔액이 음수가 되는 금액으로 시험한다. 실패한 쪽의 message가 Copywriting 정본 문구다(B4)
    - 같은 클라이언트의 대장 쓰기가 직렬화된다 — 다른 클라이언트의 쓰기는 서로 막지 않는다
    - DB 제약이 두 번째 방어선이다 — 도메인을 우회한 쓰기도 거부된다
    - 프로젝트 연결이 없는 줄이 정상이다
    - 통화가 있으면 원화 환산액이 서버 계산이고 환율이 없으면 `null`이다
    - 기획본부 계급으로 조회하면 목록이 비고(또는 거부되고) 그 판정이 정보 노출표에서 온다
    - 기록이 행동 로그에 남는다
  </behavior>
  <action>
`test/integration/reserve-ledger.test.ts`를 **먼저** 만든다(RED 확인). behavior 줄들을 케이스로 옮긴다. 핵심 셋:
- **과거 날짜 삽입 후 뒤 줄들의 잔액 재계산**(잔액이 저장 컬럼이라 이것이 필요하다)
- **음수 거부가 도메인에서 먼저** 나고 그 message가 Copywriting 정본 문구다
- **동시 출금 직렬화(B4)** — `test/integration/document-counters.test.ts`의 동시 증가 케이스와 **같은 형태**로 쓴다: 잔액 100을 만든 뒤 출금 60 둘을 `Promise.all`로 동시에 발사한다(각각 단독으로는 통과하고 둘이 합쳐지면 음수가 되는 금액이다). 단언 셋 — ① **정확히 하나만 성공한다** ② 실패한 쪽이 `UserFacingError` 계열이고 message가 Copywriting 정본 문구다 ③ **최종 저장 잔액이 40이다**(음수가 아니다). 케이스 제목에 「`CHECK (balance >= 0)`만으로는 이 경로를 막지 못한다 — 두 행 모두 자기 값은 양수다」를 적는다. 다른 클라이언트의 동시 출금이 서로 막지 않는 케이스도 하나 둔다(잠금 범위가 클라이언트 단위임을 고정한다)

`repositories/reserve-ledger.ts`:
- `listReserveEntries(viewer, opts: { scope: Scope; clientId?: string })` — `repositories/vendors.ts` 형태. `scope.rows === "none"`이면 빈 배열. 정렬은 날짜 + 보조 키
- `findReserveEntryById` — `archivedAt` 필터 여부를 호출처별로 판단하고 이유를 주석에 남긴다(04-01이 확인한 함정)
- `insertReserveEntry(viewer, input, tx?)`
- `lockClientLedger(viewer, clientId, tx)` — 그 클라이언트의 대장 행을 `FOR UPDATE`로 잠그는 함수(B4). **`tx`가 필수 인자다** — 트랜잭션 밖에서 부르면 잠금이 즉시 풀려 아무 의미가 없으므로 선택 인자로 두지 않는다. `sql` 템플릿으로 `FOR UPDATE`를 붙이고(이 저장소 첫 사용처다 — `repositories/health.ts:1`·`system-status.ts:1`의 `sql` import 선례를 따른다), 행이 0건일 때의 동작과 고른 직렬화 방법을 함수 위 주석에 적는다
- `recomputeBalancesFrom(viewer, clientId, fromDate, tx?)` — 그 날짜 이후 줄들의 잔액을 다시 계산해 UPDATE한다. **삽입과 같은 트랜잭션**에서 돌아야 하고, 그 트랜잭션은 이미 `lockClientLedger`로 잠근 상태다 — 과거 날짜 삽입 중 다른 요청이 끼어드는 경쟁도 같은 잠금이 막는다
- `setReserveEntryArchived`(보관 대상으로 결정했다면)

`domain/reserve-ledger/index.ts`:
- `RESERVE_LEDGER_DTO_SPEC` + `registerDto` — `infoItem` 배정 판단을 주석에 적는다
- `listReserveLedger(viewer, clientId, deps?)` — `scopeFor(viewer, "reserve_ledger")` + `project()` 투영. 정보 노출이 막으면 결과가 비거나 필드가 빠진다
- `addReserveEntry(viewer, input, deps?)` — 순서가 계약이다: ① `can` 쓰기 권한 ② 통화가 있으면 `latestRateFor`로 환율 기본값을 채우거나 입력값을 쓰고 `toKrw`로 환산 ③ **트랜잭션을 연다** ④ **그 트랜잭션 안에서 해당 클라이언트의 대장 행을 `FOR UPDATE`로 잠근다** ⑤ 잠근 상태에서 **잔액 예측을 계산해 음수면 도메인 오류로 거부**(message는 Copywriting 정본 — 던지면 트랜잭션이 롤백된다) ⑥ 같은 트랜잭션에서 삽입 + `recomputeBalancesFrom` ⑦ `recordAction`. 이 일곱 단계를 함수 위 주석에 번호로 적는다
  · **③④가 ⑤보다 앞인 것이 이 함수의 핵심이다(B4).** 예측을 트랜잭션 밖에서·잠금 없이 계산하면 READ COMMITTED에서 두 요청이 같은 「직전 잔액」을 읽고 **각자 양수인 행을 쓰며 두 층(도메인 판정 + `CHECK (balance >= 0)`)을 모두 통과한다** — 저장된 값은 전부 양수인데 실제 잔액만 음수다. `CHECK` 제약은 **한 행의 값**만 보므로 이 경로를 원리적으로 막을 수 없다. 이 문단을 주석에 그대로 적어 다음 사람이 잠금을 「불필요한 비용」으로 걷어내지 않게 한다
  · 잠금 범위는 **그 클라이언트의 대장**이다 — 다른 클라이언트의 쓰기를 막지 않는다. 표 전체 잠금이나 `SERIALIZABLE` 격리로 올리지 않는 이유(경합 범위가 클라이언트 단위이고 나머지는 대가만 크다)를 함께 적는다
  · 잠글 행이 하나도 없는 첫 입금에서도 동작해야 한다 — 행이 0건이면 `FOR UPDATE`가 아무것도 잠그지 않으므로 그 경우의 직렬화 방법(클라이언트 행을 잠그거나 `(client_id)` 기준 advisory lock)을 판단해 고르고 이유를 주석에 적는다. **판단을 미루지 않는다** — 첫 입금 둘이 동시에 들어오는 것은 실제로 일어난다
- 잔액 음수 판정을 **도메인에서 먼저** 하는 이유를 주석에 적는다: DB 제약이 정본이지만 그 예외 메시지는 사용자에게 보여 줄 문장이 아니다(§8-3 형식이 아니고 영문이다). 도메인이 먼저 판정해 §8-3 문장을 주고, DB 제약은 우회 경로의 방어선으로 남는다
- 새 환율 입력이 있으면 `recordRate`를 부른다(04-09 — 행동 로그가 따라온다)
- `ARCHIVABLE_TABLES`에 리저브를 더한다(보관 대상으로 결정한 경우). 순서 계약(`findById` → `isProtected` → `setArchived` → `recordAction`)을 지킨다
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/reserve-ledger.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. 과거 날짜 삽입 케이스가 깨지면 `recomputeBalancesFrom`이 없거나 트랜잭션 밖에 있다. 음수 케이스가 DB 예외로 실패하면 도메인 판정이 없어 영문 예외가 화면까지 갈 상태다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('domain/reserve-ledger/index.ts','utf8');if(!/UserFacingError|Error extends/.test(s)){console.error('NO USER FACING ERROR FOR NEGATIVE BALANCE');process.exit(1)}if(!/recordAction/.test(s)){console.error('NO ACTION LOG');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `NO USER FACING ERROR FOR NEGATIVE BALANCE` 또는 `NO ACTION LOG`가 나오고 종료 코드가 1이다</fails_when>
    <automated>node -e "const fs=require('fs');const repo=fs.readFileSync('repositories/reserve-ledger.ts','utf8').replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');if(!/FOR UPDATE|pg_advisory_xact_lock/i.test(repo)){console.error('RESERVE LEDGER HAS NO ROW LOCK — TWO CONCURRENT WITHDRAWALS BOTH PASS THE FORECAST AND BOTH PASS CHECK (B4)');process.exit(1)}const dom=fs.readFileSync('domain/reserve-ledger/index.ts','utf8').replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');const i=dom.indexOf('addReserveEntry');if(i<0){console.error('addReserveEntry NOT FOUND');process.exit(1)}const body=dom.slice(i);const t=body.indexOf('transaction'),l=body.search(/lockClientLedger|FOR UPDATE|advisory/i);if(t<0){console.error('addReserveEntry OPENS NO TRANSACTION');process.exit(1)}if(l<0||l<t){console.error('addReserveEntry LOCKS BEFORE OPENING THE TRANSACTION (OR NOT AT ALL) — THE FORECAST MUST RUN INSIDE THE LOCK (B4)');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `RESERVE LEDGER HAS NO ROW LOCK …`(B4 그 자체 — 동시 출금 둘이 두 층을 모두 통과한다) 또는 `addReserveEntry LOCKS BEFORE OPENING THE TRANSACTION (OR NOT AT ALL) …`(잠금이 트랜잭션 밖이라 즉시 풀린다) 중 하나가 나오고 종료 코드가 1이다. 판정은 주석을 걷어낸 본문에서 하고, 잠금을 advisory lock으로 구현했다면 게이트의 패턴을 실제 표현으로 맞춘다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. `no-row-type-escape`가 domain 반환 타입을 잡으면 `project()`를 거치지 않았고, `money-boundary`가 잔액 누적을 잡으면 `domain/money`를 거치지 않았다. grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 잔액이 서버 계산이고 요청 값이 무시된다
    - 과거 날짜 삽입이 뒤 줄들의 잔액을 다시 계산하고 그것이 삽입과 같은 트랜잭션이다
    - 음수 거부가 도메인에서 먼저 나고 message가 Copywriting 정본 문구이며 `UserFacingError` 계열이다
    - DB 제약이 두 번째 방어선으로 남아 있다 — **그러나 동시 경로를 막는 것은 그 제약이 아니라 행 잠금임**이 주석에 적혔다(B4)
    - 트랜잭션을 **먼저** 열고 그 클라이언트의 대장을 잠근 뒤에 잔액 예측을 계산한다 — 정규식 게이트가 순서를 확인한다
    - 동시 출금 둘 중 정확히 하나만 성공하고 최종 잔액이 음수가 아님이 `Promise.all` 통합 테스트로 증명된다
    - 실패한 쪽의 message가 Copywriting 정본 문구다
    - 다른 클라이언트의 동시 출금이 서로 막지 않는다 — 잠금 범위가 클라이언트 단위다
    - 대장 행이 0건인 첫 입금에서의 직렬화 방법이 판단·주석으로 확정됐다
    - 프로젝트 연결 없는 줄이 정상이다
    - 통화·환율·원화 환산액이 함께 기록되고 환율 없음이 `null`이다
    - 기획본부 계급 조회가 정보 노출표 판정으로 막힌다
    - 기록이 행동 로그에 남는다
    - 일곱 단계 순서가 함수 주석에 번호로 있고, ③④(트랜잭션·잠금)가 ⑤(예측)보다 앞인 이유가 문단으로 적혔다
    - `infoItem` 배정 판단이 주석에 있다
  </acceptance_criteria>
  <done>리저브 입금·출금·잔액이 서버 계산으로 동작하고, 음수 잔액이 단일 요청에서는 두 층(도메인 문장 · DB 제약)으로, 동시 요청에서는 행 잠금 직렬화로 막히며, 기획본부에게 기본 숨김이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 리저브 대장 화면 + 등록 폼 — P1 3열과 확정적 EMPTY</name>
  <files>app/(app)/pnl/reserve/page.tsx, app/(app)/pnl/reserve/reserve-list.tsx, app/(app)/pnl/reserve/reserve-form.tsx, app/(app)/pnl/reserve/actions.ts, app/(app)/pnl/reserve/actions.registry.ts, app/(app)/pnl/reserve/reserve.module.css, test/integration/leak-scan.test.ts, test/e2e/reserve.spec.ts, test/e2e/mobile-reserve.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 8의 「표」 블록 전문** — §6-1 목록 템플릿 + `ui/table` · **`ui/grid`를 쓰지 않는 근거 둘**(§7-3이 편집용 용례에 리저브를 넣지 않았고, 잔액이 행 순서에 의존하는 누적 계산이라 범위 붙여넣기·행 독립 일괄 저장과 맞지 않는다) · 줄 등록은 `?new=1` 토글 + `ui/form` · **열과 우선순위**(날짜 P2 date · 종류 입금/출금 **P1** status · 금액 **P1** money · 연결 프로젝트 P2 text 선택 · 적요 P3 text · 잔액 P2 money) · **P1 4열 문제와 해소**(P1은 연결 프로젝트 또는 적요(문자 1) + 금액(1) + 종류(1) 셋이고 잔액은 P2 접힌 줄로 내린다. 잔액은 합계 행이 대신 말한다) · 합계/잔액(마지막 행의 잔액을 합계 행에 `잔액 48,000,000`으로) · 금액은 §2-4 외화 2행 · **0건일 때 합계 행 없음(F9)** · 다섯 상태
    - 같은 문서 § UI Considerations의 「overflow · S9 리저브 대장」 backstop 행 — 「P1이 4열 후보라 §7-3의 3열 상한에 걸린다. 화면 계약 8의 해소안(잔액을 P2로)이 375px에서 실제로 읽히는지는 **DOM 실측으로 확인한다**」
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` Copywriting의 리저브 관련 행 — 1차 CTA `리저브 줄 등록` · EMPTY `이 클라이언트에 리저브 줄이 없습니다 · 리저브 줄 등록` · ERROR `리저브 대장을 불러오지 못했습니다 · 다시 시도` · 잔액 오류 `잔액이 음수가 됩니다 · 출금액을 줄이거나 입금을 먼저 기록해 주세요`
    - `ui/table/column-fold.ts`(04-08)의 `assertColumnContract` — **P1 넷을 선언하면 개발 모드 경고가 난다.** 이 화면이 그 경고를 실제로 겪지 않게 잔액을 P2로 둔다
    - `ui/select/Select.tsx`(04-07) — 리저브 종류(입금/출금)와 통화가 이것이다
    - `ui/input/Autocomplete.tsx`(04-07, D-83) — **클라이언트와 연결 프로젝트 두 칸이 쓰는 소유 컴포넌트**다. props와 라벨 있는 모드를 소스에서 확인한다. 04-07 Task 2가 `app/**`의 목록 요소 리터럴 0을 단언하는 테스트를 세워 뒀다
    - `test/e2e/fixtures.ts`(04-02가 확장) — 콘솔 오류·페이지 예외를 자동 수집해 0을 단언하는 확장 `test`. **이 플랜의 새 스펙 둘이 `@playwright/test`가 아니라 `./fixtures`에서 `test`·`expect`를 가져온다**(B7)
    - `app/(app)/projects/page.tsx`(04-11) — `?new=1` 토글의 형태. 같은 메커니즘을 쓴다
    - `app/(app)/projects/actions.registry.ts`(04-11) — 등록 형태
    - `test/integration/leak-scan.test.ts`와 `test/unit/leak-scan-coverage.test.ts` — 새 등록 파일을 import 목록에 더한다
    - `app/(app)/pnl/page.tsx` 현재 전문 — 손익 화면의 지금 상태. Task 3이 여기 링크를 더하므로 이 태스크는 라우트만 만든다
    - `test/e2e/mobile-vendors.spec.ts` — 폰 스펙 형태이자 **데이터 정리 누락의 실측 사례**
    - `playwright.config.ts`의 `MOBILE_SPEC_PATTERN` — 새 폰 스펙 파일 이름이 그 패턴에 맞아야 한다
    - `lib/viewer.ts`의 `requireSession` + `app/(app)/admin/system-status/page.tsx`의 `notFound()` 패턴 — **정보 노출이 막으면 라우트 자체가 404여야 한다**(화면 계약 8: 「서버가 라우트 자체도 막는다」)
  </read_first>
  <behavior>
    - 정보 노출이 막힌 계급으로 라우트에 직접 들어가면 404다
    - 허용된 계급에게 대장이 날짜순으로 보인다
    - 열이 여섯이고 P1이 셋(종류·금액·문자 열 하나)이며 잔액이 P2다
    - 합계 행에 마지막 행의 잔액이 `잔액 {금액}` 형태로 있다
    - **0건이면 EMPTY 한 줄이 본문 자리를 대체하고 합계 행이 없다**
    - `?new=1`이 등록 폼을 열고 기본 진입에는 없다
    - 출금액이 잔액을 넘으면 제출이 거부되고 이유가 Copywriting 정본 문구다
    - 프로젝트 연결을 비워도 등록된다
    - 통화를 고르면 환율 기본값이 채워지고 사람이 고쳐 적을 수 있다
    - 외화 줄의 금액이 두 줄로 보인다
    - 375px에서 `scrollWidth <= 375`이고 잔액이 접힌 줄에 있다
  </behavior>
  <action>
**`test/e2e/reserve.spec.ts`를 먼저 쓴다 — 화면 코드보다 앞에, RED를 눈으로 확인하고 커밋한다(W3).** **`test`·`expect`를 `@playwright/test`가 아니라 `./fixtures`에서 가져온다**(04-02의 확장 — 콘솔 오류·페이지 예외가 자동으로 수집되고 0으로 단언된다, B7). 이 플랜의 폰 스펙도 같다. E2E는 이 페이즈에서 **유일하게 테스트 선행이 가능한 계층**이다: RTL 미설치는 컴포넌트 렌더 단언을 막을 뿐 실제 브라우저를 막지 않는다. 특히 ⑤(막힌 계급에게 라우트가 404)와 ①(0건 EMPTY)은 **화면이 생기기 전에 적어 두지 않으면 구현이 끝난 뒤 「지금 동작하는 대로」 적히는** 항목이다 — 0건은 첫 진입에서 확정적으로 발생하는 상태다.

**RED 확인**: `pnpm db:reset:test && CI=true pnpm playwright test test/e2e/reserve.spec.ts`가 **실패해야** 한다. 실패 메시지를 SUMMARY에 옮겨 적고 그 다음에야 아래 구현으로 넘어간다.

`app/(app)/pnl/reserve/page.tsx`를 만든다. `requireSession()`을 직접 부르고(WR-07 패턴 — 04-11의 주석을 가져온다) **정보 노출 판정이 막으면 `notFound()`**를 낸다. 이것이 화면 계약 8의 「서버가 라우트 자체도 막는다」다. `searchParams`로 `?new=1`을 읽어 폼을 조건부 렌더한다.

`reserve-list.tsx` — `ui/table`. 열 선언을 화면 계약 8 그대로 만들되 **잔액을 `priority: 2`로** 둔다(P1 상한). `caption`·`rowName`을 채운다. 합계 행에 마지막 행의 잔액을 둔다. **0건이면 합계 행을 렌더하지 않는다**(F9) — `Table`의 `empty` 경로가 본문을 대체하므로 `totals`를 조건부로 넘긴다. 그 조건과 F9 근거를 주석에 적는다.

`reserve-form.tsx` — `ui/form` + `Select`(종류 · 통화) + `TextField`(날짜 · 금액 · 적요) + **`Autocomplete`**(클라이언트 · 연결 프로젝트). 자유 입력 자동완성 **두 칸 모두 `ui/input/Autocomplete`**(04-07, D-83)를 쓴다 — 프로젝트가 125건 규모라 `Select`가 아니라 자동완성이 맞고(고른 이유를 주석에 적는다), **목록 배선을 화면이 직접 적지 않는다.** 04-07 Task 2가 `app/**`에 목록 요소 리터럴이 0개임을 단언하는 테스트를 세워 뒀으므로 직접 배선하면 그 테스트가 `파일:줄`과 함께 RED를 낸다. 통화를 고르면 환율 기본값이 채워지게 한다 — 서버에서 온 값으로 채우고 화면이 계산하지 않는다.

자동완성 후보 목록은 **서버가 `scopeFor` 판정을 거친 목록으로만** 만든다 — ADMN-03이 「자동완성·검색에 동일하게 적용된다」를 명시한다. 접근 권한 없는 클라이언트·프로젝트 이름이 후보에 섞이면 그 자체가 정보 노출이다.

`actions.ts` + `actions.registry.ts` — 등록 액션. `authedActionClient` + zod + `revalidatePath`. 잔액 음수 거부가 도메인에서 오므로 액션은 그 오류를 그대로 흘려보낸다(`UserFacingError` 계열이라 화면에 문장이 간다). `test/integration/leak-scan.test.ts` 목록에 새 등록 파일을 더한다.

(위에서 먼저 쓴 `test/e2e/reserve.spec.ts`가 여기서 초록이 되어야 한다.) 그 스펙의 케이스는 이렇다: ① 허용된 계급으로 진입 → **0건이면 EMPTY 한 줄이고 합계 행이 DOM에 없다** ② `?new=1` → 입금 등록 → 목록에 보이고 합계 행에 잔액 ③ 잔액을 넘는 출금 → 거부되고 이유 문구가 보이며 **입력값이 남아 있다**(§7-2) ④ 프로젝트 연결 없이 등록된다 ⑤ 기획본부 계급으로 라우트 직접 진입 → 404. **변경 클릭 직후 `page.goto()`를 하지 않는다.** 만든 데이터를 치운다.

`test/e2e/mobile-reserve.spec.ts`(`mobile-*` 패턴): ① 보이는 열이 셋이다 ② `scrollWidth <= 375` ③ **잔액이 접힌 줄에 있다**(화면 계약 8의 해소안이 실제로 읽히는지 — UI-SPEC이 backstop으로 표시한 항목) ④ 행 높이 44 이상. **DOM 실측**이고 스크린샷 육안이 아니다. 만든 데이터를 치운다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/reserve.spec.ts test/e2e/mobile-reserve.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다. ①에서 합계 행이 DOM에 있으면 F9를 어겼다. `scrollWidth > 375`면 잔액이 여전히 P1이다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/column-fold.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 앞은 열 선언이 P1 상한을 넘겼을 때, 뒤는 새 등록 파일이 누수 스캔 목록에 없을 때</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 리저브 DTO가 정보 노출 항목에 매핑되지 않았다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 정보 노출이 막힌 계급에게 라우트가 404다
    - 열이 여섯이고 P1이 셋이며 잔액이 P2다
    - 0건에서 EMPTY 한 줄이 본문을 대체하고 합계 행이 DOM에 없다
    - 합계 행에 마지막 행의 잔액이 있다(0건 아닐 때)
    - `?new=1` 토글이고 기본 진입에 폼이 없다
    - 출금 거부 문구가 Copywriting 정본이고 입력값이 남는다
    - 프로젝트 연결이 선택이다
    - 통화 선택 시 환율 기본값이 서버에서 온다
    - 375px에서 `scrollWidth <= 375`이고 잔액이 접힌 줄에 있다
    - 새 액션이 등록 파일에 있고 누수 스캔 목록에 반영됐다
    - E2E 둘이 만든 데이터를 치운다
    - `test/e2e/reserve.spec.ts`가 화면 코드보다 **앞선 커밋**에 있고 그 시점의 RED 실패 메시지가 SUMMARY에 있다(W3)
    - `ui/grid`를 import하지 않았다
    - 클라이언트·연결 프로젝트 칸이 `ui/input/Autocomplete`를 쓰고 화면 코드에 목록 요소 리터럴이 0개다 — 04-07의 단위 테스트가 초록이다(D-83)
    - 자동완성 후보가 `scopeFor` 판정을 거친 목록이다(ADMN-03)
    - 새 E2E 스펙 둘이 `./fixtures`의 확장 `test`를 쓴다 — 콘솔 오류·페이지 예외 0이 자동으로 단언된다(B7)
  </acceptance_criteria>
  <done>리저브 대장이 §6-1 목록으로 서고 375px에서 P1 3열로 읽히며, 0건에서도 사용자가 막히지 않는다</done>
</task>

<task type="auto">
  <name>Task 3: 진입점 둘 — 1차 메뉴는 여전히 다섯이다 (D-64)</name>
  <files>app/(app)/pnl/page.tsx, app/(app)/layout.tsx, ui/page-header/PageHeader.tsx, ui/shell/role-menu.ts, ui/shell/MoreSheet.tsx, test/unit/ui/role-menu.test.ts, test/e2e/reserve.spec.ts, test/e2e/mobile-reserve.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quotation-ledger/04-UI-SPEC.md` **화면 계약 8의 「진입점」 블록 전문** — 표면 둘: **PC**는 손익 화면 상단 부제 줄 오른쪽의 3차 `리저브 대장`(`PageHeader`) — §6-4 손익 대시보드 머리 줄의 `[2026 ▾] [Excel 내보내기]`와 같은 자리이고 **페이지 이동이므로 3차는 `<a>`**(§10). **폰**은 §7-8 「더보기」 시트의 1차 메뉴 목록 중 **손익 아래 한 줄** `리저브 대장`, 행 높이 44, 아이콘 없음, **오른쪽 건수는 붙이지 않는다**(잔액은 금액이지 건수가 아니다)
    - 같은 블록의 **정보 노출** — 「`visible()`이 막으면 **두 표면 모두 렌더되지 않는다**(§6-0 「메뉴가 빠지면 남은 메뉴가 왼쪽으로 붙는다」와 같은 논리 — 빈 자리를 두지 않는다). 서버가 라우트 자체도 막는다」
    - 같은 블록의 마지막 문장 — 「**§6-0 · `role-menu.ts` `TOP_BAR_MENU` · 기존 E2E 단언을 고치지 않는다.** 이 페이즈가 더하는 것은 `PageHeader` 부제 줄의 링크 하나와 「더보기」 시트의 목록 한 줄뿐이다」
    - `ui/shell/role-menu.ts` **45–51행 `TOP_BAR_MENU` 전문** — 정확히 다섯(프로젝트 · 지출결의 · 법인카드 · 결재 · 손익). 그 위 주석이 「1차 메뉴 다섯의 URL 정본(이 페이즈의 유일한 출처) … 다른 곳에 URL 문자열을 복제하지 않는다」를 적었다. **이 배열을 건드리지 않는다**
    - `ui/shell/role-menu.ts`의 `buildBottomTabs`·`roleMenu`와 admin 메뉴 목록 — 폰 「더보기」 시트에 무엇이 어떻게 들어가는지. **손익 그룹 아래 한 줄을 더할 자리**를 찾는다. `moreMenu`가 「하단 탭에 없는 1차 메뉴」이므로 리저브가 그 목록에 들어갈지, 별도 필드가 필요한지 판단한다 — **1차 메뉴가 아니므로 별도 필드가 맞을 수 있다**. 고른 쪽과 이유를 주석에 적는다
    - `ui/shell/MoreSheet.tsx` — `moreMenu`/`accountGroup`/`adminMenu` 세 목록을 받아 렌더한다. 새 필드를 더하면 이 컴포넌트도 바뀐다
    - `app/(app)/layout.tsx` — 세션 계급으로 메뉴 접근 판정을 **미리 계산해** 셸에 데이터로 넘기는 지점(03-02의 계약, `allowedMenus`). 리저브 노출 판정도 여기서 계산해 넘겨야 한다 — **`ui`는 domain을 import할 수 없다**(`eslint.config.mjs` 68행)
    - `test/e2e/keyboard-nav.spec.ts` **134행** — 「메뉴 5개」를 단언한다(실측). **이 단언을 고치지 않는다** — 고쳐야 한다면 D-64를 어긴 것이다
    - `test/unit/ui/role-menu.test.ts` 전문 — SYSTEM.md 표를 읽어 기대값을 만드는 구조. 새 필드를 더하면 이 테스트도 케이스를 더한다
    - `app/(app)/pnl/page.tsx` 현재 전문 — 손익 화면. `PageHeader`를 쓰는지, 부제 줄에 무엇이 있는지 확인한다
    - `ui/page-header/PageHeader.tsx` — 부제 줄에 3차 링크를 붙일 수 있는 API인지 확인한다. 없으면 **prop을 더한다**(선택 필드로 — 기존 호출처가 그대로 통과해야 한다). `docs/design/DECISIONS.md`의 2026-09-21 「`ui/list-empty/ListEmpty`의 `action`을 선택 필드로 확장」 기록이 같은 형태의 선례이고 그 기록이 「회귀 없는 확장」이라는 판단 근거를 적어 뒀다
    - `docs/design/SYSTEM.md` §6-0 — 「브레드크럼 없음 — 제목 + 부제가 위치를 말한다」. **하위 메뉴 개념 자체가 없으므로** 「손익 하위」를 표면 둘로 못 박은 것이 D-64다
    - `docs/design/SYSTEM.md` §7-1 — 3차 버튼은 밑줄 텍스트. 페이지 이동이므로 `<a>`다(§10:914의 「3차 버튼은 `<button>`」은 **행동**일 때이고 이동은 링크다 — 화면 계약 8이 그 구분을 명시했다)
  </read_first>
  <behavior>
    - `TOP_BAR_MENU`가 여전히 정확히 다섯이다
    - `test/e2e/keyboard-nav.spec.ts`의 「메뉴 5개」 단언이 고쳐지지 않고 통과한다
    - 허용된 계급의 손익 화면 부제 줄에 3차 `리저브 대장` 링크가 있고 그것이 `<a>`다
    - 폰 「더보기」 시트의 손익 아래에 `리저브 대장` 한 줄이 있고 행 높이 44이며 아이콘·건수가 없다
    - 정보 노출이 막힌 계급에게 두 표면 모두 렌더되지 않는다 — 빈 자리도 없다
    - 노출 판정이 레이아웃에서 계산되어 셸에 데이터로 간다 — `ui`가 domain을 import하지 않는다
    - `PageHeader`의 새 prop이 선택 필드라 기존 호출처가 그대로 통과한다
  </behavior>
  <action>
`ui/page-header/PageHeader.tsx`에 부제 줄 오른쪽 3차 링크 prop을 **선택 필드로** 더한다(기존 호출처가 그대로 통과해야 한다). `ListEmpty`의 `action`을 선택 필드로 확장한 선례와 같은 형태이고, 그 선례를 주석에 인용한다. 링크는 `<a>`다(페이지 이동 — 화면 계약 8).

`app/(app)/pnl/page.tsx`에서 그 prop에 `리저브 대장` 링크를 넘긴다. **노출 판정 결과를 서버에서 받아** 조건부로 넘긴다 — 막히면 `undefined`를 넘겨 링크가 아예 렌더되지 않는다(빈 자리를 두지 않는다).

`ui/shell/role-menu.ts`에 폰 「더보기」 시트용 항목을 더한다. **`TOP_BAR_MENU`를 건드리지 않는다.** 리저브가 1차 메뉴가 아니므로 `moreMenu`에 섞지 않고 별도 필드(예: 손익 하위 목록)를 두는 것이 기본이고, 고른 쪽과 이유를 주석에 적는다. `MoreSheet.tsx`가 그 필드를 렌더하게 하고 **오른쪽 건수를 붙이지 않는다**(잔액은 금액이지 건수가 아니다 — 화면 계약 8).

`app/(app)/layout.tsx`가 리저브 노출 판정을 미리 계산해 셸에 넘긴다 — 03-02가 세운 계약(「레이아웃이 메뉴별 보기 판정을 미리 계산해 셸에 데이터로 넘긴다」)을 그대로 따른다. `ui`는 domain을 import하지 않는다.

`test/unit/ui/role-menu.test.ts`에 케이스를 더한다: ① **`TOP_BAR_MENU`가 여전히 다섯이다**(이 단언이 없으면 더한다 — D-64의 강제 지점) ② 노출이 허용되면 손익 하위 목록에 리저브가 있다 ③ 막히면 없다 ④ 건수 필드가 없다.

`test/e2e/reserve.spec.ts`에 케이스를 더한다: ① 허용된 계급으로 손익 화면 → 부제 줄에 `리저브 대장` 링크가 있고 누르면 대장으로 간다 ② 기획본부 계급으로 손익 화면 → 그 링크가 **없다**. `test/e2e/mobile-reserve.spec.ts`에 더한다: ③ 「더보기」 시트에 `리저브 대장` 한 줄이 있고 행 높이 44 이상이며 오른쪽 건수가 없다 ④ 막힌 계급에게는 없다.

**`test/e2e/keyboard-nav.spec.ts`를 고치지 않는다.** 그 파일이 이 플랜의 diff에 있으면 D-64를 어긴 것이다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다. `TOP_BAR_MENU` 다섯 단언이 실패하면 1차 메뉴를 늘린 것이다(D-64 위반)</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('ui/shell/role-menu.ts','utf8');const m=s.match(/TOP_BAR_MENU[^=]*=\s*\[([\s\S]*?)\n\]/);if(!m){console.error('TOP_BAR_MENU NOT FOUND');process.exit(1)}const n=(m[1].match(/href:/g)||[]).length;if(n!==5){console.error('TOP_BAR_MENU HAS '+n+' ENTRIES (expected 5) — D-64 FORBIDS ADDING A TOP LEVEL MENU');process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 출력에 ok 대신 `TOP_BAR_MENU HAS n ENTRIES …` 또는 `TOP_BAR_MENU NOT FOUND`가 나오고 종료 코드가 1이다. 뒤가 나오면 정규식이 배열 끝 형태를 못 잡은 것이므로 `ui/shell/role-menu.ts`를 열어 실제 모양에 맞춘다</fails_when>
    <automated>git diff --name-only test/e2e/keyboard-nav.spec.ts</automated>
    <fails_when>출력이 비어 있지 않다 — 기존 「메뉴 5개」 단언을 고쳤다는 뜻이고 그것은 D-64 위반이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test:e2e</automated>
    <fails_when>종료 코드가 0이 아니다. `keyboard-nav.spec.ts`가 깨지면 1차 메뉴를 늘린 것이고, 다른 스펙이 깨지면 이 플랜의 E2E 정리가 빠진 것이다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>종료 코드가 0이 아니다. `ui`가 domain을 import하면 boundaries가 잡는다. grep에 파이프하지 않는다</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니다. `PageHeader`의 새 prop을 필수로 만들었다면 기존 호출처 전부가 여기서 드러난다 — 선택 필드로 되돌린다</fails_when>
    <automated>pnpm build</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `TOP_BAR_MENU`가 다섯이고 그 사실이 단위 테스트와 정규식 게이트 둘로 고정됐다
    - `test/e2e/keyboard-nav.spec.ts`가 이 플랜 diff에 없다
    - 손익 화면 부제 줄에 3차 링크가 있고 `<a>`다
    - 폰 「더보기」 시트에 한 줄이 있고 건수·아이콘이 없다
    - 막힌 계급에게 두 표면 모두 렌더되지 않고 빈 자리도 없다
    - 노출 판정이 레이아웃에서 계산되고 `ui`가 domain을 import하지 않는다
    - `PageHeader`의 새 prop이 선택 필드이고 선례가 주석에 인용됐다
    - 폰 시트 필드 선택(별도 필드 vs `moreMenu`)의 이유가 주석에 있다
  </acceptance_criteria>
  <done>리저브 대장 진입점이 손익 화면 부제 줄과 폰 「더보기」 시트 둘로 서고, 1차 메뉴는 여전히 다섯이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 라우트 직접 접근 → 정보 노출 | 진입점을 숨겨도 URL을 아는 사람은 들어온다. 서버가 라우트 자체를 막아야 한다 |
| 클라이언트 요청 → 잔액 | 잔액이 저장 컬럼이라 요청에 담긴 값을 쓰면 임의 잔액이 들어간다 |
| 과거 날짜 삽입 → 이후 잔액 | 줄을 끼워 넣으면 뒤 줄들의 잔액이 틀린다. 재계산이 같은 트랜잭션에 없으면 중간 상태가 남는다 |
| DB 예외 → 화면 문구 | DB 제약 위반 메시지는 영문이고 §8-3 형식이 아니다. 그것이 화면에 그대로 가면 사용자가 무엇을 해야 할지 모른다 |
| 셸 렌더 → 권한 판정 | `ui`가 domain을 import할 수 없으므로 판정을 렌더 계층에서 할 수 없다. 레이아웃이 계산해 넘겨야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-75 | Information Disclosure | 기획본부가 URL로 리저브 대장에 직접 접근 | high | mitigate | 라우트가 정보 노출 판정을 보고 `notFound()`를 낸다(화면 계약 8 「서버가 라우트 자체도 막는다」). DTO 투영이 두 번째 층이고 E2E가 404를 단언한다 |
| T-04-76 | Information Disclosure | 리저브 필드가 응답에 남아 누수 | high | mitigate | `RESERVE_LEDGER_DTO_SPEC` + `registerDto` 등록으로 `leak-scan` 통합 테스트가 자동으로 덮는다. 정보 항목이 `staffDefault: false`다 |
| T-04-77 | Tampering | 요청에 담긴 잔액이 저장됨 | high | mitigate | 도메인이 잔액을 요청에서 읽지 않고 서버에서 누적 계산한다. 통합 테스트가 조작된 잔액을 보내고 저장값이 계산값임을 단언한다 |
| T-04-78 | Tampering | 잔액이 음수가 되는 쓰기(**단일 요청**) | high | mitigate | 도메인 판정(§8-3 문장)이 첫 층, DB CHECK 제약(04-05)이 두 번째 층. 통합 테스트가 두 층 모두 거부함을 단언한다. **동시 요청은 이 둘로 막히지 않는다 — T-04-93을 보라** |
| T-04-93 | Tampering | **동시 출금 둘이 각자 잔액을 확인하고 각자 통과해 실제 잔액이 음수가 된다.** 저장된 각 행의 잔액은 양수라 `CHECK (balance >= 0)`도, 도메인 판정도, 어떤 예외 경로도 이것을 잡지 못한다 — 틀린 값만 남는다 | high | mitigate | 트랜잭션을 **먼저** 열고 그 클라이언트의 대장 행을 `FOR UPDATE`로 잠근 **뒤에** 잔액 예측·삽입·재계산을 한다(대장 행이 0건인 첫 입금은 고른 대체 직렬화가 맡는다). 그물 셋: ① `Promise.all` 동시 출금 통합 테스트 — 정확히 하나 성공 · 실패는 Copywriting 정본 문구 · 최종 잔액이 음수 아님 ② 다른 클라이언트끼리는 막지 않음을 확인하는 케이스 ③ 리포지토리에 행 잠금이 있고 도메인이 **트랜잭션을 연 뒤에** 잠그는지 보는 정규식 게이트. 이 저장소에 `FOR UPDATE` 사용처가 0건이므로 최초 도입이고 그 사실을 주석에 적는다 |
| T-04-79 | Tampering | 과거 날짜 삽입 후 뒤 줄들의 잔액이 틀린 채 남음 | high | mitigate | 삽입과 `recomputeBalancesFrom`을 같은 트랜잭션에 담고, 그 트랜잭션은 이미 클라이언트 대장을 잠근 상태다(T-04-93) — 삽입 중 끼어드는 경쟁도 같은 잠금이 막는다. 통합 테스트 케이스가 있다 |
| T-04-80 | Information Disclosure | DB 예외 메시지가 화면에 노출되어 스키마 정보가 새어 나감 | medium | mitigate | 도메인이 먼저 판정해 `UserFacingError` 계열 문장을 준다. `lib/actions/client.ts`의 화이트리스트가 그 밖의 예외를 일반 문구로 바꾼다 |
| T-04-81 | Elevation of Privilege | 1차 메뉴를 늘려 §6-0 계약과 기존 테스트가 갈라짐 | medium | mitigate | `TOP_BAR_MENU` 다섯 단언 + 정규식 게이트 + `keyboard-nav.spec.ts`가 diff에 없음을 확인하는 세 겹 |
| T-04-82 | Information Disclosure | 환율 입력이 리저브 경로로 감사 없이 들어감 | medium | mitigate | 새 환율은 04-09의 `recordRate`를 거치므로 행동 로그가 따라온다. 리저브 도메인이 그 함수를 부른다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다(D-45, 04-RESEARCH.md § Package Legitimacy Audit: 설치 대상 없음). `git diff package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint`(eslint + stylelint) · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test:unit` · `pnpm test:integration` 통과 — 특히 `leak-scan` · `visibility` · **동시 출금 직렬화 케이스**(B4). 통합 단계는 `fileParallelism: false`지만 그것은 **파일 간** 병렬만 막고 한 테스트 안의 `Promise.all`은 막지 않는다
- `pnpm db:reset:test && CI=true pnpm test:e2e` 전체 통과 — **`keyboard-nav.spec.ts`의 「메뉴 5개」가 고쳐지지 않고 통과해야 한다**
- `git diff --name-only`에 `db/migrations/`·`test/e2e/keyboard-nav.spec.ts`가 없다
- `git diff --name-only package.json pnpm-lock.yaml`이 비어 있다
- 커밋이 셋이다 — 도메인 / 화면·폼 / 진입점
- **웨이브 종료 게이트 — 순서가 CLAUDE.md의 프론트엔드 규칙이다(N6): 싼 게이트(`pnpm lint` · `pnpm typecheck` · `pnpm build`) → 독립 DOM 감사 → 수정 → 전체 게이트 한 번.** 전체 게이트를 두 번 돌리지 않기 위한 순서다
- **독립 DOM 감사(N6)** — **실행자가 아닌 별도 에이전트**가 `CI=true`로 이 플랜의 화면 DOM을 실측 판정한다. **스크린샷 육안 판정 금지**(CLAUDE.md). 감사 대상은 ① 이 플랜의 화면 계약(열 우선순위·375px 넘침 없음·접근 가능한 이름·라벨 칸 실측 폭·포커스 링·터치 44) ② 앞 웨이브의 컴포넌트 플랜(04-07·04-08·04-10)이 SUMMARY에 **등록해 넘긴 감사 항목** — 그 플랜들은 사용처가 0이라 실측할 DOM이 없었고 이 웨이브가 그것을 실제로 렌더한다. 근거: Phase 3의 DOM 감사가 결함 7건을 찾았고, 실행자가 자기 스펙에 감사 단언을 쓰면 자기가 만든 것을 자기가 통과시킨다
- 이 플랜이 페이즈의 마지막이므로 웨이브 종료 뒤 Post-build 넷을 실제로 돌린다: `/review` → **(독립 DOM 감사)** → `/qa`(UI 변경이므로 `/design-review` 포함) → `/cso`(권한·정보 노출을 건드렸다) → `/ship`. 감사는 `/design-review`보다 **앞**이다. **즉석 검증으로 대체하지 않는다**(CLAUDE.md — Phase 3에서 전부 건너뛰었다가 나중에 `/review`가 14건을 찾았다)
</verification>

<success_criteria>
- 경영관리가 클라이언트별 리저브 입금·출금·잔액을 다룬다(RSV-01)
- 음수 잔액이 단일 요청에서는 두 층(도메인 문장 · DB 제약)에서, **동시 요청에서는 행 잠금 직렬화**에서 막히고 사용자에게 §8-3 문장이 간다(B4)
- 금액이 통화·환율·원화 환산액을 함께 담는다(FX-01)
- 기획본부에게 진입점 둘과 라우트 모두 막힌다
- 1차 메뉴가 여전히 다섯이다(D-64)
- 375px에서 P1 3열로 읽히고 0건에서 합계 행이 없다(F9)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 새 리포지토리 | `repositories/reserve-ledger.ts` |
| 새 도메인 | `domain/reserve-ledger/` — `RESERVE_LEDGER_DTO_SPEC` · `listReserveLedger` · `addReserveEntry` |
| 새 화면 | `app/(app)/pnl/reserve/page.tsx` · `reserve-list.tsx` · `reserve-form.tsx` |
| 새 서버 액션 | 리저브 줄 등록(+ 등록 파일) |
| 변경된 컴포넌트 | `ui/page-header/PageHeader.tsx`(부제 줄 3차 링크 prop, 선택 필드) · `ui/shell/role-menu.ts`(폰 시트 손익 하위 한 줄) · `ui/shell/MoreSheet.tsx` |
| 변경된 레이아웃 | `app/(app)/layout.tsx`(리저브 노출 판정 전달) |
| 새 테스트 | `test/integration/reserve-ledger.test.ts` · `test/e2e/reserve.spec.ts` · `test/e2e/mobile-reserve.spec.ts` |
| 변경된 테스트 | `test/unit/ui/role-menu.test.ts` · `test/integration/leak-scan.test.ts` |
| 새 표 · 새 마이그레이션 · 1차 메뉴 변경 · 새 의존성 | 없음 |

<output>
Create `.planning/phases/04-project-quotation-ledger/04-13-SUMMARY.md` when done
</output>
