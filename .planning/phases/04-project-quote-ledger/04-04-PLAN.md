---
phase: 04-project-quote-ledger
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - ui/table/Table.tsx
  - ui/table/Table.module.css
  - ui/table/types.ts
  - ui/table/use-grid-keyboard.ts
  - ui/table/use-clipboard-paste.ts
  - ui/table/use-dirty-storage.ts
  - ui/table/parse-tsv.ts
  - ui/table/RowSheet.tsx
  - ui/table/RowSheet.module.css
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - app/(app)/projects/actions.ts
  - app/(app)/admin/code-tables/page.tsx
  - app/(app)/admin/people/page.tsx
  - app/(app)/admin/vendors/page.tsx
  - app/(app)/admin/corp-cards/page.tsx
  - app/(app)/admin/action-log/page.tsx
  - app/(app)/admin/archive/page.tsx
  - test/unit/ui/parse-tsv.test.ts
  - test/unit/ui/dirty-storage.test.ts
  - test/integration/quote-lines-conflict.test.ts
  - test/e2e/quote-table.spec.ts
  - .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md
  - .planning/phases/04-project-quote-ledger/04-UI-SPEC.md
autonomous: false
requirements: [UX-04, UX-05, PROJ-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "표 전체가 탭 정지 하나이고 안에서는 방향키로 셀 사이를 움직인다 — Tab이 셀마다 멈추지 않는다(§7-3 (아))"
    - "Tab·Enter·방향키·Esc·저장·새 줄 단축키만으로 견적 줄 입력을 끝까지 할 수 있다 — 마우스 없이 저장까지 도달한다(UX-05)"
    - "Esc는 편집 중이면 그 셀 값을 되돌리고, 편집 중이 아니면 범위 선택을 해제한다(§7-3 (아))"
    - "Delete는 줄 삭제 확인 모달을 열고, 셀 값 비우기는 편집에 들어가 지우는 것이다 — 두 동작이 섞이지 않는다"
    - "그룹 머리글 행은 방향키가 건너뛰고 포커스를 받지 않는다(§7-3 (라))"
    - "그룹 안에서 새 줄을 만들면 그 그룹의 대분류를 물려받아 소분류가 미리 채워지고 그 셀이 편집 상태로 열린다(D-62)"
    - "줄 이동 단축키로 줄 순서를 바꿀 수 있고, 그룹 경계를 넘으면 소분류가 비워지고 그 셀이 편집 상태로 열린다(§7-3 (라))"
    - "줄 순서는 정렬이 아니라 저장된 순서 값이다 — 다시 열어도 같은 순서다"
    - "엑셀에서 복사한 여러 칸을 붙여넣으면 활성 셀부터 오른쪽·아래로 채워지고, 셀 안에 줄바꿈이 있는 인용된 칸도 한 칸으로 들어간다(D-67)"
    - "숫자 열에 붙여넣은 값은 쉼표·공백·통화 기호를 지운 뒤 숫자로 읽히고, 그래도 숫자가 아니면 그 셀이 오류 셀로 고정된다 — 조용히 버리지 않는다(§7-3 (다))"
    - "읽기 전용·잠김 셀에 붙여넣기 값이 떨어지면 건너뛰지 않고 오류 셀로 표시된다 — 값이 유실된 것처럼 보이지 않는다(§7-3 (다))"
    - "붙여넣기가 표 아래를 넘으면 새 줄이 자동으로 생기고, 오른쪽을 넘는 칸은 버리되 합계 행에 몇 칸을 버렸는지 경고가 남는다(§7-3 (다))"
    - "범위 복사는 붙여넣기와 같은 형식을 클립보드에 써서 다른 프로젝트의 표에 그대로 붙는다 — 별도 가져오기 모달이 없다(D-70, PROJ-05)"
    - "저장 시 읽은 줄 버전과 서버의 버전이 다르면 그 줄에서 **실제로 값이 달라진 셀만** 충돌로 표시되고 이유 한 줄에 바뀐 값·사람·시각이 적힌다(D-65)"
    - "충돌이 하나라도 있으면 배치 전체가 거부되고 다른 셀의 편집값은 그대로 남는다(D-65, §7-3)"
    - "오류가 한 칸이라도 있으면 화면 전체가 거부되고, 성공은 한 칸도 없다 — 부분 저장이 없다(UX-04)"
    - "오류 셀은 다른 셀을 편집해도 표시가 남는다 — 고정이고 깜빡이지 않는다(§7-3)"
    - "미저장 편집이 있는 채로 화면을 떠나려 하면 브라우저가 이탈을 경고한다(D-68)"
    - "다시 열었을 때 보관된 편집이 있으면 표 위 한 줄로 몇 칸인지 알리고 복원·버림 두 3차 버튼을 준다 — 화면 제목 위 전역 배너가 아니다(§7-3 (마))"
    - "복원한 뒤 그 사이 줄 버전이 올라갔으면 저장 시점에 충돌로 간다 — 복원이 남의 편집을 덮어쓰지 않는다(§7-3 (마))"
    - "저장 성공·「버림」에 브라우저 저장소의 임시 보관이 지워진다 — 서버 초안은 만들지 않는다(D-68)"
    - "폰에서 줄을 탭하면 접힌 값 전부를 보여주는 시트가 열리고 행동 줄이 없다 — 보기 전용이고 폰 셀 편집이 없다(D-69)"
    - "시트를 열 때 새 요청이 나가지 않는다 — 본문은 그 행에서 접힌 값이다(§7-3 (바))"
    - "화면 하단 단축키 힌트 줄이 정확히 일곱 개이고 폰에서는 숨는다(§7-9)"
    - "오류·충돌 셀이 `aria-invalid`와 `aria-describedby`로 이유 줄을 가리키고, 합계 행의 저장 결과가 `aria-live`로 읽힌다(S11)"
    - "요청 본문 크기 한도가 서버에 있고 한도를 넘는 붙여넣기 배치는 이유와 함께 거부된다 — 한도 없이 통째로 받지 않는다"
    - statement: "견적 줄이 0줄·1줄·여섯 줄일 때 0줄은 EMPTY 한 줄로 갈리고 1줄과 여섯 줄이 같은 그룹 머리글 + 같은 합계 행 형식으로 렌더되며 조수사가 바뀌지 않는다"
      verification: backstop
    - statement: "500자 비고가 폰 행 시트에서 말줄임 없이 전문으로 나오고 시트 높이가 화면의 85%를 넘으면 본문 스크롤로 읽힌다"
      verification: backstop
  prohibitions:
    - statement: "붙여넣기·저장이 사용자의 값을 조용히 버려서는 안 된다. 숫자로 읽히지 않는 칸, 읽기 전용 셀에 떨어진 칸, 표 오른쪽을 넘친 칸 — 셋 다 화면이 무슨 일이 있었는지 말해야 하고, 「어차피 못 쓰는 값이니 무시」하는 경로를 만들지 않는다. 사람이 방금 붙여넣은 것이 어디로 갔는지 모르는 상태가 이 표에서 가장 비싼 실패다"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/table/parse-tsv.ts"
      provides: "클립보드 탭 구분 텍스트를 2차원 배열로 읽는 상태 기계 파서 — 인용된 칸의 줄바꿈·탭·따옴표를 리터럴로 다룬다"
      exports: ["parseTsv", "toTsv"]
    - path: "ui/table/use-grid-keyboard.ts"
      provides: "로빙 탭 인덱스 · 방향키 · Esc · Delete · 줄 이동 · 새 줄 · 줄 복제"
      exports: ["useGridKeyboard"]
    - path: "ui/table/use-clipboard-paste.ts"
      provides: "붙여넣기 반영 — 숫자 정규화, 오류 셀 고정, 새 줄 자동 생성, 오른쪽 넘침 경고"
      exports: ["useClipboardPaste"]
    - path: "ui/table/use-dirty-storage.ts"
      provides: "미저장 편집의 브라우저 임시 보관 + 이탈 경고 + 복원·버림"
      exports: ["useDirtyStorage"]
    - path: "ui/table/RowSheet.tsx"
      provides: "폰 행 시트 — 접힌 값 전부를 보기 전용으로, 행동 줄 없음"
      exports: ["RowSheet"]
    - path: "test/unit/ui/parse-tsv.test.ts"
      provides: "인용된 칸·줄바꿈·따옴표 이스케이프·CRLF·빈 칸 경계 표"
      min_lines: 35
    - path: "test/integration/quote-lines-conflict.test.ts"
      provides: "버전 충돌 감지와 전부 거부 증명"
      min_lines: 30
    - path: "test/e2e/quote-table.spec.ts"
      provides: "키보드만으로 입력·저장, 붙여넣기, 전부 저장 또는 전부 거부(UX-04·UX-05)"
      min_lines: 40
  key_links:
    - from: "ui/table/use-clipboard-paste.ts"
      to: "ui/table/parse-tsv.ts"
      via: "클립보드 문자열을 상태 기계 파서로 읽는다(단순 분리 금지)"
      pattern: "parseTsv"
    - from: "domain/quotes/lines.ts"
      to: "repositories/quote-lines.ts"
      via: "배치 저장이 한 트랜잭션에서 버전을 비교하고 하나라도 다르면 전부 거부한다"
      pattern: "version"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/table/use-dirty-storage.ts"
      via: "미저장 편집 키가 프로젝트와 차수로 만들어진다"
      pattern: "useDirtyStorage"
---

<objective>
**As a** 기획 PM, **I want to** 이미 엑셀로 만들어 둔 견적을 그대로 복사해 붙여넣고 키보드만으로 나머지를 채운 뒤 한 번에 저장하고 싶다, **so that** 인트라넷에서 하던 칸 하나씩 클릭·입력·저장을 반복하지 않고, 저장이 절반만 들어가거나 남의 편집을 덮어쓰는 일이 구조적으로 없다.

이 플랜은 04-01이 깐 `ui/table`을 `docs/design/SYSTEM.md` §7-3 (가)~(아) 계약 **전체**로 확장한다. 04-01은 한 칸 편집과 일괄 저장까지만 만들었다 — 여기서 키보드 계약 · 범위 선택 · 붙여넣기 · 버전 충돌 · 미저장 복원 · 폰 행 시트를 더해 UX-04·UX-05를 실제로 만족시킨다.

Purpose: ROADMAP 기준 2가 요구하는 것은 두 가지다 — (a) 키보드만으로 입력이 끝난다, (b) 저장 실패·중복 저장·입력값 유실이 없다. (b)는 "전부 저장 또는 전부 거부"라는 **구조적 속성**이라 부분 구현이 성립하지 않는다. 배치의 일부만 커밋되는 경로가 하나라도 있으면 그 속성은 거짓이다.

Output: `ui/table` 훅 넷 + 폰 행 시트, 배치 저장의 버전 충돌·전부 거부, A-M3(Phase 3 이월) 매듭, 단위 2 · 통합 1 · E2E 1, 사람 확인 1(실제 엑셀 붙여넣기).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-RESEARCH.md
@.planning/phases/04-project-quote-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md
@.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@CLAUDE.md
</context>

<probe_fallback>
엣지 프로브 폴백의 전체 표와 금지 항목 규약은 `04-01-PLAN.md`의 `<probe_fallback>`에 있다. 이 플랜이 담당하는 미분류 행은 **UX-04 · UX-05 · PROJ-02**(전부 저장/전부 거부 쪽) 셋이고, 프로브가 답을 주지 못했으므로 수용 기준은 ROADMAP 기준 2 · D-61~D-69 · `04-UI-SPEC.md` §7-3 (가)~(아)와 S4·S8에서 끌어와 위 `must_haves.truths`에 적었다. 조용히 버린 항목은 없다.

`04-UI-SPEC.md`의 미해소 항목 중 U-6(표 밖 칸의 오류가 화면 전체를 거부할 때 오류 0칸인 표의 합계 행 문구)은 아래 Task 2 ⑤에서 계획 단계 판단으로 닫는다.
</probe_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 표의 키보드 계약 · 범위 선택 · 붙여넣기 · 미저장 복원 · 폰 행 시트</name>
  <files>ui/table/Table.tsx, ui/table/Table.module.css, ui/table/types.ts, ui/table/parse-tsv.ts, ui/table/use-grid-keyboard.ts, ui/table/use-clipboard-paste.ts, ui/table/use-dirty-storage.ts, ui/table/RowSheet.tsx, ui/table/RowSheet.module.css, test/unit/ui/parse-tsv.test.ts, test/unit/ui/dirty-storage.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 406–508행(§7-3 보강 (가)~(아) 전문) — 셀 3단계 · 충돌 문구 · 붙여넣기 정규화 · 그룹 머리글 · 미저장 복원 · 폰 시트 · 저장 범위 · 키보드 계약. **이 여덟 항목이 이 태스크의 계약 전문이다**
    - `docs/design/SYSTEM.md` §7-3(04-01이 위 여덟 항목을 옮겨 적은 현재 형태) · §7-8(시트 골격 + 2차 라벨 충돌 규칙) · §7-9(힌트 줄 최대 7개) · §10(접근성)
    - 같은 문서 §2-4 — 숫자·금액 표기, 0과 빈 값의 구분, 외화 두 줄 병기
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 325–344행(S4 다섯 상태 + backstop 둘) · 363–369행(S8 폰 행 시트) · 234–287행(Copywriting Contract의 셀 오류 다섯 갈래·붙여넣기 경고·복원 줄 문구)
    - `.planning/phases/04-project-quote-ledger/04-RESEARCH.md` § Pattern 4(362–420행) — 인용 규칙을 아는 상태 기계 파서의 형태와 **단순 분리가 왜 깨지는지**. 가정 A3(실제 엑셀 출력으로 검증 안 됨)도 함께 읽는다
    - `ui/table/Table.tsx`·`ui/table/types.ts`(04-01·04-02 산출물 전문) — 셀 3단계·열 우선순위·합계 행·금액 보조 줄의 현재 계약
    - `ui/kv-list/KvList.tsx`(전문) — 폰 시트 본문이 쓰는 정의 목록 컴포넌트
    - `ui/shell/MoreSheet.tsx`(또는 02-04가 만든 시트 컴포넌트 전문) — 네이티브 모달 요소로 만든 포커스 트랩 선례. 새 의존성 없이 시트를 만드는 방법이 여기 있다
    - `ui/button/Button.tsx`(전문) — `disabled`·`disabledReason`·`pending` prop
    - `docs/design/tokens.css`(전문) — 오류 셀 배경·인셋 선·dirty 외곽선·저장 성공 틴트에 쓸 토큰. 새 값을 만들지 않는다
    - `app/globals.css`의 `.sr-only` 유틸리티 — 시각적으로 숨긴 캡션에 쓴다
  </read_first>
  <behavior>
    - `parseTsv("a\tb\nc\td")` → `[["a","b"],["c","d"]]`
    - `parseTsv('a\t"b\nc"\td')` → 한 행 세 칸이고 가운데 칸 값에 줄바꿈이 들어 있다 — 행이 둘로 갈리지 않는다
    - `parseTsv('a\t"b""c"')` → 둘째 칸 값이 `b"c`다(두 개의 따옴표가 하나로 이스케이프 해제된다)
    - `parseTsv("a\tb\r\nc\td")` → 캐리지 리턴이 값에 남지 않는다
    - `parseTsv("a\t\tb")` → 가운데 빈 칸이 사라지지 않고 세 칸이다
    - `parseTsv("")` → 빈 칸 하나를 가진 한 행(값을 통째로 삼키지 않는다)
    - `toTsv(parseTsv(x))`가 `x`와 같은 표를 다시 만든다(왕복)
    - 숫자 정규화: `"1,200,000"` → `1200000` · `" 1 200 000 "` → `1200000` · `"₩1,200,000"` → `1200000` · `"약 120만"` → 숫자가 아니라 오류 셀
    - 미저장 보관: 세 칸을 고치고 새로 고치면 「저장 안 한 편집 3칸」이 뜨고, 「버림」을 누르면 보관이 지워진다
  </behavior>
  <action>
① `ui/table/parse-tsv.ts`: 04-RESEARCH.md Pattern 4의 상태 기계 파서를 구현한다. 탭이 칸 구분, 줄바꿈이 행 구분이되 **큰따옴표로 감싼 칸 안의 탭·줄바꿈·이스케이프된 따옴표는 리터럴**이다. 단순 분리를 쓰지 않는다 — 이 표에는 자유 텍스트 비고 열이 있어 줄바꿈이 실제로 들어온다. `toTsv`도 같은 규칙으로 쓴다(범위 복사가 이것을 클립보드에 쓴다).

② `ui/table/use-grid-keyboard.ts`: 로빙 탭 인덱스로 표 전체를 탭 정지 하나로 만들고 방향키로 셀 사이를 움직인다. Enter/클릭으로 편집 진입, Esc는 편집 중이면 값 되돌리기·아니면 범위 해제, Delete는 줄 삭제 확인 모달, 줄 이동·새 줄·줄 복제 단축키를 붙인다. 그룹 머리글 행은 포커스 대상에서 뺀다. 새 줄은 그 그룹의 대분류를 물려받아 소분류가 채워진 채 편집 상태로 열리고, 줄 이동이 그룹 경계를 넘으면 소분류가 비워지고 그 셀이 편집 상태로 열린다. 범위 선택 모델(앵커 셀 + 현재 셀)을 여기 둔다.

③ `ui/table/use-clipboard-paste.ts`: 클립보드의 평문을 `parseTsv`로 읽어 활성 셀부터 오른쪽·아래로 채운다. 숫자 열은 쉼표·공백·통화 기호를 지운 뒤 숫자로 읽고, 실패하면 **그 셀을 오류 셀로 고정**한다. 읽기 전용·잠김 셀에 값이 떨어지면 건너뛰지 않고 오류 셀로 표시한다. 아래를 넘으면 새 줄을 자동으로 만들고, 오른쪽을 넘는 칸은 버리되 합계 행에 몇 칸을 버렸는지 경고를 남긴다. 범위 복사는 `toTsv`로 같은 형식을 클립보드에 쓴다.

④ `ui/table/use-dirty-storage.ts`: dirty 셀이 하나라도 있으면 이탈 경고를 건다. 편집은 브라우저 저장소에 임시 보관하고 **키는 프로젝트 식별자 + 차수 식별자**(다른 표는 화면 식별자)로 만든다 — 다른 프로젝트의 편집이 섞이지 않는다. 저장 성공·「버림」에 보관을 지운다. 다시 열었을 때 보관이 있으면 **표 위 한 줄**로 알리고(전역 배너가 아니다) 복원·버림 두 3차 버튼을 준다. 복원한 셀은 dirty 모양으로 들어오고, 그 사이 줄 버전이 올라갔으면 저장 시점에 충돌로 간다.

⑤ `ui/table/Table.tsx`: 위 훅 셋을 붙이고 충돌 셀 렌더를 더한다 — 충돌 셀은 오류 셀과 같은 고정 모양이고 이유 한 줄에 바뀐 값·사람·시각이 들어가며 두 다음 한 수(덮어쓰기 / 그 값으로)는 3차 버튼이다. 오류·충돌 셀에 `aria-invalid`와 이유 줄을 가리키는 `aria-describedby`를 단다. 합계 행 오른쪽에 거부 요약(오류 N칸 / 충돌 N줄 / 붙여넣기 버림 N칸)을 두고 `aria-live`로 읽히게 한다. 화면 하단 힌트 줄은 §7-9대로 **정확히 일곱 개**이고 폰에서 숨는다.

⑥ `ui/table/RowSheet.tsx`: 폰에서 줄을 탭하면 열리는 보기 전용 시트. 머리는 항목명 + `{소분류} · {거래처}` 부제 + 닫기 아이콘 버튼(아이콘 단독이므로 접근성 이름 필수, S11), 본문은 접힌 값 전부를 정의 목록으로, 외화는 값 아래 보조 줄. **행동 줄을 두지 않는다**(D-69 — 「지출결의 올리기」는 그 문서가 생기는 페이즈가 더한다). 시트를 열 때 새 요청이 나가지 않는다. 높이는 내용에 따르되 화면의 85%를 넘으면 본문만 스크롤한다. 포커스 트랩은 기존 시트 선례대로 네이티브 모달 요소로 만들고 새 의존성을 더하지 않는다.

⑦ 단위 테스트 둘: `parse-tsv.test.ts`(위 `behavior`의 경계 표 + 왕복), `dirty-storage.test.ts`(보관 키 분리·복원·버림·저장 성공 시 삭제). 저장소는 테스트 더블로 주입해 브라우저 없이 돌린다.

⑧ 스타일은 전부 `docs/design/tokens.css`의 토큰만 쓴다 — 오류 셀 배경·좌측 인셋 선·dirty 외곽선·저장 성공 틴트·`prefers-reduced-motion`에서 틴트 끄기. 새 색·서체·radius를 만들지 않는다(D-20·D-21).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/parse-tsv.test.ts test/unit/ui/dirty-storage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다. stylelint가 색·서체·radius 리터럴을 잡으면 토큰 밖 값을 쓴 것이다</fails_when>
    <automated>git diff --stat docs/design/tokens.css package.json pnpm-lock.yaml</automated>
    <fails_when>출력이 비어 있지 않다 — 새 토큰을 만들었거나 새 의존성을 더했다(D-61 「새 런타임 의존성 0」 위반)</fails_when>
  </verify>
  <acceptance_criteria>
    - `ui/table/parse-tsv.ts`가 인용된 칸의 줄바꿈·탭·이스케이프된 따옴표를 리터럴로 다루고, 단위 테스트에 그 세 경계가 전부 있다
    - `parseTsv`·`toTsv` 왕복 케이스가 단위 테스트에 있다
    - 붙여넣기 경로에 「숫자가 아니면 무시」·「읽기 전용이면 건너뛴다」는 분기가 없다 — 셋 다 오류 셀 또는 경고로 귀결된다
    - 표 전체가 탭 정지 하나다(로빙 탭 인덱스 구현이 있고 셀마다 탭 정지를 두지 않는다)
    - 그룹 머리글 행이 방향키 이동 대상에서 제외된다
    - 미저장 보관 키가 프로젝트·차수 식별자를 포함해 다른 표와 섞이지 않는다(단위 테스트가 두 키의 분리를 단언)
    - 복원 알림이 표 위 한 줄이고 화면 제목 위 전역 배너 컴포넌트를 쓰지 않는다
    - `RowSheet`에 행동 줄이 없고 닫기 아이콘 버튼에 접근성 이름이 있다
    - 힌트 줄 항목이 정확히 일곱 개다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>표의 키보드 계약·범위 선택·붙여넣기·미저장 복원·폰 행 시트가 구현되고 단위 2개가 녹색이며 lint·typecheck·build가 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 배치 저장의 버전 충돌과 전부 거부 + 본문 한도 + A-M3 매듭</name>
  <files>domain/quotes/lines.ts, repositories/quote-lines.ts, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, app/(app)/projects/actions.ts, app/(app)/admin/code-tables/page.tsx, app/(app)/admin/people/page.tsx, app/(app)/admin/vendors/page.tsx, app/(app)/admin/corp-cards/page.tsx, app/(app)/admin/action-log/page.tsx, app/(app)/admin/archive/page.tsx, .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md, test/integration/quote-lines-conflict.test.ts</files>
  <read_first>
    - `domain/quotes/lines.ts`(04-01·04-02 산출물 전문) — 배치 저장 함수의 현재 형태와 게이트 호출 위치
    - `repositories/quote-lines.ts`(04-01 산출물 전문) — 행 타입과 현재 쿼리
    - `db/schema/quote-lines.ts`(04-01 산출물) — 줄 버전 컬럼과 계보 컬럼
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 434–446행(§7-3 보강 (나) 줄 버전 충돌) — 충돌 문구의 정확한 형태와 「하나라도 있으면 전부 거부」
    - 같은 문서 490–508행(§7-3 보강 (사) 저장 범위와 1차 버튼) — 표 밖 칸까지 한 트랜잭션에 들어가는 계약
    - 같은 문서 386–396행(체커가 남긴 경계 3건) — U-6의 원문
    - 같은 문서 234–287행(Copywriting Contract) — 거부 요약 문구의 정본. 없는 문구는 여기에 더한다
    - 같은 문서 771–785행(S11) — **A-M3 결정 요구 원문**과 (ㄱ)/(ㄴ) 두 갈래
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` A-M3 행과 「계획 단계가 정해야 할 것」 표
    - `.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md` — Task 1 체크포인트 ⑤의 답(A-M3가 (ㄱ)인지 (ㄴ)인지). **이 답이 아래 ⑥의 분기를 정한다**
    - `app/(app)/admin/code-tables/page.tsx`·`people/page.tsx`·`vendors/page.tsx`·`corp-cards/page.tsx`·`action-log/page.tsx`·`archive/page.tsx` — 표 마크업이 있는 여섯 자리와 각 표가 무엇의 목록인지(캡션 문구의 근거)
    - `app/globals.css`의 `.sr-only` — 시각적으로 숨긴 캡션 구현
    - `next.config.ts`와 `lib/actions/client.ts`(전문) — 요청 본문 한도를 둘 수 있는 자리 둘(서버 액션 설정 / 액션 클라이언트 미들웨어)
    - `test/integration/quote-lines.test.ts`(04-01 산출물 전문) — 통합 테스트 형태
  </read_first>
  <behavior>
    - 읽은 버전이 서버 버전과 같은 줄 셋을 저장하면 셋 다 저장되고 각 줄의 버전이 1 올라간다
    - 셋 중 한 줄의 서버 버전이 그 사이 올라갔으면 응답이 그 줄의 **달라진 셀 목록**과 바뀐 값·사람·시각을 담고, **세 줄 모두 저장되지 않는다**
    - 값이 실제로 같은 셀은 버전이 달라도 충돌로 표시되지 않는다(버전만 올라가고 값은 그대로인 경우)
    - 한 줄에 형식 오류가 있으면 나머지 줄도 저장되지 않고 응답이 오류 셀 좌표를 담는다
    - 본문 크기 한도를 넘는 배치는 이유 문자열과 함께 거부되고 아무 줄도 저장되지 않는다
    - 저장이 성공하면 한 번의 행동 로그 행이 남고, 거부되면 행동 로그 행이 0개다
  </behavior>
  <action>
① `repositories/quote-lines.ts`에 배치 읽기·쓰기를 더한다 — 저장 대상 줄들을 한 번에 잠그고(줄 식별자 목록으로) 현재 버전과 값을 돌려주는 함수, 그리고 버전을 올리며 갱신하는 함수. 전부 한 트랜잭션 핸들을 받는다.

② `domain/quotes/lines.ts`의 배치 저장을 완성한다: 트랜잭션 안에서 (a) 게이트 판정 → (b) 현재 행을 읽어 버전 비교 → (c) 버전이 다른 줄은 **읽은 값과 현재 값을 셀 단위로 비교해 실제로 달라진 셀만** 충돌로 모은다 → (d) 형식 오류를 모은다 → (e) **충돌·오류가 하나라도 있으면 아무것도 쓰지 않고** 충돌·오류 목록을 담은 결과를 돌려준다 → (f) 하나도 없으면 전부 쓰고 버전을 올린 뒤 행동 로그를 한 번 남긴다. 클라이언트가 보낸 계산 필드는 04-01대로 계속 무시하고 서버가 재계산한다.

③ 요청 본문 크기 한도를 서버에 둔다. `next.config.ts`의 서버 액션 본문 한도와 `lib/actions/client.ts` 미들웨어 중 **실제로 동작하는 자리**를 확인해 한 곳에 둔다(둘 다 두지 않는다 — 어느 쪽이 먼저 걸리는지 모호해진다). 한도를 넘으면 이유 문자열과 함께 거부한다. 고른 자리와 값을 `docs/ARCHITECTURE.md`가 아니라 SUMMARY에 기록한다(300줄 상한 보호).

④ `app/(app)/projects/[id]/quote-table.tsx`가 충돌·오류 결과를 표에 그대로 전달하게 한다 — 화면이 이유 문자열을 새로 만들지 않는다. 저장 결과는 §7-7대로 **고친 자리에** 붙는다(표의 dirty는 그 표 합계 행, 표 밖 칸은 그 칸 자리).

⑤ **U-6의 계획 단계 판단:** 표 밖 칸(매출 계약 금액) 하나의 오류로 화면 전체가 거부될 때, 오류가 0칸인 표의 합계 행에는 `전부 거부 · 다른 칸 오류 1칸`을 쓴다 — 「오류 N칸」을 0으로 적지 않고, 거부 사실과 원인이 이 표 밖에 있다는 것 둘 다를 말한다. 이 문구를 `04-UI-SPEC.md`의 Copywriting Contract에 더하고 판단을 `04-OPEN-ITEMS.md` U-6 행에 기록한다.

⑥ **A-M3 매듭(04-01 Task 1 ⑤의 답에 따라 분기):**
- 답이 **(ㄱ)**이면 관리자 읽기용 표 여섯 자리에 시각적으로 숨긴 캡션을 한 줄씩 더하고, 코드표 화면에는 머리글 셀의 범위 속성도 더한다. 표 구조·스타일·데이터는 건드리지 않는다 — 각 파일에서 한두 줄이다. 그리고 `04-OPEN-ITEMS.md`의 A-M3 행을 「해소 — 이 페이즈 04-04」로 바꾼다.
- 답이 **(ㄴ)**이면 관리자 파일을 하나도 건드리지 않고 `04-OPEN-ITEMS.md`의 A-M3 행에 Phase 7 이월과 사유 한 줄을 남긴다. 이 경우 `files_modified`의 관리자 여섯 파일은 변경 0줄로 남는다.
- 어느 쪽이든 `03-OPEN-ITEMS.md`는 고치지 않는다(끝난 페이즈의 산출물).

⑦ `test/integration/quote-lines-conflict.test.ts`: 위 `behavior`의 여섯 경우를 통합 테스트로 쓴다. 특히 (b)의 「세 줄 모두 저장되지 않는다」를 **저장 후 DB를 다시 읽어** 단언한다 — 응답만 보고 판단하지 않는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines-conflict.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm vitest run --project unit</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이다 — 앞 플랜의 단위 테스트가 깨졌으면 배치 저장 시그니처 변경이 기존 호출자를 망가뜨린 것이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 배치 저장이 한 트랜잭션 안에서 판정·비교·쓰기를 하고, 충돌·오류가 있으면 쓰기 호출이 하나도 실행되지 않는다
    - 충돌 판정이 버전 비교만으로 끝나지 않고 **실제로 값이 달라진 셀**을 골라낸다 — 값이 같으면 충돌로 보고되지 않는 케이스가 통합 테스트에 있다
    - 거부 뒤 DB를 다시 읽어 아무 줄도 바뀌지 않았음을 단언하는 통합 테스트 케이스가 있다
    - 거부된 저장이 행동 로그 행을 0개 남긴다(통합 테스트가 단언)
    - 요청 본문 한도가 정확히 한 자리에만 설정됐고 그 자리와 값이 SUMMARY에 기록됐다
    - 화면이 충돌·오류 이유 문자열을 만들지 않는다 — 서버 응답의 문자열을 그대로 쓴다
    - U-6의 문구가 Copywriting Contract에 더해지고 `04-OPEN-ITEMS.md` U-6 행에 판단이 기록됐다
    - A-M3가 (ㄱ) 또는 (ㄴ) 중 하나로 **기록과 함께** 매듭지어졌다 — `04-OPEN-ITEMS.md`의 A-M3 행이 「정해야 한다」 상태로 남아 있지 않다
    - `.planning/phases/03-permissions-settings-masters/03-OPEN-ITEMS.md`의 diff가 비어 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>배치 저장이 버전 충돌을 셀 단위로 감지하고 하나라도 있으면 전부 거부하며, 본문 한도가 한 자리에 있고 A-M3가 기록과 함께 매듭지어졌다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실제 엑셀에서 복사한 값이 그대로 들어가는지 사람 확인 + E2E 증명</name>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-VALIDATION.md` § Manual-Only Verifications — 이 확인이 자동화로 대체되지 않는 이유(브라우저 클립보드 주입이 실제 엑셀 출력과 같다는 보장이 없다)
    - `.planning/phases/04-project-quote-ledger/04-RESEARCH.md` 가정 A3와 Gaps 3 — 인용 규칙을 공식 문서 원문으로 확인하지 못했다는 기록
    - `ui/table/parse-tsv.ts`(Task 1 산출물 전문) — 사람 확인이 검증하는 대상
    - `test/e2e/project-register.spec.ts`(04-01 산출물 전문) · `test/e2e/fixtures.ts` — E2E 스펙 형태와 역할 픽스처
    - `playwright.config.ts` — CI에서만 프로덕션 빌드를 쓴다는 설정(완료 판정의 근거)
  </read_first>
  <action>
**Claude가 먼저 하는 것:** `test/e2e/quote-table.spec.ts`를 쓴다 — 기획 PM 역할로 프로젝트 상세를 열고 (a) 키보드만으로 줄 하나를 끝까지 입력하고 저장한다(마우스 이벤트를 쓰지 않는다), (b) 클립보드에 탭 구분 텍스트를 주입해 여러 칸 붙여넣기가 활성 셀부터 채워지는 것을 확인한다, (c) 숫자가 아닌 값을 숫자 열에 붙여넣어 그 셀이 오류로 고정되고 **저장이 전부 거부**되는 것을 확인한다, (d) 오류를 고치고 저장하면 전부 들어가는 것을 확인한다, (e) 오류 뒤에도 다른 셀의 편집값이 남아 있는 것을 확인한다(UX-04), (f) 폰 뷰포트에서 줄을 탭해 시트가 열리고 행동 줄이 없는 것을 확인한다.

**사람이 확인하는 것(자동화로 대체 불가):**
1. 실제 엑셀(또는 회사가 쓰는 스프레드시트)에서 **3×3 영역**을 복사한다. 그중 한 칸에는 **줄바꿈**을, 다른 한 칸에는 **큰따옴표**를 넣는다
2. 견적 줄 표의 활성 셀에 붙여넣는다
3. 화면에 들어간 값이 원본과 같은지 눈으로 대조한다 — 특히 줄바꿈 칸이 한 칸으로 들어갔는지, 따옴표가 사라지거나 두 개가 되지 않았는지
4. 저장한 뒤 다시 열어 같은 값인지 확인한다
5. 숫자 열에 쉼표가 있는 금액(예: `1,200,000`)을 붙여넣어 숫자로 읽히는지 확인한다

답할 것: 위 다섯 중 **어긋난 것이 있는지**, 있으면 원본 값과 화면 값을 함께 적는다. 어긋난 것이 있으면 Claude가 `parse-tsv.ts`를 고치고 그 케이스를 단위 테스트에 더한 뒤 다시 확인을 요청한다.
  </action>
  <what-built>견적 줄 표의 키보드 계약·범위 선택·붙여넣기·버전 충돌·미저장 복원·폰 행 시트를 전부 구현하고, 클립보드 탭 구분 텍스트를 인용 규칙까지 아는 상태 기계로 파싱하는 모듈을 만들었다. 키보드만 쓴 입력·저장, 붙여넣기 반영, 숫자 아닌 값의 오류 셀 고정과 전부 거부, 폰 행 시트 여섯 경우는 E2E로 자동 검증된다. 자동화가 닿지 못하는 것은 **실제 엑셀이 클립보드에 쓰는 형식**이다 — 브라우저 클립보드 주입은 그 형식을 흉내 낼 뿐이고, 인용 규칙을 공식 문서 원문으로 확인하지 못했다(04-RESEARCH.md 가정 A3 · Gaps 3).</what-built>
  <how-to-verify>
실제 엑셀(또는 회사가 쓰는 스프레드시트)을 열고 견적 줄 표 화면에서 확인한다.

1. 엑셀에서 **3×3 영역**을 복사한다. 그중 한 칸에는 **줄바꿈**을, 다른 한 칸에는 **큰따옴표**를 넣는다
2. 견적 줄 표의 활성 셀에 붙여넣는다
3. 화면에 들어간 값이 원본과 같은지 눈으로 대조한다 — 특히 줄바꿈 칸이 한 칸으로 들어갔는지, 따옴표가 사라지거나 두 개가 되지 않았는지
4. 저장한 뒤 화면을 다시 열어 같은 값인지 확인한다
5. 숫자 열에 쉼표가 있는 금액(예: `1,200,000`)을 붙여넣어 숫자로 읽히는지 확인한다

어긋난 것이 있으면 원본 값과 화면 값을 함께 적어 준다 — Claude가 파서를 고치고 그 케이스를 단위 테스트에 더한 뒤 다시 확인을 요청한다.
  </how-to-verify>
  <resume-signal>다섯 항목이 전부 같으면 「같다」라고 답한다. 어긋난 것이 있으면 항목 번호와 「원본 → 화면」 값 쌍을 적는다</resume-signal>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/quote-table.spec.ts test/e2e/project-register.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다. 앞 두 스펙이 깨지면 표 확장이 기존 편집·저장 경로를 망가뜨린 것이다</fails_when>
    <automated>pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다</fails_when>
    <human-check>실제 엑셀에서 복사한 3×3(줄바꿈 한 칸 · 따옴표 한 칸 포함)을 붙여넣어 화면 값이 원본과 같고, 저장 후 다시 열어도 같으며, 쉼표 금액이 숫자로 읽힌다</human-check>
  </verify>
  <acceptance_criteria>
    - E2E 스펙이 (a)~(f) 여섯 경우를 전부 단언한다
    - (a)가 마우스 이벤트 없이 키보드 입력만으로 저장까지 간다 — 클릭 호출이 없다
    - (c)가 오류 셀 고정과 **전부 거부** 둘 다를 단언한다(하나만 확인하고 넘어가지 않는다)
    - 사람 확인 다섯 항목 각각에 「같다」 또는 어긋난 값 쌍이 기록됐다 — 답이 비어 있는 항목이 0개다
    - 어긋난 것이 있었다면 `parse-tsv.ts`가 수정되고 그 케이스가 단위 테스트에 더해졌다
    - `pnpm test` 세 계층이 전부 녹색이다
  </acceptance_criteria>
  <done>키보드 입력·붙여넣기·전부 거부가 E2E로 증명되고, 실제 엑셀 붙여넣기가 사람 확인으로 통과했다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클립보드 → 표 | 사용자가 붙여넣는 임의 길이·형식의 문자열이 들어온다. 파서가 이 경계의 유일한 관문이다 |
| 브라우저 → Server Action(배치 저장) | 줄 여럿과 각 줄의 버전이 넘어온다. 배치 크기가 제한되지 않으면 본문이 무제한으로 커진다 |
| 브라우저 저장소 → 표 | 이전 세션의 편집이 복원된다. 다른 프로젝트·차수의 편집이 섞이면 남의 값이 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Tampering | `domain/quotes/lines.ts` 배치 저장 | high | mitigate | 버전 비교와 쓰기를 한 트랜잭션에 묶고, 충돌·오류가 하나라도 있으면 쓰기를 실행하지 않는다. 거부 뒤 DB 재조회로 무변경을 단언하는 통합 테스트를 둔다 |
| T-04-22 | Denial of Service | Server Action 본문 | high | mitigate | 요청 본문 크기 한도를 정확히 한 자리에 두고 한도 초과를 이유와 함께 거부한다. 붙여넣기 배치가 무제한 크기로 서버에 닿지 않는다 |
| T-04-23 | Tampering | `use-dirty-storage.ts`의 보관 키 | medium | mitigate | 키를 프로젝트·차수 식별자로 만들어 다른 표의 편집이 복원되지 않게 하고, 단위 테스트가 두 키의 분리를 단언한다. 복원된 편집도 저장 시점에 버전 충돌 검사를 지난다 |
| T-04-24 | Information Disclosure | 브라우저 저장소의 미저장 편집 | medium | accept | 공용 PC에서 로그아웃해도 임시 보관이 남을 수 있다. 사내 10~30명 시스템이고 보관 내용은 그 사람이 방금 친 견적 금액이라 세션 토큰급 비밀이 아니다. 저장 성공·「버림」에 즉시 지운다 |
| T-04-25 | Tampering | `parse-tsv.ts` | medium | mitigate | 인용 규칙을 아는 상태 기계로 파싱하고, 실제 엑셀 출력으로 사람이 대조한다(Task 3). 파싱 실패가 값을 버리지 않고 오류 셀로 귀결된다 |
| T-04-26 | Elevation of Privilege | 읽기 전용·잠김 셀에 붙여넣기 | high | mitigate | 클라이언트의 오류 표시는 안내일 뿐이고, 서버가 셀 단계를 다시 판정해 읽기 전용·잠김 셀의 값 변경을 거부한다. 게이트·셀 단계 판정을 클라이언트에 위임하지 않는다 |
| T-04-27 | Repudiation | 배치 저장 행동 로그 | low | mitigate | 성공한 저장만 로그 한 줄을 남기고 거부는 0줄이다(통합 테스트가 단언) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — 표·시트·파서를 전부 직접 만든다(D-61). `git diff --stat package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test` 세 계층 전부 통과
- 단위 2개 · 통합 1개 · E2E 1개가 존재하고 녹색
- `docs/design/tokens.css`·`package.json`·`pnpm-lock.yaml` diff가 비어 있다
- `03-OPEN-ITEMS.md` diff가 비어 있고 `04-OPEN-ITEMS.md`의 A-M3·U-6 두 행이 매듭지어졌다
- 실제 엑셀 붙여넣기 사람 확인 통과
</verification>

<success_criteria>
- 키보드만으로 견적 줄 입력이 끝나고 저장까지 간다
- 엑셀에서 복사한 여러 칸이 값 유실 없이 들어가고, 못 읽는 값은 오류 셀로 보인다
- 충돌·오류가 하나라도 있으면 배치 전체가 거부되고 부분 저장 경로가 없다
- 미저장 편집이 이탈 경고와 복원으로 지켜진다
- A-M3(Phase 3 이월)가 기록과 함께 매듭지어졌다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `parseTsv` · `toTsv` |
| 훅 | `useGridKeyboard` · `useClipboardPaste` · `useDirtyStorage` |
| 컴포넌트 | `ui/table/RowSheet.tsx` |
| 타입 | `CellConflict` · `CellError` · `PasteResult` · `SaveRejection` · `GridSelection` |
| 설정 | 요청 본문 크기 한도(자리와 값은 SUMMARY에 기록) |
| 문구 | Copywriting Contract에 더하는 「전부 거부 · 다른 칸 오류 N칸」(U-6 판단) |
| 테스트 | `test/unit/ui/parse-tsv.test.ts` · `test/unit/ui/dirty-storage.test.ts` · `test/integration/quote-lines-conflict.test.ts` · `test/e2e/quote-table.spec.ts` |
| 변경된 기존 심볼 | `ui/table/Table.tsx`(충돌 셀·거부 요약·힌트 줄) · `domain/quotes/lines.ts`(배치 충돌·전부 거부) · `repositories/quote-lines.ts`(배치 읽기·쓰기) · 관리자 읽기용 표 여섯 자리의 캡션(A-M3가 (ㄱ)인 경우) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md` when done
</output>
