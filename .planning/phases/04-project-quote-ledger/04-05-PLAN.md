---
phase: 04-project-quote-ledger
plan: 05
type: execute
wave: 3
depends_on: ["04-01"]
files_modified:
  - app/(app)/projects/page.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/projects.module.css
  - domain/projects/index.ts
  - domain/document-numbering/index.ts
  - domain/settings/keys.ts
  - repositories/projects.ts
  - docs/ARCHITECTURE.md
  - test/unit/domain/document-number-format.test.ts
  - test/integration/document-numbering.test.ts
  - test/integration/projects-list.test.ts
  - test/e2e/projects-list.spec.ts
autonomous: true
requirements: [PROJ-01, ADMN-09]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "프로젝트 목록의 기본 보기가 전체 상태이고 그룹 머리글이 종료일 기준 월이다 — 상태 필터는 표 위 한 줄이다(D-51)"
    - "종료일이 없는 수주중 건은 맨 아래 「기간 미정」 그룹으로 모인다(D-49·D-51)"
    - "목록의 합계(견적·실행가·차익)는 불러온 50건이 아니라 필터 전체의 합이고, 합계 행 부제의 건수가 그 사실을 말한다"
    - "합계가 집계 쿼리 한 번으로 계산된다 — 행을 전부 불러와 애플리케이션에서 더하지 않는다(ROADMAP 기준 1)"
    - "「더 보기」 3차 버튼이 합계 행 위에 있고 50건씩 이어 받는다 — 페이지 번호 목록이 없다(§6-1)"
    - "열 머리글을 누르면 정렬되고 현재 정렬 상태가 접근성 속성으로 읽힌다 — 방향 아이콘이 유일한 신호가 아니다(S11)"
    - "견적·실행가·차익 열은 정보 노출표 항목이라 계급에 따라 **서버가 열을 아예 보내지 않는다** — 클라이언트에서 숨기지 않고, 빠진 폭은 문자 열이 흡수하며 합계 행은 남은 열만으로 성립한다"
    - "보내지 않은 칸은 `—`로 채우지 않고, 값이 있는데 비어 있는 칸만 `—`다(§2-4)"
    - "데이터 0건과 필터 결과 0건이 서로 다른 한 줄로 갈린다 — 전자는 등록으로 가는 다음 한 수, 후자는 「필터 지우기」다"
    - "목록 로딩은 머리글·합계 뼈대와 회색 행 셋으로만 그려지고 반짝임이 없으며 300ms 안에 끝나면 아무것도 보이지 않는다(§7-7)"
    - "목록 로드 실패는 표 본문 자리 한 줄과 「다시 시도」 하나이고 사과 문구가 없다(§7-7)"
    - "폰 1순위 세 열은 프로젝트명·견적(없으면 실행가)·상태이고 375px에서 가로 스크롤이 0이다"
    - "목록·검색이 쓰는 인덱스 목록이 `docs/ARCHITECTURE.md`에 있고 그 문서가 300줄을 넘지 않는다"
    - "문서 번호 서식(접두어·연도 자릿수·순번 자릿수·구분자·순번 시작)이 설정 화면에서 바뀌고, 바꾼 뒤 새로 매긴 번호에만 반영된다 — 이미 매긴 번호는 그대로다(ADMN-09)"
    - "연도가 바뀌면 순번이 1부터 다시 시작한다 — 리셋은 서식이 아니라 카운터의 기간 값이 담당한다"
    - "서식 설정이 잘못된 값(자릿수 0, 빈 구분자 등)을 거부하고 기존 서식이 유지된다"
    - "문서 번호는 서버 서식 그대로의 고정 길이 문자열이고 표에서 줄바꿈·말줄임되지 않으며 자릿수 고정 폭을 쓴다(§2-4·S10)"
    - "결번이 생겨도 화면에 결번을 설명하는 문구를 덧붙이지 않는다(S10·D-42)"
    - statement: "프로젝트가 0건·1건·125건일 때 0건은 EMPTY 한 줄로 갈리고 1건과 125건이 같은 그룹 머리글 + 같은 합계 행 형식으로 렌더되며 조수사가 바뀌지 않는다"
      verification: backstop
  prohibitions:
    - statement: "목록의 합계가 화면에 불러온 50건만의 합으로 조용히 바뀌어서는 안 된다. 「더 보기」로 더 불러올수록 합계가 올라가는 표는 사람에게 거짓말을 하는 표다 — 구현이 어려워지면 합계를 지우거나 「현재 표시분」이라고 이름을 바꾸는 쪽이 낫고, 전체 합처럼 보이는 부분 합을 두는 선택지는 없다"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/projects/projects-table.tsx"
      provides: "목록 표 — 월별 그룹 머리글 · 계급별 가변 열 · 전체 집계 합계 행 · 정렬"
      contains: "aria-sort"
    - path: "domain/document-numbering/index.ts"
      provides: "번호 서식이 상수에서 설정 키로 옮겨간 형태 — 서식 조립과 카운터 증가의 유일한 지점"
      exports: ["allocateDocumentNumber", "formatDocumentNumber", "documentNumberFormat"]
    - path: "test/integration/document-numbering.test.ts"
      provides: "설정의 서식이 실제 부여된 번호에 반영되고, 서식을 바꿔도 매긴 번호는 그대로임을 증명"
      min_lines: 30
    - path: "test/integration/projects-list.test.ts"
      provides: "집계 쿼리 한 번 · 계급별 열 부재 · 월별 그룹 · 기간 미정 그룹 증명"
      min_lines: 30
    - path: "test/e2e/projects-list.spec.ts"
      provides: "필터·더 보기·정렬·합계 부제 스모크"
      min_lines: 25
  key_links:
    - from: "domain/document-numbering/index.ts"
      to: "domain/settings/registry.ts"
      via: "번호 서식을 설정 레지스트리에서 읽는다(상수가 아니다)"
      pattern: "getSettingValue"
    - from: "repositories/projects.ts"
      to: "domain/permissions/scope-for.ts"
      via: "목록과 집계가 같은 행 필터 서술자를 쓴다 — 합계가 볼 수 없는 행을 더하지 않는다"
      pattern: "Scope"
    - from: "app/(app)/projects/projects-table.tsx"
      to: "domain/permissions/project.ts"
      via: "금액 열은 투영이 실어 보낸 경우에만 렌더된다"
      pattern: "quote.amount"
---

<objective>
**As a** 기획 PM과 경영관리 담당자, **I want to** 프로젝트 원장을 인트라넷과 같은 감각(전체 상태·월별 묶음)으로 열어 필터·정렬·더 보기로 훑고 맨 아래에서 전체 합계를 보고 싶다, **so that** 「인트라넷보다 못한가」라는 이 프로젝트의 기준을 목록 화면이 통과하고, 문서 번호 서식은 코드 수정 없이 관리자가 바꾼다.

이 플랜은 04-01이 깐 목록 화면을 **완성**하고, 같은 슬라이스에서 문서 번호 서식을 상수에서 설정 키로 옮긴다. 목록의 번호 열이 서식의 유일한 사용처이므로 둘을 한 플랜에 둔다.

Purpose: ROADMAP 기준 1은 목록에 세 가지 구조적 요구를 건다 — 합계는 집계 쿼리 한 번, 인덱스 목록은 문서에, 목록 응답은 p99 500ms. 앞의 둘은 이 플랜이 만족시키고 셋째는 실제 데이터 규모가 있어야 의미가 있으므로 측정 방법만 정하고 측정은 Phase 8 리허설로 넘긴다(`04-VALIDATION.md` Manual-Only가 같은 판단을 이미 기록했다).

Output: 목록 표(월별 그룹·필터·정렬·더 보기·전체 집계 합계), 번호 서식 설정 키, 인덱스 목록 문서화, 단위 1 · 통합 2 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-RESEARCH.md
@.planning/phases/04-project-quote-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md
@CLAUDE.md
</context>

<probe_fallback>
엣지 프로브 폴백의 전체 표와 금지 항목 규약은 `04-01-PLAN.md`의 `<probe_fallback>`에 있다. 이 플랜이 담당하는 미분류 행은 **PROJ-01**(목록·집계 쪽)과 **ADMN-09** 둘이고, 프로브가 답을 주지 못했으므로 수용 기준은 ROADMAP 기준 1 · D-42·D-51 · `docs/inputs` §5의 서식 다섯 예시 · `04-UI-SPEC.md` S1·S10에서 끌어와 위 `must_haves.truths`에 적었다. 조용히 버린 항목은 없다.

**이 플랜이 `ui/table`을 고치지 않는다.** 정렬 가능 머리글·「더 보기」 자리는 04-01이 컴포넌트에 넣어 두었고 정렬 상태·페이지 크기는 호출자가 준다 — 같은 웨이브의 04-04가 그 파일을 고치므로 이 플랜은 **쓰기만** 한다. `ui/table` 변경이 꼭 필요해지면 그 사실을 SUMMARY에 적고 04-04가 끝난 뒤로 미룬다.
</probe_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 목록 완성 — 월별 그룹 · 필터 한 줄 · 정렬 · 더 보기 · 집계 쿼리 한 번</name>
  <files>app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, app/(app)/projects/projects.module.css, domain/projects/index.ts, repositories/projects.ts, docs/ARCHITECTURE.md, test/integration/projects-list.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 579–595행(S1 적용 규칙 전문) — 필터 한 줄의 구성, 그룹 머리글 규칙, 열 목록, 폰 1·2·3순위, 합계 행 형식과 「더 보기」 위치, 열 머리글 정렬
    - 같은 문서 299–309행(S1 여덟 상태) — EMPTY 두 갈래·LOADING·ERROR·POPULATED·PARTIAL·OVERFLOW·zero-one-many(backstop)·long-text
    - 같은 문서 234–287행(Copywriting Contract) — 목록 EMPTY 두 문구·필터 0건·ERROR 문구의 정본
    - `docs/design/SYSTEM.md` §6-1(목록 = 원장: 그룹 머리글·더 보기 50건·필터 한 줄·등록 토글) · §7-3(칸 수 가변·폰 전략) · §7-7(다섯 상태) · §2-4(숫자·건수 형식·0과 빈 값)
    - `app/(app)/projects/page.tsx`(04-01 산출물 전문) — 세션·권한 게이트와 등록 폼 토글의 현재 형태
    - `app/(app)/admin/vendors/page.tsx`(전문 172행) — 검색 파라미터 읽기와 `Promise.all` 데이터 로딩 관례
    - `ui/table/Table.tsx`·`ui/table/types.ts`(04-01 산출물 전문) — 정렬 가능 머리글 prop, 「더 보기」 자리, 그룹 머리글·합계 행 계약. **이 파일들을 고치지 않는다**
    - `repositories/projects.ts`·`domain/projects/index.ts`(04-01 산출물 전문) — 목록 함수의 현재 형태
    - `repositories/vendors.ts` 1–40행 — `Scope`를 where절로 번역하는 형태
    - `domain/permissions/scope-for.ts`(전문) — 서술자의 모양(팀장은 자기 팀 범위)
    - `domain/permissions/project.ts` 18–36행 — 투영이 미통과 필드를 싣지 않는 방식
    - `docs/ARCHITECTURE.md`(전문 196행) — 인덱스 목록을 적을 자리와 남은 여백(300줄 상한)
    - `db/migrations/0004_project_quote_ledger_spine.sql`(04-01 산출물) — 실제로 만들어진 인덱스 목록(문서에 적을 근거)
  </read_first>
  <behavior>
    - 필터를 걸지 않으면 모든 상태의 프로젝트가 종료일 월 그룹으로 묶여 나온다
    - 종료일이 비어 있는 프로젝트는 맨 아래 「기간 미정」 그룹에 모인다
    - 125건 중 50건만 렌더돼도 합계 행의 값은 125건 전체의 합이고 부제의 건수가 125다
    - 상태 필터를 걸면 합계도 그 필터 기준으로 다시 계산된다
    - 금액 열을 볼 수 없는 계급의 응답에는 그 열의 값이 아예 없다 — 빈 문자열·0·`—`가 아니라 필드 부재다
    - 프로젝트가 0건이면 EMPTY 한 줄과 등록으로 가는 다음 한 수가 나온다
    - 필터를 걸어 결과가 0건이면 EMPTY 문구가 다르고 다음 한 수가 「필터 지우기」다
    - 열 머리글을 누르면 정렬 방향이 바뀌고 현재 정렬 상태가 접근성 속성에 반영된다
  </behavior>
  <action>
① `repositories/projects.ts`에 목록 쿼리와 **집계 쿼리**를 더한다. 집계는 견적 합계·실행가 합계·차익 합계·건수를 **한 번의 쿼리**로 돌려주고, 목록 쿼리와 **같은 행 필터 서술자**를 써야 한다 — 합계가 그 사람이 볼 수 없는 행을 더하면 정보 노출이다. 목록 쿼리는 정렬 키와 개수·오프셋을 인자로 받는다. Phase 9의 비용 식이 같은 자리에 얹힐 수 있도록 집계 함수의 반환 형태를 열 목록으로 두고 계산식을 한 곳에 모은다.

② `domain/projects/index.ts`의 목록 함수를 확장한다 — 필터(상태·팀·연도·검색어)·정렬·페이지 인자를 받고, 목록과 집계를 함께 돌려준다. 금액 열은 정보 항목 판정을 거쳐 투영이 실어 보낼 때만 DTO에 들어간다. 그룹 나누기(종료일 월, 없으면 「기간 미정」)는 domain에서 하고 화면은 받은 그룹을 그리기만 한다 — 그래야 폰·PC가 같은 그룹을 본다.

③ `app/(app)/projects/projects-table.tsx`: `ui/table`을 읽기 형태로 쓰고 열 목록·폰 우선순위를 S1대로 준다. 열 머리글 정렬은 검색 파라미터로 오가고 현재 정렬 상태를 컴포넌트에 넘긴다. 「더 보기」는 합계 행 위 3차 버튼이고 검색 파라미터의 개수를 50씩 늘린다. 합계 행은 부제에 전체 건수를 함께 적어 「불러온 만큼의 합이 아니다」를 말한다.

④ `app/(app)/projects/page.tsx`: 필터 한 줄(상태 기본 전체 · 팀 · 연도 · 검색 · 오른쪽 1차 등록 버튼)을 표 위에 둔다. 등록 폼이 열려 있으면 1차 버튼을 렌더하지 않는 04-01의 규칙을 유지한다. EMPTY 두 갈래(데이터 0건 / 필터 0건)를 갈라 각각 다른 문구와 다음 한 수를 준다. 로딩·오류는 §7-7 계약대로.

⑤ `docs/ARCHITECTURE.md`에 「목록·검색이 쓰는 인덱스」 목록을 더한다 — 마이그레이션 0004가 실제로 만든 인덱스만 적는다(만들 예정인 것을 적지 않는다). p99 500ms 측정은 실제 데이터 규모가 필요하므로 **측정 방법 한 줄**만 적고 측정 자체는 Phase 8 리허설로 넘긴다. **300줄 상한 안에서** 줄인다.

⑥ `test/integration/projects-list.test.ts`: 집계가 쿼리 한 번인지(쿼리 로깅 또는 리포지토리 호출 계수로 단언), 합계가 페이지 크기와 무관한지, 금액 열이 계급에 따라 DTO에서 **부재**하는지, 종료일 없는 건이 「기간 미정」으로 가는지, 필터가 목록과 집계에 똑같이 걸리는지를 단언한다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — `docs/ARCHITECTURE.md`가 300행을 넘었거나 고정 토큰 하나가 사라졌다</fails_when>
    <automated>git diff --stat ui/table</automated>
    <fails_when>출력이 비어 있지 않다 — 같은 웨이브의 04-04가 이 디렉터리를 고치므로 이 플랜은 쓰기만 해야 한다</fails_when>
  </verify>
  <acceptance_criteria>
    - 집계가 쿼리 한 번으로 계산되고 그 사실을 단언하는 통합 테스트 케이스가 있다
    - 목록 쿼리와 집계 쿼리가 같은 행 필터 서술자를 쓴다 — 서로 다른 조건을 쓰는 경로가 없다
    - 합계 값이 페이지 크기와 무관함을 단언하는 케이스가 있다(50건 렌더, 125건 합계)
    - 금액 열을 볼 수 없는 계급의 DTO 키 집합에 그 필드가 **없다**(빈 값이 아니다)
    - 그룹 나누기가 domain에서 이루어지고 화면 컴포넌트에 월 계산 로직이 없다
    - EMPTY 두 갈래가 서로 다른 문구와 서로 다른 다음 한 수를 갖는다
    - `docs/ARCHITECTURE.md`의 인덱스 목록이 마이그레이션 0004가 실제로 만든 것과 일치한다
    - `ui/table` 디렉터리의 diff가 비어 있다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>목록이 월별 그룹·필터·정렬·더 보기·전체 집계 합계로 완성되고, 인덱스 목록이 문서에 있으며 통합 테스트가 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 문서 번호 서식을 상수에서 설정 키로 — 관리자가 바꾸고 매긴 번호는 그대로</name>
  <files>domain/settings/keys.ts, domain/document-numbering/index.ts, test/unit/domain/document-number-format.test.ts, test/integration/document-numbering.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `docs/inputs/phase-04-project-quote.md` §5 — 서식 다섯 예시(프로젝트·지출결의·연차·카드·구매 요청)와 사용자가 확정한 260907 안. **이 다섯이 서식 설계의 입력이다**
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` § Claude's Discretion의 「문서 번호 서식 설정 키 형태(ADMN-09) — 문서 종류별 키 묶음 대 JSON 한 개」 — 이 플랜이 고르는 항목
    - 같은 문서 D-42 — 등록 시 부여·결번 허용·같은 카운터
    - `.planning/REQUIREMENTS.md` ADMN-09 본문
    - `domain/settings/keys.ts`(전문) — 키 정의 형식(`key`·`kind`·`schema`·`label`·`hint`·`namespace`·`default`)과 레지스트리 배열 등록 위치. **04-02가 같은 파일에 환율 키를 더했으므로 그 결과를 먼저 읽는다**
    - `domain/settings/registry.ts`(전문) — `SettingDef`와 `getSettingValue(def, {asOf})`, 이력형과 단순값의 차이
    - `app/(app)/admin/settings/page.tsx`(전문) — 등록한 키가 자동으로 뜨는 화면. **이 파일을 고치지 않는다**
    - `domain/document-numbering/index.ts`(04-01 산출물 전문) — 지금 상수로 들고 있는 서식
    - `.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md` — Task 1 체크포인트 ②의 답(카운터 키·기간 값·완성 번호 예시)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 381–385행(S10 두 상태) · 749–754행(S10 적용 규칙) — 번호 표시 형식
    - `test/integration/document-counters-concurrency.test.ts`(04-01 산출물 전문) — 카운터 통합 테스트 형태
  </read_first>
  <behavior>
    - 기본 서식으로 2026년 첫 프로젝트를 등록하면 번호가 `26001`이다
    - 순번 자릿수를 4로 바꾸면 그 뒤 등록분이 `260001` 형태가 되고 이미 매긴 `26001`은 그대로다
    - 접두어를 넣으면 그 뒤 등록분에만 접두어가 붙는다
    - 연도가 2027로 바뀌면 순번이 1부터 다시 시작해 `27001`이 나온다
    - 순번 자릿수 0, 음수, 빈 구분자 같은 값은 설정 저장이 거부하고 기존 서식이 유지된다
    - 순번이 자릿수를 넘치면(예: 세 자리에 1000번째) 잘리지 않고 자릿수가 늘어난 문자열이 나온다
  </behavior>
  <action>
① **서식 키 형태를 고른다(Claude 재량 항목):** 문서 종류별 키 묶음과 JSON 한 개 중 **문서 종류별 키 묶음**을 쓴다. 근거 셋 — (a) 기존 설정 화면이 키 하나당 한 줄을 그리므로 JSON 한 덩어리는 화면에서 편집 불가능한 텍스트 영역이 된다, (b) `SettingDef`의 스키마 검증이 필드 단위로 이유를 말할 수 있다(JSON이면 「형식이 틀렸다」 한 줄뿐이다), (c) Phase 5·6·11이 문서 종류를 더할 때 키를 더하기만 하면 된다. 이 선택과 근거를 SUMMARY에 적는다.

② `domain/settings/keys.ts`에 프로젝트 문서 종류의 서식 키를 더한다 — 접두어 · 연도 자릿수 · 순번 자릿수 · 구분자 · 순번 시작값. 전부 **단순값**이다(이력형이 아니다 — 서식을 바꿔도 매긴 번호는 그대로이므로 과거 시점 조회가 필요 없다). 각 키의 스키마가 범위를 검증한다(자릿수는 1 이상, 구분자는 허용 문자만). `docs/inputs` §5의 다섯 서식 예시를 기본값 설계의 입력으로 쓰되 **이 플랜은 프로젝트 종류 하나만 등록한다** — 지출결의·연차·카드·구매 요청 키는 그 문서가 생기는 페이즈가 같은 형태로 더한다. 그 확장 방식을 키 정의 주석에 한 줄로 남긴다.

③ `domain/document-numbering/index.ts`의 `formatDocumentNumber`가 상수 대신 설정 값을 읽게 한다. 서식 조립은 **순수 함수**로 빼서 설정 조회 없이 단위 테스트가 되게 하고(`documentNumberFormat(parts, format)` 형태), 설정 조회는 그 함수를 감싸는 얇은 층에 둔다. 순번이 자릿수를 넘치면 **자르지 않는다** — 번호가 잘리면 유일성이 깨진다.

④ `test/unit/domain/document-number-format.test.ts`: 위 `behavior`의 서식 조합 표를 `it.each`로 쓴다(설정 없이 도는 순수 함수 테스트). 자릿수 넘침 케이스를 반드시 넣는다.

⑤ `test/integration/document-numbering.test.ts`: 설정을 바꾼 뒤 새 등록의 번호가 그 서식을 따르고 **이미 매긴 번호는 바뀌지 않는다**는 것, 연도가 바뀌면 순번이 1부터 시작한다는 것(카운터 기간 값이 담당), 잘못된 서식 값이 저장 단계에서 거부된다는 것을 단언한다.

⑥ `test/e2e/projects-list.spec.ts`: 목록 화면 스모크 — 필터를 걸었다 지우고, 정렬을 바꾸고, 「더 보기」를 누른 뒤에도 합계 행 값이 그대로인지(전체 합이므로 바뀌면 안 된다) 확인한다. 번호 열이 줄바꿈 없이 한 줄로 나오는 것도 같은 스펙에서 단언한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/document-number-format.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/document-numbering.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다. 동시 채번 테스트가 깨지면 서식 변경이 카운터 경로를 건드린 것이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts test/e2e/project-register.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 서식 키 형태의 선택(문서 종류별 키 묶음)과 근거 셋이 SUMMARY에 적혔다
    - 서식 키가 전부 단순값이고 `domain/settings/keys.ts`의 레지스트리 배열에 실려 설정 화면에 자동으로 뜬다
    - `app/(app)/admin/settings/page.tsx`의 diff가 비어 있다 — 키 등록만으로 화면에 뜬다
    - 서식 조립이 설정 조회 없이 호출 가능한 순수 함수다(단위 테스트가 DB·설정 없이 돈다)
    - 순번 자릿수 넘침 케이스가 단위 테스트에 있고 번호가 잘리지 않는다
    - 통합 테스트가 「서식을 바꿔도 이미 매긴 번호는 그대로」와 「연도가 바뀌면 순번 1부터」 둘 다를 단언한다
    - 잘못된 서식 값이 거부되는 케이스가 통합 테스트에 있다
    - 이 플랜이 지출결의·연차·카드·구매 요청 서식 키를 만들지 않았다(그 페이즈의 몫)
    - `pnpm test` 세 계층이 전부 녹색이다
  </acceptance_criteria>
  <done>문서 번호 서식이 설정 키로 옮겨져 관리자가 화면에서 바꿀 수 있고, 바꾼 서식이 새 번호에만 반영되며 단위 1·통합 1·E2E 1이 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검색 파라미터 → 목록 쿼리 | 필터·정렬·개수·오프셋이 URL에서 온다. 정렬 키가 검증되지 않으면 임의 컬럼 정렬이 된다 |
| domain 출구 → 목록 화면 | 견적·실행가·차익이 정보 노출표를 지나야 실린다. 합계도 같은 판정을 지나야 한다 |
| 설정 화면 → 번호 서식 | 관리자가 서식을 바꾼다. 잘못된 값이 번호 유일성을 깰 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-28 | Information Disclosure | 목록 집계 쿼리 | high | mitigate | 집계가 목록과 **같은 행 필터 서술자**를 쓰게 해 볼 수 없는 행이 합계에 들어가지 않게 한다. 통합 테스트가 팀장 범위에서 합계를 단언한다 |
| T-04-29 | Information Disclosure | 금액 열 | high | mitigate | 계급별로 서버가 열을 보내지 않는다(클라이언트 숨김 0건). DTO 키 집합 단언으로 필드 부재를 고정한다 |
| T-04-30 | Tampering | 검색 파라미터의 정렬 키 | medium | mitigate | 정렬 키를 허용 목록으로 좁혀 임의 컬럼·표현식이 들어오지 않게 하고, 목록 함수가 허용 목록 밖 값을 기본값으로 떨어뜨린다 |
| T-04-31 | Tampering | 번호 서식 설정 | high | mitigate | 서식 키 스키마가 자릿수·구분자를 검증하고, 순번이 자릿수를 넘쳐도 번호를 자르지 않는다(유일성 보존). 문서 표의 UNIQUE 제약이 최후 방어선으로 남는다 |
| T-04-32 | Denial of Service | 「더 보기」의 개수 파라미터 | medium | mitigate | 페이지 크기에 상한을 두고 그보다 큰 값이 오면 상한으로 떨어뜨린다 — 한 요청이 전체 행을 끌어오지 못하게 한다 |
| T-04-33 | Denial of Service | 목록·검색 쿼리 | low | accept | p99 500ms는 실제 데이터 규모가 있어야 의미 있는 측정이다(`04-VALIDATION.md` Manual-Only). 이 플랜은 인덱스를 문서화하고 측정 방법만 적으며 측정은 Phase 8 리허설로 넘긴다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test` 세 계층 전부 통과
- 단위 1개 · 통합 2개 · E2E 1개가 존재하고 녹색
- `docs/ARCHITECTURE.md`가 300줄 이하이고 인덱스 목록을 담는다
- `ui/table`·`app/(app)/admin/settings/page.tsx` diff가 비어 있다
- `git diff --stat package.json pnpm-lock.yaml`이 비어 있다
</verification>

<success_criteria>
- 목록이 인트라넷과 같은 감각(전체 상태·월별 묶음)으로 열리고 필터·정렬·더 보기가 동작한다
- 합계가 집계 쿼리 한 번이고 화면에 불러온 건수와 무관하다
- 금액 열이 계급에 따라 서버에서 빠진다
- 번호 서식이 코드 수정 없이 설정 화면에서 바뀌고 이미 매긴 번호는 그대로다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 설정 키 | 프로젝트 문서 번호 서식 키 묶음(접두어 · 연도 자릿수 · 순번 자릿수 · 구분자 · 순번 시작값, 전부 단순값) |
| 함수 | `documentNumberFormat`(순수 서식 조립) · 프로젝트 목록 집계 함수 |
| 컴포넌트 | `app/(app)/projects/projects-table.tsx` |
| 문서 절 | `docs/ARCHITECTURE.md`의 「목록·검색이 쓰는 인덱스」 목록과 p99 측정 방법 한 줄 |
| 테스트 | `test/unit/domain/document-number-format.test.ts` · `test/integration/document-numbering.test.ts` · `test/integration/projects-list.test.ts` · `test/e2e/projects-list.spec.ts` |
| 변경된 기존 심볼 | `domain/document-numbering/index.ts`(상수 → 설정) · `domain/projects/index.ts`(필터·정렬·페이지·집계) · `repositories/projects.ts`(목록·집계 쿼리) · `app/(app)/projects/page.tsx`(필터 한 줄·EMPTY 두 갈래) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-05-SUMMARY.md` when done
</output>
