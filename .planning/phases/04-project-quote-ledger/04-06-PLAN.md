---
phase: 04-project-quote-ledger
plan: 06
type: execute
wave: 6
depends_on: ["04-01", "04-04", "04-05", "04-08", "04-09", "04-10"]
files_modified:
  - db/migrations/0012_project_status_five_values.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0012_snapshot.json
  - db/schema/projects.ts
  - domain/seed/index.ts
  - domain/permissions/menus.ts
  - domain/projects/status-transitions.ts
  - domain/projects/status.ts
  - domain/projects/index.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/projects.ts
  - repositories/action-log.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/status-display.ts
  - app/(app)/projects/page.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/status-change.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/project-status.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/project-status.test.ts
  - test/integration/quote-lines.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/project-lifecycle.spec.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "프로젝트 상태 값은 수주중(`bidding`) · 진행(`in_progress`) · 정산(`settling`) · 완료(`completed`) · 미수주(`lost`) 다섯뿐이다 — 코드표 `project_status`와 `projects.status` 어디에도 옛 네 상태 모델의 잠금 값이 남지 않는다(D-75)"
    - "옛 잠금 상태 행은 새 마이그레이션이 `completed`로 옮겼고 0009는 한 글자도 바뀌지 않았다 — 코드표나 프로젝트 행에 알려진 네 값 밖의 상태가 있으면 마이그레이션이 지우지 않고 RAISE EXCEPTION으로 전체를 되돌린다(D-75, RESEARCH Pitfall 6 — 0009의 「표가 비어 있다」 전제를 쓰지 않는다)"
    - "사람이 하는 전환은 수주중→진행 · 수주중→미수주 · 미수주→진행 · 정산→완료 넷뿐이고, 목록 밖 요청(진행→수주중 · 진행→미수주 · 정산→미수주 · 완료→어디든 등)은 서버가 거부한다(D-75·D-79·D-44, ⚠ S7 기본값: 진행에서 사람이 가는 곳 없음)"
    - "진행→정산은 사람이 고르는 목적지가 아니다 — 상태 바꾸기 목록·액션 스키마 어디에도 정산을 고르는 길이 없다(D-76, S7 전이표 — 자동 전환은 04-11)"
    - "수주중→진행과 미수주→진행은 시작일을 요구하고, 종료일이 비어 있으면 같은 저장에서 시작일 값으로 채운다 — 시작일이 없으면 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`로 거부된다(D-82, Copywriting 막힘 — 진행 전환)"
    - "수주중·미수주의 전환은 권한표 `projects.status` 쓰기(기본: 팀장·본부 책임자·대표), 정산→완료는 `projects.complete` 쓰기(기본: 대표 — 시스템 관리자는 시드가 전 메뉴를 켠다)가 있는 사람만 한다 — 담당 PM이 액션을 직접 불러도 거부된다(D-46 갱신·D-79)"
    - "「상태 바꾸기」는 그 사람이 지금 상태에서 갈 곳이 하나라도 있을 때만 서버가 보낸다 — 진행·완료에서는 누구에게도 없고, 권한 없는 사람에게는 비활성 버튼이 아니라 버튼 자체가 없다(S3·S7)"
    - "갈 곳이 둘(수주중)이면 고르기 목록이 먼저 나오고 행마다 상태 라벨 아래 코드표 설명 한 줄이 보인다 · 하나(정산→완료)면 바로 확인 모달 · 미수주→진행은 모달 없이 즉시다(S7, D-93)"
    - "모든 전환이 `domain/rules.gate` 단일 진입점을 지나고, 막힘 이유는 게이트가 돌려준 문자열 그대로 1차 버튼 옆에 나오며 모달은 닫히지 않고 토스트는 뜨지 않는다(ROADMAP 기준 3·4, S7 error, §7-6)"
    - "전환 함수 본문에 상태 이름을 직접 비교하는 분기가 없다 — 비교는 전이표 모듈과 게이트 규칙 안에만 있다(ROADMAP 기준 4)"
    - "같은 프로젝트를 두 사람이 동시에 전환하면 한 사람만 성공하고 다른 사람은 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`를 받는다 — 로그는 한 줄이다"
    - "상태 변경이 행동 로그에 끌 수 없는 `status_change` 종류로 한 줄씩 남고(detail에 from·to), 거부된 전환은 로그를 남기지 않는다(D-50)"
    - "상세 머리 줄 부제에 현재 상태와 마지막 변경일(`진행 2026-09-18` 꼴)이 보이고 상태 이력 표·섹션은 없다 — 변경 기록이 없으면 등록일이다(D-50, S3)"
    - "상태 태그 색이 Color 매핑표대로다 — 수주중 muted · 진행 accent · 정산 warning · 완료 success · 미수주 muted, 목록 상태 열과 상세 태그가 같은 표를 쓴다(D-75, SYSTEM.md 개정 ⑤)"
    - "완료 프로젝트의 견적 줄 저장은 새 게이트 규칙 `project.line-edit`가 `완료 · 견적 줄이 잠김`으로 거부하고, 옛 이진 규칙 `project.completed-lock`은 등록 목록에서 사라졌다(D-47, RESEARCH Pitfall 5 — 교체이지 조건 추가가 아니다. 정산의 「실행가만」은 04-12가 같은 규칙을 넓힌다)"
    - "미수주 프로젝트의 줄 편집·저장은 막히지 않고 화면에 자물쇠·회색 처리가 없다(D-45)"
    - "완료로 바꾸는 확인 모달은 제목 `완료로 바꾸기` · 본문 `견적 줄이 잠기고 새 지출결의를 받지 않습니다 · 되돌리기는 없습니다` · 2차 `취소 Esc`이고, 미수주로 닫기 모달은 `미수주로 닫기` · `쌓인 비용이 팀 미수주 비용이 됩니다 · 진행으로 되돌릴 수 있습니다`다(Copywriting Destructive)"
    - "성공한 전환은 토스트 `상태 바꾸기 · 진행`(`· 미수주` · `· 완료`)를 띄우고 머리 줄 태그·부제가 새 값이 된다(Copywriting SUCCESS)"
    - statement: "상태 전환 모달·고르기 목록의 문자열이 고정 문구와 짧은 보간값(번호·프로젝트명·날짜·차수)뿐이라 `--modal-w` 480 안에서 잘리지 않는다 — 가장 긴 조합(진행으로 바꾸기 본문 세 줄)으로 DOM 감사"
      verification: backstop
    - statement: "「상태 바꾸기」가 더해진 상세 머리 줄이 375px에서 가로 스크롤을 만들지 않고 버튼이 하나도 숨거나 접히지 않는다(U-1 판단 유지) — DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "상태 변경 행동 로그가 직원 감시·성과 집계 도구가 되어서는 안 된다. 「누가 며칠 만에 수주로 바꿨는가」를 사람별로 집계해 보여주는 화면·내보내기를 이 페이즈가 만들지 않는다 — D-50이 상세에 「현재 상태 + 마지막 변경일」만 두고 새 표·새 섹션을 금지한 것은 그 집계 화면이 생기는 것을 막기 위해서다"
      status: resolved
      verification: judgment
    - statement: "완료 외의 상태가 사람의 실제 지출을 막는 잠금이 되어서는 안 된다. 미수주로 닫힌 프로젝트에 뒤늦게 도착하는 청구서(PT 제작비 등)는 실제로 일어나는 일이고, 규칙이 그것을 못 넣게 만들면 사람은 시스템 밖에서 처리한다 — 편의를 위해 미수주를 잠그는 구현은 금지한다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/0012_project_status_five_values.sql"
      provides: "상태 다섯 값 — 옛 잠금 값 재매핑(UPDATE) · 코드표 교체 · 전제 가드(네 값 밖이면 RAISE)"
      contains: "RAISE EXCEPTION"
    - path: "domain/projects/status-transitions.ts"
      provides: "상태 다섯 값과 사람의 전환 넷 + 각 전환의 권한표 동작 — 데이터만(게이트 규칙과 전환 함수가 같이 읽는다, import 순환 방지)"
      exports: ["PROJECT_STATUSES", "ALLOWED_TRANSITIONS", "ProjectStatus"]
    - path: "domain/projects/status.ts"
      provides: "전환 함수 · 그 사람의 갈 곳 목록 · 상태 코드표 목록(라벨·설명)"
      exports: ["changeProjectStatus", "statusDestinations", "listProjectStatusCatalog"]
    - path: "app/(app)/projects/[id]/status-change.tsx"
      provides: "상태 바꾸기 — 갈 곳 하나면 확인, 둘이면 고르기(설명 2행) 뒤 확인, 미수주→진행 즉시. 게이트 이유 그대로"
      contains: "changeProjectStatusAction"
    - path: "test/unit/domain/project-status.test.ts"
      provides: "전환 × 주체(PM·팀장·대표) × 선행 조건(시작일·종료일 유무) 결정표 + 옛 상태 값 리터럴 스캔"
      min_lines: 50
    - path: "test/integration/project-status.test.ts"
      provides: "실제 DB 전환·로그 한 줄·거부 무변경·동시 전환·마이그레이션 재매핑 증명"
      min_lines: 50
    - path: "test/e2e/project-lifecycle.spec.ts"
      provides: "수주중→진행(팀장) · 정산→완료(대표) · 미수주→진행 즉시 · PM에게 버튼 없음 · 완료 뒤 줄 잠김"
      min_lines: 50
  key_links:
    - from: "domain/projects/status.ts"
      to: "domain/rules/gate.ts"
      via: "사람의 전환 넷이 `project.transition`·`project.start-date-required` 두 규칙을 한 진입점으로 지난다"
      pattern: "gate("
    - from: "domain/rules/register.ts"
      to: "domain/projects/status-transitions.ts"
      via: "전환 규칙이 전이표 데이터를 읽는다 — 상태 문자열 비교가 규칙 안에만 있다"
      pattern: "ALLOWED_TRANSITIONS"
    - from: "app/(app)/projects/[id]/status-change.tsx"
      to: "domain/rules/gate.ts"
      via: "막힘 문구가 게이트가 돌려준 이유 문자열 그대로다(액션 serverError)"
      pattern: "serverError"
    - from: "domain/projects/status.ts"
      to: "domain/action-log/record.ts"
      via: "전환 성공 뒤 status_change 종류로 로그를 남긴다"
      pattern: "recordAction"
    - from: "domain/quotes/lines.ts"
      to: "domain/rules/register.ts"
      via: "견적 줄 저장 게이트가 옛 이진 규칙 대신 `project.line-edit`를 부른다"
      pattern: "project.line-edit"
---

<objective>
**As a** 기획팀장·대표, **I want to** 수주중 프로젝트를 시작일과 함께 진행으로 올리거나 미수주로 닫고, 미수주를 다시 진행으로 살리고, 정산이 끝난 프로젝트를 완료로 잠그고 싶다, **so that** 프로젝트의 다섯 상태(수주중 → 진행 → 정산 → 완료 + 미수주)가 규칙 하나로 움직이고 PM이 상태를 임의로 바꾸는 일이 구조적으로 없다.

**2026-09-23 재계획(D-75~D-82).** 옛 04-06은 네 상태 모델(D-41) 위에 상태 전환 · 차수 기계 · 고객 승인 · 프로젝트 복사 · 게이트 규칙 다섯을 한 플랜에 담았다. 다섯 상태로 바뀌면서 범위가 커져 **이 플랜은 상태 모델과 사람의 전환만** 맡고 나머지는 보완 플랜으로 나눴다(한 실행자 컨텍스트 안에 들어가게):
- 진행→정산 자동 전환(D-76) · 정산→진행 되돌리기(D-80) · 종료일 지남(D-81) · 상세 기간 칸(S13) → **04-11**
- 정산의 「실행가만」 편집 범위(D-78) · 줄 삭제·취소(D-56) · 300줄 상한(D-86) → **04-12**
- 조정 줄(D-83) · 견적 외 비용 줄(D-48) → **04-13**
- 차수 기계 · 고객 승인 게이트(D-43) · 차수 섹션 · 거래처 필수 규칙 → **04-14**
- 프로젝트 복사(D-70) · 총 매출 예상가 → **04-15**

옛 이진 게이트 `project.completed-lock`은 이 플랜이 **교체**한다(RESEARCH Pitfall 5) — 마이그레이션이 옛 잠금 값을 `completed`로 옮기는 순간 옛 규칙은 아무것도 잠그지 않게 되므로, 같은 플랜에서 새 규칙 `project.line-edit`로 바꿔 완료 잠금이 끊기는 구간을 만들지 않는다. 정산의 셀 단위 범위는 04-12가 같은 규칙을 넓힌다.

Purpose: ROADMAP 기준 3·4는 전환이 전부 `domain/rules.gate` 단일 진입점을 지나는 구조적 속성을 요구한다. Phase 5는 정산→완료의 호출자를 결재 승인으로 바꾸기만 하면 되고(D-79), Phase 9·10은 다섯 상태를 전제로 손익을 계산한다(D-75 costly).

Output: 마이그레이션 0012(상태 다섯 값) · 전이표 · 전환 함수 · 게이트 규칙 셋(`project.transition` · `project.start-date-required` · `project.line-edit`) · 권한표 메뉴 둘 · 상태 바꾸기 화면 · 단위 2 · 통합 2 · E2E 1.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md
@CLAUDE.md
# 범위 Read(큰 파일):
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 677–722행(§1 D-75 — 읽는 코드 경로 전부 · 마이그레이션 전략 · 가드 형태 · 재매핑 방향) · 843–855행(Pitfall 5·6)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 455–463행(게이트 교체) · 480–482행(가드 패턴 재사용 주의)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 484–490행(S7 상태) · 1227–1255행(S7 전이표·막힘·모달) · 287–300행(상태 → 색 매핑) · 366–378행(Copywriting SUCCESS·Destructive·확인·상태 고르기)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04**다. 프로브는 REQUIREMENTS.md의 옛 네 상태 문구로 돌았으므로(04-08 Task 3이 문구를 고친다) **수용 기준은 결정된 모델 D-75·D-79·D-82·D-44·D-45·D-50과 UI-SPEC S3·S7로 끌어와** `must_haves.truths`에 적었다. S7의 backstop 하나(long-text)와 S3 overflow는 flat-scalar 마커로 옮겼고, S7의 loading(1차 라벨 뒤 `…`)·error(게이트 이유 그대로)·partial(갈 곳의 수)은 truths로 덮었다.

**계획 단계 가정(사용자에게 표면화):**
- **⚠ S7 진행 상태의 수동 전환 — 기본값 적용:** 진행에서는 「상태 바꾸기」를 렌더하지 않는다(진행 → 미수주는 결정된 적 없다). 사용자가 「수주 뒤 계약이 깨지는 경우」를 원하면 전이표에 한 줄(진행 → 미수주, `projects.status`)을 더하는 것으로 바뀐다.
- **코드 값 문자열(D-75 계획 재량):** 정산 = `settling`, 완료 = `completed`. 옛 잠금 값은 **`completed`로 재매핑**한다(RESEARCH A7 — 옛 값의 뜻이 「잠김」이라 완료에 가깝고, 현재 행은 전부 `source='demo'`라 업무 의미를 보존할 필요가 없다). 옛 코드표 행은 재매핑 뒤 **지운다**(비활성으로 남기면 관리 화면에 쓰이지 않는 `완료(정산)`이 계속 보인다).
- **권한표 기본값:** 「팀장 이상」(D-46)을 계급 순위가 아니라 새 메뉴 `projects.status`(쓰기)로, 「대표·시스템 관리자」(D-79)를 새 메뉴 `projects.complete`(쓰기)로 표현한다(04-02의 `projects.revenue` 선례). 시드는 팀장·본부 책임자·대표에게 `projects.status` 쓰기와 **`projects` 보기**를, 대표에게 `projects.complete` 쓰기를 켠다 — 지금 시드는 기획 PM에게만 `projects` 보기를 켜므로, 보기 없이 전환 권한만 주면 팀장이 프로젝트 화면에 닿지 못한다. 경영관리는 시드 계급이 아니라 관리자가 권한표에서 배정한다(04-02와 같은 결).
- **Phase 5 연결점(D-79):** 정산→완료의 호출자는 지금 `changeProjectStatusAction`(대표·시스템 관리자)이고 Phase 5가 결재 승인 경로로 바꾼다. 전환 함수·게이트는 그대로 둔다.
</probe_fallback>

<reversibility_notes>
- D-75 다섯 상태 모델 — **costly**(CONTEXT 기록 그대로): Phase 5·9·10의 게이트·손익이 이 값들을 전제한다. 사용자가 이미 결정했으므로 체크포인트를 두지 않는다.
- 코드 값 문자열 `settling`·`completed` — **costly**: 뒤 페이즈 코드가 참조하기 시작하면 이름을 바꾸는 데 데이터 UPDATE와 참조 전부가 따라온다. 지금 데이터는 전부 demo라 one-way는 아니다.
- 권한표 새 메뉴 키 `projects.status`·`projects.complete` — **costly**: 관리자가 권한표에서 켜 둔 행이 이 키를 참조한다.
</reversibility_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 팀장이 수주중 프로젝트를 시작일과 함께 진행으로(마이그레이션부터 화면·E2E까지 한 경로)</name>
  <files>db/migrations/0012_project_status_five_values.sql, db/migrations/meta/_journal.json, db/migrations/meta/0012_snapshot.json, db/schema/projects.ts, domain/seed/index.ts, domain/permissions/menus.ts, domain/projects/status-transitions.ts, domain/projects/status.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/projects.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/status-display.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/project-lifecycle.spec.ts</files>
  <reversibility rating="costly">다섯 상태 값과 코드 문자열(settling·completed)은 Phase 5·9·10의 게이트·손익이 전제한다(D-75 costly) — 바꾸려면 데이터 재매핑과 참조 전부를 고쳐야 한다. 결정은 사용자가 했고 문자열만 계획 재량이다.</reversibility>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-75·D-79·D-82·D-44~D-46·D-50 원문
    - `db/migrations/0009_project_quote_ledger_spine.sql` 88–125행(가드 `DO $$ … RAISE EXCEPTION` 형태와 코드표 교체 문장) — **「projects 표가 비어 있다」 전제는 가져오지 않는다**(Pitfall 6)
    - `db/migrations/0011_code_item_descriptions.sql`(04-10 산출물) — 직전 마이그레이션 문체와 `description` 컬럼
    - `domain/seed/index.ts` 20–30행(상태 상수) · 120–180행(권한 시드 루프 — `DEFAULT_ROLE_ID`의 `projects` 보기·쓰기 선례)
    - `domain/permissions/menus.ts`(전문 34행) — `projects.revenue` 메뉴를 더한 04-02 주석
    - `domain/permissions/roles.ts` 20–35행(시드 계급 id)
    - `domain/rules/gate.ts`·`domain/rules/register.ts`(전문) — 등록 형태와 교체할 옛 규칙
    - `domain/quotes/lines.ts` 354–375행(저장 게이트 호출 지점)
    - `domain/projects/index.ts` 210–290행(`findProject`·`createProject` — 행 범위 `scopeFor`와 DTO)
    - `repositories/projects.ts` 211–302행(단건 조회·삽입·갱신 형태)
    - `domain/action-log/record.ts` 1–75행(`status_change`가 끌 수 없는 종류라는 것)
    - `domain/code-tables/index.ts` 60–90행(`listCodeItems`)
    - `app/(app)/projects/[id]/page.tsx`(전문) · `app/(app)/projects/[id]/quote-table.tsx` 300–360행·830–860행(QuoteLedger props와 머리 줄)
    - `ui/status-tag/StatusTag.tsx`(전문) · `ui/table/RowSheet.tsx`(네이티브 모달 요소 선례) · `ui/button/Button.tsx`(`disabled`·`disabledReason`·`pending`)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1227–1255행(S7 전이표 · 막힘 · 확인 모달 본문) · 374–375행(확인 — 진행으로 바꾸기 · 상태 고르기 목록)
    - `test/e2e/revenue-section.spec.ts` 1–60행(계급 권한을 테스트에서 켜고 두 역할로 로그인하는 형태) · `test/e2e/fixtures.ts`
  </read_first>
  <action>
① **[BLOCKING] 마이그레이션.** 데이터만 바꾸는 마이그레이션이라 `pnpm db:generate --custom --name project_status_five_values`로 빈 파일을 만든다(계획 번호 0012, **실제 번호는 생성기 기준** — 다르면 파일명·journal tag를 맞추고 SUMMARY에 적는다). 내용: 락 타임아웃 한 쌍 → `DO $$` 가드 두 개(`code_items`의 `project_status`에 알려진 네 값 `bidding`·`in_progress`·`settled`·`lost` 밖의 값이 있으면, 또는 `projects.status`에 그 네 값 밖의 값이 있으면 RAISE EXCEPTION — 이미 다섯 값으로 옮겨졌거나 손으로 더한 값일 수 있어 지우지 않고 중단) → `UPDATE projects SET status='completed', version=version+1, updated_at=now() WHERE status='settled'` → 옛 코드표 행 `settled` DELETE → `lost`의 `sort_order`를 4로 → `settling`(라벨 `정산`, sort 2, 설명 `행사 종료 · 발행 요청과 증빙 첨부를 마치는 단계`)·`completed`(라벨 `완료`, sort 3, 설명 `정산 마감 · 견적 줄이 잠기고 되돌리기 없음`) INSERT. **0009 파일은 열지만 고치지 않는다.** `pnpm lint:sql`(`Found 0 issues`) → `bash scripts/dev-db.sh && pnpm db:migrate`. `db/schema/projects.ts`의 상태 주석을 다섯 값으로 고친다(컬럼 정의는 그대로 `text`, 기본값 `bidding`).

② **시드·권한.** `domain/seed/index.ts`의 상태 상수를 다섯 값(라벨·정렬·설명이 마이그레이션과 글자 그대로 같게)으로 바꾼다. `domain/permissions/menus.ts`에 `{ key: "projects.status", label: "프로젝트 상태 변경" }`·`{ key: "projects.complete", label: "프로젝트 완료" }`를 더한다(04-02 주석과 같은 결로 이유 한 줄). 시드에 `role-team-lead`·`role-division-head`·`role-ceo`의 `projects` 보기 + `projects.status` 쓰기, `role-ceo`의 `projects.complete` 쓰기를 더한다(시스템 관리자는 기존 루프가 전 메뉴를 켠다).

③ **전이표.** `domain/projects/status-transitions.ts`(데이터만, import 없음): `PROJECT_STATUSES = ["bidding","in_progress","settling","completed","lost"] as const`, `ProjectStatus` 타입, `ALLOWED_TRANSITIONS: { from; to; menu: "projects.status" | "projects.complete" }[]` = 수주중→진행 · 수주중→미수주 · 미수주→진행(셋 다 `projects.status`) · 정산→완료(`projects.complete`). 게이트 규칙과 전환 함수가 이 파일 하나를 같이 읽는다 — `status.ts`와 `register.ts`가 서로 import하는 순환을 만들지 않는다(`test/unit/import-cycles.test.ts`).

④ **게이트 규칙(`domain/rules/register.ts`).** (a) `project.transition` — ctx `{ from, to, actorMenus: { status: boolean; complete: boolean } }`: 전이표에 없는 쌍이면 `이 상태로 바꿀 수 없습니다 · 새로 고쳐 주세요`, 있는데 그 쌍의 메뉴 권한이 없으면 `상태를 바꿀 권한이 없습니다`. (b) `project.start-date-required` — ctx `{ to, startDate }`: 목적지가 진행이고 시작일이 없으면 Copywriting 막힘 문구 그대로 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`. (c) **교체:** 옛 `project.completed-lock` 등록과 그 ctx 타입(견적 외 비용 예외 플래그 포함)을 지우고 `project.line-edit`(ctx `{ status }`)를 등록한다 — 완료면 `완료 · 견적 줄이 잠김`, 나머지 상태는 통과(정산의 「실행가만」은 04-12가 이 규칙의 ctx를 넓혀 판정한다). `domain/quotes/lines.ts`의 저장 게이트 호출과 `[id]/page.tsx`의 편집 가능 판정이 새 규칙 이름을 부르게 바꾼다.

⑤ **전환 함수(`domain/projects/status.ts`).** `changeProjectStatus(viewer, projectId, to, deps?)`: 행 범위(`scopeFor`) 안에서 프로젝트를 찾고(없으면 찾을 수 없음) → `can()`으로 두 메뉴 권한을 읽어 `gate(project, "project.transition", …)` → `gate(project, "project.start-date-required", …)` → 거부면 `GateBlockedError(reason)` → 통과하면 리포지토리 `updateProjectStatusIfCurrent(viewer, id, { expectedStatus: from, status: to, fillEndDateFromStart: boolean }, tx)`(`UPDATE … WHERE id=$1 AND status=$expected RETURNING` — 0행이면 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`; 목적지가 진행이고 종료일이 비어 있으면 같은 문장에서 `end_date = start_date`, D-82) → `recordAction(viewer, { actionType: "status_change", entity: "project", entityId, detail: { from, to } })`. `statusDestinations(viewer, project, deps?)`: 전이표에서 `from`이 지금 상태이고 그 사람이 메뉴 권한을 가진 목적지 목록. `listProjectStatusCatalog(viewer)`: 코드표 `project_status`의 활성 값(`value`·`label`·`description`)을 정렬 순으로. **함수 본문에 상태 문자열 비교를 두지 않는다.**

⑥ **액션.** `app/(app)/projects/actions.ts`에 `changeProjectStatusAction`(zod: `projectId` · `to`는 `PROJECT_STATUSES` enum) → `changeProjectStatus` → `revalidatePath`. `actions.registry.ts`에 등록(D-38).

⑦ **화면.** `app/(app)/projects/status-display.ts`: `PROJECT_STATUS_TAG_KIND`(수주중 muted · 진행 accent · 정산 warning · 완료 success · 미수주 muted — UI-SPEC Color 매핑표). `app/(app)/projects/[id]/status-change.tsx`(클라이언트): props `projectId` · `destinations`(`value`·`label`·`description`) · `startDate`·`endDate` · `currentRevisionSeq`·`currentRevisionApproved`. 2차 「상태 바꾸기」 → 갈 곳이 둘이면 고르기 목록(PC 모달 480 / 폰 시트 — 네이티브 모달 요소, RowSheet 선례, 행마다 라벨 + `--fs-sm --muted` 설명 한 줄, 2차 `취소 Esc`) → 진행을 고르면 확인 모달 `진행으로 바꾸기`(부제 `{번호} {프로젝트명}` · 본문은 Copywriting 「확인 — 진행으로 바꾸기」의 기간 줄 / 종료일 비움 줄 / 고객 승인 전 줄 — **종료일 지남 줄은 04-11이 자동 정산과 함께 더한다**) → 1차 `진행으로 바꾸기`, 2차 `취소 Esc`. 서버 거부는 액션 `serverError` 문자열을 1차 버튼 옆 막힘 자리에 그대로 쓰고 모달을 닫지 않는다(토스트 없음). 성공은 모달을 닫고 토스트 `상태 바꾸기 · 진행`. 이 태스크는 **수주중→진행 한 경로**만 끝까지 만든다(나머지 모달은 Task 2). `[id]/page.tsx`는 `statusDestinations`가 빈 목록이면 이 컴포넌트를 렌더하지 않고, `QuoteLedger`에 새 prop `statusControl`(ReactNode)과 `statusLabel`·`statusTagKind`를 넘긴다. `quote-table.tsx`는 머리 줄의 muted 고정 `StatusTag`를 받은 kind로 바꾸고 `statusControl`을 1차 버튼 앞에 둔다(그 밖의 QuoteLedger 코드는 건드리지 않는다).

⑧ **E2E(트레이서 증명).** `test/e2e/project-lifecycle.spec.ts`: 기획 PM이 시작일만 적어 프로젝트를 등록 → 팀장(`role-team-lead` 픽스처)으로 로그인해 상세를 열고 「상태 바꾸기」 → 목록에서 진행 → 확인 모달 본문에 `종료일 없음 · 시작일 {날짜}로 저장됩니다`가 보이고 → 1차를 눌러 상태 태그가 `진행`, 토스트 `상태 바꾸기 · 진행`. 같은 스펙에서 기획 PM으로 같은 상세를 열면 「상태 바꾸기」가 DOM에 없다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- db/migrations/0009_project_quote_ledger_spine.sql</automated>
    <fails_when>db:migrate가 0이 아닌 코드로 끝나거나(`error:` 줄), 마지막 diff 출력이 비어 있지 않다 — 0009를 고쳐 썼다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm vitest run --project unit test/unit/import-cycles.test.ts</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오거나 import 순환 테스트의 failed 개수가 1 이상이다</fails_when>
    <automated>pnpm playwright test test/e2e/project-lifecycle.spec.ts test/e2e/project-register.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 팀장의 진행 전환이 태그·토스트로 이어지지 않았거나 PM에게 버튼이 보인다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 락 타임아웃 한 쌍 · 가드 두 개(코드표·프로젝트 행) · 재매핑 UPDATE · 옛 코드표 행 DELETE · 새 두 값 INSERT를 담고, 0009 diff가 0줄이다
    - 시드 상태 상수의 라벨·정렬·설명이 마이그레이션과 글자 그대로 같다
    - `menus.ts`에 `projects.status`·`projects.complete`가 있고 시드가 팀장·본부 책임자·대표에게 `projects` 보기와 `projects.status` 쓰기, 대표에게 `projects.complete` 쓰기를 켠다
    - `status-transitions.ts`가 import 없는 데이터 모듈이고 import 순환 테스트가 초록이다
    - `register.ts`의 등록 목록에 옛 이진 규칙 이름이 없고 `project.transition`·`project.start-date-required`·`project.line-edit`가 있다
    - `changeProjectStatus` 본문에 상태 문자열 리터럴 비교가 없다(전이표와 게이트만 거친다)
    - 상태 갱신이 `WHERE status = 기대값` 조건부 UPDATE다
    - `changeProjectStatusAction`이 `actions.registry.ts`에 등록됐다
    - E2E가 팀장의 진행 전환(태그·토스트)과 PM 화면의 버튼 부재를 둘 다 단언한다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 다섯 값이 DB·시드·코드표에 들어가고, 팀장이 수주중 프로젝트를 진행으로 바꾸는 한 경로가 게이트·로그·화면·E2E까지 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 나머지 전환 셋 + 고르기 목록 설명 + 부제의 마지막 변경일 + 목록의 다섯 상태 표시</name>
  <files>domain/projects/status.ts, domain/projects/index.ts, repositories/action-log.ts, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, test/unit/domain/project-status.test.ts, test/integration/project-status.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1227–1255행(S7 전이표 — 미수주 → 진행은 모달 없이 즉시 · 정산 → 완료는 바로 확인 모달) · 372–378행(Destructive — 미수주로 닫기 · 완료 · 비파괴 — 진행 복귀) · 1057–1063행(S3 부제 `정산 2026-09-17`의 뜻 = 현재 상태 + 마지막 변경일)
    - `repositories/action-log.ts`(전문) — `queryActionLog`·`filterActionLog`의 조건 형태(최근 한 줄 조회를 같은 결로 더한다)
    - `app/(app)/projects/page.tsx` 20–70행(`STATUS_OPTIONS` 상수) · `app/(app)/projects/projects-table.tsx`(전문 — 로컬 상태 라벨·태그 맵)
    - `test/integration/revenue-entries.test.ts` 1–60행(계급별 권한을 켜고 viewer를 만드는 통합 테스트 형태)
    - `test/unit/vendors/account-number-plan.test.ts`(결정표 단위 테스트 형태)
  </read_first>
  <behavior>
    - 수주중→진행: `projects.status` 쓰기 + 시작일 있음 → 통과, 종료일이 비면 시작일로 채워진다 · 시작일 없음 → 막힘 문구 · 담당 PM(권한 없음) → 권한 거부
    - 수주중→미수주: `projects.status` 쓰기면 기간 없이 통과
    - 미수주→진행: `projects.status` 쓰기 + 시작일 → 통과, 번호·줄이 그대로 · 시작일 없음 → 막힘 문구
    - 정산→완료: `projects.complete` 쓰기(대표·시스템 관리자)만 통과 · 팀장(`projects.status`만 있음) → 권한 거부
    - 진행→미수주 · 진행→수주중 · 정산→미수주 · 정산→진행(상태 바꾸기 경로) · 완료→진행 · 완료→미수주 → 전부 「이 상태로 바꿀 수 없습니다」
    - `statusDestinations`: 수주중 + 팀장 → [진행, 미수주] · 진행 + 누구든 → [] · 정산 + 대표 → [완료] · 정산 + 팀장 → [] · 완료 → [] · 미수주 + 팀장 → [진행] · 담당 PM → 어느 상태든 []
    - 코드베이스 스캔: `app/`·`domain/`·`repositories/`의 `.ts`·`.tsx`에 옛 잠금 상태 값의 문자열 리터럴이 없다(마이그레이션 파일은 스캔하지 않는다)
  </behavior>
  <action>
① `test/unit/domain/project-status.test.ts`에 위 `behavior`를 결정표로 먼저 쓴다(RED) — 권한은 `deps.can`으로 주입해 DB 없이 돈다. 마지막 describe는 옛 잠금 값 리터럴 스캔이다(주석 속 영어 낱말 `unsettled` 같은 부분 일치는 단어 경계로 걸러낸다).

② `status-change.tsx`에 나머지 셋을 더한다: 미수주로 닫기 확인 모달(Copywriting Destructive — 미수주로 닫기, 2차 `취소 Esc`) · 정산→완료 확인 모달(Destructive — 완료, 갈 곳이 하나라 목록 없이 바로) · 미수주→진행은 **모달 없이 즉시**(D-44 — 잃는 것이 없다), 시작일 게이트에 막히면 「상태 바꾸기」 옆에 이유 한 줄. 고르기 목록 행의 설명은 `listProjectStatusCatalog`의 `description`이고 없으면 그 행에 2행이 없다(S14 — `—` 금지). 성공 토스트 `상태 바꾸기 · 미수주` / `· 완료` / `· 진행`.

③ **부제의 마지막 변경일(D-50).** `repositories/action-log.ts`에 `findLatestActionFor(viewer, { entity, entityId, actionType })`(최신 한 줄)를 더하고, `domain/projects/status.ts`에 `lastStatusChangeOn(viewer, project)` — 최신 `status_change` 로그의 날짜(KST 날짜 문자열), 없으면 등록일(`created_at`의 KST 날짜). `[id]/page.tsx`가 부제를 `{번호} · 상세 견적 {n}차 · {상태 라벨} {날짜}`로 만들어 `QuoteLedger`에 넘긴다(자동 정산의 날짜는 04-11이 로그 detail의 발효일로 바꾼다). 상태 이력 표·섹션을 만들지 않는다.

④ **목록의 다섯 상태.** `app/(app)/projects/page.tsx`의 `STATUS_OPTIONS` 상수를 `listProjectStatusCatalog` 결과로 바꾸고(필터 한 줄의 상태 select), `projects-table.tsx`의 로컬 라벨·태그 맵을 서버가 넘긴 라벨과 `status-display.ts`의 kind로 바꾼다(목록 개정 D-87~D-91 자체는 04-17 — 이 플랜은 상태 표시만 고친다).

⑤ `test/integration/project-status.test.ts`: (a) 네 전환이 실제 DB에서 상태를 바꾸고 `status_change` 로그가 정확히 한 줄(detail from·to), (b) 수주중→진행에서 종료일이 비었으면 시작일로 채워졌다(DB 재조회), (c) 거부된 전환(권한 없음·목록 밖·시작일 없음)은 상태·로그 무변경(DB 재조회), (d) 담당 PM viewer로 `changeProjectStatus`를 직접 불러도 거부, (e) 동시 전환 두 개(`Promise.all`) 중 하나만 성공하고 로그 한 줄, (f) 마이그레이션 뒤 상태 — 코드표 `project_status`가 정확히 다섯 값(라벨·정렬·설명이 시드 상수와 같다)이고 `projects.status`에 다섯 값 밖의 값이 0건이다(가드 자체는 적용 전 상태에서만 의미가 있어 통합 테스트로 재현하지 않는다 — SQL에 가드 두 개가 있다는 것은 Task 1 수용 기준이 본다), (g) 미수주 프로젝트의 견적 줄 저장이 통과하고 완료 프로젝트의 저장은 `완료 · 견적 줄이 잠김`으로 거부된다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-status.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 전이 결정표나 옛 값 스캔이 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 전환·로그·동시성·잠금 중 하나가 DB에서 어긋났다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior`의 여덟 줄을 전부 덮고 초록이다
    - 옛 잠금 값 리터럴 스캔 describe가 있고 초록이다
    - 미수주→진행에 확인 모달이 없고, 정산→완료는 목록 없이 바로 확인 모달이다
    - 고르기 목록 행의 설명이 코드표에서 오고, 설명 없는 값에는 2행이 없다
    - 부제의 날짜가 최신 `status_change` 로그(없으면 등록일)에서 오고 상태 이력 표·섹션 마크업이 없다
    - `page.tsx`·`projects-table.tsx`에 로컬 상태 라벨 상수가 남지 않았다
    - 통합 테스트 (a)~(g)가 전부 있고 초록이다 — (c)·(e)는 DB 재조회로 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>사람이 하는 전환 넷이 권한·시작일·동시성 규칙대로 동작하고, 상세 부제와 목록이 다섯 상태를 코드표 라벨·Color 매핑으로 보인다</done>
</task>

<task type="auto">
  <name>Task 3: 옛 이진 게이트 회귀 정리 + 생애 E2E 완성 + 화면 검증 순서</name>
  <files>test/unit/domain/rules-gate.test.ts, test/integration/quote-lines.test.ts, test/integration/leak-scan.test.ts, test/e2e/project-lifecycle.spec.ts, app/(app)/projects/[id]/project-detail.module.css</files>
  <read_first>
    - `test/unit/domain/rules-gate.test.ts`(전문) — 옛 규칙 이름과 옛 잠금 값을 쓰는 케이스(33·45행 근처)
    - `test/integration/quote-lines.test.ts` 100–120행(옛 잠금 값을 DB에 직접 넣는 픽스처)
    - `test/integration/leak-scan.test.ts`(전문) — side-effect import 목록(새 모듈·액션 레지스트리가 빠지면 커버리지 테스트가 실패한다, 04-01 Rule 2 선례)
    - `app/(app)/projects/[id]/project-detail.module.css` — 머리 줄 `flex-wrap`(U-1 판단)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <action>
① `test/unit/domain/rules-gate.test.ts`: 옛 규칙 이름을 부르던 케이스를 새 규칙 `project.line-edit`(완료 → 거부 · 정산·미수주·수주중·진행 → 통과)로 바꾸고, `listGateRules()`에 옛 이름이 없고 새 세 이름이 있다는 단언을 더한다. `project.transition`·`project.start-date-required`의 경계(목록 밖 쌍 · 메뉴 권한 없음 · 시작일 없음)를 한 줄씩 더한다. 케이스를 지우지 않고 바꾼다.

② `test/integration/quote-lines.test.ts`의 옛 잠금 값 픽스처를 `completed`로 바꾸고 거부 문구 단언을 `완료 · 견적 줄이 잠김`으로 맞춘다.

③ `test/integration/leak-scan.test.ts`의 side-effect import에 새 상태 모듈과 `actions.registry`가 이미 들어 있는지 확인하고 빠졌으면 더한다.

④ `test/e2e/project-lifecycle.spec.ts`를 완성한다: (a) 시작일 없는 수주중 프로젝트에서 팀장이 진행을 고르면 확인 모달 1차가 비활성이고 옆에 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`(게이트 문자열 그대로), 모달이 열린 채이고 토스트가 없다, (b) 팀장이 수주중→미수주 모달로 닫고 → 같은 프로젝트에서 미수주→진행을 누르면 모달 없이 즉시 진행이 된다, (c) 테스트 준비 단계에서 프로젝트 하나를 DB에 `settling`으로 두고(정산은 사람이 켜는 상태가 아니다 — 자동 전환은 04-11) 대표로 열어 「상태 바꾸기」 → 바로 `완료로 바꾸기` 모달 → 완료 태그(success), 같은 정산 프로젝트를 팀장으로 열면 「상태 바꾸기」가 없다, (d) 완료 프로젝트에서 PM의 견적 줄 저장 시도가 `완료 · 견적 줄이 잠김`으로 거부된다(셀 단위 잠금 렌더는 04-12), (e) 진행 프로젝트는 누구에게도 「상태 바꾸기」가 없다, (f) 375px 뷰포트에서 팀장 시점의 머리 줄(상태 바꾸기 포함)의 문서 가로 스크롤이 0이고 버튼이 전부 DOM에 있다.

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트(lint·typecheck·build) → 이 태스크 실행자가 아닌 **별도 서브에이전트**(model: sonnet, 프롬프트에 `verification-before-completion` 명시)가 `CI=true`로 앱을 띄워 S7 long-text backstop(가장 긴 확인 모달 본문이 480 안에서 잘리지 않음)과 S3 overflow backstop(375 머리 줄)을 DOM 실측으로 판정 → 필요하면 `project-detail.module.css`만 고친다(라벨을 줄이거나 버튼을 숨기지 않는다) → 전체 게이트 한 번(`CI=true pnpm test`). 판정 기록을 SUMMARY에 남긴다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-gate.test.ts test/unit/domain/project-status.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 옛 규칙 이름이 남았거나 새 규칙 경계가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts test/integration/project-status.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — project-lifecycle의 여섯 케이스 중 하나라도 실패하면 여기서 드러난다</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 판정 기록(S7 long-text · S3 375 머리 줄)을 SUMMARY에서 확인</test>
      <expected>확인 모달 본문이 480 안에서 잘리지 않고, 375 머리 줄에 가로 스크롤이 없으며 버튼이 숨지 않는다</expected>
      <why_human>폭·줄바꿈은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `rules-gate.test.ts`에 옛 규칙 이름이 등록 목록에 없다는 단언과 새 규칙 세 개의 경계 케이스가 있고, 기존 케이스 수가 줄지 않았다
    - `quote-lines.test.ts`의 잠금 픽스처가 `completed`이고 거부 문구가 새 규칙 문자열이다
    - `project-lifecycle.spec.ts`가 (a)~(f) 여섯 경우를 단언한다 — (a)는 막힘 문구·모달 유지·토스트 부재 셋을 모두 본다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - `CI=true pnpm test` 세 계층이 전부 초록이다
  </acceptance_criteria>
  <done>옛 이진 게이트의 흔적이 테스트에서 사라지고, 다섯 상태 생애(진행 전환·미수주 왕복·대표 완료·완료 잠금)가 E2E로 증명되며 전체 게이트가 CI=true로 초록이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 상태 전환 액션 | 목적지 상태가 넘어온다. 전이표·권한·시작일 판정이 서버에만 있어야 한다 |
| 권한표 → 전환 | 새 메뉴 두 개가 전환 주체를 정한다. 시드 기본값이 틀리면 PM이 상태를 바꾸거나 대표가 완료를 못 한다 |
| 마이그레이션 → 기존 데이터 | 옛 상태 값을 새 값으로 옮긴다. 전제가 깨진 DB에서 조용히 지우면 행이 고아가 된다 |
| domain → 게이트 | 이후 페이즈(결재 승인 호출자·증빙 필수·마감)가 얹힐 단일 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-34 | Elevation of Privilege | `changeProjectStatus` | high | mitigate | 버튼을 보내지 않는 것과 별개로 서버가 액션에서 `can()`으로 `projects.status`/`projects.complete`를 다시 판정하고 행 범위(`scopeFor`) 밖 프로젝트는 찾을 수 없음으로 떨어뜨린다. 담당 PM viewer가 함수를 직접 부르는 거부 케이스를 통합 테스트(d)로 고정한다 |
| T-04-35 | Elevation of Privilege | 정산→완료 | high | mitigate | 완료는 `projects.complete` 한 메뉴로만 열리고 시드는 대표에게만 켠다(시스템 관리자는 전 메뉴 루프). 팀장의 완료 시도 거부를 결정표·E2E(c)로 고정한다 |
| T-04-36 | Tampering | 목적지 상태 입력 | high | mitigate | zod enum이 다섯 값 밖을 막고, 전이표에 없는 쌍(진행→정산 포함)은 게이트가 거부한다. 정산은 사람이 고를 수 있는 목적지가 아니다 |
| T-04-37 | Tampering | 동시 전환 | medium | mitigate | `WHERE status = 기대값` 조건부 UPDATE로 한 사람만 성공하고 로그도 한 줄이다(통합 테스트 e) |
| T-04-38 | Repudiation | 상태 변경 이력 | medium | mitigate | `status_change`는 끌 수 없는 종류라 설정으로 이력을 없앨 수 없다. 거부된 전환은 로그를 남기지 않는다(통합 테스트 c) |
| T-04-39 | Information Disclosure | 상태 변경 로그의 활용 | medium | mitigate | 로그를 사람별로 집계해 보여주는 화면·내보내기를 만들지 않는다(금지 항목). 상세에는 마지막 변경일만 파생한다 |
| T-04-40 | Tampering | 마이그레이션 0012 | medium | mitigate | 코드표·프로젝트 행에 알려진 네 값 밖의 상태가 있으면 지우지 않고 RAISE EXCEPTION으로 전체를 되돌린다. 재매핑은 DELETE가 아니라 UPDATE다(Pitfall 6). 락 타임아웃 한 쌍 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`, 0009 diff 0줄
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `domain/rules/register.ts` 등록 목록: `project.transition` · `project.start-date-required` · `project.line-edit`(옛 이진 규칙 없음)
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 상태가 다섯 값이고 옛 잠금 값이 데이터·코드·테스트에서 사라졌다
- 사람이 하는 전환 넷이 권한표·시작일·동시성 규칙대로 게이트를 지나고, 진행·완료에는 사람이 가는 곳이 없다
- 완료 잠금이 끊기는 구간 없이 새 편집 규칙으로 교체됐다
- Phase 5는 정산→완료의 호출자만 결재 승인으로 바꾸면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/0012_project_status_five_values.sql`(실제 번호는 생성기 기준) + `meta/0012_snapshot.json` |
| 코드표 값 | `project_status`: `settling`(정산)·`completed`(완료) 추가, 옛 잠금 값 행 삭제·재매핑, `lost` 정렬 4 |
| 상수·타입(신규) | `domain/projects/status-transitions.ts` — `PROJECT_STATUSES` · `ProjectStatus` · `ALLOWED_TRANSITIONS` |
| 함수(신규) | `changeProjectStatus` · `statusDestinations` · `listProjectStatusCatalog` · `lastStatusChangeOn`(domain) · `updateProjectStatusIfCurrent`(repository) · `findLatestActionFor`(repository/action-log) |
| 게이트 규칙 | `project.transition` · `project.start-date-required` · `project.line-edit`(옛 `project.completed-lock` **삭제·교체**) |
| 권한표 메뉴(신규) | `projects.status`(프로젝트 상태 변경) · `projects.complete`(프로젝트 완료) + 시드(팀장·본부 책임자·대표: `projects` 보기 · `projects.status` 쓰기 / 대표: `projects.complete` 쓰기) |
| Server Action | `changeProjectStatusAction`(등록 포함) |
| 컴포넌트 | `app/(app)/projects/[id]/status-change.tsx`(신규) · `app/(app)/projects/status-display.ts`(신규, `PROJECT_STATUS_TAG_KIND`) |
| 변경된 기존 심볼 | `QuoteLedger` props(`statusControl`·`statusLabel`·`statusTagKind`·부제) · `saveQuoteLines` 게이트 호출 · 목록 상태 필터·상태 열 · 시드 상태 상수 · `projects.status` 주석 |
| 테스트 | `test/unit/domain/project-status.test.ts`(신규) · `test/integration/project-status.test.ts`(신규) · `test/e2e/project-lifecycle.spec.ts`(신규) · `rules-gate.test.ts`·`quote-lines.test.ts`(갱신) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 차수 기계·고객 승인·차수 섹션(04-14) · 프로젝트 복사(04-15) · 자동 정산·기간 칸(04-11) · 정산 셀 범위·줄 삭제(04-12) · 조정·견적 외 비용 줄(04-13) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md` when done
</output>
