---
phase: 04-project-quote-ledger
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-04"]
files_modified:
  - domain/rules/gate.ts
  - domain/rules/register.ts
  - domain/projects/status.ts
  - domain/projects/index.ts
  - domain/quotes/revisions.ts
  - domain/quotes/lines.ts
  - domain/settings/keys.ts
  - repositories/quote-revisions.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/revision-section.tsx
  - app/(app)/projects/[id]/status-change.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/project-form.tsx
  - test/unit/domain/project-status.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/quote-revisions.test.ts
  - test/integration/project-status.test.ts
  - test/e2e/project-lifecycle.spec.ts
autonomous: true
requirements: [PROJ-04, PROJ-05, PROJ-07]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "프로젝트 상태 네 가지(수주중 · 진행 · 완료(정산) · 미수주)만 존재하고 전환 네 가지(수주중→진행 · 수주중→미수주 · 진행→완료 · 미수주→진행)만 허용된다 — 허용 목록 밖 전환 요청은 거부된다(D-41·D-44)"
    - "상태 전환 네 가지가 전부 `domain/rules.gate` 단일 진입점을 지난다 — 전환 함수 안에 상태 이름을 직접 비교하는 분기가 흩어져 있지 않다(ROADMAP 기준 4)"
    - "전환 주체는 팀장 이상이고 담당 PM은 전환할 수 없다 — PM에게는 비활성 버튼이 아니라 버튼 자체가 서버에서 오지 않는다(D-46, S3)"
    - "기간(시작일·종료일)이 없는 프로젝트의 수주중→진행 전환은 거부되고 이유가 기간이 없다는 것을 말한다(D-49)"
    - "수주중 상태에서는 고객 승인 게이트가 통과된다 — 승인 전 차수의 줄에서도 지출 관련 동작이 막히지 않는다(D-43)"
    - "진행으로 바뀌는 순간부터 현재 차수의 승인 여부가 게이트에 걸린다(D-43)"
    - "잠기는 상태는 완료(정산) 하나뿐이다 — 미수주 프로젝트의 줄 편집과 지출 관련 동작은 계속 열려 있고 화면에 자물쇠·회색 처리가 없다(D-45)"
    - "완료(정산) 프로젝트에서 견적 줄 편집과 담당 PM의 새 지출 동작은 거부되고, 경영관리의 「견적 외 비용」 줄 추가만 예외로 통과한다 — 이 예외도 같은 게이트를 지난다(D-47)"
    - "「견적 외 비용」은 별도 표가 아니라 견적가 0인 견적 줄의 한 종류다 — 별도 '조정' 종류를 만들지 않는다(D-48)"
    - "견적 외 비용 줄의 실행가는 음수를 허용한다(D-48, EXP-14)"
    - "미수주 → 진행 복귀가 가능하고 번호·견적 줄·비용이 그대로 살아난다(D-44)"
    - "상태 변경이 행동 로그에 상태 변경 종류로 남고, 프로젝트 상세에는 현재 상태와 마지막 변경일만 보인다 — 새 표·새 섹션이 없다(D-50)"
    - "새 차수는 이전 차수 전체 복사 후 편집이고 빈 차수를 만들 수 없다 — 복사된 줄이 원본 줄 식별자를 계보로 갖는다(D-53)"
    - "차수 되돌리기가 없다 — 이전 차수로 돌아가는 동작이 어디에도 없다(D-53)"
    - "최신 차수만 현재 차수다 — 지출·합계가 전부 현재 차수 기준이고, 승인 차수와 편집 중 차수가 동시에 살아 있는 상태가 없다(D-54)"
    - "이전 차수 줄의 연결 문서가 계보를 따라 현재 차수의 대응 줄에 이어 보인다 — 문서 행을 옮기지 않는다(D-55)"
    - "연결 문서가 있는 줄의 금액 셀(수량·단가·실행가·환율)은 서버가 읽기 전용 단계로 보내고 항목명·비고는 편집 단계로 보낸다 — 고치려면 새 차수다(D-66)"
    - "읽기 전용으로 보낸 금액 셀의 값을 액션에 직접 실어 보내도 서버가 거부한다 — 셀 단계 판정이 화면 표시가 아니라 저장 경로의 판정이다(D-66)"
    - "차수의 고객 승인 표시는 담당 PM이 승인일과 함께 켜고 결재를 거치지 않으며 행동 로그에 남는다(D-56)"
    - "승인 표시 취소는 그 차수에 연결 문서가 없을 때만 가능하고, 있으면 거부되며 이유가 새 차수로 고치라고 말한다(D-56)"
    - "견적 번호는 표시용 파생값이다 — 저장 컬럼도 카운터도 없고 프로젝트 번호와 차수 순번에서 만들어진다(D-56)"
    - "연결 문서가 없는 견적 줄은 보관되고, 있는 줄은 보관 대신 '취소' 상태(견적가 0, 이력 유지)로만 바뀌며 외래키는 제한 동작이다(PROJ-05·D-56)"
    - "프로젝트 복사는 기본 정보와 현재 차수 줄(금액 포함)만 가져오고 기간·번호·사전 견적·매출 칸·연결 문서·줄 계보는 따라오지 않는다 — 새 프로젝트는 상세 1차·미승인·수주중으로 시작한다(D-70)"
    - "복사 등록 폼 위 한 줄이 어느 프로젝트에서 몇 줄을 가져왔는지 밝힌다(S2)"
    - "별도 가져오기 모달이 없다 — 다른 프로젝트의 줄은 표 간 범위 복사·붙여넣기로 가져온다(D-70)"
    - "고객 승인 게이트는 설정으로 끌 수 있고 끄면 승인 전 차수에서도 지출 관련 동작이 통과한다(ROADMAP 기준 3)"
    - "차수 섹션은 편집 가능한 셀이 구조적으로 0이라 격자가 아니라 캡션 있는 표로 렌더되고 머리글이 흰 바탕이다(§7-3 (가))"
    - "차수 섹션에 0행 상태가 없다 — 프로젝트를 등록하면 상세 견적 1차가 항상 함께 생긴다(D-53)"
    - "현재 차수 행의 동작 칸을 `—`로 채우지 않고 비워 둔다(§7-3)"
    - "차수 섹션의 폰 1순위 세 열은 차수·견적 합계·동작이고 접힌 줄은 상태·생성일·줄 수다"
    - "상태 전환 확인에서 갈 수 있는 곳이 하나면 바로 확인 모달이고 둘이면 고르는 목록 뒤 확인이다 — 완료 상태에서는 버튼 자체가 없다(S7)"
    - "버튼 넷이 다 모인 상세 머리 줄이 375px에서 가로 스크롤을 만들지 않는다 — 버튼군이 아래 줄로 내려갈 뿐 하나도 숨거나 접히지 않는다(U-1 판단의 최종 상태 확인; 판단 원문은 04-01 Task 2 ⑪)"
    - "전환이 막히면 화면이 이유를 따로 만들지 않고 게이트가 돌려준 문자열을 그대로 쓴다(ROADMAP 기준 3·UX-06)"
    - "1차 라벨에 「취소」가 들어가는 모달의 2차는 「닫기」로 적힌다(§7-8 보강)"
    - "전환 실패에 토스트를 띄우지 않는다 — 토스트는 이동이 따르는 성공 전용이다(§7-6)"
    - statement: "차수 섹션은 제 오류 자리를 갖지 않고 상세 로드 실패 한 줄에 흡수된다 — 섹션 단독 오류 렌더가 없다"
      verification: backstop
    - statement: "상태 전환 모달·시트의 문자열이 고정 문구와 짧은 보간값(담당자 이름·차수·금액)뿐이라 모달 폭 안에서 잘리지 않는다"
      verification: backstop
  prohibitions:
    - statement: "상태 변경 행동 로그가 직원 감시·성과 집계 도구가 되어서는 안 된다. 「누가 며칠 만에 수주로 바꿨는가」를 사람별로 집계해 보여주는 화면·내보내기를 이 페이즈가 만들지 않는다 — D-50이 상세에 「현재 상태 + 마지막 변경일」만 두고 새 표·새 섹션을 금지한 것은 저장 공간이 아까워서가 아니라 그 집계 화면이 생기는 것을 막기 위해서다"
      status: resolved
      verification: judgment
    - statement: "완료(정산) 외의 상태가 사람의 실제 지출을 막는 잠금이 되어서는 안 된다. 미수주로 닫힌 프로젝트에 뒤늦게 도착하는 청구서(PT 제작비 등)는 실제로 일어나는 일이고, 규칙이 그것을 못 넣게 만들면 사람은 시스템 밖에서 처리한다 — 편의를 위해 미수주를 잠그는 구현은 금지한다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/status.ts"
      provides: "상태 네 값과 허용 전환 네 가지의 정본 + 전환 함수. 전환 판정은 전부 게이트를 지난다"
      exports: ["PROJECT_STATUSES", "ALLOWED_TRANSITIONS", "changeProjectStatus"]
    - path: "domain/rules/register.ts"
      provides: "이 페이즈의 게이트 규칙 다섯 등록 — 고객 승인 · 완료 잠금 · 수주중 예외 · 기간 필수 · 거래처 필수"
      contains: "registerGateRule"
    - path: "domain/quotes/revisions.ts"
      provides: "차수 생성(전체 복사) · 현재 차수 판정 · 고객 승인 표시와 취소"
      exports: ["createRevisionFromCurrent", "currentRevision", "setCustomerApproval", "quoteDisplayNumber"]
    - path: "app/(app)/projects/[id]/revision-section.tsx"
      provides: "차수 섹션 — 읽기 형태 표, 최신이 위, 이전 차수 행에만 열기 동작"
      contains: "caption"
    - path: "app/(app)/projects/[id]/status-change.tsx"
      provides: "상태 바꾸기 — 갈 곳 하나면 확인, 둘이면 고르기 뒤 확인. 게이트 이유를 그대로 표시"
      contains: "gate"
    - path: "test/unit/domain/project-status.test.ts"
      provides: "상태 전환 네 가지 × 주체 × 선행 조건 표"
      min_lines: 40
    - path: "test/integration/quote-revisions.test.ts"
      provides: "전체 복사·계보·현재 차수·승인 표시와 취소 조건 증명"
      min_lines: 35
    - path: "test/e2e/project-lifecycle.spec.ts"
      provides: "수주중 → 진행 → 완료 한 흐름과 완료 뒤 잠금·경영관리 예외 스모크"
      min_lines: 35
  key_links:
    - from: "domain/projects/status.ts"
      to: "domain/rules/gate.ts"
      via: "네 전환이 전부 게이트 한 지점을 지난다"
      pattern: "gate("
    - from: "domain/quotes/revisions.ts"
      to: "repositories/quote-lines.ts"
      via: "새 차수가 이전 차수 줄을 전체 복사하며 계보 식별자를 남긴다"
      pattern: "copied_from_line_id"
    - from: "app/(app)/projects/[id]/status-change.tsx"
      to: "domain/rules/gate.ts"
      via: "막힘 문구가 게이트가 돌려준 이유 문자열 그대로다"
      pattern: "reason"
    - from: "domain/projects/status.ts"
      to: "domain/action-log/record.ts"
      via: "전환 성공 뒤 상태 변경 종류로 로그를 남긴다"
      pattern: "recordAction"
---

<objective>
**As a** 기획팀장, **I want to** 수주중으로 쌓아 둔 프로젝트를 진행으로 올리거나 미수주로 닫고, 견적을 새 차수로 복사해 고치고 고객 승인 표시를 켜고, 끝난 프로젝트를 완료(정산)로 잠그고 싶다, **so that** 프로젝트의 생애가 한 화면에서 끝나고 「승인 전 차수에서 돈이 나가는」 일이 규칙 하나로 막힌다.

이 플랜은 이 페이즈의 **규칙 슬라이스**다. 04-01이 만든 게이트 진입점에 이 페이즈의 규칙 다섯을 전부 등록하고, 그 규칙들이 실제로 판정하는 두 화면(차수 섹션 · 상태 바꾸기)을 같은 슬라이스에서 붙인다. 프로젝트 복사(D-70)도 여기 있다 — 차수 전체 복사와 같은 기계를 쓰기 때문이다.

Purpose: ROADMAP 기준 3이 요구하는 것은 "이 판정이 `domain/rules.gate` 단일 진입점을 거친다"는 구조적 속성이고, 기준 4는 "전환 네 가지가 전부 3의 게이트를 지난다"이다. 규칙을 화면마다 인라인으로 다시 쓰면 Phase 5·6·7·8이 증빙 필수·결재 마감·알림 조건·이전 행 면제를 얹을 자리가 사라진다(Issue 9).

Output: 게이트 규칙 다섯 + 상태 전이표 + 차수 기계 + 프로젝트 복사, 차수 섹션·상태 바꾸기 두 화면, 단위 2 · 통합 2 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-RESEARCH.md
@.planning/phases/04-project-quote-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md
@CLAUDE.md
</context>

<probe_fallback>
엣지 프로브 폴백의 전체 표와 금지 항목 규약은 `04-01-PLAN.md`의 `<probe_fallback>`에 있다. 이 플랜이 담당하는 미분류 행은 **PROJ-04 · PROJ-05(프로젝트 복사 쪽) · PROJ-07** 셋이고, 프로브가 답을 주지 못했으므로 수용 기준은 ROADMAP 기준 3·4 · D-41·D-43~D-50·D-52~D-56·D-70 · `04-UI-SPEC.md` S3·S5·S7에서 끌어와 위 `must_haves.truths`에 적었다. 조용히 버린 항목은 없다.

`04-UI-SPEC.md`의 미해소 U-2(차수 상태 열의 두 경계 낱말)는 `04-01-PLAN.md` Task 1 체크포인트 ⑥에서 사용자가 답한 값을 쓴다 — 이 플랜이 혼자 정하지 않는다.
</probe_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 상태 네 값 · 전환 네 가지 · 게이트 규칙 다섯</name>
  <files>domain/projects/status.ts, domain/rules/gate.ts, domain/rules/register.ts, domain/projects/index.ts, domain/settings/keys.ts, test/unit/domain/project-status.test.ts, test/unit/domain/rules-gate.test.ts</files>
  <reversibility rating="costly">네 상태와 「최신 차수만 현재」 둘은 Phase 5·6·9·10의 게이트·손익 경로가 전제하는 모델이다(D-41·D-54) — 뒤집으려면 저장된 상태 값의 재매핑과 그 네 페이즈의 판정 경로를 함께 고쳐야 한다. 두 결정은 잠긴 사용자 결정이므로 이 플랜은 구현만 한다.</reversibility>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-41·D-43~D-50 원문 — 네 상태, 수주중 예외, 미수주 복귀, 미수주 비잠금, 전환 주체, 완료 뒤 경영관리 예외 셋, 견적 외 비용, 기간 필수, 행동 로그
    - `.planning/ROADMAP.md` Phase 4 성공 기준 3·4 전문 — 게이트 단일 진입점과 이후 페이즈가 얹을 것들(증빙 필수·결재 마감·알림 조건·이전 행 면제)
    - `domain/rules/gate.ts`·`domain/rules/register.ts`(04-01 산출물 전문) — 규칙 등록 형태와 이미 등록된 완료 잠금 규칙
    - `domain/projects/index.ts`(04-01·04-05 산출물 전문) — 프로젝트 domain의 현재 함수 목록과 DTO 스펙
    - `domain/permissions/can.ts`·`domain/permissions/roles.ts`(전문) — 「팀장 이상」을 계급 순위가 아니라 권한표 동작으로 표현해야 하는 이유(03-01이 순위 컬럼을 두지 않기로 했다)
    - `domain/permissions/menus.ts`(전문) — 권한표의 메뉴×동작 열. 「상태 변경」 동작을 어떻게 표현할지 여기서 판단한다
    - `domain/action-log/record.ts`(04-01이 상태 변경 종류를 더한 현재 형태)
    - `domain/settings/keys.ts`(04-02·04-05가 키를 더한 현재 형태) — 고객 승인 게이트 on/off 키를 더할 자리와 형식
    - `domain/code-tables/index.ts`(전문) — 상태 코드표 조회. 상태 **값**은 코드표에 있고 **전이 규칙**은 코드 층이라는 D-41의 구분을 지킨다
    - `domain/settings/registry.ts` 1–80행 — `getSettingValue`와 `deps` 주입
    - `test/unit/domain/rules-gate.test.ts`(04-01 산출물 전문) — 확장할 단위 테스트
    - `test/unit/vendors/account-number-plan.test.ts`(전문) — 결정표 기반 순수 함수 단위 테스트 형태
  </read_first>
  <behavior>
    - 수주중 → 진행: 기간이 있고 주체가 팀장 이상이면 통과, 기간이 없으면 거부하고 이유가 기간을 말한다, 주체가 담당 PM이면 거부한다
    - 수주중 → 미수주: 팀장 이상이면 기간이 없어도 통과한다
    - 진행 → 완료(정산): 팀장 이상이면 통과한다
    - 미수주 → 진행: 팀장 이상이면 통과하고 번호·줄이 그대로다
    - 진행 → 수주중, 완료 → 진행, 완료 → 미수주 등 허용 목록 밖 전환: 전부 거부된다
    - 고객 승인 규칙: 상태가 수주중이면 승인 여부와 무관하게 통과, 진행이고 현재 차수가 미승인이면 거부, 설정으로 끄면 통과
    - 완료 잠금 규칙: 완료 상태에서 견적 줄 편집은 거부, 경영관리의 「견적 외 비용」 줄 추가는 통과, 미수주 상태에서는 전부 통과
    - 기간 필수 규칙: 시작일·종료일 중 하나라도 없으면 거부
    - 거래처 필수 규칙: 줄에 거래처가 없으면 거부(이 페이즈는 등록만 하고 호출은 Phase 5)
    - 등록되지 않은 규칙 이름은 던진다(04-01에서 이미 고정된 동작)
  </behavior>
  <action>
① `domain/projects/status.ts`에 상태 네 값과 **허용 전환 목록**을 정본으로 둔다. 상태 **값** 자체는 코드표(`project_status`)에서 오고 이 파일은 그 값들 사이의 **전이 규칙**을 갖는다(D-41의 「값은 코드표, 전이는 코드 층」). `changeProjectStatus(viewer, projectId, toStatus, deps?)`는 (a) 허용 전환인지 목록으로 확인 → (b) `gate()`로 전환 규칙을 판정 → (c) 통과하면 상태를 바꾸고 상태 변경 종류로 행동 로그를 남긴다. **함수 안에 상태 이름을 직접 비교하는 분기를 두지 않는다** — 비교는 허용 목록과 게이트 규칙 안에만 있다.

② 「전환 주체는 팀장 이상」을 **계급 순위 비교로 구현하지 않는다**(03-01이 순위 컬럼을 두지 않기로 했다). 권한표의 메뉴×동작으로 표현한다 — 프로젝트 메뉴에 상태 변경에 해당하는 동작 판정을 붙이고, 기본값 시드가 팀장·본부 책임자·대표·경영관리에게 그 칸을 켠다(D-46). `domain/permissions/menus.ts`의 동작 축에 값을 더할지, 기존 동작 중 하나로 표현할지는 그 파일의 현재 형태를 읽고 정하되 **선택과 근거를 SUMMARY에 적는다**.

③ `domain/rules/register.ts`에 규칙 다섯을 전부 등록한다:
- **고객 승인** — 상태가 수주중이면 통과(D-43), 진행 이후이고 현재 차수가 미승인이면 거부. 설정 키로 끌 수 있다(ROADMAP 기준 3)
- **완료 잠금** — 04-01이 자리만 잡아 둔 규칙을 완성한다. 완료(정산)에서 견적 줄 편집·담당 PM의 새 지출 동작은 거부, 경영관리의 「견적 외 비용」 줄 추가는 통과(D-47). **미수주에서는 전부 통과**(D-45)
- **수주중 예외** — 위 두 규칙이 공유하는 조건이라 별도 규칙으로 빼지 말지, 독립 규칙으로 둘지 판단한다. 독립으로 두면 Phase 5·6이 「수주중이면 통과」를 재사용할 수 있고, 합치면 규칙 수가 준다. **선택과 근거를 SUMMARY에 적는다**
- **기간 필수** — 수주중→진행 전환의 선행 조건(D-49)
- **거래처 필수** — 줄에 거래처가 없으면 지출 관련 동작을 거부(D-64). 이 페이즈는 **등록만** 하고 호출자는 Phase 5가 붙인다

④ `domain/settings/keys.ts`에 고객 승인 게이트 on/off 키를 더한다(단순값, 기본 켬). 04-02·04-05가 같은 파일에 키를 더했으므로 그 결과 위에 붙인다.

⑤ 단위 테스트 둘을 결정표로 쓴다. `project-status.test.ts`는 **전환 네 가지 × 주체(PM·팀장·경영관리) × 선행 조건(기간 유무)** 격자와 허용 목록 밖 전환 거부를, `rules-gate.test.ts`(04-01 파일 확장)는 규칙 다섯 각각의 통과·거부 경계와 설정으로 끈 경우를 덮는다. 설정·권한 조회는 `deps` 주입으로 고정해 DB 없이 돌린다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-status.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>node -e "const fs=require('fs');const raw=fs.readFileSync('domain/rules/register.ts','utf8');const code=raw.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n');const n=(code.match(/registerGateRule\s*[(<]/g)||[]).length;if(n<5){console.error('GATE RULES REGISTERED '+n);process.exit(1)}console.log('ok '+n)"</automated>
    <fails_when>표준 출력에 ok 대신 GATE RULES REGISTERED로 시작하는 줄이 나오고 종료 코드가 1이다 — 이 페이즈가 등록해야 할 규칙 다섯 중 일부가 빠졌다(주석 줄은 세지 않으므로 주석만으로는 통과하지 못한다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/projects/status.ts`에 허용 전환 목록이 있고 그 목록 밖 전환을 거부하는 케이스가 단위 테스트에 있다
    - 전환 함수 본문에 상태 이름을 직접 비교하는 조건문이 없다 — 비교는 허용 목록과 게이트 규칙 안에만 있다
    - 「팀장 이상」이 계급 순위 비교가 아니라 권한표 판정으로 구현됐고, 선택과 근거가 SUMMARY에 적혔다
    - 게이트 규칙이 다섯 등록됐다(검증 명령이 개수를 센다)
    - 수주중 예외를 독립 규칙으로 둘지에 대한 선택과 근거가 SUMMARY에 적혔다
    - 고객 승인 규칙에 「수주중이면 통과」와 「설정으로 끄면 통과」 두 케이스가 단위 테스트에 있다
    - 완료 잠금 규칙에 「미수주에서는 전부 통과」 케이스가 있다 — 미수주를 잠그는 경로가 없다
    - 거래처 필수 규칙이 등록되되 이 페이즈의 어떤 함수도 그것을 호출하지 않는다(Phase 5의 몫)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 네 값·전환 네 가지·게이트 규칙 다섯이 구현되고 결정표 단위 테스트가 전부 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 차수 기계 — 전체 복사 · 현재 차수 · 고객 승인 표시 · 프로젝트 복사</name>
  <files>domain/quotes/revisions.ts, domain/quotes/lines.ts, domain/projects/index.ts, repositories/quote-revisions.ts, repositories/quote-lines.ts, app/(app)/projects/project-form.tsx, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/integration/quote-revisions.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-52~D-56·D-70 원문 — 사전 견적의 정의, 전체 복사, 최신 차수만 현재, 계보로 이어 보이기, 승인 표시와 취소 조건, 견적 번호는 파생값, 프로젝트 복사 범위
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 596–614행(S2 등록 폼 + 복사 등록) — 복사 파라미터 형태와 폼 위 한 줄의 문구
    - 같은 문서 656–695행(S5 차수 섹션 전문) — **왜 이력 목록 컴포넌트가 아니라 표인지**의 네 가지 근거까지 읽는다. 이력 목록 컴포넌트를 넓히지 않는다
    - `db/schema/quote-revisions.ts`·`db/schema/quote-lines.ts`(04-01 산출물 전문) — 차수 순번·승인 컬럼·계보 컬럼·줄 상태 컬럼
    - `repositories/quote-lines.ts`(04-01·04-04 산출물 전문) — 배치 읽기·쓰기 함수. 전체 복사가 이 위에 얹힌다
    - `domain/quotes/lines.ts`(04-01·04-02·04-04 산출물 전문) — 배치 저장과 게이트 호출 위치
    - `domain/archive/index.ts`(전문) — 보관·복원 판정. 연결 문서 없는 줄의 삭제가 이것을 쓴다
    - `domain/projects/index.ts`(04-01·04-05 산출물 전문) — `createProject`. 복사 등록이 같은 함수에 인자를 더하는 형태인지 새 함수인지 판단한다
    - `app/(app)/projects/project-form.tsx`(04-01 산출물 전문) — 복사 출처 한 줄이 들어갈 자리
    - `app/(app)/admin/vendors/page.tsx` 1–71행 — 검색 파라미터 토글 관례(복사 등록이 같은 관례를 쓴다)
    - `test/integration/quote-lines.test.ts`·`test/integration/quote-lines-conflict.test.ts`(04-01·04-04 산출물 전문) — 통합 테스트 형태
  </read_first>
  <behavior>
    - 줄 여섯인 1차에서 새 차수를 만들면 2차에 줄 여섯이 생기고 각 줄이 1차의 대응 줄을 계보로 가리킨다
    - 새 차수의 승인 표시는 꺼진 상태이고 승인일·승인자가 비어 있다
    - 새 차수를 만든 뒤 현재 차수는 2차이고 1차는 현재가 아니다
    - 줄이 0개인 차수를 만들 수 없다(빈 차수 금지)
    - 담당 PM이 승인일과 함께 승인 표시를 켜면 그 차수의 승인 컬럼이 채워지고 행동 로그가 한 줄 남는다
    - 연결 문서가 없는 차수의 승인 표시를 끄면 컬럼이 비워진다
    - 연결 문서가 있는 차수의 승인 표시를 끄려 하면 거부되고 이유가 새 차수로 고치라고 말한다
    - 견적 표시 번호가 프로젝트 번호와 차수 순번에서 만들어지고 저장 컬럼이 없다
    - 연결 문서가 없는 줄을 지우면 보관되고, 있는 줄을 지우려 하면 거부되며 '취소' 상태로만 바뀐다
    - 프로젝트를 복사하면 기본 정보 네 가지와 현재 차수 줄이 새 프로젝트의 1차로 들어오고, 기간·사전 견적·매출·연결 문서·계보는 비어 있으며 상태는 수주중이다
  </behavior>
  <action>
① `repositories/quote-revisions.ts`에 차수 목록·현재 차수·다음 순번·승인 컬럼 갱신을 더한다. `repositories/quote-lines.ts`에 **차수 단위 전체 복사**(한 트랜잭션에서 읽어 새 차수 식별자로 다시 넣으며 계보 식별자를 남긴다)를 더한다.

② `domain/quotes/revisions.ts`: `createRevisionFromCurrent(viewer, projectId)`는 현재 차수의 줄을 전체 복사해 다음 순번 차수를 만든다 — 트랜잭션 하나, 승인 표시는 꺼진 채로, 줄이 0개면 만들지 않는다. `currentRevision(projectId)`은 **최신 순번**을 현재로 돌려준다(D-54 — 승인 여부와 무관하다). `setCustomerApproval(viewer, revisionId, {approvedOn} | null)`은 담당 PM 판정 뒤 켜고, 끌 때는 **그 차수에 연결 문서가 있는지** 먼저 보고 있으면 거부한다(D-56). 결재를 거치지 않고 행동 로그만 남긴다. `quoteDisplayNumber(projectNumber, seq)`는 저장 컬럼 없는 파생 문자열을 만든다.

③ `domain/quotes/lines.ts`에 **셀 단계 판정**과 줄 삭제 경로를 더한다. 셀 단계 판정(D-66): 그 줄에 연결 문서가 있으면 금액 셀 넷(수량·단가·실행가·환율)을 읽기 전용 단계로, 항목명·비고는 편집 단계로 내보낸다. 완료(정산) 프로젝트면 전부 잠김 단계다. **이 판정은 화면 표시용이 아니라 저장 경로의 판정이기도 하다** — 읽기 전용 셀의 값이 저장 페이로드에 실려 와도 서버가 무시하지 않고 거부한다(04-04의 「조용히 버리지 않는다」와 같은 원칙). 이 페이즈에는 연결 문서가 아직 없으므로 판정 입력은 Phase 5가 채울 조회 지점 하나를 두고 지금은 빈 결과를 돌려주되, **판정 로직과 거부 경로는 지금 완성하고 단위 테스트로 고정한다**. 줄 삭제는 연결 문서가 없으면 보관함 규약(`domain/archive`)으로 보관하고, 있으면 보관을 거부하고 '취소' 상태(견적가 0)로만 바꾼다. 외래키는 04-01이 제한 동작으로 걸어 뒀으므로 **DB가 마지막 방어선**이고 domain이 먼저 이유를 말한다. 이전 차수 줄의 연결 문서를 계보를 따라 현재 차수의 대응 줄에 이어 보이는 조회도 여기 둔다 — 문서 행을 옮기지 않는다(D-55).

④ `domain/projects/index.ts`에 프로젝트 복사를 더한다. 기본 정보(클라이언트·팀·담당 PM·프로젝트명)와 현재 차수 줄만 가져오고 기간·번호·사전 견적·매출 칸·연결 문서·줄 계보는 가져오지 않는다. 새 프로젝트는 상세 1차·미승인·수주중이고 번호는 새로 매긴다. `createProject`에 복사 출처 인자를 더할지 새 함수를 둘지는 그 함수의 현재 형태를 보고 정하되 **번호 부여·차수 생성 경로를 복제하지 않는다**(같은 트랜잭션 기계를 쓴다).

⑤ `app/(app)/projects/project-form.tsx`: 복사 출처가 검색 파라미터로 오면 기본 정보와 줄이 미리 채워지고 폼 위 한 줄이 어느 프로젝트에서 몇 줄을 가져왔는지 밝힌다(S2). 등록 폼이 열려 있을 때 1차 버튼을 렌더하지 않는 규칙은 복사 등록에도 똑같이 적용된다. **별도 가져오기 모달을 만들지 않는다**(D-70).

⑥ `app/(app)/projects/actions.ts`에 차수 생성·승인 표시·줄 삭제·프로젝트 복사 액션을 더하고 `actions.registry.ts`에 전부 등록한다(D-38).

⑦ `test/integration/quote-revisions.test.ts`: 위 `behavior`의 열한 경우를 통합 테스트로 쓴다. 특히 「연결 문서가 있는 줄의 보관 거부」는 **DB를 다시 읽어** 행이 남아 있고 상태만 바뀐 것을 단언한다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/quote-lines.test.ts test/integration/quote-lines-conflict.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm vitest run --project unit</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 차수 전체 복사가 한 트랜잭션이고 복사된 줄 전부에 계보 식별자가 있다(통합 테스트가 줄 수와 계보를 단언)
    - 줄이 0개인 차수를 만들 수 없다는 케이스가 통합 테스트에 있다
    - 현재 차수 판정이 최신 순번 하나뿐이다 — 승인 여부를 보는 분기가 없다
    - 견적 표시 번호에 대응하는 저장 컬럼도 카운터도 없다
    - 승인 표시 취소가 연결 문서 유무로 갈리고, 거부 케이스가 통합 테스트에 있다
    - 연결 문서가 있는 줄의 보관 거부 뒤 DB를 다시 읽어 행이 남아 있고 상태만 바뀐 것을 단언한다
    - 셀 단계 판정(D-66)이 금액 셀 넷과 항목명·비고를 서로 다른 단계로 내보내고, 읽기 전용 셀 값을 실은 저장이 **거부**되는 케이스가 테스트에 있다 — 무시하고 통과시키는 경로가 없다
    - 연결 문서 조회 지점이 하나이고 Phase 5가 그 한 곳만 채우면 되는 형태다(호출자가 여러 곳으로 흩어지지 않았다)
    - 프로젝트 복사가 번호 부여·차수 생성 경로를 복제하지 않고 기존 트랜잭션 기계를 쓴다
    - 복사 등록에서 기간·사전 견적·매출·연결 문서·계보가 비어 있음을 단언하는 케이스가 있다
    - 가져오기 모달 컴포넌트가 만들어지지 않았다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>차수 전체 복사·현재 차수 판정·승인 표시와 취소·줄 삭제 규칙·프로젝트 복사가 구현되고 통합 테스트가 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 차수 섹션 · 상태 바꾸기 두 화면 + 생애 한 흐름 E2E</name>
  <files>app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/revision-section.tsx, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/project-detail.module.css, test/integration/project-status.test.ts, test/e2e/project-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 615–638행(S3 상세 골격) — 머리 줄 구성, 「상태 바꾸기」를 팀장 이상에게만 **보내는** 패턴, 섹션 순서, 고객 승인 3차 버튼 자리, 네 숫자 줄을 만들지 않는다는 규칙
    - 같은 문서 656–695행(S5 차수 섹션) — 열 여섯, 최신이 위, 읽기 형태 렌더, 폰 우선순위와 그 선택 이유, 이전 차수 행에만 열기 동작, 현재 차수 행의 빈 동작 칸
    - 같은 문서 720–733행(S7 상태 태그와 상태 전환) — 갈 곳 하나/둘의 분기, 막힘 표시 자리, 완료에서 버튼 자체가 없음, 이력은 로그에만
    - 같은 문서 336–345행(S5 여섯 상태 + backstop 하나 + 미해소 U-2) · 357–362행(S7 세 상태 + backstop 하나)
    - 같은 문서 509–519행(§7-8 보강 — 1차 라벨에 「취소」가 들어가면 2차는 「닫기」)
    - 같은 문서 234–287행(Copywriting Contract) — 막힘·파괴 모달 문구의 정본
    - `.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md` — Task 1 체크포인트 ⑥의 답(U-2: 최신+승인 차수 칸과 미승인 이전 차수 칸의 문자열). **이 답을 그대로 쓴다**
    - `docs/design/SYSTEM.md` §7-5(04-01이 낱말 다섯과 길이 규칙을 더한 현재 형태) · §7-8(2차 라벨 규칙 포함) · §6-2(상세 = 견적 원장) · §6-3(「바꾸기」 골격) · §7-6(토스트는 성공 전용)
    - `ui/table/Table.tsx`(04-01·04-02·04-04 산출물 전문) — 읽기 형태 렌더 파생과 열 우선순위 prop
    - `ui/status-tag/StatusTag.tsx`(전문) — 상태 태그의 변형과 prop
    - `ui/history-list/HistoryList.tsx`(전문) — **이 컴포넌트를 쓰지 않는 근거**를 S5가 네 줄로 적었다. 넓히지 않는다는 판단을 확인만 한다
    - `app/(app)/projects/[id]/page.tsx`(04-01·04-02 산출물 전문) — 머리 줄과 섹션 순서의 현재 형태
    - `ui/shell/MoreSheet.tsx`(또는 02-04 시트 컴포넌트) · `ui/table/RowSheet.tsx`(04-04 산출물) — 모달·시트 골격의 선례
    - `test/e2e/project-register.spec.ts`·`test/e2e/quote-table.spec.ts`(04-01·04-04 산출물) · `test/e2e/fixtures.ts` — 역할 픽스처와 스펙 형태
  </read_first>
  <behavior>
    - 팀장으로 상세를 열면 머리 줄에 「상태 바꾸기」가 있고, 담당 PM으로 열면 그 버튼이 DOM에 없다
    - 수주중 프로젝트에서 「상태 바꾸기」를 누르면 갈 곳이 둘이라 고르는 목록이 먼저 나온다
    - 진행 프로젝트에서 누르면 갈 곳이 하나라 바로 확인 모달이 나온다
    - 완료(정산) 프로젝트에는 「상태 바꾸기」 버튼이 없다
    - 기간 없는 수주중 프로젝트에서 진행을 고르면 확인 모달의 1차가 비활성이고 옆에 기간이 없다는 이유와 기간 칸으로 데려가는 3차가 있다
    - 차수 섹션은 최신 차수가 맨 위이고 현재 차수 행의 동작 칸이 비어 있다
    - 이전 차수 행에는 그 차수를 여는 3차 버튼이 있고 누르면 잠긴 읽기 형태로 보인다
    - 승인 표시 취소 모달의 2차 라벨이 「닫기」다(1차 라벨에 「취소」가 들어가므로)
    - 전환이 게이트에 막히면 모달이 닫히지 않고 이유가 1차 버튼 옆에 나오며 토스트가 뜨지 않는다
  </behavior>
  <action>
① `app/(app)/projects/[id]/revision-section.tsx`: `ui/table`을 읽기 형태로 쓴다 — 편집 가능한 셀이 구조적으로 0이므로 컴포넌트가 자동으로 캡션 있는 표 형태를 고른다(모드 prop을 만들지 않는다). 열은 차수·생성일·줄 수·견적 합계·상태·동작 여섯, 최신이 위. 폰 1순위는 차수·견적 합계·동작이고 접힌 줄은 상태·생성일·줄 수. **현재 차수 행의 동작 칸을 `—`로 채우지 않는다.** 상태 열의 두 낱말과 U-2의 두 경계 칸 값은 04-01 체크포인트 ⑥의 답을 그대로 쓴다. **이력 목록 컴포넌트를 쓰지 않는다** — S5가 네 가지 불일치를 이미 적었다.

② `app/(app)/projects/[id]/status-change.tsx`: 「상태 바꾸기」는 **팀장 이상에게만 서버가 보낸다**(비활성 버튼이 아니다). 갈 수 있는 상태가 하나면 바로 확인 모달, 둘이면 고르는 목록(PC 모달 / 폰 시트) 뒤 확인. 모달 제목은 실제 동작이고, 1차가 막히면 비활성 + 왼쪽에 **게이트가 돌려준 이유 문자열 그대로** + 기간 칸으로 데려가는 3차. 1차 라벨에 「취소」가 들어가는 모달(승인 표시 취소 · 견적 줄 취소)의 2차는 「닫기」다(§7-8 보강). 실패에 토스트를 띄우지 않는다.

③ `app/(app)/projects/[id]/page.tsx`: 머리 줄 부제에 프로젝트 번호·현재 차수·고객 승인일·마지막 상태 변경일을 넣고 상태 태그를 붙인다. 고객 승인 3차 버튼을 부제 옆에 둔다(승인 전에는 담당 PM에게만 켜기 버튼, 승인 후에는 연결 문서가 없을 때만 취소 버튼). 차수 섹션을 §6-2 순서 ③자리에 둔다(① 견적 줄 → ② 매출 → ③ 차수). **네 숫자 줄은 여전히 만들지 않는다.** **이 태스크가 머리 줄에 마지막 버튼(②의 「상태 바꾸기」)을 더해 S3의 버튼 넷을 완성하므로, U-1의 계획 단계 판단(`04-01-PLAN.md` Task 2 ⑪ — 머리 줄은 줄바꿈하고 버튼군은 접지도 숨기지도 않으며 이 줄에도 가로 스크롤 0을 건다)이 네 버튼 상태에서 실제로 성립하는지 여기서 확인한다** — 375px에서 가로 스크롤이 생기면 04-01이 만든 머리 줄 CSS를 고치고(라벨을 줄이거나 버튼을 숨기지 않는다) 그 사실을 SUMMARY에 남긴다. 부제는 이 태스크가 네 항목으로 채우는 자리이기도 하다. 마지막 상태 변경일은 행동 로그에서 파생한다 — 새 표·새 섹션을 만들지 않는다(D-50).

④ `test/integration/project-status.test.ts`: 전환 네 가지가 실제 DB에서 상태를 바꾸고 행동 로그 한 줄을 남기는 것, 거부된 전환이 상태를 바꾸지 않고 로그도 0줄인 것, 미수주 프로젝트에서 줄 편집이 여전히 통과하는 것, 완료 프로젝트에서 경영관리의 견적 외 비용 줄 추가만 통과하는 것을 단언한다.

⑤ `test/e2e/project-lifecycle.spec.ts`: 팀장 역할로 수주중 프로젝트를 열어 (a) 기간 없이 진행 전환을 시도해 막힘 이유를 확인, (b) 기간을 채우고 진행으로 전환, (c) 새 차수를 만들어 줄이 복사된 것을 확인, (d) 담당 PM 역할로 고객 승인 표시를 켜기, (e) 완료(정산)로 전환한 뒤 견적 줄 편집이 막히는 것을 확인, (f) 경영관리 역할로 같은 프로젝트에서 견적 외 비용 줄이 추가되는 것을 확인. 담당 PM으로 열었을 때 「상태 바꾸기」가 DOM에 없는 것도 같은 스펙에서 단언한다. 마지막에 **375px 뷰포트 케이스**를 하나 더해 버튼 넷이 다 보이는 팀장 시점에서 문서 가로 스크롤이 0이고 머리 줄의 버튼 넷이 전부 DOM에 있음을 단언한다 — 04-01 E2E의 같은 케이스를 버튼이 다 모인 최종 상태에서 다시 도는 것이고, 이것이 U-1 판단의 최종 증명이다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다. stylelint가 색·서체·radius 리터럴을 잡으면 토큰 밖 값을 쓴 것이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/quote-revisions.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/project-lifecycle.spec.ts test/e2e/quote-table.spec.ts test/e2e/project-register.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다. 앞 두 스펙이 깨지면 차수·상태 섹션 추가가 상세 화면의 1차 버튼 계약이나 표 편집 경로를 망가뜨린 것이다</fails_when>
    <automated>git diff --stat ui/history-list</automated>
    <fails_when>출력이 비어 있지 않다 — 이력 목록 컴포넌트는 이 페이즈에서 손대지 않는다(S5의 네 가지 불일치 근거)</fails_when>
    <automated>pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 차수 섹션이 모드 prop 없이 읽기 형태로 렌더되고 캡션이 있다
    - 현재 차수 행의 동작 칸이 비어 있고 `—`로 채워지지 않았다
    - U-2의 두 경계 칸 값이 04-01 체크포인트 ⑥의 답과 글자 그대로 같다
    - 「상태 바꾸기」가 권한 없는 사람에게 **서버에서 오지 않는다** — 비활성 렌더가 아니다(E2E가 DOM 부재를 단언)
    - 막힘 이유가 게이트 응답 문자열 그대로다 — 화면 코드에 이유 문자열 리터럴이 없다
    - 1차 라벨에 「취소」가 들어가는 모달의 2차 라벨이 「닫기」다
    - 전환 실패 경로에 토스트 호출이 없다
    - 상세에 상태 변경 이력 표·섹션이 없다 — 현재 상태와 마지막 변경일만 있다
    - 네 숫자 줄 마크업이 여전히 없다
    - 버튼 넷이 다 모인 머리 줄이 375px에서 가로 스크롤을 만들지 않고 버튼이 하나도 숨거나 접히지 않는다(U-1 판단이 최종 상태에서 성립한다) — `project-lifecycle.spec.ts`의 375px 케이스가 가로 스크롤 0과 버튼 넷의 DOM 존재를 함께 단언한다
    - `ui/history-list` diff가 비어 있다
    - `pnpm test` 세 계층이 전부 녹색이다
  </acceptance_criteria>
  <done>차수 섹션과 상태 바꾸기가 렌더되고, 수주중 → 진행 → 완료 생애 한 흐름과 완료 뒤 잠금·경영관리 예외가 E2E로 증명되며 `pnpm test` 세 계층이 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 상태 전환 액션 | 전환 대상 상태가 넘어온다. 허용 목록과 주체 판정이 서버에만 있어야 한다 |
| 브라우저 → 승인 표시 액션 | 승인일과 차수 식별자가 넘어온다. 담당 PM 판정과 연결 문서 검사가 서버에만 있어야 한다 |
| domain → 게이트 | 이후 페이즈(증빙 필수·마감·알림·이전 행 면제)가 얹힐 단일 지점이다. 판정이 흩어지면 그 페이즈들이 각자 다시 구현한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-34 | Elevation of Privilege | `domain/projects/status.ts` | high | mitigate | 전환 주체 판정을 권한표로 하고, 버튼을 보내지 않는 것과 별개로 서버가 액션에서 다시 판정한다. 담당 PM이 액션을 직접 호출하는 케이스를 통합 테스트로 고정한다 |
| T-04-35 | Elevation of Privilege | 완료 잠금 예외(경영관리 견적 외 비용) | high | mitigate | 예외도 같은 게이트 규칙 안에서 판정하고, 예외를 타는 주체·줄 종류 둘 다 서버가 확인한다. PM이 같은 경로로 줄을 추가하는 케이스를 거부 테스트로 고정한다 |
| T-04-36 | Tampering | 승인 표시 취소 | high | mitigate | 연결 문서 유무를 서버가 조회해 판정한다. 클라이언트가 보낸 「문서 없음」 주장을 믿지 않는다 |
| T-04-37 | Tampering | 차수 전체 복사 | medium | mitigate | 복사가 한 트랜잭션이라 절반만 복사된 차수가 남지 않는다. 줄 0개 차수를 만들 수 없다는 규칙이 domain에 있다 |
| T-04-38 | Repudiation | 상태 변경 이력 | medium | mitigate | 상태 변경 종류가 끌 수 없는 종류라 설정으로 이력을 없앨 수 없다(04-01 체크포인트 ③). 거부된 전환은 로그를 남기지 않아 잡음이 되지 않는다 |
| T-04-39 | Information Disclosure | 상태 변경 로그의 활용 | medium | mitigate | 로그를 사람별로 집계해 보여주는 화면·내보내기를 만들지 않는다(위 금지 항목). 상세에는 마지막 변경일만 파생한다 |
| T-04-40 | Denial of Service | 차수 전체 복사 | low | accept | 줄 수십 개 규모라 한 트랜잭션 복사의 비용이 낮다. 인트라넷 실측 최대가 프로젝트당 수십 줄이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm test` 세 계층 전부 통과
- 단위 2개 · 통합 2개 · E2E 1개가 존재하고 녹색
- `domain/rules/register.ts`에 규칙 다섯이 등록돼 있다
- `ui/history-list` diff가 비어 있다
- `git diff --stat package.json pnpm-lock.yaml`이 비어 있다
</verification>

<success_criteria>
- 상태 전환 네 가지가 전부 게이트 단일 진입점을 지나고 잠기는 상태는 완료(정산) 하나뿐이다
- 승인 전 차수의 지출 관련 동작이 게이트로 막히고 수주중에는 막히지 않는다
- 새 차수가 전체 복사로 만들어지고 최신 차수만 현재다
- 프로젝트 복사가 D-70의 범위대로 동작하고 가져오기 모달이 없다
- Phase 5·6·7·8이 증빙 필수·마감·알림·이전 행 면제를 이 게이트에 얹기만 하면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 상수 | `PROJECT_STATUSES` · `ALLOWED_TRANSITIONS` |
| 함수(domain) | `changeProjectStatus` · `createRevisionFromCurrent` · `currentRevision` · `setCustomerApproval` · `quoteDisplayNumber` · 프로젝트 복사 함수 · 줄 삭제·취소 함수 · 계보 기반 연결 문서 조회 |
| 게이트 규칙 | 고객 승인 · 완료 잠금(완성) · 수주중 예외 · 기간 필수 · 거래처 필수 |
| 설정 키 | 고객 승인 게이트 on/off(단순값, 기본 켬) |
| 권한 동작 | 프로젝트 상태 변경 판정(권한표 기반 — 표현 방식은 SUMMARY에 기록) |
| 컴포넌트 | `app/(app)/projects/[id]/revision-section.tsx` · `app/(app)/projects/[id]/status-change.tsx` |
| Server Action | 상태 전환 · 차수 생성 · 고객 승인 표시·취소 · 줄 삭제·취소 · 프로젝트 복사 |
| 타입 | `ProjectStatus` · `StatusTransition` · `RevisionDto`(확장) |
| 테스트 | `test/unit/domain/project-status.test.ts` · `test/integration/{quote-revisions,project-status}.test.ts` · `test/e2e/project-lifecycle.spec.ts` |
| 변경된 기존 심볼 | `domain/rules/register.ts`(규칙 1 → 5) · `domain/projects/index.ts`(복사) · `domain/quotes/lines.ts`(삭제·취소·계보 조회) · `repositories/quote-revisions.ts`·`repositories/quote-lines.ts`(차수 복사) · `app/(app)/projects/[id]/page.tsx`(차수 섹션·승인 버튼·마지막 변경일) · `app/(app)/projects/project-form.tsx`(복사 등록) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md` when done
</output>
