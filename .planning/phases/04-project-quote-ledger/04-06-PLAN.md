---
phase: 04-project-quote-ledger
plan: 06
type: execute
wave: 6
depends_on: ["04-01", "04-10", "04-28"]
files_modified:

  - db/migrations/0012_project_status_five_values.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0012_snapshot.json
  - db/schema/projects.ts
  - domain/seed/index.ts
  - domain/projects/status-transitions.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - app/(app)/projects/[id]/page.tsx
  - test/unit/domain/rules-gate.test.ts
  - test/integration/quote-lines.test.ts
  - test/integration/project-status.test.ts
  - test/integration/migration-upgrade.test.ts

autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "프로젝트 상태 값은 수주중(`bidding`) · 진행(`in_progress`) · 정산(`settling`) · 완료(`completed`) · 미수주(`lost`) 다섯뿐이다 — 코드표 `project_status`와 `projects.status` 어디에도 옛 네 상태 모델의 잠금 값이 남지 않는다(D-75)"
    - "옛 잠금 상태 행은 새 마이그레이션이 `completed`로 옮겼고 0009는 한 글자도 바뀌지 않았다 — 코드표나 프로젝트 행에 알려진 네 값 밖의 상태가 있으면 마이그레이션이 지우지 않고 RAISE EXCEPTION으로 전체를 되돌린다(D-75, RESEARCH Pitfall 6 — 0009의 「표가 비어 있다」 전제를 쓰지 않는다)"
    - "새 마이그레이션의 번호는 적용 직전 journal 최고 번호 + 1(04-10의 0011 뒤)이고 파일명·snapshot·journal tag가 생성기 출력과 같다 — 계획 번호 0012와 다르면 실제 이름이 SUMMARY에 있다"
    - "코드표 `project_status` 다섯 값의 라벨·정렬·설명이 시드 상수와 글자 그대로 같고, 정산의 설명은 그 단계의 뜻 `행사 종료 · 발행 요청과 증빙 첨부를 마치는 단계`를 말한다 — 발행 요청 흐름·지출결의 마감 자체는 Phase 5·6이고 이 플랜은 상태 값만 만든다(D-77·D-93)"
    - "상태 다섯 값과 사람의 전환 넷(수주중→진행 · 수주중→미수주 · 미수주→진행 · 정산→완료)과 각 전환의 권한표 메뉴가 import 없는 데이터 모듈 한 곳에 있다 — 게이트 규칙과 전환 함수(04-20)가 이 파일을 같이 읽고 서로 import하지 않는다(ROADMAP 기준 4)"
    - "완료 프로젝트의 견적 줄 저장은 새 게이트 규칙 `project.line-edit`가 `완료 · 견적 줄 잠김`으로 거부하고, 옛 이진 규칙 `project.completed-lock`은 등록 목록에서 사라졌다 — 저장 경로와 상세의 편집 가능 판정이 같은 새 규칙을 부른다(D-47, RESEARCH Pitfall 5 — 교체이지 조건 추가가 아니다. 정산의 「실행가와 새 줄만」은 04-12가 같은 규칙을 넓힌다. 문구는 UI-SPEC rev 5 Copywriting `Error — 셀(잠김) = 표 위 한 줄`의 완료 문자열 그대로)"
    - "미수주 프로젝트의 줄 편집·저장은 막히지 않고 화면에 자물쇠·회색 처리가 없다(D-45)"
    - "옛 데이터가 있는 DB에서도 마이그레이션이 맞다 — 0010까지만 적용한 새 DB에 옛 잠금 값·진행·수주중·미수주 프로젝트와 옛 코드표 행을 넣고 나머지 마이그레이션을 적용하면, **시드를 다시 돌리기 전에** 옛 잠금 행이 `completed`가 되고 다른 행의 상태·번호·줄은 그대로이며 코드표가 다섯 값이다. 알려진 네 값 밖의 상태가 있는 DB에서는 마이그레이션이 RAISE로 멈추고 아무것도 바뀌지 않는다(CEO 리뷰 OV-6 — 시드만 검증하던 테스트의 빈틈)"
  prohibitions:
    - statement: "완료 외의 상태가 사람의 실제 지출을 막는 잠금이 되어서는 안 된다. 미수주로 닫힌 프로젝트에 뒤늦게 도착하는 청구서(PT 제작비 등)는 실제로 일어나는 일이고, 규칙이 그것을 못 넣게 만들면 사람은 시스템 밖에서 처리한다 — 편의를 위해 미수주를 잠그는 구현은 금지한다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/0012_project_status_five_values.sql"
      provides: "상태 다섯 값 — 옛 잠금 값 재매핑(UPDATE) · 코드표 교체(설명 포함) · 전제 가드(네 값 밖이면 RAISE)"
      contains: "RAISE EXCEPTION"
    - path: "domain/projects/status-transitions.ts"
      provides: "상태 다섯 값과 사람의 전환 넷 + 각 전환의 권한표 메뉴 — 데이터만(게이트 규칙과 전환 함수가 같이 읽는다, import 순환 방지)"
      exports: ["PROJECT_STATUSES", "ALLOWED_TRANSITIONS", "ProjectStatus"]
    - path: "test/integration/project-status.test.ts"
      provides: "마이그레이션 뒤 코드표·행 상태 · 미수주 저장 통과 · 완료 저장 거부 증명(04-20·04-21이 같은 파일에 전환 케이스를 더한다)"
      min_lines: 30
    - path: "test/integration/migration-upgrade.test.ts"
      provides: "0010 상태의 옛 데이터 위에 남은 마이그레이션을 적용해 재시드 전에 검사 — 재매핑·보존·가드(계약 칸 삭제 플랜 04-41 — 그룹 B 담당 — 이 삭제 가드 케이스를 더한다)"
      min_lines: 40
  key_links:
    - from: "domain/quotes/lines.ts"
      to: "domain/rules/register.ts"
      via: "견적 줄 저장 게이트가 옛 이진 규칙 대신 `project.line-edit`를 부른다"
      pattern: "project.line-edit"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/rules/register.ts"
      via: "상세의 편집 가능 판정이 저장과 같은 새 규칙을 부른다 — 화면과 서버의 잠금이 한 규칙이다"
      pattern: "project.line-edit"
    - from: "domain/seed/index.ts"
      to: "db/migrations/0012_project_status_five_values.sql"
      via: "새 DB(시드)와 기존 DB(마이그레이션)의 상태 라벨·정렬·설명이 글자 그대로 같다"
      pattern: "settling"
---

<objective>
**As a** 기획팀장·대표·경영관리, **I want to** 프로젝트 상태가 수주중 · 진행 · 정산 · 완료 · 미수주 다섯으로 DB·코드표·시드에 자리 잡고, 완료 프로젝트의 견적 줄이 새 규칙으로 끊김 없이 잠기기를, **so that** 사람의 전환(04-20)·자동 정산(04-11)·정산 편집 범위(04-12)가 같은 다섯 값 위에 얹히고 Phase 9·10의 손익이 이 값을 전제할 수 있다.

**2026-09-23 재계획(D-75~D-82) · 체커 1차 분할.** 옛 04-06은 네 상태 모델(D-41) 위에 상태 전환 · 차수 기계 · 고객 승인 · 프로젝트 복사 · 게이트 규칙 다섯을 한 플랜에 담았고, 다섯 상태 재계획 뒤에도 28파일로 한 실행자 컨텍스트를 넘었다. 그래서 상태 쪽을 셋으로 나눴다:

- **04-06(이 플랜)** — 상태 다섯 값 데이터: 마이그레이션 · 코드표·시드 상수 · 전이표 데이터 모듈 · 완료 잠금 게이트 교체
- **04-20** — 사람의 전환 넷(서버): 권한표 메뉴 둘 · 전환 게이트 둘 · 전환 함수 · 갈 곳 목록 · 액션
- **04-21** — 화면: 상태 바꾸기 · 부제의 마지막 변경일 · 목록의 다섯 상태 표시 · 생애 E2E
- 앞 재계획에서 이미 나눈 것: 자동 정산·종료일 지남(D-76·D-81) → **04-11** · 기간 칸·정산 되돌리기(D-80·S13) → **04-22** · 정산 셀 범위·줄 삭제(D-78·D-56) → **04-12** · 300줄 상한(D-86) → **04-26** · 조정·견적 외 비용 줄(D-83·D-48) → **04-13·04-23** · 차수·고객 승인(D-43·D-53~D-56) → **04-14·04-24** · 프로젝트 복사(D-70) → **04-15**

옛 이진 게이트 `project.completed-lock`은 이 플랜이 **교체**한다(RESEARCH Pitfall 5) — 마이그레이션이 옛 잠금 값을 `completed`로 옮기는 순간 옛 규칙은 아무것도 잠그지 않게 되므로, 같은 플랜에서 새 규칙 `project.line-edit`로 바꿔 완료 잠금이 끊기는 구간을 만들지 않는다. 정산의 셀 단위 범위는 04-12가 같은 규칙을 넓힌다.

Purpose: D-75는 costly 결정이다 — Phase 5·9·10의 게이트·손익이 다섯 값을 전제한다. 값 · 재매핑 · 잠금 규칙이 먼저 한 번에 서야 뒤 플랜들이 같은 데이터 위에서 돈다.

Output: 마이그레이션 0012(상태 다섯 값 — 번호는 직전 최고 + 1) · 시드 상태 상수 · 전이표 데이터 모듈 · 게이트 규칙 `project.line-edit`(옛 이진 규칙 교체) · 단위 1 갱신 · 통합 2(신규 1 · 갱신 1).

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE).** (1) **04-04 분리(사용자 D21·OV-7):** 이 플랜은 견적 표 파일을 고치지 않으므로 04-04의 사람 확인·04-08의 견적 표 쪽·04-09를 기다리지 않는다 — `depends_on`을 04-01·04-10(설명 컬럼과 마이그레이션 번호의 앞)으로 줄였다. `domain/quotes/lines.ts`를 함께 고치는 04-09는 이 플랜 뒤에 선다. (2) **OV-6:** 기존 통합 테스트는 재시드된 빈 DB만 보았다 — 0010 상태의 옛 데이터 위에 남은 마이그레이션을 적용하고 **재시드 전에** 결과를 보는 업그레이드 테스트를 더한다. (3) **A-28:** 0012는 되돌리는 마이그레이션이 없다(전진 전용) — 코드 롤백 때의 손실 있는 역 SQL과 「Phase 4는 한 PR로 배포」 전제를 SUMMARY에 적는다(릴리스 제약과 ARCHITECTURE §5 예외의 정식 기록은 그룹 B의 계약 칸 삭제 플랜 04-41 — 04-16에서 나뉨). (4) 사용자 D19-7 — 옛 잠금 값을 `completed`로 옮기는 A7 가정이 확정됐다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10, `docs/designs/plant8-erp-phase4-eng-review-260923.md`):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-01-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-10-SUMMARY.md
@CLAUDE.md

# 범위 Read(큰 파일):

# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 677–722행(§1 D-75 — 읽는 코드 경로 전부 · 마이그레이션 전략 · 가드 형태 · 재매핑 방향) · 843–855행(Pitfall 5·6)

# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 455–463행(게이트 교체) · 480–482행(가드 패턴 재사용 주의)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(상태 값과 완료 잠금)** 다. 프로브는 REQUIREMENTS.md의 옛 네 상태 문구로 돌았으므로(04-43이 문구를 고친다) **수용 기준은 결정된 모델 D-75·D-45·D-47·D-77로 끌어와** `must_haves.truths`에 적었다. S3·S7의 화면 상태와 backstop 둘(long-text · 375 머리 줄)은 화면을 만드는 04-21이, 전환의 서버 계약은 04-20이 옮겼다 — 조용히 버린 항목은 없다.

**계획 단계 가정·사용자 결정(표면화):**

- **코드 값 문자열(D-75 계획 재량 — 사용자 D19-7 확정):** 정산 = `settling`, 완료 = `completed`. 옛 잠금 값은 **`completed`로 재매핑**한다(RESEARCH A7 — 옛 값의 뜻이 「잠김」이라 완료에 가깝고, 현재 행은 전부 `source='demo'`라 업무 의미를 보존할 필요가 없다). 옛 코드표 행은 재매핑 뒤 **지운다**(비활성으로 남기면 관리 화면에 쓰이지 않는 `완료(정산)`이 계속 보인다).
- **전이표의 모양:** 사람의 전환 넷과 권한표 메뉴(`projects.status` · `projects.complete`)를 이 플랜이 데이터로 적는다. 메뉴 레지스트리 등록·시드·게이트 판정은 04-20이다 — 이 플랜의 전이표는 메뉴 키 문자열만 갖는다. 진행에서 사람이 가는 곳이 없다는 ⚠ S7 기본값은 **사용자 D13 「없음 유지」**로 확정됐다.
- **목록의 옛 라벨 한 줄:** `app/(app)/projects/page.tsx`의 상태 필터 상수와 `projects-table.tsx`의 로컬 라벨은 04-21이 코드표로 바꾼다. 04-06과 04-21 사이 몇 웨이브 동안 목록 상태 필터에 옛 선택지가 남는다(결과 0건일 뿐 오류는 없다) — 코드 리터럴 스캔은 04-21이 건다. 상세 페이지(`[id]/page.tsx` 18–23행)에도 옛 값이 있다는 것을 확인했다(CEO 리뷰 A-40 — 04-21의 스캔이 잡는다).
- **재매핑된 demo 행의 이력(A-38):** 옮겨진 행은 `status_change` 로그가 없어 완료 부제가 등록일을 보인다. demo 데이터뿐이라 받아들인다.
- **롤백 자세(A-28):** 0012는 전진 전용이다. Cloud Run 트래픽 롤백은 DB를 되돌리지 않으므로, 옛 리비전(`status === "settled"` 게이트)이 다섯 값 DB 위에서 돌면 완료 행이 잠기지 않는다. 이 위험은 「Phase 4는 한 PR로 배포하고 중간 커밋을 배포하지 않는다」(사용자 D9 전제 — 그룹 B의 04-41이 기록) 아래에서만 무해하다. 04-01·02·05가 스테이징에 배포됐는지는 모른다 — 배포 담당은 서비스 중인 `APP_GIT_SHA`를 확인한다. 불가피한 코드 롤백용 **손실 있는 역 SQL**(`UPDATE projects SET status='settled' WHERE status IN ('settling','completed')` + 코드표 되돌리기 + `lost` 정렬 3)을 SUMMARY에 적는다.
- **업그레이드 테스트 DB(OV-6):** 통합 테스트 DB(`erp_test`)는 전역 설정이 이미 최신까지 올리므로, 이 테스트는 이름에 난수를 붙인 **임시 DB**를 만들어(로컬 `erp` 계정은 `CREATE DATABASE` 권한이 있다 — `scripts/reset-test-db.sh` 선례) 0010까지만 적용하고 옛 데이터를 넣은 뒤 전체 폴더를 적용하고 끝에 DROP한다. 0010까지만 적용하는 방법은 임시 폴더에 앞 11개 SQL과 11항목으로 자른 `meta/_journal.json`을 복사해 `drizzle-orm` 마이그레이터를 부르는 것이다(전체 폴더를 다시 적용하면 마이그레이터가 이미 적용한 항목을 건너뛴다).

</probe_fallback>

<reversibility_notes>

- D-75 다섯 상태 모델 — **costly**(CONTEXT 기록 그대로): Phase 5·9·10의 게이트·손익이 이 값들을 전제한다. 사용자가 이미 결정했으므로 체크포인트를 두지 않는다.
- 코드 값 문자열 `settling`·`completed` — **costly**: 뒤 페이즈 코드가 참조하기 시작하면 이름을 바꾸는 데 데이터 UPDATE와 참조 전부가 따라온다. 지금 데이터는 전부 demo라 one-way는 아니다.

</reversibility_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 옛 잠금 값이 `completed`로 옮겨지고 완료 프로젝트의 견적 줄 저장이 새 규칙 `project.line-edit`로 잠긴다(마이그레이션 → 코드표·시드 → 전이표 데이터 → 게이트 → 저장·상세 판정 → 통합)</name>
  <files>db/migrations/0012_project_status_five_values.sql, db/migrations/meta/_journal.json, db/migrations/meta/0012_snapshot.json, db/schema/projects.ts, domain/seed/index.ts, domain/projects/status-transitions.ts, domain/rules/register.ts, domain/quotes/lines.ts, app/(app)/projects/[id]/page.tsx, test/integration/quote-lines.test.ts</files>
  <reversibility rating="costly">다섯 상태 값과 코드 문자열(settling·completed)은 Phase 5·9·10의 게이트·손익이 전제한다(D-75 costly) — 바꾸려면 데이터 재매핑과 참조 전부를 고쳐야 한다. 결정은 사용자가 했고 문자열만 계획 재량이다.</reversibility>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-45·D-47·D-75·D-77 원문
    - `db/migrations/0009_project_quote_ledger_spine.sql` 88–125행(가드 `DO $$ … RAISE EXCEPTION` 형태와 코드표 교체 문장) — **「projects 표가 비어 있다」 전제는 가져오지 않는다**(Pitfall 6)
    - `db/migrations/0011_code_item_descriptions.sql`(04-10 산출물 — 실제 번호는 04-10 SUMMARY) — 직전 마이그레이션 문체와 `description` 컬럼
    - `db/migrations/meta/_journal.json` — 마지막 항목의 `idx`·`tag`(이 플랜의 번호 = 그 `idx` + 1)
    - `domain/seed/index.ts` 20–30행(상태 상수)
    - `domain/rules/gate.ts`·`domain/rules/register.ts`(전문) — 등록 형태와 교체할 옛 규칙
    - `domain/quotes/lines.ts` 354–375행(저장 게이트 호출 지점)
    - `app/(app)/projects/[id]/page.tsx` 40–50행(편집 가능 판정의 옛 규칙 호출)
    - `test/integration/quote-lines.test.ts` 100–120행(옛 잠금 값을 DB에 직접 넣는 픽스처)
  </read_first>
  <action>
① **[BLOCKING] 마이그레이션.** 데이터만 바꾸는 마이그레이션이라 `pnpm db:generate --custom --name project_status_five_values`로 빈 파일을 만든다. **번호 규칙:** 계획 번호는 0012이고, 생성기가 매기는 번호는 적용 직전 journal의 최고 `idx` + 1이어야 한다(04-10의 0011 뒤). 생성기가 다른 번호를 주면 파일명·snapshot·journal tag를 생성기 출력 그대로 두고(손으로 번호를 바꾸지 않는다) 실제 이름을 SUMMARY에 적는다 — 아래 번호 검증 명령이 journal 연속성·파일 수·snapshot 존재를 본다. 내용: 락 타임아웃 한 쌍 → `DO $$` 가드 두 개(`code_items`의 `project_status`에 알려진 네 값 `bidding`·`in_progress`·`settled`·`lost` 밖의 값이 있으면, 또는 `projects.status`에 그 네 값 밖의 값이 있으면 RAISE EXCEPTION — 이미 다섯 값으로 옮겨졌거나 손으로 더한 값일 수 있어 지우지 않고 중단) → `UPDATE projects SET status='completed', version=version+1, updated_at=now() WHERE status='settled'` → 옛 코드표 행 `settled` DELETE → `lost`의 `sort_order`를 4로 → `settling`(라벨 `정산`, sort 2, 설명 `행사 종료 · 발행 요청과 증빙 첨부를 마치는 단계` — D-77)·`completed`(라벨 `완료`, sort 3, 설명 `정산 마감 · 견적 줄이 잠기고 되돌리기 없음`) INSERT. **0009 파일은 열지만 고치지 않는다.** `pnpm lint:sql`(`Found 0 issues`) → `bash scripts/dev-db.sh && pnpm db:migrate`. `db/schema/projects.ts`의 상태 주석을 다섯 값으로 고친다(컬럼 정의는 그대로 `text`, 기본값 `bidding`).

② **시드 상수.** `domain/seed/index.ts`의 상태 상수를 다섯 값(라벨·정렬·설명이 마이그레이션과 글자 그대로 같게 — 04-10이 넣은 수주중·진행·미수주 설명은 그대로)으로 바꾼다. 권한 시드는 건드리지 않는다(04-20).

③ **전이표 데이터 모듈.** `domain/projects/status-transitions.ts`(데이터만, import 없음): `PROJECT_STATUSES = ["bidding","in_progress","settling","completed","lost"] as const`, `ProjectStatus` 타입, `ALLOWED_TRANSITIONS: { from; to; menu: "projects.status" | "projects.complete" }[]` = 수주중→진행 · 수주중→미수주 · 미수주→진행(셋 다 `projects.status`) · 정산→완료(`projects.complete`). 04-20의 게이트 규칙과 전환 함수가 이 파일 하나를 같이 읽는다 — 그 둘이 서로 import하는 순환을 만들지 않기 위한 자리다(`test/unit/import-cycles.test.ts`).

④ **게이트 교체.** `domain/rules/register.ts`에 `project.line-edit`(ctx `{ status }`)를 등록한다 — 완료면 `완료 · 견적 줄 잠김`, 나머지 상태(수주중·진행·정산·미수주)는 통과(정산의 「실행가와 새 줄만」은 04-12가 이 규칙의 ctx를 넓혀 판정한다 — 셀 이유 문자열은 04-12의 `quoteLockReason` 한 함수로 옮겨 간다). `domain/quotes/lines.ts`의 저장 게이트 호출과 `[id]/page.tsx`의 편집 가능 판정이 새 규칙 이름을 부르게 바꾼다. 옛 완료 잠금 이진 규칙(지금 `register.ts`에 등록된 유일한 프로젝트 규칙)의 등록은 이 태스크에서 **호출자 없이 남겨 둔다** — 그 규칙을 직접 단언하는 단위 테스트가 Task 2에서 새 규칙으로 바뀔 때 함께 지운다(태스크 커밋마다 테스트가 초록, 플랜이 끝나면 옛 규칙은 없다).

⑤ **통합(트레이서 증명).** `test/integration/quote-lines.test.ts`의 옛 잠금 값 픽스처를 `completed`로 바꾸고 거부 문구 단언을 `완료 · 견적 줄 잠김`으로 맞춘다. 같은 describe에 미수주 프로젝트의 견적 줄 저장이 통과하는 케이스를 하나 더한다(D-45).
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- db/migrations/0009_project_quote_ledger_spine.sql</automated>
    <fails_when>db:migrate가 0이 아닌 코드로 끝나거나(`error:` 줄), 마지막 diff 출력이 비어 있지 않다 — 0009를 고쳐 썼다</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_project_status_five_values'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm vitest run --project unit test/unit/import-cycles.test.ts</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오거나 import 순환 테스트의 failed 개수가 1 이상이다</fails_when>
    <automated>pnpm vitest run --project integration test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 완료 프로젝트 저장이 새 규칙 문구로 거부되지 않았거나 미수주 저장이 막혔다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 락 타임아웃 한 쌍 · 가드 두 개(코드표·프로젝트 행) · 재매핑 UPDATE · 옛 코드표 행 DELETE · 새 두 값 INSERT(설명 포함)를 담고, 0009 diff가 0줄이다
    - 새 마이그레이션의 번호 = 이 플랜 실행 직전 journal 최고 `idx` + 1이고, 파일명(확장자 제외)·`meta/{번호}_snapshot.json`·journal tag가 생성기가 실제로 만든 이름과 같다(번호 검증 명령) — 계획 번호 0012와 다르면 실제 이름이 SUMMARY에 있다
    - 시드 상태 상수의 라벨·정렬·설명이 마이그레이션과 글자 그대로 같다
    - `status-transitions.ts`가 import 없는 데이터 모듈이고 `ALLOWED_TRANSITIONS`가 네 쌍과 메뉴 키를 갖는다
    - `lines.ts`의 저장 게이트와 `[id]/page.tsx`의 편집 가능 판정이 `project.line-edit`를 부르고 옛 규칙 이름을 부르는 호출자가 없다
    - 통합 테스트가 완료 저장 거부(새 문구)와 미수주 저장 통과를 둘 다 단언한다
    - 다섯 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 다섯 값이 DB·시드·코드표에 들어가고 옛 잠금 행이 완료로 옮겨졌으며, 완료 프로젝트의 견적 줄 저장과 상세 편집 판정이 새 규칙 하나로 잠긴다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 옛 이진 규칙 삭제 + 규칙 회귀 정리 + 마이그레이션 결과 통합 증명 + 옛 데이터 업그레이드 테스트(OV-6)</name>
  <files>domain/rules/register.ts, test/unit/domain/rules-gate.test.ts, test/integration/project-status.test.ts, test/integration/migration-upgrade.test.ts</files>
  <read_first>
    - `test/unit/domain/rules-gate.test.ts`(전문) — 옛 규칙 이름과 옛 잠금 값을 쓰는 케이스(33·45행 근처)
    - `test/integration/quote-lines.test.ts`(Task 1 산출물) — 프로젝트·줄 준비 형태
    - `test/integration/leak-scan.test.ts`(전문) — 새 모듈이 커버리지 목록에 필요한지 확인만
    - `test/integration/global-setup.ts`(전문) — `drizzle-orm/node-postgres/migrator`의 `migrate()`를 폴더 경로로 부르는 형태(업그레이드 테스트가 같은 함수를 임시 폴더로 부른다)
    - `scripts/reset-test-db.sh`(전문) — `erp` 계정으로 DB를 만들고 지우는 선례
    - `db/migrations/0009_project_quote_ledger_spine.sql` 88–125행 · `db/migrations/0010_revenue_entries.sql`(전문) — 0010 상태의 `projects`·`quote_revisions`·`quote_lines`·`code_items` 컬럼(옛 데이터를 넣을 때 필요한 NOT NULL 칸)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 43행(OV-6)
  </read_first>
  <behavior>
    - `listGateRules()`에 `project.line-edit`가 있고 `project.completed-lock`이 없다
    - `project.line-edit`: 완료 → `완료 · 견적 줄 잠김` · 정산·미수주·수주중·진행 → 통과
    - 마이그레이션 뒤 코드표 `project_status`가 정확히 다섯 값이고 라벨·정렬·설명이 시드 상수와 같다 · `projects.status`에 다섯 값 밖의 값이 0건이다
    - 미수주 프로젝트의 견적 줄 저장 통과 · 완료 프로젝트 저장은 `완료 · 견적 줄 잠김`
    - 업그레이드: 0010 상태 임시 DB에 옛 잠금 프로젝트 둘(줄 하나씩) · 진행 하나 · 수주중 하나 · 미수주 하나 + 옛 코드표 네 값 → 남은 마이그레이션 적용 → (재시드 없이) 옛 잠금 둘이 `completed`이고 `version`이 1 올랐다 · 나머지 셋의 상태·번호·`version` 그대로 · 프로젝트 5행·줄 수 그대로 · 코드표 `project_status`가 다섯 값(설명 포함)
    - 업그레이드 가드: 0011 상태 임시 DB의 `projects.status`에 `xyz`를 넣고 남은 마이그레이션을 적용하면 오류로 멈추고, 그 DB의 프로젝트 행·코드표가 그대로다(RAISE → 전체 롤백)
  </behavior>
  <action>
① `test/unit/domain/rules-gate.test.ts`: 옛 규칙 이름을 부르던 케이스를 새 규칙 `project.line-edit`(완료 → 거부 · 정산·미수주·수주중·진행 → 통과)로 바꾸고, `listGateRules()`에 옛 이름이 없고 새 이름이 있다는 단언을 더한다(RED — 옛 등록이 아직 남아 있다). 케이스를 지우지 않고 바꾼다.

② `domain/rules/register.ts`에서 옛 완료 잠금 이진 규칙의 등록과 그 ctx 타입(견적 외 비용 예외 플래그 포함 — D-83의 조정 줄이 대체한다, 04-13)을 지운다(GREEN). 파일 머리 주석(3–9행 — 「이 플랜이 등록하는 게이트 규칙은 하나뿐이다」로 시작해 옛 규칙 이름을 적은 문단과 견적 외 비용 예외 문단)을 지금 사실로 고친다 — 「Phase 4의 프로젝트 게이트 규칙을 등록하는 한 곳 — 규칙마다 등록한 플랜을 주석 한 줄로 적는다(`project.line-edit` 04-06, 뒤 규칙은 04-20·04-22·04-12·04-26·그룹 B)」(엔지 리뷰 A P3 낡은 주석).

③ `test/integration/project-status.test.ts`(신규 — 04-20·04-21이 같은 파일에 전환 describe를 더한다): (f) 마이그레이션 뒤 상태 — 코드표 `project_status`가 정확히 다섯 값(라벨·정렬·설명이 시드 상수와 같다)이고 `projects.status`에 다섯 값 밖의 값이 0건이다(가드 자체는 적용 전 상태에서만 의미가 있어 이 파일에서는 재현하지 않는다 — ④의 업그레이드 테스트가 재현한다), (g) 미수주 프로젝트의 견적 줄 저장이 통과하고 완료 프로젝트의 저장은 `완료 · 견적 줄 잠김`으로 거부된다(DB 재조회로 줄 무변경). 새 모듈이 누수 스캔 커버리지 목록에 필요하면(DTO·액션이 없으므로 보통은 필요 없다) 04-20이 더한다 — 이 태스크는 확인만 한다.

④ **업그레이드 테스트(OV-6, `test/integration/migration-upgrade.test.ts` 신규).** 파일 안 도우미 둘: `createScratchDb()`(난수 이름 DB를 `CREATE DATABASE … OWNER erp`로 만들고 풀을 돌려줌, `afterAll`에서 `DROP DATABASE … WITH (FORCE)`) · `migrateTo(pool, count)`(임시 폴더에 `db/migrations`의 앞 `count`개 SQL과 `count`항목으로 자른 `meta/_journal.json`을 복사해 `migrate()` — `count`가 없으면 실제 폴더 전체). 케이스 둘 — (a) 0010까지(`count` = 0010의 `idx` + 1) 적용 → 옛 데이터(`behavior` 업그레이드 줄 — 옛 잠금 둘은 줄 하나씩, `source='demo'`)를 SQL로 넣음 → 전체 적용 → **시드 함수를 부르지 않고** `behavior`의 재매핑·보존·코드표를 단언. (b) 0011까지 적용 → 알려진 네 값 밖의 상태 한 행 → 전체 적용이 오류로 끝나고 그 DB의 행이 그대로(RAISE → 전체 롤백). 번호는 파일명에서 읽는다(0010·0011을 문자열로 박지 않고 `_revenue_entries`·`_code_item_descriptions` 접미사로 찾는다 — 번호가 밀려도 테스트가 맞다). 그룹 B의 04-41(계약 칸 삭제 — 04-16에서 나뉨)이 같은 파일에 계약 칸 삭제 가드 케이스를 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-gate.test.ts test/unit/import-cycles.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 옛 규칙 이름이 등록 목록에 남았거나 새 규칙 경계가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 마이그레이션 결과가 시드와 다르거나 잠금·목록이 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm vitest run --project integration test/integration/migration-upgrade.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 옛 데이터 위 재매핑·보존이 틀렸거나 가드가 전체를 되돌리지 않았다(OV-6)</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 상세 편집 판정 교체가 기존 화면 흐름을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `rules-gate.test.ts`에 옛 규칙 이름이 등록 목록에 없다는 단언과 새 규칙의 다섯 상태 경계 케이스가 있고, 기존 케이스 수가 줄지 않았다
    - `register.ts`의 등록 목록과 코드·주석 어디에도 옛 이진 규칙 이름과 그 ctx 타입이 없다(`grep -c "completed-lock" domain/rules/register.ts` = 0 — 머리 주석 포함)
    - 통합 테스트 (f)·(g)가 있고 초록이다 — (g)의 거부는 DB 재조회로 단언한다
    - 업그레이드 테스트 (a)가 시드를 부르지 않고 재매핑·보존·코드표를 단언하고, (b)가 가드 뒤 무변경을 단언한다 · 임시 DB가 테스트 뒤 남지 않는다(`afterAll` DROP)
    - 손실 있는 역 SQL과 「한 PR 배포」 전제가 SUMMARY에 있다(A-28)
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>옛 이진 게이트의 흔적이 코드·테스트에서 사라지고, 마이그레이션 결과와 완료/미수주 잠금 규칙이 빈 DB와 옛 데이터 DB 둘 다에서 증명됐으며 전체 게이트가 CI=true로 초록이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 마이그레이션 → 기존 데이터 | 옛 상태 값을 새 값으로 옮긴다. 전제가 깨진 DB에서 조용히 지우면 행이 고아가 된다 |
| domain → 게이트 | 완료 잠금이 옛 규칙에서 새 규칙으로 바뀌는 지점. 이후 페이즈(정산 셀 범위·조정 줄)가 같은 규칙을 넓힌다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-40 | Tampering | 마이그레이션 0012 | medium | mitigate | 코드표·프로젝트 행에 알려진 네 값 밖의 상태가 있으면 지우지 않고 RAISE EXCEPTION으로 전체를 되돌린다. 재매핑은 DELETE가 아니라 UPDATE다(Pitfall 6). 락 타임아웃 한 쌍. 완료 잠금은 같은 플랜에서 새 규칙으로 바뀌어 옛 값이 사라진 뒤 잠기지 않는 구간이 플랜 밖으로 나가지 않는다(통합 (g)) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint:sql` `Found 0 issues`, 0009 diff 0줄, 마이그레이션 번호 검증 명령 초록
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `domain/rules/register.ts` 등록 목록: `project.line-edit`(옛 이진 규칙 없음)
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 상태가 다섯 값이고 옛 잠금 값이 데이터·시드·게이트·테스트에서 사라졌다(목록 화면의 옛 라벨 한 줄은 04-21)
- 완료 잠금이 끊기는 구간 없이 새 편집 규칙으로 교체됐고 미수주는 잠기지 않는다
- 04-20이 전이표 데이터를 읽어 사람의 전환을 얹을 자리가 준비됐다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/0012_project_status_five_values.sql`(계획 번호 — 실제 번호는 생성기 기준, 직전 최고 + 1) + `meta/0012_snapshot.json` + `_journal.json` 항목 |
| 코드표 값 | `project_status`: `settling`(정산)·`completed`(완료) 추가(설명 포함), 옛 잠금 값 행 삭제·재매핑, `lost` 정렬 4 |
| 상수·타입(신규) | `domain/projects/status-transitions.ts` — `PROJECT_STATUSES` · `ProjectStatus` · `ALLOWED_TRANSITIONS` |
| 게이트 규칙 | `project.line-edit`(옛 `project.completed-lock` **삭제·교체**) |
| 변경된 기존 심볼 | `saveQuoteLines` 게이트 호출 · 상세 편집 가능 판정 · 시드 상태 상수 · `projects.status` 주석 |
| 테스트 | `test/integration/project-status.test.ts`(신규 — 04-20·04-21이 확장) · `test/integration/migration-upgrade.test.ts`(신규 — 04-41 계약 칸 삭제 플랜이 확장, 그룹 B) · `rules-gate.test.ts`·`quote-lines.test.ts`(갱신) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 권한표 메뉴·전환 게이트·전환 함수·액션(04-20) · 상태 바꾸기 화면·부제·목록 상태 표시·생애 E2E(04-21) · 자동 정산·종료일 지남(04-11) · 기간 칸·정산 되돌리기(04-22) · 정산 셀 범위·줄 삭제(04-12·04-30) · 300줄 상한(04-26) · 조정·견적 외 비용 줄(04-13·04-23) · 차수·고객 승인(04-14·04-24) · 프로젝트 복사(04-15) · 「한 PR 배포」 릴리스 제약 기록(04-41, 그룹 B) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| OV-6 — 마이그레이션 테스트가 재시드된 빈 DB만 검증 | HIGH | Plan 04-06, Task 2 ④: 0010 상태 옛 데이터 위 업그레이드 테스트(재시드 전 검사) + 가드 롤백 케이스 |
| OV-7 / 사용자 D21 — 견적 표와 무관한 플랜이 04-04를 기다림 | HIGH | Plan 04-06 frontmatter: `depends_on` [04-01, 04-10] — 04-04·04-08·04-09 의존 제거(depends_on DAG 웨이브 5) |
| A-28 — 0012 전진 전용·코드 롤백 위험 | NOTE | Plan 04-06 `<probe_fallback>` 롤백 자세 + SUMMARY 역 SQL · 릴리스 제약 기록은 04-41 계약 칸 삭제 플랜(그룹 B) |
| A-38 — 재매핑 demo 행 이력 없음 | NOTE | 받아들임(demo만) — `<probe_fallback>`에 기록 |
| A-40 — 상세 페이지에도 옛 값 | NOTE | 04-21 리터럴 스캔이 잡는다 — `<probe_fallback>`에 기록 |
| A7(D19-7) · ⚠ S7(D13) | DECISION | 사용자 확정 — `<probe_fallback>` 갱신 |
| A-45(이미 있음) | NOTE | 0009 무변경·RAISE 가드·번호 검증이 Task 1 검증 명령으로 그대로 덮인다 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 A P3 — `register.ts` 머리 주석(「규칙은 하나뿐 — completed-lock」)이 낡음 | NOTE | Plan 04-06, Task 2 ② 머리 주석 교체 · 수용 기준 grep 0 |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 디자인 리뷰(`docs/designs/plant8-erp-phase4-design-review-260923.md`) · UI-SPEC rev 5

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| P0 · DR-2 — 완료 잠김 문구가 rev 5 Copywriting과 다름(`완료 · 견적 줄이 잠김`) | NOTE | Plan 04-06 전체: `완료 · 견적 줄 잠김`(rev 5 `Error — 셀(잠김) = 표 위 한 줄`) — 04-12가 같은 문자열을 `quoteLockReason` 한 함수로 옮긴다 |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W3 — 낡은 포인터 「04-08 Task 3이 문구를 고친다」 | WARNING | `<probe_fallback>`의 포인터를 현재 주인 **04-43**(REQUIREMENTS·ROADMAP 세 문구, 사용자 D16)으로 |
| I1 — 04-28과 같은 파일(`domain/quotes/lines.ts`)을 고치는데 조상 관계가 없음 | INFO | `depends_on`에 `04-28` 추가(같은 웨이브 파일 겹침 방지 — 웨이브 번호는 오케스트레이터가 다시 매긴다) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md` when done
</output>
