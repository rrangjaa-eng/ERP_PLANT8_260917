---
phase: 04-project-quote-ledger
plan: 07
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-04"]
files_modified:
  - db/schema/reserve-entries.ts
  - db/schema/index.ts
  - db/migrations/0006_reserve_entries.sql
  - db/migrations/meta/_journal.json
  - repositories/reserve-entries.ts
  - domain/reserves/index.ts
  - domain/permissions/info-items.ts
  - app/(app)/pnl/page.tsx
  - app/(app)/pnl/reserves/page.tsx
  - app/(app)/pnl/reserves/reserves-table.tsx
  - app/(app)/pnl/reserves/actions.ts
  - app/(app)/pnl/reserves/actions.registry.ts
  - app/(app)/pnl/reserves/reserves.module.css
  - test/integration/setup.ts
  - test/unit/domain/reserve-balance.test.ts
  - test/integration/reserve-entries.test.ts
  - test/e2e/reserves.spec.ts
  - .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md
autonomous: true
requirements: [RSV-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "경영관리가 클라이언트별 리저브 대장에 입금·출금을 날짜순으로 기록하면 각 줄의 잔액이 서버 계산으로 보인다(ROADMAP 기준 6)"
    - "잔액 열은 계산값이라 누구에게나 항상 읽기 전용이고 흐린 색이 아니라 본문 색이다 — 값이지 부가 정보가 아니다(S9)"
    - "그룹 머리글은 클라이언트이고 머리글 행 오른쪽에 그 클라이언트의 잔액이 서버 계산으로 붙는다(S9)"
    - "각 줄의 잔액은 그 클라이언트 전체 기준 누적값이라 「더 보기」로 몇 건을 불러왔는지와 무관하게 같다(계획 단계 판단, S9 미검토 지점)"
    - "리저브 줄은 날짜·입출금 구분·금액·프로젝트(선택)·증빙 종류·세금계산서 번호·메모를 갖고 금액은 `domain/money` 모델을 쓴다(D-60)"
    - "증빙 종류는 Phase 3 코드표에서 오고 이 페이즈가 새 목록을 만들지 않는다(D-60)"
    - "리저브 줄을 프로젝트에 연결할 수 있고 연결하지 않아도 저장된다 — 선택 항목이다(D-60)"
    - "어느 날짜에서든 누적 잔액이 음수가 되는 저장·수정·삭제는 서버가 거부하고 이유를 보인다 — 마지막 줄만 보고 판단하지 않는다(D-60, `docs/inputs` §6)"
    - "잔액 음수 거부는 배치 전체를 거부하고 그 줄의 금액 셀이 오류 셀로 고정된다(S9, §7-3)"
    - "리저브 줄은 직접 수정할 수 있고 수정이 행동 로그에 남는다(D-60)"
    - "삭제는 물리 삭제가 아니라 보관함 규약을 따른다(D-60)"
    - "리저브 대장은 정보 노출표의 새 항목이라 기획본부에게 기본 숨김이고, 손익 메뉴 권한이 없는 사람은 화면에 도달하지 못한다 — 부분 마스킹이 아니라 메뉴 수준에서 막힌다(D-59, 기준 6)"
    - "선택 열(프로젝트)이 빈 줄은 em dash 하나로 렌더되고 빈칸을 두지 않는다(§2-4)"
    - "폰 1순위 세 열은 날짜·금액·구분이고 접힌 줄은 프로젝트·잔액·메모이며 나머지 둘은 숨는다 — 375px에서 가로 스크롤이 0이다"
    - "세금계산서 번호는 숫자 고정 폭에 줄바꿈 없음이라 어떤 폭에서도 꺾이지 않는다"
    - "리저브 기록이 0건이면 EMPTY 한 줄과 줄 추가 다음 한 수가 나온다"
    - "로드 실패는 한 줄과 「다시 시도」 하나다"
    - "리저브 충당(매출 반영) 동작이 이 페이즈에 없다 — 그 자리는 Phase 9다"
    - statement: "리저브가 0건·1건·50건 이상일 때 0건은 EMPTY 한 줄로 갈리고 1건에서도 클라이언트 그룹 머리글과 오른쪽 잔액이 그대로 렌더되며 50건을 넘으면 「더 보기」 3차 버튼으로 이어 받는다"
      verification: backstop
    - statement: "리저브 메모 셀은 긴 칸 규칙 아래 PC에서 한 줄 말줄임으로 렌더되고 행 높이를 늘리지 않는다"
      verification: backstop
  prohibitions:
    - statement: "리저브 금액이 기획본부에게 보이는 어떤 경로로도 새어서는 안 된다. 화면에서 가리는 것만으로는 부족하다 — 프로젝트 상세의 합계, 목록의 집계, 앞으로 생길 Excel 내보내기 어느 쪽도 리저브 줄을 말없이 더해서는 안 된다. 「금액만 빼고 건수는 보여주자」 같은 부분 노출도 같은 실패다: 이 대장은 클라이언트와 회사 사이의 돈이고 기획본부의 업무 화면에 등장할 이유가 없다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/reserve-entries.ts"
      provides: "리저브 대장 줄 표 — 클라이언트·날짜·입출금 구분·Money 묶음·프로젝트(선택)·증빙 종류·세금계산서 번호·메모"
      exports: ["reserveEntries"]
    - path: "db/migrations/0006_reserve_entries.sql"
      provides: "리저브 표 생성 + 클라이언트·날짜 조회 인덱스 + custom_fields GIN 인덱스"
      contains: "SET LOCAL lock_timeout"
    - path: "domain/reserves/index.ts"
      provides: "리저브 대장 — 누적 잔액 계산과 음수 거부의 유일한 지점"
      exports: ["listReserves", "saveReserves", "archiveReserve", "RESERVE_DTO_SPEC"]
    - path: "app/(app)/pnl/reserves/page.tsx"
      provides: "클라이언트별 리저브 대장 화면 — 손익 메뉴 안, 권한 없으면 도달 불가"
      contains: "notFound"
    - path: "test/unit/domain/reserve-balance.test.ts"
      provides: "누적 잔액 계산과 음수 판정의 경계 표 — 중간 날짜에서만 음수가 되는 경우 포함"
      min_lines: 30
    - path: "test/integration/reserve-entries.test.ts"
      provides: "음수 거부·수정·보관·정보 노출·프로젝트 연결 증명"
      min_lines: 35
    - path: "test/e2e/reserves.spec.ts"
      provides: "경영관리는 기록, 기획 PM은 도달 불가 스모크"
      min_lines: 25
  key_links:
    - from: "domain/reserves/index.ts"
      to: "domain/money/index.ts"
      via: "잔액 누적이 금액 모듈 함수로만 이루어진다"
      pattern: "toKrw"
    - from: "app/(app)/pnl/reserves/page.tsx"
      to: "domain/permissions/can.ts"
      via: "손익 메뉴 권한이 없으면 화면이 존재하지 않는다"
      pattern: "can("
    - from: "domain/reserves/index.ts"
      to: "domain/archive/index.ts"
      via: "삭제가 보관함 규약을 지난다"
      pattern: "archive"
---

<objective>
**As a** 경영관리 담당자, **I want to** 클라이언트가 미리 맡겨 둔 리저브 금액의 입출금을 날짜순으로 적고 그 클라이언트의 잔액이 줄마다 바로 보이게 하고 싶다, **so that** 엑셀 파일로 따로 관리하던 리저브가 프로젝트와 같은 시스템 안으로 들어오고, 잔액이 음수가 되는 기록 실수를 시스템이 막는다.

이 플랜은 이 페이즈의 **마지막 독립 슬라이스**다. 앞 플랜들이 만든 것(금액 모델 · 편집 표 · 보관함 · 정보 노출표)을 전부 쓰지만 프로젝트 화면을 하나도 고치지 않아 04-06과 같은 웨이브에서 병렬로 돈다.

Purpose: ROADMAP Overview는 이 페이즈가 5플랜 상한에 닿으면 리저브 대장을 별도 페이즈로 떼라고 적었고, `04-CONTEXT.md`의 Claude 재량은 그 절단선을 「리저브 대장 하나」로 고정하되 **7플랜을 넘길 때만** 발동한다고 정했다. 이 페이즈는 정확히 7플랜이므로 리저브는 여기 남는다 — 그리고 절단선이 여기라는 사실이 이 플랜의 독립성을 설계 제약으로 만들었다(프로젝트 화면 무변경).

Output: 리저브 표 + 마이그레이션 0006, 잔액 계산·음수 거부, 대장 화면 하나, 단위 1 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-RESEARCH.md
@.planning/phases/04-project-quote-ledger/04-UI-SPEC.md
@.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md
@CLAUDE.md
</context>

<probe_fallback>
엣지 프로브 폴백의 전체 표와 금지 항목 규약은 `04-01-PLAN.md`의 `<probe_fallback>`에 있다. 이 플랜이 담당하는 미분류 행은 **RSV-01** 하나이고, 프로브가 답을 주지 못했으므로 수용 기준은 ROADMAP 기준 6 · D-59·D-60 · `docs/inputs` §6 · `04-UI-SPEC.md` S9에서 끌어와 위 `must_haves.truths`에 적었다. 조용히 버린 항목은 없다.

`04-OPEN-ITEMS.md`가 「근거가 특히 얇은 둘」로 지목한 **S9 zero-one-many — 잔액 누적 열과 페이지네이션의 상호작용**은 아래 Task 2 ③에서 계획 단계 판단으로 닫는다. 같은 문서가 지목한 **S6 overflow의 열 우선순위 미선언**은 04-02가 담당했다.

**진입 경로에 대한 명시적 가정(스펙과 다름):** `04-UI-SPEC.md` S9는 진입을 「손익 메뉴 안 항목(`role-menu.ts`에 한 줄)」이라 적지만, 그 파일의 실제 계약은 1차 메뉴를 **정확히 다섯**으로 고정하고(§6-0) 하위 메뉴 개념이 없다. 하위 메뉴를 만들면 D-23이 「셸 컴포넌트에는 역할 분기를 두지 않는다」로 지킨 구조를 건드리게 된다. 그래서 이 플랜은 **손익 첫 화면(`/pnl`)에서 리저브 대장으로 가는 링크**를 진입 경로로 삼고 `ui/shell/role-menu.ts`를 고치지 않는다. D-59의 실질(「손익 메뉴 안의 화면 하나」·「기획본부는 메뉴 수준에서 못 본다」)은 그대로 성립한다 — 손익 메뉴 권한이 없으면 `/pnl`에 닿지 못하고 링크도 보지 못한다. 이 이탈을 `04-OPEN-ITEMS.md`에 한 줄로 기록한다.
</probe_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 리저브 표와 누적 잔액 · 음수 거부</name>
  <files>db/schema/reserve-entries.ts, db/schema/index.ts, repositories/reserve-entries.ts, domain/reserves/index.ts, domain/permissions/info-items.ts, test/unit/domain/reserve-balance.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-59·D-60 원문 — 화면 위치, 줄 구성 일곱, 잔액은 서버 계산 열, 직접 수정 허용·삭제는 보관함, 어느 날짜든 음수면 거부
    - `docs/inputs/phase-04-project-quote.md` §6 — 만료 없음·잔액 음수 금지가 확정된 원문
    - `.planning/ROADMAP.md` Phase 4 성공 기준 6 전문
    - `.planning/phases/04-project-quote-ledger/04-RESEARCH.md` § Architectural Responsibility Map의 「리저브 잔액 계산」 행 — DB 윈도 함수와 애플리케이션 합산 둘 중 **계획 단계가 성능 대비 단순성으로 정한다**고 남긴 자리
    - `db/schema/vendors.ts`(전문) — 보관함 컬럼·`custom_fields`·GIN 인덱스 보일러플레이트
    - `db/schema/money-columns.ts`(04-01 산출물 전문) — 금액 네 컬럼 헬퍼
    - `db/schema/revenue-entries.ts`(04-02 산출물 전문) — 가장 가까운 선례(줄 목록 + 금액 묶음 + 보관함)
    - `db/schema/code-tables.ts`(전문) — 증빙 종류 코드표 참조 방식
    - `repositories/vendors.ts`(전문 146행) — 리포지토리 형태와 보관 조건부 UPDATE
    - `domain/vendors/index.ts`(전문 393행) — DTO 스펙·`registerDto`·`deps` 주입·`recordAction`·가드
    - `domain/money/index.ts`(04-01·04-02 산출물 전문) — 잔액 누적에 쓸 함수
    - `domain/archive/index.ts`(전문) — 보관·복원 판정
    - `domain/permissions/info-items.ts`(04-01·04-02가 항목을 더한 현재 형태)
    - `domain/permissions/menus.ts`(전문) — 손익 메뉴 키가 이미 있다는 것을 확인한다(새 메뉴를 더하지 않는다)
  </read_first>
  <behavior>
    - 입금 1,000,000 · 출금 300,000 두 줄이면 잔액이 1,000,000 → 700,000이다
    - 날짜가 같은 줄이 둘이면 잔액 계산 순서가 결정적이다(날짜 다음 보조 키로 정렬)
    - 입금 1,000,000 · 출금 1,200,000을 넣으면 거부되고 이유가 어느 날짜에서 잔액이 음수가 되는지 말한다
    - 입금 1,000,000(3월) · 출금 800,000(1월) 순서로 넣으면 **1월 시점에 잔액이 음수**이므로 거부된다 — 마지막 줄의 잔액만 보고 통과시키지 않는다
    - 기존 입금 줄의 금액을 줄여 중간 날짜의 잔액이 음수가 되면 그 수정이 거부된다
    - 입금 줄을 보관해 중간 날짜의 잔액이 음수가 되면 그 보관이 거부된다
    - 외화 줄은 원화 환산액 기준으로 잔액에 들어간다
    - 클라이언트가 다르면 잔액이 서로 섞이지 않는다
  </behavior>
  <action>
① `db/schema/reserve-entries.ts`: `id` uuid pk · `client_id` FK(restrict) · `entry_date` date NOT NULL · `direction` text NOT NULL(입금·출금 둘) · 금액 Money 묶음(04-01 헬퍼 재사용) · `project_id` FK NULL(선택 연결) · `evidence_type` text NULL(Phase 3 증빙 종류 코드표의 값) · `tax_invoice_number` text NULL · `note` text NULL · `sort_key`(같은 날짜 안의 결정적 순서) · `source` · `custom_fields` jsonb · 보관함 4컬럼 · 타임스탬프. **잔액 컬럼을 만들지 않는다** — 잔액은 계산값이고 저장하면 갱신 누락 비정규화가 된다(Eng OV-4가 같은 이유로 손익 파생 컬럼을 기각했다). 인덱스는 `(client_id, entry_date, sort_key)`와 `custom_fields` GIN. 배럴에 더한다.

② **잔액 계산 자리를 정한다(리서치가 계획 단계에 남긴 재량):** 애플리케이션 합산으로 한다. 근거 셋 — (a) 클라이언트 하나당 줄 수가 수십~수백 규모라 전체를 읽어 더하는 비용이 낮다, (b) 음수 판정이 「어느 날짜에서 처음 음수가 되는가」를 이유 문자열에 담아야 하는데 그 지점을 찾는 로직은 코드가 SQL보다 명확하다, (c) `domain/money`가 유일한 산술 지점이라는 계약을 지키려면 누적도 그 함수를 지나야 한다(윈도 함수는 DB 안에서 더한다). 규모가 커지면 리포지토리 안에서 윈도 함수로 바꿀 수 있도록 **잔액 계산을 순수 함수로 분리**해 둔다. 이 선택과 근거를 SUMMARY에 적는다.

③ `domain/reserves/index.ts`: 순수 함수 `runningBalance(rows)`가 날짜·보조 키 순으로 잔액을 누적해 각 줄의 잔액과 **처음 음수가 되는 지점**을 돌려준다. `saveReserves`는 저장 후 예상 상태로 이 함수를 돌려 음수 지점이 있으면 **아무것도 쓰지 않고** 그 줄과 이유를 돌려준다 — 저장·수정·보관 세 경로 전부 같은 검사를 지난다. 읽기는 `scopeFor` → 리포지토리 → `project(viewer, row, RESERVE_DTO_SPEC)` 투영이고 `registerDto` 등록 필수(D-38). 삭제는 `domain/archive`를 지난다. 수정·저장·보관 전부 행동 로그를 남긴다.

④ `domain/permissions/info-items.ts`에 리저브 금액 항목을 기본 숨김으로 더한다. **새 메뉴를 만들지 않는다** — 손익 메뉴 키가 이미 있고 D-59가 「손익 메뉴 안의 화면 하나」라고 정했다.

⑤ `test/unit/domain/reserve-balance.test.ts`: 위 `behavior`의 여덟 경우를 표로 쓴다. 특히 **중간 날짜에서만 음수가 되는 경우**와 **같은 날짜 두 줄의 결정적 순서**를 반드시 넣는다 — 이 둘이 「마지막 잔액만 보면 통과하는」 잘못된 구현을 잡는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/reserve-balance.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다. `plant8/money-boundary` 위반이 나오면 잔액 누적이 금액 모듈 밖에서 더해진 것이다</fails_when>
    <automated>git diff --stat domain/permissions/menus.ts ui/shell/role-menu.ts</automated>
    <fails_when>출력이 비어 있지 않다 — 손익 메뉴 키가 이미 있으므로 메뉴 레지스트리와 셸 매핑을 건드리지 않는다(D-23)</fails_when>
  </verify>
  <acceptance_criteria>
    - `db/schema/reserve-entries.ts`에 잔액 컬럼이 없다 — 잔액은 계산값이다
    - 같은 날짜 안의 순서를 결정하는 보조 키 컬럼이 있다
    - 잔액 계산이 순수 함수로 분리돼 있고 DB 없이 단위 테스트가 돈다
    - 잔액 계산 자리(애플리케이션 합산)의 선택과 근거 셋이 SUMMARY에 적혔다
    - 음수 판정이 마지막 줄이 아니라 **모든 날짜 시점**을 본다 — 중간 날짜 음수 케이스가 단위 테스트에 있다
    - 저장·수정·보관 세 경로가 전부 같은 음수 검사를 지난다
    - 거부 시 아무 줄도 쓰이지 않는다(부분 저장 없음)
    - 리저브 정보 항목이 기본 숨김으로 등록됐다
    - `domain/permissions/menus.ts`·`ui/shell/role-menu.ts` diff가 비어 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>리저브 표와 누적 잔액·음수 거부가 구현되고, 중간 날짜 음수를 포함한 경계 표 단위 테스트가 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 리저브 대장 화면 — 클라이언트 그룹 · 잔액 열 · 손익 메뉴 진입</name>
  <files>app/(app)/pnl/page.tsx, app/(app)/pnl/reserves/page.tsx, app/(app)/pnl/reserves/reserves-table.tsx, app/(app)/pnl/reserves/actions.ts, app/(app)/pnl/reserves/actions.registry.ts, app/(app)/pnl/reserves/reserves.module.css, .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 736–748행(S9 적용 규칙 전문) — 진입 경로, 그룹 머리글과 오른쪽 잔액, 열 여덟, 잔액 열의 색과 읽기 전용, 직접 수정·보관, 음수 거부 표시, 폰 우선순위
    - 같은 문서 370–380행(S9 여덟 상태 + backstop 둘)
    - 같은 문서 234–287행(Copywriting Contract) — 리저브 EMPTY·로드 실패·잔액 오류 문구의 정본
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` 「검증 때 증거를 요구할 자리 — backstop 12건」의 **S9 zero-one-many** 문단 — 잔액 누적과 페이지네이션의 상호작용이 미검토라는 지적. ③이 이것을 닫는다
    - `docs/design/SYSTEM.md` §6-1(목록 = 원장) · §7-3(표 계약 전체) · §7-7(다섯 상태) · §2-4(숫자·0과 빈 값·외화 두 줄)
    - `app/(app)/pnl/page.tsx`(전문) — 손익 첫 화면의 현재 형태와 링크를 둘 자리
    - `app/(app)/projects/[id]/revenue-section.tsx`(04-02 산출물 전문) — 편집 표 + 저장 경로의 가장 가까운 선례
    - `ui/table/Table.tsx`·`ui/table/types.ts`(04-01·04-02·04-04 산출물 전문) — 그룹 머리글·잔액 같은 읽기 전용 계산 열·합계 행·폰 우선순위 prop. **이 파일들을 고치지 않는다**
    - `ui/form/Form.tsx`·`ui/select/Select.tsx`(04-01 산출물 전문) — 구분·증빙 종류·프로젝트 선택에 쓴다
    - `app/(app)/admin/vendors/actions.ts`·`actions.registry.ts`(전문) — Server Action 다섯 규약과 레지스트리 분리
    - `app/(app)/admin/vendors/page.tsx` 1–71행 — 세션·`can` 게이트와 검색 파라미터 관례
  </read_first>
  <behavior>
    - 경영관리 역할로 `/pnl`을 열면 리저브 대장으로 가는 링크가 보이고 누르면 대장이 열린다
    - 기획 PM 역할로 `/pnl`에 접근하면 손익 메뉴 권한이 없어 화면에 닿지 못한다
    - 대장에 줄이 0건이면 EMPTY 한 줄과 줄 추가 다음 한 수가 나온다
    - 줄이 1건이어도 클라이언트 그룹 머리글과 오른쪽 잔액이 렌더된다
    - 「더 보기」로 50건에서 100건으로 늘려도 이미 보이던 줄의 잔액 값이 바뀌지 않는다
    - 잔액 셀은 클릭·Enter로 편집에 들어가지 않는다
    - 프로젝트를 고르지 않은 줄의 프로젝트 칸이 em dash 하나다
    - 잔액이 음수가 되는 저장을 시도하면 그 줄의 금액 셀이 오류로 고정되고 합계 행에 전부 거부가 표시된다
  </behavior>
  <action>
① `app/(app)/pnl/reserves/page.tsx`: 세션 확인 + 손익 메뉴 보기 권한 판정, 없으면 찾을 수 없음으로 떨어뜨린다(관리자 화면의 기존 관례와 같다). 데이터는 `Promise.all`로 한 번에 불러온다. `app/(app)/pnl/page.tsx`에 리저브 대장으로 가는 링크를 한 줄 더한다 — `ui/shell/role-menu.ts`를 고치지 않는다(위 `<probe_fallback>`의 명시적 가정). 이 이탈을 `04-OPEN-ITEMS.md`에 한 줄로 기록한다.

② `app/(app)/pnl/reserves/reserves-table.tsx`: `ui/table`을 편집 표로 쓴다 — 그룹 머리글은 클라이언트이고 머리글 행 오른쪽에 그 클라이언트의 잔액을 굵게 붙인다. 열은 날짜·구분·금액·잔액·프로젝트·증빙 종류·세금계산서 번호·메모 여덟, 날짜순. **잔액 열은 셀 단계를 읽기 전용으로 서버가 보내고** 색은 흐린 색이 아니라 본문 색이다. 폰 1순위는 날짜·금액·구분, 접힌 줄은 프로젝트·잔액·메모, 나머지 둘은 숨긴다. 세금계산서 번호는 숫자 고정 폭에 줄바꿈 없음. 구분·증빙 종류·프로젝트는 `ui/select`를 쓰고 증빙 종류 목록은 Phase 3 코드표에서 온다. 저장은 화면 1차 버튼 하나(§7-3 (사))이고 오류·거부 표시는 04-04가 만든 경로를 그대로 탄다. **`ui/table`을 고치지 않는다** — 필요해지면 SUMMARY에 적고 미룬다.

③ **S9 zero-one-many의 미검토 지점(잔액 × 페이지네이션)에 대한 계획 단계 판단:** 각 줄의 잔액은 **그 클라이언트 전체 기준 누적값**이고, 서버가 그룹 전체를 읽어 계산한 뒤 화면이 요청한 범위의 줄만 돌려준다. 「더 보기」로 더 불러와도 **이미 보이던 줄의 잔액은 바뀌지 않는다** — 이것이 목록 합계가 불러온 건수와 무관한 것과 같은 원리다(04-05의 금지 항목과 같은 뿌리). 그룹 머리글 오른쪽 잔액은 그 클라이언트의 **최종 잔액**이다. 페이지 크기에 따라 잔액이 달라지는 구현은 금지한다. 이 판단을 `04-OPEN-ITEMS.md`의 해당 문단에 기록한다.

④ `app/(app)/pnl/reserves/actions.ts`: `authedActionClient` + zod로 저장·보관 액션을 만들고 `revalidatePath`를 부르며 `./actions.registry`를 사이드이펙트 import한다. `actions.registry.ts`에 액션과 DTO 이름을 등록한다(D-38).

⑤ 스타일은 토큰만 쓴다 — 잔액 열의 본문 색, 그룹 머리글 오른쪽 잔액의 굵기. 새 색·서체·radius를 만들지 않는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다. stylelint가 색·서체·radius 리터럴을 잡으면 토큰 밖 값을 쓴 것이다</fails_when>
    <automated>git diff --stat ui/table ui/shell docs/design/tokens.css</automated>
    <fails_when>출력이 비어 있지 않다 — 이 플랜은 표·셸·토큰을 쓰기만 하고 고치지 않는다</fails_when>
    <automated>pnpm vitest run --project unit</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `/pnl/reserves`가 손익 메뉴 보기 권한 없이는 찾을 수 없음으로 떨어진다
    - 진입 링크가 `/pnl` 화면에 있고 `ui/shell/role-menu.ts` diff가 비어 있다
    - 그룹 머리글이 클라이언트이고 머리글 행 오른쪽에 잔액이 렌더된다
    - 잔액 셀의 편집 단계가 서버에서 읽기 전용으로 오고 클라이언트가 그 판단을 추론하지 않는다
    - 잔액이 페이지 크기와 무관하다 — 그룹 전체 기준 누적이고 그 판단이 `04-OPEN-ITEMS.md`에 기록됐다
    - 증빙 종류 목록이 Phase 3 코드표에서 오고 이 플랜이 새 목록 상수를 만들지 않았다
    - 이 화면의 1차 버튼이 하나다
    - 리저브 충당(매출 반영) 동작·버튼이 없다(Phase 9의 몫)
    - `ui/table`·`ui/shell`·`tokens.css` diff가 비어 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>리저브 대장 화면이 클라이언트 그룹·잔액 열·손익 메뉴 진입으로 동작하고 표·셸·토큰을 고치지 않았다</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] 마이그레이션 0006 적용 + 음수 거부·정보 노출 증명</name>
  <files>db/migrations/0006_reserve_entries.sql, db/migrations/meta/_journal.json, test/integration/setup.ts, test/integration/reserve-entries.test.ts, test/e2e/reserves.spec.ts</files>
  <precondition>로컬 Postgres가 떠 있어야 한다 — 통합·E2E는 `bash scripts/dev-db.sh`가 만든 DB를 쓴다.</precondition>
  <read_first>
    - `db/migrations/0005_revenue_entries.sql`(04-02 산출물 전문) — 락 타임아웃 한 쌍의 위치와 이 페이즈의 마이그레이션 문체
    - `db/migrations/meta/_journal.json` — 새 항목 형태. **04-02가 0005를 더했으므로 현재 상태를 먼저 읽는다**
    - `.squawk.toml`(전문) — 배제 규칙 넷. 이 파일을 고치지 않는다
    - `test/integration/setup.ts`(04-01·04-02가 표를 더한 현재 형태) — TRUNCATE 목록에 리저브 표를 더할 자리
    - `test/integration/revenue-entries.test.ts`(04-02 산출물 전문) — 정보 노출·음수 허용을 단언하는 형태
    - `test/integration/roles.test.ts`(전문) — 계급별 판정 단언 형태
    - `test/e2e/revenue-section.spec.ts`(04-02 산출물 전문) · `test/e2e/fixtures.ts` — 두 역할로 같은 화면을 여는 스펙 형태
  </read_first>
  <action>
**이 태스크는 이 플랜의 [BLOCKING] 스키마 반영 단계다** — `pnpm db:generate` → `pnpm lint:sql` → `pnpm db:migrate` 세 단계를 건너뛰면 타입·빌드는 통과하는데 DB에 표가 없는 거짓 통과가 된다.

① `pnpm db:generate`로 `0006_reserve_entries.sql`을 만들고, 생성된 SQL을 직접 읽어 첫 락 요구 문장 앞에 락 타임아웃 한 쌍을 더한다. 인덱스는 비동시 형태여야 한다. `meta/_journal.json`의 tag가 파일명과 맞는지 확인한다(04-02가 이미 0005를 더했으므로 항목이 둘 늘어난 상태다).

② `pnpm lint:sql`이 `Found 0 issues`인지 확인한다. 걸리면 `.squawk.toml`이 아니라 SQL을 고친다.

③ `bash scripts/dev-db.sh && pnpm db:migrate`로 적용하고 `test/integration/setup.ts`의 TRUNCATE 목록에 리저브 표를 더한다(자식 → 부모 순서).

④ `test/integration/reserve-entries.test.ts`: (a) 잔액이 서버 계산으로 줄마다 나온다, (b) **중간 날짜에서 음수가 되는 저장이 거부되고 DB를 다시 읽어 아무 줄도 쓰이지 않았다**, (c) 기존 줄의 금액을 줄여 중간 날짜가 음수가 되는 수정이 거부된다, (d) 입금 줄 보관으로 음수가 되면 보관이 거부된다, (e) 기획본부 계급의 응답에 리저브 줄·금액이 **실리지 않는다**(키 집합 단언), (f) 프로젝트를 연결하지 않은 줄이 저장된다, (g) 외화 줄이 원화 환산액 기준으로 잔액에 들어간다, (h) 클라이언트가 다르면 잔액이 섞이지 않는다.

⑤ `test/e2e/reserves.spec.ts`: 경영관리 역할로 `/pnl` → 리저브 대장 → 줄 추가 → 저장 → 잔액 열 확인 → 음수가 되는 출금을 시도해 오류 셀과 전부 거부를 확인. 이어서 기획 PM 역할로 `/pnl/reserves`에 직접 접근해 **화면에 닿지 못하는 것**을 단언한다.
  </action>
  <verify>
    <automated>pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 `db/migrations/`에 `0006`으로 시작하는 새 `.sql` 파일이 생기지 않는다</fails_when>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다. `Found 0 issues`가 기대 출력이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `error:`로 시작하는 줄이 나온다</fails_when>
    <automated>pnpm vitest run --project integration test/integration/reserve-entries.test.ts test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/reserves.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `db/migrations/0006_reserve_entries.sql`이 존재하고 락 타임아웃 한 쌍을 포함하며 `.squawk.toml` diff가 비어 있다
    - `meta/_journal.json`의 새 tag가 파일명(확장자 제외)과 일치한다
    - 음수 거부 통합 테스트가 **중간 날짜** 케이스를 포함하고 거부 뒤 DB 재조회로 무변경을 단언한다
    - 수정·보관 두 경로의 음수 거부 케이스가 각각 있다
    - 기획본부 응답의 키 집합 단언이 리저브 줄·금액의 **부재**를 확인한다
    - E2E가 두 역할로 같은 경로를 시도해 도달/미도달을 각각 단언한다
    - `pnpm test` 세 계층이 전부 녹색이다
  </acceptance_criteria>
  <done>마이그레이션 0006이 실제 DB에 적용되고, 잔액·음수 거부·정보 노출이 통합 1개·E2E 1개로 증명되며 `pnpm test` 세 계층이 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 리저브 저장 액션 | 날짜·구분·금액이 넘어온다. 잔액은 이 경계를 넘어와도 무시된다(계산값) |
| domain 출구 → 화면 | 리저브 금액이 정보 노출표를 지나야 실린다. 기획본부에게는 메뉴 수준에서 막힌다 |
| 리저브 줄 → 앞으로 생길 집계·내보내기 | Phase 9·10의 손익·Excel 경로가 이 줄을 말없이 더하면 노출이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-41 | Information Disclosure | `/pnl/reserves` 접근 | high | mitigate | 화면이 손익 메뉴 보기 권한을 직접 판정하고 없으면 찾을 수 없음으로 떨어뜨린다(레이아웃 판정에 기대지 않는다 — Phase 1 WR-07의 교훈). E2E가 기획 PM의 직접 접근 차단을 단언한다 |
| T-04-42 | Information Disclosure | 리저브 DTO 출구 | high | mitigate | 리저브 금액을 기본 숨김 정보 항목으로 등록하고 투영이 미통과 시 필드를 싣지 않는다. 통합 테스트가 키 집합으로 부재를 단언한다. D-38 누수 스캔 레지스트리에 액션·DTO를 등록해 이후 페이즈의 집계·내보내기가 이 줄을 말없이 더하면 스캔이 잡는다 |
| T-04-43 | Tampering | 잔액 | high | mitigate | 잔액을 저장하지 않고 요청 페이로드에서도 받지 않는다. 서버가 그룹 전체를 읽어 계산한다 |
| T-04-44 | Tampering | 음수 잔액 회피 | high | mitigate | 저장·수정·보관 세 경로가 전부 같은 검사를 지나고, 검사가 마지막 줄이 아니라 **모든 날짜 시점**을 본다. 중간 날짜 음수 케이스를 단위·통합 양쪽에 고정한다 |
| T-04-45 | Repudiation | 리저브 줄 수정·삭제 | medium | mitigate | 직접 수정이 허용되므로 수정·보관 전부 행동 로그를 남기고, 삭제는 물리 삭제가 아니라 보관함이라 행이 DB에 남는다 |
| T-04-46 | Denial of Service | 그룹 전체 읽기 | low | accept | 클라이언트당 줄 수가 수십~수백 규모라 전체 읽기 비용이 낮다. 규모가 커지면 잔액 계산을 순수 함수로 분리해 둔 덕에 리포지토리 안 윈도 함수로 교체할 수 있다 |
| T-04-47 | Denial of Service | 마이그레이션 0006 | low | mitigate | 신규 표라 락 경합이 없고 락 타임아웃 한 쌍을 둔다. `pnpm lint:sql`이 `Found 0 issues`인 것으로 확인한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm lint:sql`이 `Found 0 issues` — `.squawk.toml` 무변경
- `pnpm test` 세 계층 전부 통과
- 단위 1개 · 통합 1개 · E2E 1개가 존재하고 녹색
- `ui/table`·`ui/shell`·`docs/design/tokens.css`·`domain/permissions/menus.ts` diff가 비어 있다
- `git diff --stat package.json pnpm-lock.yaml`이 비어 있다
</verification>

<success_criteria>
- 경영관리가 클라이언트별 리저브 입출금을 기록하고 잔액이 줄마다 서버 계산으로 보인다
- 어느 날짜에서든 잔액이 음수가 되는 저장·수정·보관이 거부되고 이유가 그 날짜를 말한다
- 리저브가 기획본부에게 메뉴 수준에서 막히고 DTO에도 실리지 않는다
- Phase 9가 매출 충당(RSV-02)을 이 화면에 얹기만 하면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `reserve_entries` (`db/schema/reserve-entries.ts`) |
| 컬럼(신규) | `reserve_entries.client_id`·`entry_date`·`direction`·`project_id`·`evidence_type`·`tax_invoice_number`·`note`·`sort_key` + 금액 묶음 네 컬럼 |
| 마이그레이션 | `db/migrations/0006_reserve_entries.sql` |
| 함수 | `runningBalance`(순수) · `listReserves` · `saveReserves` · `archiveReserve` |
| 타입 | `ReserveEntryRow` · `ReserveDto` · `BalancePoint` |
| 정보 항목 | 리저브 금액(기본 숨김) |
| 리포지토리 | `repositories/reserve-entries.ts` |
| Server Action | 리저브 저장 · 리저브 보관 |
| 라우트 | `app/(app)/pnl/reserves/` (page · reserves-table · actions · actions.registry · module.css) |
| 테스트 | `test/unit/domain/reserve-balance.test.ts` · `test/integration/reserve-entries.test.ts` · `test/e2e/reserves.spec.ts` |
| 변경된 기존 심볼 | `INFO_ITEMS`(리저브 항목 추가) · `app/(app)/pnl/page.tsx`(대장 링크) · `test/integration/setup.ts`(TRUNCATE 목록) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-07-SUMMARY.md` when done
</output>
