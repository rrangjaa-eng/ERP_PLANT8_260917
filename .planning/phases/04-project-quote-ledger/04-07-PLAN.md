---
phase: 04-project-quote-ledger
plan: 07
type: execute
wave: 27
depends_on: ["04-09", "04-10", "04-18", "04-25", "04-29", "04-41"]
files_modified:

  - db/schema/reserve-entries.ts
  - db/schema/index.ts
  - db/migrations/0016_reserve_entries.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0016_snapshot.json
  - repositories/reserve-entries.ts
  - repositories/archive.ts
  - domain/reserves/index.ts
  - domain/archive/index.ts
  - domain/permissions/info-items.ts
  - test/integration/setup.ts
  - test/unit/domain/reserve-balance.test.ts
  - test/integration/reserve-entries.test.ts
  - test/integration/leak-scan.test.ts

autonomous: true
requirements: [RSV-01]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "경영관리가 클라이언트별 리저브 대장에 입금·출금을 기록하면 각 줄의 잔액과 클라이언트의 최종 잔액이 서버 계산으로 온다 — 잔액은 저장 컬럼이 아니라 계산값이다(ROADMAP 기준 6, D-60)"
    - "리저브 줄은 클라이언트 · 날짜 · 입출금 구분 · 금액(Money 모델) · 프로젝트(선택) · 증빙 종류(Phase 3 코드표 값) · 세금계산서 번호 · 메모를 갖는다 — 새 목록 상수를 만들지 않는다(D-60)"
    - "잔액은 원화 환산액으로 누적하고 **날짜마다 그날 마감 잔액**으로 음수를 판정한다 — 같은 날짜 안에서는 입금이 출금보다 먼저 오고 그다음 `created_at`·`id` 순이라(별도 순서 컬럼이 없다 — 엔지니어링 리뷰 B §2), 같은 날 출금을 입금보다 먼저 적어도 그날 마감 잔액이 0 이상이면 통과한다. 어느 날짜든 마감 잔액이 음수가 되는 저장·수정·보관·복원은 서버가 전부 거부하고 아무것도 쓰지 않으며, 그 날짜 마지막 줄의 금액 칸이 오류 칸 `이 줄 뒤 잔액 {잔액} · 금액을 줄이거나 입금 줄 먼저`가 된다(D-60 「어느 날짜든」, 사용자 D19-2 · CEO 리뷰 B-11, Copywriting Error — 리저브 잔액)"
    - "외화(USD) 리저브도 잔액은 원화 기준이다 — USD 1,000을 1,300에 입금하고 1,400에 출금하면 원화 잔액이 −100,000이라 거부된다. 우회는 출금을 입금 때 환율로 적는 것이고(04-42가 문서에 남긴다), 통화별 잔액은 만들지 않는다(사용자 D19-1 · CEO 리뷰 B-10)"
    - "리저브 저장·보관·복원은 트랜잭션 안에서 **관련 클라이언트 행(`vendors`)을 id 순서로 `FOR NO KEY UPDATE` 잠금**한 뒤 그 클라이언트 원장 전체를 다시 읽어 잔액을 판정하고 쓴다 — 같은 클라이언트의 두 저장이 동시에 와도 뒤 저장이 기다렸다가 새 잔액으로 판정되어, 둘 다 통과해 음수가 커밋되는 일이 없다. `FOR NO KEY UPDATE`는 리저브 쓰기끼리는 한 줄로 세우되 그 거래처를 가리키는 견적 줄·프로젝트·리저브 줄의 FK 검사(`FOR KEY SHARE`)는 막지 않는다(`docs/inputs` §6 「잔액 음수: 금지(DB 제약)」를 사용자 D5 「행 잠금 + 서버 검사」로 실현, CEO 리뷰 B-05, 엔지니어링 리뷰 B §1 — `FOR UPDATE`는 무관한 FK 쓰기까지 막는다)"
    - "줄의 클라이언트는 새 줄을 만들 때 정하고 첫 저장 뒤에는 바꿀 수 없다 — 기존 줄의 클라이언트를 바꾼 요청은 `클라이언트는 첫 저장 뒤 잠김 · 새 줄로 적어 주세요`로 거부되고, 서버가 보내는 셀 단계에서 기존 줄의 클라이언트 칸은 잠김이다(사용자 D6 「클라이언트 열 추가(첫 저장 후 잠금)」, CEO 리뷰 B-06)"
    - "리저브 쓰기(저장 · 보관 · 복원)는 권한표 `pnl` 쓰기 **그리고** 정보 노출 `reserve.amount`가 둘 다 있을 때만 된다 — 하나라도 없으면 `ForbiddenError`이고 DB가 그대로이며, 그 거부 메시지와 로그에는 숫자(잔액·금액)가 하나도 없다. 잔액이 실린 거부 문구(`이 줄 뒤 잔액이 …`·`복원하면 … 잔액이 …`)는 리저브 금액을 볼 수 있는 사람에게만 닿는다 — `pnl` 쓰기만 있고 노출이 꺼진 계급이 액션을 직접 불러 잔액을 떠보는 길이 없다. 경영관리는 보관함 권한(`admin.archive`) 없이도 자기가 적은 리저브 줄을 지울 수 있다. `domain/permissions/menus.ts`는 바뀌지 않는다(CEO 리뷰 B-14 · B-04 · T5, 엔지니어링 리뷰 B §1)"
    - "리저브 삭제는 물리 삭제가 아니라 보관이고, 저장과 같은 배치(`archivedIds`)·같은 잠금·같은 잔액 판정을 지난다 — 범용 `archive()`로는 리저브를 보관할 수 없다(보호 행). 보관함에서의 복원은 04-12의 `DOMAIN_RESTORERS`로 `restoreReserve`에 맡겨져 같은 잠금·`pnl` 쓰기·잔액 판정을 한 트랜잭션에서 지나고, 복원하면 어느 날짜가 음수가 되는 출금은 `복원하면 {날짜} 잔액이 {잔액}이 됩니다 · 리저브 대장에서 출금 줄을 먼저 고쳐 주세요`로 거부되며 DB가 그대로다(D-60, CEO 리뷰 B-04 · OV-2 · T5)"
    - "새 입력은 서버가 검증한다 — 금액은 04-40의 `normalizeMoneyInput` 한 규칙(KRW 환율 1 고정 · USD 환율 > 0 · 원화 환산 정수 범위)을 지나고, 이 경로가 더하는 것은 금액 > 0(부호는 구분이 정한다 — 음수 출금으로 입금을 흉내 낼 수 없다) · 달력에 있는 `YYYY-MM-DD` · 클라이언트가 존재하는 거래처 · 프로젝트를 고르면 그 프로젝트의 클라이언트가 줄의 클라이언트와 같음 · 증빙 종류가 Phase 3 코드표 값 · 이미 보관된 줄은 이 배치에서 고치거나 다시 보관할 수 없음이다 — 어긋나면 PG 오류(22008 · 23503)·일반 오류가 아니라 그 칸의 한국어 이유로 거부된다(CEO 리뷰 B-17, 엔지니어링 리뷰 B §2 · GAP 3 b–d)"
    - "환율 칸을 고친 USD 저장은 **커밋된 뒤** `fx.recent_rate.USD`를 그 값으로 갱신하고, 환율을 건드리지 않은 저장과 거부된 저장은 갱신하지 않는다(D-71 — 매출 저장과 같은 규칙, CEO 리뷰 B-12, 엔지니어링 리뷰 GAP 3e)"
    - "리저브 줄의 새로 만들기·수정·보관·복원이 행동 로그에 같은 트랜잭션으로 남고, 수정은 `document_update`(관리자가 설정에서 끌 수 있는 기존 종류) detail `{ entryId, clientId, changed: { amount | entryDate | direction: [전, 후] } }`로 무엇이 어떻게 바뀌었는지 **값 그대로** 남긴다 — 금액을 가리지 않는다. 활동 기록 메뉴·상세는 기본값으로 시스템관리자만 본다(D-60 「직접 수정 허용 — 행동 로그」, CEO 리뷰 B-16 — 엔지니어링 리뷰 ENG-D8로 확정: 사용자가 가리기를 뒤집고 지금 방식 유지)"
    - "거부된 리저브 쓰기(권한 · 잔액 음수 · 클라이언트 변경 · 입력 검증 · 복원 · 재전송 불일치)는 서버 로그 경고 `write.denied` 한 줄(보는 사람 id · 규칙 · 클라이언트·줄 id — 금액 없음)을 04-20의 `denyWrite(viewer, rule, ids, err)` 도우미(허용 필드만)로 남긴다(사용자 D19-3 · CEO 리뷰 B-26, 엔지니어링 리뷰 `denyWrite`)"
    - "새 리저브 줄은 화면이 만든 UUID를 `id`로 싣고, 서버는 같은 `id`가 이미 있으면 넣지 않는다(`ON CONFLICT (id) DO NOTHING`) — 커밋은 됐는데 응답을 잃어 같은 배치를 다시 보내도 한 행이고 잔액을 두 번 빼지 않는다. 같은 `id`에 다른 값이 오거나 남의 줄 id가 새 줄로 오면 `이미 저장된 줄과 값이 다름 · 새로 고침`로 거부된다(엔지니어링 리뷰 ENG-D10 · Codex #3)"
    - "잠긴 트랜잭션 안에서 전역 db(풀)를 부르지 않는다 — `can`·`visible`·코드표 읽기는 트랜잭션 앞에서 계산하고, 잠금 뒤 읽기와 행동 로그는 그 `tx`를 쓰며, 최근 환율 기억은 커밋 뒤다(04-32 규칙 — 같은 클라이언트에 요청이 몰리면 잠금을 쥔 채 두 번째 풀 연결을 기다려 인스턴스가 멈춘다, 엔지니어링 리뷰 B §1)"
    - "잔액 음수 거부는 그 날짜 마지막 줄의 금액 칸 오류와 함께 배치 수준 정보(날짜 · 클라이언트 id·이름 · 잔액 · 그 줄이 목록에서 놓인 쪽 번호 — 새 줄이면 없음)를 돌려준다 — 화면(04-42)이 다른 쪽에 있는 줄의 거부를 보이고 그 쪽으로 옮겨 갈 수 있다(Codex #7)"
    - "리저브 대장 전체가 정보 노출표의 새 항목 `reserve.amount`(기본 숨김)로 게이트된다 — `listReserves`는 `pnl` 보기 권한과 이 항목이 둘 다 있을 때만 줄을 돌려주고, 하나라도 없으면 빈 결과에 건수·그룹 키도 없다(날짜·메모만 보이는 부분 노출이 없다)(D-59, CEO 리뷰 B-15)"
    - "대장 읽기는 50건 번호 페이지이고 각 줄의 잔액은 그 클라이언트 원장 **전체** 기준 누적값이라 몇 쪽을 보든 같다 — 서버가 전체를 계산한 뒤 그 쪽의 줄만 자른다. 범위 밖 번호는 마지막 쪽이다(공용 `lib/paging.ts`의 `clampPage`·`pageCountFrom` — 그룹 C가 만든다, D-91, S9 2026-09-23)"
    - "리저브 대장은 Phase 4에 남는다 — 페이즈 절단 규칙(7플랜 초과 시 리저브 분리)은 사용자 D19-11로 적용하지 않기로 확정됐다. 리저브 충당(매출 반영, RSV-02) 동작은 이 페이즈에 없다(Phase 9)"
  prohibitions:
    - statement: "리저브 금액이 기획본부에게 보이는 어떤 경로로도 새어서는 안 된다. 화면에서 가리는 것만으로는 부족하다 — 프로젝트 상세의 합계, 목록의 집계, 앞으로 생길 Excel 내보내기 어느 쪽도 리저브 줄을 말없이 더해서는 안 된다. 「금액만 빼고 건수는 보여주자」 같은 부분 노출도 같은 실패다: 이 대장은 클라이언트와 회사 사이의 돈이고 기획본부의 업무 화면에 등장할 이유가 없다"
      status: resolved
      verification: test
    - statement: "어느 날짜에서든 리저브 잔액이 음수로 커밋되어서는 안 된다 — 동시 저장, 보관함 복원, 마지막 줄만 보는 판정 어느 경로로도. 음수 잔액은 고칠 도구가 없고(운영 DB 직접 명령 금지) 이후 그 클라이언트의 무관한 편집까지 전부 거부하게 만든다 — 막는 것 말고 복구 경로가 없다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/reserve-entries.ts"
      provides: "리저브 대장 줄 표 — 클라이언트·날짜·구분(CHECK)·Money 묶음·프로젝트(선택)·증빙 종류·세금계산서 번호·메모·version·보관함 컬럼(별도 순서 컬럼 없음 — 순서는 `created_at`·`id`)"
      exports: ["reserveEntries"]
    - path: "db/migrations/0016_reserve_entries.sql"
      provides: "리저브 표 생성 + (클라이언트, 날짜) 인덱스 + custom_fields GIN — 실제 번호는 생성기 기준"
      contains: "SET LOCAL lock_timeout"
    - path: "domain/reserves/index.ts"
      provides: "리저브 대장 — 잔액 계산·음수 판정·클라이언트 잠금·보관·복원의 유일한 지점"
      exports: ["runningBalance", "listReserves", "saveReserves", "restoreReserve", "RESERVE_DTO_SPEC"]
    - path: "test/unit/domain/reserve-balance.test.ts"
      provides: "날짜 마감 잔액·같은 날 입금 먼저·중간 날짜 음수·외화 원화 누적·클라이언트 분리 경계 표"
      min_lines: 40
    - path: "test/integration/reserve-entries.test.ts"
      provides: "음수 거부(저장·수정·보관·복원)·클라이언트 잠금 경합·권한(쓰기 + 노출)·노출 키 집합·검증(보관 줄 · 남의 클라이언트 프로젝트 · 없는 클라이언트 · 코드표 밖 증빙)·커밋 뒤 환율·재전송 한 행·로그 detail·페이지·거부 쪽 번호 증명"
      min_lines: 160
  key_links:
    - from: "domain/reserves/index.ts"
      to: "domain/money/index.ts"
      via: "잔액 누적과 원화 환산이 금액 모듈 함수로만 이루어진다"
      pattern: "toKrw|moneyToColumns"
    - from: "domain/reserves/index.ts"
      to: "repositories/reserve-entries.ts"
      via: "저장·보관·복원이 트랜잭션 안에서 관련 클라이언트 행을 id 순서로 `FOR NO KEY UPDATE` 잠근 뒤 원장을 다시 읽는다"
      pattern: "lockReserveClients"
    - from: "domain/archive/index.ts"
      to: "domain/reserves/index.ts"
      via: "보관함 복원이 `DOMAIN_RESTORERS`(04-12)로 `restoreReserve`에 맡겨진다 — 범용 복원 경로를 타지 않는다"
      pattern: "restoreReserve"
    - from: "domain/reserves/index.ts"
      to: "domain/permissions/visible.ts"
      via: "`pnl` 보기(읽기) 또는 `pnl` 쓰기(저장·보관·복원)와 `reserve.amount` 노출이 둘 다 있어야 줄이 실리고 쓰기가 된다"
      pattern: "reserve.amount"
    - from: "domain/reserves/index.ts"
      to: "domain/money/index.ts"
      via: "리저브 금액이 04-40의 `normalizeMoneyInput`을 지난다"
      pattern: "normalizeMoneyInput"
    - from: "domain/reserves/index.ts"
      to: "lib/paging.ts"
      via: "쪽 보정·쪽 수가 공용 `clampPage`·`pageCountFrom`에서 온다"
      pattern: "clampPage"
---

<objective>
**As a** 경영관리 담당자, **I want to** 클라이언트가 미리 맡겨 둔 리저브 금액의 입출금을 날짜순으로 적고 그 클라이언트의 잔액이 줄마다 서버 계산으로 나오며, 어떤 경로로도 잔액이 음수로 저장되지 않기를, **so that** 엑셀로 따로 관리하던 리저브가 같은 시스템으로 들어오고, 기록 실수·동시 저장·보관함 복원이 음수 잔액을 만들지 못한다.

이 플랜은 이 페이즈의 **리저브 대장 슬라이스의 서버 쪽**이다. 앞 플랜들이 만든 것(금액 모델과 04-40의 `normalizeMoneyInput` · 보관함 · 정보 노출표 · 04-12의 `DOMAIN_RESTORERS` · 04-20의 `denyWrite`·경합 도우미 · 04-32의 행동 로그 `tx`·`lock_timeout`·잠금 규칙 · 그룹 C의 공용 `lib/paging.ts`)을 쓰고 프로젝트 화면을 하나도 고치지 않는다. 리저브는 **Phase 4에 남는다** — 옛 계획이 올린 「7플랜 초과 시 리저브 분리」 절단 규칙은 사용자 D19-11로 적용하지 않기로 확정됐다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 결정 원장 B-05·B-06·T12·T5·부록 B).** 옛 04-07은 19파일이라 **둘로 나눴다** — 이 플랜은 서버(표 · 마이그레이션 · 잔액 · 잠금 · 보관/복원 · 권한 · 노출), **04-42**(뒤)는 화면(대장 · `/pnl` 링크 · 액션 · E2E · 문서)이다. 반영한 것: **사용자 D5**(B-05 — 클라이언트 행 잠금 + 서버 검사) · **사용자 D6**(B-06 — 클라이언트 칸, 첫 저장 뒤 잠금) · **T5/B-04**(보관·복원이 도메인 검사를 한 트랜잭션에서 · 경영관리는 보관함 권한 없이 삭제) · **사용자 D19-2**(B-11 — 날짜 마감 기준 판정, 같은 날 입금 먼저) · **사용자 D19-1**(B-10 — 원화 잔액 유지) · **사용자 D19-3**(B-26 — 거부 경고 로그) · **사용자 D19-11**(Phase 4 유지) · B-12(최근 환율) · B-14(쓰기 권한 이름 `pnl` 쓰기) · B-15(보기 권한 + 노출 둘 다) · B-16(수정 로그 detail) · B-17(입력 검증) · B-34(표 변경 플랜과의 병렬 — 이 플랜은 마지막 `ui/table` 변경인 04-18 뒤에 서고, 표를 쓰는 화면은 04-42로 뗐다).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 결정 번호는 「ENG-D*」로 적어 CEO 리뷰의 「사용자 D*」와 구별한다).** (1) **쓰기 권한 = `pnl` 쓰기 + `reserve.amount` 노출(B §1):** 잔액이 실린 거부 문구로 노출이 꺼진 계급이 잔액을 떠보는 길을 막는다 + 숫자 없는 거부 테스트. (2) **ENG-D8(B-16 종결):** 수정 로그는 지금 방식 `document_update`(관리자가 끌 수 있음)이고 detail의 전·후 값을 가리지 않는다 — 활동 기록은 기본값으로 시스템관리자만 본다. (3) **ENG-D10(Codex #3):** 새 줄 클라이언트 UUID + `ON CONFLICT (id) DO NOTHING` + 재전송 판정 — 응답을 잃은 재전송이 잔액을 두 번 빼지 않는다. (4) **잠금 안 전역 db 호출 금지(04-32 규칙):** `can`·`visible`·코드표는 트랜잭션 앞, 잠금 뒤는 `tx`, `rememberFxRate`는 커밋 뒤. (5) **`FOR NO KEY UPDATE`**(무관한 FK 쓰기를 막지 않는다). (6) **`sort_key` 삭제** — 순서는 (날짜, 구분, `created_at`, `id`). (7) **`normalizeMoneyInput`**(04-40)으로 손 검증 대체. (8) **`denyWrite`**(04-20). (9) **Codex #7:** 잔액 거부가 그 줄의 쪽 번호를 돌려준다(화면 이동은 04-42). (10) **GAP 3 a–e** 테스트. (11) **공용 `lib/paging.ts`**(그룹 C)를 import — `domain/projects/list-view`를 쓰지 않는다. (12) **사실 정정:** 칸 이유 모음 형태는 `domain/revenue`의 `saveEntries`가 아니라 `domain/quotes/lines.ts`의 `CellFormatError`·`SaveRejectedError`(지금 279–309행)이고, `plant8/money-boundary` 린트는 `Money` 타입 피연산자만 잡아 원화 `number` 합을 강제하지 않는다 — 잔액 산술을 금액 모듈 함수로 하는 것은 리뷰·테스트가 지킨다. **ENG-D11:** Phase 4 안에서는 앞 플랜 파일의 결함을 회귀 테스트와 SUMMARY 편차 기록과 함께 고칠 수 있다(범위는 늘리지 않는다).

**마이그레이션 번호:** 0016이다 — 확정된 순서 0011 04-10 → 0012 04-06 → 0013 04-27 → 0014 04-13 → 0015 04-41 → **0016 이 플랜**. 생성기 번호(직전 최고 + 1)가 다르면 생성기 출력 그대로 두고 실제 이름을 SUMMARY에 적는다.

**실행 순서(B-34 · 사용자 D21):** 이 플랜은 `ui/table`을 마지막으로 바꾸는 04-18 뒤에 선다(옛 문구 「04-18과 같은 웨이브(14)에서 병렬」은 틀렸다 — 04-18이 `ui/table`을 고치는 동안 그 위에 짓지 않는다). 04-04의 사람 확인은 기다리지 않는다(D21 — 견적 표와 무관). 번호 페이지 컴포넌트 `ui/pagination`은 04-29, 쪽 보정 `clampPage`·`pageCountFrom`은 그룹 C가 만드는 공용 `lib/paging.ts`(04-17·04-18 경유)다.

Output: 리저브 표 + 마이그레이션 · 잔액 순수 함수(날짜 마감 판정) · 저장(클라이언트 잠금 · 검증 · 환율 · 로그) · 보관(배치)과 복원(`DOMAIN_RESTORERS`) · 노출 게이트 · 페이지 읽기 · 단위 1 · 통합 1.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`(특히 잠금 경합 테스트가 멈추거나 흔들릴 때), 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-32-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# docs/designs/plant8-erp-phase4-ceo-review-260923.md 23–24행(B-05 · B-06 — 사용자 D5·D6) · 37행(D19) · 부록 B B-04·B-05·B-10~B-17 절(Grep `### B-04` 등) · 「## 1a. Data flow」의 saveReserves 줄

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5) 「적용 규칙 S9. 클라이언트별 리저브 대장」 1351–1376행 · 「UI Considerations S9」 560–569행 · Copywriting 375행(Error — 셀(리저브 클라이언트)) · 381–382행(Error — 리저브 잔액 · 리저브 복원(관리자)) · 365행(Error — 저장(재전송 불일치)) — 줄 번호는 `grep -n "### S9. 클라이언트별 리저브 대장"`로 다시 찾는다

# docs/inputs/phase-04-project-quote.md §6(만료 없음 · 잔액 음수 금지)

</context>

<probe_fallback>
엣지 프로브 폴백의 전체 표와 금지 항목 규약은 `04-01-PLAN.md`의 `<probe_fallback>`에 있다. 이 플랜이 담당하는 미분류 행은 **RSV-01(서버 — 원장·잔액·음수 금지·노출)** 이고, 수용 기준은 ROADMAP 기준 6 · D-59·D-60·D-71 · `docs/inputs` §6 · `04-UI-SPEC.md` S9 · CEO 리뷰 결정(D5·D6·D19)에서 끌어와 위 truths에 적었다. 화면 쪽 수용 기준(링크 · 열 · 폰 · EMPTY · 페이지 줄 렌더)은 04-42의 truths다. 조용히 버린 항목은 없다.

**계획 단계 가정(표면화):**

- **「DB 제약」의 실현(사용자 D5):** `docs/inputs` §6은 「잔액 음수: 금지(DB 제약)」다. 사용자는 D5에서 DB 트리거(B-05 B) 대신 「클라이언트별 행 잠금 + 서버 검사」를 골랐다 — 금액 산술을 `domain/money` 한 곳에 두고 Phase 9 RSV-02(매출 충당)가 같은 잠금·같은 검사를 재사용한다. 잠금 대상은 클라이언트 행(`vendors` — `projects.client_id`가 가리키는 표)이고, 한 배치가 여러 클라이언트를 건드리면 **id 오름차순으로** 잠가 교착을 피한다. 기존 줄의 클라이언트는 바뀌지 않으므로(D6) 잠글 클라이언트 id를 잠금 전에 읽어도 안전하다. **잠금 세기는 `FOR NO KEY UPDATE`**(엔지니어링 리뷰 B §1): 자기끼리는 충돌해 리저브 쓰기를 한 줄로 세우지만, 견적 줄의 `vendor_id`·프로젝트의 `client_id`·리저브 줄의 `client_id` 삽입·수정이 잡는 `FOR KEY SHARE`와는 충돌하지 않아 그 거래처를 가리키는 무관한 저장을 막지 않는다(`FOR UPDATE`는 막는다 — 반대로 그 거래처를 가리키는 견적 저장이 리저브 저장을 커밋까지 붙잡기도 한다).
- **날짜 마감 판정(사용자 D19-2):** D-60 「어느 날짜든」을 「각 날짜의 마감 잔액」으로 읽는다 — 같은 날짜 안의 입력 순서가 판정을 바꾸지 않는다. 줄마다의 잔액 표시는 같은 날짜 안에서 입금 → 출금 → `created_at` → `id` 순서로 누적한 값이다(엔지니어링 리뷰 B §2 — 옛 `sort_key`는 누가 매기는지 규칙이 없었고 판정은 날짜 마감이라 같은 날 같은 구분 안의 순서가 결과를 바꾸지 않는다. 한 배치의 새 줄은 같은 트랜잭션 시각이라 `id`가 순서를 정한다). 오류 칸은 음수가 된 날짜의 마지막 줄(표시 순서)의 금액 칸이다. 정렬 정의는 `domain/reserves`의 비교 함수 하나이고 `listReserves`·잔액 판정·쪽 번호 계산이 같이 쓴다.
- **원화 잔액(사용자 D19-1):** 외화 리저브는 드물다(OV-8). 통화별 잔액을 만들지 않고, 입금 때 환율로 출금을 적는 우회를 04-42가 문서(04-OPEN-ITEMS)에 남긴다. 이 플랜은 `runningBalance`에 그 사실을 주석 한 줄로 둔다.
- **쓰기 권한(B-14):** 새 메뉴를 만들지 않는다 — 손익 메뉴 `pnl`의 쓰기가 리저브 쓰기다(D-59 「손익 메뉴 안의 화면 하나」).
- **삭제 경로(B-04):** 리저브 삭제는 화면의 삭제 모달 → 1차 「일괄 저장」 배치의 `archivedIds`(04-12 견적 줄의 `archivedLineIds`와 같은 모양)로만 일어난다. 범용 `archive()`는 `admin.archive` 쓰기를 요구하고 잔액을 보지 않으므로, `repositories/archive.ts`의 리저브 항목은 `isProtected`가 늘 참이라 범용 보관이 거부된다(이유 주석). 보관함은 목록과 복원만 쓴다. 복원 위임 앞의 `admin.archive` 판정은 04-12의 `restore()` 형태 그대로다 — 04-12-SUMMARY에서 위임 전에 그 판정이 도는지 확인하고, 건너뛰면 `restoreReserve`가 `admin.archive` 쓰기도 확인한다.
- **수정 로그(B-16 — 엔지니어링 리뷰 ENG-D8로 확정):** 기존 `document_update` 규약(관리자가 설정에서 끌 수 있음)을 그대로 쓰고, detail의 `changed` 전·후 값(원화 금액 포함)을 **가리지 않는다**. 사용자가 「활동 기록을 보는 건 관리자밖에 없다」로 가리기 안(D8 A)을 뒤집었다 — 기본값에서 활동 기록 메뉴(`admin.action-log`)와 상세(`action_log.detail`, `staffDefault: false`)는 시스템관리자만 본다. 새 행동 종류·가리기 해석기를 만들지 않는다.
- **쓰기 권한 = `pnl` 쓰기 + `reserve.amount` 노출(엔지니어링 리뷰 B §1):** 서버 액션은 공개 끝점이라 화면 404로는 막히지 않는다. 잔액이 실린 거부 문구가 있으므로, 노출이 꺼진 계급이 `pnl` 쓰기만으로 저장·복원을 불러 잔액을 떠볼 수 없게 두 판정을 **입구에서 먼저** 한다 — 그 거부는 숫자 없는 `ForbiddenError`다.
- **재전송(ENG-D10):** 새 줄은 04-12의 견적 줄과 같은 모양 — `isNew: true` + 클라이언트 UUID `id` — 이고, 기존 줄은 `id`·`version`이다. 잠금 뒤 같은 tx로 그 id들을 읽어(보관 포함) 이미 있으면: 같은 클라이언트이고 저장된 값(날짜 · 구분 · 통화 · 외화 금액 · 환율 · 원화 · 프로젝트 · 증빙 종류 · 세금계산서 번호 · 메모)이 요청의 정규화된 값과 같을 때만 **재전송**으로 보고 원장 적용에서 빼며(잔액을 두 번 반영하지 않는다) 쓰지 않는다 — 다르거나 남의 클라이언트 줄이면 `이미 저장된 줄과 값이 다름 · 새로 고침`. 삽입은 `ON CONFLICT (id) DO NOTHING`이고 0행이면 같은 거부(잠금 아래라 같은 클라이언트끼리는 생기지 않는 경우의 방어). 04-12의 견적 줄 재전송은 같은 차수의 활성 줄이면 값 비교 없이 재전송으로 받는다 — 리저브는 잔액을 두 번 반영하거나 재전송 사이의 편집을 조용히 버리지 않도록 값까지 비교한다(차이를 SUMMARY에 적는다).
- **잠금 안 전역 db 호출 금지(04-32 규칙):** `can`·`visible`·증빙 종류 코드표 값 읽기는 `withTransaction` 앞, 잠금 뒤 읽기(원장 · 대상 줄 · 프로젝트의 클라이언트 · 쪽 번호용 정렬 키)와 행동 로그는 `tx`, `rememberFxRate`는 커밋 뒤(거부된 배치는 설정을 바꾸지 않는다 — GAP 3e).
- **누수 스캔 등록:** `registerDto`와 `test/integration/leak-scan.test.ts`의 `@/domain/reserves` side-effect import는 이 플랜이 한다(04-18 뒤라 그 파일과 겹치지 않는다). 액션 레지스트리 import는 액션을 만드는 04-42가 더한다.

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 경영관리가 한 클라이언트에 입금·출금을 저장하면 서버가 잔액을 계산하고, 마감 잔액이 음수가 되는 저장은 전부 거부된다(표 → [BLOCKING] 마이그레이션 → 클라이언트 잠금 리포지토리 → 도메인 → 통합)</name>
  <files>db/schema/reserve-entries.ts, db/schema/index.ts, db/migrations/0016_reserve_entries.sql, db/migrations/meta/_journal.json, db/migrations/meta/0016_snapshot.json, repositories/reserve-entries.ts, domain/reserves/index.ts, domain/permissions/info-items.ts, test/integration/setup.ts, test/integration/reserve-entries.test.ts</files>
  <precondition>로컬 Postgres가 떠 있어야 한다 — 통합 테스트는 `bash scripts/dev-db.sh`가 만든 DB를 쓴다.</precondition>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-59·D-60·D-71 원문
    - `db/schema/revenue-entries.ts`(04-02 산출물 전문) — 가장 가까운 선례(줄 목록 + 금액 묶음 + `version` + 보관함 + `source`) · `db/schema/money-columns.ts`
    - `db/schema/vendors.ts`(전문) — 클라이언트 행과 `custom_fields` GIN 보일러플레이트
    - `db/migrations/0010_revenue_entries.sql` 1–5행(락 타임아웃 한 쌍) · `db/migrations/meta/_journal.json` — **앞 플랜들이 0010~0015(04-02·04-10·04-06·04-27·04-13·04-41)를 더한 상태이므로 현재 상태를 먼저 읽는다**(04-41의 계약 컬럼 삭제 뒤라 이 플랜이 0016) · `.squawk.toml`(고치지 않는다)
    - `repositories/revenue-entries.ts`(04-02·04-41 — 조회·버전 조건 수정·클라이언트 id 삽입 `ON CONFLICT (id) DO NOTHING` 형태) · `lib/db-transaction.ts`(`withTransaction` — 04-32 뒤 `SET LOCAL lock_timeout`)
    - `domain/revenue/index.ts`의 `saveEntries`(04-41 뒤 — `moneyToColumns`·`fxRateTouched`·재전송 판정·커밋 뒤 환율 기억 형태) · `domain/quotes/lines.ts`의 `CellFormatError`·`SaveRejectedError`(지금 279–309행 — 칸 이유를 모아 한 번에 거부하는 형태, 이 플랜이 따르는 선례) · `domain/money/index.ts`(`toKrw`·`moneyToColumns`·04-40의 `normalizeMoneyInput`·`MoneyInputError`)
    - `domain/action-log/record.ts`(04-32 뒤 — `recordAction`의 `{ tx }` 인자) · `.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md`(`domain/rules/deny-write.ts`의 `denyWrite` 허용 필드) · `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md`(견적 줄 새 줄 id 모양) · `04-32-SUMMARY.md`(「잠금 안 전역 db 호출 금지」 — ARCHITECTURE §4-8)
    - `domain/permissions/info-items.ts`(04-16 뒤 형태) · `domain/permissions/menus.ts`(`pnl` 키가 이미 있음 — 고치지 않는다)
    - `test/integration/setup.ts`(TRUNCATE 목록 — 자식 → 부모 순서) · `test/integration/revenue-entries.test.ts`(준비·단언 형태)
  </read_first>
  <behavior>
    - 경영관리(`pnl` 쓰기 · `reserve.amount` 노출) viewer가 클라이언트 A에 입금 1,000,000(3/1) · 출금 300,000(3/5)을 한 배치로 저장 → 통과, `listReserves`의 두 줄 잔액 1,000,000 → 700,000, 클라이언트 A 최종 잔액 700,000
    - 이어서 출금 800,000(3/10)을 저장 → `이 줄 뒤 잔액 -100,000 · 금액을 줄이거나 입금 줄 먼저`(그 줄의 금액 칸)로 전부 거부, DB 재조회로 줄 수 2 그대로
    - 입금 1,000,000(3/1) · 출금 800,000(2/1)을 한 배치로 → 2/1 마감이 음수라 거부 — 마지막 잔액(200,000)만 보고 통과시키지 않는다
  </behavior>
  <action>
① **표.** `db/schema/reserve-entries.ts`: `id` uuid pk · `client_id` FK → `vendors`(restrict) · `entry_date` date NOT NULL · `direction` text NOT NULL + CHECK(`deposit`·`withdrawal`) · 금액 Money 묶음(04-01 헬퍼 `moneyColumns("amount")`) · `project_id` FK NULL(선택) · `evidence_type` text NULL(Phase 3 증빙 종류 코드표 값) · `tax_invoice_number` text NULL · `note` text NULL · `version` integer DEFAULT 1(04-02 선례 — §7-3 줄 버전 충돌) · `source` · `custom_fields` jsonb · 보관함 두 컬럼 · 타임스탬프(`created_at`이 같은 날·같은 구분 안의 순서 — 별도 순서 컬럼을 만들지 않는다, 엔지니어링 리뷰 B §2). `id`는 기본값(`gen_random_uuid()`)을 두되 새 줄은 클라이언트가 만든 UUID를 쓴다(ENG-D10). **잔액 컬럼을 만들지 않는다**(갱신 누락 비정규화 — Eng OV-4와 같은 이유). 인덱스 `(client_id, entry_date)`와 `custom_fields` GIN. 배럴(`db/schema/index.ts`)에 더한다.

② **[BLOCKING] 마이그레이션.** `pnpm db:generate --name reserve_entries`(**번호 규칙:** 생성기 번호 = 적용 직전 journal 최고 `idx` + 1 — 04-41의 `0015_drop_project_contract_columns` 뒤라 0016이다. 다르면 생성기 출력 그대로 두고 실제 이름을 SUMMARY에 적는다). 생성 SQL을 읽어 첫 락 요구 문장 앞에 락 타임아웃 한 쌍(0010 선례)을 더한다. 구분 CHECK는 `CREATE TABLE` 안에 있어야 한다 — 생성기가 별도 제약 추가 문장으로 내면 `CREATE TABLE` 안으로 옮긴다(CEO 리뷰 B-22와 같은 squawk 결). 인덱스는 비동시 형태. `pnpm lint:sql` → `bash scripts/dev-db.sh && pnpm db:migrate`. `test/integration/setup.ts`의 TRUNCATE 목록에 리저브 표를 더한다(자식 → 부모).

③ **리포지토리.** `repositories/reserve-entries.ts`: `lockReserveClients(viewer, clientIds, tx)` — `SELECT id FROM vendors WHERE id = ANY($1) ORDER BY id FOR NO KEY UPDATE`(**id 오름차순** — 한 배치가 여러 클라이언트를 건드려도 교착이 없다, 사용자 D5 · **`FOR NO KEY UPDATE`** — 무관한 FK 쓰기를 막지 않는다, 엔지니어링 리뷰 B §1) · 잠근 id 목록을 돌려준다(없는 클라이언트는 목록에 없다 — 호출자가 칸 이유로 바꾼다) · `listActiveEntriesByClients(viewer, clientIds, tx?)`(보관 제외 전부) · `findEntriesByIds(viewer, ids, tx)`(보관 포함) · `insertEntry(viewer, input, tx)`(클라이언트 `id` 포함 — `ON CONFLICT (id) DO NOTHING` + `RETURNING`, 0행이면 `null`) · `updateEntryIfVersionMatches`(`id`·`version`·`client_id` 조건, 0행이면 충돌) · `setEntryArchived(viewer, id, value, tx)`.

④ **도메인(트레이서에 필요한 만큼).** `domain/reserves/index.ts`: 순수 함수 `runningBalance(rows)`(Task 2가 결정표로 완성 — 여기서는 날짜 마감 판정의 첫 형태)와 정렬 비교 함수 하나(날짜 → 구분(입금 먼저) → `created_at` → `id`) · `saveReserves(viewer, { rows, archivedIds }, deps?: { now?; afterLock?; recordAction?; rememberFxRate? })`: **트랜잭션 앞에서** 쓰기 권한(`can(viewer, "pnl", "write")` + `visible(viewer, "reserve.amount")` — 권한 거부는 Task 3가 완성)을 계산 → `withTransaction` → 관련 클라이언트 id(새 줄의 `clientId` + 기존·보관 대상 줄의 저장된 `client_id` — 같은 `tx`로 읽는다) → `lockReserveClients` → `deps.afterLock?.()`(04-20 규약 — 경합 테스트 주입 지점) → 잠긴 tx로 그 클라이언트들의 원장 전체를 읽음 → 배치를 메모리에서 적용(새 줄·수정·보관) → `runningBalance` → 음수 날짜가 있으면 **아무것도 쓰지 않고** 그 날짜 마지막 줄의 금액 칸 오류(`SaveRejectedError` — 04-04 셀 오류 경로)로 전부 거부 → 없으면 쓰기와 행동 로그(`recordAction(viewer, entry, { tx })` — 04-32)를 같은 트랜잭션에 → 커밋 뒤 환율 기억(Task 2). 잠금 안에서 전역 db를 부르지 않는다(04-32 규칙). `listReserves(viewer, { page })`의 첫 형태(한 클라이언트 전체 + 잔액). 원화 환산은 `domain/money` 함수로만 한다(`plant8/money-boundary`는 `Money` 타입 산술만 잡으므로 원화 `number` 합의 위치는 리뷰로 지킨다). `domain/permissions/info-items.ts`에 `{ key: "reserve.amount", label: "리저브 금액", staffDefault: false }`를 더한다 — **새 메뉴를 만들지 않는다**.

⑤ **통합(트레이서 증명).** `test/integration/reserve-entries.test.ts`(신규)에 `behavior` 세 줄 — DB 재조회로 줄 수·잔액·거부 무변경을 단언한다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다 — CHECK가 별도 문장이거나 락 타임아웃이 빠졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- .squawk.toml domain/permissions/menus.ts ui/shell/role-menu.ts</automated>
    <fails_when>db:migrate가 0이 아닌 코드로 끝나거나(`error:` 줄), diff 출력이 비어 있지 않다 — 린트를 피하려고 `.squawk.toml`을 고쳤거나 메뉴 레지스트리·셸 매핑을 건드렸다(D-23 · B-14)</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_reserve_entries'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project integration test/integration/reserve-entries.test.ts</automated>
    <fails_when>eslint·tsc error가 나오거나(`plant8/money-boundary` 위반이면 `Money` 값을 금액 모듈 밖에서 계산했다) 통합 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - `reserve_entries`에 잔액 컬럼과 별도 순서 컬럼(`sort_key`)이 없고, 구분 CHECK가 `CREATE TABLE` 안에 있으며, 인덱스가 `(client_id, entry_date)`이고, 마이그레이션이 락 타임아웃 한 쌍으로 시작한다 · `.squawk.toml`·`menus.ts`·`role-menu.ts` diff 0줄
    - 번호 검증 명령이 초록이다 — 계획 번호 0016과 다르면 실제 이름이 SUMMARY에 있다
    - `saveReserves`가 트랜잭션 안에서 `lockReserveClients`(id 오름차순 `FOR NO KEY UPDATE`) 뒤에 원장을 읽고 판정한다 · 권한 판정이 트랜잭션 앞이고 잠금 뒤 읽기·로그가 `tx`다
    - 통합 테스트가 잔액 계산 · 마감 음수 전부 거부(DB 무변경) · 중간 날짜 음수를 DB 재조회로 단언한다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>리저브 표가 실제 DB에 있고, 한 클라이언트의 저장이 잠금 → 원장 재조회 → 날짜 마감 판정 → 쓰기로 끝나며 음수가 되는 배치는 한 줄도 쓰지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 잔액 규칙 결정표(날짜 마감 · 같은 날 입금 먼저 · 원화) + 입력 계약(`normalizeMoneyInput` 검증 · 참조 검증 · 클라이언트 잠금 · 재전송(ENG-D10) · 커밋 뒤 최근 환율 · 수정 로그(ENG-D8) · 거부 경고)</name>
  <files>domain/reserves/index.ts, repositories/reserve-entries.ts, test/unit/domain/reserve-balance.test.ts, test/integration/reserve-entries.test.ts</files>
  <read_first>
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-10·B-11·B-12·B-16·B-17·B-26 절(Grep `### B-1`·`### B-26`)
    - `domain/revenue/index.ts`의 `saveEntries`·`saveRevenue`(04-41 뒤 — `fxRateTouched` → 커밋 뒤 `rememberFxRate` · 새 줄 재전송 판정) · `domain/quotes/lines.ts` 279–309행(검증 오류를 셀 오류로 모으는 형태) · `domain/money/currency.ts`(`rememberFxRate`·`recentFxRate`) · `domain/money/index.ts`(04-40 `normalizeMoneyInput`·`MoneyInputError`)
    - `lib/kst-date.ts`(04-29 — 달력 날짜 검증 도우미가 있으면 재사용) · 04-20의 `denyWrite`(`.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md`) · Phase 3 증빙 종류 코드표 조회(Grep `evidence` — `domain/code-tables`)
    - `domain/action-log/record.ts` 10–70행(`document_update` — 관리자가 끌 수 있는 종류 · 상시 기록 종류 목록은 고치지 않는다)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) Copywriting 375행(`Error — 셀(리저브 클라이언트, CEO-D6)`) · 381–382행(`Error — 리저브 잔액` · `Error — 리저브 복원(보관함 — 관리자 화면)`) · 365행(`Error — 저장(재전송 불일치, ENG-D10)` — 04-12 문구로 통일)
  </read_first>
  <behavior>
    - `runningBalance`: 입금 1,000,000 · 출금 300,000 → 줄 잔액 1,000,000 → 700,000 · 입금 1,000,000(3월) · 출금 800,000(1월) → 1월 마감 음수(첫 음수 날짜 · 그날 마지막 줄 id · 잔액 −800,000) · **같은 날 출금 500,000을 입금 1,000,000보다 먼저(`created_at`이 앞선다) 적음 → 그날 마감 500,000이라 통과, 표시 순서는 입금 → 출금**(사용자 D19-2) · 같은 날 입금 100,000 · 출금 300,000 → 마감 음수 · 같은 날짜·같은 구분 두 줄은 `created_at` → `id` 순(결정적 — `created_at`이 같으면 `id`) · 외화 줄은 원화 환산액으로 누적 — USD 1,000 @1,300 입금 뒤 USD 1,000 @1,400 출금 → −100,000 음수(사용자 D19-1) · 클라이언트가 다르면 섞이지 않는다
    - 검증(통합 — 각각 칸 이유, DB 무변경): KRW 줄에 환율 1,350을 실은 요청 → 원화 = 금액 × 1로 저장, 환율 1 · USD 환율 0 → 거부 · 금액 0·음수 → 거부 · 날짜 `2026-02-30` → 거부(PG 오류 아님) · 원화 환산 2,147,483,648 이상 → 거부
    - 참조·상태 검증(통합 — GAP 3 b–d, 각각 칸 이유, DB 무변경, PG 23503 아님): 이미 보관된 줄을 수정하는 배치 · 이미 보관된 줄을 다시 `archivedIds`에 실은 배치 → 거부 · 클라이언트 A 줄에 클라이언트 B 프로젝트의 `projectId` → `이 프로젝트의 클라이언트가 아닙니다` · 없는 `clientId`(임의 UUID) → `클라이언트를 찾을 수 없습니다` · 코드표에 없는 `evidenceType` → `증빙 종류를 고르세요`
    - 재전송(통합 — ENG-D10): 새 입금 줄(클라이언트 UUID `X`)을 저장 → 커밋 → 같은 배치를 다시 저장 → 성공 · DB에 `X` 한 행 · 클라이언트 최종 잔액이 한 번만 반영 · 두 번째 저장이 `document_create`를 더 남기지 않는다 · 같은 `X`에 금액을 바꿔 다시 보냄 → `이미 저장된 줄과 값이 다름 · 새로 고침`, DB 값 첫 저장 그대로 · 다른 클라이언트 줄의 id를 새 줄 `id`로 보냄 → 같은 거부
    - 기존 줄의 `clientId`를 바꾼 요청 → `클라이언트는 첫 저장 뒤 잠김 · 새 줄로 적어 주세요`, DB 무변경(사용자 D6)
    - 서버 셀 단계: 쓰기 권한자에게 새 줄의 클라이언트 칸 `edit`, 기존 줄의 클라이언트 칸 `locked`, 잔액 칸은 누구에게나 `readonly`
    - 환율을 고친 USD 저장(`fxRateTouched: true`, 1,380) → 커밋 뒤 `fx.recent_rate.USD` = 1380 · 고치지 않은 저장 → 설정 무변경(B-12) · **환율을 고친 USD 줄(1,380)과 잔액을 음수로 만드는 출금이 한 배치 → 전부 거부 · `fx.recent_rate.USD` 무변경(GAP 3e — 커밋 뒤에만 기억)**
    - 기존 줄 금액 300,000 → 250,000 · 날짜 3/5 → 3/6 수정 → 행동 로그 `document_update` 한 줄, detail `{ entryId, clientId, changed: { amount: [300000, 250000], entryDate: ["2026-03-05", "2026-03-06"] } }` — 값이 가려지지 않은 원래 숫자다(B-16 · ENG-D8) · 새 줄 → `document_create` · 보관 → `archive`
    - 잔액 음수 · 검증 · 클라이언트 변경 · 재전송 불일치 거부마다 `write.denied` 경고 한 번(규칙 · 클라이언트·줄 id, 금액 키 없음)
  </behavior>
  <action>
① 단위 결정표(`behavior` 첫 줄)를 `test/unit/domain/reserve-balance.test.ts`에 먼저 쓴다(RED). 특히 **중간 날짜에서만 음수**, **같은 날 출금이 먼저 적힌 경우**, **원화 누적의 외화 경우**를 반드시 넣는다 — 이 셋이 「마지막 잔액만 보는」·「줄마다 보는」·「외화 금액끼리 더하는」 잘못된 구현을 잡는다.

② **`runningBalance(rows)` 완성(순수 — DB 없음).** 클라이언트별로 Task 1의 정렬 비교 함수(날짜 → 구분(입금 먼저) → `created_at` → `id` — 한 배치의 새 줄은 트랜잭션 시각을 `created_at`으로 본다)로 정렬해 원화 환산액을 누적하고, 줄마다 표시 잔액과 **날짜마다 마감 잔액**을 계산해 `{ rows: [{ id, balanceKrw }], closingByDate, firstNegative: { clientId, date, balanceKrw, lastRowId } | null }`을 돌려준다. 판정은 날짜 마감 잔액 < 0 하나다(사용자 D19-2). 원화 기준이라는 사실과 우회(입금 때 환율로 출금 적기)를 주석 한 줄로 둔다(사용자 D19-1). 잔액 산술은 `domain/money`를 지난다. 규모가 커지면 리포지토리 윈도 함수로 바꿀 수 있도록 순수 함수로 분리한 선택과 근거(줄 수 규모 · 이유 문자열에 날짜가 필요 · 산술 단일 지점)를 SUMMARY에 적는다.

③ **입력 계약(B-17 · D6 · B-12 · 엔지니어링 리뷰 B §2 · GAP 3).** 금액은 줄마다 04-40의 `normalizeMoneyInput`을 먼저 지난다 — KRW 환율 1 고정 · USD 환율 > 0 · 원화 환산 정수 범위를 이 파일에서 따로 구현하지 않고, `MoneyInputError`의 `message`를 그 줄 금액(또는 환율) 칸의 이유로 바꾼다. 이 경로가 더하는 검증: 금액 > 0(부호는 구분 — 음수 출금 금지) · 날짜는 달력 왕복이 맞는 `YYYY-MM-DD`(PG 22008로 가지 않는다 — 04-29 도우미가 있으면 재사용) · 클라이언트가 `lockReserveClients`가 돌려준 목록에 있음(없으면 `클라이언트를 찾을 수 없습니다` — PG 23503으로 가지 않는다) · `projectId`가 있으면 같은 tx로 읽은 그 프로젝트의 `client_id` = 줄의 클라이언트(아니면 `이 프로젝트의 클라이언트가 아닙니다`) · `evidenceType`이 있으면 트랜잭션 앞에서 읽은 증빙 종류 코드표 값 중 하나(아니면 `증빙 종류를 고르세요`) · 수정·보관 대상 줄이 이미 보관돼 있으면 거부(`보관된 줄 · 새로 고침`) — 어긋나면 그 줄·칸의 한국어 이유(`CellFormatError` 모음 → `SaveRejectedError`, 04-04 경로 — `domain/quotes/lines.ts` 279–309행 선례). 기존 줄의 `clientId`가 저장된 값과 다르면 같은 경로로 `클라이언트는 첫 저장 뒤 잠김 · 새 줄로 적어 주세요`. `listReserves`의 DTO에 서버 셀 단계(`cellEditability` — 기존 줄 클라이언트 `locked`, 잔액 `readonly`, 쓰기 권한 없으면 전부 `locked`)를 싣는다.

③-b **재전송(ENG-D10).** 새 줄(`isNew: true`)의 클라이언트 UUID를 잠금 뒤 같은 tx의 `findEntriesByIds`로 먼저 본다 — 있으면 같은 클라이언트·같은 저장값(`<probe_fallback>` 목록, 정규화 뒤 비교)일 때만 재전송으로 보고 원장 적용·쓰기·행동 로그에서 뺀다(잔액을 두 번 반영하지 않는다). 다르거나 남의 클라이언트 줄이면 `이미 저장된 줄과 값이 다름 · 새로 고침`로 배치 전부 거부. 없으면 `insertEntry`(`ON CONFLICT (id) DO NOTHING`)로 넣고, 0행이 오면 같은 거부. 쓴 줄이 하나도 없는 저장은 행동 로그를 남기지 않는다.

③-c **커밋 뒤 환율 기억(D-71 · GAP 3e · 04-32 규칙).** `fxRateTouched`인 USD 줄의 `(통화, 환율)`을 모아 두고 `withTransaction`이 **성공적으로 끝난 뒤** 한 번 `rememberFxRate`(04-41의 매출 경로와 같은 방식 — 04-12·04-41-SUMMARY의 커밋 뒤 방식을 따른다). 트랜잭션 안에서 부르지 않는다 — 거부·롤백된 배치가 설정을 바꾸던 결함과 잠금 안 풀 연결 대기를 함께 없앤다.

④ **수정 로그(B-16 · ENG-D8).** 기존 줄 수정은 저장 전 값(Task 1의 잠긴 tx 재조회)과 비교해 바뀐 `amount`(원화 환산액)·`entryDate`·`direction`만 `changed`에 `[전, 후]`로 싣고 `document_update`로 남긴다(`recordAction(…, { tx })` — 같은 tx). 값을 가리지 않는다 — 활동 기록은 기본값으로 시스템관리자만 보고, 사용자가 가리기 안을 뒤집었다(ENG-D8). 새 줄 `document_create`, 보관 `archive`. 행동 종류·상시 기록 설정은 바꾸지 않는다.

⑤ **거부 경고(사용자 D19-3 · B-26 · 엔지니어링 리뷰 `denyWrite`).** `saveReserves`의 모든 거부를 04-20의 `denyWrite(viewer, rule, { clientIds, entryIds }, err)`로 던진다 — 규칙은 `reserve.balance-negative`·`reserve.input`·`reserve.client-locked`·`reserve.replay-mismatch`·`reserve.forbidden` 중 하나이고, 도우미의 허용 필드에 금액·메모가 없어 실릴 수 없다. 경고 로그를 손으로 부르는 새 지점을 만들지 않는다.

⑥ 통합(`behavior` 둘째~마지막 줄)을 `reserve-entries.test.ts`에 더한다 — 로그는 `vi.spyOn(log, "warn")`과 `action_log` 재조회로 단언한다(GREEN).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/reserve-balance.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 날짜 마감 판정·같은 날 입금 먼저·원화 누적·클라이언트 분리 중 하나가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/reserve-entries.test.ts test/integration/settings.test.ts test/integration/action-log.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 위조 환율·0 이하 금액·잘못된 날짜·범위 초과·보관된 줄 수정·남의 클라이언트 프로젝트·없는 클라이언트·코드표 밖 증빙이 저장됐거나 PG 오류로 새었거나, 클라이언트 변경이 통과했거나, 재전송이 두 행을 만들었거나, 거부된 배치가 최근 환율을 바꿨거나, 수정 로그 detail·거부 경고가 계약과 다르다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc error가 나오고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `runningBalance`가 순수 함수이고 결정표에 중간 날짜 음수 · 같은 날 출금 먼저 적힘(통과) · 같은 날 마감 음수 · 외화 원화 누적(−100,000) · 클라이언트 분리 케이스가 있다
    - 판정이 날짜 마감 잔액이고 오류 칸이 그 날짜 마지막 줄이다
    - 다섯 입력 검증이 각각 칸 이유로 거부되고 DB 무변경이다(KRW 위조 환율은 1로 저장) · 금액 규칙이 `normalizeMoneyInput` 호출이고 이 파일에 KRW 환율 고정·범위 비교를 손으로 쓴 코드가 없다
    - 보관된 줄 수정·재보관 · 남의 클라이언트 프로젝트 · 없는 클라이언트 · 코드표 밖 증빙 종류가 각각 칸 이유로 거부되고 PG 오류로 새지 않는다(GAP 3 b–d)
    - 기존 줄 클라이언트 변경이 거부되고 서버 셀 단계가 그 칸을 잠근다
    - 같은 배치 재전송이 한 행·한 번의 잔액 반영이고, 값이 다른 재전송·남의 줄 id가 거부된다 · 삽입이 `ON CONFLICT (id) DO NOTHING`이다(ENG-D10)
    - 환율을 고친 저장만, 커밋된 뒤에만 최근 환율을 갱신한다 — 거부된 USD 배치 뒤 설정 무변경(GAP 3e) · `saveReserves` 트랜잭션 안에 `rememberFxRate` 호출이 없다
    - 수정 로그 detail에 바뀐 칸의 `[전, 후]`가 가려지지 않은 값으로 있다(ENG-D8)
    - 각 거부에서 `write.denied` 한 번, 필드에 금액 없음 · 거부가 `denyWrite`를 지난다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>잔액 판정이 날짜 마감·원화 기준으로 결정표에 고정되고, 위조·잘못된 입력과 클라이언트 변경이 칸 이유로 거부되며, 수정 이력과 거부가 로그에 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 권한(`pnl` 쓰기 + 노출) · 노출 · 보관/복원 · 클라이언트 잠금 경합(사용자 D5 · T5 · B-04 · B-14 · B-15) + 페이지 읽기 · 거부 줄의 쪽 번호(Codex #7) + 전체 게이트</name>
  <files>domain/reserves/index.ts, domain/archive/index.ts, repositories/archive.ts, repositories/reserve-entries.ts, test/integration/reserve-entries.test.ts, test/integration/leak-scan.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md` — `DOMAIN_RESTORERS`의 모양(엔티티 → 도메인 복원 함수), `restore()`가 위임 전에 `admin.archive`를 판정하는지, `restoreQuoteLine` 선례
    - `domain/archive/index.ts`(04-12 뒤 전문) · `repositories/archive.ts` 40–80행(`ArchivableEntry` — `isProtected`·`listArchived`)
    - `domain/permissions/can.ts`·`visible.ts`(D-35 — 둘은 독립) · `domain/permissions/project.ts`(`project(viewer, row, spec)`) · `domain/permissions/dto-registry.ts`
    - `lib/paging.ts`(그룹 C — `clampPage`·`pageCountFrom`, 공용 쪽 보정 · `domain/projects/list-view`를 import하지 않는다)
    - `test/integration/lock-race.ts`(04-20 — `deferred`·`waitForLockWaiter`) · `test/integration/project-status.test.ts`의 경합 describe(풀 크기 확인 형태)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-04·B-05·B-14·B-15 절
  </read_first>
  <behavior>
    - 권한: `pnl` 보기만 있는 사람의 저장 → `ForbiddenError`, DB 무변경, `write.denied` 한 번 · **`pnl` 쓰기는 있고 `reserve.amount` 노출이 꺼진 계급의 저장(잔액을 음수로 만드는 출금을 실어도) · 복원 → 각각 `ForbiddenError`, DB 무변경, 오류 메시지에 숫자(`/\d/`)가 하나도 없고 `write.denied` 필드에도 금액이 없다(GAP 3a — 잔액을 떠보는 길 차단)** · `pnl` 쓰기는 있고 `admin.archive`는 없는 경영관리가 배치의 `archivedIds`로 출금 줄 보관 → 통과(보관함에 항목) · 입금 줄 보관으로 중간 날짜가 음수 → 거부, DB 무변경
    - 범용 경로: `archive(viewer, "reserve_entry", id)`(보관함 권한 있는 관리자) → 보호 행 거부, DB 무변경
    - 복원: 보관해 둔 출금을 보관함 `restore(viewer, "reserve_entry", id)`로 → 복원하면 어느 날짜가 음수 → `복원하면 {날짜} 잔액이 {잔액}이 됩니다 · 리저브 대장에서 출금 줄을 먼저 고쳐 주세요`, DB 무변경 · 음수가 안 되는 출금 복원 → 통과 + `restore` 로그 · `pnl` 쓰기 없는 사람의 복원 → 거부
    - 노출(키 집합): 기획 PM(`pnl` 보기 없음) → `listReserves` 빈 결과, 건수·그룹 키 없음 · `pnl` 보기는 있고 `reserve.amount` 노출이 꺼진 계급 → 같은 빈 결과(부분 노출 없음) · 경영관리 → 줄·잔액 전부
    - 페이지: 한 클라이언트 51줄 → `page=2`의 첫 줄 잔액 = 전체 누적 기준(1쪽 마지막 줄 잔액 ± 그 줄 금액) · 그룹 최종 잔액이 두 쪽에서 같다 · `page=99` → 마지막 쪽
    - 거부 줄의 쪽 번호(Codex #7): 한 클라이언트 51줄(1쪽 첫 줄이 큰 입금, 2쪽에 큰 출금)에서 1쪽 입금 줄 금액을 줄인 배치 → 음수 날짜가 2쪽 줄에서 생겨 거부 · 거부 정보가 `{ entryId, entryDate, clientId, clientName, balanceKrw, page: 2 }` · 새 줄에서 생긴 음수 → `page: null`
    - 경합(사용자 D5): 풀 크기 ≥ 3 단언 → 연결 A의 출금 저장(잔액을 0 가까이 만드는 줄)이 `deps.afterLock`에서 멈춘 사이, 연결 B의 같은 클라이언트 출금 저장이 `waitForLockWaiter`로 락 대기 확인 → A를 풀면 A 커밋, B는 새 잔액으로 판정되어 음수 거부 → DB의 마감 잔액이 모든 날짜에서 0 이상 · 다른 클라이언트의 저장은 기다리지 않는다
  </behavior>
  <action>
① 통합(`behavior` 전부)을 먼저 쓴다(RED). 경합은 04-20의 `deferred`·`waitForLockWaiter`와 `saveReserves`의 `deps.afterLock`으로 순서를 강제한다 — 고정 지연이나 여러 요청을 한꺼번에 던지는 형태를 쓰지 않는다. 04-32 뒤 `withTransaction`은 `SET LOCAL lock_timeout = '5s'`라 락 대기 확인 뒤 곧바로 `deferred`를 풀어 대기를 5초보다 짧게 둔다.

② **권한(B-14 · 엔지니어링 리뷰 B §1).** `saveReserves`·`restoreReserve` 입구에서, **`withTransaction` 앞에서** `can(viewer, "pnl", "write")`와 `visible(viewer, "reserve.amount")`를 둘 다 계산한다 — 하나라도 거짓이면 숫자 없는 `ForbiddenError`(`리저브를 기록할 권한이 없습니다`)를 `denyWrite`(규칙 `reserve.forbidden`)로 던지고 잔액 계산에 들어가지 않는다(잔액이 실린 거부 문구는 노출이 있는 사람에게만 닿는다). 잠금 안에서 두 판정을 다시 부르지 않는다(04-32 규칙). 보관은 배치의 `archivedIds`로만: 대상 줄을 잠긴 tx로 읽어 같은 클라이언트 잠금·같은 `runningBalance` 판정을 지난 뒤 `setEntryArchived` + `archive` 로그 — `admin.archive`를 요구하지 않는다(T5).

③ **범용 보관 차단 · 복원 위임(B-04 · OV-2).** `repositories/archive.ts`의 `ARCHIVABLE_TABLES`에 `reserve_entry` 항목(라벨 `리저브`, `listArchived` — 이름은 `{날짜} {클라이언트} {구분} {금액}`이 아니라 금액 없는 `{날짜} {클라이언트} {구분}` — 보관함은 `admin.archive` 화면이라 리저브 노출 게이트 밖이다)을 더하고 `isProtected`가 늘 참이게 한다(주석: 리저브 보관은 잔액 판정을 지나는 `saveReserves`만 — 범용 `archive()`는 잔액을 보지 않는다). `domain/archive/index.ts`의 `DOMAIN_RESTORERS`(04-12)에 `reserve_entry` → `restoreReserve`를 더한다. `domain/reserves/index.ts`의 `restoreReserve(viewer, id, deps?: { afterLock?; recordAction? })`: **트랜잭션 앞에서** `pnl` 쓰기 + `reserve.amount` 노출(②와 같은 판정 — 04-12의 `restore()`가 위임 전에 `admin.archive`를 판정하지 않으면 여기서 그것도 판정) → `withTransaction` → 같은 tx로 줄(보관 포함 — 클라이언트는 바뀌지 않으므로 잠금 전에 읽어도 된다) → `lockReserveClients([clientId])` → 원장 + 이 줄로 `runningBalance` → 음수면 복원 거부 문구(`behavior`)를 `denyWrite`(규칙 `reserve.restore`)로 던짐 → `setEntryArchived(false)` + `restore` 로그(`recordAction(…, { tx })` — 같은 tx). 04-12의 보관함 위임이 바깥 `tx`를 넘기면 그 tx를 쓰고 새 트랜잭션을 열지 않는다(04-12-SUMMARY의 위임 모양 그대로).

④ **노출과 페이지(B-15 · D-91).** `RESERVE_DTO_SPEC`은 모든 필드를 `reserve.amount`로 게이트하고(대장 전체가 숨김 정보 — 날짜·메모만 싣는 부분 노출 금지) `registerDto`로 등록하며, 쪽의 줄들은 04-32의 `projectMany`로 투영한다(행 수와 무관하게 정보 항목 수만큼 조회), `test/integration/leak-scan.test.ts`의 side-effect import 목록에 `@/domain/reserves`를 더한다(D-38 — 없으면 누수 스캔이 이 DTO를 모른다). `listReserves(viewer, { page })`: `can(viewer, "pnl", "view")`와 `visible(viewer, "reserve.amount")`가 둘 다 참일 때만 — 아니면 `{ rows: [], page: 1, pageCount: 0 }`(총건수·그룹 잔액 없음). 참이면 보관 제외 전체를 (클라이언트 이름 → Task 1의 정렬 비교 함수)로 정렬해 `runningBalance`로 전체 잔액을 계산한 **뒤** 공용 `lib/paging.ts`의 `clampPage`·`pageCountFrom`(그룹 C)으로 쪽을 정해 50줄을 자르고, 그 쪽에 나타난 클라이언트의 최종 잔액(`clientBalances`)과 `page`·`pageCount`·`total`을 돌려준다. 페이지 크기 상수는 `lib/paging.ts`에 있으면 그것을, 없으면 이 모듈 상수 하나(SUMMARY) — `domain/projects/list-view`를 import하지 않는다.

④-b **거부 줄의 쪽 번호(Codex #7).** 잔액 음수 거부(`reserve.balance-negative`)는 셀 오류와 함께 배치 수준 정보 `{ entryId, entryDate, clientId, clientName, balanceKrw, page }`를 돌려준다 — `page`는 그 줄이 **지금 목록**(`listReserves`와 같은 정렬 — 클라이언트 이름 → 비교 함수)에서 놓인 쪽 번호이고, 줄이 새 줄(DB에 없음)이면 `null`(새 줄은 화면의 현재 쪽에 있다). 계산은 잠긴 `tx`로 보관 제외 줄의 정렬 키(id · 클라이언트 이름 · 날짜 · 구분 · `created_at`)만 읽어 같은 비교 함수로 위치를 구한다(정렬 정의를 둘로 두지 않는다). 04-42가 이 정보로 다른 쪽에 있는 줄의 거부를 보이고 그 쪽으로 옮긴다.

⑤ **전체 게이트(CLAUDE.md).** `CI=true pnpm test` 한 번. 실패하면 `systematic-debugging`.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/reserve-entries.test.ts test/integration/archive.test.ts test/integration/visibility.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 테스트가 타임아웃으로 끝난다 — 보기 권한이나 노출 없는 쓰기 권한으로 저장·복원했거나 그 거부 메시지에 숫자가 있었거나, 경영관리가 보관함 권한 없이 지우지 못했거나, 범용 보관·복원이 잔액 판정을 비켜 갔거나, 노출이 꺼진 계급에 줄·건수가 샜거나, 쪽마다 잔액이 달랐거나, 거부 줄의 쪽 번호가 틀렸거나, 동시 출금 둘이 함께 통과해 음수가 커밋됐다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc error가 나오고 종료 코드가 0이 아니다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - 저장·복원 입구가 트랜잭션 앞의 `can(viewer, "pnl", "write")` **그리고** `visible(viewer, "reserve.amount")`이고, 보기 권한 저장과 노출 꺼진 쓰기 권한자의 저장·복원이 숫자 없는 `ForbiddenError`로 거부된다(GAP 3a) · 경영관리는 `admin.archive` 없이 배치로 보관한다
    - 범용 `archive()`의 리저브 보관이 보호 행으로 거부되고, 보관함 복원이 `DOMAIN_RESTORERS` → `restoreReserve`(같은 tx · 클라이언트 잠금 · 잔액 판정)를 지난다 — 음수가 되는 출금 복원 거부 뒤 DB 무변경
    - 보관함 목록 이름에 금액이 없다
    - `listReserves`가 `pnl` 보기와 `reserve.amount` 노출 둘 다 요구하고, 하나라도 없으면 빈 결과에 건수·그룹 키가 없다(키 집합 단언 — 금지 항목) · 누수 스캔 import에 `@/domain/reserves`가 있다
    - 51줄 두 쪽의 잔액이 전체 누적 기준이고 `page=99`가 마지막 쪽이다 · 쪽 보정이 `lib/paging.ts`에서 온다
    - 잔액 음수 거부가 거부 줄의 날짜·클라이언트·잔액·쪽 번호(새 줄이면 `null`)를 돌려준다(Codex #7) · 쪽 번호 계산이 `listReserves`와 같은 비교 함수다
    - 경합 테스트가 풀 크기와 락 대기를 확인한 뒤 「뒤 저장이 새 잔액으로 거부」와 「모든 날짜 마감 ≥ 0」을 단언한다(사용자 D5 — 금지 항목)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>리저브 쓰기가 `pnl` 쓰기 권한과 클라이언트 잠금 아래 한 줄로 서고, 보관·복원·동시 저장 어느 경로로도 음수 잔액이 커밋되지 않으며, 리저브를 볼 수 없는 계급의 응답에는 줄도 건수도 없다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 액션 → `saveReserves`(04-42의 액션이 부른다) | 날짜·구분·금액·환율·클라이언트·보관 id가 넘어온다. 잔액은 이 경계를 넘어와도 무시된다(계산값) |
| 동시 요청 → 한 클라이언트 원장 | 두 저장·저장과 복원이 겹치면 판정과 쓰기 사이에 원장이 바뀐다 |
| 보관함(관리자 화면) → 리저브 복원 | 범용 복원이 잔액 판정을 비켜 갈 수 있다 |
| domain 출구 → 화면 | 리저브 줄이 정보 노출표를 지나야 실린다. 기획본부에게는 메뉴·노출 양쪽에서 막힌다 |
| 리저브 줄 → 앞으로 생길 집계·내보내기 | Phase 9·10의 손익·Excel 경로가 이 줄을 말없이 더하면 노출이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-42 | Information Disclosure | 리저브 DTO 출구 | high | mitigate | `RESERVE_DTO_SPEC`의 모든 필드를 기본 숨김 항목 `reserve.amount`로 게이트하고 `listReserves`가 `pnl` 보기 + 노출 둘 다 요구한다 — 하나라도 없으면 빈 결과에 건수도 없다(B-15). 키 집합 통합 테스트와 D-38 누수 스캔 import로 고정 |
| T-04-43 | Tampering | 잔액 | high | mitigate | 잔액을 저장하지 않고 요청 페이로드에서도 받지 않는다. 서버가 잠긴 tx에서 클라이언트 원장 전체를 읽어 계산한다 |
| T-04-44 | Tampering | 음수 잔액 회피(판정) | high | mitigate | 저장·수정·보관·복원 네 경로가 같은 `runningBalance`를 지나고, 판정이 마지막 줄이 아니라 **모든 날짜 마감**을 본다(사용자 D19-2). 중간 날짜 · 같은 날 순서 케이스를 단위·통합에 고정 |
| T-04-44b | Tampering | 음수 잔액 회피(동시성) | high | mitigate | 저장·보관·복원이 관련 클라이언트 행을 id 오름차순 `FOR NO KEY UPDATE`로 잠근 뒤 판정한다(사용자 D5 · B-05 — 자기끼리는 한 줄로 서고 무관한 FK 쓰기는 막지 않는다, 엔지니어링 리뷰 B §1). 두 연결 결정적 경합 테스트로 「뒤 저장 거부 · 모든 날짜 마감 ≥ 0」을 고정 |
| T-04-44c | Elevation of Privilege | 보관함 경유 우회 | high | mitigate | 범용 `archive()`는 리저브를 보호 행으로 거부하고, 복원은 `DOMAIN_RESTORERS` → `restoreReserve`(같은 tx · 잠금 · `pnl` 쓰기 · 잔액)로만 한다(B-04 · OV-2) |
| T-04-44d | Tampering | 위조 입력(환율·부호·날짜·범위·참조) | medium | mitigate | 04-40의 `normalizeMoneyInput`(KRW 환율 서버 고정 1 · USD 환율 > 0 · 정수 범위) + 이 경로의 금액 > 0 · 달력 날짜 · 존재하는 클라이언트 · 같은 클라이언트의 프로젝트 · 코드표 증빙 종류 · 보관 줄 수정 금지를 서버가 검증해 칸 이유로 거부한다 — PG 22008·23503으로 새지 않는다(B-17 · 엔지니어링 리뷰 B §2 · GAP 3 b–d). 클라이언트는 첫 저장 뒤 바꿀 수 없다(사용자 D6) |
| T-04-44e | Elevation of Privilege / Information Disclosure | 쓰기 권한 · 잔액 떠보기 | high | mitigate | 리저브 쓰기(저장·보관·복원)는 트랜잭션 앞에서 `pnl` 쓰기 **그리고** `reserve.amount` 노출로 판정하고, 하나라도 없으면 숫자 없는 `ForbiddenError`다 — 잔액이 실린 거부 문구는 노출이 있는 사람에게만 닿는다(B-14 · 엔지니어링 리뷰 B §1, GAP 3a 통합). 메뉴 레지스트리는 바뀌지 않는다 |
| T-04-45 | Repudiation | 리저브 줄 수정·삭제 | medium | mitigate | 새로 만들기·수정(바뀐 칸 `[전, 후]` — 값을 가리지 않는다, B-16 · ENG-D8)·보관·복원이 같은 tx로 행동 로그를 남기고, 삭제는 보관이라 행이 DB에 남는다. 거부는 `denyWrite`로 `write.denied`(금액 없음 — 사용자 D19-3). `document_update`를 관리자가 끌 수 있는 기존 규약은 그대로이고(ENG-D8 확정), 활동 기록은 기본값으로 시스템관리자만 본다 |
| T-04-45b | Tampering | 응답을 잃은 재전송(잔액 이중 반영) | medium | mitigate | 새 줄이 클라이언트 UUID를 싣고 잠금 뒤 같은 tx로 먼저 찾아, 같은 클라이언트·같은 값이면 재전송으로 빼고 아니면 거부하며, 삽입은 `ON CONFLICT (id) DO NOTHING`이다 — 한 행·한 번 반영을 통합으로 고정(ENG-D10) |
| T-04-45c | Denial of Service | 잠금 안 풀 연결 대기 · 거부 배치의 설정 변경 | medium | mitigate | `can`·`visible`·코드표는 트랜잭션 앞, 잠금 뒤 읽기·로그는 `tx`, 최근 환율 기억은 커밋 뒤 — 같은 클라이언트에 요청이 몰려도 잠금을 쥔 채 두 번째 풀 연결을 기다리지 않고, 거부된 배치가 `fx.recent_rate`를 바꾸지 않는다(04-32 규칙 · GAP 3e). `FOR NO KEY UPDATE`라 그 거래처를 가리키는 무관한 FK 쓰기도 막지 않는다 |
| T-04-46 | Denial of Service | 그룹 전체 읽기 · 클라이언트 잠금 대기 | low | accept | 클라이언트당 줄 수가 수십~수백이고 잠금은 그 클라이언트 행뿐이다(10~30명 규모). 잔액 계산을 순수 함수로 분리해 규모가 커지면 윈도 함수로 교체할 수 있다 |
| T-04-47 | Denial of Service | 리저브 마이그레이션 | low | mitigate | 신규 표라 락 경합이 없고 락 타임아웃 한 쌍을 둔다. `pnpm lint:sql` 0건 |
| T-04-100 | Tampering | `page` 인자 | low | mitigate | 공용 `lib/paging.ts`의 `clampPage`를 그대로 쓰고 서버가 계산한 전체 줄 배열의 구간으로 자른다 — 잔액이 쪽에 따라 달라지는 통로가 없다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` 통과
- `pnpm lint:sql`이 `Found 0 issues` — `.squawk.toml` 무변경, 마이그레이션 번호 검증 명령 초록
- `CI=true pnpm test` 세 계층 전부 통과
- `domain/permissions/menus.ts`·`ui/shell/role-menu.ts` diff 0줄, `package.json` 의존성 객체(dependencies·devDependencies·optionalDependencies·peerDependencies)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있다 (원문 비교 아님 — 04-03 재개 시 scripts 추가로 깨지지 않게)

</verification>

<success_criteria>

- 경영관리의 리저브 입출금이 클라이언트 잠금 아래 저장되고 잔액이 줄마다 서버 계산으로 나온다
- 어느 날짜에서든 마감 잔액이 음수가 되는 저장·수정·보관·복원·동시 저장이 전부 거부되고 DB가 그대로다
- 리저브가 `pnl` 보기와 노출 둘 다 없는 계급의 응답에 줄도 건수도 싣지 않는다
- 04-42가 액션·화면만 얹으면 되고, Phase 9 RSV-02가 같은 잠금·같은 판정에 매출 충당을 얹을 수 있다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `reserve_entries`(`db/schema/reserve-entries.ts`) — 구분 CHECK(`deposit`·`withdrawal`) · `version` · 금액 묶음 네 컬럼 · 보관함 컬럼(순서는 `created_at`·`id` — 별도 순서 컬럼 없음) · 인덱스 `(client_id, entry_date)` |
| 마이그레이션 | `db/migrations/0016_reserve_entries.sql`(04-41의 0015 뒤) + `meta/{번호}_snapshot.json` + `_journal.json` 항목 |
| 함수 | `runningBalance`(순수 — 날짜 마감) · `listReserves`(노출 게이트 · 페이지) · `saveReserves`(잠금 · 검증 · 보관 배치 · 환율 · 로그) · `restoreReserve`(`DOMAIN_RESTORERS`) |
| 리포지토리 | `repositories/reserve-entries.ts` — `lockReserveClients`(id 오름차순 `FOR NO KEY UPDATE`) · 조회 · 클라이언트 id 삽입(`ON CONFLICT (id) DO NOTHING`) · 버전·클라이언트 조건 수정 · 보관 |
| 정보 항목 | `reserve.amount`(리저브 금액, 기본 숨김 — 대장 전체 게이트) |
| 보관함 | `ARCHIVABLE_TABLES`의 `reserve_entry`(범용 보관 보호 · 금액 없는 이름) · `DOMAIN_RESTORERS`의 `reserve_entry` |
| 로그 | 행동 로그 `document_create`·`document_update`(`changed: [전, 후]` — 값 그대로, ENG-D8)·`archive`·`restore` · 운영 로그 `write.denied`(04-20 `denyWrite` — 금액 없음) |
| 재사용 | 04-40 `normalizeMoneyInput`·`MoneyInputError` · 04-12 `DOMAIN_RESTORERS` · 04-20 `denyWrite`·`lock-race.ts` · 04-32 `recordAction(…, { tx })`·`withTransaction`(`lock_timeout`) · 그룹 C `lib/paging.ts`(`clampPage`·`pageCountFrom`) |
| 거부 정보 | 잔액 음수 거부의 배치 수준 정보 `{ entryId, entryDate, clientId, clientName, balanceKrw, page }`(Codex #7 — 화면 이동은 04-42) |
| 테스트 | `test/unit/domain/reserve-balance.test.ts` · `test/integration/reserve-entries.test.ts`(신규) · `test/integration/setup.ts`(TRUNCATE) · `leak-scan.test.ts`(`@/domain/reserves` import) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 대장 화면 · 클라이언트 열(9번째) 렌더 · `/pnl` 링크(`can` + `visible`) · 액션·레지스트리(누수 스캔의 액션 import 포함) · E2E · 04-OPEN-ITEMS 기록(04-42 — UI-SPEC은 rev 5가 S9 클라이언트 열·리저브 문구를 이미 담아 고치지 않는다) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-05 · 사용자 D5 (T12) — 동시 저장 write skew로 음수 잔액 | CRITICAL | Plan 04-07, Task 1 ③④: `lockReserveClients`(id 오름차순 — 엔지니어링 리뷰로 `FOR NO KEY UPDATE`) 뒤 판정 · Task 3 ①: 두 연결 결정적 경합 |
| B-06 · 사용자 D6 (T12) — 새 줄의 클라이언트를 정할 방법 없음 | CRITICAL | Plan 04-07, Task 2 ③: 새 줄 `clientId` · 첫 저장 뒤 변경 거부 · 셀 단계 잠김 (화면 9번째 열은 Plan 04-42) |
| B-04 · T5 · OV-2 — 복원이 잔액 판정 우회 · 경영관리가 보관함 권한 없이 못 지움 | CRITICAL | Plan 04-07, Task 3 ②③: 배치 보관(`pnl` 쓰기) · 범용 보관 보호 · `DOMAIN_RESTORERS` → `restoreReserve` 한 tx |
| B-11 · 사용자 D19-2 (T12) — 같은 날 출금이 입금보다 먼저면 거부 | DECISION | Plan 04-07, Task 2 ②: 날짜 마감 판정 · 같은 날 입금 먼저 · 결정표 |
| B-10 · 사용자 D19-1 — 외화 잔액이 원화 기준 | DECISION | Plan 04-07, truths · Task 2 ②: 원화 유지 + 결정표 케이스 + 주석 (우회 문서는 Plan 04-42) |
| B-26 · 사용자 D19-3 — 거부 로그 | DECISION | Plan 04-07, Task 2 ⑤: 한 거부 지점 `write.denied` |
| 사용자 D19-11 — 리저브는 Phase 4 유지 | DECISION | Plan 04-07, objective · truths: 절단 규칙 미적용 확정 |
| B-12 — 최근 환율 미갱신 | WARNING | Plan 04-07, Task 2 ③: `fxRateTouched` → `rememberFxRate` + 통합 |
| B-14 — 쓰기 권한 이름 없음 | WARNING | Plan 04-07, Task 3 ②: `can(viewer, "pnl", "write")` 저장·보관·복원 |
| B-15 — `pnl` 보기 + 노출 꺼짐에서 부분 노출 | WARNING | Plan 04-07, Task 3 ④: 둘 다 요구 · 빈 결과 · 키 집합 (화면 404는 Plan 04-42) |
| B-16 — 수정 로그 detail 미정 · 상시 기록 여부 | WARNING → DECISION | Plan 04-07, Task 2 ④: `changed: [전, 후]` + 통합 · 엔지니어링 리뷰 ENG-D8로 종결 — `document_update`(관리자가 끌 수 있음) 유지, 값 가리지 않음(활동 기록은 기본값으로 시스템관리자만) |
| B-17 — KRW 위조 환율 · USD 환율 0 이하 · 음수 금액 · 잘못된 날짜 · 범위 | WARNING | Plan 04-07, Task 2 ③: 서버 검증 + 칸 이유 + 통합 |
| B-34 — 04-18과 같은 웨이브에서 `ui/table` 병렬 변경 · objective의 낡은 「웨이브(14)」 문구 | NOTE | Plan 04-07 `depends_on`에 04-18(마지막 `ui/table` 변경) · 옛 문구 정정 · `ui/table`을 쓰는 화면은 Plan 04-42(이 플랜 뒤)로 분할 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B §1 — 리저브 쓰기가 `pnl` 쓰기만으로 판정돼 잔액이 실린 거부 문구로 떠볼 수 있음 | P2 | Plan 04-07, Task 3 ②③: 트랜잭션 앞 `pnl` 쓰기 + `reserve.amount` · 숫자 없는 거부 통합(GAP 3a) |
| ENG-D8 (R7) · B-16 — 리저브 금액이 활동 기록 detail에 남음 | DECISION | Plan 04-07, truths · `<probe_fallback>` · Task 2 ④: 가리지 않음(사용자가 뒤집음) · `document_update` 유지 — B-16 종결 |
| ENG-D10 · Codex #3 (R9) — 재전송 중복(리저브) | DECISION(P1) | Plan 04-07, Task 1 ③ · Task 2 ③-b·behavior: 클라이언트 UUID · `ON CONFLICT (id) DO NOTHING` · 재전송 판정 · 한 행 통합 |
| B §1 · A §1 — 잠금 안 전역 db 호출 · 트랜잭션 안 `rememberFxRate` | P2 | Plan 04-07, Task 1 ④ · Task 2 ③-c · Task 3 ②③ |
| B §1 — `vendors` `FOR UPDATE`가 무관한 FK 쓰기를 막음 | P3 | Plan 04-07, Task 1 ③: `FOR NO KEY UPDATE` |
| B §2 — `sort_key` 배정 규칙 없음 | P3 | Plan 04-07, Task 1 ①④ · Task 2 ②: 컬럼 삭제, (날짜, 구분, `created_at`, `id`) 한 비교 함수 |
| B §2 — KRW 환율·범위 검증 재구현 | P2 | Plan 04-07, Task 2 ③: 04-40 `normalizeMoneyInput` |
| `denyWrite` 한 도우미 | P2 | Plan 04-07, Task 2 ⑤ · Task 3 ②③ |
| Codex #7 [P2] — 다른 쪽의 줄에서 난 잔액 거부 | P2 | Plan 04-07, Task 3 ④-b: 거부 정보에 쪽 번호 (화면은 Plan 04-42) |
| GAP 3 a–e — 권한·보관 줄·남의 프로젝트·없는 클라이언트·코드표 밖 증빙·거부 배치의 환율 | GAP | Plan 04-07, Task 2 behavior(b–e) · Task 3 behavior(a) |
| 공용 `lib/paging.ts` | 규약 | Plan 04-07, Task 3 ④: `domain/projects/list-view` 대신 |
| 사실 정정 — 칸 이유 모음 선례 위치 · `plant8/money-boundary`가 강제하는 범위 | P3 | Plan 04-07, objective (12) · Task 1 read_first·④·verify |

### Round 3 — 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| P0 · DR-17 · F1 — 리저브 서버 문구가 rev 5와 다름 | MEDIUM | Plan 04-07, truths · Task 2·3 behavior·action: `이 줄 뒤 잔액 {잔액} · 금액을 줄이거나 입금 줄 먼저` · `클라이언트는 첫 저장 뒤 잠김 · 새 줄로 적어 주세요` · 재전송 불일치 `이미 저장된 줄과 값이 다름 · 새로 고침`(rev 5 — 04-12 문구로 통일) · 보관된 줄 방어 거부 `보관된 줄 · 새로 고침`(rev 5 밖 — 명사형, SUMMARY 기록) · 보관함 복원 문구는 관리자 화면이라 그대로(P0 예외) |
| rev 4 UI-SPEC 줄 인용 | LOW | `<context>`·read_first를 rev 5 절 제목 + 현재 줄로 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| B-13 — `/pnl` 링크 노출 조건 | 화면 파일 — Plan 04-42 Task 1 |
| 엔지니어링 리뷰 §1 — `DOMAIN_RESTORERS`가 `domain/archive`에서 모든 도메인 모듈을 import | TODO(P3) — 지금 두 항목(견적 줄 · 리저브)이라 그대로 둔다. 세 번째 항목이 생기면 소유 모듈이 `registerRestorer("reserve_entry", restoreReserve)`로 등록하는 방식으로 바꾼다(import 순환 방지) — 이 플랜은 구현하지 않는다 |
| 엔지니어링 리뷰 §4 — `listReserves`가 모든 활성 줄을 읽어 JS에서 정렬·누적 | TODO(P3) — 줄 수가 약 2만을 넘으면 리포지토리의 윈도 함수(`SUM() OVER (PARTITION BY client_id ORDER BY …)`)로 바꾼다. 10~30명 규모에서 허용 — 이 플랜은 구현하지 않는다(순수 함수 분리가 그 교체 자리) |
| B-35 — 실행 태스크의 `.planning` 편집 | Plan 04-42로 옮겼다(04-OPEN-ITEMS만 — UI-SPEC 편집은 rev 5가 담아 2026-09-23 재계획에서 삭제, DR-1) — 04-02/04-04 선례, 사용자 인지 항목 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-07-SUMMARY.md` when done
</output>
