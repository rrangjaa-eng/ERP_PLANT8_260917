---
phase: 04-project-quote-ledger
plan: 08
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - app/(app)/projects/project-form.tsx
  - lib/shortcut.ts
  - test/unit/lib/shortcut.test.ts
  - ui/shell/TopBar.tsx
  - ui/next-turn/NextTurn.tsx
  - test/unit/ui/next-turn.test.ts
  - test/unit/design-system-docs.test.ts
  - test/e2e/project-register.spec.ts
autonomous: true
requirements: [UX-05, UX-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "프로젝트 등록 폼에서 Ctrl+Enter를 누르면 폼이 실제로 한 번 제출되고 상세로 이동한다 — 키를 누르고 있거나(자동 반복) 빠르게 두 번 누르거나 한글 입력 조합 중이거나 제출이 진행 중이거나 **성공 뒤 상세로 넘어가는 사이**(액션은 끝났지만 폼이 아직 떠 있는 100–300ms)에 다시 눌러도 두 번째 제출이 일어나지 않아 같은 이름의 프로젝트가 정확히 한 건만 생긴다(D-94, §7-9 「실제로 되는 것만」, CEO 리뷰 C-06, 엔지 리뷰 C §1 P2)"
    - "Ctrl 조합 판정은 한 함수 `isCtrlCombo(event, key)`(`lib/shortcut.ts`)다 — Ctrl 키가 눌렸고 자동 반복·한글 조합 중이 아닐 때만 참이며 메타 키를 보지 않는다. 등록 폼과 견적 표 키보드(04-28)가 같은 함수를 쓴다(엔지 리뷰 C §2 P2)"
    - "등록 폼 안의 Esc가 실제로 동작하되 입력을 잃지 않는다 — 열린 네이티브 선택 목록·날짜 선택·자동 완성이 Esc를 먼저 쓰면(이벤트가 이미 처리됐으면) 폼은 아무것도 하지 않고, 폼 값이 처음 연 값(복사 등록이면 채워진 값)과 같으면 바로 목록(`cancelHref`)으로, 한 칸이라도 다르면 입력을 버리지 않는다(폼 그대로). 제출 중에는 Esc를 무시한다. 입력이 있을 때의 `ui/confirm-dialog` 「입력 버리기」와 2차 「취소 Esc」 버튼은 04-46(CEO 리뷰 C-07 ①, DR-27, UI-SPEC rev 5 UI Considerations S2 「(rev 5) interactive — Esc」)"
    - "상단 바 검색의 kbd 표기는 `Ctrl+K`로만 바뀌고 동작·렌더 여부는 디자인 잔여 퀵 태스크 F-03이 정한다 — 이 플랜은 그 표시가 동작한다고 주장하지 않는다(D-94, C-07 ③ — 「화면에 적힌 단축키가 전부 동작」의 유일한 기록된 예외)"
    - "`docs/design/SYSTEM.md`에 Mac 명령 키 글리프(⌘)·엔터 글리프(↵)·옵션 글리프(⌥)·시프트 글리프(⇧)가 0개다 — `이 줄로 ↵`처럼 개정 ② 목록에서 빠졌던 줄까지 `Ctrl`·`Enter`·`Alt`·`Shift` 글자로 바뀐다(D-94, C-07 ④). 앱 코드(`app/`·`ui/`)의 같은 스캔은 견적 표 글리프를 고치는 04-28이 켠다"
    - "`SYSTEM.md` 개정 열네 건(①~⑯ 중 ⑦·⑮를 뺀 것 — ⑦·⑮는 `Button`·확인 모달 코드와 함께 04-46)이 각각 `DECISIONS.md` 기록 한 항목 뒤에 반영되고(기록 커밋이 SYSTEM.md 커밋보다 앞선다) `docs/design/tokens.css`는 한 줄도 바뀌지 않는다 — 단 ⑧ 안의 C-27(PC 한 쪽 틈 번호)과 폰 6쪽 창 두 문장은 04-29 Task 3이 자기 기록과 함께 쓴다(DR-33). ④는 CEO-D10·D12 문구다 — (가) 잠김 행 셋째 칸과 표 위 한 줄이 `완료 · 견적 줄 잠김` / `정산 · 실행가와 새 줄만` / `{n}차 고객 승인됨 · 고치려면 새 차수` 한 문자열이고, 정산의 줄 추가는 렌더한다(DR-2). DECISIONS에는 「D-78 개정(CEO-D10·D12)」이 남는다(CLAUDE.md 프론트엔드 규칙, UI-SPEC rev 5 「SYSTEM.md 개정 — 2026-09-23」)"
    - "UI-SPEC rev 5 「재실행 가능한 확인」 27줄이 전부 자동 단언으로 고정된다 — 문서 쪽 줄은 `test/unit/design-system-docs.test.ts`, `NextTurn.tsx`의 `대기` 색은 같은 테스트의 소스 읽기 단언, `Button.tsx`의 `reasonTone`·`aria-disabled`(11·12)와 `### 7-17`(9의 둘째 줄)은 04-46이 같은 테스트에 더한다, 앱 코드의 글리프·메타 키 두 줄은 04-28의 `test/unit/ui/shortcut-notation.test.ts`, `6쪽부터` 한 줄은 04-29 Task 3의 단언이 맡는다 — 줄마다 담당 테스트가 정해져 조용히 빠진 줄이 없다"
    - "`DECISIONS.md`에 상단 2px 진행 막대 색(§5·§7-7·§7-10의 `2px --accent`)이 §1-3 「`--accent` 사용처 다섯 곳」과 충돌한다는 사실과 「진행 막대는 앱 어디에도 아직 없고 이번에 만들지 않는다 — 디자인 잔여 퀵 태스크로」가 기록된다(CEO 리뷰 C-10, 사용자 D17)"
    - "`SYSTEM.md`의 ASCII 스케치 셋이 이 페이즈 이후의 화면과 맞다 — §6-0 공통 셸의 힌트 줄·검색 kbd가 Ctrl 표기, §6-1 목록 스케치가 「더 보기 50건」이 아니라 번호 페이지 줄과 표 위 합계 줄, §6-2 상세 스케치가 상태 바꾸기·기간 칸·차수 섹션·조정 그룹·줄 수 상한 안내·30줄 페이지 줄·고객 승인 표시 자리를 그린다(CEO 리뷰 「낡은 다이어그램 점검」 3건, T17)"
    - "「내 차례」 블록의 `대기` 태그가 §7-5 의미 목록대로 muted 색이다 — 같은 낱말이 블록마다 다른 색을 갖지 않는다(개정 ⑩)"
    - statement: "SYSTEM.md 개정 뒤에도 문서 안 기존 절(§6-7~§7-14)의 다섯 상태 계약 단언이 전부 초록이다 — 개정이 다른 절의 계약 문장을 지우지 않았다"
      verification: backstop
  prohibitions:
    - statement: "디자인 시스템 개정을 「화면 하나만 예외」로 처리하거나 DECISIONS.md 기록 없이 SYSTEM.md를 고쳐서는 안 된다. 이 페이즈의 새 규칙(정산 색·Ctrl 표기·합계 줄 위치·페이지 줄·확인 모달·P0)은 다음 화면들이 그대로 물려받는 시스템 규칙이고, 기록 없는 이탈은 다음 화면에서 같은 논쟁을 다시 부른다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "docs/design/DECISIONS.md"
      provides: "2026-09-23 Phase 4(04-08) 개정 기록 열네 건(①~⑯ 중 ⑦·⑮ 제외 — 04-46) + 진행 막대 충돌 기록 한 건 = 15(04-46 뒤 17)"
      contains: "## 2026-09-23 — Phase 4(04-08)"
    - path: "docs/design/SYSTEM.md"
      provides: "개정 ①~⑯ 반영(⑦·⑮는 04-46 · ⑧의 C-27·폰 창 두 문장은 04-29) — 숫자 쉼표·Ctrl 표기·목록 합계 줄과 번호 페이지·좁은 폭 열 접기(편집 표 포함)·편집 표 (자)·상태 태그 다섯·코드표 설명·§7-16 페이지 줄·Form.Hint 미리보기·대기 색·담당자 표기·편집 표 보강 2·힌트 줄 자리·확인 근거 한 칸·§8 P0 + §6-0·§6-1·§6-2 스케치 갱신"
      contains: "### 7-16"
    - path: "test/unit/design-system-docs.test.ts"
      provides: "UI-SPEC rev 5 「재실행 가능한 확인」 문서·소스 쪽 단언(이 플랜 몫) + SYSTEM.md 글리프(⌘↵⌥⇧) 0 + DECISIONS 기록 15건 단언(04-46이 17로)"
      contains: "2026-09-23"
    - path: "test/e2e/project-register.spec.ts"
      provides: "등록 폼 Ctrl+Enter 한 번 제출(연타 1건 · 성공 뒤 이동 지연 중 재입력 1건) · Esc(빈 폼 → 목록 · 입력 있음 → 폼 유지 — 입력 버리기 확인·선택 목록 먼저는 04-46)"
      contains: "Control+Enter"
    - path: "lib/shortcut.ts"
      provides: "Ctrl 조합 판정 한 함수(자동 반복·조합 중이면 거짓, 메타 키 무시)"
      exports: ["isCtrlCombo"]
  key_links:
    - from: "app/(app)/projects/project-form.tsx"
      to: "lib/shortcut.ts"
      via: "폼 keydown이 `isCtrlCombo(event, \"Enter\")`로 판정한다 — 04-28의 견적 표 키보드와 같은 함수"
      pattern: "isCtrlCombo"
    - from: "app/(app)/projects/project-form.tsx"
      to: "ui/button/Button.tsx"
      via: "버튼 kbd `Ctrl+Enter` 표기와 폼 keydown의 Ctrl+Enter 제출이 같은 조합이다 — 제출 경로는 폼 표준 제출 하나"
      pattern: "Ctrl\\+Enter"
    - from: "docs/design/DECISIONS.md"
      to: "docs/design/SYSTEM.md"
      via: "기록이 먼저, 규칙 개정이 뒤 — 개정 번호 ①~⑯이 두 문서에서 짝을 이룬다"
      pattern: "2026-09-23"
---

<objective>
**As a** Windows를 쓰는 기획 PM·경영관리, **I want to** 화면에 적힌 단축키(Ctrl+Enter·Esc)가 그대로 동작하되 Esc 한 번에 입력을 잃지 않고, 이 페이즈의 새 화면 규칙(정산 색·합계 줄·페이지 줄·숫자 쉼표·코드표 설명·확인 모달·안내 문구 최소)이 디자인 시스템에 먼저 적혀 있기를, **so that** 다음 플랜들이 만드는 화면이 전부 같은 규칙을 쓰고(같은 확인 모달·버튼은 바로 뒤 04-46) 「누르면 아무 일도 없는 단축키」가 사라진다.

**2026-09-23 체커 revision 1 분할(W2 — 20파일 · 110k).** 옛 Task 3(개정 ⑦·⑮ 기록 → ⑦ 규칙 + `Button` `reasonTone`·`aria-disabled` → §7-17 → `ui/confirm-dialog` 신설 → 견적 줄 삭제 확인 이관 → 등록 폼 입력 버리기)을 신설 **04-46**으로 내용 그대로 옮겼다 — **교차 그룹 계약 1·2의 주인은 이제 04-46이다.** 아래 (3)의 「뗄 수 없다」는 04-43 같은 **같은 웨이브** 분할에 대한 판단이었다 — 04-46은 이 플랜 바로 뒤에 서므로 같은 커밋 제약(⑦의 SYSTEM.md 줄 + `Button` 코드)은 04-46 Task 1 한 커밋 안에서 그대로 지켜지고, 확인 모달은 새 `Button` 뒤, 입력 버리기는 이 플랜 Task 1의 Esc 판정(`isFormPristine`) 뒤다. 이 플랜에는 D-94 트레이서(Task 1)와 SYSTEM.md 개정 열네 건 + 스케치 + `대기` 색(Task 2)이 남는다 — 10파일 · 62k. 04-28·04-29는 04-46에 의존한다(공유 파일 순서 — 뒤 웨이브의 `Button`·확인 모달 소비처는 그 사슬로 04-46에 닿는다).

이 플랜은 2026-09-23 보완 계획의 **첫 태스크**다(`04-UI-SPEC.md` rev 5 「SYSTEM.md 개정 — 2026-09-23」 677–1000행 — 개정 ①~⑯ + 「재실행 가능한 확인」). CLAUDE.md 프론트엔드 규칙 순서 — `docs/design/DECISIONS.md` 기록 → `SYSTEM.md` 개정 → 코드 — 를 그대로 지킨다.

**2026-09-23 디자인 리뷰 반영(`docs/designs/plant8-erp-phase4-design-review-260923.md`, UI-SPEC rev 5 — 재계획 그룹 C).** (1) Task 2의 개정 원문을 rev 5 ①~⑯으로 바꾼다 — ④는 CEO-D10·D12 문구(DR-2 — rev 4 원문 `정산 · 실행가만 고칠 수 있음`·「줄 추가 미렌더」를 옮기면 04-30과 반대가 된다), ⑫~⑯ 신설(편집 표 보강 2 · 힌트 줄 자리 · §7-8 확인 근거 한 칸 DR-20 · §7-17 확인 모달 DR-12 · §8 P0). ⑧의 C-27·폰 창 기록은 04-29 Task 3이 한다(DR-33). (2) **교차 그룹 계약 1·2의 주인이 이 플랜이다(옛 Task 3 — 체커 revision 1로 04-46이 주인):** `Button` `reasonTone` + `aria-disabled`(DR-10 · DR-11)와 공용 `ui/confirm-dialog`(DR-12 · DR-20 · DR-27). 뒤 웨이브의 소비처(04-16 매출 삭제 · 04-21 상태 모달 · 04-24 차수·승인 모달 · 04-42 리저브 삭제 · 저장 버튼의 `바뀐 칸 없음` info)는 전부 이 플랜 뒤다. 기존 `DeleteLineDialog`(div 모달)를 옮기고 등록 폼 Esc를 DR-27로 고친다. (3) **분할:** 코드 부분(계약 1·2)은 04-43으로 뗄 수 없다 — ⑦이 「문서와 같은 커밋에서 `Button`을 고친다」를 요구하고(SYSTEM.md가 겹친다), 확인 모달이 새 `Button`에 기대며, 등록 폼 Esc(DR-27)와 Ctrl+Enter(Task 1)가 같은 `project-form.tsx`다. 그래서 코드는 여기 두고, 이 플랜의 옛 Task 3(REQUIREMENTS·ROADMAP 세 문구, 사용자 D16)을 파일이 겹치지 않는 **04-43**(웨이브 3)으로 옮겼다. (4) 상단 2px 진행 막대 충돌 기록(D17)은 그대로다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE, `docs/designs/plant8-erp-phase4-ceo-review-260923.md`).** (1) **04-04 분리(사용자 D21·OV-7):** 옛 04-08은 견적 표 파일(키보드 훅·파서·표·`quote-table.tsx`)을 함께 고쳐 04-04의 실제 엑셀 사람 확인을 기다려야 했고, 그래서 코드표·상태·권한 플랜 전부가 04-04 뒤에 묶였다. 견적 표 쪽 Ctrl 전용 판정·글리프·앱 스캔 테스트는 **04-28**로 떼고, 이 플랜은 문서와 견적 표 밖의 단축키(등록 폼·상단 바)만 맡는다 — 04-04를 기다리지 않는다. (2) **C-06·C-07:** 등록 폼 Ctrl+Enter 연타·한글 조합·진행 중 이중 제출을 막고, 적힌 `취소 Esc`를 실제로 배선하며, 「적힌 단축키가 전부 동작」 주장을 이 플랜이 배선하는 조합으로 좁힌다. (3) **D16:** REQUIREMENTS·ROADMAP은 PROJ-04·PROJ-03·기준 4 **세 문구만** 고친다 — 2026-09-23 재계획에서 이 태스크는 **04-43**으로 옮겼다(내용 무변경). (4) **D17:** 진행 막대 색 충돌을 DECISIONS에 기록하고 막대는 만들지 않는다. (5) **T17:** SYSTEM.md 스케치 셋(§6-0·§6-1·§6-2)을 갱신한다.

Purpose: 나머지 보완 플랜들이 기대는 규칙 문서를 먼저 확정해(공용 부품 둘 `Button` 개정 · `ui/confirm-dialog`는 바로 뒤 04-46) 「화면 하나만 예외」가 생기지 않게 하고, D-94(단축키 Windows 기준)를 견적 표 밖에서 표기와 동작 둘 다 끝낸다.

Output: DECISIONS.md 기록 15건(04-46 뒤 17), SYSTEM.md 개정 ①~⑯ 중 ⑦·⑮를 뺀 열네 건(⑧의 C-27·폰 창 두 문장 제외) + 스케치 셋 + 글리프 0, 등록 폼 Ctrl+Enter(가드 포함)·Esc(빈 폼 갈래 · 입력 유지 — DR-27의 확인 모달은 04-46), `대기` 색 정정, 문서·단위·E2E 갱신.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`):** (a) 엔지 리뷰 C §1 P2 — `isExecuting` 가드는 「액션 성공 → 상세 이동」 사이를 덮지 못한다(`project-form.tsx:37-40` — 액션이 끝나면 `isExecuting`이 거짓으로 돌아오고 폼은 상세 RSC가 올 때까지 떠 있다). 제출 래치 `submittedRef`를 더하고, E2E (b)를 이동 지연으로 결정적으로 만든다(공백 9). (b) 엔지 리뷰 C §2 P2 — Ctrl 조합 판정을 공용 `lib/shortcut.ts`의 `isCtrlCombo`로 — 04-28이 견적 표 키보드에 같은 함수를 쓴다(IME·자동 반복 가드가 폼에만 있던 문제).

**실행 규율(모든 태스크):** 구현 전에 superpowers `test-driven-development` 스킬을, 테스트·빌드 실패를 쫓기 전에 `systematic-debugging` 스킬을, 「완료」를 말하기 전에 `verification-before-completion` 스킬을 호출한다. 서브에이전트에 위임하면 프롬프트에 세 스킬 이름과 `model`을 명시한다. 커밋 제목은 영어 접두어(docs:/feat:/fix:/test:) + 짧은 요약, 본문은 한국어, 한 커밋 한 의도.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@CLAUDE.md
# 범위 Read(전문 import 금지 — 큰 파일, UI-SPEC은 rev 5 — 절 이름으로 찾고 아래는 2026-09-23 현재 행):
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 677–1000행(「SYSTEM.md 개정 — 2026-09-23」 ①~⑯ + 「재실행 가능한 확인」 970–1000행)
# 같은 문서 308–441행(Copywriting Contract — P0 규칙 310–331 · 단축키 표기 438–440) · 481–488행(UI Considerations S2 — rev 5 Esc) · 1095–1116행(적용 규칙 S2) (Component Inventory `Button`·`ConfirmDialog` 행 · S11 · S16 · 「Destructive —」·「확인 —」 행은 04-46)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 818–834행(§9 D-94/D-95)
# docs/designs/plant8-erp-phase4-design-review-260923.md 35–74행(결정 원장 DR-1~DR-38 — 이 플랜: DR-2 · DR-10 · DR-11 · DR-12 · DR-20 · DR-27 · P0)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 15–46행(결정 원장 — D17·D21) · 246–258행(낡은 다이어그램 점검) · 1781–1806행(C-06·C-07) · 1843–1858행(C-10)
</context>

<probe_fallback>
엣지 프로브(`edge-coverage.json`)는 이 페이즈 요구사항 11개를 전부 `unclassified`로 돌려줬다(한국어 서술 — `04-OPEN-ITEMS.md` 「도구 이슈」). 폴백 규약 §C대로 조용히 버리지 않는다. 이 플랜이 떠맡는 미분류 행은 **UX-05(단축키 계약 — 견적 표 밖)**와 **UX-04(입력값 유실 없음 — 등록 폼 Esc, DR-27)**이고, 수용 기준은 D-94 · `04-UI-SPEC.md` rev 5 개정 ② · Copywriting 「단축키 표기(D-94)」·「확인 — 등록 폼 입력 버리기」 · UI Considerations S2(rev 5 Esc)에서 끌어와 `must_haves.truths`에 적었다 — S2 loading(2차 「취소 Esc」 aria-disabled)·S16(확인 모달 전 항목, overflow·zero-one-many backstop 포함)은 체커 revision 1로 **04-46**이 옮겨 받았고, S2의 나머지(empty·error·partial·long-text)는 등록 폼을 넓히는 04-15 몫이다. 견적 표 쪽 UX-05 행(Ctrl 전용 판정·힌트 줄·앱 스캔·충돌 셀 키보드)은 **04-28**이 옮겨 받았다 — 조용히 버린 항목은 없다. PROJ-03·PROJ-04의 **요구사항 문구**는 04-43이 고친다 — 동작 수용 기준은 결정된 모델(D-75~D-84)을 기준으로 04-06·04-11·04-16이 적는다.

**계획 단계 가정·사용자 결정(표면화):**
- `04-VALIDATION.md`의 D-94 행(「Mac 메타 키·Windows Ctrl 둘 다 같은 동작」)은 리서치 가정(A10)을 옮긴 것이고, 승인된 `04-UI-SPEC.md` rev 5 개정 ②는 「동작도 Ctrl이다 — 메타 키를 받지 않는다」로 반대다. 이 플랜은 **UI-SPEC을 따른다**(D-94 원문 「화면 표기와 동작 모두 Ctrl」). VALIDATION 행은 `/gsd-validate-phase`가 다시 맞춘다.
- 등록 폼 버튼의 kbd는 지금 표시만 있고 동작이 없다. D-94 「동작도 Ctrl」과 §7-9 「실제로 되는 것만」을 함께 만족시키려고 Ctrl+Enter 제출과 Esc 취소를 **실제로 배선**한다(새 단축키가 아니라 이미 적힌 단축키의 배선 — C-07 ①).
- **C-06 한글 입력기:** Windows Microsoft 한국어 입력기의 keydown 반복·조합 동작은 자동 테스트로 재현되지 않는다. 가드(`repeat`·`isComposing`·진행 중)는 이 플랜이 넣고 E2E는 연타를 단언하며, 실제 IME 확인은 04-31의 최종 사람 확인 목록과 `/qa`에 넣는다.
- 상단 바의 검색 kbd는 명령 팔레트가 없는 죽은 표시다(디자인 리뷰 F-03). 이 플랜은 **표기만** `Ctrl+K`로 바꾸고 렌더 여부는 F-03 퀵 태스크가 정한다. 같은 줄을 두 작업이 따로 고치지 않도록 이 사실을 **DECISIONS ② 기록 본문과 SUMMARY**에 적는다 — 퀵 태스크 문서(`.planning/quick/…`)는 D16이 승인한 범위 밖이라 손으로 고치지 않는다.
- **D16 — 세 문구만(04-43으로 이동):** REQUIREMENTS PROJ-04·PROJ-03·ROADMAP 기준 4의 세 문구와 「남은 낡은 문구」 표는 **04-43**이 한다(이 플랜의 옛 Task 3, 내용 무변경). 이 플랜은 `.planning/` 파일을 고치지 않는다.
- **계획 기본값 기록의 자리:** UI-SPEC ⚠ 기본값 일곱은 CEO 리뷰 결정 원장에서 사용자가 전부 답했다(S1·S3·S4 조정 줄 복사 = D19, S4 정산 구조 = D10·D12, S7 = D13, S13 = D14) — rev 5가 확정값으로 담았다. 리저브 절단 규칙은 D19 「리저브 Phase 4 유지」로 확정됐다.
- **`Button` 진행 중 상태(rev 5 — 계약 1 편차) · 확인 모달 막힘 자리 확장(계약 2 선택 prop `reasonTone`·`nextStep` · 목록형 행 모양):** 두 판단은 옛 Task 3과 함께 **04-46** `<probe_fallback>`으로 옮겼다(내용 무변경).
- **등록 폼 Esc — 내부 컨트롤 먼저(DR-27):** 폼의 keydown(버블 단계)은 `event.defaultPrevented`·조합 중·제출 중이면 아무것도 하지 않는다(Task 1). Chromium이 열린 네이티브 `<select>`·날짜 선택 목록의 Esc를 페이지로 보내지 않는지의 E2E(선택 목록을 연 채 Esc → 폼 유지)와 필요하면 더할 열림 추적, SUMMARY 기록은 입력 버리기 확인을 붙이는 **04-46**이 한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — D-94 한 결정을 기록 → 규칙 → 등록 폼 코드 → E2E까지 관통(개정 ② · C-06 · C-07)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, lib/shortcut.ts, test/unit/lib/shortcut.test.ts, app/(app)/projects/project-form.tsx, ui/shell/TopBar.tsx, test/e2e/project-register.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정 — 2026-09-23」의 ②(701–719행 — 25줄 목록 · 바꾸는 법 · §7-9 한 줄 · 코드 지점 · F-03과 겹침 · §11 항목)와 Copywriting 「단축키 표기(D-94)」(438–440행) · UI Considerations S2 「(rev 5) interactive — Esc」(488행)
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-94 원문
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 1781–1806행(C-06 이중 제출 · C-07 적힌 단축키 넷)
    - `docs/design/DECISIONS.md` 481–525행(04-01의 기록 형식 — 머리글 `## 2026-09-22 — Phase 4(04-01) …` 꼴)
    - `docs/design/SYSTEM.md` — Grep으로 `⌘`·`↵`·`⌥`·`⇧`가 있는 줄을 먼저 전부 찾고(개정 ②가 적은 25줄 번호는 2026-09-23 기준이라 밀렸을 수 있다 — 절 이름으로 찾는다, `이 줄로 ↵` 줄 포함) §7-9 · §11 범위만 Read
    - `app/(app)/projects/project-form.tsx`(전문 113행) — 제출 버튼·`handleSubmit`·`isExecuting`·취소 링크(`cancelHref`)·108행 `취소 Esc` 표기
    - `ui/button/Button.tsx`(전문) — `shortcut` prop은 표시 문자열뿐이라는 것을 확인한다
    - `ui/shell/TopBar.tsx` — Grep으로 검색 kbd 글리프 한 곳만
    - `test/e2e/project-register.spec.ts`(전문) · `test/e2e/fixtures.ts` — 역할 픽스처와 등록 흐름 단언 형태
  </read_first>
  <action>
① **기록 먼저.** `docs/design/DECISIONS.md` 끝에 머리글 `## 2026-09-23 — Phase 4(04-08) ②: 단축키 표기와 동작은 Windows Ctrl (D-94)`인 항목을 04-01 기록과 같은 형식(무엇을·왜·범위·되돌리는 법)으로 더한다. 이유는 UI-SPEC 개정 ② 원문(직원 전원 Windows, 표기와 동작 둘 다)을 옮기고, 범위 문단에 (a) 견적 표 쪽 판정·글리프는 04-28이 코드로 끝낸다 (b) 상단 바 검색 `Ctrl+K`는 **표기만** 바꾸고 렌더 여부는 디자인 잔여 퀵 태스크 F-03이 정한다 두 줄을 적는다.

② **규칙.** `docs/design/SYSTEM.md`에서 `⌘`·`↵`·`⌥`·`⇧` 글리프가 들어 있는 줄을 **전부** 개정 ② 「바꾸는 법」대로 바꾼다 — 저장·새 줄·복사/붙여넣기·복제·지출결의·검색·업로드 조합이 각각 `Ctrl+S`·`Ctrl+Enter`·`Ctrl+C`/`Ctrl+V`·`Ctrl+D`·`Ctrl+E`·`Ctrl+K`·`Ctrl+U`가 되고 줄 이동은 `Alt+↑↓`가 된다. 개정 ② 목록에 없던 `이 줄로 ↵` 같은 줄도 `Enter` 글자로 바꾼다(C-07 ④). §6-1 스케치의 목록 검색 칸 단축키 표기는 **대체 없이 지운다**(그 단축키는 구현된 적 없다, 04-05 Known Stubs). §7-9에 개정 ②가 적은 한 줄(조합 키 표기 규칙 · 동작도 Ctrl · 앱이 쓰는 조합의 브라우저 기본 동작 차단 · 셀 편집 중 복사/붙여넣기/전체 선택은 입력의 기본 동작)을, §11 「시스템 일치」에 `⌘`·`↵`·`⌥`·`⇧` 글리프 0개 항목을 더한다.

③ **코드 — Ctrl 조합 판정 한 함수(엔지 리뷰 C §2 P2).** `lib/shortcut.ts`(신규, 순수 — import 없음)에 `isCtrlCombo(event, key)`: `event.ctrlKey`가 참이고 `event.key === key`(대소문자 무시)이며 `event.repeat`가 거짓이고 조합 중(`event.isComposing` 또는 React 이벤트면 `nativeEvent.isComposing`)이 아닐 때만 참. **메타 키를 읽지 않는다**(D-94 — 04-28의 스캔이 `app/`·`ui/`의 메타 키 참조 0을 본다). `test/unit/lib/shortcut.test.ts`에 표를 먼저 쓴다(RED): Ctrl+Enter → 참 · Ctrl+Enter 자동 반복 → 거짓 · 조합 중 → 거짓 · Meta+Enter → 거짓 · Ctrl+Shift+S의 `key` `S` 대 `s` → 참 · Ctrl 없음 → 거짓.

③-b **코드 — 등록 폼(C-06·C-07 ① · 엔지 리뷰 C §1 P2).** `app/(app)/projects/project-form.tsx`의 `<form>`에 keydown 처리 하나를 더한다: `isCtrlCombo(event, "Enter")`가 참이고 (c) 제출이 진행 중이 아니며(`isExecuting`) (d) **제출 래치 `submittedRef`가 서 있지 않으면** 기본 동작을 막고 폼의 표준 제출(`requestSubmit`)을 부른다 — 자동 반복·조합 중은 `isCtrlCombo`가 걸러낸다. 서버 액션 경로·검증·오류 표시는 기존 제출과 **같은 경로**를 탄다(새 제출 경로를 만들지 않는다). `handleSubmit` 첫 줄에서 `submittedRef.current`가 참이거나 진행 중이면 돌아가고, 아니면 래치를 세운다 — 래치는 `onError`와 검증·서버 오류 결과(`result.validationErrors`·`serverError`)에서만 내린다(성공이면 상세로 넘어갈 때까지 서 있다 — `requestSubmit`은 버튼 가드를 지나치고, 성공 뒤 `isExecuting`은 이동 전에 거짓으로 돌아오기 때문이다). **Esc(DR-27 — 이 태스크는 빈 폼 갈래만):** 폼 keydown에서 `Escape`이고 `event.defaultPrevented`·조합 중·제출 중이 아니면(`<probe_fallback>` 「내부 컨트롤 먼저」), 폼의 지금 값이 **처음 연 값**(렌더 때 한 번 기록한 `FormData` 스냅숏 — 복사 등록이면 채워진 값)과 같을 때만 `cancelHref`로 이동한다(Next 라우터 이동 — 새 주소를 만들지 않는다). 값이 다르면 이 플랜에서는 **아무것도 하지 않는다** — 입력 버리기 확인은 04-46이 `ui/confirm-dialog`로 붙인다(이 사이에도 Esc가 입력을 버리는 커밋이 없다). 값 비교는 `isFormPristine(initial, current)` 한 함수(`project-form.tsx` 안, 칸 이름별 문자열 비교)로 둔다 — 04-46의 「취소 Esc」 버튼과 확인 모달 부제의 칸 수(`프로젝트 등록 · N칸`)가 같은 비교를 쓴다. 버튼의 kbd 표기는 `Ctrl+Enter`.

④ **코드 — 상단 바 표기.** `ui/shell/TopBar.tsx`의 검색 kbd 글자만 `Ctrl+K`로 바꾼다(렌더 여부·동작은 F-03 — 손대지 않는다).

⑤ **E2E.** `test/e2e/project-register.spec.ts`에 케이스 셋을 더한다: (a) 기획 PM으로 `/projects?new=1`을 열고 필수 네 칸을 채운 뒤 **마우스 클릭 없이** 마지막 칸에서 `Control+Enter`를 눌러 상세로 이동하고 번호가 부여된다 (b) 고유 이름으로 채운 뒤 `Control+Enter`를 빠르게 두 번 누르면(두 번째는 첫 제출 진행 중) 상세로 한 번 이동하고, 목록을 그 이름으로 검색하면 정확히 **1건**이다(C-06), (b2) **성공 뒤 이동 지연(엔지 리뷰 C 공백 9 — 결정적):** `page.route`로 상세 이동 요청(RSC 요청 — `/projects/` 경로의 `RSC` 헤더 요청)을 약속이 풀릴 때까지 붙잡아 두고, 첫 `Control+Enter` 뒤 액션 응답이 온 것을 확인한 다음(폼이 아직 보인다) `Control+Enter`를 한 번 더 누른 뒤 요청을 풀면 그 이름의 프로젝트가 정확히 **1건**이다 — 시간 경합에 기대지 않는다 (c) **빈 폼**의 칸에서 `Escape`를 누르면 등록 폼이 닫힌 목록 주소로 간다(C-07 ①, DR-27 빈 폼 갈래) · (c2) 프로젝트명 한 칸을 적은 뒤 `Escape` → 폼이 그대로이고 적은 값이 남는다(04-46 전 — 입력 손실 없음, 04-46이 이 케이스를 버리기 확인 단언으로 바꾼다). 이 태스크 커밋은 `docs:`(기록·규칙)와 `feat:`(코드·테스트) 둘로 나눈다 — 기록 커밋이 먼저다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/lib/shortcut.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이다 — 개정이 기존 문서 단언을 깨뜨렸거나 Ctrl 조합 판정 표가 어긋난다</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('docs/design/SYSTEM.md','utf8');const hit=['⌘','↵','⌥','⇧'].filter(g=>s.includes(g));if(hit.length){console.error('SYSTEM GLYPH: '+hit.join(' '));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 오류에 `SYSTEM GLYPH:`로 시작하는 줄이 나오고 종료 코드가 1이다 — SYSTEM.md에 Mac 글리프가 남았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/project-register.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — Ctrl+Enter 제출이 상세로 이동하지 않았거나, 연타가 두 건을 만들었거나, 빈 폼 Esc가 폼을 닫지 않았거나, 입력이 있는 폼의 Esc가 입력을 버렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/DECISIONS.md`에 `## 2026-09-23 — Phase 4(04-08) ②`로 시작하는 머리글이 정확히 한 줄 있고, 그 커밋이 SYSTEM.md 개정 커밋보다 앞선다(`git log --format=%s -- docs/design/DECISIONS.md docs/design/SYSTEM.md`의 순서로 확인)
    - ② 기록 본문에 「04-28이 견적 표 쪽을 끝낸다」와 「상단 바 `Ctrl+K`는 표기만 — F-03」 두 줄이 있다
    - `docs/design/SYSTEM.md`에 `⌘`·`↵`·`⌥`·`⇧`가 0개다(검증 명령)
    - `lib/shortcut.ts`의 `isCtrlCombo`가 자동 반복·조합 중이면 거짓이고 `metaKey`를 읽지 않으며 단위 표가 초록이다
    - `project-form.tsx`의 Ctrl+Enter 처리가 `isCtrlCombo`(자동 반복·조합 가드)·진행 중·`submittedRef` 래치를 갖고 폼의 표준 제출을 부르며 서버 액션을 따로 호출하지 않는다(제출 경로가 하나다) · `handleSubmit`이 래치를 세우고 오류 경로에서만 내린다
    - `project-form.tsx`의 Esc가 `isFormPristine`이 참일 때만 `cancelHref`로 이동하고, `defaultPrevented`·조합 중·제출 중이면 아무것도 하지 않는다(DR-27)
    - `project-register.spec.ts`의 케이스(a·b·b2·c·c2)가 있고 (b)·(b2)가 검색 결과 정확히 1건을 단언하며 (b2)가 이동 요청을 붙잡는 방식(고정 지연 없음)이다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>D-94가 기록·규칙·등록 폼의 표기와 동작·E2E까지 한 경로로 끝났다 — 등록 폼에 적힌 Ctrl+Enter·Esc가 실제로 동작하고 연타가 중복 등록을 만들지 않으며 SYSTEM.md에서 Mac 글리프가 사라졌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SYSTEM.md 개정 ①·③·④·⑤·⑥·⑧(C-27·폰 창 제외)·⑨·⑩·⑪·⑫·⑬·⑭·⑯ + 진행 막대 충돌 기록(D17) + 스케치 셋 갱신 → `대기` 색 코드 한 줄 → 문서 단언</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, ui/next-turn/NextTurn.tsx, test/unit/ui/next-turn.test.ts, test/unit/design-system-docs.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정 — 2026-09-23」 677–1000행 — **이 범위가 이 태스크의 계약 전문이다**(행 번호는 2026-09-23 현재, 어긋나면 절 이름 `**①`~`**⑯`으로 찾는다): ① 686–699 · ③ 721–763(「합계 규칙의 범위를 나눈다」 세 자리 731–741 · 좁은 PC 열 접기 746–751 · rev 5 보강 DR-14 편집 표 ⑴~⑷ 754–761 · §11 감사 폭 762–763) · ④ 765–807(CEO-D10·D12 정정 — (가) 잠김 행과 **셋째 칸** 767–771 · 한 문자열 772–775 · 구조 컨트롤 776–779 · (자) 780–803) · ⑤ 809–813 · ⑥ 815–823 · ⑧ 840–865(**C-27 문장 847–848과 폰 창 문장 849–850, DECISIONS 한 줄 863은 04-29 Task 3 몫 — 옮기지 않는다**, DR-33) · ⑨ 867–874 · ⑩ 876–884 · ⑪ 886–896 · ⑫ 898–926 · ⑬ 928–935 · ⑭ 937–942 · ⑯ 958–968 · 재실행 가능한 확인 970–1000. ⑦(825–838)·⑮(944–956)은 04-46이다
    - 같은 문서 250–305행(Color — 상태 → 색 매핑 확정표) · 209–249행(Typography — 숫자 서식 D-95) · 47–78행(「P0로 줄이거나 지운 문구」 표 — ⑯의 대상 문구)
    - `docs/designs/plant8-erp-phase4-design-review-260923.md` 35–74행(결정 원장 — DR-2 · 14 · 16 · 20 · 22 · 24 · 25 · 31 · 33 · 36 · P0)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 246–258행(낡은 다이어그램 점검) · 1843–1858행(C-10 — `2px --accent` 진행 막대와 §1-3 충돌)
    - `docs/design/SYSTEM.md` — 개정마다 절 이름으로 Grep한 뒤 해당 범위만 Read한다. 2026-09-23 현재 절 머리 행: §1-3 81 · §2-4 157 · §4-2 209 · §5 243 · §6-0 270 · §6-1 315 · §6-2 357 · §6-3 380 · §7-1 657 · §7-2 674 · §7-3 708 · §7-4 783 · §7-5 791 · §7-7 805 · §7-8 834 · §7-9 846 · §7-10 853 · §7-15 972 · §8 1007 · §10 1031 · §11 1043(Task 1이 줄을 바꾼 뒤에는 밀린다)
    - `test/unit/design-system-docs.test.ts`(전문) — 기존 단언과 `section()` 헬퍼. **기존 단언 중 이번 개정이 뒤집는 문장(§7-5의 옛 괄호 한정어 조항 등)을 단언하는 것이 있으면 새 규칙에 맞게 고친다(지우지 않는다)**
    - `ui/next-turn/NextTurn.tsx` 15–30행(태그 색 대응과 주석) · `test/unit/ui/next-turn.test.ts`(전문)
  </read_first>
  <behavior>
    - `대기` 항목의 태그 kind가 muted로 렌더된다(next-turn 단위 테스트) · 막힘·오늘·결재 세 태그의 kind는 danger·warning·accent 그대로다
    - SYSTEM.md에 `### 7-16` 머리글이 정확히 한 줄 있고, §7-16 안에 `next/link`와 「새 쪽의 활성 셀」(DR-23) 문장이 있다 — C-27·폰 창 문장은 아직 없다(04-29)
    - SYSTEM.md에 `더 보기 50건`·`완료(정산)`·`실행가만 고칠 수 있음`·`견적 줄이 잠김`·`매출 계약 금액`·`1칸 · 값은 남아 있습니다`·`등록할 수 없음 —`이 0번, 옛 §7-9 첫 줄의 `화면 하단에 **항상**`이 0번 나온다 — 스케치 안 글자 포함
    - SYSTEM.md에 `실행가와 새 줄만` 1번 이상 · §7-3 (가) 잠김 행에 `완료 · 견적 줄 잠김`과 `{n}차 고객 승인됨 · 고치려면 새 차수` · 정산의 줄 추가를 렌더한다는 문장(④, DR-2)
    - SYSTEM.md의 §7-4 태그 순서 문장에서 `대기`가 muted 색이고 `대기(\`--accent\`)`가 0번이다
    - 「편집 표의 합계 행」 또는 「편집 표 합계 행 위」 2번 이상 · `열이 많은 표(읽기·편집)` 1줄 · `1024 이상에서만` 1줄 이상 · `1280 · 1024 · 375` 1줄 이상(③)
    - `seenStatus` · `권한 밖인 줄` 각 1줄 이상, `` `--danger` → `--warning` → `--muted` `` 1줄(⑫) · 힌트 줄 정의가 「페이지 줄 바로 아래」(⑬) · `확인의 근거 한 칸` 1줄(⑭) · `안내 문구를 최소` 1줄(⑯)
    - SYSTEM.md에 `⌘`·`↵`·`⌥`·`⇧`가 0개다
    - DECISIONS.md에 `## 2026-09-23 — Phase 4(04-08)` 머리글이 번호별로 ①·②·③·④·⑤·⑥·⑧·⑨·⑩·⑪·⑫·⑬·⑭·⑯ 각 한 줄 + 진행 막대 충돌 기록 한 줄이다(⑦·⑮는 04-46) · ④ 머리글에 `D-78 개정(CEO-D10·D12)`이 있다
    - §6-2 스케치 블록 안에 `상태 바꾸기`·`기간`·`차수`·`조정`·`고객 승인`·`/`(페이지 줄 범위 글자) 여섯 낱말이 있다
  </behavior>
  <action>
① **기록 열세 건.** `docs/design/DECISIONS.md`에 개정 ①·③·④·⑤·⑥·⑧·⑨·⑩·⑪·⑫·⑬·⑭·⑯ 각각 한 항목씩, 머리글 `## 2026-09-23 — Phase 4(04-08) {번호}: {한 줄 제목} ({결정 ID})` 꼴로 더한다(⑦·⑮는 코드와 함께 04-46). 각 항목의 이유는 UI-SPEC rev 5 해당 개정의 「이유」 줄을 옮긴다. 결정 ID: ③ D-87·D-88·D-91 + DR-14 · ④ **`D-78 개정(CEO-D10·D12)`**·D-83·D-86·D-91·CEO-D7·DR-2(본문에 「rev 4 원문 `정산 · 실행가만 …`·「줄 추가 미렌더」로 옮기지 않은 이유 — 04-30과 반대가 된다」 한 줄) · ⑥ D-93·D-84·DR-28·DR-29 · ⑧ D-91·04-29·DR-18·DR-23(본문에 「C-27 한 쪽 틈 번호와 폰 6쪽 창은 04-29 기록이 함께 적는다 — DR-33」 한 줄) · ⑫ DR-3·5·6·16·22·25·31·24·36·4, CEO-D15·ENG-D5 · ⑬ DR-31 · ⑭ DR-20 · ⑯ P0(DR-17·DR-21). 이어서 **진행 막대 충돌 기록**(사용자 D17) 한 항목 — 머리글 `## 2026-09-23 — Phase 4(04-08) 기록: 진행 막대 색과 §1-3 충돌 · 이번에 만들지 않음 (C-10, D17)`, 본문: §5 모션·§7-7 LOADING·§7-10이 `2px --accent` 진행 막대를 적었지만 §1-3은 `--accent` 사용처를 다섯 곳으로 제한하고 막대를 넣지 않았다 · 막대는 앱 어디에도 아직 없다(§7-1 버튼 pending 300ms 포함) · 이번 페이즈는 막대를 만들지 않고 목록·페이지 이동의 로딩은 `loading.tsx` 뼈대가 맡는다 · 결정(§1-3에 여섯째로 넣기 · 중립 토큰으로 바꾸기)은 디자인 잔여 퀵 태스크가 먼저 이 파일에 적고 SYSTEM.md를 고친다(TODOS.md 「목록 상단 2px 로딩 막대」). **이 항목은 SYSTEM.md를 바꾸지 않는다** — 충돌 사실의 기록이다. 이 커밋(`docs:`)이 ②의 SYSTEM.md 변경보다 먼저다.

② **규칙 열세 건 + 스케치 셋.** `docs/design/SYSTEM.md`를 UI-SPEC rev 5 개정 원문대로 고친다 — 요약: ① §2-4 외화·환율 행 서식 + 수량·건수·식별자형 숫자 행 셋, §7-2 「숫자 입력」으로 넓히기와 설정 number 행, §11 한 항목 · ③ §6-1 번호 페이지(D-91)·표 위 합계 줄(D-88·D-90)·필터 날짜 칸 `10ch`·좁은 PC 열 접기 문단(D-87 열 구성), §7-3 「구조」 합계 행 줄·§4-2 강한 선 행·§7-7 LOADING과 컴포넌트별 표 행의 합계 범위 나누기(세 자리 같은 개정), **rev 5 보강(DR-14 · DR-36)** — 열 접기 문장을 「열이 많은 표(읽기·편집)는」으로 넓히고 편집 표 조항 ⑴ 숨은 열도 논리 열 순서 ⑵ 방향키·Tab은 숨은 열을 건너뜀 ⑶ P3에서만 · 계산 금액 합계는 합계 행 라벨 뒤 ⑷ 셀 편집·줄 추가·구조 컨트롤은 1024 이상에서만(700~1023은 보기 전용 · 힌트 줄 없음) + 외화 2행 두 묶음 줄바꿈, §11 「품질 바닥」에 DOM 감사 폭 1280 · 1024 · 375(+ 700) 한 줄 · ④ §7-3 편집 키 줄의 표 전체 선택, (가) 잠김 행의 둘째 칸 세 갈래(완료 전체 · 정산의 실행가 밖 셀과 새 줄 견적 칸 · 승인 차수의 수량·단가·상태·소분류)와 **셋째 칸** `완료 · 견적 줄 잠김` / `정산 · 실행가와 새 줄만` / `{n}차 고객 승인됨 · 고치려면 새 차수`(편집 셀이 0인 읽기 표에는 없다), 「표 위 한 줄 = 잠긴 셀의 편집 시도 이유 — 한 문자열(우선순위 완료 > 정산 > 승인 차수)」, 「잠김으로 할 수 없는 구조 행동의 컨트롤은 렌더하지 않는다」와 **정산의 줄 추가는 렌더한다**(CEO-D10), 753행 예시의 옛 라벨 정정, **(자) 신설**(페이지·키보드·범위·붙여넣기·새 줄·오류 이동·줄 수 상한) · ⑤ §7-5 의미 목록 `--warning`에 「마감 중」, 보강 줄의 완료 라벨을 `완료`로, 낱말 여섯, **옛 길이 규칙 괄호 한정어 조항 삭제** · ⑥ §7-15 `Select` 설명 한 줄, §7-3 코드표 셀 설명 한 줄, §7-2 설정 설명 규칙을 코드표 설명(40자)에 걸고 관리자 설명 칸은 실패해도 입력 유지 · 칸 아래 오류 · Esc = 서버 값 · 40자 넘으면 글자 수(DR-29), §7-15 머리 문단 사용처 정정(「프로젝트 등록 폼 · 상세 기간 칸 · 상세 총 매출 예상가 칸」) · ⑧ §7-15 뒤 **§7-16 페이지 줄 신설**(모양·현재 번호·첫/끝 미렌더·PC 8쪽 이상 첫·끝·현재 ±1 생략·한 페이지면 없음·요소 = 목록은 `next/link` `<Link>`(DR-18 — 편집 표가 있는 목록은 표를 다시 마운트하지 않는다)·화면 안 전환은 `<button>`·`href`/`onPageChange` 유니언·포커스(DR-23 — 새 쪽 활성 셀, 읽기 표는 섹션 제목/캡션)·ARIA·폰 44×44) + §6-1·§7-3 (자)가 모양을 §7-16으로 넘기는 문장 — **C-27 한 쪽 틈 문장과 폰 6쪽 창 문장은 쓰지 않는다**(04-29 Task 3이 기록과 함께 쓴다, DR-33) · ⑨ §7-15 `Form.Hint` 두 번째 쓰임(저장 결과 미리보기, 판정은 서버와 같은 domain 함수) · ⑩ §7-4 태그 순서 문장의 `대기` 색을 muted로 · ⑪ §8 규칙 3 아래 담당자 표기 한 줄 · ⑫ §7-3 (사) 끝 세 조항(저장 중 편집 잠금 · 오류가 남아도 1차 비활성 아님 · `seenStatus` 전부 거부와 미저장 편집이면 상태 트리거 비활성), 합계 행 줄 뒤 「오른쪽 끝은 한 줄 — `--danger` → `--warning` → `--muted` 순서, 저장 성공은 혼자, 붙여넣기 안내·단축키 상한 이유는 다음 저장 시도·붙여넣기 때 지움」, (가) 「행 전체가 권한 밖인 줄」 한 줄과 (다) 권한 밖 줄 건너뜀 요약, (다) 앱 형식일 때만 계산 열 무시 · 외화 경고, (나)·(아) 충돌 셀 키보드 한 줄, (마) 이전 차수 보관본 복원 줄과 표 위 줄 순서, 폰 전략 뒤 좁은 PC 보기 전용 한 줄 · ⑬ §7-9 첫 줄을 「화면의 주 편집 표 아래 한 곳 — 페이지 줄 바로 아래(없으면 합계 행 바로 아래), 같은 화면의 다른 편집 표에는 없다」로, 표기 「문장 없는 `짧은 라벨 kbd` 묶음」·1차 kbd와 겹치는 `저장` 빼기·그 사람에게 되는 것만 · ⑭ §7-8 838행 예외를 「확인의 근거 한 칸(사유 또는 날짜)」으로 + 승인일 예 · 근거 칸이 비거나 틀리면 1차 비활성 + 왼쪽 이유 · ⑯ §8 규칙 5 뒤 P0 문단, §6-3 다섯 상태 줄 ERROR 예의 안심 꼬리 삭제, §7-15 「검증 관문」·PARTIAL 행·§7-7 폼 행·§6-3 스케치의 「…할 수 없음 —」 접두 삭제. **스케치 셋(T17):** §6-0 공통 셸 스케치의 검색 kbd와 힌트 줄을 Ctrl 표기로(Task 1이 글리프는 이미 지웠다 — 여기서는 스케치의 항목이 Task 1 규칙과 맞는지 확인), §6-1 목록 스케치의 표 아래 합계 행·「더 보기 50건」을 지우고 필터 줄 바로 아래 `합계 (2026 귀속 · N건) 매출 … 수익률` 줄과 표 아래 `51–100 / 125건  이전 · 1 2 3 · 다음` 줄로, §6-2 상세 스케치에 머리 줄의 상태 태그·「상태 바꾸기」·부제의 `기간 …`·`고객 승인 {날짜} {이름}`, 표 위 잠김 한 줄 자리, 맨 아래 `조정` 그룹, 줄 수 상한 안내 자리, 합계 행 아래 `31–60 / 142줄` 페이지 줄, 표 아래 `차수` 섹션을 그린다(새 요소를 발명하지 않는다 — UI-SPEC S3·S4·S5·S12에 적힌 요소만). **새 토큰·색·서체·radius·폭 값을 만들지 않는다** — `docs/design/tokens.css`를 열지 않는다.

③ **⑩ 코드.** `ui/next-turn/NextTurn.tsx`의 `대기` 태그 kind를 muted로 바꾸고, 「결재·대기 둘 다 accent(블록 전용 색)」라고 적힌 18~20행 주석을 고친 §7-4에 맞춘다. `test/unit/ui/next-turn.test.ts`에 `대기`가 muted로 렌더되는 단언을 더한다(먼저 실패를 확인한다 — RED).

④ **문서 단언.** `test/unit/design-system-docs.test.ts`에 `describe("docs/design/SYSTEM.md — 2026-09-23 개정(04-08)")` 블록을 더해 UI-SPEC rev 5 「재실행 가능한 확인」 27줄 중 이 태스크 몫을 옮긴다 — 담당 표: 1 SYSTEM 글리프 0(넷으로 넓혀) · 4 옛 목록 페이지·옛 완료 라벨 0 · 5 `실행가만 고칠 수 있음` 0 · 6 `실행가와 새 줄만` ≥1 · 7 `대기(`--accent`)` 0 · 8 `NextTurn.tsx`에서 `대기`와 accent가 한 줄에 없음(파일을 읽는 소스 단언) · 9 중 `### 7-16` 한 줄(7-17은 04-46) · 10 합계 범위 문구 ≥2 · 13 `확인의 근거 한 칸` 한 줄 · 15 안심 꼬리 0 · 16 접두 0 · 17 `안내 문구를 최소` 한 줄 · 18 `열이 많은 표(읽기·편집)` 한 줄 · 19 `1024 이상에서만` ≥1 · 20 `1280 · 1024 · 375` ≥1 · 21 `매출 계약 금액` 0 · 22 옛 §7-9 「화면 하단 항상」 0 · 23 `견적 줄이 잠김` 0 · 24 `seenStatus` ≥1 · 25 `권한 밖인 줄` ≥1 · 26 합계 행 톤 순서 한 줄 · 27 `tokens.css` diff 0(검증 명령). 나머지 줄의 담당: 2·3(앱 코드 글리프·메타 키) = 04-28 `test/unit/ui/shortcut-notation.test.ts` · 9의 `### 7-17`·11·12(`Button.tsx`의 `reasonTone`·`aria-disabled`) = 04-46 · 14(`6쪽부터`) = 04-29 Task 3(DR-33). 여기에 `behavior`의 §6-2 스케치 낱말 여섯, (가) 잠김 행 셋째 칸 두 문자열, §7-16의 `next/link`·「새 쪽의 활성 셀」, DECISIONS 번호별 머리글(①②③④⑤⑥⑧⑨⑩⑪⑫⑬⑭⑯ + 진행 막대 = 15 — 04-46이 16 → 17로 바꾼다) 단언을 더한다. 이 테스트 파일을 SYSTEM.md 변경과 **같은 커밋**에 넣는다 — CI 경로 필터가 `docs/**`만 바뀐 커밋을 건너뛰기 때문이다(이 파일 머리 주석의 존재 이유).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/ui/next-turn.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 개정·스케치·기록 중 하나가 빠졌거나 기존 절의 계약 문장이 깨졌다</fails_when>
    <automated>git diff --stat d6b41cf -- docs/design/tokens.css</automated>
    <fails_when>출력이 비어 있지 않다 — 보완 계획 시작 커밋(d6b41cf) 이후 토큰 파일이 바뀌었다. 개정과 스케치 갱신은 토큰을 하나도 늘리지 않는다(UI-SPEC 「tokens.css diff 0줄」)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다 — 이 플랜의 완료 판정(프로덕션 빌드 기준). 등록 폼 단축키·`대기` 색·문서 개정이 기존 화면을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - DECISIONS.md에 `## 2026-09-23 — Phase 4(04-08)` 머리글이 번호별로 ①②③④⑤⑥⑧⑨⑩⑪⑫⑬⑭⑯ 각 한 줄 + 진행 막대 충돌 기록 한 줄이고(이 태스크 뒤 15 — 04-46 뒤 17), 기록 커밋이 SYSTEM.md 개정 커밋보다 앞서며, design-system-docs 테스트가 번호별 머리글을 단언한다
    - ④ 기록 머리글에 `D-78 개정(CEO-D10·D12)`이 있고, SYSTEM.md §7-3 (가) 잠김 행 셋째 칸이 rev 5 세 문자열이며 정산의 줄 추가를 렌더한다는 문장이 있다(DR-2)
    - 진행 막대 충돌 기록이 §1-3과 §5·§7-7·§7-10의 충돌·「이번에 만들지 않음」·퀵 태스크 이관을 말하고 SYSTEM.md의 §1-3 목록은 바뀌지 않았다
    - SYSTEM.md에 `### 7-16` 머리글이 한 줄이고 §7-3 안에 (자) 항목과 ⑫ 조항들((사) 세 조항 · 합계 행 한 줄 · 권한 밖 줄 · 충돌 셀 키보드 · (마) 이전 차수 보관본 · 폰 전략 좁은 PC)이 있다 · §7-16에 C-27·폰 창 문장이 **없다**(04-29 몫, DR-33)
    - §6-1 스케치에 「더 보기」가 없고 번호 페이지 줄·표 위 합계 줄이 있으며, §6-2 스케치가 `behavior`의 여섯 낱말을 담는다
    - design-system-docs 테스트의 새 describe 블록이 「재실행 가능한 확인」 담당 표의 이 태스크 몫 전부(1·4·5·6·7·8·9의 7-16·10·13·15~26)와 글리프 넷 0을 단언하고 초록이다 — 담당 표의 나머지 줄(2·3 · 9의 7-17·11·12 · 14)이 어느 태스크·플랜의 어느 테스트인지 describe 머리 주석에 적혀 있다
    - 기존 design-system-docs 단언이 하나도 삭제되지 않았다(개정과 충돌한 단언은 새 규칙으로 고쳐졌다) — `git diff d6b41cf -- test/unit/design-system-docs.test.ts`에서 기존 `it(`·`it.each(` 줄의 삭제만 있고 대응하는 추가가 없는 경우가 0이다
    - `NextTurn.tsx`의 `대기` kind가 muted이고 next-turn 단위 테스트가 그것을 단언한다
    - `docs/design/tokens.css` diff가 비어 있다
    - 네 검증 명령이 전부 초록이다(마지막 `CI=true pnpm test`)
  </acceptance_criteria>
  <done>SYSTEM.md 개정 열세 건(⑦·⑮는 04-46, ⑧의 C-27·폰 창은 04-29)과 스케치 셋이 기록과 짝을 이루어 반영되고, 진행 막대 충돌이 기록으로 남았으며, 뒤 플랜들이 근거로 삼을 규칙이 문서 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 키보드 → 등록 폼 | 단축키 조합이 제출을 일으킨다. 판정이 넓거나 가드가 없으면 의도하지 않은 제출·중복 등록이 난다 |
| 문서 → 이후 플랜 | SYSTEM.md 규칙이 뒤 플랜들의 계약이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-48 | Tampering | 등록 폼 Ctrl+Enter 제출 | medium | mitigate | 새 제출 경로를 만들지 않고 폼 표준 제출을 부른다 — 서버 액션의 인증(`authedActionClient`)·zod 검증·권한 판정이 버튼 클릭과 같은 경로로 한 번만 돈다. E2E가 결과(번호 부여)를 단언한다 |
| T-04-130 | Tampering | 등록 폼 연타·자동 반복·한글 조합·성공 뒤 이동 지연 | medium | mitigate | keydown이 `isCtrlCombo`(자동 반복·조합 가드)·진행 중·제출 래치(`submittedRef` — 성공 뒤 이동 전까지 서 있음)로 제출을 막고 `handleSubmit`도 같은 래치를 본다(C-06 · 엔지 리뷰 C). 연타 1건(b)과 이동 지연 중 재입력 1건(b2)을 E2E로 고정하고, 실제 IME는 04-31 사람 확인(필수 통과)·`/qa`에서 본다 |
| T-04-49 | Denial of Service | 브라우저 기본 동작 차단 | low | accept | 등록 폼은 Ctrl+Enter·Esc 두 키만 가로챈다. 견적 표의 조합 차단 범위는 04-28이 표 안 포커스로 한정한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm vitest run --project unit` 전부 통과(문서 단언·next-turn 포함)
- `project-register.spec.ts` 초록
- `docs/design/tokens.css` diff 0줄 · `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
- DECISIONS.md 기록 15건이 각자의 SYSTEM.md 개정보다 먼저 커밋됐다(⑦·⑮ 두 건과 ⑦의 `Button` 같은 커밋 규칙은 04-46)
- `CI=true pnpm test` 세 계층 통과
- 이 플랜의 diff에 `.planning/` 파일이 없다(D16 세 문구는 04-43)
</verification>

<success_criteria>
- 등록 폼에 적힌 Ctrl+Enter·Esc가 실제로 동작하고 연타가 중복 등록을 만들지 않는다
- 뒤 플랜들이 근거로 삼을 SYSTEM.md 규칙 열네 건(⑦·⑮는 04-46 · ⑧의 C-27·폰 창 두 문장은 04-29)과 스케치 셋이 기록과 함께 확정됐다
- 진행 막대의 색 충돌이 기록되고 막대는 만들지 않았다
- 이 플랜이 04-04의 사람 확인을 기다리지 않는다(D21)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 문서 기록 | `docs/design/DECISIONS.md` 2026-09-23 Phase 4(04-08) 항목 15건(①~⑯ 중 ⑦·⑮ 제외 + 진행 막대 충돌 — 04-46이 ⑦·⑮를 더해 17) |
| 규칙 개정 | `docs/design/SYSTEM.md` §2-4 · §4-2 · §6-0~§6-2 스케치 · §6-1 · §6-3 · §7-2 · §7-3(편집 키·(가) 셋째 칸·(나)·(다)·(마)·(사)·(아)·(자) 신설·폰 전략) · §7-4 · §7-5 · §7-7 · §7-8(⑭ 확인 근거 한 칸) · §7-9 · §7-10 · §7-15 · **§7-16 신설**(C-27·폰 창 문장은 04-29) · §8 · §11 (⑦의 §7-1·§10과 ⑮의 §7-17·§7-8 한 줄은 04-46) |
| 동작 변경 | `ProjectForm` Ctrl+Enter 표준 제출(`isCtrlCombo` · 진행 중 · `submittedRef` 래치) · Esc(내부 컨트롤 먼저 · 빈 폼 → 목록 · 입력 있음 → 폼 유지 — 「입력 버리기」와 2차 「취소 Esc」 버튼은 04-46) · `isFormPristine`(폼 안 함수 — 04-46이 같은 비교를 쓴다) |
| 모듈(신규, lib) | `lib/shortcut.ts` — `isCtrlCombo(event, key)`(04-28 견적 표 키보드가 같이 쓴다) |
| 표기 변경 | 상단 바 검색 kbd `Ctrl+K`(표기만) · `NextTurn` `대기` → muted |
| 테스트 | `test/unit/design-system-docs.test.ts`(2026-09-23 describe — 「재실행 가능한 확인」 담당 표 · 글리프 넷 · 스케치 · 기록 15건) · `test/unit/ui/next-turn.test.ts`(대기 단언) · `test/unit/lib/shortcut.test.ts`(신규) · `test/e2e/project-register.spec.ts`(Ctrl+Enter · 연타 1건 · 이동 지연 1건 · Esc 두 갈래) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 견적 표 Ctrl 전용 판정·`preventDefault`·글리프·힌트 줄·충돌 셀 키보드(DR-25)·`test/unit/ui/shortcut-notation.test.ts`(04-28) · §7-16 C-27·폰 6쪽 창 문장과 기록(04-29, DR-33) · REQUIREMENTS·ROADMAP 세 문구(04-43, D16) · 퀵 태스크 문서 F-03 메모(DECISIONS ② 본문과 SUMMARY로 대체) · 소비처의 `reasonTone="info"`·확인 모달 사용(04-16 · 04-21 · 04-22/04-30 · 04-24 · 04-42) · **⑦·⑮ 기록·규칙 · `Button` 개정 · `ui/confirm-dialog` · 삭제 확인 이관 · 등록 폼 입력 버리기(04-46 — 체커 revision 1 W2)** |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| OV-7 / 사용자 D21 — 견적 표와 무관한 플랜이 04-04 확인을 기다린다 | HIGH | Plan 04-08 전체: 견적 표 파일을 04-28로 떼고 `depends_on`에서 04-04를 뺐다(depends_on DAG 웨이브 3 — 04-04와 같은 웨이브지만 의존하지 않는다) |
| C-06 — Ctrl+Enter 연타·IME·진행 중 이중 등록 | WARNING | Plan 04-08, Task 1 ③⑤: 세 가드 + `handleSubmit` 가드 + E2E(b) 1건 단언 |
| C-07 — 적힌 단축키 넷이 동작하지 않음 | WARNING | Plan 04-08, Task 1 ③(Esc 배선)·②(`↵` 줄) · truths 좁힘(상단 바 Ctrl+K는 F-03 예외로 기록) · 견적 표 힌트 줄 Tab·Ctrl+C는 04-28이 보류, 04-19가 복원 |
| C-12 / 사용자 D16 — `.planning/` 수동 편집 범위 | DECISION | Plan 04-08, Task 3: PROJ-04·PROJ-03·기준 4 세 곳만, GSD 도구 커밋, 범위 밖 diff 0 검증 · OPEN-ITEMS·퀵 태스크 문서 편집 삭제 |
| C-10 / 사용자 D17 — 진행 막대 색 충돌 | DECISION | Plan 04-08, Task 2 ①: DECISIONS 충돌 기록(막대 미구현) |
| T17 — SYSTEM.md §6-0·§6-1·§6-2 스케치 낡음 | WARNING | Plan 04-08, Task 2 ②: 스케치 셋 갱신 + 단언 |
| A-24(D13)·A-25(D14)·A-26(D10)·S1·S3·S4(D19) — 옛 ⚠ 기본값 일곱 | DECISION | 사용자 답으로 전부 확정 — 기록처는 CEO 리뷰 결정 원장, 이 플랜은 OPEN-ITEMS를 고치지 않는다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| ROADMAP Goal·기준 1·5·트레일링 · PROJ-06의 낡은 상태 이름 | 사용자 D16이 세 곳만 승인 — SUMMARY 「남은 낡은 문구」 표로 넘겨 사용자가 `/gsd-phase` 편집 여부를 정한다 |
| 상단 바 `Ctrl+K`의 동작(F-03) | Phase 4 머지 뒤 디자인 잔여 퀵 태스크(CONTEXT Deferred) — 표기만 바꾼다 |
| 진행 막대 구현(C-10) | 사용자 D17 「만들지 않고 기록」 — TODOS.md 「목록 상단 2px 로딩 막대」 |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 C §1 P2 — 성공 뒤 이동 사이의 두 번째 제출 | WARNING | Plan 04-08, Task 1 ③-b `submittedRef` 래치 · E2E b2(이동 요청 붙잡기 — 공백 9) · T-04-130 |
| 엔지 리뷰 C §2 P2 — Ctrl 조합 판정 두 구현 | WARNING | Plan 04-08, Task 1 ③ `lib/shortcut.ts` `isCtrlCombo` + 단위 표 (04-28이 같은 함수를 쓴다) |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 98acc74 (plan-design-review, `docs/designs/plant8-erp-phase4-design-review-260923.md` · UI-SPEC rev 5 재계획 그룹 C)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-1 — 개정 원문이 rev 4의 옛 행 범위를 가리킴 | CRITICAL | Plan 04-08 context·Task 1·2 read_first를 rev 5 「SYSTEM.md 개정」 절 이름 + 현재 행으로 · 개정 ①~⑯ |
| DR-2 — ④가 D10 이전 원문 | CRITICAL | Plan 04-08, Task 2 ②④: ④ CEO-D10·D12 문구 · (가) 셋째 칸 한 문자열 · 정산 줄 추가 렌더 · DECISIONS 「D-78 개정(CEO-D10·D12)」 · 문서 단언 |
| DR-10 · DR-11 — `Button` reasonTone · aria-disabled(교차 그룹 계약 1) | HIGH | Plan 04-08, Task 3 ②: ⑦ 문서와 같은 커밋의 코드 · 단위 · 기존 사용처 E2E · T-04-360 (진행 중도 aria-disabled — rev 5, `<probe_fallback>`) |
| DR-12 · DR-20 · DR-27 — 공용 확인 모달(교차 그룹 계약 2) · 확인 근거 한 칸 · 등록 폼 Esc | HIGH | Plan 04-08, Task 2 ⑭ · Task 3 ③④⑤⑥: §7-17 · `ui/confirm-dialog` · 삭제 확인 이관 · 입력 버리기 · T-04-361·362 |
| DR-14 · DR-16 · DR-22 · DR-24 · DR-25 · DR-31 · DR-36 · DR-4 · DR-3 · DR-5 · DR-6 — 편집 표 시스템 규칙 | HIGH | Plan 04-08, Task 2 ②: ③ rev 5 보강 · ⑫ · ⑬ (코드는 각 소유 플랜) |
| DR-33 — C-27·폰 창 기록 | LOW | Plan 04-08, Task 2 ②: ⑧에서 두 문장을 빼고 04-29 Task 3이 기록과 함께 쓴다 |
| P0 · DR-17 · DR-21 — 안내 문구 최소 | DECISION | Plan 04-08, Task 2 ②: ⑯ §8 규칙 5 · 접두·안심 꼬리 삭제 · 문서 단언 |
| 「재실행 가능한 확인」 27줄 이관 | NOTE | Plan 04-08, truths · Task 2 ④ 담당 표(나머지 줄은 Task 3 · 04-28 · 04-29) |
| 04-08 크기(코드 부분 추가) | NOTE | 코드 부분은 뗄 수 없어(⑦ 같은 커밋 · 확인 모달 → `Button` · `project-form.tsx` 공유) 이 플랜에 두고, 옛 Task 3(D16)을 04-43으로 옮겼다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W2 — 04-08 크기(110k · 20파일 — 규칙: 같은 커밋 제약이 막지 않으면 분할) | WARNING | 옛 Task 3을 신설 **04-46**으로 옮김 — 이 플랜 10파일 · 62k(2 태스크), 04-46 15파일 · 56k. Round 3의 「뗄 수 없다」는 같은 웨이브 분할(04-43) 판단이었고, 04-46은 바로 뒤에 서서 ⑦ SYSTEM.md 줄 + `Button` 코드 같은 커밋 제약을 자기 Task 1 안에서 지킨다. 교차 그룹 계약 1·2 주인 = 04-46. Task 2에 `CI=true pnpm test`를 완료 판정으로 더함. `depends_on` 무변경 · 04-28·04-29가 04-46에 의존 |
| 계약 1·2 소비처 도달성 | NOTE | 04-16 · 04-21 · 04-22 · 04-23 · 04-24 · 04-26 · 04-30 · 04-42 등은 04-28·04-29 → 04-46 사슬로 닿는다(오케스트레이터 보고에 목록) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-08-SUMMARY.md` when done
</output>
