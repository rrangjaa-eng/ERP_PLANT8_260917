---
phase: 04-project-quote-ledger
plan: 10
type: execute
wave: 5
depends_on: ["04-08"]
files_modified:
  - db/schema/code-tables.ts
  - db/migrations/0011_code_item_descriptions.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0011_snapshot.json
  - repositories/code-tables.ts
  - domain/code-tables/index.ts
  - domain/seed/index.ts
  - app/(app)/admin/code-tables/page.tsx
  - app/(app)/admin/code-tables/code-item-form.tsx
  - app/(app)/admin/code-tables/actions.ts
  - app/(app)/admin/code-tables/actions.registry.ts
  - test/integration/code-item-description.test.ts
  - test/e2e/code-tables.spec.ts
autonomous: true
requirements: [UX-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "관리자가 코드표 화면에서 값마다 설명 칸을 이름 칸과 같은 인라인 편집으로 고치고, 포커스를 잃으면 저장된다(D-93, UI-SPEC S14)"
    - "설명이 40자를 넘으면 서버가 거부하고 칸 아래 `설명이 40자를 넘습니다 · 한 문장으로 줄여 주세요` 한 줄이 나오며 칸의 값은 서버 값으로 되돌아간다(S14 error, Copywriting)"
    - "기본 제공 값(상태·견적 소분류·증빙 종류)은 설명이 채워진 채로 시작한다 — 새 DB는 시드가, 이미 돌고 있는 DB는 마이그레이션이 비어 있는 설명만 채운다(D-93 「기본 제공 값은 시드로 설명을 채우고」)"
    - "새 마이그레이션의 번호는 적용 직전 journal 최고 번호 + 1(04-02의 0010 뒤)이고 파일명·snapshot·journal tag가 생성기 출력과 같다 — 계획 번호 0011과 다르면 실제 이름이 SUMMARY에 있다"
    - "코드표 화면의 표가 값 · 이름 · 설명 · 정렬 · 상태 · 동작 여섯 열이고 설명이 없는 값은 `—`다(S14 populated·empty)"
    - "「코드 추가」 폼에 선택 칸 `설명`(긴 칸)이 있다(S14)"
  prohibitions:
    - statement: "설명 칸이 사용법 안내문이나 긴 설명서가 되어 고르는 자리의 화면을 밀어내서는 안 된다. 설명은 그 값이 무엇인지 말하는 한 문장이고, 40자 상한은 화면 규칙(한 줄)이지 권고가 아니다 — 서버가 넘는 값을 받지 않는다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/0011_code_item_descriptions.sql"
      provides: "code_items.description 컬럼 + 기본 제공 값 설명 채우기(비어 있는 행만)"
      contains: "description"
    - path: "domain/code-tables/index.ts"
      provides: "설명 읽기(DTO)·고치기(40자 서버 검증)"
      exports: ["updateCodeItemDescription", "CODE_ITEM_DESCRIPTION_MAX"]
    - path: "test/integration/code-item-description.test.ts"
      provides: "설명 저장·40자 거부·권한 거부·시드 채움 증명"
      min_lines: 40
  key_links:
    - from: "app/(app)/admin/code-tables/code-item-form.tsx"
      to: "app/(app)/admin/code-tables/actions.ts"
      via: "CodeItemDescriptionInput의 blur 저장이 updateCodeItemDescriptionAction을 부르고 실패 시 서버 값으로 되돌린다(CodeItemLabelInput 선례)"
      pattern: "updateCodeItemDescriptionAction"
    - from: "domain/seed/index.ts"
      to: "db/migrations/0011_code_item_descriptions.sql"
      via: "새 DB(시드)와 기존 DB(마이그레이션)의 설명 문장이 글자 그대로 같다 — 대조 명령이 본다"
      pattern: "description"
---

<objective>
**As a** 코드표를 관리하는 관리자, **I want to** 「무대·시공」·「기타소득」·「정산」 같은 값마다 한 문장 설명을 코드표 화면에서 고치고, 기본 제공 값은 설명이 채워진 채로 시작하기를, **so that** 값을 고르는 모든 자리가 같은 설명을 쓰고 비슷한 값 사이에서 잘못 고르는 일이 준다.

이 플랜은 D-93의 **관리 화면 → 저장** 쪽을 끝낸다. 코드표 설명의 규칙(40자 · 고른 값 하나의 설명만 · 오류가 이긴다)은 04-08이 SYSTEM.md 개정 ⑥으로 먼저 적었다. **2026-09-23 체커 1차 분할:** 옛 04-10(20파일)의 고르는 자리(공용 `Select` 설명 힌트 · 거래처 기본 증빙 종류 · 견적 소분류 옵션)와 코드표 화면의 폰 P2 배치·S14 DOM 감사는 **04-25**로 옮겼다. 이 플랜은 컬럼 · 마이그레이션 · 인라인 편집 · 시드 설명 · 「코드 추가」 설명 칸만 맡는다.

Purpose: 새 상태 다섯(D-75)을 고르는 목록이 행마다 설명 2행을 요구하므로(S7) 04-06(상태 마이그레이션이 정산·완료 설명을 넣는다)보다 먼저 설명 컬럼이 있어야 하고, 04-25의 `Select` 힌트가 이 DTO의 `description`을 읽는다.

Output: `code_items.description` 컬럼 + 마이그레이션 0011(번호는 직전 최고 + 1), 설명 인라인 편집, 시드 설명, 「코드 추가」 설명 칸, 통합 1 · E2E 1 확장.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 535–545행(S14 상태) · 1366–1373행(S14 적용 규칙 — 관리 화면) · 342행(Copywriting Error — 코드표 설명)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 810–816행(§8 D-93 — 스키마·40자·CodeItemLabelInput)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 406행 · 425행 · 496–498행(CodeItemLabelInput 선례)
# docs/design/SYSTEM.md §7-3 코드표 셀 · §7-2 설정 설명(04-08 개정 ⑥ 반영본)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-04(잘못된 입력은 즉시 안내 — 코드표 설명의 관리·저장 부분)** 다. D-93은 요구사항 ID가 따로 없는 전역 결정이라 가장 가까운 UX-04 아래 둔다. UI-SPEC `## UI Considerations` S14의 여덟 항목 중 empty·error·populated는 truths로, loading(해당 없음)은 truths의 「같은 조회에 실려 온다」로 덮었다. partial(고르는 자리)과 backstop 셋(long-text · overflow · zero-one-many)은 그 화면과 DOM 감사를 맡는 **04-25**로 옮겼다 — 조용히 버린 항목은 없다.

**계획 단계 가정(표면화):** 기본 제공 값의 설명 문장은 이 플랜이 Task 2 ① 목록으로 정한다(D-93은 「시드로 채운다」까지만 정했다). 전부 40자 안 한 문장 · 명사형이고, 세율 같은 **설정값을 설명에 적지 않는다**(설정이 바뀌면 설명이 거짓이 된다). 관리자가 화면에서 언제든 고친다.

**화면 검증 순서:** 이 플랜이 바꾸는 관리 화면(설명 열·인라인 칸·추가 폼 칸)의 독립 DOM 감사는 폰 P2 CSS를 넣는 04-25가 같은 화면을 한 번에 잰다(같은 웨이브 바로 다음) — 이 플랜은 싼 게이트와 전체 게이트(`CI=true pnpm test`)를 돈다. CSS 없이 감사하면 04-25에서 한 번 더 재야 해 CLAUDE.md의 「전체 게이트를 두 번 돌리지 않기 위한 순서」와 어긋난다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 설명 한 칸을 스키마부터 코드표 화면 인라인 편집까지</name>
  <files>db/schema/code-tables.ts, db/migrations/0011_code_item_descriptions.sql, db/migrations/meta/_journal.json, db/migrations/meta/0011_snapshot.json, repositories/code-tables.ts, domain/code-tables/index.ts, app/(app)/admin/code-tables/page.tsx, app/(app)/admin/code-tables/code-item-form.tsx, app/(app)/admin/code-tables/actions.ts, app/(app)/admin/code-tables/actions.registry.ts, test/integration/code-item-description.test.ts, test/e2e/code-tables.spec.ts</files>
  <read_first>
    - `db/schema/code-tables.ts`(전문) — 컬럼 목록과 주석 규약
    - `db/migrations/0010_revenue_entries.sql`(전문) — 락 타임아웃 한 쌍 위치와 이 페이즈의 마이그레이션 문체
    - `db/migrations/meta/_journal.json` — 마지막 항목(0010)의 `idx`·`tag`(이 플랜의 번호 = 그 `idx` + 1)
    - `repositories/code-tables.ts`(전문 105행) — `updateCodeItemLabel`·`seedCodeItem` 형태
    - `domain/code-tables/index.ts`(전문) — `CodeItemDto`·`CODE_ITEM_DTO_SPEC`·`updateCodeItemLabel`의 권한·보관 가드·행동 로그
    - `app/(app)/admin/code-tables/code-item-form.tsx`(전문) — `CodeItemLabelInput`(70–93행)의 blur 저장·실패 되돌림·오류 한 줄
    - `app/(app)/admin/code-tables/actions.ts`·`actions.registry.ts`(전문) — `updateCodeItemLabelAction`과 등록
    - `app/(app)/admin/code-tables/page.tsx`(전문) — 표 열과 캡션·`th scope`(A-M3 산출물 — 유지한다)
    - `test/integration/mast-04-code-item-label.test.ts`(전문) — 이 플랜 통합 테스트의 직접 선례
    - `test/e2e/code-tables.spec.ts`(전문)
    - `.squawk.toml` — 배제 규칙 넷(고치지 않는다)
  </read_first>
  <behavior>
    - 관리자가 설명 `무대·부스 설치와 철거 공사`를 저장하면 DTO의 `description`이 그 값이다
    - 41자 설명은 거부되고 이유가 `설명이 40자를 넘습니다 · 한 문장으로 줄여 주세요`다 — DB 값은 그대로다
    - 빈 문자열로 저장하면 `description`이 null이 된다(`—` 렌더)
    - 코드표 쓰기 권한이 없는 계급의 저장은 거부된다
    - 보관된 코드 항목의 설명 저장은 거부된다(`updateCodeItemLabel`과 같은 가드)
    - 성공한 저장은 행동 로그 한 줄을 남긴다
  </behavior>
  <action>
① `db/schema/code-tables.ts`에 `description: text("description")`(NULL 허용, 기본값 없음)을 더하고 주석에 D-93·40자 상한은 서버 검증이라는 것을 적는다(DB CHECK 아님 — 설정 `hint`와 같은 결, RESEARCH §8).

② **[BLOCKING] 스키마 반영:** `pnpm db:generate --name code_item_descriptions`로 다음 번호 마이그레이션을 만든다. **번호 규칙:** 계획 번호는 `0011`이고, 생성기가 매기는 번호는 적용 직전 journal의 최고 `idx` + 1이어야 한다(04-02의 0010 뒤). 생성기가 다른 번호를 주면 파일명·snapshot·journal tag를 생성기 출력 그대로 두고(손으로 번호를 바꾸지 않는다) 실제 이름을 SUMMARY에 적는다(04-01 Rule 1 선례) — 아래 번호 검증 명령이 journal 연속성·파일 수·snapshot 존재를 본다. 생성된 SQL 첫 문장 앞에 `SET LOCAL lock_timeout = '1s';`·`SET LOCAL statement_timeout = '5s';` 한 쌍을 둔다. 같은 파일 끝에 Task 2 ①의 기본 제공 값 설명을 **`description IS NULL`인 행만** 채우는 `UPDATE "code_items" SET "description" = … WHERE "table_key" = … AND "value" = … AND "description" IS NULL` 문장들을 지금 함께 넣는다(마이그레이션 파일을 적용 뒤 고치지 않기 위해 — 문장 목록은 Task 2 ①에 있다). `pnpm lint:sql`이 `Found 0 issues`인지 확인하고, 걸리면 `.squawk.toml`이 아니라 SQL을 고친다. `bash scripts/dev-db.sh && pnpm db:migrate`로 적용한다.

③ `repositories/code-tables.ts`에 `updateCodeItemDescription(viewer, id, description: string | null)`을 더하고 `insertCodeItem`·`seedCodeItem` 입력에 선택 `description`을 받게 한다.

④ `domain/code-tables/index.ts`: `CodeItemDto`에 `description: string | null`을 더하고 DTO 스펙은 기존 `code_item.label` 정보 항목으로 게이트한다(새 정보 항목을 만들지 않는다 — 이름과 같은 구조 정보). `CODE_ITEM_DESCRIPTION_MAX = 40`과 `updateCodeItemDescription(viewer, id, description, deps?)`를 `updateCodeItemLabel`과 같은 권한 판정·보관 가드·행동 로그 순서로 만든다. 공백만 있는 값은 null로 저장하고, 40자를 넘으면 Copywriting 문구 그대로 `UserFacingError`를 던진다.

⑤ `app/(app)/admin/code-tables/actions.ts`에 `updateCodeItemDescriptionAction`(zod: id · description string, 길이 검증은 domain이 한다)을 더하고 `actions.registry.ts`에 등록한다(D-38 누수 스캔).

⑥ `code-item-form.tsx`에 `CodeItemDescriptionInput({ id, description })`을 `CodeItemLabelInput` 구조 그대로(blur 저장 · `onError`에서 서버 값으로 되돌림 · 칸 아래 `--danger` 한 줄 · `aria-invalid`·`aria-describedby` · 접근 이름 `{이름} 설명`) 만든다. `page.tsx` 표 열을 값 · 이름 · **설명** · 정렬 · 상태 · 동작 여섯으로 하고 설명 셀에 이 컴포넌트를 둔다. 캡션·`th scope`는 유지한다.

⑦ `test/integration/code-item-description.test.ts`에 위 `behavior` 여섯 경우를 쓰고(먼저 RED), `test/e2e/code-tables.spec.ts`에 관리자가 설명을 고치고 다른 칸을 눌러 저장한 뒤 새로 고쳐도 값이 남는 케이스와 41자 입력 시 오류 한 줄이 나오고 값이 되돌아가는 케이스를 더한다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_code_item_descriptions'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/code-item-description.test.ts test/integration/mast-04-code-item-label.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 설명 저장·검증·누수 스캔 등록 중 하나가 빠졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/code-tables.spec.ts</automated>
    <fails_when>lint·typecheck·build 중 error가 나오거나 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션 파일이 `description` 컬럼 추가와 `description IS NULL` 조건의 시드 설명 UPDATE를 함께 담고 락 타임아웃 한 쌍으로 시작한다
    - 새 마이그레이션의 번호 = 이 플랜 실행 직전 journal 최고 `idx` + 1이고, 파일명(확장자 제외)·`meta/{번호}_snapshot.json`·journal tag가 생성기가 실제로 만든 이름과 같다(번호 검증 명령) — 계획 번호 0011과 다르면 실제 이름이 SUMMARY에 있다
    - `.squawk.toml` diff가 0줄이고 `pnpm lint:sql`이 `Found 0 issues`다
    - `updateCodeItemDescription`이 40자 초과를 Copywriting 문구로 거부하고 통합 테스트가 거부 뒤 DB 재조회로 무변경을 단언한다
    - `updateCodeItemDescriptionAction`이 `actions.registry.ts`에 등록되고 누수 스캔 통합 테스트가 초록이다
    - 코드표 표가 여섯 열이고 캡션·`th scope`가 남아 있다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>관리자가 코드표 화면에서 설명을 인라인으로 고치고, 40자 상한이 서버에서 지켜지며, DB에 설명 컬럼이 실제로 적용됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 기본 제공 설명 시드 + 코드 추가 폼 설명 칸</name>
  <files>domain/seed/index.ts, repositories/code-tables.ts, domain/code-tables/index.ts, app/(app)/admin/code-tables/code-item-form.tsx, app/(app)/admin/code-tables/actions.ts, test/integration/code-item-description.test.ts, test/e2e/code-tables.spec.ts</files>
  <read_first>
    - `domain/seed/index.ts` 20–100행(`PROJECT_STATUS_CODES`·`QUOTE_SUBCATEGORY_CODES`·`EVIDENCE_TYPE_CODES`)
    - `app/(app)/admin/code-tables/code-item-form.tsx` 1–60행(「코드 추가」 폼)
  </read_first>
  <behavior>
    - 빈 DB에 시드를 돌리면 project_status·quote_subcategory·evidence_type의 기본 값 전부에 설명이 있다
    - 관리자가 설명을 고친 값은 시드를 다시 돌려도 덮어쓰이지 않는다
    - 「코드 추가」 폼에서 설명과 함께 추가하면 DTO에 설명이 있다 · 41자면 거부된다
  </behavior>
  <action>
① `domain/seed/index.ts` 세 상수에 `description`을 더하고 `seedCodeItem` 호출이 그 값을 넣는다(기존 `onConflictDoNothing` 그대로 — 관리자가 고친 설명을 시드가 덮지 않는다). 설명 문장(각 40자 안, 설정값·세율 숫자 없음) — Task 1 ② 마이그레이션의 UPDATE 문과 **글자 그대로 같아야 한다**:
- project_status: bidding `제안·PT 단계 · 쌓인 비용은 진행 뒤 프로젝트 비용` · in_progress `수주 확정 · 종료일 다음 날 자동으로 정산` · lost `수주 실패 · 쌓인 비용은 팀 미수주 비용` (settled는 04-06이 지우므로 설명을 넣지 않는다 · 정산·완료 두 값의 설명은 04-06이 넣는다)
- quote_subcategory: stage_construction `무대·부스 설치와 철거 공사` · print_production `현수막·배너·인쇄물·소품 제작` · staffing `진행요원·MC·모델 등 사람 비용` · etc `위 분류에 들지 않는 비용`
- evidence_type: tax_invoice `과세 거래 · 부가세가 붙는 세금계산서` · invoice `면세 거래 · 부가세 없는 계산서` · card_receipt `법인카드 결제 전표` · cash_receipt `지출 증빙용 현금영수증` · other_income `강사료·경품 등 일시 소득 · 원천징수 대상` · business_income `프리랜서 용역 대가 · 원천징수 대상` · overseas_invoice `해외 거래처 인보이스 · 부가세 없음`

② 「코드 추가」 폼에 선택 칸 `설명`(긴 칸, `TextField`)을 더하고 `createCodeItemAction`·`createCodeItem`이 받아 같은 40자 검증을 지나게 한다.

③ 통합 테스트에 시드 두 경우(빈 DB 채움 · 고친 값 보존)와 추가 폼 두 경우를 더하고, `code-tables.spec.ts`에 「코드 추가」 폼에서 설명과 함께 추가하는 케이스를 더한다. 폰 375 배치(P2 접힌 줄)와 그 E2E·DOM 감사는 04-25다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/code-item-description.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 시드 설명이 비었거나 관리자가 고친 설명을 덮었다</fails_when>
    <automated>node -e "const fs=require('fs');const m=fs.readdirSync('db/migrations').find(f=>/_code_item_descriptions\.sql$/.test(f));const sql=fs.readFileSync('db/migrations/'+m,'utf8');const seed=fs.readFileSync('domain/seed/index.ts','utf8');const re=/description:\s*\"([^\"]+)\"/g;let x,miss=[];while((x=re.exec(seed))){if(!sql.includes(x[1]))miss.push(x[1])}if(miss.length){console.error('SEED/MIGRATION MISMATCH: '+miss.join(' | '));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 오류에 `SEED/MIGRATION MISMATCH:`로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 DB(시드)와 기존 DB(마이그레이션)의 설명이 갈렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다 — 추가 폼 설명 칸 E2E가 실패하거나 코드표를 쓰는 기존 화면이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 시드 상수 세 개의 기본 값(settled 제외)에 설명이 전부 있고, 설명 문장이 마이그레이션 UPDATE와 글자 그대로 같다(검증 명령이 대조한다)
    - 모든 시드 설명이 40자 이하다(통합 테스트가 단언)
    - 시드 재실행이 관리자가 고친 설명을 덮지 않는다는 케이스가 통합 테스트에 있다
    - 「코드 추가」 폼에 선택 칸 `설명`이 있고 40자 검증을 지난다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>기본 제공 값이 설명과 함께 시작하고, 관리자는 추가·수정 두 경로에서 설명을 다룬다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 브라우저 → 설명 저장 액션 | 자유 텍스트가 코드표에 저장되고 여러 화면에 렌더된다 |
| code_items → 여러 화면 | 설명이 등록 폼·관리 폼·표 셀·상태 목록에 그대로 나간다(고르는 자리는 04-25) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-54 | Elevation of Privilege | `updateCodeItemDescriptionAction` | high | mitigate | `authedActionClient` + domain이 코드표 쓰기 권한(`can`)을 판정하고 보관된 항목을 거부한다. 쓰기 권한 없는 계급의 저장 거부를 통합 테스트로 고정한다 |
| T-04-55 | Tampering | 설명 텍스트 렌더(관리 화면) | medium | mitigate | 설명은 React 텍스트 노드로만 렌더한다(HTML 삽입 경로를 만들지 않는다). 40자 서버 상한이 긴 페이로드를 막는다. 고르는 자리의 렌더는 04-25가 같은 규칙을 따른다 |
| T-04-56 | Information Disclosure | CodeItemDto.description | low | mitigate | 기존 `code_item.label` 정보 항목으로 게이트하고 DTO 레지스트리에 등록해 누수 스캔이 덮는다 |
| T-04-57 | Denial of Service | 마이그레이션 0011 | low | mitigate | NULL 허용 컬럼 추가(표 재작성 없음) + 락 타임아웃 한 쌍. UPDATE는 기본 제공 값 14행 안팎이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`, `.squawk.toml` 무변경, 마이그레이션 번호 검증 명령 초록
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 시드 설명 = 마이그레이션 설명(대조 명령 초록)
</verification>

<success_criteria>
- 관리자가 코드표 값마다 한 문장 설명을 고치고 40자 상한이 서버에서 지켜진다
- 기본 제공 값이 설명과 함께 시작한다(새 DB·기존 DB 둘 다)
- 04-25(고르는 자리)와 04-06(정산·완료 설명)이 이 컬럼 위에 선다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컬럼(신규) | `code_items.description` text NULL |
| 마이그레이션 | `db/migrations/0011_code_item_descriptions.sql`(계획 번호 — 실제 번호는 생성기 기준, 직전 최고 + 1) + `meta/0011_snapshot.json` + `_journal.json` 항목 |
| 상수 | `CODE_ITEM_DESCRIPTION_MAX = 40` |
| 함수 | `updateCodeItemDescription`(domain) · `updateCodeItemDescription`(repository) |
| Server Action | `updateCodeItemDescriptionAction`(등록 포함) · `createCodeItemAction` 설명 인자 |
| 컴포넌트 | `CodeItemDescriptionInput`(신규) |
| 시드 | 상태 3 · 견적 소분류 4 · 증빙 종류 7의 기본 설명 |
| 변경된 기존 심볼 | `CodeItemDto`(+description) · 코드표 표 여섯 열 |
| 테스트 | `test/integration/code-item-description.test.ts`(신규) · `code-tables.spec.ts` 확장 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | `Select` 설명 힌트(`SelectOption.description`) · 거래처 기본 증빙 종류 힌트 · 견적 소분류 옵션 설명(`listProjectFormReferences`) · 코드표 폰 P2 CSS · `vendor-edit.spec.ts`·`mobile-code-tables.spec.ts` 확장 · S14 backstop 셋과 DOM 감사(04-25) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-10-SUMMARY.md` when done
</output>
