---
phase: 04-project-quote-ledger
plan: 11
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - lib/kst-date.ts
  - test/unit/lib/kst-date.test.ts
  - domain/projects/status-transitions.ts
  - domain/projects/auto-transition.ts
  - domain/projects/period.ts
  - domain/projects/responsibles.ts
  - domain/projects/status.ts
  - domain/projects/index.ts
  - domain/projects/ledger.ts
  - domain/rules/register.ts
  - repositories/projects.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/period-field.tsx
  - app/(app)/projects/[id]/status-change.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/auto-transition.test.ts
  - test/unit/domain/project-period.test.ts
  - test/integration/project-auto-settlement.test.ts
  - test/integration/project-period.test.ts
  - test/e2e/project-period.spec.ts
  - test/e2e/project-lifecycle.spec.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "진행 프로젝트는 종료일 다음 날 00:00(KST)부터 정산이다 — 종료일 2026-09-17이면 2026-09-17T14:59:59Z(KST 23:59:59)에는 진행, 2026-09-17T15:00:00Z(KST 9/18 00:00)부터 정산이다(D-76)"
    - "KST 날짜 경계는 애플리케이션이 주입된 현재 시각에서 계산해 쿼리에 값으로 넘긴다 — DB 서버의 시간대 설정과 무관하게 같은 결과다(RESEARCH Pitfall 7, 서버 시간대 Gap)"
    - "자동 전환은 멱등이다 — 같은 날 몇 번을 판정해도(동시에 두 요청이 와도) 상태 변경은 한 번이고 행동 로그 `status_change`는 한 줄이다(D-76)"
    - "자동 전환의 행동 로그 행위자는 시스템이다 — 로그의 사람 id가 비어 있고 detail에 발효일(종료일 다음 날)이 적힌다(D-76, SYSTEM_VIEWER)"
    - "상세를 열거나 목록을 열거나 그 프로젝트에 저장·상태 전환을 시도하면, 판정 전에 자동 전환이 먼저 적용된다 — 자정을 넘겨 연 화면에서 저장해도 정산 규칙이 적용된다(D-76 읽기 시점 판정)"
    - "상세 머리 줄 부제의 상태 날짜는 자동 정산이면 발효일(종료일 다음 날)이다 — 실제로 판정이 돈 날이 아니다(D-50·D-76, UI-SPEC S3 `정산 2026-09-17`)"
    - "종료일이 지난 수주중은 자동으로 바뀌지 않고 상세 상태 태그 오른쪽에 `종료일 지남`(`--fs-sm --warning` 글자)이 보이며, 상태를 바꿀 수 없는 사람에게는 `종료일 지남 · 팀장 {이름}`(팀장이 없으면 글자만)이다(D-81, Copywriting 표시 — 종료일 지남)"
    - "상세 부제에 `기간 2026-09-12 ~ 2026-09-16`(없으면 `기간 미정`, 한쪽만 있으면 `2026-09-12 ~ —`)이 보이고, 고칠 수 있는 사람에게만 바로 오른쪽에 3차 「기간 바꾸기」가 있다(S13)"
    - "기간을 고칠 수 있는 사람: 수주중·진행·미수주 = 담당 PM + 팀장 이상(`projects.status`) · 정산 = 팀장 이상만 · 완료 = 아무도(⚠ S13 기본값)"
    - "진행의 담당 PM은 종료일을 오늘(KST) 이후로만 적을 수 있고, 이른 날짜는 칸 아래 `종료일은 오늘부터 적을 수 있습니다 · 앞당기려면 팀장 {이름}` + 화면 전체 거부다 — 서버도 같은 규칙으로 거부한다(⚠ S13 두 번째 기본값)"
    - "정산에서 팀장 이상이 종료일을 오늘 이후로 늦춰 저장하면 같은 트랜잭션에서 진행으로 돌아가고 `status_change` 로그가 그 사람 이름으로 한 줄 남는다 — 새 종료일이 지나면 다시 정산이 된다(D-80)"
    - "진행 이후 상태에서 종료일을 비우고 저장하면 시작일 값으로 저장되고, 진행 이후 상태에서 시작일을 비우는 저장은 `진행부터는 시작일이 있어야 합니다 · 시작일을 적어 주세요`로 거부된다(D-82)"
    - "기간 칸의 결과 미리보기 힌트 한 줄은 조건이 맞을 때만 보인다 — `저장하면 진행으로 돌아감`(정산 + 새 종료일 ≥ 오늘) · `저장하면 정산이 됨`(진행 + 새 종료일 < 오늘) · `종료일이 비어 시작일로 저장됨`(진행 이후 + 종료일 비움). 판정은 서버 저장과 같은 domain 함수다(SYSTEM.md 개정 ⑨)"
    - "기간 칸은 `Form.Actions`가 없는 칸 폼이다 — 고치면 dirty가 되어 화면 1차 「일괄 저장 Ctrl+S N」의 N에 칸 수만큼 합류하고 견적 줄·매출과 같은 트랜잭션으로 저장된다. Enter 기본 제출은 막히고 Ctrl+S가 저장이다(S13, §7-15)"
    - "기간 칸 오류로 저장이 거부되면 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`이 붙는다(U-6)"
    - "상태 바꾸기 확인 모달의 막힘 3차 「기간 적기」는 모달을 닫고 머리 줄 기간 칸을 열어 시작일에 포커스한다, 진행으로 바꾸기 본문은 종료일이 이미 지났으면 `종료일이 지나 바로 정산이 됩니다 · 늦추려면 기간 바꾸기`를 더한다(S7, Copywriting 확인 — 진행으로 바꾸기)"
    - "날짜는 식별자형 숫자라 쉼표가 없다(D-95 예외)"
    - statement: "기간 칸이 열려도 머리 줄 375px 가로 스크롤이 0이고 칸 묶음이 머리 줄 아래에 붙는다(머리 줄 안에 입력을 두지 않는다) — DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "시스템이 한 상태 전환을 사람의 이름으로 기록하거나, 사람이 한 전환을 시스템 이름으로 기록해서는 안 된다. 자동 정산은 누구의 판단도 아닌 날짜의 결과이고, 정산→진행 되돌리기는 그 날짜를 고친 사람의 판단이다 — 행동 로그가 이 둘을 섞으면 「누가 정산을 풀었는가」에 답하지 못한다"
      status: resolved
      verification: test
    - statement: "PM이 종료일 한 칸으로 제 손으로 되돌릴 수 없는 상태 전환(진행 → 정산)을 일으켜서는 안 된다. 상태 전환은 팀장의 일이라는 D-46의 취지를 기간 칸이 우회하는 경로가 되지 않게, 진행의 PM은 오늘보다 이른 종료일을 서버에서도 저장할 수 없다"
      status: resolved
      verification: test
  artifacts:
    - path: "lib/kst-date.ts"
      provides: "주입된 현재 시각에서 KST 날짜·연도를 계산하는 순수 함수(서버 시간대 비의존)"
      exports: ["kstToday", "kstYear", "kstDateOf", "addDays"]
    - path: "domain/projects/auto-transition.ts"
      provides: "진행→정산 자동 전환 — 조건부 UPDATE … RETURNING 한 번 + 시스템 행위자 로그(멱등)"
      exports: ["applyAutoSettlement"]
    - path: "domain/projects/period.ts"
      provides: "기간 편집 권리·검증·결과 미리보기·저장 해석의 순수 함수(화면 힌트와 서버 저장이 같은 함수)"
      exports: ["periodEditRights", "validatePeriodChange", "previewPeriodChange", "resolvePeriodSave"]
    - path: "domain/projects/responsibles.ts"
      provides: "담당 PM·팀장 이름 — 담당자 표기(SYSTEM.md 개정 ⑪)의 이름 출처"
      exports: ["projectResponsibles"]
    - path: "app/(app)/projects/[id]/period-field.tsx"
      provides: "머리 줄 아래 기간 칸 묶음 — Form.Actions 없음, dirty 합류, 미리보기 힌트"
      contains: "previewPeriodChange"
    - path: "test/integration/project-auto-settlement.test.ts"
      provides: "KST 경계 · 멱등 · 동시 판정 · 시스템 행위자 · 쓰기 경로 선판정 증명"
      min_lines: 50
  key_links:
    - from: "domain/projects/index.ts"
      to: "domain/projects/auto-transition.ts"
      via: "findProject·listProjects가 반환 직전에 자동 전환을 적용한다"
      pattern: "applyAutoSettlement"
    - from: "domain/projects/ledger.ts"
      to: "domain/projects/period.ts"
      via: "기간 저장이 화면 미리보기와 같은 해석 함수로 종료일 기본값·정산→진행을 정한다"
      pattern: "resolvePeriodSave"
    - from: "app/(app)/projects/[id]/period-field.tsx"
      to: "domain/projects/period.ts"
      via: "힌트 한 줄을 서버 저장과 같은 함수로 만든다(개정 ⑨)"
      pattern: "previewPeriodChange"
    - from: "domain/projects/auto-transition.ts"
      to: "domain/action-log/record.ts"
      via: "SYSTEM_VIEWER로 기록해 행위자 id가 null이 된다"
      pattern: "SYSTEM_VIEWER"
---

<objective>
**As a** 기획 PM·팀장, **I want to** 행사 종료일 다음 날 프로젝트가 저절로 정산 단계로 넘어가고, 행사가 늘어나면 팀장이 종료일을 늦춰 진행으로 되돌리며, 종료일이 지났는데 아직 수주중인 건이 눈에 띄기를, **so that** 「끝났는데 안 닫힌 건」을 사람이 기억해서 옮기지 않아도 되고, 정산 규칙(실행가만 수정)이 날짜에 맞춰 정확히 걸린다.

이 플랜은 04-06 재계획에서 나눈 **날짜로 움직이는 절반**이다(D-76 · D-80 · D-81 · D-82의 저장 규칙 · UI-SPEC S13). 상태 모델과 사람의 전환은 04-06이 만들었다.

**D-76 실행 방식(계획 재량 — 결정):** 읽기 시점 판정. `domain/projects/auto-transition.ts`의 `applyAutoSettlement`가 `UPDATE … WHERE status = 진행 AND end_date < {오늘 KST} … RETURNING`을 한 번 실행하고, 돌아온 행마다 시스템 행위자로 로그를 남긴다. WHERE 절 자체가 멱등성을 보장한다(두 번째 호출은 0행). 호출 지점은 상세 조회 · 목록 조회 · 그 프로젝트의 저장·상태 전환 직전이다. Cloud Scheduler 예약 작업은 Phase 7이 알림 tick과 함께 **같은 함수를 부르는 얇은 배선**으로 얹는다(RESEARCH §2, A8) — 예약 작업 인프라(OIDC·IAM·`/internal/` 인증)를 이 페이즈에서 절반만 짓지 않는다.

Purpose: 04-12(정산의 실행가 한정 편집)가 걸리려면 프로젝트가 제때 정산이 되어야 하고, 04-17·04-18(목록)이 종료일 지남·정산 태그를 보이려면 목록 읽기도 같은 판정을 거쳐야 한다.

Output: KST 날짜 모듈 · 자동 전환 함수 · 기간 칸(S13) · 정산→진행 되돌리기 · 종료일 지남 표시 · 담당자 이름 출처 · 단위 3 · 통합 2 · E2E 1(+1 확장).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 시간은 `vi.useFakeTimers`로 얼리지 않고 `deps.now`로 주입한다(리포 관례, PATTERNS). 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 723–751행(§2 D-76 — 스케줄링 인프라 부재 · 구현 패턴 · 호출 지점 · 시간 주입 · KST 경계 · 서버 시간대 Gap) · 857–861행(Pitfall 7)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 436–451행(auto-transition 배정) · 484–490행(시스템 행위자·시간 주입)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 525–533행(S13 상태 + ⚠ 둘) · 1344–1364행(S13 적용 규칙) · 1057–1069행(S3 부제·종료일 지남) · 340행·352행·359행·374행(Copywriting 기간 칸 오류·종료일 지남·미리보기·진행으로 바꾸기)
# docs/design/SYSTEM.md §7-15(Form.Hint 두 번째 쓰임 — 04-08 개정 ⑨) · §8 규칙 3 아래 담당자 표기(개정 ⑪)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(날짜로 움직이는 부분)** 다. 프로브는 옛 네 상태 문구로 돌았으므로 수용 기준은 D-76·D-80·D-81·D-82와 UI-SPEC S3·S7·S13에서 끌어와 `must_haves.truths`에 적었다. S13의 다섯 상태 중 empty·loading·long-text(해당 없음)는 truths의 「칸은 항상 둘 · 서버 렌더 값」으로, error·partial은 truths로, S3 overflow는 기간 칸이 열린 상태의 backstop으로 옮겼다.

**계획 단계 가정(사용자에게 표면화):**
- **A8 — 읽기 시점 판정:** 사용자가 「아무도 화면을 열지 않아도 자정에 정확히 바뀌어야 한다」(예: 자정 기준 보고서)를 요구하면 이 가정은 틀리고 예약 작업을 앞당겨야 한다. 지금 D-76 원문은 「실행 방식은 계획이 정하되 멱등」이고, 화면을 여는 순간·저장 직전에 항상 판정하므로 사람이 보는 상태는 언제나 맞다. 상태 날짜는 로그의 **발효일**(종료일 다음 날)로 보여 판정이 늦게 돌아도 기록상의 날짜는 D-76 그대로다.
- **⚠ S13 기간 편집 주체 — 기본값 적용:** 수주중·진행·미수주 = 담당 PM + 팀장 이상, 정산 = 팀장 이상, 완료 = 아무도.
- **⚠ S13 진행 중 PM의 종료일 앞당기기 — 기본값 적용:** 진행의 담당 PM은 종료일을 오늘 이후로만. 이른 날짜는 팀장 이상.
- **「팀장 {이름}」의 출처:** 조직 표에 팀장 칸이 없다. 이 플랜은 「프로젝트 팀에 오늘(KST) 소속된 사람 중 `projects.status` 쓰기 권한이 있는 계급의 사람, 여럿이면 이름순 첫 사람」으로 정한다(계급 이름을 코드에 박지 않는다 — 04-02 선례). 없으면 이름 없는 문구로 떨어진다(Copywriting의 「팀장이 없으면」 변형).
- **서버 시간대 Gap:** 이 플랜은 서버 시간대에 기대지 않도록 짰고, 실행 중 로컬 dev DB와 test DB의 `SHOW timezone` 값을 SUMMARY에 적어 리서치 Gap을 닫는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 종료일이 지난 진행 프로젝트를 열면 정산으로 보인다(KST 경계·멱등·시스템 행위자)</name>
  <files>lib/kst-date.ts, test/unit/lib/kst-date.test.ts, domain/projects/status-transitions.ts, domain/projects/auto-transition.ts, repositories/projects.ts, domain/projects/index.ts, domain/projects/status.ts, test/unit/domain/auto-transition.test.ts, test/integration/project-auto-settlement.test.ts, test/e2e/project-period.spec.ts</files>
  <reversibility rating="reversible">읽기 시점 판정은 `applyAutoSettlement` 한 함수 뒤에 있고, Phase 7이 예약 작업에서 같은 함수를 부르면 호출자만 늘어난다 — 저장 형태·데이터 계약이 바뀌지 않는다.</reversibility>
  <read_first>
    - `domain/viewer.ts`(전문) — `SYSTEM_VIEWER`의 정의와 용도
    - `domain/action-log/record.ts` 110–145행 — 시스템 주체의 `actorId`가 null이 되는 자리
    - `domain/projects/status-transitions.ts`·`domain/projects/status.ts`(04-06 산출물 전문) — 전이표·`lastStatusChangeOn`
    - `domain/projects/index.ts` 160–230행(`listProjects`·`findProject`)
    - `repositories/projects.ts` 211–302행(단건 조회·조건부 갱신 — 04-06의 `updateProjectStatusIfCurrent`)
    - `domain/quotes/lines.ts` 440–455행(`toLocaleTimeString(…, { timeZone: "Asia/Seoul" })` — 앱 층 KST 선례)
    - `domain/settings/registry.ts` 60–95행(`asOf` 시간 인자 선례) · `domain/quotes/lines.ts`의 `deps` 인자 형태
    - `test/integration/project-status.test.ts`(04-06 산출물) — 프로젝트를 만들고 상태를 DB에 직접 두는 준비 형태
  </read_first>
  <behavior>
    - `kstToday(new Date("2026-09-17T14:59:59Z"))` → `2026-09-17` · `kstToday(new Date("2026-09-17T15:00:00Z"))` → `2026-09-18`
    - `kstDateOf(new Date("2026-12-31T15:30:00Z"))` → `2027-01-01` · `kstYear(new Date("2026-12-31T15:00:00Z"))` → `2027`
    - `addDays("2026-09-17", 1)` → `2026-09-18` · `addDays("2026-12-31", 1)` → `2027-01-01`
    - `applyAutoSettlement({ projectIds: [p] }, { now: () => 2026-09-17T14:59:59Z })` + 종료일 9/17 진행 → 바뀐 것 0 · 같은 입력에 `now` = 15:00:00Z → [p], 상태 정산, 로그 1줄(actor null, detail.effectiveOn `2026-09-18`)
    - 같은 호출을 두 번 → 두 번째는 [] · 로그는 여전히 1줄
    - 종료일이 없는 진행(있을 수 없지만 방어) · 수주중 · 미수주 · 정산 · 완료 → 바뀌지 않는다
  </behavior>
  <action>
① `lib/kst-date.ts`(순수, import 없음): `kstToday(now: Date): string`(`Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Seoul", year, month, day })` → `YYYY-MM-DD`) · `kstDateOf(date: Date): string` · `kstYear(now: Date): number` · `addDays(ymd: string, n: number): string`(UTC 자정 산술로 달력 날짜만 더한다). 단위 테스트 `behavior` 앞 세 줄(RED → GREEN).

② `domain/projects/status-transitions.ts`에 자동 전환 한 줄 `AUTO_TRANSITIONS = [{ from: "in_progress", to: "settling", trigger: "end_date_passed" }] as const`를 더한다 — 자동 전환이 쓰는 상태 값도 이 데이터 모듈 한 곳에서 온다.

③ `repositories/projects.ts`에 `settleOverdueProjects(viewer, { todayKst, projectIds?, from, to }, tx?)`: `UPDATE projects SET status = $to, version = version + 1, updated_at = now() WHERE status = $from AND end_date IS NOT NULL AND end_date < $todayKst::date [AND id = ANY($projectIds)] AND archived_at IS NULL RETURNING id, end_date`. **날짜 비교에 DB의 현재 날짜 함수나 서버 시간대를 쓰지 않는다** — 오늘은 인자로만 들어온다(Pitfall 7).

④ `domain/projects/auto-transition.ts`: `applyAutoSettlement(opts: { projectIds?: string[] }, deps?: { now?: () => Date; recordAction?; settle? }): Promise<string[]>` — `kstToday(now())` → 리포지토리 한 번 → 돌아온 행마다 `recordAction(SYSTEM_VIEWER, { actionType: "status_change", entity: "project", entityId, detail: { from, to, trigger: "end_date_passed", effectiveOn: addDays(end_date, 1) } })`. 로그 실패는 삼키지 않는다(기존 `recordAction` 규약). 단위 테스트는 `deps.settle`·`deps.recordAction`을 스텁으로 넣어 `behavior` 뒤 세 줄을 DB 없이 증명한다.

⑤ **호출 지점(이 태스크는 상세 하나):** `domain/projects/index.ts`의 `findProject`가 행을 읽기 전에 `applyAutoSettlement({ projectIds: [id] })`를 부른다. `domain/projects/status.ts`의 `lastStatusChangeOn`이 최신 로그 detail에 `effectiveOn`이 있으면 그 날짜를 쓴다(부제 `정산 2026-09-18`).

⑥ **서버 시간대 Gap 닫기:** 로컬 dev DB와 test DB에서 `SHOW timezone` 결과를 확인해 SUMMARY에 적는다.

⑦ `test/integration/project-auto-settlement.test.ts`: (a) KST 경계 전후 두 `now`로 실제 DB의 상태 변화 유무, (b) 두 번 호출 · `Promise.all` 동시 두 호출 모두 로그 1줄(DB 재조회), (c) 로그의 `actor_id` null과 detail(from·to·trigger·effectiveOn), (d) 다른 상태 넷은 바뀌지 않는다, (e) `findProject`가 돌려준 DTO의 상태가 정산이다. `test/e2e/project-period.spec.ts`를 만들고 첫 케이스: 테스트 준비에서 종료일이 어제(KST)인 진행 프로젝트를 DB에 두고 → 기획 PM으로 상세를 열면 상태 태그가 `정산`(warning)이고 부제가 `정산 {종료일 다음 날}`이다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/lib/kst-date.test.ts test/unit/domain/auto-transition.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — KST 경계나 멱등 판정이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-auto-settlement.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 실제 DB에서 경계·멱등·행위자 중 하나가 틀렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error가 나오거나 playwright failed 개수가 1 이상이다 — 상세를 열어도 정산으로 보이지 않는다</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/kst-date.ts`에 import가 없고 단위 테스트가 경계 두 줄(14:59:59Z / 15:00:00Z)과 연말 넘김을 단언한다
    - `settleOverdueProjects`의 날짜 비교가 인자로 받은 날짜 하나만 쓴다(DB 현재 날짜 함수 호출이 그 함수 본문에 없다 — 코드 리뷰 확인 + 통합 테스트 (a))
    - `applyAutoSettlement`가 `SYSTEM_VIEWER`로 기록하고 통합 테스트가 `actor_id` null을 단언한다
    - 두 번·동시 호출 뒤 로그가 1줄임을 DB 재조회로 단언한다
    - `findProject` 경로에서 자동 전환이 먼저 돈다(통합 테스트 e)
    - dev·test DB의 `SHOW timezone` 값이 SUMMARY에 있다
    - E2E 첫 케이스가 정산 태그와 발효일 부제를 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>종료일이 지난 진행 프로젝트가 상세를 여는 순간 KST 기준으로 정산이 되고, 멱등·시스템 행위자·발효일 표시가 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 상세 기간 칸(S13) — 편집 권리 · 검증 · 미리보기 · 정산→진행 되돌리기(D-80) · 종료일 기본값(D-82)</name>
  <files>domain/projects/period.ts, domain/projects/responsibles.ts, domain/rules/register.ts, domain/projects/ledger.ts, repositories/projects.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/period-field.tsx, app/(app)/projects/[id]/project-detail.module.css, test/unit/domain/project-period.test.ts, test/integration/project-period.test.ts, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1344–1364행(S13 전문 — 자리 · 칸 폼 · dirty 합류 · Enter/Ctrl+S/Esc · 미리보기 세 줄 · 오류 · 저장 성공 · 기간 적기 진입 · 쉼표 예외) · 525–533행(⚠ 두 기본값) · 340행(오류 문구 넷) · 359행(미리보기 문구 셋) · 334행(U-6 문구)
    - `docs/design/SYSTEM.md` §7-15(`Form.Actions` 없는 칸 폼 · `Form.Hint` 미리보기 — 04-08 개정 ⑨)
    - `ui/form/Form.tsx`(전문) — `Form.Field`·`Form.Hint`·`Form.Error` 마크업
    - `domain/projects/ledger.ts`(전문 40행) — 한 트랜잭션 합성 지점(옵션 tx)
    - `app/(app)/projects/actions.ts` 50–115행(`saveProjectLedgerAction` 스키마)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `dirtyCount`·`handleSave`·머리 줄(`styles.header`)·`rejectionSummary`를 찾아 그 범위만 Read(1000행 넘는 파일 — 전문 Read 금지)
    - `domain/org/index.ts`의 `teamAtDate()`와 `repositories/team-memberships.ts` — 오늘 소속 판정
    - `domain/permissions/can.ts`·`repositories/permissions.ts` — 계급별 메뉴 권한 조회(팀장 이름 찾기에 쓴다)
    - 04-06의 `domain/projects/status.ts`·`repositories/projects.ts`(`updateProjectStatusIfCurrent`)
  </read_first>
  <behavior>
    - `periodEditRights`: 수주중·진행·미수주 + 담당 PM → `pm` · + `projects.status` 권한 → `lead` · 정산 + 담당 PM(권한 없음) → `none` · 정산 + 권한 → `lead` · 완료 → 누구든 `none` · 담당이 아닌 PM → `none`
    - `validatePeriodChange`: 날짜 형식 틀림 → `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` · 종료 < 시작 → `종료일이 시작일보다 빠릅니다 · 종료일을 고쳐 주세요` · 진행 이후 + 시작일 비움 → `진행부터는 시작일이 있어야 합니다 · 시작일을 적어 주세요` · 진행 + 권리 `pm` + 새 종료 < 오늘 → `종료일은 오늘부터 적을 수 있습니다 · 앞당기려면 팀장 {이름}`(이름 없으면 `종료일은 오늘부터 적을 수 있습니다`)
    - `previewPeriodChange`: 정산 + 새 종료 ≥ 오늘 → `저장하면 진행으로 돌아감` · 진행 + 새 종료 < 오늘 → `저장하면 정산이 됨` · 진행 이후 + 종료 비움 → `종료일이 비어 시작일로 저장됨` · 그 밖 → null
    - `resolvePeriodSave`: 진행 이후 + 종료 비움 → 종료 = 시작 · 정산 + 새 종료 ≥ 오늘 → `statusChange: { from: 정산, to: 진행 }` · 수주중 + 종료 비움 → 그대로 비움
  </behavior>
  <action>
① `domain/projects/period.ts`(순수 — `lib/kst-date`와 전이표만 import, 리포지토리 import 없음 — 화면 클라이언트 컴포넌트도 이 파일을 import한다): `periodEditRights({ status, isAssignedPm, canChangeStatus })` → `"lead" | "pm" | "none"`(⚠ S13 기본값 표) · `validatePeriodChange({ status, rights, start, end, todayKst, teamLeadName })` → 칸별 오류 `{ field: "start" | "end"; reason }[]` · `previewPeriodChange({ status, newStart, newEnd, todayKst })` → 힌트 문자열 또는 null · `resolvePeriodSave({ status, newStart, newEnd, todayKst })` → `{ startDate, endDate, statusChange? }`. 결정표 단위 테스트(`behavior`)를 먼저 쓴다(RED).

② `domain/projects/responsibles.ts`: `projectResponsibles(viewer, project, deps?)` → `{ pmName, teamLeadName | null }` — 팀장 = 프로젝트 팀에 오늘(KST, `lib/kst-date`) 소속된 사람 중 계급이 `projects.status` 쓰기를 가진 사람, 여럿이면 이름순 첫 사람(위 `<probe_fallback>` 가정). 계급 이름·id를 코드에 박지 않는다.

③ `domain/rules/register.ts`에 `project.period-edit`(ctx `{ rights, errors }`: 권리가 `none`이면 `기간을 고칠 권한이 없습니다`, 검증 오류가 있으면 첫 오류 이유)을 등록한다.

④ `domain/projects/ledger.ts`: `SaveProjectLedgerInput`에 `period?: { startDate: string | null; endDate: string | null; version: number }`를 더하고, 같은 트랜잭션 안에서 (a) `applyAutoSettlement({ projectIds })`가 이미 돈 상태를 읽고(Task 3이 선판정을 모든 쓰기 경로에 건다 — 이 태스크에서는 ledger 한 곳) (b) 권리·검증을 계산해 `gate(project, "project.period-edit", …)` (c) 거부면 `SaveRejectedError`와 같은 구조로 **칸 오류를 담아 전체 거부**(표 쪽은 저장하지 않는다) (d) 통과면 `resolvePeriodSave` 결과로 `repositories/projects.ts`의 `updateProjectPeriodIfVersion(viewer, id, { startDate, endDate, version }, tx)` — 버전이 다르면 충돌 거부 — 와, `statusChange`가 있으면 04-06의 `updateProjectStatusIfCurrent`(정산→진행) + `recordAction(viewer, status_change, detail { from, to, trigger: "end_date_extended" })`(**사람 행위자**, D-80). 커밋 뒤 `applyAutoSettlement({ projectIds })`를 한 번 더 불러 진행 + 지난 종료일을 곧바로 정산으로 만든다(「다음 판정 때 바로」). `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마에 `period`를 더한다(날짜는 `YYYY-MM-DD` 문자열 또는 null).

⑤ **화면.** `app/(app)/projects/[id]/period-field.tsx`(클라이언트): 머리 줄 **아래**에 열리는 `Form` 한 묶음 — `Form.Field 시작일`(280) · `Form.Field 종료일`(280), 자리표시자 `2026-09-18`, **`Form.Actions` 없음**. 고치면 칸이 dirty(`--inset-dirty`)가 되어 `QuoteLedger`의 dirty 수에 칸 수만큼 합류하고 1차 「일괄 저장」이 같은 액션 호출에 `period`를 싣는다. Enter 기본 제출을 막고, 칸 안에서 Ctrl+S는 1차 저장과 같은 `handleSave`를 부른다. Esc는 편집 중 값을 되돌리고 두 칸이 원래 값이면 묶음을 닫는다. 힌트 한 줄은 `previewPeriodChange` 결과가 있을 때만(`Form.Hint`). 서버 칸 오류는 `Form.Error`(`aria-invalid`)로 칸 아래에, 그때 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`. 저장 성공 시 600ms `--accent-weak` 틴트(`prefers-reduced-motion`이면 없음) 뒤 묶음이 닫히고 부제·태그가 새 값(토스트 없음). `[id]/page.tsx`가 권리(`periodEditRights`)·팀장 이름·오늘(KST)을 계산해 넘기고, 부제에 `기간 …` + (권리가 있을 때만) 3차 「기간 바꾸기」를 둔다 — 칸이 열린 동안 부제의 기간 글자와 버튼은 숨긴다. `quote-table.tsx`에는 기간 칸의 dirty 합류와 `openPeriodField(focus: "start" | "end")` 한 함수만 더한다(Task 3과 04-12가 이 함수를 부른다).

⑥ 테스트: `test/integration/project-period.test.ts` — (a) 팀장이 정산 프로젝트의 종료일을 오늘 이후로 늦추면 진행 + 로그 1줄(행위자 = 그 팀장), (b) 진행의 담당 PM이 어제로 앞당기면 거부되고 DB의 기간·상태 무변경, (c) 팀장이 진행의 종료일을 어제로 앞당기면 저장 직후 정산 + 시스템 로그 1줄, (d) 진행 이후 종료일 비움 → 시작일로 저장, (e) 기간 오류와 견적 줄 변경이 같은 저장에 있으면 견적 줄도 저장되지 않는다, (f) 완료 프로젝트의 기간 저장은 누구든 거부. `project-period.spec.ts` 케이스 셋: PM의 이른 종료일 오류 문구와 표 합계 행 U-6 문구 · 팀장의 `저장하면 정산이 됨` 힌트 → 저장 → 정산 태그 · 팀장의 정산 연장 `저장하면 진행으로 돌아감` → 진행 태그.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-period.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 권리·검증·미리보기·저장 해석 결정표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-period.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 저장이 한 트랜잭션 규칙을 깨거나 기존 합성 저장이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/projects/period.ts`가 리포지토리·DB를 import하지 않고, 화면 컴포넌트와 `ledger.ts`가 같은 함수를 import한다
    - 결정표 단위 테스트가 `behavior` 네 묶음을 전부 덮는다
    - `project.period-edit` 규칙이 등록되고 기간 저장이 그 게이트를 지난다
    - 정산→진행 되돌리기 로그의 행위자가 사람이고, 자동 정산 로그의 행위자는 시스템이다(통합 테스트 a·c가 둘을 구분해 단언)
    - 기간 오류가 있는 저장에서 견적 줄·매출도 저장되지 않는다(통합 테스트 e, DB 재조회)
    - 기간 칸에 `Form.Actions`가 없고 저장은 화면 1차 버튼 하나다
    - 힌트 문자열이 화면 코드 리터럴이 아니라 `previewPeriodChange` 반환값이다
    - E2E 세 케이스가 초록이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상세에서 기간을 권리대로 고치고, 결과 미리보기가 서버 저장과 같은 판정으로 보이며, 정산 연장·앞당김·종료일 기본값이 한 트랜잭션 저장으로 동작한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 종료일 지남(D-81) + 모든 읽기·쓰기 경로의 선판정 + 상태 모달 연결 + 화면 검증</name>
  <files>domain/projects/index.ts, domain/projects/status.ts, domain/projects/ledger.ts, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, test/integration/project-auto-settlement.test.ts, test/e2e/project-period.spec.ts, test/e2e/project-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1067–1069행(S3 종료일 지남 — 상태 바꾸기가 있는 사람에게는 글자만) · 374행(확인 — 진행으로 바꾸기의 종료일 지남 줄) · 1244–1247행(막힘 3차 「기간 적기」)
    - `app/(app)/projects/[id]/status-change.tsx`(04-06 산출물 전문)
    - `domain/projects/index.ts`의 `listProjects`(04-05·04-06 산출물)
    - `domain/projects/status.ts`의 `changeProjectStatus`(04-06 산출물)
  </read_first>
  <behavior>
    - 수주중 + 종료일 < 오늘(KST) → 상세에 `종료일 지남` · 상태를 바꿀 수 없는 사람(담당 PM)에게는 `종료일 지남 · 팀장 {이름}`
    - 수주중 + 종료일 = 오늘 → 표시 없음 · 진행·정산·완료·미수주 → 표시 없음
    - 목록을 읽으면(`listProjects`) 지난 진행 프로젝트가 전부 정산으로 바뀌어 돌아온다
    - 자정을 넘겨 연 화면에서 진행 프로젝트의 줄을 저장하면 저장 판정 전에 정산이 먼저 적용된다(통합 테스트에서 `now` 주입)
    - 상태 전환 요청도 판정 전에 자동 전환을 먼저 적용한다
  </behavior>
  <action>
① **모든 경로 선판정.** `listProjects`가 조회 전에 `applyAutoSettlement({})`(프로젝트 목록 제한 없음 — 한 번의 UPDATE)를 부른다. `changeProjectStatus`와 `saveProjectLedger`(Task 2의 ledger 경로 — 기간이 없는 저장 포함)가 게이트 판정 전에 `applyAutoSettlement({ projectIds: [id] })`를 부른다. 한 요청에서 두 번 부르는 것은 무해하다(멱등) — 호출을 줄이려고 캐시를 만들지 않는다.

② **종료일 지남(D-81).** 판정 함수 `isEndDatePassed({ status, endDate, todayKst })`를 **`domain/projects/status.ts`에** 둔다(04-18 목록이 같은 함수를 쓴다 — 기간 편집 모듈과 섞지 않는다) — 수주중이고 종료일이 있고 종료일 < 오늘이면 참. `[id]/page.tsx`가 계산해 넘기고 `quote-table.tsx` 머리 줄의 상태 태그 오른쪽에 `--fs-sm --warning` 글자 `종료일 지남`, 「상태 바꾸기」가 없는 사람에게는 `종료일 지남 · 팀장 {이름}`(없으면 글자만). 태그가 아니라 글자다(UI-SPEC Color 표).

③ **상태 모달 연결.** 04-06은 `status-change.tsx`를 페이지가 만든 ReactNode로 넘겼다. 이 태스크에서 `QuoteLedger`가 그 컴포넌트를 props로 받아 **안에서** 렌더하도록 바꿔(`statusControl` → `statusChangeProps`), 막힘 줄의 3차 「기간 적기」가 모달을 닫고 `openPeriodField("start")`를 부르게 한다(기간 칸을 고칠 권리가 없으면 3차 없이 게이트 문자열만). 진행으로 바꾸기 확인 모달 본문에 종료일이 이미 지났을 때의 줄 `종료일이 지나 바로 정산이 됩니다 · 늦추려면 기간 바꾸기`를 더한다(Copywriting).

④ 테스트: `project-auto-settlement.test.ts`에 (f) `listProjects`가 지난 진행을 정산으로 돌려준다, (g) `now`를 자정 뒤로 주입한 `saveProjectLedger`의 판정이 정산 기준이다(04-12 전이라 정산도 이 시점엔 통과하므로, 상태 값이 정산으로 바뀐 것과 로그 1줄을 단언한다), (h) `changeProjectStatus` 직전 선판정. `project-period.spec.ts`에 종료일 지남 두 변형(팀장 = 글자만 · PM = `· 팀장 {이름}`)과 「기간 적기」가 시작일 칸에 포커스하는 케이스, `project-lifecycle.spec.ts`의 진행 확인 모달에 종료일 지남 줄 케이스.

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S3 overflow backstop(기간 칸이 열린 375 머리 줄 가로 스크롤 0 · 칸 묶음이 머리 줄 아래)을 DOM 실측 → 수정 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-auto-settlement.test.ts test/integration/project-status.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 목록·저장·전환 경로 중 하나가 선판정을 건너뛴다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S3 overflow 판정(기간 칸이 열린 375 머리 줄)을 SUMMARY에서 확인</test>
      <expected>가로 스크롤 0, 칸 묶음이 머리 줄 아래, 버튼이 숨지 않음</expected>
      <why_human>폭·배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `listProjects`·`findProject`·`changeProjectStatus`·`saveProjectLedger` 네 경로가 `applyAutoSettlement`를 부른다(통합 테스트 e·f·g·h)
    - `isEndDatePassed`가 `domain/projects/status.ts`에 있고 종료일 = 오늘은 거짓이다(단위 또는 통합 단언)
    - 종료일 지남이 태그가 아니라 글자이고, 담당 PM 변형에 팀장 이름이 붙는다(E2E)
    - 「기간 적기」가 모달을 닫고 시작일 칸에 포커스한다(E2E)
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>어느 경로로 프로젝트를 보든 저장하든 먼저 날짜 판정이 돌고, 종료일이 지난 수주중이 상세에서 눈에 띄며, 상태 모달이 기간 칸으로 이어진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(기간) | 시작일·종료일이 넘어온다. 권리·검증·정산 되돌리기 판정이 서버에만 있어야 한다 |
| 시스템 주체 → 상태 | 읽기 요청이 쓰기(자동 전환)를 일으킨다. 요청한 사람의 권한으로 쓰지 않는다 |
| DB 시간대 → 날짜 경계 | 서버 설정이 경계를 바꾸면 전환 시각이 틀어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-77 | Elevation of Privilege | 기간 칸으로 상태 바꾸기 | high | mitigate | 정산→진행(D-80)은 `project.period-edit` 게이트가 권리 `lead`(팀장 이상)일 때만 통과하고, 진행의 PM은 오늘보다 이른 종료일을 서버에서 저장할 수 없다(⚠ S13). 두 거부를 통합 테스트(a·b)로 고정한다 |
| T-04-78 | Spoofing | 자동 전환 행위자 | medium | mitigate | 자동 전환은 요청한 viewer가 아니라 `SYSTEM_VIEWER`로 기록되어 actor가 null이다. 사람의 되돌리기는 그 viewer로 기록된다 — 통합 테스트가 둘을 구분한다 |
| T-04-79 | Tampering | 날짜 경계 | medium | mitigate | 오늘(KST)은 앱이 주입된 시각에서 계산해 쿼리 인자로 넘긴다. DB 현재 날짜·서버 시간대를 쓰지 않는다(Pitfall 7). 경계 전후 두 시각을 통합 테스트로 고정한다 |
| T-04-80 | Tampering | 기간 동시 편집 | medium | mitigate | 기간 갱신이 프로젝트 `version` 조건부라 다른 사람의 사이 편집을 덮지 않는다 |
| T-04-81 | Denial of Service | 목록 읽기의 UPDATE | low | accept | 목록 읽기마다 조건부 UPDATE 한 번이 돈다. 대상이 없으면 0행이고 인덱스(`projects_status_idx`·`projects_end_date_idx`)를 탄다. 10~30명 규모에서 비용이 낮다 |
| T-04-82 | Repudiation | 상태 날짜 | low | mitigate | 자동 전환 로그 detail에 발효일을 남겨, 판정이 늦게 돌아도 기록상 전환일이 D-76과 같다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- dev·test DB `SHOW timezone` 값이 SUMMARY에 기록됨
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 진행 프로젝트가 종료일 다음 날(KST)부터 정산으로 보이고, 몇 번을 판정해도 로그는 한 줄이다
- 팀장이 종료일을 늦춰 정산을 진행으로 되돌리고, PM은 종료일로 상태를 바꿀 수 없다
- 종료일이 지난 수주중이 상세에서 눈에 띄고 다음 한 수(상태 바꾸기 또는 담당 팀장)가 보인다
- Phase 7은 예약 작업에서 `applyAutoSettlement`를 부르기만 하면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `lib/kst-date.ts` — `kstToday` · `kstDateOf` · `kstYear` · `addDays` |
| 함수(신규, domain) | `applyAutoSettlement`(`domain/projects/auto-transition.ts`) · `periodEditRights` · `validatePeriodChange` · `previewPeriodChange` · `resolvePeriodSave`(`domain/projects/period.ts`) · `projectResponsibles`(`domain/projects/responsibles.ts`) · `isEndDatePassed`(`domain/projects/status.ts`) |
| 함수(신규, repository) | `settleOverdueProjects` · `updateProjectPeriodIfVersion`(`repositories/projects.ts`) |
| 상수 | `AUTO_TRANSITIONS`(`status-transitions.ts`) |
| 게이트 규칙 | `project.period-edit` |
| 로그 detail 규약 | `status_change` detail `{ from, to, trigger: "end_date_passed" \| "end_date_extended", effectiveOn? }` |
| 입력 계약 | `SaveProjectLedgerInput.period` · `saveProjectLedgerAction` 스키마 `period` |
| 컴포넌트 | `app/(app)/projects/[id]/period-field.tsx`(신규) · `QuoteLedger.openPeriodField` · `statusChangeProps` |
| 테스트 | `test/unit/lib/kst-date.test.ts` · `test/unit/domain/auto-transition.test.ts` · `test/unit/domain/project-period.test.ts` · `test/integration/project-auto-settlement.test.ts` · `test/integration/project-period.test.ts` · `test/e2e/project-period.spec.ts`(전부 신규) · `project-lifecycle.spec.ts`(확장) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md` when done
</output>
