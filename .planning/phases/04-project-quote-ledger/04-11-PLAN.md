---
phase: 04-project-quote-ledger
plan: 11
type: execute
wave: 9
depends_on: ["04-21"]
files_modified:
  - lib/kst-date.ts
  - test/unit/lib/kst-date.test.ts
  - domain/projects/status-transitions.ts
  - domain/projects/auto-transition.ts
  - domain/projects/status.ts
  - domain/projects/index.ts
  - domain/projects/responsibles.ts
  - repositories/projects.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/auto-transition.test.ts
  - test/integration/project-auto-settlement.test.ts
  - test/e2e/project-period.spec.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "진행 프로젝트는 종료일 다음 날 00:00(KST)부터 정산이다 — 종료일 2026-09-17이면 2026-09-17T14:59:59Z(KST 23:59:59)에는 진행, 2026-09-17T15:00:00Z(KST 9/18 00:00)부터 정산이다(D-76)"
    - "KST 날짜 경계는 애플리케이션이 주입된 현재 시각에서 계산해 쿼리에 값으로 넘긴다 — DB 서버의 시간대 설정과 무관하게 같은 결과다(RESEARCH Pitfall 7, 서버 시간대 Gap)"
    - "자동 전환은 멱등이다 — 같은 날 몇 번을 판정해도(동시에 두 요청이 와도) 상태 변경은 한 번이고 행동 로그 `status_change`는 한 줄이다(D-76)"
    - "자동 전환의 행동 로그 행위자는 시스템이다 — 로그의 사람 id가 비어 있고 detail에 발효일(종료일 다음 날)이 적힌다(D-76, SYSTEM_VIEWER — 사람의 되돌리기와 구분하는 금지 항목은 04-22가 함께 증명한다)"
    - "상세를 열거나 목록을 열거나 그 프로젝트의 상태 전환을 시도하면, 판정 전에 자동 전환이 먼저 적용된다 — 자정을 넘겨 연 화면에서 상태를 바꿔도 정산 기준으로 판정된다(D-76 읽기 시점 판정 — 합성 저장 경로의 선판정은 저장을 넓히는 04-22가 같은 함수로 건다)"
    - "상세 머리 줄 부제의 상태 날짜는 자동 정산이면 발효일(종료일 다음 날)이다 — 실제로 판정이 돈 날이 아니다(D-50·D-76, UI-SPEC S3 `정산 2026-09-17`)"
    - "종료일이 지난 수주중은 자동으로 바뀌지 않고 상세 상태 태그 오른쪽에 `종료일 지남`(`--fs-sm --warning` 글자)이 보이며, 상태를 바꿀 수 없는 사람에게는 `종료일 지남 · 팀장 {이름}`(팀장이 없으면 글자만)이다(D-81, Copywriting 표시 — 종료일 지남)"
    - "「팀장 {이름}」은 프로젝트 팀에 오늘(KST) 소속된 사람 중 `projects.status` 쓰기 계급의 사람이고 여럿이면 이름순 첫 사람이다 — 계급 이름·id를 코드에 박지 않는다(SYSTEM.md 개정 ⑪ 담당자 표기의 이름 출처)"
  artifacts:
    - path: "lib/kst-date.ts"
      provides: "주입된 현재 시각에서 KST 날짜·연도를 계산하는 순수 함수(서버 시간대 비의존)"
      exports: ["kstToday", "kstYear", "kstDateOf", "addDays"]
    - path: "domain/projects/auto-transition.ts"
      provides: "진행→정산 자동 전환 — 조건부 UPDATE … RETURNING 한 번 + 시스템 행위자 로그(멱등)"
      exports: ["applyAutoSettlement"]
    - path: "domain/projects/responsibles.ts"
      provides: "담당 PM·팀장 이름 — 담당자 표기(SYSTEM.md 개정 ⑪)의 이름 출처"
      exports: ["projectResponsibles"]
    - path: "test/integration/project-auto-settlement.test.ts"
      provides: "KST 경계 · 멱등 · 동시 판정 · 시스템 행위자 · 읽기·쓰기 경로 선판정 증명"
      min_lines: 50
  key_links:
    - from: "domain/projects/index.ts"
      to: "domain/projects/auto-transition.ts"
      via: "findProject·listProjects가 반환 직전에 자동 전환을 적용한다"
      pattern: "applyAutoSettlement"
    - from: "domain/projects/status.ts"
      to: "domain/projects/auto-transition.ts"
      via: "changeProjectStatus가 게이트 판정 전에 자동 전환을 적용한다(04-20 함수에 선판정 한 줄)"
      pattern: "applyAutoSettlement"
    - from: "domain/projects/auto-transition.ts"
      to: "domain/action-log/record.ts"
      via: "SYSTEM_VIEWER로 기록해 행위자 id가 null이 된다"
      pattern: "SYSTEM_VIEWER"
---

<objective>
**As a** 기획 PM·팀장, **I want to** 행사 종료일 다음 날 프로젝트가 저절로 정산 단계로 넘어가고, 종료일이 지났는데 아직 수주중인 건이 상세에서 눈에 띄기를, **so that** 「끝났는데 안 닫힌 건」을 사람이 기억해서 옮기지 않아도 되고, 정산 규칙(실행가만 수정)이 날짜에 맞춰 정확히 걸린다.

이 플랜은 04-06 재계획에서 나눈 **날짜로 움직이는 절반**의 서버 쪽이다(D-76 · D-81). **2026-09-23 체커 1차 분할:** 옛 04-11(23파일)이 한 실행자 컨텍스트를 넘어, 상세 기간 칸(S13) · 정산→진행 되돌리기(D-80) · 진행 이후 기간 저장 규칙(D-82) · 상태 모달의 「기간 적기」 연결을 **04-22**로 옮겼다. 이 플랜은 KST 날짜 모듈 · 자동 정산 · 상세·목록·상태 전환 경로의 선판정 · 종료일 지남 표시와 그 이름 출처를 맡는다. 상태 모델은 04-06, 사람의 전환은 04-20, 상태 화면은 04-21이 만들었다.

**D-76 실행 방식(계획 재량 — 결정):** 읽기 시점 판정. `domain/projects/auto-transition.ts`의 `applyAutoSettlement`가 `UPDATE … WHERE status = 진행 AND end_date < {오늘 KST} … RETURNING`을 한 번 실행하고, 돌아온 행마다 시스템 행위자로 로그를 남긴다. WHERE 절 자체가 멱등성을 보장한다(두 번째 호출은 0행). 호출 지점은 상세 조회 · 목록 조회 · 그 프로젝트의 상태 전환·저장 직전이다(저장 직전 호출은 합성 저장을 넓히는 04-22가 건다). Cloud Scheduler 예약 작업은 Phase 7이 알림 tick과 함께 **같은 함수를 부르는 얇은 배선**으로 얹는다(RESEARCH §2, A8) — 예약 작업 인프라(OIDC·IAM·`/internal/` 인증)를 이 페이즈에서 절반만 짓지 않는다.

Purpose: 04-12(정산의 실행가 한정 편집)가 걸리려면 프로젝트가 제때 정산이 되어야 하고, 04-17·04-18(목록)이 종료일 지남·정산 태그를 보이려면 목록 읽기도 같은 판정을 거쳐야 한다. 04-22의 기간 칸이 이 플랜의 함수·이름 출처를 그대로 쓴다.

Output: KST 날짜 모듈 · 자동 전환 함수 · 상세·목록·상태 전환 세 경로 선판정(저장 경로는 04-22) · 종료일 지남 표시 · 담당자 이름 출처 · 단위 2 · 통합 1 · E2E 1.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 시간은 `vi.useFakeTimers`로 얼리지 않고 `deps.now`로 주입한다(리포 관례, PATTERNS). `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-21-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 723–751행(§2 D-76 — 스케줄링 인프라 부재 · 구현 패턴 · 호출 지점 · 시간 주입 · KST 경계 · 서버 시간대 Gap) · 857–861행(Pitfall 7)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 436–451행(auto-transition 배정) · 484–490행(시스템 행위자·시간 주입)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1057–1069행(S3 부제·종료일 지남) · 352행(Copywriting 종료일 지남)
# docs/design/SYSTEM.md §8 규칙 3 아래 담당자 표기(개정 ⑪)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(날짜로 움직이는 부분 — 서버·표시)** 다. 프로브는 옛 네 상태 문구로 돌았으므로 수용 기준은 D-76·D-81과 UI-SPEC S3에서 끌어와 `must_haves.truths`에 적었다. 옛 04-11이 옮겼던 S13의 상태·backstop(기간 칸이 열린 375 머리 줄)과 금지 항목 둘(행위자 섞임 · PM의 종료일 우회)은 그것을 구현하는 04-22로 함께 갔다 — 조용히 버린 항목은 없다. 종료일 지남 글자가 더해진 머리 줄의 375 폭은 04-21의 S3 overflow backstop을 같은 방법으로 다시 잰다(Task 3).

**계획 단계 가정(사용자에게 표면화):**
- **A8 — 읽기 시점 판정:** 사용자가 「아무도 화면을 열지 않아도 자정에 정확히 바뀌어야 한다」(예: 자정 기준 보고서)를 요구하면 이 가정은 틀리고 예약 작업을 앞당겨야 한다. 지금 D-76 원문은 「실행 방식은 계획이 정하되 멱등」이고, 화면을 여는 순간·저장 직전에 항상 판정하므로 사람이 보는 상태는 언제나 맞다. 상태 날짜는 로그의 **발효일**(종료일 다음 날)로 보여 판정이 늦게 돌아도 기록상의 날짜는 D-76 그대로다.
- **「팀장 {이름}」의 출처:** 조직 표에 팀장 칸이 없다. 이 플랜은 「프로젝트 팀에 오늘(KST) 소속된 사람 중 `projects.status` 쓰기 권한이 있는 계급의 사람, 여럿이면 이름순 첫 사람」으로 정한다(계급 이름을 코드에 박지 않는다 — 04-02 선례). 없으면 이름 없는 문구로 떨어진다(Copywriting의 「팀장이 없으면」 변형). 04-22의 기간 칸 오류 문구와 04-12의 EMPTY가 같은 함수를 쓴다.
- **서버 시간대 Gap:** 이 플랜은 서버 시간대에 기대지 않도록 짰고, 실행 중 로컬 dev DB와 test DB의 `SHOW timezone` 값을 SUMMARY에 적어 리서치 Gap을 닫는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 종료일이 지난 진행 프로젝트를 열면 정산으로 보인다(KST 경계·멱등·시스템 행위자)</name>
  <files>lib/kst-date.ts, test/unit/lib/kst-date.test.ts, domain/projects/status-transitions.ts, domain/projects/auto-transition.ts, repositories/projects.ts, domain/projects/index.ts, domain/projects/status.ts, test/unit/domain/auto-transition.test.ts, test/integration/project-auto-settlement.test.ts, test/e2e/project-period.spec.ts</files>
  <reversibility rating="reversible">읽기 시점 판정은 `applyAutoSettlement` 한 함수 뒤에 있고, Phase 7이 예약 작업에서 같은 함수를 부르면 호출자만 늘어난다 — 저장 형태·데이터 계약이 바뀌지 않는다.</reversibility>
  <read_first>
    - `domain/viewer.ts`(전문) — `SYSTEM_VIEWER`의 정의와 용도
    - `domain/action-log/record.ts` 110–145행 — 시스템 주체의 `actorId`가 null이 되는 자리
    - `domain/projects/status-transitions.ts`(04-06 산출물 전문) · `domain/projects/status.ts`(04-20·04-21 산출물 전문 — `changeProjectStatus`·`lastStatusChangeOn`)
    - `domain/projects/index.ts` 160–230행(`listProjects`·`findProject`)
    - `repositories/projects.ts` 211–302행(단건 조회·조건부 갱신 — 04-20의 `updateProjectStatusIfCurrent`)
    - `domain/quotes/lines.ts` 440–455행(`toLocaleTimeString(…, { timeZone: "Asia/Seoul" })` — 앱 층 KST 선례)
    - `domain/settings/registry.ts` 60–95행(`asOf` 시간 인자 선례) · `domain/quotes/lines.ts`의 `deps` 인자 형태
    - `test/integration/project-status.test.ts`(04-06·04-20 산출물) — 프로젝트를 만들고 상태를 DB에 직접 두는 준비 형태
  </read_first>
  <behavior>
    - `kstToday(new Date("2026-09-17T14:59:59Z"))` → `2026-09-17` · `kstToday(new Date("2026-09-17T15:00:00Z"))` → `2026-09-18`
    - `kstDateOf(new Date("2026-12-31T15:30:00Z"))` → `2027-01-01` · `kstYear(new Date("2026-12-31T15:00:00Z"))` → `2027`
    - `addDays("2026-09-17", 1)` → `2026-09-18` · `addDays("2026-12-31", 1)` → `2027-01-01`
    - `applyAutoSettlement({ projectIds: [p] }, { now: () => 2026-09-17T14:59:59Z })` + 종료일 9/17 진행 → 바뀐 것 0 · 같은 입력에 `now` = 15:00:00Z → [p], 상태 정산, 로그 1줄(actor null, detail.effectiveOn `2026-09-18`)
    - 같은 호출을 두 번 → 두 번째는 [] · 로그는 여전히 1줄
    - 종료일이 없는 진행(있을 수 없지만 방어) · 수주중 · 미수주 · 정산 · 완료 → 바뀌지 않는다
  </behavior>
  <action>
① `lib/kst-date.ts`(순수, import 없음): `kstToday(now: Date): string`(`Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Seoul", year, month, day })` → `YYYY-MM-DD`) · `kstDateOf(date: Date): string` · `kstYear(now: Date): number` · `addDays(ymd: string, n: number): string`(UTC 자정 산술로 달력 날짜만 더한다). 단위 테스트 `behavior` 앞 세 줄(RED → GREEN).

② `domain/projects/status-transitions.ts`에 자동 전환 한 줄 `AUTO_TRANSITIONS = [{ from: "in_progress", to: "settling", trigger: "end_date_passed" }] as const`를 더한다 — 자동 전환이 쓰는 상태 값도 이 데이터 모듈 한 곳에서 온다.

③ `repositories/projects.ts`에 `settleOverdueProjects(viewer, { todayKst, projectIds?, from, to }, tx?)`: `UPDATE projects SET status = $to, version = version + 1, updated_at = now() WHERE status = $from AND end_date IS NOT NULL AND end_date < $todayKst::date [AND id = ANY($projectIds)] AND archived_at IS NULL RETURNING id, end_date`. **날짜 비교에 DB의 현재 날짜 함수나 서버 시간대를 쓰지 않는다** — 오늘은 인자로만 들어온다(Pitfall 7).

④ `domain/projects/auto-transition.ts`: `applyAutoSettlement(opts: { projectIds?: string[] }, deps?: { now?: () => Date; recordAction?; settle? }): Promise<string[]>` — `kstToday(now())` → 리포지토리 한 번 → 돌아온 행마다 `recordAction(SYSTEM_VIEWER, { actionType: "status_change", entity: "project", entityId, detail: { from, to, trigger: "end_date_passed", effectiveOn: addDays(end_date, 1) } })`. 로그 실패는 삼키지 않는다(기존 `recordAction` 규약). 단위 테스트는 `deps.settle`·`deps.recordAction`을 스텁으로 넣어 `behavior` 뒤 세 줄을 DB 없이 증명한다.

⑤ **호출 지점(이 태스크는 상세 하나):** `domain/projects/index.ts`의 `findProject`가 행을 읽기 전에 `applyAutoSettlement({ projectIds: [id] })`를 부른다. `domain/projects/status.ts`의 `lastStatusChangeOn`(04-21)이 최신 로그 detail에 `effectiveOn`이 있으면 그 날짜를 쓴다(부제 `정산 2026-09-18`).

⑥ **서버 시간대 Gap 닫기:** 로컬 dev DB와 test DB에서 `SHOW timezone` 결과를 확인해 SUMMARY에 적는다.

⑦ `test/integration/project-auto-settlement.test.ts`: (a) KST 경계 전후 두 `now`로 실제 DB의 상태 변화 유무, (b) 두 번 호출 · `Promise.all` 동시 두 호출 모두 로그 1줄(DB 재조회), (c) 로그의 `actor_id` null과 detail(from·to·trigger·effectiveOn), (d) 다른 상태 넷은 바뀌지 않는다, (e) `findProject`가 돌려준 DTO의 상태가 정산이다. `test/e2e/project-period.spec.ts`를 만들고 첫 케이스: 테스트 준비에서 종료일이 어제(KST)인 진행 프로젝트를 DB에 두고 → 기획 PM으로 상세를 열면 상태 태그가 `정산`(warning)이고 부제가 `정산 {종료일 다음 날}`이다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/lib/kst-date.test.ts test/unit/domain/auto-transition.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — KST 경계나 멱등 판정이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-auto-settlement.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 실제 DB에서 경계·멱등·행위자 중 하나가 틀렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error가 나오거나 playwright failed 개수가 1 이상이다 — 상세를 열어도 정산으로 보이지 않는다</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/kst-date.ts`에 import가 없고 단위 테스트가 경계 두 줄(14:59:59Z / 15:00:00Z)과 연말 넘김을 단언한다
    - `settleOverdueProjects`의 날짜 비교가 인자로 받은 날짜 하나만 쓴다(DB 현재 날짜 함수 호출이 그 함수 본문에 없다 — 코드 리뷰 확인 + 통합 테스트 (a))
    - `applyAutoSettlement`가 `SYSTEM_VIEWER`로 기록하고 통합 테스트가 `actor_id` null을 단언한다
    - 두 번·동시 호출 뒤 로그가 1줄임을 DB 재조회로 단언한다
    - `findProject` 경로에서 자동 전환이 먼저 돈다(통합 테스트 e)
    - dev·test DB의 `SHOW timezone` 값이 SUMMARY에 있다
    - E2E 첫 케이스가 정산 태그와 발효일 부제를 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>종료일이 지난 진행 프로젝트가 상세를 여는 순간 KST 기준으로 정산이 되고, 멱등·시스템 행위자·발효일 표시가 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 목록·상태 전환 경로의 선판정(D-76 읽기 시점 판정 — 합성 저장 경로는 04-22)</name>
  <files>domain/projects/index.ts, domain/projects/status.ts, test/integration/project-auto-settlement.test.ts</files>
  <read_first>
    - `domain/projects/index.ts`의 `listProjects`(04-05·04-21 산출물)
    - `domain/projects/status.ts`의 `changeProjectStatus`(04-20 산출물)
  </read_first>
  <behavior>
    - 목록을 읽으면(`listProjects`) 지난 진행 프로젝트가 전부 정산으로 바뀌어 돌아온다
    - 자정을 넘겨 연 화면에서 상태 전환을 요청해도 판정 전에 자동 전환이 먼저 적용된다(통합 테스트에서 `now` 주입)
  </behavior>
  <action>
① 통합 케이스를 먼저 쓴다(RED): `project-auto-settlement.test.ts`에 (f) `listProjects`가 지난 진행을 정산으로 돌려준다, (h) `changeProjectStatus` 직전 선판정(`now` 주입 — 지난 진행 프로젝트에 대한 전환 요청이 정산 기준으로 판정되고 로그는 시스템 1줄). 합성 저장 경로의 (g)는 04-22가 `project-period.test.ts`에 쓴다(저장 입력을 넓히는 플랜이 그 경로의 선판정도 건다).

② `listProjects`가 조회 전에 `applyAutoSettlement({})`(프로젝트 목록 제한 없음 — 한 번의 UPDATE)를 부른다. `changeProjectStatus`(04-20)가 게이트 판정 전에 `applyAutoSettlement({ projectIds: [id] })`를 부른다 — `deps.now`를 받아 넘긴다. 한 요청에서 두 번 부르는 것은 무해하다(멱등) — 호출을 줄이려고 캐시를 만들지 않는다(GREEN).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-auto-settlement.test.ts test/integration/project-status.test.ts test/integration/projects-list.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 목록·전환 경로 중 하나가 선판정을 건너뛰거나, 선판정이 04-20의 전환·기존 저장을 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `listProjects`·`findProject`·`changeProjectStatus` 세 경로가 `applyAutoSettlement`를 부른다(통합 테스트 e·f·h) — 네 번째 경로(`saveProjectLedger`)는 04-22 통합 (g)
    - (h)가 `deps.now` 주입으로 자정 뒤 판정을 재현한다 — 가짜 타이머를 쓰지 않는다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상세·목록으로 프로젝트를 보거나 상태를 바꾸면 먼저 날짜 판정이 돈다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 종료일 지남(D-81) + 담당자 이름 출처 + 화면 검증(머리 줄 375)</name>
  <files>domain/projects/status.ts, domain/projects/responsibles.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/unit/domain/auto-transition.test.ts, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1067–1069행(S3 종료일 지남 — 상태 바꾸기가 있는 사람에게는 글자만) · 352행(Copywriting 표시 — 종료일 지남)
    - `domain/org/index.ts`의 `teamAtDate()`와 `repositories/team-memberships.ts` — 오늘 소속 판정
    - `domain/permissions/can.ts`·`repositories/permissions.ts` — 계급별 메뉴 권한 조회(팀장 이름 찾기에 쓴다)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 머리 줄 `StatusTag`·`statusControl`(04-21)을 찾아 범위만 Read
  </read_first>
  <behavior>
    - `isEndDatePassed`: 수주중 + 종료일 < 오늘(KST) → 참 · 수주중 + 종료일 = 오늘 → 거짓 · 수주중 + 종료일 없음 → 거짓 · 진행·정산·완료·미수주 → 거짓
    - 상세에 `종료일 지남` · 상태를 바꿀 수 없는 사람(담당 PM)에게는 `종료일 지남 · 팀장 {이름}` · 팀장이 없으면 `종료일 지남`만
  </behavior>
  <action>
① `test/unit/domain/auto-transition.test.ts`에 `isEndDatePassed` 결정표 describe를 먼저 쓴다(RED). 판정 함수 `isEndDatePassed({ status, endDate, todayKst })`를 **`domain/projects/status.ts`에** 둔다(04-18 목록과 04-22 상태 모달이 같은 함수를 쓴다 — 기간 편집 모듈과 섞지 않는다) — 수주중이고 종료일이 있고 종료일 < 오늘이면 참(GREEN).

② `domain/projects/responsibles.ts`: `projectResponsibles(viewer, project, deps?)` → `{ pmName, teamLeadName | null }` — 팀장 = 프로젝트 팀에 오늘(KST, `lib/kst-date`) 소속된 사람 중 계급이 `projects.status` 쓰기를 가진 사람, 여럿이면 이름순 첫 사람(`<probe_fallback>` 가정). 계급 이름·id를 코드에 박지 않는다.

③ **화면(D-81).** `[id]/page.tsx`가 `isEndDatePassed`와 `projectResponsibles`를 계산해 넘기고, `quote-table.tsx` 머리 줄의 상태 태그 오른쪽에 `--fs-sm --warning` 글자 `종료일 지남`, 「상태 바꾸기」가 없는 사람에게는 `종료일 지남 · 팀장 {이름}`(없으면 글자만). 태그가 아니라 글자다(UI-SPEC Color 표). E2E(`project-period.spec.ts`)에 두 변형(팀장 = 글자만 · 담당 PM = `· 팀장 {이름}`)과 종료일 = 오늘인 수주중에 표시가 없는 케이스를 더한다.

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 04-21의 S3 overflow backstop을 이 상태로 다시 잰다(`종료일 지남 · 팀장 {이름}`이 붙은 담당 PM 시점과 「상태 바꾸기」가 있는 팀장 시점의 375 머리 줄 — 가로 스크롤 0, 글자·버튼이 숨지 않음) → 필요하면 `project-detail.module.css`만 고친다(글자를 줄이거나 버튼을 숨기지 않는다) → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/auto-transition.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 종료일 = 오늘을 지남으로 판정하거나 수주중 밖 상태에서 참이 된다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 종료일 지남 두 변형 E2E가 실패하거나 기존 화면이 깨졌다</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S3 overflow 재판정(종료일 지남 글자가 붙은 375 머리 줄)을 SUMMARY에서 확인</test>
      <expected>가로 스크롤 0, 글자·버튼이 숨지 않음</expected>
      <why_human>폭·배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `isEndDatePassed`가 `domain/projects/status.ts`에 있고 종료일 = 오늘은 거짓이다(단위 결정표)
    - 종료일 지남이 태그가 아니라 글자이고, 담당 PM 변형에 팀장 이름이 붙는다(E2E)
    - `projectResponsibles`에 계급 이름·id 리터럴이 없다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>종료일이 지난 수주중이 상세에서 눈에 띄고 다음 한 수(상태 바꾸기 또는 담당 팀장)가 보이며, 04-22·04-12·04-18이 쓸 이름 출처와 판정 함수가 준비됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시스템 주체 → 상태 | 읽기 요청이 쓰기(자동 전환)를 일으킨다. 요청한 사람의 권한으로 쓰지 않는다 |
| DB 시간대 → 날짜 경계 | 서버 설정이 경계를 바꾸면 전환 시각이 틀어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-78 | Spoofing | 자동 전환 행위자 | medium | mitigate | 자동 전환은 요청한 viewer가 아니라 `SYSTEM_VIEWER`로 기록되어 actor가 null이다(통합 테스트 c). 사람의 되돌리기 쪽 기록과의 구분 단언은 04-22 통합 테스트(a·c) |
| T-04-79 | Tampering | 날짜 경계 | medium | mitigate | 오늘(KST)은 앱이 주입된 시각에서 계산해 쿼리 인자로 넘긴다. DB 현재 날짜·서버 시간대를 쓰지 않는다(Pitfall 7). 경계 전후 두 시각을 통합 테스트로 고정한다 |
| T-04-81 | Denial of Service | 목록 읽기의 UPDATE | low | accept | 목록 읽기마다 조건부 UPDATE 한 번이 돈다. 대상이 없으면 0행이고 인덱스(`projects_status_idx`·`projects_end_date_idx`)를 탄다. 10~30명 규모에서 비용이 낮다 |
| T-04-82 | Repudiation | 상태 날짜 | low | mitigate | 자동 전환 로그 detail에 발효일을 남겨, 판정이 늦게 돌아도 기록상 전환일이 D-76과 같다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- dev·test DB `SHOW timezone` 값이 SUMMARY에 기록됨
- 독립 DOM 감사(종료일 지남이 붙은 375 머리 줄) 판정이 SUMMARY에 있다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 진행 프로젝트가 종료일 다음 날(KST)부터 정산으로 보이고, 몇 번을 판정해도 로그는 한 줄이다
- 상세·목록·상태 전환 세 경로가 판정 전에 자동 전환을 적용한다(합성 저장 경로는 04-22)
- 종료일이 지난 수주중이 상세에서 눈에 띄고 다음 한 수(상태 바꾸기 또는 담당 팀장)가 보인다
- Phase 7은 예약 작업에서 `applyAutoSettlement`를 부르기만 하면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `lib/kst-date.ts` — `kstToday` · `kstDateOf` · `kstYear` · `addDays` |
| 함수(신규, domain) | `applyAutoSettlement`(`domain/projects/auto-transition.ts`) · `projectResponsibles`(`domain/projects/responsibles.ts`) · `isEndDatePassed`(`domain/projects/status.ts`) |
| 함수(신규, repository) | `settleOverdueProjects`(`repositories/projects.ts`) |
| 상수 | `AUTO_TRANSITIONS`(`status-transitions.ts`) |
| 로그 detail 규약 | `status_change` detail `{ from, to, trigger: "end_date_passed", effectiveOn }`(시스템 행위자 — 사람 쪽 `end_date_extended`는 04-22) |
| 변경된 기존 심볼 | `findProject`·`listProjects`·`changeProjectStatus`(선판정 — `saveProjectLedger`는 04-22) · `lastStatusChangeOn`(발효일) · `QuoteLedger` 머리 줄(종료일 지남 글자) |
| 테스트 | `test/unit/lib/kst-date.test.ts` · `test/unit/domain/auto-transition.test.ts` · `test/integration/project-auto-settlement.test.ts` · `test/e2e/project-period.spec.ts`(전부 신규 — 04-22가 E2E를 확장) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 상세 기간 칸(S13) · 정산→진행 되돌리기(D-80) · 진행 이후 기간 저장 규칙(D-82) · 합성 저장 경로의 선판정 · 「기간 적기」 연결 · 게이트 `project.period-edit` · 금지 항목 둘(04-22) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md` when done
</output>
