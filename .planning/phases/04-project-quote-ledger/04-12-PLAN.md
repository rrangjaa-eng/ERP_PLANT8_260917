---
phase: 04-project-quote-ledger
plan: 12
type: execute
wave: 13
depends_on: ["04-44"]
files_modified:

  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - test/unit/domain/quote-lines.test.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - repositories/archive.ts
  - domain/archive/index.ts
  - repositories/projects.ts
  - domain/projects/ledger.ts
  - domain/revenue/index.ts
  - app/(app)/projects/actions.ts
  - test/integration/quote-lines.test.ts
  - test/integration/tx-safety.test.ts

autonomous: true
requirements: [PROJ-02, UX-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "정산 프로젝트에서 담당 PM은 기존 견적 줄의 실행가 셀만 편집할 수 있고 수량·단가·견적가·소분류·항목·거래처·상태·비고 셀은 잠김이다 — 셀 단계를 서버가 줄마다 판정해 DTO(`cellEditability`)로 보낸다(D-78 — 사용자 D10 개정, UI-SPEC S4 상태별 셀 단계)"
    - "정산에서 담당 PM은 **새 줄을 추가할 수 있다** — 새 줄은 소분류·항목·거래처·비고·실행가만 적고 견적 칸은 잠김이다: **단가 0원(원화) · 수량은 기본값 1 그대로**(견적가 0). 단가가 0이 아니거나 원화가 아니거나 수량이 1이 아닌 새 줄은 서버가 `정산 · 새 줄은 실행가만`으로 거부한다 — 기존 수량 검증(수량 > 0)을 느슨하게 하지 않는다(사용자 D10·D12, CEO A-26·A-26a, 엔지 리뷰 A §2 P1)"
    - "정산에서 줄 삭제(보관) · 기존 줄의 순서 이동 · 줄 복제 · 새 차수는 서버가 거부한다 — `structuralEditability`가 정산에서 추가만 참이고 나머지는 거짓이며, 새 차수 판정 입력도 같은 함수다(사용자 D10 — D-78 개정)"
    - "정산에서 실행가 밖 칸의 값을 저장 페이로드에 직접 실어 보내도 서버가 그 칸을 `정산 · 실행가와 새 줄만`으로 거부하고 배치 전체가 저장되지 않는다 — 실행가만 바꾼 저장은 통과한다(D-78, RESEARCH Validation D-78)"
    - "잠긴 칸의 거부 이유와 표 위 한 줄(04-30)은 **한 문자열 · 한 함수**다 — 정산 `정산 · 실행가와 새 줄만` · 완료 `완료 · 견적 줄 잠김`을 `quoteLockReason`만 만들고 게이트 규칙·화면이 그 반환값을 쓴다(DR-2, UI-SPEC rev 5 Copywriting `Error — 셀(잠김) = 표 위 한 줄`)"
    - "바뀐 칸 판정은 쓰기와 같은 정규화를 거친 값끼리 비교한다 — 환율 `1350` 대 `\"1350.0000\"`, 수량 `2` 대 `\"2.00\"`, 비고 null 대 undefined가 「바뀜」으로 잡혀 정산 저장이 헛거부되지 않는다(CEO A-21)"
    - "줄 순서는 흔들리지 않는다 — 순서를 싣지 않은 저장은 기존 줄의 `sort_order`를 그대로 두고 새 줄을 차수 끝(`max + 1`, 배치 순서)에 붙인다. 이동·중간 삽입·복제가 있는 저장은 활성 줄 전체의 표시 순서 `order: string[]`(새 줄 id 포함)을 한 번 싣고 서버가 그 순서로 `sort_order`를 다시 쓴다. 조회는 `sort_order` 다음 줄 id로 정렬해 같은 값이 있어도 두 번 읽은 순서가 같다. 정산에서는 기존 줄의 상대 순서가 그대로인 `order`만 받는다(CEO A-03, 엔지 리뷰 A §2 P2)"
    - "새 줄은 화면이 만든 uuid를 id로 싣고(`isNew: true`) 서버는 같은 id가 이미 있으면 넣지 않는다(`ON CONFLICT (id) DO NOTHING`) — 커밋은 됐는데 응답을 잃어 **같은 값으로** 다시 보낸 재전송은 아무것도 하지 않고 성공으로 끝나 그 줄은 한 번만 있다. 같은 id인데 값이 다르거나 다른 차수·프로젝트·보관된 줄의 id면 배치 전체를 거부한다 — 재전송 규칙은 매출·리저브(그룹 B)와 같다(ENG-D10)"
    - "견적 줄 저장의 결과는 그 차수의 **활성 줄 전체**(표시 순서)다 — 3줄 중 1줄만 고쳐 저장해도 화면이 받는 목록은 3줄이다(엔지 리뷰 A §2 P2 — 04-30이 화면에서 쓴다)"
    - "견적 줄 저장은 트랜잭션 안에서 프로젝트 행을 배타 잠금(`loadProjectForGate`)한 뒤 판정하고 쓴다 — 저장과 같은 프로젝트의 정산→완료 전환이 겹치면 한쪽이 기다렸다가 새 상태로 판정되어, 완료 프로젝트에 줄 편집이 성공으로 들어가는 일이 없다(CEO A-06·OV-3)"
    - "견적 줄 저장은 세 단계다 — 트랜잭션 전(권한·사용자 정의 필드 스키마) · 트랜잭션 안(차수 조회·잠금·판정·쓰기·활성 줄 읽기·행동 로그 — 전부 `tx`) · 커밋 뒤(`projectMany` 투영 · 트랜잭션을 연 쪽이 최근 환율 기억). 잠근 트랜잭션 안에서 풀을 부르지 않으므로, 풀 크기 2에서 같은 프로젝트에 새 줄을 싣는 합성 저장 셋을 동시에 보내면 10초 안에 **셋 다 성공**한다(ENG-D3 ① — ARCHITECTURE §4-8)"
    - "견적 줄·매출 저장이 트랜잭션을 받으면 행동 로그도 그 트랜잭션으로 쓴다 — 줄은 유효하고 매출 줄이 게이트·검증에 걸려 합성 저장이 되돌아가면 `document_update` 로그도 남지 않는다(유령 로그 없음, 엔지 리뷰 A §1 P2)"
    - "합성 저장에서 기간과 줄을 함께 바꾸면 줄은 **저장 뒤 상태**로 판정된다 — 종료일을 늦춰 정산을 진행으로 되돌리며 단가를 고친 저장은 통과하고, 종료일을 앞당겨 정산을 만들며 단가를 고친 저장은 `정산 · 실행가와 새 줄만`으로 전부 거부된다(기간도 안 바뀜)(ENG-D6 — 04-22가 순서를 고정. 두 저장 모두 `seenStatus`는 바꾸기 전 상태다 — 사람 자신의 기간 변경은 04-22의 상태 바뀜 거부에 걸리지 않는다)"
    - "실린 줄 id는 그 차수의 줄만 찾고 고친다 — 다른 차수·프로젝트의 줄 id를 섞은 배치는 0행이 되어 전부 거부된다(CEO B-01 줄 소속 — 현재 차수 판정은 차수가 여럿이 되는 04-14)"
    - "완료 프로젝트는 모든 셀이 잠김이다(D-47) · 미수주·수주중·진행의 편집은 04-04 그대로 전부 열려 있다(D-45)"
    - "저장된 줄을 지우면 일괄 저장 때 같은 트랜잭션에서 보관(`archived_at`)되고 보관함에서 복원할 수 있다 — 물리 삭제가 없다. 보관된 줄은 상세 견적 목록·목록 합계(견적·실행가·수익금)에서 빠지고, 보관된 줄 id로 고치는 요청은 거부된다(D-56, CEO A-04)"
    - "보관함에서 견적 줄을 복원하면 저장과 같은 규칙을 한 트랜잭션에서 지난다 — 완료 프로젝트 줄은 복원이 거부되고, 정산 프로젝트 줄은 견적가가 0일 때만 복원된다(CEO A-19·OV-2 — 줄 수 상한·승인 합계 검사는 같은 복원 함수에 04-26·04-14가 더한다)"
    - "연결 문서가 있는 줄은 보관 대신 `취소` 상태로만 바뀌고(견적가 0, 이력 유지) 금액 셀(수량·단가·실행가·환율)이 읽기 전용이며, 그 값을 실어 보내면 `지출결의 {번호} 연결됨 · 고치려면 새 차수`로 거부된다 — 판정 입력(연결 문서 조회)은 한 지점이고 이 페이즈에서는 비어 있다(D-56·D-66, PROJ-02)"
    - "`취소` 상태 줄의 견적가는 서버가 0으로 계산해 저장한다(PROJ-02 「견적가 0, 이력 유지」) — 줄 상태 입력은 도메인 상수 `QUOTE_LINE_STATUSES`의 값만, 프로젝트 id·줄 id는 uuid 형식만 받는다(CEO A-37, 엔지 리뷰 A P3)"
    - "이 플랜의 규칙(정산 칸·구조·줄 소속·보관 줄·순서·복원)에 걸린 거부는 화면 거부와 함께 04-20의 `denyWrite` 한 함수로 서버 운영 로그 `write.denied` 한 줄(보는 사람 id · 규칙 이름 · 오류 종류 · 프로젝트·차수·줄 id — 금액 없음)을 남기고, 행동 로그(`action_log`)에는 남기지 않는다 — `saveQuoteLines`의 거부 지점은 하나이고 뒤 규칙(04-26 상한 · 그룹 B의 조정·차수·승인)도 그 지점을 지난다(사용자 D19-3, CEO 리뷰 B-26, 엔지 리뷰 B)"
  prohibitions:
    - statement: "정산의 편집 제한이 실제 원가 입력을 막는 잠금이 되어서는 안 된다. 정산은 끝난 행사의 실제 비용(실행가)을 맞추는 단계이고(D-77·D-78), 실행가 셀이나 늦게 발견된 비용의 새 줄(사용자 D10)을 잠그는 구현은 PM이 원가를 시스템 밖에서 맞추게 만든다"
      status: resolved
      verification: test
    - statement: "정산에서 PM이 추가한 줄이 견적 금액을 만들어서는 안 된다. 정산은 견적이 끝난 뒤의 단계이고, 새 줄의 견적 칸이 0이 아니면 계약·손익의 견적 쪽 숫자가 행사 뒤에 바뀐다(사용자 D12)"
      status: resolved
      verification: test
    - statement: "되돌린 저장이 성공 기록을 남겨서는 안 된다. 거부된 배치의 `document_update` 로그는 「줄을 저장·보관했다」는 거짓 감사 기록이 된다 — 로그는 쓰기와 같은 트랜잭션이어야 한다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/edit-scope.ts"
      provides: "상태 × 쓰기 권한 × 연결 문서 × 새 줄 여부의 셀 단계와 구조 편집 가능성을 한 곳에서 판정하는 순수 함수(게이트 규칙과 DTO가 같이 쓴다) — 셀 단계 유니온과 줄 상태 목록을 domain이 선언한다"
      exports: ["QUOTE_LINE_FIELDS", "QUOTE_LINE_STATUSES", "QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT", "lineCellEditability", "structuralEditability", "quoteCellsZero", "quoteLockReason", "linkedDocumentReason"]
    - path: "test/unit/domain/quote-edit-scope.test.ts"
      provides: "다섯 상태 × 권한 × 연결 문서 × 새 줄 결정표 + 구조 결정표 + 정산 새 줄 견적 칸 판정 + 순서 판정"
      min_lines: 70
    - path: "test/integration/quote-lines.test.ts"
      provides: "정산 허용·거부 매트릭스 · 정규화 비교 · 순서(append·order·동률·정산) · 줄 소속 · 재전송 멱등 · 보관·취소 · 보관 줄 합계 제외 · 복원 규칙 · 전체 목록 결과 · 유령 로그 · 합성 저장 판정 순서 · 저장 대 전환 경합(두 순서)"
      min_lines: 160
  key_links:
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "`project.line-edit` 규칙이 DTO와 같은 셀 단계·구조 함수로 저장·복원을 판정한다"
      pattern: "lineCellEditability"
    - from: "domain/quotes/lines.ts"
      to: "domain/projects/auto-transition.ts"
      via: "`saveQuoteLines`와 `restoreQuoteLine`이 tx 안에서 `loadProjectForGate`로 프로젝트 행을 배타 잠금하고 판정한다"
      pattern: "loadProjectForGate"
    - from: "domain/projects/ledger.ts"
      to: "domain/quotes/lines.ts"
      via: "합성 저장이 트랜잭션 전 `prepareQuoteLineSave` · 트랜잭션 안 `writeQuoteLinesInTx` · 커밋 뒤 `finishQuoteLineSave`를 부른다 — 잠금 안 풀 호출 없음"
      pattern: "writeQuoteLinesInTx"
    - from: "domain/quotes/lines.ts"
      to: "domain/rules/deny-write.ts"
      via: "규칙 거부가 `denyWrite` 한 함수로 운영 로그를 남긴다(04-20)"
      pattern: "denyWrite("
    - from: "domain/quotes/lines.ts"
      to: "domain/permissions/project.ts"
      via: "목록·저장 결과를 `projectMany`로 투영한다 — 300줄에서도 노출 조회가 항목 수만큼(04-32)"
      pattern: "projectMany"
    - from: "domain/archive/index.ts"
      to: "domain/quotes/lines.ts"
      via: "보관함 복원이 견적 줄이면 도메인 복원 함수에 통째로 맡긴다(범용 복원이 도메인 검사를 우회하지 않는다)"
      pattern: "restoreQuoteLine"
    - from: "repositories/projects.ts"
      to: "db/schema/quote-lines.ts"
      via: "목록 합계 하위 질의가 보관된 줄을 뺀다"
      pattern: "archivedAt"
---

<objective>
**As a** 정산 단계의 기획 PM, **I want to** 끝난 행사의 견적 구조는 그대로 둔 채 실제로 쓴 돈(실행가)을 맞추고 늦게 발견된 비용은 새 줄로 더하되 견적 금액은 건드리지 못하며, 완료된 프로젝트는 손댈 수 없게 잠기고, 지운 줄은 보관함으로 가기를, **so that** 정산 중에 견적이 바뀌어 계약 금액·손익이 흔들리지 않고, 서버가 받는 칸과 화면이 여는 칸이 같은 함수에서 나온다.

이 플랜은 견적 줄 표의 **편집 범위 서버 쪽 한 곳**을 만든다(RESEARCH §3 — 셀 편집 가능성은 상태 × 연결 문서 × 계급의 조합이고 한 함수에서 합쳐야 한다). 04-06이 만든 게이트 규칙 `project.line-edit`(완료 잠금)를 셀·구조 단위로 넓히고(D-78 — 사용자 D10·D12 개정), 04-04가 클라이언트에서만 지우던 줄 삭제를 서버 보관 경로로 잇는다(D-56).

**2026-09-23 CEO 리뷰 반영 — 서버·화면 분할.** 리뷰 정정(A-03·A-04·A-06·A-19·A-21·A-37 · B-01 줄 소속 · OV-1·OV-2·OV-3)과 사용자 D10·D12(정산 새 줄 허용 · 견적 칸 0 잠금)가 더해지며 옛 04-12(14파일)는 파일 상한을 넘었다. 이 플랜은 **서버 쪽**(편집 범위 함수 · 게이트 · 저장 판정 · 보관·취소·복원 · 합계 제외 · 경합)이고, 화면 쪽(셀 단계를 격자에 반영 · 표 위 한 줄 · 힌트 줄 · EMPTY · 구조 컨트롤 · 모달 · 순서 전송)은 **04-30**이 잇는다. 차수당 줄 수 상한(D-86)은 04-26, 조정 줄(D-83)·견적 외 비용 줄(D-48)의 줄 종류 축은 04-13·04-23, 고객 승인 차수의 견적 칸 잠금과 승인 합계 불변(사용자 D7 · OV-1)은 승인을 만드는 04-14가 이 플랜의 두 함수에 축을 더한다(그룹 B).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`).** (a) **엔지 리뷰 A §2 P1 — 정산 새 줄의 「견적 칸 0」:** 옛 정의(수량 0 그리고 단가 0)는 기존 검증(`lines.ts:395-403` 수량 > 0)과 모순이라 통합 (g)가 늘 빨갛다 — 「단가 0원(원화) · 수량 기본 1 잠금」으로 바꾼다. (b) **ENG-D10 — 재시도 중복:** 새 줄이 화면이 만든 uuid를 id로 싣고 서버는 `ON CONFLICT (id) DO NOTHING` — 응답을 잃은 재전송이 줄을 두 번 넣지 않는다. (c) **엔지 리뷰 A §2 P2 — 순서:** 끝에 붙이는 새 줄은 순서를 싣지 않고(서버 `max + 1`), 이동·중간 삽입은 `order: string[]` 한 번, 조회는 줄 id로 동률을 깬다. (d) **엔지 리뷰 A §2 P2 — 저장 결과:** 활성 줄 전체를 돌려준다(지금은 저장한 줄만 돌려줘 화면이 나머지 줄을 잃는다). (e) **엔지 리뷰 A §1 P2 — 유령 로그:** `saveQuoteLines`·`saveRevenue`가 받은 트랜잭션으로 로그를 쓴다. (f) **ENG-D3 ① — 잠금 안 풀 호출 금지:** 저장을 트랜잭션 전·안·뒤 세 단계로 나누고 합성 저장도 그 단계를 부른다(풀 2 동시 저장 셋 — 셋 다 성공). (g) **ENG-D3 ②:** 목록·저장 결과를 `projectMany`로 투영한다(300줄 → 항목 수만큼 조회). (h) **ENG-D6:** 합성 저장의 판정 순서(04-22)가 정산 규칙으로 이어지는 결과를 여기서 단언한다. (i) **엔지 리뷰 A P3:** 줄 상태 도메인 상수 `QUOTE_LINE_STATUSES` 신설(지금은 화면 라벨 표뿐), 낡은 주석(`repositories/projects.ts:24-26` · `lines.ts:348-353`) 정리. (j) **엔지 리뷰 B:** 거부 운영 로그는 04-20의 `denyWrite`.

Purpose: 04-11이 프로젝트를 제때 정산으로 만들고 04-22가 합성 저장 안에 잠금 읽기를 세웠으니, 이제 정산이 실제로 「실행가 수정 + 새 줄(견적 0)」이 되어야 한다. D-77은 정산 단계의 뜻(발행 요청·증빙 마감)을 정했지만 그 흐름은 Phase 5·6이다 — 이 플랜은 편집 범위만 만든다.

Output: 편집 범위 순수 함수 · 줄 상태 상수 · 게이트 확장 · 저장 세 단계 · 정규화 비교 · 순서(append·`order`·동률) · 줄 소속 · 재전송 멱등 삽입 · 줄 보관·취소·복원 · 보관 줄 합계 제외 · 연결 문서 조회 지점 · 입력 검증 · 전체 목록 결과 · 로그 tx(견적 줄·매출) · 저장 대 전환 경합 테스트 · 단위 2 확장 · 통합 1 확장 · `tx-safety` 확장.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 경합 순서는 `test/integration/lock-race.ts`(04-20)의 `deferred`·`waitForLockWaiter`로 정한다 — `sleep` 금지. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-44-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-32-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 753–761행(§3 D-78/D-83 — 판정 지점) · 845–849행(Pitfall 5)

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5) `## 적용 규칙` 「S4」의 「상태별 편집 범위 · 조정 줄 · 페이지 · 상한」 1183–1199행(셀 단계 표 1185–1194 · 정산 구조 1195–1197) · `## Copywriting Contract` `Error — 셀(읽기 전용, D-66)`~`Error — 정산 서버 거부(방어, 04-12)` 371–373행 · `Error — 저장(재전송 불일치)`·`Error — 저장(순서·소속, 방어)` 365–366행 — rev 4 줄 번호는 쓰지 않는다

# docs/designs/plant8-erp-phase4-ceo-review-260923.md 748–762행(A-03·A-04·A-06) · 790행(A-19) · 794행(A-21) · 1057–1084행(B-01)

# docs/designs/plant8-erp-phase4-eng-review-260923.md 64–68행(R5 ENG-D6) · 92–99행(R9 ENG-D10) · 116–140행(묶음 A) · 부록 A §2(quoteCellsZero · sortOrder · 저장 결과 · 줄 상태 상수) · §1(유령 로그 · 잠금 안 풀 호출)

# docs/ARCHITECTURE.md §4-8(04-32 — 트랜잭션·잠금 규약)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-02(취소 상태·연결 문서 줄)** · **UX-04(전부 저장 또는 전부 거부 — 잠긴 칸 · 중복 저장 없음)** 둘이다(UX-05의 잠긴 상태 무동작은 화면이라 04-30). 수용 기준은 D-77·D-78(사용자 D10·D12 개정)·D-56·D-66과 UI-SPEC S4에서 끌어왔다. S4의 empty·zero-one-many backstop과 표 위 한 줄·힌트 줄·EMPTY 변형·모달 truths는 화면을 맡는 04-30으로 함께 갔다 — 조용히 버린 항목은 없다.

**계획 단계 가정·확정(표면화):**

- **정산 편집 매트릭스(사용자 D10·D12 확정 — D-78 개정):** 정산에서 담당 PM(쓰기 권한)은 기존 줄의 실행가 수정 + 새 줄 추가(소분류·항목·거래처·비고·실행가). 삭제·기존 줄 순서 이동·복제·새 차수는 서버 거부. 완료는 전부 잠김.
- **정산 새 줄의 「견적 칸 0」(엔지 리뷰 A §2 P1 정정):** 견적 칸은 수량·단가(통화·외화 금액·환율 포함)이고 **잠김**이다. 서버 판정 `quoteCellsZero({ quantity, unitPrice })` = 단가 통화가 원화 · 단가 금액 0 · 수량이 비었거나 1(기본값)일 때만 참 — 견적가가 0이 되는 조합이다. 수량 0을 받으려고 기존 수량 검증(> 0)을 느슨하게 하지 않는다. 줄 상태 칸은 새 줄에서도 기본값 그대로(잠김). 화면 기본값(수량 1 · 단가 원화 0)은 04-30이 같은 정의로 만든다. 사용자가 「수량 1」 표시가 이상하다고 하면 표시만 바꾼다(저장 규칙은 그대로) — SUMMARY에 한 줄.
- **거부 문구 = UI-SPEC rev 5 Copywriting 원문(계약 — SUMMARY에 따로 차이 표를 두지 않는다):** 잠긴 칸의 이유는 표 위 한 줄과 **한 문자열**이다(DR-2 · P0 「같은 사실을 두 자리에 쓰지 않는다」) — 정산 `정산 · 실행가와 새 줄만` · 완료 `완료 · 견적 줄 잠김`(`Error — 셀(잠김) = 표 위 한 줄`). 이 문자열은 `domain/quotes/edit-scope.ts`의 `quoteLockReason({ status })` 한 함수가 만들고 게이트 규칙과 04-30의 표 위 한 줄(`tableLockLine`)이 같이 부른다 — 입력이 객체라 승인 차수 문구(`{n}차 고객 승인됨 · 고치려면 새 차수`, 우선순위 완료 > 정산 > 승인)는 04-14·04-40(그룹 B)이 같은 함수에 더한다. 읽기 전용 칸 `지출결의 {번호} 연결됨 · 고치려면 새 차수`(`Error — 셀(읽기 전용, D-66)` — `linkedDocumentReason(number)`, 번호는 연결 문서 조회 결과의 첫 번호). 서버 방어 `정산 · 새 줄은 실행가만` · `정산 · 줄 삭제·이동 없음`(`Error — 정산 서버 거부(방어, 04-12)`) · `줄 순서가 맞지 않음 · 새로 고침`(`Error — 저장(순서·소속, 방어)`) · `이미 저장된 줄과 값이 다름 · 새로 고침`(`Error — 저장(재전송 불일치, ENG-D10)`). rev 5에 없는 방어 문구 둘은 P0 명사형으로 적고 SUMMARY 「새 문구」에 남긴다(화면은 이 요청을 만들지 않는다): `보관된 줄 · 새로 고침`(보관된 줄 id 수정) · `연결 문서 있음 · 삭제 대신 취소`(연결 문서 줄의 보관 — 화면은 `견적 줄 취소` 모달로 간다).
- **합성 저장의 `seenStatus`(계약 4 — 04-22가 만든다):** 이 플랜의 통합 테스트가 `saveProjectLedger`를 부를 때는 화면이 본 상태(`seenStatus`)를 싣는다 — 준비한 프로젝트의 현재 상태 그대로. 기간을 바꾸며 상태가 바뀌는 저장(ENG-D6 (v))도 `seenStatus`는 **바꾸기 전** 상태다(사람 자신의 기간 변경은 거부 사유가 아니다 — 04-22는 잠금 직후 첫 판정 행과만 비교한다). 상태가 이미 달라진 저장은 이 플랜의 규칙에 닿기 전에 04-22가 `상태가 {상태}로 바뀜 · 전부 거부`로 거부한다.
- **바뀐 칸 판정의 기준:** 서버는 클라이언트 baseline이 아니라 **DB의 현재 값**과, **쓰기 페이로드를 만드는 같은 정규화 함수**를 거친 값끼리 비교한다(A-21). baseline은 충돌 판정(04-04) 전용으로 그대로 둔다.
- **줄 id와 새 줄(ENG-D10):** 모든 입력 줄이 `id`(uuid)를 싣는다. 새 줄은 `isNew: true` + 화면이 만든 uuid(04-30 — `crypto.randomUUID()`), 기존 줄은 `version`(+ baseline). 서버 삽입은 `insertQuoteLineIfAbsent`(`INSERT … ON CONFLICT (id) DO NOTHING RETURNING`) — 돌아온 행이 없으면 같은 tx에서 그 id를 다시 읽어 (1) **같은 차수의 활성 줄이고 정규화한 값(A-21 비교와 같은 표현 — 소분류·항목·거래처·수량·단가 셋·실행가 셋·상태·비고·사용자 정의 필드)이 전부 같으면** 이미 들어간 재전송으로 보고 아무것도 쓰지 않은 채 결과에 넣는다(no-op) (2) 값이 하나라도 다르면 `이미 저장된 줄과 값이 다름 · 새로 고침`로 (3) 다른 차수·프로젝트의 줄이거나 보관된 줄이면 소속 거부로 — (2)(3)은 배치 전체를 `denyWrite`(규칙 `quote.line-replay` · `quote.line-membership`)로 거부한다. 그룹 B의 매출·리저브 재전송도 같은 세 갈래다(교차 그룹 합의). 기존 줄 수정이 섞인 배치의 재전송은 버전 충돌로 전부 거부되어 새 줄도 다시 들어가지 않는다(중복 0 — 충돌 표시로 끝나는 것은 04-04 동작 그대로).
- **순서(엔지 리뷰 A §2 P2):** 입력 `order?: string[]` = 저장 뒤 활성 줄 전체의 표시 순서(보관할 줄 제외 · 새 줄 id 포함). (1) `order`가 없으면 기존 줄의 `sort_order`는 그대로, 새 줄은 차수의 `max(sort_order) + 1`부터 배치 순서로. (2) `order`가 있으면 그 집합이 「현재 활성 줄 − 이번 보관 + 이번 새 줄」과 정확히 같아야 하고(아니면 `줄 순서가 맞지 않음 · 새로 고침` 전부 거부), 서버가 `sort_order = 위치`로 다시 쓴다 — 순서만 바뀐 줄은 `version`을 올리지 않는다(순서는 셀 충돌 판정 대상이 아니다, `setQuoteLineSortOrders`). (3) 게이트의 `reorder`는 `order` 안 **기존 줄의 상대 순서**가 지금과 다를 때만 — 정산은 기존 줄의 상대 순서가 같은 `order`(새 줄은 어디에 끼어도 된다 — 04-19의 그룹 끝 삽입)만 받는다. (4) `listQuoteLinesByRevision`은 `sort_order`, 줄 id 순으로 정렬한다(데모 데이터의 옛 `?? index` 중복값도 결정적).
- **저장 세 단계(ENG-D3 ① — ARCHITECTURE §4-8):** `prepareQuoteLineSave(viewer, revisionId, deps?)`(트랜잭션 전 — `can(projects, write)` · 차수 행(차수가 어느 프로젝트의 것인지는 바뀌지 않는 사실이라 트랜잭션 밖에서 읽는다) · 사용자 정의 필드 스키마; 뒤 플랜이 설정 값을 더한다) → `writeQuoteLinesInTx(viewer, prepared, input, tx, deps?)`(트랜잭션 안 — `loadProjectForGate`·연결 문서 조회·줄 조회·판정·쓰기·활성 줄 읽기·`recordAction({ tx })`, 풀 호출 없음) → `finishQuoteLineSave(viewer, written, deps?)`(커밋 뒤 — `projectMany` 투영). 단독 `saveQuoteLines(viewer, revisionId, input, deps?)`는 셋을 차례로 부르고, 합성 저장(`saveProjectLedger`)은 04-22가 고정한 순서 안에서 준비(트랜잭션 전)·쓰기(재판정 뒤)·마무리(커밋 뒤)를 직접 부른다.
- **커밋 뒤 최근 환율 기억(교차 그룹 규약 — 04-41·04-07이 따른다):** 트랜잭션 안 쓰기 단계는 환율을 기억하지 않고 **기억할 목록** `fxToRemember: { currency, rate }[]`(외화 단가 환율 칸을 이번 저장에서 실제로 고친 줄 — D-71)을 결과로 돌려준다. **트랜잭션을 연 쪽**(단독 `saveQuoteLines` · `saveProjectLedger`)이 커밋 뒤 그 목록마다 `rememberFxRate`(`domain/money/currency.ts` — 고치지 않는다)를 부른다. 매출 쓰기 단계(그룹 B)도 같은 모양의 `fxToRemember`를 돌려주고 `saveProjectLedger`가 둘을 합쳐 한 번에 기억한다 — 04-44가 이미 커밋 뒤에 두는 총 매출 예상가의 USD 환율 기억(`fxRateTouched`일 때)도 같은 목록에 합친다. 기억이 실패해도 저장은 성공으로 돌려주고 `log.error("fx.remember_failed", { currency })` 한 줄을 남긴다(지금은 트랜잭션 안이라 실패가 저장 전체를 되돌렸다 — 편의 기억이 저장을 막지 않게 한다, SUMMARY에 한 줄).
- **잠금 기제(OV-3 — A-06의 FOR SHARE 정정):** 프로젝트 행 `FOR UPDATE`(04-20 `lockProjectForWrite`)를 04-11의 `loadProjectForGate`로 잡는다. 합성 저장(04-22)이 이미 같은 tx에서 잡았으면 다시 잡아도 곧바로 돌아온다 — 04-22의 재판정 뒤라 **새 행**을 읽는다(ENG-D6).
- **D-66 연결 문서 조회 지점:** `linkedDocumentsByLine(viewer, revisionId, tx?)`가 이 페이즈에서는 빈 결과를 돌려준다. Phase 5가 이 한 함수만 채운다(트랜잭션 안에서 불리므로 `tx`를 받는다).
- **승인 축(OV-1·사용자 D7 — 그룹 B):** 두 판정 함수의 입력은 객체라 04-14가 `approved`를 더해도 호출자가 바뀌지 않는다. 복원이 같은 게이트를 지나므로(아래 Task 3) 승인 차수의 복원 거부도 04-14의 축 한 줄로 따라온다.
- **「04-14」 표기:** 그룹 B 계획자가 04-14를 04-14(차수·승인·요약)와 04-40(쓰기 직렬화·위조 차단·승인 차수 잠금)으로 나눴다 — 이 문서의 「04-14」는 둘을 함께 가리킨다.
- **매출 쪽(그룹 B 영역):** 이 플랜은 `domain/revenue/index.ts`의 `saveRevenue`가 받은 `tx`를 `recordAction`에 넘기는 **한 곳만** 고친다(유령 로그 — 엔지 리뷰 A가 이 플랜에 배정). 매출 저장의 세 단계화·클라이언트 uuid(ENG-D10)는 매출을 다시 짜는 그룹 B 플랜이 같은 규약으로 한다.
- **누수 스캔:** `test/integration/leak-scan.test.ts`는 DTO 등록(`registerDto`) 뒤 초록인지 **돌려 확인만** 한다.

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 정산 프로젝트에서 PM이 실행가만 고쳐 저장한다(판정 함수 → 게이트 → 세 단계 저장 · tx 안 잠금 → 정규화 비교 → 순서 보존 → 줄 소속 → 쓰기 → 로그 tx → 전체 목록 → `projectMany` → 통합)</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, domain/projects/ledger.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `domain/rules/register.ts`(04-06·04-20·04-22 산출물 전문) — `project.line-edit`의 현재 ctx
    - `domain/quotes/lines.ts` 100–180행(DTO·투영·`listQuoteLines`) · 185–270행(`QuoteLineWriteRow`·비교 칸 목록) · 340–560행(`saveQuoteLines` — 풀 호출 자리: `can` 362 · 차수 조회 366 · 필드 스키마 377 · `rememberFxRate` 485 · `recordAction` 541 · `project` 549 / 판정·쓰기 · 479–505행 정규화 · 489행 `sortOrder: input.sortOrder ?? index`)
    - `repositories/quote-lines.ts`(전문 134행 — `listQuoteLinesByRevision`의 `orderBy` · `findQuoteLinesByIds` · `updateQuoteLineIfVersionMatches`의 WHERE)
    - `domain/projects/auto-transition.ts`(04-11 — `loadProjectForGate`) · `domain/projects/ledger.ts`(04-22 — 트랜잭션 전 사실 · 콜백 순서 (a)~(g))
    - `domain/permissions/project.ts`(04-32 — `projectMany`) · `domain/action-log/record.ts`(04-32 — `deps.tx`) · `domain/rules/deny-write.ts`(04-20)
    - `docs/ARCHITECTURE.md` §4-8(04-32)
    - `ui/table/types.ts` 1–20행 — `CellEditability` 세 값(domain이 같은 모양의 유니온을 따로 선언한다 — A-32, domain은 `ui`를 import할 수 없다)
    - `test/integration/quote-lines.test.ts`(전문) — 저장 통합 테스트 형태와 04-06이 바꾼 잠금 픽스처
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## 적용 규칙` 「S4」 셀 단계 표 1185–1194행 · `## Copywriting Contract` 371–373행(읽기 전용 · 잠김 = 표 위 한 줄 · 정산 서버 거부 원문)
  </read_first>
  <behavior>
    - `lineCellEditability({ status: 정산, canWrite: true, hasLinkedDocuments: false, isNewLine: false })` → 실행가 `edit`, 나머지 일곱 칸 `locked`
    - 같은 입력의 `isNewLine: true`(정산) → 소분류·항목·거래처·비고·실행가 `edit`, 수량·단가·상태 `locked`(D10·D12)
    - 완료 → 전부 `locked` · 수주중·진행·미수주 → 전부 `edit`(새 줄도)
    - `hasLinkedDocuments: true` + 진행 → 수량·단가·실행가 `readonly`, 항목·비고 `edit`(D-66) · 정산 + 연결 문서 → 실행가도 `readonly`
    - `canWrite: false` → 전부 `locked`
    - `quoteLockReason({ status: 정산 })` → `정산 · 실행가와 새 줄만` · 완료 → `완료 · 견적 줄 잠김` · 수주중·진행·미수주 → null(UI-SPEC rev 5 `Error — 셀(잠김) = 표 위 한 줄` — 표 위 한 줄과 한 문자열, DR-2) · `linkedDocumentReason("26001-0004")` → `지출결의 26001-0004 연결됨 · 고치려면 새 차수`
    - 게이트 `project.line-edit`: 정산 + update [실행가] → 통과 · [실행가, 단가] → `정산 · 실행가와 새 줄만` · 완료 + [비고] → `완료 · 견적 줄 잠김` · 진행 + 연결 문서 + [수량] → `지출결의 {번호} 연결됨 · 고치려면 새 차수`
    - 정규화 비교: DB `fx_rate "1350.0000"` · `quantity "2.00"` · `note null` 대 입력 `1350` · `2` · `undefined` → 바뀐 칸 없음(A-21)
  </behavior>
  <action>
① **`domain/quotes/edit-scope.ts`(순수 — 리포지토리 import 없음).** 셀 단계 유니온 `QuoteCellEditability = "edit" | "readonly" | "locked"`를 **이 파일에서 선언**한다(A-32 — `ui/table/types.ts`의 `CellEditability`와 구조가 같아 화면에 그대로 넘어간다). `QUOTE_LINE_STATUSES = ["not_started", "cancelled"] as const`와 타입 `QuoteLineStatus`(엔지 리뷰 A P3 — 지금 목록은 `quote-table.tsx:52-54` 화면 라벨 표뿐이다; 액션 enum·금액 계산·04-30의 라벨 표가 이 상수를 쓴다) · `QUOTE_LINE_FIELDS = ["subcategory","itemName","vendorId","quantity","unitPrice","execution","lineStatus","note"] as const` · `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT = ["quantity","unitPrice","lineStatus"] as const` · `lineCellEditability({ status, canWrite, hasLinkedDocuments, isNewLine })` → 칸별 단계(UI-SPEC rev 5 「S4」 셀 단계 표 + 사용자 D10·D12 새 줄 행). 잠긴 칸의 이유 문자열도 이 파일이 만든다 — `quoteLockReason({ status })`(정산 `정산 · 실행가와 새 줄만` · 완료 `완료 · 견적 줄 잠김` · 그 밖 null — rev 5 Copywriting `Error — 셀(잠김) = 표 위 한 줄`, DR-2: 표 위 한 줄(04-30 `tableLockLine`)과 게이트 거부 이유가 이 함수 하나를 부른다)과 `linkedDocumentReason(number)`(`지출결의 {번호} 연결됨 · 고치려면 새 차수` — `Error — 셀(읽기 전용, D-66)`). 입력은 객체 하나 — 04-13(줄 종류)·04-14(승인)가 필드를 더해도 호출자가 바뀌지 않는다. 상태 값 비교는 이 파일과 게이트 규칙 안에만 둔다. 결정표 단위 테스트(`behavior` 앞 여섯 줄)를 먼저 쓴다(RED).

② **게이트 확장.** `project.line-edit`의 ctx를 `{ status, hasLinkedDocuments, change: { kind: "update"; fields: QuoteLineField[] } | { kind: "insert"; quoteCellsZero: boolean } | { kind: "archive" } | { kind: "reorder" } | { kind: "duplicate" } | { kind: "restore"; quoteAmountZero: boolean } }`로 넓히고, `update`는 바뀐 칸마다 `lineCellEditability`가 `edit`인지 본다 — 잠김이면 `quoteLockReason({ status })`의 반환 문자열(정산 `정산 · 실행가와 새 줄만` · 완료 `완료 · 견적 줄 잠김` — 게이트 규칙 안에 문자열 리터럴을 두지 않는다), 읽기 전용이면 `linkedDocumentReason(그 줄 연결 문서의 첫 번호)`. 구조 변경 판정은 Task 2가 채운다 — 이 태스크에서는 완료만 거부하고 나머지는 통과(04-06 동작 유지).

③ **저장 세 단계(ENG-D3 ① — `<probe_fallback>`).** `domain/quotes/lines.ts`의 `saveQuoteLines`를 `prepareQuoteLineSave` · `writeQuoteLinesInTx` · `finishQuoteLineSave`로 나누고 `saveQuoteLines`는 셋을 차례로 부른다. **트랜잭션 안 단계에서 풀을 부르지 않는다** — 차수 행은 준비 단계가 읽어 넘기고(`repositories/quote-revisions.ts`는 고치지 않는다), 줄 조회·쓰기·목록은 전부 `tx`(`listQuoteLinesByRevision`·`findQuoteLineById`에 선택 `tx`를 더한다), `recordAction(viewer, { actionType: "document_update", entity: QUOTE_LINE_ENTITY, entityId: revisionId, detail: { lineIds, archivedLineIds } }, { tx })`(엔지 리뷰 A §1 P2 — 유령 로그), 거부는 `denyWrite`(04-20). `domain/projects/ledger.ts`는 04-22가 고정한 순서 그대로 — 견적 줄이 있으면 트랜잭션 **전에** `prepareQuoteLineSave`, 콜백 안 (f) 자리에서 `writeQuoteLinesInTx`, 커밋 **뒤** `finishQuoteLineSave`와 `fxToRemember`의 `rememberFxRate`(`<probe_fallback>` 커밋 뒤 기억 규약 — 매출 쪽 목록이 생기면 같은 자리에서 합친다). `listQuoteLines`와 `finishQuoteLineSave`는 `projectMany`(04-32)로 투영한다(ENG-D3 ②).

④ **트랜잭션 안 판정 순서(A-06·OV-3·A-21·A-03·B-01).** (a) 차수의 프로젝트 id로 `loadProjectForGate(viewer, projectId, { now, tx, afterLock })`를 불러 잠근 행을 얻는다 — 모든 프로젝트 게이트를 이 행으로 판정한다(합성 저장에서는 04-22의 재판정 뒤라 **새 행** — ENG-D6). `deps.now`·`deps.afterLock`을 받는다(`afterLock`은 테스트 전용 주입 지점). (b) `repositories/quote-lines.ts`의 `findQuoteLinesByIds(viewer, ids, { revisionId }, tx)`로 `revision_id = $revisionId`로 좁히고, `updateQuoteLineIfVersionMatches`의 WHERE에도 `revision_id`를 더한다 — 찾은 줄 수가 실린 기존 줄 수와 다르면(다른 차수·프로젝트 줄 섞임) 아무것도 쓰지 않고 `denyWrite(…, "quote.line-membership", …)`로 전부 거부한다(B-01 줄 소속). (c) 기존 줄마다 **쓰기 페이로드를 먼저 만들고**(지금 쓰기 직전에 부르는 정규화 — 수량 고정 소수·환율 문자열·빈 비고 null — 을 판정 앞으로 옮긴다) 그 페이로드 칸과 DB 현재 행 칸을 같은 표현끼리 비교해 바뀐 칸 목록을 만든다(A-21 — 금액은 통화·외화 금액·환율 셋 중 하나라도 다르면 그 칸이 바뀐 것). (d) **순서 보존(A-03):** 이 태스크는 `order`가 없는 저장만 다룬다 — 기존 줄의 `sort_order`는 입력과 무관하게 **현재 값 그대로** 쓴다(지금의 `?? index` 제거 — 저장마다 순서가 재설정되는 선행 버그). `listQuoteLinesByRevision`의 정렬에 줄 id를 둘째 키로 더한다. 새 줄·`order`는 Task 2. (e) 줄마다 게이트를 부르고, 거부된 칸은 기존 형식 오류와 같은 `CellFormatError`(줄·칸·이유)로 모아 **하나라도 있으면 아무것도 쓰지 않고** `SaveRejectedError`로 전부 거부한다(04-04 규약). **거부 지점은 하나다(B-26 · 사용자 D19-3 · 엔지 리뷰 B):** 게이트·소속 판정으로 배치를 거부하기 직전 `denyWrite(viewer, rule, { projectId, revisionId, lineIds }, err)`를 한 번 부른다(금액 키 없음 — 행동 로그에는 남기지 않는다). 형식 오류(숫자 아님 등)·버전 충돌은 권한 거부가 아니라 이 로그를 남기지 않는다. 뒤 플랜의 게이트(04-26 `quote.line-cap` · 그룹 B의 조정·차수·승인)는 같은 지점을 지나므로 따로 로그 호출을 더하지 않는다. 바뀐 칸이 없는 줄은 게이트를 부르지 않는다. 연결 문서 여부는 Task 2의 조회 지점이 생기기 전까지 `false`. (f) 쓰기 뒤 같은 tx에서 차수의 활성 줄 전체(`listQuoteLinesByRevision(…, tx)`)를 읽어 결과로 넘긴다(엔지 리뷰 A §2 P2 — 화면이 저장한 줄만 받던 선행 문제). 낡은 머리 주석(`lines.ts:348-353`의 옛 단계 순서)을 이 순서로 고친다.

⑤ **DTO.** `QuoteLineDto`에 `cellEditability: Record<QuoteLineField, QuoteCellEditability>`를 더한다 — `listQuoteLines(viewer, revisionId, ctx)`가 프로젝트 상태·쓰기 권한을 받아 줄마다 `lineCellEditability`로 계산해 싣는다(정보 항목 `project.value`, `registerDto` 갱신 — 누수 스캔). 화면이 이 값을 읽는 배선은 04-30이다.

⑥ **통합(`test/integration/quote-lines.test.ts`).** describe `정산 편집 매트릭스(D10·D12)` 첫 케이스들: (a) 정산 프로젝트에서 실행가만 바꾼 저장 통과 · DB 재조회로 그 줄의 `sort_order` 무변경(A-03), (b) 단가를 같이 바꾼 조작 페이로드 전부 거부(DB 재조회로 실행가도 안 바뀜) · `log.warn` 스파이로 `write.denied` 한 번, 그 필드에 금액 키 없음(B-26), (c) 완료에서 비고만 바꾼 저장 거부, (d) 정산에서 환율 `1350`·수량 `2`·비고 undefined를 실어 실행가만 바꾼 저장 통과(A-21 헛거부 없음), (e) 진행 회귀 — 3줄 중 3번째 줄만 고쳐 저장 → 세 줄 순서 그대로(A-03) **그리고 저장 결과 `lines`가 3줄**(표시 순서, 엔지 리뷰 A §2 P2), (f) 다른 프로젝트 줄 id를 섞은 배치 전부 거부 · 두 프로젝트 무변경(B-01).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/rules-gate.test.ts test/unit/domain/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/quote-lines-conflict.test.ts test/integration/leak-scan.test.ts test/integration/project-period.test.ts test/integration/tx-safety.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 정산의 조작 페이로드가 통과했거나, 정규화 차이로 헛거부됐거나, 순서가 재설정됐거나, 결과가 저장한 줄만 담았거나, 다른 차수 줄이 섞였는데 저장됐거나, 기존 충돌 판정·04-22 합성 저장(판정 순서·풀 2 동시 기간 저장)이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(boundaries 포함 — domain이 `ui`를 import하면 여기서 잡힌다)·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `edit-scope.ts`가 리포지토리·`ui`를 import하지 않고 셀 단계 유니온과 `QUOTE_LINE_STATUSES`를 직접 선언하며, 게이트 규칙과 `listQuoteLines`가 같은 `lineCellEditability`를 부른다
    - 결정표 단위 테스트가 `behavior` 여덟 줄을 덮는다(`quoteLockReason`·`linkedDocumentReason` 포함)
    - 잠김 이유 문자열 `정산 · 실행가와 새 줄만`·`완료 · 견적 줄 잠김`이 `domain/`에서 `edit-scope.ts` 한 파일에만 리터럴로 있다(`grep -rln "실행가와 새 줄만" domain` → `domain/quotes/edit-scope.ts` 하나 — DR-2 한 함수)
    - `writeQuoteLinesInTx` 본문의 리포지토리 호출이 전부 `tx`를 받고, 본문에 `can(`·`quoteLineCustomFieldsSchema(`·`rememberFxRate(`·`project(`·`projectMany(` 호출이 없다(Grep — 트랜잭션 전·뒤 단계에만 있다)
    - `writeQuoteLinesInTx`의 첫 프로젝트 판정이 `loadProjectForGate`이고, 바뀐 칸 판정이 DB 현재 값 대 정규화된 쓰기 페이로드다(baseline을 판정 입력으로 쓰지 않는다)
    - `lines.ts`에 `sortOrder: input.sortOrder ?? index` 꼴(배치 안 위치로 순서를 쓰는 식)이 없고, `listQuoteLinesByRevision`의 정렬이 `sort_order`, 줄 id 두 키다
    - `findQuoteLinesByIds`·`updateQuoteLineIfVersionMatches`가 `revision_id` 조건을 가진다
    - `saveQuoteLines`의 권한·규칙 거부가 `denyWrite` 한 호출 지점을 지나고 통합 (b)가 그 경고와 금액 키 부재를 단언한다(B-26) · `lines.ts`에 `log.warn("write.denied"` 직접 호출이 없다
    - 합성 저장이 트랜잭션 전 `prepareQuoteLineSave` · 콜백 안 `writeQuoteLinesInTx` · 커밋 뒤 `finishQuoteLineSave` 순이다
    - 통합 (a)~(f)가 DB 재조회로 단언하고 초록이다 · (e)가 결과 `lines` 3줄을 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산 PM의 실행가 한 칸 편집이 판정 함수·게이트·세 단계 저장·잠금·정규화 비교·순서 보존·줄 소속·쓰기·로그 tx·전체 목록 결과·DTO를 한 번에 관통하고, 나머지 칸은 서버에서 잠겼다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 정산 구조 매트릭스(새 줄 단가 0·수량 1 · 삭제·이동·복제·새 차수 거부) · 새 줄 멱등 삽입(ENG-D10) · 순서 `order`·append · 줄 보관과 합계 제외 · 취소(견적가 0) · 연결 문서 조회 지점 · 입력 검증</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, test/unit/domain/quote-lines.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, repositories/archive.ts, repositories/projects.ts, domain/projects/ledger.ts, app/(app)/projects/actions.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `repositories/archive.ts`(전문) — `ArchivableEntry`·`ARCHIVABLE_TABLES` 등록 형태(보관함 화면이 이 목록 하나로 복원한다)
    - `repositories/quote-lines.ts`(Task 1 이후 — `listQuoteLinesByRevision`·`insertQuoteLine`)
    - `repositories/projects.ts` 20–38행(`lineSumsSubquery` — 보관 줄 포함, 24–26행 낡은 주석 「상태와 무관하게 전 줄을 더한다」) · 150–200행(목록·합계가 그 하위 질의를 쓰는 자리)
    - `domain/quotes/lines.ts`의 `computeQuoteLineAmounts`(323–340행) · 입력 검증(395–403행 수량 > 0 — **느슨하게 하지 않는다**) · Task 1이 바꾼 판정 자리
    - `app/(app)/projects/actions.ts` 55–100행(`saveProjectLedgerAction` 스키마 — `projectId: z.string().min(1)` · `lineStatus: z.string()` · 줄 `id` 선택)
  </read_first>
  <behavior>
    - `structuralEditability({ status, canWrite })`: 수주중·진행·미수주 + 쓰기 → `{ insert, archive, reorder, duplicate, newRevision }` 전부 참 · 정산 + 쓰기 → `insert`만 참(D10) · 완료 → 전부 거짓 · 쓰기 없음 → 전부 거짓
    - `quoteCellsZero`: 원화 단가 0 · 수량 없음 → 참 · 원화 단가 0 · 수량 1 → 참 · 원화 단가 0 · 수량 2 → 거짓 · 단가 1원 → 거짓 · USD 단가 0 → 거짓(엔지 리뷰 A §2 P1)
    - 게이트: 정산 + `insert`(`quoteCellsZero` 참) → 통과 · 정산 + `insert`(거짓) → `정산 · 새 줄은 실행가만` · 정산 + `archive`/`reorder`/`duplicate` → `정산 · 줄 삭제·이동 없음` · 완료 → `완료 · 견적 줄 잠김`
    - 순서 판정(순수 `orderChange(current, order, { archivedIds, newIds })`): 집합이 다르면 `mismatch` · 기존 줄 상대 순서 같음 + 새 줄이 중간 → `insertOnly` · 기존 줄 상대 순서 바뀜 → `reorder`
    - 연결 문서 있는 줄의 `archive` → `연결 문서 있음 · 삭제 대신 취소`(취소 상태로만)
    - `computeQuoteLineAmounts`에 상태 `cancelled`가 오면 견적가 0, 차익 = −실행가
  </behavior>
  <action>
① `edit-scope.ts`에 `structuralEditability({ status, canWrite })` → `{ insert, archive, reorder, duplicate, newRevision }`(사용자 D10 — 정산은 `insert`만) · `quoteCellsZero({ quantity, unitPrice })`(`<probe_fallback>` 정의) · 순수 순서 판정 `orderChange`를 더한다. `newRevision`은 차수를 만드는 04-14(그룹 B)가 새 차수 게이트의 입력으로 쓴다. 결정표를 먼저 쓴다(RED).

② **게이트 구조 판정.** `project.line-edit`의 `insert`·`archive`·`reorder`·`duplicate`를 `structuralEditability`로 판정하고, 정산의 `insert`는 `quoteCellsZero`가 참일 때만 통과시킨다(사용자 D12 — 아니면 `정산 · 새 줄은 실행가만`). 연결 문서가 있는 줄의 `archive`는 `연결 문서 있음 · 삭제 대신 취소`로 거부한다. `writeQuoteLinesInTx`가 `isNew` 줄을 `insert`로, 입력에 복제 표시(`duplicatedFrom?: string`)가 있으면 `duplicate`로, `orderChange`가 `reorder`면 `reorder`로 게이트에 싣는다. 구조 거부는 칸 오류가 아니라 `GateBlockedError`(게이트 이유 그대로)로 전체 거부한다(`denyWrite` 한 지점).

③ **새 줄 멱등 삽입(ENG-D10).** `QuoteLineWriteRow`가 `id: string`(필수)과 `isNew?: true`를 갖는다. `repositories/quote-lines.ts`의 삽입을 `insertQuoteLineIfAbsent(viewer, row, tx)`(`ON CONFLICT (id) DO NOTHING RETURNING`)로 바꾸고, 돌아온 행이 없으면 같은 tx에서 그 id를 다시 읽어 `<probe_fallback>`의 세 갈래(같은 값 → no-op · 다른 값 → `quote.line-replay` 거부 · 다른 차수/보관 → 소속 거부)로 판정한다. 값 비교는 Task 1 ④(c)의 정규화 비교 함수를 그대로 쓴다. 04-26의 줄 수 상한은 이 판정 뒤의 **실제로 새로 들어갈 줄 수**를 센다(그 플랜이 쓴다 — 주석 한 줄).

④ **순서(`order`·append — 엔지 리뷰 A §2 P2).** 입력에 `order?: string[]`을 더하고(`SaveProjectLedgerInput.quoteLines`에도 같은 필드 — `ledger.ts`는 그대로 넘긴다) `<probe_fallback>` 순서 규칙 (1)~(3)을 구현한다 — `order`가 없으면 새 줄은 `max(sort_order) + 1`부터, 있으면 집합 검사(`mismatch` → `줄 순서가 맞지 않음 · 새로 고침` 전부 거부) 뒤 `repositories/quote-lines.ts`의 `setQuoteLineSortOrders(viewer, revisionId, pairs, tx)`(값이 바뀐 줄만, `version` 올리지 않음)로 다시 쓴다.

⑤ **줄 보관과 합계 제외(A-04).** `repositories/archive.ts`의 `ARCHIVABLE_TABLES`에 `quote_line` 항목(`quote_lines` 표, 표시 이름 `견적 줄`, 이름 칸 `item_name`)을 더해 보관함 화면에 나오게 한다(복원 경로는 Task 3이 도메인 함수로 돌린다). `repositories/quote-lines.ts`에 `archiveQuoteLines(viewer, { ids, revisionId, archivedBy }, tx)`(그 차수 소속이고 아직 보관되지 않은 줄만, 반환 행 수 확인)를 더하고, `listQuoteLinesByRevision`에 `archived_at IS NULL`을, `findQuoteLinesByIds`의 결과에 보관된 줄이 있으면 `보관된 줄 · 새로 고침`로 전부 거부하게 한다. `repositories/projects.ts`의 `lineSumsSubquery`(목록·합계의 견적·실행가 합)에 `archived_at IS NULL`을 더하고 24–26행의 낡은 주석(「상태와 무관하게 전 줄을 더한다」)을 「보관된 줄은 빼고 취소 줄은 견적가 0으로 더한다」로 고친다(엔지 리뷰 A P3 — 04-17이 같은 하위 질의의 돈 합계 캐스트와 현재 차수 한정을 고친다). 입력에 `archivedLineIds?: string[]`를 더하고(`SaveProjectLedgerInput.quoteLines`에도), 판정 단계에서 각 id에 `archive` 게이트를 부른 뒤 쓰기 단계에서 **같은 트랜잭션**으로 보관한다 — 성공한 저장의 행동 로그 detail에 보관한 줄 id를 싣는다(04-04의 「성공 한 줄 · 거부 0줄」 유지).

⑥ **취소(견적가 0)와 입력 검증(A-37 · 엔지 리뷰 A P3).** `computeQuoteLineAmounts`가 `lineStatus`를 받아 `cancelled`면 견적가 0(수량·단가 컬럼은 그대로 — 이력 유지)으로 계산한다(PROJ-02). 기존 단위 테스트(`test/unit/domain/quote-lines.test.ts`)에 한 줄 더한다. `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마: `projectId`·줄 `id`를 `z.string().uuid()`로, 두 `lineStatus`를 `z.enum(QUOTE_LINE_STATUSES)`로 좁히고, `isNew`(`z.literal(true).optional()` — 없으면 `version` 필수), `quoteLines.archivedLineIds`(`z.array(z.string().uuid())`), `quoteLines.order`(`z.array(z.string().uuid())` 선택)를 더한다 — 줄 상태 목록은 `QUOTE_LINE_STATUSES`에서 가져온다(새 목록을 만들지 않는다).

⑦ **연결 문서 조회 지점(D-66).** `domain/quotes/lines.ts`에 `linkedDocumentsByLine(viewer, revisionId, tx?): Promise<Map<string, { number: string }[]>>` 한 함수를 두고 이 페이즈에서는 빈 Map을 돌려준다(Phase 5가 이 함수만 채운다 — 주석에 한 줄, 트랜잭션 안에서 불리므로 `tx`를 받는다). `listQuoteLines`·`writeQuoteLinesInTx`가 이 함수의 결과로 `hasLinkedDocuments`를 정한다. DTO에 `hasLinkedDocuments`와 `readonlyReason`(연결 문서가 있으면 `linkedDocumentReason(첫 번호)`, 없으면 null — 04-30이 읽기 전용 셀 편집 시도의 이유 줄로 그대로 쓴다, DR-35)을 싣는다(`registerDto` 갱신 — 누수 스캔).

⑧ **통합(`test/integration/quote-lines.test.ts`).** describe `정산 편집 매트릭스(D10·D12)` 계속: (g) 정산에서 단가 원화 0 · 수량 없음인 새 줄(실행가 300,000)을 끼운 배치 통과 · DB에 수량 1 · 견적가 0 · 실행가 300,000, (h) 정산에서 단가 1,000원인 새 줄 배치 · 수량 2인 새 줄 배치 각각 전부 거부(DB 무변경), (i) 정산에서 보관 거부 · 기존 줄 상대 순서를 바꾼 `order` 거부 · 기존 순서를 지키고 새 줄을 가운데 끼운 `order`는 통과, (j) 완료에서 새 줄·보관 거부. describe `순서·재전송(엔지 리뷰 A · ENG-D10)`: (u) 진행에서 한 줄을 가운데에 복제(`order` 실음)하고 다른 줄을 보관한 저장 뒤 `listQuoteLines`를 두 번 읽으면 둘 다 화면 순서와 같다(공백 3), (u2) 보관 뒤 `order` 없이 새 줄을 더하면 그 줄이 마지막이다, (u3) `order`가 활성 줄 집합과 다르면 `줄 순서가 맞지 않음 · 새로 고침`로 전부 거부, (w) 새 줄 둘을 실은 배치를 저장한 뒤 **같은 배치(같은 id)를 다시 보내면** 둘째도 성공이고 그 차수의 줄이 두 개만 늘었다(응답 유실 재전송 — ENG-D10), (w2) 다른 프로젝트 차수에 이미 있는 줄 id를 `isNew`로 실은 배치는 전부 거부(소속), (w3) 같은 id를 **다른 실행가로** 다시 보낸 배치는 `이미 저장된 줄과 값이 다름 · 새로 고침`로 전부 거부되고 DB 값이 첫 저장 그대로다(재전송 규칙 — 그룹 B와 같음). describe `보관·취소(D-56·A-04)`: (k) 수주중에서 보관 뒤 DB에 행이 남고 `archived_at`이 채워지며 보관함 목록에 나온다 · `listQuoteLines`에 없다 · `listProjects`·`aggregateProjects`의 실행가·견적 합에서 빠진다, (l) 보관된 줄 id로 고치는 배치 거부, (m) 거부된 배치에서 보관도 안 됐다, (n) 취소 줄의 견적가 0, (o) `lineStatus: "아무글자"`·`projectId: "abc"`·줄 `id: "x"` 액션 입력이 검증 오류(액션 스키마 단위 — `safeParse`).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/quote-lines.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 구조 판정·`quoteCellsZero`·순서 판정·취소 견적가·게이트 표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/archive.test.ts test/integration/leak-scan.test.ts test/integration/projects-list.test.ts test/integration/project-period.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 정산 매트릭스가 사용자 D10·D12(단가 0·수량 1)와 다르거나, 재전송이 줄을 두 번 넣었거나, 순서가 두 번 읽기에서 흔들렸거나, 보관이 트랜잭션 밖에서 일어났거나, 보관 줄이 합계에 남았거나, 보관함 등록이 빠졌거나, DTO 등록이 누수 스캔을 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 여섯 줄을 덮고, `structuralEditability`의 정산 행이 `insert`만 참이며 `quoteCellsZero`가 수량 1·없음만 참이다
    - `lines.ts`의 수량 > 0 검증이 그대로다 — `grep -c "quantity <= 0" domain/quotes/lines.ts`가 1 이상이고 그 오류 문구 `0보다 큰 수를 적어 주세요`가 남아 있다
    - 정산 새 줄 허용(g)·견적 칸 거부 둘(h)·보관·순서 거부와 새 줄 가운데 끼우기 통과(i)가 DB 재조회로 초록이다
    - 삽입이 `ON CONFLICT (id) DO NOTHING`이고 같은 값 재전송 no-op(w)·소속(w2)·다른 값 재전송 거부(w3)가 초록이다(ENG-D10)
    - `writeQuoteLinesInTx`가 `rememberFxRate`를 부르지 않고 `fxToRemember`를 돌려주며, 단독 `saveQuoteLines`와 `saveProjectLedger`가 커밋 뒤에 기억한다(Grep)
    - 순서 (u)·(u2)·(u3)이 초록이고, 순서만 바뀐 줄의 `version`이 그대로다
    - 보관이 같은 트랜잭션이고 거부된 배치에서 보관도 없다(통합 m)
    - `lineSumsSubquery`와 `listQuoteLinesByRevision`에 `archived_at IS NULL` 조건이 있고 (k)가 목록·합계 제외를 단언하며, `repositories/projects.ts` 24–26행 주석이 새 규칙을 말한다
    - `ARCHIVABLE_TABLES`에 `quote_line`이 있고 보관한 줄이 보관함 목록에 나온다
    - 액션 스키마의 `projectId`·줄 `id`가 uuid, `lineStatus`가 `QUOTE_LINE_STATUSES` enum이다(A-37)
    - `linkedDocumentsByLine`가 한 함수뿐이고 이 페이즈에서 빈 결과를 돌려준다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산에서 새 줄(단가 0 · 수량 1)만 더해지고 구조 변경은 거부되며, 새 줄 재전송이 중복을 만들지 않고, 순서가 두 번 읽어도 같으며, 줄 삭제가 같은 트랜잭션의 보관으로 저장되고 보관 줄은 목록 합계에서 빠지며, 취소 줄의 견적가가 0이고 입력이 정해진 값만 받는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 보관함 복원의 도메인 검사(같은 트랜잭션) · 저장 대 전환 결정적 경합 · 유령 로그 없음(견적 줄·매출 tx) · 합성 저장 판정 순서의 결과(ENG-D6) · 풀 2 동시 견적 줄 저장 셋</name>
  <files>domain/archive/index.ts, repositories/archive.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, domain/rules/register.ts, domain/revenue/index.ts, test/integration/quote-lines.test.ts, test/integration/tx-safety.test.ts</files>
  <read_first>
    - `domain/archive/index.ts` 40–90행(`archive`·`restore` — 지금 복원은 `setArchived` 한 번 + 로그, 트랜잭션·도메인 검사 없음)
    - `repositories/archive.ts` 40–56행(`ArchivableEntry` — `setArchived(viewer, id, value)`에 tx 인자 없음)
    - `domain/revenue/index.ts` 320–350행(`saveRevenue`의 `if (tx) { … await runSave(tx); await recordAction(…` — tx 없이 로그) — 이 플랜은 `recordAction`에 `tx`를 넘기는 한 곳만 고친다
    - `test/integration/lock-race.ts`(04-20) · `test/integration/project-status.test.ts`의 경합 describe(04-20 — `afterLock`에 `deferred`를 거는 형태)
    - `test/integration/tx-safety.test.ts`(04-32·04-20·04-22 — 풀 크기 2 모듈 격리와 describe 형태)
    - `test/integration/archive.test.ts` — 보관함 복원 통합 테스트 형태 · `test/integration/revenue-entries.test.ts` — 게이트·검증에 걸리는 매출 줄을 만드는 법
  </read_first>
  <behavior>
    - 보관함에서 완료 프로젝트의 견적 줄 복원 → `완료 · 견적 줄 잠김`으로 거부, 줄은 보관 그대로(DB 재조회)
    - 정산 프로젝트의 견적가 ≠ 0 줄 복원 → `정산 · 새 줄은 실행가만`으로 거부 · 견적가 0 줄 복원 → 통과
    - 진행 프로젝트 줄 복원 → 통과, 복원 로그 한 줄이 같은 트랜잭션
    - 경합(두 순서): 저장이 잠금 뒤 멈춘 동안 정산→완료 전환은 기다린다 → 저장 커밋(정산 규칙) 뒤 완료 · 전환이 잠금 뒤 멈춘 동안 저장은 기다린다 → 완료 커밋 뒤 저장은 `완료 · 견적 줄 잠김`으로 거부 — 두 경우 모두 「완료 프로젝트에 편집이 들어감」이 없다
    - 유령 로그(t): 유효한 견적 줄 변경 + 검증·게이트에 걸리는 매출 줄을 한 합성 저장으로 보내면 전부 거부되고, 그 차수의 `quote_line` `document_update` 로그 수와 매출 쪽 로그 수가 저장 전과 같다
    - 판정 순서의 결과(v · ENG-D6): 시스템 관리자가 정산 프로젝트의 종료일을 오늘 이후로 늦추며 같은 저장에서 단가를 고치면 통과(진행 규칙) · 진행 프로젝트의 종료일을 어제로 앞당기며 단가를 고치면 `정산 · 실행가와 새 줄만`으로 전부 거부되고 기간·상태·단가 모두 무변경
    - 풀 2 동시 견적 줄 저장 셋: 같은 프로젝트에 새 줄 하나씩(서로 다른 uuid) 싣는 합성 저장 셋을 풀 크기 2에서 동시에 보내면 10초 안에 셋 다 성공하고 줄이 셋 늘었다
  </behavior>
  <action>
① **도메인 복원(A-19·OV-2).** `domain/quotes/lines.ts`에 `restoreQuoteLine(viewer, id, deps?: { now?; recordAction? })`를 둔다 — 트랜잭션 전에 권한(`can`)과 줄(보관된 것 포함)·그 차수의 프로젝트 id를 읽고, 한 `withTransaction` 안에서 그 프로젝트를 `loadProjectForGate(…, { tx })`로 잠근 뒤 줄을 `tx`로 다시 읽어(아직 보관 상태 · 같은 차수인지 확인) 판정 → `project.line-edit`에 `{ kind: "restore", quoteAmountZero }`(정산은 견적가 0일 때만, 완료는 거부 — 복원은 그 상태에서 줄을 더하는 것과 같다) → `repositories/quote-lines.ts`에 더하는 `restoreQuoteLineRow(viewer, id, tx)`(보관 해제, 반환 행 수 확인) → `recordAction(viewer, { actionType: "restore", entity: "quote_line", entityId }, { tx })`. 게이트 거부면 `denyWrite`(규칙 · 줄 id, 금액 없음)로 던진다(B-26). 게이트 ctx가 객체라 줄 수 상한(04-26)·승인 합계(04-14, 그룹 B)가 같은 함수에 검사를 더한다. `domain/archive/index.ts`의 `restore`는 엔티티별 도메인 복원 함수 표 `DOMAIN_RESTORERS: Partial<Record<string, (viewer, id, deps) => Promise<void>>>`를 먼저 보고, 있으면(`quote_line` → `restoreQuoteLine`) **통째로 맡긴다** — 범용 `setArchived` 경로는 표에 없는 엔티티만 탄다(리저브 복원도 같은 표에 04-07이 더한다 — 그룹 B). `repositories/archive.ts`의 `quote_line` 항목에는 범용 복원이 타지 않는다는 주석 한 줄을 둔다.

② **유령 로그 없음(엔지 리뷰 A §1 P2).** `domain/revenue/index.ts`의 `saveRevenue`가 `tx`를 받았을 때 `recordAction(…, { tx })`로 넘기는 **한 곳**을 고친다(견적 줄은 Task 1이 이미 `tx`). 매출 저장의 다른 구조는 그대로 둔다(그룹 B 영역 — `<probe_fallback>`).

③ **경합 테스트(A-06·OV-3, `test/integration/quote-lines.test.ts` describe `잠금·경합`).** `saveQuoteLines`의 `deps.afterLock`에 `deferred`를 걸어 멈춘 연결 A(정산 프로젝트의 실행가 저장)와 연결 B(`changeProjectStatus` 정산→완료)로: (p) A 먼저 — B가 `waitForLockWaiter`로 대기에 든 것을 확인 → A를 풀면 A 커밋(실행가 반영) → B 커밋(완료) · (q) B 먼저(`changeProjectStatus`의 `afterLock`에서 멈춤) — A가 대기에 든 것을 확인 → B를 풀면 완료 커밋 → A는 `완료 · 견적 줄 잠김`으로 거부, 실행가 무변경. 두 순서 모두 최종 DB에 「완료 상태에서 바뀐 줄」이 없다. (r) 복원 대 전환: 같은 두 순서로 `restoreQuoteLine`과 정산→완료 — 완료 뒤에 복원이 들어가지 않는다. (s) 보관함 복원 세 케이스(`behavior` 앞 세 줄, DB 재조회).

④ **유령 로그·판정 순서 통합.** 같은 파일 describe `합성 저장`에 (t)(`behavior` 유령 로그 줄 — 엔지 리뷰 A 공백 3)와 (v)(`behavior` 판정 순서 줄 — ENG-D6, 기간 권리 `lead`와 `projects` 쓰기를 둘 다 가진 시스템 관리자 viewer, 날짜는 `now` 주입)를 더한다. 두 케이스의 `saveProjectLedger` 호출은 04-22 계약대로 `seenStatus`(준비한 프로젝트의 **저장 전** 상태)를 싣는다 — (v)의 앞당기기 거부는 04-22의 상태 바뀜 거부가 아니라 이 플랜의 셀 게이트(`정산 · 실행가와 새 줄만`)에서 나야 한다(거부 문자열로 구분해 단언).

⑤ **풀 2 동시 저장(ENG-D3 ①).** `test/integration/tx-safety.test.ts`의 `풀 2 · 동시 합성 저장 셋` describe에 「셋 다 성공」 케이스를 더한다(`behavior` 마지막 줄) — 04-32가 「셋 다 끝난다(성공 또는 UserFacing)」로 남겨 둔 자리다. 시간 초과가 나면 `writeQuoteLinesInTx` 안에 남은 풀 호출을 찾아 트랜잭션 앞뒤로 옮긴다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 복원 갈래의 게이트 판정이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/archive.test.ts test/integration/project-status.test.ts test/integration/project-period.test.ts test/integration/revenue-entries.test.ts test/integration/tx-safety.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 복원이 도메인 검사를 우회했거나, 경합 순서가 결정적이지 않거나(고정 지연), 완료 프로젝트에 편집·복원이 들어갔거나, 거부된 합성 저장이 로그를 남겼거나, 기간을 앞당긴 저장의 줄이 옛 상태로 판정됐거나, 풀 2 동시 견적 줄 저장이 시간 초과됐거나, 다른 엔티티의 보관함 복원·매출 저장이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/archive/index.ts`의 `restore`가 `DOMAIN_RESTORERS`에 있는 엔티티를 도메인 함수에 맡기고, `restoreQuoteLine`이 잠금·게이트·보관 해제·로그를 한 `withTransaction`에서 한다(트랜잭션 안 풀 호출 없음)
    - 보관함 복원 세 케이스(완료 거부 · 정산 견적가 조건 · 진행 통과)가 DB 재조회로 초록이고, 기존 `archive.test.ts`(다른 엔티티)가 수정 없이 초록이다
    - 경합 (p)(q)(r)이 `deferred`·`waitForLockWaiter`로 순서를 정하고 `setTimeout` 고정 지연이 없다
    - `saveRevenue`의 `recordAction`이 받은 `tx`를 넘기고 (t)가 로그 수 무변경을 단언한다(금지 항목 셋째)
    - (v)가 두 방향(늦추기 통과 · 앞당기기 거부 + 기간 무변경)을 단언한다(ENG-D6)
    - `tx-safety.test.ts`에 풀 2 동시 견적 줄 저장 「셋 다 성공」 케이스가 있고 초록이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>보관함 복원이 저장과 같은 규칙을 한 트랜잭션에서 지나고, 저장·복원·전환이 프로젝트 행 잠금으로 직렬화되어 완료 프로젝트에 어떤 변경도 성공으로 들어가지 않으며, 거부된 합성 저장이 로그를 남기지 않고, 기간과 줄을 함께 저장하면 저장 뒤 상태로 판정되며, 동시 저장이 풀을 굳히지 않음이 결정적 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장 | 줄 값·새 줄(화면 uuid)·보관 id·순서(`order`)·줄 상태·차수 id가 넘어온다. 셀 단계·구조·소속·순서 판정이 서버에만 있어야 한다 |
| 보관함 화면 → 복원 | 범용 복원 경로가 도메인 잠금을 모른다 |
| 동시 요청 → 프로젝트 행 · 커넥션 풀 | 저장·전환·복원이 같은 프로젝트를 동시에 바꾼다. 잠근 트랜잭션이 풀을 한 번 더 잡으면 요청이 굳는다 |
| 연결 문서 조회 → 금액 칸 | Phase 5가 채울 조회 결과가 금액 칸의 읽기 전용 판정을 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-58 | Elevation of Privilege | 정산·완료의 잠긴 칸 | high | mitigate | 서버가 DB 현재 값과 정규화된 쓰기 페이로드를 비교해 바뀐 칸을 정하고 `project.line-edit`가 칸마다 판정한다 — 클라이언트 baseline·화면 셀 단계를 믿지 않는다(A-21). 조작 페이로드 거부를 통합 테스트로 고정한다 |
| T-04-59 | Tampering | 보관 id·줄 id 목록·새 줄 id | high | mitigate | 찾기·고치기·보관이 모두 `revision_id` 조건이고 결과 행 수를 확인해 다르면 전부 거부한다(B-01 줄 소속). 보관된 줄 id 수정도 거부한다(A-04). 화면 uuid로 들어온 새 줄이 이미 다른 차수에 있으면 삽입하지 않고 전부 거부한다(통합 w2) |
| T-04-61 | Repudiation | 줄 삭제·복원 | medium | mitigate | 물리 삭제 없이 보관(`archived_at`·`archived_by`)하고 저장 로그 detail에 보관 id를 남긴다. 복원 로그는 복원과 같은 트랜잭션이다 |
| T-04-62 | Tampering | 연결 문서 줄의 금액 | medium | mitigate | 연결 문서 조회를 한 함수로 두고 그 결과로 금액 칸을 읽기 전용으로 판정한다. Phase 5가 조회를 채우면 같은 판정이 곧바로 적용된다 |
| T-04-166 | Elevation of Privilege | 보관함 범용 복원 | high | mitigate | (CEO A-19·OV-2) 견적 줄 복원은 `DOMAIN_RESTORERS`로 도메인 함수에 맡겨 잠금·게이트를 저장과 같은 트랜잭션에서 지난다 — 완료 줄 복원 거부를 통합 s로 고정한다 |
| T-04-167 | Tampering | 저장 대 전환 경합 | high | mitigate | (CEO A-06·OV-3) 프로젝트 행 `FOR UPDATE`로 저장·복원·전환을 직렬화하고, 두 연결 결정적 경합 테스트(p·q·r)로 「완료 프로젝트에 편집 성공」이 없음을 고정한다 |
| T-04-168 | Tampering | 정산 새 줄의 견적 금액 | medium | mitigate | (사용자 D12 · 엔지 리뷰 A P1 정정) 정산의 `insert`는 단가 원화 0 · 수량 기본 1일 때만 게이트를 통과한다 — 화면 잠금과 별개로 서버가 판정한다(통합 g·h) |
| T-04-174 | Repudiation | 거부된 쓰기의 운영 기록 | low | mitigate | (CEO B-26 · 사용자 D19-3 · 엔지 리뷰 B) 정산 칸·구조·줄 소속·순서·복원 거부를 `denyWrite` 한 함수(04-20, 허용 목록 필드 — 금액 없음)로 남긴다 — 통합 (b) 스파이 단언 |
| T-04-169 | Tampering | 줄 상태·프로젝트 id·줄 id 입력 | low | mitigate | (CEO A-37 · 엔지 리뷰 A P3) 액션 스키마가 줄 상태를 `QUOTE_LINE_STATUSES` enum, 프로젝트·줄 id를 uuid로 좁혀 임의 문자열이 금액 규칙(`cancelled`)·SQL에 닿지 않는다 |
| T-04-310 | Tampering | 재전송·중복 저장 | medium | mitigate | (ENG-D10) 새 줄이 화면 uuid를 싣고 `ON CONFLICT (id) DO NOTHING`으로 넣는다 — 같은 값 재전송은 no-op(통합 w), 같은 id의 다른 값·다른 소속은 거부(통합 w2·w3)라 재전송 id로 남의 줄·저장된 값을 조용히 덮지 못한다. UX-04 「중복 저장 없음」 |
| T-04-311 | Repudiation | 거부된 합성 저장의 성공 로그 | medium | mitigate | (엔지 리뷰 A §1 P2) 견적 줄·매출 저장의 `recordAction`이 받은 트랜잭션으로 쓴다 — 되돌린 배치에 `document_update`가 남지 않는다(통합 t, 금지 항목 셋째) |
| T-04-312 | Denial of Service | 잠근 트랜잭션 안 풀 호출 | medium | mitigate | (ENG-D3 ①) 저장을 트랜잭션 전·안·뒤로 나누고 안에서는 `tx`만 쓴다 — 풀 2 동시 저장 셋이 셋 다 성공하는 통합 케이스로 고정한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `quote-edit-scope`·`quote-lines`·`rules-gate` 초록
- 통합 `quote-lines`(매트릭스·순서·재전송·보관·복원·유령 로그·판정 순서·경합)·`archive`·`projects-list`·`project-period`·`leak-scan`·`tx-safety` 초록
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 정산 PM은 실행가 수정과 견적 0(단가 0 · 수량 1) 새 줄 추가만, 완료는 아무것도 서버를 통과하지 못하고, 판정은 DTO와 같은 함수다
- 줄 삭제가 보관으로 저장되고 보관 줄이 목록 합계에서 빠지며, 복원이 같은 규칙을 지난다
- 새 줄 재전송이 중복을 만들지 않고, 순서가 두 번 읽어도 같으며, 저장 결과가 활성 줄 전체다
- 저장·복원·전환이 한 프로젝트에서 겹쳐도 결과가 한 가지이고, 거부된 저장은 로그를 남기지 않으며, 동시 저장이 풀을 굳히지 않는다
- Phase 5는 연결 문서 조회 한 함수만 채우면 된다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/quotes/edit-scope.ts` — `QuoteCellEditability`(유니온) · `QUOTE_LINE_STATUSES`·`QuoteLineStatus` · `QUOTE_LINE_FIELDS` · `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT` · `lineCellEditability` · `structuralEditability`(`{ insert, archive, reorder, duplicate, newRevision }`) · `quoteCellsZero` · `orderChange` · `quoteLockReason`(잠긴 칸 이유 = 표 위 한 줄, DR-2) · `linkedDocumentReason` |
| 게이트 규칙 | `project.line-edit`(ctx 확장: `change` 여섯 종류 — update · insert · archive · reorder · duplicate · restore — 와 `hasLinkedDocuments`) |
| 함수(신규) | `prepareQuoteLineSave` · `writeQuoteLinesInTx` · `finishQuoteLineSave`(저장 세 단계) · `linkedDocumentsByLine`(연결 문서 조회 지점, 이 페이즈 빈 결과) · `restoreQuoteLine`(domain) · `archiveQuoteLines` · `insertQuoteLineIfAbsent` · `setQuoteLineSortOrders` · `restoreQuoteLineRow`(repository) · `DOMAIN_RESTORERS`(`domain/archive/index.ts`) |
| DTO 필드 | `QuoteLineDto.cellEditability` · `QuoteLineDto.hasLinkedDocuments` · `QuoteLineDto.readonlyReason`(DR-35 — 04-30의 이유 줄) |
| 입력 계약 | 줄 `id`(uuid 필수) · 새 줄 `isNew: true`(화면 uuid) · `duplicatedFrom?` · `order?: string[]` · `archivedLineIds` · `saveProjectLedgerAction` 스키마(`projectId`·줄 id uuid · `lineStatus` enum · `isNew` · `order` · `archivedLineIds`) — 04-13(그룹 B)부터 줄 행 스키마는 `domain/quotes/lines.ts`의 `quoteLineRowInputSchema`로 옮겨진다 |
| 쓰기 단계 결과 | `writeQuoteLinesInTx` → `{ activeRows, fxToRemember }` — 커밋 뒤 기억은 트랜잭션을 연 쪽(교차 그룹 규약) |
| 결과 계약 | `SaveQuoteLinesResult.lines` = 차수의 활성 줄 전체(표시 순서) |
| 보관함 등록 | `ARCHIVABLE_TABLES`에 `quote_line`(복원은 `DOMAIN_RESTORERS`) |
| 운영 로그 | `write.denied`(`denyWrite` — `saveQuoteLines`·`restoreQuoteLine`의 한 거부 지점, 금액 없음 · 04-26·04-13·04-40이 같은 지점을 지난다) · `fx.remember_failed`(error — 커밋 뒤 환율 기억 실패, 매출 쪽과 공용) |
| 문구(UI-SPEC rev 5 원문) | `정산 · 실행가와 새 줄만` · `완료 · 견적 줄 잠김`(`quoteLockReason` — 04-30 표 위 한 줄과 한 문자열) · `지출결의 {번호} 연결됨 · 고치려면 새 차수`(`linkedDocumentReason`) · `정산 · 새 줄은 실행가만` · `정산 · 줄 삭제·이동 없음` · `줄 순서가 맞지 않음 · 새로 고침` · `이미 저장된 줄과 값이 다름 · 새로 고침` |
| 새 문구(rev 5에 없는 서버 방어 — SUMMARY 「새 문구」) | `보관된 줄 · 새로 고침` · `연결 문서 있음 · 삭제 대신 취소` |
| 변경된 기존 심볼 | `saveQuoteLines`(세 단계 · tx 안 `loadProjectForGate` · 정규화 비교 · 순서 · 줄 소속 · 멱등 삽입 · 로그 tx · 전체 목록 · `projectMany`) · `saveProjectLedger`(견적 줄 세 단계 호출) · `saveRevenue`(로그 tx 한 곳) · `findQuoteLinesByIds`·`updateQuoteLineIfVersionMatches`(`revision_id` 조건) · `listQuoteLinesByRevision`(보관 제외 · id 둘째 정렬) · `lineSumsSubquery`(보관 줄 제외 · 주석) · `computeQuoteLineAmounts`(취소 → 견적가 0) · `listQuoteLines`(셀 단계 · `projectMany`) · `restore`(도메인 복원 위임) |
| 테스트 | `test/unit/domain/quote-edit-scope.test.ts`(신규) · `test/integration/quote-lines.test.ts`·`test/unit/domain/quote-lines.test.ts` 확장 · `test/integration/tx-safety.test.ts`(풀 2 동시 견적 줄 저장 셋 다 성공) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 화면 반영 전부(셀 단계 격자 · 표 위 한 줄 · 힌트 줄 · EMPTY · 구조 컨트롤 · 삭제·취소 모달 · 순서·uuid 전송 · 전체 목록 반영 · D-77 버튼 부재 · S4 backstop 둘 — **04-30**) · 차수당 줄 수 상한(04-26) · 현재 차수 판정·승인 축·새 차수 게이트(04-14, 그룹 B) · 리저브 복원(04-07, 그룹 B) · 매출 저장의 세 단계화·화면 uuid(그룹 B) |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-03 — 저장마다 순서 재설정 · 정산에서 순서 헛판정 | CRITICAL | Plan 04-12, Task 1 ③(d): 미전송이면 현재 값 유지 · 통합 a·e (클라이언트 전송은 04-30) |
| A-04 — 보관 줄이 목록 합계·상세에 남음 | CRITICAL | Plan 04-12, Task 2 ③: `listQuoteLinesByRevision`·`lineSumsSubquery` 제외 · 보관 줄 수정 거부 · 통합 k·l |
| A-06 / OV-3 — 잠금 없는 게이트 · FOR SHARE로는 부족 | CRITICAL | Plan 04-12, Task 1 ③(a) `loadProjectForGate`(FOR UPDATE) · Task 3 ② 두 연결 경합 p·q·r |
| A-12 — 완료 계약 편집 | WARNING | 끝 상태로 충족 — 04-41이 계약 입력 경로(액션 스키마 · `saveRevenue` 분기 · 쓰기 함수 · 컬럼)를 통째로 지워 완료든 아니든 계약 금액을 저장할 길이 없다. 이 플랜은 중간 게이트(완료 계약 거부 갈래)를 만들지 않는다 — 04-41이 곧 지울 코드를 세웠다 지우는 일이고(Simplicity First), Phase 4는 PR #38 한 번에 배포되어 04-12~04-41 사이 상태는 어느 환경에도 가지 않는다(04-41 DECISIONS 릴리스 제약). 1차 렌더 `canSave`는 04-22 · 그 계약 항 삭제는 04-16 |
| A-19 / OV-2 — 범용 복원이 도메인 검사 우회 | WARNING | Plan 04-12, Task 3 ①: `DOMAIN_RESTORERS` + `restoreQuoteLine` 한 tx · 통합 s |
| A-21 — 정규화 차이로 헛거부 | WARNING | Plan 04-12, Task 1 ③(c) · 통합 d |
| A-26 / 사용자 D10 — 정산 줄 추가 허용, 삭제·이동·새 차수 거부 | DECISION | Plan 04-12, Task 2 ①②: `structuralEditability` 정산 = `insert`만 · 통합 g·i |
| A-26a / 사용자 D12 — 새 줄 견적 칸 0 잠금 | DECISION | Plan 04-12, Task 1 ① `isNewLine` 행 · Task 2 ② `quoteCellsZero` · 통합 h · 금지 항목 2 |
| A-32 — domain의 `ui` import 금지 | NOTE | Plan 04-12, Task 1 ①: 유니온을 domain이 선언 |
| A-37 — 줄 상태·프로젝트 id 검증 | NOTE | Plan 04-12, Task 2 ④ · 통합 o |
| B-26 / 사용자 D19-3 — 거부된 쓰기가 운영 기록에 없음 | DECISION | Plan 04-12, Task 1 ③(e) 한 거부 지점 `write.denied` · Task 3 ① 복원 거부 · 통합 b 스파이 · T-04-174 (04-13 조정 거부·04-26 상한 거부가 같은 지점을 지난다) |
| B-01 (04-12 부분) — 줄 소속 | CRITICAL | Plan 04-12, Task 1 ③(b): `revision_id` 조건 + 행 수 확인 · 통합 f (현재 차수 판정은 04-14, 그룹 B) |
| OV-1 (04-12 부분) — 승인 합계 불변 | HIGH | Plan 04-12: 판정 입력을 객체로 두고 복원까지 같은 게이트를 지나게 해 04-14가 승인 축 하나로 삽입·취소·보관·복원을 막게 함(그룹 B가 구현) |
| A-33 — 흩어진 선판정 | NOTE | `loadProjectForGate` 재사용(04-11) |
| OV-7 / 사용자 D21 | HIGH | 서버 파일만 고쳐 화면 쪽(04-30)과 분리 — 견적 표 파일을 이 플랜에서 고치지 않는다 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| B-01 현재 차수 판정 · 새 차수 대 저장 · 승인 대 저장 경합 | 차수가 여럿이 되는 04-14(그룹 B 담당)에서 — 이 플랜 시점에는 차수가 하나라 현재 차수 불일치가 생기지 않는다 |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 A §2 P1 — 「수량 0 그리고 단가 0」이 수량 > 0 검증과 모순(통합 g가 늘 빨강) | HIGH | Supersedes Round 1 A-26a의 「수량 = 0」: Plan 04-12, `<probe_fallback>` · Task 2 ① `quoteCellsZero`(단가 원화 0 · 수량 없음/1) · 통합 g·h · 수량 검증 무변경 수용 기준 · T-04-168 (화면 기본값은 04-30) |
| ENG-D10 — 응답 유실 재전송이 새 줄을 두 번 넣음 | DECISION | Plan 04-12, Task 2 ③ 화면 uuid + `isNew` + `ON CONFLICT (id) DO NOTHING` · 같은 값 no-op / 다른 값·다른 소속 거부(그룹 B와 같은 재전송 규칙) · 통합 w·w2·w3 · T-04-310 (화면 uuid는 04-30) |
| 교차 그룹 — 커밋 뒤 환율 기억 규약 | NOTE | Plan 04-12, `<probe_fallback>` `fxToRemember` + 트랜잭션을 연 쪽이 커밋 뒤 기억 · Task 1 ③ · 수용 기준 (04-41·04-07이 따른다) |
| 엔지 리뷰 A §2 P2 — 부분 순서 전송의 동률·틈 · 정렬 동률 깨기 없음 | WARNING | Supersedes Round 1 A-03의 「새 줄은 입력값」: Plan 04-12, Task 1 ④(d) id 둘째 정렬 · Task 2 ④ append `max + 1` · `order` 한 번 · 정산은 기존 상대 순서 유지 · 통합 u·u2·u3·i (전송은 04-30) |
| 엔지 리뷰 A §2 P2 — 저장 결과가 저장한 줄만 | WARNING | Plan 04-12, Task 1 ④(f) 활성 줄 전체 · 통합 e (화면 E2E는 04-30) |
| 엔지 리뷰 A §1 P2 — 유령 로그(견적 줄·매출) | HIGH | Plan 04-12, Task 1 ③ `recordAction({ tx })` · Task 3 ② `saveRevenue` 한 곳 · 통합 t(공백 3) · 금지 항목 셋째 · T-04-311 (Round 1 표의 Task 번호는 그때 본문 기준 — 이번 개정으로 Task 1 ③ → ④, Task 2 ③·④ → ⑤·⑥으로 밀렸다) |
| ENG-D3 ① — 잠근 트랜잭션 안 풀 호출 | DECISION | Plan 04-12, `<probe_fallback>` 저장 세 단계 · Task 1 ③ · 합성 저장 호출 순서 · Task 3 ⑤ 풀 2 동시 저장 셋 다 성공 · T-04-312 |
| ENG-D3 ② — `project()` N+1(300줄) | DECISION | Plan 04-12, Task 1 ③⑤ `listQuoteLines`·`finishQuoteLineSave`가 `projectMany`(04-32) |
| ENG-D6 — 기간 + 줄 판정 순서의 결과 | DECISION | Plan 04-12, Task 3 ④ 통합 v(늦추기 통과 · 앞당기기 거부) — 순서 자체는 04-22 |
| 엔지 리뷰 A P3 — 줄 상태 도메인 상수 없음 | NOTE | Plan 04-12, Task 1 ① `QUOTE_LINE_STATUSES` · Task 2 ⑥ 액션 enum (화면 라벨 표는 04-30) |
| 엔지 리뷰 A P3 — 낡은 주석(`repositories/projects.ts:24-26` · `lines.ts:348-353`) | NOTE | Plan 04-12, Task 1 ④(f) · Task 2 ⑤ |
| 엔지 리뷰 B — `write.denied` 한 함수 | NOTE | Plan 04-12, `denyWrite`(04-20) 한 지점 · T-04-174 |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 한 행씩 도는 쓰기 루프가 300줄 붙여넣기 동안 프로젝트 잠금을 쥠(엔지 리뷰 A §4 P3) | 지금 규모(10~30명)에서 허용 — 아프면 여러 행 `INSERT … RETURNING`으로 묶는다 |
| 매출 저장의 세 단계화·화면 uuid | 매출을 다시 짜는 그룹 B 플랜의 몫(같은 규약 — 교차 그룹 메모) |

### Round 3 — 디자인 리뷰(`docs/designs/plant8-erp-phase4-design-review-260923.md`) · UI-SPEC rev 5

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-1 — `<probe_fallback>` 147행의 D16 오인용(「D16이 UI-SPEC 편집을 막았다」)과 SUMMARY 「문구 차이」 우회 | CRITICAL | 삭제 — rev 5 Copywriting이 계약이다. D16은 REQUIREMENTS·ROADMAP 세 문구 승인일 뿐이다. 「새 거부 문구」 줄을 「거부 문구 = rev 5 원문」 줄로 교체, Artifacts의 「새 문구(문구 차이)」 행 교체 |
| DR-2 — 정산 셀 이유와 표 위 한 줄이 서로 다른 문자열 | CRITICAL | Plan 04-12, Task 1 ①②·truths: `quoteLockReason({ status })` 한 함수(정산 `정산 · 실행가와 새 줄만` · 완료 `완료 · 견적 줄 잠김`)를 게이트가 부르고 04-30 `tableLockLine`이 같은 함수를 부른다 · 수용 기준 grep(domain에서 한 파일) |
| P0 · rev 5 F1 — 서버 거부 문구 `…습니다 · 새로 고쳐 주세요` 꼴 | NOTE | 전 문서: `정산 · 새 줄은 실행가만` · `정산 · 줄 삭제·이동 없음` · `줄 순서가 맞지 않음 · 새로 고침` · `이미 저장된 줄과 값이 다름 · 새로 고침` · `지출결의 {번호} 연결됨 · 고치려면 새 차수`(rev 5 원문) · rev 5에 없는 방어 문구 둘은 명사형(SUMMARY 「새 문구」) |
| DR-6 — `seenStatus`(계약 4) | NOTE | 서버 비교는 04-22(합성 저장의 잠금 직후 첫 판정 행과 비교) — 이 플랜은 합성 저장 테스트 호출에 `seenStatus`를 싣고 (v)가 셀 게이트 거부임을 문자열로 구분한다(`<probe_fallback>` 새 줄 · Task 3 ④) |
| DR-1 — rev 4 줄 번호 | NOTE | `<context>`·Task 1 `read_first`를 rev 5 절 제목 + 현재 줄로 |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B5 — 04-22 분할(04-22 → 04-44 → 04-12 사슬) | NOTE | `depends_on`을 `04-22`에서 `04-44`로(04-44가 04-22에 의존하므로 04-22의 합성 저장 tx 순서·`seenStatus`는 그대로 조상) · `<context>`에 04-44 SUMMARY · 커밋 뒤 환율 기억 목록에 04-44의 총 매출 예상가 기억을 합친다. `seenStatus`(계약 4) 주인은 여전히 04-22 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md` when done
</output>
