---
phase: 04-project-quote-ledger
plan: 12
type: execute
wave: 11
depends_on: ["04-11", "04-22"]
files_modified:
  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - test/unit/domain/quote-lines.test.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - repositories/archive.ts
  - domain/projects/ledger.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/integration/quote-lines.test.ts
  - test/e2e/quote-edit-scope.spec.ts
autonomous: true
requirements: [PROJ-02, UX-04, UX-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "정산 프로젝트에서 담당 PM은 견적 줄의 실행가 셀만 편집할 수 있고 수량·단가·견적가·소분류·항목·거래처·상태·비고 셀은 잠김이다 — 셀 단계를 서버가 줄마다 판정해 보내고 화면은 상태 문자열을 해석하지 않는다(D-78, UI-SPEC S4 상태별 셀 단계, §7-3 (가))"
    - "정산에서 실행가 밖 셀의 값을 저장 페이로드에 직접 실어 보내도 서버가 그 칸을 `정산 · 실행가만 고칠 수 있음`으로 거부하고 배치 전체가 저장되지 않는다 — 실행가만 바꾼 저장은 통과한다(D-78, RESEARCH Validation D-78)"
    - "정산에서는 줄 추가 · 첫 줄 만들기 · 줄 삭제 · 줄 이동 · 줄 복제가 불가능하다 — 컨트롤이 렌더되지 않고 Delete · Ctrl+Enter · Ctrl+D · Alt+↑↓는 아무 일도 하지 않으며, 서버도 새 줄·보관·순서 변경을 거부한다(⚠ S4 정산 구조 잠금 = D-78 Claude 해석 기본값)"
    - "정산 단계에서 이 페이즈가 만드는 것은 상태(04-06)와 편집 범위(D-78)뿐이다 — 정산 프로젝트 상세에 매출 세금계산서 발행 요청·지출결의 마감 버튼이나 동작이 없다(D-77 — 발행 요청 흐름과 지출결의 마감은 Phase 5·6)"
    - "완료 프로젝트는 PM에게 모든 셀이 잠김이고 표가 편집 격자가 아니라 캡션 있는 읽기 표(흰 머리글)로 렌더되며 1차 저장 버튼이 없다(D-47, §7-3 (가), U-5)"
    - "표 위 한 줄이 이유를 말한다 — 정산 `정산 · 실행가만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김`. 줄이 0개면 이 줄을 그리지 않는다(S4, SYSTEM.md 개정 ④)"
    - "힌트 줄은 그 사람에게 실제로 되는 것만 적는다 — 정산의 PM은 `이동 Tab ↑↓←→ · 범위 복사 Ctrl+C / 붙여넣기 Ctrl+V · 취소 Esc · 저장 Ctrl+S`(§7-9, UI-SPEC S4 힌트 줄)"
    - "0줄 표의 EMPTY 한 줄이 상태 × 보는 사람으로 갈린다 — 줄 추가 권한자 `… · 첫 줄 만들기`+Ctrl+Enter · 같은 상태의 권한 없는 사람 `… · 담당 PM {이름}` · 정산의 기간 편집자 `… · 정산 · 기간 바꾸기`(누르면 기간 칸 종료일에 포커스) · 정산의 그 밖 `… · 정산 · 기간 늦추기는 팀장 {이름}` · 완료 `… · 완료 · 견적 줄이 잠김` — 렌더되지 않는 행동을 문구에 적지 않는다(S4 empty, Copywriting Empty — 견적 줄 표)"
    - "미수주·수주중·진행 프로젝트의 편집은 04-04 그대로 전부 열려 있다(D-45)"
    - "저장된 줄을 지우면 확인 모달(`견적 줄 삭제` · 부제 `{항목} · {견적가}` · 1차 `견적 줄 삭제` · 2차 `취소 Esc`) 뒤 일괄 저장 때 같은 트랜잭션에서 보관(`archived_at`)되고, 보관함 화면에서 복원할 수 있다 — 물리 삭제가 없다(D-56, Copywriting Destructive)"
    - "연결 문서가 있는 줄은 보관 대신 `취소` 상태로만 바뀌고(견적가 0, 이력 유지) 금액 셀(수량·단가·실행가·환율)이 읽기 전용이며, 그 값을 실어 보내면 `지출결의 연결됨 · 고치려면 새 차수`로 거부된다 — 판정 입력(연결 문서 조회)은 한 지점이고 이 페이즈에서는 비어 있다(D-56·D-66, PROJ-02)"
    - "`취소` 상태 줄의 견적가는 서버가 0으로 계산해 저장한다(PROJ-02 「견적가 0, 이력 유지」)"
    - statement: "견적 줄 EMPTY 변형이 다섯 상태 × 세 계급(담당 PM·팀장·경영관리) + 권한 없는 사람 렌더에서 문구와 버튼 유무가 Copywriting과 일치한다 — 렌더 감사"
      verification: backstop
    - statement: "견적 줄이 0·1·6줄일 때 0줄은 EMPTY 한 줄, 1줄과 6줄은 같은 그룹 머리글 + `합계 (공급가액 · N줄)` 형식이고 조수사가 바뀌지 않는다 — 렌더 감사"
      verification: backstop
  prohibitions:
    - statement: "정산의 편집 제한이 실제 원가 입력을 막는 잠금이 되어서는 안 된다. 정산은 끝난 행사의 실제 비용(실행가)을 맞추는 단계이고(D-77·D-78), 실행가 셀을 잠그는 구현은 PM이 원가를 시스템 밖에서 맞추게 만든다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/edit-scope.ts"
      provides: "상태 × 계급 × 연결 문서의 셀 단계 · 구조 편집 가능성 · 표 위 한 줄 · EMPTY 변형을 한 곳에서 판정하는 순수 함수(게이트 규칙과 화면이 같이 쓴다)"
      exports: ["QUOTE_LINE_FIELDS", "lineCellEditability", "structuralEditability", "tableLockLine", "quoteTableEmptyState"]
    - path: "test/unit/domain/quote-edit-scope.test.ts"
      provides: "다섯 상태 × 계급 × 연결 문서 결정표 + EMPTY 우선순위 표"
      min_lines: 60
    - path: "test/e2e/quote-edit-scope.spec.ts"
      provides: "정산 PM 실행가만 · 구조 컨트롤 부재 · 완료 읽기 표 · 삭제→보관 · 정산 EMPTY 두 변형(상한 케이스는 04-26이 더한다)"
      min_lines: 50
  key_links:
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "`project.line-edit` 규칙이 화면과 같은 셀 단계 함수로 저장을 판정한다"
      pattern: "lineCellEditability"
    - from: "domain/quotes/lines.ts"
      to: "domain/rules/gate.ts"
      via: "줄마다 서버가 DB 현재 값과 비교해 바뀐 칸 목록을 만들어 게이트에 싣는다(클라이언트 baseline을 믿지 않는다)"
      pattern: "project.line-edit"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "domain/quotes/lines.ts"
      via: "열의 편집 가능성이 DTO의 `cellEditability`에서 온다 — 화면이 상태를 해석하지 않는다"
      pattern: "cellEditability"
    - from: "domain/quotes/lines.ts"
      to: "repositories/archive.ts"
      via: "줄 삭제가 보관함 등록 표(`quote_line`)를 지나 같은 트랜잭션에서 보관된다"
      pattern: "archiveQuoteLines"
---

<objective>
**As a** 정산 단계의 기획 PM, **I want to** 끝난 행사의 견적 구조는 그대로 둔 채 실제로 쓴 돈(실행가)만 맞추고, 완료된 프로젝트는 손댈 수 없게 잠기며, 지운 줄은 보관함으로 가기를, **so that** 정산 중에 견적이 바뀌어 계약 금액·손익이 흔들리는 일이 없고, 화면에 보이는 편집 가능 칸과 서버가 받는 칸이 언제나 같다.

이 플랜은 견적 줄 표의 **편집 범위 한 곳**을 만든다(RESEARCH §3 — 셀 편집 가능성은 상태 × 연결 문서 × 계급의 조합이고 한 함수에서 합쳐야 한다). 04-06이 만든 게이트 규칙 `project.line-edit`(완료 잠금)를 셀 단위로 넓히고(D-78), 04-04가 클라이언트에서만 지우던 줄 삭제를 서버 보관 경로로 잇는다(D-56). **2026-09-23 체커 1차 분할:** 옛 Task 3의 차수당 줄 수 상한(D-86)은 **04-26**으로 옮겼고, 옛 Task 2(13파일)는 서버 쪽(구조 게이트 · 보관 · 취소 · 연결 문서 조회 지점)과 화면 쪽(표 위 한 줄 · 힌트 줄 · EMPTY · 구조 컨트롤 · 모달)으로 나눴다. 조정 줄(D-83)·견적 외 비용 줄(D-48)은 이 함수에 줄 종류 축을 더하는 04-13·04-23이다.

Purpose: 04-11이 프로젝트를 제때 정산으로 만들고 04-22가 기간 칸을 열었으니, 이제 정산이 실제로 「실행가만」이 되어야 한다. 셀 단계를 화면이 추론하면 UX-06·ROADMAP 기준 3 위반이다. D-77은 정산 단계의 뜻(발행 요청·증빙 마감)을 정했지만 그 흐름은 Phase 5·6이다 — 이 플랜은 편집 범위만 만들고 그 흐름을 흉내 내는 버튼을 두지 않는다.

Output: 편집 범위 순수 함수 · 게이트 확장 · 서버 변경 칸 판정 · 줄 보관·취소 경로 · 연결 문서 조회 지점 · 표 화면 반영 · 단위 1 · 통합 1 확장 · E2E 1.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 753–761행(§3 D-78/D-83 — 판정 지점) · 845–849행(Pitfall 5)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 442–456행(S4 상태) · 1114–1141행(S4 2026-09-23 개정 — 상태별 셀 단계 표 · 정산 구조 잠금) · 1147–1152행(힌트 줄) · 321–347행(Copywriting EMPTY 여덟 · 셀 오류) · 368–369행(Destructive — 견적 줄 삭제·취소)
# docs/design/SYSTEM.md §7-3 (가) 잠김 행
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-02(취소 상태·연결 문서 줄)** · **UX-04(전부 저장 또는 전부 거부 — 잠긴 칸)** · **UX-05(단축키 — 잠긴 상태의 무동작)** 셋이다. 수용 기준은 D-77·D-78·D-56·D-66과 UI-SPEC S4에서 끌어왔다. S4 `## UI Considerations`의 empty(일곱 변형) backstop · zero-one-many backstop은 flat-scalar 마커로, 2026-09-23 partial(정산)은 truths로 옮겼다. S4 partial(상한)과 그 금지 항목(붙여넣기를 조용히 자르지 않는다)은 상한을 구현하는 04-26이, 조정 줄 쪽 항목(조정 그룹 · 조정 줄 추가 상한 · 조정 줄과 복사)은 04-13·04-23·04-14가 옮긴다.

**계획 단계 가정(표면화):**
- **⚠ S4 정산 중 구조 잠금 — 기본값 적용(= D-78 Claude 해석):** 정산에서 줄 추가·삭제·이동·복제·새 차수가 잠긴다. 사용자가 뒤집으면 `structuralEditability`의 정산 한 줄과 EMPTY 정산 두 변형만 바뀐다.
- **바뀐 칸 판정의 기준:** 서버는 클라이언트가 보낸 baseline이 아니라 **DB의 현재 값**과 비교해 바뀐 칸을 정한다 — baseline은 충돌 판정(04-04) 전용으로 그대로 둔다. 조작된 페이로드가 「바뀐 칸 없음」을 주장해도 판정이 흔들리지 않는다.
- **D-66 연결 문서 조회 지점:** `linkedDocumentsByLine(viewer, revisionId)`가 이 페이즈에서는 빈 결과를 돌려준다. Phase 5가 이 한 함수만 채운다(옛 04-06 Task 2 ③의 설계 그대로).
- **누수 스캔:** `test/integration/leak-scan.test.ts`는 DTO 등록(`registerDto`) 뒤 초록인지 **돌려 확인만** 한다 — DTO 등록은 `lines.ts` 안에서 일어나므로 이 파일을 고칠 일이 없어 `files_modified`에 두지 않았다(고칠 일이 생기면 SUMMARY에 적는다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 정산 프로젝트에서 PM이 실행가만 고쳐 저장한다(판정 함수 → 게이트 → 저장 → DTO → 격자 → E2E)</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/integration/quote-lines.test.ts, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `domain/rules/register.ts`(04-06·04-20·04-22 산출물 전문) — `project.line-edit`의 현재 ctx
    - `domain/quotes/lines.ts` 100–160행(DTO·투영) · 185–270행(`QuoteLineWriteRow`·비교 칸 목록) · 354–551행(`saveQuoteLines` — 게이트 호출·사전 판정·쓰기)
    - `ui/table/types.ts`(전문) — `CellEditability` 세 단계와 열의 `editability`
    - `ui/table/Table.tsx` 70–140행 — 편집 가능 셀 스캔으로 `role="grid"`·머리글 바탕이 정해지는 자리
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `editability: () =>`(열 정의 568~712행 근처)·`isEditable: () =>`(735~745행 근처)·`editable` prop을 찾아 범위만 Read
    - `app/(app)/projects/[id]/page.tsx`(전문)
    - `test/integration/quote-lines.test.ts`(전문) — 저장 통합 테스트 형태와 04-06이 바꾼 잠금 픽스처
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1116–1124행(상태별 셀 단계 표)
  </read_first>
  <behavior>
    - `lineCellEditability({ status: 정산, canWrite: true, hasLinkedDocuments: false })` → 실행가 `edit`, 나머지 일곱 칸 `locked`
    - 같은 입력의 완료 → 전부 `locked` · 수주중·진행·미수주 → 전부 `edit`
    - `hasLinkedDocuments: true` + 진행 → 수량·단가·실행가 `readonly`, 항목·비고 `edit`(D-66) · 정산 + 연결 문서 → 실행가도 `readonly`
    - `canWrite: false` → 전부 `locked`
    - 게이트 `project.line-edit`: 정산 + 바뀐 칸 [실행가] → 통과 · [실행가, 단가] → `정산 · 실행가만 고칠 수 있음` · 완료 + [비고] → `완료 · 견적 줄이 잠김` · 진행 + 연결 문서 + [수량] → `지출결의 연결됨 · 고치려면 새 차수`
  </behavior>
  <action>
① `domain/quotes/edit-scope.ts`(순수 — 리포지토리 import 없음): `QUOTE_LINE_FIELDS = ["subcategory","itemName","vendorId","quantity","unitPrice","execution","lineStatus","note"] as const` · `lineCellEditability({ status, canWrite, hasLinkedDocuments })` → 칸별 `CellEditability`(UI-SPEC S4 표) · `structuralEditability`는 Task 2, `tableLockLine` · `quoteTableEmptyState`는 Task 3이 채운다(이 태스크는 셀 단계만). 상태 값 비교는 이 파일과 게이트 규칙 안에만 둔다. 결정표 단위 테스트(`behavior`)를 먼저 쓴다(RED).

② **게이트 확장.** `project.line-edit`의 ctx를 `{ status, hasLinkedDocuments, change: { kind: "update"; fields: QuoteLineField[] } | { kind: "insert" } | { kind: "archive" } | { kind: "reorder" } }`로 넓히고, `update`는 바뀐 칸마다 `lineCellEditability`가 `edit`인지 본다 — 잠김이면 상태별 이유(정산 `정산 · 실행가만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김`), 읽기 전용이면 `지출결의 연결됨 · 고치려면 새 차수`. 구조 변경(`insert`·`archive`·`reorder`)은 Task 2가 판정을 채운다 — 이 태스크에서는 완료만 거부하고 나머지는 통과(04-06 동작 유지).

③ **서버 변경 칸 판정.** `saveQuoteLines`가 기존 줄마다 **DB 현재 값**(이미 읽고 있는 `currentRowsById`)과 들어온 값을 칸별로 비교해 바뀐 칸 목록을 만든다(금액은 통화·외화 금액·환율 셋 중 하나라도 다르면 그 칸이 바뀐 것). 줄마다 게이트를 부르고, 거부된 칸은 기존 형식 오류와 같은 `CellFormatError`(줄·칸·이유)로 모아 **하나라도 있으면 아무것도 쓰지 않고** `SaveRejectedError`로 전부 거부한다(04-04 규약). 바뀐 칸이 없는 줄은 게이트를 부르지 않는다. 연결 문서 여부는 Task 2의 조회 지점이 생기기 전까지 `false`.

④ **DTO.** `QuoteLineDto`에 `cellEditability: Record<QuoteLineField, CellEditability>`를 더한다 — `listQuoteLines(viewer, revisionId, ctx)`가 프로젝트 상태·쓰기 권한을 받아 줄마다 `lineCellEditability`로 계산해 싣는다(정보 항목 `project.value`, `registerDto` 갱신 — 누수 스캔). `[id]/page.tsx`는 `editable` boolean 대신 이 DTO를 넘기고, `quote-table.tsx`의 열 `editability`·키보드 `isEditable`이 행의 `cellEditability[칸]`을 읽는다(새 줄은 서버 규칙과 같은 함수로 클라이언트가 만든 기본값 — 새 줄을 만들 수 있는 상태에서는 전부 `edit`). 이렇게 하면 완료 PM의 표는 편집 셀 0개라 `ui/table`이 캡션 있는 읽기 표로 자동 전환된다(모드 prop 없음).

⑤ 표 위 한 줄은 Task 3 — 이 태스크의 E2E는 셀 단계만 본다. `test/integration/quote-lines.test.ts`에: 정산 프로젝트에서 실행가만 바꾼 저장 통과 · 단가를 같이 바꾼 조작 페이로드 전부 거부(DB 재조회로 실행가도 안 바뀜) · 완료에서 비고만 바꾼 저장 거부. `test/e2e/quote-edit-scope.spec.ts` 첫 케이스: 정산 프로젝트(준비에서 상태를 두고 줄 두 개)를 PM으로 열어 실행가 셀은 Enter로 편집에 들어가고 단가 셀은 편집에 들어가지 않으며(`aria-readonly`), 실행가를 고쳐 Ctrl+S로 저장하면 합계 행에 `저장됨`이 나온다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/rules-gate.test.ts test/unit/domain/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/quote-lines-conflict.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 정산의 조작 페이로드가 통과했거나 기존 충돌 판정이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-edit-scope.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `edit-scope.ts`가 리포지토리를 import하지 않고, 게이트 규칙과 `listQuoteLines`가 같은 `lineCellEditability`를 부른다
    - 결정표 단위 테스트가 `behavior` 다섯 줄을 덮는다
    - `saveQuoteLines`의 바뀐 칸 판정이 DB 현재 값을 기준으로 한다(baseline을 판정 입력으로 쓰지 않는다)
    - 정산 조작 페이로드 거부 뒤 DB 재조회로 **실행가까지** 바뀌지 않았음을 단언한다(전부 거부)
    - `quote-table.tsx`의 열 편집 가능성이 `cellEditability`에서 오고 `editable` boolean prop이 사라졌다
    - `QuoteLineDto` 등록 갱신 뒤 누수 스캔 통합 테스트가 초록이다
    - E2E 첫 케이스가 실행가 편집·단가 읽기·저장 성공을 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산 PM의 실행가 한 칸 편집이 판정 함수·게이트·저장·DTO·격자·E2E를 한 번에 관통하고, 나머지 칸은 화면과 서버에서 똑같이 잠겼다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 서버 — 구조 게이트 · 줄 삭제(보관)와 취소(견적가 0) · 연결 문서 조회 지점</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, test/unit/domain/quote-lines.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, repositories/archive.ts, domain/projects/ledger.ts, app/(app)/projects/actions.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1125–1127행(정산 구조 잠금)
    - `repositories/archive.ts`(전문) — `ARCHIVABLE_TABLES` 등록 형태(보관함 화면이 이 목록 하나로 복원한다)
    - `domain/archive/index.ts` 40–90행(`archive`·`restore`)
    - `repositories/quote-lines.ts`(전문 134행)
    - `domain/quotes/lines.ts`의 `computeQuoteLineAmounts`(323–340행) · `saveQuoteLines`(Task 1이 넣은 변경 칸 판정 자리)
    - `domain/projects/ledger.ts`(전문) · `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마(`quoteLines`)
  </read_first>
  <behavior>
    - `structuralEditability({ status, canWrite })`: 수주중·진행·미수주 + 쓰기 → 추가·삭제·이동·복제 전부 가능 · 정산·완료 → 전부 불가 · 쓰기 없음 → 전부 불가
    - `computeQuoteLineAmounts`에 상태 `cancelled`가 오면 견적가 0, 차익 = −실행가
    - 게이트: 정산 + `insert` / `archive` / `reorder` → `정산 · 실행가만 고칠 수 있음` · 완료 → `완료 · 견적 줄이 잠김`
    - 연결 문서 있는 줄의 `archive` → `연결 문서가 있어 삭제 대신 취소로 바꿔 주세요`(취소 상태로만)
  </behavior>
  <action>
① `edit-scope.ts`에 `structuralEditability`를 더한다. 결정표를 먼저 쓴다(RED). `tableLockLine`·`quoteTableEmptyState`는 화면 쪽 Task 3이다.

② **게이트 구조 판정.** `project.line-edit`의 `insert`·`archive`·`reorder`를 `structuralEditability`로 판정하고, 연결 문서가 있는 줄의 `archive`는 `연결 문서가 있어 삭제 대신 취소로 바꿔 주세요`로 거부한다. `saveQuoteLines`가 새 줄(id 없음)을 `insert`로, `sortOrder`가 바뀐 기존 줄을 `reorder`로 게이트에 싣는다. 구조 거부는 칸 오류가 아니라 `GateBlockedError`(게이트 이유 그대로)로 전체 거부한다.

③ **줄 보관.** `repositories/archive.ts`의 `ARCHIVABLE_TABLES`에 `quote_line` 항목(`quote_lines` 표, 표시 이름 `견적 줄`, 이름 칸 `item_name`)을 더해 보관함 화면이 복원할 수 있게 한다. `repositories/quote-lines.ts`에 `archiveQuoteLines(viewer, { ids, revisionId, archivedBy }, tx)`(그 차수 소속이고 아직 보관되지 않은 줄만, 반환 행 수 확인)를 더한다. `saveQuoteLines` 입력에 `archivedLineIds?: string[]`를 더하고, 판정 단계에서 각 id에 `archive` 게이트를 부른 뒤 쓰기 단계에서 **같은 트랜잭션**으로 보관한다 — 성공한 저장의 행동 로그 detail에 보관한 줄 id를 싣는다(04-04의 「성공 한 줄 · 거부 0줄」 유지). `domain/projects/ledger.ts`와 `saveProjectLedgerAction` 스키마(`quoteLines.archivedLineIds`)에 통과시킨다.

④ **취소(견적가 0).** `computeQuoteLineAmounts`가 `lineStatus`를 받아 `cancelled`면 견적가 0(수량·단가 컬럼은 그대로 — 이력 유지)으로 계산한다(PROJ-02). 기존 단위 테스트(`test/unit/domain/quote-lines.test.ts`)에 한 줄 더한다.

⑤ **연결 문서 조회 지점(D-66).** `domain/quotes/lines.ts`에 `linkedDocumentsByLine(viewer, revisionId): Promise<Map<string, { number: string }[]>>` 한 함수를 두고 이 페이즈에서는 빈 Map을 돌려준다(Phase 5가 이 함수만 채운다 — 주석에 한 줄). `listQuoteLines`·`saveQuoteLines`가 이 함수의 결과로 `hasLinkedDocuments`를 정한다. DTO에 `hasLinkedDocuments`를 싣는다(`registerDto` 갱신 — 누수 스캔).

⑥ **통합(`test/integration/quote-lines.test.ts`).** 정산에서 새 줄 끼운 배치 거부 · 완료에서 보관 거부 · 수주중에서 보관 뒤 DB에 행이 남고 `archived_at`이 채워지며 보관함 목록에 나온다 · 거부된 배치에서 보관도 안 됐다 · 취소 줄의 견적가 0.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/quote-lines.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 구조 판정·취소 견적가·게이트 표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/archive.test.ts test/integration/leak-scan.test.ts test/integration/project-period.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 보관이 트랜잭션 밖에서 일어났거나, 보관함 등록이 빠졌거나, DTO 등록이 누수 스캔을 깨뜨렸거나, 합성 저장 입력 확장이 04-22의 기간 저장을 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 네 줄을 덮는다
    - 보관이 `saveQuoteLines`와 같은 트랜잭션이고 거부된 배치에서 보관도 없다(통합 테스트, DB 재조회)
    - `ARCHIVABLE_TABLES`에 `quote_line`이 있고 보관한 줄이 보관함 목록에 나온다
    - `linkedDocumentsByLine`가 한 함수뿐이고 이 페이즈에서 빈 결과를 돌려준다
    - 취소 줄의 견적가가 0으로 저장된다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산·완료의 구조 변경이 서버에서 거부되고, 줄 삭제가 같은 트랜잭션의 보관으로 저장되며, 취소 줄의 견적가가 0이고 연결 문서 조회 지점이 한 곳에 섰다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 화면 — 표 위 한 줄 · 힌트 줄 · EMPTY 변형 · 구조 컨트롤 · 삭제·취소 모달 + 화면 검증(S4 backstop 둘)</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 321–328행(EMPTY 고르는 규칙 · 여덟 행 · 우선순위) · 368–369행(Destructive — 견적 줄 삭제·취소, 2차 라벨 규칙) · 1147–1151행(힌트 줄 빼기)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `confirmDeleteLine`·`DeleteLineDialog`·`addLine`·`duplicateLine`·`moveLine`·`hintRow`·`emptyMessage`를 찾아 범위만 Read
    - `domain/projects/responsibles.ts`(04-11 산출물 — 담당 PM·팀장 이름) · `domain/projects/period.ts`의 `periodEditRights`와 `QuoteLedger.openPeriodField`(04-22 산출물)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - `tableLockLine`: 정산 → `정산 · 실행가만 고칠 수 있음` · 완료 → `완료 · 견적 줄이 잠김` · 그 밖 → null
    - `quoteTableEmptyState`: 진행 + 줄 추가 가능 → 「첫 줄 만들기」 · 진행 + 불가 → `담당 PM {이름}` · 정산 + 기간 권리 lead → 「기간 바꾸기」 · 정산 + 그 밖 → `정산 · 기간 늦추기는 팀장 {이름}`(팀장 없음 → `정산 · 줄을 추가할 수 없음`) · 완료 → `완료 · 견적 줄이 잠김` · 줄 추가 가능이 기간 권리보다 우선
  </behavior>
  <action>
① `edit-scope.ts`에 `tableLockLine` · `quoteTableEmptyState({ status, canAddLine, periodRights, pmName, teamLeadName })` → `{ message, action?: "addLine" | "openPeriodEnd" }`(Copywriting 여덟 행의 우선순위 ① 첫 줄 만들기 ③ 기간 바꾸기 ④ 담당자 이름 ⑤ 완료 사실 — ② 조정 줄 추가는 04-23이 더한다)을 더한다. 결정표를 먼저 쓴다(RED).

② **화면.** `[id]/page.tsx`가 `structuralEditability`·`tableLockLine`·`quoteTableEmptyState` 결과를 넘긴다(담당자 이름은 04-11의 `projectResponsibles`, 기간 권리는 04-22의 `periodEditRights`). `quote-table.tsx`: 「줄 추가」·EMPTY 3차·Delete 모달·Ctrl+Enter·Ctrl+D·Alt+↑↓를 구조 가능성으로 켜고 끈다(불가면 **렌더하지 않고** 키는 무동작 — 비활성 버튼을 세우지 않는다) · 표 위 한 줄(줄이 1개 이상일 때만) · 힌트 줄은 가능한 항목만(정산 PM: 넷) · EMPTY는 서버가 고른 문구와 액션(「기간 바꾸기」는 04-22의 `openPeriodField("end")`) · 저장된 줄 삭제는 확인 모달(제목 `견적 줄 삭제` · 부제 `{항목} · {견적가}` · 본문 `보관함으로 이동합니다 · 관리자가 복원할 수 있습니다` · 1차 `견적 줄 삭제` · 2차 `취소 Esc` — 지금 1차 라벨 `삭제` 단독은 §8 규칙 1 위반이라 고친다) 뒤 화면에서 빠지고 `archivedLineIds`에 들어가 dirty 한 건으로 센다. 연결 문서가 있는 줄의 Delete는 `견적 줄 취소` 모달(1차 `견적 줄 취소` · 2차 `닫기 Esc` — §7-8 보강)로 상태 칸을 `취소`로 바꾼다. 정산 화면에 발행 요청·지출결의 마감 같은 버튼을 두지 않는다(D-77 — Phase 5·6).

③ **E2E(`quote-edit-scope.spec.ts`).** 정산 PM 화면에 「줄 추가」가 없고 Ctrl+Enter가 줄을 만들지 않는다 · 표 위 한 줄 · 정산 상세에 `발행 요청`·`마감` 이름의 버튼이 없다(D-77) · 완료 PM 화면이 캡션 읽기 표이고 1차 버튼이 없다 · 수주중에서 줄 삭제 → 저장 → 새로 고쳐도 없다 · 0줄 정산 표의 EMPTY가 PM에게 `정산 · 기간 늦추기는 팀장 {이름}`, 팀장에게 「기간 바꾸기」.

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S4 backstop 둘(EMPTY 변형 × 상태 × 계급 렌더 · 0/1/6줄 합계 형식)을 DOM 실측 → 필요하면 `project-detail.module.css`만 고친다 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 표 위 한 줄·EMPTY 우선순위 표가 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-edit-scope.spec.ts test/e2e/archive.spec.ts test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 구조 컨트롤이 정산에서 렌더되거나 EMPTY 「기간 바꾸기」가 04-22의 기간 칸으로 이어지지 않는다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S4 backstop 두 판정을 SUMMARY에서 확인</test>
      <expected>EMPTY 변형 문구·버튼 유무가 Copywriting과 같고, 0/1/6줄에서 합계 형식이 같다</expected>
      <why_human>상태 × 계급 조합 렌더는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 두 묶음을 덮는다
    - 정산·완료에서 「줄 추가」·EMPTY 「첫 줄 만들기」가 DOM에 없고(비활성 렌더가 아니다) Ctrl+Enter 뒤 줄 수가 그대로다(E2E)
    - 표 위 한 줄이 줄 1개 이상일 때만 렌더된다
    - 정산 PM의 힌트 줄이 항목 넷이다
    - 삭제 모달 1차 라벨이 `견적 줄 삭제`이고, 연결 문서 줄의 취소 모달 2차가 `닫기 Esc`다
    - 정산 상세에 발행 요청·지출결의 마감 버튼이 없다(E2E, D-77)
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산·완료의 구조 잠금이 화면·키·서버에서 같고, EMPTY가 사람마다 실제 다음 한 수를 말하며, 줄 삭제·취소 모달이 Copywriting 그대로다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장 | 줄 값·새 줄·보관 id·순서가 넘어온다. 셀 단계·구조 판정이 서버에만 있어야 한다 |
| 연결 문서 조회 → 금액 칸 | Phase 5가 채울 조회 결과가 금액 칸의 읽기 전용 판정을 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-58 | Elevation of Privilege | 정산·완료의 잠긴 칸 | high | mitigate | 서버가 DB 현재 값과 비교해 바뀐 칸을 정하고 `project.line-edit`가 칸마다 판정한다 — 클라이언트 baseline·화면 셀 단계를 믿지 않는다. 조작 페이로드 거부를 통합 테스트로 고정한다 |
| T-04-59 | Tampering | 보관 id 목록 | high | mitigate | 보관은 그 차수 소속·미보관 줄만 대상으로 하고(리포지토리 조건), 구조 게이트를 지나며, 저장과 같은 트랜잭션이다. 다른 프로젝트 줄 id를 섞어 보내면 0행이 되어 거부된다 |
| T-04-61 | Repudiation | 줄 삭제 | medium | mitigate | 물리 삭제 없이 보관(`archived_at`·`archived_by`)하고 저장 로그 detail에 보관 id를 남긴다. 보관함에서 복원된다 |
| T-04-62 | Tampering | 연결 문서 줄의 금액 | medium | mitigate | 연결 문서 조회를 한 함수로 두고 그 결과로 금액 칸을 읽기 전용으로 판정한다. Phase 5가 조회를 채우면 같은 판정이 곧바로 적용된다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `project.line-edit`가 셀·구조 판정을 한다(상한 게이트 `quote.line-cap`은 04-26)
- 독립 DOM 감사(S4 backstop 둘) 판정이 SUMMARY에 있다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 정산 PM은 실행가만, 완료는 아무것도 고칠 수 없고 화면과 서버의 판정이 같은 함수다
- 줄 삭제가 보관으로 저장되고 취소 줄의 견적가가 0이다
- 정산 화면이 편집 범위만 바꾸고 Phase 5·6의 발행 요청·마감 흐름을 흉내 내지 않는다(D-77)
- Phase 5는 연결 문서 조회 한 함수만 채우면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/quotes/edit-scope.ts` — `QUOTE_LINE_FIELDS` · `lineCellEditability` · `structuralEditability` · `tableLockLine` · `quoteTableEmptyState` |
| 게이트 규칙 | `project.line-edit`(ctx 확장: `change`·`hasLinkedDocuments`) |
| 함수(신규) | `linkedDocumentsByLine`(연결 문서 조회 지점, 이 페이즈 빈 결과) · `archiveQuoteLines`(repository) |
| DTO 필드 | `QuoteLineDto.cellEditability` · `QuoteLineDto.hasLinkedDocuments` |
| 입력 계약 | `QuoteLineWriteRow`와 별도로 `saveQuoteLines(…, { archivedLineIds })` · `saveProjectLedgerAction` 스키마 `quoteLines.archivedLineIds` |
| 보관함 등록 | `ARCHIVABLE_TABLES`에 `quote_line` |
| 변경된 기존 심볼 | `computeQuoteLineAmounts`(취소 → 견적가 0) · `listQuoteLines`(셀 단계 계산) · `QuoteLedger`(`editable` 제거 → 셀 단계·구조·EMPTY props) · `DeleteLineDialog` 라벨 |
| 테스트 | `test/unit/domain/quote-edit-scope.test.ts` · `test/e2e/quote-edit-scope.spec.ts`(신규) · `test/integration/quote-lines.test.ts`·`test/unit/domain/quote-lines.test.ts` 확장 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 차수당 줄 수 상한(D-86) — 설정 키 `quote_line.max_per_revision` · 게이트 `quote.line-cap` · `countActiveLinesByRevision` · `test/integration/quote-line-cap.test.ts` · 붙여넣기 전부 거부(04-26) |
| 새 스키마·마이그레이션 | 없음 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md` when done
</output>
