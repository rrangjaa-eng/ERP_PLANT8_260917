---
phase: 04-project-quote-ledger
plan: 12
type: execute
wave: 11
depends_on: ["04-22"]
files_modified:
  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - test/unit/domain/quote-lines.test.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - repositories/archive.ts
  - domain/archive/index.ts
  - repositories/projects.ts
  - domain/projects/ledger.ts
  - app/(app)/projects/actions.ts
  - test/integration/quote-lines.test.ts
autonomous: true
requirements: [PROJ-02, UX-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "정산 프로젝트에서 담당 PM은 기존 견적 줄의 실행가 셀만 편집할 수 있고 수량·단가·견적가·소분류·항목·거래처·상태·비고 셀은 잠김이다 — 셀 단계를 서버가 줄마다 판정해 DTO(`cellEditability`)로 보낸다(D-78 — 사용자 D10 개정, UI-SPEC S4 상태별 셀 단계)"
    - "정산에서 담당 PM은 **새 줄을 추가할 수 있다** — 새 줄은 소분류·항목·거래처·비고·실행가만 적고 견적 칸(수량·단가)은 0 잠금이다. 견적 칸이 0이 아닌 새 줄은 서버가 `정산 · 새 줄은 실행가만 적을 수 있음`으로 거부한다(사용자 D10·D12, CEO A-26·A-26a)"
    - "정산에서 줄 삭제(보관) · 순서 이동 · 줄 복제 · 새 차수는 서버가 거부한다 — `structuralEditability`가 정산에서 추가만 참이고 나머지는 거짓이며, 새 차수 판정 입력도 같은 함수다(사용자 D10 — D-78 개정)"
    - "정산에서 실행가 밖 칸의 값을 저장 페이로드에 직접 실어 보내도 서버가 그 칸을 `정산 · 실행가만 고칠 수 있음`으로 거부하고 배치 전체가 저장되지 않는다 — 실행가만 바꾼 저장은 통과한다(D-78, RESEARCH Validation D-78)"
    - "바뀐 칸 판정은 쓰기와 같은 정규화를 거친 값끼리 비교한다 — 환율 `1350` 대 `\"1350.0000\"`, 수량 `2` 대 `\"2.00\"`, 비고 null 대 undefined가 「바뀜」으로 잡혀 정산 저장이 헛거부되지 않는다(CEO A-21)"
    - "`sortOrder`를 싣지 않은 기존 줄은 저장해도 순서가 그대로다 — 지금 저장할 때마다 순서가 재설정되는 선행 버그를 고친다. 순서 변경은 `sortOrder`가 실려 있고 현재 값과 다를 때만이다(CEO A-03)"
    - "견적 줄 저장은 트랜잭션 안에서 프로젝트 행을 배타 잠금(`loadProjectForGate`)한 뒤 판정하고 쓴다 — 저장과 같은 프로젝트의 정산→완료 전환이 겹치면 한쪽이 기다렸다가 새 상태로 판정되어, 완료 프로젝트에 줄 편집이 성공으로 들어가는 일이 없다(CEO A-06·OV-3)"
    - "실린 줄 id는 그 차수의 줄만 찾고 고친다 — 다른 차수·프로젝트의 줄 id를 섞은 배치는 0행이 되어 전부 거부된다(CEO B-01 줄 소속 — 현재 차수 판정은 차수가 여럿이 되는 04-14)"
    - "완료 프로젝트는 모든 셀이 잠김이다(D-47) · 미수주·수주중·진행의 편집은 04-04 그대로 전부 열려 있다(D-45)"
    - "저장된 줄을 지우면 일괄 저장 때 같은 트랜잭션에서 보관(`archived_at`)되고 보관함에서 복원할 수 있다 — 물리 삭제가 없다. 보관된 줄은 상세 견적 목록·목록 합계(견적·실행가·수익금)에서 빠지고, 보관된 줄 id로 고치는 요청은 거부된다(D-56, CEO A-04)"
    - "보관함에서 견적 줄을 복원하면 저장과 같은 규칙을 한 트랜잭션에서 지난다 — 완료 프로젝트 줄은 복원이 거부되고, 정산 프로젝트 줄은 견적가가 0일 때만 복원된다(CEO A-19·OV-2 — 줄 수 상한·승인 합계 검사는 같은 복원 함수에 04-26·04-14가 더한다)"
    - "연결 문서가 있는 줄은 보관 대신 `취소` 상태로만 바뀌고(견적가 0, 이력 유지) 금액 셀(수량·단가·실행가·환율)이 읽기 전용이며, 그 값을 실어 보내면 `지출결의 연결됨 · 고치려면 새 차수`로 거부된다 — 판정 입력(연결 문서 조회)은 한 지점이고 이 페이즈에서는 비어 있다(D-56·D-66, PROJ-02)"
    - "`취소` 상태 줄의 견적가는 서버가 0으로 계산해 저장한다(PROJ-02 「견적가 0, 이력 유지」) — 줄 상태 입력은 정해진 값 목록만, 프로젝트 id는 uuid 형식만 받는다(CEO A-37)"
    - "이 플랜의 규칙(정산 칸·구조·줄 소속·보관 줄·복원·완료 계약)에 걸린 거부는 화면 거부와 함께 서버 운영 로그에 경고 `write.denied` 한 줄(보는 사람 id · 규칙 이름 · 이유 · 프로젝트·차수·줄 id — 금액 없음)을 남기고, 행동 로그(`action_log`)에는 남기지 않는다 — `saveQuoteLines`의 거부 지점은 하나이고 뒤 규칙(04-26 상한 · 그룹 B의 조정·차수·승인)도 그 지점을 지난다(사용자 D19-3, CEO 리뷰 B-26)"
    - "완료 프로젝트의 계약 금액 저장은 `완료 · 계약 금액이 잠김`으로 거부된다(CEO A-12 — 계약 칸은 그룹 B의 04-16(파생 읽기)·04-41(입력 경로·컬럼 삭제)이 견적 파생으로 바꿀 때까지 남는다)"
  prohibitions:
    - statement: "정산의 편집 제한이 실제 원가 입력을 막는 잠금이 되어서는 안 된다. 정산은 끝난 행사의 실제 비용(실행가)을 맞추는 단계이고(D-77·D-78), 실행가 셀이나 늦게 발견된 비용의 새 줄(사용자 D10)을 잠그는 구현은 PM이 원가를 시스템 밖에서 맞추게 만든다"
      status: resolved
      verification: test
    - statement: "정산에서 PM이 추가한 줄이 견적 금액을 만들어서는 안 된다. 정산은 견적이 끝난 뒤의 단계이고, 새 줄의 견적 칸이 0이 아니면 계약·손익의 견적 쪽 숫자가 행사 뒤에 바뀐다(사용자 D12)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/edit-scope.ts"
      provides: "상태 × 쓰기 권한 × 연결 문서 × 새 줄 여부의 셀 단계와 구조 편집 가능성을 한 곳에서 판정하는 순수 함수(게이트 규칙과 DTO가 같이 쓴다) — 셀 단계 유니온을 domain이 선언한다"
      exports: ["QUOTE_LINE_FIELDS", "QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT", "lineCellEditability", "structuralEditability"]
    - path: "test/unit/domain/quote-edit-scope.test.ts"
      provides: "다섯 상태 × 권한 × 연결 문서 × 새 줄 결정표 + 구조 결정표"
      min_lines: 60
    - path: "test/integration/quote-lines.test.ts"
      provides: "정산 허용·거부 매트릭스 · 정규화 비교 · 순서 보존 · 줄 소속 · 보관·취소 · 보관 줄 합계 제외 · 복원 규칙 · 저장 대 전환 경합(두 순서)"
      min_lines: 120
  key_links:
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "`project.line-edit` 규칙이 DTO와 같은 셀 단계·구조 함수로 저장·복원을 판정한다"
      pattern: "lineCellEditability"
    - from: "domain/quotes/lines.ts"
      to: "domain/projects/auto-transition.ts"
      via: "`saveQuoteLines`와 `restoreQuoteLine`이 tx 안에서 `loadProjectForGate`로 프로젝트 행을 배타 잠금하고 판정한다"
      pattern: "loadProjectForGate"
    - from: "domain/archive/index.ts"
      to: "domain/quotes/lines.ts"
      via: "보관함 복원이 견적 줄이면 도메인 복원 함수에 통째로 맡긴다(범용 복원이 도메인 검사를 우회하지 않는다)"
      pattern: "restoreQuoteLine"
    - from: "repositories/projects.ts"
      to: "db/schema/quote-lines.ts"
      via: "목록 합계 하위 질의가 보관된 줄을 뺀다"
      pattern: "archivedAt"
---

<objective>
**As a** 정산 단계의 기획 PM, **I want to** 끝난 행사의 견적 구조는 그대로 둔 채 실제로 쓴 돈(실행가)을 맞추고 늦게 발견된 비용은 새 줄로 더하되 견적 금액은 건드리지 못하며, 완료된 프로젝트는 손댈 수 없게 잠기고, 지운 줄은 보관함으로 가기를, **so that** 정산 중에 견적이 바뀌어 계약 금액·손익이 흔들리지 않고, 서버가 받는 칸과 화면이 여는 칸이 같은 함수에서 나온다.

이 플랜은 견적 줄 표의 **편집 범위 서버 쪽 한 곳**을 만든다(RESEARCH §3 — 셀 편집 가능성은 상태 × 연결 문서 × 계급의 조합이고 한 함수에서 합쳐야 한다). 04-06이 만든 게이트 규칙 `project.line-edit`(완료 잠금)를 셀·구조 단위로 넓히고(D-78 — 사용자 D10·D12 개정), 04-04가 클라이언트에서만 지우던 줄 삭제를 서버 보관 경로로 잇는다(D-56).

**2026-09-23 CEO 리뷰 반영 — 서버·화면 분할.** 리뷰 정정(A-03·A-04·A-06·A-19·A-21·A-37 · B-01 줄 소속 · OV-1·OV-2·OV-3)과 사용자 D10·D12(정산 새 줄 허용 · 견적 칸 0 잠금)가 더해지며 옛 04-12(14파일)는 파일 상한을 넘었다. 이 플랜은 **서버 쪽**(편집 범위 함수 · 게이트 · 저장 판정 · 보관·취소·복원 · 합계 제외 · 경합)이고, 화면 쪽(셀 단계를 격자에 반영 · 표 위 한 줄 · 힌트 줄 · EMPTY · 구조 컨트롤 · 모달 · 순서 전송)은 **04-30**이 잇는다. 차수당 줄 수 상한(D-86)은 04-26, 조정 줄(D-83)·견적 외 비용 줄(D-48)의 줄 종류 축은 04-13·04-23, 고객 승인 차수의 견적 칸 잠금과 승인 합계 불변(사용자 D7 · OV-1)은 승인을 만드는 04-14가 이 플랜의 두 함수에 축을 더한다(그룹 B).

Purpose: 04-11이 프로젝트를 제때 정산으로 만들고 04-22가 합성 저장 안에 잠금 읽기를 세웠으니, 이제 정산이 실제로 「실행가 수정 + 새 줄(견적 0)」이 되어야 한다. D-77은 정산 단계의 뜻(발행 요청·증빙 마감)을 정했지만 그 흐름은 Phase 5·6이다 — 이 플랜은 편집 범위만 만든다.

Output: 편집 범위 순수 함수 · 게이트 확장 · 정규화 비교 · 순서 보존 · 줄 소속 · 줄 보관·취소·복원 · 보관 줄 합계 제외 · 연결 문서 조회 지점 · 입력 검증 · 저장 대 전환 경합 테스트 · 단위 2 확장 · 통합 1 확장.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 경합 순서는 `test/integration/lock-race.ts`(04-20)의 `deferred`·`waitForLockWaiter`로 정한다 — `sleep` 금지. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 753–761행(§3 D-78/D-83 — 판정 지점) · 845–849행(Pitfall 5)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1114–1141행(S4 2026-09-23 개정 — 상태별 셀 단계 표 · 정산 구조 잠금; 정산 줄 추가는 사용자 D10이 뒤집었다)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 748–762행(A-03·A-04·A-06) · 790행(A-19) · 794행(A-21) · 1057–1084행(B-01)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-02(취소 상태·연결 문서 줄)** · **UX-04(전부 저장 또는 전부 거부 — 잠긴 칸)** 둘이다(UX-05의 잠긴 상태 무동작은 화면이라 04-30). 수용 기준은 D-77·D-78(사용자 D10·D12 개정)·D-56·D-66과 UI-SPEC S4에서 끌어왔다. S4의 empty·zero-one-many backstop과 표 위 한 줄·힌트 줄·EMPTY 변형·모달 truths는 화면을 맡는 04-30으로 함께 갔다 — 조용히 버린 항목은 없다.

**계획 단계 가정·확정(표면화):**
- **정산 편집 매트릭스(사용자 D10·D12 확정 — D-78 개정):** 정산에서 담당 PM(쓰기 권한)은 기존 줄의 실행가 수정 + 새 줄 추가(소분류·항목·거래처·비고·실행가, 견적 칸 0 잠금). 삭제·순서 이동·복제·새 차수는 서버 거부. 완료는 전부 잠김. 새 줄의 「견적 칸」은 수량·단가(통화·외화 금액·환율 포함)이고, 서버는 새 줄의 수량 = 0 **그리고** 단가 = 0(원화)일 때만 받는다 — 견적가가 0이 되는 조합이다. 줄 상태 칸은 새 줄에서도 기본값 그대로(잠김).
- **새 거부 문구(UI-SPEC에 없음 — 사용자 D16이 UI-SPEC 편집을 막았으므로 SUMMARY에 「문구 차이」로 적는다):** `정산 · 새 줄은 실행가만 적을 수 있음`(견적 칸이 0이 아닌 새 줄) · `정산 · 줄 삭제·이동은 할 수 없음`(보관·순서) · `완료 · 계약 금액이 잠김`(A-12). 기존 `정산 · 실행가만 고칠 수 있음`·`완료 · 견적 줄이 잠김`·`지출결의 연결됨 · 고치려면 새 차수`는 그대로.
- **바뀐 칸 판정의 기준:** 서버는 클라이언트 baseline이 아니라 **DB의 현재 값**과, **쓰기 페이로드를 만드는 같은 정규화 함수**를 거친 값끼리 비교한다(A-21). baseline은 충돌 판정(04-04) 전용으로 그대로 둔다.
- **잠금 기제(OV-3 — A-06의 FOR SHARE 정정):** 프로젝트 행 `FOR UPDATE`(04-20 `lockProjectForWrite`)를 04-11의 `loadProjectForGate`로 잡는다. 합성 저장(04-22)이 이미 같은 tx에서 잡았으면 다시 잡아도 곧바로 돌아온다.
- **D-66 연결 문서 조회 지점:** `linkedDocumentsByLine(viewer, revisionId)`가 이 페이즈에서는 빈 결과를 돌려준다. Phase 5가 이 한 함수만 채운다.
- **승인 축(OV-1·사용자 D7 — 그룹 B):** 두 판정 함수의 입력은 객체라 04-14가 `approved`를 더해도 호출자가 바뀌지 않는다. 복원이 같은 게이트를 지나므로(아래 Task 3) 승인 차수의 복원 거부도 04-14의 축 한 줄로 따라온다.
- **「04-14」 표기:** 그룹 B 계획자가 04-14를 04-14(차수·승인·요약)와 04-40(쓰기 직렬화·위조 차단·승인 차수 잠금)으로 나눴다 — 이 문서의 「04-14」는 둘을 함께 가리킨다.
- **누수 스캔:** `test/integration/leak-scan.test.ts`는 DTO 등록(`registerDto`) 뒤 초록인지 **돌려 확인만** 한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 정산 프로젝트에서 PM이 실행가만 고쳐 저장한다(판정 함수 → 게이트 → tx 안 잠금 → 정규화 비교 → 순서 보존 → 줄 소속 → 쓰기 → DTO → 통합)</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `domain/rules/register.ts`(04-06·04-20·04-22 산출물 전문) — `project.line-edit`의 현재 ctx
    - `domain/quotes/lines.ts` 100–160행(DTO·투영) · 185–270행(`QuoteLineWriteRow`·비교 칸 목록) · 354–551행(`saveQuoteLines` — 게이트 호출·사전 판정·쓰기 · 479–505행 정규화 · 489행 `sortOrder: input.sortOrder ?? index`)
    - `repositories/quote-lines.ts`(전문 134행 — `findQuoteLinesByIds`·`updateQuoteLineIfVersionMatches`의 WHERE)
    - `domain/projects/auto-transition.ts`(04-11 — `loadProjectForGate`) · `domain/projects/ledger.ts`(04-22 — tx 첫 단계 잠금 읽기)
    - `lib/log.ts`(전문) — `log.warn(event, fields)` 한 줄 JSON(04-20의 `write.denied` 선례)
    - `ui/table/types.ts` 1–20행 — `CellEditability` 세 값(domain이 같은 모양의 유니온을 따로 선언한다 — A-32, domain은 `ui`를 import할 수 없다)
    - `test/integration/quote-lines.test.ts`(전문) — 저장 통합 테스트 형태와 04-06이 바꾼 잠금 픽스처
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1116–1124행(상태별 셀 단계 표)
  </read_first>
  <behavior>
    - `lineCellEditability({ status: 정산, canWrite: true, hasLinkedDocuments: false, isNewLine: false })` → 실행가 `edit`, 나머지 일곱 칸 `locked`
    - 같은 입력의 `isNewLine: true`(정산) → 소분류·항목·거래처·비고·실행가 `edit`, 수량·단가·상태 `locked`(D10·D12)
    - 완료 → 전부 `locked` · 수주중·진행·미수주 → 전부 `edit`(새 줄도)
    - `hasLinkedDocuments: true` + 진행 → 수량·단가·실행가 `readonly`, 항목·비고 `edit`(D-66) · 정산 + 연결 문서 → 실행가도 `readonly`
    - `canWrite: false` → 전부 `locked`
    - 게이트 `project.line-edit`: 정산 + update [실행가] → 통과 · [실행가, 단가] → `정산 · 실행가만 고칠 수 있음` · 완료 + [비고] → `완료 · 견적 줄이 잠김` · 진행 + 연결 문서 + [수량] → `지출결의 연결됨 · 고치려면 새 차수`
    - 정규화 비교: DB `fx_rate "1350.0000"` · `quantity "2.00"` · `note null` 대 입력 `1350` · `2` · `undefined` → 바뀐 칸 없음(A-21)
  </behavior>
  <action>
① **`domain/quotes/edit-scope.ts`(순수 — 리포지토리 import 없음).** 셀 단계 유니온 `QuoteCellEditability = "edit" | "readonly" | "locked"`를 **이 파일에서 선언**한다(A-32 — `ui/table/types.ts`의 `CellEditability`와 구조가 같아 화면에 그대로 넘어간다). `QUOTE_LINE_FIELDS = ["subcategory","itemName","vendorId","quantity","unitPrice","execution","lineStatus","note"] as const` · `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT = ["quantity","unitPrice","lineStatus"] as const` · `lineCellEditability({ status, canWrite, hasLinkedDocuments, isNewLine })` → 칸별 단계(UI-SPEC S4 표 + 사용자 D10·D12 새 줄 행). 입력은 객체 하나 — 04-13(줄 종류)·04-14(승인)가 필드를 더해도 호출자가 바뀌지 않는다. 상태 값 비교는 이 파일과 게이트 규칙 안에만 둔다. 결정표 단위 테스트(`behavior` 앞 다섯 줄)를 먼저 쓴다(RED).

② **게이트 확장.** `project.line-edit`의 ctx를 `{ status, hasLinkedDocuments, change: { kind: "update"; fields: QuoteLineField[] } | { kind: "insert"; quoteCellsZero: boolean } | { kind: "archive" } | { kind: "reorder" } | { kind: "duplicate" } | { kind: "restore"; quoteAmountZero: boolean } | { kind: "contract" } }`로 넓히고, `update`는 바뀐 칸마다 `lineCellEditability`가 `edit`인지 본다 — 잠김이면 상태별 이유(정산 `정산 · 실행가만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김`), 읽기 전용이면 `지출결의 연결됨 · 고치려면 새 차수`. 구조 변경 판정은 Task 2가 채운다 — 이 태스크에서는 완료만 거부하고 나머지는 통과(04-06 동작 유지).

③ **`saveQuoteLines` 판정 순서(A-06·OV-3·A-21·A-03·B-01).** (a) tx 첫 단계에서 차수의 프로젝트 id로 `loadProjectForGate(viewer, projectId, { now, tx, afterLock })`를 불러 잠근 행을 얻는다 — 모든 프로젝트 게이트를 이 행으로 판정한다(`deps.now`·`deps.afterLock`을 받는다 — `afterLock`은 테스트 전용 주입 지점). (b) `repositories/quote-lines.ts`의 `findQuoteLinesByIds(viewer, ids, tx)`에 `revisionId` 인자를 더해 `revision_id = $revisionId`로 좁히고, `updateQuoteLineIfVersionMatches`의 WHERE에도 `revision_id`를 더한다 — 찾은 줄 수가 실린 기존 줄 수와 다르면(다른 차수·프로젝트 줄 섞임) 아무것도 쓰지 않고 `UserFacingError`로 전부 거부한다(B-01 줄 소속). (c) 기존 줄마다 **쓰기 페이로드를 먼저 만들고**(지금 쓰기 직전에 부르는 정규화 — 수량 고정 소수·환율 문자열·빈 비고 null — 을 판정 앞으로 옮긴다) 그 페이로드 칸과 DB 현재 행 칸을 같은 표현끼리 비교해 바뀐 칸 목록을 만든다(A-21 — 금액은 통화·외화 금액·환율 셋 중 하나라도 다르면 그 칸이 바뀐 것). (d) **순서(A-03):** 기존 줄의 `sortOrder`는 입력에 없으면 **현재 값 그대로** 쓰고(지금의 `?? index` 제거 — 저장마다 순서가 재설정되는 선행 버그), 입력에 있고 현재 값과 다를 때만 순서 변경(`reorder`)으로 게이트에 싣는다. 새 줄의 `sortOrder`는 입력값, 없으면 차수 끝 다음 번호. (e) 줄마다 게이트를 부르고, 거부된 칸은 기존 형식 오류와 같은 `CellFormatError`(줄·칸·이유)로 모아 **하나라도 있으면 아무것도 쓰지 않고** `SaveRejectedError`로 전부 거부한다(04-04 규약). **거부 지점은 하나다(B-26 · 사용자 D19-3):** 게이트·소속 판정으로 배치를 거부하기 직전 `log.warn("write.denied", { viewerId, rule, reason, projectId, revisionId, lineIds })`를 한 번 부른다(`lib/log`, 금액 키 없음 — 행동 로그에는 남기지 않는다). 형식 오류(숫자 아님 등)·버전 충돌은 권한 거부가 아니라 이 로그를 남기지 않는다. 뒤 플랜의 게이트(04-26 `quote.line-cap` · 그룹 B의 조정·차수·승인)는 같은 지점을 지나므로 따로 로그 호출을 더하지 않는다. 바뀐 칸이 없는 줄은 게이트를 부르지 않는다. 연결 문서 여부는 Task 2의 조회 지점이 생기기 전까지 `false`.

④ **DTO.** `QuoteLineDto`에 `cellEditability: Record<QuoteLineField, QuoteCellEditability>`를 더한다 — `listQuoteLines(viewer, revisionId, ctx)`가 프로젝트 상태·쓰기 권한을 받아 줄마다 `lineCellEditability`로 계산해 싣는다(정보 항목 `project.value`, `registerDto` 갱신 — 누수 스캔). 화면이 이 값을 읽는 배선은 04-30이다.

⑤ **통합(`test/integration/quote-lines.test.ts`).** describe `정산 편집 매트릭스(D10·D12)` 첫 케이스들: (a) 정산 프로젝트에서 실행가만 바꾼 저장 통과 · DB 재조회로 그 줄의 `sort_order` 무변경(A-03), (b) 단가를 같이 바꾼 조작 페이로드 전부 거부(DB 재조회로 실행가도 안 바뀜) · `log.warn` 스파이로 `write.denied` 한 번, 그 필드에 금액 키 없음(B-26), (c) 완료에서 비고만 바꾼 저장 거부, (d) 정산에서 환율 `1350`·수량 `2`·비고 undefined를 실어 실행가만 바꾼 저장 통과(A-21 헛거부 없음), (e) 진행 회귀 — 3줄 중 3번째 줄만 고쳐 저장 → 세 줄 순서 그대로(A-03), (f) 다른 프로젝트 줄 id를 섞은 배치 전부 거부 · 두 프로젝트 무변경(B-01).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/rules-gate.test.ts test/unit/domain/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/quote-lines-conflict.test.ts test/integration/leak-scan.test.ts test/integration/project-period.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 정산의 조작 페이로드가 통과했거나, 정규화 차이로 헛거부됐거나, 순서가 재설정됐거나, 다른 차수 줄이 섞였는데 저장됐거나, 기존 충돌 판정·04-22 합성 저장이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(boundaries 포함 — domain이 `ui`를 import하면 여기서 잡힌다)·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `edit-scope.ts`가 리포지토리·`ui`를 import하지 않고 셀 단계 유니온을 직접 선언하며, 게이트 규칙과 `listQuoteLines`가 같은 `lineCellEditability`를 부른다
    - 결정표 단위 테스트가 `behavior` 일곱 줄을 덮는다
    - `saveQuoteLines`의 tx 첫 단계가 `loadProjectForGate`이고, 바뀐 칸 판정이 DB 현재 값 대 정규화된 쓰기 페이로드다(baseline을 판정 입력으로 쓰지 않는다)
    - `lines.ts`에 `sortOrder: input.sortOrder ?? index` 꼴(배치 안 위치로 순서를 쓰는 식)이 없다
    - `findQuoteLinesByIds`·`updateQuoteLineIfVersionMatches`가 `revision_id` 조건을 가진다
    - `saveQuoteLines`에 권한·규칙 거부의 `write.denied` 호출 지점이 하나이고 통합 (b)가 그 경고와 금액 키 부재를 단언한다(B-26)
    - 통합 (a)~(f)가 DB 재조회로 단언하고 초록이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산 PM의 실행가 한 칸 편집이 판정 함수·게이트·잠금·정규화 비교·순서 보존·줄 소속·쓰기·DTO를 한 번에 관통하고, 나머지 칸은 서버에서 잠겼다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 정산 구조 매트릭스(새 줄 견적 0 · 삭제·이동·복제·새 차수 거부) · 줄 보관과 합계 제외 · 취소(견적가 0) · 연결 문서 조회 지점 · 입력 검증</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, test/unit/domain/quote-lines.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, repositories/archive.ts, repositories/projects.ts, app/(app)/projects/actions.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `repositories/archive.ts`(전문) — `ArchivableEntry`·`ARCHIVABLE_TABLES` 등록 형태(보관함 화면이 이 목록 하나로 복원한다)
    - `repositories/quote-lines.ts` 11–30행(`listQuoteLinesByRevision` — `archived_at` 조건 없음)
    - `repositories/projects.ts` 27–38행(`lineSumsSubquery` — 보관 줄 포함) · 150–200행(목록·합계가 그 하위 질의를 쓰는 자리)
    - `domain/quotes/lines.ts`의 `computeQuoteLineAmounts`(323–340행) · `saveQuoteLines`(Task 1이 바꾼 판정 자리)
    - `app/(app)/projects/actions.ts` 55–100행(`saveProjectLedgerAction` 스키마 — `projectId: z.string().min(1)` · `lineStatus: z.string()`)
    - 줄 상태 값의 정본 — Grep `cancelled`로 `domain/quotes/`에서 상태 목록 상수(또는 코드표 키)를 찾는다
  </read_first>
  <behavior>
    - `structuralEditability({ status, canWrite })`: 수주중·진행·미수주 + 쓰기 → `{ insert, archive, reorder, duplicate, newRevision }` 전부 참 · 정산 + 쓰기 → `insert`만 참(D10) · 완료 → 전부 거짓 · 쓰기 없음 → 전부 거짓
    - 게이트: 정산 + `insert`(견적 칸 0) → 통과 · 정산 + `insert`(수량 1 또는 단가 ≠ 0) → `정산 · 새 줄은 실행가만 적을 수 있음` · 정산 + `archive`/`reorder`/`duplicate` → `정산 · 줄 삭제·이동은 할 수 없음` · 완료 → `완료 · 견적 줄이 잠김`
    - 연결 문서 있는 줄의 `archive` → `연결 문서가 있어 삭제 대신 취소로 바꿔 주세요`(취소 상태로만)
    - `computeQuoteLineAmounts`에 상태 `cancelled`가 오면 견적가 0, 차익 = −실행가
  </behavior>
  <action>
① `edit-scope.ts`에 `structuralEditability({ status, canWrite })` → `{ insert, archive, reorder, duplicate, newRevision }`를 더한다(사용자 D10 — 정산은 `insert`만). `newRevision`은 차수를 만드는 04-14(그룹 B)가 새 차수 게이트의 입력으로 쓴다. 결정표를 먼저 쓴다(RED).

② **게이트 구조 판정.** `project.line-edit`의 `insert`·`archive`·`reorder`·`duplicate`를 `structuralEditability`로 판정하고, 정산의 `insert`는 `quoteCellsZero`(수량 = 0 그리고 단가 원화 0 — 통화 원화, 외화 금액·환율 없음)가 참일 때만 통과시킨다(사용자 D12 — 아니면 `정산 · 새 줄은 실행가만 적을 수 있음`). 연결 문서가 있는 줄의 `archive`는 `연결 문서가 있어 삭제 대신 취소로 바꿔 주세요`로 거부한다. `saveQuoteLines`가 새 줄(id 없음)을 `insert`로, 입력에 복제 표시(`duplicatedFrom?: string` — 없으면 새 줄)가 있으면 `duplicate`로, Task 1의 순서 변경을 `reorder`로 게이트에 싣는다. 구조 거부는 칸 오류가 아니라 `GateBlockedError`(게이트 이유 그대로)로 전체 거부한다.

③ **줄 보관과 합계 제외(A-04).** `repositories/archive.ts`의 `ARCHIVABLE_TABLES`에 `quote_line` 항목(`quote_lines` 표, 표시 이름 `견적 줄`, 이름 칸 `item_name`)을 더해 보관함 화면에 나오게 한다(복원 경로는 Task 3이 도메인 함수로 돌린다). `repositories/quote-lines.ts`에 `archiveQuoteLines(viewer, { ids, revisionId, archivedBy }, tx)`(그 차수 소속이고 아직 보관되지 않은 줄만, 반환 행 수 확인)를 더하고, `listQuoteLinesByRevision`에 `archived_at IS NULL`을, `findQuoteLinesByIds`의 결과에 보관된 줄이 있으면 `saveQuoteLines`가 `보관된 줄은 고칠 수 없습니다 · 새로 고쳐 주세요`로 전부 거부하게 한다. `repositories/projects.ts`의 `lineSumsSubquery`(목록·합계의 견적·실행가 합)에 `archived_at IS NULL`을 더한다(04-17이 같은 하위 질의의 돈 합계 캐스트를 고친다 — C-01). `saveQuoteLines` 입력에 `archivedLineIds?: string[]`를 더하고, 판정 단계에서 각 id에 `archive` 게이트를 부른 뒤 쓰기 단계에서 **같은 트랜잭션**으로 보관한다 — 성공한 저장의 행동 로그 detail에 보관한 줄 id를 싣는다(04-04의 「성공 한 줄 · 거부 0줄」 유지).

④ **취소(견적가 0)와 입력 검증(A-37).** `computeQuoteLineAmounts`가 `lineStatus`를 받아 `cancelled`면 견적가 0(수량·단가 컬럼은 그대로 — 이력 유지)으로 계산한다(PROJ-02). 기존 단위 테스트(`test/unit/domain/quote-lines.test.ts`)에 한 줄 더한다. `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마에서 `projectId`를 `z.string().uuid()`로, 두 `lineStatus`를 줄 상태 값 목록의 `z.enum`으로 좁히고 `quoteLines.archivedLineIds`(`z.array(z.string().uuid())`)를 더한다 — 줄 상태 목록은 도메인 상수에서 가져온다(새 목록을 만들지 않는다).

⑤ **연결 문서 조회 지점(D-66).** `domain/quotes/lines.ts`에 `linkedDocumentsByLine(viewer, revisionId): Promise<Map<string, { number: string }[]>>` 한 함수를 두고 이 페이즈에서는 빈 Map을 돌려준다(Phase 5가 이 함수만 채운다 — 주석에 한 줄). `listQuoteLines`·`saveQuoteLines`가 이 함수의 결과로 `hasLinkedDocuments`를 정한다. DTO에 `hasLinkedDocuments`를 싣는다(`registerDto` 갱신 — 누수 스캔).

⑥ **통합(`test/integration/quote-lines.test.ts`).** describe `정산 편집 매트릭스(D10·D12)` 계속: (g) 정산에서 견적 칸 0인 새 줄(실행가 300,000) 끼운 배치 통과 · DB에 견적가 0 · 실행가 300,000, (h) 정산에서 수량 1인 새 줄 배치 전부 거부(DB 무변경), (i) 정산에서 보관 거부 · 순서 변경(`sortOrder` 조작) 거부, (j) 완료에서 새 줄·보관 거부. describe `보관·취소(D-56·A-04)`: (k) 수주중에서 보관 뒤 DB에 행이 남고 `archived_at`이 채워지며 보관함 목록에 나온다 · `listQuoteLines`에 없다 · `listProjects`·`aggregateProjects`의 실행가·견적 합에서 빠진다, (l) 보관된 줄 id로 고치는 배치 거부, (m) 거부된 배치에서 보관도 안 됐다, (n) 취소 줄의 견적가 0, (o) `lineStatus: "아무글자"`·`projectId: "abc"` 액션 입력이 검증 오류(액션 스키마 단위 — `safeParse`).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/quote-lines.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 구조 판정·취소 견적가·게이트 표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/archive.test.ts test/integration/leak-scan.test.ts test/integration/projects-list.test.ts test/integration/project-period.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 정산 매트릭스가 사용자 D10·D12와 다르거나, 보관이 트랜잭션 밖에서 일어났거나, 보관 줄이 합계에 남았거나, 보관함 등록이 빠졌거나, DTO 등록이 누수 스캔을 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 네 줄을 덮고, `structuralEditability`의 정산 행이 `insert`만 참이다
    - 정산 새 줄 허용(g)·견적 칸 거부(h)·보관·순서 거부(i)가 DB 재조회로 초록이다
    - 보관이 `saveQuoteLines`와 같은 트랜잭션이고 거부된 배치에서 보관도 없다(통합 m)
    - `lineSumsSubquery`와 `listQuoteLinesByRevision`에 `archived_at IS NULL` 조건이 있고 (k)가 목록·합계 제외를 단언한다
    - `ARCHIVABLE_TABLES`에 `quote_line`이 있고 보관한 줄이 보관함 목록에 나온다
    - 액션 스키마의 `projectId`가 uuid, `lineStatus`가 enum이다(A-37)
    - `linkedDocumentsByLine`가 한 함수뿐이고 이 페이즈에서 빈 결과를 돌려준다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산에서 새 줄(견적 0)만 더해지고 구조 변경은 거부되며, 줄 삭제가 같은 트랜잭션의 보관으로 저장되고 보관 줄은 목록 합계에서 빠지며, 취소 줄의 견적가가 0이고 입력이 정해진 값만 받는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 보관함 복원의 도메인 검사(같은 트랜잭션) · 완료 계약 잠금 · 저장 대 전환 결정적 경합 테스트</name>
  <files>domain/archive/index.ts, repositories/archive.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, domain/projects/ledger.ts, domain/rules/register.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `domain/archive/index.ts` 40–90행(`archive`·`restore` — 지금 복원은 `setArchived` 한 번 + 로그, 트랜잭션·도메인 검사 없음)
    - `repositories/archive.ts` 40–56행(`ArchivableEntry` — `setArchived(viewer, id, value)`에 tx 인자 없음)
    - `domain/projects/ledger.ts`(04-22 산출물 전문 — tx 첫 단계 잠금 읽기와 `input.revenue.contract`가 `saveRevenue`로 가는 자리)
    - `test/integration/lock-race.ts`(04-20) · `test/integration/project-status.test.ts`의 경합 describe(04-20 — `afterLock`에 `deferred`를 거는 형태)
    - `test/integration/archive.test.ts` — 보관함 복원 통합 테스트 형태
  </read_first>
  <behavior>
    - 보관함에서 완료 프로젝트의 견적 줄 복원 → `완료 · 견적 줄이 잠김`으로 거부, 줄은 보관 그대로(DB 재조회)
    - 정산 프로젝트의 견적가 ≠ 0 줄 복원 → `정산 · 새 줄은 실행가만 적을 수 있음`으로 거부 · 견적가 0 줄 복원 → 통과
    - 진행 프로젝트 줄 복원 → 통과, 복원 로그 한 줄이 같은 트랜잭션
    - 완료 프로젝트에 계약 금액을 실은 합성 저장 → `완료 · 계약 금액이 잠김`, 아무것도 쓰지 않음
    - 경합(두 순서): 저장이 잠금 뒤 멈춘 동안 정산→완료 전환은 기다린다 → 저장 커밋(정산 규칙) 뒤 완료 · 전환이 잠금 뒤 멈춘 동안 저장은 기다린다 → 완료 커밋 뒤 저장은 `완료 · 견적 줄이 잠김`으로 거부 — 두 경우 모두 「완료 프로젝트에 편집이 들어감」이 없다
  </behavior>
  <action>
① **도메인 복원(A-19·OV-2).** `domain/quotes/lines.ts`에 `restoreQuoteLine(viewer, id, deps?: { now?; recordAction? })`를 둔다 — 한 `withTransaction` 안에서 줄(보관된 것 포함)을 찾고 → 차수의 프로젝트를 `loadProjectForGate(…, { tx })`로 잠그고 판정 → `project.line-edit`에 `{ kind: "restore", quoteAmountZero }`(정산은 견적가 0일 때만, 완료는 거부 — 복원은 그 상태에서 줄을 더하는 것과 같다) → `repositories/quote-lines.ts`에 더하는 `restoreQuoteLineRow(viewer, id, tx)`(보관 해제, 반환 행 수 확인) → `recordAction(viewer, { actionType: "restore", entity: "quote_line", entityId }, { tx })`. 게이트 거부면 Task 1과 같은 `write.denied` 경고 한 줄(규칙 · 줄 id, 금액 없음)을 남기고 던진다(B-26). 게이트 ctx가 객체라 줄 수 상한(04-26)·승인 합계(04-14, 그룹 B)가 같은 함수에 검사를 더한다. `domain/archive/index.ts`의 `restore`는 엔티티별 도메인 복원 함수 표 `DOMAIN_RESTORERS: Partial<Record<string, (viewer, id, deps) => Promise<void>>>`를 먼저 보고, 있으면(`quote_line` → `restoreQuoteLine`) **통째로 맡긴다** — 범용 `setArchived` 경로는 표에 없는 엔티티만 탄다(리저브 복원도 같은 표에 04-07이 더한다 — 그룹 B). `repositories/archive.ts`의 `quote_line` 항목에는 범용 복원이 타지 않는다는 주석 한 줄을 둔다.

② **완료 계약 잠금(A-12).** `domain/projects/ledger.ts`가 tx 첫 단계에서 얻은 잠근 행이 완료이고 `input.revenue?.contract`가 실려 있으면 `project.line-edit`의 `{ kind: "contract" }`로 게이트 → `완료 · 계약 금액이 잠김` 전체 거부(`domain/rules/register.ts`에 그 한 갈래). 계약 칸은 그룹 B의 04-16·04-41이 견적 파생으로 바꿀 때 없어진다.

③ **경합 테스트(A-06·OV-3, `test/integration/quote-lines.test.ts` describe `잠금·경합`).** `saveQuoteLines`의 `deps.afterLock`에 `deferred`를 걸어 멈춘 연결 A(정산 프로젝트의 실행가 저장)와 연결 B(`changeProjectStatus` 정산→완료)로: (p) A 먼저 — B가 `waitForLockWaiter`로 대기에 든 것을 확인 → A를 풀면 A 커밋(실행가 반영) → B 커밋(완료) · (q) B 먼저(`changeProjectStatus`의 `afterLock`에서 멈춤) — A가 대기에 든 것을 확인 → B를 풀면 완료 커밋 → A는 `완료 · 견적 줄이 잠김`으로 거부, 실행가 무변경. 두 순서 모두 최종 DB에 「완료 상태에서 바뀐 줄」이 없다. (r) 복원 대 전환: 같은 두 순서로 `restoreQuoteLine`과 정산→완료 — 완료 뒤에 복원이 들어가지 않는다. (s) 완료 계약 저장 거부(DB 무변경). (t) 보관함 복원 세 케이스(`behavior` 앞 세 줄, DB 재조회).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 복원·계약 갈래의 게이트 판정이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/archive.test.ts test/integration/project-status.test.ts test/integration/project-period.test.ts test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 복원이 도메인 검사를 우회했거나, 경합 순서가 결정적이지 않거나(고정 지연), 완료 프로젝트에 편집·복원·계약이 들어갔거나, 다른 엔티티의 보관함 복원이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/archive/index.ts`의 `restore`가 `DOMAIN_RESTORERS`에 있는 엔티티를 도메인 함수에 맡기고, `restoreQuoteLine`이 잠금·게이트·보관 해제·로그를 한 `withTransaction`에서 한다
    - 보관함 복원 세 케이스(완료 거부 · 정산 견적가 조건 · 진행 통과)가 DB 재조회로 초록이고, 기존 `archive.test.ts`(다른 엔티티)가 수정 없이 초록이다
    - 경합 (p)(q)(r)이 `deferred`·`waitForLockWaiter`로 순서를 정하고 `setTimeout` 고정 지연이 없다
    - 완료 계약 저장 거부(s)가 초록이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>보관함 복원이 저장과 같은 규칙을 한 트랜잭션에서 지나고, 저장·복원·전환이 프로젝트 행 잠금으로 직렬화되어 완료 프로젝트에 어떤 변경도 성공으로 들어가지 않음이 결정적 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장 | 줄 값·새 줄·보관 id·순서·줄 상태·차수 id가 넘어온다. 셀 단계·구조·소속 판정이 서버에만 있어야 한다 |
| 보관함 화면 → 복원 | 범용 복원 경로가 도메인 잠금을 모른다 |
| 동시 요청 → 프로젝트 행 | 저장·전환·복원이 같은 프로젝트를 동시에 바꾼다 |
| 연결 문서 조회 → 금액 칸 | Phase 5가 채울 조회 결과가 금액 칸의 읽기 전용 판정을 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-58 | Elevation of Privilege | 정산·완료의 잠긴 칸 | high | mitigate | 서버가 DB 현재 값과 정규화된 쓰기 페이로드를 비교해 바뀐 칸을 정하고 `project.line-edit`가 칸마다 판정한다 — 클라이언트 baseline·화면 셀 단계를 믿지 않는다(A-21). 조작 페이로드 거부를 통합 테스트로 고정한다 |
| T-04-59 | Tampering | 보관 id·줄 id 목록 | high | mitigate | 찾기·고치기·보관이 모두 `revision_id` 조건이고 결과 행 수를 확인해 다르면 전부 거부한다(B-01 줄 소속 — 04-12가 보관 id에만 걸던 것을 모든 줄로 넓힘). 보관된 줄 id 수정도 거부한다(A-04) |
| T-04-61 | Repudiation | 줄 삭제·복원 | medium | mitigate | 물리 삭제 없이 보관(`archived_at`·`archived_by`)하고 저장 로그 detail에 보관 id를 남긴다. 복원 로그는 복원과 같은 트랜잭션이다 |
| T-04-62 | Tampering | 연결 문서 줄의 금액 | medium | mitigate | 연결 문서 조회를 한 함수로 두고 그 결과로 금액 칸을 읽기 전용으로 판정한다. Phase 5가 조회를 채우면 같은 판정이 곧바로 적용된다 |
| T-04-166 | Elevation of Privilege | 보관함 범용 복원 | high | mitigate | (CEO A-19·OV-2) 견적 줄 복원은 `DOMAIN_RESTORERS`로 도메인 함수에 맡겨 잠금·게이트를 저장과 같은 트랜잭션에서 지난다 — 완료 줄 복원 거부를 통합 t로 고정한다 |
| T-04-167 | Tampering | 저장 대 전환 경합 | high | mitigate | (CEO A-06·OV-3) 프로젝트 행 `FOR UPDATE`로 저장·복원·전환을 직렬화하고, 두 연결 결정적 경합 테스트(p·q·r)로 「완료 프로젝트에 편집 성공」이 없음을 고정한다 |
| T-04-168 | Tampering | 정산 새 줄의 견적 금액 | medium | mitigate | (사용자 D12) 정산의 `insert`는 수량 0 · 단가 원화 0일 때만 게이트를 통과한다 — 화면 잠금과 별개로 서버가 판정한다(통합 h) |
| T-04-174 | Repudiation | 거부된 쓰기의 운영 기록 | low | mitigate | (CEO B-26 · 사용자 D19-3) 정산 칸·구조·줄 소속·복원·완료 계약 거부가 화면에만 보이던 빈틈을 `saveQuoteLines`·`restoreQuoteLine`의 한 거부 지점 `write.denied` 경고(보는 사람·규칙·대상 id, 금액 없음)로 메운다 — 통합 (b) 스파이 단언 |
| T-04-169 | Tampering | 줄 상태·프로젝트 id 입력 | low | mitigate | (CEO A-37) 액션 스키마가 줄 상태를 enum, 프로젝트 id를 uuid로 좁혀 임의 문자열이 금액 규칙(`cancelled`)에 닿지 않는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `quote-edit-scope`·`quote-lines`·`rules-gate` 초록
- 통합 `quote-lines`(매트릭스·보관·복원·경합)·`archive`·`projects-list`·`project-period`·`leak-scan` 초록
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 정산 PM은 실행가 수정과 견적 0 새 줄 추가만, 완료는 아무것도 서버를 통과하지 못하고, 판정은 DTO와 같은 함수다
- 줄 삭제가 보관으로 저장되고 보관 줄이 목록 합계에서 빠지며, 복원이 같은 규칙을 지난다
- 저장·복원·전환이 한 프로젝트에서 겹쳐도 결과가 한 가지다
- Phase 5는 연결 문서 조회 한 함수만 채우면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/quotes/edit-scope.ts` — `QuoteCellEditability`(유니온) · `QUOTE_LINE_FIELDS` · `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT` · `lineCellEditability` · `structuralEditability`(`{ insert, archive, reorder, duplicate, newRevision }`) |
| 게이트 규칙 | `project.line-edit`(ctx 확장: `change` 일곱 종류 — update · insert · archive · reorder · duplicate · restore · contract — 와 `hasLinkedDocuments`) |
| 함수(신규) | `linkedDocumentsByLine`(연결 문서 조회 지점, 이 페이즈 빈 결과) · `restoreQuoteLine`(domain) · `archiveQuoteLines`(repository) · `DOMAIN_RESTORERS`(`domain/archive/index.ts`) |
| DTO 필드 | `QuoteLineDto.cellEditability` · `QuoteLineDto.hasLinkedDocuments` |
| 입력 계약 | `saveQuoteLines(…, { archivedLineIds })` · 새 줄 `duplicatedFrom?` · `saveProjectLedgerAction` 스키마 `quoteLines.archivedLineIds`(uuid 배열) · `projectId` uuid · `lineStatus` enum |
| 보관함 등록 | `ARCHIVABLE_TABLES`에 `quote_line`(복원은 `DOMAIN_RESTORERS`) |
| 운영 로그 | `write.denied`(경고 — `saveQuoteLines`·`restoreQuoteLine`의 한 거부 지점, 금액 없음 · 04-26·04-13·04-40이 같은 지점을 지난다) |
| 변경된 기존 심볼 | `saveQuoteLines`(tx 첫 단계 `loadProjectForGate` · 정규화 비교 · 순서 보존 · 줄 소속 · `deps.afterLock`) · `findQuoteLinesByIds`·`updateQuoteLineIfVersionMatches`(`revision_id` 조건) · `listQuoteLinesByRevision`·`lineSumsSubquery`(보관 줄 제외) · `computeQuoteLineAmounts`(취소 → 견적가 0) · `listQuoteLines`(셀 단계 계산) · `saveProjectLedger`(완료 계약 거부) · `restore`(도메인 복원 위임) |
| 테스트 | `test/unit/domain/quote-edit-scope.test.ts`(신규) · `test/integration/quote-lines.test.ts`·`test/unit/domain/quote-lines.test.ts` 확장 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 화면 반영 전부(셀 단계 격자 · 표 위 한 줄 · 힌트 줄 · EMPTY · 구조 컨트롤 · 삭제·취소 모달 · 순서 전송 · D-77 버튼 부재 · S4 backstop 둘 — **04-30**) · 차수당 줄 수 상한(04-26) · 현재 차수 판정·승인 축·새 차수 게이트(04-14, 그룹 B) · 리저브 복원(04-07, 그룹 B) |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-03 — 저장마다 순서 재설정 · 정산에서 순서 헛판정 | CRITICAL | Plan 04-12, Task 1 ③(d): 미전송이면 현재 값 유지 · 통합 a·e (클라이언트 전송은 04-30) |
| A-04 — 보관 줄이 목록 합계·상세에 남음 | CRITICAL | Plan 04-12, Task 2 ③: `listQuoteLinesByRevision`·`lineSumsSubquery` 제외 · 보관 줄 수정 거부 · 통합 k·l |
| A-06 / OV-3 — 잠금 없는 게이트 · FOR SHARE로는 부족 | CRITICAL | Plan 04-12, Task 1 ③(a) `loadProjectForGate`(FOR UPDATE) · Task 3 ③ 두 연결 경합 p·q·r |
| A-12 — 완료 계약 편집 | WARNING | Plan 04-12, Task 3 ② (1차 렌더 `canSave`는 04-22) |
| A-19 / OV-2 — 범용 복원이 도메인 검사 우회 | WARNING | Plan 04-12, Task 3 ①: `DOMAIN_RESTORERS` + `restoreQuoteLine` 한 tx · 통합 t |
| A-21 — 정규화 차이로 헛거부 | WARNING | Plan 04-12, Task 1 ③(c) · 통합 d |
| A-26 / 사용자 D10 — 정산 줄 추가 허용, 삭제·이동·새 차수 거부 | DECISION | Plan 04-12, Task 2 ①②: `structuralEditability` 정산 = `insert`만 · 통합 g·i |
| A-26a / 사용자 D12 — 새 줄 견적 칸 0 잠금 | DECISION | Plan 04-12, Task 1 ① `isNewLine` 행 · Task 2 ② `quoteCellsZero` · 통합 h · 금지 항목 2 |
| A-32 — domain의 `ui` import 금지 | NOTE | Plan 04-12, Task 1 ①: 유니온을 domain이 선언 |
| A-37 — 줄 상태·프로젝트 id 검증 | NOTE | Plan 04-12, Task 2 ④ · 통합 o |
| B-26 / 사용자 D19-3 — 거부된 쓰기가 운영 기록에 없음 | DECISION | Plan 04-12, Task 1 ③(e) 한 거부 지점 `write.denied` · Task 3 ① 복원 거부 · 통합 b 스파이 · T-04-174 (04-13 조정 거부·04-26 상한 거부가 같은 지점을 지난다) |
| B-01 (04-12 부분) — 줄 소속 | CRITICAL | Plan 04-12, Task 1 ③(b): `revision_id` 조건 + 행 수 확인 · 통합 f (현재 차수 판정은 04-14, 그룹 B) |
| OV-1 (04-12 부분) — 승인 합계 불변 | HIGH | Plan 04-12: 판정 입력을 객체로 두고 복원까지 같은 게이트를 지나게 해 04-14가 승인 축 하나로 삽입·취소·보관·복원을 막게 함(그룹 B가 구현) |
| A-33 — 흩어진 선판정 | NOTE | `loadProjectForGate` 재사용(04-11) |
| OV-7 / 사용자 D21 | HIGH | 서버 파일만 고쳐 화면 쪽(04-30)과 분리 — 견적 표 파일을 이 플랜에서 고치지 않는다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-01 현재 차수 판정 · 새 차수 대 저장 · 승인 대 저장 경합 | 차수가 여럿이 되는 04-14(그룹 B 담당)에서 — 이 플랜 시점에는 차수가 하나라 현재 차수 불일치가 생기지 않는다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md` when done
</output>
