---
phase: 04-project-quote-ledger
plan: 13
type: execute
wave: 14
depends_on: ["04-12", "04-26"]
files_modified:
  - db/schema/quote-lines.ts
  - db/migrations/0014_quote_line_kind.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0014_snapshot.json
  - domain/permissions/menus.ts
  - domain/quotes/edit-scope.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/actions.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - test/unit/domain/quote-lines.test.ts
  - test/integration/quote-line-kinds.test.ts
  - test/integration/projects-list.test.ts
autonomous: true
requirements: [PROJ-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "견적 줄에는 종류가 셋이다 — 견적 줄(`quote`) · 견적 외 비용(`out_of_quote`) · 조정(`adjustment`). 종류는 한 컬럼 `quote_lines.line_kind`가 정본이고 줄을 만든 뒤에는 바뀌지 않는다(D-48·D-83 계획 재량 — 아래 assumption_delta 결정)"
    - "새 마이그레이션의 번호는 적용 직전 journal 최고 번호 + 1이고 파일명·snapshot·journal tag가 생성기 출력과 같다 — 계획 번호는 0014다(0011 04-10 → 0012 04-06 → 0013 04-27 `role_work_scope` → 0014 이 플랜 → 0015 04-41 → 0016 04-07). 생성기가 다른 번호를 주면 실제 이름이 SUMMARY에 있다"
    - "`line_kind`의 세 값 CHECK는 `ADD COLUMN` 문장 안 인라인 제약이고 `pnpm lint:sql`이 0건이다 — 별도 제약 추가 문장·같은 파일 안 NOT VALID 뒤 검증 형태는 squawk가 거부하는 것이 확인됐다(CEO 리뷰 B-22)"
    - "조정 줄은 권한표 `projects.adjustment` 쓰기(경영관리 — 관리자가 배정)가 있는 사람만 만들고 고치고 지운다 — 프로젝트 상태와 무관하다(정산·완료 포함)(D-83)"
    - "상태 판정은 줄마다 한다 — `saveQuoteLines` 입구에 배치 단위 상태 게이트가 남아 있지 않아, 경영관리의 완료 프로젝트 조정 줄 저장이 입구에서 `완료 · 견적 줄이 잠김`으로 막히지 않는다(CEO 리뷰 B-36)"
    - "조정 줄 규칙에 걸린 거부(PM의 조정 칸 변경·보관·조정 새 줄, 조정 권한만 있는 사람의 견적 줄 변경)는 화면 거부와 함께 서버 로그에 경고 `write.denied` 한 줄(보는 사람 id · 규칙 이름 · 프로젝트·차수·줄 id — 금액 없음)을 남긴다 — 04-12가 만든 `saveQuoteLines`의 한 거부 지점을 지나고 새 로그 호출 지점을 만들지 않는다(사용자 D19-3, CEO 리뷰 B-26)"
    - "담당 PM에게 조정 줄은 어느 상태에서든 셀 단계가 전부 잠김으로 서버에서 오고, 조정 줄 칸을 고친 값을 실어 보내면 `조정 줄 · 경영관리만 고칠 수 있음`으로 배치 전부가 거부된다(D-83, Copywriting Error — 셀(조정 줄, PM) — 화면 렌더는 04-23)"
    - "조정 줄은 견적가 0 · 실행가 = 금액(음수 허용)이고 서버가 수량 1·단가 0으로 저장해 견적가 0을 계산한다(D-83)"
    - "조정 줄의 실행가는 합계 행·차익·목록의 실행가·수익금에 포함된다 — 상세 합계와 목록 집계가 같은 금액을 보인다(D-83·D-87)"
    - "견적 외 비용 줄은 별도 표가 아니라 견적가 0인 견적 줄의 한 종류다 — 담당 PM이 줄을 추가할 수 있는 상태(수주중·진행·미수주, 그리고 사용자 D10으로 정산)에서 새 줄로 만들 수 있고, 실행가 음수를 허용한다(D-48, EXP-14, ROADMAP 기준 2 — 버튼·그룹 화면은 04-23)"
    - "견적 외 비용 줄은 견적 줄처럼 04-12의 편집 범위를 따른다 — 정산 = 실행가만 고치고 새 줄 추가는 되지만 삭제·순서 이동은 서버가 거부(사용자 D10 — D-78 개정), 정산에서 만든 새 줄의 견적 칸은 0으로 잠김(사용자 D12 — 견적 외 비용은 원래 수량·단가가 잠겨 있어 같은 결), 완료 = 잠김. 조정 줄만 상태 예외다(D-83)"
    - "견적 줄(`quote`)의 실행가는 여전히 음수를 받지 않는다 — 음수는 견적 외 비용·조정 두 종류만(EXP-14 「매출·조정 항목은 음수 허용」)"
    - "차수 줄 수 상한(04-26)은 조정 줄도 센다 — 상한에 닿은 차수에서 경영관리의 조정 줄 추가도 서버가 거부한다(S4)"
  prohibitions:
    - statement: "조정 줄이 PM에게 보이지 않는 채로 실행가·수익금을 바꿔서는 안 된다. 이 시스템의 핵심은 기획본부와 경영관리가 같은 숫자를 보는 것이다 — 경영관리의 원가 보정은 PM 화면의 같은 표 안에 행으로 보이고(읽기 전용), 합계가 그 행을 포함한다는 것이 눈으로 확인돼야 한다. 조정을 숨긴 별도 표·숨은 합계 가산 경로를 만들지 않는다(서버·합계 쪽은 이 플랜의 불변식 테스트, 화면 쪽은 04-23 E2E)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/0014_quote_line_kind.sql"
      provides: "quote_lines.line_kind(기본 quote, 세 값 CHECK) — 기존 줄은 전부 quote"
      contains: "line_kind"
    - path: "domain/quotes/edit-scope.ts"
      provides: "셀 단계·구조 판정에 줄 종류 × 조정 권한 축 추가"
      exports: ["QUOTE_LINE_KINDS", "lineCellEditability", "structuralEditability"]
    - path: "test/integration/quote-line-kinds.test.ts"
      provides: "조정 줄 권한·상태 무관·PM 거부·음수·견적가 0 · 견적 외 비용 · 종류 불변 · 상한·보관에 조정 줄 포함"
      min_lines: 60
  key_links:
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "`project.line-edit`가 줄 종류와 조정 권한을 싣고 같은 판정 함수를 부른다"
      pattern: "lineKind"
    - from: "repositories/projects.ts"
      to: "db/schema/quote-lines.ts"
      via: "목록 실행가 합계가 현재 차수의 모든 종류 줄을 더한다(조정 포함) — 불변식 테스트로 고정"
      pattern: "execution"
    - from: "domain/quotes/lines.ts"
      to: "domain/permissions/menus.ts"
      via: "저장 입구 권한이 `projects` 쓰기 또는 `projects.adjustment` 쓰기다 — 줄마다의 판정은 게이트"
      pattern: "projects.adjustment"
---

<objective>
**As a** 경영관리 담당자와 기획 PM, **I want to** 경영관리는 외화 송금 수수료 같은 비용을 프로젝트 상태와 상관없이 「조정」 줄로 넣고, PM은 견적에 없던 비용을 「견적 외 비용」 줄로 붙이며, 서버가 종류마다 권한·음수·상태 규칙을 판정하기를, **so that** 견적 밖에서 생긴 실제 원가가 전부 같은 원장·같은 합계에 들어가고 PM이 경영관리의 보정을 바꾸는 길이 없다.

이 플랜은 04-12의 편집 범위 함수에 **줄 종류 축**을 더한다(D-83 조정 · D-48 견적 외 비용). 04-12가 상태 × 계급 × 연결 문서로 셀 단계를 판정했고, 여기서 종류(견적 줄 · 견적 외 비용 · 조정)와 조정 권한이 한 함수에 들어간다. 옛 04-06의 「완료 뒤 경영관리의 견적 외 비용 줄 예외」(D-47 옛 형태)는 D-83의 조정 줄이 대체한다 — **옛 형태(완료에서만 쓰는 견적 외 비용 예외 플래그)를 되살리지 않는다.** **2026-09-23 체커 1차 분할:** 옛 04-13(19파일 — Task 1 하나가 15파일)을 서버와 화면으로 나눴다. 이 플랜은 **서버 쪽**(마이그레이션 · 권한표 메뉴 · 종류 축 판정 · 게이트 · 저장 규칙 · DTO · 합계 불변식)이고, 조정·견적 외 비용 그룹·버튼·PM 읽기 전용 화면·삭제 모달·소분류 설명 힌트는 **04-23**이 이 DTO 위에 그린다.

Purpose: 조정 줄은 목록의 실행가·수익금(D-87)과 계약 이후 원가에 들어가는 돈이다. 종류가 소분류 문자열에 숨어 있으면 그룹·권한·복사 제외(04-14·04-15)가 전부 문자열 비교에 매달린다.

Output: 마이그레이션 0014(`line_kind` — 04-27의 0013 뒤) · 권한표 메뉴 `projects.adjustment` · 편집 범위 종류 축 · 게이트 ctx 확장 · 저장 규칙(입구 권한 · 종류 불변 · 음수 허용 종류 · 견적가 0) · DTO `lineKind` · 통합 1 · 불변식 테스트.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 부록 B).** (1) **B-22:** 드리즐이 만드는 별도 CHECK 제약 문장도, 옛 계획의 대안(같은 파일 안 NOT VALID 뒤 검증)도 squawk가 거부한다 — `ADD COLUMN` 안 인라인 CHECK로 손으로 고친다(스냅숏의 끝 상태는 같다). (2) **B-36:** `saveQuoteLines` 입구의 배치 단위 상태 게이트를 줄 단위 판정으로 바꾼다. (3) **사용자 D10·D12:** 정산에서도 PM이 새 줄(견적 칸 0)을 추가할 수 있다 — 견적 외 비용 줄의 정산 판정을 그에 맞춘다(옛 「정산에서 견적 외 비용 새 줄 거부」 케이스를 뒤집는다). (4) **사용자 D19-3 · B-26:** 조정 규칙에 걸린 거부를 서버 로그 경고로 남긴다. (5) **B-18:** 목록 집계의 SUM이 아직 문자열로 올 수 있어(04-17 C-01이 고친다) 불변식 테스트는 값 동일성을 숫자로 바꿔 비교한다. (6) **OV-2:** 04-12가 만든 견적 줄 복원 검사에 줄 종류·조정 권한을 싣는다. 마이그레이션 번호는 0014다(04-27의 `0013_role_work_scope` 뒤 — 오케스트레이터가 확정한 순서).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-26-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 761행(「조정」 줄 — is_adjustment 권장과 근거 셋)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 419행(quote_lines 컬럼 추가 배정)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1116–1136행(상태별 셀 단계 · 조정 줄 행 모양·삭제·복사·상한) · 338–339행(Copywriting 셀 잠김·조정 줄 PM)
</context>

<assumption_delta_decision>
- **신호:** 한 종류였던 견적 줄에 두 번째·세 번째 종류(견적 외 비용 · 조정)가 생겼다(pluralization). 감지기(`assumption-delta scan`)는 `custom_fields` 문구에서 `chosen` 신호를 냈지만 그 문구는 이 플랜과 무관하다 — `custom_fields`는 이 플랜에서 바뀌지 않는다(no-change).
- **주 명사:** 줄 종류(`line_kind`).
- **결정: promote.** 리서치 권장은 불리언 `is_adjustment`를 **곁에 더하는** 것이었고, 견적 외 비용은 소분류 문자열로 가르는 안이었다. 그러면 「종류」가 불리언 하나와 소분류 매직 문자열 둘에 나뉘어 산다(add-alongside). 이 플랜은 한 컬럼 `line_kind`(`quote` · `out_of_quote` · `adjustment`, CHECK)를 정본으로 올리고, 그룹 머리글·권한·음수 허용·복사 제외가 전부 이 컬럼 하나를 본다. 소분류(`subcategory`)는 `quote` 종류의 세부 분류로 내려간다 — 나머지 두 종류의 소분류 칸은 종류 값을 그대로 적어 NOT NULL을 지킬 뿐 판정에 쓰지 않는다.
- **불변식 테스트(권장 채택):** 「모든 종류의 줄이 저장 → 상세 합계 → 목록 집계를 같은 금액으로 지난다」 — 뒤 페이즈가 특정 종류를 합계에서 빼는 회귀를 곧바로 잡는다(Task 2).
</assumption_delta_decision>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-02(견적 외 비용 줄 · 조정 줄 — 서버·합계)** 다. 수용 기준은 D-48·D-83·EXP-14·ROADMAP 기준 2와 UI-SPEC S4에서 끌어왔다. S4 `## UI Considerations`의 「조정 줄 추가 상한」 partial은 서버 쪽 truths(상한이 조정 줄을 센다)로 여기, 화면 쪽 truths와 조정 줄이 붙은 표의 overflow backstop은 04-23으로 옮겼다. 「조정 줄과 복사」(⚠ S4)는 복사를 만드는 04-14(새 차수)·04-15(프로젝트 복사)가 옮긴다.

**계획 단계 가정(표면화):**
- **경영관리 = 권한표 `projects.adjustment` 쓰기.** 시드 계급에 경영관리가 없어 기본값은 아무에게도 켜지 않는다(04-02 `projects.revenue`와 같은 결 — 관리자가 실제 담당 계급에 배정). 발행·입금 쓰기(`projects.revenue`)와 한 메뉴로 묶지 않는 이유: 매출 기록과 원가 보정은 다른 일이고, 한쪽만 맡는 사람이 생길 수 있다.
- **조정 줄의 저장 권한:** 조정 권한만 있고 `projects` 쓰기가 없는 사람도 조정 줄을 저장할 수 있어야 한다. 그래서 `saveQuoteLines`의 입구 권한 판정을 「`projects` 쓰기 또는 `projects.adjustment` 쓰기」로 넓히고, 줄마다의 판정은 게이트가 한다(조정 권한만 있는 사람의 견적 줄 변경은 게이트가 잠김으로 거부).
- **조정 줄의 순서:** 조정 그룹 안에서는 만든 순서이고 그룹 밖으로 옮길 수 없다(고정 그룹 — 구조 판정 `move` 불가, 화면 쪽은 04-23).
- **누수 스캔:** `QuoteLineDto`에 `lineKind`를 더하는 등록은 `lines.ts` 안이라 `test/integration/leak-scan.test.ts`는 **돌려 확인만** 한다(`files_modified`에 두지 않았다 — 고칠 일이 생기면 SUMMARY에 적는다).
- **차수·줄 소속 검사(CEO 리뷰 B-01):** 게이트가 보는 상태는 04-12가 트랜잭션 첫 단계에서 `loadProjectForGate`로 잠근 프로젝트 행(OV-3)에서 온다. 「줄이 보낸 차수 소속인가」(`revision_id` 조건)는 04-12가 이미 검사하고, 「보낸 차수가 현재 차수인가」 재확인과 차수가 둘일 때의 증명은 04-14 뒤의 04-40이 넣는다 — 이 플랜의 차수는 여전히 1차 하나다.
- **정산의 새 줄(사용자 D10·D12):** 정산 구조 판정(추가 가능 · 삭제·이동 불가 · 새 줄 견적 칸 0 잠금)은 04-12의 `structuralEditability`·`lineCellEditability`가 정한다. 이 플랜은 견적 외 비용 종류가 그 판정을 그대로 따르게만 한다 — 견적 외 비용은 수량·단가가 원래 잠겨 있어 D12의 「견적 칸 0」과 저절로 맞는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 경영관리가 완료 프로젝트에 조정 줄 한 줄을 저장하고 목록 집계에 들어간다(마이그레이션 → 권한표 → 종류 축 → 게이트 → 저장 → DTO → 통합)</name>
  <files>db/schema/quote-lines.ts, db/migrations/0014_quote_line_kind.sql, db/migrations/meta/_journal.json, db/migrations/meta/0014_snapshot.json, domain/permissions/menus.ts, domain/quotes/edit-scope.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, test/integration/quote-line-kinds.test.ts</files>
  <reversibility rating="costly">`line_kind` 컬럼과 세 값은 Phase 6(경영관리 대리 등록의 견적 외 비용 줄, EXP-16)·Phase 9(손익 원가)가 읽는다 — 이름·값을 바꾸면 마이그레이션과 그 페이즈들의 조회가 따라온다. 지금 데이터는 전부 demo라 one-way는 아니다.</reversibility>
  <read_first>
    - `db/schema/quote-lines.ts`(전문) · `db/migrations/0009_project_quote_ledger_spine.sql` 55–70행(`line_status` 정의 — 같은 결의 text 컬럼)
    - `db/migrations/meta/_journal.json` — 마지막 항목의 `idx`·`tag`(이 플랜의 번호 = 그 `idx` + 1 — 04-06의 상태 마이그레이션 뒤)
    - `.squawk.toml`(전문) — 제외 규칙 넷(`adding-required-field` 포함). CHECK 제약 추가가 걸리면 SQL을 고친다
    - `domain/permissions/menus.ts`(전문) — 04-20이 더한 두 메뉴와 주석
    - `domain/quotes/edit-scope.ts`·`test/unit/domain/quote-edit-scope.test.ts`(04-12 산출물 전문)
    - `domain/rules/register.ts`의 `project.line-edit`·`quote.line-cap`(04-12·04-26 산출물)
    - `domain/quotes/lines.ts` — 입구 권한 판정(`can(viewer, "projects", "write")`) · 형식 오류(실행가 음수 거부) · `computeQuoteLineAmounts` · DTO
    - `repositories/projects.ts` 24–40행(`lineSumsSubquery` — 목록 합계가 줄 종류와 무관하게 더한다는 것을 확인만)
    - `test/integration/quote-lines.test.ts`(전문) — 저장 통합 테스트 형태와 계급 권한을 켜는 준비
  </read_first>
  <behavior>
    - 경영관리(`projects.adjustment` 쓰기만) viewer가 완료 프로젝트 현재 차수에 조정 새 줄(실행가 −120,000)을 저장 → 통과, 견적가 0 · 수량 1 · 단가 0 · 차익 = 0 − (−120,000) = 120,000(서버 계산)
    - 같은 프로젝트의 `listProjects` 실행가가 조정 줄만큼 바뀐다(DB 집계)
    - 조정 권한이 없는 담당 PM이 같은 새 줄을 보내면 `조정 줄 · 경영관리만 고칠 수 있음`으로 거부, DB 무변경
  </behavior>
  <action>
① **[BLOCKING] 마이그레이션.** `db/schema/quote-lines.ts`에 `lineKind: text("line_kind").notNull().default("quote")`와 CHECK 제약(세 값)을 더하고 `pnpm db:generate --name quote_line_kind`로 만든다. **번호 규칙:** 계획 번호는 0014이고(04-27의 `0013_role_work_scope` 뒤 — 실행 직전 journal 최고 `idx`는 13), 생성기가 매기는 번호는 적용 직전 journal의 최고 `idx` + 1이어야 한다. 생성기가 다른 번호를 주면 파일명·snapshot·journal tag를 생성기 출력 그대로 두고(손으로 번호를 바꾸지 않는다) 실제 이름을 SUMMARY에 적는다. 생성 SQL 앞에 락 타임아웃 한 쌍. 기존 줄은 기본값으로 전부 `quote`가 된다. **CHECK 형태(CEO 리뷰 B-22 — squawk 2.65 실측):** 생성기는 컬럼 추가와 별도 문장으로 제약을 더하는데 squawk가 그 형태를 거부하고, 같은 파일 안에서 NOT VALID로 더한 뒤 검증하는 형태도 「같은 트랜잭션에서 모든 읽기를 막는다」로 거부한다. 생성된 SQL을 손으로 고쳐 제약을 **컬럼 추가 문장 안에 인라인**으로 둔다 — `ALTER TABLE "quote_lines" ADD COLUMN "line_kind" text DEFAULT 'quote' NOT NULL CONSTRAINT "quote_lines_line_kind_check" CHECK ("line_kind" IN ('quote', 'out_of_quote', 'adjustment'))` 한 문장(제약 이름은 스냅숏의 이름과 같게). 끝 상태가 스냅숏과 같으므로 다음 생성에서 차이가 나지 않는다. `.squawk.toml`은 고치지 않는다. `bash scripts/dev-db.sh && pnpm db:migrate`.

② **권한표.** `domain/permissions/menus.ts`에 `{ key: "projects.adjustment", label: "견적 조정 줄" }`을 더한다(시드 기본값 없음 — `<probe_fallback>` 가정, 04-02 주석과 같은 결로 이유 한 줄).

③ **종류 축(트레이서에 필요한 만큼).** `edit-scope.ts`에 `QUOTE_LINE_KINDS = ["quote","out_of_quote","adjustment"] as const`와 판정 입력 `lineKind`·`canAdjust`를 더한다: 조정 줄은 상태를 보지 않고 `canAdjust`로만 판정(칸: 항목·거래처·실행가·비고만 `edit`, 나머지 `locked` / 구조: 추가·삭제 가능, 이동 불가), 권한 없으면 전부 `locked` — 잠김 이유는 `조정 줄 · 경영관리만 고칠 수 있음`. `project.line-edit`의 ctx에 `lineKind`·`actorCanAdjust`를 더하고 같은 함수를 부른다. 견적 외 비용의 전체 규칙과 결정표는 Task 2.

③-b **입구의 배치 단위 상태 게이트 제거(CEO 리뷰 B-36).** `domain/quotes/lines.ts`의 `saveQuoteLines` 입구(옛 `project.completed-lock` 자리 — 04-06이 `project.line-edit`로 바꿨고 04-12가 줄 단위로 넓혔다)를 Grep으로 확인해, 줄 종류를 싣지 않은 채 배치 전체에 상태를 판정하는 게이트 호출이 남아 있으면 없앤다. 상태 판정은 줄마다 `lineKind`·`actorCanAdjust`를 실은 `project.line-edit` 호출 하나뿐이어야 한다. 남겨 두면 경영관리의 완료 프로젝트 조정 줄 저장이 입구에서 `완료 · 견적 줄이 잠김`으로 막힌다 — `behavior` 첫 줄이 이 회귀를 잡는다. 04-12가 이미 줄 단위만 남겼으면 확인만 하고 SUMMARY에 적는다.

④ **저장.** `saveQuoteLines`: 입구 권한을 「`projects` 쓰기 또는 `projects.adjustment` 쓰기」로 넓힌다(둘 다 없으면 기존 `ForbiddenError`). `QuoteLineWriteRow`에 새 줄에서만 쓰는 `lineKind?`를 더하고, 조정 줄은 서버가 수량 1 · 단가 0(원화)로 저장해 견적가 0을 계산하며 소분류 칸에는 종류 값을 적는다. 조정 줄의 실행가 음수를 허용한다. 차익은 기존 `profit()`(견적가 − 실행가) 그대로다. `repositories/quote-lines.ts`의 삽입·조회가 `line_kind`를 쓰고 읽는다. DTO에 `lineKind`를 싣는다(`registerDto` 갱신).

⑤ **통합(트레이서 증명).** `test/integration/quote-line-kinds.test.ts`(신규)에 `behavior` 세 줄 — DB 재조회로 저장 값·집계·거부 무변경을 단언한다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- .squawk.toml</automated>
    <fails_when>db:migrate가 0이 아닌 코드로 끝나거나(`error:` 줄), diff 출력이 비어 있지 않다 — 린트를 피하려고 `.squawk.toml`을 고쳤다</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_quote_line_kind'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-kinds.test.ts test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>lint·typecheck error가 나오거나 통합 failed 개수가 1 이상이다 — 조정 줄이 저장·집계로 이어지지 않았거나, PM이 조정 줄을 넣었거나, 기존 저장·DTO 등록이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 `line_kind`(기본 `quote`, 세 값 CHECK)를 더하고 락 타임아웃 한 쌍으로 시작하며 `.squawk.toml` diff가 0줄이다
    - CHECK가 `ADD COLUMN "line_kind"` 문장 안에 인라인으로 있고 그 마이그레이션에 제약만 따로 더하는 문장이 없다 — `pnpm lint:sql` 0건(B-22)
    - `saveQuoteLines` 입구에 줄 종류 없이 배치 전체 상태를 판정하는 게이트 호출이 없다 — 통합 테스트의 완료 프로젝트 조정 줄 저장이 통과한다(B-36)
    - 새 마이그레이션의 번호 = 이 플랜 실행 직전 journal 최고 `idx` + 1이고, 파일명(확장자 제외)·`meta/{번호}_snapshot.json`·journal tag가 생성기가 실제로 만든 이름과 같다(번호 검증 명령) — 계획 번호 0014와 다르면 실제 이름이 SUMMARY에 있다
    - `menus.ts`에 `projects.adjustment`가 있고 시드에 이 메뉴의 기본 권한이 없다
    - 조정 줄 판정이 상태를 보지 않는다
    - 통합 테스트가 경영관리의 완료 프로젝트 조정 줄 저장(견적가 0·차익)·목록 집계 반영·PM 거부 무변경을 DB 재조회로 단언한다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>경영관리가 상태와 무관하게 조정 줄을 저장하고, 그 금액이 목록 실행가에 같은 값으로 들어가는 한 경로가 스키마부터 서버 저장까지 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 종류 축 결정표 + PM 거부·종류 불변·음수 허용 종류 + 견적 외 비용 서버 규칙 + 상한·보관의 조정 줄 + 액션 스키마 + 합계 불변식</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, test/unit/domain/quote-lines.test.ts, domain/quotes/lines.ts, app/(app)/projects/actions.ts, test/integration/quote-line-kinds.test.ts, test/integration/projects-list.test.ts</files>
  <read_first>
    - `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마(`quoteLines` 행 — 04-12의 `archivedLineIds` 포함)
    - `test/integration/projects-list.test.ts`(04-05 산출물 전문) — 목록 집계 단언 형태
    - `test/integration/quote-line-cap.test.ts`(04-26 산출물) — 상한 설정을 테스트에서 바꾸는 형태
    - `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md` — 견적 줄 복원 검사 `restoreQuoteLine`(OV-2 — 보관함 `restore()`가 `DOMAIN_RESTORERS`로 맡기는 domain 함수, `{ kind: "restore"; quoteAmountZero }` 게이트)과 정산 구조 판정(D10)의 실제 모양
    - `lib/log.ts`(전문 29행) — `log.warn(event, fields)` 한 줄 JSON 로그
  </read_first>
  <behavior>
    - `lineCellEditability({ lineKind: adjustment, canAdjust: true, status: 완료 })` → 항목·거래처·실행가·비고 `edit`, 소분류·수량·단가·상태 `locked`
    - 같은 입력 + `canAdjust: false`(담당 PM) → 전부 `locked`
    - `structuralEditability({ lineKind: adjustment, canAdjust: true, status: 정산 })` → 추가·삭제 가능, 이동 불가
    - 견적 외 비용: 진행 + `projects` 쓰기 → 추가 가능 · 수량·단가 항상 `locked` · 정산 → 추가 가능(사용자 D10), 실행가 `edit`, 수량·단가 `locked`(D12 — 견적 칸 0), 삭제·이동 불가(04-12 정산 구조 규칙) · 완료 → 전부 잠김
    - 게이트: 조정 줄 `insert` + 조정 권한 없음 → `조정 줄 · 경영관리만 고칠 수 있음` · + 권한 + 완료 → 통과
    - 저장: 견적 줄 실행가 −1 → 여전히 형식 오류 · 견적 외 비용 실행가 −50,000 → 통과 · 기존 줄의 종류 변경 요청 → `줄 종류는 바꿀 수 없습니다`
    - 담당 PM이 조정 줄 실행가를 바꾼 페이로드 → 배치 전부 거부(DB 재조회) + 서버 로그 경고 `write.denied` 한 줄(금액 키 없음) · 담당 PM의 조정 줄 보관 요청 → 거부 · 경영관리의 조정 줄 보관 → 통과, 보관함에 `견적 줄` 항목
    - 정산에서 담당 PM의 견적 외 비용 새 줄(실행가 −30,000) → 통과, 견적가 0 · 정산에서 기존 견적 외 비용 줄 보관 → 거부(D10)
    - 완료 프로젝트의 보관된 조정 줄을 조정 권한과 보관함 권한이 있는 사람이 보관함에서 복원 → 통과 · 보관함 권한만 있고 조정 권한이 없는 사람 → 거부, DB 무변경(OV-2 — 복원도 종류 판정을 지난다)
    - 상한 3 · 줄 3 → 경영관리의 조정 줄 추가 거부(조정 줄도 센다) · 조정 권한만 있는 사람이 견적 줄을 바꾸면 거부
    - 불변식: 한 차수에 견적 줄 · 견적 외 비용 · 조정 줄이 하나씩 있으면 상세 합계 행의 실행가 = 목록 집계의 실행가 = 세 줄 실행가 합
  </behavior>
  <action>
① 결정표(`behavior` 앞 다섯 줄)를 `test/unit/domain/quote-edit-scope.test.ts`에, 음수·종류 불변을 `test/unit/domain/quote-lines.test.ts`에 먼저 쓴다(RED). `edit-scope.ts`에 견적 외 비용 규칙(견적 줄의 상태 규칙을 따르되 수량·단가가 항상 `locked`)과 조정 줄의 구조(이동 불가)를 채운다(GREEN).

② **저장 규칙.** `saveQuoteLines`: 기존 줄의 종류 변경 요청은 `줄 종류는 바꿀 수 없습니다`로 거부 · 견적 외 비용 새 줄도 서버가 수량 1 · 단가 0으로 저장해 견적가 0 · 실행가 음수 허용을 견적 외 비용·조정 두 종류로(견적 줄은 그대로 거부). 정산의 견적 외 비용 새 줄은 04-12의 정산 구조 판정(사용자 D10 — 추가 가능 · 삭제·이동 불가)을 그대로 지난다 — 이 종류만의 정산 예외를 따로 두지 않는다. `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` `quoteLines` 행 스키마에 새 줄 전용 `lineKind`(세 값 enum, 선택)를 더한다.

②-b **거부 경고 로그(사용자 D19-3 · CEO 리뷰 B-26).** 조정 규칙(`조정 줄 · 경영관리만 고칠 수 있음` · 조정 권한만 있는 사람의 견적 줄 변경 거부)으로 배치가 거부되면 04-12가 만든 `saveQuoteLines`의 **한 거부 지점**(`write.denied` — 04-12-SUMMARY의 이름)을 지나 `{ viewerId, rule: "project.line-edit", reason, projectId, revisionId, lineIds }`가 한 번 남게 한다 — 새 `log.warn` 호출 지점을 더하지 않고, 조정 거부가 그 지점을 지나지 않으면 그 지점으로 모은다. 금액·항목 이름은 싣지 않는다(서버 로그는 정보 노출표 밖이다). `handleServerError`는 고치지 않는다(보는 사람·대상 id를 모른다).

②-c **복원 검사의 종류 판정(OV-2).** 04-12의 `restoreQuoteLine`(보관함 복원이 `DOMAIN_RESTORERS`로 맡겨 같은 트랜잭션·같은 잠금에서 부르는 domain 함수)이 `project.line-edit` `restore`를 부를 때 그 줄의 `lineKind`와 복원하는 사람의 `projects.adjustment` 쓰기 여부를 싣게 한다 — 보관된 조정 줄은 상태와 무관하게 조정 권한으로만 복원된다.

③ **통합.** `quote-line-kinds.test.ts`에 `behavior`의 서버 쪽 나머지(PM 조정 칸 변경 거부 + `log.warn` 스파이로 `write.denied` 한 번 · 금액 키 없음 · PM 보관 거부 · 경영관리 보관 통과+보관함 · 완료 프로젝트 조정 줄의 보관함 복원 — 조정 권한 있음 통과 / 없음 거부·DB 무변경 · 상한 계산에 조정 줄 포함(04-26 상한 설정을 3으로) · 조정 권한만 있는 사람이 견적 줄을 바꾸면 거부 · 정산·완료에서 경영관리 조정 줄 추가 통과 · 견적 외 비용 음수 허용 · **정산에서 담당 PM의 견적 외 비용 새 줄 통과(견적가 0)** · 정산에서 기존 견적 외 비용 줄 보관 거부)를 더한다.

④ **불변식 테스트.** `test/integration/projects-list.test.ts`에 `behavior` 마지막 줄을 더한다 — 세 종류 줄을 한 차수에 저장하고 `listQuoteLines` 합계와 `aggregateProjects`·`listProjects`의 실행가가 같음을 단언한다(금지 항목의 합계 쪽). **숫자 비교(CEO 리뷰 B-18):** 지금 목록 쿼리의 SUM은 Postgres bigint라 node-pg가 문자열로 돌려주고 집계는 `::int` 캐스트다 — 04-17(C-01)이 리포지토리 경계에서 숫자로 바꾸기 전까지 이 테스트는 목록 쪽 값을 `Number()`로 바꿔 **값의 동일성**만 단언한다(타입 단언 `typeof === "number"`는 04-17이 더한다 — 주석 한 줄). 이 플랜은 `repositories/projects.ts`를 고치지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 종류 축 결정표나 음수·종류 불변 규칙이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-kinds.test.ts test/integration/projects-list.test.ts test/integration/quote-line-cap.test.ts test/integration/archive.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — PM이 조정 줄을 바꿨거나, 상한·보관 계산이 조정 줄을 빠뜨렸거나, 종류별 합계가 상세와 목록에서 갈렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 저장 입구 권한·DTO 변경이 기존 표 흐름을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표가 `behavior` 앞 다섯 줄을 덮고 조정 줄 판정에 정산·완료 케이스가 있으며, 견적 외 비용의 정산 행이 「추가 가능 · 실행가 편집 · 수량·단가 잠김 · 삭제·이동 불가」다(사용자 D10·D12)
    - 기존 줄의 종류 변경 요청이 거부된다 · 견적 줄 실행가 음수가 여전히 형식 오류다
    - PM의 조정 칸 변경 거부 뒤 DB 재조회로 배치 전체 무변경을 단언하고, 같은 거부에서 `write.denied` 경고가 한 번 나오며 그 필드에 금액이 없다(B-26)
    - 정산에서 견적 외 비용 새 줄 통과 · 기존 줄 보관 거부가 통합 테스트에 있다(옛 「정산 새 줄 거부」 케이스가 남아 있지 않다)
    - 보관된 조정 줄의 보관함 복원이 조정 권한으로 갈린다(OV-2)
    - 상한 계산에 조정 줄이 포함된다(통합)
    - 불변식 테스트가 세 종류 줄의 실행가 합이 상세·목록에서 같음을 단언한다(금지 항목)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>줄 종류 셋의 권한·음수·상태·불변 규칙이 서버에서 판정되고, 모든 종류의 줄이 같은 합계에 들어간다는 불변식이 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(줄 종류) | 새 줄의 종류와 조정 줄 값이 넘어온다. 종류별 권한·음수 허용 판정이 서버에만 있어야 한다 |
| 권한표 → 조정 | 새 메뉴 `projects.adjustment`가 조정 권한을 정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-63 | Elevation of Privilege | 조정 줄 만들기·고치기·지우기 | high | mitigate | `project.line-edit`가 줄 종류와 `projects.adjustment` 권한으로 판정하고, PM의 조정 줄 변경·보관·조정 종류 새 줄을 전부 거부한다. 거부 케이스를 통합 테스트로 고정한다(화면 쪽 렌더는 04-23) |
| T-04-64 | Tampering | 줄 종류 바꾸기 | high | mitigate | 종류는 새 줄에서만 정해지고 기존 줄의 종류 변경 요청은 거부된다 — 견적 줄을 조정 줄로 바꿔 음수·상태 예외를 얻는 경로를 막는다 |
| T-04-65 | Tampering | 음수 실행가 | medium | mitigate | 음수 허용은 견적 외 비용·조정 두 종류뿐이고 견적 줄은 형식 오류로 거부된다(EXP-14) |
| T-04-66 | Information Disclosure | 조정 줄의 가시성 | medium | mitigate | 조정 줄은 같은 표·같은 DTO(`quote.amount` 정보 항목)로 PM에게도 실린다 — 숨은 가산 경로가 없다는 것을 불변식 테스트가 합계로 확인한다(금지 항목). 화면 쪽 렌더 단언은 04-23 E2E |
| T-04-67 | Denial of Service | 마이그레이션 0014(`quote_line_kind`) | low | mitigate | 기본값 있는 NOT NULL 컬럼 추가(Postgres 빠른 기본값 — 표 재작성 없음) + 락 타임아웃 한 쌍. CHECK는 `ADD COLUMN` 안 인라인 제약이다 — drizzle 마이그레이터가 파일 전체를 한 트랜잭션으로 감싸므로 NOT VALID 뒤 같은 파일에서 검증하는 형태는 락 시간을 줄이지 못하고 squawk도 거부한다(CEO 리뷰 B-22, 옛 「락 시간 단축」 주장 정정) |
| T-04-63b | Repudiation | 조정 규칙 거부 | low | mitigate | 거부된 쓰기가 화면에만 보이고 운영 기록에 없던 빈틈을 `write.denied` 경고 로그(보는 사람·규칙·대상 id, 금액 없음)로 메운다(사용자 D19-3, B-26) |
| T-04-63c | Elevation of Privilege | 보관된 조정 줄 복원 | medium | mitigate | 보관함 복원이 04-12의 복원 검사를 같은 트랜잭션에서 지나고 그 검사가 줄 종류·조정 권한을 싣는다 — 조정 권한 없는 사람이 보관함으로 조정 줄을 되살리는 길이 없다(OV-2) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`(인라인 CHECK — B-22), `.squawk.toml` 무변경, 마이그레이션 번호 검증 명령 초록
- `pnpm lint` · `pnpm typecheck` 통과
- `CI=true pnpm test` 세 계층 통과
- 종류 불변식 테스트 초록
</verification>

<success_criteria>
- 경영관리가 어느 상태에서든 조정 줄로 원가를 보정할 수 있고 PM은 서버에서 그것을 바꿀 수 없다
- 견적 외 비용 줄이 견적가 0인 견적 줄의 한 종류로 저장된다
- 줄 종류가 한 컬럼에 있고 모든 종류가 같은 합계에 들어간다
- 04-23이 DTO(`lineKind`·`cellEditability`)만으로 화면을 그릴 수 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컬럼(신규) | `quote_lines.line_kind` text NOT NULL DEFAULT `'quote'` + 인라인 CHECK `quote_lines_line_kind_check`(`quote`·`out_of_quote`·`adjustment`) |
| 마이그레이션 | `db/migrations/0014_quote_line_kind.sql`(04-27의 `0013_role_work_scope` 뒤 — 생성기 번호가 다르면 실제 이름을 SUMMARY에) + `meta/{번호}_snapshot.json` + `_journal.json` 항목 |
| 로그 이벤트 | `write.denied`(경고 — 조정 규칙 거부, 금액 없음) |
| 변경된 기존 심볼(다른 플랜 산출물) | 04-12의 견적 줄 복원 검사 — 줄 종류·조정 권한을 싣는다 · `saveQuoteLines` 입구의 배치 단위 상태 게이트 제거(B-36) |
| 권한표 메뉴(신규) | `projects.adjustment`(견적 조정 줄) — 시드 기본값 없음 |
| 상수 | `QUOTE_LINE_KINDS` |
| 변경된 판정 | `lineCellEditability`·`structuralEditability`(종류·조정 권한 축) · `project.line-edit` ctx(`lineKind`·`actorCanAdjust`) |
| 입력·DTO | `QuoteLineWriteRow.lineKind`(새 줄만) · `saveProjectLedgerAction` 스키마 `quoteLines[].lineKind` · `QuoteLineDto.lineKind` |
| 변경된 기존 심볼 | `saveQuoteLines` 입구 권한(`projects` 또는 `projects.adjustment` 쓰기) · 음수 허용 종류 · 종류 변경 거부 |
| 테스트 | `test/integration/quote-line-kinds.test.ts`(신규) · `projects-list.test.ts` 종류 불변식 · `quote-edit-scope.test.ts`·`quote-lines.test.ts`(단위) 확장 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 조정·견적 외 비용 그룹·버튼·행 모양 · PM 조정 셀 오류 · 조정 삭제 모달 · 표 위 한 줄·EMPTY 조정 변형 · 소분류 설명 힌트 · `test/e2e/quote-line-kinds.spec.ts` · 표 overflow backstop(04-23) · 차수·줄 소속 검사(B-01 — 줄 소속은 04-12, 현재 차수 재확인은 04-40) · 목록 SUM 숫자 타입(C-01 — 04-17) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-22 — CHECK 추가 형태가 squawk에 걸리고 대안(NOT VALID + 같은 파일 검증)도 걸림 | WARNING | Plan 04-13, Task 1 ①: `ADD COLUMN` 안 인라인 CHECK로 손 편집 · T-04-67 정정 · 수용 기준 |
| B-36 — 입구의 배치 단위 상태 게이트가 완료 조정 줄 저장을 막음 | NOTE | Plan 04-13, Task 1 ③-b: 입구 게이트 제거, 줄 단위만 · 트레이서 첫 behavior가 회귀를 잡음 |
| 사용자 D10·D12 — 정산에서 새 줄 추가(견적 칸 0) | DECISION | Plan 04-13, truths·Task 2 behavior·③: 견적 외 비용의 정산 행 = 추가 가능·실행가만·삭제/이동 불가, 옛 「정산 새 줄 거부」 케이스 뒤집음 |
| 사용자 D19-3 · B-26 — 거부된 쓰기가 운영 기록에 없음 | DECISION | Plan 04-13, Task 2 ②-b: `write.denied` 경고(금액 없음) + 통합 스파이 단언 |
| B-18 — SUM(bigint)이 문자열로 와 불변식 `toBe`가 깨짐 | WARNING | Plan 04-13, Task 2 ④: 목록 값을 숫자로 바꿔 값 동일성 단언, 타입 수정은 04-17(C-01) |
| OV-2 — 보관함 복원이 도메인 검사 우회 | HIGH | Plan 04-13, Task 2 ②-c: 04-12 복원 검사에 줄 종류·조정 권한을 싣는다 + 통합 케이스 |
| B-01 — 차수·줄 소속 미검사(이 플랜의 게이트 ctx에 영향) | CRITICAL | 이 플랜은 차수가 하나라 해당 없음 — `<probe_fallback>`에 근거. 줄 소속은 Plan 04-12, 현재 차수 재확인·여러 차수 증명은 Plan 04-40 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 없음 | 이 플랜 몫의 리뷰 항목은 전부 반영했다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-13-SUMMARY.md` when done
</output>
