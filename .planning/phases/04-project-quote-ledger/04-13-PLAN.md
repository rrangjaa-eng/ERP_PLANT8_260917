---
phase: 04-project-quote-ledger
plan: 13
type: execute
wave: 9
depends_on: ["04-12", "04-10"]
files_modified:
  - db/schema/quote-lines.ts
  - db/migrations/0013_quote_line_kind.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0013_snapshot.json
  - domain/permissions/menus.ts
  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - test/unit/domain/quote-lines.test.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/integration/quote-line-kinds.test.ts
  - test/integration/projects-list.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/quote-line-kinds.spec.ts
autonomous: true
requirements: [PROJ-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "견적 줄에는 종류가 셋이다 — 견적 줄(`quote`) · 견적 외 비용(`out_of_quote`) · 조정(`adjustment`). 종류는 한 컬럼 `quote_lines.line_kind`가 정본이고 줄을 만든 뒤에는 바뀌지 않는다(D-48·D-83 계획 재량 — 아래 assumption_delta 결정)"
    - "조정 줄은 권한표 `projects.adjustment` 쓰기(경영관리 — 관리자가 배정)가 있는 사람만 만들고 고치고 지운다 — 프로젝트 상태와 무관하다(정산·완료 포함)(D-83)"
    - "담당 PM에게 조정 줄은 어느 상태에서든 행 전체가 읽기 전용이고, 조정 줄 칸을 고친 값을 실어 보내면 `조정 줄 · 경영관리만 고칠 수 있음`으로 전부 거부된다(D-83, Copywriting Error — 셀(조정 줄, PM))"
    - "조정 줄은 견적가 0 · 실행가 = 금액(음수 허용)이고 수량·단가·상태 칸은 `—`다 — 서버가 수량 1·단가 0으로 저장해 견적가 0을 계산한다(D-83, S4 행 모양)"
    - "조정 줄은 표 맨 아래 고정 그룹 `조정`에 모인다 — 코드표 그룹 뒤, 견적 외 비용 그룹보다도 뒤. 새 색·아이콘이 없고 소분류 칸은 읽기 전용 글자 `조정`이다(S4)"
    - "조정 줄의 실행가는 합계 행·차익·목록의 실행가·수익금에 포함된다 — 상세 합계와 목록 집계가 같은 금액을 보인다(D-83·D-87)"
    - "경영관리에게 「조정 줄 추가」 3차가 상태와 무관하게 보이고, 차수 줄 수 상한에 닿으면 비활성 + `조정 줄 추가할 수 없음 — 300줄 상한 · 상한은 관리자 설정`이다(S4, Copywriting 막힘 — 조정 줄 추가(상한))"
    - "완료 프로젝트의 표 위 한 줄은 경영관리에게 `완료 · 견적 줄이 잠김 · 조정 줄 추가`다 · 0줄 표의 EMPTY는 경영관리에게 `… · 조정 줄 추가`(줄 추가 권한이 함께 있는 상태에서는 「첫 줄 만들기」가 이긴다)(S4, Copywriting)"
    - "경영관리의 조정 줄 삭제 모달은 제목 `조정 줄 삭제` · 부제 `{항목} · {실행가}` · 1차 `조정 줄 삭제` · 2차 `취소 Esc`이고 보관함으로 간다(Copywriting Destructive — 조정 줄 삭제)"
    - "견적 외 비용 줄은 별도 표가 아니라 견적가 0인 견적 줄의 한 종류다 — 담당 PM이 줄을 추가할 수 있는 상태(수주중·진행·미수주)에서 3차 「견적 외 비용 줄 추가」로 만들고, 소분류 자리와 그룹 머리글이 `견적 외 비용`이며, 실행가 음수를 허용한다(D-48, EXP-14, ROADMAP 기준 2)"
    - "견적 외 비용 줄은 견적 줄처럼 04-12의 편집 범위(정산 = 실행가만 · 완료 = 잠김)를 따른다 — 조정 줄만 상태 예외다"
    - "견적 줄(`quote`)의 실행가는 여전히 음수를 받지 않는다 — 음수는 견적 외 비용·조정 두 종류만(EXP-14 「매출·조정 항목은 음수 허용」)"
    - "견적 줄 소분류 셀을 편집하는 동안 고른 소분류의 코드표 설명이 셀 아래 한 줄로 보이고, 편집이 끝나면 사라지며 같은 자리에 오류 이유가 있으면 오류가 이긴다(D-93, SYSTEM.md 개정 ⑥)"
    - statement: "조정·견적 외 비용 그룹이 붙은 표가 PC·375에서 가로 스크롤 0이고 폰 P1 세 열(항목·실행가·상태)이 조정 행에서도 같다 — DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "조정 줄이 PM에게 보이지 않는 채로 실행가·수익금을 바꿔서는 안 된다. 이 시스템의 핵심은 기획본부와 경영관리가 같은 숫자를 보는 것이다 — 경영관리의 원가 보정은 PM 화면의 같은 표 안에 행으로 보이고(읽기 전용), 합계가 그 행을 포함한다는 것이 눈으로 확인돼야 한다. 조정을 숨긴 별도 표·숨은 합계 가산 경로를 만들지 않는다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/0013_quote_line_kind.sql"
      provides: "quote_lines.line_kind(기본 quote, 세 값 CHECK) — 기존 줄은 전부 quote"
      contains: "line_kind"
    - path: "domain/quotes/edit-scope.ts"
      provides: "셀 단계·구조 판정에 줄 종류 × 조정 권한 축 추가"
      exports: ["QUOTE_LINE_KINDS", "lineCellEditability", "structuralEditability"]
    - path: "test/integration/quote-line-kinds.test.ts"
      provides: "조정 줄 권한·상태 무관·PM 거부·음수·견적가 0 · 견적 외 비용 · 종류 불변 · 상세 합계 = 목록 집계 불변식"
      min_lines: 60
    - path: "test/e2e/quote-line-kinds.spec.ts"
      provides: "경영관리 완료 프로젝트 조정 줄 추가 · PM 읽기 전용 · 견적 외 비용 줄 · 소분류 설명 힌트"
      min_lines: 40
  key_links:
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "`project.line-edit`가 줄 종류와 조정 권한을 싣고 같은 판정 함수를 부른다"
      pattern: "lineKind"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "domain/quotes/lines.ts"
      via: "그룹 머리글이 소분류 문자열보다 줄 종류를 먼저 본다 — 조정 그룹이 맨 아래 고정"
      pattern: "lineKind"
    - from: "repositories/projects.ts"
      to: "db/schema/quote-lines.ts"
      via: "목록 실행가 합계가 현재 차수의 모든 종류 줄을 더한다(조정 포함) — 불변식 테스트로 고정"
      pattern: "execution"
---

<objective>
**As a** 경영관리 담당자와 기획 PM, **I want to** 경영관리는 외화 송금 수수료 같은 비용을 프로젝트 상태와 상관없이 「조정」 줄로 넣고, PM은 견적에 없던 비용을 「견적 외 비용」 줄로 붙이기를, **so that** 견적 밖에서 생긴 실제 원가가 전부 같은 표·같은 합계에 들어가고, PM도 경영관리가 무엇을 보정했는지 표 안에서 본다.

이 플랜은 04-12의 편집 범위 함수에 **줄 종류 축**을 더한다(D-83 조정 · D-48 견적 외 비용). 04-12가 상태 × 계급 × 연결 문서로 셀 단계를 판정했고, 여기서 종류(견적 줄 · 견적 외 비용 · 조정)와 조정 권한이 한 함수에 들어간다. 옛 04-06의 「완료 뒤 경영관리의 견적 외 비용 줄 예외」(D-47 옛 형태)는 D-83의 조정 줄이 대체한다 — **옛 형태(완료에서만 쓰는 견적 외 비용 예외 플래그)를 되살리지 않는다.**

Purpose: 조정 줄은 목록의 실행가·수익금(D-87)과 계약 이후 원가에 들어가는 돈이다. 종류가 소분류 문자열에 숨어 있으면 그룹·권한·복사 제외(04-14·04-15)가 전부 문자열 비교에 매달린다.

Output: 마이그레이션 0013(`line_kind`) · 권한표 메뉴 `projects.adjustment` · 편집 범위 종류 축 · 조정·견적 외 비용 화면 · 소분류 설명 힌트 · 통합 1 · E2E 1 · 불변식 테스트.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 761행(「조정」 줄 — is_adjustment 권장과 근거 셋)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 419행(quote_lines 컬럼 추가 배정)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 453행·456행(S4 조정·상한 partial) · 1105–1106행(견적 외 비용) · 1116–1136행(상태별 셀 단계 · 조정 줄 행 모양·삭제·복사·상한) · 338–339행·349행·377행(Copywriting 셀 잠김·조정 줄 PM·막힘·Destructive)
</context>

<assumption_delta_decision>
- **신호:** 한 종류였던 견적 줄에 두 번째·세 번째 종류(견적 외 비용 · 조정)가 생겼다(pluralization). 감지기(`assumption-delta scan`)는 `custom_fields` 문구에서 `chosen` 신호를 냈지만 그 문구는 이 플랜과 무관하다 — `custom_fields`는 이 플랜에서 바뀌지 않는다(no-change).
- **주 명사:** 줄 종류(`line_kind`).
- **결정: promote.** 리서치 권장은 불리언 `is_adjustment`를 **곁에 더하는** 것이었고, 견적 외 비용은 소분류 문자열로 가르는 안이었다. 그러면 「종류」가 불리언 하나와 소분류 매직 문자열 둘에 나뉘어 산다(add-alongside). 이 플랜은 한 컬럼 `line_kind`(`quote` · `out_of_quote` · `adjustment`, CHECK)를 정본으로 올리고, 그룹 머리글·권한·음수 허용·복사 제외가 전부 이 컬럼 하나를 본다. 소분류(`subcategory`)는 `quote` 종류의 세부 분류로 내려간다 — 나머지 두 종류의 소분류 칸은 종류 값을 그대로 적어 NOT NULL을 지킬 뿐 판정에 쓰지 않는다.
- **불변식 테스트(권장 채택):** 「모든 종류의 줄이 저장 → 상세 합계 → 목록 집계를 같은 금액으로 지난다」 — 뒤 페이즈가 특정 종류를 합계에서 빼는 회귀를 곧바로 잡는다(Task 3).
</assumption_delta_decision>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-02(견적 외 비용 줄 · 조정 줄)** 이다. 수용 기준은 D-48·D-83·EXP-14·ROADMAP 기준 2와 UI-SPEC S4에서 끌어왔다. S4 `## UI Considerations`의 「조정 줄 추가 상한」 partial은 truths로, 조정 줄이 붙은 표의 overflow는 backstop 마커로 옮겼다. 「조정 줄과 복사」(⚠ S4)는 복사를 만드는 04-14(새 차수)·04-15(프로젝트 복사)가 옮긴다.

**계획 단계 가정(표면화):**
- **경영관리 = 권한표 `projects.adjustment` 쓰기.** 시드 계급에 경영관리가 없어 기본값은 아무에게도 켜지 않는다(04-02 `projects.revenue`와 같은 결 — 관리자가 실제 담당 계급에 배정). 발행·입금 쓰기(`projects.revenue`)와 한 메뉴로 묶지 않는 이유: 매출 기록과 원가 보정은 다른 일이고, 한쪽만 맡는 사람이 생길 수 있다.
- **조정 줄의 저장 권한:** 조정 권한만 있고 `projects` 쓰기가 없는 사람도 조정 줄을 저장할 수 있어야 한다. 그래서 `saveQuoteLines`의 입구 권한 판정을 「`projects` 쓰기 또는 `projects.adjustment` 쓰기」로 넓히고, 줄마다의 판정은 게이트가 한다(조정 권한만 있는 사람의 견적 줄 변경은 게이트가 잠김으로 거부).
- **조정 줄의 순서:** 조정 그룹 안에서는 만든 순서이고 Alt+↑↓로 그룹 밖으로 옮길 수 없다(고정 그룹).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 경영관리가 완료 프로젝트에 조정 줄 한 줄을 넣고, 상세 합계와 목록 실행가에 들어간다</name>
  <files>db/schema/quote-lines.ts, db/migrations/0013_quote_line_kind.sql, db/migrations/meta/_journal.json, db/migrations/meta/0013_snapshot.json, domain/permissions/menus.ts, domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, test/unit/domain/quote-lines.test.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-line-kinds.spec.ts</files>
  <reversibility rating="costly">`line_kind` 컬럼과 세 값은 Phase 6(경영관리 대리 등록의 견적 외 비용 줄, EXP-16)·Phase 9(손익 원가)가 읽는다 — 이름·값을 바꾸면 마이그레이션과 그 페이즈들의 조회가 따라온다. 지금 데이터는 전부 demo라 one-way는 아니다.</reversibility>
  <read_first>
    - `db/schema/quote-lines.ts`(전문) · `db/migrations/0009_project_quote_ledger_spine.sql` 55–70행(`line_status` 정의 — 같은 결의 text 컬럼)
    - `.squawk.toml`(전문) — 제외 규칙 넷(`adding-required-field` 포함). CHECK 제약 추가가 걸리면 SQL을 고친다
    - `domain/permissions/menus.ts`(전문) — 04-06이 더한 두 메뉴와 주석
    - `domain/quotes/edit-scope.ts`·`test/unit/domain/quote-edit-scope.test.ts`(04-12 산출물 전문)
    - `domain/rules/register.ts`의 `project.line-edit`(04-12 산출물)
    - `domain/quotes/lines.ts` — 입구 권한 판정(`can(viewer, "projects", "write")`) · 형식 오류(실행가 음수 거부) · `computeQuoteLineAmounts` · DTO
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `groupBy`·`subcategoryLabel`·`addLine`·`addLineButton`·열 정의를 찾아 범위만 Read
    - `repositories/projects.ts` 24–40행(`lineSumsSubquery` — 목록 합계가 줄 종류와 무관하게 더한다는 것을 확인)
    - `test/e2e/revenue-section.spec.ts` 10–25행(계급 권한을 테스트에서 켜는 형태)
  </read_first>
  <behavior>
    - `lineCellEditability({ lineKind: adjustment, canAdjust: true, status: 완료 })` → 항목·거래처·실행가·비고 `edit`, 소분류·수량·단가·상태 `locked`
    - 같은 입력 + `canAdjust: false`(담당 PM) → 전부 `locked`
    - `structuralEditability({ lineKind: adjustment, canAdjust: true, status: 정산 })` → 추가·삭제 가능, 이동 불가
    - 게이트: 조정 줄 `insert` + 조정 권한 없음 → `조정 줄 · 경영관리만 고칠 수 있음` · + 권한 + 완료 → 통과
    - 저장: 조정 줄 실행가 −120,000 → 견적가 0 · 차익 = 0 − (−120,000) = 120,000(서버 계산) · 견적 줄 실행가 −1 → 여전히 형식 오류
  </behavior>
  <action>
① **[BLOCKING] 마이그레이션.** `db/schema/quote-lines.ts`에 `lineKind: text("line_kind").notNull().default("quote")`와 CHECK 제약(세 값)을 더하고 `pnpm db:generate`(계획 번호 0013, 실제 번호는 생성기 기준 — 다르면 맞추고 SUMMARY에 적는다). 생성 SQL 앞에 락 타임아웃 한 쌍. 기존 줄은 기본값으로 전부 `quote`가 된다. `pnpm lint:sql`이 CHECK 추가를 경고하면 제약을 `NOT VALID`로 더한 뒤 같은 파일에서 `VALIDATE CONSTRAINT`하는 형태로 고친다(`.squawk.toml`은 고치지 않는다). `bash scripts/dev-db.sh && pnpm db:migrate`.

② **권한표.** `domain/permissions/menus.ts`에 `{ key: "projects.adjustment", label: "견적 조정 줄" }`을 더한다(시드 기본값 없음 — 위 가정, 04-02 주석과 같은 결로 이유 한 줄).

③ **종류 축.** `edit-scope.ts`에 `QUOTE_LINE_KINDS = ["quote","out_of_quote","adjustment"] as const`와 판정 입력 `lineKind`·`canAdjust`를 더한다: 조정 줄은 상태를 보지 않고 `canAdjust`로만 판정(칸: 항목·거래처·실행가·비고만 `edit`, 나머지 `locked` / 구조: 추가·삭제 가능, 이동 불가), 권한 없으면 전부 `locked` — 잠김 이유는 `조정 줄 · 경영관리만 고칠 수 있음`. 견적 외 비용은 견적 줄의 상태 규칙을 따르되 수량·단가가 항상 `locked`. 결정표(`behavior`)를 먼저 쓴다(RED). `project.line-edit`의 ctx에 `lineKind`·`actorCanAdjust`를 더하고 같은 함수를 부른다.

④ **저장.** `saveQuoteLines`: 입구 권한을 「`projects` 쓰기 또는 `projects.adjustment` 쓰기」로 넓힌다(둘 다 없으면 기존 `ForbiddenError`). `QuoteLineWriteRow`에 새 줄에서만 쓰는 `lineKind?`를 더하고(기존 줄의 종류 변경 요청은 `줄 종류는 바꿀 수 없습니다`로 거부), 조정·견적 외 비용 줄은 서버가 수량 1 · 단가 0(원화)로 저장해 견적가 0을 계산하며 소분류 칸에는 종류 값을 적는다. 실행가 음수 허용을 이 두 종류로 넓힌다(견적 줄은 그대로 거부). 차익은 기존 `profit()`(견적가 − 실행가) 그대로다. DTO에 `lineKind`를 싣는다(`registerDto` 갱신).

⑤ **화면(한 경로).** `[id]/page.tsx`가 `can(viewer, "projects.adjustment", "write")`를 계산해 넘기고, `quote-table.tsx`는 (a) 그룹을 줄 종류 먼저 가른다 — `quote`는 소분류 코드표 그룹(정렬 순), 그 뒤 `견적 외 비용`, **맨 아래 고정 그룹 `조정`** (b) 조정 권한자에게 3차 「조정 줄 추가」를 「줄 추가」 옆에(「줄 추가」가 렌더되지 않는 정산·완료에서는 혼자) 렌더해 조정 그룹 끝에 새 줄을 만들고 실행가 셀을 편집 상태로 연다 (c) 조정 행은 소분류 칸 글자 `조정`, 수량·단가·상태 `—`, 견적가 `0`. 새 색·아이콘을 쓰지 않는다.

⑥ **E2E(트레이서 증명).** `test/e2e/quote-line-kinds.spec.ts` 첫 케이스: 준비에서 `role-ceo`에 `projects` 보기 · `projects.adjustment` 쓰기를 켜고 완료 프로젝트(줄 둘)를 둔다 → 대표로 상세를 열면 표 맨 아래에 「조정 줄 추가」 → 항목 `외화 송금 수수료` · 실행가 `120000` → Ctrl+S → 맨 아래 그룹 `조정`에 그 줄이 있고 합계 행 실행가가 120,000만큼 늘었다 → `/projects` 목록의 그 프로젝트 실행가(현재 목록 열)가 같은 금액을 반영한다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/quote-lines.test.ts</automated>
    <fails_when>db:migrate가 실패하거나 단위 테스트 failed 개수가 1 이상이다 — 종류 축 결정표가 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-line-kinds.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 완료 프로젝트의 조정 줄이 저장·합계·목록으로 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 `line_kind`(기본 `quote`, 세 값 CHECK)를 더하고 락 타임아웃 한 쌍으로 시작하며 `.squawk.toml` diff가 0줄이다
    - `menus.ts`에 `projects.adjustment`가 있고 시드에 이 메뉴의 기본 권한이 없다
    - 조정 줄 판정이 상태를 보지 않는다(결정표에 정산·완료 케이스가 있다)
    - 기존 줄의 종류 변경 요청이 거부된다
    - 견적 줄 실행가 음수가 여전히 형식 오류다
    - 그룹 순서가 코드표 그룹 → 견적 외 비용 → 조정이다
    - E2E가 완료 프로젝트에서 조정 줄 저장·합계 증가·목록 반영 셋을 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>경영관리가 상태와 무관하게 조정 줄을 넣고, 그 금액이 상세 합계와 목록 실행가에 같은 값으로 들어가는 한 경로가 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PM 시점과 조정 줄의 나머지 계약 — 읽기 전용 이유 · 삭제 모달 · 표 위 한 줄 · EMPTY · 상한</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, domain/quotes/lines.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/integration/quote-line-kinds.test.ts, test/e2e/quote-line-kinds.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1119–1136행(경영관리 열 · 조정 줄 행 · 삭제 · 상한) · 327행(EMPTY 경영관리) · 338–339행(셀 잠김 경영관리 다음 한 수 · 조정 줄 PM) · 349행 · 377행
    - `domain/quotes/edit-scope.ts`의 `tableLockLine`·`quoteTableEmptyState`(04-12)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `DeleteLineDialog`·`lineCap`·`disabledReason`·`tableLockLine`을 찾아 범위만 Read
  </read_first>
  <behavior>
    - `tableLockLine({ status: 완료, canAdjust: true })` → `완료 · 견적 줄이 잠김 · 조정 줄 추가` · `canAdjust: false` → `완료 · 견적 줄이 잠김`
    - `quoteTableEmptyState`: 완료 + 조정 권한 → 「조정 줄 추가」 · 진행 + 줄 추가 권한 + 조정 권한 → 「첫 줄 만들기」(①이 이긴다) · 정산 + 조정 권한(기간 권리 없음) → 「조정 줄 추가」
    - 담당 PM이 조정 줄 실행가를 바꾼 페이로드 → `조정 줄 · 경영관리만 고칠 수 있음`, 배치 전부 거부(DB 재조회)
    - 담당 PM의 조정 줄 보관 요청 → 거부 · 경영관리의 조정 줄 보관 → 통과, 보관함에 `견적 줄` 항목으로 보인다
    - 상한 3 · 줄 3 → 경영관리의 조정 줄 추가 거부(조정 줄도 센다)
  </behavior>
  <action>
① `tableLockLine`·`quoteTableEmptyState`에 조정 권한을 더한다(우선순위 ② 「조정 줄 추가」 — Copywriting EMPTY 고르는 규칙). 결정표 먼저(RED).

② `quote-table.tsx`: 담당 PM이 조정 행의 셀에서 Enter를 누르거나 값을 붙여넣으면 그 셀이 오류 셀로 고정되고 이유 한 줄 `조정 줄 · 경영관리만 고칠 수 있음`(04-04의 잠긴 셀 붙여넣기 규약 그대로 — 조용히 건너뛰지 않는다). 경영관리의 Delete는 조정 행이면 `조정 줄 삭제` 모달(부제 `{항목} · {실행가}` · 본문 `보관함으로 이동합니다 · 관리자가 복원할 수 있습니다` · 1차 `조정 줄 삭제` · 2차 `취소 Esc`) — 04-12의 보관 경로(`archivedLineIds`)를 그대로 탄다. 「조정 줄 추가」는 04-12의 상한 판정을 같이 쓰고 닿으면 `disabled` + `조정 줄 추가할 수 없음 — {cap}줄 상한 · 상한은 관리자 설정`. 완료 프로젝트에서 조정 권한자에게 1차 「일괄 저장」이 렌더된다(편집 셀이 생기므로 — U-5 규칙의 결과).

③ `test/integration/quote-line-kinds.test.ts`: `behavior`의 서버 쪽 셋(PM 조정 칸 변경 거부 · PM 보관 거부 · 경영관리 보관 통과+보관함) · 상한 계산에 조정 줄 포함 · 조정 권한만 있는 사람이 견적 줄을 바꾸면 거부 · 정산·완료에서 경영관리 조정 줄 추가 통과. E2E: 담당 PM으로 같은 완료 프로젝트를 열면 조정 행이 보이고(숨기지 않는다) 셀 편집이 오류 이유로 막히며 「조정 줄 추가」가 없다 · 경영관리 조정 줄 삭제 모달 라벨.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-kinds.test.ts test/integration/quote-line-cap.test.ts test/integration/archive.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — PM이 조정 줄을 바꿨거나 상한·보관 계산이 조정 줄을 빠뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-line-kinds.spec.ts test/e2e/quote-edit-scope.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표가 `behavior` 앞 두 묶음을 덮는다
    - PM의 조정 칸 변경 거부 뒤 DB 재조회로 배치 전체 무변경을 단언한다
    - PM 화면에 조정 행이 렌더되고(E2E가 행 존재를 단언) 편집 시도가 오류 이유로 막힌다
    - 조정 줄 삭제 모달의 1차 라벨이 `조정 줄 삭제`이고 보관함에서 복원 가능하다
    - 상한 계산에 조정 줄이 포함된다(통합 테스트)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>PM은 조정 줄을 보되 고칠 수 없고, 경영관리는 어느 상태에서든 조정 줄을 넣고 지우며, 상한·EMPTY·표 위 한 줄이 조정 권한을 반영한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 견적 외 비용 줄(D-48) + 소분류 설명 힌트(D-93) + 종류 불변식 + 화면 검증</name>
  <files>domain/quotes/lines.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/integration/quote-line-kinds.test.ts, test/integration/projects-list.test.ts, test/integration/leak-scan.test.ts, test/e2e/quote-line-kinds.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1105–1106행(견적 외 비용) · 1152행(코드표 설명 — 소분류 셀 편집 중) · 1380행(편집 표의 코드표 셀 행)
    - `ui/select/Select.tsx`(04-10 산출물 — 옵션 `description`이 있으면 편집 중 힌트, 오류가 이긴다)
    - `domain/projects/references.ts`(04-10 산출물 — 소분류 옵션의 `description`)
    - `test/integration/projects-list.test.ts`(04-05·04-06 산출물 전문) — 목록 집계 단언 형태
  </read_first>
  <behavior>
    - 진행 + 담당 PM → 「견적 외 비용 줄 추가」로 만든 줄: 그룹 `견적 외 비용` · 견적가 0 · 실행가 −50,000 저장 가능
    - 정산 → 「견적 외 비용 줄 추가」가 렌더되지 않고 서버도 새 줄을 거부한다(04-12 구조 잠금) · 기존 견적 외 비용 줄의 실행가는 정산에서 고칠 수 있다
    - 불변식: 한 차수에 견적 줄 · 견적 외 비용 · 조정 줄이 하나씩 있으면 상세 합계 행의 실행가 = 목록 집계의 실행가 = 세 줄 실행가 합
  </behavior>
  <action>
① **견적 외 비용.** `quote-table.tsx`에 3차 「견적 외 비용 줄 추가」를 「줄 추가」와 같은 구조 가능성·상한 조건으로 렌더한다 — 새 줄은 `lineKind: out_of_quote`로 `견적 외 비용` 그룹 끝에 생기고 항목 셀이 편집 상태로 열린다. 행 모양: 소분류 칸 글자 `견적 외 비용` · 수량·단가 `—` · 견적가 `0` · 실행가 편집(음수 허용) · 상태·비고는 견적 줄과 같다. 서버 규칙은 Task 1 ④가 이미 만들었다.

② **소분류 설명 힌트.** `quote-table.tsx`의 소분류 셀 편집기에 넘기는 옵션에 `references.ts`의 `description`을 싣는다 — 04-10의 `Select`가 편집 중에만 셀 아래 한 줄로 설명을 보이고 오류가 있으면 오류가 이긴다. 읽기 상태의 행 높이는 늘지 않는다. 필터 줄·폰 행 시트에는 설명을 보이지 않는다(S14 표).

③ **불변식 테스트.** `test/integration/projects-list.test.ts`에 `behavior` 셋째 줄을 더한다 — 세 종류 줄을 한 차수에 저장하고 `listQuoteLines` 합계와 `aggregateProjects`·`listProjects`의 실행가가 같음을 단언한다. `quote-line-kinds.test.ts`에 견적 외 비용 두 줄(음수 허용 · 정산 새 줄 거부)을 더한다. `leak-scan.test.ts`가 DTO 갱신 뒤 초록인지 확인한다.

④ **E2E.** 담당 PM이 진행 프로젝트에서 견적 외 비용 줄을 −50,000으로 저장 · 소분류 셀 편집 중 설명 한 줄이 보이고 편집을 끝내면 사라진다.

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 조정·견적 외 비용 그룹이 붙은 표의 PC·375 가로 스크롤 0과 폰 P1 세 열(backstop)을 DOM 실측 → 수정 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-kinds.test.ts test/integration/projects-list.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 종류별 합계가 상세와 목록에서 갈렸거나 견적 외 비용 규칙이 틀렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 판정(조정·견적 외 비용 그룹이 붙은 표의 PC·375 레이아웃)을 SUMMARY에서 확인</test>
      <expected>가로 스크롤 0, 폰 P1이 항목·실행가·상태 세 열</expected>
      <why_human>폭·열 접기는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 「견적 외 비용 줄 추가」가 줄 추가 가능 상태에서만 렌더되고 만든 줄의 종류가 `out_of_quote`다
    - 견적 외 비용 줄의 실행가 음수 저장이 통과한다(통합)
    - 소분류 셀 편집 중에만 설명 한 줄이 보인다(E2E)
    - 불변식 테스트가 세 종류 줄의 실행가 합이 상세·목록에서 같음을 단언한다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>견적 외 비용 줄이 견적 줄 원장의 한 종류로 동작하고, 모든 종류의 줄이 같은 합계에 들어간다는 불변식이 테스트로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(줄 종류) | 새 줄의 종류와 조정 줄 값이 넘어온다. 종류별 권한·음수 허용 판정이 서버에만 있어야 한다 |
| 권한표 → 조정 | 새 메뉴 `projects.adjustment`가 조정 권한을 정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-63 | Elevation of Privilege | 조정 줄 만들기·고치기·지우기 | high | mitigate | `project.line-edit`가 줄 종류와 `projects.adjustment` 권한으로 판정하고, PM의 조정 줄 변경·보관·조정 종류 새 줄을 전부 거부한다. 거부 케이스를 통합 테스트로 고정한다 |
| T-04-64 | Tampering | 줄 종류 바꾸기 | high | mitigate | 종류는 새 줄에서만 정해지고 기존 줄의 종류 변경 요청은 거부된다 — 견적 줄을 조정 줄로 바꿔 음수·상태 예외를 얻는 경로를 막는다 |
| T-04-65 | Tampering | 음수 실행가 | medium | mitigate | 음수 허용은 견적 외 비용·조정 두 종류뿐이고 견적 줄은 형식 오류로 거부된다(EXP-14) |
| T-04-66 | Information Disclosure | 조정 줄의 가시성 | medium | mitigate | 조정 줄은 같은 표·같은 DTO(`quote.amount` 정보 항목)로 PM에게도 보인다 — 숨은 가산 경로가 없다는 것을 불변식 테스트가 합계로 확인한다(금지 항목) |
| T-04-67 | Denial of Service | 마이그레이션 0013 | low | mitigate | 기본값 있는 NOT NULL 컬럼 추가(Postgres 빠른 기본값 — 표 재작성 없음) + 락 타임아웃 한 쌍. CHECK는 `NOT VALID` 뒤 검증 형태로 락 시간을 줄인다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`, `.squawk.toml` 무변경
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 종류 불변식 테스트 초록
</verification>

<success_criteria>
- 경영관리가 어느 상태에서든 조정 줄로 원가를 보정하고, PM은 그것을 같은 표에서 본다
- 견적 외 비용 줄이 견적가 0인 견적 줄의 한 종류로 동작한다
- 줄 종류가 한 컬럼에 있고 모든 종류가 같은 합계에 들어간다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컬럼(신규) | `quote_lines.line_kind` text NOT NULL DEFAULT `'quote'` + CHECK(`quote`·`out_of_quote`·`adjustment`) |
| 마이그레이션 | `db/migrations/0013_quote_line_kind.sql`(실제 번호는 생성기 기준) + `meta/0013_snapshot.json` |
| 권한표 메뉴(신규) | `projects.adjustment`(견적 조정 줄) — 시드 기본값 없음 |
| 상수 | `QUOTE_LINE_KINDS` |
| 변경된 판정 | `lineCellEditability`·`structuralEditability`·`tableLockLine`·`quoteTableEmptyState`(종류·조정 권한 축) · `project.line-edit` ctx(`lineKind`·`actorCanAdjust`) |
| 입력·DTO | `QuoteLineWriteRow.lineKind`(새 줄만) · `QuoteLineDto.lineKind` |
| 변경된 기존 심볼 | `saveQuoteLines` 입구 권한(`projects` 또는 `projects.adjustment` 쓰기) · 음수 허용 종류 · `QuoteLedger` 그룹 순서·「조정 줄 추가」·「견적 외 비용 줄 추가」·조정 삭제 모달 · 소분류 셀 설명 힌트 |
| 테스트 | `test/integration/quote-line-kinds.test.ts` · `test/e2e/quote-line-kinds.spec.ts`(신규) · `projects-list.test.ts` 종류 불변식 · `quote-edit-scope.test.ts` 확장 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-13-SUMMARY.md` when done
</output>
