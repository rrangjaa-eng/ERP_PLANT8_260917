---
phase: 04-project-quote-ledger
plan: 14
type: execute
wave: 16
depends_on: ["04-13", "04-23"]
files_modified:
  - domain/quotes/revisions.ts
  - domain/quotes/edit-scope.ts
  - domain/quotes/lines.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - repositories/quote-revisions.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - test/unit/domain/quote-revisions.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/quote-revisions.test.ts
  - test/integration/leak-scan.test.ts
autonomous: true
requirements: [PROJ-07, PROJ-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "새 차수는 현재 차수의 견적 줄(견적 줄·견적 외 비용, 보관 제외) 전체를 한 트랜잭션에서 복사해 만들고, 복사된 줄마다 원본 줄 id를 계보(`copied_from_line_id`)로 갖는다 — 복사는 INSERT…SELECT 한 문장이고 복사본의 `version`은 1이며, 줄 상태·비고·거래처·정렬 순서·`custom_fields`·금액 컬럼·소분류·줄 종류가 원본 그대로다. 새 차수는 승인 표시가 꺼져 있다(D-53, CEO 리뷰 B-32, 엔지니어링 리뷰 GAP 5b)"
    - "복사할 견적 줄이 0개면 새 차수를 만들 수 없다 — 빈 차수가 없다(D-53)"
    - "조정 줄은 새 차수로 복사되지 않고 같은 줄(값·작성자·id 그대로)이 새 현재 차수로 옮겨 간다 — **보관된 조정 줄까지 전부** 옮겨, 나중에 보관함에서 복원해도 현재 차수에 돌아온다. 목록의 실행가·수익금은 차수가 바뀌어도 조정 금액만큼 그대로다(⚠ S4 「조정 줄과 복사」 기본값 = 사용자 D19-10, CEO 리뷰 B-21, D-87 — 이전 차수 읽기 표에 조정 그룹이 없는 화면 쪽은 04-24)"
    - "새 차수는 수주중·진행·미수주에서만 서버가 만든다 — 정산·완료는 게이트가 거부한다(S3, 사용자 D10 「정산의 새 차수는 서버 거부」 · D-47 — 버튼 렌더는 04-24)"
    - "최신 차수만 현재 차수다 — 승인 여부와 무관하게 순번이 가장 큰 차수이고, 차수 되돌리기 동작이 어디에도 없다(D-53·D-54)"
    - "새 차수 요청은 사용자가 보고 있던 차수 id(`fromRevisionId`)를 싣는다 — 그 id가 지금의 현재 차수가 아니면(더블클릭의 두 번째 요청 · 오래된 탭) 서버가 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`로 거부하고 차수 수가 그대로다. 두 요청이 겹치면 프로젝트 행 잠금이 둘을 한 줄로 세워 뒤 요청이 같은 확인에서 거부되고, 그래도 `(project_id, seq)` 유일 제약 위반(`23505` · `quote_revisions_project_seq_key`)이 오면 같은 문구가 되어 반쪽 차수가 남지 않는다 — 이 대응은 `deps.insertRevision`에 합성 `23505` 오류를 주입한 테스트로 증명한다(CEO 리뷰 B-02 · 엔지니어링 리뷰 B §1 — 다른 연결로 같은 순번 행을 넣는 흉내는 이 함수의 `FOR UPDATE`와 FK `FOR KEY SHARE`가 부딪혀 멈춘다. 두 연결 결정적 경합 증명은 04-40)"
    - "새 차수·고객 승인 표시는 트랜잭션 첫 단계에서 04-11의 `loadProjectForGate`(04-20 `lockProjectForWrite`의 프로젝트 행 배타 잠금 + 같은 트랜잭션 안 자동 정산 판정)로 잠근 행으로 판정하고, 현재 차수·보던 차수·복사 대상 줄 수·승인 기준값·연결 문서 판정이 전부 잠금 뒤 같은 트랜잭션의 읽기다 — 같은 프로젝트의 줄 저장(04-12)과 한 줄로 선다. 테스트 주입 지점 `deps.afterLock`을 그대로 넘긴다(CEO 리뷰 OV-3·B-01·A-13·A-33 — 트랜잭션 밖 선판정을 두지 않는다)"
    - "잠긴 트랜잭션 안에서 전역 db(풀)를 부르지 않는다 — `projects` 쓰기 같은 `can()` 권한 판정은 `withTransaction` **앞**에서 계산해 넘기고, 잠금 안의 읽기·연결 문서 조회·행동 로그는 전부 그 `tx`를 쓴다(04-32 규칙 — 행 잠금을 쥔 채 두 번째 풀 연결을 기다리는 멈춤, 엔지니어링 리뷰 B §1)"
    - "견적 줄(견적 줄·견적 외 비용, 보관 제외)이 0개인 차수는 승인 표시를 켤 수 없다 — `승인할 견적 줄이 없음 · 첫 줄 만들기`로 거부된다. 빈 1차를 승인하면 견적가 있는 줄도 새 차수도 못 만드는 막다른 길을 막는다(엔지니어링 리뷰 ENG-D4)"
    - "승인 표시 요청은 PM이 보고 승인하는 기준값 — 그 차수의 견적 합계(`seenTotalKrw`)와 내용 토큰(`contentToken`) — 을 싣고, 서버가 잠금 뒤 같은 트랜잭션에서 다시 계산한 값과 하나라도 다르면 `견적이 바뀌었습니다 · 새로 고쳐 주세요`로 거부하고 승인 표시가 꺼진 채다. 내용 토큰은 그 차수의 견적 줄·견적 외 비용 줄(보관 포함)의 (id · version · 보관 여부)를 id 순으로 이은 문자열의 `md5`다 — 줄 추가·수정·보관·복원이 모두 바꾸고, 조정 줄은 들어가지 않는다. 승인 취소(끄기)는 기준값을 받지 않는다(엔지니어링 리뷰 ENG-D9 · Codex #2 — 다른 사람이 방금 저장한 금액을 PM이 모른 채 승인하는 길)"
    - "차수의 고객 승인 표시는 담당 PM(`projects.pm_user_id`이면서 `projects` 쓰기 권한이 있는 사람)이 승인일과 함께 켜고 결재를 거치지 않으며 행동 로그 `document_update`(detail `kind: customer_approval`)에 남는다 — 담당이 아니거나 쓰기 권한이 없는 사람의 요청은 서버가 거부한다(D-56, CEO 리뷰 B-30 — Phase 5 결재 필터가 `document_approve`와 헷갈리지 않는 종류)"
    - "승인일은 `YYYY-MM-DD`(KST 달력 날짜)로 받아 그날 KST 00:00 순간으로 저장하고, 읽을 때 같은 날짜로 돌아온다 — `customer_approved_at`은 시간대 없는 timestamp라 SQL에서 날짜로 바로 자르면 하루 앞 날짜가 된다. 저장·읽기는 04-29 `lib/kst-date`의 `kstDayStart` ↔ `kstDateOf` 한 쌍만 쓴다(CEO 리뷰 B-25)"
    - "완료에서는 승인 표시 변경을 서버가 거부하고, 승인일은 저장된 채로 남는다 — 수주중·진행·미수주·정산에서는 바꿀 수 있다(⚠ S3 「상태별 고객 승인」 기본값 = 사용자 D19-9 — 버튼 렌더는 04-24)"
    - "승인 표시 취소는 그 차수에 연결 문서가 없을 때만 되고, 있으면 서버가 `연결 문서가 있어 승인 표시를 취소할 수 없습니다 · 새 차수로 고쳐 주세요`로 거부한다(D-56)"
    - "견적 표시 번호 `26001-2차`는 저장 컬럼·카운터 없이 프로젝트 번호와 순번에서 만든다(D-56)"
    - "고객 승인 게이트 규칙 `quote.customer-approval`이 등록되어 있다 — 수주중·미수주이면 승인과 무관하게 통과(D-43 + 사용자 D8 「미수주도 면제」), 진행·정산·완료이고 현재 차수가 미승인이면 거부(담당 PM에게 `{n}차 고객 승인 전 · 고객 승인 표시`, 그 밖 `{n}차 고객 승인 전 · 담당 PM {이름}`), 설정 `project.customer_approval_gate`를 끄면 통과한다 — 이 페이즈에는 호출자가 없고 Phase 5 지출결의가 부른다(D-43, ROADMAP 기준 3, PROJ-07, CEO 리뷰 B-08)"
    - "거래처 필수 규칙 `quote.vendor-required`가 등록되어 있다(`거래처 없음 · 거래처 고르기`) — 호출은 Phase 5다(D-64)"
    - "이전 차수 줄의 연결 문서가 계보를 따라 현재 차수의 대응 줄에 이어 보이도록 해석하는 함수가 연결 문서 조회 지점 안에 있다 — 문서 행을 옮기지 않는다. 문서 출처가 Phase 5라 지금은 순수 함수와 빈 결과뿐이고 그 이상 키우지 않는다(D-55; CEO 리뷰 B-33)"
    - "차수 요약이 순번 · 생성일 · 줄 수(견적 줄 + 견적 외 비용, 보관 제외) · 견적 합계 · 상태 낱말(최신 미승인 `현재` · 승인 `승인` · 승인 없이 지나간 이전 차수 빈 값) · 승인 기준값의 내용 토큰을 서버에서 돌려준다 — 한 번의 GROUP BY 쿼리이고 합계는 JS 숫자다(S5, U-2 사용자 확정, CEO 리뷰 B-18, ENG-D9 — 표 렌더·승인 모달은 04-24)"
    - "차수 요약의 견적 합계는 DTO 명세(합계 → 정보 항목 `quote.amount`)와 `registerDto`를 지난다 — `quote.amount`를 볼 수 없는 계급의 응답에 합계 키가 없고 누수 스캔 레지스트리가 이 DTO를 안다(D-38, CEO 리뷰 B-20)"
    - "이전 차수 잠김 조회(`listQuoteLines(…, { revisionSeq })`, 현재보다 작은 순번)는 모든 줄의 모든 셀이 `locked`이고 조정 줄이 없으며, `quote.amount`를 숨긴 계급에게는 견적 금액 키가 현재 차수 조회와 같이 빠진다(엔지니어링 리뷰 GAP 5c)"
    - "새 차수·승인 표시 요청이 거부되면(오래된 차수 · 담당 아님 · 권한 없음 · 빈 차수 · 기준값 불일치) 화면 거부와 함께 서버 로그 경고 `write.denied` 한 줄(보는 사람·규칙·프로젝트·차수 id, 금액 없음)이 04-20의 `denyWrite(viewer, rule, ids, err)` 도우미 하나로 남는다(사용자 D19-3, CEO 리뷰 B-26, 엔지니어링 리뷰 `denyWrite`)"
  prohibitions:
    - statement: "승인 전 차수에서 돈이 나가는 길을 「승인된 이전 차수」로 우회하게 해서는 안 된다. 현재 차수는 최신 순번 하나뿐이고(D-54), 게이트는 이전 승인 차수를 대신 보지 않는다 — 승인을 덜 받으려는 습관을 막는 것이 이 규칙의 목적이다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/revisions.ts"
      provides: "새 차수(전체 복사 + 조정 줄 이동 · 보던 차수 확인) · 고객 승인 표시(빈 차수 거부 · 기준값 확인)와 취소 · 차수 요약(DTO 명세 · 내용 토큰) · 견적 표시 번호 · 계보 해석"
      exports: ["createRevisionFromCurrent", "setCustomerApproval", "listRevisionSummaries", "quoteDisplayNumber", "resolveLinkedDocumentsByLineage", "REVISION_SUMMARY_DTO_SPEC"]
    - path: "test/integration/quote-revisions.test.ts"
      provides: "전체 복사(보존 컬럼)·계보·조정 이동(보관 포함)·빈 차수 거부·보던 차수 불일치 거부·23505 주입 대응·승인 권한·빈 차수 승인 거부·기준값 불일치 거부·완료 거부·취소 조건·승인일 왕복·요약 키 집합·이전 차수 잠김 조회 증명"
      min_lines: 110
    - path: "test/unit/domain/quote-revisions.test.ts"
      provides: "표시 번호 · 계보 해석(사슬·끊긴 계보) · 차수 상태 낱말 표"
      min_lines: 30
  key_links:
    - from: "domain/quotes/revisions.ts"
      to: "repositories/quote-lines.ts"
      via: "새 차수가 한 트랜잭션에서 견적 줄을 계보와 함께 복사하고 조정 줄을 옮긴다"
      pattern: "copied_from_line_id"
    - from: "domain/quotes/revisions.ts"
      to: "domain/rules/gate.ts"
      via: "새 차수·승인 변경이 게이트(`quote.revision-create`·`quote.approval-toggle`)를 지난다"
      pattern: "gate("
    - from: "domain/quotes/revisions.ts"
      to: "domain/projects/auto-transition.ts"
      via: "새 차수·승인 표시가 트랜잭션 첫 단계에서 `loadProjectForGate`로 프로젝트 행을 잠근다 — 줄 저장(04-12)과 같은 잠금"
      pattern: "loadProjectForGate"
    - from: "domain/quotes/revisions.ts"
      to: "repositories/quote-revisions.ts"
      via: "승인 표시가 잠금 뒤 같은 tx로 `approvalBasis`(합계 · 내용 토큰)를 다시 계산해 요청의 기준값과 비교한다"
      pattern: "approvalBasis"
    - from: "domain/rules/register.ts"
      to: "domain/settings/keys.ts"
      via: "고객 승인 게이트가 설정 키로 꺼진다"
      pattern: "project.customer_approval_gate"
    - from: "domain/quotes/lines.ts"
      to: "domain/quotes/revisions.ts"
      via: "연결 문서 조회 지점이 계보 해석 함수를 거쳐 현재 차수 줄에 문서를 잇는다 — 조회 지점은 여전히 하나"
      pattern: "resolveLinkedDocumentsByLineage"
---

<objective>
**As a** 기획 PM, **I want to** 네고로 바뀐 견적을 이전 차수를 통째로 복사한 새 차수로 고치고, 고객이 승인한 차수에 승인일을 표시하며, 그 판정을 서버가 한 규칙으로 하기를, **so that** 견적의 변천이 차수로 남고 「현재 차수가 승인됐는가」 하나로 지출(Phase 5)과 계약 금액(04-16)이 정해진다.

이 플랜은 옛 04-06 Task 2·3에서 **차수 기계와 고객 승인**을 옮겨 왔다(D-53~D-56 · D-43 · D-64 거래처 필수 등록). 2026-09-23 결정이 더한 것: 정산·완료에서 새 차수가 없다(⚠ S4 기본값) · 조정 줄은 복사되지 않고 새 현재 차수로 옮겨 간다(⚠ S4 기본값) · 완료에서는 승인 표시 변경이 없다(⚠ S3 기본값). **2026-09-23 체커 1차 분할:** 옛 04-14(20파일 — 세 태스크가 다 10파일 이상)를 서버와 화면으로 나눴다. 이 플랜은 **서버 쪽**(차수 domain · 리포지토리 · 게이트 규칙 넷 · 설정 키 · 액션 둘 · 계보 해석 · 차수 요약)이고, 모달 셋 · 머리 줄 버튼·부제 · 차수 섹션 · 이전 차수 읽기 표 · Copywriting 두 줄은 **04-24**가 이 액션과 함수 위에 그린다.

Purpose: ROADMAP 기준 3 — 차수마다 고객 승인 표시가 있고 승인 전 차수의 줄에서는 지출 동작이 게이트로 막힌다(설정으로 끌 수 있음). 이 페이즈에는 지출결의가 없으므로 게이트 규칙을 등록하고 단위 테스트로 판정을 고정하며, Phase 5가 호출자만 붙인다.

Output: 차수 domain · 게이트 규칙 넷 · 설정 키 하나 · 액션 둘 · 이전 차수 잠김 조회 · 승인일 KST 왕복(04-29 도우미 재사용) · 단위 2 · 통합 1.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 결정 원장·T2·T3·T4·T13·부록 B).** 반영할 것이 한 플랜(15파일 미만 · 3태스크)에 들어가지 않아 **둘로 나눴다.** 이 플랜은 04-24가 화면을 그리는 데 필요한 **서버 계약**(새 차수 · 고객 승인 · 게이트 · 요약 · 계보 · 이전 차수 보기)을 만들고, 사실 정정을 그 자리에서 반영한다 — B-02(`fromRevisionId`로 보던 차수 확인) · B-21(보관된 조정 줄까지 이동) · B-32(INSERT…SELECT · `version` 1) · B-25(승인일 KST 왕복) · B-30(승인에 `projects` 쓰기 + 행동 종류) · B-20(요약 DTO 명세·누수 스캔) · B-18(요약 합계 숫자) · 사용자 D8(미수주도 고객 승인 게이트 면제, T13) · 사용자 D19-3(거부 경고 로그). **잠금(OV-3, T2의 04-14 몫):** 그룹 A가 만든 04-11의 `loadProjectForGate`(04-20 `lockProjectForWrite` 배타 잠금 + 잠금 안 자동 정산)를 새 차수·승인의 트랜잭션 첫 단계에서 부른다 — 04-11이 이 자리를 그룹 B의 호출처로 적어 두었다. **04-40**(이 플랜 바로 뒤)은 줄 저장의 현재 차수 재확인(B-01) · 차수가 둘일 때의 소속·일치 증명 · 두 연결 결정적 경합 테스트 · 승인 차수의 견적 칸 잠금과 승인 합계를 바꾸는 조작 거부(사용자 D7 · OV-1, T4)를 맡는다. 그래서 이 플랜에는 `Promise.all` 경합 테스트를 두지 않는다(B-02 — 타이밍 의존이라 흔들린다).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 결정 번호는 「ENG-D*」로 적어 CEO 리뷰의 「사용자 D*」와 구별한다).** (1) **ENG-D4:** 견적 줄 0개 차수의 승인 표시 거부(`승인할 견적 줄이 없음 · 첫 줄 만들기`). (2) **ENG-D9(Codex #2):** 승인 요청이 PM이 본 합계와 내용 토큰을 싣고, 잠금 뒤 다시 계산한 값과 다르면 `견적이 바뀌었습니다 · 새로 고쳐 주세요` — 순차 테스트는 여기, 두 연결 경합(오래된 기준값으로 온 승인)은 04-40, 화면(미저장 편집 중 승인 버튼 비활성)은 04-24. (3) **풀 고갈 규칙(04-32):** 잠긴 트랜잭션 안에서 `can()`·설정 등 전역 db를 부르지 않는다 — 트랜잭션 앞에서 계산하거나 `tx`를 넘긴다. (4) **23505 테스트:** `deps.insertRevision` 주입 합성 오류(다른 연결 삽입 흉내는 자기 잠금과 교착한다). (5) **`denyWrite`**(04-20 도우미)로 거부 로그. (6) **GAP 5:** 복사 보존 컬럼(비고·거래처·정렬·`custom_fields`·상태) · 이전 차수 잠김 조회의 셀·조정 줄·키 집합 단언. (7) 행동 로그는 04-32의 `recordAction(viewer, entry, { tx })`로 같은 트랜잭션에 쓴다(뒤 단계가 되돌리면 로그도 없다 — 유령 로그 방지). **ENG-D11:** Phase 4 안에서는 앞 플랜 파일의 결함을 회귀 테스트와 SUMMARY 편차 기록과 함께 고칠 수 있다(범위는 늘리지 않는다).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-13-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1154–1194행(S5 적용 규칙 — 상태 낱말 · 복사해 새 차수) · 439행·457–461행(⚠ S3 · ⚠ S4 조정 줄과 복사) · 360행·370–371행(Copywriting 막힘 — 지출결의(고객 승인) · 새 차수)
# .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md U-2 행(사용자 확정 값)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(차수·고객 승인 — 서버)** 과 **PROJ-05(견적 줄 복사 — 차수 전체 복사 쪽)** 다. 수용 기준은 D-43·D-53~D-56·D-64·ROADMAP 기준 3과 UI-SPEC S3·S5에서 끌어왔다. S5 `## UI Considerations`의 error backstop과 overflow, S3·S5의 렌더 truths는 화면을 그리는 04-24로 옮겼다 — 이 플랜은 그 화면이 쓰는 서버 계약(게이트 문자열 · 요약 · 표시 번호 · 잠김 조회)을 truths로 덮는다.

**계획 단계 가정(사용자에게 표면화 → 2026-09-23 사용자 확정):**
- **⚠ S3 상태별 고객 승인 — 기본값 적용(사용자 D19-9로 확정):** 수주중·진행·미수주·정산 = 승인 변경 가능, 완료 = 서버 거부(버튼도 없음 — 04-24).
- **⚠ S4 조정 줄과 복사 — 기본값 적용(사용자 D19-10으로 확정):** 새 차수는 조정 줄을 복사하지 않고 **같은 줄을 새 현재 차수로 옮긴다**(04-13의 `line_kind` 모델 — 조정 줄은 늘 현재 차수에 산다). 이전 차수 읽기 표에 조정 그룹이 없는 것이 이 모델의 결과다. 보관된 조정 줄도 함께 옮긴다(B-21 — 옮기지 않으면 복원했을 때 이전 차수에 되살아나 화면에도 목록 합계에도 안 보인다).
- **정산의 새 차수(사용자 D10):** 정산에서 새 줄 추가는 열렸지만 새 차수는 여전히 서버가 거부한다 — `quote.revision-create`의 정산 거부는 그대로다.
- **고객 승인 게이트의 미수주(사용자 D8):** D-43 원문은 수주중만 면제였지만, 미수주는 잠기지 않고(D-45) 뒤늦은 PT 청구서가 오며 승인 차수가 거의 없다 — 미수주도 면제한다. 이 페이즈에는 호출자가 없고 결정표 단위 테스트가 Phase 5를 위해 동작을 고정한다.
- **승인 표시의 권한(B-30):** 담당 PM 판정(`viewer.id = pm_user_id`)에 `projects` 쓰기 권한을 더한다 — 권한표에서 쓰기를 거둔 PM이 승인을 켜는 길을 막는다. 행동 로그는 `document_update` + detail `kind: "customer_approval"`(Phase 5의 결재 필터가 보는 `document_approve`와 섞이지 않는다).
- **복사와 줄 수 상한(B-32):** 새 차수 복사는 04-26의 상한을 다시 보지 않는다 — 복사 줄 수는 원본 차수의 줄 수와 같고, 관리자가 상한을 그보다 낮췄더라도 04-26의 「줄을 늘릴 때만 거부」 규칙(CEO 리뷰 A-20 정정)이 이후 편집·보관을 막지 않는다.
- **차수 요약의 「줄 수」:** 그 차수의 견적 줄(견적 줄 + 견적 외 비용, 보관 제외)이다 — 조정 줄은 세지 않는다(새 차수 모달의 `{k}줄`과 같은 기준).
- **승인 기준값의 내용 토큰(ENG-D9 — 「가장 단순하면서 맞는 것」의 선택):** `max(version)`·`max(updated_at)`은 새 줄 추가(버전 1)·보관을 놓치고, 차수 내용 카운터 컬럼은 마이그레이션과 모든 쓰기 경로의 증가를 요구한다. 그래서 `md5(string_agg(id || ':' || version || ':' || (archived_at IS NULL), ',' ORDER BY id))`(견적 줄·견적 외 비용, 보관 포함, 줄 0개면 빈 문자열의 md5)를 SQL에서 계산한다 — Postgres 내장 함수라 새 의존성이 없고, 줄 추가·수정(버전 증가)·보관·복원이 전부 토큰을 바꾼다. 조정 줄은 고객 승인 범위가 아니라 뺀다. 실행가만 바뀐 저장도 줄 버전을 올려 토큰을 바꾸므로 그 사이의 승인은 거부된다 — 보수적인 쪽이 맞다(PM이 새로 고쳐 다시 승인한다). 견적 합계와 토큰을 **둘 다** 비교한다(사용자에게 보인 숫자가 서버와 같다는 것을 명시적으로 확인).
- **견적 금액을 볼 수 없는 사람의 승인(ENG-D9의 결과):** 기준값에 합계가 들어가므로 `quote.amount`를 볼 수 없는 계급은 확인할 합계가 없다 — 04-24가 그런 사람에게 승인 버튼을 그리지 않고, 액션 스키마가 `seenTotalKrw`를 필수로 받는다. 새 규칙을 만드는 것이 아니라 「본 합계를 싣는다」의 직접 결과임을 SUMMARY에 적는다.
- **UI-SPEC에 없는 문구 둘(계획 단계 판단 — 04-04 U-6 선례대로 04-24가 Copywriting에 더한다):** ⑴ 「고객 승인 표시」 모달 — 제목 `고객 승인 표시` · 본문 `{차수} · 고객이 승인한 날` + 날짜 칸(기본값 오늘 KST) · 1차 `고객 승인 표시` · 2차 `취소 Esc`. D-56 「승인일과 함께 켠다」를 만족하려면 날짜를 받을 자리가 필요한데 S3는 버튼만 적었다. ⑵ 빈 차수 거부 이유 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기` — 이 문자열은 이 플랜의 게이트가 돌려준다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 담당 PM이 새 차수를 만든다(복사·계보·조정 이동 리포지토리 → 구조 판정 → 게이트 → domain → 액션 → 통합)</name>
  <files>domain/quotes/revisions.ts, domain/quotes/edit-scope.ts, domain/rules/register.ts, repositories/quote-revisions.ts, repositories/quote-lines.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/unit/domain/rules-gate.test.ts, test/integration/quote-revisions.test.ts, test/integration/leak-scan.test.ts</files>
  <read_first>
    - `db/schema/quote-revisions.ts`·`db/schema/quote-lines.ts`(전문) — `(project_id, seq)` 유일 제약 이름 `quote_revisions_project_seq_key` · 계보 컬럼 · `line_kind` · `version`
    - `repositories/quote-revisions.ts`(전문 71행) · `repositories/quote-lines.ts`(전문)
    - `domain/quotes/lines.ts` 160–175행(`getCurrentQuoteRevision` — 최신 순번)
    - `domain/quotes/edit-scope.ts`(04-12·04-13·04-23 산출물) — `structuralEditability`(정산 = 추가 가능 · 삭제·이동 불가 — 사용자 D10)
    - `domain/projects/auto-transition.ts`(04-11) — `loadProjectForGate(viewer, id, { now, tx, afterLock })`: 프로젝트 행 배타 잠금(04-20 `lockProjectForWrite`) + 같은 tx 안 자동 정산 판정 · 잠근 행 반환(쓰기 경로의 선판정 정본 — A-13·A-33)
    - `domain/action-log/record.ts` 10–35행(핵심 행동 종류 — `document_create`) · 04-32 뒤의 세 번째 인자 `{ tx }`(같은 트랜잭션 기록)
    - `lib/pg-errors.ts`(전문 — `isUniqueViolation`이 `cause`까지 본다) · `.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md`의 `domain/rules/deny-write.ts`의 `denyWrite(viewer, rule, ids, err)`(허용 필드) · `.planning/phases/04-project-quote-ledger/04-32-SUMMARY.md`의 「잠금 안 전역 db 호출 금지」 규칙(ARCHITECTURE §4-8)과 `withTransaction`의 `SET LOCAL lock_timeout`
    - `domain/archive/index.ts` 70–91행(`restore`) · `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md`·`04-13-SUMMARY.md`(견적 줄 복원 검사의 이름·종류 판정)
    - `app/(app)/projects/actions.ts`·`actions.registry.ts` · `test/integration/leak-scan.test.ts`(side-effect import 목록)
  </read_first>
  <behavior>
    - 견적 줄 여섯 + 조정 줄 둘(하나는 보관됨)인 1차 → 새 차수(`fromRevisionId` = 1차): 2차에 견적 줄 여섯(각각 계보 = 1차 대응 줄 id, `version` 1, 줄 상태는 원본 그대로), 조정 줄 둘(보관된 것 포함)은 id 그대로 2차 소속, 1차에는 견적 줄 여섯만 남는다
    - 복사 보존(GAP 5b): 비고가 있고 거래처가 지정되고 `custom_fields`에 값이 있으며 취소 상태이고 정렬 순서가 3인 원본 줄 → 복사본의 `note`·`vendor_id`·`sort_order`·`custom_fields`·`line_status`·소분류·`line_kind`·금액 컬럼이 원본과 같다(DB 재조회로 칸마다)
    - 2차의 승인 표시·승인자는 비어 있고 현재 차수는 2차다
    - 이어서 그 보관된 조정 줄을 보관함에서 복원(조정 권한 + 보관함 권한) → 2차의 조정 그룹에 돌아오고 `listQuoteLines(2차)`와 목록 실행가가 그 금액만큼 늘었다(B-21)
    - 같은 `fromRevisionId`(1차)로 두 번째 요청 → `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`, 차수 수 2 그대로(더블클릭·오래된 탭 — 결정적, B-02) + `write.denied` 경고 한 번
    - 견적 줄 0 + 조정 줄 1 → 거부(빈 차수)
    - 정산·완료 → 거부 · 담당 PM이 아닌 `projects` 쓰기 권한자 → 허용(D-53은 주체를 정하지 않았다 — 견적 줄 편집 권리와 같다)
    - 순번 유일 제약 위반: 테스트가 `deps.insertRevision`에 `code: "23505"`·`constraint: "quote_revisions_project_seq_key"`를 `cause`에 담은 합성 오류를 던지는 함수를 주입 → 같은 동시 생성 거부 문구(UserFacing) · 차수 수 그대로 · 복사된 줄·옮긴 조정 줄 없음(반쪽 차수 없음) · `write.denied` 한 번 · 제약 이름이 다른 23505는 그대로 다시 던져진다(엔지니어링 리뷰 B §1 — 다른 연결로 같은 순번 행을 넣는 흉내는 이 함수의 프로젝트 행 `FOR UPDATE`와 FK 검사의 `FOR KEY SHARE`가 부딪혀 멈춘다)
  </behavior>
  <action>
① **리포지토리.** `repositories/quote-revisions.ts`에 `insertRevision(viewer, { projectId, seq }, tx)`(유일 제약 위반은 호출자가 잡는다)를 더하고, `findLatestQuoteRevision`·`findQuoteRevisionById`에 선택 `tx` 인자를 더한다(잠긴 트랜잭션 안에서 읽는다 — 04-40도 쓴다). `repositories/quote-lines.ts`에 `copyQuoteLines(viewer, { fromRevisionId, toRevisionId, withLineage: boolean }, tx)` — **INSERT…SELECT 한 문장**(CEO 리뷰 B-32 · §7 성능)으로 보관되지 않은 `quote`·`out_of_quote` 줄을 새 차수 id로 넣고, 복사본의 `version`은 1이며 **나머지 업무 컬럼은 전부 원본 그대로**(소분류 · 항목 · 거래처 · 수량 · 금액 묶음 · 견적가·실행가·차익 · 줄 상태 · 비고 · 정렬 순서 · `custom_fields` · `line_kind` — 엔지니어링 리뷰 GAP 5b), `withLineage`면 `copied_from_line_id`에 원본 id(04-15의 프로젝트 복사는 `false`) · 넣은 행 수를 돌려준다. SELECT 목록에 컬럼을 빠뜨리면 기본값이 조용히 들어가므로 GAP 5b 통합 케이스가 칸마다 단언한다. `moveAdjustmentLines(viewer, { fromRevisionId, toRevisionId }, tx)` — **보관 여부와 무관하게 모든** 조정 줄의 `revision_id`를 바꾸고 `version`을 올린다(B-21 — 보관된 조정 줄을 두고 가면 나중 복원이 이전 차수에 되살려 화면·목록 합계에서 사라진다). 옮긴 행 수를 돌려준다.

② **판정.** `edit-scope.ts`의 `structuralEditability`에 `canCreateRevision`(수주중·진행·미수주 + `projects` 쓰기 — 사용자 D10으로 정산에 줄 추가가 열렸어도 새 차수는 닫혀 있다)을 더하고, `domain/rules/register.ts`에 `quote.revision-create`(ctx `{ canCreateRevision, copyableLineCount, status }` — 정산 `정산 · 실행가만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김` · 0줄 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기`)을 등록한다. 결정표 케이스(정산·완료·0줄·통과)를 `test/unit/domain/rules-gate.test.ts`에 먼저 쓴다(RED).

③ **domain.** `domain/quotes/revisions.ts`: `createRevisionFromCurrent(viewer, { projectId, fromRevisionId }, deps?: { now?; afterLock?; insertRevision? })` — 행 범위 안 프로젝트 확인과 `can(viewer, "projects", "write")`(`canCreateRevision`의 권한 항)을 **`withTransaction` 앞에서** 계산한다(04-32 규칙 — 잠금 안에서 전역 db를 부르면 동시 요청이 풀 연결을 다 쥔 채 멈춘다, 엔지니어링 리뷰 B §1) → `withTransaction` → **첫 단계 `loadProjectForGate(viewer, projectId, { now, tx, afterLock })`**(04-11 — 배타 잠금 + 잠금 안 자동 정산. 트랜잭션 밖 선판정을 두지 않는다 — A-13·A-33) → 같은 tx로 현재 차수(최신 순번) → **현재 차수 id ≠ `fromRevisionId`이면 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`로 거부**(B-02 — 더블클릭의 두 번째 요청은 Next.js가 직렬화하고, 동시 요청은 잠금이 직렬화하므로 이 확인이 결정적으로 막는다) → 같은 tx로 복사 대상 줄 수 → 게이트(잠근 행의 상태 + 앞서 계산한 권한으로) → `(deps.insertRevision ?? insertRevision)`(순번+1, tx) · 복사(계보) · 조정 이동 → 유일 제약 위반(`lib/pg-errors.ts`의 `isUniqueViolation(e, "quote_revisions_project_seq_key")` — drizzle이 감싼 오류의 `cause`까지 본다)이면 같은 거부 문구, 그 밖의 오류는 그대로 다시 던진다 → 행동 로그 `document_create`(detail `{ kind: "quote_revision", seq, copiedLineCount, movedAdjustmentCount }`)를 04-32의 `recordAction(viewer, entry, { tx })`로 같은 트랜잭션에. 거부는 전부 04-20의 `denyWrite(viewer, rule, { projectId, revisionId: fromRevisionId }, err)`를 지난다(사용자 D19-3 · B-26 — 허용 필드만이라 금액이 실릴 수 없다, 경고 로그를 손으로 부르는 새 지점을 만들지 않는다). 두 연결 결정적 경합 테스트는 04-40이 이 함수의 `deps.afterLock`으로 만든다.

④ **액션.** `createRevisionAction`(zod: `projectId` uuid · `fromRevisionId` uuid — 둘 다 필수)을 `app/(app)/projects/actions.ts`에 더하고 `actions.registry.ts`에 등록한다(D-38). 모달·버튼은 04-24(모달이 보고 있던 차수 id를 싣는다).

⑤ **통합(트레이서 증명).** `test/integration/quote-revisions.test.ts`에 `behavior` 여덟 줄(DB 재조회로 줄 수·계보·`version`·보존 컬럼(GAP 5b)·조정 줄 소속(보관 포함)·1차 잔존·복원 뒤 목록 실행가 — 목록 값은 `Number()`로 비교, 04-17 C-01 전이라 문자열일 수 있다). 23505 케이스는 `deps.insertRevision` 주입으로만 만든다 — 다른 연결에서 같은 순번 행을 넣지 않는다(교착). 보관함 복원은 `domain/archive`의 `restore` + 04-12·04-13의 견적 줄 복원 검사를 그대로 지난다. `write.denied`는 `vi.spyOn(log, "warn")`으로 단언한다. **여러 요청을 한꺼번에 던져 동시 생성을 흉내 내는 테스트를 두지 않는다** — 두 호출이 직렬화되면 둘 다 성공해 흔들린다(B-02). 이 규칙은 여러 호출을 한꺼번에 던지는 모든 형태에 적용된다. 두 연결을 강제하는 결정적 경합은 04-40이 잠금과 함께 만든다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — `quote.revision-create` 결정표(정산·완료·0줄)가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/quote-line-kinds.test.ts test/integration/archive.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 복사·계보·`version`·조정 이동(보관 포함)·복원 위치·보던 차수 불일치 거부·빈 차수 중 하나가 틀렸거나 새 액션이 누수 스캔 커버리지에서 빠졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 차수 생성이 한 트랜잭션(insert·복사·이동)이고, 거부·유일 제약 충돌 뒤 DB에 반쪽 차수가 남지 않는다(통합 테스트 DB 재조회) · 23505 케이스가 `deps.insertRevision` 주입이고 다른 연결 삽입을 쓰지 않는다
    - 복사가 INSERT…SELECT 한 문장이고 복사본 전부에 계보가 있으며 `version`이 1이다 · 비고·거래처·정렬 순서·`custom_fields`·줄 상태가 원본과 같다(GAP 5b) · 조정 줄은 복사본 없이 id가 그대로이고 보관된 조정 줄도 새 차수 소속이다(B-21·B-32)
    - `createRevisionFromCurrent`의 권한 `can()`이 `withTransaction` 앞에 있고, 잠금 뒤의 읽기·행동 로그가 전부 `tx`를 쓴다(잠금 안 전역 db 호출 없음) · 거부가 `denyWrite`를 지난다
    - 보관된 조정 줄을 새 차수 뒤에 복원하면 현재 차수 표와 목록 실행가에 들어간다(통합)
    - `createRevisionAction` 스키마에 `fromRevisionId`가 필수이고, 같은 id로 두 번째 요청이 결정적으로 거부되며 차수 수가 늘지 않는다(B-02)
    - 이 파일에 `Promise.all`로 새 차수 경합을 흉내 내는 케이스가 없다
    - 현재 차수 판정이 최신 순번 하나이고 승인 여부를 보지 않는다
    - `createRevisionFromCurrent`의 트랜잭션 첫 단계가 `loadProjectForGate`이고 `deps.afterLock`을 넘긴다 — 현재 차수·보던 차수·줄 수 판정이 그 뒤 같은 tx의 읽기다(OV-3)
    - 정산·완료의 새 차수 요청이 게이트 문자열로 거부된다(통합)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM의 새 차수 요청이 보던 차수를 확인한 뒤 견적 줄을 계보와 함께 복사하고 조정 줄을 전부 옮기며, 정산·완료·빈 차수·두 번째 요청이 서버에서 막힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 고객 승인 표시와 취소(서버 — 승인일 KST 왕복 · 쓰기 권한 · 빈 차수 거부(ENG-D4) · 본 기준값 확인(ENG-D9)) + 고객 승인 게이트(D-43 + 사용자 D8 미수주 면제) + 승인 토글 게이트 + 거래처 필수 등록(D-64) + 설정 키</name>
  <files>domain/quotes/revisions.ts, domain/rules/register.ts, domain/settings/keys.ts, repositories/quote-revisions.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/unit/domain/rules-gate.test.ts, test/unit/domain/quote-revisions.test.ts, test/integration/quote-revisions.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-43·D-45·D-54·D-56·D-64 원문
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-08·B-25·B-30 절(Grep `### B-08`·`### B-25`·`### B-30`)
    - `domain/settings/keys.ts`(단순값 boolean 키 선례 — `project.force_complete.*`) · 레지스트리 커버리지 테스트 규약
    - `domain/quotes/lines.ts`의 `linkedDocumentsByLine`(04-12 — 연결 문서 조회 한 지점)
    - `lib/kst-date.ts`(04-29 — `kstToday`·`kstDateOf`·`kstDayStart`, 왕복 단위 표 포함) — 승인일 검증의 오늘(KST)과 저장·읽기 쪽 변환. **고치지 않는다**
    - `db/schema/quote-revisions.ts` 17행(`customer_approved_at` — 시간대 없는 timestamp) · drizzle `timestamp()`가 JS `Date`를 UTC 벽시계 값으로 쓰고 같은 규칙으로 읽는다는 점(`node_modules/drizzle-orm/pg-core/columns/timestamp.js`)
    - `test/unit/domain/rules-gate.test.ts`(04-06·04-20·04-12 갱신본)
  </read_first>
  <behavior>
    - `quote.customer-approval`: 수주중 + 미승인 → 통과 · **미수주 + 미승인 → 통과(사용자 D8)** · 진행 + 미승인 + 설정 켬 → `{n}차 고객 승인 전 · 고객 승인 표시`(담당 PM) / `{n}차 고객 승인 전 · 담당 PM {이름}`(그 밖) · 진행 + 승인 → 통과 · 설정 끔 → 통과 · 정산 + 미승인 → 거부 · 완료 + 미승인 → 거부
    - `quote.approval-toggle`: 담당 PM + 진행 + 켜기 → 통과 · 담당이 아닌 사람 → `고객 승인 표시는 담당 PM이 합니다` · 담당 PM이지만 `projects` 쓰기 없음 → 같은 문구(B-30) · 완료 → `완료 · 견적 줄이 잠김` · 정산 + 켜기 → 통과(사용자 D19-9) · 끄기 + 연결 문서 있음 → `연결 문서가 있어 승인 표시를 취소할 수 없습니다 · 새 차수로 고쳐 주세요` · 현재 차수가 아닌 차수 → `현재 차수만 승인 표시할 수 있습니다` · **켜기 + 견적 줄 0개(`approvableLineCount: 0`) → `승인할 견적 줄이 없음 · 첫 줄 만들기`(ENG-D4)** · **켜기 + 기준값 불일치(`basisMatches: false`) → `견적이 바뀌었습니다 · 새로 고쳐 주세요`(ENG-D9)** · 끄기는 줄 수·기준값을 보지 않는다 · 우선순위: 담당·권한 → 완료 → 현재 차수 → 빈 차수 → 기준값 → 연결 문서
    - `quote.vendor-required`: 거래처 없음 → `거래처 없음 · 거래처 고르기` · 있음 → 통과
    - 승인일 왕복: `setCustomerApproval`이 `2026-09-19`로 저장할 순간이 `kstDayStart("2026-09-19")`(= `2026-09-18T15:00:00.000Z`)이고, 승인 DTO의 승인일이 `kstDateOf`로 `2026-09-19`다(B-25 — 도우미 자체의 왕복 표는 04-29 단위 테스트)
    - 승인 기준값(ENG-D9, 통합 — 순차): 담당 PM이 `listRevisionSummaries`로 현재 차수의 합계·토큰을 읽음 → 다른 사람이 수량을 고쳐 저장 → 그 옛 기준값으로 켜기 → `견적이 바뀌었습니다 · 새로 고쳐 주세요`, 승인 컬럼 null 그대로, `write.denied` 한 번 · 새로 읽은 기준값으로 켜기 → 통과 · 실행가만 고친 저장 뒤 옛 기준값 → 역시 거부(줄 버전이 바뀌었다 — 보수적) · 조정 줄만 더한 저장 뒤 옛 기준값 → 통과(조정 줄은 토큰 밖) · 합계는 맞고 토큰만 다른 요청 → 거부
    - 빈 차수(ENG-D4, 통합): 등록 직후 줄 0개인 1차 켜기 → `승인할 견적 줄이 없음 · 첫 줄 만들기`, 승인 컬럼 null · 조정 줄만 있고 견적 줄 0개 → 같은 거부 · 보관된 견적 줄만 있음 → 같은 거부
  </behavior>
  <action>
① `domain/settings/keys.ts`에 `PROJECT_CUSTOMER_APPROVAL_GATE`(`project.customer_approval_gate`, 단순값 boolean, 기본 true, namespace `프로젝트`, 라벨 `고객 승인 게이트`, 힌트 한 문장 `끄면 고객 승인 전 차수에서도 지출결의를 올릴 수 있음`)를 더한다.

② `domain/rules/register.ts`에 `quote.customer-approval`(ctx `{ status, revisionSeq, revisionApproved, gateEnabled, actorIsAssignedPm, pmName }` — **수주중·미수주 예외를 이 규칙 안에 둔다**: 독립 규칙으로 빼지 않는다, 쓰는 규칙이 이것 하나뿐이다. 미수주 면제는 사용자 D8 — D-43 원문 「수주중만」을 넓힌 것이라 규칙 주석에 한 줄) · `quote.approval-toggle`(ctx `{ turningOn, status, actorIsAssignedPm, actorCanWrite, isCurrentRevision, approvableLineCount, basisMatches, hasLinkedDocuments }` — B-30 · **ENG-D4 빈 차수 · ENG-D9 기준값**, 켜기에서만 줄 수·기준값을 본다, 우선순위는 `behavior` 둘째 줄) · `quote.vendor-required`(ctx `{ vendorId }`, 이 페이즈 호출자 없음 — 주석 한 줄 「Phase 5 지출결의·구매 요청이 부른다」)를 등록한다. 결정표 단위 테스트(`behavior` 앞 세 줄)를 먼저 쓴다(RED) — 설정은 ctx로 넣어 DB 없이 돈다.

②-b **승인 기준값(ENG-D9).** `repositories/quote-revisions.ts`에 `approvalBasis(viewer, revisionId, tx)` → `{ totalKrw: number; contentToken: string; approvableLineCount: number }` 한 쿼리: `totalKrw`·`approvableLineCount`는 보관 제외 `quote`·`out_of_quote` 줄의 `coalesce(sum(quote_amount_krw), 0)::bigint`(`.mapWith(Number)`)와 `count(*)::int`, `contentToken`은 같은 두 종류의 **보관 포함** 줄에 대한 `md5(coalesce(string_agg(id::text || ':' || version::text || ':' || (archived_at IS NULL)::text, ',' ORDER BY id), ''))`(FILTER로 한 번에 — 근거는 `<probe_fallback>`). Task 3의 `summarizeRevisions`가 같은 토큰 식을 차수별로 싣는다 — 식은 리포지토리 안 한 상수(`sql` 조각)로 두어 두 쿼리가 갈라지지 않게 한다.

③ **승인일 KST 변환(CEO 리뷰 B-25).** 새 도우미를 만들지 않는다 — 저장은 04-29의 `kstDayStart(ymd)`(그 KST 날짜의 00:00 순간), 읽기는 `kstDateOf(date)`를 쓴다. 둘이 한 쌍이고, `customer_approved_at`을 SQL에서 날짜로 자르거나 화면·도메인이 따로 시간대를 계산하는 코드를 두지 않는다(시간대 없는 timestamp라 SQL 날짜 캐스트는 하루 앞 날짜를 준다). `setCustomerApproval`의 저장·DTO 변환이 이 한 쌍을 쓰는지를 `behavior` 넷째 줄로 `test/unit/domain/quote-revisions.test.ts`(저장 값 계산의 순수 부분)와 통합 ⑥에 둔다(RED → GREEN).

④ `domain/quotes/revisions.ts`에 `setCustomerApproval(viewer, revisionId, input: { approvedOn: string; seenTotalKrw: number; contentToken: string } | null, deps?: { now?; afterLock?; linkedDocuments? })` — 차수로 프로젝트 id 찾기와 `can(viewer, "projects", "write")`(B-30)를 **`withTransaction` 앞에서** 계산한다(04-32 규칙 — 잠금 안 전역 db 호출 금지, 엔지니어링 리뷰 B §1) → `withTransaction` → **첫 단계 `loadProjectForGate(viewer, projectId, { now, tx, afterLock })`**(Task 1과 같은 잠금 — 줄 저장·새 차수와 한 줄로 선다) → 같은 tx로 차수·현재 차수 → 담당 PM 여부(잠근 행의 `pm_user_id`) → 현재 차수 여부 → (켜기면) 같은 tx로 `approvalBasis(viewer, revisionId, tx)`를 읽어 `approvableLineCount`와 `basisMatches`(`seenTotalKrw === totalKrw && contentToken === 서버 토큰`)를 만든다(ENG-D4 · ENG-D9) → (끄기면) 연결 문서 유무 — 조회가 tx를 받게 한다(이 페이즈는 빈 결과 — `deps.linkedDocuments` 주입) → `quote.approval-toggle` → `repositories/quote-revisions.ts`의 `setRevisionApproval`(켜기: `customer_approved_at` = `kstDayStart(approvedOn)`, `customer_approved_by` = viewer / 끄기: 둘 다 null, tx) → 행동 로그 `document_update`(detail `{ kind: "customer_approval", revisionSeq, approvedOn }` 또는 `{ kind: "customer_approval", revisionSeq, cleared: true }` — B-30: Phase 5 결재 필터가 보는 `document_approve`를 쓰지 않는다)를 `recordAction(viewer, entry, { tx })`로 같은 트랜잭션에. 결재를 거치지 않는다. 승인일은 `YYYY-MM-DD`이고 `kstToday()`보다 뒤면 거부한다(`승인일은 오늘까지 적을 수 있습니다`). 거부는 전부 04-20의 `denyWrite(viewer, "quote.approval-toggle", { projectId, revisionId }, err)`를 지난다(D19-3 · B-26 — 기준값 합계는 허용 필드가 아니라 로그에 실리지 않는다). 승인 DTO(04-24 부제)가 싣는 승인일은 서버가 `kstDateOf`로 만든 `YYYY-MM-DD` 문자열이다. 승인 대 저장 경합(오래된 기준값으로 온 승인 포함)은 04-40이 이 함수의 `deps.afterLock`으로 만든다.

⑤ **액션.** `setCustomerApprovalAction`(zod: `revisionId` uuid · `approval`은 `{ approvedOn: z.iso.date(), seenTotalKrw: z.number().int(), contentToken: z.string().length(32) }` 또는 null — 잘못된 날짜가 PG 오류로 가지 않고, 켜기 요청은 기준값 없이는 스키마를 지나지 못한다)을 더하고 등록한다. 모달은 04-24(현재 차수 요약 행의 합계·토큰을 싣는다).

⑥ **통합.** 담당 PM 켜기 → 컬럼·로그(`document_update` · `kind: customer_approval`) · **승인일 DB 왕복: `2026-09-19`로 켜고 다시 읽으면 `2026-09-19`, 저장된 순간은 `2026-09-18T15:00:00Z`(B-25)** · 담당이 아닌 팀장 요청 거부 · 담당 PM이지만 권한표에서 `projects` 쓰기를 끈 경우 거부(B-30) · 완료에서 켜기·끄기 거부(승인일은 그대로 남음) · 정산에서 켜기 통과(D19-9) · 이전 차수 켜기 거부 · 끄기 + 연결 문서(`deps`로 조회 결과를 한 건 주입) 거부 · 미래 승인일 거부 · **빈 차수 켜기 거부(ENG-D4 — `behavior` 해당 줄 셋)** · **기준값 순차 케이스(ENG-D9 — `behavior` 해당 줄 다섯)** · 거부마다 `write.denied` 한 번 · 게이트가 이전 승인 차수를 대신 보지 않는다(2차 미승인 + 1차 승인 → `quote.customer-approval` 거부 — 금지 항목). 테스트의 켜기 호출은 전부 그 순간의 `approvalBasis`(또는 요약 행)에서 기준값을 받아 싣는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-gate.test.ts test/unit/domain/quote-revisions.test.ts test/unit/lib/kst-date.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 고객 승인(미수주 면제 포함)·승인 토글(쓰기 권한 포함)·거래처 필수 결정표나 승인일 KST 왕복이 어긋나거나, 04-11의 KST 날짜 함수가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/settings.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 승인 권한·쓰기 권한·완료·정산·연결 문서·미래 날짜·승인일 왕복·빈 차수 승인(ENG-D4)·옛 기준값 승인(ENG-D9) 중 하나가 틀렸거나 설정 키가 레지스트리 규약을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 게이트 규칙 셋(`quote.customer-approval`·`quote.approval-toggle`·`quote.vendor-required`)이 등록되고 결정표가 `behavior` 앞 세 줄을 덮는다
    - 수주중·미수주 예외가 `quote.customer-approval` 안에 있고(미수주 + 미승인 통과 케이스 — 사용자 D8), 그 선택이 SUMMARY에 한 줄로 적혔다
    - `quote.vendor-required`를 이 페이즈의 어떤 함수도 부르지 않는다(단위 테스트만)
    - `PROJECT_CUSTOMER_APPROVAL_GATE` 키가 있고 설정 화면에 나온다
    - 승인 표시가 결재 경로 없이 저장되고 행동 로그 `document_update`(`kind: customer_approval`) 한 줄이 남는다(B-30)
    - `setCustomerApproval`의 트랜잭션 첫 단계가 `loadProjectForGate`이고 `deps.afterLock`을 넘긴다(OV-3) · `can()`이 트랜잭션 앞이고 잠금 뒤 읽기·로그가 `tx`다
    - 담당 PM이라도 `projects` 쓰기가 없으면 거부된다(통합, B-30)
    - 견적 줄 0개(조정 줄만·보관 줄만 포함) 차수의 켜기가 `승인할 견적 줄이 없음 · 첫 줄 만들기`로 거부된다(단위 + 통합, ENG-D4)
    - 옛 기준값(합계 또는 토큰 불일치)의 켜기가 `견적이 바뀌었습니다 · 새로 고쳐 주세요`로 거부되고 승인 컬럼이 null 그대로이며, 새 기준값은 통과하고, 조정 줄만의 변경은 토큰을 바꾸지 않는다(통합, ENG-D9) · `setCustomerApprovalAction` 스키마가 켜기에 `seenTotalKrw`·`contentToken`을 필수로 받는다
    - `approvalBasis`와 `summarizeRevisions`의 토큰 식이 리포지토리의 한 `sql` 조각이다
    - 승인일이 04-29의 `kstDayStart` ↔ `kstDateOf` 한 쌍으로만 저장·읽기되고(이 플랜이 `lib/kst-date.ts`를 고치지 않는다) KST 자정 왕복이 통합에 있다(B-25)
    - 이전 승인 차수로 대신하지 않는다는 케이스가 통합 테스트에 있다(금지 항목)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM의 고객 승인 켜기·끄기가 서버에서 조건대로 판정되고 승인일이 KST 날짜 그대로 왕복하며, 고객 승인(수주중·미수주 면제)·거래처 필수 게이트가 Phase 5를 위해 등록·고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 계보 해석(D-55) + 차수 요약(DTO 명세·누수 스캔·숫자 합계) · 표시 번호 + 이전 차수 잠김 조회</name>
  <files>domain/quotes/revisions.ts, domain/quotes/lines.ts, repositories/quote-revisions.ts, test/unit/domain/quote-revisions.test.ts, test/integration/quote-revisions.test.ts, test/integration/leak-scan.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` U-2 행 — 사용자 확정 값(최신+승인 = `승인`만, 미승인 이전 = 빈 칸)
    - `domain/quotes/lines.ts`의 `linkedDocumentsByLine`·`listQuoteLines`·`QUOTE_LINE_DTO_SPEC`·`registerDto` 호출(04-12 — 조회 지점, 셀 단계 계산, DTO 명세 선례)
    - `domain/permissions/project.ts`(`project(viewer, row, spec)` 투영) · `domain/permissions/dto-registry.ts`(`registerDto`)
    - `test/integration/leak-scan.test.ts` 1–40행(side-effect import 목록 — 새 domain 모듈 등록 자리) · `test/integration/revenue-entries.test.ts`의 (d) 키 집합 단언 형태
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` C-01 절(Grep `### C-01`) — 돈 합계를 `::bigint` 뒤 `mapWith(Number)`로 리포지토리 경계에서 숫자로 바꾸는 규칙
  </read_first>
  <behavior>
    - `quoteDisplayNumber("26001", 2)` → `26001-2차`
    - `resolveLinkedDocumentsByLineage`: 1차 줄 A에 문서 X, 2차 줄 B(계보 A), 3차 줄 C(계보 B) → 현재 차수 C에 X가 이어진다 · 계보가 끊긴(새 차수에서 지운) 줄의 문서는 어느 현재 줄에도 붙지 않고 「차수에서 빠진 줄의 문서」 목록으로 따로 돌아온다(D-55 「견적 외 비용처럼 프로젝트에 남는다」)
    - `listRevisionSummaries`: 차수 셋(1차 승인 · 2차 미승인 · 3차 미승인 최신) → 상태 `[3차: 현재, 2차: 빈 값, 1차: 승인]` · 최신이 승인이면 `승인`만 · 견적 합계가 `typeof === "number"`(1,500,000,000짜리 줄 둘이어도 정확한 합)
    - `quote.amount`를 숨긴 계급으로 `listRevisionSummaries` → 행마다 견적 합계 키가 없다(순번·생성일·줄 수·상태는 있다) — B-20
    - 이전 차수 잠김 조회(통합 — GAP 5c): 조정 줄이 있는 프로젝트에 2차를 만든 뒤 `listQuoteLines(viewer, projectId, { revisionSeq: 1 })` → 돌아온 **모든 줄의 모든** `cellEditability` 값이 `locked`(쓰기 권한자에게도) · `lineKind === "adjustment"`인 줄이 0개 · 줄 id가 1차 줄 id와 같다 · `quote.amount`를 숨긴 계급으로 같은 조회 → 줄마다 견적 금액 키(단가·견적가·차익)가 현재 차수 조회와 같이 없다 · 현재 이상 또는 없는 순번이면 현재 차수 조회로 떨어진다
    - `listRevisionSummaries`의 현재 차수 행 `contentToken`이 같은 순간의 `approvalBasis(…).contentToken`과 같다(ENG-D9 — 04-24 모달이 싣는 값)
  </behavior>
  <action>
① 단위 테스트(`behavior` 앞 세 줄의 순수 부분)를 먼저 쓴다(RED). `domain/quotes/revisions.ts`에 `quoteDisplayNumber(projectNumber, seq)` → `${projectNumber}-${seq}차`(저장 없음) · 순수 함수 `resolveLinkedDocumentsByLineage(linesAcrossRevisions, docsByLineId)`(계보 사슬을 따라 현재 차수 줄에 문서를 잇고, 끊긴 계보의 문서는 별도 목록 — **문서 출처가 Phase 5라 이 함수와 빈 결과 말고 저장·캐시·화면을 더하지 않는다**, CEO 리뷰 B-33) · `listRevisionSummaries(viewer, projectId)`(순번 · 생성일(KST — `kstDateOf`) · 줄 수(견적 줄+견적 외 비용, 보관 제외) · 견적 합계 · 상태 낱말 — U-2 확정값)를 더한다(GREEN).

② **요약 쿼리(CEO 리뷰 §7 · B-18).** `repositories/quote-revisions.ts`에 `summarizeRevisions(viewer, projectId)` — 차수별 줄 수·견적 합계·내용 토큰(Task 2 ②-b의 같은 `sql` 조각 — ENG-D9)을 **GROUP BY 한 번**으로(차수마다 따로 묻지 않는다, 보관·종류 조건은 FILTER), 합계는 `sql<number>` `coalesce(sum(quote_amount_krw), 0)::bigint`에 `.mapWith(Number)`, 줄 수는 `count(*)::int`로 리포지토리 경계에서 JS 숫자로 바꾼다(node-pg는 bigint를 문자열로 돌려준다 — 04-17 C-01과 같은 규칙).

③ **요약 DTO 명세(CEO 리뷰 B-20).** `REVISION_SUMMARY_DTO_SPEC`을 두어 견적 합계 필드를 정보 항목 `quote.amount`로, 나머지(순번·생성일·줄 수·상태·승인일·승인자·내용 토큰 — 토큰은 줄 id·버전의 해시라 금액이 없다)를 `project.value`로 게이트하고 `listRevisionSummaries`가 04-32의 `projectMany(viewer, rows, REVISION_SUMMARY_DTO_SPEC)`로 투영한다(여러 행을 정보 항목 수만큼의 조회로 — 엔지니어링 리뷰 ENG-D3 ②). `registerDto`로 등록하고 `test/integration/leak-scan.test.ts`의 side-effect import에 `@/domain/quotes/revisions`를 더한다(없으면 누수 스캔이 이 DTO를 모른다). 통합 테스트에 `behavior` 넷째 줄(키 집합)을 더한다.

④ `domain/quotes/lines.ts`의 `linkedDocumentsByLine`이 문서 출처(이 페이즈 빈 결과)에서 받은 것을 이 해석 함수로 현재 차수에 잇게 한다 — 조회 지점은 여전히 하나다. `listQuoteLines`에 이전 차수 잠김 조회 옵션(`{ revisionSeq }` — 현재보다 작은 순번이면 셀 단계 전부 `locked`, 조정 줄 없음; 그 밖은 현재 차수)을 더한다 — 투영은 현재 차수 조회와 같은 `QUOTE_LINE_DTO_SPEC`을 지난다(새 명세를 만들지 않는다). 04-24가 `?revision=` 보기에 이 옵션을 쓴다. `behavior`의 잠김 조회 통합(GAP 5c — 모든 셀 `locked` · 조정 줄 0 · 숨긴 계급 키 집합)을 `quote-revisions.test.ts`에 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-revisions.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 계보 해석이나 상태 낱말이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 요약 합계가 숫자가 아니거나, `quote.amount`를 숨긴 계급에 합계가 새거나, 요약 DTO가 누수 스캔에 없거나, 조회 지점 변경이 기존 저장·DTO를 깨뜨렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - `resolveLinkedDocumentsByLineage`가 순수 함수이고 계보 사슬·끊긴 계보 두 경우가 단위 테스트에 있다
    - `linkedDocumentsByLine`이 여전히 유일한 조회 지점이고 이 해석 함수를 거친다
    - 견적 표시 번호에 저장 컬럼·카운터가 없다
    - 차수 요약의 상태 낱말이 U-2 확정값이다(단위)
    - 차수 요약이 GROUP BY 한 번이고 합계가 JS 숫자다(통합 `typeof`, B-18)
    - `REVISION_SUMMARY_DTO_SPEC`이 합계를 `quote.amount`로 게이트하고 `registerDto` 등록 + 누수 스캔 import + 키 집합 단언이 있다(B-20)
    - 이전 차수 잠김 조회가 모든 줄의 모든 셀 `locked`·조정 줄 0개·`quote.amount` 숨긴 계급의 금액 키 부재를 통합에서 단언한다(GAP 5c)
    - 요약 행에 내용 토큰이 있고 `approvalBasis`의 토큰과 같다(ENG-D9)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>연결 문서가 계보를 따라 현재 줄에 이어질 자리가 Phase 5를 위해 준비되고, 04-24가 그릴 차수 요약(승인 기준값 포함)·표시 번호·잠김 조회가 서버에서 나온다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 새 차수·승인 액션 | 프로젝트·차수 id와 승인일이 넘어온다. 담당 PM 판정·상태·연결 문서 검사가 서버에만 있어야 한다 |
| 게이트 → Phase 5 | 고객 승인·거래처 필수 규칙이 지출 동작의 단일 판정 지점이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-68 | Elevation of Privilege | 고객 승인 표시 | high | mitigate | 서버가 `viewer.id = projects.pm_user_id` **그리고** `projects` 쓰기 권한으로 담당 PM을 판정하고(버튼을 숨기는 것과 별개 — 화면 쪽은 04-24, B-30), 완료·이전 차수·미래 날짜를 거부한다. 담당이 아닌 팀장 · 쓰기를 거둔 PM 요청 거부를 통합 테스트로 고정한다 |
| T-04-69 | Tampering | 승인 표시 취소 | high | mitigate | 연결 문서 유무를 서버의 조회 지점이 판정한다 — 클라이언트의 「문서 없음」 주장을 받지 않는다 |
| T-04-70 | Tampering | 새 차수 | medium | mitigate | 한 트랜잭션(insert·복사·이동)이라 반쪽 차수가 남지 않고, 요청이 보던 차수 id(`fromRevisionId`)를 싣고 서버가 현재 차수와 다르면 거부해 더블클릭·오래된 탭의 두 번째 차수를 막는다(B-02). 같은 순간의 두 요청은 `loadProjectForGate`의 프로젝트 행 잠금이 직렬화하고 `(project_id, seq)` 유일 제약이 마지막으로 막는다(두 연결 경합 증명은 04-40). 정산·완료·빈 차수는 게이트가 거부한다 |
| T-04-71 | Elevation of Privilege | 승인 게이트 우회 | high | mitigate | 게이트가 최신 순번 차수 하나만 본다(D-54) — 이전 승인 차수로 대신하지 않는다(금지 항목, 통합 테스트). 설정으로 끄는 것과 수주중·미수주 면제(D-43 · 사용자 D8)만 허용된 우회다(ROADMAP 기준 3) |
| T-04-72 | Repudiation | 승인 표시 | low | mitigate | 켜기·끄기가 행동 로그 `document_update`(`kind: customer_approval`)에 차수·승인일과 함께 남고, 승인자는 `customer_approved_by`에 저장된다. 거부된 요청은 `write.denied` 경고로 남는다(사용자 D19-3 · B-26) |
| T-04-72b | Tampering | 승인일 시간대 | medium | mitigate | 승인일은 04-29의 `kstDayStart` ↔ `kstDateOf` 한 쌍으로만 KST 00:00 순간 ↔ 달력 날짜로 바뀌고, SQL 날짜 캐스트를 쓰지 않는다 — 하루 앞 날짜로 보이거나 미래 날짜 판정이 어긋나는 길이 없다(B-25). 액션 스키마가 날짜 형식을 검증해 잘못된 날짜가 PG 오류로 가지 않는다 |
| T-04-72c | Information Disclosure | 차수 요약의 견적 합계 | medium | mitigate | 요약 DTO 명세가 합계를 `quote.amount`로 게이트하고 누수 스캔 레지스트리에 등록된다 — 견적 금액을 숨긴 계급에 합계 키가 오지 않는다(B-20, 키 집합 통합 테스트). 이전 차수 잠김 조회도 같은 명세를 지난다(GAP 5c) |
| T-04-72d | Tampering | 본 적 없는 금액의 승인(오래된 화면) | high | mitigate | 승인 요청이 PM이 본 합계와 내용 토큰을 싣고, 서버가 잠금 뒤 같은 tx로 다시 계산해 다르면 거부한다(ENG-D9 · Codex #2). 순차 통합 케이스는 여기, 두 연결 경합은 04-40 |
| T-04-72e | Tampering | 빈 차수 승인으로 막다른 길 | medium | mitigate | 견적 줄 0개 차수의 승인 표시를 게이트가 거부한다(ENG-D4) — 승인 뒤 견적가 있는 줄도 새 차수도 못 만드는 상태가 생기지 않는다 |
| T-04-72f | Denial of Service | 잠금 안 풀 연결 대기 | medium | mitigate | 두 쓰기 함수의 `can()`을 트랜잭션 앞에서 계산하고, 잠금 뒤 읽기·연결 문서·행동 로그가 `tx`를 쓴다 — 행 잠금을 쥔 채 두 번째 풀 연결을 기다리지 않는다. 대기는 04-32의 `lock_timeout`이 오류로 끊는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- `CI=true pnpm test` 세 계층 통과
- `register.ts`에 `quote.revision-create`·`quote.customer-approval`(수주중·미수주 면제)·`quote.approval-toggle`·`quote.vendor-required` 등록
- `package.json` 의존성 객체(dependencies·devDependencies·optionalDependencies·peerDependencies)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있다 (원문 비교 아님 — 04-03 재개 시 scripts 추가로 깨지지 않게)
</verification>

<success_criteria>
- 새 차수가 보던 차수를 확인한 뒤 전체 복사로 만들어지고 조정 줄(보관 포함)은 옮겨 가며, 최신 차수만 현재다
- 담당 PM(쓰기 권한 포함)이 현재 차수의 고객 승인을 KST 날짜 그대로 켜고 조건대로만 끄는 것이 서버에서 판정된다
- Phase 5는 지출결의에서 `quote.customer-approval`·`quote.vendor-required`를 부르고 연결 문서 출처를 채우기만 하면 된다
- 새 차수·승인이 줄 저장과 같은 프로젝트 행 잠금으로 한 줄로 서고, 04-40이 그 위에 현재 차수 재확인·경합 증명·승인 차수 잠금을 더하며, 04-24가 액션·요약·잠김 조회만으로 차수 화면을 그릴 수 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/quotes/revisions.ts` — `createRevisionFromCurrent`(`{ projectId, fromRevisionId }`, `deps.insertRevision` 주입 지점) · `setCustomerApproval`(켜기 입력 `{ approvedOn, seenTotalKrw, contentToken }`) · `listRevisionSummaries`(내용 토큰 포함) · `REVISION_SUMMARY_DTO_SPEC`(registerDto) · `quoteDisplayNumber` · `resolveLinkedDocumentsByLineage` |
| 함수(repository) | `insertRevision` · `setRevisionApproval` · `approvalBasis`(합계·토큰·승인 가능 줄 수 — ENG-D4·D9) · `summarizeRevisions`(GROUP BY 한 번, 숫자 합계, 같은 토큰 식) · `findLatestQuoteRevision`·`findQuoteRevisionById`의 선택 `tx`(`quote-revisions.ts`) · `copyQuoteLines`(INSERT…SELECT, `version` 1, 업무 컬럼 전부 보존) · `moveAdjustmentLines`(보관 포함 전부)(`quote-lines.ts`) |
| 게이트 규칙(신규) | `quote.revision-create` · `quote.customer-approval`(수주중·미수주 예외 포함 — 사용자 D8) · `quote.approval-toggle`(쓰기 권한 — B-30 · 빈 차수 — ENG-D4 · 기준값 — ENG-D9) · `quote.vendor-required`(등록만) |
| 거부 문구(신규 — Copywriting은 04-24) | `승인할 견적 줄이 없음 · 첫 줄 만들기`(ENG-D4) · `견적이 바뀌었습니다 · 새로 고쳐 주세요`(ENG-D9) |
| 설정 키(신규) | `project.customer_approval_gate`(`PROJECT_CUSTOMER_APPROVAL_GATE`, 단순값, 기본 켬) |
| Server Action | `createRevisionAction`(`projectId`·`fromRevisionId`) · `setCustomerApprovalAction`(`revisionId`·`approval` = `{ approvedOn, seenTotalKrw, contentToken }` 또는 null)(등록 포함) |
| 행동 로그 | 새 차수 `document_create`(`kind: quote_revision`) · 승인 `document_update`(`kind: customer_approval`) |
| 로그 이벤트 | `write.denied`(경고 — 새 차수·승인 거부, 금액 없음) |
| 재사용 | `loadProjectForGate`(04-11 — 04-20 `lockProjectForWrite` 배타 잠금) — 새 차수·승인의 트랜잭션 첫 단계, `deps.afterLock` 전달 |
| 변경된 기존 심볼 | `structuralEditability.canCreateRevision` · `linkedDocumentsByLine`(계보 해석 경유) · `listQuoteLines`(이전 차수 잠김 조회 옵션) · `leak-scan.test.ts` import 목록 |
| 테스트 | `test/unit/domain/quote-revisions.test.ts` · `test/integration/quote-revisions.test.ts`(신규) · `rules-gate.test.ts` 확장 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 모달 셋(`revision-dialogs.tsx`) · 차수 섹션(`revision-section.tsx`) · 머리 줄 버튼·부제 · 이전 차수 읽기 표 화면 · UI-SPEC Copywriting 행 · `test/e2e/quote-revisions.spec.ts` · S5 backstop 둘(04-24) · **줄 저장의 현재 차수 재확인·여러 차수 소속·일치 증명·두 연결 결정적 경합·승인 차수 견적 칸 잠금과 승인 합계 불변(04-40 — CEO 리뷰 B-01·OV-3·B-07·OV-1)** |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-02 — 더블클릭·오래된 탭이 되돌릴 수 없는 차수를 하나 더 만듦 · `Promise.all` 테스트가 흔들림 | CRITICAL | Plan 04-14, Task 1 ③④⑤: `fromRevisionId` 필수 + 잠금 뒤 불일치 거부 + 결정적 순차 테스트, 한꺼번에 던지는 테스트 제거 (두 연결 경합 증명은 Plan 04-40 Task 1) |
| OV-3 · B-01 (T2의 04-14 몫) · A-13·A-33 — 새 차수·승인에 잠금 없음, 선판정 자리 | CRITICAL | Plan 04-14, Task 1 ③ · Task 2 ④: 트랜잭션 첫 단계 `loadProjectForGate`(Plan 04-11 — 04-20 배타 잠금 + 잠금 안 선판정) · `deps.afterLock` 전달 · 수용 기준 |
| B-21 — 보관된 조정 줄을 옮기지 않아 복원 시 안 보이고 안 세어짐 | WARNING | Plan 04-14, Task 1 ①⑤: 전부 이동 + 보관→새 차수→복원 통합 케이스 |
| B-32 — 복사 방식·`version` 미정 | NOTE | Plan 04-14, Task 1 ①: INSERT…SELECT, `version` 1, 상한 재검사 없음의 근거(A-20) |
| 사용자 D8 · B-08 (T13) — 미수주도 고객 승인 게이트 면제 | DECISION | Plan 04-14, Task 2 ②: 규칙 안 면제 + 결정표 케이스 |
| B-25 — 승인일이 시간대 없는 timestamp라 하루 어긋남 | WARNING | Plan 04-14, Task 2 ③④⑥: 04-29의 `kstDayStart` ↔ `kstDateOf` 한 쌍(새 도우미 없음), DB 왕복 통합 |
| B-30 — 승인이 쓰기 권한을 요구하지 않음 · 행동 종류 미정 | NOTE | Plan 04-14, Task 2 ②④⑥: `projects` 쓰기 요구 + `document_update`(`kind: customer_approval`) |
| B-20 — 차수 요약 합계가 DTO 명세·누수 스캔 밖 | WARNING | Plan 04-14, Task 3 ③: `REVISION_SUMMARY_DTO_SPEC`(`quote.amount`) + registerDto + 누수 스캔 import + 키 집합 |
| B-18 — 요약 SUM이 문자열 | WARNING | Plan 04-14, Task 3 ②: `::bigint` + `mapWith(Number)`, GROUP BY 한 번 |
| B-33 — 계보 해석은 Phase 5 전까지 데이터 없음 | NOTE | Plan 04-14, Task 3 ①: 순수 함수·빈 결과로 한정(확장 금지) |
| 사용자 D19-3 · B-26 — 거부된 쓰기 로그 없음 | DECISION | Plan 04-14, Task 1 ③ · Task 2 ④: `write.denied` 경고 |
| 사용자 D19-9 — 상태별 고객 승인 = UI-SPEC 기본값 | DECISION | Plan 04-14, `<probe_fallback>`·Task 2 behavior: 정산 허용·완료 거부 확정 |
| 사용자 D19-10 — 조정 줄은 복사하지 않고 새 차수로 이동 | DECISION | Plan 04-14, truths·Task 1: 확정 반영(보관 포함) |
| 사용자 D10 — 정산의 새 차수는 서버 거부 | DECISION | Plan 04-14, Task 1 ②: `canCreateRevision` 정산 제외 유지 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-D4 (R3) — 빈 차수 승인이 막다른 길 | DECISION | Plan 04-14, Task 2 ②④⑥: `quote.approval-toggle` 빈 차수 거부 + 단위·통합 |
| ENG-D9 · Codex #2 (R8) — 승인이 PM이 본 내용을 가리키지 않음 | DECISION(P1) | Plan 04-14, Task 2 ②-b④⑤⑥: `approvalBasis`(합계 + md5 내용 토큰) · 잠금 뒤 비교 · 스키마 필수 · 순차 통합 · Task 3 ②: 요약 행 토큰 (경합은 Plan 04-40, 화면은 Plan 04-24) |
| B §1 — 23505 테스트가 자기 잠금과 교착 | P2 | Plan 04-14, Task 1 behavior·③⑤: `deps.insertRevision` 합성 오류 주입 |
| B §1 · A §1 — 잠금 안 전역 db 호출(풀 고갈) | P2 | Plan 04-14, Task 1 ③ · Task 2 ④: `can()`을 트랜잭션 앞으로, 잠금 뒤 읽기·로그는 `tx` |
| A §1 유령 로그 | P2 | Plan 04-14, Task 1 ③ · Task 2 ④: `recordAction(…, { tx })` |
| `denyWrite` 한 도우미 | P2 | Plan 04-14, Task 1 ③ · Task 2 ④ |
| GAP 5b — 복사 보존 컬럼 | GAP | Plan 04-14, Task 1 ①·behavior·⑤ |
| GAP 5c — 이전 차수 잠김 조회 단언 | GAP | Plan 04-14, Task 3 behavior·④ |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-01 · OV-3 · T2 — 줄 저장의 현재 차수 재확인·여러 차수 소속·일치 증명·두 연결 경합 | 이 플랜을 15파일 미만으로 지키려고 분할 — Plan 04-40(바로 뒤)이 맡는다. 잠금 자체는 이 플랜(새 차수·승인)과 Plan 04-12(줄 저장)가 건다 |
| B-07 · OV-1 · 사용자 D7 (T4) — 승인 차수 견적 칸 잠금·승인 합계 불변 | 같은 분할 — Plan 04-40 Task 3 |
| ENG-D9 두 연결 경합(저장이 잠금을 쥔 사이 옛 기준값의 승인) · 미저장 편집 중 승인 버튼 비활성 | Plan 04-40 Task 3 경합 (D) · Plan 04-24 Task 2 |
| 엔지니어링 리뷰 §4 — 새 차수마다 줄이 늘어 `lineSumsSubquery`가 전 표를 묶음 | 04-17 소유(현재 차수만 합산 — 그룹 C). 이 플랜은 성능 TODO로만 둔다 |


<output>
Create `.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md` when done
</output>
