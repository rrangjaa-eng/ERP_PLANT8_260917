---
phase: 04-project-quote-ledger
plan: 14
type: execute
wave: 10
depends_on: ["04-13"]
files_modified:
  - domain/quotes/revisions.ts
  - domain/quotes/edit-scope.ts
  - domain/quotes/lines.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - repositories/quote-revisions.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revision-section.tsx
  - app/(app)/projects/[id]/revision-dialogs.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/quote-revisions.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/quote-revisions.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/quote-revisions.spec.ts
  - .planning/phases/04-project-quote-ledger/04-UI-SPEC.md
autonomous: true
requirements: [PROJ-07, PROJ-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "새 차수는 현재 차수의 견적 줄(견적 줄·견적 외 비용, 보관 제외) 전체를 한 트랜잭션에서 복사해 만들고, 복사된 줄마다 원본 줄 id를 계보(`copied_from_line_id`)로 갖는다 — 새 차수는 승인 표시가 꺼져 있다(D-53)"
    - "복사할 견적 줄이 0개면 새 차수를 만들 수 없다 — 빈 차수가 없다(D-53)"
    - "조정 줄은 새 차수로 복사되지 않고 같은 줄(값·작성자·id 그대로)이 새 현재 차수로 옮겨 가 맨 아래 `조정` 그룹에 계속 보인다 — 이전 차수 읽기 표에는 조정 그룹이 없고, 목록의 실행가·수익금은 차수가 바뀌어도 조정 금액만큼 그대로다(⚠ S4 「조정 줄과 복사」 기본값, D-87)"
    - "「복사해 새 차수」 확인 모달의 `{k}줄`은 조정 줄을 세지 않고, 조정 줄이 있으면 결과 줄 `조정 {m}줄은 복사하지 않고 {n+1}차에서도 그대로 보입니다`가 더해진다 · 1차 `새 차수 만들기` · 2차 `취소 Esc` · 성공 토스트 `새 차수 · 상세 견적 {n+1}차`(Copywriting Destructive — 새 차수, SUCCESS)"
    - "「복사해 새 차수」는 수주중·진행·미수주에서만 렌더되고, 정산·완료에서는 없으며 서버도 거부한다(S3, ⚠ S4 정산 구조 잠금 · D-47)"
    - "최신 차수만 현재 차수다 — 승인 여부와 무관하게 순번이 가장 큰 차수이고, 차수 되돌리기 동작이 어디에도 없다(D-53·D-54)"
    - "차수의 고객 승인 표시는 담당 PM이 승인일과 함께 켜고 결재를 거치지 않으며 행동 로그에 남는다 — 담당이 아닌 사람의 요청은 서버가 거부한다(D-56)"
    - "「고객 승인 표시」·「승인 표시 취소」는 수주중·진행·미수주·정산에서 담당 PM에게만 렌더되고 완료에서는 둘 다 없다 — 서버도 완료의 승인 변경을 거부하며, 승인일은 완료에서도 부제에 그대로 보인다(⚠ S3 「상태별 고객 승인」 기본값)"
    - "승인 표시 취소는 그 차수에 연결 문서가 없을 때만 되고, 있으면 버튼이 없으며 서버는 `연결 문서가 있어 승인 표시를 취소할 수 없습니다 · 새 차수로 고쳐 주세요`로 거부한다(D-56)"
    - "승인 표시 취소 모달은 제목 `고객 승인 표시 취소` · 본문 `{차수} 승인일이 지워집니다 · 이 차수의 지출결의가 다시 막힙니다` · 1차 `승인 표시 취소` · 2차 `닫기 Esc`다(Copywriting, §7-8 보강)"
    - "부제에 `고객 승인 2026-09-19 박서연`이 붙고 견적 표시 번호 `26001-2차`는 저장 컬럼·카운터 없이 프로젝트 번호와 순번에서 만든다(D-56)"
    - "고객 승인 게이트 규칙 `quote.customer-approval`이 등록되어 있다 — 수주중이면 승인과 무관하게 통과, 진행 이후이고 현재 차수가 미승인이면 거부(담당 PM에게 `{n}차 고객 승인 전 · 고객 승인 표시`, 그 밖 `{n}차 고객 승인 전 · 담당 PM {이름}`), 설정 `project.customer_approval_gate`를 끄면 통과한다 — 이 페이즈에는 호출자가 없고 Phase 5 지출결의가 부른다(D-43, ROADMAP 기준 3, PROJ-07)"
    - "거래처 필수 규칙 `quote.vendor-required`가 등록되어 있다(`거래처 없음 · 거래처 고르기`) — 호출은 Phase 5다(D-64)"
    - "이전 차수 줄의 연결 문서가 계보를 따라 현재 차수의 대응 줄에 이어 보이도록 해석하는 함수가 연결 문서 조회 지점 안에 있다 — 문서 행을 옮기지 않는다(D-55; 문서 출처는 Phase 5)"
    - "차수 섹션은 차수 · 생성일 · 줄 수 · 견적 합계 · 상태 · 동작 여섯 열, 최신이 위이고, 편집 가능한 셀이 0이라 캡션 있는 읽기 표(흰 머리글)다 — 이력 목록 컴포넌트를 쓰지 않는다(S5)"
    - "차수 섹션 상태 열: 최신이면서 미승인 = `현재`(accent) · 승인된 차수 = `승인`(success, 최신이어도 `승인`만) · 승인 없이 지나간 이전 차수 = 빈 칸(`—`로 채우지 않는다) · 현재 차수 행의 동작 칸도 빈 칸(U-2 사용자 확정, S5)"
    - "이전 차수 행의 3차 「차수 열기」는 그 차수를 잠김 모양의 읽기 표로 보여 주고(0줄이면 `이 차수에 견적 줄이 없습니다`), 되돌리기·편집·저장이 없다(S5, Copywriting Empty — 이전 차수)"
    - "차수 섹션 폰 P1은 차수·견적 합계·(동작), 접힌 줄은 상태·생성일·줄 수이고 빈 값 자리를 건너뛴다(S5)"
    - statement: "차수 섹션은 제 오류 자리를 갖지 않고 상세 로드 실패 한 줄에 흡수된다 — 섹션 단독 오류 렌더가 없다"
      verification: backstop
    - statement: "차수 섹션이 375px에서 가로 스크롤 0이고 현재 행의 빈 동작 칸을 `—`로 채우지 않는다 — DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "승인 전 차수에서 돈이 나가는 길을 「승인된 이전 차수」로 우회하게 해서는 안 된다. 현재 차수는 최신 순번 하나뿐이고(D-54), 게이트는 이전 승인 차수를 대신 보지 않는다 — 승인을 덜 받으려는 습관을 막는 것이 이 규칙의 목적이다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/revisions.ts"
      provides: "새 차수(전체 복사 + 조정 줄 이동) · 고객 승인 표시와 취소 · 차수 요약 · 견적 표시 번호 · 계보 해석"
      exports: ["createRevisionFromCurrent", "setCustomerApproval", "listRevisionSummaries", "quoteDisplayNumber", "resolveLinkedDocumentsByLineage"]
    - path: "app/(app)/projects/[id]/revision-section.tsx"
      provides: "차수 섹션 — ui/table 읽기 형태, 최신이 위, 이전 차수에만 「차수 열기」"
      contains: "caption"
    - path: "app/(app)/projects/[id]/revision-dialogs.tsx"
      provides: "복사해 새 차수 · 고객 승인 표시 · 승인 표시 취소 모달"
      contains: "createRevisionAction"
    - path: "test/integration/quote-revisions.test.ts"
      provides: "전체 복사·계보·조정 이동·빈 차수 거부·승인 권한·완료 거부·취소 조건·동시 생성 증명"
      min_lines: 60
    - path: "test/e2e/quote-revisions.spec.ts"
      provides: "새 차수 · 승인 표시와 취소 · 차수 열기 읽기 표 스모크"
      min_lines: 40
  key_links:
    - from: "domain/quotes/revisions.ts"
      to: "repositories/quote-lines.ts"
      via: "새 차수가 한 트랜잭션에서 견적 줄을 계보와 함께 복사하고 조정 줄을 옮긴다"
      pattern: "copied_from_line_id"
    - from: "domain/quotes/revisions.ts"
      to: "domain/rules/gate.ts"
      via: "새 차수·승인 변경이 게이트(`quote.revision-create`·`quote.approval-toggle`)를 지난다"
      pattern: "gate("
    - from: "domain/rules/register.ts"
      to: "domain/settings/keys.ts"
      via: "고객 승인 게이트가 설정 키로 꺼진다"
      pattern: "project.customer_approval_gate"
---

<objective>
**As a** 기획 PM, **I want to** 네고로 바뀐 견적을 이전 차수를 통째로 복사한 새 차수로 고치고, 고객이 승인한 차수에 승인일을 표시하며, 지난 차수를 읽기 전용으로 열어 보고 싶다, **so that** 견적의 변천이 차수로 남고 「현재 차수가 승인됐는가」 하나로 지출(Phase 5)과 계약 금액(04-16)이 정해진다.

이 플랜은 옛 04-06 Task 2·3에서 **차수 기계와 고객 승인**을 옮겨 왔다(D-53~D-56 · D-43 · D-64 거래처 필수 등록). 2026-09-23 결정이 더한 것: 정산·완료에서 새 차수가 없다(⚠ S4 기본값) · 조정 줄은 복사되지 않고 새 현재 차수로 옮겨 간다(⚠ S4 기본값) · 완료에서는 승인 표시 변경이 없다(⚠ S3 기본값).

Purpose: ROADMAP 기준 3 — 차수마다 고객 승인 표시가 있고 승인 전 차수의 줄에서는 지출 동작이 게이트로 막힌다(설정으로 끌 수 있음). 이 페이즈에는 지출결의가 없으므로 게이트 규칙을 등록하고 단위 테스트로 판정을 고정하며, Phase 5가 호출자만 붙인다.

Output: 차수 domain · 게이트 규칙 넷 · 설정 키 하나 · 모달 셋 · 차수 섹션 · 이전 차수 읽기 표 · 단위 2 · 통합 1 · E2E 1.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-13-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 463–471행(S5 상태) · 1154–1194행(S5 적용 규칙 — 왜 이력 목록이 아닌가 · 열 · 폰 우선순위 · 상태 낱말 · 차수 열기 · 복사해 새 차수) · 1057–1094행(S3 머리 줄 — 버튼 렌더 조건 · 고객 승인 버튼 · 상태별) · 439행·457–461행(⚠ S3 · ⚠ S4 조정 줄과 복사) · 328행·360행·366행·370–371행(Copywriting)
# .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md U-2 행(사용자 확정 값)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(차수·고객 승인)** 과 **PROJ-05(견적 줄 복사 — 차수 전체 복사 쪽)** 다. 수용 기준은 D-43·D-53~D-56·D-64·ROADMAP 기준 3과 UI-SPEC S3·S5에서 끌어왔다. S5 `## UI Considerations`의 error backstop과 overflow는 마커로, empty(구조적으로 없음)·loading(상세 한 번)·populated·partial(U-2)·zero-one-many는 truths로 옮겼다.

**계획 단계 가정(사용자에게 표면화):**
- **⚠ S3 상태별 고객 승인 — 기본값 적용:** 수주중·진행·미수주·정산 = 렌더, 완료 = 둘 다 없음(서버도 거부).
- **⚠ S4 조정 줄과 복사 — 기본값 적용:** 새 차수는 조정 줄을 복사하지 않고 **같은 줄을 새 현재 차수로 옮긴다**(04-13의 `line_kind` 모델 — 조정 줄은 늘 현재 차수에 산다). 이전 차수 읽기 표에 조정 그룹이 없는 것이 이 모델의 결과다.
- **차수 섹션의 「줄 수」:** 그 차수의 견적 줄(견적 줄 + 견적 외 비용, 보관 제외)이다 — 조정 줄은 세지 않는다(새 차수 모달의 `{k}줄`과 같은 기준).
- **UI-SPEC에 없는 문구 둘(계획 단계 판단 — 04-04 U-6 선례대로 Copywriting에 더한다):** ⑴ 「고객 승인 표시」 모달 — 제목 `고객 승인 표시` · 본문 `{차수} · 고객이 승인한 날` + 날짜 칸(기본값 오늘 KST) · 1차 `고객 승인 표시` · 2차 `취소 Esc`. D-56 「승인일과 함께 켠다」를 만족하려면 날짜를 받을 자리가 필요한데 S3는 버튼만 적었다. ⑵ 빈 차수 거부 이유 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기`.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 담당 PM이 「복사해 새 차수」로 2차를 만든다(복사·계보·조정 이동 → 게이트 → 액션 → 모달 → E2E)</name>
  <files>domain/quotes/revisions.ts, domain/quotes/edit-scope.ts, domain/rules/register.ts, repositories/quote-revisions.ts, repositories/quote-lines.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/revision-dialogs.tsx, test/integration/quote-revisions.test.ts, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `db/schema/quote-revisions.ts`·`db/schema/quote-lines.ts`(전문) — `(project_id, seq)` 유일 제약 · 계보 컬럼 · `line_kind`
    - `repositories/quote-revisions.ts`(전문 71행) · `repositories/quote-lines.ts`(전문)
    - `domain/quotes/lines.ts` 160–175행(`getCurrentQuoteRevision` — 최신 순번)
    - `domain/quotes/edit-scope.ts`(04-12·04-13 산출물) — `structuralEditability`
    - `domain/projects/auto-transition.ts`(04-11) — 쓰기 전 선판정
    - `domain/action-log/record.ts` 10–35행(핵심 행동 종류 — 차수 생성에 맞는 기존 종류를 고른다)
    - `app/(app)/projects/[id]/status-change.tsx`(04-06·04-11) — 네이티브 모달 요소 모달 형태 선례
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 371행(Destructive — 새 차수) · 1192–1194행
  </read_first>
  <behavior>
    - 견적 줄 여섯 + 조정 줄 둘인 1차 → 새 차수: 2차에 견적 줄 여섯(각각 계보 = 1차 대응 줄 id), 조정 줄 둘은 id 그대로 2차 소속, 1차에는 견적 줄 여섯만 남는다
    - 2차의 승인 표시·승인자는 비어 있고 현재 차수는 2차다
    - 견적 줄 0 + 조정 줄 1 → 거부(빈 차수)
    - 정산·완료 → 거부 · 담당 PM이 아닌 `projects` 쓰기 권한자 → 허용(D-53은 주체를 정하지 않았다 — 견적 줄 편집 권리와 같다)
    - 두 요청이 동시에 새 차수를 만들면 하나만 성공하고 다른 쪽은 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`
  </behavior>
  <action>
① **리포지토리.** `repositories/quote-revisions.ts`에 `insertRevision(viewer, { projectId, seq }, tx)`(유일 제약 위반은 호출자가 잡는다). `repositories/quote-lines.ts`에 `copyQuoteLines(viewer, { fromRevisionId, toRevisionId, withLineage: boolean }, tx)` — 보관되지 않은 `quote`·`out_of_quote` 줄을 정렬 순서 그대로 새 차수 id로 넣고, `withLineage`면 `copied_from_line_id`에 원본 id(04-15의 프로젝트 복사는 `false`) · `moveAdjustmentLines(viewer, { fromRevisionId, toRevisionId }, tx)` — 보관되지 않은 조정 줄의 `revision_id`를 바꾸고 `version`을 올린다.

② **판정.** `edit-scope.ts`의 `structuralEditability`에 `canCreateRevision`(수주중·진행·미수주 + `projects` 쓰기)을 더하고, `domain/rules/register.ts`에 `quote.revision-create`(ctx `{ canCreateRevision, copyableLineCount, status }` — 정산 `정산 · 실행가만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김` · 0줄 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기`)을 등록한다.

③ **domain.** `domain/quotes/revisions.ts`: `createRevisionFromCurrent(viewer, projectId, deps?)` — `applyAutoSettlement` 선판정 → 행 범위 안 프로젝트 → 현재 차수(최신 순번) → 복사 대상 줄 수 → 게이트 → `withTransaction`으로 insert(순번+1) · 복사(계보) · 조정 이동 → 유일 제약 위반이면 동시 생성 거부 문구 → 행동 로그(기존 핵심 종류 중 문서 생성에 해당하는 것, detail에 새 순번·복사 줄 수·이동 줄 수). `quoteDisplayNumber(projectNumber, seq)` → `${projectNumber}-${seq}차`(저장 없음).

④ **액션·화면.** `createRevisionAction`(zod: `projectId`)과 등록. `revision-dialogs.tsx`의 `NewRevisionDialog`: 머리 줄 2차 「복사해 새 차수」(page가 `canCreateRevision`일 때만 렌더) → 모달(Copywriting 그대로, `{k}`는 조정 줄 제외 · 조정 줄이 있으면 결과 줄 하나 더 `--fs-sm --muted`) → 서버 거부는 1차 옆 막힘 자리(게이트 문자열 그대로, 모달 유지, 토스트 없음) → 성공은 토스트 `새 차수 · 상세 견적 {n+1}차` + 새로 고침으로 새 현재 차수 표. `quote-table.tsx`는 머리 줄 버튼 자리에 이 컴포넌트를 받는 prop 하나만 더한다. 새 차수로 바뀌면 미저장 보관 키(프로젝트+차수)가 달라져 이전 차수 편집이 섞이지 않는다(04-04 설계 — 확인만).

⑤ **테스트.** `test/integration/quote-revisions.test.ts`에 `behavior` 다섯 줄(DB 재조회로 줄 수·계보·조정 줄 소속·1차 잔존을 단언, 동시 생성은 `Promise.all`). `test/e2e/quote-revisions.spec.ts` 첫 케이스: 담당 PM이 줄 둘인 수주중 프로젝트에서 「복사해 새 차수」 → 모달 본문 `상세 견적 1차의 2줄을 복사해 2차를 만듭니다 · 차수는 되돌릴 수 없습니다` → 1차 → 토스트 · 부제 `상세 견적 2차` · 표에 줄 둘.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/quote-line-kinds.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 복사·계보·조정 이동·빈 차수·동시성 중 하나가 틀렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 차수 생성이 한 트랜잭션(insert·복사·이동)이고, 거부·동시 충돌 뒤 DB에 반쪽 차수가 남지 않는다(통합 테스트 DB 재조회)
    - 복사된 줄 전부에 계보가 있고 조정 줄은 복사본 없이 id가 그대로다
    - 현재 차수 판정이 최신 순번 하나이고 승인 여부를 보지 않는다
    - 견적 표시 번호에 저장 컬럼·카운터가 없다
    - 정산·완료에서 「복사해 새 차수」가 DOM에 없다(E2E 또는 페이지 조건 단언)
    - `createRevisionAction`이 등록됐다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM이 새 차수를 만들면 견적 줄이 계보와 함께 복사되고 조정 줄은 새 현재 차수로 옮겨 가며, 정산·완료·빈 차수·동시 생성이 서버에서 막힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 고객 승인 표시와 취소 + 고객 승인 게이트(D-43) + 설정 키 + 부제</name>
  <files>domain/quotes/revisions.ts, domain/quotes/lines.ts, domain/rules/register.ts, domain/settings/keys.ts, repositories/quote-revisions.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/revision-dialogs.tsx, test/unit/domain/rules-gate.test.ts, test/integration/quote-revisions.test.ts, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-43·D-54·D-56 원문
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1091–1094행(고객 승인 버튼 · 상태별) · 360행(막힘 — 지출결의(고객 승인)) · 370행(Destructive — 고객 승인 표시 취소)
    - `domain/settings/keys.ts`(단순값 boolean 키 선례 — `project.force_complete.*`) · 레지스트리 커버리지 테스트 규약
    - `domain/quotes/lines.ts`의 `linkedDocumentsByLine`(04-12 — 연결 문서 조회 한 지점)
    - `lib/kst-date.ts`(04-11) — 승인일 기본값 오늘(KST)
    - `test/unit/domain/rules-gate.test.ts`(04-06·04-12 갱신본)
  </read_first>
  <behavior>
    - `quote.customer-approval`: 수주중 + 미승인 → 통과 · 진행 + 미승인 + 설정 켬 → `{n}차 고객 승인 전 · 고객 승인 표시`(담당 PM) / `{n}차 고객 승인 전 · 담당 PM {이름}`(그 밖) · 진행 + 승인 → 통과 · 설정 끔 → 통과 · 정산·미수주 + 미승인 → 거부(수주중만 예외 — D-43)
    - `quote.approval-toggle`: 담당 PM + 진행 + 켜기 → 통과 · 담당이 아닌 사람 → `고객 승인 표시는 담당 PM이 합니다` · 완료 → `완료 · 견적 줄이 잠김` · 끄기 + 연결 문서 있음 → `연결 문서가 있어 승인 표시를 취소할 수 없습니다 · 새 차수로 고쳐 주세요` · 현재 차수가 아닌 차수 → `현재 차수만 승인 표시할 수 있습니다`
    - `quote.vendor-required`: 거래처 없음 → `거래처 없음 · 거래처 고르기` · 있음 → 통과
  </behavior>
  <action>
① `domain/settings/keys.ts`에 `PROJECT_CUSTOMER_APPROVAL_GATE`(`project.customer_approval_gate`, 단순값 boolean, 기본 true, namespace `프로젝트`, 라벨 `고객 승인 게이트`, 힌트 한 문장 `끄면 고객 승인 전 차수에서도 지출결의를 올릴 수 있음`)를 더한다.

② `domain/rules/register.ts`에 `quote.customer-approval`(ctx `{ status, revisionSeq, revisionApproved, gateEnabled, actorIsAssignedPm, pmName }` — **수주중 예외를 이 규칙 안에 둔다**: 독립 규칙으로 빼지 않는다, 쓰는 규칙이 이것 하나뿐이다) · `quote.approval-toggle` · `quote.vendor-required`(ctx `{ vendorId }`, 이 페이즈 호출자 없음 — 주석 한 줄 「Phase 5 지출결의·구매 요청이 부른다」)를 등록한다. 결정표 단위 테스트(`behavior`)를 먼저 쓴다(RED) — 설정은 ctx로 넣어 DB 없이 돈다.

③ `domain/quotes/revisions.ts`에 `setCustomerApproval(viewer, revisionId, input: { approvedOn: string } | null, deps?)` — 선판정 → 프로젝트·차수 → 현재 차수 여부 → (끄기면) `linkedDocumentsByLine`로 연결 문서 유무 → `quote.approval-toggle` → `repositories/quote-revisions.ts`의 `setRevisionApproval`(켜기: `customer_approved_at` = 승인일의 KST 00:00, `customer_approved_by` = viewer / 끄기: 둘 다 null) → 행동 로그(detail에 차수·승인일). 결재를 거치지 않는다. 승인일은 `YYYY-MM-DD`이고 미래 날짜는 거부한다(`승인일은 오늘까지 적을 수 있습니다`).

④ **액션·화면.** `setCustomerApprovalAction`(zod: `revisionId` · `approvedOn` 날짜 문자열 또는 null) 등록. `revision-dialogs.tsx`에 `ApprovalDialog`(제목 `고객 승인 표시` · 본문 `{n}차 · 고객이 승인한 날` + `Form.Field` 날짜 칸 기본 오늘 · 1차 `고객 승인 표시` · 2차 `취소 Esc`)와 `ApprovalCancelDialog`(Copywriting 그대로, 2차 `닫기 Esc`). `[id]/page.tsx`: 담당 PM이고 상태가 완료가 아니면 부제 옆 3차 — 미승인이면 「고객 승인 표시」, 승인됐고 연결 문서가 없으면 「승인 표시 취소」(있으면 버튼 없음). 부제에 `고객 승인 {날짜} {승인자 이름}`(승인일은 완료에서도 보인다).

⑤ **테스트.** 통합: 담당 PM 켜기 → 컬럼·로그 · 담당이 아닌 팀장 요청 거부 · 완료에서 켜기·끄기 거부 · 이전 차수 켜기 거부 · 끄기 + 연결 문서(`deps`로 조회 결과를 한 건 주입) 거부 · 미래 승인일 거부. E2E: 담당 PM이 오늘 날짜로 승인 표시 → 부제에 `고객 승인 {오늘}` → 「승인 표시 취소」 모달의 2차 라벨 `닫기 Esc` → 취소 → 부제에서 사라진다 · 완료 프로젝트에는 두 버튼이 없고 승인일이 보인다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 고객 승인·승인 토글·거래처 필수 결정표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/settings.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 게이트 규칙 셋(`quote.customer-approval`·`quote.approval-toggle`·`quote.vendor-required`)이 등록되고 결정표가 `behavior`를 덮는다
    - 수주중 예외가 `quote.customer-approval` 안에 있고 그 선택이 SUMMARY에 한 줄로 적혔다
    - `quote.vendor-required`를 이 페이즈의 어떤 함수도 부르지 않는다(단위 테스트만)
    - `PROJECT_CUSTOMER_APPROVAL_GATE` 키가 있고 설정 화면에 나온다
    - 승인 표시가 결재 경로 없이 저장되고 행동 로그가 한 줄 남는다
    - 완료에서 두 버튼이 DOM에 없다(E2E)
    - 승인 표시 취소 모달의 2차 라벨이 `닫기 Esc`다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM이 현재 차수의 고객 승인을 날짜와 함께 켜고 조건대로 끄며, 고객 승인·거래처 필수 게이트가 Phase 5를 위해 등록·고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 차수 섹션(S5) + 이전 차수 읽기 표 + 계보 해석(D-55) + Copywriting 두 줄 + 화면 검증</name>
  <files>domain/quotes/revisions.ts, domain/quotes/lines.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/revision-section.tsx, app/(app)/projects/[id]/project-detail.module.css, test/unit/domain/quote-revisions.test.ts, test/integration/leak-scan.test.ts, test/e2e/quote-revisions.spec.ts, .planning/phases/04-project-quote-ledger/04-UI-SPEC.md</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1154–1194행(S5 전문 — 이력 목록을 쓰지 않는 네 근거 · 열 · P1/P2 · 상태 낱말 · 차수 열기)
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` U-2 행 — 사용자 확정 값(최신+승인 = `승인`만, 미승인 이전 = 빈 칸)
    - `ui/table/Table.tsx`·`ui/table/types.ts` — 읽기 형태 자동 판정(편집 셀 0) · `summary`·열 우선순위
    - `ui/history-list/HistoryList.tsx` — **쓰지 않는다는 판단을 확인만** 한다
    - `app/(app)/projects/[id]/page.tsx`(현재) — 섹션 순서 ① 견적 줄 ② 매출 ③ 차수
    - `domain/quotes/lines.ts`의 `linkedDocumentsByLine`(04-12)
  </read_first>
  <behavior>
    - `quoteDisplayNumber("26001", 2)` → `26001-2차`
    - `resolveLinkedDocumentsByLineage`: 1차 줄 A에 문서 X, 2차 줄 B(계보 A), 3차 줄 C(계보 B) → 현재 차수 C에 X가 이어진다 · 계보가 끊긴(새 차수에서 지운) 줄의 문서는 어느 현재 줄에도 붙지 않고 「차수에서 빠진 줄의 문서」 목록으로 따로 돌아온다(D-55 「견적 외 비용처럼 프로젝트에 남는다」)
    - `listRevisionSummaries`: 차수 셋(1차 승인 · 2차 미승인 · 3차 미승인 최신) → 상태 `[3차: 현재, 2차: 빈 칸, 1차: 승인]` · 최신이 승인이면 `승인`만
  </behavior>
  <action>
① `domain/quotes/revisions.ts`에 순수 함수 `resolveLinkedDocumentsByLineage(linesAcrossRevisions, docsByLineId)`(계보 사슬을 따라 현재 차수 줄에 문서를 잇고, 끊긴 계보의 문서는 별도 목록)와 `listRevisionSummaries(viewer, projectId)`(순번 · 생성일(KST) · 줄 수(견적 줄+견적 외 비용, 보관 제외) · 견적 합계 · 상태 낱말 — U-2 확정값)를 더한다. `domain/quotes/lines.ts`의 `linkedDocumentsByLine`이 문서 출처(이 페이즈 빈 결과)에서 받은 것을 이 해석 함수로 현재 차수에 잇게 한다 — 조회 지점은 여전히 하나다. 단위 테스트(`behavior`)를 먼저 쓴다(RED).

② `app/(app)/projects/[id]/revision-section.tsx`: `ui/table`을 쓴다 — 편집 셀이 없으니 자동으로 캡션 있는 읽기 표(모드 prop을 만들지 않는다). 섹션 제목 `차수`(2px `--line-strong`으로 시작하는 §6-2 섹션), 열 여섯(차수 `{n}차` · 생성일 · 줄 수 · 견적 합계 · 상태(§7-5 `text`: `현재` accent · `승인` success) · 동작), 최신이 위, 폰 P1 = 차수·견적 합계·동작, P2 접힌 줄 = 상태·생성일·줄 수(빈 값 자리 건너뜀). 이전 차수 행에만 3차 「차수 열기」(`?revision={n}` 링크 — 이동이라 `<a>`). 현재 행의 동작 칸과 미승인 이전 차수의 상태 칸은 **빈 칸**이다(`—` 금지). 섹션 단독 오류 자리를 만들지 않는다. `ui/history-list`를 쓰지도 고치지도 않는다.

③ **이전 차수 읽기 표.** `[id]/page.tsx`가 `revision` 검색 파라미터(현재보다 작은 순번)를 받으면 그 차수의 견적 줄을 `QuoteLedger`에 **잠김 모드로** 넘긴다 — 모든 셀 `locked`(서버가 `cellEditability`를 전부 잠김으로 계산), 조정 그룹 없음(조정 줄은 현재 차수에 산다), 1차 저장·구조 컨트롤·미저장 복원 없음, 표 위에 `{n}차 · 읽기 전용` 한 줄과 현재 차수로 돌아가는 3차 링크, 0줄이면 `이 차수에 견적 줄이 없습니다`(버튼 없음). 되돌리기 동작을 만들지 않는다.

④ **Copywriting 두 줄.** `04-UI-SPEC.md` Copywriting Contract 표에 이 플랜이 정한 두 행(「확인 — 고객 승인 표시」 · 「막힘 — 새 차수(빈 차수)」)을 더하고 행 끝에 `(04-14 계획 단계 판단)`을 붙인다 — 04-04 U-6 선례. 다른 행은 건드리지 않는다.

⑤ E2E: 차수 셋을 만든 프로젝트에서 섹션 행 순서·상태 낱말(U-2)·현재 행 빈 동작 칸 · 「차수 열기」로 1차가 읽기 표(캡션 · 흰 머리글 · 저장 버튼 없음)로 열린다 · 375에서 섹션 가로 스크롤 0. `leak-scan.test.ts`에 새 DTO·액션이 덮이는지 확인한다.

⑥ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S5 backstop 둘(섹션 오류 자리 없음 · 375 빈 칸 유지)을 DOM 실측 → 수정 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-revisions.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 계보 해석이나 상태 낱말이 어긋난다</fails_when>
    <automated>git diff --stat d6b41cf -- ui/history-list</automated>
    <fails_when>출력이 비어 있지 않다 — 이력 목록 컴포넌트를 고쳤다(S5의 네 근거)</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S5 판정 두 개를 SUMMARY에서 확인</test>
      <expected>차수 섹션에 단독 오류 자리가 없고, 375에서 가로 스크롤 0 · 현재 행 동작 칸이 빈 칸</expected>
      <why_human>렌더 배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `resolveLinkedDocumentsByLineage`가 순수 함수이고 계보 사슬·끊긴 계보 두 경우가 단위 테스트에 있다
    - `linkedDocumentsByLine`이 여전히 유일한 조회 지점이고 이 해석 함수를 거친다
    - 차수 섹션이 모드 prop 없이 캡션 읽기 표이고 `ui/history-list` diff가 0줄이다
    - 상태 칸·현재 행 동작 칸이 U-2 확정값대로 렌더된다(빈 칸 — `—` 없음)
    - 이전 차수 보기에 저장·구조 컨트롤·조정 그룹이 없다(E2E)
    - UI-SPEC Copywriting에 두 행이 더해지고 다른 행 diff가 0줄이다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>차수 섹션이 차수의 변천을 읽기 표로 보이고, 지난 차수를 잠긴 표로 열어 보며, 연결 문서가 계보를 따라 현재 줄에 이어질 자리가 Phase 5를 위해 준비됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 새 차수·승인 액션 | 프로젝트·차수 id와 승인일이 넘어온다. 담당 PM 판정·상태·연결 문서 검사가 서버에만 있어야 한다 |
| 게이트 → Phase 5 | 고객 승인·거래처 필수 규칙이 지출 동작의 단일 판정 지점이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-68 | Elevation of Privilege | 고객 승인 표시 | high | mitigate | 서버가 `viewer.id = projects.pm_user_id`로 담당 PM을 판정하고(버튼을 숨기는 것과 별개), 완료·이전 차수·미래 날짜를 거부한다. 담당이 아닌 팀장 요청 거부를 통합 테스트로 고정한다 |
| T-04-69 | Tampering | 승인 표시 취소 | high | mitigate | 연결 문서 유무를 서버의 조회 지점이 판정한다 — 클라이언트의 「문서 없음」 주장을 받지 않는다 |
| T-04-70 | Tampering | 새 차수 | medium | mitigate | 한 트랜잭션(insert·복사·이동)이라 반쪽 차수가 남지 않고, `(project_id, seq)` 유일 제약이 동시 생성을 막는다. 정산·완료·빈 차수는 게이트가 거부한다 |
| T-04-71 | Elevation of Privilege | 승인 게이트 우회 | high | mitigate | 게이트가 최신 순번 차수 하나만 본다(D-54) — 이전 승인 차수로 대신하지 않는다(금지 항목). 설정으로 끄는 것만 허용된 우회다(ROADMAP 기준 3) |
| T-04-72 | Repudiation | 승인 표시 | low | mitigate | 켜기·끄기가 행동 로그에 차수·승인일과 함께 남고, 승인자는 `customer_approved_by`에 저장된다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `register.ts`에 `quote.revision-create`·`quote.customer-approval`·`quote.approval-toggle`·`quote.vendor-required` 등록
- `ui/history-list` diff 0줄, `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 새 차수가 전체 복사로 만들어지고 조정 줄은 옮겨 가며, 최신 차수만 현재다
- 담당 PM이 현재 차수의 고객 승인을 날짜와 함께 켜고, 조건대로만 끈다
- Phase 5는 지출결의에서 `quote.customer-approval`·`quote.vendor-required`를 부르고 연결 문서 출처를 채우기만 하면 된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/quotes/revisions.ts` — `createRevisionFromCurrent` · `setCustomerApproval` · `listRevisionSummaries` · `quoteDisplayNumber` · `resolveLinkedDocumentsByLineage` |
| 함수(repository) | `insertRevision` · `setRevisionApproval`(`quote-revisions.ts`) · `copyQuoteLines` · `moveAdjustmentLines`(`quote-lines.ts`) |
| 게이트 규칙(신규) | `quote.revision-create` · `quote.customer-approval`(수주중 예외 포함) · `quote.approval-toggle` · `quote.vendor-required`(등록만) |
| 설정 키(신규) | `project.customer_approval_gate`(`PROJECT_CUSTOMER_APPROVAL_GATE`, 단순값, 기본 켬) |
| Server Action | `createRevisionAction` · `setCustomerApprovalAction`(등록 포함) |
| 컴포넌트(신규) | `app/(app)/projects/[id]/revision-section.tsx` · `app/(app)/projects/[id]/revision-dialogs.tsx`(`NewRevisionDialog` · `ApprovalDialog` · `ApprovalCancelDialog`) |
| 변경된 기존 심볼 | `structuralEditability.canCreateRevision` · `linkedDocumentsByLine`(계보 해석 경유) · `QuoteLedger`(차수 버튼·승인 버튼 자리 · 잠김 보기 모드) · 상세 부제(고객 승인일) |
| 문서 | `04-UI-SPEC.md` Copywriting 두 행(확인 — 고객 승인 표시 · 막힘 — 새 차수) |
| 테스트 | `test/unit/domain/quote-revisions.test.ts` · `test/integration/quote-revisions.test.ts` · `test/e2e/quote-revisions.spec.ts`(신규) · `rules-gate.test.ts` 확장 |
| 새 스키마·마이그레이션 | 없음 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md` when done
</output>
