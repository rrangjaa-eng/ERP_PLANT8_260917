---
phase: 04-project-quote-ledger
plan: 14
type: execute
wave: 15
depends_on: ["04-13", "04-23"]
files_modified:
  - domain/quotes/revisions.ts
  - domain/quotes/edit-scope.ts
  - domain/quotes/lines.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - repositories/quote-revisions.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - test/unit/domain/quote-revisions.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/quote-revisions.test.ts
  - test/integration/leak-scan.test.ts
autonomous: true
requirements: [PROJ-07, PROJ-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "새 차수는 현재 차수의 견적 줄(견적 줄·견적 외 비용, 보관 제외) 전체를 한 트랜잭션에서 복사해 만들고, 복사된 줄마다 원본 줄 id를 계보(`copied_from_line_id`)로 갖는다 — 새 차수는 승인 표시가 꺼져 있다(D-53)"
    - "복사할 견적 줄이 0개면 새 차수를 만들 수 없다 — 빈 차수가 없다(D-53)"
    - "조정 줄은 새 차수로 복사되지 않고 같은 줄(값·작성자·id 그대로)이 새 현재 차수로 옮겨 간다 — 목록의 실행가·수익금은 차수가 바뀌어도 조정 금액만큼 그대로다(⚠ S4 「조정 줄과 복사」 기본값, D-87 — 이전 차수 읽기 표에 조정 그룹이 없는 화면 쪽은 04-24)"
    - "새 차수는 수주중·진행·미수주에서만 서버가 만든다 — 정산·완료는 게이트가 거부한다(S3, ⚠ S4 정산 구조 잠금 · D-47 — 버튼 렌더는 04-24)"
    - "최신 차수만 현재 차수다 — 승인 여부와 무관하게 순번이 가장 큰 차수이고, 차수 되돌리기 동작이 어디에도 없다(D-53·D-54)"
    - "두 요청이 동시에 새 차수를 만들면 하나만 성공하고 다른 쪽은 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`를 받는다 — 반쪽 차수가 남지 않는다"
    - "차수의 고객 승인 표시는 담당 PM이 승인일과 함께 켜고 결재를 거치지 않으며 행동 로그에 남는다 — 담당이 아닌 사람의 요청은 서버가 거부한다(D-56)"
    - "완료에서는 승인 표시 변경을 서버가 거부하고, 승인일은 저장된 채로 남는다(⚠ S3 「상태별 고객 승인」 기본값 — 버튼 렌더는 04-24)"
    - "승인 표시 취소는 그 차수에 연결 문서가 없을 때만 되고, 있으면 서버가 `연결 문서가 있어 승인 표시를 취소할 수 없습니다 · 새 차수로 고쳐 주세요`로 거부한다(D-56)"
    - "견적 표시 번호 `26001-2차`는 저장 컬럼·카운터 없이 프로젝트 번호와 순번에서 만든다(D-56)"
    - "고객 승인 게이트 규칙 `quote.customer-approval`이 등록되어 있다 — 수주중이면 승인과 무관하게 통과, 진행 이후이고 현재 차수가 미승인이면 거부(담당 PM에게 `{n}차 고객 승인 전 · 고객 승인 표시`, 그 밖 `{n}차 고객 승인 전 · 담당 PM {이름}`), 설정 `project.customer_approval_gate`를 끄면 통과한다 — 이 페이즈에는 호출자가 없고 Phase 5 지출결의가 부른다(D-43, ROADMAP 기준 3, PROJ-07)"
    - "거래처 필수 규칙 `quote.vendor-required`가 등록되어 있다(`거래처 없음 · 거래처 고르기`) — 호출은 Phase 5다(D-64)"
    - "이전 차수 줄의 연결 문서가 계보를 따라 현재 차수의 대응 줄에 이어 보이도록 해석하는 함수가 연결 문서 조회 지점 안에 있다 — 문서 행을 옮기지 않는다(D-55; 문서 출처는 Phase 5)"
    - "차수 요약이 순번 · 생성일 · 줄 수(견적 줄 + 견적 외 비용, 보관 제외) · 견적 합계 · 상태 낱말(최신 미승인 `현재` · 승인 `승인` · 승인 없이 지나간 이전 차수 빈 값)을 서버에서 돌려준다(S5, U-2 사용자 확정 — 표 렌더는 04-24)"
  prohibitions:
    - statement: "승인 전 차수에서 돈이 나가는 길을 「승인된 이전 차수」로 우회하게 해서는 안 된다. 현재 차수는 최신 순번 하나뿐이고(D-54), 게이트는 이전 승인 차수를 대신 보지 않는다 — 승인을 덜 받으려는 습관을 막는 것이 이 규칙의 목적이다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/revisions.ts"
      provides: "새 차수(전체 복사 + 조정 줄 이동) · 고객 승인 표시와 취소 · 차수 요약 · 견적 표시 번호 · 계보 해석"
      exports: ["createRevisionFromCurrent", "setCustomerApproval", "listRevisionSummaries", "quoteDisplayNumber", "resolveLinkedDocumentsByLineage"]
    - path: "test/integration/quote-revisions.test.ts"
      provides: "전체 복사·계보·조정 이동·빈 차수 거부·승인 권한·완료 거부·취소 조건·동시 생성 증명"
      min_lines: 60
    - path: "test/unit/domain/quote-revisions.test.ts"
      provides: "표시 번호 · 계보 해석(사슬·끊긴 계보) · 차수 상태 낱말 표"
      min_lines: 30
  key_links:
    - from: "domain/quotes/revisions.ts"
      to: "repositories/quote-lines.ts"
      via: "새 차수가 한 트랜잭션에서 견적 줄을 계보와 함께 복사하고 조정 줄을 옮긴다"
      pattern: "copied_from_line_id"
    - from: "domain/quotes/revisions.ts"
      to: "domain/rules/gate.ts"
      via: "새 차수·승인 변경이 게이트(`quote.revision-create`·`quote.approval-toggle`)를 지난다"
      pattern: "gate("
    - from: "domain/rules/register.ts"
      to: "domain/settings/keys.ts"
      via: "고객 승인 게이트가 설정 키로 꺼진다"
      pattern: "project.customer_approval_gate"
    - from: "domain/quotes/lines.ts"
      to: "domain/quotes/revisions.ts"
      via: "연결 문서 조회 지점이 계보 해석 함수를 거쳐 현재 차수 줄에 문서를 잇는다 — 조회 지점은 여전히 하나"
      pattern: "resolveLinkedDocumentsByLineage"
---

<objective>
**As a** 기획 PM, **I want to** 네고로 바뀐 견적을 이전 차수를 통째로 복사한 새 차수로 고치고, 고객이 승인한 차수에 승인일을 표시하며, 그 판정을 서버가 한 규칙으로 하기를, **so that** 견적의 변천이 차수로 남고 「현재 차수가 승인됐는가」 하나로 지출(Phase 5)과 계약 금액(04-16)이 정해진다.

이 플랜은 옛 04-06 Task 2·3에서 **차수 기계와 고객 승인**을 옮겨 왔다(D-53~D-56 · D-43 · D-64 거래처 필수 등록). 2026-09-23 결정이 더한 것: 정산·완료에서 새 차수가 없다(⚠ S4 기본값) · 조정 줄은 복사되지 않고 새 현재 차수로 옮겨 간다(⚠ S4 기본값) · 완료에서는 승인 표시 변경이 없다(⚠ S3 기본값). **2026-09-23 체커 1차 분할:** 옛 04-14(20파일 — 세 태스크가 다 10파일 이상)를 서버와 화면으로 나눴다. 이 플랜은 **서버 쪽**(차수 domain · 리포지토리 · 게이트 규칙 넷 · 설정 키 · 액션 둘 · 계보 해석 · 차수 요약)이고, 모달 셋 · 머리 줄 버튼·부제 · 차수 섹션 · 이전 차수 읽기 표 · Copywriting 두 줄은 **04-24**가 이 액션과 함수 위에 그린다.

Purpose: ROADMAP 기준 3 — 차수마다 고객 승인 표시가 있고 승인 전 차수의 줄에서는 지출 동작이 게이트로 막힌다(설정으로 끌 수 있음). 이 페이즈에는 지출결의가 없으므로 게이트 규칙을 등록하고 단위 테스트로 판정을 고정하며, Phase 5가 호출자만 붙인다.

Output: 차수 domain · 게이트 규칙 넷 · 설정 키 하나 · 액션 둘 · 이전 차수 잠김 조회 · 단위 2 · 통합 1.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-13-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1154–1194행(S5 적용 규칙 — 상태 낱말 · 복사해 새 차수) · 439행·457–461행(⚠ S3 · ⚠ S4 조정 줄과 복사) · 360행·370–371행(Copywriting 막힘 — 지출결의(고객 승인) · 새 차수)
# .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md U-2 행(사용자 확정 값)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(차수·고객 승인 — 서버)** 과 **PROJ-05(견적 줄 복사 — 차수 전체 복사 쪽)** 다. 수용 기준은 D-43·D-53~D-56·D-64·ROADMAP 기준 3과 UI-SPEC S3·S5에서 끌어왔다. S5 `## UI Considerations`의 error backstop과 overflow, S3·S5의 렌더 truths는 화면을 그리는 04-24로 옮겼다 — 이 플랜은 그 화면이 쓰는 서버 계약(게이트 문자열 · 요약 · 표시 번호 · 잠김 조회)을 truths로 덮는다.

**계획 단계 가정(사용자에게 표면화):**
- **⚠ S3 상태별 고객 승인 — 기본값 적용:** 수주중·진행·미수주·정산 = 승인 변경 가능, 완료 = 서버 거부(버튼도 없음 — 04-24).
- **⚠ S4 조정 줄과 복사 — 기본값 적용:** 새 차수는 조정 줄을 복사하지 않고 **같은 줄을 새 현재 차수로 옮긴다**(04-13의 `line_kind` 모델 — 조정 줄은 늘 현재 차수에 산다). 이전 차수 읽기 표에 조정 그룹이 없는 것이 이 모델의 결과다.
- **차수 요약의 「줄 수」:** 그 차수의 견적 줄(견적 줄 + 견적 외 비용, 보관 제외)이다 — 조정 줄은 세지 않는다(새 차수 모달의 `{k}줄`과 같은 기준).
- **UI-SPEC에 없는 문구 둘(계획 단계 판단 — 04-04 U-6 선례대로 04-24가 Copywriting에 더한다):** ⑴ 「고객 승인 표시」 모달 — 제목 `고객 승인 표시` · 본문 `{차수} · 고객이 승인한 날` + 날짜 칸(기본값 오늘 KST) · 1차 `고객 승인 표시` · 2차 `취소 Esc`. D-56 「승인일과 함께 켠다」를 만족하려면 날짜를 받을 자리가 필요한데 S3는 버튼만 적었다. ⑵ 빈 차수 거부 이유 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기` — 이 문자열은 이 플랜의 게이트가 돌려준다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 담당 PM이 새 차수를 만든다(복사·계보·조정 이동 리포지토리 → 구조 판정 → 게이트 → domain → 액션 → 통합)</name>
  <files>domain/quotes/revisions.ts, domain/quotes/edit-scope.ts, domain/rules/register.ts, repositories/quote-revisions.ts, repositories/quote-lines.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/integration/quote-revisions.test.ts, test/integration/leak-scan.test.ts</files>
  <read_first>
    - `db/schema/quote-revisions.ts`·`db/schema/quote-lines.ts`(전문) — `(project_id, seq)` 유일 제약 · 계보 컬럼 · `line_kind`
    - `repositories/quote-revisions.ts`(전문 71행) · `repositories/quote-lines.ts`(전문)
    - `domain/quotes/lines.ts` 160–175행(`getCurrentQuoteRevision` — 최신 순번)
    - `domain/quotes/edit-scope.ts`(04-12·04-13·04-23 산출물) — `structuralEditability`
    - `domain/projects/auto-transition.ts`(04-11) — 쓰기 전 선판정
    - `domain/action-log/record.ts` 10–35행(핵심 행동 종류 — 차수 생성에 맞는 기존 종류를 고른다)
    - `app/(app)/projects/actions.ts`·`actions.registry.ts` · `test/integration/leak-scan.test.ts`(side-effect import 목록)
  </read_first>
  <behavior>
    - 견적 줄 여섯 + 조정 줄 둘인 1차 → 새 차수: 2차에 견적 줄 여섯(각각 계보 = 1차 대응 줄 id), 조정 줄 둘은 id 그대로 2차 소속, 1차에는 견적 줄 여섯만 남는다
    - 2차의 승인 표시·승인자는 비어 있고 현재 차수는 2차다
    - 견적 줄 0 + 조정 줄 1 → 거부(빈 차수)
    - 정산·완료 → 거부 · 담당 PM이 아닌 `projects` 쓰기 권한자 → 허용(D-53은 주체를 정하지 않았다 — 견적 줄 편집 권리와 같다)
    - 두 요청이 동시에 새 차수를 만들면 하나만 성공하고 다른 쪽은 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`
  </behavior>
  <action>
① **리포지토리.** `repositories/quote-revisions.ts`에 `insertRevision(viewer, { projectId, seq }, tx)`(유일 제약 위반은 호출자가 잡는다). `repositories/quote-lines.ts`에 `copyQuoteLines(viewer, { fromRevisionId, toRevisionId, withLineage: boolean }, tx)` — 보관되지 않은 `quote`·`out_of_quote` 줄을 정렬 순서 그대로 새 차수 id로 넣고, `withLineage`면 `copied_from_line_id`에 원본 id(04-15의 프로젝트 복사는 `false`) · `moveAdjustmentLines(viewer, { fromRevisionId, toRevisionId }, tx)` — 보관되지 않은 조정 줄의 `revision_id`를 바꾸고 `version`을 올린다.

② **판정.** `edit-scope.ts`의 `structuralEditability`에 `canCreateRevision`(수주중·진행·미수주 + `projects` 쓰기)을 더하고, `domain/rules/register.ts`에 `quote.revision-create`(ctx `{ canCreateRevision, copyableLineCount, status }` — 정산 `정산 · 실행가만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김` · 0줄 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기`)을 등록한다.

③ **domain.** `domain/quotes/revisions.ts`: `createRevisionFromCurrent(viewer, projectId, deps?)` — `applyAutoSettlement` 선판정 → 행 범위 안 프로젝트 → 현재 차수(최신 순번) → 복사 대상 줄 수 → 게이트 → `withTransaction`으로 insert(순번+1) · 복사(계보) · 조정 이동 → 유일 제약 위반이면 동시 생성 거부 문구 → 행동 로그(기존 핵심 종류 중 문서 생성에 해당하는 것, detail에 새 순번·복사 줄 수·이동 줄 수).

④ **액션.** `createRevisionAction`(zod: `projectId`)을 `app/(app)/projects/actions.ts`에 더하고 `actions.registry.ts`에 등록한다(D-38). `leak-scan.test.ts`의 side-effect import에 새 모듈이 빠졌으면 더한다. 모달·버튼은 04-24.

⑤ **통합(트레이서 증명).** `test/integration/quote-revisions.test.ts`에 `behavior` 다섯 줄(DB 재조회로 줄 수·계보·조정 줄 소속·1차 잔존을 단언, 동시 생성은 `Promise.all`).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/quote-line-kinds.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 복사·계보·조정 이동·빈 차수·동시성 중 하나가 틀렸거나 새 액션이 누수 스캔 커버리지에서 빠졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 차수 생성이 한 트랜잭션(insert·복사·이동)이고, 거부·동시 충돌 뒤 DB에 반쪽 차수가 남지 않는다(통합 테스트 DB 재조회)
    - 복사된 줄 전부에 계보가 있고 조정 줄은 복사본 없이 id가 그대로다
    - 현재 차수 판정이 최신 순번 하나이고 승인 여부를 보지 않는다
    - 정산·완료의 새 차수 요청이 게이트 문자열로 거부된다(통합)
    - `createRevisionAction`이 등록됐다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM의 새 차수 요청이 견적 줄을 계보와 함께 복사하고 조정 줄을 옮기며, 정산·완료·빈 차수·동시 생성이 서버에서 막힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 고객 승인 표시와 취소(서버) + 고객 승인 게이트(D-43) + 승인 토글 게이트 + 거래처 필수 등록(D-64) + 설정 키</name>
  <files>domain/quotes/revisions.ts, domain/rules/register.ts, domain/settings/keys.ts, repositories/quote-revisions.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/unit/domain/rules-gate.test.ts, test/integration/quote-revisions.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-43·D-54·D-56·D-64 원문
    - `domain/settings/keys.ts`(단순값 boolean 키 선례 — `project.force_complete.*`) · 레지스트리 커버리지 테스트 규약
    - `domain/quotes/lines.ts`의 `linkedDocumentsByLine`(04-12 — 연결 문서 조회 한 지점)
    - `lib/kst-date.ts`(04-11) — 승인일 검증의 오늘(KST)
    - `test/unit/domain/rules-gate.test.ts`(04-06·04-20·04-12 갱신본)
  </read_first>
  <behavior>
    - `quote.customer-approval`: 수주중 + 미승인 → 통과 · 진행 + 미승인 + 설정 켬 → `{n}차 고객 승인 전 · 고객 승인 표시`(담당 PM) / `{n}차 고객 승인 전 · 담당 PM {이름}`(그 밖) · 진행 + 승인 → 통과 · 설정 끔 → 통과 · 정산·미수주 + 미승인 → 거부(수주중만 예외 — D-43)
    - `quote.approval-toggle`: 담당 PM + 진행 + 켜기 → 통과 · 담당이 아닌 사람 → `고객 승인 표시는 담당 PM이 합니다` · 완료 → `완료 · 견적 줄이 잠김` · 끄기 + 연결 문서 있음 → `연결 문서가 있어 승인 표시를 취소할 수 없습니다 · 새 차수로 고쳐 주세요` · 현재 차수가 아닌 차수 → `현재 차수만 승인 표시할 수 있습니다`
    - `quote.vendor-required`: 거래처 없음 → `거래처 없음 · 거래처 고르기` · 있음 → 통과
  </behavior>
  <action>
① `domain/settings/keys.ts`에 `PROJECT_CUSTOMER_APPROVAL_GATE`(`project.customer_approval_gate`, 단순값 boolean, 기본 true, namespace `프로젝트`, 라벨 `고객 승인 게이트`, 힌트 한 문장 `끄면 고객 승인 전 차수에서도 지출결의를 올릴 수 있음`)를 더한다.

② `domain/rules/register.ts`에 `quote.customer-approval`(ctx `{ status, revisionSeq, revisionApproved, gateEnabled, actorIsAssignedPm, pmName }` — **수주중 예외를 이 규칙 안에 둔다**: 독립 규칙으로 빼지 않는다, 쓰는 규칙이 이것 하나뿐이다) · `quote.approval-toggle` · `quote.vendor-required`(ctx `{ vendorId }`, 이 페이즈 호출자 없음 — 주석 한 줄 「Phase 5 지출결의·구매 요청이 부른다」)를 등록한다. 결정표 단위 테스트(`behavior`)를 먼저 쓴다(RED) — 설정은 ctx로 넣어 DB 없이 돈다.

③ `domain/quotes/revisions.ts`에 `setCustomerApproval(viewer, revisionId, input: { approvedOn: string } | null, deps?)` — 선판정 → 프로젝트·차수 → 현재 차수 여부 → (끄기면) `linkedDocumentsByLine`로 연결 문서 유무 → `quote.approval-toggle` → `repositories/quote-revisions.ts`의 `setRevisionApproval`(켜기: `customer_approved_at` = 승인일의 KST 00:00, `customer_approved_by` = viewer / 끄기: 둘 다 null) → 행동 로그(detail에 차수·승인일). 결재를 거치지 않는다. 승인일은 `YYYY-MM-DD`이고 미래 날짜는 거부한다(`승인일은 오늘까지 적을 수 있습니다`).

④ **액션.** `setCustomerApprovalAction`(zod: `revisionId` · `approvedOn` 날짜 문자열 또는 null)을 더하고 등록한다. 모달은 04-24.

⑤ **통합.** 담당 PM 켜기 → 컬럼·로그 · 담당이 아닌 팀장 요청 거부 · 완료에서 켜기·끄기 거부(승인일은 그대로 남음) · 이전 차수 켜기 거부 · 끄기 + 연결 문서(`deps`로 조회 결과를 한 건 주입) 거부 · 미래 승인일 거부 · 게이트가 이전 승인 차수를 대신 보지 않는다(2차 미승인 + 1차 승인 → `quote.customer-approval` 거부 — 금지 항목).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 고객 승인·승인 토글·거래처 필수 결정표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/settings.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 승인 권한·완료·연결 문서·미래 날짜 거부 중 하나가 틀렸거나 설정 키가 레지스트리 규약을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 게이트 규칙 셋(`quote.customer-approval`·`quote.approval-toggle`·`quote.vendor-required`)이 등록되고 결정표가 `behavior`를 덮는다
    - 수주중 예외가 `quote.customer-approval` 안에 있고 그 선택이 SUMMARY에 한 줄로 적혔다
    - `quote.vendor-required`를 이 페이즈의 어떤 함수도 부르지 않는다(단위 테스트만)
    - `PROJECT_CUSTOMER_APPROVAL_GATE` 키가 있고 설정 화면에 나온다
    - 승인 표시가 결재 경로 없이 저장되고 행동 로그가 한 줄 남는다
    - 이전 승인 차수로 대신하지 않는다는 케이스가 통합 테스트에 있다(금지 항목)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM의 고객 승인 켜기·끄기가 서버에서 조건대로 판정되고, 고객 승인·거래처 필수 게이트가 Phase 5를 위해 등록·고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 계보 해석(D-55) + 차수 요약·표시 번호 + 이전 차수 잠김 조회</name>
  <files>domain/quotes/revisions.ts, domain/quotes/lines.ts, test/unit/domain/quote-revisions.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` U-2 행 — 사용자 확정 값(최신+승인 = `승인`만, 미승인 이전 = 빈 칸)
    - `domain/quotes/lines.ts`의 `linkedDocumentsByLine`·`listQuoteLines`(04-12 — 조회 지점과 셀 단계 계산)
  </read_first>
  <behavior>
    - `quoteDisplayNumber("26001", 2)` → `26001-2차`
    - `resolveLinkedDocumentsByLineage`: 1차 줄 A에 문서 X, 2차 줄 B(계보 A), 3차 줄 C(계보 B) → 현재 차수 C에 X가 이어진다 · 계보가 끊긴(새 차수에서 지운) 줄의 문서는 어느 현재 줄에도 붙지 않고 「차수에서 빠진 줄의 문서」 목록으로 따로 돌아온다(D-55 「견적 외 비용처럼 프로젝트에 남는다」)
    - `listRevisionSummaries`: 차수 셋(1차 승인 · 2차 미승인 · 3차 미승인 최신) → 상태 `[3차: 현재, 2차: 빈 값, 1차: 승인]` · 최신이 승인이면 `승인`만
    - 이전 차수 잠김 조회: 현재보다 작은 순번이면 모든 셀 `locked` · 조정 줄 없음 · 현재 이상 또는 없는 순번이면 현재 차수 조회로 떨어진다
  </behavior>
  <action>
① 단위 테스트(`behavior` 앞 세 줄)를 먼저 쓴다(RED). `domain/quotes/revisions.ts`에 `quoteDisplayNumber(projectNumber, seq)` → `${projectNumber}-${seq}차`(저장 없음) · 순수 함수 `resolveLinkedDocumentsByLineage(linesAcrossRevisions, docsByLineId)`(계보 사슬을 따라 현재 차수 줄에 문서를 잇고, 끊긴 계보의 문서는 별도 목록) · `listRevisionSummaries(viewer, projectId)`(순번 · 생성일(KST) · 줄 수(견적 줄+견적 외 비용, 보관 제외) · 견적 합계 · 상태 낱말 — U-2 확정값)를 더한다(GREEN).

② `domain/quotes/lines.ts`의 `linkedDocumentsByLine`이 문서 출처(이 페이즈 빈 결과)에서 받은 것을 이 해석 함수로 현재 차수에 잇게 한다 — 조회 지점은 여전히 하나다. `listQuoteLines`에 이전 차수 잠김 조회 옵션(`{ revisionSeq }` — 현재보다 작은 순번이면 셀 단계 전부 `locked`, 조정 줄 없음; 그 밖은 현재 차수)을 더한다. 04-24가 `?revision=` 보기에 이 옵션을 쓴다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-revisions.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 계보 해석이나 상태 낱말이 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revisions.test.ts test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 조회 지점 변경이 기존 저장·DTO를 깨뜨렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - `resolveLinkedDocumentsByLineage`가 순수 함수이고 계보 사슬·끊긴 계보 두 경우가 단위 테스트에 있다
    - `linkedDocumentsByLine`이 여전히 유일한 조회 지점이고 이 해석 함수를 거친다
    - 견적 표시 번호에 저장 컬럼·카운터가 없다
    - 차수 요약의 상태 낱말이 U-2 확정값이다(단위)
    - 이전 차수 잠김 조회가 모든 셀 `locked`·조정 줄 없음을 돌려준다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>연결 문서가 계보를 따라 현재 줄에 이어질 자리가 Phase 5를 위해 준비되고, 04-24가 그릴 차수 요약·표시 번호·잠김 조회가 서버에서 나온다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 새 차수·승인 액션 | 프로젝트·차수 id와 승인일이 넘어온다. 담당 PM 판정·상태·연결 문서 검사가 서버에만 있어야 한다 |
| 게이트 → Phase 5 | 고객 승인·거래처 필수 규칙이 지출 동작의 단일 판정 지점이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-68 | Elevation of Privilege | 고객 승인 표시 | high | mitigate | 서버가 `viewer.id = projects.pm_user_id`로 담당 PM을 판정하고(버튼을 숨기는 것과 별개 — 화면 쪽은 04-24), 완료·이전 차수·미래 날짜를 거부한다. 담당이 아닌 팀장 요청 거부를 통합 테스트로 고정한다 |
| T-04-69 | Tampering | 승인 표시 취소 | high | mitigate | 연결 문서 유무를 서버의 조회 지점이 판정한다 — 클라이언트의 「문서 없음」 주장을 받지 않는다 |
| T-04-70 | Tampering | 새 차수 | medium | mitigate | 한 트랜잭션(insert·복사·이동)이라 반쪽 차수가 남지 않고, `(project_id, seq)` 유일 제약이 동시 생성을 막는다. 정산·완료·빈 차수는 게이트가 거부한다 |
| T-04-71 | Elevation of Privilege | 승인 게이트 우회 | high | mitigate | 게이트가 최신 순번 차수 하나만 본다(D-54) — 이전 승인 차수로 대신하지 않는다(금지 항목, 통합 테스트). 설정으로 끄는 것만 허용된 우회다(ROADMAP 기준 3) |
| T-04-72 | Repudiation | 승인 표시 | low | mitigate | 켜기·끄기가 행동 로그에 차수·승인일과 함께 남고, 승인자는 `customer_approved_by`에 저장된다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- `CI=true pnpm test` 세 계층 통과
- `register.ts`에 `quote.revision-create`·`quote.customer-approval`·`quote.approval-toggle`·`quote.vendor-required` 등록
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 새 차수가 전체 복사로 만들어지고 조정 줄은 옮겨 가며, 최신 차수만 현재다
- 담당 PM이 현재 차수의 고객 승인을 날짜와 함께 켜고 조건대로만 끄는 것이 서버에서 판정된다
- Phase 5는 지출결의에서 `quote.customer-approval`·`quote.vendor-required`를 부르고 연결 문서 출처를 채우기만 하면 된다
- 04-24가 액션·요약·잠김 조회만으로 차수 화면을 그릴 수 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/quotes/revisions.ts` — `createRevisionFromCurrent` · `setCustomerApproval` · `listRevisionSummaries` · `quoteDisplayNumber` · `resolveLinkedDocumentsByLineage` |
| 함수(repository) | `insertRevision` · `setRevisionApproval`(`quote-revisions.ts`) · `copyQuoteLines` · `moveAdjustmentLines`(`quote-lines.ts`) |
| 게이트 규칙(신규) | `quote.revision-create` · `quote.customer-approval`(수주중 예외 포함) · `quote.approval-toggle` · `quote.vendor-required`(등록만) |
| 설정 키(신규) | `project.customer_approval_gate`(`PROJECT_CUSTOMER_APPROVAL_GATE`, 단순값, 기본 켬) |
| Server Action | `createRevisionAction` · `setCustomerApprovalAction`(등록 포함) |
| 변경된 기존 심볼 | `structuralEditability.canCreateRevision` · `linkedDocumentsByLine`(계보 해석 경유) · `listQuoteLines`(이전 차수 잠김 조회 옵션) |
| 테스트 | `test/unit/domain/quote-revisions.test.ts` · `test/integration/quote-revisions.test.ts`(신규) · `rules-gate.test.ts` 확장 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 모달 셋(`revision-dialogs.tsx`) · 차수 섹션(`revision-section.tsx`) · 머리 줄 버튼·부제 · 이전 차수 읽기 표 화면 · UI-SPEC Copywriting 두 행 · `test/e2e/quote-revisions.spec.ts` · S5 backstop 둘(04-24) |
| 새 스키마·마이그레이션 | 없음 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md` when done
</output>
