---
phase: 04-project-quote-ledger
plan: 15
type: execute
wave: 19
depends_on: ["04-14", "04-24", "04-09"]
files_modified:
  - domain/projects/index.ts
  - repositories/projects.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/project-form.tsx
  - app/(app)/projects/page.tsx
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - test/integration/project-copy.test.ts
  - test/e2e/project-copy.spec.ts
  - test/e2e/project-register.spec.ts
autonomous: true
requirements: [PROJ-05, PROJ-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "프로젝트 상세 머리 줄의 2차 「프로젝트 복사」가 등록 폼을 `?new=1&copyFrom={id}`로 열고, 폼에 원본의 클라이언트·팀·담당 PM·프로젝트명이 채워지며 폼 위 한 줄이 `{번호} {프로젝트명}에서 복사 · {k}줄`이라고 밝힌다 — `{k}`는 조정 줄을 세지 않는다(D-70, S2, ⚠ S4 「조정 줄과 복사」)"
    - "복사 등록을 제출하면 새 번호의 수주중 프로젝트가 상세 견적 1차(미승인)와 함께 생기고, 원본 현재 차수의 견적 줄(견적 줄·견적 외 비용, 보관 제외, 금액 포함)이 1차로 들어온다 — 복사본은 `version` 1이고 한 INSERT…SELECT로 원본 차수의 한 시점을 읽으며, 비고·거래처·정렬 순서·`custom_fields`·줄 상태는 원본 그대로다(04-14 `copyQuoteLines` — D-70, CEO 리뷰 B-32)"
    - "복사 등록에 따라오지 않는 것: 기간 · 번호 · 총 매출 예상가 · 매출(발행·입금) · 연결 문서 · 줄 계보(복사된 줄의 계보 칸은 비어 있다) · 조정 줄(사용자 D19-10) · **취소 상태 줄**(연결 문서 때문에 거둔 줄 — 새 프로젝트에 문서가 없고, 미착수로 되살리면 옛 견적가가 조용히 더해진다, B-32) — 원본 프로젝트는 한 칸도 바뀌지 않는다(D-70, ⚠ S4)"
    - "별도 가져오기 모달이 없다 — 다른 프로젝트의 줄 일부는 표 간 범위 복사·붙여넣기(04-04)로 가져온다(D-70, PROJ-05)"
    - "등록 폼이 열려 있으면(`?new=1`, 복사 등록 포함) 목록 필터 줄의 1차 「프로젝트 등록」이 렌더되지 않는다(S2 — 한 화면 1차 하나)"
    - "등록 폼에 선택 칸 `총 매출 예상가`(금액 + 통화 + 환율)가 있고, 적으면 프로젝트의 사전 견적(`pre_estimate_*`)으로 저장된다 — 견적 줄이 아니라 프로젝트 속성이고 고객 승인 표시·지출결의가 없다(D-52, S2)"
    - "총 매출 예상가의 통화를 USD로 고르면 환율 기본값이 설정의 USD 최근 환율에서 채워지고, 환율을 고친 저장만 그 설정을 갱신한다 — 금액 칸은 타이핑 중 천 단위 쉼표가 들어간다(D-71, D-95)"
    - "총 매출 예상가 입력은 서버가 검증한다 — 04-40의 `normalizeMoneyInput` 한 규칙(KRW면 요청 환율을 버리고 1 · USD면 환율 > 0 · 원화 환산액이 정수 컬럼 범위 안)을 지나고, 부호 규칙(금액 ≥ 0)만 이 경로가 더한다. 어긋나면 일반 오류가 아니라 그 칸의 한국어 이유로 거부된다 — 이 플랜이 KRW 고정·범위 검사를 따로 구현하지 않는다(CEO 리뷰 B-17 — KRW에 환율 1350을 실어 원화 금액을 부풀리는 위조 차단, 엔지니어링 리뷰 B §2 — 금액 입력 정규화 한 곳)"
    - "환율을 고친 USD 총 매출 예상가의 최근 환율 기억(`rememberFxRate`)은 등록 트랜잭션이 **커밋된 뒤**에 한 번 돈다 — 거부·롤백된 등록은 설정을 바꾸지 않고, 번호 부여 트랜잭션 안에서 전역 db를 부르지 않는다. 복사 출처 확인(행 범위)도 트랜잭션 앞에서 하거나 그 `tx`로 한다(04-32 규칙, 엔지니어링 리뷰 B §1)"
    - "원본이 보는 사람의 행 범위 밖이거나 보관된 프로젝트면 복사 등록이 서버에서 거부된다 — 폼 미리 채우기도 일어나지 않고, 거부는 서버 로그 경고 `write.denied` 한 줄(보는 사람·출처 id, 금액 없음)로 04-20의 `denyWrite(viewer, rule, ids, err)` 도우미를 지나 남는다(사용자 D19-3, CEO 리뷰 B-26, 엔지니어링 리뷰 `denyWrite`)"
    - "복사 등록 폼에 손대지 않고 `Esc`(또는 2차 「취소 Esc」)를 누르면 확인 없이 바로 목록으로 간다 — 04-08의 입력 버리기 확인(DR-27)이 비교하는 「폼이 처음 연 값」이 복사로 미리 채운 값이기 때문이고, 한 칸이라도 바꾼 뒤에는 `ui/confirm-dialog` 「입력 버리기」가 뜬다(UI-SPEC rev 5 「적용 규칙 S2」 `Esc`(DR-27) · UI Considerations S2 (rev 5) interactive — `Esc`)"
    - "총 매출 예상가 칸의 오류는 칸 아래 `Form.Error` 한 줄이고 글자는 rev 5 그대로다 — 음수 `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요` · 숫자 아님 `숫자가 아닙니다 · 12,400,000처럼 적어 주세요` · 환율 0 이하 `환율은 0보다 커야 합니다 · 환율을 고쳐 주세요` · 원화 환산 상한 넘김 `금액이 상한을 넘습니다 · 2,147,483,647원 이하`(04-40 `MoneyInputError`의 `message` 그대로) — 등록 폼 전체 오류는 1차 옆 `등록하지 못했습니다 · 총 매출 예상가 1칸`이고 입력값은 남는다(Copywriting `Error — 총 매출 예상가 칸` · `Error — 등록 폼 제출`, UI Considerations S2 error)"
    - "등록 뒤 총 매출 예상가는 상세 머리 줄 부제에 같은 라벨로 보이고 거기서 고친다 — 그 표시·수정 칸(S17)은 그룹 A의 04-22가 만든다(계약 8 · DR-28 · DR-37). 이 플랜은 등록·복사 쪽만 맡는다(UI-SPEC rev 5 「적용 규칙 S2」 1102행)"
    - statement: "복사 등록 폼의 출처 한 줄과 가장 긴 프로젝트명·총 매출 예상가 칸이 1280 · 1024 · 375에서 폼 최대 폭 720과 칸 폭 3종(200·280·480) 안에 머물고 가로 스크롤 0이다 — DOM 감사(UI Considerations S2 long-text · DR-14 감사 폭)"
      verification: backstop
  prohibitions:
    - statement: "복사 등록이 원본 프로젝트의 돈 기록을 끌고 와서는 안 된다. 매출 발행·입금, 연결 문서, 조정 줄, 줄 계보가 새 프로젝트로 따라오면 두 프로젝트가 같은 돈을 두 번 세거나 원본의 증빙이 엉뚱한 프로젝트에 붙는다 — 복사는 기본 정보와 견적 줄 구조까지만이다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/index.ts"
      provides: "`createProject`가 복사 출처와 총 매출 예상가를 받는다 — 번호 부여·1차 생성과 같은 트랜잭션에서 줄을 복사(계보 없음)"
      contains: "copyFromProjectId"
    - path: "test/integration/project-copy.test.ts"
      provides: "복사 범위 · 제외 목록 · 원본 무변경 · 범위 밖 거부 · 총 매출 예상가 저장 증명"
      min_lines: 45
    - path: "test/e2e/project-copy.spec.ts"
      provides: "상세 → 프로젝트 복사 → 미리 채운 폼 → Ctrl+Enter 등록 → 새 상세에 줄"
      min_lines: 30
  key_links:
    - from: "domain/projects/index.ts"
      to: "repositories/quote-lines.ts"
      via: "04-14의 `copyQuoteLines`를 계보 없이(`withLineage: false`) 재사용한다 — 복사 기계를 복제하지 않는다"
      pattern: "copyQuoteLines"
    - from: "app/(app)/projects/project-form.tsx"
      to: "app/(app)/projects/actions.ts"
      via: "복사 출처 id와 총 매출 예상가가 같은 등록 액션으로 간다"
      pattern: "copyFromProjectId"
---

<objective>
**As a** 기획 PM, **I want to** 지난번과 비슷한 행사를 이전 프로젝트에서 복사해 등록하고, 등록할 때 총 매출 예상가를 함께 적고 싶다, **so that** 반복되는 견적 구조를 다시 치지 않고, 상세 견적이 없는 초기에도 매출 규모가 기록된다.

이 플랜은 옛 04-06 Task 2의 **프로젝트 복사**(D-70)를 옮겨 오고, 04-01이 만든 등록 폼에서 빠져 있던 **총 매출 예상가**(D-52 · UI-SPEC S2 — 스키마는 04-01의 `pre_estimate_*`가 있고 입력 칸만 없다)를 채운다. 두 일이 같은 폼·같은 `createProject`를 고친다. 줄 복사는 04-14의 `copyQuoteLines`를 계보 없이 재사용한다.

Purpose: ROADMAP 기준 1 「이전 프로젝트·견적 줄 복사와 기본값 채움으로 반복 입력이 없다」와 PROJ-05.

Output: 복사 등록 경로 · 총 매출 예상가 칸 · 상세의 「프로젝트 복사」 · 통합 1 · E2E 1.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 부록 B).** (1) **B-32:** 프로젝트 복사는 04-14의 INSERT…SELECT 복사(`version` 1)를 쓰고 **취소 상태 줄을 빼고** 복사한다 — 출처 한 줄의 `{k}`도 같은 기준. (2) **B-17:** 총 매출 예상가의 KRW 환율 1 고정 · USD 환율 > 0 · 금액 범위를 서버가 검증한다. (3) **사용자 D19-10:** 조정 줄 제외는 기본값이 아니라 확정이다. (4) **사용자 D19-3 · B-26:** 범위 밖 출처 거부를 경고 로그로 남긴다. (5) B-37(빈 칸 = 0원)은 이미 이 계획의 판단으로 SUMMARY에 남기게 돼 있다 — 바꾸지 않는다.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`).** (1) **금액 입력 정규화 한 곳(B §2):** 총 매출 예상가의 KRW 환율 1 고정·USD 환율 > 0·정수 범위는 04-40이 `domain/money`에 둔 `normalizeMoneyInput`을 쓴다 — 이 플랜의 손 검증기를 지우고 부호(≥ 0)만 더한다. (2) **잠금 안 전역 db 호출 금지(04-32):** 번호 부여 트랜잭션 안에서 `rememberFxRate`를 부르지 않고 커밋 뒤에, 복사 출처 확인은 트랜잭션 앞 또는 `tx`로. (3) **`denyWrite`**(04-20 도우미)로 범위 밖 출처 거부 로그. (4) **read_first 정정:** 통화·환율 칸 선례는 `revenue-section.tsx` 210–253행(90–120행이 아니다) — 그 계약 금액 칸은 04-16이 지우므로 오래 남는 선례는 `quote-table.tsx`의 `UnitPriceEditCell`이다.

**2026-09-23 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`).** (1) **DR-28 · DR-37:** 옛 계획의 「이 페이즈 상세 화면에는 총 매출 예상가 표시 자리가 없다」를 지웠다 — 상세 부제 표시와 수정 칸(S17)은 그룹 A의 04-22가 만든다(계약 8). 등록·복사 동작은 그대로다. (2) **DR-27:** 복사 등록의 「처음 연 값」은 미리 채운 값이다 — 손대지 않은 복사 폼의 `Esc`는 바로 목록, 바꾼 뒤에는 04-08의 입력 버리기 확인(E2E 한 줄). (3) **P0 문구:** 총 매출 예상가 칸 오류를 rev 5 Copywriting 글자로(`총 매출 예상가는 0 이상 · 금액을 고쳐 주세요` 등), 범위 밖 복사 출처의 방어 거부를 명사형 `복사할 프로젝트 없음 · 새로 고침`으로. (4) **DR-14:** DOM 감사 폭 1280 · 1024 · 375.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5) 「UI Considerations S2」 481–488행(여섯 상태 + (rev 5) `Esc`) · 「적용 규칙 S2」 1095–1116행(칸 · 필수 넷 · 총 매출 예상가 → 상세 부제(S17) · 1차 미렌더 · 복사 등록 · `Esc`(DR-27)) · 「UI Considerations S4」 517–520행(해소 — 조정 줄과 복사, 프로젝트 복사) · Copywriting 377행(Error — 총 매출 예상가 칸) · 383행(Error — 등록 폼 제출) · 429행(확인 — 등록 폼 입력 버리기) — 줄 번호는 `grep -n "### S2. 프로젝트 등록"`로 다시 찾는다
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-05(프로젝트 복사)** 와 **PROJ-01(등록 칸 — 총 매출 예상가)** 이다. 수용 기준은 D-52·D-70·D-71과 UI-SPEC S2에서 끌어왔다. S2 `## UI Considerations`의 long-text backstop은 마커로 옮겼고, empty·loading·error·partial(복사 등록의 부분 채움)은 truths로 덮었다.

**계획 단계 가정(표면화):**
- **⚠ S4 조정 줄과 복사 — 기본값 적용(사용자 D19-10으로 확정):** 프로젝트 복사는 조정 줄을 가져오지 않고 출처 한 줄의 줄 수에서도 뺀다.
- **취소 상태 줄(CEO 리뷰 B-32의 두 선택지 중 「빼기」):** 줄 상태를 미착수로 되돌려 가져오는 안도 있었지만, 취소 줄은 연결 문서 때문에 견적가 0으로 거둔 줄이라 되살리면 옛 견적가가 새 프로젝트 합계에 조용히 들어간다. 새 차수 복사(04-14)는 계보로 연결 문서를 이어야 하므로 취소 줄을 그대로 복사하고, 프로젝트 복사만 뺀다 — `copyQuoteLines`의 선택 인자 하나로 가른다.
- **복사와 줄 수 상한:** 복사 줄 수는 원본 차수의 줄 수 이하라 04-26 상한을 다시 보지 않는다(04-14와 같은 근거 — A-20 정정 뒤 상한은 줄을 늘릴 때만 거부).
- **총 매출 예상가 칸이 빠져 있던 것:** 04-01 등록 폼·액션에 이 칸이 없다(스키마 컬럼은 있고 `createProject`가 0을 넣는다). D-52·S2가 요구하는 칸이라 이 플랜이 채운다 — 이 보완 계획의 D-75~D-95 범위 밖에서 발견한 **실행된 플랜의 빈자리**다(SUMMARY에 적는다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 상세에서 「프로젝트 복사」 → 미리 채운 등록 폼 → 등록 → 새 프로젝트 1차에 줄</name>
  <files>domain/projects/index.ts, repositories/projects.ts, repositories/quote-lines.ts, app/(app)/projects/actions.ts, app/(app)/projects/project-form.tsx, app/(app)/projects/page.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/integration/project-copy.test.ts, test/e2e/project-copy.spec.ts</files>
  <read_first>
    - `domain/projects/index.ts` 214–290행(`findProject` · `ProjectInput` · `createProject` — 번호 부여·1차 생성 트랜잭션)
    - `repositories/projects.ts` 223–275행(`insertProject` 입력)
    - `repositories/quote-lines.ts`의 `copyQuoteLines`(04-14 산출물 — INSERT…SELECT, `version` 1) · 줄 취소 상태 값(04-12 — Grep `cancel`)
    - `.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md`의 `domain/rules/deny-write.ts`의 `denyWrite(viewer, rule, ids, err)`(허용 필드 — `sourceProjectId` 포함) · `04-32-SUMMARY.md`의 「잠금 안 전역 db 호출 금지」
    - `app/(app)/projects/actions.ts` 1–45행(`createProjectAction`)
    - `app/(app)/projects/project-form.tsx`(전문) · `app/(app)/projects/page.tsx`(전문 — `?new=1` 토글과 1차 미렌더 규칙)
    - `app/(app)/admin/vendors/page.tsx` 1–71행(검색 파라미터 토글 관례)
    - `test/integration/project-status.test.ts`(04-06·04-20) — 프로젝트·줄 준비 형태
    - `.planning/phases/04-project-quote-ledger/04-08-SUMMARY.md` — 등록 폼 입력 버리기 확인(DR-27)이 「처음 연 값」을 받는 자리 · `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「적용 규칙 S2」 1109–1116행(복사 등록 · `Esc`)
  </read_first>
  <behavior>
    - 원본(줄: 견적 줄 둘 · 견적 외 비용 하나 · 조정 하나 · 보관된 줄 하나 · 취소 상태 견적 줄 하나, 기간·총 매출 예상가·발행 줄 있음, 2차 승인)을 복사 등록 → 새 프로젝트: 수주중 · 새 번호 · 1차 미승인 · 줄 셋(견적 줄 둘 + 견적 외 비용, 금액 그대로, 계보 비어 있음, `version` 1) · 취소 줄 없음 · 기간·총 매출 예상가 비어 있음 · 발행·입금 줄 0
    - 원본의 줄·차수·상태·매출이 복사 뒤에도 그대로다
    - 출처 한 줄의 `{k}`(= `getProjectCopySource`의 줄 수)가 실제 복사된 줄 수(3)와 같다
    - 보는 사람 범위 밖 원본 id → 거부 + `write.denied` 경고 한 번(금액 없음)
  </behavior>
  <action>
① `domain/projects/index.ts`: `ProjectInput`에 선택 `copyFromProjectId`를 더하고 `createProject`가 같은 트랜잭션 안에서 (a) 출처를 행 범위(`scopeFor`) 안에서 찾고(없거나 보관됐으면 방어 거부 `복사할 프로젝트 없음 · 새로 고침` — rev 5 밖 문구라 P0 · F1 명사형, SUMMARY에 기록 — 04-20의 `denyWrite(viewer, "project.copy-source", { sourceProjectId }, err)`를 지나 던진다, 사용자 D19-3 · B-26. 출처 확인은 번호 부여 트랜잭션 **앞**에서 하거나 그 `tx`로 한다 — 잠금 안에서 전역 db를 부르지 않는다, 04-32 규칙) (b) 번호 부여·프로젝트 insert·1차 insert(기존 경로 그대로 — **번호 부여·차수 생성 코드를 복제하지 않는다**) (c) 출처 현재 차수 → 새 1차로 `copyQuoteLines({ withLineage: false, excludeCancelled: true })`. `repositories/quote-lines.ts`의 `copyQuoteLines`(04-14)에 선택 `excludeCancelled`(기본 false — 새 차수 복사는 취소 줄도 계보째 가져간다)를 더해 같은 INSERT…SELECT의 WHERE에 취소 상태 제외를 붙인다(CEO 리뷰 B-32 — 복사 기계를 복제하지 않는다). 기간·총 매출 예상가는 **입력 값만** 쓴다(출처에서 가져오지 않는다). 행동 로그 detail에 출처 id와 복사한 줄 수. 새 함수 `getProjectCopySource(viewer, id)` → `{ number, name, clientId, teamId, pmUserId, lineCount }`(줄 수는 견적 줄+견적 외 비용, 보관·취소 제외 — 복사와 같은 기준)를 폼 미리 채우기용으로 둔다.

② `createProjectAction` 스키마에 `copyFromProjectId?`를 더한다. `app/(app)/projects/page.tsx`가 `copyFrom` 검색 파라미터를 받으면 `getProjectCopySource`로 미리 채움 값을 만들어 `ProjectForm`에 넘기고(범위 밖이면 미리 채우지 않고 일반 등록 폼), 폼 위에 `--fs-sm --muted` 한 줄 `{번호} {프로젝트명}에서 복사 · {k}줄`. 폼은 숨은 칸으로 출처 id를 싣는다. `?new=1`일 때 1차 「프로젝트 등록」을 렌더하지 않는 기존 규칙이 `copyFrom`에도 그대로인지 확인한다. **가져오기 모달을 만들지 않는다.** **`Esc`와 미리 채움(DR-27):** 04-08이 만든 등록 폼의 입력 버리기 확인(`ui/confirm-dialog` 「입력 버리기」)은 「폼이 처음 연 값」과 지금 값을 비교한다 — 복사 등록에서는 미리 채운 값이 그 처음 값이 되게 넘긴다(손대지 않은 복사 폼의 `Esc`는 확인 없이 바로 목록, 한 칸이라도 바꾸면 확인). 04-08의 비교 방식을 바꾸지 않고 처음 값을 넘기는 자리만 쓴다.

③ `[id]/page.tsx`·`quote-table.tsx`: `projects` 쓰기 권한자에게 머리 줄 2차 「프로젝트 복사」(`/projects?new=1&copyFrom={id}#project-form`로 가는 링크 — 이동이라 `<a>`, §10)를 버튼군에 둔다(버튼 자리 prop 하나).

④ `test/integration/project-copy.test.ts`에 `behavior` 세 줄(DB 재조회). `test/e2e/project-copy.spec.ts`: 담당 PM이 줄 둘인 프로젝트 상세에서 「프로젝트 복사」 → 폼에 원본 값과 출처 한 줄 `… · 2줄` → 프로젝트명만 바꾸고 Ctrl+Enter → 새 상세에 줄 둘, 상태 수주중, 부제의 번호가 원본과 다르고 표에 `조정` 그룹이 없다(원본에 조정 줄이 있어도 — UI Considerations S4 해소). 둘째 케이스(DR-27): 복사 폼을 열고 아무것도 바꾸지 않은 채 `Esc` → 대화상자 없이 목록 · 다시 복사 폼을 열어 프로젝트명을 바꾼 뒤 `Esc` → `입력 버리기` 확인이 뜨고 2차 `취소 Esc` → 폼이 바꾼 값 그대로.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-copy.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 복사 범위가 틀렸거나 번호 부여 경로가 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-copy.spec.ts test/e2e/project-register.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 복사가 번호 부여·1차 생성과 같은 트랜잭션이고 그 경로의 코드를 복제하지 않는다(`createProject` 안의 분기 하나)
    - 복사된 줄의 계보 칸이 비어 있고 `version`이 1이다(통합 테스트)
    - 손대지 않은 복사 폼의 `Esc`가 확인 없이 목록으로, 바꾼 뒤의 `Esc`가 `입력 버리기` 확인으로 간다(E2E, DR-27)
    - 조정 줄·보관 줄·취소 줄·기간·총 매출 예상가·매출 줄이 따라오지 않는다(통합 테스트 각각)
    - 복사는 04-14의 `copyQuoteLines` 하나를 선택 인자로 쓴다 — 두 번째 복사 쿼리가 없다
    - 범위 밖 출처 거부에서 `write.denied`가 한 번이다
    - 원본 무변경을 DB 재조회로 단언한다
    - 가져오기 모달 컴포넌트가 없다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상세의 「프로젝트 복사」에서 등록까지 한 경로가 끝나고, 복사 범위가 D-70 그대로임이 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 등록 폼의 총 매출 예상가(D-52) + 화면 검증</name>
  <files>domain/projects/index.ts, app/(app)/projects/actions.ts, app/(app)/projects/project-form.tsx, test/integration/project-copy.test.ts, test/e2e/project-register.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「적용 규칙 S2」 1097–1102행(칸 · 필수 넷 · 총 매출 예상가 라벨) · Copywriting 377행(`Error — 총 매출 예상가 칸`) · 383행(`Error — 등록 폼 제출`)
    - `domain/money/index.ts`(`moneyToColumns` · 04-40의 `normalizeMoneyInput`·`MoneyInputError` — 사유 `fx-rate`·`range`·`not-finite`와 한국어 `message`) · `domain/money/currency.ts`(`recentFxRate`·`rememberFxRate`)
    - `app/(app)/projects/[id]/revenue-section.tsx` 210–253행(계약 금액의 통화 Select + 금액 + 환율 칸 — 04-02 형태, 04-16이 뒤에 지운다) · `app/(app)/projects/[id]/quote-table.tsx`의 `UnitPriceEditCell`(Grep — 통화·금액·환율 + touched 처리, 오래 남는 선례) — 같은 모양을 쓴다
    - `ui/input/TextField.tsx`의 `numberKind`(04-09)
    - `ui/form/Form.tsx` · `ui/select/Select.tsx`
  </read_first>
  <behavior>
    - 총 매출 예상가 KRW 120,000,000 입력 → `pre_estimate_amount_krw` 120000000, 통화 KRW, 환율 1
    - KRW 120,000,000에 환율 1,350을 실은 위조 요청 → 원화 120,000,000 · 환율 1로 저장된다(서버가 환율을 1로 고정 — B-17)
    - USD 100,000 · 환율 1,350 → 외화 금액·환율·원화 환산액이 `domain/money`로 저장되고, 환율을 고쳤으면 `fx.recent_rate.USD`가 1350으로 갱신된다
    - USD 환율 0 · 음수 금액 · 원화 환산 2,147,483,648 이상 → 각각 한국어 이유로 거부, 프로젝트 행 없음(B-17) · 환율을 고친(`preEstimateFxRateTouched`) USD 등록이 거부되면 `fx.recent_rate.USD` 무변경(커밋 뒤에만 기억)
    - 칸을 비우면 원화 0 · KRW · 환율 1로 저장된다(04-01의 기본 저장과 같다 — 「없음」과 0을 가르는 새 컬럼을 만들지 않는다. 0원 예상가는 업무상 없음으로 읽는다 — CEO 리뷰 B-37 확인)
  </behavior>
  <action>
① `ProjectInput`에 선택 `preEstimate?: { currency, amount, fxRate }`와 `preEstimateFxRateTouched?`를 더하고 `createProject`가 `moneyToColumns`로 저장한다(원화 환산은 `domain/money`). **입력 검증(CEO 리뷰 B-17 · 엔지니어링 리뷰 B §2):** 트랜잭션 앞에서 04-40의 `normalizeMoneyInput(preEstimate)`을 부른다 — KRW 환율 1 고정·USD 환율 > 0·정수 범위는 그 한 함수가 하고, 이 경로는 부호(금액 ≥ 0 — 음수면 `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요`, rev 5 Copywriting)만 더한다. `MoneyInputError`는 그 `message`(04-40 — rev 5 글자 그대로)를 `총 매출 예상가` 칸의 서버 필드 오류로 돌려준다 — 04-01 등록 폼의 칸 오류 경로(`Form.Error`)와 1차 옆 `등록하지 못했습니다 · 총 매출 예상가 1칸`이 그대로 쓴다(PG 22003·일반 오류로 가지 않는다) — **KRW 고정·범위 검사를 이 파일에 따로 구현하지 않는다**(두 번째 검증기 금지). 칸을 비우면 04-01과 같은 기본 저장(원화 0 · KRW · 환율 1 — `pre_estimate_amount_krw`가 NOT NULL이다)을 쓰고 새 컬럼을 만들지 않는다(판단을 SUMMARY에 한 줄). 환율을 고친 USD 저장만 `rememberFxRate`(D-71)를 부르되 **등록 트랜잭션이 커밋된 뒤** 한 번 부른다(04-32 규칙 — 잠금 안 전역 db 호출 금지 · 거부된 등록이 설정을 바꾸지 않는다). `createProjectAction` 스키마에 같은 필드를 더하고 zod에서도 `fxRate > 0`·`amount ≥ 0`을 먼저 거른다.

② `project-form.tsx`: 선택 칸 `총 매출 예상가`(금액 280 + 통화 Select 200 + 환율 280, KRW면 환율 칸 숨김, USD 기본 환율은 `recentFxRate("USD")` 실제 값) — 금액 칸은 `TextField numberKind`(04-09 쉼표 입력). 라벨은 사용자 용어 그대로. 필수 표시(`*`)를 쓰지 않는다. 복사 등록에서는 비어 있다.

③ 테스트: 통합에 `behavior` 다섯 줄(위조·거부 케이스는 DB 재조회로 행 없음·저장 값) · E2E(`project-register.spec.ts`)에 총 매출 예상가 `120000000`을 치면 칸에 `120,000,000`이 보이고 등록이 성공하는 케이스(저장 값은 통합 테스트가 DB로 단언한다 — 상세 부제의 표시·수정 칸은 04-22 몫이다, 계약 8) · 음수 금액을 쳐서 제출하면 칸 아래 `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요`와 1차 옆 `등록하지 못했습니다 · 총 매출 예상가 1칸`이 보이고 다른 칸 입력이 남는 케이스.

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S2 long-text backstop(최장 프로젝트명·출처 한 줄·총 매출 예상가 칸이 폼 720과 칸 폭 3종 안, 가로 스크롤 0)을 **1280 · 1024 · 375**에서 DOM 실측(DR-14) → 수정 → `CI=true pnpm test` 한 번. 폭별 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-copy.test.ts test/integration/settings.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 총 매출 예상가 저장이나 최근 환율 갱신 규칙이 틀렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S2 long-text 판정을 SUMMARY에서 확인</test>
      <expected>1280 · 1024 · 375 모두 최장 입력에서도 폼이 720을 넘지 않고 칸 폭 3종을 벗어나지 않으며 가로 스크롤 0</expected>
      <why_human>폭 판정은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 등록 폼에 `총 매출 예상가` 칸이 있고 필수 표시가 없다
    - 금액 칸이 04-09의 쉼표 입력을 쓴다(E2E가 `120,000,000` 표시를 단언)
    - USD 저장의 원화 환산이 `domain/money`에서 계산된다(모듈 밖 산술 없음 — `plant8/money-boundary` 린트 통과)
    - 환율을 고친 저장만 최근 환율 설정을 갱신한다(통합)
    - KRW 위조 환율이 1로 고정되고, USD 환율 0 · 음수 금액 · 정수 범위 초과가 rev 5 Copywriting 글자(`환율은 0보다 커야 합니다 · 환율을 고쳐 주세요` · `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요` · `금액이 상한을 넘습니다 · 2,147,483,647원 이하`)의 칸 오류로 거부된다(통합, B-17) · 환율·범위 판정이 `normalizeMoneyInput` 호출이고 `domain/projects/index.ts`에 KRW 환율 고정·범위 비교를 손으로 쓴 코드가 없다(B §2)
    - `rememberFxRate`가 등록 트랜잭션 커밋 뒤에만 불리고, 거부된 USD 등록 뒤 `fx.recent_rate.USD`가 그대로다(통합)
    - 빈 칸 저장이 04-01 기본값과 같다는 판단이 SUMMARY에 있다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>등록 폼이 UI-SPEC S2의 칸을 전부 갖추고, 총 매출 예상가가 금액 모델로 저장된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 등록 액션(복사 출처) | 출처 프로젝트 id가 넘어온다. 행 범위·보관 여부 판정이 서버에만 있어야 한다 |
| 원본 → 새 프로젝트 | 복사가 돈 기록을 끌고 오면 이중 계상이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-73 | Information Disclosure | 복사 출처 id | high | mitigate | 출처를 `scopeFor` 행 범위 안에서만 찾는다 — 볼 수 없는 프로젝트의 이름·줄이 미리 채우기나 복사로 새지 않는다. 범위 밖 id 거부를 통합 테스트로 고정하고 `write.denied` 경고로 남긴다(사용자 D19-3 · B-26) |
| T-04-74 | Tampering | 복사 범위 | high | mitigate | 복사는 기본 정보 넷과 견적 줄(견적 줄·견적 외 비용, 취소 줄 제외)만 한다. 매출·연결 문서·조정·계보·기간·사전 견적·취소 줄 제외를 통합 테스트로 하나씩 고정한다(금지 항목, B-32) |
| T-04-75 | Tampering | 총 매출 예상가 | medium | mitigate | 원화 환산은 서버 `domain/money`가 하고 클라이언트 계산값을 받지 않는다. KRW 환율 1 고정·USD 환율 > 0·정수 범위는 04-40의 `normalizeMoneyInput` 한 규칙이, 금액 ≥ 0은 이 경로가 검증한다(B-17 · 엔지니어링 리뷰 B §2 — KRW에 큰 환율을 실어 원화 금액을 부풀리는 위조 차단). 최근 환율 갱신은 touched 플래그로만, 커밋 뒤에만(04-02 T-04-11과 같은 결 · 04-32 규칙) |
| T-04-76 | Denial of Service | 대량 줄 복사 | low | accept | 복사 줄 수는 차수 상한(04-26, 기본 300) 안이고 한 트랜잭션이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 복사 제외 목록 통합 테스트 초록
</verification>

<success_criteria>
- 이전 프로젝트에서 복사해 등록하면 기본 정보와 견적 줄만 따라오고 돈 기록은 따라오지 않는다
- 등록할 때 총 매출 예상가를 금액 모델로 적는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 입력 계약 | `ProjectInput.copyFromProjectId` · `ProjectInput.preEstimate` · `preEstimateFxRateTouched` · `createProjectAction` 스키마 같은 필드 |
| 함수(신규) | `getProjectCopySource`(domain) |
| 재사용(04-40) | `normalizeMoneyInput`·`MoneyInputError`(`domain/money`) — 총 매출 예상가 검증 · 04-20의 `denyWrite` |
| 변경된 기존 심볼 | `createProject`(복사 분기 · 사전 견적 저장·검증 · 커밋 뒤 최근 환율 기억) · `copyQuoteLines`(선택 `excludeCancelled` — 04-14 산출물) · `ProjectForm`(총 매출 예상가 칸 · 복사 미리 채움 = 입력 버리기 확인의 처음 값(DR-27) · 출처 한 줄) · 목록 페이지(`copyFrom` 파라미터) · 상세 머리 줄(「프로젝트 복사」 링크) |
| 로그 이벤트 | `write.denied`(경고 — 범위 밖 복사 출처, 금액 없음) |
| 테스트 | `test/integration/project-copy.test.ts` · `test/e2e/project-copy.spec.ts`(신규) · `project-register.spec.ts` 확장 |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-32 — 프로젝트 복사의 줄 상태·`version`·복사 방식 미정 | NOTE | Plan 04-15, Task 1 ①: 04-14의 INSERT…SELECT(`version` 1) + `excludeCancelled` · `{k}` 같은 기준 · 통합 |
| B-17 — KRW 환율 위조·USD 환율 0 이하·범위 밖 금액 | WARNING | Plan 04-15, Task 2 ①③: 서버 KRW 환율 1 고정 + 검증 + 한국어 이유, 통합 케이스 |
| 사용자 D19-10 — 조정 줄 복사 제외 확정 | DECISION | Plan 04-15, `<probe_fallback>`·truths: 확정 표기 |
| 사용자 D19-3 · B-26 — 거부 로그 | DECISION | Plan 04-15, Task 1 ①: 범위 밖 출처 `write.denied` |
| B-37 — 빈 총 매출 예상가 = 0원 | NOTE | 이미 반영(ALREADY-COVERED) — Task 2 behavior·SUMMARY 판단 유지 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B §2 — KRW 환율 1·범위 검증이 기능마다 재구현 | P2 | Plan 04-15, Task 2 ①: 04-40의 `normalizeMoneyInput` 사용, 손 검증기 제거 · 부호만 이 경로 |
| B §1 · A §1 — 잠금 안 전역 db 호출 · 거부된 저장이 최근 환율을 바꿈 | P2 | Plan 04-15, Task 1 ① · Task 2 ①: 출처 확인은 트랜잭션 앞/`tx`, `rememberFxRate`는 커밋 뒤 + 거부 케이스 |
| `denyWrite` 한 도우미 | P2 | Plan 04-15, Task 1 ① |
| B §2 — read_first 줄 범위(revenue-section 90–120 → 210–253) | P3 | Plan 04-15, Task 2 read_first(+ `UnitPriceEditCell`) |

### Round 3 — 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-28 — 총 매출 예상가를 등록에서 받지만 상세에 자리가 없음(옛 04-15 「표시 자리 없음」 문장) | MEDIUM | Plan 04-15, truths · Task 2 ③: 문장 삭제 — 상세 부제 표시·수정은 그룹 A Plan 04-22(계약 8 · DR-37) · 등록·복사 동작 유지 |
| DR-27 — 등록 폼 Esc(복사 등록의 처음 값) | MEDIUM | Plan 04-15, truths · Task 1 ②④: 미리 채운 값을 처음 값으로 넘김 · E2E (확인 다이얼로그는 Plan 04-08) |
| P0 · DR-17 — 총 매출 예상가 오류 · 방어 거부 문구 | MEDIUM | Plan 04-15, truths · Task 1 ① · Task 2 ①③: rev 5 Copywriting 글자 · `복사할 프로젝트 없음 · 새로 고침` |
| DR-14 — DOM 감사 폭 1024 | HIGH | Plan 04-15, Task 2 ④: 1280 · 1024 · 375 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 엔지니어링 리뷰 §3 — 보관된 거래처를 가진 줄의 복사 | P3 관찰(리뷰가 결정·수정을 요구하지 않음) — 복사는 거래처 id를 그대로 옮기고 표의 거래처 칸이 보관 거래처를 표시하는 기존 규약을 따른다. 실행에서 드러나면 SUMMARY에 적는다 |
| 복사 중 원본 변경(READ COMMITTED 읽기 분리) | 리뷰가 NOTE로 낮은 영향 판정 — 복사가 INSERT…SELECT 한 문장이라 원본 차수의 한 시점을 읽는다. 추가 잠금 없음 |


<output>
Create `.planning/phases/04-project-quote-ledger/04-15-SUMMARY.md` when done
</output>
