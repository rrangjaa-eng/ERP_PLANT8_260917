---
phase: 04-project-quote-ledger
plan: 16
type: execute
wave: 18
depends_on: ["04-15"]
files_modified:
  - domain/revenue/index.ts
  - domain/projects/index.ts
  - domain/projects/ledger.ts
  - domain/permissions/info-items.ts
  - db/schema/projects.ts
  - db/migrations/0014_drop_project_contract_columns.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0014_snapshot.json
  - repositories/projects.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - test/integration/revenue-entries.test.ts
  - test/integration/custom-fields.test.ts
  - test/integration/visibility.test.ts
  - test/e2e/revenue-section.spec.ts
autonomous: true
requirements: [PROJ-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "계약 금액 입력 칸이 없다 — 매출 섹션의 계약 금액은 고객 승인이 표시된 **현재 차수**의 견적 합계(공급가)이고, `KvList` 한 줄 `계약 금액` `48,000,000` + 2행 `부가세 10% 4,800,000 · 합계 52,800,000 · 2차 고객 승인 합계`로 보인다(D-84, UI-SPEC S6, Copywriting 표시 — 계약 금액(파생))"
    - "현재 차수가 미승인이면(새 차수를 만든 직후 등) 계약 금액은 `—` + 2행 `{n}차 고객 승인 전`이다 — 이전 승인 차수의 합계로 대신하지 않는다(D-84 「현재 차수」)"
    - "계약 금액의 부가세·합계는 서버가 `domain/money`와 세금 설정으로 계산한다 — 화면은 계산하지 않는다(PNL-09, 기준 5)"
    - "네고로 금액이 바뀌면 새 차수로 반영한다 — 계약 금액을 따로 고치는 경로(화면 칸 · 액션 필드 · 도메인 함수 · 리포지토리 함수 · DB 컬럼)가 전부 없다(D-84, 옛 D-57의 「계약 금액 PM 쓰기」 폐기)"
    - "`projects.contract_*` 네 컬럼이 새 마이그레이션으로 제거되고 코드에 참조가 없다 — 0010은 고쳐 쓰지 않는다(D-84, RESEARCH §4)"
    - "매출 섹션의 1차 저장 합류(dirty N)에서 계약 금액 칸이 빠지고, 저장 버튼 렌더 조건이 「편집할 수 있는 칸이 구조적으로 하나라도 있는가」(견적 줄 셀 · 발행·입금 쓰기 · 기간 칸)다(U-5, S3)"
    - "세금계산서 발행액은 기획본부(PM)에게 기본 공개다 — PM에게 발행 줄 표가 흰 머리글 읽기 표(캡션)로 오고, 줄이 0이면 `발행한 세금계산서가 없습니다 · 발행은 경영관리`(버튼 없음)다(D-85, S6, Copywriting)"
    - "입금액은 PM에게 기본 숨김을 유지한다 — 입금 줄 표가 서버에서 오지 않고(빈 배열이 아니라 필드 부재), 입금 표 합계 행의 미수·초과 입금 표시도 함께 없으며, 입금액을 역산할 수 있는 다른 필드(잔액 등)도 오지 않는다(D-85, S6 partial)"
    - "PM에게 매출 섹션 부제는 `공급가액 기준`만이고, 입금 표를 받는 사람에게는 `공급가액 기준 · 입금액만 통장 합계`다(Copywriting PNL-09 표시)"
    - "정보 노출표 기본값 변경은 새로 시드되는 기획 PM 계급과, 시드를 다시 돌린 기존 DB의 기획 PM 계급 행에 적용된다(시드가 기본 계급 행을 upsert한다 — RESEARCH §5 소급 여부 Gap 해소)"
    - statement: "발행·입금 표(열 셋)와 계약 금액 2행이 375px에서 가로 스크롤 0이고 숫자가 꺾이지 않는다 — DOM 감사"
      verification: backstop
    - statement: "발행·입금 표의 메모 셀이 PC에서 한 줄 말줄임이고 행 높이를 늘리지 않는다 — 긴 메모 DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "입금액이 기획본부에게 역산 경로로 새어서는 안 된다. 발행액을 공개한 뒤 미수·초과 입금·잔액 같은 파생값이 하나라도 PM 응답에 남으면 발행액 − 미수 = 입금액이 된다 — 입금 표를 숨기는 것은 표 단위 필드 부재이고, 그 표에서 파생된 값도 같이 빠져야 한다"
      status: resolved
      verification: test
    - statement: "계약 금액이 이전 승인 차수의 합계로 조용히 대신 채워져서는 안 된다. 현재 차수가 미승인이면 계약이 아직 확정되지 않은 것이고, 옛 합계를 보이면 PM과 경영관리가 서로 다른 계약 금액을 믿게 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/revenue/index.ts"
      provides: "파생 계약 금액(현재 차수 승인 여부 · 견적 합계 · 부가세·합계 · 출처 문구) — 쓰기 경로 없음"
      contains: "고객 승인"
    - path: "db/migrations/0014_drop_project_contract_columns.sql"
      provides: "projects.contract_* 네 컬럼 제거(락 타임아웃 한 쌍)"
      contains: "DROP COLUMN"
    - path: "domain/permissions/info-items.ts"
      provides: "revenue.issued_amount 기본 공개 · revenue.paid_amount 기본 숨김"
      contains: "revenue.issued_amount"
  key_links:
    - from: "domain/revenue/index.ts"
      to: "domain/quotes/revisions.ts"
      via: "계약 금액이 현재 차수(최신 순번)와 그 승인 표시에서 파생된다"
      pattern: "customerApprovedAt"
    - from: "app/(app)/projects/[id]/revenue-section.tsx"
      to: "domain/revenue/index.ts"
      via: "계약 금액이 입력이 아니라 DTO의 파생 값 `KvList` 한 줄이다"
      pattern: "KvList"
    - from: "domain/permissions/info-items.ts"
      to: "domain/seed/index.ts"
      via: "기본 계급의 노출 행이 staffDefault로 시드된다"
      pattern: "staffDefault"
---

<objective>
**As a** 기획 PM과 경영관리, **I want to** 계약 금액을 따로 적지 않고 고객이 승인한 현재 차수의 견적 합계로 보고, PM도 세금계산서 발행액을 보되 입금액은 경영관리만 보기를, **so that** 계약 금액이 견적과 어긋나는 일이 구조적으로 없고, PM은 발행 진행을 알되 입금 사정은 경영관리 정보로 남는다.

이 플랜은 04-02가 만든 매출 섹션을 D-84·D-85로 고친다. 옛 D-57의 「계약 금액은 PM」과 「발행액 기획본부 기본 숨김」 두 부분은 **대체됐다** — 이 플랜은 계약 금액 입력의 모든 경로(화면 칸 · `saveProjectLedgerAction`의 `revenue.contract`·`contractFxRateTouched` · `saveRevenue`의 계약 분기 · `updateProjectContract` · `projects.contract_*` 컬럼)를 지운다(PATTERNS Resolved Gaps — 별도 「매출 계약 쓰기」 액션은 없고 `saveProjectLedgerAction` 한 곳이다).

Purpose: 기준 5 「매출 칸의 발행액은 공급가액으로 적고 부가세·합계는 서버가 계산」(04-08이 문구 정정)과 PROJ-03(파생 계약 금액). 04-17·04-18의 목록 매출 열·기준이 D-85의 발행액 공개를 전제한다.

Output: 파생 계약 금액 · 계약 금액 쓰기 경로 제거 + 마이그레이션 0014 · 발행액 공개 · 통합 3 갱신 · E2E 갱신.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 763–786행(§4 D-84 제거 범위 · §5 D-85 노출 전환과 소급 Gap)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 402행(saveProjectLedgerAction) · 420행(revenue-section 배정) · 492–494행(표 단위 노출)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 473–482행(S6 상태) · 1196–1225행(S6 적용 규칙) · 329행·358행·380–385행(Copywriting)
</context>

<assumption_delta_decision>
- **신호:** 입력하던 값(계약 금액)이 파생값이 됐다 — 「chosen → derived」의 역방향.
- **주 명사:** 고객 승인된 현재 차수의 견적 합계.
- **결정: promote.** 파생값을 유일한 계약 금액으로 올리고 저장 컬럼·입력 경로를 **지운다**. 컬럼을 남겨 두고 파생값을 곁에 보이는 add-alongside는 「어느 쪽이 계약 금액인가」를 다시 열어 두는 일이라 택하지 않는다. 지금 행은 전부 `source='demo'`이고(Eng OV-1) 값은 파생으로 대체되므로 잃는 업무 데이터가 없다.
</assumption_delta_decision>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-03**이다. 프로브는 옛 문구(「계약 금액 … 을 기록한다」)로 돌았으므로 수용 기준은 결정된 D-84·D-85와 UI-SPEC S6으로 끌어왔다(04-08이 요구사항 문구를 고친다). S6 `## UI Considerations`의 overflow·long-text backstop은 마커로, empty·loading·error·populated·partial·zero-one-many는 truths로 옮겼다(U-3의 「EMPTY와 합계 행 공존」은 04-02 동작 그대로).

**계획 단계 가정(표면화):**
- **컬럼 삭제(계획 재량):** D-84 원문은 「제거하거나 파생 표시로 바꾼다」. 이 플랜은 **삭제**한다(위 결정). 배포 순간 옛 리비전이 잠깐 옛 컬럼을 읽을 수 있는 창이 있다(스테이징, 수십 초) — 위협 목록에 accept로 적었다.
- **D-85의 기존 DB 반영:** 정보 노출표는 계급별 저장 행이다. 시드(`seedMasterData`)가 기본 계급(기획 PM)의 행을 `staffDefault`로 upsert하므로 기본값을 바꾸고 시드를 다시 돌리면 기존 DB에도 반영된다 — 별도 데이터 마이그레이션을 만들지 않는다. 대신 관리자가 기획 PM 행을 손으로 바꿔 둔 운영 DB에서는 시드가 그 값을 덮는다는 기존 시드 규약이 그대로 적용된다(이 플랜이 새로 만드는 동작이 아니다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 계약 금액이 고객 승인된 현재 차수 합계로 보인다(도메인 파생 → DTO → 매출 섹션 한 줄 → E2E)</name>
  <files>domain/revenue/index.ts, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, test/integration/revenue-entries.test.ts, test/e2e/revenue-section.spec.ts</files>
  <read_first>
    - `domain/revenue/index.ts`(전문 350행) — `ContractInfo`·`listRevenue`의 계약 계산(176–183행)·`computeVat`·`REVENUE_DTO_SPEC`·`saveRevenue`의 계약 분기
    - `domain/quotes/revisions.ts`(04-14) — 현재 차수·승인 표시 · `repositories/quote-lines.ts`(현재 차수 합계에 쓸 조회)
    - `app/(app)/projects/[id]/revenue-section.tsx`(전문 305행) — 계약 금액 `Form.Field`와 통화·환율 칸
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `contractDraft`·`updateContract`·`contractVat`·`canWriteContract`를 찾아 범위만 Read
    - `ui/kv-list/KvList.tsx`(전문)
    - `test/integration/revenue-entries.test.ts`(전문) · `test/e2e/revenue-section.spec.ts`(전문)
  </read_first>
  <behavior>
    - 현재 차수(2차) 승인 · 견적 합계 48,000,000 → `contract = { amountKrw: 48000000, vatKrw: 4800000, totalKrw: 52800000, source: "2차 고객 승인 합계" }`
    - 3차를 만들어 현재가 미승인 → `contract = { amountKrw: null, pending: "3차 고객 승인 전" }` — 2차 합계를 쓰지 않는다
    - 현재 차수에 견적 외 비용·조정 줄이 있어도 계약 금액은 견적가 합계라 변하지 않는다(두 종류 모두 견적가 0)
    - 보관된 줄은 합계에서 빠진다
  </behavior>
  <action>
① `domain/revenue/index.ts`: `ContractInfo`를 파생형으로 바꾼다 — `{ amountKrw: number | null; vatKrw: number | null; totalKrw: number | null; sourceLabel: string | null; pendingLabel: string | null }`. `listRevenue`가 현재 차수(최신 순번)와 승인 표시를 읽어 승인이면 보관 제외 줄의 `quote_amount_krw` 합(리포지토리 SUM 한 번 — 04-05 목록 합계와 같은 결)과 `computeVat`(기존 세금 설정 경로)로 채우고 `{n}차 고객 승인 합계`, 미승인이면 금액 셋 null + `{n}차 고객 승인 전`. **이전 승인 차수를 찾는 코드를 두지 않는다.** 결정표 통합 테스트(`behavior`)를 먼저 쓴다(RED — 이 파일의 기존 계약 금액 쓰기 케이스는 Task 2에서 지운다).

② `revenue-section.tsx`: 계약 금액 `Form.Field`(금액·통화·환율)를 지우고 `KvList` 한 줄 — 라벨 `계약 금액` · 값(`--fs-base` 700 `tabular-nums`, `formatKrw`) 또는 `—` · 2행 `--fs-sm --muted`(`부가세 10% {vat} · 합계 {total} · {sourceLabel}` 또는 `{pendingLabel}`). 부가세 비율 글자는 세금 설정에서 계산된 값의 표시이고 화면이 곱하지 않는다(DTO가 비율 문자열을 함께 싣게 하거나 금액만 보인다 — 선택을 SUMMARY에 적는다). 통화·환율 칸이 없다.

③ `quote-table.tsx`·`[id]/page.tsx`: 계약 금액 draft 상태·dirty 합류·저장 페이로드의 `revenue.contract`·`canWriteContract`·계약 부가세 props를 지운다. 1차 「일괄 저장」 렌더 조건을 「편집 가능한 견적 줄 셀이 하나라도 있음(`cellEditability`) · 발행·입금 쓰기 · 기간 칸 권리(04-22)」 중 하나로 바꾼다(U-5).

④ E2E(`revenue-section.spec.ts` 갱신): 계약 금액 입력 단계를 지우고 — 담당 PM이 줄 두 개를 저장하고 고객 승인 표시(04-24의 모달 — 서버는 04-14) → 매출 섹션 `계약 금액`이 견적 합계와 같다 → 새 차수를 만들면 `—` + `2차 고객 승인 전`.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 파생 계약 금액이 현재 차수 승인 규칙과 다르다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ContractInfo`에 입력용 필드가 없고 파생 필드만 있다
    - 미승인 현재 차수에서 이전 승인 차수 합계를 쓰지 않는다는 케이스가 통합 테스트에 있다(금지 항목)
    - `revenue-section.tsx`에 계약 금액 입력 요소(`id="contract-amount"`)가 없다
    - `quote-table.tsx`에 계약 금액 draft·dirty 합류가 없다
    - 1차 버튼 렌더 조건이 편집 가능 칸 존재로 판정된다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>매출 섹션의 계약 금액이 고객 승인된 현재 차수 합계로만 보이고 화면에 입력 칸이 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 계약 금액 쓰기 경로 전부 제거 + [BLOCKING] 컬럼 삭제 마이그레이션</name>
  <files>domain/revenue/index.ts, domain/projects/index.ts, domain/projects/ledger.ts, app/(app)/projects/actions.ts, repositories/projects.ts, db/schema/projects.ts, db/migrations/0014_drop_project_contract_columns.sql, db/migrations/meta/_journal.json, db/migrations/meta/0014_snapshot.json, test/integration/revenue-entries.test.ts, test/integration/custom-fields.test.ts</files>
  <reversibility rating="costly">컬럼 삭제는 데이터 손실이지만 현재 행은 전부 demo이고(Eng OV-1) 값은 파생으로 대체된다 — 되돌리려면 컬럼 네 개를 다시 더하는 마이그레이션 하나와 입력 경로 복원이 필요하다.</reversibility>
  <read_first>
    - `db/schema/projects.ts`(전문) · `db/schema/money-columns.ts`(전문)
    - `db/migrations/0010_revenue_entries.sql`(전문 — 이 컬럼을 더한 문장과 `DEFAULT 0` 사연, **고쳐 쓰지 않는다**)
    - `repositories/projects.ts` 223–302행(`insertProject`·`updateProjectContract`)
    - `domain/projects/index.ts` 244–290행(`createProject`의 contract 기본값)
    - `domain/revenue/index.ts` 223–350행(`SaveRevenueInput`·`saveRevenue` 계약 분기)
    - `app/(app)/projects/actions.ts` 90–115행(`revenue` 스키마)
    - `test/integration/custom-fields.test.ts` — contract 참조 줄(Grep)
  </read_first>
  <behavior>
    - `saveProjectLedgerAction`에 `revenue.contract`를 실어 보내면 zod 스키마에 필드가 없어 파싱되지 않는다(무시) — 그리고 어떤 경로로도 계약 금액이 저장되지 않는다
    - `grep` 수준: `app/`·`domain/`·`repositories/`·`db/schema/`에 계약 금액 컬럼·함수 참조가 0이다(스캔 테스트)
  </behavior>
  <action>
① `SaveRevenueInput`에서 `contract`·`contractFxRateTouched`를, `saveRevenue`에서 계약 분기를, `app/(app)/projects/actions.ts` `saveProjectLedgerAction`의 `revenue` 스키마에서 같은 두 필드를 지운다. `domain/projects/ledger.ts`가 그 필드를 넘기던 곳이 있으면 지운다. `repositories/projects.ts`의 `updateProjectContract`·`ProjectContractUpdateInput`과 `insertProject`의 계약 컬럼 값, `domain/projects/index.ts` `createProject`의 계약 기본값을 지운다(내 변경이 만든 고아만 정리 — CLAUDE.md).

② **[BLOCKING] 마이그레이션.** `db/schema/projects.ts`에서 `...moneyColumns("contract")`와 그 주석을 지우고 `pnpm db:generate --name drop_project_contract_columns`(**번호 규칙:** 계획 번호는 0014이고, 생성기가 매기는 번호는 적용 직전 journal의 최고 `idx` + 1이어야 한다 — 04-13의 `line_kind` 뒤. 생성기가 다른 번호를 주면 파일명·snapshot·journal tag를 생성기 출력 그대로 두고 실제 이름을 SUMMARY에 적는다 — 아래 번호 검증 명령이 본다). 생성 SQL이 네 컬럼 `DROP COLUMN`인지 읽어 확인하고 앞에 락 타임아웃 한 쌍. 0010은 고쳐 쓰지 않는다. `pnpm lint:sql`(`Found 0 issues` — DROP COLUMN 경고가 나면 SQL 쪽에서 해결하고 `.squawk.toml`은 고치지 않는다) → `bash scripts/dev-db.sh && pnpm db:migrate`.

③ 테스트: `revenue-entries.test.ts`의 계약 금액 쓰기 케이스를 지우고(Task 1이 파생 케이스를 더했다) `behavior` 첫 줄(스키마 무시)을 더한다. `custom-fields.test.ts`의 계약 참조를 고친다. 스캔 단언(계약 금액 컬럼 접두어·함수 이름이 `app/`·`domain/`·`repositories/`·`db/schema/`에 없다)을 `revenue-entries.test.ts` 안 describe 하나로 둔다 — 마이그레이션 파일은 스캔하지 않는다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- db/migrations/0010_revenue_entries.sql</automated>
    <fails_when>db:migrate가 실패하거나(`error:` 줄), diff 출력이 비어 있지 않다 — 0010을 고쳐 썼다</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_drop_project_contract_columns'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/custom-fields.test.ts test/integration/projects-list.test.ts test/integration/project-copy.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 컬럼 제거가 등록·목록·복사 경로를 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 `contract_currency`·`contract_foreign_amount`·`contract_fx_rate`·`contract_amount_krw` 네 컬럼을 지우고 락 타임아웃 한 쌍으로 시작한다
    - 0010 diff가 0줄이고 `.squawk.toml` diff가 0줄이다
    - 새 마이그레이션의 번호 = 이 플랜 실행 직전 journal 최고 `idx` + 1이고, 파일명·`meta/{번호}_snapshot.json`·journal tag가 생성기가 실제로 만든 이름과 같다(번호 검증 명령) — 계획 번호 0014와 다르면 실제 이름이 SUMMARY에 있다
    - `saveProjectLedgerAction` 스키마와 `SaveRevenueInput`에 계약 필드가 없다
    - `updateProjectContract`가 없다
    - 계약 금액 참조 스캔 describe가 초록이다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>계약 금액을 입력하는 길이 화면·액션·도메인·리포지토리·DB 어디에도 남지 않았다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 발행액 PM 공개 · 입금액 숨김 유지(D-85) + 역산 경로 점검 + 화면 검증</name>
  <files>domain/permissions/info-items.ts, domain/revenue/index.ts, app/(app)/projects/[id]/revenue-section.tsx, test/integration/revenue-entries.test.ts, test/integration/visibility.test.ts, test/e2e/revenue-section.spec.ts</files>
  <read_first>
    - `domain/permissions/info-items.ts` 45–56행(발행·입금 항목과 주석)
    - `domain/seed/index.ts` 160–180행(기본 계급 노출 행 upsert)
    - `domain/revenue/index.ts` 95–170행(`RevenueDto`·`REVENUE_DTO_SPEC` — 각 필드의 정보 항목. **`balanceKrw` 같은 파생 필드가 어느 항목으로 게이트되는지 확인한다**)
    - `test/integration/revenue-entries.test.ts`의 (d) 케이스 · `test/integration/visibility.test.ts`(시드 뒤 노출 판정 형태)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1211–1225행(발행 표 PM 읽기 · 입금 표 부재 · 부제)
  </read_first>
  <behavior>
    - 기획 PM viewer의 `listRevenue` → `issuedEntries` 있음 · `paidEntries` 키 없음 · 입금에서 파생된 모든 필드(미수·잔액) 키 없음
    - 경영관리(두 항목 공개) → 셋 다 있음
    - 기존 DB에서 시드를 다시 돌리면 기획 PM의 `revenue.issued_amount` 노출 행이 true가 된다
  </behavior>
  <action>
① `info-items.ts`의 `revenue.issued_amount` `staffDefault`를 true로 바꾸고 주석을 D-85로 고친다(입금액은 false 그대로).

② **역산 경로 점검.** `REVENUE_DTO_SPEC`의 모든 필드를 입금 의존 여부로 나눠, 입금 합계·미수·초과 입금·잔액처럼 입금 값에서 파생되는 필드가 전부 `revenue.paid_amount`(또는 둘 다를 요구하는 방식)로 게이트되게 고친다 — 발행 항목만으로 게이트된 입금 파생 필드가 있으면 그것이 이 플랜의 누수다(금지 항목). 판단 표를 SUMMARY에 적는다.

③ `revenue-section.tsx`: 쓰기 권한이 없는 사람에게 발행 표는 흰 머리글 캡션 읽기 표(편집 셀 0 — `ui/table` 자동)이고 추가 버튼이 없으며 EMPTY `발행한 세금계산서가 없습니다 · 발행은 경영관리`. 입금 표는 DTO에 필드가 없으면 섹션에서 통째로 렌더하지 않는다. 부제는 입금 표가 있을 때만 `공급가액 기준 · 입금액만 통장 합계`, 없으면 `공급가액 기준`.

④ 테스트: `revenue-entries.test.ts` (d)의 기대값을 「발행은 옴 · 입금은 필드 부재 · 입금 파생 필드 부재」로 **고친다**(지우지 않는다). `visibility.test.ts`에 `behavior` 셋째 줄(시드 재실행 뒤 기획 PM 행 true). E2E: 기획 PM으로 발행 줄이 있는 프로젝트를 열면 발행 표가 읽기 표로 보이고 입금 표·미수 표시가 DOM에 없다 · 발행 줄 0이면 `· 발행은 경영관리`.

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S6 backstop 둘(375 발행·입금 표·계약 금액 2행 · 긴 메모 말줄임)을 DOM 실측 → 수정 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/visibility.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — PM 응답에 입금 또는 입금 파생 필드가 남았거나 시드 반영이 안 됐다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S6 판정 두 개를 SUMMARY에서 확인</test>
      <expected>375에서 가로 스크롤 0 · 숫자 꺾임 없음, 긴 메모가 한 줄 말줄임</expected>
      <why_human>폭·말줄임은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `revenue.issued_amount`의 `staffDefault`가 true, `revenue.paid_amount`가 false다
    - PM 응답 키 집합 단언이 발행 있음 · 입금 부재 · 입금 파생 필드 부재를 모두 본다(통합 d)
    - 입금 파생 필드 게이트 판단 표가 SUMMARY에 있다
    - (d) 케이스가 삭제되지 않고 기대값만 바뀌었다
    - 시드 재실행 반영 케이스가 있다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>PM이 발행액을 읽기 표로 보고, 입금액과 그 파생값은 PM 응답 어디에도 없다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain 출구(RevenueDto) → 화면 | 발행·입금 필드가 정보 노출표를 지나야 실린다. 파생 필드가 역산 통로가 될 수 있다 |
| 브라우저 → 일괄 저장 | 옛 계약 금액 필드를 실어 보내는 클라이언트 |
| 마이그레이션 → 운영 중 리비전 | 컬럼 삭제와 옛 리비전의 읽기가 겹치는 짧은 창 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-83 | Information Disclosure | 입금액 역산 | high | mitigate | 입금 표와 입금 파생 필드(미수·초과 입금·잔액)를 전부 `revenue.paid_amount`로 게이트하고, PM 응답 키 집합에서 부재를 통합 테스트로 단언한다(금지 항목). D-38 누수 스캔 레지스트리가 DTO 필드별 항목을 덮는다 |
| T-04-84 | Tampering | 옛 계약 금액 필드 | medium | mitigate | 액션 스키마에서 필드를 지워 파싱되지 않고, 도메인·리포지토리 쓰기 함수와 컬럼이 없어 저장 경로 자체가 없다 |
| T-04-85 | Tampering | 파생 계약 금액 | medium | mitigate | 서버가 현재 차수·승인·합계·부가세를 계산하고 화면은 표시만 한다. 이전 승인 차수로 대신하지 않는다(금지 항목) |
| T-04-86 | Denial of Service | 컬럼 삭제 배포 창 | low | accept | 마이그레이션 Job이 새 리비전 트래픽 전환 전에 돌 때 옛 리비전이 옛 컬럼을 읽으면 수십 초 오류가 날 수 있다. 스테이징·사내 10~30명·전환 전 demo 데이터라 받아들이고, 배포 기록에 한 줄 남긴다 |
| T-04-87 | Information Disclosure | 노출표 기본값 반영 | low | mitigate | 시드 upsert가 기본 계급 행에만 적용되는 기존 규약을 그대로 쓰고, 시드 재실행 반영을 통합 테스트로 고정한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`, 0010·`.squawk.toml` diff 0줄, 마이그레이션 번호 검증 명령 초록
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 계약 금액 참조 스캔 초록
</verification>

<success_criteria>
- 계약 금액이 고객 승인된 현재 차수의 견적 합계에서만 나오고 입력 경로가 없다
- PM이 발행액을 보고 입금액과 그 파생값은 보지 못한다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/0014_drop_project_contract_columns.sql`(계획 번호 — 실제 번호는 생성기 기준, 직전 최고 + 1) + `meta/0014_snapshot.json` + `_journal.json` 항목 |
| 컬럼(삭제) | `projects.contract_currency` · `contract_foreign_amount` · `contract_fx_rate` · `contract_amount_krw` |
| 타입(변경) | `ContractInfo` — `amountKrw \| null` · `vatKrw \| null` · `totalKrw \| null` · `sourceLabel` · `pendingLabel` |
| 삭제된 심볼 | `updateProjectContract` · `ProjectContractUpdateInput` · `SaveRevenueInput.contract`·`contractFxRateTouched` · `saveProjectLedgerAction` 스키마 `revenue.contract`·`contractFxRateTouched` · `QuoteLedger` 계약 draft·props |
| 정보 노출 기본값 | `revenue.issued_amount` staffDefault false → **true** |
| 변경된 기존 심볼 | `listRevenue`(파생 계약) · `RevenueSection`(KvList 계약 한 줄 · 발행 읽기 표 · 입금 표 조건 렌더 · 부제) · 1차 저장 렌더 조건 |
| 테스트 | `revenue-entries.test.ts`(파생·스키마 무시·스캔·(d) 기대값) · `custom-fields.test.ts` · `visibility.test.ts` · `revenue-section.spec.ts` 갱신 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-16-SUMMARY.md` when done
</output>
