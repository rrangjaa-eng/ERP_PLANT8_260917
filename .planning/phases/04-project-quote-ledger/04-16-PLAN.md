---
phase: 04-project-quote-ledger
plan: 16
type: execute
wave: 20
depends_on: ["04-15"]
files_modified:
  - domain/revenue/index.ts
  - domain/permissions/info-items.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - test/integration/revenue-entries.test.ts
  - test/integration/contract-invariant.test.ts
  - test/integration/visibility.test.ts
  - test/e2e/revenue-section.spec.ts
autonomous: true
requirements: [PROJ-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "계약 금액 입력 칸이 없다 — 매출 섹션의 계약 금액은 고객 승인이 표시된 **현재 차수**의 견적 합계(공급가)이고, `KvList` 한 줄 `계약 금액` `48,000,000` + 2행 `부가세 10% 4,800,000 · 합계 52,800,000 · 2차 고객 승인 합계`로 보인다(D-84, UI-SPEC S6, Copywriting 표시 — 계약 금액(파생))"
    - "현재 차수가 미승인이면(새 차수를 만든 직후 등) 계약 금액은 `—` + 2행 `{n}차 고객 승인 전`이다 — 이전 승인 차수의 합계로 대신하지 않는다(D-84 「현재 차수」)"
    - "계약 금액의 부가세·합계는 서버가 `domain/money`와 세금 설정으로 계산한다 — 화면은 계산하지 않는다(PNL-09, 기준 5). 부가세 기준일은 그 차수의 **승인일**이다 — 세율 설정이 바뀌어도 이미 승인된 계약의 부가세가 오늘 세율로 흔들리지 않는다(CEO 리뷰 B-27)"
    - "계약 금액의 견적 합계는 리포지토리 SUM 한 번이고 JS 숫자로 온다(`::bigint` 뒤 숫자 변환) — 부가세 계산이 문자열 이어 붙이기가 되지 않고 21억 원을 넘어도 정확하다(CEO 리뷰 B-18)"
    - "계약 금액 = 승인된 현재 차수의 견적 합계가 **불변**이다 — 승인 뒤 수량·단가·상태 변경, 견적가 있는 새 줄·보관·복원이 전부 거부되므로(04-40) 어떤 저장·복원 시도 뒤에도 계약 금액이 승인 시점 값 그대로이고, 실행가 변경은 계약 금액을 바꾸지 않는다. 바꾸려면 승인 표시를 끄거나 새 차수다(사용자 D7 답의 약속, CEO 리뷰 OV-1 — 불변식 통합 테스트)"
    - "계약 금액 필드는 정보 항목 `quote.amount`로 게이트된다 — 값이 견적 합계이므로, 견적 금액을 숨긴 계급은 `project.value`가 보여도 계약 금액 키를 받지 않는다(CEO 리뷰 B-19)"
    - "화면·저장 페이로드에 계약 금액 입력이 없다 — 네고로 금액이 바뀌면 새 차수로 반영한다(D-84, 옛 D-57의 「계약 금액 PM 쓰기」 폐기). 서버의 쓰기 경로(액션 필드 · 도메인 · 리포지토리)와 `projects.contract_*` 컬럼 제거는 바로 뒤 04-41이다"
    - "매출 섹션의 1차 저장 합류(dirty N)에서 계약 금액 칸이 빠지고, 저장 버튼 렌더 조건이 「편집할 수 있는 칸이 구조적으로 하나라도 있는가」(견적 줄 셀 · 발행·입금 쓰기 · 기간 칸)다(U-5, S3)"
    - "세금계산서 발행액은 기획본부(PM)에게 기본 공개다 — PM에게 발행 줄 표가 흰 머리글 읽기 표(캡션)로 오고, 줄이 0이면 `발행한 세금계산서가 없습니다 · 발행은 경영관리`(버튼 없음)다(D-85, S6, Copywriting)"
    - "PM도 서버가 계산한 발행 합계(`issuedTotalKrw`)를 받는다 — 발행 합계는 입금에서 파생되지 않으므로 발행 항목(`revenue.issued_amount`)으로 게이트하고, 잔액·입금 합계만 입금 항목(`revenue.paid_amount`)에 남는다(CEO 리뷰 B-28)"
    - "입금액은 PM에게 기본 숨김을 유지한다 — 입금 줄 표가 서버에서 오지 않고(빈 배열이 아니라 필드 부재), 입금 표 합계 행의 미수·초과 입금 표시도 함께 없으며, 입금액을 역산할 수 있는 다른 필드(잔액 등)도 오지 않는다(D-85, S6 partial)"
    - "PM에게 매출 섹션 부제는 `공급가액 기준`만이고, 입금 표를 받는 사람에게는 `공급가액 기준 · 입금액만 통장 합계`다(Copywriting PNL-09 표시)"
    - "정보 노출표 기본값 변경은 새로 시드되는 기획 PM 계급과, 시드를 다시 돌린 기존 DB의 기획 PM 계급 행에 적용된다(시드가 기본 계급 행을 upsert한다 — RESEARCH §5 소급 여부 Gap 해소). 팀장·본부 책임자 계급 행은 시드가 건드리지 않아 발행액 기본 숨김이 남는다 — 관리자가 노출표에서 켠다(CEO 리뷰 B-29, 관리자 확인 항목)"
    - statement: "발행·입금 표(열 셋)와 계약 금액 2행이 375px에서 가로 스크롤 0이고 숫자가 꺾이지 않는다 — DOM 감사"
      verification: backstop
    - statement: "발행·입금 표의 메모 셀이 PC에서 한 줄 말줄임이고 행 높이를 늘리지 않는다 — 긴 메모 DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "입금액이 기획본부에게 역산 경로로 새어서는 안 된다. 발행액을 공개한 뒤 미수·초과 입금·잔액 같은 파생값이 하나라도 PM 응답에 남으면 발행액 − 미수 = 입금액이 된다 — 입금 표를 숨기는 것은 표 단위 필드 부재이고, 그 표에서 파생된 값도 같이 빠져야 한다"
      status: resolved
      verification: test
    - statement: "계약 금액이 이전 승인 차수의 합계로 조용히 대신 채워져서는 안 된다. 현재 차수가 미승인이면 계약이 아직 확정되지 않은 것이고, 옛 합계를 보이면 PM과 경영관리가 서로 다른 계약 금액을 믿게 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/revenue/index.ts"
      provides: "파생 계약 금액(현재 차수 승인 여부 · 견적 합계 · 승인일 기준 부가세·합계 · 출처 문구, `quote.amount` 게이트)"
      contains: "고객 승인"
    - path: "test/integration/contract-invariant.test.ts"
      provides: "승인 뒤 모든 합계 변경 시도가 거부되고 계약 금액이 승인값 그대로 · 실행가 변경 무영향 · 승인 해제·새 차수 뒤 값"
      min_lines: 50
    - path: "domain/permissions/info-items.ts"
      provides: "revenue.issued_amount 기본 공개 · revenue.paid_amount 기본 숨김"
      contains: "revenue.issued_amount"
  key_links:
    - from: "domain/revenue/index.ts"
      to: "domain/quotes/revisions.ts"
      via: "계약 금액이 현재 차수(최신 순번)와 그 승인 표시에서 파생된다"
      pattern: "customerApprovedAt"
    - from: "app/(app)/projects/[id]/revenue-section.tsx"
      to: "domain/revenue/index.ts"
      via: "계약 금액이 입력이 아니라 DTO의 파생 값 `KvList` 한 줄이다"
      pattern: "KvList"
    - from: "domain/permissions/info-items.ts"
      to: "domain/seed/index.ts"
      via: "기본 계급의 노출 행이 staffDefault로 시드된다"
      pattern: "staffDefault"
    - from: "domain/revenue/index.ts"
      to: "repositories/quote-lines.ts"
      via: "승인된 현재 차수의 견적 합계를 숫자 SUM 한 번으로 읽는다"
      pattern: "mapWith\\(Number\\)|sumQuoteAmount"
---

<objective>
**As a** 기획 PM과 경영관리, **I want to** 계약 금액을 따로 적지 않고 고객이 승인한 현재 차수의 견적 합계로 보고, PM도 세금계산서 발행액을 보되 입금액은 경영관리만 보기를, **so that** 계약 금액이 견적과 어긋나는 일이 구조적으로 없고, PM은 발행 진행을 알되 입금 사정은 경영관리 정보로 남는다.

이 플랜은 04-02가 만든 매출 섹션을 D-84·D-85로 고친다. 옛 D-57의 「계약 금액은 PM」과 「발행액 기획본부 기본 숨김」 두 부분은 **대체됐다** — 이 플랜은 계약 금액 입력의 모든 경로(화면 칸 · `saveProjectLedgerAction`의 `revenue.contract`·`contractFxRateTouched` · `saveRevenue`의 계약 분기 · `updateProjectContract` · `projects.contract_*` 컬럼)를 지운다(PATTERNS Resolved Gaps — 별도 「매출 계약 쓰기」 액션은 없고 `saveProjectLedgerAction` 한 곳이다).

Purpose: 기준 5 「매출 칸의 발행액은 공급가액으로 적고 부가세·합계는 서버가 계산」(04-08이 문구 정정)과 PROJ-03(파생 계약 금액). 04-17·04-18의 목록 매출 열·기준이 D-85의 발행액 공개를 전제한다.

Output: 파생 계약 금액(승인일 기준 부가세 · 숫자 SUM · `quote.amount` 게이트) · 화면의 계약 금액 입력 제거 · 승인 합계 불변식 테스트 · 발행액 공개 · 통합 3 갱신 · E2E 갱신.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` T4·T14·OV-1·OV-6·부록 B).** 옛 04-16은 17파일이라 **둘로 나눴다.** 이 플랜은 계약 금액을 **읽는 쪽**(파생 · 화면)과 D-85 노출을 맡고, **04-41**(바로 뒤)이 계약 금액 쓰기 경로와 `projects.contract_*` 컬럼을 지운다(사용자 D9 — RAISE 가드 · ARCHITECTURE §5 예외 기록 · 「Phase 4는 PR #38 한 번에 배포」 제약 · 0010 상태 업그레이드 테스트). 이 플랜에 들어온 사실 정정: B-18(SUM 숫자) · B-19(계약 금액은 `quote.amount`로 게이트) · B-27(부가세 기준일 = 승인일) · B-28(발행 합계는 발행 항목으로 게이트) · B-29(팀장 행은 시드 밖 — 관리자 확인) · OV-1(계약 금액 = 승인 합계 불변식 테스트 — 사용자 D7의 약속, 잠금은 04-40).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 763–786행(§4 D-84 제거 범위 · §5 D-85 노출 전환과 소급 Gap)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 402행(saveProjectLedgerAction) · 420행(revenue-section 배정) · 492–494행(표 단위 노출)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 473–482행(S6 상태) · 1196–1225행(S6 적용 규칙) · 329행·358행·380–385행(Copywriting)
</context>

<assumption_delta_decision>
- **신호:** 입력하던 값(계약 금액)이 파생값이 됐다 — 「chosen → derived」의 역방향.
- **주 명사:** 고객 승인된 현재 차수의 견적 합계.
- **결정: promote.** 파생값을 유일한 계약 금액으로 올리고 저장 컬럼·입력 경로를 **지운다**. 컬럼을 남겨 두고 파생값을 곁에 보이는 add-alongside는 「어느 쪽이 계약 금액인가」를 다시 열어 두는 일이라 택하지 않는다. 지금 행은 전부 `source='demo'`이고(Eng OV-1) 값은 파생으로 대체되므로 잃는 업무 데이터가 없다. 이 결정은 사용자 D9(「지금 삭제 + 가드」)로 확정됐고, 삭제는 04-41이 한다.
</assumption_delta_decision>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-03**이다. 프로브는 옛 문구(「계약 금액 … 을 기록한다」)로 돌았으므로 수용 기준은 결정된 D-84·D-85와 UI-SPEC S6으로 끌어왔다(04-08이 요구사항 문구를 고친다). S6 `## UI Considerations`의 overflow·long-text backstop은 마커로, empty·loading·error·populated·partial·zero-one-many는 truths로 옮겼다(U-3의 「EMPTY와 합계 행 공존」은 04-02 동작 그대로).

**계획 단계 가정(표면화):**
- **컬럼 삭제(사용자 D9로 확정 — 04-41):** D-84 원문은 「제거하거나 파생 표시로 바꾼다」. 삭제는 04-41이 RAISE 가드와 함께 하고, 배포 창 위험(옛 리비전이 옛 컬럼을 읽는 창)은 「Phase 4는 한 PR로 한 번에 배포」 전제 아래에서만 무해하다 — 그 기록도 04-41이다.
- **부가세 기준일(B-27):** `listRevenue`는 지금 계약 부가세를 `new Date()`로 계산한다. 승인된 계약의 부가세는 승인일 기준이어야 세율 설정 변경이 과거 계약을 흔들지 않는다 — 승인일(KST 00:00 순간, 04-14)을 `asOf`로 넘긴다.
- **D-85의 기존 DB 반영:** 정보 노출표는 계급별 저장 행이다. 시드(`seedMasterData`)가 기본 계급(기획 PM)의 행을 `staffDefault`로 upsert하므로 기본값을 바꾸고 시드를 다시 돌리면 기존 DB에도 반영된다 — 별도 데이터 마이그레이션을 만들지 않는다. 대신 관리자가 기획 PM 행을 손으로 바꿔 둔 운영 DB에서는 시드가 그 값을 덮는다는 기존 시드 규약이 그대로 적용된다(이 플랜이 새로 만드는 동작이 아니다). 시드는 시스템 관리자·기획 PM 두 계급의 노출 행만 쓰므로 팀장·본부 책임자는 발행액이 기본 숨김으로 남는다 — 관리자가 노출표에서 켤지 확인할 항목으로 SUMMARY에 적는다(CEO 리뷰 B-29, ALREADY-COVERED — T-04-87). 04-20이 권한 시드를 「없을 때만 넣기」로 바꾼 것(CEO 리뷰 A-05)은 권한 행 이야기이고 노출 행 upsert는 바뀌지 않았다 — 04-20-SUMMARY에서 확인하고, 노출 행도 바뀌었으면 위 「시드 재실행 반영」 케이스를 「새 DB에서만 반영」으로 고친 뒤 SUMMARY에 적는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 계약 금액이 고객 승인된 현재 차수 합계로 보인다(도메인 파생 → DTO → 매출 섹션 한 줄 → E2E)</name>
  <files>domain/revenue/index.ts, repositories/quote-lines.ts, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, test/integration/revenue-entries.test.ts, test/e2e/revenue-section.spec.ts</files>
  <read_first>
    - `domain/revenue/index.ts`(전문 350행) — `ContractInfo`·`listRevenue`의 계약 계산(176–183행 — 부가세 기준일이 `new Date()`)·`computeVat`·`REVENUE_DTO_SPEC`(131행 `contract` → `project.value`)·`saveRevenue`의 계약 분기
    - `domain/quotes/revisions.ts`(04-14) — 현재 차수·승인 표시·승인일(KST 00:00 순간) · `repositories/quote-lines.ts`(현재 차수 합계에 쓸 조회)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-18·B-19·B-27 절(Grep `### B-18`·`### B-19`·`### B-27`)
    - `app/(app)/projects/[id]/revenue-section.tsx`(전문 305행) — 계약 금액 `Form.Field`와 통화·환율 칸
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `contractDraft`·`updateContract`·`contractVat`·`canWriteContract`를 찾아 범위만 Read
    - `ui/kv-list/KvList.tsx`(전문)
    - `test/integration/revenue-entries.test.ts`(전문) · `test/e2e/revenue-section.spec.ts`(전문)
  </read_first>
  <behavior>
    - 현재 차수(2차) 승인 · 견적 합계 48,000,000 → `contract = { amountKrw: 48000000, vatKrw: 4800000, totalKrw: 52800000, source: "2차 고객 승인 합계" }` · 세 금액 모두 `typeof === "number"`
    - 견적 줄 둘이 1,500,000,000씩(합 3,000,000,000)이고 승인 → `amountKrw` 3000000000(숫자), 부가세·합계가 숫자 덧셈 결과(B-18)
    - 3차를 만들어 현재가 미승인 → `contract = { amountKrw: null, pending: "3차 고객 승인 전" }` — 2차 합계를 쓰지 않는다
    - 현재 차수에 견적 외 비용·조정 줄이 있어도 계약 금액은 견적가 합계라 변하지 않는다(두 종류 모두 견적가 0)
    - 보관된 줄은 합계에서 빠진다
    - 승인일 2026-01-10에 승인된 계약: 세율 설정에 2026-06-01부터 새 세율 행을 더해도 계약 부가세는 승인일 세율로 계산된다(B-27)
    - `quote.amount`를 숨기고 `project.value`는 보이는 계급 → 응답에 `contract` 키가 없다(B-19)
  </behavior>
  <action>
① `domain/revenue/index.ts`: `ContractInfo`를 파생형으로 바꾼다 — `{ amountKrw: number | null; vatKrw: number | null; totalKrw: number | null; sourceLabel: string | null; pendingLabel: string | null }`. `listRevenue`가 현재 차수(최신 순번)와 승인 표시를 읽어 승인이면 보관 제외 줄의 `quote_amount_krw` 합과 `computeVat`(기존 세금 설정 경로 — **`asOf` = 그 차수의 승인일**, CEO 리뷰 B-27)로 채우고 `{n}차 고객 승인 합계`, 미승인이면 금액 셋 null + `{n}차 고객 승인 전`. **이전 승인 차수를 찾는 코드를 두지 않는다.** `REVENUE_DTO_SPEC`의 `contract` 필드 정보 항목을 `project.value`에서 `quote.amount`로 바꾼다(B-19 — 값이 견적 합계다). 결정표 통합 테스트(`behavior`)를 먼저 쓴다(RED). 이 파일의 기존 계약 금액 케이스 중 「쓴 값을 `listRevenue`로 다시 읽는」 단언은 파생 규칙과 부딪히므로 이 태스크에서 지운다 — 서버 쓰기 경로 자체의 제거와 「옛 필드는 스키마가 무시」 케이스는 04-41이다(그때까지 옛 쓰기 분기는 컬럼에만 쓰고 아무 화면도 읽지 않는다).

①-b **합계 쿼리(B-18).** `repositories/quote-lines.ts`에 `sumQuoteAmountByRevision(viewer, revisionId, tx?)` — 보관 제외 `quote_amount_krw`의 `coalesce(sum(…), 0)::bigint`를 `sql<number>`에 `.mapWith(Number)`로 리포지토리 경계에서 숫자로 바꿔 돌려준다(node-pg는 bigint를 문자열로 준다 — 04-17 C-01과 같은 규칙). `computeVat`에는 이 숫자만 들어간다.

② `revenue-section.tsx`: 계약 금액 `Form.Field`(금액·통화·환율)를 지우고 `KvList` 한 줄 — 라벨 `계약 금액` · 값(`--fs-base` 700 `tabular-nums`, `formatKrw`) 또는 `—` · 2행 `--fs-sm --muted`(`부가세 10% {vat} · 합계 {total} · {sourceLabel}` 또는 `{pendingLabel}`). 부가세 비율 글자는 세금 설정에서 계산된 값의 표시이고 화면이 곱하지 않는다(DTO가 비율 문자열을 함께 싣게 하거나 금액만 보인다 — 선택을 SUMMARY에 적는다). 통화·환율 칸이 없다.

③ `quote-table.tsx`·`[id]/page.tsx`: 계약 금액 draft 상태·dirty 합류·저장 페이로드의 `revenue.contract`·`canWriteContract`·계약 부가세 props를 지운다. 1차 「일괄 저장」 렌더 조건은 04-22가 `[id]/page.tsx`에서 서버 계산하는 `canSave` 하나다 — 그 식에서 계약 칸 항 `(canWriteContract && status !== 완료)`를 지워(계약 입력이 없어져 죽은 항이다) 「편집 가능한 견적 줄 셀이 하나라도 있음(`cellEditability` — 04-30) · 발행·입금 쓰기 · 기간 칸 권리(04-22)」 중 하나가 되게 한다(U-5). 새 렌더 조건을 따로 만들지 않는다.

④ E2E(`revenue-section.spec.ts` 갱신): 계약 금액 입력 단계를 지우고 — 담당 PM이 줄 두 개를 저장하고 고객 승인 표시(04-24의 모달 — 서버는 04-14) → 매출 섹션 `계약 금액`이 견적 합계와 같다 → 새 차수를 만들면 `—` + `2차 고객 승인 전`.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 파생 계약 금액이 현재 차수 승인 규칙과 다르거나, 합계가 숫자가 아니거나, 부가세가 오늘 세율로 계산됐거나, `quote.amount`를 숨긴 계급에 계약 금액이 샜다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ContractInfo`에 입력용 필드가 없고 파생 필드만 있다
    - 미승인 현재 차수에서 이전 승인 차수 합계를 쓰지 않는다는 케이스가 통합 테스트에 있다(금지 항목)
    - 계약 부가세의 기준일이 승인일이다(통합 — 세율 행 추가 케이스, B-27)
    - `REVENUE_DTO_SPEC`의 `contract`가 `quote.amount`로 게이트되고 키 부재 케이스가 있다(B-19)
    - 합계 SUM이 리포지토리에서 숫자로 오고 30억 케이스가 정확하다(B-18)
    - `revenue-section.tsx`에 계약 금액 입력 요소(`id="contract-amount"`)가 없다
    - `quote-table.tsx`에 계약 금액 draft·dirty 합류가 없다
    - 1차 버튼 렌더 조건이 04-22의 `canSave` 하나이고 그 식에 `canWriteContract` 항이 없다(편집 가능 셀 · 발행·입금 쓰기 · 기간 권리)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>매출 섹션의 계약 금액이 고객 승인된 현재 차수 합계로만 보이고 화면에 입력 칸이 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 계약 금액 = 승인 합계 불변식(사용자 D7 · OV-1) — 승인 뒤 모든 합계 변경 시도·복원·실행가 변경·승인 해제·새 차수</name>
  <files>test/integration/contract-invariant.test.ts, domain/revenue/index.ts</files>
  <read_first>
    - `test/integration/quote-approved-lock.test.ts`(04-40 산출물) — 승인 차수에서 거부되는 조작과 준비 형태(재사용)
    - `domain/quotes/revisions.ts`의 `setCustomerApproval`·`createRevisionFromCurrent`(04-14·04-40) · `domain/quotes/lines.ts`의 `saveQuoteLines` · `domain/archive/index.ts`의 `restore`
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 38행(OV-1 — 「계약 금액은 항상 승인값과 같다」)
  </read_first>
  <behavior>
    - 2차 견적 합계 48,000,000으로 승인 → `listRevenue().contract.amountKrw` = 48,000,000
    - 이어서 (a) 수량 변경 (b) 단가 변경 (c) 견적가 있는 새 줄 (d) 견적가 있는 줄 취소 (e) 견적가 있는 줄 보관 (f) 승인 전에 보관해 둔 견적가 있는 줄의 보관함 복원 — 각각 거부되고, **매번** 계약 금액이 48,000,000 그대로다
    - (g) 실행가 변경 · 견적 외 비용 새 줄 · 조정 새 줄 → 통과, 계약 금액 48,000,000 그대로
    - 승인 표시를 끄면 → `—` + `2차 고객 승인 전` · 수량을 고쳐 합계 50,000,000 → 다시 승인하면 50,000,000
    - 3차를 만들면 → `—` + `3차 고객 승인 전`(2차 합계로 대신하지 않는다 — 금지 항목)
  </behavior>
  <action>
① `test/integration/contract-invariant.test.ts`(신규)에 `behavior` 다섯 줄을 쓴다 — 조작은 04-40이 만든 서버 경로(`saveQuoteLines`·`domain/archive`의 `restore`)를 그대로 부르고, 각 조작 뒤 `listRevenue`의 계약 금액과 DB의 승인 차수 견적 합(보관 제외)이 둘 다 승인값인지 단언한다(OV-1 — 사용자 D7 답의 약속을 계약 금액 쪽에서 증명). 준비·조작 도우미는 04-40의 테스트 파일 모양을 따르되 복사하지 말고 필요한 만큼만 쓴다.

② 이 테스트가 빨간데 04-40의 잠금이 원인이면 04-40 범위를 고치지 말고 `systematic-debugging`으로 원인을 찾아 SUMMARY에 적은 뒤 멈춘다. 계약 파생(`domain/revenue/index.ts`)이 원인이면(예: 보관 줄을 합에 넣음, 승인 순번이 아닌 차수를 봄) 여기서 고친다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/contract-invariant.test.ts test/integration/quote-approved-lock.test.ts test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 승인 뒤 어떤 조작으로 계약 금액이 움직였거나, 실행가·견적 외 비용·조정이 계약 금액을 바꿨거나, 미승인 현재 차수에서 이전 승인 합계가 보였다</fails_when>
  </verify>
  <acceptance_criteria>
    - 조작 (a)~(f) 각각 뒤에 계약 금액과 DB 합이 승인값과 같다는 단언이 있다(OV-1)
    - (g)의 세 조작이 통과하고 계약 금액이 그대로다
    - 승인 해제 → 수정 → 재승인 경로가 새 합계를 계약 금액으로 보인다
    - 새 차수 뒤 미승인 현재 차수에서 `—`가 보인다(금지 항목)
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>「계약 금액은 항상 승인값과 같다」가 승인 뒤 모든 쓰기·복원 경로에서 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 발행액 PM 공개 · 입금액 숨김 유지(D-85) + 역산 경로 점검 + 화면 검증</name>
  <files>domain/permissions/info-items.ts, domain/revenue/index.ts, app/(app)/projects/[id]/revenue-section.tsx, test/integration/revenue-entries.test.ts, test/integration/visibility.test.ts, test/e2e/revenue-section.spec.ts</files>
  <read_first>
    - `domain/permissions/info-items.ts` 45–56행(발행·입금 항목과 주석)
    - `domain/seed/index.ts` 160–180행(기본 계급 노출 행 upsert)
    - `domain/revenue/index.ts` 95–170행(`RevenueDto`·`REVENUE_DTO_SPEC` — 각 필드의 정보 항목. **`balanceKrw` 같은 파생 필드가 어느 항목으로 게이트되는지 확인한다**)
    - `test/integration/revenue-entries.test.ts`의 (d) 케이스 · `test/integration/visibility.test.ts`(시드 뒤 노출 판정 형태)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1211–1225행(발행 표 PM 읽기 · 입금 표 부재 · 부제)
  </read_first>
  <behavior>
    - 기획 PM viewer의 `listRevenue` → `issuedEntries` 있음 · `issuedTotalKrw` 있음(B-28) · `paidEntries` 키 없음 · 입금에서 파생된 모든 필드(`paidGrossTotalKrw`·`balanceKrw`·미수) 키 없음
    - 경영관리(두 항목 공개) → 전부 있음
    - 기존 DB에서 시드를 다시 돌리면 기획 PM의 `revenue.issued_amount` 노출 행이 true가 된다
  </behavior>
  <action>
① `info-items.ts`의 `revenue.issued_amount` `staffDefault`를 true로 바꾸고 주석을 D-85로 고친다(입금액은 false 그대로).

② **역산 경로 점검.** `REVENUE_DTO_SPEC`의 모든 필드를 입금 의존 여부로 나눠, 입금 합계·미수·초과 입금·잔액처럼 입금 값에서 파생되는 필드가 전부 `revenue.paid_amount`(또는 둘 다를 요구하는 방식)로 게이트되게 고친다 — 발행 항목만으로 게이트된 입금 파생 필드가 있으면 그것이 이 플랜의 누수다(금지 항목). **반대 방향(CEO 리뷰 B-28):** 지금 `issuedTotalKrw`는 입금에서 파생되지 않는데도 `revenue.paid_amount`로 게이트되어 D-85 뒤 PM이 서버가 계산한 발행 합계를 받지 못한다 — `revenue.issued_amount`로 옮긴다(`paidGrossTotalKrw`·`balanceKrw`는 입금 항목 그대로). 판단 표(필드 · 입금 의존 여부 · 게이트 항목)를 SUMMARY에 적는다.

③ `revenue-section.tsx`: 쓰기 권한이 없는 사람에게 발행 표는 흰 머리글 캡션 읽기 표(편집 셀 0 — `ui/table` 자동)이고 추가 버튼이 없으며 EMPTY `발행한 세금계산서가 없습니다 · 발행은 경영관리`. 입금 표는 DTO에 필드가 없으면 섹션에서 통째로 렌더하지 않는다. 부제는 입금 표가 있을 때만 `공급가액 기준 · 입금액만 통장 합계`, 없으면 `공급가액 기준`.

④ 테스트: `revenue-entries.test.ts` (d)의 기대값을 「발행은 옴 · 입금은 필드 부재 · 입금 파생 필드 부재」로 **고친다**(지우지 않는다). `visibility.test.ts`에 `behavior` 셋째 줄(시드 재실행 뒤 기획 PM 행 true). E2E: 기획 PM으로 발행 줄이 있는 프로젝트를 열면 발행 표가 읽기 표로 보이고 입금 표·미수 표시가 DOM에 없다 · 발행 줄 0이면 `· 발행은 경영관리`.

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S6 backstop 둘(375 발행·입금 표·계약 금액 2행 · 긴 메모 말줄임)을 DOM 실측 → 수정 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/visibility.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — PM 응답에 입금 또는 입금 파생 필드가 남았거나 시드 반영이 안 됐다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S6 판정 두 개를 SUMMARY에서 확인</test>
      <expected>375에서 가로 스크롤 0 · 숫자 꺾임 없음, 긴 메모가 한 줄 말줄임</expected>
      <why_human>폭·말줄임은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `revenue.issued_amount`의 `staffDefault`가 true, `revenue.paid_amount`가 false다
    - PM 응답 키 집합 단언이 발행 줄·발행 합계 있음 · 입금 부재 · 입금 파생 필드 부재를 모두 본다(통합 d, B-28)
    - `REVENUE_DTO_SPEC`에서 `issuedTotalKrw`가 `revenue.issued_amount`로, `paidGrossTotalKrw`·`balanceKrw`가 `revenue.paid_amount`로 게이트된다
    - 입금 파생 필드 게이트 판단 표가 SUMMARY에 있다
    - (d) 케이스가 삭제되지 않고 기대값만 바뀌었다
    - 시드 재실행 반영 케이스가 있다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>PM이 발행액을 읽기 표로 보고, 입금액과 그 파생값은 PM 응답 어디에도 없다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain 출구(RevenueDto) → 화면 | 발행·입금 필드가 정보 노출표를 지나야 실린다. 파생 필드가 역산 통로가 될 수 있다 |
| 브라우저 → 매출 섹션 | 화면에서 계약 금액 입력이 사라진다(서버 쪽 필드 제거는 04-41) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-83 | Information Disclosure | 입금액 역산 | high | mitigate | 입금 표와 입금 파생 필드(미수·초과 입금·잔액)를 전부 `revenue.paid_amount`로 게이트하고, PM 응답 키 집합에서 부재를 통합 테스트로 단언한다(금지 항목). 발행 합계는 입금에서 파생되지 않으므로 발행 항목으로 옮긴다(B-28). D-38 누수 스캔 레지스트리가 DTO 필드별 항목을 덮는다 |
| T-04-85 | Tampering | 파생 계약 금액 | medium | mitigate | 서버가 현재 차수·승인·합계(숫자 SUM — B-18)·부가세(승인일 기준 — B-27)를 계산하고 화면은 표시만 한다. 이전 승인 차수로 대신하지 않는다(금지 항목). 승인 뒤 합계 불변은 04-40의 잠금 + 이 플랜의 불변식 테스트(OV-1) |
| T-04-85b | Information Disclosure | 계약 금액 필드 | medium | mitigate | 계약 금액은 견적 합계라 `quote.amount`로 게이트한다 — 견적 금액을 숨긴 계급에 `project.value`만으로 새지 않는다(B-19, 키 부재 통합 케이스) |
| T-04-87 | Information Disclosure | 노출표 기본값 반영 | low | mitigate | 시드 upsert가 기본 계급 행에만 적용되는 기존 규약을 그대로 쓰고, 시드 재실행 반영을 통합 테스트로 고정한다. 팀장·본부 책임자 행은 시드 밖이라 관리자 확인 항목으로 SUMMARY에 남긴다(B-29) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |

(옛 T-04-84 「옛 계약 금액 필드」·T-04-86 「컬럼 삭제 배포 창」은 쓰기 경로·컬럼을 지우는 04-41로 옮겼다.)
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 계약 금액 불변식 테스트 초록
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 계약 금액이 고객 승인된 현재 차수의 견적 합계에서만 나오고, 승인 뒤에는 어떤 쓰기로도 움직이지 않으며, 화면에 입력 칸이 없다
- PM이 발행액과 발행 합계를 보고 입금액과 그 파생값은 보지 못한다
- 04-41이 서버 쓰기 경로와 컬럼을 지워도 화면·파생이 바뀌지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 타입(변경) | `ContractInfo` — `amountKrw \| null` · `vatKrw \| null` · `totalKrw \| null` · `sourceLabel` · `pendingLabel` |
| 함수(repository) | `sumQuoteAmountByRevision`(`repositories/quote-lines.ts` — `::bigint` + `mapWith(Number)`) |
| 삭제된 심볼(화면) | `QuoteLedger` 계약 draft·dirty 합류·props · `RevenueSection` 계약 금액 `Form.Field`(통화·환율 칸 포함) |
| 정보 노출 기본값 | `revenue.issued_amount` staffDefault false → **true** |
| DTO 게이트 변경 | `contract` → `quote.amount`(B-19) · `issuedTotalKrw` → `revenue.issued_amount`(B-28) |
| 변경된 기존 심볼 | `listRevenue`(파생 계약 · 승인일 기준 부가세) · `RevenueSection`(KvList 계약 한 줄 · 발행 읽기 표 · 입금 표 조건 렌더 · 부제) · 04-22의 `canSave`(계약 칸 항 제거) |
| 테스트 | `test/integration/contract-invariant.test.ts`(신규) · `revenue-entries.test.ts`(파생·키 집합·(d) 기대값) · `visibility.test.ts` · `revenue-section.spec.ts` 갱신 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 계약 금액 서버 쓰기 경로 제거 · `projects.contract_*` DROP 마이그레이션(RAISE 가드 · squawk 인라인 무시) · 0010 상태 업그레이드 테스트 케이스 · ARCHITECTURE §5 예외·「Phase 4 한 번에 배포」 기록 · 계약 참조 스캔 · 「옛 필드는 스키마가 무시」 케이스(04-41) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| OV-1 · 사용자 D7 (T4) — 계약 금액 = 승인값 약속 | HIGH | Plan 04-16, Task 2: 승인 뒤 여섯 조작 거부 · 매번 계약 금액·DB 합 불변 · 실행가 무영향 · 해제/재승인 · 새 차수 |
| B-18 — 계약 SUM이 문자열로 와 부가세가 이어 붙음 | WARNING | Plan 04-16, Task 1 ①-b: `sumQuoteAmountByRevision` `::bigint` + `mapWith(Number)`, 30억 케이스 |
| B-19 — 계약 금액이 `project.value`로 게이트됨 | WARNING | Plan 04-16, Task 1 ①: `quote.amount`로 이동 + 키 부재 케이스 |
| B-27 — 계약 부가세 기준일이 오늘 | NOTE | Plan 04-16, Task 1 ①: `asOf` = 승인일 + 세율 행 추가 케이스 |
| B-28 — 발행 합계가 입금 항목으로 게이트됨 | NOTE | Plan 04-16, Task 3 ②: `issued_amount`로 이동, 키 집합 단언 |
| B-29 — 팀장·본부 책임자 발행액 기본 숨김(시드 밖) | NOTE | 이미 반영(T-04-87) — `<probe_fallback>`·truth에 관리자 확인 항목으로 명시 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-09 · 사용자 D9 · OV-6 (T14) — DROP 가드·예외 기록·업그레이드 테스트 | 파일 수 때문에 분할 — Plan 04-41(바로 뒤)이 전부 맡는다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-16-SUMMARY.md` when done
</output>
