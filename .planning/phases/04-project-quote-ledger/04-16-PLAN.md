---
phase: 04-project-quote-ledger
plan: 16
type: execute
wave: 20
depends_on: ["04-15"]
files_modified:
  - domain/revenue/index.ts
  - domain/permissions/info-items.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/integration/revenue-entries.test.ts
  - test/integration/contract-invariant.test.ts
  - test/integration/visibility.test.ts
  - test/e2e/revenue-section.spec.ts
autonomous: true
requirements: [PROJ-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "계약 금액 입력 칸이 없다 — 매출 섹션의 계약 금액은 고객 승인이 표시된 **현재 차수**의 견적 합계(공급가)이고, `KvList` 한 줄 `계약 금액` `48,000,000` + 2행 `부가세 10% 4,800,000 · 합계 52,800,000 · 2차 고객 승인 합계`로 보인다(D-84, UI-SPEC S6, Copywriting 표시 — 계약 금액(파생))"
    - "현재 차수가 미승인이면(새 차수를 만든 직후 등) 계약 금액은 `—` + 2행 `{n}차 고객 승인 전`이다 — 이전 승인 차수의 합계로 대신하지 않는다(D-84 「현재 차수」)"
    - "계약 금액의 부가세·합계는 서버가 `domain/money`와 세금 설정으로 계산한다 — 화면은 계산하지 않는다(PNL-09, 기준 5). 부가세 기준일은 그 차수의 **승인일**이다 — 세율 설정이 바뀌어도 이미 승인된 계약의 부가세가 오늘 세율로 흔들리지 않는다. 기준일은 승인일의 KST 달력 날짜(`kstDateOf(customer_approved_at)`)를 UTC 자정 `Date`로 만든 값이다 — 저장된 순간(KST 00:00 = 전날 UTC 15:00)을 그대로 넘기면 설정 조회가 UTC 날짜로 잘라 **전날** 세율을 쓴다. 새 세율의 시행일에 승인된 계약은 새 세율로 계산된다(CEO 리뷰 B-27, 엔지니어링 리뷰 B §1 — 매출 줄이 `new Date(row.entryDate)`로 같은 문제를 피하는 선례)"
    - "계약 금액의 견적 합계는 리포지토리 SUM 한 번이고 JS 숫자로 온다(`::bigint` 뒤 숫자 변환) — 부가세 계산이 문자열 이어 붙이기가 되지 않고 21억 원을 넘어도 정확하다(CEO 리뷰 B-18)"
    - "계약 금액 = 승인된 현재 차수의 견적 합계가 **불변**이다 — 승인 뒤 수량·단가·상태·소분류 변경, USD 환율만·통화만 바꾼 저장, 견적가 있는 새 줄·보관·복원이 전부 거부되므로(04-40) 어떤 저장·복원 시도 뒤에도 계약 금액이 승인 시점 값 그대로이고, 실행가 변경은 계약 금액을 바꾸지 않는다. 바꾸려면 승인 표시를 끄거나 새 차수다(사용자 D7 답의 약속, CEO 리뷰 OV-1, 엔지니어링 리뷰 GAP 1 — 불변식 통합 테스트)"
    - "계약 금액 필드는 정보 항목 `quote.amount`로 게이트된다 — 값이 견적 합계이므로, 견적 금액을 숨긴 계급은 `project.value`가 보여도 계약 금액 키를 받지 않는다(CEO 리뷰 B-19)"
    - "화면·저장 페이로드에 계약 금액 입력이 없다 — 네고로 금액이 바뀌면 새 차수로 반영한다(D-84, 옛 D-57의 「계약 금액 PM 쓰기」 폐기). 서버의 쓰기 경로(액션 필드 · 도메인 · 리포지토리)와 `projects.contract_*` 컬럼 제거는 바로 뒤 04-41이다"
    - "매출 섹션의 1차 저장 합류(dirty N)에서 계약 금액 칸이 빠지고, 저장 버튼 렌더 조건이 「편집할 수 있는 칸이 구조적으로 하나라도 있는가」(견적 줄 셀 · 발행·입금 쓰기 · 기간 칸)다(U-5, S3)"
    - "세금계산서 발행액은 기획본부(PM)에게 기본 공개다 — PM에게 발행 줄 표가 흰 머리글 읽기 표(캡션)로 오고, 줄이 0이면 `발행한 세금계산서가 없습니다 · 발행은 경영관리`(버튼 없음)다(D-85, S6, Copywriting)"
    - "PM도 서버가 계산한 발행 합계(`issuedTotalKrw`)를 받는다 — 발행 합계는 입금에서 파생되지 않으므로 발행 항목(`revenue.issued_amount`)으로 게이트하고, 잔액·입금 합계만 입금 항목(`revenue.paid_amount`)에 남는다(CEO 리뷰 B-28)"
    - "입금액은 PM에게 기본 숨김을 유지한다 — 입금 줄 표가 서버에서 오지 않고(빈 배열이 아니라 필드 부재), 입금 표 합계 행의 미수·초과 입금 표시도 함께 없으며, 입금액을 역산할 수 있는 다른 필드(잔액 등)도 오지 않는다(D-85, S6 partial)"
    - "PM에게 매출 섹션 부제는 `공급가액 기준`만이고, 입금 표를 받는 사람에게는 `공급가액 기준 · 입금액만 통장 합계`다(Copywriting PNL-09 표시)"
    - "정보 노출표 기본값 변경은 새로 시드되는 기획 PM 계급과, 시드를 다시 돌린 기존 DB의 기획 PM 계급 행에 적용된다(시드가 기본 계급 행을 upsert한다 — RESEARCH §5 소급 여부 Gap 해소). 팀장·본부 책임자 계급 행은 시드가 건드리지 않아 발행액 기본 숨김이 남는다 — 관리자가 노출표에서 켠다(CEO 리뷰 B-29, 관리자 확인 항목)"
    - "발행·입금 표는 각자 제 EMPTY 한 줄을 갖는다 — 경영관리(쓰기)는 `발행한 세금계산서가 없습니다 · 발행 줄 추가` / `입금 줄이 없습니다 · 입금 줄 추가`(3차 버튼), 표를 읽기로만 받는 사람은 `· 발행은 경영관리` / `· 입금은 경영관리`(버튼 없음), **1024px 미만(폰 · 좁은 PC)은 경영관리에게도 사실만** `발행한 세금계산서가 없습니다` / `입금 줄이 없습니다` · 발행 줄이 있고 입금 줄이 0이면 입금 표의 EMPTY 한 줄과 합계 행이 함께 보이고 합계 0 + 오른쪽 미수 전액이다(Copywriting `Empty — 발행 줄 / 입금 줄` · UI Considerations S6 empty · zero-one-many, DR-24 · DR-36)"
    - "매출 두 표의 LOADING은 §7-7 표 뼈대이고 계약 금액 줄은 서버 렌더라 로딩 구간이 없다 · 셀 오류는 그 표 합계 행 오른쪽 `오류 1칸 · 전부 거부`이고 화면 전체가 거부된다 · 입금 합계 ≠ 발행 합계면 입금 표 합계 행 오른쪽 `미수 3,400,000` / `초과 입금 500,000`(`--warning`) · 매출 표에는 힌트 줄이 없다(UI Considerations S6 loading · error · partial, Copywriting `Error — 표 배치 저장 실패` · `PARTIAL — 미수 / 초과 입금`, DR-31)"
    - "입금액 셀의 2행은 `공급가액 12,400,000`이고(옛 `· 서버 계산` 꼬리가 없다 — P0), 외화 입금이면 `USD 4,400.00 @1,318.1818` · `공급가액 12,400,000` **두 묶음**이며 폭이 모자라면 두 묶음 사이에서만 줄바꿈하고 묶음 안 숫자는 꺾지 않는다(UI-SPEC rev 5 「적용 규칙 S6」 · DR-15)"
    - "폰(<700)에서 발행·입금 표의 P1은 **날짜 · 금액 두 열**이고 금액 셀의 2행은 셀 안이 아니라 접힌 줄로 내려간다 — 접힌 줄은 발행 = 메모, 입금 = 외화 묶음 · 공급가액 묶음 · 메모 순이고 숫자 묶음은 꺾이지 않으며 메모는 줄바꿈한다(같은 정보를 두 번 보이지 않는다 — DR-15, UI Considerations S6 overflow)"
    - "700~1023px(좁은 PC)과 폰에서 발행·입금 표는 **보기 전용**이다 — 셀이 `Enter`·클릭에 편집으로 들어가지 않고 흰 머리글 읽기 표로 그려지며 「발행 줄 추가」·「입금 줄 추가」가 없다. 판정은 그룹 A가 견적 표에 둔 1024 편집 게이트 하나다(DR-36 · 계약 6, 「적용 규칙 S6」)"
    - "「일괄 저장」 요청 동안 발행·입금 표는 보이되 편집에 들어가지 않는다 — 클릭·`Enter`·글자 입력·붙여넣기·`Delete`가 무반응이고 방향키 이동·범위 선택·`Ctrl+C`는 되며 격자가 `aria-busy=true`이고 추가 버튼이 비활성이다. 응답이 오면 성공·실패 모두 즉시 풀린다 — `RevenueSection`이 `QuoteLedger`의 `saveLocked`(계약 3, 그룹 A 소유)를 받아 두 표에 그대로 넘긴다(DR-3, UI Considerations S19 loading · 「적용 규칙 S19」)"
    - statement: "발행·입금 표와 계약 금액 2행이 1280 · 1024 · 375에서 가로 스크롤 0이고 숫자가 꺾이지 않으며, 375에서 P1이 날짜·금액 두 열이고 외화·공급가액·메모가 접힌 줄에서 각자 한 묶음으로 묶음 사이에서만 줄바꿈한다 — 최장 외화 · 13자리 금액 · 긴 메모로 DOM 감사(UI Considerations S6 overflow — DR-15 · DR-14 감사 폭)"
      verification: backstop
    - statement: "발행·입금 표의 메모 셀이 PC에서 한 줄 말줄임이고 행 높이를 늘리지 않으며 폰에서는 접힌 줄에서 줄바꿈한다 — 긴 메모 DOM 감사(UI Considerations S6 long-text)"
      verification: backstop
    - statement: "1000px에서 발행·입금 표가 흰 머리글 읽기 표이고 추가 버튼이 DOM에 없다 — DOM 감사(DR-36)"
      verification: backstop
  prohibitions:
    - statement: "입금액이 기획본부에게 역산 경로로 새어서는 안 된다. 발행액을 공개한 뒤 미수·초과 입금·잔액 같은 파생값이 하나라도 PM 응답에 남으면 발행액 − 미수 = 입금액이 된다 — 입금 표를 숨기는 것은 표 단위 필드 부재이고, 그 표에서 파생된 값도 같이 빠져야 한다"
      status: resolved
      verification: test
    - statement: "계약 금액이 이전 승인 차수의 합계로 조용히 대신 채워져서는 안 된다. 현재 차수가 미승인이면 계약이 아직 확정되지 않은 것이고, 옛 합계를 보이면 PM과 경영관리가 서로 다른 계약 금액을 믿게 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/revenue/index.ts"
      provides: "파생 계약 금액(현재 차수 승인 여부 · 견적 합계 · 승인일 기준 부가세·합계 · 출처 문구, `quote.amount` 게이트)"
      contains: "고객 승인"
    - path: "test/integration/contract-invariant.test.ts"
      provides: "승인 뒤 모든 합계 변경 시도가 거부되고 계약 금액이 승인값 그대로 · 실행가 변경 무영향 · 승인 해제·새 차수 뒤 값"
      min_lines: 50
    - path: "domain/permissions/info-items.ts"
      provides: "revenue.issued_amount 기본 공개 · revenue.paid_amount 기본 숨김"
      contains: "revenue.issued_amount"
  key_links:
    - from: "domain/revenue/index.ts"
      to: "domain/quotes/revisions.ts"
      via: "계약 금액이 현재 차수(최신 순번)와 그 승인 표시에서 파생된다"
      pattern: "customerApprovedAt"
    - from: "app/(app)/projects/[id]/revenue-section.tsx"
      to: "domain/revenue/index.ts"
      via: "계약 금액이 입력이 아니라 DTO의 파생 값 `KvList` 한 줄이다"
      pattern: "KvList"
    - from: "domain/permissions/info-items.ts"
      to: "domain/seed/index.ts"
      via: "기본 계급의 노출 행이 staffDefault로 시드된다"
      pattern: "staffDefault"
    - from: "domain/revenue/index.ts"
      to: "repositories/quote-lines.ts"
      via: "승인된 현재 차수의 견적 합계를 숫자 SUM 한 번으로 읽는다"
      pattern: "mapWith\\(Number\\)|sumQuoteAmount"
---

<objective>
**As a** 기획 PM과 경영관리, **I want to** 계약 금액을 따로 적지 않고 고객이 승인한 현재 차수의 견적 합계로 보고, PM도 세금계산서 발행액을 보되 입금액은 경영관리만 보기를, **so that** 계약 금액이 견적과 어긋나는 일이 구조적으로 없고, PM은 발행 진행을 알되 입금 사정은 경영관리 정보로 남는다.

이 플랜은 04-02가 만든 매출 섹션을 D-84·D-85로 고친다. 옛 D-57의 「계약 금액은 PM」과 「발행액 기획본부 기본 숨김」 두 부분은 **대체됐다** — 이 플랜은 계약 금액 입력의 모든 경로(화면 칸 · `saveProjectLedgerAction`의 `revenue.contract`·`contractFxRateTouched` · `saveRevenue`의 계약 분기 · `updateProjectContract` · `projects.contract_*` 컬럼)를 지운다(PATTERNS Resolved Gaps — 별도 「매출 계약 쓰기」 액션은 없고 `saveProjectLedgerAction` 한 곳이다).

Purpose: 기준 5 「매출 칸의 발행액은 공급가액으로 적고 부가세·합계는 서버가 계산」(04-08이 문구 정정)과 PROJ-03(파생 계약 금액). 04-17·04-18의 목록 매출 열·기준이 D-85의 발행액 공개를 전제한다.

Output: 파생 계약 금액(승인일 기준 부가세 · 숫자 SUM · `quote.amount` 게이트) · 화면의 계약 금액 입력 제거 · 승인 합계 불변식 테스트 · 발행액 공개 · 통합 3 갱신 · E2E 갱신.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` T4·T14·OV-1·OV-6·부록 B).** 옛 04-16은 17파일이라 **둘로 나눴다.** 이 플랜은 계약 금액을 **읽는 쪽**(파생 · 화면)과 D-85 노출을 맡고, **04-41**(바로 뒤)이 계약 금액 쓰기 경로와 `projects.contract_*` 컬럼을 지운다(사용자 D9 — RAISE 가드 · ARCHITECTURE §5 예외 기록 · 「Phase 4는 PR #38 한 번에 배포」 제약 · 0010 상태 업그레이드 테스트). 이 플랜에 들어온 사실 정정: B-18(SUM 숫자) · B-19(계약 금액은 `quote.amount`로 게이트) · B-27(부가세 기준일 = 승인일) · B-28(발행 합계는 발행 항목으로 게이트) · B-29(팀장 행은 시드 밖 — 관리자 확인) · OV-1(계약 금액 = 승인 합계 불변식 테스트 — 사용자 D7의 약속, 잠금은 04-40).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 결정 번호는 「ENG-D*」로 적어 CEO 리뷰의 「사용자 D*」와 구별한다).** (1) **부가세 기준일 KST 경계(B §1):** `asOf` = `new Date(kstDateOf(approvedAt))` + 새 세율 시행일 당일 승인 케이스(GAP 4). (2) **GAP 1의 계약 금액 쪽:** 불변식 (h) — 승인 차수의 USD 환율만·통화만 바꾼 저장 뒤에도 계약 금액 그대로. (3) **낡은 주석:** `domain/revenue/index.ts` 169행. (4) **ENG-D11:** Task 2 ②의 「04-40이 원인이면 멈춘다」를 「그 자리만 고치고 회귀 테스트 + SUMMARY 편차」로 풀었다.

**2026-09-23 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md` — 「DR-*」, 전역 원칙 P0).** (1) **DR-15:** 폰 매출 표 P1 = 날짜 · 금액, 메모는 접힌 줄, 외화와 공급가액은 각각 한 묶음으로 줄바꿈(숫자는 꺾지 않는다) — `ui/table`을 고치지 않고 이 화면의 열 선언(`secondaryLine`·접힌 줄 `summary`)과 `project-detail.module.css`로 한다. (2) **DR-36:** 700~1023px은 보기 전용(추가·편집 없음), 1024 미만 EMPTY는 사실만 — 그룹 A의 1024 편집 게이트를 같이 쓴다(계약 6). (3) **DR-3:** 매출 두 표가 `saveLocked`(계약 3 — 그룹 A가 `QuoteLedger`에서 계산)를 받아 저장 중 편집 진입을 막는다. (4) **P0:** 입금 셀 2행의 `· 서버 계산` 꼬리 삭제. (5) **DR-12:** 매출 줄에는 삭제 동작이 없어(rev 5 S16의 열두 사용처에 없다) 확인 다이얼로그가 필요 없다. (6) **DR-14:** DOM 감사 폭 1280 · 1024 · 375(+ 좁은 PC 1000). **태스크 재배치:** 화면 몫이 커져 옛 Task 3의 서버 부분(노출 기본값 · DTO 게이트 · 역산 점검 · 통합 (d)·시드)을 Task 2(서버 · 불변식)로 옮기고, Task 3을 매출 섹션 화면 하나로 모았다 — 파일·검증 명령은 그대로 나뉘어 옮겨졌다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 763–786행(§4 D-84 제거 범위 · §5 D-85 노출 전환과 소급 Gap)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 402행(saveProjectLedgerAction) · 420행(revenue-section 배정) · 492–494행(표 단위 노출)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5) 「UI Considerations S6」 533–542행(여덟 상태) · 「적용 규칙 S6. 매출 섹션」 1282–1315행 · 「UI Considerations S19」 651행 · Copywriting 356행(Empty — 발행 줄 / 입금 줄) · 361행(Error — 표 배치 저장 실패) · 409행(PARTIAL — 미수 / 초과 입금) · 411행(표시 — 계약 금액(파생)) · 432–436행(PNL-09 표시 의무) — 줄 번호는 `grep -n "### S6. 매출 섹션"`로 다시 찾는다
</context>

<assumption_delta_decision>
- **신호:** 입력하던 값(계약 금액)이 파생값이 됐다 — 「chosen → derived」의 역방향.
- **주 명사:** 고객 승인된 현재 차수의 견적 합계.
- **결정: promote.** 파생값을 유일한 계약 금액으로 올리고 저장 컬럼·입력 경로를 **지운다**. 컬럼을 남겨 두고 파생값을 곁에 보이는 add-alongside는 「어느 쪽이 계약 금액인가」를 다시 열어 두는 일이라 택하지 않는다. 지금 행은 전부 `source='demo'`이고(Eng OV-1) 값은 파생으로 대체되므로 잃는 업무 데이터가 없다. 이 결정은 사용자 D9(「지금 삭제 + 가드」)로 확정됐고, 삭제는 04-41이 한다.
</assumption_delta_decision>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-03**이다. 프로브는 옛 문구(「계약 금액 … 을 기록한다」)로 돌았으므로 수용 기준은 결정된 D-84·D-85와 UI-SPEC S6으로 끌어왔다(04-08이 요구사항 문구를 고친다). S6 `## UI Considerations`의 overflow·long-text backstop은 마커로, empty·loading·error·populated·partial·zero-one-many는 truths로 옮겼다(U-3의 「EMPTY와 합계 행 공존」은 04-02 동작 그대로).

**계획 단계 가정(표면화):**
- **컬럼 삭제(사용자 D9로 확정 — 04-41):** D-84 원문은 「제거하거나 파생 표시로 바꾼다」. 삭제는 04-41이 RAISE 가드와 함께 하고, 배포 창 위험(옛 리비전이 옛 컬럼을 읽는 창)은 「Phase 4는 한 PR로 한 번에 배포」 전제 아래에서만 무해하다 — 그 기록도 04-41이다.
- **부가세 기준일(B-27):** `listRevenue`는 지금 계약 부가세를 `new Date()`로 계산한다. 승인된 계약의 부가세는 승인일 기준이어야 세율 설정 변경이 과거 계약을 흔들지 않는다 — 승인일의 KST 달력 날짜(`kstDateOf(customer_approved_at)`)를 UTC 자정 `Date`로 만들어 `asOf`로 넘긴다. 저장된 순간(04-14 — KST 00:00 = 전날 UTC 15:00)을 그대로 넘기면 `domain/settings/registry.ts`의 `dateOnly`가 UTC 날짜로 잘라 하루 앞 세율을 쓴다(엔지니어링 리뷰 B §1).
- **D-85의 기존 DB 반영:** 정보 노출표는 계급별 저장 행이다. 시드(`seedMasterData`)가 기본 계급(기획 PM)의 행을 `staffDefault`로 upsert하므로 기본값을 바꾸고 시드를 다시 돌리면 기존 DB에도 반영된다 — 별도 데이터 마이그레이션을 만들지 않는다. 대신 관리자가 기획 PM 행을 손으로 바꿔 둔 운영 DB에서는 시드가 그 값을 덮는다는 기존 시드 규약이 그대로 적용된다(이 플랜이 새로 만드는 동작이 아니다). 시드는 시스템 관리자·기획 PM 두 계급의 노출 행만 쓰므로 팀장·본부 책임자는 발행액이 기본 숨김으로 남는다 — 관리자가 노출표에서 켤지 확인할 항목으로 SUMMARY에 적는다(CEO 리뷰 B-29, ALREADY-COVERED — T-04-87). 04-20이 권한 시드를 「없을 때만 넣기」로 바꾼 것(CEO 리뷰 A-05)은 권한 행 이야기이고 노출 행 upsert는 바뀌지 않았다 — 04-20-SUMMARY에서 확인하고, 노출 행도 바뀌었으면 위 「시드 재실행 반영」 케이스를 「새 DB에서만 반영」으로 고친 뒤 SUMMARY에 적는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 계약 금액이 고객 승인된 현재 차수 합계로 보인다(도메인 파생 → DTO → 매출 섹션 한 줄 → E2E)</name>
  <files>domain/revenue/index.ts, repositories/quote-lines.ts, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, test/integration/revenue-entries.test.ts, test/e2e/revenue-section.spec.ts</files>
  <read_first>
    - `domain/revenue/index.ts`(전문 350행) — `ContractInfo`·`listRevenue`의 계약 계산(176–183행 — 부가세 기준일이 `new Date()`)·발행 줄의 `new Date(row.entryDate)`(191행 — UTC 자정 날짜 선례)·`computeVat`·`REVENUE_DTO_SPEC`(131행 `contract` → `project.value`)·`saveRevenue`의 계약 분기 · 169행 머리 주석(「계약 금액 + 발행·입금 두 표」)
    - `domain/settings/registry.ts` 46–48행·75행(`dateOnly(asOf)` = `toISOString().slice(0, 10)` — UTC 날짜로 자른다) · `lib/kst-date.ts`(04-29 — `kstDateOf`)
    - `domain/quotes/revisions.ts`(04-14) — 현재 차수·승인 표시·승인일(KST 00:00 순간) · `repositories/quote-lines.ts`(현재 차수 합계에 쓸 조회)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-18·B-19·B-27 절(Grep `### B-18`·`### B-19`·`### B-27`)
    - `app/(app)/projects/[id]/revenue-section.tsx`(전문 305행) — 계약 금액 `Form.Field`와 통화·환율 칸
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `contractDraft`·`updateContract`·`contractVat`·`canWriteContract`를 찾아 범위만 Read
    - `ui/kv-list/KvList.tsx`(전문)
    - `test/integration/revenue-entries.test.ts`(전문) · `test/e2e/revenue-section.spec.ts`(전문)
  </read_first>
  <behavior>
    - 현재 차수(2차) 승인 · 견적 합계 48,000,000 → `contract = { amountKrw: 48000000, vatKrw: 4800000, totalKrw: 52800000, source: "2차 고객 승인 합계" }` · 세 금액 모두 `typeof === "number"`
    - 견적 줄 둘이 1,500,000,000씩(합 3,000,000,000)이고 승인 → `amountKrw` 3000000000(숫자), 부가세·합계가 숫자 덧셈 결과(B-18)
    - 3차를 만들어 현재가 미승인 → `contract = { amountKrw: null, pending: "3차 고객 승인 전" }` — 2차 합계를 쓰지 않는다
    - 현재 차수에 견적 외 비용·조정 줄이 있어도 계약 금액은 견적가 합계라 변하지 않는다(두 종류 모두 견적가 0)
    - 보관된 줄은 합계에서 빠진다
    - 승인일 2026-01-10에 승인된 계약: 세율 설정에 2026-06-01부터 새 세율 행을 더해도 계약 부가세는 승인일 세율로 계산된다(B-27)
    - **경계일(엔지니어링 리뷰 GAP 4):** 세율 설정에 `effective_from = 2026-09-19`인 새 세율 행을 더하고 2026-09-19로 승인 → 계약 부가세가 **새** 세율로 계산된다(저장된 순간 `2026-09-18T15:00:00Z`를 그대로 넘기면 옛 세율이 나와 이 케이스가 빨갛다)
    - `quote.amount`를 숨기고 `project.value`는 보이는 계급 → 응답에 `contract` 키가 없다(B-19)
  </behavior>
  <action>
① `domain/revenue/index.ts`: `ContractInfo`를 파생형으로 바꾼다 — `{ amountKrw: number | null; vatKrw: number | null; totalKrw: number | null; sourceLabel: string | null; pendingLabel: string | null }`. `listRevenue`가 현재 차수(최신 순번)와 승인 표시를 읽어 승인이면 보관 제외 줄의 `quote_amount_krw` 합과 `computeVat`(기존 세금 설정 경로 — **`asOf` = `new Date(kstDateOf(customerApprovedAt))`**, 승인일의 KST 날짜를 UTC 자정으로 — 저장된 순간을 그대로 넘기지 않는다, CEO 리뷰 B-27 · 엔지니어링 리뷰 B §1)로 채우고 `{n}차 고객 승인 합계`, 미승인이면 금액 셋 null + `{n}차 고객 승인 전`. **이전 승인 차수를 찾는 코드를 두지 않는다.** `REVENUE_DTO_SPEC`의 `contract` 필드 정보 항목을 `project.value`에서 `quote.amount`로 바꾼다(B-19 — 값이 견적 합계다). 169행 머리 주석(「계약 금액 + 발행·입금 두 표」)을 「파생 계약 금액(고객 승인된 현재 차수 합계) + 발행·입금 두 표」로 고친다(엔지니어링 리뷰 낡은 주석). 결정표 통합 테스트(`behavior` — 경계일 케이스 포함)를 먼저 쓴다(RED). 이 파일의 기존 계약 금액 케이스 중 「쓴 값을 `listRevenue`로 다시 읽는」 단언은 파생 규칙과 부딪히므로 이 태스크에서 지운다 — 서버 쓰기 경로 자체의 제거와 「옛 필드는 스키마가 무시」 케이스는 04-41이다(그때까지 옛 쓰기 분기는 컬럼에만 쓰고 아무 화면도 읽지 않는다).

①-b **합계 쿼리(B-18).** `repositories/quote-lines.ts`에 `sumQuoteAmountByRevision(viewer, revisionId, tx?)` — 보관 제외 `quote_amount_krw`의 `coalesce(sum(…), 0)::bigint`를 `sql<number>`에 `.mapWith(Number)`로 리포지토리 경계에서 숫자로 바꿔 돌려준다(node-pg는 bigint를 문자열로 준다 — 04-17 C-01과 같은 규칙). `computeVat`에는 이 숫자만 들어간다.

② `revenue-section.tsx`: 계약 금액 `Form.Field`(금액·통화·환율)를 지우고 `KvList` 한 줄 — 라벨 `계약 금액` · 값(`--fs-base` 700 `tabular-nums`, `formatKrw`) 또는 `—` · 2행 `--fs-sm --muted`(`부가세 10% {vat} · 합계 {total} · {sourceLabel}` 또는 `{pendingLabel}`). 부가세 비율 글자는 세금 설정에서 계산된 값의 표시이고 화면이 곱하지 않는다(DTO가 비율 문자열을 함께 싣게 하거나 금액만 보인다 — 선택을 SUMMARY에 적는다). 통화·환율 칸이 없다.

③ `quote-table.tsx`·`[id]/page.tsx`: 계약 금액 draft 상태·dirty 합류·저장 페이로드의 `revenue.contract`·`canWriteContract`·계약 부가세 props를 지운다. 1차 「일괄 저장」 렌더 조건은 04-22가 `[id]/page.tsx`에서 서버 계산하는 `canSave` 하나다 — 그 식에서 계약 칸 항 `(canWriteContract && status !== 완료)`를 지워(계약 입력이 없어져 죽은 항이다) 「편집 가능한 견적 줄 셀이 하나라도 있음(`cellEditability` — 04-30) · 발행·입금 쓰기 · 기간 칸 권리(04-22)」 중 하나가 되게 한다(U-5). 새 렌더 조건을 따로 만들지 않는다.

④ E2E(`revenue-section.spec.ts` 갱신): 계약 금액 입력 단계를 지우고 — 담당 PM이 줄 두 개를 저장하고 고객 승인 표시(04-24의 모달 — 서버는 04-14) → 매출 섹션 `계약 금액`이 견적 합계와 같다 → 새 차수를 만들면 `—` + `2차 고객 승인 전`.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 파생 계약 금액이 현재 차수 승인 규칙과 다르거나, 합계가 숫자가 아니거나, 부가세가 오늘 세율 또는 승인 전날 세율(KST 경계)로 계산됐거나, `quote.amount`를 숨긴 계급에 계약 금액이 샜다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ContractInfo`에 입력용 필드가 없고 파생 필드만 있다
    - 미승인 현재 차수에서 이전 승인 차수 합계를 쓰지 않는다는 케이스가 통합 테스트에 있다(금지 항목)
    - 계약 부가세의 기준일이 승인일이다(통합 — 세율 행 추가 케이스, B-27) · 새 세율 시행일 당일 승인이 새 세율로 계산된다(경계일, GAP 4) · `asOf`가 `kstDateOf`를 거친 날짜다
    - `REVENUE_DTO_SPEC`의 `contract`가 `quote.amount`로 게이트되고 키 부재 케이스가 있다(B-19)
    - 합계 SUM이 리포지토리에서 숫자로 오고 30억 케이스가 정확하다(B-18)
    - `revenue-section.tsx`에 계약 금액 입력 요소(`id="contract-amount"`)가 없다
    - `quote-table.tsx`에 계약 금액 draft·dirty 합류가 없다
    - 1차 버튼 렌더 조건이 04-22의 `canSave` 하나이고 그 식에 `canWriteContract` 항이 없다(편집 가능 셀 · 발행·입금 쓰기 · 기간 권리)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>매출 섹션의 계약 금액이 고객 승인된 현재 차수 합계로만 보이고 화면에 입력 칸이 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 계약 금액 = 승인 합계 불변식(사용자 D7 · OV-1) + 발행액 PM 공개 · 입금액 숨김 유지(D-85 — 노출 기본값 · DTO 게이트 · 역산 경로 점검, 서버)</name>
  <files>test/integration/contract-invariant.test.ts, domain/revenue/index.ts, domain/permissions/info-items.ts, test/integration/revenue-entries.test.ts, test/integration/visibility.test.ts</files>
  <read_first>
    - `test/integration/quote-approved-lock.test.ts`(04-40 산출물) — 승인 차수에서 거부되는 조작과 준비 형태(재사용)
    - `domain/quotes/revisions.ts`의 `setCustomerApproval`·`createRevisionFromCurrent`(04-14·04-40) · `domain/quotes/lines.ts`의 `saveQuoteLines` · `domain/archive/index.ts`의 `restore`
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 38행(OV-1 — 「계약 금액은 항상 승인값과 같다」)
    - `domain/permissions/info-items.ts` 45–56행(발행·입금 항목과 주석) · `domain/seed/index.ts` 160–180행(기본 계급 노출 행 upsert)
    - `domain/revenue/index.ts` 95–170행(`RevenueDto`·`REVENUE_DTO_SPEC` — 각 필드의 정보 항목. **`balanceKrw` 같은 파생 필드가 어느 항목으로 게이트되는지 확인한다**)
    - `test/integration/revenue-entries.test.ts`의 (d) 케이스 · `test/integration/visibility.test.ts`(시드 뒤 노출 판정 형태)
  </read_first>
  <behavior>
    - 2차 견적 합계 48,000,000으로 승인 → `listRevenue().contract.amountKrw` = 48,000,000
    - 이어서 (a) 수량 변경 (b) 단가 변경 (c) 견적가 있는 새 줄 (d) 견적가 있는 줄 취소 (e) 견적가 있는 줄 보관 (f) 승인 전에 보관해 둔 견적가 있는 줄의 보관함 복원 — 각각 거부되고, **매번** 계약 금액이 48,000,000 그대로다
    - (g) 실행가 변경 · 견적 외 비용 새 줄 · 조정 새 줄 → 통과, 계약 금액 48,000,000 그대로
    - (h) USD 단가 줄이 있는 승인 차수에서 금액은 그대로 두고 환율만 1,350 → 1,400으로 보낸 저장 · 통화만 바꾼 저장 → 각각 거부, 계약 금액과 DB 합 그대로(엔지니어링 리뷰 GAP 1 — 04-40의 「다시 계산한 견적가 ≠ 저장값」 불변식의 계약 금액 쪽)
    - 승인 표시를 끄면 → `—` + `2차 고객 승인 전` · 수량을 고쳐 합계 50,000,000 → 다시 승인하면 50,000,000
    - 3차를 만들면 → `—` + `3차 고객 승인 전`(2차 합계로 대신하지 않는다 — 금지 항목)
    - 기획 PM viewer의 `listRevenue` → `issuedEntries` 있음 · `issuedTotalKrw` 있음(B-28) · `paidEntries` 키 없음 · 입금에서 파생된 모든 필드(`paidGrossTotalKrw`·`balanceKrw`·미수) 키 없음 · 경영관리(두 항목 공개) → 전부 있음
    - 기존 DB에서 시드를 다시 돌리면 기획 PM의 `revenue.issued_amount` 노출 행이 true가 된다
  </behavior>
  <action>
① `test/integration/contract-invariant.test.ts`(신규)에 불변식 `behavior` 여섯 줄을 쓴다 — 승인은 04-14의 `setCustomerApproval`에 그 순간의 `approvalBasis`(합계 · 내용 토큰 — ENG-D9)를 기준값으로 실어 켜고, 조작은 04-40이 만든 서버 경로(`saveQuoteLines`·`domain/archive`의 `restore`)를 그대로 부르며, 각 조작 뒤 `listRevenue`의 계약 금액과 DB의 승인 차수 견적 합(보관 제외)이 둘 다 승인값인지 단언한다(OV-1 — 사용자 D7 답의 약속을 계약 금액 쪽에서 증명). 준비·조작 도우미는 04-40의 테스트 파일 모양을 따르되 복사하지 말고 필요한 만큼만 쓴다.

② 이 테스트가 빨간데 04-40의 잠금이 원인이면 `systematic-debugging`으로 원인을 찾아 04-40 파일의 **그 자리만** 고치고 회귀 테스트(이 파일의 빨간 케이스)와 함께 SUMMARY 편차로 적는다(엔지니어링 리뷰 ENG-D11 — 범위는 늘리지 않는다). 계약 파생(`domain/revenue/index.ts`)이 원인이면(예: 보관 줄을 합에 넣음, 승인 순번이 아닌 차수를 봄) 여기서 고친다.

③ **노출 기본값(D-85).** `info-items.ts`의 `revenue.issued_amount` `staffDefault`를 true로 바꾸고 주석을 D-85로 고친다(입금액은 false 그대로).

④ **역산 경로 점검.** `REVENUE_DTO_SPEC`의 모든 필드를 입금 의존 여부로 나눠, 입금 합계·미수·초과 입금·잔액처럼 입금 값에서 파생되는 필드가 전부 `revenue.paid_amount`(또는 둘 다를 요구하는 방식)로 게이트되게 고친다 — 발행 항목만으로 게이트된 입금 파생 필드가 있으면 그것이 이 플랜의 누수다(금지 항목). **반대 방향(CEO 리뷰 B-28):** 지금 `issuedTotalKrw`는 입금에서 파생되지 않는데도 `revenue.paid_amount`로 게이트되어 D-85 뒤 PM이 서버가 계산한 발행 합계를 받지 못한다 — `revenue.issued_amount`로 옮긴다(`paidGrossTotalKrw`·`balanceKrw`는 입금 항목 그대로). 판단 표(필드 · 입금 의존 여부 · 게이트 항목)를 SUMMARY에 적는다.

⑤ 테스트: `revenue-entries.test.ts` (d)의 기대값을 「발행은 옴 · 입금은 필드 부재 · 입금 파생 필드 부재」로 **고친다**(지우지 않는다). `visibility.test.ts`에 시드 재실행 뒤 기획 PM 행 true 케이스.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/contract-invariant.test.ts test/integration/quote-approved-lock.test.ts test/integration/revenue-entries.test.ts test/integration/visibility.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 승인 뒤 어떤 조작(환율만·통화만 바꾼 저장 포함)으로 계약 금액이 움직였거나, 실행가·견적 외 비용·조정이 계약 금액을 바꿨거나, 미승인 현재 차수에서 이전 승인 합계가 보였거나, PM 응답에 입금 또는 입금 파생 필드가 남았거나, 발행 합계가 PM에게 빠졌거나, 시드 반영이 안 됐다</fails_when>
  </verify>
  <acceptance_criteria>
    - 조작 (a)~(f)와 (h)(USD 환율만·통화만 — GAP 1) 각각 뒤에 계약 금액과 DB 합이 승인값과 같다는 단언이 있다(OV-1) · (g)의 세 조작이 통과하고 계약 금액이 그대로다
    - 승인 해제 → 수정 → 재승인 경로가 새 합계를 계약 금액으로 보이고, 새 차수 뒤 미승인 현재 차수에서 `—`가 보인다(금지 항목)
    - `revenue.issued_amount`의 `staffDefault`가 true, `revenue.paid_amount`가 false다
    - PM 응답 키 집합 단언이 발행 줄·발행 합계 있음 · 입금 부재 · 입금 파생 필드 부재를 모두 본다(통합 d, B-28) · (d) 케이스가 삭제되지 않고 기대값만 바뀌었다
    - `REVENUE_DTO_SPEC`에서 `issuedTotalKrw`가 `revenue.issued_amount`로, `paidGrossTotalKrw`·`balanceKrw`가 `revenue.paid_amount`로 게이트되고 판단 표가 SUMMARY에 있다
    - 시드 재실행 반영 케이스가 있다
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>「계약 금액은 항상 승인값과 같다」가 승인 뒤 모든 쓰기·복원 경로에서 테스트로 고정됐고, PM이 발행액과 발행 합계를 받되 입금액과 그 파생값은 PM 응답 어디에도 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 매출 섹션 화면 — 발행 읽기 표 · 입금 표 부재 · 부제(D-85) + 폰 배치(DR-15) + 1024 미만 보기 전용(DR-36) + 저장 중 편집 잠금(계약 3 · DR-3) + 2행 꼬리 삭제(P0) + 화면 검증</name>
  <files>app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/revenue-section.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「적용 규칙 S6. 매출 섹션」 1282–1315행(발행 표 PM 읽기 · 입금 표 부재 · 부제 · 입금 셀 2행 두 묶음 · 폰 P1 · 1024 미만 보기 전용 · 힌트 줄 없음) · 「UI Considerations S6」 533–542행 · 「UI Considerations S19」 651행(저장 중 편집 잠금) · 「적용 규칙 S19」 1562–1565행 · Copywriting 356행(Empty — 발행 줄 / 입금 줄 — 1024 미만 사실만) · 361행 · 409행 · 432–436행(PNL-09) · 「SYSTEM.md 개정 ⑫」 900–902행((사) ⑴ 저장 중 잠금) · 922–924행(좁은 PC 보기 전용) — `grep -n "### S6. 매출 섹션"`로 줄 확인
    - `docs/designs/plant8-erp-phase4-design-review-260923.md` 결정 원장 3·15·36행(DR-3 · DR-15 · DR-36)
    - `app/(app)/projects/[id]/revenue-section.tsx`(전문 — Task 1 뒤. 열 선언의 `priority`·`secondaryLine`·`summary`, 입금 셀 2행 문자열 175행 근처)
    - `ui/table/Table.tsx` 195–205행(`secondaryLine`은 셀 안 2행 — 폰에서도) · 250–260행(접힌 줄 `p2Values` = P2 열의 `summary` 또는 `cell`) · `ui/table/Table.module.css` 173–190행(폰 P2·P3) · `ui/table/types.ts`(`summary`·`secondaryLine`) — **고치지 않는다**
    - `.planning/phases/04-project-quote-ledger/04-30-SUMMARY.md`(그룹 A — 계약 3·6 소유) · `04-30-PLAN.md` Task 2 ③④(Grep `saveLocked`·`useEditableWidth`): `QuoteLedger`가 `saveLocked = isExecuting`을 `RevenueSection`에 이미 넘기고 `Table.saveLocked`(`ui/table/save-lock.ts`의 `isGridActionAllowed`)로 발행·입금 표를 잠그며, `useEditableWidth()` 폭 판정을 `RevenueSection`에 넘겨 1024 미만 보기 전용 · 추가 버튼 미렌더 · EMPTY 사실만을 이미 만든다(계획) — 이 태스크는 그 배선을 확인하고, 이 플랜이 바꾼 표(파생 계약 금액 · D-85 · DR-15)에서 깨지지 않게 지킨다
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `RevenueSection` 렌더 자리와 넘기는 props를 찾아 범위만 Read
    - `test/e2e/revenue-section.spec.ts`(전문 — Task 1 뒤) · `playwright.config.ts`
  </read_first>
  <behavior>
    - 기획 PM(발행 읽기) → 발행 표가 흰 머리글 `<table>` + 캡션, 추가 버튼 없음, 0줄이면 `발행한 세금계산서가 없습니다 · 발행은 경영관리` · 입금 표·미수 표시가 DOM에 없음 · 부제 `공급가액 기준`
    - 경영관리 1280 → 두 표 격자 + 「발행 줄 추가」·「입금 줄 추가」 · 부제 `공급가액 기준 · 입금액만 통장 합계` · 외화 입금 셀 2행이 `USD 4,400.00 @1,318.1818` · `공급가액 12,400,000` 두 묶음이고 `서버 계산` 글자가 없다
    - 경영관리 1000(700~1023) → 두 표가 흰 머리글 읽기 표, 추가 버튼 없음, 금액 셀 `Enter`·클릭이 편집을 열지 않음, 두 표 0줄 EMPTY가 `발행한 세금계산서가 없습니다` / `입금 줄이 없습니다`(버튼 없음)
    - 경영관리 375 → 두 표의 보이는 열이 날짜·금액 둘, 금액 셀 안에 2행이 보이지 않음, 입금 행 접힌 줄이 외화 묶음 · 공급가액 묶음 · 메모 순, 숫자 묶음의 계산된 `white-space`가 `nowrap`, 문서 가로 스크롤 0
    - 저장 중 잠금(DR-3): 경영관리가 발행 금액을 고치고 Ctrl+S → 테스트가 그 서버 액션 응답을 붙잡아 두는 동안(Playwright `page.route`) 발행 표 격자 `aria-busy="true"` · 다른 발행 셀에서 `Enter`·글자 입력이 편집을 열지 않음 · 방향키 이동은 됨 · 「발행 줄 추가」 비활성 → 응답을 풀면 즉시 편집이 열린다
    - 매출 표 아래에 힌트 줄이 없다
  </behavior>
  <action>
① **D-85 화면.** `revenue-section.tsx`: 쓰기 권한이 없는 사람에게 발행 표는 흰 머리글 캡션 읽기 표(편집 셀 0 — `ui/table` 자동)이고 추가 버튼이 없으며 EMPTY `발행한 세금계산서가 없습니다 · 발행은 경영관리`. 입금 표는 DTO에 필드가 없으면 섹션에서 통째로 렌더하지 않는다. 부제는 입금 표가 있을 때만 `공급가액 기준 · 입금액만 통장 합계`, 없으면 `공급가액 기준`.

② **2행과 폰 배치(DR-15 · P0).** 입금액 셀 2행 문자열의 `· 서버 계산` 꼬리를 지워 `공급가액 {금액}`으로 두고, 외화 입금이면 `USD … @…` 묶음과 `공급가액 …` 묶음을 각각 줄바꿈 없는 묶음(`project-detail.module.css`의 한 클래스 — `white-space: nowrap`, 새 토큰 없음) 두 개로 ` · ` 사이에서만 줄바꿈되게 렌더한다. 폰(<700): 두 표의 P1은 날짜·금액 두 열 그대로이고, 금액 열 `secondaryLine`의 내용은 700 미만에서 숨기는 클래스로 감싼다 — 대신 P2 열인 메모의 `summary`(접힌 줄 전용 값 — `ui/table/types.ts`)가 입금 행이면 외화 묶음 · 공급가액 묶음 · 메모 순, 발행 행이면 메모를 돌려준다(같은 정보를 두 번 보이지 않는다 — §7-3). `ui/table`을 고치지 않는다(이 화면의 열 선언과 CSS만). 계약 금액 2행(Task 1)도 ` · ` 묶음 단위로 줄바꿈한다.

③ **1024 미만 보기 전용(DR-36 · 계약 6).** 04-30이 `RevenueSection`에 넘긴 `useEditableWidth()` 판정(두 번째 폭 판정을 만들지 않는다)이 이 플랜의 발행·입금 표에서도 그대로 도는지 확인한다 — 1024 미만이면 두 표의 셀 편집 가능성이 편집 아님이라 `ui/table`이 읽기 표로 그리고 「발행 줄 추가」·「입금 줄 추가」가 없으며 EMPTY는 버튼 없는 사실만(`발행한 세금계산서가 없습니다` / `입금 줄이 없습니다` — Copywriting 1024 미만 행)이다. Task 1·①의 D-85 읽기 표 분기와 겹쳐도 판정이 하나로 모이게 한다(쓰기 권한 없음 또는 1024 미만 → 읽기). 04-30이 이 배선을 두지 않았으면 `QuoteLedger`에서 `useEditableWidth()` 값을 넘기는 한 자리만 더하고 SUMMARY 편차로 적는다(ENG-D11).

④ **저장 중 편집 잠금(DR-3 · 계약 3).** `RevenueSection`의 `saveLocked` prop(04-30이 `QuoteLedger`에서 넘긴다)이 이 플랜이 바꾼 두 표의 `Table.saveLocked`까지 그대로 가는지 확인한다(편집 진입 무반응 · 이동·선택·복사 허용 · `aria-busy`) — 없으면 `QuoteLedger`가 계산한 `saveLocked`를 넘기는 한 자리만 더한다(새 값을 계산하지 않는다, ENG-D11). 두 3차 추가 버튼은 저장 중 비활성이다(S3 loading — 서버 액션 중 다른 버튼 비활성, 이유 글자 없음). 매출 표에 힌트 줄을 두지 않는다(DR-31 — 확인만).

⑤ **E2E(`revenue-section.spec.ts` 갱신).** `behavior` 전부 — 1000·375는 `page.setViewportSize`, 저장 중 잠금은 서버 액션 요청을 `page.route`로 붙잡아 두는 동안 단언한다. Task 1의 계약 금액 케이스와 같은 파일이다.

⑥ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S6 backstop 셋을 DOM 실측한다 — (a) **1280 · 1024 · 375** 발행·입금 표와 계약 금액 2행의 가로 스크롤 0 · 숫자 꺾임 없음 · 375 P1 두 열과 접힌 줄 묶음 줄바꿈(최장 외화 · 13자리 금액 · 긴 메모) (b) 긴 메모의 PC 한 줄 말줄임 · 폰 접힌 줄 줄바꿈 (c) 1000에서 읽기 표 · 추가 버튼 부재 → 수정(`project-detail.module.css`만) → `CI=true pnpm test` 한 번. 폭별 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/revenue-section.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — PM에게 입금 표·미수가 보이거나 발행 읽기 표·EMPTY·부제가 계약과 다르거나, 입금 셀 2행에 옛 꼬리가 남았거나 두 묶음이 아니거나, 375에서 P1이 두 열이 아니거나 2행이 셀 안에 보이거나, 1000에서 편집·추가가 되거나 EMPTY에 버튼이 있거나, 저장 중에 발행 셀이 편집에 들어가거나 응답 뒤 풀리지 않거나, 견적 표 흐름이 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S6 판정 셋(1280 · 1024 · 375 · 1000)을 SUMMARY에서 확인</test>
      <expected>세 폭 가로 스크롤 0 · 숫자 꺾임 없음 · 375 P1 두 열 · 접힌 줄 묶음 줄바꿈 · 긴 메모 PC 말줄임 · 1000 읽기 표와 추가 버튼 부재</expected>
      <why_human>폭·말줄임·묶음 줄바꿈은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 발행 표가 쓰기 권한 없는 사람에게 읽기 표 · 추가 버튼 없음 · `· 발행은 경영관리` EMPTY이고, 입금 표가 DTO에 없으면 섹션에서 빠지며 부제가 그에 따른다(E2E)
    - `revenue-section.tsx`에 `서버 계산` 글자가 없고 외화 입금 2행이 두 묶음이다(E2E — 글자 부재 단언은 화면 DOM 대상)
    - 375에서 P1 두 열 · 2행이 접힌 줄 · 숫자 묶음 `nowrap`이고 `ui/table` diff에 이 태스크의 변경이 없다(DR-15)
    - 1000에서 두 표가 읽기 표이고 추가 버튼·EMPTY 버튼이 없으며, 폭 판정이 그룹 A의 1024 게이트 하나다(DR-36)
    - `RevenueSection`이 `saveLocked`를 받아 두 표에 넘기고, 저장 중 발행 셀 편집 진입이 없으며 응답 뒤 풀린다(E2E, DR-3)
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>PM이 발행액을 읽기 표로 보고, 매출 표가 폰에서는 날짜·금액 중심으로 숫자를 꺾지 않고, 좁은 PC에서는 보기만 하며, 저장 중에는 편집에 들어가지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain 출구(RevenueDto) → 화면 | 발행·입금 필드가 정보 노출표를 지나야 실린다. 파생 필드가 역산 통로가 될 수 있다 |
| 브라우저 → 매출 섹션 | 화면에서 계약 금액 입력이 사라진다(서버 쪽 필드 제거는 04-41) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-83 | Information Disclosure | 입금액 역산 | high | mitigate | 입금 표와 입금 파생 필드(미수·초과 입금·잔액)를 전부 `revenue.paid_amount`로 게이트하고, PM 응답 키 집합에서 부재를 통합 테스트로 단언한다(금지 항목). 발행 합계는 입금에서 파생되지 않으므로 발행 항목으로 옮긴다(B-28). D-38 누수 스캔 레지스트리가 DTO 필드별 항목을 덮는다 |
| T-04-85 | Tampering | 파생 계약 금액 | medium | mitigate | 서버가 현재 차수·승인·합계(숫자 SUM — B-18)·부가세(승인일의 KST 날짜 기준 — B-27, 경계일 케이스)를 계산하고 화면은 표시만 한다. 이전 승인 차수로 대신하지 않는다(금지 항목). 승인 뒤 합계 불변은 04-40의 잠금 + 이 플랜의 불변식 테스트(OV-1) |
| T-04-85b | Information Disclosure | 계약 금액 필드 | medium | mitigate | 계약 금액은 견적 합계라 `quote.amount`로 게이트한다 — 견적 금액을 숨긴 계급에 `project.value`만으로 새지 않는다(B-19, 키 부재 통합 케이스) |
| T-04-85c | Tampering | 1024 미만 보기 전용 · 저장 중 잠금(화면 쪽) | low | accept | 둘 다 편집 손실을 막는 화면 동작이다(DR-36 · DR-3) — 권한 판정이 아니다. 매출 쓰기 권한·검증은 서버(`saveRevenue` — 04-41)가 폭·잠금과 무관하게 한다 |
| T-04-87 | Information Disclosure | 노출표 기본값 반영 | low | mitigate | 시드 upsert가 기본 계급 행에만 적용되는 기존 규약을 그대로 쓰고, 시드 재실행 반영을 통합 테스트로 고정한다. 팀장·본부 책임자 행은 시드 밖이라 관리자 확인 항목으로 SUMMARY에 남긴다(B-29) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |

(옛 T-04-84 「옛 계약 금액 필드」·T-04-86 「컬럼 삭제 배포 창」은 쓰기 경로·컬럼을 지우는 04-41로 옮겼다.)
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 계약 금액 불변식 테스트 초록
- `package.json` 의존성 객체(dependencies·devDependencies·optionalDependencies·peerDependencies)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있다 (원문 비교 아님 — 04-03 재개 시 scripts 추가로 깨지지 않게)
</verification>

<success_criteria>
- 계약 금액이 고객 승인된 현재 차수의 견적 합계에서만 나오고, 승인 뒤에는 어떤 쓰기로도 움직이지 않으며, 화면에 입력 칸이 없다
- PM이 발행액과 발행 합계를 보고 입금액과 그 파생값은 보지 못한다
- 04-41이 서버 쓰기 경로와 컬럼을 지워도 화면·파생이 바뀌지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 타입(변경) | `ContractInfo` — `amountKrw \| null` · `vatKrw \| null` · `totalKrw \| null` · `sourceLabel` · `pendingLabel` |
| 함수(repository) | `sumQuoteAmountByRevision`(`repositories/quote-lines.ts` — `::bigint` + `mapWith(Number)`) |
| 삭제된 심볼(화면) | `QuoteLedger` 계약 draft·dirty 합류·props · `RevenueSection` 계약 금액 `Form.Field`(통화·환율 칸 포함) |
| 정보 노출 기본값 | `revenue.issued_amount` staffDefault false → **true** |
| DTO 게이트 변경 | `contract` → `quote.amount`(B-19) · `issuedTotalKrw` → `revenue.issued_amount`(B-28) |
| 변경된 기존 심볼 | `listRevenue`(파생 계약 · 승인일 기준 부가세) · `RevenueSection`(KvList 계약 한 줄 · 발행 읽기 표 · 입금 표 조건 렌더 · 부제 · 입금 셀 2행 두 묶음(`· 서버 계산` 삭제) · 폰 접힌 줄 `summary`(DR-15) · 1024 게이트 prop(DR-36) · `saveLocked` prop(계약 3 · DR-3)) · 04-22의 `canSave`(계약 칸 항 제거) · `project-detail.module.css`(숫자 묶음 `nowrap` · 폰 2행 숨김 클래스 — 새 토큰 없음) |
| 테스트 | `test/integration/contract-invariant.test.ts`(신규) · `revenue-entries.test.ts`(파생·키 집합·(d) 기대값) · `visibility.test.ts` · `revenue-section.spec.ts` 갱신(계약 금액 · D-85 읽기 표 · 375 배치 · 1000 보기 전용 · 저장 중 잠금) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 계약 금액 서버 쓰기 경로 제거 · `projects.contract_*` DROP 마이그레이션(RAISE 가드 · squawk 인라인 무시) · 0010 상태 업그레이드 테스트 케이스 · ARCHITECTURE §5 예외·「Phase 4 한 번에 배포」 기록 · 계약 참조 스캔 · 「옛 필드는 스키마가 무시」 케이스(04-41) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| OV-1 · 사용자 D7 (T4) — 계약 금액 = 승인값 약속 | HIGH | Plan 04-16, Task 2: 승인 뒤 여섯 조작 거부 · 매번 계약 금액·DB 합 불변 · 실행가 무영향 · 해제/재승인 · 새 차수 |
| B-18 — 계약 SUM이 문자열로 와 부가세가 이어 붙음 | WARNING | Plan 04-16, Task 1 ①-b: `sumQuoteAmountByRevision` `::bigint` + `mapWith(Number)`, 30억 케이스 |
| B-19 — 계약 금액이 `project.value`로 게이트됨 | WARNING | Plan 04-16, Task 1 ①: `quote.amount`로 이동 + 키 부재 케이스 |
| B-27 — 계약 부가세 기준일이 오늘 | NOTE | Plan 04-16, Task 1 ①: `asOf` = 승인일 + 세율 행 추가 케이스 |
| B-28 — 발행 합계가 입금 항목으로 게이트됨 | NOTE | Plan 04-16, Task 2 ④(재배치 전 Task 3 ②): `issued_amount`로 이동, 키 집합 단언 |
| B-29 — 팀장·본부 책임자 발행액 기본 숨김(시드 밖) | NOTE | 이미 반영(T-04-87) — `<probe_fallback>`·truth에 관리자 확인 항목으로 명시 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B §1 — 부가세 기준일이 KST 경계에서 하루 앞 | P2 | Plan 04-16, Task 1 ①: `asOf = new Date(kstDateOf(approvedAt))` · behavior 경계일 케이스(GAP 4) |
| GAP 1 — 승인 차수 USD 환율만·통화만 바꾼 저장(계약 금액 쪽) | CRITICAL GAP | Plan 04-16, Task 2 behavior (h) (잠금 판정은 Plan 04-40) |
| 낡은 주석 `domain/revenue/index.ts` 169행 | P3 | Plan 04-16, Task 1 ① |
| ENG-D11 (R10) — 멈춤 규칙 완화 | DECISION | Plan 04-16, Task 2 ② |

### Round 3 — 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-15 — 폰 375 매출 표: 날짜·금액·메모 모두 P1, 외화와 공급가액이 같은 보조 줄(Codex) | HIGH | Plan 04-16, truths · Task 3 ②⑤⑥: P1 날짜·금액 · 2행은 접힌 줄(메모 `summary`) · 외화·공급가액 각각 `nowrap` 묶음 · 375 E2E + DOM 감사 (`ui/table` 무변경) |
| DR-36 — 700~1023 매출 표 보기 전용 · 1024 미만 EMPTY 사실만 | 후속 | Plan 04-16, truths · Task 3 ③: 그룹 A 1024 게이트 재사용 · 1000 E2E |
| DR-3 · 계약 3 — 저장 중 입력이 응답에 덮임(매출 표) | CRITICAL | Plan 04-16, truths · Task 3 ④: `RevenueSection`이 `saveLocked`를 받아 두 표에 넘김 · 저장 중 편집 진입 없음 E2E |
| P0 — 입금 셀 2행 `· 서버 계산` 꼬리 | LOW | Plan 04-16, Task 3 ②: 삭제 |
| DR-12 — 확인 모달 공용 컴포넌트(매출 줄 삭제) | — | 해당 없음 — 매출 줄 삭제 동작이 없다(rev 5 S16 열두 사용처에 없음) |
| DR-14 — DOM 감사 폭 1024 | HIGH | Plan 04-16, Task 3 ⑥: 1280 · 1024 · 375 + 1000 |
| 태스크 재배치 | — | 옛 Task 3의 서버 몫(노출 기본값 · DTO 게이트 · 역산 점검 · 통합 (d) · 시드)을 Task 2로, Task 3은 화면만 — 모든 옛 수용 기준은 그대로 옮겨졌다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-09 · 사용자 D9 · OV-6 (T14) — DROP 가드·예외 기록·업그레이드 테스트 | 파일 수 때문에 분할 — Plan 04-41(바로 뒤)이 전부 맡는다 |
| 엔지니어링 리뷰 §4 — `listRevenue` 설정 N+1(줄마다 세금 설정 약 4회) | TODO(P3) — 한 호출 안에서 `asOf` 날짜별 설정 메모이즈. 10~30명 규모에서 허용, 이 플랜은 구현하지 않는다 |
| 매출 쓰기 경로의 KRW 환율·재전송 중복·프로젝트/종류 조건(엔지니어링 리뷰 B §2 · ENG-D10 · Codex #1) | 쓰기 쪽이라 Plan 04-41 Task 3 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-16-SUMMARY.md` when done
</output>
