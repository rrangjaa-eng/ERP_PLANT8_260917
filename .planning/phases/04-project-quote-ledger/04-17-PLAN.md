---
phase: 04-project-quote-ledger
plan: 17
type: execute
wave: 25
depends_on: ["04-16", "04-40", "04-41", "04-29"]
files_modified:

  - repositories/projects.ts
  - domain/projects/index.ts
  - domain/projects/list-view.ts
  - test/unit/domain/project-list-view.test.ts
  - app/(app)/projects/page.tsx
  - app/(app)/projects/list-totals.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/projects.module.css
  - test/integration/projects-list.test.ts
  - test/integration/project-auto-settlement.test.ts
  - test/integration/quote-lines.test.ts
  - test/e2e/projects-list.spec.ts

autonomous: true
requirements: [PROJ-01, UX-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/projects`를 조건 없이 열면 올해(KST) 보기다 — 연도 필터 값이 올해이고, 프로젝트 기간이 올해와 겹치는 행이 보인다. 해를 걸친 프로젝트는 겹치는 해마다 목록에 나온다(D-89)"
    - "연도 선택지 맨 위에 `전체 연도`가 있고 고르면 연도 범위가 없다(URL `year=all`) — 조건 없는 URL은 전체 연도가 아니라 올해다(D-89)"
    - "합계는 표 **위** 한 줄이다 — 필터 줄 바로 아래 `합계 (2026 귀속 · 48건)` + `매출 · 견적 · 실행가 · 수익금 · 수익률` 쌍, 아래 `2px --line-strong`. 표 아래 합계 행과 「더 보기 50건」은 없다(D-88, SYSTEM.md 개정 ③)"
    - "합계는 페이지와 무관하게 필터 전체 기준이고 집계 쿼리 한 번으로 나온다 — 목록 쿼리와 같은 행 필터 함수를 쓴다(D-88·D-91, T-04-28 유지)"
    - "목록 합계는 21억 원을 넘어도 오류가 나지 않고 정확하다 — 돈 합계는 `bigint`로 더해 리포지토리 경계에서 JS 숫자로 바꾸고, 건수는 정수다. 리포지토리는 모든 집계·행 금액을 문자열이 아닌 숫자로 돌려준다(CEO C-01 — 지금은 `::int` 캐스트가 21억 초과에서 목록 전체를 오류 화면으로 만들고, 합계가 문자열로 와서 쉼표 없이 보인다)"
    - "합계에는 귀속 연도(종료일의 해)가 보기 범위 안인 행만 전액 들어간다. 범위 밖에서 끝나는 행은 보이되 합계에서 빠지고, 기간 칸 2행에 `2027 귀속`(기간 보기면 `2026-11 귀속`)이 붙으며, 합계 줄 오른쪽에 `2027 귀속 3건 제외`(기간 보기면 `기간 밖 귀속 N건 제외`)가 붙는다(D-90, Copywriting 표시 — 귀속·목록 합계 줄)"
    - "종료일이 없는 행은 맨 아래 `기간 미정` 그룹에 보이고 언제나 합계 밖이며 `기간 미정 N건 제외`로 적힌다. 0건인 제외 표시는 쓰지 않는다(D-90)"
    - "괄호의 건수는 합계에 든 행 수이고, 표에 보이는 전체 행 수는 페이지 줄의 `/ N건`이 말한다. 상태 필터가 있으면 `합계 (진행 · 2026 귀속 · 12건)`, 전체 연도면 `합계 (전체 연도 · 125건)`, 기간 필터면 `합계 (2026-09-01 ~ 2026-10-31 귀속 · 12건)`(Copywriting 표시 — 목록 합계 줄)"
    - "행마다의 수익금 기준은 **상태가 정산(`settling`)·완료(`completed`)이고 발행 줄이 1개 이상일 때만 `발행`**(기준 금액 = 발행 합계, 음수 줄 포함)이고, 그 밖은 전부 `견적`이다 — 수주중·진행·미수주는 발행 줄이 있어도 견적 기준이라 부분 발행 순간 수익금이 크게 음수로 뒤집히지 않고(견적 1억·실행가 8천만·발행 3천만인 진행 건 = +2천만), 정산·완료라도 발행 줄이 0개면 견적 기준(매출 `—`, 수익금 = 견적 − 실행가)이라 −실행가로 떨어지지 않는다. 판정은 순수 함수 `profitBasisFor(status, issuedCount)` 하나와 그와 같은 SQL 식이고 통합 테스트가 다섯 상태 × {발행 0, 1 이상}에서 둘이 같음을 단언한다(교차 그룹 계약 7, DR-8 · DR-38 — CONTEXT D-87 계산 규칙 개정, UI-SPEC rev 5 적용 규칙 S1 「기준」 · UI Considerations S1 partial ②, Copywriting 「표시 — 수익금 기준」)"
    - "합계 수익률 = 합계 수익금 ÷ 각 행 기준 금액(위 규칙으로 고른 견적 또는 발행 합계)의 합이고, 그 분모가 0 **이하**면 `—`다 · 행 수익률도 기준 금액 ≤ 0이면 `—` — 수정세금계산서로 발행 합계가 음수가 된 행이 부호가 뒤집힌 비율을 만들지 않는다. 전부 서버 계산이다(D-87·D-88, DR-8 · DR-38, 사용자 D19 — C-16, UI-SPEC rev 5 적용 규칙 S1 「합계 수익률」)"
    - "매출·기준은 발행(`issue`) 줄만 더한다 — 입금(`payment`) 줄은 매출·수익금에 들어가지 않는다(D-85 — 입금 줄이 있는 픽스처로 단언, CEO C-14)"
    - "합계 줄은 받은 금액만 쓴다 — 견적·실행가는 `quote.amount`, 매출은 `revenue.issued_amount`, 수익금·수익률은 두 항목을 모두 볼 수 있을 때만 서버가 보낸다(발행 기준 수익금 + 실행가 = 발행 합계 역산). 둘 다 못 보면 금액 쌍이 하나도 없다. 합계 DTO는 DTO 레지스트리에 등록돼 누수 스캔이 보고, 수익금·수익률 필드는 **all-of 정보 항목** `[quote.amount, revenue.issued_amount]`(04-32)로 명세한다 — 손으로 키를 빼는 코드가 없다(S1 partial ①, D-38, CEO C-14, ENG-D3 ②)"
    - "목록 행도 `project()` 투영을 거친다 — `loadProjectList`가 리포지토리 행을 `projectMany(viewer, rows, PROJECT_LIST_DTO_SPEC)`로 투영한 뒤에만 화면(클라이언트 컴포넌트 props — RSC 페이로드)에 넘긴다. 행 금액 칸(`revenueKrw`·`profitRate` 등)이 리포지토리 행에 생겨도 명세 밖이면 응답에 없고, 금액을 볼 수 없는 계급의 행에는 금액 키가 없다 — 키 집합 테스트가 `loadProjectList` 경로를 본다(엔지 리뷰 C §1 P1)"
    - "목록 요청의 조회 수는 행 수와 무관하다 — 노출 판정이 정보 항목마다 한 번이라(`projectMany`) 1행 보기와 50행 보기의 DB 호출 수가 같다(ENG-D3 ② · 엔지 리뷰 C §1 P2 — 지금은 50행 × 17필드 ≈ 850회)"
    - "목록·합계의 견적·실행가 합은 **현재 차수의 줄만** 모아 더하고, 발행 합은 행 필터를 지난 프로젝트마다 구한다 — 견적 줄·발행 줄 표 전체를 요청마다 GROUP BY하지 않는다(엔지 리뷰 C §4 P2). 기준 쿼리 계획(`EXPLAIN`)을 SUMMARY에 남긴다"
    - "보관된 프로젝트는 **누구에게나** 목록과 합계에서 빠진다 — 보관함을 볼 수 있는 시스템 관리자도 같다(사용자 D18 — C-21)"
    - "목록은 50건씩 번호 페이지다 — 50건 이하면 페이지 줄이 없고 51건부터 표 아래 `51–100 / 125건  이전 · 1 2 3 · 다음`이 생긴다(04-29 `Pagination` 링크 갈래). URL은 `?page=N`이고 필터·정렬 파라미터를 보존하며 옛 `count` 누적 파라미터는 없어졌다(D-91, S12)"
    - "범위 밖 페이지 번호(`?page=99`)는 오류 화면이 아니라 마지막 페이지로 보인다. 숫자가 아니거나 1보다 작으면 1쪽이다(S12 error)"
    - "종료월 그룹이 페이지를 넘으면 다음 페이지 맨 위에 같은 그룹 머리글이 다시 보인다(D-91, SYSTEM.md 개정 ③)"
    - "목록 합계에서 보관된 견적 줄과 발행 줄은 빠진다 — 상세 합계 행과 같은 줄 집합이다(04-12 보관 경로와의 정합, CEO A-04)"
    - "목록 화면은 한 요청에서 자동 전환 판정을 **먼저 한 번**(04-11의 `settleForProjectList` — 행 범위 확인 뒤) 하고 합계 → 페이지 보정 → 목록 순으로 읽는다 — 판정이 실패해도 목록은 저장된 상태로 그려진다. 옛 도메인 입구 `listProjects`·`aggregateProjects`는 지워지고 `loadProjectList`가 유일한 입구다(04-11 fail-open, CEO A-07·C-09·C-23, 엔지 리뷰 C §1 P1)"
    - "연간 합계·귀속 구간은 읽기 시점 집계이고 저장 컬럼이 없다 — 날짜 원본(`start_date`·`end_date`·`revenue_entries.entry_date`)에서 매번 계산하므로, Phase 9·10이 연도별 손익·인센티브의 연도 기준을 다시 정해도 마이그레이션이 필요 없다(D-92의 이 페이즈 제약. 연도 기준 결정 자체는 이 페이즈 범위 밖)"
    - "합계 줄·페이지 줄의 숫자는 `lib/format-number.ts`로 쉼표가 붙고, 연도(`2026`)·`2027 귀속`·기간 칸 날짜에는 쉼표가 없다(D-95, S15)"
    - statement: "N=1/50/51/125 렌더에서 1과 125가 같은 `합계 (… · N건)` 형식이고, 50 이하는 페이지 줄 없음, 51부터 있음, 제외 0건이면 제외 표시 없음 — 통합·E2E 렌더(N=0 빈 목록 세 갈래는 04-48)"
      verification: backstop
    - "이 플랜의 끝은 `CI=true` 프로덕션 빌드의 목록 E2E로 판정한다 — 필터 줄·빈 목록·폰 첫 화면·로딩과 목록 화면 전체의 독립 DOM 감사(합계 줄·페이지 줄의 폰 줄바꿈 backstop 포함)는 04-48(엔지 r2 E2-01 분할)이 맡고, 04-17과 04-48은 같은 머지 묶음 ④ 안이라 그 사이 상태는 배포되지 않는다"
  prohibitions:
    - statement: "합계가 사용자가 볼 수 없는 행이나 필드를 더해서는 안 된다. 목록과 합계가 다른 행 필터를 쓰면 합계가 그 사람에게 가려진 프로젝트의 금액을 드러낸다 — 귀속 조건은 같은 행 필터 **위에** 얹는 추가 분류여야 하고, 수익금·수익률은 매출과 견적을 둘 다 볼 수 있는 사람에게만 가야 한다"
      status: resolved
      verification: test
    - statement: "같은 프로젝트가 두 해의 합계에 두 번 들어가서는 안 된다. 해를 걸친 프로젝트는 겹치는 해마다 목록에 보이지만 전액은 종료일의 해 한 곳에만 들어간다 — 해마다 나눠 넣거나 양쪽에 넣으면 연간 합계를 더한 값이 회사 전체 합계를 넘는다(D-90)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/list-view.ts"
      provides: "보기 범위 · 귀속 라벨 · 합계 제목 · 제외 문구 · 구간 건수 합 · 수익금 기준 판정(계약 7) — 순수 함수(쪽 보정·쪽 수는 `lib/paging` — 04-29). 기간 칸 서식 · 기간 필터 판정 · 연도 선택지 · URL 파라미터 정규화 · 연도 자동 전환(DR-30) · 폰 필터 요약(DR-26)은 04-48이 같은 파일에 더한다"
      exports: ["resolveListRange", "attributionLabel", "totalsTitle", "exclusionText", "bucketTotal", "profitBasisFor"]
    - path: "app/(app)/projects/list-totals.tsx"
      provides: "표 위 합계 줄(dl) + 제외 표시"
      contains: "<dl"
    - path: "repositories/projects.ts"
      provides: "겹침 필터 · 기간 미정 노출 규칙 · 보관 프로젝트 항상 제외 · 발행 합계 · 기준(정산·완료 + 발행 줄 ≥ 1일 때만 발행 — 계약 7)·수익금·수익률 · 귀속 구간별 집계 한 번 · OFFSET 페이지 · 모든 집계가 JS 숫자(bigint → Number)"
      contains: "mapWith(Number)"
    - path: "test/integration/projects-list.test.ts"
      provides: "겹침·귀속·제외·이중 계산 금지·페이지·범위 밖 페이지·보관 줄·보관 프로젝트 제외·합계 필드 노출(셋 + 없음)·21억 초과·입금 줄 제외 증명(틀린 파라미터·기간 보기·`none` 갈래는 04-48이 같은 파일에 더한다)"
  key_links:
    - from: "repositories/projects.ts"
      to: "repositories/projects.ts"
      via: "listProjectsPage와 aggregateProjects가 같은 projectFilterConditions를 부르고, 집계는 그 위에 귀속 구간 CASE만 얹는다"
      pattern: "projectFilterConditions"
    - from: "domain/projects/index.ts"
      to: "domain/projects/auto-transition.ts"
      via: "loadProjectList가 집계·목록보다 먼저 `settleForProjectList`(04-11)로 자동 전환을 한 번 적용하고 리포지토리 함수를 직접 부른다 — 판정이 요청마다 여러 번 돌지 않는다"
      pattern: "settleForProjectList"
    - from: "domain/projects/index.ts"
      to: "domain/permissions/project.ts"
      via: "목록 행을 `projectMany(viewer, rows, PROJECT_LIST_DTO_SPEC)`로 투영한 뒤에만 넘긴다(엔지 리뷰 C §1 P1 · ENG-D3 ②)"
      pattern: "projectMany"
    - from: "domain/projects/index.ts"
      to: "lib/paging.ts"
      via: "쪽 보정·쪽 수·쪽 크기를 공용 모듈에서(04-29 — 04-19·04-07과 같은 함수)"
      pattern: "clampPage"
    - from: "app/(app)/projects/page.tsx"
      to: "ui/pagination/Pagination.tsx"
      via: "서버 페이지가 04-29 컴포넌트의 href 함수 갈래로 페이지 줄을 렌더한다(필터·정렬 보존)"
      pattern: "Pagination"
    - from: "domain/projects/index.ts"
      to: "lib/kst-date.ts"
      via: "loadProjectList의 기본 연도가 주입 가능한 now에서 KST로 나온다"
      pattern: "kstYear"
    - from: "domain/projects/index.ts"
      to: "domain/permissions/dto-registry.ts"
      via: "`ProjectListTotals`가 `registerDto`로 등록돼 누수 스캔이 합계 키를 본다(CEO C-14)"
      pattern: "registerDto"
---

<objective>
**As a** 경영관리와 기획 PM, **I want to** 프로젝트 목록을 열면 올해 걸친 프로젝트와 그 합계가 표 위에 먼저 보이고, 해를 걸친 프로젝트는 겹치는 해마다 보이되 합계는 끝나는 해에만 들어가며, 125건을 번호 페이지로 넘기고 싶다, **so that** 인트라넷처럼 합계를 먼저 읽고, 연간 합계를 더해도 이중 계산이 없으며, 「더 보기」를 누적하다 위치를 잃지 않는다.

머지 묶음: ④/4 목록·리저브 — 웨이브 25~30(04-17 → 04-31)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

이 플랜은 04-05가 만든 목록(전체 연도 기본 · 종료일 연도 단일 매칭 · 표 아래 합계 · 「더 보기」 누적)을 D-88·D-89·D-90·D-91(목록 부분)로 고친다. 옛 D-51의 「더 보기 50건」과 전체 연도 기본은 **대체됐다**(D-51의 전체 상태 기본·종료월 그룹은 유지). 열 구성(클라이언트 열·매출·기준·수익금·수익률 D-87)·머리글 정렬·좁은 폭 열 접기·`종료일 지남`(D-81 목록)은 04-18, 견적 줄 30줄 페이지는 04-19다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE):** C-01(21억 초과 합계 오류·문자열 합계 — 선행 버그) · C-08(틀린 URL 파라미터가 오류 화면) · C-09·A-07·C-23(판정 한 번 → 합계 → 보정 → 목록, fail-open) · C-10(진행 막대 서술 정정 — 사용자 D17로 만들지 않고 04-08이 DECISIONS에 기록) · C-11(기간 미정 기본값 — 사용자 D19 확정) · C-14(입금 줄 픽스처 · 키 집합 「없음」 · 합계 DTO 등록) · C-16(기준 ≤ 0 → `—`, 사용자 D19) · C-17(E2E 연도 `kstYear`) · C-21(보관 프로젝트 항상 제외, 사용자 D18) · C-24(필터 바꿔도 정렬 유지 — 04-48) · C-26(04-17~04-19는 한 배포 — 머지 묶음 ④에 함께, 04-47·04-48 포함) · A-04(보관 줄 제외 확인). C-08·C-24·DR-26·DR-30의 화면 쪽은 엔지 r2 분할(E2-01)로 04-48이 맡는다. **순서(T16 — 웨이브 22):** `repositories/projects.ts`·`domain/projects/index.ts`·목록 `page.tsx`를 먼저 고치는 그룹 B의 04-15·04-40(04-14 분할, 쓰기 직렬화)·04-16·04-41(계약 칸 삭제) 뒤에 서고, 견적 표 쪽 나눔 04-19와는 겹치는 파일이 없어 같은 웨이브에서 병렬로 돈다. **파일 상한 분할:** `ui/pagination`은 04-29로 앞당겨 만들었고(C-27 포함) 이 플랜은 목록에 꽂기만 한다. 상세는 끝의 Review Dispositions Ledger.

Purpose: 목록의 숫자가 **어느 범위의 무엇**인지가 이 플랜에서 정해진다. 04-18이 행 단위 금액 열을 올리려면 같은 SQL(발행 합계·기준·수익금)이 먼저 있어야 한다.

Output: 목록 범위·귀속·합계·페이지·파라미터의 순수 함수 모듈 · 리포지토리 확장(숫자 경계 포함) · 목록 화면 개정 · 단위 1 · 통합 1(확장) · E2E 1(개정).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`):** (a) **엔지 리뷰 C §1 P1 — 목록 행 투영:** 새 입구 `loadProjectList`가 리포지토리를 직접 부르는데 행을 `project()`로 투영하라는 단계가 없었고 키 집합 테스트는 옛 `listProjects`를 보고 있었다 — `projectMany`로 투영하고, 키 집합 테스트를 새 입구로 옮기며, 옛 도메인 `listProjects`·`aggregateProjects`를 지운다(그 호출자 테스트 셋 — 04-05·04-11·04-12가 쓴 — 을 이 플랜이 새 입구로 옮긴다, ENG-D11). (b) **ENG-D3 ②:** 목록 행 노출 판정 N+1(50행 × 17필드) → `projectMany` · 합계 DTO 수익 두 칸을 all-of 정보 항목으로. (c) **엔지 리뷰 C §4 P2:** 견적 줄 합·발행 합을 현재 차수·필터 뒤 프로젝트로 좁힌다 + `EXPLAIN` 기록. (d) **엔지 리뷰 C §2 P2:** 기간 두 칸을 묶음 포커스 이탈(+ Enter)에서 제출 · 창 밖 요청 연도를 선택지에 넣는다. (e) **엔지 리뷰 C §1 P3:** 쪽 보정은 `lib/paging`(04-29) — 이 플랜의 `list-view.ts`에서 뺀다. (f) **엔지 리뷰 C P3 낡은 주석:** `repositories/projects.ts:124-127`·`:180-182`·`ProjectListFilter.year` 문서(`:43-44`)·`page.tsx:24-25`(「더 보기」)·`filter-bar.tsx:11`(「필터를 바꾸면 정렬·더 보기 누적도」 — 이제 정렬은 남는다)·`projects.module.css`의 `.loadMore` 규칙. (g) 테스트 공백 1·7·8·10(엔지 리뷰 C §3).

**2026-09-23 디자인 리뷰 반영(`docs/designs/plant8-erp-phase4-design-review-260923.md`, UI-SPEC rev 5 — 재계획 그룹 C):** (a) **교차 그룹 계약 7 · DR-8 · DR-38 — 수익금 기준 개정(CONTEXT D-87 대체):** `발행` 기준은 정산·완료이면서 발행 줄이 1개 이상일 때만, 그 밖은 견적 — 순수 함수 `profitBasisFor` + 같은 SQL 식 + 다섯 상태 TDD(Task 1). 행 열 표시는 04-18. (b) **DR-30 — 연도 자동 전환:** 옛 「R이 비면 빈 결과(`{ empty: true }`)」 경로를 없애고, 기간이 연도 밖이면 서버가 연도를 바꾼 주소로 보내며 반대 방향(연도를 바꿔 기간과 어긋남)은 필터 줄이 기간을 비운다(04-48 — 엔지 r2 분할). (c) **DR-26 — 폰 첫 화면:** 검색 전폭 → [「필터」 · 요약 · 1차] 한 줄 → 펼치는 필터 묶음(04-48 — 엔지 r2 분할). (d) DOM 감사 폭에 1024를 더한다(교차 그룹 계약 6). (e) **DR-32(클라이언트 두 줄)·DR-34(`종료일 지남` `--fs-xs`)는 04-18로:** 두 열 표시를 만드는 플랜이 04-18이다(이 플랜은 클라이언트 열·상태 2행을 그리지 않는다).

**2026-09-24 엔지니어링 리뷰 r2 반영(`docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 1 · E2-01 · E2-08).** (a) **분할(E2-01):** 옛 Task 3(기간 필터 · 연도 자동 전환 · 서버 판정 오류 · 틀린 파라미터 정규화 · 정렬 유지 · 필터 지우기 · 빈 목록 세 갈래 · 기간 칸 서식 · 폰 첫 화면 · 로딩 뼈대 + 목록 화면 검증)을 신규 **04-48**(웨이브 26, `depends_on: ["04-17"]`)로 옮겼다 — 그 truths·backstop(합계 줄·페이지 줄의 폰 줄바꿈 DOM 감사 포함)과 `filter-bar.tsx`·`loading.tsx`도 04-48 몫이다. 04-18은 이제 04-48 뒤다. **파라미터 정규화(C-08)는 이 플랜으로 당기지 않는다** — 페이지 쿼리 계약의 `page`는 04-29의 `clampPage`가 숫자 아님·1 미만·범위 밖을 이미 보정하고, `year`·`teamId` 정규화 전 상태(04-17 끝 ~ 04-48 끝)는 같은 머지 묶음 ④ 안이라 어느 환경에도 배포되지 않는다(분할안 「필요하면 당긴다」의 판단 — 필요하지 않다). (b) **E2-08(수용):** 목록과 합계는 여전히 두 문장이라 사이에 다른 요청의 정산이 커밋되면 기준(견적/발행)이 순간 어긋날 수 있다 — T-04-370 accept(30명 규모, 새로 고치면 맞는다, 스냅숏 트랜잭션은 04-32 풀 규약과 충돌). (c) 이 플랜의 끝 판정은 `CI=true` 목록 E2E다(Task 2) — 전체 게이트 한 번과 독립 DOM 감사는 04-48 끝에서 목록 화면 전체(이 플랜의 합계 줄·페이지 줄 포함)를 대상으로 한다(CLAUDE.md 화면 검증 순서).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전(커밋 전 포함) `verification-before-completion`을 Skill로 호출한다. 테스트·빌드·린트가 실패하면 코드를 고치기 전에 `systematic-debugging`부터 부른다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시하고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(이 플랜 하나). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 완료 판정은 로컬 dev가 아니라 `CI=true`다. 새 의존성 0개. 테스트 준비에서 차수를 고객 승인 상태로 둘 일이 생기면 그룹 B 04-14의 `setCustomerApproval`에 `approvalBasis(viewer, revisionId, tx)`(또는 `listRevisionSummaries`)로 읽은 `{ approvedOn, seenTotalKrw, contentToken }`을 넘긴다 — 승인 행을 SQL로 직접 만들지 않는다(그룹 B 계약).

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-05-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-29-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다(U-2는 `## UI Considerations` S5 partial 529행의 사용자 확정값). 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md rev 5(절 이름으로 찾고 아래는 2026-09-23 현재 행) — UI Considerations S1(466–479행 — rev 5 연도 자동 전환·폰 첫 화면·폰 요약 long-text 포함) · S12(576–585행) · 적용 규칙 S1(1006–1093행 — 필터 한 줄 · 연도 자동 전환 1022–1024 · 폰 첫 화면 1027–1030 · 합계 줄 · 합계 수익률 1042 · 그룹 · 기준 1072–1077 · 기간 칸) · 「SYSTEM.md 개정」 ⑧ §7-16(840–865행) · ③(721–763행) · Copywriting 346–348행(Empty — 목록 셋) · 359행(Error — 목록) · 378행(Error — 목록 기간 필터) · 399–403행(표시 — 귀속 · 수익금 기준 · 목록 합계 줄 · 폰 필터 요약 · 페이지)

# docs/designs/plant8-erp-phase4-design-review-260923.md 44행(DR-8) · 62행(DR-26) · 66행(DR-30) · 74행(DR-38)

# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 788–801행(§6 D-87~D-91 — 겹침 조건 · 귀속 분리 · OFFSET Gap)

# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 405행·421–422행·466–470행

# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1696–1709행(C-01) · 1807–1842행(C-08·C-09) · 1892–1904행(C-14)

# docs/designs/plant8-erp-phase4-eng-review-260923.md 142–166행(묶음 C) · 부록 C §1(목록 투영 · N+1 · all-of · 쪽 보정) · §2(기간 제출 · 연도 선택지 · 낡은 주석) · §3 GAP 1·7·8·10 · §4(현재 차수 합)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-01(목록 부분)**이다. 프로브는 04-05 시점 목록으로 돌았으므로 수용 기준은 D-88~D-91과 UI-SPEC rev 5 S1·S12로 끌어왔다. S1·S12 `## UI Considerations`의 empty·loading·error·populated·partial·zero-one-many 중 이 플랜 범위(범위·합계·페이지·수익금 기준의 서버 계산 ②·연도 자동 전환·폰 첫 화면·폰 요약 long-text)는 truths로, overflow(폰 합계 줄·페이지 줄)·zero-one-many·폰 첫 화면·연도 자동 전환 렌더는 backstop 마커로 옮겼다. S1 partial ②의 **열 표시**(`기준` 열)·③⑥과 열 폭(클라이언트 두 줄 DR-32 · `종료일 지남` `--fs-xs` DR-34)·좁은 PC 열 접기·폰 열 우선순위·long-text는 04-18 몫이다. 페이지 줄 컴포넌트 자체의 truths는 04-29에 있고, 이 플랜은 목록 화면 배선·DOM 감사를 맡는다.

**⚠ S1 「기간 미정」 행이 어느 보기에 나오는가 — 사용자 D19 확정(C-11 권장안):** 종료일이 없는 행 중 **수주중은 보기 범위와 무관하게 항상** 보이고, 그 밖의 상태(미수주 등)는 **등록일(KST 날짜)이 보기 범위 R 안**일 때만 보인다. 전체 연도 + 기간 없음이면 전부 보인다. 되돌리기: 조건 한 줄(reversible).

**계획 단계 가정(표면화):**

- **시작일만 없는 행**(종료일은 있음)은 겹침 판정에서 시작일 = 종료일로 본다. D-82 이후 진행·정산·완료는 시작일이 있으므로 이 경우는 수주중·미수주의 과거 데이터뿐이다.
- **연도와 기간이 겹치지 않는 조합(DR-30 — 옛 「R이 비면 사용자 필터 0건」을 대체):** 서버 페이지가 조회 전에 `reconcileListYear`로 연도를 고쳐 같은 주소(`year`만 바꾸고 `page`는 뺀다)로 `redirect`한다 — 고친 뒤에는 연도와 기간이 늘 겹치므로 `resolveListRange`가 빈 구간을 받지 않는다(빈 구간과의 겹침 식이 두 끝을 모두 넘는 행을 잘못 잡는 문제도 사라진다). 규칙: 기간이 형식 오류면 바꾸지 않는다 · `전체 연도`면 바꾸지 않는다 · 시작·종료가 같은 해면 그 해 · 해를 걸치면 `전체 연도` · **한쪽만 적힌 기간이 연도와 겹치지 않으면 `전체 연도`**(계획 기본값 — 열린 기간은 해를 넘어 이어지므로 한 해로 좁히지 않는다, reversible — 조건 한 줄). `reconcileListYear`는 멱등이다(고친 주소를 다시 넣으면 그대로 — 되돌이 이동 없음).
- **반대 방향(DR-30 · rev 5 계획 기본값):** 서버는 「사용자가 방금 연도를 바꿨는지」를 주소만으로 알 수 없다 — 그래서 필터 줄(클라이언트)이 연도 선택 상자를 바꿀 때 기간 두 칸에 값이 있고 새 연도와 겹치지 않으면(`periodOverlapsYear` — 같은 순수 함수를 클라이언트가 import) 기간 두 칸을 비운 뒤 제출한다. 서버에 도착한 「연도 + 겹치지 않는 기간」은 늘 기간 쪽이 새 입력이라 연도를 기간에 맞춘다.
- **한쪽이 열린 기간 보기의 괄호 라벨**은 열린 쪽을 `—`로 쓴다(`2026-09-01 ~ — 귀속`).
- **연도 보기의 제외 문구 순서**는 해 오름차순 뒤 `기간 미정`(`2025 귀속 1건 제외 · 2027 귀속 3건 제외 · 기간 미정 5건 제외`).
- **상단 2px 진행 막대(C-10 서술 정정 · 사용자 D17):** SYSTEM.md는 그 막대를 `2px --accent`로 정해 두었다(§5·§7-7·§7-10) — 색이 없는 것이 아니라 §1-3 「`--accent` 사용처 다섯 곳」과 충돌하는 것이 문제이고, 막대는 앱 어디에도 아직 없다. 사용자 D17 「만들지 않고 기록」에 따라 이 플랜은 막대를 만들지 않는다(충돌 기록은 04-08이 `DECISIONS.md`에 남겼고, 막대는 디자인 잔여 퀵 태스크로 이연 — CEO 리뷰 「범위 밖」). 목록의 필터·정렬·쪽 이동은 전체 문서 GET이라 `loading.tsx`가 스트리밍된다 — 로딩 표시는 그 뼈대다.
- **틀린 파라미터(C-08):** 도메인 입구가 `normalizeListParams`로 정규화한 뒤에만 쓴다 — `teamId`는 uuid 모양이고 뷰어가 고를 수 있는 팀 목록에 있어야, 연도는 `all` 또는 2000–2100 정수, 배열이면 첫 값. 기간 칸은 오류를 칸 아래에 보이는 별도 규칙(`parseListPeriod`)이 그대로다.
- **배포 묶음(C-26 — 머지 묶음 규칙으로 고침, 2026-09-24):** 04-17과 04-18 사이에는 합계가 D-87 수익금, 행이 옛 차익을 보인다 — 04-17·04-48·04-47·04-18·04-19는 모두 머지 묶음 ④(웨이브 25~30)에 들어 **한 번의 배포**로 나가고 되돌릴 때도 함께 되돌린다(사용자 결정 2026-09-23 — 묶음마다 PR 하나, 묶음 중간 상태 배포 금지. 강제 장치는 04-50의 `deploy.yml` main 전용 가드와 `rollback.sh` 스키마 하한). 옛 「Phase 4는 한 PR로 배포」 전제는 이 규칙으로 대체됐다.
- **연도 자동 전환·반대 방향·틀린 파라미터 가정(위 세 줄)** 은 04-48이 구현한다 — 이 플랜의 `resolveListRange`는 04-48의 `reconcileListYear`를 지난 연도만 받는다는 계약을 지금부터 따른다(빈 교집합 갈래를 만들지 않는다).

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 올해 기본 보기 · 기간 겹침 · 표 위 귀속 합계 줄 · 숫자 경계(리포지토리 → 도메인 → 합계 줄 → E2E)</name>
  <files>repositories/projects.ts, domain/projects/list-view.ts, domain/projects/index.ts, test/unit/domain/project-list-view.test.ts, app/(app)/projects/page.tsx, app/(app)/projects/list-totals.tsx, app/(app)/projects/projects-table.tsx, app/(app)/projects/projects.module.css, test/integration/projects-list.test.ts, test/integration/project-auto-settlement.test.ts, test/integration/quote-lines.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `repositories/projects.ts` 1–210행(`currentRevisionsSubquery`·`lineSumsSubquery`(04-12가 보관 제외를 더했다)·`projectFilterConditions`(88행 `includeArchived`)·`resolveSortColumn`·`listProjectsPage`·`aggregateProjects`(197–200행 `::int`))
    - `domain/projects/index.ts` 80–215행(`registerDto` 두 곳 · `PROJECT_LIST_DTO_SPEC`·`listProjects`·`aggregateProjects` — 04-11 뒤에는 판정을 부르지 않는다 · `settleForProjectList`)
    - `domain/permissions/project.ts`(04-32 — `projectMany`·`InfoItemRef` all-of) · `lib/paging.ts`(04-29)
    - Grep `listProjects(`·`aggregateProjects(`로 `app/`·`domain/`·`test/`의 호출자를 모두 찾는다 — 04-05의 `projects-list.test.ts`, 04-11의 `project-auto-settlement.test.ts`(f~f4), 04-12의 `quote-lines.test.ts`(k), 그룹 B가 더한 호출이 있으면 그것까지
    - `lib/kst-date.ts`(04-29 — `kstToday`·`kstYear`·`kstDateOf`) · `lib/format-number.ts`(04-09 — `formatKrw`·`formatPercent`·`formatCount`)
    - `db/schema/revenue-entries.ts`(전문 — `kind` `issue`/`payment`, `amountKrw`, `archivedAt`)
    - `app/(app)/projects/page.tsx`(전문) · `app/(app)/projects/projects-table.tsx`(전문) · `app/(app)/projects/projects.module.css`(전문)
    - `test/integration/projects-list.test.ts`(전문 — 픽스처 만드는 법, (a) 왕복 한 번 단언 방식) · `test/integration/leak-scan.test.ts`(등록 DTO를 훑는 방식) · `test/e2e/projects-list.spec.ts`(전문) · `test/e2e/fixtures.ts`
    - `domain/permissions/visible.ts`(정보 항목 판정 함수 이름)
  </read_first>
  <behavior>
    - `resolveListRange({ year: 2026 })` → `{ start: "2026-01-01", end: "2026-12-31", kind: "year", year: 2026 }` · `{ year: "all" }` → `null` · `{ year: 2026, period: { from: "2026-09-01" } }` → `{ start: "2026-09-01", end: "2026-12-31", kind: "period" }` — 빈 교집합 갈래는 없다(연도와 기간이 어긋나면 04-48의 `reconcileListYear`가 조회 전에 연도를 고친다, DR-30)
    - `attributionLabel({ endDate: "2027-02-10", range: 2026 연도 보기 })` → `2027 귀속` · 기간 보기 R=2026-09-01~2026-10-31, 종료 2026-11-20 → `2026-11 귀속` · 종료일이 R 안 → null · 종료일 없음 → null
    - `exclusionText({ byYear: { 2027: 3, 2025: 1 }, undetermined: 5, kind: "year" })` → `2025 귀속 1건 제외 · 2027 귀속 3건 제외 · 기간 미정 5건 제외` · 기간 보기 `{ outside: 2 }` → `기간 밖 귀속 2건 제외` · 전부 0 → null
    - `totalsTitle({ statusLabel: "진행", range: 2026 연도, count: 12 })` → `합계 (진행 · 2026 귀속 · 12건)` · 전체 연도 → `합계 (전체 연도 · 125건)` · 건수 1,250 → `1,250건`
    - 통합: 2026-11-10~2027-02-05 프로젝트는 2026 보기와 2027 보기 **둘 다**에 나오고, 합계에는 2027 보기에만 들어간다 — 두 해 합계의 합 = 전체 연도 합계(기간 미정 제외분 빼고)
    - 단위(계약 7 — `profitBasisFor(status, issuedCount)`): `("bidding", 2)`·`("in_progress", 2)`·`("lost", 1)` → `quote`(부분 발행이 기준을 바꾸지 않는다, DR-8) · `("settling", 1)`·`("completed", 3)` → `issued` · `("settling", 0)`·`("completed", 0)` → `quote`(DR-38) · 발행 줄 0이면 어떤 상태든 `quote`
    - 통합(DR-8): 진행 프로젝트 — 견적 100,000,000 · 실행가 80,000,000 · 발행 30,000,000 → 행 `profitBasis` `quote` · 수익금 20,000,000(−50,000,000이 아니다) · `revenueKrw` 30,000,000 · 수익률 0.2
    - 통합(DR-8 · DR-38): 정산 프로젝트 — 견적 12,000,000 · 실행가 7,000,000 · 발행 10,000,000과 −1,000,000 → `issued` · 수익금 2,000,000 · 수익률 ≈ 0.2222 / 정산 프로젝트 — 같은 견적·실행가 · 발행 줄 0 → `quote` · `revenueKrw` null · 수익금 5,000,000 / 완료 프로젝트 — 발행 +1,000,000과 −2,000,000 · 실행가 500,000 → `issued` · 수익금 −1,500,000 · 행 수익률 null(C-16)
    - 통합(계약 7 정합): 다섯 상태 × {발행 줄 0, 1 이상}의 픽스처 열 행마다 리포지토리 행의 `profitBasis`가 `profitBasisFor(status, issuedCount)`와 같다 — SQL 식과 순수 함수가 갈라지지 않는다
    - 통합(합계): 위 행이 섞인 보기의 합계 수익금 = Σ(행 기준 금액 − 실행가) · 합계 수익률 = Σ수익금 ÷ Σ행 기준 금액 · Σ기준 ≤ 0이면 null
    - 통합(C-01): 견적 줄 하나가 1,500,000,000원인 프로젝트 둘 → 전체 연도 합계 견적이 정확히 3,000,000,000이고, 합계와 행의 모든 금액·건수 필드가 `typeof === "number"`
    - 통합(C-14): 발행 1,000,000 + 입금 1,000,000인 프로젝트의 매출·기준이 1,000,000(입금이 더해지지 않는다)
    - 통합: 보관된 견적 줄과 보관된 발행 줄은 합계에서 빠진다 · 보관된 프로젝트는 시스템 관리자의 목록·합계에서도 빠진다(D18)
    - 통합(C-14): 합계 키 집합 — `quote.amount`만 → 견적·실행가 · `revenue.issued_amount`만 → 매출 · 둘 다 → 다섯 · **둘 다 없음 → 금액 키 0개**
    - 통합(엔지 리뷰 C 공백 1): `quote.amount`·`revenue.issued_amount`를 끈 계급의 `loadProjectList` **행**에 `quoteAmountKrw`·`executionAmountKrw`·`revenueKrw`·`profitKrw`·`profitRate`·`profitBasis` 키가 하나도 없다 — 옛 `listProjects` 경로가 아니라 새 입구로 단언한다
    - 통합(ENG-D3 ②): `pool.query`(와 `pool.connect`) 스파이로 센 `loadProjectList` 한 번의 DB 호출 수가 1행 보기와 50행 보기에서 같다
  </behavior>
  <action>
① **리포지토리(`repositories/projects.ts`).** (a) **숫자 경계(C-01):** 모든 돈 집계를 `::bigint`로 캐스트하고 `sql<number>\`…\`.mapWith(Number)`로 리포지토리 경계에서 JS 숫자로 바꾼다 — 목록 행 칸(`lineSumsSubquery`가 만드는 합)과 `aggregateProjects`의 합 모두. 건수는 `::int`. 함수 머리 주석에 불변식 한 줄: 「이 파일의 모든 집계·금액 반환은 JS number다」. (b) `lineSumsSubquery`의 보관 줄 제외(04-12)를 확인만 한다 — 통합 케이스가 고정한다(A-04). **현재 차수만(엔지 리뷰 C §4 P2):** 그 하위 질의가 `quote_lines.revision_id IN (현재 차수 하위 질의)`로 현재 차수의 줄만 모아 더하게 한다(지금은 모든 차수·모든 프로젝트의 줄을 요청마다 GROUP BY한다 — `quote_lines_revision_sort_idx`를 탄다). (c) 발행 합계를 더한다 — `revenue_entries`에서 **`kind = 'issue'`만** · 보관 제외 · 프로젝트별 `sum(amount_krw)::bigint`와 줄 수(C-14 — 입금 제외가 이 조건 하나에 달렸다). 표 전체 GROUP BY 파생 표가 아니라 **행 필터를 지난 프로젝트마다** 구하는 `LEFT JOIN LATERAL`(`revenue_entries_project_kind_date_idx`)로 둔다(엔지 리뷰 C §4 P2). 두 선택과 기준 `EXPLAIN`(125건 보기의 집계·목록 두 문장)을 SUMMARY에 남긴다. (d) 행 단위 SQL 식 셋을 한 곳(함수 하나)에 정의한다 — **기준 금액(교차 그룹 계약 7 · DR-8 · DR-38): 상태가 `settling`·`completed`이고 발행 줄 수 > 0이면 발행 합계, 그 밖은 견적 합계** · 수익금(기준 − 실행가) · 수익률(`CASE WHEN 기준 > 0 THEN 수익금::float8 / 기준 END` — 0 이하는 null, 사용자 D19 · C-16). 상태 값 문자열은 04-06이 정한 코드(`settling`·`completed`) 그대로이고 SQL에 리터럴을 흩지 않도록 04-06의 `domain/projects/status-transitions.ts`(`PROJECT_STATUSES` · `ProjectStatus` — 데이터만 있는 파일)에서 가져온다. `profitBasis`(`quote`/`issued`)도 같은 CASE로 만든다. 목록 행(`ProjectListRow`)이 이 값들과 `revenueKrw`(발행 줄 0이면 null)·`issuedCount`·`profitBasis`(`quote`/`issued`)를 싣는다 — **이 칸들을 DTO 명세에 올리는 것은 04-18**이고, 명세에 없는 칸은 ③의 투영이 응답에서 뺀다. (e) `ProjectListFilter`의 `year`를 `range?: { start?: string; end?: string }`로 바꾸고 `projectFilterConditions`의 연도 조건을 **겹침**으로 바꾼다: 종료일이 있으면 `coalesce(시작일, 종료일) <= R.end`(있을 때) AND `종료일 >= R.start`(있을 때), 종료일이 없으면 `상태 = bidding` OR 등록일(KST 날짜)이 R 안(⚠ S1, `<probe_fallback>`). 등록일의 KST 변환식은 `created_at` 컬럼 형(시간대 없음)과 세션 `TimeZone = UTC`(04-11 통합 단언)를 전제로 정한다. (f) **보관 프로젝트(D18):** 88행의 `if (!scope.includeArchived)` 조건을 지우고 목록·합계는 항상 `archived_at IS NULL`을 건다(보관함 화면은 이 함수를 쓰지 않는다 — Grep으로 호출처 확인). `ProjectListFilter.year` 문서 주석(43–44행)을 `range` 설명으로 고친다(엔지 리뷰 C P3). (g) `aggregateProjects`는 같은 `projectFilterConditions` 위에 **귀속 구간** CASE(종료일 없음 → `undetermined` · 종료일이 R 안 또는 R 없음 → `in` · 그 밖 → 종료 연도 문자열)로 `GROUP BY`한 행들을 **한 번의 쿼리**로 돌려준다 — 구간마다 건수·매출·견적·실행가·수익금·기준 합(전부 bigint → Number).

② **순수 함수(`domain/projects/list-view.ts` 신규, import는 `lib/`와 04-06의 `domain/projects/status-transitions.ts`(데이터만)뿐 — 서버 전용 모듈을 import하지 않는다: 04-48에서 필터 줄 클라이언트가 이 파일의 겹침 함수를 import한다).** `resolveListRange` · `attributionLabel` · `exclusionText` · `totalsTitle`(건수는 `formatCount`) · **`profitBasisFor(status, issuedCount)`**(계약 7 — `behavior` 단위 줄) — `behavior` 앞 네 줄과 계약 7 표. 단위 테스트를 먼저 쓴다(RED → GREEN).

③ **도메인(`domain/projects/index.ts`).** 화면의 유일한 입구 `loadProjectList(viewer, query, deps?: { now?: () => Date; repo?: { aggregate; listPage; countVisible } })`를 더한다(`deps.repo`는 단위 테스트의 스텁 자리): 04-11의 `settleForProjectList(viewer)`를 **먼저 한 번** 부르고(행 범위 확인 뒤 판정 · 실패해도 04-11이 로그만 남기고 계속 — C-09) → `year` 해석(없으면 `kstYear(now())`, `all`이면 없음) → `resolveListRange`(연도는 페이지가 `reconcileListYear`를 지난 값 — 04-48) → **리포지토리 `aggregateProjects`를 직접** → 합계 조립. 목록 `page.tsx`에 04-11이 둔 `settleForProjectList` 호출은 지운다(이 입구 안으로 옮겼다). **옛 도메인 입구 삭제(엔지 리뷰 C §1 P1):** 도메인 `listProjects`·`aggregateProjects`를 지운다 — 범위·페이지가 없는 다른 의미의 입구가 테스트 전용으로 남지 않게 한다. 그 호출자(`read_first` Grep)는 전부 `loadProjectList`로 옮긴다: `projects-list.test.ts`의 기존 케이스(04-05 — 전체 연도 `year: "all"` 보기로), `project-auto-settlement.test.ts`의 f~f4(04-11 — 「판정 한 번 뒤 목록 건수 = 합계 건수」는 `loadProjectList` 결과의 행 수와 합계 건수로), `quote-lines.test.ts`의 (k)(04-12 — 보관 줄이 목록·합계에서 빠짐). 옮긴 테스트의 단언 의미는 바꾸지 않는다 — SUMMARY 편차(ENG-D11)에 옮긴 케이스 목록을 적는다. **행 투영(엔지 리뷰 C §1 P1 · ENG-D3 ②):** 목록 행은 `projectMany(viewer, rows, PROJECT_LIST_DTO_SPEC)`(04-32)로 투영한 결과만 돌려준다 — 리포지토리 행을 그대로 넘기지 않는다. **목록 읽기 실패의 운영 로그(C-09에 접힌 §8 관측성 NOTE):** 집계·목록 쿼리가 던지면 `loadProjectList`가 `log.error("project.list_failed", { code: 오류의 PG 코드 })` 한 줄(필터 값·금액 없음)을 남기고 **그대로 다시 던진다** — 화면은 기존 `error.tsx`가 맡고, 지금은 Next 기본 stderr만 남던 실패에 구조화된 이벤트가 생긴다(판정 실패는 04-11의 `project.auto_settle_failed`, 이름은 점 표기로 통일). 합계 DTO `ProjectListTotals`: `title`·`count`(귀속 `in` 건수)와 금액 — 견적·실행가는 `quote.amount`, 매출은 `revenue.issued_amount`, 수익금·수익률은 **두 항목을 모두** 볼 수 있을 때만 키를 싣는다(필드 부재, 빈 값 아님) · `exclusionText`. **`ProjectListTotals`를 `registerDto`로 등록한다**(C-14 · D-38 — 누수 스캔이 합계 키를 본다). 합계 DTO의 수익금·수익률 필드는 `infoItem: ["quote.amount", "revenue.issued_amount"]`(all-of — 04-32)로 명세하고 `projectMany`/`project()`로 투영한다 — 「둘 다 볼 때만」을 손으로 키를 빼는 코드로 만들지 않는다(엔지 리뷰 C §1 P2 · ENG-D3 ②). 합계 수익률은 `Σ수익금 ÷ Σ기준`(Σ기준 ≤ 0이면 null). 목록 행 DTO에는 `attributionLabel`을 `groupLabel`처럼 파생해 붙인다.

④ **화면.** `app/(app)/projects/list-totals.tsx`(서버 컴포넌트): 필터 줄 바로 아래 한 줄 — 제목 `합계 (…)`(라벨 모양) + `<dl>` 쌍(`dt` 라벨 `--fs-sm --muted` 600 · `dd` 금액 `--fs-lg` 700 `tabular-nums`, `formatKrw`·`formatPercent`) + 오른쪽 끝(폰은 아래 줄) 제외 문구 `--fs-sm --muted`, 아래 `2px --line-strong`. 카드·타일·배경 면 없음. 받은 키만 쌍으로 그린다. `projects-table.tsx`에서 표 아래 합계 줄(`summaryRow`)과 그 조립을 지운다(「더 보기」 링크 제거는 Task 2). 기간 열 셀에 `secondaryLine`으로 `attributionLabel`(`--fs-sm --muted`, `nowrap`)을 둔다. `page.tsx`는 `loadProjectList`를 부르고, 연도 선택지 맨 위에 `전체 연도`(값 `all`)를 두며 기본 선택을 올해로 한다(연도 목록 기준도 `kstYear`). `projects.module.css`에 합계 줄 규칙(토큰만 — D-20).

⑤ **E2E(트레이서 증명, `projects-list.spec.ts` 개정).** 올해·내년은 테스트 프로세스의 UTC가 아니라 `kstYear(new Date())`(`lib/kst-date`)로 계산한다(C-17 — 12월 31일 15:00~24:00 UTC에 깨지지 않게). 다른 워커의 데이터와 섞이지 않게 고유 검색어(`q=`)로 격리한 프로젝트 셋을 준비한다(올해 안 · 올해~내년 걸침 · 기간 미정 수주중 — 날짜는 `kstToday` 기준으로 만든다) → 조건 없이 `/projects?q=…` → 연도 선택 값이 올해, 세 행이 보이고, 합계 줄이 **표보다 위**에 있으며 괄호 건수가 1(올해 안만), 제외 문구 `{내년} 귀속 1건 제외 · 기간 미정 1건 제외`, 걸침 행 기간 칸 2행 `{내년} 귀속`, 걸침 행이 종료월 그룹 한 곳에만 한 번 있다(D-89·D-51) → 연도를 `전체 연도`로 바꾸면 괄호가 `전체 연도 · 2건`이고 제외 문구가 `기간 미정 1건 제외`만 남는다. 합계 금액이 쉼표 서식이다(C-01 — 문자열 합계가 사라졌다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 범위·귀속·제외·제목 규칙이 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts test/integration/quote-line-kinds.test.ts test/integration/leak-scan.test.ts test/integration/project-auto-settlement.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 겹침·귀속 합계·이중 계산 금지·21억 초과·입금 제외·보관 제외·합계·행 필드 노출·조회 수 중 하나가 틀렸거나, 옮긴 04-11·04-12 케이스·조정 줄 불변식(04-13)·누수 스캔이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 올해 기본 보기와 표 위 합계 줄이 끝까지 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `repositories/projects.ts`의 돈 집계에 `::int` 캐스트가 없고(`grep -n "sum(.*)::int" repositories/projects.ts` 0건) 집계 식이 `mapWith(Number)`를 쓴다(C-01)
    - 통합 C-01 케이스가 합계 3,000,000,000과 모든 필드 `typeof === "number"`를 단언한다
    - `projectFilterConditions`를 부르는 함수가 `listProjectsPage`·`aggregateProjects` 둘이고, 집계가 별도의 행 필터를 만들지 않는다(귀속은 GROUP BY 구간 식)
    - `projectFilterConditions`에 `includeArchived` 분기가 없고 시스템 관리자 목록·합계에서 보관 프로젝트가 빠진다(D18 통합)
    - 통합 테스트에 「걸친 프로젝트가 두 해 목록에 나오고 합계는 종료 해 한 곳」 케이스와 「두 해 합계의 합 = 전체 연도 합계(기간 미정 제외)」 단언이 있다(금지 항목)
    - D-89 전부가 단언으로 덮인다 — 조건 없는 URL = 올해 보기(E2E) · 연도 보기의 겹침 조건(통합) · 걸친 프로젝트가 겹치는 해마다 보임(통합) · 걸친 행이 각 보기에서 종료월 그룹 한 곳에만 한 번(D-51) · 기간 필터 겹침은 04-48 · 걸친 행 기간 칸 서식은 04-48 단위
    - 통합 테스트에 등록일 KST 경계 케이스가 있다 — `created_at`이 KST 2027-01-01 00:30(UTC 전날)인 종료일 없는 미수주가 2027 보기에 나오고 2026 보기에 나오지 않는다
    - 통합 (a)의 「집계 SQL 왕복 한 번」 단언이 귀속 구간이 생긴 뒤에도 그대로 초록이다
    - 입금 줄 픽스처(C-14) · 키 집합 넷(없음 포함) · 음수 기준 수익률 null(C-16) 케이스가 있다
    - `profitBasisFor` 단위 표(수주중·진행·미수주 부분 발행 → 견적 · 정산·완료 발행 ≥1 → 발행 · 정산·완료 발행 0 → 견적)와 통합의 DR-8 진행 건(수익금 +20,000,000) · DR-38 정산 0 발행 건(매출 null · 견적 기준) · 다섯 상태 × {0, ≥1} 정합 케이스가 초록이다 — 리포지토리 기준 식에 상태 조건이 있다(계약 7)
    - `ProjectListTotals`가 `registerDto`로 등록되고 누수 스캔이 초록이다
    - `loadProjectList`가 쿼리 실패 때 `project.list_failed` 오류 로그를 한 줄 남기고 같은 오류를 다시 던진다 — 단위 테스트가 스텁 리포지토리(`deps.repo`)로 단언한다(엔지 리뷰 C 공백 7 — Grep 대신)
    - `loadProjectList`가 행을 `projectMany(…, PROJECT_LIST_DTO_SPEC)`로 투영하고, 도메인 `listProjects`·`aggregateProjects` 정의와 호출이 `app/`·`domain/`·`test/`에 없다(Grep 0) · 옮긴 테스트(04-05·04-11·04-12 케이스)가 초록이다
    - 합계 DTO 수익 두 칸의 명세가 all-of 정보 항목이고 `domain/projects/index.ts`에 「둘 다 볼 때만」을 위한 수동 키 삭제 코드가 없다
    - 견적 줄 합이 현재 차수 줄만, 발행 합이 `LEFT JOIN LATERAL`이고 `EXPLAIN` 기준이 SUMMARY에 있다
    - 목록 페이지(`page.tsx`)에 `settleForProjectList` 직접 호출이 없다(입구 안으로 옮겨졌다)
    - 통합 행 키 집합(공백 1)과 조회 수 동일(ENG-D3 ②) 케이스가 초록이다
    - `list-totals.tsx`가 `<dl>`을 쓰고 `projects-table.tsx`에 표 아래 합계 줄 렌더가 없다
    - E2E가 연도를 `kstYear`로 계산한다 — `grep -n "getFullYear\|getUTCFullYear" test/e2e/projects-list.spec.ts` 0건(C-17)
    - 이 플랜이 `db/migrations/`에 파일을 더하지 않는다 — 연도 합계를 저장 컬럼으로 만들지 않는다(D-92)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>조건 없이 연 목록이 올해 보기이고, 합계가 표 위에서 귀속 연도 행만 21억을 넘어도 정확히 더하며 제외 건수를 말한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 번호 페이지 50건 — 04-29 `Pagination` 배선 + OFFSET + 범위 밖 번호 보정 + 그룹 머리글 반복 + 요청 순서</name>
  <files>repositories/projects.ts, domain/projects/index.ts, domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `ui/pagination/Pagination.tsx`·`ui/pagination/page-window.ts`(04-29 산출물 전문 — `href` 함수 갈래 · `pageRangeText` · 폰 창) · `lib/paging.ts`(04-29 — `clampPage`·`pageCountFrom`·`LIST_PAGE_SIZE`)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정」 ⑧(840–865행 — §7-16 모양·현재 번호·렌더하지 않는 이전/다음·생략·요소 `next/link`·ARIA·폰) · 적용 규칙 S12(1413–1428행)
    - `ui/table/Table.tsx` 61–100행(`groupRows` — 페이지의 첫 행 그룹 머리글이 자연히 렌더되는지 확인)
    - `domain/projects/index.ts` 140–185행(`PROJECT_LIST_DEFAULT_LIMIT`·`PROJECT_LIST_MAX_LIMIT`·`listProjects`)
    - `app/(app)/projects/page.tsx`(Task 1 이후 — `count` 파라미터·`loadMoreHref` 조립)
  </read_first>
  <behavior>
    - (C-01) `bucketTotal([{ count: 30 }, { count: 25 }, { count: 1 }])` → 56이고 `pageCountFrom(56, LIST_PAGE_SIZE)`(04-29) → 2 — 구간 건수가 숫자로 더해진다(문자열 이어 붙이기 `"30251"`이 아니다). 쪽 보정 표는 04-29 `lib/paging`의 단위가 맡는다
    - (엔지 리뷰 C 공백 7) 스텁 리포지토리의 집계가 던지면 `loadProjectList`가 `log.error("project.list_failed", { code })`를 정확히 한 번 부르고 같은 오류를 다시 던진다
    - 통합: 125행 보기에서 `page=3` → 101~125번째 25행, `page=99` → 마지막 페이지와 같은 행, 페이지 1~3의 행을 이어 붙이면 전체와 순서까지 같다(겹침·누락 없음)
  </behavior>
  <action>
① **서버 페이지.** `repositories/projects.ts` `listProjectsPage`가 `limit` 대신 `{ offset, limit }`을 받아 `.offset()`을 쓴다(정렬 끝에 `projects.id`가 이미 있어 페이지 간 순서가 안정적이다). `list-view.ts`에 `bucketTotal`(구간 건수 숫자 합 — `behavior` 첫 줄, 단위 먼저)을 더하고 쪽 보정·쪽 수는 `lib/paging`(04-29)의 `clampPage`·`pageCountFrom`을 import한다 — 이 파일에 쪽 보정을 따로 만들지 않는다(엔지 리뷰 C §1 P3). `domain/projects/index.ts`: 04-05의 목록 개수 상수 둘(기본 개수 · 「더 보기」 누적 상한)을 지우고 `LIST_PAGE_SIZE`(04-29, 50)를 쓴다(`PROJECT_LIST_PAGE_SIZE`를 따로 두지 않는다 — 리저브 04-07과 같은 상수), `loadProjectList`의 순서를 **판정 한 번 → 집계 → `pageCountFrom(bucketTotal(구간))` → `clampPage` → 목록(리포지토리 `listProjectsPage` 직접) → `projectMany` 투영**으로 고정한다(C-23 — 목록 행이 필요 없는 0건이면 `emptyKind` 판정용 건수 조회만). 결과에 `page`·`pageCount`·`total`. 옛 `count` 파라미터는 읽지 않는다.

② **화면.** `projects-table.tsx`에서 「더 보기 50건」 링크와 `loadMoreHref` prop을 지우고, `projects.module.css`의 `.loadMore` 규칙과 `page.tsx` 24–25행 「더 보기」 주석, `repositories/projects.ts` 124–127행(「`limit`이 곧 화면의 "더 보기" 누적 개수」)·180–182행(집계의 `limit` 메모) 주석을 지금 사실(번호 페이지 · OFFSET)로 고친다(엔지 리뷰 C P3 낡은 주석). `page.tsx`가 표 아래에 04-29의 `Pagination`(label `프로젝트`, `href` 함수 = 지금 필터·정렬 파라미터를 보존하고 `page`만 바꾼 `/projects?…`, `rangeText` = `pageRangeText(…, "건")`)을 렌더한다. 필터 폼은 `page`를 싣지 않으므로 필터를 바꾸면 1쪽이다. 그룹 머리글 반복은 `Table`의 `groupBy`가 페이지의 첫 행 그룹을 그리는 것으로 성립한다 — 표에 추가 코드를 넣지 않고 E2E로 확인한다.

③ **테스트.** 통합(`behavior` 셋째 줄) · E2E: 같은 검색어로 격리한 51건(한 달에 몰아 같은 종료월 그룹) → 페이지 줄 `1–50 / 51건` · 「이전」 없음 · `2`를 누르면 URL `page=2`, 한 행, 그 행 위에 같은 종료월 그룹 머리글이 다시 있고 「다음」 없음 → 상태 필터를 바꾸면 URL에 `page`가 없다 → `page=99`로 직접 열면 마지막 페이지다. 옛 「더 보기」 케이스를 지운다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts test/unit/ui/pagination.test.ts test/unit/lib/paging.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 페이지 보정·구간 건수 합이 계약과 다르거나 04-29 컴포넌트 계약이 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts test/integration/project-auto-settlement.test.ts test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — OFFSET 페이지가 겹치거나 빠지거나, 범위 밖 번호가 마지막 페이지가 아니거나, Task 1에서 옮긴 04-11·04-12 케이스·누수 스캔이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; bash scripts/reset-test-db.sh &amp;&amp; CI=true pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 프로덕션 빌드(`CI=true`)에서 합계 줄·페이지 줄·그룹 머리글 반복이 이어지지 않는다(로컬 dev 초록은 완료 신호가 아니다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `PROJECT_LIST_MAX_LIMIT`·`PROJECT_LIST_DEFAULT_LIMIT` 참조가 `app/`·`domain/`에 없고 `page.tsx`가 `params.count`를 읽지 않는다(Grep 0) · `list-view.ts`에 쪽 보정 함수 정의가 없고 `lib/paging`을 import한다
    - `grep -rn "더 보기\|loadMore" 'app/(app)/projects' repositories/projects.ts --exclude=filter-bar.tsx`가 0건이다(낡은 주석·CSS 규칙 포함 — `filter-bar.tsx` 11행 머리 주석은 04-48 Task 2가 고친다, 엔지 r2 분할)
    - 단위에 `project.list_failed` 한 번 + 다시 던짐 케이스(스텁 리포지토리)가 있다
    - `listProjectsPage`가 `.offset(`을 쓴다
    - `loadProjectList` 본문이 판정 → 집계 → 보정 → 목록 순이고 도메인 `listProjects`·`aggregateProjects`를 부르지 않는다
    - 통합 테스트에 「페이지 이어 붙이기 = 전체」 단언과 `page=99` 보정 단언이 있다 · 단위에 구간 건수 숫자 합 케이스가 있다(C-01)
    - E2E에 2쪽 맨 위 그룹 머리글 반복 단언이 있다
    - 목록 E2E가 `CI=true`(프로덕션 빌드)로 초록이다 — 이 플랜의 완료 판정(04-48 전의 마지막 목록 확인)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>목록이 50건씩 번호 페이지로 나뉘고, 범위 밖 번호는 마지막 페이지이며, 페이지를 넘는 그룹 머리글이 다시 보이고, 한 요청의 읽기 순서가 고정됐다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 URL → 목록 서버 페이지 | `year`·`from`·`to`·`page`·`sort`·`dir`·`status`·`teamId`·`q` 전부 신뢰하지 않는 입력(배열로도 온다) |
| domain 출구(목록 행·합계 DTO) → 화면 | 행 단위 금액이 리포지토리 행에 생겼다 — 투영 명세 밖이면 나가지 않아야 한다 |
| 집계 쿼리 ↔ 목록 쿼리 | 두 쿼리가 다른 행 필터를 쓰면 합계가 가려진 행을 드러낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-88 | Information Disclosure | 합계 줄 | high | mitigate | 집계가 목록과 같은 `projectFilterConditions`(보관·범위·상태·팀·검색·scope) 위에 귀속 구간만 얹는다. 수익금·수익률은 `quote.amount`와 `revenue.issued_amount`를 **둘 다** 볼 때만 키를 싣는다(발행 역산 차단). 계급별 합계 키 집합 넷(둘 다 없음 포함)을 통합 테스트로 단언하고, `ProjectListTotals`를 DTO 레지스트리에 등록해 누수 스캔이 본다(C-14) |
| T-04-89 | Information Disclosure | 행 단위 금액(리포지토리 행) | high | mitigate | (엔지 리뷰 C §1 P1 — 투영 단계가 없어 클라이언트 props로 금액이 새는 경로) `loadProjectList`가 행을 `projectMany(…, PROJECT_LIST_DTO_SPEC)`로 투영한 결과만 넘기고, 이 플랜은 새 행 필드를 DTO 명세에 넣지 않는다 — 명세 밖 키는 응답에 없다. 금액을 끈 계급의 행 키 집합을 새 입구로 단언하고(공백 1), 옛 입구를 지워 다른 경로가 남지 않게 한다. 04-18이 필드별 정보 항목으로 명세에 올린다 |
| 운영 로그 | `project.list_failed`(오류 — 목록 쿼리 실패, PG 코드만 · 다시 던짐) |
| T-04-91 | Denial of Service | 한 요청의 행 수·합계 범위 | medium | mitigate | 옛 `count` 누적(최대 1000) 경로를 지우고 페이지 크기를 50으로 고정한다. 집계는 GROUP BY 구간 수(해 수 + 2) 행만 돌려준다. 돈 합계를 `bigint`로 더해 21억 초과 합계가 목록 전체를 오류 화면으로 만들지 않는다(C-01) |
| T-04-370 | Tampering | 목록·합계 두 문장의 스냅숏 차이(엔지 r2 E2-08) | low | accept | 합계(집계)와 목록이 여전히 두 문장이라 사이에 다른 요청의 자동 정산이 커밋되면 한 요청 안에서 기준(견적/발행)이 순간 어긋날 수 있다 — 30명 규모이고 새로 고치면 맞는다. 같은 스냅숏 트랜잭션은 도입하지 않는다(04-32 풀 규약과 병렬 읽기 이득이 충돌 — 엔지 r2 D2-7) |
| T-04-92 | Tampering | 자동 전환과 목록의 순서 | low | mitigate | (CEO C-09 서술 정정 — 집계와 목록은 여전히 두 문장이라 스냅숏을 공유하지 않는다) `loadProjectList`가 판정을 한 번 먼저 하고 집계 → 목록 순으로 읽어 자정 뒤 첫 화면의 어긋남 창을 줄인다. 판정 실패는 04-11의 fail-open으로 목록을 막지 않는다 |
| T-04-315 | Denial of Service | 목록 요청의 조회 수 · 표 전체 집계 | medium | mitigate | (ENG-D3 ② · 엔지 리뷰 C §4) 행 노출 판정은 `projectMany`로 항목당 한 번(1행 = 50행 조회 수 단언), 견적 줄 합은 현재 차수만, 발행 합은 필터 뒤 LATERAL — `EXPLAIN` 기준을 SUMMARY에 둔다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `project-list-view.test.ts` · 통합 `projects-list.test.ts`(+ `quote-line-kinds.test.ts` 불변식 · `leak-scan.test.ts`) 초록
- `CI=true` 목록 E2E(`projects-list.spec.ts`) 초록 — 독립 DOM 감사(1280·1024·375)와 전체 게이트 `CI=true pnpm test` 한 번은 04-48 끝에서 목록 화면 전체를 대상으로 한다(엔지 r2 E2-01 분할)
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 조건 없는 목록이 올해 보기이고, 합계가 표 위에서 귀속 연도 행만 전액(21억 초과도 정확히) 더하며, 제외 건수를 말한다
- 해를 걸친 프로젝트가 두 해 목록에 보이지만 연간 합계에는 한 번만 들어간다
- 목록이 50건 번호 페이지이고 범위 밖 번호·그룹 반복 규칙이 지켜진다(필터 변경 시 1쪽·정렬 유지·기간 필터·틀린 파라미터·연도 자동 전환은 04-48)
- 수익금 기준이 정산·완료 + 발행 줄 1개 이상일 때만 발행이다 — 부분 발행이 진행 건의 수익금을 뒤집지 않는다(계약 7, DR-8 · DR-38)

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/projects/list-view.ts` — `resolveListRange` · `attributionLabel` · `exclusionText` · `totalsTitle` · `bucketTotal` · **`profitBasisFor`**(계약 7) (쪽 보정·쪽 수는 `lib/paging` — 04-29) — 서버 전용 import 없음(04-48이 필터 줄 클라이언트용 함수를 같은 파일에 더한다) |
| 컴포넌트(신규, app) | `app/(app)/projects/list-totals.tsx` — 표 위 합계 줄 |
| 함수(신규, domain) | `loadProjectList(viewer, query, deps?)` — 목록 화면의 유일한 입구(판정 한 번 → 집계 → 보정 → 목록 → `projectMany` 투영) |
| 함수(삭제, domain) | `listProjects` · `aggregateProjects`(옛 입구 — 호출자 테스트는 `loadProjectList`로 옮김) |
| 타입(신규) | `ProjectListTotals`(DTO 레지스트리 등록) · 목록 결과(`rows` · `totals` · `page` · `pageCount` · `total` — `emptyKind` · `periodErrors`는 04-48이 더한다) |
| 상수(변경) | `PROJECT_LIST_DEFAULT_LIMIT` · `PROJECT_LIST_MAX_LIMIT` 삭제 → `LIST_PAGE_SIZE`(04-29 `lib/paging`, 50) |
| 리포지토리(변경) | 돈 집계 `::bigint` + `mapWith(Number)`(C-01) · 견적 줄 합 현재 차수만 · 발행 합 `LEFT JOIN LATERAL`(신규, 발행만) · 행 단위 기준·수익금·수익률 식(기준 ≤ 0 → null) · `ProjectListFilter.range`(겹침 + 기간 미정 규칙) · 보관 프로젝트 항상 제외(D18) · `listProjectsPage({ offset, limit })` · `aggregateProjects`(귀속 구간 GROUP BY) |
| 재사용(04-29) | `ui/pagination/Pagination.tsx`(링크 갈래) · `pageRangeText` |
| URL 파라미터 | `year`(없음 = 올해, `all`) · `page`(신규) — `count` 폐기(`from`·`to`·정렬 숨은 값·연도 자동 전환 `redirect`는 04-48) |
| 리포지토리 식(변경) | 기준 금액 = 정산·완료 + 발행 줄 ≥ 1이면 발행 합계, 그 밖은 견적(계약 7 — CONTEXT D-87 계산 규칙 개정) |
| 테스트 | `test/unit/domain/project-list-view.test.ts`(신규 — `loadProjectList` 스텁 오류 포함) · `test/integration/projects-list.test.ts`(확장 — 행 키 집합·조회 수) · `project-auto-settlement.test.ts`·`quote-lines.test.ts`(옛 입구 호출을 새 입구로 옮김) · `test/e2e/projects-list.spec.ts`(개정) |
| 위협(신규) | T-04-370(목록·합계 스냅숏 차이 — E2-08 accept) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | `ui/pagination` 컴포넌트·번호 창(04-29) · 상단 진행 막대(사용자 D17 — 이연) · 옛 Task 3 전부(04-48 — 엔지 r2 E2-01: 기간 필터 · 연도 자동 전환 · 파라미터 정규화 · 정렬 유지 · 필터 지우기 · 빈 목록 세 갈래 · 기간 칸 서식 · 폰 첫 화면 · 로딩 뼈대 · 목록 화면 DOM 감사 · T-04-90 · T-04-367) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| C-01 — 합계 `::int` 21억 초과 오류 · 문자열 합계 | CRITICAL | Plan 04-17, Task 1 ①(a) `::bigint` + `mapWith(Number)` · 통합 3,000,000,000 + `typeof` · Task 2 `pageCountFrom` 단위 |
| C-08 — 틀린 URL 파라미터가 500 | WARNING | Plan 04-17, Task 3 ①② `normalizeListParams` · 통합 파라미터 표 · E2E · T-04-90 정정 |
| C-09 / 사용자 D19 — 목록 읽기 판정 실패 격리 | WARNING | 04-11 fail-open + Plan 04-17, Task 1 ③ 판정 한 번 · T-04-92 서술 정정 |
| C-10 / 사용자 D17 — 진행 막대 서술 오류 | NOTE | Plan 04-17 `<probe_fallback>` 정정 · 만들지 않음(기록은 04-08) |
| C-11 / 사용자 D19 — 기간 미정 노출 기본값 | DECISION | 확정 반영(`<probe_fallback>`) |
| C-14 — 입금 줄 제외·키 집합·합계 DTO 등록 | WARNING | Plan 04-17, Task 1 ①(c)·③ · 통합 입금 픽스처·키 집합 넷 · `registerDto` |
| C-16 / 사용자 D19 — 기준 ≤ 0 수익률 | DECISION | Plan 04-17, Task 1 ①(d) `CASE WHEN 기준 > 0` · 합계도 Σ기준 ≤ 0 → null |
| C-17 — E2E 연도가 UTC | NOTE | Plan 04-17, Task 1 ⑤·Task 3 ⑤ `kstYear` · grep 0건 |
| C-21 / 사용자 D18 — 보관 프로젝트 항상 제외 | DECISION | Plan 04-17, Task 1 ①(f) · 통합 |
| §8 관측성(C-09에 접힘) — 목록 읽기 실패가 stderr만 | NOTE | Plan 04-17, Task 1 ③: `project.list_failed` 오류 로그 후 다시 던짐 |
| C-23 — 요청 순서·성능 | NOTE | Plan 04-17, Task 2 ① 순서 고정 · DOM 감사 (f) 참고값 |
| C-24 — 필터 바꾸면 정렬 사라짐 | NOTE | Plan 04-17, Task 3 ③ 숨은 `sort`·`dir` · E2E |
| C-26 — 04-17~04-19 중간 상태 | NOTE | `<probe_fallback>` 배포 묶음 — 2026-09-24 머지 묶음 ④(04-17·04-19·04-47·04-48·04-18 한 배포)로 고쳐 씀(옛 「Phase 4 한 PR」) |
| C-27 — 번호 창 한 쪽 틈 | NOTE | 04-29가 구현(이 플랜은 재사용) |
| A-04 — 보관 줄 목록 합계 | CRITICAL | 04-12가 고치고 Plan 04-17 통합이 확인 |
| A-07 — 합계·목록 판정 순서 | WARNING | Plan 04-17, Task 1 ③·Task 2 ① |
| OV-5 — 읽기 판정 격리 | HIGH | 04-11 구현 + 목록 입구의 판정 한 번 |
| 파일 상한(옛 16파일) | WARNING | `ui/pagination`을 04-29로 떼어 12파일 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 목록 상단 2px 로딩 막대 | 사용자 D17 「만들지 않고 기록」 — 디자인 잔여 퀵 태스크로 이연(CEO 리뷰 범위 밖) |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 C §1 P1 — `loadProjectList` 행 투영 단계 없음 · 키 집합 테스트가 옛 입구 | HIGH | Plan 04-17, Task 1 ③ `projectMany` 투영 · 옛 `listProjects`·`aggregateProjects` 삭제와 호출자 테스트 이관(04-05·04-11·04-12 — ENG-D11) · 통합 행 키 집합(공백 1) · T-04-89 상향 |
| ENG-D3 ② / 엔지 리뷰 C §1 P2 — 행×필드 노출 N+1 · all-of 수동 삭제 | DECISION | Plan 04-17, Task 1 ③ `projectMany` · 합계 수익 두 칸 all-of 명세 · 통합 조회 수 1행 = 50행 · T-04-315 |
| 엔지 리뷰 C §4 P2 — 견적 줄·발행 합이 표 전체 GROUP BY | WARNING | Plan 04-17, Task 1 ①(b)(c) 현재 차수만 · LATERAL · `EXPLAIN` 기록(DOM 감사 f · SUMMARY) |
| 엔지 리뷰 C §2 P2 — 기간 두 칸 사이에서 문서 다시 로드 | WARNING | Plan 04-17, Task 3 ③ 묶음 `focusout` + `relatedTarget` + Enter · E2E |
| 엔지 리뷰 C §2 P2 — 창 밖 연도가 선택지에 없어 `year=all`로 새어 나감 | WARNING | Plan 04-17, Task 3 ① `yearOptions` · 단위(공백 10) · E2E |
| 엔지 리뷰 C §1 P3 — 쪽 보정 세 벌 | NOTE | Plan 04-17, Task 2 ① `lib/paging`(04-29) import · `bucketTotal`만 이 파일에 |
| 엔지 리뷰 C P3 — 낡은 주석·`.loadMore` | NOTE | Plan 04-17, Task 1 ①(f) · Task 2 ② · Task 3 ③ · 수용 기준 grep |
| 엔지 리뷰 C 공백 7·8 — 목록 실패 로그 Grep · `none` 갈래 통합 | NOTE | Plan 04-17, Task 2 behavior(스텁 리포지토리) · Task 3 behavior(`none`) |
| 엔지 리뷰 A P3 — 목록 요청당 판정 한 번(04-11 뒤) | NOTE | Plan 04-17, Task 1 ③ `settleForProjectList`를 입구 안으로 옮기고 페이지 호출 제거 |
| 교차 그룹 — 승인 준비는 `approvalBasis` | NOTE | objective 실행 규율 문장 |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 기간 겹침 인덱스 | 엔지 리뷰 「범위 밖」 — `projects`가 5만 행을 넘으면 |
| `q` 검색 ILIKE 와일드카드 이스케이프 · 목록 **행** 링크 `next/link` | 엔지 리뷰 「범위 밖」 — 원래 있던 낮은 위험 · 지금 허용(페이지 줄 링크는 계약 5로 `next/link` — 04-29) |

### Round 3 — 98acc74 (plan-design-review, `docs/designs/plant8-erp-phase4-design-review-260923.md` · UI-SPEC rev 5 재계획 그룹 C)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-8 · DR-38 — 부분 발행 순간 수익금이 음수로 뒤집힘 · 정산·완료 발행 0이면 −실행가(교차 그룹 계약 7) | HIGH | Plan 04-17, truth · Task 1 ①(d) 상태 조건 CASE · ② `profitBasisFor` · 단위 표 · 통합(DR-8 진행 건 · DR-38 정산 0 발행 · 다섯 상태 정합) · 합계 분모 |
| DR-30 — 연도·기간 교집합이 빈 「검색 결과 없음」 | MEDIUM | Plan 04-17, truth · `<probe_fallback>` 규칙·반대 방향 · Task 3 ①②③ `reconcileListYear` + `redirect` · 연도 `onChange` 기간 비우기 · E2E 셋 · T-04-367 |
| DR-26 — 폰 첫 화면(목록) | MEDIUM | Plan 04-17, truths(요약 explicit · 첫 화면 backstop) · Task 3 ③ 「필터」 disclosure · 요약 · `primaryAction` · E2E · DOM 감사 (g) |
| 교차 그룹 계약 6 — DOM 감사 폭 1024 | NOTE | Task 3 ⑥ 1280·1024·375 · backstop 문구 |
| DR-32 · DR-34 — 클라이언트 두 줄 · `종료일 지남` `--fs-xs` | LOW | 04-18로(클라이언트 열·상태 2행을 만드는 플랜) |
| DR-1 — rev 4 행 범위 인용 | NOTE | context·read_first를 rev 5 절 이름 + 현재 행으로 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 4 — plan-checker 반복 1(2026-09-23 revision)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W2 — 120k · 14파일(한 실행자 문맥) | WARNING | 이번 반복 규칙(「04-17은 중복 제거로 줄이고, 비테스트 파일이 여전히 15개를 넘을 때만 분할」)에 따라 **분할하지 않는다**: 비테스트 10 · 전체 14파일로 한도 안이다. B4·W4·W1의 중복 제거는 이 플랜과 겹치는 몫이 없고(쪽 보정은 이미 04-29 `lib/paging` — 엔지 리뷰 C §1 P3), 남은 테스트 파일 넷은 새 입구로 옮기는 옛 호출자(ENG-D11 — 04-05·04-11·04-12 테스트)라 지울 수 없다. 세 태스크는 한 관심사씩(합계·숫자 경계 / 페이지 / 필터·연도 자동 전환·폰)이라 각 태스크가 따로 한 문맥에 든다. 변경 없음 |

### Round 5 — 엔지니어링 리뷰 r2 반영(2026-09-24 재계획 · `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` · 머지 묶음 결정)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2-01 — 95KB·3태스크로 체커 상한 초과 | HIGH | 옛 Task 3을 신규 04-48(웨이브 26, `depends_on: ["04-17"]`)로 분리 — truths 11개 · backstop 넷(폰 합계 줄·폰 첫 화면·연도 자동 전환 렌더·폰 페이지 줄) · T-04-90 · T-04-367 · `filter-bar.tsx`·`loading.tsx`가 함께 이동. estimate 120k → 85k, 2태스크 |
| E2-01 분할안 — 파라미터 정규화를 04-17 Task 2로 당길지 | NOTE | 당기지 않음 — `page`는 04-29 `clampPage`가 이미 보정하고, 정규화 전 구간은 같은 묶음 ④ 안이라 배포되지 않는다(objective r2 (a)) |
| E2-08 — 목록·합계 스냅숏 차이 | LOW | T-04-370 accept |
| 머지 묶음 결정(HANDOFF decisions[0] — R11 대체) · C-26 | DECISION | objective 머지 묶음 줄 · `<probe_fallback>` 배포 묶음 줄 · C-26 행 고쳐 씀 · Task 2 E2E `CI=true`(완료 판정) |
| CEO r2 지시 3 — T-04-SC 「04-03 재개」 문구 | LOW | 삭제 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-17-SUMMARY.md` when done
</output>
