---
phase: 04-project-quote-ledger
plan: 17
type: execute
wave: 22
depends_on: ["04-16", "04-40", "04-41", "04-29"]
files_modified:
  - repositories/projects.ts
  - domain/projects/index.ts
  - domain/projects/list-view.ts
  - test/unit/domain/project-list-view.test.ts
  - app/(app)/projects/page.tsx
  - app/(app)/projects/filter-bar.tsx
  - app/(app)/projects/list-totals.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/projects.module.css
  - app/(app)/projects/loading.tsx
  - test/integration/projects-list.test.ts
  - test/e2e/projects-list.spec.ts
autonomous: true
requirements: [PROJ-01, UX-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/projects`를 조건 없이 열면 올해(KST) 보기다 — 연도 필터 값이 올해이고, 프로젝트 기간이 올해와 겹치는 행이 보인다. 해를 걸친 프로젝트는 겹치는 해마다 목록에 나온다(D-89)"
    - "연도 선택지 맨 위에 `전체 연도`가 있고 고르면 연도 범위가 없다(URL `year=all`) — 조건 없는 URL은 전체 연도가 아니라 올해다(D-89)"
    - "합계는 표 **위** 한 줄이다 — 필터 줄 바로 아래 `합계 (2026 귀속 · 48건)` + `매출 · 견적 · 실행가 · 수익금 · 수익률` 쌍, 아래 `2px --line-strong`. 표 아래 합계 행과 「더 보기 50건」은 없다(D-88, SYSTEM.md 개정 ③)"
    - "합계는 페이지와 무관하게 필터 전체 기준이고 집계 쿼리 한 번으로 나온다 — 목록 쿼리와 같은 행 필터 함수를 쓴다(D-88·D-91, T-04-28 유지)"
    - "목록 합계는 21억 원을 넘어도 오류가 나지 않고 정확하다 — 돈 합계는 `bigint`로 더해 리포지토리 경계에서 JS 숫자로 바꾸고, 건수는 정수다. 리포지토리는 모든 집계·행 금액을 문자열이 아닌 숫자로 돌려준다(CEO C-01 — 지금은 `::int` 캐스트가 21억 초과에서 목록 전체를 오류 화면으로 만들고, 합계가 문자열로 와서 쉼표 없이 보인다)"
    - "합계에는 귀속 연도(종료일의 해)가 보기 범위 안인 행만 전액 들어간다. 범위 밖에서 끝나는 행은 보이되 합계에서 빠지고, 기간 칸 2행에 `2027 귀속`(기간 보기면 `2026-11 귀속`)이 붙으며, 합계 줄 오른쪽에 `2027 귀속 3건 제외`(기간 보기면 `기간 밖 귀속 N건 제외`)가 붙는다(D-90, Copywriting 표시 — 귀속·목록 합계 줄)"
    - "종료일이 없는 행은 맨 아래 `기간 미정` 그룹에 보이고 언제나 합계 밖이며 `기간 미정 N건 제외`로 적힌다. 0건인 제외 표시는 쓰지 않는다(D-90)"
    - "괄호의 건수는 합계에 든 행 수이고, 표에 보이는 전체 행 수는 페이지 줄의 `/ N건`이 말한다. 상태 필터가 있으면 `합계 (진행 · 2026 귀속 · 12건)`, 전체 연도면 `합계 (전체 연도 · 125건)`, 기간 필터면 `합계 (2026-09-01 ~ 2026-10-31 귀속 · 12건)`(Copywriting 표시 — 목록 합계 줄)"
    - "합계 수익률 = 합계 수익금 ÷ 행마다의 기준 금액(발행 줄이 있으면 발행 합계, 없으면 견적) 합이고, 기준이 0 **이하**면 `—`다 — 수정세금계산서로 발행 합계가 음수가 된 행이 부호가 뒤집힌 비율을 만들지 않는다. 전부 서버 계산이다(D-87·D-88, 사용자 D19 — C-16 권장안)"
    - "매출·기준은 발행(`issue`) 줄만 더한다 — 입금(`payment`) 줄은 매출·수익금에 들어가지 않는다(D-85 — 입금 줄이 있는 픽스처로 단언, CEO C-14)"
    - "합계 줄은 받은 금액만 쓴다 — 견적·실행가는 `quote.amount`, 매출은 `revenue.issued_amount`, 수익금·수익률은 두 항목을 모두 볼 수 있을 때만 서버가 보낸다(발행 기준 수익금 + 실행가 = 발행 합계 역산). 둘 다 못 보면 금액 쌍이 하나도 없다. 합계 DTO는 DTO 레지스트리에 등록돼 누수 스캔이 본다(S1 partial ①, D-38, CEO C-14)"
    - "보관된 프로젝트는 **누구에게나** 목록과 합계에서 빠진다 — 보관함을 볼 수 있는 시스템 관리자도 같다(사용자 D18 — C-21)"
    - "목록은 50건씩 번호 페이지다 — 50건 이하면 페이지 줄이 없고 51건부터 표 아래 `51–100 / 125건  이전 · 1 2 3 · 다음`이 생긴다(04-29 `Pagination` 링크 갈래). URL은 `?page=N`이고 필터·정렬 파라미터를 보존하며 옛 `count` 누적 파라미터는 없어졌다(D-91, S12)"
    - "범위 밖 페이지 번호(`?page=99`)는 오류 화면이 아니라 마지막 페이지로 보인다. 숫자가 아니거나 1보다 작으면 1쪽이다(S12 error)"
    - "틀린 URL 파라미터는 오류 화면이 아니라 기본 보기로 떨어진다 — uuid가 아니거나 목록에 없는 `teamId`는 전체 팀, 2000–2100 밖의 연도는 올해, 같은 이름이 여럿이면 첫 값만 쓴다. 틀린 값이 SQL에 닿지 않는다(CEO C-08)"
    - "종료월 그룹이 페이지를 넘으면 다음 페이지 맨 위에 같은 그룹 머리글이 다시 보인다(D-91, SYSTEM.md 개정 ③)"
    - "필터 줄에 `기간 [2026-01-01] ~ [2026-12-31]` 날짜 칸 둘이 있고(폭 `10ch` + 좌우 `--cell-pad-x`, 자리표시자 `2026-09-18`) blur·Enter에서 GET으로 다시 제출된다. 보기 범위 R = 연도 범위 ∩ 기간(있는 쪽만)이고 행은 프로젝트 기간이 R과 겹치면 보인다(D-89, S1 필터 한 줄)"
    - "기간 칸의 형식이 틀리거나 거꾸로면 칸 아래 한 줄 `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` / `기간이 거꾸로입니다 · 앞 날짜를 먼저 적어 주세요`가 보이고 기간 필터는 적용되지 않는다 — 판정은 서버다(UX-04, Copywriting Error — 목록 기간 필터)"
    - "필터를 바꾸면 1쪽으로 돌아가되 지금 정렬은 유지된다 — 필터 폼이 `sort`·`dir`을 숨은 값으로 싣는다. 「필터 지우기」는 기본 보기(올해·전체 상태·전체 팀)와 다른 값이 하나라도 있을 때만 보이고 누르면 기본 보기다 — 올해 연도 값은 필터로 세지 않는다(S1, CEO C-24)"
    - "빈 목록은 세 갈래다 — 볼 수 있는 프로젝트가 하나도 없으면 `등록된 프로젝트가 없습니다 · 프로젝트 등록`, 기본 보기(올해)가 0건이면 `2026년에 걸친 프로젝트가 없습니다 · 전체 연도 보기`, 사용자 필터가 0건이면 `조건에 맞는 프로젝트가 없습니다 · 필터 지우기`. 셋 다 합계 줄과 페이지 줄을 그리지 않는다(S1 empty)"
    - "첫 번째 빈 갈래일 때만 필터 줄의 1차 「프로젝트 등록」을 생략한다(EMPTY가 같은 행동을 준다) — 나머지 두 갈래에서는 1차가 그대로 있다"
    - "로딩 뼈대는 합계 줄 자리에 라벨만(금액 자리 막대 없음) + 머리글 + `--surface` 행 3개이고 반짝임이 없다 — 표 아래 합계 행 뼈대가 없다. 상단 2px 진행 막대는 만들지 않는다(S1 loading, SYSTEM.md 개정 ③ §7-7, 사용자 D17)"
    - "기간 칸은 같은 해이고 그 해가 보기 연도면 `09-12 ~ 09-16`, 같은 해지만 보기 연도가 아니거나 없으면 `2025-03-01 ~ 03-05`, 해를 걸치면 `2026-11 ~ 2027-02`, 한쪽만 있으면 없는 쪽이 `—`다. `nowrap`이다(D-89, S1 열 — 기간)"
    - "목록 합계에서 보관된 견적 줄과 발행 줄은 빠진다 — 상세 합계 행과 같은 줄 집합이다(04-12 보관 경로와의 정합, CEO A-04)"
    - "목록 화면은 한 요청에서 자동 전환 판정을 **먼저 한 번** 하고 합계 → 페이지 보정 → 목록 순으로 읽는다 — 판정이 실패해도 목록은 저장된 상태로 그려진다(04-11 fail-open, CEO A-07·C-09·C-23)"
    - "연간 합계·귀속 구간은 읽기 시점 집계이고 저장 컬럼이 없다 — 날짜 원본(`start_date`·`end_date`·`revenue_entries.entry_date`)에서 매번 계산하므로, Phase 9·10이 연도별 손익·인센티브의 연도 기준을 다시 정해도 마이그레이션이 필요 없다(D-92의 이 페이즈 제약. 연도 기준 결정 자체는 이 페이즈 범위 밖)"
    - "합계 줄·페이지 줄의 숫자는 `lib/format-number.ts`로 쉼표가 붙고, 연도(`2026`)·`2027 귀속`·기간 칸 날짜에는 쉼표가 없다(D-95, S15)"
    - statement: "합계 줄은 폰(375)에서 `라벨 값` 쌍 단위로 줄바꿈하고 쌍 안은 꺾이지 않으며 제외 표시는 아래 줄로 간다 — 가로 스크롤 0. DOM 감사"
      verification: backstop
    - statement: "페이지 줄이 폰에서 번호 44×44이고 한 줄에 안 들어가면 범위 글자가 위 줄로 올라간다 — 가로 스크롤 0. DOM 감사"
      verification: backstop
    - statement: "N=0/1/50/51/125 렌더에서 0은 EMPTY, 1과 125가 같은 `합계 (… · N건)` 형식이고, 50 이하는 페이지 줄 없음, 51부터 있음, 제외 0건이면 제외 표시 없음 — 통합·E2E 렌더"
      verification: backstop
  prohibitions:
    - statement: "합계가 사용자가 볼 수 없는 행이나 필드를 더해서는 안 된다. 목록과 합계가 다른 행 필터를 쓰면 합계가 그 사람에게 가려진 프로젝트의 금액을 드러낸다 — 귀속 조건은 같은 행 필터 **위에** 얹는 추가 분류여야 하고, 수익금·수익률은 매출과 견적을 둘 다 볼 수 있는 사람에게만 가야 한다"
      status: resolved
      verification: test
    - statement: "같은 프로젝트가 두 해의 합계에 두 번 들어가서는 안 된다. 해를 걸친 프로젝트는 겹치는 해마다 목록에 보이지만 전액은 종료일의 해 한 곳에만 들어간다 — 해마다 나눠 넣거나 양쪽에 넣으면 연간 합계를 더한 값이 회사 전체 합계를 넘는다(D-90)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/list-view.ts"
      provides: "보기 범위 · 귀속 라벨 · 합계 제목 · 제외 문구 · 기간 칸 서식 · 기간 필터 판정 · 페이지 보정 · URL 파라미터 정규화 — 순수 함수"
      exports: ["resolveListRange", "attributionLabel", "totalsTitle", "exclusionText", "formatListPeriod", "parseListPeriod", "clampPage", "normalizeListParams"]
    - path: "app/(app)/projects/list-totals.tsx"
      provides: "표 위 합계 줄(dl) + 제외 표시"
      contains: "<dl"
    - path: "repositories/projects.ts"
      provides: "겹침 필터 · 기간 미정 노출 규칙 · 보관 프로젝트 항상 제외 · 발행 합계 · 기준·수익금·수익률 · 귀속 구간별 집계 한 번 · OFFSET 페이지 · 모든 집계가 JS 숫자(bigint → Number)"
      contains: "mapWith(Number)"
    - path: "test/integration/projects-list.test.ts"
      provides: "겹침·귀속·제외·이중 계산 금지·페이지·범위 밖 페이지·보관 줄·보관 프로젝트 제외·합계 필드 노출(셋 + 없음)·21억 초과·입금 줄 제외·틀린 파라미터 증명"
  key_links:
    - from: "repositories/projects.ts"
      to: "repositories/projects.ts"
      via: "listProjectsPage와 aggregateProjects가 같은 projectFilterConditions를 부르고, 집계는 그 위에 귀속 구간 CASE만 얹는다"
      pattern: "projectFilterConditions"
    - from: "domain/projects/index.ts"
      to: "domain/projects/auto-transition.ts"
      via: "loadProjectList가 집계·목록보다 먼저 자동 전환을 한 번 적용하고 리포지토리 함수를 직접 부른다(04-11 — 판정이 요청마다 여러 번 돌지 않는다)"
      pattern: "applyAutoSettlement"
    - from: "app/(app)/projects/page.tsx"
      to: "ui/pagination/Pagination.tsx"
      via: "서버 페이지가 04-29 컴포넌트의 href 함수 갈래로 페이지 줄을 렌더한다(필터·정렬 보존)"
      pattern: "Pagination"
    - from: "domain/projects/index.ts"
      to: "lib/kst-date.ts"
      via: "loadProjectList의 기본 연도가 주입 가능한 now에서 KST로 나온다"
      pattern: "kstYear"
    - from: "domain/projects/index.ts"
      to: "domain/permissions/dto-registry.ts"
      via: "`ProjectListTotals`가 `registerDto`로 등록돼 누수 스캔이 합계 키를 본다(CEO C-14)"
      pattern: "registerDto"
---

<objective>
**As a** 경영관리와 기획 PM, **I want to** 프로젝트 목록을 열면 올해 걸친 프로젝트와 그 합계가 표 위에 먼저 보이고, 해를 걸친 프로젝트는 겹치는 해마다 보이되 합계는 끝나는 해에만 들어가며, 125건을 번호 페이지로 넘기고 싶다, **so that** 인트라넷처럼 합계를 먼저 읽고, 연간 합계를 더해도 이중 계산이 없으며, 「더 보기」를 누적하다 위치를 잃지 않는다.

이 플랜은 04-05가 만든 목록(전체 연도 기본 · 종료일 연도 단일 매칭 · 표 아래 합계 · 「더 보기」 누적)을 D-88·D-89·D-90·D-91(목록 부분)로 고친다. 옛 D-51의 「더 보기 50건」과 전체 연도 기본은 **대체됐다**(D-51의 전체 상태 기본·종료월 그룹은 유지). 열 구성(클라이언트 열·매출·기준·수익금·수익률 D-87)·머리글 정렬·좁은 폭 열 접기·`종료일 지남`(D-81 목록)은 04-18, 견적 줄 30줄 페이지는 04-19다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE):** C-01(21억 초과 합계 오류·문자열 합계 — 선행 버그) · C-08(틀린 URL 파라미터가 오류 화면) · C-09·A-07·C-23(판정 한 번 → 합계 → 보정 → 목록, fail-open) · C-10(진행 막대 서술 정정 — 사용자 D17로 만들지 않고 04-08이 DECISIONS에 기록) · C-11(기간 미정 기본값 — 사용자 D19 확정) · C-14(입금 줄 픽스처 · 키 집합 「없음」 · 합계 DTO 등록) · C-16(기준 ≤ 0 → `—`, 사용자 D19) · C-17(E2E 연도 `kstYear`) · C-21(보관 프로젝트 항상 제외, 사용자 D18) · C-24(필터 바꿔도 정렬 유지) · C-26(04-17~04-19는 한 배포 — 「Phase 4는 한 PR」 제약에 포함) · A-04(보관 줄 제외 확인). **순서(T16 — 웨이브 22):** `repositories/projects.ts`·`domain/projects/index.ts`·목록 `page.tsx`를 먼저 고치는 그룹 B의 04-15·04-40(04-14 분할, 쓰기 직렬화)·04-16·04-41(계약 칸 삭제) 뒤에 서고, 견적 표 쪽 나눔 04-19와는 겹치는 파일이 없어 같은 웨이브에서 병렬로 돈다. **파일 상한 분할:** `ui/pagination`은 04-29로 앞당겨 만들었고(C-27 포함) 이 플랜은 목록에 꽂기만 한다. 상세는 끝의 Review Dispositions Ledger.

Purpose: 목록의 숫자가 **어느 범위의 무엇**인지가 이 플랜에서 정해진다. 04-18이 행 단위 금액 열을 올리려면 같은 SQL(발행 합계·기준·수익금)이 먼저 있어야 한다.

Output: 목록 범위·귀속·합계·페이지·파라미터의 순수 함수 모듈 · 리포지토리 확장(숫자 경계 포함) · 목록 화면 개정 · 단위 1 · 통합 1(확장) · E2E 1(개정).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-05-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-29-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 413–423행(S1 상태 + ⚠ 기간 미정) · 515–523행(S12 상태) · 944–1037행(S1 적용 규칙 — 필터·합계 줄·그룹·기간 칸) · 874–895행(§7-16 페이지 줄 모양) · 790–822행(SYSTEM.md 개정 ③) · 318–320행·332행·341행·353–356행(Copywriting)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 788–801행(§6 D-87~D-91 — 겹침 조건 · 귀속 분리 · OFFSET Gap)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 405행·421–422행·466–470행
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1696–1709행(C-01) · 1807–1842행(C-08·C-09) · 1892–1904행(C-14)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-01(목록 부분)**이다. 프로브는 04-05 시점 목록으로 돌았으므로 수용 기준은 D-88~D-91과 UI-SPEC S1·S12로 끌어왔다. S1·S12 `## UI Considerations`의 empty·loading·error·populated·partial·zero-one-many 중 이 플랜 범위(범위·합계·페이지)는 truths로, overflow(폰 합계 줄·페이지 줄)·zero-one-many는 backstop 마커로 옮겼다. S1 partial ②③⑥과 열 폭·좁은 PC 열 접기·폰 열 우선순위는 04-18 몫이다. 페이지 줄 컴포넌트 자체의 truths는 04-29에 있고, 이 플랜은 목록 화면 배선·DOM 감사를 맡는다.

**⚠ S1 「기간 미정」 행이 어느 보기에 나오는가 — 사용자 D19 확정(C-11 권장안):** 종료일이 없는 행 중 **수주중은 보기 범위와 무관하게 항상** 보이고, 그 밖의 상태(미수주 등)는 **등록일(KST 날짜)이 보기 범위 R 안**일 때만 보인다. 전체 연도 + 기간 없음이면 전부 보인다. 되돌리기: 조건 한 줄(reversible).

**계획 단계 가정(표면화):**
- **시작일만 없는 행**(종료일은 있음)은 겹침 판정에서 시작일 = 종료일로 본다. D-82 이후 진행·정산·완료는 시작일이 있으므로 이 경우는 수주중·미수주의 과거 데이터뿐이다.
- **R이 비는 조합**(예: 연도 2026 + 기간 2025-01-01~2025-03-01)은 쿼리를 보내지 않고 사용자 필터 0건 EMPTY다 — 빈 구간과의 겹침 식은 두 끝을 모두 넘는 행을 잘못 잡는다.
- **한쪽이 열린 기간 보기의 괄호 라벨**은 열린 쪽을 `—`로 쓴다(`2026-09-01 ~ — 귀속`).
- **연도 보기의 제외 문구 순서**는 해 오름차순 뒤 `기간 미정`(`2025 귀속 1건 제외 · 2027 귀속 3건 제외 · 기간 미정 5건 제외`).
- **상단 2px 진행 막대(C-10 서술 정정 · 사용자 D17):** SYSTEM.md는 그 막대를 `2px --accent`로 정해 두었다(§5·§7-7·§7-10) — 색이 없는 것이 아니라 §1-3 「`--accent` 사용처 다섯 곳」과 충돌하는 것이 문제이고, 막대는 앱 어디에도 아직 없다. 사용자 D17 「만들지 않고 기록」에 따라 이 플랜은 막대를 만들지 않는다(충돌 기록은 04-08이 `DECISIONS.md`에 남겼고, 막대는 디자인 잔여 퀵 태스크로 이연 — CEO 리뷰 「범위 밖」). 목록의 필터·정렬·쪽 이동은 전체 문서 GET이라 `loading.tsx`가 스트리밍된다 — 로딩 표시는 그 뼈대다.
- **틀린 파라미터(C-08):** 도메인 입구가 `normalizeListParams`로 정규화한 뒤에만 쓴다 — `teamId`는 uuid 모양이고 뷰어가 고를 수 있는 팀 목록에 있어야, 연도는 `all` 또는 2000–2100 정수, 배열이면 첫 값. 기간 칸은 오류를 칸 아래에 보이는 별도 규칙(`parseListPeriod`)이 그대로다.
- **배포 묶음(C-26):** 04-17과 04-18 사이에는 합계가 D-87 수익금, 행이 옛 차익을 보인다 — 「Phase 4는 한 PR로 배포」(사용자 D9 전제 — 그룹 B의 04-41이 기록)에 포함되어 중간 상태가 배포되지 않는다 — 04-17·04-18·04-19는 같은 배포로 나가고 되돌릴 때도 함께 되돌린다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 올해 기본 보기 · 기간 겹침 · 표 위 귀속 합계 줄 · 숫자 경계(리포지토리 → 도메인 → 합계 줄 → E2E)</name>
  <files>repositories/projects.ts, domain/projects/list-view.ts, domain/projects/index.ts, test/unit/domain/project-list-view.test.ts, app/(app)/projects/page.tsx, app/(app)/projects/list-totals.tsx, app/(app)/projects/projects-table.tsx, app/(app)/projects/projects.module.css, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `repositories/projects.ts` 1–210행(`currentRevisionsSubquery`·`lineSumsSubquery`(04-12가 보관 제외를 더했다)·`projectFilterConditions`(88행 `includeArchived`)·`resolveSortColumn`·`listProjectsPage`·`aggregateProjects`(197–200행 `::int`))
    - `domain/projects/index.ts` 80–215행(`registerDto` 두 곳 · `PROJECT_LIST_DTO_SPEC`·`listProjects`·`aggregateProjects` — 04-11이 넣은 `applyAutoSettlement` 호출)
    - `lib/kst-date.ts`(04-29 — `kstToday`·`kstYear`·`kstDateOf`) · `lib/format-number.ts`(04-09 — `formatKrw`·`formatPercent`·`formatCount`)
    - `db/schema/revenue-entries.ts`(전문 — `kind` `issue`/`payment`, `amountKrw`, `archivedAt`)
    - `app/(app)/projects/page.tsx`(전문) · `app/(app)/projects/projects-table.tsx`(전문) · `app/(app)/projects/projects.module.css`(전문)
    - `test/integration/projects-list.test.ts`(전문 — 픽스처 만드는 법, (a) 왕복 한 번 단언 방식) · `test/integration/leak-scan.test.ts`(등록 DTO를 훑는 방식) · `test/e2e/projects-list.spec.ts`(전문) · `test/e2e/fixtures.ts`
    - `domain/permissions/visible.ts`(정보 항목 판정 함수 이름)
  </read_first>
  <behavior>
    - `resolveListRange({ year: 2026 })` → `{ start: "2026-01-01", end: "2026-12-31", kind: "year", year: 2026 }` · `{ year: "all" }` → `null` · `{ year: 2026, period: { from: "2026-09-01" } }` → `{ start: "2026-09-01", end: "2026-12-31", kind: "period" }` · `{ year: 2026, period: { from: "2025-01-01", to: "2025-03-01" } }` → `{ empty: true }`
    - `attributionLabel({ endDate: "2027-02-10", range: 2026 연도 보기 })` → `2027 귀속` · 기간 보기 R=2026-09-01~2026-10-31, 종료 2026-11-20 → `2026-11 귀속` · 종료일이 R 안 → null · 종료일 없음 → null
    - `exclusionText({ byYear: { 2027: 3, 2025: 1 }, undetermined: 5, kind: "year" })` → `2025 귀속 1건 제외 · 2027 귀속 3건 제외 · 기간 미정 5건 제외` · 기간 보기 `{ outside: 2 }` → `기간 밖 귀속 2건 제외` · 전부 0 → null
    - `totalsTitle({ statusLabel: "진행", range: 2026 연도, count: 12 })` → `합계 (진행 · 2026 귀속 · 12건)` · 전체 연도 → `합계 (전체 연도 · 125건)` · 건수 1,250 → `1,250건`
    - 통합: 2026-11-10~2027-02-05 프로젝트는 2026 보기와 2027 보기 **둘 다**에 나오고, 합계에는 2027 보기에만 들어간다 — 두 해 합계의 합 = 전체 연도 합계(기간 미정 제외분 빼고)
    - 통합: 발행 줄이 있는 행은 기준 금액이 발행 합계(음수 줄 포함), 없는 행은 견적 — 합계 수익금 = Σ(기준 − 실행가), 합계 수익률 = Σ수익금 ÷ Σ기준(Σ기준 ≤ 0이면 null) · 발행 합계가 음수인 행의 행 수익률은 null(C-16)
    - 통합(C-01): 견적 줄 하나가 1,500,000,000원인 프로젝트 둘 → 전체 연도 합계 견적이 정확히 3,000,000,000이고, 합계와 행의 모든 금액·건수 필드가 `typeof === "number"`
    - 통합(C-14): 발행 1,000,000 + 입금 1,000,000인 프로젝트의 매출·기준이 1,000,000(입금이 더해지지 않는다)
    - 통합: 보관된 견적 줄과 보관된 발행 줄은 합계에서 빠진다 · 보관된 프로젝트는 시스템 관리자의 목록·합계에서도 빠진다(D18)
    - 통합(C-14): 키 집합 — `quote.amount`만 → 견적·실행가 · `revenue.issued_amount`만 → 매출 · 둘 다 → 다섯 · **둘 다 없음 → 금액 키 0개**
  </behavior>
  <action>
① **리포지토리(`repositories/projects.ts`).** (a) **숫자 경계(C-01):** 모든 돈 집계를 `::bigint`로 캐스트하고 `sql<number>\`…\`.mapWith(Number)`로 리포지토리 경계에서 JS 숫자로 바꾼다 — 목록 행 칸(`lineSumsSubquery`가 만드는 합)과 `aggregateProjects`의 합 모두. 건수는 `::int`. 함수 머리 주석에 불변식 한 줄: 「이 파일의 모든 집계·금액 반환은 JS number다」. (b) `lineSumsSubquery`의 보관 줄 제외(04-12)를 확인만 한다 — 통합 케이스가 고정한다(A-04). (c) 발행 합계 파생 표 `issuedSumsSubquery`를 더한다 — `revenue_entries`에서 **`kind = 'issue'`만** · 보관 제외 · 프로젝트별 `sum(amount_krw)::bigint`와 줄 수(C-14 — 입금 제외가 이 조건 하나에 달렸다). (d) 행 단위 SQL 식 셋을 한 곳(함수 하나)에 정의한다 — 기준 금액(발행 줄 수 > 0이면 발행 합계, 아니면 견적 합계) · 수익금(기준 − 실행가) · 수익률(`CASE WHEN 기준 > 0 THEN 수익금::float8 / 기준 END` — 0 이하는 null, 사용자 D19 · C-16). 목록 행(`ProjectListRow`)이 이 값들과 `revenueKrw`(발행 줄 0이면 null)·`issuedCount`·`profitBasis`(`quote`/`issued`)를 싣는다 — **DTO 투영은 04-18이 한다**. (e) `ProjectListFilter`의 `year`를 `range?: { start?: string; end?: string }`로 바꾸고 `projectFilterConditions`의 연도 조건을 **겹침**으로 바꾼다: 종료일이 있으면 `coalesce(시작일, 종료일) <= R.end`(있을 때) AND `종료일 >= R.start`(있을 때), 종료일이 없으면 `상태 = bidding` OR 등록일(KST 날짜)이 R 안(⚠ S1, `<probe_fallback>`). 등록일의 KST 변환식은 `created_at` 컬럼 형(시간대 없음)과 세션 `TimeZone = UTC`(04-11 통합 단언)를 전제로 정한다. (f) **보관 프로젝트(D18):** 88행의 `if (!scope.includeArchived)` 조건을 지우고 목록·합계는 항상 `archived_at IS NULL`을 건다(보관함 화면은 이 함수를 쓰지 않는다 — Grep으로 호출처 확인). (g) `aggregateProjects`는 같은 `projectFilterConditions` 위에 **귀속 구간** CASE(종료일 없음 → `undetermined` · 종료일이 R 안 또는 R 없음 → `in` · 그 밖 → 종료 연도 문자열)로 `GROUP BY`한 행들을 **한 번의 쿼리**로 돌려준다 — 구간마다 건수·매출·견적·실행가·수익금·기준 합(전부 bigint → Number).

② **순수 함수(`domain/projects/list-view.ts` 신규, import는 `lib/`만).** `resolveListRange` · `attributionLabel` · `exclusionText` · `totalsTitle`(건수는 `formatCount`) — `behavior` 앞 네 줄. 단위 테스트를 먼저 쓴다(RED → GREEN).

③ **도메인(`domain/projects/index.ts`).** 화면의 유일한 입구 `loadProjectList(viewer, query, deps?: { now?: () => Date })`를 더한다: 04-11의 `applyAutoSettlement({})`를 **먼저 한 번** 부르고(실패해도 04-11이 로그만 남기고 계속 — C-09) → `year` 해석(없으면 `kstYear(now())`, `all`이면 없음) → `resolveListRange` → (R이 비면 쿼리 없이 빈 결과) → **리포지토리 `aggregateProjects`를 직접** → 합계 조립. 도메인 `listProjects`·`aggregateProjects`(각자 판정을 부르는 04-11 입구)를 거치지 않는다 — 한 요청에서 판정이 여러 번 돌지 않는다(A-07·C-23). **목록 읽기 실패의 운영 로그(C-09에 접힌 §8 관측성 NOTE):** 집계·목록 쿼리가 던지면 `loadProjectList`가 `log.error("project.list_failed", { code: 오류의 PG 코드 })` 한 줄(필터 값·금액 없음)을 남기고 **그대로 다시 던진다** — 화면은 기존 `error.tsx`가 맡고, 지금은 Next 기본 stderr만 남던 실패에 구조화된 이벤트가 생긴다(판정 실패는 04-11의 `project.auto_settle_failed`, 이름은 점 표기로 통일). 합계 DTO `ProjectListTotals`: `title`·`count`(귀속 `in` 건수)와 금액 — 견적·실행가는 `quote.amount`, 매출은 `revenue.issued_amount`, 수익금·수익률은 **두 항목을 모두** 볼 수 있을 때만 키를 싣는다(필드 부재, 빈 값 아님) · `exclusionText`. **`ProjectListTotals`를 `registerDto`로 등록한다**(C-14 · D-38 — 누수 스캔이 합계 키를 본다). 합계 수익률은 `Σ수익금 ÷ Σ기준`(Σ기준 ≤ 0이면 null). 목록 행 DTO에는 `attributionLabel`을 `groupLabel`처럼 파생해 붙인다.

④ **화면.** `app/(app)/projects/list-totals.tsx`(서버 컴포넌트): 필터 줄 바로 아래 한 줄 — 제목 `합계 (…)`(라벨 모양) + `<dl>` 쌍(`dt` 라벨 `--fs-sm --muted` 600 · `dd` 금액 `--fs-lg` 700 `tabular-nums`, `formatKrw`·`formatPercent`) + 오른쪽 끝(폰은 아래 줄) 제외 문구 `--fs-sm --muted`, 아래 `2px --line-strong`. 카드·타일·배경 면 없음. 받은 키만 쌍으로 그린다. `projects-table.tsx`에서 표 아래 합계 줄(`summaryRow`)과 그 조립을 지운다(「더 보기」 링크 제거는 Task 2). 기간 열 셀에 `secondaryLine`으로 `attributionLabel`(`--fs-sm --muted`, `nowrap`)을 둔다. `page.tsx`는 `loadProjectList`를 부르고, 연도 선택지 맨 위에 `전체 연도`(값 `all`)를 두며 기본 선택을 올해로 한다(연도 목록 기준도 `kstYear`). `projects.module.css`에 합계 줄 규칙(토큰만 — D-20).

⑤ **E2E(트레이서 증명, `projects-list.spec.ts` 개정).** 올해·내년은 테스트 프로세스의 UTC가 아니라 `kstYear(new Date())`(`lib/kst-date`)로 계산한다(C-17 — 12월 31일 15:00~24:00 UTC에 깨지지 않게). 다른 워커의 데이터와 섞이지 않게 고유 검색어(`q=`)로 격리한 프로젝트 셋을 준비한다(올해 안 · 올해~내년 걸침 · 기간 미정 수주중 — 날짜는 `kstToday` 기준으로 만든다) → 조건 없이 `/projects?q=…` → 연도 선택 값이 올해, 세 행이 보이고, 합계 줄이 **표보다 위**에 있으며 괄호 건수가 1(올해 안만), 제외 문구 `{내년} 귀속 1건 제외 · 기간 미정 1건 제외`, 걸침 행 기간 칸 2행 `{내년} 귀속`, 걸침 행이 종료월 그룹 한 곳에만 한 번 있다(D-89·D-51) → 연도를 `전체 연도`로 바꾸면 괄호가 `전체 연도 · 2건`이고 제외 문구가 `기간 미정 1건 제외`만 남는다. 합계 금액이 쉼표 서식이다(C-01 — 문자열 합계가 사라졌다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 범위·귀속·제외·제목 규칙이 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts test/integration/quote-line-kinds.test.ts test/integration/leak-scan.test.ts test/integration/project-auto-settlement.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 겹침·귀속 합계·이중 계산 금지·21억 초과·입금 제외·보관 제외·합계 필드 노출 중 하나가 틀렸거나, 조정 줄 불변식(04-13)·누수 스캔·04-11 판정이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 올해 기본 보기와 표 위 합계 줄이 끝까지 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `repositories/projects.ts`의 돈 집계에 `::int` 캐스트가 없고(`grep -n "sum(.*)::int" repositories/projects.ts` 0건) 집계 식이 `mapWith(Number)`를 쓴다(C-01)
    - 통합 C-01 케이스가 합계 3,000,000,000과 모든 필드 `typeof === "number"`를 단언한다
    - `projectFilterConditions`를 부르는 함수가 `listProjectsPage`·`aggregateProjects` 둘이고, 집계가 별도의 행 필터를 만들지 않는다(귀속은 GROUP BY 구간 식)
    - `projectFilterConditions`에 `includeArchived` 분기가 없고 시스템 관리자 목록·합계에서 보관 프로젝트가 빠진다(D18 통합)
    - 통합 테스트에 「걸친 프로젝트가 두 해 목록에 나오고 합계는 종료 해 한 곳」 케이스와 「두 해 합계의 합 = 전체 연도 합계(기간 미정 제외)」 단언이 있다(금지 항목)
    - D-89 전부가 단언으로 덮인다 — 조건 없는 URL = 올해 보기(E2E) · 연도 보기의 겹침 조건(통합) · 걸친 프로젝트가 겹치는 해마다 보임(통합) · 걸친 행이 각 보기에서 종료월 그룹 한 곳에만 한 번(D-51) · 기간 필터 겹침은 Task 3 · 걸친 행 기간 칸 서식은 Task 3 단위
    - 통합 테스트에 등록일 KST 경계 케이스가 있다 — `created_at`이 KST 2027-01-01 00:30(UTC 전날)인 종료일 없는 미수주가 2027 보기에 나오고 2026 보기에 나오지 않는다
    - 통합 (a)의 「집계 SQL 왕복 한 번」 단언이 귀속 구간이 생긴 뒤에도 그대로 초록이다
    - 입금 줄 픽스처(C-14) · 키 집합 넷(없음 포함) · 음수 기준 수익률 null(C-16) 케이스가 있다
    - `ProjectListTotals`가 `registerDto`로 등록되고 누수 스캔이 초록이다
    - `loadProjectList`가 쿼리 실패 때 `project.list_failed` 오류 로그를 한 줄 남기고 같은 오류를 다시 던진다(Grep — 삼키지 않는다)
    - `list-totals.tsx`가 `<dl>`을 쓰고 `projects-table.tsx`에 표 아래 합계 줄 렌더가 없다
    - E2E가 연도를 `kstYear`로 계산한다 — `grep -n "getFullYear\|getUTCFullYear" test/e2e/projects-list.spec.ts` 0건(C-17)
    - 이 플랜이 `db/migrations/`에 파일을 더하지 않는다 — 연도 합계를 저장 컬럼으로 만들지 않는다(D-92)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>조건 없이 연 목록이 올해 보기이고, 합계가 표 위에서 귀속 연도 행만 21억을 넘어도 정확히 더하며 제외 건수를 말한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 번호 페이지 50건 — 04-29 `Pagination` 배선 + OFFSET + 범위 밖 번호 보정 + 그룹 머리글 반복 + 요청 순서</name>
  <files>repositories/projects.ts, domain/projects/index.ts, domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `ui/pagination/Pagination.tsx`·`ui/pagination/page-window.ts`(04-29 산출물 전문 — `href` 함수 갈래 · `pageRangeText`)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 874–895행(§7-16 페이지 줄 — 모양·현재 번호·렌더하지 않는 이전/다음·8쪽 이상 생략·요소·ARIA·폰)
    - `ui/table/Table.tsx` 61–100행(`groupRows` — 페이지의 첫 행 그룹 머리글이 자연히 렌더되는지 확인)
    - `domain/projects/index.ts` 140–185행(`PROJECT_LIST_DEFAULT_LIMIT`·`PROJECT_LIST_MAX_LIMIT`·`listProjects`)
    - `app/(app)/projects/page.tsx`(Task 1 이후 — `count` 파라미터·`loadMoreHref` 조립)
  </read_first>
  <behavior>
    - `clampPage("99", 3)` → 3 · `clampPage("abc", 3)` → 1 · `clampPage("0", 3)` → 1 · `clampPage(undefined, 0)` → 1
    - (C-01) `pageCountFrom({ buckets: [{ count: 30 }, { count: 25 }, { count: 1 }], pageSize: 50 })` → 2 — 구간 건수가 숫자로 더해진다(문자열 이어 붙이기 `"30251"`이 아니다)
    - 통합: 125행 보기에서 `page=3` → 101~125번째 25행, `page=99` → 마지막 페이지와 같은 행, 페이지 1~3의 행을 이어 붙이면 전체와 순서까지 같다(겹침·누락 없음)
  </behavior>
  <action>
① **서버 페이지.** `repositories/projects.ts` `listProjectsPage`가 `limit` 대신 `{ offset, limit }`을 받아 `.offset()`을 쓴다(정렬 끝에 `projects.id`가 이미 있어 페이지 간 순서가 안정적이다). `list-view.ts`에 `clampPage`와 `pageCountFrom`(`behavior` 앞 둘 — 단위 먼저)을 더한다. `domain/projects/index.ts`: 04-05의 목록 개수 상수 둘(기본 개수 · 「더 보기」 누적 상한)을 `PROJECT_LIST_PAGE_SIZE = 50` 하나로 바꾸고, `loadProjectList`의 순서를 **판정 한 번 → 집계 → `pageCountFrom`(보이는 행 = 모든 구간 합) → `clampPage` → 목록(리포지토리 `listProjectsPage` 직접)**으로 고정한다(C-23 — 목록 행이 필요 없는 0건이면 `emptyKind` 판정용 건수 조회만). 결과에 `page`·`pageCount`·`total`. 옛 `count` 파라미터는 읽지 않는다.

② **화면.** `projects-table.tsx`에서 「더 보기 50건」 링크와 `loadMoreHref` prop을 지운다. `page.tsx`가 표 아래에 04-29의 `Pagination`(label `프로젝트`, `href` 함수 = 지금 필터·정렬 파라미터를 보존하고 `page`만 바꾼 `/projects?…`, `rangeText` = `pageRangeText(…, "건")`)을 렌더한다. 필터 폼은 `page`를 싣지 않으므로 필터를 바꾸면 1쪽이다. 그룹 머리글 반복은 `Table`의 `groupBy`가 페이지의 첫 행 그룹을 그리는 것으로 성립한다 — 표에 추가 코드를 넣지 않고 E2E로 확인한다.

③ **테스트.** 통합(`behavior` 셋째 줄) · E2E: 같은 검색어로 격리한 51건(한 달에 몰아 같은 종료월 그룹) → 페이지 줄 `1–50 / 51건` · 「이전」 없음 · `2`를 누르면 URL `page=2`, 한 행, 그 행 위에 같은 종료월 그룹 머리글이 다시 있고 「다음」 없음 → 상태 필터를 바꾸면 URL에 `page`가 없다 → `page=99`로 직접 열면 마지막 페이지다. 옛 「더 보기」 케이스를 지운다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts test/unit/ui/pagination.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 페이지 보정·구간 건수 합이 계약과 다르거나 04-29 컴포넌트 계약이 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — OFFSET 페이지가 겹치거나 빠지거나, 범위 밖 번호가 마지막 페이지가 아니다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `PROJECT_LIST_MAX_LIMIT` 참조가 `app/`·`domain/`에 없고 `page.tsx`가 `params.count`를 읽지 않는다(Grep 0)
    - `listProjectsPage`가 `.offset(`을 쓴다
    - `loadProjectList` 본문이 판정 → 집계 → 보정 → 목록 순이고 도메인 `listProjects`·`aggregateProjects`를 부르지 않는다
    - 통합 테스트에 「페이지 이어 붙이기 = 전체」 단언과 `page=99` 보정 단언이 있다 · 단위에 구간 건수 숫자 합 케이스가 있다(C-01)
    - E2E에 2쪽 맨 위 그룹 머리글 반복 단언이 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>목록이 50건씩 번호 페이지로 나뉘고, 범위 밖 번호는 마지막 페이지이며, 페이지를 넘는 그룹 머리글이 다시 보이고, 한 요청의 읽기 순서가 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 기간 필터(겹침) · 서버 판정 오류 · 틀린 파라미터 정규화 · 정렬 유지 · 필터 지우기 · 빈 목록 세 갈래 · 기간 칸 서식 · 로딩 뼈대 + 화면 검증</name>
  <files>domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, domain/projects/index.ts, app/(app)/projects/filter-bar.tsx, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, app/(app)/projects/projects.module.css, app/(app)/projects/loading.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `app/(app)/projects/filter-bar.tsx`(전문 112행 — 라벨-위 배치 · `requestSubmit` · 「필터 지우기」 조건 · 폼이 싣는 값)
    - `app/(app)/projects/loading.tsx`(전문 49행 — 지금의 표 아래 합계 행 뼈대)
    - `app/(app)/projects/page.tsx` 60–75행(`teamId`가 그대로 uuid 비교로 가는 자리 — C-08)
    - `app/(app)/projects/error.tsx`(전문 — 오류 문구가 Copywriting과 같은지만 확인, 고칠 것이 없으면 건드리지 않는다)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 944–975행(필터 한 줄 · 보기 범위 R · 필터 지우기) · 1018–1021행(기간 칸 서식)
    - `docs/design/SYSTEM.md` §7-2 오류 한 줄 규칙(Grep `7-2`로 위치를 찾아 그 절만)
  </read_first>
  <behavior>
    - `parseListPeriod("2026-09-01", "2026-10-31")` → 범위 둘, 오류 없음 · `("2026-9-1", "")` → 오류 `from: 날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요`, 범위 없음 · `("2026-02-30", "")` → 같은 형식 오류(달력에 없는 날) · `("0000-01-01", "")` → 같은 형식 오류(2000–2100 밖, C-08) · `("2026-10-31", "2026-09-01")` → 오류 `to: 기간이 거꾸로입니다 · 앞 날짜를 먼저 적어 주세요`, 범위 없음 · 둘 다 빈 값 → 범위·오류 없음
    - `normalizeListParams({ teamId: "abc", year: "0000", status: ["in_progress", "settling"], page: "2" }, { teamIds })` → `{ teamId: undefined, year: 올해, status: "in_progress", page: "2" }` · 목록에 없는 uuid `teamId` → undefined · `year: "all"` → `"all"` · `year: "2031"` → 2031(C-08)
    - `formatListPeriod("2026-09-12", "2026-09-16", 2026)` → `09-12 ~ 09-16` · `("2025-03-01", "2025-03-05", 2026)` → `2025-03-01 ~ 03-05` · `("2025-03-01", "2025-03-05", null)` → `2025-03-01 ~ 03-05` · `("2026-11-10", "2027-02-05", 2026)` → `2026-11 ~ 2027-02` · `("2026-09-12", null, 2026)` → `09-12 ~ —` · `(null, null, 2026)` → `—`
    - 빈 갈래 판정: 범위 안 0건 + 사용자 필터 없음 + 볼 수 있는 프로젝트 0 → `none` · 사용자 필터 없음 + 다른 해에 프로젝트 있음 → `default-view`(문구의 연도 = 보기 연도) · 사용자 필터 있음 → `filtered`
    - 통합: 연도 2026 + 기간 2026-09-01~2026-10-31이면 R = 그 기간이고, 2026-10-20~2026-11-15 행은 보이되 `2026-11 귀속`으로 합계에서 빠져 `기간 밖 귀속 1건 제외`
    - 통합(C-08): 틀린 파라미터 표(`teamId=abc` · `teamId=`다른 뷰어 팀 밖 uuid · `year=0000` · `year=99999` · `page=-1` · `status` 두 개)마다 `loadProjectList`가 던지지 않고 기본 보기와 같은 결과다
  </behavior>
  <action>
① **순수 함수(`list-view.ts`).** `parseListPeriod(from, to)`(형식 `YYYY-MM-DD` + 달력에 있는 날짜 + 2000–2100, 거꾸로 판정 — 오류가 하나라도 있으면 기간 범위 전체를 적용하지 않는다) · `normalizeListParams(raw, { teamIds, thisYear })`(C-08 — 배열이면 첫 값, `teamId`는 uuid 모양 + `teamIds`에 있을 때만, 연도는 `all` 또는 2000–2100 정수 아니면 올해) · `formatListPeriod(start, end, viewYear | null)`(식별자형 날짜라 쉼표 없음) · 빈 갈래 판정 `emptyKind`. 단위 테스트를 먼저 쓴다.

② **도메인.** `loadProjectList`가 맨 앞에서 `normalizeListParams`로 URL 값을 정규화한다(뷰어가 고를 수 있는 팀 목록은 이미 필터 줄이 쓰는 조회를 재사용) — 정규화 밖 값은 SQL에 닿지 않는다. `from`·`to`는 `parseListPeriod`로 판정해 `periodErrors`를 결과에 싣고(서버가 판정 — UX-04), 오류가 없을 때만 `resolveListRange`의 기간으로 넘긴다. 결과가 0건이면 `emptyKind`를 싣는다 — `none` 판정에만 쓰는 필터 없는 건수 조회 한 번은 0건일 때만 한다. 사용자 필터 판정: 상태·팀·검색어·기간 값(형식 오류여도 값이 있으면 필터) · `year=all` · 올해가 아닌 연도. 올해 연도 값은 필터로 세지 않는다. `page.tsx`는 `searchParams`를 `string | string[]`로 받아 그대로 도메인에 넘긴다(C-08 — 페이지가 `string`으로 단정하지 않는다).

③ **필터 줄(`filter-bar.tsx`).** 연도 뒤에 `기간` 라벨 하나 아래 날짜 텍스트 입력 둘(`name` `from`·`to`, 자리표시자 `2026-09-18`, 사이 `~`, 폭 `10ch` + 좌우 `--cell-pad-x` — 새 폭 값 없음, SYSTEM.md 개정 ③)을 둔다. blur에서 값이 처음과 달라졌을 때만 `requestSubmit`, Enter는 기본 GET 제출. 서버 오류가 있으면 해당 칸에 `aria-invalid`와 `aria-describedby`, 칸 아래 한 줄 `--fs-sm --danger`(거꾸로 오류는 `to` 칸 아래). 입력 값은 오류여도 그대로 남긴다. **정렬 유지(C-24):** 지금 `sort`·`dir`가 있으면 폼에 `type="hidden"` 입력 둘로 싣는다(`page`는 싣지 않는다 — 1쪽). 「필터 지우기」(`/projects`)는 도메인의 사용자 필터 판정이 참일 때만.

④ **화면.** `page.tsx`: 빈 갈래 셋 — `none`은 `ListEmpty` `등록된 프로젝트가 없습니다` + `프로젝트 등록`(`?new=1#project-form`), `default-view`는 `{연도}년에 걸친 프로젝트가 없습니다` + `전체 연도 보기`(`/projects?year=all`), `filtered`는 `조건에 맞는 프로젝트가 없습니다` + `필터 지우기`(`/projects`). 셋 다 합계 줄·페이지 줄 없음. 필터 줄의 1차 「프로젝트 등록」 생략 조건을 `none` 갈래로 바꾼다(`?new=1`일 때 생략하는 기존 규칙은 그대로). `projects-table.tsx`의 기간 셀을 `formatListPeriod`(보기 연도 전달)로 바꾸고 `nowrap`. `loading.tsx`: 표 아래 합계 행 뼈대를 지우고 필터 줄 아래 합계 줄 자리에 라벨 `합계`만(금액 막대 없음), 머리글 + `--surface` 행 3개, 반짝임 없음, 기존 지연 표시 규칙 유지. 상단 진행 막대를 만들지 않는다(사용자 D17).

⑤ **E2E 추가.** 격리 검색어로 — 기간 `2026-9-1` 입력 후 blur → 칸 아래 형식 오류 한 줄, 목록은 기간 필터 없는 결과 그대로 · 거꾸로 입력 → 거꾸로 오류 · 올바른 기간 → 괄호가 `{from} ~ {to} 귀속` · 머리글 정렬을 바꾼 뒤 상태 필터를 바꾸면 URL에 `sort`가 남고 `page`가 없다(C-24) · `/projects?teamId=abc&year=0000`을 직접 열면 오류 화면이 아니라 올해 보기다(C-08) · 「필터 지우기」 → `/projects`이고 연도 값이 올해 · 기본 보기 0건 문구와 「전체 연도 보기」 이동(올해에 걸친 행이 없는 격리 검색어로). 연도는 `kstYear(new Date())`.

⑥ **화면 검증(CLAUDE.md 순서).** 싼 게이트(lint·typecheck·build) → **독립 DOM 감사**(실행자가 아닌 별도 서브에이전트, `model` 명시, `CI=true` 프로덕션 빌드에서 DOM 실측, 스크린샷 육안 판정 금지): 1280·375 두 폭에서 (a) 합계 줄이 표 위이고 아래 선이 `--line-strong` 2px (b) 375에서 합계 줄 쌍이 쌍 단위로만 줄바꿈되고 문서 가로 스크롤 0 (c) 페이지 줄 번호의 폰 크기 ≥ 44×44, 현재 번호에 `aria-current`, 1쪽에 「이전」 없음 (d) 기간 칸·`2027 귀속`이 한 줄(nowrap) (e) 합계 금액에 쉼표, 연도·날짜에 쉼표 없음 (f) 125건 보기 첫 응답 시간(서버 응답 헤더 기준, 참고값 — C-23) → 발견 수정 → 전체 게이트 **한 번** `CI=true pnpm test`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 판정·파라미터 정규화·기간 칸 서식·빈 갈래 판정이 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 보기의 겹침·귀속 제외가 틀렸거나 틀린 파라미터가 던졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>종료 코드가 0이 아니거나 단위·통합·E2E 어느 계층이든 failed 개수가 1 이상이다</fails_when>
    <human-check>독립 DOM 감사 보고서(1280·375 두 폭, (a)~(f) 항목별 실측값)가 SUMMARY에 붙어 있다</human-check>
  </verify>
  <acceptance_criteria>
    - `parseListPeriod`가 달력에 없는 날(`2026-02-30`)과 범위 밖 연도(`0000-01-01`)를 형식 오류로 잡는 단위 케이스가 있다
    - 틀린 파라미터 표 통합 케이스가 전부 기본 보기 결과와 같고 던지지 않는다(C-08)
    - 기간 오류가 있으면 목록 결과가 기간 필터 없는 결과와 같다는 통합 또는 E2E 단언이 있다
    - 필터 폼이 `sort`·`dir` 숨은 값을 싣고 `page`를 싣지 않는다(C-24 E2E)
    - 빈 갈래 셋이 각자의 문구·링크로 렌더되고 셋 다 합계 줄·페이지 줄이 없다
    - 1차 「프로젝트 등록」이 `default-view`·`filtered` 갈래에서 렌더된다
    - `loading.tsx`에 표 아래 합계 행 뼈대가 없고 합계 줄 자리에 라벨만 있으며, 진행 막대 요소가 없다(D17)
    - 독립 DOM 감사 보고서가 SUMMARY에 있고 `CI=true pnpm test`가 초록이다
  </acceptance_criteria>
  <done>기간 필터가 겹침으로 동작하고 틀린 입력·파라미터는 서버가 판정해 오류 화면 없이 처리하며, 필터를 바꿔도 정렬이 남고, 빈 목록이 세 갈래로 다음 한 수를 준다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 URL → 목록 서버 페이지 | `year`·`from`·`to`·`page`·`sort`·`dir`·`status`·`teamId`·`q` 전부 신뢰하지 않는 입력(배열로도 온다) |
| domain 출구(목록 행·합계 DTO) → 화면 | 행 단위 금액이 리포지토리 행에 생겼다 — 투영 명세 밖이면 나가지 않아야 한다 |
| 집계 쿼리 ↔ 목록 쿼리 | 두 쿼리가 다른 행 필터를 쓰면 합계가 가려진 행을 드러낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-88 | Information Disclosure | 합계 줄 | high | mitigate | 집계가 목록과 같은 `projectFilterConditions`(보관·범위·상태·팀·검색·scope) 위에 귀속 구간만 얹는다. 수익금·수익률은 `quote.amount`와 `revenue.issued_amount`를 **둘 다** 볼 때만 키를 싣는다(발행 역산 차단). 계급별 합계 키 집합 넷(둘 다 없음 포함)을 통합 테스트로 단언하고, `ProjectListTotals`를 DTO 레지스트리에 등록해 누수 스캔이 본다(C-14) |
| T-04-89 | Information Disclosure | 행 단위 금액(리포지토리 행) | medium | mitigate | 이 플랜은 새 행 필드를 DTO 명세에 넣지 않는다 — `project()`는 명세 밖 키를 결코 싣지 않는다. 04-18이 필드별 정보 항목으로 명세에 올리고 누수 스캔 레지스트리를 갱신한다 |
| T-04-90 | Tampering | URL 파라미터 | medium | mitigate | (CEO C-08 정정 — 지금은 `teamId`가 검증 없이 uuid 비교로 가서 22P02, 새 연도 범위는 `0000`에서 22008) `normalizeListParams`가 배열 첫 값·uuid 모양 + 팀 목록 소속·연도 2000–2100을 강제하고, `page`는 `clampPage`, 기간은 `parseListPeriod` 통과분만, 정렬은 허용 목록 — 전부 Drizzle 매개변수로 들어가고 SQL 문자열 연결이 없다. 틀린 파라미터 표 통합 테스트로 고정한다 |
| 운영 로그 | `project.list_failed`(오류 — 목록 쿼리 실패, PG 코드만 · 다시 던짐) |
| T-04-91 | Denial of Service | 한 요청의 행 수·합계 범위 | medium | mitigate | 옛 `count` 누적(최대 1000) 경로를 지우고 페이지 크기를 50으로 고정한다. 집계는 GROUP BY 구간 수(해 수 + 2) 행만 돌려준다. 돈 합계를 `bigint`로 더해 21억 초과 합계가 목록 전체를 오류 화면으로 만들지 않는다(C-01) |
| T-04-92 | Tampering | 자동 전환과 목록의 순서 | low | mitigate | (CEO C-09 서술 정정 — 집계와 목록은 여전히 두 문장이라 스냅숏을 공유하지 않는다) `loadProjectList`가 판정을 한 번 먼저 하고 집계 → 목록 순으로 읽어 자정 뒤 첫 화면의 어긋남 창을 줄인다. 판정 실패는 04-11의 fail-open으로 목록을 막지 않는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `project-list-view.test.ts` · 통합 `projects-list.test.ts`(+ `quote-line-kinds.test.ts` 불변식 · `leak-scan.test.ts`) 초록
- 독립 DOM 감사(1280·375) 보고서가 SUMMARY에 있다
- `CI=true pnpm test` 세 계층 통과
- `git diff --stat d6b41cf -- package.json pnpm-lock.yaml` 출력 없음
</verification>

<success_criteria>
- 조건 없는 목록이 올해 보기이고, 합계가 표 위에서 귀속 연도 행만 전액(21억 초과도 정확히) 더하며, 제외 건수를 말한다
- 해를 걸친 프로젝트가 두 해 목록에 보이지만 연간 합계에는 한 번만 들어간다
- 목록이 50건 번호 페이지이고 범위 밖 번호·그룹 반복·필터 변경 시 1쪽·정렬 유지 규칙이 지켜진다
- 기간 필터가 겹침으로 동작하고 틀린 입력·파라미터가 오류 화면을 만들지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/projects/list-view.ts` — `resolveListRange` · `attributionLabel` · `exclusionText` · `totalsTitle` · `formatListPeriod` · `parseListPeriod` · `clampPage` · `pageCountFrom` · `normalizeListParams` · 빈 갈래 판정 |
| 컴포넌트(신규, app) | `app/(app)/projects/list-totals.tsx` — 표 위 합계 줄 |
| 함수(신규, domain) | `loadProjectList(viewer, query, deps?)` — 목록 화면의 유일한 입구(판정 한 번 → 집계 → 보정 → 목록) |
| 타입(신규) | `ProjectListTotals`(DTO 레지스트리 등록) · 목록 결과(`rows` · `totals` · `page` · `pageCount` · `total` · `emptyKind` · `periodErrors`) |
| 상수(변경) | `PROJECT_LIST_DEFAULT_LIMIT` · `PROJECT_LIST_MAX_LIMIT` → `PROJECT_LIST_PAGE_SIZE = 50` |
| 리포지토리(변경) | 돈 집계 `::bigint` + `mapWith(Number)`(C-01) · `issuedSumsSubquery`(신규, 발행만) · 행 단위 기준·수익금·수익률 식(기준 ≤ 0 → null) · `ProjectListFilter.range`(겹침 + 기간 미정 규칙) · 보관 프로젝트 항상 제외(D18) · `listProjectsPage({ offset, limit })` · `aggregateProjects`(귀속 구간 GROUP BY) |
| 재사용(04-29) | `ui/pagination/Pagination.tsx`(링크 갈래) · `pageRangeText` |
| URL 파라미터 | `year`(없음 = 올해, `all`) · `from` · `to` · `page`(신규) · 필터 폼의 `sort`·`dir` 숨은 값 — `count` 폐기 |
| 테스트 | `test/unit/domain/project-list-view.test.ts`(신규) · `test/integration/projects-list.test.ts`(확장) · `test/e2e/projects-list.spec.ts`(개정) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | `ui/pagination` 컴포넌트·번호 창(04-29) · 상단 진행 막대(사용자 D17 — 이연) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| C-01 — 합계 `::int` 21억 초과 오류 · 문자열 합계 | CRITICAL | Plan 04-17, Task 1 ①(a) `::bigint` + `mapWith(Number)` · 통합 3,000,000,000 + `typeof` · Task 2 `pageCountFrom` 단위 |
| C-08 — 틀린 URL 파라미터가 500 | WARNING | Plan 04-17, Task 3 ①② `normalizeListParams` · 통합 파라미터 표 · E2E · T-04-90 정정 |
| C-09 / 사용자 D19 — 목록 읽기 판정 실패 격리 | WARNING | 04-11 fail-open + Plan 04-17, Task 1 ③ 판정 한 번 · T-04-92 서술 정정 |
| C-10 / 사용자 D17 — 진행 막대 서술 오류 | NOTE | Plan 04-17 `<probe_fallback>` 정정 · 만들지 않음(기록은 04-08) |
| C-11 / 사용자 D19 — 기간 미정 노출 기본값 | DECISION | 확정 반영(`<probe_fallback>`) |
| C-14 — 입금 줄 제외·키 집합·합계 DTO 등록 | WARNING | Plan 04-17, Task 1 ①(c)·③ · 통합 입금 픽스처·키 집합 넷 · `registerDto` |
| C-16 / 사용자 D19 — 기준 ≤ 0 수익률 | DECISION | Plan 04-17, Task 1 ①(d) `CASE WHEN 기준 > 0` · 합계도 Σ기준 ≤ 0 → null |
| C-17 — E2E 연도가 UTC | NOTE | Plan 04-17, Task 1 ⑤·Task 3 ⑤ `kstYear` · grep 0건 |
| C-21 / 사용자 D18 — 보관 프로젝트 항상 제외 | DECISION | Plan 04-17, Task 1 ①(f) · 통합 |
| §8 관측성(C-09에 접힘) — 목록 읽기 실패가 stderr만 | NOTE | Plan 04-17, Task 1 ③: `project.list_failed` 오류 로그 후 다시 던짐 |
| C-23 — 요청 순서·성능 | NOTE | Plan 04-17, Task 2 ① 순서 고정 · DOM 감사 (f) 참고값 |
| C-24 — 필터 바꾸면 정렬 사라짐 | NOTE | Plan 04-17, Task 3 ③ 숨은 `sort`·`dir` · E2E |
| C-26 — 04-17~04-19 중간 상태 | NOTE | `<probe_fallback>` 배포 묶음(Phase 4 한 PR) |
| C-27 — 번호 창 한 쪽 틈 | NOTE | 04-29가 구현(이 플랜은 재사용) |
| A-04 — 보관 줄 목록 합계 | CRITICAL | 04-12가 고치고 Plan 04-17 통합이 확인 |
| A-07 — 합계·목록 판정 순서 | WARNING | Plan 04-17, Task 1 ③·Task 2 ① |
| OV-5 — 읽기 판정 격리 | HIGH | 04-11 구현 + 목록 입구의 판정 한 번 |
| 파일 상한(옛 16파일) | WARNING | `ui/pagination`을 04-29로 떼어 12파일 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 목록 상단 2px 로딩 막대 | 사용자 D17 「만들지 않고 기록」 — 디자인 잔여 퀵 태스크로 이연(CEO 리뷰 범위 밖) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-17-SUMMARY.md` when done
</output>
