---
phase: 04-project-quote-ledger
plan: 17
type: execute
wave: 13
depends_on: ["04-16"]
files_modified:
  - repositories/projects.ts
  - domain/projects/index.ts
  - domain/projects/list-view.ts
  - test/unit/domain/project-list-view.test.ts
  - ui/pagination/Pagination.tsx
  - ui/pagination/Pagination.module.css
  - ui/pagination/page-window.ts
  - test/unit/ui/pagination.test.ts
  - app/(app)/projects/page.tsx
  - app/(app)/projects/filter-bar.tsx
  - app/(app)/projects/list-totals.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/projects.module.css
  - app/(app)/projects/loading.tsx
  - test/integration/projects-list.test.ts
  - test/e2e/projects-list.spec.ts
autonomous: true
requirements: [PROJ-01, UX-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/projects`를 조건 없이 열면 올해(KST) 보기다 — 연도 필터 값이 올해이고, 프로젝트 기간이 올해와 겹치는 행이 보인다. 해를 걸친 프로젝트는 겹치는 해마다 목록에 나온다(D-89)"
    - "연도 선택지 맨 위에 `전체 연도`가 있고 고르면 연도 범위가 없다(URL `year=all`) — 조건 없는 URL은 전체 연도가 아니라 올해다(D-89)"
    - "합계는 표 **위** 한 줄이다 — 필터 줄 바로 아래 `합계 (2026 귀속 · 48건)` + `매출 · 견적 · 실행가 · 수익금 · 수익률` 쌍, 아래 `2px --line-strong`. 표 아래 합계 행과 「더 보기 50건」은 없다(D-88, SYSTEM.md 개정 ③)"
    - "합계는 페이지와 무관하게 필터 전체 기준이고 집계 쿼리 한 번으로 나온다 — 목록 쿼리와 같은 행 필터 함수를 쓴다(D-88·D-91, T-04-28 유지)"
    - "합계에는 귀속 연도(종료일의 해)가 보기 범위 안인 행만 전액 들어간다. 범위 밖에서 끝나는 행은 보이되 합계에서 빠지고, 기간 칸 2행에 `2027 귀속`(기간 보기면 `2026-11 귀속`)이 붙으며, 합계 줄 오른쪽에 `2027 귀속 3건 제외`(기간 보기면 `기간 밖 귀속 N건 제외`)가 붙는다(D-90, Copywriting 표시 — 귀속·목록 합계 줄)"
    - "종료일이 없는 행은 맨 아래 `기간 미정` 그룹에 보이고 언제나 합계 밖이며 `기간 미정 N건 제외`로 적힌다. 0건인 제외 표시는 쓰지 않는다(D-90)"
    - "괄호의 건수는 합계에 든 행 수이고, 표에 보이는 전체 행 수는 페이지 줄의 `/ N건`이 말한다. 상태 필터가 있으면 `합계 (진행 · 2026 귀속 · 12건)`, 전체 연도면 `합계 (전체 연도 · 125건)`, 기간 필터면 `합계 (2026-09-01 ~ 2026-10-31 귀속 · 12건)`(Copywriting 표시 — 목록 합계 줄)"
    - "합계 수익률 = 합계 수익금 ÷ 행마다의 기준 금액(발행 줄이 있으면 발행 합계, 없으면 견적) 합, 분모 0이면 `—` — 전부 서버 계산이다(D-87·D-88)"
    - "합계 줄은 받은 금액만 쓴다 — 견적·실행가는 `quote.amount`, 매출은 `revenue.issued_amount`, 수익금·수익률은 두 항목을 모두 볼 수 있을 때만 서버가 보낸다(발행 기준 수익금 + 실행가 = 발행 합계 역산). 빠진 금액의 라벨을 두지 않는다(S1 partial ①)"
    - "목록은 50건씩 번호 페이지다 — 50건 이하면 페이지 줄이 없고 51건부터 표 아래 `51–100 / 125건  이전 · 1 2 3 · 다음`이 생긴다. URL은 `?page=N`이고 필터·정렬 파라미터를 보존하며 옛 `count` 누적 파라미터는 없어졌다(D-91, S12)"
    - "현재 번호는 `--fg` 700 글자이고 링크가 아니다(`aria-current=\"page\"`). 첫 페이지에 「이전」, 끝 페이지에 「다음」을 렌더하지 않는다. 8쪽 이상이면 `1 … 4 5 6 … 12`이고 `…`는 포커스를 받지 않는다(S12 populated·overflow)"
    - "범위 밖 페이지 번호(`?page=99`)는 오류 화면이 아니라 마지막 페이지로 보인다. 숫자가 아니거나 1보다 작으면 1쪽이다(S12 error)"
    - "종료월 그룹이 페이지를 넘으면 다음 페이지 맨 위에 같은 그룹 머리글이 다시 보인다(D-91, SYSTEM.md 개정 ③)"
    - "필터 줄에 `기간 [2026-01-01] ~ [2026-12-31]` 날짜 칸 둘이 있고(폭 `10ch` + 좌우 `--cell-pad-x`, 자리표시자 `2026-09-18`) blur·Enter에서 GET으로 다시 제출된다. 보기 범위 R = 연도 범위 ∩ 기간(있는 쪽만)이고 행은 프로젝트 기간이 R과 겹치면 보인다(D-89, S1 필터 한 줄)"
    - "기간 칸의 형식이 틀리거나 거꾸로면 칸 아래 한 줄 `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` / `기간이 거꾸로입니다 · 앞 날짜를 먼저 적어 주세요`가 보이고 기간 필터는 적용되지 않는다 — 판정은 서버다(UX-04, Copywriting Error — 목록 기간 필터)"
    - "필터·정렬을 바꾸면 1쪽으로 돌아간다. 「필터 지우기」는 기본 보기(올해·전체 상태·전체 팀)와 다른 값이 하나라도 있을 때만 보이고 누르면 기본 보기다 — 올해 연도 값은 필터로 세지 않는다(S1)"
    - "빈 목록은 세 갈래다 — 볼 수 있는 프로젝트가 하나도 없으면 `등록된 프로젝트가 없습니다 · 프로젝트 등록`, 기본 보기(올해)가 0건이면 `2026년에 걸친 프로젝트가 없습니다 · 전체 연도 보기`, 사용자 필터가 0건이면 `조건에 맞는 프로젝트가 없습니다 · 필터 지우기`. 셋 다 합계 줄과 페이지 줄을 그리지 않는다(S1 empty)"
    - "첫 번째 빈 갈래일 때만 필터 줄의 1차 「프로젝트 등록」을 생략한다(EMPTY가 같은 행동을 준다) — 나머지 두 갈래에서는 1차가 그대로 있다"
    - "로딩 뼈대는 합계 줄 자리에 라벨만(금액 자리 막대 없음) + 머리글 + `--surface` 행 3개이고 반짝임이 없다 — 표 아래 합계 행 뼈대가 없다(S1 loading, SYSTEM.md 개정 ③ §7-7)"
    - "기간 칸은 같은 해이고 그 해가 보기 연도면 `09-12 ~ 09-16`, 같은 해지만 보기 연도가 아니거나 없으면 `2025-03-01 ~ 03-05`, 해를 걸치면 `2026-11 ~ 2027-02`, 한쪽만 있으면 없는 쪽이 `—`다. `nowrap`이다(D-89, S1 열 — 기간)"
    - "목록 합계에서 보관된 견적 줄은 빠진다 — 상세 합계 행과 같은 줄 집합이다(04-12 보관 경로와의 정합)"
    - "연간 합계·귀속 구간은 읽기 시점 집계이고 저장 컬럼이 없다 — 날짜 원본(`start_date`·`end_date`·`revenue_entries.entry_date`)에서 매번 계산하므로, Phase 9·10이 연도별 손익·인센티브의 연도 기준을 다시 정해도 마이그레이션이 필요 없다(D-92의 이 페이즈 제약. 연도 기준 결정 자체는 이 페이즈 범위 밖)"
    - "합계 줄·페이지 줄의 숫자는 `lib/format-number.ts`로 쉼표가 붙고, 연도(`2026`)·`2027 귀속`·기간 칸 날짜에는 쉼표가 없다(D-95, S15)"
    - statement: "합계 줄은 폰(375)에서 `라벨 값` 쌍 단위로 줄바꿈하고 쌍 안은 꺾이지 않으며 제외 표시는 아래 줄로 간다 — 가로 스크롤 0. DOM 감사"
      verification: backstop
    - statement: "페이지 줄이 폰에서 번호 44×44이고 한 줄에 안 들어가면 범위 글자가 위 줄로 올라간다 — 가로 스크롤 0. DOM 감사"
      verification: backstop
    - statement: "N=0/1/50/51/125 렌더에서 0은 EMPTY, 1과 125가 같은 `합계 (… · N건)` 형식이고, 50 이하는 페이지 줄 없음, 51부터 있음, 제외 0건이면 제외 표시 없음 — 통합·E2E 렌더"
      verification: backstop
  prohibitions:
    - statement: "합계가 사용자가 볼 수 없는 행이나 필드를 더해서는 안 된다. 목록과 합계가 다른 행 필터를 쓰면 합계가 그 사람에게 가려진 프로젝트의 금액을 드러낸다 — 귀속 조건은 같은 행 필터 **위에** 얹는 추가 분류여야 하고, 수익금·수익률은 매출과 견적을 둘 다 볼 수 있는 사람에게만 가야 한다"
      status: resolved
      verification: test
    - statement: "같은 프로젝트가 두 해의 합계에 두 번 들어가서는 안 된다. 해를 걸친 프로젝트는 겹치는 해마다 목록에 보이지만 전액은 종료일의 해 한 곳에만 들어간다 — 해마다 나눠 넣거나 양쪽에 넣으면 연간 합계를 더한 값이 회사 전체 합계를 넘는다(D-90)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/list-view.ts"
      provides: "보기 범위 · 귀속 라벨 · 합계 제목 · 제외 문구 · 기간 칸 서식 · 기간 필터 판정 — 순수 함수"
      exports: ["resolveListRange", "attributionLabel", "totalsTitle", "exclusionText", "formatListPeriod", "parseListPeriod", "clampPage"]
    - path: "ui/pagination/Pagination.tsx"
      provides: "번호 페이지 줄 — 링크(href 함수)·버튼(onPageChange) 유니언, 오류 수 표시"
      exports: ["Pagination"]
    - path: "ui/pagination/page-window.ts"
      provides: "보이는 번호 창(첫·끝·현재 ±1, 사이 생략) · 범위 문구"
      exports: ["pageWindow", "pageRangeText"]
    - path: "app/(app)/projects/list-totals.tsx"
      provides: "표 위 합계 줄(dl) + 제외 표시"
      contains: "<dl"
    - path: "repositories/projects.ts"
      provides: "겹침 필터 · 기간 미정 노출 규칙 · 발행 합계 · 기준·수익금·수익률 · 귀속 구간별 집계 한 번 · OFFSET 페이지"
      contains: "offset"
    - path: "test/integration/projects-list.test.ts"
      provides: "겹침·귀속·제외·이중 계산 금지·페이지·범위 밖 페이지·보관 줄 제외·합계 필드 노출 증명"
  key_links:
    - from: "repositories/projects.ts"
      to: "repositories/projects.ts"
      via: "listProjectsPage와 aggregateProjects가 같은 projectFilterConditions를 부르고, 집계는 그 위에 귀속 구간 CASE만 얹는다"
      pattern: "projectFilterConditions"
    - from: "domain/projects/index.ts"
      to: "domain/projects/auto-transition.ts"
      via: "loadProjectList가 집계·목록보다 먼저 자동 전환을 한 번 적용해 두 쿼리가 같은 상태를 본다(04-11)"
      pattern: "applyAutoSettlement"
    - from: "app/(app)/projects/page.tsx"
      to: "ui/pagination/Pagination.tsx"
      via: "서버 페이지가 href 함수 갈래로 페이지 줄을 렌더한다(필터·정렬 보존)"
      pattern: "Pagination"
    - from: "domain/projects/index.ts"
      to: "lib/kst-date.ts"
      via: "loadProjectList의 기본 연도가 주입 가능한 now에서 KST로 나온다"
      pattern: "kstYear"
---

<objective>
**As a** 경영관리와 기획 PM, **I want to** 프로젝트 목록을 열면 올해 걸친 프로젝트와 그 합계가 표 위에 먼저 보이고, 해를 걸친 프로젝트는 겹치는 해마다 보이되 합계는 끝나는 해에만 들어가며, 125건을 번호 페이지로 넘기고 싶다, **so that** 인트라넷처럼 합계를 먼저 읽고, 연간 합계를 더해도 이중 계산이 없으며, 「더 보기」를 누적하다 위치를 잃지 않는다.

이 플랜은 04-05가 만든 목록(전체 연도 기본 · 종료일 연도 단일 매칭 · 표 아래 합계 · 「더 보기」 누적)을 D-88·D-89·D-90·D-91(목록 부분)로 고친다. 옛 D-51의 「더 보기 50건」과 전체 연도 기본은 **대체됐다**(D-51의 전체 상태 기본·종료월 그룹은 유지). 열 구성(클라이언트 열·매출·기준·수익금·수익률 D-87)·머리글 정렬·좁은 폭 열 접기·`종료일 지남`(D-81 목록)은 04-18, 견적 줄 30줄 페이지는 04-19다.

Purpose: 목록의 숫자가 **어느 범위의 무엇**인지가 이 플랜에서 정해진다. 04-18이 행 단위 금액 열을 올리려면 같은 SQL(발행 합계·기준·수익금)이 먼저 있어야 하고, 04-07(리저브 대장)은 여기서 만드는 `ui/pagination`을 쓴다.

Output: 목록 범위·귀속·합계·페이지의 순수 함수 모듈 · 페이지 컴포넌트 · 리포지토리 확장 · 목록 화면 개정 · 단위 2 · 통합 1(확장) · E2E 1(개정).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-05-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 413–423행(S1 상태 + ⚠ 기간 미정) · 515–523행(S12 상태) · 944–1037행(S1 적용 규칙 — 필터·합계 줄·그룹·기간 칸) · 874–895행(§7-16 페이지 줄 모양) · 790–822행(SYSTEM.md 개정 ③) · 318–320행·332행·341행·353–356행(Copywriting)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 788–801행(§6 D-87~D-91 — 겹침 조건 · 귀속 분리 · OFFSET Gap)
# .planning/phases/04-project-quote-ledger/04-PATTERNS.md 405행·421–422행·466–470행
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-01(목록 부분)**이다. 프로브는 04-05 시점 목록으로 돌았으므로 수용 기준은 D-88~D-91과 UI-SPEC S1·S12로 끌어왔다. S1·S12 `## UI Considerations`의 empty·loading·error·populated·partial·zero-one-many 중 이 플랜 범위(범위·합계·페이지)는 truths로, overflow(폰 합계 줄·페이지 줄)·zero-one-many는 backstop 마커로 옮겼다. S1 partial ②③⑥과 열 폭·좁은 PC 열 접기·폰 열 우선순위는 04-18 몫이다.

**⚠ S1 「기간 미정」 행이 어느 보기에 나오는가 — 계획 단계 가정(권장 기본값 채택, 사용자 확인 대상):** 종료일이 없는 행 중 **수주중은 보기 범위와 무관하게 항상** 보이고, 그 밖의 상태(미수주 등)는 **등록일(KST 날짜)이 보기 범위 R 안**일 때만 보인다. 전체 연도 + 기간 없음이면 전부 보인다. 근거: 문자 그대로(항상 보임)면 날짜 없는 미수주가 모든 해의 목록에 영원히 남는다(D-82 — 수주중→미수주는 날짜를 요구하지 않는다). 되돌리기: 조건 한 줄(reversible).

**계획 단계 가정(표면화):**
- **시작일만 없는 행**(종료일은 있음)은 겹침 판정에서 시작일 = 종료일로 본다. D-82 이후 진행·정산·완료는 시작일이 있으므로 이 경우는 수주중·미수주의 과거 데이터뿐이다.
- **R이 비는 조합**(예: 연도 2026 + 기간 2025-01-01~2025-03-01)은 쿼리를 보내지 않고 사용자 필터 0건 EMPTY다 — 빈 구간과의 겹침 식은 두 끝을 모두 넘는 행을 잘못 잡는다.
- **한쪽이 열린 기간 보기의 괄호 라벨**은 열린 쪽을 `—`로 쓴다(`2026-09-01 ~ — 귀속`) — 기간 칸의 「한쪽만 있으면 없는 쪽이 `—`」 규칙을 그대로 쓴다. 연도가 있으면 R의 두 끝이 항상 있으므로 전체 연도 + 한쪽 기간일 때만 생긴다.
- **연도 보기의 제외 문구 순서**는 해 오름차순 뒤 `기간 미정`(`2025 귀속 1건 제외 · 2027 귀속 3건 제외 · 기간 미정 5건 제외`).
- **상단 2px 진행 바는 이 플랜이 만들지 않는다.** S1·S12 loading은 GET 이동에 상단 진행 바를 말하지만, 그 바는 앱 어디에도 아직 없는 §5 모션 표의 공용 요소이고 **색 토큰이 SYSTEM.md에 정해져 있지 않다**(§1-3 포인트 다섯 곳 목록에도 없다) — 정보 부족이다. 이 화면의 로딩 표시는 `loading.tsx` 뼈대다. 사용자 확인 항목으로 반환한다(화면 하나만의 예외를 만들지 않기 위해 여기서 색을 고르지 않는다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 올해 기본 보기 · 기간 겹침 · 표 위 귀속 합계 줄(리포지토리 → 도메인 → 합계 줄 → E2E)</name>
  <files>repositories/projects.ts, domain/projects/list-view.ts, domain/projects/index.ts, test/unit/domain/project-list-view.test.ts, app/(app)/projects/page.tsx, app/(app)/projects/list-totals.tsx, app/(app)/projects/projects-table.tsx, app/(app)/projects/projects.module.css, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `repositories/projects.ts` 1–210행(`currentRevisionsSubquery`·`lineSumsSubquery`·`projectFilterConditions`·`resolveSortColumn`·`listProjectsPage`·`aggregateProjects`)
    - `domain/projects/index.ts` 100–215행(`PROJECT_LIST_DTO_SPEC`·`listProjects`·`aggregateProjects` — 04-11이 넣은 자동 전환 호출 위치를 Grep `applyAutoSettlement`로 확인)
    - `lib/kst-date.ts`(04-11 — `kstToday`·`kstYear`·`kstDateOf`) · `lib/format-number.ts`(04-09 — `formatKrw`·`formatPercent`·`formatCount`)
    - `db/schema/revenue-entries.ts`(전문 — `kind` `issue`/`payment`, `amountKrw`, `archivedAt`) · `db/schema/quote-lines.ts`의 `archivedAt`
    - `app/(app)/projects/page.tsx`(전문) · `app/(app)/projects/projects-table.tsx`(전문) · `app/(app)/projects/projects.module.css`(전문)
    - `test/integration/projects-list.test.ts`(전문 174행 — 픽스처 만드는 법, (a) 왕복 한 번 단언 방식) · `test/e2e/projects-list.spec.ts`(전문) · `test/e2e/fixtures.ts`(프로젝트·발행 줄 준비 도우미)
    - `domain/permissions/visible.ts`(정보 항목 판정 함수 이름)
  </read_first>
  <behavior>
    - `resolveListRange({ year: 2026 })` → `{ start: "2026-01-01", end: "2026-12-31", kind: "year", year: 2026 }` · `{ year: "all" }` → `null` · `{ year: 2026, period: { from: "2026-09-01" } }` → `{ start: "2026-09-01", end: "2026-12-31", kind: "period" }` · `{ year: 2026, period: { from: "2025-01-01", to: "2025-03-01" } }` → `{ empty: true }`
    - `attributionLabel({ endDate: "2027-02-10", range: 2026 연도 보기 })` → `2027 귀속` · 기간 보기 R=2026-09-01~2026-10-31, 종료 2026-11-20 → `2026-11 귀속` · 종료일이 R 안 → null · 종료일 없음 → null
    - `exclusionText({ byYear: { 2027: 3, 2025: 1 }, undetermined: 5, kind: "year" })` → `2025 귀속 1건 제외 · 2027 귀속 3건 제외 · 기간 미정 5건 제외` · 기간 보기 `{ outside: 2 }` → `기간 밖 귀속 2건 제외` · 전부 0 → null
    - `totalsTitle({ statusLabel: "진행", range: 2026 연도, count: 12 })` → `합계 (진행 · 2026 귀속 · 12건)` · 전체 연도 → `합계 (전체 연도 · 125건)` · 건수 1,250 → `1,250건`
    - 통합: 2026-11-10~2027-02-05 프로젝트는 2026 보기와 2027 보기 **둘 다**에 나오고, 합계에는 2027 보기에만 들어간다 — 두 해 합계의 합 = 전체 연도 합계(기간 미정 제외분 빼고)
    - 통합: 발행 줄이 있는 행은 기준 금액이 발행 합계(음수 줄 포함), 없는 행은 견적 — 합계 수익금 = Σ(기준 − 실행가), 합계 수익률 = 합계 수익금 ÷ Σ기준
    - 통합: 보관된 견적 줄과 보관된 발행 줄은 합계에서 빠진다
    - 통합: `quote.amount`만 볼 수 있는 계급의 합계에 매출·수익금·수익률 키가 없고, `revenue.issued_amount`만 볼 수 있는 계급의 합계에 견적·실행가·수익금·수익률 키가 없다
  </behavior>
  <action>
① **리포지토리(`repositories/projects.ts`).** (a) `lineSumsSubquery`에 보관 제외(`quote_lines.archived_at IS NULL`)를 더한다 — 지금은 보관 줄까지 더해 04-12 이후 상세 합계와 어긋난다. (b) 발행 합계 파생 표 `issuedSumsSubquery`를 더한다 — `revenue_entries`에서 `kind = issue` · 보관 제외 · 프로젝트별 `sum(amount_krw)`와 줄 수. (c) 행 단위 SQL 식 셋을 한 곳(함수 하나)에 정의한다 — 기준 금액(발행 줄 수 > 0이면 발행 합계, 아니면 견적 합계) · 수익금(기준 − 실행가) · 수익률(`수익금::float8 / nullif(기준, 0)`, 비율). 목록 행(`ProjectListRow`)이 이 값들과 `revenueKrw`(발행 줄 0이면 null)·`issuedCount`·`profitBasis`(`quote`/`issued`)를 싣는다 — **DTO 투영은 04-18이 한다**(이 플랜의 DTO 명세에 넣지 않으므로 응답으로 나가지 않는다). (d) `ProjectListFilter`의 `year`를 `range?: { start?: string; end?: string }`로 바꾸고 `projectFilterConditions`의 연도 조건을 **겹침**으로 바꾼다: 종료일이 있으면 `coalesce(시작일, 종료일) <= R.end`(있을 때) AND `종료일 >= R.start`(있을 때), 종료일이 없으면 `상태 = bidding` OR 등록일(KST 날짜)이 R 안(⚠ S1 가정, `<probe_fallback>`). 등록일의 KST 변환식은 `created_at` 컬럼 형(시간대 없음)과 04-11 SUMMARY가 기록한 세션 `TimeZone`을 확인하고 정한다 — 아래 KST 경계 통합 케이스가 그 식을 고정한다. (e) `aggregateProjects`는 같은 `projectFilterConditions` 위에 **귀속 구간** CASE(종료일 없음 → `undetermined` · 종료일이 R 안 또는 R 없음 → `in` · 그 밖 → 종료 연도 문자열)로 `GROUP BY`한 행들을 **한 번의 쿼리**로 돌려준다 — 구간마다 건수·매출·견적·실행가·수익금·기준 합. 필터 서술자를 목록과 따로 만들지 않는다(RESEARCH §6 D-90 — 불변식 유지, 추가 분류만 얹는다).

② **순수 함수(`domain/projects/list-view.ts` 신규, import는 `lib/`만).** `resolveListRange` · `attributionLabel` · `exclusionText` · `totalsTitle`(건수는 `formatCount`) — `behavior` 앞 네 줄. 단위 테스트를 먼저 쓴다(RED → GREEN).

③ **도메인(`domain/projects/index.ts`).** 화면의 유일한 입구 `loadProjectList(viewer, query, deps?: { now?: () => Date })`를 더한다: 04-11의 자동 전환을 **먼저 한 번** 적용하고(지금 `listProjects` 안에 있으면 이 함수 앞으로 옮겨 집계와 목록이 같은 상태를 본다) → `year` 해석(없으면 `kstYear(now())`, `all`이면 없음) → `resolveListRange` → (R이 비면 쿼리 없이 빈 결과) → 집계 → 합계 조립. 합계 DTO `ProjectListTotals`: `title`·`count`(귀속 `in` 건수)와 금액 — 견적·실행가는 `quote.amount`, 매출은 `revenue.issued_amount`, 수익금·수익률은 **두 항목을 모두** 볼 수 있을 때만 키를 싣는다(필드 부재, 빈 값 아님) · `exclusionText`. 합계 수익률은 `Σ수익금 ÷ Σ기준`(0이면 null). 목록 행 DTO에는 `attributionLabel`을 `groupLabel`처럼 파생해 붙인다. 기존 `aggregateProjects`·`listProjects`는 이 함수의 부품으로 남기되 `year` 인자를 범위로 바꾼다.

④ **화면.** `app/(app)/projects/list-totals.tsx`(서버 컴포넌트): 필터 줄 바로 아래 한 줄 — 제목 `합계 (…)`(라벨 모양) + `<dl>` 쌍(`dt` 라벨 `--fs-sm --muted` 600 · `dd` 금액 `--fs-lg` 700 `tabular-nums`, `formatKrw`·`formatPercent`) + 오른쪽 끝(폰은 아래 줄) 제외 문구 `--fs-sm --muted`, 아래 `2px --line-strong`. 카드·타일·배경 면 없음. 받은 키만 쌍으로 그린다. `projects-table.tsx`에서 표 아래 합계 줄(`summaryRow`)과 그 조립을 지운다(「더 보기」 링크 제거는 Task 2 — 이 태스크는 합계만 옮긴다). 기간 열 셀에 `secondaryLine`으로 `attributionLabel`(`--fs-sm --muted`, `nowrap`)을 둔다. `page.tsx`는 `loadProjectList`를 부르고, 연도 선택지 맨 위에 `전체 연도`(값 `all`)를 두며 기본 선택을 올해로 한다(연도 목록 기준도 `kstYear`). `projects.module.css`에 합계 줄 규칙(토큰만 — D-20).

⑤ **E2E(트레이서 증명, `projects-list.spec.ts` 개정).** 다른 워커의 데이터와 섞이지 않게 고유 검색어(`q=`)로 격리한 프로젝트 셋을 준비한다(올해 안 · 올해~내년 걸침 · 기간 미정 수주중) → 조건 없이 `/projects?q=…` → 연도 선택 값이 올해, 세 행이 보이고, 합계 줄이 **표보다 위**에 있으며 괄호 건수가 1(올해 안만), 제외 문구 `{내년} 귀속 1건 제외 · 기간 미정 1건 제외`, 걸침 행 기간 칸 2행 `{내년} 귀속` → 연도를 `전체 연도`로 바꾸면 괄호가 `전체 연도 · 2건`이고 제외 문구가 `기간 미정 1건 제외`만 남는다. 옛 「더 보기」 케이스는 Task 2가 페이지 케이스로 바꾼다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 범위·귀속·제외·제목 규칙이 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts test/integration/quote-line-kinds.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 겹침·귀속 합계·이중 계산 금지·보관 제외·합계 필드 노출 중 하나가 틀렸거나, 조정 줄 불변식(04-13)이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 올해 기본 보기와 표 위 합계 줄이 끝까지 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `projectFilterConditions`를 부르는 함수가 `listProjectsPage`·`aggregateProjects` 둘이고, 집계가 별도의 행 필터를 만들지 않는다(귀속은 GROUP BY 구간 식)
    - 통합 테스트에 「걸친 프로젝트가 두 해 목록에 나오고 합계는 종료 해 한 곳」 케이스와 「두 해 합계의 합 = 전체 연도 합계(기간 미정 제외)」 단언이 있다(금지 항목)
    - 통합 테스트에 등록일 KST 경계 케이스가 있다 — `created_at`이 KST 2027-01-01 00:30(UTC 전날)인 종료일 없는 미수주가 2027 보기에 나오고 2026 보기에 나오지 않는다
    - 통합 테스트 (a)의 「집계 SQL 왕복 한 번」 단언이 귀속 구간이 생긴 뒤에도 그대로 초록이다
    - 통합 테스트에 보관한 견적 줄·보관한 발행 줄이 합계에서 빠지는 케이스가 있다
    - 합계 DTO 키 집합 단언 둘(견적만 볼 수 있는 계급 · 발행만 볼 수 있는 계급)이 있다
    - `list-totals.tsx`가 `<dl>`을 쓰고 `projects-table.tsx`에 표 아래 합계 줄 렌더가 없다
    - 이 플랜이 `db/migrations/`에 파일을 더하지 않는다 — 연도 합계를 저장 컬럼으로 만들지 않는다(D-92)
    - URL에 연도가 없을 때 연도 선택 값이 `kstYear`로 계산된 올해다(E2E)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>조건 없이 연 목록이 올해 보기이고, 합계가 표 위에서 귀속 연도 행만 더하며 제외 건수를 말한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 번호 페이지 50건 — `ui/pagination` 신설 + OFFSET + 범위 밖 번호 보정 + 그룹 머리글 반복</name>
  <files>ui/pagination/Pagination.tsx, ui/pagination/Pagination.module.css, ui/pagination/page-window.ts, test/unit/ui/pagination.test.ts, repositories/projects.ts, domain/projects/index.ts, domain/projects/list-view.ts, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 874–895행(§7-16 페이지 줄 — 모양·현재 번호·렌더하지 않는 이전/다음·8쪽 이상 생략·요소·ARIA·폰)
    - `ui/list-empty/ListEmpty.tsx`(전문 — `href`/`onClick` 유니언과 3차 버튼 클래스 선례) · `ui/list-empty/ListEmpty.module.css`(3차 모양 규칙)
    - `ui/table/Table.tsx` 61–100행(`groupRows` — 페이지의 첫 행 그룹 머리글이 자연히 렌더되는지 확인)
    - `domain/projects/index.ts` 140–185행(`PROJECT_LIST_DEFAULT_LIMIT`·`PROJECT_LIST_MAX_LIMIT`·`listProjects`)
    - `app/(app)/projects/page.tsx`(Task 1 이후 — `count` 파라미터·`loadMoreHref` 조립)
  </read_first>
  <behavior>
    - `pageWindow(1, 1)` → `[1]` · `pageWindow(2, 3)` → `[1, 2, 3]` · `pageWindow(1, 7)` → `[1..7]` · `pageWindow(5, 12)` → `[1, "gap", 4, 5, 6, "gap", 12]` · `pageWindow(1, 12)` → `[1, 2, "gap", 12]` · `pageWindow(3, 12)` → `[1, 2, 3, 4, "gap", 12]` · `pageWindow(12, 12)` → `[1, "gap", 11, 12]`
    - `pageRangeText({ page: 2, pageSize: 50, total: 125, unit: "건" })` → `51–100 / 125건` · `{ page: 3, … }` → `101–125 / 125건` · `{ page: 2, pageSize: 30, total: 142, unit: "줄" }` → `31–60 / 142줄` · 총 1,250 → `/ 1,250건`
    - `clampPage("99", 3)` → 3 · `clampPage("abc", 3)` → 1 · `clampPage("0", 3)` → 1 · `clampPage(undefined, 0)` → 1
    - 통합: 125행 보기에서 `page=3` → 101~125번째 25행, `page=99` → 마지막 페이지와 같은 행, 페이지 1~3의 행을 이어 붙이면 전체와 순서까지 같다(겹침·누락 없음)
    - 렌더(단위): `pageCount` 1이면 아무것도 렌더하지 않는다 · 1쪽에 「이전」 없음 · 끝 쪽에 「다음」 없음 · 현재 번호가 링크·버튼이 아니고 `aria-current="page"` · `errorCounts: { 2: 2 }`이면 2쪽 번호 옆 `오류 2`와 접근 이름 `2쪽, 오류 2칸`
  </behavior>
  <action>
① **`ui/pagination/page-window.ts`**(순수, `lib/format-number`만 import): `pageWindow(page, pageCount)` — 7쪽 이하면 전부, 8쪽 이상이면 첫·끝·현재 ±1과 사이 `"gap"` · `pageRangeText({ page, pageSize, total, unit })` — 시작·끝·총수에 `formatCount`, 구분은 en dash. 단위 테스트(`behavior` 앞 둘)를 먼저 쓴다.

② **`ui/pagination/Pagination.tsx`**(훅 없는 표현 컴포넌트 — 서버·클라이언트 어디서나 렌더): props `label`(표 이름 — `<nav aria-label="{label} 페이지">`) · `page` · `pageCount` · `rangeText` · `errorCounts?`(쪽 → 오류 칸 수, 편집 표 전용) + 이동 유니언 `{ href: (page: number) => string }` | `{ onPageChange: (page: number) => void }`(ListEmpty 선례 — 이동이면 `<a>`, 화면 안 전환이면 `<button type="button">`, §10). `pageCount <= 1`이면 null. 모양은 §7-16 그대로: 왼쪽 범위 글자 `--fs-sm --muted`, 오른쪽 `이전 · 번호들 · 다음`(3차 = `--accent` 글자 + 밑줄, `--fs-sm` 600) · 현재 번호 `--fg` 700 밑줄 없는 `<span aria-current="page">` · `…`는 포커스 없는 글자 · 오류 수가 있는 번호 옆 `오류 N`(`--fs-xs --danger`)과 그 링크·버튼의 접근 이름 `N쪽, 오류 M칸`. 아이콘·`‹ ›` 글리프 없음. 폰(<700)에서 번호·이전·다음은 `--touch-min` 44×44, 넘치면 범위 글자가 위 줄로(`flex-wrap`). `Pagination.module.css`는 토큰만(D-20). 렌더 단위 테스트(`behavior` 마지막 줄)는 `test/unit/ui/pagination.test.ts`에 `react-dom/server`의 정적 렌더로 쓴다(새 의존성 없이 — 기존 단위 테스트가 컴포넌트를 렌더하는 방식을 Grep으로 찾아 따른다).

③ **서버 페이지.** `repositories/projects.ts` `listProjectsPage`가 `limit` 대신 `{ offset, limit }`을 받아 `.offset()`을 쓴다(정렬 끝에 `projects.id`가 이미 있어 페이지 간 순서가 안정적이다). `domain/projects/index.ts`: 04-05의 목록 개수 상수 둘(기본 개수 · 「더 보기」 누적 상한)을 `PROJECT_LIST_PAGE_SIZE = 50` 하나로 바꾸고, `loadProjectList`가 집계의 보이는 행 수(모든 구간 합)로 `pageCount`를 내고 `clampPage`(`list-view.ts`에 추가, `behavior` 셋째 줄)로 보정한 뒤 목록을 읽는다 — 결과에 `page`·`pageCount`·`total`. 옛 `count` 파라미터는 읽지 않는다.

④ **화면.** `projects-table.tsx`에서 「더 보기 50건」 링크와 `loadMoreHref` prop을 지운다. `page.tsx`가 표 아래에 `Pagination`(label `프로젝트`, `href` 함수 = 지금 필터·정렬 파라미터를 보존하고 `page`만 바꾼 `/projects?…`, `rangeText` = `pageRangeText(…, "건")`)을 렌더한다. 필터 폼은 `page`를 싣지 않으므로 필터를 바꾸면 1쪽이다. 그룹 머리글 반복은 `Table`의 `groupBy`가 페이지의 첫 행 그룹을 그리는 것으로 성립한다 — 표에 추가 코드를 넣지 않고 E2E로 확인한다.

⑤ **테스트.** 통합(`behavior` 넷째 줄) · E2E: 같은 검색어로 격리한 51건(한 달에 몰아 같은 종료월 그룹) → 페이지 줄 `1–50 / 51건` · 「이전」 없음 · `2`를 누르면 URL `page=2`, 한 행, 그 행 위에 같은 종료월 그룹 머리글이 다시 있고 「다음」 없음 → 상태 필터를 바꾸면 URL에 `page`가 없다 → `page=99`로 직접 열면 마지막 페이지다. 옛 「더 보기」 케이스를 지운다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/pagination.test.ts test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 번호 창·범위 문구·보정·렌더 규칙이 §7-16과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — OFFSET 페이지가 겹치거나 빠지거나, 범위 밖 번호가 마지막 페이지가 아니다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ui/pagination/Pagination.tsx`가 `ui`·`lib` 밖을 import하지 않는다(eslint boundaries 초록)
    - 이동 유니언이 `href` 함수 갈래와 `onPageChange` 갈래 둘이고, 링크 갈래는 `<a>`, 버튼 갈래는 `<button type="button">`을 렌더한다(단위 렌더 단언)
    - `PROJECT_LIST_MAX_LIMIT` 참조가 `app/`·`domain/`에 없고 `page.tsx`가 `params.count`를 읽지 않는다(Grep 0)
    - `listProjectsPage`가 `.offset(`을 쓴다
    - 통합 테스트에 「페이지 이어 붙이기 = 전체」 단언과 `page=99` 보정 단언이 있다
    - E2E에 2쪽 맨 위 그룹 머리글 반복 단언이 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>목록이 50건씩 번호 페이지로 나뉘고, 범위 밖 번호는 마지막 페이지이며, 페이지를 넘는 그룹 머리글이 다시 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 기간 필터(겹침) · 서버 판정 오류 · 필터 지우기 · 빈 목록 세 갈래 · 기간 칸 서식 · 로딩 뼈대 + 화면 검증</name>
  <files>domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, domain/projects/index.ts, app/(app)/projects/filter-bar.tsx, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, app/(app)/projects/projects.module.css, app/(app)/projects/loading.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `app/(app)/projects/filter-bar.tsx`(전문 112행 — 라벨-위 배치 · `requestSubmit` · 「필터 지우기」 조건)
    - `app/(app)/projects/loading.tsx`(전문 49행 — 지금의 표 아래 합계 행 뼈대)
    - `app/(app)/projects/error.tsx`(전문 — 오류 문구가 Copywriting과 같은지만 확인, 고칠 것이 없으면 건드리지 않는다)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 944–975행(필터 한 줄 · 보기 범위 R · 필터 지우기) · 1018–1021행(기간 칸 서식)
    - `docs/design/SYSTEM.md` §7-2 오류 한 줄 규칙(Grep `7-2`로 위치를 찾아 그 절만)
  </read_first>
  <behavior>
    - `parseListPeriod("2026-09-01", "2026-10-31")` → 범위 둘, 오류 없음 · `("2026-9-1", "")` → 오류 `from: 날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요`, 범위 없음 · `("2026-02-30", "")` → 같은 형식 오류(달력에 없는 날) · `("2026-10-31", "2026-09-01")` → 오류 `to: 기간이 거꾸로입니다 · 앞 날짜를 먼저 적어 주세요`, 범위 없음 · 둘 다 빈 값 → 범위·오류 없음
    - `formatListPeriod("2026-09-12", "2026-09-16", 2026)` → `09-12 ~ 09-16` · `("2025-03-01", "2025-03-05", 2026)` → `2025-03-01 ~ 03-05` · `("2025-03-01", "2025-03-05", null)` → `2025-03-01 ~ 03-05` · `("2026-11-10", "2027-02-05", 2026)` → `2026-11 ~ 2027-02` · `("2026-09-12", null, 2026)` → `09-12 ~ —` · `(null, null, 2026)` → `—`
    - 빈 갈래 판정: 범위 안 0건 + 사용자 필터 없음 + 볼 수 있는 프로젝트 0 → `none` · 사용자 필터 없음 + 다른 해에 프로젝트 있음 → `default-view`(문구의 연도 = 보기 연도) · 사용자 필터 있음 → `filtered`
    - 통합: 연도 2026 + 기간 2026-09-01~2026-10-31이면 R = 그 기간이고, 2026-10-20~2026-11-15 행은 보이되 `2026-11 귀속`으로 합계에서 빠져 `기간 밖 귀속 1건 제외`
  </behavior>
  <action>
① **순수 함수(`list-view.ts`).** `parseListPeriod(from, to)`(형식 `YYYY-MM-DD` + 달력에 있는 날짜, 거꾸로 판정 — 오류가 하나라도 있으면 기간 범위 전체를 적용하지 않는다) · `formatListPeriod(start, end, viewYear | null)`(`behavior` 둘째 줄 — 식별자형 날짜라 쉼표 없음) · 빈 갈래 판정 `emptyKind`. 단위 테스트를 먼저 쓴다.

② **도메인.** `loadProjectList`가 `from`·`to`를 `parseListPeriod`로 판정해 `periodErrors`를 결과에 싣고(서버가 판정 — UX-04), 오류가 없을 때만 `resolveListRange`의 기간으로 넘긴다. 결과가 0건이면 `emptyKind`를 싣는다 — `none` 판정에만 쓰는 필터 없는 건수 조회 한 번은 0건일 때만 한다. 사용자 필터 판정: 상태·팀·검색어·기간 값(형식 오류여도 값이 있으면 필터) · `year=all` · 올해가 아닌 연도. 올해 연도 값은 필터로 세지 않는다.

③ **필터 줄(`filter-bar.tsx`).** 연도 뒤에 `기간` 라벨 하나 아래 날짜 텍스트 입력 둘(`name` `from`·`to`, 자리표시자 `2026-09-18`, 사이 `~`, 폭 `10ch` + 좌우 `--cell-pad-x` — 새 폭 값 없음, SYSTEM.md 개정 ③)을 둔다. blur에서 값이 처음과 달라졌을 때만 `requestSubmit`, Enter는 기본 GET 제출. 서버 오류가 있으면 해당 칸에 `aria-invalid`와 `aria-describedby`, 칸 아래 한 줄 `--fs-sm --danger`(거꾸로 오류는 `to` 칸 아래). 입력 값은 오류여도 그대로 남긴다. 「필터 지우기」(`/projects`)는 도메인의 사용자 필터 판정이 참일 때만.

④ **화면.** `page.tsx`: 빈 갈래 셋 — `none`은 `ListEmpty` `등록된 프로젝트가 없습니다` + `프로젝트 등록`(`?new=1#project-form`), `default-view`는 `{연도}년에 걸친 프로젝트가 없습니다` + `전체 연도 보기`(`/projects?year=all`), `filtered`는 `조건에 맞는 프로젝트가 없습니다` + `필터 지우기`(`/projects`). 셋 다 합계 줄·페이지 줄 없음. 필터 줄의 1차 「프로젝트 등록」 생략 조건을 `none` 갈래로 바꾼다(`?new=1`일 때 생략하는 기존 규칙은 그대로). `projects-table.tsx`의 기간 셀을 `formatListPeriod`(보기 연도 전달)로 바꾸고 `nowrap`. `loading.tsx`: 표 아래 합계 행 뼈대를 지우고 필터 줄 아래 합계 줄 자리에 라벨 `합계`만(금액 막대 없음), 머리글 + `--surface` 행 3개, 반짝임 없음, 기존 지연 표시 규칙 유지.

⑤ **E2E 추가.** 격리 검색어로 — 기간 `2026-9-1` 입력 후 blur → 칸 아래 형식 오류 한 줄, 목록은 기간 필터 없는 결과 그대로 · 거꾸로 입력 → 거꾸로 오류 · 올바른 기간 → 괄호가 `{from} ~ {to} 귀속` · 「필터 지우기」 → `/projects`이고 연도 값이 올해 · 기본 보기 0건 문구와 「전체 연도 보기」 이동(올해에 걸친 행이 없는 격리 검색어로).

⑥ **화면 검증(CLAUDE.md 순서).** 싼 게이트(lint·typecheck·build) → **독립 DOM 감사**(실행자가 아닌 별도 서브에이전트, `model` 명시, `CI=true` 프로덕션 빌드에서 DOM 실측, 스크린샷 육안 판정 금지): 1280·375 두 폭에서 (a) 합계 줄이 표 위이고 아래 선이 `--line-strong` 2px (b) 375에서 합계 줄 쌍이 쌍 단위로만 줄바꿈되고 문서 가로 스크롤 0 (c) 페이지 줄 번호의 폰 크기 ≥ 44×44, 현재 번호에 `aria-current`, 1쪽에 「이전」 없음 (d) 기간 칸·`2027 귀속`이 한 줄(nowrap) (e) 합계 금액에 쉼표, 연도·날짜에 쉼표 없음 → 발견 수정 → 전체 게이트 **한 번** `CI=true pnpm test`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 판정·기간 칸 서식·빈 갈래 판정이 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 보기의 겹침·귀속 제외가 틀렸다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>종료 코드가 0이 아니거나 단위·통합·E2E 어느 계층이든 failed 개수가 1 이상이다</fails_when>
    <human-check>독립 DOM 감사 보고서(1280·375 두 폭, (a)~(e) 항목별 실측값)가 SUMMARY에 붙어 있다</human-check>
  </verify>
  <acceptance_criteria>
    - `parseListPeriod`가 달력에 없는 날(`2026-02-30`)을 형식 오류로 잡는 단위 케이스가 있다
    - 기간 오류가 있으면 목록 결과가 기간 필터 없는 결과와 같다는 통합 또는 E2E 단언이 있다
    - 빈 갈래 셋이 각자의 문구·링크로 렌더되고 셋 다 합계 줄·페이지 줄이 없다(E2E 또는 통합 + 렌더 단언)
    - 1차 「프로젝트 등록」이 `default-view`·`filtered` 갈래에서 렌더된다
    - `loading.tsx`에 표 아래 합계 행 뼈대가 없고 합계 줄 자리에 라벨만 있다
    - 독립 DOM 감사 보고서가 SUMMARY에 있고 `CI=true pnpm test`가 초록이다
  </acceptance_criteria>
  <done>기간 필터가 겹침으로 동작하고 틀린 입력은 서버가 칸 아래에서 말하며, 빈 목록이 세 갈래로 다음 한 수를 준다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 URL → 목록 서버 페이지 | `year`·`from`·`to`·`page`·`sort`·`status`·`teamId`·`q` 전부 신뢰하지 않는 입력 |
| domain 출구(목록 행·합계 DTO) → 화면 | 행 단위 금액이 리포지토리 행에 생겼다 — 투영 명세 밖이면 나가지 않아야 한다 |
| 집계 쿼리 ↔ 목록 쿼리 | 두 쿼리가 다른 행 필터를 쓰면 합계가 가려진 행을 드러낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-88 | Information Disclosure | 합계 줄 | high | mitigate | 집계가 목록과 같은 `projectFilterConditions`(보관·범위·상태·팀·검색·scope) 위에 귀속 구간만 얹는다. 수익금·수익률은 `quote.amount`와 `revenue.issued_amount`를 **둘 다** 볼 때만 키를 싣는다(발행 역산 차단). 계급별 합계 키 집합을 통합 테스트로 단언한다 |
| T-04-89 | Information Disclosure | 행 단위 금액(리포지토리 행) | medium | mitigate | 이 플랜은 새 행 필드를 DTO 명세에 넣지 않는다 — `project()`는 명세 밖 키를 결코 싣지 않는다. 04-18이 필드별 정보 항목으로 명세에 올리고 누수 스캔 레지스트리를 갱신한다 |
| T-04-90 | Tampering | URL 파라미터 | medium | mitigate | `page`는 `clampPage`로 1..pageCount 정수, 연도는 네 자리 또는 `all`, 기간은 `parseListPeriod` 통과분만, 정렬은 허용 목록 — 전부 Drizzle 매개변수로 들어가고 SQL 문자열 연결이 없다 |
| T-04-91 | Denial of Service | 한 요청의 행 수 | low | mitigate | 옛 `count` 누적(최대 1000) 경로를 지우고 페이지 크기를 50으로 고정한다. 집계는 GROUP BY 구간 수(해 수 + 2) 행만 돌려준다 |
| T-04-92 | Tampering | 자동 전환과 목록의 순서 | low | mitigate | `loadProjectList`가 집계·목록 전에 자동 전환을 한 번 적용한다 — 두 쿼리가 서로 다른 상태를 보고 건수와 행이 어긋나는 창을 없앤다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 2 파일 · 통합 `projects-list.test.ts`(+ `quote-line-kinds.test.ts` 불변식) 초록
- 독립 DOM 감사(1280·375) 보고서가 SUMMARY에 있다
- `CI=true pnpm test` 세 계층 통과
- `git diff --stat d6b41cf -- package.json pnpm-lock.yaml` 출력 없음
</verification>

<success_criteria>
- 조건 없는 목록이 올해 보기이고, 합계가 표 위에서 귀속 연도 행만 전액 더하며, 제외 건수를 말한다
- 해를 걸친 프로젝트가 두 해 목록에 보이지만 연간 합계에는 한 번만 들어간다
- 목록이 50건 번호 페이지이고 범위 밖 번호·그룹 반복·필터 변경 시 1쪽 규칙이 지켜진다
- 기간 필터가 겹침으로 동작하고 틀린 입력은 서버 판정으로 칸 아래에 보인다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/projects/list-view.ts` — `resolveListRange` · `attributionLabel` · `exclusionText` · `totalsTitle` · `formatListPeriod` · `parseListPeriod` · `clampPage` · 빈 갈래 판정 |
| 컴포넌트(신규, ui) | `ui/pagination/Pagination.tsx` + `Pagination.module.css` — `href` 함수 / `onPageChange` 유니언, `errorCounts` |
| 모듈(신규, ui) | `ui/pagination/page-window.ts` — `pageWindow` · `pageRangeText` |
| 컴포넌트(신규, app) | `app/(app)/projects/list-totals.tsx` — 표 위 합계 줄 |
| 함수(신규, domain) | `loadProjectList(viewer, query, deps?)` — 목록 화면의 유일한 입구 |
| 타입(신규) | `ProjectListTotals` · 목록 결과(`rows` · `totals` · `page` · `pageCount` · `total` · `emptyKind` · `periodErrors`) |
| 상수(변경) | `PROJECT_LIST_DEFAULT_LIMIT` · `PROJECT_LIST_MAX_LIMIT` → `PROJECT_LIST_PAGE_SIZE = 50` |
| 리포지토리(변경) | `lineSumsSubquery`(보관 제외) · `issuedSumsSubquery`(신규) · 행 단위 기준·수익금·수익률 식 · `ProjectListFilter.range`(겹침 + 기간 미정 규칙) · `listProjectsPage({ offset, limit })` · `aggregateProjects`(귀속 구간 GROUP BY) |
| URL 파라미터 | `year`(없음 = 올해, `all`) · `from` · `to` · `page`(신규) — `count` 폐기 |
| 테스트 | `test/unit/domain/project-list-view.test.ts`(신규) · `test/unit/ui/pagination.test.ts`(신규) · `test/integration/projects-list.test.ts`(확장) · `test/e2e/projects-list.spec.ts`(개정) |
| 사용자 확인 항목 | ⚠ S1 기간 미정 노출 기본값 · 상단 2px 진행 바 색 미정(이 플랜은 만들지 않음) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-17-SUMMARY.md` when done
</output>
