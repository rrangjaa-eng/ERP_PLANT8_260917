---
phase: 04-project-quote-ledger
plan: 20
type: execute
wave: 7
depends_on: ["04-27", "04-29"]
files_modified:
  - domain/permissions/menus.ts
  - domain/seed/index.ts
  - domain/rules/register.ts
  - domain/projects/status.ts
  - repositories/projects.ts
  - repositories/permissions.ts
  - repositories/action-log.ts
  - domain/action-log/record.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - test/unit/domain/project-status.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/project-status.test.ts
  - test/integration/lock-race.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사람이 하는 전환은 수주중→진행 · 수주중→미수주 · 미수주→진행 · 정산→완료 넷뿐이고, 목록 밖 요청(진행→수주중 · 진행→미수주 · 정산→미수주 · 완료→어디든 등)은 서버가 `이 상태로 바꿀 수 없습니다 · 새로 고쳐 주세요`로 거부한다(D-75·D-79·D-44, 진행에서 사람이 가는 곳 없음 = 사용자 D13 확정)"
    - "진행→정산은 사람이 고르는 목적지가 아니다 — 전이표에 그 쌍이 없어 액션으로 정산을 보내도 게이트가 거부하고, 갈 곳 목록(`statusDestinations`)에 정산이 나오는 경우가 없다(D-76, S7 전이표 — 자동 전환은 04-11)"
    - "수주중→진행과 미수주→진행은 시작일을 요구하고, 종료일이 비어 있으면 같은 UPDATE 문장에서 시작일 값으로 채운다 — 시작일이 없으면 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`로 거부된다(D-82, Copywriting 막힘 — 진행 전환)"
    - "수주중·미수주의 전환은 권한표 `projects.status` 쓰기(기본: 팀장·본부 책임자·대표), 정산→완료는 `projects.complete` 쓰기(기본: 대표 — 시스템 관리자는 시드가 전 메뉴를 켠다)가 있는 사람만 한다 — 담당 PM이 액션을 직접 불러도 거부된다(D-46 갱신·D-79)"
    - "업무 범위가 `자기 팀`인 계급(팀장)은 **오늘(KST) 발령 이력상 자기 소속 팀의 프로젝트만** 전환한다 — 다른 팀 프로젝트는 권한 메뉴가 있어도 `다른 팀 프로젝트의 상태는 바꿀 수 없습니다`로 거부된다. 업무 범위가 `전사`인 계급(본부 책임자·대표·시스템 관리자)은 팀과 무관하다. 소속 판정은 한 함수(`actorCoversProjectTeam`)이고 전환 게이트·갈 곳 목록·기간 수정 권리(04-22)가 같은 함수를 부른다(사용자 D11·D20, OV-4)"
    - "Phase 4에서 정산→완료를 할 수 있는 사람은 대표와 시스템 관리자뿐이다 — 통합 테스트가 두 계급의 통과와 팀장·담당 PM의 거부를 DB 재조회로 단언한다. 전환 함수·게이트는 호출자를 모른다: Phase 5는 결재 승인 경로에서 같은 `changeProjectStatus`를 부르고, 결재 트랜잭션을 바깥 트랜잭션으로 넘길 수 있다(D-79, CEO 리뷰 A-23)"
    - "시드는 팀장·본부 책임자·대표에게 `projects` 보기와 `projects.status` 쓰기를, 대표에게 `projects.complete` 쓰기를 **없을 때만** 넣는다 — 관리자가 권한표에서 끈 값을 다음 배포의 시드가 되살리지 않는다. 시스템 관리자의 전 메뉴 루프만 계속 덮어쓴다(D-46·D-79, CEO 리뷰 A-05)"
    - "`statusDestinations`는 그 사람이 지금 상태에서 갈 수 있는 곳과 **서버가 판정한 막힘 이유**를 함께 돌려준다(`{ to, blockedReason }` — 시작일이 없으면 진행의 이유가 게이트 문자열 그대로) — 진행·완료는 누구에게도 빈 목록, 권한이 없는 사람(담당 PM)과 다른 팀 팀장은 어느 상태든 빈 목록이다. 화면(04-21)은 이 목록이 비면 「상태 바꾸기」를 렌더하지 않고, 막힘 이유를 스스로 만들지 않는다(S3·S7, CEO 리뷰 A-09)"
    - "모든 사람의 전환이 `domain/rules.gate` 단일 진입점을 `project.transition`·`project.start-date-required` 두 규칙으로 지나고, 거부는 게이트가 돌려준 이유 문자열 그대로 액션 `serverError`로 나간다(ROADMAP 기준 3·4)"
    - "전환 함수 본문에 상태 이름을 직접 비교하는 분기가 없다 — 비교는 전이표 데이터 모듈(04-06)과 게이트 규칙 안에만 있다(ROADMAP 기준 4)"
    - "전환 요청은 **화면이 본 현재 상태(`from`)**를 싣고, 서버는 프로젝트 행을 배타 잠금(`FOR UPDATE`)으로 읽어 그 상태가 `from`과 다르면 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`로 거부한다 — 오래된 화면이 동료가 방금 닫은 프로젝트를 다른 합법 쌍으로 조용히 되살리지 못하고, 동시에 두 사람이 전환하면 한 사람만 성공한다(CEO 리뷰 A-11·OV-3)"
    - "상태 변경과 행동 로그 `status_change` 한 줄(detail에 from·to·`trigger: \"manual\"`)이 **한 트랜잭션**이다 — 로그 쓰기가 실패하면 상태도 바뀌지 않는다. 거부된 전환은 행동 로그를 남기지 않고, 운영 로그에 `write.denied`(사람 id · 규칙 · 프로젝트 id · from·to — 금액 없음) 한 줄을 남긴다(D-50, CEO 리뷰 A-01·A-39·B-26, 사용자 D19)"
    - "상태 고르기 목록이 쓸 코드표 목록(`listProjectStatusCatalog`)은 `projects` 보기 권한으로 읽고(코드표 관리 권한이 없는 팀장·대표에게도 온다), 라벨은 관리자가 비활성으로 돌린 값까지 포함해 찾으며, 고르기 목록·필터에 나오는 값은 `PROJECT_STATUSES` 다섯뿐이다 — 설명이 없는 값은 `description`이 null이다(D-93, S7·S14, CEO 리뷰 A-10)"
  prohibitions:
    - statement: "상태 전환 권한이 팀의 경계를 넘어서는 안 된다. 사용자는 「각 기획팀의 팀장까지만」(D-46)을 「팀장은 자기 팀 프로젝트만」(D11)으로 확정했다 — 권한 메뉴 하나로 모든 팀 프로젝트를 닫거나 되살리는 구현은 이 결정을 조용히 넓힌다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/status.ts"
      provides: "전환 함수(바깥 트랜잭션 선택) · 갈 곳 목록(막힘 이유 포함) · 상태 코드표 목록 · 업무 범위 판정 한 함수"
      exports: ["changeProjectStatus", "statusDestinations", "listProjectStatusCatalog", "actorCoversProjectTeam"]
    - path: "repositories/projects.ts"
      provides: "프로젝트 행 배타 잠금 읽기 · 조건부 상태 갱신(종료일 채움)"
      contains: "lockProjectForWrite"
    - path: "repositories/permissions.ts"
      provides: "없을 때만 넣는 권한 시드 함수"
      contains: "insertPermissionIfAbsent"
    - path: "test/integration/lock-race.ts"
      provides: "두 연결 경합을 시간 지연 없이 재현하는 테스트 도우미 — 잠금 대기 확인 · 지연 약속"
      exports: ["waitForLockWaiter", "deferred"]
    - path: "test/unit/domain/project-status.test.ts"
      provides: "전환 × 주체(담당 PM·팀장(자기 팀/다른 팀)·본부 책임자·대표·시스템 관리자) × 선행 조건 결정표 + 갈 곳 목록 표"
      min_lines: 60
    - path: "test/integration/project-status.test.ts"
      provides: "04-06의 마이그레이션·잠금 케이스에 더해 실제 DB 전환·로그 한 줄·원자성·거부 무변경·PM 직접 호출 거부·오래된 화면·두 연결 경합·팀 범위·대표/시스템 관리자 완료·시드 보존 증명"
      min_lines: 120
  key_links:
    - from: "domain/projects/status.ts"
      to: "domain/rules/gate.ts"
      via: "사람의 전환 넷이 `project.transition`·`project.start-date-required` 두 규칙을 한 진입점으로 지난다"
      pattern: "gate("
    - from: "domain/rules/register.ts"
      to: "domain/projects/status-transitions.ts"
      via: "전환 규칙이 04-06의 전이표 데이터를 읽는다 — 상태 문자열 비교가 규칙 안에만 있다"
      pattern: "ALLOWED_TRANSITIONS"
    - from: "domain/projects/status.ts"
      to: "domain/action-log/record.ts"
      via: "전환 성공이 같은 트랜잭션으로 status_change를 남긴다 — recordAction이 tx를 받는다"
      pattern: "recordAction"
    - from: "domain/projects/status.ts"
      to: "domain/org/index.ts"
      via: "업무 범위 `team`의 소속을 오늘(KST) 발령 이력으로 찾는다"
      pattern: "teamAtDate"
    - from: "app/(app)/projects/actions.ts"
      to: "domain/projects/status.ts"
      via: "changeProjectStatusAction이 zod enum(from·to)을 지나 전환 함수 하나를 부른다 — 액션에 판정이 없다"
      pattern: "changeProjectStatus("
---

<objective>
**As a** 기획팀장·대표, **I want to** 자기 팀의 수주중 프로젝트를 시작일과 함께 진행으로 올리거나 미수주로 닫고, 미수주를 다시 진행으로 살리고, 정산이 끝난 프로젝트를 완료로 잠그는 일을 서버가 규칙 하나로 판정하기를, **so that** PM이 상태를 임의로 바꾸는 일도, 팀장이 남의 팀 프로젝트를 바꾸는 일도 구조적으로 없고 Phase 5는 완료 전환의 호출자만 결재 승인으로 바꾸면 된다.

**2026-09-23 체커 1차 분할.** 옛 04-06(28파일)이 한 실행자 컨텍스트를 넘어 셋으로 나눴다. 이 플랜은 **가운데 조각 — 사람의 전환 넷의 서버 쪽**이다. 상태 다섯 값·코드표·전이표 데이터 모듈·완료 잠금 게이트 교체는 04-06이 이미 만들었고, 상태 바꾸기 화면·부제의 마지막 변경일·목록의 다섯 상태 표시·생애 E2E는 04-21이 이 플랜의 액션 위에 얹는다. 이 플랜에는 화면이 없다 — 증명은 단위 결정표와 실제 DB 통합 테스트다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE).** (1) **A-01·A-23:** `appendActionLog`는 트랜잭션을 받지 못해 「UPDATE → 로그」가 두 번의 자동 커밋이었다 — 로그가 실패하면 상태만 바뀐 채 남는다. 두 함수에 선택 `tx`를 더하고 전환을 한 트랜잭션으로 묶으며, Phase 5가 결재 트랜잭션을 넘길 수 있게 한다(이 플랜이 첫 사용처라 행동 로그 두 파일을 여기서 고친다). (2) **OV-3·A-11:** 전환은 프로젝트 행 배타 잠금(`FOR UPDATE`) 아래에서 판정하고, 화면이 본 상태(`from`)를 기대값으로 쓴다 — 두 연결 경합을 시간 지연 없이 재현하는 테스트 도우미를 여기서 만든다(04-11·04-22·04-12·04-26과 그룹 B의 04-40·04-07이 쓴다). (3) **A-27 → 사용자 D11·D20·OV-4:** 팀장은 자기 팀만 — 04-27이 만든 계급 업무 범위를 읽는 **한 규칙** `actorCoversProjectTeam`을 여기 두고 전환 게이트에 싣는다. (4) **A-05:** 시드가 배포마다 권한 행을 덮어써 관리자가 끈 권한을 되살렸다 — 시스템 관리자 밖의 계급 기본값은 없을 때만 넣는다. (5) **A-09·A-10·A-39·B-26(D19):** 갈 곳 목록에 서버 판정 막힘 이유, 코드표 목록을 `references.ts` 선례로, 거부 운영 로그와 `trigger: "manual"`. (6) 사용자 D13 — 진행에서 사람이 가는 곳 없음(⚠ S7) 확정.

Purpose: ROADMAP 기준 3·4는 전환이 전부 `domain/rules.gate` 단일 진입점을 지나는 구조적 속성을 요구한다. D-79는 Phase 4의 완료 주체를 대표·시스템 관리자로 두고, Phase 5가 결재 승인을 호출자로 바꿀 때 전환 함수는 그대로라고 정했다.

Output: 권한표 메뉴 둘 + 없을 때만 넣는 시드 · 게이트 규칙 둘(팀 범위 포함) · 전환 함수(한 트랜잭션·배타 잠금·바깥 tx)·갈 곳 목록(막힘 이유)·코드표 목록·업무 범위 판정 · 잠금 읽기·조건부 갱신 리포지토리 · 행동 로그 tx · 액션(`from`) · 경합 테스트 도우미 · 단위 1(신규) · 통합 1(04-06 파일 확장).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 시간은 `deps.now`로 주입한다(가짜 타이머 없음). 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-27-SUMMARY.md
@CLAUDE.md
# 범위 Read(큰 파일):
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 677–722행(§1 D-75 — 읽는 코드 경로 전부)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1227–1255행(S7 전이표·막힘 문구) · 366–378행(Copywriting 막힘·확인)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 740–775행(A-01·A-05·A-09·A-10·A-11) · 806행(A-27) · 810행·822행(A-23·A-39) · 40–41행(OV-3·OV-4)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(사람의 전환)** 다. 프로브는 REQUIREMENTS.md의 옛 네 상태 문구로 돌았으므로(04-08 Task 3이 문구를 고친다) **수용 기준은 결정된 모델 D-44·D-46·D-50·D-75·D-79·D-82, 사용자 D11·D13·D20과 UI-SPEC S7 전이표로 끌어와** `must_haves.truths`에 적었다. S7의 화면 상태(loading·error 렌더·partial 렌더)와 backstop은 화면을 만드는 04-21이 옮겼다 — 이 플랜은 그 화면이 쓰는 서버 계약(게이트 문자열·갈 곳 목록과 막힘 이유·설명 목록)을 truths로 덮는다.

**계획 단계 가정·사용자 결정(표면화):**
- **진행 상태의 수동 전환(⚠ S7) — 사용자 D13 「없음 유지」:** 진행에서 사람이 가는 곳이 없다. 수주 뒤 계약이 깨진 건은 정산→완료로 닫는다.
- **권한표 기본값:** 「팀장 이상」(D-46)을 계급 순위가 아니라 새 메뉴 `projects.status`(쓰기) **+ 계급 업무 범위(04-27)**로, 「대표·시스템 관리자」(D-79)를 새 메뉴 `projects.complete`(쓰기)로 표현한다(04-02의 `projects.revenue` 선례). 시드는 팀장·본부 책임자·대표에게 `projects.status` 쓰기와 **`projects` 보기**를, 대표에게 `projects.complete` 쓰기를 켠다 — 보기 없이 전환 권한만 주면 팀장이 프로젝트 화면에 닿지 못한다. **경영관리는 시드 계급이 아니라** 관리자가 권한표에서 `projects.status`를 배정하고, 전사 업무라면 계급 화면에서 업무 범위를 `전사`로 바꿔야 한다(04-27) — 이 안내를 SUMMARY에 적는다(CEO 리뷰 A-43).
- **업무 범위 판정(D11·D20):** `actorCoversProjectTeam(viewer, project, { todayKst }, deps?)` — 계급 업무 범위가 `company`면 참, `team`이면 `teamAtDate(viewer, viewer.id, todayKst)`의 팀 id가 프로젝트 팀과 같을 때만 참(발령 이력이 없으면 거짓 — 임의의 기본 팀으로 떨어지지 않는다, ARCHITECTURE §4-3). 오늘은 `lib/kst-date`(04-29)의 `kstToday(now())`. 이 결과를 게이트 ctx의 사실(`actorCoversTeam`)로 넘기고 판정은 게이트 규칙 안에서 한다 — `can`·`visible`·`scopeFor`는 바꾸지 않는다(OV-4).
- **다른 팀 거부 문구(새 문구):** `다른 팀 프로젝트의 상태는 바꿀 수 없습니다` — UI-SPEC Copywriting에 없는 문자열이라 SUMMARY 「새 문구」 표에 적는다(D16: `.planning/` UI-SPEC을 손으로 고치지 않는다).
- **거부 운영 로그(D19 — B-26 권장안):** 게이트·권한 거부 때 `log.warn("write.denied", { actorId, rule, entity: "project", entityId, from, to })` — 금액 없음. 행동 로그(`action_log`)에는 남기지 않는다(T-04-38 유지).
- **시스템 행위자의 계급 표시:** `SYSTEM_VIEWER`의 로그는 `actorRoleId='role-sysadmin'`이다 — 계급으로 로그를 거르는 화면이 시스템과 시스템 관리자를 섞을 수 있다는 사실을 SUMMARY에 한 줄로 남긴다(A-39 NOTE, 동작 변경 없음).
- **Phase 5 연결점(D-79·A-23):** 정산→완료의 호출자는 지금 `changeProjectStatusAction`(대표·시스템 관리자)이고 Phase 5가 결재 승인 경로로 바꾼다. 전환 함수·게이트는 그대로 두고, 결재 트랜잭션을 `deps.tx`로 넘기면 같은 트랜잭션에서 돈다.
- **경합 테스트 도우미:** `test/integration/lock-race.ts`의 `waitForLockWaiter(pool, { timeoutMs })`는 `pg_stat_activity`에서 이 DB의 `wait_event_type = 'Lock'`인 연결이 생길 때까지 짧게 폴링하고, `deferred()`는 테스트가 푸는 약속을 만든다. domain 쓰기 함수는 테스트 전용 주입 지점 `deps.afterLock?: () => Promise<void>`(잠금 획득 직후 호출)를 갖는다 — **시간 지연(sleep)으로 순서를 맞추지 않는다**.
</probe_fallback>

<reversibility_notes>
- 권한표 새 메뉴 키 `projects.status`·`projects.complete` — **costly**: 관리자가 권한표에서 켜 둔 행이 이 키를 참조한다. 이름을 바꾸면 권한 행 데이터 이관이 따라온다.
- 전환 함수 서명 `changeProjectStatus(viewer, projectId, { from, to }, deps?)` — **reversible**: 호출자는 이 페이즈의 액션 하나와 Phase 5의 결재 승인 하나뿐이다.
- 시드의 「없을 때만」 전환 — **reversible**: 시드 함수 한 곳이다.
</reversibility_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 자기 팀 팀장이 수주중 프로젝트를 시작일과 함께 진행으로(권한표 → 시드 → 업무 범위 판정 → 게이트 → 잠금 읽기 → 조건부 UPDATE + 로그 한 트랜잭션 → 액션 → 통합)</name>
  <files>domain/permissions/menus.ts, domain/seed/index.ts, repositories/permissions.ts, domain/rules/register.ts, domain/projects/status.ts, repositories/projects.ts, repositories/action-log.ts, domain/action-log/record.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/integration/project-status.test.ts</files>
  <reversibility rating="costly">권한표 메뉴 키 둘은 관리자가 켠 권한 행이 참조한다 — 이름을 바꾸면 권한 행 이관이 따라온다. 키 이름은 04-02 `projects.revenue` 선례를 따른 계획 재량이다.</reversibility>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-44·D-46·D-50·D-79·D-82 원문
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 740–742행(A-01 — 로그가 트랜잭션 밖인 근거와 수정안) · 756–758행(A-05)
    - `domain/permissions/menus.ts`(전문) — `projects.revenue` 메뉴를 더한 04-02 주석
    - `domain/permissions/roles.ts` 20–35행(시드 계급 id) · 04-27이 더한 `workScope`(DTO·리포지토리 조회)
    - `domain/seed/index.ts` 120–180행(권한 시드 루프 — `DEFAULT_ROLE_ID`의 `projects` 보기·쓰기 선례 · 시스템 관리자 전 메뉴 루프)
    - `repositories/permissions.ts`(전문 107행) — `upsertPermission`의 `onConflictDoUpdate`
    - `domain/rules/gate.ts`·`domain/rules/register.ts`(04-06 산출물 전문) — 등록 형태와 `project.line-edit`
    - `domain/projects/status-transitions.ts`(04-06 산출물 전문) — `PROJECT_STATUSES`·`ALLOWED_TRANSITIONS`
    - `domain/projects/index.ts` 210–290행(`findProject` — 행 범위 `scopeFor`와 DTO)
    - `domain/org/index.ts` 175–205행(`teamAtDate` — 조회일 이하 가장 늦은 발령, 없으면 null)
    - `lib/kst-date.ts`(04-29 산출물) · `lib/db-transaction.ts`(`withTransaction`) · `db/client.ts` 43–48행(`DbOrTx`)
    - `repositories/projects.ts` 211–302행(단건 조회·삽입·갱신 형태)
    - `repositories/action-log.ts` 1–40행(`appendActionLog` — 풀 `db`만 쓴다) · `domain/action-log/record.ts` 1–75행·110–145행(`status_change`가 끌 수 없는 종류 · `recordAction` 본문)
    - `app/(app)/projects/actions.ts`·`actions.registry.ts`(전문) — 액션 규약과 등록
    - `test/integration/leak-scan.test.ts`(전문) — side-effect import 목록(프로젝트 액션 레지스트리가 이미 들어 있는지 확인만 — 없으면 SUMMARY에 적고 이 플랜 밖에서 고치지 않는다)
    - `test/integration/revenue-entries.test.ts` 1–60행(계급별 권한을 켜고 viewer를 만드는 통합 테스트 형태) · `test/integration/project-status.test.ts`(04-06 산출물) · `test/integration/team-memberships.test.ts`(발령 이력 준비 형태)
  </read_first>
  <action>
① **권한표·시드(D-46·D-79·A-05).** `domain/permissions/menus.ts`에 `{ key: "projects.status", label: "프로젝트 상태 변경" }`·`{ key: "projects.complete", label: "프로젝트 완료" }`를 더한다(04-02 주석과 같은 결로 이유 한 줄). `repositories/permissions.ts`에 `insertPermissionIfAbsent(viewer, row)`(`onConflictDoNothing`)를 더한다. `domain/seed/index.ts`: `role-team-lead`·`role-division-head`·`role-ceo`의 `projects` 보기 + `projects.status` 쓰기, `role-ceo`의 `projects.complete` 쓰기를 **`insertPermissionIfAbsent`로** 넣고, 기존 기획 PM(`DEFAULT_ROLE_ID`)의 `projects` 보기·쓰기 시드도 같은 함수로 바꾼다 — 시스템 관리자 전 메뉴 루프만 `upsertPermission` 그대로(관리자가 시스템 관리자 권한을 끄는 경우는 없다). 04-06·04-27이 바꾼 상수는 건드리지 않는다.

② **행동 로그 트랜잭션(A-01).** `repositories/action-log.ts`의 `appendActionLog`에 선택 인자 `tx?: DbOrTx`(없으면 풀 `db`)를 더하고, `domain/action-log/record.ts`의 `recordAction`이 `deps.tx`(또는 인자)를 받아 그대로 넘긴다. 기존 호출자는 바뀌지 않는다(인자 없음 = 지금 동작).

③ **업무 범위 판정·게이트 규칙.** `domain/projects/status.ts`(신규)에 `actorCoversProjectTeam(viewer, project, { todayKst }, deps?)`(`<probe_fallback>` 판정 — 계급 업무 범위는 04-27의 리포지토리로 읽는다). `domain/rules/register.ts`: (a) `project.transition` — ctx `{ from, to, actorMenus: { status: boolean; complete: boolean }, actorCoversTeam: boolean }`: 04-06의 `ALLOWED_TRANSITIONS`에 없는 쌍이면 `이 상태로 바꿀 수 없습니다 · 새로 고쳐 주세요`, 그 쌍의 메뉴 권한이 없으면 `상태를 바꿀 권한이 없습니다`, 권한은 있는데 팀 범위 밖이면 `다른 팀 프로젝트의 상태는 바꿀 수 없습니다`. (b) `project.start-date-required` — ctx `{ to, startDate }`: 목적지가 진행이고 시작일이 없으면 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`. 04-06의 `project.line-edit`는 건드리지 않는다.

④ **리포지토리.** `repositories/projects.ts`에 `lockProjectForWrite(viewer, id, tx)`(`SELECT … FOR UPDATE` — 행 또는 null)와 `updateProjectStatusIfCurrent(viewer, id, { expectedStatus, status, fillEndDateFromStart: boolean }, tx)`(`UPDATE … SET status, version + 1, updated_at = now() [, end_date = coalesce(end_date, start_date)] WHERE id = $1 AND status = $expected RETURNING`)를 더한다. 목적지가 진행이면 종료일 채움을 같은 문장에서 한다(D-82).

⑤ **전환 함수.** `changeProjectStatus(viewer, projectId, { from, to }, deps?: { now?; tx?; afterLock?; can?; recordAction? })`: 바깥 `tx`가 없으면 `withTransaction`을 연다 → 행 범위(`scopeFor`) 확인 → `lockProjectForWrite`(없으면 찾을 수 없음) → `deps.afterLock?.()` → 잠근 행의 상태가 `from`과 다르면 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`(UserFacing — A-11) → `can()`으로 두 메뉴 권한 · `actorCoversProjectTeam` → `gate(project, "project.transition", …)` → `gate(project, "project.start-date-required", …)` → 거부면 `GateBlockedError(reason)` + `log.warn("write.denied", …)` → 통과하면 `updateProjectStatusIfCurrent(expected = from, tx)`(0행이면 위와 같은 동시 변경 거부) → `recordAction(viewer, { actionType: "status_change", entity: "project", entityId, detail: { from, to, trigger: "manual" } }, { tx })`. 로그가 던지면 트랜잭션이 되돌아가 상태도 그대로다. 전이표 import는 `status-transitions.ts`에서만 하고 `register.ts`와 서로 import하지 않는다(`test/unit/import-cycles.test.ts`). **함수 본문에 상태 문자열 비교를 두지 않는다**(`from` 비교는 행 값과 입력 값의 일치 확인이지 상태 이름 분기가 아니다) — 호출자 구분도 두지 않는다(D-79).

⑥ **액션.** `app/(app)/projects/actions.ts`에 `changeProjectStatusAction`(zod: `projectId` uuid · `from`·`to`는 `PROJECT_STATUSES` enum) → `changeProjectStatus` → `revalidatePath`. `actions.registry.ts`에 등록한다(D-38).

⑦ **통합(트레이서 증명, `test/integration/project-status.test.ts`에 describe 추가).** 팀 A에 오늘(KST) 발령된 팀장 viewer(권한 시드 반영)가 팀 A의 시작일만 있고 종료일이 빈 수주중 프로젝트를 `{ from: bidding, to: in_progress }`로 바꾸면 DB 재조회로 상태 `in_progress` · 종료일 = 시작일 · `status_change` 로그 정확히 한 줄(detail from·to·`trigger: "manual"`)을 단언한다. 같은 프로젝트를 담당 PM viewer로 `changeProjectStatus`에 직접 넘기면 권한 거부이고 상태·로그가 그대로다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm vitest run --project unit test/unit/import-cycles.test.ts</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오거나 import 순환 테스트의 failed 개수가 1 이상이다 — 전환 함수와 게이트 등록이 서로 import한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/leak-scan.test.ts test/integration/action-log.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 팀장의 진행 전환이 DB·로그로 이어지지 않았거나, PM이 통과했거나, 새 액션이 누수 스캔 커버리지에서 빠졌거나, 행동 로그의 tx 인자가 기존 호출을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `menus.ts`에 `projects.status`·`projects.complete`가 있고 시드가 팀장·본부 책임자·대표에게 `projects` 보기와 `projects.status` 쓰기, 대표에게 `projects.complete` 쓰기를 `insertPermissionIfAbsent`로 넣는다 · 시스템 관리자 루프만 `upsertPermission`이다
    - `appendActionLog`·`recordAction`이 선택 `tx`를 받고 인자가 없을 때 동작이 그대로다(`action-log.test.ts` 초록)
    - `register.ts`에 `project.transition`·`project.start-date-required`가 등록되고 두 규칙이 `ALLOWED_TRANSITIONS`를 읽으며 `project.transition`이 팀 범위 사실(`actorCoversTeam`)을 판정한다
    - `changeProjectStatus`가 한 트랜잭션 안에서 `lockProjectForWrite` → 판정 → 조건부 UPDATE → `recordAction({ tx })` 순이고, 본문에 상태 문자열 리터럴 비교와 호출자 분기가 없다
    - `changeProjectStatusAction` 스키마가 `from`·`to`를 받고 `actions.registry.ts`에 등록됐다
    - 통합 테스트가 팀장의 진행 전환(상태·종료일·로그 한 줄·`trigger`)과 PM 직접 호출 거부(무변경)를 DB 재조회로 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>자기 팀 팀장이 수주중 프로젝트를 진행으로 바꾸는 한 경로가 권한표·업무 범위·게이트·잠금·한 트랜잭션 갱신과 로그·액션까지 서버에서 끝났고, PM은 액션을 직접 불러도 막힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 나머지 전환 셋 + 팀 범위 + 갈 곳 목록(막힘 이유) + 상태 코드표 목록 + 결정표 + 대표·시스템 관리자 완료(D-79)</name>
  <files>domain/projects/status.ts, domain/rules/register.ts, test/unit/domain/project-status.test.ts, test/unit/domain/rules-gate.test.ts, test/integration/project-status.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1227–1255행(S7 전이표 — 미수주 → 진행은 모달 없이 즉시 · 정산 → 완료는 바로 확인 모달: 이 플랜은 그 화면이 쓰는 갈 곳 목록만 만든다)
    - `domain/projects/references.ts`(전문 — `projects` 보기 게이트 + 리포지토리를 `scope: all`로 부르는 선례, A-10)
    - `repositories/code-tables.ts` 1–30행(`listCodeItems`의 `includeInactive`)
    - `test/unit/vendors/account-number-plan.test.ts`(결정표 단위 테스트 형태)
    - `test/unit/domain/rules-gate.test.ts`(04-06 갱신본 전문)
  </read_first>
  <behavior>
    - 수주중→진행: `projects.status` 쓰기 + 팀 범위 안 + 시작일 있음 → 통과, 종료일이 비면 시작일로 채워진다 · 시작일 없음 → 막힘 문구 · 담당 PM(권한 없음) → 권한 거부 · 다른 팀 팀장 → `다른 팀 프로젝트의 상태는 바꿀 수 없습니다` · 본부 책임자(전사, 팀 발령 없음) → 통과
    - 수주중→미수주: `projects.status` 쓰기 + 팀 범위 안이면 기간 없이 통과
    - 미수주→진행: `projects.status` 쓰기 + 팀 범위 안 + 시작일 → 통과, 번호·줄이 그대로 · 시작일 없음 → 막힘 문구
    - 정산→완료: `projects.complete` 쓰기(대표·시스템 관리자 — 둘 다 전사)만 통과 · 팀장(`projects.status`만 있음) → 권한 거부 · 담당 PM → 권한 거부(D-79)
    - 진행→미수주 · 진행→수주중 · 진행→정산 · 정산→미수주 · 정산→진행(상태 바꾸기 경로) · 완료→진행 · 완료→미수주 → 전부 「이 상태로 바꿀 수 없습니다」
    - `statusDestinations`: 수주중 + 자기 팀 팀장 + 시작일 있음 → `[{ to: 진행, blockedReason: null }, { to: 미수주, blockedReason: null }]` · 시작일 없음 → 진행의 `blockedReason` = 게이트 막힘 문구 · 진행 + 누구든 → [] · 정산 + 대표 → [완료] · 정산 + 시스템 관리자 → [완료] · 정산 + 팀장 → [] · 완료 → [] · 미수주 + 자기 팀 팀장 → [진행] · 다른 팀 팀장 → 어느 상태든 [] · 담당 PM → 어느 상태든 []
    - `listProjectStatusCatalog`: 팀장 viewer(코드표 관리 권한 없음)에게도 다섯 값을 정렬 순(수주중·진행·정산·완료·미수주)으로, 설명이 없으면 `description: null` · 관리자가 `settling`을 비활성으로 돌려도 라벨 찾기에서 `정산`이 나온다 · 코드표에 관리자가 더한 여섯째 값은 목록에 나오지 않는다(`PROJECT_STATUSES`만)
  </behavior>
  <action>
① `test/unit/domain/project-status.test.ts`에 위 `behavior`를 결정표로 먼저 쓴다(RED) — 권한은 `deps.can`, 업무 범위·소속은 `deps`로 주입해 DB 없이 돈다. 주체 여섯(담당 PM · 자기 팀 팀장 · 다른 팀 팀장 · 본부 책임자 · 대표 · 시스템 관리자)을 표의 열로 둔다.

② `domain/projects/status.ts`에 `statusDestinations(viewer, project, deps?)` — 전이표에서 `from`이 지금 상태이고 그 사람이 메뉴 권한과 팀 범위를 가진 목적지마다 `project.start-date-required`를 서버에서 돌려 `{ to, blockedReason }`을 만든다(진행→정산 쌍이 전이표에 없으므로 정산은 결과에 나올 수 없다) — 와 `listProjectStatusCatalog(viewer, deps?)` — `domain/projects/references.ts` 선례: `can(viewer, "projects", "view")`가 없으면 거부, 있으면 리포지토리 `listCodeItems`를 `{ scope: { rows: "all", includeArchived: false }, includeInactive: true }`로 불러 `PROJECT_STATUSES` 다섯의 `value`·`label`·`description`만 정렬 순으로 돌려준다. 나머지 전환 셋은 Task 1의 같은 함수가 전이표로 처리한다 — 전환별 분기를 더하지 않는다(GREEN).

③ `test/unit/domain/rules-gate.test.ts`에 `project.transition`·`project.start-date-required`의 경계(목록 밖 쌍 · 메뉴 권한 없음 · 팀 범위 밖 · 시작일 없음)를 한 줄씩 더한다. 04-06이 바꾼 `project.line-edit` 케이스는 그대로 둔다.

④ `test/integration/project-status.test.ts`에 describe를 더한다: (a) 네 전환이 실제 DB에서 상태를 바꾸고 `status_change` 로그가 정확히 한 줄(detail from·to·`trigger`), (b) 수주중→진행에서 종료일이 비었으면 시작일로 채워졌다(DB 재조회 — Task 1 케이스를 미수주→진행에도), (c) 거부된 전환(권한 없음·목록 밖·시작일 없음)은 상태·로그 무변경(DB 재조회), (d) 담당 PM viewer로 `changeProjectStatus`를 직접 불러도 거부, (h) **D-79** — 정산 프로젝트를 대표 viewer와 시스템 관리자 viewer가 각각 완료로 바꾸면 통과(프로젝트 둘), 팀장·담당 PM viewer는 거부되고 상태가 정산 그대로다, (j) **D11** — 팀 B에 발령된 팀장이 팀 A 프로젝트를 바꾸면 `다른 팀 프로젝트의 상태는 바꿀 수 없습니다`(무변경), 본부 책임자(전사)는 통과, 어제 팀 A에서 팀 B로 옮긴 팀장은 오늘(`deps.now` 주입) 팀 A 프로젝트를 바꿀 수 없다 — 발령 이력의 오늘 값으로 판정한다, (k) **A-10** — 팀장 viewer의 `listProjectStatusCatalog`가 다섯 값, `settling` 비활성화 뒤에도 라벨 `정산`. 정산 상태는 테스트 준비 단계에서 DB에 직접 둔다(자동 전환은 04-11). (f)·(g)는 04-06이 이미 쓴 마이그레이션·잠금 케이스다 — 지우지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-status.test.ts test/unit/domain/rules-gate.test.ts test/unit/import-cycles.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 전이 결정표·팀 범위·갈 곳 목록·규칙 경계가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/quote-lines.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 전환·로그·팀 범위·완료 권한·코드표 목록 중 하나가 DB에서 어긋났거나 04-06의 잠금·목록이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior`의 여섯 묶음을 전부 덮고 주체 여섯이 열로 있다
    - `statusDestinations`가 어떤 입력에서도 정산을 돌려주지 않고, 시작일 없는 진행 목적지의 `blockedReason`이 게이트 문자열과 글자 그대로 같다
    - `listProjectStatusCatalog`가 `projects` 보기로 게이트하고 `includeInactive: true`로 라벨을 찾으며 `PROJECT_STATUSES` 밖 값을 돌려주지 않는다
    - `rules-gate.test.ts`에 새 두 규칙의 경계 케이스(팀 범위 포함)가 있고 04-06의 케이스 수가 줄지 않았다
    - 통합 테스트 (a)~(d)·(h)·(j)·(k)가 전부 있고 초록이다 — 거부 쪽은 DB 재조회로 단언한다
    - D-79: 대표·시스템 관리자의 완료 통과와 팀장·담당 PM의 거부가 통합 테스트에 각각 있고, `changeProjectStatus`에 호출자 구분 인자가 없다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>사람이 하는 전환 넷이 권한·팀 범위·시작일 규칙대로 서버에서 동작하고, 완료는 대표·시스템 관리자만 하며, 화면이 쓸 갈 곳 목록(막힘 이유 포함)과 설명 목록이 준비됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 원자성·경합·시드 보존 증명 — 로그 실패 롤백(A-01) · 오래된 화면(A-11) · 두 연결 경합(OV-3) · 재시드 보존(A-05) · 거부 운영 로그 · 바깥 트랜잭션(A-23)</name>
  <files>test/integration/lock-race.ts, test/integration/project-status.test.ts, domain/projects/status.ts</files>
  <read_first>
    - `test/integration/project-status.test.ts`(Task 1·2 이후)
    - `test/integration/setup.ts` — 통합 테스트의 풀·DB 준비(두 번째 연결을 여는 방법)
    - `lib/log.ts`(전문) — `log.warn`이 한 줄 JSON을 `console.log`로 쓴다(테스트가 `console.log`를 가로채 단언한다)
    - `domain/seed/index.ts`의 `seedMasterData` · `domain/permissions/matrix.ts` 89–113행(`setPermissionCell` — 관리자가 권한표 칸을 끄는 경로)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 40행(OV-3 — 결정적 경합 테스트)
  </read_first>
  <behavior>
    - A-01: `deps.recordAction`이 던지면 `changeProjectStatus`가 오류로 끝나고 DB 재조회의 상태·`version`·`status_change` 로그 수가 전부 그대로다
    - A-11: 동료가 수주중을 미수주로 닫은 뒤 오래된 화면이 `{ from: bidding, to: in_progress }`를 보내면 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`이고 상태가 미수주 그대로다(미수주→진행이 합법 쌍이어도 되살아나지 않는다)
    - OV-3: 연결 A가 전환을 잠금 직후(`afterLock`)에서 멈춘 동안 연결 B의 같은 프로젝트 전환이 잠금 대기에 들어가고(`waitForLockWaiter`), A를 풀면 A가 커밋되고 B는 `from` 불일치로 거부된다 — 로그 한 줄
    - A-05: 관리자가 팀장 `projects.status` 쓰기를 끈 뒤 `seedMasterData`를 다시 돌려도 꺼진 채다 · 빈 DB의 첫 시드는 켠다
    - 거부 운영 로그: 권한 없는 전환 한 번에 `write.denied` 한 줄(규칙·프로젝트 id·from·to, 금액 키 없음)
    - A-23: 바깥 트랜잭션을 `deps.tx`로 넘기고 그 트랜잭션을 되돌리면 상태·로그가 둘 다 없다
  </behavior>
  <action>
① **`test/integration/lock-race.ts`**(테스트 도우미 — `*.test.ts`가 아니라 수집되지 않는다): `deferred<T>()`(`{ promise, resolve }`) · `waitForLockWaiter(pool, { timeoutMs = 5000 })`(이 DB에서 `wait_event_type = 'Lock'`인 연결이 1 이상이 될 때까지 짧게 폴링, 시간이 다하면 던진다). 이 파일은 04-11·04-22·04-12·04-26과 그룹 B의 04-40·04-07이 가져다 쓴다.

② `test/integration/project-status.test.ts`에 describe `원자성·경합(A-01·A-11·OV-3·A-05·A-23)`을 더하고 `behavior` 여섯 줄을 쓴다 — 경합 케이스는 A를 `deps.afterLock`에 걸어 멈추고, B를 별도 연결로 시작한 뒤 `waitForLockWaiter`로 B의 대기를 확인하고 A를 푼다(**sleep 금지**). 거부 운영 로그는 `console.log` 가로채기로 JSON 줄을 읽어 단언한다. RED가 나면 `domain/projects/status.ts`의 빈틈(예: 로그 실패 시 트랜잭션 밖 부작용, 바깥 tx 무시)을 고친다.

③ 이 전환 경로의 선판정(자정을 넘긴 진행 프로젝트는 판정 전에 정산으로)은 04-11이 `loadProjectForGate`로 같은 잠금 안에 넣는다 — 이 플랜은 잠금 읽기까지만 한다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 로그 실패가 상태를 남겼거나, 오래된 화면이 전환했거나, 경합에서 둘 다 성공했거나, 시드가 관리자 변경을 덮었거나, 바깥 트랜잭션 롤백이 로그를 남겼다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 시드 전환·행동 로그 tx가 기존 화면 흐름을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/integration/lock-race.ts`가 `deferred`·`waitForLockWaiter`를 내보내고 `setTimeout` 기반 고정 지연이 경합 순서를 정하는 코드가 테스트에 없다(폴링 간격만)
    - `behavior` 여섯 줄이 각각 케이스로 있고 전부 DB 재조회로 단언한다
    - 재시드 보존 케이스가 관리자 경로(`setPermissionCell`)로 권한을 끈다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 변경과 로그가 한 트랜잭션이고, 오래된 화면·동시 요청이 결정적으로 한 사람만 성공하며, 배포 시드가 관리자 권한 변경을 덮지 않는다는 것이 실제 DB에서 증명됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 상태 전환 액션 | 목적지 상태와 화면이 본 상태가 넘어온다. 전이표·권한·팀 범위·시작일 판정이 서버에만 있어야 한다 |
| 권한표·계급 업무 범위 → 전환 | 새 메뉴 두 개와 업무 범위가 전환 주체를 정한다. 시드 기본값이 틀리거나 덮이면 PM이 상태를 바꾸거나 관리자가 끈 권한이 살아난다 |
| domain → 게이트 | 이후 페이즈(결재 승인 호출자·증빙 필수·마감)가 얹힐 단일 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-34 | Elevation of Privilege | `changeProjectStatus` | high | mitigate | 화면이 버튼을 보내지 않는 것(04-21)과 별개로 서버가 `can()`으로 `projects.status`/`projects.complete`를 다시 판정하고 행 범위(`scopeFor`) 밖 프로젝트는 찾을 수 없음으로 떨어뜨린다. 담당 PM viewer가 함수를 직접 부르는 거부 케이스를 통합 테스트(Task 1 ⑦ · (d))로 고정한다 |
| T-04-35 | Elevation of Privilege | 정산→완료 | high | mitigate | 완료는 `projects.complete` 한 메뉴로만 열리고 시드는 대표에게만 켠다(시스템 관리자는 전 메뉴 루프). 팀장·담당 PM의 완료 시도 거부를 결정표와 통합 테스트(h)로 고정한다 — 화면 쪽(팀장에게 버튼 없음)은 04-21 E2E(c) |
| T-04-106 | Elevation of Privilege | 다른 팀 프로젝트 전환 | high | mitigate | `project.transition`이 업무 범위 사실(`actorCoversTeam`)을 판정하고, 소속은 오늘(KST) 발령 이력의 팀이다(D11·D20). 다른 팀 팀장 거부·전사 통과·어제 팀을 옮긴 팀장 거부를 통합 테스트(j)로 고정한다 |
| T-04-36 | Tampering | 목적지 상태 입력 | high | mitigate | zod enum이 다섯 값 밖을 막고, 전이표에 없는 쌍(진행→정산 포함)은 게이트가 거부한다. 정산은 사람이 고를 수 있는 목적지가 아니다 |
| T-04-37 | Tampering | 동시 전환 | medium | mitigate | 프로젝트 행 배타 잠금 아래에서 판정하고 `WHERE status = 기대값` 조건부 UPDATE가 뒤를 받친다. 두 연결 경합을 시간 지연 없이 재현해 한 사람만 성공·로그 한 줄을 고정한다(Task 3, OV-3) |
| T-04-105 | Tampering | 오래된 화면 제출 | medium | mitigate | 요청이 화면이 본 `from`을 싣고 잠근 행과 다르면 거부한다 — 동료의 결정을 다른 합법 쌍으로 되살리지 못한다(A-11, Task 3) |
| T-04-104 | Repudiation | 상태 변경과 로그의 원자성 | high | mitigate | `recordAction`이 전환 트랜잭션을 받아 로그 실패 시 상태도 되돌아간다 — 「상태는 바뀌었는데 로그가 없다」가 구조적으로 없다(A-01, Task 3) |
| T-04-38 | Repudiation | 상태 변경 이력 | medium | mitigate | `status_change`는 끌 수 없는 종류라 설정으로 이력을 없앨 수 없다. 거부된 전환은 행동 로그를 남기지 않고 운영 로그 `write.denied`(금액 없음)만 남긴다(통합 테스트 c · Task 3) |
| T-04-103 | Elevation of Privilege | 배포 시드 | high | mitigate | 시스템 관리자 밖 계급의 권한 기본값은 `insertPermissionIfAbsent`로 없을 때만 넣는다 — 관리자가 끈 권한이 다음 배포에 되살아나지 않는다(A-05, Task 3) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, import 순환 테스트 초록
- `CI=true pnpm test` 세 계층 통과
- `domain/rules/register.ts` 등록 목록: `project.transition` · `project.start-date-required`(04-06의 `project.line-edit`와 함께)
- `domain/permissions/scope-for.ts`·`can.ts`·`visible.ts` diff 0줄(업무 범위는 게이트 입력일 뿐)
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 사람이 하는 전환 넷이 권한표·팀 범위·시작일·동시성 규칙대로 게이트를 지나고, 진행·완료에는 사람이 가는 곳이 없다
- 상태 변경과 로그가 한 트랜잭션이고 오래된 화면·동시 요청이 결정적으로 한 사람만 성공한다
- Phase 4의 완료 주체가 대표·시스템 관리자뿐이고, Phase 5는 정산→완료의 호출자만 결재 승인으로 바꾸면 된다
- 배포 시드가 관리자 권한 변경을 덮지 않는다
- 04-21이 화면을 얹을 서버 계약(액션 · 갈 곳 목록과 막힘 이유 · 설명 목록 · 게이트 문자열)이 준비됐다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/projects/status.ts` — `changeProjectStatus`(바깥 tx 선택) · `statusDestinations`(`{ to, blockedReason }`) · `listProjectStatusCatalog` · `actorCoversProjectTeam` |
| 함수(신규, repository) | `lockProjectForWrite` · `updateProjectStatusIfCurrent`(`repositories/projects.ts`) · `insertPermissionIfAbsent`(`repositories/permissions.ts`) |
| 함수(변경) | `appendActionLog(…, tx?)` · `recordAction`의 `tx` 전달 |
| 게이트 규칙(신규) | `project.transition`(팀 범위 포함) · `project.start-date-required` |
| 권한표 메뉴(신규) | `projects.status`(프로젝트 상태 변경) · `projects.complete`(프로젝트 완료) + 없을 때만 넣는 시드(팀장·본부 책임자·대표: `projects` 보기 · `projects.status` 쓰기 / 대표: `projects.complete` 쓰기) |
| Server Action | `changeProjectStatusAction`(`from`·`to`, 등록 포함) |
| 로그 규약 | `status_change` detail `{ from, to, trigger: "manual" }` · 운영 로그 `write.denied` |
| 테스트 도우미 | `test/integration/lock-race.ts`(`deferred` · `waitForLockWaiter`) |
| 새 문구 | `다른 팀 프로젝트의 상태는 바꿀 수 없습니다`(SUMMARY 「새 문구」 표) |
| 테스트 | `test/unit/domain/project-status.test.ts`(신규) · `test/integration/project-status.test.ts`(전환·팀 범위·원자성·경합·시드 보존 describe 추가) · `rules-gate.test.ts`(경계 추가) |
| 새 스키마·마이그레이션·화면 | 없음 — 계급 업무 범위 컬럼은 04-27, 화면은 04-21 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-01 — 상태 변경과 로그가 두 번의 자동 커밋 | CRITICAL | Plan 04-20, Task 1 ②⑤: `appendActionLog`·`recordAction` 선택 tx + 한 트랜잭션 전환 · Task 3 로그 실패 롤백 증명 |
| A-05 — 배포 시드가 관리자 권한 회수를 되살림 | CRITICAL | Plan 04-20, Task 1 ①: `insertPermissionIfAbsent` · Task 3 재시드 보존 증명 |
| OV-3 — FOR SHARE로는 부족, 배타 잠금 + 결정적 경합 테스트 | HIGH | Plan 04-20, Task 1 ④⑤ `lockProjectForWrite` · Task 3 두 연결 경합 + `lock-race.ts` 도우미(뒤 플랜 공용) |
| A-27 → 사용자 D11·D20 / OV-4 — 팀장은 자기 팀만 | DECISION | Plan 04-20, Task 1 ③ `actorCoversProjectTeam` + 게이트 · Task 2 (j) |
| A-09 — 막힘 이유를 화면이 만들어야 함 | WARNING | Plan 04-20, Task 2 ②: `statusDestinations`가 `{ to, blockedReason }` |
| A-10 — 코드표 목록이 팀장에게 빈 배열·비활성 라벨 유실 | WARNING | Plan 04-20, Task 2 ②: `references.ts` 선례 · `includeInactive` · (k) |
| A-11 — 오래된 화면이 다른 합법 쌍으로 전환 | WARNING | Plan 04-20, Task 1 ⑤⑥ `from` · Task 3 |
| A-23 — Phase 5가 결재 tx 안에서 부를 자리 | NOTE | Plan 04-20, Task 1 ⑤ `deps.tx` · Task 3 |
| A-39 · B-26(사용자 D19) — 거부 운영 로그·`trigger: "manual"`·시스템 계급 표시 | NOTE | Plan 04-20, Task 1 ⑤ · Task 3 · SUMMARY 메모 |
| A-24(사용자 D13) | DECISION | 진행에서 사람이 가는 곳 없음 확정 — `<probe_fallback>` |
| A-43·A-44(이미 있음) | NOTE | 경영관리 배정 안내(SUMMARY) · PM 직접 호출 거부(통합 d) 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md` when done
</output>
