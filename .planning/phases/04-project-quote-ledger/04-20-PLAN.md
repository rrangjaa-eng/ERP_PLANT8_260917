---
phase: 04-project-quote-ledger
plan: 20
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - domain/permissions/menus.ts
  - domain/seed/index.ts
  - domain/rules/register.ts
  - domain/projects/status.ts
  - repositories/projects.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - test/unit/domain/project-status.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/project-status.test.ts
  - test/integration/leak-scan.test.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "사람이 하는 전환은 수주중→진행 · 수주중→미수주 · 미수주→진행 · 정산→완료 넷뿐이고, 목록 밖 요청(진행→수주중 · 진행→미수주 · 정산→미수주 · 완료→어디든 등)은 서버가 `이 상태로 바꿀 수 없습니다 · 새로 고쳐 주세요`로 거부한다(D-75·D-79·D-44, ⚠ S7 기본값: 진행에서 사람이 가는 곳 없음)"
    - "진행→정산은 사람이 고르는 목적지가 아니다 — 전이표에 그 쌍이 없어 액션으로 정산을 보내도 게이트가 거부하고, 갈 곳 목록(`statusDestinations`)에 정산이 나오는 경우가 없다(D-76, S7 전이표 — 자동 전환은 04-11)"
    - "수주중→진행과 미수주→진행은 시작일을 요구하고, 종료일이 비어 있으면 같은 UPDATE 문장에서 시작일 값으로 채운다 — 시작일이 없으면 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`로 거부된다(D-82, Copywriting 막힘 — 진행 전환)"
    - "수주중·미수주의 전환은 권한표 `projects.status` 쓰기(기본: 팀장·본부 책임자·대표), 정산→완료는 `projects.complete` 쓰기(기본: 대표 — 시스템 관리자는 시드가 전 메뉴를 켠다)가 있는 사람만 한다 — 담당 PM이 액션을 직접 불러도 거부된다(D-46 갱신·D-79)"
    - "Phase 4에서 정산→완료를 할 수 있는 사람은 대표와 시스템 관리자뿐이다 — 통합 테스트가 두 계급의 통과와 팀장·담당 PM의 거부를 DB 재조회로 단언한다. 전환 함수·게이트는 호출자를 모른다: Phase 5는 결재 승인 경로에서 같은 `changeProjectStatus`를 부르기만 하고 함수·규칙을 고치지 않는다(D-79)"
    - "시드는 팀장·본부 책임자·대표에게 `projects` 보기와 `projects.status` 쓰기를, 대표에게 `projects.complete` 쓰기를 켠다 — 보기 없이 전환 권한만 주면 팀장이 프로젝트 화면에 닿지 못한다(D-46·D-79)"
    - "`statusDestinations`는 그 사람이 지금 상태에서 갈 수 있는 곳만 돌려준다 — 진행·완료는 누구에게도 빈 목록, 권한이 없는 사람(담당 PM)은 어느 상태든 빈 목록이다. 화면(04-21)은 이 목록이 비면 「상태 바꾸기」를 렌더하지 않는다(S3·S7)"
    - "모든 사람의 전환이 `domain/rules.gate` 단일 진입점을 `project.transition`·`project.start-date-required` 두 규칙으로 지나고, 거부는 게이트가 돌려준 이유 문자열 그대로 액션 `serverError`로 나간다(ROADMAP 기준 3·4)"
    - "전환 함수 본문에 상태 이름을 직접 비교하는 분기가 없다 — 비교는 전이표 데이터 모듈(04-06)과 게이트 규칙 안에만 있다(ROADMAP 기준 4)"
    - "같은 프로젝트를 두 사람이 동시에 전환하면 한 사람만 성공하고 다른 사람은 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`를 받는다 — 로그는 한 줄이다"
    - "상태 변경이 행동 로그에 끌 수 없는 `status_change` 종류로 한 줄씩 남고(detail에 from·to), 거부된 전환은 로그를 남기지 않는다(D-50)"
    - "상태 고르기 목록이 쓸 코드표 목록(`listProjectStatusCatalog`)이 활성 값의 값·라벨·설명을 정렬 순으로 돌려준다 — 설명이 없는 값은 `description`이 null이다(D-93, S7·S14)"
  artifacts:
    - path: "domain/projects/status.ts"
      provides: "전환 함수 · 그 사람의 갈 곳 목록 · 상태 코드표 목록(라벨·설명)"
      exports: ["changeProjectStatus", "statusDestinations", "listProjectStatusCatalog"]
    - path: "test/unit/domain/project-status.test.ts"
      provides: "전환 × 주체(담당 PM·팀장·대표·시스템 관리자) × 선행 조건(시작일·종료일 유무) 결정표 + 갈 곳 목록 표"
      min_lines: 50
    - path: "test/integration/project-status.test.ts"
      provides: "04-06의 마이그레이션·잠금 케이스에 더해 실제 DB 전환·로그 한 줄·거부 무변경·PM 직접 호출 거부·동시 전환·대표/시스템 관리자 완료 증명"
      min_lines: 80
  key_links:
    - from: "domain/projects/status.ts"
      to: "domain/rules/gate.ts"
      via: "사람의 전환 넷이 `project.transition`·`project.start-date-required` 두 규칙을 한 진입점으로 지난다"
      pattern: "gate("
    - from: "domain/rules/register.ts"
      to: "domain/projects/status-transitions.ts"
      via: "전환 규칙이 04-06의 전이표 데이터를 읽는다 — 상태 문자열 비교가 규칙 안에만 있다"
      pattern: "ALLOWED_TRANSITIONS"
    - from: "domain/projects/status.ts"
      to: "domain/action-log/record.ts"
      via: "전환 성공 뒤 status_change 종류로 로그를 남긴다"
      pattern: "recordAction"
    - from: "app/(app)/projects/actions.ts"
      to: "domain/projects/status.ts"
      via: "changeProjectStatusAction이 zod enum을 지나 전환 함수 하나를 부른다 — 액션에 판정이 없다"
      pattern: "changeProjectStatus("
---

<objective>
**As a** 기획팀장·대표, **I want to** 수주중 프로젝트를 시작일과 함께 진행으로 올리거나 미수주로 닫고, 미수주를 다시 진행으로 살리고, 정산이 끝난 프로젝트를 완료로 잠그는 일을 서버가 규칙 하나로 판정하기를, **so that** PM이 상태를 임의로 바꾸는 일이 구조적으로 없고 Phase 5는 완료 전환의 호출자만 결재 승인으로 바꾸면 된다.

**2026-09-23 체커 1차 분할.** 옛 04-06(28파일)이 한 실행자 컨텍스트를 넘어 셋으로 나눴다. 이 플랜은 **가운데 조각 — 사람의 전환 넷의 서버 쪽**이다. 상태 다섯 값·코드표·전이표 데이터 모듈·완료 잠금 게이트 교체는 04-06이 이미 만들었고, 상태 바꾸기 화면·부제의 마지막 변경일·목록의 다섯 상태 표시·생애 E2E는 04-21이 이 플랜의 액션 위에 얹는다. 이 플랜에는 화면이 없다 — 증명은 단위 결정표와 실제 DB 통합 테스트다.

Purpose: ROADMAP 기준 3·4는 전환이 전부 `domain/rules.gate` 단일 진입점을 지나는 구조적 속성을 요구한다. D-79는 Phase 4의 완료 주체를 대표·시스템 관리자로 두고, Phase 5가 결재 승인을 호출자로 바꿀 때 전환 함수는 그대로라고 정했다.

Output: 권한표 메뉴 둘 + 시드 · 게이트 규칙 둘(`project.transition` · `project.start-date-required`) · 전환 함수·갈 곳 목록·코드표 목록 · 조건부 상태 갱신 리포지토리 함수 · 액션 · 단위 1(신규) · 통합 1(04-06 파일 확장).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@CLAUDE.md
# 범위 Read(큰 파일):
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 677–722행(§1 D-75 — 읽는 코드 경로 전부)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1227–1255행(S7 전이표·막힘 문구) · 366–378행(Copywriting 막힘·확인)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(사람의 전환)** 다. 프로브는 REQUIREMENTS.md의 옛 네 상태 문구로 돌았으므로(04-08 Task 3이 문구를 고친다) **수용 기준은 결정된 모델 D-44·D-46·D-50·D-75·D-79·D-82와 UI-SPEC S7 전이표로 끌어와** `must_haves.truths`에 적었다. S7의 화면 상태(loading·error 렌더·partial 렌더)와 backstop은 화면을 만드는 04-21이 옮겼다 — 이 플랜은 그 화면이 쓰는 서버 계약(게이트 문자열·갈 곳 목록·설명 목록)을 truths로 덮는다.

**계획 단계 가정(사용자에게 표면화):**
- **⚠ S7 진행 상태의 수동 전환 — 기본값 적용:** 진행에서 사람이 가는 곳이 없다(진행 → 미수주는 결정된 적 없다). 사용자가 「수주 뒤 계약이 깨지는 경우」를 원하면 04-06의 전이표에 한 줄(진행 → 미수주, `projects.status`)을 더하는 것으로 바뀐다.
- **권한표 기본값:** 「팀장 이상」(D-46)을 계급 순위가 아니라 새 메뉴 `projects.status`(쓰기)로, 「대표·시스템 관리자」(D-79)를 새 메뉴 `projects.complete`(쓰기)로 표현한다(04-02의 `projects.revenue` 선례). 시드는 팀장·본부 책임자·대표에게 `projects.status` 쓰기와 **`projects` 보기**를, 대표에게 `projects.complete` 쓰기를 켠다 — 지금 시드는 기획 PM에게만 `projects` 보기를 켜므로, 보기 없이 전환 권한만 주면 팀장이 프로젝트 화면에 닿지 못한다. 경영관리는 시드 계급이 아니라 관리자가 권한표에서 배정한다(04-02와 같은 결).
- **Phase 5 연결점(D-79):** 정산→완료의 호출자는 지금 `changeProjectStatusAction`(대표·시스템 관리자)이고 Phase 5가 결재 승인 경로로 바꾼다. 전환 함수·게이트는 그대로 둔다.
</probe_fallback>

<reversibility_notes>
- 권한표 새 메뉴 키 `projects.status`·`projects.complete` — **costly**: 관리자가 권한표에서 켜 둔 행이 이 키를 참조한다. 이름을 바꾸면 권한 행 데이터 이관이 따라온다.
- 전환 함수 서명 `changeProjectStatus(viewer, projectId, to, deps?)` — **reversible**: 호출자는 이 페이즈의 액션 하나와 Phase 5의 결재 승인 하나뿐이다.
</reversibility_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 팀장이 수주중 프로젝트를 시작일과 함께 진행으로(권한표 → 시드 → 게이트 두 규칙 → 전환 함수 → 조건부 UPDATE → 로그 → 액션 → 통합)</name>
  <files>domain/permissions/menus.ts, domain/seed/index.ts, domain/rules/register.ts, domain/projects/status.ts, repositories/projects.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, test/integration/project-status.test.ts, test/integration/leak-scan.test.ts</files>
  <reversibility rating="costly">권한표 메뉴 키 둘은 관리자가 켠 권한 행이 참조한다 — 이름을 바꾸면 권한 행 이관이 따라온다. 키 이름은 04-02 `projects.revenue` 선례를 따른 계획 재량이다.</reversibility>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-44·D-46·D-50·D-79·D-82 원문
    - `domain/permissions/menus.ts`(전문) — `projects.revenue` 메뉴를 더한 04-02 주석
    - `domain/permissions/roles.ts` 20–35행(시드 계급 id)
    - `domain/seed/index.ts` 120–180행(권한 시드 루프 — `DEFAULT_ROLE_ID`의 `projects` 보기·쓰기 선례 · 시스템 관리자 전 메뉴 루프)
    - `domain/rules/gate.ts`·`domain/rules/register.ts`(04-06 산출물 전문) — 등록 형태와 `project.line-edit`
    - `domain/projects/status-transitions.ts`(04-06 산출물 전문) — `PROJECT_STATUSES`·`ALLOWED_TRANSITIONS`
    - `domain/projects/index.ts` 210–290행(`findProject` — 행 범위 `scopeFor`와 DTO)
    - `repositories/projects.ts` 211–302행(단건 조회·삽입·갱신 형태)
    - `domain/action-log/record.ts` 1–75행(`status_change`가 끌 수 없는 종류라는 것)
    - `app/(app)/projects/actions.ts`·`actions.registry.ts`(전문) — 액션 규약과 등록
    - `test/integration/leak-scan.test.ts`(전문) — side-effect import 목록(새 모듈·액션 레지스트리가 빠지면 커버리지 테스트가 실패한다, 04-01 Rule 2 선례)
    - `test/integration/revenue-entries.test.ts` 1–60행(계급별 권한을 켜고 viewer를 만드는 통합 테스트 형태) · `test/integration/project-status.test.ts`(04-06 산출물)
  </read_first>
  <action>
① **권한표·시드(D-46·D-79).** `domain/permissions/menus.ts`에 `{ key: "projects.status", label: "프로젝트 상태 변경" }`·`{ key: "projects.complete", label: "프로젝트 완료" }`를 더한다(04-02 주석과 같은 결로 이유 한 줄). `domain/seed/index.ts`에 `role-team-lead`·`role-division-head`·`role-ceo`의 `projects` 보기 + `projects.status` 쓰기, `role-ceo`의 `projects.complete` 쓰기를 더한다(시스템 관리자는 기존 루프가 전 메뉴를 켠다 — 따로 더하지 않는다). 04-06이 바꾼 상태 상수는 건드리지 않는다.

② **게이트 규칙(`domain/rules/register.ts`).** (a) `project.transition` — ctx `{ from, to, actorMenus: { status: boolean; complete: boolean } }`: 04-06의 `ALLOWED_TRANSITIONS`에 없는 쌍이면 `이 상태로 바꿀 수 없습니다 · 새로 고쳐 주세요`, 있는데 그 쌍의 메뉴 권한이 없으면 `상태를 바꿀 권한이 없습니다`. (b) `project.start-date-required` — ctx `{ to, startDate }`: 목적지가 진행이고 시작일이 없으면 Copywriting 막힘 문구 그대로 `진행으로 바꿀 수 없음 — 시작일 없음 · 기간 적기`. 04-06의 `project.line-edit`는 건드리지 않는다.

③ **전환 함수(`domain/projects/status.ts` 신규).** `changeProjectStatus(viewer, projectId, to, deps?)`: 행 범위(`scopeFor`) 안에서 프로젝트를 찾고(없으면 찾을 수 없음) → `can()`으로 두 메뉴 권한을 읽어 `gate(project, "project.transition", …)` → `gate(project, "project.start-date-required", …)` → 거부면 `GateBlockedError(reason)` → 통과하면 리포지토리 조건부 갱신 → `recordAction(viewer, { actionType: "status_change", entity: "project", entityId, detail: { from, to } })`. 전이표 import는 `status-transitions.ts`에서만 하고 `register.ts`와 서로 import하지 않는다(`test/unit/import-cycles.test.ts`). **함수 본문에 상태 문자열 비교를 두지 않는다** — 호출자 구분도 두지 않는다(D-79: Phase 5가 같은 함수를 부른다).

④ **리포지토리(`repositories/projects.ts`).** `updateProjectStatusIfCurrent(viewer, id, { expectedStatus, status, fillEndDateFromStart: boolean }, tx?)` — `UPDATE … SET status = $status, version = version + 1, updated_at = now() [, end_date = coalesce(end_date, start_date)] WHERE id = $1 AND status = $expected RETURNING` — 0행이면 도메인이 `다른 사람이 먼저 상태를 바꿨습니다 · 새로 고쳐 주세요`로 거부한다. 목적지가 진행이면 종료일 채움을 같은 문장에서 한다(D-82).

⑤ **액션.** `app/(app)/projects/actions.ts`에 `changeProjectStatusAction`(zod: `projectId` · `to`는 `PROJECT_STATUSES` enum) → `changeProjectStatus` → `revalidatePath`. `actions.registry.ts`에 등록한다(D-38). `test/integration/leak-scan.test.ts`의 side-effect import에 새 상태 모듈과 `actions.registry`가 들어 있는지 확인하고 빠졌으면 더한다.

⑥ **통합(트레이서 증명, `test/integration/project-status.test.ts`에 describe 추가).** 팀장 viewer(권한 시드 반영)가 시작일만 있고 종료일이 빈 수주중 프로젝트를 진행으로 바꾸면 DB 재조회로 상태 `in_progress` · 종료일 = 시작일 · `status_change` 로그 정확히 한 줄(detail from·to)을 단언한다. 같은 프로젝트를 담당 PM viewer로 `changeProjectStatus`에 직접 넘기면 권한 거부이고 상태·로그가 그대로다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm vitest run --project unit test/unit/import-cycles.test.ts</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오거나 import 순환 테스트의 failed 개수가 1 이상이다 — 전환 함수와 게이트 등록이 서로 import한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 팀장의 진행 전환이 DB·로그로 이어지지 않았거나, PM이 통과했거나, 새 액션·모듈이 누수 스캔 커버리지에서 빠졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `menus.ts`에 `projects.status`·`projects.complete`가 있고 시드가 팀장·본부 책임자·대표에게 `projects` 보기와 `projects.status` 쓰기, 대표에게 `projects.complete` 쓰기를 켠다
    - `register.ts`에 `project.transition`·`project.start-date-required`가 등록되고 두 규칙이 `ALLOWED_TRANSITIONS`를 읽는다(상태 문자열 표를 규칙 안에 새로 만들지 않는다)
    - `changeProjectStatus` 본문에 상태 문자열 리터럴 비교와 호출자 분기가 없다(전이표와 게이트만 거친다)
    - 상태 갱신이 `WHERE status = 기대값` 조건부 UPDATE이고 진행 목적지에서 종료일 채움이 같은 문장이다
    - `changeProjectStatusAction`이 `actions.registry.ts`에 등록되고 누수 스캔 통합 테스트가 초록이다
    - 통합 테스트가 팀장의 진행 전환(상태·종료일·로그 한 줄)과 PM 직접 호출 거부(무변경)를 DB 재조회로 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>팀장이 수주중 프로젝트를 진행으로 바꾸는 한 경로가 권한표·게이트·조건부 갱신·로그·액션까지 서버에서 끝났고, PM은 액션을 직접 불러도 막힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 나머지 전환 셋 + 갈 곳 목록 + 상태 코드표 목록 + 결정표 + 대표·시스템 관리자 완료(D-79) 통합 증명</name>
  <files>domain/projects/status.ts, domain/rules/register.ts, test/unit/domain/project-status.test.ts, test/unit/domain/rules-gate.test.ts, test/integration/project-status.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1227–1255행(S7 전이표 — 미수주 → 진행은 모달 없이 즉시 · 정산 → 완료는 바로 확인 모달: 이 플랜은 그 화면이 쓰는 갈 곳 목록만 만든다)
    - `domain/code-tables/index.ts` 60–90행(`listCodeItems` — 04-10이 더한 `description`)
    - `test/unit/vendors/account-number-plan.test.ts`(결정표 단위 테스트 형태)
    - `test/unit/domain/rules-gate.test.ts`(04-06 갱신본 전문)
  </read_first>
  <behavior>
    - 수주중→진행: `projects.status` 쓰기 + 시작일 있음 → 통과, 종료일이 비면 시작일로 채워진다 · 시작일 없음 → 막힘 문구 · 담당 PM(권한 없음) → 권한 거부
    - 수주중→미수주: `projects.status` 쓰기면 기간 없이 통과
    - 미수주→진행: `projects.status` 쓰기 + 시작일 → 통과, 번호·줄이 그대로 · 시작일 없음 → 막힘 문구
    - 정산→완료: `projects.complete` 쓰기(대표·시스템 관리자)만 통과 · 팀장(`projects.status`만 있음) → 권한 거부 · 담당 PM → 권한 거부(D-79)
    - 진행→미수주 · 진행→수주중 · 진행→정산 · 정산→미수주 · 정산→진행(상태 바꾸기 경로) · 완료→진행 · 완료→미수주 → 전부 「이 상태로 바꿀 수 없습니다」
    - `statusDestinations`: 수주중 + 팀장 → [진행, 미수주] · 진행 + 누구든 → [] · 정산 + 대표 → [완료] · 정산 + 시스템 관리자 → [완료] · 정산 + 팀장 → [] · 완료 → [] · 미수주 + 팀장 → [진행] · 담당 PM → 어느 상태든 []
    - `listProjectStatusCatalog`: 활성 값 다섯을 정렬 순(수주중·진행·정산·완료·미수주)으로, 설명이 없으면 `description: null`
  </behavior>
  <action>
① `test/unit/domain/project-status.test.ts`에 위 `behavior`를 결정표로 먼저 쓴다(RED) — 권한은 `deps.can`으로 주입해 DB 없이 돈다. 주체 넷(담당 PM · 팀장 · 대표 · 시스템 관리자)을 표의 열로 둔다.

② `domain/projects/status.ts`에 `statusDestinations(viewer, project, deps?)`(전이표에서 `from`이 지금 상태이고 그 사람이 메뉴 권한을 가진 목적지 목록 — 진행→정산 쌍이 전이표에 없으므로 정산은 결과에 나올 수 없다)와 `listProjectStatusCatalog(viewer)`(코드표 `project_status`의 활성 값 `value`·`label`·`description`을 정렬 순으로)를 더한다. 나머지 전환 셋은 Task 1의 같은 함수가 전이표로 처리한다 — 전환별 분기를 더하지 않는다(GREEN).

③ `test/unit/domain/rules-gate.test.ts`에 `project.transition`·`project.start-date-required`의 경계(목록 밖 쌍 · 메뉴 권한 없음 · 시작일 없음)를 한 줄씩 더한다. 04-06이 바꾼 `project.line-edit` 케이스는 그대로 둔다.

④ `test/integration/project-status.test.ts`에 describe를 더한다: (a) 네 전환이 실제 DB에서 상태를 바꾸고 `status_change` 로그가 정확히 한 줄(detail from·to), (b) 수주중→진행에서 종료일이 비었으면 시작일로 채워졌다(DB 재조회 — Task 1 케이스를 미수주→진행에도), (c) 거부된 전환(권한 없음·목록 밖·시작일 없음)은 상태·로그 무변경(DB 재조회), (d) 담당 PM viewer로 `changeProjectStatus`를 직접 불러도 거부, (e) 동시 전환 두 개(`Promise.all`) 중 하나만 성공하고 로그 한 줄, (h) **D-79** — 정산 프로젝트를 대표 viewer와 시스템 관리자 viewer가 각각 완료로 바꾸면 통과(프로젝트 둘), 팀장·담당 PM viewer는 거부되고 상태가 정산 그대로다. 정산 상태는 테스트 준비 단계에서 DB에 직접 둔다(사람이 켜는 상태가 아니다 — 자동 전환은 04-11). (f)·(g)는 04-06이 이미 쓴 마이그레이션·잠금 케이스다 — 지우지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-status.test.ts test/unit/domain/rules-gate.test.ts test/unit/import-cycles.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 전이 결정표·갈 곳 목록·규칙 경계가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/quote-lines.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 전환·로그·동시성·완료 권한 중 하나가 DB에서 어긋났거나 04-06의 잠금·목록이 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 서버 전환이 기존 화면 흐름을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior`의 일곱 줄을 전부 덮고 주체 넷(담당 PM·팀장·대표·시스템 관리자)이 열로 있다
    - `statusDestinations`가 어떤 입력에서도 정산을 돌려주지 않는다(결정표에 진행 + 모든 주체 행이 있다)
    - `rules-gate.test.ts`에 새 두 규칙의 경계 케이스가 있고 04-06의 케이스 수가 줄지 않았다
    - 통합 테스트 (a)~(e)·(h)가 전부 있고 초록이다 — (c)·(e)·(h)의 거부 쪽은 DB 재조회로 단언한다
    - D-79: 대표·시스템 관리자의 완료 통과와 팀장·담당 PM의 거부가 통합 테스트에 각각 있고, `changeProjectStatus`에 호출자 구분 인자가 없다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>사람이 하는 전환 넷이 권한·시작일·동시성 규칙대로 서버에서 동작하고, 완료는 대표·시스템 관리자만 하며, 화면이 쓸 갈 곳 목록과 설명 목록이 준비됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 상태 전환 액션 | 목적지 상태가 넘어온다. 전이표·권한·시작일 판정이 서버에만 있어야 한다 |
| 권한표 → 전환 | 새 메뉴 두 개가 전환 주체를 정한다. 시드 기본값이 틀리면 PM이 상태를 바꾸거나 대표가 완료를 못 한다 |
| domain → 게이트 | 이후 페이즈(결재 승인 호출자·증빙 필수·마감)가 얹힐 단일 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-34 | Elevation of Privilege | `changeProjectStatus` | high | mitigate | 화면이 버튼을 보내지 않는 것(04-21)과 별개로 서버가 `can()`으로 `projects.status`/`projects.complete`를 다시 판정하고 행 범위(`scopeFor`) 밖 프로젝트는 찾을 수 없음으로 떨어뜨린다. 담당 PM viewer가 함수를 직접 부르는 거부 케이스를 통합 테스트(Task 1 ⑥ · (d))로 고정한다 |
| T-04-35 | Elevation of Privilege | 정산→완료 | high | mitigate | 완료는 `projects.complete` 한 메뉴로만 열리고 시드는 대표에게만 켠다(시스템 관리자는 전 메뉴 루프). 팀장·담당 PM의 완료 시도 거부를 결정표와 통합 테스트(h)로 고정한다 — 화면 쪽(팀장에게 버튼 없음)은 04-21 E2E(c) |
| T-04-36 | Tampering | 목적지 상태 입력 | high | mitigate | zod enum이 다섯 값 밖을 막고, 전이표에 없는 쌍(진행→정산 포함)은 게이트가 거부한다. 정산은 사람이 고를 수 있는 목적지가 아니다 |
| T-04-37 | Tampering | 동시 전환 | medium | mitigate | `WHERE status = 기대값` 조건부 UPDATE로 한 사람만 성공하고 로그도 한 줄이다(통합 테스트 e) |
| T-04-38 | Repudiation | 상태 변경 이력 | medium | mitigate | `status_change`는 끌 수 없는 종류라 설정으로 이력을 없앨 수 없다. 거부된 전환은 로그를 남기지 않는다(통합 테스트 c) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, import 순환 테스트 초록
- `CI=true pnpm test` 세 계층 통과
- `domain/rules/register.ts` 등록 목록: `project.transition` · `project.start-date-required`(04-06의 `project.line-edit`와 함께)
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 사람이 하는 전환 넷이 권한표·시작일·동시성 규칙대로 게이트를 지나고, 진행·완료에는 사람이 가는 곳이 없다
- Phase 4의 완료 주체가 대표·시스템 관리자뿐이고, Phase 5는 정산→완료의 호출자만 결재 승인으로 바꾸면 된다
- 04-21이 화면을 얹을 서버 계약(액션 · 갈 곳 목록 · 설명 목록 · 게이트 문자열)이 준비됐다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규) | `domain/projects/status.ts` — `changeProjectStatus` · `statusDestinations` · `listProjectStatusCatalog` |
| 함수(신규, repository) | `updateProjectStatusIfCurrent`(`repositories/projects.ts`) |
| 게이트 규칙(신규) | `project.transition` · `project.start-date-required` |
| 권한표 메뉴(신규) | `projects.status`(프로젝트 상태 변경) · `projects.complete`(프로젝트 완료) + 시드(팀장·본부 책임자·대표: `projects` 보기 · `projects.status` 쓰기 / 대표: `projects.complete` 쓰기) |
| Server Action | `changeProjectStatusAction`(등록 포함) |
| 테스트 | `test/unit/domain/project-status.test.ts`(신규) · `test/integration/project-status.test.ts`(04-06 파일에 전환 describe 추가) · `rules-gate.test.ts`(경계 추가) · `leak-scan.test.ts`(등록 확인) |
| 새 스키마·마이그레이션·화면 | 없음 — 화면은 04-21 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md` when done
</output>
