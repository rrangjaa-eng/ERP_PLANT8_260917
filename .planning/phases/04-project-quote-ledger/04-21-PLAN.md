---
phase: 04-project-quote-ledger
plan: 21
type: execute
wave: 9
depends_on: ["04-20", "04-09"]
files_modified:

  - repositories/action-log.ts
  - domain/projects/status.ts
  - domain/projects/index.ts
  - app/(app)/projects/status-display.ts
  - app/(app)/projects/page.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/status-change.tsx
  - app/(app)/projects/[id]/unsaved-edits.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/project-status.test.ts
  - test/integration/project-status.test.ts
  - test/e2e/project-lifecycle.spec.ts

autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "「상태 바꾸기」는 그 사람이 지금 상태에서 갈 곳이 하나라도 있을 때만 서버가 보낸다(04-20 `statusDestinations`) — 진행·완료에서는 누구에게도 없고, 권한 없는 사람에게는 비활성 버튼이 아니라 버튼 자체가 없다. 미수주에서는 갈 곳이 진행 하나라 「상태 바꾸기」 대신 2차 **`진행으로 되돌리기`**가 곧 동작이다(S3·S7, DR-7)"
    - "업무 범위가 `자기 팀`인 팀장이 **다른 팀** 프로젝트를 열면 「상태 바꾸기」·「진행으로 되돌리기」가 없다 — 같은 규칙(`actorCoversProjectTeam`)을 서버 전환·갈 곳 목록·버튼 표시가 같이 쓴다. 본부 책임자·대표는 팀과 무관하게 보인다(사용자 D11·D20, CEO OV-4)"
    - "갈 곳이 게이트에 막혀 있으면(시작일 없음) 확인 모달 1차가 **제출 전부터** `aria-disabled`(포커스가 가고 이유가 `aria-describedby`로 읽힌다)이고 1차 왼쪽에 서버가 `statusDestinations`의 `blockedReason`으로 보낸 `시작일 없음 · 기간 적기`가 그대로 보인다 — 즉시 경로(「진행으로 되돌리기」)면 그 버튼 옆에 같은 글자. 화면이 이유를 만들지 않는다(CEO A-09, S7, UI-SPEC rev 5 Copywriting `막힘 — 진행 전환`)"
    - "요청 중에는 모달 1차가 `…`(`ui/confirm-dialog`의 `primary.pending`)이고 2차·목록 행이 `aria-disabled`, `Esc`·가림막 클릭이 무시돼 두 번 제출되지 않는다(상단 2px 진행 바는 사용자 D17로 만들지 않는다). 상태가 그새 바뀌어 거부되면 서버 문자열 `상태가 {상태}로 바뀜 · 새로 고침`(04-20)이 1차 왼쪽 막힘 자리에 그대로 붙고 모달은 닫히지 않는다. 모달·목록이 닫히면 포커스는 트리거로, 전환 성공으로 트리거가 사라졌으면 머리 줄 제목으로 돌아온다 — 액션은 화면이 본 상태 `from`을 싣는다(CEO A-11·A-34, UI-SPEC rev 5 S7·S16)"
    - "갈 곳이 둘(수주중)이면 목록형 `ui/confirm-dialog`(제목 `상태 바꾸기` · 행마다 상태 라벨 + 코드표 설명 2행 — 설명이 없는 값은 2행이 없다, `—` 금지 · 2차 `취소 Esc` · 1차 없음)가 먼저 나오고 행을 고르면 확인 모달 · 하나(정산→완료)면 바로 확인 모달이다(S7, D-93, Copywriting `상태 고르기 목록(갈 곳 둘)`)"
    - "미수주의 「진행으로 되돌리기」는 **종료일이 오늘(KST)보다 이르거나 현재 차수가 고객 승인 전이면** 확인 모달 `진행으로 되돌리기`(결과 줄: 해당하는 것만 `종료일 지남 · 바로 정산` / `{n}차 고객 승인 전 · 진행부터 지출결의 멈춤`)을 거치고, 둘 다 아니면 모달 없이 즉시 전환한다(DR-7, UI-SPEC rev 5 S7 전이표 · Copywriting `확인 — 진행으로 바꾸기 · 진행으로 되돌리기`)"
    - "고르기 목록에 정산이 없다 — 진행→정산은 사람이 고르는 목적지가 아니다(D-76)"
    - "완료로 바꾸는 길은 정산 프로젝트를 여는 대표·시스템 관리자에게만 보인다 — 같은 정산 프로젝트를 팀장·담당 PM이 열면 「상태 바꾸기」가 없다(D-79)"
    - "막힘 이유는 게이트 문자열 그대로 1차 버튼 왼쪽(즉시 경로는 「진행으로 되돌리기」 옆)에 나오고 모달은 닫히지 않으며 토스트는 뜨지 않는다(ROADMAP 기준 3·4, S7 error, §7-6)"
    - "상세 머리 줄 부제에 현재 상태와 마지막 변경일(`진행 2026-09-18` 꼴)이 보이고 상태 이력 표·섹션은 없다 — 변경 기록이 없으면 등록일이다(D-50, S3)"
    - "상태 태그 색이 Color 매핑표대로다 — 수주중 muted · 진행 accent · 정산 warning · 완료 success · 미수주 muted, 목록 상태 열과 상세 태그가 같은 표를 쓴다(D-75, SYSTEM.md 개정 ⑤)"
    - "완료로 바꾸는 확인 모달은 제목·1차 `완료로 바꾸기` · 부제 `{번호} {프로젝트명}` · 결과 `견적 줄 잠김 · 새 지출결의 받지 않음 · 되돌리기 없음` · 2차 `취소 Esc`이고, 미수주로 닫기 모달은 제목·1차 `미수주로 닫기` · 부제 `{번호} {프로젝트명}` · 결과 `쌓인 비용이 팀 미수주 비용이 됨 · 진행으로 되돌리기 있음` · 2차 `취소 Esc`다(UI-SPEC rev 5 Copywriting `Destructive — 완료` · `Destructive — 미수주로 닫기`)"
    - "진행으로 바꾸기 확인 모달은 제목·1차 `진행으로 바꾸기` · 부제 `{번호} {프로젝트명}` · 결과 줄(해당하는 것만, 최대 셋): 종료일이 비면 `종료일 없음 · 시작일로 저장` · 저장될 종료일(비면 시작일)이 오늘보다 이르면 `종료일 지남 · 바로 정산` · 그 밖에 종료일이 있으면 `기간 {시작} ~ {종료} · {종료 + 1일} 정산` · 현재 차수가 승인 전이면 `{n}차 고객 승인 전 · 진행부터 지출결의 멈춤` — `종료일 지남` 줄의 3차 「기간 바꾸기」와 막힘 줄의 3차 「기간 적기」는 04-44가 더한다(기간 칸은 04-22 — UI-SPEC rev 5 Copywriting `확인 — 진행으로 바꾸기 · 진행으로 되돌리기`)"
    - "성공한 전환의 토스트는 **누른 1차(즉시 경로는 누른 2차) 라벨 · 번호**다 — `진행으로 바꾸기 · 26001` · `진행으로 되돌리기 · 26001` · `미수주로 닫기 · 26001` · `완료로 바꾸기 · 26001` — 머리 줄 태그·부제가 새 값이 된다(DR-21, Copywriting `SUCCESS — 등록 / 새 차수 / 상태 전환`)"
    - "`QuoteLedger`의 미저장 편집 수 N(1차 「일괄 저장 N」의 N)이 1 이상이면 「상태 바꾸기」·「진행으로 되돌리기」 **자체가** 비활성(`aria-disabled`, `reasonTone: block`) + 옆에 `저장 안 한 편집 {N}칸 · 먼저 일괄 저장` — 고르기 목록·확인 모달이 열리지 않고, 저장해 N이 0이 되면 다시 켜진다. 문구는 `unsavedEditsReason(N)` 한 함수이고 새 차수·고객 승인 모달(그룹 B 04-24)이 같은 함수를 쓴다(DR-6 · B-03 · ENG-D9, UI-SPEC rev 5 S3·S7 · Copywriting `막힘 — 미저장 편집`)"
    - "상태 전환의 고르기 목록·확인 모달은 전부 `ui/confirm-dialog`(04-46, 계약 2)다 — PC `--modal-w` 480 모달 / 폰(<700) 하단 시트, 포커스 가둠·`Esc` = 2차·트리거 복귀를 그 컴포넌트가 한다. 이 플랜은 모달 마크업(div·네이티브 dialog)을 따로 만들지 않는다(DR-12, UI-SPEC rev 5 S16)"
    - "폰(<700) 머리 줄은 제목 → 상태 태그 → 부제 → 버튼 줄(「상태 바꾸기」 또는 「진행으로 되돌리기」 · 2차 「더보기」) 순서다. 「더보기」(`aria-expanded` · `aria-controls`)는 복사·차수 작업 묶음(`HeaderCopyActions`)에 버튼이 하나라도 있을 때만 렌더되고, 누르면 바로 아래 한 줄로 펼친다(시트 아님 · 새 컴포넌트 없음) — 묶음의 버튼(「복사해 새 차수」·「프로젝트 복사」)은 그룹 B(04-24·04-15)가 이 묶음 안에 둔다. PC(≥700)에는 「더보기」가 없고 버튼이 전부 보인다(DR-26, UI-SPEC rev 5 S3 폰 첫 화면)"
    - "목록 상태 필터와 상태 열이 코드표 라벨과 `status-display.ts`의 색을 쓰고, `app/`·`domain/`·`repositories/` 코드에 옛 네 상태 모델의 잠금 값 리터럴이 남지 않는다(D-75)"
    - "생애 E2E는 계급 권한·정보 노출을 **손으로 켜지 않는다** — 04-20 시드만으로 같은 팀 팀장이 상세 머리 줄의 번호·프로젝트명과 「상태 바꾸기」를 보고, 대표가 정산 프로젝트를 완료로 바꾼다(ENG-D2 — 테스트가 시드 빈틈을 가리지 않는다)"
    - "같은 트랜잭션에서 `status_change` 두 줄(자동 정산 시스템 + 사람 전환)이 같은 시각으로 남아도 부제의 마지막 변경일은 나중에 쓴 줄(`seq`가 큰 줄)에서 온다 — 최신 한 줄 조회가 `occurred_at` 다음에 `seq`로 정렬한다(엔지 리뷰 A 테스트 공백 6)"
    - "진행에서는 누구에게도 상태 전환 버튼이 없다 — 행사가 일찍 끝나면 팀장이 종료일을 당긴다(해소 CEO-D13, UI-SPEC rev 5 `## UI Considerations` S7)"
    - statement: "상태 전환 모달·고르기 목록의 문자열이 고정 문구와 짧은 보간값(번호·프로젝트명·날짜·차수)뿐이라 `--modal-w` 480(PC)과 폰 시트 안에서 잘리지 않는다 — 가장 긴 조합(진행으로 바꾸기 결과 줄 셋 + 40자 프로젝트명 부제)으로 1280 · 1024 · 375 DOM 감사"
      verification: backstop
    - statement: "「상태 바꾸기」가 더해진 상세 머리 줄이 1280 · 1024에서 가로 스크롤을 만들지 않고 버튼이 하나도 숨지 않으며(U-1), 375에서는 가로 스크롤 0 · 폰 순서(제목 → 태그 → 부제 → 버튼 줄)이고 복사·차수 버튼이 있으면 「더보기」 뒤로 접힌다(DR-26) — DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "상태 변경 행동 로그가 직원 감시·성과 집계 도구가 되어서는 안 된다. 「누가 며칠 만에 수주로 바꿨는가」를 사람별로 집계해 보여주는 화면·내보내기를 이 페이즈가 만들지 않는다 — D-50이 상세에 「현재 상태 + 마지막 변경일」만 두고 새 표·새 섹션을 금지한 것은 그 집계 화면이 생기는 것을 막기 위해서다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/(app)/projects/[id]/status-change.tsx"
      provides: "상태 바꾸기 · 진행으로 되돌리기 — `ui/confirm-dialog`로 갈 곳 하나면 확인, 둘이면 목록형 뒤 확인, 미수주→진행은 조건부 확인(DR-7). 게이트 이유 그대로 · 미저장 편집이면 트리거 비활성(DR-6)"
      contains: "ConfirmDialog"
    - path: "app/(app)/projects/[id]/unsaved-edits.ts"
      provides: "`저장 안 한 편집 N칸 · 먼저 일괄 저장` 한 함수(상태 전환 트리거 · 그룹 B의 새 차수·고객 승인 모달 공용)"
      exports: ["unsavedEditsReason"]
    - path: "app/(app)/projects/status-display.ts"
      provides: "상태 → 태그 색 매핑 한 곳(목록·상세 공용)"
      exports: ["PROJECT_STATUS_TAG_KIND"]
    - path: "domain/projects/status.ts"
      provides: "04-20 함수에 더해 마지막 상태 변경일(없으면 등록일) — 정리(prune)된 로그 행도 포함해 읽는다(CEO A-30)"
      exports: ["lastStatusChangeOn"]
    - path: "test/e2e/project-lifecycle.spec.ts"
      provides: "수주중→진행(팀장) · 시작일 막힘 · 미수주 왕복(조건부 모달 · 즉시) · 정산→완료(대표) · PM·진행에게 버튼 없음 · 완료 뒤 줄 잠김 · 미저장 편집 게이트 · 폰 머리 줄"
      min_lines: 60
  key_links:
    - from: "app/(app)/projects/[id]/status-change.tsx"
      to: "domain/rules/gate.ts"
      via: "막힘 문구가 게이트가 돌려준 이유 문자열 그대로다(액션 serverError · `blockedReason`)"
      pattern: "serverError"
    - from: "app/(app)/projects/[id]/status-change.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "고르기 목록(목록형 `options`)·확인 모달(`resultLines` · `primary.disabledReason`)이 04-46의 공용 컴포넌트다(DR-12)"
      pattern: "ConfirmDialog"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/unsaved-edits.ts"
      via: "`QuoteLedger`의 `dirtyCount`로 상태 전환 트리거를 막는 문구가 한 함수에서 온다(DR-6)"
      pattern: "unsavedEditsReason"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/projects/status.ts"
      via: "갈 곳 목록이 비면 상태 바꾸기를 렌더하지 않는다 — 화면이 상태 문자열로 판단하지 않는다"
      pattern: "statusDestinations"
    - from: "app/(app)/projects/projects-table.tsx"
      to: "app/(app)/projects/status-display.ts"
      via: "목록 상태 열이 상세와 같은 색 매핑을 쓴다"
      pattern: "PROJECT_STATUS_TAG_KIND"
    - from: "domain/projects/status.ts"
      to: "repositories/action-log.ts"
      via: "부제의 날짜가 최신 status_change 로그 한 줄에서 온다(정리 표시 행 포함)"
      pattern: "findLatestActionFor"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/status-change.tsx"
      via: "`QuoteLedger`가 `statusChange: StatusChangeProps | null` 데이터 props를 받아 상태 바꾸기를 직접 렌더한다 — ReactNode를 받지 않는다(CEO A-31, 04-44가 같은 props를 넓힌다 — `periodRights`·`onOpenPeriodField`)"
      pattern: "StatusChangeProps"
---

<objective>
**As a** 기획팀장·대표, **I want to** 프로젝트 상세에서 「상태 바꾸기」 하나로 수주중을 진행이나 미수주로 옮기고, 미수주를 되살리고, 정산을 완료로 닫으며, 목록과 상세가 같은 색으로 다섯 상태를 보이기를, **so that** 04-20의 서버 규칙이 화면에서 그대로 읽히고 PM은 상태를 바꾸는 버튼을 볼 일조차 없다.

**2026-09-23 체커 1차 분할.** 옛 04-06(28파일)을 셋으로 나눈 **마지막 조각 — 화면**이다. 04-06이 상태 다섯 값과 완료 잠금 게이트를, 04-20이 사람의 전환 넷의 서버 쪽(권한표 메뉴 · 게이트 · 전환 함수 · 갈 곳 목록 · 코드표 목록 · 액션)을 만들었다. 이 플랜은 그 위에 상태 바꾸기 화면 · 상세 부제의 마지막 변경일(D-50) · 목록의 다섯 상태 표시 · 생애 E2E를 얹는다. 자동 정산과 종료일 지남은 04-11, 기간 칸은 04-22, 「기간 적기」·「기간 바꾸기」 모달 연결은 04-44다.

Purpose: ROADMAP 기준 3·4의 「막힘은 게이트 문자열 그대로」가 화면에서 증명돼야 하고, 04-11·04-22·04-44·04-17·04-18이 이 플랜의 상태 표시(색 매핑·부제·상태 바꾸기 컴포넌트)를 넓힌다.

Output: `status-display.ts` · `status-change.tsx` · 부제의 마지막 변경일(`lastStatusChangeOn` + `findLatestActionFor`) · 목록 상태 필터·열 · 옛 값 리터럴 스캔 · 통합 1 확장 · E2E 1.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE).** (1) A-09: 막힌 갈 곳의 이유를 서버(`statusDestinations`의 `blockedReason`)가 보내고 화면은 그대로 쓴다. (2) A-11: 액션에 화면이 본 `from`을 싣는다. (3) A-31: 상태 바꾸기는 `QuoteLedger`가 데이터 props(`StatusChangeProps`)로 받아 직접 렌더한다 — 04-44가 ReactNode를 props로 다시 뜯어고치지 않게 지금 정한다. (4) A-34: 제출 중 비활성·상태 변경 거부 때 새로 고침 안내·포커스 복귀. (5) A-30: 부제 날짜는 정리된 로그 행도 읽는다. (6) A-35: 옛 값 스캔은 따옴표로 감싼 리터럴만 잡는다. (7) A-18: E2E의 날짜는 `lib/kst-date`(04-29)의 `kstToday(new Date())`에서 더해 만들고 고정 날짜·「오늘」 경계를 E2E에 두지 않는다. (8) 사용자 D11·D20(OV-4): 다른 팀 팀장에게 버튼이 없음을 E2E로 고정한다. (9) OV-7: 04-09가 `quote-table.tsx`·`projects-table.tsx`를 먼저 고치므로 04-09 뒤(웨이브 8)로 옮겼다.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`).** (10) **ENG-D2:** 옛 E2E 선례(`revenue-section.spec.ts:18`)처럼 테스트가 노출·권한을 손으로 켜면 시드의 빈틈(팀장·대표 노출 행 없음)이 가려진다 — 이 플랜의 E2E는 계급 권한·노출을 켜는 호출을 하나도 두지 않고, 같은 팀 팀장이 머리 줄의 번호·이름과 「상태 바꾸기」를 보는 것을 단언한다(시드는 04-20). (11) **엔지 리뷰 A 테스트 공백 6:** 같은 트랜잭션의 두 `status_change`(04-11 자동 정산 + 사람 전환)가 같은 `occurred_at`을 가지므로 `findLatestActionFor`가 `seq`로도 정렬하고 통합 (i2)로 고정한다. 04-11이 잠금 안에서 직전 변경일을 읽으므로 `findLatestActionFor`는 선택 `tx`를 받는다(ARCHITECTURE §4-8 — 잠근 트랜잭션 안 풀 호출 금지).

**2026-09-23 디자인 리뷰 반영(`docs/designs/plant8-erp-phase4-design-review-260923.md`, UI-SPEC rev 5).** (12) **DR-12:** 상태 전환의 고르기 목록·확인 모달을 전부 04-46의 `ui/confirm-dialog`로 — 이 플랜은 네이티브 dialog 마크업을 따로 만들지 않는다(포커스 가둠·`Esc`·트리거 복귀·폰 시트를 컴포넌트가 한다). 04-46은 이미 이 플랜의 조상이다(04-21 → 04-09 → 04-28 → 04-46) — `depends_on`을 바꾸지 않는다. (13) **DR-7:** 미수주 → 진행은 2차 `진행으로 되돌리기` — 종료일 지남 또는 승인 전이면 확인 모달(결과 줄에 자동 정산·지출결의 멈춤), 나머지는 즉시. (14) **DR-6:** 미저장 편집이 있으면 「상태 바꾸기」·「진행으로 되돌리기」 자체가 `aria-disabled` + `저장 안 한 편집 N칸 · 먼저 일괄 저장`(`unsavedEditsReason` — 그룹 B 04-24가 재사용). 저장 쪽 `seenStatus` 거부는 04-22(계약 4). (15) **DR-21 · P0:** 토스트 = 누른 버튼 라벨 · 번호, 모달 결과 줄은 rev 5 Copywriting 명사형, 상태 바뀜 거부는 서버 문자열 `상태가 … 바뀜 · 새로 고침`만(옛 A-34의 `새로 고쳐 주세요` 링크 버튼은 rev 5가 지웠다). (16) **DR-26:** 폰 머리 줄 순서와 복사·차수 작업 묶음의 「더보기」 자리(버튼은 그룹 B가 넣는다). (17) **DR-14:** DOM 감사 폭 1280 · 1024 · 375.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다(예: 시드만으로 팀장이 상세를 못 보면 04-20의 시드를 고치고 통합 케이스를 더한다 — E2E에서 노출을 켜서 넘기지 않는다).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-20-SUMMARY.md
@CLAUDE.md

# 범위 Read(큰 파일):

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5 — rev 4 줄 번호는 쓰지 않는다) `## UI Considerations` 「S3」 490–500행 · 「S7」 544–552행 · 「S16」 617–627행 · `## 적용 규칙` 「S3」 1118–1155행(부제 1120–1123 · 미저장 편집 1142–1143 · 폰 첫 화면 1151–1155) · 「S7」 1317–1348행 · 「S16」 1493–1527행 · `## Color` 「상태 → 색 매핑」 289–300행 · `## Copywriting Contract` 2차 버튼 343행 · `막힘 — 진행 전환` 385행 · `막힘 — 미저장 편집` 390행 · `막힘 — 상태 전환` 395행 · `SUCCESS` 415행 · Destructive·확인·상태 고르기 425–428행

# @.planning/phases/04-project-quote-ledger/04-46-SUMMARY.md — `ui/confirm-dialog`·`Button` 개정의 실제 props(계약 1·2)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(화면 부분)** 다. 수용 기준은 D-44·D-50·D-75·D-79·D-93과 UI-SPEC S3·S7로 끌어와 `must_haves.truths`에 적었다. rev 5 `## UI Considerations`의 S7 전 행(loading · error · long-text backstop · partial 갈 곳의 수 · partial 미저장 편집 · 해소 CEO-D13)과 S3의 이 플랜 몫(partial 상태별 머리 줄 컨트롤 · interactive 미저장 편집 · 폰 첫 화면 backstop · overflow)을 truths로 옮겼다 — S3 error(상태 바뀜 저장 거부)는 04-22, S3 loading(저장 중 잠금)은 04-30, 부제의 `기간 …` 항목은 04-22, `총 매출 예상가 …` 항목과 폰 부제 순서(DR-26)는 04-44가 옮긴다. 서버 쪽 가정(⚠ S7 진행 수동 전환 없음 · 권한표 기본값 · Phase 5 연결점)은 04-20의 `<probe_fallback>`에 있다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 팀장이 상세에서 수주중 프로젝트를 진행으로(갈 곳 목록 → 상태 바꾸기 → 목록형 확인 모달 → 확인 모달 → 액션 → 태그·토스트 → E2E)</name>
  <files>app/(app)/projects/status-display.ts, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/project-lifecycle.spec.ts</files>
  <read_first>
    - `domain/projects/status.ts`(04-20 산출물 전문) — `statusDestinations`(`{ to, blockedReason }`)·`listProjectStatusCatalog`·`actorCoversProjectTeam`
    - `app/(app)/projects/actions.ts`의 `changeProjectStatusAction`(04-20 — 입력 `{ projectId, from, to }`)
    - `ui/confirm-dialog/ConfirmDialog.tsx`(04-46 산출물 전문 — 계약 2: `open`·`onClose`·`title`·`subtitle`·`resultLines`·`primary: { label, onConfirm, pending, disabledReason }`·`secondaryLabel`·목록형 `options`, 포커스·`Esc`·트리거 복귀) · `ui/button/Button.tsx`(04-46 개정 — `disabledReason`이 있으면 `aria-disabled` + `aria-describedby`, `reasonTone`)
    - `lib/kst-date.ts`(04-29) — E2E 날짜 준비와 결과 줄 날짜에 쓰는 `kstToday`·`addDays`
    - `app/(app)/projects/[id]/page.tsx`(전문) · `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `StatusTag`·`styles.header`·`QuoteLedger`의 props 선언을 찾아 범위만 Read
    - `ui/status-tag/StatusTag.tsx`(전문)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## 적용 규칙` 「S7. 상태 태그와 상태 전환」 1317–1348행 · 「S16. 확인 모달」 1493–1527행 · `## Copywriting Contract` `SUCCESS — 등록 / 새 차수 / 상태 전환` 415행 · `확인 — 진행으로 바꾸기 · 진행으로 되돌리기` 427행 · `상태 고르기 목록(갈 곳 둘)` 428행 · `## Color` 「상태 → 색 매핑」 289–300행
    - `test/e2e/fixtures.ts` · `test/e2e/revenue-section.spec.ts` 1–60행(두 역할로 로그인하는 형태만 참고 — 그 파일 18행처럼 노출·권한을 손으로 켜는 방식은 **따르지 않는다**, ENG-D2)
  </read_first>
  <action>
① `app/(app)/projects/status-display.ts`: `PROJECT_STATUS_TAG_KIND`(수주중 muted · 진행 accent · 정산 warning · 완료 success · 미수주 muted — UI-SPEC rev 5 Color 매핑표). 새 색을 만들지 않는다 — `StatusTag`의 기존 kind만 쓴다.

② `app/(app)/projects/[id]/status-change.tsx`(클라이언트): **데이터 props 타입 `StatusChangeProps`를 이 파일에서 내보낸다(A-31)** — `projectId` · `projectNumber`(토스트) · `projectLabel`(`{번호} {프로젝트명}` — 부제) · `from`(서버가 읽은 현재 상태) · `destinations`(`value`·`label`·`description`·`blockedReason: string | null`) · `startDate`·`endDate` · `settleOn`(종료일 + 1일 KST 날짜 문자열, 종료일이 없으면 null) · `endDateBeforeToday`(저장될 종료일 — 비면 시작일 — 이 오늘(KST)보다 이른가, 서버 계산) · `currentRevisionSeq`·`currentRevisionApproved`. 04-44가 기간 관련 필드(`periodRights`·`onOpenPeriodField`)를 이 타입에 더한다. **모든 모달은 `ui/confirm-dialog`(DR-12)** — 이 파일에 모달 마크업을 만들지 않는다. 2차 「상태 바꾸기」 → 갈 곳이 둘이면 목록형 `ConfirmDialog`(제목 `상태 바꾸기`, `options` = 갈 곳마다 라벨 + 설명 2행, 설명이 없으면 2행 없음, 2차 `취소 Esc`) → 진행을 고르면 확인 `ConfirmDialog`(제목·1차 `진행으로 바꾸기`, 부제 `projectLabel`, `resultLines` = `<must_haves>`의 진행으로 바꾸기 결과 줄 규칙 — `종료일 없음 · 시작일로 저장` / `종료일 지남 · 바로 정산` / `기간 {시작} ~ {종료} · {settleOn} 정산` / `{n}차 고객 승인 전 · 진행부터 지출결의 멈춤` 중 해당하는 것만, 2차 기본 `취소 Esc`). **고른 갈 곳의 `blockedReason`이 있으면 `primary.disabledReason`으로 넘긴다** — 1차가 제출 전부터 `aria-disabled`이고 그 문자열이 1차 왼쪽에 그대로 보인다(A-09 — 화면이 이유를 만들지 않는다). 액션은 `{ projectId, from, to }`로 부른다(A-11). 서버 거부는 액션 `serverError` 문자열을 막힘 자리에 그대로 쓰고 모달을 닫지 않는다(토스트 없음) — 상태가 그새 바뀐 거부는 서버 문자열이 이미 `상태가 {상태}로 바뀜 · 새로 고침`(04-20)이라 화면이 버튼·안내를 덧붙이지 않는다(P0 · rev 5 S7). 요청 중에는 `primary.pending`을 참으로 둔다 — 2차·목록 행 `aria-disabled`와 `Esc` 무시는 컴포넌트가 한다(두 번 제출 금지, A-34 — 상단 2px 진행 바는 사용자 D17로 만들지 않는다). 닫히면 포커스 복귀도 컴포넌트가 한다(트리거가 사라졌으면 머리 줄 제목, S16). 성공은 모달을 닫고 토스트 `진행으로 바꾸기 · {projectNumber}`(DR-21 — 누른 1차 라벨). 이 태스크는 **수주중→진행 한 경로**만 끝까지 만든다(나머지 갈래는 Task 2).

③ `[id]/page.tsx`는 `statusDestinations`가 빈 목록이면 `statusChange: null`을 넘기고(컴포넌트가 렌더되지 않는다), 아니면 `listProjectStatusCatalog`로 갈 곳의 라벨·설명을 채우고 `lib/kst-date`로 `settleOn`·`endDateBeforeToday`를 계산한 `StatusChangeProps`를 만들어 `QuoteLedger`에 새 prop `statusChange`와 `statusLabel`·`statusTagKind`를 넘긴다(A-31 — ReactNode prop을 만들지 않는다). `quote-table.tsx`는 머리 줄의 muted 고정 `StatusTag`를 받은 kind로 바꾸고 `statusChange`가 있으면 1차 버튼 앞에 `<StatusChange {...statusChange} />`를 렌더한다(그 밖의 QuoteLedger 코드는 건드리지 않는다).

④ **E2E(트레이서 증명).** `test/e2e/project-lifecycle.spec.ts`: 날짜는 전부 `kstToday(new Date())`에 `addDays`로 더해 만든다(A-18 — 고정 날짜 금지, 시작일 = 오늘 + 7). **계급 권한·정보 노출을 켜는 호출(`upsertVisibility`·`upsertPermission`·`setPermissionCell`·`setVisibilityCell`)을 이 스펙에 두지 않는다(ENG-D2)** — 준비 단계가 만드는 것은 사람·발령 이력·프로젝트 행뿐이다. 기획 PM이 시작일만 적어 프로젝트를 등록 → 그 팀에 오늘 발령된 팀장(`role-team-lead` 픽스처)으로 로그인해 상세를 열면 머리 줄에 번호·프로젝트명이 보이고 「상태 바꾸기」 → 목록형 모달(`getByRole("dialog")`)에 `진행`·`미수주` 두 행(정산 없음)과 각 설명 한 줄 → 진행 → 확인 모달 결과 줄에 `종료일 없음 · 시작일로 저장`과 `1차 고객 승인 전 · 진행부터 지출결의 멈춤`이 보이고 → 1차 `진행으로 바꾸기`를 눌러 상태 태그가 `진행`, 토스트 `진행으로 바꾸기 · {번호}`. 같은 스펙에서 기획 PM으로 같은 상세를 열면 「상태 바꾸기」가 DOM에 없다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오고 종료 코드가 0이 아니다 — stylelint가 색 리터럴을 잡으면 토큰 밖 값을 썼다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/project-lifecycle.spec.ts test/e2e/project-register.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 팀장의 진행 전환이 태그·토스트로 이어지지 않았거나 PM에게 버튼이 보이거나 고르기 목록에 정산이 있거나 결과 줄·토스트 문구가 rev 5 원문과 다르다</fails_when>
  </verify>
  <acceptance_criteria>
    - `status-change.tsx`가 `ConfirmDialog`를 import하고 이 파일에 `<dialog`·`showModal` 문자열이 없다(`grep -cE "<dialog|showModal" 'app/(app)/projects/[id]/status-change.tsx'` = 0 — DR-12)
    - `status-change.tsx`가 액션 `serverError`와 `blockedReason`을 가공 없이 막힘 자리(`primary.disabledReason`)에 쓰고, 거부 때 모달을 닫지 않으며 토스트를 부르지 않는다
    - `StatusChangeProps` 타입이 `status-change.tsx`에서 export되고 `QuoteLedger` props에 ReactNode 형 상태 컨트롤이 없다(A-31)
    - 액션 호출에 `from`이 실린다(A-11) · 요청 중 `primary.pending`이 참이다(A-34)
    - E2E 파일에 `\d{4}-\d{2}-\d{2}` 꼴 날짜 리터럴이 없다(A-18 — `grep -nE "'20[0-9]{2}-[0-9]{2}-[0-9]{2}'" test/e2e/project-lifecycle.spec.ts` 0건)
    - `[id]/page.tsx`가 갈 곳 목록이 비면 `status-change.tsx`를 렌더하지 않는다(비활성 버튼 렌더가 아니다)
    - 머리 줄 태그 kind가 `PROJECT_STATUS_TAG_KIND`에서 온다
    - E2E가 팀장의 진행 전환(태그 · 토스트 `진행으로 바꾸기 · {번호}`) · 결과 줄 `종료일 없음 · 시작일로 저장` · 고르기 목록에 정산 없음 · PM 화면의 버튼 부재와 팀장 화면 머리 줄의 번호·프로젝트명을 단언한다
    - `grep -cE "upsertVisibility|upsertPermission|setPermissionCell|setVisibilityCell" test/e2e/project-lifecycle.spec.ts`가 0이다(ENG-D2)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>팀장이 상세에서 수주중 프로젝트를 진행으로 바꾸는 한 경로가 갈 곳 목록·공용 확인 모달·액션·태그·토스트·E2E까지 끝났고, PM에게는 버튼이 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 나머지 갈래(미수주로 닫기 · 완료로 바꾸기 · 진행으로 되돌리기 — DR-7) + 고르기 목록 설명 + 부제의 마지막 변경일 + 목록의 다섯 상태 표시 + 옛 값 리터럴 스캔</name>
  <files>repositories/action-log.ts, domain/projects/status.ts, domain/projects/index.ts, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, test/unit/domain/project-status.test.ts, test/integration/project-status.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## Copywriting Contract` `Destructive — 미수주로 닫기` · `Destructive — 완료` · `확인 — 진행으로 바꾸기 · 진행으로 되돌리기` 425–427행 · `비파괴 — 복원 / 버림 / 복사` 430행(되돌리기 즉시 경로의 시작일 게이트) · `## 적용 규칙` 「S7」 전이표 미수주 행 1331행 · 「S3」 부제 1120–1123행(`정산 2026-09-17`의 뜻 = 현재 상태 + 마지막 변경일)
    - `repositories/action-log.ts`(전문) — `queryActionLog`·`filterActionLog`의 조건 형태(최근 한 줄 조회를 같은 결로 더한다)
    - `domain/projects/index.ts` 210–290행(`findProject` DTO — 등록일 필드 유무 확인)
    - `app/(app)/projects/page.tsx` 20–70행(`STATUS_OPTIONS` 상수) · `app/(app)/projects/projects-table.tsx`(전문 — 로컬 상태 라벨·태그 맵)
    - `test/unit/domain/project-status.test.ts`(04-20 산출물) — 마지막 describe로 스캔을 더할 자리
    - `app/(app)/projects/[id]/status-change.tsx`(Task 1 산출물)
  </read_first>
  <behavior>
    - `lastStatusChangeOn`: 최신 `status_change` 로그가 2026-09-18 KST면 `2026-09-18` · 로그가 없으면 등록일(`created_at`)의 KST 날짜
    - 코드베이스 스캔: `app/`·`domain/`·`repositories/`의 `.ts`·`.tsx`에 옛 잠금 상태 값의 **따옴표로 감싼** 문자열 리터럴(작은따옴표·큰따옴표·백틱)이 없다 — `settledAt` 같은 식별자·주석 속 낱말은 걸리지 않는다(마이그레이션 파일은 스캔하지 않는다, CEO A-35)
    - `lastStatusChangeOn`: 최신 `status_change` 로그 행이 정리(prune) 표시된 행이어도 그 날짜를 쓴다(CEO A-30)
    - `lastStatusChangeOn`: 한 트랜잭션에서 `status_change` 두 줄(시스템 자동 정산 → 사람 전환)을 남기면 `seq`가 큰 줄(사람 전환)의 detail·날짜를 쓴다 — 두 줄의 `occurred_at`이 같아도 결과가 흔들리지 않는다(엔지 리뷰 A 공백 6)
    - 미수주 → 2차 `진행으로 되돌리기`: 종료일이 오늘보다 이르거나 현재 차수가 승인 전이면 확인 모달, 둘 다 아니면 즉시 액션(DR-7) · 정산→완료는 목록 없이 바로 확인 모달이다
  </behavior>
  <action>
① `test/unit/domain/project-status.test.ts`에 마지막 describe로 옛 잠금 값 리터럴 스캔을 먼저 쓴다(RED — 지금 `page.tsx`·`projects-table.tsx`에 남아 있다). 정규식은 따옴표 세 종류로 감싼 값만 잡는다(CEO A-35 — 앞뒤에 `['"\`]`) — 단어 경계 방식은 뒤에 생길 식별자·주석에 헛걸린다.

② `status-change.tsx`에 나머지 갈래를 더한다 — 전부 `ConfirmDialog`: **미수주로 닫기**(제목·1차 `미수주로 닫기` · 부제 `projectLabel` · 결과 `쌓인 비용이 팀 미수주 비용이 됨 · 진행으로 되돌리기 있음` · 2차 `취소 Esc`) · **완료로 바꾸기**(제목·1차 `완료로 바꾸기` · 부제 · 결과 `견적 줄 잠김 · 새 지출결의 받지 않음 · 되돌리기 없음` · 2차 `취소 Esc` — 갈 곳이 하나라 목록 없이 바로, 대표·시스템 관리자에게만 갈 곳이 생긴다, D-79) · **진행으로 되돌리기(DR-7)** — 미수주에서는 「상태 바꾸기」 대신 2차 `진행으로 되돌리기`를 렌더한다(갈 곳이 진행 하나라 버튼이 곧 동작). `endDateBeforeToday`가 참이거나 `currentRevisionApproved`가 거짓이면 확인 `ConfirmDialog`(제목·1차 `진행으로 되돌리기` · 부제 · 결과 줄 해당하는 것만 `종료일 지남 · 바로 정산` / `{n}차 고객 승인 전 · 진행부터 지출결의 멈춤` · 2차 `취소 Esc` · 막히면 `primary.disabledReason` = `blockedReason`), 둘 다 아니면 모달 없이 즉시 액션을 부른다 — 즉시 경로도 시작일 게이트는 지나므로 `blockedReason`이 있으면 「진행으로 되돌리기」 버튼 자체가 `disabledReason`(`시작일 없음 · 기간 적기`)으로 `aria-disabled`다(rev 5 `비파괴` 행). 고르기 목록 행의 설명은 `listProjectStatusCatalog`의 `description`이고 없으면 그 행에 2행이 없다(S14 — `—` 금지). 성공 토스트는 누른 버튼 라벨 · 번호 — `미수주로 닫기 · {번호}` / `완료로 바꾸기 · {번호}` / `진행으로 되돌리기 · {번호}`(DR-21).

③ **부제의 마지막 변경일(D-50).** `repositories/action-log.ts`에 `findLatestActionFor(viewer, { entity, entityId, actionType }, tx?)`(최신 한 줄 — 정렬 `occurred_at DESC, seq DESC`, 선택 `tx`는 04-11이 잠근 트랜잭션 안에서 직전 변경일을 읽을 때 넘긴다)를 더하고 — **정리(prune)로 표시된 행을 거르지 않는다**(CEO A-30: 정리 작업이 `status_change` 행에 표시를 남길 수 있고, 거르면 부제가 조용히 등록일로 돌아간다 — 같은 파일 85–101행의 정리 경로를 읽고 조건을 맞춘다) — `domain/projects/status.ts`에 `lastStatusChangeOn(viewer, project)` — 최신 `status_change` 로그의 날짜(KST 날짜 문자열), 없으면 등록일(`created_at`의 KST 날짜). `findProject` DTO에 등록일이 없으면 `domain/projects/index.ts`에서 더한다(정보 항목은 기존 프로젝트 항목 — 새 항목을 만들지 않는다). `[id]/page.tsx`가 부제를 `{번호} · 상세 견적 {n}차 · {상태 라벨} {날짜}`로 만들어 `QuoteLedger`에 넘긴다(자동 정산의 날짜는 04-11이 로그 detail의 발효일로 바꾼다). 상태 이력 표·섹션을 만들지 않는다. `test/integration/project-status.test.ts`에 (i) 전환 뒤 `lastStatusChangeOn`이 로그 날짜, 전환 없는 프로젝트는 등록일(KST), 최신 로그 행을 정리 표시로 바꿔도 그 날짜(A-30) 케이스와 (i2) 한 `withTransaction` 안에서 `recordAction(…, { tx })`로 시스템 `status_change`(detail `effectiveOn` 있음) → 사람 `status_change` 순서로 두 줄을 남긴 뒤 `lastStatusChangeOn`이 사람 줄의 날짜를 쓰고, 두 줄의 `occurred_at`이 같음을 함께 단언하는 케이스(엔지 리뷰 A 공백 6)를 더한다.

④ **목록의 다섯 상태.** `app/(app)/projects/page.tsx`의 `STATUS_OPTIONS` 상수를 `listProjectStatusCatalog` 결과로 바꾸고(필터 한 줄의 상태 select), `projects-table.tsx`의 로컬 라벨·태그 맵을 서버가 넘긴 라벨과 `status-display.ts`의 kind로 바꾼다(목록 개정 D-87~D-91 자체는 04-17·04-18 — 이 플랜은 상태 표시만 고친다). ①의 스캔이 GREEN이 된다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-status.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 옛 잠금 값 리터럴이 코드에 남았거나 04-20의 결정표가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-status.test.ts test/integration/projects-list.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 마지막 변경일 조회·목록 상태·DTO 등록 중 하나가 어긋났다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 옛 잠금 값 리터럴 스캔 describe가 있고 초록이다
    - 미수주에서 「상태 바꾸기」가 아니라 2차 `진행으로 되돌리기`가 렌더되고, 모달을 거치는 조건이 `endDateBeforeToday || !currentRevisionApproved` 하나다(DR-7) · 정산→완료는 목록 없이 바로 확인 모달이다
    - 모달 결과 줄·1차 라벨이 rev 5 Copywriting 원문과 글자 그대로 같다(Task 3 E2E b·b3·c가 결과 줄을 단언한다)
    - 상태 전환 화면 파일에 문장형 본문이 없다 — `grep -cE "습니다|됩니다" 'app/(app)/projects/[id]/status-change.tsx'` = 0(P0)
    - 고르기 목록 행의 설명이 코드표에서 오고, 설명 없는 값에는 2행이 없다
    - 부제의 날짜가 최신 `status_change` 로그(없으면 등록일)에서 오고 상태 이력 표·섹션 마크업이 없다(통합 (i))
    - `findLatestActionFor`가 `occurred_at` 다음 `seq`로 정렬하고 선택 `tx`를 받으며, 같은 트랜잭션 두 줄 케이스(i2)가 초록이다
    - `page.tsx`·`projects-table.tsx`에 로컬 상태 라벨 상수가 남지 않았고 목록 태그 kind가 `PROJECT_STATUS_TAG_KIND`에서 온다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 바꾸기·진행으로 되돌리기의 모든 갈래가 공용 확인 모달로 화면에 있고, 상세 부제와 목록이 다섯 상태를 코드표 라벨·Color 매핑으로 보이며, 옛 값이 코드에서 사라졌다</done>
</task>

<task type="auto">
  <name>Task 3: 미저장 편집 게이트(DR-6) · 폰 머리 줄과 「더보기」(DR-26) · 생애 E2E 완성 · 화면 검증(1280 · 1024 · 375)</name>
  <files>app/(app)/projects/[id]/unsaved-edits.ts, app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/project-lifecycle.spec.ts, app/(app)/projects/[id]/project-detail.module.css</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## 적용 규칙` 「S3」의 「미저장 편집이 있으면(DR-6 · B-03 · ENG-D9)」 1142–1143행 · 「폰 첫 화면(<700, DR-26)」 1151–1155행 · 「S7」 미저장 편집(DR-6) 1338–1339행 · `## Copywriting Contract` `막힘 — 미저장 편집` 390행 · `## UI Considerations` 「S3」 490–500행(interactive — 미저장 편집 · 폰 첫 화면) · 「S7」 544–552행
    - `app/(app)/projects/[id]/project-detail.module.css` — 머리 줄 `flex-wrap`(U-1 판단)과 폰 미디어 쿼리 자리
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `dirtyCount`·머리 줄(`styles.header`·`headerActions`)·`<StatusChange`를 찾아 범위만 Read
    - `ui/button/Button.tsx`(04-46 — `disabledReason`·`reasonTone`·`aria-disabled`)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
    - `test/e2e/project-lifecycle.spec.ts`(Task 1 산출물)
  </read_first>
  <action>
① **미저장 편집 게이트(DR-6).** `app/(app)/projects/[id]/unsaved-edits.ts`에 순수 함수 `unsavedEditsReason(count: number): string | null`(`count ≥ 1`이면 `저장 안 한 편집 {count}칸 · 먼저 일괄 저장`, 아니면 null — rev 5 Copywriting `막힘 — 미저장 편집` 원문)을 둔다 — 그룹 B의 새 차수·고객 승인 모달(04-24)이 같은 함수를 쓴다(같은 글자, B-03·ENG-D9). `QuoteLedger`가 `<StatusChange {...statusChange} dirtyCount={dirtyCount} />`로 넘기고, `status-change.tsx`의 트리거 둘(「상태 바꾸기」·「진행으로 되돌리기」)은 `unsavedEditsReason(dirtyCount)`가 있으면 `disabled` + `disabledReason`(04-46 `Button` — `aria-disabled` · `aria-describedby` · `reasonTone` 기본 `block`)이 되어 고르기 목록·확인 모달을 열지 않는다. 미저장 이유가 시작일 게이트 이유보다 먼저다(트리거를 막는 쪽이 이긴다). 자정 자동 정산처럼 사람이 막을 수 없는 변화는 04-22의 `seenStatus`가 저장에서 잡는다(계약 4) — 이 태스크는 트리거만 막는다.

② **폰 머리 줄(DR-26).** `quote-table.tsx` 머리 줄을 폰(<700)에서 제목 → 상태 태그 → 부제 → 버튼 줄 순서로 쌓는다(`project-detail.module.css`의 폰 미디어 쿼리 — 새 토큰·색 없음, PC ≥700은 지금 `flex-wrap` 그대로). 복사·차수 작업이 들어갈 자리로 머리 줄 안에 `HeaderCopyActions` 묶음(자식을 받는 지역 컴포넌트, `id`가 있는 한 요소)을 두고, 폰에서는 묶음에 자식이 하나라도 있을 때만 버튼 줄 끝에 2차 「더보기」(`aria-expanded`, `aria-controls` = 묶음 id)를 렌더해 누르면 바로 아래 한 줄로 펼친다 — 자식이 없으면 「더보기」를 렌더하지 않는다(빈 컨트롤 금지). PC에서는 묶음이 버튼군 안에 그대로 보이고 「더보기」는 없다. 이 플랜 시점에는 묶음에 들어갈 버튼이 아직 없다 — 「복사해 새 차수」(04-24)·「프로젝트 복사」(04-15, 그룹 B)가 이 묶음 안에 버튼을 둔다(폰 부제의 `기간 …` 첫 항목 배치는 04-44 — DR-26).

③ `test/e2e/project-lifecycle.spec.ts`를 완성한다(날짜 준비는 전부 `kstToday(new Date())` + `addDays` — A-18, 진행 픽스처의 종료일은 오늘 + 7. **계급 권한·노출을 켜는 호출은 여기에도 없다** — 대표·본부 책임자·다른 팀 팀장은 시드 계급의 사람과 발령 이력만 만들어 쓴다, ENG-D2. 현재 차수의 고객 승인은 준비 단계에서 `quote_revisions.customer_approved_at`을 DB에 직접 둔다 — 승인 화면은 04-14·04-24): (a) 시작일 없는 수주중 프로젝트에서 팀장이 진행을 고르면 확인 모달 1차가 **누르기 전부터** `aria-disabled="true"`이고 왼쪽에 `시작일 없음 · 기간 적기`(서버 `blockedReason` 그대로, A-09), 모달이 열린 채이고 토스트가 없다, (b) 팀장이 수주중→미수주 모달(결과 줄 `쌓인 비용이 팀 미수주 비용이 됨 · 진행으로 되돌리기 있음`)로 닫고 토스트 `미수주로 닫기 · {번호}` → 같은 프로젝트(현재 차수 승인 전)에서 「상태 바꾸기」가 없고 2차 `진행으로 되돌리기`가 있으며 누르면 확인 모달에 `1차 고객 승인 전 · 진행부터 지출결의 멈춤` → 1차 → 진행, 토스트 `진행으로 되돌리기 · {번호}`(DR-7 · DR-21), (b2) 현재 차수가 승인되고 종료일이 오늘 + 7인 미수주에서 「진행으로 되돌리기」를 누르면 모달 없이 곧바로 진행이고 토스트 `진행으로 되돌리기 · {번호}`, (b3) 현재 차수가 승인되고 종료일이 오늘 − 3인 미수주에서 누르면 확인 모달 결과 줄에 `종료일 지남 · 바로 정산`, (c) 테스트 준비 단계에서 프로젝트 하나를 DB에 `settling`으로 두고(정산은 사람이 켜는 상태가 아니다 — 자동 전환은 04-11) 대표로 열어 「상태 바꾸기」 → 바로 `완료로 바꾸기` 모달(결과 `견적 줄 잠김 · 새 지출결의 받지 않음 · 되돌리기 없음`) → 완료 태그(success), 같은 정산 프로젝트를 팀장·담당 PM으로 열면 「상태 바꾸기」가 없다(D-79), (d) 완료 프로젝트에서 PM의 견적 줄 저장 시도가 `완료 · 견적 줄 잠김`으로 거부된다(04-06 규칙 — 셀 단위 잠금 렌더는 04-30), (e) 진행 프로젝트는 누구에게도 「상태 바꾸기」가 없다, (g) **다른 팀 팀장(D11·OV-4):** 준비 단계에서 두 번째 팀과 그 팀 팀장 계정(오늘 기준 발령 이력)을 만들고, 첫 팀의 수주중 프로젝트를 그 팀장으로 열면 「상태 바꾸기」가 DOM에 없다 — 같은 프로젝트를 본부 책임자(업무 범위 전사)로 열면 있다, (h) 미수주로 닫기 확인 모달에서 1차를 `dblclick`해도 상태가 미수주 한 번만 바뀐다 — 막힘 자리에 두 번째 요청의 `상태가 미수주로 바뀜 · 새로 고침`이 나오지 않고 성공 토스트가 한 번이다(A-34), (i) **미저장 편집 게이트(DR-6):** 수주중 프로젝트에서 팀장이 견적 줄 실행가 셀 하나를 고치면 「상태 바꾸기」가 `aria-disabled="true"`이고 옆에 `저장 안 한 편집 1칸 · 먼저 일괄 저장`이 보이며 그 요소가 버튼의 `aria-describedby` 대상이고, 눌러도 `dialog`가 열리지 않는다 → `Control+s`로 저장한 뒤 버튼이 다시 켜져 누르면 목록형 모달이 열린다, (f) 375px 뷰포트에서 팀장 시점의 머리 줄 요소 순서가 제목 → 상태 태그 → 부제 → 「상태 바꾸기」이고 문서 가로 스크롤이 0이며, 복사·차수 묶음이 비어 있으므로 「더보기」가 DOM에 없다.

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트(lint·typecheck·build) → 이 태스크 실행자가 아닌 **별도 서브에이전트**(model: sonnet, 프롬프트에 `verification-before-completion` 명시)가 `CI=true`로 앱을 띄워 **1280 · 1024 · 375** 세 폭(DR-14 감사 폭)에서 S7 long-text backstop(가장 긴 확인 모달 결과 줄 셋 + 40자 이름이 480 모달·폰 시트 안에서 잘리지 않음)과 S3 overflow backstop(머리 줄 — PC 두 폭은 버튼이 숨지 않음, 375는 폰 순서 · 가로 스크롤 0)을 DOM 실측으로 판정 → 필요하면 `project-detail.module.css`만 고친다(라벨을 줄이거나 PC 버튼을 숨기지 않는다) → 전체 게이트 한 번(`CI=true pnpm test`). 판정 기록을 SUMMARY에 남긴다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — project-lifecycle의 케이스 중 하나라도 실패하면 여기서 드러난다</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 판정 기록(S7 long-text · S3 머리 줄 — 1280 · 1024 · 375)을 SUMMARY에서 확인</test>
      <expected>확인 모달 결과 줄이 480 모달·폰 시트 안에서 잘리지 않고, PC 두 폭 머리 줄에 가로 스크롤이 없고 버튼이 숨지 않으며, 375 머리 줄이 제목 → 태그 → 부제 → 버튼 줄 순서다</expected>
      <why_human>폭·줄바꿈은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `unsavedEditsReason`이 `app/(app)/projects/[id]/unsaved-edits.ts` 하나에만 정의되고, 그 문자열 `먼저 일괄 저장`이 `app/`에서 그 파일에만 리터럴로 있다(`grep -rln "먼저 일괄 저장" app` → 그 파일 하나)
    - 트리거 둘이 미저장 편집이면 `aria-disabled`이고 목록·모달이 열리지 않으며 저장 뒤 켜진다(E2E i)
    - 「더보기」가 폰에서 묶음에 자식이 있을 때만 렌더되는 조건을 갖고, 지금 화면(자식 없음)에서는 DOM에 없다(E2E f)
    - `project-lifecycle.spec.ts`에 계급 권한·노출을 켜는 호출이 0건이다(Task 1의 grep과 같은 명령) — 대표 완료(c)·본부 책임자(g)가 시드만으로 통과한다
    - `project-lifecycle.spec.ts`가 (a)~(i)·(b2)·(b3) 경우를 단언한다 — (a)는 제출 전 `aria-disabled`·막힘 문구·모달 유지·토스트 부재를, (b)·(b2)·(b3)는 DR-7의 조건부 모달·즉시 경로·토스트 문구를, (c)는 대표의 완료와 팀장·PM의 버튼 부재를, (g)는 다른 팀 팀장의 버튼 부재와 본부 책임자의 버튼 존재를 본다
    - E2E가 단언하는 사용자 문자열이 전부 rev 5 Copywriting 원문이다(P0 — 스펙 파일에 `할 수 없음 —`·`고쳐 주세요`·`습니다 ·` 꼴이 없다: `grep -cE "할 수 없음 —|새로 고쳐 주세요|습니다 ·" test/e2e/project-lifecycle.spec.ts` = 0)
    - 독립 DOM 감사 판정(세 폭)이 SUMMARY에 있다
    - `CI=true pnpm test` 세 계층이 전부 초록이다
  </acceptance_criteria>
  <done>미저장 편집이 상태 전환 트리거를 막고, 폰 머리 줄이 이름·상태·부제를 먼저 보이며 「더보기」 자리가 준비됐고, 다섯 상태 생애(진행 전환 · 시작일 막힘 · 미수주 왕복의 조건부 모달 · 대표 완료 · 완료 잠금)가 E2E로 증명되고, 모달·머리 줄 폭이 세 폭 DOM 감사로 판정됐으며 전체 게이트가 CI=true로 초록이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 페이지 → 상태 바꾸기 컴포넌트 | 갈 곳 목록이 서버에서 온다. 화면이 상태 문자열로 버튼을 결정하면 서버 규칙과 어긋난다 |
| 행동 로그 → 상세 부제 | 로그를 읽어 날짜를 파생한다. 파생이 집계 화면으로 커지면 감시 도구가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-34 | Elevation of Privilege | 상태 바꾸기 렌더(화면 쪽) | high | mitigate | 버튼 렌더는 04-20의 `statusDestinations`가 비었는지만 본다 — 화면이 권한을 추론하지 않는다. 버튼을 숨기는 것은 보조이고 판정은 04-20의 서버 재판정이다. PM 화면의 버튼 부재를 E2E(Task 1 · (e))로 고정한다 |
| T-04-35 | Elevation of Privilege | 완료 모달(화면 쪽) | high | mitigate | 정산 프로젝트의 「상태 바꾸기」가 대표·시스템 관리자에게만 렌더되고 팀장·PM에게 없음을 E2E(c)로 고정한다. 서버 거부는 04-20 통합 테스트(h) |
| T-04-161 | Elevation of Privilege | 다른 팀 팀장의 상태 바꾸기 렌더 | high | mitigate | (CEO OV-4·사용자 D11) 버튼 렌더·갈 곳 목록·서버 전환이 같은 `actorCoversProjectTeam`을 지난다(04-20). 화면은 목록이 비었는지만 보고, 다른 팀 팀장의 버튼 부재를 E2E (g)로 고정한다. 서버 거부는 04-20 통합 (j) |
| T-04-39 | Information Disclosure | 상태 변경 로그의 활용 | medium | mitigate | 로그를 사람별로 집계해 보여주는 화면·내보내기를 만들지 않는다(금지 항목). 상세에는 `findLatestActionFor` 한 줄로 마지막 변경일만 파생하고, 행위자 이름을 부제에 싣지 않는다 |
| T-04-307 | Elevation of Privilege | 시드 빈틈을 가리는 테스트 | medium | mitigate | (ENG-D2) 생애 E2E가 노출·권한을 손으로 켜지 않아 시드만으로 팀장·대표 흐름이 서는지를 증명한다 — 켜는 호출 0건 grep을 수용 기준으로 둔다 |
| T-04-330 | Tampering | 미저장 편집이 상태 전환으로 사라짐(UX-04) | medium | mitigate | (DR-6) 미저장 편집이 있으면 트리거 둘을 `aria-disabled`로 막아 고르기 목록·모달이 열리지 않는다(E2E i). 화면이 막을 수 없는 자동 정산·동료 전환은 04-22의 `seenStatus` 서버 비교가 저장 전체를 거부한다(계약 4) — 화면 게이트는 보조이고 판정은 서버다 |
| T-04-331 | Spoofing | 즉시 되돌리기로 자동 정산 경고를 건너뜀 | low | mitigate | (DR-7) 종료일 지남·승인 전이면 확인 모달을 거쳐 결과 줄로 알린다 — 판정 입력(`endDateBeforeToday`·`currentRevisionApproved`)은 서버가 계산해 넘기고, 전환 자체의 권한·시작일 게이트는 04-20 서버 재판정이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 독립 DOM 감사(S7 long-text · S3 머리 줄 — 1280 · 1024 · 375) 판정이 SUMMARY에 있다
- 상태 전환 모달이 전부 `ui/confirm-dialog`이고 사용자 문자열이 rev 5 Copywriting 원문이다
- `CI=true pnpm test` 세 계층 통과
- `app/`·`domain/`·`repositories/`에 옛 잠금 값 리터럴 0(스캔 describe 초록)
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 팀장·대표가 상세의 「상태 바꾸기」(미수주는 「진행으로 되돌리기」)로 사람의 전환 넷을 하고, PM·진행·완료에는 버튼이 없다
- 미저장 편집이 있으면 상태 전환 트리거가 막혀 편집이 전환으로 사라지지 않는다(DR-6)
- 막힘 이유가 게이트 문자열 그대로 보이고 모달이 닫히지 않는다
- 목록과 상세가 같은 색 매핑·코드표 라벨로 다섯 상태를 보이고, 부제가 마지막 변경일만 말한다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컴포넌트(신규) | `app/(app)/projects/[id]/status-change.tsx`(모든 모달 = `ui/confirm-dialog`) · `app/(app)/projects/status-display.ts`(`PROJECT_STATUS_TAG_KIND`) |
| 함수(신규) | `lastStatusChangeOn`(`domain/projects/status.ts`) · `findLatestActionFor(…, tx?)`(`repositories/action-log.ts` — `occurred_at`·`seq` 정렬) · `unsavedEditsReason(count)`(`app/(app)/projects/[id]/unsaved-edits.ts` — `저장 안 한 편집 N칸 · 먼저 일괄 저장`, 그룹 B 04-24가 재사용) |
| 타입(신규) | `StatusChangeProps`(`status-change.tsx` export — `projectId`·`projectNumber`·`projectLabel`·`from`·`destinations[{ value, label, description, blockedReason }]`·`startDate`·`endDate`·`settleOn`·`endDateBeforeToday`·`currentRevisionSeq`·`currentRevisionApproved`) |
| 머리 줄 자리(신규) | `HeaderCopyActions`(`quote-table.tsx` 지역 묶음 — 폰에서 자식이 있을 때만 2차 「더보기」, `aria-expanded`·`aria-controls`) — 그룹 B의 「복사해 새 차수」(04-24)·「프로젝트 복사」(04-15)가 이 안에 버튼을 둔다 |
| 변경된 기존 심볼 | `QuoteLedger` props(`statusChange: StatusChangeProps \| null`·`statusLabel`·`statusTagKind`·부제) · `<StatusChange dirtyCount>` · 머리 줄 폰 순서 CSS · 목록 상태 필터(`STATUS_OPTIONS` → 코드표) · 목록 상태 열 · `findProject` DTO(등록일 — 없을 때만) |
| 화면 문구(rev 5 원문) | 토스트 `진행으로 바꾸기 · {번호}` · `진행으로 되돌리기 · {번호}` · `미수주로 닫기 · {번호}` · `완료로 바꾸기 · {번호}` · 결과 줄(`종료일 없음 · 시작일로 저장` · `종료일 지남 · 바로 정산` · `기간 … · … 정산` · `{n}차 고객 승인 전 · 진행부터 지출결의 멈춤` · `쌓인 비용이 팀 미수주 비용이 됨 · 진행으로 되돌리기 있음` · `견적 줄 잠김 · 새 지출결의 받지 않음 · 되돌리기 없음`) |
| 테스트 | `test/e2e/project-lifecycle.spec.ts`(신규) · `test/unit/domain/project-status.test.ts`(옛 값 스캔 describe) · `test/integration/project-status.test.ts`((i)·(i2) 마지막 변경일) |
| 새 스키마·마이그레이션·액션 | 없음 — 액션은 04-20 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-09 — 막힌 갈 곳의 이유를 화면이 만들게 됨 | WARNING | Plan 04-21, Task 1 ②: `blockedReason`(04-20)으로 제출 전 비활성 + 문자열 그대로 · E2E (a) |
| A-11 — 오래된 화면이 다른 전환을 조용히 수행 | WARNING | Plan 04-21, Task 1 ②: 액션에 `from` |
| A-18 — 고정 날짜·「오늘」 경계 E2E가 시한폭탄 | WARNING | Plan 04-21, Task 1 ④·Task 3 ①: `kstToday` + `addDays`, 날짜 리터럴 0건 grep |
| A-30 — 정리된 로그 행을 걸러 부제가 등록일로 | NOTE | Plan 04-21, Task 2 ③: `findLatestActionFor`가 정리 행 포함 · 통합 (i) |
| A-31 — ReactNode → props 재작업 | NOTE | Plan 04-21, Task 1 ②③: `StatusChangeProps` 데이터 props를 지금 정함(04-44가 넓힘 — Round 4 포인터 정정, 옛 04-22) |
| A-34 — 두 번 제출·새로 고침 안내·포커스 복귀 | NOTE | Plan 04-21, Task 1 ② · E2E (h) |
| A-35 — 옛 값 스캔이 식별자에 헛걸림 | NOTE | Plan 04-21, Task 2 ①: 따옴표 리터럴만 |
| OV-4 / 사용자 D11·D20 — 팀장 자기 팀 한정 | HIGH | Plan 04-21, E2E (g) · T-04-161 (서버 규칙은 04-20·04-27) |
| OV-7 / 사용자 D21 | HIGH | `quote-table.tsx`·`projects-table.tsx`를 04-09 뒤에 고치도록 `depends_on`에 04-09 — depends_on DAG 웨이브 8 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-D2 — E2E가 노출·권한을 손으로 켜 시드 빈틈을 가림 | DECISION | Plan 04-21, Task 1 ④ · Task 3 ①: 켜는 호출 0건(grep 수용 기준) · 팀장 머리 줄 번호·이름 단언 · T-04-307 (시드는 04-20) |
| 엔지 리뷰 A 테스트 공백 6 — 같은 트랜잭션 두 `status_change`의 최신 판정 | NOTE | Plan 04-21, Task 2 ③: `findLatestActionFor` `occurred_at DESC, seq DESC` · 통합 (i2) |
| ENG-D3 ① 규약 — 04-11이 잠금 안에서 직전 변경일을 읽음 | NOTE | Plan 04-21, Task 2 ③: `findLatestActionFor(…, tx?)` |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단(시드 빈틈은 04-20 파일에서 고친다) |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 디자인 리뷰(`docs/designs/plant8-erp-phase4-design-review-260923.md`) · UI-SPEC rev 5

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-12 — 확인 모달을 파일마다 따로 만듦 | HIGH | Plan 04-21, Task 1 ②·Task 2 ②: 고르기 목록(목록형)·확인 모달 전부 `ui/confirm-dialog`(04-46, 계약 2 — 조상 체인 04-09 → 04-28 → 04-46로 이미 앞, `depends_on` 무변경) · 수용 기준 grep(`<dialog`·`showModal` 0) |
| DR-7 — 미수주 → 진행이 일반 라벨 버튼으로 즉시 실행 | HIGH | Plan 04-21, truths · Task 2 ②: 2차 `진행으로 되돌리기`, 종료일 지남 또는 승인 전이면 확인 모달(결과 줄 `종료일 지남 · 바로 정산` / `{n}차 고객 승인 전 · 진행부터 지출결의 멈춤`), 아니면 즉시 · E2E b·b2·b3 |
| DR-6 — 미저장 편집이 있는데 상태 전환 가능 | HIGH | Plan 04-21, Task 3 ①: `unsavedEditsReason` + 트리거 둘 `aria-disabled`(`reasonTone` block) · E2E i · T-04-330 (저장 쪽 `seenStatus`는 04-22) |
| DR-21 · P0 — 토스트 동사 불일치 · 「…합니다」 본문 · `새로 고쳐 주세요` 링크 | NOTE | truths · Task 1·2: 토스트 = 누른 라벨 · 번호, 결과 줄 rev 5 명사형, 상태 바뀜은 서버 문자열만(옛 A-34 링크 버튼 삭제 — rev 5 S7) · Task 3 수용 기준 grep |
| DR-26 — 폰 첫 화면 머리 줄 | NOTE | Plan 04-21(머리 줄 컨트롤 소유), Task 3 ②: 폰 순서 + `HeaderCopyActions` 묶음의 「더보기」(자식이 있을 때만). 부제 `기간 …` 첫 항목은 04-44(Round 4 포인터 정정, 옛 04-22), 묶음 안 버튼은 그룹 B(04-24·04-15) |
| DR-10 · DR-11 — 비활성 이유의 톤·연결 | NOTE | 막힘은 `Button`/`ConfirmDialog`의 `disabledReason`(`aria-disabled` · `aria-describedby`, 기본 block) — E2E a·i가 `aria-disabled`·`aria-describedby`를 단언 |
| DR-14 — DOM 감사 폭 1024 | NOTE | backstop 둘 · Task 3 ④ · human-check: 1280 · 1024 · 375 |
| DR-1 — rev 4 줄 번호 | NOTE | `<context>`·`read_first`를 rev 5 절 제목 + 현재 줄로 |

### Round 4 — 체커 revision 1 후속 정리(2026-09-23 · 04-08 · 04-22 분할 포인터)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 분할 포인터 — `Button`(계약 1)·`ui/confirm-dialog`(계약 2)의 주인 | NOTE | 「04-08」 → **04-46** 전부(truth · key_link · objective (12) 조상 사슬 04-21 → 04-09 → 04-28 → 04-46 · `<context>` SUMMARY · read_first 둘 · Task 3 ① · Round 3 DR-12 행). `depends_on` 변경 없음 |
| 분할 포인터 — 「기간 적기」·「기간 바꾸기」 모달 연결 · `StatusChangeProps` 확장 · 폰 부제 순서 · 총 매출 예상가 부제 | NOTE | truth(「기간 적기」) · key_link/truth(A-31) · objective · `<probe_fallback>` · Task 1 ② · Task 3 ② · Round 1·3 행: 「04-22」 → **04-44**(옛 04-22 Task 3 분할). 기간 칸 · 부제 `기간 …` 항목 · `seenStatus` 거부(계약 4)는 04-22에 남아 그대로. 04-44 · 04-22는 이 플랜의 후행(이 플랜이 만든 것을 넓힌다) — 의존 추가 없음 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-21-SUMMARY.md` when done
</output>
