---
phase: 04-project-quote-ledger
plan: 22
type: execute
wave: 10
depends_on: ["04-11"]
files_modified:
  - domain/projects/period.ts
  - domain/rules/register.ts
  - domain/projects/ledger.ts
  - repositories/projects.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/period-field.tsx
  - app/(app)/projects/[id]/status-change.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/project-period.test.ts
  - test/integration/project-period.test.ts
  - test/e2e/project-period.spec.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "상세 부제에 `기간 2026-09-12 ~ 2026-09-16`(없으면 `기간 미정`, 한쪽만 있으면 `2026-09-12 ~ —`)이 보이고, 고칠 수 있는 사람에게만 바로 오른쪽에 3차 「기간 바꾸기」가 있다(S13)"
    - "기간을 고칠 수 있는 사람: 수주중·진행·미수주 = 담당 PM + 팀장 이상(`projects.status`) · 정산 = 팀장 이상만 · 완료 = 아무도(⚠ S13 기본값)"
    - "진행의 담당 PM은 종료일을 오늘(KST) 이후로만 적을 수 있고, 이른 날짜는 칸 아래 `종료일은 오늘부터 적을 수 있습니다 · 앞당기려면 팀장 {이름}` + 화면 전체 거부다 — 서버도 같은 규칙으로 거부한다(⚠ S13 두 번째 기본값)"
    - "정산에서 팀장 이상이 종료일을 오늘 이후로 늦춰 저장하면 같은 트랜잭션에서 진행으로 돌아가고 `status_change` 로그가 그 사람 이름으로 한 줄 남는다 — 새 종료일이 지나면 다시 정산이 된다(D-80)"
    - "진행 이후 상태에서 종료일을 비우고 저장하면 시작일 값으로 저장되고, 진행 이후 상태에서 시작일을 비우는 저장은 `진행부터는 시작일이 있어야 합니다 · 시작일을 적어 주세요`로 거부된다(D-82)"
    - "기간 칸의 결과 미리보기 힌트 한 줄은 조건이 맞을 때만 보인다 — `저장하면 진행으로 돌아감`(정산 + 새 종료일 ≥ 오늘) · `저장하면 정산이 됨`(진행 + 새 종료일 < 오늘) · `종료일이 비어 시작일로 저장됨`(진행 이후 + 종료일 비움). 판정은 서버 저장과 같은 domain 함수다(SYSTEM.md 개정 ⑨)"
    - "기간 칸은 `Form.Actions`가 없는 칸 폼이다 — 고치면 dirty가 되어 화면 1차 「일괄 저장 Ctrl+S N」의 N에 칸 수만큼 합류하고 견적 줄·매출과 같은 트랜잭션으로 저장된다. Enter 기본 제출은 막히고 Ctrl+S가 저장이다(S13, §7-15)"
    - "기간 칸 오류로 저장이 거부되면 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`이 붙는다(U-6)"
    - "상태 바꾸기 확인 모달의 막힘 3차 「기간 적기」는 모달을 닫고 머리 줄 기간 칸을 열어 시작일에 포커스한다(기간 권리가 없으면 3차 없이 게이트 문자열만), 진행으로 바꾸기 본문은 종료일이 이미 지났으면 `종료일이 지나 바로 정산이 됩니다 · 늦추려면 기간 바꾸기`를 더한다(S7, Copywriting 확인 — 진행으로 바꾸기)"
    - "날짜는 식별자형 숫자라 쉼표가 없다(D-95 예외)"
    - "일괄 저장(합성 저장)은 게이트 판정 전에 04-11의 자동 전환을 먼저 적용한다 — 자정을 넘겨 연 화면에서 진행 프로젝트의 줄을 저장해도 정산 기준으로 판정된다(D-76 읽기 시점 판정의 저장 경로 — 상세·목록·상태 전환 경로는 04-11)"
    - statement: "기간 칸이 열려도 머리 줄 375px 가로 스크롤이 0이고 칸 묶음이 머리 줄 아래에 붙는다(머리 줄 안에 입력을 두지 않는다) — DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "시스템이 한 상태 전환을 사람의 이름으로 기록하거나, 사람이 한 전환을 시스템 이름으로 기록해서는 안 된다. 자동 정산은 누구의 판단도 아닌 날짜의 결과이고, 정산→진행 되돌리기는 그 날짜를 고친 사람의 판단이다 — 행동 로그가 이 둘을 섞으면 「누가 정산을 풀었는가」에 답하지 못한다"
      status: resolved
      verification: test
    - statement: "PM이 종료일 한 칸으로 제 손으로 되돌릴 수 없는 상태 전환(진행 → 정산)을 일으켜서는 안 된다. 상태 전환은 팀장의 일이라는 D-46의 취지를 기간 칸이 우회하는 경로가 되지 않게, 진행의 PM은 오늘보다 이른 종료일을 서버에서도 저장할 수 없다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/period.ts"
      provides: "기간 편집 권리·검증·결과 미리보기·저장 해석의 순수 함수(화면 힌트와 서버 저장이 같은 함수)"
      exports: ["periodEditRights", "validatePeriodChange", "previewPeriodChange", "resolvePeriodSave"]
    - path: "app/(app)/projects/[id]/period-field.tsx"
      provides: "머리 줄 아래 기간 칸 묶음 — Form.Actions 없음, dirty 합류, 미리보기 힌트"
      contains: "previewPeriodChange"
    - path: "test/unit/domain/project-period.test.ts"
      provides: "권리 · 검증 · 미리보기 · 저장 해석 결정표"
      min_lines: 40
    - path: "test/integration/project-period.test.ts"
      provides: "정산 연장(사람 로그) · PM 앞당김 거부 · 팀장 앞당김 즉시 정산(시스템 로그) · 종료일 기본값 · 한 트랜잭션 전부 거부 · 완료 거부"
      min_lines: 50
  key_links:
    - from: "domain/projects/ledger.ts"
      to: "domain/projects/period.ts"
      via: "기간 저장이 화면 미리보기와 같은 해석 함수로 종료일 기본값·정산→진행을 정한다"
      pattern: "resolvePeriodSave"
    - from: "app/(app)/projects/[id]/period-field.tsx"
      to: "domain/projects/period.ts"
      via: "힌트 한 줄을 서버 저장과 같은 함수로 만든다(개정 ⑨)"
      pattern: "previewPeriodChange"
    - from: "domain/projects/ledger.ts"
      to: "domain/projects/auto-transition.ts"
      via: "게이트 판정 전에 자동 전환을 적용하고(저장 경로 선판정), 커밋 뒤 한 번 더 불러 진행 + 지난 종료일을 곧바로 정산으로 만든다(04-11 함수 재사용)"
      pattern: "applyAutoSettlement"
    - from: "app/(app)/projects/[id]/status-change.tsx"
      to: "app/(app)/projects/[id]/quote-table.tsx"
      via: "막힘 3차 「기간 적기」가 QuoteLedger의 openPeriodField를 부른다"
      pattern: "openPeriodField"
---

<objective>
**As a** 기획 PM·팀장, **I want to** 상세 머리 줄 아래에서 프로젝트 기간을 권리대로 고치고, 저장 전에 그 결과(정산으로 넘어감 · 진행으로 돌아감 · 종료일 기본값)를 한 줄로 미리 보며, 행사가 늘어나면 팀장이 종료일을 늦춰 정산을 진행으로 되돌리기를, **so that** 날짜 한 칸이 상태를 바꾸는 규칙이 화면과 서버에서 같고, PM은 기간 칸으로 상태 전환 권한을 우회하지 못한다.

**2026-09-23 체커 1차 분할.** 옛 04-11(23파일)을 둘로 나눈 **화면 쪽 — 상세 기간 칸(S13)** 이다(D-80 · D-82의 저장 규칙 · ⚠ S13 기본값 둘 · S7 「기간 적기」 연결). 날짜로 움직이는 서버 쪽(KST 모듈 · 자동 정산 · 모든 경로 선판정 · 종료일 지남 · 담당자 이름 출처)은 04-11이 먼저 만들었다 — 이 플랜은 그 `applyAutoSettlement`·`projectResponsibles`·`lib/kst-date`를 부른다.

Purpose: 04-12(정산의 실행가 한정 편집)의 EMPTY 「기간 바꾸기」가 이 플랜의 `openPeriodField`와 `periodEditRights`를 쓰고, D-80이 정산을 되돌리는 유일한 길이 이 칸이다.

Output: 기간 편집 순수 함수 모듈 · 게이트 규칙 `project.period-edit` · 합성 저장의 `period` 입력과 저장 경로 선판정 · 기간 칸 컴포넌트 · 상태 모달 연결 · 단위 1 · 통합 1 · E2E 1(04-11 스펙 확장).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 시간은 `vi.useFakeTimers`로 얼리지 않고 `deps.now`로 주입한다(리포 관례, PATTERNS). `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-21-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 525–533행(S13 상태 + ⚠ 둘) · 1344–1364행(S13 적용 규칙) · 1244–1247행(S7 막힘 3차 「기간 적기」) · 340행·359행·374행·334행(Copywriting 기간 칸 오류·미리보기·진행으로 바꾸기·U-6)
# docs/design/SYSTEM.md §7-15(Form.Hint 두 번째 쓰임 — 04-08 개정 ⑨)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(기간 칸으로 움직이는 부분)** 다. 프로브는 옛 네 상태 문구로 돌았으므로 수용 기준은 D-80·D-82와 UI-SPEC S7·S13에서 끌어와 `must_haves.truths`에 적었다. S13의 다섯 상태 중 empty·loading·long-text(해당 없음)는 truths의 「칸은 항상 둘 · 서버 렌더 값」으로, error·partial은 truths로, S3 overflow는 기간 칸이 열린 상태의 backstop으로 옮겼다(옛 04-11에서 그대로).

**계획 단계 가정(사용자에게 표면화):**
- **⚠ S13 기간 편집 주체 — 기본값 적용:** 수주중·진행·미수주 = 담당 PM + 팀장 이상, 정산 = 팀장 이상, 완료 = 아무도.
- **⚠ S13 진행 중 PM의 종료일 앞당기기 — 기본값 적용:** 진행의 담당 PM은 종료일을 오늘 이후로만. 이른 날짜는 팀장 이상.
- **「팀장 {이름}」의 출처:** 04-11의 `projectResponsibles`(프로젝트 팀에 오늘 소속된 `projects.status` 쓰기 계급의 사람, 여럿이면 이름순 첫 사람). 없으면 이름 없는 문구로 떨어진다.
- **E2E 파일:** 옛 04-11이 `project-lifecycle.spec.ts`에 두려던 「진행 확인 모달의 종료일 지남 줄」 케이스는 기간 칸과 같은 준비를 쓰므로 `project-period.spec.ts`에 둔다(파일 수를 줄인 선택 — 단언 내용은 같다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 팀장이 정산 프로젝트의 종료일을 늦춰 진행으로 되돌린다(순수 함수 → 게이트 → 합성 저장 → 조건부 갱신 → 액션 → 기간 칸 → 일괄 저장 → E2E)</name>
  <files>domain/projects/period.ts, domain/rules/register.ts, domain/projects/ledger.ts, repositories/projects.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/period-field.tsx, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1344–1364행(S13 전문 — 자리 · 칸 폼 · dirty 합류 · Enter/Ctrl+S/Esc · 미리보기 세 줄 · 오류 · 저장 성공 · 기간 적기 진입 · 쉼표 예외) · 525–533행(⚠ 두 기본값)
    - `docs/design/SYSTEM.md` §7-15(`Form.Actions` 없는 칸 폼 · `Form.Hint` 미리보기 — 04-08 개정 ⑨)
    - `ui/form/Form.tsx`(전문) — `Form.Field`·`Form.Hint`·`Form.Error` 마크업
    - `domain/projects/ledger.ts`(전문 40행) — 한 트랜잭션 합성 지점(옵션 tx)과 `deps` 형태
    - `domain/projects/auto-transition.ts`·`domain/projects/responsibles.ts`·`lib/kst-date.ts`(04-11 산출물)
    - `domain/projects/status.ts`·`repositories/projects.ts`(04-20 `updateProjectStatusIfCurrent` · 04-11 `settleOverdueProjects`)
    - `app/(app)/projects/actions.ts` 50–115행(`saveProjectLedgerAction` 스키마)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `dirtyCount`·`handleSave`·머리 줄(`styles.header`)·`rejectionSummary`를 찾아 그 범위만 Read(전문 Read 금지)
    - `test/e2e/project-period.spec.ts`(04-11 산출물) — 프로젝트를 DB에 상태·날짜로 두는 준비 형태
  </read_first>
  <action>
① `domain/projects/period.ts`(순수 — `lib/kst-date`와 04-06 전이표만 import, 리포지토리 import 없음 — 화면 클라이언트 컴포넌트도 이 파일을 import한다): 이 태스크는 트레이서에 필요한 둘만 먼저 만든다 — `periodEditRights({ status, isAssignedPm, canChangeStatus })` → `"lead" | "pm" | "none"`(⚠ S13 기본값 표) · `resolvePeriodSave({ status, newStart, newEnd, todayKst })` → `{ startDate, endDate, statusChange? }`(정산 + 새 종료 ≥ 오늘 → `statusChange: { from: 정산, to: 진행 }`). 검증·미리보기는 Task 2.

② `domain/rules/register.ts`에 `project.period-edit`(ctx `{ rights, errors }`: 권리가 `none`이면 `기간을 고칠 권한이 없습니다`, 검증 오류가 있으면 첫 오류 이유)을 등록한다.

③ **합성 저장(`domain/projects/ledger.ts`).** `SaveProjectLedgerInput`에 `period?: { startDate: string | null; endDate: string | null; version: number }`를 더하고, (a) **저장 경로 선판정(D-76):** 게이트 판정 전에 04-11의 `applyAutoSettlement({ projectIds: [id] }, { now })`를 먼저 부르고(기간이 없는 저장 포함 — `deps.now`를 받아 넘긴다) 그 뒤의 상태로 판정한다, 같은 트랜잭션 안에서 (b) 권리(`periodEditRights` — 담당 PM 여부와 `projects.status` 권한)를 계산해 `gate(project, "project.period-edit", { rights, errors })` — 검증 함수는 Task 2가 만들므로 이 태스크의 `errors`는 빈 목록이다 (c) 거부면 `SaveRejectedError`와 같은 구조로 **칸 오류를 담아 전체 거부**(표 쪽은 저장하지 않는다) (d) 통과면 `resolvePeriodSave` 결과로 `repositories/projects.ts`의 `updateProjectPeriodIfVersion(viewer, id, { startDate, endDate, version }, tx)`(버전이 다르면 충돌 거부)와, `statusChange`가 있으면 04-20의 `updateProjectStatusIfCurrent`(정산→진행) + `recordAction(viewer, status_change, detail { from, to, trigger: "end_date_extended" })`(**사람 행위자**, D-80). 커밋 뒤 04-11의 `applyAutoSettlement({ projectIds })`를 한 번 더 불러 진행 + 지난 종료일을 곧바로 정산으로 만든다(「다음 판정 때 바로」). `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마에 `period`를 더한다(날짜는 `YYYY-MM-DD` 문자열 또는 null).

④ **화면(한 경로).** `app/(app)/projects/[id]/period-field.tsx`(클라이언트): 머리 줄 **아래**에 열리는 `Form` 한 묶음 — `Form.Field 시작일`(280) · `Form.Field 종료일`(280), 자리표시자 `2026-09-18`, **`Form.Actions` 없음**. 고치면 칸이 dirty(`--inset-dirty`)가 되어 `QuoteLedger`의 dirty 수에 칸 수만큼 합류하고 1차 「일괄 저장」이 같은 액션 호출에 `period`를 싣는다. 저장 성공 시 600ms `--accent-weak` 틴트(`prefers-reduced-motion`이면 없음) 뒤 묶음이 닫히고 부제·태그가 새 값(토스트 없음). `[id]/page.tsx`가 권리(`periodEditRights`)·팀장 이름(04-11 `projectResponsibles`)·오늘(KST)을 계산해 넘기고, 부제에 `기간 …` + (권리가 있을 때만) 3차 「기간 바꾸기」를 둔다 — 칸이 열린 동안 부제의 기간 글자와 버튼은 숨긴다. `quote-table.tsx`에는 기간 칸의 dirty 합류와 `openPeriodField(focus: "start" | "end")` 한 함수만 더한다(Task 3과 04-12가 이 함수를 부른다).

⑤ **E2E(트레이서 증명, `project-period.spec.ts` 확장).** 준비에서 종료일이 어제(KST)인 정산 프로젝트를 둔다 → 팀장으로 상세 → 「기간 바꾸기」 → 종료일을 내일로 → 1차 「일괄 저장 Ctrl+S 1」 → 태그 `진행`, 부제 기간이 새 종료일.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-auto-settlement.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 합성 저장에 기간을 더하면서 04-11의 선판정이나 기존 견적 줄·매출 저장이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 팀장의 종료일 연장이 진행 태그로 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/projects/period.ts`가 리포지토리·DB를 import하지 않는다
    - `project.period-edit` 규칙이 등록되고 기간 저장이 그 게이트를 지난다
    - 기간 저장이 `saveProjectLedger`의 한 트랜잭션 안이고 `updateProjectPeriodIfVersion`이 버전 조건부다
    - 정산→진행 되돌리기 로그의 detail에 `trigger: "end_date_extended"`가 있고 행위자가 그 viewer다
    - 기간 칸에 `Form.Actions`가 없고 저장은 화면 1차 버튼 하나다
    - E2E가 팀장의 정산 연장 → 진행 태그를 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>팀장이 상세 기간 칸에서 정산 프로젝트의 종료일을 늦추면 같은 저장에서 진행으로 돌아가는 한 경로가 순수 함수·게이트·합성 저장·화면·E2E까지 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 권리·검증·미리보기 결정표 + PM 이른 종료일 거부 + 종료일 기본값(D-82) + U-6 + 통합 증명</name>
  <files>domain/projects/period.ts, domain/projects/ledger.ts, app/(app)/projects/[id]/period-field.tsx, app/(app)/projects/[id]/quote-table.tsx, test/unit/domain/project-period.test.ts, test/integration/project-period.test.ts, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 340행(오류 문구 넷) · 359행(미리보기 문구 셋) · 334행(U-6 문구)
    - `domain/org/index.ts`의 `teamAtDate()`와 `repositories/team-memberships.ts` — 04-11 `projectResponsibles`가 쓰는 오늘 소속 판정(확인만)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `rejectionSummary`·합계 행 요약 자리를 찾아 범위만 Read
  </read_first>
  <behavior>
    - `periodEditRights`: 수주중·진행·미수주 + 담당 PM → `pm` · + `projects.status` 권한 → `lead` · 정산 + 담당 PM(권한 없음) → `none` · 정산 + 권한 → `lead` · 완료 → 누구든 `none` · 담당이 아닌 PM → `none`
    - `validatePeriodChange`: 날짜 형식 틀림 → `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` · 종료 < 시작 → `종료일이 시작일보다 빠릅니다 · 종료일을 고쳐 주세요` · 진행 이후 + 시작일 비움 → `진행부터는 시작일이 있어야 합니다 · 시작일을 적어 주세요` · 진행 + 권리 `pm` + 새 종료 < 오늘 → `종료일은 오늘부터 적을 수 있습니다 · 앞당기려면 팀장 {이름}`(이름 없으면 `종료일은 오늘부터 적을 수 있습니다`)
    - `previewPeriodChange`: 정산 + 새 종료 ≥ 오늘 → `저장하면 진행으로 돌아감` · 진행 + 새 종료 < 오늘 → `저장하면 정산이 됨` · 진행 이후 + 종료 비움 → `종료일이 비어 시작일로 저장됨` · 그 밖 → null
    - `resolvePeriodSave`: 진행 이후 + 종료 비움 → 종료 = 시작 · 정산 + 새 종료 ≥ 오늘 → `statusChange: { from: 정산, to: 진행 }` · 수주중 + 종료 비움 → 그대로 비움
  </behavior>
  <action>
① `test/unit/domain/project-period.test.ts`에 `behavior` 네 묶음을 결정표로 먼저 쓴다(RED). `period.ts`에 `validatePeriodChange({ status, rights, start, end, todayKst, teamLeadName })` → 칸별 오류 `{ field: "start" | "end"; reason }[]`와 `previewPeriodChange({ status, newStart, newEnd, todayKst })` → 힌트 문자열 또는 null을 더하고, Task 1의 두 함수를 표 전체로 넓힌다(GREEN). `ledger.ts`는 Task 1에서 게이트 ctx에 빈 오류 목록을 넘기던 자리를 `validatePeriodChange` 결과로 바꾼다(팀장 이름은 04-11 `projectResponsibles`).

② **화면.** `period-field.tsx`: Enter 기본 제출을 막고, 칸 안에서 Ctrl+S는 1차 저장과 같은 `handleSave`를 부른다. Esc는 편집 중 값을 되돌리고 두 칸이 원래 값이면 묶음을 닫는다. 힌트 한 줄은 `previewPeriodChange` 결과가 있을 때만(`Form.Hint`). 서버 칸 오류는 `Form.Error`(`aria-invalid`)로 칸 아래에. `quote-table.tsx`: 기간 칸 오류로 거부되면 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`(U-6). 날짜에는 쉼표가 없다(D-95 예외 — `lib/format-number`를 쓰지 않는다).

③ `test/integration/project-period.test.ts`(신규): (a) 팀장이 정산 프로젝트의 종료일을 오늘 이후로 늦추면 진행 + 로그 1줄(행위자 = 그 팀장, `trigger: end_date_extended`), (b) 진행의 담당 PM이 어제로 앞당기면 거부되고 DB의 기간·상태 무변경, (c) 팀장이 진행의 종료일을 어제로 앞당기면 저장 직후 정산 + **시스템** 로그 1줄(`actor_id` null) — (a)와 (c)가 행위자를 구분해 단언한다(금지 항목 1), (d) 진행 이후 종료일 비움 → 시작일로 저장, (e) 기간 오류와 견적 줄 변경이 같은 저장에 있으면 견적 줄도 저장되지 않는다(DB 재조회), (f) 완료 프로젝트의 기간 저장은 누구든 거부, (g) `now`를 자정 뒤로 주입한 기간 없는 `saveProjectLedger`(견적 줄만)의 판정이 정산 기준이다 — 04-12 전이라 정산도 이 시점엔 저장이 통과하므로, 상태 값이 정산으로 바뀐 것과 시스템 로그 1줄을 단언한다(옛 04-11 통합 (g)가 여기로 왔다). E2E(`project-period.spec.ts`): PM의 이른 종료일 오류 문구와 표 합계 행 U-6 문구 · 팀장의 `저장하면 정산이 됨` 힌트 → 저장 → 정산 태그.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-period.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 권리·검증·미리보기·저장 해석 결정표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-period.test.ts test/integration/project-auto-settlement.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 저장이 한 트랜잭션 규칙을 깨거나, 행위자가 섞였거나, 기존 합성 저장이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 네 묶음을 전부 덮는다
    - 화면 컴포넌트와 `ledger.ts`가 같은 `period.ts` 함수를 import하고, 힌트 문자열이 화면 코드 리터럴이 아니라 `previewPeriodChange` 반환값이다
    - 정산→진행 되돌리기 로그의 행위자가 사람이고 자동 정산 로그의 행위자는 시스템이다(통합 a·c가 둘을 구분해 단언)
    - 진행의 PM이 이른 종료일을 서버에서도 저장하지 못한다(통합 b, DB 재조회)
    - 기간 오류가 있는 저장에서 견적 줄·매출도 저장되지 않는다(통합 e, DB 재조회)
    - 합성 저장이 게이트 판정 전에 `applyAutoSettlement`를 부른다 — `deps.now` 주입으로 자정 뒤 판정을 재현한다(통합 g, 가짜 타이머 없음)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>기간을 권리대로 고치고, 결과 미리보기가 서버 저장과 같은 판정으로 보이며, 정산 연장·앞당김·종료일 기본값이 한 트랜잭션 저장으로 동작하고 행위자가 섞이지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 상태 모달 연결(「기간 적기」 · 종료일 지남 줄) + 화면 검증(기간 칸이 열린 375 머리 줄)</name>
  <files>app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 374행(확인 — 진행으로 바꾸기의 종료일 지남 줄) · 1244–1247행(막힘 3차 「기간 적기」)
    - `app/(app)/projects/[id]/status-change.tsx`(04-21 산출물 전문)
    - `domain/projects/status.ts`의 `isEndDatePassed`(04-11)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - 시작일 없는 수주중에서 팀장이 진행을 고르면 막힘 줄 옆 3차 「기간 적기」가 있고, 누르면 모달이 닫히고 기간 칸이 열려 시작일에 포커스가 있다
    - 기간 권리가 없는 사람에게는 막힘 줄에 게이트 문자열만 있고 3차가 없다
    - 종료일이 이미 지난 수주중을 진행으로 바꾸는 확인 모달 본문에 `종료일이 지나 바로 정산이 됩니다 · 늦추려면 기간 바꾸기`가 있다
  </behavior>
  <action>
① **상태 모달 연결.** 04-21은 `status-change.tsx`를 페이지가 만든 ReactNode(`statusControl`)로 넘겼다. 이 태스크에서 `QuoteLedger`가 그 컴포넌트를 props로 받아 **안에서** 렌더하도록 바꿔(`statusControl` → `statusChangeProps`), 막힘 줄의 3차 「기간 적기」가 모달을 닫고 Task 1의 `openPeriodField("start")`를 부르게 한다(기간 칸을 고칠 권리가 없으면 3차 없이 게이트 문자열만). `[id]/page.tsx`가 기간 권리와 04-11의 `isEndDatePassed` 결과를 `statusChangeProps`에 싣는다. 진행으로 바꾸기 확인 모달 본문에 종료일이 이미 지났을 때의 줄 `종료일이 지나 바로 정산이 됩니다 · 늦추려면 기간 바꾸기`를 더한다(Copywriting).

② **E2E(`project-period.spec.ts`).** `behavior` 세 줄을 케이스로 더한다(「기간 적기」 포커스 · 권리 없는 사람의 3차 부재 · 종료일 지남 줄). 옛 04-11이 `project-lifecycle.spec.ts`에 두려던 종료일 지남 줄 케이스가 이것이다(`<probe_fallback>`).

③ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S3 overflow backstop(기간 칸이 열린 375 머리 줄 가로 스크롤 0 · 칸 묶음이 머리 줄 아래)을 DOM 실측 → 필요하면 `project-detail.module.css`만 고친다 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts test/e2e/project-lifecycle.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 「기간 적기」가 기간 칸으로 이어지지 않거나 04-21의 상태 모달 흐름이 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S3 overflow 판정(기간 칸이 열린 375 머리 줄)을 SUMMARY에서 확인</test>
      <expected>가로 스크롤 0, 칸 묶음이 머리 줄 아래, 버튼이 숨지 않음</expected>
      <why_human>폭·배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 「기간 적기」가 모달을 닫고 시작일 칸에 포커스한다(E2E) · 권리 없는 사람에게는 3차가 없다(E2E)
    - 진행 확인 모달의 종료일 지남 줄이 `isEndDatePassed`가 참일 때만 보인다(E2E)
    - `QuoteLedger`가 `statusChangeProps`로 상태 바꾸기를 안에서 렌더하고 `statusControl` ReactNode prop이 남지 않았다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 모달의 막힘이 기간 칸으로 이어지고 종료일이 지난 수주중의 결과가 모달에서 미리 보이며, 기간 칸이 열린 머리 줄이 375에서 넘치지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(기간) | 시작일·종료일이 넘어온다. 권리·검증·정산 되돌리기 판정이 서버에만 있어야 한다 |
| 기간 칸 → 상태 | 날짜 한 칸이 상태를 바꾼다(정산→진행 · 진행→정산). 상태 전환 권한의 우회로가 될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-77 | Elevation of Privilege | 기간 칸으로 상태 바꾸기 | high | mitigate | 정산→진행(D-80)은 `project.period-edit` 게이트가 권리 `lead`(팀장 이상)일 때만 통과하고, 진행의 PM은 오늘보다 이른 종료일을 서버에서 저장할 수 없다(⚠ S13). 두 거부를 통합 테스트(a·b)로 고정한다 |
| T-04-78 | Spoofing | 되돌리기 행위자(사람 쪽) | medium | mitigate | 정산→진행 되돌리기는 요청한 viewer로 기록되고 자동 정산은 `SYSTEM_VIEWER`(04-11)로 기록된다 — 통합 테스트 a·c가 두 로그의 행위자를 구분해 단언한다(금지 항목 1) |
| T-04-80 | Tampering | 기간 동시 편집 | medium | mitigate | 기간 갱신이 프로젝트 `version` 조건부라 다른 사람의 사이 편집을 덮지 않는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 독립 DOM 감사(기간 칸이 열린 375 머리 줄) 판정이 SUMMARY에 있다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 팀장이 종료일을 늦춰 정산을 진행으로 되돌리고, PM은 종료일로 상태를 바꿀 수 없다
- 기간 칸의 미리보기가 서버 저장과 같은 판정이고, 기간 저장이 견적 줄·매출과 한 트랜잭션이다
- 상태 모달의 「기간 적기」가 기간 칸으로 이어진다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/projects/period.ts` — `periodEditRights` · `validatePeriodChange` · `previewPeriodChange` · `resolvePeriodSave` |
| 함수(신규, repository) | `updateProjectPeriodIfVersion`(`repositories/projects.ts`) |
| 게이트 규칙 | `project.period-edit` |
| 로그 detail 규약 | `status_change` detail `{ from, to, trigger: "end_date_extended" }`(사람 행위자 — 자동 쪽 `end_date_passed`는 04-11) |
| 입력 계약 | `SaveProjectLedgerInput.period` · `saveProjectLedgerAction` 스키마 `period` |
| 변경된 기존 심볼 | `saveProjectLedger`(판정 전 자동 전환 선판정 · 커밋 뒤 재판정 · `deps.now`) |
| 컴포넌트 | `app/(app)/projects/[id]/period-field.tsx`(신규) · `QuoteLedger.openPeriodField` · `statusChangeProps`(04-21의 `statusControl` 대체) |
| 테스트 | `test/unit/domain/project-period.test.ts` · `test/integration/project-period.test.ts`(신규) · `test/e2e/project-period.spec.ts`(04-11 파일 확장) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md` when done
</output>
