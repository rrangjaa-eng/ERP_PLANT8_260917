---
phase: 04-project-quote-ledger
plan: 22
type: execute
wave: 10
depends_on: ["04-11"]
files_modified:
  - domain/projects/period.ts
  - domain/rules/register.ts
  - domain/projects/ledger.ts
  - repositories/projects.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/period-field.tsx
  - ui/table/use-dirty-storage.ts
  - test/unit/domain/project-period.test.ts
  - test/unit/ui/dirty-storage.test.ts
  - test/integration/project-period.test.ts
  - test/integration/tx-safety.test.ts
  - test/e2e/project-period.spec.ts
  - test/e2e/ledger-save-flow.spec.ts
autonomous: true
requirements: [PROJ-04, UX-04]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "상세 부제에 `기간 2026-09-12 ~ 2026-09-16`(없으면 `기간 미정`, 한쪽만 있으면 `2026-09-12 ~ —`)이 보이고, 고칠 수 있는 사람에게만 바로 오른쪽에 3차 「기간 바꾸기」가 있다 — 칸을 열면 빈 입력 둘(자리표시자 `2026-09-18`)이고 칸은 항상 둘이다(S13 empty · long-text 해당 없음)"
    - "기간을 고칠 수 있는 사람: 수주중·진행·미수주 = 담당 PM + 팀장 이상(`projects.status`) · 정산 = 팀장 이상만 · 완료 = 아무도. 업무 범위가 `자기 팀`인 팀장은 **자기 팀 프로젝트에서만** 팀장 권리를 갖는다 — 다른 팀 프로젝트는 담당 PM이 아니면 권리가 없다. 담당 PM의 권리는 `projects` 쓰기가 있을 때만이다 — 쓰기를 회수당한 담당 PM은 기간도 못 고친다(해소 CEO-D14 · D11 · D20 — UI-SPEC rev 5 `## UI Considerations` S13, 엔지 리뷰 A P3)"
    - "진행의 담당 PM은 **저장될** 종료일을 오늘(KST) 이후로만 만들 수 있다 — 이른 날짜를 적거나 종료일을 비워 과거 시작일로 저장되게 하는 저장 모두 칸 아래 `종료일이 오늘보다 빠름 · 앞당기기는 팀장 {이름}`(팀장이 없으면 `종료일이 오늘보다 빠름`) + 화면 전체 거부이고, 서버도 같은 규칙으로 거부한다(해소 CEO-D14, UI-SPEC rev 5 Copywriting `Error — 기간 칸`, CEO A-02)"
    - "정산에서 팀장 이상이 종료일을 오늘 이후로 늦춰 저장하면 같은 트랜잭션에서 진행으로 돌아가고 `status_change` 로그가 그 사람 이름으로(detail에 `endDate: { from, to }`) 한 줄 남는다 — 새 종료일이 지나면 다시 정산이 된다(D-80, CEO A-01·A-16)"
    - "기간을 바꾼 저장은 누가 무엇을 바꿨는지 `document_update` 로그 한 줄(detail `startDate`·`endDate`의 `{ from, to }`)을 같은 트랜잭션에 남긴다 — 팀장이 진행의 종료일을 앞당겨 정산이 되면 「누가 정산을 일으켰나」에 이 줄이 답한다(CEO A-16)"
    - "진행 이후 상태에서 종료일을 비우고 저장하면 시작일 값으로 저장되고, 진행 이후 상태에서 시작일을 비우는 저장은 `진행부터는 시작일이 있어야 합니다 · 시작일을 적어 주세요`로 거부된다(D-82, rev 5 Copywriting `Error — 기간 칸`)"
    - "`2026-02-30`처럼 달력에 없는 날짜는 칸 아래 `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요`로 거부되고 DB 오류가 되지 않는다 · 다른 사람이 먼저 기간을 바꿨으면 `다른 사람이 먼저 기간을 바꿈 · 새로 고침`이다(CEO A-22, rev 5 Copywriting `Error — 기간 칸` · F1 명사형)"
    - "기간 충돌은 **화면이 읽은 {시작일, 종료일} 기준값**과 잠근 행의 기간을 비교해 판정한다 — `projects.version`(상태 변경·자동 정산도 올리는 값)을 쓰지 않으므로, 화면을 연 뒤 동료가 상태를 바꾸거나 자정이 지나 자동 정산이 돌아도 기간이 그대로면 헛충돌이 없다. 저장 결과는 `{ status, startDate, endDate }`를 돌려주고 화면이 그 값으로 기준값을 바꾼다 — 새로 고침 없이 기간을 두 번 저장해도 둘 다 통과한다(엔지 리뷰 A §1 P1)"
    - "기간과 견적 줄(·매출)을 한 번에 저장하면 판정 순서가 고정이다 — 기간 쓰기 → 같은 트랜잭션에서 재판정(자동 정산 · 정산→진행 되돌리기) → 그 **새 행**으로 견적 줄·매출을 판정한다. 종료일을 늦춰 정산을 진행으로 되돌리며 단가를 고친 저장은 진행 규칙으로, 종료일을 앞당겨 정산을 만들며 단가를 고친 저장은 정산 규칙으로 판정된다(ENG-D6 — 저장 뒤 상태로 판정)"
    - "기간 칸의 결과 미리보기 힌트 한 줄은 **저장될 값**(종료일 기본값을 적용한 뒤)으로 판정한다 — `저장하면 진행으로 돌아감`(정산 + 새 종료일 ≥ 오늘) · `저장하면 정산이 됨`(진행 + 저장될 종료일 < 오늘 — 종료일을 비워 과거 시작일이 되는 경우 포함) · `종료일이 비어 시작일로 저장됨`(진행 이후 + 종료일 비움, 정산이 되지 않을 때). 판정은 서버 저장과 같은 domain 함수다(SYSTEM.md 개정 ⑨, CEO A-02, rev 5 Copywriting `표시 — 기간 칸 결과 미리보기`)"
    - "기간 칸은 `Form.Actions`가 없는 칸 폼이다(총 매출 예상가 칸도 같은 형태 — 04-44) — 고치면 dirty가 되어 화면 1차 「일괄 저장 N」의 N에 바뀐 칸 수만큼 합류하고 견적 줄·매출과 같은 트랜잭션으로 저장된다. Enter 기본 제출은 막히고 Ctrl+S가 저장이다. `Esc`는 편집 중 값을 되돌리고 칸이 모두 원래 값이면 묶음을 닫으며, 묶음이 닫히면 포커스가 그 칸을 연 3차로 돌아온다(S13 · S17, §7-15, CEO A-34)"
    - "1차 「일괄 저장」은 서버가 계산한 `canSave`(편집 가능 칸 · 기간 권리 · 매출 기록 권리 · 완료가 아닌 계약 편집 중 하나라도 — 총 매출 예상가 권리 항은 04-44가 더한다)가 참일 때만 렌더된다 — 한 화면에 쓰기 권리가 갈려도 1차는 하나이고 판정은 서버가 칸마다 한다. 권한 메뉴가 없는 팀장도 기간을 저장할 수 있고, 완료 프로젝트의 담당 PM에게는 1차가 없다(CEO A-12, UI-SPEC rev 5 S19 partial · 「프로브 항목 밖」 U-5)"
    - "N = 0이면 1차는 `일괄 저장` + kbd `Ctrl+S`, 비활성(`aria-disabled` — 포커스가 남는다) + 이유 `바뀐 칸 없음`(`reasonTone: \"info\"` → `--muted`)이다 — 옛 `바뀐 칸 없음 · 고칠 칸을 눌러 주세요`를 쓰지 않는다(DR-10 · DR-11 · P0, rev 5 Copywriting `Primary CTA — 견적 원장` · 「프로브 항목 밖」 N = 0)"
    - "기간 칸 오류로 저장이 거부되면 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`이 붙는다(U-6 — 총 매출 예상가 칸 오류도 같은 자리, 04-44)"
    - "저장 요청은 화면이 본 상태 `seenStatus`를 싣고, 서버는 합성 저장 트랜잭션의 **잠금 직후 첫 판정 행**(`loadProjectForGate` 반환 — 자정 자동 정산 반영)과 비교해 다르면 **저장 전체를 거부**한다 — 합계 행 오른쪽 `상태가 {상태}로 바뀜 · 전부 거부`(`--danger`, 상태 이름은 서버 코드표 라벨), 트랜잭션 전체가 롤백돼 줄·매출·기간(04-44 뒤에는 총 매출 예상가도)이 하나도 바뀌지 않는다. 사람 자신의 기간 변경으로 같은 저장 안에서 상태가 바뀌는 것은 거부 사유가 아니다(비교는 기간 쓰기 전)(DR-6 · 계약 4, UI-SPEC rev 5 Copywriting `Error — 저장(상태가 바뀜, DR-6)` · S19 error)"
    - "상태 바뀜으로 거부되면 화면이 새 상태로 다시 그려진다 — 머리 줄 상태 태그 · 셀 단계 · 컨트롤이 서버 값이 되고, 편집은 브라우저 보관본에 남아 표 위 복원 줄 `저장 안 한 편집 N칸 · 복원 / 버림`으로 돌아오며, 합계 행의 거부 줄은 남는다. 새 상태에서 허용되지 않는 칸은 복원 뒤 다음 저장에서 셀 오류가 된다(DR-6, S19 error · S18 현재 차수)"
    - "미저장 편집은 사용자가 칸을 바꾸는 순간(셀 커밋 · 붙여넣기 · 줄 추가·삭제 · 표 밖 칸 입력) 브라우저 보관본(키 = 프로젝트 id + 차수 id)에 쓰이고, 「복원」은 그 편집을 dirty 모양으로 다시 들여오며 「버림」은 확인 없이 지운다 — 서버 값으로 다시 그리는 것은 보관본을 건드리지 않고, 저장 성공만 보관본을 지운다(D-68, UI-SPEC rev 5 S18 · S19 성공)"
    - "날짜는 식별자형 숫자라 쉼표가 없다(D-95 예외)"
    - "일괄 저장(합성 저장)은 트랜잭션 **안에서** 프로젝트 행을 배타 잠금하고 04-11의 `loadProjectForGate`로 자동 전환을 먼저 판정한 뒤 게이트를 돈다 — 자정을 넘겨 열어 둔 편집 화면의 저장은 자동 정산이 반영된 행으로 판정되어 `seenStatus`가 다르면 전부 거부되고, 같은 트랜잭션이 자기 잠금을 기다리며 멈추지 않는다(D-76, CEO A-13·OV-5, DR-6)"
    - "일괄 저장에 실린 견적 줄 차수가 그 프로젝트의 차수가 아니면 아무것도 쓰지 않고 `차수와 프로젝트가 맞지 않음 · 새로 고침`으로 거부된다(CEO A-14, rev 5 Copywriting `Error — 저장(순서·소속, 방어)`)"
    - "기간이 실린 합성 저장의 트랜잭션 안에서는 풀을 부르지 않는다 — 권리의 사실(`projects.status`·`projects` 쓰기 권한 · 업무 범위·오늘 소속 · 기간 거부 문구의 팀장 이름)은 트랜잭션을 열기 **전에** 읽고, 상태 바뀜 거부 문구의 라벨은 롤백 **뒤** 트랜잭션 밖에서 읽는다. 풀 크기 2에서 서로 다른 프로젝트의 기간 저장 셋을 동시에 보내면 10초 안에 셋 다 성공한다(ENG-D3 ① — ARCHITECTURE §4-8)"
    - "기간·상태 바뀜 E2E는 계급 권한·정보 노출을 **손으로 켜지 않는다** — 04-20 시드만으로 같은 팀 팀장이 기간을 고치고 머리 줄을 본다(ENG-D2)"
  prohibitions:
    - statement: "시스템이 한 상태 전환을 사람의 이름으로 기록하거나, 사람이 한 전환을 시스템 이름으로 기록해서는 안 된다. 자동 정산은 누구의 판단도 아닌 날짜의 결과이고, 정산→진행 되돌리기는 그 날짜를 고친 사람의 판단이다 — 행동 로그가 이 둘을 섞으면 「누가 정산을 풀었는가」에 답하지 못한다"
      status: resolved
      verification: test
    - statement: "PM이 종료일 한 칸으로 제 손으로 되돌릴 수 없는 상태 전환(진행 → 정산)을 일으켜서는 안 된다. 상태 전환은 팀장의 일이라는 D-46의 취지를 기간 칸이 우회하는 경로가 되지 않게, 진행의 PM은 **저장될** 종료일이 오늘보다 이른 저장(이른 날짜 · 종료일 비움 + 과거 시작일)을 서버에서도 할 수 없다"
      status: resolved
      verification: test
    - statement: "화면이 본 상태와 서버 상태가 다른 저장이 일부만 성공해서는 안 된다. 자정 자동 정산·동료 전환 뒤의 오래된 화면이 새 상태 규칙으로 통과하는 칸만 저장하면 사용자는 무엇이 저장됐는지 모른다 — 저장 전체를 거부하고 편집은 보관본으로 돌려준다(DR-6)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/period.ts"
      provides: "기간 편집 권리·검증·결과 미리보기·저장 해석의 순수 함수(화면 힌트와 서버 저장이 같은 함수, 검증·미리보기는 해석 결과 위에서) — 04-44의 총 매출 예상가 칸도 같은 권리 함수"
      exports: ["periodEditRights", "validatePeriodChange", "previewPeriodChange", "resolvePeriodSave"]
    - path: "app/(app)/projects/[id]/period-field.tsx"
      provides: "머리 줄 아래 기간 칸 묶음 — Form.Actions 없음, dirty 합류, 미리보기 힌트"
      contains: "previewPeriodChange"
    - path: "test/unit/domain/project-period.test.ts"
      provides: "권리(팀 범위 포함) · 검증(달력·해석 뒤 값) · 미리보기 · 저장 해석 결정표"
      min_lines: 50
    - path: "test/integration/project-period.test.ts"
      provides: "정산 연장(사람 로그) · PM 앞당김·비움 거부 · 팀장 앞당김 즉시 정산(시스템 로그) · 기간 변경 로그 · 종료일 기본값 · 한 트랜잭션 전부 거부 · 완료 거부 · 다른 팀 팀장 · 차수 불일치 · 자정 넘긴 편집의 상태 바뀜 거부 · 동료 전환 뒤 상태 바뀜 거부 · 정산 대 연장 경합 · 로그 실패 롤백(총 매출 예상가 저장·거부는 04-44가 같은 파일에 더한다)"
      min_lines: 110
    - path: "test/e2e/ledger-save-flow.spec.ts"
      provides: "저장 흐름(S19) — 상태 바뀜 거부 → 태그 다시 그림 → 복원 줄로 편집 회수(이 플랜) · 저장 중 잠금(04-30이 더한다)"
      min_lines: 30
  key_links:
    - from: "domain/projects/ledger.ts"
      to: "domain/projects/period.ts"
      via: "기간 저장이 화면 미리보기와 같은 해석 함수로 종료일 기본값·정산→진행을 정하고, 검증은 해석 결과 위에서 돈다"
      pattern: "resolvePeriodSave"
    - from: "app/(app)/projects/[id]/period-field.tsx"
      to: "domain/projects/period.ts"
      via: "힌트 한 줄을 서버 저장과 같은 함수로 만든다(개정 ⑨)"
      pattern: "previewPeriodChange"
    - from: "domain/projects/ledger.ts"
      to: "domain/projects/auto-transition.ts"
      via: "합성 저장이 트랜잭션 안에서 `loadProjectForGate`로 잠그고 판정한다 — 풀에서 따로 UPDATE하지 않는다. `seenStatus`는 이 첫 판정 행과 비교한다"
      pattern: "loadProjectForGate"
    - from: "app/(app)/projects/actions.ts"
      to: "domain/projects/status.ts"
      via: "상태 바뀜 거부 문구가 04-20의 `statusChangedMessage(label, \"전부 거부\")` 한 함수에서 온다(롤백 뒤 라벨 조회)"
      pattern: "statusChangedMessage"
    - from: "domain/projects/period.ts"
      to: "domain/projects/status.ts"
      via: "팀장 권리가 `actorCoversProjectTeam`(04-20) 결과를 입력으로 받는다 — 전환·기간·총 매출 예상가·버튼 표시가 한 규칙"
      pattern: "actorCoversTeam"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/table/use-dirty-storage.ts"
      via: "편집 순간 `persist`, 「복원」 반환값 병합, 상태 바뀜 거부 뒤 `recount` — 복원 줄이 실제로 편집을 돌려준다(D-68 · DR-6)"
      pattern: "persist("
---

<objective>
**As a** 기획 PM·팀장, **I want to** 상세 머리 줄 아래에서 프로젝트 기간을 권리대로 고치고, 저장 전에 그 결과(정산으로 넘어감 · 진행으로 돌아감 · 종료일 기본값)를 한 줄로 미리 보며, 행사가 늘어나면 팀장이 종료일을 늦춰 정산을 진행으로 되돌리기를, **so that** 날짜 한 칸이 상태를 바꾸는 규칙이 화면과 서버에서 같고, PM은 기간 칸으로 상태 전환 권한을 우회하지 못한다.

**2026-09-23 체커 1차 분할.** 옛 04-11(23파일)을 둘로 나눈 **화면 쪽 — 상세 기간 칸(S13)** 이다(D-80 · D-82의 저장 규칙 · ⚠ S13 기본값 둘 · S7 「기간 적기」 연결). 날짜로 움직이는 서버 쪽(자동 정산 · 읽기·쓰기 경로 선판정 · 종료일 지남 · 담당자 이름 출처)은 04-11이, KST 날짜 모듈은 04-29가, 잠금·로그 트랜잭션·업무 범위 판정은 04-20·04-27이 먼저 만들었다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE).** A-01(되돌리기 로그를 합성 저장 tx로) · A-02(검증·미리보기를 해석 뒤 값으로 — 종료일 비움 우회 차단) · A-12(`canSave` 서버 계산) · A-13(선판정을 tx 안 잠금 읽기로) · A-14(차수·프로젝트 일치) · A-16(기간 변경 로그) · A-22(달력 검사·충돌 문구) · A-31(04-21의 `StatusChangeProps`를 넓힘 — ReactNode 재작업 없음) · A-34(포커스 복귀·새로 고침 안내) · 사용자 D14(기본값 확정) · 사용자 D11·D20(팀장 권리는 자기 팀) · OV-5(편집 화면 자정 넘김 · 정산 대 종료일 연장 경합 · 로그 실패 테스트). 상세는 끝의 Review Dispositions Ledger.

**2026-09-23 체커 revision 1 분할(B5 — 21파일 · 128k · 네 관심사).** 옛 Task 3(상태 모달의 「기간 적기」·「기간 바꾸기」 연결 · 총 매출 예상가 칸(계약 8 · DR-28 · DR-37) · 폰 부제 순서(DR-26) · 머리 줄 독립 DOM 감사(DR-14))을 **04-44**로 내용 그대로 옮겼다. 이 플랜에는 기간 칸 + 게이트(Task 1 · Task 2 ①②③⑥⑦) · 상태 바뀜 전부 거부(DR-6 — `seenStatus`, Task 2 ④⑤) · 미저장 보관 배선 수리(D-68 — `persist`·「복원」 병합·`recount`, Task 2 ⑤)가 남는다 — 15파일 · 88k. 사슬은 04-11 → 04-22 → 04-44 → 04-12로 곧게 이어진다(04-12의 `depends_on`만 04-44로 바뀐다). **D-68 `persist` 배선의 유일한 구현 주인은 이 플랜 Task 2 ⑤다** — 04-44(총 매출 예상가 키)·04-30(새 줄 uuid 키)·04-24(이전 차수 복원 줄)는 그 배선을 넓히거나 전제로 단언할 뿐 다시 만들지 않는다. 머리 줄의 독립 DOM 감사(기간 칸·총 매출 예상가 칸이 열린 머리 줄 — 1280 · 1024 · 375)는 두 칸이 모두 선 04-44에서 한 번 한다(CLAUDE.md 「전체 게이트를 두 번 돌리지 않는다」).

Purpose: 04-30(정산 편집 화면)의 EMPTY 「기간 바꾸기」와 04-44의 상태 모달 3차가 이 플랜의 `openPeriodField`와 `periodEditRights`를 쓰고, D-80이 정산을 되돌리는 유일한 길이 이 칸이다. 04-12의 견적 줄 저장은 이 플랜이 합성 저장 tx 안에 세운 잠금 읽기와 `seenStatus` 비교를 그대로 쓴다.

Output: 기간 편집 순수 함수 모듈 · 게이트 규칙 `project.period-edit` · 합성 저장의 `period` 입력과 tx 안 선판정 · 필수 `seenStatus`와 상태 바뀜 전부 거부 · 기간 칸 컴포넌트 · 미저장 보관 배선(`persist` · 「복원」 병합 · `recount`) · 단위 2 · 통합 1 · E2E 2(04-11 스펙 확장 · `ledger-save-flow.spec.ts` 신규).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`).** (a) **엔지 리뷰 A §1 P1 — 기간 낙관 검사:** 옛 계획은 `projects.version`을 기간 충돌 표지로 썼는데 그 값은 자동 정산(04-11)·상태 전환(04-20)도 올린다 — (j)의 두 순서와 「자정 뒤 저장」이 전부 헛충돌이 되고, 결과가 버전을 돌려주지 않아 새로 고침 없는 둘째 저장이 늘 충돌했다. 화면이 읽은 기간을 기준값으로 비교하고 결과에 `{ status, startDate, endDate }`를 돌려준다(테스트 m·m2). (b) **ENG-D6:** 기간 + 줄의 판정 순서를 「기간 쓰기 → 재판정 → 새 행으로 줄·매출」로 고정한다(테스트 n — 정산 규칙으로 판정되는 결과는 정산 편집 매트릭스를 만드는 04-12가 단언). (c) **ENG-D3 ①:** 기간 권리의 사실을 트랜잭션 전에 읽고, 풀 크기 2 동시 기간 저장 셋 케이스를 `tx-safety.test.ts`에 더한다. (d) **엔지 리뷰 A P3:** `periodEditRights`의 `pm`은 `projects` 쓰기가 있을 때만. (e) **ENG-D2:** E2E가 권한·노출을 손으로 켜지 않는다. (f) **엔지 리뷰 B:** 거부 운영 로그는 04-20의 `denyWrite` 한 함수로.

**2026-09-23 디자인 리뷰 반영(`docs/designs/plant8-erp-phase4-design-review-260923.md`, UI-SPEC rev 5).** (g) **DR-6 · 계약 4 — 이 플랜이 서버 쪽 소유자:** 합성 저장의 잠금 직후 첫 판정(`loadProjectForGate` — 이 플랜이 세운 트랜잭션 첫 단계)이 `seenStatus` 비교가 서야 할 유일한 자리이고(기간 쓰기 **전**이라 사람 자신의 기간 변경은 거부 사유가 아니다), 액션 스키마·결과 계약도 이 플랜이 연다 — 04-12는 그 뒤 (f) 자리에 견적 줄을 꽂을 뿐이라 여기서 한 번 서면 04-12·그룹 B의 매출 쓰기가 같은 보호를 받는다. 화면 쪽(다시 그림 · 복원 줄)과 E2E도 여기서 끝낸다 — 그 전제인 미저장 보관 배선(D-68)이 04-04 실행분에 비어 있어 같은 태스크가 잇는다(ENG-D11). (h) **계약 8 · DR-28 · DR-37:** 총 매출 예상가 칸을 기간 칸과 같은 형태·권리(`periodEditRights`)·같은 일괄 저장으로 — 체커 revision 1 분할로 **04-44**가 만든다. (i) **DR-10 · DR-11 · P0:** N = 0의 1차 이유 `바뀐 칸 없음`(`info`). (j) **P0 · rev 5 F1:** 기간 칸 오류 `종료일이 오늘보다 빠름 · 앞당기기는 팀장 {이름}` · 동시 수정 `다른 사람이 먼저 기간을 바꿈 · 새로 고침` · 차수 불일치 `차수와 프로젝트가 맞지 않음 · 새로 고침`. (k) **S7 연결:** 모달의 3차 「기간 적기」(`primary.nextStep`)와 결과 줄 `종료일 지남 · 바로 정산`의 3차 「기간 바꾸기」 — **04-44**가 이 플랜의 `openPeriodField`로 잇는다(결과 줄 글자 자체는 04-21이 rev 5로 만든다). (l) **DR-26:** 폰 부제 첫 항목 `기간 …` — **04-44**(폰 머리 줄 순서·「더보기」는 04-21). (m) **DR-14:** 머리 줄 DOM 감사 1280 · 1024 · 375 — **04-44**. 저장 중 편집 잠금(DR-3 · 계약 3)은 네 편집기(견적 격자 · 매출 표 · 기간 칸 · 총 매출 예상가 칸)가 모두 선 뒤 한 곳에서 거는 04-30이 소유한다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 시간은 `vi.useFakeTimers`로 얼리지 않고 `deps.now`로 주입한다(리포 관례, PATTERNS). 경합 순서는 `test/integration/lock-race.ts`(04-20)의 `deferred`·`waitForLockWaiter`로 정한다 — `sleep` 금지. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-11-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-21-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5 — rev 4 줄 번호는 쓰지 않는다) `## UI Considerations` 「S13」 587–596행 · 「S18」 638–648행 · 「S19」 649–657행 · `## 적용 규칙` 「S3」 1118–1155행 · 「S13」 1430–1456행 · 「S18」 1545–1558행 · 「S19」 1560–1576행 · `## Copywriting Contract` `Primary CTA — 견적 원장` 341행 · `Error — 표 배치 저장 실패(U-6)` 362행 · `Error — 저장(상태가 바뀜, DR-6)` 363행 · `Error — 저장(순서·소속, 방어)` 366행 · `Error — 기간 칸` 376행 · `표시 — 기간 칸 결과 미리보기` 413행 · `미저장 편집 복원(현재 차수)` 416행 (S7 · S17 · 총 매출 예상가 문구는 04-44)
# @.planning/phases/04-project-quote-ledger/04-46-SUMMARY.md — `Button`(`reasonTone`·`aria-disabled`)의 실제 props(계약 1 — 04-08 분할로 04-46이 만든다)
# docs/design/SYSTEM.md §7-15(Form.Hint 두 번째 쓰임 — 04-08 개정 ⑨)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 614–644행(부록 A 4.2 기간 저장 그림자 경로 · 4.3 비동기 순서 시나리오)
# docs/designs/plant8-erp-phase4-eng-review-260923.md 64–68행(R5 ENG-D6) · 116–140행(묶음 A — 기간 낙관 검사 · 기간 권리 쓰기 권한) · 부록 A §1 「period optimistic lock on projects.version」·「mixed batch」
# docs/ARCHITECTURE.md §4-8(04-32 — 트랜잭션·잠금 규약)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(기간 칸으로 움직이는 부분)** 다. 수용 기준은 D-80·D-82와 UI-SPEC S7·S13에서 끌어와 `must_haves.truths`에 적었다. S13의 다섯 상태 중 empty·loading·long-text(해당 없음)는 truths의 「칸은 항상 둘 · 서버 렌더 값」으로, error·partial은 truths로, S3 overflow는 기간 칸이 열린 상태의 backstop으로 옮겼다(옛 04-11에서 그대로).

**계획 단계 가정(사용자 확정 반영):**
- **⚠ S13 기간 편집 주체 — 사용자 D14 확정:** 수주중·진행·미수주 = 담당 PM + 팀장 이상, 정산 = 팀장 이상, 완료 = 아무도. 「팀장 이상」은 `projects.status` 쓰기 **그리고** `actorCoversProjectTeam`(04-20 — 업무 범위 `company`면 전사, `team`이면 오늘 발령 팀 = 프로젝트 팀)이 참일 때다(사용자 D11·D20).
- **⚠ S13 진행 중 PM의 종료일 앞당기기 — 사용자 D14 확정:** 진행의 담당 PM은 **저장될** 종료일(`resolvePeriodSave` 결과)이 오늘 이후여야 한다(CEO A-02 — 입력 칸 값이 아니라 해석 뒤 값으로 판정해야 「종료일 비움 → 과거 시작일로 저장」 우회가 막힌다).
- **「팀장 {이름}」의 출처:** 04-11의 `projectResponsibles`(업무 범위 `team` + `projects.status` 쓰기 계급의 오늘 발령자, 여럿이면 이름순 첫 사람). 없으면 이름 없는 문구로 떨어진다.
- **`canSave`의 편집 칸 항(A-12):** 이 플랜 시점에는 셀 단계 DTO(04-12)가 없어 기존 `editable` 값을 쓴다. 04-30이 `editable`을 셀 단계로 바꿀 때 같은 자리의 항을 「편집 가능 셀이 하나라도」로 바꾼다.
- **E2E 파일:** 기간 칸 케이스는 04-11이 만든 `project-period.spec.ts`를 넓힌다(「진행 확인 모달의 종료일 지남 줄」 케이스는 04-44가 같은 파일에 둔다). E2E 날짜는 전부 `kstToday(new Date())` + `addDays`(CEO A-18). **계급 권한·정보 노출을 켜는 호출(`upsertVisibility`·`upsertPermission`·`setPermissionCell`·`setVisibilityCell`)을 두지 않는다**(ENG-D2 — 시드는 04-20).
- **기간 기준값(엔지 리뷰 A §1 P1):** 요청의 `period`는 `{ startDate, endDate, baseline: { startDate, endDate } }` — `baseline`은 화면이 서버 렌더에서 받은 기간(또는 직전 저장 결과). 잠근 행의 기간과 기준값이 다르면 충돌이다. `projects.version`은 이 판정에 쓰지 않는다(상태 변경·자동 정산이 그 값을 올린다). 조건부 갱신 `updateProjectPeriod(…, { startDate, endDate, expected: baseline }, tx)`(`WHERE start_date IS NOT DISTINCT FROM $expectedStart AND end_date IS NOT DISTINCT FROM $expectedEnd`)는 잠금 뒤의 이중 안전장치다.
- **판정 순서(ENG-D6 — 사용자 결정 「저장 후 상태로 판정」):** 트랜잭션 전에 차수 소속(바뀌지 않는 사실)을 확인하고, 트랜잭션 안에서 ① 잠금 읽기·선판정(`loadProjectForGate`) ② 기간 판정·쓰기(되돌리기 포함) ③ 재판정(`loadProjectForGate` 한 번 더) ④ 견적 줄 ⑤ 매출 — ④·⑤는 ③이 만든 새 행으로 판정되고, 결과의 `project`도 그 행이다.
- **`seenStatus`(DR-6 · 계약 4 — 계획 단계 판단):** 비교 대상은 합성 저장 트랜잭션의 **첫** `loadProjectForGate` 반환 행이다(자정 자동 정산이 반영된 상태 — 04-11). 다르면 04-20의 `StatusChangedError`(상태 값)를 던져 트랜잭션 전체를 되돌린다 — 같은 tx에서 방금 한 자동 정산도 되돌아가지만 읽기 경로가 다음 렌더에 같은 판정을 다시 하므로 잃는 것이 없다(DB 무변경 — 계약 4). 문구의 라벨은 롤백 **뒤** 트랜잭션 밖에서 코드표로 찾는다(합성 저장은 늘 제 트랜잭션을 열어 바깥 tx가 없다). 액션은 이 거부만 `{ statusChanged: { message } }` 데이터로 돌려준다 — 화면이 오류 문자열을 해석해 갈래를 고르지 않게 하는 최소 판별자다. 권한 위반이 아니므로 `write.denied`를 남기지 않는다.
- **미저장 보관 배선(D-68 — 04-04 실행분 확인 결과):** 지금 `quote-table.tsx`는 `useDirtyStorage(projectId, revisionId, 0)`만 부르고 `persist`를 어디서도 부르지 않으며 「복원」의 반환값을 버린다 — 보관·복원이 실제로는 동작하지 않는다. 상태 바뀜 거부의 「편집은 복원 줄로 돌아온다」(계약 4)가 이것에 기대므로 Task 2 ⑤가 잇는다(ENG-D11 편차). 보관 규칙: 사용자가 칸을 바꾸는 순간에만 쓰고, 서버 값으로 다시 그리는 경로는 보관본을 건드리지 않으며, 저장 성공만 지운다. 새 줄 키는 지금 `clientKey`이고 04-30이 화면 uuid로 바꾼다(그룹 B 04-24의 이전 차수 복원 줄 DR-4도 이 배선 위에 선다).
- **방어 문구(rev 5에 없는 서버 거부 — 사용자 2026-09-23 승인):** `기간 바꾸기 권한 없음`은 화면이 권리 없는 사람에게 「기간 바꾸기」를 렌더하지 않아 위조 요청으로만 닿는다 — rev 5 명사형 `원인 · 다음 행동` 꼴을 따르고 SUMMARY 「새 문구」 표에 적는다. 총 매출 예상가(계약 8)의 판단과 사용자 확정(0 = 미입력 · 나중 저장이 이김)은 04-44 `<probe_fallback>`으로 옮겼다.
- **트랜잭션 전 사실(ENG-D3 ①):** 기간이 실린 저장은 트랜잭션을 열기 전에 `can(viewer, "projects.status", "write")` · `can(viewer, "projects", "write")` · `loadActorTeamScope`(04-20)를 읽고, 권리가 `pm`이 될 수 있는 사람(담당 PM)일 때만 거부 문구용 팀장 이름(`projectResponsibles`, 04-11)을 읽는다. 트랜잭션 안의 권리 판정은 이 사실과 잠근 행(상태·팀·담당 PM)만으로 순수하게 한다. 견적 줄·매출 쪽의 규약 준수는 04-12(견적 줄)·그룹 B(매출)가 한다 — 이 플랜 시점의 `saveQuoteLines`는 아직 트랜잭션 안에서 풀을 부른다(04-32 `<probe_fallback>`).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 팀장이 정산 프로젝트의 종료일을 늦춰 진행으로 되돌린다(순수 함수 → 게이트 → tx 안 잠금·판정 → 조건부 갱신 → 같은 tx 로그 → 액션 → 기간 칸 → 일괄 저장 → E2E)</name>
  <files>domain/projects/period.ts, domain/rules/register.ts, domain/projects/ledger.ts, repositories/projects.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/period-field.tsx, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## 적용 규칙` 「S13. 상세 기간 칸」 1430–1456행(자리 · 칸 폼 · dirty 합류 · Enter/Ctrl+S/Esc · 미리보기 세 줄 · 오류 · 저장 성공 · 기간 적기 진입 · 쉼표 예외) · `## UI Considerations` 「S13」 587–596행(해소 CEO-D14 둘) · `## Copywriting Contract` `Primary CTA — 견적 원장` 341행(N = 0 이유 `바뀐 칸 없음` · `info`)
    - `docs/design/SYSTEM.md` §7-15(`Form.Actions` 없는 칸 폼 · `Form.Hint` 미리보기 — 04-08 개정 ⑨)
    - `ui/form/Form.tsx`(전문) — `Form.Field`·`Form.Hint`·`Form.Error` 마크업
    - `domain/projects/ledger.ts`(전문) — 한 트랜잭션 합성 지점과 `deps` 형태
    - `domain/projects/auto-transition.ts`(04-11 — `loadProjectForGate`) · `domain/projects/responsibles.ts`(04-11) · `domain/projects/status.ts`(04-20 `actorCoversProjectTeam`·`updateProjectStatusIfCurrent` 호출 형태) · `lib/kst-date.ts`(04-29)
    - `domain/action-log/record.ts` 110–150행 — `recordAction`의 `tx` 인자(04-20)
    - `app/(app)/projects/actions.ts` 50–115행(`saveProjectLedgerAction` 스키마)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `dirtyCount`·`handleSave`·머리 줄(`styles.header`)·`rejectionSummary`·1차 버튼 렌더 조건(`editable ||`)을 찾아 그 범위만 Read(전문 Read 금지)
    - `test/e2e/project-period.spec.ts`(04-11 산출물) — 프로젝트를 DB에 상태·날짜로 두는 준비 형태
  </read_first>
  <action>
① `domain/projects/period.ts`(순수 — `lib/kst-date`와 04-06 전이표만 import, 리포지토리 import 없음 — 화면 클라이언트 컴포넌트도 이 파일을 import한다): 이 태스크는 트레이서에 필요한 둘만 먼저 만든다 — `periodEditRights({ status, isAssignedPm, canWrite, canChangeStatus, actorCoversTeam })` → `"lead" | "pm" | "none"`(사용자 D14 표 — `lead`는 `canChangeStatus && actorCoversTeam`일 때만, 사용자 D11·D20 · `pm`은 `isAssignedPm && canWrite`일 때만, 엔지 리뷰 A P3) · `resolvePeriodSave({ status, newStart, newEnd, todayKst })` → `{ startDate, endDate, statusChange? }`(정산 + 새 종료 ≥ 오늘 → `statusChange: { from: 정산, to: 진행 }`). 검증·미리보기는 Task 2.

② `domain/rules/register.ts`에 `project.period-edit`(ctx `{ rights, errors }`: 권리가 `none`이면 `기간 바꾸기 권한 없음`(rev 5에 없는 방어 문구 — 화면은 권리가 없으면 「기간 바꾸기」를 렌더하지 않는다, P0 명사형), 검증 오류가 있으면 첫 오류 이유)을 등록한다.

③ **합성 저장(`domain/projects/ledger.ts`) — 순서 고정(ENG-D6)·기간 기준값(엔지 리뷰 A P1)·트랜잭션 전 사실(ENG-D3 ①).** `SaveProjectLedgerInput`에 `period?: { startDate: string | null; endDate: string | null; baseline: { startDate: string | null; endDate: string | null } }`를 더한다(`version` 없음). **트랜잭션 전(풀):** 기간이 실렸으면 `<probe_fallback>`의 사실(두 `can` · `loadActorTeamScope`)을 읽는다. `quoteLines`가 있으면 그 `revisionId`의 차수를 읽어 이 `projectId` 소속인지 확인하고(차수가 어느 프로젝트의 것인지는 바뀌지 않는 사실이라 트랜잭션 밖에서 읽는다) 아니면 `denyWrite(viewer, "quote.revision-project", { projectId, revisionId }, new UserFacingError("차수와 프로젝트가 맞지 않음 · 새로 고침"))`로 아무것도 쓰지 않는다(A-14 — 그룹 B의 04-40이 같은 검사·문구를 재사용하고 「현재 차수인가」를 잠금 안에서 더한다). **`withTransaction` 콜백 안(tx만, 이 순서):** (a) `loadProjectForGate(viewer, projectId, { now, tx })`(04-11 — 행 배타 잠금 + 자동 전환 판정)로 잠근 행을 얻는다 — 풀에서 따로 UPDATE하지 않는다(A-13: 같은 요청의 tx가 잡은 잠금을 풀 연결이 기다리며 멈춘다). 기간이 없는 저장도 이 단계를 지난다. (b) (차수 소속은 트랜잭션 전에 끝났다.) (c) 기간이 있으면: 잠근 행의 기간이 `baseline`과 다르면 `다른 사람이 먼저 기간을 바꿈 · 새로 고침`(UserFacing, A-22 — rev 5 Copywriting `Error — 기간 칸` 원문) · 권리 `periodEditRights({ status: lockedRow.status, isAssignedPm, canWrite, canChangeStatus, actorCoversTeam: coversProjectTeam(scope, lockedRow.teamId) })`(04-20의 순수 비교) → `gate(lockedRow, "project.period-edit", { rights, errors })` — 검증 함수는 Task 2가 만들므로 이 태스크의 `errors`는 빈 목록이다. 거부면 `SaveRejectedError`와 같은 구조로 **칸 오류를 담아 전체 거부**(표 쪽은 저장하지 않는다)하고 `denyWrite(viewer, "project.period-edit", { projectId }, err)`(04-20 — 허용 목록 필드, 금액·날짜 없음, 사용자 D19-3·CEO 리뷰 B-26). (d) 통과면 `resolvePeriodSave` 결과로 `repositories/projects.ts`의 `updateProjectPeriod(viewer, id, { startDate, endDate, expected: baseline }, tx)`(기간이 기준값과 같을 때만 — 0행이면 (c)의 충돌 문구)와, `statusChange`가 있으면 04-20의 `updateProjectStatusIfCurrent(…, tx)`(정산→진행) + `recordAction(viewer, { actionType: "status_change", entity: "project", entityId, detail: { from, to, trigger: "end_date_extended", endDate: { from, to } } }, { tx })`(**사람 행위자, 같은 tx** — D-80, A-01·A-16). (e) **재판정:** 기간 쓰기 뒤 같은 tx에서 `loadProjectForGate`를 한 번 더 부른다 — 이미 잠근 행이라 곧바로 돌아오고, 새 종료일이 지난 진행이면 시스템 행위자 로그로 정산이 된다(「다음 판정 때 바로」를 같은 커밋에). (f) **그 다음에** 견적 줄 → 매출을 같은 `tx`로 저장한다 — 둘은 (e)가 만든 새 행으로 판정된다(ENG-D6 — 04-12 뒤 `saveQuoteLines`는 안에서 `loadProjectForGate`를 다시 불러 같은 잠긴 새 행을 읽는다). (g) (e)가 돌려준 행(견적 줄·매출 저장은 프로젝트 행을 바꾸지 않는다)의 `status`·`startDate`·`endDate`를 `SaveProjectLedgerResult`에 `project`로 더해 커밋 뒤 돌려준다. 커밋 뒤 풀에서 따로 자동 전환을 부르지 않는다. `app/(app)/projects/actions.ts`의 `saveProjectLedgerAction` 스키마에 `period`(날짜는 `YYYY-MM-DD` 문자열 또는 null, `baseline`도 같은 모양)를 더한다. 옛 계획의 `updateProjectPeriodIfVersion`은 만들지 않는다.

④ **화면(한 경로).** `app/(app)/projects/[id]/period-field.tsx`(클라이언트): 머리 줄 **아래**에 열리는 `Form` 한 묶음 — `Form.Field 시작일`(280) · `Form.Field 종료일`(280), 자리표시자 `2026-09-18`, **`Form.Actions` 없음**. 고치면 칸이 dirty(`--inset-dirty`)가 되어 `QuoteLedger`의 dirty 수에 칸 수만큼 합류하고 1차 「일괄 저장」이 같은 액션 호출에 `period`(기준값 = 서버 렌더에서 받은 기간 또는 직전 저장 결과)를 싣는다. 저장 성공 시 결과의 `project`로 기준값·부제·태그를 바꾸고(상태가 바뀌었으면 `router.refresh()`로 서버가 계산하는 권리·`canSave`를 다시 받는다), 600ms `--accent-weak` 틴트(`prefers-reduced-motion`이면 없음) 뒤 묶음이 닫힌다(토스트 없음). `[id]/page.tsx`가 권리(`periodEditRights`)·팀장 이름(04-11 `projectResponsibles`)·오늘(KST)을 계산해 넘기고, 부제에 `기간 …` + (권리가 있을 때만) 3차 「기간 바꾸기」를 둔다 — 칸이 열린 동안 부제의 기간 글자와 버튼은 숨긴다. **1차 렌더 조건(A-12):** `[id]/page.tsx`가 `canSave = editable || periodRights !== "none" || canWriteEntries || (canWriteContract && status !== 완료)`를 계산해 넘기고, `quote-table.tsx`의 1차 렌더 조건(`editable || canWriteContract || canWriteEntries`)을 `canSave` 하나로 바꾼다. **N = 0의 이유(DR-10 · DR-11 · P0):** 1차의 `dirtyCount === 0` 갈래 이유를 `바뀐 칸 없음` + `reasonTone="info"`(04-46 `Button` 개정 — `aria-disabled`라 포커스가 남는다), kbd를 `Ctrl+S`로 둔다 — 지금 이유 문자열(뒤에 사용법 꼬리가 붙은 꼴)을 바꾼다. 오류 칸이 있을 때의 1차 갈래는 건드리지 않는다(DR-5 — 오류로 이동하는 1차는 04-19). `quote-table.tsx`에는 그 밖에 기간 칸의 dirty 합류와 `openPeriodField(focus: "start" | "end")` 한 함수만 더한다(04-44의 상태 모달 3차와 04-30의 EMPTY 「기간 바꾸기」가 이 함수를 부른다).

⑤ **E2E(트레이서 증명, `project-period.spec.ts` 확장 — 권한·노출을 켜는 호출 없음, ENG-D2).** 준비에서 종료일이 `addDays(kstToday(new Date()), -1)`인 정산 프로젝트를 둔다 → 그 팀에 오늘 발령된 팀장으로 상세 → 「기간 바꾸기」 → 종료일을 `addDays(오늘, 7)`로 → 1차 「일괄 저장 Ctrl+S 1」 → 태그 `진행`, 부제 기간이 새 종료일 → **새로 고치지 않고** 다시 「기간 바꾸기」 → 종료일을 `addDays(오늘, 9)`로 → 저장 → 충돌 문구 없이 부제가 새 종료일(엔지 리뷰 A P1).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-auto-settlement.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 합성 저장에 잠금 읽기·기간을 더하면서 04-11의 선판정이나 기존 견적 줄·매출 저장이 깨졌거나, 같은 요청 안에서 잠금 대기로 시간 초과가 났다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 팀장의 종료일 연장이 진행 태그로 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/projects/period.ts`가 리포지토리·DB를 import하지 않는다
    - `project.period-edit` 규칙이 등록되고 기간 저장이 그 게이트를 지난다
    - `saveProjectLedger`의 tx 콜백 첫 호출이 `loadProjectForGate(…, { tx })`이고, 본문에 풀 연결로 도는 `applyAutoSettlement` 호출이 없다(A-13)
    - 차수 소속 확인이 트랜잭션 전이고, 콜백 안 순서가 잠금 읽기 → 기간 판정·쓰기 → `loadProjectForGate` 재판정 → 견적 줄 → 매출 순이고 결과의 `project`가 재판정 행이다(ENG-D6 — 코드 순서로 확인)
    - 기간 저장이 `saveProjectLedger`의 한 트랜잭션 안이고 `updateProjectPeriod`가 기준값 조건부다 — `ledger.ts`·`period.ts`의 기간 판정에 `version` 참조가 없다
    - 기간 권리의 `can`·`loadActorTeamScope`가 `withTransaction` 호출보다 앞에 있다(ENG-D3 ①)
    - `SaveProjectLedgerResult`가 `project: { status, startDate, endDate }`를 싣고 기간 칸이 그 값으로 기준값을 바꾼다
    - 정산→진행 되돌리기 로그가 `recordAction(…, { tx })`로 쓰이고 detail에 `trigger: "end_date_extended"`와 `endDate: { from, to }`가 있다
    - 기간 칸에 `Form.Actions`가 없고 저장은 화면 1차 버튼 하나이며, 1차 렌더 조건이 `canSave` 하나다
    - N = 0의 1차가 이유 `바뀐 칸 없음`과 `reasonTone="info"`로 렌더되고(Grep) 옛 사용법 꼬리 문자열이 `quote-table.tsx`에 없다(`grep -c "고칠 칸을 눌러" 'app/(app)/projects/[id]/quote-table.tsx'` = 0)
    - E2E가 팀장의 정산 연장 → 진행 태그와 새로 고침 없는 둘째 기간 저장 통과를 단언하고, 스펙에 권한·노출을 켜는 호출이 0건이다(`grep -cE "upsertVisibility|upsertPermission|setPermissionCell|setVisibilityCell" test/e2e/project-period.spec.ts` = 0)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>팀장이 상세 기간 칸에서 정산 프로젝트의 종료일을 늦추면 같은 트랜잭션에서 진행으로 돌아가고 사람 로그가 같이 커밋되는 한 경로가 순수 함수·게이트·합성 저장·화면·E2E까지 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 권리·검증·미리보기 결정표(해석 뒤 값 · 달력 · 팀 범위) + 기간 변경 로그 + U-6 + 통합 증명(경합·자정·로그 실패) + 상태 바뀜 저장 거부 끝까지(`seenStatus` 서버 → 화면 다시 그림 → 복원 줄, DR-6)</name>
  <files>domain/projects/period.ts, domain/projects/ledger.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/period-field.tsx, app/(app)/projects/[id]/quote-table.tsx, ui/table/use-dirty-storage.ts, test/unit/domain/project-period.test.ts, test/unit/ui/dirty-storage.test.ts, test/integration/project-period.test.ts, test/integration/tx-safety.test.ts, test/e2e/project-period.spec.ts, test/e2e/ledger-save-flow.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## Copywriting Contract` `Error — 표 배치 저장 실패(표 밖 칸 오류로 전부 거부, U-6)` 362행 · `Error — 저장(상태가 바뀜, DR-6)` 363행 · `Error — 기간 칸` 376행 · `표시 — 기간 칸 결과 미리보기` 413행 · `미저장 편집 복원(현재 차수)` 416행 · `## 적용 규칙` 「S18」 1545–1558행 · 「S19」 상태가 바뀜 1570–1572행 · `## UI Considerations` 「S19」 649–657행
    - `domain/projects/period.ts`(Task 1 산출물) · `domain/projects/status.ts`의 `statusChangedMessage`·`StatusChangedError`·`listProjectStatusCatalog`(04-20)
    - `test/integration/lock-race.ts`(04-20 산출물) · `test/integration/project-auto-settlement.test.ts`(04-11 — `deps.recordAction` 실패 주입과 `loadProjectForGate`의 `afterLock` 형태)
    - `test/integration/tx-safety.test.ts`(04-32·04-20 — 풀 크기 2 모듈 격리 방법과 describe 형태, 04-32의 `saveProjectLedger` 호출 셋)
    - `ui/table/use-dirty-storage.ts`(전문 — `persist`·`restore`·`discard`·`clearAfterSave`·`restorableCount`) · `test/unit/ui/dirty-storage.test.ts`(04-04 — 저장소 테스트 더블)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `useDirtyStorage(`·`dirtyStorage.restore()`·`rejectionSummary`·`onSuccess`·합계 행 요약 자리를 찾아 범위만 Read(04-04 실행분은 `persist`를 부르지 않고 「복원」의 반환값을 버린다 — `quote-table.tsx` 860–866행 근처, 보관 훅 세 번째 인자가 `0`)
  </read_first>
  <behavior>
    - `periodEditRights`: 수주중·진행·미수주 + 담당 PM(`projects` 쓰기 있음) → `pm` · 담당 PM인데 `projects` 쓰기가 없음 → `none`(엔지 리뷰 A P3) · + `projects.status` 권한 + 자기 팀(또는 전사) → `lead` · 다른 팀 팀장(업무 범위 `team`, 담당 PM 아님) → `none` · 정산 + 담당 PM(권한 없음) → `none` · 정산 + 권한 + 자기 팀 → `lead` · 완료 → 누구든 `none` · 담당이 아닌 PM → `none`
    - `resolvePeriodSave`: 진행 이후 + 종료 비움 → 종료 = 시작 · 정산 + 새 종료 ≥ 오늘 → `statusChange: { from: 정산, to: 진행 }` · 수주중 + 종료 비움 → 그대로 비움
    - `validatePeriodChange`(**`resolvePeriodSave` 결과 위에서** 판정): 날짜 형식 틀림 또는 달력에 없는 날짜(`2026-02-30` — `YYYY-MM-DD` 정규식 뒤 UTC 왕복 비교) → `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` · 종료 < 시작 → `종료일이 시작일보다 빠릅니다 · 종료일을 고쳐 주세요` · 진행 이후 + 시작일 비움 → `진행부터는 시작일이 있어야 합니다 · 시작일을 적어 주세요` · 진행 + 권리 `pm` + **저장될** 종료 < 오늘 → `종료일이 오늘보다 빠름 · 앞당기기는 팀장 {이름}`(이름 없으면 `종료일이 오늘보다 빠름`) — 진행·`pm`·시작일 < 오늘·종료 비움도 이 오류(종료 칸에, A-02)
    - `previewPeriodChange`(해석 결과 위에서): 정산 + 새 종료 ≥ 오늘 → `저장하면 진행으로 돌아감` · 진행 + 저장될 종료 < 오늘 → `저장하면 정산이 됨`(종료 비움으로 과거 시작일이 되는 경우 포함 — 「비어 시작일로」보다 우선, A-02) · 진행 이후 + 종료 비움 + 정산이 되지 않음 → `종료일이 비어 시작일로 저장됨` · 그 밖 → null
    - `useDirtyStorage`의 새 `recount()`: 저장소에 편집 3칸이 있으면 `restorableCount`를 3으로 다시 읽는다 · `persist({})`를 부르지 않은 채 화면 상태만 바뀌면 저장소는 그대로다(테스트 더블)
  </behavior>
  <action>
① `test/unit/domain/project-period.test.ts`에 `behavior` 앞 네 묶음을 결정표로 먼저 쓴다(RED). `period.ts`에 `validatePeriodChange({ status, rights, start, end, todayKst, teamLeadName })` → 칸별 오류 `{ field: "start" | "end"; reason }[]`와 `previewPeriodChange({ status, newStart, newEnd, todayKst })` → 힌트 문자열 또는 null을 더한다 — 둘 다 **안에서 먼저 `resolvePeriodSave`를 불러** 그 결과(저장될 시작·종료)로 판정한다(A-02). 달력 검사는 형식 검사 바로 뒤 한 함수(A-22). 문자열은 UI-SPEC rev 5 Copywriting `Error — 기간 칸`·`표시 — 기간 칸 결과 미리보기` 원문 그대로다(P0 — rev 4의 「…습니다」 문장 꼴을 쓰지 않는다). Task 1의 두 함수를 표 전체로 넓힌다(GREEN). `ledger.ts`는 Task 1에서 게이트 ctx에 빈 오류 목록을 넘기던 자리를 `validatePeriodChange` 결과로 바꾼다(팀장 이름은 04-11 `projectResponsibles` — 트랜잭션 전에, 기간이 실렸고 담당 PM일 때만 읽는다, `<probe_fallback>`).

② **기간 변경 로그(A-16).** `ledger.ts`가 기간 쓰기가 실제로 값을 바꿨을 때 같은 `tx`로 `recordAction(viewer, { actionType: "document_update", entity: "project", entityId, detail: { startDate: { from, to }, endDate: { from, to } } }, { tx })` 한 줄을 남긴다(바뀌지 않은 칸은 detail에 싣지 않는다). 거부 때는 행동 로그를 남기지 않는다(04-20 규약 — 운영 로그 `write.denied`만).

③ **화면(기간 칸).** `period-field.tsx`: Enter 기본 제출을 막고, 칸 안에서 Ctrl+S는 1차 저장과 같은 `handleSave`를 부른다. Esc는 편집 중 값을 되돌리고 두 칸이 원래 값이면 묶음을 닫는다 — 묶음이 닫히면 포커스가 「기간 바꾸기」로 돌아온다(A-34). 힌트 한 줄은 `previewPeriodChange` 결과가 있을 때만(`Form.Hint`). 서버 칸 오류는 `Form.Error`(`aria-invalid`)로 칸 아래에 — 동시 수정 거부는 서버 문자열 `다른 사람이 먼저 기간을 바꿈 · 새로 고침` 그대로이고 화면이 링크·안내를 덧붙이지 않는다(P0 · rev 5 F1). `quote-table.tsx`: 기간 칸 오류로 거부되면 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`(U-6). 날짜에는 쉼표가 없다(D-95 예외 — `lib/format-number`를 쓰지 않는다).

④ **상태 바뀜 저장 거부 — 서버(DR-6 · 계약 4).** `SaveProjectLedgerInput`에 **필수** `seenStatus: ProjectStatus`를, `saveProjectLedgerAction` 스키마에 `seenStatus: z.enum(PROJECT_STATUSES)`(필수)를 더한다. `ledger.ts` 콜백의 (a) `loadProjectForGate` 바로 뒤, 기간 판정(c) **앞에서** `lockedRow.status !== input.seenStatus`이면 04-20의 `StatusChangedError`(상태 값)를 던진다 — 트랜잭션 전체가 롤백된다(같은 tx의 자동 정산과 그 시스템 로그도 되돌아가고, 화면이 다시 그릴 때 읽기 경로 `findProject`(04-11)가 같은 판정을 다시 해 시스템 로그 1줄로 정산한다 — SUMMARY에 한 줄). 거부는 권한 위반이 아니라 `write.denied`를 남기지 않는다. `saveProjectLedgerAction`은 트랜잭션이 끝난 **뒤**(롤백 뒤, 풀) 그 오류를 잡아 `listProjectStatusCatalog`의 라벨로 `statusChangedMessage(label, "전부 거부")`를 만들고 데이터 `{ statusChanged: { message } }`로 돌려준다(오류 문자열을 화면이 해석하지 않게 — 그 밖의 거부는 지금처럼 `serverError`). 화면은 모든 저장에 `seenStatus`(= `QuoteLedger`의 `status` prop — `[id]/page.tsx`가 넘기지 않았으면 더한다)를 싣는다. 04-32의 `tx-safety.test.ts`에 있는 `saveProjectLedger` 호출 셋에 `seenStatus`를 싣는다(그 프로젝트의 준비 상태).

⑤ **상태 바뀜 저장 거부 — 화면과 미저장 보관(D-68 · DR-6).** 먼저 `quote-table.tsx`의 보관 배선을 확인한다 — 편집 순간 `dirtyStorage.persist(…)`가 불리고 「복원」의 반환값이 줄 상태에 병합되는지(Grep `persist(`). 04-04 실행분처럼 `persist` 호출이 없고 `restore()` 반환값을 버리면 **이 태스크가 잇는다(ENG-D11 — SUMMARY 편차에 파일·원인·테스트 이름)**: (i) 사용자가 칸을 바꾸는 순간(셀 커밋 · 붙여넣기 반영 · 줄 추가·복제·삭제 · 기간 칸 입력)에만 `persist`로 현재 편집 전체를 쓴다 — 키는 기존 줄 `{줄 id}:{열 키}` → 값, 새 줄은 줄 전체(지금 식별자 `clientKey` — 04-30이 화면 uuid로 바꾼다), 기간 칸 `period:start`·`period:end`(04-44가 총 매출 예상가 칸 키 `preEstimate:*`를 이 배선에 더한다 — 이 태스크가 D-68 `persist` 배선의 유일한 구현 주인이다). 서버 값으로 다시 그리는 경로는 `persist`를 부르지 않는다(보관본이 비지 않게). (ii) 「복원」은 돌려받은 편집을 dirty 모양으로 병합한다(기존 줄 칸 덮기 · 새 줄 끝에 다시 만들기 · 기간 칸 열고 값 넣기). (iii) `useDirtyStorage(projectId, revisionId, dirtyCount)`에 실제 `dirtyCount`를 넘겨 이탈 경고가 동작하게 한다(04-04가 `0`을 넘겼다). (iv) `ui/table/use-dirty-storage.ts`에 `recount()`(저장소를 다시 읽어 `restorableCount`를 갱신)를 더하고 `test/unit/ui/dirty-storage.test.ts`에 `behavior` 마지막 줄을 쓴다(RED → GREEN). 그 다음 거부 처리: 결과가 `data.statusChanged`면 (1) 합계 행 오른쪽 거부 자리에 `message`(`--danger`)를 두고 (2) 보관본을 지우지 않은 채 `router.refresh()`로 서버가 새 상태·셀 단계·컨트롤·`canSave`를 다시 보내게 하고 (3) `QuoteLedger`가 `status` prop이 바뀐 것을 감지하면 화면 편집 상태(줄·매출·기간 칸)를 서버 props로 되돌리고 `dirtyStorage.recount()`를 불러 표 위 복원 줄 `저장 안 한 편집 N칸 · 복원 / 버림`이 뜨게 한다 — 합계 행 거부 줄은 남긴다. 사용자의 기간 저장 성공으로 상태가 바뀐 경우도 같은 감지 경로를 타지만 저장 성공이 이미 보관본을 지웠으므로 복원 줄이 없다.

⑥ `test/integration/project-period.test.ts`(신규): (a) 팀장이 정산 프로젝트의 종료일을 오늘 이후로 늦추면 진행 + `status_change` 로그 1줄(행위자 = 그 팀장, `trigger: end_date_extended`, `endDate.from/to`) + `document_update` 1줄, (b) 진행의 담당 PM이 어제로 앞당기면 `종료일이 오늘보다 빠름 · 앞당기기는 팀장 {이름}`으로 거부되고 DB의 기간·상태 무변경 · `log.warn` 스파이로 `write.denied` 한 번(B-26), (b2) 진행의 담당 PM이 시작일 < 오늘인 프로젝트에서 **종료일을 비우면** 거부되고 DB 무변경(A-02), (c) 팀장이 진행의 종료일을 어제로 앞당기면 같은 커밋에서 정산 + **시스템** 로그 1줄(`actor_id` null) + 팀장의 `document_update` 1줄 — (a)와 (c)가 행위자를 구분해 단언한다(금지 항목 1), (d) 진행 이후 종료일 비움(시작일 ≥ 오늘) → 시작일로 저장, (e) 기간 오류와 견적 줄 변경이 같은 저장에 있으면 견적 줄도 저장되지 않는다(DB 재조회), (f) 완료 프로젝트의 기간 저장은 누구든 거부, (g) **편집 화면 자정 넘김(OV-5 · DR-6):** `seenStatus: in_progress`로 `now`를 종료일 다음 날 00:00:01 KST로 주입한 기간 없는 `saveProjectLedger`(견적 줄만)가 `StatusChangedError`(상태 `settling`)로 거부되고, DB 재조회로 줄 값·상태(진행 그대로 — 롤백)·`status_change` 로그 수가 저장 전과 같다 → 이어 `findProject`(같은 `now`)가 정산을 돌려주고 시스템 로그가 1줄이다, (g2) **동료 전환 뒤 오래된 화면(DR-6):** `seenStatus: bidding`인 저장을, 준비 단계에서 동료가 같은 프로젝트를 미수주로 닫은 뒤 보내면 거부되고 액션 결과가 `{ statusChanged: { message: "상태가 미수주로 바뀜 · 전부 거부" } }`이며(액션을 직접 부르는 한 케이스) 줄·매출·기간이 무변경이다, (g3) 기간을 늦춰 정산을 진행으로 되돌리는 팀장의 저장(`seenStatus: settling`)은 상태 바뀜 거부에 걸리지 않고 통과한다(비교는 기간 쓰기 전), (h) **다른 팀 팀장(D11):** 업무 범위 `team`인 다른 팀 팀장의 정산 연장은 `기간 바꾸기 권한 없음`으로 거부 · 본부 책임자(전사)는 통과, (i) **차수 불일치(A-14):** 다른 프로젝트의 `revisionId`를 실은 저장은 `차수와 프로젝트가 맞지 않음 · 새로 고침`으로 거부되고 두 프로젝트 모두 무변경 · `vi.spyOn(log, "warn")`으로 `write.denied` 호출이 **정확히 한 번**이고 그 필드의 `rule`이 `quote.revision-project`(차수·프로젝트 불일치 규칙)이며, 필드에 금액 키·날짜 키(`startDate`·`endDate` 등)가 없고, `saveQuoteLines` 쪽에서 두 번째 호출이 오지 않는다(불일치 검사가 `saveQuoteLines` 호출 전에 끝나므로 — 04-12가 그 함수에 거부 지점을 더한 뒤에도 이 「한 번」 단언이 그대로 맞아야 한다, B-26 · 사용자 D19-3), (j) **정산 대 종료일 연장 경합(OV-5):** 연결 A의 읽기 판정(`applyAutoSettlement`, `deps.recordAction`에 `deferred`를 걸어 UPDATE 뒤 멈춤)이 행을 잠근 동안 연결 B(팀장)의 연장 저장(`seenStatus: settling` — 화면은 이미 정산을 봤다)이 `waitForLockWaiter`로 대기에 든 것을 확인하고 A를 푼다 → 최종 상태 진행, 로그는 시스템 정산 1줄 + 사람 되돌리기 1줄 · 순서를 바꿔 B가 `afterLock`에서 멈춘 동안 A는 기다리지 않고 0행으로 돌아온다(SKIP LOCKED) → B를 풀면 진행, 정산·되돌리기 로그가 B의 tx에서 각 1줄, (k) **로그 실패 롤백(A-01):** 되돌리기 저장에서 `deps.recordAction`이 던지면 기간·상태·견적 줄 모두 무변경(DB 재조회), (l) **기준값 충돌:** 동료가 먼저 종료일을 바꾼 뒤 옛 기준값으로 보낸 저장은 `다른 사람이 먼저 기간을 바꿈 · 새로 고침`이고 DB 무변경, (m) **새로 고침 없는 두 번(엔지 리뷰 A P1 · 공백 2):** 첫 기간 저장이 돌려준 `project`의 기간을 기준값으로(그리고 `seenStatus`를 돌려준 `project.status`로) 둘째 기간 저장을 보내면 둘 다 통과한다, (m2) **헛충돌 없음:** 화면을 연 뒤 상태만 바뀐 프로젝트에 화면이 읽은 기간 그대로의 기준값과 **바뀐 뒤 상태**를 `seenStatus`로 실은 저장은 기간 충돌 없이 판정된다(기간 기준값은 상태와 무관 — (j) 두 순서도 이 규칙으로 기대 결과 그대로 초록이어야 한다), (n) **판정 순서(ENG-D6):** 시스템 관리자(기간 권리 `lead` + `projects` 쓰기)가 진행 프로젝트의 종료일을 어제로 앞당기며 같은 저장에 견적 줄 하나를 고치면(`seenStatus: in_progress`) 한 커밋에서 시스템 `status_change`(정산)의 `seq`가 견적 줄 `document_update`의 `seq`보다 작다(기간 쓰기 → 재판정 → 줄 저장 순서) — 그 줄이 정산 규칙으로 판정되는 결과(단가 변경 거부 등)는 정산 편집 매트릭스를 만드는 04-12가 같은 순서 위에서 단언한다. `test/integration/tx-safety.test.ts`에 describe `풀 2 · 동시 기간 저장 셋(04-22)`을 더한다 — 서로 다른 세 프로젝트에 기간만 실은 합성 저장 셋을 풀 크기 2에서 동시에 보내면 10초 안에 **셋 다 성공**(시간 초과 거부 0 — 기간 경로가 트랜잭션 안에서 풀을 부르지 않는다, ENG-D3 ①).

⑦ **E2E.** `project-period.spec.ts`: PM의 이른 종료일 오류 `종료일이 오늘보다 빠름 · 앞당기기는 팀장 {이름}`과 표 합계 행 `전부 거부 · 다른 칸 오류 1칸` · 팀장의 `저장하면 정산이 됨` 힌트 → 저장 → 정산 태그 · 기간 칸을 Esc로 닫으면 포커스가 「기간 바꾸기」. **`test/e2e/ledger-save-flow.spec.ts`(신규 — S19 저장 흐름, 04-30이 저장 중 잠금 케이스를 더한다):** 준비에서 줄 둘인 수주중 프로젝트를 두고 팀장으로 상세를 연다 → 실행가 셀 하나를 고친다(N = 1) → **다른 브라우저 컨텍스트**(같은 팀장 두 번째 로그인)에서 같은 프로젝트를 미수주로 닫는다 → 첫 화면에서 `Control+s` → 합계 행에 `상태가 미수주로 바뀜 · 전부 거부`, 머리 줄 상태 태그가 `미수주`로 다시 그려지고 「진행으로 되돌리기」가 보이며, 표 위에 `저장 안 한 편집 1칸 · 복원 / 버림` → 「복원」을 누르면 그 셀이 고친 값으로 dirty(N = 1)다 → 새로 고친 뒤 DB 값은 옛 값이다(저장되지 않았다). 권한·노출을 켜는 호출을 두지 않는다(ENG-D2).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-period.test.ts test/unit/ui/dirty-storage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 권리·검증·미리보기·저장 해석 결정표가 어긋나거나 보관 훅의 `recount`가 저장소를 다시 읽지 않는다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-period.test.ts test/integration/project-auto-settlement.test.ts test/integration/project-status.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts test/integration/tx-safety.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 저장이 한 트랜잭션 규칙을 깨거나, 행위자가 섞였거나, 경합 순서가 결정적이지 않거나, 버전 때문에 헛충돌이 났거나, 판정 순서가 다르거나, 화면이 본 상태와 다른 저장이 통과했거나 일부만 썼거나, 사람 자신의 기간 변경이 상태 바뀜으로 헛거부됐거나, 기간 경로가 트랜잭션 안에서 풀을 불러 풀 2 동시 저장이 시간 초과됐거나, 기존 합성 저장이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts test/e2e/ledger-save-flow.spec.ts test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 상태 바뀜 거부 뒤 태그가 다시 그려지지 않았거나 복원 줄이 편집을 돌려주지 않았거나 기간 칸 문구가 rev 5 원문과 다르다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준 — 이 플랜의 완료 판정. 머리 줄 독립 DOM 감사는 두 표 밖 칸이 모두 선 04-44가 한 번 한다)</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 기간 네 묶음을 전부 덮고, `validatePeriodChange`·`previewPeriodChange`가 본문에서 `resolvePeriodSave`를 부른다(A-02)
    - 화면 컴포넌트와 `ledger.ts`가 같은 `period.ts` 함수를 import하고, 힌트 문자열이 화면 코드 리터럴이 아니라 `previewPeriodChange` 반환값이다
    - 정산→진행 되돌리기 로그의 행위자가 사람이고 자동 정산 로그의 행위자는 시스템이다(통합 a·c가 둘을 구분해 단언)
    - 진행의 PM이 이른 종료일도, 종료일 비움 + 과거 시작일도 서버에서 저장하지 못한다(통합 b·b2, DB 재조회)
    - 기간 오류가 있는 저장에서 견적 줄·매출도 저장되지 않는다(통합 e, DB 재조회)
    - `seenStatus`가 액션 스키마와 `SaveProjectLedgerInput`에 필수이고, 비교가 `ledger.ts` 콜백에서 첫 `loadProjectForGate` 호출 바로 뒤 · 기간 판정 앞에 있다(코드 순서로 확인) · 상태 바뀜 문구는 `app/(app)/projects/actions.ts`가 `statusChangedMessage(`를 `"전부 거부"` 꼬리로 불러 만든다(Grep — 문구를 화면·액션에서 따로 조립하지 않는다)
    - 통합 (g)·(g2)·(g3)가 초록이다 — 상태가 다른 저장은 DB 무변경, 사람 자신의 기간 변경은 통과
    - `quote-table.tsx`가 편집 순간 `persist`를 부르고 「복원」의 반환값을 병합하며, 보관 훅 세 번째 인자가 실제 `dirtyCount`다(Grep) · `recount` 단위 케이스가 초록이다
    - `ledger-save-flow.spec.ts`가 상태 바뀜 거부 문구 · 다시 그려진 태그 · 복원 줄 · 복원 뒤 dirty · 새로 고침 뒤 옛 값을 단언한다
    - 경합 (j) 두 순서가 `sleep` 없이 `deferred`·`waitForLockWaiter`로 결정적이다
    - 다른 팀 팀장 거부(h)·차수 불일치 거부(i)·로그 실패 롤백(k)·기준값 충돌(l)·새로 고침 없는 두 번(m)·헛충돌 없음(m2)·판정 순서(n)가 초록이다
    - `tx-safety.test.ts`의 `풀 2 · 동시 기간 저장 셋(04-22)`이 셋 다 성공이고, 04-32의 합성 저장 호출이 `seenStatus`를 싣는다
    - 통합 (i)가 `log.warn` 스파이로 `write.denied` 정확히 한 번(규칙 `quote.revision-project`, 금액·날짜 키 없음, `saveQuoteLines`발 두 번째 호출 없음)을 단언한다(B-26)
    - 네 검증 명령이 전부 초록이다(마지막 `CI=true pnpm test`)
  </acceptance_criteria>
  <done>기간을 권리대로 고치고 결과 미리보기가 저장될 값으로 서버 저장과 같은 판정을 보이며, 정산 연장·앞당김·종료일 기본값이 한 트랜잭션 저장으로 동작하고 행위자가 섞이지 않으며, 화면이 본 상태와 다른 저장은 통째로 거부되고 화면이 새 상태로 다시 그려진 뒤 편집이 복원 줄로 돌아온다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(기간) | 시작일·종료일·차수 id가 넘어온다. 권리·검증·정산 되돌리기·소속 판정이 서버에만 있어야 한다 |
| 기간 칸 → 상태 | 날짜 한 칸이 상태를 바꾼다(정산→진행 · 진행→정산). 상태 전환 권한의 우회로가 될 수 있다 |
| 읽기 판정 ↔ 저장 트랜잭션 | 같은 프로젝트 행을 두 경로가 동시에 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-77 | Elevation of Privilege | 기간 칸으로 상태 바꾸기 | high | mitigate | 정산→진행(D-80)은 `project.period-edit` 게이트가 권리 `lead`(팀장 이상 + 자기 팀)일 때만 통과하고, 진행의 PM은 **저장될** 종료일이 오늘보다 이른 저장(이른 날짜 · 종료일 비움 + 과거 시작일)을 서버에서 할 수 없다(A-02). 통합 a·b·b2·h로 고정한다 |
| T-04-78 | Spoofing | 되돌리기 행위자(사람 쪽) | medium | mitigate | 정산→진행 되돌리기는 요청한 viewer로, 자동 정산은 `SYSTEM_VIEWER`(04-11)로 기록된다 — 통합 a·c가 두 로그의 행위자를 구분해 단언한다(금지 항목 1). 기간 변경 자체는 `document_update`로 사람 이름이 남는다(A-16) |
| T-04-80 | Tampering | 기간 동시 편집 | medium | mitigate | (엔지 리뷰 A P1 정정) 기간 갱신이 화면이 읽은 기간 기준값 조건부이고 충돌은 명시 문구로 거부된다(A-22 · 통합 l). `projects.version`을 쓰지 않아 상태 변경·자동 정산이 헛충돌을 만들지 않는다(통합 m·m2 · (j)). 합성 저장은 행 배타 잠금 안에서 판정한다 |
| T-04-309 | Tampering | 기간 + 줄 합성 저장의 판정 기준 | medium | mitigate | (ENG-D6) 기간 쓰기 → 재판정 → 새 행으로 줄·매출 판정 순서를 코드로 고정하고 통합 n(순서)·04-12(결과)로 단언한다 — 옛 상태로 판정해 정산 규칙을 비껴가는 저장이 없다 |
| T-04-163 | Tampering | 합성 저장의 차수 id | high | mitigate | (CEO A-14) 실린 `revisionId`의 차수가 경로의 `projectId` 소속이 아니면 쓰기 전에 거부한다 — 다른 프로젝트 차수로 게이트·권리 판정을 비껴가는 페이로드를 통합 i로 고정한다 |
| T-04-164 | Repudiation | 상태 변경과 로그의 분리 | medium | mitigate | (CEO A-01) 되돌리기·기간 변경·같은 커밋의 자동 정산 로그가 전부 합성 저장 tx로 쓰인다 — 로그 실패 주입 시 전부 롤백(통합 k) |
| T-04-175 | Repudiation | 기간 거부의 운영 기록 | low | mitigate | (CEO B-26 · 사용자 D19-3) 권리 없음·검증·차수 불일치 거부가 화면에만 보이던 빈틈을 `write.denied` 경고(보는 사람·규칙·프로젝트 id, 금액·날짜 없음)로 메운다 — 행동 로그에는 남기지 않는다(04-20 규약). 통합 b·i 스파이 단언(i는 규칙 `quote.revision-project` 정확히 한 번) |
| T-04-332 | Tampering | 오래된 화면의 부분 저장(DR-6) | high | mitigate | 합성 저장이 `seenStatus`를 잠금 직후 첫 판정 행과 비교해 다르면 트랜잭션 전체를 되돌린다 — 자정 자동 정산·동료 전환 뒤의 화면이 새 상태 규칙으로 일부 칸만 통과시키는 경로가 없다(통합 g·g2, 금지 항목 3). 비교는 기간 쓰기 전이라 사람 자신의 기간 변경은 막지 않는다(통합 g3) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 경합·자정·로그 실패 통합 케이스가 `sleep` 없이 결정적이다
- SUMMARY 「새 문구」 표에 `기간 바꾸기 권한 없음`(rev 5에 없는 방어 문구 — 사용자 2026-09-23 승인)이 있다
- 상태 바뀜 저장 거부가 통합(DB 무변경)·E2E(다시 그림 · 복원 줄)로 증명됐다
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
</verification>

<success_criteria>
- 팀장(자기 팀)이 종료일을 늦춰 정산을 진행으로 되돌리고, PM은 어떤 입력으로도 종료일로 상태를 바꿀 수 없다
- 기간 칸의 미리보기가 저장될 값으로 서버 저장과 같은 판정이고, 기간 저장이 견적 줄·매출·로그와 한 트랜잭션이다
- 화면이 본 상태와 다른 저장은 통째로 거부되고 편집은 복원 줄로 돌아온다(DR-6)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/projects/period.ts` — `periodEditRights`(팀 범위 입력) · `validatePeriodChange`(해석 뒤 값 · 달력) · `previewPeriodChange`(해석 뒤 값) · `resolvePeriodSave` |
| 함수(신규, repository) | `updateProjectPeriod`(`repositories/projects.ts` — 기간 기준값 조건부) |
| 게이트 규칙 | `project.period-edit` |
| 로그 detail 규약 | `status_change` detail `{ from, to, trigger: "end_date_extended", endDate: { from, to } }`(사람 행위자) · `document_update` detail `{ startDate?: { from, to }, endDate?: { from, to } }` |
| 운영 로그 | `write.denied`(경고 — 기간 권리·검증 거부는 규칙 `project.period-edit`, 차수 불일치 거부는 규칙 `quote.revision-project`, 금액·날짜 없음) |
| 입력 계약 | `SaveProjectLedgerInput.period`(`{ startDate, endDate, baseline }`) · `saveProjectLedgerAction` 스키마 `period` · 결과 `SaveProjectLedgerResult.project`(`{ status, startDate, endDate }`) |
| 변경된 기존 심볼 | `saveProjectLedger`(tx 첫 단계 `loadProjectForGate` · `seenStatus` 비교 · 차수 소속 확인 · 기간 쓰기 뒤 같은 tx 재판정 · `deps.now`) · `QuoteLedger`(1차 렌더 `canSave` · N = 0 이유 `바뀐 칸 없음`(`info`) · `openPeriodField` · `status` prop 변화 감지 → 서버 props로 다시 그림) — `StatusChangeProps` 확장은 04-44 |
| 컴포넌트 | `app/(app)/projects/[id]/period-field.tsx`(신규) |
| 상태 바뀜 거부(계약 4) | `SaveProjectLedgerInput.seenStatus`(필수) · 액션 스키마 `seenStatus` · 액션 결과 `{ statusChanged: { message } }` · 문구 `statusChangedMessage(label, "전부 거부")`(04-20) |
| 보관 훅(변경) | `useDirtyStorage`에 `recount()` · `QuoteLedger`의 `persist` 호출(편집 순간) · 「복원」 병합 · 실제 `dirtyCount` |
| 테스트 | `test/unit/domain/project-period.test.ts` · `test/unit/ui/dirty-storage.test.ts`(`recount`) · `test/integration/project-period.test.ts`(신규 — 상태 바뀜 거부 포함, 04-44가 총 매출 예상가 케이스를 더한다) · `test/integration/tx-safety.test.ts`(풀 2 동시 기간 저장 셋 · 04-32 호출에 `seenStatus`) · `test/e2e/project-period.spec.ts`(04-11 파일 확장 — 04-44가 더 넓힌다) · `test/e2e/ledger-save-flow.spec.ts`(신규 — S19, 04-30이 확장) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 상태 모달 3차 「기간 적기」·「기간 바꾸기」 연결 · 총 매출 예상가 칸 전부(`pre-estimate.ts` · `pre-estimate-field.tsx` · 게이트 `project.pre-estimate-edit` · `updateProjectPreEstimate` · `ProjectDto.preEstimate` · 입력 `preEstimate`) · 폰 부제 순서 · 머리 줄 독립 DOM 감사(04-44, 체커 revision 1 B5) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-01 — 되돌리기 로그를 풀에 쓰면 유령 로그 | CRITICAL | Plan 04-22, Task 1 ③(e): `recordAction(…, { tx })` · 통합 k |
| A-02 — 종료일 비움으로 PM이 정산을 일으킴 | CRITICAL | Plan 04-22, Task 2 behavior·①: 검증·미리보기를 `resolvePeriodSave` 결과 위에서 · 통합 b2 · 금지 항목 2 문구 |
| A-06 (04-22 부분) — 잠금 없는 게이트 읽기 | CRITICAL | Plan 04-22, Task 1 ③(a): tx 첫 단계 `loadProjectForGate`(04-11·04-20 배타 잠금 — OV-3이 FOR SHARE를 FOR UPDATE로 정정) |
| A-12 — 1차 렌더 조건 | WARNING | Plan 04-22, Task 1 ④: `canSave` 서버 계산(편집 칸 항은 04-30이 셀 단계로 바꿈 · 계약 항 `(canWriteContract && status !== 완료)`는 04-16이 지움). 서버 쪽 완료 계약 거부 게이트는 두지 않는다 — A-12는 04-41이 계약 입력 경로를 통째로 지우는 끝 상태로 충족된다 |
| A-13 — tx 안에서 풀 UPDATE가 자기 잠금을 기다림 | WARNING | Plan 04-22, Task 1 ③(a)(f): 선판정·재판정 모두 같은 tx |
| A-14 — 차수·프로젝트 불일치 | WARNING | Plan 04-22, Task 1 ③(b) · 통합 i · T-04-163 |
| A-16 — 기간 변경 무기록 | WARNING | Plan 04-22, Task 2 ② · Task 1 ③(e) detail `endDate` · 통합 a·c |
| A-22 — 달력 없는 날짜·충돌 문구 | WARNING | Plan 04-22, Task 2 behavior(달력 왕복)·Task 1 ③(e) 충돌 문구 · 통합 l |
| A-25 / 사용자 D14 — 기간 권리 기본값 | DECISION | 확정 반영(`<probe_fallback>`), 팀장 권리는 자기 팀 |
| A-27 / OV-4 / 사용자 D11·D20 — 팀장 자기 팀 | HIGH | Plan 04-22, Task 1 ① `periodEditRights`의 `actorCoversTeam` · 통합 h |
| A-31 — 상태 컨트롤 props | NOTE | Plan 04-44, Task 3 ①(체커 revision 1로 04-22 옛 Task 3에서 이동): 04-21의 `StatusChangeProps`를 넓힘 |
| B-26 / 사용자 D19-3 — 거부된 쓰기의 운영 기록 | DECISION | Plan 04-22, Task 1 ③(d) `write.denied` · 통합 b·i 스파이(i: 불일치 규칙 정확히 한 번, `saveQuoteLines`발 두 번째 호출 없음) |
| A-34 — 포커스 복귀·새로 고침 안내 | NOTE | Plan 04-22, Task 2 ③ · E2E Esc 포커스 |
| OV-5 — 편집 화면 자정 넘김 · 정산 대 연장 경합 · 로그 실패 | HIGH | Plan 04-22, 통합 g·j(두 순서)·k |
| OV-3 — 결정적 경합 테스트 | HIGH | Plan 04-22, 통합 j(`deferred`·`waitForLockWaiter`, sleep 금지) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 A §1 P1 — 기간 낙관 잠금이 `projects.version`(자동 정산·전환과 충돌) · 결과에 버전 없음 | HIGH | Supersedes Round 1 A-22의 「버전 조건부」: Plan 04-22, Task 1 ③(c)(d)(g) 기간 기준값 · `updateProjectPeriod` · 결과 `project` · ④ 화면이 기준값 갱신 · 통합 l·m(공백 2)·m2 · E2E 새로 고침 없는 두 번 · T-04-80 |
| ENG-D6 — 기간 + 줄 합성 저장의 판정 순서 | DECISION | Plan 04-22, Task 1 ③ 순서(기간 쓰기 → 재판정 → 줄 → 매출) · 통합 n · T-04-309 (정산 규칙 결과는 04-12 통합) |
| ENG-D3 ① — 잠근 트랜잭션 안 풀 호출 금지 | DECISION | Plan 04-22, Task 1 ③ 트랜잭션 전 사실 · Task 2 ④ `tx-safety` 풀 2 동시 기간 저장 셋(셋 다 성공) |
| 엔지 리뷰 A P3 — `periodEditRights`가 `projects` 쓰기를 안 봄 | NOTE | Plan 04-22, Task 1 ① `canWrite` · Task 2 behavior 줄 |
| ENG-D2 — E2E가 권한·노출을 손으로 켜지 않음 | DECISION | Plan 04-22, `<probe_fallback>` · Task 1 ⑤ · 수용 기준 grep 0(옛 Task 3 ③ 몫은 04-44) |
| 엔지 리뷰 B — `write.denied` 한 함수 | NOTE | Plan 04-22, Task 1 ③(b)(c) `denyWrite`(04-20) |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 기간 변경 로그 종류(`document_update`는 관리자가 끌 수 있음 — 엔지 리뷰 A U4) | 사용자 결정 대상으로 올라가지 않았다(ENG-D1~D12에 없음) — 지금 규약(A-16 `document_update`) 유지. 「누가 정산을 일으켰나」의 시스템 쪽 기록은 끌 수 없는 `status_change`가 남긴다 |

### Round 3 — 디자인 리뷰(`docs/designs/plant8-erp-phase4-design-review-260923.md`) · UI-SPEC rev 5

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-6 · 계약 4 — 자정 자동 정산·동료 전환 뒤 오래된 화면의 저장이 새 상태 규칙으로 조용히 판정됨 | HIGH | Plan 04-22(서버 소유 — 트랜잭션 첫 판정 자리가 이 플랜), Task 2 ④⑤: 필수 `seenStatus` · 첫 `loadProjectForGate` 행과 비교 · 전체 롤백 · 액션 `{ statusChanged }` · 화면 다시 그림 · 복원 줄 · 통합 g·g2·g3 · E2E `ledger-save-flow.spec.ts` · T-04-332 · 금지 항목 3 |
| DR-6 전제 — 미저장 보관이 실제로 동작하지 않음(04-04 실행분: `persist` 호출 없음 · 「복원」 반환값 버림 · 이탈 경고 인자 0) | HIGH | Plan 04-22, Task 2 ⑤ (i)~(iv): 편집 순간 `persist` · 복원 병합 · 실제 `dirtyCount` · `recount()` — ENG-D11 편차로 SUMMARY에 |
| DR-28 · DR-37 · 계약 8 — 총 매출 예상가 표시 자리·수정 권리 | HIGH | Plan 04-44(체커 revision 1로 이동 — 옛 04-22 Task 3 ②③): 부제 + 3차 + 칸 묶음(기간 칸과 같은 형태) · 권리 `periodEditRights` · 같은 일괄 저장 · 통합 o~o4 · E2E · T-04-333·334 |
| DR-10 · DR-11 · P0 — N = 0 이유 | NOTE | Task 1 ④: `바뀐 칸 없음` + `reasonTone="info"` · kbd `Ctrl+S` · 수용 기준 grep |
| P0 · DR-21 · rev 5 F1 — 기간 칸·동시 수정·차수 불일치 문구 | NOTE | truths · Task 1·2: `종료일이 오늘보다 빠름 · 앞당기기는 팀장 {이름}` · `다른 사람이 먼저 기간을 바꿈 · 새로 고침` · `차수와 프로젝트가 맞지 않음 · 새로 고침`(옛 A-34 「새로 고쳐 주세요」 링크 버튼 삭제) · `기간 바꾸기 권한 없음`(방어 문구 — 명사형) |
| rev 5 S7 — 진행 모달의 종료일 지남 줄과 「기간 바꾸기」 | NOTE | Plan 04-44 Task 3 ①(이동): 결과 줄 글자는 04-21, 이 플랜은 3차 「기간 적기」(`primary.nextStep`)·「기간 바꾸기」(결과 줄) 연결 |
| DR-26 — 폰 부제 첫 항목 | NOTE | Plan 04-44 Task 3 ②(이동 — 폰 머리 줄 순서·「더보기」는 04-21) |
| DR-14 — DOM 감사 폭 1024 | NOTE | Plan 04-44 Task 3 ④(이동 — 머리 줄 backstop · human-check: 1280 · 1024 · 375) |
| DR-1 — rev 4 줄 번호 | NOTE | `<context>`·`read_first`를 rev 5 절 제목 + 현재 줄로 |
| UI Considerations 끌어올림 — S13 · S17 · S19(error · partial) · S3(overflow · 폰 첫 화면 부제) | NOTE | S17 · S3 줄은 체커 revision 1로 04-44 truths로 이동 · `must_haves.truths` 새 줄들(explicit → 문자열, backstop → `{ statement, verification: backstop }`). S19 loading(저장 중 잠금)·성공(전체 목록 다시 그림)은 04-30 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 총 매출 예상가의 동시 수정 충돌 판정 | (Round 4에서 해소 — 사용자 2026-09-23 확정: 나중 저장이 이긴다, 04-44 truths) |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B5 — 04-22 크기(21파일 · 128k · 네 관심사: 기간 칸+게이트 · 총 매출 예상가 칸 · DR-6 서버 거부 · D-68 보관 수리) | BLOCKER | 옛 Task 3(상태 모달 연결 · 총 매출 예상가 칸 · 폰 부제 순서 · 머리 줄 DOM 감사)을 신설 **04-44**로 옮김 — 이 플랜 15파일 · 88k(2 태스크), 04-44 14파일 · 52k. 사슬 04-11 → 04-22 → 04-44 → 04-12(04-12 `depends_on` 한 줄만 바뀜). Task 2에 `CI=true pnpm test`를 완료 판정으로 더함 |
| W4 연관 — D-68 `persist` 배선 주인이 하나여야 함 | NOTE | objective 분할 문단 · Task 2 ⑤ (i): 이 플랜 Task 2 ⑤가 유일한 구현 주인 — 04-44 · 04-30 · 04-24는 넓히거나 전제로 단언만 |
| 사용자 확정 2026-09-23 — 방어 문구(rev 5에 없는 서버 거부) | DECISION | `<probe_fallback>` · verification: `기간 바꾸기 권한 없음`을 명사형 그대로 SUMMARY 「새 문구」 표에 |
| 04-08 분할(W2) — 계약 1(`Button`) 주인이 04-46으로 | NOTE | `<context>` · Task 1 ④: `Button` `reasonTone`의 출처를 04-46으로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md` when done
</output>
