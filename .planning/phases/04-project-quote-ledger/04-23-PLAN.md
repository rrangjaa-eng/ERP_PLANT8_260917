---
phase: 04-project-quote-ledger
plan: 23
type: execute
wave: 15
depends_on: ["04-13", "04-25"]
files_modified:
  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/e2e/quote-line-kinds.spec.ts
autonomous: true
requirements: [PROJ-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "조정 줄은 표 맨 아래 고정 그룹 `조정`에 모인다 — 코드표 그룹 뒤, 견적 외 비용 그룹보다도 뒤. 새 색·아이콘이 없고 소분류 칸은 읽기 전용 글자 `조정`이다(S4)"
    - "조정 줄 행은 견적가 `0` · 실행가 = 금액(음수 허용)이고 수량·단가·상태 칸은 `—`다(D-83, S4 행 모양 — 서버 저장 규칙은 04-13)"
    - "담당 PM 화면에도 조정 행이 같은 표 안에 렌더된다(숨기지 않는다) — **권한 밖 줄(DR-22)**: 잠김 단계 모양이고, 셀에서 `Enter`를 누르거나 클릭해도 편집이 열리지 않으며 **이유 글자·오류 표시가 없다**. 「조정 줄 추가」가 없다 — 맨 아래 `조정` 그룹과 잠긴 모양이 이유다(D-83, UI-SPEC rev 5 「적용 규칙 S4」 「조정」 줄 · Copywriting `Error — 셀(조정 줄, PM)` 「문구 없음」 · 04-13 금지 항목의 화면 쪽)"
    - "담당 PM이 조정 행에서 `Delete`를 누르면 아무 일도 일어나지 않는다 — 삭제 모달이 열리지 않고 줄이 빠지지 않으며 1차 「일괄 저장 N」의 N이 늘지 않고 이유 글자도 없다. 조정 줄 보관이 페이로드에 실려 배치 전체가 서버에서 거부되는 경로로 가지 않는다(CEO 리뷰 B-31 · DR-22)"
    - "담당 PM의 붙여넣기가 조정 행 칸에 떨어지면 그 칸은 값이 바뀌지 않고 오류 셀도 되지 않으며, 합계 행 오른쪽 붙여넣기 요약에 `조정 줄 N칸 건너뜀`(`--muted`)으로만 세어진다 — 같은 붙여넣기에서 권한 밖 줄이 아닌 잠긴 칸(정산 · 승인 차수 · 연결 문서)은 지금처럼 오류 셀 + 짧은 이유 한 줄이다(DR-22 · DR-35, UI-SPEC rev 5 「SYSTEM.md 개정 ⑫」 (가)·(다) · Copywriting `표시 — 합계 행 오른쪽 한 줄` · UI Considerations S4 partial ④)"
    - "경영관리에게 「조정 줄 추가」 3차가 상태와 무관하게 보이고(완료에서는 「줄 추가」 없이 혼자 — 정산에는 사용자 D10으로 「줄 추가」가 있어 그 옆), 차수 줄 수 상한에 닿으면 `aria-disabled` + 옆에 `300줄 상한 · 상한은 관리자 설정`(`block`, 숫자는 04-26의 `lineCap`)이다(S4, Copywriting 막힘 — 조정 줄 추가(상한), 계약 1 `Button` 개정)"
    - "「조정 줄 추가」·「견적 외 비용 줄 추가」와 EMPTY의 「조정 줄 추가」는 **1024px 이상에서만** 렌더된다 — 700~1023(좁은 PC)과 폰에서는 표가 보기 전용이라 버튼이 없고 EMPTY는 사실만(`이 프로젝트에 견적 줄이 없습니다`)이다(DR-24 · DR-36, UI-SPEC rev 5 UI Considerations S4 (rev 5) 폰 · 좁은 PC 구조 컨트롤 · Copywriting `Empty — 견적 줄 표 (고르는 규칙)`)"
    - "조정 권한만 있고 `projects` 쓰기가 없는 경영관리도 조정 행의 거래처 칸을 편집할 때 거래처 목록이 비어 있지 않다 — 상세가 참조 목록(거래처·소분류)을 `projects` 쓰기 **또는** 조정 권한이 있으면 불러온다(CEO 리뷰 B-23)"
    - "완료 프로젝트의 표 위 한 줄은 조정 권한자(조정 행이 편집 셀이라 격자)에게만 `완료 · 견적 줄 잠김`이고, 편집 셀이 0인 PM의 흰 머리글 읽기 표에는 잠김 줄이 없다(P0 — 흰 머리글이 말한다) · 0줄 표의 EMPTY는 경영관리에게 `이 프로젝트에 견적 줄이 없습니다 · 조정 줄 추가`(줄 추가 권한이 함께 있는 상태 — 수주중·진행·미수주, 그리고 사용자 D10으로 정산 — 에서는 「첫 줄 만들기」가 이긴다)(S4 상태별 편집 범위, Copywriting `Error — 셀(잠김) = 표 위 한 줄` · `Empty — 견적 줄 표 · 경영관리(조정 권한), 상태 무관`)"
    - "경영관리의 조정 줄 삭제는 `ui/confirm-dialog`(04-46 — 계약 2)다 — 제목 `조정 줄 삭제` · 부제 `{항목} · {실행가}` · 결과 `보관함으로 옮겨짐 · 복원은 관리자` · 1차 `조정 줄 삭제` · 2차 `취소 Esc`. 1차가 그 줄을 보관 목록에 넣어 dirty에 합류하고 일괄 저장 때 보관함으로 간다 · 열리면 포커스가 1차, `Esc` = 2차, 닫히면 트리거 셀로 포커스가 돌아온다(Copywriting Destructive — 조정 줄 삭제, UI-SPEC rev 5 S16)"
    - "견적 외 비용 줄은 담당 PM이 줄을 추가할 수 있는 상태(수주중·진행·미수주, 그리고 사용자 D10으로 정산)에서 3차 「견적 외 비용 줄 추가」로 만들고, 소분류 자리와 그룹 머리글이 `견적 외 비용`이며 수량·단가는 `—`, 실행가 음수를 허용한다 — 정산에서 만든 줄도 실행가만 편집이고(D12 — 견적 칸 0), 완료에서는 이 버튼이 없다(D-48, EXP-14, ROADMAP 기준 2)"
    - "견적 줄 소분류 셀을 편집하는 동안 고른 소분류의 코드표 설명이 셀 아래 한 줄로 보이고, 편집이 끝나면 사라지며 같은 자리에 오류 이유가 있으면 오류가 이긴다 — 읽기 상태의 행 높이는 늘지 않고 필터 줄·폰 행 시트에는 설명이 없다(D-93, SYSTEM.md 개정 ⑥, S14 표)"
    - statement: "조정·견적 외 비용 그룹이 붙은 표가 1280 · 1024 · 375에서 가로 스크롤 0이고 폰 P1 세 열(항목·실행가·상태)이 조정 행에서도 같다 — DOM 감사(DR-14 감사 폭)"
      verification: backstop
    - statement: "1000px(좁은 PC)과 375px에서 「조정 줄 추가」·「견적 외 비용 줄 추가」·EMPTY 「조정 줄 추가」가 DOM에 없고 조정 행 셀이 `Enter`·클릭에 편집으로 들어가지 않는다 — DOM 감사(UI Considerations S4 (rev 5) 폰 · 좁은 PC 구조 컨트롤)"
      verification: backstop
  artifacts:
    - path: "test/e2e/quote-line-kinds.spec.ts"
      provides: "경영관리 완료 프로젝트 조정 줄 추가 · PM 권한 밖 줄(Enter·Delete 무반응 · 글자·오류 없음 · 붙여넣기 건너뜀 요약) · 다른 잠긴 칸의 이유·오류 유지(DR-35) · 조정 줄 삭제 확인(`ui/confirm-dialog`) · 견적 외 비용 줄 · 소분류 설명 힌트"
      min_lines: 40
    - path: "domain/quotes/edit-scope.ts"
      provides: "표 위 한 줄·EMPTY 판정·힌트 키에 조정 권한 축(04-30 함수 확장 — EMPTY 우선순위 ② 자리)"
      exports: ["tableLockLine", "quoteTableEmptyState", "visibleHintKeys"]
  key_links:
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "domain/quotes/lines.ts"
      via: "그룹 머리글이 소분류 문자열보다 줄 종류(`lineKind`)를 먼저 본다 — 조정 그룹이 맨 아래 고정"
      pattern: "lineKind"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/permissions/can.ts"
      via: "조정 권한(`projects.adjustment` 쓰기)을 서버가 계산해 넘긴다 — 화면이 계급을 추론하지 않는다"
      pattern: "projects.adjustment"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/select/Select.tsx"
      via: "소분류 셀 편집기가 04-25의 옵션 설명을 받아 편집 중에만 한 줄로 보인다"
      pattern: "description"
---

<objective>
**As a** 경영관리 담당자와 기획 PM, **I want to** 경영관리는 완료 프로젝트에서도 표 맨 아래 「조정」 줄을 넣고 지우며, PM은 그 줄을 같은 표에서 보되 고칠 수 없고, 견적에 없던 비용은 「견적 외 비용」 줄로 붙이며, 소분류를 고를 때 그 뜻을 한 줄로 보기를, **so that** 경영관리의 원가 보정이 PM 화면에서 숨지 않고 같은 합계로 읽힌다.

**2026-09-23 체커 1차 분할.** 옛 04-13(19파일 — Task 1 하나가 15파일)을 서버와 화면으로 나눈 **화면 쪽**이다. 04-13이 줄 종류 컬럼(`line_kind`) · 조정 권한 메뉴 · 종류 축 판정 · 저장 규칙 · 합계 불변식을 서버에서 끝냈다. 이 플랜은 그 DTO(`lineKind` · `cellEditability`)를 받아 조정·견적 외 비용 그룹과 버튼 · PM 시점의 읽기 전용 · 삭제 모달 · 표 위 한 줄과 EMPTY의 조정 변형 · 소분류 설명 힌트(D-93 — 옵션 설명은 04-25)를 그린다.

Purpose: 금지 항목(조정 줄이 PM에게 보이지 않는 채로 실행가·수익금을 바꾸면 안 된다)의 화면 쪽 증명이 이 플랜의 E2E다. 04-14·04-24(새 차수의 조정 줄 이동)와 04-07(리저브 증빙 종류 설명 줄)이 이 플랜의 그룹·설명 줄 모양을 따른다.

Output: `QuoteLedger` 그룹 순서 · 「조정 줄 추가」·「견적 외 비용 줄 추가」(1024 이상) · 조정 행 모양 · PM 권한 밖 줄(동작으로만 잠김 · 붙여넣기 건너뜀 요약) · 조정 줄 삭제 확인(`ui/confirm-dialog`) · 표 위 한 줄·EMPTY 조정 변형 · 소분류 설명 힌트 · 단위 확장 · E2E 1.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 부록 B).** (1) **사용자 D10·D12:** 정산에서도 PM이 새 줄을 추가한다(견적 칸 0) — 「견적 외 비용 줄 추가」가 정산에도 렌더되고, 「조정 줄 추가」가 「줄 추가」 없이 혼자 서는 상태는 완료 하나다. (2) **B-23:** 조정 권한만 있는 경영관리에게 거래처 목록이 빈 채로 오던 참조 로드 조건을 고친다. (3) **B-31:** PM의 조정 행 Delete를 무동작 + 이유로 정한다. (4) **B-24**(30줄 페이지에서 고정 그룹의 새 줄 자리)는 견적 표 페이지(04-19) 위에서 04-47 Task 1 ④의 몫이다 — 아래 `<probe_fallback>`.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 결정 번호는 「ENG-D*」로 적어 CEO 리뷰의 「사용자 D*」와 구별한다).** 이 화면 플랜에 닿는 것은 둘이다. (1) **새 줄 id(ENG-D10 — 견적 줄은 다른 그룹 플랜 몫):** 그룹 줄 추가 처리 지점 `addLineToGroup(lineKind)`는 표의 기존 새 줄 생성 함수(`newDraftLine` — 04-30이 ENG-D10으로 `crypto.randomUUID()` + `isNew: true`를 싣게 한 한 곳)를 그대로 거쳐 새 줄을 만든다 — 이 플랜이 새 줄 id를 따로 만들지 않아 재전송 중복 방지가 조정·견적 외 비용 줄에도 저절로 적용된다. (2) **소분류(04-13 `superRefine`):** 조정·견적 외 비용 새 줄은 소분류를 빈 값으로 보낸다 — 서버가 종류 값을 적고, 액션 스키마는 `quote` 종류에서만 소분류를 요구한다. 화면은 이 두 종류의 소분류 칸을 읽기 전용 글자(`조정`·`견적 외 비용`)로만 그린다.

**2026-09-23 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md` — 결정 번호 「DR-*」, 전역 원칙 P0).** (1) **DR-22 · DR-35:** PM에게 조정 줄은 **권한 밖 줄** — 잠긴 모양이고 `Enter`·`Delete`가 아무 일도 하지 않으며 이유 글자·오류 표시가 없고, 붙여넣기는 그 칸을 건너뛰어 기존 붙여넣기 요약에 `조정 줄 N칸 건너뜀`으로만 센다. 다른 잠긴 칸(정산 · 승인 차수 · 연결 문서)은 짧은 이유 한 줄 + 붙여넣기 오류 칸 그대로(옛 계획의 「조정 행 오류 셀 + 이유 문장」을 뒤집는다). (2) **DR-12:** 조정 줄 삭제 확인은 04-46의 `ui/confirm-dialog`(계약 2). (3) **DR-36 · DR-24:** 「조정 줄 추가」·「견적 외 비용 줄 추가」·EMPTY의 「조정 줄 추가」는 1024px 이상에서만(계약 6 — 그룹 A가 견적 표 「줄 추가」에 둔 같은 게이트를 쓴다). (4) **P0 문구:** `완료 · 견적 줄 잠김`(조정 권한자의 격자에서만 — `· 조정 줄 추가` 꼬리 없음) · 상한 `300줄 상한 · 상한은 관리자 설정`(`…할 수 없음 —` 접두 삭제, 계약 1 `Button` `aria-disabled`) · 삭제 결과 줄 `보관함으로 옮겨짐 · 복원은 관리자`. (5) **DR-14:** 독립 DOM 감사 폭 1280 · 1024 · 375(+ 좁은 PC 1000).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-13-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-25-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5) 「UI Considerations S4」 501–520행(partial ④ 조정 줄(PM) · (rev 5) 폰 · 좁은 PC 구조 컨트롤 · 해소 — 조정 줄과 복사) · 「적용 규칙 S4. 견적 줄 표」 1164행(견적 외 비용) · 1183–1208행(상태별 편집 범위 · 「조정」 줄 — PM에게는 DR-22 · 복사 · 상한) · 1223행(표 위 한 줄 순서) · 1228–1230행(1024 미만 · 코드표 설명) · 「SYSTEM.md 개정 ⑫」 910–913행(권한 밖 줄 · 붙여넣기 건너뜀) · Copywriting 349–354행(Empty — 견적 줄 표) · 372–374행(Error — 셀(잠김) · 셀(조정 줄, PM) 문구 없음) · 387행(막힘 — 조정 줄 추가(상한)) · 404행(표시 — 합계 행 오른쪽 한 줄) · 420행(Destructive — 조정 줄 삭제) · 「S16. 확인 모달」 1493–1526행 — 줄 번호는 편집 시점에 `grep -n "### S4. 견적 줄 표" .planning/phases/04-project-quote-ledger/04-UI-SPEC.md`로 다시 찾는다
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-02(조정 줄·견적 외 비용 줄의 화면)** 다. 수용 기준은 D-48·D-83·D-93과 UI-SPEC S4에서 끌어왔다(옛 04-13에서 그대로 — 서버 쪽 truths와 금지 항목은 04-13에 남았다). S4 `## UI Considerations`의 「조정 줄 추가 상한」 partial은 truths로, 조정 줄이 붙은 표의 overflow는 backstop 마커로 옮겼다.

**계획 단계 가정(표면화 — 04-13에서 옮김):** 조정 줄의 순서는 조정 그룹 안에서 만든 순서이고 Alt+↑↓로 그룹 밖으로 옮길 수 없다(고정 그룹). 경영관리 = 권한표 `projects.adjustment` 쓰기(시드 기본값 없음 — 04-13).

**30줄 페이지와 고정 그룹의 새 줄(CEO 리뷰 B-24 — 이 플랜 밖):** UI-SPEC §7-3 (자)의 「새 줄은 만든 페이지에 붙는다」는 표 맨 아래 고정 그룹(`조정`·`견적 외 비용`)의 새 줄에는 자리가 정해져 있지 않다. 견적 표 30줄 페이지는 이 플랜 뒤의 04-19가 만든다 — 이 플랜 시점에는 페이지가 없어 새 줄이 늘 그룹 끝에 보인다. 해법(버튼이 그 그룹의 끝을 가진 페이지로 이동해 새 줄에 포커스 · 31줄 이상 표 E2E)은 04-47 Task 1 ④가 가진다(`revealRowId` — 이 플랜의 두 그룹 버튼 처리 지점에서 올린다).

**정산의 줄 추가(사용자 D10·D12):** 정산에서 「줄 추가」·「견적 외 비용 줄 추가」가 렌더되는지는 04-12의 `structuralEditability`(서버 판정)가 정한다 — 이 플랜은 그 결과를 따라 렌더만 하고 상태 문자열을 보지 않는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 경영관리가 완료 프로젝트 화면에서 조정 줄을 넣고, 맨 아래 조정 그룹 · 합계 증가 · 목록 반영이 보인다</name>
  <files>app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-line-kinds.spec.ts</files>
  <read_first>
    - `domain/quotes/lines.ts`의 `QuoteLineDto`(04-13 산출물 — `lineKind`·`cellEditability`) · `QuoteLineWriteRow.lineKind`
    - `domain/quotes/edit-scope.ts`(04-12·04-13 산출물) — `structuralEditability`의 조정 권한 축
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `groupBy`·`subcategoryLabel`·`addLine`·`addLineButton`·열 정의를 찾아 범위만 Read
    - `test/e2e/revenue-section.spec.ts` 10–25행(계급 권한을 테스트에서 켜는 형태)
  </read_first>
  <action>
① `[id]/page.tsx`가 `can(viewer, "projects.adjustment", "write")`를 계산해 넘긴다. **참조 목록 로드 조건(CEO 리뷰 B-23):** 지금 거래처·소분류 참조(`listProjectFormReferences`)는 `canWrite`일 때만 불러와 조정 권한만 있는 경영관리에게 편집 가능한 거래처 칸이 빈 선택으로 열린다 — 조건을 `canWrite || canAdjust`로 바꾼다.

② `quote-table.tsx`: (a) 그룹을 줄 종류 먼저 가른다 — `quote`는 소분류 코드표 그룹(정렬 순), 그 뒤 `견적 외 비용`, **맨 아래 고정 그룹 `조정`** (b) 조정 권한자에게 3차 「조정 줄 추가」를 「줄 추가」 옆에(「줄 추가」가 렌더되지 않는 완료에서는 혼자 — 정산은 사용자 D10으로 「줄 추가」가 있다) 렌더해 조정 그룹 끝에 새 줄(`lineKind: adjustment`)을 만들고 실행가 셀을 편집 상태로 연다 — 그룹을 지정해 줄을 더하는 버튼(이 「조정 줄 추가」와 Task 3의 「견적 외 비용 줄 추가」)은 `quote-table.tsx`의 **처리 지점 하나**(`addLineToGroup(lineKind)` — 새 줄 id를 돌려준다)를 지난다. 이 처리 지점은 표의 기존 새 줄 생성 함수(`newDraftLine`)로 줄을 만들고 `lineKind`만 덧씌운다 — 새 줄 id 규칙(ENG-D10 — 04-30이 그 함수에 건 `crypto.randomUUID()` + `isNew: true`)을 그대로 받고, 조정·견적 외 비용 새 줄의 소분류는 빈 값으로 보낸다(04-13 `superRefine` — 서버가 종류 값을 적는다). 뒤의 04-47 Task 1 ④가 이 한 지점에서 `revealRowId`를 올려 그룹 끝 쪽으로 옮긴다(B-24) (c) 조정 행은 소분류 칸 글자 `조정`, 수량·단가·상태 `—`, 견적가 `0`. 새 색·아이콘을 쓰지 않는다. (d) 「조정 줄 추가」는 **1024px 이상에서만** 렌더한다(DR-36 · 계약 6) — 04-30이 견적 표 「줄 추가」·EMPTY 「첫 줄 만들기」에 쓴 `useEditableWidth()`(`ui/table/use-editable-width.ts` — 04-30 SUMMARY로 이름 확인)를 같이 쓰고 두 번째 폭 판정을 만들지 않는다. 1024 미만에서 조정 행 셀은 04-30의 `readonly` 전환을 그대로 받는다. 완료 프로젝트에서 조정 권한자에게 1차 「일괄 저장」이 렌더된다(편집 셀이 생기므로 — U-5 규칙의 결과).

③ **E2E(트레이서 증명).** `test/e2e/quote-line-kinds.spec.ts` 첫 케이스: 준비에서 `role-ceo`에 `projects` 보기 · `projects.adjustment` 쓰기를 켜고(`projects` 쓰기는 켜지 않는다 — 조정 권한만) 완료 프로젝트(줄 둘)를 둔다 → 대표로 상세를 열면 표 맨 아래에 「조정 줄 추가」 → 항목 `외화 송금 수수료` · 거래처 칸에서 거래처 하나를 고른다(B-23 — 목록이 비어 있지 않다) · 실행가 `120000` → Ctrl+S → 맨 아래 그룹 `조정`에 그 줄과 고른 거래처가 있고 합계 행 실행가가 120,000만큼 늘었다 → `/projects` 목록의 그 프로젝트 실행가(현재 목록 열)가 같은 금액을 반영한다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오고 종료 코드가 0이 아니다 — stylelint가 색 리터럴을 잡으면 조정 행에 새 색을 썼다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/quote-line-kinds.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 완료 프로젝트의 조정 줄이 화면 저장·합계·목록으로 이어지지 않았거나 기존 표 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 그룹 순서가 코드표 그룹 → 견적 외 비용 → 조정이고 그룹 판정이 `lineKind`를 먼저 본다
    - 조정 행에 새 색·아이콘이 없다(stylelint 초록 + 코드 리뷰)
    - `[id]/page.tsx`의 참조 목록 로드 조건이 `projects` 쓰기 또는 조정 권한이다(B-23)
    - E2E가 조정 권한만 있는 사람으로 완료 프로젝트에서 거래처를 고른 조정 줄 저장·합계 증가·목록 반영을 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>경영관리가 완료 프로젝트 화면에서 조정 줄을 넣으면 맨 아래 조정 그룹에 보이고 그 금액이 합계와 목록 실행가에 같은 값으로 들어간다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PM 시점과 조정 줄의 나머지 화면 계약 — 권한 밖 줄(DR-22 · DR-35: Enter·Delete 무반응 · 글자·오류 없음 · 붙여넣기 건너뜀 요약) · 삭제 확인(`ui/confirm-dialog`) · 표 위 한 줄 · EMPTY · 상한 비활성</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-line-kinds.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「적용 규칙 S4」 1183–1208행(상태별 편집 범위 표 · 「조정」 줄 — PM에게는 DR-22 · 상한) · 「SYSTEM.md 개정 ⑫」 910–913행((가) 권한 밖 줄 · (다) 권한 밖 칸 건너뜀) · Copywriting 349–354행(Empty — 견적 줄 표) · 372–374행(Error — 셀(잠김) = 표 위 한 줄 · 셀(조정 줄, PM) 문구 없음) · 387행(막힘 — 조정 줄 추가(상한)) · 404행(표시 — 합계 행 오른쪽 한 줄 — `조정 줄 3칸 건너뜀`은 muted) · 420행(Destructive — 조정 줄 삭제) · 「S16. 확인 모달」 1493–1526행
    - `docs/designs/plant8-erp-phase4-design-review-260923.md` 결정 원장 22·35행(DR-22 사용자 지정 · DR-35 범위)
    - `domain/quotes/edit-scope.ts`의 `tableLockLine`·`quoteTableEmptyState`·`visibleHintKeys`(04-30 — EMPTY 우선순위 ② 「조정 줄 추가」 자리를 이 플랜 몫으로 비워 두었다) · `structuralEditability`(04-12) · 04-13의 조정 줄 잠김(이유 문자열 없음 — DR-22)
    - `ui/confirm-dialog/ConfirmDialog.tsx`(04-46 — 계약 2의 props) · `ui/button/Button.tsx`(04-46 — `disabledReason` → `aria-disabled` + `aria-describedby`, 계약 1)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `ConfirmDialog`(04-46이 옮긴 견적 줄 삭제 확인)·`handlePasteAtCell`·`applyPaste`·`pasteWarning`(붙여넣기 요약)·`lineCap`(04-26)·`disabledReason`·`tableLockLine`·`onDeleteRow`를 찾아 범위만 Read
    - `ui/table/use-clipboard-paste.ts`(`applyPaste` 결과 모양 — 칸마다 값·오류) · `test/e2e/quote-table.spec.ts` 50–60행(`ClipboardEvent('paste')` + `DataTransfer` 붙여넣기 선례)
  </read_first>
  <behavior>
    - `tableLockLine({ status: 완료, canAdjust: true })` → `완료 · 견적 줄 잠김`(격자 — 조정 행이 편집 셀) · `canAdjust: false` → 줄 없음(편집 셀 0인 흰 머리글 읽기 표, P0) — 04-30이 이미 그렇게 두었으면 확인만
    - `quoteTableEmptyState`: 완료 + 조정 권한 → 「조정 줄 추가」 · 진행 + 줄 추가 권한 + 조정 권한 → 「첫 줄 만들기」(①이 이긴다) · 정산 + 줄 추가 권한(사용자 D10) + 조정 권한 → 「첫 줄 만들기」(①) · 정산 + 조정 권한만(`projects` 쓰기 없음, 기간 권리 없음) → 「조정 줄 추가」
  </behavior>
  <action>
① 04-30의 `tableLockLine`·`quoteTableEmptyState`에 조정 권한을 더한다 — `tableLockLine`은 완료에서 조정 권한자에게만 `완료 · 견적 줄 잠김`(꼬리 없음 — P0, 다음 한 수는 「조정 줄 추가」 버튼이 말한다), `quoteTableEmptyState`는 04-30이 비워 둔 우선순위 ② 자리에 「조정 줄 추가」를 채운다(Copywriting EMPTY 고르는 규칙 — 새 판정 함수를 만들지 않는다). 줄 추가 가능 여부(①)는 04-12의 `structuralEditability` 결과를 그대로 받는다 — 사용자 D10으로 정산에도 줄 추가가 있으므로 정산의 ①·② 순서는 다른 상태와 같다. 04-30의 `visibleHintKeys`에는 조정 구조(추가는 「조정 줄 추가」 버튼으로만 · 이동·복제 불가)를 넘겨, 조정 권한만 있는 사람의 힌트 줄에 견적 줄 새 줄(`Ctrl+Enter`)·줄 이동·줄 복제가 남지 않게 한다(C-07 — 되는 키만). 결정표 먼저(RED → GREEN). `[id]/page.tsx`가 세 함수에 조정 권한을 넘긴다. EMPTY의 「조정 줄 추가」 버튼도 1024px 이상에서만 렌더한다(Task 1 ②(d)의 같은 게이트 — 그 밖의 폭은 사실만).

② **권한 밖 줄(DR-22 — 사용자 지정, DR-35로 범위 확정).** `quote-table.tsx`에서 조정 권한이 없는 사람에게 조정 행(`lineKind === "adjustment"`)은 **권한 밖 줄**이다: (a) 셀은 서버 DTO의 `locked` 모양 그대로이고 `Enter`·클릭·글자 입력이 편집 모드를 열지 않으며, 이유 한 줄·오류 셀을 그리지 않는다(표 위 잠김 줄에도 조정 줄 이유를 더하지 않는다) (b) `Delete`는 삭제 확인을 열지 않고 보관 목록(`archivedLineIds`)에 넣지 않으며 아무 표시도 하지 않는다(CEO 리뷰 B-31 — 서버까지 가서 배치 전체가 거부되는 경로를 만들지 않는다) (c) 붙여넣기(`handlePasteAtCell`)가 그 행의 칸에 떨어지면 값을 바꾸지 않고 오류로도 두지 않으며 그 칸 수를 세어, 04-04의 붙여넣기 요약 문자열(합계 행 오른쪽)에 `조정 줄 {N}칸 건너뜀` 한 항목으로 더한다 — `ui/table`의 `applyPaste`에 칸마다 건너뛰기 판정을 받을 자리가 없으면 `handlePasteAtCell`에서 결과를 걸러 같은 효과를 내고 `ui/table`은 고치지 않는다(뒤의 04-47이 요약 항목들을 한 줄 결합 순서 — danger → warning → muted — 로 모은다, DR-16). **권한 밖 줄이 아닌 잠긴 칸**(정산 · 승인 차수 · 연결 문서 — DR-35)은 이 분기를 타지 않는다: 편집 시도 때 짧은 이유 한 줄, 붙여넣기는 오류 셀(04-04 규약 그대로). 경영관리의 `Delete`는 조정 행이면 04-46이 옮긴 `ui/confirm-dialog`로 `조정 줄 삭제` 확인(제목 `조정 줄 삭제` · 부제 `{항목} · {실행가}` · 결과 줄 `보관함으로 옮겨짐 · 복원은 관리자` · 1차 `조정 줄 삭제` · 2차 `취소 Esc` — 새 모달 마크업을 만들지 않는다, DR-12) — 1차가 04-12의 보관 경로(`archivedLineIds`)를 그대로 탄다. 「조정 줄 추가」는 04-26의 상한 판정(`lineCap`)을 같이 쓰고 닿으면 `Button`의 `disabledReason`으로 `{cap}줄 상한 · 상한은 관리자 설정`(계약 1 — `aria-disabled` + `aria-describedby`, `reasonTone` 기본 `block`).

③ E2E: 담당 PM으로 같은 완료 프로젝트를 열면 조정 행이 보이고(숨기지 않는다) 「조정 줄 추가」가 없다 · **담당 PM으로 진행 프로젝트의 조정 행 실행가 셀에서 `Enter` → 편집 입력 요소가 생기지 않고, 그 행 아래 이유 줄·오류 셀 표시(오류 셀 클래스)가 0개다 · `Delete` → 대화상자가 열리지 않고 행이 그대로이며 1차 「일괄 저장 N」의 N이 늘지 않는다(B-31 · DR-22)** · **정산 프로젝트(준비에서 DB로 — 기존 견적 줄 하나 + 조정 줄 하나)에서 담당 PM이 두 줄에 걸친 TSV를 수량 열부터 붙여 넣는다(04-04 E2E의 `ClipboardEvent('paste')` 선례) → 조정 행 칸은 값이 그대로이고 오류 셀이 아니며 합계 행 요약에 `조정 줄 N칸 건너뜀` · 견적 줄의 수량 칸은 오류 셀이고 그 칸의 이유가 `정산 · 실행가와 새 줄만`(DR-35 — 권한 밖 줄만 조용하다)** · 경영관리 조정 줄 삭제 확인의 제목·결과 줄·1차 라벨 → 1차 → 저장 → 보관함에 `견적 줄` 항목 · 상한을 줄 수에 맞춘 차수에서 경영관리의 「조정 줄 추가」가 `aria-disabled`이고 `300줄 상한 · 상한은 관리자 설정`의 숫자가 설정 값이다(`aria-describedby`로 연결).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 표 위 한 줄·EMPTY의 조정 변형 표가 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-line-kinds.spec.ts test/e2e/quote-edit-scope.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — PM이 조정 행을 못 보거나 고칠 수 있거나, 조정 행에 이유 글자·오류 셀이 생기거나, Delete가 모달·dirty를 만들거나, 붙여넣기가 조정 칸을 바꾸거나 오류로 두거나 요약에 세지 않았거나, 권한 밖이 아닌 잠긴 칸의 이유·오류 셀이 사라졌거나(DR-35), 삭제 확인·상한 화면이 계약과 다르거나, 기존 표 붙여넣기 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표가 `behavior` 두 묶음을 덮고 정산 두 케이스(줄 추가 권한 있음 → 첫 줄 만들기 · 조정 권한만 → 조정 줄 추가)가 있다 · 04-30의 `quoteTableEmptyState`·`visibleHintKeys`를 확장했고 새 판정 함수가 없다 · 완료 표 위 줄이 조정 권한자에게만 `완료 · 견적 줄 잠김`이다
    - PM 화면에 조정 행이 렌더되고(E2E가 행 존재를 단언) `Enter`가 편집을 열지 않으며 이유 글자·오류 셀이 없다(E2E, DR-22)
    - PM의 조정 행 `Delete`가 모달·dirty·표시 없이 무반응이다(E2E, B-31 · DR-22)
    - PM의 붙여넣기가 조정 칸을 건너뛰고 요약에 `조정 줄 N칸 건너뜀`만 남기며, 같은 붙여넣기의 정산 잠긴 칸은 오류 셀 + `정산 · 실행가와 새 줄만`이다(E2E, DR-22 · DR-35) · `ui/table` diff에 이 태스크의 변경이 없다
    - 조정 줄 삭제 확인이 `ui/confirm-dialog`이고 제목·결과 줄·1차 라벨이 rev 5 Copywriting이며 보관함에서 복원 가능하다(E2E) · `quote-table.tsx`에 조정 줄 삭제용 `<div>` 모달 마크업이 없다
    - 「조정 줄 추가」의 상한 이유가 `Button` `disabledReason`(`aria-disabled`)이고 숫자가 04-26의 `lineCap`에서 온다(새 상한 계산 없음)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>PM은 조정 줄을 보되 글자 없이 동작으로만 막히고, 붙여넣기는 조정 칸을 건너뛰어 요약에만 세며, 다른 잠긴 칸은 이유를 그대로 보이고, 경영관리는 어느 상태에서든 조정 줄을 넣고 확인 뒤 지우며, 상한·EMPTY·표 위 한 줄이 조정 권한을 반영한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 견적 외 비용 줄 추가(D-48) + 소분류 설명 힌트(D-93) + 화면 검증</name>
  <files>app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-line-kinds.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「적용 규칙 S4」 1164행(견적 외 비용) · 1228–1230행(1024 미만 보기 전용 · 코드표 설명 — 소분류 셀 편집 중) · 「적용 규칙 S14」 1472행(편집 표의 코드표 셀 행) · 「UI Considerations S4」 515행((rev 5) 폰 · 좁은 PC 구조 컨트롤 — backstop)
    - `ui/select/Select.tsx`(04-25 산출물 — 옵션 `description`이 있으면 편집 중 힌트, 오류가 이긴다)
    - `domain/projects/references.ts`(04-25 산출물 — 소분류 옵션의 `description`)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - 진행 + 담당 PM → 「견적 외 비용 줄 추가」로 만든 줄: 그룹 `견적 외 비용` · 견적가 0 · 실행가 −50,000 저장 가능
    - 정산 + 담당 PM → 「견적 외 비용 줄 추가」가 렌더되고(사용자 D10) 만든 줄의 수량·단가가 `—`, 실행가 −30,000 저장 가능(D12 — 견적 칸 0) · 완료 → 렌더되지 않는다(서버 거부는 04-13 통합)
    - 소분류 셀 편집 중 설명 한 줄이 보이고 편집을 끝내면 사라진다 · 오류가 있으면 오류만
  </behavior>
  <action>
① **견적 외 비용.** `quote-table.tsx`에 3차 「견적 외 비용 줄 추가」를 Task 1의 처리 지점 `addLineToGroup("out_of_quote")`로(두 번째 처리 지점을 만들지 않는다 — 04-47의 `revealRowId`가 한 곳에 걸린다) 「줄 추가」와 같은 구조 가능성(04-12의 `structuralEditability` — 사용자 D10으로 정산 포함, 완료 제외)·상한 조건·**1024px 이상 조건**(Task 1 ②(d)의 같은 게이트 — DR-36)으로 렌더한다 — 새 줄은 `lineKind: out_of_quote`로 `견적 외 비용` 그룹 끝에 생기고 항목 셀이 편집 상태로 열린다. 행 모양: 소분류 칸 글자 `견적 외 비용` · 수량·단가 `—` · 견적가 `0` · 실행가 편집(음수 허용) · 상태·비고는 견적 줄과 같다(정산의 새 줄 셀 단계는 04-12의 판정 · 04-30의 화면 새 줄 기본값 그대로 — 실행가·항목 편집, 견적 칸 잠김. 이 플랜이 정산 격자 동작을 따로 만들지 않는다). 서버 규칙은 04-13이 이미 만들었다.

② **소분류 설명 힌트.** `quote-table.tsx`의 소분류 셀 편집기에 넘기는 옵션에 04-25의 `references.ts` `description`을 싣는다 — 04-25의 `Select`가 편집 중에만 셀 아래 한 줄로 설명을 보이고 오류가 있으면 오류가 이긴다. 읽기 상태의 행 높이는 늘지 않는다. 필터 줄·폰 행 시트에는 설명을 보이지 않는다(S14 표).

③ **E2E.** 담당 PM이 진행 프로젝트에서 견적 외 비용 줄을 −50,000으로 저장 · 정산 프로젝트(준비에서 DB로)에서 「견적 외 비용 줄 추가」로 만든 줄의 수량·단가가 `—`이고 실행가 −30,000이 저장된다(사용자 D10·D12) · 완료 프로젝트에 「견적 외 비용 줄 추가」가 없다 · 소분류 셀 편집 중 설명 한 줄이 보이고 편집을 끝내면 사라진다.

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 backstop 둘을 DOM 실측한다: (a) 조정·견적 외 비용 그룹이 붙은 표의 **1280 · 1024 · 375** 가로 스크롤 0과 폰 P1 세 열(DR-14 감사 폭) (b) **1000 · 375**에서 「조정 줄 추가」·「견적 외 비용 줄 추가」·EMPTY 「조정 줄 추가」가 DOM에 없고 조정 행 셀이 `Enter`·클릭에 편집으로 들어가지 않음(DR-36) → 필요하면 `project-detail.module.css`만 고친다 → `CI=true pnpm test` 한 번. 폭별 실측값을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-line-kinds.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 견적 외 비용 줄이나 소분류 설명 힌트가 계약과 다르다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 판정(조정·견적 외 비용 그룹이 붙은 표의 1280 · 1024 · 375 레이아웃 · 1000 · 375의 구조 컨트롤 부재)을 SUMMARY에서 확인</test>
      <expected>세 폭 모두 가로 스크롤 0, 폰 P1이 항목·실행가·상태 세 열, 1000 · 375에서 두 추가 버튼·EMPTY 「조정 줄 추가」 없음</expected>
      <why_human>폭·열 접기는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 「견적 외 비용 줄 추가」가 줄 추가 가능 상태(정산 포함, 완료 제외)이고 1024px 이상일 때만 렌더되고 만든 줄의 종류가 `out_of_quote`다(E2E · DOM 감사)
    - 소분류 셀 편집 중에만 설명 한 줄이 보인다(E2E)
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>견적 외 비용 줄이 화면에서 견적 줄 원장의 한 종류로 만들어지고, 소분류를 고르는 동안 그 뜻이 한 줄로 보이며, 종류 그룹이 붙은 표가 폭 계약을 지킨다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 DTO(`lineKind`·`cellEditability`) → 표 | 셀 단계·그룹이 서버에서 온다. 화면이 계급·상태를 추론하면 서버 규칙과 어긋난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-63 | Elevation of Privilege | 조정 줄 편집 컨트롤(화면 쪽) | high | mitigate | 「조정 줄 추가」·조정 행 편집 가능성이 서버가 넘긴 조정 권한과 `cellEditability`에서만 온다. PM의 편집 시도(Enter·Delete·붙여넣기)가 권한 밖 줄로 무반응인 것을 E2E로 고정하고(DR-22), 우회 페이로드 거부는 04-13 통합 테스트가 본다 |
| T-04-66 | Information Disclosure | 조정 줄의 가시성(화면 쪽) | medium | mitigate | 조정 행을 PM에게도 같은 표에 렌더한다 — 숨김 분기를 두지 않는다. E2E가 PM 화면의 행 존재를 단언한다(04-13 금지 항목의 화면 쪽). 합계 쪽은 04-13 불변식 테스트 |
| T-04-66c | Tampering | 권한 밖 줄 붙여넣기 건너뜀(DR-22) | low | mitigate | 화면은 조정 칸 값을 바꾸지 않아 페이로드에 싣지 않는다 — 위조 페이로드로 조정 칸을 실어도 04-13의 게이트가 방어 거부(`조정 줄 · 경영관리만`)로 배치 전부를 거부한다. 건너뛴 칸 수는 요약 글자로 보여 조용히 버리지 않는다 |
| T-04-66b | Information Disclosure | 참조 목록 로드 조건 넓히기(B-23) | low | accept | 거래처·소분류 참조는 이미 `projects` 쓰기 권한자에게 내려가는 목록이고 금액이 없다 — 조정 권한자에게 더 내려도 새 정보가 노출되지 않는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 독립 DOM 감사(조정·견적 외 비용 그룹 표 1280 · 1024 · 375 · 좁은 PC 1000의 구조 컨트롤 부재) 판정이 SUMMARY에 있다
- `package.json` 의존성 객체(dependencies·devDependencies·optionalDependencies·peerDependencies)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있다 (원문 비교 아님 — 04-03 재개 시 scripts 추가로 깨지지 않게)
</verification>

<success_criteria>
- 경영관리가 어느 상태에서든 화면에서 조정 줄로 원가를 보정하고, PM은 그것을 같은 표에서 보되 고칠 수 없다
- 견적 외 비용 줄이 화면에서 견적가 0인 견적 줄의 한 종류로 만들어진다
- 소분류를 고르는 동안 코드표 설명이 한 줄로 보인다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 변경된 판정 | `tableLockLine`·`quoteTableEmptyState`(우선순위 ② 자리)·`visibleHintKeys`(조정 권한 축 — 04-30 함수 확장) |
| 변경된 기존 심볼 | `QuoteLedger` 그룹 순서(`lineKind` 먼저)·「조정 줄 추가」·「견적 외 비용 줄 추가」(정산 포함 — D10)·조정 행 모양·PM 권한 밖 줄(Enter·Delete 무반응 · 글자·오류 없음 · 붙여넣기 건너뜀 + 요약 `조정 줄 N칸 건너뜀` — DR-22 · DR-35)·조정 줄 삭제 확인(`ui/confirm-dialog` — DR-12)·두 추가 버튼의 1024 게이트(DR-36) · 그룹 줄 추가 처리 지점 하나(`addLineToGroup` — 04-47의 `revealRowId` 자리) · 소분류 셀 설명 힌트 · 상세 페이지의 조정 권한 전달 · 참조 목록 로드 조건 `canWrite \|\| canAdjust`(B-23) |
| 테스트 | `test/e2e/quote-line-kinds.spec.ts`(신규) · `quote-edit-scope.test.ts` 확장 |
| 새 스키마·마이그레이션·액션 | 없음 — 서버 쪽은 04-13 |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 30줄 페이지에서 고정 그룹(`조정`·`견적 외 비용`) 새 줄의 페이지 이동·포커스(CEO 리뷰 B-24 — 04-47 Task 1 ④) · 정산 격자의 새 줄·구조 컨트롤(사용자 D10·D12 — 04-30) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 사용자 D10·D12 — 정산에서 PM 새 줄 추가(견적 칸 0) | DECISION | Plan 04-23, truths·Task 1 ②(b)·Task 2 behavior·Task 3 ①③: 「견적 외 비용 줄 추가」 정산 렌더, 「조정 줄 추가」 단독은 완료만, EMPTY 정산 ① 우선 |
| B-23 — 조정 권한만 있는 경영관리의 거래처 목록이 빔 | WARNING | Plan 04-23, Task 1 ①③: 참조 로드 `canWrite \|\| canAdjust` + E2E 거래처 선택 |
| B-31 — PM의 조정 행 Delete 미정(배치 거부로 감) | NOTE | Plan 04-23, Task 2 ②③: 무동작 + 이유 한 줄, E2E |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-D10 (R9) — 새 줄 클라이언트 UUID(재전송 중복) | DECISION(P1) | 견적 줄 쪽 구현은 다른 그룹 플랜. Plan 04-23, objective·Task 1 ②(b): `addLineToGroup`이 표의 한 새 줄 생성 함수를 거쳐 조정·견적 외 비용 새 줄도 같은 id 규칙을 받는다 |
| B §2 — 조정·견적 외 비용 새 줄의 빈 소분류(zod) | P3 | 스키마 수정은 Plan 04-13. Plan 04-23, objective·Task 1 ②(b)·Task 3 ①: 두 종류 새 줄은 소분류를 빈 값으로 보낸다 |

### Round 3 — 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-22 (사용자 지정 · P0 출발점) — PM의 읽기 전용 조정 줄에서 Enter가 오류 셀 | MEDIUM | Plan 04-23, truths · Task 2 ②(a)(b)(c)·③: 권한 밖 줄 — Enter·Delete 무반응, 글자·오류 없음, 붙여넣기 건너뜀 + 요약 `조정 줄 N칸 건너뜀` · E2E |
| DR-35 — DR-22의 범위(다른 잠긴 칸은 이유 + 붙여넣기 오류 유지) | 후속 | Plan 04-23, Task 2 ②·③: 정산 잠긴 칸 오류 셀 + `정산 · 실행가와 새 줄만` 같은 붙여넣기 E2E |
| DR-12 — 확인 모달 공용 컴포넌트 | HIGH | Plan 04-23, Task 2 ②: 조정 줄 삭제 = `ui/confirm-dialog`(Plan 04-46) · rev 5 결과 줄 |
| DR-36 · DR-24 — 1024 미만 표 보기 전용 · 추가 컨트롤 미렌더 | 후속 | Plan 04-23, truths · Task 1 ②(d) · Task 2 ① · Task 3 ①: 두 추가 버튼·EMPTY 「조정 줄 추가」는 1024 이상에서만(그룹 A 게이트 재사용) · DOM 감사 1000·375 |
| DR-14 — DOM 감사 폭 1024 | HIGH | Plan 04-23, Task 3 ④: 1280 · 1024 · 375 |
| P0 · F2 — `…할 수 없음 —` 접두 · 완료 표 위 줄 꼬리 · 삭제 본문 문장 | MEDIUM | Plan 04-23, truths · Task 2: `300줄 상한 · 상한은 관리자 설정` · `완료 · 견적 줄 잠김`(조정 권한자 격자에서만) · `보관함으로 옮겨짐 · 복원은 관리자` |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-24 — 30줄 페이지에서 고정 그룹 새 줄의 자리 | 견적 표 페이지는 뒤 플랜 04-19가 만든다 — 이 플랜 시점에 페이지가 없다. Plan 04-47 Task 1 ④(`revealRowId`로 그룹 끝 쪽 이동 + 포커스 · 31줄 E2E — 옛 04-19 Task 3 ④, Round 4 포인터 정정)가 반영했다 |

### Round 4 — 체커 revision 1 후속 정리(2026-09-23 · 04-08 · 04-19 분할 포인터)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 분할 포인터 — `Button`(계약 1)·`ui/confirm-dialog`(계약 2)·견적 줄 삭제 확인 이관의 주인 | NOTE | truth · objective (2) · read_first 둘 · Task 2 ② · Round 3 DR-12 행: 「04-08」 → **04-46**. 04-46은 조상(04-23 → 04-13 → 04-12 → 04-44 → 04-22 → 04-11 → 04-21 → 04-09 → 04-28 → 04-46) — `depends_on` 변경 없음 |
| 분할 포인터 — B-24(그룹 버튼 새 줄의 쪽 이동) · 합계 행 한 줄(DR-16) | NOTE | objective (4) · `<probe_fallback>` · Task 1 ②(b) · Task 2 ① · Artifacts · 「다른 플랜으로 간 것」 · Round 3 Deferred B-24 행: 「04-19 Task 3 ④」 → **04-47 Task 1 ④**, 한 줄 결합(DR-16) → 04-47. 쪽 나눔 자체는 04-19 그대로. 04-47은 이 플랜의 후행(`addLineToGroup`의 소비자 — 04-47 → 04-19 → 04-23) — 의존 추가 없음 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-23-SUMMARY.md` when done
</output>
