---
phase: 04-project-quote-ledger
plan: 24
type: execute
wave: 20
depends_on: ["04-14", "04-40"]
files_modified:

  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revision-dialogs.tsx
  - app/(app)/projects/[id]/revision-section.tsx
  - app/(app)/projects/[id]/previous-revision.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - ui/table/use-dirty-storage.ts
  - test/unit/ui/dirty-storage.test.ts
  - test/e2e/quote-revisions.spec.ts

autonomous: true
requirements: [PROJ-07, PROJ-05, UX-04]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "「복사해 새 차수」 확인은 `ui/confirm-dialog`(04-46 — 계약 2)다: 제목 `복사해 새 차수` · 부제 `상세 견적 {n}차 · {k}줄` · 결과 줄 `{n}차 {k}줄을 복사해 {n+1}차 · 되돌리기 없음`(조정 줄이 있으면 한 줄 더 `조정 {m}줄은 {n+1}차에도 그대로`) · 1차 `새 차수 만들기` · 2차 `취소 Esc` — `{k}`는 조정 줄을 세지 않는다. 성공하면 다이얼로그가 닫히고 토스트 `새 차수 만들기 · 상세 견적 {n+1}차`(누른 1차 라벨 — DR-21)가 뜬다(UI-SPEC rev 5 Copywriting `Destructive — 새 차수` · `SUCCESS — 등록 / 새 차수 / 상태 전환` · 「적용 규칙 S5」)"
    - "「복사해 새 차수」는 수주중·진행·미수주에서만 렌더되고 정산·완료에서는 없다 — 렌더 조건은 서버의 `canCreateRevision` 하나다(CEO-D10 — 정산에 줄 추가는 열렸지만 새 차수는 서버 거부 · D-47, 「적용 규칙 S3」)"
    - "새 차수의 1차는 화면이 보고 있던 차수 id(`fromRevisionId`)를 싣고, 누른 뒤 응답이 올 때까지 `…`이다 — 더블클릭해도 차수가 하나만 생기고, 다른 탭에서 먼저 만들었으면 `다른 사람이 먼저 새 차수를 만듦 · 새로 고침`이 1차 왼쪽 막힘 자리에 나온다(CEO 리뷰 B-02, Copywriting `막힘 — 새 차수(동시·오래된 화면, B-02)`)"
    - "표에 저장 안 한 편집이 있으면(1차 「일괄 저장 N」의 N ≥ 1) 새 차수·고객 승인 표시 다이얼로그의 1차가 `aria-disabled`(포커스는 받고 이유가 `aria-describedby`로 읽힌다)이고 1차 왼쪽에 `저장 안 한 편집 {N}칸 · 먼저 일괄 저장`(`block`)이 보인다 — 「상태 바꾸기」의 DR-6 차단과 같은 함수 · 같은 글자이고, 저장하면 켜진다. 새 차수로 보관 키가 바뀌어 입력이 조용히 사라지는 길이 없다(CEO 리뷰 B-03 · ENG-D9 · DR-6, Copywriting `막힘 — 미저장 편집`, UI Considerations S3 (rev 5) interactive)"
    - "서버 거부는 게이트 문자열 그대로 1차 왼쪽 막힘 자리에 나오고 다이얼로그가 닫히지 않으며 토스트가 없다 — 빈 차수면 `복사할 견적 줄 없음 · 첫 줄 만들기`(UI Considerations S16 error, Copywriting `막힘 — 새 차수(빈 차수, 04-14)`)"
    - "「고객 승인 표시」·「승인 표시 취소」는 수주중·진행·미수주·정산에서 담당 PM(쓰기 권한 포함 — 서버 판정)에게만 렌더되고 완료에서는 둘 다 없다 — 승인일은 완료에서도 부제에 그대로 보인다(CEO-D19 · D-56 · B-30, UI Considerations S3 해소 — 상태별 고객 승인)"
    - "현재 차수가 승인되면 표의 기존 견적 줄 수량·단가·상태·소분류 칸이 잠김 모양이 되고 표 위 한 줄과 잠긴 셀의 편집 시도 이유(짧은 이유 한 줄 — DR-35)가 `{n}차 고객 승인됨 · 고치려면 새 차수`다 — 실행가는 그대로 편집되고, 새 줄의 수량·단가는 잠긴 채 생긴다(새 줄의 소분류는 고른다). 판정은 04-40의 함수 결과이고 화면이 승인 여부로 칸을 추론하지 않는다(CEO-D7 · OV-1 · ENG-D7, Copywriting `Error — 셀(잠김) = 표 위 한 줄`, UI Considerations S4 partial ③)"
    - "「고객 승인 표시」는 `ui/confirm-dialog`다: 제목 `고객 승인 표시` · 부제 `상세 견적 {n}차 · {합계}` · 확인 근거 한 칸 `승인일`(`evidenceField` — 날짜, 기본 오늘 KST, 280 — 개정 ⑭ · DR-20) · 1차 `고객 승인 표시` · 2차 `취소 Esc` · 결과 줄 없음. 열리면 포커스가 승인일 칸이고, 칸이 비거나 형식이 틀리면 1차가 막히고 칸 아래 `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요`가 붙는다. 승인되면 토스트 없이 부제·차수 섹션 상태가 `승인`으로 바뀐다(Copywriting `확인 — 고객 승인 표시(04-14 · DR-20)`, 「적용 규칙 S5」 · 「S16」)"
    - "「고객 승인 표시」의 1차는 화면이 보고 있는 현재 차수의 견적 합계와 내용 토큰(04-14 `listRevisionSummaries`의 현재 차수 행)을 기준값으로 싣고, 그 사이 다른 사람이 견적을 바꿨으면 서버 거부 `견적이 바뀜 · 새로 고침`이 1차 왼쪽 막힘 자리에 나오며 다이얼로그가 열린 채다 — PM이 보지 않은 금액이 승인되지 않는다(ENG-D9 · Codex #2, Copywriting `막힘 — 고객 승인(견적 바뀜, ENG-D9)`)"
    - "견적 줄이 0개인 현재 차수에서 승인을 누르면 서버 거부 `승인할 견적 줄이 없음 · 첫 줄 만들기`가 막힘 자리에 나온다 — 화면이 줄 수로 버튼을 따로 판정하지 않는다(ENG-D4, Copywriting `막힘 — 고객 승인(빈 차수, ENG-D4)`)"
    - "견적 금액(`quote.amount`)을 볼 수 없는 사람에게는 확인할 합계가 없어 「고객 승인 표시」가 렌더되지 않는다 — 요약 행에 합계 키가 없으면 버튼이 없다(ENG-D9의 결과 — 04-14 `<probe_fallback>`)"
    - "승인 표시 취소는 그 차수에 연결 문서가 없을 때만 버튼이 있고, `ui/confirm-dialog`로 제목 `고객 승인 표시 취소` · 부제 `상세 견적 {n}차 · 승인일 {날짜}` · 결과 줄 `승인일 지워짐 · 이 차수의 지출결의가 다시 막힘` · 1차 `승인 표시 취소` · 2차 `닫기 Esc`(1차에 「취소」가 있어 컴포넌트가 파생)다(D-56, Copywriting `Destructive — 고객 승인 표시 취소`)"
    - "세 다이얼로그는 `Esc` = 2차이고, 제출 중에는 1차 `…` · 나머지 `aria-disabled` · `Esc`·가림막 무시다. 닫히면 트리거로 포커스가 돌아오고, 트리거가 사라졌으면(승인 표시 성공으로 「고객 승인 표시」가 「승인 표시 취소」로 바뀜) 머리 줄 제목으로 간다 — 폰(<700)은 같은 내용의 하단 시트다(UI Considerations S16 loading · interactive · populated)"
    - "부제에 `고객 승인 2026-09-19 박서연`이 붙고 견적 표시 번호 `26001-2차`가 보인다 — 번호는 04-14의 `quoteDisplayNumber` 파생이고 화면이 조립하지 않는다. 승인일 글자는 서버가 KST 달력 날짜로 만든 문자열 그대로이고 화면이 시간대를 계산하지 않는다(D-56, CEO 리뷰 B-25)"
    - "차수 섹션은 차수 · 생성일 · 줄 수 · 견적 합계 · 상태 · 동작 여섯 열, 최신이 위이고, 편집 가능한 셀이 0이라 캡션 있는 읽기 표(흰 머리글)다 — 이력 목록 컴포넌트를 쓰지 않는다(S5)"
    - "차수 섹션 상태 열: 최신이면서 미승인 = `현재`(accent) · 승인된 차수 = `승인`(success, 최신이어도 `승인`만) · 승인 없이 지나간 이전 차수 = 빈 칸(`—`로 채우지 않는다) · 현재 차수 행의 동작 칸도 빈 칸 · 차수가 하나뿐이면 「차수 열기」가 표에 없다(U-2 사용자 확정, UI Considerations S5 partial · zero-one-many)"
    - "이전 차수 행의 3차 「차수 열기」는 `<button aria-expanded aria-controls>`이고 누르면 **차수 섹션 바로 아래**에 이전 차수 읽기 섹션이 펼쳐진다 — 제목 `상세 견적 {n}차`(`--fs-sm` 600, 2px `--line-strong`으로 시작, `tabindex=-1`) + 그 차수 견적 줄 읽기 표(흰 머리글 `<table>` + 캡션 `상세 견적 {n}차 견적 줄` · 편집·저장·구조 컨트롤·복원 줄·힌트 줄·`조정` 그룹 없음 · 열 순서는 현재 견적 줄 표와 같다). 열리면 버튼 라벨이 `차수 닫기`, 포커스는 섹션 제목, 닫히면 버튼으로 돌아오고, 한 번에 하나다(DR-13, 「적용 규칙 S5」 · UI Considerations S5 (rev 5) populated · interactive)"
    - "이전 차수 읽기 섹션을 열고 닫아도 현재 원장 · 1차 「일괄 저장 N」 · dirty · URL이 그대로다 — `?revision=` 검색 파라미터로 원장을 바꾸는 경로가 없고 원장에 잠김 보기 모드가 없다. 현재 표에 미저장 편집이 있는 채 「차수 열기」 → 편집·N 유지(DR-13 — 편집 손실 경로 제거, UI Considerations S5 (rev 5) E2E)"
    - "읽기 섹션의 줄은 그 차수를 처음 열 때만 서버(04-14 `listRevisionLinesAction`)에서 받고 그동안 섹션 자리에 표 LOADING 뼈대(머리글 + `--surface` 행 3개, 300ms 지연)가 보인다. 받지 못하면 섹션 자리 한 줄 `{n}차 불러오지 못함 · 다시 시도`(`--danger`, 「다시 시도」 = 2차 — 그 차수 줄만 다시 받는다, 현재 원장 무영향), 0줄이면 `이 차수에 견적 줄이 없습니다`(버튼 없음)다(Copywriting `Error — 이전 차수 읽기 섹션(DR-13)` · `Empty — 이전 차수 읽기 섹션`, UI Considerations S5 (rev 5) loading · error · empty)"
    - "읽기 섹션의 합계 행은 전체 줄 기준 `합계 (공급가액 · N줄)` · 줄 번호는 전체 표시 순서로 이어 센다 · 환율 없는 줄은 `계산 불가 · 환율 없음` + `계산 불가 N건` 배지이고 합계는 계산된 것만 · 폰은 S4와 같은 P1(항목 · 실행가 · 상태)이다. 이 플랜은 읽기 섹션을 자르지 않고 전체 줄을 넘긴다 — 31줄부터의 30줄 페이지 줄(`ui/pagination` `<button>` 갈래)과 쪽 전환 뒤 섹션 제목 포커스는 04-19가 `ui/table` `pagination`(`focusHeadingId`)으로 붙인다(쪽 나눔 한 구현 — W2)(UI Considerations S5 (rev 5) partial · zero-one-many · overflow · interactive — 사용자 확정)"
    - "차수 섹션 폰 P1은 차수·견적 합계·(동작), 접힌 줄은 상태·생성일·줄 수이고 빈 값 자리를 건너뛴다(S5 overflow · partial)"
    - "현재 차수 표를 열 때 같은 프로젝트의 **다른 차수 id**로 브라우저에 보관된 편집이 있으면 표 위에 `{n}차 저장 안 한 편집 {N}칸 · 복사 / 버림`(`--fs-sm --muted` 한 줄 + 3차 둘 — 배너 아님)이 현재 차수 복원 줄 뒤 · 잠김 줄 앞에 보인다. 여럿이면 가장 최근 차수 하나만 보이고 버리면 다음 것이 보인다 — 현재 차수에 자동으로 합치지 않는다(DR-4 · DR-31, 「적용 규칙 S18」 · Copywriting `미저장 편집 복원(이전 차수, DR-4)` · UI Considerations S18 populated)"
    - "「복사」는 보관된 편집이 있는 그 차수의 줄 전체(보관된 값을 그 차수 값 위에 덮은 행)를 견적 줄 표 열 순서 TSV + 앱 형식(`application/x-plant8-quote-lines+json`)으로 클립보드에 쓰고(직렬화는 이 플랜의 `quoteLineReadColumns` `copyText` + `quoteLineClipboardMeta` 한 곳 — 뒤 웨이브 04-19의 `Ctrl+C` 격자 복사가 같은 함수를 쓰는 **형식의 주인**, UI-SPEC rev 5 적용 규칙 S18 「`Ctrl+C` 범위 복사와 같은 형식(D-70)」 1552–1553행 · W1), 버튼 자리에 `복사됨 {N}칸`(`--success`)을 보이며 줄은 남는다 — 현재 차수 표에서 붙여 넣으면 그 칸들이 dirty가 되고 계산 열은 앱 형식 규칙대로 무시된다. 클립보드 쓰기가 막히면 「복사」 옆 `복사하지 못함`(`block`)이고 보관본은 지우지 않는다 · 「버림」은 확인 없이 그 보관본만 지우고 줄이 사라진다(서버 값은 그대로)(DR-4 · D-70, UI Considerations S18 interactive · error)"
    - "다른 차수 보관본이 없으면 이전 차수 복원 줄 자체가 없고(빈 줄 · 안내 없음), 보관본의 줄이 이미 보관되거나 잠겼어도 「복사」는 보관된 값을 그대로 쓴다 — 판정은 붙여 넣은 뒤 저장 때 서버가 한다 · 이전 차수 읽기 섹션에는 복원 줄이 없다(UI Considerations S18 empty · partial)"
    - statement: "차수 섹션은 제 오류 자리를 갖지 않고 상세 로드 실패 한 줄에 흡수된다 — 섹션 단독 오류 렌더가 없다"
      verification: backstop
    - statement: "차수 섹션과 이전 차수 읽기 섹션이 1280 · 1024 · 375에서 가로 스크롤 0이고 현재 행의 빈 동작 칸을 `—`로 채우지 않는다 — DOM 감사(DR-14 감사 폭)"
      verification: backstop
    - statement: "이전 차수 읽기 섹션이 0줄 · 1줄 · 31줄에서 각각 EMPTY 한 줄 · 한 줄 표 · 31줄 표로 렌더되고 합계 행 형식 `합계 (공급가액 · N줄)`과 조수사가 S4와 같다 — 렌더 감사(UI Considerations S5 (rev 5) zero-one-many). 31줄의 페이지 줄 있음·1줄의 없음은 04-19 감사"
      verification: backstop
    - statement: "375에서 이전 차수 복원 줄과 현재 차수 복원 줄(04-30 — 모든 폭에서 렌더, UI-SPEC rev 5 UI Considerations S18 overflow 649행)의 글자는 `keep-all`로 줄바꿈하고 — 이전 차수 복원 줄은 04-30이 `project-detail.module.css`에 둔 `.restoreBanner`(줄) + `.restoreActions`(3차 둘 묶음) 두 클래스를 그대로 쓴다(새 CSS 규칙 없음) — 3차 두 개(각 44px `--touch-min`)는 함께 다음 줄로 내려가며 두 줄은 각자 줄바꿈한다 · 가로 스크롤 0 — DOM 감사(UI Considerations S18 overflow — 사용자 확정)"
      verification: backstop
    - statement: "세 다이얼로그(새 차수 · 고객 승인 표시 · 승인 표시 취소)가 PC 480 모달과 375 하단 시트에서 제목·부제·결과 줄이 잘리지 않고(`keep-all` · 묶음 단위 줄바꿈 · 숫자 꺾임 없음), 결과 줄 0·1·2개에서 행동 줄 위치가 흔들리지 않는다 — DOM 감사(UI Considerations S16 overflow · zero-one-many의 이 플랜 사용처)"
      verification: backstop
  artifacts:
    - path: "app/(app)/projects/[id]/revision-section.tsx"
      provides: "차수 섹션 — ui/table 읽기 형태, 최신이 위, 이전 차수에만 「차수 열기」/「차수 닫기」 토글 버튼과 그 아래 이전 차수 읽기 섹션"
      contains: "aria-expanded"
    - path: "app/(app)/projects/[id]/revision-dialogs.tsx"
      provides: "복사해 새 차수 · 고객 승인 표시(확인 근거 칸 승인일) · 승인 표시 취소 — 셋 다 ui/confirm-dialog"
      contains: "ConfirmDialog"
    - path: "app/(app)/projects/[id]/previous-revision.tsx"
      provides: "이전 차수 읽기 섹션(처음 열 때만 받음 · 오류 줄 · 제목 포커스 — 쪽 나눔은 04-19 `pagination`) + 이전 차수 복원 줄(복사 · 버림) + 견적 줄 복사 형식의 주인(읽기 열 `copyText` · `quoteLineClipboardMeta` · `quoteLineClipboard` — 04-19 격자 복사도 이것을 쓴다, W1)"
      contains: "listRevisionLinesAction"
    - path: "ui/table/use-dirty-storage.ts"
      provides: "같은 프로젝트의 다른 차수 보관본 찾기(순수 함수 — 키 모양 그대로)"
      exports: ["findOtherRevisionDrafts"]
    - path: "test/e2e/quote-revisions.spec.ts"
      provides: "새 차수(더블클릭 한 번 · 미저장 편집 막힘 · 빈 차수) · 승인 표시와 취소(확인 근거 칸 · 미저장 편집 막힘 · 옛 기준값 거부 · 빈 차수 거부 · 포커스 복귀) · 승인 차수 칸 잠금(소분류 포함) · 완료의 버튼 부재 · 차수 섹션 · 이전 차수 읽기 섹션(원장 무영향 · 처음만 받음 · 오류 · 쪽) · 두 탭 이전 차수 보관본 복사→붙여넣기"
      min_lines: 140
  key_links:
    - from: "app/(app)/projects/[id]/revision-dialogs.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "세 확인이 04-46의 공용 다이얼로그 props(제목 · 부제 · 결과 줄 · 확인 근거 칸 · 1차 막힘 이유)만 채운다 — 자체 모달 마크업이 없다(DR-12)"
      pattern: "ConfirmDialog"
    - from: "app/(app)/projects/[id]/revision-dialogs.tsx"
      to: "app/(app)/projects/actions.ts"
      via: "다이얼로그의 1차가 04-14의 `createRevisionAction`·`setCustomerApprovalAction`을 부르고 거부 문자열을 그대로 막힘 자리에 쓴다"
      pattern: "setCustomerApprovalAction"
    - from: "app/(app)/projects/[id]/previous-revision.tsx"
      to: "app/(app)/projects/actions.ts"
      via: "읽기 섹션과 복원 줄 「복사」가 04-14의 보기 액션 `listRevisionLinesAction` 하나로 그 차수 줄을 받는다 — 원장을 바꾸지 않는다(DR-13 · DR-4)"
      pattern: "listRevisionLinesAction"
    - from: "app/(app)/projects/[id]/previous-revision.tsx"
      to: "ui/table/parse-tsv.ts"
      via: "복원 줄 「복사」의 TSV를 `quoteLineClipboard`가 `toTsv`(읽기 열 `copyText`)로 만들고 앱 형식 JSON은 `quoteLineClipboardMeta` — 견적 줄 복사 형식의 한 직렬화이고 04-19의 격자 `Ctrl+C`가 같은 두 함수를 쓴다(W1)"
      pattern: "quoteLineClipboardMeta"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/table/use-dirty-storage.ts"
      via: "표 위 한 줄 자리(현재 복원 → 이전 차수 복원 → 잠김)의 이전 차수 줄이 `findOtherRevisionDrafts`로 같은 프로젝트의 다른 차수 보관본을 찾는다"
      pattern: "findOtherRevisionDrafts"
    - from: "app/(app)/projects/[id]/revision-section.tsx"
      to: "ui/table/Table.tsx"
      via: "편집 셀 0개라 자동으로 캡션 읽기 표 — 모드 prop을 만들지 않는다"
      pattern: "caption"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/quotes/revisions.ts"
      via: "섹션 행 · 상태 낱말 · 표시 번호 · 차수 id↔순번이 서버 함수에서 온다"
      pattern: "listRevisionSummaries"
---

<objective>
**As a** 기획 PM, **I want to** 상세에서 「복사해 새 차수」로 네고 견적을 새 차수로 만들고, 고객이 승인한 날을 표시하거나 조건대로 거두며, 차수 섹션 아래에서 지난 차수를 읽기 전용으로 펼쳐 보고, 동료가 새 차수를 만들어 밀려난 내 미저장 편집을 복사해 되찾고 싶다, **so that** 견적의 변천이 화면에서 한눈에 읽히고 「현재 차수가 승인됐는가」가 부제 한 줄로 보이며, 어떤 차수 변화에도 입력이 조용히 사라지지 않는다.

**2026-09-23 체커 1차 분할.** 옛 04-14(20파일)를 서버와 화면으로 나눈 **화면 쪽**이다. 04-14가 차수 domain(새 차수 · 고객 승인 표시와 취소 · 계보 해석 · 차수 요약(차수 id 포함) · 표시 번호 · 이전 차수 잠김 조회와 그 보기 액션 `listRevisionLinesAction`) · 게이트 규칙 넷 · 설정 키를 서버에서 끝냈다. 이 플랜은 그 위에 확인 다이얼로그 셋 · 머리 줄 버튼과 부제 · 차수 섹션 · 이전 차수 읽기 섹션 · 이전 차수 복원 줄을 그린다. UI-SPEC은 고치지 않는다 — 모든 문구가 rev 5 Copywriting에 있다.

Purpose: ROADMAP 기준 3의 「차수마다 고객 승인 표시」가 화면에서 보이고, 04-16(계약 금액 = 승인된 현재 차수 합계)의 E2E가 이 플랜의 승인 표시 다이얼로그로 승인을 켠다.

Output: `revision-dialogs.tsx`(`ui/confirm-dialog` 셋) · `revision-section.tsx` · `previous-revision.tsx`(읽기 섹션 · 복원 줄 · 읽기 열) · 머리 줄 버튼·부제 · 승인 차수 잠금 표시 · `findOtherRevisionDrafts`(+ 단위) · E2E 1.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` T3·T4·부록 B).** (1) **B-02:** 새 차수 다이얼로그가 보던 차수 id를 싣고 대기 중 1차가 `…`다 — 더블클릭 E2E. (2) **B-03:** 미저장 편집이 있으면 다이얼로그 1차를 막는다(보관 키가 바뀌어 입력이 사라지는 길). (3) **사용자 D7 · OV-1:** 승인 차수의 견적 칸 잠금을 표에 그린다(판정은 04-40). (4) **B-25·B-30:** 승인일 글자·버튼 렌더 조건은 서버 결과 그대로. 옛 계획이 Copywriting에 더하던 네 행은 UI-SPEC rev 5에 이미 있다.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 「ENG-D*」).** (1) **ENG-D9(Codex #2):** 승인 다이얼로그가 현재 차수의 합계·내용 토큰을 싣고, 미저장 편집이 있으면 새 차수와 같은 막힘 — 옛 기준값 거부는 막힘 자리에. (2) **ENG-D4:** 빈 차수 승인 거부 문구를 막힘 자리에 그대로. (3) **ENG-D7:** 승인 차수 잠김 칸에 소분류(판정은 04-40 — 화면은 DTO 그대로). 옛 계획의 Copywriting 두 행도 rev 5에 있다.

**2026-09-23 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md` — 「DR-*」, 전역 원칙 P0).** (1) **DR-13:** 「차수 열기」를 `?revision=n` 원장 바꿔치기에서 **차수 섹션 아래 잠긴 읽기 섹션을 펼치는 버튼**으로 바꿨다 — 현재 원장 · 1차 · dirty · URL은 그대로, 줄은 처음 열 때만 받고(04-14 보기 액션), 오류 줄 `{n}차 불러오지 못함 · 다시 시도` — 30줄 쪽 나눔과 쪽 전환 뒤 제목 포커스는 04-19의 `ui/table` `pagination`이 붙인다(W2 — 쪽 나눔 한 구현). 옛 문구 `{n}차 · 읽기 전용`과 원장 잠김 모드를 지웠다(Task 3). (2) **DR-4:** 동료가 새 차수를 만들어 밀려난 보관본을 표 위 `{n}차 저장 안 한 편집 N칸 · 복사 / 버림`으로 되찾는다 — 복사는 `Ctrl+C` 범위 복사와 같은 TSV + 앱 형식이고 그 직렬화(읽기 열 `copyText` · `quoteLineClipboardMeta`)의 주인이 이 플랜이다 — 뒤 웨이브 04-19의 격자 복사가 가져다 쓴다(W1), 자동 병합 없음(Task 4, `ui/table/use-dirty-storage.ts`에 다른 차수 찾기). 옛 「옛 키의 복원이 뜨지 않음 — SUMMARY에 사실만」 이월을 닫는다. (3) **DR-12 · DR-20:** 세 확인은 04-46의 `ui/confirm-dialog`(계약 2), 승인일은 그 컴포넌트의 확인 근거 칸(`evidenceField`) — 04-46은 이 플랜의 조상(04-14 → … → 04-20 → 04-29 → 04-46)이라 `depends_on`을 더하지 않는다. (4) **DR-21 · P0 · F1 · F2:** 토스트 `새 차수 만들기 · 상세 견적 {n+1}차` · 막힘 `저장 안 한 편집 {N}칸 · 먼저 일괄 저장` · `다른 사람이 먼저 새 차수를 만듦 · 새로 고침` · `견적이 바뀜 · 새로 고침` · `복사할 견적 줄 없음 · 첫 줄 만들기` · 승인 다이얼로그 본문 문장 삭제(라벨 `승인일`이 말한다). (5) **DR-1:** 옛 Task 3의 UI-SPEC 직접 편집(Copywriting 여덟 행)을 지웠다 — rev 5가 담았다. (6) **DR-14:** 독립 DOM 감사 폭 1280 · 1024 · 375. (7) **DR-31:** 표 위 한 줄 순서 현재 복원 → 이전 차수 복원 → 잠김.

**태스크 수(넷)에 대해.** 권장 2~3을 넘지만 새 플랜으로 나누면 같은 파일(`quote-table.tsx`·`[id]/page.tsx`)을 고치는 뒤 웨이브가 전부 밀린다(사용자 결정: 순차 웨이브 유지 · 연쇄 이동 금지). 그래서 한 플랜에 두고 각 태스크를 한 관심사(새 차수 · 승인 · 지난 차수 보기 · 지난 차수 편집 되찾기)로 자르며, 이전 차수 줄을 받는 서버 액션은 04-14로 옮겨 이 플랜의 서버 몫을 없앴다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md
@CLAUDE.md

# 범위 Read(UI-SPEC rev 5 — 줄 번호는 편집 시점에 `grep -n "### S5. 차수 섹션" .planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 등으로 다시 찾는다):

# 04-UI-SPEC.md 「적용 규칙 S3」 1118–1154행(머리 줄 · 고객 승인 버튼 · 섹션 순서 · 미저장 편집 막힘) · 「적용 규칙 S5」 1232–1280행 · 「S16. 확인 모달」 1493–1526행 · 「S18. 미저장 편집 복원 줄」 1545–1558행

# 04-UI-SPEC.md 「UI Considerations」 S3 490–499행 · S4 512행(partial ③ 승인 차수) · S5 522–531행 · S16 617–626행 · S18 638–647행

# 04-UI-SPEC.md Copywriting 355행 · 360행 · 364행 · 372행 · 390–394행 · 415–417행 · 422–424행

# 04-UI-SPEC.md 「SYSTEM.md 개정 ⑫」 919–921행((마) 이전 차수 보관본) · 「⑭」 937행(확인 근거 한 칸)

# docs/designs/plant8-erp-phase4-design-review-260923.md 결정 원장 4·12·13·20·21·31행

# .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md U-2 행(사용자 확정 값)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(차수·고객 승인의 화면)** · **PROJ-05(차수 전체 복사의 화면)** · **UX-04(차수 변화에도 입력 유실 없음 — DR-4)** 다. 수용 기준은 D-53~D-56 · D-68 · D-70과 UI-SPEC rev 5 S3·S5·S16·S18에서 끌어왔다(서버 쪽 truths와 금지 항목은 04-14에). `## UI Considerations`의 S5(이전 차수 읽기 섹션 포함) · S18 · 이 플랜이 쓰는 S16 사용처의 행은 truths로 옮겼다 — explicit은 문장으로, backstop은 `verification: backstop` 마커로(S5 error · S5 overflow + 1024 · S5 읽기 섹션 zero-one-many · S18 overflow · S16 overflow·zero-one-many).

**문구:** 이 플랜이 단언하는 화면 글자는 전부 UI-SPEC rev 5 Copywriting에 있다 — 옛 계획이 Copywriting에 더하던 여덟 행은 rev 5가 담았고(일부는 P0로 줄었다), 이 플랜은 UI-SPEC을 고치지 않는다. 서버가 돌려주는 rev 5 밖 방어 문구(담당 아님 · 연결 문서 · 미래 승인일 · 정산 새 차수)는 04-14가 명사형으로 정했고 화면은 막힘 자리에 그대로 쓴다.

**미저장 편집과 새 차수(CEO 리뷰 B-03 → 디자인 리뷰 DR-4):** 보관 키는 프로젝트 + 차수(04-04 설계) 그대로다. 이 탭의 새 차수는 다이얼로그 1차 막힘으로 막는다(B-03). 다른 사람이 만든 새 차수로 이 탭의 저장이 거부되면(04-40 — `다른 사람이 새 차수를 만듦 · 새로 고침`) 편집은 옛 키에 남는다 — 새로 고치면 **이전 차수 복원 줄**이 그 보관본을 찾아 복사·버림을 준다(DR-4, Task 4). 자동으로 현재 차수에 합치지 않는다(줄 id가 다르고, 새 차수에서 바뀐 줄 위에 덮으면 값이 틀린다).

**계약 가정(그룹 A · C — 공유 브리프의 교차 그룹 계약):** 계약 1(`Button` `aria-disabled`·`reasonTone`)과 계약 2(`ui/confirm-dialog`)는 04-46, 계약 5(`ui/pagination`)와 `lib/paging`은 04-29 — 모두 이 플랜의 조상이다(`ui/pagination`은 이 플랜이 쓰지 않는다 — 읽기 섹션 쪽 나눔은 04-19). 미저장 편집 막힘 글자를 만드는 함수는 그룹 A가 DR-6(「상태 바꾸기」 비활성)에 만든 것을 쓴다(04-21의 `unsavedEditsReason` — 04-21 SUMMARY에서 Grep `먼저 일괄 저장`). 04-19(뒤 웨이브)가 표의 범위 복사(`Ctrl+C` · 앱 형식)와 표 쪽 나누기를 만든다. 이 플랜이 앞서므로 **견적 줄 복사 형식(TSV 열 글자 + 앱 형식 JSON)의 주인은 이 플랜**이다 — `quoteLineReadColumns`의 열마다 `copyText`, 줄마다 `{ currency }`를 싣는 `quoteLineClipboardMeta`, 둘을 묶는 `quoteLineClipboard`를 `previous-revision.tsx`에 두고, 04-19의 격자 복사가 `copyText`·`quoteLineClipboardMeta`를 가져다 쓴다(W1 — 직렬화 한 곳, UI-SPEC rev 5 적용 규칙 S18 「`Ctrl+C` 범위 복사와 같은 형식(D-70)」). 읽기 섹션은 자르지 않고 전체 줄을 표에 넘긴다 — 쪽 나눔은 04-19가 `pagination`으로 붙인다(W2 — 두 구현을 두지 않는다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 담당 PM이 「복사해 새 차수」로 2차를 만든다(머리 줄 버튼 → `ui/confirm-dialog`(보던 차수 id · 대기 중 `…` · 미저장 편집 막힘) → 액션 → 토스트 · 부제 · 새 현재 차수 표 → E2E)</name>
  <files>app/(app)/projects/[id]/revision-dialogs.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `domain/quotes/revisions.ts`(04-14 — `createRevisionFromCurrent`·`quoteDisplayNumber`·`listRevisionSummaries`) · `app/(app)/projects/actions.ts`의 `createRevisionAction`(04-14 — `projectId`·`fromRevisionId` 필수)
    - `domain/quotes/edit-scope.ts`의 `structuralEditability.canCreateRevision`(04-14)
    - `ui/confirm-dialog/ConfirmDialog.tsx`(04-46 — 계약 2: `open`·`onClose`·`title`·`subtitle`·`resultLines`·`evidenceField`·`primary { label, shortcut, onConfirm, pending, disabledReason }`·`secondaryLabel`, 포커스·`Esc`·트리거 복귀) · `ui/button/Button.tsx`(04-46 — 계약 1 `aria-disabled` + `aria-describedby`)
    - `app/(app)/projects/[id]/status-change.tsx`(04-21·04-44 — 04-21이 만들고 04-44가 「기간 적기」·「기간 바꾸기」 링크를 더했다 · `ui/confirm-dialog` 사용 선례) · `app/(app)/projects/[id]/unsaved-edits.ts`(04-21 — DR-6 미저장 편집 막힘 글자 `저장 안 한 편집 {N}칸 · 먼저 일괄 저장`을 만드는 `unsavedEditsReason` — Grep `먼저 일괄 저장`)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) Copywriting 390–392행(막힘 — 미저장 편집 · 새 차수(빈 차수) · 새 차수(동시·오래된 화면)) · 415행(SUCCESS — 새 차수 토스트) · 423행(Destructive — 새 차수) · 「적용 규칙 S5」 1274–1276행 · 「S16」 1493–1526행 — `grep -n "Destructive — 새 차수"`로 줄 확인
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 머리 줄 버튼 자리·`dirtyCount`(1차 「일괄 저장 N」의 N)·`useDirtyStorage`(보관 키 = 프로젝트+차수)를 찾아 범위만 Read
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-02·B-03 절(Grep `### B-02`·`### B-03`)
  </read_first>
  <action>
① `revision-dialogs.tsx`의 `NewRevisionDialog`: 머리 줄 2차 「복사해 새 차수」(page가 `canCreateRevision`일 때만 렌더) → 04-46의 `ConfirmDialog`에 Copywriting `Destructive — 새 차수` 그대로를 채운다(제목 `복사해 새 차수` · 부제 `상세 견적 {n}차 · {k}줄` · `resultLines` 첫 줄 `{n}차 {k}줄을 복사해 {n+1}차 · 되돌리기 없음`, 조정 줄이 있으면 둘째 줄 `조정 {m}줄은 {n+1}차에도 그대로` · `primary.label` `새 차수 만들기` · 2차는 컴포넌트 기본 `취소 Esc`) — `{k}`는 조정 줄 제외. 자체 모달 마크업을 만들지 않는다(DR-12). 1차 `onConfirm`은 `createRevisionAction({ projectId, fromRevisionId })` — `fromRevisionId`는 page가 넘긴 **지금 보고 있는 현재 차수 id**다(CEO 리뷰 B-02). 액션이 진행 중이면 `primary.pending`으로 1차가 `…`이고 두 번째 누름이 요청을 만들지 않는다(next-safe-action의 진행 상태). 서버 거부 문자열은 `primary.disabledReason`으로 1차 왼쪽 막힘 자리에 그대로 둔다(다이얼로그 유지 · 토스트 없음 — 계약 2의 막힘 자리는 하나다). 성공은 다이얼로그를 닫고 토스트 `새 차수 만들기 · 상세 견적 {n+1}차`(DR-21 — 누른 1차 라벨) + 새로 고침으로 새 현재 차수 표.

② **미저장 편집 막힘(CEO 리뷰 B-03 · DR-6과 같은 함수).** `QuoteLedger`의 `dirtyCount` ≥ 1이면 `primary.disabledReason`에 그룹 A가 「상태 바꾸기」에 쓴 미저장 편집 막힘 함수의 결과(`저장 안 한 편집 {N}칸 · 먼저 일괄 저장` — Copywriting `막힘 — 미저장 편집`)를 넘긴다 — 1차는 `aria-disabled`라 열릴 때 포커스를 받고 이유가 읽힌다(계약 1·2). 저장해 N이 0이 되면 켜진다. 같은 글자를 만드는 두 번째 함수를 만들지 않는다(그 함수가 없으면 `status-change.tsx` 쪽 문자열을 한 함수로 꺼내 둘이 쓰게 하고 SUMMARY 편차로 적는다 — ENG-D11). `quote-table.tsx`의 머리 줄 버튼 자리는 컴포넌트가 아니라 `{ dirtyCount }`를 받는 렌더 함수 prop 하나로 더한다 — 다이얼로그가 표의 dirty 상태를 읽을 다른 경로를 만들지 않는다.

③ `[id]/page.tsx`가 `canCreateRevision`·현재 차수 id·현재 차수 순번·복사 대상 줄 수·조정 줄 수를 넘긴다(04-14 요약 행에서 — 화면이 줄을 세지 않는다).

④ **E2E(트레이서 증명).** `test/e2e/quote-revisions.spec.ts` 첫 케이스: 담당 PM이 줄 둘인 수주중 프로젝트에서 「복사해 새 차수」 → 다이얼로그 제목 `복사해 새 차수` · 부제 `상세 견적 1차 · 2줄` · 결과 줄 `1차 2줄을 복사해 2차 · 되돌리기 없음` → 1차를 **두 번 빠르게** 누른다(`dblclick`) → 토스트 `새 차수 만들기 · 상세 견적 2차` 한 번 · 부제 `상세 견적 2차` · 표에 줄 둘 · DB의 차수가 정확히 둘(B-02). 둘째 케이스: 셀 하나를 고친 채 다이얼로그를 열면 포커스가 1차에 있고 1차가 `aria-disabled="true"`이며 `저장 안 한 편집 1칸 · 먼저 일괄 저장`이 1차의 `aria-describedby` 대상이다 → `Esc` → 포커스가 「복사해 새 차수」로 돌아온다 → Ctrl+S → 다시 열면 1차가 켜져 있다(B-03 · DR-6). 셋째: 준비 SQL로 견적 줄 0개(조정 줄 하나)인 현재 차수에서 1차 → `복사할 견적 줄 없음 · 첫 줄 만들기`, 다이얼로그 유지 · 토스트 없음. 같은 스펙에서 정산 프로젝트(준비에서 DB로)에는 「복사해 새 차수」가 DOM에 없다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 새 차수 다이얼로그가 토스트·부제·표로 이어지지 않았거나, 더블클릭이 차수를 둘 만들었거나, 미저장 편집이 있는데 1차가 켜져 있거나 이유가 `aria-describedby`로 이어지지 않았거나, 제목·부제·결과 줄·토스트 글자가 rev 5와 다르거나, 빈 차수 거부가 막힘 자리에 없거나, 정산에서 버튼이 보이거나 기존 표 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 「복사해 새 차수」 렌더 조건이 서버의 `canCreateRevision` 하나다(화면이 상태 문자열을 보지 않는다)
    - `revision-dialogs.tsx`가 04-46의 `ConfirmDialog`를 import하고 자체 모달 마크업이 없다(코드 리뷰, DR-12)
    - 다이얼로그가 `fromRevisionId`를 싣고 진행 중 1차가 `…`다 — E2E 더블클릭 뒤 차수가 정확히 하나 늘었다(B-02)
    - `dirtyCount` ≥ 1이면 1차 `aria-disabled` + `저장 안 한 편집 {N}칸 · 먼저 일괄 저장`(DR-6과 같은 함수), 저장 뒤 켜진다(E2E, B-03)
    - 제목·부제·결과 줄·토스트가 rev 5 Copywriting 글자이고 `{k}`가 조정 줄을 세지 않으며, 빈 차수 거부가 막힘 자리에 그대로 나온다(E2E)
    - E2E가 정산의 버튼 부재를 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM이 상세에서 새 차수를 만드는 한 경로가 버튼 · 공용 확인 다이얼로그 · 액션 · 토스트 · 부제 · E2E까지 끝났고, 더블클릭과 미저장 편집이 되돌릴 수 없는 차수나 입력 손실을 만들지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 고객 승인 표시·취소(`ui/confirm-dialog` — 승인일 = 확인 근거 칸(DR-20) · 기준값 싣기 · 미저장 편집 막힘 — ENG-D9) + 부제의 승인일·표시 번호 + 승인 차수 견적 칸 잠금의 표시(사용자 D7 · ENG-D7)</name>
  <files>app/(app)/projects/[id]/revision-dialogs.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) Copywriting 367행(Error — 셀(형식) 날짜) · 372행(Error — 셀(잠김) — 승인 차수) · 393–394행(막힘 — 고객 승인(빈 차수 · 견적 바뀜)) · 422행(Destructive — 고객 승인 표시 취소) · 424행(확인 — 고객 승인 표시) · 「적용 규칙 S3」 1147–1149행(고객 승인 버튼 · 상태별) · 「적용 규칙 S4」 1189행(승인 차수 셀 단계) · 「적용 규칙 S5」 1277–1280행 · 「S16」 1522–1526행 · 「SYSTEM.md 개정 ⑭」 937행(확인 근거 한 칸) — `grep -n "확인 — 고객 승인 표시"`로 줄 확인
    - `domain/quotes/revisions.ts`의 `setCustomerApproval`·`listRevisionSummaries`(04-14·04-40 — 현재 차수 행의 합계·`contentToken`) · `app/(app)/projects/actions.ts`의 `setCustomerApprovalAction`(04-14 — `approval` = `{ approvedOn, seenTotalKrw, contentToken }` 또는 null)
    - `docs/designs/plant8-erp-phase4-eng-review-260923.md` R3·R6·R8(ENG-D4·D7·D9 — Grep `### R8`)
    - `domain/quotes/edit-scope.ts`의 `lineCellEditability`·`tableLockLine`의 `approvedSeq` 축과 새 줄 견적 칸 잠금 기본값(04-40)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `tableLockLine`·새 줄 기본 셀 단계 계산 자리를 찾아 범위만 Read
    - `ui/confirm-dialog/ConfirmDialog.tsx`(04-46 — `evidenceField` 슬롯과 첫 포커스 · 막힘 표시 방식은 04-46 SUMMARY) · `ui/form/Form.tsx`(`Form.Field` · `Form.Error`)
    - `lib/kst-date.ts`(04-29 — `kstToday`) · `lib/format-number.ts`(부제 합계)
  </read_first>
  <behavior>
    - 담당 PM + 진행 + 현재 차수 미승인 → 「고객 승인 표시」 · 승인됐고 연결 문서 없음 → 「승인 표시 취소」 · 담당이 아닌 사람 → 둘 다 없음 · 완료 → 둘 다 없음(승인일은 부제에 보임) · 정산 → 담당 PM에게 렌더(CEO-D19)
    - 「고객 승인 표시」를 열면 포커스가 `승인일` 칸이고 기본값이 오늘(KST) · 칸을 비우면 1차가 막히고 칸 아래 `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` · 내일 날짜로 1차 → 서버 거부 `승인일이 오늘보다 늦음 · 날짜를 고쳐 주세요`가 막힘 자리에, 다이얼로그 유지 · 날짜를 오늘로 고치면 그 거부 글자가 지워지고 1차가 켜진다
    - 셀 하나를 고친 채(N = 1) 「고객 승인 표시」를 열면 1차가 `aria-disabled`이고 `저장 안 한 편집 1칸 · 먼저 일괄 저장`이 보인다 → 다이얼로그를 닫고 Ctrl+S → 다시 열면 1차가 켜져 있다(ENG-D9)
    - 상세를 연 뒤 테스트가 다른 사용자로 같은 차수의 수량을 서버에서 바꿔 저장(도메인 호출) → 새로 고치지 않은 화면에서 승인 표시 → 1차 왼쪽 `견적이 바뀜 · 새로 고침`, 다이얼로그 유지, 부제에 승인일 없음 → 새로 고친 뒤 다시 승인 → 통과(ENG-D9 — 옛 기준값)
    - 견적 줄 0개인 새 프로젝트에서 승인 표시 → 1차 왼쪽 `승인할 견적 줄이 없음 · 첫 줄 만들기`(ENG-D4)
    - 승인이 통과하면 토스트가 없고 다이얼로그가 닫히며, 트리거 「고객 승인 표시」가 「승인 표시 취소」로 바뀌었으므로 포커스가 머리 줄 제목으로 간다 · 부제 `고객 승인 {오늘} {이름}` · 차수 섹션(Task 3 뒤) 상태 `승인`
    - 승인 뒤 표: 수량 셀에서 Enter → 편집에 들어가지 않고 짧은 이유 한 줄 `{n}차 고객 승인됨 · 고치려면 새 차수` · 소분류 셀도 같다(ENG-D7) · 표 위 한 줄도 같은 글자 · 실행가 셀은 편집·저장된다 · 「줄 추가」로 만든 새 줄의 수량·단가가 잠겨 있다 · 승인 표시 취소 뒤 수량·소분류가 다시 편집된다
    - 「승인 표시 취소」 다이얼로그: 제목 `고객 승인 표시 취소` · 부제 `상세 견적 {n}차 · 승인일 {날짜}` · 결과 줄 `승인일 지워짐 · 이 차수의 지출결의가 다시 막힘` · 1차 `승인 표시 취소` · 2차 `닫기 Esc`
  </behavior>
  <action>
① E2E 케이스를 먼저 쓴다(RED): `behavior` 전부 · 완료 프로젝트에는 두 버튼이 없고 승인일이 보인다 · 팀장(담당 아님)에게는 두 버튼이 없다. 옛 기준값 케이스의 「다른 사용자의 저장」은 테스트 준비 코드가 도메인 `saveQuoteLines`를 직접 불러 만든다(두 번째 브라우저를 띄우지 않는다).

② `revision-dialogs.tsx`에 `ApprovalDialog`와 `ApprovalCancelDialog` — 둘 다 04-46의 `ConfirmDialog`(DR-12). `ApprovalDialog`: 제목 `고객 승인 표시` · 부제 `상세 견적 {n}차 · {합계}`(합계는 요약 행 값을 `lib/format-number.ts`로 — 화면이 다시 더하지 않는다) · **`evidenceField`에 `Form.Field` 날짜 칸 하나**(라벨 `승인일`, 280, 기본값 `kstToday()` — 개정 ⑭ · DR-20, 모달 안 폼은 이 한 칸뿐) · `primary.label` `고객 승인 표시` · 결과 줄 없음. 칸이 비거나 `YYYY-MM-DD`가 아니면 1차를 막고 이유는 칸 아래 `Form.Error` `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` 한 자리다(같은 사실을 두 자리에 쓰지 않는다 — 04-46 컴포넌트가 막힘에 이유 id를 요구하면 그 칸 오류의 id를 잇는 방식을 04-46 SUMMARY에서 확인해 쓴다). **기준값(ENG-D9):** 1차는 page가 넘긴 현재 차수 요약 행의 `seenTotalKrw`(요약 DTO의 견적 합계 그대로)와 `contentToken`을 `setCustomerApprovalAction({ revisionId, approval: { approvedOn, seenTotalKrw, contentToken } })`에 싣는다. **막힘 순서:** 미저장 편집(Task 1 ②의 같은 함수 — `dirtyCount` ≥ 1) → 날짜 형식 → 서버 거부 문자열(빈 차수 · 옛 기준값 · 미래 날짜 등 — 그대로 `primary.disabledReason`, 승인일을 바꾸면 거부 글자를 지운다). 성공은 토스트 없이 닫고(결과가 부제·차수 섹션에 보인다 — S5) 새로 고침. `ApprovalCancelDialog`: Copywriting `Destructive — 고객 승인 표시 취소` 그대로(제목 · 부제 `상세 견적 {n}차 · 승인일 {날짜}` · 결과 줄 · `primary.label` `승인 표시 취소` — 2차 `닫기 Esc`는 컴포넌트가 1차의 「취소」에서 파생) · `approval: null`, 기준값·미저장 막힘 없음. 두 트리거 버튼도 Task 1 ②의 머리 줄 렌더 함수 prop(`{ dirtyCount }`)으로 그린다(두 번째 막힘 구현을 만들지 않는다). 닫힌 뒤 포커스 복귀·트리거가 사라진 경우의 머리 줄 제목 포커스는 04-46 컴포넌트 계약 그대로다 — 머리 줄 제목이 포커스를 받을 수 있는지(`tabindex="-1"`)만 확인한다.

③ `[id]/page.tsx`: 서버가 계산한 「승인 버튼을 볼 사람」(담당 PM + `projects` 쓰기 — CEO 리뷰 B-30)이고 상태가 완료가 아니며 **현재 차수 요약 행에 합계 키가 있으면**(ENG-D9) 부제 옆 3차 — 미승인이면 「고객 승인 표시」, 승인됐고 연결 문서가 없으면 「승인 표시 취소」(있으면 버튼 없음 — 판정 입력은 04-14의 서버 결과). 부제에 `고객 승인 {날짜} {승인자 이름}`(날짜 글자는 서버가 `kstDateOf`로 만든 문자열 그대로 — B-25)과 04-14의 `quoteDisplayNumber` 결과(GREEN).

④ **승인 차수 잠금의 표시(사용자 D7 · OV-1 · ENG-D7).** `[id]/page.tsx`가 현재 차수의 승인 순번(`approvedSeq`)을 `tableLockLine`과 표에 넘기고, `quote-table.tsx`는 새 줄 기본 셀 단계를 만드는 자리(04-12·04-30의 클라이언트 기본값 — 서버와 같은 `lineCellEditability`)에 `approvedSeq`를 실어 새 줄의 수량·단가를 잠근 채 만든다(새 줄의 소분류는 04-40 판정대로 편집). 기존 줄의 잠긴 칸(수량·단가·상태·소분류)은 서버 DTO `cellEditability`(04-40)에서 오므로 화면이 승인 여부로 칸을 추론하는 분기를 만들지 않는다. 잠긴 칸의 편집 시도 이유는 04-04·04-12의 짧은 이유 줄 경로(= 표 위 한 줄과 같은 문자열 — 개정 ④ · DR-35)다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts test/e2e/quote-edit-scope.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 승인 표시·취소가 부제로 이어지지 않거나, 완료·비담당에게 버튼이 보이거나, 승인일 칸이 첫 포커스가 아니거나 형식 오류·미래 날짜가 막힘이 아니거나, 승인 차수에서 수량·소분류가 편집되거나 실행가가 막히거나, 미저장 편집 중 승인 1차가 켜져 있거나, 옛 기준값·빈 차수 승인이 통과했거나, 승인 성공에 토스트가 떴거나 포커스가 머리 줄 제목으로 가지 않았거나, 기존 셀 단계 표시가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 완료에서 두 버튼이 DOM에 없고 승인일은 부제에 보인다(E2E) · 담당이 아닌 사람에게 두 버튼이 없다(E2E)
    - 승인 다이얼로그가 `ConfirmDialog`의 `evidenceField`에 날짜 칸 하나(`승인일`, 기본 오늘 KST)를 두고 본문 문장이 없으며, 열리면 그 칸이 포커스다 · 칸이 비면 1차가 막히고 칸 아래 형식 오류가 한 자리에만 있다(E2E, DR-20)
    - 승인 표시 취소 다이얼로그의 제목·부제·결과 줄이 rev 5이고 2차 라벨이 `닫기 Esc`다
    - 부제의 표시 번호·승인일이 서버 파생값이고 화면에 번호 조립·시간대 계산 코드가 없다
    - 승인 차수에서 수량·소분류 편집이 짧은 이유 한 줄과 함께 막히고 실행가는 저장되며, 새 줄의 수량·단가가 잠긴 채 생긴다 · 승인 취소 뒤 풀린다(E2E, ENG-D7)
    - 승인 다이얼로그가 현재 차수 요약 행의 합계·`contentToken`을 싣고(화면 합산 없음), 미저장 편집이 있으면 1차가 `aria-disabled` + 이유이며 저장 뒤 켜진다 · 옛 기준값 승인이 `견적이 바뀜 · 새로 고침`으로 막힘 자리에 나오고 부제에 승인일이 생기지 않는다(E2E, ENG-D9)
    - 미저장 편집 막힘이 새 차수 다이얼로그 · 「상태 바꾸기」와 같은 한 함수다(두 번째 막힘 구현 없음)
    - 빈 차수 승인 거부 문구가 막힘 자리에 그대로 나오고, 승인 성공에 토스트가 없으며 포커스가 머리 줄 제목으로 간다(E2E)
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>담당 PM이 현재 차수의 고객 승인을 본 합계 그대로·확인 근거 칸의 날짜와 함께 켜고 조건대로 끄며, 미저장 편집·옛 화면·빈 차수에서는 켜지지 않고, 부제가 승인일과 표시 번호를 보이며, 승인 차수의 견적 칸이 잠긴 모양으로 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 차수 섹션(S5) + 이전 차수 읽기 섹션(DR-13 — 차수 섹션 아래 펼침 · 처음 열 때만 받음 · 오류 줄 · 제목 포커스 · 원장 무영향) + 견적 줄 읽기 열(복사 글자 `copyText`의 주인 — W1)</name>
  <files>app/(app)/projects/[id]/revision-section.tsx, app/(app)/projects/[id]/previous-revision.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「적용 규칙 S5. 차수 섹션」 1232–1280행(이력 목록을 쓰지 않는 네 근거 · 열 · P1/P2 · 상태 낱말 · 「차수 열기」 = 이전 차수 읽기 섹션) · 「UI Considerations S5」 522–531행(여덟 상태 + (rev 5) 이전 차수 읽기 섹션 전 상태) · Copywriting 343행(`다시 시도` = 2차) · 355행(Empty — 이전 차수 읽기 섹션) · 360행(Error — 이전 차수 읽기 섹션) · 「적용 규칙 S3」 1145행(섹션 순서 ① → ② → ③ → ④) · 「적용 규칙 S12」 1413–1429행 · 「UI Considerations S12」 583행 — `grep -n "### S5. 차수 섹션"`로 줄 확인
    - `docs/designs/plant8-erp-phase4-design-review-260923.md` 결정 원장 13행(DR-13)
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` U-2 행 — 사용자 확정 값(최신+승인 = `승인`만, 미승인 이전 = 빈 칸)
    - `ui/table/Table.tsx`·`ui/table/types.ts` — 읽기 형태 자동 판정(편집 셀 0) · 캡션 · 열 우선순위 · 그룹 · `footer`
    - `ui/history-list/HistoryList.tsx` — **쓰지 않는다는 판단을 확인만** 한다
    - `lib/format-number.ts`(셀 글자와 `copyText`의 쉼표 서식) · `ui/table/types.ts`의 `TableColumn`(이 플랜은 고치지 않는다 — `copyText`는 반환 타입의 교집합으로 싣는다)
    - `domain/quotes/revisions.ts`의 `listRevisionSummaries`(04-14 — 차수 id 포함) · `app/(app)/projects/actions.ts`의 `listRevisionLinesAction`(04-14 — 이전 차수 잠김 조회 보기 액션)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 열 정의(머리글 · 우선순위 · 셀 글자 · 외화 2행)·그룹 함수(04-23 — `lineKind` 먼저)·합계 행·`계산 불가` 배지·표 LOADING 뼈대 사용처를 찾아 범위만 Read
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - 차수 셋(1차 승인 · 2차 미승인 · 3차 미승인 최신) → 섹션 행 순서 3차·2차·1차, 상태 `현재`·빈 칸·`승인`, 3차 행 동작 칸 빈 칸, 2차·1차에 3차 버튼 「차수 열기」(`aria-expanded="false"`) · 차수가 하나뿐인 프로젝트에는 「차수 열기」가 없다
    - 「차수 열기」(1차) → 그 버튼 `aria-expanded="true"` · 라벨 `차수 닫기` · 차수 섹션 바로 아래 섹션 제목 `상세 견적 1차`에 포커스 · 캡션 `상세 견적 1차 견적 줄`의 `<table>`(흰 머리글 · `role="grid"` 아님) · 편집 입력·저장 버튼·구조 컨트롤·복원 줄·힌트 줄·`조정` 그룹 없음 · 머리글 글자 순서가 현재 견적 줄 표와 같다 → 「차수 열기」(2차) → 섹션이 2차로 바뀐다(한 번에 하나) → 「차수 닫기」 → 섹션이 사라지고 포커스가 그 버튼
    - 현재 표 셀 하나를 고친 채(N = 1) 「차수 열기」 → 「차수 닫기」 → 1차 「일괄 저장 1」 그대로 · URL 검색 파라미터 무변경 · 그 셀 dirty 모양 그대로(DR-13)
    - 같은 차수를 닫았다 다시 열면 서버 액션 요청이 다시 나가지 않는다(처음 열 때만 — 요청 수 단언)
    - 첫 요청을 끊으면(Playwright `page.route`로 그 서버 액션 요청 한 번 중단) 섹션 자리에 `1차 불러오지 못함 · 다시 시도`, 현재 원장·N 무영향 → 「다시 시도」 → 읽기 표
    - 31줄 이전 차수(준비 SQL) → 합계 행 `합계 (공급가액 · 31줄)` · 줄 번호 1~31이 끊기지 않는다(30줄 쪽 나눔은 04-19) · 0줄 이전 차수(준비 SQL) → `이 차수에 견적 줄이 없습니다`(버튼 없음)
  </behavior>
  <action>
① `app/(app)/projects/[id]/revision-section.tsx`: `ui/table`을 쓴다 — 편집 셀이 없으니 자동으로 캡션 있는 읽기 표(모드 prop을 만들지 않는다). 섹션 제목 `차수`(2px `--line-strong`으로 시작하는 §6-2 섹션), 열 여섯(차수 `{n}차` · 생성일 · 줄 수 · 견적 합계 · 상태(§7-5 `text`: `현재` accent · `승인` success) · 동작), 최신이 위, 폰 P1 = 차수·견적 합계·동작, P2 접힌 줄 = 상태·생성일·줄 수(빈 값 자리 건너뜀). 이전 차수 행에만 3차 **`<button type="button">` 「차수 열기」**(`aria-expanded` · `aria-controls` = 읽기 섹션 id, 열리면 라벨 `차수 닫기` — DR-13. 이동이 아니므로 링크가 아니다). 열린 차수 순번(`openSeq`) 상태는 이 컴포넌트가 갖고, 자기 표 바로 아래에 `PreviousRevisionSection`을 다음 §6-2 섹션으로 렌더한다(섹션 순서 ③ → ④ — 「적용 규칙 S3」). 현재 행의 동작 칸과 미승인 이전 차수의 상태 칸은 **빈 칸**이다(`—` 금지). 섹션 단독 오류 자리를 만들지 않는다. `ui/history-list`를 쓰지도 고치지도 않는다. 행 데이터는 04-14의 `listRevisionSummaries`.

② **이전 차수 읽기 섹션(DR-13).** `app/(app)/projects/[id]/previous-revision.tsx`에 `PreviousRevisionSection({ projectId, seq, references })`: 제목 `상세 견적 {n}차`(`--fs-sm` 600, 2px `--line-strong`으로 시작, `tabindex="-1"`, 버튼의 `aria-controls`가 가리키는 id) — 열릴 때(순번이 바뀔 때) 제목에 포커스. **데이터:** 그 순번을 처음 열 때만 04-14의 `listRevisionLinesAction({ projectId, revisionSeq })`를 부르고 결과를 순번별로 컴포넌트 안에 둔다(다시 열면 다시 부르지 않는다). 기다리는 동안 섹션 자리에 표 LOADING 뼈대(머리글 + `--surface` 행 3개, 300ms 지연 — 기존 표 LOADING 형태 재사용), 실패하면 섹션 자리 한 줄 `{n}차 불러오지 못함 · 다시 시도`(`--danger`, 「다시 시도」는 2차 버튼 — 그 순번만 다시 부른다). **읽기 표:** 이 파일에 `quoteLineReadColumns(references)`를 두고 내보낸다 — 견적 줄 표와 **같은 열 키·순서·머리글·우선순위·좁은 PC 열 접기(`collapseBelow` — 04-30)·셀 글자·외화 2행**(D-64 열한 열 · P1 항목·실행가·상태 · P2 소분류·거래처 · P3 나머지)의 읽기 렌더만 갖는다. **각 열에 `copyText(row): string`**(그 열의 화면 첫 줄 글자 그대로 — `lib/format-number` 쉼표 서식, 소분류·거래처는 라벨, 상태는 낱말, 외화 줄의 단가는 원화 첫 줄, 2행 없음)을 두고, 입력 행은 값 필드만의 좁은 타입 `QuoteLineCopyRow`(이 파일에서 export — 읽기 섹션 DTO 행 · 보관본을 덮은 행 · 04-19 격자 편집 행이 모두 이 모양으로 들어온다)다. 반환 타입은 `TableColumn`에 `copyText`를 더한 교집합이다(04-19가 `TableColumn.copyText?`를 더하면 그대로 맞는다 — 이 플랜은 `ui/table/types.ts`를 고치지 않는다). 이 `copyText`가 견적 줄 복사 글자의 **유일한 정의**다 — Task 4의 복원 줄 「복사」와 뒤 웨이브 04-19의 `Ctrl+C` 격자 복사가 둘 다 이것을 쓴다(W1). 서버가 셀을 전부 `locked`로 보내므로 `ui/table`이 자동으로 흰 머리글 `<table>` + 캡션 `상세 견적 {n}차 견적 줄`을 그린다. 그룹은 견적 줄 표의 그룹 함수(04-23 — `lineKind` 먼저)를 그대로 쓰고(모듈 지역 함수면 `quote-table.tsx`에서 export만 더한다 — 두 번째 그룹 구현 금지), `조정` 그룹은 서버가 보내지 않아 없다. 줄 번호는 이어 센다, 합계 행 `합계 (공급가액 · {N}줄)`은 전체 줄 기준, 환율 없는 줄은 견적 줄 표와 같은 `계산 불가 · 환율 없음` + `계산 불가 N건` 배지. **쪽:** 이 플랜은 줄을 자르지 않고 그룹 정렬 뒤 표시 순서의 전체 줄을 표에 넘긴다 — 31줄부터의 30줄 페이지 줄과 쪽 전환 뒤 섹션 제목 포커스(S5 (rev 5))는 04-19가 이 읽기 표에 `pagination`(`focusHeadingId` = 제목 id)을 넘겨 붙인다(쪽 나눔 한 구현 — W2). 제목 id는 안정된 값으로 둔다(`aria-controls`가 이미 가리킨다). 0줄이면 `이 차수에 견적 줄이 없습니다`(버튼 없음). **원장 무영향:** `QuoteLedger`에 잠김 보기 모드를 만들지 않고, 검색 파라미터를 읽거나 쓰지 않으며, 1차·dirty·보관본에 닿지 않는다 — 되돌리기 동작이 없다(D-53).

③ `[id]/page.tsx`: 매출 섹션 뒤에 `RevisionSection`(요약 행 · `projectId` · 읽기 열이 쓸 참조 — 소분류·거래처 라벨)을 렌더한다. `quote-table.tsx`는 그룹 함수(와 필요하면 읽기 셀 서식 함수)의 export만 더하고 동작을 바꾸지 않는다.

④ E2E(`behavior` 전부).
  </action>
  <verify>
    <automated>git diff --stat d6b41cf -- ui/history-list</automated>
    <fails_when>출력이 비어 있지 않다 — 이력 목록 컴포넌트를 고쳤다(S5의 네 근거)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 섹션 순서·상태 낱말·빈 칸이 계약과 다르거나, 「차수 열기」가 버튼·`aria-expanded`·라벨 토글·제목 포커스를 지키지 않거나, 읽기 섹션이 원장의 N·dirty·URL을 바꾸거나, 같은 차수를 다시 열 때 요청이 다시 나가거나, 오류 줄·다시 시도·0줄·31줄 합계·이어지는 번호가 계약과 다르거나, 기존 표 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 차수 섹션이 모드 prop 없이 캡션 읽기 표이고 `ui/history-list` diff가 0줄이다
    - 상태 칸·현재 행 동작 칸이 U-2 확정값대로 렌더된다(빈 칸 — `—` 없음)
    - 「차수 열기」가 `aria-expanded`·`aria-controls`가 있는 버튼이고 열리면 `차수 닫기` · 섹션 제목 포커스, 닫히면 버튼 포커스다(E2E, DR-13)
    - 이전 차수 읽기 섹션에 편집·저장·구조 컨트롤·복원 줄·힌트·조정 그룹이 없고, 원장의 N·dirty·URL이 그대로다(E2E) · 이 플랜 코드에 `revision` 검색 파라미터 처리와 원장 잠김 모드가 없다(Grep `searchParams` — `[id]/page.tsx`에 `revision` 키 없음)
    - 줄은 처음 열 때만 받고, 실패하면 `{n}차 불러오지 못함 · 다시 시도`가 섹션 자리에만 나온다 · 0줄 EMPTY · 31줄 합계 행·이어지는 번호(E2E) · `previous-revision.tsx`에 줄을 30개로 자르는 코드가 없다(쪽 나눔은 04-19 — W2)
    - 읽기 표의 열 정의가 `quoteLineReadColumns` 한 곳이고 머리글 순서가 견적 줄 표와 같다(E2E) · 그룹 함수를 두 번 구현하지 않았다 · 각 열에 `copyText`가 있고 `QuoteLineCopyRow`를 export한다(W1)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>차수 섹션이 차수의 변천을 읽기 표로 보이고, 지난 차수를 섹션 아래에 잠긴 표로 펼쳐 보며, 그동안 현재 원장의 편집은 한 칸도 흔들리지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 이전 차수 보관본 복원 줄(DR-4 — 같은 프로젝트의 다른 차수 보관본 찾기 · 「복사」 = 범위 복사와 같은 TSV + 앱 형식 · 「버림」) + 독립 DOM 감사(1280 · 1024 · 375) + 전체 게이트</name>
  <precondition>`app/(app)/projects/[id]/quote-table.tsx`의 주석이 아닌 줄에 `persist(` 호출이 1건 이상 있다(D-68 보관 배선 — 구현 주인 04-22 Task 2 ⑤). 없으면 멈추고 보고한다 — 이 태스크가 구현하지 않는다</precondition>
  <files>ui/table/use-dirty-storage.ts, test/unit/ui/dirty-storage.test.ts, app/(app)/projects/[id]/previous-revision.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `ui/table/use-dirty-storage.ts`(전문 143행 — `DirtyStorageLike` · `dirtyStorageKey` = `quote-ledger:dirty:{scopeId}:{subScopeId}` · `loadDirtyEdits`의 손상 JSON 무시 · `countDirtyEdits` · `clearDirtyEdits` · `useDirtyStorage`) · `test/unit/ui/dirty-storage.test.ts`(전문 — 메모리 저장소 형태)
    - `.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md` — D-68 보관본의 실제 모양(줄 id 키 · 칸 값)과 현재 차수 복원 줄(`저장 안 한 편집 N칸 · 복원 / 버림`)의 배선 자리
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「S18. 미저장 편집 복원 줄」 1545–1558행 · 「UI Considerations S18」 638–647행 · Copywriting 364행(Error — 저장(현재 차수 아님)) · 416–417행(미저장 편집 복원 — 현재 · 이전 차수) · 「적용 규칙 S4」 1209–1212행(앱 형식 붙여넣기 — 계산 열 무시) · 1223행(표 위 한 줄 순서 — DR-31) · 「SYSTEM.md 개정 ⑫」 919–921행((마) 이전 차수 보관본) — `grep -n "### S18"`로 줄 확인
    - `docs/designs/plant8-erp-phase4-design-review-260923.md` 결정 원장 4행(DR-4) · 31행(DR-31)
    - `ui/table/parse-tsv.ts` 80–95행(`toTsv`) — 견적 줄 복사 형식의 주인은 이 플랜이다(W1): 뒤 웨이브 04-19의 격자 `Ctrl+C`가 이 플랜의 `copyText`·`quoteLineClipboardMeta`를 가져다 쓴다(`.planning/phases/04-project-quote-ledger/04-19-PLAN.md` Task 2 ③④ — Grep `quoteLineClipboardMeta`)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 표 위 한 줄 자리(`restoreBanner` · `tableLockLine`)·`useDirtyStorage` 호출·`handlePasteAtCell`을 찾아 범위만 Read · `app/(app)/projects/[id]/previous-revision.tsx`(Task 3 — `quoteLineReadColumns`)
    - `test/e2e/quote-table.spec.ts` 50–60행(`ClipboardEvent('paste')` + `DataTransfer` 붙여넣기 선례)
    - `app/(app)/projects/[id]/project-detail.module.css` — 04-30의 현재 차수 복원 줄 클래스 `.restoreBanner` · `.restoreActions`(Grep — 이전 차수 복원 줄이 그대로 쓴다)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드
  </read_first>
  <behavior>
    - (단위) `findOtherRevisionDrafts(storage, "P", "R2")`: 저장소 키 `quote-ledger:dirty:P:R1`(칸 둘) · `…:P:R2`(현재) · `…:Q:R9`(다른 프로젝트) · `…:P:R3`(손상 JSON) · `…:P:R4`(빈 객체) · 다른 접두 키 → `[{ revisionId: "R1", count: 2 }]`만 · 저장소 항목이 0개면 빈 배열
    - (단위) 그 차수 보관본을 지우는 것은 기존 `clearDirtyEdits(storage, "P", "R1")` 하나이고 `…:P:R2` 키는 그대로다 · 기존 `dirtyStorageKey`·`loadDirtyEdits` 케이스가 그대로 초록이다(D-68 현재 차수 복원 무영향)
    - (E2E 두 탭) 같은 브라우저 문맥의 탭 A·B(같은 보관소): A가 1차 표 첫 줄의 **항목** 셀을 고친다(저장 안 함 — 보관됨) → B가 같은 프로젝트를 열어 「복사해 새 차수」로 2차를 만든다 → A를 새로 고친다 → 표 위에 `1차 저장 안 한 편집 1칸 · 복사 / 버림`(잠김 줄이 있으면 그 위) → 「복사」 → 그 버튼 자리에 `복사됨 1칸`, 줄은 남는다 → 복사된 `text/plain`에서 그 줄의 **입력 열만**(읽기 열 순서의 소분류 → 단가 연속 구간 — 번호·견적가·차익·상태 같은 계산 열이 끼지 않는다) 잘라 `text/plain`만 실은 붙여넣기를 2차 표 같은 줄 소분류 칸에 보낸다 → 항목 칸이 고친 값으로 dirty · 1차 「일괄 저장 N」의 N 증가 · 오류 칸 0 → 「버림」 → 줄이 사라지고 저장소에 1차 키가 없다
    - (E2E) `document.execCommand`가 거짓을 돌려주게 바꾼 뒤 「복사」 → 「복사」 옆 `복사하지 못함`, 줄·보관본 그대로
    - (E2E) 다른 차수 보관본이 둘(1차·2차, 현재 3차) → 줄은 `2차 저장 안 한 편집 …` 하나 → 「버림」 → `1차 저장 안 한 편집 …`
    - (E2E) 이전 차수 읽기 섹션(Task 3)을 열어도 그 안에 복원 줄이 없다
  </behavior>
  <action>
⓪ **전제 확인(D-68 보관 배선 — 이 태스크는 구현하지 않는다, W4).** 이 태스크는 현재 차수 표의 미저장 편집이 브라우저 보관소에 쓰인다는 것(`useDirtyStorage`의 `persist`가 편집 순간마다 불림 — 04-04 D-68 · 교차 그룹 계약 4 「편집은 보관본에 남아 복원 줄로 돌아온다」)을 전제로 한다. 그 배선의 **구현 주인은 04-22 Task 2 ⑤** 하나다. 시작 전에 `<precondition>`을 확인한다 — `quote-table.tsx`의 주석이 아닌 줄에 `persist(` 호출이 있어야 한다. 없으면 이 태스크를 시작하지 않고 멈춰 「D-68 보관 배선 없음 — 주인 플랜 확인 필요」로 보고한다. 여기서 대신 구현하지 않는다(두 번째 구현 금지). 보관 모양·키는 04-04 그대로다.

① **다른 차수 보관본 찾기(`ui/table/use-dirty-storage.ts`).** 단위 테스트(`behavior` 단위 두 줄)를 `test/unit/ui/dirty-storage.test.ts`에 먼저 쓴다(RED). 키를 훑을 수 있는 저장소 타입 `EnumerableDirtyStorage = DirtyStorageLike & { readonly length: number; key(index: number): string | null }`(`window.localStorage`가 만족한다)과 순수 함수 `findOtherRevisionDrafts(storage, scopeId, currentSubScopeId): { revisionId: string; count: number }[]`를 더한다 — `dirtyStorageKey(scopeId, "")` 접두의 키만 보고, 현재 차수 키를 빼고, 기존 `loadDirtyEdits`로 읽어(손상 JSON은 건너뛴다) `countDirtyEdits`가 0인 것은 뺀다. 키 모양·기존 함수·`useDirtyStorage`는 바꾸지 않는다 — 보관 키는 `프로젝트 id + 차수 id` 그대로다(S18 · D-68). 지우기는 기존 `clearDirtyEdits`를 쓴다(GREEN).

② **이전 차수 복원 줄(DR-4).** `previous-revision.tsx`에 `PreviousRevisionDraftRow({ projectId, currentRevisionId, revisions })`: 마운트 때 한 번 `findOtherRevisionDrafts`를 브라우저 저장소로 부르고(SSR·저장소 없음이면 줄 없음 — `useDirtyStorage`의 `browserStorage`와 같은 안전 처리), 차수 id를 `revisions`(04-14 요약 행의 id·순번)로 순번에 맞춰(모르는 id는 무시) **가장 큰 순번 하나**만 `{n}차 저장 안 한 편집 {N}칸` + 3차 「복사」·「버림」 한 줄로 그린다(`--fs-sm --muted`, 배너 아님 — 폭이 모자라면 글자는 `keep-all`, 두 버튼은 함께 줄바꿈). 모양은 04-30 Task 3 ④-b의 현재 차수 복원 줄과 같은 두 클래스 `.restoreBanner`(줄) · `.restoreActions`(「복사」·「버림」 묶음)를 그대로 쓴다 — 이 줄만의 CSS 규칙을 새로 만들지 않는다. 줄이 보이면 그 차수의 줄을 04-14의 `listRevisionLinesAction`으로 한 번 받아 둔다(클릭 처리기 안에서 동기로 복사하기 위해). **「복사」:** 보관본(04-04 D-68 모양)의 값을 그 차수 줄 DTO 위에 덮어 `QuoteLineCopyRow` 행을 만들고(보관본에만 있는 새 줄은 보관값만), 이 파일의 `quoteLineClipboard(rows, references)` → `{ tsv, json }` 하나로 직렬화한다 — `tsv` = `toTsv`(행마다 Task 3 `quoteLineReadColumns`의 열 순서 `copyText`), `json` = `quoteLineClipboardMeta(rows)`(줄마다 `{ currency }` 배열의 JSON — 이 파일에서 export). 이 두 함수가 견적 줄 복사 형식의 유일한 직렬화다 — 뒤 웨이브 04-19의 `Ctrl+C` 격자 복사가 같은 `copyText`·`quoteLineClipboardMeta`를 가져다 쓴다(W1). 클릭 처리기 안에서 `document`에 한 번만 도는 `copy` 리스너를 걸고 `document.execCommand("copy")`를 부른다 — 리스너가 `clipboardData.setData("text/plain", tsv)`와 `setData("application/x-plant8-quote-lines+json", json)`을 하고 기본 동작을 막는다. `Ctrl+C` 범위 복사(04-19 — 네이티브 `copy` 이벤트)와 같은 `DataTransfer` 경로라 같은 브라우저 안 붙여넣기에 앱 형식이 돌아오고, 붙여넣기 쪽의 계산 열 무시 규칙(CEO-D15 · ENG-D5)은 뒤 웨이브 04-47이 만든다 — 앱 형식을 실어 두면 04-47 뒤에 전체 행 붙여넣기의 계산 열이 무시된다(이 플랜 시점에는 그 규칙이 없어 계산 열에 떨어진 값은 04-04 규칙대로 오류 칸이다 — ④ E2E가 입력 열만 붙이는 이유)(클립보드 권한 API는 쓰지 않는다). 성공이면 버튼 자리에 `복사됨 {N}칸`(`--success`)을 보이고 줄은 남긴다. 반환이 거짓이거나 던지거나 그 차수 줄을 아직 받지 못했으면 「복사」 옆 `복사하지 못함`(`block`)이고 보관본은 그대로다(다시 누르면 필요할 때 다시 받는다 — 「복사」가 곧 다시 시도). **「버림」:** 확인 없이 `clearDirtyEdits(storage, projectId, 그 차수 id)` 뒤 줄을 지우고, 다른 차수 보관본이 남았으면 그다음 순번의 줄이 보인다. 현재 차수에 자동으로 합치는 동작을 만들지 않는다(줄 id가 다르다).

③ **표 위 한 줄의 자리(DR-31).** `quote-table.tsx`의 표 위 한 줄 자리에 `PreviousRevisionDraftRow`를 **현재 차수 복원 줄 뒤 · 잠김 줄(`tableLockLine`) 앞**에 렌더한다. `[id]/page.tsx`가 `QuoteLedger`에 `revisions`(04-14 요약 행의 id·순번)를 넘긴다. 이전 차수 읽기 섹션(Task 3)에는 이 줄을 두지 않는다. **폭:** 이 줄은 폭과 무관하게 렌더한다 — UI-SPEC rev 5 UI Considerations S18 overflow(사용자 확정 — 647행)가 375에서 두 복원 줄의 줄바꿈을 정했고, 현재 차수 복원 줄도 04-30이 모든 폭에서 그린다(같은 규칙). 이 태스크는 현재 차수 줄의 조건을 바꾸지 않는다 — 두 줄이 375에서 각자 줄바꿈하고 3차 둘이 함께 내려가는지는 ⑤ DOM 감사가 본다.

④ E2E(`behavior`의 E2E 네 줄). 두 탭은 한 브라우저 문맥의 페이지 둘이라 같은 `localStorage`를 쓴다. **두 탭 E2E는 04-47에 기대지 않는다:** 이 플랜(웨이브 18)에는 앱 형식 붙여넣기의 계산 열 무시(04-47 — 뒤 웨이브)가 아직 없으므로, 붙여넣기는 입력 열 연속 구간(소분류 → 단가)만 `text/plain`으로 보낸다 — 「복사」가 두 형식을 쓰는지는 `copy` 리스너로 따로 단언하고, 계산 열이 끼는 전체 행 왕복은 04-47 Task 1 ⑤의 프로젝트 간 왕복 E2E가 증명한다. 「복사」의 클립보드 내용은 테스트가 `window`에 붙인 `copy` 리스너로 `text/plain`과 앱 형식을 읽고, 같은 두 형식을 실은 `ClipboardEvent('paste')`(`DataTransfer`)를 대상 셀에 보낸다(`quote-table.spec.ts` 선례 — 클립보드 권한 불필요). 실패 케이스는 `page.evaluate`로 `document.execCommand`를 거짓을 돌려주는 함수로 바꾼다.

⑤ **화면 검증 순서(CLAUDE.md).** 싼 게이트(lint·typecheck·build) → **독립 DOM 감사**(별도 서브에이전트, model: sonnet, `verification-before-completion` 명시, `CI=true` 프로덕션 빌드 DOM 실측, 스크린샷 육안 금지)를 **1280 · 1024 · 375**에서: (a) 차수 섹션 단독 오류 자리 없음 (b) 차수 섹션·이전 차수 읽기 섹션 가로 스크롤 0, 현재 행 동작 칸 빈 칸 (c) 읽기 섹션 0 · 1 · 31줄 렌더(EMPTY · 표 · 합계 형식 — 페이지 줄은 04-19 감사) (d) 375의 이전 차수 복원 줄과 현재 차수 복원 줄 — `keep-all` 줄바꿈, 44px 3차 둘이 함께 내려감, 두 줄이 각자 줄바꿈, 가로 스크롤 0 (e) 세 다이얼로그 — 1280·1024의 480 모달과 375 하단 시트에서 제목·부제·결과 줄 잘림 없음, 결과 줄 0·1·2개의 행동 줄 위치 → 발견 수정(`project-detail.module.css`만) → 전체 게이트 **한 번** `CI=true pnpm test`. 폭별 실측값을 SUMMARY에 붙인다.
  </action>
  <verify>
    <automated>grep -v '^\s*//' "app/(app)/projects/[id]/quote-table.tsx" | grep -c 'persist('</automated>
    <fails_when>출력이 0이다 — D-68 보관 배선(구현 주인 04-22 Task 2 ⑤)이 없어 이전 차수 보관본이 생기지 않는다. 이 플랜에서 구현하지 않고 멈춘다(W4)</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/dirty-storage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 다른 차수 찾기가 현재 차수·다른 프로젝트·손상 JSON·빈 보관본을 섞었거나, 기존 보관 키·복원 케이스가 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 두 탭 뒤 이전 차수 복원 줄이 안 보이거나 순서(현재 복원 → 이전 차수 복원 → 잠김)가 다르거나, 「복사」가 두 형식을 쓰지 않거나 붙여넣은 칸이 dirty가 되지 않거나, 실패 때 `복사하지 못함`이 없거나 보관본이 지워졌거나, 「버림」이 다음 차수를 보이지 않거나, 읽기 섹션에 복원 줄이 있거나, 기존 표 붙여넣기가 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 보고서(1280 · 1024 · 375, 항목 (a)~(e)별 실측값)를 SUMMARY에서 확인</test>
      <expected>차수 섹션 단독 오류 자리 없음 · 세 폭 가로 스크롤 0 · 빈 동작 칸 · 읽기 섹션 0/1/31줄 형식 · 복원 줄 줄바꿈 규칙 · 세 다이얼로그 잘림 없음</expected>
      <why_human>렌더 배치·줄바꿈은 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `findOtherRevisionDrafts`가 `ui/table/use-dirty-storage.ts`의 순수 함수이고 단위 테스트가 현재 차수 · 다른 프로젝트 · 손상 JSON · 빈 보관본 · 다른 접두를 덮는다 · 기존 키 모양과 `useDirtyStorage`가 바뀌지 않았다
    - 두 탭 E2E가 `1차 저장 안 한 편집 1칸 · 복사 / 버림` → 「복사」 → `복사됨 1칸` → 2차 표 붙여넣기(입력 열 소분류 → 단가만 — 04-47의 계산 열 규칙에 기대지 않는다) → 항목 칸 dirty · N 증가 · 오류 칸 0 → 「버림」 → 저장소 키 없음을 단언한다(DR-4)
    - 「복사」가 `text/plain` TSV와 `application/x-plant8-quote-lines+json`을 같은 `copy` 이벤트로 쓰고(E2E가 리스너로 확인), 실패 때 `복사하지 못함`을 보이며 보관본을 지우지 않는다 · 두 형식이 `quoteLineClipboard`(= 읽기 열 `copyText` + `quoteLineClipboardMeta`) 한 곳에서 나온다(W1 — 이 플랜 시점 `app/`에서 앱 형식 JSON을 만드는 코드는 `previous-revision.tsx`뿐이다, Grep)
    - 표 위 한 줄 순서가 현재 복원 → 이전 차수 복원 → 잠김이다(E2E, DR-31) · 여러 보관본이면 가장 최근 차수 하나만 보이고 버리면 다음 것이 보인다
    - 이전 차수 편집을 현재 차수에 자동으로 합치는 코드가 없다(코드 리뷰 — 「복사」만 있다)
    - 시작 전에 전제(`persist(` 호출 있음)를 확인했고, 이 플랜의 diff가 `quote-table.tsx`에 `persist(` 호출을 더하지 않는다 — 보관 배선의 구현 주인은 04-22 Task 2 ⑤ 쪽 하나다(W4)
    - 독립 DOM 감사 보고서(1280 · 1024 · 375)가 SUMMARY에 있다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>동료가 새 차수를 만들어 밀려난 미저장 편집이 표 위 한 줄로 다시 제시되어 복사로 새 차수에 옮겨지거나 버려지고, 차수 화면 전체가 세 폭의 DOM 실측과 전체 게이트를 통과했다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 판정 → 머리 줄 버튼 | 새 차수·승인 버튼의 렌더 조건이 서버에서 온다. 화면이 담당 PM·상태를 추론하면 서버 규칙과 어긋난다 |
| 서버 보기 액션(`listRevisionLinesAction`) → 이전 차수 읽기 섹션 · 복원 줄 복사 | 프로젝트 id와 순번이 넘어간다 — 행 범위·투영은 04-14 서버에 있다 |
| 브라우저 저장소 → 이전 차수 복원 줄 | 같은 브라우저의 보관본(D-68)을 읽는다 — 사용자 기기 안의 믿을 수 없는 데이터다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-68 | Elevation of Privilege | 고객 승인 버튼(화면 쪽) | high | mitigate | 버튼 렌더는 서버가 계산한 담당 PM 여부(쓰기 권한 포함 — B-30)·상태·연결 문서 결과만 본다. 비담당·완료에서 버튼 부재를 E2E로 고정하고, 서버 거부는 04-14 통합 테스트가 본다 |
| T-04-70b | Tampering | 새 차수 중복 요청(화면 쪽) | medium | mitigate | 다이얼로그가 보던 차수 id를 싣고 진행 중 1차가 `…`다 — 더블클릭 E2E가 차수 하나만 늘었음을 단언한다(B-02). 서버 판정은 04-14·04-40 |
| T-04-70c | Denial of Service | 미저장 입력 손실(UX-04) | medium | mitigate | 이 탭의 새 차수·승인은 미저장 편집이 있으면 1차가 막힌다(B-03 · ENG-D9) · 이전 차수 읽기 섹션은 원장을 바꾸지 않는다(DR-13) · 다른 사람의 새 차수로 밀려난 보관본은 이전 차수 복원 줄로 다시 제시된다(DR-4) — 세 경로 모두 E2E |
| T-04-70d | Tampering | 본 적 없는 금액의 승인(화면 쪽) | high | mitigate | 승인 다이얼로그가 보고 있는 현재 차수의 합계·내용 토큰을 싣고, 미저장 편집이 있으면 1차를 막는다 — 옛 기준값은 서버가 거부하고 `견적이 바뀜 · 새로 고침`이 막힘 자리에 나온다(ENG-D9 · Codex #2, E2E). 서버 판정·경합은 04-14·04-40 |
| T-04-124b | Tampering | 승인 차수 견적 칸(화면 쪽) | medium | mitigate | 잠긴 칸(수량·단가·상태·소분류 — ENG-D7)은 서버 DTO와 같은 판정 함수의 결과로만 그린다 — 화면이 풀어 둔 칸을 보내도 04-40의 게이트가 거부한다 |
| T-04-71 | Elevation of Privilege | 이전 차수 보기(화면 쪽) | high | mitigate | 이전 차수 읽기 섹션은 04-14 보기 액션의 잠김 조회(서버가 전부 `locked`)를 흰 머리글 읽기 표로 그릴 뿐 편집·저장·구조 컨트롤이 없고 원장을 바꾸지 않는다 — 이전 승인 차수를 현재처럼 편집·사용하는 화면 경로가 없다. URL 검색 파라미터를 받지 않는다(DR-13) |
| T-04-71c | Tampering | 브라우저 보관본 → 복사 | low | mitigate | 보관본은 현재 차수에 자동으로 합치지 않고 클립보드로만 나간다 — 붙여 넣은 값은 일반 붙여넣기 검증(04-04 · 04-47 앱 형식 규칙)과 저장 때 서버 판정(04-12 · 04-40 — 셀 단계 · 승인 잠금 · 금액 정규화)을 그대로 지난다 |
| T-04-71d | Information Disclosure | 클립보드의 금액 | low | accept | 복사는 사용자가 누른 행동이고, 내용은 그 사람에게 이미 보이는 DTO 투영(숨긴 금액 키 없음)과 그 사람이 친 보관값뿐이다 — 새로 노출되는 정보가 없다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `ui/history-list` diff 0줄, `package.json` 의존성 객체(dependencies·devDependencies·optionalDependencies·peerDependencies)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있다 (원문 비교 아님 — 04-03 재개 시 scripts 추가로 깨지지 않게)
- 이 플랜의 커밋이 `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`를 고치지 않았다 — `git log --format=%s -- .planning/phases/04-project-quote-ledger/04-UI-SPEC.md | grep -c '(04-24)'`가 0(DR-1)
- 독립 DOM 감사(1280 · 1024 · 375 — S5 · S5 읽기 섹션 · S18 · S16 사용처) 판정이 SUMMARY에 있다

</verification>

<success_criteria>

- 담당 PM이 상세에서 공용 확인 다이얼로그로 새 차수를 만들고, 고객 승인을 확인 근거 칸의 날짜와 함께 켜고 조건대로 끈다
- 차수 섹션이 변천을 읽기 표로 보이고, 지난 차수를 그 아래 잠긴 섹션으로 펼쳐 보는 동안 현재 원장의 편집이 흔들리지 않는다
- 동료의 새 차수로 밀려난 미저장 편집이 이전 차수 복원 줄로 다시 제시되어 복사·버림으로 처리된다
- 완료·비담당·정산의 버튼 부재가 E2E로 고정됐다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컴포넌트(신규) | `app/(app)/projects/[id]/revision-section.tsx`(`RevisionSection` — 「차수 열기」/「차수 닫기」 토글) · `app/(app)/projects/[id]/revision-dialogs.tsx`(`NewRevisionDialog` · `ApprovalDialog` · `ApprovalCancelDialog` — 셋 다 `ui/confirm-dialog`) · `app/(app)/projects/[id]/previous-revision.tsx`(`PreviousRevisionSection` · `PreviousRevisionDraftRow` · `quoteLineReadColumns`(열마다 `copyText`) · `quoteLineClipboardMeta` · `quoteLineClipboard` · 타입 `QuoteLineCopyRow` — 견적 줄 복사 형식의 주인, W1) |
| 함수(신규 — ui) | `findOtherRevisionDrafts` · 타입 `EnumerableDirtyStorage`(`ui/table/use-dirty-storage.ts` — 기존 키 모양·훅 무변경) |
| 변경된 기존 심볼 | `QuoteLedger`(차수·승인 버튼 자리 — `{ dirtyCount }` 렌더 함수 prop · 새 줄 기본값의 승인 차수 잠금 · 표 위 한 줄 자리의 이전 차수 복원 줄(현재 복원 → 이전 차수 복원 → 잠김) · `revisions` prop · 그룹 함수 export) · 상세 부제(고객 승인일 · 표시 번호) · 상세 섹션 순서(③ 차수 → ④ 이전 차수 읽기 섹션) |
| 다이얼로그 입력 | `ApprovalDialog` → `setCustomerApprovalAction({ revisionId, approval: { approvedOn, seenTotalKrw, contentToken } })`(ENG-D9 — 현재 차수 요약 행 값, 승인일은 `evidenceField`) · 미저장 편집 막힘(그룹 A의 DR-6 함수 하나) |
| 서버 호출(04-14 소유) | `listRevisionLinesAction`(이전 차수 읽기 섹션 · 복원 줄 복사) |
| 클립보드 형식 | `text/plain`(TSV — `quoteLineReadColumns` 열 순서 `copyText`) + `application/x-plant8-quote-lines+json`(줄별 `{ currency }` — `quoteLineClipboardMeta`), 네이티브 `copy` 이벤트 — 이 플랜이 형식의 주인이고 04-19 격자 `Ctrl+C`가 같은 두 함수를 쓴다(W1) |
| 테스트 | `test/e2e/quote-revisions.spec.ts`(신규 — 더블클릭 · 미저장 막힘(새 차수·승인) · 빈 차수 · 승인일 칸 · 옛 기준값 승인 거부 · 빈 차수 승인 거부 · 승인 차수 잠금(소분류 포함) · 차수 섹션 · 읽기 섹션 · 두 탭 복원 줄) · `test/unit/ui/dirty-storage.test.ts`(다른 차수 찾기 확장) |
| 문서 | 없음 — UI-SPEC rev 5가 이 플랜의 문구를 전부 담았다(옛 「Copywriting 여덟 행」 태스크 삭제, DR-1) |
| 새 스키마·마이그레이션·액션 | 없음 — 서버 쪽은 04-14·04-40 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-02 (T3) — 새 차수 더블클릭·오래된 탭, 대기 중 버튼 상태 미정 | CRITICAL | Plan 04-24, Task 1 ①④: `fromRevisionId` 싣기 + 진행 중 1차 비활성 + 더블클릭 E2E(차수 하나) |
| B-03 (T3) — 미저장 편집이 새 차수로 조용히 사라짐(UX-04) | CRITICAL | Plan 04-24, Task 1 ②④: `dirtyCount` ≥ 1이면 1차 막힘 + 이유(Round 3에서 rev 5 글자 `저장 안 한 편집 {N}칸 · 먼저 일괄 저장` · DR-6과 같은 함수로), E2E |
| 사용자 D7 · OV-1 (T4) — 승인 차수 견적 칸 잠금의 화면 | HIGH | Plan 04-24, Task 2 ④: `approvedSeq`를 표 위 한 줄·새 줄 기본값에 싣기, E2E |
| B-25 — 승인일 하루 어긋남(화면 쪽) | WARNING | Plan 04-24, Task 2 ②: 서버 날짜 문자열 그대로, 화면 시간대 계산 금지 |
| B-30 — 승인 버튼 렌더 조건(쓰기 권한) | NOTE | Plan 04-24, Task 2 ②: 서버 판정 결과로만 렌더 |
| 사용자 D19-9 — 상태별 고객 승인 기본값 확정 | DECISION | Plan 04-24, `<probe_fallback>`·Task 2 behavior: 정산 렌더·완료 부재 |
| 사용자 D10 — 정산의 새 차수 없음 | DECISION | Plan 04-24, truths: 「복사해 새 차수」 정산 미렌더 유지 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-D9 · Codex #2 (R8) — 승인이 본 내용을 가리키지 않음(화면) · 미저장 편집 중 승인 | DECISION(P1) | Plan 04-24, Task 2 ②·behavior: 요약 행 합계·토큰 싣기 · 새 차수와 같은 미저장 편집 막힘 · 옛 기준값 거부 E2E (문구는 UI-SPEC rev 5 — 옛 Task 3 ③ Copywriting 편집은 Round 3에서 삭제) |
| ENG-D4 (R3) — 빈 차수 승인 거부(화면) | DECISION | Plan 04-24, Task 2: 서버 문구를 막힘 자리에 · E2E (문구는 rev 5) |
| ENG-D7 (R6) — 승인 차수 소분류 잠금(화면) | DECISION | Plan 04-24, truths · Task 2 ③: DTO 셀 단계 그대로 · E2E |

### Round 3 — 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-4 — 동료가 새 차수를 만들면 내 미저장 편집의 복구 경로가 없음(옛 이월 행) | CRITICAL | Plan 04-24, Task 4: `findOtherRevisionDrafts`(단위 TDD) · 표 위 `{n}차 저장 안 한 편집 N칸 · 복사 / 버림`(복사 = 범위 복사와 같은 TSV + 앱 형식, 자동 병합 없음) · 두 탭 E2E — 옛 Deferred 행을 닫는다 |
| DR-13 — 「차수 열기」가 `?revision=n`으로 원장을 바꿔치기(편집 손실 · 저장 없는 편집 칸) | HIGH | Plan 04-24, Task 3: `<button aria-expanded aria-controls>` → 차수 섹션 아래 잠긴 읽기 섹션(처음 열 때만 받음 · 오류 줄 · 30줄 쪽 · 제목 포커스) · 원장 잠김 모드와 `{n}차 · 읽기 전용` 삭제 · 서버 액션은 Plan 04-14 |
| DR-12 — 확인 모달 공용 컴포넌트 | HIGH | Plan 04-24, Task 1 ① · Task 2 ②: 세 확인 = `ui/confirm-dialog`(Plan 04-46 — 조상) |
| DR-20 — 승인 모달 안 날짜 칸(§7-8 예외) | MEDIUM | Plan 04-24, Task 2 ②: 승인일 = `evidenceField`(개정 ⑭) · 첫 포커스 · 형식 오류는 칸 아래 한 자리 |
| DR-21 · P0 · F1 · F2 — 토스트 동사 · 막힘·거부 문구 · 모달 본문 문장 | MEDIUM | Plan 04-24, truths · Task 1·2 E2E: `새 차수 만들기 · 상세 견적 {n+1}차` · `저장 안 한 편집 {N}칸 · 먼저 일괄 저장` · `… · 새로 고침` 명사형 · `복사할 견적 줄 없음 · 첫 줄 만들기` · 승인·취소 본문 → 부제·결과 줄 |
| DR-1 — 플랜이 UI-SPEC을 직접 고침(옛 Task 3 ③ Copywriting 여덟 행) | CRITICAL | 삭제 — rev 5가 담았다 · `files_modified`에서 UI-SPEC 제외 · verification에 이 플랜 커밋의 UI-SPEC 무변경 |
| DR-31 — 표 위 줄 순서 | MEDIUM | Plan 04-24, Task 4 ③: 현재 복원 → 이전 차수 복원 → 잠김 |
| DR-14 — DOM 감사 폭 1024 | HIGH | Plan 04-24, Task 4 ⑤: 1280 · 1024 · 375 |
| UI Considerations S5 · S18 · S16 사용처 lift | — | Plan 04-24, `must_haves.truths`(explicit 문장 + backstop 다섯) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | 옛 「새 차수 뒤 옛 보관 키 복원 없음」 이월은 DR-4(Round 3)로 닫혔다 |

### Round 4 — plan-checker 반복 1(2026-09-23 revision)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W4 — Task 4 ⓪이 D-68 `persist` 배선을 조건부로 다시 구현(주인은 04-22 Task 2 ⑤) | WARNING | Plan 04-24, Task 4: ⓪을 구현 없는 확인으로 바꾸고 `<precondition>`(주석 아닌 `persist(` 호출 ≥ 1 — 주인 04-22 Task 2 ⑤) + 첫 검증 명령(grep, 0이면 멈춤) + 수용 기준(이 플랜 diff에 `persist(` 추가 없음). 대신 구현하는 갈래를 지웠다 |
| W1 — 견적 줄 복사 직렬화 두 벌(04-19 · 04-24) | WARNING | Plan 04-24가 형식의 주인임을 명시: Task 3 ② 읽기 열마다 `copyText` + `QuoteLineCopyRow` · Task 4 ② `quoteLineClipboard`(= `toTsv`(`copyText`) + `quoteLineClipboardMeta`) · truth · probe_fallback · key_link · Artifacts. 옛 「04-19와 같은 모양」 문구를 「04-19가 이 함수를 쓴다」로 뒤집었다(04-19 Round 4가 소비 쪽) |
| W2 — 4태스크 · 98k(쪽 나눔 중복) | WARNING | 9파일(비테스트 7) ≤ 15라 분할하지 않는다(규칙). 중복 제거로 줄였다: 이전 차수 읽기 섹션의 수동 30줄 자르기·`ui/pagination` 배선·31줄 쪽·제목 포커스 E2E를 지우고 04-19의 `ui/table` `pagination` 한 구현으로 옮김(truth · backstop · Task 3 name·read_first·behavior·action·fails_when·수용 기준 · Task 4 ⑤(c) · 04-19 Round 4가 받음) + W4의 대체 구현 갈래 삭제 · 추정 98k → 88k |
| B4 연동 — 현재 차수 복원 줄의 폭 조건 문구 | NOTE | Plan 04-24, Task 4 ③ · backstop · ⑤(d): 「04-30이 1024 이상에서만 그리면 SUMMARY에 어긋남 기록」 조건문을 지우고 rev 5 S18 overflow(647행)대로 두 줄 모두 모든 폭에서 렌더되는 것으로 적음(현재 차수 줄의 폭 규칙 구현은 04-30 — 레인 1의 B4) |

### Round 5 — 체커 revision 1 후속 정리(2026-09-23 · 분할 포인터 · 두 탭 E2E 웨이브 순서)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 분할 포인터 — `Button`(계약 1)·`ui/confirm-dialog`(계약 2)의 주인 | NOTE | 「04-08」 17곳(truth · key_link · objective (3) 조상 사슬 · 계약 가정 · read_first · Task 1 ① · Task 2 ② · 수용 기준 · Round 3 행) → **04-46**. 조상 사슬 04-24 → 04-14 → 04-13 → 04-12 → 04-44 → 04-22 → 04-11 → 04-21 → 04-20 → 04-29 → 04-46 — `depends_on` 변경 없음 |
| 분할 포인터 — 상태 모달·미저장 막힘 글자 | NOTE | read_first `status-change.tsx`(04-21·04-22 → 04-21·04-44) + 막힘 글자 함수 `unsaved-edits.ts` `unsavedEditsReason`(04-21) · 계약 가정 줄. 04-21·04-44 모두 조상 |
| W4 포인터 — D-68 `persist` 배선 주인 | NOTE | `<precondition>` · ⓪ · 첫 검증 fails_when · Round 4 W4 행: 「04-22 Task 2 ⑤ 또는 04-22 분할에서 그 몫을 받은 플랜」 → 정확히 「04-22 Task 2 ⑤」(분할 뒤에도 04-22에 남았다 — 04-22 Task 2 ⑤가 유일한 구현 주인) |
| 이전 차수 복원 줄의 모양 | NOTE | backstop 줄 · Task 4 ② · read_first: 04-30의 `.restoreBanner` + `.restoreActions`를 그대로 쓴다(새 CSS 규칙 없음). 04-30은 조상(04-13 → 04-26 → 04-30) |
| 두 탭 E2E가 뒤 웨이브(04-47)의 계산 열 무시 규칙에 기댐 | WARNING | behavior 두 탭 줄 · ② · ④ · 수용 기준: 붙여넣기는 입력 열(소분류 → 단가)만 `text/plain`으로 — 이 웨이브(18)에 없는 04-47 규칙에 기대지 않는다고 ④에 명시. 전체 행 왕복(계산 열 무시)은 04-47 Task 1 ⑤ |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-24-SUMMARY.md` when done
</output>
