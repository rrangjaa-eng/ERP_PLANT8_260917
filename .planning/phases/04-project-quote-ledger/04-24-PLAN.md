---
phase: 04-project-quote-ledger
plan: 24
type: execute
wave: 18
depends_on: ["04-14", "04-40"]
files_modified:
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revision-dialogs.tsx
  - app/(app)/projects/[id]/revision-section.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/e2e/quote-revisions.spec.ts
  - .planning/phases/04-project-quote-ledger/04-UI-SPEC.md
autonomous: true
requirements: [PROJ-07, PROJ-05]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "「복사해 새 차수」 확인 모달의 `{k}줄`은 조정 줄을 세지 않고, 조정 줄이 있으면 결과 줄 `조정 {m}줄은 복사하지 않고 {n+1}차에서도 그대로 보입니다`가 더해진다 · 1차 `새 차수 만들기` · 2차 `취소 Esc` · 성공 토스트 `새 차수 · 상세 견적 {n+1}차`(Copywriting Destructive — 새 차수, SUCCESS)"
    - "「복사해 새 차수」는 수주중·진행·미수주에서만 렌더되고, 정산·완료에서는 없다(S3, 사용자 D10 — 정산에 줄 추가는 열렸지만 새 차수는 서버 거부 · D-47 — 서버 거부는 04-14)"
    - "새 차수 모달의 1차는 화면이 보고 있던 차수 id(`fromRevisionId`)를 싣고, 누른 뒤 응답이 올 때까지 비활성이다 — 더블클릭해도 차수가 하나만 생기고, 다른 탭에서 먼저 만들었으면 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`가 막힘 자리에 나온다(CEO 리뷰 B-02)"
    - "표에 저장 안 한 편집이 있으면(1차 「일괄 저장 N」의 N ≥ 1) 새 차수 모달의 1차가 비활성이고 그 옆에 `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`가 보인다 — 저장하면 켜진다. 새 차수로 미저장 보관 키가 바뀌어 입력이 조용히 사라지는 길이 없다(CEO 리뷰 B-03, UX-04, D-68, §7-1 비활성 + 이유)"
    - "서버 거부는 게이트 문자열 그대로 1차 옆 막힘 자리에 나오고 모달이 닫히지 않으며 토스트가 없다 — 빈 차수면 `새 차수를 만들 수 없음 — 복사할 견적 줄 없음 · 첫 줄 만들기`(S3, 04-14 계획 단계 판단 문구)"
    - "「고객 승인 표시」·「승인 표시 취소」는 수주중·진행·미수주·정산에서 담당 PM(쓰기 권한 포함 — 서버 판정)에게만 렌더되고 완료에서는 둘 다 없다 — 승인일은 완료에서도 부제에 그대로 보인다(⚠ S3 「상태별 고객 승인」 기본값 = 사용자 D19-9, D-56, CEO 리뷰 B-30)"
    - "현재 차수가 승인되면 표의 기존 견적 줄 수량·단가·상태·소분류 칸이 잠김 모양이 되고 표 위 한 줄과 잠긴 셀의 편집 시도 이유가 `{n}차 고객 승인됨 · 고치려면 새 차수`다 — 실행가는 그대로 편집되고, 새 줄의 수량·단가는 잠긴 채 생긴다(새 줄의 소분류는 고른다). 판정은 04-40의 함수 결과이고 화면이 승인 여부로 칸을 추론하지 않는다(사용자 D7 · OV-1 · 엔지니어링 리뷰 ENG-D7)"
    - "「고객 승인 표시」 모달은 제목 `고객 승인 표시` · 본문 `{n}차 · 고객이 승인한 날` + 날짜 칸(기본값 오늘 KST) · 1차 `고객 승인 표시` · 2차 `취소 Esc`다(04-14 계획 단계 판단 — Copywriting에 더한다)"
    - "「고객 승인 표시」 모달의 1차는 화면이 보고 있는 현재 차수의 견적 합계와 내용 토큰(04-14 `listRevisionSummaries`의 현재 차수 행)을 기준값으로 싣고, 그 사이 다른 사람이 견적을 바꿨으면 서버 거부 `견적이 바뀌었습니다 · 새로 고쳐 주세요`가 1차 옆 막힘 자리에 나오며 모달이 열린 채다 — PM이 보지 않은 금액이 승인되지 않는다(엔지니어링 리뷰 ENG-D9 · Codex #2)"
    - "표에 저장 안 한 편집이 있으면(1차 「일괄 저장 N」의 N ≥ 1) 「고객 승인 표시」 모달의 1차가 비활성이고 그 옆에 `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`가 보인다 — 새 차수 모달과 같은 차단이고, 저장하면 켜진다(ENG-D9 · B-03과 같은 결)"
    - "견적 줄이 0개인 현재 차수에서 승인을 누르면 서버 거부 `승인할 견적 줄이 없음 · 첫 줄 만들기`가 막힘 자리에 나온다 — 화면이 줄 수로 버튼을 따로 판정하지 않는다(엔지니어링 리뷰 ENG-D4 — 서버 판정은 04-14)"
    - "견적 금액(`quote.amount`)을 볼 수 없는 사람에게는 확인할 합계가 없어 「고객 승인 표시」가 렌더되지 않는다 — 요약 행에 합계 키가 없으면 버튼이 없다(ENG-D9의 결과 — 04-14 `<probe_fallback>`)"
    - "승인 표시 취소는 그 차수에 연결 문서가 없을 때만 버튼이 있고, 취소 모달은 제목 `고객 승인 표시 취소` · 본문 `{차수} 승인일이 지워집니다 · 이 차수의 지출결의가 다시 막힙니다` · 1차 `승인 표시 취소` · 2차 `닫기 Esc`다(D-56, Copywriting, §7-8 보강)"
    - "부제에 `고객 승인 2026-09-19 박서연`이 붙고 견적 표시 번호 `26001-2차`가 보인다 — 번호는 04-14의 `quoteDisplayNumber` 파생이고 화면이 조립하지 않는다. 승인일 글자는 서버가 KST 달력 날짜로 만든 문자열 그대로이고 화면이 시간대를 계산하지 않는다(D-56, CEO 리뷰 B-25)"
    - "차수 섹션은 차수 · 생성일 · 줄 수 · 견적 합계 · 상태 · 동작 여섯 열, 최신이 위이고, 편집 가능한 셀이 0이라 캡션 있는 읽기 표(흰 머리글)다 — 이력 목록 컴포넌트를 쓰지 않는다(S5)"
    - "차수 섹션 상태 열: 최신이면서 미승인 = `현재`(accent) · 승인된 차수 = `승인`(success, 최신이어도 `승인`만) · 승인 없이 지나간 이전 차수 = 빈 칸(`—`로 채우지 않는다) · 현재 차수 행의 동작 칸도 빈 칸(U-2 사용자 확정, S5)"
    - "이전 차수 행의 3차 「차수 열기」는 그 차수를 잠김 모양의 읽기 표로 보여 주고(0줄이면 `이 차수에 견적 줄이 없습니다`), 되돌리기·편집·저장·구조 컨트롤·미저장 복원이 없으며 조정 그룹이 없다(S5, Copywriting Empty — 이전 차수, ⚠ S4 조정 줄 이동의 결과)"
    - "차수 섹션 폰 P1은 차수·견적 합계·(동작), 접힌 줄은 상태·생성일·줄 수이고 빈 값 자리를 건너뛴다(S5)"
    - statement: "차수 섹션은 제 오류 자리를 갖지 않고 상세 로드 실패 한 줄에 흡수된다 — 섹션 단독 오류 렌더가 없다"
      verification: backstop
    - statement: "차수 섹션이 375px에서 가로 스크롤 0이고 현재 행의 빈 동작 칸을 `—`로 채우지 않는다 — DOM 감사"
      verification: backstop
  artifacts:
    - path: "app/(app)/projects/[id]/revision-section.tsx"
      provides: "차수 섹션 — ui/table 읽기 형태, 최신이 위, 이전 차수에만 「차수 열기」"
      contains: "caption"
    - path: "app/(app)/projects/[id]/revision-dialogs.tsx"
      provides: "복사해 새 차수 · 고객 승인 표시 · 승인 표시 취소 모달"
      contains: "createRevisionAction"
    - path: "test/e2e/quote-revisions.spec.ts"
      provides: "새 차수(더블클릭 한 번 · 미저장 편집 차단) · 승인 표시와 취소(미저장 편집 차단 · 옛 기준값 거부 · 빈 차수 거부) · 승인 차수 칸 잠금(소분류 포함) · 완료의 버튼 부재 · 차수 섹션 · 차수 열기 읽기 표 스모크"
      min_lines: 80
  key_links:
    - from: "app/(app)/projects/[id]/revision-dialogs.tsx"
      to: "app/(app)/projects/actions.ts"
      via: "모달의 1차가 04-14의 `createRevisionAction`·`setCustomerApprovalAction`을 부르고 거부 문자열을 그대로 쓴다"
      pattern: "setCustomerApprovalAction"
    - from: "app/(app)/projects/[id]/revision-section.tsx"
      to: "ui/table/Table.tsx"
      via: "편집 셀 0개라 자동으로 캡션 읽기 표 — 모드 prop을 만들지 않는다"
      pattern: "caption"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/quotes/revisions.ts"
      via: "섹션 행·상태 낱말·표시 번호가 서버 함수에서 온다"
      pattern: "listRevisionSummaries"
---

<objective>
**As a** 기획 PM, **I want to** 상세에서 「복사해 새 차수」로 네고 견적을 새 차수로 만들고, 고객이 승인한 날을 표시하거나 조건대로 거두며, 차수 섹션에서 지난 차수를 읽기 전용으로 열어 보고 싶다, **so that** 견적의 변천이 화면에서 한눈에 읽히고 「현재 차수가 승인됐는가」가 부제 한 줄로 보인다.

**2026-09-23 체커 1차 분할.** 옛 04-14(20파일 — 세 태스크가 다 10파일 이상)를 서버와 화면으로 나눈 **화면 쪽**이다. 04-14가 차수 domain(새 차수 · 고객 승인 표시와 취소 · 계보 해석 · 차수 요약 · 표시 번호) · 게이트 규칙 넷 · 설정 키 · 액션 둘을 서버에서 끝냈다. 이 플랜은 그 위에 모달 셋 · 머리 줄 버튼과 부제 · 차수 섹션 · 이전 차수 읽기 표를 그리고, Copywriting에 계획 단계 문구 두 줄을 더한다.

Purpose: ROADMAP 기준 3의 「차수마다 고객 승인 표시」가 화면에서 보이고, 04-16(계약 금액 = 승인된 현재 차수 합계)의 E2E가 이 플랜의 승인 표시 모달로 승인을 켠다.

Output: `revision-dialogs.tsx`(모달 셋) · `revision-section.tsx` · 머리 줄 버튼·부제 · 이전 차수 잠김 보기 · UI-SPEC Copywriting 두 행 · E2E 1.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — `docs/designs/plant8-erp-phase4-ceo-review-260923.md` T3·T4·부록 B).** (1) **B-02:** 새 차수 모달이 보던 차수 id를 싣고 대기 중 1차를 끈다 — 더블클릭 E2E. (2) **B-03:** 옛 계획은 「새 차수로 미저장 보관 키가 바뀌어 섞이지 않는다 — 확인만」이라 했지만, 그 결과 옛 키의 입력이 다시 제시되지 않아 조용히 사라진다(UX-04 위반) — 미저장 편집이 있으면 모달 1차를 비활성 + 이유로 막는다. (3) **사용자 D7 · OV-1:** 승인 차수의 견적 칸 잠금을 표에 그린다(판정은 04-40). (4) **B-25·B-30:** 승인일 글자·버튼 렌더 조건은 서버 결과 그대로. (5) Copywriting에 더하는 행이 넷 늘었다(미저장 편집 막힘 · 동시 새 차수 · 옛 차수 저장 거부 · 승인 차수 잠김). 이 플랜은 04-14에서 나뉜 04-40(잠금·승인 차수 판정) 뒤에 선다.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 결정 번호는 「ENG-D*」로 적어 CEO 리뷰의 「사용자 D*」와 구별한다).** (1) **ENG-D9(Codex #2):** 승인 모달이 보고 있는 현재 차수의 합계·내용 토큰을 싣고, 미저장 편집이 있으면 새 차수 모달과 같은 차단으로 1차를 끈다 — 옛 기준값 거부는 막힘 자리에. (2) **ENG-D4:** 빈 차수 승인 거부 문구를 막힘 자리에 그대로. (3) **ENG-D7:** 승인 차수 잠김 칸에 소분류가 더해진다(판정은 04-40 — 화면은 DTO 그대로). (4) Copywriting에 두 행(빈 차수 승인 · 견적 바뀜)을 더하고 미저장 편집 행을 새 차수·승인 둘에 쓰는 한 행으로 적는다 — 모두 여덟 행.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 463–471행(S5 상태) · 1154–1194행(S5 적용 규칙 — 왜 이력 목록이 아닌가 · 열 · 폰 우선순위 · 상태 낱말 · 차수 열기 · 복사해 새 차수) · 1057–1094행(S3 머리 줄 — 버튼 렌더 조건 · 고객 승인 버튼 · 상태별) · 439행·457–461행(⚠ S3 · ⚠ S4 조정 줄과 복사) · 328행·360행·366행·370–371행(Copywriting)
# .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md U-2 행(사용자 확정 값)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(차수·고객 승인의 화면)** 과 **PROJ-05(차수 전체 복사의 화면)** 다. 수용 기준은 D-53~D-56과 UI-SPEC S3·S5에서 끌어왔다(옛 04-14에서 그대로 — 서버 쪽 truths와 금지 항목은 04-14에 남았다). S5 `## UI Considerations`의 error backstop과 overflow는 마커로, empty(구조적으로 없음)·loading(상세 한 번)·populated·partial(U-2)·zero-one-many는 truths로 옮겼다.

**계획 단계 가정(04-14에서 옮김):** ⚠ S3 상태별 고객 승인 — 수주중·진행·미수주·정산 = 렌더, 완료 = 둘 다 없음(사용자 D19-9로 확정). UI-SPEC에 없는 문구 둘(「고객 승인 표시」 모달 · 빈 차수 거부 이유)은 04-04 U-6 선례대로 이 플랜이 Copywriting에 더한다. CEO 리뷰가 만든 문구 넷(미저장 편집 막힘 · 동시 새 차수 · 옛 차수 저장 거부 · 승인 차수 잠김)도 같은 방식으로 더한다.

**미저장 편집과 새 차수(CEO 리뷰 B-03):** 미저장 보관 키가 프로젝트 + 차수라(04-04 설계) 새 차수가 되면 옛 키의 보관이 다시 제시되지 않는다. 보관 키를 바꾸거나 옛 편집을 새 차수로 옮기는 대신 **모달에서 막는다** — 편집은 저장하거나 버린 뒤 새 차수를 만든다. 다른 사람이 만든 새 차수로 이 탭의 저장이 거부되는 경우(04-40)는 거부 이유가 「새로 고쳐 주세요」를 말하고 보관은 옛 키에 남는다 — 새로 고친 뒤 옛 차수의 복원 줄은 뜨지 않으므로, 그 사실을 SUMMARY에 한 줄로 적는다(이 플랜이 새 동작을 만들지 않는다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 담당 PM이 「복사해 새 차수」 모달로 2차를 만든다(머리 줄 버튼 → 모달(보던 차수 id · 대기 중 비활성 · 미저장 편집 차단) → 액션 → 토스트 · 부제 · 새 현재 차수 표 → E2E)</name>
  <files>app/(app)/projects/[id]/revision-dialogs.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `domain/quotes/revisions.ts`(04-14 산출물 — `createRevisionFromCurrent`·`quoteDisplayNumber`) · `app/(app)/projects/actions.ts`의 `createRevisionAction`(04-14 — `projectId`·`fromRevisionId` 필수)
    - `domain/quotes/edit-scope.ts`의 `structuralEditability.canCreateRevision`(04-14)
    - `app/(app)/projects/[id]/status-change.tsx`(04-21·04-22 산출물) — 네이티브 모달 요소 모달 형태·막힘 자리·대기 중 비활성 선례
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 371행(Destructive — 새 차수) · 1192–1194행
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 머리 줄 버튼 자리·`dirtyCount`(1차 「일괄 저장 N」의 N — 견적 줄·매출 dirty 합)·`useDirtyStorage`(보관 키 = 프로젝트+차수)를 찾아 범위만 Read
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-02·B-03 절(Grep `### B-02`·`### B-03`)
  </read_first>
  <action>
① `revision-dialogs.tsx`의 `NewRevisionDialog`: 머리 줄 2차 「복사해 새 차수」(page가 `canCreateRevision`일 때만 렌더) → 모달(Copywriting 그대로, `{k}`는 조정 줄 제외 · 조정 줄이 있으면 결과 줄 하나 더 `--fs-sm --muted`) → 1차는 `createRevisionAction({ projectId, fromRevisionId })`를 부른다 — `fromRevisionId`는 page가 넘긴 **지금 보고 있는 현재 차수 id**다(CEO 리뷰 B-02). 액션이 진행 중이면 1차를 `disabled`로 두어 두 번째 누름이 요청을 만들지 않는다(next-safe-action의 진행 상태). 서버 거부는 1차 옆 막힘 자리(게이트·동시 생성 문자열 그대로, 모달 유지, 토스트 없음) → 성공은 토스트 `새 차수 · 상세 견적 {n+1}차` + 새로 고침으로 새 현재 차수 표.

② **미저장 편집 차단(CEO 리뷰 B-03).** 표에 저장 안 한 편집이 있으면(`QuoteLedger`의 `dirtyCount` ≥ 1 — 1차 「일괄 저장 N」의 N과 같은 값) 모달 1차를 `disabled`로 렌더하고 그 옆 막힘 자리에 `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`(§7-1 비활성 + 이유). 저장해 N이 0이 되면 켜진다. 그래서 `quote-table.tsx`의 머리 줄 버튼 자리는 컴포넌트가 아니라 `{ dirtyCount }`를 받는 렌더 함수 prop 하나로 더한다 — 모달이 표의 dirty 상태를 읽을 다른 경로를 만들지 않는다. 옛 계획의 「보관 키가 달라져 섞이지 않는다 — 확인만」은 입력이 조용히 사라지는 결과라 이 차단으로 대체한다.

③ `[id]/page.tsx`가 `canCreateRevision`·현재 차수 id·복사 대상 줄 수·조정 줄 수를 넘긴다.

④ **E2E(트레이서 증명).** `test/e2e/quote-revisions.spec.ts` 첫 케이스: 담당 PM이 줄 둘인 수주중 프로젝트에서 「복사해 새 차수」 → 모달 본문 `상세 견적 1차의 2줄을 복사해 2차를 만듭니다 · 차수는 되돌릴 수 없습니다` → 1차를 **두 번 빠르게** 누른다(`dblclick`) → 토스트 한 번 · 부제 `상세 견적 2차` · 표에 줄 둘 · 차수 섹션(Task 3 전에는 DB 조회)에 차수가 정확히 둘(B-02). 둘째 케이스: 셀 하나를 고친 채 모달을 열면 1차가 비활성이고 `저장 안 한 편집 1칸 · 먼저 저장해 주세요`가 보인다 → 모달을 닫고 Ctrl+S → 다시 열면 1차가 켜져 있다(B-03). 같은 스펙에서 정산 프로젝트(준비에서 DB로)에는 「복사해 새 차수」가 DOM에 없다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 새 차수 모달이 토스트·부제·표로 이어지지 않았거나, 더블클릭이 차수를 둘 만들었거나, 미저장 편집이 있는데 1차가 켜져 있거나, 정산에서 버튼이 보이거나 기존 표 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 「복사해 새 차수」 렌더 조건이 서버의 `canCreateRevision` 하나다(화면이 상태 문자열을 보지 않는다)
    - 모달이 `fromRevisionId`를 싣고 진행 중 1차가 비활성이다 — E2E 더블클릭 뒤 차수가 정확히 하나 늘었다(B-02)
    - `dirtyCount` ≥ 1이면 1차 비활성 + `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`, 저장 뒤 켜진다(E2E, B-03)
    - 모달 `{k}`가 조정 줄을 세지 않고, 거부 때 게이트 문자열이 그대로 막힘 자리에 나온다
    - E2E가 새 차수 생성(토스트·부제·줄 둘)과 정산의 버튼 부재를 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>담당 PM이 상세에서 새 차수를 만드는 한 경로가 버튼·모달·액션·토스트·부제·E2E까지 끝났고, 더블클릭과 미저장 편집이 되돌릴 수 없는 차수나 입력 손실을 만들지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 고객 승인 표시·취소 모달(기준값 싣기 · 미저장 편집 차단 — ENG-D9) + 부제의 승인일·표시 번호 + 승인 차수 견적 칸 잠금의 표시(사용자 D7 · ENG-D7)</name>
  <files>app/(app)/projects/[id]/revision-dialogs.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1091–1094행(고객 승인 버튼 · 상태별) · 370행(Destructive — 고객 승인 표시 취소) · 336–339행(셀 잠김 이유 줄 형식)
    - `domain/quotes/revisions.ts`의 `setCustomerApproval`·`listRevisionSummaries`(04-14·04-40 — 현재 차수 행의 합계·`contentToken`) · `app/(app)/projects/actions.ts`의 `setCustomerApprovalAction`(04-14 — `approval` = `{ approvedOn, seenTotalKrw, contentToken }` 또는 null)
    - `docs/designs/plant8-erp-phase4-eng-review-260923.md` R3·R6·R8(ENG-D4·D7·D9 — Grep `### R8`)
    - `domain/quotes/edit-scope.ts`의 `lineCellEditability`·`tableLockLine`의 `approvedSeq` 축과 새 줄 견적 칸 잠금 기본값(04-40)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `tableLockLine`·새 줄 기본 셀 단계 계산 자리를 찾아 범위만 Read
    - `ui/form/Form.tsx` — 모달 안 날짜 칸(`Form.Field`)
    - `lib/kst-date.ts`(04-29 — `kstToday`) — 승인일 기본값 오늘(KST)
  </read_first>
  <behavior>
    - 담당 PM + 진행 + 현재 차수 미승인 → 「고객 승인 표시」 · 승인됐고 연결 문서 없음 → 「승인 표시 취소」 · 담당이 아닌 사람 → 둘 다 없음 · 완료 → 둘 다 없음(승인일은 부제에 보임) · 정산 → 담당 PM에게 렌더(사용자 D19-9)
    - 승인 표시 모달의 날짜 칸 기본값이 오늘(KST)이고, 서버가 미래 날짜를 거부하면 이유가 1차 옆에 그대로 보이고 모달이 열린 채다
    - 셀 하나를 고친 채(N = 1) 「고객 승인 표시」 모달을 열면 1차가 비활성이고 `저장 안 한 편집 1칸 · 먼저 저장해 주세요`가 보인다 → 모달을 닫고 Ctrl+S → 다시 열면 1차가 켜져 있다(ENG-D9)
    - 상세를 연 뒤 테스트가 다른 사용자로 같은 차수의 수량을 서버에서 바꿔 저장(도메인 호출) → 새로 고치지 않은 화면에서 승인 표시 → 1차 옆 `견적이 바뀌었습니다 · 새로 고쳐 주세요`, 모달 유지, 부제에 승인일 없음 → 새로 고친 뒤 다시 승인 → 통과(ENG-D9 — 옛 기준값)
    - 견적 줄 0개인 새 프로젝트에서 승인 표시 → 1차 옆 `승인할 견적 줄이 없음 · 첫 줄 만들기`(ENG-D4)
    - 승인 뒤 표: 수량 셀에서 Enter → 편집에 들어가지 않고 이유 `{n}차 고객 승인됨 · 고치려면 새 차수` · 소분류 셀도 같다(ENG-D7) · 표 위 한 줄도 같은 글자 · 실행가 셀은 편집·저장된다 · 「줄 추가」로 만든 새 줄의 수량·단가가 잠겨 있다 · 승인 표시 취소 뒤 수량·소분류가 다시 편집된다
  </behavior>
  <action>
① E2E 케이스를 먼저 쓴다(RED): 담당 PM이 오늘 날짜로 승인 표시 → 부제에 `고객 승인 {오늘} {이름}` → 승인 차수 표(`behavior` 마지막 줄) → 「승인 표시 취소」 모달의 2차 라벨 `닫기 Esc` → 취소 → 부제에서 사라지고 수량이 다시 편집된다 · 완료 프로젝트에는 두 버튼이 없고 승인일이 보인다 · 팀장(담당 아님)에게는 두 버튼이 없다 · **미저장 편집 차단 · 옛 기준값 거부 · 빈 차수 거부(`behavior` 셋째~다섯째 줄 — ENG-D9·D4)**. 옛 기준값 케이스의 「다른 사용자의 저장」은 테스트 준비 코드가 도메인 `saveQuoteLines`를 직접 불러 만든다(두 번째 브라우저를 띄우지 않는다).

② `revision-dialogs.tsx`에 `ApprovalDialog`(제목 `고객 승인 표시` · 본문 `{n}차 · 고객이 승인한 날` + `Form.Field` 날짜 칸 기본 오늘 KST · 1차 `고객 승인 표시` · 2차 `취소 Esc`, 진행 중 1차 비활성)와 `ApprovalCancelDialog`(Copywriting 그대로, 2차 `닫기 Esc`) — 둘 다 `setCustomerApprovalAction`을 부르고 거부 문자열을 그대로 쓴다. **기준값(ENG-D9):** `ApprovalDialog`는 page가 넘긴 현재 차수 요약 행의 `seenTotalKrw`(요약 DTO의 견적 합계 그대로)와 `contentToken`을 `approval`에 싣는다 — 화면이 합계를 다시 더하지 않는다. **미저장 편집 차단(ENG-D9 — B-03과 같은 결):** 두 승인 버튼도 Task 1 ②의 머리 줄 렌더 함수 prop(`{ dirtyCount }`)으로 그려 `dirtyCount` ≥ 1이면 `ApprovalDialog`의 1차를 `disabled`로 두고 막힘 자리에 `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`(새 차수 모달과 같은 글자 · 같은 차단 함수 하나 — 두 번째 차단 구현을 만들지 않는다). 승인 취소는 기준값·차단 없이 그대로다. `[id]/page.tsx`: 서버가 계산한 「승인 버튼을 볼 사람」(담당 PM + `projects` 쓰기 — CEO 리뷰 B-30) 이고 상태가 완료가 아니며 **현재 차수 요약 행에 합계 키가 있으면**(ENG-D9 — 견적 금액을 볼 수 없으면 확인할 합계가 없다) 부제 옆 3차 — 미승인이면 「고객 승인 표시」, 승인됐고 연결 문서가 없으면 「승인 표시 취소」(있으면 버튼 없음 — 판정 입력은 04-14의 서버 결과). 빈 차수(ENG-D4)와 옛 기준값 거부는 서버 문자열 그대로 막힘 자리에 — 화면이 줄 수·기준값을 따로 판정하지 않는다. 부제에 `고객 승인 {날짜} {승인자 이름}`(승인일은 완료에서도 보인다 — 날짜 글자는 서버가 `kstDateOf`로 만든 문자열 그대로, 화면이 `Date`로 바꾸지 않는다 — B-25)과 04-14의 `quoteDisplayNumber` 결과(GREEN).

③ **승인 차수 잠금의 표시(사용자 D7 · OV-1 · ENG-D7).** `[id]/page.tsx`가 현재 차수의 승인 순번(`approvedSeq`)을 `tableLockLine`과 표에 넘기고, `quote-table.tsx`는 새 줄 기본 셀 단계를 만드는 자리(04-12·04-30의 클라이언트 기본값 — 서버와 같은 `lineCellEditability`)에 `approvedSeq`를 실어 새 줄의 수량·단가를 잠근 채 만든다(새 줄의 소분류는 04-40 판정대로 편집). 기존 줄의 잠긴 칸(수량·단가·상태·소분류)은 서버 DTO `cellEditability`(04-40)에서 이미 오므로 화면이 승인 여부로 칸을 추론하는 분기를 만들지 않는다. 잠긴 칸의 편집 시도 이유는 04-04·04-12의 이유 줄 경로 그대로다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts test/e2e/quote-edit-scope.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 승인 표시·취소가 부제로 이어지지 않거나, 완료·비담당에게 버튼이 보이거나, 승인 차수에서 수량·소분류가 편집되거나 실행가가 막히거나, 미저장 편집 중 승인 1차가 켜져 있거나, 옛 기준값·빈 차수 승인이 통과했거나, 기존 셀 단계 표시가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 완료에서 두 버튼이 DOM에 없고 승인일은 부제에 보인다(E2E)
    - 담당이 아닌 사람에게 두 버튼이 없다(E2E)
    - 승인 표시 취소 모달의 2차 라벨이 `닫기 Esc`다
    - 부제의 표시 번호·승인일이 서버 파생값이고 화면에 번호 조립·시간대 계산 코드가 없다
    - 승인 차수에서 수량·소분류 편집이 이유와 함께 막히고 실행가는 저장되며, 새 줄의 수량·단가가 잠긴 채 생긴다 · 승인 취소 뒤 풀린다(E2E, ENG-D7)
    - 승인 모달이 현재 차수 요약 행의 합계·`contentToken`을 싣고(화면 합산 없음), 미저장 편집이 있으면 1차가 비활성 + 이유이며 저장 뒤 켜진다 · 옛 기준값 승인이 `견적이 바뀌었습니다 · 새로 고쳐 주세요`로 막힘 자리에 나오고 부제에 승인일이 생기지 않는다(E2E, ENG-D9)
    - 미저장 편집 차단이 새 차수 모달과 같은 한 함수다(두 번째 차단 구현 없음)
    - 빈 차수 승인 거부 문구가 막힘 자리에 그대로 나온다(E2E, ENG-D4)
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>담당 PM이 현재 차수의 고객 승인을 본 합계 그대로·날짜와 함께 켜고 조건대로 끄며, 미저장 편집·옛 화면·빈 차수에서는 켜지지 않고, 부제가 승인일과 표시 번호를 보이며, 승인 차수의 견적 칸이 잠긴 모양으로 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 차수 섹션(S5) + 이전 차수 읽기 표 + Copywriting 두 줄 + 화면 검증</name>
  <files>app/(app)/projects/[id]/revision-section.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-revisions.spec.ts, .planning/phases/04-project-quote-ledger/04-UI-SPEC.md</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1154–1194행(S5 전문 — 이력 목록을 쓰지 않는 네 근거 · 열 · P1/P2 · 상태 낱말 · 차수 열기)
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` U-2 행 — 사용자 확정 값(최신+승인 = `승인`만, 미승인 이전 = 빈 칸)
    - `ui/table/Table.tsx`·`ui/table/types.ts` — 읽기 형태 자동 판정(편집 셀 0) · `summary`·열 우선순위
    - `ui/history-list/HistoryList.tsx` — **쓰지 않는다는 판단을 확인만** 한다
    - `domain/quotes/revisions.ts`의 `listRevisionSummaries` · `domain/quotes/lines.ts`의 이전 차수 잠김 조회(04-14 산출물)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - 차수 셋(1차 승인 · 2차 미승인 · 3차 미승인 최신) → 섹션 행 순서 3차·2차·1차, 상태 `현재`·빈 칸·`승인`, 3차 행 동작 칸 빈 칸, 2차·1차에 「차수 열기」
    - 「차수 열기」로 1차 → 캡션 읽기 표(흰 머리글) · 저장 버튼·구조 컨트롤 없음 · 조정 그룹 없음 · 표 위 `1차 · 읽기 전용` + 현재 차수로 돌아가는 3차 링크
  </behavior>
  <action>
① `app/(app)/projects/[id]/revision-section.tsx`: `ui/table`을 쓴다 — 편집 셀이 없으니 자동으로 캡션 있는 읽기 표(모드 prop을 만들지 않는다). 섹션 제목 `차수`(2px `--line-strong`으로 시작하는 §6-2 섹션), 열 여섯(차수 `{n}차` · 생성일 · 줄 수 · 견적 합계 · 상태(§7-5 `text`: `현재` accent · `승인` success) · 동작), 최신이 위, 폰 P1 = 차수·견적 합계·동작, P2 접힌 줄 = 상태·생성일·줄 수(빈 값 자리 건너뜀). 이전 차수 행에만 3차 「차수 열기」(`?revision={n}` 링크 — 이동이라 `<a>`). 현재 행의 동작 칸과 미승인 이전 차수의 상태 칸은 **빈 칸**이다(`—` 금지). 섹션 단독 오류 자리를 만들지 않는다. `ui/history-list`를 쓰지도 고치지도 않는다. 행 데이터는 04-14의 `listRevisionSummaries`.

② **이전 차수 읽기 표.** `[id]/page.tsx`가 `revision` 검색 파라미터(현재보다 작은 순번)를 받으면 04-14의 잠김 조회로 그 차수의 견적 줄을 `QuoteLedger`에 **잠김 모드로** 넘긴다 — 모든 셀 `locked`(서버가 계산), 조정 그룹 없음(조정 줄은 현재 차수에 산다), 1차 저장·구조 컨트롤·미저장 복원 없음, 표 위에 `{n}차 · 읽기 전용` 한 줄과 현재 차수로 돌아가는 3차 링크, 0줄이면 `이 차수에 견적 줄이 없습니다`(버튼 없음). `quote-table.tsx`는 잠김 모드에서 미저장 복원·구조 컨트롤을 렌더하지 않는 분기 하나만 더한다. 되돌리기 동작을 만들지 않는다.

③ **Copywriting 여덟 줄.** `04-UI-SPEC.md` Copywriting Contract 표에 04-14가 정한 두 행(「확인 — 고객 승인 표시」 · 「막힘 — 새 차수(빈 차수)」)을 더하고 행 끝에 `(04-14 계획 단계 판단)`을 붙인다 — 04-04 U-6 선례. CEO 리뷰가 만든 네 행을 같은 표에 더하고 행 끝에 `(CEO 리뷰 2026-09-23)`을 붙인다: 「막힘 — 새 차수·고객 승인(미저장 편집)」 `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`(B-03 · ENG-D9 — 두 모달이 같은 글자) · 「막힘 — 새 차수(동시·오래된 화면)」 `다른 사람이 먼저 새 차수를 만들었습니다 · 새로 고쳐 주세요`(B-02) · 「Error — 저장(현재 차수 아님)」 `다른 사람이 새 차수를 만들었습니다 · 새로 고쳐 주세요`(B-01, 총계 행) · 「Error — 셀(잠김, 고객 승인 차수)」 `{n}차 고객 승인됨 · 고치려면 새 차수`(사용자 D7 · ENG-D7 — 수량·단가·상태·소분류, 표 위 한 줄과 잠긴 셀 이유 줄에 같은 글자). 엔지니어링 리뷰가 만든 두 행을 더하고 행 끝에 `(엔지니어링 리뷰 2026-09-23)`을 붙인다: 「막힘 — 고객 승인(빈 차수)」 `승인할 견적 줄이 없음 · 첫 줄 만들기`(ENG-D4) · 「막힘 — 고객 승인(견적 바뀜)」 `견적이 바뀌었습니다 · 새로 고쳐 주세요`(ENG-D9). 다른 행은 건드리지 않는다.

④ E2E(`behavior` 두 줄 + 375에서 섹션 가로 스크롤 0).

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S5 backstop 둘(섹션 오류 자리 없음 · 375 빈 칸 유지)을 DOM 실측 → 필요하면 `project-detail.module.css`만 고친다 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>git diff --stat d6b41cf -- ui/history-list</automated>
    <fails_when>출력이 비어 있지 않다 — 이력 목록 컴포넌트를 고쳤다(S5의 네 근거)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-revisions.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 섹션 순서·상태 낱말·빈 칸·이전 차수 잠김 보기 중 하나가 계약과 다르다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S5 판정 두 개를 SUMMARY에서 확인</test>
      <expected>차수 섹션에 단독 오류 자리가 없고, 375에서 가로 스크롤 0 · 현재 행 동작 칸이 빈 칸</expected>
      <why_human>렌더 배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 차수 섹션이 모드 prop 없이 캡션 읽기 표이고 `ui/history-list` diff가 0줄이다
    - 상태 칸·현재 행 동작 칸이 U-2 확정값대로 렌더된다(빈 칸 — `—` 없음)
    - 이전 차수 보기에 저장·구조 컨트롤·조정 그룹이 없다(E2E)
    - UI-SPEC Copywriting에 여덟 행(04-14 판단 둘 + CEO 리뷰 넷 + 엔지니어링 리뷰 둘)이 더해지고 다른 행 diff가 0줄이다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>차수 섹션이 차수의 변천을 읽기 표로 보이고, 지난 차수를 잠긴 표로 열어 보며, Copywriting이 계획 단계 문구 두 줄을 갖는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 판정 → 머리 줄 버튼 | 새 차수·승인 버튼의 렌더 조건이 서버에서 온다. 화면이 담당 PM·상태를 추론하면 서버 규칙과 어긋난다 |
| 검색 파라미터 `revision` → 이전 차수 보기 | 순번이 URL로 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-68 | Elevation of Privilege | 고객 승인 버튼(화면 쪽) | high | mitigate | 버튼 렌더는 서버가 계산한 담당 PM 여부(쓰기 권한 포함 — B-30)·상태·연결 문서 결과만 본다. 비담당·완료에서 버튼 부재를 E2E로 고정하고, 서버 거부는 04-14 통합 테스트가 본다 |
| T-04-70b | Tampering | 새 차수 중복 요청(화면 쪽) | medium | mitigate | 모달이 보던 차수 id를 싣고 진행 중 1차를 끈다 — 더블클릭 E2E가 차수 하나만 늘었음을 단언한다(B-02). 서버 판정은 04-14·04-40 |
| T-04-70c | Denial of Service | 미저장 입력 손실(UX-04) | medium | mitigate | 미저장 편집이 있으면 새 차수 모달 1차가 이유와 함께 비활성이다 — 보관 키가 바뀌어 입력이 조용히 사라지는 길이 없다(B-03, E2E) |
| T-04-70d | Tampering | 본 적 없는 금액의 승인(화면 쪽) | high | mitigate | 승인 모달이 보고 있는 현재 차수의 합계·내용 토큰을 싣고, 미저장 편집이 있으면 1차를 끈다 — 옛 기준값은 서버가 거부하고 그 문구가 막힘 자리에 나온다(ENG-D9 · Codex #2, E2E). 서버 판정·경합은 04-14·04-40 |
| T-04-124b | Tampering | 승인 차수 견적 칸(화면 쪽) | medium | mitigate | 잠긴 칸(수량·단가·상태·소분류 — ENG-D7)은 서버 DTO와 같은 판정 함수의 결과로만 그린다 — 화면이 풀어 둔 칸을 보내도 04-40의 게이트가 거부한다 |
| T-04-71 | Elevation of Privilege | 이전 차수 보기(화면 쪽) | high | mitigate | 이전 차수 보기는 서버가 전부 `locked`로 계산한 잠김 조회이고 저장·구조 컨트롤이 없다 — 이전 승인 차수를 현재처럼 편집·사용하는 화면 경로가 없다(04-14 금지 항목의 화면 쪽). `revision` 파라미터는 현재보다 작은 정수 순번만 받고 그 밖은 현재 차수 보기로 떨어진다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `ui/history-list` diff 0줄, `package.json`·`pnpm-lock.yaml` diff 0줄
- 독립 DOM 감사(S5 둘) 판정이 SUMMARY에 있다
</verification>

<success_criteria>
- 담당 PM이 상세에서 새 차수를 만들고 고객 승인을 날짜와 함께 켜고 조건대로 끈다
- 차수 섹션이 변천을 읽기 표로 보이고 지난 차수를 잠긴 표로 연다
- 완료·비담당·정산의 버튼 부재가 E2E로 고정됐다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컴포넌트(신규) | `app/(app)/projects/[id]/revision-section.tsx` · `app/(app)/projects/[id]/revision-dialogs.tsx`(`NewRevisionDialog` · `ApprovalDialog` · `ApprovalCancelDialog`) |
| 변경된 기존 심볼 | `QuoteLedger`(차수·승인 버튼 자리 — `{ dirtyCount }` 렌더 함수 prop · 잠김 보기 모드 · 새 줄 기본값의 승인 차수 잠금) · 상세 부제(고객 승인일 · 표시 번호) · 상세 섹션 순서(차수 섹션) · 표 위 한 줄(승인 차수 문구) |
| 모달 입력 | `ApprovalDialog` → `setCustomerApprovalAction({ revisionId, approval: { approvedOn, seenTotalKrw, contentToken } })`(ENG-D9 — 현재 차수 요약 행 값) · 미저장 편집 차단(새 차수와 같은 함수) |
| 문서 | `04-UI-SPEC.md` Copywriting 여덟 행(확인 — 고객 승인 표시 · 막힘 — 새 차수(빈 차수) · 막힘 — 새 차수·고객 승인(미저장 편집) · 막힘 — 새 차수(동시·오래된 화면) · Error — 저장(현재 차수 아님) · Error — 셀(잠김, 고객 승인 차수) · 막힘 — 고객 승인(빈 차수) · 막힘 — 고객 승인(견적 바뀜)) |
| 테스트 | `test/e2e/quote-revisions.spec.ts`(신규 — 더블클릭 · 미저장 차단(새 차수·승인) · 옛 기준값 승인 거부 · 빈 차수 승인 거부 · 승인 차수 잠금(소분류 포함)) |
| 새 스키마·마이그레이션·액션 | 없음 — 서버 쪽은 04-14·04-40 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-02 (T3) — 새 차수 더블클릭·오래된 탭, 대기 중 버튼 상태 미정 | CRITICAL | Plan 04-24, Task 1 ①④: `fromRevisionId` 싣기 + 진행 중 1차 비활성 + 더블클릭 E2E(차수 하나) |
| B-03 (T3) — 미저장 편집이 새 차수로 조용히 사라짐(UX-04) | CRITICAL | Plan 04-24, Task 1 ②④: `dirtyCount` ≥ 1이면 1차 비활성 + `저장 안 한 편집 {N}칸 · 먼저 저장해 주세요`, E2E · Task 3 ③ Copywriting |
| 사용자 D7 · OV-1 (T4) — 승인 차수 견적 칸 잠금의 화면 | HIGH | Plan 04-24, Task 2 ③: `approvedSeq`를 표 위 한 줄·새 줄 기본값에 싣기, E2E |
| B-25 — 승인일 하루 어긋남(화면 쪽) | WARNING | Plan 04-24, Task 2 ②: 서버 날짜 문자열 그대로, 화면 시간대 계산 금지 |
| B-30 — 승인 버튼 렌더 조건(쓰기 권한) | NOTE | Plan 04-24, Task 2 ②: 서버 판정 결과로만 렌더 |
| 사용자 D19-9 — 상태별 고객 승인 기본값 확정 | DECISION | Plan 04-24, `<probe_fallback>`·Task 2 behavior: 정산 렌더·완료 부재 |
| 사용자 D10 — 정산의 새 차수 없음 | DECISION | Plan 04-24, truths: 「복사해 새 차수」 정산 미렌더 유지 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-D9 · Codex #2 (R8) — 승인이 본 내용을 가리키지 않음(화면) · 미저장 편집 중 승인 | DECISION(P1) | Plan 04-24, Task 2 ②·behavior: 요약 행 합계·토큰 싣기 · 새 차수와 같은 미저장 편집 차단 · 옛 기준값 거부 E2E · Task 3 ③ Copywriting |
| ENG-D4 (R3) — 빈 차수 승인 거부(화면) | DECISION | Plan 04-24, Task 2: 서버 문구를 막힘 자리에 · E2E · Task 3 ③ Copywriting |
| ENG-D7 (R6) — 승인 차수 소분류 잠금(화면) | DECISION | Plan 04-24, truths · Task 2 ③: DTO 셀 단계 그대로 · E2E |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 다른 사람이 만든 새 차수로 이 탭의 저장이 거부된 뒤 옛 보관 키의 복원이 뜨지 않음 | 리뷰가 다루지 않은 잔여 경로 — 거부 문구가 「새로 고쳐 주세요」를 말한다. 새 동작을 만들지 않고 SUMMARY에 사실만 적는다(HOLD SCOPE) |


<output>
Create `.planning/phases/04-project-quote-ledger/04-24-SUMMARY.md` when done
</output>
