---
phase: 04-project-quote-ledger
plan: 25
type: execute
wave: 6
depends_on: ["04-10"]
files_modified:
  - ui/select/Select.tsx
  - ui/select/Select.module.css
  - domain/projects/references.ts
  - app/(app)/admin/vendors/vendor-form.tsx
  - app/(app)/admin/code-tables/code-tables.module.css
  - test/e2e/vendor-edit.spec.ts
  - test/e2e/mobile-code-tables.spec.ts
autonomous: true
requirements: [UX-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "코드표 값을 고르는 `Select`는 고른 값의 설명을 컨트롤 바로 아래 힌트 한 줄로 보이고, 설명이 없는 값을 고르면 힌트 줄 자체가 없다 — `—` 힌트를 띄우지 않는다(S14 partial, SYSTEM.md 개정 ⑥, D-93)"
    - "같은 자리에 오류 이유가 있으면 오류가 힌트를 이긴다(개정 ⑥)"
    - "네이티브 `<option>` 안에 설명을 넣지 않고 툴팁·커스텀 드롭다운을 만들지 않는다(§7-2, S14)"
    - "거래처 편집 폼의 기본 증빙 종류 칸이 고른 증빙 종류의 설명 한 줄을 보인다 — 고르는 자리의 첫 사용처(D-93)"
    - "견적 소분류 옵션이 코드표 설명을 싣고 온다 — 견적 줄 표 셀에 연결하는 일은 표 파일을 고치는 04-23의 몫이다(D-93)"
    - "폰(<700)의 코드표 화면에서 설명은 P2 접힌 줄에 한 번만 보이고 값·정렬·동작 열이 숨으며, 표의 설명 열은 `keep-all` 줄바꿈이다(S14 overflow 사용자 확정)"
    - statement: "설명 40자가 PC 긴 칸 폭 안에서 한 줄에 들어가고 폰에서는 말줄임 없이 줄바꿈하며, 코드표 표에서는 keep-all 줄바꿈으로 전문이 보인다 — DOM 감사"
      verification: backstop
    - statement: "폰 375에서 코드표 설명이 P2 접힌 줄에 보이고 값·정렬·동작 열이 숨으며 같은 설명이 두 번 보이지 않는다, 가로 스크롤 0 — DOM 감사"
      verification: backstop
    - statement: "설명 0개(전부 `—`)·1개·전부인 세 경우에서 설명 열 폭과 줄 높이가 흔들리지 않는다 — 렌더 감사"
      verification: backstop
  artifacts:
    - path: "ui/select/Select.tsx"
      provides: "옵션 설명이 있으면 고른 값의 설명을 컨트롤 아래 힌트 한 줄로"
      contains: "description"
    - path: "test/e2e/vendor-edit.spec.ts"
      provides: "거래처 기본 증빙 종류의 설명 힌트 표시·부재"
      min_lines: 20
  key_links:
    - from: "domain/projects/references.ts"
      to: "ui/select/Select.tsx"
      via: "견적 소분류 옵션이 설명을 싣고 와 편집 중 셀 아래 힌트가 된다(04-23이 표에 연결)"
      pattern: "description"
    - from: "app/(app)/admin/vendors/vendor-form.tsx"
      to: "domain/code-tables/index.ts"
      via: "증빙 종류 목록이 04-10의 `CodeItemDto.description`을 싣고 온다 — 화면이 설명을 따로 들고 있지 않다"
      pattern: "description"
---

<objective>
**As a** 코드표 값을 고르는 기획 PM·관리자, **I want to** 「기타소득」·「무대·시공」 같은 값을 고를 때 그 뜻을 한 줄로 보고, 폰에서도 코드표 설명이 읽히기를, **so that** 비슷한 값 사이에서 잘못 골라 세금 계산·견적 분류가 틀어지는 일이 준다.

**2026-09-23 체커 1차 분할.** 옛 04-10(20파일)을 둘로 나눈 **고르는 자리와 폰 배치** 쪽이다(D-93). 04-10이 `code_items.description` 컬럼 · 마이그레이션 · 관리 화면 인라인 편집 · 시드 설명 · 코드 추가 폼 설명 칸을 끝냈다. 이 플랜은 공용 `Select`의 설명 힌트와 그 첫 사용처(거래처 기본 증빙 종류), 견적 소분류 옵션의 설명 싣기, 코드표 화면의 폰 P2 배치를 맡고 S14 backstop 셋을 DOM 감사로 판정한다. 고르는 자리 넷 중 나머지는 각 화면 플랜이 이 `Select`를 쓰며 붙인다 — 견적 소분류 셀(04-23) · 상태 고르기 목록(04-21, 코드표 목록을 직접 씀) · 리저브 증빙 종류(04-07).

Purpose: 04-23(견적 소분류 셀 힌트)과 04-07(리저브 증빙 종류 설명 줄)이 이 플랜의 `SelectOption.description`을 쓴다.

Output: `Select` 설명 힌트 · 소분류 옵션 설명 · 거래처 폼 힌트 · 코드표 폰 P2 CSS · E2E 2 확장 · S14 DOM 감사.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-10-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 535–545행(S14 상태) · 1366–1384행(S14 적용 규칙 — 고르는 자리 표)
# docs/design/SYSTEM.md §7-15 Select · §7-3 코드표 셀 · §7-2(04-08 개정 ⑥ 반영본)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-04(잘못된 입력은 즉시 안내 — 코드표 설명의 고르는 자리·폰 배치)** 다. D-93은 요구사항 ID가 따로 없는 전역 결정이라 가장 가까운 UX-04 아래 둔다(04-10과 같은 결). UI-SPEC `## UI Considerations` S14의 여덟 항목 중 backstop 셋(long-text · overflow · zero-one-many)은 이 플랜이 flat-scalar 마커로 옮겼고(DOM 감사가 여기 있으므로), partial은 truths로, empty·error·populated와 40자 금지 항목은 서버·관리 화면을 만든 04-10에 남았다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 관리자가 거래처 편집에서 기본 증빙 종류를 고르면 그 설명이 한 줄로 보인다(옵션 설명 → Select 힌트 → 거래처 폼 → E2E)</name>
  <files>ui/select/Select.tsx, ui/select/Select.module.css, app/(app)/admin/vendors/vendor-form.tsx, test/e2e/vendor-edit.spec.ts</files>
  <read_first>
    - `ui/select/Select.tsx`(전문) · `ui/select/Select.module.css` — 옵션 타입·`error` 표시·첫 옵션 `—`
    - `ui/form/Form.tsx` — `Form.Hint`의 마크업·클래스(같은 모양을 쓴다)
    - `docs/design/SYSTEM.md` §7-15 Select · §7-3 코드표 셀 설명 줄(04-08 개정 ⑥)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1374–1384행(고르는 자리 표 — 폼 Select · 편집 표 셀 · 상태 목록 · 필터 줄 제외 · 폰 시트 제외)
    - `app/(app)/admin/vendors/vendor-form.tsx` 55–160행(기본 증빙 종류 select) · 그 화면이 증빙 종류 목록을 받는 경로(`app/(app)/admin/vendors/page.tsx` — 04-10 이후 `CodeItemDto.description`이 이미 실려 있는지 확인만)
    - `test/e2e/vendor-edit.spec.ts`(전문)
  </read_first>
  <behavior>
    - 설명이 있는 옵션을 고르면 컨트롤 바로 아래 힌트 한 줄에 그 설명이 보이고, 다른 값으로 바꾸면 즉시 바뀐다
    - 설명이 없는 옵션을 고르면 힌트 요소가 DOM에 없다
    - `error`가 있으면 힌트 대신 오류 한 줄만 보인다
    - `—`(빈 값)를 고르면 힌트가 없다
  </behavior>
  <action>
① E2E 케이스를 먼저 쓴다(RED): `test/e2e/vendor-edit.spec.ts`에 관리자가 거래처 편집에서 기본 증빙 종류를 「기타소득」으로 바꾸면 칸 아래에 그 설명 한 줄이 보이고, 설명이 없는 값으로 바꾸면 힌트 줄이 사라진다.

② `ui/select/Select.tsx`: `SelectOption`에 선택 필드 `description?: string | null`을 더한다. 옵션 중 하나라도 설명이 있으면 컴포넌트가 고른 값을 로컬 상태로 따라가며(제어·비제어 둘 다 — 기존 `value`/`defaultValue`/`onChange`를 그대로 넘긴다) 컨트롤 바로 아래에 `Form.Hint`와 같은 모양(`--fs-sm --muted`)의 한 줄을 렌더한다. 설명이 없거나 `error`가 있으면 힌트를 렌더하지 않는다(오류가 이긴다). 힌트 요소를 `aria-describedby`로 잇는다(오류가 있으면 오류만). 이 동작 때문에 클라이언트 컴포넌트 선언이 필요하면 더한다(지금 사용처 셋이 전부 클라이언트 컴포넌트다). **`<option>` 안에 설명을 넣지 않는다.** `Select.module.css`는 토큰만(D-20).

③ `app/(app)/admin/vendors/vendor-form.tsx`의 기본 증빙 종류 칸이 증빙 종류 설명을 받아 같은 힌트 한 줄을 보이게 한다 — 이 칸이 네이티브 `<select>`를 직접 쓰면 공용 `Select`로 바꾸지 말고(관리자 폼 이관은 Phase 7, A-H2) 같은 모양의 힌트 한 줄만 옆에 붙인다(GREEN).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/vendor-edit.spec.ts test/e2e/project-register.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 힌트가 안 보이거나, `Select`를 쓰는 기존 화면(등록 폼·매출)이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `SelectOption`에 `description`이 있고, 설명 없는 값·빈 값·오류가 있을 때 힌트 요소가 DOM에 없다(E2E가 부재를 단언)
    - `<option>` 텍스트에 설명이 들어가지 않는다
    - 거래처 폼 기본 증빙 종류 칸에 힌트 한 줄이 나오는 E2E가 초록이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>코드표 값을 고르는 공용 컨트롤이 고른 값의 설명을 한 줄로 보이고, 첫 사용처(거래처 기본 증빙 종류)가 동작한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 견적 소분류 옵션 설명 + 코드표 화면 폰 P2 배치 + 화면 검증(S14 backstop 셋)</name>
  <files>domain/projects/references.ts, app/(app)/admin/code-tables/code-tables.module.css, test/e2e/mobile-code-tables.spec.ts</files>
  <read_first>
    - `domain/projects/references.ts`(전문 64행) — 소분류 옵션 조회
    - `app/(app)/admin/code-tables/code-tables.module.css`(전문) — 표 열 우선순위·폰 접힌 줄 스타일
    - `app/(app)/admin/code-tables/page.tsx`(04-10 산출물 — 설명 열 여섯 번째 칸)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 540–545행(S14 long-text·partial·overflow·zero-one-many — 폰 P2 사용자 확정)
    - `test/e2e/mobile-code-tables.spec.ts`(전문) — 폰 코드표 스펙 형태
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - `listProjectFormReferences`의 소분류 옵션이 `description`을 싣는다(설명 없는 값은 null)
    - 폰 375 코드표에서 설명이 접힌 줄에 한 번만 보이고 값·정렬·동작 열이 숨으며 가로 스크롤이 0이다
  </behavior>
  <action>
① E2E 케이스를 먼저 쓴다(RED): `test/e2e/mobile-code-tables.spec.ts`에 폰 375에서 설명이 접힌 줄에 한 번만 보이고 가로 스크롤이 0인 케이스.

② `domain/projects/references.ts`의 소분류 옵션이 `description`을 싣게 한다(코드표 DTO에서 온다). 견적 줄 표 셀에 실제로 연결하는 일은 표 파일을 고치는 04-23의 몫이다 — 이 플랜은 `quote-table.tsx`를 건드리지 않는다.

③ `code-tables.module.css`: 설명 열 `word-break: keep-all` 줄바꿈 · 폰(&lt;700)에서 설명은 **P2 접힌 줄**에 보이고 값·정렬·동작 열이 P3로 숨는다(S14 overflow 사용자 확정) — 같은 설명을 두 번 렌더하지 않는다. 토큰만 쓴다(GREEN).

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 코드표 화면·거래처 폼의 S14 backstop 셋(40자 한 줄/폰 줄바꿈 · 폰 P2 한 번 · 0/1/전부 열 폭 안정)을 DOM 실측으로 판정 → 필요하면 이 플랜의 CSS 두 파일만 고친다 → 전체 게이트 한 번(`CI=true pnpm test`). 04-10이 만든 설명 열·인라인 칸도 이 감사가 함께 잰다. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/mobile-code-tables.spec.ts test/e2e/code-tables.spec.ts</automated>
    <fails_when>lint·build error 또는 playwright failed 개수가 1 이상이다 — 폰에서 설명이 두 번 보이거나 가로 스크롤이 생겼거나 04-10의 코드표 흐름이 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S14 backstop 세 판정을 SUMMARY에서 확인</test>
      <expected>40자 설명이 PC 한 줄·폰 줄바꿈, 폰 P2에 한 번, 설명 개수와 무관하게 열 폭 안정</expected>
      <why_human>폭·줄바꿈·배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `references.ts`의 소분류 옵션이 설명을 싣는다
    - 이 플랜의 커밋들(`git log --stat`)에 `quote-table.tsx`가 없다 — SUMMARY에 커밋 해시와 함께 적는다
    - 폰 375에서 설명이 한 번만 보이고 가로 스크롤이 0이다(E2E)
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>뒤 플랜들이 옵션에 설명을 싣기만 하면 되고, 코드표 화면이 폰에서도 설명을 한 번씩 읽히게 보인다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code_items → 고르는 자리 | 관리자가 적은 자유 텍스트가 등록·관리 폼과 표 셀에 그대로 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-55 | Tampering | 설명 텍스트 렌더(고르는 자리) | medium | mitigate | `Select` 힌트와 거래처 폼 힌트는 설명을 React 텍스트 노드로만 렌더한다(HTML 삽입 경로를 만들지 않는다). 40자 서버 상한(04-10)이 긴 페이로드를 막는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 독립 DOM 감사(S14 backstop 셋) 판정이 SUMMARY에 있다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 코드표 값을 고르는 공용 컨트롤이 고른 값의 설명을 한 줄로 보인다
- 견적 소분류 옵션이 설명을 싣고, 코드표 화면이 폰에서 설명을 한 번씩 보인다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컴포넌트 | `Select` 설명 힌트(`SelectOption.description`) |
| 변경된 기존 심볼 | `listProjectFormReferences` 소분류 옵션(+description) · 거래처 폼 기본 증빙 종류 힌트 · 코드표 표 설명 열 `keep-all`·폰 P2 |
| 테스트 | `vendor-edit.spec.ts`·`mobile-code-tables.spec.ts` 확장 |
| 새 스키마·마이그레이션·액션 | 없음 — 컬럼·마이그레이션·액션은 04-10 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-25-SUMMARY.md` when done
</output>
