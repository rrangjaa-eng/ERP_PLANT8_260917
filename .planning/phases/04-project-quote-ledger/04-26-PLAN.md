---
phase: 04-project-quote-ledger
plan: 26
type: execute
wave: 13
depends_on: ["04-30"]
files_modified:
  - domain/settings/keys.ts
  - domain/rules/register.ts
  - domain/quotes/lines.ts
  - repositories/quote-lines.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/integration/quote-line-cap.test.ts
  - test/e2e/quote-edit-scope.spec.ts
autonomous: true
requirements: [UX-04, UX-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "한 차수의 줄 수 상한은 설정 키(기본 300)이고, 상한에 닿으면 「줄 추가」가 비활성 + `줄 추가할 수 없음 — 300줄 상한 · 상한은 관리자 설정`, Ctrl+Enter·Ctrl+D는 줄을 만들지 않고 합계 행에 같은 이유를 적는다(D-86, Copywriting 막힘 — 줄 추가(상한))"
    - "상한을 넘기는 붙여넣기는 한 칸도 바꾸지 않고 전부 거부되며 합계 행에 `붙여넣기 전부 거부 · 300줄 상한을 20줄 넘음 · 줄 수를 줄여 다시 붙여 주세요`가 나온다(D-86)"
    - "클라이언트 판정을 우회해 상한을 넘는 배치를 보내도 서버가 `300줄 상한을 넘어 전부 거부 · 줄을 지워 주세요`로 전부 거부한다 — 상한은 보관된 줄을 빼고 조정·견적 외 비용·취소 줄을 포함해 센다(D-86, S4 — 조정·견적 외 비용 줄은 04-13이 만들며 같은 셈에 들어간다)"
    - "상한은 **줄을 더할 때만** 막는다 — 관리자가 상한을 지금 줄 수보다 낮추면 기존 줄 고치기와 일부 보관은 통과하고, 새 줄·복원만 거부된다(D-86 「추가」 제한, CEO A-20)"
    - "보관함 복원도 한 줄을 더하는 것이라 같은 상한 판정을 지난다(CEO A-19·OV-2 — 04-12의 `restoreQuoteLine`)"
    - "두 사람이 동시에 줄을 더해도 상한을 넘지 않는다 — 줄 수를 프로젝트 행 배타 잠금 안에서 세므로 뒤에 온 저장은 앞 저장이 커밋한 줄까지 세어 판정한다(CEO A-36·OV-3)"
    - "문구 속 숫자 300은 설정 키 값이다 — 관리자가 상한을 바꾸면 문구의 숫자도 바뀐다(S4 300줄 상한)"
  prohibitions:
    - statement: "상한을 넘는 붙여넣기가 견적을 조용히 잘라서는 안 된다. 오른쪽 넘침은 버림 + 경고지만 줄 수 상한은 버리면 견적이 잘리고 합계가 거짓이 된다 — 넘치면 한 칸도 바꾸지 않고 전부 거부하고 이유를 보인다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/settings/keys.ts"
      provides: "차수당 견적 줄 상한 설정 키(단순값, 기본 300)"
      contains: "QUOTE_LINE_MAX_PER_REVISION"
    - path: "test/integration/quote-line-cap.test.ts"
      provides: "상한 서버 거부(클라이언트 우회 흉내) · 보관 줄 제외 계산 · 취소 줄 포함 · 설정 값 변경 반영 · 상한 낮춘 뒤 고치기 통과 · 복원 상한 · 동시 추가 경합"
      min_lines: 60
  key_links:
    - from: "domain/quotes/lines.ts"
      to: "domain/settings/keys.ts"
      via: "상한이 설정 키에서 오고 `quote.line-cap` 게이트가 판정한다"
      pattern: "QUOTE_LINE_MAX_PER_REVISION"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/page.tsx"
      via: "화면 상한 판정과 문구의 숫자가 서버가 읽은 같은 설정 값(`lineCap`)이다"
      pattern: "lineCap"
---

<objective>
**As a** 기획 PM, **I want to** 한 차수에 줄을 무한정 붙여 넣지 못하고, 상한에 닿으면 왜 줄을 더할 수 없는지 바로 보이며, 넘치는 붙여넣기는 견적을 몰래 자르지 않고 통째로 거부되기를, **so that** 견적 합계가 언제나 붙여 넣은 전부를 말하고 한 화면 표가 감당할 수 있는 크기에 머문다.

**2026-09-23 체커 1차 분할.** 옛 04-12(17파일)의 Task 3 — 차수당 줄 수 상한(D-86) — 을 떼어 온 플랜이다. 04-12는 정산의 편집 범위·구조 잠금·줄 보관·취소를 맡고, 이 플랜은 그 위에 상한 설정 키 · 게이트 · 서버 거부 · 「줄 추가」 비활성 · 붙여넣기 전부 거부를 얹는다. 04-13(조정·견적 외 비용 줄)이 이 상한 판정을 같이 쓰므로 04-13보다 먼저 선다.

Purpose: D-86 · D-67 · §7-3의 「조용히 버리지 않는다」. 04-19(견적 줄 30줄 페이지)와 04-15(프로젝트 복사)가 이 상한을 전제로 한 화면·트랜잭션 크기를 잡는다.

Output: 설정 키 `quote_line.max_per_revision` · 게이트 `quote.line-cap` · 활성 줄 수 조회 · 화면 상한(비활성·키 무동작·붙여넣기 전부 거부) · 통합 1 · E2E 확장.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE):** A-17(E2E가 전역 상한을 3으로 바꾸면 병렬 스펙이 흔들린다 → 기본 상한 300에서 SQL로 줄을 채운 준비) · A-20(상한은 추가만 막는다) · A-36·OV-3(동시 추가 — 04-12의 프로젝트 행 배타 잠금 안에서 세고 두 연결 결정적 경합 테스트) · A-19·OV-2(복원도 상한). OV-7: 견적 표 화면을 04-30 뒤에 고친다(웨이브 13). 상세는 끝의 Review Dispositions Ledger.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-30-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 753–761행(§3 D-86 — 상한 키 · 붙여넣기 상한)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1142–1146행(300줄 상한 — 세는 줄·문구) · 348–351행(막힘 문구 넷)
# docs/design/SYSTEM.md §7-3 (자) 줄 수 상한(04-08 개정 ④)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-04(전부 저장 또는 전부 거부 — 상한)** · **UX-05(단축키 — 상한에서의 무동작)** 다. 수용 기준은 D-86과 UI-SPEC S4의 partial(상한)에서 끌어와 `must_haves.truths`에 적었다(옛 04-12에서 그대로). S4의 나머지 상태(empty 변형 · zero-one-many backstop)는 04-12에 남았다.

**계획 단계 가정(표면화):** 세는 줄은 「보관되지 않은 모든 종류의 줄」이다 — 취소 상태 줄도 줄 자리와 이력을 차지하므로 센다(S4). 조정·견적 외 비용 줄은 04-13이 만들고, 그 플랜의 통합 테스트가 조정 줄이 같은 셈에 들어가는지 단언한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 상한 3인 차수에서 줄 추가가 막힌다(설정 키 → 게이트 → 활성 줄 수 → 서버 거부 → 상한 값 전달 → 「줄 추가」 비활성 → E2E)</name>
  <files>domain/settings/keys.ts, domain/rules/register.ts, domain/quotes/lines.ts, repositories/quote-lines.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/integration/quote-line-cap.test.ts, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `domain/settings/keys.ts` 175–190행(`fx.recent_rate.USD` 단순값 키 형태) · 레지스트리 커버리지 테스트(`test/unit/settings/` 아래 — 새 키가 읽히는지 검사하는 테스트가 있으면 그 규약)
    - `domain/settings/registry.ts`의 `getSettingValue`
    - `domain/quotes/lines.ts`의 `saveQuoteLines` 판정 단계(04-12가 넣은 `archivedLineIds`·구조 게이트 위치 — Grep `archivedLineIds`)
    - `repositories/quote-lines.ts`(전문 — 04-12의 `archiveQuoteLines`)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `addLine`·`addLineButton`·`structural`(04-12 판정 · 04-30 배선)을 찾아 범위만 Read
    - `test/integration/settings.test.ts` — 테스트에서 설정 값을 바꾸는 형태
    - `test/e2e/quote-edit-scope.spec.ts`(04-30 산출물) — 준비·로그인 형태
  </read_first>
  <behavior>
    - 상한 3, 기존 줄 3 → 새 줄 1개 저장 시도 → `3줄 상한을 넘어 전부 거부 · 줄을 지워 주세요` · DB 줄 수 3 그대로
    - 상한 3, 기존 줄 3 중 1개 보관 + 새 줄 1개 → 통과(보관 줄은 세지 않는다)
    - 상한 3, 기존 2 + 취소 상태 1 → 새 줄 거부(취소 줄도 센다)
    - 상한 설정을 5로 바꾸면 같은 저장이 통과한다
    - (A-20) 상한 3, 기존 줄 5 → 한 줄의 실행가만 고친 저장 통과 · 한 줄 보관 저장 통과 · 새 줄 1 → 거부
    - (A-19) 상한 3, 활성 줄 3 + 보관 줄 1 → 보관 줄 복원 거부(`3줄 상한을 넘어 …`) · 활성 줄 2이면 복원 통과
  </behavior>
  <action>
① `domain/settings/keys.ts`에 `QUOTE_LINE_MAX_PER_REVISION`(`quote_line.max_per_revision`, 단순값, `z.coerce.number().int().min(1)`, 기본 300, namespace `견적 표`, 라벨 `차수당 견적 줄 상한`, 힌트 한 문장 `한 차수에 둘 수 있는 견적 줄 수 — 조정·취소 줄 포함`)을 더한다. 설정 화면에 자동으로 나온다(Phase 3 레지스트리).

② `test/integration/quote-line-cap.test.ts`에 `behavior` 여섯 줄을 먼저 쓴다(RED — 설정 값은 테스트에서 3·5로 바꾸고 `try/finally`로 되돌린다).

③ `domain/rules/register.ts`에 `quote.line-cap`(ctx `{ newLines, countAfter, cap }` → **`newLines > 0` 이고** `countAfter > cap`이면 `{cap}줄 상한을 넘어 전부 거부 · 줄을 지워 주세요` — 줄을 더하지 않는 저장은 상한과 무관하다, A-20)을 등록한다. `repositories/quote-lines.ts`에 `countActiveLinesByRevision(viewer, revisionId, tx)`(보관 제외 전부)를 더하고, `saveQuoteLines`가 **04-12의 `loadProjectForGate` 잠금 뒤** 같은 `tx`로 `현재 활성 줄 수 − 이번에 보관할 활성 줄 수 + 새 줄 수`를 계산해 게이트를 부른다 — 거부면 `GateBlockedError`로 전체 거부(04-12의 한 거부 지점을 지나 `write.denied` 경고가 한 번 남는다 — 따로 로그 호출을 더하지 않는다, B-26). 04-12의 `restoreQuoteLine`도 같은 잠금 뒤 `newLines: 1`로 이 게이트를 부른다(A-19·OV-2)(GREEN).

④ **화면(한 경로).** `[id]/page.tsx`가 `getSettingValue(QUOTE_LINE_MAX_PER_REVISION)`을 읽어 `lineCap`으로 넘긴다. `quote-table.tsx`: 화면의 활성 줄 수(보관 표시한 줄 제외, 새 줄 포함)가 상한이면 「줄 추가」를 `disabled` + `disabledReason` `줄 추가할 수 없음 — {cap}줄 상한 · 상한은 관리자 설정`(`--danger`). 문구의 숫자는 `lineCap` 값이다. 서버 거부 문구는 기존 거부 요약 자리에 그대로 쓴다.

⑤ **E2E(트레이서 증명, `quote-edit-scope.spec.ts` 확장).** **전역 상한 설정을 바꾸지 않는다(A-17 — Playwright가 스펙 파일을 병렬로 돌려 다른 스펙이 낮은 상한에 걸리고, 실패하면 영구 테스트 DB에 남는다).** 기본 상한 300 그대로, 준비 단계에서 SQL 한 문장(`INSERT … SELECT generate_series(1, 300)`)으로 그 프로젝트 차수에 줄 300개를 채운다 → 「줄 추가」가 비활성이고 이유 `줄 추가할 수 없음 — 300줄 상한 · 상한은 관리자 설정`이 보인다. 설정 값을 바꾸는 케이스는 통합 테스트에만 두고 `try/finally`로 되돌린다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-cap.test.ts test/integration/settings.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 상한이 서버에서 안 지켜지거나 보관·취소 줄 계산이 틀렸거나 04-12의 저장이 깨졌다</fails_when>
    <automated>pnpm vitest run --project unit</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 새 설정 키가 레지스트리 커버리지 규약을 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-edit-scope.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 상한에서 「줄 추가」가 비활성·이유 문구로 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `QUOTE_LINE_MAX_PER_REVISION` 키가 있고 설정 화면에 나온다
    - `quote.line-cap` 게이트가 등록되고 `saveQuoteLines`가 판정 단계에서 부른다
    - 상한 거부 뒤 DB 줄 수 무변경(통합 테스트, DB 재조회)
    - 보관 줄 제외 · 취소 줄 포함 · 상한 낮춘 뒤 고치기·보관 통과(A-20) · 복원 상한(A-19) 케이스가 각각 있다
    - 문구의 상한 숫자가 설정 값에서 온다(E2E는 기본 300, 통합은 3·5)
    - E2E 파일에 설정 값을 바꾸는 호출이 없다(A-17) · 통합의 설정 변경은 `try/finally`로 되돌린다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>차수당 줄 수 상한이 설정 키 하나로 서버 저장과 「줄 추가」에서 같은 숫자로 지켜진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 붙여넣기 전부 거부 + Ctrl+Enter·Ctrl+D 무동작 + 화면 검증</name>
  <files>app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/integration/quote-line-cap.test.ts, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `handlePasteAtCell`·`duplicateLine`·`pasteWarning`·합계 행 요약 자리를 찾아 범위만 Read
    - `ui/table/use-clipboard-paste.ts`(04-04 산출물) — 붙여넣기가 새 줄을 만드는 계산(필요한 새 줄 수를 미리 셀 자리)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - 상한 300 · 줄 300(SQL 준비)에서 Ctrl+Enter·Ctrl+D → 줄 수 그대로, 합계 행 오른쪽에 `줄 추가할 수 없음 — 300줄 상한 · 상한은 관리자 설정`
    - 상한 300 · 줄 299에서 세 줄짜리 TSV를 마지막 줄에 붙여 넣어 새 줄 2가 필요해 상한을 1줄 넘으면 → 셀 값이 하나도 바뀌지 않고 합계 행에 `붙여넣기 전부 거부 · 300줄 상한을 1줄 넘음 · 줄 수를 줄여 다시 붙여 주세요`
    - 서버 우회(상한을 넘는 새 줄을 직접 실은 배치) → 통합 테스트에서 전부 거부
    - (A-36·OV-3) 상한 3 · 활성 줄 2에서 두 연결이 각각 새 줄 1개를 저장 → 하나만 성공하고 활성 줄은 3(두 순서 모두)
  </behavior>
  <action>
① E2E 케이스(`behavior` 앞 두 줄 — 준비는 Task 1의 SQL 채우기, 전역 설정 변경 없음)를 먼저 쓴다(RED). `test/integration/quote-line-cap.test.ts`에 클라이언트 판정을 거치지 않고 상한을 넘는 새 줄 두 개를 직접 실은 배치가 전부 거부되는 케이스와, **동시 추가 경합(A-36·OV-3)** — 연결 A의 저장을 `saveQuoteLines`의 `deps.afterLock`(04-12)에 `deferred`로 멈추고 연결 B의 저장이 `waitForLockWaiter`(04-20 `test/integration/lock-race.ts`)로 대기에 든 것을 확인한 뒤 A를 풀어 A 성공 · B `3줄 상한을 넘어 …` 거부 · 활성 줄 3 — 을 더한다(`sleep` 금지).

② `quote-table.tsx`: Ctrl+Enter·Ctrl+D는 상한에서 줄을 만들지 않고 합계 행 오른쪽에 같은 이유(`--danger`). 붙여넣기는 적용 **전에** 새로 생길 줄 수를 계산해 상한을 넘으면 **한 칸도 바꾸지 않고** 합계 행에 `붙여넣기 전부 거부 · {cap}줄 상한을 {초과}줄 넘음 · 줄 수를 줄여 다시 붙여 주세요`(`--danger`). 오른쪽 넘침(열 초과)의 기존 「버림 + 경고」 규칙은 건드리지 않는다(GREEN).

③ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S4 partial(상한) 렌더 — 「줄 추가」 비활성 이유와 합계 행의 거부 문구가 PC·375에서 잘리지 않고 문서 가로 스크롤 0 — 을 DOM 실측 → 필요하면 `project-detail.module.css`만 고친다(문구를 줄이지 않는다) → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-cap.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 클라이언트 우회 배치가 통과했다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 붙여넣기 전부 거부·키 무동작 E2E가 실패하거나 기존 표 흐름이 깨졌다</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S4 partial(상한) 판정을 SUMMARY에서 확인</test>
      <expected>비활성 이유와 합계 행 거부 문구가 PC·375에서 잘리지 않고 가로 스크롤 0</expected>
      <why_human>문구 폭·줄바꿈은 소스 단언으로 증명되지 않는다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 붙여넣기 전부 거부 E2E가 셀 값 무변경까지 단언한다(금지 항목)
    - Ctrl+Enter·Ctrl+D 뒤 줄 수가 그대로이고 이유 문구가 보인다(E2E)
    - 클라이언트 우회 배치의 서버 전부 거부 케이스가 통합 테스트에 있다
    - 독립 DOM 감사 판정이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상한이 키·붙여넣기·서버의 모든 입력 경로에서 같은 숫자로 지켜지고, 넘치는 붙여넣기는 조용히 잘리지 않고 전부 거부된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 붙여넣기 → 상한 | 클라이언트 판정은 편의일 뿐, 서버가 같은 상한으로 다시 판정한다 |
| 설정 레지스트리 → 게이트 | 관리자가 바꾼 상한 값이 화면과 서버에 같이 적용돼야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-60 | Denial of Service | 줄 수 무제한 | medium | mitigate | 설정 상한(기본 300)을 서버 게이트가 강제하고, 04-04의 요청 본문 한도가 그 앞을 막는다. 클라이언트 우회 배치 거부를 통합 테스트로 고정한다 |
| T-04-171 | Tampering | 동시 추가·복원으로 상한 우회 | medium | mitigate | (CEO A-36·A-19·OV-3) 줄 수를 프로젝트 행 `FOR UPDATE` 안에서 세고 복원도 같은 게이트를 지난다 — 두 연결 결정적 경합 테스트와 복원 상한 케이스로 고정한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `register.ts`에 `quote.line-cap` 등록, `QUOTE_LINE_MAX_PER_REVISION`이 설정 화면에 나온다
- 독립 DOM 감사(S4 partial 상한) 판정이 SUMMARY에 있다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 차수당 줄 수 상한이 설정 하나로 모든 입력 경로(줄 추가·키·붙여넣기·서버)에서 지켜진다
- 넘치는 붙여넣기는 견적을 자르지 않고 전부 거부되며 이유가 보인다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 설정 키(신규) | `quote_line.max_per_revision`(`QUOTE_LINE_MAX_PER_REVISION`, 단순값, 기본 300) |
| 게이트 규칙(신규) | `quote.line-cap` |
| 함수(신규, repository) | `countActiveLinesByRevision`(`repositories/quote-lines.ts`) |
| 변경된 기존 심볼 | `saveQuoteLines`(잠금 뒤 상한 판정 — 추가가 있을 때만) · `restoreQuoteLine`(상한 판정) · `QuoteLedger`(`lineCap` prop · 「줄 추가」 비활성 · Ctrl+Enter/Ctrl+D 무동작 · 붙여넣기 전부 거부) |
| 테스트 | `test/integration/quote-line-cap.test.ts`(신규) · `test/e2e/quote-edit-scope.spec.ts`(상한 케이스 확장) |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-17 — E2E 전역 상한 3이 병렬 스펙을 흔듦 | WARNING | Plan 04-26, Task 1 ⑤·Task 2 ①: 기본 300 + SQL 300줄 준비, 설정 변경은 통합에서 `try/finally` |
| A-20 — 상한을 낮추면 모든 저장이 막힘 | WARNING | Plan 04-26, Task 1 ③: `newLines > 0` 조건 · behavior A-20 줄 |
| A-36 / OV-3 — 동시 추가로 상한 초과 | NOTE→HIGH | Plan 04-26, Task 1 ③(잠금 뒤 셈) · Task 2 ① 두 연결 경합 · T-04-171 |
| A-19 / OV-2 — 복원이 상한 우회 | WARNING | Plan 04-26, Task 1 ③: `restoreQuoteLine`이 같은 게이트 · behavior A-19 줄 |
| OV-7 / 사용자 D21 | HIGH | 견적 표 화면을 04-30 뒤에 — 웨이브 13 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| C-05 상한 경계의 끝 줄바꿈 붙여넣기 | 파서를 고치는 04-19와 최종 확인 04-31이 다룬다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-26-SUMMARY.md` when done
</output>
