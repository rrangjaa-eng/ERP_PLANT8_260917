---
phase: 04-project-quote-ledger
plan: 27
type: execute
wave: 6
depends_on: ["04-06"]
files_modified:
  - db/schema/roles.ts
  - db/migrations/0013_role_work_scope.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0013_snapshot.json
  - repositories/roles.ts
  - domain/permissions/roles.ts
  - domain/seed/index.ts
  - app/(app)/admin/people/actions.ts
  - app/(app)/admin/people/actions.registry.ts
  - app/(app)/admin/people/roles/roles-client.tsx
  - test/integration/roles.test.ts
  - test/e2e/roles.spec.ts
autonomous: true
requirements: [PROJ-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "계급마다 「업무 범위」가 `자기 팀`(`team`) 또는 `전사`(`company`) 하나다 — 새 DB는 시드가, 이미 돌고 있는 DB는 마이그레이션이 채운다: 팀장 = 자기 팀 · 본부 책임자·대표·시스템 관리자 = 전사 · 기획 PM = 자기 팀. 화면에서 새로 만든 계급의 기본값은 자기 팀이다(사용자 D11·D20 — 「팀장은 자기 팀 프로젝트만, 본부장·대표·시스템 관리자는 전사」)"
    - "관리자(`admin.people` 쓰기)가 계급 화면의 `업무 범위` 칸에서 값을 바꾸면 저장되고 행동 로그 `permission_change` 한 줄이 남는다 — 쓰기 권한이 없는 사람의 요청은 서버가 거부한다(D20)"
    - "시드를 다시 돌려도 관리자가 바꾼 업무 범위가 덮이지 않는다 — 시드는 계급 행이 없을 때만 넣는다(CEO 리뷰 A-05와 같은 원칙)"
    - "업무 범위는 **보기 범위가 아니다** — `can`·`visible`·`scopeFor`는 바뀌지 않고, 이 값은 상태 전환·기간 수정·「상태 바꾸기」 렌더를 판정하는 게이트 입력(04-20·04-21·04-22)으로만 읽힌다. 순서를 비교하는 계급 순위도 아니다(값 둘, 비교 없음 — Phase 3 결정 「순위 컬럼 없음」 유지, OV-4 「scopeFor는 건드리지 않음」)"
    - "새 마이그레이션의 번호는 적용 직전 journal 최고 번호 + 1(04-06의 0012 뒤)이고 파일명·snapshot·journal tag가 생성기 출력과 같다 — 계획 번호 0013과 다르면 실제 이름이 SUMMARY에 있다. 두 값 CHECK는 컬럼 정의 안에 인라인으로 두어 squawk가 통과한다(CEO 리뷰 B-22의 확인된 형태)"
  prohibitions:
    - statement: "업무 범위가 「누가 무엇을 볼 수 있는가」를 바꾸는 네 번째 판정 경로가 되어서는 안 된다. 보기 권한은 `can`·`visible`·`scopeFor` 세 함수에만 있다는 Phase 3 계약(ROADMAP Phase 3 기준 2)을 지키려면, 이 값은 사람의 **행동**(상태 전환·기간 수정)을 판정하는 게이트의 입력으로만 쓰이고 목록·상세의 보기 범위에 끼어들지 않는다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/0013_role_work_scope.sql"
      provides: "roles.work_scope(기본 team, 두 값 인라인 CHECK) + 시드 계급 셋을 company로"
      contains: "work_scope"
    - path: "domain/permissions/roles.ts"
      provides: "계급 업무 범위 읽기(DTO)·바꾸기(admin.people 쓰기) + 시드 상수의 업무 범위"
      exports: ["ROLE_WORK_SCOPES", "setRoleWorkScope"]
    - path: "app/(app)/admin/people/roles/roles-client.tsx"
      provides: "계급 표의 `업무 범위` 칸(자기 팀/전사)"
      contains: "업무 범위"
    - path: "test/integration/roles.test.ts"
      provides: "시드 값 · 관리자 변경·로그 · 권한 거부 · 재시드 보존 · 새 계급 기본값"
  key_links:
    - from: "app/(app)/admin/people/roles/roles-client.tsx"
      to: "app/(app)/admin/people/actions.ts"
      via: "업무 범위 선택이 setRoleWorkScopeAction을 부르고 실패하면 서버 값으로 되돌린다"
      pattern: "setRoleWorkScopeAction"
    - from: "domain/seed/index.ts"
      to: "db/migrations/0013_role_work_scope.sql"
      via: "새 DB(시드)와 기존 DB(마이그레이션)의 계급별 업무 범위가 같다"
      pattern: "workScope"
---

<objective>
**As a** 시스템 관리자와 기획팀장, **I want to** 계급마다 「업무 범위: 자기 팀 / 전사」가 정해져 있어 팀장은 자기 팀 프로젝트만 상태를 바꾸고 본부장·대표는 전사를 다루며, 관리자가 그 값을 계급 화면에서 바꿀 수 있기를, **so that** 다른 팀 팀장이 남의 팀 프로젝트를 닫거나 되살리는 일이 규칙으로 막히고, 경영관리 같은 새 계급도 화면에서 범위를 정할 수 있다.

**2026-09-23 CEO 리뷰 신설 플랜(T11 — 사용자 D11·D20, OV-4).** 옛 계획은 「팀장 이상」(D-46)을 권한표 메뉴 `projects.status` 하나로 표현해 어느 팀 팀장이든 모든 팀 프로젝트를 바꿀 수 있었다(A-27). 사용자는 D11에서 「팀장은 자기 팀 프로젝트만, 본부장·대표는 전사」를, D20에서 「계급에 업무 범위 칸」을 골랐다. 이 플랜은 그 **데이터와 관리 화면**을 만든다 — 계급 컬럼 · 마이그레이션 · 시드 · 관리자 변경 경로 · 계급 화면 칸. 이 값을 읽는 **한 규칙**(오늘 기준 발령 이력으로 소속 팀을 찾아 프로젝트 팀과 비교)은 상태 전환 게이트를 만드는 04-20이 `domain/projects/status.ts`에 두고, 상태 전환·「상태 바꾸기」 렌더(04-21)·기간 수정 권리(04-22)가 같은 함수를 부른다.

이 플랜은 견적 표와 무관해 04-04의 사람 확인을 기다리지 않는다(사용자 D21).

Purpose: D11·D20의 판정 입력이 계급 데이터로 서야 04-20이 전환 게이트에 팀 조건을 얹을 수 있다.

Output: `roles.work_scope` + 마이그레이션 0013(번호는 직전 최고 + 1) · 시드 · 관리자 변경 domain·액션 · 계급 화면 칸 · 통합·E2E 확장.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`):** 엔지 리뷰 A 테스트 공백 7 — `roles.spec.ts`가 시드 계급 `role-team-lead`의 업무 범위를 바꿨다 되돌리면, 같은 시각 다른 워커에서 도는 `project-lifecycle`·`project-period` 스펙(팀장 전환·기간 권리)이 그 사이에 흔들린다(`playwright.config.ts:50` `fullyParallel: false`여도 파일은 워커끼리 병렬, `deferred-items.md`의 병렬 흔들림 기록). E2E는 `beforeAll`에서 만든 **임시 계급**의 업무 범위만 바꾸고, 시드 계급은 읽기만 한다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 29행(A-27 · D11) · 41행(OV-4 · D20) · 316–319행(T11) · 806행(A-27 원문) · 1298–1306행(B-22 — 인라인 CHECK가 squawk를 통과한 형태)
# .planning/phases/03-permissions-settings-masters/03-CONTEXT.md D-33~D-35(계급은 데이터 · 판정 세 함수)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(상태 전환 주체의 팀 범위 — 데이터·관리 화면)** 다. 수용 기준은 사용자 답 D11·D20과 CEO 리뷰 OV-4·T11에서 끌어와 `must_haves.truths`에 적었다. 판정 쪽 truths(다른 팀 팀장 전환 거부 · 본부장 허용 · 버튼 부재 · 기간 수정)는 04-20·04-21·04-22가 적는다.

**계획 단계 가정(사용자에게 표면화):**
- **시드 값:** 팀장 = 자기 팀, 본부 책임자·대표·시스템 관리자 = 전사(D20 원문), 기획 PM = 자기 팀(`projects.status`가 없어 판정에 쓰이지 않는다). **경영관리는 시드 계급이 아니다** — 관리자가 만든 계급은 기본값 자기 팀으로 시작하므로, 경영관리에게 `projects.status`를 켜서 전사 업무를 맡기려면 관리자가 이 칸을 `전사`로 바꿔야 한다. 이 안내를 이 플랜의 SUMMARY와 04-20 SUMMARY의 권한표 기본값 안내에 함께 적는다(화면에 새 도움말 문구를 만들지 않는다).
- **Phase 3 결정과의 관계:** `db/schema/roles.ts` 머리 주석은 「순위(rank) 컬럼을 두지 않는다 — 네 번째 판정 경로가 된다」고 적었다. 업무 범위는 **순위가 아니고**(두 값, 크기 비교 없음) **보기 권한도 아니다**(can·visible·scopeFor 무변경) — 게이트 규칙의 입력 사실(「담당 PM인가」와 같은 종류)이다. 이 구분을 같은 주석에 한 문단으로 더한다. 사용자가 D20으로 승인한 모델이다.
- **변경 로그 종류:** 계급 이름 바꾸기와 같은 `permission_change`(끌 수 있는 종류가 아니다 — `record.ts` 확인), detail `{ workScope: { from, to } }`.
</probe_fallback>

<reversibility_notes>
- `roles.work_scope`와 두 값 — **costly**: 04-20·04-21·04-22의 게이트가 읽고, 관리자가 바꾼 값이 운영 데이터가 된다. 되돌리려면 컬럼 삭제 마이그레이션과 세 플랜의 판정 되돌리기가 따라온다. 사용자가 D20으로 정했으므로 체크포인트를 두지 않는다.
</reversibility_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 관리자가 계급 화면에서 팀장의 업무 범위를 전사로 바꾼다(스키마 → 마이그레이션 → 시드 → 리포지토리 → domain → 액션 → 화면 → E2E)</name>
  <files>db/schema/roles.ts, db/migrations/0013_role_work_scope.sql, db/migrations/meta/_journal.json, db/migrations/meta/0013_snapshot.json, repositories/roles.ts, domain/permissions/roles.ts, domain/seed/index.ts, app/(app)/admin/people/actions.ts, app/(app)/admin/people/actions.registry.ts, app/(app)/admin/people/roles/roles-client.tsx, test/e2e/roles.spec.ts</files>
  <reversibility rating="costly">계급 업무 범위는 세 플랜의 게이트가 읽는 운영 데이터가 된다 — 사용자 D20 결정이고 값 이름(`team`·`company`)만 계획 재량이다.</reversibility>
  <read_first>
    - `db/schema/roles.ts`(전문) — 머리 주석(순위 컬럼 없음)과 컬럼 목록
    - `db/migrations/0012_*.sql`(04-06 산출물 — 실제 번호는 04-06 SUMMARY) — 직전 마이그레이션 문체와 락 타임아웃 한 쌍
    - `db/migrations/meta/_journal.json` — 마지막 항목의 `idx`·`tag`(이 플랜의 번호 = 그 `idx` + 1)
    - `.squawk.toml`(전문) — 제외 규칙 넷(고치지 않는다)
    - `repositories/roles.ts`(전문 75행) — `seedRole`(onConflictDoNothing)·`renameRole` 형태
    - `domain/permissions/roles.ts`(전문 132행) — `SEED_ROLES`·`RoleDto`·`ROLE_DTO_SPEC`·`renameRole`의 권한 판정·로그 순서
    - `domain/seed/index.ts` 120–135행(계급 시드 루프)
    - `app/(app)/admin/people/actions.ts` 60–90행(`renameRoleAction`) · `actions.registry.ts`(전문)
    - `app/(app)/admin/people/roles/roles-client.tsx`(전문 145행) — `RoleRow`의 이름 칸 blur 저장·오류 한 줄
    - `test/e2e/roles.spec.ts`(전문)
  </read_first>
  <action>
① **스키마.** `db/schema/roles.ts`에 `workScope: text("work_scope").notNull().default("team")`와 두 값 CHECK(`team`·`company`)를 더하고, 머리 주석에 `<probe_fallback>`의 「업무 범위는 순위도 보기 권한도 아니다 — 게이트 입력」 문단을 더한다.

② **[BLOCKING] 마이그레이션.** `pnpm db:generate --name role_work_scope`로 만든다. **번호 규칙:** 계획 번호는 0013이고, 생성기가 매기는 번호는 적용 직전 journal의 최고 `idx` + 1이어야 한다(04-06의 0012 뒤). 생성기가 다른 번호를 주면 파일명·snapshot·journal tag를 생성기 출력 그대로 두고(손으로 번호를 바꾸지 않는다) 실제 이름을 SUMMARY에 적는다. 생성 SQL을 **손으로 인라인 형태로 고친다**(B-22 — drizzle이 따로 내는 `ADD CONSTRAINT … CHECK`와 같은 파일의 `NOT VALID` + `VALIDATE` 둘 다 squawk가 거부한다): 락 타임아웃 한 쌍 → `ALTER TABLE "roles" ADD COLUMN "work_scope" text DEFAULT 'team' NOT NULL CONSTRAINT "roles_work_scope_check" CHECK ("work_scope" IN ('team','company'))` → `UPDATE "roles" SET "work_scope" = 'company' WHERE "id" IN ('role-division-head','role-ceo','role-sysadmin')`. 최종 스키마 상태는 snapshot과 같다. `pnpm lint:sql`(`Found 0 issues` — 걸리면 `.squawk.toml`이 아니라 SQL을 고친다) → `bash scripts/dev-db.sh && pnpm db:migrate`.

③ **시드·리포지토리·domain.** `domain/permissions/roles.ts`: `ROLE_WORK_SCOPES = ["team","company"] as const`와 타입, `SEED_ROLES` 각 항목에 `workScope`(팀장 team · 본부 책임자·대표·시스템 관리자 company · 기획 PM team), `RoleDto`·`ROLE_DTO_SPEC`에 `workScope`(정보 항목 `role.value` — 새 항목을 만들지 않는다, `registerDto` 갱신), `setRoleWorkScope(viewer, id, workScope, deps?)` — `renameRole`과 같은 순서(`can(viewer, "admin.people", "write")` → 리포지토리 → `recordAction(permission_change, entity roles, detail { workScope: { from, to } })`), 없는 계급은 찾을 수 없음. `repositories/roles.ts`: `setRoleWorkScope(viewer, id, workScope)`를 더하고 `seedRole`·`insertRole` 입력에 `workScope`를 받는다(`seedRole`은 `onConflictDoNothing` 그대로 — 관리자 변경을 덮지 않는다). `domain/seed/index.ts`의 계급 시드 루프가 `workScope`를 넘긴다.

④ **액션·화면.** `app/(app)/admin/people/actions.ts`에 `setRoleWorkScopeAction`(zod: `id` · `workScope` enum)을 더하고 `actions.registry.ts`에 등록한다(D-38). `roles-client.tsx`의 표에 `업무 범위` 열을 이름 열 뒤에 더한다 — 기존 이름 칸과 같은 `styles.select` 네이티브 select(선택지 `자기 팀`·`전사`, 접근 이름 `{계급 이름} 업무 범위`), 바꾸면 곧바로 액션을 부르고 실패하면 서버 값으로 되돌리며 오류를 칸 아래 한 줄로(이름 칸과 같은 자리·클래스). 보관된 계급 행은 select를 비활성으로(이름 칸 규칙과 같게). 새 색·서체·폭 값을 만들지 않는다.

⑤ **E2E(트레이서 증명, `roles.spec.ts` 확장 — 엔지 리뷰 A 공백 7).** `beforeAll`에서 이 스펙 전용 임시 계급(고유 이름 — 예: `E2E 업무범위 {타임스탬프}`)을 만든다(기존 스펙의 계급 생성 방식 그대로). 시스템 관리자로 `/admin/people/roles`를 열면 팀장 행 `업무 범위`가 `자기 팀`, 대표 행이 `전사`, 임시 계급 행이 `자기 팀`(새 계급 기본값) → **임시 계급**을 `전사`로 바꾸고 새로 고쳐도 `전사` → 다시 `자기 팀`. **시드 계급(`role-team-lead` 등)의 업무 범위는 바꾸지 않는다** — 다른 워커의 전환·기간 스펙이 그 값을 읽는다. `afterAll`에서 임시 계급을 보관한다(실패해도 돌도록).
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_role_work_scope'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- .squawk.toml</automated>
    <fails_when>db:migrate가 0이 아닌 코드로 끝나거나(`error:` 줄), diff 출력이 비어 있지 않다 — 린트를 피하려고 `.squawk.toml`을 고쳤다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/roles.spec.ts test/e2e/mobile-roles.spec.ts</automated>
    <fails_when>lint·typecheck·build error가 나오거나 playwright failed 개수가 1 이상이다 — 업무 범위 칸이 저장·새로 고침으로 이어지지 않았거나 계급 화면의 기존 흐름(폰 포함)이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 락 타임아웃 한 쌍 · 인라인 CHECK가 있는 `ADD COLUMN` · 시드 계급 셋의 `company` UPDATE를 담고 `.squawk.toml` diff가 0줄이다
    - 새 마이그레이션의 번호 = 이 플랜 실행 직전 journal 최고 `idx` + 1이고 파일명·`meta/{번호}_snapshot.json`·journal tag가 생성기가 실제로 만든 이름과 같다(번호 검증 명령) — 계획 번호 0013과 다르면 실제 이름이 SUMMARY에 있다
    - `SEED_ROLES`의 업무 범위가 `<probe_fallback>` 표와 같고 `seedRole`이 `onConflictDoNothing`을 유지한다
    - `setRoleWorkScopeAction`이 `actions.registry.ts`에 등록됐다
    - `db/schema/roles.ts` 머리 주석에 「순위도 보기 권한도 아니다」 문단이 있다
    - `domain/permissions/scope-for.ts`·`can.ts`·`visible.ts` diff가 0줄이다(금지 항목)
    - E2E가 시드 값(팀장 `자기 팀` · 대표 `전사`)을 읽기로만 단언하고, 임시 계급의 `전사` → 새로 고침 → 되돌리기를 단언한다 — 스펙에 시드 계급 행의 업무 범위 select를 바꾸는 동작이 없다(엔지 리뷰 A 공백 7)
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>계급에 업무 범위 칸이 생기고 관리자가 화면에서 바꾸면 저장되며, 새 DB와 기존 DB가 같은 계급별 기본값을 갖는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 업무 범위 규칙의 서버 증명 — 시드 값·권한 거부·로그·재시드 보존·새 계급 기본값</name>
  <files>test/integration/roles.test.ts, domain/permissions/roles.ts</files>
  <read_first>
    - `test/integration/roles.test.ts`(전문) — 계급 통합 테스트 형태와 viewer 준비
    - `domain/seed/index.ts`의 `seedMasterData`(재시드 호출 형태)
    - `domain/action-log/record.ts` 1–75행 — `permission_change`가 기록되는 방식
  </read_first>
  <behavior>
    - 빈 DB에 시드를 돌리면 `role-team-lead` = team · `role-division-head`·`role-ceo`·`role-sysadmin` = company · `role-pm` = team
    - 시스템 관리자가 `role-team-lead`를 company로 바꾸면 DB 값이 company이고 `permission_change` 로그 한 줄(detail `workScope.from = team`, `to = company`)
    - `admin.people` 쓰기가 없는 viewer(기획 PM)의 변경은 거부되고 DB 값이 그대로다
    - 관리자가 바꾼 뒤 `seedMasterData`를 다시 돌려도 값이 company 그대로다
    - `createRole`로 만든 새 계급의 업무 범위는 team이다
    - 두 값 밖의 값(`all`)은 액션 스키마에서 거부된다(domain 직접 호출은 DB CHECK가 거부)
  </behavior>
  <action>
① `test/integration/roles.test.ts`에 describe `계급 업무 범위(D11·D20)`를 더하고 `behavior` 여섯 줄을 먼저 쓴다(RED가 나면 Task 1의 빈틈을 `domain/permissions/roles.ts`에서 고친다 — 예: 로그 detail 모양, 없는 계급 처리). 거부 케이스는 DB 재조회로 무변경을 단언한다.

② 이 규칙을 읽는 판정(오늘 기준 발령 이력의 소속 팀 × 프로젝트 팀)은 04-20이 `domain/projects/status.ts`에 두고 같은 파일의 통합 테스트로 증명한다 — 이 태스크는 데이터 쪽만 끝낸다. 경영관리 같은 비시드 계급의 업무 범위를 관리자가 정해야 한다는 안내를 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/roles.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 시드 값·권한 거부·로그·재시드 보존·기본값 중 하나가 틀렸거나 새 DTO 필드·액션이 누수 스캔에서 빠졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 계급 컬럼 추가가 기존 계급·권한 흐름을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `roles.test.ts`에 `behavior` 여섯 줄이 각각 케이스로 있고 거부 케이스는 DB 재조회로 무변경을 단언한다
    - 재시드 보존 케이스가 있다(A-05와 같은 원칙)
    - 비시드 계급 안내가 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>계급 업무 범위가 시드·관리자 변경·재시드·새 계급 네 경로에서 규칙대로 동작하고, 04-20이 읽을 데이터가 준비됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 브라우저 → 업무 범위 액션 | 계급의 행동 범위가 바뀐다. 쓰기 권한 판정이 서버에만 있어야 한다 |
| 시드 → 운영 DB | 배포마다 도는 시드가 관리자 변경을 덮으면 좁혀 둔 범위가 조용히 넓어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-101 | Elevation of Privilege | `setRoleWorkScopeAction` | high | mitigate | `authedActionClient` + domain이 `admin.people` 쓰기를 다시 판정하고 zod enum이 두 값 밖을 막으며 DB CHECK가 마지막 방어다. 권한 없는 변경 거부를 통합 테스트로 고정한다 |
| T-04-102 | Tampering | 시드 재실행 | medium | mitigate | 계급 시드는 `onConflictDoNothing`이라 관리자가 바꾼 업무 범위를 덮지 않는다 — 재시드 보존 케이스로 고정한다 |
| T-04-173 | Repudiation | 업무 범위 변경 | low | mitigate | 변경마다 `permission_change` 로그에 from·to를 남긴다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`, `.squawk.toml` 무변경, 마이그레이션 번호 검증 명령 초록
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- `domain/permissions/scope-for.ts`·`can.ts`·`visible.ts` diff 0줄
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
</verification>

<success_criteria>
- 계급마다 업무 범위가 있고 관리자가 화면에서 바꾼다
- 새 DB와 기존 DB가 같은 기본값을 갖고, 재시드가 관리자 변경을 덮지 않는다
- 보기 권한 세 함수는 바뀌지 않았다
- 이 플랜이 04-04의 사람 확인을 기다리지 않는다(D21)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컬럼(신규) | `roles.work_scope` text NOT NULL DEFAULT `'team'` + CHECK(`team`·`company`) |
| 마이그레이션 | `db/migrations/0013_role_work_scope.sql`(계획 번호 — 실제 번호는 생성기 기준, 직전 최고 + 1) + `meta/0013_snapshot.json` + `_journal.json` 항목 |
| 상수·함수 | `ROLE_WORK_SCOPES` · `setRoleWorkScope`(domain · repository) · `SEED_ROLES[].workScope` |
| Server Action | `setRoleWorkScopeAction`(등록 포함) |
| 화면 | 계급 표 `업무 범위` 열(자기 팀/전사) |
| 변경된 기존 심볼 | `RoleDto`(+workScope) · `seedRole`·`insertRole` 입력 · `db/schema/roles.ts` 머리 주석 |
| 테스트 | `test/integration/roles.test.ts`(업무 범위 describe) · `test/e2e/roles.spec.ts`(임시 계급의 칸 변경 — 시드 계급 읽기만) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 업무 범위를 읽는 규칙 `actorCoversProjectTeam`과 전환 게이트(04-20) · 「상태 바꾸기」 렌더(04-21) · 기간 수정 권리(04-22) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-27 / 사용자 D11 — 팀장이 다른 팀 프로젝트를 바꿈 | DECISION | Plan 04-27(신규): 계급 업무 범위 데이터·화면 — 판정은 04-20·04-21·04-22 |
| OV-4 / 사용자 D20 — 팀 한정을 계급 칸으로 표현, scopeFor 무변경 | HIGH | Plan 04-27, Task 1: `roles.work_scope`·시드·관리 화면 · 금지 항목(보기 권한 무변경) |
| B-22 — CHECK 추가가 squawk에 걸림 | WARNING | Plan 04-27, Task 1 ②: 인라인 CHECK 형태로 손 편집 |
| OV-7 / 사용자 D21 | HIGH | 견적 표 무관 — 04-06 뒤 웨이브 6, 04-04와 분리 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 A 테스트 공백 7 — E2E가 공유 시드 계급을 바꿔 병렬 스펙을 흔듦 | WARNING | Plan 04-27, Task 1 ⑤: `beforeAll` 임시 계급만 바꾸고 시드 계급은 읽기 · 수용 기준 |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-27-SUMMARY.md` when done
</output>
