---
phase: 04-project-quote-ledger
plan: 29
type: execute
wave: 4
depends_on: ["04-08"]
files_modified:
  - ui/pagination/page-window.ts
  - ui/pagination/Pagination.tsx
  - ui/pagination/Pagination.module.css
  - test/unit/ui/pagination.test.ts
  - lib/kst-date.ts
  - test/unit/lib/kst-date.test.ts
autonomous: true
requirements: [PROJ-01, UX-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "페이지 줄은 `51–100 / 125건  이전 · 1 2 3 · 다음` 모양이다 — 왼쪽 범위 글자(`--fs-sm --muted`), 오른쪽 이전·번호·다음(3차 모양), 현재 번호는 `--fg` 700 글자이고 링크·버튼이 아니며 `aria-current=\"page\"`를 갖는다(SYSTEM.md §7-16 — 04-08 개정 ⑧, D-91)"
    - "한 쪽뿐이면 페이지 줄 자체가 없다 · 첫 쪽에 「이전」, 끝 쪽에 「다음」을 렌더하지 않는다 · 8쪽 이상이면 첫·끝·현재 ±1만 보이고 사이는 포커스를 받지 않는 `…`다(§7-16)"
    - "건너뛸 쪽이 **하나뿐**이면 `…` 대신 그 번호를 보인다 — 4쪽/12쪽이면 `1 2 3 4 5 … 12`, 9쪽/12쪽이면 `1 … 8 9 10 11 12`(한 쪽을 `…` 뒤에 숨기지 않는다, CEO 리뷰 C-27)"
    - "이동이 주소 이동이면 번호는 `<a href>`, 화면 안 전환이면 `<button type=\"button\">`이다 — 한 컴포넌트가 두 갈래를 유니언 prop으로 받는다(§7-16, §10 「이동이면 `<a>`」, ListEmpty 선례)"
    - "편집 표 쪽은 다른 쪽의 오류 칸 수를 번호 옆 `오류 N`(`--fs-xs --danger`)으로 보이고 그 링크·버튼의 접근 이름이 `N쪽, 오류 M칸`이다(§7-16, S12)"
    - "폰(<700)에서 번호·이전·다음은 `--touch-min` 44×44이고, 한 줄에 안 들어가면 범위 글자가 위 줄로 올라간다(§7-16 폰)"
    - "범위·건수 숫자는 천 단위 쉼표(`/ 1,250건`), 구분은 en dash다(D-95)"
    - "KST 날짜는 주입된 현재 시각에서 애플리케이션이 계산한다 — `2026-09-17T14:59:59Z`는 `2026-09-17`, `2026-09-17T15:00:00Z`는 `2026-09-18`이고 서버·DB 시간대와 무관하다(RESEARCH Pitfall 7 — 04-11·04-20·04-17·04-14가 이 모듈을 쓴다)"
    - "KST 날짜 하나를 그날 KST 00:00의 시각으로 바꾸고 다시 KST 날짜로 읽으면 같은 날짜다 — `timestamp without time zone` 컬럼에 날짜를 저장·재조회해도 하루 밀리지 않는다(CEO 리뷰 B-25, 04-14 고객 승인일)"
  prohibitions:
    - statement: "페이지 줄이 화면마다 따로 만들어져서는 안 된다. 목록(50건)·리저브(50건)·견적 표(30줄)가 같은 모양·같은 접근성 계약을 가져야 사용자가 한 번 배운 것으로 세 화면을 넘긴다 — 모양이 다른 두 번째 페이지 줄을 만들지 않는다(§7-16 「한 컴포넌트」)"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/pagination/page-window.ts"
      provides: "보이는 번호 창(첫·끝·현재 ±1, 한 쪽 틈은 번호로) · 범위 문구 — 순수 함수"
      exports: ["pageWindow", "pageRangeText"]
    - path: "ui/pagination/Pagination.tsx"
      provides: "번호 페이지 줄 — href 함수 / onPageChange 유니언, 쪽별 오류 수"
      exports: ["Pagination"]
    - path: "lib/kst-date.ts"
      provides: "주입된 현재 시각에서 KST 날짜·연도를 계산하는 순수 함수 + KST 날짜 ↔ 그날 00:00 시각 왕복"
      exports: ["kstToday", "kstDateOf", "kstYear", "addDays", "kstDayStart"]
    - path: "test/unit/ui/pagination.test.ts"
      provides: "번호 창 표(C-27 포함) · 범위 문구 · 정적 렌더(유니언·aria-current·첫/끝·오류 수)"
      min_lines: 40
  key_links:
    - from: "ui/pagination/Pagination.tsx"
      to: "ui/pagination/page-window.ts"
      via: "번호 창과 범위 문구를 순수 함수에서 받는다 — 컴포넌트가 창을 계산하지 않는다"
      pattern: "pageWindow"
---

<objective>
**As a** 목록·리저브·견적 표를 넘겨 보는 경영관리·기획 PM, **I want to** 세 화면이 같은 모양의 번호 페이지 줄을 쓰고 건너뛴 쪽이 하나뿐이면 그 번호가 그냥 보이기를, **so that** 한 번 배운 페이지 줄로 세 화면을 넘기고 `…` 뒤에 숨은 한 쪽을 찾느라 헤매지 않는다. 그리고 뒤 플랜들이 KST 날짜를 한 모듈에서 얻는다.

**2026-09-23 CEO 리뷰 반영 — 공용 부품 둘을 앞당긴 플랜.** (1) 옛 04-17(16파일)은 목록 개정과 `ui/pagination` 신설을 한 플랜에 담아 파일 상한을 넘었다. 페이지 줄은 견적 표와 무관한 순수 `ui` 부품이라 **04-04 확인과 분리해 먼저** 만든다(사용자 D21·OV-7) — 목록(04-17)·견적 표(04-19)·리저브(04-07 — 그룹 B)가 쓴다. C-27(한 쪽 틈을 `…`로 숨김)을 번호 창 규칙에 넣는다. (2) 옛 04-11이 만들던 `lib/kst-date`를 앞당긴다 — 사용자 D11·D20(팀장은 자기 팀만, 「오늘 기준 발령 이력으로 소속 팀 판정」)을 04-20이 전환 규칙에 넣으려면 04-11보다 먼저 KST 오늘이 필요하고, A-18(E2E 날짜 시한폭탄)을 막으려면 04-21의 E2E가 처음부터 `kstToday` 기준 상대 날짜를 써야 한다. B-25(승인일 `timestamp without time zone` 하루 밀림)의 왕복 함수도 여기 둔다.

Purpose: SYSTEM.md §7-16(04-08 개정 ⑧)을 코드로 옮긴 유일한 페이지 줄과, 날짜 경계가 서버 시간대에 흔들리지 않는 KST 모듈 하나.

Output: `ui/pagination`(순수 번호 창 + 표현 컴포넌트 + CSS) · `lib/kst-date` · 단위 2.

**실행 규율:** 구현 전에 superpowers `test-driven-development`, 실패를 쫓기 전에 `systematic-debugging`, 「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬 이름과 `model`을 프롬프트에 명시한다. 시간은 인자로 주입한다(가짜 타이머 없음). 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 874–895행(§7-16 페이지 줄 — 모양·현재 번호·렌더하지 않는 이전/다음·8쪽 이상 생략·요소·ARIA·폰) · 515–523행(S12 상태)
# docs/design/SYSTEM.md §7-16(04-08 이후 — Grep `### 7-16`)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 723–751행(§2 D-76 — KST 경계 · 서버 시간대 Gap) · 857–861행(Pitfall 7)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1977–1980행(C-27) · 1322–1327행(B-25)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 없다 — 페이지 줄은 UI-SPEC S12로 분류돼 있고(D-91), 옛 04-17 Task 2의 컴포넌트 truths·`behavior`를 그대로 옮기고 C-27만 더했다. S12의 화면별 상태(목록 링크 갈래 · 견적 표 버튼 갈래 · 범위 밖 번호 보정)는 각 화면 플랜(04-17·04-19·04-07)이 맡는다. `lib/kst-date`의 truths는 옛 04-11 Task 1의 `behavior` 앞 세 줄을 옮겼다.

**계획 단계 가정(표면화):**
- **DOM 감사의 자리:** 이 플랜에는 페이지 줄을 그리는 화면이 없다. 모양·폰 44×44·가로 스크롤 0의 독립 DOM 감사는 처음 화면에 꽂는 04-17이 한다 — 여기서는 정적 렌더 단위 테스트와 stylelint(토큰만)로 계약을 고정한다.
- **숫자 서식:** `lib/format-number`(04-09)는 이 플랜 뒤에 생긴다(04-09는 견적 표 파일을 고쳐 04-04 뒤에 선다). 범위 글자의 쉼표는 `Intl.NumberFormat("ko-KR")` 한 인스턴스로 만들고, 04-09 Task 2가 표시 지점 이관 목록에 이 파일을 넣어 `formatCount`로 바꾼다(D-95 「한 모듈」 — 04-09 `files_modified`에 이 파일이 있다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 번호 창 순수 함수 → Pagination 컴포넌트 → 정적 렌더 단언(C-27 포함)</name>
  <files>ui/pagination/page-window.ts, ui/pagination/Pagination.tsx, ui/pagination/Pagination.module.css, test/unit/ui/pagination.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 874–895행(§7-16 전문)
    - `docs/design/SYSTEM.md` §7-16(04-08 개정 ⑧ 반영본) · §10(이동이면 `<a>`) · §6-0 중단점(폰 <700)
    - `ui/list-empty/ListEmpty.tsx`(전문 — `href`/`onClick` 유니언과 3차 버튼 클래스 선례) · `ui/list-empty/ListEmpty.module.css`(3차 모양 규칙)
    - `docs/design/tokens.css` — `--touch-min`·`--fs-sm`·`--fs-xs`·`--fg`·`--muted`·`--danger`·`--accent`가 있는지 확인만(새 토큰을 만들지 않는다)
    - 기존 단위 테스트 중 컴포넌트를 `react-dom/server`로 렌더하는 것(Grep `renderToStaticMarkup`) — 같은 방식을 따른다
  </read_first>
  <behavior>
    - `pageWindow(1, 1)` → `[1]` · `pageWindow(2, 3)` → `[1, 2, 3]` · `pageWindow(1, 7)` → `[1..7]` · `pageWindow(5, 12)` → `[1, "gap", 4, 5, 6, "gap", 12]` · `pageWindow(1, 12)` → `[1, 2, "gap", 12]` · `pageWindow(3, 12)` → `[1, 2, 3, 4, "gap", 12]` · `pageWindow(12, 12)` → `[1, "gap", 11, 12]`
    - C-27: `pageWindow(4, 12)` → `[1, 2, 3, 4, 5, "gap", 12]`(1과 3 사이 한 쪽 2를 번호로) · `pageWindow(9, 12)` → `[1, "gap", 8, 9, 10, 11, 12]`(10과 12 사이 한 쪽 11을 번호로)
    - `pageRangeText({ page: 2, pageSize: 50, total: 125, unit: "건" })` → `51–100 / 125건` · `{ page: 3, … }` → `101–125 / 125건` · `{ page: 2, pageSize: 30, total: 142, unit: "줄" }` → `31–60 / 142줄` · 총 1,250 → `/ 1,250건`
    - 렌더: `pageCount` 1이면 아무것도 렌더하지 않는다 · 1쪽에 「이전」 없음 · 끝 쪽에 「다음」 없음 · 현재 번호가 링크·버튼이 아니고 `aria-current="page"` · `href` 갈래는 `<a>`, `onPageChange` 갈래는 `<button type="button">` · `errorCounts: { 2: 2 }`이면 2쪽 번호 옆 `오류 2`와 접근 이름 `2쪽, 오류 2칸` · `…`는 링크·버튼이 아니다
  </behavior>
  <action>
① **`ui/pagination/page-window.ts`**(순수, `ui`·`lib`만 import): `pageWindow(page, pageCount)` — 7쪽 이하면 전부, 8쪽 이상이면 첫·끝·현재 ±1과 사이 `"gap"`. **틈이 정확히 한 쪽이면 `"gap"` 대신 그 번호를 넣는다(C-27).** `pageRangeText({ page, pageSize, total, unit })` — 시작·끝·총수에 쉼표(`<probe_fallback>` — `Intl.NumberFormat("ko-KR")` 인스턴스 하나), 구분은 en dash. `behavior` 앞 셋을 단위 테스트로 먼저 쓴다(RED → GREEN).

② **`ui/pagination/Pagination.tsx`**(훅 없는 표현 컴포넌트 — 서버·클라이언트 어디서나 렌더): props `label`(표 이름 — `<nav aria-label="{label} 페이지">`) · `page` · `pageCount` · `rangeText` · `errorCounts?`(쪽 → 오류 칸 수, 편집 표 전용) + 이동 유니언 `{ href: (page: number) => string }` | `{ onPageChange: (page: number) => void }`(ListEmpty 선례 — 이동이면 `<a>`, 화면 안 전환이면 `<button type="button">`, §10). `pageCount <= 1`이면 null. 모양은 §7-16 그대로: 왼쪽 범위 글자 `--fs-sm --muted`, 오른쪽 `이전 · 번호들 · 다음`(3차 = `--accent` 글자 + 밑줄, `--fs-sm` 600) · 현재 번호 `--fg` 700 밑줄 없는 `<span aria-current="page">` · `…`는 포커스 없는 글자 · 오류 수가 있는 번호 옆 `오류 N`(`--fs-xs --danger`)과 그 링크·버튼의 접근 이름 `N쪽, 오류 M칸`. 아이콘·`‹ ›` 글리프 없음.

③ **`Pagination.module.css`**: 토큰만(D-20). 폰(<700)에서 번호·이전·다음은 `--touch-min` 44×44, 넘치면 범위 글자가 위 줄로(`flex-wrap`). 새 중단점 없음(§6-0의 700만).

④ 렌더 단위 테스트(`behavior` 마지막 줄)는 `test/unit/ui/pagination.test.ts`에 정적 렌더로 쓴다(새 의존성 없이 — 기존 단위 테스트가 컴포넌트를 렌더하는 방식을 따른다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/pagination.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 번호 창(C-27 포함)·범위 문구·렌더 규칙이 §7-16과 다르다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(boundaries 포함)·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — `ui`가 `domain`·`app`을 import했거나 토큰 밖 값을 썼다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ui/pagination/Pagination.tsx`가 `ui`·`lib` 밖을 import하지 않는다(eslint boundaries 초록)
    - 번호 창 표에 C-27 두 줄((4,12)·(9,12))이 있고 초록이다
    - 이동 유니언이 `href` 함수 갈래와 `onPageChange` 갈래 둘이고, 링크 갈래는 `<a>`, 버튼 갈래는 `<button type="button">`을 렌더한다(단위 렌더 단언)
    - `aria-current="page"`가 현재 번호 하나에만 있고 1쪽에 「이전」, 끝 쪽에 「다음」이 없다(단위 렌더 단언)
    - `Pagination.module.css`가 토큰만 쓴다(stylelint 초록)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>세 화면이 같이 쓸 페이지 줄 하나가 §7-16 계약과 C-27 규칙대로 렌더되는 것이 단위 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `lib/kst-date` — KST 오늘·날짜·연도·날짜 더하기 · KST 날짜 ↔ 그날 00:00 왕복(B-25)</name>
  <files>lib/kst-date.ts, test/unit/lib/kst-date.test.ts</files>
  <read_first>
    - `lib/pg-errors.ts`와 `test/unit/lib/pg-errors.test.ts` — `lib/` 순수 모듈과 단위 테스트의 기존 형태
    - `domain/quotes/lines.ts` 440–455행(`toLocaleTimeString(…, { timeZone: "Asia/Seoul" })` — 앱 층 KST 선례)
    - `db/schema/quote-revisions.ts` — `customer_approved_at`이 `timestamp`(시간대 없음)라는 것을 확인만(B-25)
    - `.planning/phases/04-project-quote-ledger/04-RESEARCH.md` 857–861행(Pitfall 7)
  </read_first>
  <behavior>
    - `kstToday(new Date("2026-09-17T14:59:59Z"))` → `2026-09-17` · `kstToday(new Date("2026-09-17T15:00:00Z"))` → `2026-09-18`
    - `kstDateOf(new Date("2026-12-31T15:30:00Z"))` → `2027-01-01` · `kstYear(new Date("2026-12-31T15:00:00Z"))` → `2027`
    - `addDays("2026-09-17", 1)` → `2026-09-18` · `addDays("2026-12-31", 1)` → `2027-01-01` · `addDays("2024-02-28", 1)` → `2024-02-29`
    - `kstDayStart("2026-09-19")` → `2026-09-18T15:00:00.000Z`인 `Date` · `kstDateOf(kstDayStart(d)) === d`가 연말·윤년 포함 표 전체에서 참(B-25)
  </behavior>
  <action>
① `lib/kst-date.ts`(순수, import 없음): `kstToday(now: Date): string`(`Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Seoul", year, month, day })` → `YYYY-MM-DD`) · `kstDateOf(date: Date): string` · `kstYear(now: Date): number` · `addDays(ymd: string, n: number): string`(UTC 자정 산술로 달력 날짜만 더한다) · `kstDayStart(ymd: string): Date`(그날 KST 00:00 = 전날 15:00Z). **현재 시각을 모듈 안에서 읽지 않는다** — 호출자가 `now`를 넘긴다(deps 주입 관례).

② 단위 테스트(`behavior` 넷)를 먼저 쓴다(RED → GREEN). 04-11이 원래 이 모듈을 만들 계획이었다는 것과 이 플랜으로 앞당긴 이유(D11·A-18·B-25)를 SUMMARY에 한 줄로 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/lib/kst-date.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — KST 경계·연말·왕복이 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/kst-date.ts`에 import가 없고 `new Date()`·`Date.now()` 호출이 없다(현재 시각은 인자)
    - 단위 테스트가 경계 두 줄(14:59:59Z / 15:00:00Z) · 연말 넘김 · 윤년 · `kstDayStart` 왕복을 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>뒤 플랜들이 KST 오늘·연도·승인일 왕복을 서버 시간대와 무관하게 한 모듈에서 얻는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 시계·DB 시간대 → 날짜 경계 | 서버·DB 설정이 KST 날짜를 바꾸면 자동 정산·팀 소속·승인일이 하루 틀어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-79 | Tampering | 날짜 경계 | medium | mitigate | 오늘(KST)은 앱이 주입된 시각에서 계산한다 — 모듈이 현재 시각·서버 시간대를 읽지 않는다(Pitfall 7). 경계 전후 두 시각과 왕복을 단위 테스트로 고정한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(`Intl` 내장). `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- 단위 `test/unit/ui/pagination.test.ts` · `test/unit/lib/kst-date.test.ts` 초록
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 목록·리저브·견적 표가 쓸 페이지 줄 하나가 §7-16과 C-27대로 선다
- 뒤 플랜들이 KST 날짜·연도·승인일 왕복을 한 모듈에서 얻는다
- 이 플랜이 04-04의 사람 확인을 기다리지 않는다(D21)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, ui) | `ui/pagination/page-window.ts` — `pageWindow`(C-27 한 쪽 틈 규칙) · `pageRangeText` |
| 컴포넌트(신규, ui) | `ui/pagination/Pagination.tsx` + `Pagination.module.css` — `href` 함수 / `onPageChange` 유니언, `errorCounts` |
| 모듈(신규, lib) | `lib/kst-date.ts` — `kstToday` · `kstDateOf` · `kstYear` · `addDays` · `kstDayStart` |
| 테스트 | `test/unit/ui/pagination.test.ts` · `test/unit/lib/kst-date.test.ts`(신규) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 페이지 줄의 화면 배선과 DOM 감사(04-17 목록 · 04-19 견적 표 · 04-07 계열 리저브 화면) · 범위 글자 쉼표의 `formatCount` 이관(04-09) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 옛 04-17 파일 상한 초과(16) | WARNING | Plan 04-29(신규): `ui/pagination`을 떼어 먼저 만든다 |
| OV-7 / 사용자 D21 — 견적 표와 무관한 부품이 04-04를 기다림 | HIGH | Plan 04-29는 04-08만 기다린다(웨이브 4) |
| C-27 — `…`가 한 쪽을 숨김 | NOTE | Plan 04-29, Task 1 ①: 한 쪽 틈은 번호, (4,12)·(9,12) 단위 줄 |
| A-18 — E2E 날짜 시한폭탄 · D11/D20 — 오늘 기준 소속 팀 | WARNING | Plan 04-29, Task 2: `lib/kst-date`를 04-21·04-20보다 먼저 만든다 |
| B-25 — 승인일 KST 00:00 하루 밀림 | WARNING | Plan 04-29, Task 2: `kstDayStart` 왕복 함수와 단위 표(04-14가 쓴다) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-29-SUMMARY.md` when done
</output>
