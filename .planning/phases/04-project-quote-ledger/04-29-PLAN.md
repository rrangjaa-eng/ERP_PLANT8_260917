---
phase: 04-project-quote-ledger
plan: 29
type: execute
wave: 4
depends_on: ["04-08", "04-46"]
files_modified:
  - ui/pagination/page-window.ts
  - ui/pagination/Pagination.tsx
  - ui/pagination/Pagination.module.css
  - test/unit/ui/pagination.test.ts
  - lib/kst-date.ts
  - test/unit/lib/kst-date.test.ts
  - lib/paging.ts
  - test/unit/lib/paging.test.ts
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - test/unit/design-system-docs.test.ts
autonomous: true
requirements: [PROJ-01, UX-05]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "페이지 줄은 `51–100 / 125건  이전 · 1 2 3 · 다음` 모양이다 — 왼쪽 범위 글자(`--fs-sm --muted`), 오른쪽 이전·번호·다음(3차 모양), 현재 번호는 `--fg` 700 글자이고 링크·버튼이 아니며 `aria-current=\"page\"`를 갖는다(SYSTEM.md §7-16 — 04-08 개정 ⑧, D-91)"
    - "한 쪽뿐이면 페이지 줄 자체가 없다 · 첫 쪽에 「이전」, 끝 쪽에 「다음」을 렌더하지 않는다 · 8쪽 이상이면 첫·끝·현재 ±1만 보이고 사이는 포커스를 받지 않는 `…`다(§7-16)"
    - "건너뛸 쪽이 **하나뿐**이면 `…` 대신 그 번호를 보인다 — 4쪽/12쪽이면 `1 2 3 4 5 … 12`, 9쪽/12쪽이면 `1 … 8 9 10 11 12`(한 쪽을 `…` 뒤에 숨기지 않는다, CEO 리뷰 C-27)"
    - "이동이 주소 이동이면 번호·「이전」·「다음」은 `next/link`의 `<Link href>`(`<a>`로 렌더되는 클라이언트 이동 — 새 문서를 받지 않아 편집 표가 있는 목록(리저브 대장)의 편집 맵이 남는다), 화면 안 전환이면 `<button type=\"button\">`이다 — 한 컴포넌트가 두 갈래를 유니언 prop으로 받고, 링크 갈래는 **모든 사용처**에서 `next/link`다(교차 그룹 계약 5, DR-18, §7-16, §10 「이동이면 `<a>`」, ListEmpty 선례)"
    - "목록·리저브의 링크 갈래는 클라이언트 이동이라 새 쪽이 올 때까지 지금 쪽이 그대로 보이고 진행 바가 없다 · 견적 줄 표는 이미 받은 줄을 화면 안에서 나눠 로딩 구간이 없다(UI-SPEC rev 5 UI Considerations S12 loading, CEO-D17)"
    - "편집 표 쪽은 다른 쪽의 오류 칸 수를 번호 옆 `오류 N`(`--fs-xs --danger`)으로 보이고 그 링크·버튼의 접근 이름이 `N쪽, 오류 M칸`이다(§7-16, S12)"
    - "폰(<700)에서 번호·이전·다음은 `--touch-min` 44×44이고, 한 줄에 안 들어가면 범위 글자가 위 줄로 올라간다(§7-16 폰)"
    - "폰(<700)은 **6쪽부터** 첫·현재·끝 번호만 보이고 사이는 `…`다(한 쪽 틈은 번호 — C-27) — 5쪽 이하는 전부. 그래서 번호 줄(이전·번호·다음, 44×44)이 375 폭의 내용 폭(347px) 안에서 한 줄이다. 이 규칙과 PC의 C-27(한 쪽 틈은 번호)이 **하나의** `DECISIONS.md` 04-29 기록 뒤 `SYSTEM.md` §7-16에 함께 적힌다 — 04-08은 ⑧에서 이 두 문장을 쓰지 않았다(DR-33, 엔지 리뷰 C P3 — 사용자 결정 「6쪽부터 간격 창」)"
    - statement: "1쪽이면 페이지 줄 없음, 2쪽이면 `1 2`, 12쪽이면 생략 부호 — 넓은 창·폰 창 모두 렌더로 확인(UI-SPEC rev 5 UI Considerations S12 zero-one-many — 화면 렌더 실측은 04-17 DOM 감사)"
      verification: backstop
    - statement: "PC 8쪽 이상 생략 · 폰 6쪽부터 창 · 폰 번호 44×44 · 범위 글자가 넘치면 위 줄 · 가로 스크롤 0 — 1280·1024·375 독립 DOM 감사(UI-SPEC rev 5 UI Considerations S12 overflow — 첫 화면 배선인 04-17이 잰다)"
      verification: backstop
    - "쪽 번호 보정·쪽 수 계산·쪽 크기가 한 모듈(`lib/paging.ts`)에 있다 — 목록(04-17)·견적 표(04-19)·리저브(04-07, 그룹 B)가 같은 `clampPage`·`pageCountFrom`과 쪽 크기 상수(`LIST_PAGE_SIZE` 50 · `QUOTE_TABLE_PAGE_SIZE` 30)를 import한다 — 범위 밖 쪽 번호는 마지막 쪽, 숫자가 아니거나 1보다 작으면 1쪽(엔지 리뷰 C §1 P3 — 세 번 따로 만들던 보정을 하나로)"
    - "범위·건수 숫자는 천 단위 쉼표(`/ 1,250건`), 구분은 en dash다(D-95)"
    - "KST 날짜는 주입된 현재 시각에서 애플리케이션이 계산한다 — `2026-09-17T14:59:59Z`는 `2026-09-17`, `2026-09-17T15:00:00Z`는 `2026-09-18`이고 서버·DB 시간대와 무관하다(RESEARCH Pitfall 7 — 04-11·04-20·04-17·04-14가 이 모듈을 쓴다)"
    - "KST 날짜 하나를 그날 KST 00:00의 시각으로 바꾸고 다시 KST 날짜로 읽으면 같은 날짜다 — `timestamp without time zone` 컬럼에 날짜를 저장·재조회해도 하루 밀리지 않는다(CEO 리뷰 B-25, 04-14 고객 승인일)"
  prohibitions:
    - statement: "페이지 줄이 화면마다 따로 만들어져서는 안 된다. 목록(50건)·리저브(50건)·견적 표(30줄)가 같은 모양·같은 접근성 계약을 가져야 사용자가 한 번 배운 것으로 세 화면을 넘긴다 — 모양이 다른 두 번째 페이지 줄을 만들지 않는다(§7-16 「한 컴포넌트」)"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/pagination/page-window.ts"
      provides: "보이는 번호 창(첫·끝·현재 ±1, 한 쪽 틈은 번호로) · 범위 문구 — 순수 함수"
      exports: ["pageWindow", "pageRangeText"]
    - path: "ui/pagination/Pagination.tsx"
      provides: "번호 페이지 줄 — href 함수 / onPageChange 유니언, 쪽별 오류 수"
      exports: ["Pagination"]
    - path: "lib/kst-date.ts"
      provides: "주입된 현재 시각에서 KST 날짜·연도를 계산하는 순수 함수 + KST 날짜 ↔ 그날 00:00 시각 왕복"
      exports: ["kstToday", "kstDateOf", "kstYear", "addDays", "kstDayStart"]
    - path: "lib/paging.ts"
      provides: "쪽 번호 보정 · 쪽 수 · 쪽 크기 상수 — 목록·견적 표·리저브 공용 순수 함수"
      exports: ["clampPage", "pageCountFrom", "LIST_PAGE_SIZE", "QUOTE_TABLE_PAGE_SIZE"]
    - path: "test/unit/ui/pagination.test.ts"
      provides: "번호 창 표(C-27 포함) · 범위 문구 · 정적 렌더(유니언·aria-current·첫/끝·오류 수)"
      min_lines: 40
  key_links:
    - from: "ui/pagination/Pagination.tsx"
      to: "ui/pagination/page-window.ts"
      via: "번호 창과 범위 문구를 순수 함수에서 받는다 — 컴포넌트가 창을 계산하지 않는다"
      pattern: "pageWindow"
    - from: "ui/pagination/Pagination.tsx"
      to: "next/link"
      via: "링크 갈래의 번호·이전·다음이 `next/link` `<Link>`다 — 목록·리저브의 쪽 이동이 문서 다시 받기가 아니라 클라이언트 이동이다(계약 5, DR-18)"
      pattern: "next/link"
---

<objective>
**As a** 목록·리저브·견적 표를 넘겨 보는 경영관리·기획 PM, **I want to** 세 화면이 같은 모양의 번호 페이지 줄을 쓰고 건너뛴 쪽이 하나뿐이면 그 번호가 그냥 보이기를, **so that** 한 번 배운 페이지 줄로 세 화면을 넘기고 `…` 뒤에 숨은 한 쪽을 찾느라 헤매지 않는다. 그리고 뒤 플랜들이 KST 날짜를 한 모듈에서 얻는다.

**2026-09-23 CEO 리뷰 반영 — 공용 부품 둘을 앞당긴 플랜.** (1) 옛 04-17(16파일)은 목록 개정과 `ui/pagination` 신설을 한 플랜에 담아 파일 상한을 넘었다. 페이지 줄은 견적 표와 무관한 순수 `ui` 부품이라 **04-04 확인과 분리해 먼저** 만든다(사용자 D21·OV-7) — 목록(04-17)·견적 표(04-19)·리저브(04-07 — 그룹 B)가 쓴다. C-27(한 쪽 틈을 `…`로 숨김)을 번호 창 규칙에 넣는다. (2) 옛 04-11이 만들던 `lib/kst-date`를 앞당긴다 — 사용자 D11·D20(팀장은 자기 팀만, 「오늘 기준 발령 이력으로 소속 팀 판정」)을 04-20이 전환 규칙에 넣으려면 04-11보다 먼저 KST 오늘이 필요하고, A-18(E2E 날짜 시한폭탄)을 막으려면 04-21의 E2E가 처음부터 `kstToday` 기준 상대 날짜를 써야 한다. B-25(승인일 `timestamp without time zone` 하루 밀림)의 왕복 함수도 여기 둔다.

Purpose: SYSTEM.md §7-16(04-08 개정 ⑧)을 코드로 옮긴 유일한 페이지 줄과, 날짜 경계가 서버 시간대에 흔들리지 않는 KST 모듈 하나.

Output: `ui/pagination`(순수 번호 창 + 표현 컴포넌트 + CSS, 링크 갈래 = `next/link`) · `lib/kst-date` · `lib/paging` · 단위 3 · DECISIONS 04-29 기록(C-27 + 폰 창)과 §7-16 두 문장.

**2026-09-23 디자인 리뷰 반영(UI-SPEC rev 5 — 재계획 그룹 C):** (a) **교차 그룹 계약 5 · DR-18:** 링크 갈래는 모든 사용처에서 `next/link` `<Link>`다 — 리저브 대장(04-42)이 쪽을 넘어도 편집 맵이 남는다(목록 **행** 링크는 이 계약 밖 — 엔지 리뷰 「범위 밖」 그대로). (b) **DR-33:** Task 3의 기록·§7-16이 폰 6쪽 창뿐 아니라 **C-27(PC 한 쪽 틈은 번호)**도 함께 적는다 — 04-08이 ⑧을 옮길 때 이 두 문장을 빼 두었다. (c) 화면 안 전환 뒤 포커스(DR-23)는 이 표현 컴포넌트가 아니라 쪽 상태를 가진 `ui/table`(04-19)이 옮긴다 — 이 컴포넌트는 `onPageChange`만 부른다.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`):** (a) 엔지 리뷰 C §1 P3 — 쪽 번호 보정이 목록(04-17 도메인)·리저브(04-07이 목록 모듈을 import)·견적 표(04-19 `ui/table`)에서 세 번 만들어질 뻔했다 — `lib/paging.ts` 하나(`clampPage`·`pageCountFrom`·쪽 크기 상수)로 여기서 먼저 만든다(domain·ui 둘 다 `lib`를 import할 수 있다). (b) 엔지 리뷰 C P3 — 375에서 7쪽 번호 + 이전·다음은 9 × 44 = 396px로 내용 폭 347px을 넘어 번호 줄이 꺾인다. 사용자 결정대로 **폰은 6쪽부터 간격 창**(첫·현재·끝)을 쓰고 §7-16에 적는다(기록 → 규칙 → 코드 순서, CLAUDE.md 프론트엔드 규칙).

**실행 규율:** 구현 전에 superpowers `test-driven-development`, 실패를 쫓기 전에 `systematic-debugging`, 「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬 이름과 `model`을 프롬프트에 명시한다. 시간은 인자로 주입한다(가짜 타이머 없음). 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md rev 5(절 이름으로 찾고 아래는 2026-09-23 현재 행) — 「SYSTEM.md 개정」 ⑧(840–865행 — §7-16 모양·현재 번호·렌더하지 않는 이전/다음·생략(C-27 847–848 · 폰 창 849–850)·요소 `next/link`·포커스·ARIA·폰 · DECISIONS 한 줄 863) · UI Considerations S12(576–585행) · 적용 규칙 S12(1413–1428행) · Component Inventory `Pagination` 행(148행)
# docs/designs/plant8-erp-phase4-design-review-260923.md 54행(DR-18) · 69행(DR-33)
# docs/design/SYSTEM.md §7-16(04-08 이후 — Grep `### 7-16`)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 723–751행(§2 D-76 — KST 경계 · 서버 시간대 Gap) · 857–861행(Pitfall 7)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1977–1980행(C-27) · 1322–1327행(B-25)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 없다 — 페이지 줄은 UI-SPEC S12로 분류돼 있고(D-91), 옛 04-17 Task 2의 컴포넌트 truths·`behavior`를 그대로 옮기고 C-27만 더했다. S12의 화면별 상태(목록 링크 갈래 · 견적 표 버튼 갈래 · 범위 밖 번호 보정)는 각 화면 플랜(04-17·04-19·04-07)이 맡는다. `lib/kst-date`의 truths는 옛 04-11 Task 1의 `behavior` 앞 세 줄을 옮겼다.

**계획 단계 가정(표면화):**
- **DOM 감사의 자리:** 이 플랜에는 페이지 줄을 그리는 화면이 없다. 모양·폰 44×44·가로 스크롤 0·링크 갈래의 클라이언트 이동의 독립 DOM 감사(1280·1024·375 — 교차 그룹 계약 6)는 처음 화면에 꽂는 04-17이 한다 — 여기서는 정적 렌더 단위 테스트와 stylelint(토큰만)로 계약을 고정한다.
- **숫자 서식:** `lib/format-number`(04-09)는 이 플랜 뒤에 생긴다(04-09는 견적 표 파일을 고쳐 04-04 뒤에 선다). 범위 글자의 쉼표는 `Intl.NumberFormat("ko-KR")` 한 인스턴스로 만들고, 04-09 Task 2가 표시 지점 이관 목록에 이 파일을 넣어 `formatCount`로 바꾼다(D-95 「한 모듈」 — 04-09 `files_modified`에 이 파일이 있다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 번호 창 순수 함수 → Pagination 컴포넌트 → 정적 렌더 단언(C-27 포함)</name>
  <files>ui/pagination/page-window.ts, ui/pagination/Pagination.tsx, ui/pagination/Pagination.module.css, test/unit/ui/pagination.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정」 ⑧(840–865행 — §7-16 전문) · UI Considerations S12(576–585행)
    - `docs/design/SYSTEM.md` §7-16(04-08 개정 ⑧ 반영본) · §10(이동이면 `<a>`) · §6-0 중단점(폰 <700)
    - `ui/list-empty/ListEmpty.tsx`(전문 — `href`/`onClick` 유니언과 3차 버튼 클래스 선례) · `ui/list-empty/ListEmpty.module.css`(3차 모양 규칙)
    - `docs/design/tokens.css` — `--touch-min`·`--fs-sm`·`--fs-xs`·`--fg`·`--muted`·`--danger`·`--accent`가 있는지 확인만(새 토큰을 만들지 않는다)
    - 기존 단위 테스트 중 컴포넌트를 `react-dom/server`로 렌더하는 것(Grep `renderToStaticMarkup`) — 같은 방식을 따른다
  </read_first>
  <behavior>
    - `pageWindow(1, 1)` → `[1]` · `pageWindow(2, 3)` → `[1, 2, 3]` · `pageWindow(1, 7)` → `[1..7]` · `pageWindow(5, 12)` → `[1, "gap", 4, 5, 6, "gap", 12]` · `pageWindow(1, 12)` → `[1, 2, "gap", 12]` · `pageWindow(3, 12)` → `[1, 2, 3, 4, "gap", 12]` · `pageWindow(12, 12)` → `[1, "gap", 11, 12]`
    - C-27: `pageWindow(4, 12)` → `[1, 2, 3, 4, 5, "gap", 12]`(1과 3 사이 한 쪽 2를 번호로) · `pageWindow(9, 12)` → `[1, "gap", 8, 9, 10, 11, 12]`(10과 12 사이 한 쪽 11을 번호로)
    - `pageRangeText({ page: 2, pageSize: 50, total: 125, unit: "건" })` → `51–100 / 125건` · `{ page: 3, … }` → `101–125 / 125건` · `{ page: 2, pageSize: 30, total: 142, unit: "줄" }` → `31–60 / 142줄` · 총 1,250 → `/ 1,250건`
    - 렌더: `pageCount` 1이면 아무것도 렌더하지 않는다 · 1쪽에 「이전」 없음 · 끝 쪽에 「다음」 없음 · 현재 번호가 링크·버튼이 아니고 `aria-current="page"` · `href` 갈래는 `next/link` `<Link>`가 그린 `<a href="…">`, `onPageChange` 갈래는 `<button type="button">` · `errorCounts: { 2: 2 }`이면 2쪽 번호 옆 `오류 2`와 접근 이름 `2쪽, 오류 2칸` · `…`는 링크·버튼이 아니다
  </behavior>
  <action>
① **`ui/pagination/page-window.ts`**(순수, `ui`·`lib`만 import): `pageWindow(page, pageCount)` — 7쪽 이하면 전부, 8쪽 이상이면 첫·끝·현재 ±1과 사이 `"gap"`. **틈이 정확히 한 쪽이면 `"gap"` 대신 그 번호를 넣는다(C-27).** `pageRangeText({ page, pageSize, total, unit })` — 시작·끝·총수에 쉼표(`<probe_fallback>` — `Intl.NumberFormat("ko-KR")` 인스턴스 하나), 구분은 en dash. `behavior` 앞 셋을 단위 테스트로 먼저 쓴다(RED → GREEN).

② **`ui/pagination/Pagination.tsx`**(훅 없는 표현 컴포넌트 — 서버·클라이언트 어디서나 렌더): props `label`(표 이름 — `<nav aria-label="{label} 페이지">`) · `page` · `pageCount` · `rangeText` · `errorCounts?`(쪽 → 오류 칸 수, 편집 표 전용) + 이동 유니언 `{ href: (page: number) => string }` | `{ onPageChange: (page: number) => void }`(ListEmpty 선례 — 이동이면 링크, 화면 안 전환이면 `<button type="button">`, §10). **링크 갈래는 `next/link`의 `Link`로 그린다**(교차 그룹 계약 5, DR-18 — 목록·리저브의 쪽 이동이 문서 다시 받기가 아니라 클라이언트 이동이 되어 리저브 편집 맵이 남는다). `Link`는 서버 컴포넌트 안에서도 렌더되므로 목록 서버 페이지가 `href` 함수를 넘겨도 된다(함수는 이 컴포넌트 안에서 문자열이 된다). 화면 안 전환 뒤 포커스 이동(DR-23)은 이 컴포넌트가 하지 않는다 — 쪽 상태를 가진 `ui/table`(04-19)이 한다. `pageCount <= 1`이면 null. 모양은 §7-16 그대로: 왼쪽 범위 글자 `--fs-sm --muted`, 오른쪽 `이전 · 번호들 · 다음`(3차 = `--accent` 글자 + 밑줄, `--fs-sm` 600) · 현재 번호 `--fg` 700 밑줄 없는 `<span aria-current="page">` · `…`는 포커스 없는 글자 · 오류 수가 있는 번호 옆 `오류 N`(`--fs-xs --danger`)과 그 링크·버튼의 접근 이름 `N쪽, 오류 M칸`. 아이콘·`‹ ›` 글리프 없음.

③ **`Pagination.module.css`**: 토큰만(D-20). 폰(<700)에서 번호·이전·다음은 `--touch-min` 44×44, 넘치면 범위 글자가 위 줄로(`flex-wrap`). 새 중단점 없음(§6-0의 700만).

④ 렌더 단위 테스트(`behavior` 마지막 줄)는 `test/unit/ui/pagination.test.ts`에 정적 렌더로 쓴다(새 의존성 없이 — 기존 단위 테스트가 컴포넌트를 렌더하는 방식을 따른다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/pagination.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 번호 창(C-27 포함)·범위 문구·렌더 규칙이 §7-16과 다르다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(boundaries 포함)·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — `ui`가 `domain`·`app`을 import했거나 토큰 밖 값을 썼다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ui/pagination/Pagination.tsx`가 앱 계층 중 `ui`·`lib` 밖(`app`·`domain`·`repositories`·`db`)을 import하지 않고(eslint boundaries 초록), 링크 갈래가 `next/link`의 `Link`다(Grep `from "next/link"` 1) — 링크 갈래에 맨 `<a href` 리터럴이 없다
    - 번호 창 표에 C-27 두 줄((4,12)·(9,12))이 있고 초록이다
    - 이동 유니언이 `href` 함수 갈래와 `onPageChange` 갈래 둘이고, 링크 갈래는 `<a>`, 버튼 갈래는 `<button type="button">`을 렌더한다(단위 렌더 단언)
    - `aria-current="page"`가 현재 번호 하나에만 있고 1쪽에 「이전」, 끝 쪽에 「다음」이 없다(단위 렌더 단언)
    - `Pagination.module.css`가 토큰만 쓴다(stylelint 초록)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>세 화면이 같이 쓸 페이지 줄 하나가 §7-16 계약과 C-27 규칙대로 렌더되는 것이 단위 테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `lib/kst-date` — KST 오늘·날짜·연도·날짜 더하기 · KST 날짜 ↔ 그날 00:00 왕복(B-25) + `lib/paging` — 쪽 보정·쪽 수·쪽 크기(엔지 리뷰 C)</name>
  <files>lib/kst-date.ts, test/unit/lib/kst-date.test.ts, lib/paging.ts, test/unit/lib/paging.test.ts</files>
  <read_first>
    - `lib/pg-errors.ts`와 `test/unit/lib/pg-errors.test.ts` — `lib/` 순수 모듈과 단위 테스트의 기존 형태
    - `domain/quotes/lines.ts` 440–455행(`toLocaleTimeString(…, { timeZone: "Asia/Seoul" })` — 앱 층 KST 선례)
    - `db/schema/quote-revisions.ts` — `customer_approved_at`이 `timestamp`(시간대 없음)라는 것을 확인만(B-25)
    - `.planning/phases/04-project-quote-ledger/04-RESEARCH.md` 857–861행(Pitfall 7)
  </read_first>
  <behavior>
    - `kstToday(new Date("2026-09-17T14:59:59Z"))` → `2026-09-17` · `kstToday(new Date("2026-09-17T15:00:00Z"))` → `2026-09-18`
    - `kstDateOf(new Date("2026-12-31T15:30:00Z"))` → `2027-01-01` · `kstYear(new Date("2026-12-31T15:00:00Z"))` → `2027`
    - `addDays("2026-09-17", 1)` → `2026-09-18` · `addDays("2026-12-31", 1)` → `2027-01-01` · `addDays("2024-02-28", 1)` → `2024-02-29`
    - `kstDayStart("2026-09-19")` → `2026-09-18T15:00:00.000Z`인 `Date` · `kstDateOf(kstDayStart(d)) === d`가 연말·윤년 포함 표 전체에서 참(B-25)
    - `clampPage("99", 3)` → 3 · `clampPage("abc", 3)` → 1 · `clampPage("0", 3)` → 1 · `clampPage("-1", 3)` → 1 · `clampPage(undefined, 0)` → 1 · `clampPage(2, 1)` → 1(행이 줄어 쪽이 사라짐 — 04-19) · `clampPage("2.5", 3)` → 1(정수 아님)
    - `pageCountFrom(125, 50)` → 3 · `pageCountFrom(0, 50)` → 0 · `pageCountFrom(50, 50)` → 1 · `pageCountFrom(51, 50)` → 2 · `pageCountFrom(31, 30)` → 2
    - `LIST_PAGE_SIZE === 50` · `QUOTE_TABLE_PAGE_SIZE === 30`(D-91)
  </behavior>
  <action>
① `lib/kst-date.ts`(순수, import 없음): `kstToday(now: Date): string`(`Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Seoul", year, month, day })` → `YYYY-MM-DD`) · `kstDateOf(date: Date): string` · `kstYear(now: Date): number` · `addDays(ymd: string, n: number): string`(UTC 자정 산술로 달력 날짜만 더한다) · `kstDayStart(ymd: string): Date`(그날 KST 00:00 = 전날 15:00Z). **현재 시각을 모듈 안에서 읽지 않는다** — 호출자가 `now`를 넘긴다(deps 주입 관례).

② 단위 테스트(`behavior` 앞 넷)를 먼저 쓴다(RED → GREEN). 04-11이 원래 이 모듈을 만들 계획이었다는 것과 이 플랜으로 앞당긴 이유(D11·A-18·B-25)를 SUMMARY에 한 줄로 적는다.

③ **`lib/paging.ts`(순수, import 없음 — 엔지 리뷰 C §1 P3).** `clampPage(raw: string | number | undefined, pageCount: number): number`(정수 문자열·숫자만 받고 그 밖은 1 · 1 미만은 1 · `pageCount`를 넘으면 `max(pageCount, 1)`) · `pageCountFrom(total: number, pageSize: number): number`(`Math.ceil(total / pageSize)`, 0건이면 0) · `LIST_PAGE_SIZE = 50`(목록·리저브 — D-91) · `QUOTE_TABLE_PAGE_SIZE = 30`(견적 표 — D-91). `test/unit/lib/paging.test.ts`에 `behavior` 뒤 셋을 먼저 쓴다. 쓰는 곳: 04-17(목록 — 구간 건수 합은 목록 쪽 함수가 더한 뒤 `pageCountFrom`), 04-19(`ui/table` 쪽 보정 — 매 렌더), 04-07(리저브 — 그룹 B). 이 파일 머리 주석에 세 쓰는 곳을 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/lib/kst-date.test.ts test/unit/lib/paging.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — KST 경계·연말·왕복이나 쪽 보정·쪽 수가 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/kst-date.ts`에 import가 없고 `new Date()`·`Date.now()` 호출이 없다(현재 시각은 인자)
    - 단위 테스트가 경계 두 줄(14:59:59Z / 15:00:00Z) · 연말 넘김 · 윤년 · `kstDayStart` 왕복을 단언한다
    - `lib/paging.ts`가 `clampPage`·`pageCountFrom`·`LIST_PAGE_SIZE`·`QUOTE_TABLE_PAGE_SIZE`를 내보내고 import가 없으며 단위 표가 초록이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>뒤 플랜들이 KST 오늘·연도·승인일 왕복을 서버 시간대와 무관하게 한 모듈에서 얻고, 세 화면이 쪽 보정·쪽 수·쪽 크기를 한 모듈에서 얻는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 번호 창 규칙 기록 — PC 한 쪽 틈은 번호(C-27) + 폰 6쪽부터 첫·현재·끝(엔지 리뷰 C P3) · 기록 → §7-16 두 문장 → 폰 창 코드 → 단언(DR-33)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, ui/pagination/page-window.ts, ui/pagination/Pagination.tsx, ui/pagination/Pagination.module.css, test/unit/ui/pagination.test.ts, test/unit/design-system-docs.test.ts</files>
  <read_first>
    - `docs/design/DECISIONS.md` 끝의 04-08 기록 형식(`## 2026-09-23 — Phase 4(04-08) …`)
    - `docs/design/SYSTEM.md` §7-16(04-08 개정 ⑧ — Grep `### 7-16`) · §6-0 중단점 · 폰 컨테이너 여백(SYSTEM.md 182행 근처 — 좌우 14px)
    - `test/unit/design-system-docs.test.ts`의 04-08 describe(§7-16 단언 형태) — CI 경로 필터 때문에 문서 변경과 같은 커밋에 넣는 이유(파일 머리 주석)
    - `ui/pagination/page-window.ts`·`Pagination.tsx`·`Pagination.module.css`(Task 1 산출물)
    - `docs/designs/plant8-erp-phase4-eng-review-260923.md` 부록 C §2 「at 375 px, 7 page numbers …」
  </read_first>
  <behavior>
    - `pageWindow(4, 5, { compact: true })` → `[1, 2, 3, 4, 5]`(5쪽 이하 전부)
    - `pageWindow(6, 12, { compact: true })` → `[1, "gap", 6, "gap", 12]` · `pageWindow(1, 12, { compact: true })` → `[1, "gap", 12]` · `pageWindow(3, 12, { compact: true })` → `[1, 2, 3, "gap", 12]`(한 쪽 틈 2는 번호 — C-27) · `pageWindow(4, 6, { compact: true })` → `[1, "gap", 4, 5, 6]`
    - `compact` 없으면 Task 1 표와 같다(넓은 창 무변경)
    - 렌더: 넓은 창 목록과 폰 창 목록이 각각 하나이고 폰 창은 700 미만에서만, 넓은 창은 700 이상에서만 보인다(CSS 클래스 — 단위는 두 목록의 존재와 클래스만) · 두 목록 모두 현재 번호에 `aria-current="page"`
    - 문서: SYSTEM.md §7-16에 C-27 문장(「건너뛸 쪽이 정확히 하나면 `…` 대신 그 번호」 + 예 `1 2 3 4 5 … 12` · `1 … 8 9 10 11 12`)과 「폰(<700)은 … 6쪽부터」 문장이 각 한 줄 있고(`6쪽부터` 한 줄 = UI-SPEC rev 5 「재실행 가능한 확인」 14번 줄의 담당), DECISIONS.md에 `## 2026-09-23 — Phase 4(04-29)` 머리글이 한 줄 있으며 그 본문이 C-27과 폰 창을 둘 다 말한다(DR-33)
  </behavior>
  <action>
① **기록 먼저(DR-33 — 한 항목에 둘).** `docs/design/DECISIONS.md` 끝에 `## 2026-09-23 — Phase 4(04-29): 페이지 줄 번호 창 — PC 한 쪽 틈은 번호(C-27) · 폰 6쪽부터 첫·현재·끝 (CEO C-27 · 엔지 리뷰 C P3 · DR-33)` 항목을 04-08 기록 형식(무엇을 · 왜 · 범위 · 되돌리는 법)으로 더한다 — 왜: C-27은 「`…` 뒤에 한 쪽만 숨으면 그 한 쪽을 찾느라 헤맨다」(지금까지 04-29 코드에만 있고 DECISIONS·§7-16에 없었다 — 디자인 리뷰 #33), 폰 창은 375 내용 폭 347px 대 7쪽 번호 + 이전·다음 9 × 44 = 396px. 이 커밋(`docs:`)이 ②보다 먼저다.

② **규칙.** `docs/design/SYSTEM.md` §7-16의 생략 줄(04-08이 쓴 「PC는 7쪽 이하면 전부, 8쪽 이상이면 첫 · 끝 · 현재 ±1과 사이 `…`」) 뒤에 UI-SPEC rev 5 ⑧ 847–850행 원문대로 두 문장을 더한다 — 「**건너뛸 쪽이 정확히 하나면 `…` 대신 그 번호를 보인다**(4/12 → `1 2 3 4 5 … 12`, 9/12 → `1 … 8 9 10 11 12` — 한 쪽을 `…` 뒤에 숨기지 않는다)」와 「폰(<700)은 5쪽 이하면 전부, **6쪽부터 첫 · 현재 · 끝**과 사이 `…`(한 쪽 틈은 번호) — 번호 줄이 한 줄에 든다」. 새 토큰·폭 값을 만들지 않는다. `test/unit/design-system-docs.test.ts`에 세 단언(§7-16 C-27 문장 · 폰 문장 `6쪽부터` 한 줄 · DECISIONS 04-29 머리글 한 줄과 본문의 `C-27`·`6쪽부터`)을 **같은 커밋**에 더한다(CI 경로 필터 — 그 파일 머리 주석).

③ **코드.** `pageWindow(page, pageCount, opts?: { compact?: boolean })` — `compact`면 5쪽 이하 전부, 6쪽부터 첫·현재·끝과 사이 `"gap"`(한 쪽 틈은 번호). `Pagination`은 한 `<nav>` 안에 넓은 창 목록과 폰 창 목록을 둘 다 렌더하고 `Pagination.module.css`가 700 중단점에서 하나만 보이게 한다(`display: none`은 접근성 트리·탭 순서에서도 빠진다 — 보이는 목록 하나만 읽힌다). 범위 글자·이전·다음은 하나씩. 새 중단점 없음(§6-0의 700만). `behavior` 앞 넷을 단위로 먼저 쓴다(RED → GREEN).

④ 화면 배선 뒤 375에서 번호 줄이 한 줄인지의 DOM 실측은 처음 화면에 꽂는 04-17의 독립 DOM 감사가 12쪽 보기로 잰다(`<probe_fallback>` DOM 감사의 자리).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/pagination.test.ts test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 폰 창 표·두 목록 렌더·§7-16 문장·기록 머리글 중 하나가 어긋났거나 기존 문서 단언이 깨졌다</fails_when>
    <automated>git diff --stat d6b41cf -- docs/design/tokens.css &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>diff 출력이 비어 있지 않거나(토큰 파일이 바뀌었다) eslint·stylelint·tsc 중 error가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - DECISIONS.md에 `## 2026-09-23 — Phase 4(04-29)` 머리글이 한 줄이고 그 커밋이 SYSTEM.md 변경 커밋보다 앞선다(`git log --format=%s -- docs/design/DECISIONS.md docs/design/SYSTEM.md`)
    - SYSTEM.md §7-16에 C-27 문장과 폰 6쪽 규칙 문장이 각 한 줄 있고 design-system-docs 테스트가 셋(두 문장 + 기록)을 단언한다 — 04-08의 「재실행 가능한 확인」 14번 줄(`6쪽부터`)이 이 단언으로 덮인다(DR-33)
    - `pageWindow`의 `compact` 표(5쪽 이하 전부 · 6쪽부터 첫·현재·끝 · 한 쪽 틈 번호)가 초록이고 넓은 창 표(Task 1)가 수정 없이 초록이다
    - `Pagination.module.css`가 700 중단점 하나로 두 목록 중 하나만 보이게 하고 토큰만 쓴다(stylelint 초록) · `docs/design/tokens.css` diff 0줄
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>폰 페이지 줄이 6쪽부터 짧은 번호 창을 써 375에서 한 줄에 들고, 그 규칙과 PC의 C-27이 하나의 기록과 §7-16에 먼저 적혔다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 시계·DB 시간대 → 날짜 경계 | 서버·DB 설정이 KST 날짜를 바꾸면 자동 정산·팀 소속·승인일이 하루 틀어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-79 | Tampering | 날짜 경계 | medium | mitigate | 오늘(KST)은 앱이 주입된 시각에서 계산한다 — 모듈이 현재 시각·서버 시간대를 읽지 않는다(Pitfall 7). 경계 전후 두 시각과 왕복을 단위 테스트로 고정한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(`Intl` 내장). `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- 단위 `test/unit/ui/pagination.test.ts` · `test/unit/lib/kst-date.test.ts` 초록
- 단위 `test/unit/lib/paging.test.ts` · `test/unit/design-system-docs.test.ts` 초록 · `docs/design/tokens.css` diff 0줄
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
</verification>

<success_criteria>
- 목록·리저브·견적 표가 쓸 페이지 줄 하나가 §7-16과 C-27대로 선다
- 뒤 플랜들이 KST 날짜·연도·승인일 왕복을 한 모듈에서 얻는다
- 이 플랜이 04-04의 사람 확인을 기다리지 않는다(D21)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, ui) | `ui/pagination/page-window.ts` — `pageWindow`(C-27 한 쪽 틈 규칙) · `pageRangeText` |
| 컴포넌트(신규, ui) | `ui/pagination/Pagination.tsx` + `Pagination.module.css` — `href` 함수(링크 갈래 = `next/link` `<Link>`, 교차 그룹 계약 5 · DR-18) / `onPageChange`(`<button>`) 유니언, `errorCounts` |
| 모듈(신규, lib) | `lib/kst-date.ts` — `kstToday` · `kstDateOf` · `kstYear` · `addDays` · `kstDayStart` |
| 모듈(신규, lib) | `lib/paging.ts` — `clampPage` · `pageCountFrom` · `LIST_PAGE_SIZE`(50) · `QUOTE_TABLE_PAGE_SIZE`(30) — 04-17·04-19·04-07(그룹 B) 공용 |
| 문서 | `docs/design/DECISIONS.md` 04-29 기록(C-27 + 폰 6쪽 창 — DR-33) · `docs/design/SYSTEM.md` §7-16 두 문장(C-27 · 폰 6쪽 규칙) |
| 함수(변경) | `pageWindow(page, pageCount, { compact })` — 폰 창 · `Pagination` 두 목록(700 중단점) |
| 테스트 | `test/unit/ui/pagination.test.ts` · `test/unit/lib/kst-date.test.ts`(신규) |
| 이 플랜에서 빠져 다른 플랜으로 간 것 | 페이지 줄의 화면 배선과 DOM 감사(04-17 목록 · 04-19 견적 표 · 04-07 계열 리저브 화면) · 범위 글자 쉼표의 `formatCount` 이관(04-09) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 옛 04-17 파일 상한 초과(16) | WARNING | Plan 04-29(신규): `ui/pagination`을 떼어 먼저 만든다 |
| OV-7 / 사용자 D21 — 견적 표와 무관한 부품이 04-04를 기다림 | HIGH | Plan 04-29는 04-08만 기다린다(웨이브 4) |
| C-27 — `…`가 한 쪽을 숨김 | NOTE | Plan 04-29, Task 1 ①: 한 쪽 틈은 번호, (4,12)·(9,12) 단위 줄 |
| A-18 — E2E 날짜 시한폭탄 · D11/D20 — 오늘 기준 소속 팀 | WARNING | Plan 04-29, Task 2: `lib/kst-date`를 04-21·04-20보다 먼저 만든다 |
| B-25 — 승인일 KST 00:00 하루 밀림 | WARNING | Plan 04-29, Task 2: `kstDayStart` 왕복 함수와 단위 표(04-14가 쓴다) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 C §1 P3 — 쪽 보정 세 번 만듦(목록·리저브·견적 표) | NOTE | Plan 04-29, Task 2 ③ `lib/paging.ts`(`clampPage`·`pageCountFrom`·쪽 크기 상수) — 04-17·04-19·04-07이 import |
| 엔지 리뷰 C P3 — 375에서 7쪽 번호 줄이 넘침 → 사용자 「6쪽부터 간격 창, §7-16에 적기」 | NOTE | Plan 04-29, Task 3 기록 → §7-16 → `pageWindow` `compact` → 두 목록 렌더 · 단언 (실측은 04-17 DOM 감사) |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 98acc74 (plan-design-review, `docs/designs/plant8-erp-phase4-design-review-260923.md` · UI-SPEC rev 5 재계획 그룹 C)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-18 — 리저브 쪽 번호가 일반 `<a>`라 쪽 이동에 편집 손실(교차 그룹 계약 5) | MEDIUM | Plan 04-29, truth · Task 1 ② 링크 갈래 = `next/link` `Link`(모든 사용처) · 렌더 단언 · key_link |
| DR-33 — C-27이 코드에만 있고 DECISIONS·§7-16에 없음 | LOW | Plan 04-29, Task 3 ①②: 기록 한 항목에 C-27 + 폰 창 · §7-16 두 문장 · 단언(「재실행 가능한 확인」 14번 줄 담당) |
| S12 UI Considerations 끌어올리기 | NOTE | truths — loading(클라이언트 이동 · 진행 바 없음) · zero-one-many·overflow backstop(실측 04-17) |
| 교차 그룹 계약 6 — DOM 감사 폭 1024 | NOTE | `<probe_fallback>` 04-17 감사 폭 1280·1024·375 |
| DR-1 — rev 4 행 범위 인용 | NOTE | context·read_first를 rev 5 ⑧·S12 절 이름 + 현재 행으로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 화면 안 전환 뒤 포커스(DR-23) | 이 플랜 범위 아님 — 쪽 상태를 가진 `ui/table`(04-19)이 한다 |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W2 — 04-08 분할(신설 04-46) | NOTE | `depends_on`에 `04-46` 추가 — 04-08 분할로 ⑦·⑮ 기록·§7-17·문서 단언이 04-46으로 갔고, 이 플랜과 `docs/design/DECISIONS.md`·`docs/design/SYSTEM.md`·`test/unit/design-system-docs.test.ts`를 같이 고친다(옛 순서 04-08 → 04-29 유지). §7-16·「재실행 가능한 확인」 14번은 04-08에 남아 본문 포인터는 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-29-SUMMARY.md` when done
</output>
