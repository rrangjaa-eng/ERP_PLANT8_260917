---
phase: 04-project-quote-ledger
plan: 30
type: execute
wave: 14
depends_on: ["04-12"]
files_modified:

  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - ui/table/Table.tsx
  - ui/table/types.ts
  - ui/table/use-grid-keyboard.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - test/e2e/quote-edit-scope.spec.ts

autonomous: true
requirements: [PROJ-02, UX-05, UX-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "정산 프로젝트를 담당 PM이 열면 기존 줄은 실행가 셀만 편집에 들어가고 나머지 셀은 `aria-readonly`이며, 셀 단계는 서버 DTO(`cellEditability`)에서 온다 — 화면은 상태 문자열을 해석하지 않는다(D-78 — 사용자 D10 개정, §7-3 (가), UX-06)"
    - "정산의 담당 PM은 「줄 추가」·`Ctrl+Enter`로 새 줄을 만들 수 있고, 새 줄은 소분류·항목·거래처·비고·실행가만 편집에 들어가며 수량 셀은 `1`, 단가 셀은 원화 `0`으로 잠겨 있다 — 견적가 0(사용자 D10·D12, 엔지 리뷰 A §2 P1 — 04-12의 `quoteCellsZero`와 같은 정의)"
    - "정산에서는 줄 삭제 · 줄 이동 · 줄 복제 컨트롤이 렌더되지 않고 `Delete`·`Alt+↑↓`·`Ctrl+D`는 아무 일도 하지 않는다 — 비활성 버튼을 세우지 않는다(사용자 D10, §7-9)"
    - "정산 단계에서 이 페이즈가 만드는 것은 상태(04-06)와 편집 범위(D-78)뿐이다 — 정산 프로젝트 상세에 매출 세금계산서 발행 요청·지출결의 마감 버튼이나 동작이 없다(D-77 — 발행 요청 흐름과 지출결의 마감은 Phase 5·6)"
    - "완료 프로젝트는 PM에게 모든 셀이 잠김이고 표가 편집 격자가 아니라 캡션 있는 읽기 표(흰 머리글)로 렌더되며 1차 저장 버튼 · 표 위 잠김 줄 · 힌트 줄이 없다 — 흰 머리글이 잠김을 말한다(D-47, §7-3 (가), U-5, P0)"
    - "표 위 잠김 줄은 잠긴 셀 편집 시도의 이유와 **한 문자열**이다 — 정산 `정산 · 실행가와 새 줄만` · 완료 `완료 · 견적 줄 잠김`(편집 셀이 있는 격자 — 경영관리, 04-23 이후 — 에서만). 둘 다 04-12의 `quoteLockReason`이 만든다. 줄이 0개이거나 편집 셀이 0인 읽기 표에는 이 줄을 그리지 않는다(DR-2, UI-SPEC rev 5 Copywriting `Error — 셀(잠김) = 표 위 한 줄` · S4 셀 단계 표)"
    - "잠긴 셀(정산의 실행가 밖 칸 · 승인 차수 잠금 — 04-40)이나 읽기 전용 셀(연결 문서, D-66)에서 Enter·글자 입력·Delete를 누르면 편집 모드가 열리지 않고 그 셀 아래 짧은 이유 한 줄(`정산 · 실행가와 새 줄만` · `지출결의 {번호} 연결됨 · 고치려면 새 차수`)이 보이며, 그 셀에 떨어진 붙여넣기 값은 같은 이유의 오류 셀이 된다 — 편집 시도 이유 줄은 고정 오류가 아니라 포커스가 셀을 떠나면 사라진다(DR-35, rev 5 Copywriting `Error — 셀(읽기 전용, D-66)` · `Error — 셀(잠김)`. PM의 조정 줄은 이 이유를 띄우지 않는다 — DR-22, 04-23)"
    - "표 위 한 줄의 순서는 ① 현재 차수 복원 줄 ② 이전 차수 복원 줄(그룹 B 04-24) ③ 잠김 줄이다 — 셋 다 `--fs-sm --muted` 한 줄씩(DR-31, UI-SPEC rev 5 S4 「표 위 한 줄의 순서」 · S18)"
    - "힌트 줄은 그 사람에게 실제로 되는 것만 적고 `저장`을 적지 않는다(1차 kbd `Ctrl+S`가 말한다) — 04-28의 힌트 항목 배열에서 구조 편집 가능성으로 항목을 뺀다: 정산 PM은 줄 이동·줄 복제가 없고 새 줄은 남는다 · 완료 PM(읽기 표)과 1024 미만(04-49 — 폭 규칙)에는 힌트 줄 자체가 없다. 최종 문자열 `이동 Tab ↑↓←→ · 복사 Ctrl+C · 붙여넣기 Ctrl+V · 취소 Esc · 새 줄 Ctrl+Enter`(정산 PM)은 Tab·복사를 더하는 04-19 뒤에 완성된다(§7-9, UI-SPEC rev 5 S4 「힌트 줄」, CEO C-07)"
    - "0줄 표의 EMPTY 한 줄이 상태 × 보는 사람으로 갈린다 — 줄 추가 권한자(정산의 담당 PM 포함) `이 프로젝트에 견적 줄이 없습니다 · 첫 줄 만들기`+kbd `Ctrl+Enter` · 줄 추가 불가 + 기간 권리 `lead` `… · 기간 바꾸기`(누르면 기간 칸 종료일에 포커스) · 그 밖 `… · 담당 PM {이름}` · 완료 `이 프로젝트에 견적 줄이 없습니다`(사실만) — 렌더되지 않는 행동을 문구에 적지 않는다. 1024 미만에서는 「첫 줄 만들기」를 뺀 사실만(「기간 바꾸기」는 표 밖 칸이라 남는다 — 폭 판정은 04-49). 담당자 이름(`projectResponsibles`)은 표가 비었거나 종료일 지남이 보일 때만 조회한다(S4 empty, rev 5 Copywriting `Empty — 견적 줄 표` 행들, DR-24 · DR-36, 엔지 리뷰 A §1 P2)"
    - "미수주·수주중·진행 프로젝트의 편집은 04-04 그대로 전부 열려 있다(D-45)"
    - "저장된 줄을 지우면 `ui/confirm-dialog` 확인 모달(제목·1차 `견적 줄 삭제` · 부제 `{항목} · {견적가}` · 결과 `보관함으로 옮겨짐 · 복원은 관리자` · 2차 `취소 Esc`) 뒤 dirty 한 건이 되어 일괄 저장 때 보관되고, 연결 문서가 있는 줄은 `견적 줄 취소` 모달(결과 `견적가 0 · 이력과 연결된 지출결의는 그대로` · 1차 `견적 줄 취소` · 2차 `닫기 Esc`)로 상태만 `취소`가 된다(D-56, rev 5 Copywriting Destructive 두 행, DR-12)"
    - "새 줄은 만들 때 화면이 uuid를 id로 붙이고(`isNew`) 저장 요청이 실패·유실돼 다시 보내도 같은 id를 싣는다 — 커밋은 됐는데 응답을 잃은 재전송이 줄을 두 번 만들지 않는다. 미저장 보관(04-22 배선)의 새 줄 키도 이 id다(ENG-D10)"
    - "순서는 두 가지로만 보낸다 — 끝에 붙인 새 줄뿐인 저장은 순서를 싣지 않고(서버가 끝에 붙인다), 줄 이동·가운데 삽입·복제가 있는 저장은 활성 줄 전체의 표시 순서 `order`를 한 번 싣는다. 옮긴 두 줄은 dirty가 되고, 고치기만 한 저장은 순서를 싣지 않아 순서가 그대로다 — 가운데 줄을 지우고 새 줄을 더해도 새로 고친 뒤 그 줄이 마지막이다(CEO A-03 클라이언트 쪽, 엔지 리뷰 A §2 P2)"
    - "저장이 성공하면 표는 서버가 돌려준 차수의 활성 줄 전체로 다시 그린다 — 3줄 중 2번째만 고쳐 저장해도 새로 고침 없이 3줄이 그대로 보이고, 저장 요청 뒤에 친 입력은 저장 중 잠금(04-49 — 엔지 r2 분할) 때문에 없으므로 응답이 입력을 덮는 경로가 없다(엔지 리뷰 A §2 P2, UI-SPEC rev 5 S19 populated — 성공). PR #38 알려진 문제 「부분 저장 뒤 표가 저장한 줄만 남음」을 이 truth(Task 1 ③ · E2E e2)가 닫는다"
    - "셀 편집기를 연 채(Enter·Tab으로 커밋하지 않은 채) `Ctrl+S`나 1차 「일괄 저장」을 누르면 그 활성 셀의 **마지막 값**이 먼저 커밋되어 저장 페이로드에 들어간다 — 편집기 커밋과 저장 페이로드 수집이 한 플랜(이 플랜)에 있고, 서버 요청 본문과 새로 고친 뒤 값이 둘 다 그 값이다(엔지 r2 분할안 — Codex 「편집기 커밋과 페이로드를 함께」)"
    - "줄 상태 라벨 표는 도메인 상수 `QUOTE_LINE_STATUSES`(04-12)로 타입이 정해진다 — 화면이 상태 값 목록을 따로 두지 않는다(엔지 리뷰 A P3)"
    - "1차 「일괄 저장」 렌더 조건 `canSave`의 편집 항은 「편집 가능 셀이 하나라도 있거나 줄을 추가할 수 있음」이다 — `editable` boolean prop이 사라진다(CEO A-12, 04-22 `canSave`의 마무리)"
  prohibitions:
    - statement: "화면이 상태 문자열로 셀·구조 편집 가능성을 추론해서는 안 된다. 서버 판정(04-12)과 화면이 갈라지면 사용자가 연 칸이 저장 때 거부되거나(UX-04 위반), 서버가 받는 칸이 화면에 잠겨 보인다 — 셀 단계는 DTO, 구조는 `structuralEditability` 결과만 쓴다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/edit-scope.ts"
      provides: "04-12 판정 함수에 더해 표 위 한 줄 · EMPTY 변형 · 힌트 항목 거르기(순수 — 표 위 한 줄은 `quoteLockReason`을 부른다)"
      exports: ["tableLockLine", "quoteTableEmptyState", "visibleHintKeys"]
    - path: "test/e2e/quote-edit-scope.spec.ts"
      provides: "정산 PM 실행가·새 줄(견적 0 잠김) · 구조 컨트롤 부재 · 잠긴 셀 이유 줄 · 완료 읽기 표 · 삭제→보관 · 정산 EMPTY · D-77 버튼 부재 · 순서 저장 · 좁은 폭 보기 전용(상한 케이스는 04-26이 더한다)"
      min_lines: 90
  key_links:
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "domain/quotes/lines.ts"
      via: "열의 편집 가능성이 DTO의 `cellEditability`에서 온다 — 화면이 상태를 해석하지 않는다"
      pattern: "cellEditability"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/quotes/edit-scope.ts"
      via: "구조 가능성·표 위 한 줄·EMPTY·힌트 항목을 서버에서 계산해 넘긴다"
      pattern: "structuralEditability"
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "게이트 거부 이유(04-12)와 이 플랜의 표 위 한 줄(`tableLockLine`)이 같은 `quoteLockReason`을 부른다 — 표 위 한 줄 = 셀 이유 한 문자열(DR-2)"
      pattern: "quoteLockReason"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/period-field.tsx"
      via: "EMPTY 「기간 바꾸기」가 04-22의 `openPeriodField(\"end\")`를 부른다(저장 중 `saveLocked` 읽기 전용은 04-49)"
      pattern: "openPeriodField"
---

<objective>
**As a** 정산 단계의 기획 PM, **I want to** 견적 표에서 지금 고칠 수 있는 칸만 편집에 들어가고, 늦게 발견된 비용은 새 줄로 실행가만 적으며, 할 수 없는 동작(삭제·이동·복제)은 버튼도 키도 없기를, **so that** 화면에서 연 칸이 저장 때 거부되는 일이 없고, 왜 잠겼는지를 표 위 한 줄이 말해 준다.

머지 묶음: ②/4 상태 전환 — 웨이브 2~18(04-50 → 04-23)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**2026-09-23 CEO 리뷰로 04-12에서 떼어 낸 화면 쪽 플랜.** 04-12가 편집 범위의 서버 판정(셀 단계 DTO · 구조 판정 · 보관·복원 · 경합)을 만들었고, 이 플랜은 그 결과를 견적 표에 그린다 — 셀 단계 격자 · 정산 새 줄(견적 칸 0 잠금, 사용자 D10·D12) · 구조 컨트롤 · 표 위 한 줄 · 힌트 줄(04-28 배열 거르기) · EMPTY 변형 · 삭제·취소 모달 · 순서 전송(A-03) · `canSave` 마무리(A-12) · D-77 버튼 부재.

Purpose: 셀 단계를 화면이 추론하면 UX-06·ROADMAP 기준 3 위반이다. 04-26(상한 EMPTY·붙여넣기 거부)·04-23(조정 줄 EMPTY 우선순위 ②)·04-24(차수 모달)가 이 플랜의 EMPTY 함수·힌트 거르기·구조 props를 넓힌다.

Output: 화면용 순수 함수 셋 · 견적 표 배선 · 잠긴 셀 이유 줄 · 저장 직전 활성 셀 커밋 · 단위 1(확장) · E2E 1(신규). 저장 중 잠금 · 폭 규칙 · 복원 줄 모든 폭 · 독립 DOM 감사는 04-49(엔지 r2 분할).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`).** 04-12의 서버 계약이 바뀐 만큼 화면이 따라간다: (a) 정산 새 줄 기본값을 **수량 1 · 단가 원화 0**(엔지 리뷰 A §2 P1 — 옛 「수량 0」은 서버 검증과 모순), (b) 새 줄에 화면 uuid(ENG-D10 — 재전송 중복 방지), (c) 순서는 「끝에 붙이면 싣지 않음 / 이동·가운데 삽입·복제면 `order` 한 번」(엔지 리뷰 A §2 P2), (d) 저장 결과(활성 줄 전체)로 표를 다시 그림(엔지 리뷰 A §2 P2 — 지금은 저장한 줄만 남는다), (e) 라벨 표를 `QUOTE_LINE_STATUSES`로(엔지 리뷰 A P3), (f) 담당자 이름은 필요할 때만 조회.

**2026-09-23 디자인 리뷰 반영(`docs/designs/plant8-erp-phase4-design-review-260923.md`, UI-SPEC rev 5).** (g) **DR-2:** 표 위 한 줄과 잠긴 셀 이유를 04-12의 `quoteLockReason` 한 문자열로 — `정산 · 실행가와 새 줄만`, 완료 줄은 편집 셀이 있는 격자에서만(PM 읽기 표에는 없다, P0). 옛 「D16이 UI-SPEC 편집을 막았다」 인용과 SUMMARY 「문구 차이」 우회는 지웠다 — rev 5가 계약이다(DR-1). (h) **DR-35:** 잠긴·읽기 전용 셀의 편집 시도에 짧은 이유 한 줄 + 붙여넣기 오류 셀 유지. (i) **DR-31:** 표 위 줄 순서 복원 → 잠김. (j) **DR-3 · 계약 3:** 저장 요청 동안 네 편집기의 편집 진입 잠금(`Table.saveLocked` · `aria-busy`) — 엔지 r2 분할로 **04-49**가 소유한다. (k) **DR-14 · DR-24 · DR-36 · 계약 6:** 1024 이상에서만 표 편집·구조 컨트롤, 700~1023 보기 전용, 좁은 PC 열 접기, 1024 미만 1차는 N ≥ 1이면 렌더(표 셀 dirty 포함 — UI-SPEC rev 5 후속 결정 R1) — **04-49**. (l) **DR-12:** 삭제·취소 모달은 04-46의 `ui/confirm-dialog`(옛 `DeleteLineDialog`는 04-46이 옮겼다 — 체커 revision 1의 04-08 분할). (m) DOM 감사 1280 · 1024 · 375(+ 1000) — **04-49**가 이 플랜의 EMPTY 변형·0/1/6줄 합계 형식 backstop까지 함께 판정한다.

**2026-09-24 엔지니어링 리뷰 r2 반영(`docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 3 · E2-02).** (a) **분할(E2-02):** 옛 Task 3(저장 중 잠금 `save-lock` · 1024 미만 보기 전용 · 좁은 PC 열 접기 · 복원 줄 모든 폭 + 화면 검증 1280/1024/375)을 신규 **04-49**(웨이브 15, `depends_on: ["04-30"]`)로 옮겼다 — 그 truths·backstop 다섯(S4 EMPTY 변형 · 0/1/6줄 · 가로 스크롤 · 1024 미만 구조 컨트롤 · S18 복원 줄)·금지 항목(저장 중 입력 유실)·T-04-335·T-04-336과 `save-lock.ts`·`use-editable-width.ts`·`ledger-save-flow.spec.ts` 등도 04-49 몫이다. 04-26은 이제 04-49 뒤다. (b) **편집기 커밋과 저장 페이로드 수집은 이 플랜에 둔다(분할안 — Codex 지적):** 「활성 셀의 마지막 값이 전송 페이로드에 든다」를 Task 1이 단언한다(E2E e3). 04-49의 저장 중 잠금은 이 커밋 뒤에 걸린다. (c) 이 플랜의 끝 판정은 `CI=true` E2E다(Task 2) — 전체 게이트 한 번과 독립 DOM 감사는 04-49 끝.

**PR #38 알려진 문제(이 플랜 몫 — PR #38 본문 표):** 「부분 저장 뒤 표가 저장한 줄만 남음」(Task 1 ③ · E2E e2) · 「줄 순서 섞임」(Task 2 ④ · E2E f·h — 서버 규칙은 04-12) · 「삭제 미반영」(Task 2 ② 보관 dirty · E2E e — 서버 보관은 04-12). 「미저장 복원 미연결」은 04-22(보관·「복원」 배선)와 04-49(복원 줄 모든 폭)가 닫는다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전(커밋 전 포함) `verification-before-completion`을 Skill로 호출한다. 테스트·빌드·린트가 실패하면 코드를 고치기 전에 `systematic-debugging`부터 부른다. 서브에이전트 위임 시 세 스킬과 `model`을 명시하고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(이 플랜 하나). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 완료 판정은 로컬 dev가 아니라 `CI=true`다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. E2E 날짜는 `kstToday(new Date())` + `addDays`(CEO A-18). 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-28-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다(U-2는 `## UI Considerations` S5 partial 529행의 사용자 확정값). 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5 — rev 4 줄 번호는 쓰지 않는다) `## UI Considerations` 「S4」 501–521행 · 「S13」 587–596행 · 「S17」 628–637행 · 「S19」 649–657행 · `## 적용 규칙` 「S3」 1차 1132–1135행 · 「S4」 1156–1231행(좁은 PC 1170–1178 · 셀 단계 표 1185–1194 · 정산 구조 1195–1197 · 표 위 한 줄 순서 1223 · 힌트 줄 1224–1227 · 1024 미만 1230–1231) · 「S19」 1560–1576행 · 첫머리 표 「rev 5 후속 결정」 R1 47행(1024 미만 1차 = N ≥ 1이면 렌더) · `## Copywriting Contract` `Primary CTA — 견적 원장` 342행 · `Empty — 견적 줄 표` 349–354행 · `Error — 셀(읽기 전용)`·`Error — 셀(잠김)` 371–372행 · Destructive 418–419행

# @.planning/phases/04-project-quote-ledger/04-46-SUMMARY.md — `ui/confirm-dialog`(옛 `DeleteLineDialog` 이관 자리)·`Button` 개정(04-08 분할로 04-46이 만든다)

# docs/design/SYSTEM.md §7-3 (가) 잠김 행 · §7-9 표시 단축키

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-05(단축키 — 잠긴 상태의 무동작)** 와 PROJ-02·UX-04의 화면 부분이다. 수용 기준은 04-12 SUMMARY의 판정 결과·UI-SPEC S4·Copywriting에서 끌어왔다. S4 `## UI Considerations`의 empty(변형) backstop · zero-one-many backstop은 flat-scalar 마커로, 2026-09-23 partial(정산)은 truths로 옮겼다. S4 partial(상한)과 그 금지 항목은 04-26이, 조정 줄 쪽 항목은 04-13·04-23·04-14가 옮긴다.

**계획 단계 가정·확정(표면화):**

- **문구 = UI-SPEC rev 5 Copywriting 원문(계약):** rev 5가 사용자 D10·D12와 디자인 리뷰(DR-2 · P0)를 흡수했다 — 표 위 한 줄 = 잠긴 셀 이유 한 문자열 `정산 · 실행가와 새 줄만` · `완료 · 견적 줄 잠김`(04-12 `quoteLockReason`) · EMPTY 변형(`Empty — 견적 줄 표` 행들) · 힌트 줄(`저장` 없음). SUMMARY에 따로 차이 표를 두지 않는다.
- **정산 새 줄의 기본값(엔지 리뷰 A §2 P1 정정):** 수량 1 · 단가 원화 0 · 상태 기본값(견적가 0) — 04-12의 `quoteCellsZero`(단가 원화 0 · 수량 없음/1)와 같은 정의다. 줄 추가 경로(「줄 추가」·`Ctrl+Enter`·EMPTY 첫 줄)가 이 기본값으로 만든다. 수량 칸에 `1`이 보이는 것이 이상하다는 사용자 의견이 나오면 표시만 바꾼다(저장 규칙은 그대로) — SUMMARY에 한 줄.
- **새 줄 id(ENG-D10):** 새 줄을 만드는 모든 경로(「줄 추가」·`Ctrl+Enter`·`Ctrl+D` 복제·붙여넣기로 생긴 줄·EMPTY 첫 줄)가 `crypto.randomUUID()`를 id로 붙이고 `isNew: true`를 둔다. 저장이 성공하면 서버 목록이 그 id 그대로 돌아오므로 화면 줄이 바뀌지 않고, 실패하면 같은 id로 다시 보낸다. 미저장 복원(D-68) 저장 키도 이 id를 쓴다(04-04의 줄 id 기준 — 04-47 Task 2 ②가 확인한다). 붙여넣기로 표 끝을 넘어 생기는 줄은 04-47 Task 1 ③이 이 새 줄 경로를 부른다.
- **순서 전송(엔지 리뷰 A §2 P2 — 04-12 서버 규칙):** 저장 직전 화면 배열에서 (1) 이동·가운데 삽입·복제가 없고 새 줄이 전부 끝에 있으면 `order`를 싣지 않는다 (2) 하나라도 있으면 보관할 줄을 뺀 활성 줄 전체의 id를 표시 순서로 `order`에 싣는다. 줄마다 `sortOrder`를 싣지 않는다(옛 계획의 「새 줄·옮긴 줄에만 `sortOrder`」는 동률·틈을 만들었다). 붙여넣기가 새 줄의 잠긴 칸(수량·단가)을 채우면 04-04 규약대로 그 칸이 오류로 남는다(잠긴 칸 붙여넣기 — C-03의 「잠긴 칸은 오류」와 같은 규칙).
- **저장 중 잠금 · 폭 판정 · 1024 미만 1차 렌더(R1):** 엔지 r2 분할로 04-49의 `<probe_fallback>`으로 옮겼다 — 이 플랜은 셀 단계·구조·EMPTY 판정을 폭과 무관하게 서버 값대로 그리고, 1024 미만에서 무엇을 거두는지는 04-49 Task 2가 정본이다.
- **새 줄의 셀 단계:** 저장 전 새 줄은 서버 DTO가 없으므로 04-12의 `lineCellEditability({ …, isNewLine: true })`를 클라이언트가 같은 입력으로 부른다(같은 함수 — 순수, `edit-scope.ts`는 리포지토리를 import하지 않아 클라이언트 번들에 들어갈 수 있다).

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 정산 PM의 견적 표가 서버 셀 단계로 열리고 실행가만 고쳐 저장된다(DTO → 격자 → 키보드 → 저장 → E2E)</name>
  <files>app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, ui/table/Table.tsx, ui/table/use-grid-keyboard.ts, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `domain/quotes/edit-scope.ts`(04-12 산출물 전문) · `domain/quotes/lines.ts`의 `QuoteLineDto`(`cellEditability`·`hasLinkedDocuments`)
    - `ui/table/types.ts`(전문) — `CellEditability` 세 단계와 열의 `editability`
    - `ui/table/Table.tsx` 70–140행 — 편집 가능 셀 스캔으로 `role="grid"`·머리글 바탕이 정해지는 자리
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `editability: () =>`(열 정의)·`isEditable: () =>`·`editable` prop·1차 렌더 조건(`canSave`, 04-22)·저장 성공 처리(`setLines(data.quoteLines.lines…` — 04-12 뒤 결과가 활성 줄 전체)를 찾아 범위만 Read
    - `app/(app)/projects/[id]/page.tsx`(전문)
    - `test/e2e/quote-table.spec.ts`·`test/e2e/project-period.spec.ts` — 상태를 DB에 두는 준비와 역할 로그인 형태
  </read_first>
  <action>
① `[id]/page.tsx`는 `editable` boolean 대신 04-12의 DTO(줄마다 `cellEditability`)와 `structuralEditability` 결과를 넘긴다. `quote-table.tsx`의 열 `editability`·키보드 `isEditable`이 행의 `cellEditability[칸]`을 읽고, 저장 전 새 줄은 `lineCellEditability({ status, canWrite, hasLinkedDocuments: false, isNewLine: true })`로 만든 값을 쓴다(`<probe_fallback>`). 이렇게 하면 완료 PM의 표는 편집 셀 0개라 `ui/table`이 캡션 있는 읽기 표로 자동 전환된다(모드 prop 없음). `editable` prop을 지운다.

② **`canSave` 마무리(A-12).** 04-22가 둔 `canSave`의 `editable` 항을 「줄 중 편집 가능 셀이 하나라도 있음 또는 `structural.insert`」로 바꾼다 — 서버(`page.tsx`)가 계산한다.

③ **저장 결과 반영(엔지 리뷰 A §2 P2).** 저장 성공 처리가 서버가 돌려준 `quoteLines.lines`(차수의 활성 줄 전체 — 04-12)로 `lines`를 통째로 바꾼다 — 저장한 줄만으로 바꾸지 않는다(지금 동작은 3줄 중 1줄을 고치면 1줄만 남는다). 줄 상태 라벨 표(`LINE_STATUS_LABELS`)의 타입을 `Record<QuoteLineStatus, string>`(04-12 `QUOTE_LINE_STATUSES`)로 바꾼다.

③-b **저장 직전 활성 셀 커밋(엔지 r2 분할안 — 편집기 커밋과 페이로드 수집을 한 플랜에).** `Ctrl+S`(04-28 배선)와 1차 「일괄 저장」이 저장 페이로드를 모으기 **전에** 열려 있는 셀 편집기의 값을 커밋한다 — 커밋은 Enter·Tab 커밋과 같은 경로(같은 셀 검증 · dirty 표시)를 지나고, 커밋이 셀 오류를 내면 그 오류가 04-04 규약대로 전부 거부에 든다. 지금 코드가 이미 그렇게 하면 코드를 바꾸지 않고 E2E로 고정만 한다(SUMMARY에 「이미 충족」). `ui/table`에 커밋 입구가 없으면 `Table.tsx`·`use-grid-keyboard.ts`에 최소한의 입구(열린 편집기를 커밋하는 함수 하나)만 더한다 — 04-49의 저장 중 잠금은 이 커밋 뒤에 걸린다.

④ **E2E(트레이서 증명, `test/e2e/quote-edit-scope.spec.ts` 신규).** 준비에서 정산 프로젝트(종료일 `addDays(kstToday(new Date()), -3)`, 줄 둘)를 두고 담당 PM으로 연다 → 실행가 셀은 Enter로 편집에 들어가고 단가 셀은 편집에 들어가지 않으며(`aria-readonly`) → 실행가를 고쳐 `Control+s` → 합계 행에 `저장됨` → 새로 고쳐도 실행가가 새 값. 같은 스펙에서 완료 프로젝트를 PM으로 열면 표가 캡션 있는 읽기 표이고 1차 버튼이 없다. (e2) 진행 프로젝트(줄 셋)에서 2번째 줄의 실행가만 고쳐 `Control+s` → **새로 고치기 전에** `tbody tr` 줄 행이 셋이다(엔지 리뷰 A 공백 4 · PR #38 알려진 문제 「저장한 줄만 남음」). (e3) **활성 셀 마지막 값(엔지 r2):** 진행 프로젝트의 실행가 셀에서 Enter로 편집기를 열고 `987654`를 친 뒤 Enter·Tab 없이 곧바로 `Control+s` → `page.waitForRequest`로 잡은 서버 액션 요청 본문에 `987654`가 있고, 합계 행 `저장됨` 뒤 새로 고쳐도 그 셀이 `987,654`다 · 같은 준비로 1차 「일괄 저장」을 눌러도 같다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — `editable` prop 제거가 호출부 어딘가에 남았다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/quote-edit-scope.spec.ts test/e2e/quote-table.spec.ts test/e2e/project-period.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 정산 PM의 실행가 편집·단가 읽기·저장이 이어지지 않았거나, 완료 표가 읽기 표로 바뀌지 않았거나, 진행 프로젝트의 04-04 편집 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `quote-table.tsx`의 열 편집 가능성이 `cellEditability`에서 오고 `editable` boolean prop이 사라졌다(Grep `editable:` · `editable={` 0건 — `cellEditability`·`canSave` 제외)
    - 1차 렌더 조건이 `canSave` 하나이고 그 편집 항이 셀 단계·구조에서 온다
    - E2E가 정산 PM의 실행가 편집·단가 `aria-readonly`·저장 뒤 재조회와 완료 읽기 표·1차 부재를 단언한다
    - 저장 성공 처리가 결과 목록 전체로 `lines`를 바꾸고 (e2) 「새로 고치기 전 세 줄」이 초록이다 · `LINE_STATUS_LABELS`가 `QuoteLineStatus`로 타입된다
    - (e3) 편집기를 연 채 `Control+s`·1차 「일괄 저장」 두 경로 모두 요청 본문과 새로 고친 뒤 값에 활성 셀의 마지막 값이 있다(엔지 r2 — 편집기 커밋 → 페이로드)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산 PM의 견적 표가 서버 셀 단계로 열리고 실행가 한 칸 편집이 저장까지 이어지며, 완료는 읽기 표가 됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 구조 컨트롤 · 정산 새 줄 · 표 위 한 줄(DR-2 · DR-31) · 잠긴 셀 이유 줄(DR-35) · 힌트 줄 · EMPTY · 삭제·취소 모달 · 순서 전송</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, ui/table/Table.tsx, ui/table/types.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## Copywriting Contract` `Empty — 견적 줄 표` 349–354행(고르는 규칙 · 변형 · 우선순위 · 1024 미만) · `Error — 셀(읽기 전용, D-66)`·`Error — 셀(잠김) = 표 위 한 줄` 371–372행 · `Destructive — 견적 줄 삭제`·`견적 줄 취소` 418–419행 · `## 적용 규칙` 「S4」 셀 단계 표 1185–1194행 · 정산 구조 1195–1197행 · 「표 위 한 줄의 순서(DR-31)」 1223행 · 「힌트 줄」 1224–1227행 · `## UI Considerations` 「S4」 501–521행
    - `domain/quotes/edit-scope.ts`(04-12 — `quoteLockReason`·`linkedDocumentReason`·`structuralEditability`·`lineCellEditability`) · `domain/quotes/lines.ts`의 `QuoteLineDto`(`cellEditability`·`hasLinkedDocuments`·`readonlyReason`)
    - `ui/table/Table.tsx` 100–160행(`useGridKeyboard` 배선 · `onEnterEdit`) · 260–330행(셀 렌더 · `cellIssue` 줄) · `ui/table/types.ts`(`CellIssue`)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `confirmDeleteLine`·`ConfirmDialog`(04-46이 옛 `DeleteLineDialog`를 옮긴 자리)·`addLine`·`duplicateLine`·`moveLine`·힌트 항목 배열(04-28)·`emptyMessage`·`restoreBanner`를 찾아 범위만 Read
    - `ui/confirm-dialog/ConfirmDialog.tsx`(04-46 — `secondaryLabelFor`가 1차 라벨의 「취소」로 2차 `닫기 Esc`를 고른다)
    - `domain/projects/responsibles.ts`(04-11 — 담당 PM·팀장 이름) · `domain/projects/period.ts`의 `periodEditRights`와 `QuoteLedger.openPeriodField`(04-22)
  </read_first>
  <behavior>
    - `tableLockLine({ status, hasEditableCells, lineCount })`: 정산 + 편집 셀 있음 + 줄 ≥ 1 → `quoteLockReason({ status })` = `정산 · 실행가와 새 줄만` · 완료 + 편집 셀 없음(PM 읽기 표) → null · 완료 + 편집 셀 있음(경영관리 격자) → `완료 · 견적 줄 잠김` · 줄 0 → null · 수주중·진행·미수주 → null
    - `quoteTableEmptyState({ status, canAddLine, periodRights, pmName })`: 줄 추가 가능(진행·정산 등) → `{ message: "이 프로젝트에 견적 줄이 없습니다", action: { kind: "addLine", label: "첫 줄 만들기" } }` · 진행 + 불가 → `{ message: "이 프로젝트에 견적 줄이 없습니다 · 담당 PM {이름}" }` · 정산 + 불가 + 기간 권리 `lead` → `{ message: "이 프로젝트에 견적 줄이 없습니다", action: { kind: "openPeriodEnd", label: "기간 바꾸기" } }` · 정산 + 불가 + 그 밖 → `… · 담당 PM {이름}` · 완료 → `{ message: "이 프로젝트에 견적 줄이 없습니다" }`(버튼·꼬리 없음) · 줄 추가 가능이 기간 권리보다 우선
    - `visibleHintKeys(allKeys, structural)`: 정산(`insert`만) → 줄 이동·줄 복제 키가 빠지고 새 줄은 남는다 · 완료(전부 거짓) → 새 줄·줄 이동·줄 복제가 빠진다 · 진행 → 그대로 · 어느 경우든 `저장` 키는 없다(1차 kbd가 말한다)
  </behavior>
  <action>
① `edit-scope.ts`에 `tableLockLine`(`quoteLockReason`(04-12)을 부른다 — 문자열 리터럴을 새로 두지 않는다, DR-2 한 함수) · `quoteTableEmptyState(…)` → `{ message, action?: { kind: "addLine" | "openPeriodEnd", label } }`(rev 5 Copywriting 우선순위 ① 첫 줄 만들기 ③ 기간 바꾸기 ④ 담당자 이름 ⑤ 완료 사실 — ② 조정 줄 추가는 04-23이 더한다) · `visibleHintKeys(allKeys, structural)`(04-28이 둔 힌트 항목 배열의 키 목록에서 구조 가능성으로 빼고 `저장` 키가 있으면 뺀다 — 문자열 리터럴을 새로 만들지 않는다)을 더한다. 결정표를 먼저 쓴다(RED).

② **화면.** `[id]/page.tsx`가 `tableLockLine`·`quoteTableEmptyState` 결과를 넘긴다(담당자 이름은 04-11의 `projectResponsibles` — **줄이 0개이거나 종료일 지남일 때만** 부른다, 기간 권리는 04-22의 `periodEditRights`). `quote-table.tsx`: 「줄 추가」·EMPTY 3차·`Ctrl+Enter`는 `structural.insert`로, Delete 모달은 `structural.archive`로, `Alt+↑↓`는 `structural.reorder`로, `Ctrl+D`는 `structural.duplicate`로 켜고 끈다(불가면 **렌더하지 않고** 키는 무동작 — 비활성 버튼을 세우지 않는다) · 새 줄은 모든 경로에서 `crypto.randomUUID()` id와 `isNew: true`로 만들고(`<probe_fallback>` — ENG-D10), 04-22가 이은 미저장 보관의 새 줄 키를 이 id로 바꾼다 · 정산 새 줄은 수량 1 · 단가 원화 0 기본값이며 셀 단계는 `isNewLine: true` 판정 · **표 위 한 줄의 순서(DR-31)**: 현재 차수 복원 줄(04-22) → (이전 차수 복원 줄 자리 — 그룹 B 04-24) → 잠김 줄(`tableLockLine` 결과가 있을 때만) · 힌트 줄은 `visibleHintKeys` 결과만, 읽기 표(편집 셀 0)에는 없다 · EMPTY는 서버가 고른 `message`와 `action`(「첫 줄 만들기」 + kbd `Ctrl+Enter` → `addLine`, 「기간 바꾸기」 → 04-22의 `openPeriodField("end")`) · 저장된 줄 삭제는 04-46이 옮긴 `ConfirmDialog`의 내용을 rev 5로(제목·1차 `견적 줄 삭제` · 부제 `{항목} · {견적가}` · 결과 `보관함으로 옮겨짐 · 복원은 관리자` · 2차 `취소 Esc` — 1차 라벨 `삭제` 단독은 §8 규칙 1 위반이라 고친다) 뒤 화면에서 빠지고 `archivedLineIds`에 들어가 dirty 한 건으로 센다. 연결 문서가 있는 줄의 Delete는 같은 컴포넌트로 `견적 줄 취소` 모달(결과 `견적가 0 · 이력과 연결된 지출결의는 그대로` · 1차 `견적 줄 취소` · 2차는 컴포넌트가 `닫기 Esc`로 고른다 — §7-8 보강)을 열어 상태 칸을 `취소`로 바꾼다. 정산 화면에 발행 요청·지출결의 마감 같은 버튼을 두지 않는다(D-77 — Phase 5·6).

③ **잠긴 셀 편집 시도의 이유 줄(DR-35).** `ui/table/types.ts`의 `CellIssue` `kind`에 `"reason"`을 더하고(`error`·`conflict`와 같은 셀 아래 한 줄 자리, 오류 셀 배경·좌측 인셋은 그리지 않는다 — 고정 오류가 아니다), `ui/table/Table.tsx`에 선택 콜백 `onBlockedEdit?(row, columnKey)`를 더해 편집 셀이 있는 격자에서 `edit`이 아닌 셀에 Enter·글자 입력·셀 안 Delete가 오면 편집 모드를 열지 않고 이 콜백만 부른다. `QuoteLedger`는 그 셀의 이유를 `kind: "reason"`으로 보이고 포커스가 셀을 떠나면 지운다 — 이유는 `locked` 셀이면 서버가 넘긴 `lockReason`(`quoteLockReason` — 표 위 한 줄과 같은 문자열), `readonly` 셀이면 DTO `readonlyReason`(`지출결의 {번호} 연결됨 · 고치려면 새 차수`, 04-12). 이유가 없는 잠김(PM의 조정 줄 — 04-23이 줄 종류로 표시)은 아무것도 띄우지 않는다(DR-22). 붙여넣기가 잠긴·읽기 전용 셀에 떨어지면 04-04 규약대로 오류 셀이 되고 그 이유가 같은 문자열이다(`kind: "error"` — 오류 수에 든다). `errorCellCount`는 `reason`을 세지 않는다.

④ **순서 전송(A-03 클라이언트 · 엔지 리뷰 A §2 P2).** `moveLine`(`Alt+↑↓`)이 옮긴 두 줄을 모두 dirty로 표시하고, 저장 페이로드는 `<probe_fallback>` 「순서 전송」 두 경우로만 순서를 싣는다 — 이동·가운데 삽입·복제가 있으면 활성 줄 전체 `order` 한 번, 아니면 없음. 줄마다 `sortOrder`를 싣지 않는다. 보관할 줄(`archivedLineIds`)은 `order`에서 뺀다.

⑤ **E2E(`quote-edit-scope.spec.ts`).** (a) 정산 PM 화면에 줄 삭제·이동·복제 컨트롤이 DOM에 없고 `Delete`·`Alt+ArrowDown`·`Control+d` 뒤 줄 수·순서가 그대로다, (b) 정산 PM이 「줄 추가」로 새 줄을 만들면 수량·단가 셀이 `aria-readonly`이고 수량 `1` · 단가 `0`이며, 항목·실행가를 적어 `Control+s` → 저장 뒤 새로 고쳐도 그 줄이 수량 1 · 견적가 0 · 실행가 값으로 있다(사용자 D10·D12, 엔지 리뷰 A P1), (c) 표 위 한 줄 `정산 · 실행가와 새 줄만`, 정산 PM 힌트 줄에 `새 줄 Ctrl+Enter`가 있고 `줄 이동`·`줄 복제`·`저장`이 없다, (c2) **잠긴 셀 이유(DR-35):** 정산 PM이 기존 줄 단가 셀에서 `Enter` → 입력 요소가 생기지 않고 셀 아래 `정산 · 실행가와 새 줄만` → 방향키로 옮기면 그 줄이 사라진다 · 같은 단가 셀에 `1000`을 붙여 넣으면 그 셀이 오류 셀(`aria-invalid`)이고 이유가 같은 문자열이다, (c3) 완료 프로젝트를 PM으로 열면 표 위 잠김 줄 · 힌트 줄이 DOM에 없다(P0), (d) 정산 상세에 `발행 요청`·`마감` 이름의 버튼이 없다(D-77), (e) 수주중에서 줄 삭제 → 모달(`getByRole("dialog")`) 결과 줄 `보관함으로 옮겨짐 · 복원은 관리자` · 1차 `견적 줄 삭제` → 저장 → 새로 고쳐도 없다, (f) 진행에서 3번째 줄을 `Alt+ArrowUp`으로 옮기고 저장 → 새로 고쳐도 옮긴 순서 · 다른 줄의 실행가만 고쳐 저장 → 순서 그대로(A-03), (g) 0줄 정산 표의 EMPTY가 PM에게 `이 프로젝트에 견적 줄이 없습니다` + 「첫 줄 만들기」, 그 팀 팀장(줄 추가 불가 · 기간 권리 `lead`)에게 같은 문장 + 「기간 바꾸기」(누르면 종료일 칸 포커스), 0줄 완료 표는 `이 프로젝트에 견적 줄이 없습니다`만, (h) 진행 프로젝트(줄 셋)에서 가운데 줄을 삭제(보관 모달)하고 「줄 추가」로 새 줄을 더해 저장 → 새로 고쳐도 새 줄이 마지막이다(엔지 리뷰 A 공백 — 순서), (i) **재전송(ENG-D10):** 새 줄 하나를 만들고 `page.route`로 저장 요청을 서버까지 보낸 뒤(`route.fetch()`) 응답을 버리고(`route.abort()`) 화면에 저장 실패가 보이면 `Control+s`를 다시 눌러 성공 → 새로 고친 뒤 그 항목 이름의 줄이 **하나**다, (j) **표 위 한 줄 순서(DR-31):** 정산 프로젝트에서 셀 하나를 고친 채 새로 고치면 복원 줄이 잠김 줄보다 DOM 순서상 앞에 있다. 이 스펙은 계급 권한·노출을 켜지 않는다(ENG-D2 — 팀장 시점도 04-20 시드만).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 표 위 한 줄·EMPTY 우선순위·힌트 거르기 표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; bash scripts/reset-test-db.sh &amp;&amp; CI=true pnpm playwright test test/e2e/quote-edit-scope.spec.ts test/e2e/archive.spec.ts test/e2e/project-period.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 구조 컨트롤이 정산에서 렌더되거나, 정산 새 줄이 견적 칸을 열거나, 잠긴 셀이 이유 줄 없이 무반응이거나 편집으로 들어가거나, EMPTY 「기간 바꾸기」가 04-22의 기간 칸으로 이어지지 않거나, 순서가 저장에서 사라지거나, 문구가 rev 5 원문과 다르다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 세 묶음을 덮고, `tableLockLine`이 `quoteLockReason`을 부르며 `edit-scope.ts`에 `실행가와 새 줄만` 리터럴이 한 번(04-12의 `quoteLockReason` 안)만 있다
    - 정산에서 줄 삭제·이동·복제 컨트롤이 DOM에 없고(비활성 렌더가 아니다) 키 뒤 줄 수·순서가 그대로다(E2E a)
    - 정산 새 줄이 견적 칸 0 잠김으로 저장된다(E2E b — DB 재조회 대신 새로 고침 뒤 화면 값)
    - 표 위 잠김 줄이 편집 셀이 있는 격자에서 줄 1개 이상일 때만 렌더되고 복원 줄 뒤에 온다(E2E c·c3·j) · 정산 PM의 힌트 줄에 줄 이동·줄 복제·저장이 없고 힌트 문자열 리터럴이 04-28 배열 밖에 새로 생기지 않았다
    - 잠긴 셀의 Enter가 편집 모드를 열지 않고 이유 줄을 보이며, 붙여넣기는 같은 이유의 오류 셀이 된다(E2E c2) · `CellIssue.kind`에 `reason`이 있고 `errorCellCount`가 그것을 세지 않는다(Grep)
    - 삭제 모달이 `ConfirmDialog`이고 1차 라벨이 `견적 줄 삭제`·결과 줄이 `보관함으로 옮겨짐 · 복원은 관리자`이며, 연결 문서 줄의 취소 모달 2차가 `닫기 Esc`다
    - 옮긴 순서가 저장되고 고치기만 한 저장은 순서를 바꾸지 않으며(E2E f), 가운데 줄 보관 뒤 더한 새 줄이 마지막이다(E2E h) · 페이로드에 줄별 `sortOrder`가 없다(Grep)
    - 새 줄을 만드는 경로가 전부 `crypto.randomUUID()`를 쓰고(Grep) 응답 유실 재전송 뒤 줄이 하나다(E2E i)
    - 정산 상세에 발행 요청·지출결의 마감 버튼이 없다(E2E d, D-77)
    - E2E가 `CI=true`(프로덕션 빌드)로 초록이다 — 이 플랜의 완료 판정(04-49 전의 마지막 견적 표 확인)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산·완료의 편집 범위가 화면·키·서버에서 같은 판정이고 잠긴 칸은 표 위 한 줄과 같은 이유를 짧게 말하며, 정산 PM이 새 줄로 늦은 비용을 적을 수 있고, EMPTY가 사람마다 실제 다음 한 수를 말하며, 순서가 저장에서 지켜진다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 판정 → 격자 | 셀 단계·구조 가능성이 서버에서 온다. 화면이 따로 추론하면 서버와 갈라진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-58 | Elevation of Privilege | 정산·완료의 잠긴 칸(화면 쪽) | high | mitigate | 화면은 DTO `cellEditability`와 `structuralEditability`만 읽고 상태 문자열을 해석하지 않는다(금지 항목). 잠긴 칸을 여는 것은 보조이고 판정은 04-12 서버 재판정이다 — 조작 페이로드 거부는 04-12 통합 테스트 |
| T-04-170 | Tampering | 정산 새 줄의 견적 칸(화면 쪽) | low | mitigate | 새 줄의 수량·단가를 0 잠금으로 만들고 붙여넣기가 잠긴 칸을 채우면 오류로 남긴다. 서버 거부(04-12 통합 h)가 최종 방어선이다 |
| T-04-313 | Tampering | 재전송·순서 페이로드(화면 쪽) | low | mitigate | (ENG-D10 · 엔지 리뷰 A P2) 새 줄 id를 화면이 한 번 만들어 재전송에도 유지하고, 순서는 활성 줄 전체 `order` 하나로만 보낸다 — 서버가 집합·소속·정산 규칙을 다시 판정한다(04-12 통합 u·w). 화면 쪽 증명은 E2E h·i |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(`crypto.randomUUID`는 브라우저 내장). `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true` E2E(`quote-edit-scope` · `quote-table` · `archive` · `project-period`) 초록 — 전체 게이트 한 번과 독립 DOM 감사(이 플랜의 S4 backstop 포함)는 04-49 끝(엔지 r2 E2-02 분할)
- 화면 문구가 rev 5 Copywriting 원문이다(표 위 한 줄 = 잠긴 셀 이유 · EMPTY · 모달)
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 정산 PM은 실행가 수정과 견적 0 새 줄만, 완료는 읽기 표로 보이고, 화면과 서버의 판정이 같은 함수다
- 할 수 없는 구조 동작은 버튼도 키도 없고, 잠긴 칸은 표 위 한 줄과 같은 이유를 짧게 말한다
- 저장할 때 열려 있던 셀의 마지막 값이 페이로드에 들고, 저장 뒤 표가 활성 줄 전체로 다시 그려진다(저장 중 잠금·1024 미만 규칙은 04-49)
- 정산 화면이 편집 범위만 바꾸고 Phase 5·6의 발행 요청·마감 흐름을 흉내 내지 않는다(D-77)

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(신규, domain 순수) | `tableLockLine`(`quoteLockReason` 호출 — DR-2) · `quoteTableEmptyState`(`{ message, action? }`) · `visibleHintKeys`(`domain/quotes/edit-scope.ts`) |
| 모듈(04-49로 옮김) | `ui/table/save-lock.ts` · `ui/table/use-editable-width.ts`(엔지 r2 E2-02) |
| `ui/table` API(변경) | `TableProps.onBlockedEdit?(row, columnKey)` · `CellIssue.kind`에 `"reason"`(고정 오류 아님) · 열린 셀 편집기 커밋 입구(없을 때만 — Task 1 ③-b) — `saveLocked`·`collapseBelow`·숨은 열 키 이동은 04-49 |
| 변경된 기존 심볼 | `QuoteLedger`(`editable` 제거 → 셀 단계·구조·EMPTY·표 위 한 줄 props · `canSave` 편집 항 · 저장 결과 전체로 `lines` 교체 · 저장 직전 활성 셀 커밋 · 표 위 줄 순서 복원 → 잠김) · 삭제·취소 `ConfirmDialog` 내용(rev 5) · `moveLine`(옮긴 두 줄 dirty) · 새 줄 경로(`crypto.randomUUID` · `isNew` · 보관 키) · 저장 페이로드(`order`는 이동·가운데 삽입·복제가 있을 때만 · 줄별 `sortOrder` 없음) · `LINE_STATUS_LABELS`(`QuoteLineStatus` 타입) — `saveLocked`·`useEditableWidth`·1024 미만 1차·복원 줄 모든 폭은 04-49 |
| 화면 문구(rev 5 원문) | `정산 · 실행가와 새 줄만` · `완료 · 견적 줄 잠김`(표 위 한 줄 = 셀 이유) · `이 프로젝트에 견적 줄이 없습니다` + 「첫 줄 만들기」/「기간 바꾸기」/`· 담당 PM {이름}` · 삭제 모달 `보관함으로 옮겨짐 · 복원은 관리자` · 취소 모달 `견적가 0 · 이력과 연결된 지출결의는 그대로` |
| 테스트 | `test/e2e/quote-edit-scope.spec.ts`(신규 — e3 활성 셀 마지막 값 포함 · 04-49가 폭 케이스를, 04-26이 상한 케이스를 더한다) · `test/unit/domain/quote-edit-scope.test.ts`(확장) |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-26 / 사용자 D10 — 정산 줄 추가 허용, 삭제·이동·복제 없음 | DECISION | Plan 04-30, Task 2 ②: 구조 가능성으로 컨트롤·키 on/off · E2E a·b |
| A-26a / 사용자 D12 — 새 줄 견적 칸 0 잠금 | DECISION | Plan 04-30, Task 2 ② 기본값 0 + `isNewLine` 셀 단계 · E2E b |
| A-03 (클라이언트) — 순서 미전송 | CRITICAL | Plan 04-30, Task 2 ③: 새 줄·옮긴 줄만 `sortOrder` · E2E f |
| A-12 — 1차 렌더 조건 | WARNING | Plan 04-30, Task 1 ②: `canSave` 편집 항을 셀 단계·구조로 |
| C-07 — 힌트 줄은 되는 키만 | WARNING | Plan 04-30, Task 2 ①: `visibleHintKeys`(04-28 배열 거르기) |
| A-18 — E2E 날짜 | WARNING | Plan 04-30: `kstToday` + `addDays` |
| OV-7 / 사용자 D21 | HIGH | 04-12에서 화면 쪽을 떼어 서버 판정(04-12)과 분리 — 견적 표 파일은 04-28 뒤 순서만 지킨다 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음 — Round 3에서 해소: rev 5가 D10 문구를 흡수했고 D16 인용은 오인용이었다) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 엔지 리뷰 A §2 P1 — 정산 새 줄 기본값 「수량 0」이 서버 검증과 모순 | HIGH | Supersedes Round 1 A-26a의 「수량 0」: Plan 04-30, `<probe_fallback>` 수량 1 · 단가 원화 0 · Task 2 ② · E2E b |
| ENG-D10 — 재전송 중복(화면 쪽) | DECISION | Plan 04-30, `<probe_fallback>` 새 줄 uuid · Task 2 ② · E2E i(`route.fetch` 뒤 응답 버림 → 다시 저장 → 한 줄) · T-04-313 (서버는 04-12) |
| 엔지 리뷰 A §2 P2 — 순서 전송(부분 `sortOrder`의 동률·틈) | WARNING | Supersedes Round 1 A-03 「새 줄·옮긴 줄만 `sortOrder`」: Plan 04-30, Task 2 ③ 두 경우만(`order` 한 번 / 없음) · E2E f·h |
| 엔지 리뷰 A §2 P2 — 저장 뒤 화면이 저장한 줄만 남김 | WARNING | Plan 04-30, Task 1 ③ 결과 전체로 교체 · E2E e2(공백 4) |
| 엔지 리뷰 A P3 — 줄 상태 도메인 상수 | NOTE | Plan 04-30, Task 1 ③ `LINE_STATUS_LABELS`를 `QuoteLineStatus`로 |
| 엔지 리뷰 A §1 P2 — 담당자 이름 N+1 · 필요할 때만 | NOTE | Plan 04-30, Task 2 ② 줄 0개·종료일 지남일 때만 `projectResponsibles` |
| ENG-D2 — E2E가 권한·노출을 켜지 않음 | DECISION | Plan 04-30, Task 2 ④ 마지막 문장 |
| ENG-D11 — 앞 플랜 결함 멈춤 규칙 완화 | DECISION | objective 「앞 플랜 결함」 문단 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 디자인 리뷰(`docs/designs/plant8-erp-phase4-design-review-260923.md`) · UI-SPEC rev 5

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-1 — `<probe_fallback>` 106행의 D16 오인용과 SUMMARY 「문구 차이」 우회 | CRITICAL | 삭제 — 「문구 = rev 5 원문(계약)」 줄로 교체, Task 2 ⑤·검증·Artifacts·Round 1 Deferred 행에서 「문구 차이」 제거 |
| DR-2 — 정산 표 위 한 줄과 셀 이유가 다른 문자열 | CRITICAL | Plan 04-30, Task 2 ①: `tableLockLine`이 04-12 `quoteLockReason`을 부른다(`정산 · 실행가와 새 줄만` · 완료는 편집 셀 격자에서만) · E2E c·c3 |
| DR-3 · 계약 3 — 저장 응답이 저장 중 입력을 덮음 | CRITICAL | Plan 04-30(소유 — 네 편집기가 선 뒤 한 곳), Task 3 ①②③: `isGridActionAllowed` 단위 · `Table.saveLocked`(`aria-busy`) · 매출 표·기간·총 매출 예상가 칸 `readOnly` · 연타 무시 · E2E `ledger-save-flow.spec.ts` · T-04-335 · 금지 항목 2 |
| DR-35 — 연결 문서·정산·승인 잠김 칸의 이유 한 줄 + 붙여넣기 오류 셀 | NOTE | Plan 04-30, Task 2 ③: `onBlockedEdit` + `CellIssue` `reason` · E2E c2 (PM 조정 줄의 무문구는 04-23) |
| DR-31 — 표 위 한 줄 순서 | NOTE | Task 2 ② · E2E j: 복원 줄 → (이전 차수 복원 줄) → 잠김 줄 |
| DR-14 · DR-24 · DR-36 · 계약 6 — 좁은 폭 편집·열 접기 | HIGH | Plan 04-30, Task 3 ④: `useEditableWidth` · `collapseBelow` · 1024 미만 보기 전용 · 1차는 표 밖 칸 dirty일 때만 · E2E k·l·m · backstop 둘 · T-04-336 |
| DR-12 — 삭제·취소 모달 | NOTE | Task 2 ②: 04-46이 옮긴 `ConfirmDialog`에 rev 5 내용(결과 줄 · 2차 자동 `닫기 Esc`) (Round 5 포인터 정정 — 옛 「04-08」) |
| P0 — 완료 PM 읽기 표의 잠김 줄 · 힌트 `저장` · EMPTY 완료 꼬리 | NOTE | truths · Task 2: 읽기 표에는 잠김 줄·힌트 없음 · 힌트에서 `저장` 제거 · 완료 EMPTY는 사실만 |
| UI Considerations 끌어올림 — S4(empty · zero-one-many · overflow · 폰·좁은 PC 구조 컨트롤 · partial 정산) · S19(loading · 성공) · S13·S17 loading | NOTE | `must_haves.truths` — backstop 넷 + explicit 줄들 |
| DR-1 — rev 4 줄 번호 | NOTE | `<context>`·`read_first`를 rev 5 절 제목 + 현재 줄로 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 충돌 셀 키보드(DR-25) | 이 플랜의 범위가 아니다 — `ui/table` 충돌 셀을 가진 플랜(T21)이 맡는다 |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B4 — 현재 차수 복원 줄을 1024 미만에서 숨김(옛 `<probe_fallback>` 폭 판정 · Task 3 ④) — UI-SPEC rev 5 S18 overflow(사용자 2026-09-23 resolved explicit)와 S19 error(DR-6 → 복원 줄)에 반함 | BLOCKER | 숨김 두 곳을 지움 · truths(explicit 한 줄 + S18 overflow backstop) · Task 3 ④-b(모든 폭 렌더 · `keep-all` · 두 3차 한 묶음 함께 줄바꿈 · 폰 44px · 두 줄 각자 줄바꿈) · E2E l2(375 복원 줄 → 「복원」 → 기간 칸 dirty + 1차) · l3(1000 읽기 표 dirty 셀) · DOM 감사에 S18 overflow · `<probe_fallback>` 「rev 5 공백」(1024 미만 표 칸만 복원 시 저장 수단 미정 — 새 UI·문구 없이 보고) |
| W2 — 크기(104k · 17파일) | WARNING | 테스트 아닌 파일 13개 ≤ 15 — 규칙대로 **분할하지 않는다**. 폭 판정 가정 줄을 Task 3 ④ 정본을 가리키게 줄여 중복을 덜었다(B4 추가분과 상쇄 — 추정 104k 유지) |
| 04-22 분할(B5)·04-08 분할(W2) — 포인터 | NOTE | `pre-estimate-field.tsx`는 04-44 산출물 · `ui/confirm-dialog`·`Button` 개정은 04-46 산출물로 `<context>`·read_first·Task 2 ②·objective (l) 정정 · 네 편집기 선행을 「04-22 · 04-44 다음」으로. `depends_on`은 그대로(04-12 → 04-44 → 04-22, 04-28 → 04-46 사슬로 둘 다 조상) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 1024 미만에서 표 칸 편집만 복원된 경우의 저장 수단 | rev 5 공백 — 새 UI·문구는 P0상 계획이 만들지 않는다. 오케스트레이터에 보고해 사용자·UI-SPEC 판단을 받는다(그 전까지 rev 5 S3 규칙 그대로) — **Round 5에서 해소(UI-SPEC rev 5 후속 결정 R1)** |

### Round 5 — 체커 revision 1 후속 정리(2026-09-23 · 사용자 후속 결정 R1 · 분할 포인터)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 사용자 결정 R1 — 1024 미만 1차 「일괄 저장 N」은 N ≥ 1이면 항상 렌더(표 셀 dirty · 「복원」 칸 포함), 표는 보기 전용 그대로 · 안내 글자 없음 | DECISION | Supersedes Round 3 DR-24 행의 「1차는 표 밖 칸 dirty일 때만」과 Round 4 Deferred 「rev 5 공백」: truths 둘 · key_link · objective (k) · `<context>` · `<probe_fallback>`(공백 보고를 R1 결정 줄로 교체) · Task 3 read_first · ④ · ④-b · E2E l · l2(375 표 칸만 복원 → 1차 → 저장) · l3(1000 표 칸만 복원 → 1차 → 저장) · fails_when · 수용 기준 · success_criteria. UI-SPEC rev 5 후속 결정 R1(47행) · 적용 규칙 S3 1132–1135행 인용 |
| 분할 포인터(04-19 → 04-47 · 04-08 → 04-46) | NOTE | `<probe_fallback>` 새 줄 id 줄: 복원 키 id 확인 = 04-47 Task 2 ② · 붙여넣기 새 줄 = 04-47 Task 1 ③ · Round 3 DR-12 행 「04-08」 → 04-46. `depends_on` 변경 없음(04-47은 이 플랜의 후행 — 소비자가 아니라 생산자에게 알리는 포인터) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 6 — 엔지니어링 리뷰 r2 반영(2026-09-24 재계획 · `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` · 머지 묶음 결정)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2-02 — 77KB·3태스크로 체커 상한 초과 | HIGH | 옛 Task 3을 신규 04-49(웨이브 15)로 분리 — truths 넷 · backstop 다섯 · 금지 항목 하나 · T-04-335·T-04-336 · `save-lock.ts`·`use-editable-width.ts`·`ledger-save-flow.spec.ts`와 매출·기간·총 매출 예상가 칸 파일이 함께 이동. estimate 104k → 72k, 2태스크. `<probe_fallback>` 세 줄(저장 중 잠금 소유 · 폭 판정 · R1)은 04-49로 |
| 분할안 — 편집기 커밋과 저장 페이로드 수집을 한 플랜에(「활성 셀 마지막 값 → 페이로드」) | NOTE | truth · Task 1 ③-b · E2E e3(요청 본문 + 새로 고친 뒤 값, `Ctrl+S`·1차 두 경로) |
| PR #38 알려진 문제 — 저장한 줄만 남음 · 줄 순서 섞임 · 삭제 미반영 | NOTE | objective 「PR #38 알려진 문제」 문단 — Task 1 ③ · Task 2 ②④ · E2E e·e2·f·h(서버 쪽은 04-12). 미저장 복원 미연결은 04-22 · 04-49 |
| 머지 묶음 결정(HANDOFF decisions[0]) | DECISION | objective 머지 묶음 ② 줄 · Task 2 E2E `CI=true`(완료 판정) |
| CEO r2 지시 3 — T-04-SC 「04-03 재개」 문구 | LOW | 삭제 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-30-SUMMARY.md` when done
</output>
