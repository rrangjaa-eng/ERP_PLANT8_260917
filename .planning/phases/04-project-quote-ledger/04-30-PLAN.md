---
phase: 04-project-quote-ledger
plan: 30
type: execute
wave: 12
depends_on: ["04-12"]
files_modified:
  - domain/quotes/edit-scope.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/e2e/quote-edit-scope.spec.ts
autonomous: true
requirements: [PROJ-02, UX-05, UX-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "정산 프로젝트를 담당 PM이 열면 기존 줄은 실행가 셀만 편집에 들어가고 나머지 셀은 `aria-readonly`이며, 셀 단계는 서버 DTO(`cellEditability`)에서 온다 — 화면은 상태 문자열을 해석하지 않는다(D-78 — 사용자 D10 개정, §7-3 (가), UX-06)"
    - "정산의 담당 PM은 「줄 추가」·`Ctrl+Enter`로 새 줄을 만들 수 있고, 새 줄은 소분류·항목·거래처·비고·실행가만 편집에 들어가며 수량·단가 셀은 0으로 잠겨 있다(사용자 D10·D12)"
    - "정산에서는 줄 삭제 · 줄 이동 · 줄 복제 컨트롤이 렌더되지 않고 `Delete`·`Alt+↑↓`·`Ctrl+D`는 아무 일도 하지 않는다 — 비활성 버튼을 세우지 않는다(사용자 D10, §7-9)"
    - "정산 단계에서 이 페이즈가 만드는 것은 상태(04-06)와 편집 범위(D-78)뿐이다 — 정산 프로젝트 상세에 매출 세금계산서 발행 요청·지출결의 마감 버튼이나 동작이 없다(D-77 — 발행 요청 흐름과 지출결의 마감은 Phase 5·6)"
    - "완료 프로젝트는 PM에게 모든 셀이 잠김이고 표가 편집 격자가 아니라 캡션 있는 읽기 표(흰 머리글)로 렌더되며 1차 저장 버튼이 없다(D-47, §7-3 (가), U-5)"
    - "표 위 한 줄이 이유를 말한다 — 정산 `정산 · 실행가와 새 줄만 고칠 수 있음` · 완료 `완료 · 견적 줄이 잠김`. 줄이 0개면 이 줄을 그리지 않는다(S4, SYSTEM.md 개정 ④ — 정산 문구는 사용자 D10으로 바뀐 범위를 말한다)"
    - "힌트 줄은 그 사람에게 실제로 되는 것만 적는다 — 04-28의 힌트 항목 배열에서 구조 편집 가능성으로 항목을 뺀다: 정산 PM은 `이동 ↑↓←→ · 붙여넣기 Ctrl+V · 취소 Esc · 새 줄 Ctrl+Enter · 저장 Ctrl+S`(줄 이동·줄 복제 없음). Tab·범위 복사는 04-19가 더한다(§7-9, UI-SPEC S4 힌트 줄, CEO C-07)"
    - "0줄 표의 EMPTY 한 줄이 상태 × 보는 사람으로 갈린다 — 줄 추가 권한자(정산의 담당 PM 포함) `… · 첫 줄 만들기`+Ctrl+Enter · 줄 추가 불가 + 기간 권리 `lead` `… · 기간 바꾸기`(누르면 기간 칸 종료일에 포커스) · 그 밖 `… · 담당 PM {이름}` · 완료 `… · 완료 · 견적 줄이 잠김` — 렌더되지 않는 행동을 문구에 적지 않는다(S4 empty, Copywriting Empty — 사용자 D10으로 정산 PM 변형이 「첫 줄 만들기」가 됐다)"
    - "미수주·수주중·진행 프로젝트의 편집은 04-04 그대로 전부 열려 있다(D-45)"
    - "저장된 줄을 지우면 확인 모달(`견적 줄 삭제` · 부제 `{항목} · {견적가}` · 1차 `견적 줄 삭제` · 2차 `취소 Esc`) 뒤 dirty 한 건이 되어 일괄 저장 때 보관되고, 연결 문서가 있는 줄은 `견적 줄 취소` 모달(1차 `견적 줄 취소` · 2차 `닫기 Esc`)로 상태만 `취소`가 된다(D-56, Copywriting Destructive)"
    - "줄을 옮기거나 새로 만든 줄만 저장 페이로드에 표시 순서 `sortOrder`를 싣는다 — 옮긴 두 줄 모두 dirty가 되고, 고치기만 한 줄은 순서를 싣지 않아 순서가 그대로다(CEO A-03 클라이언트 쪽)"
    - "1차 「일괄 저장」 렌더 조건 `canSave`의 편집 항은 「편집 가능 셀이 하나라도 있거나 줄을 추가할 수 있음」이다 — `editable` boolean prop이 사라진다(CEO A-12, 04-22 `canSave`의 마무리)"
    - statement: "견적 줄 EMPTY 변형이 다섯 상태 × 세 계급(담당 PM·팀장·경영관리) + 권한 없는 사람 렌더에서 문구와 버튼 유무가 이 플랜의 표(사용자 D10 반영)와 일치한다 — 렌더 감사"
      verification: backstop
    - statement: "견적 줄이 0·1·6줄일 때 0줄은 EMPTY 한 줄, 1줄과 6줄은 같은 그룹 머리글 + `합계 (공급가액 · N줄)` 형식이고 조수사가 바뀌지 않는다 — 렌더 감사"
      verification: backstop
  prohibitions:
    - statement: "화면이 상태 문자열로 셀·구조 편집 가능성을 추론해서는 안 된다. 서버 판정(04-12)과 화면이 갈라지면 사용자가 연 칸이 저장 때 거부되거나(UX-04 위반), 서버가 받는 칸이 화면에 잠겨 보인다 — 셀 단계는 DTO, 구조는 `structuralEditability` 결과만 쓴다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/quotes/edit-scope.ts"
      provides: "04-12 판정 함수에 더해 표 위 한 줄 · EMPTY 변형 · 힌트 항목 거르기(순수)"
      exports: ["tableLockLine", "quoteTableEmptyState", "visibleHintKeys"]
    - path: "test/e2e/quote-edit-scope.spec.ts"
      provides: "정산 PM 실행가·새 줄(견적 0 잠김) · 구조 컨트롤 부재 · 완료 읽기 표 · 삭제→보관 · 정산 EMPTY · D-77 버튼 부재 · 순서 저장(상한 케이스는 04-26이 더한다)"
      min_lines: 70
  key_links:
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "domain/quotes/lines.ts"
      via: "열의 편집 가능성이 DTO의 `cellEditability`에서 온다 — 화면이 상태를 해석하지 않는다"
      pattern: "cellEditability"
    - from: "app/(app)/projects/[id]/page.tsx"
      to: "domain/quotes/edit-scope.ts"
      via: "구조 가능성·표 위 한 줄·EMPTY·힌트 항목을 서버에서 계산해 넘긴다"
      pattern: "structuralEditability"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/period-field.tsx"
      via: "EMPTY 「기간 바꾸기」가 04-22의 `openPeriodField(\"end\")`를 부른다"
      pattern: "openPeriodField"
---

<objective>
**As a** 정산 단계의 기획 PM, **I want to** 견적 표에서 지금 고칠 수 있는 칸만 편집에 들어가고, 늦게 발견된 비용은 새 줄로 실행가만 적으며, 할 수 없는 동작(삭제·이동·복제)은 버튼도 키도 없기를, **so that** 화면에서 연 칸이 저장 때 거부되는 일이 없고, 왜 잠겼는지를 표 위 한 줄이 말해 준다.

**2026-09-23 CEO 리뷰로 04-12에서 떼어 낸 화면 쪽 플랜.** 04-12가 편집 범위의 서버 판정(셀 단계 DTO · 구조 판정 · 보관·복원 · 경합)을 만들었고, 이 플랜은 그 결과를 견적 표에 그린다 — 셀 단계 격자 · 정산 새 줄(견적 칸 0 잠금, 사용자 D10·D12) · 구조 컨트롤 · 표 위 한 줄 · 힌트 줄(04-28 배열 거르기) · EMPTY 변형 · 삭제·취소 모달 · 순서 전송(A-03) · `canSave` 마무리(A-12) · D-77 버튼 부재.

Purpose: 셀 단계를 화면이 추론하면 UX-06·ROADMAP 기준 3 위반이다. 04-26(상한 EMPTY·붙여넣기 거부)·04-23(조정 줄 EMPTY 우선순위 ②)·04-24(차수 모달)가 이 플랜의 EMPTY 함수·힌트 거르기·구조 props를 넓힌다.

Output: 화면용 순수 함수 셋 · 견적 표 배선 · E2E 1(신규) · 독립 DOM 감사.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. E2E 날짜는 `kstToday(new Date())` + `addDays`(CEO A-18). 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-28-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 442–456행(S4 상태) · 1114–1141행(S4 상태별 셀 단계 표 · 정산 구조 잠금 — 줄 추가는 사용자 D10이 뒤집었다) · 1147–1152행(힌트 줄) · 321–347행(Copywriting EMPTY 여덟 · 셀 오류) · 368–369행(Destructive — 견적 줄 삭제·취소)
# docs/design/SYSTEM.md §7-3 (가) 잠김 행 · §7-9 표시 단축키
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-05(단축키 — 잠긴 상태의 무동작)** 와 PROJ-02·UX-04의 화면 부분이다. 수용 기준은 04-12 SUMMARY의 판정 결과·UI-SPEC S4·Copywriting에서 끌어왔다. S4 `## UI Considerations`의 empty(변형) backstop · zero-one-many backstop은 flat-scalar 마커로, 2026-09-23 partial(정산)은 truths로 옮겼다. S4 partial(상한)과 그 금지 항목은 04-26이, 조정 줄 쪽 항목은 04-13·04-23·04-14가 옮긴다.

**계획 단계 가정·확정(표면화):**
- **사용자 D10으로 바뀐 문구(UI-SPEC은 사용자 D16에 따라 고치지 않는다 — SUMMARY에 「문구 차이」 표로 남긴다):** 표 위 한 줄 정산 `정산 · 실행가와 새 줄만 고칠 수 있음`(옛 `정산 · 실행가만 고칠 수 있음`) · 정산 EMPTY — 담당 PM은 「첫 줄 만들기」, 그 밖은 기간 권리에 따라 「기간 바꾸기」 또는 `담당 PM {이름}`(옛 `정산 · 기간 늦추기는 팀장 {이름}`은 PM이 줄을 더할 수 없던 전제의 문구라 쓰지 않는다) · 힌트 줄 정산 PM 다섯 항목.
- **정산 새 줄의 기본값:** 수량 0 · 단가 원화 0 · 상태 기본값(견적가 0). 줄 추가 경로(「줄 추가」·`Ctrl+Enter`·EMPTY 첫 줄)가 이 기본값으로 만든다. 붙여넣기가 새 줄의 잠긴 칸(수량·단가)을 채우면 04-04 규약대로 그 칸이 오류로 남는다(잠긴 칸 붙여넣기 — C-03의 「잠긴 칸은 오류」와 같은 규칙).
- **새 줄의 셀 단계:** 저장 전 새 줄은 서버 DTO가 없으므로 04-12의 `lineCellEditability({ …, isNewLine: true })`를 클라이언트가 같은 입력으로 부른다(같은 함수 — 순수, `edit-scope.ts`는 리포지토리를 import하지 않아 클라이언트 번들에 들어갈 수 있다).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 정산 PM의 견적 표가 서버 셀 단계로 열리고 실행가만 고쳐 저장된다(DTO → 격자 → 키보드 → 저장 → E2E)</name>
  <files>app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `domain/quotes/edit-scope.ts`(04-12 산출물 전문) · `domain/quotes/lines.ts`의 `QuoteLineDto`(`cellEditability`·`hasLinkedDocuments`)
    - `ui/table/types.ts`(전문) — `CellEditability` 세 단계와 열의 `editability`
    - `ui/table/Table.tsx` 70–140행 — 편집 가능 셀 스캔으로 `role="grid"`·머리글 바탕이 정해지는 자리
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `editability: () =>`(열 정의)·`isEditable: () =>`·`editable` prop·1차 렌더 조건(`canSave`, 04-22)을 찾아 범위만 Read
    - `app/(app)/projects/[id]/page.tsx`(전문)
    - `test/e2e/quote-table.spec.ts`·`test/e2e/project-period.spec.ts` — 상태를 DB에 두는 준비와 역할 로그인 형태
  </read_first>
  <action>
① `[id]/page.tsx`는 `editable` boolean 대신 04-12의 DTO(줄마다 `cellEditability`)와 `structuralEditability` 결과를 넘긴다. `quote-table.tsx`의 열 `editability`·키보드 `isEditable`이 행의 `cellEditability[칸]`을 읽고, 저장 전 새 줄은 `lineCellEditability({ status, canWrite, hasLinkedDocuments: false, isNewLine: true })`로 만든 값을 쓴다(`<probe_fallback>`). 이렇게 하면 완료 PM의 표는 편집 셀 0개라 `ui/table`이 캡션 있는 읽기 표로 자동 전환된다(모드 prop 없음). `editable` prop을 지운다.

② **`canSave` 마무리(A-12).** 04-22가 둔 `canSave`의 `editable` 항을 「줄 중 편집 가능 셀이 하나라도 있음 또는 `structural.insert`」로 바꾼다 — 서버(`page.tsx`)가 계산한다.

③ **E2E(트레이서 증명, `test/e2e/quote-edit-scope.spec.ts` 신규).** 준비에서 정산 프로젝트(종료일 `addDays(kstToday(new Date()), -3)`, 줄 둘)를 두고 담당 PM으로 연다 → 실행가 셀은 Enter로 편집에 들어가고 단가 셀은 편집에 들어가지 않으며(`aria-readonly`) → 실행가를 고쳐 `Control+s` → 합계 행에 `저장됨` → 새로 고쳐도 실행가가 새 값. 같은 스펙에서 완료 프로젝트를 PM으로 열면 표가 캡션 있는 읽기 표이고 1차 버튼이 없다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — `editable` prop 제거가 호출부 어딘가에 남았다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/quote-edit-scope.spec.ts test/e2e/quote-table.spec.ts test/e2e/project-period.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 정산 PM의 실행가 편집·단가 읽기·저장이 이어지지 않았거나, 완료 표가 읽기 표로 바뀌지 않았거나, 진행 프로젝트의 04-04 편집 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `quote-table.tsx`의 열 편집 가능성이 `cellEditability`에서 오고 `editable` boolean prop이 사라졌다(Grep `editable:` · `editable={` 0건 — `cellEditability`·`canSave` 제외)
    - 1차 렌더 조건이 `canSave` 하나이고 그 편집 항이 셀 단계·구조에서 온다
    - E2E가 정산 PM의 실행가 편집·단가 `aria-readonly`·저장 뒤 재조회와 완료 읽기 표·1차 부재를 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산 PM의 견적 표가 서버 셀 단계로 열리고 실행가 한 칸 편집이 저장까지 이어지며, 완료는 읽기 표가 됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 구조 컨트롤 · 정산 새 줄 · 표 위 한 줄 · 힌트 줄 · EMPTY · 삭제·취소 모달 · 순서 전송 + 화면 검증(S4 backstop 둘)</name>
  <files>domain/quotes/edit-scope.ts, test/unit/domain/quote-edit-scope.test.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 321–328행(EMPTY 고르는 규칙 · 여덟 행 · 우선순위) · 368–369행(Destructive — 견적 줄 삭제·취소, 2차 라벨 규칙) · 1147–1151행(힌트 줄 빼기)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `confirmDeleteLine`·`DeleteLineDialog`·`addLine`·`duplicateLine`·`moveLine`·힌트 항목 배열(04-28)·`emptyMessage`를 찾아 범위만 Read
    - `domain/projects/responsibles.ts`(04-11 — 담당 PM·팀장 이름) · `domain/projects/period.ts`의 `periodEditRights`와 `QuoteLedger.openPeriodField`(04-22)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - `tableLockLine`: 정산 → `정산 · 실행가와 새 줄만 고칠 수 있음` · 완료 → `완료 · 견적 줄이 잠김` · 그 밖 → null
    - `quoteTableEmptyState({ status, canAddLine, periodRights, pmName, teamLeadName })`: 진행·정산 + 줄 추가 가능 → 「첫 줄 만들기」 · 진행 + 불가 → `담당 PM {이름}` · 정산 + 불가 + 기간 권리 `lead` → 「기간 바꾸기」 · 정산 + 불가 + 그 밖 → `담당 PM {이름}` · 완료 → `완료 · 견적 줄이 잠김` · 줄 추가 가능이 기간 권리보다 우선
    - `visibleHintKeys(allKeys, structural)`: 정산(`insert`만) → 줄 이동·줄 복제 키가 빠지고 새 줄은 남는다 · 완료(전부 거짓) → 새 줄·줄 이동·줄 복제가 빠진다 · 진행 → 그대로
  </behavior>
  <action>
① `edit-scope.ts`에 `tableLockLine` · `quoteTableEmptyState(…)` → `{ message, action?: "addLine" | "openPeriodEnd" }`(Copywriting 우선순위 ① 첫 줄 만들기 ③ 기간 바꾸기 ④ 담당자 이름 ⑤ 완료 사실 — ② 조정 줄 추가는 04-23이 더한다) · `visibleHintKeys(allKeys, structural)`(04-28이 둔 힌트 항목 배열의 키 목록에서 구조 가능성으로 뺀다 — 문자열 리터럴을 새로 만들지 않는다)을 더한다. 결정표를 먼저 쓴다(RED).

② **화면.** `[id]/page.tsx`가 `tableLockLine`·`quoteTableEmptyState` 결과를 넘긴다(담당자 이름은 04-11의 `projectResponsibles`, 기간 권리는 04-22의 `periodEditRights`). `quote-table.tsx`: 「줄 추가」·EMPTY 3차·`Ctrl+Enter`는 `structural.insert`로, Delete 모달은 `structural.archive`로, `Alt+↑↓`는 `structural.reorder`로, `Ctrl+D`는 `structural.duplicate`로 켜고 끈다(불가면 **렌더하지 않고** 키는 무동작 — 비활성 버튼을 세우지 않는다) · 정산 새 줄은 수량 0 · 단가 원화 0 기본값으로 만들고 셀 단계는 `isNewLine: true` 판정 · 표 위 한 줄(줄이 1개 이상일 때만) · 힌트 줄은 `visibleHintKeys` 결과만 · EMPTY는 서버가 고른 문구와 액션(「기간 바꾸기」는 04-22의 `openPeriodField("end")`) · 저장된 줄 삭제는 확인 모달(제목 `견적 줄 삭제` · 부제 `{항목} · {견적가}` · 본문 `보관함으로 이동합니다 · 관리자가 복원할 수 있습니다` · 1차 `견적 줄 삭제` · 2차 `취소 Esc` — 지금 1차 라벨 `삭제` 단독은 §8 규칙 1 위반이라 고친다) 뒤 화면에서 빠지고 `archivedLineIds`에 들어가 dirty 한 건으로 센다. 연결 문서가 있는 줄의 Delete는 `견적 줄 취소` 모달(1차 `견적 줄 취소` · 2차 `닫기 Esc` — §7-8 보강)로 상태 칸을 `취소`로 바꾼다. 정산 화면에 발행 요청·지출결의 마감 같은 버튼을 두지 않는다(D-77 — Phase 5·6).

③ **순서 전송(A-03 클라이언트).** `moveLine`(`Alt+↑↓`)이 옮긴 두 줄을 모두 dirty로 표시하고, 저장 페이로드는 **새 줄과 옮긴 줄에만** 표시 순서 기준 `sortOrder`(전체 목록 위치)를 싣는다 — 고치기만 한 줄은 `sortOrder`를 싣지 않는다(04-12 서버가 현재 값을 지킨다).

④ **E2E(`quote-edit-scope.spec.ts`).** (a) 정산 PM 화면에 줄 삭제·이동·복제 컨트롤이 DOM에 없고 `Delete`·`Alt+ArrowDown`·`Control+d` 뒤 줄 수·순서가 그대로다, (b) 정산 PM이 「줄 추가」로 새 줄을 만들면 수량·단가 셀이 `aria-readonly`이고 0이며, 항목·실행가를 적어 `Control+s` → 저장 뒤 새로 고쳐도 그 줄이 견적가 0 · 실행가 값으로 있다(사용자 D10·D12), (c) 표 위 한 줄 `정산 · 실행가와 새 줄만 고칠 수 있음`, 정산 PM 힌트 줄 다섯 항목, (d) 정산 상세에 `발행 요청`·`마감` 이름의 버튼이 없다(D-77), (e) 수주중에서 줄 삭제 → 모달 1차 `견적 줄 삭제` → 저장 → 새로 고쳐도 없다, (f) 진행에서 3번째 줄을 `Alt+ArrowUp`으로 옮기고 저장 → 새로 고쳐도 옮긴 순서 · 다른 줄의 실행가만 고쳐 저장 → 순서 그대로(A-03), (g) 0줄 정산 표의 EMPTY가 PM에게 「첫 줄 만들기」, 그 팀 팀장(줄 추가 불가 · 기간 권리 `lead`)에게 「기간 바꾸기」(누르면 종료일 칸 포커스).

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 S4 backstop 둘(EMPTY 변형 × 상태 × 계급 렌더 · 0/1/6줄 합계 형식)을 DOM 실측 → 필요하면 `project-detail.module.css`만 고친다 → `CI=true pnpm test` 한 번. 판정과 「문구 차이」 표(UI-SPEC 원문 → 이 플랜 문구, 사용자 D10 근거)를 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 표 위 한 줄·EMPTY 우선순위·힌트 거르기 표가 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-edit-scope.spec.ts test/e2e/archive.spec.ts test/e2e/project-period.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 구조 컨트롤이 정산에서 렌더되거나, 정산 새 줄이 견적 칸을 열거나, EMPTY 「기간 바꾸기」가 04-22의 기간 칸으로 이어지지 않거나, 순서가 저장에서 사라진다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S4 backstop 두 판정과 「문구 차이」 표를 SUMMARY에서 확인</test>
      <expected>EMPTY 변형 문구·버튼 유무가 이 플랜의 표와 같고, 0/1/6줄에서 합계 형식이 같다</expected>
      <why_human>상태 × 계급 조합 렌더는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 결정표 단위 테스트가 `behavior` 세 묶음을 덮는다
    - 정산에서 줄 삭제·이동·복제 컨트롤이 DOM에 없고(비활성 렌더가 아니다) 키 뒤 줄 수·순서가 그대로다(E2E a)
    - 정산 새 줄이 견적 칸 0 잠김으로 저장된다(E2E b — DB 재조회 대신 새로 고침 뒤 화면 값)
    - 표 위 한 줄이 줄 1개 이상일 때만 렌더되고, 정산 PM의 힌트 줄이 항목 다섯이며 힌트 문자열 리터럴이 04-28 배열 밖에 새로 생기지 않았다
    - 삭제 모달 1차 라벨이 `견적 줄 삭제`이고, 연결 문서 줄의 취소 모달 2차가 `닫기 Esc`다
    - 옮긴 순서가 저장되고 고치기만 한 저장은 순서를 바꾸지 않는다(E2E f)
    - 정산 상세에 발행 요청·지출결의 마감 버튼이 없다(E2E d, D-77)
    - 독립 DOM 감사 판정과 「문구 차이」 표가 SUMMARY에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>정산·완료의 편집 범위가 화면·키·서버에서 같은 판정이고, 정산 PM이 새 줄로 늦은 비용을 적을 수 있으며, EMPTY가 사람마다 실제 다음 한 수를 말하고, 순서가 저장에서 지켜진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 판정 → 격자 | 셀 단계·구조 가능성이 서버에서 온다. 화면이 따로 추론하면 서버와 갈라진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-58 | Elevation of Privilege | 정산·완료의 잠긴 칸(화면 쪽) | high | mitigate | 화면은 DTO `cellEditability`와 `structuralEditability`만 읽고 상태 문자열을 해석하지 않는다(금지 항목). 잠긴 칸을 여는 것은 보조이고 판정은 04-12 서버 재판정이다 — 조작 페이로드 거부는 04-12 통합 테스트 |
| T-04-170 | Tampering | 정산 새 줄의 견적 칸(화면 쪽) | low | mitigate | 새 줄의 수량·단가를 0 잠금으로 만들고 붙여넣기가 잠긴 칸을 채우면 오류로 남긴다. 서버 거부(04-12 통합 h)가 최종 방어선이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 독립 DOM 감사(S4 backstop 둘) 판정과 「문구 차이」 표가 SUMMARY에 있다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 정산 PM은 실행가 수정과 견적 0 새 줄만, 완료는 읽기 표로 보이고, 화면과 서버의 판정이 같은 함수다
- 할 수 없는 구조 동작은 버튼도 키도 없다
- 정산 화면이 편집 범위만 바꾸고 Phase 5·6의 발행 요청·마감 흐름을 흉내 내지 않는다(D-77)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(신규, domain 순수) | `tableLockLine` · `quoteTableEmptyState` · `visibleHintKeys`(`domain/quotes/edit-scope.ts`) |
| 변경된 기존 심볼 | `QuoteLedger`(`editable` 제거 → 셀 단계·구조·EMPTY·표 위 한 줄 props · `canSave` 편집 항) · `DeleteLineDialog` 라벨 · `moveLine`(옮긴 두 줄 dirty) · 저장 페이로드(`sortOrder`는 새 줄·옮긴 줄만) |
| 화면 문구(사용자 D10으로 바뀜 — SUMMARY 「문구 차이」) | `정산 · 실행가와 새 줄만 고칠 수 있음` · 정산 EMPTY 변형 · 정산 힌트 줄 다섯 항목 |
| 테스트 | `test/e2e/quote-edit-scope.spec.ts`(신규 — 04-26이 상한 케이스를 더한다) · `test/unit/domain/quote-edit-scope.test.ts`(확장) |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-26 / 사용자 D10 — 정산 줄 추가 허용, 삭제·이동·복제 없음 | DECISION | Plan 04-30, Task 2 ②: 구조 가능성으로 컨트롤·키 on/off · E2E a·b |
| A-26a / 사용자 D12 — 새 줄 견적 칸 0 잠금 | DECISION | Plan 04-30, Task 2 ② 기본값 0 + `isNewLine` 셀 단계 · E2E b |
| A-03 (클라이언트) — 순서 미전송 | CRITICAL | Plan 04-30, Task 2 ③: 새 줄·옮긴 줄만 `sortOrder` · E2E f |
| A-12 — 1차 렌더 조건 | WARNING | Plan 04-30, Task 1 ②: `canSave` 편집 항을 셀 단계·구조로 |
| C-07 — 힌트 줄은 되는 키만 | WARNING | Plan 04-30, Task 2 ①: `visibleHintKeys`(04-28 배열 거르기) |
| A-18 — E2E 날짜 | WARNING | Plan 04-30: `kstToday` + `addDays` |
| OV-7 / 사용자 D21 | HIGH | 04-12에서 화면 쪽을 떼어 서버 판정(04-12)과 분리 — 견적 표 파일은 04-28 뒤 순서만 지킨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| UI-SPEC 문구 갱신 | 사용자 D16 「이 세 곳만 승인」 — UI-SPEC 편집 대신 SUMMARY 「문구 차이」 표로 남긴다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-30-SUMMARY.md` when done
</output>
