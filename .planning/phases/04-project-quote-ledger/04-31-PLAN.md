---
phase: 04-project-quote-ledger
plan: 31
type: execute
wave: 24
depends_on: ["04-18", "04-19"]
files_modified:
  - test/e2e/excel-paste-final.spec.ts
  - .github/workflows/deploy.yml
  - test/unit/deploy/workflows.test.ts
  - ui/table/parse-tsv.ts
  - ui/table/use-clipboard-paste.ts
  - test/unit/ui/parse-tsv.test.ts
  - test/unit/ui/use-clipboard-paste.test.ts
  - ui/table/Table.tsx
  - test/e2e/quote-table.spec.ts
  - app/(app)/projects/project-form.tsx
  - lib/shortcut.ts
  - test/unit/lib/shortcut.test.ts
  - test/e2e/project-register.spec.ts
autonomous: false
requirements: [UX-05, UX-04, PROJ-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "실제 엑셀에서 복사한 영역(줄바꿈 칸 · 큰따옴표 칸 포함)을 **마지막 모양의** 견적 표(쪽 나눔 · 새 줄 고정 · 계산 열 무시가 들어간 04-19 이후)에 붙여넣으면 화면 값이 원본과 같고, 저장 뒤 다시 열어도 같다 — 04-04의 사람 확인을 최종 표에서 다시 통과한다(사용자 D21, CEO OV-7)"
    - "Windows 엑셀이 복사 영역 끝에 붙이는 줄바꿈이 빈 새 줄을 만들지 않는다 — 45줄을 복사하면 요약이 `45줄`이고 오류 칸이 없으며, 줄 수 상한(300)까지 남은 수와 정확히 같은 줄 수를 붙여넣어도 거부되지 않는다(CEO C-05)"
    - "다른 프로젝트 견적을 `Ctrl+A → Ctrl+C → Ctrl+V`로 옮기면(앱 전용 클립보드 형식이 실린 붙여넣기) 계산 열(번호·견적가·차익·상태)은 무시되고 합계 행 요약에 건너뛴 칸 수가 보이며, 외화 줄이 원화로 들어가면 그 사실이 경고로 보이고, 오류 칸 0 · 원화 합계가 원본과 같다(사용자 D15, CEO C-03)"
    - "실제 엑셀에서 6열(`소분류·항목·거래처·수량·단가·실행가`)을 복사해 소분류 칸에 붙이면 실행가 값이 떨어지는 견적가 칸이 오류 칸이다 — 앱 형식이 없는 붙여넣기에서 계산 열 자리의 값이 조용히 사라지지 않는다(ENG-D5 — 엔지 리뷰 R4)"
    - "이 플랜의 모든 필수 확인(자동 E2E · 사람 확인 일곱 항목 · 전체 게이트)이 통과해야 끝난다 — 어긋난 항목을 「후속 작업」으로 넘기고 통과로 적는 길이 없다. 고칠 파일이 앞 플랜의 산출물이어도 이 플랜이 고치고 회귀 테스트를 붙인다(Codex #5 · ENG-D11)"
    - "배포 워크플로의 스테이징·프로덕션 잡은 main ref에서만 돈다 — 다른 브랜치에서 workflow_dispatch로 띄우면 클라우드 인증 전에 실패한다(ENG-D12 — 엔지 리뷰 R11, Phase 4 한 PR 배포 규칙 강제)"
    - "견적 표에서 `Ctrl+C`로 복사한 범위를 엑셀에 붙이면 열이 어긋나지 않는다(CEO C-19 — 네이티브 복사 이벤트)"
    - "등록 폼에서 Microsoft 한국어 입력기로 조합 중 `Ctrl+Enter`를 누르거나 길게 눌러도 프로젝트가 한 건만 생긴다(CEO C-06 — 자동 테스트로 재현되지 않는 입력기 동작)"
  prohibitions:
    - statement: "브라우저 클립보드 주입 E2E가 초록이라는 사실로 실제 엑셀 붙여넣기의 통과를 대신해서는 안 된다. 주입은 엑셀이 클립보드에 쓰는 형식을 흉내 낼 뿐이고(04-RESEARCH 가정 A3·Gaps 3), 04-04 확인 뒤 표가 쪽 나눔·계산 열 무시·새 줄 고정으로 바뀌었다 — 최종 표에서 사람이 한 번 더 본다"
      status: resolved
      verification: judgment
    - statement: "사람 확인에서 어긋난 항목(복사·입력기 포함)을 파일 소유를 이유로 후속 작업으로 돌리고 이 플랜을 끝내서는 안 된다 — 최종 재확인(사용자 D21)이 알려진 실패를 안고 배포되는 통로가 된다(Codex #5)"
      status: resolved
      verification: judgment
    - statement: "main이 아닌 ref의 코드가 스테이징·프로덕션에 배포돼서는 안 된다 — 한 PR 배포 규칙(C-26)을 워크플로가 강제한다(ENG-D12)"
      status: resolved
      verification: test
  artifacts:
    - path: "test/e2e/excel-paste-final.spec.ts"
      provides: "엑셀 모양 클립보드(CRLF · 끝 줄바꿈 · 인용 줄바꿈 · 두 겹 따옴표 · 통화 기호 · 쉼표) 최종 표 회귀 + 엑셀 6열 계산 열 자리 오류 + 프로젝트 간 왕복 + 상한 경계"
      min_lines: 60
    - path: ".github/workflows/deploy.yml"
      provides: "스테이징·프로덕션 잡 첫 스텝의 main 전용 가드(ENG-D12)"
      contains: "ref-guard"
    - path: "test/unit/deploy/workflows.test.ts"
      provides: "두 잡의 ref-guard 존재 · 인증 전 위치 · env 전달 단언"
      contains: "ref-guard"
  key_links:
    - from: "test/e2e/excel-paste-final.spec.ts"
      to: "ui/table/parse-tsv.ts"
      via: "엑셀 모양 문자열이 최종 표의 붙여넣기 경로(`use-clipboard-paste` → `parseTsv`)를 끝까지 지난다"
      pattern: "Control\\+V|ControlOrMeta"
    - from: "test/unit/deploy/workflows.test.ts"
      to: ".github/workflows/deploy.yml"
      via: "잡 블록을 잘라 ref-guard가 id: auth보다 앞이고 env REF로 refs/heads/main을 비교하는지 본다"
      pattern: "ref-guard"
---

<objective>
**As a** 엑셀에서 견적을 옮겨 오는 기획 PM, **I want to** 견적 표가 쪽 나눔·계산 열 무시·새 줄 고정으로 모양이 바뀐 **뒤에도** 실제 엑셀 붙여넣기와 프로젝트 간 복사가 그대로 맞기를, **so that** 04-04에서 사람이 확인한 붙여넣기가 뒤 플랜들의 변경으로 조용히 깨진 채 배포되지 않는다.

**2026-09-23 CEO 리뷰로 새로 생긴 플랜(사용자 D21 「분리 + 마지막에 재확인」, OV-7).** 견적 표와 무관한 플랜들을 04-04의 사람 확인에서 떼어 먼저 돌리는 대신, 표를 마지막으로 바꾸는 04-19와 `ui/table`을 마지막으로 고치는 04-18 뒤에 실제 엑셀 확인을 한 번 더 둔다. 04-04 Task 3의 다섯 항목을 최종 표에서 다시 보고, 리뷰가 「실제 클립보드 형식은 사람 확인에서」라고 남긴 C-05(끝 줄바꿈) · C-03(프로젝트 간 복사, 사용자 D15) · C-19(표 → 엑셀 복사) · C-06(한글 입력기 연타)을 더한다.

Purpose: 자동 E2E는 클립보드 주입이라 엑셀이 실제로 쓰는 형식을 증명하지 못한다(04-RESEARCH 가정 A3 · Gaps 3). 이 플랜이 페이즈의 마지막 사람 확인이다.

Output: 엑셀 모양 최종 회귀 E2E 1 · 배포 워크플로 main 전용 가드와 그 단위 단언 · 사람 확인 한 번 · (어긋남이 나올 때만) 그 자리의 수정과 회귀 케이스.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`):** (a) **Codex #5(P1):** 옛 계획은 복사·입력기 어긋남을 「범위 밖 파일」이라며 `/gsd-quick` 후속으로 돌리고 확인을 끝낼 수 있었다 — 최종 재확인(사용자 D21)을 무력화한다. 이제 모든 필수 확인이 통과해야 끝나고, 고칠 자리(`Table.tsx` 복사 · `project-form.tsx`·`lib/shortcut.ts` 입력기)를 `files_modified`에 올려 이 플랜이 고친다(ENG-D11 — 회귀 테스트 + SUMMARY 편차). (b) **ENG-D12(R11 — Codex #6):** `deploy.yml`의 스테이징·프로덕션 잡이 main이 아닌 ref를 거부한다(workflow_dispatch 포함) — Task 2. (c) **ENG-D5(R4):** 엑셀 6열 사례를 Task 1 (a2)와 사람 확인 항목 7에 더한다(계산 열 무시는 앱 복사일 때만 — 04-19). (d) 공급망 검사 문구를 의존성 객체 비교로.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개. 사람 확인은 자동 승인할 수 없다(`workflow.auto_advance` 무시). 확인용 프로젝트 준비에서 차수를 고객 승인 상태로 둘 일이 생기면 그룹 B 04-14의 `setCustomerApproval`에 `approvalBasis(viewer, revisionId, tx)`(또는 `listRevisionSummaries`)로 읽은 `{ approvedOn, seenTotalKrw, contentToken }`을 넘긴다 — 승인 행을 SQL로 직접 만들지 않는다(그룹 B 계약).

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-19-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-04-PLAN.md — Task 3(`checkpoint:human-verify`) 전문: 사람 확인 다섯 항목의 원형
# .planning/phases/04-project-quote-ledger/04-VALIDATION.md § Manual-Only Verifications
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1727–1790행(C-03 · C-04 · C-05 · C-06) · 1929–1935행(C-19)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 없다 — UX-05·UX-04·PROJ-05의 붙여넣기 부분은 04-04·04-19가 분류·구현했고, 이 플랜은 그 결과를 실제 엑셀로 다시 본다.

**계획 단계 가정(표면화):**
- **어긋남이 없으면 붙여넣기·복사·입력기 코드 변경이 없다.** `files_modified`의 `ui/table/parse-tsv.ts`·`ui/table/use-clipboard-paste.ts`·`test/unit/ui/parse-tsv.test.ts`·`test/unit/ui/use-clipboard-paste.test.ts`(붙여넣기 형식 — 항목 1·2·3·7) · `ui/table/Table.tsx`·`test/e2e/quote-table.spec.ts`(표 → 엑셀 복사 — 항목 4 뒷부분) · `app/(app)/projects/project-form.tsx`·`lib/shortcut.ts`·`test/unit/lib/shortcut.test.ts`·`test/e2e/project-register.spec.ts`(입력기 — 항목 6)는 사람 확인에서 어긋남이 나왔을 때 고칠 자리다. 어긋남이 없었으면 SUMMARY에 「변경 없음」을 적는다. `deploy.yml`·`workflows.test.ts`는 Task 2가 늘 고친다.
- **어긋남은 여기서 고친다(Codex #5 · ENG-D11):** 어느 항목이든 어긋나면 이 플랜이 그 자리를 고치고, 관찰한 원본(엑셀 문자열 · 입력기 keydown의 `key`·`isComposing`·`keyCode`·`repeat` 모양)을 회귀 케이스로 더한 뒤 사람 확인을 다시 요청한다 — 전 항목이 「같다」가 될 때까지. 후속 작업(`/gsd-quick` 등)으로 돌리는 경로가 없다. 입력기 동작은 자동으로 재현되지 않으므로 회귀 케이스는 관찰한 keydown 모양을 `isCtrlCombo` 단위 표(`test/unit/lib/shortcut.test.ts`)에 싣는 것이다.
- **CI 브라우저:** E2E는 Chromium 하나다. 사람 확인은 사용자가 실제로 쓰는 Windows + Edge/Chrome + 회사 엑셀로 한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 엑셀 모양 클립보드가 최종 표를 끝까지 지난다(CRLF · 끝 줄바꿈 · 인용 · 쪽 경계 · 프로젝트 간 왕복 · 상한 경계)</name>
  <files>test/e2e/excel-paste-final.spec.ts</files>
  <read_first>
    - `test/e2e/quote-table.spec.ts`(04-04·04-19 산출물) — 클립보드 주입 도우미·줄 대량 준비·페이지 이동 단언 형태
    - `ui/table/parse-tsv.ts`(전문) · `test/unit/ui/parse-tsv.test.ts` — 04-19가 더한 끝 줄바꿈 처리(C-05)와 인용 규칙
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `계산 열`·`외화`·`붙여넣기` 요약 문구를 찾아 범위만 Read(04-19의 D15 문구 그대로 단언한다)
    - `test/e2e/fixtures.ts` · `playwright.config.ts`(`CI=true`에서만 프로덕션 빌드)
  </read_first>
  <action>
① `test/e2e/excel-paste-final.spec.ts`(신규)에 엑셀 모양 문자열을 상수로 둔다 — 줄 끝 `\r\n`, 마지막 줄 뒤에도 `\r\n`, 한 칸에 인용된 줄바꿈(`"첫 줄\r\n둘째 줄"`), 한 칸에 두 겹 따옴표(`"12""x"""`), 빈 칸(연속 탭), 금액 `₩1,200,000`·`1,234`(04-04 숫자 정규화 규칙 — 쉼표·통화 기호 제거 — 그대로 숫자가 되어야 한다).

② 케이스(기획 PM, 날짜 준비는 `kstToday(new Date())` + `addDays`):
- (a) 10줄 표 첫 셀에 45줄 엑셀 모양 문자열 → 45줄이 들어가고 **46번째 빈 줄이 없으며** 오류 칸 0, 인용 칸이 한 칸 안의 두 줄, 두 겹 따옴표가 따옴표 하나 — 저장 · 새로 고침 뒤 같은 값(C-05, 04-04 사람 확인 1–4의 자동판).
- (a2) **엑셀 6열(ENG-D5):** 앱 전용 형식 없이 `text/plain`만 실은 `소분류⇥항목⇥거래처⇥수량⇥단가⇥실행가` 두 줄(끝 `\r\n`)을 소분류 칸에 → 견적가 칸 둘이 오류 셀(`aria-invalid`)이고 합계 행에 `오류 2칸`, 요약에 `계산 열` 조각이 없으며 저장이 거부 경로를 탄다(04-19 규칙 — 실행가 값이 조용히 사라지지 않는다).
- (b) 60줄 표 29번째 줄에서 5줄 붙여넣기 → 29~33번째가 바뀌고 화면은 1쪽, 요약에 `2쪽까지`(04-19 문구) — 쪽 경계에서 멈추지 않는다.
- (c) 프로젝트 A(두 소분류 그룹 · USD 줄 1개 · 45줄)에서 `Control+A` → `Control+C`(네이티브 복사 이벤트 — 04-19 C-19) → 프로젝트 B(0줄)에서 첫 줄 만들기 → `Control+V` → 오류 칸 0, 요약에 계산 열 무시 칸 수와 외화 경고(04-19의 D15 문구), B의 원화 합계 = A의 원화 합계(C-03, 사용자 D15).
- (d) 상한 경계: 줄 수 상한(설정 기본 300)에서 남은 수가 정확히 N인 표에 끝 줄바꿈이 붙은 N줄 → 전부 들어간다(거부되지 않는다) · N+1줄 → 04-26의 전부 거부(C-05).

③ `CI=true`로 이 스펙과 붙여넣기 관련 기존 스펙을 한 번에 돌린다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/excel-paste-final.spec.ts test/e2e/quote-table.spec.ts test/e2e/number-format.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 최종 표에서 엑셀 모양 붙여넣기·프로젝트 간 왕복·상한 경계 중 하나가 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - 스펙이 (a)·(a2)·(b)·(c)·(d) 다섯 경우를 단언하고 (a)가 「46번째 줄 없음」과 저장 뒤 재조회 값을 둘 다 본다 · (a2)가 앱 형식 없는 6열의 견적가 자리 오류 칸과 `계산 열` 조각 없음을 본다(ENG-D5)
    - (c)가 오류 칸 0 · 계산 열 무시 수 · 외화 경고 · 합계 같음 넷을 모두 단언한다
    - 스펙에 날짜 리터럴이 없다 — `grep -nE "'20[0-9]{2}-[0-9]{2}-[0-9]{2}'" test/e2e/excel-paste-final.spec.ts` 0건
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>엑셀 모양 클립보드가 쪽 나눔·계산 열 무시·상한까지 들어간 최종 표를 끝까지 지나고, 엑셀 6열의 계산 열 자리 값이 오류 칸이 되는 것이 CI=true E2E로 증명됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 배포는 main에서만 — deploy.yml의 스테이징·프로덕션 잡이 main이 아닌 ref를 인증 전에 거부한다(ENG-D12)</name>
  <files>.github/workflows/deploy.yml, test/unit/deploy/workflows.test.ts</files>
  <read_first>
    - `.github/workflows/deploy.yml`(전문 115행 — 머리 주석 · `on:`의 push `branches: [main]`과 `workflow_dispatch` 입력 · 잡 `ci`·`staging`·`production` · production `guard` 스텝이 입력을 env로 넘기는 방식)
    - `test/unit/deploy/workflows.test.ts` 1–130행(YAML 파서 없는 텍스트 메타 검사 — 잡 블록을 `indexOf("\n  staging:")`·`indexOf("\n  production:")`로 잘라 본다 · T-1-32 env 전달 단언)
    - `docs/designs/plant8-erp-phase4-eng-review-260923.md` R11(ENG-D12 — Codex #6)
  </read_first>
  <behavior>
    - `staging` 블록과 `production` 블록 각각에 `id: ref-guard` 스텝이 정확히 하나 있고, 블록 안에서 그 위치가 `id: auth`보다 앞이다(클라우드 자격 증명을 받기 전에 멈춘다)
    - 두 ref-guard 스텝 조각(다음 `- ` 스텝 시작까지)이 env로 `REF`에 `github.ref`를 받고, 본문이 `"$REF"`를 `refs/heads/main`과 비교해 다르면 `exit 1`한다 — ref 값은 env 줄로만 들어온다(T-1-32 셸 인젝션 규약)
    - 기존 단언(push `branches: [main]` · paths 재포함 순서 · production `if:` 줄 · guard 스텝 · deploy.sh 인자)이 그대로 초록
  </behavior>
  <action>
① **테스트 먼저(RED).** `test/unit/deploy/workflows.test.ts`의 `describe("deploy.yml")`에 `behavior` 앞 두 줄을 `it` 둘로 더한다 — 기존 방식대로 잡 블록을 문자열로 잘라, ref-guard의 문자열 위치가 `id: auth`보다 앞인지와 ref-guard 조각에 env `REF` 줄 · `refs/heads/main` · `exit 1`이 있는지, 조각의 `run:` 이후 본문이 ref를 `$REF`로만 읽는지(표현식 보간 `${{`가 `run:` 이후에 없음)를 본다. 지금 파일로 두 `it`이 실패하는 것을 확인한다. 기존 `it`은 지우거나 완화하지 않는다.

② **`deploy.yml`(GREEN — ENG-D12).** `staging`·`production` 두 잡의 첫 스텝(`actions/checkout` 앞)으로 `- id: ref-guard` · `shell: bash` · env `REF`(값 `github.ref` 표현식) · `run:` 블록을 둔다 — `REF`가 `refs/heads/main`이 아니면 `::error::` 한 줄(「main이 아닌 ref는 배포하지 않습니다(ENG-D12) — main에서 다시 실행하세요」와 받은 ref)을 찍고 `exit 1`. push 트리거는 이미 `branches: [main]`이라 이 가드가 실제로 막는 것은 다른 브랜치에서 띄운 workflow_dispatch다(Phase 4 한 PR 배포 규칙 C-26 — main에 머지된 코드만 배포된다). 잡의 `if:` 조건은 바꾸지 않는다 — 조건으로 거르면 실패가 아니라 「skipped」로 보여 거부가 눈에 띄지 않는다. 머리 주석에 한 줄을 더한다: 「스테이징·프로덕션 잡은 main ref에서만 돈다 — 다른 브랜치의 workflow_dispatch는 ref-guard 스텝에서 인증 전에 실패한다(ENG-D12)」. `ci` 잡·`on:` 트리거·다른 스텝은 건드리지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/workflows.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 스테이징·프로덕션 잡 중 하나에 main 전용 가드가 없거나 인증 스텝보다 뒤에 있거나, ref가 `run:` 본문에 직접 보간됐거나, 기존 배포 워크플로 단언이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `deploy.yml`의 `staging`·`production` 블록에 `id: ref-guard`가 각각 한 번 있고 `id: auth`보다 앞이다(단위 단언 둘)
    - ref-guard가 env `REF`로 ref를 받고 `refs/heads/main`이 아니면 `exit 1`한다 — `run:` 본문은 `$REF`만 읽는다(단위 단언)
    - `workflows.test.ts`의 기존 `it` 개수가 줄지 않았고 전부 초록이다
    - `deploy.yml`의 바뀐 줄이 머리 주석 한 줄과 두 ref-guard 스텝뿐이다(`on:` 블록 · `ci` 잡 · 기존 스텝 무변경 — diff 검토로 확인해 SUMMARY에 적는다)
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>main이 아닌 브랜치에서 workflow_dispatch로 배포를 띄우면 스테이징·프로덕션 잡이 클라우드 인증 전에 실패하고, 이 규칙이 단위 테스트로 고정됐다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실제 엑셀·입력기로 최종 표를 사람이 확인(04-04 다섯 항목 + C-05 · C-03 · C-19 · C-06 · ENG-D5) — 전 항목 통과까지</name>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md` — 04-04 사람 확인의 답(어긋났던 항목과 고친 케이스)
    - `.planning/phases/04-project-quote-ledger/04-19-SUMMARY.md` — 계산 열 무시·외화 경고 문구, 복사 이벤트 확인 결과
    - `ui/table/parse-tsv.ts` · `ui/table/use-clipboard-paste.ts` — 붙여넣기 형식 어긋남(1·2·3·7)이 나오면 고칠 자리
    - `ui/table/Table.tsx`의 `onCopy` 처리기(04-19 — Grep `onCopy`) — 표 → 엑셀 복사(4 뒷부분) 어긋남의 자리
    - `app/(app)/projects/project-form.tsx`의 keydown 처리기 · `lib/shortcut.ts`(04-08 — `isCtrlCombo`) — 입력기(6) 어긋남의 자리
  </read_first>
  <action>
**Claude가 먼저 하는 것:** Task 1이 초록인지 확인하고, 사용자가 확인에 쓸 프로젝트 둘(엑셀 붙여넣기용 빈 프로젝트 · USD 줄이 있는 45줄 프로젝트)을 로컬 dev DB에 준비해 번호를 알려 준다. 앱은 `CI=true` 프로덕션 빌드로 띄운다.

**사람이 확인하는 것(자동화로 대체 불가):** 아래 `<how-to-verify>` 일곱 항목.

**어긋난 것이 있으면 이 플랜이 고친다(Codex #5 · ENG-D11 — 후속 작업으로 넘기지 않는다):** 붙여넣기 형식(1·2·3·7)은 `parse-tsv.ts` 또는 `use-clipboard-paste.ts`를 고치고 그 원본 문자열을 `test/unit/ui/parse-tsv.test.ts` 또는 `test/unit/ui/use-clipboard-paste.test.ts`에 케이스로 더한다. 표 → 엑셀 복사(4 뒷부분)는 `Table.tsx`의 복사 처리기(TSV 조립 · 열 순서 · 줄 끝)를 고치고 `quote-table.spec.ts`의 복사 캡처 케이스에 그 모양을 단언으로 더한다. 입력기(6)는 관찰한 keydown 모양을 `test/unit/lib/shortcut.test.ts`의 `isCtrlCombo` 표에 더하고 `lib/shortcut.ts` 또는 `project-form.tsx`의 가드를 고치며, `project-register.spec.ts`에 같은 모양의 합성 keydown 케이스를 더한다. 고친 뒤 Task 1·2의 검증과 아래 검증을 다시 돌리고 사람 확인을 **다시 요청**한다 — 전 항목이 「같다」가 될 때까지 반복한다. 앞 플랜 파일을 고쳤으면 SUMMARY 편차(파일 · 원인 · 테스트 이름)로 적는다.
  </action>
  <what-built>견적 표가 04-04 뒤에 쪽 나눔(04-19) · 계산 열 무시(앱에서 복사한 붙여넣기만 — 엑셀은 오류 칸, ENG-D5)와 외화 경고(04-19, 사용자 D15) · 끝 줄바꿈 처리(04-19, C-05) · 네이티브 복사 이벤트(04-19, C-19) · 새 줄 고정 · 쉼표 입력(04-09) · 정산 편집 범위(04-12·04-30) · 줄 수 상한(04-26)으로 바뀌었다. 등록 폼에는 Ctrl+Enter 연타 가드(04-08, C-06)가 들어갔다. 엑셀 모양 문자열의 자동 회귀는 Task 1이 초록이다. 자동화가 닿지 못하는 것은 **실제 엑셀이 클립보드에 쓰는 형식**과 **Windows 한국어 입력기의 keydown**이다.</what-built>
  <how-to-verify>
Windows + Edge 또는 Chrome + 회사 엑셀로, Claude가 알려 준 두 프로젝트에서 확인한다.

1. 엑셀에서 **3×3 영역**(한 칸에 **줄바꿈**, 다른 한 칸에 **큰따옴표**)을 복사해 빈 프로젝트 견적 표의 활성 셀에 붙여넣는다 → 화면 값이 원본과 같은가(줄바꿈 칸이 한 칸, 따옴표가 사라지거나 둘이 되지 않음). 저장 뒤 다시 열어도 같은가
2. 숫자 열에 쉼표 금액(`1,200,000`)을 붙여넣으면 숫자로 읽히는가
3. 엑셀에서 **45줄 × 여러 열**을 복사해 붙여넣는다 → 요약이 `45줄`이고 맨 끝에 빈 줄·오류 칸이 없는가(C-05)
4. 45줄 프로젝트(USD 줄 포함)에서 `Ctrl+A` → `Ctrl+C` → 빈 프로젝트에서 첫 줄 만들기 → `Ctrl+V` → 오류 칸이 없고, 계산 열을 건너뛴 칸 수와 외화 경고가 보이며, 두 프로젝트의 합계가 같은가(C-03, 사용자 D15). 같은 복사를 엑셀 빈 시트에 붙여 열이 어긋나지 않는가(C-19)
5. 붙여넣기가 2쪽까지 넘어가면 합계 행 오른쪽에 `… · 2쪽까지`가 보이고 화면은 시작 쪽에 머무는가
6. 프로젝트 등록 폼에서 한국어 입력기로 프로젝트명을 **조합 중인 상태**로 `Ctrl+Enter`를 누르고, 다른 이름으로 `Ctrl+Enter`를 **길게** 눌러 본다 → 각각 프로젝트가 **한 건**만 생기는가(C-06)
7. 엑셀에서 **6열**(소분류 · 항목 · 거래처 · 수량 · 단가 · 실행가) 두 줄을 복사해 빈 프로젝트 견적 표의 **소분류** 칸에 붙여넣는다 → 실행가 값이 떨어진 견적가 칸이 오류 칸으로 보이고 합계 행에 `오류 2칸`이 뜨는가 — 값이 오류 표시 없이 사라지지 않는가(ENG-D5)

어긋난 것이 있으면 항목 번호와 「원본 → 화면」 값 쌍을 적어 준다. Claude가 고친 뒤 다시 확인을 요청한다.
  </how-to-verify>
  <resume-signal>일곱 항목이 전부 같으면 「같다」라고 답한다. 어긋난 것이 있으면 항목 번호와 「원본 → 화면」 값 쌍을 적는다 — 고친 뒤 다시 확인한다</resume-signal>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/excel-paste-final.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 사람 확인 뒤 고친 파서가 최종 표의 회귀를 깨뜨렸다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/parse-tsv.test.ts test/unit/ui/use-clipboard-paste.test.ts test/unit/lib/shortcut.test.ts test/unit/deploy/workflows.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 사람 확인에서 나온 원본 문자열·keydown 모양 케이스가 초록이 아니거나 main 전용 가드 단언이 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>종료 코드가 0이 아니거나 단위·통합·E2E 어느 계층이든 failed 개수가 1 이상이다 — 페이즈 마지막 확인에서 고친 자리가 다른 화면을 깨뜨렸다(전체 게이트 한 번)</fails_when>
    <human-check>실제 엑셀·입력기로 일곱 항목을 확인해 전부 「같다」다 — 어긋났던 항목은 이 플랜에서 고쳐진 뒤 다시 「같다」를 받았다</human-check>
  </verify>
  <acceptance_criteria>
    - 사람 확인 일곱 항목의 **최종** 답이 전부 「같다」다 — 어긋난 채 남은 항목 0개, 후속 작업으로 넘긴 항목 0개(Codex #5)
    - 어긋남이 있었던 항목마다 고친 파일 · 더한 회귀 케이스(원본 문자열 또는 keydown 모양) · 다시 받은 「같다」가 SUMMARY에 있고, 앞 플랜 파일을 고쳤으면 편차로 적혀 있다(ENG-D11)
    - 항목 7(엑셀 6열)의 답이 SUMMARY에 있다(ENG-D5)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>최종 견적 표에서 실제 엑셀 붙여넣기(6열 포함)·프로젝트 간 복사·표 → 엑셀 복사·입력기 연타가 사람 확인으로 전부 통과했고, 어긋났던 것은 이 플랜에서 고쳐 회귀 케이스로 고정됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시스템 클립보드(엑셀) → 견적 표 | 엑셀이 쓰는 형식을 앱이 해석한다. 해석이 틀리면 값이 조용히 바뀐 채 저장될 수 있다 |
| GitHub Actions 실행 ref → 클라우드 배포 | workflow_dispatch는 어느 브랜치에서든 띄울 수 있다 — 리뷰·머지되지 않은 코드가 스테이징·프로덕션 자격 증명을 받는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-165 | Tampering | 실제 엑셀 클립보드 해석 | medium | mitigate | 최종 표에서 사람 확인(blocking)을 한 번 더 하고, 어긋난 원본 문자열을 단위 케이스로 고정한다. 해석 결과는 여전히 서버의 전부 저장/전부 거부(04-04)와 셀 검증을 지난다 |
| T-04-317 | Elevation of Privilege | `deploy.yml` 스테이징·프로덕션 잡 | high | mitigate | (ENG-D12 — Codex #6) 두 잡의 첫 스텝 ref-guard가 `refs/heads/main`이 아닌 ref에서 WIF 인증 전에 `exit 1`한다. ref는 env로만 받는다(셸 인젝션 규약 T-1-32). 단위 테스트가 두 잡의 가드 존재 · 인증 전 위치 · env 전달을 단언한다 |
| T-04-318 | Repudiation | 최종 사람 확인의 통과 기록 | medium | mitigate | (Codex #5) 어긋난 항목을 후속으로 넘기고 통과로 적는 경로를 없앴다 — 일곱 항목의 최종 답이 전부 「같다」이고 고친 자리마다 회귀 케이스와 다시 받은 답이 SUMMARY에 남는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- `CI=true` 붙여넣기 E2E 초록 · `workflows.test.ts`(main 전용 가드) 초록
- 사람 확인 일곱 항목의 최종 답이 전부 「같다」로 SUMMARY에 있다(후속으로 넘긴 항목 0)
- `CI=true pnpm test` 세 계층 통과
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
</verification>

<success_criteria>
- 04-04 뒤에 바뀐 최종 견적 표에서 실제 엑셀 붙여넣기가 사람 확인으로 다시 통과했다
- 프로젝트 간 복사가 오류 칸 없이 합계를 보존하고, 끝 줄바꿈이 빈 줄을 만들지 않으며, 엑셀 6열의 계산 열 자리 값이 오류 칸이 된다
- 모든 필수 확인이 통과했다 — 알려진 실패를 안고 끝나지 않는다
- main이 아닌 ref에서 배포가 시작되지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 테스트(신규) | `test/e2e/excel-paste-final.spec.ts` |
| 워크플로(변경) | `.github/workflows/deploy.yml` — 스테이징·프로덕션 잡 첫 스텝 `ref-guard`(main 전용, ENG-D12) · 테스트 `test/unit/deploy/workflows.test.ts`(확장) |
| 사람 확인 기록 | SUMMARY의 일곱 항목 최종 답(전부 「같다」) · 어긋났던 항목의 원본 → 화면 값 쌍과 고친 자리 |
| 조건부 수정 | 붙여넣기 `ui/table/parse-tsv.ts`·`ui/table/use-clipboard-paste.ts`(+ 두 단위 테스트) · 복사 `ui/table/Table.tsx`(+ `quote-table.spec.ts`) · 입력기 `app/(app)/projects/project-form.tsx`·`lib/shortcut.ts`(+ `shortcut.test.ts`·`project-register.spec.ts`) — 어긋남이 있을 때만 |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| OV-7 / 사용자 D21 — 최종 표 변경 뒤 실제 엑셀 확인 없음 | HIGH | Plan 04-31(신규): 04-18·04-19 뒤 `checkpoint:human-verify` blocking(웨이브 24 — 페이즈 마지막) |
| C-05 — 실제 클립보드 형식은 사람 확인에서 | WARNING | Plan 04-31, Task 1 (a)(d) · Task 2 항목 3 |
| C-03 / 사용자 D15 — 프로젝트 간 복사 | WARNING | Plan 04-31, Task 1 (c) · Task 2 항목 4 (구현은 04-19) |
| C-19 — 복사 이벤트의 실제 동작 | NOTE | Plan 04-31, Task 2 항목 4 뒷부분 (구현은 04-19) |
| C-06 — Windows 한국어 입력기 keydown | WARNING | Plan 04-31, Task 2 항목 6 (가드는 04-08) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Codex #5 (P1) — 최종 확인이 복사·입력기 실패를 후속 작업으로 돌리고 통과 | DECISION | Plan 04-31, truth · 금지 항목 · `<probe_fallback>` · Task 3 action(여기서 고치고 다시 확인 — `/gsd-quick` 경로 삭제) · 고칠 자리를 `files_modified`에 · 수용 기준 「최종 답 전부 같다 · 후속 0」 · 전체 게이트 `CI=true pnpm test` · T-04-318 |
| ENG-D12 (R11 — Codex #6) — 한 PR 배포 규칙 강제 | DECISION | Plan 04-31, Task 2(신규): `deploy.yml` 스테이징·프로덕션 잡 ref-guard(인증 전 · env `REF`) + `workflows.test.ts` 단언 · T-04-317 |
| ENG-D5 (R4) — 엑셀 6열 사례 | DECISION | Plan 04-31, Task 1 (a2) E2E · Task 3 항목 7 · truth (구현은 04-19) |
| ENG-D11 — 앞 플랜 결함은 고치고 회귀 테스트 + 편차 | DECISION | objective 「앞 플랜 결함」 문단 · Task 3 action |
| 엔지 리뷰 C 04-03 [P3] — 공급망 검사가 `package.json` 원문 비교 | NOTE | T-04-SC · `<verification>` 문구 |
| 교차 그룹 — 승인 준비는 `approvalBasis` | NOTE | objective 실행 규율 문장 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-31-SUMMARY.md` when done
</output>
