---
phase: 04-project-quote-ledger
plan: 31
type: execute
wave: 30
depends_on: ["04-18", "04-47", "04-42", "04-51"]
files_modified:

  - test/e2e/excel-paste-final.spec.ts
  - test/fixtures/excel-clipboard.ts
  - ui/table/parse-tsv.ts
  - ui/table/use-clipboard-paste.ts
  - test/unit/ui/parse-tsv.test.ts
  - test/unit/ui/use-clipboard-paste.test.ts
  - ui/table/Table.tsx
  - test/e2e/quote-table.spec.ts
  - app/(app)/projects/project-form.tsx
  - lib/shortcut.ts
  - test/unit/lib/shortcut.test.ts
  - test/e2e/project-register.spec.ts

autonomous: false
requirements: [UX-05, UX-04, PROJ-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "실제 Windows 엑셀에서 **캡처한 클립보드 원문**(04-04의 2026-09-23 3×3 캡처 + 이 플랜이 받는 6열·45줄 캡처 — 줄바꿈 칸은 인용, 따옴표만 있는 칸은 원문 그대로, 금액의 공백·쉼표·`₩`)을 **마지막 모양의** 견적 표(쪽 나눔 · 새 줄 고정 · 계산 열 무시 · 합계 행 한 줄이 들어간 04-19·04-47 이후)에 재생하면 화면 값이 원본과 같고 저장 뒤 다시 열어도 같다 — 04-04의 확인을 최종 표에서 **캡처 원문 재생(단위 · E2E)으로** 다시 통과한다(사용자 D21 · CEO OV-7 · 2026-09-23 사용자 승인 — 04-04 Task 3 종결과 같은 방식: 사람의 눈 재확인을 실제 캡처 바이트 재생으로 대체)"
    - "Windows 엑셀이 복사 영역 끝에 붙이는 줄바꿈이 빈 새 줄을 만들지 않는다 — 45줄을 복사하면 요약이 `45줄`이고 오류 칸이 없으며, 줄 수 상한(300)까지 남은 수와 정확히 같은 줄 수를 붙여넣어도 거부되지 않는다(CEO C-05)"
    - "다른 프로젝트 견적을 `Ctrl+A → Ctrl+C → Ctrl+V`로 옮기면(앱 전용 클립보드 형식이 실린 붙여넣기) 계산 열(번호·견적가·차익·상태)은 무시되고 합계 행 요약에 건너뛴 칸 수가 보이며, 외화 줄이 원화로 들어가면 그 사실이 경고로 보이고, 오류 칸 0 · 원화 합계가 원본과 같다(사용자 D15, CEO C-03)"
    - "실제 엑셀에서 6열(`소분류·항목·거래처·수량·단가·실행가`)을 복사해 소분류 칸에 붙이면 실행가 값이 떨어지는 견적가 칸이 오류 칸이다 — 앱 형식이 없는 붙여넣기에서 계산 열 자리의 값이 조용히 사라지지 않는다(ENG-D5 — 엔지 리뷰 R4)"
    - "이 플랜의 모든 필수 확인(캡처 원문 재생 단위·E2E · 사람 확인 PC 항목 · 독립 DOM 감사 · `/design-review` · 전체 게이트)이 통과해야 끝난다 — 어긋난 항목을 「후속 작업」으로 넘기고 통과로 적는 길이 없다. 고칠 파일이 앞 플랜의 산출물이어도 이 플랜이 고치고 회귀 테스트를 붙인다(Codex #5 · ENG-D11)"
    - "페이즈의 화면 전체(목록 · 등록 폼 · 상세 견적 원장 · 매출 · 차수 · 코드표 · 리저브 대장)가 **`/design-review` 한 번**으로 `SYSTEM.md` §11 「시스템 일치」 목록과 대조된다 — 새 색·서체·radius·그림자 0 · 카드 0 · 포인트 색(`--accent`) 다섯 곳 밖 사용 0 · 이유 없는 비활성 버튼 0 · 한 화면 1차 버튼 하나 · 관리자 화면(`/admin/*`) 밖 안내 문구 0(P0) · 폰 P1 3열 이하. 발견은 이 플랜이 고치고 SUMMARY에 항목별 결과가 남는다(DR-19, UI-SPEC rev 5 S11 「페이즈 검증 순서」)"
    - statement: "`/design-review` 앞에 독립 DOM 감사가 페이즈 화면 전체를 1280·1024·375 세 폭에서 실측한다 — 문서 가로 스크롤 0 · 1024에서 편집 표가 편집 가능하고 700~1023(1000)에서 보기 전용(셀 편집·줄 추가·힌트 줄 없음) · 폰 첫 화면(목록·상세) · 확인 모달 PC/시트(교차 그룹 계약 6, DR-14 · DR-36)"
      verification: backstop
    - "견적 표에서 `Ctrl+C`로 복사한 범위를 엑셀에 붙이면 열이 어긋나지 않는다(CEO C-19 — 네이티브 복사 이벤트)"
    - "등록 폼에서 Microsoft 한국어 입력기로 조합 중 `Ctrl+Enter`를 누르거나 길게 눌러도 프로젝트가 한 건만 생기고, 조합 중 `Esc`가 입력을 버리지 않는다(CEO C-06 · DR-27 — 자동 테스트로 재현되지 않는 입력기 동작, PC 사람 확인)"
    - "페이즈 최종 게이트: 새 DB(`scripts/reset-test-db.sh`)에서 `CI=true` 전체 E2E를 **연속 3회** 돌려 세 번 모두 초록일 때만 통과다 — `retries`는 0 그대로이고, 첫 실패의 trace(`--trace=retain-on-failure`, 회차별 출력 폴더)를 보존하며, 원인이 설명되지 않는 실패는 `systematic-debugging`으로 원인을 찾아 고친 뒤 3회를 처음부터 다시 돈다 — 한 번 초록은 증거가 아니다(엔지 r2 E2-06 · CEO r2 지시 5 정정 · deferred-items.md의 전체 스위트 간헐 실패)"
    - "마이그레이션이 0011(04-10) → 0012(04-06) → 0013(04-27) → 0014(04-13) → 0015(04-41) → 0016(04-07) 순서로 journal `idx`·파일명·snapshot이 이어지고 빈 번호가 없으며, 0015 가드(업무 행의 `contract_amount_krw <> 0` **또는** `contract_foreign_amount IS NOT NULL`이면 RAISE — 04-41)와 0012 재매핑 가드의 옛 데이터 업그레이드 테스트가 초록이다 · `-- rollback-floor:` 표시는 0012·0015에 있고(04-50 규약 — 0016은 04-07의 결정 ① 답이 (b)일 때만) 0011·0013·0014에는 없다(엔지 r2 지시 8 · E2-03 · E2-04 · E2-05)"
  prohibitions:
    - statement: "**손으로 만든** 엑셀 모양 문자열의 주입 E2E가 초록이라는 사실로 실제 엑셀 붙여넣기의 통과를 대신해서는 안 된다 — 흉내 낸 형식은 04-04에서 실제 엑셀과 달랐다(따옴표만 있는 칸). 실제 엑셀에서 캡처한 원문을 최종 표에 재생해야 하고(사용자 승인 2026-09-23 — 04-04 방식), 캡처가 없는 형식(6열 · 45줄)은 이 플랜이 사용자에게 캡처를 받아 재생한다"
      status: resolved
      verification: judgment
    - statement: "사람 확인에서 어긋난 항목(복사·입력기 포함)을 파일 소유를 이유로 후속 작업으로 돌리고 이 플랜을 끝내서는 안 된다 — 최종 재확인(사용자 D21)이 알려진 실패를 안고 배포되는 통로가 된다(Codex #5)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/e2e/excel-paste-final.spec.ts"
      provides: "실제 엑셀 캡처 원문(3×3 · 6열 · 45줄) 재생 최종 표 회귀 + 끝 줄바꿈 방어 + 엑셀 6열 계산 열 자리 오류 + 프로젝트 간 왕복 + 상한 경계"
      min_lines: 60
    - path: "test/fixtures/excel-clipboard.ts"
      provides: "실제 Windows 엑셀 클립보드 `text/plain` 캡처 원문 상수(04-04의 2026-09-23 3×3 캡처를 옮기고 6열·45줄 캡처를 더한다) — 단위(`parse-tsv.test.ts`)와 E2E가 같은 바이트를 재생한다"
      contains: "REAL_EXCEL_WINDOWS_20260923"
  key_links:
    - from: "test/e2e/excel-paste-final.spec.ts"
      to: "ui/table/parse-tsv.ts"
      via: "실제 엑셀 캡처 원문과 그 행 모양으로 조립한 문자열이 최종 표의 붙여넣기 경로(`use-clipboard-paste` → `parseTsv`)를 끝까지 지난다"
      pattern: "Control\\+V|ControlOrMeta"
    - from: "test/unit/ui/parse-tsv.test.ts"
      to: "test/fixtures/excel-clipboard.ts"
      via: "단위와 E2E가 같은 캡처 바이트를 import해 재생한다 — 원문이 한 곳에만 있다(04-04 방식, 사용자 승인 2026-09-23)"
      pattern: "REAL_EXCEL_WINDOWS_20260923"
---

<objective>
**As a** 엑셀에서 견적을 옮겨 오는 기획 PM, **I want to** 견적 표가 쪽 나눔·계산 열 무시·새 줄 고정으로 모양이 바뀐 **뒤에도** 실제 엑셀 붙여넣기와 프로젝트 간 복사가 그대로 맞기를, **so that** 04-04에서 사람이 확인한 붙여넣기가 뒤 플랜들의 변경으로 조용히 깨진 채 배포되지 않는다.

머지 묶음: ④/4 목록·리저브 — 웨이브 25~30(04-17 → 04-31)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

이 플랜이 묶음 ④이자 Phase 4의 마지막이다 — 이 플랜 실행이 끝나면 묶음 ④ PR이 Post-build(/review → /qa(+ UI가 바뀌었으므로 /design-review) → /cso → /ship)를 거쳐 main에 머지된다. Post-build는 워크플로 단계이고 이 플랜의 실행자 태스크가 아니다. 그 뒤 페이즈 종료 절차(/gsd-verify-work 등)는 HANDOFF의 남은 단계를 따른다.

**2026-09-23 CEO 리뷰로 새로 생긴 플랜(사용자 D21 「분리 + 마지막에 재확인」, OV-7).** 견적 표와 무관한 플랜들을 04-04의 사람 확인에서 떼어 먼저 돌리는 대신, 표를 마지막으로 바꾸는 04-19·04-47(04-19 분할)과 `ui/table`을 마지막으로 고치는 04-18 뒤에 실제 엑셀 확인을 한 번 더 둔다. 04-04 Task 3의 다섯 항목을 최종 표에서 다시 보고, 리뷰가 「실제 클립보드 형식은 사람 확인에서」라고 남긴 C-05(끝 줄바꿈) · C-03(프로젝트 간 복사, 사용자 D15) · C-19(표 → 엑셀 복사) · C-06(한글 입력기 연타)을 더한다.

Purpose: 자동 E2E는 클립보드 주입이라 엑셀이 실제로 쓰는 형식을 증명하지 못한다(04-RESEARCH 가정 A3 · Gaps 3). 이 플랜이 페이즈의 마지막 사람 확인이다.

Output: 실제 엑셀 캡처 원문 fixture 모듈 · 캡처 재생 최종 회귀 E2E 1 · 페이즈 최종 독립 DOM 감사(1280·1024·375)와 `/design-review` 1회 · 사람 확인 한 번(캡처 둘 + PC 전용 두 항목) · (어긋남이 나올 때만) 그 자리의 수정과 회귀 케이스.

**2026-09-23 디자인 리뷰 반영(`docs/designs/plant8-erp-phase4-design-review-260923.md`, UI-SPEC rev 5 — 재계획 그룹 C):** (a) **DR-19:** 페이즈에서 `/design-review`를 부르는 곳이 없었다 — Task 3 앞부분에 한 번(§11 「시스템 일치」 대조)을 둔다. (b) **교차 그룹 계약 6 · DR-14:** 그 앞 독립 DOM 감사 폭에 1024를 더한다(1280·1024·375 + 1000). (c) **04-04 Task 3 종결 방식 재사용(사용자 승인 2026-09-23):** 04-04는 실제 Windows 엑셀 클립보드 원문(3×3)을 캡처해 단위·E2E로 재생하는 것으로 사람의 눈 재확인을 대체했다(`test/unit/ui/parse-tsv.test.ts` — 커밋 `2d01155`·`eba9223`·`0af4a8b`). 이 플랜도 엑셀 → 앱 붙여넣기 항목(옛 1·2·3·5·7)을 **캡처 원문 재생**으로 판정하고 — 아직 캡처가 없는 6열·45줄 원문은 사람에게 받는다 — PC에서만 되는 항목(프로젝트 간 복사와 표 → 엑셀 · 한국어 입력기)만 사람 확인으로 남긴다. 캡처로 드러난 실제 규칙(따옴표만 있는 칸은 인용되지 않는다)에 맞춰 손으로 만든 문자열을 캡처 원문으로 바꾼다. (d) **웨이브 26(04-42 뒤):** `/design-review`가 페이즈 화면 **전체**를 봐야 하므로(리저브 대장 S9 포함) 마지막 UI 플랜 04-42 뒤로 옮겼다 — 이 플랜에 기대는 플랜이 없어 다른 웨이브는 밀리지 않는다.

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md`):** (a) **Codex #5(P1):** 옛 계획은 복사·입력기 어긋남을 「범위 밖 파일」이라며 `/gsd-quick` 후속으로 돌리고 확인을 끝낼 수 있었다 — 최종 재확인(사용자 D21)을 무력화한다. 이제 모든 필수 확인이 통과해야 끝나고, 고칠 자리(`Table.tsx` 복사 · `project-form.tsx`·`lib/shortcut.ts` 입력기)를 `files_modified`에 올려 이 플랜이 고친다(ENG-D11 — 회귀 테스트 + SUMMARY 편차). (b) **ENG-D12(R11 — Codex #6):** `deploy.yml`의 스테이징·프로덕션 잡이 main이 아닌 ref를 거부한다(workflow_dispatch 포함) — 2026-09-24 엔지 r2 재계획으로 **04-50**(묶음 ② 첫 웨이브)으로 옮겼다(아래). (c) **ENG-D5(R4):** 엑셀 6열 사례를 Task 1 (a2)와 사람 확인 항목 7에 더한다(계산 열 무시는 앱 복사일 때만 — 04-47). (d) 공급망 검사 문구를 의존성 객체 비교로.

**2026-09-24 엔지니어링 리뷰 r2 반영(`docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 8 · 9 — E2-03 · E2-04 · E2-06 · 머지 묶음 결정).** (a) **이 플랜이 실제 최종 게이트다(E2-03 — CEO r2 지시 5의 「04-47」「0014」는 04-31·0015의 오기):** 새 **Task 3** — `CI=true` 전체 E2E를 새 DB로 **연속 3회**, 첫 실패 trace 보존, 설명되지 않는 실패는 `systematic-debugging` 조사 뒤에만 통과(E2-06, `retries` 0 유지) + 마이그레이션 순서 0011→0016 · 0015 가드(외화 조건 포함 — E2-05) · `rollback-floor` 표시 검증. 옛 Task 3(DOM 감사 · `/design-review` · 사람 확인)은 **Task 2**가 되고 그 끝의 전체 게이트 한 번은 Task 3의 3회 판정이 대신한다(전체 게이트를 두 번 돌리지 않는다). (b) **옛 Task 2(`deploy.yml` main 전용 가드 — ENG-D12)는 04-50으로 옮겼다:** 머지 묶음 결정으로 배포가 묶음마다 세 번이 되어, 「main에 머지된 묶음만 배포된다」는 가드가 첫 묶음(②) 배포 **전에** 있어야 한다(E2-04 롤백 하한 가드와 같은 이유 — 하한 판정이 main 이력을 전제한다). 그 truths·금지 항목·T-04-317·`workflows.test.ts` 단언이 함께 갔다. (c) 이 플랜은 페이즈 마지막 플랜 그대로다(웨이브 30 — 04-49 삽입으로 +1).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전(커밋 전 포함) `verification-before-completion`을 Skill로 호출한다. 테스트·빌드·린트가 실패하면 코드를 고치기 전에 `systematic-debugging`부터 부른다. 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번 — 사람 확인(Task 2) 뒤 이어가기도 새 세션이다. 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다(`/cso`는 별도 worktree). 완료 판정은 로컬 dev가 아니라 `CI=true`다. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개. 사람 확인은 자동 승인할 수 없다(`workflow.auto_advance` 무시). 확인용 프로젝트 준비에서 차수를 고객 승인 상태로 둘 일이 생기면 그룹 B 04-14의 `setCustomerApproval`에 `approvalBasis(viewer, revisionId, tx)`(또는 `listRevisionSummaries`)로 읽은 `{ approvedOn, seenTotalKrw, contentToken }`을 넘긴다 — 승인 행을 SQL로 직접 만들지 않는다(그룹 B 계약).

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-19-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-47-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다(U-2는 `## UI Considerations` S5 partial 529행의 사용자 확정값). 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-04-PLAN.md — Task 3(`checkpoint:human-verify`) 전문: 사람 확인 다섯 항목의 원형

# .planning/phases/04-project-quote-ledger/04-VALIDATION.md § Manual-Only Verifications

# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1727–1790행(C-03 · C-04 · C-05 · C-06) · 1929–1935행(C-19)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 없다 — UX-05·UX-04·PROJ-05의 붙여넣기 부분은 04-04·04-19·04-47이 분류·구현했고, 이 플랜은 그 결과를 실제 엑셀로 다시 본다.

**계획 단계 가정(표면화):**

- **어긋남이 없으면 붙여넣기·복사·입력기 코드 변경이 없다.** `files_modified`의 `ui/table/parse-tsv.ts`·`ui/table/use-clipboard-paste.ts`·`test/unit/ui/use-clipboard-paste.test.ts`(캡처 재생에서 어긋난 붙여넣기 형식) · `ui/table/Table.tsx`·`test/e2e/quote-table.spec.ts`(표 → 엑셀 복사 — (C) 뒷부분) · `app/(app)/projects/project-form.tsx`·`lib/shortcut.ts`·`test/unit/lib/shortcut.test.ts`·`test/e2e/project-register.spec.ts`(입력기 — (D))는 어긋남이 나왔을 때 고칠 자리다. 어긋남이 없었으면 SUMMARY에 「변경 없음」을 적는다. `test/fixtures/excel-clipboard.ts`·`test/unit/ui/parse-tsv.test.ts`(캡처 원문과 그 단언)·`excel-paste-final.spec.ts`·`deploy.yml`·`workflows.test.ts`는 늘 고친다. DOM 감사·`/design-review`의 발견 수정은 그 자리의 앞 플랜 파일을 ENG-D11 규칙(회귀 테스트 + SUMMARY 편차)으로 고친다 — 미리 목록을 정할 수 없는 파일이다.
- **캡처 원문을 받는 방법(04-04와 같은 결):** 엑셀에서 복사한 뒤 같은 PC 브라우저 개발자 도구 콘솔에서 `JSON.stringify(await navigator.clipboard.readText())` 결과를 받는다 — 이스케이프된 문자열이라 탭·CR·LF·따옴표가 보이고, fixture에는 그 바이트를 고치지 않고 옮긴다. 캡처가 사람 확인 뒤에 도착하므로 6열·45줄 재생 케이스는 Task 3에서 더한다(Task 1은 이미 있는 3×3 캡처로 최종 표를 먼저 증명한다).
- **어긋남은 여기서 고친다(Codex #5 · ENG-D11):** 어느 항목이든 어긋나면 이 플랜이 그 자리를 고치고, 관찰한 원본(엑셀 문자열 · 입력기 keydown의 `key`·`isComposing`·`keyCode`·`repeat` 모양)을 회귀 케이스로 더한 뒤 사람 확인을 다시 요청한다 — 전 항목이 「같다」가 될 때까지. 후속 작업(`/gsd-quick` 등)으로 돌리는 경로가 없다. 입력기 동작은 자동으로 재현되지 않으므로 회귀 케이스는 관찰한 keydown 모양을 `isCtrlCombo` 단위 표(`test/unit/lib/shortcut.test.ts`)에 싣는 것이다.
- **CI 브라우저:** E2E는 Chromium 하나다. 사람 확인은 사용자가 실제로 쓰는 Windows + Edge/Chrome + 회사 엑셀로 한다.

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 엑셀 모양 클립보드가 최종 표를 끝까지 지난다(CRLF · 끝 줄바꿈 · 인용 · 쪽 경계 · 프로젝트 간 왕복 · 상한 경계)</name>
  <files>test/fixtures/excel-clipboard.ts, test/unit/ui/parse-tsv.test.ts, test/e2e/excel-paste-final.spec.ts</files>
  <read_first>
    - `test/e2e/quote-table.spec.ts`(04-04·04-19·04-47 산출물) — 클립보드 주입 도우미(`pasteIntoFocusedCell`)·줄 대량 준비·페이지 이동 단언 형태, (g) 「실제 엑셀(Windows, 2026-09-23 캡처)」 케이스(186행 근처 — 열 매핑: 항목 열과 수량·단가 열이 거래처 칸을 사이에 두고 나뉜다)
    - `ui/table/parse-tsv.ts`(전문) · `test/unit/ui/parse-tsv.test.ts` — 04-04의 캡처 상수 `REAL_EXCEL_WINDOWS_20260923`(따옴표만 있는 칸은 원문 그대로 · 줄바꿈 칸만 인용 · 인용 칸 안 CRLF → LF · 마지막 줄 뒤 줄바꿈 없음)와 04-47이 더한 끝 줄바꿈 처리(C-05)
    - `.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md` 170–195행(사람 확인 — 캡처 원문 재생으로 대체한 경위)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `계산 열`·`외화`·`붙여넣기` 요약 문구를 찾아 범위만 Read(04-47의 D15 문구 그대로 단언한다)
    - `test/e2e/fixtures.ts` · `playwright.config.ts`(`CI=true`에서만 프로덕션 빌드)
  </read_first>
  <action>
① **캡처 원문 fixture(04-04 방식 재사용).** `test/fixtures/excel-clipboard.ts`(신규)로 04-04의 캡처 상수 `REAL_EXCEL_WINDOWS_20260923`을 **바이트 그대로** 옮겨 내보내고, `test/unit/ui/parse-tsv.test.ts`는 그 모듈을 import하도록 바꾼다(단언은 그대로 — 원문이 한 곳에만 있게). `test/e2e/excel-paste-final.spec.ts`(신규)는 같은 모듈을 import한다. 45줄 케이스의 문자열은 **캡처의 행 모양**(줄 끝 `\r\n` · 따옴표만 있는 칸 원문 그대로 · 줄바꿈 칸만 인용 · 금액의 앞뒤 공백·쉼표·`₩` · 마지막 줄 뒤 줄바꿈 없음)을 45번 이어 만든 조립 문자열이고, 끝 줄바꿈이 있는 변형(C-05 방어 — 캡처에는 없었다)을 하나 더 둔다. 손으로 지어낸 두 겹 따옴표 칸처럼 캡처와 다른 규칙의 문자열은 쓰지 않는다 — 실제 6열·45줄 캡처는 Task 3에서 받아 같은 모듈에 더한다.

② 케이스(기획 PM, 날짜 준비는 `kstToday(new Date())` + `addDays`):

- (a) 3×3 캡처(`REAL_EXCEL_WINDOWS_20260923` — 헤더 행·번호 열을 뺀 칸을 04-04 (g)와 같은 열 매핑으로)를 **쪽 나눔이 있는 최종 표**(31줄 이상 프로젝트의 2쪽)에 재생 → 따옴표만 있는 칸이 따옴표를 지니고, 줄바꿈 칸이 한 칸 안의 두 줄, 금액이 숫자 — 저장 · 새로 고침 뒤 같은 값 · 10줄 표 첫 셀에 45줄 조립 문자열(두 변형) → 45줄이 들어가고 **46번째 빈 줄이 없으며** 오류 칸 0(C-05).
- (a2) **엑셀 6열(ENG-D5):** 앱 전용 형식 없이 `text/plain`만 실은 `소분류⇥항목⇥거래처⇥수량⇥단가⇥실행가` 두 줄(끝 `\r\n`)을 소분류 칸에 → 견적가 칸 둘이 오류 셀(`aria-invalid`)이고 합계 행에 `오류 2칸`, 요약에 `계산 열` 조각이 없으며 저장이 거부 경로를 탄다(04-47 규칙 — 실행가 값이 조용히 사라지지 않는다).
- (b) 60줄 표 29번째 줄에서 5줄 붙여넣기 → 29~33번째가 바뀌고 화면은 1쪽, 요약에 `2쪽까지`(04-47 문구) — 쪽 경계에서 멈추지 않는다.
- (c) 프로젝트 A(두 소분류 그룹 · USD 줄 1개 · 45줄)에서 `Control+A` → `Control+C`(네이티브 복사 이벤트 — 04-19 C-19) → 프로젝트 B(0줄)에서 첫 줄 만들기 → `Control+V` → 오류 칸 0, 요약에 계산 열 무시 칸 수와 외화 경고(04-47의 D15 문구), B의 원화 합계 = A의 원화 합계(C-03, 사용자 D15).
- (d) 상한 경계: 줄 수 상한(설정 기본 300)에서 남은 수가 정확히 N인 표에 끝 줄바꿈이 붙은 N줄 → 전부 들어간다(거부되지 않는다) · N+1줄 → 04-26의 전부 거부(C-05).

③ `CI=true`로 이 스펙과 붙여넣기 관련 기존 스펙을 한 번에 돌린다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project unit test/unit/ui/parse-tsv.test.ts</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하거나 `parse-tsv` 단위 failed 개수가 1 이상이다 — 캡처 상수를 fixture 모듈로 옮기며 바이트가 바뀌었거나 import가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/excel-paste-final.spec.ts test/e2e/quote-table.spec.ts test/e2e/number-format.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 최종 표에서 엑셀 모양 붙여넣기·프로젝트 간 왕복·상한 경계 중 하나가 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - 원문 캡처가 `test/fixtures/excel-clipboard.ts` 한 곳에만 있고(`parse-tsv.test.ts`와 스펙이 import — Grep `REAL_EXCEL_WINDOWS_20260923` 정의 1), 스펙이 (a)·(a2)·(b)·(c)·(d) 다섯 경우를 단언하며 (a)가 캡처 재생의 따옴표·줄바꿈·금액과 「46번째 줄 없음」, 저장 뒤 재조회 값을 본다 · (a2)가 앱 형식 없는 6열의 견적가 자리 오류 칸과 `계산 열` 조각 없음을 본다(ENG-D5)
    - (c)가 오류 칸 0 · 계산 열 무시 수 · 외화 경고 · 합계 같음 넷을 모두 단언한다
    - 스펙에 날짜 리터럴이 없다 — `grep -nE "'20[0-9]{2}-[0-9]{2}-[0-9]{2}'" test/e2e/excel-paste-final.spec.ts` 0건
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>실제 엑셀 캡처 원문이 쪽 나눔·계산 열 무시·상한까지 들어간 최종 표를 끝까지 지나고, 엑셀 6열의 계산 열 자리 값이 오류 칸이 되는 것이 CI=true E2E로 증명됐다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 페이즈 최종 화면 검증(독립 DOM 감사 1280·1024·375 → `/design-review` 1회 — DR-19) + 실제 엑셀 캡처 원문 받기·재생 + PC에서만 되는 사람 확인(프로젝트 간 복사·표 → 엑셀 · 한국어 입력기) — 전 항목 통과까지</name>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-04-SUMMARY.md` 170–195행(「사람 확인(Task 3) — 결과와 처리」 — 사용자 승인: 사람 재확인을 실제 엑셀 원문 재생(단위·E2E)으로 대체한 방식과 캡처 원문)
    - `.planning/phases/04-project-quote-ledger/04-47-SUMMARY.md` — 계산 열 무시·외화 경고 문구, 합계 행 한 줄 · `04-19-SUMMARY.md` — 복사 이벤트 확인 결과
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 S11 「페이즈 검증 순서(CLAUDE.md · DR-19 · DR-14)」(1408–1411행) · 「SYSTEM.md 개정」 ⑯(958–968행 — P0) · Component Inventory 「신규 다섯」 문단(151–154행)
    - `docs/design/SYSTEM.md` §11 「검증 — 모든 화면이 통과해야 하는 것」(Grep `## 11`) — `/design-review`에 넘길 대조 목록
    - `test/fixtures/excel-clipboard.ts`(Task 1) · `ui/table/parse-tsv.ts` · `ui/table/use-clipboard-paste.ts` — 캡처 재생에서 어긋남이 나오면 고칠 자리
    - `ui/table/Table.tsx`의 `onCopy` 처리기(04-19 — Grep `onCopy`) — 표 → 엑셀 복사 어긋남의 자리
    - `app/(app)/projects/project-form.tsx`의 keydown 처리기 · `lib/shortcut.ts`(04-08 — `isCtrlCombo`) — 입력기 어긋남의 자리
  </read_first>
  <action>
**Claude가 먼저 하는 것(사람 확인 전 — 화면 검증 순서, CLAUDE.md · UI-SPEC rev 5 S11):**
① Task 1이 초록인지 확인한다.
② **독립 DOM 감사(페이즈 최종)** — 실행자가 아닌 별도 서브에이전트(`model` 명시), `CI=true` 프로덕션 빌드에서 DOM 실측(스크린샷 육안 판정 금지), **1280 · 1024 · 375 세 폭**(+ 열 접기 경계 1000 — 교차 그룹 계약 6): 목록(합계 줄 · 열 접기 · 폰 첫 화면 — 검색·요약·1차가 첫 화면, 필터 접힘) · 등록 폼(입력 버리기 확인 PC 모달/폰 시트) · 상세 견적 원장(1024에서 편집 가능 · 1000과 375에서 보기 전용 — 셀 편집·줄 추가·힌트 줄 없음 · 1차는 dirty 0이면 없고 N ≥ 1이면 렌더(표 셀 dirty·「복원」 칸 포함 — UI-SPEC rev 5 후속 결정 R1 47행) · 힌트 줄이 견적 표 페이지 줄 바로 아래 한 곳 · 합계 행 오른쪽 한 줄) · 매출(폰 P1 날짜·금액) · 차수 섹션과 이전 차수 읽기 섹션 · 코드표(설명 칸 · 글자 수) · 리저브 대장(04-42) — 항목마다 문서 가로 스크롤 0과 폭별 실측값. 발견은 이 플랜이 고친다(ENG-D11 — 앞 플랜 파일이면 회귀 테스트 + SUMMARY 편차).
③ **`/design-review` 한 번(DR-19)** — Skill `design-review`를 `CI=true` 프로덕션 빌드에 대해 부르고 대조 목록을 `SYSTEM.md` §11 「시스템 일치」로 준다: 새 색·서체·radius·그림자 0(`git diff d6b41cf -- docs/design/tokens.css` 0줄) · 카드 0 · 포인트 색(`--accent`) 사용이 §1-3 다섯 곳 밖 0 · 이유 없는 비활성 버튼 0(비활성은 전부 이유 글자 + `aria-disabled` — 04-46) · 한 화면 1차 버튼 하나(등록 폼이 열리면 목록 1차 없음 포함) · 관리자 화면(`/admin/*`) 밖 안내 문구 0(P0 — 「…할 수 있습니다」·「값은 남아 있습니다」·「…할 수 없음 —」 접두 없음) · 폰 P1 3열 이하 · ⌘ 표기 0 · 숫자 쉼표(식별자형 제외). 결과를 항목별 「통과 / 고친 파일」로 SUMMARY에 적는다. 발견은 ②와 같이 고친다.
④ ②③에서 고친 것은 Task 3의 최종 게이트(`CI=true` 단위·통합 한 번 + 전체 E2E 새 DB 연속 3회)가 판정한다 — 여기서 전체 게이트를 따로 돌리지 않는다(CLAUDE.md — 전체 게이트를 두 번 돌리지 않는다).
⑤ 사용자가 확인에 쓸 프로젝트 둘(엑셀 붙여넣기용 빈 프로젝트 · USD 줄이 있는 45줄 프로젝트)을 로컬 dev DB에 준비해 번호를 알려 주고, 앱은 `CI=true` 프로덕션 빌드로 띄운다.

**사람에게 받는 것(자동화로 대체 불가 — `<how-to-verify>`):** (A)(B) 실제 엑셀 클립보드 **원문 캡처 둘** · (C) 프로젝트 간 복사와 표 → 엑셀 복사 · (D) 한국어 입력기. 04-04 Task 3과 같은 방식으로 **엑셀 → 앱 붙여넣기의 눈 재확인은 하지 않는다** — 캡처 원문을 재생하는 단위·E2E가 그 확인을 대신한다(사용자 승인 2026-09-23).

**캡처를 받은 뒤 Claude가 하는 것:** (A)(B) 원문을 `test/fixtures/excel-clipboard.ts`에 그대로(이스케이프만 — 바이트를 고치지 않는다) 상수로 더하고, `test/unit/ui/parse-tsv.test.ts`에 파싱 결과 단언(칸 수 · 인용 칸 · 따옴표만 있는 칸 · 금액)을, `test/e2e/excel-paste-final.spec.ts`에 최종 표 재생 케이스를 더한다 — (A) 6열 두 줄을 소분류 칸에 → 실행가 값이 떨어진 견적가 칸 둘이 오류 셀이고 합계 행에 `오류 2칸`(앱 형식 없음 — ENG-D5), (B) 45줄 → 46번째 빈 줄 없음 · 오류 칸 0 · 저장 뒤 같은 값 · 끝에 줄바꿈이 있는지 없는지 캡처 그대로(C-05). 재생이 어긋나면 `parse-tsv.ts`·`use-clipboard-paste.ts`를 고치고 그 원문을 회귀 케이스로 남긴 뒤 다시 돌린다 — 캡처 재생은 사람의 재확인 없이 자동 단언이 판정한다.

**어긋난 것이 있으면 이 플랜이 고친다(Codex #5 · ENG-D11 — 후속 작업으로 넘기지 않는다):** 표 → 엑셀 복사(C)는 `Table.tsx`의 복사 처리기(TSV 조립 · 열 순서 · 줄 끝)를 고치고 `quote-table.spec.ts`의 복사 캡처 케이스에 그 모양을 단언으로 더한다. 입력기(D)는 관찰한 keydown 모양(`key` · `isComposing` · `keyCode` · `repeat`)을 `test/unit/lib/shortcut.test.ts`의 `isCtrlCombo` 표에 더하고 `lib/shortcut.ts` 또는 `project-form.tsx`의 가드를 고치며, `project-register.spec.ts`에 같은 모양의 합성 keydown 케이스를 더한다. (C)(D)를 고쳤으면 검증을 다시 돌리고 그 항목의 사람 확인을 **다시 요청**한다 — 전 항목이 「같다」가 될 때까지. 앞 플랜 파일을 고쳤으면 SUMMARY 편차(파일 · 원인 · 테스트 이름)로 적는다.
  </action>
  <what-built>견적 표가 04-04 뒤에 쪽 나눔(04-19) · 계산 열 무시(앱에서 복사한 붙여넣기만 — 엑셀은 오류 칸, ENG-D5)와 외화 경고(04-47, 사용자 D15) · 합계 행 한 줄(DR-16 — 04-47) · 끝 줄바꿈 처리(04-47, C-05) · 네이티브 복사 이벤트(04-19, C-19) · 새 줄 고정 · 쉼표 입력(04-09) · 정산 편집 범위(04-12·04-30) · 줄 수 상한(04-26) · 충돌 셀(04-28)로 바뀌었다. 등록 폼에는 Ctrl+Enter 연타 가드(04-08, C-06)와 Esc 입력 버리기 확인(04-46, DR-27)이 들어갔다. 04-04의 실제 엑셀 3×3 캡처 원문은 Task 1이 최종 표에서 재생해 초록이다. 페이즈 화면 전체의 DOM 감사와 `/design-review`는 이 태스크 앞부분에서 끝났다. 자동화가 닿지 못하는 것은 **아직 캡처가 없는 엑셀 형식(6열 · 45줄)의 원문**, **실제 Edge/Chrome의 앱 전용 클립보드 형식 왕복과 엑셀로 붙여넣기**, **Windows 한국어 입력기의 keydown**이다.</what-built>
  <how-to-verify>
Windows + Edge 또는 Chrome + 회사 엑셀로, Claude가 알려 준 두 프로젝트에서 한다.

(A) **원문 캡처 — 6열 두 줄.** 엑셀에서 소분류 · 항목 · 거래처 · 수량 · 단가 · 실행가 여섯 열 두 줄(한 칸에 줄바꿈, 다른 한 칸에 큰따옴표, 금액에 쉼표)을 복사한 뒤, 같은 PC 브라우저의 개발자 도구 콘솔에서 `copy(JSON.stringify(await navigator.clipboard.readText()))`를 실행하고(클립보드 권한 허용) 그 결과 문자열을 그대로 붙여 준다 — 04-04의 3×3 캡처와 같은 방식이다.
(B) **원문 캡처 — 45줄.** 엑셀에서 45줄 × 여러 열(빈 칸 하나 포함)을 복사해 (A)와 같은 방법으로 원문 문자열을 붙여 준다.
(C) **프로젝트 간 복사 · 표 → 엑셀.** 45줄 프로젝트(USD 줄 포함)에서 `Ctrl+A` → `Ctrl+C` → 빈 프로젝트에서 첫 줄 만들기 → `Ctrl+V` → 오류 칸이 없고, 합계 행 오른쪽 한 줄에 `붙여넣기 45줄 · 외화 1줄 원화로 · 계산 열 N칸 무시`가 보이며, 두 프로젝트의 합계가 같은가(C-03, 사용자 D15). 같은 복사를 엑셀 빈 시트에 붙여 열이 어긋나지 않는가(C-19)
(D) **한국어 입력기.** 프로젝트 등록 폼에서 프로젝트명을 **조합 중인 상태**로 `Ctrl+Enter`를 누르고, 다른 이름으로 `Ctrl+Enter`를 **길게** 눌러 본다 → 각각 프로젝트가 **한 건**만 생기는가(C-06). 조합 중에 `Esc`를 눌러도 입력이 사라지지 않는가(DR-27)

(C)(D)에서 어긋난 것이 있으면 항목 기호와 「기대 → 화면」 값 쌍을 적어 준다. Claude가 고친 뒤 그 항목을 다시 확인해 달라고 요청한다.
  </how-to-verify>
  <resume-signal>(A)(B) 원문 문자열 두 개를 붙이고, (C)(D)가 전부 같으면 「같다」라고 답한다. 어긋난 것이 있으면 항목 기호와 「기대 → 화면」 값 쌍을 적는다 — 고친 뒤 다시 확인한다</resume-signal>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/parse-tsv.test.ts test/unit/ui/use-clipboard-paste.test.ts test/unit/lib/shortcut.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 받은 캡처 원문(6열 · 45줄)의 파싱 단언이 초록이 아니거나, 입력기 keydown 모양 케이스가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/excel-paste-final.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 캡처 원문 재생(3×3 · 6열 · 45줄)이 최종 표에서 원본과 다르거나, 고친 복사·파서가 최종 표의 회귀를 깨뜨렸다</fails_when>
    <human-check>페이즈 최종 독립 DOM 감사 보고서(1280·1024·375, 화면별 실측값)와 `/design-review` §11 대조 결과(항목별 통과 / 고친 파일)가 SUMMARY에 있다 · 캡처 원문 둘이 fixture에 있고 재생 단언이 초록이다 · (C)(D)의 최종 답이 「같다」다 — 어긋났던 항목은 이 플랜에서 고쳐진 뒤 다시 「같다」를 받았다</human-check>
  </verify>
  <acceptance_criteria>
    - 독립 DOM 감사(1280·1024·375 — 목록 · 등록 폼 · 상세 · 매출 · 차수 · 코드표 · 리저브)와 `/design-review` 1회(§11 대조 — 새 색·서체·radius·그림자 0 · 카드 0 · 포인트 색 다섯 곳 · 이유 없는 비활성 0 · 한 화면 1차 하나 · `/admin/*` 밖 안내 문구 0 · 폰 P1 3열 이하)의 결과가 SUMMARY에 항목별로 있고, 발견은 전부 고쳐졌다(DR-19 · DR-14)
    - `test/fixtures/excel-clipboard.ts`에 04-04 3×3 캡처와 (A)(B) 캡처가 원문 그대로 있고, 단위(`parse-tsv.test.ts`)와 E2E(`excel-paste-final.spec.ts`)가 그 상수를 import해 재생한다 — (A)는 앱 형식 없는 6열의 견적가 자리 오류 칸, (B)는 46번째 줄 없음 · 저장 뒤 같은 값을 단언한다
    - 사람 확인 (C)(D)의 **최종** 답이 전부 「같다」다 — 어긋난 채 남은 항목 0개, 후속 작업으로 넘긴 항목 0개(Codex #5)
    - 어긋남이 있었던 항목마다 고친 파일 · 더한 회귀 케이스(원문 문자열 또는 keydown 모양) · 다시 받은 「같다」가 SUMMARY에 있고, 앞 플랜 파일을 고쳤으면 편차로 적혀 있다(ENG-D11)
    - 두 검증 명령이 전부 초록이다(전체 게이트는 Task 3)
  </acceptance_criteria>
  <done>페이즈 화면 전체가 세 폭 DOM 감사와 `/design-review` 한 번으로 시스템 규칙과 대조됐고, 실제 엑셀 캡처 원문(3×3 · 6열 · 45줄)이 최종 표에서 재생으로 통과했으며, PC에서만 되는 프로젝트 간 복사·표 → 엑셀·입력기 확인이 사람 확인으로 전부 통과했다</done>
</task>

<task type="auto">
  <name>Task 3: 페이즈 최종 게이트 — `CI=true` 전체 E2E 새 DB 연속 3회(trace 보존 · 플레이크 조사) + 마이그레이션 순서 0011→0016 · 0015 가드 · 롤백 하한 표시 검증(엔지 r2 지시 8 — E2-03 · E2-05 · E2-06)</name>
  <files>test/integration/migration-upgrade.test.ts</files>
  <read_first>
    - `playwright.config.ts`(전문 — `retries: 0` · `webServer`가 `CI`에서만 `pnpm build && pnpm start` · 워커 수 · `trace` 설정)
    - `scripts/reset-test-db.sh`(전문 — `erp_test`를 지우고 다시 만든다) · `test/e2e/global-setup.ts`(마이그레이션만 돌리고 truncate하지 않는다)
    - `.planning/phases/04-project-quote-ledger/deferred-items.md`(전문 — 전체 스위트에서만 간헐적으로 실패하던 기록 · `destination stream closed early`)
    - `db/migrations/meta/_journal.json`(현재 전문) · `db/migrations/`의 0011~0016 파일 첫 줄
    - `test/integration/migration-upgrade.test.ts`(04-06 · 04-41 산출물 — 0012 재매핑 가드 · 0015 가드 (c)와 외화 픽스처 케이스)
    - `.planning/phases/04-project-quote-ledger/04-50-SUMMARY.md` · `04-07-SUMMARY.md`(결정 ① 답 — 0016에 `rollback-floor` 표시가 있어야 하는지)
    - `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` E2-03 · E2-05 · E2-06 행
  </read_first>
  <action>
① **마이그레이션 순서 · 가드 · 하한 표시(E2-03 · E2-05 · E2-04).** 아래 검증 명령의 journal 검사로 `idx` 11~16이 `0011_`~`0016_` 접두 파일명과 snapshot을 빈 번호 없이 갖는지 본다 — 계획 번호와 실제 이름이 다르게 생성된 플랜이 있으면(각 플랜 SUMMARY) 그 실제 이름으로 순서만 본다. `grep -l '^-- rollback-floor:' db/migrations/*.sql` 결과가 0012·0015(그리고 04-07 SUMMARY의 결정 ① 답이 (b)면 0016)이고 0011·0013·0014에는 없음을 확인한다. `test/integration/migration-upgrade.test.ts`의 0012 재매핑 가드와 0015 가드 케이스(업무 행 원화 계약 ≠ 0 · **원화 0 + 외화 값만 있는 업무 행**(04-41 E2-05 픽스처) → RAISE, demo 행만 → 통과)가 있는지 확인하고, 빠진 케이스가 있으면 이 파일에 더한다(ENG-D11 — 04-41·04-06 산출물의 공백, SUMMARY 편차).

② **최종 E2E 3회(E2-06 — Skill `verification-before-completion` 먼저).** 아래 검증 명령의 반복문으로 회차마다 `scripts/reset-test-db.sh`로 `erp_test`를 새로 만들고 `CI=true pnpm playwright test --trace=retain-on-failure --output=test-results/final-gate-run-{회차}`를 돌린다 — `retries`를 켜지 않는다. 세 번 모두 초록이면 통과다. 통합 테스트·`next build`를 쓰는 다른 에이전트가 같은 작업 트리·`erp_test`에서 돌고 있지 않을 때만 시작한다.

③ **실패하면(플레이크 조사 — 이 태스크의 핵심).** 첫 실패 회차의 `test-results/final-gate-run-{회차}` trace를 지우지 않고 보존하고(경로를 SUMMARY에 적는다), 코드를 만지기 전에 Skill `systematic-debugging`을 호출해 재현 → 원인 → 수정 → 회귀 테스트 순서로 간다. `deferred-items.md`가 기록한 전체 스위트 간헐 실패(`destination stream closed early` · 다른 워커 데이터와 섞이는 목록 스펙)와 같은 원인인지 먼저 대조한다. 원인이 앞 플랜 파일이면 ENG-D11로 고치고 SUMMARY 편차에 적는다. 고친 뒤에는 **1회차부터** 다시 세 번을 돈다 — 이어 붙여 세지 않는다. 원인을 설명하지 못한 실패는 통과로 적지 않는다(「재실행하니 초록」은 증거가 아니다) — 원인이 이 페이즈 밖(인프라·도구)으로 확인되면 증거(trace · 로그 줄)와 함께 SUMMARY에 적고 사용자 확인을 받는다.

④ `CI=true` 단위·통합을 한 번 돌린다(아래 첫 검증 명령) — E2E 3회와 합쳐 페이즈 전체 게이트다.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,bad=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')||!fs.existsSync('db/migrations/meta/'+p(i)+'_snapshot.json')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,last=e[e.length-1];if(bad.length||n!==e.length||last.idx&lt;16){console.error('MIGRATION ORDER: bad='+bad+' sql='+n+' journal='+e.length+' last='+last.tag);process.exit(1)}console.log('ok '+e.slice(11).map(x=>x.tag).join(' '))"</automated>
    <fails_when>표준 오류에 `MIGRATION ORDER:`로 시작하는 줄이 나오고 종료 코드가 1이다 — 0011~0016 중 빈 번호·순서 어긋남·snapshot 누락이 있거나 journal 마지막 `idx`가 16보다 작다</fails_when>
    <automated>test "$(grep -l '^-- rollback-floor:' db/migrations/0011_*.sql db/migrations/0013_*.sql db/migrations/0014_*.sql 2>/dev/null | wc -l)" -eq 0 &amp;&amp; test "$(grep -l '^-- rollback-floor:' db/migrations/0012_*.sql db/migrations/0015_*.sql | wc -l)" -eq 2</automated>
    <fails_when>종료 코드가 0이 아니다 — 0012·0015 중 `-- rollback-floor:` 표시가 빠졌거나, 확장 전용 마이그레이션(0011·0013·0014)에 하한 표시가 붙었다(04-50 규약)</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; CI=true pnpm vitest run --project unit &amp;&amp; CI=true pnpm vitest run --project integration</automated>
    <fails_when>squawk가 0이 아닌 개수를 출력하거나, 단위·통합 어느 쪽이든 종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 `migration-upgrade.test.ts`의 0012·0015(외화 픽스처 포함) 가드 케이스가 빨갛다</fails_when>
    <automated>for i in 1 2 3; do bash scripts/reset-test-db.sh &amp;&amp; CI=true pnpm playwright test --trace=retain-on-failure --output=test-results/final-gate-run-$i || exit 1; done</automated>
    <fails_when>종료 코드가 0이 아니다 — 세 회차 중 하나라도 playwright failed 개수가 1 이상이다(첫 실패 회차의 trace는 `test-results/final-gate-run-{회차}`에 남는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - journal 검사가 `ok`와 0011~0016 태그 여섯을 출력한다 · `rollback-floor` 표시 검사가 초록이다(0012·0015 있음, 0011·0013·0014 없음 — 0016은 04-07 결정 ① 답과 일치)
    - `migration-upgrade.test.ts`에 0015 가드의 외화 픽스처 케이스(원화 0 + `contract_foreign_amount` 값 → RAISE)와 0012 재매핑 가드 케이스가 있고 초록이다
    - `CI=true` 전체 E2E가 새 DB로 **연속 3회** 초록이다 — SUMMARY에 회차별 결과(통과 수 · 소요 시간)와, 실패가 있었다면 보존한 trace 경로 · `systematic-debugging` 원인 · 고친 파일 · 회귀 테스트 · 다시 1회차부터 돈 세 번의 결과가 있다
    - `playwright.config.ts`의 `retries`가 0 그대로다(`grep -c "retries: 0" playwright.config.ts` = 1 — 이 플랜이 재시도를 켜지 않았다)
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>마이그레이션 0011~0016이 순서대로 적용되고 0015 가드가 외화 값까지 막으며 롤백 하한 표시가 제자리에 있고, 새 DB에서 CI=true 전체 E2E가 연속 세 번 초록이다 — 초록 한 번이 아니라 세 번이 페이즈의 완료 판정이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시스템 클립보드(엑셀) → 견적 표 | 엑셀이 쓰는 형식을 앱이 해석한다. 해석이 틀리면 값이 조용히 바뀐 채 저장될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-165 | Tampering | 실제 엑셀 클립보드 해석 | medium | mitigate | 최종 표에서 사람 확인(blocking)을 한 번 더 하고, 어긋난 원본 문자열을 단위 케이스로 고정한다. 해석 결과는 여전히 서버의 전부 저장/전부 거부(04-04)와 셀 검증을 지난다 |
| T-04-318 | Repudiation | 최종 사람 확인의 통과 기록 | medium | mitigate | (Codex #5) 어긋난 항목을 후속으로 넘기고 통과로 적는 경로를 없앴다 — PC 항목(C)(D)의 최종 답이 전부 「같다」이고, 엑셀 붙여넣기 항목은 캡처 원문 재생 단언(자동, 사용자 승인)이 판정하며, 고친 자리마다 회귀 케이스와 다시 받은 답이 SUMMARY에 남는다 |
| T-04-369 | Tampering | 캡처 원문 fixture | low | mitigate | fixture는 사용자가 준 원문을 이스케이프만 해 옮기고 바이트를 고치지 않는다 — 단위 테스트가 원문 그대로의 파싱 결과를 단언하므로 원문을 「맞춰서」 고치면 04-04 캡처 단언이 깨진다. 캡처에는 업무 데이터가 아니라 확인용 예시 값만 담게 사용자에게 요청한다(`<how-to-verify>`의 예시 범위) |
| T-04-373 | Repudiation | 페이즈 최종 판정의 거짓 초록(엔지 r2 E2-06) | medium | mitigate | `retries: 0`인 채 초록 한 번은 약한 증거다 — 새 DB `CI=true` 전체 E2E 연속 3회만 통과로 치고, 첫 실패 trace를 보존하며, 설명되지 않는 실패는 `systematic-debugging` 원인 규명 뒤 1회차부터 다시 돈다(Task 3). 회차별 결과와 조사 기록이 SUMMARY에 남는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` 통과
- `CI=true` 붙여넣기 E2E(캡처 원문 재생) 초록
- 페이즈 최종 독립 DOM 감사(1280·1024·375)와 `/design-review` §11 대조 결과가 SUMMARY에 있다(DR-19)
- 캡처 원문 둘(6열 · 45줄)이 fixture에 있고 재생 단언이 초록 · 사람 확인 (C)(D)의 최종 답이 전부 「같다」로 SUMMARY에 있다(후속으로 넘긴 항목 0)
- 페이즈 최종 게이트(Task 3): `CI=true` 단위·통합 한 번 + 전체 E2E 새 DB 연속 3회 초록 · 마이그레이션 0011→0016 순서 · 0015 가드(외화 포함) · `rollback-floor` 표시(0012·0015) 검증 초록
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 04-04 뒤에 바뀐 최종 견적 표에서 실제 엑셀 붙여넣기가 캡처 원문 재생으로 다시 통과했다(04-04 방식)
- 페이즈 화면 전체가 `/design-review` 한 번으로 SYSTEM.md §11과 대조됐다(DR-19)
- 프로젝트 간 복사가 오류 칸 없이 합계를 보존하고, 끝 줄바꿈이 빈 줄을 만들지 않으며, 엑셀 6열의 계산 열 자리 값이 오류 칸이 된다
- 모든 필수 확인이 통과했다 — 알려진 실패를 안고 끝나지 않는다
- 새 DB에서 CI=true 전체 E2E가 연속 세 번 초록이고, 마이그레이션 0011→0016과 0015 가드·롤백 하한 표시가 검증됐다(main 전용 배포 가드는 04-50으로 옮겼다)

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 테스트(신규) | `test/e2e/excel-paste-final.spec.ts`(캡처 원문 재생) |
| fixture(신규) | `test/fixtures/excel-clipboard.ts` — 실제 Windows 엑셀 클립보드 원문 상수(`REAL_EXCEL_WINDOWS_20260923` 이동 + 6열 · 45줄 캡처) · `test/unit/ui/parse-tsv.test.ts`가 import |
| 검증 기록 | 페이즈 최종 독립 DOM 감사(1280·1024·375) · `/design-review` §11 대조 결과(DR-19) |
| 최종 게이트 기록 | 전체 E2E 새 DB 연속 3회 결과(`test-results/final-gate-run-{1,2,3}` — 실패 회차 trace 보존) · 마이그레이션 순서·가드·하한 표시 검증 결과 · (있으면) 플레이크 원인·수정·회귀 테스트 |
| 워크플로(옮김) | `.github/workflows/deploy.yml` main 전용 가드(ENG-D12)는 04-50이 만든다(엔지 r2 재계획) |
| 사람 확인 기록 | SUMMARY의 캡처 원문 둘 · (C)(D) 최종 답(전부 「같다」) · 어긋났던 항목의 기대 → 화면 값 쌍과 고친 자리 |
| 조건부 수정 | 붙여넣기 `ui/table/parse-tsv.ts`·`ui/table/use-clipboard-paste.ts`(+ 두 단위 테스트) · 복사 `ui/table/Table.tsx`(+ `quote-table.spec.ts`) · 입력기 `app/(app)/projects/project-form.tsx`·`lib/shortcut.ts`(+ `shortcut.test.ts`·`project-register.spec.ts`) — 어긋남이 있을 때만 |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| OV-7 / 사용자 D21 — 최종 표 변경 뒤 실제 엑셀 확인 없음 | HIGH | Plan 04-31(신규): 04-18·04-19 뒤 `checkpoint:human-verify` blocking(웨이브 24 — 페이즈 마지막) |
| C-05 — 실제 클립보드 형식은 사람 확인에서 | WARNING | Plan 04-31, Task 1 (a)(d) · Task 2 항목 3 |
| C-03 / 사용자 D15 — 프로젝트 간 복사 | WARNING | Plan 04-31, Task 1 (c) · Task 2 항목 4 (구현은 04-19) |
| C-19 — 복사 이벤트의 실제 동작 | NOTE | Plan 04-31, Task 2 항목 4 뒷부분 (구현은 04-19) |
| C-06 — Windows 한국어 입력기 keydown | WARNING | Plan 04-31, Task 2 항목 6 (가드는 04-08) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Codex #5 (P1) — 최종 확인이 복사·입력기 실패를 후속 작업으로 돌리고 통과 | DECISION | Plan 04-31, truth · 금지 항목 · `<probe_fallback>` · Task 3 action(여기서 고치고 다시 확인 — `/gsd-quick` 경로 삭제) · 고칠 자리를 `files_modified`에 · 수용 기준 「최종 답 전부 같다 · 후속 0」 · 전체 게이트 `CI=true pnpm test` · T-04-318 |
| ENG-D12 (R11 — Codex #6) — 한 PR 배포 규칙 강제 | DECISION | Plan 04-31, Task 2(신규): `deploy.yml` 스테이징·프로덕션 잡 ref-guard(인증 전 · env `REF`) + `workflows.test.ts` 단언 · T-04-317 (2026-09-24 → Plan 04-50 Task 2로 내용 그대로 옮김 — 머지 묶음 규칙의 강제 장치) |
| ENG-D5 (R4) — 엑셀 6열 사례 | DECISION | Plan 04-31, Task 1 (a2) E2E · Task 3 항목 7 · truth (구현은 04-19) |
| ENG-D11 — 앞 플랜 결함은 고치고 회귀 테스트 + 편차 | DECISION | objective 「앞 플랜 결함」 문단 · Task 3 action |
| 엔지 리뷰 C 04-03 [P3] — 공급망 검사가 `package.json` 원문 비교 | NOTE | T-04-SC · `<verification>` 문구 |
| 교차 그룹 — 승인 준비는 `approvalBasis` | NOTE | objective 실행 규율 문장 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 98acc74 (plan-design-review, `docs/designs/plant8-erp-phase4-design-review-260923.md` · UI-SPEC rev 5 재계획 그룹 C)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-19 — `/design-review`를 부르는 플랜 0개, §11 대조 없음 | MEDIUM | Plan 04-31, truth · Task 3 ③ `/design-review` 1회(§11 목록) · 발견 수정 · SUMMARY 항목별 결과 |
| DR-14 · 교차 그룹 계약 6 — DOM 감사 폭에 1024 | HIGH | Plan 04-31, backstop truth · Task 3 ② 페이즈 최종 독립 DOM 감사 1280·1024·375(+ 1000) |
| 04-04 Task 3 종결(실제 캡처 원문 재생 — 사용자 승인) | DECISION | Plan 04-31, truth · 금지 항목 · Task 1 ① fixture 모듈 · Task 3 캡처 둘 받기 + 재생 · 사람 확인은 PC 전용 두 항목(프로젝트 간 복사·표 → 엑셀 · 입력기)만 |
| `/design-review`가 리저브 대장(04-42)까지 봐야 함 | NOTE | depends_on에 04-42 · 웨이브 26 — 이 플랜에 기대는 플랜이 없어 다른 웨이브 무변경 |
| DR-27 — 조합 중 Esc가 입력을 버리지 않음 | NOTE | Task 3 (D)에 한 줄(입력기 확인과 같은 자리) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 4 — plan-checker 반복 1(2026-09-23 revision)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W2 — 04-19 분할(04-19 = 쪽 나눔·키보드·복사·힌트 줄, 새 04-47 = 붙여넣기·합계 행 한 줄·DR-5·저장 거부 쪽) | WARNING | Plan 04-31 depends_on 04-19 → 04-47(04-47 → 04-19로 도달 유지, 04-18도 04-47 뒤) · 포인터를 몫대로 고침: 계산 열 무시·외화 경고·끝 줄바꿈·`2쪽까지`·합계 행 한 줄 = 04-47, 쪽 나눔·네이티브 복사(C-19) = 04-19 · context에 04-47-SUMMARY. 위 Round 1–2의 「구현은 04-19」 행 중 C-03·ENG-D5는 이제 04-47이 구현한다 |

### Round 5 — 체커 revision 1 후속 정리(2026-09-23 · 분할 포인터 · 사용자 후속 결정 R1)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 분할 포인터 — 비활성 버튼(계약 1)·입력 버리기 확인(DR-27)·계산 열 무시의 주인 | NOTE | Task 3 ② 감사 항목 「`aria-disabled` — 04-08」 → 04-46 · Task 3 what-built 「Esc 입력 버리기 확인(04-08)」 → 04-46(Ctrl+Enter 연타 가드는 04-08 그대로) · objective (c) 「계산 열 무시 — 04-19」 → 04-47. `lib/shortcut.ts` `isCtrlCombo`(C-06 가드)는 04-08에 남았으므로 그대로. 04-46은 조상(04-42 → 04-07 → 04-09 → 04-06 → 04-28 → 04-46), 04-47은 직접 의존 — `depends_on` 변경 없음 |
| 사용자 결정 R1 — 1024 미만 1차는 N ≥ 1이면 렌더 | NOTE | Task 3 ② 페이즈 최종 DOM 감사의 상세 견적 원장 항목: 「1차는 표 밖 칸 dirty일 때만」 → 「dirty 0이면 없고 N ≥ 1이면 렌더(표 셀 dirty 포함)」(UI-SPEC rev 5 후속 결정 R1) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 6 — 엔지니어링 리뷰 r2 반영(2026-09-24 재계획 · `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 8 · 9 · 머지 묶음 결정)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2-03 · CEO r2 지시 5 정정 — 최종 게이트는 04-31, 가드는 0015 | HIGH | 새 Task 3(최종 게이트) · truths 둘(3회 판정 · 마이그레이션 순서·0015 가드·하한 표시) · T-04-373 |
| E2-06 — 플레이크 판정 수치 없음 | MEDIUM | Task 3 ②③: 새 DB `CI=true` 전체 E2E 연속 3회 · `--trace=retain-on-failure` 회차별 출력 · `systematic-debugging` 조사 뒤 1회차부터 재실행 · `retries` 0 유지(수용 기준 `git diff` 0줄) |
| E2-05 — 0015 가드 외화 조건 | MEDIUM | 구현은 04-41 · 이 플랜 Task 3 ①이 업그레이드 테스트의 외화 픽스처 존재·초록을 확인(없으면 ENG-D11로 더함) |
| 지시 9 · 사용자 결정(.continue-here decisions_made) — E2-04를 첫 묶음 전에 | DECISION | 옛 Task 2(`deploy.yml` main 전용 가드 · ENG-D12)와 E2-04 롤백 하한 가드를 04-50(묶음 ② 첫 웨이브)으로 옮김 — truths·금지 항목·T-04-317·`workflows.test.ts` 단언 이동 · files_modified에서 `deploy.yml`·`workflows.test.ts` 삭제. 옛 Task 3 → Task 2(끝의 전체 게이트 한 번은 Task 3이 대신 — 두 번 돌리지 않는다) |
| 머지 묶음 결정(HANDOFF decisions[0] — R11 대체) | DECISION | objective 머지 묶음 ④ 줄 · 묶음 ④·페이즈 마지막 플랜 Post-build 줄 · 웨이브 29 → 30 |
| CEO r2 지시 3 — T-04-SC 「04-03 재개」 문구 | LOW | 삭제 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 사람 확인 항목(엑셀 원문 2종 6열·45줄 캡처 · 앱→엑셀 복사 · 한글 IME 조합 중 Esc) | 자동화로 대체하지 않는다 — Task 2 `checkpoint:human-verify` 그대로(HANDOFF human_actions_pending) |

### Round 7 — 2026-09-24 체커 revision 1(04-42 분할 → 04-51)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| scope_sanity(04-42) — 결정 ②가 새 Plan 04-51(웨이브 29)로 분할 | NOTE | `depends_on`에 04-51 추가 — 묶음 ④의 모든 플랜이 이 플랜 앞에서 끝난다. 웨이브 30 그대로(04-42·04-51 모두 웨이브 29) · 이 플랜의 태스크·검증 무변경 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-31-SUMMARY.md` when done
</output>
