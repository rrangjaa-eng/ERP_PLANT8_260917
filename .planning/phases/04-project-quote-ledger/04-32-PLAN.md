---
phase: 04-project-quote-ledger
plan: 32
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - lib/db-transaction.ts
  - db/client.ts
  - test/integration/tx-safety.test.ts
  - repositories/action-log.ts
  - domain/action-log/record.ts
  - repositories/settings.ts
  - docs/ARCHITECTURE.md
  - test/integration/action-log.test.ts
  - domain/permissions/project.ts
  - domain/permissions/dto-registry.ts
  - test/unit/permissions/project.test.ts
  - test/integration/leak-scan.test.ts
autonomous: true
requirements: [PROJ-02, PROJ-04, UX-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`withTransaction`이 여는 모든 트랜잭션은 첫 문장으로 `SET LOCAL lock_timeout = '5s'`를 돌린다 — 다른 트랜잭션이 잡은 행 잠금을 5초 넘게 기다리지 않고, 그 경우 요청은 원시 PG 오류가 아니라 `다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장`(UserFacing)로 끝난다(ENG-D3 ①)"
    - "커넥션 풀은 `connectionTimeoutMillis` 5,000을 갖는다 — 풀이 비어 연결을 얻지 못하면 무한히 기다리지 않고 5초 뒤 실패하며, 트랜잭션 안에서 난 그 실패도 같은 UserFacing 문구로 끝난다(ENG-D3 ①)"
    - "풀 크기 2에서 합성 저장(`saveProjectLedger`) 셋을 동시에 보내도 10초 안에 셋 다 끝난다 — 각 결과는 성공이거나 위 UserFacing 거부이고, 멈춘 요청·원시 풀 오류가 없다(ENG-D3 ① 통합 테스트 — 04-20·04-22·04-12가 같은 파일에 「셋 다 성공」 케이스를 더한다)"
    - "`recordAction`·`appendActionLog`가 선택 `tx`를 받고, `tx`가 있으면 로그 쓰기와 끌 수 있는 종류의 설정 조회가 **둘 다 그 tx로** 돈다 — 잠근 트랜잭션 안에서 풀 연결을 하나 더 잡지 않는다. 인자가 없으면 지금 동작 그대로다(CEO A-01 토대 — 04-20에서 앞당김, ENG-D3 ① 규칙)"
    - "`docs/ARCHITECTURE.md`에 트랜잭션·잠금 규약 절이 있다 — 프로젝트 쓰기 트랜잭션은 첫 단계에서 프로젝트 행을 잠그고(`loadProjectForGate`·`lockProjectForWrite`), 잠근 트랜잭션 안에서는 전역 `db`(풀)를 부르지 않으며(모든 읽기·쓰기에 `tx`를 넘기거나 `can`·`visible`·설정·투영을 트랜잭션 밖 앞뒤로 옮긴다), 거부 운영 로그는 `denyWrite` 한 함수, 경합 테스트는 `afterLock` 주입 지점을 쓴다(ENG-D3 ① · 엔지 리뷰 A 「잠금 규약 문단」)"
    - "`project()`는 한 번 부를 때 명세에 나오는 정보 항목마다 노출 여부를 **한 번만** 조회하고, 여러 행은 `projectMany(viewer, rows, spec)`가 행 수와 무관하게 서로 다른 정보 항목 수만큼만 조회한다 — 300행 × 18필드(정보 항목 둘)에서 노출 조회는 2회다(ENG-D3 ②)"
    - "DTO 명세의 필드는 정보 항목 하나(`string`) 또는 **모두 필요한 항목 목록**(`readonly string[]`, all-of)을 가진다 — 목록이면 그 항목을 전부 볼 수 있을 때만 키가 실린다. `registerDto`와 누수 스캔도 목록을 받아 각 항목이 `INFO_ITEMS`에 있고 조회되는지 본다(ENG-D3 ② · 엔지 리뷰 C §1 — 04-17 합계 DTO와 04-18 수익 열 셋이 쓴다)"
  prohibitions:
    - statement: "잠금을 잡은 트랜잭션이 풀에서 두 번째 연결을 기다리며 멈춰서는 안 된다. 풀 5개에 동시 저장 다섯이 각자 연결 하나를 쥔 채 두 번째 연결을 기다리면 PG는 교착을 모르고 요청이 Cloud Run 시간 제한까지 걸린다 — 시간 제한(잠금 5초 · 풀 5초)이 멈춤을 거부로 바꾸고, 규약(잠금 안 풀 호출 금지)이 거부 자체를 없앤다"
      status: resolved
      verification: test
  artifacts:
    - path: "lib/db-transaction.ts"
      provides: "`withTransaction` — `SET LOCAL lock_timeout` + 잠금·풀 시간 초과의 UserFacing 변환"
      contains: "lock_timeout"
    - path: "db/client.ts"
      provides: "풀 `connectionTimeoutMillis`(두 연결 경로 모두)"
      contains: "connectionTimeoutMillis"
    - path: "test/integration/tx-safety.test.ts"
      provides: "풀 2 × 동시 합성 저장 3 · lock_timeout 값 · 잠금 대기 초과 거부 — 뒤 플랜들이 「셋 다 성공」 케이스를 더하는 파일"
      min_lines: 60
    - path: "domain/permissions/project.ts"
      provides: "호출당 정보 항목 한 번 조회 · `projectMany` · all-of 정보 항목"
      exports: ["project", "projectMany", "InfoItemRef"]
    - path: "test/unit/permissions/project.test.ts"
      provides: "노출 조회 횟수(세는 스텁) · all-of 조합 표"
  key_links:
    - from: "lib/db-transaction.ts"
      to: "db/client.ts"
      via: "모든 domain 트랜잭션이 `withTransaction` 한 곳에서 잠금 시간 제한을 얻는다"
      pattern: "SET LOCAL lock_timeout"
    - from: "domain/action-log/record.ts"
      to: "repositories/settings.ts"
      via: "`tx`가 있으면 끌 수 있는 종류의 설정 조회도 같은 tx로 읽는다 — 잠근 트랜잭션 안의 풀 호출 0"
      pattern: "findSimpleValue"
    - from: "test/integration/leak-scan.test.ts"
      to: "domain/permissions/dto-registry.ts"
      via: "all-of 필드의 항목 목록을 하나씩 등록·조회 확인한다"
      pattern: "infoItem"
---

<objective>
**As a** 동시에 저장하는 기획 PM·팀장·경영관리, **I want to** 여러 사람이 같은 순간에 저장하거나 상태를 바꿔도 요청이 멈춰 버리지 않고 몇 초 안에 성공하거나 「잠시 뒤 다시」로 끝나며, 300줄 견적과 50행 목록이 노출 판정 때문에 수천 번 DB를 두드리지 않기를, **so that** 풀 연결 다섯 개짜리 운영 인스턴스가 저장 몇 건에 굳지 않고, 뒤 플랜들이 잠금·투영을 같은 규약 위에 얹는다.

머지 묶음: ②/4 상태 전환 — 웨이브 2~18(04-50 → 04-23)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**실행 규율 보강(2026-09-24 재계획 — CLAUDE.md · hooks 3bed7b1 · 세션 B 교훈):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋(문서 포함)과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(플랜 하나 — 체크포인트 이어가기도 새 세션). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 화면 검증은 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번이고, 완료 판정은 로컬 dev가 아니라 `CI=true`다.

**2026-09-23 엔지니어링 리뷰(ENG-D3, `docs/designs/plant8-erp-phase4-eng-review-260923.md` R2) 신설 플랜.** 사용자는 기반 수정 셋을 모두 Phase 4에 넣기로 했다: ① 잠금 트랜잭션·풀의 시간 제한과 「잠금 안에서 전역 db 호출 금지」 규칙(「첫 잠금 플랜 04-20에」) ② `project()`의 N+1 노출 조회와 all-of 정보 항목 ③ 시드의 노출값 덮어쓰기(04-20이 한다). ①을 04-20에 그대로 넣으면 04-20이 18파일이 되어 파일 상한(15)을 넘는다 — 그래서 **첫 잠금 플랜(04-20) 바로 앞 토대**로 이 플랜을 떼었다. 04-20의 `recordAction`·`appendActionLog` `tx` 인자(CEO A-01)도 같은 관심사라 여기로 앞당겼다. ②는 N+1이 가장 큰 곳(04-12 견적 300줄 · 04-17 목록)보다 앞에 있어야 하고 all-of를 쓰는 04-17·04-18보다 앞이어야 해서 같은 토대에 둔다. 이 플랜은 견적 표 파일을 건드리지 않아 04-04의 사람 확인을 기다리지 않는다(사용자 D21).

Purpose: 04-20·04-11·04-22·04-12·04-26·그룹 B의 04-40·04-07이 전부 프로젝트 행 `FOR UPDATE` 안에서 판정한다. 그 전에 (a) 잠금 대기와 풀 대기에 상한이 있고 (b) 규약이 문서로 서 있으며 (c) 로그가 같은 트랜잭션으로 들어가는 길이 있어야 한다. 또 투영 조회 횟수를 행 수와 떼어 놓아야 300줄 상세와 50행 목록이 풀을 잡아먹지 않는다.

Output: `withTransaction` 잠금 시간 제한·오류 변환 · 풀 연결 시간 제한 · 행동 로그 `tx`(설정 조회 포함) · ARCHITECTURE §4-8 · `project()` 호출당 조회 한 번·`projectMany`·all-of · 통합 2(신규 1) · 단위 1(확장) · 누수 스캔 확장.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 경합·대기 순서는 `sleep`이 아니라 약속(`deferred`)과 폴링으로 정한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-02-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# docs/designs/plant8-erp-phase4-eng-review-260923.md 36–47행(R2 — ENG-D3) · 116–140행(결정 없이 반영 — 묶음 A) · 부록 A 「All locking plans … app-level deadlock」 · 「project() runs one visibility query per field per row」 · 부록 C §1 「requires both info items」
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 없다 — 풀·잠금 시간 제한과 투영 조회 횟수는 요구사항 문장이 아니라 PROJ-02·PROJ-04·UX-04(저장 실패·중복 저장·입력값 유실 없음)의 비기능 전제다. 수용 기준은 엔지니어링 리뷰 ENG-D3와 부록 A·C의 근거(파일:줄)에서 끌어왔다.

**계획 단계 가정(표면화):**
- **시간 값:** `lock_timeout` 5초 · 풀 `connectionTimeoutMillis` 5,000(ENG-D3 원문 값). 설정 키·환경 변수로 빼지 않는다(요청되지 않은 설정 가능성은 만들지 않는다 — CLAUDE.md). `statement_timeout`은 ENG-D3 범위 밖이라 두지 않는다.
- **새 문구:** `다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장` — UI-SPEC rev 5 Copywriting에 없는 서버 방어 문구다. P0(rev 5 Copywriting 첫 절 — 가장 짧은 `원인 · 다음 행동`, `…습니다` 꼴 금지)대로 명사형으로 적고 SUMMARY 「새 문구」 표에 남긴다(rev 5 Copywriting에 올릴지는 페이즈 검증 때 보고).
- **풀 크기 2의 재현 방법:** 통합 설정(`test/integration/setup.ts`)이 기본 풀을 먼저 만든다. 이 파일만 `vi.resetModules()` 뒤 `DB_POOL_MAX=2`를 넣고 `@/db/client`·`@/domain/projects/ledger`를 **동적 import**해 크기 2의 두 번째 풀로 합성 저장을 돌린다(끝나면 그 풀을 닫고 환경 변수를 되돌린다). 테스트가 새 풀의 `options.max === 2`를 먼저 단언한다 — 모듈이 격리되지 않으면 여기서 빨갛게 된다. 이 방법이 통하지 않으면 `DB_POOL_MAX=2 pnpm vitest run …` 한 줄을 이 파일 전용 검증 명령으로 두는 쪽으로 바꾸고 SUMMARY에 적는다.
- **이 플랜 시점의 합성 저장은 아직 규약을 어긴다:** `saveQuoteLines`는 원장 트랜잭션 안에서 `can()`·차수 조회·사용자 정의 필드 스키마·`rememberFxRate`·`recordAction`·`project()`를 풀로 부른다(엔지 리뷰 부록 A 근거 줄). 그래서 이 플랜의 동시 저장 케이스는 「셋 다 끝난다 — 성공 또는 UserFacing 거부」까지만 단언한다. 규약을 지키게 고친 04-22(기간)·04-12(견적 줄)가 같은 파일에 **「셋 다 성공」** 케이스를 더한다(그룹 B의 매출·리저브는 그룹 B 플랜이 더한다).
- **all-of 표기:** `infoItem: string | readonly string[]`. 목록은 비어 있으면 안 된다(등록 때 거부). 판정은 목록의 모든 항목이 `visible`일 때만 참.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 풀 크기 2에서 동시 합성 저장 셋이 10초 안에 끝난다(풀 연결 시간 제한 → `withTransaction` 잠금 시간 제한 → 오류 변환 → 통합)</name>
  <files>db/client.ts, lib/db-transaction.ts, test/integration/tx-safety.test.ts</files>
  <read_first>
    - `db/client.ts`(전문 58행) — `createPool`의 두 경로(Cloud SQL 커넥터 · `DATABASE_URL`)와 `DbOrTx`
    - `lib/db-transaction.ts`(전문) — `db.transaction(fn)` 한 줄
    - `lib/actions/user-facing-error.ts` — `UserFacingError`
    - `lib/env.ts` 30–60행(`DB_POOL_MAX: numberWithDefault(5)`)
    - `domain/projects/ledger.ts`(전문) — `saveProjectLedger`의 트랜잭션 형태
    - `test/integration/setup.ts`(전문) — 파일마다 TRUNCATE·시드, 기본 풀
    - `test/integration/quote-lines.test.ts` 1–80행 — 프로젝트·차수·줄을 준비하는 형태(합성 저장 입력을 만드는 법)
    - `test/unit/db-client-close.test.ts`(전문) — `pg.Pool`을 모킹한 기존 단위 테스트(생성 인자가 늘어도 깨지지 않는지 확인만)
  </read_first>
  <behavior>
    - `withTransaction` 안에서 `SHOW lock_timeout` → `5s`
    - 연결 A가 한 프로젝트 행을 `FOR UPDATE`로 잡고 약속으로 멈춘 동안, `withTransaction` 안에서 같은 행을 잠그려는 호출 B는 약 5초 뒤 `UserFacingError`(문구 위 truths)로 끝난다 — 원시 오류 코드 `55P03`이 호출자에게 나가지 않는다
    - 새 풀의 `options.max === 2`이고 `options.connectionTimeoutMillis === 5000`
    - 풀 크기 2에서 같은 프로젝트에 새 줄 하나씩 싣는 `saveProjectLedger` 셋을 `Promise.allSettled`로 동시에 보내면 10초 안에 셋 다 끝나고, 거부된 것은 전부 `UserFacingError`다(원시 `timeout exceeded when trying to connect` 오류 0)
  </behavior>
  <action>
① **풀(`db/client.ts`).** 두 `new Pool({ … })` 모두에 `connectionTimeoutMillis: POOL_CONNECTION_TIMEOUT_MS`(모듈 상수 5,000 — 주석 한 줄: ENG-D3 ①, 풀 대기 무한 방지)를 더한다. 다른 옵션·`DbOrTx`는 바꾸지 않는다.

② **`withTransaction`(`lib/db-transaction.ts`).** 콜백 앞에서 트랜잭션 객체로 `SET LOCAL lock_timeout = '5s'`를 한 번 돌린 뒤 `fn(tx)`를 부른다(`db.transaction`이 주는 tx는 `execute`를 가진다 — `DbOrTx` 타입은 넓히지 않는다). `db.transaction` 전체를 감싸 두 시간 초과를 `UserFacingError`(truths 문구)로 바꿔 던진다: PG 오류 코드 `55P03`(lock_not_available) · pg-pool의 연결 시간 초과(메시지 `timeout exceeded when trying to connect`). 판정은 작은 함수 하나(`isTxTimeoutError`)에 두고 그 밖의 오류는 그대로 다시 던진다. 머리 주석에 이 절의 이유(ENG-D3 ①)와 ARCHITECTURE §4-8(Task 2) 참조를 한 줄씩 더한다.

③ **통합(`test/integration/tx-safety.test.ts` 신규).** describe `잠금·풀 시간 제한(ENG-D3 ①)`: (a) `SHOW lock_timeout`, (b) 잠금 대기 초과 → UserFacing(연결 A는 풀에서 직접 연 클라이언트로 `BEGIN; SELECT … FOR UPDATE`를 하고 약속으로 멈춘다 — B가 끝난 뒤 A를 `ROLLBACK`), (c) 풀 크기 2 동시 합성 저장 셋(`<probe_fallback>`의 모듈 격리 방법 — 새 풀 옵션 단언이 먼저) — 시작부터 셋 다 끝날 때까지 10초 미만, 각 결과는 성공 또는 `UserFacingError`. (c)의 describe 이름을 `풀 2 · 동시 합성 저장 셋`으로 두고 주석에 「04-22·04-12가 규약을 지키게 고친 뒤 같은 describe에 「셋 다 성공」 케이스를 더한다」를 적는다. `sleep`으로 순서를 맞추지 않는다(대기는 폴링과 약속).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/tx-safety.test.ts test/integration/quote-lines.test.ts test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 트랜잭션에 잠금 시간 제한이 없거나, 시간 초과가 원시 오류로 나가거나, 풀 크기 2 동시 저장이 10초 안에 끝나지 않거나(멈춤), 기존 합성 저장이 깨졌다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/db-client-close.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 eslint·tsc error가 나온다 — 풀 생성 인자 변경이 커넥터 닫기 테스트를 깨뜨렸거나 타입이 어긋났다</fails_when>
  </verify>
  <acceptance_criteria>
    - `db/client.ts`의 두 `new Pool(` 호출이 모두 `connectionTimeoutMillis`를 갖는다(`grep -c "connectionTimeoutMillis" db/client.ts`가 2 이상 — 상수 정의 포함)
    - `lib/db-transaction.ts`가 콜백 전에 `SET LOCAL lock_timeout = '5s'`를 돌리고, `55P03`과 풀 연결 시간 초과만 `UserFacingError`로 바꾼다(그 밖의 오류는 그대로)
    - `tx-safety.test.ts`에 (a)(b)(c)가 있고 (c)가 새 풀의 `max === 2`를 먼저 단언한다
    - `sleep`·고정 지연으로 순서를 정하는 코드가 테스트에 없다(폴링 간격만)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>잠금 대기와 풀 대기에 5초 상한이 생기고, 풀 두 개에 동시 저장 셋이 몰려도 요청이 멈추지 않고 10초 안에 성공 또는 「잠시 뒤 다시」로 끝난다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 행동 로그 `tx`(설정 조회까지 같은 tx) + ARCHITECTURE §4-8 트랜잭션·잠금 규약</name>
  <files>repositories/action-log.ts, domain/action-log/record.ts, repositories/settings.ts, docs/ARCHITECTURE.md, test/integration/action-log.test.ts</files>
  <read_first>
    - `repositories/action-log.ts` 1–40행(`appendActionLog` — 풀 `db`만 쓴다)
    - `domain/action-log/record.ts` 60–150행(`ALWAYS_ON_ACTION_TYPES` · `RecordActionDeps` · `defaultIsActionTypeEnabled`의 동적 import와 `getSettingValue(ACTION_LOG_OPTIONAL_TYPES)` · `recordAction` 본문)
    - `domain/settings/registry.ts` 60–92행(`getSettingValue(def, opts, deps)` — `deps.findSimpleValue` 주입 자리. 이 파일은 고치지 않는다)
    - `repositories/settings.ts` 1–15행(`findSimpleValue`)
    - `docs/ARCHITECTURE.md` §4-2·§4-6·§4-7(절 문체) · `test/unit/docs-limits.test.ts`(ARCHITECTURE 300줄 상한)
    - `test/integration/action-log.test.ts`(전문) — 기존 기록 단언 형태
    - `.planning/phases/04-project-quote-ledger/04-20-PLAN.md` Task 1 ⑤ · `04-11-PLAN.md` Task 2 ③ · `04-12-PLAN.md` Task 1 ③ — §4-8이 적을 규약을 실제로 쓰는 자리(이름을 맞추려고 읽기만)
  </read_first>
  <behavior>
    - `recordAction(viewer, entry)`(tx 없음) → 지금과 같은 행 한 줄 · 설정으로 끈 종류는 행 없음
    - `withTransaction` 안에서 `recordAction(viewer, { actionType: "document_update", … }, { tx })` 뒤 트랜잭션을 되돌리면 `action_log`에 그 행이 없다
    - 같은 호출 동안 `pool.connect` 호출 수가 0이다(스파이를 호출 직전에 켜고 직후에 끈다) — 로그 쓰기와 끌 수 있는 종류의 설정 조회가 둘 다 tx로 돈다
    - 끌 수 있는 종류를 설정에서 끈 상태에서 `{ tx }`로 부르면 행이 없다(설정 조회가 tx 경로에서도 동작)
  </behavior>
  <action>
① **리포지토리.** `repositories/action-log.ts`의 `appendActionLog(viewer, entry, tx?: DbOrTx)` — 없으면 풀 `db`. `repositories/settings.ts`의 `findSimpleValue(viewer, key, tx?: DbOrTx)` — 같은 규칙. 기존 호출자는 바뀌지 않는다.

② **`recordAction`(`domain/action-log/record.ts`).** `RecordActionDeps`에 `tx?: DbOrTx`를 더한다. `tx`가 있으면 (a) `appendActionLog(…, tx)` (b) 끌 수 있는 종류의 판정도 `defaultIsActionTypeEnabled(actionType, tx)`로 — 그 안의 `getSettingValue(ACTION_LOG_OPTIONAL_TYPES, undefined, { findSimpleValue: (v, k) => findSimpleValue(v, k, tx) })`로 같은 tx에서 읽는다(`domain/settings/registry.ts`는 이미 `deps.findSimpleValue` 주입 자리가 있다 — 그 파일은 고치지 않는다). fail-open(읽기 실패 시 기록) 규칙은 그대로. 끌 수 없는 종류(`status_change` 등)는 설정을 아예 읽지 않는 지금 규칙 그대로. 이 인자를 04-20(상태 전환)·04-11(자동 정산)·04-22(기간)·04-12(견적 줄)·그룹 B가 쓴다 — 04-20 계획에 있던 이 변경을 여기로 앞당겼다(SUMMARY에 한 줄).

③ **`docs/ARCHITECTURE.md` §4-8 「트랜잭션·잠금 규약(Phase 4)」**를 §4-7 뒤에 20줄 이하로 더한다(300줄 상한 — `docs-limits` 테스트): (1) 트랜잭션은 `withTransaction` 하나로 연다 — `lock_timeout 5s`, 풀 `connectionTimeoutMillis 5s`, 두 시간 초과는 UserFacing 「잠시 뒤 다시」. (2) 프로젝트를 바꾸는 쓰기 트랜잭션은 **첫 단계에서** 프로젝트 행을 잠근다 — `loadProjectForGate`(자동 정산 판정 포함, 04-11) 또는 `lockProjectForWrite`(04-20). 합성 저장은 한 번 잡은 잠금을 그 안의 함수들이 다시 잡아도 곧바로 돌아온다. (3) **잠근 트랜잭션 안에서는 전역 `db`(풀)를 부르지 않는다** — 리포지토리 읽기·쓰기와 `recordAction`에 `tx`를 넘기고, `can`·`visible`·`scopeFor`·설정 값·계급 업무 범위·소속 팀처럼 잠근 행과 무관한 사실은 **트랜잭션을 열기 전에** 읽으며, `project()`·`projectMany` 투영과 최근 환율 기억처럼 커밋 뒤에 해도 되는 일은 **커밋 뒤에** 한다. (4) 거부 운영 로그는 `denyWrite`(04-20, `domain/rules/deny-write.ts`) 한 함수로 남긴다 — 금액 키가 없는 허용 목록 필드. (5) 경합 테스트는 쓰기 함수의 `deps.afterLock` 주입 지점과 `test/integration/lock-race.ts`(04-20)의 `deferred`·`waitForLockWaiter`로 순서를 정하고 `sleep`을 쓰지 않는다. (6) 규약 준수의 통합 증명은 `test/integration/tx-safety.test.ts`(풀 2 · 동시 저장 셋). 아직 만들어지지 않은 이름(04-11·04-20의 함수)은 「(04-xx가 만든다)」로 표시한다 — 그 함수들은 같은 머지 묶음 ② 안에서 만들어진다(2026-09-24 고침 — 옛 「Phase 4는 한 PR로 배포」는 묶음마다 PR 하나 규칙으로 대체 · 정본 04-50 DECISIONS 항목).

④ **통합(`test/integration/action-log.test.ts`).** `behavior` 넷을 describe `tx 인자(ENG-D3 ①)`로 먼저 쓴다(RED → GREEN). `pool.connect` 스파이는 `@/db/client`의 `pool`에 건다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/action-log.test.ts test/integration/action-log-query.test.ts test/integration/settings.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — tx 인자가 기존 기록을 깨뜨렸거나, 되돌린 트랜잭션에 로그가 남았거나, tx 경로에서 풀 연결을 하나 더 잡았거나, 설정으로 끈 종류가 tx 경로에서 기록됐다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts test/unit/import-cycles.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 eslint·tsc error가 나온다 — ARCHITECTURE가 300줄을 넘었거나 record ↔ settings 순환이 정적 import로 바뀌었다</fails_when>
  </verify>
  <acceptance_criteria>
    - `appendActionLog`·`findSimpleValue`가 선택 `tx`를 받고 인자가 없을 때 동작이 그대로다(기존 통합 테스트가 수정 없이 초록)
    - `recordAction`이 `deps.tx`를 로그 쓰기와 설정 조회 둘 다에 넘기고, `domain/settings/registry.ts` diff가 0줄이다
    - `action-log.test.ts`에 `behavior` 넷이 있고 되돌림·풀 연결 0을 단언한다
    - `docs/ARCHITECTURE.md`에 `## 4-8.` 머리글이 한 줄이고 (1)~(6)을 담으며 `docs-limits` 테스트가 초록이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>잠근 트랜잭션 안에서 로그를 남겨도 풀 연결을 하나 더 잡지 않고, 되돌리면 로그도 사라지며, 뒤 플랜들이 따를 트랜잭션·잠금 규약이 문서로 섰다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `project()` 호출당 정보 항목 한 번 조회 · `projectMany` · all-of 정보 항목(명세 · 등록 · 누수 스캔)</name>
  <files>domain/permissions/project.ts, domain/permissions/dto-registry.ts, test/unit/permissions/project.test.ts, test/integration/leak-scan.test.ts</files>
  <read_first>
    - `domain/permissions/project.ts`(전문 36행) — 필드마다 `visibleFn` 호출
    - `domain/permissions/visible.ts`(전문) — 조회 한 번 = `findVisibility` 한 번
    - `domain/permissions/dto-registry.ts`(전문 19행)
    - `test/unit/permissions/project.test.ts`(전문) — 기존 투영 단언(멱등·키 순서·spec 밖 키)
    - `test/integration/leak-scan.test.ts` 40–60행(`buildDtoCases`) · 110–175행(DTO 축·내보내기 축의 `field.infoItem` 사용 자리)
    - Grep `fields.map((field) => ({ key: field.key, infoItem: field.infoItem }))` — `registerDto`를 부르는 DTO 모듈들(타입이 넓어져도 그대로 컴파일되는지 확인만)
  </read_first>
  <behavior>
    - 세는 `visible` 스텁으로 `project(viewer, row, spec)` — 16필드가 전부 `quote.amount`면 스텁 호출 1회 · 필드 셋이 항목 둘을 쓰면 2회
    - `projectMany(viewer, rows[300], spec)`(18필드, 서로 다른 항목 2) → 스텁 호출 2회이고 결과가 행마다 `project()`를 부른 것과 같다(키 순서 포함)
    - all-of 필드 `{ key: "profitKrw", infoItem: ["quote.amount", "revenue.issued_amount"] }` — 둘 다 참 → 키 있음 · 하나라도 거짓 → 키 없음 · 둘 다 거짓 → 키 없음
    - 기존 규칙 유지: 원본에 `from` 키가 없으면 건너뜀(멱등) · spec 밖 키는 싣지 않음 · 결과 키 순서 = spec 선언 순서
    - `registerDto`가 빈 목록 `infoItem: []`을 거부한다
    - 누수 스캔: all-of 필드의 항목 각각이 `INFO_ITEMS`에 있고 계급마다 `visible`이 boolean으로 확정된다
  </behavior>
  <action>
① **`domain/permissions/project.ts`.** `export type InfoItemRef = string | readonly string[]`, `DtoSpec`의 필드 `infoItem: InfoItemRef`. 내부 도우미가 spec에서 서로 다른 정보 항목을 모아 `Map<string, boolean>`으로 **한 번씩** `visibleFn`을 부르고(항목끼리는 순서대로 — 같은 요청의 풀을 한꺼번에 잡지 않는다), 필드는 그 맵으로 판정한다(목록이면 전부 참일 때만). `projectMany(viewer, rows, spec, deps?)`는 그 맵을 한 번 만들어 모든 행에 쓴다. `project()`는 `projectMany(viewer, [row], spec, deps)`의 첫 결과다(한 구현). 반환 타입·멱등·spec 밖 키 규칙은 그대로.

② **`domain/permissions/dto-registry.ts`.** `DtoRegistryEntry.fields`의 `infoItem: InfoItemRef`(project.ts의 타입을 import). `registerDto`가 빈 목록을 `Error`로 거부한다(중복 이름 거부와 같은 자리).

③ **단위(`test/unit/permissions/project.test.ts`).** `behavior` 앞 넷을 먼저 쓴다(RED → GREEN). 세는 스텁은 `deps.visible`로 넣는다.

④ **누수 스캔(`test/integration/leak-scan.test.ts`).** DTO 축과 내보내기 축에서 `field.infoItem`을 목록으로 펼쳐(문자열이면 한 원소) 항목마다 등록·조회를 확인한다. 케이스 이름 생성과 결정성 단언은 그대로. 새 all-of 필드는 04-17·04-18이 만든다 — 이 태스크는 스캔이 목록을 받는지까지(`registerDto` 빈 목록 거부 한 케이스를 여기 두어도 된다).

⑤ **호출자 전환은 각 플랜이 한다:** 견적 줄 목록·저장 결과(04-12) · 목록 행(04-17) · 매출·리저브(그룹 B)가 `rows.map(project)`를 `projectMany`로 바꾼다. 이 플랜은 기존 호출자를 바꾸지 않는다 — `project()`만으로도 한 행 안의 같은 항목 중복 조회는 이 태스크에서 사라진다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/permissions/project.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 조회 횟수가 행·필드 수를 따라가거나, all-of 판정·기존 투영 규칙이 틀렸다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts test/integration/visibility.test.ts test/integration/projects-list.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 누수 스캔이 목록 필드를 처리하지 못했거나, 투영 결과가 기존 화면 경로(목록·견적 줄)에서 달라졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·tsc·next build 중 error가 나오고 종료 코드가 0이 아니다 — 넓어진 `infoItem` 타입이 `registerDto` 호출자를 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `project.ts`에서 `visibleFn` 호출이 서로 다른 정보 항목 루프 안에만 있고 필드 루프 안에 없다
    - `projectMany`가 export되고 `project()`가 그것을 부른다(구현 하나)
    - 단위 테스트가 호출 횟수 1·2·2(300행)와 all-of 세 조합, 기존 규칙 셋을 단언한다
    - 누수 스캔이 목록 `infoItem`을 펼쳐 검사하고 초록이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>투영의 노출 조회가 행 수와 무관해지고, 「두 항목을 모두 볼 때만」이 명세·등록·누수 스캔에서 한 표기로 표현된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 동시 요청 → 커넥션 풀 | 요청마다 연결을 쥐고 잠금을 기다린다. 상한이 없으면 몇 건이 인스턴스 전체를 굳힌다 |
| domain 출구 → 화면 | `project()`가 필드 노출을 판정한다. 조회를 줄이는 변경이 판정을 바꾸면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-301 | Denial of Service | 잠금 트랜잭션 안의 풀 호출 · 무한 대기 | high | mitigate | `lock_timeout 5s`·풀 `connectionTimeoutMillis 5s`가 멈춤을 UserFacing 거부로 바꾸고, 규약(ARCHITECTURE §4-8 — 잠금 안 풀 호출 금지)을 문서로 세운다. 풀 2 × 동시 저장 3이 10초 안에 끝나는 통합 테스트(Task 1 c)로 고정하고, 규약을 지킨 뒤 「셋 다 성공」은 04-20·04-22·04-12가 같은 파일에서 증명한다 |
| T-04-302 | Repudiation | 되돌린 트랜잭션의 로그 | medium | mitigate | `recordAction`이 `tx`로 쓰면 트랜잭션과 함께 되돌아간다 — 「거부된 배치의 성공 로그」가 구조적으로 없다(Task 2 되돌림 케이스, 04-12 유령 로그 테스트의 토대) |
| T-04-303 | Information Disclosure | `project()` 판정 변경 | high | mitigate | 조회를 항목당 한 번으로 줄여도 필드 판정 규칙(spec 밖 키 금지 · 숨김이면 키 부재)은 같다 — 기존 투영 단위·누수 스캔·목록·견적 줄 통합이 수정 없이 초록이어야 한다. all-of는 목록의 **모든** 항목이 참일 때만 키를 싣는다(단위 세 조합) |
| T-04-304 | Denial of Service | 행 × 필드 노출 조회(N+1) | medium | mitigate | `projectMany`가 요청당 서로 다른 항목 수만큼만 조회한다(300행 → 2회 단위 단언). 호출자 전환은 04-12·04-17·그룹 B |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 통합 `tx-safety` · `action-log` · `leak-scan` 초록, 단위 `project` · `docs-limits` 초록
- `domain/settings/registry.ts`·`domain/permissions/visible.ts`·`can.ts`·`scope-for.ts` diff 0줄
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
</verification>

<success_criteria>
- 동시 저장이 풀을 굳히지 않고 5초 상한 안에서 성공 또는 「잠시 뒤 다시」로 끝난다
- 로그가 잠근 트랜잭션과 함께 커밋·롤백되고 그 과정에서 풀 연결을 더 잡지 않는다
- 투영 조회가 행 수와 무관하고 all-of 노출이 한 표기로 선다
- 뒤 잠금 플랜들이 따를 규약이 ARCHITECTURE에 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(변경) | `withTransaction`(`SET LOCAL lock_timeout = '5s'` · 시간 초과 → UserFacing) · `appendActionLog(…, tx?)` · `findSimpleValue(…, tx?)` · `recordAction(…, { tx })`(설정 조회까지 tx) · `project()`(항목당 한 번) |
| 함수(신규) | `projectMany` · `isTxTimeoutError`(`lib/db-transaction.ts` 내부) |
| 타입(신규) | `InfoItemRef = string \| readonly string[]`(all-of) |
| 풀 옵션 | `connectionTimeoutMillis: 5000`(두 연결 경로) |
| 문서 | `docs/ARCHITECTURE.md` §4-8 트랜잭션·잠금 규약 |
| 새 문구 | `다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장`(SUMMARY 「새 문구」) |
| 테스트 | `test/integration/tx-safety.test.ts`(신규 — 04-20·04-22·04-12·그룹 B가 확장) · `action-log.test.ts`(tx describe) · `test/unit/permissions/project.test.ts`(조회 횟수·all-of) · `leak-scan.test.ts`(목록 펼침) |
| 앞 플랜에서 옮겨 온 것 | `recordAction`·`appendActionLog`의 `tx` 인자(옛 04-20 Task 1 ②) |

## Review Dispositions Ledger

### Round 2 — ebcc051 (plan-eng-review, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-D3 ① — 잠금·풀 시간 제한 없음 · 잠금 안 전역 db 호출(엔지 리뷰 A §1 P2 「app-level deadlock」) | DECISION | Plan 04-32(신설 — 04-20이 18파일이 되어 파일 상한 초과, 첫 잠금 플랜 04-20 바로 앞 토대로 분리), Task 1 `lock_timeout`·`connectionTimeoutMillis`·UserFacing 변환·풀 2 × 동시 3 통합 · Task 2 규약 §4-8 |
| ENG-D3 ② — `project()` N+1 · all-of 정보 항목(엔지 리뷰 A §4 P2 · C §1 P2 두 건) | DECISION | Plan 04-32, Task 3: 항목당 한 번 · `projectMany` · `InfoItemRef` · 누수 스캔 펼침 · 세는 스텁 단위(300행 → 2회). 호출자 전환은 04-12·04-17, all-of 사용은 04-17(합계)·04-18(수익 열) |
| 엔지 리뷰 A P3 — ARCHITECTURE에 잠금 규약 문단 없음 | NOTE | Plan 04-32, Task 2 ③ §4-8 |
| 엔지 리뷰 A §1 P2 — 유령 로그(토대) | HIGH | Plan 04-32, Task 2: `recordAction` `tx`(설정 조회 포함) · 되돌림 단언 — 견적 줄·매출 쪽 적용과 유령 로그 테스트(t)는 04-12 |
| 엔지 리뷰 C P3 — 의존성 검사가 `package.json` 원문 비교 | NOTE | T-04-SC·verification: 의존성 객체 + lockfile 비교 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| `statement_timeout` | ENG-D3 범위 밖(사용자가 고른 두 시간 제한만) — 엔지 리뷰 A 「TODO candidates」 5 |

### Round 3 — 디자인 리뷰(`docs/designs/plant8-erp-phase4-design-review-260923.md`) · UI-SPEC rev 5

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| P0 · rev 5 F1 — 거부 문구가 `…습니다 · …해 주세요` 꼴 · D16 오인용(DR-1) | NOTE | Plan 04-32, truths·`<probe_fallback>` 「새 문구」·Artifacts: `다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장`(rev 5에 없는 방어 문구 — 명사형) · D16 인용 삭제(D16은 REQUIREMENTS·ROADMAP 세 문구 승인일 뿐이다) |
| 교차 그룹 — `seenStatus`(계약 4, 04-22) | NOTE | 이 플랜의 `tx-safety.test.ts` 합성 저장 호출은 04-22가 `seenStatus`를 싣게 고친다 — 이 플랜은 그대로 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-32-SUMMARY.md` when done
</output>
