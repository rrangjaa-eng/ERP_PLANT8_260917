---
phase: 04-project-quote-ledger
plan: 40
type: execute
wave: 17
depends_on: ["04-14"]
files_modified:
  - domain/quotes/lines.ts
  - domain/quotes/revisions.ts
  - domain/quotes/edit-scope.ts
  - domain/rules/register.ts
  - test/unit/domain/quote-edit-scope.test.ts
  - test/unit/domain/rules-gate.test.ts
  - test/integration/quote-revision-races.test.ts
  - test/integration/quote-approved-lock.test.ts
  - test/integration/quote-lines.test.ts
autonomous: true
requirements: [PROJ-07, PROJ-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "한 프로젝트의 견적 줄 저장 · 새 차수 · 고객 승인 표시는 트랜잭션 첫 단계에서 04-11의 `loadProjectForGate`(04-20 `lockProjectForWrite`의 행 배타 잠금 + 같은 트랜잭션 안 자동 정산 판정)로 프로젝트 행을 잠그고, 현재 차수·승인 여부·상태·줄 수 판정을 전부 잠금 뒤 같은 트랜잭션의 읽기로 한다 — 줄 저장은 04-12가, 새 차수·승인은 04-14가 이 잠금을 걸었고, 이 플랜은 두 연결로 경합을 강제하는 결정적 테스트로 그것을 증명한다(CEO 리뷰 OV-3 — 공유 잠금 정정 · A-13·A-33)"
    - "현재 차수가 아닌 차수로 온 견적 줄 저장은 잠금 뒤 다시 읽은 최신 차수와 비교해 `다른 사람이 새 차수를 만들었습니다 · 새로 고쳐 주세요`로 전부 거부되고 DB가 그대로다 — 오래된 탭이 이전(승인됐을 수도 있는) 차수에 조용히 쓰는 길이 없다(CEO 리뷰 B-01)"
    - "두 연결 결정적 경합에서 잃어버린 수정이 없다: 새 차수가 잠금을 쥔 동안 온 옛 차수 저장은 기다렸다가 거부되고, 저장이 잠금을 쥔 동안 온 새 차수는 기다렸다가 저장된 값을 복사한다 · 승인이 잠금을 쥔 동안 온 수량 저장은 기다렸다가 승인 차수 잠김으로 거부되고 실행가만 바꾼 저장은 통과한다 · 저장이 잠금을 쥔 동안 온 승인은 저장된 값을 승인한다(OV-3, B-01·B-07)"
    - "차수가 둘 이상인 프로젝트에서도 줄 소속이 지켜진다 — 1차 저장 페이로드에 같은 프로젝트 2차의 줄 id 하나가 섞이면 04-12의 `revision_id` 조건이 배치 전부를 거부하고, 일괄 저장의 `projectId`와 견적 줄 차수의 프로젝트가 다르면 04-22의 검사가 견적 줄·매출 어느 쪽도 쓰지 않는다 — 차수가 하나뿐이던 04-12·04-22 시점에는 증명할 수 없던 경우다(B-01 · A-14)"
    - "승인된 현재 차수에서 `quote` 종류 줄의 수량·단가(그래서 견적가)·상태 칸은 잠기고 이유는 `{n}차 고객 승인됨 · 고치려면 새 차수`다 — 실행가·항목·거래처·비고는 그대로 편집되고 견적 외 비용·조정 줄의 판정은 바뀌지 않는다(사용자 D7 「승인 차수 견적 칸 잠금 — 실행가는 편집 가능, 바꾸려면 새 차수」, B-07 A)"
    - "승인된 현재 차수에서 견적 합계를 바꾸는 모든 조작 — 견적 칸이 0이 아닌 새 줄 · 견적가가 있는 줄의 취소·취소 해제·보관 · 견적가가 있는 줄의 복원 — 을 서버가 같은 이유로 거부한다. 새 `quote` 줄은 정산 새 줄과 같은 칸 집합이 잠긴 채(수량·단가·상태 — 견적 칸 0) 추가된다(사용자 D12와 같은 결). 그래서 승인 차수의 견적 합계 = 승인 시점의 합계가 불변이다(CEO 리뷰 OV-1 — D7 답의 약속 「계약 금액은 항상 승인값과 같다」, 계약 금액 쪽 단언은 04-16)"
    - "보관함 복원도 같은 트랜잭션·같은 잠금 뒤에서 같은 판정을 지난다 — 04-12의 `restoreQuoteLine`이 현재 차수가 아닌 차수의 줄 복원과 승인 차수의 합계를 바꾸는 복원을 거부한다(OV-2 · B-01과 같은 결)"
    - "승인 표시를 끄면(연결 문서 없을 때 — D-56) 같은 차수의 수량·단가 편집이 다시 된다 — 잠금은 저장된 승인 상태에서만 파생되고 따로 저장되지 않는다"
    - "`tableLockLine`이 승인된 현재 차수에 `{n}차 고객 승인됨 · 고치려면 새 차수`를 돌려주고, 정산·완료 문구가 그보다 이긴다(화면 렌더는 04-24)"
    - "이 플랜이 더하는 거부(현재 차수 아님 · 승인 차수 잠김 · 과거 차수 복원)는 화면 거부와 함께 서버 로그 경고 `write.denied`를 **정확히 한 번**(보는 사람·규칙·프로젝트·차수·줄 id, 금액 없음) 남긴다 — 04-12의 `saveQuoteLines`·`restoreQuoteLine` 한 거부 지점을 지나고 두 번째 `log.warn`을 만들지 않는다. 프로젝트-차수 불일치의 경고는 04-22가 남기고 이 플랜은 그것이 한 번인지만 단언한다(사용자 D19-3, CEO 리뷰 B-26)"
  prohibitions:
    - statement: "승인된 차수의 견적 합계가 그 승인 표시를 그대로 둔 채 바뀌어서는 안 된다. 라벨은 「{n}차 고객 승인 합계」인데 값이 조용히 움직이면 PM과 경영관리가 서로 다른 계약 금액을 믿게 된다 — 고치려면 새 차수다(D-84 · 사용자 D7). 오래된 탭·위조 페이로드가 과거 차수나 남의 줄에 쓰는 것도 같은 실패다(B-01)"
      status: resolved
      verification: test
  artifacts:
    - path: "test/integration/quote-revision-races.test.ts"
      provides: "두 연결 결정적 경합 — 새 차수 대 저장(양 순서) · 승인 대 저장(양 순서) · 풀 크기 단언"
      min_lines: 90
    - path: "test/integration/quote-approved-lock.test.ts"
      provides: "승인 차수에서 칸 잠금·삽입·취소·보관·복원 거부와 실행가 허용 · 승인 합계 불변 · 승인 해제 뒤 편집 재개"
      min_lines: 60
    - path: "domain/quotes/edit-scope.ts"
      provides: "셀 단계·구조·표 위 한 줄에 승인 차수 축(`approvedSeq`)"
      exports: ["lineCellEditability", "structuralEditability", "tableLockLine"]
  key_links:
    - from: "domain/quotes/lines.ts"
      to: "repositories/quote-revisions.ts"
      via: "`saveQuoteLines`가 `loadProjectForGate`로 잠근 뒤 같은 트랜잭션에서 최신 차수를 다시 읽어 보낸 차수와 비교한다"
      pattern: "findLatestQuoteRevision"
    - from: "test/integration/quote-revision-races.test.ts"
      to: "test/integration/lock-race.ts"
      via: "경합 순서를 04-20의 `deferred`·`waitForLockWaiter`와 쓰기 함수의 `deps.afterLock`으로 강제한다 — 고정 지연이 없다"
      pattern: "waitForLockWaiter"
    - from: "domain/rules/register.ts"
      to: "domain/quotes/edit-scope.ts"
      via: "`project.line-edit`가 승인 차수 축을 싣고 같은 판정 함수를 부른다"
      pattern: "approvedSeq"
---

<objective>
**As a** 기획 PM과 경영관리, **I want to** 한 프로젝트의 견적 저장·새 차수·고객 승인이 서로 덮어쓰지 않고, 오래된 탭이 지난 차수에 쓰지 못하며, 고객이 승인한 차수의 견적 합계는 새 차수 없이 바뀌지 않기를, **so that** 「현재 차수가 승인됐는가」와 「계약 금액 = 승인 합계」가 동시성·위조 아래에서도 거짓이 되지 않는다.

**2026-09-23 CEO 리뷰 신설 플랜(04-14 분할 — T2의 04-14 몫 · T4 · OV-1 · OV-3 · B-01 · B-07).** 그룹 A가 잠금의 뼈대를 먼저 놓았다 — 04-20의 `lockProjectForWrite`(행 배타 잠금)와 경합 도우미 `test/integration/lock-race.ts`, 04-11의 `loadProjectForGate`(잠금 + 같은 트랜잭션 안 자동 정산), 04-12의 줄 저장 잠금·`revision_id` 줄 소속·`restoreQuoteLine`, 04-22의 프로젝트-차수 일치. 04-14가 새 차수·승인에 같은 `loadProjectForGate`를 걸었다. 이 플랜은 **그 위에 빠진 것만** 더한다: 줄 저장의 현재 차수 재확인(B-01 — 과거 차수로의 저장), 차수가 둘 이상일 때만 드러나는 소속·일치 경우의 증명, 두 연결 결정적 경합 테스트(OV-3 — `Promise.all`류는 두 호출이 직렬화되면 흔들린다), 그리고 승인 차수의 견적 칸 잠금과 합계를 바꾸는 삽입·취소·보관·복원 거부(사용자 D7 · OV-1). 새 잠금 도우미·새 소속 검사·새 로그 호출 지점을 만들지 않는다.

Purpose: Phase 5(지출결의 게이트)·Phase 9(손익)가 「현재 차수 = 최신 순번」과 「승인 차수 = 굳은 합계」 하나를 전제한다(D-54). 이 전제가 오래된 탭 하나로 깨지면 뒤 페이즈가 조용한 데이터 손상을 물려받는다(CEO 리뷰 「12개월 이상 대비 위치」).

Output: 줄 저장의 현재 차수 재확인 · 승인 차수 축(셀·구조·표 위 한 줄·게이트·복원) · 두 연결 경합 통합 1 · 승인 잠금 통합 1 · 여러 차수 소속 통합 확장 · 결정표 단위 확장.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`(경합 테스트가 멈추거나 흔들리면 특히), 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개. 화면 파일은 고치지 않는다 — 잠금 이유의 렌더는 04-24.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-14-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 38–40행(OV-1·OV-2·OV-3) · B-01·B-07 절(Grep `### B-01`·`### B-07`) · 부록 B 「1b. State」 도식(Grep `## 1b. State`)
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 336–339행(Copywriting Error — 셀 잠김·읽기 전용) · 1116–1136행(상태별 셀 단계)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(차수·고객 승인 — 동시성·승인 차수 불변)** 과 **PROJ-02(견적 줄 저장 — 현재 차수·위조 차단)** 다. 수용 기준은 D-53·D-54·D-56·D-84와 사용자 D7, CEO 리뷰 B-01·B-07·OV-1·OV-2·OV-3에서 끌어왔다. 화면 쪽(승인 차수 표 위 한 줄 · 새 줄의 잠긴 견적 칸 · 오류 이유)은 04-24가 이 플랜의 판정 함수로 그린다 — 이 플랜의 truths는 서버 계약만이다.

**계획 단계 가정(표면화):**
- **잠금은 새로 만들지 않는다:** 행 배타 잠금은 04-20의 `lockProjectForWrite`, 잠금 + 선판정은 04-11의 `loadProjectForGate(viewer, id, { now, tx, afterLock })`가 정본이다. 줄 저장(04-12)·새 차수와 승인(04-14)·합성 저장(04-22)이 이미 트랜잭션 첫 단계에서 그것을 부른다. 자동 정산 선판정은 그 함수가 **잠금 안 같은 트랜잭션에서** 한다(A-13의 「자기 잠금 대기」는 풀 연결로 UPDATE할 때의 문제였고, 04-11이 같은 tx로 옮겨 풀었다) — 이 플랜은 트랜잭션 밖 선판정을 두지 않는다. SUMMARY에서 세 쓰기 함수가 모두 `loadProjectForGate`를 부르는지 확인하고, 빠진 곳이 있으면 이 플랜이 그 한 줄만 더한다(도우미를 새로 만들지 않는다).
- **경합 테스트 주입 지점:** 그룹 A 규약대로 쓰기 함수의 `deps.afterLock?: () => Promise<void>`(잠금 획득 직후 — `loadProjectForGate`가 부른다)와 04-20의 `deferred()`·`waitForLockWaiter(pool, { timeoutMs })`를 쓴다. 새 도우미를 만들지 않는다.
- **줄 소속·일치는 이미 있다:** `revision_id` 조건과 「찾은 수 ≠ 보낸 수 → 전부 거부」는 04-12(B-01 줄 소속), 프로젝트-차수 일치는 04-22(A-14)다. 이 플랜은 그 검사가 **차수가 둘 이상인 프로젝트**(04-14 뒤에야 만들 수 있다)에서도 맞다는 테스트만 더한다. 경고 로그도 이미 있다 — 줄 소속 거부는 04-12의 한 지점, 불일치 거부는 04-22(`차수와 프로젝트가 맞지 않습니다 · 새로 고쳐 주세요`)가 남긴다. 이 플랜은 「정확히 한 번」을 단언하고 새 `log.warn`을 더하지 않는다.
- **승인 차수의 새 줄:** 사용자 D7은 「견적 칸 잠금」이고 OV-1은 「합계를 바꾸는 삽입 거부」다. 새 `quote` 줄 자체는 허용하되 04-12의 정산 새 줄과 같은 칸 집합(`QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT` — 수량·단가·상태)을 잠근다(사용자 D12가 정산 새 줄에 고른 모양과 같다) — 게이트는 04-12의 `insert.quoteCellsZero`가 거짓인 새 줄만 거부한다. 견적 외 비용·조정 줄은 견적가가 늘 0이라 영향이 없다.
- **상태 칸:** `quote` 줄의 취소(견적가 0)와 취소 해제는 둘 다 합계를 바꾸므로 승인 차수에서 상태 칸도 잠근다 — 연결 문서가 있는 줄을 거둬야 하면 새 차수다(D-56 「고치려면 새 차수」).
- **복원과 과거 차수:** 저장이 과거 차수로 쓰지 못하므로(B-01) 보관함 복원도 현재 차수가 아닌 차수의 줄을 되살리지 않는다. 조정 줄은 04-14(B-21)가 보관된 것까지 전부 현재 차수로 옮기므로 이 규칙에 걸리지 않는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 새 차수가 잠금을 쥔 동안 온 옛 차수 저장이 기다렸다가 거부된다(잠금 뒤 현재 차수 재확인 → 한 거부 지점 → 두 연결 결정적 경합 통합)</name>
  <files>domain/quotes/lines.ts, domain/quotes/revisions.ts, test/integration/quote-revision-races.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md`·`04-14-SUMMARY.md` — `saveQuoteLines`·`createRevisionFromCurrent`·`setCustomerApproval`가 트랜잭션 첫 단계에서 `loadProjectForGate`를 부르는지, `deps.afterLock`을 넘기는지, `saveQuoteLines`의 한 거부 지점(`write.denied`)의 실제 이름
    - `domain/projects/auto-transition.ts`(04-11) — `loadProjectForGate(viewer, id, { now, tx, afterLock })`
    - `domain/quotes/lines.ts`의 `saveQuoteLines`(04-12·04-13·04-14 뒤 형태 — 잠금 자리 · 차수로 프로젝트 id를 찾는 자리 · 거부 지점)
    - `repositories/quote-revisions.ts`(04-14 뒤 — `findLatestQuoteRevision`의 `tx` 인자)
    - `test/integration/lock-race.ts`(04-20 — `deferred`·`waitForLockWaiter`) · `test/integration/project-status.test.ts`의 경합 describe(04-20 — 풀 크기 확인과 `afterLock`에 `deferred`를 거는 형태)
    - `db/client.ts`(`pool` — `max`는 `DB_POOL_MAX`)
  </read_first>
  <behavior>
    - 풀 최대 커넥션이 3 이상이다(두 트랜잭션 + 관찰 연결 — 아니면 아래 증명이 가짜로 통과한다)
    - (A) 새 차수(`createRevisionFromCurrent`의 `deps.afterLock`에 `deferred`)가 잠금을 쥐고 멈춘 사이 두 번째 연결의 1차 저장(실행가 변경)이 시작된다 → `waitForLockWaiter`로 그 연결이 락 대기임을 확인 → 풀면 새 차수가 커밋되고 저장은 `다른 사람이 새 차수를 만들었습니다 · 새로 고쳐 주세요`로 거부 → DB: 1차 줄 그대로, 2차 줄은 저장 전 값
    - (B) 저장(`saveQuoteLines`의 `deps.afterLock`)이 잠금을 쥐고 멈춘 사이 새 차수(`fromRevisionId` = 1차)가 락 대기 → 저장 커밋 뒤 새 차수가 진행되어 2차에 **저장된** 실행가가 복사된다
    - 경합 없이 새 차수 뒤 1차로 저장 → 같은 거부 + `write.denied` 경고 한 번(규칙 `quote.current-revision`, 금액 키 없음)
  </behavior>
  <action>
① 경합 통합 테스트(`behavior` 넷)를 먼저 쓴다(RED). `test/integration/quote-revision-races.test.ts`(신규): 04-20의 `deferred`·`waitForLockWaiter`만 쓰고 순서를 **훅과 락 대기 확인**으로 강제한다 — 시간 경과(`setTimeout` 고정 지연)나 여러 요청을 한꺼번에 던지는 형태를 쓰지 않는다. 결과는 DB 재조회로 단언하고 `write.denied`는 `vi.spyOn(log, "warn")`으로 본다.

② **현재 차수 재확인(B-01).** `saveQuoteLines`가 `loadProjectForGate`로 잠근 직후, 같은 `tx`로 `findLatestQuoteRevision(viewer, projectId, tx)`를 읽어 보낸 `revisionId`가 최신이 아니면 **아무것도 쓰지 않고** `다른 사람이 새 차수를 만들었습니다 · 새로 고쳐 주세요`로 전부 거부한다(총계 행 거부 이유 — 04-04의 표시 경로 그대로). 거부는 04-12가 만든 `saveQuoteLines`의 한 거부 지점을 지나 `write.denied`(규칙 `quote.current-revision` · 프로젝트·차수 id, 금액 없음)를 남긴다 — 새 `log.warn` 호출 지점을 만들지 않는다. 04-12·04-14 SUMMARY에서 세 쓰기 함수(`saveQuoteLines`·`createRevisionFromCurrent`·`setCustomerApproval`)의 잠금이나 `deps.afterLock` 전달이 빠진 곳을 찾으면 그 함수에 `loadProjectForGate` 호출·`afterLock` 전달 한 줄만 더하고 SUMMARY에 적는다(`domain/quotes/revisions.ts`는 이 경우에만 고친다 — 다 있으면 확인만).

③ GREEN 뒤 `quote-revisions.test.ts`·`quote-lines.test.ts`(04-12·04-14)가 그대로 초록인지 돌린다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-revision-races.test.ts test/integration/quote-revisions.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나오거나 테스트가 타임아웃으로 끝난다 — 두 번째 연결이 기다리지 않았거나(잠금 누락), 잠금 뒤 현재 차수 재확인이 빠져 옛 차수 저장이 통과했거나, 선판정이 풀 연결에서 자기 잠금을 기다려 멈췄다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `saveQuoteLines`의 현재 차수 판정이 `loadProjectForGate` 뒤 같은 트랜잭션의 읽기다
    - 이 플랜이 잠금 도우미·경합 도우미를 새로 만들지 않았다(`lock-race.ts`·`lockProjectForWrite`·`loadProjectForGate` 재사용 — SUMMARY에 명시)
    - 경합 테스트가 풀 크기(≥ 3)를 먼저 단언하고, 두 번째 연결이 실제로 락 대기였음을 `waitForLockWaiter`로 확인한 뒤 결과를 단언한다(양 순서) · `setTimeout` 고정 지연이 순서를 정하지 않는다
    - 옛 차수 저장 거부 뒤 DB 재조회로 무변경을 단언하고 `write.denied`가 한 번이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>새 차수와 줄 저장이 프로젝트 행 잠금으로 한 줄로 서고, 새 차수 뒤에 온 옛 차수 저장이 결정적 경합 테스트에서 거부된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 여러 차수에서의 소속·일치 증명 — 같은 프로젝트 다른 차수 줄 · 완료 프로젝트 줄 · 프로젝트-차수 불일치(매출 포함) + 경고 로그 한 번 단언</name>
  <files>domain/quotes/lines.ts, test/integration/quote-lines.test.ts</files>
  <read_first>
    - `domain/projects/ledger.ts`(04-12·04-22 뒤 — 읽기만) — 04-22의 프로젝트-차수 일치 검사(A-14)·거부 문구 `차수와 프로젝트가 맞지 않습니다 · 새로 고쳐 주세요`·그 경고 로그
    - `domain/quotes/lines.ts` — 04-12의 `findQuoteLinesByIds`(`revisionId` 조건)·「찾은 수 ≠ 보낸 수」 거부와 거부 문구
    - `test/integration/quote-lines.test.ts`(전문 — 04-12의 (f) 다른 프로젝트 줄 케이스와 준비 형태) · `test/integration/project-period.test.ts`의 통합 i(04-22 — 불일치 케이스 형태)
  </read_first>
  <behavior>
    - 1·2차가 있는 프로젝트: 2차(현재) 저장 페이로드에 1차 줄 id 하나 → 04-12의 줄 소속 문구로 배치 전부 거부, DB 무변경(두 차수 모두)
    - 진행 프로젝트 저장에 다른 **완료** 프로젝트의 줄 id를 섞음 → 같은 거부 — 완료 잠금을 남의 줄로 비켜 가지 못한다
    - `saveProjectLedger(projectId = A, quoteLines.revisionId = B의 현재 차수, revenue 발행 줄 하나)` → `차수와 프로젝트가 맞지 않습니다 · 새로 고쳐 주세요`(04-22)로 거부, A·B의 견적 줄과 A의 매출 둘 다 무변경
    - 세 거부 각각 `write.denied` 경고가 **정확히 한 번**(금액 키 없음) — 두 번째 로그 호출이 없다
  </behavior>
  <action>
① `test/integration/quote-lines.test.ts`에 describe `여러 차수 소속·일치(04-40 · B-01 · A-14)`로 `behavior` 넷을 먼저 쓴다(RED 또는 곧바로 GREEN — 04-12·04-22 검사가 이미 맞으면 테스트만 남는다. 그 사실을 SUMMARY에 적는다). 준비는 04-14의 `createRevisionFromCurrent`로 2차를 만든다.

② **경고 로그는 단언만(사용자 D19-3 · B-26).** 줄 소속 거부는 04-12의 한 거부 지점, 불일치 거부는 04-22가 `write.denied`를 남긴다 — 이 태스크는 `vi.spyOn(log, "warn")`으로 세 거부 각각 「정확히 한 번 · 금액 키 없음」을 단언하고 새 `log.warn`을 더하지 않는다.

③ 검사 자체가 틀렸으면(예: 줄 소속 조건이 조회에만 있고 버전 조건 수정에는 없음) `systematic-debugging`으로 원인을 찾아 `domain/quotes/lines.ts`의 그 자리만 고치고 SUMMARY에 적는다 — 04-22의 불일치 검사(`domain/projects/ledger.ts`)가 틀렸거나 로그가 없으면 고치지 말고 멈춰 SUMMARY·오케스트레이터에 알린다(이 플랜의 파일이 아니다).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-lines.test.ts test/integration/quote-lines-conflict.test.ts test/integration/project-period.test.ts test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 같은 프로젝트 다른 차수·다른 프로젝트 줄 id가 통과했거나, 프로젝트-차수 불일치에서 매출이 쓰였거나, 경고 로그가 없거나 두 번이거나 금액을 실었다</fails_when>
  </verify>
  <acceptance_criteria>
    - 같은 프로젝트 다른 차수 줄 · 완료 프로젝트 줄이 섞인 배치가 전부 거부되고 DB 재조회로 무변경이 단언된다(차수 둘인 준비)
    - 프로젝트-차수 불일치에서 견적 줄·매출 둘 다 무변경이다(통합)
    - 각 거부에서 `write.denied`가 정확히 한 번, 필드에 금액 없음 · 이 플랜이 `log.warn` 호출 지점을 새로 만들지 않았다
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>차수가 여럿인 프로젝트에서도 위조 페이로드가 남의 차수·남의 줄·남의 프로젝트에 닿지 못한다는 것이 증명됐고, 거부가 운영 로그에 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 승인 차수 잠금(사용자 D7 · OV-1) — 셀·구조·표 위 한 줄·게이트·복원 + 승인 대 저장 결정적 경합 + 합계 불변</name>
  <files>domain/quotes/edit-scope.ts, domain/rules/register.ts, domain/quotes/lines.ts, test/unit/domain/quote-edit-scope.test.ts, test/unit/domain/rules-gate.test.ts, test/integration/quote-approved-lock.test.ts, test/integration/quote-revision-races.test.ts</files>
  <read_first>
    - `domain/quotes/edit-scope.ts`(04-12·04-13·04-14 뒤 전문) — `lineCellEditability` 입력 객체·우선순위, `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT`(정산 새 줄 잠금 칸 — 사용자 D12), `tableLockLine`
    - `domain/rules/register.ts`의 `project.line-edit`(04-12 — `change` 유니언: `update.fields` · `insert.quoteCellsZero` · `archive` · `restore.quoteAmountZero` · 줄 종류 ctx — 04-13)
    - `domain/quotes/lines.ts` — 줄 취소(견적가 0 — 04-12)·보관 판정 · `restoreQuoteLine`(04-12 — 보관함 복원의 도메인 함수, `DOMAIN_RESTORERS`) · `listQuoteLines`의 셀 단계 계산
    - `domain/quotes/revisions.ts`의 `setCustomerApproval`(04-14 — `deps.afterLock`)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 336–339행(Copywriting 셀 잠김 문구 형식)
    - `test/unit/domain/quote-edit-scope.test.ts`·`test/unit/domain/rules-gate.test.ts`(현재 결정표)
  </read_first>
  <behavior>
    - `lineCellEditability({ status: 진행, lineKind: quote, approvedSeq: 2, canWrite: true, … })` → 수량·단가·상태 `locked`, 실행가·항목·거래처·비고 `edit` · 같은 입력 + `approvedSeq: null` → 전부 `edit` · 정산 + 승인 → 정산 규칙 그대로(실행가만) · 같은 입력 + `isNewLine: true` → 수량·단가·상태 `locked`
    - 같은 입력의 `lineKind: out_of_quote`·`adjustment` → 승인과 무관(04-13 규칙 그대로)
    - `tableLockLine({ status: 진행, approvedSeq: 2 })` → `2차 고객 승인됨 · 고치려면 새 차수` · 정산·완료 → 그 상태 문구
    - 게이트 `project.line-edit`: 승인 + `update [quantity]` → `2차 고객 승인됨 · 고치려면 새 차수` · 승인 + `update [execution]` → 통과 · 승인 + `insert`(`quoteCellsZero: false`) → 거부 · 승인 + `insert`(`quoteCellsZero: true`) → 통과 · 승인 + `archive`(`quoteAmountZero: false`) → 거부 · 승인 + `archive`(`quoteAmountZero: true`) → 통과 · 승인 + `restore`(`quoteAmountZero: false`) → 거부
    - 통합(승인된 2차): 수량 변경 거부 · 실행가 변경 통과 · 단가 100,000 새 줄 거부 · 단가 0 새 줄 통과(수량 1·단가 0 저장) · 견적가 있는 줄 취소 거부 · 보관 거부 · 승인 전에 보관해 둔 견적가 있는 줄의 보관함 복원(`domain/archive`의 `restore` → `restoreQuoteLine`) 거부 · 1차(현재 아님) 줄 복원 거부 · 견적 외 비용·조정 새 줄 통과 · **모든 거부 뒤 2차 견적 합계(SUM, 보관 제외) = 승인 직전 합계** · 승인 표시를 끄면 수량 변경 통과
    - 경합: (C) 승인(`setCustomerApproval`의 `deps.afterLock`)이 잠금을 쥔 사이 수량 저장이 락 대기 → 승인 커밋 → 저장 거부(승인 문구) · 실행가만 바꾼 저장이면 통과 · (D) 저장이 잠금을 쥔 사이 승인이 락 대기 → 저장 커밋 → 승인 진행, 승인된 합계 = 저장 뒤 합계
  </behavior>
  <action>
① 결정표 단위(`behavior` 앞 넷)를 `test/unit/domain/quote-edit-scope.test.ts`·`test/unit/domain/rules-gate.test.ts`에 먼저 쓴다(RED).

② **판정(사용자 D7 · B-07 A).** `edit-scope.ts`의 판정 입력 객체에 `approvedSeq: number | null`(현재 차수가 승인됐으면 그 순번)을 더한다 — `quote` 종류 줄의 수량·단가·상태를 `locked`로(정산·완료·연결 문서 규칙이 이미 잠근 칸은 그 이유가 이긴다: 완료 > 정산 > 연결 문서 > 승인), 새 `quote` 줄(`isNewLine`)은 04-12의 정산 새 줄과 같은 칸 집합 `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT`를 잠근다(사용자 D12와 같은 모양 — 새 상수를 만들지 않는다). `tableLockLine`은 정산·완료가 아니고 승인이면 `{n}차 고객 승인됨 · 고치려면 새 차수`. 잠김 이유 문자열은 이 파일과 게이트에만 있다.

③ **게이트(OV-1).** `project.line-edit` ctx에 `approvedSeq`를 싣고 04-12의 `change` 유니언을 그대로 쓴다 — `update`는 바뀐 칸이 승인 잠금 칸이면 거부, `insert`는 04-12의 `quoteCellsZero`가 거짓이면 거부, `archive`에는 그 줄의 서버 계산 견적가로 만든 `quoteAmountZero`를 더해 거짓이면 거부, `restore`는 04-12의 `quoteAmountZero`가 거짓이면 거부(취소·취소 해제는 상태 칸 `update`로 막힌다). 이유는 모두 `{n}차 고객 승인됨 · 고치려면 새 차수`.

④ **저장·목록·복원.** `saveQuoteLines`가 잠긴 트랜잭션 안에서 읽은 현재 차수(Task 1)의 승인 여부로 `approvedSeq`를 정해 모든 줄 게이트와 보관 판정에 싣는다(거부는 04-12의 한 거부 지점 — 규칙 `project.line-edit`). `listQuoteLines`가 같은 값으로 `cellEditability`를 계산해 화면이 잠긴 칸을 받는다. 04-12의 `restoreQuoteLine`에 두 조건을 더한다 — 잠금 뒤 같은 tx로 읽은 최신 차수가 그 줄의 차수가 아니면 `이전 차수의 줄은 복원할 수 없습니다 · 현재 차수에서 새로 만들어 주세요`, 현재 차수가 승인됐으면 `approvedSeq`를 게이트에 실어 견적가가 있는 줄 복원을 승인 문구로 거부(OV-2). 두 거부 모두 `restoreQuoteLine`의 기존 `write.denied` 지점을 지난다.

⑤ **통합.** `test/integration/quote-approved-lock.test.ts`(신규)에 `behavior` 통합 줄 — 승인은 04-14의 `setCustomerApproval`로 켜고, 복원은 보관함 경로(`domain/archive`의 `restore`)로 부르며, 합계 불변은 각 거부 뒤 DB의 `quote_amount_krw` 합(보관 제외)으로 단언한다. `test/integration/quote-revision-races.test.ts`에 경합 (C)·(D)를 Task 1과 같은 방식(`setCustomerApproval`·`saveQuoteLines`의 `deps.afterLock` + `waitForLockWaiter`)으로 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-edit-scope.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 승인 차수 축의 셀·새 줄·표 위 한 줄·게이트 결정표가 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/quote-approved-lock.test.ts test/integration/quote-revision-races.test.ts test/integration/quote-line-kinds.test.ts test/integration/quote-revisions.test.ts test/integration/archive.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 타임아웃이다 — 승인 차수에서 합계를 바꾸는 조작이 통과했거나, 실행가·견적 외 비용·조정이 잘못 막혔거나, 복원이 과거 차수·승인 합계 판정을 건너뛰었거나, 승인 대 저장 경합에서 잃어버린 수정이 생겼다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 판정 입력 변경이 표 흐름을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 결정표가 `behavior` 앞 넷을 덮고 이유 우선순위(완료 > 정산 > 연결 문서 > 승인) 케이스가 있다
    - 승인 차수의 새 줄 잠금이 04-12의 `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT`를 재사용한다(새 잠금 칸 상수 없음)
    - 승인 차수에서 수량·단가·상태 변경 · 견적가 있는 새 줄 · 견적가 있는 줄의 보관·취소 · 복원이 전부 거부되고, 각 거부 뒤 승인 차수 견적 합계가 그대로다(통합 DB 합)
    - 실행가 변경 · 견적가 0 새 줄 · 견적 외 비용·조정 새 줄은 통과한다
    - 승인 표시를 끄면 같은 수량 변경이 통과한다
    - 과거 차수 줄의 보관함 복원이 `restoreQuoteLine`에서 거부된다(OV-2 · B-01)
    - 경합 (C)·(D)가 락 대기 확인 뒤 결과를 단언한다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>고객이 승인한 현재 차수의 견적 합계가 어떤 쓰기·복원·경합으로도 새 차수 없이 바뀌지 않고, 실행가는 계속 고칠 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(`projectId`·`revisionId`·줄 id) | 오래된 탭·위조 요청이 과거 차수·남의 줄·남의 프로젝트를 가리킬 수 있다 |
| 동시 요청 → 한 프로젝트 | 저장·새 차수·승인이 겹치면 판정과 쓰기 사이에 상태가 바뀐다 |
| 보관함 → 견적 줄 복원 | 관리자 화면의 복원이 줄 저장 규칙을 우회할 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-201 | Tampering | 과거 차수로의 저장 | high | mitigate | `loadProjectForGate` 뒤 같은 tx로 다시 읽은 최신 차수와 보낸 `revisionId`를 비교해 다르면 전부 거부한다(B-01). 결정적 경합 테스트와 순차 테스트로 고정 |
| T-04-202 | Elevation of Privilege | 남의 줄 id로 잠금 우회 | high | mitigate | 04-12의 `revision_id` 조건(조회·버전 조건 수정)과 「찾은 수 ≠ 보낸 수 → 전부 거부」가 막는다 — 이 플랜은 차수가 둘인 프로젝트와 완료 프로젝트 줄로 그것을 증명한다(B-01) |
| T-04-203 | Tampering | 프로젝트-차수 불일치 | medium | mitigate | 04-22의 일치 검사(와 경고 로그)가 견적 줄·매출 어느 쪽도 쓰지 않게 막고, 이 플랜이 차수 둘 준비로 증명한다(B-01 · A-14) |
| T-04-204 | Tampering | 동시 저장·새 차수·승인(잃어버린 수정 · TOCTOU) | high | mitigate | 세 쓰기가 `loadProjectForGate`(04-11 · 04-20 배타 잠금) 뒤에 판정·쓰기를 한다(OV-3). 두 연결 결정적 경합(양 순서 × 새 차수·승인)을 `waitForLockWaiter`로 고정한다 |
| T-04-205 | Tampering | 승인 차수 합계 드리프트 | high | mitigate | 승인 차수에서 견적 칸 잠금 + 합계를 바꾸는 삽입·취소·보관·복원 거부(사용자 D7 · OV-1). 각 거부 뒤 DB 합계 불변 단언. 계약 금액 쪽 불변 단언은 04-16 |
| T-04-206 | Elevation of Privilege | 보관함 복원 우회 | medium | mitigate | 04-12의 `restoreQuoteLine`(같은 tx · 같은 잠금)이 현재 차수 여부와 승인 합계를 본다(OV-2) |
| T-04-207 | Repudiation | 거부된 쓰기 | low | mitigate | 모든 거부가 `write.denied` 경고(보는 사람·규칙·대상 id, 금액 없음)를 정확히 한 번 남긴다 — 줄 저장·복원은 04-12의 한 지점, 불일치는 04-22. 이 플랜은 호출 지점을 늘리지 않고 스파이로 「한 번」을 단언한다(사용자 D19-3 · B-26) |
| T-04-208 | Denial of Service | 행 잠금 대기 | low | accept | 잠금은 한 프로젝트 행이고 트랜잭션은 줄 300개 상한 안의 짧은 쓰기다(10~30명 규모). 선판정이 잠금 안 같은 tx라 자기 잠금 대기가 없다(04-11 — A-13) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- `CI=true pnpm test` 세 계층 통과
- 경합 테스트가 풀 크기와 락 대기를 먼저 확인하고 양 순서를 단언한다
- `package.json`·`pnpm-lock.yaml` diff 0줄
</verification>

<success_criteria>
- 한 프로젝트의 저장·새 차수·승인이 한 줄로 서고, 오래된 탭·위조 요청이 과거 차수·남의 줄·남의 프로젝트에 쓰지 못한다 — 여러 차수 준비와 두 연결 경합으로 증명됐다
- 고객이 승인한 현재 차수의 견적 합계가 새 차수 없이는 바뀌지 않고 실행가는 계속 고칠 수 있다
- 04-24가 승인 차수 잠금을 판정 함수의 결과만으로 그리고, 04-16이 「계약 금액 = 승인 합계」를 불변식으로 단언할 수 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 재사용(새로 만들지 않음) | `lockProjectForWrite`(04-20) · `loadProjectForGate`(04-11) · `test/integration/lock-race.ts`의 `deferred`·`waitForLockWaiter`(04-20) · 04-12의 `revision_id` 줄 소속 · 04-22의 프로젝트-차수 일치 · 04-12의 `restoreQuoteLine`·한 거부 지점 · `QUOTE_FIELDS_LOCKED_IN_SETTLING_INSERT` |
| 변경된 판정 | `lineCellEditability`·`structuralEditability`(새 줄 견적 칸)·`tableLockLine`에 `approvedSeq` 축 · `project.line-edit` ctx(`approvedSeq` · `archive.quoteAmountZero`) |
| 변경된 기존 심볼 | `saveQuoteLines`(잠금 뒤 현재 차수 재확인 · 승인 축) · `restoreQuoteLine`(현재 차수·승인 합계) · `listQuoteLines`(승인 잠금 셀 단계) |
| 거부 문구(신규) | `다른 사람이 새 차수를 만들었습니다 · 새로 고쳐 주세요` · `{n}차 고객 승인됨 · 고치려면 새 차수` · `이전 차수의 줄은 복원할 수 없습니다 · 현재 차수에서 새로 만들어 주세요` |
| 로그 이벤트 | `write.denied`(경고 — 04-12의 한 지점을 지나는 규칙 `quote.current-revision`·`project.line-edit`, 금액 없음 · 새 호출 지점 없음) |
| 테스트 | `test/integration/quote-revision-races.test.ts` · `test/integration/quote-approved-lock.test.ts`(신규) · `quote-lines.test.ts`(여러 차수 소속·일치) · `quote-edit-scope.test.ts` · `rules-gate.test.ts` 확장 |
| 새 스키마·마이그레이션·액션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-01 — 과거 차수 저장 · 남의 줄 id · 프로젝트-차수 불일치 미검사 | CRITICAL | Plan 04-40, Task 1 ②(잠금 뒤 현재 차수 재확인) · Task 2(여러 차수 소속·일치 증명 — 검사 자체는 Plan 04-12 줄 소속 · Plan 04-22 A-14) |
| OV-3 · T2(04-14 몫) — 공유 잠금으로는 경합을 못 막음, 두 연결 결정적 테스트 | HIGH | 잠금은 Plan 04-14(`loadProjectForGate`) · Plan 04-12 · Plan 04-20/04-11 뼈대. Plan 04-40, Task 1 ① · Task 3 ⑤: 락 대기 확인 경합(새 차수·승인 × 양 순서) |
| B-07 · 사용자 D7 · OV-1 (T4) — 승인 뒤 견적 칸 편집·합계 드리프트 | HIGH | Plan 04-40, Task 3: 셀·새 줄·게이트·복원 + 합계 불변 통합(계약 금액 쪽은 Plan 04-16) |
| OV-2 — 보관함 복원 우회(승인 합계·과거 차수) | HIGH | Plan 04-40, Task 3 ④: `restoreQuoteLine`(Plan 04-12)에 현재 차수·승인 합계 |
| 사용자 D19-3 · B-26 — 거부 로그 | DECISION | Plan 04-40, Task 1 ② · Task 3 ④: 04-12의 한 거부 지점 재사용 · Task 2 ②: 04-22 불일치 로그 포함 「정확히 한 번」 단언 |
| A-13 — 선판정 자리(풀 연결이면 자기 잠금 대기) | WARNING | Plan 04-11 `loadProjectForGate`가 잠금 안 같은 tx에서 판정 — 이 플랜은 트랜잭션 밖 선판정을 두지 않는다(`<probe_fallback>`) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 승인 차수 잠금의 화면 렌더(표 위 한 줄·오류 이유·새 줄 잠긴 칸) | 화면 파일은 04-24가 고친다 — 이 플랜은 판정 함수만 만든다 |
| A-06·A-36(줄 저장 대 상태 전환 · 상한 경합) | 04-12·04-26의 몫(그룹 A) — 이 플랜은 같은 잠금을 쓰고 새로 만들지 않는다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-40-SUMMARY.md` when done
</output>
