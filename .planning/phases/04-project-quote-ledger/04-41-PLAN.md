---
phase: 04-project-quote-ledger
plan: 41
type: execute
wave: 21
depends_on: ["04-16"]
files_modified:
  - domain/revenue/index.ts
  - domain/projects/index.ts
  - domain/projects/ledger.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - repositories/projects.ts
  - repositories/revenue-entries.ts
  - db/schema/projects.ts
  - db/migrations/0015_drop_project_contract_columns.sql
  - db/migrations/meta/_journal.json
  - db/migrations/meta/0015_snapshot.json
  - test/integration/revenue-entries.test.ts
  - test/integration/migration-upgrade.test.ts
  - docs/design/DECISIONS.md
autonomous: true
requirements: [PROJ-03]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "계약 금액을 따로 저장하는 길이 화면·액션·도메인·리포지토리·DB 어디에도 없다 — `saveProjectLedgerAction`에 옛 `revenue.contract`를 실어 보내면 zod가 필드를 버리고 아무것도 저장되지 않으며, 계약 금액은 04-16의 파생값(고객 승인된 현재 차수 합계) 하나뿐이다(D-84, 옛 D-57 「계약 금액 PM 쓰기」 폐기)"
    - "`projects.contract_*` 네 컬럼이 새 마이그레이션으로 제거되고 `app/`·`domain/`·`repositories/`·`db/schema/`에 그 컬럼·쓰기 함수 이름이 0건이다 — 0010은 고쳐 쓰지 않는다(D-84, 사용자 D9 「지금 삭제 + 가드」)"
    - "삭제 마이그레이션은 DROP 전에 업무 데이터(`source <> 'demo'`) 중 계약 원화 금액이 0이 아닌 행을 세고 한 건이라도 있으면 RAISE로 멈춘다 — 이번에 적용할 마이그레이션 전부가 한 트랜잭션이라 그 DB는 한 칸도 바뀌지 않는다(사용자 D9 「값 있으면 RAISE」, CEO 리뷰 B-09 A — 0009 가드 선례)"
    - "`ban-drop-column`은 `.squawk.toml`이 아니라 각 DROP 문장 바로 위 인라인 무시 주석과 이유 한 줄로만 비켜 가고 `pnpm lint:sql`이 0건이다(CEO 리뷰 B-09 — squawk 2.65 실측)"
    - "0010까지만 적용한 DB에 옛 데이터(계약 값이 있는 demo 행 · 계약 값 0인 업무 행 · 견적 줄)를 넣고 나머지 마이그레이션을 적용하면, **시드를 다시 돌리기 전에** 네 컬럼이 없고 프로젝트 행 수·번호·줄 수가 그대로다. 계약 값이 있는 업무 행이 하나라도 있으면 적용이 가드 문구로 실패하고 그 DB의 컬럼·값·앞 마이그레이션 결과가 전부 그대로다(CEO 리뷰 OV-6 — 시드만 검증하던 빈틈)"
    - "매출 줄 수정은 `id`·`version`·`project_id`·`kind`가 모두 맞을 때만 쓰인다 — 프로젝트 A의 저장이 프로젝트 B의 줄을 고치거나, 발행 표로 입금 줄 id를 흘려 입금 금액을 바꾸는 길이 없고, 그런 요청은 `줄을 찾을 수 없음 · 새로 고침`로 거부되며 `write.denied`가 한 번 남는다(Codex #1 — 지금 `repositories/revenue-entries.ts` 91행은 `id`·`version`만 본다)"
    - "새 발행·입금 줄은 화면이 만든 UUID를 `id`로 싣고, 서버는 같은 `id`가 이미 있으면 넣지 않는다(`ON CONFLICT (id) DO NOTHING`) — 커밋은 됐는데 응답을 잃어 같은 배치를 다시 보내도 한 행이고, 같은 `id`에 다른 값이 오거나 남의 줄 id가 새 줄로 오면 `이미 저장된 줄과 값이 다름 · 새로 고침`로 거부된다(엔지니어링 리뷰 ENG-D10 · Codex #3)"
    - "매출 줄 금액은 04-40의 `normalizeMoneyInput`을 지난다 — KRW 환율은 서버가 1로 고정하고, USD 환율 0 이하·원화 환산 범위 밖은 PG 오류가 아니라 한국어 이유로 배치 전부 거부된다(엔지니어링 리뷰 B §2)"
    - "매출 저장은 잠긴 트랜잭션 안에서 전역 db(풀)를 부르지 않는다 — 권한은 트랜잭션 앞에서 계산해 넘기고, 최근 환율 기억은 04-12의 교차 그룹 규약대로 쓰기 단계가 돌려준 `fxToRemember`를 트랜잭션을 연 쪽이 커밋 뒤 한 번 기억하며(거부된 배치는 설정을 바꾸지 않고, 기억 실패는 저장을 실패시키지 않은 채 `fx.remember_failed` 한 줄을 남긴다), 행동 로그는 그 `tx`로 쓴다(04-32 규칙, 엔지니어링 리뷰 B §1 · A §1 유령 로그)"
    - "`docs/design/DECISIONS.md`에 ARCHITECTURE §5 「확장 전용(DROP 없음)」 원칙의 예외 한 건과 릴리스 제약 「Phase 4는 PR #38 한 번에 배포한다 — 중간 커밋·중간 플랜 상태를 배포하지 않는다」, 그 제약이 필요한 이유(0010 ADD와 이 DROP이 한 배포에서 상쇄되고 옛 리비전인 Phase 3 코드는 `projects`를 읽지 않는다), 코드 롤백 때 쓸 역 SQL(네 컬럼을 0010 정의로 다시 더하기)이 기록된다(사용자 D9 전제, CEO 리뷰 OV-6·B-09)"
  artifacts:
    - path: "db/migrations/0015_drop_project_contract_columns.sql"
      provides: "RAISE 가드 + `projects.contract_*` 네 컬럼 DROP(인라인 squawk 무시 + 이유) — 04-13의 0014 뒤"
      contains: "RAISE EXCEPTION"
    - path: "test/integration/migration-upgrade.test.ts"
      provides: "0010 상태 옛 데이터 위 업그레이드(재시드 전 검사) · 계약 값 있는 업무 행에서 가드 실패와 전체 무변경 — 04-06이 만든 파일에 케이스를 더한다"
      contains: "drop_project_contract_columns"
    - path: "docs/design/DECISIONS.md"
      provides: "ARCHITECTURE §5 예외 · 「Phase 4는 PR #38 한 번에 배포」 릴리스 제약 · 역 SQL"
      contains: "PR #38"
    - path: "repositories/revenue-entries.ts"
      provides: "매출 줄 수정의 프로젝트·종류 조건 · 클라이언트 id 삽입(`ON CONFLICT (id) DO NOTHING`)"
      contains: "onConflictDoNothing"
  key_links:
    - from: "domain/revenue/index.ts"
      to: "repositories/revenue-entries.ts"
      via: "`saveEntries`가 수정에 `projectId`·`kind`를 넘기고, 새 줄은 클라이언트 id로 넣되 이미 있으면 재전송 판정을 한다"
      pattern: "updateRevenueEntryIfVersionMatches"
    - from: "domain/revenue/index.ts"
      to: "domain/money/index.ts"
      via: "매출 줄 금액이 `normalizeMoneyInput`을 지난다(04-40 소유)"
      pattern: "normalizeMoneyInput"
    - from: "db/migrations/0015_drop_project_contract_columns.sql"
      to: "db/migrations/0009_project_quote_ledger_spine.sql"
      via: "0009의 RAISE 가드 형태(DO 블록 · 세기 · 한국어 이유)를 그대로 따른다"
      pattern: "RAISE EXCEPTION"
    - from: "test/integration/migration-upgrade.test.ts"
      to: "db/migrations/meta/_journal.json"
      via: "04-06의 `migrateTo(pool, count)`로 0010까지 적용한 임시 DB에 옛 데이터를 넣고 나머지를 적용한다 — 번호는 파일명 접미사로 찾는다"
      pattern: "migrateTo"
    - from: "app/(app)/projects/actions.ts"
      to: "domain/revenue/index.ts"
      via: "일괄 저장 스키마와 `SaveRevenueInput`에 계약 필드가 없다 — 옛 필드는 zod가 버린다"
      pattern: "saveRevenue"
---

<objective>
**As a** 경영관리와 운영 담당자, **I want to** 계약 금액을 따로 적는 옛 저장 경로와 컬럼이 코드·DB에서 완전히 사라지되, 실제 계약 값이 들어 있는 DB에서는 삭제가 멈추고 배포 제약이 기록되기를, **so that** 계약 금액이 「고객 승인된 현재 차수 합계」 하나로만 존재하고, 컬럼 삭제가 데이터를 조용히 잃거나 중간 배포에서 500을 내는 일이 없다.

**2026-09-23 CEO 리뷰 신설 플랜(04-16 분할 — T14 · 사용자 D9 · OV-6 · B-09).** 옛 04-16(17파일)의 Task 2(계약 쓰기 경로 제거 + 컬럼 삭제 마이그레이션)를 이 플랜으로 옮겼다. 04-16이 계약 금액의 **읽는 쪽**(파생 · 화면 입력 제거 · 불변식)을 끝냈고, 이 플랜이 **서버 쓰기 경로와 컬럼**을 지운다. 사용자 D9 「지금 삭제 + 가드」가 더한 것: DROP 전 RAISE 가드(값이 있으면 멈춤) · ARCHITECTURE §5 예외 기록 · 「Phase 4는 PR #38 한 번에 배포」 전제. OV-6이 더한 것: 재시드된 빈 DB가 아니라 **0010 상태의 옛 데이터 위에** 남은 마이그레이션을 적용하고 재시드 전에 보는 업그레이드 테스트(04-06이 만든 `test/integration/migration-upgrade.test.ts`에 케이스를 더한다).

Purpose: 0010 ADD와 이 DROP은 한 배포 안에서 상쇄된다 — `origin/main`의 마이그레이션은 0008까지라 이 컬럼은 어느 공유 DB에도 간 적이 없다(CEO 리뷰 시스템 감사). 그 안전은 「Phase 4를 한 번에 배포」가 지켜질 때만 성립하므로 전제를 문서로 남기고, 전제가 깨진 DB(실제 계약 값이 있는 DB)에서는 마이그레이션이 스스로 멈추게 한다.

Output: 계약 쓰기 경로 제거(액션 스키마 · `SaveRevenueInput` · `saveRevenue` 분기 · `updateProjectContract` · `insertProject`·`createProject` 기본값) · 컬럼 삭제 마이그레이션(가드 + 인라인 무시) · 업그레이드 테스트 두 케이스 · 참조 스캔 · DECISIONS 기록 한 건(마이그레이션과 같은 커밋) · 매출 쓰기 경로 굳히기(프로젝트·종류 조건 · 새 줄 클라이언트 UUID · 금액 입력 정규화 · 커밋 뒤 환율 기억 · 잠금 안 권한 조회 제거).

**2026-09-23 엔지니어링 리뷰 반영(`docs/designs/plant8-erp-phase4-eng-review-260923.md` — 결정 번호는 「ENG-D*」로 적어 CEO 리뷰의 「사용자 D*」와 구별한다).** 이 플랜이 매출(발행·입금) 쓰기 경로를 소유하므로 그 경로의 지적을 여기서 받는다 — **Codex #1**(수정 조건에 `project_id`·`kind`) · **ENG-D10**(새 줄 클라이언트 UUID · 재전송 한 행 — 견적 줄 쪽은 다른 그룹 플랜) · **B §2**(04-40의 `normalizeMoneyInput`) · **B §1·A §1**(잠금 안 전역 db 호출 금지 — 권한은 트랜잭션 앞, 최근 환율은 커밋 뒤, 행동 로그는 `tx`). 세 태스크 한도를 지키려고 옛 Task 3(DECISIONS 기록)을 Task 2(마이그레이션)에 합쳤다 — 옛 계획도 두 일을 같은 커밋에 두라고 했다. 새 Task 3이 매출 쓰기 경로 굳히기와 전체 게이트다. 낡은 주석(`domain/revenue/index.ts` 287–290행 「계약 금액은 기존 "projects" write(PM)」 · `domain/projects/ledger.ts` 6–8행 「매출 섹션(계약 금액·발행 줄·입금 줄)」)은 계약 경로를 지우는 Task 1이 고친다. **ENG-D11:** Phase 4 안에서는 앞 플랜 파일의 결함을 회귀 테스트와 SUMMARY 편차 기록과 함께 고칠 수 있다(범위는 늘리지 않는다).

**마이그레이션 번호:** 0015다 — 확정된 순서 0011 04-10 → 0012 04-06 → 0013 04-27 `role_work_scope` → 0014 04-13 `quote_line_kind` → **0015 이 플랜** → 0016 04-07 `reserve_entries`. 생성기 번호(직전 최고 + 1)가 다르면 생성기 출력 그대로 두고 실제 이름을 SUMMARY에 적는다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`(특히 업그레이드 테스트의 임시 DB·마이그레이터가 이상하게 굴 때), 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-16-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-06-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 27행(B-09 · 사용자 D9) · 43행(OV-6) · B-09 절(Grep `### B-09`) · 부록 B 「## 9. Deployment」 도식(Grep `## 9. Deployment`)
# docs/ARCHITECTURE.md 168–176행(§5 DB·마이그레이션 — 「확장 전용(컬럼 추가만, DROP 없음)」)
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 763–786행(§4 D-84 제거 범위)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-03(계약 금액 — 쓰기 경로·컬럼 제거)** 이다. 수용 기준은 D-84와 사용자 D9, CEO 리뷰 B-09·OV-6에서 끌어왔다. 화면·파생·불변식은 04-16의 truths다.

**계획 단계 가정(표면화):**
- **가드 조건:** 사용자 D9는 「값 있으면 RAISE」다. 그런데 0010이 더한 네 컬럼 중 셋(`contract_currency`·`contract_fx_rate`·`contract_amount_krw`)은 NOT NULL 기본값이 있어 「널이 아님」을 문자 그대로 쓰면 모든 행에서 가드가 발동한다. 그래서 「값이 있다」를 CEO 리뷰 B-09 A의 조건 그대로 **업무 데이터(`source <> 'demo'`)의 `contract_amount_krw <> 0`** 으로 읽는다 — demo 행은 이관 전 자료라 파생값으로 대체되고(Eng OV-1), 로컬 개발 DB에 04-02 때 적어 둔 demo 계약 값이 있어도 마이그레이션이 막히지 않는다. 외화 계약도 원화 환산액이 0이 아니므로 같은 조건에 걸린다.
- **기록 위치:** 이 저장소의 결정 기록 문서는 `docs/design/DECISIONS.md` 하나다(다른 결정 문서 없음 — 확인함). 사용자 D9 답이 「ARCHITECTURE §5 예외를 DECISIONS에 기록」이라 그 파일에 한 항목을 더한다. 그룹 A 플랜(04-06 A-28 롤백 자세 · 04-17 C-26 배포 묶음)도 「Phase 4는 한 PR로 배포」 기록을 이 플랜에 맡겼다 — 이 DECISIONS 항목이 그 릴리스 제약의 **정본 하나**이고, 0012(04-06)의 전진 전용·04-17/04-18 중간 상태도 같은 제약 아래라는 한 줄을 본문에 둔다. `docs/ARCHITECTURE.md` 본문은 고치지 않는다(사용자 D9 답이 「DECISIONS에 기록」).
- **역 SQL:** 이 마이그레이션은 전진 전용이다(되돌리는 마이그레이션 파일을 만들지 않는다). 코드를 Phase 4 중간 리비전으로 되돌려야 하는 사고 때 쓸 역 SQL — 네 컬럼을 0010의 정의(`contract_currency text DEFAULT 'KRW' NOT NULL` · `contract_foreign_amount numeric(14,2)` · `contract_fx_rate numeric(12,4) DEFAULT '1.0000' NOT NULL` · `contract_amount_krw integer DEFAULT 0 NOT NULL`)로 다시 더하기 — 를 DECISIONS 항목의 「되돌리는 법」에 적는다(CEO 리뷰 B-09 롤백 도식 「reverse migration re-adding 4 columns — prepare it」). 값은 돌아오지 않는다(파생값이 정본).
- **완료 계약 게이트는 없다(A-12):** 04-12는 완료 프로젝트의 계약 저장을 막는 중간 게이트를 만들지 않는다 — 이 플랜이 곧 지울 코드를 세웠다 지우는 일이라서다(Simplicity First). Phase 4는 PR #38 한 번에 배포되므로 04-12~04-41 사이의 상태는 어느 환경에도 가지 않는다. A-12(완료 계약 편집)는 이 플랜이 계약 입력 경로를 통째로 지우는 **끝 상태**로 충족된다 — 지울 고아 게이트 갈래가 없다.
- **`custom-fields.test.ts`:** 옛 계획은 이 파일의 「계약 참조」를 고친다고 적었지만, 그 파일의 `contractNote`는 커스텀 필드 키 이름이지 계약 컬럼이 아니다(확인함) — 고치지 않고 회귀로만 돌린다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 옛 계약 필드를 실은 일괄 저장이 아무것도 저장하지 않는다(액션 스키마 → 원장 합성 저장 → 매출 도메인 → 리포지토리 → 통합)</name>
  <files>app/(app)/projects/actions.ts, domain/projects/ledger.ts, domain/revenue/index.ts, domain/projects/index.ts, repositories/projects.ts, test/integration/revenue-entries.test.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-16-SUMMARY.md` — 04-16이 남긴 옛 쓰기 분기(「컬럼에만 쓰고 아무 화면도 읽지 않는다」)의 실제 자리
    - `app/(app)/projects/actions.ts` — `saveProjectLedgerAction`의 `revenue` 스키마(`contract`·`contractFxRateTouched` — 지금 100–101행)
    - `domain/projects/ledger.ts`(전문 — 04-12·04-22·04-40 뒤 형태) — 계약 필드 전달
    - `domain/revenue/index.ts` — `SaveRevenueInput`·`saveRevenue`의 계약 분기(Grep `contract`)
    - `repositories/projects.ts` — `updateProjectContract`·`ProjectContractUpdateInput`·`insertProject` 입력의 계약 네 칸(지금 236–298행)
    - `domain/projects/index.ts` — `createProject`의 계약 기본값(지금 274–277행)
    - `test/integration/revenue-entries.test.ts`(전문 — 04-16이 파생 케이스를 더한 뒤)
  </read_first>
  <behavior>
    - `saveProjectLedger`(액션 스키마를 지난 입력)에 `revenue.contract = { currency: "KRW", amount: 50000000, fxRate: 1 }`과 발행 줄 하나를 실으면 발행 줄만 저장되고, 프로젝트 행의 계약 컬럼은 저장 전 값(기본값) 그대로이며, `listRevenue().contract`는 04-16의 파생값 그대로다(컬럼 자체는 Task 2가 지운다)
    - 액션 zod 스키마의 파싱 결과에 `contract`·`contractFxRateTouched` 키가 없다(옛 필드는 버려진다 — 거부가 아니라 무시)
    - 참조 스캔: `app/`·`domain/`·`repositories/`·`db/schema/`의 `.ts`·`.tsx`에 `contract_amount_krw`·`contract_currency`·`contract_fx_rate`·`contract_foreign_amount`·`contractAmountKrw`·`contractCurrency`·`contractFxRate`·`contractForeignAmount`·`updateProjectContract`·`contractFxRateTouched`가 0건(마이그레이션 폴더는 보지 않는다)
  </behavior>
  <action>
① 통합 테스트(`behavior` 셋 — 스캔은 `revenue-entries.test.ts` 안 describe 하나 `계약 금액 쓰기 경로 없음(04-41)`, `node:fs`로 네 폴더를 훑는다)를 먼저 쓴다(RED). 04-16이 이 파일에 남긴 「옛 필드는 스키마가 무시」 자리를 이 describe로 채운다.

② **쓰기 경로 제거(D-84).** `saveProjectLedgerAction`의 `revenue` 스키마에서 `contract`·`contractFxRateTouched`를 지운다(zod 객체는 모르는 키를 버리므로 옛 클라이언트가 보내도 거부가 아니라 무시다). `SaveRevenueInput`에서 같은 두 필드를, `saveRevenue`에서 계약 분기를 지운다. `domain/projects/ledger.ts`가 그 필드를 넘기던 곳을 지운다. `repositories/projects.ts`의 `updateProjectContract`·`ProjectContractUpdateInput`과 `insertProject` 입력의 계약 네 칸, `domain/projects/index.ts` `createProject`의 계약 기본값을 지운다 — 컬럼은 Task 2가 지우므로 이 순간에는 DB 기본값이 채운다. **낡은 주석(엔지니어링 리뷰):** `saveRevenue` 머리 주석(지금 287–290행 — 「계약 금액은 기존 "projects" write(PM), 발행·입금 줄은 새 "projects.revenue" write」)을 발행·입금 줄 권한 하나로, `domain/projects/ledger.ts` 머리 주석(6–8행 — 「매출 섹션(계약 금액·발행 줄·입금 줄)」)에서 계약 금액을 뺀다.

③ `pnpm lint && pnpm typecheck`로 남은 참조를 찾아 고친다(GREEN).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc error가 나온다 — 지운 계약 필드·함수를 아직 누가 부른다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts test/integration/project-copy.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 옛 계약 필드가 어딘가에 저장됐거나, 참조 스캔이 남은 이름을 찾았거나, 원장 합성 저장·등록·복사 경로가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `saveProjectLedgerAction` 스키마와 `SaveRevenueInput`에 계약 필드가 없고, 옛 필드를 실은 저장이 발행 줄만 저장한다(통합)
    - `updateProjectContract`·`ProjectContractUpdateInput`이 없고 `insertProject`·`createProject`에 계약 칸이 없다
    - 참조 스캔 describe가 초록이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>계약 금액을 저장하는 코드 경로가 액션부터 리포지토리까지 없고, 옛 필드를 보내도 아무것도 저장되지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [BLOCKING] 컬럼 삭제 마이그레이션 — RAISE 가드 · 인라인 squawk 무시 · 0010 상태 업그레이드 테스트(OV-6) + ARCHITECTURE §5 예외 · 「Phase 4는 PR #38 한 번에 배포」 · 역 SQL 기록(사용자 D9 — 같은 커밋)</name>
  <files>db/schema/projects.ts, db/migrations/0015_drop_project_contract_columns.sql, db/migrations/meta/_journal.json, db/migrations/meta/0015_snapshot.json, test/integration/migration-upgrade.test.ts, docs/design/DECISIONS.md</files>
  <reversibility rating="costly">컬럼 삭제는 값을 지운다. 지금은 공유 DB 어디에도 이 컬럼이 간 적이 없고(main = 0008), 업무 값이 있으면 가드가 멈추며, 값은 파생값으로 대체된다 — 되돌리려면 DECISIONS의 역 SQL(네 컬럼 다시 더하기)과 입력 경로 복원이 필요하다. 배포 뒤라면 2/5(CEO 리뷰 §10).</reversibility>
  <read_first>
    - `db/schema/projects.ts`(전문) · `db/schema/money-columns.ts`(전문)
    - `db/migrations/0010_revenue_entries.sql` 1–29행(락 타임아웃 한 쌍 · 네 컬럼 정의 — **고쳐 쓰지 않는다**)
    - `db/migrations/0009_project_quote_ledger_spine.sql` 91–124행(DO 블록 RAISE 가드 선례 — 세기 · 한국어 이유 · 「조용히 건너뛰지 않는다」)
    - `db/migrations/meta/_journal.json` — 마지막 항목의 `idx`·`tag`(이 플랜의 번호 = 그 `idx` + 1)
    - `.squawk.toml`(전문) — 제외 규칙 넷. **고치지 않는다**
    - `test/integration/migration-upgrade.test.ts`(04-06 산출물 전문) — `createScratchDb()`·`migrateTo(pool, count)`·번호를 파일명 접미사로 찾는 형태 · 04-06의 케이스 (a)(b)
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` B-09 절(Grep `### B-09`) · 11행(시스템 감사 — main = 0008) · 부록 B 「## 9. Deployment」 두 도식
    - `docs/design/DECISIONS.md` 481–525행(04-01 기록 형식 — `## 날짜 — 제목` · **결정** · **왜** · **버린 대안** · **범위** · `---`) · `docs/ARCHITECTURE.md` 168–192행(§5 원칙 · §6 배포 파이프라인 — migrate Job이 새 리비전보다 먼저 돈다 · rollback.sh는 트래픽만 되돌린다)
    - `test/unit/design-system-docs.test.ts` — DECISIONS 머리글 개수를 세는 describe들의 정규식(04-08의 `Phase 4(04-08)` 12건 단언이 이 항목을 세지 않는지 확인)
  </read_first>
  <behavior>
    - (c) 0010까지 적용한 임시 DB에 demo 프로젝트 하나(계약 원화 5,000,000 · 견적 줄 둘)와 업무 프로젝트 하나(`source = 'intranet'`, 계약 원화 0 · 견적 줄 하나)를 넣고 나머지 마이그레이션 전체 적용 → **시드를 부르지 않고**: `information_schema.columns`에 `projects.contract_%`가 0개 · 프로젝트 2행 · 두 행의 번호 그대로 · 견적 줄 3행 그대로
    - (d) 같은 준비에서 업무 프로젝트의 계약 원화를 5,000,000으로 두고 전체 적용 → 오류(메시지에 가드 문구 `계약 금액`), 그 DB의 `projects.contract_amount_krw`가 그대로 5,000,000이고 이번 적용의 앞 마이그레이션 결과도 없다(예: 04-13의 `quote_lines.line_kind` 컬럼 부재 — 모두 한 트랜잭션)
    - 로컬 DB `pnpm db:migrate` 통과 · `pnpm lint:sql` 0건
  </behavior>
  <action>
① 업그레이드 케이스 (c)(d)를 `test/integration/migration-upgrade.test.ts`에 describe `계약 칸 삭제(04-41 · D9 · OV-6)`로 먼저 쓴다(RED) — 04-06의 `createScratchDb()`·`migrateTo(pool, count)`를 그대로 쓰고, 0010의 `count`는 `_revenue_entries` 접미사 파일의 `idx` + 1로 찾는다(번호를 문자열로 박지 않는다). 옛 데이터는 0010 스키마의 NOT NULL 칸(번호·클라이언트·팀·옛 상태 값 등 — 04-06 (a)의 준비 SQL을 따른다)을 채운 SQL로 넣는다. 시드 함수를 부르지 않는다.

② **[BLOCKING] 스키마·생성.** `db/schema/projects.ts`에서 `...moneyColumns("contract")`와 그 주석을 지우고 `pnpm db:generate --name drop_project_contract_columns`. **번호 규칙:** 생성기가 매기는 번호 = 적용 직전 journal 최고 `idx` + 1 — 04-13의 `0014_quote_line_kind` 뒤라 0015다(다르면 파일명·snapshot·journal tag를 생성기 출력 그대로 두고 실제 이름을 SUMMARY에 적는다 — 아래 번호 검증 명령이 본다).

③ **생성 SQL 손 편집(끝 상태는 스냅숏과 같다).** 생성된 네 `DROP COLUMN` 문장을 읽어 확인한 뒤: (a) 첫 줄 앞에 머리 주석(Phase 4(04-41) · 사용자 D9 · ARCHITECTURE §5 예외 — DECISIONS 기록 참조) + `SET LOCAL lock_timeout = '1s';`·`SET LOCAL statement_timeout = '5s';`(0010 선례). (b) DROP보다 앞에 0009 선례 형태의 `DO $$ … $$` 가드 — `SELECT count(*) … FROM "projects" WHERE "source" <> 'demo' AND "contract_amount_krw" <> 0`이 0보다 크면 `RAISE EXCEPTION '업무 데이터 %건에 계약 금액이 있습니다 — 파생 계약 금액(고객 승인된 현재 차수 합계)으로 옮길지 먼저 정해야 해 컬럼을 지우지 않고 마이그레이션을 중단합니다 (04-41, 사용자 D9)'`. (c) 각 `DROP COLUMN` 문장 바로 위 줄에 `-- squawk-ignore ban-drop-column` 과 그 위에 이유 한 줄(`-- 사용자 D9: 0010 ADD와 한 배포에서 상쇄 — 공유 DB에 간 적 없음, 업무 값은 위 가드가 막는다`). `.squawk.toml`은 고치지 않는다. 0010은 고치지 않는다.

④ `pnpm lint:sql` → `bash scripts/dev-db.sh && pnpm db:migrate`(로컬 DB에 업무 계약 값이 있으면 가드가 멈춘다 — 그 경우 로컬 DB를 `scripts/reset-test-db.sh`류로 다시 만들고 SUMMARY에 적는다. 가드를 약하게 고치지 않는다) → 업그레이드 테스트(GREEN).

⑤ **결정 기록(사용자 D9 · OV-6 — 옛 Task 3을 여기로 합쳤다: CI 경로 필터가 `docs/**`만 바뀐 커밋을 건너뛰므로(04-08 기록) 이 기록은 마이그레이션과 같은 커밋이어야 한다).** `docs/design/DECISIONS.md` 끝에 04-01 기록과 같은 형식으로 한 항목을 더한다. 머리글 `## 2026-09-23 — Phase 4(04-41) ARCHITECTURE §5 예외: projects.contract_* DROP · 「Phase 4는 PR #38 한 번에 배포」 (사용자 D9 · CEO 리뷰 B-09·OV-6)`. **결정**: 0010이 더한 네 컬럼을 이 플랜의 마이그레이션이 지운다 — ARCHITECTURE §5 「확장 전용(DROP 없음)」의 예외 한 건. 전제(릴리스 제약): Phase 4는 PR #38 한 번으로 배포하고 중간 커밋·중간 플랜 상태를 어느 환경에도 배포하지 않는다 — 배포 워크플로가 main 밖 ref를 거부하는 강제 장치는 엔지니어링 리뷰 ENG-D12(다른 그룹 플랜)다. **왜**: 0010 ADD와 이 DROP이 한 배포 안에서 상쇄되고(main = 0008 — 공유 DB에 간 적 없음), 롤백 대상인 Phase 3 코드는 `projects`를 읽지 않는다 · 중간 배포라면 04-02 코드가 도는 리비전이 지운 컬럼을 읽어 500을 낸다 · 업무 값이 있는 DB는 가드가 멈춘다. **버린 대안**: 코드만 지우고 DROP은 다음 확장-축소 릴리스로(B-09 B) · 컬럼 유지(B-09 C) — 둘 다 「어느 쪽이 계약 금액인가」를 다시 열어 둔다. **되돌리는 법**: 전진 전용 — 코드 롤백이 Phase 4 중간 리비전을 가리키면 먼저 역 SQL(네 컬럼을 0010 정의로 다시 더하기 — `<probe_fallback>` 문구 그대로)을 적용한다. 값은 돌아오지 않는다(파생값이 정본). 같은 제약 아래 있는 것 한 줄: 0012 상태 재매핑(04-06 — 전진 전용)과 목록 합계·열의 중간 상태(04-17·04-18 — C-26)도 한 번에 배포해야 무해하다. **범위**: `db/migrations/0015_drop_project_contract_columns.sql` · 배포 절차(한 번에 배포). 이 항목은 SYSTEM.md를 바꾸지 않는다.
  </action>
  <verify>
    <automated>pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 `Found` 뒤 0이 아닌 개수가 나온다 — 인라인 무시가 DROP 문장 바로 위에 없거나 락 타임아웃 한 쌍이 빠졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; git diff --stat d6b41cf -- db/migrations/0010_revenue_entries.sql .squawk.toml</automated>
    <fails_when>db:migrate가 0이 아닌 코드로 끝나거나(`error:` 줄), diff 출력이 비어 있지 않다 — 0010을 고쳐 썼거나 린트를 피하려고 `.squawk.toml`을 고쳤다</fails_when>
    <automated>node -e "const fs=require('fs'),p=n=>String(n).padStart(4,'0'),e=require('./db/migrations/meta/_journal.json').entries,l=e[e.length-1],gap=e.filter((x,i)=>x.idx!==i||!x.tag.startsWith(p(i)+'_')).map(x=>x.tag),n=fs.readdirSync('db/migrations').filter(f=>f.endsWith('.sql')).length,c=[gap.length===0,n===e.length,l.tag.endsWith('_drop_project_contract_columns'),fs.existsSync('db/migrations/'+l.tag+'.sql'),fs.existsSync('db/migrations/meta/'+p(l.idx)+'_snapshot.json')];if(c.includes(false)){console.error('MIGRATION NUMBERING: last='+l.tag+' gaps='+gap+' sql='+n+' journal='+e.length+' checks='+c);process.exit(1)}console.log('ok '+l.tag)"</automated>
    <fails_when>표준 오류에 `MIGRATION NUMBERING:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — 새 마이그레이션 번호가 직전 최고 번호 + 1이 아니거나, journal에 빈 번호가 있거나, 파일명·snapshot·journal tag가 생성기 출력과 어긋난다</fails_when>
    <automated>pnpm vitest run --project integration test/integration/migration-upgrade.test.ts test/integration/revenue-entries.test.ts test/integration/custom-fields.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 테스트가 타임아웃으로 끝난다 — 옛 데이터 위 삭제가 행·줄을 잃었거나, 업무 계약 값이 있는데 가드가 멈추지 않았거나, 가드 실패 뒤 DB가 일부만 바뀌었거나, 컬럼 제거가 목록·커스텀 필드 경로를 깨뜨렸다</fails_when>
    <automated>grep -c '^## 2026-09-23 — Phase 4(04-41) ARCHITECTURE §5 예외' docs/design/DECISIONS.md</automated>
    <fails_when>출력이 1이 아니다 — 기록 머리글이 없거나 두 번 들어갔다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 새 DECISIONS 항목이 다른 플랜의 머리글 개수 단언을 깨뜨렸다(정규식이 날짜만 세서 깨지면 ENG-D11에 따라 그 정규식을 이 기록을 세지 않게 고치고 회귀 케이스 + SUMMARY 편차로 적는다 — 이 기록을 지우지 않는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션이 락 타임아웃 한 쌍 → DO 블록 가드(`source <> 'demo' AND contract_amount_krw <> 0`) → 네 `DROP COLUMN` 순서이고, 각 DROP 바로 위에 `-- squawk-ignore ban-drop-column`과 이유 줄이 있다
    - `pnpm lint:sql` 0건 · 0010과 `.squawk.toml` diff 0줄
    - 번호 검증 명령이 초록이다 — 계획 번호 0015와 다르면 실제 이름이 SUMMARY에 있다
    - 업그레이드 (c)가 시드를 부르지 않고 컬럼 부재·행 보존·줄 보존을 단언한다 · (d)가 가드 실패 뒤 계약 값·앞 마이그레이션 결과까지 무변경을 단언한다 · 임시 DB가 남지 않는다
    - DECISIONS에 04-41 머리글이 정확히 한 줄이고 본문에 `PR #38`·「중간」 배포 금지·역 SQL(네 컬럼 정의)·가드 조건·같은 제약 아래의 04-06(0012)·04-17/04-18(C-26) 한 줄·강제 장치 ENG-D12 참조가 있다 · 마이그레이션과 같은 커밋이다
    - 기록이 SYSTEM.md를 바꾸지 않는다(`git diff --stat -- docs/design/SYSTEM.md` 빈 출력)
    - 여섯 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>업무 계약 값이 없는 DB에서는 네 컬럼이 사라지고 기존 행이 보존되며, 값이 있는 DB에서는 마이그레이션이 한 칸도 바꾸지 않고 멈춘다 — 옛 데이터 위 업그레이드로 증명됐고, 컬럼 삭제가 ARCHITECTURE §5의 기록된 예외로 그 전제(한 번에 배포)·되돌리는 법과 함께 같은 커밋에 남았다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 매출(발행·입금) 쓰기 경로 굳히기 — 행의 프로젝트·종류 조건(Codex #1) · 새 줄 클라이언트 UUID와 재전송(ENG-D10) · 금액 입력 정규화(`normalizeMoneyInput`) · 커밋 뒤 환율 기억 · 잠금 안 권한 조회 제거 + 전체 게이트</name>
  <files>repositories/revenue-entries.ts, domain/revenue/index.ts, domain/projects/ledger.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/quote-table.tsx, test/integration/revenue-entries.test.ts</files>
  <read_first>
    - `repositories/revenue-entries.ts`(전문 94행 — `insertRevenueEntry` · `updateRevenueEntryIfVersionMatches` 91행의 `WHERE id AND version`만 · `findRevenueEntryById`)
    - `domain/revenue/index.ts` — `saveEntries`(지금 246–285행 — 줄마다 `moneyToColumns` · `rememberFxRate`가 트랜잭션 안 · 버전 충돌 문구) · `saveRevenue`(권한 `can()` · `tx`가 있을 때의 `recordAction` 호출)
    - `domain/projects/ledger.ts`(04-12·04-22·Task 1 뒤) — 합성 저장의 `withTransaction`·잠금 자리 · 권한을 트랜잭션 앞에서 계산하는 자리(04-12)
    - `.planning/phases/04-project-quote-ledger/04-12-PLAN.md` `<probe_fallback>` 「커밋 뒤 최근 환율 기억(교차 그룹 규약)」(Grep `fxToRemember`) — 쓰기 단계가 `fxToRemember: { currency, rate }[]`를 돌려주고 트랜잭션을 연 쪽이 커밋 뒤 `rememberFxRate`, 실패는 `log.error("fx.remember_failed", { currency })`
    - `.planning/phases/04-project-quote-ledger/04-12-SUMMARY.md` — 견적 줄에 건 ENG-D10 새 줄 id 모양(필드 이름 · 재전송 판정), `saveProjectLedger`가 견적 줄 `fxToRemember`를 커밋 뒤 기억하는 실제 자리, 원장 합성 저장의 `recordAction(…, { tx })` 여부 · `04-20-SUMMARY.md`(`denyWrite(viewer, rule, ids, err)` — `domain/rules/deny-write.ts`) · `04-32-SUMMARY.md`(「잠금 안 전역 db 호출 금지」 · `withTransaction`의 `lock_timeout`) · `04-40-SUMMARY.md`(`normalizeMoneyInput`·`MoneyInputError`)
    - `app/(app)/projects/actions.ts` 44–51행(`revenueEntryRowSchema` — `id` 선택) · `app/(app)/projects/[id]/quote-table.tsx`의 `newEntryDraft`(지금 161–169행 — `clientKey`만 있고 `id` 없음)와 저장 페이로드(지금 526–545행)
    - `docs/designs/plant8-erp-phase4-eng-review-260923.md` R9(ENG-D10 — Grep `### R9`) · 「Codex」 절 #1
  </read_first>
  <behavior>
    - 행 범위(Codex #1): 프로젝트 A의 저장에 프로젝트 B 발행 줄의 `id`·맞는 `version`을 실음 → 거부 `줄을 찾을 수 없음 · 새로 고침`, B의 줄 무변경, `write.denied` 한 번(규칙 `revenue.entry-scope`, 금액 없음) · A의 `issuedEntries`에 A의 **입금** 줄 id를 실음 → 같은 거부, 입금 줄 무변경 · 같은 프로젝트·같은 종류에서 버전만 다름 → 기존 버전 충돌 문구 그대로
    - 재전송(ENG-D10): 새 발행 줄(`isNew: true`, 클라이언트 UUID `X`)을 저장 → 커밋 → 응답을 잃었다고 보고 **같은 페이로드**를 다시 저장 → 성공 · DB에 `X` 한 행 · 두 번째 저장이 행동 로그를 더 남기지 않는다(쓴 줄이 없으면 로그 없음) · 같은 `X`에 금액을 바꿔 다시 보냄 → 거부 `이미 저장된 줄과 값이 다름 · 새로 고침`, DB 값은 첫 저장 그대로 · 다른 프로젝트(또는 다른 종류)의 줄 id를 새 줄 `id`로 보냄 → 같은 거부, 그 줄 무변경 · **보관된 줄의 재전송(I1):** 같은 프로젝트·같은 종류·같은 값으로 저장된 줄 `Y`를 준비에서 보관(SQL로 `archived_at`·`archived_by` 채움)한 뒤 `Y`를 새 줄 `id`(`isNew: true`, 같은 값)로 보냄 → 같은 거부(`이미 저장된 줄과 값이 다름 · 새로 고침`), `write.denied` 한 번(규칙 `revenue.replay-mismatch`, 금액 없음), `Y`는 보관 상태·값·`version` 그대로 — 재전송 no-op 성공으로 넘어가거나 보관이 풀리지 않는다 · 원장 합성 저장(`saveProjectLedger`)으로 같은 재전송을 해도 한 행
    - 금액 입력: KRW 발행 줄에 환율 1,350을 실은 요청 → 원화 = 금액 × 1, 환율 1로 저장 · USD 환율 0 → 한국어 이유로 거부, 배치 전부 무변경 · 원화 환산 범위 밖 → 같은 거부(PG 22003 아님)
    - 커밋 뒤 환율 기억(04-12 규약): 환율을 고친 USD 발행 줄(1,380) 뒤에 거부되는 줄이 있는 배치 → 전부 거부 · `fx.recent_rate.USD` 무변경 · 같은 USD 줄 하나만의 배치 → 커밋 뒤 1380 · `saveProjectLedger`로 환율을 고친 USD 발행 줄(1,380)과 견적 줄 하나를 함께 저장 → 커밋 뒤 1380(합성 저장이 매출 `fxToRemember`를 견적 줄 목록과 합쳐 기억한다) · 같은 합성 배치가 견적 줄 거부로 끝나면 무변경 · `saveRevenue`의 `deps.rememberFxRate`가 던지게 주입 → 저장은 성공(줄 저장됨, 결과 정상) · `log.error("fx.remember_failed")` 한 번(`currency`만, 금액 없음)
    - 권한 판정 위치: `saveRevenue`에 `tx`와 `deps.rights`(트랜잭션 앞에서 계산한 값)를 주면 `deps.can` 스파이가 0회 불린다 — 잠긴 트랜잭션 안에서 권한을 조회하지 않는다 · `rights.canWriteEntries: false`면 기존 `ForbiddenError` 그대로
  </behavior>
  <action>
① 통합(`behavior` 전부)을 `test/integration/revenue-entries.test.ts`에 describe `매출 쓰기 경로(04-41 · Codex #1 · ENG-D10)`로 먼저 쓴다(RED).

② **행 범위(Codex #1).** `repositories/revenue-entries.ts`의 `updateRevenueEntryIfVersionMatches`가 `projectId`·`kind`를 받아 `WHERE id AND version AND project_id AND kind`로 고친다. 0행이면 `saveEntries`가 같은 tx로 `findRevenueEntryById`를 읽어 같은 프로젝트·같은 종류면 기존 버전 충돌 문구, 아니면(없음 · 남의 프로젝트 · 다른 종류) `줄을 찾을 수 없음 · 새로 고침`로 04-20의 `denyWrite(viewer, "revenue.entry-scope", { projectId, entryIds }, err)`를 지나 던진다 — 발행 표로 입금 줄 id를 흘려 입금 금액을 바꾸는 길과 다른 프로젝트의 줄을 고치는 길이 닫힌다.

③ **새 줄 id와 재전송(ENG-D10).** 새 줄은 04-12가 견적 줄에 정한 모양 그대로 `isNew: true` + 클라이언트가 만든 UUID `id`를 싣고, 기존 줄은 `id`·`version`이다. `app/(app)/projects/actions.ts`의 `revenueEntryRowSchema`에서 `id`를 UUID 필수로, `isNew`(선택 `true`)를 더하고 `version`은 선택으로 둔다. `app/(app)/projects/[id]/quote-table.tsx`의 `newEntryDraft`가 `crypto.randomUUID()`로 `id`를 만들고 `clientKey`도 그 값으로 두며, 저장 페이로드가 `version`이 없는 줄에 `isNew: true`를 싣는다(`EntryDraft` 타입은 바꾸지 않는다). `saveEntries`: `isNew`면 새 줄 — 같은 tx로 그 `id`의 행을 먼저 읽어, 없으면 `insertRevenueEntry`(`id` 포함, `ON CONFLICT (id) DO NOTHING` + `RETURNING` — 0행이면 아래 「있으면」과 같게 다시 읽어 판정), 있으면 같은 프로젝트·같은 종류이고 **보관되지 않았으며**(`archived_at`이 비어 있음 — 04-12 견적 줄 재전송의 「보관된 줄이면 거부」와 같은 결) 저장된 값(날짜 · 통화 · 외화 금액 · 환율 · 원화 · 메모)이 요청의 정규화된 값과 같을 때만 **재전송**으로 보고 쓰지 않고 그 행을 결과에 싣는다 — 하나라도 아니면(값 다름 · 남의 프로젝트·종류 · 보관된 줄) `이미 저장된 줄과 값이 다름 · 새로 고침`(04-20의 `denyWrite` — 규칙 `revenue.replay-mismatch`)로 배치 전부를 거부하고 보관된 줄을 되살리지 않는다. 쓴 줄이 하나도 없는 저장은 행동 로그를 남기지 않는다. `repositories/revenue-entries.ts`의 `RevenueEntryInsertInput`에 `id`를 더한다.

④ **금액 입력 정규화(엔지니어링 리뷰 B §2).** `saveEntries`가 줄마다 04-40의 `normalizeMoneyInput`을 먼저 부르고 `MoneyInputError`를 `{발행|입금} {날짜} {message}` 한국어 `UserFacingError`로 바꿔 배치 전부를 거부한다(`message`는 04-40이 정한 UI-SPEC rev 5 Copywriting `Error — 셀(금액 범위)`·`Error — 셀(환율)` 글자 그대로 — 예 `입금 2026-09-18 환율은 0보다 커야 합니다 · 환율을 고쳐 주세요`) — 이 파일에 KRW 환율 고정·범위 검사를 따로 쓰지 않는다. 부호 규칙은 더하지 않는다(요청 범위 밖).

⑤ **잠금 안 전역 db 호출 제거(04-32 규칙 · 엔지니어링 리뷰 B §1).** (a) **커밋 뒤 환율 기억 — 04-12의 교차 그룹 규약.** `saveEntries` 안의 `rememberFxRate` 호출을 없앤다. 매출의 트랜잭션 안 쓰기 단계(`saveEntries` → `tx`를 받은 `saveRevenue`)는 환율을 기억하지 않고 결과에 `fxToRemember: { currency, rate }[]`(외화 줄 중 이번 저장에서 환율 칸을 실제로 고친 줄 — D-71, 04-12의 견적 줄 목록과 같은 모양)를 싣는다. **트랜잭션을 연 쪽**이 커밋 뒤 그 목록마다 `rememberFxRate`(`domain/money/currency.ts` — 고치지 않는다)를 부른다: `saveRevenue` 단독 호출이면 `saveRevenue`가 자기 `withTransaction`이 끝난 뒤, `saveProjectLedger`면 04-12가 견적 줄 `fxToRemember`를 기억하는 커밋 뒤 자리에서 견적 줄 목록과 매출 목록을 합쳐 한 번에 부른다. 기억이 실패해도 저장은 성공으로 돌려주고 `log.error("fx.remember_failed", { currency })` 한 줄을 남긴다(04-12와 같은 이벤트 — 편의 기억이 저장을 막지 않는다). 실패 주입을 위해 `saveRevenue`가 `deps.rememberFxRate?`를 받는다(04-07 `saveReserves`와 같은 형태). 거부·롤백된 배치는 커밋이 없어 목록을 기억하지 않는다(거부된 배치가 설정을 바꾸던 결함도 함께 닫힌다). (b) `saveRevenue`가 `tx` 안에서 `can()`을 부르지 않게 `deps.rights?: { canWriteEntries: boolean }`(트랜잭션 앞에서 계산한 값)를 받고, `domain/projects/ledger.ts`가 `withTransaction` **앞**에서 계산해 넘긴다 — 04-12가 원장 합성 저장에서 이미 이렇게 했으면 확인만 하고 SUMMARY에 적는다. (c) `tx`가 있으면 행동 로그를 `recordAction(viewer, entry, { tx })`로 같은 트랜잭션에 쓴다(유령 로그 방지 — 04-12가 이미 했으면 확인만). 행동 로그 detail에는 지금처럼 금액을 싣지 않는다.

⑥ **전체 게이트(CLAUDE.md — 로컬 dev 통과는 완료 신호가 아니다).** `CI=true pnpm test` 한 번. 실패하면 `systematic-debugging`으로 원인을 찾는다. 원인이 앞 플랜 파일이면 ENG-D11에 따라 그 자리만 고치고 회귀 테스트 + SUMMARY 편차로 적는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts test/integration/settings.test.ts test/integration/action-log.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 다른 프로젝트·다른 종류의 줄 id로 매출이 고쳐졌거나, 같은 새 줄 재전송이 두 행을 만들었거나 값이 다른 재전송·보관된 줄 id의 재전송이 통과했거나, KRW 위조 환율이 저장됐거나, 거부된 배치가 최근 환율을 바꿨거나 환율 기억 실패가 저장을 실패시켰거나, 권한 판정이 잠금 뒤에 불렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 새 발행·입금 줄의 클라이언트 UUID가 액션 스키마·저장 경로와 맞지 않아 화면 저장이 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - `updateRevenueEntryIfVersionMatches`의 조건이 `id`·`version`·`project_id`·`kind` 넷이고, 남의 프로젝트·다른 종류 줄 id 거부가 DB 무변경과 함께 통합에 있다(Codex #1)
    - 새 발행·입금 줄이 `isNew: true` + 클라이언트 UUID를 싣고(`newEntryDraft` — 04-12 견적 줄과 같은 모양), 액션 스키마가 `id`를 UUID 필수로 받으며, 같은 페이로드 재전송이 한 행 · 값이 다른 재전송과 남의 줄 id가 거부된다 · 보관된 줄 id의 재전송이 거부되고 그 줄이 보관 상태·값 그대로다(통합, ENG-D10 · I1) · 삽입이 `ON CONFLICT (id) DO NOTHING`이다
    - `saveEntries`가 `normalizeMoneyInput`을 부르고 이 파일에 KRW 환율 고정·범위 비교를 손으로 쓴 코드가 없다
    - `saveEntries` 안에 `rememberFxRate` 호출이 없고 매출 쓰기 단계가 `fxToRemember`를 돌려주며, `saveRevenue` 단독과 `saveProjectLedger`(견적 줄 목록과 합쳐)가 커밋 뒤에 기억한다(Grep + 통합) · 거부된 배치 뒤 최근 환율이 그대로다 · 기억 실패 주입에서 저장은 성공하고 `fx.remember_failed`가 한 번이다 · `saveRevenue`가 `tx` 안에서 `can()`을 부르지 않는다 · `tx`가 있으면 행동 로그가 그 `tx`로 쓰인다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>매출 줄 쓰기가 자기 프로젝트·자기 종류의 줄에만 닿고, 응답을 잃은 재전송이 줄을 두 번 만들지 않으며, 금액 입력이 한 규칙으로 정규화되고, 잠긴 트랜잭션 안에서 풀 연결을 더 붙잡지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장 | 옛 클라이언트가 계약 금액 필드를 실어 보낼 수 있다 |
| 마이그레이션 → 운영 중 리비전 | 컬럼 삭제와 옛 리비전의 읽기가 겹치는 창 |
| 마이그레이션 → 기존 데이터 | 업무 값이 있는 DB에서 컬럼 삭제가 값을 지운다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-84 | Tampering | 옛 계약 금액 필드 | medium | mitigate | 액션 스키마에서 필드를 지워 zod가 버리고, 도메인·리포지토리 쓰기 함수와 컬럼이 없어 저장 경로 자체가 없다 — 통합 테스트와 참조 스캔으로 고정(Task 1) |
| T-04-86 | Denial of Service | 컬럼 삭제 배포 창 | medium | mitigate | 「Phase 4는 PR #38 한 번에 배포」 릴리스 제약을 DECISIONS에 기록한다(사용자 D9). 한 번에 배포하면 옛 리비전은 `projects`를 읽지 않는 Phase 3 코드라 창에서 오류가 없다. 중간 리비전으로의 코드 롤백은 역 SQL을 먼저 적용한다(CEO 리뷰 B-09 롤백 도식) |
| T-04-86b | Tampering | 업무 계약 값 손실 | high | mitigate | DROP 전 RAISE 가드가 업무 데이터(`source <> 'demo'`)의 계약 원화 금액을 세고 한 건이라도 있으면 전체 적용을 멈춘다 — 모든 대기 마이그레이션이 한 트랜잭션이라 부분 적용이 없다. 옛 데이터 위 업그레이드 테스트 (d)로 고정(OV-6) |
| T-04-86c | Tampering | 린트 우회 | low | mitigate | `ban-drop-column`은 DROP 문장 바로 위 인라인 무시 + 이유로만 비켜 가고 `.squawk.toml` diff 0줄을 검증 명령이 본다 — 다른 마이그레이션의 DROP이 조용히 통과하는 전역 예외를 만들지 않는다 |
| T-04-210 | Tampering / Elevation of Privilege | 매출 줄 id 위조(남의 프로젝트 · 입금 줄을 발행 표로) | high | mitigate | 수정 조건이 `id`·`version`·`project_id`·`kind` 넷이고, 0행이면 같은 tx로 다시 읽어 남의 줄이면 04-20의 `denyWrite`와 함께 거부한다 — 통합 두 케이스로 고정(Codex #1) |
| T-04-211 | Tampering | 응답을 잃은 재전송의 중복 줄 · 보관된 줄 되살리기 | medium | mitigate | 새 줄이 클라이언트 UUID를 싣고 삽입이 `ON CONFLICT (id) DO NOTHING`이며, 이미 있는 id는 같은 프로젝트·종류·값이고 보관되지 않았을 때만 재전송으로 보고 아니면 거부한다 — 한 행 · 값이 다른 재전송 거부 · 남의 줄 id 거부 · 보관된 줄 id 재전송 거부를 통합으로 고정(ENG-D10 · I1) |
| T-04-212 | Tampering | 위조 금액 입력(KRW 환율 · 범위) | medium | mitigate | 매출 줄이 04-40의 `normalizeMoneyInput`을 지난다 — KRW 환율 1 고정, USD 환율 0 이하·범위 밖은 한국어 이유로 배치 전부 거부(엔지니어링 리뷰 B §2) |
| T-04-213 | Denial of Service | 잠금 안 풀 연결 대기 · 거부된 배치의 설정 변경 | medium | mitigate | 권한은 트랜잭션 앞에서 계산해 넘기고(`deps.rights`), 최근 환율 기억은 쓰기 단계가 돌려준 `fxToRemember`를 트랜잭션을 연 쪽이 커밋 뒤에(04-12 규약 — 실패는 `fx.remember_failed`만 남기고 저장은 성공), 행동 로그는 `tx`로 — 행 잠금을 쥔 채 두 번째 풀 연결을 기다리지 않고, 거부된 배치가 `fx.recent_rate`를 바꾸지 않는다(04-32 규칙) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(04-03이 재개되어 스크립트를 더해도 이 검사는 초록, 엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint:sql` `Found 0 issues`, 0010·`.squawk.toml` diff 0줄, 마이그레이션 번호 검증 명령 초록
- `pnpm lint` · `pnpm typecheck` 통과
- 업그레이드 테스트 (c)(d) 초록 — 재시드 전 검사
- `CI=true pnpm test` 세 계층 통과
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)
</verification>

<success_criteria>
- 계약 금액을 입력·저장하는 길이 코드와 DB 어디에도 없다
- 컬럼 삭제가 업무 값이 있는 DB에서는 멈추고, 없는 DB에서는 기존 행을 보존한다 — 옛 데이터 위에서 증명됐다
- ARCHITECTURE §5 예외와 「Phase 4 한 번에 배포」 제약, 되돌리는 법이 결정 기록에 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/0015_drop_project_contract_columns.sql`(04-13의 0014 뒤 — 생성기 번호가 다르면 실제 이름을 SUMMARY에) + `meta/{번호}_snapshot.json` + `_journal.json` 항목 |
| 컬럼(삭제) | `projects.contract_currency` · `contract_foreign_amount` · `contract_fx_rate` · `contract_amount_krw` |
| 삭제된 심볼 | `updateProjectContract` · `ProjectContractUpdateInput` · `SaveRevenueInput.contract`·`contractFxRateTouched` · `saveProjectLedgerAction` 스키마 `revenue.contract`·`contractFxRateTouched` · `insertProject`·`createProject`의 계약 칸 |
| 문서 | `docs/design/DECISIONS.md` 한 항목(ARCHITECTURE §5 예외 · 릴리스 제약 · 역 SQL) |
| 변경된 기존 심볼(매출 쓰기) | `updateRevenueEntryIfVersionMatches`(`projectId`·`kind` 조건) · `insertRevenueEntry`(클라이언트 `id` · `ON CONFLICT (id) DO NOTHING`) · `saveEntries`(재전송 판정 — 보관된 줄 거부 포함 · `normalizeMoneyInput` · `fxToRemember` 반환) · `saveRevenue`(`deps.rights` · `deps.rememberFxRate` · `tx` 행동 로그 · 단독 호출이면 커밋 뒤 `fxToRemember` 기억) · `revenueEntryRowSchema`(`id` UUID 필수) · `newEntryDraft`(`crypto.randomUUID()`) · `saveProjectLedger`(권한 선계산 · 커밋 뒤 견적 줄·매출 `fxToRemember`를 합쳐 기억) |
| 운영 로그 | `write.denied`(04-20 `denyWrite` — 규칙 `revenue.entry-scope`·`revenue.replay-mismatch`, 금액 없음) · `fx.remember_failed`(04-12와 공용 — 커밋 뒤 기억 실패, 저장은 성공) |
| 거부 문구(신규) | `줄을 찾을 수 없음 · 새로 고침`(Codex #1) · `이미 저장된 줄과 값이 다름 · 새로 고침`(ENG-D10) |
| 테스트 | `migration-upgrade.test.ts`(케이스 c·d 추가 — 04-06 파일) · `revenue-entries.test.ts`(스키마 무시 · 참조 스캔 · 행 범위 · 재전송 · 금액 입력 · 커밋 뒤 환율 · 권한 선계산) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-09 · 사용자 D9 (T14) — ARCHITECTURE §5 「DROP 없음」 위반 · 배포 창 · 데이터 손실 | DECISION | Plan 04-41, Task 2 ③: RAISE 가드 + 인라인 squawk 무시 · Task 2 ⑤: DECISIONS에 예외·「PR #38 한 번에 배포」·역 SQL(같은 커밋) |
| OV-6 (T14) — 마이그레이션 테스트가 재시드된 빈 DB만 검증 | HIGH | Plan 04-41, Task 2 ①: 0010 상태 옛 데이터 위 업그레이드 (c) 보존 · (d) 가드 실패 전체 무변경, 재시드 전 검사 |
| 옛 04-16 Task 2 — 계약 쓰기 경로 제거 | (분할) | Plan 04-41, Task 1: 액션·도메인·리포지토리 제거 + 참조 스캔 |
| A-12 — 완료 계약 편집 | WARNING | 끝 상태로 충족 — Plan 04-41, Task 1 ②가 계약 입력 경로(액션 스키마 · `SaveRevenueInput` · `saveRevenue` 분기 · `updateProjectContract`)를 통째로 지우고 Task 2가 컬럼을 지워, 완료든 아니든 계약 금액을 저장할 길이 없다. 04-12는 중간 게이트(완료 계약 거부 갈래)를 만들지 않으므로 이 플랜이 지울 고아도 없다 — 세웠다 지우는 코드를 없앴다(Simplicity First · Phase 4는 PR #38 한 번에 배포, 중간 상태 미배포). 1차 렌더 `canSave`는 04-22 · 그 계약 항 삭제는 04-16 |

### Round 2 — 엔지니어링 리뷰(ebcc051, `docs/designs/plant8-erp-phase4-eng-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Codex #1 [P1] — 매출 수정이 `id`·`version`만 본다(`repositories/revenue-entries.ts:91`) | P1 | Plan 04-41, Task 3 ②·behavior: 네 조건 + 남의 줄 거부 통합 |
| ENG-D10 · Codex #3 (R9) — 응답을 잃은 재전송이 매출 줄을 두 번 넣는다 | DECISION(P1) | Plan 04-41, Task 3 ③·behavior: 클라이언트 UUID · `ON CONFLICT (id) DO NOTHING` · 재전송 판정 · 한 행 통합 (견적 줄은 다른 그룹, 리저브는 Plan 04-07) |
| B §2 — 금액 입력 정규화 한 곳 | P2 | Plan 04-41, Task 3 ④: 04-40 `normalizeMoneyInput` |
| B §1 · A §1 — 잠금 안 전역 db 호출 · 커밋 전 `rememberFxRate` · 유령 로그 | P2 | Plan 04-41, Task 3 ⑤ |
| 낡은 주석(`domain/revenue/index.ts` 287–290 · `domain/projects/ledger.ts` 6–8) | P3 | Plan 04-41, Task 1 ② |
| B §2 — `node:fs` 참조 스캔이 `tsc`와 겹침 | P3 | 유지(싸다 — 원시 SQL 문자열의 snake_case 이름을 잡는 몫이 남는다). 변경 없음 |
| 세 태스크 한도 | 구조 | 옛 Task 3(DECISIONS)을 Task 2에 합침(같은 커밋 요구와 일치) · 새 Task 3 = 매출 쓰기 굳히기 + 전체 게이트 |

### Round 3 — plan-checker(2026-09-23)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W1 — `denyWrite`의 출처 | WARNING | 04-12 → 04-20(`domain/rules/deny-write.ts`) · read_first를 04-20-SUMMARY로 |
| W2 — T-04-SC가 `package.json` 원문 diff | WARNING | 04-12의 의존성 객체 + lockfile 비교 문구로 교체(T-04-SC · verification) |
| W3 — 커밋 뒤 환율 기억이 조건부 문구 | WARNING | Plan 04-41, Task 3 ⑤(a): 04-12 규약 확정 — 쓰기 단계가 `fxToRemember` 반환 · `saveRevenue` 단독/`saveProjectLedger`(합쳐서)가 커밋 뒤 기억 · 실패는 `fx.remember_failed`만 · behavior·수용 기준에 합성 저장·실패 주입 케이스 |
| W4 — 매출 쓰기 위협 ID 네 개가 04-17의 번호와 겹침 | WARNING | T-04-210..213으로 재번호 |
| I1 — 보관된 줄 id의 재전송 | INFO | Plan 04-41, Task 3 ③(보관된 줄은 재전송 아님 → 거부) · behavior·수용 기준 통합 케이스 · T-04-211 |

### Round 4 — 디자인 리뷰 · UI-SPEC rev 5 재계획(`docs/designs/plant8-erp-phase4-design-review-260923.md`)
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| P0 · F1 — 매출 쓰기 거부 문구가 rev 5와 다름 | MEDIUM | Plan 04-41, truths · Task 3 behavior·②③: 재전송 불일치 = rev 5 `이미 저장된 줄과 값이 다름 · 새로 고침`(04-12 문구로 통일 — Copywriting `Error — 저장(재전송 불일치, ENG-D10)`) · 행 범위 밖 = `줄을 찾을 수 없음 · 새로 고침`(rev 5 밖 방어 문구 — 명사형, SUMMARY 기록) · 금액 입력 오류는 04-40의 rev 5 글자 앞에 `{발행\|입금} {날짜}` |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| `docs/ARCHITECTURE.md` §5 본문 옆 참조 한 줄 | 사용자 D9 답은 「DECISIONS에 기록」이다 — 그룹 A(04-06·04-17)의 릴리스 제약 참조도 이 플랜의 DECISIONS 항목을 정본으로 가리킨다. ARCHITECTURE.md 본문 편집은 승인 범위 밖이라 하지 않는다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-41-SUMMARY.md` when done
</output>
