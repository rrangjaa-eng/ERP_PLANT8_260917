---
phase: 04-project-quote-ledger
plan: 42
type: execute
wave: 25
depends_on: ["04-07"]
files_modified:
  - app/(app)/pnl/page.tsx
  - app/(app)/pnl/reserves/page.tsx
  - app/(app)/pnl/reserves/reserves-table.tsx
  - app/(app)/pnl/reserves/actions.ts
  - app/(app)/pnl/reserves/actions.registry.ts
  - app/(app)/pnl/reserves/reserves.module.css
  - test/integration/leak-scan.test.ts
  - test/e2e/reserves.spec.ts
  - .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md
  - .planning/phases/04-project-quote-ledger/04-UI-SPEC.md
autonomous: true
requirements: [RSV-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "손익 첫 화면(`/pnl`)의 리저브 대장 링크는 `pnl` 보기 권한과 `reserve.amount` 노출이 **둘 다** 있는 사람에게만 렌더된다 — 상단 메뉴는 역할과 상관없이 다섯 전부 보이므로 기획 PM도 `/pnl`에 닿지만 링크를 보지 못하고, 404로 가는 링크가 없다(CEO 리뷰 B-13, D-59)"
    - "`/pnl/reserves`는 `pnl` 보기 권한과 `reserve.amount` 노출이 둘 다 있을 때만 존재하고 하나라도 없으면 찾을 수 없음이다 — 행·그룹·건수를 가린 채 렌더하는 부분 노출이 없다(CEO 리뷰 B-15, T-04-41)"
    - "대장 열은 날짜 · 구분 · 금액 · 잔액 · 프로젝트 · 증빙 종류 · 세금계산서 번호 · 메모 · **클라이언트** 아홉이다 — 클라이언트는 거래처 Select이고 새 줄에서만 편집되며 첫 저장 뒤에는 잠김(서버 셀 단계)이다. 「리저브 줄 추가」로 만든 새 줄은 클라이언트 칸이 편집 상태로 열린다(사용자 D6 · CEO 리뷰 B-06)"
    - "그룹 머리글은 클라이언트이고 머리글 행 오른쪽에 그 클라이언트의 최종 잔액이 서버 계산으로 붙는다 — 잔액 열은 누구에게나 읽기 전용이고 흐린 색이 아니라 본문 색이다(S9)"
    - "대장은 50건 번호 페이지다(`ui/pagination` 링크 갈래, `?page=N`, 50건 이하면 페이지 줄 없음). 페이지를 넘는 클라이언트 그룹은 다음 쪽 맨 위에 머리글과 **같은 최종 잔액**을 다시 보이고, 줄 잔액은 쪽과 무관하다(D-91, S9 2026-09-23 — 서버 계산은 04-07)"
    - "잔액이 음수가 되는 저장을 시도하면 서버가 전부 거부하고 그 날짜 마지막 줄의 금액 셀이 오류 셀 `이 줄 뒤 잔액이 {잔액}이 됩니다 · 금액을 줄이거나 입금 줄을 먼저 적어 주세요`로 고정되며 합계 행에 전부 거부가 표시된다 — 같은 날 출금을 입금보다 먼저 적어도 그날 마감이 0 이상이면 저장된다(S9, 사용자 D19-2)"
    - "리저브 줄 삭제는 모달(제목 `리저브 줄 삭제` · 본문 `보관함으로 이동합니다 · 잔액이 다시 계산됩니다` · 1차 `리저브 줄 삭제` · 2차 `취소 Esc`) 뒤 1차 「일괄 저장」 배치에 실려 잔액 판정과 함께 저장된다 — 경영관리에게 보관함 권한이 없어도 된다(Copywriting Destructive — 리저브 줄 삭제, CEO 리뷰 B-04 · T5)"
    - "USD 줄의 환율 칸 기본값은 설정의 최근 USD 환율이고, 환율을 고친 줄만 `fxRateTouched`를 싣는다 — 금액 칸은 타이핑 중 천 단위 쉼표가 들어가고 서버로는 쉼표를 뗀 값이 간다(D-71 · CEO 리뷰 B-12, D-95)"
    - "증빙 종류 칸을 편집하는 동안 고른 값의 코드표 설명이 셀 아래 한 줄로 보이고 설명이 없는 값이면 그 줄이 없다 — 구분(입금/출금)과 클라이언트는 설명이 없다(D-93, S14)"
    - "1차 버튼은 `일괄 저장 N` + kbd `Ctrl+S`이고 힌트 줄은 이 화면에서 실제로 되는 키만 `Ctrl` 표기로 적는다(D-94, Copywriting Primary CTA — 리저브)"
    - "쓰기 권한이 없는 사람(`pnl` 보기 + 노출만)에게는 편집 셀이 0이라 흰 머리글 읽기 표이고, 0건이면 `리저브 기록이 없습니다 · 기록은 경영관리`(버튼 없음)다 · 쓰기 권한자의 0건은 `리저브 기록이 없습니다 · 리저브 줄 추가` · 로드 실패는 `리저브 대장을 불러오지 못했습니다 · 다시 시도` 한 줄(Copywriting Empty·Error — 리저브)"
    - "선택 열(프로젝트)이 빈 줄은 em dash 하나이고, 세금계산서 번호는 숫자 고정 폭 줄바꿈 없음이며, 폰 1순위 세 열은 날짜·금액·구분, 접힌 줄은 프로젝트·잔액·메모, 클라이언트·증빙 종류·세금계산서 번호는 폰에서 숨는다(그룹 머리글이 클라이언트를 말한다) — 375px 가로 스크롤 0(S9)"
    - "리저브 충당(매출 반영) 동작·버튼이 없다 — Phase 9다"
    - "리저브 액션과 DTO가 D-38 누수 스캔 레지스트리에 들어 있다 — `test/integration/leak-scan.test.ts`가 04-07이 더한 `@/domain/reserves`에 이어 이 라우트의 `actions.registry`를 불러 스캔한다"
    - statement: "리저브가 0건·1건·51건일 때 0건은 EMPTY 한 줄, 1건에서도 클라이언트 그룹 머리글과 오른쪽 잔액이 렌더되고, 51건이면 번호 페이지로 나뉘어 2쪽 맨 위에 같은 클라이언트 머리글과 같은 잔액이 다시 보인다 — N=0/1/51 렌더"
      verification: backstop
    - statement: "리저브 메모 셀은 긴 칸 규칙 아래 PC에서 한 줄 말줄임으로 렌더되고 행 높이를 늘리지 않는다"
      verification: backstop
  prohibitions:
    - statement: "리저브 금액이 기획본부에게 보이는 어떤 경로로도 새어서는 안 된다 — 링크 한 줄·빈 표·건수 표시도 부분 노출이다. `/pnl`에서 링크를 가리고 `/pnl/reserves`가 404인 것이 화면 쪽 증명이고, DTO 쪽은 04-07의 키 집합 테스트다"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/pnl/reserves/page.tsx"
      provides: "클라이언트별 리저브 대장 화면 — `pnl` 보기 + `reserve.amount` 노출 없으면 찾을 수 없음"
      contains: "notFound"
    - path: "app/(app)/pnl/reserves/reserves-table.tsx"
      provides: "편집 표 — 클라이언트 그룹 · 잔액 열 · 클라이언트 열(첫 저장 뒤 잠김) · 삭제 모달 · 번호 페이지"
      contains: "clientId"
    - path: "app/(app)/pnl/reserves/actions.ts"
      provides: "리저브 일괄 저장 액션(줄 + `archivedIds`) — `authedActionClient` + zod"
      contains: "saveReserves"
    - path: "test/e2e/reserves.spec.ts"
      provides: "경영관리 기록·클라이언트 선택·음수 거부·삭제 · 기획 PM 링크 부재와 404 · 노출 꺼진 계급 404"
      min_lines: 60
  key_links:
    - from: "app/(app)/pnl/page.tsx"
      to: "domain/permissions/visible.ts"
      via: "링크 렌더 조건이 `can(pnl, view)`와 `visible(reserve.amount)` 둘 다다"
      pattern: "reserve.amount"
    - from: "app/(app)/pnl/reserves/actions.ts"
      to: "domain/reserves/index.ts"
      via: "일괄 저장이 04-07의 `saveReserves` 하나를 부른다 — 잔액·잠금·검증은 서버"
      pattern: "saveReserves"
    - from: "app/(app)/pnl/reserves/reserves-table.tsx"
      to: "ui/table/Table.tsx"
      via: "편집 표 계약(§7-3)을 쓰기만 한다 — 셀 단계는 서버 DTO에서 온다"
      pattern: "cellEditability"
---

<objective>
**As a** 경영관리 담당자, **I want to** 손익 화면에서 리저브 대장으로 들어가 클라이언트를 골라 입출금 줄을 적고 잔액을 줄마다 보며, 음수가 되는 기록은 그 자리에서 이유와 함께 막히기를, **so that** 엑셀로 따로 관리하던 리저브를 시스템 안에서 기록하고, 기획본부에게는 대장의 존재조차 보이지 않는다.

**2026-09-23 CEO 리뷰 신설 플랜(04-07 분할 — 화면 쪽).** 04-07이 표 · 잔액 · 클라이언트 잠금 · 보관/복원 · 권한 · 노출을 서버에서 끝냈다. 이 플랜은 그 위에 액션 · 대장 화면 · `/pnl` 링크 · E2E · 문서 기록을 얹는다. 반영한 것: **사용자 D6**(B-06 — 9번째 열 클라이언트, 첫 저장 뒤 잠김) · **B-13**(링크는 `pnl` 보기 + 노출일 때만 — 옛 계획의 「손익 권한이 없으면 `/pnl`에 닿지 못한다」는 사실이 아니었다: `/pnl`은 세션만 보고 상단 메뉴는 다섯 전부 보인다) · **B-15**(화면도 둘 다 요구) · **B-04 · T5**(삭제는 일괄 저장 배치로 — 보관함 권한 불필요) · **B-12**(최근 환율 기본값 · touched) · **사용자 D19-1**(원화 잔액의 우회를 문서로) · **사용자 D19-2**(같은 날 순서) · **B-34**(마지막 `ui/table` 변경인 04-18 뒤 — 04-07 경유 — 에 선다. 이 플랜은 `ui/table`을 쓰기만 한다).

Purpose: ROADMAP 기준 6 「경영관리가 클라이언트별 리저브 대장에 입출금을 기록하고 잔액이 보인다」의 화면 쪽과 D-59 「기획본부 기본 숨김」의 화면 쪽 증명.

Output: 액션·레지스트리 · 대장 화면(9열 · 번호 페이지 · 삭제 모달 · 오류 셀) · `/pnl` 링크 · 누수 스캔 등록 · E2E 1 · 04-OPEN-ITEMS 기록 · UI-SPEC S9·Copywriting 갱신.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시한다. 새 의존성 0개. **`ui/table`·`ui/shell`·`docs/design/tokens.css`를 고치지 않는다.**
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-07-SUMMARY.md
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 1259–1276행(S9 적용 규칙) · 499–508행(S9 여덟 상태 + backstop 둘) · 874–895행(§7-16 페이지 줄) · 315·317·331·332·344·376행(Copywriting 리저브) · 1380행(편집 표 코드표 셀 설명)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md B-06·B-13·B-15 절(Grep `### B-06` 등)
</context>

<probe_fallback>
이 플랜이 담당하는 미분류 행은 **RSV-01(화면)** 이다. 수용 기준은 ROADMAP 기준 6 · D-59·D-60·D-91·D-93·D-94·D-95 · UI-SPEC S9 · CEO 리뷰 B-06·B-13·B-15에서 끌어와 위 truths에 적었다. S9 `## UI Considerations`의 zero-one-many·long-text backstop은 마커로 옮겼다.

**진입 경로에 대한 명시적 가정(스펙과 다름 — 옛 04-07에서 옮김, B-13으로 정정):** UI-SPEC S9는 진입을 「손익 메뉴 안 항목(`role-menu.ts`에 한 줄)」이라 적지만, 그 파일의 계약은 1차 메뉴를 **정확히 다섯**으로 고정하고 하위 메뉴가 없으며 역할과 상관없이 다섯 전부 보인다(D-23). 그래서 **`/pnl` 화면 안의 링크**를 진입 경로로 삼고 `ui/shell/role-menu.ts`를 고치지 않는다. 옛 계획은 「손익 권한이 없으면 `/pnl`에 닿지 못한다」고 했지만 `/pnl`은 세션만 본다(CEO 리뷰 B-13) — D-59의 「기획본부는 못 본다」는 **링크를 `pnl` 보기 + `reserve.amount` 노출일 때만 그리고, 대장 화면이 같은 두 조건으로 404**인 것으로 성립한다. `/pnl` 화면 자체의 권한 판정은 이 플랜이 바꾸지 않는다(손익 원장 화면은 Phase 9).

**클라이언트 열의 폰 우선순위(사용자 D6 — UI-SPEC에 없던 열):** 그룹 머리글이 클라이언트를 말하므로 폰에서는 숨긴다(P3). S9 폰 P1·P2는 그대로다.

**`.planning` 편집(CEO 리뷰 B-35 — 사용자 인지 항목):** 이 플랜의 Task 3은 04-OPEN-ITEMS·04-UI-SPEC을 고친다. CLAUDE.md는 `.planning/` 수동 편집을 금지하지만 04-02·04-04·04-24의 선례(커밋 `ab59d69`·`2c25a6c`)가 있고 CEO 리뷰 B-06이 「S9와 Copywriting을 고친다」를 수정안에 넣었다 — 커밋은 `gsd_run query commit`으로 한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 경영관리가 `/pnl`의 링크로 대장을 열어 클라이언트를 고른 줄을 저장하고 잔액을 본다(링크 조건 → 페이지 게이트 → 표 → 액션 → 04-07 도메인 → E2E)</name>
  <files>app/(app)/pnl/page.tsx, app/(app)/pnl/reserves/page.tsx, app/(app)/pnl/reserves/reserves-table.tsx, app/(app)/pnl/reserves/actions.ts, app/(app)/pnl/reserves/actions.registry.ts, test/integration/leak-scan.test.ts, test/e2e/reserves.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-07-SUMMARY.md` — `listReserves`·`saveReserves`의 실제 입력·출력 모양(`cellEditability` · `clientBalances` · `page`·`pageCount`) · 거부 오류 형태
    - `app/(app)/pnl/page.tsx`(전문 21행 — `requireSession()`만) · `ui/shell/role-menu.ts` 50–60행(다섯 메뉴 항상 — 고치지 않는다)
    - `app/(app)/admin/vendors/actions.ts`·`actions.registry.ts`(Server Action 규약과 레지스트리 분리) · `app/(app)/admin/vendors/page.tsx` 1–71행(세션·`can` 게이트)
    - `app/(app)/projects/[id]/revenue-section.tsx`(04-02·04-16 뒤 — 편집 표 + 금액 셀(통화·환율) + 1차 저장 선례)
    - `ui/table/Table.tsx`·`ui/table/types.ts`(04-18·04-19 뒤 — 그룹 머리글·읽기 전용 계산 열·폰 우선순위 prop. **고치지 않는다**)
    - `test/integration/leak-scan.test.ts` 1–40행(side-effect import 목록)
    - `test/e2e/revenue-section.spec.ts` 10–25행(계급 권한을 테스트에서 켜는 형태) · `test/e2e/fixtures.ts`
  </read_first>
  <action>
① **액션.** `app/(app)/pnl/reserves/actions.ts`: `authedActionClient` + zod로 `saveReservesAction`(`rows`: 새 줄은 `clientId` 필수 · 기존 줄은 `id`·`version` · `entryDate`·`direction`·금액·`fxRateTouched`·선택 칸 / `archivedIds`) 하나 — 04-07의 `saveReserves`를 부르고 `revalidatePath`, `./actions.registry`를 side-effect import. `actions.registry.ts`에 액션과 DTO 이름을 등록한다(D-38). 보관은 이 액션의 `archivedIds`로만 한다 — 별도 보관 액션을 만들지 않는다(B-04).

② **링크(B-13).** `app/(app)/pnl/page.tsx`: 서버에서 `can(viewer, "pnl", "view")`와 `visible(viewer, "reserve.amount")`가 둘 다 참이면 리저브 대장 링크 한 줄(`<a>` — 이동, §10)을 그린다. 아니면 그리지 않는다. 나머지 화면은 그대로.

③ **페이지 게이트(B-15).** `app/(app)/pnl/reserves/page.tsx`: 세션 → 같은 두 판정 → 하나라도 거짓이면 `notFound()`(레이아웃 판정에 기대지 않는다 — Phase 1 WR-07). 데이터는 `listReserves(viewer, { page })`와 참조(거래처·프로젝트·증빙 종류 옵션)를 `Promise.all`로 한 번에.

④ **표(트레이서 몫).** `reserves-table.tsx`: `ui/table` 편집 표 — 그룹 머리글 = 클라이언트 + 오른쪽 최종 잔액(굵게), 열 아홉(날짜 · 구분 · 금액 · 잔액 · 프로젝트 · 증빙 종류 · 세금계산서 번호 · 메모 · 클라이언트), 셀 단계는 서버 DTO(`cellEditability`) 그대로. 3차 「리저브 줄 추가」는 새 줄을 만들고 **클라이언트 칸을 편집 상태로 연다**(Select — 거래처 옵션). 1차 「일괄 저장 N」이 액션을 부르고, 거부는 04-04의 셀 오류·합계 행 경로를 그대로 탄다.

⑤ **누수 스캔.** `test/integration/leak-scan.test.ts`의 side-effect import에 `@/app/(app)/pnl/reserves/actions.registry`를 더한다(`@/domain/reserves`는 04-07이 이미 더했다 — 같은 줄을 두 번 넣지 않는다).

⑥ **E2E(트레이서 증명).** `test/e2e/reserves.spec.ts` 첫 케이스: 준비에서 경영관리 계급(테스트용 계급에 `pnl` 보기·쓰기 · `reserve.amount` 노출)과 거래처 둘 → `/pnl` → 링크 → 「리저브 줄 추가」 → 클라이언트 칸에서 거래처 A 선택 · 날짜 · 구분 입금 · 금액 `1500000` → Ctrl+S → 그룹 머리글 A와 오른쪽 잔액 `1,500,000`, 줄 잔액 `1,500,000` → 다시 불러오면 그 줄의 클라이언트 칸이 잠김(편집 진입 안 됨).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts &amp;&amp; pnpm playwright test test/e2e/reserves.spec.ts</automated>
    <fails_when>통합·playwright failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 리저브 액션·DTO가 누수 스캔에서 빠졌거나, 링크 → 대장 → 클라이언트 선택 → 저장 → 잔액 경로가 끊겼거나, 저장 뒤 클라이언트 칸이 잠기지 않았다</fails_when>
    <automated>git diff --stat d6b41cf -- ui/table ui/shell docs/design/tokens.css domain/permissions/menus.ts</automated>
    <fails_when>출력이 비어 있지 않다 — 이 플랜은 표·셸·토큰·메뉴 레지스트리를 쓰기만 하고 고치지 않는다(D-23 · B-34)</fails_when>
  </verify>
  <acceptance_criteria>
    - `/pnl` 링크와 `/pnl/reserves` 게이트가 `can(pnl, view)`와 `visible(reserve.amount)` 둘 다를 요구한다
    - 액션이 04-07의 `saveReserves` 하나를 부르고 레지스트리에 등록되며 누수 스캔 import에 이 라우트의 `actions.registry`가 있다
    - 새 줄의 클라이언트 칸이 Select로 열리고 저장 뒤 잠김이다(E2E)
    - `ui/table`·`ui/shell`·`tokens.css`·`menus.ts` diff 0줄
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>경영관리가 `/pnl`에서 대장으로 들어가 클라이언트를 고른 줄을 저장하고 서버 계산 잔액을 보는 한 경로가 화면부터 DB까지 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 대장 화면의 나머지 계약 — 번호 페이지 · 오류 셀 · 삭제 모달 · 쉼표·환율 · 증빙 설명 · Ctrl · 읽기 전용 · 폰 + 기획 PM·노출 꺼짐 차단 E2E</name>
  <files>app/(app)/pnl/reserves/reserves-table.tsx, app/(app)/pnl/reserves/page.tsx, app/(app)/pnl/reserves/reserves.module.css, test/e2e/reserves.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1259–1276행 · 499–508행 · 874–895행 · Copywriting 315·317·331·332·344·376행
    - `ui/pagination/Pagination.tsx`·`page-window.ts`(04-29) · `app/(app)/projects/page.tsx`(04-17 — `href` 함수 갈래와 범위 밖 번호 선례)
    - `lib/format-number.ts`(`formatKrw`·`formatForeignLine`) · `ui/input/use-comma-input.ts`(04-09) · `ui/select/Select.tsx`의 `SelectOption.description`(04-25) · `app/(app)/projects/[id]/quote-table.tsx`의 소분류 편집 중 설명 줄(04-23 — Grep `description`)
    - `domain/money/currency.ts`(`recentFxRate`) · `app/(app)/projects/[id]/revenue-section.tsx`의 환율 칸 기본값·touched 처리(04-02)
    - `docs/design/SYSTEM.md` §6-1 · §7-3 · §7-7 · §2-4
  </read_first>
  <behavior>
    - 한 클라이언트 51줄 → 페이지 줄 · 2쪽 맨 위에 같은 클라이언트 머리글과 같은 최종 잔액 · 2쪽 첫 줄 잔액 = 1쪽 마지막 줄 잔액 ± 그 줄 금액 · `?page=99` → 마지막 쪽
    - 잔액이 음수가 되는 출금 저장 → 그 날짜 마지막 줄 금액 셀이 오류 셀 + Copywriting 문구 · 합계 행 전부 거부 · 같은 날 출금을 먼저 적고 입금을 뒤에 적은 배치(마감 ≥ 0) → 저장된다
    - 삭제 모달 라벨 → 1차 → 「일괄 저장 N」의 N이 늘고 → Ctrl+S → 줄이 사라지고 잔액 재계산 · 보관함에 `리저브` 항목(경영관리 계급에 `admin.archive` 없음)
    - 금액 칸 `1500000` → `1,500,000` 표시 · USD 선택 시 환율 칸에 최근 USD 환율 · 증빙 종류 편집 중 설명 한 줄 · 1차 kbd `Ctrl+S`
    - `pnl` 보기 + 노출만 있는 계급 → 흰 머리글 읽기 표, 0건이면 `리저브 기록이 없습니다 · 기록은 경영관리`(버튼 없음) · 프로젝트 없는 줄의 프로젝트 칸 em dash · 잔액 셀은 클릭·Enter로 편집에 들어가지 않는다
    - 기획 PM → `/pnl`에 링크가 DOM에 없고 `/pnl/reserves` 직접 접근은 찾을 수 없음 · `pnl` 보기는 있고 `reserve.amount` 노출이 꺼진 계급 → 링크 없음 + 404(B-13·B-15)
  </behavior>
  <action>
① E2E 케이스(`behavior` 여섯 줄 — 51줄은 준비 SQL로)를 먼저 쓴다(RED).

② **번호 페이지(D-91).** `page.tsx`가 `?page=N`을 `listReserves`에 넘기고, 표 아래 `Pagination`(label `리저브`, `href` 함수 갈래 — 이동이라 `<a>`, 범위 문구 단위 `건`, 50건 이하면 없음). 쪽의 첫 줄이 앞 쪽과 같은 클라이언트면 `ui/table`의 `groupBy`가 그 쪽 첫 그룹 머리글을 그리고, 오른쪽 잔액은 04-07의 `clientBalances`(최종 잔액)에서 온다 — 화면이 잔액을 더하지 않는다.

③ **오류·삭제.** 음수 거부는 04-04의 셀 오류 경로 그대로(서버가 준 줄·칸·이유). Delete → `리저브 줄 삭제` 모달(Copywriting 그대로) → 1차가 그 줄을 `archivedIds`에 넣고 dirty에 합류(04-12 견적 줄 삭제와 같은 모양) → 저장 때 서버가 잔액과 함께 판정한다.

④ **입력.** 금액 셀은 04-09 쉼표 입력(`useCommaInput` 또는 `TextField` `numberKind`), 표시는 `lib/format-number.ts`(외화 2행 `formatForeignLine`) — 라우트 안에 로컬 숫자 서식 함수를 두지 않는다. 통화·환율은 04-02 매출 표의 금액 셀 형태 그대로 — USD 환율 기본값은 `recentFxRate("USD")` 실제 값, 사용자가 환율을 고친 줄만 `fxRateTouched: true`(B-12). 증빙 종류 편집 셀은 04-25의 `SelectOption.description`으로 편집 중에만 한 줄(04-23 소분류와 같은 모양) — 구분·클라이언트는 설명 없음. 1차 `일괄 저장 N` + kbd `Ctrl+S`, 힌트 줄은 이 화면에서 실제로 되는 키만.

⑤ **읽기 전용·폰·스타일.** 쓰기 권한이 없으면 서버 셀 단계가 전부 잠김이라 `ui/table`이 자동으로 캡션 읽기 표 — EMPTY 두 갈래(Copywriting). 폰 P1 날짜·금액·구분, P2 프로젝트·잔액·메모, 클라이언트·증빙 종류·세금계산서 번호 숨김. 세금계산서 번호 숫자 고정 폭 nowrap. 잔액 열 본문 색(`--fg`), 머리글 잔액 굵기 — 토큰만(`reserves.module.css`), 새 색·서체·radius 없음.

⑥ 차단 E2E(`behavior` 마지막 줄) — 기획 PM과 노출 꺼진 계급 둘 다(GREEN).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 error가 나오고 종료 코드가 0이 아니다 — stylelint가 색·서체·radius 리터럴을 잡으면 토큰 밖 값을 썼다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/reserves.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 쪽마다 잔액이 달라졌거나, 음수 거부가 오류 셀로 안 보였거나, 같은 날 순서 배치가 거부됐거나, 삭제가 보관함 권한을 요구했거나, 기획 PM·노출 꺼진 계급이 링크나 대장을 봤다</fails_when>
  </verify>
  <acceptance_criteria>
    - 대장이 `ui/pagination` `href` 갈래로 50건 번호 페이지를 렌더하고 「더 보기」가 없으며, 2쪽 머리글 잔액이 1쪽과 같다(E2E)
    - 음수 거부가 날짜 마지막 줄의 금액 오류 셀과 합계 행 전부 거부로 보이고, 같은 날 출금 먼저 적은 배치는 저장된다(E2E)
    - 삭제 모달 1차 라벨이 `리저브 줄 삭제`이고 삭제가 일괄 저장으로 들어가며 보관함 권한 없는 경영관리로 성공한다(E2E)
    - 금액 표시가 `lib/format-number.ts`를 쓰고 이 라우트 안에 로컬 숫자 서식 함수가 없다 · 환율 기본값이 최근 USD 환율이다
    - 증빙 종류 설명이 편집 중에만 보이고 증빙 종류 목록이 Phase 3 코드표에서 온다(새 목록 상수 없음)
    - 기획 PM·노출 꺼진 계급에게 링크가 DOM에 없고 대장이 404다(E2E — B-13·B-15)
    - 리저브 충당 동작·버튼이 없다 · 이 화면의 1차 버튼이 하나다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>대장 화면이 S9 계약(페이지·오류·삭제·입력·읽기 전용·폰)대로 동작하고, 기획본부와 노출 꺼진 계급에게는 링크도 화면도 없다</done>
</task>

<task type="auto">
  <name>Task 3: 문서 기록(04-OPEN-ITEMS · UI-SPEC S9·Copywriting — 사용자 D6·D19-1·D19-11) + 독립 DOM 감사 + 전체 게이트</name>
  <files>.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md, .planning/phases/04-project-quote-ledger/04-UI-SPEC.md, app/(app)/pnl/reserves/reserves.module.css</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md` 80–90행(「S9 zero-one-many」 — 잔액 누적과 페이지네이션 미검토 지적)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 1259–1276행(S9) · 304–390행(Copywriting Contract 표 — 행 형식)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <action>
① **04-OPEN-ITEMS 기록(네 줄 — 다른 문단은 건드리지 않는다).** 「S9 zero-one-many」 문단 끝에 해소 한 줄: 줄 잔액은 그 클라이언트 원장 전체 누적(서버 계산 뒤 쪽 자르기) · 머리글 잔액은 최종 잔액 · 쪽 크기와 무관(04-07·04-42). 그 아래 세 줄: (a) 진입 경로 이탈 — `role-menu.ts` 대신 `/pnl` 링크, 링크와 대장은 `pnl` 보기 + `reserve.amount` 노출일 때만(B-13·B-15) (b) **원화 잔액의 우회(사용자 D19-1)** — USD 리저브도 잔액은 원화 환산액으로 누적한다, 환율이 다른 날 같은 외화 금액을 출금하면 원화 잔액이 음수로 거부될 수 있다 → 출금을 입금 때 환율로 적는다 (c) **리저브는 Phase 4에 남는다(사용자 D19-11)** — 페이즈 절단 규칙을 적용하지 않는다.

② **UI-SPEC S9·Copywriting(사용자 D6 · CEO 리뷰 B-06 수정안 「S9와 Copywriting을 고친다」).** S9 열 줄에 `· 클라이언트(거래처 Select — 새 줄에서만 편집, 첫 저장 뒤 잠김 · 폰 숨김 — 그룹 머리글이 말한다)`를 더하고 행 끝에 `(CEO 리뷰 2026-09-23 · 사용자 D6)`. Copywriting 표에 두 행을 더하고 행 끝에 `(CEO 리뷰 2026-09-23)`: 「Error — 셀(리저브 클라이언트 잠김)」 `클라이언트는 첫 저장 뒤 바꿀 수 없습니다 · 새 줄로 적어 주세요` · 「Error — 리저브 복원(보관함)」 `복원하면 {날짜} 잔액이 {잔액}이 됩니다 · 리저브 대장에서 출금 줄을 먼저 고쳐 주세요`. 다른 행은 건드리지 않는다.

③ **화면 검증 순서(CLAUDE.md).** 싼 게이트(lint·typecheck·build) → **독립 DOM 감사**(별도 서브에이전트, model: sonnet, `verification-before-completion` 명시, `CI=true` 프로덕션 빌드 DOM 실측, 스크린샷 육안 금지): 1280·375에서 잔액 열 색 `--fg` · 세금계산서 번호 nowrap · 375 P1 세 열 · 가로 스크롤 0 · 긴 메모 한 줄 말줄임 · 페이지 줄 번호 44×44 · 금액 쉼표 · N=0/1/51 렌더(backstop) → 발견 수정(`reserves.module.css`만) → 전체 게이트 **한 번** `CI=true pnpm test`. 감사 보고(항목별 실측값)를 SUMMARY에 붙인다.
  </action>
  <verify>
    <automated>grep -c '클라이언트는 첫 저장 뒤 바꿀 수 없습니다' .planning/phases/04-project-quote-ledger/04-UI-SPEC.md</automated>
    <fails_when>출력이 1이 아니다 — Copywriting 행이 없거나 두 번 들어갔다</fails_when>
    <automated>grep -c 'D19-1\|D19-11' .planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md</automated>
    <fails_when>출력이 2보다 작다 — 원화 잔액 우회나 Phase 4 유지 기록이 빠졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 보고서(1280·375, 항목별 실측값)를 SUMMARY에서 확인</test>
      <expected>잔액 열 `--fg` · 세금계산서 번호 nowrap · 375 P1 세 열 · 가로 스크롤 0 · 메모 한 줄 말줄임 · 페이지 번호 44×44 · N=0/1/51 렌더가 계약대로</expected>
      <why_human>폭·색·말줄임·렌더 배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 04-OPEN-ITEMS에 S9 해소 · 진입 경로 이탈 · 원화 잔액 우회(D19-1) · Phase 4 유지(D19-11) 네 줄이 있고 다른 문단 diff가 0줄이다
    - UI-SPEC S9 열 줄에 클라이언트가 있고 Copywriting에 두 행이 더해졌으며 다른 행 diff가 0줄이다
    - 독립 DOM 감사 보고서가 SUMMARY에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>리저브 화면의 스펙 이탈·사용자 결정·우회가 문서에 남고, 대장이 폭·색 계약을 DOM 실측으로 지키며 전체 게이트가 초록이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 리저브 저장 액션 | 줄·보관 id·클라이언트·환율이 넘어온다. 판정은 전부 04-07의 서버 |
| 세션 → `/pnl`·`/pnl/reserves` | 상단 메뉴는 누구에게나 다섯 전부라 기획 PM도 `/pnl`에 닿는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-41 | Information Disclosure | `/pnl/reserves` 접근 | high | mitigate | 화면이 `pnl` 보기 + `reserve.amount` 노출을 직접 판정하고 하나라도 없으면 찾을 수 없음(레이아웃 판정에 기대지 않는다 — Phase 1 WR-07). E2E가 기획 PM과 노출 꺼진 계급의 직접 접근 차단을 단언한다(B-15) |
| T-04-41b | Information Disclosure | `/pnl`의 대장 링크 | medium | mitigate | 링크를 같은 두 조건일 때만 그린다 — 404로 가는 링크가 대장의 존재를 알리지 않는다. E2E가 기획 PM 화면의 링크 부재를 단언한다(B-13) |
| T-04-42b | Information Disclosure | 누수 스캔 커버리지 | medium | mitigate | 리저브 DTO·액션을 `leak-scan.test.ts` import에 더해 이후 페이즈의 집계·내보내기가 이 줄을 말없이 더하면 스캔이 잡는다(D-38) |
| T-04-43b | Tampering | 화면이 보낸 잔액·셀 단계 | low | mitigate | 화면은 잔액·클라이언트 잠금·셀 단계를 서버 DTO로만 그리고 보내지 않는다 — 우회 페이로드는 04-07이 거부한다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat d6b41cf -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 전부 통과 · 독립 DOM 감사 보고서가 SUMMARY에 있다
- `ui/table`·`ui/shell`·`docs/design/tokens.css`·`domain/permissions/menus.ts`·`package.json`·`pnpm-lock.yaml` diff가 비어 있다
</verification>

<success_criteria>
- 경영관리가 `/pnl`에서 리저브 대장으로 들어가 클라이언트별 입출금을 적고 잔액을 줄마다 본다
- 음수가 되는 기록이 그 자리의 오류 셀로 막히고, 삭제는 잔액 판정과 함께 저장된다
- 기획본부와 노출이 꺼진 계급에게는 링크도 화면도 없다
- 스펙 이탈과 사용자 결정(D6·D19-1·D19-11)이 문서에 남았다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 라우트 | `app/(app)/pnl/reserves/`(page · reserves-table · actions · actions.registry · module.css) |
| Server Action | `saveReservesAction`(줄 + `archivedIds`) — 등록 포함 |
| 변경된 기존 심볼 | `app/(app)/pnl/page.tsx`(대장 링크 — `can` + `visible`) · `leak-scan.test.ts` import 한 줄(액션 레지스트리) |
| 재사용(변경 없음) | `ui/table` · `ui/pagination`(04-29 — 링크 갈래) · `lib/format-number.ts` · 쉼표 입력(04-09) · `SelectOption.description`(04-25) · 04-07의 `listReserves`·`saveReserves` |
| 문서 | `04-OPEN-ITEMS.md` 네 줄 · `04-UI-SPEC.md` S9 열 줄 + Copywriting 두 행 |
| 테스트 | `test/e2e/reserves.spec.ts`(신규) |

## Review Dispositions Ledger

### Round 1 — 06a6f1b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-06 · 사용자 D6 (T12) — 새 줄의 클라이언트를 정할 방법 없음(화면) | CRITICAL | Plan 04-42, Task 1 ④⑥: 9번째 열 클라이언트 Select · 저장 뒤 잠김 E2E · Task 3 ②: S9·Copywriting |
| B-13 — `/pnl`은 세션만 보고 메뉴는 다섯 전부라 PM이 404 링크를 봄 | WARNING | Plan 04-42, Task 1 ②: 링크 `can` + `visible` · Task 2 ⑥: 링크 부재 E2E |
| B-15 — 보기 권한 + 노출 꺼짐에서 부분 노출(화면) | WARNING | Plan 04-42, Task 1 ③: 두 조건 404 · Task 2 ⑥ E2E |
| B-04 · T5 — 경영관리가 보관함 권한 없이 삭제(화면) | CRITICAL | Plan 04-42, Task 2 ③: 삭제 모달 → `archivedIds` 일괄 저장 · E2E |
| B-12 — 최근 환율 기본값 · touched(화면) | WARNING | Plan 04-42, Task 2 ④ |
| 사용자 D19-1 — 원화 잔액 우회 문서화 | DECISION | Plan 04-42, Task 3 ①(b) |
| 사용자 D19-2 — 같은 날 순서(화면) | DECISION | Plan 04-42, Task 2 behavior · E2E |
| 사용자 D19-11 — 리저브 Phase 4 유지 | DECISION | Plan 04-42, Task 3 ①(c) |
| B-34 — `ui/table` 변경 플랜과 병렬 | NOTE | Plan 04-42 `depends_on` 04-07(→ 04-18, 마지막 `ui/table` 변경 · 04-19는 04-18 앞) — 표 변경 뒤에 선다 · `ui/table` diff 0줄 검증 |
| B-35 — 실행 태스크의 `.planning` 편집 | NOTE(ALREADY-COVERED) | `<probe_fallback>`에 선례·사용자 인지 항목으로 명시 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| `/pnl` 화면 자체의 권한 판정(세션만) | 손익 원장 화면은 Phase 9 — 이 플랜은 링크 조건만 고친다(HOLD SCOPE) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-42-SUMMARY.md` when done
</output>
