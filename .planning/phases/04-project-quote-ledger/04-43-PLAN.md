---
phase: 04-project-quote-ledger
plan: 43
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PROJ-03, PROJ-04]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md PROJ-04가 다섯 상태(수주중·진행·정산·완료·미수주)·진행→정산 자동 전환·정산→완료의 대표/시스템 관리자 직접 전환·정산에서 PM의 실행가 편집과 견적가 0인 새 줄(CEO-D10·D12)을 말하고, PROJ-03이 계약 금액을 입력 칸이 아니라 고객 승인된 현재 차수 견적 합계로 말하며, ROADMAP.md Phase 4 성공 기준 4가 같은 모델로 고쳐진다 — **이 세 곳만** 고친다(D-75·D-76·D-79·D-84, 사용자 D16)"
    - "두 파일의 변경이 `docs(04):` 제목의 GSD 도구 커밋 하나로 남고, ROADMAP.md diff는 성공 기준 4 한 줄뿐이다(Goal·다른 기준·트레일링·Plans 목록 블록·진행 표 diff 0)"
    - "D16 밖의 낡은 문구(ROADMAP Goal·기준 1·5·트레일링 · PROJ-06)는 고치지 않고 SUMMARY 「남은 낡은 문구」 표로 남아 사용자가 `/gsd-phase` 편집 여부를 정한다"
  prohibitions:
    - statement: "사용자가 승인한 세 문구(PROJ-04 · PROJ-03 · ROADMAP 기준 4) 밖의 `.planning/` 파일을 이 플랜이 손으로 고쳐서는 안 된다. CLAUDE.md는 `.planning/` 수동 편집을 금지하고, 사용자는 D16에서 이 세 곳만 예외로 승인했다 — 나머지 낡은 문구와 계획 기본값 기록은 SUMMARY에 남긴다"
      status: resolved
      verification: test
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROJ-04(다섯 상태 모델) · PROJ-03(파생 계약 금액) 두 줄"
      contains: "**PROJ-04**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 성공 기준 4 한 줄(사람·자동 전환 · 정산 편집 범위 · 파생 계약 금액)"
      contains: "정산 → 완료"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/04-project-quote-ledger/04-CONTEXT.md"
      via: "두 줄의 문장마다 결정 ID(D-75·D-76·D-78·D-79·D-80·D-82·D-84 · 사용자 D10·D11·D12)가 붙어 CONTEXT 원문으로 거슬러 간다"
      pattern: "D-84"
---

<objective>
**As a** 요구사항·로드맵을 기준으로 페이즈를 검증하는 사용자, **I want to** PROJ-04·PROJ-03·ROADMAP 기준 4가 결정된 다섯 상태·파생 계약 금액 모델을 말하기를, **so that** `/gsd-verify-work`가 옛 네 상태 문구로 이 페이즈를 판정하지 않는다.

**2026-09-23 재계획(디자인 리뷰 반영 — 그룹 C)으로 04-08에서 떼어 낸 플랜.** 04-08의 옛 Task 3을 **내용 그대로** 옮겼다. 04-08이 교차 그룹 계약 1·2(`Button` 개정 · `ui/confirm-dialog`)와 SYSTEM.md 개정 ⑫~⑯을 떠안아 커졌는데, 그 코드 부분은 04-08에서 뗄 수 없어(⑦ 「문서와 같은 커밋」 · 확인 모달 → `Button` · `project-form.tsx` 공유) 파일이 전혀 겹치지 않는 이 태스크를 뗐다(그 뒤 체커 revision 1에서 계약 1·2의 코드 부분 — `Button` 개정 · `ui/confirm-dialog` · 입력 버리기 — 은 **04-46**으로 옮겨졌다). 04-08과 같은 웨이브 3이고 서로 기다리지 않는다(겹치는 파일 0). 문구는 사용자 D16이 승인한 세 곳 그대로이고, 디자인 리뷰 결정(DR-1~DR-38)과 어긋나는 문장이 없다 — PROJ-04의 정산 편집 범위는 CEO-D10·D12(= UI-SPEC rev 5 S4)와 같다.

Purpose: 계획 문서의 요구사항 문구를 결정된 모델에 맞춘다(D16 — 세 곳만).

Output: REQUIREMENTS.md 두 줄 · ROADMAP.md 한 줄 · SUMMARY 「남은 낡은 문구」 표.

**실행 규율:** 「완료」를 말하기 전에 superpowers `verification-before-completion` 스킬을, 검증이 실패하면 쫓기 전에 `systematic-debugging` 스킬을 호출한다(이 플랜은 코드가 없어 `test-driven-development`는 해당 없음 — 검증 명령이 계약이다). 서브에이전트에 위임하면 프롬프트에 스킬 이름과 `model`을 명시한다. 커밋은 GSD 도구 커밋 하나다.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
# 범위 Read:
# .planning/phases/04-project-quote-ledger/04-RESEARCH.md 835–842행(§10 — PROJ-04·기준 4·PROJ-03의 현재 문구와 바뀐 이유)
# .planning/phases/04-project-quote-ledger/04-CONTEXT.md D-75~D-84(83–100행)
# docs/designs/plant8-erp-phase4-ceo-review-260923.md 34행(결정 원장 C-12 · 사용자 D16)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-03·PROJ-04의 요구사항 문구**다. 동작 수용 기준은 결정된 모델(D-75~D-84)을 기준으로 04-06·04-11·04-16·04-20·04-21이 적는다 — 이 플랜은 문구만 고친다. 04-06:110 · 04-20:143의 「04-08 Task 3이 문구를 고친다」는 이 플랜을 가리킨다(뜻 무변경 — 그 두 플랜은 다른 그룹 소유라 여기서 고치지 않는다).

**계획 단계 가정(표면화):** 없음 — 문구 원문은 옛 04-08 Task 3(CEO 리뷰 C-12 · 사용자 D16 승인)을 그대로 쓴다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — REQUIREMENTS·ROADMAP 세 문구(D16 — PROJ-04 · PROJ-03 · 기준 4)를 고치고 GSD 도구 커밋 하나로 남긴다</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-RESEARCH.md` 835–842행(§10 — PROJ-04·기준 4·PROJ-03의 현재 문구와 바뀐 이유)
    - `.planning/phases/04-project-quote-ledger/04-CONTEXT.md` D-75~D-84 원문
    - `docs/designs/plant8-erp-phase4-ceo-review-260923.md` 34행(결정 원장 C-12 · 사용자 D16 「이 세 곳만 승인」)
    - `.planning/REQUIREMENTS.md` 26–32행(PROJ-01~07) — 고칠 두 줄만 Read
    - `.planning/ROADMAP.md` — `### Phase 4` 절의 성공 기준 4 한 줄만(Grep `  4. 프로젝트에 계약 금액`). **Goal·다른 기준·트레일링 문단·Plans 목록 블록은 건드리지 않는다**
    - `git show edd0d73 --stat` — 1차 보강이 쓴 커밋 형태(문구만 · `docs(04):` 제목 · GSD 도구 커밋)
  </read_first>
  <action>
① **REQUIREMENTS.md — 두 줄만.** 체크 상태·추적 표는 건드리지 않는다.
- PROJ-04: 상태는 수주중 → 진행 → 정산 → 완료와 미수주 **다섯** 가지. 진행 → 정산은 종료일 다음 날 00:00(KST)에 시스템이 바꾼다(D-76). 사람의 전환은 수주중 → 진행(시작일 필요, 종료일이 비면 시작일로, D-82)·수주중 → 미수주·미수주 → 진행 복귀(팀장 이상 — 팀장은 자기 팀, 사용자 D11), 정산 → 완료(Phase 4는 대표·시스템 관리자 직접, Phase 5부터 PM 결재 요청 → 대표 승인, D-79), 정산 → 진행은 팀장 이상이 종료일을 늦출 때(D-80). 정산에서 PM은 실행가를 고치고 견적가 0인 새 줄을 더할 수 있다(D-78, 사용자 D10·D12). 잠기는 상태는 완료 하나뿐이고 완료된 프로젝트에만 새 지출결의를 올릴 수 없다. 수주중 비용·미수주 비용 문장은 그대로 둔다.
- PROJ-03: 「계약 금액」은 입력 칸이 아니라 고객 승인이 표시된 현재 차수의 견적 합계(공급가)이고, 세금계산서 발행일·발행액과 입금일·입금액을 기록한다. 발행액은 공급가액으로 적고 부가세·합계는 자동, 입금액은 통장 합계로 적고 역산. 별도 매출 모듈 없음(D-84).

② **ROADMAP.md — 성공 기준 4 한 줄만.** 「세금계산서 발행일/발행액·입금일/입금액을 적을 수 있고 계약 금액은 고객 승인된 현재 차수 견적 합계에서 파생된다. 상태 전환(사람) 수주중 → 진행 · 수주중 → 미수주 · 미수주 → 진행 복귀 · 정산 → 완료 · 정산 → 진행(종료일 늦추기)과 자동 전환 진행 → 정산이 전부 3의 게이트를 지난다. 정산은 실행가와 견적가 0인 새 줄만 고치는 상태이고 잠기는 상태는 완료 하나뿐이다」로 바꾸고, 괄호 안 Phase 5·6·9 문장과 음수 규칙·취소 상태 문장은 그대로 둔다. **Goal · 기준 1·2·3·5·6·7 · 트레일링 문단 · Plans 목록 블록 · 진행 표는 한 글자도 바꾸지 않는다**(D16).

③ 두 파일을 한 커밋으로 `node .claude/gsd-core/bin/gsd-tools.cjs query commit "docs(04): D-75~D-84 2차 보강 — PROJ-04·PROJ-03·기준 4 (D16)" --files .planning/REQUIREMENTS.md .planning/ROADMAP.md`로 커밋한다(1차 보강 `edd0d73`과 같은 경로 — 손 커밋이 아니라 GSD 도구 커밋).

④ SUMMARY에 「남은 낡은 문구」 표(자리 · 지금 문구 · 결정 ID · D16으로 고치지 않은 이유)를 적는다 — ROADMAP Goal의 `수주중→진행→완료(정산)` · 기준 1의 `목록 합계(견적·실행가·차익)`(D-87·D-88·D-90, 수익금 기준은 DR-8·DR-38) · 기준 5의 `매출 칸의 계약 금액·발행액` · 트레일링 문단의 `완료(정산)` · PROJ-06의 `완료(정산)`. 사용자가 `/gsd-phase` 편집으로 고칠지 정한다.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const line=r.split('\n').find(l=>l.includes('**PROJ-04**'))||'';const need=['수주중','진행','정산','완료','미수주','다섯'];const miss=need.filter(w=>!line.includes(w));const p3=r.split('\n').find(l=>l.includes('**PROJ-03**'))||'';if(miss.length||!p3.includes('고객 승인')){console.error('REQ missing: '+miss.join(',')+(p3.includes('고객 승인')?'':' PROJ-03'));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 오류에 `REQ missing:`으로 시작하는 줄이 나오고 종료 코드가 1이다 — PROJ-04가 다섯 상태 모델을, PROJ-03이 고객 승인 파생 계약 금액을 말하지 않는다</fails_when>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/ROADMAP.md','utf8');const s=r.slice(r.indexOf('### Phase 4'),r.indexOf('### Phase 5'));const c4=s.split('\n').find(l=>l.trim().startsWith('4.'))||'';const bad=['진행 → 완료(정산)','계약 금액·세금계산서'].filter(b=>c4.includes(b));if(!c4||bad.length||!c4.includes('정산 → 완료')){console.error('ROADMAP c4 stale: '+bad.join(' | '));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 오류에 `ROADMAP c4 stale:`로 시작하는 줄이 나오고 종료 코드가 1이다 — 성공 기준 4에 옛 전환 문구나 계약 금액 입력 문구가 남았거나 새 전환이 없다</fails_when>
    <automated>node -e "const {execFileSync}=require('child_process');const c=execFileSync('git',['log','-1','--format=%H','--','.planning/REQUIREMENTS.md']).toString().trim();const d=execFileSync('git',['diff',c+'^..'+c,'--','.planning/ROADMAP.md']).toString();const n=d.split('\n').filter(l=>/^[-+][^-+]/.test(l)).length;if(n>2){console.error('ROADMAP diff lines: '+n);process.exit(1)}console.log(n)"</automated>
    <fails_when>표준 오류에 `ROADMAP diff lines:`가 나오고 종료 코드가 1이거나 `git`이 실패해 0이 아닌 코드로 끝난다 — 이 보강 커밋(REQUIREMENTS.md를 바꾼 마지막 커밋으로 고정한 범위)에서 기준 4 한 줄(삭제 1·추가 1) 밖의 ROADMAP 줄이 바뀌었다(D16 위반)</fails_when>
    <automated>git log -1 --format=%s -- .planning/REQUIREMENTS.md</automated>
    <fails_when>출력 제목이 `docs(04):`로 시작하지 않는다 — 보강이 GSD 도구 커밋으로 남지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md의 PROJ-04 줄이 다섯 상태·자동 정산·대표/시스템 관리자 완료·정산의 실행가 편집과 견적가 0 새 줄을 말한다
    - REQUIREMENTS.md의 PROJ-03 줄이 계약 금액을 파생값으로 말하고 입력 칸이라고 말하지 않는다
    - REQUIREMENTS.md의 추적 표(요구사항 × 페이즈 × 상태)와 체크박스 상태 diff가 0줄이고, PROJ-04·PROJ-03 밖의 줄 diff가 0줄이다
    - ROADMAP.md diff가 성공 기준 4 한 줄뿐이다(Goal·다른 기준·트레일링·Plans 블록 diff 0줄)
    - 두 파일이 `docs(04):` 제목의 한 커밋이다
    - SUMMARY에 「남은 낡은 문구」 표가 있다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>사용자가 승인한 세 문구가 D-75~D-84 모델과 일치하고, 승인 밖의 낡은 문구는 고치지 않은 채 목록으로 남았다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 실행자 → `.planning/` | 사람이 승인한 범위 밖의 계획 문서 수동 편집 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-50 | Repudiation | 요구사항 문구 변경 | low | mitigate | 문구 변경을 GSD 도구 커밋 하나로 남기고 결정 ID(D-75·D-76·D-79·D-84·D10·D11·D12)를 문구에 붙여 추적 가능하게 한다. D16 범위 밖 줄의 diff 0을 검증 명령으로 본다(04-08에서 옮긴 위협 — 같은 ID) |
| T-04-364 | Tampering | 승인 범위 밖 계획 문서 편집 | low | mitigate | ROADMAP diff를 REQUIREMENTS 커밋 기준으로 두 줄 이하로 묶는 검증 명령과 `docs(04):` GSD 도구 커밋 단언 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(문서만 고친다). `package.json`의 의존성 객체와 `pnpm-lock.yaml`이 d6b41cf와 같다 |
</threat_model>

<verification>
- 네 검증 명령(REQUIREMENTS 두 줄 · ROADMAP 기준 4 · ROADMAP diff 두 줄 이하 · `docs(04):` 제목) 초록
- 이 플랜의 diff가 `.planning/REQUIREMENTS.md`·`.planning/ROADMAP.md` 두 파일뿐이다
</verification>

<success_criteria>
- 사용자가 승인한 세 문구가 결정된 다섯 상태·파생 계약 금액 모델과 일치한다
- 승인 밖의 낡은 문구는 SUMMARY 표로 남았다
- 04-08과 파일이 겹치지 않아 같은 웨이브에서 서로 기다리지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 계획 문서 | `.planning/REQUIREMENTS.md` PROJ-03·04 문구 · `.planning/ROADMAP.md` Phase 4 기준 4 |
| 기록 | SUMMARY 「남은 낡은 문구」 표 |
| 코드·스키마·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 06a6f1b (옛 04-08 Task 3)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| C-12 / 사용자 D16 — `.planning/` 수동 편집 범위 | DECISION | Plan 04-43, Task 1: PROJ-04·PROJ-03·기준 4 세 곳만, GSD 도구 커밋, 범위 밖 diff 0 검증 |

### Round 3 — 98acc74 (plan-design-review 재계획 — 그룹 C)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 04-08 크기(교차 그룹 계약 1·2 추가) | NOTE | 04-08의 옛 Task 3을 파일이 겹치지 않는 이 플랜으로 옮겼다(내용 무변경, 웨이브 3) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| ROADMAP Goal·기준 1·5·트레일링 · PROJ-06의 낡은 문구 | 사용자 D16이 세 곳만 승인 — SUMMARY 표로 넘겨 사용자가 `/gsd-phase` 편집 여부를 정한다 |

### Round 4 — 체커 revision 1 후속 정리(2026-09-23 · 04-08 분할 포인터)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 분할 포인터 — 교차 그룹 계약 1·2의 현재 주인 | NOTE | objective 첫 문단에 괄호 한 줄: 계약 1·2 코드 부분은 이제 **04-46**. 이 플랜의 범위(REQUIREMENTS·ROADMAP 세 문구)와 `depends_on`은 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-43-SUMMARY.md` when done
</output>
