---
phase: 04-project-quote-ledger
plan: 44
type: execute
wave: 12
depends_on: ["04-22"]
files_modified:

  - domain/projects/pre-estimate.ts
  - domain/projects/index.ts
  - domain/rules/register.ts
  - domain/projects/ledger.ts
  - repositories/projects.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/pre-estimate-field.tsx
  - app/(app)/projects/[id]/status-change.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/project-pre-estimate.test.ts
  - test/integration/project-period.test.ts
  - test/e2e/project-period.spec.ts

autonomous: true
requirements: [PROJ-04, PROJ-07, UX-04, FX-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "상세 부제에 `총 매출 예상가 50,000,000`(외화면 ` · USD 40,000.00 @1,350`을 이어 쓴다, 값이 없으면 `총 매출 예상가 —`)이 보이고, 기간 칸과 같은 권리(`periodEditRights`)가 있는 사람에게만 바로 오른쪽 3차 「총 매출 예상가 바꾸기」가 있다 — 권리가 없으면 값만. 현재 차수 견적 합계·계약 금액과 별도 값이다(견적 줄로 채우지 않는다, D-52)(DR-28 · DR-37 · 계약 8, UI-SPEC rev 5 `## UI Considerations` S17 populated · partial · 해소 DR-37)"
    - "저장된 총 매출 예상가 0은 「입력하지 않음」이다 — 부제가 `총 매출 예상가 —`이고 스키마를 바꾸지 않는다(사용자 2026-09-23 확정 · S17 empty)"
    - "「총 매출 예상가 바꾸기」를 누르면 머리 줄 아래(기간 칸 묶음이 열려 있으면 그 아래)에 금액(280) · 통화 Select(200) · 환율(280, KRW면 숨김, 기본값 `fx.recent_rate.USD`) 한 묶음이 열리고 부제의 값과 3차는 칸이 열린 동안 숨는다. 저장 성공 뒤 600ms 틴트 후 묶음이 닫히고 부제가 새 값이다(토스트 없음). 환율을 고친 USD 저장만 최근 환율 키를 갱신한다(S17, D-71)"
    - "총 매출 예상가 칸은 `Form.Actions`가 없는 칸 폼이다 — 고치면 dirty가 되어 1차 「일괄 저장 N」의 N에 바뀐 칸 수만큼 합류하고 견적 줄·매출·기간과 같은 트랜잭션으로 저장된다. Enter 기본 제출은 막히고 Ctrl+S가 저장이며, `Esc`는 편집 중 값을 되돌리고 칸이 모두 원래 값이면 묶음을 닫고 포커스가 3차로 돌아온다(S17 · §7-15, 04-22 기간 칸과 같은 형태)"
    - "총 매출 예상가 칸 오류는 칸 아래 `Form.Error` — `숫자가 아닙니다 · 12,400,000처럼 적어 주세요` · `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요` · 환율 행은 `환율이 없습니다 · USD 환율을 적어 주세요` · `환율은 0보다 커야 합니다 · 환율을 고쳐 주세요` — 화면 전체 거부이고 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`이 붙는다(서버도 같은 함수)(S17 error · U-6, rev 5 Copywriting `Error — 총 매출 예상가 칸`)"
    - "총 매출 예상가를 두 사람이 따로 고쳐 저장하면 나중 저장이 이긴다 — 버전·기준값 검사를 두지 않는다(사용자 2026-09-23 확정 · rev 5 S17에 동시 수정 규칙 없음)"
    - "1차 「일괄 저장」의 서버 계산 `canSave`에 총 매출 예상가 권리 항(`periodEditRights !== \"none\"` 그리고 `quote.amount` 노출)이 더해진다 — 권한 메뉴가 없는 같은 팀 팀장도 총 매출 예상가만 고쳐 저장할 수 있다(CEO A-12, S19 partial)"
    - "총 매출 예상가가 실린 합성 저장의 트랜잭션 안에서는 풀을 부르지 않는다 — 권리의 사실(04-22가 읽는 것과 같다)과 `quote.amount` 노출은 트랜잭션을 열기 **전에** 읽고, 최근 환율 기억은 커밋 **뒤**에 한다(ENG-D3 ① — ARCHITECTURE §4-8)"
    - "상태 바꾸기 확인 모달의 막힘 3차 「기간 적기」(`ui/confirm-dialog`의 `primary.nextStep`)는 모달을 닫고 머리 줄 기간 칸을 열어 **시작일**에 포커스하고, 진행으로 바꾸기·진행으로 되돌리기 모달의 결과 줄 `종료일 지남 · 바로 정산` 오른쪽 3차 「기간 바꾸기」는 모달을 닫고 **종료일**에 포커스한다 — 둘 다 기간 권리가 없으면 3차 없이 글자만(S7 · S13, rev 5 Copywriting `확인 — 진행으로 바꾸기 · 진행으로 되돌리기`)"
    - "폰(<700) 머리 줄의 부제 **첫 항목**은 `기간 …` + 「기간 바꾸기」이고 나머지 부제(`번호 · 상세 견적 n차 · … · 총 매출 예상가 … · 정산 날짜`)가 그 아래 줄바꿈된다 — 04-21이 만든 폰 순서(제목 → 상태 태그 → 부제 → 버튼 줄)의 부제 안 순서다. 1차 「일괄 저장 N」의 폰·좁은 PC 렌더 조건은 N ≥ 1이면 렌더(표 밖 칸 · 표 셀 dirty — 「복원」 칸 포함, 안내 글자 없음)이고 04-30 Task 3 ④가 만든다(UI-SPEC rev 5 후속 결정 R1 47행 — 옛 「표 밖 칸이 dirty일 때만」을 대신한다 · DR-26 · DR-36, S3 폰 첫 화면)"
    - "총 매출 예상가·상태 모달 연결 E2E는 계급 권한·정보 노출을 **손으로 켜지 않는다** — 04-20 시드만으로 같은 팀 팀장이 고치고 머리 줄을 본다(ENG-D2)"
    - statement: "기간 칸·총 매출 예상가 칸이 열려도 머리 줄이 1280 · 1024 · 375 세 폭에서 가로 스크롤 0이고 칸 묶음이 머리 줄 아래에 붙으며(머리 줄 안에 입력을 두지 않는다), 375에서는 칸 묶음이 전폭·라벨 위이고 부제 첫 항목이 `기간 …`이다 — DOM 감사(UI-SPEC rev 5 `## UI Considerations` S3 overflow · 폰 첫 화면 · S17)"
      verification: backstop
  artifacts:
    - path: "domain/projects/pre-estimate.ts"
      provides: "총 매출 예상가 칸 검증(순수 — 화면과 서버가 같은 함수, rev 5 문구 원문)"
      exports: ["validatePreEstimateChange"]
    - path: "app/(app)/projects/[id]/pre-estimate-field.tsx"
      provides: "부제 `총 매출 예상가 …` + 3차 + 머리 줄 아래 칸 묶음(금액 · 통화 · 환율) — Form.Actions 없음, dirty 합류"
      contains: "validatePreEstimateChange"
    - path: "test/unit/domain/project-pre-estimate.test.ts"
      provides: "총 매출 예상가 검증 결정표(KRW · USD · 음수 · 숫자 아님 · 환율 없음 · 환율 0 · KRW 환율 1)"
      min_lines: 30
  key_links:
    - from: "app/(app)/projects/[id]/pre-estimate-field.tsx"
      to: "domain/projects/pre-estimate.ts"
      via: "칸 오류를 서버 저장과 같은 검증 함수로 만든다"
      pattern: "validatePreEstimateChange"
    - from: "domain/projects/ledger.ts"
      to: "domain/projects/period.ts"
      via: "총 매출 예상가 권리가 기간과 같은 `periodEditRights`에서 온다(DR-37 — 별도 권리 함수 없음)"
      pattern: "periodEditRights"
    - from: "domain/projects/ledger.ts"
      to: "domain/money/index.ts"
      via: "쓰기가 `moneyToColumns`를 지나 04-40의 `normalizeMoneyInput`을 자동으로 따른다"
      pattern: "moneyToColumns"
    - from: "app/(app)/projects/[id]/status-change.tsx"
      to: "app/(app)/projects/[id]/quote-table.tsx"
      via: "막힘 3차 「기간 적기」와 결과 줄 3차 「기간 바꾸기」가 QuoteLedger의 openPeriodField(04-22)를 부른다(04-21의 `StatusChangeProps`를 넓힌다)"
      pattern: "openPeriodField"
---

<objective>
**As a** 기획 PM·팀장, **I want to** 상세 머리 줄 부제에서 등록 때 적은 총 매출 예상가를 보고 기간 칸과 같은 방식·같은 권리로 고치며, 상태 바꾸기 모달이 기간 때문에 막히면 모달에서 바로 기간 칸으로 가기를, **so that** 사전 견적 값이 등록 뒤에도 보이고 고쳐지며, 막힌 전환의 다음 한 수가 한 번의 누름이다.

머지 묶음: ②/4 상태 전환 — 웨이브 2~18(04-50 → 04-23)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**실행 규율 보강(2026-09-24 재계획 — CLAUDE.md · hooks 3bed7b1 · 세션 B 교훈):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋(문서 포함)과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(플랜 하나 — 체크포인트 이어가기도 새 세션). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 화면 검증은 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번이고, 완료 판정은 로컬 dev가 아니라 `CI=true`다.

**2026-09-23 체커 revision 1 분할(B5).** 옛 04-22(21파일 · 128k · 네 관심사)를 둘로 나눈 **뒤쪽 절반**이다 — 옛 04-22 Task 3(상태 모달 연결 · 총 매출 예상가 칸 · 폰 부제 순서 · 머리 줄 화면 검증)을 내용 그대로 옮기고 세 태스크(트레이서 · 확장 · 연결·화면 검증)로 나눴다. 기간 칸 · `openPeriodField` · `periodEditRights` · 합성 저장 tx 순서 · 필수 `seenStatus`(DR-6) · 미저장 보관 배선(D-68 `persist`)은 04-22가 먼저 만들었다 — 이 플랜은 그 위에 선다. 사슬은 04-22 → **04-44** → 04-12로 곧게 이어진다(04-12의 `depends_on`이 04-22에서 04-44로 바뀌었다).

Purpose: DR-28 · DR-37 · 계약 8 — 등록 폼에서 받는 총 매출 예상가(D-52)가 상세에 자리가 없었다. 기간 칸과 같은 자리·형태·권리로 두어 머리 줄 아래 표 밖 칸이 둘이 되고, 04-30(저장 중 잠금 · 좁은 폭 1차 규칙)은 이 두 칸을 함께 잠근다. S7의 「기간 적기」·「기간 바꾸기」는 04-22의 기간 칸으로 가는 유일한 모달 경로다.

Output: 순수 검증 모듈 하나 · DTO 필드 `preEstimate` · 게이트 규칙 `project.pre-estimate-edit` · 합성 저장의 `preEstimate` 입력 · 총 매출 예상가 칸 컴포넌트 · 상태 모달 3차 둘 · 폰 부제 순서 · 단위 1 · 통합 케이스 o~o4(04-22 파일 확장) · E2E(04-22 파일 확장) · 머리 줄 독립 DOM 감사(1280 · 1024 · 375).

**반영된 리뷰(옛 04-22에서 그대로):** CEO A-12(`canSave` 서버 계산) · A-31(04-21의 `StatusChangeProps`를 넓힘 — ReactNode 재작업 없음) · ENG-D2(E2E가 권한·노출을 켜지 않음) · ENG-D3 ①(트랜잭션 전 사실) · 디자인 리뷰 DR-28 · DR-37 · 계약 8 · DR-26 · DR-14 · rev 5 S7 연결. 사용자 2026-09-23 확정 두 건(나중 저장이 이김 · 0 = 미입력)을 truths에 적었다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion` 호출. 서브에이전트 위임 시 세 스킬과 `model`을 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 시간은 `deps.now`로 주입한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-21-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다(U-2는 `## UI Considerations` S5 partial 529행의 사용자 확정값). 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md(rev 5 — rev 4 줄 번호는 쓰지 않는다) `## UI Considerations` 「S3」 490–500행(overflow · 폰 첫 화면) · 「S17」 628–637행 · `## 적용 규칙` 「S3」 부제의 `총 매출 예상가` 1126–1127행 · 폰 첫 화면 1151–1155행 · 「S7」 막힘 1335–1337행 · 「S17. 상세 총 매출 예상가 칸」 1528–1544행 · `## Copywriting Contract` `Primary CTA — 견적 원장` 341행 · `3차 버튼` 345행 · `Error — 표 배치 저장 실패(U-6)` 362행 · `Error — 총 매출 예상가 칸` 377행 · `표시 — 총 매출 예상가(DR-28)` 412행 · `확인 — 진행으로 바꾸기 · 진행으로 되돌리기` 427행

# @.planning/phases/04-project-quote-ledger/04-46-SUMMARY.md — `ui/confirm-dialog`(`primary.nextStep`·`resultLines`)의 실제 props(계약 2)

# docs/design/SYSTEM.md §7-15(Form.Hint · 칸 폼)

# docs/ARCHITECTURE.md §4-8(04-32 — 트랜잭션·잠금 규약)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-07(사전 견적의 표시·수정)** · **FX-01(총 매출 예상가의 통화·환율)** · **PROJ-04(상태 모달 → 기간 칸)** 다. 수용 기준은 D-52 · D-71 · DR-28 · DR-37과 UI-SPEC rev 5 S3 · S7 · S17에서 끌어와 `must_haves.truths`에 적었다. S17의 다섯 상태 중 empty · error · populated · partial은 truths로, loading(저장 중 잠금)은 04-30으로, long-text는 해당 없음(숫자 칸)이다. S3 overflow · 폰 첫 화면은 두 칸이 열린 머리 줄의 backstop으로 옮겼다(옛 04-22 그대로).

**계획 단계 가정(사용자 확정 반영):**

- **총 매출 예상가(계약 8 — 옛 04-22 판단 그대로):** 값은 프로젝트의 `pre_estimate_*` 돈 칸(04-01 — `amount_krw` NOT NULL, 등록 기본 0)이라 **0을 미입력으로 보고** 부제에 `—`를 쓴다(S15 「0과 빈 값」 — **사용자 2026-09-23 확정**, 스키마 변경 없음). DTO 투영 정보 항목은 `quote.amount`(견적 금액과 같은 계열 — 새 항목 없음), 권리는 `periodEditRights` + 그 노출(DR-37 — 볼 수 없는 값은 고칠 수 없다). 동시 수정 기준값은 두지 않는다 — **나중 저장이 이긴다(사용자 2026-09-23 확정)**. 쓰기는 `moneyToColumns`를 지나 04-40의 `normalizeMoneyInput`을 자동으로 따르고, 등록 폼의 입력 칸·복사 규칙은 그룹 B 04-15 그대로다(04-15는 「표시 자리 없음」 메모만 지운다 — 계약 8).
- **방어 문구(rev 5에 없는 서버 거부):** `총 매출 예상가 바꾸기 권한 없음`은 화면이 3차를 렌더하지 않아 위조 요청으로만 닿는다 — rev 5 명사형 `원인 · 다음 행동` 꼴을 따르고 SUMMARY 「새 문구」 표에 적는다(사용자 2026-09-23 승인).
- **합성 저장의 `seenStatus`(04-22 계약 4):** 이 플랜의 통합 케이스가 `saveProjectLedger`를 부를 때는 준비한 프로젝트의 현재 상태를 `seenStatus`로 싣는다(필수 — 04-22가 만든다).
- **트랜잭션 전 사실(ENG-D3 ①):** 총 매출 예상가가 실린 저장은 04-22가 기간에 읽는 사실(`can(viewer, "projects.status", "write")` · `can(viewer, "projects", "write")` · `loadActorTeamScope`)과 `visible(viewer, "quote.amount")`를 트랜잭션을 열기 전에 읽는다. 트랜잭션 안의 권리 판정은 이 사실과 잠근 행만으로 순수하게 한다.
- **E2E 파일:** 옛 04-11이 `project-lifecycle.spec.ts`에 두려던 「진행 확인 모달의 종료일 지남 줄」 케이스는 기간 칸과 같은 준비를 쓰므로 `project-period.spec.ts`에 둔다. E2E 날짜는 전부 `kstToday(new Date())` + `addDays`(CEO A-18). **계급 권한·정보 노출을 켜는 호출(`upsertVisibility`·`upsertPermission`·`setPermissionCell`·`setVisibilityCell`)을 두지 않는다**(ENG-D2 — 시드는 04-20).
- **머리 줄 화면 검증의 자리:** 기간 칸(04-22)과 총 매출 예상가 칸(이 플랜)이 같은 머리 줄 아래 묶음이라 독립 DOM 감사는 두 칸이 모두 선 이 플랜에서 한 번 한다(CLAUDE.md 「전체 게이트를 두 번 돌리지 않는다」).

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 팀장이 수주중 프로젝트의 총 매출 예상가를 부제 3차로 열어 고쳐 일괄 저장한다(순수 검증 → DTO → 게이트 → tx 안 쓰기 → 액션 → 칸 → E2E)</name>
  <files>domain/projects/pre-estimate.ts, test/unit/domain/project-pre-estimate.test.ts, domain/projects/index.ts, domain/rules/register.ts, domain/projects/ledger.ts, repositories/projects.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/pre-estimate-field.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, test/integration/project-period.test.ts, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## 적용 규칙` 「S17. 상세 총 매출 예상가 칸」 1528–1544행 · 「S3」 부제의 `총 매출 예상가` 1126–1127행 · `## Copywriting Contract` `Error — 총 매출 예상가 칸` 377행 · `표시 — 총 매출 예상가(DR-28)` 412행 · `## UI Considerations` 「S17」 628–637행
    - `domain/projects/period.ts`(04-22 — `periodEditRights`) · `domain/projects/ledger.ts`(04-22 뒤 전문 — 트랜잭션 전 사실 · 콜백 순서 (a)~(g) · 기간 판정 (c)와 쓰기 (d)의 자리 · `seenStatus` 비교)
    - `app/(app)/projects/[id]/period-field.tsx`(04-22 — 같은 형태의 칸 폼 · dirty 합류 · 600ms 틴트 · `persist` 호출 자리)
    - `domain/money/index.ts` 55–95행(`moneyFromRow`·`moneyToColumns`) · `domain/money/currency.ts`(`recentFxRate`·`rememberFxRate`) · `db/schema/projects.ts`(`preEstimate` 돈 칸 — `amount_krw`는 NOT NULL, 등록 기본 0)
    - `domain/projects/index.ts` 40–82행(`ProjectDto`·`PROJECT_DTO_SPEC`·`registerDto`) · `domain/permissions/info-items.ts`(`quote.amount`)
    - `ui/input/TextField.tsx`(04-09 — `numberKind`) · `ui/select/Select.tsx` · `lib/format-number.ts`(04-09 — `formatKrw`·`formatForeignLine`)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `openPeriodField`·기간 칸 dirty 합류 자리·`canSave`를 찾아 범위만 Read
  </read_first>
  <behavior>
    - `validatePreEstimateChange`: `{ currency: KRW, amount: 50000000 }` → [] · `amount: -1` → 금액 칸 `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요` · `amount: NaN` → 금액 칸 `숫자가 아닙니다 · 12,400,000처럼 적어 주세요` · `{ currency: USD, amount: 40000, fxRate: null }` → 환율 칸 `환율이 없습니다 · USD 환율을 적어 주세요` · `fxRate: 0` → 환율 칸 `환율은 0보다 커야 합니다 · 환율을 고쳐 주세요` · KRW에 환율 3을 실어도 오류 없이 1로 본다
  </behavior>
  <action>
① **순수 검증(RED → GREEN).** `test/unit/domain/project-pre-estimate.test.ts`에 `behavior` 표를 결정표로 먼저 쓴다(RED). `domain/projects/pre-estimate.ts`(순수 — `domain/money`만 import, 화면 클라이언트 컴포넌트도 import한다): `validatePreEstimateChange({ currency, amount, fxRate })` → 칸별 오류 `{ field: "amount" | "fxRate"; reason }[]` — 문자열은 rev 5 Copywriting `Error — 총 매출 예상가 칸` 원문, 환율 행은 셀 환율 오류와 같은 글자(GREEN).

② **서버(계약 8 · DR-28 · DR-37).** `domain/projects/index.ts`의 `ProjectDto`·`PROJECT_DTO_SPEC`에 `preEstimate`(`moneyFromRow` — 정보 항목 `quote.amount`, `registerDto` 갱신 — 누수 스캔)를 더한다. `domain/rules/register.ts`에 `project.pre-estimate-edit`(ctx `{ rights, canSeeAmount, errors }`: 권리 `none`이거나 금액을 볼 수 없으면 `총 매출 예상가 바꾸기 권한 없음`(rev 5에 없는 방어 문구 — 명사형, SUMMARY 「새 문구」 표), 검증 오류가 있으면 첫 이유)을 등록한다. `SaveProjectLedgerInput`·`saveProjectLedgerAction` 스키마에 `preEstimate?: { currency, amount, fxRate, fxRateTouched }`를 더한다. `ledger.ts`: 트랜잭션 전에 04-22가 기간에 읽는 권리 사실 + `visible(viewer, "quote.amount")`를 읽고(ENG-D3 ① — 기간이 없고 총 매출 예상가만 실린 저장도 같은 사실을 읽는다), 콜백 안에서는 04-22의 기간 판정 (c) **바로 뒤** 같은 잠근 행·같은 권리(`periodEditRights` — DR-37)로 `validatePreEstimateChange` → `gate(lockedRow, "project.pre-estimate-edit", …)`를 돈다. 통과하면 기간 쓰기 (d) 뒤에 `repositories/projects.ts`의 `updateProjectPreEstimate(viewer, id, moneyToColumns(input), tx)`로 쓴다 — `moneyToColumns`를 지나므로 04-40이 거기에 넣는 `normalizeMoneyInput`(KRW 환율 1 · 정수 범위)을 자동으로 따른다. 값이 바뀌었으면 04-22의 기간 변경과 같은 `document_update` 로그 한 줄(기간을 함께 바꿨으면 그 줄)의 detail에 `preEstimateChanged: true`를 더한다(금액은 싣지 않는다). 동시 수정 기준값은 두지 않는다(사용자 2026-09-23 확정 — 나중 저장이 이긴다).

③ **화면(한 경로 — KRW).** `app/(app)/projects/[id]/pre-estimate-field.tsx`(클라이언트): 부제 항목 `총 매출 예상가 {formatKrw}`(외화면 ` · {formatForeignLine}`, `amountKrw`가 0이면 `총 매출 예상가 —` — 사용자 2026-09-23 확정) + 권리가 있으면 3차 「총 매출 예상가 바꾸기」. 누르면 머리 줄 **아래**(기간 칸 묶음이 열려 있으면 그 아래)에 `Form` 한 묶음 — `Form.Field 총 매출 예상가`(280, 04-09 `TextField` `numberKind` — KRW면 `krw`, USD면 `foreign`) · 통화 `Select`(200) · `Form.Field 환율`(280, KRW면 숨김, 기본값 서버가 넘긴 `recentFxRate("USD")`), **`Form.Actions` 없음**. 부제의 값과 3차는 칸이 열린 동안 숨는다. 고치면 칸이 dirty(`--inset-dirty`)가 되어 `QuoteLedger`의 N에 바뀐 칸 수만큼 합류하고 1차가 같은 액션 호출에 `preEstimate`를 싣는다(04-22의 `seenStatus`도 그대로 실린다). 성공 시 600ms `--accent-weak` 틴트(`prefers-reduced-motion`이면 없음) 뒤 닫힘(토스트 없음). `[id]/page.tsx`가 권리(`periodEditRights !== "none"` 그리고 `quote.amount` 노출)·최근 환율을 계산해 넘기고, 04-22의 `canSave` 계산에 총 매출 예상가 권리 항을 더한다.

④ **증명.** `test/integration/project-period.test.ts`(04-22 파일)에 (o) 팀장이 수주중 프로젝트의 총 매출 예상가를 50,000,000으로 저장(`seenStatus: bidding`) → DB `pre_estimate_amount_krw` 50000000 · `document_update` detail `preEstimateChanged` · detail에 금액 키 없음을 더한다. `test/e2e/project-period.spec.ts`(04-22 파일 확장 — 권한·노출을 켜는 호출 없음, ENG-D2): 그 팀에 오늘 발령된 팀장이 수주중 프로젝트 상세 → 「총 매출 예상가 바꾸기」 → 금액 `50000000` → 칸에 `50,000,000` → `Control+s` → 부제 `총 매출 예상가 50,000,000` · 새로 고쳐도 같은 값 · 값이 0인 프로젝트의 부제는 `총 매출 예상가 —`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-pre-estimate.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 총 매출 예상가 검증 결정표가 rev 5 문구·규칙과 어긋난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-period.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 총 매출 예상가 저장이 DB·로그에 남지 않았거나, 로그에 금액이 실렸거나, DTO 등록이 누수 스캔을 깨뜨렸거나, 04-22의 기간·상태 바뀜 케이스가 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 부제 3차 → 칸 → 일괄 저장 → 새 부제 값의 한 경로가 이어지지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/projects/pre-estimate.ts`가 `domain/money` 밖(리포지토리·DB)을 import하지 않고, 화면(`pre-estimate-field.tsx`)과 `ledger.ts`가 같은 `validatePreEstimateChange`를 import한다(Grep) · 결정표가 `behavior` 한 줄을 전부 덮는다
    - 총 매출 예상가 권리가 `periodEditRights`에서 온다(DR-37 — 별도 권리 함수 없음, Grep) · 쓰기가 `moneyToColumns`를 지난다(Grep)
    - `visible(viewer, "quote.amount")`와 권리 `can` 호출이 `withTransaction` 호출보다 앞에 있다(ENG-D3 ① — 코드 순서)
    - 통합 (o)가 초록이다 — DB 값 · `preEstimateChanged` · 금액 키 없음
    - `ProjectDto.preEstimate`가 `quote.amount`로 투영되고 누수 스캔이 초록이다
    - E2E가 부제 3차 → 저장 → 새 부제 값 → 새로 고친 뒤 같은 값과 0의 `총 매출 예상가 —`를 단언하고, 스펙에 권한·노출을 켜는 호출이 0건이다(`grep -cE "upsertVisibility|upsertPermission|setPermissionCell|setVisibilityCell" test/e2e/project-period.spec.ts` = 0)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>팀장이 부제의 3차로 총 매출 예상가 칸을 열어 KRW 값을 고치고 화면 1차 한 번으로 저장하는 한 경로가 순수 검증·DTO·게이트·합성 저장·화면·E2E까지 끝났다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 총 매출 예상가 칸 넓히기 — 외화·환율(커밋 뒤 최근 환율) · 칸 오류와 U-6 · 권리 거부 · 키(Enter·Ctrl+S·Esc) · 미저장 보관 키</name>
  <files>domain/projects/ledger.ts, app/(app)/projects/[id]/pre-estimate-field.tsx, app/(app)/projects/[id]/quote-table.tsx, test/integration/project-period.test.ts, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## UI Considerations` 「S17」 error · partial 630–633행 · `## Copywriting Contract` `Error — 표 배치 저장 실패(표 밖 칸 오류로 전부 거부, U-6)` 362행 · `Error — 총 매출 예상가 칸` 377행
    - `domain/projects/ledger.ts`(Task 1 뒤) — 기간 오류와 칸 오류를 모아 전체 거부하는 자리(04-22 (c)) · 커밋 뒤 단계
    - `domain/money/currency.ts`(`rememberFxRate`) · `domain/rules/deny-write.ts`(04-20 `denyWrite`)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `persist(`·`dirtyStorage.restore`·합계 행 U-6 자리(`전부 거부 · 다른 칸 오류`)를 찾아 범위만 Read(04-22 Task 2 ⑤가 이은 보관 배선)
    - `test/integration/project-period.test.ts`(04-22 · Task 1 — 준비·`log.warn` 스파이 형태)
  </read_first>
  <behavior>
    - 정산 프로젝트의 담당 PM(권리 `none`)이 보낸 총 매출 예상가 저장은 `총 매출 예상가 바꾸기 권한 없음`으로 거부되고 DB 무변경 · `write.denied` 정확히 한 번(금액 키 없음)
    - 음수 금액과 견적 줄 변경을 한 저장에 실으면 둘 다 저장되지 않는다(DB 재조회)
    - USD 40,000 · 환율 1,350 · `fxRateTouched`인 저장은 커밋 뒤 최근 환율 키가 1350이고, 거부된 저장은 키를 바꾸지 않는다
  </behavior>
  <action>
① **서버 — 거부와 커밋 뒤 기억.** `ledger.ts`: 게이트 거부는 04-22의 기간 오류와 함께 **칸 오류를 모아 전체 거부**(U-6 — 표 쪽은 저장하지 않는다)하고 `denyWrite(viewer, "project.pre-estimate-edit", { projectId }, err)`(04-20 — 허용 목록 필드, 금액 없음). 커밋 **뒤** USD이고 `fxRateTouched`면 `rememberFxRate("USD", rate)` — 실패해도 저장은 성공, `log.error("fx.remember_failed", { currency })`(04-12가 같은 자리에서 견적 줄 쪽 `fxToRemember` 목록과 합친다). 거부된 저장은 기억하지 않는다. `test/integration/project-period.test.ts`에 `behavior` 세 줄을 (o2)·(o3)·(o4)로 먼저 쓴다(RED → GREEN) — 호출은 준비 상태를 `seenStatus`로 싣는다.

② **화면 — 외화 · 오류 · 키 · 보관.** `pre-estimate-field.tsx`: 통화 `Select`가 USD면 환율 행을 보이고 금액 칸 `numberKind`를 `foreign`으로, 환율 칸을 고치면 `fxRateTouched`를 참으로 싣는다. Enter 기본 제출 막음 · 칸 안 Ctrl+S는 1차 저장과 같은 `handleSave` · Esc는 편집 중 값을 되돌리고 모든 칸이 원래 값이면 묶음을 닫고 포커스를 「총 매출 예상가 바꾸기」로 돌린다(04-22 기간 칸과 같은 규칙) · 서버 칸 오류는 `Form.Error`(`aria-invalid`)로 칸 아래에 서버 문자열 그대로 · `quote-table.tsx`: 총 매출 예상가 칸 오류로 거부되면 각 표 합계 행에 `전부 거부 · 다른 칸 오류 1칸`(U-6 — 04-22가 기간 칸에 둔 같은 자리). **미저장 보관(D-68 — 04-22 Task 2 ⑤의 배선에 키를 더한다):** 칸을 바꾸는 순간 `persist` 대상에 `preEstimate:amount`·`preEstimate:currency`·`preEstimate:fxRate`를 싣고, 「복원」은 이 키가 있으면 묶음을 열고 값을 dirty로 넣는다. 서버 값으로 다시 그리는 경로는 `persist`를 부르지 않는다(04-22 규칙).

③ **E2E(`project-period.spec.ts`).** 정산 프로젝트의 담당 PM에게는 「총 매출 예상가 바꾸기」가 없다 · 팀장이 금액 칸에 `-1`을 넣고 `Control+s` → 칸 아래 `총 매출 예상가는 0 이상 · 금액을 고쳐 주세요` + 견적 표 합계 행 `전부 거부 · 다른 칸 오류 1칸` · Esc 두 번이면 묶음이 닫히고 포커스가 「총 매출 예상가 바꾸기」. 권한·노출을 켜는 호출을 두지 않는다(ENG-D2).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/project-period.test.ts test/integration/settings.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 총 매출 예상가가 권리 밖에서 저장됐거나, 오류 있는 저장에서 다른 칸이 저장됐거나, 거부된 저장이 최근 환율을 바꿨거나, 기억 실패가 저장을 되돌렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts test/e2e/ledger-save-flow.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 칸 오류·U-6·권리 없는 사람의 3차 부재·Esc 포커스 복귀가 rev 5와 다르거나 04-22의 저장 흐름(상태 바뀜 거부 · 복원 줄)이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 (o2)~(o4)가 초록이다 — 권리 없는 저장 거부(`write.denied` 한 번 · 금액 키 없음) · 오류 있는 합성 저장 전부 거부 · 커밋 뒤에만 최근 환율 기억
    - `rememberFxRate` 호출이 `withTransaction` 콜백 밖(커밋 뒤)에 있다(Grep·코드 순서)
    - `pre-estimate-field.tsx`에 `Form.Actions`가 없고, 칸 오류 문자열이 화면 코드 리터럴이 아니라 `validatePreEstimateChange`·서버 반환값이다
    - 보관 키 `preEstimate:`가 `persist` 대상에 있고 「복원」이 묶음을 연다(Grep)
    - E2E가 음수 금액 오류 + U-6 · PM의 3차 부재 · Esc 포커스 복귀를 단언한다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>총 매출 예상가 칸이 외화·환율·오류·권리 거부·키·미저장 보관까지 기간 칸과 같은 규칙으로 동작하고, 최근 환율은 성공한 저장의 커밋 뒤에만 바뀐다</done>
</task>

<task type="auto">
  <name>Task 3: 상태 모달 연결(「기간 적기」 · 「기간 바꾸기」) + 폰 부제 순서(DR-26) + 머리 줄 화면 검증(1280 · 1024 · 375)</name>
  <files>app/(app)/projects/[id]/status-change.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/project-period.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## 적용 규칙` 「S7」 막힘 1335–1337행 · 「S3」 폰 첫 화면 1151–1155행 · `## Copywriting Contract` `3차 버튼` 345행 · `확인 — 진행으로 바꾸기 · 진행으로 되돌리기` 427행 · `## UI Considerations` 「S3」 overflow · 폰 첫 화면 490–500행
    - `app/(app)/projects/[id]/status-change.tsx`(04-21 산출물 전문 — `StatusChangeProps`의 `endDateBeforeToday`·결과 줄 조립) · `ui/confirm-dialog/ConfirmDialog.tsx`(04-46 — `primary.nextStep`·`resultLines: ReactNode[]`)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `openPeriodField`(04-22)·`<StatusChange`를 찾아 범위만 Read · `app/(app)/projects/[id]/project-detail.module.css` — Grep으로 머리 줄·부제·폰 미디어 쿼리 자리
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <action>
① **상태 모달 연결(A-31 — 04-21이 데이터 props로 정했다).** `status-change.tsx`의 `StatusChangeProps`에 `periodRights`·`onOpenPeriodField?`를 더한다(`endDateBeforeToday`는 04-21이 이미 싣는다) — ReactNode ↔ props 재작업은 없다. `QuoteLedger`가 `onOpenPeriodField`에 04-22의 `openPeriodField`를 넣어 렌더한다. 막힌 확인 모달의 `primary.nextStep`에 3차 「기간 적기」(모달을 닫고 `openPeriodField("start")`)를, 진행으로 바꾸기·진행으로 되돌리기 모달의 결과 줄 `종료일 지남 · 바로 정산` 오른쪽에 3차 「기간 바꾸기」(모달을 닫고 `openPeriodField("end")`)를 둔다 — 권리가 `none`이면 두 3차 없이 글자만. `[id]/page.tsx`가 기간 권리를 `StatusChangeProps`에 싣는다. 모달 결과 줄의 문구는 04-21이 만든 rev 5 원문 그대로다(이 플랜이 결과 줄 글자를 새로 만들지 않는다).

② **폰 부제 순서(DR-26 — 04-21이 만든 폰 머리 줄 안).** `project-detail.module.css`의 폰(<700) 규칙으로 부제 첫 항목을 `기간 …` + 「기간 바꾸기」로, 나머지 부제(번호 · 차수 · 고객 승인 · 총 매출 예상가 · 상태 날짜)를 그 아래 `--fs-sm --muted` 줄바꿈으로 둔다(새 토큰 없음). 칸 묶음(기간 · 총 매출 예상가)은 폰에서 전폭·라벨 위(`ui/form` 폰 규칙 그대로).

③ **E2E(`project-period.spec.ts`).** 시작일 없는 수주중에서 팀장이 진행을 고르면 막힘 `시작일 없음 · 기간 적기` 옆 3차 「기간 적기」 → 누르면 모달이 닫히고 기간 칸이 열려 시작일에 포커스 · 종료일이 `addDays(kstToday(new Date()), -3)`인 수주중을 진행으로 바꾸는 확인 모달의 결과 줄 `종료일 지남 · 바로 정산` 오른쪽 3차 「기간 바꾸기」 → 누르면 모달이 닫히고 종료일에 포커스 · 미수주의 「진행으로 되돌리기」 모달도 같다 · 기간 권리가 없는 사람에게는 두 줄에 글자만 있고 3차가 없다. 뷰포트 375×812에서 부제 첫 항목이 `기간 `으로 시작한다. 권한·노출을 켜는 호출을 두지 않는다(ENG-D2).

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 별도 서브에이전트(model: sonnet, `verification-before-completion` 명시)가 `CI=true`로 **1280 · 1024 · 375** 세 폭(DR-14 감사 폭)에서 S3 overflow backstop(기간 칸·총 매출 예상가 칸이 열린 머리 줄 — 가로 스크롤 0 · 칸 묶음이 머리 줄 아래 · 375는 부제 첫 항목 `기간 …` · 칸 묶음 전폭)을 DOM 실측 → 필요하면 `project-detail.module.css`만 고친다 → `CI=true pnpm test` 한 번. 판정을 SUMMARY에 적는다(스크린샷 육안 금지).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/project-period.spec.ts test/e2e/project-lifecycle.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 「기간 적기」·「기간 바꾸기」가 기간 칸으로 이어지지 않거나 04-21의 상태 모달 흐름이 깨졌거나 375 부제 첫 항목이 기간이 아니다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S3 overflow 판정(기간 칸·총 매출 예상가 칸이 열린 머리 줄 — 1280 · 1024 · 375)을 SUMMARY에서 확인</test>
      <expected>세 폭 모두 가로 스크롤 0, 칸 묶음이 머리 줄 아래, PC 버튼이 숨지 않음, 375에서 부제 첫 항목이 `기간 …`이고 칸 묶음이 전폭</expected>
      <why_human>폭·배치는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 「기간 적기」가 모달을 닫고 시작일 칸에, 결과 줄 「기간 바꾸기」가 종료일 칸에 포커스한다(E2E) · 권리 없는 사람에게는 두 3차가 없다(E2E)
    - `StatusChangeProps`가 넓혀졌을 뿐 `QuoteLedger`에 ReactNode 형 상태 컨트롤 prop이 생기지 않았다(A-31) · 모달 결과 줄 문자열 리터럴을 이 플랜이 새로 만들지 않았다(04-21의 조립 그대로)
    - `project-detail.module.css`에 새 토큰·색·중단점이 없다(700만 — stylelint 초록)
    - 독립 DOM 감사 판정(세 폭)이 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>상태 모달의 막힘·종료일 지남이 기간 칸으로 이어지고, 폰 부제가 기간을 먼저 보이며, 두 칸이 열린 머리 줄이 세 폭에서 넘치지 않고 전체 게이트가 CI=true로 초록이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 저장(총 매출 예상가) | 통화·금액·환율·`fxRateTouched`가 넘어온다. 권리·노출·검증 판정이 서버에만 있어야 한다 |
| 행동 로그 → 로그 열람자 | 로그 detail은 금액을 볼 수 없는 사람에게도 보일 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-333 | Elevation of Privilege | 총 매출 예상가 쓰기(계약 8) | medium | mitigate | `project.pre-estimate-edit` 게이트가 기간과 같은 권리 함수 + `quote.amount` 노출을 서버에서 판정하고 거부는 `denyWrite`로 남긴다 — 3차를 숨기는 것은 보조다(통합 o2) |
| T-04-334 | Information Disclosure | 총 매출 예상가 금액의 노출 | medium | mitigate | DTO는 `quote.amount`로 투영하고(누수 스캔), 행동 로그 detail에는 `preEstimateChanged: true`만 — 금액을 싣지 않는다(통합 o) |
| T-04-337 | Tampering | 최근 환율 키(`fx.recent_rate.USD`) | low | mitigate | 커밋이 끝난 저장에서 `fxRateTouched`일 때만 기억한다 — 거부된 저장·환율을 고치지 않은 저장이 키를 바꾸지 않는다(통합 o4). 값은 04-40의 `normalizeMoneyInput`을 지난 환율이다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `CI=true pnpm test` 세 계층 통과
- 독립 DOM 감사(기간 칸·총 매출 예상가 칸이 열린 머리 줄 — 1280 · 1024 · 375) 판정이 SUMMARY에 있다
- SUMMARY 「새 문구」 표에 `총 매출 예상가 바꾸기 권한 없음`(방어 문구 — 사용자 2026-09-23 승인)이 있다
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 총 매출 예상가가 부제에 보이고 기간 칸과 같은 권리·형태·일괄 저장으로 고쳐진다(계약 8 · DR-28 · DR-37)
- 권리 없는 저장·오류 있는 저장은 서버에서 전부 거부되고 최근 환율은 성공한 저장만 바꾼다
- 상태 모달의 「기간 적기」·「기간 바꾸기」가 기간 칸으로 이어진다
- 폰 부제가 기간을 먼저 보이고 두 칸이 열린 머리 줄이 세 폭에서 넘치지 않는다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/projects/pre-estimate.ts` — `validatePreEstimateChange` |
| 게이트 규칙 | `project.pre-estimate-edit` |
| 함수(신규, repository) | `updateProjectPreEstimate`(`repositories/projects.ts`) |
| DTO 필드 | `ProjectDto.preEstimate`(Money — 정보 항목 `quote.amount`) |
| 입력 계약(추가) | `SaveProjectLedgerInput.preEstimate`(`{ currency, amount, fxRate, fxRateTouched }`) · `saveProjectLedgerAction` 스키마 `preEstimate` |
| 로그 detail 규약(추가) | `document_update` detail `preEstimateChanged: true`(금액 없음) · 운영 로그 `write.denied`(규칙 `project.pre-estimate-edit`) · `fx.remember_failed`(커밋 뒤 기억 실패) |
| 컴포넌트 | `app/(app)/projects/[id]/pre-estimate-field.tsx`(신규 — 부제 `총 매출 예상가 …` + 3차 + 칸 묶음) |
| 변경된 기존 심볼 | `StatusChangeProps`(`periodRights`·`onOpenPeriodField` — `endDateBeforeToday`는 04-21) · `QuoteLedger`(총 매출 예상가 dirty 합류 · 보관 키 `preEstimate:*` · 「복원」 병합 · U-6) · `canSave`(총 매출 예상가 권리 항) · `saveProjectLedger`(기간 판정 뒤 총 매출 예상가 게이트 · 쓰기 · 커밋 뒤 환율 기억) |
| 테스트 | `test/unit/domain/project-pre-estimate.test.ts`(신규) · `test/integration/project-period.test.ts`(04-22 파일 — o~o4) · `test/e2e/project-period.spec.ts`(04-22 파일 — 총 매출 예상가 · 모달 연결 · 375 부제) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Rounds 1–3 — 옛 04-22 Task 3에서 이어받음

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-12 — 1차 렌더 조건 | WARNING | Plan 04-44, Task 1 ③: 04-22의 `canSave`에 총 매출 예상가 권리 항 |
| A-31 — 상태 컨트롤 props | NOTE | Plan 04-44, Task 3 ①: 04-21의 `StatusChangeProps`를 넓힘 |
| ENG-D2 — E2E가 권한·노출을 손으로 켜지 않음 | DECISION | Plan 04-44, `<probe_fallback>` · 세 태스크 E2E · 수용 기준 grep 0 |
| ENG-D3 ① — 잠근 트랜잭션 안 풀 호출 금지 | DECISION | Plan 04-44, Task 1 ② 트랜잭션 전 사실 · Task 2 ① 커밋 뒤 환율 기억 |
| DR-28 · DR-37 · 계약 8 — 총 매출 예상가 표시 자리·수정 권리 | HIGH | Plan 04-44, Task 1 ②③ · Task 2: 부제 + 3차 + 칸 묶음(기간 칸과 같은 형태) · 권리 `periodEditRights` · 같은 일괄 저장 · 통합 o~o4 · E2E · T-04-333·334 |
| rev 5 S7 — 진행 모달의 종료일 지남 줄과 「기간 바꾸기」 | NOTE | Task 3 ①: 결과 줄 글자는 04-21, 이 플랜은 3차 「기간 적기」(`primary.nextStep`)·「기간 바꾸기」(결과 줄) 연결 |
| DR-26 — 폰 부제 첫 항목 | NOTE | Task 3 ② (폰 머리 줄 순서·「더보기」는 04-21) |
| DR-14 — DOM 감사 폭 1024 | NOTE | backstop · Task 3 ④ · human-check: 1280 · 1024 · 375 |
| UI Considerations 끌어올림 — S17 · S3(overflow · 폰 첫 화면 부제) | NOTE | `must_haves.truths`(explicit → 문자열, backstop → `{ statement, verification: backstop }`) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음 — 옛 Deferred 「총 매출 예상가의 동시 수정 충돌 판정」은 사용자 2026-09-23 확정으로 해소: 나중 저장이 이긴다) | — |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B5 — 04-22 크기(21파일 · 128k · 네 관심사) | BLOCKER | 신설 Plan 04-44: 옛 04-22 Task 3(상태 모달 연결 · 총 매출 예상가 칸 · 폰 부제 · 머리 줄 감사)을 옮겨 세 태스크로 나눔 — 14파일 · 52k. 사슬 04-22 → 04-44 → 04-12 |
| 사용자 확정 2026-09-23 — 총 매출 예상가 동시 수정 · 0의 뜻 · 방어 문구 | DECISION | truths 두 줄(나중 저장이 이김 · 0 = `총 매출 예상가 —`) · `<probe_fallback>` · SUMMARY 「새 문구」에 `총 매출 예상가 바꾸기 권한 없음` |
| 04-08 분할(W2) — 계약 2 주인이 04-46으로 | NOTE | `<context>`·Task 3 read_first가 `ui/confirm-dialog`를 04-46 산출물로 가리킨다 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 5 — 체커 revision 1 후속 정리(2026-09-23 · 사용자 후속 결정 R1)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 사용자 결정 R1 — 1024 미만 1차 「일괄 저장 N」은 N ≥ 1이면 렌더(표 셀 dirty 포함) | NOTE | 폰 부제 순서 truth의 1차 렌더 조건 인용: 「표 밖 칸이 dirty일 때만」 → N ≥ 1이면 렌더(UI-SPEC rev 5 후속 결정 R1). 구현 주인 04-30 Task 3 ④ — 이 플랜의 코드·E2E 변경 없음(04-30은 이 플랜의 후행 — 의존 추가 없음) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-44-SUMMARY.md` when done
</output>
