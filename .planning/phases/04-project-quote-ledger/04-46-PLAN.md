---
phase: 04-project-quote-ledger
plan: 46
type: execute
wave: 4
depends_on: ["04-08"]
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - ui/button/Button.tsx
  - ui/button/Button.module.css
  - test/unit/ui/button.test.ts
  - ui/confirm-dialog/ConfirmDialog.tsx
  - ui/confirm-dialog/ConfirmDialog.module.css
  - test/unit/ui/confirm-dialog.test.ts
  - test/unit/design-system-docs.test.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - app/(app)/projects/project-form.tsx
  - test/e2e/project-register.spec.ts
  - test/e2e/quote-table.spec.ts
  - test/e2e/action-log.spec.ts
autonomous: true
requirements: [UX-05, UX-04]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`ui/button`의 `Button`이 `reasonTone?: \"block\" | \"info\"`(기본 `block`)를 받는다 — `block` 이유는 `--fs-sm --danger`, `info` 이유는 `--fs-sm --muted`다. 이유가 있는 비활성과 `pending`은 네이티브 `disabled`가 아니라 `aria-disabled=\"true\"`라 탭 순서에 남아 포커스되고 클릭·Enter·암묵 제출이 무시되며, 버튼의 `aria-describedby`가 이유 글자의 `id`(`useId`)를 가리킨다. 기존 사용처는 `block` 기본값이라 색이 바뀌지 않는다(교차 그룹 계약 1, DR-10 · DR-11, SYSTEM.md 개정 ⑦, UI-SPEC rev 5 Component Inventory `Button` 행 · S11 「비활성 버튼(DR-11)」)"
    - "`DECISIONS.md` ⑦·⑮ 기록이 각자의 `SYSTEM.md` 개정보다 먼저 커밋되고, ⑦의 SYSTEM.md 줄과 `Button` 코드는 **같은 커밋**이다 — 04-08의 열다섯 건과 합쳐 04-08 머리글이 17이다(DR-10, CLAUDE.md 프론트엔드 규칙, UI-SPEC rev 5 「SYSTEM.md 개정 — 2026-09-23」 ⑦ · ⑮)"
    - "확인 모달·위험 행동 시트는 공용 `ui/confirm-dialog/ConfirmDialog` 하나다 — 네이티브 `<dialog>`의 `showModal()`(포커스 가두기 · `::backdrop` = `--scrim` · Esc), PC(≥700) 폭 `--modal-w` 모달 / 폰(<700) 하단 시트. 제목(= 실제 동작) · 부제(대상 한 줄) · 결과 줄 0~3 · 확인 근거 한 칸(사유 또는 날짜) · 막힘 이유 자리(1차 왼쪽, `reasonTone`) + 다음 한 수 3차 · 2차 · 1차(`Ctrl+Enter` kbd, 생략하면 목록형). 2차 라벨은 1차 라벨에서 자동 파생된다 — 1차에 「취소」가 있으면 `닫기 Esc`, 아니면 `취소 Esc`(교차 그룹 계약 2, DR-12 · DR-20, SYSTEM.md 개정 ⑮ §7-17 · ⑭, UI-SPEC rev 5 S16)"
    - "확인 모달이 열리면 포커스는 확인 근거 칸 → 없으면 1차(`aria-disabled`여도 포커스되고 이유가 읽힌다) → 목록형이면 첫 행이다. Esc는 2차와 같고, 닫히면 포커스가 트리거로 돌아온다 — 트리거가 사라졌으면 화면 제목으로. 제출 중에는 1차 `…` + 2차·확인 근거 칸·목록 행 `aria-disabled`, Esc·가림막 클릭 무시. 서버 거부 문자열은 1차 왼쪽 막힘 자리(`block`)에 그대로 붙고 다이얼로그는 닫히지 않으며 토스트가 없다(UI-SPEC rev 5 UI Considerations S16 interactive · loading · error)"
    - "견적 줄 삭제 확인은 옛 div 모달(`DeleteLineDialog`)이 아니라 `ui/confirm-dialog`다 — 제목 `견적 줄 삭제` · 부제 `{항목} · {견적가}` · 결과 `보관함으로 옮겨짐 · 복원은 관리자` · 1차 `견적 줄 삭제` · 2차 `취소 Esc`. Esc로 닫으면 포커스가 Delete를 누른 셀로 돌아온다(DR-12, UI-SPEC rev 5 S4 「삭제(D-56)」, Copywriting 「Destructive — 견적 줄 삭제(연결 문서 없음)」)"
    - "등록 폼 버튼 줄의 2차 `취소 Esc`와 폼 안의 Esc가 실제로 동작하되 입력을 잃지 않는다 — 열린 네이티브 선택 목록·날짜 선택·자동 완성이 Esc를 먼저 쓰면(이벤트가 이미 처리됐으면) 폼은 아무것도 하지 않고, 폼 값이 처음 연 값(복사 등록이면 채워진 값)과 같으면 바로 목록(`cancelHref`)으로, 한 칸이라도 다르면 `ui/confirm-dialog` 「입력 버리기」(부제 `프로젝트 등록 · 3칸`, 1차 `입력 버리기`, 2차 `취소 Esc`, 결과 줄 없음)를 연다 — 1차면 목록으로, 2차면 폼으로 돌아가 원래 칸에 포커스. 제출 중에는 Esc를 무시한다(CEO 리뷰 C-07 ①, DR-27, UI-SPEC rev 5 UI Considerations S2 「(rev 5) interactive — Esc」, Copywriting 「확인 — 등록 폼 입력 버리기」 — Esc 판정·빈 폼 갈래는 04-08)"
    - "등록 폼 제출 중에는 1차 라벨 뒤 `…`이고 같은 폼의 2차 「취소 Esc」가 `aria-disabled`(포커스가 남는다)이며 Esc가 무시된다 — 진행 바 없음(UI-SPEC rev 5 UI Considerations S2 loading, CEO-D17)"
    - statement: "확인 모달의 제목·부제는 `keep-all` 줄바꿈(말줄임 없음), 결과 줄은 ` · ` 묶음 단위로 줄바꿈하고 숫자는 꺾지 않으며, 폰 시트는 최대 85vh에 본문만 스크롤한다 — 결과 줄 0개(입력 버리기)·1개(견적 줄 삭제)에서 행동 줄 위치가 흔들리지 않고, 폰 시트 머리의 닫기 x에 `aria-label=\"닫기\"`, 행동 줄 버튼이 `--touch-min` 이상이고 1차가 2차의 두 배 폭이다 — 1280·1024·375 독립 DOM 감사(UI-SPEC rev 5 UI Considerations S16 overflow · zero-one-many)"
      verification: backstop
  prohibitions:
    - statement: "확인 모달을 화면이 `<div>`로 따로 만들어서는 안 된다 — 포커스 가두기·Esc·첫 포커스·폰 시트가 화면마다 갈리고(옛 `DeleteLineDialog`가 그랬다), 뒤 플랜의 확인 모달 열한 곳이 같은 결함을 복사한다. 이 플랜 뒤에 `app/`에 `role=\"alertdialog\"`·`aria-modal` div 모달이 남지 않는다"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/button/Button.tsx"
      provides: "`reasonTone` block/info · 비활성·진행 중 = `aria-disabled` + 클릭 가드 + `aria-describedby` → 이유 id(교차 그룹 계약 1)"
      contains: "reasonTone"
    - path: "test/unit/ui/button.test.ts"
      provides: "`Button` 정적 렌더 — aria-disabled · describedby · info/block 톤 · pending"
      min_lines: 20
    - path: "ui/confirm-dialog/ConfirmDialog.tsx"
      provides: "공용 확인 모달·시트 — 네이티브 dialog, 슬롯(제목·부제·결과 줄·확인 근거 한 칸·막힘 이유·1차·2차·목록형), 2차 라벨 자동 파생, 첫 포커스·Esc·포커스 복귀(교차 그룹 계약 2)"
      exports: ["ConfirmDialog", "secondaryLabelFor", "initialFocusTarget"]
    - path: "test/unit/ui/confirm-dialog.test.ts"
      provides: "2차 라벨 파생 표 · 첫 포커스 대상 표 · 정적 렌더(슬롯·목록형·막힌 1차 aria-disabled·결과 줄 수)"
      min_lines: 40
  key_links:
    - from: "ui/confirm-dialog/ConfirmDialog.tsx"
      to: "ui/button/Button.tsx"
      via: "1차·2차가 `Button`이고 막힌 1차는 `disabledReason`·`reasonTone`으로 `aria-disabled`가 되어 포커스를 받는다 — 확인 모달이 비활성 규칙을 따로 만들지 않는다"
      pattern: "disabledReason"
    - from: "ui/confirm-dialog/ConfirmDialog.tsx"
      to: "lib/shortcut.ts"
      via: "다이얼로그 안 Ctrl+Enter가 04-08의 `isCtrlCombo`(자동 반복·조합 가드)로 1차를 부른다"
      pattern: "isCtrlCombo"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "견적 줄 삭제 확인이 공용 확인 모달로 옮겨지고 옛 `DeleteLineDialog` 정의와 그 CSS 클래스가 지워진다"
      pattern: "ConfirmDialog"
    - from: "app/(app)/projects/project-form.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "입력이 있는 폼의 Esc·「취소 Esc」가 「입력 버리기」 확인을 연다(DR-27 — 04-08의 `isFormPristine` 비교를 그대로 쓴다)"
      pattern: "입력 버리기"
---

<objective>
**As a** 키보드로 일하는 기획 PM·경영관리, **I want to** 비활성 버튼의 이유를 키보드로도 읽고, 모든 확인 모달이 같은 모양·같은 포커스·같은 Esc로 동작하며, 등록 폼의 Esc 한 번에 입력을 잃지 않기를, **so that** 뒤 플랜들이 만드는 확인 모달 열한 곳과 비활성 버튼이 전부 같은 공용 부품 둘을 쓰고 화면마다 다른 결함이 생기지 않는다.

머지 묶음: ②/4 상태 전환 — 웨이브 2~18(04-50 → 04-23)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**실행 규율 보강(2026-09-24 재계획 — CLAUDE.md · hooks 3bed7b1 · 세션 B 교훈):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋(문서 포함)과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(플랜 하나 — 체크포인트 이어가기도 새 세션). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 화면 검증은 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번이고, 완료 판정은 로컬 dev가 아니라 `CI=true`다.

**PR #38 알려진 문제(이 플랜 몫):** QA 「삭제 대화상자 포커스」(원 보고서 유실) — 견적 줄 삭제 확인을 옛 div 모달(`DeleteLineDialog`)에서 공용 `ui/confirm-dialog`로 옮기는 이 플랜이 닫는다: 네이티브 `<dialog>` `showModal()`의 포커스 가두기 · 열릴 때 첫 포커스(확인 근거 칸 → 1차 → 목록형 첫 행) · Esc · 닫히면 포커스가 연 요소로 돌아감(기존 truths · 금지 항목 · E2E — 새 태스크 없음). 원 증상이 이와 다르면 사용자 재진술이 필요하다(계획 인계 질문).

**2026-09-23 체커 revision 1 분할(W2 — 04-08 20파일 · 110k).** 옛 04-08 Task 3(개정 ⑦·⑮ 기록 → ⑦ 규칙 + `Button` `reasonTone`·`aria-disabled` → §7-17 → `ui/confirm-dialog` 신설 → 견적 줄 삭제 확인 이관 → 등록 폼 입력 버리기)을 내용 그대로 옮겨 두 태스크(트레이서 · 확장)로 나눴다. **교차 그룹 계약 1(`Button` 개정)·2(`ui/confirm-dialog`)의 주인은 이제 이 플랜이다**(공유 브리프의 「owner: 04-08」을 대신한다). 같은 커밋 제약은 이 플랜 안에서 그대로 지킨다 — ⑦의 SYSTEM.md 줄과 `Button` 코드는 Task 1의 한 커밋, 확인 모달은 새 `Button` 뒤(Task 2), 등록 폼 입력 버리기는 04-08 Task 1의 Esc 판정(`isFormPristine`) 뒤다. 04-08과는 `SYSTEM.md`·`DECISIONS.md`·`design-system-docs.test.ts`·`project-form.tsx`·`project-register.spec.ts`가 겹쳐 04-08 바로 뒤에 선다. 04-28(`quote-table.tsx`·`quote-table.spec.ts`)과 04-29(`SYSTEM.md`·`DECISIONS.md`·문서 테스트)는 이 플랜에 의존한다 — 뒤 웨이브의 모든 소비처(04-16 · 04-21 · 04-22 · 04-23 · 04-24 · 04-26 · 04-30 · 04-42 …)는 04-28·04-29를 지나 이 플랜에 닿는다.

Purpose: 나머지 보완 플랜들이 기대는 공용 부품 둘(`Button` 개정 · `ui/confirm-dialog`)을 기록·규칙과 같은 커밋으로 먼저 확정해 화면마다 다른 확인 모달과 「포커스가 사라지는 비활성 버튼」이 생기지 않게 한다.

Output: DECISIONS.md 기록 2건(⑦ · ⑮ — 04-08 머리글 합계 17), SYSTEM.md ⑦(§7-1 · §10) · ⑮(§7-17 신설 · §7-8 한 줄), `Button` `reasonTone`·`aria-disabled`, `ui/confirm-dialog` 신설, `DeleteLineDialog` 이관, 등록 폼 입력 버리기(DR-27), 단위 2 · 문서 단언 · E2E 셋 · 독립 DOM 감사.

**반영된 리뷰(옛 04-08에서 그대로):** 디자인 리뷰 DR-10 · DR-11 · DR-12 · DR-20 · DR-27 · 교차 그룹 계약 1·2 · CEO C-07 ① · CEO-D17(진행 바 없음).

**실행 규율(모든 태스크):** 구현 전에 superpowers `test-driven-development` 스킬을, 테스트·빌드 실패를 쫓기 전에 `systematic-debugging` 스킬을, 「완료」를 말하기 전에 `verification-before-completion` 스킬을 호출한다. 서브에이전트에 위임하면 프롬프트에 세 스킬 이름과 `model`을 명시한다. 커밋 제목은 영어 접두어(docs:/feat:/fix:/test:) + 짧은 요약, 본문은 한국어, 한 커밋 한 의도.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다 — 이 플랜 목표 밖의 기능·리팩터를 더하지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다(U-2는 `## UI Considerations` S5 partial 529행의 사용자 확정값). 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-08-SUMMARY.md
@CLAUDE.md
# 범위 Read(전문 import 금지 — 큰 파일, UI-SPEC은 rev 5 — 절 이름으로 찾고 아래는 2026-09-23 현재 행):
# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md 「SYSTEM.md 개정 — 2026-09-23」 ⑦ 825–838행 · ⑭ 937–942행 · ⑮ 944–956행 · 「재실행 가능한 확인」 970–1000행
# 같은 문서 Component Inventory `Button` 행 131행 · `ConfirmDialog` 행 149행 · Copywriting 「Primary CTA — 등록 폼」 340행 · 「Destructive — 견적 줄 삭제(연결 문서 없음)」 418행 · 「확인 — 등록 폼 입력 버리기(DR-27)」 429행 · UI Considerations S2 481–488행 · S16 617–626행 · 적용 규칙 S2 Esc 1114–1116행 · S4 「삭제(D-56)」 1179–1181행 · S11 「비활성 버튼(DR-11)」「확인 모달(S16)」 1393–1396행 · 적용 규칙 S16 1493–1526행
# docs/designs/plant8-erp-phase4-design-review-260923.md 35–74행(결정 원장 — 이 플랜: DR-10 · DR-11 · DR-12 · DR-20 · DR-27 · P0)
</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-05(비활성 버튼·확인 모달의 키보드 계약)** 와 **UX-04(입력값 유실 없음 — 등록 폼 입력 버리기, DR-27)** 다(옛 04-08 Task 3 몫). 수용 기준은 UI-SPEC rev 5 개정 ⑦ · ⑮ · UI Considerations S2(rev 5 Esc · loading) · S16(확인 모달 전 항목)에서 끌어와 `must_haves.truths`에 적었다 — S16의 overflow·zero-one-many는 backstop(1280·1024·375 DOM 감사). 조용히 버린 항목은 없다.

**계획 단계 가정·사용자 결정(옛 04-08에서 그대로):**
- **`Button`의 진행 중(pending) 상태 — rev 5를 따른다:** 재계획 공유 지침의 계약 1 요약은 「네이티브 `disabled`는 `pending` 동안만」이라고 적었지만, 승인된 rev 5는 다섯 자리(개정 ⑦ §7-1 줄 · Component Inventory `Button` 행 · S2/S7/S16 loading · S11 「비활성 버튼(DR-11)」)에서 **진행 중 버튼도 `aria-disabled`**(포커스가 버튼에 남아 저장 중에도 body로 튀지 않는다)라고 정했다. 이 플랜은 rev 5를 따른다 — 소비처가 보는 API(`disabled` · `disabledReason` · `reasonTone` · `pending`)는 계약 1과 같고, 다른 것은 진행 중 버튼의 속성 하나다. 이중 제출은 클릭 가드(`preventDefault` — 암묵 제출도 막는다)와 화면의 진행 중·래치 가드가 막는다. 이 선택을 SUMMARY 「계약 1 편차」로 적는다. 이유 없는 비활성(UX-06 위반 — 개발 경고 유지)도 같은 `aria-disabled` 경로다(네이티브 `disabled`를 쓰는 갈래가 남지 않는다).
- **확인 모달의 막힘 자리 확장(계약 2에 더하는 선택 prop 둘):** rev 5 §7-17 슬롯은 막힘 이유 옆 「다음 한 수 3차」와 이유 톤(`reasonTone`)을 요구한다(예: 진행 모달의 `시작일 없음 · 기간 적기` + 3차 「기간 적기」 — 04-21 · 04-44). 계약 2의 `primary` 모양 `{ label, shortcut?, onConfirm, pending?, disabledReason? }`에 **선택** 키 `reasonTone?`(기본 `block`)·`nextStep?: ReactNode`만 더한다 — 기존 키의 뜻은 바꾸지 않는다. 목록형 `options`의 행 모양은 `{ label, description?, onSelect }`로 정한다(04-21 상태 고르기 — 행 2행 = 코드표 설명). SUMMARY 「계약 2 확장」으로 적는다.
- **등록 폼 Esc — 내부 컨트롤 먼저(DR-27):** Esc 판정(`defaultPrevented`·조합 중·제출 중이면 무시)과 빈 폼 갈래는 04-08 Task 1이 만들었다. Chromium이 열린 네이티브 `<select>`·날짜 선택 목록의 Esc를 페이지로 보내지 않는지는 이 플랜의 E2E(선택 목록을 연 채 Esc → 폼 유지)로 확인한다 — 보낸다면 대상이 `<select>`·날짜 칸이고 직전 입력이 목록을 연 입력(클릭 · `Alt+↓` · `Space`)일 때 그 Esc 하나를 무시하는 열림 추적을 더한다. 어느 쪽이었는지 SUMMARY 「내부 컨트롤 Esc 확인 결과」에 적는다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 개정 ⑦ 한 결정을 기록 → 규칙 + `Button` 코드(같은 커밋) → 기존 사용처 화면(행동 로그 「정리」) → E2E까지 관통(DR-10 · DR-11, 교차 그룹 계약 1)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, ui/button/Button.tsx, ui/button/Button.module.css, test/unit/ui/button.test.ts, test/unit/design-system-docs.test.ts, test/e2e/action-log.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정 — 2026-09-23」 ⑦(825–838행 — §7-1 두 줄 · 코드 계약 · §10 한 줄 · `Button.tsx` 44~58행 지점) · Component Inventory `Button` 행(131행) · S11 「비활성 버튼(DR-11)」(1393–1394행) · 「재실행 가능한 확인」 11·12(970–1000행 중)
    - `docs/design/DECISIONS.md` 끝의 04-08 기록 형식(`## 2026-09-23 — Phase 4(04-08) …` — 무엇을 · 왜 · 범위 · 되돌리는 법)
    - `ui/button/Button.tsx`(전문 58행) · `ui/button/Button.module.css`(전문 105행 — `.btn:disabled` 25행 · `.reason` 102–105행)
    - `test/unit/design-system-docs.test.ts`(04-08 뒤 — 2026-09-23 describe와 DECISIONS 머리글 개수 단언 15)
    - `app/(app)/admin/action-log/filter-bar.tsx` 187–203행(`PruneControl` — 지금 `disabledReason`을 쓰는 기존 사용처) · `test/e2e/action-log.spec.ts`(전문 — 「0건」 필터를 만드는 방법)
    - Grep `from "@/ui/button/Button"`(`app/`·`ui/`) — 사용처 17곳이 전부 `"use client"` 파일이다(2026-09-23 확인). `Button`에 `"use client"`를 달아도 서버 컴포넌트 사용처가 없어 동작이 바뀌지 않는다
  </read_first>
  <behavior>
    - `Button` 정적 렌더: `disabled` + `disabledReason: "바뀐 칸 없음"` + `reasonTone: "info"` → `<button>`에 `aria-disabled="true"`와 `aria-describedby`(= 이유 요소의 `id`)가 있고 **`disabled` 속성이 없다** · 이유 요소에 info 클래스 · `reasonTone`을 빼면 block 클래스
    - `Button` 정적 렌더: `pending` → `aria-disabled="true"` · `…` · 이유 요소 없음 · `disabled` 속성 없음 · 활성 버튼 → `aria-disabled`·`aria-describedby` 없음 · 호출자가 준 `aria-describedby`가 있으면 이유 id와 함께 남는다
    - E2E(기존 사용처 회귀 — DR-11): 행동 로그 0건 필터에서 「정리」가 `aria-disabled="true"`이고 `Tab`으로 포커스되며 `aria-describedby`가 `정리할 행이 없습니다`를 가리키고, 클릭해도 확인 줄이 열리지 않는다
  </behavior>
  <action>
① **기록 먼저.** `docs/design/DECISIONS.md`에 ⑦(머리글 `## 2026-09-23 — Phase 4(04-08) ⑦: 비활성 이유 두 색 + aria-disabled — ui/button 코드 포함 (U-4 · DR-10 · DR-11)`)을 04-01 기록 형식(무엇을 · 왜 · 범위 · 되돌리는 법)으로 더한다 — 머리글은 개정 번호 체계대로 `Phase 4(04-08)`를 그대로 쓴다(실행 플랜은 04-46이지만 개정 ①~⑯은 한 묶음이다 — 본문 「범위」에 「04-46이 실행」 한 줄). 본문에 「진행 중 버튼도 `aria-disabled`(rev 5 — 공유 지침 계약 1 요약과 다른 한 가지, `<probe_fallback>`)」 한 줄. 이 커밋(`docs:`)이 ②보다 먼저다.

② **⑦ 규칙 + `Button` 코드 — 같은 커밋(DR-10: 문서만 고치면 실물이 기록 없는 이탈).** `docs/design/SYSTEM.md` §7-1의 「이유 텍스트 `--fs-sm --danger`」 뒤에 ⑦ 원문 세 줄(정상 상태 이유는 `--muted` · `reasonTone` 코드 계약 · 이유 있는 비활성과 진행 중 버튼은 `aria-disabled` + `aria-describedby`)을, §10 「실시간 알림」 줄에 괄호 한 줄을 더한다. `ui/button/Button.tsx`: 파일 첫 줄에 `"use client"`(클릭 가드와 `useId` — 사용처가 전부 클라이언트, `read_first`) · prop `reasonTone?: "block" | "info"`(기본 `block`) · `const inactive = pending || disabled` · **네이티브 `disabled`를 넘기지 않는다** — `inactive`면 `aria-disabled="true"` · 이유 글자는 `disabled && !pending && disabledReason`일 때 `useId()`로 만든 `id`를 달고 block이면 `.reason`, info면 새 `.reasonInfo`(`color: var(--muted)`) · 버튼의 `aria-describedby`는 호출자가 준 값과 이유 id를 공백으로 잇는다 · 클릭 처리기는 `inactive`면 `event.preventDefault()` 뒤 돌아가고(호출자의 `onClick`을 부르지 않는다 — `type="submit"`의 클릭·Enter·폼 안 Enter 암묵 제출이 모두 취소된다) 아니면 호출자의 `onClick`을 부른다. `disabledReason`·`pending`의 문서 주석을 새 동작으로 고치고, 이유 없는 비활성 개발 경고는 그대로 둔다. `Button.module.css`: `.btn:disabled` 규칙과 hover의 `:not(:disabled)`를 `[aria-disabled="true"]` 기준으로 바꾸고 `.reasonInfo`를 더한다(토큰만). `test/unit/ui/button.test.ts`(신규 — `react-dom/server`의 `renderToStaticMarkup` + `createElement`, 새 의존성 없음 · 04-29와 같은 방식)에 `behavior` 앞 두 줄을 먼저 쓴다(RED). 톤 단언은 테스트가 같은 CSS 모듈을 import해 이유 요소에 `styles.reasonInfo`/`styles.reason`이 붙는지 본다. 같은 커밋에서 `test/unit/design-system-docs.test.ts`에 ⑦ 단언(§7-1 `--muted` 문장 · `aria-disabled` 문장 · 「재실행 가능한 확인」 11·12 = `Button.tsx`를 읽어 `reasonTone`·`aria-disabled` 각 1 이상)을 더하고 DECISIONS 04-08 머리글 개수를 15에서 16으로 바꾼다.

③ **기존 사용처 E2E(트레이서 증명).** `test/e2e/action-log.spec.ts`에 `behavior` 셋째 줄을 더한다(0건 필터는 기존 스펙의 방식을 따른다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/button.test.ts test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — `Button`의 aria-disabled·describedby·톤이 계약과 다르거나 ⑦ 문서 단언(재실행 확인 11·12)·기록 16건이 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint(토큰만)·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — `disabled` 속성 제거가 사용처 어딘가의 타입·스타일을 깨뜨렸다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/action-log.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 기존 사용처의 비활성 버튼이 포커스·이유 읽기·클릭 무시를 지키지 않는다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - DECISIONS.md에 ⑦ 머리글이 한 줄이고 04-08 머리글 합계가 16이며, ⑦ 기록 커밋이 `Button.tsx` 변경 커밋보다 앞서고 ⑦의 SYSTEM.md 줄과 `Button.tsx`·`Button.module.css` 변경이 **같은 커밋**이다(`git log --format=%H -- ui/button/Button.tsx`의 첫 커밋이 SYSTEM.md도 바꾼다)
    - `Button.tsx`에 `reasonTone`과 `aria-disabled`가 있고(재실행 확인 11·12), `<button>`에 네이티브 `disabled` 속성을 넘기는 코드가 없으며, 클릭 가드가 `preventDefault`를 부른다 · 단위 테스트가 info/block 톤·describedby·pending을 단언한다
    - 행동 로그 「정리」 E2E가 초록이다(aria-disabled · Tab 포커스 · describedby · 클릭 무시)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>개정 ⑦이 기록 → 규칙과 같은 커밋의 `Button` 코드 → 기존 사용처 화면 → E2E까지 한 경로로 끝났다 — 비활성 버튼이 포커스를 잃지 않고 이유를 읽힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 개정 ⑮ 기록 → §7-17 → `ui/confirm-dialog` 신설 → 견적 줄 삭제 확인 이관 → 등록 폼 입력 버리기(DR-27) + 화면 검증(1280 · 1024 · 375) — 교차 그룹 계약 2</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, test/unit/design-system-docs.test.ts, ui/confirm-dialog/ConfirmDialog.tsx, ui/confirm-dialog/ConfirmDialog.module.css, test/unit/ui/confirm-dialog.test.ts, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/project-detail.module.css, app/(app)/projects/project-form.tsx, test/e2e/project-register.spec.ts, test/e2e/quote-table.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정 — 2026-09-23」 ⑮(944–956행 — §7-17 슬롯·2차 라벨 파생·포커스·제출 중) · ⑭(937–942행 — 확인 근거 한 칸)
    - 같은 문서 Component Inventory `ConfirmDialog` 행(149행) · UI Considerations S16(617–626행 — empty·loading·error·populated·partial·overflow·interactive·zero-one-many) · 적용 규칙 S16(1493–1526행 — 사용처 12곳 표 · PC/폰 · 동작) · S11 「확인 모달(S16)」(1395–1396행) · 적용 규칙 S2 Esc(1114–1116행) · S4 「삭제(D-56)」(1179–1181행) · Copywriting 「Destructive — 견적 줄 삭제(연결 문서 없음)」(418행) · 「확인 — 등록 폼 입력 버리기(DR-27)」(429행) · 「Primary CTA — 등록 폼」(340행)
    - `.planning/phases/04-project-quote-ledger/04-PATTERNS.md` — Grep `MoreSheet`·`dialog`(네이티브 dialog 선례 줄만)
    - `ui/shell/MoreSheet.tsx`(전문 159행 — `showModal()`·Esc·포커스 복귀의 선례) · `ui/shell/MoreSheet.module.css` · `ui/table/RowSheet.tsx`(전문 85행 — 폰 시트 머리 · 닫기 x `aria-label="닫기"` · 85vh 본문 스크롤)
    - `ui/button/Button.tsx`(Task 1 뒤) · `lib/shortcut.ts`(04-08 — `isCtrlCombo`)
    - `app/(app)/projects/[id]/quote-table.tsx` 905–1007행(옛 삭제 확인 컴포넌트 정의 974–1007 · 사용 918–925) · `app/(app)/projects/[id]/project-detail.module.css` — Grep `dialog`로 옛 모달 클래스(가림막·창·제목·부제·본문·행동 줄)가 이 컴포넌트 밖에서 쓰이는지 확인
    - `app/(app)/projects/project-form.tsx`(04-08 뒤 — Esc 처리 · `isFormPristine` · 「취소」 자리)
    - `docs/design/tokens.css` — `--modal-w`·`--scrim`·`--touch-min`·`--line-strong`·`--muted`·`--danger`가 있는지 확인만(새 토큰을 만들지 않는다)
  </read_first>
  <behavior>
    - `secondaryLabelFor("견적 줄 삭제")` → `취소` · `("견적 줄 취소")` → `닫기` · `("승인 표시 취소")` → `닫기` · `("입력 버리기")` → `취소`(2차는 그 낱말 + kbd `Esc`)
    - `initialFocusTarget({ hasEvidence: true, hasPrimary: true })` → `evidence` · `({ hasEvidence: false, hasPrimary: true, primaryBlocked: true })` → `primary` · `({ optionCount: 3 })` → `firstOption`
    - `ConfirmDialog` 정적 렌더(`open`): 제목 요소 id를 `<dialog aria-labelledby>`가 가리키고, 부제·결과 줄(0 / 1 / 3개 그대로)·확인 근거 칸(준 경우만)이 그려지며, 1차에 kbd `Ctrl+Enter`, 2차 라벨이 파생값 · 막힌 1차(`disabledReason`)는 `aria-disabled` + 이유 · 목록형(`options`)은 1차가 없고 행마다 라벨 + 설명 2행 버튼 · 폰 머리의 닫기 x에 `aria-label="닫기"`
    - E2E(견적 줄 삭제): 저장된 줄에서 `Delete` → 제목 `견적 줄 삭제` 다이얼로그 · 결과 줄 `보관함으로 옮겨짐 · 복원은 관리자` · 포커스가 1차 `견적 줄 삭제` · `Tab`이 다이얼로그 밖으로 나가지 않음 · `Escape` → 닫히고 포커스가 그 셀 · 다시 `Delete` → 1차 → 일괄 저장 건수 +1
    - E2E(등록 폼): 프로젝트명 한 칸 입력 → `Escape` → 제목 `입력 버리기`, 부제 `프로젝트 등록 · 1칸`, 포커스가 1차 → `Escape`(= 2차) → 폼이 그대로이고 포커스가 프로젝트명 칸, 값이 남음 → 다시 `Escape` → 1차 `입력 버리기` → 목록 주소 · 빈 폼에서 2차 「취소 Esc」 클릭 → 목록 · 클라이언트 선택 목록을 연 채 `Escape` → 폼 유지
  </behavior>
  <action>
① **기록 먼저.** `docs/design/DECISIONS.md`에 ⑮(`## 2026-09-23 — Phase 4(04-08) ⑮: §7-17 확인 모달 ui/confirm-dialog (DR-12 · DR-20 · DR-27)`)를 같은 형식으로 더한다 — 본문에 「옛 div 삭제 확인을 이 컴포넌트로 옮긴다 · 막힘 자리에 선택 prop `reasonTone`·`nextStep`을 더했다 · 04-46이 실행」 한 줄. 이 커밋(`docs:`)이 ②보다 먼저다. 이제 04-08 머리글은 17이다.

② **⑮ 규칙.** `docs/design/SYSTEM.md` §7-16 뒤에 `### 7-17 확인 모달 ui/confirm-dialog`를 ⑮ 원문대로(네이티브 dialog · PC `--modal-w`/폰 시트 · 슬롯 · 2차 라벨 파생 · 포커스 · 제출 중 · 서버 거부) 두고 §7-8에 「모달·시트는 §7-17 컴포넌트로 만든다」 한 줄을 더한다. `test/unit/design-system-docs.test.ts`에 「재실행 가능한 확인」 9(`### 7-16`·`### 7-17` 두 줄)를 더하고 DECISIONS 04-08 머리글 개수를 17로 바꾼다 — 같은 커밋.

③ **`ui/confirm-dialog/ConfirmDialog.tsx`(신규, `"use client"`) — 교차 그룹 계약 2.** props: `open` · `onClose` · `title`(= 실제 동작) · `subtitle?` · `resultLines?: ReactNode[]`(0~3) · `evidenceField?: ReactNode`(§7-8 예외의 확인 근거 한 칸 — 사유 또는 날짜) · `primary?: { label; shortcut?; onConfirm; pending?; disabledReason?; reasonTone?; nextStep? }`(뒤 둘은 선택 확장 — `<probe_fallback>`) · `secondaryLabel?`(기본 = `secondaryLabelFor(primary.label)`) · `options?: { label; description?; onSelect }[]`(목록형 — 1차 없음). `primary`와 `options`는 둘 중 하나다(타입 유니언). 같은 파일이 순수 함수 `secondaryLabelFor(primaryLabel)`(라벨에 「취소」가 있으면 `닫기`, 아니면 `취소`)와 `initialFocusTarget({ hasEvidence, hasPrimary, primaryBlocked, optionCount })`를 내보낸다. `test/unit/ui/confirm-dialog.test.ts`(신규 — 정적 렌더)에 `behavior` 앞 세 줄을 먼저 쓴다(RED). 동작: `open`이 참이 되면 그 순간의 `document.activeElement`를 트리거로 기억하고 `<dialog>`의 `showModal()`, 거짓이 되면 `close()` 뒤 트리거가 아직 문서에 있으면 트리거로, 없으면 화면의 `h1`(머리 줄 제목)로 포커스(필요하면 `tabindex="-1"`). 열린 직후 포커스는 `initialFocusTarget` 대상(확인 근거 칸의 첫 입력 → 1차 버튼 → 첫 목록 행). 네이티브 `cancel` 이벤트(Esc)는 기본 동작을 막고 제출 중이 아니면 `onClose`(= 2차). 가림막 클릭(다이얼로그 요소 자체를 누른 클릭)도 제출 중이 아니면 `onClose`. 다이얼로그 안에서 `isCtrlCombo(event, "Enter")`(04-08 `lib/shortcut.ts`)는 1차가 막히지 않았고 제출 중이 아니면 `primary.onConfirm`. 1차는 `Button` `variant="primary"` + `shortcut` 기본 `Ctrl+Enter` + `pending`·`disabledReason`·`reasonTone`을 그대로 넘기고(막히면 `aria-disabled`라 포커스가 머문다), `nextStep`은 1차 왼쪽 이유 옆에 둔다. 2차는 `Button` `variant="secondary"` + 낱말 + `shortcut="Esc"`, 제출 중이면 `aria-disabled="true"` 속성을 직접 넘기고 클릭을 무시한다. 제출 중에는 확인 근거 칸 묶음에 `aria-disabled="true"`, 목록 행 무시. 서버 거부는 호출자가 `primary.disabledReason`에 문자열을 넣어 1차 왼쪽 막힘 자리(`block`)에 보이고 다이얼로그는 닫히지 않는다(토스트 없음 — 호출자 몫). `ConfirmDialog.module.css`: PC(≥700) 폭 `--modal-w`, `2px --line-strong`, `::backdrop` = `--scrim`, 제목 `--fs-lg` 700 · 부제·결과 줄 `--fs-sm --muted` · 제목·부제 `word-break: keep-all`(말줄임 없음) · 행동 줄 오른쪽 아래(2차 · 1차) · 1차 `Button`의 이유가 1차 **왼쪽**에 오도록 1차 자리의 `Button` 바깥 span 배치를 이 모듈에서 뒤집는다(`Button` API는 바꾸지 않는다). 폰(<700): §7-8 시트 골격(머리 고정 — 제목·부제·닫기 x 20 `aria-label="닫기"` · 본문 스크롤 최대 85vh · 행동 줄 버튼 높이 `--touch-min` · 1차가 2차의 두 배 폭). 새 중단점 없음(700만) · 토큰만.

④ **견적 줄 삭제 확인 이관(DR-12).** `quote-table.tsx`에서 옛 div 삭제 확인 컴포넌트의 사용 자리를 `ConfirmDialog`로 바꾼다 — 제목 `견적 줄 삭제` · 부제 `{항목명 또는 (항목명 없음)} · {formatKrw(견적가)}` · 결과 줄 하나 `보관함으로 옮겨짐 · 복원은 관리자` · 1차 `견적 줄 삭제`(기존 확인 처리기) · 닫기 = 기존 취소 처리기. 옛 컴포넌트 함수 `DeleteLineDialog`와 그 머리 주석을 지우고, `project-detail.module.css`에서 그 컴포넌트만 쓰던 모달 클래스들을 지운다(`read_first` Grep으로 다른 사용처가 없음을 확인한 것만 — 내 변경이 만든 고아만). 다이얼로그는 격자 밖(지금 자리)에 두어 다이얼로그 안의 키가 격자 키보드 처리기에 닿지 않는다. 포커스 복귀는 `ConfirmDialog`가 기억한 트리거(Delete를 누른 셀)로 된다.

⑤ **등록 폼 입력 버리기(DR-27).** `project-form.tsx`: 04-08 Task 1의 Esc 처리에서 값이 처음과 **다를 때** `ConfirmDialog`를 연다 — 제목 `입력 버리기` · 부제 `프로젝트 등록 · {N}칸`(N = 처음 값과 다른 칸 수 — 04-08의 `isFormPristine`과 같은 비교에서 센다) · 결과 줄 없음 · 1차 `입력 버리기` → `cancelHref`로 이동 · 2차(`취소 Esc`) → 닫고 원래 칸으로(트리거 복귀). 버튼 줄의 옛 취소 링크를 2차 `Button`(`variant="secondary"`, 낱말 `취소` + `shortcut="Esc"`)으로 바꾸고 누르면 Esc와 같은 판정을 탄다(처음 값과 같으면 이동, 다르면 확인). 제출 중에는 둘 다 무시하고 2차 「취소 Esc」는 `aria-disabled`다(S2 loading — 1차 `…` + 같은 폼의 다른 버튼 비활성). 다이얼로그는 `<form>` **밖**(형제)에 렌더해 그 안의 Esc·Enter가 폼 keydown·폼 제출로 가지 않게 한다. 04-08 Task 1의 E2E (c2)를 `behavior`의 등록 폼 줄로 바꾼다.

⑥ **E2E.** `quote-table.spec.ts`·`project-register.spec.ts`에 `behavior`의 E2E 두 줄을 더한다.

⑦ **화면 검증(CLAUDE.md 순서).** 싼 게이트(lint·typecheck·build) → **독립 DOM 감사**(실행자가 아닌 별도 서브에이전트, `model` 명시, `CI=true` 프로덕션 빌드에서 DOM 실측, 스크린샷 육안 판정 금지) 1280·1024·375 세 폭: (a) 등록 폼 「입력 버리기」와 견적 줄 삭제 확인이 1280·1024에서 폭 = `--modal-w` 계산값, 가림막 있음 (b) 375에서 두 확인이 하단 시트 — 닫기 x `aria-label="닫기"`, 행동 줄 버튼 높이 ≥ 44px, 1차 폭 ≈ 2차 폭 × 2, 문서 가로 스크롤 0 (c) 제목이 말줄임 없이 줄바꿈(40자 프로젝트명 부제로) (d) 열린 동안 `document.activeElement`가 다이얼로그 안 (e) 행동 로그 「정리」의 이유 글자 계산 색이 `--danger` 값이고 `aria-describedby` 대상의 글자와 같다 → 발견 수정 → 전체 게이트 **한 번** `CI=true pnpm test`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/confirm-dialog.test.ts test/unit/ui/button.test.ts test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 2차 라벨 파생·첫 포커스 표·다이얼로그 슬롯, ⑮ 문서 단언(재실행 확인 9)·기록 17건 중 하나가 계약과 다르다</fails_when>
    <automated>node -e "const fs=require('fs'),p=require('path');const hits=[];const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=p.join(d,e.name);if(e.isDirectory())walk(f);else if(/\.tsx$/.test(f)&&/role=\"alertdialog\"/.test(fs.readFileSync(f,'utf8')))hits.push(f)}};walk('app');walk('ui');const q=fs.readFileSync('app/(app)/projects/[id]/quote-table.tsx','utf8');if(hits.length||!q.includes('ConfirmDialog')||/function DeleteLineDialog/.test(q)){console.error('DIV MODAL LEFT: '+hits.join(' ')+' confirm='+q.includes('ConfirmDialog'));process.exit(1)}console.log('ok')"</automated>
    <fails_when>표준 오류에 `DIV MODAL LEFT:`로 시작하는 줄이 나오고 종료 코드가 1이다 — `app/`·`ui/`에 div 확인 모달이 남았거나 견적 줄 삭제 확인이 `ConfirmDialog`로 옮겨지지 않았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint(boundaries — `ui/confirm-dialog`는 `ui`·`lib`만 import)·stylelint(토큰만)·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/project-register.spec.ts test/e2e/quote-table.spec.ts test/e2e/action-log.spec.ts</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 입력 버리기·삭제 확인의 포커스·Esc·복귀가 계약과 다르거나 Task 1의 기존 사용처 회귀가 깨졌다(프로덕션 빌드 기준)</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다 — `Button` 개정(네이티브 disabled → aria-disabled)이나 확인 모달 이관이 다른 화면의 기존 E2E를 깨뜨렸다</fails_when>
    <human-check>독립 DOM 감사 보고서(1280·1024·375 세 폭, (a)~(e) 항목별 실측값)가 SUMMARY에 붙어 있다</human-check>
  </verify>
  <acceptance_criteria>
    - DECISIONS.md에 ⑮ 머리글이 한 줄이고 04-08 머리글 합계가 17이며, ⑮ 기록 커밋이 §7-17 커밋보다 앞선다 · SYSTEM.md에 `### 7-16`·`### 7-17`이 각 한 줄이다(재실행 확인 9)
    - `ui/confirm-dialog/ConfirmDialog.tsx`가 `ConfirmDialog`·`secondaryLabelFor`·`initialFocusTarget`을 내보내고 계약 2의 props(+ 선택 `reasonTone`·`nextStep`, 목록형 행 `{ label, description?, onSelect }`)를 받으며, `<dialog>`의 `showModal()`을 쓴다 · 단위 표가 초록이다
    - `quote-table.tsx`의 삭제 확인이 `ConfirmDialog`이고 옛 함수 정의가 없으며, 지운 CSS 클래스는 그 컴포넌트만 쓰던 것뿐이다(검증 명령 · diff 검토)
    - `project-form.tsx`의 입력이 있는 Esc와 2차 「취소 Esc」가 「입력 버리기」 확인을 열고, 다이얼로그가 `<form>` 밖에 렌더된다
    - E2E 두 줄(삭제 확인 · 등록 폼 입력 버리기)과 Task 1의 행동 로그 회귀가 초록이다
    - 독립 DOM 감사 보고서(1280·1024·375)가 SUMMARY에 있고 `CI=true pnpm test`가 초록이다
    - SUMMARY에 「계약 1 편차(진행 중 aria-disabled)」 · 「계약 2 확장(`reasonTone`·`nextStep`·목록형 행 모양)」 · 「내부 컨트롤 Esc 확인 결과」가 있다
    - 다섯 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>뒤 웨이브의 확인 모달 열한 곳이 기댈 공용 확인 모달이 기록·규칙과 함께 섰고, 옛 div 삭제 확인이 옮겨졌으며, 등록 폼의 Esc가 입력을 잃지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 키보드·포인터 → 비활성 버튼 | `aria-disabled` 버튼은 포커스를 받으므로 클릭·Enter·폼 암묵 제출이 가드 없이 동작하면 막힌 행동이 실행된다 |
| 확인 모달 → 파괴적 행동 | 삭제·입력 버리기 같은 되돌리기 어려운 행동이 한 번의 확인으로 실행된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-360 | Elevation of Privilege | `Button` `aria-disabled`(네이티브 disabled 대체, DR-11) | medium | mitigate | 비활성·진행 중 버튼의 클릭 처리기가 `preventDefault` 뒤 호출자 `onClick`을 부르지 않는다 — `type="submit"`의 클릭·Enter·폼 안 Enter 암묵 제출이 모두 취소된다. 화면 가드는 보조이고, 막힘의 최종 판정은 여전히 서버 액션(`authedActionClient` · `rules.gate` · 권한)이다. 단위(속성)와 E2E(기존 사용처 「정리」 클릭 무시)로 고정한다 |
| T-04-361 | Tampering | 확인 모달 이중 확정(1차 연타 · Ctrl+Enter 반복) | medium | mitigate | 제출 중에는 1차가 `pending`(= `aria-disabled` + 가드), Ctrl+Enter는 `isCtrlCombo`(자동 반복 무시) + 제출 중 무시, Esc·가림막 클릭 무시. 서버 쪽 멱등·재전송 방어는 각 소비처(04-12·04-21·04-24)의 기존 계약이 받친다 |
| T-04-362 | Denial of Service | 등록 폼 입력 손실(Esc 한 번) | low | mitigate | (DR-27) 입력이 있는 폼의 Esc·「취소」는 확인 없이 입력을 버리지 않는다 — 처음 값과의 비교 함수 하나(04-08) + `ui/confirm-dialog` 「입력 버리기」. E2E로 고정한다 |
| T-04-363 | Spoofing | 확인 모달 안 서버 거부 문자열 | low | accept | 거부 문자열은 서버가 만든 고정 문구(`rules.gate` 등)이고 React 텍스트 노드로만 렌더된다(HTML 삽입 경로 없음) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04-RESEARCH.md § Package Legitimacy Audit: 대상 없음 — `react-dom/server`는 기존 의존성). `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `pnpm vitest run --project unit` 전부 통과(`button.test.ts` · `confirm-dialog.test.ts` · 문서 단언 포함)
- `project-register.spec.ts` · `quote-table.spec.ts` · `action-log.spec.ts` 초록(`CI=true`) · 독립 DOM 감사(1280·1024·375) 보고서가 SUMMARY에 있다
- DECISIONS.md ⑦·⑮ 기록이 각자의 SYSTEM.md 개정보다 먼저 커밋됐고, ⑦의 SYSTEM.md 줄과 `Button` 코드는 같은 커밋이다 · 04-08 머리글 합계 17
- `CI=true pnpm test` 세 계층 통과
- `docs/design/tokens.css` diff 0줄 · `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다
</verification>

<success_criteria>
- 뒤 웨이브가 쓸 `Button` 개정(`reasonTone` · `aria-disabled`)과 공용 `ui/confirm-dialog`가 기록·규칙과 같은 커밋으로 섰다(교차 그룹 계약 1·2)
- 옛 div 삭제 확인이 옮겨져 `app/`·`ui/`에 div 확인 모달이 없다
- 등록 폼 Esc가 입력을 잃지 않는다(DR-27)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 문서 기록 | `docs/design/DECISIONS.md` 2026-09-23 Phase 4(04-08) ⑦ · ⑮(04-08의 15건과 합쳐 17) |
| 규칙 개정 | `docs/design/SYSTEM.md` §7-1 · §7-8 · §10 · **§7-17 신설** |
| 컴포넌트(변경, ui) | `Button` — `reasonTone?: "block" \| "info"` · 비활성·진행 중 `aria-disabled`(네이티브 disabled 없음) + 클릭 가드 + `aria-describedby` → 이유 id · `"use client"` · CSS `.reasonInfo`(교차 그룹 계약 1) |
| 컴포넌트(신규, ui) | `ui/confirm-dialog/ConfirmDialog.tsx` + `ConfirmDialog.module.css` — `ConfirmDialog`(props `open`·`onClose`·`title`·`subtitle?`·`resultLines?`·`evidenceField?`·`primary?{label, shortcut?, onConfirm, pending?, disabledReason?, reasonTone?, nextStep?}`·`secondaryLabel?`·`options?{label, description?, onSelect}[]`) · `secondaryLabelFor` · `initialFocusTarget`(교차 그룹 계약 2) |
| 이관 | `quote-table.tsx`의 옛 div 삭제 확인 → `ConfirmDialog`(함수와 그 CSS 클래스 삭제) |
| 동작 변경 | `ProjectForm` 입력 있음 Esc → 「입력 버리기」 · 옛 취소 링크 → 2차 `Button` 「취소 Esc」 |
| 테스트 | `test/unit/ui/button.test.ts`(신규) · `test/unit/ui/confirm-dialog.test.ts`(신규) · `test/unit/design-system-docs.test.ts`(재실행 확인 9·11·12 · 기록 17건) · `test/e2e/project-register.spec.ts`(입력 버리기 · 선택 목록 먼저) · `test/e2e/quote-table.spec.ts`(삭제 확인 포커스·Esc·복귀) · `test/e2e/action-log.spec.ts`(aria-disabled 회귀) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Rounds 1–3 — 옛 04-08 Task 3에서 이어받음

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-10 · DR-11 — `Button` reasonTone · aria-disabled(교차 그룹 계약 1) | HIGH | Plan 04-46, Task 1 ②: ⑦ 문서와 같은 커밋의 코드 · 단위 · 기존 사용처 E2E · T-04-360 (진행 중도 aria-disabled — rev 5, `<probe_fallback>`) |
| DR-12 · DR-20 · DR-27 — 공용 확인 모달(교차 그룹 계약 2) · 확인 근거 한 칸 · 등록 폼 Esc | HIGH | Plan 04-46, Task 2 ②③④⑤: §7-17 · `ui/confirm-dialog` · 삭제 확인 이관 · 입력 버리기 · T-04-361·362 (⑭ 확인 근거 규칙 문장은 04-08 Task 2) |
| C-07 ① — 적힌 `취소 Esc`가 동작하지 않음 | WARNING | Plan 04-46, Task 2 ⑤: 2차 `Button` 「취소 Esc」가 Esc와 같은 판정(빈 폼 갈래는 04-08 Task 1) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 4 — 체커 revision 1(2026-09-23)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W2 — 04-08 크기(110k · 20파일) — 규칙: 같은 커밋 제약이 막지 않으면 분할 | WARNING | 신설 Plan 04-46: 옛 04-08 Task 3을 옮겨 트레이서(⑦ → `Button` → 기존 사용처 E2E)와 확장(⑮ → 확인 모달 → 이관 → 입력 버리기 → 감사)으로 — 15파일 · 56k. 같은 커밋 제약(⑦ SYSTEM.md 줄 + `Button` 코드)은 Task 1 한 커밋 안이라 분할을 막지 않는다. `depends_on` [04-08] · 04-28·04-29가 이 플랜에 의존(공유 파일 순서 + 소비처 도달) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-46-SUMMARY.md` when done
</output>
