---
phase: 04-project-quote-ledger
plan: 47
type: execute
wave: 26
depends_on: ["04-19"]
files_modified:

  - ui/table/parse-tsv.ts
  - test/unit/ui/parse-tsv.test.ts
  - ui/table/use-clipboard-paste.ts
  - test/unit/ui/use-clipboard-paste.test.ts
  - ui/table/types.ts
  - ui/table/Table.tsx
  - ui/table/paging.ts
  - test/unit/ui/table-paging.test.ts
  - ui/table/footer-notice.ts
  - test/unit/ui/footer-notice.test.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - test/e2e/quote-table.spec.ts

autonomous: true
requirements: [UX-05, UX-04, PROJ-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "다른 프로젝트에서 복사한 견적을 붙여넣으면(클립보드에 앱 전용 형식 `application/x-plant8-quote-lines+json`이 있을 때) 계산 열(번호·견적가·차익·상태)은 들어가지 않고 무시되며 합계 행 요약에 `계산 열 N칸 무시`가 붙는다 — 오류 칸이 되지 않는다. 잠긴 칸(상태별 잠김)은 여전히 오류다(사용자 D15, CEO C-03)"
    - "앱 전용 형식이 **없는** 붙여넣기(엑셀·다른 프로그램)는 계산 열 자리에 떨어진 값을 04-04처럼 오류 칸으로 만든다 — 소분류에서 붙인 6열 엑셀(`소분류·항목·거래처·수량·단가·실행가`)의 실행가가 견적가 칸에 떨어지면 그 칸이 오류이고 요약에 `계산 열 N칸 무시`가 없다. 값이 조용히 사라지지 않는다(ENG-D5 — 엔지 리뷰 C §1 P1)"
    - "붙여넣기로 새로 생긴 줄도 04-30의 새 줄 경로처럼 `crypto.randomUUID()` id와 `isNew`를 가진다 — id 없는 새 줄을 만들지 않는다(ENG-D10)"
    - "복사해 온 줄 중 외화 줄이 원화 금액으로 들어가거나, 붙여넣기가 기존 외화 줄의 단가를 원화로 바꾸면 요약에 `외화 N줄 원화로`(`--warning`)가 붙는다 — 통화가 조용히 바뀌지 않는다. 앱 형식의 줄별 통화는 04-24의 `quoteLineClipboardMeta` 모양(`{ currency }`)을 읽는다(사용자 D15 「통화 다르면 경고」, CEO C-03, Copywriting 「PARTIAL — 붙여넣기 통화」)"
    - "엑셀이 복사 영역 끝에 붙이는 줄바꿈 하나는 빈 줄이 되지 않는다 — `a⇥b` + 줄바꿈은 한 줄, 45줄 복사는 `붙여넣기 45줄`이고, 상한까지 남은 수와 같은 줄 수의 붙여넣기가 거부되지 않는다(CEO C-05)"
    - "붙여넣기는 활성 셀에서 표시 줄 순서대로 페이지를 넘어 채우고(표 끝을 넘으면 새 줄), 화면은 시작 페이지에 머물며, 다른 페이지까지 채웠으면 합계 행 오른쪽에 `붙여넣기 45줄 · 2쪽까지`(`--muted`)가 보인다(§7-3 (자))"
    - "새 줄(`Ctrl+Enter`·`Ctrl+D`·붙여넣기)은 만든 페이지에 붙어 보인다 — 그 페이지가 잠시 30줄을 넘을 수 있고, 저장·다시 불러오기·페이지 이동 뒤에 30줄 단위로 다시 나뉜다. 1쪽에 고정된 새 줄에서 `↓`는 표시 순서상 다음 줄(2쪽)로 간다(C-18). 그룹을 지정해 줄을 더하는 버튼(조정 줄 추가·견적 외 비용 줄 추가 — 04-23)은 그 그룹 끝이 있는 페이지로 옮겨 가 새 줄에 포커스한다(§7-3 (자), CEO B-24)"
    - "저장이 거부되면 오류 셀이 있는 첫 페이지로 옮기고 첫 오류 셀에 포커스한다. 오류가 있는 다른 페이지 번호 옆에 `오류 N`(`--fs-xs --danger`, 접근 이름 `2쪽, 오류 2칸`)이 보인다(S4 error, S12, Copywriting 표시 — 페이지)"
    - "**고정된 오류 셀·해소되지 않은 충돌 셀이 남아 있어도 1차 「일괄 저장」은 비활성이 아니다** — 누르면(버튼·`Ctrl+S` 모두) 서버를 부르지 않고 첫 오류로 간다: 표 밖 칸(04-22의 기간 칸 · 04-44의 총 매출 예상가 칸)이 오류면 그 칸, 아니면 견적 줄 표의 첫 오류 셀이 있는 쪽으로 옮겨 그 셀, 아니면 매출 표의 첫 오류 셀(04-16이 04-41 봉투의 매출 줄 칸을 고정 오류 셀로 그린 것 — B3). 옛 클라이언트 오류 게이트 문자열과 그 비활성은 없어진다(P0). 오류 셀을 고치면(값이 바뀌면) 그 셀의 오류 표시가 풀리고 dirty가 된다(DR-5, UI-SPEC rev 5 UI Considerations S19 「error — 오류가 남은 채 1차」 652행, SYSTEM.md 개정 ⑫ (사) ⑵, §6-3 「ERROR는 1차를 살려 둔다」)"
    - "합계 행 오른쪽은 **한 줄**이다 — 항목을 `--danger` → `--warning` → `--muted` 순서로 ` · `로 잇고 각 항목은 제 색이다. 저장 성공 `저장됨 6줄 14:02`(`--success`)는 혼자 선다(나머지를 지운다). 붙여넣기 묶음은 `붙여넣기 45줄`로 시작해 warning(`오른쪽 2칸 버림` · `외화 1줄 원화로`) → muted(`계산 열 12칸 무시` · `조정 줄 3칸 건너뜀` · `2쪽까지`). 붙여넣기·단축키 상한 항목은 **다음 저장 시도(또는 다음 붙여넣기) 때 지워지고** 저장 결과가 그 자리를 채운다. 견적 표의 거부 요약은 견적 표에 제 오류·충돌 칸이 있을 때 들고, 제 칸이 0인데 다른 표(매출 칸)·표 밖 칸 오류로 거부됐으면 그 자리는 빈칸이 아니라 danger `전부 거부 · 다른 칸 오류 N칸`(04-16 `otherCellsRejectedText` — UI-SPEC rev 5 후속 결정 R2 47행 · Copywriting 363행)이다(DR-16, UI-SPEC rev 5 Copywriting 「표시 — 합계 행 오른쪽 한 줄(DR-16)」 404–408행, UI Considerations S4 partial, SYSTEM.md 개정 ⑫ 합계 행 한 줄)"
    - "dirty·오류·충돌 표시는 페이지를 오가도 남는다 — 미저장 편집의 브라우저 복원(D-68)도 페이지와 무관하게 줄 기준이다(S4 error)"
    - statement: "합계 행 오른쪽 한 줄 조각의 계산 색이 danger → warning → muted 순서이고 네 조각 이상에서 합계 행 안에서 잘리지 않으며(줄바꿈은 조각 경계) · 쪽 번호 옆 `오류 N`의 글자 크기·색 토큰과 접근 이름 `N쪽, 오류 M칸` · 오류가 남은 채 1차가 `aria-disabled`가 아님 · 375 가로 스크롤 0 — 1280·1024·375 독립 DOM 감사(교차 그룹 계약 6)"
      verification: backstop
  prohibitions:
    - statement: "붙여넣기가 페이지 끝에서 조용히 멈추거나 보이는 쪽의 줄만 채워서는 안 된다. 페이지는 보기일 뿐이다 — 채우기는 표시 순서로 쪽을 넘고, 채운 줄은 저장 때 전부 실린다(D-91 「견적 합계는 항상 전체 줄 기준」, D-67 「조용히 버리지 않는다」). 합계·저장 몫은 04-19의 금지 항목이다"
      status: resolved
      verification: test
    - statement: "붙여넣기가 값을 조용히 바꾸거나 버려서는 안 된다 — 무시한 계산 열, 원화로 바뀐 외화 줄, 끝 줄바꿈 처리 모두 요약 글자로 보이거나(무시·통화) 원본과 같은 결과여야 한다(끝 줄바꿈)(사용자 D15, D-67). 계산 열 무시는 앱에서 복사한 붙여넣기에만 — 엑셀에서 온 값이 계산 열 자리에 떨어지면 오류 칸이다(ENG-D5)"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/table/footer-notice.ts"
      provides: "합계 행 오른쪽 한 줄 조립 — danger → warning → muted 순서 · 저장 성공은 혼자 · 순수 함수(DR-16)"
      exports: ["composeFooterNotice"]
    - path: "test/unit/ui/footer-notice.test.ts"
      provides: "합계 행 한 줄 순서·색·성공 단독·빈 목록 표"
      min_lines: 20
    - path: "test/unit/ui/use-clipboard-paste.test.ts"
      provides: "`applyPaste` 결정표 — 앱 형식 유무에 따른 계산 열 무시/오류(6열 엑셀 사례)"
      contains: "application/x-plant8-quote-lines+json"
    - path: "ui/table/parse-tsv.ts"
      provides: "끝 줄바꿈 하나 제거 뒤 파싱(엑셀 CRLF)"
      contains: "parseTsv"
    - path: "ui/table/Table.tsx"
      provides: "새 줄 고정(`pinned` 채우기) · `revealRowId`(그룹 버튼 쪽 이동) · `footerNotices`/`footerSuccess`(합계 행 한 줄) · `firstIssueSignal`(첫 오류로 이동 — DR-5 · 저장 거부) · 쪽별 오류 수"
      contains: "firstIssueSignal"
  key_links:
    - from: "ui/table/use-clipboard-paste.ts"
      to: "ui/table/types.ts"
      via: "열의 `pasteRole`(`input` · `computed`)로 — 앱 형식이 있을 때만 — 계산 열을 무시하고 센다, 없으면 오류 칸(ENG-D5)"
      pattern: "computed"
    - from: "ui/table/Table.tsx"
      to: "ui/table/footer-notice.ts"
      via: "합계 행 오른쪽 요약을 `composeFooterNotice` 한 함수로 조립한다 — 화면이 넘긴 항목(붙여넣기 · 상한 · 저장 결과 · 거부 요약)과 표가 세는 오류·충돌이 한 줄이 된다(DR-16)"
      pattern: "composeFooterNotice"
    - from: "ui/table/Table.tsx"
      to: "ui/table/paging.ts"
      via: "직전 분할에 없던 줄 id(새 줄)를 만들어질 때의 쪽에 `splitPages`의 `pinned`로 고정한다(04-19가 둔 입력)"
      pattern: "pinned"
    - from: "ui/table/Table.tsx"
      to: "ui/pagination/Pagination.tsx"
      via: "쪽별 오류 칸 수를 `errorCounts`로 넘긴다(04-29) — 지금 쪽은 세지 않는다"
      pattern: "errorCounts"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/table/Table.tsx"
      via: "1차 「일괄 저장」이 고정 오류·충돌이 남아 있으면 서버를 부르지 않고 첫 오류로 이동 신호(`firstIssueSignal`)를 올린다 — 옛 오류 칸 게이트 비활성 대신(DR-5)"
      pattern: "firstIssueSignal"
    - from: "app/(app)/projects/[id]/revenue-section.tsx"
      to: "ui/table/Table.tsx"
      via: "받은 `firstIssueSignal`을 두 매출 `Table`에 넘기고, 첫 고정 오류 셀(04-16 `EntryDraft.cellErrors` — B3)이 있는 표가 반응한다"
      pattern: "firstIssueSignal"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/quote-table.tsx"
      via: "04-23의 그룹 버튼 단일 처리 지점 `addLineToGroup(lineKind)`이 돌려준 새 줄 id로 `revealRowId`를 설정한다(B-24)"
      pattern: "addLineToGroup"
---

<objective>
**As a** 기획 PM, **I want to** 다른 프로젝트 견적을 통째로 복사해 와도 오류 칸 없이 들어오고, 엑셀에서 붙인 값은 사라지지 않으며, 긴 견적에서 붙여넣기·저장 거부·남은 오류가 페이지 경계와 무관하게 한 표처럼 동작하기를, **so that** 이전 견적을 옮기는 일(PROJ-05)이 막히지 않고, 페이지 경계 때문에 줄이 잘리거나 값이 조용히 바뀌지 않으며, 오류가 남은 채 저장을 누르면 고칠 칸으로 곧장 간다.

머지 묶음: ④/4 목록·리저브 — 웨이브 25~30(04-17 → 04-31)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**실행 규율 보강(2026-09-24 재계획 — CLAUDE.md · hooks 3bed7b1 · 세션 B 교훈):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋(문서 포함)과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(플랜 하나 — 체크포인트 이어가기도 새 세션). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 화면 검증은 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번이고, 완료 판정은 로컬 dev가 아니라 `CI=true`다.

**2026-09-23 체커 반복 1 분할(W2) — 04-19에서 떼어 낸 플랜.** 옛 04-19(16파일 · 130k)의 Task 3을 이 플랜으로 옮겼다. 04-19가 쪽 나눔(`pagination`·`splitPages`의 `pinned` 입력)·쪽 경계 키보드(`{ rowId, colKey }` 포커스)·네이티브 복사(04-24 직렬화)·힌트 줄을 끝냈고, 이 플랜은 그 위에 **붙여넣기**(계산 열 무시·통화 경고·끝 줄바꿈·쪽을 넘는 채우기·새 줄 고정·그룹 버튼 쪽 이동) · **합계 행 한 줄**(DR-16) · **오류가 남은 채 1차**(DR-5) · **저장 거부 뒤 오류 쪽과 `오류 N`** · **표시 유지**를 얹는다. 옛 Task 3의 단계·behavior·수용 기준을 두 태스크로 나눴고 내용은 줄이지 않았다.

**2026-09-23 CEO 리뷰 반영(HOLD SCOPE — 04-19에서 옮김):** C-03·사용자 D15(계산 열 무시 + 건너뛴 수 + 외화 경고) · C-05(엑셀 끝 줄바꿈) · C-18의 E2E 몫(고정된 새 줄에서 `↓`) · B-24(그룹 버튼 새 줄의 쪽 — 04-23 발견). 이 플랜 뒤 04-31이 실제 엑셀로 최종 사람 확인을 한다(사용자 D21). `ui/table`을 같이 고치는 04-18(머리글 정렬·열 접기)은 이 플랜 **뒤**다.

**2026-09-23 엔지니어링 리뷰 반영(04-19에서 옮김):** (a) **ENG-D5(R4 — 엔지 리뷰 C §1 P1):** 계산 열 무시는 클립보드에 `application/x-plant8-quote-lines+json`이 있을 때(앱에서 복사)만 — 그 밖(엑셀)은 04-04의 오류 칸을 유지하고, 6열 엑셀 사례를 단위·E2E로 고정한다(04-31(a)도 같은 사례를 사람이 확인). (e) **ENG-D10:** 붙여넣기로 생긴 새 줄도 04-30의 새 줄 경로(`crypto.randomUUID()` · `isNew`). (f) 숫자 정규화는 04-09가 합친 `normalizeNumericPaste` → `stripNumberInput` 하나 — 이 플랜이 새 정규화를 만들지 않는다.

**2026-09-23 디자인 리뷰 반영(04-19에서 옮김 — UI-SPEC rev 5 재계획 그룹 C):** (a) **DR-5:** 고정 오류·충돌이 남아도 1차는 살아 있고, 누르면 서버를 부르지 않고 첫 오류(표 밖 칸 → 견적 표의 첫 오류 쪽·셀 → 매출 표)로 간다 — 옛 클라이언트 게이트 문자열과 비활성을 지운다(Task 2). (b) **DR-16:** 합계 행 오른쪽을 한 줄로(danger → warning → muted, 성공은 혼자, 붙여넣기 안내는 다음 저장 시도 때 지움) — 순수 조립 함수 `composeFooterNotice`(Task 1). (c) 04-28의 거부 봉투(서버 형식 오류·충돌 → 고정 셀)가 DR-5·저장 거부 쪽 이동의 입력이다. 매출 표의 고정 오류 셀은 04-16이 04-41 봉투의 매출 줄 칸(`rowId` · `amount`)으로 그린다(B3). (d) DOM 감사 폭 1280·1024·375(교차 그룹 계약 6). (e) 교차 그룹 계약 3(저장 중 편집 잠금 `saveLocked`)은 그대로다 — 저장 중 잠금 E2E가 초록으로 남는다.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시한다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다.

Purpose: 04-19가 표를 쪽으로 나눈 뒤에도 붙여넣기·저장 거부·남은 오류가 쪽 경계에서 줄을 잃거나 튀지 않게 한다 — D-91이 계획에 맡긴 「경계에서 붙여넣기가 어떻게 넘는지」와 D-67 「조용히 버리지 않는다」의 답.

Output: 파서 끝 줄바꿈 · 붙여넣기 결정(출처 · 계산 열 · 통화) · 쪽을 넘는 채우기 · 새 줄 고정과 그룹 버튼 쪽 이동 · 합계 행 한 줄 조립(`composeFooterNotice`) · 첫 오류로 이동(`firstIssueSignal`) · 쪽별 오류 표시 · 단위 4 · E2E 확장 · 화면 검증.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-19-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-28-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-16-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다(U-2는 `## UI Considerations` S5 partial 529행의 사용자 확정값). 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md rev 5(절 이름으로 찾고 아래는 2026-09-23 현재 행) — 「SYSTEM.md 개정」 ④ (자)(780–803행 — 붙여넣기 · 새 줄 · 저장 거부 792–799) · ⑫(898–926행 — (사) ⑴⑵⑶ · 합계 행 한 줄 907–909) · UI Considerations S4(501–520행 — error 505 · partial 붙여넣기 507 · error 다른 페이지 513) · S6 error(537행) · S19(649–656행 — 오류가 남은 채 1차 652) · 적용 규칙 S4(1209–1230행 — 붙여넣기 · 합계 행 한 줄 · 표 위 한 줄 순서) · Copywriting 361–362행(Error — 표 배치 저장 실패 · U-6) · 403–408행(표시 — 페이지 · 합계 행 오른쪽 한 줄 · PARTIAL 붙여넣기 셋)

# docs/designs/plant8-erp-phase4-design-review-260923.md 결정 원장 DR-5 · DR-16 행

# docs/designs/plant8-erp-phase4-ceo-review-260923.md 1727–1779행(C-03 · C-05) · 1924–1935행(C-18) · 1315–1320행(B-24)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 없다 — 04-19에서 옮긴 S4 error(다른 페이지의 오류)·partial(붙여넣기 요약 한 줄, DR-16) · S19 error(오류가 남은 채 1차, DR-5)는 UI-SPEC rev 5에 분류돼 있고 truths로, 합계 행 한 줄·`오류 N`·1차 렌더는 backstop 마커(1280·1024·375 DOM 감사)로 옮겼다.

**계획 단계 가정(표면화 — 04-19에서 옮김):**

- **외화 경고의 근거(사용자 D15):** 04-19의 복사 이벤트가 `text/plain` TSV와 함께 앱 전용 형식 `application/x-plant8-quote-lines+json`(줄마다 `{ currency }` — 04-24 `quoteLineClipboardMeta`가 주인)을 싣는다 — Chromium 계열(회사 표준 Chrome·Edge)은 사용자 정의 형식을 같은 브라우저 안 붙여넣기에 돌려준다. 엑셀에서 온 붙여넣기에는 이 형식이 없어 외화 경고도 없다(엑셀 값에는 통화 정보가 없다). 붙여넣기가 기존 외화 줄의 단가 칸을 덮으면(지금 코드가 `unitPriceCurrency: "KRW"`로 바꾼다) 그 줄도 같은 경고에 센다. 이전 차수 복원 줄 「복사」(04-24)도 같은 두 형식이라 같은 규칙을 탄다.
- **계산 열(ENG-D5):** 열 정의에 `pasteRole: "input" | "computed"`를 둔다 — 번호·견적가·차익·상태가 `computed`. 붙여넣기는 위치로 열을 맞추는 지금 규칙을 그대로 쓰되, **클립보드에 앱 전용 형식이 있을 때만** `computed` 열 칸을 소비만 하고 값을 넣지 않으며 센다. 앱 형식이 없으면(엑셀 등) `computed` 칸은 04-04 규칙대로 오류 칸이다 — 엑셀 6열(`소분류·항목·거래처·수량·단가·실행가`)을 소분류에서 붙이면 실행가가 견적가 자리에 떨어지는데, 그 값을 무시하면 실행가가 조용히 사라지기 때문이다(엔지 리뷰 C §1 P1). 잠긴 입력 칸(상태별 잠김)은 두 경우 모두 04-04 규칙대로 오류다.
- **새 줄 고정:** `Table`이 직전 분할에 없던 id(새 줄)를 만들어질 때의 쪽에 고정하고(04-19의 `splitPages` `pinned` 입력), 재분할은 04-19의 세 시점(저장 성공 · 서버 다시 불러오기 · 사용자의 페이지 이동)뿐이다.
- **매출 표의 오류(B3):** 매출 줄 금액 입력 오류는 04-41이 `SaveRejectedError`의 칸 오류(줄 id · `amount`)로 던지고 04-28의 봉투 `{ rejected: { summary, cells } }`로 온다. 04-16이 그 칸을 매출 줄 `EntryDraft.cellErrors`로 떼어 매출 표의 고정 오류 셀과 그 표 합계 행 `오류 N칸 · 전부 거부`로 그린다. 이 플랜은 그 칸을 DR-5의 「남은 오류」 셈과 첫 오류 이동(매출 표 순서)에 넣을 뿐 봉투·매출 표 렌더를 바꾸지 않는다.

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 다른 프로젝트에서 복사한 45줄을 붙여넣으면 계산 열은 무시되고 통화 변화는 경고되며 합계 행 오른쪽이 한 줄로 요약되고, 저장 뒤 원화 합계가 같다(파서 → 붙여넣기 결정 → 쪽을 넘는 채우기 · 새 줄 고정 → 합계 행 한 줄 → 저장 → E2E) + 그룹 버튼 쪽 이동(B-24)</name>
  <files>ui/table/parse-tsv.ts, test/unit/ui/parse-tsv.test.ts, ui/table/use-clipboard-paste.ts, test/unit/ui/use-clipboard-paste.test.ts, ui/table/types.ts, ui/table/Table.tsx, ui/table/paging.ts, test/unit/ui/table-paging.test.ts, ui/table/footer-notice.ts, test/unit/ui/footer-notice.test.ts, app/(app)/projects/[id]/quote-table.tsx, test/e2e/quote-table.spec.ts</files>
  <read_first>
    - `ui/table/parse-tsv.ts`(전문 — 62–64행 끝 빈 줄 · 90행 근처 `normalizeNumericPaste` — 04-09 이후 `stripNumberInput`에 맡긴다, 이 플랜은 숫자 정규화를 새로 만들지 않는다) · `test/unit/ui/parse-tsv.test.ts`(전문)
    - `ui/table/use-clipboard-paste.ts`(전문 — `applyPaste` 결과 형태, 열 위치 매핑, 71행 `읽기 전용·잠김 셀에 값이 떨어졌습니다` 오류 — 앱 형식이 없는 붙여넣기에서 그대로 쓴다)
    - `ui/table/Table.tsx`·`ui/table/paging.ts`(04-19 뒤 — `pagination` · `splitPages`의 `pinned` 입력 · `pageOfRow` · 합계 행 오른쪽 요약(오류 N칸 / 충돌 N줄 / 붙여넣기 버림 N칸)을 그리는 곳)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep `handlePasteAtCell`·`applyPaste`·`unitPriceCurrency: "KRW"`(792행 근처)·`addLine`·`duplicateLine`·`addLineToGroup`(04-23 Task 1 ② — 그룹 버튼의 단일 처리 지점, 새 줄 id를 돌려준다)·`handleSave`·04-26 상한 이유(Grep `300줄 상한`)·04-28 `rejectionSummary`로 경로들의 범위만 Read
    - `app/(app)/projects/[id]/previous-revision.tsx`(04-24 — `quoteLineClipboardMeta`: 앱 형식 JSON 모양 `{ currency }[]`의 주인 — 붙여넣기는 그 모양을 읽는다)
    - `.planning/phases/04-project-quote-ledger/04-19-SUMMARY.md` — 복사 캡처 도우미(E2E) · `pinned` 입력 · 포커스 모델 · 새 줄 경로
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정」 ④ (자)(792–799행 — 붙여넣기 · 새 줄) · ⑫(907–909행 — 합계 행 한 줄) · Copywriting 404–408행(표시 — 합계 행 오른쪽 한 줄 · PARTIAL 붙여넣기) · UI Considerations S4 partial(507행)
  </read_first>
  <behavior>
    - (C-05) `parseTsv("a\tb\r\n")` → `[["a","b"]]` · `parseTsv("a\tb\n")` → `[["a","b"]]` · `parseTsv("a\tb\r\n\r\n")` → `[["a","b"],[""]]`(끝 줄바꿈 **하나만** 뗀다) · `parseTsv("")` → `[[""]]` · 인용 칸 안의 줄바꿈은 그대로
    - (C-03) 앱 형식이 실린 번호 열 시작 11열 붙여넣기(번호·소분류·…·견적가·차익·상태) → 계산 열 넷의 칸은 값이 들어가지 않고 오류 칸 0, 요약 `계산 열 N칸 무시`(N = 줄 수 × 4)
    - (ENG-D5 · 엔지 리뷰 C 공백 3) 앱 형식이 **없는** 6열 엑셀 `소분류⇥항목⇥거래처⇥수량⇥단가⇥실행가` 한 줄을 소분류 칸에서 붙여넣기 → 견적가 자리 칸(엑셀의 실행가 값)이 `읽기 전용·잠김 셀에 값이 떨어졌습니다` 오류 칸이고 `ignoredComputedCells` 0 · 요약에 `계산 열` 조각 없음 · 같은 6열을 앱 형식과 함께 붙이면 그 칸은 무시(1칸)이고 오류 0 — `applyPaste` 단위(`test/unit/ui/use-clipboard-paste.test.ts`)
    - (ENG-D10) 붙여넣기가 표 끝을 넘어 만든 새 줄은 모두 uuid 형식 id와 `isNew: true`를 가진다
    - (C-03) 앱 전용 형식에 외화 줄 1이 표시된 45줄 붙여넣기 → 요약에 `외화 1줄 원화로`(`--warning`) · 기존 USD 줄의 단가 칸을 덮는 붙여넣기 → 그 줄도 센다 · 엑셀처럼 앱 형식이 없으면 외화 표시 없음
    - (DR-16 단위 — `composeFooterNotice`) `[muted "2쪽까지", warning "오른쪽 2칸 버림", danger "오류 1칸 · 전부 거부", muted "계산 열 12칸 무시", warning "외화 1줄 원화로"]` + 붙여넣기 머리 `붙여넣기 45줄` → 조각 순서 `오류 1칸 · 전부 거부` → `붙여넣기 45줄` → `오른쪽 2칸 버림` → `외화 1줄 원화로` → `계산 열 12칸 무시` → `2쪽까지`, 각 조각이 제 톤 · 성공 `저장됨 6줄 14:02`가 있으면 그 하나만 · 빈 목록 → 조각 0개
    - (DR-16) 붙여넣기 요약이 보이는 상태에서 1차를 누르면(저장 시도) 붙여넣기·단축키 상한 조각이 지워지고 저장 결과(성공 `저장됨 …` 또는 거부 요약)가 그 자리를 채운다 · 다음 붙여넣기도 이전 붙여넣기 조각을 바꾼다
    - 142줄 표 1쪽 20번째 줄에서 45줄 붙여넣기 → 표시 순서 20~64번째 줄이 바뀌고 화면은 1쪽, 요약 `붙여넣기 45줄 · 3쪽까지` → 저장 뒤 새로 고쳐 20~64번째 줄이 전부 붙여 넣은 값이다(금지 항목 — 보이는 쪽만 채우지 않는다)
    - 10줄 표(1쪽)에서 1번째 줄에 45줄 붙여넣기 → 10줄 덮고 35줄 새로 만들어 1쪽에 45줄이 보이고(고정) 페이지 줄 없음 → 저장 성공 뒤 `1–30 / 45줄` 두 쪽
    - 2쪽에서 Ctrl+Enter로 만든 새 줄은 저장 전까지 2쪽에 있다 · (C-18 E2E — 04-19 단위의 짝) 1쪽에서 새 줄을 만들어 1쪽이 31줄일 때 그 줄에서 `↓` → 2쪽의 표시 순서상 다음 줄로 포커스가 옮는다
    - (B-24) 31줄 이상 표의 1쪽에서 그룹 버튼(조정 줄 추가)을 누르면 그 그룹 끝이 있는 쪽으로 옮겨 새 줄에 포커스한다
  </behavior>
  <action>
① **끝 줄바꿈(C-05).** `behavior` 첫 줄을 단위 케이스로 먼저 쓴다(RED). `parseTsv`가 파싱 전에 입력 끝의 줄바꿈 **하나**(`\r\n` 또는 `\n`)만 뗀다 — 인용 안 줄바꿈은 상태 기계가 그대로 다룬다. `parseTsv("") → [[""]]`는 유지(GREEN).

② **계산 열과 통화(C-03 · 사용자 D15) + 합계 행 한 줄(DR-16).** 결정표(`behavior`의 11열 앱 · 6열 엑셀 · 6열 앱)를 `test/unit/ui/use-clipboard-paste.test.ts`에, 조립 표(`behavior`의 DR-16 단위 줄)를 `test/unit/ui/footer-notice.test.ts`에 먼저 쓴다(RED). `ui/table/types.ts`의 `TableColumn`에 `pasteRole?: "input" | "computed"`(기본 `input`)를 더한다. `use-clipboard-paste.ts`의 `applyPaste`가 붙여넣기 출처 `source: "app" | "external"`(이벤트의 `clipboardData.types`에 `application/x-plant8-quote-lines+json`이 있으면 `app`)을 받아, 위치로 열을 맞추는 지금 규칙을 그대로 쓰되 **`app`일 때만** `computed` 열 칸을 소비만 하고(값을 넣지 않고 오류로 만들지 않는다) `ignoredComputedCells`로 센다. `external`이면 `computed` 칸은 지금(04-04)처럼 `읽기 전용·잠김 셀에 값이 떨어졌습니다` 오류 칸이다 — 엑셀 6열의 실행가가 견적가 자리에서 조용히 사라지지 않는다(ENG-D5 — 엔지 리뷰 C §1 P1). 잠긴 입력 칸은 두 출처 모두 04-04 규칙대로 오류다. 앱 형식이 있으면 줄마다 `currency`(04-24 `quoteLineClipboardMeta` 모양)를 읽어 결과에 `sourceCurrencies`로 싣는다(파싱 실패는 `external`과 같게 — 경고 없음 · 계산 열은 오류). `quote-table.tsx`의 각 열에 `pasteRole`(번호·견적가·차익·상태 `computed`, 나머지 `input`)을 주고, `handlePasteAtCell`은 외화였던 원본 줄 수 + 이번 붙여넣기로 단가 칸이 원화로 바뀐 기존 외화 줄 수를 더해 `foreignToKrw`로 센다(792행의 `unitPriceCurrency: "KRW"` 강제는 그대로 — 값 보존은 원화 첫 줄 복사로 된다). 합계 행 오른쪽 요약은 **한 줄 조립(DR-16)**으로 옮긴다 — `ui/table/footer-notice.ts`(신규, 순수)의 `composeFooterNotice(items, { successText? })`: 항목 `{ tone: "danger" | "warning" | "muted", text }`을 danger → warning → muted 순서(같은 톤 안에서는 들어온 순서, 붙여넣기 머리 `붙여넣기 {N}줄`은 warning·muted 붙여넣기 조각 바로 앞)로 세우고, `successText`가 있으면 그 하나만(`--success`) 돌려준다. `TableProps.footerNotices?`(항목 목록)와 `footerSuccess?`를 받아 `Table`이 기존 합계 행 오른쪽 `aria-live` 자리에 조각마다 제 톤 클래스로 ` · ` 잇기 렌더한다 — `Table`이 지금 스스로 세는 오류·충돌 요약도 같은 목록의 danger 항목으로 합친다(두 곳에서 따로 그리지 않는다). 견적 표의 서버 거부 요약(04-28 `rejectionSummary`)은 견적 표에 제 오류·충돌 칸이 있을 때 danger 항목으로 넣고, 제 칸이 0인데 다른 표(매출 칸)·표 밖 칸 오류로 거부됐으면 04-16의 `otherCellsRejectedText` 글자(`전부 거부 · 다른 칸 오류 N칸`)를 danger 항목으로 넣는다 — 빈 자리로 두지 않는다(UI-SPEC rev 5 후속 결정 R2 47행 · Copywriting 363행 · S19 664행). 매출 칸의 제 글자(`오류 N칸 · 전부 거부`)는 04-16의 매출 표 합계 행이 말한다(rev 5 S6 error 「그 표 합계 행」). 붙여넣기 조각: `붙여넣기 {N}줄` · warning `오른쪽 {n}칸 버림`(04-04의 지금 경고 문자열을 이 조각으로 바꾼다) · warning `외화 {j}줄 원화로` · muted `계산 열 {M}칸 무시` · muted `조정 줄 {k}칸 건너뜀`(04-23이 만든 조각이 있으면 같은 목록으로) · muted `{k}쪽까지` — 0인 조각은 넣지 않는다. 단축키 상한 이유(04-26 — `300줄 상한 · 상한은 관리자 설정`)는 danger 항목. **수명:** `quote-table.tsx`가 붙여넣기·단축키 상한 항목을 상태 하나에 두고 1차를 누를 때(저장 시도 — Task 2의 DR-5 이동 경로 포함)와 다음 붙여넣기 때 비운다(GREEN).

③ **쪽을 넘는 붙여넣기.** `onPasteAtCell`이 채운(바꾸거나 만든) 줄 id 목록을 돌려주게 넓힌다. 표 끝을 넘어 만드는 새 줄은 04-30이 둔 새 줄 경로(`crypto.randomUUID()` id · `isNew: true`)로 만든다 — 붙여넣기만의 id 없는 새 줄을 만들지 않는다(ENG-D10). 채우는 순서는 **표시 순서**(04-19의 정규화 — C-04). `Table`은 시작 쪽에 머물고, 채운 줄의 마지막 쪽(`pageOfRow`)이 시작 쪽보다 뒤면 요약에 `{k}쪽까지`를 더한다. 새로 만든 줄은 시작 쪽에 고정한다. 상한(04-26)을 넘는 붙여넣기의 전부 거부는 04-26 경로 그대로다. 저장 페이로드는 전체 줄 배열의 dirty 줄이라 채운 줄이 쪽과 무관하게 전부 실린다(금지 항목).

④ **새 줄 고정과 그룹 버튼(B-24).** 고정 규칙 단위 케이스(`splitPages`에 직전 분할에 없던 id가 들어오면 만들어진 쪽에 남는지 — `test/unit/ui/table-paging.test.ts`)를 먼저 더한다(RED). `Table`이 직전 분할에 없던 id(새 줄 — `Ctrl+Enter`·`Ctrl+D`·붙여넣기·「줄 추가」)를 **만들어질 때의 쪽**에 고정한다(04-19의 `splitPages` `pinned`). 화면이 `revealRowId`를 넘기면(그룹을 지정해 더하는 버튼 — 04-23의 조정 줄 추가·견적 외 비용 줄 추가) `Table`은 그 줄의 표시 위치(그룹 끝 — 04-19 정규화)가 있는 쪽으로 옮기고 그 쪽에 고정한 뒤 그 줄 첫 편집 셀에 포커스한다. `quote-table.tsx`는 04-23 Task 1 ②의 **단일 처리 지점 `addLineToGroup(lineKind)`**(조정 줄 추가·견적 외 비용 줄 추가가 둘 다 지나는 곳)에서, 그 함수가 돌려준 **새 줄 id**로 `revealRowId`를 설정한다 — 버튼마다 따로 걸지 않고, 표시 위치에서 새 줄을 다시 찾지 않는다. 재분할은 04-19의 세 시점뿐이다(GREEN).

⑤ **E2E 추가**(`quote-table.spec.ts` — `behavior`의 E2E 줄들을 준비와 함께). **엑셀 6열(ENG-D5):** 앱 형식 없이 `text/plain`만 실은 합성 붙여넣기(`소분류⇥항목⇥거래처⇥수량⇥단가⇥실행가` 두 줄)를 소분류 칸에 → 견적가 칸 둘이 오류 셀(`aria-invalid`)이고 합계 행에 `오류 2칸`, 요약에 `계산 열` 조각이 없다 · 04-04가 둔 외부 붙여넣기 오류 케이스(있으면)가 그대로 초록. **프로젝트 간 왕복(C-03):** 두 그룹 · USD 줄 1개 · 45줄 프로젝트 A에서 `Control+a` → `Control+c`(04-19의 캡처로 `text/plain`과 앱 형식을 얻는다) → 프로젝트 B(0줄)에서 첫 줄 만들기 → 번호 열에 두 형식을 실은 합성 붙여넣기 이벤트 → 오류 칸 0 · 합계 행 오른쪽 한 줄이 `붙여넣기 45줄 · 외화 1줄 원화로 · 계산 열 180칸 무시`(조각 순서·톤 — DR-16) · 저장 뒤 B의 원화 합계 = A의 원화 합계 · 저장 뒤 붙여넣기 조각이 사라지고 `저장됨 …`만. 쪽을 넘는 붙여넣기(142줄 · `3쪽까지` · 저장 뒤 20~64번째 값) · 10줄 표에 45줄(저장 전 한 쪽 45줄 → 저장 뒤 두 쪽) · 2쪽 `Control+Enter` 새 줄이 2쪽에 머묾 · 1쪽 고정 새 줄에서 `↓` → 2쪽 다음 줄(C-18) · 그룹 버튼으로 그룹 끝 쪽 이동 + 포커스(B-24).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/parse-tsv.test.ts test/unit/ui/table-paging.test.ts test/unit/ui/use-clipboard-paste.test.ts test/unit/ui/footer-notice.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 끝 줄바꿈 처리·인용 규칙·새 줄 고정 규칙·합계 행 조각 순서가 계약과 다르거나, 앱 형식이 없는 엑셀 붙여넣기의 계산 열 자리 값이 오류 칸이 되지 않는다(ENG-D5)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-table.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 계산 열 무시·통화 경고·합계 행 한 줄·쪽을 넘는 붙여넣기·새 줄 고정·그룹 버튼 쪽 이동 중 하나가 화면까지 이어지지 않았거나 04-19의 쪽 나눔·키보드·복사 케이스가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `parseTsv` 단위 케이스에 `"a\tb\r\n"` → 한 줄과 「끝 줄바꿈 하나만」 케이스가 있다(C-05)
    - 프로젝트 간 왕복 E2E가 오류 칸 0 · 계산 열 무시 수 · 외화 경고 · 합계 같음 넷을 단언한다(C-03, 사용자 D15)
    - 열 정의에 `pasteRole: "computed"`가 번호·견적가·차익·상태 넷에만 있다
    - `applyPaste`가 앱 형식 유무를 입력으로 받고, 단위 결정표에 「6열 엑셀(앱 형식 없음) → 견적가 자리 오류 · 무시 0」과 「같은 6열 + 앱 형식 → 무시 1 · 오류 0」이 있다 · E2E에 엑셀 6열 오류 칸 케이스가 있다(ENG-D5 · 엔지 리뷰 C 공백 3)
    - 붙여넣기로 만든 새 줄의 id가 `crypto.randomUUID()`에서 온다(Grep — 붙여넣기 새 줄 경로가 04-30의 새 줄 생성 함수를 부른다)
    - E2E에 「쪽을 넘는 붙여넣기 뒤 화면이 시작 쪽이고 요약에 `3쪽까지` · 저장 뒤 채운 줄 전부 저장」(금지 항목) · 「10줄 표에 45줄 → 저장 전 한 쪽 45줄 → 저장 뒤 두 쪽」 · 「고정 새 줄에서 `↓` → 2쪽」(C-18) · 「그룹 버튼으로 그룹 끝 쪽 이동 + 포커스」(B-24) 케이스가 있다
    - `composeFooterNotice` 단위 표가 초록이고 `Table`이 합계 행 오른쪽을 이 함수 하나로 그리며, E2E가 조각 순서·톤과 저장 시도 때 붙여넣기 조각이 지워지는 것을 단언한다(DR-16) · `외화 N줄 원화로` 뒤에 다른 낱말이 붙지 않는다(rev 5 문구)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>다른 프로젝트 견적과 엑셀 붙여넣기가 계산 열·통화·끝 줄바꿈 규칙대로 들어오고 그 결과가 합계 행 한 줄로 보이며, 붙여넣기·새 줄·그룹 버튼이 쪽 경계에서 줄을 잃거나 튀지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 오류가 남은 채 1차는 서버 없이 첫 오류로(DR-5) · 저장 거부 뒤 첫 오류 쪽과 번호 옆 `오류 N` · 쪽을 오가도 표시 유지 + 화면 검증(1280 · 1024 · 375) + 전체 게이트</name>
  <files>ui/table/Table.tsx, ui/table/types.ts, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/revenue-section.tsx, test/e2e/quote-table.spec.ts</files>
  <read_first>
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 1차 「일괄 저장」의 `disabledReason` 조건(오류 칸 수 갈래 — P0 표 「오류 게이트」 행이 지우라고 한 옛 문자열)과 `handleSave`의 클라이언트 게이트, 04-28의 거부 봉투 처리(`rejected`·`cellConflicts`·`rejectionSummary`)와 04-16이 그 앞에 둔 매출 칸 떼어 내기(`routeRejectedRevenueCells`)와 제 오류 0칸인 표의 글자(`otherCellsRejectedText` — 후속 결정 R2), 그룹 A의 표 밖 칸(04-22 기간 칸 · 04-44 총 매출 예상가 칸 — Grep `periodDraft`·`preEstimate` 류)과 그 오류 상태를 찾아 범위만 Read
    - `app/(app)/projects/[id]/revenue-section.tsx` — 매출 두 표의 `Table` 호출부와 04-16의 `EntryDraft.cellErrors` → `cellIssue`(고정 오류 셀, B3)
    - `ui/table/Table.tsx`(Task 1 뒤) — `cellIssue`의 `kind`(`error` · `conflict` · 04-30의 `reason`) · 쪽 상태 · `Pagination` 호출
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` rev 5 「SYSTEM.md 개정」 ⑫(900–906행 — (사) ⑵ 오류가 남아도 1차) · UI Considerations S4 error 다른 페이지(513행) · S19 error(652행) · Copywriting 403행(표시 — 페이지)
    - `test/e2e/quote-table.spec.ts` — 04-28의 서버 형식 오류 셀 준비 방식(거부 봉투) · `playwright.config.ts`(`CI=true`에서만 프로덕션 빌드)
  </read_first>
  <behavior>
    - (DR-5) 3쪽 한 셀에 형식 오류(붙여넣기 `abc`) → 1쪽으로 돌아가 1차 「일괄 저장」 클릭 → 서버 액션 요청 0건 · 쪽 번호 3이 `aria-current` · `document.activeElement`가 그 오류 셀 · `Control+s`도 같다 · 1차가 `aria-disabled`가 아니다 · 옛 오류 칸 게이트 문자열이 화면에 없다 · 그 셀을 숫자로 고치면 오류 표시가 풀리고 다시 누르면 저장된다
    - (DR-5) 표 밖 칸(기간 칸)에 서버 오류가 고정된 상태 + 견적 표 오류 → 1차 → 포커스가 기간 칸 입력(화면 위쪽이 먼저)
    - 1쪽 5행과 3쪽 2행에 서버 오류 → 거부 뒤 1쪽 + 5행 첫 오류 셀 포커스, 3쪽 번호 옆 `오류 1` · 1쪽만 오류가 있으면 다른 번호 옆 표시 없음
    - 2쪽의 dirty 셀 표시가 1쪽 → 2쪽 왕복 뒤에도 남는다
    - (R2) 경영관리가 발행 줄 금액 칸만 범위 밖(`3000000000`)으로 저장 → 서버 거부 뒤 견적 줄 표 합계 행 한 줄이 danger `전부 거부 · 다른 칸 오류 1칸`(빈 자리 아님 — `composeFooterNotice` 경로를 지난 뒤) · 발행 표 합계 행 `오류 1칸 · 전부 거부` · 포커스가 그 발행 금액 셀(견적 표에 오류가 없으니 매출 표가 첫 오류) · 셀을 고쳐 저장이 성공하면 두 글자가 사라지고 `저장됨 …`만
  </behavior>
  <action>
① E2E(`behavior` 넷)를 먼저 쓴다(RED). 서버 액션 요청 수는 `page.on("request")`로 `Next-Action` 헤더가 있는 POST를 센다(0). 서버 오류는 04-28의 거부 봉투(서버 형식 오류 셀) 방식을 따른다.

② **저장 거부 · 오류가 남은 채 1차(DR-5) · 표시 유지.** `TableProps.firstIssueSignal?: number`를 더한다(`pagination`이 없는 표에도 쓴다 — 매출 표). 값이 바뀌면 `Table`이 `cellIssue`가 **오류·충돌**(`kind: "error" | "conflict"`)인 셀을 표시 순서로 훑어 첫 셀의 쪽으로 옮기고(쪽이 있으면) 그 셀에 포커스한다 — 04-30의 `kind: "reason"`(잠긴 셀 편집 시도 이유 — 고정 오류가 아니다)은 세지 않고, 찾은 셀이 없으면 아무것도 하지 않는다. `Pagination`에 쪽별 오류 칸 수(`errorCounts`)를 넘긴다 — 지금 쪽은 세지 않는다. `quote-table.tsx`는 두 자리에서 신호를 올린다: (a) 저장 거부(04-28 거부 봉투의 형식 오류·충돌 · 게이트) 처리 지점 — 거부가 매출 칸·표 밖 칸만이면 견적 표 합계 행은 Task 1 ②대로 `전부 거부 · 다른 칸 오류 N칸`(후속 결정 R2)이고 신호는 첫 오류가 있는 곳(표 밖 칸 → 매출 표)이 받는다 (b) **1차 「일괄 저장」(버튼·`Ctrl+S`)을 눌렀는데 고정된 오류 칸·해소되지 않은 충돌 칸이 하나라도 남아 있을 때 — 서버 액션을 부르지 않고** 첫 오류로 간다(DR-5): 표 밖 칸(04-22의 기간 칸·총 매출 예상가 칸 — 그 칸의 오류 상태)이 오류면 그 칸 입력에 포커스(묶음이 닫혀 있으면 기간 칸은 04-22의 `openPeriodField`, 총 매출 예상가 칸은 04-44가 둔 묶음 여는 경로), 아니면 견적 줄 표의 `firstIssueSignal`, 아니면 매출 표 — `revenue-section.tsx`가 받은 신호를 두 매출 `Table`에 넘기고 첫 오류가 있는 표가 반응한다. 「남은 오류」 셈에는 매출 줄의 고정 오류 칸(04-16 `EntryDraft.cellErrors` — B3)도 든다. 이 이동도 저장 시도라 Task 1의 붙여넣기·상한 조각 비우기를 지난다. 1차의 `disabledReason`에서 오류 칸 수 갈래(옛 클라이언트 게이트 문자열)를 지우고 `handleSave`의 클라이언트 게이트를 이 이동으로 바꾼다 — 1차는 오류 때문에 비활성이 되지 않는다(`바뀐 칸 없음` info와 저장 중 `…`만 남는다 — 그룹 A 몫은 건드리지 않는다). 오류 셀 값을 고치면 그 셀의 오류·충돌 표시가 풀린다(04-28 경로 그대로). dirty·오류·충돌·저장 틴트 판정이 줄 id와 열 키로만 되는지 확인하고, 쪽 안 위치로 계산하는 곳이 있으면 id 기준으로 고친다. 미저장 복원(D-68)의 저장 키가 id 기준임을 04-19 SUMMARY에서 확인한 결과를 SUMMARY에 옮겨 적는다(GREEN).

③ **화면 검증(CLAUDE.md 순서).** 싼 게이트 → **독립 DOM 감사**(별도 서브에이전트, `model` 명시, `verification-before-completion` 명시, `CI=true` 프로덕션 빌드 DOM 실측, 스크린샷 육안 금지) **1280·1024·375 세 폭**(교차 그룹 계약 6): `오류 N` 글자 크기·색 토큰 · 접근 이름 `N쪽, 오류 M칸` · 합계 행 오른쪽 한 줄 조각의 계산 색이 danger → warning → muted 순서이고 네 조각 이상에서 합계 행 안에서 잘리지 않음(줄바꿈은 조각 경계) · 1차 「일괄 저장」이 오류가 남아도 `aria-disabled`가 아님 · 375 가로 스크롤 0 → 발견 수정 → 전체 게이트 **한 번** `CI=true pnpm test`.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts test/e2e/ledger-save-flow.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 오류가 남은 채 1차가 서버를 불렀거나 비활성이거나 첫 오류(표 밖 칸 → 견적 표 → 매출 표)로 가지 않았거나, 매출 칸만 거부된 저장에서 견적 줄 표 합계 행이 비었거나 `전부 거부 · 다른 칸 오류 1칸`이 아니거나(R2), 거부 뒤 첫 오류 쪽·`오류 N`이 계약과 다르거나, 쪽 왕복 뒤 dirty 표시가 사라졌거나, 매출 표·저장 흐름(저장 중 잠금·상태 바뀜 거부) 케이스가 깨졌다</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>종료 코드가 0이 아니거나 단위·통합·E2E 어느 계층이든 failed 개수가 1 이상이다 — 이 플랜의 붙여넣기·저장 흐름 변경이 기존 표 동작을 깨뜨렸다(프로덕션 빌드 기준)</fails_when>
    <human-check>독립 DOM 감사 보고서(1280·1024·375 세 폭, 항목별 실측값)가 SUMMARY에 붙어 있다 — 실제 엑셀 확인은 04-31</human-check>
  </verify>
  <acceptance_criteria>
    - 옛 클라이언트 오류 게이트 문자열이 앱에 없고(`grep -rn "고쳐야 저장됩니다" app ui` 0건) 1차가 오류 때문에 비활성이 되지 않으며, DR-5 E2E 두 줄(3쪽 오류 · 서버 요청 0 · 포커스 / 표 밖 칸 먼저)이 초록이다
    - 1차의 「남은 오류」 셈과 첫 오류 이동에 매출 표의 고정 오류 칸(04-16 `EntryDraft.cellErrors`)이 든다 — 매출 표가 두 매출 `Table`에 `firstIssueSignal`을 넘긴다(Grep, B3)
    - E2E에 「거부 뒤 첫 오류 쪽 + 다른 쪽 `오류 N`」 · 「쪽 왕복 뒤 dirty 표시 유지」 케이스가 있다
    - E2E R2 줄(매출 칸만 거부 → 견적 줄 표 합계 행 danger `전부 거부 · 다른 칸 오류 1칸` · 포커스는 발행 금액 셀)이 초록이고, `quote-table.tsx`의 합계 행 항목 조립이 견적 줄 칸 0개 거부에서 `otherCellsRejectedText`를 부른다(Grep — UI-SPEC rev 5 후속 결정 R2)
    - `firstIssueSignal`이 `kind: "reason"` 셀을 세지 않는다(코드 리뷰 — 04-30 이유 줄과 고정 오류의 구별)
    - 독립 DOM 감사 보고서가 SUMMARY에 있고 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>오류가 남은 채 저장을 누르면 서버를 부르지 않고 고칠 칸(표 밖 칸 → 견적 표의 쪽 · 셀 → 매출 표)으로 가고, 거부 뒤에는 오류가 있는 쪽을 번호 옆 글자로 알려 주며, 쪽을 오가도 편집 표시가 남고, 세 폭 DOM 실측과 전체 게이트를 통과했다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시스템 클립보드 → 표 | 다른 프로젝트·엑셀에서 온 문자열과 앱 전용 형식을 해석한다 |
| 화면 안 쪽 나눔 → 붙여넣기 대상 | 붙여넣기 채우기가 보기(쪽)에 묶이면 줄이 잘린 채 저장될 수 있는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-97b | Tampering | 쪽 나눔과 붙여넣기 대상 | high | mitigate | 붙여넣기는 표시 순서로 쪽을 넘어 채우고(시작 쪽에 머물며 `{k}쪽까지`), 채운 줄은 화면의 전체 줄 배열에 들어가 저장 때 전부 실린다 — 142줄 표에서 20~64번째 붙여넣기 뒤 저장·재조회 E2E로 단언한다(금지 항목). 서버의 전부 저장/전부 거부(04-04)와 줄 상한(04-26)은 바뀌지 않는다 |
| T-04-172 | Tampering | 앱 전용 클립보드 형식 | low | mitigate | `application/x-plant8-quote-lines+json`은 경고 문구와 「계산 열 칸을 무시할지 오류로 둘지」의 근거로만 쓴다(ENG-D5) — 값·통화·줄 수를 이 형식에서 가져와 저장하지 않는다(값은 `text/plain` 경로와 서버 검증을 그대로 지난다). 형식을 위조해도 계산 열 자리의 값이 저장되는 길은 없다(두 경우 모두 그 칸에 값을 넣지 않는다 — 차이는 오류 표시뿐). 파싱 실패는 앱 형식 없음(`external`)으로 떨어진다 |
| T-04-368 | Tampering | 오류가 남은 채 1차(DR-5) | low | mitigate | 1차가 살아 있어도 고정 오류·해소되지 않은 충돌이 있으면(매출 표 칸 포함) 저장 요청을 보내지 않는다(서버 요청 0 E2E) — 서버는 어차피 전부 거부하지만(04-04) 헛요청·헛 거부 로그를 만들지 않는다. 판정 기준(오류 칸 셈)은 04-28의 셀 상태와 04-16의 매출 칸 상태다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `test/unit/ui/parse-tsv.test.ts` · `test/unit/ui/table-paging.test.ts` · `test/unit/ui/use-clipboard-paste.test.ts` · `test/unit/ui/footer-notice.test.ts` 초록
- 독립 DOM 감사(1280·1024·375) 보고서가 SUMMARY에 있다
- `CI=true pnpm test` 세 계층 통과
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 다른 프로젝트 견적이 오류 칸 없이 들어오고 무시·통화 변화가 합계 행 한 줄 글자로 보이며, 엑셀에서 온 값이 계산 열 자리에 떨어지면 오류 칸이다(ENG-D5)
- 붙여넣기·새 줄·그룹 버튼·저장 거부가 (자)대로 쪽 경계를 넘고, 붙여넣기가 채운 줄이 전부 저장된다
- 오류가 남은 채 1차를 누르면 서버 없이 첫 오류로 가고, 거부 뒤 오류가 있는 쪽이 번호 옆에 보인다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, ui) | `ui/table/footer-notice.ts` — `composeFooterNotice`(danger → warning → muted · 성공 단독) |
| `ui/table` API(추가) | `TableProps.firstIssueSignal?`(DR-5 · 저장 거부) · `TableProps.footerNotices?`·`footerSuccess?`(합계 행 한 줄, DR-16) · `TableProps.revealRowId?` · `TableColumn.pasteRole?` · `onPasteAtCell`이 채운 줄 id를 돌려줌 · 새 줄 고정(04-19 `splitPages` `pinned` 채우기) · 쪽별 오류 수 → `Pagination` `errorCounts` |
| 파서(변경) | `parseTsv` — 끝 줄바꿈 하나 제거(C-05) |
| 붙여넣기(변경) | `applyPaste` — 출처 `source: "app" \| "external"` · 앱 출처일 때만 계산 열 소비·집계(`ignoredComputedCells`), 외부 출처는 04-04 오류 칸(ENG-D5) · 원본 통화(`sourceCurrencies` — 04-24 `quoteLineClipboardMeta` 모양) |
| 동작 변경 | 1차 「일괄 저장」 — 고정 오류·충돌이 남아도 활성, 누르면 서버를 부르지 않고 첫 오류(표 밖 칸 → 견적 표 쪽·셀 → 매출 표 — 04-16 매출 칸 포함)로(DR-5) · 옛 오류 칸 게이트 삭제 · 견적 표 거부 요약은 견적 표에 제 칸이 있을 때만 |
| 화면 문구(신규 사용) | 합계 행 한 줄 `붙여넣기 45줄 · 오른쪽 2칸 버림 · 외화 1줄 원화로 · 계산 열 N칸 무시 · 2쪽까지` · `오류 N`(`N쪽, 오류 M칸`) |
| 테스트 | `test/unit/ui/use-clipboard-paste.test.ts`(신규) · `test/unit/ui/footer-notice.test.ts`(신규) · `test/unit/ui/parse-tsv.test.ts`(확장) · `test/unit/ui/table-paging.test.ts`(새 줄 고정 확장) · `test/e2e/quote-table.spec.ts`(확장 — 엑셀 6열 · 프로젝트 간 왕복 · 쪽을 넘는 붙여넣기 · 새 줄 고정 · B-24 · 합계 행 한 줄 · DR-5 · 오류 쪽) |

## Review Dispositions Ledger

### Round 1~3 — 04-19에서 옮긴 행(06a6f1b · ebcc051 · 98acc74)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| C-03 / 사용자 D15 — 프로젝트 간 복사의 계산 열 오류 · 통화 누락 | DECISION | Plan 04-47, Task 1 ②⑤: `pasteRole: "computed"` 무시·집계 · 앱 형식으로 외화 경고 · 왕복 E2E (옛 04-19 Task 3 ②⑥) |
| C-05 — 엑셀 끝 줄바꿈 유령 줄 | WARNING | Plan 04-47, Task 1 ①: 끝 줄바꿈 하나 제거 · 단위 (상한 경계는 04-31) |
| C-18 — 새 줄 고정 뒤 ↓가 제자리(E2E 몫) | NOTE | Plan 04-47, Task 1 behavior·⑤ — 단위는 04-19 |
| B-24 — 그룹 버튼 새 줄의 쪽 | WARNING | Plan 04-47, Task 1 ④: 04-23의 `addLineToGroup(lineKind)`이 돌려준 새 줄 id로 `revealRowId` → 그룹 끝 쪽 이동 + 포커스 · E2E |
| ENG-D5 (R4) — 엑셀 6열 실행가가 조용히 사라짐 | DECISION | Plan 04-47, truth · `<probe_fallback>` · Task 1 ② `source: "app" \| "external"` · 단위 결정표 · E2E · T-04-172 |
| ENG-D10 — 붙여넣기 새 줄의 id | DECISION | Plan 04-47, truth · Task 1 ③ 04-30 새 줄 경로 · 수용 기준 |
| 엔지 리뷰 A — 숫자 정규화 한 구현 | NOTE | Plan 04-47, objective (f) · Task 1 read_first — 새 정규화 없음 |
| DR-5 — 오류 칸이 다른 쪽에 있으면 1차 비활성 | HIGH | Plan 04-47, truth · Task 2 ② 1차 활성 + 서버 요청 없이 첫 오류로 · `firstIssueSignal` · 옛 게이트 문자열 삭제 · E2E 둘 · T-04-368 |
| DR-16 — 합계 행 오른쪽 안내가 순서·수명 없이 겹침 | MEDIUM | Plan 04-47, truth · Task 1 ② `composeFooterNotice` · `footerNotices` · 저장 시도 때 지움 · 단위 · E2E |

### Round 4 — plan-checker 반복 1(2026-09-23 revision) — 신설

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| W2 — 04-19가 16파일 · 130k | WARNING | Plan 04-47 신설: 옛 04-19 Task 3을 두 태스크(붙여넣기 + 합계 행 한 줄 / DR-5 + 저장 거부 쪽 + 화면 검증)로 옮김 · depends_on 04-19 · 뒤 소비자 04-18·04-31이 이 플랜에 의존(04-19는 이 플랜을 거쳐 도달). 각 태스크의 RED → GREEN 순서는 옛 단계 그대로 |
| B3 — 매출 금액 입력 오류가 셀 좌표 없는 한 문자열 | BLOCKER | 생산은 Plan 04-41(봉투 칸 — 줄 id · `amount`), 화면 대응은 Plan 04-16(매출 칸 → `EntryDraft.cellErrors` · 그 표 합계 행). 이 플랜은 그 칸을 DR-5의 「남은 오류」 셈과 첫 오류 이동(매출 표)에 넣고, 견적 표의 거부 요약을 견적 표에 제 칸이 있을 때만 두게 했다(Task 1 ② · Task 2 ②) |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 실제 엑셀 6열·Windows 클립보드 형식 확인 | 04-31(사람 확인) — 자동 E2E는 주입이라 증명하지 못한다 |

### Round 5 — 체커 revision 1 후속 정리(2026-09-23 · 사용자 후속 결정 R2 · 분할 포인터)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 사용자 결정 R2 — 다른 표 오류로만 전부 거부되면 오류 없는 표도 합계 행 `전부 거부 · 다른 칸 오류 N칸` | DECISION | Supersedes truth·Task 1 ②의 「견적 표 거부 요약은 제 칸이 있을 때만 — 매출 칸만 거부되면 매출 표가 말한다」: 제 칸 0인 거부에서 견적 표 합계 행 danger 항목 = 04-16 `otherCellsRejectedText` · Task 2 behavior R2 E2E(발행 칸만 거부 → 견적 표 글자 · 첫 오류 = 매출 표) · ② (a) · fails_when · 수용 기준. 함수 주인 04-16은 이 플랜의 조상(04-19 → 04-16) |
| 분할 포인터 — 총 매출 예상가 칸의 주인 | NOTE | truth(DR-5) · Task 2 read_first · ② (b): 「04-22의 기간 칸·총 매출 예상가 칸」 → 04-22 기간 칸 · **04-44** 총 매출 예상가 칸(여는 경로 포함). 04-44는 조상(04-19 → 04-16 → 04-15 → 04-14 → 04-13 → 04-12 → 04-44) — `depends_on` 변경 없음 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-47-SUMMARY.md` when done
</output>
