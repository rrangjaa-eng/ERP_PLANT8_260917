---
phase: 04-project-quote-ledger
plan: 48
type: execute
wave: 26
depends_on: ["04-17"]
files_modified:

  - domain/projects/list-view.ts
  - test/unit/domain/project-list-view.test.ts
  - domain/projects/index.ts
  - app/(app)/projects/filter-bar.tsx
  - app/(app)/projects/page.tsx
  - app/(app)/projects/projects-table.tsx
  - app/(app)/projects/projects.module.css
  - app/(app)/projects/loading.tsx
  - test/integration/projects-list.test.ts
  - test/e2e/projects-list.spec.ts

autonomous: true
requirements: [PROJ-01, UX-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "필터 줄에 `기간 [2026-01-01] ~ [2026-12-31]` 날짜 칸 둘이 있고(폭 `10ch` + 좌우 `--cell-pad-x`, 자리표시자 `2026-09-18`) **두 칸 묶음에서 포커스가 두 칸 밖으로 나갈 때**(값이 처음과 달라졌을 때만)와 Enter에서 GET으로 다시 제출된다 — 시작일을 적고 Tab으로 종료일로 옮기는 사이에는 제출·새 문서 로드가 없다. 보기 범위 R = 연도 범위 ∩ 기간(있는 쪽만)이고 행은 프로젝트 기간이 R과 겹치면 보인다(D-89, UI-SPEC 적용 규칙 S1 필터 한 줄 — 「blur」를 묶음 단위로 읽는다, 엔지 리뷰 C §2 P2)"
    - "기간 칸의 형식이 틀리거나 거꾸로면 칸 아래 한 줄 `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` / `기간이 거꾸로입니다 · 앞 날짜를 먼저 적어 주세요`가 보이고 기간 필터는 적용되지 않는다 — 판정은 서버다(UX-04, UI-SPEC Copywriting Error — 목록 기간 필터)"
    - "틀린 URL 파라미터는 오류 화면이 아니라 기본 보기로 떨어진다 — uuid가 아니거나 목록에 없는 `teamId`는 전체 팀, 2000–2100 밖의 연도는 올해, 같은 이름이 여럿이면 첫 값만 쓴다. 틀린 값이 SQL에 닿지 않는다(CEO C-08)"
    - "2000–2100 안이지만 연도 선택지 창(올해 ±) 밖인 연도(`year=2031`)는 선택지에 그 연도가 더해져 **선택된 채** 보인다 — 네이티브 선택 상자가 맞는 선택지가 없어 첫 선택지(`전체 연도`)를 보이고 다음 필터 변경이 조용히 `year=all`로 바뀌는 일이 없다(엔지 리뷰 C §2 P2)"
    - "필터를 바꾸면 1쪽으로 돌아가되 지금 정렬은 유지된다 — 필터 폼이 `sort`·`dir`을 숨은 값으로 싣는다. 「필터 지우기」는 기본 보기(올해·전체 상태·전체 팀)와 다른 값이 하나라도 있을 때만 보이고 누르면 기본 보기다 — 올해 연도 값은 필터로 세지 않는다(UI-SPEC 적용 규칙 S1, CEO C-24)"
    - "빈 목록은 세 갈래다 — 볼 수 있는 프로젝트가 하나도 없으면 `등록된 프로젝트가 없습니다 · 프로젝트 등록`, 기본 보기(올해)가 0건이면 `2026년에 걸친 프로젝트가 없습니다 · 전체 연도 보기`, 사용자 필터가 0건이면 `조건에 맞는 프로젝트가 없습니다 · 필터 지우기`. 셋 다 합계 줄과 페이지 줄을 그리지 않는다. **기간이 선택 연도 밖이라 생기는 0건은 없다** — 연도가 스스로 바뀐다(아래)(UI-SPEC UI Considerations S1 empty, DR-30)"
    - "**연도 자동 전환(DR-30 · P0 — 문구 없음):** 제출된 기간(형식 오류 없음)이 선택 연도와 겹치지 않으면 서버가 연도를 기간의 연도(시작·종료가 같은 해)로, 해를 걸치거나 한쪽이 열려 있으면 `전체 연도`로 바꾼 주소로 보낸다 — 연도 선택 상자·URL `year=`·폰 필터 요약이 그 값이고 빈 결과 상태가 생기지 않는다. 반대 방향: 기간이 있는 채 사용자가 연도를 바꿔 겹치지 않게 되면 필터 줄이 기간 두 칸을 비우고 제출한다(방금 고른 연도를 되돌리지 않는다). 「필터 지우기」는 그대로 기본 보기(UI-SPEC 적용 규칙 S1 「연도 자동 전환(DR-30)」 · UI Considerations S1 「(rev 5) interactive — 연도 자동 전환」)"
    - "폰 필터 요약은 지금 필터 값만 ` · `로 잇는다 — `2026 · 전체 상태 · 전체 팀`(전체 연도면 `전체 연도`, 기간이 있으면 끝에 `2026-09-01 ~ 2026-10-31`) · 라벨 없음 · `--fs-sm --muted`. 길면 ` · ` 값 경계에서만 줄바꿈하고 값 안에서 꺾지 않으며 말줄임하지 않는다 · 요약은 필터 컨트롤의 현재 값과 늘 같다(연도 자동 전환 뒤에도)(DR-26, UI-SPEC UI Considerations S1 「(rev 5) long-text — 폰 필터 요약」 resolved explicit, Copywriting 「표시 — 폰 필터 요약」)"
    - "첫 번째 빈 갈래일 때만 필터 줄의 1차 「프로젝트 등록」을 생략한다(EMPTY가 같은 행동을 준다) — 나머지 두 갈래에서는 1차가 그대로 있다"
    - "로딩 뼈대는 합계 줄 자리에 라벨만(금액 자리 막대 없음) + 머리글 + `--surface` 행 3개이고 반짝임이 없다 — 표 아래 합계 행 뼈대가 없다. 상단 2px 진행 막대는 만들지 않는다(UI-SPEC UI Considerations S1 loading, SYSTEM.md 개정 ③ §7-7, 사용자 D17)"
    - "기간 칸은 같은 해이고 그 해가 보기 연도면 `09-12 ~ 09-16`, 같은 해지만 보기 연도가 아니거나 없으면 `2025-03-01 ~ 03-05`, 해를 걸치면 `2026-11 ~ 2027-02`, 한쪽만 있으면 없는 쪽이 `—`다. `nowrap`이다(D-89, UI-SPEC 적용 규칙 S1 열 — 기간)"
    - "목록 화면(04-17의 합계 줄·페이지 줄 · 이 플랜의 필터 줄·빈 목록·폰 첫 화면·로딩)의 독립 DOM 감사(1280·1024·375)와 전체 게이트 `CI=true pnpm test` 한 번이 이 플랜 끝에서 돈다 — 04-17은 목록 E2E까지만 `CI=true`로 판정했다(엔지 r2 E2-01 분할, CLAUDE.md 화면 검증 순서)"
    - statement: "합계 줄은 폰(375)에서 `라벨 값` 쌍 단위로 줄바꿈하고 쌍 안은 꺾이지 않으며 제외 표시는 아래 줄로 간다 — 가로 스크롤 0. 1280·1024·375 DOM 감사(04-17 산출물 — 감사는 이 플랜 Task 3)"
      verification: backstop
    - statement: "폰 첫 화면(<700, DR-26): 제목 → 검색 칸(전폭) → 한 줄에 2차 「필터」(`aria-expanded`, `aria-controls` = 필터 묶음, 44×44) · 필터 요약 · 오른쪽 1차 「프로젝트 등록」 → (펼치면) 상태·팀·연도·기간 네 칸 라벨-위 → 합계 줄 → 표. 기본은 접힘. PC(≥700)는 필터 한 줄 그대로이고 「필터」·요약이 없다 — 375 DOM 감사: 첫 화면에 검색·요약·1차가 스크롤 없이 보이고 필터 넷은 접혀 있으며 가로 스크롤 0(UI-SPEC UI Considerations S1 「(rev 5) 폰 첫 화면(DR-26)」)"
      verification: backstop
    - statement: "연도 자동 전환 렌더 — 기간 `{올해−1}-01-01 ~ {올해−1}-03-01` + 연도 올해 제출 → 연도 `{올해−1}`, 행 있음 · 기간이 해를 걸치고 선택 연도와 겹치지 않음 → `전체 연도` — E2E 렌더로 확인(UI-SPEC UI Considerations S1 interactive backstop)"
      verification: backstop
    - statement: "페이지 줄이 폰에서 번호 44×44이고 한 줄에 안 들어가면 범위 글자가 위 줄로 올라간다 — 가로 스크롤 0. DOM 감사(04-17 배선 · 04-29 컴포넌트 — 감사는 이 플랜 Task 3)"
      verification: backstop
  prohibitions:
    - statement: "연도와 기간의 조합이 「검색 결과 없음」 빈 상태를 만들어서는 안 된다 — 기간이 선택 연도 밖이면 서버가 연도를 고친 주소로 보내고(문구 없음, P0), 도메인·리포지토리에 빈 교집합 결과 갈래가 없다(DR-30)"
      status: resolved
      verification: test
    - statement: "URL로 들어온 `teamId`·`year`·`page`·기간 값이 정규화·판정을 지나지 않고 SQL에 닿아서는 안 된다 — 틀린 값이 22P02·22008 오류 화면을 만들던 경로(CEO C-08)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/projects/list-view.ts"
      provides: "04-17의 범위·귀속·합계 함수에 더하는 기간 칸 서식 · 기간 필터 판정 · 연도 선택지 · URL 파라미터 정규화 · 연도-기간 겹침과 연도 자동 전환(DR-30) · 폰 필터 요약(DR-26) · 빈 갈래 판정 — 순수 함수, 서버 전용 import 없음(필터 줄 클라이언트가 겹침 함수를 쓴다)"
      exports: ["formatListPeriod", "parseListPeriod", "yearOptions", "normalizeListParams", "periodOverlapsYear", "reconcileListYear", "filterSummary"]
    - path: "app/(app)/projects/filter-bar.tsx"
      provides: "기간 두 칸(묶음 `focusout` 제출) · 서버 오류 한 줄 · 연도 바꿀 때 어긋난 기간 비우기(DR-30) · 정렬 숨은 값 · 폰 「필터」 disclosure + 요약 + `primaryAction` 자리(DR-26)"
      contains: "focusout"
    - path: "app/(app)/projects/loading.tsx"
      provides: "합계 줄 자리 라벨만 + 머리글 + `--surface` 행 3개(표 아래 합계 행 뼈대 없음 · 진행 막대 없음)"
    - path: "test/e2e/projects-list.spec.ts"
      provides: "기간 필터(묶음 제출 · 형식·거꾸로 오류) · 창 밖 연도 · 정렬 유지 · 틀린 파라미터 · 필터 지우기 · 빈 목록 · 연도 자동 전환 셋 · 폰 첫 화면"
  key_links:
    - from: "app/(app)/projects/filter-bar.tsx"
      to: "domain/projects/list-view.ts"
      via: "연도 선택 상자 `onChange`가 같은 순수 함수 `periodOverlapsYear`로 어긋난 기간을 판정해 두 칸을 비운다(DR-30 반대 방향)"
      pattern: "periodOverlapsYear"
    - from: "app/(app)/projects/page.tsx"
      to: "domain/projects/list-view.ts"
      via: "`loadProjectList` 전에 `reconcileListYear`로 연도를 고친 같은 경로 상대 주소로 `redirect`한다 — 조회·자동 정산 판정이 돌기 전"
      pattern: "reconcileListYear"
    - from: "domain/projects/index.ts"
      to: "domain/projects/list-view.ts"
      via: "`loadProjectList`가 맨 앞에서 `normalizeListParams`로 URL 값을 정규화하고 `parseListPeriod`로 기간을 판정해 `periodErrors`를 싣는다"
      pattern: "normalizeListParams"
---

<objective>
**As a** 경영관리와 기획 PM, **I want to** 프로젝트 목록을 기간으로 좁히고, 기간과 연도가 어긋나면 연도가 스스로 맞춰지며, 틀린 주소나 빈 결과에서도 다음 한 수가 보이고, 폰에서는 검색·요약·등록이 첫 화면에 보이기를, **so that** 원하는 기간의 합계를 헤매지 않고 읽고, 오류 화면이나 「결과 없음」 막다른 길 없이 목록을 다룬다.

머지 묶음: ④/4 목록·리저브 — 웨이브 25~30(04-17 → 04-31)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**2026-09-24 엔지니어링 리뷰 r2 신설 플랜(`docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 1 · E2-01 — 04-17 분할).** 04-17(95KB · 3태스크)이 체커 상한을 넘어 옛 **Task 3**(기간 필터 · 연도 자동 전환 · 서버 판정 오류 · 틀린 파라미터 정규화 · 정렬 유지 · 필터 지우기 · 빈 목록 세 갈래 · 기간 칸 서식 · 폰 첫 화면 · 로딩 뼈대 + 화면 검증)을 **내용 그대로** 이 플랜으로 옮겼다. 트레이서 우선 규칙에 맞춰 옛 한 태스크를 세 태스크로 나눴다 — Task 1 트레이서(기간 필터 한 경로 · 서버 판정) · Task 2(연도 자동 전환 · 정규화 · 연도 선택지 · 정렬 유지 · 필터 지우기 · 빈 목록 · 기간 칸 서식) · Task 3(폰 첫 화면 · 로딩 뼈대 + 목록 화면 전체 DOM 감사 · 전체 게이트 한 번). 옛 04-17의 truths·backstop·T-04-90·T-04-367도 함께 옮겼다. 04-17의 합계 줄·페이지 줄 폰 backstop 감사도 여기서 한다(04-17은 목록 E2E를 `CI=true`로 판정하고 끝난다). 04-18은 이제 이 플랜 뒤다(`projects-table.tsx`·`list-view.ts`가 겹친다). 04-47과 같은 웨이브 26이지만 파일이 겹치지 않는다(04-47은 `ui/table/*`·`quote-table`).

Purpose: 목록의 **조회 조건**(기간 · 연도 · 파라미터)과 빈·로딩·폰 상태를 끝낸다. 04-17이 정한 「어느 범위의 무엇」 위에서 사람이 범위를 바꾸는 길이 여기다.

Output: `list-view.ts`에 순수 함수 일곱(+ 빈 갈래 판정) · `loadProjectList`의 정규화·기간 판정·빈 갈래 · 필터 줄 기간 두 칸과 폰 disclosure · 빈 목록 세 갈래 · 로딩 뼈대 · 단위(확장) · 통합(확장) · E2E(확장) · 목록 화면 독립 DOM 감사 · 전체 게이트 한 번.

**이어받은 결정(04-17 원문 그대로):** CEO C-08(틀린 URL 파라미터) · C-24(필터 바꿔도 정렬 유지) · 사용자 D17(진행 막대 만들지 않음) · 엔지 리뷰 C §2 P2(기간 묶음 제출 · 창 밖 연도) · 공백 8·10 · 디자인 리뷰 DR-26(폰 첫 화면) · DR-30(연도 자동 전환) · 교차 그룹 계약 6(DOM 감사 폭 1024).

**실행 규율(CLAUDE.md · hooks):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`를 호출한다(RED → GREEN → 리팩터). 테스트·빌드·린트 실패를 쫓기 전에 Skill `systematic-debugging`을 호출하고 코드를 만지기 전에 원인을 찾는다. 커밋 전과 「완료」를 말하기 전에 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시하고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(이 플랜 하나). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 화면 검증 순서는 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번이고, 완료 판정은 로컬 dev가 아니라 `CI=true`다. 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**(04-17의 합계 줄·페이지 줄 포함)의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-17-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-29-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다. 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md rev 5 — 「## 적용 규칙」 S1(1008–1096행 — 필터 한 줄 1012–1030 · 보기 범위 R · 연도 자동 전환 1022–1024 · 필터 지우기 · 폰 첫 화면 1027–1030 · 기간 칸 서식 1067–1069) · S12(1415–1431행) · `## UI Considerations` S1(467–481행 — 「(rev 5) interactive — 연도 자동 전환」·「(rev 5) 폰 첫 화면」·「(rev 5) long-text — 폰 필터 요약」 476–478) · Copywriting 344행(2차 버튼 — 목록(폰)) · 346–348행(Empty — 목록 셋) · 359행(Error — 목록) · 378행(Error — 목록 기간 필터) · 402행(표시 — 폰 필터 요약)

# docs/designs/plant8-erp-phase4-design-review-260923.md 62행(DR-26) · 66행(DR-30)

# docs/designs/plant8-erp-phase4-eng-review-260923.md 부록 C §2(기간 제출 · 연도 선택지 · 낡은 주석) · §3 GAP 8·10

# docs/designs/plant8-erp-phase4-eng-review-260923-r2.md 「분할안」 04-17 → 04-17 + 04-48

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-01(목록 조회 조건 부분)** 과 **UX-04(목록 입력 판정 부분)** 다(스펙 없는 프로브 보고의 PROJ-01·UX-04 행은 `unresolved` — 아래 가정으로 표면화하고 조용히 버리지 않는다). 수용 기준은 04-17 옛 Task 3과 D-89 · UI-SPEC rev 5 S1·S12에서 끌어왔고, S1 `## UI Considerations`의 empty·loading·error·interactive·long-text 중 이 플랜 범위는 truths로, 폰 첫 화면·연도 자동 전환 렌더·폰 합계 줄·폰 페이지 줄은 backstop 마커로 옮겼다.

**계획 단계 가정(표면화 — 04-17 원문 그대로 이어받음):**

- **연도와 기간이 겹치지 않는 조합(DR-30 — 옛 「R이 비면 사용자 필터 0건」을 대체):** 서버 페이지가 조회 전에 `reconcileListYear`로 연도를 고쳐 같은 주소(`year`만 바꾸고 `page`는 뺀다)로 `redirect`한다 — 고친 뒤에는 연도와 기간이 늘 겹치므로 04-17의 `resolveListRange`가 빈 구간을 받지 않는다. 규칙: 기간이 형식 오류면 바꾸지 않는다 · `전체 연도`면 바꾸지 않는다 · 시작·종료가 같은 해면 그 해 · 해를 걸치면 `전체 연도` · **한쪽만 적힌 기간이 연도와 겹치지 않으면 `전체 연도`**(계획 기본값 — reversible, 조건 한 줄). `reconcileListYear`는 멱등이다(고친 주소를 다시 넣으면 그대로 — 되돌이 이동 없음).
- **반대 방향(DR-30 · rev 5 계획 기본값):** 서버는 「사용자가 방금 연도를 바꿨는지」를 주소만으로 알 수 없다 — 그래서 필터 줄(클라이언트)이 연도 선택 상자를 바꿀 때 기간 두 칸에 값이 있고 새 연도와 겹치지 않으면(`periodOverlapsYear` — 같은 순수 함수를 클라이언트가 import) 기간 두 칸을 비운 뒤 제출한다.
- **한쪽이 열린 기간 보기의 괄호 라벨**은 열린 쪽을 `—`로 쓴다(`2026-09-01 ~ — 귀속` — 04-17의 `totalsTitle`이 받는 기간 값).
- **틀린 파라미터(C-08):** 도메인 입구가 `normalizeListParams`로 정규화한 뒤에만 쓴다 — `teamId`는 uuid 모양이고 뷰어가 고를 수 있는 팀 목록에 있어야, 연도는 `all` 또는 2000–2100 정수, 배열이면 첫 값. 기간 칸은 오류를 칸 아래에 보이는 별도 규칙(`parseListPeriod`)이 그대로다. 04-17 끝 ~ 이 플랜 끝 사이에는 정규화가 없지만 같은 머지 묶음 ④ 안이라 배포되지 않는다.
- **상단 2px 진행 막대(사용자 D17):** 만들지 않는다 — 목록의 필터·정렬·쪽 이동은 전체 문서 GET이라 `loading.tsx`가 스트리밍된다.
- **PR #38 알려진 문제 「합계 줄」(QA 16건 중 이름만 남은 항목):** 원 보고서가 유실돼 증상을 모른다. 목록 합계 줄은 04-17이 표 아래 합계 행을 없애고 표 위 한 줄로 다시 만들었고(D-88), 이 플랜의 Task 3 DOM 감사가 그 합계 줄을 1280·1024·375에서 실측한다 — 감사에서 드러난 결함은 ENG-D11로 고친다. 원 증상이 이것과 다르면 사용자 재진술이 필요하다(계획 인계 질문).

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 기간 두 칸이 묶음 단위로 제출되고 서버가 형식·거꾸로를 판정해 겹침 보기와 기간 귀속 합계를 돌려준다(필터 줄 → 도메인 → 04-17 리포지토리 → 합계 줄 → E2E)</name>
  <files>domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, domain/projects/index.ts, app/(app)/projects/filter-bar.tsx, app/(app)/projects/page.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `domain/projects/list-view.ts`(04-17 산출물 전문 — `resolveListRange`의 기간 입력 모양 · `totalsTitle`의 기간 보기 괄호 · 서버 전용 import가 없다는 제약)
    - `domain/projects/index.ts`의 `loadProjectList`(04-17 — 판정 한 번 → 집계 → 보정 → 목록 순서, `deps.repo` 스텁 자리)
    - `app/(app)/projects/filter-bar.tsx`(전문 112행 — 라벨-위 배치 · `requestSubmit` · 「필터 지우기」 조건 · 폼이 싣는 값)
    - `app/(app)/projects/page.tsx`(04-17 이후 전문 — `searchParams` 처리와 연도 선택지 자리)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 「## 적용 규칙」 S1 필터 한 줄(1012–1021행) · Copywriting 378행(Error — 목록 기간 필터)
    - `docs/design/SYSTEM.md` §7-2 오류 한 줄 규칙(Grep `7-2`로 위치를 찾아 그 절만)
    - `test/integration/projects-list.test.ts`·`test/e2e/projects-list.spec.ts`(04-17 산출물 — 격리 검색어 `q=`·`kstYear` 준비 형태)
  </read_first>
  <behavior>
    - `parseListPeriod("2026-09-01", "2026-10-31")` → 범위 둘, 오류 없음 · `("2026-9-1", "")` → 오류 `from: 날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요`, 범위 없음 · `("2026-02-30", "")` → 같은 형식 오류(달력에 없는 날) · `("0000-01-01", "")` → 같은 형식 오류(2000–2100 밖, C-08) · `("2026-10-31", "2026-09-01")` → 오류 `to: 기간이 거꾸로입니다 · 앞 날짜를 먼저 적어 주세요`, 범위 없음 · 둘 다 빈 값 → 범위·오류 없음
    - 통합: 연도 2026 + 기간 2026-09-01~2026-10-31이면 R = 그 기간이고, 2026-10-20~2026-11-15 행은 보이되 `2026-11 귀속`으로 합계에서 빠져 `기간 밖 귀속 1건 제외`
    - 통합: 기간 형식 오류가 있는 요청의 `loadProjectList` 결과 행·합계가 기간 필터 없는 같은 요청의 결과와 같고, `periodErrors`에 그 칸의 오류가 있다
  </behavior>
  <action>
① **순수 함수(`list-view.ts` — TDD, 단위 먼저 RED).** `parseListPeriod(from, to)`를 더한다 — 형식 `YYYY-MM-DD` + 달력에 있는 날짜 + 2000–2100, 거꾸로 판정. 오류가 하나라도 있으면 기간 범위 전체를 적용하지 않는다. `behavior` 첫 줄을 `test/unit/domain/project-list-view.test.ts`에 먼저 쓴다.

② **도메인.** `loadProjectList`가 `from`·`to`를 `parseListPeriod`로 판정해 `periodErrors`를 결과에 싣고(서버가 판정 — UX-04), 오류가 없을 때만 04-17의 `resolveListRange`에 기간으로 넘긴다. `page.tsx`는 `searchParams`의 `from`·`to`를 그대로 도메인에 넘긴다.

③ **필터 줄(`filter-bar.tsx`).** 연도 뒤에 `기간` 라벨 하나 아래 날짜 텍스트 입력 둘(`name` `from`·`to`, 자리표시자 `2026-09-18`, 사이 `~`, 폭 `10ch` + 좌우 `--cell-pad-x` — 새 폭 값 없음, SYSTEM.md 개정 ③)을 한 묶음 요소로 감싼다. **묶음의 `focusout`에서 `relatedTarget`이 두 칸 밖일 때만**, 그리고 두 칸 값 중 하나라도 처음과 달라졌을 때만 `requestSubmit` — 시작일에서 Tab으로 종료일로 옮기는 사이에는 제출하지 않는다(엔지 리뷰 C §2 P2). Enter는 기본 GET 제출. 서버 오류가 있으면 해당 칸에 `aria-invalid`와 `aria-describedby`, 칸 아래 한 줄 `--fs-sm --danger`(거꾸로 오류는 `to` 칸 아래). 입력 값은 오류여도 그대로 남긴다.

④ **테스트.** 통합(`behavior` 둘째·셋째 줄) · E2E(격리 검색어, 연도는 `kstYear(new Date())`): 시작일을 적고 `Tab`으로 종료일 칸에 가면 **문서가 다시 로드되지 않고**(URL·포커스 그대로) 종료일까지 적은 뒤 묶음 밖으로 `Tab` 하면 한 번 제출된다 · 기간 `2026-9-1` 입력 후 묶음 밖으로 → 칸 아래 형식 오류 한 줄, 목록은 기간 필터 없는 결과 그대로 · 거꾸로 입력 → 거꾸로 오류 · 올바른 기간 → 합계 괄호가 `{from} ~ {to} 귀속`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 형식·달력·범위·거꾸로 판정이 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 기간 보기의 겹침·귀속 제외가 틀렸거나, 기간 오류가 있는데 기간 필터가 적용됐다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 기간 두 칸 사이에서 문서가 다시 로드됐거나, 서버 오류 한 줄이 칸 아래에 없거나, 기간 보기 괄호가 다르다</fails_when>
  </verify>
  <acceptance_criteria>
    - `parseListPeriod`가 달력에 없는 날(`2026-02-30`)과 범위 밖 연도(`0000-01-01`)를 형식 오류로 잡는 단위 케이스가 있다
    - `filter-bar.tsx`의 기간 제출이 묶음 `focusout`(+ `relatedTarget` 판정)과 Enter뿐이고 기간 두 칸에 칸별 `onBlur` 제출 처리기가 없다(코드 리뷰) · 시작일 → Tab → 종료일 사이 새 로드 없음 E2E가 초록이다
    - 기간 오류가 있으면 목록 결과가 기간 필터 없는 결과와 같다는 통합 단언이 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>기간 두 칸이 묶음 단위로 한 번 제출되고, 서버가 형식·거꾸로를 판정해 칸 아래에 알리며, 올바른 기간은 겹침 보기와 기간 귀속 합계로 끝까지 이어진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 연도 자동 전환(DR-30) · 틀린 파라미터 정규화(C-08) · 창 밖 연도 선택지 · 정렬 유지(C-24) · 필터 지우기 · 빈 목록 세 갈래 · 기간 칸 서식</name>
  <files>domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, domain/projects/index.ts, app/(app)/projects/filter-bar.tsx, app/(app)/projects/page.tsx, app/(app)/projects/projects-table.tsx, test/integration/projects-list.test.ts, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `app/(app)/projects/page.tsx` 60–75행(`teamId`가 그대로 uuid 비교로 가는 자리 — C-08) · 38–41행(고정 5년 연도 선택지)
    - `app/(app)/projects/error.tsx`(전문 — 오류 문구가 Copywriting과 같은지만 확인, 고칠 것이 없으면 건드리지 않는다)
    - `app/(app)/projects/projects-table.tsx`(04-17 이후 — 기간 셀 · `ListEmpty` 자리)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 「## 적용 규칙」 S1 연도 자동 전환(1022–1024행) · 필터 지우기 · 기간 칸 서식(1067–1069행) · `## UI Considerations` S1 「(rev 5) interactive — 연도 자동 전환」(476행) · Copywriting 346–348행(Empty — 목록 셋)
    - Task 1 산출물(`parseListPeriod` · `periodErrors`)
  </read_first>
  <behavior>
    - `normalizeListParams({ teamId: "abc", year: "0000", status: ["in_progress", "settling"], page: "2" }, { teamIds })` → `{ teamId: undefined, year: 올해, status: "in_progress", page: "2" }` · 목록에 없는 uuid `teamId` → undefined · `year: "all"` → `"all"` · `year: "2031"` → 2031(C-08)
    - (엔지 리뷰 C 공백 10) `yearOptions(2026, 2031)` → 기본 창(2027 · 2026 · 2025 · 2024 · 2023)에 2031이 더해진 목록(내림차순) · `yearOptions(2026, 2025)` → 기본 창 그대로 · `yearOptions(2026, "all")` → 기본 창 그대로
    - `formatListPeriod("2026-09-12", "2026-09-16", 2026)` → `09-12 ~ 09-16` · `("2025-03-01", "2025-03-05", 2026)` → `2025-03-01 ~ 03-05` · `("2025-03-01", "2025-03-05", null)` → `2025-03-01 ~ 03-05` · `("2026-11-10", "2027-02-05", 2026)` → `2026-11 ~ 2027-02` · `("2026-09-12", null, 2026)` → `09-12 ~ —` · `(null, null, 2026)` → `—`
    - 빈 갈래 판정: 범위 안 0건 + 사용자 필터 없음 + 볼 수 있는 프로젝트 0 → `none` · 사용자 필터 없음 + 다른 해에 프로젝트 있음 → `default-view`(문구의 연도 = 보기 연도) · 사용자 필터 있음 → `filtered`
    - 통합(엔지 리뷰 C 공백 8): 볼 수 있는 프로젝트가 하나도 없는 viewer(다른 팀 범위 · 빈 DB)의 `loadProjectList` → `emptyKind: "none"`
    - (DR-30) `periodOverlapsYear({ from: "2025-01-01", to: "2025-03-01" }, 2026)` → false · `(…, 2025)` → true · `({ from: "2025-11-01", to: "2026-02-01" }, 2026)` → true · `({ from: "2027-03-01" }, 2026)` → false · `(기간 없음, 2026)` → true · `(…, "all")` → true
    - (DR-30) `reconcileListYear({ year: "2026", from: "2025-01-01", to: "2025-03-01" })` → `"2025"` · `({ year: "2024", from: "2025-11-01", to: "2026-02-01" })` → `"all"` · `({ year: "2026", from: "2027-03-01", to: "" })` → `"all"`(열린 기간 — 계획 기본값) · `({ year: "2026", from: "2026-09-01", to: "2026-10-31" })` → `null`(바꿀 것 없음) · `({ year: "all", … })` → `null` · 형식 오류 기간 → `null` · 멱등: 돌려준 연도를 다시 넣으면 `null`
    - 통합(C-08): 틀린 파라미터 표(`teamId=abc` · `teamId=`다른 뷰어 팀 밖 uuid · `year=0000` · `year=99999` · `page=-1` · `status` 두 개)마다 `loadProjectList`가 던지지 않고 기본 보기와 같은 결과다
  </behavior>
  <action>
① **순수 함수(`list-view.ts` — 단위 먼저 RED).** `periodOverlapsYear(period, year)` · `reconcileListYear({ year, from, to })`(`<probe_fallback>` 규칙: 기간 형식 오류·`all`·겹침이면 `null`, 같은 해면 그 해, 해를 걸치거나 한쪽이 열려 있으면 `all`, 멱등) · `yearOptions(thisYear, requested)`(기본 창 + 창 밖이지만 정규화를 통과한 요청 연도, 엔지 리뷰 C §2 P2) · `normalizeListParams(raw, { teamIds, thisYear })`(C-08 — 배열이면 첫 값, `teamId`는 uuid 모양 + `teamIds`에 있을 때만, 연도는 `all` 또는 2000–2100 정수 아니면 올해) · `formatListPeriod(start, end, viewYear | null)`(식별자형 날짜라 쉼표 없음) · 빈 갈래 판정(결과 필드 `emptyKind`).

② **도메인.** `loadProjectList`가 맨 앞에서 `normalizeListParams`로 URL 값을 정규화한다(뷰어가 고를 수 있는 팀 목록은 이미 필터 줄이 쓰는 조회를 재사용) — 정규화 밖 값은 SQL에 닿지 않는다. 결과가 0건이면 `emptyKind`를 싣는다 — `none` 판정에만 쓰는 필터 없는 건수 조회 한 번은 0건일 때만 한다. 사용자 필터 판정: 상태·팀·검색어·기간 값(형식 오류여도 값이 있으면 필터) · `year=all` · 올해가 아닌 연도. 올해 연도 값은 필터로 세지 않는다. `page.tsx`는 `searchParams`를 `string | string[]`로 받아 그대로 도메인에 넘긴다(C-08 — 페이지가 `string`으로 단정하지 않는다). **연도 자동 전환(DR-30):** `page.tsx`는 `loadProjectList`를 부르기 **전에** `reconcileListYear`(배열이면 첫 값)를 부르고 결과가 있으면 지금 주소에서 `year`만 그 값으로 바꾸고 `page`를 뺀 **같은 경로의 상대 주소**로 `redirect`(next/navigation)한다 — 조회·자동 정산 판정이 한 번도 돌지 않은 채 옮겨 가므로 판정이 두 번 돌지 않는다(문구 없음, P0). 연도 선택지는 `yearOptions(kstYear, 정규화된 요청 연도)`로 만든다 — 지금의 고정 5년(`page.tsx:38-41`)을 대신한다.

③ **필터 줄(`filter-bar.tsx`).** **연도를 바꿀 때(DR-30 반대 방향):** 연도 선택 상자 `onChange`에서 기간 두 칸에 값이 있고 `periodOverlapsYear(기간, 새 연도)`가 거짓이면 두 칸을 비운 뒤 `requestSubmit` — 방금 고른 연도를 되돌리지 않는다(`list-view.ts`의 순수 함수를 import — 서버 전용 모듈이 아니다). 11행의 낡은 머리 주석(「필터를 바꾸면 정렬·더 보기 누적도」)을 지금 규칙(필터를 바꾸면 1쪽, 정렬은 유지)으로 고친다. **정렬 유지(C-24):** 지금 `sort`·`dir`가 있으면 폼에 `type="hidden"` 입력 둘로 싣는다(`page`는 싣지 않는다 — 1쪽). 「필터 지우기」(`/projects`)는 도메인의 사용자 필터 판정이 참일 때만.

④ **화면.** `page.tsx`: 빈 갈래 셋 — `none`은 `ListEmpty` `등록된 프로젝트가 없습니다` + `프로젝트 등록`(`?new=1#project-form`), `default-view`는 `{연도}년에 걸친 프로젝트가 없습니다` + `전체 연도 보기`(`/projects?year=all`), `filtered`는 `조건에 맞는 프로젝트가 없습니다` + `필터 지우기`(`/projects`). 셋 다 합계 줄·페이지 줄 없음. 필터 줄의 1차 「프로젝트 등록」 생략 조건을 `none` 갈래로 바꾼다(`?new=1`일 때 생략하는 기존 규칙은 그대로). `projects-table.tsx`의 기간 셀을 `formatListPeriod`(보기 연도 전달)로 바꾸고 `nowrap`.

⑤ **테스트.** 통합(`behavior`의 공백 8 · C-08 표) · E2E(격리 검색어, 연도는 `kstYear(new Date())`): `/projects?year={올해 + 5}`를 직접 열면 연도 선택 상자가 그 연도를 선택한 채이고 상태 필터를 바꿔도 URL의 `year`가 그대로다(공백 10) · 머리글 정렬을 바꾼 뒤 상태 필터를 바꾸면 URL에 `sort`가 남고 `page`가 없다(C-24) · `/projects?teamId=abc&year=0000`을 직접 열면 오류 화면이 아니라 올해 보기다(C-08) · 「필터 지우기」 → `/projects`이고 연도 값이 올해 · 기본 보기 0건 문구와 「전체 연도 보기」 이동(올해에 걸친 행이 없는 격리 검색어로). **연도 자동 전환(DR-30):** 격리 검색어로 작년 1~3월 프로젝트를 준비 → 기간 `{작년}-01-01 ~ {작년}-03-01`을 적고 묶음 밖으로 → URL `year={작년}` · 연도 선택 상자 값 `{작년}` · 행이 보이고 빈 상태 문구가 없다 · 기간 `{재작년}-11-01 ~ {작년}-02-01`(연도 올해) → URL `year=all` · 연도 `전체 연도` · 올해 기간이 있는 채 연도를 작년으로 바꾸면 URL에 `from`·`to`가 없고 기간 두 칸이 빈다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 파라미터 정규화·연도 선택지·기간 칸 서식·빈 갈래·겹침·연도 자동 전환(멱등 포함) 표가 계약과 다르다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 틀린 파라미터가 던졌거나 기본 보기와 다르거나, `none` 갈래가 아니다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/projects-list.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 연도 자동 전환·반대 방향·창 밖 연도·정렬 유지·틀린 파라미터·빈 목록 중 하나가 계약과 다르다</fails_when>
  </verify>
  <acceptance_criteria>
    - `yearOptions` 단위 표와 창 밖 연도 E2E가 초록이다(공백 10) · `emptyKind: "none"` 통합 케이스가 초록이다(공백 8)
    - 틀린 파라미터 표 통합 케이스가 전부 기본 보기 결과와 같고 던지지 않는다(C-08)
    - 필터 폼이 `sort`·`dir` 숨은 값을 싣고 `page`를 싣지 않는다(C-24 E2E)
    - 빈 갈래 셋이 각자의 문구·링크로 렌더되고 셋 다 합계 줄·페이지 줄이 없다 · 1차 「프로젝트 등록」이 `default-view`·`filtered` 갈래에서 렌더된다
    - `periodOverlapsYear`·`reconcileListYear`(멱등 포함) 단위 표가 초록이고, `page.tsx`가 `loadProjectList` 전에 `reconcileListYear`로 `redirect`하며, 도메인·리포지토리에 빈 교집합 결과 갈래가 없다(`grep -rn "empty: true" domain/projects repositories/projects.ts` 0건 — DR-30)
    - 필터 줄 연도 `onChange`가 겹치지 않는 기간을 비우고 제출한다 · 연도 자동 전환 E2E 셋(작년 · 걸친 기간 → 전체 연도 · 반대 방향)이 초록이다(DR-30)
    - `filter-bar.tsx` 머리 주석에 「더 보기」 낱말이 없다(`grep -c "더 보기" 'app/(app)/projects/filter-bar.tsx'` 0)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>기간과 연도가 어긋나면 연도가 스스로 맞춰지고, 틀린 파라미터는 서버가 판정해 오류 화면 없이 기본 보기로 떨어지며, 필터를 바꿔도 정렬이 남고, 빈 목록이 세 갈래로 다음 한 수를 준다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 폰 첫 화면(DR-26) · 로딩 뼈대 + 목록 화면 전체 검증(독립 DOM 감사 1280·1024·375 — 04-17의 합계 줄·페이지 줄 포함 → 수정 → 전체 게이트 한 번)</name>
  <files>domain/projects/list-view.ts, test/unit/domain/project-list-view.test.ts, app/(app)/projects/filter-bar.tsx, app/(app)/projects/page.tsx, app/(app)/projects/projects.module.css, app/(app)/projects/loading.tsx, test/e2e/projects-list.spec.ts</files>
  <read_first>
    - `app/(app)/projects/loading.tsx`(전문 49행 — 지금의 표 아래 합계 행 뼈대)
    - `app/(app)/projects/filter-bar.tsx`(Task 1·2 이후 전문)
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 「## 적용 규칙」 S1 폰 첫 화면(1027–1030행) · S12(1415–1431행 — 페이지 줄 폰) · `## UI Considerations` S1 「(rev 5) 폰 첫 화면」·「(rev 5) long-text — 폰 필터 요약」(477–478행) · Copywriting 「2차 버튼 — 목록(폰)」(344행)·「표시 — 폰 필터 요약」(402행) · Design System 「「필터」는 아이콘 없이 글자」
    - `docs/design/SYSTEM.md` §6-0 중단점(폰 <700) · §10 disclosure(`aria-expanded`) — Grep으로 그 줄만
    - `app/(app)/projects/list-totals.tsx`(04-17 — 합계 줄 폰 줄바꿈 규칙) · `ui/pagination/Pagination.tsx`(04-29 — 폰 번호 44×44)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <behavior>
    - (DR-26) `filterSummary({ year: 2026, statusLabel: "전체 상태", teamLabel: "전체 팀" })` → 값 배열 `["2026", "전체 상태", "전체 팀"]`(화면이 ` · `로 잇는다) · 전체 연도 → 첫 값 `전체 연도` · 기간이 있으면 끝에 `2026-09-01 ~ 2026-10-31`(한쪽이 열려 있으면 `2026-09-01 ~ —`) · 상태·팀 필터가 있으면 그 라벨
  </behavior>
  <action>
① **순수 함수(단위 먼저 RED).** `filterSummary(...)`(DR-26 — 값 배열)를 `list-view.ts`에 더한다.

② **폰 첫 화면(DR-26 — `filter-bar.tsx`).** 필터 줄이 1차 「프로젝트 등록」 자리를 prop(`primaryAction` — 없으면 비움)으로 받아 한 컨테이너 안에서 700 중단점 CSS만으로 두 배치를 만든다 — PC(≥700): 지금 그대로 상태·팀·연도·기간·검색·(필터 지우기)·오른쪽 1차 한 줄이고 「필터」·요약은 그리지 않는다(`display: none` — 접근성 트리에서도 빠진다). 폰(<700): 검색 칸 전폭 → 한 줄에 2차 `Button` 「필터」(`aria-expanded`, `aria-controls` = 네 칸 묶음 `id`, 높이·폭 `--touch-min`) · 요약(`filterSummary` 값마다 `white-space: nowrap` 조각, 사이 ` · ` — 값 경계에서만 줄바꿈 · 말줄임 없음 · `--fs-sm --muted`) · 오른쪽 1차 → 「필터」를 누르면 바로 아래에 상태·팀·연도·기간 네 칸이 라벨-위로 펼쳐진다(시트 아님, 기본 접힘 — 접힘은 폰 미디어 쿼리 안에서만 숨긴다) → 「필터 지우기」. 아이콘 없이 글자. 새 토큰·중단점 없음. `page.tsx`는 1차를 필터 줄의 `primaryAction` 자리로 넘긴다(Task 2의 `none` 갈래 생략 규칙 그대로).

③ **로딩(`loading.tsx`).** 표 아래 합계 행 뼈대를 지우고 필터 줄 아래 합계 줄 자리에 라벨 `합계`만(금액 막대 없음), 머리글 + `--surface` 행 3개, 반짝임 없음, 기존 지연 표시 규칙 유지. 상단 진행 막대를 만들지 않는다(사용자 D17).

④ **E2E(폰 첫 화면, 375 폭).** 「필터」의 `aria-expanded`가 `false`이고 네 칸이 보이지 않는다 → 누르면 `true`이고 네 칸이 보인다 · 요약 글자가 `{올해} · 전체 상태 · 전체 팀` · 연도 자동 전환 뒤 요약 첫 값이 새 연도다 · PC(1280)에는 「필터」 버튼이 보이지 않는다. 연도는 `kstYear(new Date())`.

⑤ **화면 검증(CLAUDE.md 순서 — 목록 화면 전체).** 싼 게이트(lint·typecheck·build) → **독립 DOM 감사**(실행자가 아닌 별도 서브에이전트, `model` 명시, 프롬프트에 `verification-before-completion` 명시, `CI=true` 프로덕션 빌드에서 DOM 실측, 스크린샷 육안 판정 금지): **1280·1024·375 세 폭**(교차 그룹 계약 6)에서 (a) 합계 줄이 표 위이고 아래 선이 `--line-strong` 2px (b) 375에서 합계 줄 쌍이 쌍 단위로만 줄바꿈되고 문서 가로 스크롤 0 (c) 페이지 줄 번호의 폰 크기 ≥ 44×44, 현재 번호에 `aria-current`, 1쪽에 「이전」 없음, 12쪽 보기의 375에서 번호 줄(이전·번호·다음)이 한 줄(04-29의 폰 6쪽 창) (d) 기간 칸·`2027 귀속`이 한 줄(nowrap) (e) 합계 금액에 쉼표, 연도·날짜에 쉼표 없음 (f) 125건 보기 첫 응답 시간(서버 응답 헤더 기준, 참고값 — C-23)과 집계·목록 두 문장의 `EXPLAIN` 기준(04-17 SUMMARY 값과 비교, 참고값) (g) **폰 첫 화면(DR-26):** 375×812 뷰포트에서 검색 칸·요약·1차가 첫 화면 안(`getBoundingClientRect().bottom` ≤ 뷰포트 높이)이고 네 칸이 접혀 있으며 「필터」가 44×44 이상, 긴 팀 이름 + 기간 필터 요약이 ` · ` 경계에서만 줄바꿈하고 말줄임이 없다(`text-overflow`·잘림 0) (h) 1024에서 필터 줄이 한 줄 또는 `flex-wrap` 줄바꿈이고 가로 스크롤 0 (i) 페이지 줄 링크가 클라이언트 이동이다(2쪽 링크 클릭 뒤 문서 다시 받기 없음 — `next/link`, 04-29) → 발견 수정(04-17 파일이면 ENG-D11 — 회귀 테스트 + SUMMARY 편차) → 전체 게이트 **한 번** `CI=true pnpm test`(통합·E2E는 다른 에이전트와 동시에 돌리지 않는다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/project-list-view.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 폰 필터 요약 값 배열이 필터 값과 다르다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; bash scripts/reset-test-db.sh &amp;&amp; CI=true pnpm test</automated>
    <fails_when>종료 코드가 0이 아니거나 단위·통합·E2E 어느 계층이든 failed 개수가 1 이상이다(프로덕션 빌드 기준) — 폰 첫 화면·로딩·DOM 감사에서 고친 자리가 다른 화면을 깨뜨렸다</fails_when>
    <human-check>독립 DOM 감사 보고서(1280·1024·375 세 폭, (a)~(i) 항목별 실측값 — 04-17의 합계 줄·페이지 줄 포함)가 SUMMARY에 붙어 있다</human-check>
  </verify>
  <acceptance_criteria>
    - `filterSummary` 단위 표가 초록이다
    - 폰 「필터」가 `aria-expanded`·`aria-controls`를 갖고 요약이 필터 값과 같으며 PC에서 「필터」·요약이 렌더되지 않는다 — E2E와 DOM 감사 (g)가 초록이다(DR-26)
    - `loading.tsx`에 표 아래 합계 행 뼈대가 없고 합계 줄 자리에 라벨만 있으며, 진행 막대 요소가 없다(D17)
    - 독립 DOM 감사 보고서(1280·1024·375, (a)~(i))가 SUMMARY에 있고 `CI=true pnpm test`가 한 번 초록이다
    - `projects.module.css`·`filter-bar.tsx`에 새 색·폭 토큰이 없다 — `git diff d6b41cf -- docs/design/tokens.css`가 0줄이다
  </acceptance_criteria>
  <done>폰 첫 화면이 검색·요약·등록을 먼저 보이고 필터는 펼치며, 로딩이 새 합계 줄 자리를 따르고, 목록 화면 전체(04-17 포함)가 세 폭 DOM 감사와 CI=true 전체 게이트를 통과했다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 URL → 목록 서버 페이지 | `year`·`from`·`to`·`page`·`sort`·`dir`·`status`·`teamId`·`q` 전부 신뢰하지 않는 입력(배열로도 온다) |
| 서버 `redirect` → 브라우저 | 연도 자동 전환이 만드는 이동 주소 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-90 | Tampering | URL 파라미터 | medium | mitigate | (04-17에서 옮김 · CEO C-08 정정 — 지금은 `teamId`가 검증 없이 uuid 비교로 가서 22P02, 새 연도 범위는 `0000`에서 22008) `normalizeListParams`가 배열 첫 값·uuid 모양 + 팀 목록 소속·연도 2000–2100을 강제하고, `page`는 `clampPage`, 기간은 `parseListPeriod` 통과분만, 정렬은 허용 목록 — 전부 Drizzle 매개변수로 들어가고 SQL 문자열 연결이 없다. 틀린 파라미터 표 통합 테스트로 고정한다 |
| T-04-367 | Tampering | 연도 자동 전환 `redirect`(DR-30) | low | mitigate | (04-17에서 옮김) 이동 주소는 지금 경로(`/projects`)의 상대 주소이고 `year` 값은 `reconcileListYear`가 만든 4자리 연도 또는 `all`뿐이다 — 외부 주소로 가는 열린 리디렉트가 없다. 나머지 파라미터는 받은 그대로 되돌려 주되 조회 전 `normalizeListParams`가 다시 정규화한다. `reconcileListYear`의 멱등 단위 케이스가 되돌이 이동을 막는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `project-list-view.test.ts` · 통합 `projects-list.test.ts` 초록
- 목록 화면 전체의 독립 DOM 감사(1280·1024·375) 보고서가 SUMMARY에 있다
- `CI=true pnpm test` 세 계층 한 번 통과
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 기간 필터가 겹침으로 동작하고 틀린 입력·파라미터가 오류 화면을 만들지 않으며, 기간이 연도 밖이면 연도가 스스로 바뀌어 빈 결과가 생기지 않는다(DR-30)
- 필터를 바꾸면 1쪽이되 정렬이 남고, 빈 목록이 세 갈래로 다음 한 수를 준다
- 폰 첫 화면이 검색 · 필터 요약 · 1차를 먼저 보이고 상세 필터는 펼친다(DR-26)
- 목록 화면 전체(04-17의 합계 줄·페이지 줄 포함)가 세 폭 DOM 감사와 CI=true 전체 게이트를 통과했다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(신규, domain 순수 — `domain/projects/list-view.ts`에 더함) | `formatListPeriod` · `parseListPeriod` · `yearOptions` · `normalizeListParams` · **`periodOverlapsYear`** · **`reconcileListYear`**(DR-30) · **`filterSummary`**(DR-26) · 빈 갈래 판정 |
| 결과 필드(신규) | `loadProjectList` 결과의 `emptyKind`(`none` · `default-view` · `filtered`) · `periodErrors`(`from`·`to` 칸 오류) |
| 화면(변경) | `app/(app)/projects/filter-bar.tsx` — 기간 두 칸(묶음 `focusout` 제출) · 서버 오류 한 줄 · 연도 바꿀 때 어긋난 기간 비우기(DR-30) · 정렬 숨은 값(C-24) · 폰 「필터」 disclosure + 요약 + `primaryAction` prop(DR-26) · `page.tsx` — 연도 자동 전환 `redirect` · `yearOptions` 선택지 · 빈 갈래 셋 · `projects-table.tsx` — 기간 셀 `formatListPeriod` · `loading.tsx` — 합계 줄 자리 라벨만 |
| URL 파라미터 | `from` · `to`(신규) · 연도가 기간과 어긋나면 서버가 고친 주소로 `redirect`(DR-30) · 필터 폼의 `sort`·`dir` 숨은 값 |
| 테스트 | `test/unit/domain/project-list-view.test.ts`(확장) · `test/integration/projects-list.test.ts`(확장 — 기간 보기 · 틀린 파라미터 표 · `none`) · `test/e2e/projects-list.spec.ts`(확장) |
| 검증 기록 | 목록 화면 독립 DOM 감사(1280·1024·375, (a)~(i)) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 엔지니어링 리뷰 r2 신설(2026-09-24 · `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 1 · 12)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2-01 — 04-17 체커 상한 초과 | HIGH | 04-17 옛 Task 3을 내용 그대로 옮겨 트레이서 우선 세 태스크로 나눔 · truths·backstop·T-04-90·T-04-367 이동 · `depends_on: ["04-17"]` · 웨이브 26(04-47과 파일 겹침 없음) |
| 지시 12 — UI-SPEC 인용 정본 | NOTE | `<context>` 인용 규칙 줄 · read_first를 「## 적용 규칙」 S1·S12 절 이름 + 현재 행으로 |
| 머지 묶음 결정(HANDOFF decisions[0]) | DECISION | objective 머지 묶음 ④ 줄 · C-26 한 배포(04-17 `<probe_fallback>`) |
| PR #38 알려진 문제 「합계 줄」(QA, 원 보고서 유실) | NOTE | `<probe_fallback>` 마지막 줄 — Task 3 DOM 감사가 합계 줄을 실측, 원 증상이 다르면 사용자 재진술 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 기간 겹침 인덱스 · `q` 검색 ILIKE 이스케이프 · 목록 행 링크 `next/link` | 04-17 Round 2 Deferred 그대로(엔지 리뷰 「범위 밖」) |

<output>
Create `.planning/phases/04-project-quote-ledger/04-48-SUMMARY.md` when done
</output>
