---
phase: 04-project-quote-ledger
plan: 49
type: execute
wave: 15
depends_on: ["04-30"]
files_modified:

  - ui/table/save-lock.ts
  - test/unit/ui/save-lock.test.ts
  - ui/table/use-editable-width.ts
  - ui/table/Table.tsx
  - ui/table/types.ts
  - ui/table/Table.module.css
  - ui/table/use-grid-keyboard.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - app/(app)/projects/[id]/period-field.tsx
  - app/(app)/projects/[id]/pre-estimate-field.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/e2e/ledger-save-flow.spec.ts
  - test/e2e/quote-edit-scope.spec.ts

autonomous: true
requirements: [UX-05, UX-04, PROJ-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "현재 차수 복원 줄 `저장 안 한 편집 N칸 · 복원 / 버림`은 표 편집 컨트롤이 아니라 표 위 한 줄이라 **모든 폭(1280 · 1024 · 1000 · 375)에서** 렌더된다 — 상태 바뀜 거부(DR-6)가 편집을 이 줄로 돌려보내므로 폰에서도 사라지지 않는다. 1024 미만에서 「복원」은 rev 5대로 편집을 dirty 모양(`ui/table`의 기존 dirty 인셋 — 읽기 표에도 그려진다)으로 들여오고 N에 더하며(표는 보기 전용 그대로 · 표 밖 칸 편집은 그 칸을 연다), 1차 「일괄 저장 N」은 N ≥ 1이면 렌더된다 — 표 칸만 복원돼도 렌더되고, N은 복원된 표 칸까지 전부 세어 그 폭에서 한 번에 저장한다(UI-SPEC rev 5 후속 결정 R1 47행 · `## UI Considerations` S18 overflow 해소 2026-09-23 · S19 error — 상태가 바뀜 · 적용 규칙 S3 1차 1132–1135행)"
    - "「일괄 저장」 요청 중에는 그 화면의 모든 편집 칸(견적 줄 표 · 매출 발행/입금 표 · 기간 칸 · 총 매출 예상가 칸)이 **보이되 편집에 들어가지 않는다** — 클릭·`Enter`·글자 입력·붙여넣기·`Delete`·`Ctrl+Enter`·`Ctrl+D`·`Alt+↑↓`가 무반응이고 방향키 이동·범위 선택·`Ctrl+C`는 된다. 격자에 `aria-busy=\"true\"`, 신호는 1차 `일괄 저장…` 하나(새 색·덧씌움 없음, P0). 응답이 오면 성공·실패 모두 즉시 풀리고, 저장 중 `Ctrl+S` 반복·IME 조합 입력은 무시된다(DR-3 · 계약 3, UI-SPEC rev 5 S19 loading · S13 · S17 loading)"
    - "견적 줄 표 편집·줄 추가·구조 컨트롤은 **1024px 이상에서만** 있다 — 700~1023은 보기 전용(표 셀 편집·「줄 추가」·EMPTY 「첫 줄 만들기」·구조 키·힌트 줄 없음, 표 밖 칸 기간·총 매출 예상가만 편집), 700 미만은 폰 규칙(같은 보기 전용 + 행 탭 → 행 시트). 1024 미만에서 1차 「일괄 저장 N」은 N ≥ 1이면 — 표 밖 칸이든 표 셀(「복원」으로 들여온 칸 포함)이든 — 렌더되고 N = 0이면 렌더되지 않으며, 안내 글자를 더하지 않는다(UI-SPEC rev 5 후속 결정 R1 47행 — 옛 「표 밖 칸이 dirty일 때만」을 대신한다, P0). 매출 발행·입금 표도 같은 폭 규칙을 따른다(DR-14 · DR-24 · DR-36 · 계약 6, UI-SPEC rev 5 S4 「1024 미만」 · Copywriting `Primary CTA — 견적 원장`)"
    - "좁은 PC 열 접기: 1024~1279는 번호·차익을 숨긴 9열(차익 합계는 합계 행 라벨 뒤 `차익 …`), 700~1023은 소분류·항목·거래처·실행가·상태 5열(견적가·차익 합계는 합계 행 라벨 뒤) — 숨은 열은 방향키·`Tab`이 건너뛰고 붙여넣기·`Ctrl+C`의 논리 열 순서에는 남는다(폭에 따라 같은 TSV가 다른 칸에 떨어지지 않는다). 외화 2행은 1280 미만에서 `USD 4,400.00`과 `@1,318.1818` 두 묶음 사이에서만 줄바꿈한다(DR-14, UI-SPEC rev 5 S4 「좁은 PC」 표)"
    - "폰(700 미만)은 견적 줄 표의 셀 편집에 들어가지 않고 행을 탭하면 행 시트(04-04 · UI-SPEC 적용 규칙 S8)로 읽는다 — 폰에서 셀 편집 입력이 생기지 않는다(PR #38 /qa 알려진 문제 「폰 편집」의 이 플랜 몫 — 원 증상은 원 보고서 유실로 모르고, 폰 규칙의 정본은 UI-SPEC 적용 규칙 S4 「1024 미만」 · S8이다)"
    - "견적 원장 화면(04-30의 셀 단계·구조·EMPTY · 이 플랜의 저장 중 잠금·폭 규칙·복원 줄)의 독립 DOM 감사(1280 · 1024 · 375 + 1000)와 전체 게이트 `CI=true pnpm test` 한 번이 이 플랜 끝에서 돈다 — 04-30은 E2E까지만 `CI=true`로 판정했다(엔지 r2 E2-02 분할, CLAUDE.md 화면 검증 순서)"
    - statement: "견적 줄 EMPTY 변형이 다섯 상태 × 세 계급(담당 PM·팀장·경영관리) + 권한 없는 사람 + 폰·좁은 PC 렌더에서 문구와 버튼 유무가 rev 5 Copywriting `Empty — 견적 줄 표` 행과 일치한다 — 렌더 감사(UI-SPEC rev 5 S4 empty)"
      verification: backstop
    - statement: "견적 줄이 0·1·6줄일 때 0줄은 EMPTY 한 줄, 1줄과 6줄은 같은 그룹 머리글 + `합계 (공급가액 · N줄)` 형식이고 조수사가 바뀌지 않는다 — 렌더 감사(S4 zero-one-many)"
      verification: backstop
    - statement: "견적 줄 표가 1280 · 1024 · 375에서 문서 가로 스크롤 0이고, 단가 열 폭이 외화 2행 전체 길이로 넓어지지 않는다 — DOM 감사(S4 overflow)"
      verification: backstop
    - statement: "1024 미만(375 · 1000)에서 「줄 추가」·「조정 줄 추가」·「견적 외 비용 줄 추가」·EMPTY 「첫 줄 만들기」가 렌더되지 않고 셀이 Enter·클릭에 편집으로 들어가지 않으며 힌트 줄이 없다 — DOM 감사(S4 폰 · 좁은 PC 구조 컨트롤)"
      verification: backstop
    - statement: "375에서 현재 차수 복원 줄의 글자는 `keep-all`로 줄바꿈하고 3차 둘(「복원」·「버림」, 각 44px `--touch-min`)은 **함께** 다음 줄로 내려가며(둘이 갈라지지 않는다), 복원 줄은 제 줄(블록)이라 뒤에 오는 이전 차수 복원 줄(04-24)과 **각자** 줄바꿈하고, 문서 가로 스크롤 0 — 1280 · 1024에서도 렌더된다 — DOM 감사(UI-SPEC rev 5 `## UI Considerations` S18 overflow — resolved explicit, 사용자 2026-09-23)"
      verification: backstop
  prohibitions:
    - statement: "저장 요청이 진행 중인 동안 친 입력이 응답으로 조용히 사라져서는 안 된다. 저장 응답은 표를 서버 목록으로 다시 그리므로 그 사이 입력은 저장됨 표시 아래로 사라진다 — 요청 동안 편집 진입 자체를 막는다(DR-3)"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/table/save-lock.ts"
      provides: "저장 중 잠금에서 허용되는 격자 동작 판정(순수) — `Table`·`use-grid-keyboard`가 같은 함수를 부른다(계약 3)"
      exports: ["isGridActionAllowed", "GridAction"]
    - path: "ui/table/use-editable-width.ts"
      provides: "표 편집 가능 폭(≥1024) 훅 — 견적 표·매출 표(그리고 그룹 B의 리저브 대장)가 같은 기준을 쓴다(DR-36)"
      exports: ["useEditableWidth"]
    - path: "test/e2e/ledger-save-flow.spec.ts"
      provides: "저장 흐름(S19) — 저장 중 잠금 케이스(이 플랜) · 상태 바뀜 거부(04-22)"
      min_lines: 50
    - path: "test/e2e/quote-edit-scope.spec.ts"
      provides: "(04-30 스펙에 더함) 좁은 폭 보기 전용 k·l · 복원 줄 375/1000 l2·l3 · 숨은 열 키 이동 m · 폰 행 시트 읽기"
  key_links:
    - from: "ui/table/Table.tsx"
      to: "ui/table/save-lock.ts"
      via: "`saveLocked`이면 편집 진입 동작을 `isGridActionAllowed`로 거른다 — 격자 모양은 그대로, `aria-busy`"
      pattern: "isGridActionAllowed"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "ui/table/use-editable-width.ts"
      via: "1024 미만이면 표를 보기 전용으로(편집 컨트롤·힌트·첫 줄 만들기 미렌더), 1차는 N ≥ 1이면 렌더 — 표 셀 dirty 포함(후속 결정 R1)"
      pattern: "useEditableWidth"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/period-field.tsx"
      via: "저장 중에는 `saveLocked`로 기간 두 칸이 `readOnly`가 된다(값·포커스 유지)"
      pattern: "saveLocked"
---

<objective>
**As a** 견적 원장에서 저장하고 폭이 다른 화면으로 보는 기획 PM·경영관리, **I want to** 저장 요청 동안에는 칸이 보이되 편집에 들어가지 않고, 좁은 화면에서는 표가 보기 전용이되 복원한 편집을 그 폭에서도 저장할 수 있기를, **so that** 저장 응답이 방금 친 입력을 덮지 않고, 폰·좁은 PC에서 표가 깨지거나 편집이 사라지지 않는다.

머지 묶음: ②/4 상태 전환 — 웨이브 2~18(04-50 → 04-23)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**2026-09-24 엔지니어링 리뷰 r2 신설 플랜(`docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 3 · E2-02 — 04-30 분할).** 04-30(77KB · 3태스크)이 체커 상한을 넘어 옛 **Task 3**(저장 중 잠금 `save-lock` · 1024 미만 보기 전용 · 좁은 PC 열 접기 · 현재 차수 복원 줄 모든 폭 + 화면 검증 1280/1024/375)을 **내용 그대로** 옮겼다. 트레이서 우선 규칙에 맞춰 두 태스크로 나눴다 — Task 1 트레이서(저장 중 잠금: 순수 판정 → `ui/table` 편집 진입 게이트 → `QuoteLedger` 배선 → E2E) · Task 2(폭 규칙 · 복원 줄 모든 폭 + 견적 원장 화면 DOM 감사 · 전체 게이트 한 번). 04-30의 truths·backstop·금지 항목·T-04-335·T-04-336·`<probe_fallback>` 세 줄(저장 중 잠금 소유 · 폭 판정 · R1)도 함께 옮겼다. **편집기 커밋과 저장 페이로드 수집은 04-30에 남았다**(04-30 Task 1 ③-b · E2E e3 — 분할안 「활성 셀 마지막 값 → 페이로드」). 이 플랜의 저장 중 잠금은 그 커밋 **뒤**에 걸린다. 04-26은 이제 이 플랜 뒤다(`quote-table.tsx`가 겹친다) — 웨이브 15 이후 모든 플랜이 한 웨이브씩 밀렸다.

**PR #38 알려진 문제(이 플랜 몫):** 「미저장 복원 미연결」의 복원 줄 쪽 — 04-22가 보관·「복원」을 잇고 이 플랜이 복원 줄을 모든 폭(1280 · 1024 · 1000 · 375)에서 보이게 하며 1024 미만에서도 복원한 편집을 저장하게 한다(Task 2 ② · E2E l2·l3). 「폰 편집」(QA, 원 보고서 유실) — 폰 규칙(보기 전용 + 행 시트)을 E2E로 고정한다(Task 2 · E2E l · l4). 원 증상이 이것과 다르면 사용자 재진술이 필요하다(계획 인계 질문).

Purpose: 저장 흐름(S19)의 마지막 구멍(저장 중 입력 유실 — DR-3)을 막고, 폭 계약(DR-14 · DR-24 · DR-36 · 계약 6)과 복원 줄(S18)을 끝낸다.

Output: `ui/table/save-lock.ts`(순수) · `ui/table/use-editable-width.ts` · `Table.saveLocked`·`collapseBelow` · `QuoteLedger`·`RevenueSection`·기간·총 매출 예상가 칸 배선 · 단위 1 · E2E 2(확장) · 견적 원장 화면 독립 DOM 감사 · 전체 게이트 한 번.

**이어받은 결정(04-30 원문 그대로):** 디자인 리뷰 DR-3 · 계약 3(저장 중 잠금 소유) · DR-14 · DR-24 · DR-36 · 계약 6(폭 규칙) · 체커 revision 1 B4(복원 줄 모든 폭 — S18 overflow resolved explicit) · UI-SPEC rev 5 후속 결정 R1(1024 미만 1차는 N ≥ 1이면 렌더, 재론하지 않는다).

**실행 규율(CLAUDE.md · hooks):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`를 호출한다(RED → GREEN → 리팩터). 테스트·빌드·린트 실패를 쫓기 전에 Skill `systematic-debugging`을 호출하고 코드를 만지기 전에 원인을 찾는다. 커밋 전과 「완료」를 말하기 전에 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시하고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(이 플랜 하나). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다. 화면 검증 순서는 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번이고, 완료 판정은 `CI=true`다. `quote-table.tsx`는 1,000행이 넘는다 — Grep으로 자리를 찾고 범위만 Read한다. E2E 날짜는 `kstToday(new Date())` + `addDays`(CEO A-18). 새 의존성 0개.

**앞 플랜 결함(ENG-D11 — 엔지 리뷰 R10):** 이 플랜의 검증이 **앞 Phase 4 플랜 파일**(04-30의 셀 단계·EMPTY 포함)의 결함 때문에 실패하면 넘기지 말고 그 파일을 고친다 — 결함을 재현하는 회귀 테스트를 더하고 SUMMARY에 편차(deviation: 파일 · 원인 · 테스트 이름)로 적는다. 범위를 늘리지 않는다는 규칙은 그대로다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-30-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-22-SUMMARY.md
@.planning/phases/04-project-quote-ledger/04-44-SUMMARY.md
@CLAUDE.md

# 범위 Read:

# UI-SPEC 인용 규칙(엔지 r2 지시 12 · CEO r2 C2-14): `04-UI-SPEC.md`는 S번호와 「## 적용 규칙」 절(1006행~)을 정본으로 인용한다 — 상단 요약·`## UI Considerations`(445행~) 서술과 어긋나면 적용 규칙을 따른다. 아래 행 번호는 2026-09-24 현재이고 절 이름으로 찾는다.

# .planning/phases/04-project-quote-ledger/04-UI-SPEC.md rev 5 — 「## 적용 규칙」 S3 1차 렌더(1024 미만, 1120–1157행 중 1132–1135) · S4(1158–1233행 — 좁은 PC 표 · 1024 미만) · S8 폰 행 시트(1351행) · S18(1548–1562행) · S19 저장 중 편집 잠금(1563–1581행) · `## UI Considerations` S4 overflow·폰·좁은 PC 구조 컨트롤(502–522행) · S18 overflow(640–650행 — resolved explicit 2026-09-23) · S19 loading · error — 상태가 바뀜(651–668행) · 첫머리 표 「rev 5 후속 결정」 R1 47행 · `## Copywriting Contract` `Primary CTA — 견적 원장` 342행

# docs/designs/plant8-erp-phase4-design-review-260923.md DR-3 · DR-14 · DR-24 · DR-36(행은 Grep)

# docs/designs/plant8-erp-phase4-eng-review-260923-r2.md 「분할안」 04-30 → 04-30 + 04-49

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **UX-05(저장 중 단축키 무동작)** 와 **UX-04(입력값 유실 없음 — 저장 중 입력)**, PROJ-02 화면의 폭 부분이다(스펙 없는 프로브 보고의 UX-05·UX-04·PROJ-02 행은 `unresolved` — 아래 가정으로 표면화하고 조용히 버리지 않는다). 수용 기준은 04-30 옛 Task 3과 UI-SPEC rev 5 S4·S18·S19에서 끌어왔다. S4의 empty(변형)·zero-one-many·overflow·폰 구조 컨트롤 backstop과 S18 overflow backstop은 flat-scalar 마커로 옮겼다(EMPTY 변형·0/1/6줄은 04-30 산출물이고 감사는 이 플랜).

**계획 단계 가정·확정(표면화 — 04-30 원문 그대로 이어받음):**

- **저장 중 잠금의 소유(DR-3 · 계약 3 — 04-30에서 옮김, 이 플랜):** 잠금은 편집 진입의 문제라 셀 단계·구조 배선과 같은 자리(`ui/table`의 편집 진입 + `QuoteLedger`)에서 한 번 건다 — 네 편집기(견적 격자 · 매출 표 · 기간 칸 · 총 매출 예상가 칸)가 모두 선 뒤(04-22 · 04-44 다음)라 한 태스크에서 전부 잠근다. 격자 모양을 읽기 표로 바꾸지 않는다(깜빡임·포커스 손실 없음) — `aria-busy`와 동작 거르기만.
- **폭 판정(DR-36 — 계획 판단):** 폭은 클라이언트만 안다 — 서버는 권리대로 컨트롤을 보내고, `useEditableWidth()`(서버 스냅숏 참)가 1024 미만에서 편집 컨트롤을 거둔다(좁은 화면은 수화 직후 한 번 격자 → 읽기 표). 거두는 목록은 Task 2 ①이 정본이다. **현재 차수 복원 줄은 거두지 않는다** — 편집 컨트롤이 아니라 표 위 한 줄이고, UI-SPEC rev 5 S18 overflow(사용자 2026-09-23 resolved explicit)가 폰 폭 줄바꿈과 375 DOM 감사를 정했으며, S19 error(DR-6)가 상태 바뀜 거부의 편집을 이 줄로 돌려보낸다(Task 2 ②).
- **1024 미만 1차 렌더(UI-SPEC rev 5 후속 결정 R1 — 사용자 2026-09-23 결정, 재론하지 않는다):** 어느 폭에서든 화면 1차 「일괄 저장 N」은 N ≥ 1이면 렌더된다 — 표 셀 편집(1024 미만에서 「복원」으로 들여온 칸 포함)도 N에 들어 1차를 세운다. 1024 미만에서 표는 보기 전용 그대로다(DR-36 불변) — 바뀌는 것은 1차의 렌더 조건 하나이고 안내 글자는 없다(P0). 옛 「표 밖 칸이 dirty일 때만」(DR-24 · DR-36)과 그 공백 보고는 이 결정으로 닫혔다(Task 2 ①② · E2E l2 · l3).
- **편집기 커밋과의 순서:** 04-30 Task 1 ③-b가 저장 페이로드를 모으기 전에 열린 편집기를 커밋한다. 이 플랜의 `saveLocked`는 그 뒤(요청 시작 = `isExecuting`)에 참이 되므로 잠금이 걸리는 순간 열려 있는 편집기는 원칙상 없다 — 남아 있으면 커밋하고 닫는다(값을 버리지 않는다).

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 저장 요청 동안 네 편집기가 보이되 편집에 들어가지 않는다(순수 판정 `isGridActionAllowed` → `Table.saveLocked` 편집 진입 게이트 → `QuoteLedger` 배선 → E2E)</name>
  <files>ui/table/save-lock.ts, test/unit/ui/save-lock.test.ts, ui/table/Table.tsx, ui/table/use-grid-keyboard.ts, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/period-field.tsx, app/(app)/projects/[id]/pre-estimate-field.tsx, test/e2e/ledger-save-flow.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) 「## 적용 규칙」 S19 저장 중 편집 잠금(1563–1581행) · `## UI Considerations` S19 loading(651행 근처)
    - `ui/table/Table.tsx`(전문 — `useGridKeyboard` 배선 · 셀 클릭 편집 · `handleTablePaste` · 격자/읽기 표 전환 · 04-30이 더한 `onBlockedEdit`·편집기 커밋 입구) · `ui/table/use-grid-keyboard.ts`(전문 — 키 분기)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 `useAction(`·`isExecuting`·`handleSave`·1차 렌더 조건(`canSave`)·`<RevenueSection`을 찾아 범위만 Read · `app/(app)/projects/[id]/revenue-section.tsx`(전문 — 발행·입금 `Table`과 추가 버튼·계약 입력)
    - `app/(app)/projects/[id]/period-field.tsx`(04-22 산출물)·`pre-estimate-field.tsx`(04-44 산출물)
    - `test/e2e/ledger-save-flow.spec.ts`(04-22 산출물 — 준비·로그인 형태)
  </read_first>
  <behavior>
    - `isGridActionAllowed(action, { saveLocked: true })`: `enterEdit`·`typeChar`·`paste`·`deleteCell`·`deleteRow`·`newRow`·`duplicateRow`·`moveRow`·`save` → 거짓 · `move`·`select`·`copy` → 참
    - `isGridActionAllowed(action, { saveLocked: false })` → 모든 동작 참
  </behavior>
  <action>
① **저장 중 잠금 판정(순수, TDD).** `ui/table/save-lock.ts`에 `GridAction` 유니언(`enterEdit` · `typeChar` · `paste` · `deleteCell` · `deleteRow` · `newRow` · `duplicateRow` · `moveRow` · `save` · `move` · `select` · `copy`)과 `isGridActionAllowed(action, { saveLocked })`를 둔다. `test/unit/ui/save-lock.test.ts`에 `behavior` 두 줄을 먼저 쓴다(RED → GREEN).

② **`ui/table` 편집 진입 게이트(계약 3).** `Table`에 `saveLocked?: boolean` prop을 더한다 — 참이면 격자 모양(`role="grid"`·머리글 바탕)은 그대로 두고 격자 요소에 `aria-busy="true"`, 셀 클릭 편집·`onEnterEdit`·붙여넣기(`handleTablePaste`)·`keyboard` 핸들러(삭제·새 줄·복제·줄 이동·저장)를 `isGridActionAllowed`로 거른다(`use-grid-keyboard.ts`가 같은 함수를 받아 키 분기에서 부른다 — 방향키·범위 선택·`Ctrl+C`는 그대로). `saveLocked`가 참이 되는 순간 열려 있던 셀 편집기가 남아 있으면 04-30의 커밋 입구로 커밋하고 닫는다(값을 버리지 않는다). 한글 조합 중(`isComposing`) 키는 편집을 시작하지 않는다. 새 색·덧씌움·스피너를 더하지 않는다(P0 — 신호는 1차 `일괄 저장…` 하나).

③ **`QuoteLedger` 배선.** `saveLocked = isExecuting`을 만들어 견적 줄 `Table`, `RevenueSection`(그 안의 발행·입금 `Table`과 계약 입력 — 계약 입력은 묶음 ③의 04-16·04-41이 지운다), `PeriodField`·`PreEstimateField`(입력을 `readOnly`로 — `disabled`가 아니라 값이 보이고 포커스가 남는다)에 넘긴다. `handleSave`는 `isExecuting`이면 곧바로 돌아온다(자동 반복·연타 `Ctrl+S` 무시 — 요청 한 번). 응답이 오면(성공·실패·상태 바뀜 거부 모두) `isExecuting`이 거짓이 되어 곧바로 풀린다. 그룹 B의 매출 표 개편(04-16)과 리저브 대장(04-42)은 같은 `saveLocked` prop을 받는다(계약 3).

④ **E2E(트레이서 증명 — `test/e2e/ledger-save-flow.spec.ts`에 describe `저장 중 잠금(DR-3)` 추가).** 줄 둘인 진행 프로젝트를 담당 PM으로 열고 실행가 셀 하나를 고친 뒤, `page.route`로 서버 액션 요청(POST + `Next-Action` 헤더)을 `deferred`가 풀릴 때까지 붙잡는다 → `Control+s` → 1차가 `일괄 저장…`이고 격자에 `aria-busy="true"` → 다른 편집 가능 셀에서 `Enter`를 누르고 `9`를 쳐도 입력 요소가 생기지 않고 셀 값이 그대로이며, `ArrowDown`으로 포커스는 옮겨진다 → `Control+s`를 한 번 더 눌러도 붙잡힌 요청이 1개다 → 기간 칸(열어 둔 경우)의 입력이 `readonly`다 → 요청을 풀면 합계 행 `저장됨`, `aria-busy`가 없고 `Enter`가 다시 편집 모드를 연다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/save-lock.test.ts test/unit/ui/parse-tsv.test.ts test/unit/domain/quote-edit-scope.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 저장 중 잠금의 허용 동작 표가 어긋나거나 붙여넣기·편집 범위 단위가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/ledger-save-flow.spec.ts test/e2e/quote-edit-scope.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 저장 중 셀이 편집으로 들어갔거나 요청이 두 번 갔거나 잠금이 풀리지 않았거나, 04-30의 셀 단계·활성 셀 커밋(e3)·매출 흐름이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `isGridActionAllowed`가 `ui/table/save-lock.ts` 하나에 있고 `Table.tsx`·`use-grid-keyboard.ts`가 같은 함수를 부른다(`grep -rln "isGridActionAllowed" ui/table` 결과가 세 파일 — 정의 1 + 호출 2) · 단위 두 줄이 초록이다
    - `Table`에 `saveLocked` prop이 있고 참일 때 격자 요소에 `aria-busy="true"`가 붙으며 `role="grid"`가 바뀌지 않는다(E2E)
    - `QuoteLedger`가 `saveLocked = isExecuting`을 견적 줄 표 · `RevenueSection` · 기간 칸 · 총 매출 예상가 칸에 넘기고, `handleSave`가 `isExecuting`이면 요청을 보내지 않는다(Grep + E2E 「붙잡힌 요청 1개」)
    - 저장 중 편집 진입 없음 → 응답 뒤 편집 가능이 E2E로 증명된다(DR-3) · 04-30 E2E e3(활성 셀 마지막 값)가 그대로 초록이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>저장 요청 동안 화면의 네 편집기가 보이되 편집에 들어가지 않아 응답이 입력을 덮지 않고, 연타 저장이 요청을 하나만 만든다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 폭 규칙(1024 미만 보기 전용 · 좁은 PC 열 접기 — DR-14 · DR-24 · DR-36 · 계약 6) · 현재 차수 복원 줄 모든 폭(S18 overflow · R1) · 폰 행 시트 + 견적 원장 화면 검증(독립 DOM 감사 1280 · 1024 · 375 · 1000 → 수정 → 전체 게이트 한 번)</name>
  <files>ui/table/use-editable-width.ts, ui/table/Table.tsx, ui/table/types.ts, ui/table/Table.module.css, ui/table/use-grid-keyboard.ts, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/project-detail.module.css, test/e2e/quote-edit-scope.spec.ts</files>
  <read_first>
    - `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(rev 5) `## UI Considerations` 「S18」(640–650행 — overflow resolved explicit 2026-09-23) · 「S19」 error — 상태가 바뀜 · 「## 적용 규칙」 「S18」(1548–1562행) · 「S4」 좁은 PC 표 · 「1024 미만」(1158–1233행 중) · 「S3」 1차 렌더(1024 미만, 1132–1135행) · 「S8」 폰 행 시트(1351행) · 첫머리 표 「rev 5 후속 결정」 R1 47행 · `## Copywriting Contract` `Primary CTA — 견적 원장` 342행 · `## UI Considerations` 「S4」 overflow·폰·좁은 PC 구조 컨트롤
    - `ui/table/Table.tsx`(Task 1 이후 전문) · `ui/table/types.ts` · `ui/table/Table.module.css`(폰 미디어 쿼리 175행 근처)
    - `app/(app)/projects/[id]/quote-table.tsx` — Grep으로 열 정의(`key: "sort"` …)·1차 렌더 조건(`canSave`)·`restoreBanner`(04-04 · 04-22 배선)를 찾아 범위만 Read · `revenue-section.tsx`(Task 1 이후)
    - `test/e2e/quote-edit-scope.spec.ts`(04-30 산출물) · `test/e2e/quote-table.spec.ts` (f) 폰 행 시트 케이스(04-04)
    - `playwright.config.ts` — `CI=true`에서만 프로덕션 빌드(완료 판정 근거)
  </read_first>
  <action>
① **폭 규칙(DR-14 · DR-24 · DR-36 · 계약 6).** `ui/table/use-editable-width.ts`에 `useEditableWidth()` — `matchMedia("(min-width: 1024px)")`를 `useSyncExternalStore`로 구독하고 서버 스냅숏은 참(PC 렌더가 기본, 좁은 화면은 수화 뒤 바뀐다)(그룹 B 리저브 대장이 같은 훅을 쓴다). `TableColumn`에 `collapseBelow?: 1280 | 1024`를 더한다 — 그 폭 **미만**이고 700 이상에서 숨고, 1024 미만은 `1280` 열도 숨는다(`Table.module.css`에 1024·1280 두 미디어 쿼리만 — 새 토큰 없음. 목록 열 접기 04-18이 같은 API를 쓴다). 숨은 열은 `Table`의 방향키·`Tab` 이동이 건너뛰고(같은 폭 판정) 붙여넣기·`Ctrl+C`의 논리 열 순서에는 남는다. 견적 줄 열: 번호·차익 `collapseBelow: 1280`, 수량·단가·견적가·비고 `collapseBelow: 1024` — 합계 행 라벨 뒤에 숨은 열의 합계(1024~1279 `차익 {합}`, 700~1023 `견적 {합} · 차익 {합}`)를 그 폭에서만 보인다. 외화 2행은 1280 미만에서 두 묶음 사이에서만 줄바꿈한다(`project-detail.module.css` — 단가·실행가 열이 2행 전체 길이로 넓어지지 않게). **1024 미만**(`useEditableWidth()` 거짓)이면 `QuoteLedger`는 견적 줄 열의 편집 가능성을 전부 `readonly`로 넘겨 표가 캡션 있는 읽기 표가 되게 하고(구조 키가 붙지 않는다), 「줄 추가」·EMPTY 「첫 줄 만들기」(`action.kind === "addLine"`만 뺀다 — 「기간 바꾸기」는 표 밖 칸이라 남긴다)·힌트 줄을 렌더하지 않는다 — 현재 차수 복원 줄은 거두지 않는다(②). `RevenueSection`에도 같은 폭 판정을 넘겨 발행·입금 표를 보기 전용으로, 추가 버튼을 미렌더로, EMPTY를 사실만으로 둔다. 1차 「일괄 저장 N」은 1024 미만에서 N(표 셀 dirty — 「복원」으로 들여온 칸 포함 — + 표 밖 칸 기간 · 총 매출 예상가 dirty)이 1 이상이면 렌더하고 0이면 렌더하지 않는다 — 렌더 조건에 「표 밖 칸 dirty」 갈래를 두지 않고 1024 이상과 같은 dirty 셈 하나를 쓰며, 안내 글자를 더하지 않는다(UI-SPEC rev 5 후속 결정 R1 — 적용 규칙 S3 1132–1135행, P0). 저장 페이로드는 폭과 무관하게 화면의 전체 dirty다(표가 읽기 표여도 줄 상태는 그대로 실린다). 폰(<700)의 행 탭 → 행 시트는 그대로다(UI-SPEC 적용 규칙 S8).

② **현재 차수 복원 줄 — 모든 폭(S18 overflow · DR-6, 체커 revision 1 B4).** 복원 줄(`quote-table.tsx`의 `restoreBanner` 자리 — 04-04 · 04-22 배선)은 `useEditableWidth()`와 무관하게 렌더한다. 1024 미만의 「복원」은 rev 5 S18 그대로 편집을 dirty 모양으로 들여오고 N에 더한다 — 표 칸 편집은 보기 전용 읽기 표에 기존 `ui/table` dirty 인셋으로 보이고(새 표시 없음), 표 밖 칸 편집은 04-22 · 04-44의 병합대로 그 칸을 연다. 1차 렌더는 ①의 1024 미만 규칙(R1)이다 — 표 칸만 복원돼도 N ≥ 1이라 1차 `일괄 저장 N`이 렌더되고 그 폭에서 저장된다(새 UI·문구 없음). 모양(S18 overflow — 새 토큰 없음): 복원 줄은 제 줄(블록)이고 `--fs-sm --muted` · `word-break: keep-all` · `flex-wrap: wrap`, 두 3차(「복원」·「버림」)는 한 묶음 요소(`restoreActions` — 묶음 안은 줄바꿈하지 않는다)로 감싸 **함께** 다음 줄로 내려가게 하고, 폰(<700)에서 두 버튼은 `min-height: var(--touch-min)`이다. 뒤에 오는 이전 차수 복원 줄(04-24)은 같은 두 클래스를 쓰면 제 줄에서 **각자** 줄바꿈한다(04-24에 보고). 가로 스크롤 0.

③ **E2E(`test/e2e/quote-edit-scope.spec.ts`).** (k) 뷰포트 1000×800에서 진행 프로젝트의 견적 줄 표가 `role="grid"`가 아니고(캡션 있는 `<table>`), 「줄 추가」·힌트 줄이 없으며, 0줄 표 EMPTY에 「첫 줄 만들기」가 없고, 1차가 없다가 기간 칸 종료일을 바꾸면 1차가 생긴다, (l) 뷰포트 375×812에서 같은 단언(「줄 추가」 없음 · dirty 0이면 1차 없음 → 기간 칸을 바꾸면 1차), (l2) **복원 줄 375(B4 · S18):** 같은 375에서 진행 프로젝트의 담당 PM이 기간 칸 종료일을 `addDays(kstToday(new Date()), 10)`으로 바꾼 채(1차가 생긴다) 새로 고치면 표 위에 `저장 안 한 편집 1칸 · 복원 / 버림`과 「복원」·「버림」 두 버튼이 보이고, 두 버튼의 `boundingBox().y`가 같으며 높이가 각각 44 이상이다 → 「복원」 → 기간 칸이 열려 종료일이 적은 값이고 1차 `일괄 저장 1`이 보인다 · **표 칸만(R1):** 1280에서 실행가 셀 하나만 고친 뒤(표 밖 칸은 그대로) 뷰포트를 375로 바꿔 새로 고치면 복원 줄이 보이고 「복원」 뒤 1차 `일괄 저장 1`이 렌더되며, 누르면 합계 행 `저장됨`이고 새로 고친 뒤 그 셀이 고친 값이다, (l3) 뷰포트 1000×800에서도 셀 편집이 보관된 채 새로 고치면(1280에서 셀을 고친 뒤 뷰포트를 1000으로 바꿔 새로 고침) 복원 줄이 보이고 「복원」 뒤 그 셀에 dirty 인셋이 있으며 표는 여전히 `role="grid"`가 아니고, 1차 `일괄 저장 1`이 렌더되어(표 밖 칸 dirty 0 — R1) 누르면 합계 행 `저장됨` → 새로 고친 뒤 그 셀이 고친 값이다, (l4) **폰 행 시트(PR #38 「폰 편집」의 이 플랜 몫):** 375에서 견적 줄 한 행을 탭하면 행 시트(`getByRole("dialog")`)가 열리고 그 안과 표 어디에도 셀 편집 입력(`input`·`textarea`·`contenteditable`)이 없다, (m) 뷰포트 1100×800에서 번호·차익 열이 보이지 않고, 실행가 셀에서 `ArrowRight`를 누르면 포커스가 상태 셀로 간다(숨은 차익을 건너뛴다), 합계 행에 `차익 `으로 시작하는 합계가 보인다.

④ **화면 검증 순서(CLAUDE.md — 견적 원장 화면).** 싼 게이트 → 별도 서브에이전트(`model` 명시 — 예: sonnet, 프롬프트에 `verification-before-completion` 명시)가 `CI=true`로 **1280 · 1024 · 375**(+ 700~1023 대표 폭 1000) DOM을 실측해 S4 backstop 넷(EMPTY 변형 × 상태 × 계급 렌더 — 04-30 산출물 · 0/1/6줄 합계 형식 — 04-30 산출물 · 가로 스크롤 0과 단가 열 폭 · 1024 미만 구조 컨트롤 부재)과 **S18 overflow backstop**(현재 차수 복원 줄이 1280 · 1024 · 375에서 렌더 · 375에서 글자 `word-break: keep-all` 계산값 · 두 3차 높이 ≥ 44px이고 같은 줄 · 문서 가로 스크롤 0)과 저장 중 잠금의 `aria-busy`를 판정 → 필요하면 `project-detail.module.css`·`ui/table/Table.module.css`만 고친다(04-30 파일이면 ENG-D11 — 회귀 테스트 + SUMMARY 편차) → 전체 게이트 **한 번** `CI=true pnpm test`(다른 에이전트와 동시에 돌리지 않는다). 판정을 SUMMARY에 적는다(스크린샷 육안 금지).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/save-lock.test.ts test/unit/ui/parse-tsv.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 숨은 열이 붙여넣기 논리 열 순서를 바꿨거나 잠금 표가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm playwright test test/e2e/ledger-save-flow.spec.ts test/e2e/quote-edit-scope.spec.ts test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts test/e2e/project-period.spec.ts</automated>
    <fails_when>lint·typecheck·build error 또는 playwright failed 개수가 1 이상이다 — 1024 미만에서 편집 컨트롤이 렌더됐거나, 1024 미만에서 표 칸만 복원된 편집에 1차가 렌더되지 않았거나 그 저장이 값을 남기지 않았거나(R1), 폰에서 셀 편집 입력이 생겼거나, 숨은 열이 키 이동을 막거나, 기존 표·매출·기간 흐름이 깨졌다</fails_when>
    <automated>bash scripts/reset-test-db.sh &amp;&amp; CI=true pnpm test</automated>
    <fails_when>세 계층 중 어느 하나라도 0이 아닌 코드로 끝나거나 failed 개수가 1 이상이다(프로덕션 빌드 기준)</fails_when>
    <human-check>
      <test>독립 DOM 감사 에이전트의 S4 backstop 네 판정 · S18 overflow 판정 · 저장 중 잠금 판정(1280 · 1024 · 375 · 1000)을 SUMMARY에서 확인</test>
      <expected>EMPTY 변형 문구·버튼 유무가 rev 5 Copywriting과 같고, 0/1/6줄에서 합계 형식이 같으며, 세 폭 가로 스크롤 0 · 단가 열이 외화 2행으로 넓어지지 않고, 1024 미만에 구조 컨트롤이 없으며, 현재 차수 복원 줄이 375에서도 렌더되고 두 3차가 함께 줄바꿈하며(각 44px 이상), 저장 중 격자가 `aria-busy="true"`다</expected>
      <why_human>상태 × 계급 × 폭 조합 렌더는 소스 단언으로 증명되지 않는 backstop 항목이다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `useEditableWidth`가 `ui/table/use-editable-width.ts` 하나에 정의되고 `TableColumn.collapseBelow`가 1024·1280 두 값만 받는다 · `Table.module.css`의 새 미디어 쿼리가 두 값만 쓴다
    - 1024 미만에서 견적 줄·매출 표가 보기 전용이고 「줄 추가」·힌트 줄·「첫 줄 만들기」가 없으며 1차는 dirty 0이면 없고 N ≥ 1이면 렌더된다(E2E k·l) · 표 칸만 복원된 편집에도 1차가 375·1000에서 렌더되고 누르면 저장된다(E2E l2 · l3 — UI-SPEC rev 5 후속 결정 R1) · 1차 렌더 조건에 「표 밖 칸 dirty」만 세는 갈래가 없다(코드 리뷰) · 1100에서 숨은 차익 열을 방향키가 건너뛴다(E2E m)
    - 현재 차수 복원 줄이 `useEditableWidth()` 조건 밖에서 렌더되고(Grep — 복원 줄 렌더 조건에 폭 판정이 없다), 375에서 복원 줄·두 3차(같은 줄 · 44px)·「복원」 뒤 기간 칸 dirty와 1차가, 1000에서 「복원」 뒤 읽기 표의 dirty 셀과 1차 저장이 E2E로 증명된다(l2 · l3, B4 · R1) · `project-detail.module.css`의 복원 줄 규칙이 `keep-all`과 `restoreActions` 묶음을 갖고 새 토큰이 없다
    - 375에서 행 탭 → 행 시트가 열리고 셀 편집 입력이 없다(E2E l4)
    - 독립 DOM 감사 판정(1280 · 1024 · 375 · 1000)이 SUMMARY에 있고 `CI=true pnpm test`가 한 번 초록이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>1024 미만에서는 표가 보기 전용 · 좁은 PC에서는 정해진 열이 접히며, 현재 차수 복원 줄은 375까지 모든 폭에서 S18 줄바꿈 규칙으로 보이고 그 폭에서 저장되며, 폰은 행 시트로 읽고, 견적 원장 화면 전체가 네 폭 DOM 감사와 CI=true 전체 게이트를 통과했다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 판정 → 격자 | 셀 단계·구조 가능성은 서버에서 온다(04-30) — 폭 규칙은 그 위의 표시 규칙일 뿐 권한이 아니다 |
| 저장 요청 진행 중 화면 → 서버 응답 | 응답이 표를 서버 목록으로 다시 그린다 — 그 사이 입력이 조용히 사라지는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-335 | Tampering | 저장 중 입력의 유실(DR-3) | medium | mitigate | (04-30에서 옮김) 저장 요청 동안 `Table.saveLocked`가 편집 진입 동작을 `isGridActionAllowed`로 거르고 표 밖 칸은 `readOnly` — 응답이 입력을 덮는 경로가 없고, 연타 `Ctrl+S`가 두 번째 요청을 만들지 않는다(E2E 「붙잡힌 요청 1개」 · 금지 항목 2) |
| T-04-336 | Elevation of Privilege | 폭으로 숨긴 편집 컨트롤(화면 쪽) | low | accept | (04-30에서 옮김) 1024 미만의 편집 컨트롤 미렌더는 표시 규칙일 뿐 권한이 아니다 — 권한·셀 단계는 서버(04-12)가 폭과 무관하게 판정하므로 좁은 폭에서 요청을 조작해도 서버 규칙 밖으로 나가지 않는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- 단위 `save-lock.test.ts` 초록
- 독립 DOM 감사(S4 backstop 넷 · S18 overflow · 저장 중 잠금 — 1280 · 1024 · 375 · 1000) 판정이 SUMMARY에 있다
- `CI=true pnpm test` 세 계층 한 번 통과
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 저장 요청 동안 편집 진입이 막혀 입력이 사라지지 않고, 연타 저장이 요청을 하나만 만든다
- 1024 미만에서는 표가 보기 전용이되 dirty가 하나라도 있으면(복원한 표 칸 포함) 1차로 그 폭에서 저장된다(R1)
- 현재 차수 복원 줄이 모든 폭에서 보이고 375에서 S18 줄바꿈 규칙을 지킨다
- 견적 원장 화면 전체(04-30 포함)가 네 폭 DOM 감사와 CI=true 전체 게이트를 통과했다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, ui) | `ui/table/save-lock.ts` — `GridAction` · `isGridActionAllowed`(계약 3) · `ui/table/use-editable-width.ts` — `useEditableWidth`(≥1024, 그룹 B 리저브 대장 공용) |
| `ui/table` API(변경) | `TableProps.saveLocked?`(격자 `aria-busy` · 편집 진입 거르기) · `TableColumn.collapseBelow?: 1280 \| 1024`(04-18이 목록에 같은 API를 쓴다) · 숨은 열 키 이동 건너뜀 |
| 변경된 기존 심볼 | `QuoteLedger`(`saveLocked = isExecuting` · `useEditableWidth` 폭 규칙 · 1024 미만 1차 렌더 조건 · 현재 차수 복원 줄 모든 폭 — `keep-all` · 두 3차 `restoreActions` 묶음 · 폰 44px) · `RevenueSection`(`saveLocked` · 폭 판정 prop — 그룹 B 04-16이 유지) · `PeriodField`·`PreEstimateField`(`saveLocked` → `readOnly`) |
| 테스트 | `test/unit/ui/save-lock.test.ts`(신규) · `test/e2e/ledger-save-flow.spec.ts`(저장 중 잠금 describe) · `test/e2e/quote-edit-scope.spec.ts`(k · l · l2 · l3 · l4 · m 추가) |
| 검증 기록 | 견적 원장 화면 독립 DOM 감사(1280 · 1024 · 375 · 1000) |
| 새 스키마·마이그레이션 | 없음 |

## Review Dispositions Ledger

### Round 1 — 엔지니어링 리뷰 r2 신설(2026-09-24 · `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 3 · 4 · 12)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2-02 — 04-30 체커 상한 초과 | HIGH | 04-30 옛 Task 3을 내용 그대로 옮겨 트레이서 우선 두 태스크로 나눔 · truths·backstop·금지 항목·T-04-335·T-04-336·`<probe_fallback>` 세 줄 이동 · `depends_on: ["04-30"]` · 웨이브 15(04-26이 뒤로 — 웨이브 15 이후 +1) |
| 분할안 — 편집기 커밋은 04-30에 | NOTE | `<probe_fallback>` 「편집기 커밋과의 순서」 · Task 1 ②(잠금 때 남은 편집기는 04-30 입구로 커밋) · 수용 기준 e3 초록 |
| PR #38 알려진 문제 — 미저장 복원(복원 줄 쪽) · 폰 편집(QA) | NOTE | objective 「PR #38 알려진 문제」 문단 · Task 2 ② · E2E l2·l3·l4 |
| 지시 12 — UI-SPEC 인용 정본 | NOTE | `<context>` 인용 규칙 줄 · read_first를 「## 적용 규칙」 S3·S4·S8·S18·S19 절 이름 + 현재 행으로 |
| 머지 묶음 결정(HANDOFF decisions[0]) | DECISION | objective 머지 묶음 ② 줄 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 충돌 셀 키보드(DR-25) | 04-30 Round 3 Deferred 그대로 — `ui/table` 충돌 셀을 가진 플랜(T21)이 맡는다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-49-SUMMARY.md` when done
</output>
