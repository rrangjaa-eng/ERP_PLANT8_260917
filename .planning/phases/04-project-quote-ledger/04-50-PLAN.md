---
phase: 04-project-quote-ledger
plan: 50
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:

  - scripts/rollback.sh
  - test/unit/deploy/rollback-sh.test.ts
  - docs/design/DECISIONS.md
  - docs/OPERATIONS.md
  - .github/workflows/deploy.yml
  - test/unit/deploy/workflows.test.ts

autonomous: true
requirements: [PROJ-04, PROJ-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`scripts/rollback.sh`는 트래픽을 옮기기 전에 **스키마 하한**을 정한다 — 스크립트를 돌리는 체크아웃의 `db/migrations/*.sql` 중 `-- rollback-floor:`로 시작하는 줄이 있는 파일을 번호순으로 보고 가장 최신 파일을 더한 커밋(`git log --diff-filter=A`)이 하한이다. 후보 배포의 `APP_GIT_SHA`가 그 하한 커밋을 조상으로 갖지 않으면(`git merge-base --is-ancestor` 거짓) 트래픽을 옮기지 않고 `exit 1`로 거부한다(엔지 r2 E2-04)"
    - "거부 메시지는 stderr 한 덩어리로 하한 마이그레이션 파일 이름 · 하한 커밋 SHA · 후보 리비전과 그 SHA · 복구 절차 한 줄(「전진 수정이 먼저 — 불가피하면 쓰기를 멈춘 뒤 그 마이그레이션의 역 SQL을 적용하고 사람이 트래픽을 옮긴다 · docs/design/DECISIONS.md 04-50 항목」)을 적는다 — `update-traffic` 호출이 없다"
    - "하한 표시가 있는 마이그레이션이 하나도 없으면 `rollback.sh`의 동작은 지금과 같다 — 기존 단위 테스트 여덟 개가 바뀌지 않고 초록이다"
    - "하한이 있을 때 후보에 `APP_GIT_SHA`가 없거나 그 SHA가 로컬 git 이력에 없으면 호환을 증명할 수 없으므로 거부한다 — 메시지가 「최신 main을 fetch한 체크아웃에서 다시 실행」을 말한다(fail-closed). 배포 잡의 체크아웃은 이미 `fetch-depth: 0`이다"
    - "하한은 가장 최신 표시 마이그레이션 하나다 — 0012(묶음 ②)와 0015(묶음 ③)가 둘 다 있으면 0015를 더한 커밋이 하한이고, 0012 뒤·0015 전 커밋을 가리키는 후보는 거부된다"
    - "`scripts/deploy.sh`는 바꾸지 않는다 — 스모크 실패 자동 롤백이 `rollback.sh`를 한 번 부르는 경로가 그대로라, 하한에 막히면 기존 `rollback FAILED …` 줄로 끝나고 트래픽은 새 리비전에 남는다(옛 리비전이 새 스키마 위에서 조용히 틀리게 도는 것보다 전진 수정이 안전하다). `deploy-sh.test.ts` 기존 케이스가 초록이다"
    - "`docs/design/DECISIONS.md`에 04-50 항목 하나가 있다 — 스키마 하한 규칙과 표시 규약(`-- rollback-floor: <이유>`를 파일 첫 줄에, 직전 배포 코드와 호환되지 않는 마이그레이션 — 값 재매핑·컬럼 삭제·타입 변경 — 에만) · 머지 묶음 배포 규칙(Phase 4 남은 플랜은 ② 상태 전환 · ③ 차수·승인 · ④ 목록·리저브 세 묶음을 묶음마다 PR 하나로 Post-build 뒤 main에 머지하고 main에서만 배포 — 사용자 결정 2026-09-23, 엔지 리뷰 R11 대체) · 복구 절차(전진 수정 또는 쓰기 중지 후 역 SQL) · 배포 창 사실 한 줄(migrate Job이 새 리비전보다 먼저 돈다 — 하한 표시 마이그레이션이 있는 묶음은 그 사이 옛 리비전이 새 스키마 위에서 돈다)"
    - "`docs/OPERATIONS.md` §5 롤백이 하한 거부를 말한다 — 「확장-축소 규칙이라 DB는 그대로 둬도 된다」는 취지의 옛 문장이 하한 규칙과 DECISIONS 참조로 바뀐다(문서가 스크립트 동작과 어긋나지 않는다, 300줄 상한 유지)"
    - "배포 워크플로의 스테이징·프로덕션 잡은 main ref에서만 돈다 — 다른 브랜치에서 workflow_dispatch로 띄우면 클라우드 인증 전에 실패한다(ENG-D12 — 엔지 리뷰 R11에서 나온 가드를 머지 묶음 규칙의 강제 장치로 04-31에서 옮김)"
  prohibitions:
    - statement: "스키마 하한보다 오래된 배포로 트래픽을 옮겨서는 안 된다 — 0012 뒤 옛 리비전은 다섯 값 DB에서 완료 행을 잠그지 못하고, 0015 뒤 옛 리비전은 없는 계약 컬럼을 읽는다. 둘 다 오류 없이 조용히 틀린다(엔지 r2 E2-04 — 치명 공백)"
      status: resolved
      verification: test
    - statement: "main이 아닌 ref의 코드가 스테이징·프로덕션에 배포돼서는 안 된다 — 묶음 중간 상태가 배포되면 머지 묶음 규칙(C-26 포함)이 깨지고, 롤백 하한 판정이 전제하는 main 이력 밖 리비전이 생긴다(ENG-D12)"
      status: resolved
      verification: test
  artifacts:
    - path: "scripts/rollback.sh"
      provides: "기존 APP_GIT_SHA 기반 후보 선택 뒤 스키마 하한 판정 — 하한 = 체크아웃에서 가장 최신 `-- rollback-floor:` 마이그레이션을 더한 커밋, 후보가 그 커밋을 조상으로 갖지 않으면 거부"
      contains: "rollback-floor"
    - path: "test/unit/deploy/rollback-sh.test.ts"
      provides: "하한 없음(기존 여덟) · 하한 아래 후보 거부 · 하한 포함 후보 허용 · SHA 없는 후보 거부 · 이력에 없는 SHA 거부 · 표시 둘 중 최신이 하한"
      contains: "rollback-floor"
    - path: "docs/design/DECISIONS.md"
      provides: "04-50 항목 — 스키마 하한 규칙 · 표시 규약 · 머지 묶음 배포 규칙 · 복구 절차 · 배포 창 사실"
      contains: "rollback-floor"
    - path: ".github/workflows/deploy.yml"
      provides: "스테이징·프로덕션 잡 첫 스텝의 main 전용 가드(ENG-D12)"
      contains: "ref-guard"
    - path: "test/unit/deploy/workflows.test.ts"
      provides: "두 잡의 ref-guard 존재 · 인증 전 위치 · env 전달 단언"
      contains: "ref-guard"
  key_links:
    - from: "scripts/rollback.sh"
      to: "db/migrations/"
      via: "`-- rollback-floor:` 줄이 있는 가장 최신 마이그레이션 파일을 더한 커밋을 하한으로 삼는다 — 04-06(0012)·04-41(0015)이 그 줄을 단다"
      pattern: "rollback-floor"
    - from: "scripts/deploy.sh"
      to: "scripts/rollback.sh"
      via: "스모크 실패 자동 롤백(`smoke_failed`)이 같은 스크립트를 부른다 — 하한 거부가 자동 경로에도 걸린다(deploy.sh 무변경)"
      pattern: "rollback.sh"
    - from: "test/unit/deploy/workflows.test.ts"
      to: ".github/workflows/deploy.yml"
      via: "잡 블록을 잘라 ref-guard가 id: auth보다 앞이고 env REF로 refs/heads/main을 비교하는지 본다"
      pattern: "ref-guard"
---

<objective>
**As a** 배포와 사고 대응을 맡은 운영 담당자, **I want to** 되돌리기 스크립트가 지금 DB 스키마와 맞지 않는 옛 배포로는 트래픽을 옮기지 않고, main에 머지된 묶음만 배포되기를, **so that** Phase 4를 세 번에 나눠 배포하는 동안 트래픽 롤백 한 번으로 옛 코드가 새 스키마 위에서 조용히 틀린 값을 만드는 사고가 구조적으로 없다.

머지 묶음: ②/4 상태 전환 — 웨이브 2~18(04-50 → 04-23)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**2026-09-24 엔지니어링 리뷰 r2 신설 플랜(`docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 9 · E2-04 — 사용자 결정으로 자리 이동).** 엔지 r2는 E2-04(`rollback.sh`가 스키마 호환성을 보지 않는 치명 공백)를 04-31 Task 2에 넣으라고 했다. 그 뒤 사용자가 Phase 4를 세 묶음으로 나눠 배포하기로 하면서 「배포가 여러 번이 되므로 E2-04(rollback.sh 스키마 하한 가드)를 첫 묶음 전에 갖춘다」고 정했다(`.continue-here.md` decisions_made). 그래서 이 가드는 **묶음 ②의 첫 웨이브(2)** 에서, 첫 마이그레이션(0011 — 04-10, 웨이브 4)보다 먼저 선다 — 04-10의 `depends_on`에 이 플랜을 더했다. 웨이브를 2로 둔 이유: 웨이브 3의 04-08도 `docs/design/DECISIONS.md`를 고쳐 같은 웨이브에 두면 파일이 겹친다(같은 웨이브 겹침 금지) — 웨이브 2는 04-02(묶음 ①, 실행 완료)뿐이라 이 플랜이 묶음 ②에서 가장 먼저 돌고 뒤 웨이브가 밀리지 않는다. `depends_on: ["04-01"]`은 묶음 ①(PR #38 — 04-01·02·04·05, main 3c1b015)이 머지된 상태 위에서 도는 첫 묶음 ② 플랜이라는 뜻이다(04-01 산출물을 쓰지는 않는다). 새 플랜으로 둔 이유: 웨이브 3의 기존 플랜(04-08 문서·단축키 · 04-32 트랜잭션 규약 · 04-43 요구사항 문구)은 각자 한 관심사이고 배포 스크립트와 무관하다 — 거기에 얹으면 한 플랜 한 관심사가 깨진다.

**04-31 Task 2(`deploy.yml` main 전용 가드 — ENG-D12)도 함께 옮겼다(내용 그대로 — 새 장치 없음).** 기록된 근거: 그 가드의 목적은 「main에 머지된 코드만 배포된다」(옛 C-26 한 PR 배포 규칙의 강제)이고 T-04-317(high)은 「리뷰·머지되지 않은 코드가 스테이징·프로덕션 자격 증명을 받는 경계」다. 묶음 배포에서는 이 규칙이 묶음 ② 첫 배포부터 걸려야 한다 — 다른 브랜치의 workflow_dispatch가 묶음 중간 상태(예: 0012만 있고 전환 화면이 없는 상태 · C-26의 04-17만 있는 상태)를 배포하면 묶음 규칙이 깨지고, 이 플랜의 하한 판정이 전제하는 「배포 리비전 = main 이력의 커밋」도 깨진다. 그래서 E2-04와 같은 자리에 둔다.

Purpose: 트래픽 롤백은 DB를 되돌리지 않는다. 묶음 ②가 0012(상태 다섯 값 재매핑)를, 묶음 ③이 0015(계약 컬럼 삭제)를 싣는 순간 그 이전 배포로의 트래픽 롤백은 안전하지 않다 — 지금 `rollback.sh`는 그 사실을 모르고 가장 최근의 다른 배포를 고른다(테스트·처리·알림 없음, 조용히 틀림 — E2-04 실패 모드 표). 이 플랜이 그 선택을 하한에서 멈추게 하고 규칙을 기록한다.

Output: `rollback.sh` 하한 판정 · 단위 테스트 여섯 케이스 · DECISIONS 항목 하나 · OPERATIONS §5 갱신 · `deploy.yml` 두 잡 ref-guard · `workflows.test.ts` 단언 둘.

**하한 표시를 다는 플랜:** 0012(04-06 Task 1) · 0015(04-41 Task 2) — 각 플랜에 「파일 첫 줄 `-- rollback-floor: <이유>`」 단계와 수용 기준을 넣었다. 0011(설명 컬럼 추가) · 0013(업무 범위 컬럼 추가) · 0014(줄 종류 컬럼 추가) · 0016(리저브 새 표)은 확장 전용이라 표시가 없다 — 단 0016은 04-07의 결정 ① 답이 (b)(원화 금액 컬럼 `bigint` 전환)면 표시를 단다(04-07). 표시가 맞는지는 04-31 Task 3 최종 게이트가 다시 본다.

**실행 규율(CLAUDE.md · hooks):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`를 호출한다(RED → GREEN → 리팩터). 테스트·린트 실패를 쫓기 전에 Skill `systematic-debugging`을 호출하고 코드를 만지기 전에 원인을 찾는다. 커밋 전(문서 커밋 포함)과 「완료」를 말하기 전에 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시하고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(이 플랜 하나). 이 플랜은 실제 GCP·DB에 명령을 보내지 않는다 — 모든 확인은 fakebin `gcloud`와 임시 git 저장소의 단위 테스트다(프로덕션 DB 직접 명령 금지). 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

# 범위 Read:

# docs/designs/plant8-erp-phase4-eng-review-260923-r2.md — E2-04 행 · 실패 모드 표 첫 줄 · 반영 지시 9 · 「이미 있는 것」(rollback.sh의 APP_GIT_SHA 기반 배포 단위 선택)

# .planning/phases/04-project-quote-ledger/.continue-here.md decisions_made 첫 줄(머지 묶음 · E2-04를 첫 묶음 전에) — 읽기만

# docs/designs/plant8-erp-phase4-eng-review-260923.md R11(ENG-D12 — Codex #6)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **PROJ-04(상태 다섯 값 — 0012 배포 안전)** 와 **PROJ-03(계약 금액 컬럼 삭제 — 0015 배포 안전)** 의 운영 부분이다(스펙 없는 프로브 보고의 두 행은 `unresolved` — 아래 가정으로 표면화한다). 수용 기준은 엔지 r2 E2-04 · 실패 모드 표 · ENG-D12에서 끌어왔다.

**계획 단계 가정(표면화):**

- **하한의 출처 = 체크아웃(보수적):** `rollback.sh`는 DB에 접속하지 않는다(gcloud만). 그래서 「적용된 가장 최신 비호환 마이그레이션」을 DB 대신 **스크립트를 돌리는 체크아웃**의 마이그레이션 파일로 본다 — CI 배포 잡에서는 방금 migrate Job이 적용한 바로 그 SHA이고, 수동 실행은 최신 main 체크아웃에서 한다(OPERATIONS §5에 적는다). main이 배포보다 앞서 있으면 하한이 더 높게 잡혀 **더 많이 거부할 수는 있어도 덜 거부하지는 않는다.** 거부된 경우의 사람 경로는 복구 절차(아래)다.
- **표시 규약 = 파일 첫 줄 주석:** 따로 호환표 파일을 두면 마이그레이션과 떨어져 어긋난다. 비호환 마이그레이션을 만드는 플랜이 그 파일에 한 줄을 단다(04-06 · 04-41 — 이번 재계획에서 두 플랜에 단계를 넣었다). Drizzle 마이그레이터와 squawk는 주석 줄을 무시한다.
- **우회 플래그를 만들지 않는다(Simplicity):** 하한 아래로 되돌려야 하는 사고는 전진 수정이 먼저이고, 불가피하면 쓰기를 멈춘 뒤 역 SQL(0012 — 04-06 SUMMARY의 손실 있는 역 SQL · 0015 — 04-41 DECISIONS 항목의 「복구 절차(쓰기 중지)」)을 적용하고 사람이 `gcloud run services update-traffic`로 직접 옮긴다. 스크립트에 「무시하고 되돌리기」 옵션을 두지 않는다 — 그 옵션이 E2-04가 막으려는 바로 그 경로다.
- **배포 창(사용자 확인 대기 — 계획 인계 질문):** `deploy.sh`는 migrate Job을 새 리비전보다 먼저 돌린다(ARCHITECTURE §6). 하한 표시 마이그레이션이 있는 묶음(② — 0012 · ③ — 0015)은 migrate가 끝나고 새 리비전이 100%를 받기까지(분 단위) 옛 리비전(② 배포 땐 PR #38 코드, ③ 배포 땐 묶음 ② 코드)이 새 스키마 위에서 돈다 — 0015 뒤 묶음 ② 코드가 계약 컬럼을 읽는 요청은 오류를 낸다. 옛 D9 전제(「한 번에 배포하면 옛 리비전은 `projects`를 읽지 않는 Phase 3 코드」)는 PR #38 머지와 묶음 결정으로 더는 성립하지 않는다. 이 플랜은 이 사실을 DECISIONS에 적기만 하고 처리(배포 시각 선택 등)는 정하지 않는다 — 기록에 없는 결정이라 묶음 ②·③ /ship 전에 사용자에게 묻는다(T-04-372).

</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 하한 아래 배포로의 롤백이 거부된다(임시 git 저장소 + fakebin gcloud → rollback.sh 하한 판정 → 거부 메시지 · update-traffic 없음) + DECISIONS · OPERATIONS 기록</name>
  <files>scripts/rollback.sh, test/unit/deploy/rollback-sh.test.ts, docs/design/DECISIONS.md, docs/OPERATIONS.md</files>
  <read_first>
    - `scripts/rollback.sh`(전문 121행 — `SERVING` 찾기 · `rev_git_sha` · 같은 APP_GIT_SHA 건너뛰기 · `PREV` 선택 · `update-traffic`)
    - `test/unit/deploy/rollback-sh.test.ts`(전문 158행 — `setupRepo`가 임시 git 저장소를 만들고 `scripts/`·`infra/`를 복사해 한 번 커밋 · `rollback(repoDir, state)`가 fakebin `gcloud`에 `revisions`·`serving`·`revision-git-sha-<rev>`를 준다 · 기존 `it` 여덟)
    - `test/unit/deploy/fakebin/gcloud`(Grep `revision-git-sha`·`update-traffic` — 로그에 남는 모양)
    - `scripts/deploy.sh` 510–540행(`smoke_failed` — 자동 롤백 한 번) · 650–677행(`main` 순서: `run_migrate` → `run_seed` → `deploy_service` → `smoke`)
    - `docs/OPERATIONS.md` §5 롤백(115–122행) · `test/unit/docs-limits.test.ts`(OPERATIONS 300줄 상한)
    - `docs/design/DECISIONS.md` 끝 60행(기록 형식 — `## 날짜 — 제목` · **결정** · **왜** · **버린 대안** · **범위** · `---`) · `test/unit/design-system-docs.test.ts`(DECISIONS 머리글 개수를 세는 describe — 새 항목이 날짜별 개수 단언을 깨지 않는지)
    - `.github/workflows/deploy.yml` 50–60행 · 80–90행(두 잡의 `actions/checkout` `fetch-depth: 0` — 하한 판정에 필요한 이력이 CI에 있다는 근거)
  </read_first>
  <behavior>
    - (하한 없음) 기존 `it` 여덟이 그대로 초록 — 마이그레이션 표시가 없는 저장소에서는 판정이 아무것도 거부하지 않는다
    - (하한 아래 거부) 커밋 C1(기존 init) → C2(`db/migrations/0012_x.sql` 첫 줄 `-- rollback-floor: 상태 재매핑`) → C3. 리비전 `[v2, v1]` · 서빙 v2(SHA C3) · v1(SHA C1) → 종료 코드 1 · stderr에 `0012_x.sql` · C2의 SHA · `v1` · 복구 절차 문구 · 로그에 `update-traffic` 없음
    - (하한 포함 허용) 같은 저장소에서 v1의 SHA가 C2 → 종료 코드 0 · `update-traffic`이 `v1=100`
    - (SHA 없는 후보) 하한이 있고 v1에 `APP_GIT_SHA`가 없다 → 종료 코드 1 · stderr에 `APP_GIT_SHA` 없음과 하한 이름
    - (이력에 없는 SHA) 하한이 있고 v1의 SHA가 저장소에 없는 40자 hex → 종료 코드 1 · stderr에 `git fetch`(최신 main 체크아웃에서 다시)
    - (표시 둘 — 최신이 하한) C2(0012 표시) → C3 → C4(`0015_y.sql` 표시) → C5. 서빙 v3(C5) · v2(C3) → 거부(0015가 하한) · 후보 SHA가 C4면 허용
  </behavior>
  <action>
① **테스트 먼저(RED — Skill `test-driven-development`).** `test/unit/deploy/rollback-sh.test.ts`에 describe `스키마 하한(E2-04)`을 더하고 `behavior`의 다섯 케이스(하한 없음 제외)를 `it`으로 쓴다. 임시 저장소 준비는 기존 `setupRepo` 뒤에 파일을 쓰고 `git add`·`git commit`을 한 번씩 더 부르는 작은 도우미(`commitFile(repoDir, path, content): string` — 새 커밋 SHA를 `git rev-parse HEAD`로 돌려준다)로 한다. `revisionShas`에 그 SHA를 넣는다. 지금 스크립트로 새 `it`들이 실패하는 것(하한 아래 후보로 `update-traffic`이 불린다)을 확인한다. 기존 `it` 여덟은 지우거나 완화하지 않는다.

② **`scripts/rollback.sh`(GREEN).** 지금의 `PREV` 선택(같은 APP_GIT_SHA 건너뛰기 포함)이 끝난 뒤, `update-traffic` **앞에** 하한 판정 한 덩어리를 둔다: (a) `ROOT_DIR`의 `db/migrations/*.sql` 중 `^-- rollback-floor:` 줄이 있는 파일을 이름순으로 모아 마지막 파일을 고른다 — 없으면 판정 없이 지금 동작 그대로. (b) 그 파일을 더한 커밋을 `git -C "$ROOT_DIR" log --diff-filter=A --format=%H -1 -- <파일>`로 구한다. (c) 후보(`PREV`)의 `APP_GIT_SHA`가 비었거나, `git -C "$ROOT_DIR" cat-file -e "<sha>^{commit}"`가 실패하거나, `git -C "$ROOT_DIR" merge-base --is-ancestor <하한 커밋> <sha>`가 거짓이면 stderr에 `truths` 두 번째 줄의 내용(하한 파일 · 하한 커밋 · 후보 리비전과 SHA · 이유 — SHA 없음 / 이력에 없음(`git fetch` 뒤 최신 main 체크아웃에서 다시) / 하한보다 오래됨 · 복구 절차 한 줄과 DECISIONS 04-50 참조)을 찍고 `exit 1`. 셸 규약은 파일의 기존 모양(`set -euo pipefail` · 인용된 변수)을 따른다 — 후보 SHA는 gcloud 응답에서 온 값이라 명령 인자로만 쓰고 `eval`하지 않는다. 머리 주석에 두 줄을 더한다: 「스키마 하한(E2-04): 체크아웃의 가장 최신 `-- rollback-floor:` 마이그레이션을 더한 커밋을 조상으로 갖지 않는 배포로는 되돌리지 않는다 — 트래픽 롤백은 DB를 되돌리지 않는다」 · 「수동 실행은 최신 main 체크아웃에서」. 우회 옵션을 만들지 않는다. `scripts/deploy.sh`는 고치지 않는다.

③ **결정 기록(`docs/design/DECISIONS.md` — 스크립트와 같은 커밋).** 04-01 기록과 같은 형식으로 끝에 한 항목을 더한다. 머리글 `## {실행일 KST} — Phase 4(04-50) 롤백 스키마 하한 · 머지 묶음 배포 규칙 (엔지 리뷰 r2 E2-04 · ENG-D12 · 사용자 결정 2026-09-23)`. **결정**: (1) 직전 배포 코드와 호환되지 않는 마이그레이션(값 재매핑 · 컬럼 삭제 · 타입 변경)은 파일 첫 줄에 `-- rollback-floor: <이유>`를 단다 — Phase 4에서는 0012(04-06 상태 재매핑)와 0015(04-41 계약 컬럼 삭제). (2) `scripts/rollback.sh`는 체크아웃에서 가장 최신 표시 파일을 더한 커밋을 스키마 하한으로 삼고, 그 커밋을 조상으로 갖지 않는 배포(APP_GIT_SHA)로는 트래픽을 옮기지 않는다 — APP_GIT_SHA가 없거나 로컬 이력에 없으면 거부한다. (3) Phase 4의 남은 플랜은 세 머지 묶음(② 상태 전환 04-50~04-23 · ③ 차수·승인 04-14~04-41 · ④ 목록·리저브 04-17~04-31)으로 묶음마다 PR 하나로 Post-build 뒤 main에 머지하고, 배포는 main에서만 한다(`deploy.yml` ref-guard — ENG-D12) — 묶음 중간 커밋·중간 플랜 상태를 어느 환경에도 배포하지 않는다(엔지 리뷰 R11 「Phase 4 한 PR」 대체). **왜**: Cloud Run 트래픽 롤백은 DB를 되돌리지 않는다 — 0012 뒤 옛 리비전은 다섯 값 DB에서 완료 행을 잠그지 못하고, 0015 뒤 옛 리비전은 없는 컬럼을 읽는다. 배포가 묶음마다 세 번이 되어 그런 후보가 실제로 생긴다. 배포 창 사실 한 줄: `deploy.sh`는 migrate Job을 새 리비전보다 먼저 돌리므로, 하한 표시 마이그레이션이 있는 묶음은 migrate 뒤 새 리비전이 100%가 될 때까지 옛 리비전이 새 스키마 위에서 돈다. **버린 대안**: 따로 두는 호환표 파일(마이그레이션과 떨어져 어긋난다) · DB 적용 이력 조회(`rollback.sh`는 DB에 접속하지 않는다) · 하한 무시 옵션(E2-04가 막는 경로 그 자체). **되돌리는 법(복구 절차)**: 하한 아래가 필요한 사고는 ① 전진 수정(새 커밋 → main → 배포)이 먼저다 ② 불가피하면 쓰기를 멈춘 상태에서 해당 마이그레이션의 역 SQL(0012 — 04-06 SUMMARY · 0015 — 04-41 항목)을 적용한 뒤 사람이 `gcloud run services update-traffic`로 옮긴다. **범위**: `scripts/rollback.sh` · 배포 절차 · 표시를 다는 마이그레이션. 이 항목은 SYSTEM.md를 바꾸지 않는다.

④ **`docs/OPERATIONS.md` §5.** 「DB는 확장-축소 규칙(컬럼 추가만)이라 DB 쪽은 손대지 않아도 된다」는 취지의 지금 문장(§5 둘째 문장)을 지금 사실로 고친다: 롤백 후보가 스키마 하한(가장 최신 `-- rollback-floor:` 마이그레이션을 더한 커밋)보다 오래되면 스크립트가 거부하고 복구는 DECISIONS 04-50 항목의 절차를 따른다 · 수동 실행은 최신 main 체크아웃에서 · 데이터 손상은 여전히 백업 복원(OPS-03). 다른 절은 건드리지 않는다(300줄 상한 — `docs-limits` 테스트).
  </action>
  <verify>
    <automated>bash -n scripts/rollback.sh &amp;&amp; pnpm vitest run --project unit test/unit/deploy/rollback-sh.test.ts test/unit/deploy/deploy-sh.test.ts</automated>
    <fails_when>`bash -n`이 문법 오류를 출력하거나, 종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 하한 아래 후보로 `update-traffic`이 불렸거나, 하한 포함 후보가 거부됐거나, SHA 없음·이력 없음이 통과했거나, 표시 둘 중 최신이 하한이 아니거나, 기존 롤백·배포 케이스가 깨졌다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — OPERATIONS가 300줄 상한을 넘었거나, 새 DECISIONS 머리글이 다른 플랜의 머리글 개수 단언을 깨뜨렸다(깨지면 ENG-D11 — 정규식을 이 기록을 세지 않게 고치고 회귀 케이스 + SUMMARY 편차, 이 기록을 지우지 않는다)</fails_when>
    <automated>grep -cE '^## 20[0-9]{2}-[0-9]{2}-[0-9]{2} — Phase 4\(04-50\)' docs/design/DECISIONS.md</automated>
    <fails_when>출력이 `1`이 아니다 — 04-50 결정 기록이 없거나 두 번 들어갔다</fails_when>
  </verify>
  <acceptance_criteria>
    - `rollback-sh.test.ts`에 describe `스키마 하한(E2-04)`과 `behavior`의 다섯 새 케이스가 있고, 기존 `it` 개수가 줄지 않았으며 전부 초록이다
    - `scripts/rollback.sh`에 `rollback-floor` · `merge-base --is-ancestor` · `cat-file -e`가 있고 `update-traffic` 호출이 하한 판정 **뒤** 한 곳이다(Grep 줄 번호 비교) · `eval`이 없다
    - `scripts/deploy.sh`의 diff가 0줄이다(`git diff --stat HEAD -- scripts/deploy.sh` 출력 없음 — 이 태스크 커밋 전 작업 트리 기준)
    - DECISIONS에 04-50 머리글이 정확히 한 줄이고 본문에 `rollback-floor` · 머지 묶음 셋(②·③·④) · 복구 절차(전진 수정 · 쓰기 중지 후 역 SQL) · 배포 창 한 줄 · ENG-D12 참조가 있다 · 스크립트와 같은 커밋이다
    - OPERATIONS §5에 「확장-축소 규칙이라 되돌릴 필요가 없다」 문장이 없고(`grep -c "되돌릴 필요가 없다" docs/OPERATIONS.md` = 0) 하한 거부와 DECISIONS 04-50 참조가 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>하한 표시 마이그레이션이 있는 저장소에서 rollback.sh가 하한 아래 배포로는 트래픽을 옮기지 않고 이유와 복구 절차를 말하며, 표시가 없으면 지금과 똑같이 동작하고, 그 규칙과 머지 묶음 배포 규칙이 DECISIONS·OPERATIONS에 기록됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 배포는 main에서만 — deploy.yml의 스테이징·프로덕션 잡이 main이 아닌 ref를 인증 전에 거부한다(ENG-D12 — 04-31 Task 2에서 내용 그대로 옮김)</name>
  <files>.github/workflows/deploy.yml, test/unit/deploy/workflows.test.ts</files>
  <read_first>
    - `.github/workflows/deploy.yml`(전문 115행 — 머리 주석 · `on:`의 push `branches: [main]`과 `workflow_dispatch` 입력 · 잡 `ci`·`staging`·`production` · production `guard` 스텝이 입력을 env로 넘기는 방식)
    - `test/unit/deploy/workflows.test.ts` 1–130행(YAML 파서 없는 텍스트 메타 검사 — 잡 블록을 `indexOf("\n  staging:")`·`indexOf("\n  production:")`로 잘라 본다 · T-1-32 env 전달 단언)
    - `docs/designs/plant8-erp-phase4-eng-review-260923.md` R11(ENG-D12 — Codex #6)
  </read_first>
  <behavior>
    - `staging` 블록과 `production` 블록 각각에 `id: ref-guard` 스텝이 정확히 하나 있고, 블록 안에서 그 위치가 `id: auth`보다 앞이다(클라우드 자격 증명을 받기 전에 멈춘다)
    - 두 ref-guard 스텝 조각(다음 `- ` 스텝 시작까지)이 env로 `REF`에 `github.ref`를 받고, 본문이 `"$REF"`를 `refs/heads/main`과 비교해 다르면 `exit 1`한다 — ref 값은 env 줄로만 들어온다(T-1-32 셸 인젝션 규약)
    - 기존 단언(push `branches: [main]` · paths 재포함 순서 · production `if:` 줄 · guard 스텝 · deploy.sh 인자)이 그대로 초록
  </behavior>
  <action>
① **테스트 먼저(RED).** `test/unit/deploy/workflows.test.ts`의 `describe("deploy.yml")`에 `behavior` 앞 두 줄을 `it` 둘로 더한다 — 기존 방식대로 잡 블록을 문자열로 잘라, ref-guard의 문자열 위치가 `id: auth`보다 앞인지와 ref-guard 조각에 env `REF` 줄 · `refs/heads/main` · `exit 1`이 있는지, 조각의 `run:` 이후 본문이 ref를 `$REF`로만 읽는지(표현식 보간 `${{`가 `run:` 이후에 없음)를 본다. 지금 파일로 두 `it`이 실패하는 것을 확인한다. 기존 `it`은 지우거나 완화하지 않는다.

② **`deploy.yml`(GREEN — ENG-D12).** `staging`·`production` 두 잡의 첫 스텝(`actions/checkout` 앞)으로 `- id: ref-guard` · `shell: bash` · env `REF`(값 `github.ref` 표현식) · `run:` 블록을 둔다 — `REF`가 `refs/heads/main`이 아니면 `::error::` 한 줄(「main이 아닌 ref는 배포하지 않습니다(ENG-D12) — main에서 다시 실행하세요」와 받은 ref)을 찍고 `exit 1`. push 트리거는 이미 `branches: [main]`이라 이 가드가 실제로 막는 것은 다른 브랜치에서 띄운 workflow_dispatch다(머지 묶음 규칙 — main에 머지된 묶음만 배포된다, DECISIONS 04-50 항목). 잡의 `if:` 조건은 바꾸지 않는다 — 조건으로 거르면 실패가 아니라 「skipped」로 보여 거부가 눈에 띄지 않는다. 머리 주석에 한 줄을 더한다: 「스테이징·프로덕션 잡은 main ref에서만 돈다 — 다른 브랜치의 workflow_dispatch는 ref-guard 스텝에서 인증 전에 실패한다(ENG-D12 · 머지 묶음 규칙)」. `ci` 잡·`on:` 트리거·다른 스텝은 건드리지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/workflows.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 스테이징·프로덕션 잡 중 하나에 main 전용 가드가 없거나 인증 스텝보다 뒤에 있거나, ref가 `run:` 본문에 직접 보간됐거나, 기존 배포 워크플로 단언이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `deploy.yml`의 `staging`·`production` 블록에 `id: ref-guard`가 각각 한 번 있고 `id: auth`보다 앞이다(단위 단언 둘)
    - ref-guard가 env `REF`로 ref를 받고 `refs/heads/main`이 아니면 `exit 1`한다 — `run:` 본문은 `$REF`만 읽는다(단위 단언)
    - `workflows.test.ts`의 기존 `it` 개수가 줄지 않았고 전부 초록이다
    - `deploy.yml`의 바뀐 줄이 머리 주석 한 줄과 두 ref-guard 스텝뿐이다(`on:` 블록 · `ci` 잡 · 기존 스텝 무변경 — diff 검토로 확인해 SUMMARY에 적는다)
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>main이 아닌 브랜치에서 workflow_dispatch로 배포를 띄우면 스테이징·프로덕션 잡이 클라우드 인증 전에 실패하고, 이 규칙이 단위 테스트로 고정됐다 — 묶음 ② 첫 배포 전에</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 트래픽 롤백 → 새 스키마 DB | 롤백은 코드만 옛 리비전으로 옮기고 DB는 그대로 둔다 — 옛 코드와 새 스키마가 만나는 경계 |
| gcloud 응답(리비전 env `APP_GIT_SHA`) → 롤백 스크립트 | 스크립트가 신뢰하는 입력 — git 명령의 인자로만 쓰인다 |
| GitHub Actions 실행 ref → 클라우드 배포 | workflow_dispatch는 어느 브랜치에서든 띄울 수 있다 — 리뷰·머지되지 않은 코드가 스테이징·프로덕션 자격 증명을 받는 경계 |
| migrate Job → 새 리비전 100% 사이(배포 창) | 새 스키마가 먼저 적용되고 옛 리비전이 잠시 계속 서빙한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-371 | Tampering | 스키마와 맞지 않는 옛 리비전으로의 트래픽 롤백(엔지 r2 E2-04 — 치명 공백) | high | mitigate | `rollback.sh`가 체크아웃의 가장 최신 `-- rollback-floor:` 마이그레이션을 더한 커밋을 하한으로 삼아, 그 커밋을 조상으로 갖지 않거나 SHA가 없거나 이력에 없는 후보를 `update-traffic` 전에 거부한다(fail-closed). 자동 경로(`deploy.sh` 스모크 실패)도 같은 스크립트를 부른다. 단위 테스트 다섯 케이스가 고정하고, 표시 위치는 04-31 최종 게이트가 다시 본다 |
| T-04-317 | Elevation of Privilege | `deploy.yml` 스테이징·프로덕션 잡 | high | mitigate | (04-31에서 옮김 · ENG-D12 — Codex #6) 두 잡의 첫 스텝 ref-guard가 `refs/heads/main`이 아닌 ref에서 WIF 인증 전에 `exit 1`한다. ref는 env로만 받는다(셸 인젝션 규약 T-1-32). 단위 테스트가 두 잡의 가드 존재 · 인증 전 위치 · env 전달을 단언한다 |
| T-04-372 | Denial of Service | 배포 창 — migrate 뒤 새 리비전 100% 전(묶음 ② 0012 · 묶음 ③ 0015) | medium | accept | **사용자 확인 대기(계획 인계 질문).** 창은 `deploy.sh`의 migrate → seed → deploy_service 사이(분 단위)이고, 그동안 옛 리비전이 새 스키마 위에서 돈다(0015 뒤 묶음 ② 코드의 계약 컬럼 읽기는 오류). 옛 D9 전제(옛 리비전 = `projects`를 읽지 않는 Phase 3 코드)는 PR #38 머지로 사라졌다. 이 플랜은 사실을 DECISIONS에 적고, 처리(프로덕션 승격 시각 선택 등 — 프로덕션은 workflow_dispatch로만 승격되어 사람이 시각을 고른다)는 묶음 ②·③ /ship 전에 사용자가 정한다. 롤백 쪽 위험은 T-04-371이 막는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- `bash -n scripts/rollback.sh` 통과 · 단위 `rollback-sh.test.ts`(새 다섯 + 기존 여덟) · `deploy-sh.test.ts` · `workflows.test.ts` · `docs-limits.test.ts` · `design-system-docs.test.ts` 초록
- DECISIONS 04-50 항목 한 줄 · OPERATIONS §5 갱신
- `scripts/deploy.sh` 무변경
- `package.json` 의존성 객체·`pnpm-lock.yaml`이 d6b41cf와 같다(원문 diff가 아니라 의존성 객체 비교)

</verification>

<success_criteria>

- 하한 아래 배포로의 트래픽 롤백이 수동·자동 경로 모두에서 거부되고, 그 이유와 복구 절차가 메시지·DECISIONS·OPERATIONS에 있다
- 하한 표시가 없는 동안(0011 전) 롤백 동작은 지금과 같다
- main이 아닌 ref에서 스테이징·프로덕션 배포가 시작되지 않는다 — 묶음 ② 첫 배포 전에 두 가드가 선다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 스크립트(변경) | `scripts/rollback.sh` — 스키마 하한 판정(`-- rollback-floor:` 최신 파일을 더한 커밋 · `merge-base --is-ancestor` · SHA 없음·이력 없음 거부 · 복구 절차 메시지) |
| 규약(신규) | 마이그레이션 첫 줄 표시 `-- rollback-floor: <이유>` — 04-06(0012) · 04-41(0015)이 단다(04-07은 결정 ① (b)일 때 0016) |
| 워크플로(변경) | `.github/workflows/deploy.yml` — 스테이징·프로덕션 잡 첫 스텝 `ref-guard`(main 전용, ENG-D12 — 04-31에서 옮김) |
| 테스트 | `test/unit/deploy/rollback-sh.test.ts`(describe `스키마 하한(E2-04)` · 도우미 `commitFile`) · `test/unit/deploy/workflows.test.ts`(ref-guard 두 단언) |
| 문서 | `docs/design/DECISIONS.md` 04-50 항목(하한 규칙 · 표시 규약 · 머지 묶음 배포 규칙 · 복구 절차 · 배포 창 사실) · `docs/OPERATIONS.md` §5 |
| 위협(신규) | T-04-371(하한 아래 롤백) · T-04-372(배포 창 — 사용자 확인 대기) · T-04-317(04-31에서 옮김) |
| 새 스키마·마이그레이션·설정 키 | 없음 |

## Review Dispositions Ledger

### Round 1 — 엔지니어링 리뷰 r2 신설(2026-09-24 · `docs/designs/plant8-erp-phase4-eng-review-260923-r2.md` 반영 지시 9 · 사용자 결정 「E2-04를 첫 묶음 전에」)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2-04 — `rollback.sh`가 스키마 호환성을 보지 않음(치명 공백) | CRITICAL | Task 1: 하한 판정 · 단위 다섯 케이스 · DECISIONS(「하한보다 오래된 리비전으로 롤백 거부 — 하한 = 가장 최신 비호환 마이그레이션을 더한 커밋 · 복구는 전진 수정 또는 쓰기 중지 후 역 SQL」 — 지시 9의 「Phase 4 머지 이전 SHA」를 묶음 배포에 맞게 일반화) · OPERATIONS §5 · T-04-371 |
| 지시 9의 자리(04-31 Task 2) → 첫 묶음 전(사용자 결정 `.continue-here.md` decisions_made) | DECISION | 새 플랜 04-50(웨이브 2 — 묶음 ② 첫 웨이브, 04-08과 `DECISIONS.md`가 겹쳐 웨이브 3에 두지 않음) · 04-10(첫 마이그레이션 0011)의 `depends_on`에 04-50 · 기존 웨이브 3 플랜에 얹지 않은 이유는 objective |
| ENG-D12 `deploy.yml` main 전용 가드의 자리 | DECISION | 04-31 Task 2를 내용 그대로 Task 2로 옮김 — 기록된 근거(「main에 머지된 코드만 배포」 · T-04-317 high)가 묶음 ② 첫 배포부터 필요하고, 하한 판정이 main 이력을 전제한다(objective). 새 장치 없음 |
| 배포 창 — 옛 리비전이 새 스키마 위에서 도는 migrate 뒤 구간 | NOTE | 사용자 확인 대기 — T-04-372 accept(인계 질문) · DECISIONS에 사실만 · 묶음 ②·③ 마지막 플랜 objective에 /ship 전 확인 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 역 SQL 리허설 절차(쓰기 중지 상태) | E2-04 원문 「리허설한 절차」 — 리허설 실행은 이 플랜 범위가 아니다(실제 DB 명령 금지). 역 SQL 문안은 04-06 SUMMARY · 04-41 DECISIONS가 남긴다 |

<output>
Create `.planning/phases/04-project-quote-ledger/04-50-SUMMARY.md` when done
</output>
