---
phase: 04-project-quote-ledger
plan: 51
type: execute
wave: 29
depends_on: ["04-07"]
files_modified:

  - domain/document-numbering/index.ts
  - repositories/document-counters.ts
  - app/(app)/admin/settings/actions.ts
  - domain/settings/keys.ts
  - test/integration/document-numbering.test.ts
  - test/integration/settings.test.ts
  - docs/design/DECISIONS.md

autonomous: false
requirements: [ADMN-09]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "결정 ②(문서 번호 순번 시작값을 이미 매긴 번호보다 낮출 때의 번호 충돌)의 사용자 답이 Task 1에서 기록되고 Task 2가 그 답대로 실행된다 — 어느 답이든 올해 발급된 번호와 같은 번호가 다시 매겨지지 않는다(`UNIQUE(format_key, number)` 위반으로 등록이 실패하는 경로가 없다 — 통합 테스트) · 답과 근거가 DECISIONS에 한 항목으로 남는다(HANDOFF open_questions[1] · PR #38 알려진 문제 「채번 시작값을 낮추면 번호 충돌」)"
    - "이미 매긴 프로젝트 번호는 순번 시작값을 어떻게 바꿔도 그대로다 — 설정 변경이 발급된 번호를 다시 쓰지 않는다(ADMN-09 · CONTEXT 「서식을 바꿔도 매긴 번호는 유지」)"
    - "두 연결이 동시에 프로젝트를 등록해도 서로 다른 번호다 — 04-01의 동시 발급 단언이 결정 ② 구현 뒤에도 초록이다(ROADMAP 기준 1 · Issue 10)"
  artifacts:
    - path: "test/integration/document-numbering.test.ts"
      provides: "describe `순번 시작값 낮추기(결정 ②)` — 답에 맞는 케이스 + 공통 동시성 케이스"
      contains: "순번 시작값 낮추기"
    - path: "docs/design/DECISIONS.md"
      provides: "결정 ② 항목 한 줄 — 답 · 날짜 · 근거 · 이미 매긴 번호 불변(ADMN-09)"
      contains: "결정 ②"
  key_links:
    - from: "domain/document-numbering/index.ts"
      to: "repositories/document-counters.ts"
      via: "번호 발급이 카운터 행 잠금(`allocateNumber` — `UPDATE … RETURNING`) 한 곳을 지난다 — (b)의 보정도 이 잠금 안이고, (a)의 「올해 이미 매긴 최대 표시 순번」도 같은 카운터 행에서 온다"
      pattern: "allocateNumber"
    - from: "test/integration/document-numbering.test.ts"
      to: "domain/document-numbering/index.ts"
      via: "결정 ② 케이스가 실제 DB로 발급·설정 저장을 불러 겹침 없음을 단언한다"
      pattern: "순번 시작값 낮추기"
---

<objective>
**As a** 시스템관리자, **I want to** 문서 번호 순번 시작값을 연중에 이미 매긴 번호보다 낮게 바꾸려 할 때 새 프로젝트 번호가 이미 발급된 번호와 겹치지 않기를, **so that** 설정 하나를 바꿨다고 어느 날 프로젝트 등록이 번호 중복으로 실패하는 일이 없다.

머지 묶음: ④/4 목록·리저브 — 웨이브 25~30(04-17 → 04-31)을 PR 하나로 Post-build 뒤 main에 머지·배포한다(사용자 결정 2026-09-23 · HANDOFF decisions[0] — 엔지 리뷰 R11 「Phase 4 한 PR」 대체). 묶음 중간 상태는 어느 환경에도 배포하지 않는다.

**실행 규율 보강(2026-09-24 재계획 — CLAUDE.md · hooks 3bed7b1 · 세션 B 교훈):** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋(문서 포함)과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적고, 서브에이전트의 「RED 확인」 보고는 수정만 stash해 직접 재현해 다시 확인한다. 한 세션에 gsd-executor 한 번(플랜 하나 — 체크포인트 이어가기도 새 세션). 통합 테스트·E2E·`next build`는 같은 작업 트리·`erp_test` DB를 쓰는 다른 에이전트와 동시에 돌리지 않는다(같은 웨이브의 04-42와 번갈아 돌린다). 완료 판정은 로컬 dev가 아니라 `CI=true`다.

**2026-09-24 체커 revision — 04-42에서 분할(scope_sanity: 04-42가 `<task>` 다섯).** 결정 ②의 `checkpoint:decision`과 답별 구현을 04-42(옛 Task 3·4)에서 이 플랜으로 **내용 그대로** 옮겼다 — 질문 · 두 옵션 · 결정의 무게 · 답별 구현 · 검증은 바뀌지 않았고, 계획은 답을 고르지 않는다. PR #38 알려진 문제 「채번 시작값을 낮추면 번호 충돌」과 HANDOFF open_questions[1]은 기록에 답이 없다 — 04-05(실행 완료)가 순번 시작값을 「항상 1부터 도는 카운터 위의 표시 오프셋」으로 구현해, 연중에 시작값을 이미 매긴 번호보다 낮추면 다음 번호가 이미 매긴 번호와 겹쳐 `UNIQUE(format_key, number)` 위반으로 등록이 실패할 수 있다(설정 설명은 「연도가 바뀌어 순번이 다시 시작할 때의 첫 값」이다). **자리(웨이브 29 — 04-42와 같은 웨이브):** 채번 코드(`domain/document-numbering` · 설정 `document_number.project.*`)를 가진 미실행 플랜이 없다(04-05는 실행 완료 — 동결). 이 플랜이 고치는 `docs/design/DECISIONS.md`를 04-07(웨이브 28)이 먼저 고치므로 `depends_on`은 04-07이고, 04-42와는 파일이 겹치지 않는다. 이 플랜에 기대는 것은 04-31(어차피 사람 확인에서 멈춘다) 하나뿐이라 답이 늦어도 멈추는 것은 페이즈 마지막 플랜뿐이고, 묶음 ②·③과 묶음 ④의 앞 플랜 · 04-42는 이 답을 기다리지 않는다. 결정의 무게: `costly`(두 답 모두 설정 저장·채번 한 곳의 코드이고 데이터 마이그레이션이 없다).

Purpose: ROADMAP 기준 1 「번호는 설정된 서식(… 순번 범위)으로 자동 부여 · `UNIQUE(format_key, number)`가 중복을 막는다」가 설정 변경 뒤에도 등록 실패로 드러나지 않게 한다(ADMN-09).

Output: 결정 ② 사용자 답 · 답에 맞는 설정 검증 또는 카운터 보정 · 통합 테스트 describe 하나 · DECISIONS 항목 한 줄.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 명시한다. 새 의존성 0개. 화면 배치 변경이 없어 독립 DOM 감사 대상이 아니다 — (a)의 새 문구는 설정 화면의 기존 필드 오류 자리를 쓴다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/04-project-quote-ledger/04-05-SUMMARY.md
@CLAUDE.md

# 범위 Read: `.planning/HANDOFF.json` open_questions 둘째 줄(읽기만) · `domain/document-numbering/index.ts`(전문 106행) · `repositories/document-counters.ts`(전문 67행) · `app/(app)/admin/settings/actions.ts`(전문 58행) · `domain/settings/keys.ts`의 `DOCUMENT_NUMBER_PROJECT_SEQ_START`(Grep)

</context>

<probe_fallback>
이 플랜이 떠맡는 미분류 행은 **ADMN-09(문서 번호 서식 설정)의 순번 시작값 경계**다. 수용 기준은 ROADMAP 기준 1(`UNIQUE(format_key, number)` · 결번 허용) · CONTEXT Claude's Discretion 「서식을 바꿔도 매긴 번호는 유지, 연도 전환 시 순번 리셋은 `document_counters.period`가 담당」 · 04-05-SUMMARY(표시 오프셋 vs 저장 카운터 분리)에서 끌어와 위 truths에 적었다. 답에 따라 갈리는 동작은 답별 behavior와 수용 기준으로 둘 다 적었다 — 계획이 답을 고르지 않는다.

**`.planning` 편집:** 없다. 결정 기록은 `docs/design/DECISIONS.md` 한 곳이다.
</probe_fallback>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 결정 ② — 문서 번호 순번 시작값(`document_number.project.seq_start`)을 이미 매긴 번호보다 낮추면 생기는 번호 충돌: 설정 검증(현재 최대 이하 거부) vs 카운터 보정(HANDOFF open_questions[1] · PR #38 알려진 문제)</name>
  <read_first>
    - `.planning/HANDOFF.json` open_questions 둘째 줄(읽기만)
    - `domain/document-numbering/index.ts`(04-01·04-05 — `period` = 서기 연도 · 표시 순번 = 카운터 + 시작값 − 1)
    - `domain/settings/keys.ts`의 `DOCUMENT_NUMBER_PROJECT_SEQ_START`(스키마 `int().min(0)` · 설명 「연도가 바뀌어 순번이 다시 시작할 때의 첫 값」)
    - `.planning/phases/04-project-quote-ledger/04-05-SUMMARY.md` 55·63행(표시 오프셋 vs 저장 카운터 분리 결정)
  </read_first>
  <decision>연중에 순번 시작값을 이미 매긴 번호보다 낮추는 설정 변경을 어떻게 다룰지</decision>
  <context>지금 표시 순번 = 그해 카운터 + 시작값 − 1이다(카운터는 해마다 1부터). 예: 시작값 100으로 100~102를 매긴 뒤 시작값을 1로 바꾸면 그해 다음 번호는 4 · 5 …이고, 카운터가 100에 닿는 날 이미 매긴 100과 겹쳐 등록이 실패한다. 이미 매긴 번호는 바꾸지 않는다(ADMN-09). 두 답 모두 설정 저장·채번 한 곳의 코드이고 데이터 마이그레이션이 없다(costly).</context>
  <options>
    <option id="option-a">
      <name>(a) 설정 검증 — 올해 이미 매긴 최대 표시 순번 이하로 내리는 값은 저장을 거부</name>
      <pros>규칙이 단순하다 — 저장된 시작값이 늘 실제로 쓰이고, 이미 매긴 번호와 겹칠 수 없다 · 채번 코드는 그대로</pros>
      <cons>연중에 시작값을 낮출 수 없다(다음 해에 적용하려면 해가 바뀐 뒤 바꿔야 한다) · 관리자 설정 화면에 새 오류 문구 하나 — 제안: `순번 시작값이 이미 매긴 번호 {최대}와 겹칩니다 · {최대 + 1} 이상으로 적어 주세요`(§7-2 「원인 · 다음 행동」 · 관리자 화면)</cons>
    </option>
    <option id="option-b">
      <name>(b) 카운터 보정 — 낮춰도 저장하고, 채번이 올해 이미 매긴 최대 표시 순번 뒤에서 이어 간다</name>
      <pros>관리자가 언제든 값을 바꿀 수 있고, 낮춘 값은 겹치지 않는 범위에서 이어지다가 다음 해부터 그대로 적용된다 — 설정 설명(「순번이 다시 시작할 때의 첫 값」)과 맞는다 · 새 문구 없음</pros>
      <cons>연중에 낮춘 값이 올해 번호에는 바로 보이지 않는다(겹침을 피해 이어 간다) · 채번 한 곳에 「올해 최대 표시 순번」 조회(행 잠금 안)가 더해진다</cons>
    </option>
  </options>
  <resume-signal>「(a)」 또는 「(b)」로 답한다((a)의 제안 문구를 바꾸려면 원하는 문구를 함께 적는다)</resume-signal>
  <acceptance_criteria>
    - 사용자 답(「(a)」/「(b)」와 근거 한 줄, (a)면 확정 문구)이 이 플랜 SUMMARY와 Task 2의 DECISIONS 항목에 그대로 적혀 있다 — 계획·실행자가 답을 고르지 않았다(자동 승인 없음)
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 2: 트레이서 — 결정 ② 답대로 순번 시작값을 낮춰도 이미 매긴 번호와 겹치지 않는다(설정 저장 또는 채번 → 카운터 행 잠금 → DB → 통합 테스트) + DECISIONS + 전체 게이트</name>
  <files>domain/document-numbering/index.ts, repositories/document-counters.ts, app/(app)/admin/settings/actions.ts, domain/settings/keys.ts, test/integration/document-numbering.test.ts, test/integration/settings.test.ts, docs/design/DECISIONS.md</files>
  <reversibility rating="costly">설정 저장·채번 한 곳의 규칙이다 — 다른 답으로 바꾸려면 그 한 곳과 통합 테스트를 다시 쓴다(데이터 마이그레이션 없음).</reversibility>
  <precondition>Task 1의 사용자 답(「(a)」/「(b)」)이 있어야 한다.</precondition>
  <read_first>
    - `domain/document-numbering/index.ts`(전문) · `repositories/document-counters.ts`(전문 — `(counter_key, period)` 행 잠금 `UPDATE … RETURNING`)
    - `domain/settings/registry.ts`의 `setSettingValue`(94행 근처) · `app/(app)/admin/settings/actions.ts`(전문 — 설정 저장 액션과 필드 오류 모양) · `app/(app)/admin/settings/settings-form-client.tsx`(오류 표시 자리 — 04-09 뒤)
    - `test/integration/document-numbering.test.ts`(04-01·04-05 산출물 — 준비·동시 발급 형태) · `test/integration/settings.test.ts`
    - `docs/design/DECISIONS.md` 끝 30행(기록 형식 — 04-07이 결정 ① 항목을 막 더했다)
  </read_first>
  <behavior>
    - 공통 준비: 시작값 100으로 올해 프로젝트 셋을 등록(표시 순번 100 · 101 · 102)
    - **(a)면:** 시작값을 50으로 저장 → 거부(Task 1에서 확정한 문구의 필드 오류), 설정 값 100 그대로 · 102로 저장 → 거부 · 103으로 저장 → 통과, 다음 등록 번호가 이미 매긴 번호와 겹치지 않는다
    - **(b)면:** 시작값을 1로 저장 → 통과 · 다음 등록 번호의 표시 순번이 103(이미 매긴 최대 + 1)이고 그 뒤로 104 · 105 … 겹침 없이 이어진다 · 다음 해(`now` 주입) 첫 등록은 표시 순번 1이다
    - 공통: 두 연결이 동시에 등록해도 서로 다른 번호다(04-01 동시성 단언 유지)
  </behavior>
  <action>
① **테스트 먼저(RED — Skill `test-driven-development`).** `test/integration/document-numbering.test.ts`에 describe `순번 시작값 낮추기(결정 ②)`로 답에 맞는 `behavior` 줄과 공통 줄을 쓴다(설정 저장은 관리자 경로 — (a)면 `app/(app)/admin/settings/actions.ts`의 저장 액션 또는 그 도메인 함수를 부른다). 지금 코드로 (a)의 거부가 없거나 (b)의 다음 번호가 겹치는 것을 확인한다.

② **(a)면 — 설정 검증.** `document_number.project.seq_start` 저장 때만, 새 값이 올해(`kstYear(now)`) 프로젝트 카운터로 이미 매긴 최대 표시 순번 이하이면 저장하지 않고 Task 1에서 확정한 문구를 그 칸의 필드 오류로 돌려준다 — 검사 자리는 설정 저장 경로 한 곳(`setSettingValue` 앞의 키별 검증 훅이나 설정 저장 액션 — 이미 있는 필드 오류 모양을 따른다). 최대 표시 순번은 `document-numbering`에 함수 하나(예: 그해 카운터에서 계산)로 두고 설정 쪽이 부른다. 문구 리터럴은 한 곳.

③ **(b)면 — 카운터 보정.** `domain/document-numbering`의 번호 발급이 행 잠금 안에서 「올해 이미 매긴 최대 표시 순번」보다 작거나 같은 표시 순번을 만들지 않게 카운터를 보정한다(보정은 같은 트랜잭션의 카운터 행에 — 결번은 허용, 겹침은 없음). 설정 저장 쪽은 바꾸지 않는다. `DOCUMENT_NUMBER_PROJECT_SEQ_START`의 설명 문구가 이 동작과 맞는지 확인하고 다르면 한 줄로 고친다(「연도가 바뀌어 순번이 다시 시작할 때의 첫 값 · 올해 이미 매긴 번호 뒤에서 이어 간다」).

④ **결정 기록.** `docs/design/DECISIONS.md`에 「결정 ② — {답}(사용자 답 {날짜}) · 순번 시작값과 이미 매긴 번호의 관계 · 이미 매긴 번호는 바꾸지 않는다(ADMN-09)」 한 항목을 코드와 같은 커밋에 더한다.

⑤ **전체 게이트 한 번.** ①~④가 초록이면 `CI=true pnpm test`를 한 번 돌린다(옛 04-42 Task 5의 전체 게이트가 결정 ② 코드까지 덮던 몫 — 같은 웨이브의 04-42와 실행 순서가 정해져 있지 않아 이 플랜이 직접 한다). 04-42와 동시에 돌리지 않는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/document-numbering.test.ts test/integration/settings.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 시작값을 낮춘 뒤 이미 매긴 번호와 같은 번호가 나왔거나, (a)의 거부가 없거나 설정 값이 바뀌었거나, (b)의 다음 번호가 최대 + 1이 아니거나, 동시 발급이 같은 번호를 냈다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project unit test/unit/design-system-docs.test.ts</automated>
    <fails_when>eslint·tsc error가 나오거나 종료 코드가 0이 아니다 — 새 DECISIONS 항목이 머리글 개수 단언을 깨뜨렸다(깨지면 ENG-D11 — 정규식을 고치고 회귀 케이스 + SUMMARY 편차)</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다(프로덕션 빌드 기준) — 결정 ② 구현이 프로젝트 등록·설정 저장의 다른 경로를 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - describe `순번 시작값 낮추기(결정 ②)`가 답에 맞는 케이스와 공통 동시성 케이스를 갖고 초록이다
    - (a)면: 확정 문구 리터럴이 코드에 한 곳이고, 거부된 저장 뒤 설정 값이 그대로다(DB 재조회) · (b)면: 설정 저장 경로 diff가 없고(`git diff --stat HEAD -- app/(app)/admin/settings/actions.ts` 출력 없음 — 이 태스크 커밋 전 작업 트리 기준) 채번 한 곳이 보정한다
    - DECISIONS에 결정 ② 항목이 한 줄이고 답·날짜·근거가 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>순번 시작값을 이미 매긴 번호보다 낮춰도 같은 번호가 다시 매겨지는 경로가 없고, 사용자가 고른 규칙이 테스트와 결정 기록으로 고정됐으며 전체 게이트가 초록이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 브라우저 → 설정 저장 액션 | 순번 시작값(정수)이 넘어온다 — 판정은 서버의 설정 저장 경로 |
| 등록 요청 → 채번 | 번호는 카운터 행 잠금 안에서만 만들어진다 — 화면이 번호를 보내지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-380 | Tampering | 문서 번호 순번 시작값 변경 뒤 번호 충돌(결정 ②) | medium | mitigate | 답이 (a)면 설정 저장이 이미 매긴 최대 이하 값을 거부하고 · (b)면 채번이 행 잠금 안에서 이미 매긴 최대 뒤로 카운터를 보정한다 — 어느 쪽이든 `UNIQUE(format_key, number)` 위반 경로가 없고 통합 테스트가 고정한다. 답은 Task 1이 사용자에게 받는다 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json`의 의존성 객체(`dependencies`·`devDependencies`·`optionalDependencies`·`peerDependencies`)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 — `package.json` 원문 비교가 아니다(엔지 리뷰 C) |
</threat_model>

<verification>

- 통합 `document-numbering.test.ts`(describe `순번 시작값 낮추기(결정 ②)`) · `settings.test.ts` · `document-counters-concurrency.test.ts` 초록
- `pnpm lint` · `pnpm typecheck` · 단위 `design-system-docs.test.ts` 통과 · `CI=true pnpm test` 세 계층 전부 통과
- DECISIONS 결정 ② 항목 한 줄
- `package.json` 의존성 객체(dependencies·devDependencies·optionalDependencies·peerDependencies)가 d6b41cf와 같고 `pnpm-lock.yaml` diff가 비어 있다 (원문 비교 아님)

</verification>

<success_criteria>

- 사용자가 결정 ②에 답했고, 그 답이 SUMMARY와 DECISIONS에 그대로 있다
- 순번 시작값을 이미 매긴 번호보다 낮춰도 프로젝트 등록이 번호 중복으로 실패하지 않는다
- 이미 매긴 번호는 바뀌지 않고, 동시 등록은 서로 다른 번호다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 변경된 기존 심볼 | (a)면 설정 저장 경로의 `document_number.project.seq_start` 검증 + `document-numbering`의 올해 최대 표시 순번 함수 · (b)면 `domain/document-numbering`의 발급 보정(행 잠금 안) + `DOCUMENT_NUMBER_PROJECT_SEQ_START` 설명 한 줄 |
| 문서 | `docs/design/DECISIONS.md` 결정 ② 한 줄 |
| 테스트 | `test/integration/document-numbering.test.ts` describe `순번 시작값 낮추기(결정 ②)` |

## Review Dispositions Ledger

### Round 1 — 2026-09-24 체커 revision 1(04-42 분할)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| scope_sanity — 04-42가 `<task>` 다섯(`checkpoint:decision` 하나 · 실행 넷) | WARNING | 04-42의 결정 ② 두 태스크(옛 Task 3 체크포인트 · Task 4 구현)를 이 플랜으로 옮겼다 — 04-42는 세 태스크, 이 플랜은 두 태스크. 질문·옵션·답별 구현·검증 내용은 그대로이고 구현 태스크가 이 플랜의 트레이서다. 04-42 Task 5가 덮던 전체 게이트는 Task 2 ⑤로 가져왔다 |
| PR #38 알려진 문제 「채번 시작값을 낮추면 번호 충돌」 · HANDOFF open_questions[1] | DECISION(미결) | Task 1 `checkpoint:decision`(옵션 (a) 설정 검증 · (b) 카운터 보정 · costly) · Task 2 답별 구현 · 답별 behavior·수용 기준 · truth · T-04-380 · `autonomous: false` — 04-42 Round 5의 배정을 이 플랜이 이어받는다 |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/04-project-quote-ledger/04-51-SUMMARY.md` when done
</output>
