---
phase: "04"
slug: "project-quote-ledger"
status: draft
shadcn_initialized: false
preset: none
created: "2026-09-22"
reviewed_at: "2026-09-22"
---

# Phase 4 — UI Design Contract

> 프로젝트·견적 원장. `docs/design/SYSTEM.md` + `tokens.css`가 이미 확정돼 있으므로 이 문서는 새 시각
> 언어를 만들지 않는다. 기존 계약을 이 페이즈의 표면 11개에 적용하고, **`SYSTEM.md` 개정 네 건의
> 초안**(§7-3 표 구현 계약 보강 · §7-5 상태 태그 보강(의미 목록 + 길이 규칙) · §7-8 2차 라벨 충돌
> 규칙 · §7-15 폼(`ui/form`·`ui/select`) 신설)을 쓰는 것이 핵심 산출물이다.
>
> 엑셀식 표의 **동작** 계약(Tab/Enter·방향키·범위 복사·붙여넣기·Esc·⌘S/⌘↵·전부 저장 또는 전부
> 거부·충돌 표시)은 Phase 2에서 §7-3으로 이미 고정됐고 D-61~D-69가 그것을 확인했다. 여기서 더하는
> 것은 §7-3이 아직 말하지 않은 것들이다: **셀 편집 가능성 3단계 · 줄 버전 충돌의 실제 문구 ·
> 붙여넣기 정규화 · 줄 순서와 그룹 머리글 · 미저장 편집 복원 · 폰 행 시트 · 저장 범위**.
> 자체 구현/라이브러리 선택은 D-61이 **자체 구현**으로 이미 닫았다(CEO OV-7의 선택권은 소진됐다) —
> 아래 계약은 그 결정을 전제로 쓰였고, 만약 계획 단계가 라이브러리로 되돌린다면 그 라이브러리가
> 「(가)~(아)」 여덟 항목을 **전부** 만족하고 토큰·마크업 계약(D-20 stylelint, radius 0, 카드 없음)을
> 깨지 않는지가 채택 조건이다.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — shadcn 미사용. 자체 디자인 시스템(`docs/design/SYSTEM.md` §0~§11 확정, `docs/design/tokens.css`) |
| Preset | 해당 없음 |
| Component library | 없음(헤드리스 UI 라이브러리 미사용) — 네이티브 HTML 엘리먼트 + CSS Modules(D-19). 표도 자체 구현(D-61, 새 런타임 의존성 0) |
| Icon library | Lucide 세트 하나, 패키지 의존 없이 SVG를 `components/icons/`에 인라인 복사(§9). 이 페이즈가 새로 쓰는 아이콘: 정렬 방향(arrow-up/down, 목록 열 머리글) · 닫기(x, 시트·모달) 뿐 |
| Font | Pretendard Variable, 자체 호스팅 `public/fonts/pretendard/`(woff2 동적 서브셋, §2-1) |

컴포넌트는 `tokens.css`의 변수만 참조한다(D-21). **이 페이즈는 새 색·서체·radius·그림자·토큰을 0개
만든다.** §7-3이 지정한 `--g-50`(편집 중 행·범위 선택)은 의미 토큰 `--accent-weak`로 참조한다(§1-2
"컴포넌트는 의미 토큰만"). 표 머리글의 `--g-100`/`--g-950`은 대응하는 의미 토큰이 없어 §7-3 원문
그대로 `--g-*`를 직접 참조한다 — 이미 `ui/permission-grid`가 같은 자리에서 같은 예외를 쓰고 있어
새 예외가 아니다.

이 페이즈가 `SYSTEM.md`를 고치는 지점은 **네 건**이고 전부 아래 §7 초안과 「DECISIONS.md에 남길 것」에
적혀 있다: **§7-3 보강**(편집 표 구현 계약) · **§7-5 보강**(상태 태그 — **의미 목록 + 길이 규칙**
두 가지가 ② 한 항목의 범위다) · **§7-8 보강**(2차 라벨 충돌 규칙) · **§7-15 신설**(폼·셀렉트).
네 건 모두 시스템 자체를 고치는 개정이고, **화면 하나만의 예외는 하나도 남기지 않는다**
(CLAUDE.md 프론트엔드 규칙).

---

## Component Inventory

Enumerated by `find ui -maxdepth 1 -mindepth 1 -type d | sort` — 15 modules(컴포넌트 14 + `ui/logout`
훅 1) — 로컬 `ui/`(npm 패키지 아님, CSS Modules, D-19) — 2026-09-22.

이 표는 **닫힌 목록이 아니다.** 표 밖 네이티브 HTML(`<table>`·`<select>`·`<input>`·`<dialog>`)을
쓰는 것이 이 시스템의 기본값이고(§7-2 "커스텀 드롭다운 만들지 않는다"), 이 페이즈가 신설하는 셋은
"신규(이 페이즈)"로 표시했다.

| Component | Import path | Notes |
|-----------|-------------|-------|
| Button | `ui/button/Button` | §7-1 1차/2차/3차. `pending`·`disabled`+`disabledReason`·`shortcut` props가 이미 있다 — 「일괄 저장 ⌘S」의 `…`·막힘 이유·kbd를 새로 만들지 않는다 |
| TextField | `ui/input/TextField` | §7-2 텍스트 입력. `aria-invalid`·`aria-describedby` 구현됨. **이 페이즈가 네이티브 `required` 위임을 걷어낸다**(A-H3) |
| StatusTag | `ui/status-tag/StatusTag` | §7-5. `kind` 5종 × `variant` `tag`/`text`. 프로젝트 상태 4종(제목 옆 `tag`, 목록 상태 열 `text`), 견적 줄 상태 2종 |
| ListEmpty | `ui/list-empty/ListEmpty` | §7-7 EMPTY/ERROR 한 줄 + 다음 한 수. `action`이 `href`/`onClick` 유니언 — 표의 「첫 줄 만들기」는 `onClick`(이동이 아니다), 목록의 「프로젝트 등록」은 `href`(`?new=1`) |
| PageHeader | `ui/page-header/PageHeader` | §6-0 화면 제목 + 부제. 상세 머리 줄의 제목·번호·차수·상태 태그가 여기 들어간다 |
| KvList | `ui/kv-list/KvList` | §7-8 시트 상세의 `dl` 골격. **폰 행 시트(D-69)의 본문**이 이것 |
| HistoryList | `ui/history-list/HistoryList` | §7-14. **이 페이즈에서 쓰지 않는다.** 차수 섹션을 세 번째 사용처로 삼는 안을 검토했으나 계약이 맞지 않아 접었다 — 근거는 S5 |
| Toast | `ui/toast/Toast` | §7-6. 화면 이동이 따르는 행동만: 프로젝트 등록·새 차수·상태 전환. **표 저장 결과는 토스트를 띄우지 않는다**(합계 행에 있다) |
| Banner | `ui/banner/Banner` | §7-11. 이 페이즈에서 새로 쓰는 배너 없음 — 미저장 편집 복원 줄은 배너가 아니다(아래 (마)) |
| FormAlert | `ui/form-alert/FormAlert` | **이 페이즈의 폼에서는 쓰지 않는다** — §7-7 폼 행이 "상단 한 줄 없음"을 규정하고 §7-15가 오류를 `Form.Error`·`Form.Actions`로 옮긴다 |
| NextTurn | `ui/next-turn/NextTurn` | §7-4. 첫 화면 전용 — 프로젝트 목록·상세에는 이 블록이 **없다** |
| PermissionGrid | `ui/permission-grid/PermissionGrid` | 관리자 전용(§7-13). 이 페이즈와 무관. **편집 표와 혼동하지 않는다**(D-40) |
| Shell / TopBar / BottomTabs / MoreSheet | `ui/shell/*` | 배선됨(D-23). 이 페이즈는 `role-menu.ts`에 손익 메뉴의 「리저브」 항목만 더한다(D-59) — 셸 컴포넌트는 건드리지 않는다 |
| AuthFrame · `ui/logout` 훅 | `ui/auth-frame`, `ui/logout` | 로그인 전용. 이 페이즈와 무관 |
| **Table**(신규, 이 페이즈) | `ui/table/`(컴포넌트명은 계획 단계) | §7-3 편집 표 + 읽기 표. 편집 = 견적 줄·매출 발행/입금 줄·리저브, 읽기 = 프로젝트 목록·차수 섹션·이전 차수 견적 줄. 이후 페이즈의 법인카드·목표도 공유한다(D-61) |
| **Form**(신규, 이 페이즈) | `ui/form/` | §7-15 신설 초안. 이 페이즈의 사용처 **둘** — 프로젝트 등록 폼(S2, `Form.Actions` 있음) · 매출 계약 금액 칸(S6, `Form.Actions` 없는 단일 칸 폼). Phase 7이 관리자 폼 8개 + `/account`를 이관(A-H2) |
| **Select**(신규, 이 페이즈) | `ui/select/` | §7-2 네이티브 `<select>` + 시스템 테두리. 클라이언트·PM·팀·통화·구분·소분류 |

신규 셋은 모두 `DESIGN.md` §4 규칙 3("두 곳 이상에서 쓰일 때만")을 만족한다: Table 7곳(편집 4 =
견적 줄·발행 줄·입금 줄·리저브 / 읽기 3 = 프로젝트 목록·차수 섹션·이전 차수 견적 줄),
**Form 2곳**(프로젝트 등록 폼 · 매출 계약 금액 칸), Select 6곳 이상.

**리저브는 Form의 사용처가 아니다.** 리저브 대장은 §7-3 편집 표 + 3차 「리저브 줄 추가」 + 1차
「일괄 저장 ⌘S N」으로 돌아간다(S9) — 줄을 폼으로 받지 않는다. 규칙 3의 문턱은 「두 곳 이상」이므로
Form은 2곳으로 그대로 성립한다. 숫자를 부풀려 규칙을 통과시키지 않는다.

---

## Spacing Scale

`tokens.css`의 실제 토큰(4px 단위, D-20 — 간격은 stylelint 리터럴 금지 대상이 아니다):

| Token | Value | Usage (이 페이즈) |
|-------|-------|-------|
| `--s-1` | 4px | 라벨-입력 세로 간격, 상태 태그와 부제 사이 |
| `--s-2` | 8px | 표 셀 가로 패딩(`--cell-pad-x`), 버튼 사이 |
| `--s-3` | 12px | 그룹 머리글 위 여백, 섹션 선 아래 |
| `--s-4` | 16px | 폼 항목 사이, 힌트 줄 항목 사이 |
| `--s-5` | 20px | PC 컨테이너 좌우 패딩(폰 14px) |
| `--s-6` | 24px | 폼 묶음 사이, 섹션 2px 선 위 |
| `--s-8` | 32px | 표와 다음 섹션 제목 사이 |
| `--s-12` | 48px | 화면 맨 아래 여백(힌트 줄 위) |

표·폼 전용 치수 토큰(전부 기존값, §3):

| Token | Value | Usage |
|-------|-------|-------|
| `--cell-pad-y` / `--cell-pad-x` | 6px(폰 10px) / 8px | 표 셀 패딩 |
| `--row-min` | 36px(폰 44px) | 행 최소 높이. **고정 높이가 아니다** — 외화 2행·항목명 줄바꿈이면 내용을 따른다 |
| `--label-w` | 96px | PC 폼 라벨 열 |
| `--form-max` | 720px | 폼 한 열 최대 폭 |
| `--modal-w` | 480px | 확인 모달(삭제·취소·상태 전환·새 차수) |
| `--control-h` | 32px(폰 40px) | 버튼·입력 높이 |
| `--touch-min` | 44px | 폰 행·시트 버튼·행 시트 안 행동 |

칸 폭은 **3종만**(§6-3): select 200 · 짧은 칸(금액·날짜·수량·환율) 280 · 긴 칸(프로젝트명·비고·메모)
480. 네 번째 폭을 만들지 않는다.

**Exceptions:** 없음. 이 페이즈가 새로 도입하는 간격 값·치수 토큰은 0개다.

---

## Typography

`SYSTEM.md` §2-2·§2-3 그대로(굵기 3단계: 400·600·700):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body(표 셀·목록 행·폼 입력·본문) | `--fs-base` 14px(폰 15px) | 400 | 1.5(표 `--lh-table`) / 1.6(본문 `--lh-body`) |
| Label(라벨·표 머리글·힌트 줄·외화 2행·서버 계산 한 줄) | `--fs-sm` 12px | 600(라벨·머리글) / 400(2행·힌트) | 1.5 |
| Heading(화면 제목 = 프로젝트명, 합계 금액) | `--fs-lg` 18px | 700 | 1.4 |
| Micro(상태 태그·행 번호·kbd·`계산 불가` 배지) | `--fs-xs` 11px | 600 | 1.4 |
| Display | **사용 없음** — `--fs-xl`/`--fs-2xl`은 인쇄·외부 수령자·손익 대시보드 KPI 전용(§2-2). 원장·상세·폼에 큰 숫자를 두지 않는다(안티슬롭) |

- **화면당 4단계를 넘지 않는다**(§2-2). 이 페이즈의 모든 화면은 `xs · sm · base · lg` 정확히 넷이다.
- 숫자(금액·수량·환율·건수·문서 번호·날짜)는 `tabular-nums`, 우측 정렬, `white-space: nowrap`,
  `letter-spacing: 0`(§2-4). 폭이 모자라면 숫자를 꺾지 않고 **열을 접는다**(§7-3 폰 전략).
- 문자 열: 항목명·프로젝트명은 `word-break: keep-all` 줄바꿈 허용, 거래처·클라이언트는 한 줄 말줄임.
- 합계 행만 700. 네 숫자 줄(PNL-01)은 이 페이즈에서 렌더하지 않는다(Phase 9).

---

## Color

`SYSTEM.md` §1 의미 토큰만(새 색 0개):

| Role | Value | Usage (이 페이즈) |
|------|-------|-------|
| Dominant (60%) | `--bg` `#FFFFFF` | 바탕 전체, 읽기용 표 머리글, 편집 셀 |
| Secondary (30%) | `--surface` `#F3F7F5` → `--accent-weak`(`--g-50`) → `--g-100` (3단계, 중첩 금지) | 행 hover·비활성 컨트롤(`--surface`) / 편집 중 행·범위 선택(`--accent-weak`) / **편집용 표 머리글·합계 행**(`--g-100` + `--g-950` 글자) |
| Accent (10%) | `--accent`(`--g-700` `#005446`) | §1-3의 다섯 곳만 — 아래 목록 |
| Destructive | `--danger` `#9B1C1C` + `--danger-weak` `#FBE9E9` | 오류 셀 배경 + 좌측 `--inset-error` 2px 선 + 이유 한 줄. **붉은 버튼은 없다** — 삭제·취소·완료 전환은 색이 아니라 확인 모달로 구분한다(§7-1) |
| Warning | `--warning` `#8A5A00` | 미수·초과 입금 금액, 붙여넣기 일부 버림 안내. 넓은 면을 채우지 않는다(§1-3 규칙 3) |
| Success | `--success`(`--g-600`) | 합계 행 `저장됨 6줄 14:02`, 차수 `승인` 태그, dirty 셀 외곽선(`--inset-dirty`) |

**Accent reserved for** (§1-3 다섯 곳, 이 페이즈의 구체적 자리):

1. 「내 차례」 태그·**행동 링크** — 이 페이즈의 목록·상세에는 「내 차례」 블록이 없다. 해당하는 것은
   ⓐ 3차 버튼 글자 + 밑줄(「더 보기 50건」·「첫 줄 만들기」·「고객 승인 표시」·「복원 / 버림」·「덮어쓰기 /
   그 값으로」·「견적 외 비용 줄 추가」·「차수 열기」)과 ⓑ **`--accent` 상태 태그**(`진행` · 차수
   `현재`).
   ⓑ가 ①에 드는 근거: §7-5가 `--accent`를 상태 태그의 다섯 색 중 하나로 이미 허용하고, ①의 문구가
   「내 차례 **태그**·행동 링크」로 태그를 포함한다. 즉 이 페이즈의 포인트 색 사용처는 §1-3의 다섯
   군데 밖으로 나가지 않는다(§11 `/design-review`의 「포인트 색 사용처 §1-3의 5군데 안」 검사 근거)
2. **편집 중 셀·행** — 표 안 입력의 아래 1px 밑줄, 활성 셀 `2px --accent` 외곽선, 범위 선택
   `--accent-weak` 배경
3. **1차 버튼** — 화면당 하나: 목록 「프로젝트 등록」 / 등록 폼 「프로젝트 등록 ⌘↵」 / 상세 「일괄
   저장 ⌘S N」 / 리저브 「일괄 저장 ⌘S N」 / 확인 모달의 1차.
   목록과 등록 폼은 **같은 라우트의 배타 상태**다(§6-1 `?new=1` 토글, D-39) — 폼이 열리면 목록의
   1차는 렌더되지 않는다(S2). 그래서 이 둘이 동시에 세어지는 일은 없다.
4. **포커스 링** — `:focus-visible` `outline 2px --focus offset 2px`
5. **현재 위치** — 상단 바 현재 메뉴 밑줄(셸, 이미 배선). 이 페이즈가 더하지 않는다

**DECISIONS.md에 남길 것 — §7-5 보강 한 건(②). 범위는 둘이다: 의미 목록(낱말 다섯) + 길이 규칙.**

§7-5는 태그 색마다 **괄호 안에 의미 목록**을 달아 둔다: `--danger`(막힘·증빙 없음·반려) ·
`--warning`(오늘·마감 임박) · `--accent`(내 차례·결재 중·편집 중) · `--success`(승인·연결·저장됨) ·
`--muted`(대기·미착수·임시). 이 페이즈는 **다섯 목록이 전부 닫힌 목록이라고 읽는다** — §7-5 원문에
「예」·「등」 같은 여지가 없고, 열린 목록으로 읽으면 색 선택이 화면마다 갈려 §7-5가 규칙 구실을
못한다. 그 읽기 아래에서 이 페이즈가 들여오는 상태 낱말 중 **다섯이 목록 밖**이다:

| 낱말 | 색 | §7-5의 어느 목록에 더하는가 |
|---|---|---|
| `진행` | `--accent` | 내 차례·결재 중·편집 중 **+ 진행 중** |
| 차수 `현재` | `--accent` | 〃 **+ 지금 기준이 되는 것**(최신 차수, D-54) |
| `완료(정산)` | `--success` | 승인·연결·저장됨 **+ 끝남(잠김)** |
| `미수주` | `--muted` | 대기·미착수·임시 **+ 닫힌 정상 결과** |
| 견적 줄 `취소` | `--muted` | 〃 **+ 무효가 된 줄** |

(`수주중`은 `대기`, 견적 줄 `미착수`는 `미착수`, 차수 `승인`은 `승인`에 이미 있다 — 보강 대상이 아니다.)

**② 길이 규칙 보강 — `완료(정산)`.** §7-5의 다음 줄은 「태그 글자는 두 글자~네 글자 명사: `막힘`
`오늘` `결재` `대기` `승인` `반려` `편집 중`. 문장 금지」다. 이 페이즈가 들여오는 여덟 낱말 중 일곱
(수주중·진행·미수주·미착수·취소·현재·승인)은 두~세 글자로 규칙 안이지만 **`완료(정산)` 하나가
규칙 밖**이다 — 일곱 예시 어디에도 괄호가 없다. 이 값은 제목 옆 `tag`와 목록 상태 열 `text`에
그대로 쓰이므로 규칙이 걸린다.

**라벨은 바꾸지 않는다.** `완료(정산)`은 **D-41**(「프로젝트 상태는 수주중 → 진행 → 완료(정산) +
미수주 네 가지다」, Reversibility costly)과 ROADMAP Phase 4 성공 기준 4가 잠근 업무 용어다.
(상태 **전환 주체**가 팀장 이상이라는 것은 D-46이고 라벨 잠금과는 다른 결정이다 — 두 결정을
섞지 않는다.) 그러므로 고치는 쪽은 라벨이 아니라 §7-5이고, 화면 예외가 아니라 시스템 규칙으로
고친다: **「상태 값이 업무 용어로 잠긴 경우 네 글자 명사 + 괄호 한정어 하나를 허용한다
(`완료(정산)`). 괄호 안은 두 글자, 문장 금지는 그대로.」** 괄호를 장식으로 쓰는 길을 열지 않으려고
「업무 용어로 잠긴 경우」라는 조건을 붙였다 — 앞으로 같은 자리에서 같은 답이 나온다.

계획 단계의 첫 태스크가 `docs/design/DECISIONS.md`에 **한 항목**으로 남긴다: 「§7-5 상태 태그를 두
가지로 보강한다. ⑴ 의미 목록에 낱말 다섯을 더한다(accent: 진행 중 · 지금 기준 / success: 끝남 /
muted: 닫힌 정상 결과 · 무효) — 프로젝트 목록 기본 보기가 전체 상태(D-51)라 네 상태가 한 화면에
섞이고, 지금 돌아가는 건과 끝난 건·안 된 건을 글자만으로 훑기 어렵다. 차수·견적 줄 상태도 같은
§7-5 태그를 쓴다. ⑵ 길이 규칙에 한 줄을 더한다 — 업무 용어로 잠긴 상태 값은 네 글자 명사 + 괄호
한정어 하나를 허용한다(`완료(정산)`, D-41이 잠근 값). 괄호 안은 두 글자, 문장 금지는 그대로」.
그리고 `SYSTEM.md` §7-5를 그대로 고친다. **둘 다 한 번에 고친다** — 한쪽만 고치고 나머지를 화면
예외로 두는 것이 CLAUDE.md 프론트엔드 규칙(「화면 하나만 예외 금지」·「기록 없는 시스템 이탈 금지」)이
막는 일이다.

**상태 → 색 매핑 (확정)**

| 상태 | `kind` | 자리 |
|---|---|---|
| 수주중 | `muted` | 제목 옆 `tag` · 목록 상태 열 `text` — §7-5 `대기` |
| 진행 | `accent` | 〃 — **§7-5 보강 대상** |
| 완료(정산) | `success` | 〃 — **§7-5 보강 대상** |
| 미수주 | `muted` | 〃 — **§7-5 보강 대상**. **붉게 칠하지 않는다.** 미수주는 막힘이 아니라 정상 결과이고 잠기지도 않는다(D-45). 수주중과 색이 같아도 글자가 다르다(§10 "상태는 항상 글자다") |
| 견적 줄 `미착수` | `muted` | 표 상태 열 `text` — §7-5 `미착수` |
| 견적 줄 `취소` | `muted` | 〃 — **§7-5 보강 대상**. 취소선·회색 행 배경을 쓰지 않는다(§1-3 규칙 3) |
| 차수 `현재` | `accent` | 차수 섹션(S5, 읽기 표 상태 열 `text`) — **§7-5 보강 대상** |
| 차수 `승인` | `success` | 〃 — §7-5 `승인` |

---

## Copywriting Contract

`SYSTEM.md` §8 규칙 그대로(버튼 = 실제 동작, 오류 = 원인·다음 행동, 사과·이모지·느낌표 없음,
안내 문구 없음). 용어는 §8 규칙 7: 견적 줄(항목 X) · 실행가(원가 X) · 차익 · 지출결의 · 차수 ·
거래처 · 클라이언트. 사전 견적의 화면 라벨은 사용자 용어 **「총 매출 예상가」**(D-52).

| Element | Copy |
|---------|------|
| Primary CTA — 목록 | `프로젝트 등록`(1차, 필터 줄 오른쪽 = §6-1 「새 지출결의」 자리, `?new=1` 토글) |
| Primary CTA — 등록 폼 | `프로젝트 등록 ⌘↵`(1차) · `취소 Esc`(2차, 목록으로) |
| Primary CTA — 견적 원장 | `일괄 저장 ⌘S 3`(1차, 건수는 화면 전체 dirty 칸 수) |
| Primary CTA — 리저브 | `일괄 저장 ⌘S 2`(1차) |
| 2차 버튼 — 견적 원장 | `복사해 새 차수` · `프로젝트 복사` · `상태 바꾸기`(팀장 이상에게만 렌더) |
| 3차 버튼 | `더 보기 50건` · `첫 줄 만들기 ⌘↵` · `견적 외 비용 줄 추가` · `고객 승인 표시` · `승인 표시 취소` · `발행 줄 추가` · `입금 줄 추가` · `리저브 줄 추가` · `차수 열기`(이전 차수) · `복원` / `버림` · `덮어쓰기` / `그 값으로` · `필터 지우기` |
| Empty — 프로젝트 목록 | `등록된 프로젝트가 없습니다 · 프로젝트 등록` |
| Empty — 목록 필터 0건 | `조건에 맞는 프로젝트가 없습니다 · 필터 지우기` |
| Empty — 견적 줄 표 | `이 프로젝트에 견적 줄이 없습니다 · 첫 줄 만들기 ⌘↵`(§7-7 원문 예시 그대로) |
| Empty — 발행 줄 / 입금 줄 | `발행한 세금계산서가 없습니다 · 발행 줄 추가` / `입금 줄이 없습니다 · 입금 줄 추가` |
| Empty — 차수 섹션 | **해당 없음** — 프로젝트를 등록하면 상세 견적 1차가 항상 함께 생긴다(D-53 "빈 차수는 만들지 않는다") |
| Empty — 리저브 | `리저브 기록이 없습니다 · 리저브 줄 추가` |
| Error — 목록 / 상세 / 리저브 | `프로젝트 목록을 불러오지 못했습니다 · 다시 시도` / `견적 원장을 불러오지 못했습니다 · 다시 시도` / `리저브 대장을 불러오지 못했습니다 · 다시 시도` |
| Error — 표 배치 저장 실패 | 합계 행 오른쪽 `오류 1칸 · 전부 거부`(`--danger`). 충돌이면 `충돌 1줄 · 전부 거부` |
| Error — 표 배치 저장 실패(표 밖 칸 오류로 전부 거부, U-6) | 오류가 0칸인 표의 합계 행에는 `전부 거부 · 다른 칸 오류 1칸`(`--danger`) — 「오류 N칸」을 0으로 적지 않고 거부 사실과 원인이 이 표 밖에 있다는 것 둘 다를 말한다(04-04 Task 2 ⑤ 계획 단계 판단) |
| Error — 셀(형식) | `숫자가 아닙니다 · 12,400,000처럼 적어 주세요` · `날짜 형식이 아닙니다 · 2026-09-18처럼 적어 주세요` |
| Error — 셀(환율) | `환율이 없습니다 · USD 환율을 적어 주세요`. 저장 전 표시값은 `계산 불가 · 환율 없음`(§2-4) |
| Error — 셀(읽기 전용) | `지출결의 26001-0004 연결됨 · 고치려면 새 차수`(D-66) |
| Error — 셀(잠김) | `완료(정산) · 견적 줄이 잠김`. 경영관리에게만 다음 한 수: `· 견적 외 비용 줄 추가`(D-47·D-48) |
| Error — 셀(충돌) | `다른 사람이 14:01에 9,800,000으로 바꿈 · 덮어쓰기 / 그 값으로`(§7-3 원문 형식, D-65) |
| Error — 리저브 잔액 | `이 줄 뒤 잔액이 -300,000이 됩니다 · 금액을 줄이거나 입금 줄을 먼저 적어 주세요`(D-60) |
| Error — 등록 폼 제출 | 버튼 옆 `등록하지 못했습니다 · 클라이언트 1칸 · 값은 남아 있습니다`(§6-3 ERROR 행) |
| 막힘 — 등록 폼 필수 | `등록할 수 없음 — 클라이언트 · 담당 PM 2칸 · 클라이언트 고르기` |
| 막힘 — 진행 전환 | `진행으로 바꿀 수 없음 — 기간 없음 · 기간 적기`(D-49 게이트) |
| 막힘 — 지출결의(고객 승인) | 담당 PM에게: `2차 고객 승인 전 · 고객 승인 표시`. 그 밖의 사람에게: `2차 고객 승인 전 · 담당 PM 박서연`(다음 한 수가 그 사람 권한 밖이면 버튼 대신 담당자를 적는다) |
| 막힘 — 거래처 | `거래처 없음 · 거래처 고르기` |
| PARTIAL — 붙여넣기 일부 버림 | 합계 행 오른쪽 `붙여넣기 · 오른쪽 2칸 버림`(`--warning`) |
| PARTIAL — 미수 / 초과 입금 | 매출 합계 행 `미수 3,400,000` / `초과 입금 500,000`(`--warning`) |
| PARTIAL — 계산 불가 | `계산 불가 1건` 테두리 배지(`--fs-xs --muted`, §7-7) |
| SUCCESS — 표 | 합계 행 오른쪽 `저장됨 6줄 14:02`(`--success`) + 편집 행 600ms `--accent-weak` 틴트. **토스트 없음**(§7-6) |
| SUCCESS — 등록 / 새 차수 / 상태 전환 | 이동 + 토스트 `프로젝트 등록 · 26001` / `새 차수 · 상세 견적 3차` / `상태 바꾸기 · 진행` |
| 미저장 편집 복원 | 표 위 한 줄 `저장 안 한 편집 3칸 · 복원 / 버림`(D-68) |
| Destructive — 견적 줄 삭제(연결 문서 없음) | 모달 제목 `견적 줄 삭제` · 부제 `{항목} · {견적가}` · 본문 `보관함으로 이동합니다 · 관리자가 복원할 수 있습니다` · 1차 `삭제` · 2차 `취소 Esc` |
| Destructive — 견적 줄 취소(연결 문서 있음) | 모달 제목 `견적 줄 취소` · 본문 `견적가가 0이 되고 이력은 남습니다 · 연결된 지출결의는 그대로입니다` · 1차 `견적 줄 취소` · **2차 `닫기 Esc`** — 아래 §7-8 보강의 일반 규칙(1차 라벨에 「취소」가 들어가면 2차는 `닫기 Esc`)이 적용된 결과다. 이 모달만의 예외가 아니다 |
| Destructive — 고객 승인 표시 취소 | 모달 제목 `고객 승인 표시 취소` · 본문 `{차수} 승인일이 지워집니다 · 이 차수의 지출결의가 다시 막힙니다` · 1차 `승인 표시 취소` · **2차 `닫기 Esc`**(같은 §7-8 보강 규칙 — 1차에 「취소」가 있다). 연결 문서가 있으면 버튼 자체가 없다(D-56) |
| Destructive — 새 차수 | 모달 제목 `복사해 새 차수` · 본문 `상세 견적 2차의 6줄을 복사해 3차를 만듭니다 · 차수는 되돌릴 수 없습니다` · 1차 `새 차수 만들기` · 2차 `취소 Esc`(D-53) |
| Destructive — 미수주로 닫기 | 모달 제목 `미수주로 닫기` · 본문 `쌓인 비용이 팀 미수주 비용이 됩니다 · 진행으로 되돌릴 수 있습니다` · 1차 `미수주로 닫기` · 2차 `취소 Esc`(D-44·D-45) |
| Destructive — 완료(정산) | 모달 제목 `완료(정산)로 바꾸기` · 본문 `견적 줄이 잠기고 새 지출결의를 받지 않습니다 · 되돌리기는 없습니다` · 1차 `완료로 바꾸기` · 2차 `취소 Esc`(D-46, 팀장 이상) |
| Destructive — 리저브 줄 삭제 | 모달 제목 `리저브 줄 삭제` · 본문 `보관함으로 이동합니다 · 잔액이 다시 계산됩니다` · 1차 `삭제` · 2차 `취소 Esc`(D-60) |
| 비파괴 — 복원 / 버림 / 진행 복귀 | 확인 모달 없음, 즉시 실행. 미수주 → 진행 복귀는 잃는 것이 없다(D-44) |

**PNL-09 표시 의무:** "모든 금액은 공급가액 기준이며 화면에 명시된다"는 안내 문구(§8 규칙 5) 금지의
예외가 아니라 **형식 표기**다. 자리는 둘뿐: ① 편집 표 합계 행 부제 `합계 (공급가액 · 6줄)`(§6-2 원문)
② 매출 섹션 제목 부제 `공급가액 기준 · 입금액만 통장 합계`. 열 머리글에 `(원)`을 쓰지 않는다(§2-4).

---

## UI Considerations

> UI-consideration probe(워크플로 9.5)가 올린 57건의 해소 결과다.
> **resolved explicit 43 · resolved backstop 11 · unresolved 6.**
> (+ 체커가 남긴 경계 3건은 프로브 항목 어디에도 대응하지 않아 맨 아래에 따로 적었다.)
>
> 이 절은 **상태 커버리지**만 다룬다 — EMPTY·ERROR **문구 자체**는 위 `## Copywriting Contract`에
> 있고 여기서는 그 행을 **가리키기만 한다**(중복 금지). 표면 id는 `## 적용 규칙`의 S1~S10과 같다.
> `verification: backstop`은 소스 단언으로 증명되지 않고 **렌더·DOM 감사로만** 증명되는 항목이다 —
> 검증 시점에 증거가 없으면 조용히 통과시키지 않는다.

### S1. 프로젝트 목록 (list-collection · interactive-control)

- **empty** — 데이터 0건과 필터 0건이 서로 다른 한 줄로 갈린다: 전자는 `ListEmpty` + `href`(`?new=1`), 후자는 「필터 지우기」. 그림·일러스트 없음. (Copywriting `Empty — 프로젝트 목록`·`Empty — 목록 필터 0건`, Component Inventory `ListEmpty` 행, §7-7 EMPTY)
- **loading** — 목록은 머리글·합계 행 뼈대 + `--surface` 행 3개로만 그려지고 스피너·스켈레톤 반짝임이 없으며, 300ms 안에 끝나면 아무것도 보이지 않는다(지연 표시). 「더 보기 50건」·정렬·필터는 §7-1 버튼 규칙(라벨 뒤 `…`, 300ms 후 상단 2px 진행 바). (§7-7 LOADING 행 + 표 행)
- **error** — 표 본문 자리에 `--danger` 한 줄 + `다시 시도` 하나, 사과 문구 없음. (Copywriting `Error — 목록`, §7-7 ERROR)
- **populated** — 125건 기준: 종료일 월 그룹 머리글 + 9열(계급에 따라 열 수 가변) + 합계 행 `합계 (전체 · 125건)` + 합계 행 위 「더 보기 50건」. 합계는 불러온 50건이 아니라 필터 전체의 집계이고 부제의 건수가 그 사실을 말한다. (S1 본문)
- **partial** — 계급이 낮아 서버가 견적·실행가·차익 열을 보내지 않으면 빠진 폭을 문자 열이 흡수하고 합계 행은 남은 열만으로 성립한다(클라이언트 숨김 금지). 종료일 없는 건은 맨 아래 `기간 미정` 그룹, 폰 P1 금액은 견적이 없으면 실행가. 값이 있는데 비어 있는 칸만 `—`이고 보내지 않은 칸은 `—`로 채우지 않는다. (S1 본문, §7-3 칸 수 가변, §2-4)
- **overflow** — 세로는 페이지네이션 없이 「더 보기 50건」 3차 버튼으로 늘어나고, 가로는 375px에서 칸 접기로 스크롤 0이며, 숫자는 어떤 폭에서도 꺾지 않고 열을 접는다. (S1 본문, §6-1, S11, §2-4)
- **zero-one-many** — statement: 0건은 EMPTY 한 줄로 갈리고 1건과 125건이 같은 그룹 머리글 · `합계 (전체 · N건)` 형식을 쓰며 조수사가 바뀌지 않는다(§2-4 건수 형식) — N=0/1/125 렌더로 확인. · verification: backstop
- **long-text** — 긴 프로젝트명은 `word-break: keep-all`로 줄바꿈되고 행 높이가 따라 늘며(고정 높이 금지), 클라이언트 부제·거래처는 한 줄 말줄임이다. (Typography 문자 열 규칙, §7-3 행·열 폭)

### S2. 프로젝트 등록 폼 (form)

- **empty** — 폼 EMPTY는 「해당 없음」으로 이미 닫혀 있다(폼은 항상 칸이 있다). 진짜 빈 데이터인 Select 옵션 0개는 첫 옵션이 `—`이고 제출이 막히며 이유가 `Form.Actions`에 쓰인다. (§7-15 다섯 상태 EMPTY 행, §7-15 `Select` 항목)
- **loading** — 초기 렌더에 클라이언트 로딩 구간이 없다(서버 컴포넌트가 기본값을 채운다). 제출 중에만 1차 라벨 뒤 `…` + 같은 폼의 다른 버튼 비활성, 300ms 넘으면 상단 2px 진행 바. (§7-15 LOADING 행, §7-7 버튼 행)
- **error** — 서버 필드 오류는 해당 `Form.Error` 한 줄, 폼 전체 오류는 `Form.Actions`의 이유 자리. 폼 상단 오류 상자를 두지 않고 입력값은 오류 뒤에도 남는다(UX-04). 문구는 Copywriting `Error — 등록 폼 제출`. (§7-15 검증 관문)
- **partial** — 필수 4칸 중 하나라도 비면 제출 버튼이 비활성 + 옆에 막힘 한 줄(Copywriting `막힘 — 등록 폼 필수`)이고 `*` 표시를 쓰지 않는다. 복사 등록은 기간·번호·총 매출 예상가·매출 칸·연결 문서가 따라오지 않은 부분 채움 상태로 열리고 폼 위 한 줄이 출처를 밝힌다. (§7-15 PARTIAL 행, S2 복사 등록)
- **long-text** — statement: 긴 프로젝트명·긴 값을 넣어도 칸 폭이 3종(select 200 · 짧은 칸 280 · 긴 칸 480)을 벗어나지 않고 폼이 `--form-max` 720을 넘지 않는다 — 최장 입력으로 확인. · verification: backstop

### S3. 프로젝트 상세 골격 (static-content · interactive-control)

- **loading** — 상세는 서버 컴포넌트 렌더라 초기 로딩 구간이 없고, 「일괄 저장 ⌘S N」 등 서버 액션 중에만 §7-1 진행 상태(라벨 뒤 `…` + 다른 버튼 비활성, 300ms 후 상단 2px 진행 바)가 보인다. (§7-15 LOADING 행, Component Inventory `Button` `pending`)
- **error** — 화면 로드 실패는 Copywriting `Error — 상세` 한 줄 + `다시 시도`. 저장 실패는 화면 전체 거부이고 결과는 고친 자리에 붙는다(표는 그 표 합계 행, 표 밖 칸은 칸 아래). ((사), §7-7 ERROR)
- **overflow** — ⚠ unresolved — planner must treat as assumption. 하단 힌트 줄은 「최대 7개 · 폰에서 숨는다」로 닫혀 있으나(§7-9), 머리 줄(제목 + 부제 4항목 + 상태 태그 + 버튼 4개)이 좁은 폭·375px에서 줄바꿈하는지 버튼군이 접히는지를 스펙이 말하지 않는다.
- **long-text** — statement: 상세 제목의 긴 프로젝트명은 말줄임 없이 `word-break: keep-all`로 줄바꿈되고 부제·상태 태그가 그 아래로 밀리지 않는다(Typography가 프로젝트명을 명시) — 40자 이상 이름으로 확인. · verification: backstop

### S4. 견적 줄 표 (list-collection · form)

- **empty** — 표 본문 자리 한 줄 `이 프로젝트에 견적 줄이 없습니다 · 첫 줄 만들기 ⌘↵`이고 그 3차는 `onClick`(이동이 아니다). (Copywriting `Empty — 견적 줄 표`, Component Inventory `ListEmpty` 행, §7-7 EMPTY)
- **loading** — 머리글·합계 행 뼈대 + `--surface` 행 3개, 반짝임 없음, 300ms 지연 표시. 저장 중은 화면 1차 버튼 하나에서만 보인다. (§7-7 표 LOADING, §7-1)
- **error** — 오류 셀은 `--danger-weak` 배경 + 좌측 2px 인셋으로 **고정**되고(다른 셀을 편집해도 남는다) 이유 한 줄이 셀 아래 붙으며, 합계 행 오른쪽에 `오류 1칸 · 전부 거부`(충돌이면 `충돌 1줄 · 전부 거부`)가 온다. 셀 오류 다섯 갈래(형식·환율·읽기 전용·잠김·충돌) 문구는 Copywriting `Error — 셀(…)` 행. 로드 실패는 `Error — 상세` 한 줄. (§7-3, (나))
- **populated** — 대분류 그룹 머리글 + 11열 + 합계 행 `합계 (공급가액 · 6줄)`. 외화가 있는 행만 금액 셀이 두 줄이 되고 행 높이가 그 셀을 따른다(고정 높이 금지). (S4 본문, §7-3 외화 병기)
- **partial** — 값이 없는 거래처는 `—`, 수량 기본 1, 환율이 없으면 표시값 `계산 불가 · 환율 없음` + `계산 불가 N건` 배지이고 합계는 계산된 것만 더한다(빈 자리를 0으로 채우지 않는다). 붙여넣기가 오른쪽을 넘치면 합계 행에 `붙여넣기 · 오른쪽 2칸 버림`. 한 표에 편집·읽기 전용·잠김 셀이 섞여도 머리글 바탕은 화면 안에서 바뀌지 않는다. ((가), (다), Copywriting PARTIAL 행, §7-7 PARTIAL)
- **overflow** — 비고는 PC 한 줄 말줄임, 폰에서는 P3으로 내려가 행 시트에서 읽는다. 폰 P1은 항목 · 실행가 · 상태 3열이고 375px 가로 스크롤 0이다. (S4 본문, D-64, §7-3 폰 전략, S11)
- **zero-one-many** — statement: 0줄은 EMPTY 한 줄로 갈리고, 1줄과 6줄이 같은 그룹 머리글 + `합계 (공급가액 · N줄)` 형식으로 렌더되며 조수사가 바뀌지 않는다 — N=0/1/6 렌더로 확인. · verification: backstop
- **long-text** — 항목명은 keep-all 줄바꿈으로 행 높이를 늘리고, 거래처는 한 줄 말줄임, 자유 텍스트 비고도 PC에서 한 줄 말줄임이다. 숫자는 어떤 폭에서도 줄바꿈하지 않는다. (Typography 문자 열 규칙, S4 본문, §2-4)

### S5. 차수 섹션 (list-collection)

- **empty** — 0행 상태가 존재하지 않는다: 프로젝트를 등록하면 상세 견적 1차가 항상 함께 생기므로 EMPTY 자리를 만들지 않는다(빈 차수 금지). (Copywriting `Empty — 차수 섹션`, D-53)
- **loading** — 섹션 단독 로딩이 없고 상세 화면 한 번의 로딩에 들어간다. 그 형태는 §7-7 표 LOADING(머리글·합계 뼈대 + `--surface` 행 3개)과 같다. (§7-15 LOADING 행, §7-7 표 행)
- **error** — statement: 차수 섹션은 제 오류 자리를 갖지 않고 상세 로드 실패 한 줄(Copywriting `Error — 상세`)에 흡수된다 — 섹션 단독 오류 렌더가 없음을 확인. · verification: backstop
- **populated** — 6열(차수·생성일·줄 수·견적 합계·상태·동작) · 최신이 위 · 상태 열은 `현재`/`승인` 두 낱말 · 이전 차수 행에만 「차수 열기」. 편집 가능한 셀이 0이므로 `role="grid"`가 아니라 `<table>` + 시각적으로 숨긴 `<caption>`이고 머리글은 흰 바탕이다. (S5 본문, (가))
- **partial** — ⚠ unresolved — planner must treat as assumption. 현재 차수 행의 동작 칸을 `—`로 채우지 않는 것은 확정이지만((가)·§7-3), 상태 열의 두 경계 — 최신이면서 고객 승인까지 된 차수의 칸, 그리고 승인되지 않은 이전 차수의 칸 — 에 어떤 낱말이(또는 빈 값이) 오는지는 정해지지 않았다.
- **overflow** — P1 = 차수 · 견적 합계 · (동작), P2 접힌 줄 = 상태 · 생성일 · 줄 수, P3 없음. 375px 가로 스크롤 0이고 `현재` 행의 빈 동작 칸을 `—`로 채우지 않는다. (S5 열 우선순위 선언, §7-3 폰 전략, S11)
- **zero-one-many** — 0은 구조적으로 없고(위 empty), 차수가 하나뿐이면 `현재` 한 행에 동작 칸이 비어 「차수 열기」가 표에 하나도 없으며, 여럿이면 최신이 맨 위이고 그 아래 행에만 버튼이 붙는다. (S5 본문, D-53·D-54)

### S6. 매출 섹션 (form · list-collection)

- **empty** — 발행 줄 표와 입금 줄 표가 각각 제 EMPTY 한 줄 + 제 3차 추가 버튼을 갖고, 계약 금액 칸은 폼이라 EMPTY가 없다. (Copywriting `Empty — 발행 줄 / 입금 줄`, §7-15 EMPTY 행)
- **loading** — 두 표 모두 §7-7 표 LOADING 뼈대이고 계약 금액 칸은 서버 렌더라 로딩 구간이 없다. 저장 중 표시는 화면 1차 버튼 하나에만 난다. (§7-7 표 행, (사))
- **error** — 표 셀 오류는 그 표 합계 행에 `오류 1칸 · 전부 거부`로, 계약 금액 칸 오류는 칸 아래 `Form.Error` 한 줄(「원인 · 다음 행동」)로 나오고, 어느 쪽이든 화면 전체가 거부된다. ((사), S6 본문, Copywriting `Error — 표 배치 저장 실패`)
- **populated** — 계약 금액 칸 + `Form.Hint` 서버 계산 한 줄 + 발행·입금 두 표. 입금 셀은 통장 합계 1행 + 공급가액 역산 2행이고 외화면 같은 2행에 이어 쓴다(3행으로 늘리지 않는다). (S6 본문, §2-4 외화 두 줄 병기)
- **partial** — 입금 합계 ≠ 발행 합계면 입금 표 합계 행 오른쪽에 `미수` 또는 `초과 입금`(`--warning`, 넓은 면을 칠하지 않는다). 기획본부에게는 두 표가 **표 단위로** 빠지고 섹션에 계약 금액만 남는다. PM이 아닌 사람에게 계약 금액 칸은 읽기 전용이라 dirty가 될 수 없고 `N`에 세어지지 않는다. (S6 본문, D-57·D-58)
- **overflow** — statement: 발행·입금 표는 열이 셋(발행일/입금일 · 금액 · 메모)이라 §7-3 P1 최대 3열 안에 전부 들어가고 375px에서 가로 스크롤이 0이다 — DOM 감사로 확인. · verification: backstop
- **zero-one-many** — ⚠ unresolved — planner must treat as assumption. 발행 줄이 있고 입금 줄이 0일 때 입금 표 본문 자리는 EMPTY 한 줄이 되는데, 미수 금액이 붙을 **입금 표 합계 행이 그때도 렌더되는지**(EMPTY 한 줄과 합계 행의 공존)를 스펙이 말하지 않는다.
- **long-text** — statement: 발행·입금 표의 메모 셀은 긴 칸(480) 규칙 아래 PC에서 한 줄 말줄임으로 렌더되고 행 높이를 늘리지 않는다(S4 비고와 같은 취급) — 긴 메모 DOM 감사. · verification: backstop

### S7. 상태 전환 (interactive-control)

- **loading** — 확인 모달의 1차는 제출 중 라벨 뒤 `…` + 같은 모달의 다른 버튼 비활성, 300ms 넘으면 상단 2px 진행 바. 모달·시트 자체에는 LOADING이 없다(이미 로드된 데이터를 여는 상호작용). (§7-7 버튼 행 · 시트/모달 행, §7-1)
- **error** — 화면이 이유를 따로 만들지 않고 `domain/rules.gate`가 돌려준 문자열을 §7-1 막힘 자리(1차 버튼 옆)에 그대로 쓰며 모달·시트는 닫히지 않는다. 실패에는 토스트를 띄우지 않는다(토스트는 이동이 따르는 SUCCESS 전용). (S7 본문, §7-7 시트/모달 ERROR, Copywriting `막힘 —` 행, §7-6)
- **long-text** — statement: 모달·시트에 들어가는 문자열은 Copywriting `막힘`/`Destructive` 행의 고정 문구 + 짧은 보간값(담당자 이름 · 차수 · 금액)뿐이고 `--modal-w` 480 안에서 잘리지 않는다 — 가장 긴 조합(`2차 고객 승인 전 · 담당 PM {이름}`)으로 확인. · verification: backstop

### S8. 폰 행 시트 (static-content · interactive-control)

- **loading** — 시트에는 LOADING이 없다. 본문은 그 행에서 접힌 P2·P3 값이라 시트를 열 때 새 요청이 나가지 않는다. (§7-7 시트/모달 행, (바), §7-3 폰 칸 접기)
- **error** — 보기 전용이라 확인 행동이 없고(행동 줄 없음 · 폰 셀 편집 없음) 제출·조회 실패 상태를 갖지 않는다. §7-7 시트/모달 ERROR가 말하는 「확인 행동 실패」가 이 시트에는 발생하지 않는다. ((바), §6-2)
- **overflow** — 머리(제목 · 부제 · 닫기 x 20)는 고정, 본문만 스크롤, 높이는 내용 따라 최대 85vh. (§7-8 시트)
- **long-text** — statement: 긴 비고가 시트에서 말줄임 없이 전문으로 나오고(표의 PC 말줄임이 시트에 적용되지 않는다) 85vh를 넘으면 본문 스크롤로 읽힌다 — 500자 비고로 확인. · verification: backstop

### S9. 리저브 대장 (list-collection · form)

- **empty** — 표 본문 자리 한 줄 `리저브 기록이 없습니다 · 리저브 줄 추가`. (Copywriting `Empty — 리저브`, §7-7 EMPTY)
- **loading** — §7-7 표 LOADING(머리글·합계 뼈대 + `--surface` 행 3개, 반짝임 없음, 300ms 지연 표시). 저장 중은 1차 「일괄 저장 ⌘S N」에서만 보인다. (§7-7 표 행, §7-1)
- **error** — 로드 실패는 `리저브 대장을 불러오지 못했습니다 · 다시 시도` 한 줄. 어느 날짜든 잔액이 음수가 되면 서버가 거부하고 그 줄의 금액 셀이 오류 셀로 고정되며 이유는 Copywriting `Error — 리저브 잔액` 행, 배치 전체는 전부 거부된다. (S9 본문, D-60, §7-3)
- **populated** — 클라이언트 그룹 머리글 + 머리글 행 오른쪽 잔액(서버 계산, 700) + 8열 · 날짜순. 잔액 열은 계산값이라 항상 읽기 전용이고 `--muted`가 아니라 `--fg`다. (S9 본문, D-60)
- **partial** — 선택 열(프로젝트)이 빈 줄은 `—` 하나로 렌더되고 빈칸을 두지 않는다. 잔액은 계산값이라 비는 일이 없고, 권한이 없으면 행이 아니라 **손익 메뉴 자체가 없다**(부분 마스킹이 아니다). (§2-4 「0과 빈 값」, §7-3 칸 수 가변, S9 본문·D-59)
- **overflow** — 폰 P1 = 날짜 · 금액 · 구분, P2 접힌 줄 = 프로젝트 · 잔액 · 메모, 나머지(증빙 종류 · 세금계산서 번호)는 P3으로 숨고 375px 가로 스크롤 0이다. 세금계산서 번호는 `tabular-nums` + `nowrap`이라 꺾이지 않는다. (S9 본문, §7-3 폰 전략, S11, §2-4)
- **zero-one-many** — statement: 0건은 EMPTY 한 줄, 1건에서도 클라이언트 그룹 머리글 + 오른쪽 잔액이 그대로 렌더되고, 50건을 넘으면 §6-1의 「더 보기 50건」 3차 버튼으로 이어 받는다(S9가 §6-1을 상속한다) — N=0/1/50+ 렌더로 확인. · verification: backstop
- **long-text** — statement: 리저브 메모 셀은 긴 칸(480) 규칙 아래 PC에서 한 줄 말줄임으로 렌더되고 행 높이를 늘리지 않는다(S4 비고와 같은 취급) — 긴 메모 DOM 감사. · verification: backstop

### S10. 문서 번호 표시 (static-content)

- **overflow** — 번호 열은 자릿수 고정 폭(`ch`)이고 `white-space: nowrap`이라 어떤 폭에서도 줄바꿈하지 않으며, 폭이 모자라면 숫자를 꺾는 대신 열을 접는다(목록의 번호 열은 폰 P3). (S10 본문, §2-4, §7-3 폰 전략)
- **long-text** — 문서 번호는 서버 서식 그대로의 고정 길이 문자열(`26001` · `26001-1차`)이고 모노 서체가 아니라 `tabular-nums`이며 말줄임·줄바꿈을 적용하지 않는다. 결번이 생겨도 화면에 설명을 덧붙이지 않는다. (S10 본문, §2-4 문서 번호, D-42)

### 프로브 항목 밖 — 체커가 남긴 경계 3건 (여전히 미해결)

아래 셋은 8개 카테고리 × 표면 조합 어디에도 떨어지지 않아 57건에 세어지지 않았다. 그래도
계획 단계가 가정으로 다뤄야 한다.

- **「일괄 저장 ⌘S N」의 `N` = 0** — ⚠ unresolved — planner must treat as assumption. dirty가 0칸일 때 라벨이 `일괄 저장 ⌘S 0`인지 숫자를 떼는지, 버튼이 비활성인지(그렇다면 §7-1 「이유 있는 비활성」의 이유 문구가 무엇인지) 스펙에 없다.
- **편집 가능한 셀이 0인 사람에게 1차 저장 버튼을 렌더하는가** — ⚠ unresolved — planner must treat as assumption. (가)는 머리글 바탕과 `role` 파생만 정하고, 그 사람의 머리 줄에 「일괄 저장 ⌘S N」이 아예 안 오는지 `N`=0으로 오는지는 정해지지 않았다(그 화면의 1차가 0개가 되는 경우이기도 하다).
- **표 밖 칸 하나의 오류가 화면 전체를 거부할 때 각 합계 행의 문구** — ⚠ unresolved — planner must treat as assumption. (사)는 「각 표의 합계 행에도 거부가 표시된다」까지만 정했고, 그 표 안의 오류가 0칸인데 `오류 1칸 · 전부 거부`를 그대로 쓸 수는 없다 — 그 자리의 실제 문자열이 Copywriting Contract에 없다.

---

## SYSTEM.md §7 보강·신설 초안 (이 페이즈의 핵심 산출물)

> 아래는 `docs/design/SYSTEM.md`에 그대로 삽입될 초안이다. 기존 §7-1~§7-14와 같은 문체·구조를
> 따른다. 코드 작성 전에 실제 `SYSTEM.md`에 옮겨 적고 `DECISIONS.md`에 남기는 것이 계획 단계의 첫
> 태스크여야 한다(CLAUDE.md 프론트엔드 규칙). `DECISIONS.md` 항목은 **넷**이다:
> ① §7-3 보강(편집 표 구현 계약 여덟 항목) · ② §7-5 보강(위 Color 절 — **의미 목록 낱말 다섯 +
> 길이 규칙 한 줄**) · ③ §7-8 보강(2차 라벨 충돌 규칙) · ④ §7-15 신설(폼·셀렉트 — **`Form.Actions`를
> 두는 조건 포함**). 넷 모두 시스템을 고치는 개정이고, 어느 것도 화면 하나만의 예외로 두지 않는다.

### 7-3 보강 — 편집 표의 구현 계약 (Phase 4)

§7-3 본문(구조·편집 키·외화 병기·폰 칸 접기·칸 수 가변)은 그대로 둔다. 아래 여덟 항목을 그 절 끝에
더한다. 표는 **자체 구현**이다 — HTML `<table>` + 키보드 포커스 이동·범위 선택·붙여넣기를 직접 쓰고
새 런타임 의존성을 더하지 않는다(D-61).

**(가) 셀 편집 가능성 3단계 — 서버가 셀마다 판정해 보낸다(클라이언트 추론 금지)**

| 단계 | 언제 | 모양 | ARIA |
|---|---|---|---|
| 편집 | 기본 | 흰 셀. 클릭 또는 Enter로 편집 | `aria-readonly="false"` |
| 읽기 전용 | 연결 문서가 있는 줄의 금액 셀(수량·단가·실행가·환율) — 항목명·비고는 편집(D-66). 계산 열(견적가·차익·잔액)도 항상 이 단계 | 값만, 배경 없음. 포커스는 되지만 편집에 들어가지 않는다. 편집 시도 시 셀 아래 한 줄 `--fs-sm --muted` 이유(§7-2와 같은 자리) | `aria-readonly="true"` |
| 잠김 | 완료(정산) 프로젝트의 견적 줄 전체(D-47) | 읽기 전용과 같고, 표 위에 한 줄 `--fs-sm --muted` `완료(정산) · 견적 줄이 잠김` | 〃 |

- **머리글 바탕은 "이 사람이 이 표에서 편집할 수 있는 셀이 하나라도 있는가"로 정한다.** 있으면
  `--g-100` + `--g-950`(편집 가능 신호), 없으면 흰 바탕(읽기용 표). 완료 프로젝트를 보는 PM은 흰
  바탕, 같은 화면을 보는 경영관리는 `--g-100`(「견적 외 비용 줄 추가」가 가능하므로)이다. 사람마다
  다르고 **화면 안에서 바뀌지 않는다**.
- 편집 가능한 셀이 하나도 없는 표는 `role="grid"`가 아니라 `<table>` + 시각적으로 숨긴 `<caption>`로
  렌더한다(§10).
- **이것은 모드 토글이 아니다.** §7-3 본문의 「읽기 모드와 편집 모드를 나누지 않는다 — 편집 권한이
  있으면 표는 항상 편집 가능하다」는 그대로다. 위 두 줄은 사용자가 켜고 끄는 모드가 아니라 **편집
  가능한 셀 수가 0일 때 컴포넌트가 자동으로 취하는 렌더 형태**이고, 그 판정은 서버가 보낸 셀 단계에서
  파생된다. 이 문서에서 「`ui/table` 읽기 모드」라고 적은 곳은 전부 이 파생 형태를 가리킨다.

**(나) 줄 버전 충돌(D-65)**

- 각 줄은 `version`을 함께 받고 함께 보낸다. 저장 요청이 읽은 버전과 다르면 **그 줄에서 실제로 값이
  달라진 셀만** §7-3 충돌 모양(오류 셀과 같은 고정)으로 표시하고, 이유 한 줄에 **실제 바뀐 값·사람·
  시각**을 적는다: `다른 사람이 14:01에 9,800,000으로 바꿈 · 덮어쓰기 / 그 값으로`. 두 다음 한 수는
  3차 버튼이다.
- 충돌이 **하나라도** 있으면 §7-3 「전부 저장 또는 전부 거부」대로 배치 전체를 거부하고 합계 행에
  `충돌 1줄 · 전부 거부`. 다른 셀의 편집값은 그대로 남는다.

**(다) 붙여넣기 정규화(D-67)**

- 클립보드 `text/plain`을 TSV로 읽는다(행 `\n`, 칸 `\t`). 활성 셀에서 오른쪽·아래로 채운다.
- 숫자 열: 쉼표·공백·통화 기호를 지우고 숫자로 읽는다. 그래도 숫자가 아니면 **그 셀을 오류 셀로
  고정**한다(조용히 버리지 않는다).
- 문자 열: 그대로, 앞뒤 공백만 지운다.
- 읽기 전용·잠김 셀에 값이 떨어지면 **건너뛰지 않고 오류 셀로** 표시한다 — 조용히 버리면 값이
  유실된 것처럼 보인다.
- 아래를 넘으면 새 줄 자동(§7-3 원문). **오른쪽을 넘는 칸은 버리고** 합계 행에
  `붙여넣기 · 오른쪽 2칸 버림`(`--warning`)을 남긴다.
- ⌘C 범위 복사도 같은 TSV를 클립보드에 쓴다 — 그래서 **다른 프로젝트의 표에서 그대로 붙는다**
  (별도 가져오기 모달을 만들지 않는 이유, D-70·PROJ-05).

**(라) 줄 순서와 그룹 머리글(D-62·D-63)**

- 그룹 머리글 = 대분류. 줄의 **소분류 열 값에서 파생**되고 그룹 순서는 코드표 `sort_order`다. 대분류
  열을 따로 두지 않는다.
- 그룹 머리글 행은 편집 대상이 아니고 포커스를 받지 않는다 — 방향키가 건너뛴다.
- 그룹 안에서 만든 새 줄(⌘↵)은 그 그룹의 대분류를 물려받는다(소분류가 미리 채워지고 그 셀이 편집
  상태로 열린다).
- 줄 이동은 `Alt+↑` / `Alt+↓`. 그룹 경계를 넘으면 소분류가 비워지고 그 셀이 편집 상태로 열린다.
- 줄 순서는 입력 순서 값으로 저장한다(정렬이 아니라 값이다).

**(마) 미저장 편집 복원(D-68)**

- dirty 셀이 하나라도 있으면 `beforeunload`로 이탈을 경고한다.
- 편집은 브라우저 저장소에 임시 보관한다. 키는 `프로젝트 id + 차수 id`(리저브는 화면 id). 저장
  성공·「버림」에 지운다.
- 다시 열었을 때 보관된 편집이 있으면 **표 위 한 줄**로 알린다(`--fs-sm`):
  `저장 안 한 편집 3칸 · 복원 / 버림`, 둘 다 3차 버튼. **§7-11 배너가 아니다** — 배너는 화면 제목 위
  전역 조건의 자리이고 이것은 한 표에 붙은 상태다.
- 「복원」하면 그 셀들이 dirty 모양으로 들어온다. 그 사이 줄의 `version`이 올라갔으면 저장 시점에
  (나)의 충돌로 간다. 「버림」은 확인 모달 없이 즉시(저장된 값은 그대로다).

**(바) 폰 행 시트(D-69)**

- §7-8 시트 골격. 머리 = 항목명(`--fs-lg`) + 부제 `{소분류} · {거래처}`.
- 본문 = `KvList`(`dl`)로 접힌 P2·P3 전부: 수량 · 단가 · 견적가 · 차익 · 환율 · 비고 · 상태. 외화는
  값 아래 2행(`--fs-sm --muted`).
- **행동 줄을 두지 않는다 — 보기 전용이다.** 「지출결의 올리기」는 그 문서가 생기는 페이즈가 더한다.
- 폰에서 셀 편집은 없다(§6-2). P1 3열은 **항목 · 실행가 · 상태**(D-64).

**(사) 저장 범위와 1차 버튼**

- 화면의 1차 버튼 **「일괄 저장 ⌘S N」 하나**가 그 화면의 **모든 편집 표**(견적 줄 + 매출 발행 줄 +
  입금 줄) **+ 표 밖의 편집 칸(매출 계약 금액)**의 dirty를 **한 트랜잭션**으로 저장한다.
  `N` = 화면 전체 dirty 칸 수(표 셀 + 계약 금액 칸).
- **저장 범위는 「이 화면에서 고칠 수 있는 것 전부」이지 「표」가 아니다.** 표 밖에 편집 칸이 하나라도
  있으면 그 칸도 같은 1차 버튼·같은 트랜잭션에 들어간다. 그러지 않으면 그 칸이 제 저장 버튼을 갖게
  되고 한 화면에 1차가 둘이 된다(§7-1 「한 화면에 1개」).
- 결과는 **고친 자리에 붙는다**(§7-7 SUCCESS): 표의 dirty는 그 표의 합계 행에, 표 밖 칸의 dirty는 그
  칸 자리에(성공 = 600ms `--accent-weak` 틴트, 오류 = 칸 아래 `Form.Error` 한 줄).
- 오류·충돌이 한 칸이라도 있으면 **화면 전체가 거부**된다(§7-3 「전부 저장 또는 전부 거부」). 표 밖
  칸의 오류도 같은 거부에 걸리고, 각 표의 합계 행에도 거부가 표시된다.
- 쓰기 권한이 갈려도(계약 금액 = PM, 발행·입금 = 경영관리, D-57) 버튼은 하나다 — 권한 판정은
  서버가 칸마다 하고(위 (가)), 한 사람에게 dirty가 되는 칸은 애초에 그 사람이 고칠 수 있는 칸뿐이다.
- 표가 여럿이어도 저장 자리는 하나다 — 사용자가 「저장」의 위치를 하나만 기억한다.

**(아) `role="grid"` 키보드 계약(§10 보강)**

- 표 전체가 탭 정지 **1개**. 안에서는 방향키 로빙 `tabindex`로 움직인다.
- `Esc`: 편집 중이면 그 셀 값을 되돌리고, 편집 중이 아니면 범위 선택을 해제한다.
- `Delete`: 줄 삭제 확인 모달(§7-3 원문). 셀 값 비우기는 편집에 들어가 지운다.
- 오류·충돌 셀은 `aria-invalid="true"` + `aria-describedby`로 이유 줄을 가리킨다. 합계 행의 저장
  결과는 `aria-live="polite"`.
- 힌트 줄(§7-9, 최대 7개)은 이 화면에서 정확히 일곱이다:
  `이동 Tab ↑↓←→ · 범위 복사 ⌘C / 붙여넣기 ⌘V · 취소 Esc · 새 줄 ⌘↵ · 줄 이동 Alt↑↓ · 줄 복제 ⌘D · 저장 ⌘S`.
  지출결의 `⌘E`가 붙는 페이즈에서 `줄 복제 ⌘D`를 뺀다(일곱을 넘지 않는다).

### 7-8 보강 — 2차 라벨이 1차와 겹칠 때 (Phase 4)

§7-8은 확인 모달·시트의 2차를 「취소 Esc」로 고정한다. 그런데 **취소하는 것이 행동 자체인 모달**이
이 페이즈에서 둘 생긴다(`견적 줄 취소` · `승인 표시 취소`). 같은 줄에 「견적 줄 취소」와 「취소 Esc」가
나란히 서면 어느 쪽이 되돌리기인지 읽히지 않는다. 아래 한 줄을 §7-8에 더한다.

- **1차 라벨에 「취소」가 들어가면 그 모달·시트의 2차는 「닫기 Esc」로 적는다.** 그 밖에는 언제나
  「취소 Esc」다. 라벨만 바뀌고 동작(Esc · 포커스 복귀 · 아무 일도 일어나지 않음)은 그대로다.
- 화면별 예외가 아니라 **1차 라벨에서 기계적으로 파생되는 규칙**이다 — 앞으로 「반려 취소」·
  「예약 취소」 같은 모달이 생겨도 같은 자리에서 같은 답이 나온다.

### 7-15. 폼 (`ui/form` · `ui/select`) — 신설

§6-3이 폼 **화면**의 레이아웃이고 §7-2가 **입력 하나**의 규칙이라면, 이 절은 그 둘을 잇는 **컴포넌트
계약**이다. Phase 4가 만들고(**사용처 둘 — 프로젝트 등록 폼 · 매출 계약 금액 칸.** 리저브 줄은 폼이
아니라 §7-3 편집 표다, S9) **Phase 7**이 관리자 폼 8개와
`/account`를 이 계약으로 이관한다(03-OPEN-ITEMS A-H2·A-H3).

**구성**

- `Form` — `<form noValidate>` + 한 열, `max-width: var(--form-max)`(720), 왼쪽 정렬. 항목 사이
  `--s-4`, 묶음 사이 `--s-6` + `1px --line`.
- `Form.Field` — 라벨 열(PC `--label-w` 96, `--fs-sm --muted` 600) + 컨트롤. 폰(<700px)에서 라벨이
  위로 올라가고 컨트롤은 전폭.
- 칸 폭 **3종만**: select 200 · 짧은 칸(금액·날짜·수량·환율) 280 · 긴 칸(이름·비고) 480.
- `Form.Hint` — 컨트롤 바로 아래 한 줄 `--fs-sm --muted`, 숫자만 700. **서버 계산 값의 자리**(§6-3):
  `부가세 10% 4,800,000 · 합계 52,800,000 · 서버 계산`. 읽기 전용 행을 따로 두지 않는다.
- `Form.Error` — 컨트롤 아래 한 줄 `--fs-sm --danger` 「원인 · 다음 행동」. `TextField`의
  `aria-describedby`가 이 줄을 가리킨다.
- `Form.Actions` — 폼 맨 아래 한 줄: 1차(제출) + 옆에 막힘 이유(`--fs-sm --danger`) + 다음 한 수 3차,
  오른쪽 끝에 2차. 1차 버튼의 `aria-describedby`가 이유를 가리킨다(`role="alert"`가 아니다 — 누르기
  전에 읽혀야 한다, §10).
- **`Form.Actions`는 폼이 자기 제출을 가질 때만 둔다.** `Form.Actions`는 1차 버튼을 데려오므로, 이미
  1차를 가진 화면 안에 섞여 들어간 칸에 이것을 두면 한 화면에 1차가 둘이 된다(§7-1 「한 화면에 1개」,
  §11 `/design-review`가 잡는다). 그런 칸은 `Form.Field` + `Form.Hint` / `Form.Error`만 쓰고
  **`Form.Actions` 없는 단일 칸 폼**으로 렌더한다 — 제출은 그 화면의 1차 「일괄 저장 ⌘S N」 하나이고,
  칸이 dirty면 `N`에 1칸으로 합류해 §7-3 「전부 저장 또는 전부 거부」에 같이 걸린다(위 (사)).
  `<form>` 요소는 그대로 두되 `Enter` 기본 제출을 막고 ⌘S에 맡긴다.
  **이것은 화면별 예외가 아니라 §7-15의 일반 규칙이다** — 「이 칸의 저장은 누구 책임인가」를 폼이
  아니라 화면이 답한다. 대안으로 §7-2 설정 화면식 blur 즉시 저장도 검토했으나, 같은 화면의 표가
  ⌘S 일괄 저장이라 한 화면에 저장 의미가 둘이 되고 「전부 저장 또는 전부 거부」가 칸 하나를 비켜
  가므로 접었다.
- `Select` — 네이티브 `<select>` + 시스템 테두리(§7-2). 커스텀 드롭다운을 만들지 않는다. 고를 것이
  없으면 첫 옵션이 `—`이고 제출이 막히며 이유는 `Form.Actions`에 쓴다.
- 자동으로 채워진 칸은 입력이 아니라 텍스트 + 「바꾸기」 3차 버튼(§6-3).

**검증 관문은 서버 하나** (A-H3 해소)

- 네이티브 `required`·`pattern`·`type="number"` **검증 위임을 걷어낸다.** 브라우저 기본 말풍선(영문·
  시스템 서체)이 §7-2의 「원인 · 다음 행동」을 가로채기 때문이다. `noValidate`를 `<form>`에 둔다.
- 필수 표시 `*`를 두지 않는다(§7-2). 필수인데 비면 **제출 버튼이 이유를 말한다**:
  `등록할 수 없음 — 클라이언트 · 담당 PM 2칸 · 클라이언트 고르기`.
- 서버가 돌려준 필드 오류는 해당 `Form.Error`에, 폼 전체 오류는 `Form.Actions`의 이유 자리에.
  **폼 상단 오류 상자를 두지 않는다**(§7-7 폼 행 "상단 한 줄 없음").
- 입력값은 오류 뒤에도 남는다(UX-04).

**다섯 상태**

| 상태 | 모양 |
|---|---|
| LOADING | **해당 없음** — 서버 컴포넌트가 기본값을 채워 렌더한다. 제출 중은 §7-1 버튼 규칙(라벨 뒤 `…`, 같은 폼의 다른 버튼도 비활성, 300ms 넘으면 상단 2px 진행 바) |
| EMPTY | **해당 없음** — 폼은 항상 칸이 있다 |
| ERROR | 위 「검증 관문」 |
| SUCCESS | 화면이 바뀌는 폼(등록 → 상세)은 이동 + §7-6 토스트. 화면이 그대로인 폼은 버튼 자리에 결과 텍스트, 입력은 읽기 전용 |
| PARTIAL | 제출 버튼 비활성 + 옆에 `…할 수 없음 — 이유 · 다음 한 수`(§6-3·UX-06) |

---

## 적용 규칙 (기존 템플릿에 붙는 화면별 계약 — 새 절 아님)

### S1. 프로젝트 목록 `/projects` (§6-1)

- 「내 차례」 블록 **없음**(§7-4는 첫 화면 전용). `app/(app)/projects/page.tsx`의 EMPTY 자리(D-22)를
  이 화면이 채운다.
- 필터 한 줄(표 위): `[전체 상태 ▾]`(기본값 = 전체, D-51) · `[팀 ▾]`(`scopeFor` 범위 안) · `[2026 ▾]`
  · 검색 `⌘/` · 오른쪽 1차 「프로젝트 등록」(`?new=1` 토글, D-39). 필터 패널·사이드바 없음.
- **그룹 머리글 = 종료일 기준 월**(D-51): `2026-09`. 종료일이 없는 수주중 건(D-49)은 맨 아래 그룹
  `기간 미정`.
- 열: 번호 · 프로젝트명(클라이언트 부제) · 담당 PM · 팀 · 기간 · 견적 · 실행가 · 차익 · 상태.
  견적·실행가·차익은 정보 노출표 항목 — **계급에 따라 서버가 열을 아예 보내지 않는다**(§7-3 칸 수
  가변, 클라이언트에서 숨기지 않는다). 빠진 열의 폭은 문자 열이 흡수한다.
- 폰 P1 3열: 프로젝트명 · 견적(없으면 실행가) · 상태. P2 접힌 줄:
  `클라이언트 · 담당 PM · 09-12 ~ 09-16`. P3(팀·번호)은 상세에서 본다.
- 합계 행: `합계 (전체 · 125건)` + 견적·실행가·차익. **합계는 불러온 50건이 아니라 필터 전체의 합**
  이다(집계 쿼리 1회) — 부제의 건수가 그 사실을 말한다. 「더 보기 50건」 3차 버튼은 합계 행 위.
- 열 머리글 클릭 = 정렬(§6-1). 정렬 방향 아이콘 16px.

### S2. 프로젝트 등록 `/projects?new=1` (§6-1 토글 + §7-15)

- 칸: 클라이언트(Select 200, 자동완성) · 프로젝트명(480) · 담당 PM(Select 200) · 팀(Select 200) ·
  시작일(280) · 종료일(280) · **총 매출 예상가**(280 + 통화 Select 200 + 환율 280).
- 필수는 클라이언트·프로젝트명·담당 PM·팀 넷(`docs/inputs` §9). **기간은 선택**(D-49) — 진행 전환
  게이트가 그때 말하므로 폼에 설명을 두지 않는다(§8 규칙 5).
- 「총 매출 예상가」는 사전 견적이다(D-52). 견적 줄이 아니라 프로젝트 속성이고 이 칸에서 지출결의를
  올릴 수 없다. 라벨은 사용자 용어 그대로.
- 상태·번호는 폼에 없다 — 등록은 항상 수주중이고 번호는 서버가 매긴다(D-42). 둘은 등록 후 상세
  머리 줄에서 처음 보인다.
- **폼이 열려 있으면(`?new=1`) 목록 필터 줄의 1차 「프로젝트 등록」은 렌더되지 않는다.** S1과 S2는
  §6-1·D-39에 따라 **같은 라우트**이므로, 그러지 않으면 한 화면에 1차 버튼이 둘 된다(§7-1 「한
  화면에 1개」 위반이고 §11 `/design-review`가 잡는다). 그 자리에는 아무것도 두지 않는다 — 돌아가는
  길은 폼 자신의 2차 「취소 Esc」다(§6-1 원문). 이 규칙은 `?new=1&copyFrom=`에도 같이 적용된다.
- **복사 등록**(D-70): `?new=1&copyFrom={id}`. 기본 정보 + 현재 차수 줄이 미리 채워지고 폼 위 한 줄
  `--fs-sm --muted` `26001 아이오닉9에서 복사 · 6줄`. 기간·번호·총 매출 예상가·매출 칸·연결 문서·줄
  계보는 따라오지 않는다. 별도 가져오기 모달은 없다.
- 제출 → 상세로 이동 + 토스트 `프로젝트 등록 · 26001`.

### S3. 프로젝트 상세 = 견적 원장 `/projects/[id]` (§6-2)

- 머리 줄: 제목 = 프로젝트명(`--fs-lg` 700) · 부제 = `26001 · 상세 견적 2차 · 고객 승인 2026-09-19
  박서연 · 상태 2026-09-19` + 상태 태그(§7-5 `tag`). 오른쪽 버튼군: 2차 「복사해 새 차수」 · 2차
  「프로젝트 복사」 · 2차 「상태 바꾸기」 · **1차 「일괄 저장 ⌘S N」**.
- **이 화면의 1차는 「일괄 저장 ⌘S N」 하나뿐이다.** `N`은 견적 줄 표 + 매출 발행·입금 표 + **매출
  계약 금액 칸**의 dirty를 전부 센다(위 (사)). 표 밖 칸도 여기로 들어오므로 섹션마다 저장 버튼이
  생기지 않는다 — 그래서 §7-1 「한 화면에 1개」가 유지된다.
- 「상태 바꾸기」는 팀장 이상에게만 **렌더된다**(D-46). PM에게는 비활성 버튼이 아니라 **버튼이 없다**
  — 상태 전환이 팀장의 일이라는 조직 규칙이고, 이유를 보여 줄 다음 한 수가 그 사람에게 없다.
  이것은 §7-1 「이유 있는 비활성」의 **예외가 아니라** 이 시스템이 이미 쓰는 권한·정보 노출 패턴의
  적용이다: **할 수 없는 사람에게는 서버가 그 컨트롤을 아예 보내지 않는다**(§7-3 「칸 수 가변」과
  같은 논리 — 클라이언트에서 숨기지 않는다). 같은 패턴이 이 페이즈에 셋 더 있다: 기획본부에게 매출
  발행·입금 표를 보내지 않는 것(S6) · 기획본부에게 리저브 메뉴가 없는 것(S9) ·
  `ui/permission-grid` 마스킹. 비활성 규칙은 **렌더된 버튼**에 적용되는 규칙이고, 보내지 않은 것은
  그 규칙의 대상이 아니다 — 그래서 `SYSTEM.md` 개정 네 건 어디에도 등록하지 않는다(새 규칙이 아니다).
- **네 숫자 줄(PNL-01)은 이 페이즈에서 렌더하지 않는다.** 그 자리의 마크업을 미리 만들지 않는다
  (Phase 9). 표가 머리 줄 바로 아래 붙는다.
- 탭 없음. 섹션(2px `--line-strong`으로 시작) 순서 **① 견적 줄 표 → ② 매출 → ③ 차수**. 연결 문서
  섹션은 그 문서가 생기는 페이즈가 더한다 — 빈 섹션을 미리 두지 않는다.
- 고객 승인(D-56)은 머리 줄 부제 옆 3차 버튼: 승인 전 `고객 승인 표시`(담당 PM에게만), 승인 후
  `승인 표시 취소`(연결 문서가 없을 때만). 승인일은 부제에 `고객 승인 2026-09-19 박서연`.
- 힌트 줄(§7-9)이 화면 하단에 항상 보인다. 폰에서는 숨는다.

### S4. 견적 줄 표 (§7-3 + 위 보강)

- 열(D-64): 번호(28px, `--fs-xs --faint`) · 소분류(Select) · 항목 · 거래처(비우면 `—`) · 수량(기본
  1) · 단가 · 견적가 · 실행가 · 차익 · 상태 · 비고.
- **견적가 = 수량 × 단가, 차익 = 견적가 − 실행가. 둘 다 서버 계산·저장이며 셀은 읽기 전용**(D-63,
  PROJ-02). 브라우저 계산값은 저장하지 않는다.
- 상태 열 값은 이 페이즈에서 **미착수 · 취소** 둘뿐(D-64). 지출결의 중·증빙 없음·지급 완료는 이후
  페이즈가 연결 문서에서 파생한다(저장 상태가 아니다).
- `견적 외 비용` 줄은 **별도 표가 아니라 견적가 0인 견적 줄의 한 종류**다(D-48, `docs/inputs` §8).
  소분류 자리에 그 이름이 오고 그룹 머리글도 `견적 외 비용`이 된다. 실행가 음수를 허용한다(EXP-14).
- 외화: 편집은 **외화 금액·환율 두 칸**, 원화는 계산값(§7-3). 통화 Select가 금액 칸 왼쪽에 있고
  KRW면 환율 칸이 숨는다(1 고정, D-71). 환율 기본값은 `fx.recent_rate.USD`에서 실제 값으로 채워진다
  (자리표시자가 아니다). 표시는 §2-4 두 줄 병기, 합계 행은 원화만.
- 비고는 자유 텍스트 한 열, PC 말줄임 · 폰 P3(D-64).
- 삭제(D-56): 연결 문서가 없으면 Delete → 확인 모달 → 보관함(`archived_at`). 있으면 삭제 대신 `취소`
  상태(견적가 0, 이력 유지)만, FK는 RESTRICT.

### S5. 차수 섹션 (`ui/table` 읽기 모드 — §7-14가 아니다)

**왜 §7-14가 아닌가.** 차수 섹션을 `ui/history-list`의 세 번째 사용처로 삼는 안을 먼저 검토했고,
실제 구현 계약(`ui/history-list/HistoryList.tsx`)을 읽고 접었다. 넷이 맞지 않는다.

| 맞지 않는 곳 | §7-14 / `HistoryList`의 계약 | S5가 필요한 것 |
|---|---|---|
| 열 | `HistoryEntry = { effectiveFrom, displayValue, status }` — 값 칸 **하나** | 차수·생성일·줄 수·견적 합계·상태·동작 **여섯**. 셋을 `displayValue` 한 칸에 욱여넣어야 한다 |
| 상태 | `"active" \| "past" \| "scheduled"` → 화면 글자 `적용 중`·`예정` 고정 | `현재`·`승인` — 날짜가 아니라 **승인 여부**에서 나오고, 위 유니언의 어느 값에도 대응하지 않는다 |
| 행 동작 | 동작 칸은 `onCancel` 하나이고 `scheduled` 행에만 렌더된다 | 이전 차수 행의 3차 「차수 열기」 — 취소가 아니라 이동이고, 차수에 `scheduled`가 없다 |
| 추가 | 「새 이력 추가」 3차 버튼 + 표 안에 펼쳐지는 편집 행. §7-14가 **"모달 아니다"라고 명시**한다 | 「복사해 새 차수」는 머리 줄 **2차 버튼 + §7-8 확인 모달**(D-53). §7-14가 배제한 바로 그 경로다 |

**그래서 차수 섹션은 `ui/table`을 읽기 모드로 쓴다.** 이 페이즈가 어차피 만드는 컴포넌트이고, 읽기
모드가 이미 필요한 것을 전부 준다(열 수 가변 · `<table>` + 시각적으로 숨긴 `<caption>` · 흰 머리글 ·
행 안 3차 버튼 · 폰 칸 접기). `HistoryList`를 넓히면 「이력형 설정 값·발령일 이력」이라는 §7-14의
정체가 흐려지고, 쓰지 않을 열·상태·동작 슬롯이 설정 화면 쪽에 남는다 — 새 계약을 쓰는 것보다 비싸다.
**§7-14는 이 페이즈에서 손대지 않는다.**

- 열: 차수 · 생성일 · 줄 수 · 견적 합계 · 상태 · (동작). 최신이 위. 편집 가능한 셀이 하나도 없으므로
  위 (가)대로 `role="grid"`가 아니라 `<table>` + `<caption>`(시각적으로 숨김), 머리글은 흰 바탕.
- **「읽기 모드」는 모드 토글이 아니다**(위 (가) 마지막 줄). 사용자가 켜고 끄는 상태가 아니라 편집
  가능한 셀 수가 0일 때 `ui/table`이 자동으로 취하는 렌더 형태다. 차수 섹션은 편집 가능한 셀이
  구조적으로 0이라 누구에게나 이 형태이고, 그래서 §7-3의 「읽기/편집 모드를 나누지 않는다」와
  충돌하지 않는다.
- **열 우선순위(§7-3 폰 전략)**: **P1 = 차수 · 견적 합계 · (동작)** — 문자 1 + 숫자 1 + 행동 1로
  §7-3의 P1 세 자리를 정확히 채운다. **P2 접힌 줄 = 상태 · 생성일 · 줄 수**(`현재 · 2026-09-19 ·
  6줄`). **P3 없음.**
  세 번째 P1 자리를 상태가 아니라 동작으로 준 이유: 상태 값이 `현재`·`승인` 둘뿐이고 **`현재`는 항상
  맨 위 한 행**이라 순서가 이미 그것을 말하는 반면, 이 표에서 사용자가 실제로 하는 일은 이전 차수로
  가는 것 하나뿐이다. 상태는 접힌 줄 첫 값으로 폰에서도 계속 읽힌다.
  현재 차수 행에는 동작이 없다(지금 보고 있는 차수다) — 그 칸을 `—`로 채우지 않는다(§7-3).
- 상태 열은 §7-5 `text` 변형 색 글자: 최신 차수 `현재`(`accent`), 승인된 차수 `승인`(`success`).
  **최신 차수만 현재이고** 승인 차수와 편집 중 차수가 동시에 살아 있는 상태는 없다(D-54).
  두 낱말 중 `현재`는 위 Color 절의 §7-5 보강 대상이다.
- 이전 차수 행의 3차 **「차수 열기」**(§8 규칙 1 「버튼 = 실제 동작」, §7-1의 3차 예시 「견적 열기」와
  같은 꼴 — 동사만 두지 않는다) → 그 차수를 **잠김 모양의 읽기 표**로 본다(위 (가), 같은 렌더 형태).
  되돌리기는 없다(D-53).
- 「복사해 새 차수」(머리 줄 2차) → 확인 모달 → 이전 차수 전체 복사 + 토스트. 빈 차수를 만들지
  않는다. 견적 번호 `26001-2차`는 표시용 파생값이다(D-56) — 저장 컬럼이 없다.

### S6. 매출 섹션 (§6-2 섹션 + §7-3 편집 표)

- 섹션 제목 「매출」 + 부제 `--fs-sm --muted` `공급가액 기준 · 입금액만 통장 합계`(PNL-09).
- **계약 금액**: `Form.Field` 한 칸(280 + 통화·환율). 쓰기 = PM(D-57). `Form.Hint`에
  `부가세 10% 4,800,000 · 합계 52,800,000 · 서버 계산`.
- **계약 금액 칸은 `Form.Actions`를 두지 않는다**(§7-15 일반 규칙). 제 저장 버튼을 가지면 이 화면에
  1차가 둘이 되기 때문이다(머리 줄의 「일괄 저장 ⌘S N」과 겹친다, §7-1 「한 화면에 1개」).
  대신 **고치면 dirty가 되어 화면의 1차 「일괄 저장 ⌘S N」의 `N`에 1칸으로 합류하고 같은 트랜잭션으로
  저장된다**(위 (사)). dirty 모양은 표 셀과 같은 `inset --inset-dirty` 외곽선이고, 저장 성공은 600ms
  `--accent-weak` 틴트, 서버 오류는 칸 아래 `Form.Error` 한 줄(「원인 · 다음 행동」)이며 그 오류도
  §7-3 「전부 저장 또는 전부 거부」에 걸려 화면 전체가 거부된다.
- 경영관리만 있는 화면(PM이 아닌 사람)에서 이 칸은 서버가 읽기 전용으로 보낸다 — dirty가 될 수
  없으므로 `N`에 세어지지 않는다.
- **발행 줄 표**(열: 발행일 · 발행액 · 메모) · **입금 줄 표**(열: 입금일 · 입금액 · 메모) — 각각
  독립한 §7-3 편집 표이고 쓰기 = 경영관리(D-57·D-58). 3차 「발행 줄 추가」 / 「입금 줄 추가」.
- **입금액 셀만 통장 합계**(부가세 포함)로 적고 `grossFromTotal()`로 공급가액을 역산한다. 셀 2행
  (`--fs-sm --muted`)에 `공급가액 12,400,000 · 서버 계산`. 외화 입금이면 같은 2행에 이어 쓴다:
  `USD 4,400.00 @1,318.1818 · 공급가액 12,400,000` — §2-4의 두 줄 병기를 3행으로 늘리지 않는다.
- 미수(D-58): 입금 합계 ≠ 발행 합계면 입금 표 합계 행 오른쪽에 `미수 3,400,000` 또는
  `초과 입금 500,000`(`--warning`). 넓은 면을 칠하지 않는다.
- 음수 허용(EXP-14): 환불·할인 줄은 음수 금액, 하이픈 마이너스, 괄호·붉은색 금지(§2-4).
- 정보 노출: 발행액·입금액은 정보 노출표 항목이라 기획본부에게는 **두 표를 서버가 보내지 않는다**
  (섹션에 계약 금액만 남는다). 열 단위가 아니라 표 단위로 빠진다.

### S7. 상태 태그와 상태 전환 (§7-5 + §7-8)

- 「상태 바꾸기」 2차 → 갈 수 있는 곳이 **하나면 바로 그 확인 모달**(제목 = 실제 동작), **둘이면**
  §6-3 「바꾸기」 골격의 목록(PC 모달 480 / 폰 시트) 후 확인.
  수주중 → 진행 · 미수주(둘) / 진행 → 완료(정산)(하나) / 미수주 → 진행(하나) / 완료 → **없음**
  (버튼 자체가 없다).
- 막힘: 기간 없이 진행으로 가려 하면 모달 1차 비활성 + 왼쪽에 `기간 없음 · 기간 적기`, 3차가 등록
  폼의 기간 칸으로 데려간다(D-49).
- **잠기는 상태는 완료(정산) 하나뿐**(D-45). 미수주는 잠기지 않고 지출결의도 계속 올라간다 — 화면에
  자물쇠·회색 처리를 하지 않는다.
- 이력은 행동 로그에만 남는다(D-50). 상세에는 **현재 상태 + 마지막 변경일**만. 새 표·새 섹션 없음.
- 전환은 전부 `domain/rules.gate` 단일 진입점을 지나고, 화면은 게이트가 돌려준 이유 문자열을 그대로
  §7-1 막힘 자리에 쓴다(화면이 이유를 따로 만들지 않는다).

### S8. 폰 행 시트 — 위 (바)

### S9. 클라이언트별 리저브 대장 `/pnl/reserves` (§6-1 + §7-3)

- 진입은 손익 메뉴 안 항목(D-59, `role-menu.ts`에 한 줄). 기획본부는 메뉴가 없어 도달하지 않는다 —
  "기본 숨김"이 메뉴 수준에서 지켜진다.
- **그룹 머리글 = 클라이언트**, 머리글 행 오른쪽에 그 클라이언트의 **잔액**(서버 계산, 700).
- 열(D-60): 날짜 · 구분(입금/출금 Select) · 금액 · 잔액 · 프로젝트(선택) · 증빙 종류 · 세금계산서
  번호 · 메모. 날짜순.
- **잔액 열은 계산값이라 항상 읽기 전용**이고 `--muted`가 아니라 `--fg`다(값이다, 부가 정보가 아니다).
- 직접 수정 허용(행동 로그), 삭제는 보관함. **어느 날짜든 잔액이 음수가 되면 서버가 거부하고 이유를
  보인다** — 그 줄의 금액 셀이 오류 셀이 되고 이유는 Copywriting 표의 문구.
- 폰 P1 3열: 날짜 · 금액 · 구분. P2: 프로젝트 · 잔액 · 메모.
- 금액은 `domain/money` 모델(통화·환율·원화 환산액) — 외화 표시는 §2-4 두 줄 병기 그대로.

### S10. 문서 번호 표시 (ADMN-09)

- §2-4 「문서 번호」 형식: 서버 서식 그대로, **모노 서체 아님**, `tabular-nums`. 프로젝트 `26001`,
  견적 `26001-1차`(표시용 파생값). 목록의 번호 열 폭은 자릿수 고정(`ch`).
- 번호는 등록 시 부여되고(D-42) 미수주 건의 결번은 정상이다 — 화면에 결번을 설명하지 않는다.

### S11. 접근성·이관 정리 (§10 보강)

- 편집 표: `role="grid"` · 셀 `role="gridcell"` · 활성 셀 `aria-selected` · `aria-readonly` 3단계
  (위 (가)) · 오류 셀 `aria-invalid` + `aria-describedby` · 합계 행 `aria-live="polite"`.
- 읽기 표는 **셋**이다: 프로젝트 목록 · 차수 섹션 · 이전 차수 견적 줄. 전부 `<table>` + 시각적으로
  숨긴 `<caption>`. (**잠긴 견적 줄은 네 번째 사용처가 아니다** — S4의 같은 표가 위 (가)의 판정에서
  편집 가능한 셀 0으로 나왔을 때 취하는 렌더 형태다. 컴포넌트 사용처를 셀 때 따로 세지 않는다.)
  **`ui/table`은 이 렌더 형태에서 `<caption>`을 항상 렌더한다** — 그래서 셋 다 처음부터 캡션이 있고,
  잠김 상태의 견적 줄 표도 같은 경로로 캡션을 얻는다.
- **아이콘 단독 버튼은 `aria-label` 필수**(§9 · §10). 이 페이즈가 들여오는 아이콘 단독 버튼은 §7-8
  시트 머리의 **닫기(x) 20px 하나**이고 `aria-label="닫기"`를 단다. 목록 열 머리글의 정렬 방향
  아이콘은 글자 라벨이 있는 버튼 안의 장식이므로 `aria-hidden="true"`이고, 정렬 상태는
  `aria-sort`로 말한다(아이콘이 유일한 신호가 되지 않는다).
- **A-M3는 이 페이즈에서 닫히지 않는다 — 계획 단계가 정해야 한다(결정 필요).**
  `03-OPEN-ITEMS.md:84`는 A-M3(관리자 읽기용 표 6종에 `<caption>` 없음, 코드표는 `th scope`도 없음)를
  「Phase 4 표 작업(칸 접기)과 같은 커밋에 넣는다」로 예약했다. 그러나 이 스펙에서 관리자 표 6종의
  `ui/table` 이관은 **Phase 7**(§7-15 폼 이관과 같은 묶음)이고, 이 페이즈는 그 6종을 건드리지 않는다.
  둘은 그대로 두면 충돌한다. 계획 단계가 **둘 중 하나를 고른다**:
  **(ㄱ)** 이관과 무관하게 이 페이즈에서 6종에 `<caption>`(+ 코드표 `th scope`)만 한 줄씩 넣어 A-M3를
  약속대로 닫는다 — 표 6개를 건드리지만 전부 한 줄씩이고 Phase 7 이관 때 자연히 흡수된다. 또는
  **(ㄴ)** A-M3를 Phase 7로 옮기고 그 사유를 `04-OPEN-ITEMS.md`에 남긴다.
  `03-OPEN-ITEMS.md`는 끝난 페이즈의 산출물이므로 **이 페이즈가 고치지 않는다** — 결정은 Phase 4
  계획서와 `04-OPEN-ITEMS.md`에 적는다. 조용히 넘어가는 것만은 안 된다.
- **폰 375px 칸 접기**(DOM 감사 2·6·7 이월): §7-3 칸 접기 패턴이 코드베이스에 처음 생기는 자리가
  `ui/table`이다. 이 페이즈의 표 4곳은 처음부터 가로 스크롤 0으로 만든다. 관리자 표 이관은 Phase 7
  검수다.
- 터치 목표 폰 44×44(행·시트 버튼·3차 버튼), PC 32px 컨트롤 허용.
- `prefers-reduced-motion: reduce`에서 저장 성공 600ms 틴트를 끈다(`--dur-saved`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| — | 해당 없음 | shadcn 미사용(`Tool: none`). 이 페이즈의 신규 컴포넌트(`ui/table`·`ui/form`·`ui/select`)는 리포 안에서 직접 구현하며 외부 레지스트리·서드파티 블록을 쓰지 않는다. **D-61이 표 라이브러리를 명시적으로 배제**했고 새 런타임 의존성은 0이다 — 안전 게이트 자체가 적용되지 않는다 |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS (§7-8 2차 라벨 충돌 규칙 신설 + 파괴 모달 7개 전부 1차·2차 쌍. 3차 라벨 `열기` → `차수 열기`로 명사 보강)
- [x] Dimension 2 Visuals: PASS (S11 아이콘 단독 버튼 `aria-label`. S5 열 우선순위 P1 3열 선언 — §11 「P1 열 3개 이하」 통과)
- [x] Dimension 3 Color: PASS (§7-5 보강 ②가 의미 목록 다섯 낱말 + 길이 규칙 한 줄을 함께 덮는다 — 화면 예외 0. 포인트 색 사용처가 §1-3 ①의 「태그」에 드는 근거를 명시)
- [x] Dimension 3 Form/저장 경로: PASS (매출 계약 금액 칸의 저장 경로 확정 — §7-15 일반 규칙 「`Form.Actions`는 폼이 자기 제출을 가질 때만」 + (사) 저장 범위 확대. 1차는 화면당 하나 유지)
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS
- [x] Dimension 7 Inventory Provenance: PASS (Form 사용처를 본문과 맞춰 3곳 → **2곳**으로 정정. `DESIGN.md` §4 규칙 3의 문턱은 「두 곳 이상」이라 그대로 성립)

**Approval:** draft (체커 재검증 대기)

**개정 기록 2026-09-22 (rev 1)** — 체커 승인 후 오케스트레이터가 확인한 결함 7건 반영:
① 차수 섹션을 §7-14 재사용 → `ui/table` 읽기 모드로 정정(근거 표는 S5) ②「딱 하나」 주장 정정 —
§7-5 보강 대상을 다섯 낱말로 넓힘 ③ §7-8 2차 라벨 규칙 신설 + 파괴 모달 3개에 2차 라벨 추가
④ S11에 아이콘 단독 버튼 `aria-label` ⑤ `?new=1` 1차 버튼 충돌 해소(S2·Accent 목록)
⑥ §7-15 이관 주체 「다음 페이즈」 → Phase 7 ⑦ A-M3 일정 충돌을 계획 단계 결정 항목으로 명시
(`03-OPEN-ITEMS.md`는 건드리지 않음).

**개정 기록 2026-09-22 (rev 2)** — 체커 재검증의 BLOCK 1건 + FLAG 1건 + 권고 6건 반영:

**BLOCK — S3 「매출 계약 금액」의 저장 경로.** 그 칸에 저장 경로가 없었고, `Form.Actions`를 주면
`Form.Actions`가 1차를 데려오므로 S3에 1차가 둘 된다(§7-1 「한 화면에 1개」). **화면의 1차에
합류시키는 쪽**을 골랐다 — §7-15에 **「`Form.Actions`는 폼이 자기 제출을 가질 때만 둔다」**를 일반
규칙으로 더하고(「`Form.Actions` 없는 단일 칸 폼」 형태 정의 포함), (사)의 저장 범위를 「모든 편집 표
**+ 표 밖의 편집 칸**」으로 넓혔으며, S6에 dirty·성공·오류 모양을 적었다. **왜 이쪽인가:** 이미 이
화면의 저장은 ⌘S 하나이고 §7-3이 「전부 저장 또는 전부 거부」를 요구한다. §7-2 설정 화면식 blur 즉시
저장도 검토했으나 한 화면에 저장 의미가 둘이 되고 그 칸만 전부-거부를 비켜 가므로 접었다(그 검토
결과도 §7-15에 남겼다). 반영 자리: §7-15 `Form.Actions` 항목 · (사) 저장 범위 · S3 머리 줄 · S6.

**BLOCK 부수 — 「Form 3곳」 정정.** 본문(S9)은 리저브 줄을 §7-3 편집 표로 규정하는데 사용처 계산만
폼으로 세고 있었다. **2곳**(프로젝트 등록 폼 · 매출 계약 금액 칸)으로 고치고 Component Inventory와
§7-15 머리글을 맞췄다. `DESIGN.md` §4 규칙 3의 문턱이 「두 곳 이상」이라 숫자를 부풀리지 않았다.

**FLAG — `완료(정산)`의 §7-5 길이 규칙 위반.** 「범위 밖」이라는 지난 판단을 철회한다. 이 문서는
이미 §7-5를 개정 대상으로 열어 두었고 45~48행이 「화면 예외 0」을 주장하므로, 바로 다음 줄의 길이
규칙만 침묵하는 것은 일관되지 않는다(CLAUDE.md 프론트엔드 「기록 없는 시스템 이탈 금지」).
**라벨은 그대로 둔다** — `완료(정산)`은 **D-41**과 ROADMAP Phase 4 성공 기준 4가 잠근 업무 용어다
(지난 회차에 이 잠금을 D-46으로 잘못 적었다. D-46은 **상태 전환 주체**이고 라벨 잠금이 아니다).
대신 DECISIONS ②의 범위를 「의미 목록 + 길이 규칙」으로 넓혀 「업무 용어로 잠긴 상태 값은 네 글자
명사 + 괄호 한정어 하나를 허용한다(괄호 안 두 글자, 문장 금지 유지)」를 §7-5에 넣는다.
`SYSTEM.md` 개정 건수는 **네 건 그대로**다(②의 범위가 넓어졌을 뿐 새 항목이 아니다).

**권고 1** S5 차수 섹션 열 우선순위 선언: **P1 = 차수 · 견적 합계 · (동작)**, P2 접힌 줄 = 상태 ·
생성일 · 줄 수, P3 없음. 세 번째 P1을 상태가 아니라 동작에 준 이유를 한 줄로 적었다(`현재`는 항상
맨 위 한 행이라 순서가 이미 말한다).
**권고 2** Accent 예약 ①에 `--accent` **상태 태그**(`진행`·차수 `현재`)를 명시하고, §1-3 ①의
「태그」가 그것을 덮는 근거를 적었다 — §11 「포인트 색 5군데 안」 검사의 근거가 생겼다.
**권고 3** S3 「상태 바꾸기」 미렌더에서 **「예외」라는 말을 뺐다.** 새 규칙이 아니라 「할 수 없는
사람에게는 서버가 보내지 않는다」는 기존 권한·정보 노출 패턴의 적용이고, 같은 패턴의 다른 셋
(S6 매출 표 · S9 리저브 메뉴 · `ui/permission-grid`)을 함께 적었다. DECISIONS 항목으로 올리지
않는다 — 새 규칙이 아니므로 「네 건 · 예외 0」 주장과 어긋나지 않는다.
**권고 4** S11 읽기 표 열거를 셋(목록 · 차수 섹션 · 이전 차수)으로 고치고, 잠긴 견적 줄이 S4 표의
렌더 형태이지 별도 사용처가 아님을 적었다.
**권고 5** 「`ui/table` 읽기 모드」가 §7-3의 「읽기/편집 모드를 나누지 않는다」와 부딪히지 않도록
(가)에 **「모드 토글이 아니라 편집 가능한 셀 0에서 파생되는 렌더 형태」**를 명문화하고 S5가 참조한다.
**권고 6** 3차 라벨 `열기` → **`차수 열기`**(§8 규칙 1, §7-1의 「견적 열기」와 같은 꼴). Copywriting
표·S5 본문·비교 표를 함께 고쳤다.

**건드리지 않은 것(확인됨):** 굵기 3단계(400·600·700) · `--cell-pad-y` 6px(폰 10px) ·
42~43행의 `--g-100`/`--g-950` 직접 참조(§7-3 원문) · `03-OPEN-ITEMS.md`(A-M3 결정 필요 블록 유지).

**개정 기록 2026-09-22 (rev 3)** — UI-consideration probe 57건 해소. `## UI Considerations` 절을
`## Copywriting Contract`과 `## SYSTEM.md §7 보강·신설 초안` 사이에 **추가만** 했다(다른 절은 한 줄도
고치지 않았다 — 체커 승인 상태 유지). 결과 **explicit 43 · backstop 11 · unresolved 6**
(S3 overflow 머리 줄 · S5 partial 상태 열 경계 · S6 zero-one-many 입금 0줄일 때의 미수 자리 3건)
+ 프로브 항목 밖 경계 3건(`N`=0 라벨 · 편집 셀 0인 사람의 1차 버튼 · 표 밖 오류 시 합계 행 문구) —
두 괄호의 합이 6이다(U-1~U-6). 문구는 옮겨 적지 않고 Copywriting Contract 행을 가리키기만 했다.
