---
phase: 04.1-approvals-leave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/approvals.ts
  - db/schema/leave.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - repositories/approvals.ts
  - repositories/leave-requests.ts
  - repositories/org-snapshot.ts
  - repositories/permissions.ts
  - repositories/settings.ts
  - repositories/action-log.ts
  - domain/approvals/route.ts
  - domain/approvals/kinds.ts
  - domain/approvals/dto.ts
  - domain/approvals/index.ts
  - domain/approvals/tx-log.ts
  - domain/leave/days.ts
  - domain/leave/dto.ts
  - domain/leave/index.ts
  - domain/leave/access.ts
  - domain/document-numbering/index.ts
  - domain/settings/registry.ts
  - domain/settings/keys.ts
  - domain/permissions/info-items.ts
  - domain/seed/approvals-leave.ts
  - domain/seed/index.ts
  - lib/dates.ts
  - test/integration/approvals-route-fixed.test.ts
  - test/integration/approvals-concurrency.test.ts
  - test/integration/approvals-inbox-projection.test.ts
  - test/integration/leak-scan.test.ts
  - test/unit/domain/approvals/next-step.test.ts
  - test/unit/domain/approvals/resolve-step.test.ts
  - test/unit/domain/approvals/document-kind-registry.test.ts
  - test/unit/domain/leave/days.test.ts
  - test/unit/domain/leave/route-config.test.ts
  - test/unit/lib/dates.test.ts
  - test/unit/db/migration-journal.test.ts
autonomous: true
requirements: [EXP-03, EXP-04, LEAV-01, ADMN-04]

estimate:
  tokens: 165000
  raw_tokens: 165000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`domain/approvals`의 `nextStep(status, event)`는 draft→submitted→in_review→approved/rejected/withdrawn 표만 허용하는 순수 함수이고, 표 밖 전이(approved·withdrawn 뒤 모든 사건, draft에서 approve 등)는 예외로 거부된다(ROADMAP 기준 1 · EXP-03 ordering)"
    - "결재 모듈은 문서 종류를 하드코딩하지 않는다 — `registerDocumentKind`로 등록된 종류만 제출되고, 테스트 전용 두 번째 종류가 같은 `submitDocument`·`approveDocument`·`nextStep`으로 최종 승인까지 간다(ROADMAP 기준 1)"
    - "제출하면 결재선이 `approval_routes`(차수 1개) + `approval_steps`(사용 중인 단계마다 한 행: 계급 · 조직 범위 종류 · 해석된 대상 id · 단계 이름)로 문서에 고정된다. 제출 뒤 설정을 바꿔도 이미 제출된 문서의 단계 행은 바뀌지 않는다(EXP-04)"
    - "기안자 팀·기안자 본부 범위는 제출 시점의 기안자 소속으로 해석해 대상 id로 저장한다 — 뒤에 기안자가 발령되어도 그 문서의 범위는 그대로다(EXP-04 adjacency)"
    - "단계의 사람은 저장하지 않는다. 지금 단계·남은 단계의 담당은 표시·처리 시점마다 (재직 중인 사람 × 계급 × 현재 소속)으로 다시 해석하고, 이미 처리한 단계는 저장된 처리자 그대로다(EXP-04)"
    - "3단 「경영관리본부 담당」은 새 사람↔본부 표 없이 `team_memberships → teams.org_unit_id`로 해석하고 계급 조건이 없다 — 경영관리본부 아래 팀에 현재 소속된 사람은 누구든 후보다(오케스트레이터 결정 1 · U1 해소)"
    - "(ENG-1) 담당 해석의 「대상 id가 null이면 0명」은 `team`·`org_unit` 범위 행(제출 때 `drafter_team`·`drafter_org_unit`·`org_unit` 설정에서 나온 행 — 기안자 팀이 없거나 특정 부서가 `\"\"`인 경우)에만 적용된다. `company`(전사) 범위는 대상 id를 보지 않고 재직자 전원(계급 조건이 있으면 그 계급 재직자 전원)이다 — 그래서 전사 단계는 빈 자리로 새지 않는다: 1단 승인 뒤 4단 `대표 × 전사`는 대표 폴백이 아니라 4단 자체로 처리되고(폴백 행 0개), `본부 책임자 × 전사` 단계의 후보는 대표가 아니라 모든 본부의 본부 책임자다"
    - "빈 자리(후보 0명)는 건너뛴다. 진행 위치에서 남은 단계가 있는데 전부 비면 대표가 받는다. 대표도 없으면 제출이 거부된다(`대표 없음 · 관리자에게 대표 계급 확인 요청`). 결재 없이 최종 승인되는 문서는 없다(입력 §2 · EXP-03 empty·adjacency)"
    - "차수 끝까지 훑었는데 이 차수의 승인(`approved`)이 0건이면 — 관리자가 단계를 전부 껐거나, 팀장 기안 + 자기 승인 `skip` + 뒤 단계가 꺼진 경우 — 최종 승인이 아니라 대표 폴백이다. 대표가 기안자이면 대표가 자기 문서를 명시적으로 승인(`self_approved`)하고, 대표 계급 재직자가 없으면 제출이 거부된다. `skip`으로 통과한 단계는 승인으로 세지 않는다(CEO-1 · 05-CONTEXT 「결재 없이 통과하는 문서는 없다」)"
    - "한 사람은 한 차수에서 한 번만 승인한다 — 이 차수에 이미 승인한 사람은 뒤 단계(대표 폴백 포함)의 후보에서 빠지고, 그래서 후보가 0명이 된 단계는 빈 자리로 건너뛴다(CEO-5 · 사용자 결정 A 「한 번만」, 2026-09-24 10:39 카드). 자기 승인 복원(`self_approve`)도 예외가 아니다 — 기안자가 이 차수에 이미 승인했으면(본인 승인 포함) 기안자가 유일 담당인 뒤 단계는 기안자를 다시 후보로 두지 않고 빈 자리다(ENG-2)"
    - "3단 `org_unit_id`처럼 기본값 없는 결재선 설정 키에 행이 없으면 원시 오류 대신 `\"\"`(특정 부서 없음)로 읽어 그 단계를 빈 자리로 처리한다 — 연차 신청·미리보기가 막히지 않는다(CEO-7)"
    - "04.1의 「오늘」 · 회계연도 · 문서 번호 연도 · 스냅숏 기준일은 전부 `lib/dates.ts`의 `seoulToday()`(서울 날짜 `YYYY-MM-DD`)에서 나온다 — `2026-12-31T15:30Z`에 낸 신청의 번호는 `LV27-…`이다(CEO-11)"
    - "제출 뒤 지금 단계의 담당이 사라지면(보관·퇴직·발령) 그 단계는 제출 때와 같은 규칙으로 빈 자리가 되어 건너뛰고, 뒤 단계가 전부 비면 대표가 받는다. 이미 승인한 사람의 기록은 그대로 남는다(오케스트레이터 결정 3 · U2 해소)"
    - "(ENG-3 · D2) 「후보 0명 · 이번 차수 승인 ≥ 1 → 최종」은 `approveDocument`가 지금 viewer의 승인을 더한 **뒤** 계산할 때만 최종(`approve_final`)이다. 행동 전 계산(`getApprovalView` · `listMyInbox` · `approveDocument`의 후보 판정)에서 DB 상태가 `submitted`·`in_review`인데 walkRoute가 최종을 내면(예: 대표가 1단을 승인한 뒤 남은 단계의 담당이 전부 보관·퇴직) 그 문서는 막힘과 똑같이 다룬다 — 지금 단계 `담당 없음` · 누구의 결재함 `mine`에도 없음 · `log.warn(\"approval.route_blocked\")` · 승인은 `NotCurrentHolderError` · 기안자 회수는 가능(04.1-02) · 관리자가 담당을 복구하면 다시 처리 가능. 읽기 경로는 상태를 쓰지 않는다(쓰기 부작용 없음). 이 판정은 walkRoute 안 한 곳(입력 `at`: 행동 전 / 승인 반영 뒤)에만 있어 읽기·쓰기 경로가 같은 결과를 낸다"
    - "기안자가 단계 담당이면 문서 종류의 자기 승인 설정을 따른다 — 연차 기본값 `skip`은 그 단계를 통과로 보고 다음 단계로 간다, `self_approve`는 기안자 한 사람만 후보로 둔다(기안자가 이 차수에 아직 승인하지 않았을 때만 — 이미 승인했으면 빈 자리, ENG-2). 대표가 기안자이고 다른 단계가 전부 비면 대표가 자기 문서를 명시적으로 승인하고 그 단계에 `self_approved`가 기록된다(오케스트레이터 결정 4 · U4 해소)"
    - "같은 문서의 결재함·결재선 보기를 몇 번 조회해도 `approval_routes`·`approval_steps` 행이 늘거나 바뀌지 않는다 — 표시 시점 재해석은 읽기 전용이다(EXP-04 idempotency)"
    - "승인은 매 시도마다 서버가 후보 여부를 다시 판정한다 — 메뉴 권한이 아니라 결재선이 권한이다. 후보가 아니면 `지금 담당이 아님 · 새로 고침`으로 거부된다. 기안자 · 무관한 사람 · 차례 전의 대표가 맞는 version으로 승인해도 거부되고, 그때 단계 처리 기록은 0건 · version은 그대로다(CEO-8)"
    - "상태 변경은 `approval_instances.version` 조건부 UPDATE 한 번으로만 일어난다 — 0행이면 충돌이고 두 번째 같은 요청(더블 클릭)은 단계 기록을 하나 더 만들지 않는다(EXP-03 idempotency)"
    - "승인 판정·쓰기 순서가 고정되어 있다: version 확인(불일치면 관련자 판정으로 오류 종류만 가른다 — ENG-6 · D1) → 종결 상태 확인(version이 맞아도 같은 관련자 판정 — 관련자가 아니면 상태 문구 대신 `NotCurrentHolderError` · reason `not_holder`, 04.1-02 Task 3과 같은 규칙 — X-4) → 후보 판정 → 상태 UPDATE(version 조건) 먼저 → 단계·폴백 행 쓰기. 그래서 진 쪽은 원시 DB 오류가 아니라 정확한 문구를 받는다(CEO-6)"
    - "(ENG-6 · D1) version 불일치일 때 viewer가 관련자 — 기안자 · 이 인스턴스에서 단계를 처리한 사람(`acted_by`, 모든 차수) · 지금 차수 단계들의 지금 해석한 담당(기안자 제외 전 담당 집합) — 이면 `ApprovalConflictError`(처리자 이름 · 시각 · 상태 상세 문구는 04.1-02 Task 3이 조립), 아니면 `NotCurrentHolderError`(`지금 담당이 아님 · 새로 고침`)이고 `log.info(\"approval.refused\")` reason은 `not_holder`다. 종결 상태 거부(2)도 같은 규칙이다. 관련자 판정 `isApprovalParty`(`domain/approvals/conflict-message.ts`)와 두 갈래 배선은 04.1-02 Task 3이 만든다 — 04.1-01은 그 재료로 walkRoute 결과에 `currentHolderIds`(지금 차수 단계 행 전부를 지금 스냅숏으로 해석한 담당의 합집합, 기안자·승인자를 빼기 전 — walkRoute가 지금 대표 폴백 자리에 있으면 그 폴백 후보(대표 계급 재직자 전원)도 넣는다: 폴백 단계 행은 승인 때 생기지만 후보는 그 전부터 지금 담당이다 — X-3)를 내보내고, 04.1-01 단독 상태의 불일치·종결 오류 문구에는 처리자 이름 · 시각이 없다(기본 문구)"
    - "트랜잭션 안에서는 `tx`를 받는 리포지토리 호출만 돈다 — 결재선 설정 · 번호 서식 설정 · 조직 스냅숏 · `can()` · 행동 로그 켜짐 여부(`loadActionLogGate`)는 `withTransaction` 전에 읽고, 문서 번호 할당(카운터 행 잠금)과 그 번호 기록이 마지막 쓰기다. 기본 풀(`DB_POOL_MAX` 5)에서 연차 제출 6건을 동시에 보내도 전부 끝나고 번호 6개가 서로 다르다(CEO-2 · Phase 4 04-32 ARCHITECTURE §4-8 규약)"
    - "결재함 한 번 그리기(`listMyInbox`)와 내 연차 목록(`listMyLeave`)의 노출 조회(`findVisibility`)는 요청 단위 메모로 정보 항목마다 한 번 — 행 20개여도 2회 이하다. `approval_steps(acted_by)`에 인덱스가 있다(CEO-17)"
    - "결재선이 막히면(`막힘`) `log.warn(\"approval.route_blocked\")`, 승인 거부(충돌 · 후보 아님 · 종결)는 `log.info(\"approval.refused\")`가 id와 사유 코드만 담아 남긴다 — 운영자가 로그로 본다(CEO-18)"
    - "나중에 만든 계급(`createRole`)은 결재·연차 정보 노출 행이 없어 그 사람의 결재함 항목이 빈 DTO가 된다 — 알려진 한계(기본값 A)로 통합 테스트가 지금 동작을 기록하고 04.1-07이 TODOS.md와 SUMMARY에 옮긴다. `domain/permissions/roles.ts`는 고치지 않는다(CEO-10)"
    - "연차 신청의 일수는 정수 1/4일(`days_quarters`)로 저장된다 — 종일 = 기간 안 평일 수 × 4, 반차 = 2, 반반차 = 1, 재택 = 0. 0.25 + 0.25 + 0.5가 정확히 1일이다(LEAV-01 precision)"
    - "회계연도(1월 1일~12월 31일)를 넘는 기간은 신청이 거부된다(`기간이 회계연도를 넘음 · 12-31과 01-01로 나눠 신청`), 반차·반반차는 하루뿐이고 오전/오후가 필수다(LEAV-01 boundary)"
    - "연차 문서 번호는 첫 제출 때 `allocateDocumentNumber(counterKey \"leave\")`로 매겨지고 기본 서식은 `LV26-0001`이다(`document_number.leave.*` 다섯 키, 계획 가정 — UI-SPEC Assumptions #16)"
    - "결재선 설정은 JSON 한 개가 아니라 필드 단위 키 17개다 — `approval_route.leave.self_approval` + `approval_route.leave.step{1..4}.{enabled,role_id,scope,org_unit_id}`. 기본값은 1단 팀장 × 기안자 팀 · 2단 본부 책임자 × 기안자 본부 · 3단 계급 없음 × 특정 부서(경영관리본부) · 4단 대표 × 전사, 넷 다 사용, 자기 승인 = 건너뜀(ADMN-04 · 입력 §1·§3)"
    - "(CX-R3) 제출 전 결재선 미리보기 `previewRoute(viewer, {kind})`는 제출과 같은 조립 함수로 지금 설정·지금 소속을 해석하되 아무것도 쓰지 않고 `RoutePreviewDTO`를 돌려준다 — 담당 이름(`holderNames`)과 기안자 이름(`drafterName`)은 viewer가 `approval.value` 노출 투영(결재함·결재선 보기 DTO와 같은 `project()` + 요청 단위 메모)을 통과할 때만 실리고, 통과하지 못하면 단계는 자리 이름(`label` — 계급·부서 이름)과 건너뜀 표시(`skipped`)만 싣는다. `role.value`도 없으면 `label`은 빠지지 않고 `N단`(목록 순번, 대표 폴백 자리 포함)이다(CX2-W1). 두 명세(`routePreview` · `routePreviewStep`)는 `registerDto`로 등록되어 누수 스캔 생성기가 케이스를 만든다"
    - "새 DTO(결재함 항목·결재선 보기·연차 신청·연차 잔고)는 `registerDto`로 등록되고 모든 필드가 `approval.value`·`leave.value` 정보 항목에 매핑되어 누수 스캔 DTO 축에 들어간다(ROADMAP 기준 5)"
    - "제출·승인은 행동 로그에 남고(`document_submit`·`document_approve`, entity `approval_instance`) 그 로그 행은 상태 전이와 **같은 트랜잭션**에서 쓰인다 — 로그 쓰기가 실패하면 신청 행 · 인스턴스 · 단계 · 번호 카운터 · 상태 변경이 함께 롤백되고, 커밋된 전이에는 로그 행이 반드시 있다(ROADMAP 기준 5 · Codex HIGH 원자성 · Phase 4 ARCHITECTURE §4-8 (3))"
    - "`db/migrations/meta/_journal.json`은 idx가 0..n-1로 빈틈없고, 각 tag가 0 채운 idx로 시작하며, `when`이 엄격히 증가하고, 항목마다 `.sql`과 `meta/{idx 네 자리}_snapshot.json`이 있으며 journal에 없는 스냅숏 파일이 없다 — 이 불변식을 단위 테스트가 지킨다. 번호 범위는 단언하지 않는다 — 페이즈별 번호 범위 지정은 폐지됐다(오케스트레이터 결정 7b · 11:48 교차 페이즈 마이그레이션 규칙 · 조정자 11:53 규칙)"
    - "제출이 읽는 결재선 설정 17키는 SELECT **한 문장**(`findSimpleValues` → `getSimpleSettingValues`)으로 한 번에 읽힌다 — Postgres 문장 스냅숏이라 한 제출의 단계 행은 한 시점에 커밋된 설정 한 벌에서만 나오고, 관리자 저장이 제출과 겹쳐도 두 시점의 값이 섞이지 않는다. 설정 화면은 칸마다 독립 upsert로 저장하므로 보장 단위는 「한 커밋 시점」이다(EXP-04 concurrency · Codex HIGH — 전에는 backstop이던 항목을 Task 3의 단위 가드 + 통합 경주 테스트로 옮겼다)"
  prohibitions:
    - statement: "결재 단계에 사람 id를 저장해 두고 표시 시점에 그 값을 그대로 보여 주어서는 안 된다(처리한 단계 제외). 사람을 저장하면 발령·퇴직 뒤 결재가 사라진 사람에게 묶이고 EXP-04의 「표시 시점 현재 담당」이 거짓이 된다"
      status: resolved
      verification: test
    - statement: "`nextStep()`이나 결재선 해석 함수 안에 문서 종류 이름(`leave` 등)으로 분기하는 코드가 있어서는 안 된다. 종류별 차이는 등록한 종류 정의(결재선 설정 로더·자기 승인 값)로만 들어온다"
      status: resolved
      verification: test
    - statement: "마이그레이션 파일 이름을 손으로 바꾸거나 `_journal.json`을 손으로 고쳐서는 안 된다 — 생성은 `pnpm db:generate`만 쓴다(오케스트레이터 결정 7a)"
      status: resolved
      verification: test
    - statement: "`withTransaction` 콜백 안에서 전역 풀을 쓰는 읽기(`getSettingValue` · `listOrgSnapshot` · `can()` · `loadRouteConfig` · `project()`)를 불러서는 안 된다 — 잠근 트랜잭션이 풀 연결을 더 기다리면 풀 5에서 동시 제출이 멈춘다(CEO-2)"
      status: resolved
      verification: test
    - statement: "「오늘」·연도를 `new Date().getFullYear()`나 `toISOString()` 앞 10자로 만들어서는 안 된다 — Cloud Run은 UTC라 한국 시간 0~9시에 조용히 하루·한 해가 틀린다(CEO-11)"
      status: resolved
      verification: test
    - statement: "결재 전이를 커밋한 뒤 풀 연결로 `recordAction`을 따로 불러서는 안 된다 — 로그 INSERT가 실패하면 문서는 바뀌었는데 감사 기록이 없거나, 사용자는 실패를 보는데 상태는 바뀌어 있다. 전이의 로그는 `recordActionInTx`로 같은 `tx`에서만 쓴다(Codex HIGH 원자성)"
      status: resolved
      verification: test
    - statement: "결재선 설정 키를 `getSettingValue`로 한 키씩 읽어 결재선을 조립해서는 안 된다 — 키마다 따로 SELECT하면 그 사이의 관리자 저장이 섞인다(Codex HIGH 스냅숏)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/approvals.ts"
      provides: "approval_instances · approval_routes · approval_steps 세 표(ROADMAP 기준 1)"
      contains: "approval_steps"
    - path: "db/schema/leave.ts"
      provides: "leave_requests 표 — 정수 1/4일 일수, 회계연도, 번호"
      contains: "days_quarters"
    - path: "domain/approvals/route.ts"
      provides: "순수 함수 nextStep · resolveHolders · walkRoute — DB 없이 단위 테스트된다. walkRoute는 비공개 도우미 candidatesFor · advance · fallback · describeHolders로 나뉜다(CEO-13). 동작은 Task 3의 결정표 한 장(R1~R6 · W1~W13)이 정의하고 칸마다 단위 사례가 있다 — 전사 범위 null 무시(ENG-1) · 자기 승인 복원의 이번 차수 승인자 제외(ENG-2) · 행동 전 최종 = 막힘(ENG-3 · D2, 입력 `at`). 결과에 `currentHolderIds`(지금 차수 전 담당 합집합 + 지금 대표 폴백 자리면 폴백 후보 — 04.1-02 관련자 판정 재료, ENG-6 · D1 · X-3)"
      exports: ["nextStep", "resolveHolders", "walkRoute"]
    - path: "domain/approvals/index.ts"
      provides: "prepareSubmission(트랜잭션 전 읽기) · submitDocument · approveDocument · listMyInbox · getApprovalView · previewRoute(`RoutePreviewDTO` — 저장 없음, 이름은 `approval.value` 투영 통과 때만, CX-R3) — 트랜잭션·낙관적 잠금·행동 로그(같은 tx)·운영 로그·요청 단위 노출 메모"
      exports: ["prepareSubmission", "submitDocument", "approveDocument", "listMyInbox", "getApprovalView", "previewRoute"]
    - path: "domain/approvals/tx-log.ts"
      provides: "loadActionLogGate(트랜잭션 전 켜짐 여부 선읽기, fail-open) · recordActionInTx(`recordAction`의 deps 주입으로 같은 tx에 로그 행) — `domain/action-log/*`는 고치지 않는다(Codex HIGH 원자성)"
      exports: ["loadActionLogGate", "recordActionInTx"]
    - path: "repositories/settings.ts"
      provides: "파일 끝에 덧붙인 findSimpleValues(viewer, keys) — `WHERE key IN (…)` SELECT 한 문장(Codex HIGH 스냅숏)"
      contains: "findSimpleValues"
    - path: "lib/dates.ts"
      provides: "seoulToday(now) — 서울 날짜 YYYY-MM-DD, 04.1의 모든 「오늘」·연도의 출처(CEO-11)"
      exports: ["seoulToday"]
    - path: "domain/leave/access.ts"
      provides: "연차 문서 보임 규칙 한 곳 — index.ts와 04.1-03 balance-service.ts가 함께 import해 순환이 없다(CEO-4)"
      exports: ["canSeeLeaveDocument"]
    - path: "test/integration/approvals-concurrency.test.ts"
      provides: "풀 5에서 연차 제출 6건 동시 — 전부 끝나고 번호가 다르다(CEO-2). 04.1-02 Task 3이 describe를 더한다"
      min_lines: 25
    - path: "test/integration/approvals-inbox-projection.test.ts"
      provides: "결재함 · 내 연차 목록 20행 노출 조회 2회 이하(CEO-17) · 새 계급 빈 DTO 한계 기록(CEO-10)"
      min_lines: 30
    - path: "domain/approvals/kinds.ts"
      provides: "문서 종류 등록부 — registerDocumentKind · getDocumentKind(종류 정의 선택 필드 `routeSettings?` · `canResubmit?` — 후자는 04.1-02가 쓴다, CX-W1)"
      exports: ["registerDocumentKind", "getDocumentKind"]
    - path: "domain/leave/index.ts"
      provides: "연차 종류 등록과 submitLeave · getLeave · listMyLeave"
      exports: ["submitLeave", "getLeave", "listMyLeave"]
    - path: "test/integration/approvals-route-fixed.test.ts"
      provides: "제출 → 팀장 승인 → 빈 자리 건너뜀 → 대표 승인 → 최종 승인 트레이서와 결재선 고정·재해석 증명"
      min_lines: 80
    - path: "test/unit/db/migration-journal.test.ts"
      provides: "journal idx 연속 · tag 접두 · when 단조 증가 · 항목마다 `.sql`과 `meta/{idx 네 자리}_snapshot.json` 존재 · journal에 없는 고아 스냅숏 없음 가드(Task 2 ③ (a)~(f))"
      min_lines: 20
  key_links:
    - from: "domain/leave/index.ts"
      to: "domain/approvals/kinds.ts"
      via: "모듈 적재 때 registerDocumentKind로 연차 종류를 등록한다 — approvals가 leave를 import하지 않는다"
      pattern: "registerDocumentKind"
    - from: "domain/approvals/index.ts"
      to: "repositories/approvals.ts"
      via: "version 조건부 UPDATE … RETURNING, 0행이면 충돌"
      pattern: "version"
    - from: "domain/approvals/index.ts"
      to: "repositories/org-snapshot.ts"
      via: "처리·표시 시점마다 재직자 × 계급 × 현재 소속 스냅숏을 읽어 walkRoute에 넘긴다"
      pattern: "listOrgSnapshot"
    - from: "domain/leave/index.ts"
      to: "domain/document-numbering/index.ts"
      via: "첫 제출 때 counterKey \"leave\"로 번호를 매긴다"
      pattern: "allocateDocumentNumber"
    - from: "domain/seed/index.ts"
      to: "domain/seed/approvals-leave.ts"
      via: "seedMasterData 끝에 한 줄 호출 — 3단 특정 부서 = 경영관리본부 id, 새 정보 항목 노출 행"
      pattern: "seedApprovalsLeave"
    - from: "domain/leave/index.ts"
      to: "lib/dates.ts"
      via: "제출일·번호 연도·스냅숏 기준일을 seoulToday(deps.now)로 만든다"
      pattern: "seoulToday"
    - from: "domain/leave/index.ts"
      to: "domain/leave/access.ts"
      via: "getLeave · listMyLeave의 보임 규칙을 canSeeLeaveDocument 하나로"
      pattern: "canSeeLeaveDocument"
    - from: "domain/approvals/index.ts"
      to: "domain/permissions/project.ts"
      via: "project(viewer, row, spec, { visible }) — 요청마다 만든 메모 visible을 주입(모듈 전역 캐시 아님)"
      pattern: "visible"
    - from: "domain/leave/index.ts"
      to: "domain/settings/registry.ts"
      via: "loadRouteConfig가 17키를 getSimpleSettingValues 한 번(→ findSimpleValues SELECT 한 문장)으로 읽는다"
      pattern: "getSimpleSettingValues"
    - from: "domain/approvals/tx-log.ts"
      to: "repositories/action-log.ts"
      via: "recordAction의 deps.appendActionLog 자리에 (v, e) => appendActionLog(v, e, tx) — 전이와 같은 tx"
      pattern: "appendActionLog"
---

<objective>
**As a** 연차를 내는 직원과 그 연차를 결재하는 팀장·대표, **I want to** 연차 한 건을 제출하면 그 순간의 결재선이 문서에 고정되고 지금 담당인 사람이 차례대로 승인해 최종 승인까지 가기를, **so that** 결재가 사람 기억이나 메신저가 아니라 시스템의 한 경로로 흐르고, Phase 5는 지출결의를 문서 종류로 더하기만 하면 된다.

이 플랜은 페이즈의 **트레이서**다. 가장 얇은 경로 하나 — 종일 연차 한 건이 제출 → 팀장 승인 → 빈 자리 건너뜀 → 대표 승인 → 최종 승인 — 를 DB 표 · 리포지토리 · 도메인 서비스 · 설정 키 · 문서 번호 · 시드까지 관통시키고 통합 테스트로 끝에서 끝까지 증명한다. 화면 층의 트레이서는 04.1-02 Task 1이 이 서비스 위에 얹는다(서버 액션 파일은 Vitest가 import할 수 없어 화면 경로는 Playwright로만 증명된다 — 두 트레이서를 나눈 이유).

Purpose: ROADMAP 기준 1(표 3개 + `nextStep()` + 등록식 문서 종류 + 빈 자리·대표·자기 승인)과 기준 2의 「제출 시점 고정 · 표시 시점 재해석」이 이 플랜에서 성립한다. 뒤 플랜 넷(화면·잔고·설정·결재 화면)이 전부 이 엔진 위에 선다. ADMN-04는 **결재 부분만(partially covered — 결재 부분)** 이 페이즈가 덮는다 — 결재 단계·담당 계급·조직 범위·규칙 on/off의 설정 키가 이 플랜, 화면이 04.1-04다. 알림 시점·대상은 Phase 7/04.2, 세율·수식은 Phase 5 몫이다(ROADMAP 04.1 기준 2 · REQUIREMENTS 추적표).

Output: 새 표 4개와 마이그레이션 1개(Task 1 안 세 단위 커밋 — ENG-22), `domain/approvals`(순수 함수 + 결정표 + 서비스 + 종류 등록부 + DTO), `domain/leave`(일수 계산 + 제출 + 보임 규칙 `access.ts`), `lib/dates.ts`(`seoulToday`), 설정 키 22개(결재선 17 · 번호 5) + 레지스트리 `SettingDef` 표시 필드 3개(타입만), 시드 한 줄, 단위 6 · 통합 3 · 가드 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md
@.planning/phases/04.1-approvals-leave/04.1-PATTERNS.md
@.planning/phases/04.1-approvals-leave/04.1-VALIDATION.md
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다 — 모든 task가 실패 테스트 먼저다. 테스트·마이그레이션·CI가 하나라도 실패하면 원인을 쫓기 전에 `systematic-debugging` 스킬을 호출한다. 커밋하거나 「완료」라고 말하기 전에 `verification-before-completion` 스킬을 호출하고 아래 verify 명령의 실제 출력으로 판정한다. 서브에이전트에 위임하면 그 프롬프트에도 세 스킬 이름을 적는다(CLAUDE.md [Build]).
- 이 페이즈는 Phase 4와 나란히 간다(ROADMAP 04.1 절). **건드리지 않는 파일**: `domain/rules/register.ts` · `ui/next-turn/*` · 견적 줄·프로젝트 상태·차수·계약 파일 · `domain/permissions/roles.ts`(대표 계급 id 상수는 `domain/approvals` 안에 둔다) · `domain/action-log/*`(재사용만). `domain/settings/keys.ts`·`test/integration/leak-scan.test.ts`·`repositories/settings.ts`(새 함수 `findSimpleValues`)는 **파일 끝에 덧붙이기만** 한다(오케스트레이터 결정 9). `repositories/action-log.ts`는 `appendActionLog`에 선택 셋째 인자 `tx`만 더한다 — Phase 4 04-32가 같은 파일에 **글자 그대로 같은 시그니처**를 더했으므로(`git show origin/claude/gsd-progress-e1nzgu:repositories/action-log.ts`) 그 모양을 복사한다. 병합 때(04.1-07) 이 두 파일은 main 쪽을 택하고 04.1이 덧붙인 함수만 다시 얹는다.
- 마이그레이션은 `pnpm db:generate`로만 만든다. 파일 이름·`_journal.json`을 손대지 않는다. 먼저 병합된 다른 페이즈(Phase 4 등)의 마이그레이션과의 번호 충돌은 04.1-07 Task 1이 병합 직전에 재생성으로 푼다(오케스트레이터 결정 7).
- **마이그레이션 번호(조정자 11:53 규칙 — 페이즈별 번호 범위 지정 폐지, 11:48 교차 페이즈 규칙 포함 · 2026-09-24 20:52 KST 개정 규칙 — 번호 범위 없음 · 병합 순서 제약 없음, 누가 먼저 병합하든 병합 직전에 재생성)**: 브랜치에서는 `pnpm db:generate`가 주는 번호를 그대로 쓴다 — 그 번호는 임시다. drizzle-kit은 이름순 마지막 스냅숏을 이전 스키마로 쓰고 새 번호를 journal 마지막 idx + 1로 매기므로, 병합 직전 04.1-07 Task 1이 「origin/main 병합 → 04.1 자신의 `.sql`과 그 `meta/NNNN_snapshot.json`(각 플랜 SUMMARY `migration_manifest`에 적힌 정확한 목록만)과 journal 항목(손 편집 없이 — 방법은 04.1-07) 삭제 → `pnpm db:generate`」로 재생성해 번호가 main 마지막 + 1이 된다. 어떤 테스트·검사·SUMMARY도 번호 범위를 단언하거나 보고하지 않는다 — 04.1-07은 받은 번호와 연속성만 적는다. 파일 이름을 손으로 바꾸거나 `_journal.json`을 손으로 고치는 일은 이 페이즈 내내 금지다.
- **실행 순서(공유 DB)**: 이 페이즈의 일곱 플랜은 한 줄로 이어진 체인이다(웨이브마다 플랜 하나: 01 → 03 → 02 → 04 → 05 → 06 → 07). 모든 플랜이 같은 로컬 DB(`scripts/dev-db.sh`의 고정 DB `erp` · `erp_test`)에서 통합·E2E와 DB 재생성을 돌리므로, 두 플랜이 동시에 돌지 않는 것은 이 순서가 보장한다 — 잠금·새 도구를 두지 않는다.
- 첫 task를 시작하기 전에 `git rev-parse HEAD` 값을 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도 남긴다). 이 플랜의 diff 기준 검사는 전부 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다 — 커밋한 뒤의 맨 `git diff`는 늘 비어 있어 검사가 공허해진다.
- **「빈 DB」는 명령으로 만든다(CEO-16)**: `scripts/dev-db.sh`는 없는 DB만 만들고 **지우지 않는다**, 통합 global-setup은 `erp_test`에 증분 적용만 한다. 그래서 마이그레이션을 지우고 다시 만들었으면 옛 적용 기록 때문에 drizzle이 새 파일을 조용히 건너뛰거나 「already exists」로 실패한다. 빈 DB 증명과 재생성 뒤 적용은 `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`(테스트 DB)로 하고, 개발 DB `erp`가 필요하면 `PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp WITH (FORCE)' -c 'CREATE DATABASE erp OWNER erp' && pnpm db:migrate`로 다시 만든다(`scripts/reset-test-db.sh`와 같은 모양).
- **「오늘」(CEO-11)**: Cloud Run 프로세스는 `TZ`가 없어 UTC다. 04.1 코드의 「오늘」·연도는 `lib/dates.ts` `seoulToday(now)`에서만 얻고, 시각 의존 함수는 `deps.now?: Date`를 받아 테스트가 시계를 고정한다. 이력형 설정은 `getSettingValue(def, { asOf })`에 서울 날짜를 명시로 넘긴다(레지스트리 기본 asOf는 UTC 날짜다).
</execution_notes>

<probe_fallback>
엣지 프로브가 스펙 없이 돌아 `coverage.json`에 미해결 18개가 남았다. 이 플랜이 닫는 것은 **EXP-03**(adjacency · empty · ordering · idempotency)과 **EXP-04**(adjacency · empty · idempotency · concurrency) 그리고 **LEAV-01**(boundary · precision)이다 — 각각 위 `must_haves.truths`의 문장 하나로 적었다. EXP-04 concurrency는 전에 backstop 항목이었으나 Codex HIGH(설정 17키 키별 SELECT) 반영으로 SELECT 한 문장 + Task 3의 단위 가드 · 결정적 · 경주 통합 테스트로 옮겼다. 나머지(EXP-03 concurrency · EXP-04 ordering · EXP-05 둘 · LEAV-01 idempotency·concurrency · ADMN-04 둘)는 04.1-02 · 04.1-03 · 04.1-04가 닫는다. 조용히 버린 항목은 없다.

UI-SPEC의 미해결 넷 중 이 플랜이 해소하는 것: **U1**(3단 담당 해석 경로 = 팀 경로, 오케스트레이터 결정 1) · **U2**(제출 뒤 담당 소멸 = 빈 자리 건너뜀 → 뒤가 전부 비면 대표, 결정 3) · **U4**(대표 본인 연차 = 명시적 본인 승인, 결정 4). U3(설정 동적 옵션)은 이 플랜이 `SettingDef`에 선택 필드 타입만 더하고 화면 배선은 04.1-04가 한다.

**계획 가정(확인 필요 시 뒤집으면 해당 함수만 바뀐다)**
1. 연차 번호 기본 서식 = 접두어 `LV` · 연도 2자리 · 구분자 `-` · 순번 4자리 · 시작 1 → `LV26-0001`. 서식 조립 함수가 접두어를 연도 앞에 두므로(`documentNumberFormat`) `26LV-…` 모양은 서식 키만으로 만들 수 없다. 번호 연도 = 첫 제출 날짜(서울 날짜 `seoulToday` — CEO-11)의 연도(프로젝트 번호가 등록 연도를 쓰는 선례)
2. 결재 문서가 보이는 사람 = 기안자 · 지금 단계 후보 · 이 문서에서 한 번이라도 처리한 사람. 그 밖은 없는 문서(404)다(D-17 선례)
3. **모든 단계(대표 폴백 포함)**의 후보에서 이 차수에 이미 승인한 사람을 뺀다 — 사용자 결정 CEO-5 A 「한 번만」(2026-09-24 10:39 카드, 확정). 그래서 후보가 0명이 된 단계는 빈 자리로 건너뛴다. 대표 폴백 후보가 0명이고 이 차수 승인이 1건 이상이면 최종 승인이다(같은 사람이 두 번 승인하지 않는다)
4. 연차 기간이 회계연도를 넘으면 거부한다(새 문구 `기간이 회계연도를 넘음 · 12-31과 01-01로 나눠 신청` — `원인 · 다음 행동` 규약)
5. 새로 만든 계급의 결재·연차 정보 노출 행이 없는 문제(CEO-10)는 기본값 A — 알려진 한계로 기록만 한다. `createRole`에 노출 행 시드를 넣거나 결재 필드를 숨김 불가로 바꾸지 않는다(`roles.ts`는 이 페이즈 금지 파일 · D-35 노출 모델 유지). 공개 전에 관리자가 새 계급을 만들면 정보 노출표에서 `결재 정보`·`연차 정보`를 켜야 한다 — 04.1-07이 TODOS.md와 SUMMARY에 옮긴다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 종일 연차 한 건이 제출 → 팀장 승인 → 빈 자리 건너뜀 → 대표 승인 → 최종 승인 (DB · 리포지토리 · 도메인 · 설정 · 번호 · 시드)</name>
  <files>db/schema/approvals.ts, db/schema/leave.ts, db/schema/index.ts, repositories/approvals.ts, repositories/leave-requests.ts, repositories/org-snapshot.ts, repositories/permissions.ts, repositories/settings.ts, repositories/action-log.ts, domain/approvals/route.ts, domain/approvals/kinds.ts, domain/approvals/dto.ts, domain/approvals/index.ts, domain/approvals/tx-log.ts, domain/leave/days.ts, domain/leave/dto.ts, domain/leave/index.ts, domain/leave/access.ts, domain/document-numbering/index.ts, domain/settings/registry.ts, domain/settings/keys.ts, domain/permissions/info-items.ts, domain/seed/approvals-leave.ts, domain/seed/index.ts, lib/dates.ts, test/integration/approvals-route-fixed.test.ts, test/integration/approvals-concurrency.test.ts, test/integration/leak-scan.test.ts, test/unit/domain/leave/days.test.ts, test/unit/lib/dates.test.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md` 110–270행(System Architecture Diagram · Recommended Project Structure · Pattern 1~5 · Anti-Patterns) — 순수 상태 기계, 제출 시점 고정 + 표시 시점 재해석, 낙관적 잠금, 설정 레지스트리 결재 단계, 번호 재사용
    - 같은 문서 284–332행(Pitfall 1~4) — 특히 Pitfall 2(3단 해석 경로)와 Pitfall 3(`scopeFor()`를 결재함 필터로 쓰지 않는다)
    - `.planning/phases/04.1-approvals-leave/04.1-PATTERNS.md`(전문) — 파일별 가장 가까운 선례와 병합 위험 표
    - `docs/inputs/phase-05-approval.md` §1~§5 — 결재선 4단 · 빈 자리 · 자기 승인 · 반려 · 연차 원문
    - `db/schema/org.ts` · `db/schema/auth.ts` · `db/schema/index.ts` — FK 대상(users.id는 text, teams.id·org_units.id는 uuid)과 barrel 끝 줄
    - `db/schema/` 안의 `version` 열을 가진 표 하나(`grep -rn "version" db/schema`로 찾는다) — 낙관적 잠금 열 선언 모양
    - `repositories/quote-lines.ts`의 `WHERE version = … RETURNING` 갱신 함수 — 0행 = 충돌 판정 모양
    - `repositories/team-memberships.ts` · `repositories/users.ts` · `repositories/document-counters.ts`(`DbOrTx`) — viewer 첫 인자 규약과 트랜잭션 전달
    - `lib/db-transaction.ts` — `withTransaction(fn)`; domain은 db를 import하지 않는다
    - `domain/org/index.ts` — `teamAtDate`와 `deps?: Partial<…>` 주입 모양
    - `domain/people/index.ts` 1–90행 — `DtoSpec` · `registerDto` · `project()` 사용 모양
    - `domain/document-numbering/index.ts`(전문) — `DOCUMENT_NUMBER_FORMAT_DEFS`에 줄 하나를 더하는 자리
    - `domain/settings/registry.ts`(전문 245행) · `domain/settings/keys.ts` 230–330행 — `SettingDef` 필드, `document_number.project.*` 다섯 키 모양, `SETTING_DEFS` 배열
    - `test/unit/settings/registry-coverage.test.ts` — `export const NAME: SettingDef<…> = {` 형식과 「`readBy` 없는 키는 domain/settings 밖에서 참조되어야 한다」 규칙
    - `domain/action-log/record.ts` 8–100행 — `recordAction(viewer, {actionType, entity, entityId, documentId, detail})`, 트랜잭션 인자 없음
    - `domain/permissions/info-items.ts`(전문) · `domain/permissions/visible.ts` — 노출 행이 없으면 거짓(대표·본부 책임자·팀장은 지금 노출 행이 없다)
    - `repositories/permissions.ts` 56–120행 — `insertPermissionIfAbsent`와 `upsertVisibility` 모양
    - `domain/seed/index.ts` 98–232행 — ORG_SEED(경영관리본부/경영관리팀), 설정 기본값 루프가 `default === undefined`를 건너뛰는 규칙
    - `test/integration/setup.ts` — 매 테스트 전 전 표 비우고 재시드
    - `test/integration/leak-scan.test.ts` 1–40행 — side-effect import 목록의 끝
    - `domain/quotes/lines.ts` 450–465행 — `timeZone: "Asia/Seoul"` 선례(`seoulToday`가 같은 방식으로 Intl을 쓴다)
    - `git show origin/claude/gsd-progress-e1nzgu:docs/ARCHITECTURE.md | sed -n 168,190p` — Phase 4 04-32의 §4-8 트랜잭션·잠금 규약(3) 「잠근 트랜잭션 안에서는 전역 db(풀)를 부르지 않는다」 원문
    - `lib/env.ts` 50–58행(`DB_POOL_MAX` 기본 5) · `domain/document-numbering/index.ts` 66–106행(`loadDocumentNumberFormat`이 `getSettingValue` 다섯 번 — 트랜잭션 안에서 부르면 풀 연결을 더 잡는다)
    - `repositories/settings.ts` 1–20행(`findSimpleValue` — 키 하나에 SELECT 하나, Codex HIGH 스냅숏의 근거) · `domain/settings/registry.ts` 60–92행(`getSettingValue`의 default · `SettingNotFoundError` 판정 — 새 일괄 함수가 같은 규칙을 한 번 읽은 행에 적용한다)
    - `repositories/action-log.ts` 1–40행(`appendActionLog`가 전역 `db`로 INSERT) · `domain/action-log/record.ts` 80–140행(`RecordActionDeps` — `appendActionLog`·`isActionTypeEnabled` 주입 자리, 끌 수 있는 종류는 설정을 풀로 읽는다, fail-open) · `git show origin/claude/gsd-progress-e1nzgu:repositories/action-log.ts | sed -n 18,40p`(Phase 4 04-32가 더한 `tx?: DbOrTx` — 같은 모양으로 복사한다)
  </read_first>
  <behavior>
    - (통합) 기획1팀에 기획 PM 기안자·팀장 계급 한 명, 대표 계급 한 명이 있고 기획본부 책임자·경영관리본부 소속자가 없을 때: 기안자가 2026-09-21(월)~09-23(수) 종일을 신청하면 인스턴스 `submitted`, 번호 `LV26-0001`, 차수 1의 단계 행이 넷(팀장 · 본부 책임자 · 경영관리본부 · 대표)이고 `days_quarters` = 12
    - (통합) 팀장의 `listMyInbox` `mine`에 그 문서가 있고 대표·기안자에게는 없다
    - (통합) 팀장이 승인하면 상태 `in_review`, 2·3단은 후보 0명이라 건너뛰고 지금 단계는 4단(대표)이다
    - (통합) 대표가 승인하면 상태 `approved`이고 단계 두 개에 처리자·시각·`approved`가 저장되어 있다 — 대표의 기록은 4단 행(`step_index` 4 · `is_fallback` 거짓)에 있고 `is_fallback` 참인 행은 0개다(ENG-1 — 전사 범위가 null → 0명으로 새면 4단이 빈 자리가 되고 대표가 폴백 행으로 승인해 이 단언이 깨진다)
    - (통합) 제출 뒤 설정 `approval_route.leave.step2.role_id`를 바꿔도 그 문서의 단계 행 넷은 그대로다
    - (통합) 행동 로그에 `document_submit` 1건 · `document_approve` 2건이 entity `approval_instance`로 남는다
    - (단위) 종일 월~수 = 12쿼터, 금~월 = 8쿼터(주말 제외), 토~일만 = 0 → 거부, 반차 = 2 · 반반차 = 1 · 재택 3일 = 0, 12-30~01-02 = 거부, 반차에 오전/오후가 없으면 거부, 종료일 < 시작일 거부
    - (단위, CEO-11) `seoulToday(new Date("2026-12-31T15:30:00Z"))` = `2027-01-01`, `seoulToday(new Date("2026-12-31T14:59:59Z"))` = `2026-12-31`
    - (통합, CEO-11) `deps.now` = 2026-12-31T15:30Z로 2027-01-04(월) 종일을 신청하면 번호 `LV27-0001` · `fiscal_year` 2027
    - (통합, CEO-2) 서로 다른 기안자 여섯 명이 `submitLeave`를 `Promise.all`로 동시에 부르면 기본 풀(`DB_POOL_MAX` 5)에서 여섯 건 전부 테스트 제한 시간 안에 끝나고 번호 여섯 개가 서로 다르다(`LV26-0001`~`LV26-0006`, 순서는 단언하지 않음)
    - (통합, Codex HIGH 원자성) `deps.appendActionLog`를 던지게 주입하고 `submitLeave`를 부르면 오류가 나고 `leave_requests` · `approval_instances` · `approval_routes` · `approval_steps` 행 수 · 문서 번호 카운터 값 · `action_log` 행 수가 호출 전과 같다. 같은 주입으로 1단 후보가 `approveDocument`를 부르면 오류가 나고 인스턴스 `version` · 상태 · 단계 처리 기록 · `action_log` 행 수가 그대로다. 주입 없이 다시 부르면 둘 다 성공하고 로그 행이 1건씩 생긴다
  </behavior>
  <action>
**중간 커밋 지점(ENG-22 — 이 task는 크므로 실패하면 되돌릴 단위를 셋으로 나눈다).** 각 단위는 확인 명령이 녹색일 때만 커밋하고(한 커밋 한 의도 · 제목 영어 접두어 + 본문 한국어), 한 단위가 실패하면 마지막 녹색 단위 커밋으로 돌아가(`git restore` · push 전 로컬의 `git reset --hard <직전 단위 커밋>`) 그 단위만 다시 한다 — 앞 단위 커밋은 그대로 둔다.
- **단위 A 「스키마 + 마이그레이션」** = 아래 ①. 확인: `pnpm typecheck` · `bash scripts/dev-db.sh && pnpm db:migrate` · `pnpm db:generate` 재실행이 "No schema changes". 커밋 예 `feat(04.1-01): add approvals and leave schema with migration`.
- **단위 B 「순수 함수」** = ③ `domain/approvals/route.ts`(기본 경로 — 결정표 전 칸은 Task 3) · ⑤의 `domain/leave/days.ts` · ⑥의 `lib/dates.ts`. `days.test.ts` · `dates.test.ts`를 먼저 적어 실패를 본 뒤 초록. 확인: `pnpm vitest run --project unit test/unit/domain/leave/days.test.ts test/unit/lib/dates.test.ts` · `pnpm typecheck`. 커밋 예 `feat(04.1-01): add pure approval route, leave day count and seoulToday`.
- **단위 C 「서비스」** = ② ④ ⑤ 나머지 ⑥ 나머지 ⑦ ⑧ ⑨. 트레이서 통합 describe를 먼저 적어 실패를 본 뒤 초록. 확인: 아래 verify 세 명령 전부. 커밋 예 `feat(04.1-01): wire approval and leave services end to end`.
- 스키마는 단위 A에서 끝낸다. 단위 B·C 도중 스키마를 고쳐야 하면 두 번째 마이그레이션을 만들지 않는다 — 단위 A의 마이그레이션을 **git으로** A 이전 상태로 되돌리고(`git checkout <A 커밋>^ -- db/migrations/meta/_journal.json`, A가 만든 `.sql`·스냅숏은 `git rm`) 스키마를 고친 뒤 `pnpm db:generate`로 다시 만들어 04.1-01의 SQL을 한 파일로 유지하고, 실행 노트의 「빈 DB는 명령으로 만든다」대로 개발·테스트 DB를 다시 만든다. journal·SQL을 손으로 고치지 않는다(결정 7a).

① **스키마** `db/schema/approvals.ts`: `approval_instances`(id uuid PK · `document_kind` text · `document_id` uuid · `drafter_id` text FK users · `status` text 기본 `draft` · `current_round` int 기본 0 · `version` int 기본 1 · `updated_by` text FK users null · `created_at`·`updated_at` timestamptz, UNIQUE(document_kind, document_id), status 인덱스) · `approval_routes`(id · `instance_id` FK · `round` int · `self_approval` text · `drafter_team_id` uuid null · `drafter_org_unit_id` uuid null · `submitted_at`, UNIQUE(instance_id, round)) · `approval_steps`(id · `route_id` FK · `step_index` int · `label` text · `role_id` text null(FK 없음 — null = 계급 조건 없음) · `scope_kind` text(`team`|`org_unit`|`company`) · `scope_target_id` uuid null · `is_fallback` boolean 기본 false · `acted_by` text FK users null · `acted_at` null · `action` text null(`approved`|`rejected`) · `reason` text null · `self_approved` boolean 기본 false, UNIQUE(route_id, step_index), `acted_by` 인덱스 — 처리함 목록 조회용, CEO-17). `db/schema/leave.ts`: `leave_requests`(id · `number` text null UNIQUE · `drafter_id` · `kind` text(`full_day`|`half_day`|`quarter_day`|`remote`) · `start_date`·`end_date` date(문자열 모드, org.ts 선례) · `half` text null(`am`|`pm`) · `days_quarters` int · `fiscal_year` int · `note` text null · timestamps). 열 이름·제약은 기존 스키마 파일 관례(snake_case, `withTimezone`)를 그대로 따른다. 상태 기계 표의 문자열 열에는 DB CHECK를 건다(Codex MEDIUM — `db/schema/corp-cards.ts`의 `check()` 선례): `approval_instances.status` ∈ {draft, submitted, in_review, approved, rejected, withdrawn} · `approval_steps.scope_kind` ∈ {team, org_unit, company} · `approval_steps.action`은 null 또는 {approved, rejected} · `approval_routes.self_approval` ∈ {skip, self_approve} · `leave_requests.kind` ∈ 네 값 · `leave_requests.half`는 null 또는 {am, pm} · `leave_requests.days_quarters` ≥ 0. 값 목록은 도메인 상수와 같은 문자열이다. `db/schema/index.ts` 끝에 두 줄 export. 이어서 `pnpm db:generate`로 마이그레이션을 만들고(손 편집 금지 — 결정 7a) `bash scripts/dev-db.sh && pnpm db:migrate`로 적용한다.

② **리포지토리**(전부 viewer 첫 인자, `DbOrTx` 선택 인자): `repositories/approvals.ts` — 인스턴스 삽입, 차수·단계 삽입, 인스턴스·**모든 차수**·단계 한 번에 읽기(관련자 판정에 모든 차수의 `acted_by`가 필요하다 — ENG-6 · D1), `updateInstanceStatus(viewer, {id, expectedVersion, status, currentRound, updatedBy}, tx)`(version 조건 + `version + 1` + RETURNING, 0행이면 null), 단계 처리 기록, 대표 폴백 단계 행 삽입(`is_fallback` = true · 계급 = 대표 계급 id · 범위 `company` · 대상 id null · 이름 `대표` — 저장된 폴백 행도 ENG-1 규칙으로 대표 전원에 해석된다), 내 결재함 후보 목록용 진행 중 인스턴스 목록(`submitted`·`in_review`), 내가 처리한 인스턴스 최근 50건. `repositories/leave-requests.ts` — 삽입·번호 기록·단건·기안자별 목록. `repositories/org-snapshot.ts` — `listOrgSnapshot(viewer, asOf)`: 보관되지 않은 사람마다 id·이름·계급·`asOf` 이하 최신 발령의 팀 id·그 팀의 본부 id를 **쿼리 한 번**으로(사용자 30명 규모 — 사람마다 따로 조회하지 않는다). `repositories/permissions.ts` 끝에 `insertVisibilityIfAbsent`(insertPermissionIfAbsent와 같은 모양 — 관리자가 바꾼 노출 값을 재시드가 덮지 않게). `repositories/settings.ts` **끝에** `findSimpleValues(viewer, keys)` — `settings_simple`에서 `key IN (…)` **SELECT 한 문장**으로 행 배열을 돌려준다(키마다 따로 부르지 않는다 — Codex HIGH 스냅숏). `repositories/action-log.ts`의 `appendActionLog`에 선택 셋째 인자 `tx?: DbOrTx`(`@/db/client`의 타입)를 더하고 INSERT를 `(tx ?? db)`로 한다 — 인자가 없으면 지금 동작 그대로(Phase 4 04-32와 같은 모양, execution_notes).

③ **순수 함수** `domain/approvals/route.ts`(DB·설정 import 없음): `nextStep(status, event)` — 사건은 `submit`·`approve`·`approve_final`·`reject`·`withdraw`·`resubmit`, 표 밖이면 `InvalidTransitionError`. `resolveHolders(step, snapshot)` — 계급 조건(null이면 무조건) × 범위(`team`=팀 id 일치, `org_unit`=본부 id 일치, `company`=전원). **대상 id가 null이면 0명인 규칙은 `team`·`org_unit`에만 건다 — `company`는 대상 id를 보지 않는다**(ENG-1 — `prepareSubmission`이 전사 범위의 대상 id를 늘 null로 저장하므로, null → 0명을 전사에 걸면 전사 단계가 늘 빈 자리가 되어 앞 단계 승인만으로 조용히 최종 승인된다). `walkRoute({steps, selfApproval, drafterId, snapshot, fallbackRoleId, at})` — `at`은 `"before_action"`(행동 전 계산: 제출 준비 · 결재선 보기 · 결재함 · 승인의 후보 판정) 또는 `"after_approval"`(승인 반영 뒤: `approveDocument`가 지금 viewer의 승인을 처리 기록에 더해 결과를 정할 때)이고 기본값이 없는 필수 입력이다. 처리 기록 뒤 첫 단계부터 훑어 「처리 가능(후보 목록 · 본인 승인 여부)」·「최종」·「대표 폴백」·「막힘(대표 없음 · 행동 전 최종)」 중 하나와 표시용 단계 목록, 그리고 `currentHolderIds`(지금 차수 단계 행 전부 — 처리한 단계와 저장된 폴백 행 포함 — 를 스냅숏으로 `resolveHolders`한 담당의 합집합에, 결과가 「대표 폴백」이면 그 폴백 후보(대표 계급 재직자 전원 — 폴백 단계 행이 아직 없어도 지금 담당이다, X-3)를 더한 것. 기안자·승인자를 빼기 전. 막힘(대표 없음 · 행동 전 최종)이면 폴백 후보를 더하지 않는다. 04.1-02 Task 3 `isApprovalParty`의 입력 이름과 같다 — ENG-6 · D1)를 돌려준다. **`at`이 `"before_action"`인데 결과가 최종이면 막힘을 돌려준다**(ENG-3 · D2 — DB 상태가 진행 중인 문서의 「최종」은 조회로 생긴 고아 최종이라 누구도 처리할 수 없다. 호출자는 「최종」을 스스로 해석하지 않고 이 한 판정만 쓴다 — 읽기·쓰기 경로가 같은 결과를 낸다). 후보 = 담당 − 기안자 − 이 차수 승인자. 기안자가 담당이었는데 남은 후보가 없으면 자기 승인 값을 따른다(`skip` = 통과로 보고 계속, `self_approve` = 기안자가 이 차수에 아직 승인하지 않았으면 기안자 단독 후보 · 이미 승인했으면 빈 자리 — ENG-2). 칸별 규칙은 Task 3 ②의 결정표가 유일한 정의다. 이 task는 기본 경로(처리 가능 · 빈 자리 건너뜀 · 최종 · 전부 비면 대표)만 초록으로 만들고, 가장자리 규칙의 전 사례는 Task 3가 테스트로 굳힌다. **문서 종류 이름으로 분기하지 않는다**(금지 항목).

④ **종류 등록부·서비스** `domain/approvals/kinds.ts`: `registerDocumentKind({kind, label, loadRouteConfig, href, describeDocuments, routeSettings?, canResubmit?})`(`canResubmit?: (viewer: Viewer) => Promise<boolean>`은 선택 — 04.1-02의 `getApprovalView`가 이 필드가 있고 참일 때만 `다시 신청`을 보이고, 연차는 04.1-02에서 `canResubmit: canWriteLeave`로 등록한다. 이 플랜은 타입에 선택 필드만 둔다 — 테스트 안에서 등록하는 테스트 전용 종류 `test_memo`(Task 3 · 04.1-02)는 이 필드를 두지 않는다, CX-W1 · CX2-W2. `routeSettings`는 선택 — 그 종류의 결재선 설정 키 묶음 `{selfApproval, steps: [{enabled, roleId, scope, orgUnitId}] × 4}`이고, 04.1-04의 설정 화면 경고가 등록된 종류를 순회할 때 쓴다. 테스트 전용 종류는 이 값이 없어 설정 화면에 나타나지 않는다. 같은 kind 두 번 등록은 예외) · `getDocumentKind(kind)`(없으면 예외). `domain/approvals/dto.ts`: 결재함 항목 · 결재선 보기 DTO 명세를 `registerDto`로 등록 — 필드 전부 `approval.value`. `domain/approvals/index.ts`: 대표 계급 id 상수(`role-ceo`)를 이 모듈 안에 둔다. **읽기와 쓰기를 나눈다(CEO-2 — Phase 4 04-32 ARCHITECTURE §4-8 (3))**: `prepareSubmission(viewer, {kind, drafterId, deps?})` — **트랜잭션 전에** 종류의 `loadRouteConfig()`를 **한 번** 불러 얻은 객체 하나(17키를 SELECT 한 문장으로 — ⑥), 행동 로그 켜짐 여부(`loadActionLogGate()` — ⑨), `listOrgSnapshot(viewer, today)`(today = `seoulToday(deps.now)`), 기안자의 제출일 소속 팀·본부 id를 읽고 단계 계획(사용 중인 단계만 순서대로 — 범위 `drafter_team`/`drafter_org_unit`은 그 팀·본부 id, `org_unit`은 설정의 부서 id, `company`는 null, 단계 이름 `label`은 계급 이름 · 없으면 부서 이름 · 전사면 `전사`)과 walkRoute 결과를 만든다. walkRoute가 「막힘」이면 여기서 제출을 거부한다(트랜잭션을 열기 전). `submitDocument(viewer, prepared, {documentId}, tx)` — 준비된 계획을 `tx` 리포지토리 호출로만 인스턴스·차수·단계 행에 쓰고, 그 끝에 `recordActionInTx(viewer, {document_submit …}, tx, prepared.gate)`로 로그 행을 같은 tx에 쓴다(설정·스냅숏을 다시 읽지 않는다 — Codex HIGH 원자성). `approveDocument(viewer, {instanceId, expectedVersion}, deps?)` — 스냅숏과 행동 로그 켜짐 여부는 트랜잭션 **전에** 읽고, 트랜잭션 안에서는 인스턴스·차수·단계를 `tx`로 읽은 뒤 **이 순서로 판정·기록한다(CEO-6)**: (1) `version !== expectedVersion`이면 오류 — 후보 판정보다 먼저. 오류 종류는 D1대로 가른다(ENG-6): viewer가 관련자(기안자 · 모든 차수 단계의 `acted_by` · walkRoute(`at: "before_action"`) 결과 `currentHolderIds` — 지금 차수 단계 행 전부를 트랜잭션 전 스냅숏으로 `resolveHolders`한 담당의 합집합 + 지금 대표 폴백 자리면 폴백 후보(X-3), 기안자·승인자를 빼기 전)면 `ApprovalConflictError`(04.1-02 Task 3가 문구를 `updated_by`·상태로 조립한다), 아니면 `NotCurrentHolderError`(`지금 담당이 아님 · 새로 고침`, 로그 reason `not_holder`). 관련자 판정 `isApprovalParty`와 이 갈래 배선은 04.1-02 Task 3이 넣는다 — 이 플랜은 판정 재료(`currentHolderIds` · 모든 차수 `acted_by`)를 준비하고, 불일치를 전부 기본 문구(처리자 이름·시각 없음)의 `ApprovalConflictError`로 둔다 → (2) 종결 상태면 거부 — 종결 판정은 사건별이다(CX-B1 · 04.1-02 ② (2)와 같은 규칙): `approved`·`withdrawn`은 모든 사건에 종결, `rejected`는 기안자의 `resubmit`을 뺀 모든 사건에 종결(`nextStep("rejected", "resubmit")`만이 `rejected`에서 나가는 유일한 전이 — 기안자 판정은 04.1-02 ② (3)). 그래서 `approveDocument`(사건 `approve`)에는 세 상태가 모두 종결이다. 04.1-02 Task 3 뒤에는 관련자에게만 `최종 승인됨 · 새로 고침` 등 상태 문구, 관련자가 아니면 version이 맞아도 `NotCurrentHolderError` + `not_holder`(D1과 같은 갈래). 이 플랜에서는 상태 문구 → (3) walkRoute(`at: "before_action"`)로 viewer가 후보가 아니면 `NotCurrentHolderError`(`지금 담당이 아님 · 새로 고침`) — 결과가 막힘(행동 전 최종 포함 — D2)이면 누구도 후보가 아니다 → (4) viewer의 승인을 처리 기록에 더한 walkRoute(`at: "after_approval"`)로 결과(`approve`/`approve_final`, 폴백 행 필요 여부)를 메모리에서 계산하고 `nextStep` — `approve_final`은 이 계산의 최종에서만 나온다(ENG-3 · D2) → (5) **상태 UPDATE를 먼저**(version 조건 + `version + 1`, 0행이면 다시 읽어 충돌 오류) → (6) 그다음 단계 처리 기록·폴백 행 삽입 → (7) 같은 tx에서 `recordActionInTx`로 `document_approve` 로그(Codex HIGH 원자성 — 로그가 실패하면 (5)(6)도 롤백). 이 순서라 두 경쟁자는 UPDATE의 행 잠금에서 줄을 서고, 진 쪽은 단계 행을 쓰기 전에 0행으로 멈춘다(UNIQUE 위반 원시 오류가 나지 않는다). 오류 클래스는 `UserFacingError` 하위 `ApprovalConflictError`(이 task는 기본 문구까지) · `NotCurrentHolderError`. `approveDocument`·`submitLeave`의 `deps`는 선택 `appendActionLog`를 받아 `recordActionInTx`에 넘긴다(실패 주입 테스트용 — 프로덕션 호출자는 넘기지 않는다). `listMyInbox(viewer)` — 진행 중 인스턴스마다 walkRoute(`at: "before_action"`)를 돌려 viewer가 후보인 것(`mine`, 제출 오름차순 — 막힘이면 행동 전 최종을 포함해 누구의 `mine`에도 넣지 않는다, D2)과 viewer가 처리한 것(`processed`, 처리 내림차순 50건)을 종류의 `describeDocuments`로 요약을 붙여 DTO로 돌려준다. `getApprovalView(viewer, {kind, documentId})` — 보이는 사람(계획 가정 2)이 아니면 null, 맞으면 단계 표시 목록 · viewer가 할 수 있는 행동 · version(walkRoute `at: "before_action"` — 막힘이면 지금 단계 `담당 없음`이고 승인·반려 행동 없음. 기안자의 회수는 막힘 · 고아 최종과 무관하게 가능하다 — D2 · X-2, 회수는 04.1-02). `listMyInbox`·`getApprovalView`는 읽기 전용이다 — 행동 전 최종(D2)을 만나도 상태·version·단계 행을 쓰지 않는다. **`scopeFor()`로 결재함을 거르지 않는다**(Pitfall 3).

⑤ **연차** `domain/leave/days.ts`(순수): `countLeaveQuarters({kind, startDate, endDate, half})` — 평일 수 × 단위, 검증 실패는 칸 이름과 UI-SPEC Copywriting 「Error — 입력」 문구를 담은 오류 목록으로 돌려준다(주말만 · 종료일 < 시작일 · 반차·반반차 하루 · 오전/오후 필수 · 회계연도 넘음). `formatLeaveDays(quarters)` → `3일`·`0.5일`·`0.25일`. 공휴일은 빼지 않는다(UI-SPEC Assumptions #4). `domain/leave/dto.ts`: 연차 신청 DTO와 **연차 잔고 DTO 명세**(연차·월차 두 줄 필드 — 04.1-03이 채운다, 인터페이스 먼저)를 `registerDto`로 등록 — 필드 전부 `leave.value`. `domain/leave/index.ts`: 적재 때 `registerDocumentKind`(종류 키 = leave, 라벨 = 연차, 결재선 로더 · href · 요약 함수 · 아래 17키 묶음을 `routeSettings`로)와 `import "./dto"`; `loadRouteConfig`는 아래 설정 17키를 `getSimpleSettingValues` **한 번**(SELECT 한 문장 — ⑥)으로 읽어 객체 하나로 조립(키마다 `getSettingValue`를 부르지 않는다 — 금지 항목); `submitLeave(viewer, input, deps?)` — today = `seoulToday(deps.now)`(CEO-11) → days 검증 → **트랜잭션 전 읽기**: `prepareSubmission` · 연차 번호 서식(`loadDocumentNumberFormat("leave")`) → `withTransaction` 안에서 `tx` 호출만: 신청 행 삽입 → `submitDocument(prepared, tx)`(인스턴스 · 차수 · 단계 · `document_submit` 로그 행) → **마지막 쓰기로** `allocateDocumentNumber`(카운터 키 = leave, 연도 = today의 연도, 미리 읽은 서식, tx)와 번호 기록(카운터 행 잠금을 가장 짧게 잡는다 — CEO-2). 커밋 뒤에 따로 부르는 행동 로그는 없다(Codex HIGH 원자성). `domain/leave/access.ts`의 `canSeeLeaveDocument(viewer, leaveRow, deps?)` 하나에 문서 보임 규칙(계획 가정 2 — 결재 쪽 판정은 `getApprovalView`의 규칙을 그대로 쓴다)을 두고 `getLeave`·`listMyLeave(viewer, fiscalYear)`가 이것만 부른다 — 04.1-03의 `balance-service.ts`도 `index.ts`가 아니라 이 파일을 import해 순환이 생기지 않는다(CEO-4).

⑥ **설정·번호** `domain/settings/registry.ts`의 `SettingDef`에 선택 필드 셋을 **타입만** 더한다(동작은 04.1-04): `optionLabels?: Record<string, string>` · `dynamicOptions?: "roles" | "org_units"` · `effectiveFromRule?: "year_start"`. 같은 파일에 **덧붙이기만 하는** 일괄 읽기 `getSimpleSettingValues(defs, deps?)`(Codex HIGH 스냅숏): 비이력형 정의 배열을 받아(이력형이 섞이면 `SettingKindMismatchError`) `findSimpleValues(SYSTEM_VIEWER, keys)`를 **한 번** 부르고, 정의 순서대로 값 배열을 돌려준다 — 행이 있으면 `def.schema.parse`, 없으면 `def.default`, default도 없으면 `undefined`(던지지 않는다 — 호출자가 정한다, CEO-7의 `""` 처리는 Task 3). `deps.findSimpleValues`로 주입 가능. 기존 `getSettingValue`는 바꾸지 않는다. `domain/settings/keys.ts` **끝에** 결재선 17키(namespace `연차 결재선`, 라벨·힌트는 UI-SPEC Copywriting 「관리자 설정 라벨·힌트(S8)」 그대로, `self_approval`은 enum `skip`|`self_approve` + optionLabels `건너뜀`/`본인 승인`, `scope`는 enum `drafter_team`|`drafter_org_unit`|`company`|`org_unit` + optionLabels `기안자 팀`/`기안자 본부`/`전사`/`특정 부서`, `role_id`는 z.string() + dynamicOptions `roles`, `org_unit_id`는 z.string() + dynamicOptions `org_units`; 3단 `org_unit_id`만 default 없음 — 시드가 채운다. `role_id` 힌트는 `— 이면 계급 무관 · 그 범위의 누구나`(계획 가정 — UI-SPEC에 없는 한 문장), `org_unit_id`에는 힌트를 두지 않는다 — 04.1-04가 조직 범위가 특정 부서가 아닐 때 네 `N단 특정 부서` 칸을 비활성화해 그 상태가 뜻을 전한다(CLAUDE.md §7 안내 문구 최소, CX-W2). 04.1-04는 keys.ts를 고치지 않으므로 라벨·힌트·optionLabels·dynamicOptions는 여기서 최종형으로 적는다)와 `document_number.leave.*` 다섯 키(namespace `문서 번호`, 계획 가정 1)를 덧붙이고 `SETTING_DEFS` 배열에 올린다. `domain/document-numbering/index.ts` `DOCUMENT_NUMBER_FORMAT_DEFS`에 `leave` 줄 하나, 그리고 **덧붙이기만 하는** 두 변경(CEO-2): `loadDocumentNumberFormat`에 `export`를 붙이고, `allocateDocumentNumber`의 input에 선택 필드 `format?: DocumentNumberFormat`을 더해 주어지면 설정을 다시 읽지 않고 `documentNumberFormat`으로 조립한다(없으면 지금 동작 그대로 — 프로젝트 번호 호출자는 바뀌지 않는다). 새 파일 `lib/dates.ts`: `seoulToday(now = new Date()): string` — `Intl.DateTimeFormat`(`timeZone: "Asia/Seoul"`)으로 `YYYY-MM-DD`(CEO-11, `domain/quotes/lines.ts` 선례와 같은 방식). 04.1 domain 코드는 연도를 `Number(seoulToday(now).slice(0, 4))`로 얻는다.

⑦ **정보 항목·시드·누수 스캔** `domain/permissions/info-items.ts` 끝에 `approval.value`(결재 정보) · `leave.value`(연차 정보), 둘 다 staffDefault 참. `domain/seed/approvals-leave.ts`의 `seedApprovalsLeave(viewer)` — `findOrgUnitByName("경영관리본부")`의 id를 3단 부서 키에 `seedSimpleValue`(이미 값이 있으면 두지 않는다), 두 정보 항목을 계급 5종 모두에 `insertVisibilityIfAbsent` 참. `domain/seed/index.ts`는 return 직전에 **한 줄** 호출만 더한다. `test/integration/leak-scan.test.ts` **파일 끝에** `import "@/domain/approvals";`와 `import "@/domain/leave";` 두 줄(ESM import는 끌어올려진다 — 프로젝트 eslint에 `import/first` 규칙이 없음을 확인했다).

⑧ **테스트** `test/integration/approvals-route-fixed.test.ts`의 첫 describe가 behavior 여섯 줄과 LV27 사례를 순서대로 단언한다(사람·발령은 도메인·리포지토리 함수로 만든다 — SQL 직접 삽입 금지). `test/unit/domain/leave/days.test.ts`가 일수 단위 사례를, `test/unit/lib/dates.test.ts`가 `seoulToday` 두 사례를 단언한다. `test/integration/approvals-concurrency.test.ts`(새 파일 — 04.1-02 Task 3가 describe를 더한다)의 첫 describe가 동시 제출 6건을 단언한다. `approvals-route-fixed.test.ts`의 둘째 describe `행동 로그 실패 주입 — 전이와 함께 롤백`이 behavior의 원자성 사례를 단언한다(행 수 · 카운터 · version을 호출 전후로 비교).

⑨ **행동 로그 원자성(Codex HIGH — 01·02 공통 장치)** 새 파일 `domain/approvals/tx-log.ts`: `loadActionLogGate(deps?)` — 트랜잭션 **전에** `ACTION_LOG_OPTIONAL_TYPES`를 `getSettingValue`로 한 번 읽어 `(actionType) => boolean`을 돌려준다(끌 수 없는 종류는 항상 참, 읽기가 실패하면 전부 참 — `domain/action-log/record.ts`의 fail-open 규칙과 같다). `recordActionInTx(viewer, entry, tx, gate, deps?)` — `recordAction(viewer, entry, { appendActionLog: (v, e) => (deps?.appendActionLog ?? appendActionLog)(v, e, tx), isActionTypeEnabled: async (t) => gate(t) })`. 그래서 로그 쓰기도 켜짐 판정도 풀 연결을 잡지 않고(CEO-2) 로그 행이 전이와 같은 커밋에 들어간다. `domain/action-log/*`는 고치지 않는다 — 그 파일의 deps 주입 계약만 쓴다. Phase 4 병합 뒤 `recordAction`에 `deps.tx`가 생겨도 이 주입은 그대로 동작한다(04.1-07). 이 페이즈의 결재 전이(제출 · 승인, 04.1-02의 반려 · 회수 · 다시 신청)만 이 경로를 쓰고, Phase 3 코드의 커밋 뒤 기록은 바꾸지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/leave/days.test.ts test/unit/lib/dates.test.ts test/unit/settings</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — registry-coverage가 새 키 하나라도 「참조 없음」으로 잡거나 `seoulToday`가 UTC 날짜를 돌려주면 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-route-fixed.test.ts test/integration/approvals-concurrency.test.ts test/integration/leak-scan.test.ts test/integration/seed-permissions.test.ts</automated>
    <fails_when>마이그레이션 적용이 실패하거나, 트레이서 describe의 단언 하나라도 실패하거나(최종 상태가 approved가 아님 · 단계 행 수가 4가 아님 · 번호가 LV26-0001/LV27-0001이 아님), 동시 제출 6건이 제한 시간을 넘기거나(풀 고갈) 번호가 겹치거나, 로그 실패 주입 뒤 신청·인스턴스·단계 행이나 카운터·version 변화가 남거나(Codex HIGH 원자성), 누수 스캔이 `approval.value`/`leave.value` 미등록으로 실패한다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(repository-viewer-param · require-action-client · money-boundary 포함) 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - 트레이서 경로(제출 → 팀장 승인 → 2·3단 건너뜀 → 대표 승인 → approved)가 통합 테스트 한 describe에서 끝까지 녹색이다
    - (ENG-22) Task 1의 커밋이 단위 A(스키마 + 마이그레이션) → B(순수 함수) → C(서비스) 순서의 세 커밋 이상이고, SUMMARY가 단위마다 커밋 해시와 그 시점 확인 명령의 결과를 적는다. 단위 A 커밋의 diff는 `db/schema/*`와 `db/migrations/*`뿐이다(`git show --name-only <A 커밋>`)
    - `domain/approvals/route.ts`가 `@/db`·`@/repositories`·`@/domain/settings`를 import하지 않는다(`grep -v '^\s*//' domain/approvals/route.ts | grep -c "from \"@/\(db\|repositories\|domain/settings\)"`이 0)
    - `domain/approvals/*.ts`에 `"leave"` 문자열 리터럴이 없다(`grep -v '^\s*//' domain/approvals/*.ts | grep -c '"leave"'`이 0)
    - `approval_steps`에 사람 id를 담는 열은 `acted_by`(처리 기록)뿐이다
    - 결재선 설정 17키 · 번호 5키가 `SETTING_DEFS`에 있고 registry-coverage 단위 테스트가 녹색이다
    - `domain/seed/index.ts` diff가 한 줄 추가(+ import 한 줄)뿐이다
    - `test/integration/leak-scan.test.ts` diff가 파일 끝 덧붙임뿐이다(`git diff -U0 "$PLAN_BASE"..HEAD -- test/integration/leak-scan.test.ts`의 hunk가 마지막 줄 뒤 하나)
    - (CEO-2) `withTransaction` 콜백 안에서 부르는 함수가 전부 `tx`를 받는 리포지토리·`submitDocument`·`allocateDocumentNumber`뿐이다 — 실행자가 `domain/leave/index.ts`·`domain/approvals/index.ts`의 콜백 범위 호출 목록을 SUMMARY에 표로 적고, 동시 제출 6건 통합 사례가 런타임 증거다. `domain/document-numbering/index.ts` diff는 `leave` 줄 · `export` 한 단어 · 선택 `format` 분기뿐이다
    - (CEO-11) `grep -rnE "getFullYear\(\)|new Date\(\)\.toISOString" domain/approvals domain/leave`가 0건이고 `lib/dates.ts`가 `seoulToday`를 내보낸다
    - (CEO-4) `domain/leave/access.ts`가 `canSeeLeaveDocument`를 내보내고 `domain/leave/index.ts`가 그것을 쓴다(`grep -c "canSeeLeaveDocument" domain/leave/index.ts`가 1 이상)
    - (CEO-17) `db/schema/approvals.ts`에 `acted_by` 인덱스 선언이 있다
    - (Codex HIGH 원자성) `recordAction(`을 직접 부르는 곳이 `domain/approvals`·`domain/leave` 안에서 `tx-log.ts` 하나뿐이다(`grep -rln "recordAction(" domain/approvals domain/leave`가 `domain/approvals/tx-log.ts` 한 줄) · `git diff --stat "$PLAN_BASE"..HEAD -- domain/action-log`가 비어 있다 · `repositories/action-log.ts` diff가 선택 `tx` 인자와 `(tx ?? db)`뿐이다
    - (Codex HIGH 스냅숏) `repositories/settings.ts` diff가 파일 끝 `findSimpleValues` 덧붙임뿐이고, `domain/leave/index.ts`에 `getSettingValue(` 호출이 없다(`grep -c "getSettingValue(" domain/leave/index.ts`가 0 — 결재선 17키는 `getSimpleSettingValues`로만)
    - (Codex MEDIUM) `db/schema/approvals.ts` · `db/schema/leave.ts`에 `check(` 선언이 여섯 개 이상 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>종일 연차 한 건이 DB·도메인·설정·번호를 관통해 제출부터 최종 승인까지 가고, 그 경로가 통합 테스트로 증명된다. 설정은 SELECT 한 문장으로, 스냅숏·로그 켜짐 여부는 트랜잭션 전에 읽고, 로그 행은 전이와 같은 tx에 쓰며, 번호는 마지막에 매기고, 「오늘」은 서울 날짜다</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 스키마 확정 — 마이그레이션 적용 · SQL 린트 · journal 가드</name>
  <files>db/migrations/meta/_journal.json, test/unit/db/migration-journal.test.ts</files>
  <read_first>
    - `db/migrations/meta/_journal.json`(전문) — 지금 main 11항목(idx 0~10)
    - `scripts/migrate-runner.ts` 40–70행 — 54행은 drizzle `migrate()`를 **부르기만** 한다
    - `node_modules/drizzle-orm/pg-core/dialect.js` 50–70행 — 57~62행이 건너뛰기 판정의 실제 자리: 마지막 적용 `created_at`보다 `when`이 오래된 마이그레이션은 조용히 건너뛴다(가드 테스트가 막는 위험의 근거)
    - `.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md` 286–300행(Pitfall 1)
    - `test/unit/docs-limits.test.ts` — 파일을 읽어 단언하는 단위 테스트 모양
  </read_first>
  <action>
① Task 1이 만든 스키마 위에서 `pnpm db:generate`를 다시 돌려 **새 파일이 생기지 않음**을 확인한다(스키마와 마이그레이션이 일치). 생기면 Task 1의 스키마 변경이 두 번에 나뉜 것이므로 새 파일을 지우지 말고 `systematic-debugging`으로 원인을 찾는다.

② **실제로 빈 DB를 만들어**(CEO-16 — `scripts/dev-db.sh`는 DB를 지우지 않는다) 전부 적용한다: `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`. Task 1 도중 마이그레이션을 지우고 다시 만든 적이 있으면 개발 DB `erp`도 실행 노트의 psql 한 줄로 다시 만든 뒤 `pnpm db:migrate`한다. 이어 `pnpm lint:sql`(squawk)을 통과시킨다. squawk가 새 SQL을 거부하면 **스키마 TS를 고쳐 재생성**한다 — SQL 파일을 손으로 고치지 않는다(결정 7a).

③ `test/unit/db/migration-journal.test.ts`를 만든다(결정 7b · 11:48 교차 페이즈 규칙): `_journal.json`을 읽어 (a) `entries[i].idx === i`(0..n-1 빈틈 없음), (b) 각 `tag`가 `String(idx).padStart(4, "0") + "_"`로 시작, (c) `when`이 앞 항목보다 엄격히 크다, (d) 각 tag의 `.sql` 파일이 `db/migrations/`에 있다, (e) 각 idx의 `db/migrations/meta/{idx 네 자리}_snapshot.json`이 있다, (f) `meta/` 안의 `*_snapshot.json` 파일 집합이 journal idx 집합과 정확히 같다 — journal에 없는 스냅숏이 남으면 drizzle-kit이 이름순 마지막 스냅숏을 이전 스키마로 잘못 잡는다를 단언한다. 실패 메시지는 위반한 idx·tag·파일을 적는다. **번호 범위를 단언하지 않는다(조정자 11:53 — 번호 범위 지정 폐지).** 「마지막 스냅숏 = 지금 스키마」는 이 저장소의 기존 장치인 `pnpm db:generate` 재실행 결과 "No schema changes"로 증명한다(첫 검증 명령 — 단위 테스트 안에서 drizzle-kit을 돌리지 않는다). 이 테스트는 main의 11항목에서도 녹색이어야 한다(04.1이 더한 항목을 빼도 성립하는 조건만 단언한다).

④ **마이그레이션 manifest**: SUMMARY 머리(frontmatter)에 기계 판독용 `migration_manifest:` 목록을 적는다 — 04.1-01이 만든 journal tag · SQL 경로 · 스냅숏 경로 각 한 줄(04.1-07 Task 1이 병합 때 **이 목록에 있는 파일만** 지운다 — Codex HIGH 07).
  </action>
  <verify>
    <automated>pnpm db:generate 2>&amp;1 | tee /dev/stderr | grep -q "No schema changes"</automated>
    <fails_when>출력에 "No schema changes"가 없다 — 스키마 TS와 생성된 마이그레이션이 어긋났다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql</automated>
    <fails_when>DROP/CREATE 또는 빈 `erp_test`로의 마이그레이션 적용이 0이 아닌 코드로 끝나거나 squawk가 위반을 하나라도 출력한다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 "No test files found"가 나온다 — journal 항목의 스냅숏이 없거나 journal에 없는 스냅숏이 남아 있으면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `db/migrations/`에 04.1이 만든 SQL 파일이 정확히 하나이고 이름이 drizzle-kit이 준 그대로다
    - `git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json`의 변경이 끝에 항목 하나 추가뿐이다
    - journal 가드 테스트가 여섯 조건(a~f — 스냅숏 존재 · 고아 스냅숏 없음 포함)을 단언하고 녹색이며, 테스트 파일에 하드코딩된 네 자리 마이그레이션 번호가 없다(`grep -cE "\b00[0-9]{2}\b" test/unit/db/migration-journal.test.ts`가 0 — 번호 범위를 단언하지 않는다)
    - SUMMARY frontmatter에 `migration_manifest:`가 있고 tag · SQL · 스냅숏 세 경로가 실제 파일과 같다
    - `pnpm lint:sql` 경고·오류 0
  </acceptance_criteria>
  <done>스키마가 DB에 적용되었고 재생성 차이가 0이며, journal 순서를 지키는 가드 테스트가 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 결재선 가장자리 규칙을 결정표 한 장과 테스트로 굳힌다 — 자기 승인 · 승인 0건 대표 폴백 · 한 사람 한 번 · 전사 범위 · 담당 소멸 · 고아 최종 = 막힘 · 설정 빈 값 · 설정 한 번 읽기 · 비후보 거부 · 관련자 판정 재료 · 운영 로그 · 두 번째 문서 종류</name>
  <files>domain/approvals/route.ts, domain/approvals/index.ts, domain/leave/index.ts, test/unit/domain/approvals/next-step.test.ts, test/unit/domain/approvals/resolve-step.test.ts, test/unit/domain/approvals/document-kind-registry.test.ts, test/unit/domain/leave/route-config.test.ts, test/integration/approvals-route-fixed.test.ts, test/integration/approvals-concurrency.test.ts</files>
  <read_first>
    - Task 1 산출물 `domain/approvals/route.ts` · `domain/approvals/kinds.ts` · `domain/approvals/index.ts`(전문)
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 473–476행(U1~U4 — 해소 기록)과 210행(제출 뒤 결재선 문구 — 후보 1·2·3명 이상 표시, `— 경영관리 · 담당 없음`)
    - `.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md` 177–223행(Pattern 1·2)
    - `test/unit/org/` 안의 테스트 하나 — `deps` 주입으로 DB 없이 도메인 함수를 시험하는 모양
    - `domain/settings/registry.ts` 80–95행 — 기본값 없는 단순 키에 행이 없으면 `SettingNotFoundError`를 던지는 자리(CEO-7)
    - `lib/log.ts`(전문 28행) — `log.info`/`log.warn(event, fields)` 한 줄 JSON(CEO-18)
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` 132–137행(CEO-1) · 159–164행(CEO-5) — 규칙의 근거
  </read_first>
  <behavior>
    - nextStep: 여섯 사건 × 여섯 상태 표 전체를 단언 — 허용 칸은 기대 상태, 나머지는 `InvalidTransitionError`
    - 기안자가 팀장이고 자기 승인 `skip`: 1단이 통과로 표시되고(`팀장 단계 건너뜀(자기 승인 없음)`) 지금 단계는 2단
    - 같은 조건에서 `self_approve`: 1단 후보가 기안자 한 사람이고 승인하면 `self_approved` = 참
    - 1단에 팀장이 둘: 후보 둘 다, 둘 중 누구든 처리 가능. 표시 이름은 1명 = 이름, 2명 = `이수아 · 박지훈`, 3명 이상 = `이수아 외 2명`
    - 1~4단이 전부 빈 자리(대표 계급도 없음): walkRoute = 막힘, `prepareSubmission`이 제출 거부(트랜잭션을 열지 않는다)
    - 1~3단 빈 자리 + 기안자가 대표 + 자기 승인 `skip`: 대표 폴백 후보가 기안자 한 사람이고 승인하면 `self_approved` = 참으로 최종 승인(결정 4)
    - 제출 뒤 1단 팀장을 보관(`setUserArchived`)하면 1단은 빈 자리로 건너뛰고 지금 단계가 다음 비지 않은 단계가 된다. 이미 승인한 단계의 처리자는 보관 뒤에도 그대로 보인다(결정 3)
    - 1단 승인 뒤 남은 단계(설정으로 대표 단계를 끈 결재선)가 전부 비면 대표 폴백으로 가고, 대표가 이미 이 차수에서 승인했다면 최종 승인이다(계획 가정 3)
    - 제출 뒤 기안자가 다른 팀으로 발령돼도 1단 범위(제출 때의 팀 id)는 그대로다
    - 테스트 전용 종류 `test_memo`(메모리 결재선 설정 — 1단 팀장 하나)가 같은 submitDocument · approveDocument로 최종 승인까지 간다. 같은 kind 두 번 등록은 예외, 등록 안 된 kind 제출은 예외
    - (단위, CEO-1 ①) 네 단계가 전부 꺼져 단계 행이 0개: walkRoute = 대표 폴백, 후보 = 대표 — 최종 승인이 아니다
    - (단위, CEO-1 ②) 기안자가 팀장 · 1단만 사용 · 자기 승인 `skip`: 1단은 통과로 표시되지만 이 차수 승인이 0건이라 대표 폴백, 후보 = 대표
    - (단위, CEO-1 ③) 네 단계 전부 꺼짐 + 기안자가 대표: 폴백 후보가 기안자뿐 → 대표가 본인 승인하면 `self_approved` = 참으로 최종 승인(결정 4)
    - (단위 + prepareSubmission, CEO-1 ④) 네 단계 전부 꺼짐 + 대표 계급 재직자 없음: walkRoute = 막힘, 제출이 `대표 없음 · 관리자에게 대표 계급 확인 요청`으로 거부된다
    - (단위, CEO-5) 경영관리팀 기안자: 경영관리팀 팀장이 1단을 승인하면 3단 후보에서 빠지고(3단의 다른 후보가 없으면 3단은 빈 자리로 건너뜀), 경영관리팀에 소속된 대표가 3단을 승인하면 4단은 후보 0명 → 빈 자리로 건너뛰어 최종 승인이다. 같은 사람의 단계 처리 기록은 차수마다 1건
    - (단위, 결정표) `resolve-step.test.ts`의 사례 이름이 Task 3 ② 결정표의 칸 id(R1~R6 · W1~W13)로 시작하고 19칸 모두 사례가 하나 이상이다 — 위 사례들도 해당 칸 id를 이름 앞에 단다
    - (단위, ENG-1 · R2 · R5 · R6) `resolveHolders`: `team` 범위 대상 id null → 0명 · `company` 범위(대상 id null) × 대표 계급 → 대표 재직자 전원 · `company` × 계급 없음 → 재직자 전원(walkRoute 후보는 기안자를 뺀 전원)
    - (단위, ENG-1 「전사 단계만 켠 결재선」 · R5 · W1 · W12) 1~3단 꺼짐 · 4단 대표 × 전사: 단계 행 1개(`company` · 대상 id null)이고 walkRoute(`before_action`) = 처리 가능, 지금 단계 = 4단 자체(폴백 아님), 후보 = 대표. 대표 승인을 더한 walkRoute(`after_approval`) = 최종
    - (단위, ENG-1 「1단 승인 뒤 전사 단계」 · R5 · W1) 1단 팀장 × 기안자 팀 · 2·3단 꺼짐 · 4단 본부 책임자 × 전사: 팀장 승인을 더한 walkRoute(`after_approval`) = 처리 가능, 지금 단계 = 4단, 후보 = 모든 본부의 본부 책임자이고 대표는 후보가 아니다(전사에 null → 0명이 걸리면 4단이 빈 자리가 되어 대표 폴백으로 새므로 이 단언이 깨진다)
    - (단위, ENG-2 「1단 본인 승인 + 3단 유일 담당 = 기안자」 · W5 · W6) 경영관리팀 팀장이 기안자 · 자기 승인 `self_approve` · 2단 꺼짐 · 경영관리팀 소속은 기안자뿐 · 대표는 다른 팀: 1단 후보 = 기안자 단독(W5). 기안자의 1단 본인 승인(`self_approved`)을 더한 walkRoute(`after_approval`)는 3단을 빈 자리로 건너뛰고(W6) 지금 단계 = 4단, 후보 = 대표. 같은 처리 기록으로 부른 `before_action`에서도 기안자는 3단 후보가 아니다 — 한 차수에서 한 사람이 두 번 승인할 수 없다
    - (단위, ENG-3 · D2 「대표 1단 승인 뒤 남은 담당 전원 소멸」 · W9 · W10) 1단 대표 × 전사 · 2단 본부 책임자 × 기안자 본부 · 3단 경영관리본부 · 4단 대표 × 전사, 1단에 대표 승인 기록, 스냅숏에 2·3단 담당 없음: 같은 입력으로 `at: "before_action"` = 막힘(표시 지금 단계 `담당 없음`, 후보 0명) · `at: "after_approval"` = 최종
    - (단위, ENG-3 · D2 「남은 단계가 기안자 skip 통과뿐」 · W4 · W13) 경영관리팀 기안자 · 자기 승인 `skip` · 2·4단 꺼짐 · 1단 경영관리팀 팀장 승인 기록 · 3단 담당 = 기안자와 그 팀장뿐: 3단은 통과(W4)이고 `before_action` = 막힘 · `after_approval` = 최종
    - (단위, ENG-6 · D1 재료) walkRoute 결과 `currentHolderIds`: 기안자가 팀장(1단 담당)이고 1단 승인 기록 · 2단 담당 둘 · 3단 빈 자리 · 4단 대표일 때 = {기안자 · 1단 처리자 · 2단 둘 · 대표}(기안자·승인자를 빼기 전 합집합, 순서 무관) · 단계 전부 빈 자리면 빈 배열 · `at` 값에 따라 바뀌지 않는다
    - (단위, ENG-6 · D1 · X-3 「폴백 자리의 폴백 후보는 지금 담당」) 1단 팀장 × 기안자 팀 승인 기록 · 2·3단 켜짐이나 담당 없음 · 4단 꺼짐 · 대표 계급 재직자 둘(폴백 단계 행 없음): walkRoute(`before_action`) = 대표 폴백이고 `currentHolderIds` = {1단 팀장 · 대표 둘}(대표 둘이 폴백 행 없이 들어 있다). 같은 결재선에서 대표 계급 재직자가 없으면 막힘이고 `currentHolderIds` = {1단 팀장}(폴백 후보를 더하지 않는다)
    - (단위, CEO-7) `loadRouteConfig`에 주입한 일괄 설정 읽기(`findSimpleValues`)가 3단 `org_unit_id` 행을 돌려주지 않으면(값 `undefined`) 결과 3단 `orgUnitId`가 `""`이고 다른 16칸은 그대로다
    - (단위, Codex HIGH 스냅숏) `loadRouteConfig` 한 번에 주입한 `findSimpleValues`가 **정확히 1회**, 17키 전부를 한 배열로 받아 불리고, 함께 주입한 키 단위 `findSimpleValue`(부르면 던지는 가짜)는 0회다
    - (통합, Codex HIGH 스냅숏 — 결정적) 테스트 안 Drizzle 트랜잭션이 `step1.role_id`·`step2.role_id`를 둘 다 새 값 Y로 바꾸고 **커밋 전에 멈춘** 동안 `prepareSubmission`을 부르면 1·2단 계급이 둘 다 옛 값 X이고, 커밋 뒤 다시 부르면 둘 다 Y다
    - (통합, Codex HIGH 스냅숏 — 경주) 두 키를 한 트랜잭션으로 바꾸는 기존 설정 가져오기 경로(`applySettingsImport`)로 X↔Y를 번갈아 쓰는 루프와 `prepareSubmission`을 `Promise.all`로 30회 겹쳐 돌리면, 매번 준비된 결재선의 1·2단 계급 쌍이 (X, X) 또는 (Y, Y)이고 (X, Y)·(Y, X)는 한 번도 나오지 않는다
    - (통합, CEO-7) 시드 뒤 3단 `org_unit_id` 설정 행을 지우고 연차를 제출하면 원시 오류 없이 제출되고, 3단 단계 행의 대상 id가 null이며 3단은 빈 자리로 건너뛴다
    - (통합, CEO-8) 1단이 지금 단계인 문서에서 기안자 · 무관한 기획 PM · 대표(4단 전)가 각각 맞는 version으로 `approveDocument`를 부르면 셋 다 `NotCurrentHolderError`, 단계 처리 기록 0건, `version` 그대로다
    - (통합, CEO-18) 위 세 거부마다 `log.info`가 `approval.refused`와 `{instanceId, viewerId, reason: "not_holder", expectedVersion, actualVersion}`으로 한 번씩 불린다(`vi.spyOn(log, "info")`). 제출 뒤 모든 담당과 대표를 보관해 막힌 문서의 `getApprovalView`는 `log.warn("approval.route_blocked", {instanceId, kind, round, stepIndex})`를 한 번 부른다. 로그 필드에 이름·사유 글자가 없다
    - (통합, ENG-6 · D1 — 04.1-01 단독 상태) 팀장이 1단을 승인해 version이 2가 된 문서에 옛 version 1로 `approveDocument`를 부르면: 무관한 기획 PM · 기안자 둘 다 거부되고 단계 처리 기록 0건 · version 2 그대로다. 「처리자 이름 · `HH:MM` 없음」은 **무관한 기획 PM의 오류 문구에만** 단언한다. 기안자 쪽은 오류가 `ApprovalConflictError`라는 종류만 단언하고 문구는 단언하지 않는다(CX-R01 — 기안자는 D1의 관련자라 04.1-02 Task 3 뒤 처리자 이름 · 시각이 담긴 상세 문구를 받으며, 그 문구는 04.1-02가 단언한다). 이 사례는 오류 종류를 무관한 사용자 쪽에서 단언하지 않는다 — 무관한 사용자의 `NotCurrentHolderError` · reason `not_holder` 정확 일치 단언은 관련자 갈래를 넣는 04.1-02 Task 3이 더한다(그래서 04.1-02 뒤에도 이 사례는 그대로 녹색이다)
    - (통합, ENG-3 · D2 — 담당 소멸 고아 최종, 픽스처 도우미 `setupOrphanFinal()` — X-2) 기획본부 책임자 한 명 · 경영관리팀 소속 한 명(대표는 경영관리팀 밖)이 있는 픽스처에서 결재선 설정 1단 = 대표 × 전사(나머지 기본값)로 제출 → 대표가 1단 승인(`in_review`, 지금 단계 2단) → 2·3단 담당 둘 다 보관(`setUserArchived`): 대표의 `getApprovalView` 한 번이 지금 단계 `담당 없음` · 승인·반려 행동 없음을 돌려주고 `log.warn("approval.route_blocked")`를 정확히 1회 부른다 · 기안자 · 대표 · 무관한 기획 PM의 `listMyInbox` `mine`에 그 문서가 0건 · 두 조회 뒤 인스턴스 상태 `in_review` · version · 단계 행이 조회 전과 같다(읽기 경로 쓰기 없음) · 대표가 맞는 version으로 `approveDocument` → `NotCurrentHolderError` · 기록 0건. 이어 새 사람을 경영관리팀에 발령하면(담당 복구) 그 사람의 `mine`에 한 건이 생기고 그 사람의 승인으로 상태가 `approved`가 된다. 같은 픽스처의 「기안자 회수 성공」 사례는 회수를 만드는 04.1-02 Task 2가 **이 describe 안에** `setupOrphanFinal()`로 더한다(X-2 — 한 곳)
  </behavior>
  <action>
① behavior의 순수 사례를 `next-step.test.ts`·`resolve-step.test.ts`에 먼저 적고 실패를 확인한 뒤 `route.ts`를 고쳐 초록으로 만든다. walkRoute는 입력(단계·처리 기록·스냅숏·자기 승인 값·폴백 계급)만으로 결정적이어야 한다 — 「오늘」이 필요하면 인자로 받는다.

② 대표 폴백 규칙을 한 곳(walkRoute)에 둔다. **후보 = 담당 − 기안자 − 이 차수에 이미 승인한 사람**(모든 단계 공통 — 사용자 결정 CEO-5 A). 끝까지 훑어 처리할 단계가 없을 때 **폴백 조건** = (진행 위치에서 남은 단계가 있었는데 전부 빈 자리) **또는 (이 차수의 `approved` 기록이 0건)**(CEO-1 — 단계를 전부 껐거나 `skip`만으로 끝나도 결재 없이 통과하지 않는다. `skip` 통과는 승인으로 세지 않고 빈 자리로도 세지 않는다). 폴백 자리에는 `skip`을 적용하지 않는다(적용하면 승인 0건이 되기 때문이다 — 결정 4). **`resolveHolders`·`candidatesFor`·`fallback`의 동작은 아래 결정표 두 장이 유일한 정의다**(eng-review 아키텍처 권고 — 칸마다 단위 사례가 하나 이상 있고 테스트 이름이 칸 id로 시작한다. 표에 없는 조합은 없다).

담당 해석(`resolveHolders`, ENG-1):

| 칸 | 범위 · 대상 id | 계급 조건 | 담당 | 단위 사례 |
|---|---|---|---|---|
| R1 | `team` · 팀 id | 있으면 그 계급 · null이면 무관 | 그 팀 현재 소속 재직자(계급 조건 적용) | 트레이서 1단 팀장 |
| R2 | `team` · null(기안자 팀 없음) | 무관 | 0명 | 「팀 없는 기안자의 1단은 빈 자리」(ENG-1) |
| R3 | `org_unit` · 본부 id | 있으면 그 계급 · null이면 무관 | 그 본부 아래 팀 현재 소속 재직자(계급 조건 적용) | 3단 경영관리본부 담당(U1 · 계급 없음) |
| R4 | `org_unit` · null(설정 `""`, CEO-7) | 무관 | 0명 | 「3단 부서 없음은 빈 자리」 |
| R5 | `company` · 대상 id 무시(늘 null로 저장) | 있음 | 전사 그 계급 재직자 전원 | 「전사 단계만 켠 결재선」(ENG-1) · 「1단 승인 뒤 전사 단계」(ENG-1) |
| R6 | `company` · 대상 id 무시 | 없음 | 재직자 전원 | 「전사 × 계급 없음은 기안자를 뺀 전원」(ENG-1) |

자리 판정(`candidatesFor` · `fallback` · 끝 판정). H = 지금 해석한 담당, A = 이 차수 승인자(본인 승인 포함), C = H − 기안자 − A. 폴백 자리의 H = 대표 계급 재직자, C = H − A − 기안자:

| 칸 | 자리 | 담당 · 후보 | 기안자 ∈ H | 이 차수 승인자 | 자기 승인 값 | 호출 시점(`at`) | 결과 | 단위 사례 |
|---|---|---|---|---|---|---|---|---|
| W1 | 단계 | C ≠ ∅ | 무관 | 무관 | 무관 | 무관 | 처리 가능 · 후보 C | 「1단 팀장 둘」 |
| W2 | 단계 | H = ∅ | — | 무관 | 무관 | 무관 | 빈 자리(건너뜀) | 「2·3단 담당 없음」 · 「제출 뒤 1단 팀장 보관」 |
| W3 | 단계 | H ≠ ∅ · C = ∅ | 아니오 | H 전원이 A | 무관 | 무관 | 빈 자리(CEO-5) | CEO-5 경영관리팀 기안자 |
| W4 | 단계 | C = ∅ | 예 | 무관 | `skip` | 무관 | 통과(`… 건너뜀(자기 승인 없음)`) | 「기안자 팀장 · skip」 |
| W5 | 단계 | C = ∅ | 예 | 기안자 ∉ A | `self_approve` | 무관 | 처리 가능 · 후보 = 기안자 단독(승인하면 `self_approved`) | 「기안자 팀장 · self_approve」 |
| W6 | 단계 | C = ∅ | 예 | 기안자 ∈ A | `self_approve` | 무관 | 빈 자리(ENG-2 — 한 차수 한 사람 한 승인) | 「1단 본인 승인 + 3단 유일 담당 = 기안자」 |
| W7 | 폴백 | C ≠ ∅ | 무관 | 무관 | 적용 안 함 | 무관 | 처리 가능 · 후보 C(승인하면 폴백 행) | CEO-1 ① · ② |
| W8 | 폴백 | C = ∅ · 기안자 ∈ H − A | 예 | 기안자 ∉ A | 적용 안 함 | 무관 | 처리 가능 · 후보 = 기안자 본인 승인(`self_approved`) | CEO-1 ③ · 「1~3단 빈 자리 + 기안자 대표」(결정 4) |
| W9 | 폴백 | H − A = ∅ | 무관 | 1건 이상 | 적용 안 함 | 승인 반영 뒤 | 최종(`approve_final`) | CEO-5 대표 3단 승인 · 계획 가정 3 |
| W10 | 폴백 | H − A = ∅ | 무관 | 1건 이상 | 적용 안 함 | 행동 전 | 막힘(고아 최종 — ENG-3 · D2) | 「대표 1단 승인 뒤 남은 담당 전원 소멸」 |
| W11 | 폴백 | H − A = ∅ | 무관 | 0건 | 적용 안 함 | 무관 | 막힘(대표 없음 — 제출 준비면 제출 거부) | CEO-1 ④ · 「1~4단 빈 자리 + 대표 없음」 |
| W12 | 끝(폴백 조건 거짓) | — | — | 1건 이상 | 무관 | 승인 반영 뒤 | 최종(`approve_final`) | 트레이서 대표 4단 승인 |
| W13 | 끝(폴백 조건 거짓) | — | — | 1건 이상 | 무관 | 행동 전 | 막힘(고아 최종 — ENG-3 · D2) | 「남은 단계가 기안자 skip 통과뿐」 |

막힘(W10 · W11 · W13)은 모두 같게 다룬다: 제출 준비면 제출 거부, 진행 중이면 지금 단계를 `담당 없음`으로 표시하고 누구의 결재함 `mine`에도 뜨지 않으며 승인은 `NotCurrentHolderError` — 관리자가 조직(담당)을 고치면 다시 처리 가능이 된다. 호출 시점 열이 `무관`이 아닌 칸은 W9 · W10 · W12 · W13뿐이고, 그 판정은 walkRoute 안(`at`)에만 있다(D2 — 호출자가 「최종」을 따로 해석하지 않는다).

③ 표시 목록의 후보 이름 조립(1명·2명·3명 이상)을 walkRoute 표시 결과에 넣어 화면이 문자열을 조립하지 않게 한다. 처리한 단계는 저장된 처리자 이름, 지금·남은 단계는 스냅숏 해석이다.

④ `document-kind-registry.test.ts`: 테스트 전용 종류를 등록·중복·미등록 세 사례로 단언하고, 등록부는 테스트마다 초기화할 수 있게 테스트용 초기화 함수를 **테스트 파일 안에서만** 쓰는 형태로 둔다(프로덕션 코드에 테스트 전용 분기를 두지 않는다 — 필요하면 등록부를 `new Map`으로 감싼 팩토리를 내보내 테스트가 새 인스턴스를 만든다).

⑤ `approvals-route-fixed.test.ts`에 둘째·셋째 describe를 더한다 — 담당 소멸(보관)·발령 뒤 범위 고정·`test_memo` 종류 끝까지. 이어 describe `담당 소멸 뒤 고아 최종 — 막힘으로 다룬다(ENG-3 · D2)`가 behavior의 D2 통합 사례를 단언한다(보관 해제가 아니라 새 사람 발령으로 담당을 복구한다 · 조회 전후 인스턴스 행을 비교해 읽기 경로의 쓰기 0을 단언 · `vi.spyOn(log, "warn")`). 04.1-02 Task 2가 같은 describe에 「기안자 회수 성공」 사례를 더할 수 있게 픽스처를 describe 안 도우미 함수 `setupOrphanFinal()` 하나로 둔다 — 대표 1단 승인 · 2·3단 담당 보관까지 끝낸 상태에서 `{instanceId, version, drafter, ceo, planningPm}`을 돌려준다(이름은 04.1-02 Task 2와 같게 — X-2). 통합에서 `test_memo`의 `document_id`는 임의 uuid다(문서 표 없음 — 결재 모듈이 문서 표를 모른다는 증거).

⑥ **설정 빈 값(CEO-7) · 한 번 읽기(Codex HIGH 스냅숏)**: `domain/leave/index.ts`의 `loadRouteConfig`는 Task 1 ⑥의 `getSimpleSettingValues`가 돌려준 값 중 기본값 없는 `org_unit_id` 키(지금은 3단)가 `undefined`이면 `""`로 읽는다(일괄 함수는 없는 키에 던지지 않으므로 `SettingNotFoundError`를 잡는 갈래가 필요 없다 — 동작은 CEO-7과 같다) — `""`는 「특정 부서 없음」이라 대상 id null → 빈 자리 → 건너뜀/대표 폴백이고, 04.1-04의 설정 화면 경고(`부서 없음 · 이 단계는 빈 자리로 건너뜀`)와 같은 뜻이다. 다른 키가 `undefined`이면(기본값이 있어 생기지 않는 경우) 원인 키 이름을 담아 던진다. 로더의 일괄 읽기를 `deps.findSimpleValues`로 주입받게 해 `test/unit/domain/leave/route-config.test.ts`가 DB 없이 돈다. 스냅숏 통합 두 사례는 `test/integration/approvals-concurrency.test.ts`에 describe 하나로 둔다 — 결정적 사례의 멈춘 트랜잭션은 테스트 안의 Drizzle `db.transaction` + 약속(deferred)으로 만들고(`sleep` 금지), 경주 사례의 쓰기는 두 키를 한 트랜잭션으로 쓰는 기존 `applySettingsImport`(설정 JSON 가져오기의 실제 경로)를 쓴다. 관리자 화면 저장은 칸마다 독립이므로 이 테스트가 증명하는 성질은 「한 제출 = 한 커밋 시점의 설정」이다(truth 문구와 같다). 통합 사례에서 설정 행 삭제는 시드 뒤 테스트 안에서만 한다(설정 리포지토리에 삭제 함수가 없으면 테스트 파일 안의 Drizzle `delete` — 프로덕션 코드에 삭제 함수를 더하지 않는다).

⑦ **비후보 거부 · 운영 로그(CEO-8 · CEO-18)**: 넷째 describe가 behavior의 비후보 세 사례를 단언한다(단계 처리 기록 수와 `version`을 호출 전후로 비교). `approveDocument`의 거부 갈래(충돌 · 후보 아님 · 종결)는 던지기 직전에 `log.info("approval.refused", {instanceId, viewerId, reason: "conflict" | "not_holder" | "final", expectedVersion, actualVersion})`를, walkRoute가 「막힘」을 돌려준 `approveDocument` · `getApprovalView` · `listMyInbox`는 요청마다 문서당 한 번 `log.warn("approval.route_blocked", {instanceId, kind, round, stepIndex})`를 남긴다. 필드는 id·코드·숫자만 — 이름·반려 사유 글자를 싣지 않는다(T-04.1-42). **관련자 판정 재료(ENG-6 · D1)**: walkRoute 결과에 `currentHolderIds`(Task 1 ③)를 채우고, `approveDocument`는 (1)·(2) 갈래를 한 도우미 안에 두어 04.1-02 Task 3이 그 자리에 `isApprovalParty(viewer.id, {drafterId, actedByIds, currentHolderIds})`(04.1-02가 `domain/approvals/conflict-message.ts`에 만든다)를 한 번에 끼울 수 있게 한다 — `actedByIds`는 Task 1 ②의 모든 차수 읽기에서, `currentHolderIds`는 트랜잭션 전 스냅숏으로 부른 walkRoute에서 온다. 이 플랜은 관련자 판정 함수를 따로 만들지 않는다(중복 금지). 다섯째 describe가 behavior의 ENG-6 단독 상태 사례를 단언한다.

⑧ **리팩터(CEO-13, 동작 보존)**: 위 표가 전부 초록이 된 뒤 walkRoute를 `route.ts` 안의 비공개 도우미 넷으로 나눈다 — `candidatesFor(step, snapshot, ctx)`(담당 − 기안자 − 이 차수 승인자) · `advance(steps, actions, ctx)`(처리 기록 · 빈 자리 · 자기 승인 통과) · `fallback(ctx)`(대표 · 본인 승인 · 최종 · 막힘) · `describeHolders(names)`(1명 · 2명 · 3명 이상 표시). 내보내는 이름은 `walkRoute` 그대로이고 **단위 표는 한 줄도 고치지 않은 채** 다시 초록이어야 한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/approvals test/unit/domain/leave/route-config.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 승인 0건이 최종으로 끝나거나(CEO-1), 1단 승인자가 3단 후보에 남거나(CEO-5), 전사 단계가 빈 자리로 새거나(ENG-1), 이미 본인 승인한 기안자가 뒤 단계 후보로 돌아오거나(ENG-2), 행동 전 최종이 막힘이 아니거나(ENG-3 · D2), 설정 빈 값이 예외로 새면(CEO-7) 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-route-fixed.test.ts test/integration/approvals-concurrency.test.ts</automated>
    <fails_when>담당 소멸 · 범위 고정 · test_memo · 설정 행 삭제 뒤 제출 · 비후보 거부 세 사례(기록 0건 · version 불변) · 로그 스파이 · 고아 최종(D2 — 조회가 상태를 쓰거나 새 담당이 승인하지 못함) · 옛 version 단독 상태(무관한 기획 PM 문구에 이름·시각 — 기안자는 오류 종류만, CX-R01) describe 중 하나라도 실패하거나, 멈춘 설정 트랜잭션 동안 한 키만 새 값으로 읽히거나 경주 30회 중 한 번이라도 (X, Y)·(Y, X) 쌍이 나오면(Codex HIGH 스냅숏) failed가 1 이상이다</fails_when>
    <automated>pnpm test:unit</automated>
    <fails_when>단위 프로젝트 전체에서 failed가 1 이상이다(import-cycles 테스트 포함 — domain/approvals가 domain/leave를 import하면 여기서 깨진다)</fails_when>
  </verify>
  <acceptance_criteria>
    - nextStep 표 전체(36칸)가 단위 테스트로 고정되어 있다
    - U2·U4 해소 규칙과 계획 가정 3이 각각 단위 테스트 사례 하나 이상으로 있다
    - 담당 소멸 뒤 이미 승인한 사람이 표시 목록에 그대로 있다는 통합 단언이 있다
    - `test_memo` 종류가 프로덕션 코드 어디에도 없다(`grep -rn "test_memo" domain app repositories`가 0건)
    - CEO-1 단위 네 사례(전부 꺼짐 → 대표 · 팀장 기안 skip → 대표 · 대표 기안 → 본인 승인 · 대표 없음 → 거부)와 CEO-5 경영관리팀 기안자 사례가 `resolve-step.test.ts`에 있다
    - 비후보 거부 세 사례가 각각 단계 처리 기록 0건과 `version` 불변을 단언하고, 로그 스파이가 `approval.refused`·`approval.route_blocked`를 단언한다
    - (결정표) Task 3 ②의 두 표가 plan 그대로 있고 `resolve-step.test.ts`가 19칸을 모두 덮는다 — `grep -oE "\b(R[1-6]|W1[0-3]|W[1-9])\b" test/unit/domain/approvals/resolve-step.test.ts | sort -u | wc -l`이 19
    - (ENG-1 · ENG-2) 「전사 단계만 켠 결재선」 · 「1단 승인 뒤 전사 단계」 · 「1단 본인 승인 + 3단 유일 담당 = 기안자」 세 사례가 `resolve-step.test.ts`에 있고, 트레이서의 「`is_fallback` 행 0개」 단언이 녹색이다
    - (ENG-3 · D2) 같은 입력의 `before_action` = 막힘 · `after_approval` = 최종 단위 두 사례(W10 · W13)와 통합 describe `담당 소멸 뒤 고아 최종`이 있고, 그 describe가 조회 전후 인스턴스 행 동일(쓰기 0) · `log.warn` 1회 · `mine` 0건 · 새 담당 승인 뒤 `approved`를 단언한다. `domain/approvals/index.ts`에서 walkRoute 결과의 최종을 `at` 없이 따로 해석하는 분기가 없다(SUMMARY에 walkRoute 호출 지점과 `at` 값을 표로 적는다)
    - (ENG-6 · D1) walkRoute 결과에 `currentHolderIds`가 있고 단위 사례가 그 합집합과 폴백 자리의 폴백 후보 포함 · 막힘의 미포함을 단언한다(X-3) · 04.1-01 단독 상태의 옛 version 통합 사례가 무관한 기획 PM의 오류 문구에 처리자 이름 · `HH:MM`이 없음을, 기안자에게는 `ApprovalConflictError` 종류만 단언한다(기안자 문구 단언 없음 — CX-R01)
    - `route.ts`에 비공개 도우미 넷(`candidatesFor` · `advance` · `fallback` · `describeHolders`)이 있고 내보내는 이름은 `nextStep` · `resolveHolders` · `walkRoute`뿐이다. 리팩터 커밋의 diff에 `test/unit/domain/approvals/*` 변경이 없다
    - (Codex HIGH 스냅숏) `route-config.test.ts`에 「일괄 읽기 1회 · 키 단위 읽기 0회」 단언이 있고, `approvals-concurrency.test.ts`에 결정적(멈춘 트랜잭션) · 경주(30회) 두 사례가 있다 — 전에는 backstop이던 EXP-04 concurrency가 테스트로 닫혔다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재선의 모든 가장자리 규칙(자기 승인 두 방식 · 빈 자리 · 대표 폴백 · 승인 0건 폴백 · 한 사람 한 번(자기 승인 복원 포함) · 전사 범위 · 대표 본인 · 담당 소멸 · 고아 최종 = 막힘 · 범위 고정 · 설정 빈 값 · 비후보 거부 · 두 번째 종류)이 결정표 한 장과 칸별 테스트로 굳었고, 막힘·거부가 운영 로그에 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 결재함 투영 — 요청 단위 노출 메모로 조회 수를 정보 항목 수로(CEO-17) · 새 계급 노출 한계를 테스트로 기록(CEO-10) · 결재선 미리보기 `RoutePreviewDTO` 투영(CX-R3)</name>
  <files>domain/approvals/index.ts, domain/approvals/dto.ts, domain/approvals/kinds.ts, domain/leave/index.ts, test/integration/approvals-inbox-projection.test.ts</files>
  <read_first>
    - `domain/permissions/project.ts`(전문 36행) — `project(viewer, row, spec, deps?: { visible })`: 필드마다 `visible()`을 부른다(N+1의 자리)
    - `domain/permissions/visible.ts`(전문) — `visible(viewer, infoItem, deps?: { findVisibility })`, 행이 없으면 거짓
    - `domain/permissions/roles.ts` 90–110행(`createRole` — 노출 행을 만들지 않는다. **읽기만**, 이 페이즈 금지 파일)
    - Task 1·3 산출물 `domain/approvals/index.ts`(`listMyInbox` · `getApprovalView`) · `domain/approvals/kinds.ts`(`describeDocuments`) · `domain/leave/index.ts`
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` Section 7(CEO-17) · Section 3(CEO-10)
    - `domain/permissions/dto-registry.ts`(전문 20행 — `registerDto({name, fields: [{key, infoItem}]})`, 누수 스캔 `buildDtoCases`가 이 배열에서 케이스를 만든다) · `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 209행(제출 전 결재선 한 줄 — 빈 자리는 그리지 않는다, 자기 승인 건너뜀 한 마디)(CX-R3)
  </read_first>
  <behavior>
    - (통합, CEO-17) 대표가 지금 단계 후보인 진행 중 연차 20건과 대표가 처리한 문서 3건이 있을 때, 세는 `findVisibility`를 주입해 대표의 `listMyInbox`를 한 번 부르면 `findVisibility` 호출이 2회 이하(정보 항목 `approval.value` · `leave.value`마다 한 번)이고, 결과 DTO는 주입 없이 부른 결과와 같다
    - (통합, CEO-17) 같은 viewer로 `listMyInbox`를 두 번 부르면 두 번째도 다시 조회한다(메모는 요청 하나 안에서만 산다 — 노출표를 바꾸면 다음 요청에 바로 반영)
    - (통합, CEO-17) 기안자 본인 연차 20건이 있을 때 세는 `findVisibility`를 주입한 `listMyLeave` 한 번의 호출이 2회 이하이고 결과는 주입 없이 부른 결과와 같다
    - (통합, CX-R3 투영 켜짐) 트레이서 픽스처(기획1팀 기안자 · 팀장 · 대표, 2·3단 빈 자리)에서 기안자가 `previewRoute(viewer, {kind: "leave"})`를 부르면 `drafterName` = 기안자 이름, `steps` = `[{label: "팀장", holderNames: 팀장 이름, skipped: false}, {label: "대표", holderNames: 대표 이름, skipped: false}]`(빈 자리 2·3단은 목록에 없다) · 호출 전후 `approval_instances` · `approval_routes` · `approval_steps` 행 수가 같다(저장 없음)
    - (통합, CX-R3 투영 꺼짐) 같은 픽스처에서 기안자 계급의 `approval.value` 노출을 거짓으로 바꾼 뒤(`upsertVisibility`) 다시 부르면 결과에 `drafterName`이 없고 각 단계의 `Object.keys`가 정확히 `["label", "skipped"]`이며 `JSON.stringify(결과)`에 팀장 · 대표 · 기안자 이름이 한 글자도 없다. 기안자가 팀장 · 자기 승인 `skip`인 결재선에서는 1단이 `{label: "팀장", skipped: true}`로 남는다(건너뜀 한 마디의 재료)
    - (통합, CX2-W1 자리 이름 대체) `approval.value` · `role.value` 노출이 둘 다 없는 계급(`createRole` 뒤 두 노출 행을 거짓으로 `upsertVisibility`)의 사용자가 같은 트레이서 결재선 조직에서 `previewRoute`를 부르면 `steps`의 `label`이 차례로 `1단` · `2단`(팀장 자리 · 대표 자리)이고 각 단계의 `Object.keys`가 정확히 `["label"]`이며 `drafterName`이 없고 `JSON.stringify(결과)`에 사람 이름 · 계급 이름(`팀장` · `대표`)이 없다
    - (통합, CEO-10 한계 기록) `createRole`로 만든 새 계급의 사람을 경영관리팀에 발령하고 1·2단이 빈 자리인 연차를 제출하면 그 사람은 3단 후보로 `mine`에 한 줄이 잡히지만 그 줄의 DTO에 `approval.value` 필드가 하나도 없다(노출 행 없음 → `visible` 거짓). 테스트 이름에 「알려진 한계 — 새 계급은 정보 노출표에서 결재·연차 정보를 켜야 함(TODOS)」을 적는다
  </behavior>
  <action>
① `domain/approvals/index.ts`: `listMyInbox(viewer, deps?)` · `getApprovalView(viewer, input, deps?)`가 요청마다 **새로** 노출 메모를 하나 만든다 — 정보 항목 → `visible(viewer, item, { findVisibility })`의 Promise를 담는 지역 Map이고, 모든 `project(viewer, row, spec, { visible: 메모 })` 호출과 종류의 `describeDocuments(viewer, ids, { visible: 메모 })`에 같은 메모를 넘긴다. `deps.findVisibility`(선택)는 테스트가 호출 수를 세려고 주입한다. **모듈 전역 캐시를 두지 않는다**(요청 사이에 노출 값이 새면 권한 변경이 늦게 반영된다). Phase 4 04-32의 `projectMany`가 실행 시점 main에 있으면 그것을 써도 같은 성질(정보 항목마다 한 번)이면 된다 — SUMMARY에 어느 쪽인지 적는다.

② `domain/approvals/kinds.ts`의 `describeDocuments` 시그니처에 선택 인자 `deps?: { visible }`를 더하고, `domain/leave/index.ts`의 연차 구현이 받은 `visible`을 자기 `project()`에 넘긴다(없으면 기본 `visible`). `domain/leave/index.ts`의 `listMyLeave(viewer, input, deps?)`도 ①과 같은 요청 단위 메모를 만들어 행마다의 `project()`에 넘긴다(CEO-17 remedy가 이름을 든 세 곳 중 둘째 — 셋째 결재 시트 상세는 04.1-05 T1이 같은 메모를 잇는다). 인박스 행 수만큼 연차 행을 따로 읽지 않는다 — `describeDocuments`는 id 목록을 한 번에 읽는다.

③ **결재선 미리보기(CX-R3)** `domain/approvals/dto.ts`에 `RoutePreviewDTO`(`{ drafterName?: string; steps: Partial<RoutePreviewStepDTO>[] }`)와 두 명세를 `registerDto`로 등록한다 — `ROUTE_PREVIEW_DTO_SPEC`(이름 `routePreview`: `drafterName` → `approval.value`) · `ROUTE_PREVIEW_STEP_DTO_SPEC`(이름 `routePreviewStep`: `label` → `role.value`(단계 자리 이름 = 계급 이름 · 부서 이름 · `전사` · `대표` — 사람이 아닌 구조 정보라 기본 참인 계급 정보 항목) · `holderNames` → `approval.value`(지금 해석한 담당 표시 — 1명 = 이름, 2명 = `이수아 · 박지훈`, 3명 이상 = `이수아 외 2명`, Task 3의 `describeHolders`와 같은 규칙) · `skipped` → `role.value`(기안자 자기 승인 `skip`으로 통과하는 자리)). `domain/approvals/index.ts`의 `prepareSubmission`에서 단계 계획 + walkRoute(`at: "before_action"`) 부분을 비공개 도우미 하나로 떼어 `prepareSubmission`과 새 `previewRoute(viewer, {kind}, deps?)`가 **같은 도우미**를 부른다(기안자 = viewer, today = `seoulToday(deps.now)` — 미리보기와 실제 고정이 어긋나지 않게, 제출 경로 코드를 둘로 나누지 않는다). `previewRoute`는 트랜잭션을 열지 않고 아무것도 쓰지 않으며, 도우미가 막힘을 내면 제출과 같은 거부 오류를 그대로 던진다. 결과는 빈 자리를 뺀 표시 순서의 단계(대표 폴백이면 끝에 `대표` 자리, 건너뜀 자리는 `holderNames` 없이 `skipped: true`)를 행마다 `project(viewer, 단계행, ROUTE_PREVIEW_STEP_DTO_SPEC, { visible: 메모 })`로, 머리를 `ROUTE_PREVIEW_DTO_SPEC`으로 투영한다 — 메모는 ①과 같은 요청 단위 지역 Map(결재함 · 결재선 보기와 같은 투영). 이름 문자열을 투영 밖에서 붙이지 않는다. 투영에서 `label`이 빠지면(viewer에게 `role.value`가 없음) 그 자리에 `N단`(N = 반환 목록 안 1부터의 순번, 대표 폴백 자리 포함)을 넣는다 — 순번은 사람·계급 정보가 아니라 자리가 사라지지 않게 하는 값이다. `skipped`는 지금처럼 `role.value` 투영을 따른다(CX2-W1). `leave` write 판정은 이 함수가 아니라 04.1-06의 `previewLeaveAction` 첫 문장 `assertLeaveWrite`가 한다(결재 모듈은 연차 권한을 모른다). 04.1-06은 이 함수를 새로 만들지 않고 `RoutePreviewDTO`를 그리기만 한다.

④ `test/integration/approvals-inbox-projection.test.ts`가 behavior 일곱 줄(CX-R3 두 줄 · CX2-W1 한 줄 포함)을 단언한다. 새 계급 사례는 `domain/permissions/roles.ts`를 **고치지 않고** 그 함수를 부르기만 한다 — 이 사례는 결함을 고치는 테스트가 아니라 지금 동작의 기록이다(기본값 A). 04.1-07 Task 2가 이 한계를 TODOS.md와 SUMMARY에 옮긴다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-inbox-projection.test.ts test/integration/approvals-route-fixed.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>`listMyInbox` 또는 `listMyLeave`의 `findVisibility` 호출이 3회 이상이거나, 메모 결과 DTO가 주입 없는 결과와 다르거나, 새 계급 사례의 DTO에 결재 필드가 실리거나(한계 기록이 현실과 어긋남), `approval.value`를 끈 계급의 `previewRoute` 결과에 이름 필드나 이름 글자가 하나라도 실리거나 켠 계급의 결과에 `holderNames`가 없거나 미리보기가 결재 행을 하나라도 쓰거나(CX-R3), 기존 트레이서·누수 스캔이 회귀하면(`routePreview` · `routePreviewStep` 케이스가 생기지 않으면 포함) failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - 20행 결재함과 20행 `listMyLeave`의 노출 조회 수 단언(각 ≤ 2)이 있다
    - `domain/approvals/index.ts`에 모듈 최상위 `new Map(` 선언이 없다(`grep -nE "^(export )?const [A-Za-z_]+ = new Map\(" domain/approvals/index.ts`가 0줄 — 메모는 함수 안 지역 값)
    - `git diff --stat "$PLAN_BASE"..HEAD -- domain/permissions/roles.ts domain/permissions/project.ts domain/permissions/visible.ts`가 비어 있다
    - (CX-R3) `domain/approvals/dto.ts`에 `name: "routePreview"`와 `name: "routePreviewStep"` 등록이 각각 한 번이다(`grep -cE 'name: "routePreview(Step)?"' domain/approvals/dto.ts`가 2) · `holderNames`의 정보 항목이 `approval.value`다 · `domain/approvals/index.ts`에서 `previewRoute`와 `prepareSubmission`이 같은 단계 계획 도우미를 부른다(실행자가 SUMMARY에 도우미 이름을 적는다) · `previewRoute` 본문에 `withTransaction`이 없다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재함 한 번이 노출표를 정보 항목 수만큼만 읽고, 새 계급의 노출 한계가 테스트로 기록되어 04.1-07이 TODOS로 옮길 수 있으며, 결재선 미리보기는 `approval.value`를 통과한 viewer에게만 이름을 싣는 `RoutePreviewDTO`로 나간다(CX-R3)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 호출자(서버 액션) → `approveDocument` | 결재 권한은 메뉴 권한이 아니라 결재선이다. 호출자가 보낸 인스턴스 id·version은 신뢰하지 않는다 |
| 설정(관리자) → 제출 시점 결재선 | 관리자가 바꾼 설정이 진행 중 문서에 새어 들어가면 결재선이 사후에 바뀐다 |
| domain 출구 → 화면 | 결재함·결재선·연차 DTO가 정보 노출표를 지나야 한다 |
| 마이그레이션 저장소 → 운영 DB | 병렬 브랜치가 만든 journal 순서가 어긋나면 운영 적용 때 조용히 건너뛴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Elevation of Privilege | `approveDocument` | high | mitigate | 트랜잭션 안에서 매번 walkRoute로 후보를 다시 계산하고 viewer가 후보가 아니면 거부한다. 메뉴 권한 `can()`을 결재 권한으로 쓰지 않는다. Task 3 넷째 describe가 기안자 · 무관한 PM · 차례 전 대표의 맞는-version 승인 거부와 기록 0건 · version 불변을 단언한다(CEO-8) |
| T-04.1-41 | Elevation of Privilege | walkRoute 끝 판정 · 관리자 단계 끄기 | high | mitigate | 차수 승인 0건이면 최종이 아니라 대표 폴백(대표 기안 = 본인 승인, 대표 없음 = 제출 거부). `skip` 통과는 승인으로 세지 않는다. 단위 네 사례(Task 3) + 전부 끈 설정의 통합 사례(04.1-04 Task 1)(CEO-1). 전사 범위는 대상 id null을 0명으로 보지 않아 전사 단계가 빈 자리로 새어 한 단계 결재 없이 최종 승인되지 않고(ENG-1 — 결정표 R5 · R6 단위 사례 + 트레이서 「`is_fallback` 행 0개」), 자기 승인 복원도 이 차수 승인자를 빼 한 사람이 한 차수에 두 번 승인하지 않는다(ENG-2 — W6) |
| T-04.1-41b | Denial of Service | 진행 중 문서의 고아 최종(담당 소멸 뒤 walkRoute 최종 · DB `in_review`) | medium | mitigate | 행동 전 계산의 최종은 막힘과 같다(ENG-3 · D2): 지금 단계 `담당 없음` · 결재함 0건 · `log.warn("approval.route_blocked")`로 운영자에게 보이고, 기안자 회수(04.1-02)와 관리자 담당 복구로 풀린다. 읽기 경로는 쓰지 않는다 — 판정은 walkRoute `at` 한 곳. 결정표 W10 · W13 단위 + 통합 describe `담당 소멸 뒤 고아 최종` |
| T-04.1-42 | Information Disclosure | 운영 로그 `approval.refused`·`approval.route_blocked` | low | mitigate | 로그 필드를 id · 사유 코드 · 숫자로 한정 — 이름 · 반려 사유 글자 · 연차 내용 없음. 로그 스파이 단언이 필드 목록을 고정한다(CEO-18) |
| T-04.1-43 | Denial of Service | 제출·승인 트랜잭션의 풀 점유 | medium | mitigate | 설정 · 번호 서식 · 스냅숏 · 권한을 트랜잭션 전에 읽고 안에서는 `tx` 호출만, 번호 할당이 마지막 쓰기. 풀 5에서 동시 제출 6건 통합 사례(CEO-2) |
| T-04.1-44 | Denial of Service | 결재함 투영 N+1 | low | mitigate | 요청 단위 노출 메모 — 20행에 조회 2회 이하 통합 단언, `approval_steps(acted_by)` 인덱스(CEO-17) |
| T-04.1-45 | Denial of Service (가용성) | 새 계급 결재자의 빈 DTO | low | accept | 기본값 A — 알려진 한계. 지금 동작을 Task 4 통합 테스트가 기록하고 04.1-07이 TODOS.md·SUMMARY 공개 전 점검표에 「새 계급은 정보 노출표에서 결재·연차 정보 켜기」를 남긴다. `roles.ts`·D-35 모델은 바꾸지 않는다(CEO-10) |
| T-04.1-02 | Tampering | 진행 중 문서의 결재선 | medium | mitigate | 단계 행(계급 · 범위 종류 · 대상 id · 이름)을 제출 때 저장하고 이후 설정을 읽지 않는다. 설정 변경 뒤 단계 행 불변을 통합 테스트가 단언한다 |
| T-04.1-51 | Tampering | 제출 중 관리자 설정 변경 → 두 시점이 섞인 결재선 | high | mitigate | 결재선 17키를 `findSimpleValues` SELECT 한 문장으로 읽는다(Postgres 문장 스냅숏). Task 3 단위 가드(일괄 1회 · 키별 0회) + 멈춘 설정 트랜잭션 결정적 사례 + 30회 경주 사례가 섞인 쌍 0을 단언(Codex HIGH) |
| T-04.1-52 | Repudiation | 전이 커밋과 행동 로그의 분리 | high | mitigate | `recordActionInTx`로 로그 행을 전이와 같은 tx에 쓴다 — 켜짐 여부는 트랜잭션 전 `loadActionLogGate`(fail-open). 로그 실패 주입 시 신청·인스턴스·단계·카운터·version이 전부 그대로라는 통합 단언(Codex HIGH 원자성) |
| T-04.1-03 | Information Disclosure | 결재·연차 DTO와 문서 조회 | high | mitigate | 모든 DTO를 `registerDto` + `project()`로 내보내고 필드를 정보 항목에 매핑(누수 스캔 DTO 축). 문서는 기안자·후보·처리자만 보고 나머지는 null(→ 404). `scopeFor()`로 결재함을 거르지 않는다. 결재선 미리보기도 `RoutePreviewDTO`로 투영해 `approval.value`가 꺼진 계급은 `leave` write가 있어도 담당 이름을 받지 않는다 — Task 4 켜짐/꺼짐 통합 사례(CX-R3) |
| T-04.1-04 | Repudiation | 제출·승인 | medium | mitigate | 같은 tx 안 `recordActionInTx`(`document_submit`·`document_approve`, entity `approval_instance`, detail에 단계 번호·차수) — 커밋된 전이에는 로그가 반드시 있다(T-04.1-52). 단계 행의 `acted_by`·`acted_at`이 두 번째 기록이다 |
| T-04.1-53 | Tampering | 수기 SQL · 잘못된 리포지토리 호출로 들어간 상태 문자열 | low | mitigate | 상태 · 범위 · 처리 · 자기 승인 · 연차 종류 · 오전/오후 열에 DB CHECK, `days_quarters` ≥ 0(Codex MEDIUM, `corp-cards.ts` 선례) |
| T-04.1-05 | Tampering | `_journal.json` 순서 · 스냅숏 짝 | medium | mitigate | journal 가드 단위 테스트(idx 연속 · tag 접두 · when 증가 · 항목마다 `.sql`과 스냅숏 존재 · journal에 없는 고아 스냅숏 없음 — 고아 스냅숏이 남으면 drizzle-kit이 이름순 마지막 스냅숏을 이전 스키마로 잘못 잡는다)가 CI 단위 단계에서 막고, 병합 직전 재생성은 04.1-07 Task 1이 한다 |
| T-04.1-06 | Tampering | 동시 승인 | high | mitigate | `version` 조건부 UPDATE 한 번 — 0행이면 충돌. 판정 순서 version → 종결 → 후보 → UPDATE 먼저 → 단계 행(CEO-6). 두 순서 · 폴백 경합 통합 증명은 04.1-02 Task 3. version 불일치 · 종결 거부 문구의 상세(처리자 이름 · 시각 · 상태)는 관련자에게만 — 판정 재료 `currentHolderIds`는 이 플랜, 판정과 문구는 04.1-02 Task 3(T-04.1-12b · ENG-6 · D1) |
| T-04.1-07 | Denial of Service | 조직 스냅숏 조회 | low | accept | 사용자 10~30명 사내 시스템 — 처리·표시마다 쿼리 한 번으로 스냅숏을 읽는다. 캐시를 만들지 않는다 |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(04.1-RESEARCH.md § Package Legitimacy Audit: 대상 없음). `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` 통과
- `pnpm test:unit` 전체 통과(journal 가드 · registry-coverage · import-cycles · `seoulToday` · 결재선 표 포함)
- `bash scripts/dev-db.sh && pnpm db:reset:test && pnpm db:migrate && pnpm test:integration` 전체 통과(기존 스위트 회귀 없음 — `erp_test`를 비운 뒤 global-setup이 전부 다시 적용)
- `pnpm db:generate` 재실행 결과 "No schema changes"
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음
- `git diff --stat "$PLAN_BASE"..HEAD -- domain/rules/register.ts ui/next-turn domain/permissions/roles.ts domain/action-log` 비어 있음
</verification>

<success_criteria>
- 연차 한 건이 제출부터 최종 승인까지 엔진 하나로 간다(트레이서)
- 결재선이 제출 때 고정되고 사람은 매번 다시 해석된다
- 빈 자리 · 대표 폴백 · 승인 0건 폴백 · 한 사람 한 번 · 자기 승인 · 담당 소멸 · 설정 빈 값 규칙이 테스트로 고정되었다
- 테스트 전용 두 번째 종류가 같은 엔진으로 끝까지 가 하드코딩이 없음을 증명한다
- 트랜잭션 안에서는 `tx` 호출만 돌고(풀 5 · 동시 제출 6건), 「오늘」은 서울 날짜이며, 결재함 투영은 정보 항목 수만큼만 노출표를 읽는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `approval_instances` · `approval_routes` · `approval_steps` · `leave_requests`(상태 기계 문자열 열 DB CHECK — Codex MEDIUM) |
| 마이그레이션 | drizzle-kit이 이름 붙인 SQL 1개 + 그 `meta/NNNN_snapshot.json`(브랜치 임시 번호 — 병합 때 04.1-07 Task 1이 SUMMARY `migration_manifest`에 적힌 파일만 지우고 main 마지막 + 1로 재생성 — 번호 범위 지정 없음, 조정자 11:53) |
| 함수 | `nextStep` · `resolveHolders` · `walkRoute`(순수, 비공개 도우미 `candidatesFor` · `advance` · `fallback` · `describeHolders`) · `prepareSubmission` · `previewRoute`(CX-R3) · `submitDocument` · `approveDocument` · `listMyInbox` · `getApprovalView` · `registerDocumentKind` · `getDocumentKind` · `countLeaveQuarters` · `formatLeaveDays` · `submitLeave` · `getLeave` · `listMyLeave` · `canSeeLeaveDocument`(`domain/leave/access.ts`) · `seoulToday`(`lib/dates.ts`) · `listOrgSnapshot` · `insertVisibilityIfAbsent` · `seedApprovalsLeave` · `findSimpleValues`(`repositories/settings.ts`) · `getSimpleSettingValues`(`domain/settings/registry.ts`) · `loadActionLogGate` · `recordActionInTx`(`domain/approvals/tx-log.ts`) |
| 오류 클래스 | `ApprovalConflictError` · `NotCurrentHolderError`(둘 다 `UserFacingError` 하위) |
| 인덱스 | `approval_steps(acted_by)`(CEO-17) |
| 운영 로그 | `approval.refused`(info — id · 사유 코드 · version) · `approval.route_blocked`(warn — instanceId · kind · round · stepIndex)(CEO-18) |
| DTO | 결재함 항목 · 결재선 보기(`approval.value`) · `RoutePreviewDTO`(`routePreview` · `routePreviewStep` — 이름은 `approval.value`, 자리 이름은 `role.value`, CX-R3) · 연차 신청 · 연차 잔고(`leave.value`) |
| 설정 키 | `approval_route.leave.*` 17개 · `document_number.leave.*` 5개 · `SettingDef` 선택 필드 3개(타입) |
| 정보 항목 | `approval.value` · `leave.value` |
| 테스트 | `test/integration/approvals-route-fixed.test.ts` · `test/integration/approvals-concurrency.test.ts`(04.1-02가 이어 씀) · `test/integration/approvals-inbox-projection.test.ts` · `test/unit/domain/approvals/{next-step,resolve-step,document-kind-registry}.test.ts` · `test/unit/domain/leave/{days,route-config}.test.ts` · `test/unit/lib/dates.test.ts` · `test/unit/db/migration-journal.test.ts` |
| 변경된 기존 심볼 | `SettingDef`(선택 필드 3) · `DOCUMENT_NUMBER_FORMAT_DEFS`(leave 줄) · `loadDocumentNumberFormat`(export) · `allocateDocumentNumber`(선택 `format`) · `seedMasterData`(한 줄) · `INFO_ITEMS`(끝에 2) · `SETTING_DEFS`(끝에 22) · 종류 정의 `describeDocuments`(선택 `deps.visible`) · `appendActionLog`(선택 `tx` — Phase 4 04-32와 같은 모양) |
| 기록 | SUMMARY frontmatter `migration_manifest`(tag · SQL · 스냅숏 경로 — 04.1-07이 지울 파일의 유일한 출처) |

## Review Dispositions Ledger

### Round 1 — 1b86370

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 결재선 설정 17키가 키별 SELECT라 제출 중 관리자 변경이 섞일 수 있음(L39@1b86370) | HIGH | Plan 01, Task 1 ②⑥: `findSimpleValues` SELECT 한 문장 + `getSimpleSettingValues` · Task 3 ⑥: 단위 가드(일괄 1회 · 키별 0회) + 멈춘 트랜잭션 결정적 사례 + `applySettingsImport` 30회 경주 사례. backstop truth를 test truth로 교체 |
| 업무 전이와 행동 로그가 원자적이지 않음 — 커밋 뒤 전역 DB로 기록(L40@1b86370) | HIGH | Plan 01, Task 1 ②④⑤⑨: `appendActionLog` 선택 `tx`(Phase 4 04-32와 같은 모양) + `domain/approvals/tx-log.ts`(`loadActionLogGate` 선읽기 · `recordActionInTx`)로 로그 행을 전이와 같은 tx에. 선례 확인 — main의 Phase 3 코드는 커밋 뒤 기록이지만 Phase 4 ARCHITECTURE §4-8 (3)이 「잠근 트랜잭션 안의 `recordAction`에 `tx`를 넘긴다」로 바꿨고 04.1이 그 규약을 따른다. 로그 실패 주입 통합 사례(행·카운터·version 불변) |
| 상태 기계 문자열 열에 DB CHECK 없음(L41@1b86370) | MEDIUM | Plan 01, Task 1 ①: status · scope_kind · action · self_approval · kind · half CHECK와 `days_quarters` ≥ 0(`corp-cards.ts` `check()` 선례), 수락 기준 `check(` 6개 이상 |
| journal 가드가 스냅숏을 보지 않음 · 번호 범위 단언 금지(11:48 교차 페이즈 규칙 · 11:53 번호 범위 지정 폐지, 조정자 전달) | — | Plan 01, Task 2 ③④: 가드에 (e) 항목별 스냅숏 존재 · (f) 고아 스냅숏 없음 추가, 하드코딩 번호 리터럴 없음 수락 기준 · 실행 노트의 번호 범위 문구를 11:53 규칙으로 교체, 「마지막 스냅숏 = 스키마」는 `pnpm db:generate` "No schema changes", SUMMARY `migration_manifest` |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 플랜 크기가 지나치게 큼 — 순수 상태 기계·스키마와 서비스·설정·시드를 나눠야(L42@1b86370) | Rejected — 오케스트레이터 제약(직렬 체인 01→03→02→04→05→06→07 고정 · 플랜 수 고정 · 플랜당 최대 4 task). 이미 네 task로 나뉘어 각자 verify가 있고, 트레이서(Task 1)가 가장 앞 컨텍스트에서 전 층을 관통한다. 추정치를 165k로 올려 기록했다. 2026-09-24 eng-review ENG-22(「거절 이유 불충분」)로 Task 1 안에 중간 커밋 지점 셋(스키마 + 마이그레이션 → 순수 함수 → 서비스)을 두었다 — 플랜 수는 그대로 |

### Round 2 — /plan-eng-review 2026-09-24(docs/designs/plant8-erp-phase04.1-eng-review-260924.md)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-1 전사 단계가 늘 빈 자리(`company` 대상 id null → 0명) | BLOCKER | Task 1 ③ null → 0명을 `team`·`org_unit`에만 · ② 폴백 행도 `company`로 저장 · Task 3 결정표 R5 · R6 + 「전사 단계만 켠 결재선」 · 「1단 승인 뒤 전사 단계」 · 트레이서 `is_fallback` 행 0개 |
| ENG-2 자기 승인 복원이 이번 차수 승인자를 안 뺌 | BLOCKER | Task 1 ③ · Task 3 결정표 W6 + 「1단 본인 승인 + 3단 유일 담당 = 기안자」 · truth 2줄 |
| ENG-3 조회 시점 고아 최종(D2) | BLOCKER | walkRoute 입력 `at`(행동 전 최종 = 막힘, 한 곳) · Task 1 ④ 읽기 경로 쓰기 없음 · Task 3 W10 · W13 단위 + 통합 describe `담당 소멸 뒤 고아 최종` · T-04.1-41b |
| ENG-6 충돌 상세 문구가 권한 판정보다 먼저(D1, 01 쪽) | BLOCKER | Task 1 ② 모든 차수 `acted_by` 읽기 · ③ walkRoute `currentHolderIds` · ④ (1)(2) 갈래를 D1로 기술(판정 `isApprovalParty`와 문구는 04.1-02 Task 3) · 단독 상태 옛 version 통합 사례 |
| ENG-22 플랜 01 크기 | MEDIUM | Task 1 중간 커밋 단위 A · B · C와 되돌림 규칙 · 수락 기준 |
| 아키텍처 권고 — candidatesFor 결정표 한 장 | 권고 | Task 3 ② 표 두 장(R1~R6 · W1~W13, 호출 시점 열 포함) · 칸 id로 시작하는 단위 사례 19칸 수락 기준 |
| 옛 체커 — journal 가드 `provides` · T-04.1-05에 스냅숏 존재 · 고아 스냅숏 조건 | 정보 | artifacts `migration-journal.test.ts` provides · T-04.1-05 문구 |

### Round 3 — Codex 1회차(docs/designs/phase04.1-revision-260924/codex-round1-A.md · -B.md · -C.md · triage)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| R01 단독 상태 옛 version 사례가 기안자 문구의 이름·시각 부재까지 단언해 04.1-02 뒤 깨짐 | BLOCKER | Task 3 behavior · fails_when · 수락 기준 — 이름·시각 부재는 무관한 기획 PM에만, 기안자는 `ApprovalConflictError` 종류만(CX-R01) |
| B1 종결 상태 확인이 `rejected`에서 다시 신청까지 막음(01 쪽 규칙 문장) | BLOCKER | Task 1 ④ (2) 사건별 종결 판정 — `approved`·`withdrawn` 모든 사건, `rejected`는 기안자 `resubmit` 빼고 전부(04.1-02 ② (2)와 같은 문장). 테스트는 04.1-02(CX-B1) |
| C-R3 결재선 미리보기가 `approval.value` 투영 없이 이름을 실음(01 쪽) | BLOCKER | Task 4 ③ `previewRoute` → `RoutePreviewDTO`(`routePreview` · `routePreviewStep` 등록, 이름은 `approval.value`, 자리 이름은 `role.value`) · 제출과 같은 계획 도우미 · 켜짐/꺼짐 통합 두 사례 · truth · T-04.1-03(CX-R3) |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-01-SUMMARY.md` when done
</output>
