---
phase: 04.1-approvals-leave
plan: 01
subsystem: approvals
tags: [approvals, leave, drizzle, postgres, state-machine, settings-registry, tracer]
plan_base: 71654521df64cc1fd07ecde2696beb4687709254
plan_head_before: 71654521df64cc1fd07ecde2696beb4687709254

requires:
  - phase: 04-project-quote
    provides: "document_counters 번호 할당 · withTransaction(SET LOCAL lock_timeout) · appendActionLog/recordAction의 선택 tx · projectMany · lib/kst-date"
provides:
  - "결재 3표(approval_instances · approval_routes · approval_steps) + leave_requests + 마이그레이션 1개"
  - "순수 결재선 엔진 nextStep · resolveHolders · walkRoute(결정표 R1~R6 · W1~W13)"
  - "결재 서비스 prepareSubmission · submitDocument · approveDocument · listMyInbox · getApprovalView · previewRoute · canSeeApprovalDocument"
  - "문서 종류 등록부 registerDocumentKind · getDocumentKind(routeSettings? · canResubmit?)"
  - "연차 submitLeave · getLeave · listMyLeave · canSeeLeaveDocument · countLeaveQuarters · formatLeaveDays"
  - "설정 키 22개(결재선 17 · 연차 번호 5) + SettingDef 선택 필드 3개 + getSimpleSettingValues(SELECT 한 문장)"
  - "tx-log.ts loadActionLogGate · recordActionInTx(전이와 같은 tx의 행동 로그)"
  - "lib/dates.ts seoulToday"
affects: [04.1-02, 04.1-03, 04.1-04, 04.1-05, 04.1-06, 04.1-07, phase-05-expense]

actuals:
  tokens: 69262
  tasks: 4
  commits: 7

tech-stack:
  added: []
  patterns:
    - "트랜잭션 전 읽기(설정 · 스냅숏 · 로그 켜짐 · 번호 서식) → 트랜잭션 안에서는 tx 리포지토리 호출만, 번호 할당이 마지막 쓰기"
    - "결재 판정 순서: version → 종결 → 후보 → 상태 UPDATE 먼저 → 단계 기록 → 같은 tx 로그"
    - "walkRoute 입력 at(before_action / after_approval) 한 곳에서 고아 최종 = 막힘 판정"
    - "요청 단위 노출 메모 createVisibleMemo(모듈 전역 캐시 없음)"

key-files:
  created:
    - db/schema/approvals.ts
    - db/schema/leave.ts
    - db/migrations/0017_supreme_micromacro.sql
    - db/migrations/meta/0017_snapshot.json
    - repositories/approvals.ts
    - repositories/leave-requests.ts
    - repositories/org-snapshot.ts
    - domain/approvals/route.ts
    - domain/approvals/kinds.ts
    - domain/approvals/dto.ts
    - domain/approvals/index.ts
    - domain/approvals/tx-log.ts
    - domain/leave/days.ts
    - domain/leave/dto.ts
    - domain/leave/index.ts
    - domain/leave/access.ts
    - domain/seed/approvals-leave.ts
    - lib/dates.ts
    - test/integration/approvals-route-fixed.test.ts
    - test/integration/approvals-concurrency.test.ts
    - test/integration/approvals-inbox-projection.test.ts
    - test/integration/approvals-fixtures.ts
    - test/unit/domain/approvals/next-step.test.ts
    - test/unit/domain/approvals/resolve-step.test.ts
    - test/unit/domain/approvals/document-kind-registry.test.ts
    - test/unit/domain/leave/days.test.ts
    - test/unit/domain/leave/route-config.test.ts
    - test/unit/lib/dates.test.ts
    - test/unit/db/migration-journal.test.ts
  modified:
    - db/schema/index.ts
    - db/migrations/meta/_journal.json
    - repositories/settings.ts
    - domain/settings/registry.ts
    - domain/settings/keys.ts
    - domain/document-numbering/index.ts
    - domain/permissions/info-items.ts
    - domain/seed/index.ts
    - test/integration/leak-scan.test.ts

migration_manifest:
  - journal_tag: "0017_supreme_micromacro"
    sql: "db/migrations/0017_supreme_micromacro.sql"
    snapshot: "db/migrations/meta/0017_snapshot.json"
    hand_insert: "66~68행 — SET LOCAL lock_timeout = '1s'; · SET LOCAL statement_timeout = '5s'; · --> statement-breakpoint (첫 ALTER TABLE … FOREIGN KEY 앞, 04.1-07 재생성 때 같은 삽입을 다시 한다)"

key-decisions:
  - "lib/dates.ts seoulToday는 Phase 4의 lib/kst-date.ts kstDateOf를 재사용한다(같은 Intl 방식, 새 계산 없음)"
  - "appendActionLog의 선택 tx · insertVisibilityIfAbsent · loadDocumentNumberFormat export · allocateDocumentNumber format은 main에 이미 있어(Phase 4 병합) 고치지 않았다"
  - "새 설정 22키는 keys.ts 끝에 덧붙이고 SETTING_DEFS.push(...)로 등록했다(기존 배열 선언은 그대로 — 병합 충돌 최소)"
  - "반차·반반차의 종료일이 시작일과 다르면 `반차·반반차는 하루뿐 · 날짜 하나만 적기`로 거부(UI-SPEC에 없는 한 줄 — 계획 가정)"
  - "기본 충돌 문구 `다른 처리가 먼저 끝남 · 새로 고침`, 종결 문구 `최종 승인됨/반려됨/회수됨 · 새로 고침`(04.1-02 Task 3이 관련자 판정으로 상세 문구를 조립)"
  - "통합 테스트는 시계를 deps.now = 2026-09-18T03:00Z로 고정하고 발령일을 2026-01-01로 둔다(A-08 — 실행 연도가 바뀌어도 LV26 단언이 깨지지 않음)"

requirements-completed: [EXP-03, EXP-04, LEAV-01, ADMN-04]

coverage:
  - id: D1
    description: "트레이서 — 종일 연차 제출 → 팀장 승인 → 2·3단 건너뜀 → 대표 승인 → approved, 결재선 고정 · 행동 로그 · LV26/LV27 번호"
    requirement: "EXP-03"
    verification:
      - kind: integration
        ref: "test/integration/approvals-route-fixed.test.ts#끝에서 끝까지 — 결재선 고정 · 재해석 · 행동 로그"
        status: pass
    human_judgment: false
  - id: D2
    description: "결재선 결정표(R1~R6 · W1~W13) · nextStep 36칸"
    requirement: "EXP-03"
    verification:
      - kind: unit
        ref: "test/unit/domain/approvals/resolve-step.test.ts · next-step.test.ts"
        status: pass
    human_judgment: false
  - id: D3
    description: "제출 시점 고정 · 표시 시점 재해석 · 담당 소멸 · 발령 범위 고정 · 고아 최종 막힘 · 종결 조회 · 폴백 step_index · 설정 스냅숏"
    requirement: "EXP-04"
    verification:
      - kind: integration
        ref: "test/integration/approvals-route-fixed.test.ts · approvals-concurrency.test.ts"
        status: pass
    human_judgment: false
  - id: D4
    description: "연차 일수(정수 1/4일) · 경계 검증 · 서울 날짜"
    requirement: "LEAV-01"
    verification:
      - kind: unit
        ref: "test/unit/domain/leave/days.test.ts · test/unit/lib/dates.test.ts"
        status: pass
    human_judgment: false
  - id: D5
    description: "결재선 설정 17키(결재 부분) — 일괄 읽기 1회 · 빈 부서 \"\" · org_unit_id uuid 스키마"
    requirement: "ADMN-04"
    verification:
      - kind: unit
        ref: "test/unit/domain/leave/route-config.test.ts · test/unit/settings/registry-coverage.test.ts"
        status: pass
    human_judgment: false
  - id: D6
    description: "결재함 · 내 연차 노출 메모(조회 2회 이하) · 결재선 미리보기 투영 · 새 계급 한계 기록 · 누수 스캔"
    requirement: "EXP-03"
    verification:
      - kind: integration
        ref: "test/integration/approvals-inbox-projection.test.ts · leak-scan.test.ts"
        status: pass
    human_judgment: false

duration: 61min
completed: 2026-09-26
status: complete
---

# Phase 04.1 Plan 01: 결재 엔진·연차 트레이서 Summary

**문서 종류를 모르는 결재 엔진(결정표 walkRoute · version 낙관적 잠금 · 같은 tx 행동 로그)과 그 위에 등록된 연차 — 종일 연차 한 건이 제출부터 최종 승인까지 DB·설정·번호(LV26-0001)·시드를 관통한다**

## Performance

- **Duration:** 61분
- **Started:** 2026-09-26T14:26:28Z
- **Completed:** 2026-09-26T15:27:51Z
- **Tasks:** 4 (커밋 7)
- **Files modified:** 38
- **PLAN_BASE:** `71654521df64cc1fd07ecde2696beb4687709254`

## Accomplishments
- 결재 3표 + 연차 표, 상태 기계 문자열 열 CHECK 7개, `approval_steps(acted_by)` 인덱스 — 사람 id는 처리 기록(`acted_by`)에만
- 순수 `walkRoute`가 결정표 19칸(자기 승인 두 방식 · 한 사람 한 번 · 승인 0건 대표 폴백 · 전사 범위 · 고아 최종 = 막힘)을 한 곳에서 판정하고 `currentHolderIds`(04.1-02 관련자 판정 재료)를 낸다
- `approveDocument`의 고정 순서: version → 종결 → 후보 → 상태 UPDATE 먼저 → 단계·폴백 행 → 같은 tx 로그. 비후보·충돌·종결은 `approval.refused`, 막힘은 `approval.route_blocked` 운영 로그(id·코드만)
- 결재선 설정 17키를 SELECT 한 문장으로 읽는다 — 멈춘 트랜잭션 결정적 사례 · 30회 경주 사례에서 섞인 값 0
- 풀 5에서 동시 제출 6건이 끝나고 번호가 서로 다르다. 로그 쓰기 실패를 주입하면 신청·인스턴스·단계·카운터·version이 함께 롤백된다
- 결재함·내 연차 목록은 요청 단위 노출 메모로 노출표 조회 2회 이하, 결재선 미리보기는 `RoutePreviewDTO` 투영(이름은 `approval.value`)

## Task Commits

1. **Task 1 단위 A — 스키마 + 마이그레이션** - `5ab6dde` (feat) — 확인: `pnpm typecheck` 0 · `dev-db.sh && db:migrate` applied · `db:generate` "No schema changes". diff는 `db/schema/*` · `db/migrations/*`뿐
2. **Task 1 단위 B — 순수 함수** - `33643e8` (feat) — RED(stub, assertion 실패 25건) → GREEN: 단위 4파일 25건 통과 · typecheck 0
3. **Task 1 단위 C — 서비스** - `d2d0a6f` (feat) — 통합 4파일(트레이서 · 동시 제출 · 누수 스캔 · seed-permissions) 1143건 통과 · lint · typecheck 0. ⚡ Tracer verified end-to-end — expanding
4. **Task 2 — 마이그레이션 가드 · squawk** - `63b1c27` (test) — RED(고아 스냅숏 주입 → (f) 실패) → GREEN 7건, 빈 erp_test 적용 · lint:sql 0건
5. **Task 3 — 결정표 가장자리 규칙** - `a3b42dd` (feat) — RED 12건(자기 승인 · A 제외 · CEO-1 · D2 · CEO-7) → GREEN 단위 96 · 통합 18
6. **Task 3 ⑧ — 리팩터(동작 보존)** - `d676415` (refactor) — `route.ts`만 변경, 테스트 파일 무변경, 단위 96 · 통합 18 녹색
7. **Task 4 — 투영 메모 · 미리보기** - `285adfd` (feat) — RED(`findVisibility` 0회 · `previewRoute` 없음) → GREEN 7건 + 트레이서 + 누수 스캔 1179건

## CEO-2 — 트랜잭션 콜백 안 호출 목록

| 함수 | 콜백 안에서 부르는 것(전부 `tx` 인자) |
|---|---|
| `submitLeave`(domain/leave/index.ts) | `insertLeaveRequest` → `submitDocument`(`insertApprovalInstance` · `insertApprovalRoute` · `insertApprovalSteps` · `recordActionInTx`) → `allocateDocumentNumber` → `setLeaveNumber` |
| `approveDocument`(domain/approvals/index.ts) | `findApprovalGraphById` → (순수 `walkRoute` · `nextStep`) → `updateInstanceStatus` → `insertFallbackStep` 또는 `recordStepAction` → `recordActionInTx` |

트랜잭션 전: `prepareSubmission`(loadRouteConfig 1회 · `listOrgSnapshot` · `listRouteLabelNames` · `loadActionLogGate`) · `loadDocumentNumberFormat` · 승인의 스냅숏 · 게이트.

## walkRoute 호출 지점과 `at`

| 호출 지점 | at |
|---|---|
| `planRoute`(prepareSubmission · previewRoute 공용) | before_action |
| `walkGraph`(approveDocument 후보 판정 · getApprovalView · canSeeApprovalDocument) | before_action |
| `approveDocument` 승인 반영 뒤 결과 | after_approval |
| `listMyInbox` | before_action |

호출자는 「최종」을 따로 해석하지 않는다 — 행동 전 최종은 walkRoute가 `blocked(orphan_final)`로 돌려준다.

## 인용(수락 기준)

- A-01 판정 줄(`getApprovalView` · `canSeeApprovalDocument` → `canSeeLeaveDocument`가 함께 쓰는 `readApprovalState`): `if (IN_PROGRESS.includes(graph.instance.status)) {`
- A-02 폴백 step_index(`repositories/approvals.ts` `insertFallbackStep`): `.select({ maxIndex: max(approvalSteps.stepIndex) })` … `const stepIndex = (agg?.maxIndex ?? 0) + 1;`
- 미리보기와 제출이 같은 도우미: `planRoute`
- 노출 메모: Phase 4의 `projectMany`는 호출 하나 안에서만 항목을 모으므로, 요청 전체에 걸친 메모는 새 `createVisibleMemo`(`project(..., { visible })`로 주입)를 썼다
- 마이그레이션 손 삽입(삽입 전 생성본과의 차이 — 추가 줄만):
  ```
  65a66,68
  > SET LOCAL lock_timeout = '1s';
  > SET LOCAL statement_timeout = '5s';
  > --> statement-breakpoint
  ```

## Decisions Made

frontmatter `key-decisions` 참고. 요점만: main에 이미 들어온 Phase 4 산출물(`appendActionLog` tx · `insertVisibilityIfAbsent` · 번호 서식 export · `projectMany` · `kst-date`)은 다시 만들지 않고 썼다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - 막힘] 플랜이 더하라던 기존 심볼이 main에 이미 있음**
- **Found during:** Task 1
- **Issue:** `repositories/action-log.ts`의 `tx?` · `repositories/permissions.ts`의 `insertVisibilityIfAbsent` · `loadDocumentNumberFormat` export · `allocateDocumentNumber`의 `format`이 Phase 4 병합으로 이미 있다
- **Fix:** 그 파일은 고치지 않고 그대로 썼다(`repositories/action-log.ts` · `repositories/permissions.ts` diff 0)
- **Committed in:** d2d0a6f

**2. [Rule 3 - 막힘] `SETTING_DEFS` 배열 뒤에 선언한 키를 배열 안에 넣을 수 없음**
- **Issue:** 「keys.ts 끝에 덧붙이기만」과 「SETTING_DEFS에 올린다」를 같이 지키면 선언 전 참조가 된다
- **Fix:** 파일 끝에 22키를 선언하고 `SETTING_DEFS.push(...)`로 등록(기존 줄 무변경)
- **Committed in:** d2d0a6f

**3. [Rule 3] `repositories/settings.ts` import 한 줄 변경**
- **Issue:** 끝에 덧붙인 `findSimpleValues`가 `inArray`를 쓴다
- **Fix:** 첫 줄 drizzle import에 `inArray` 추가 — 그 밖의 diff는 파일 끝 덧붙임뿐
- **Committed in:** d2d0a6f

**4. [Rule 2] 통합 테스트 공용 픽스처 파일 추가**
- **Issue:** 세 통합 파일이 같은 사람·발령·행 수 도우미를 쓴다(SQL 직접 삽입 금지 규칙)
- **Fix:** `test/integration/approvals-fixtures.ts`(테스트 수집 대상 아님) — `createAccount` · `insertMembership`만 쓴다
- **Committed in:** d2d0a6f

**5. [Rule 1] CEO-17 조회 수 단언이 공허하게 통과하던 문제**
- **Found during:** Task 4 RED
- **Issue:** 주입한 `findVisibility`가 무시돼도 `≤ 2`가 0회로 통과했다
- **Fix:** `> 0` 단언을 더해 주입이 실제로 쓰일 때만 통과하게 했다(RED 확인 뒤 구현)
- **Committed in:** 285adfd

---

**Total deviations:** 5 (Rule 1 1 · Rule 2 1 · Rule 3 3)
**Impact on plan:** 범위 확장 없음. 모두 main 현황에 맞추거나 테스트를 정직하게 만드는 변경이다.

## Issues Encountered

- 단위 C의 통합 테스트는 모듈이 없어 import 오류로 먼저 실패했다(assertion RED가 아님). 보완으로 돌연변이 검사를 했다: 전사 범위 null → 0명(ENG-1)을 되살리면 트레이서가 깨지고, 종결 상태에서도 walkRoute를 돌리면 A-01 사례가, 폴백 step_index를 「행 수 + 1」로 바꾸면 A-02 사례가 깨지는 것을 확인했다
- 같은 방식으로 `recordActionInTx`가 `tx`를 넘기지 않게 바꿔 봤지만 로그 실패 주입 사례와 동시 제출 6건 사례는 둘 다 통과했다. 실패 주입은 던지면 어차피 롤백되고, 동시 제출은 호출이 트랜잭션 전 읽기로 엇갈려 풀이 고갈되지 않았다. 계획이 정한 테스트 모양 그대로이며 「tx 없는 로그」 결정적 가드는 아니다 — 아래 오케스트레이터 참고
- Task 3 W3 단위 사례를 처음에 잘못 적었다(기안자가 3단 담당이라 W4가 맞았음) — 기안자가 담당이 아닌 픽스처로 고쳐 적었다

## 검증 게이트(최종 커밋 직전, 빈 erp_test 재생성 뒤)

- `pnpm typecheck` 통과(rc 0)
- `pnpm lint` 통과(rc 0, 오류 0)
- `pnpm lint:sql` 통과(0 issues in 18 files)
- `pnpm test:unit` 통과(104파일 · 1405건)
- `pnpm test:integration` 통과(59파일 · 1708건, `db:reset:test` 뒤)
- `pnpm db:generate` 재실행 rc 0 · "No schema changes"
- `git diff --stat PLAN_BASE..HEAD -- package.json pnpm-lock.yaml domain/rules/register.ts ui/next-turn domain/permissions/roles.ts domain/permissions/project.ts domain/permissions/visible.ts domain/action-log` 비어 있음
- E2E: 이 플랜은 화면이 없어 요구하지 않는다(돌리지 않음)

## 건너뛴 절차(오케스트레이터 지시)

- execute-plan.md의 STATE.md · ROADMAP.md · REQUIREMENTS.md 갱신 단계와 HANDOFF.json은 건너뛰었다(다른 세션 소유 — 오케스트레이터가 처리)
- 이 플랜에는 Codex 교차 검토 단계가 없어 Opus 대체 검토를 띄우지 않았다

## Known Stubs

- `domain/leave/dto.ts` `LEAVE_BALANCE_DTO_SPEC`(fiscalYear · annual · monthly) — 인터페이스만 있고 쓰는 곳이 없다. 04.1-03이 잔고 계산과 함께 채운다(플랜 명시)

## 알려진 한계(04.1-07이 TODOS로 옮김)

- CEO-10: `createRole`로 만든 새 계급은 `approval.value` · `leave.value` 노출 행이 없어 결재함 줄이 빈 DTO가 된다 — `approvals-inbox-projection.test.ts`가 지금 동작을 기록했다. 공개 전에 관리자가 노출표에서 두 항목을 켜야 한다

## Threat Flags

없음 — 새 표·서비스는 플랜의 threat_model(T-04.1-01~53) 범위 안이고 새 네트워크 입구·액션은 없다(액션은 04.1-02 이후).

## User Setup Required

None.

## Next Phase Readiness

- 04.1-03(잔고)이 `canSeeLeaveDocument` · `LEAVE_BALANCE_DTO_SPEC` · `lib/dates.ts`에 덧붙이고, 04.1-02가 `staleOrFinalRefusal` 자리에 관련자 판정을, `setupOrphanFinal()` describe에 회수 사례를 더할 수 있다
- 오케스트레이터 참고: (1) 마이그레이션 번호 0017은 브랜치 임시 번호다. 04.1-07 재생성 때 `migration_manifest`의 파일만 지우고 SET LOCAL 블록을 다시 넣는다. (2) 「전이 로그가 tx 밖으로 새는」 회귀를 결정적으로 막는 가드는 없다(위 Issues) — 필요하면 04.1-02 Task 3에서 lock-client 방식 가드를 검토
- 한도 풀리면 Codex 재확인 필요: 이 플랜 실행분은 교차 검토를 받지 않았다

## Self-Check: PASSED

- 생성 파일 29개 존재 확인, 커밋 7개(5ab6dde · 33643e8 · d2d0a6f · 63b1c27 · a3b42dd · d676415 · 285adfd) `git log`에 있음

---
*Phase: 04.1-approvals-leave*
*Completed: 2026-09-26*
