---
phase: 04.1-approvals-leave
plan: 02
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-03"]
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - app/(app)/document-kinds.ts
  - app/(app)/leave/status-display.ts
  - app/(app)/leave/actions.ts
  - app/(app)/leave/actions.registry.ts
  - app/(app)/leave/new/page.tsx
  - app/(app)/leave/new/leave-form.tsx
  - app/(app)/leave/[id]/page.tsx
  - app/(app)/approvals/page.tsx
  - app/(app)/approvals/inbox-table.tsx
  - app/(app)/approvals/actions.ts
  - app/(app)/approvals/actions.registry.ts
  - domain/permissions/menus.ts
  - domain/seed/approvals-leave.ts
  - domain/approvals/index.ts
  - domain/approvals/dto.ts
  - domain/approvals/kinds.ts
  - domain/approvals/conflict-message.ts
  - domain/leave/resubmit.ts
  - domain/leave/access.ts
  - domain/leave/index.ts
  - domain/leave/balance-service.ts
  - repositories/approvals.ts
  - repositories/leave-requests.ts
  - test/integration/leak-scan.test.ts
  - test/integration/approvals-lifecycle.test.ts
  - test/integration/approvals-route-fixed.test.ts
  - test/integration/approvals-concurrency.test.ts
  - test/unit/domain/approvals/conflict-message.test.ts
  - test/unit/document-kinds-import.test.ts
  - test/unit/action-registry-completeness.test.ts
  - test/unit/e2e/leave-dates.test.ts
  - test/integration/leave-permission.test.ts
  - test/e2e/leave-dates.ts
  - test/e2e/leave-approval.spec.ts
autonomous: true
requirements: [EXP-03, EXP-04, EXP-05]

estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 `/leave/new`에서 연차를 신청하면 `/leave/[id]`로 이동하고 머리에 번호와 `결재 중` 태그가 보이며, 1단 결재자의 `/approvals` `내 결재` 그룹에 그 문서가 한 줄로 뜬다(EXP-05 흐름의 화면 트레이서)"
    - "결재자는 `/approvals` PC 행의 3차 `승인`으로 확인 없이 즉시 승인하고, 그 행은 `내 결재`에서 `처리함`으로 옮겨 간다(UI-SPEC Assumptions #11 · S4 populated)"
    - "`/approvals`는 메뉴 게이트가 없는 개인 결재함이다 — 세션만 요구하고 내용은 결재선 후보·처리 기록으로만 정해진다. 0건이면 `결재할 건이 없습니다 · 연차 목록 보기`(→ `/leave`)다(오케스트레이터 결정 11 — UI-SPEC EMPTY 문구도 이 값으로 정정됨, CEO-22)"
    - "문서 종류 등록은 side-effect import 순서에 기대지 않는다 — `app/(app)/document-kinds.ts` 한 파일이 종류 모듈을 import하고, `@/domain/approvals`나 `@/domain/leave/resubmit`을 런타임으로 부르는 `app/` 서버 파일은 전부 이 파일을 import한다. 단위 가드 테스트가 빠진 파일을 잡는다(CEO-3). `import type`은 세지 않고 `\"use client\"` 파일은 이 조건에서 빠지는 대신 두 모듈을 런타임 import하지 않아야 한다 — 클라이언트 컴포넌트가 가드를 맞추려고 `document-kinds`(→ DB 코드)를 번들에 끌어오지 않는다(B-A3)"
    - "승인·반려·회수·다시 신청은 공용 전이 함수 하나를 지나고 순서가 고정이다: version 확인(불일치면 관련자 판정으로 오류 종류만 가른다 — ENG-6) → 종결 상태 확인(사건별 — `approved` · `withdrawn`은 모든 사건에 종결, `rejected`는 기안자의 `resubmit`만 빼고 종결이라 `nextStep(rejected, resubmit)`이 반려에서 나가는 유일한 전이다 — CX-B1) → 후보(또는 기안자) 판정 → 상태 UPDATE 먼저 → 단계·차수 행 쓰기. 결재선 설정·스냅숏은 트랜잭션 전에 읽는다(CEO-6 · CEO-2)"
    - "(ENG-6 · D1) version 불일치의 상세 충돌 문구(처리자 이름 · 시각 · 상태)는 관련자에게만 간다 — 관련자 = 기안자 · 이 인스턴스에서 단계를 처리한 사람(`acted_by`, 모든 차수) · 지금 차수 단계들의 지금 해석한 담당(기안자 제외 전 담당 집합 — 지금 자리가 대표 폴백이면 폴백 단계 행이 아직 없어도 그 폴백 후보 포함, X-3). 관련자가 아닌 로그인 사용자가 인스턴스 id와 옛 version으로 승인 · 반려 · 회수 · 다시 신청을 불러도 `NotCurrentHolderError`(`지금 담당이 아님 · 새로 고침`)만 받고 `approval.refused` reason은 `not_holder`다 — `handleServerError`가 `UserFacingError` 문구를 그대로 화면에 보내므로(`lib/actions/handle-server-error.ts:24`) 막는 곳은 도메인이다"
    - "비후보(기안자 · 무관한 사람 · 차례 전의 대표)의 반려는 맞는 version이어도 거부되고 단계 기록 0건 · version 불변이다. 모든 거부는 `approval.refused` 운영 로그(id·사유 코드만)를 남긴다(CEO-8 · CEO-18)"
    - "대표 폴백 후보 둘이 동시에 승인하면 하나만 성공하고 진 쪽은 `{이름}이/가 HH:MM에 승인함 · 새로 고침`을 받으며 폴백 단계 행은 1개다. 반려된 문서의 다시 신청을 두 번 동시에 보내면 차수 2가 하나만 생기고 진 쪽은 `{기안자}이/가 HH:MM에 다시 신청함 · 새로 고침`을 받는다 — 원시 UNIQUE 위반 오류나 빈 문구가 사용자에게 가지 않는다(CEO-6 · B-C1)"
    - "(B-A1 · CX-R3와 같은 규칙) 신청 · 다시 신청 · 승인 · 반려 네 액션이 돌려주는 토스트 재료(다음 담당 이름 · 기안자 이름 · 차감 일수)는 등록 DTO `ApprovalActionResultDto`로 `project()`를 지난 값이다 — 결재자 계급에 `approval.value`가 꺼져 있으면 반환 JSON에 사람 이름이 없고 `leave.value`가 꺼져 있으면 일수가 없으며, 토스트는 그 조각을 뺀 `승인 · 결재 요청됨` · `승인 · 최종 승인` · `반려 · 기안자에게 돌아감` · `연차 신청 · 결재 요청됨` · `연차 다시 신청 · 결재 요청됨`이다. 미리보기 · 상세에서 숨긴 이름 · 일수가 액션 반환 경로로 새지 않는다"
    - "`/leave/new`는 `can(viewer, \"leave\", \"write\")`가 없으면 404다. `leave` 메뉴 키가 MENUS에 있고 계급 5종 모두 view·write가 기본 허용이다(UI-SPEC S10)"
    - "`leave` write 판정은 페이지만이 아니라 도메인에서도 한다 — `submitLeave` · `resubmitLeave` · `previewLeaveBalance`는 첫 줄에서 `assertLeaveWrite`(`domain/leave/access.ts`)를 불러, 로그인만 한 사용자가 서버 액션을 직접 불러도 `ForbiddenError`로 거부되고 신청 행 · 결재 인스턴스 · 번호 카운터가 하나도 생기지 않는다. 회수는 기안자 본인 판정만 한다(권한이 거둬진 기안자도 자기 진행 중 신청을 거둘 수 있어야 한다 — 계획 가정 4). 반려 문서의 가능 행동 `다시 신청`도 같은 판정 함수(`canWriteLeave`)가 참일 때만 나와, write가 거둬진 기안자에게 실행할 수 없는 폼이 보이지 않는다(CX-W1)(Codex HIGH 02 · `authedActionClient`는 로그인·크기만 본다)"
    - "결재자가 반려하면 사유(공백 제외 1자 이상 500자 이하)가 그 단계에 저장되고 상태가 `rejected`가 되어 기안자에게 돌아간다. 사유가 비거나 500자를 넘으면 서버가 거부한다(EXP-03 · 입력 §4)"
    - "기안자는 최종 승인 전(`submitted`·`in_review`)에만 회수한다. 회수는 끝 상태이고 이미 승인한 단계 기록은 남는다. 최종 승인된 연차는 회수가 없다(UI-SPEC Assumptions #6)"
    - "반려된 연차를 기안자가 고쳐 다시 신청하면 번호는 그대로이고 새 차수(round + 1)의 결재선이 다시 신청한 시점의 설정으로 고정된다. 이전 차수의 단계·반려 사유는 지워지지 않는다(EXP-04 ordering · Assumptions #7)"
    - "승인↔회수 · 승인↔반려 동시 조작은 두 순서 모두 하나만 성공하고, 진 쪽(담당자 · 기안자끼리의 경주라 진 쪽은 늘 관련자 — ENG-6)은 서버 값으로 만든 `박서연이 14:01에 회수함 · 새로 고침` · `이수아가 14:01에 승인함 · 새로 고침` · `김팀장이 14:01에 반려함 · 새로 고침` · `최종 승인됨 · 새로 고침` 중 하나를 받는다(EXP-03 concurrency · ROADMAP 기준 3)"
    - "같은 단계의 후보 둘이 동시에 승인하면 하나만 기록되고 다른 쪽은 `{먼저 승인한 사람}이/가 HH:MM에 승인함 · 새로 고침`을 받는다 — 단계 처리 기록이 둘이 되지 않는다(EXP-05 concurrency)"
    - "반려·회수·다시 신청도 행동 로그에 남는다(`document_reject`·`document_withdraw`·`document_submit` detail round) — 로그 행은 04.1-01 `recordActionInTx`로 전이와 **같은 트랜잭션**에 쓰여, 로그 INSERT가 실패하면 상태 · version · 단계 · 차수 행이 전부 그대로다(Codex HIGH 원자성). 새 서버 액션은 전부 `actions.registry.ts`로 등록되어 누수 스캔 액션 축에 들어가고, 단위 가드가 registry가 아니라 `app/**/actions.ts`에서 출발해 모든 액션 파일이 옆 등록 파일을 갖고 내보낸 액션 이름이 등록 이름에 전부 있다는 것을 증명한다 — 등록 파일 없는 기존 액션 파일은 명시적 예외 목록(`app/(app)/account/actions.ts` · `changePasswordAction`)에만 있을 수 있다(ROADMAP 기준 5 · Codex MEDIUM · ENG-19)"
    - "E2E가 만드는 연차 날짜는 실행하는 날이 1월 1일이든 12월 31일이든 유효하다 — 스펙은 `test/e2e/leave-dates.ts`의 헬퍼로 「그해 3월 첫 월요일 + 주 오프셋」에서 평일 범위를 만들어 범위가 회계연도를 넘지 않고, 헬퍼의 단위 테스트가 1월 1일 · 6월 30일 · 12월 31일 · 윤년을 증명한다(Codex HIGH 06)"
    - "상태 → 색·자리 매핑은 `app/(app)/leave/status-display.ts` 한 곳이고 UI-SPEC Color 절 매핑표와 같다 — 회수 = `--muted`, 내 결재 = `--accent`는 SYSTEM.md §7-5 개정(A2)이 먼저 들어간 뒤에 코드가 쓴다"
    - statement: "결재함·신청 폼·문서 화면이 폭 1280 · 1024 · 375에서 가로 스크롤 0이고 결재함 표에 숨긴 caption `결재함`이 있다(UI-SPEC UI Considerations responsive·a11y backstop 행 — 화면 완성은 04.1-05·04.1-06이 하고 이 플랜은 첫 형태를 만든다)"
      verification: backstop
  prohibitions:
    - statement: "충돌이 났을 때 서버나 클라이언트가 자동으로 다시 시도해서는 안 된다. 진 쪽 사람은 무슨 일이 있었는지(누가 · 언제 · 무엇을) 보고 스스로 새로 고친다 — 자동 재시도는 회수된 문서를 승인하는 사고를 만든다"
      status: resolved
      verification: test
    - statement: "`can(viewer, \"approvals\", \"approve\")` 같은 메뉴 권한으로 승인·반려 가능 여부를 정해서는 안 된다. 결재 권한은 결재선(지금 단계의 후보)이고, 액션 등록의 메뉴·동작 값은 누수 스캔의 분류일 뿐이다"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/approvals/page.tsx"
      provides: "개인 결재함 — 내 결재 · 처리함 두 그룹, PC 행 3차 승인"
      contains: "requireSession"
    - path: "app/(app)/leave/new/leave-form.tsx"
      provides: "연차 신청 폼 첫 형태(종류 · 날짜 · 오전/오후 · 비고)"
    - path: "app/(app)/leave/status-display.ts"
      provides: "문서 상태 → kind·자리 매핑의 유일한 출처"
      exports: ["leaveStatusDisplay"]
    - path: "domain/approvals/conflict-message.ts"
      provides: "충돌 문구 조립(사람 · 시각 · 동작, 이/가 조사) + 관련자 판정(D1 — 상세 문구를 받을 자격, ENG-6)"
      exports: ["buildConflictMessage", "isApprovalParty"]
    - path: "test/integration/approvals-concurrency.test.ts"
      provides: "04.1-01의 동시 제출 describe에 더해 승인↔회수 · 승인↔반려 두 순서 + 동시 실행 경주 + 폴백 경합 + 다시 신청 중복 증명"
      min_lines: 90
    - path: "app/(app)/document-kinds.ts"
      provides: "문서 종류 등록 한 곳 — 연차 모듈 side-effect import 한 줄(Phase 5가 줄을 더한다)(CEO-3)"
      contains: "@/domain/leave"
    - path: "test/unit/document-kinds-import.test.ts"
      provides: "@/domain/approvals · @/domain/leave/resubmit을 런타임 import하는 app 서버 파일이 document-kinds도 import하는지 가드(CEO-3) — `import type` 제외 · `\"use client\"` 파일은 런타임 import 0건 단언으로 대신(B-A3)"
      min_lines: 20
    - path: "test/e2e/leave-approval.spec.ts"
      provides: "신청 → 결재함 → 승인 → 최종 승인 PC 흐름"
      min_lines: 40
    - path: "test/e2e/leave-dates.ts"
      provides: "E2E 연차 날짜 헬퍼 — 기준일의 서울 연도 안 평일 범위(Playwright import 없음, 단위 테스트 대상)"
      exports: ["leaveWeekdayRange"]
    - path: "test/unit/action-registry-completeness.test.ts"
      provides: "app/**/actions.ts 전부를 열거해 각 파일의 액션 이름 ⊆ 옆 actions.registry.ts의 등록 이름, 등록 파일 없는 액션 파일은 예외 목록에만(Codex MEDIUM 누수 스캔 완전성 · ENG-19)"
      min_lines: 25
    - path: "test/integration/leave-permission.test.ts"
      provides: "leave write 없는 계급의 신청 · 다시 신청 · 미리보기 직접 호출 거부 + 행 0건(Codex HIGH 02)"
      min_lines: 40
  key_links:
    - from: "app/(app)/approvals/actions.ts"
      to: "domain/approvals/index.ts"
      via: "approveDocument · rejectDocument — expectedVersion을 폼에서 받아 그대로 넘긴다"
      pattern: "expectedVersion"
    - from: "app/(app)/approvals/actions.ts"
      to: "domain/approvals/dto.ts"
      via: "approveAction · rejectAction(그리고 leave/actions.ts의 submitLeaveAction · resubmitLeaveAction)이 토스트 재료를 projectActionResult로 투영한 뒤 돌려준다(B-A1)"
      pattern: "projectActionResult"
    - from: "app/(app)/approvals/page.tsx"
      to: "app/(app)/document-kinds.ts"
      via: "종류 등록 한 곳을 import해 연차 종류 등록을 보장한다(요약·링크를 종류가 준다) — 가드 테스트가 빠짐을 잡는다(CEO-3)"
      pattern: "document-kinds"
    - from: "test/integration/leak-scan.test.ts"
      to: "app/(app)/approvals/actions.registry.ts"
      via: "파일 끝 side-effect import — leak-scan-coverage 단위 테스트가 목록 누락을 막는다"
      pattern: "approvals/actions.registry"
    - from: "domain/leave/index.ts"
      to: "domain/leave/access.ts"
      via: "submitLeave 첫 줄 assertLeaveWrite — resubmit.ts · balance-service.ts의 previewLeaveBalance도 같은 함수"
      pattern: "assertLeaveWrite"
    - from: "domain/approvals/index.ts"
      to: "domain/approvals/tx-log.ts"
      via: "공용 전이 함수가 반려·회수·다시 신청 로그를 같은 tx에 쓴다"
      pattern: "recordActionInTx"
    - from: "domain/approvals/index.ts"
      to: "domain/approvals/conflict-message.ts"
      via: "0행 갱신 뒤 현재 행을 다시 읽어 문구를 만든다"
      pattern: "buildConflictMessage"
    - from: "domain/approvals/index.ts"
      to: "domain/approvals/conflict-message.ts"
      via: "version 불일치 때 관련자면 상세 충돌 오류, 아니면 NotCurrentHolderError(ENG-6 · D1)"
      pattern: "isApprovalParty"
---

<objective>
**As a** 연차를 내는 직원과 결재자, **I want to** 화면에서 연차를 신청하고 결재함에서 승인·반려하며, 기안자는 결재가 끝나기 전에 회수하고 반려되면 고쳐 다시 신청하기를, **so that** 결재의 모든 갈래(승인 · 반려 · 회수 · 다시 신청)가 화면과 서버에서 한 규칙으로 돌고, 두 사람이 동시에 눌러도 문서 상태가 꼬이지 않는다.

Task 1은 **화면 층 트레이서**다 — 04.1-01의 엔진 위에 `/leave/new` → `/leave/[id]` → `/approvals` 승인 경로 하나를 서버 액션·권한·누수 스캔·E2E까지 관통시킨다. Task 2·3은 거기서 갈래(반려·회수·다시 신청)와 동시성으로 넓힌다. 화면의 완성형(문서 화면 행동 줄 · 폰 결재 시트 · 반려/회수 확인 · 결재선 컴포넌트)은 04.1-05, 신청 폼·목록 완성형은 04.1-06이다.

Purpose: ROADMAP 기준 3(승인·반려·회수 + version 낙관적 잠금 두 순서 증명)의 서버 쪽 전부와 기준 5(새 액션 누수 스캔 · 행동 로그)의 액션 부분이 이 플랜에서 성립한다.

Output: 화면 셋의 첫 형태, 서버 액션 두 파일(+ 등록 파일), 반려·회수·다시 신청 도메인, 충돌 문구, SYSTEM.md A2 개정, 단위 1 · 통합 2 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md
@.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md
@.planning/phases/04.1-approvals-leave/04.1-PATTERNS.md
@.planning/phases/04.1-approvals-leave/04.1-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다. 테스트·빌드·E2E가 실패하면 원인을 쫓기 전에 `systematic-debugging`을 호출한다. 커밋하거나 「완료」를 말하기 전에 `verification-before-completion`을 호출하고 verify 명령의 실제 출력으로 판정한다. 서브에이전트 프롬프트에도 세 스킬 이름을 적는다.
- 화면 검증 순서(CLAUDE.md 프론트엔드): 싼 게이트(lint·typecheck·build) → 독립 DOM 감사(실행자가 아닌 별도 에이전트, `CI=true`, 스크린샷 육안 금지) → 수정 → 전체 게이트 한 번.
- SYSTEM.md 개정은 `docs/design/DECISIONS.md` 먼저 → `SYSTEM.md` → 코드 순서다(CLAUDE.md). 새 색·서체·radius 0개 — 토큰은 `docs/design/tokens.css`에서만.
- 04.1-04(설정 화면 — 이 플랜 바로 뒤)와 파일이 겹치지 않는다. 04.1-03(바로 앞)이 만든 `domain/leave/balance-service.ts`와 04.1-01의 `domain/leave/index.ts` · `domain/leave/access.ts`는 Task 2가 `assertLeaveWrite` 한 함수를 더하고 부르는 줄만 고친다(Codex HIGH 02) — 직렬 체인이라 동시에 고치는 플랜이 없다. 이 플랜은 마이그레이션을 만들지 않는다(스키마 변경 없음) — `db/`에 diff가 생기면 멈추고 원인을 적는다.
- 건드리지 않는 파일: `domain/rules/register.ts` · `ui/next-turn/*` · 첫 화면 「내 차례」 블록(오케스트레이터 결정 9 · UI-SPEC Assumptions #9) · `domain/action-log/*`. `domain/permissions/menus.ts`·`test/integration/leak-scan.test.ts`는 끝에 덧붙이기만.
- 첫 task를 시작하기 전에 `git rev-parse HEAD` 값을 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도 남긴다). 이 플랜의 diff 기준 검사는 전부 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다 — 커밋한 뒤의 맨 `git diff`는 늘 비어 있어 검사가 공허해진다. Bash 호출 사이에 셸 변수가 남지 않으므로(C-05와 같은 문제) 값은 잡는 즉시 `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-04.1-02.env"`로 파일에 쓰고(`.git` 안이라 `git status`에 안 잡힌다), `$PLAN_BASE`를 쓰는 검사는 전부 `. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" &&`로 시작한다 — 파일이 없거나 값이 비면 그 검사는 실패다. 빈 값이면 `git diff "$PLAN_BASE"..HEAD`가 `HEAD..HEAD`(빈 diff · rc 0)가 되어 공허하게 통과하기 때문이다(04.1-01 · 04.1-03과 같은 모양).
- **실행 순서(공유 DB)**: 이 페이즈의 일곱 플랜은 한 줄로 이어진 체인이다(웨이브마다 플랜 하나: 01 → 03 → **02** → 04 → 05 → 06 → 07). 이 플랜은 04.1-03이 끝난 뒤 시작하고 04.1-04는 이 플랜이 끝난 뒤 시작한다. 로컬 Postgres(`scripts/dev-db.sh`의 고정 DB `erp` · `erp_test`)는 모든 플랜이 공유하고 통합 테스트 setup이 매 테스트 전 전 표를 비우므로, 두 플랜이 동시에 돌지 않는 것은 이 순서가 보장한다 — 잠금·새 도구를 두지 않는다. 검증 명령은 전부 `bash scripts/dev-db.sh && pnpm db:migrate`로 시작하므로 04.1-03의 마이그레이션까지 적용된 DB에서 돈다. 원인 모를 「행이 사라짐」 실패가 나면 다른 실행이 같은 DB를 쓰고 있지 않은지 먼저 확인한다(`systematic-debugging`).
</execution_notes>

<probe_fallback>
이 플랜이 닫는 미해결 엣지: **EXP-03 concurrency**(두 순서 + 경주 — 「승인↔회수 · 승인↔반려 동시 조작」 truth) · **EXP-04 ordering**(다시 신청 = 새 차수 — 「반려된 연차를 … 다시 신청」 truth) · **EXP-05 concurrency**(후보 둘 동시 승인 — 「같은 단계의 후보 둘」 truth). EXP-05 idempotency(폰에서 두 번 탭)는 04.1-05가 화면(`aria-disabled`)과 이 플랜의 서버 version 충돌을 묶어 닫는다.

**계획 가정**
1. 결재함 EMPTY의 다음 한 수는 `연차 목록 보기`(→ `/leave`)다 — 오케스트레이터 결정 11. 지출결의 목록은 Phase 5 화면이라 이 페이즈에 없다(UI-SPEC Copywriting도 이 값으로 정정됨 — CEO-22)
2. `/leave/[id]`는 메뉴 게이트가 아니라 문서 보임 규칙(기안자 · 지금 후보 · 처리자)만 따른다 — 결재자는 `leave` 메뉴 권한과 무관하게 자기 차례 문서를 본다. `/leave/new`만 `leave` write 게이트다
3. 충돌 문구의 시각은 서울 시간 `HH:MM`, 이름 뒤 조사는 마지막 음절 받침으로 이/가를 고른다
4. `leave` write는 「새로 신청하는 일」(신청 · 다시 신청 · 신청 미리보기)의 권한이다. 회수는 기안자 본인 판정만 한다 — 권한이 거둬진 직원이 이미 낸 신청을 거두지 못하면 결재자가 대신 반려해야 하는 우회가 생긴다. 승인·반려는 결재선 후보가 권한이다(금지 항목)
5. E2E 날짜는 과거여도 된다 — 신청 규칙이 지난 날짜를 막지 않는다(04.1-01 `countLeaveQuarters`는 평일 수 · 회계연도 경계만 본다). 헬퍼는 기준일의 서울 연도 안에서만 날짜를 만든다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 화면 트레이서 — `/leave/new` 신청 → `/leave/[id]` → `/approvals` 승인 → 최종 승인 (액션 · 권한 · 누수 스캔 · E2E)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, app/(app)/document-kinds.ts, app/(app)/leave/status-display.ts, app/(app)/leave/actions.ts, app/(app)/leave/actions.registry.ts, app/(app)/leave/new/page.tsx, app/(app)/leave/new/leave-form.tsx, app/(app)/leave/[id]/page.tsx, app/(app)/approvals/page.tsx, app/(app)/approvals/inbox-table.tsx, app/(app)/approvals/actions.ts, app/(app)/approvals/actions.registry.ts, domain/approvals/dto.ts, domain/permissions/menus.ts, domain/seed/approvals-leave.ts, test/integration/leak-scan.test.ts, test/unit/document-kinds-import.test.ts, test/unit/action-registry-completeness.test.ts, test/e2e/leave-dates.ts, test/unit/e2e/leave-dates.test.ts, test/e2e/leave-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 49–80행(Component Inventory) · 136–177행(Color · 상태 → 색 매핑표) · 178–224행(Copywriting 전문) · 256–285행(S2) · 286–319행(S3 — 이 task는 머리와 KvList만) · 320–347행(S4) · 398–416행(A2)
    - `docs/design/SYSTEM.md` §6-1(315–356행) · §6-3(380–413행) · §7-5(791–798행) · §7-7(805–833행) · §7-15(972–1006행) · §8(1007–1020행)
    - `docs/design/DECISIONS.md` 끝 60행 — 개정 기록의 형식
    - `app/(app)/approvals/page.tsx`(현재 자리표시 전문) · `app/(app)/projects/page.tsx` · `app/(app)/projects/actions.ts` · `app/(app)/projects/actions.registry.ts` — 세션·권한 게이트, `authedActionClient`, 등록 파일 분리 이유
    - Phase 4의 상태 표시 선례: 로컬에는 그 원격 참조가 없으므로(`git branch -r` = `origin/main` · `origin/claude/phase-04.1-plan-1iqtwe`뿐 — B-C5) 먼저 `git fetch origin claude/gsd-progress-e1nzgu`를 하고, `git show origin/claude/gsd-progress-e1nzgu --stat`에서 `status-display.ts`를 찾아 읽는다(가져오기가 실패하거나 없으면 UI-SPEC 매핑표만으로 만든다)
    - `ui/table/Table.tsx` · `ui/table/types.ts` · `ui/list-empty` · `ui/page-header` · `ui/kv-list` · `ui/status-tag` · `ui/form` · `ui/select` · `ui/toast` — 쓰기만 한다(이 플랜은 `ui/`를 고치지 않는다)
    - `lib/actions/registry.ts` — `registerAction({name, menu, action, dto})` 모양
    - `domain/permissions/menus.ts`(전문) · `domain/seed/approvals-leave.ts`(04.1-01 산출물)
    - `test/unit/leak-scan-coverage.test.ts` · `test/unit/page-auth-guard.test.ts` — 새 `actions.registry.ts`는 leak-scan에 import되어야 하고 새 page.tsx는 `requireSession`을 가져야 한다
    - `test/e2e/fixtures.ts` · `test/e2e/mobile-org.spec.ts`(로그인 헬퍼 모양) · `playwright.config.ts`(clock 고정 없음 — 47행 부근) · `vitest.config.ts`의 unit 프로젝트 include(`test/unit/**/*.test.ts`) · `lib/dates.ts`(`seoulToday`)
    - `node_modules/next/dist/docs/`에서 Server Actions·`redirect`·`notFound` 문서 — Next 16 변경점 확인(CLAUDE.md)
  </read_first>
  <behavior>
    - (E2E, desktop) 픽스처로 전용 본부·팀을 만들고 기안자(기획 PM)·팀장·본부 책임자·경영관리팀 소속 한 명·대표를 둔다. 기안자가 `/leave/new`에서 종일 두 평일을 신청하면 `/leave/[id]`로 가고 머리에 번호 `LV`로 시작하는 문자열과 `결재 중`이 보인다
    - (E2E) 팀장이 `/approvals`에서 `내 결재` 행의 `승인`을 누르면 그 행이 `처리함`으로 옮겨 가고 토스트가 `승인 · 결재 요청됨 → {본부 책임자 이름}`이다
    - (E2E) 나머지 세 단계를 차례로 승인하면 마지막 승인의 토스트가 `승인 · 최종 승인 · 2일 차감`이고 기안자의 `/leave/[id]` 머리 태그가 `승인`이다
    - (E2E) 결재 차례가 아닌 사람이 `/leave/[id]`를 열면 404다
    - (통합) leak-scan 액션 축에 새 액션이 전부 나타나고 메뉴·동작·DTO 참조가 전부 등록되어 있다
    - (단위, Codex MEDIUM · ENG-19) 열거의 출발점은 registry가 아니라 `app/` 아래 모든 `actions.ts`다. 각 `actions.ts`에 대해: 옆 `actions.registry.ts`가 있으면 `export const …Action` 이름이 전부 그 등록 파일의 `name: "…"`에 있다. 옆 등록 파일이 없으면 그 파일 경로와 액션 이름 집합이 예외 목록 항목과 **정확히** 같아야 한다(지금은 `app/(app)/account/actions.ts` → `["changePasswordAction"]` 하나 — 여기에 액션이 하나 더 생기면 실패). 검출기 사례: (a) 일부러 빠뜨린 가짜 등록 목록 → 빠진 이름을 적고 실패, (b) 등록 파일도 예외 항목도 없는 가짜 `actions.ts` 경로 → 그 경로를 적고 실패, (c) 예외 목록에 있는데 파일이 없거나 이제 등록 파일이 생긴 항목 → 낡은 예외로 실패, (d) 실제 열거 결과에 `app/(app)/account/actions.ts` · `app/(app)/leave/actions.ts` · `app/(app)/approvals/actions.ts`가 들어 있다(공허한 통과 방지)
    - (단위, CEO-3 · B-A3 — `document-kinds-import.test.ts` 검출기, 가짜 `(경로, 소스)` 입력) `"use client"` 첫 줄 + `import type { … } from "@/domain/approvals"`만 있는 파일 → 위반 없음(오탐 아님) · 서버 파일의 `import type`만 → 위반 없음 · 서버 파일이 `@/domain/approvals/…`를 런타임 import하고 `document-kinds`가 없음 → (a) 위반 · `"use client"` 파일이 `@/domain/approvals`나 `@/domain/leave/resubmit`을 런타임 import → (d) 위반(경로를 실패 메시지에)
    - (단위, Codex HIGH 06) `leaveWeekdayRange("2026-01-01", {week: 0, weekdays: 2})` → `2026-03-02 ~ 2026-03-03`. 기준일 `2026-06-30` · `2026-12-31` · `2028-02-29`(윤년, 3월 첫 월요일 2028-03-06)에서도 시작이 월요일이고 시작·끝이 기준일과 같은 해이며 평일 수가 요청과 같다. `week` 0..30 · `weekdays` 1..20 밖이면 예외
  </behavior>
  <action>
① **A2 개정 먼저**: `docs/design/DECISIONS.md`에 A2 결정(UI-SPEC 406–408행 — §7-5 의미 목록에 `회수`→`--muted`·`내 결재`→`--accent`, 표 상태 열의 `{단계} 결재 중`은 글자라 두~네 글자 규칙 밖, §1-2 `--warning` 용도에 「잔여 초과(연차)」)을 기록하고 `docs/design/SYSTEM.md` §7-5와 §1-2 `--warning` 행을 그 문장대로 고친다. 새 토큰 0개.

② `app/(app)/leave/status-display.ts`의 `leaveStatusDisplay(…)`: UI-SPEC 매핑표를 그대로 옮긴다(결재 중 · `{단계} 결재 중` · 내 결재 · 승인 · 반려 · 회수 · 대기 · 담당 없음 → kind와 자리 tag/text). 화면 코드가 상태 문자열로 색을 직접 고르지 않게 이 함수만 쓴다.

③ **권한**: `domain/permissions/menus.ts` 끝에 `leave`(라벨 `연차`) 메뉴를 덧붙인다(시스템 관리자는 기존 시드 루프가 자동으로 받는다). `domain/seed/approvals-leave.ts`에 계급 5종 × `leave` view·write를 `insertPermissionIfAbsent`로 더한다.

④ **서버 액션**(`authedActionClient` — require-action-client 규칙): `app/(app)/leave/actions.ts`의 `submitLeaveAction`(zod: kind enum · startDate · endDate · half 선택 · note 선택 500자) → `submitLeave` → 새 문서 id를 돌려주고 클라이언트가 `/leave/[id]`로 이동. `app/(app)/approvals/actions.ts`의 `approveAction`({instanceId, expectedVersion}) → `approveDocument` → 다음 담당 이름 또는 최종 여부·차감 일수를 돌려준다(토스트 문구 재료). 두 파일 옆 `actions.registry.ts`에서 `registerAction` — 메뉴 `leave`·동작 `write`, 메뉴 `approvals`·동작 `approve`(분류일 뿐 — 금지 항목). 액션이 돌려주는 DTO 이름을 등록한다.
**액션 반환값 투영(B-A1 — CX-R3와 같은 규칙)**: 토스트 재료를 그대로 돌려주지 않는다. `domain/approvals/dto.ts`(04.1-01의 결재 DTO 등록 파일)에 DTO `ApprovalActionResultDto`를 `registerDto`로 더하고 — 필드 `nextHolderNames` · `drafterName` → `approval.value`, `deductedDays` → `leave.value`(01 결재함 DTO · 03 신청용 잔고 DTO와 같은 기존 정보 항목, 새 정보 항목 없음) — 같은 파일에 `projectActionResult(viewer, raw, deps?)`를 export한다: 이름 · 일수 필드는 이 DTO로 `project()`하고 구조 값(`documentId` · `final`)만 투영 밖에서 그대로 붙인다. 이 task의 `submitLeaveAction` · `approveAction`과 Task 2의 `resubmitLeaveAction` · `rejectAction` 네 액션은 전부 도메인 결과를 이 함수에 넣은 값을 돌려주고, 네 액션의 등록 `dtoName`은 `ApprovalActionResultDto`다. 클라이언트 토스트는 받은 필드로만 조립한다 — 빠진 조각은 문구에서 뺀다: `승인 · 결재 요청됨` · `승인 · 최종 승인` · `반려 · 기안자에게 돌아감` · `연차 신청 · 결재 요청됨` · `연차 다시 신청 · 결재 요청됨`(UI-SPEC 214행 문구에서 이름 · 일수 조각만 뺀 모양 — 새 낱말 없음). 기본 시드는 모든 계급에 두 항목이 보이므로 behavior E2E의 전체 문구는 그대로다.

⑤ **화면**(서버 컴포넌트 + 필요한 곳만 client):
- `app/(app)/leave/new/page.tsx`: `requireSession` → `leave` 메뉴 write 판정이 거짓이면 `notFound()` → PageHeader `연차 신청` → `leave-form.tsx`. 폼 첫 형태 = 종류 Select(`종일`·`반차`·`반반차`·`재택`) · 날짜(종일·재택은 시작·종료, 반차·반반차는 하루 + `오전`/`오후`) · 비고(긴 칸 480) · 1차 `연차 신청` · 2차 `취소`(→ `/leave`). 칸 오류는 서버가 돌려준 칸별 문구를 칸 아래 한 줄로(§7-15, 입력값 보존). 성공하면 토스트 `연차 신청 · 결재 요청됨 → {1단 담당}` 후 `/leave/[id]`. 잔고 행·결재선 한 줄·`Ctrl+Enter`·입력 버리기 확인은 04.1-06이 더한다.
- `app/(app)/leave/[id]/page.tsx`: `requireSession` → `getLeave`가 null이면 `notFound()` → PageHeader 제목 `연차 — 종일 09-18 ~ 09-20` + 번호 · 상태 태그(status-display) → KvList(종류 · 기간 · 일수 · 비고 · 기안). 행동 줄·결재선 목록은 04.1-05.
- `app/(app)/approvals/page.tsx`: `requireSession`만(메뉴 게이트 없음 — 개인 결재함), `import "@/app/(app)/document-kinds"`(종류 등록 한 곳 — ⑧) → `listMyInbox` → `inbox-table.tsx`에 `ui/table` 그룹 머리글 `내 결재`(비면 머리글째 없음) · `처리함`. 열 = 문서(`연차 · 종일 09-18 ~ 09-20`, 문서 → `/leave/[id]` 링크) · 기안(`박서연 · 09-15`) · 일수 · 상태(status-display 글자) · 행동(PC만, `내 결재` 행에만 3차 `승인` — 확인 없이 즉시, 반려 버튼은 04.1-05가 확인 모달과 함께 더한다). 0건이면 `ListEmpty` `결재할 건이 없습니다` + 행동 `연차 목록 보기` → `/leave`(결정 11). 숨긴 caption `결재함`.

⑥ **누수 스캔**: `test/integration/leak-scan.test.ts` **파일 끝에** 두 등록 파일의 side-effect import를 덧붙인다(04.1-01이 붙인 두 줄 아래).

⑦ **E2E** `test/e2e/leave-approval.spec.ts`(desktop 프로젝트): 다른 스펙의 픽스처와 섞이지 않게 **전용 본부·팀**을 도메인 함수로 만들고 거기에 기안자·팀장·본부 책임자를 발령한다. 3단은 기존 `경영관리팀`에 이 스펙의 한 명을 발령해 후보로 넣고, 4단은 이 스펙의 대표 픽스처가 후보가 된다(다른 스펙의 사람이 같은 자리 후보여도 이 스펙의 사람이 처리할 수 있으면 된다 — 누구든 후보면 처리 가능). 로그인은 `mobile-org.spec.ts`의 헬퍼 모양을 따른다. 신청 날짜는 `leaveWeekdayRange(today, {week: 0, weekdays: 2})`로 만든다 — 실행하는 날과 무관하게 그해 안의 평일 두 날이다. `today`는 **테스트마다 맨 앞에서 `seoulToday()`를 한 번** 불러 얻고, 그 테스트의 모든 날짜 · 연도 · 기대 문자열을 이 값 하나에서 만든다 — 한 테스트 안에서 `seoulToday()`를 두 번 부르지 않는다(실행 중 서울 날짜가 바뀌어도 한 사례 안의 날짜가 서로 다른 날·연도에서 나오지 않게, CXF2-B-RF03. 04.1-06이 `onStableSeoulDay`를 더하기 전에 도는 플랜이라 그 헬퍼는 쓰지 않는다). behavior E2E 네 줄을 단언한다.

⑨ **E2E 날짜 헬퍼(Codex HIGH 06)** `test/e2e/leave-dates.ts`: `leaveWeekdayRange(today, {week, weekdays})` — `today`(`YYYY-MM-DD`, 스펙이 테스트마다 한 번 부른 `seoulToday()` 값을 넘긴다 — CXF2-B-RF03)의 연도 Y에서 `Y-03-01` 이후 첫 월요일을 기준으로 `week`주 뒤 월요일부터 토·일을 건너뛰며 `weekdays`번째 평일까지를 `{startDate, endDate}`로 돌려준다. 날짜 산술은 UTC 자정 기준 문자열 연산(시간대가 날짜를 밀지 않게). 인자 범위(week 0..30 · weekdays 1..20)가 끝을 Y 안(늦어도 11월)으로 묶고, 범위 밖이면 예외다. 이 파일은 Playwright를 import하지 않는다 — 단위 테스트 `test/unit/e2e/leave-dates.test.ts`가 behavior의 네 기준일을 단언한다. 04.1-05 · 04.1-06의 E2E도 이 헬퍼만 쓴다(스펙마다 다른 `week`로 기간이 겹치지 않게).

⑩ **누수 스캔 완전성(Codex MEDIUM · ENG-19)** `test/unit/action-registry-completeness.test.ts`(모양은 `test/unit/leak-scan-coverage.test.ts`의 재귀 파일 찾기): 열거는 **`app/` 아래 `actions.ts` 전부**에서 시작한다(registry에서 시작하면 등록 파일이 없는 액션 파일이 보이지 않는다 — 실측: `app/(app)/account/actions.ts`의 `changePasswordAction`). 각 파일에서 `export const (\w+Action)\b` 이름을 모으고, 같은 디렉터리에 `actions.registry.ts`가 있으면 그 `name: "…"` 집합에 전부 있는지, 없으면 테스트 파일 안의 예외 목록 `ACTIONS_WITHOUT_REGISTRY`(경로 → 액션 이름 배열, 항목마다 이유 주석 한 줄)와 정확히 같은지 단언한다. 예외 목록의 초깃값은 `app/(app)/account/actions.ts` → `["changePasswordAction"]` 하나다(이유: 본인 비밀번호 변경 — 세션 본인만 대상이고 돌려주는 DTO가 없어 누수 스캔 DTO 축에 올릴 것이 없다. Phase 1 기존 액션). 새 액션 파일을 예외 목록에 넣지 않는다 — 04.1의 두 `actions.ts`는 등록 파일을 가진다. 판정 로직은 `(files, registries, exceptions) → 위반 목록` 순수 함수로 두어 behavior의 검출기 사례 (a)~(c)를 가짜 입력으로 돌린다(빠진 이름 · 등록도 예외도 없는 경로 · 낡은 예외 항목을 실패 메시지에). 기존 등록 파일은 이미 이 조건을 지킨다 — 실패하면 테스트가 아니라 빠진 등록을 고친다. 실행 시점에 `app/` 아래 등록 파일 없는 `actions.ts`가 account 말고 더 있으면(Phase 4 병합 등) 멈추고 그 파일을 SUMMARY에 적은 뒤 등록 파일을 만들지 예외로 둘지 판단 근거와 함께 처리한다.

⑧ **종류 등록 한 곳(CEO-3)**: `app/(app)/document-kinds.ts`를 만든다 — 내용은 연차 모듈(`@/domain/leave`)의 side-effect import 한 줄과 「결재 모듈을 부르는 모든 app 진입점이 이 파일을 import한다 · Phase 5는 여기에 줄을 더한다」 주석 한 줄. 이 task에서 `@/domain/approvals`를 import하는 app 파일(`approvals/page.tsx` · `approvals/actions.ts` · `leave/[id]/page.tsx`가 결재 조회를 쓰면 그것도)은 전부 이 파일을 import한다. `test/unit/document-kinds-import.test.ts`(모양은 `test/unit/page-auth-guard.test.ts` — `app/` 아래 `.ts`·`.tsx`를 전부 훑는다): (a) `@/domain/approvals`(하위 경로 포함) 또는 `@/domain/leave/resubmit`을 **런타임** import하는 서버 파일은 `document-kinds`도 import한다 — 검출 정규식은 `import type …`을 세지 않는다(`test/unit/import-cycles.test.ts`의 `(type\s+)?` 분기와 같은 규칙, B-A3), (b) `document-kinds.ts` 자신은 `@/domain/leave`를 import한다, (c) 훑은 파일 중 조건 (a)에 걸린 파일이 1개 이상이다(빈 목록이면 공허한 통과), (d) 첫 문장이 `"use client"` 지시문인 파일은 (a)에서 건너뛰고, 대신 두 모듈을 런타임 import하지 않는다(타입 import만 허용 — 04.1-05 `inbox-table.tsx` · `approval-sheet.tsx` · `document-actions.tsx`, 04.1-04 `settings-form-client.tsx`가 결재 타입을 `import type`으로 가져와도 가드를 맞추려 `document-kinds` → `@/domain/leave` → DB 코드를 클라이언트 번들에 끌어오지 않게, B-A3). 판정은 `(경로, 소스) 목록 → 위반 목록` 순수 함수로 두고 behavior의 B-A3 검출기 사례를 가짜 입력으로 돌린다(⑩과 같은 모양). 실패 메시지에 빠진 파일 경로를 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts test/unit/page-auth-guard.test.ts test/unit/design-system-docs.test.ts test/unit/document-kinds-import.test.ts test/unit/action-registry-completeness.test.ts test/unit/e2e/leave-dates.test.ts</automated>
    <fails_when>새 등록 파일이 leak-scan에 import되지 않았거나, 새 page.tsx에 세션 검사가 없거나, SYSTEM.md 개정이 문서 구조 테스트를 깨거나, 결재 모듈을 런타임으로 부르는 app 서버 파일 중 `document-kinds`를 import하지 않은 것이 있거나, `"use client"` + `import type`만 있는 가짜 파일이 위반으로 잡히거나(오탐 — B-A3) `"use client"` 파일의 런타임 import가 통과하거나, `actions.ts`의 액션 하나라도 등록되지 않았거나, 날짜 헬퍼가 네 기준일 중 하나에서 다른 해 · 월요일 아닌 시작 · 틀린 평일 수를 내면 failed가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts</automated>
    <fails_when>새 액션의 메뉴(`leave`)·동작·DTO 중 하나라도 레지스트리에 없어 액션 축 케이스가 실패한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/leave-approval.spec.ts --project desktop</automated>
    <fails_when>playwright 출력의 failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 최종 승인 토스트 또는 `승인` 머리 태그가 나오지 않으면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - DECISIONS.md의 A2 기록 커밋이 SYSTEM.md 개정과 같은 커밋이거나 그보다 앞서고, status-display.ts 커밋보다 앞선다(`git log --oneline -- docs/design app/\(app\)/leave/status-display.ts` 순서)
    - `app/(app)/approvals/page.tsx`에 `can(` 호출이 없다(`grep -v '^\s*//' "app/(app)/approvals/page.tsx" | grep -c "can("`이 0)
    - `app/(app)/leave/new/page.tsx`에 `"leave", "write"` 게이트가 있다
    - `domain/permissions/menus.ts`·`test/integration/leak-scan.test.ts` diff가 파일 끝 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" && git diff -U0 "$PLAN_BASE"..HEAD -- domain/permissions/menus.ts test/integration/leak-scan.test.ts`의 `-` 줄이 0이고 hunk가 파일마다 마지막 줄 뒤 하나)
    - 결재함 EMPTY 행동의 href가 `/leave`다
    - `ui/` 디렉터리 diff가 비어 있다(`. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- ui/`가 종료 코드 0 · 출력 없음)
    - (CEO-3) `app/(app)/document-kinds.ts`가 있고 `app/(app)/approvals/page.tsx`에 `@/domain/leave` side-effect import가 없다(`grep -c "import \"@/domain/leave\"" "app/(app)/approvals/page.tsx"`이 0 — 등록은 `document-kinds`로만)
    - (B-A3) `test/unit/document-kinds-import.test.ts`에 `"use client"` + `import type` 오탐 없음 사례와 `"use client"` 런타임 import 위반 사례가 있다(`grep -c "use client" test/unit/document-kinds-import.test.ts`이 2 이상)
    - (B-A1) `domain/approvals/dto.ts`에 `ApprovalActionResultDto` 등록과 `export … projectActionResult`가 있고, `approveAction` · `submitLeaveAction`이 결과를 이 함수에 넣어 돌려준다(`grep -c "projectActionResult(" "app/(app)/approvals/actions.ts" "app/(app)/leave/actions.ts"`이 파일마다 1 이상 — Task 2 뒤에는 2 이상) · 두 `actions.registry.ts`의 해당 등록 `dtoName`이 `ApprovalActionResultDto`다
    - (CXF2-B-RF03) `test/e2e/leave-approval.spec.ts`의 `seoulToday()` 호출 수가 `test(` 수 이하다(`[ "$(grep -c 'seoulToday()' test/e2e/leave-approval.spec.ts)" -le "$(grep -cE '^\s*test\(' test/e2e/leave-approval.spec.ts)" ]` — 테스트마다 맨 앞 한 번)
    - (Codex HIGH 06) `test/e2e/leave-approval.spec.ts`가 `leaveWeekdayRange`를 쓰고 날짜 리터럴이 없다(`grep -cE '"20[0-9]{2}-[0-9]{2}-[0-9]{2}"' test/e2e/leave-approval.spec.ts`가 0) · `test/e2e/leave-dates.ts`에 `@playwright` import가 없다
    - (Codex MEDIUM · ENG-19) `test/unit/action-registry-completeness.test.ts`가 있고 녹색이며, 열거가 `actions.ts` 파일명으로 시작한다(`grep -c '"actions.ts"' test/unit/action-registry-completeness.test.ts`이 1 이상) · 예외 목록이 `app/(app)/account/actions.ts` 한 항목뿐이다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>직원이 화면에서 연차를 신청하고 결재자들이 결재함에서 차례로 승인해 최종 승인까지 가는 흐름이 E2E로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 반려 · 회수 · 다시 신청 + 연차 쓰기 권한의 도메인 강제 — 도메인 · 액션 · 통합 증명(전이와 로그 한 트랜잭션)</name>
  <files>domain/approvals/index.ts, domain/approvals/kinds.ts, domain/leave/resubmit.ts, domain/leave/access.ts, domain/leave/index.ts, domain/leave/balance-service.ts, repositories/approvals.ts, repositories/leave-requests.ts, app/(app)/approvals/actions.ts, app/(app)/approvals/actions.registry.ts, app/(app)/leave/actions.ts, app/(app)/leave/actions.registry.ts, test/integration/approvals-lifecycle.test.ts, test/integration/leave-permission.test.ts, test/integration/approvals-route-fixed.test.ts</files>
  <read_first>
    - `domain/approvals/index.ts` · `domain/approvals/route.ts` · `repositories/approvals.ts`(04.1-01 산출물 전문)
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 204·214·216–217행(반려 사유 막힘 · 토스트 · 반려/회수 결과 줄) · 492–513행(Assumptions #6·#7)
    - `docs/inputs/phase-05-approval.md` §4 — 반려 사유 자유 입력 필수, 기안자에게 복귀 → 고쳐 재상신
    - `domain/leave/index.ts` · `domain/leave/days.ts`(04.1-01) — 다시 신청 때 같은 검증을 재사용
    - `domain/leave/access.ts`(04.1-01 — `canSeeLeaveDocument`) · `domain/leave/balance-service.ts`(04.1-03 — `previewLeaveBalance`) · `domain/people/index.ts`의 `registerPerson` 첫 줄(`can` → `ForbiddenError` 선례) · `lib/actions/client.ts` 20–40행(`authedActionClient`는 로그인·크기만 본다)
    - `domain/approvals/tx-log.ts`(04.1-01 — `loadActionLogGate` · `recordActionInTx`) · `test/integration/approvals-route-fixed.test.ts`의 「행동 로그 실패 주입」 describe(주입 모양) · describe 「담당 소멸 뒤 고아 최종 — 막힘으로 다룬다(ENG-3 · D2)」와 그 안의 도우미 `setupOrphanFinal()`(04.1-01 — X-2)
    - `domain/permissions/roles.ts`의 `createRole` · `domain/permissions/matrix.ts`의 `setPermissionCell` — 권한 없는 계급을 테스트에서 만드는 방법
  </read_first>
  <behavior>
    - 1단 후보가 사유 `현장 일정 겹침`으로 반려하면 상태 `rejected`, 1단 행에 `action = rejected` · 사유 · 처리자 · 시각이 있고, 기안자의 결재함이 아니라 기안자 문서가 `반려`다
    - 사유가 빈 문자열·공백뿐·501자면 반려가 거부되고 상태는 그대로다
    - 기안자가 `submitted`·`in_review`에서 회수하면 `withdrawn`이고 이미 승인한 단계 기록은 남는다. `approved`에서 회수하면 `최종 승인됨 · 새로 고침`으로 거부된다. 기안자가 아닌 사람의 회수는 거부된다
    - 반려된 문서를 기안자가 날짜를 고쳐 다시 신청하면 번호가 같고 차수 2의 단계 행이 새로 생기며 차수 1의 반려 기록이 남는다. 그사이 설정의 2단 계급을 바꿨다면 차수 2에만 새 계급이 들어간다
    - `rejected`가 아닌 문서의 다시 신청, 기안자가 아닌 사람의 다시 신청은 거부된다
    - (CX-B1 — 사건별 종결 검사, `approvals-lifecycle.test.ts`) 반려된 문서(`rejected`)에서 기안자가 **지금 version**으로 `resubmitLeave` → 공용 전이의 (2)를 통과해 성공(차수 2 · version + 1 · 상태 `submitted`). 같은 반려 문서에 지금 version으로 기안자의 `withdrawDocument` · 반려한 1단 팀장의 `approveDocument` · `rejectDocument`(유효한 사유) → 셋 다 거부 · 상태 `rejected` · version · 단계 기록 수 · 차수 수 그대로(문구 갈래는 Task 3 사례가 단언). 승인된 문서(`approved`)와 회수된 문서(`withdrawn`)에서 기안자의 지금 version `resubmitLeave` → 거부 · 차수 2 없음
    - 행동 로그에 `document_reject`·`document_withdraw`·`document_submit`(detail.round = 2)이 남는다
    - (CEO-8) 1단이 지금 단계인 문서에서 기안자 · 무관한 기획 PM · 대표(4단 전)가 각각 맞는 version과 유효한 사유로 `rejectDocument`를 부르면 셋 다 `NotCurrentHolderError`, 단계 기록 0건, `version` 그대로, 상태 그대로다
    - (CEO-18) 위 세 반려 거부와 비기안자 회수 거부마다 `log.info("approval.refused", …)`가 사유 코드(`not_holder` · `not_drafter`)와 함께 한 번씩 불리고 필드에 반려 사유 글자가 없다
    - (통합, ENG-3 · D2 · X-1 · X-2 「고아 최종 문서의 기안자 회수 성공」 — `approvals-route-fixed.test.ts`의 04.1-01 describe 「담당 소멸 뒤 고아 최종 — 막힘으로 다룬다(ENG-3 · D2)」 안, 픽스처 `setupOrphanFinal()` 재사용) 대표가 1단을 승인하고 2·3단 담당이 전부 보관된 `in_review` 문서에서: 대표가 맞는 version과 유효한 사유로 `rejectDocument` → `NotCurrentHolderError`(행동 전 walkRoute가 막힘 — 후보 0명) · 단계 기록 · version 그대로. 기안자의 `getApprovalView` 가능 행동이 정확히 `[회수]`다(대표의 것은 빈 목록 — X-5). 이어 기안자가 맞는 version으로 `withdrawDocument` → 성공 · 상태 `withdrawn` · version + 1 · 1단 대표 승인 기록이 남음 · `document_withdraw` 로그 1건
    - (통합, X-5 — `approvals-lifecycle.test.ts`) 기안자(지금 담당 아님)의 `getApprovalView` 가능 행동: `submitted` · `in_review` → `[회수]` · `rejected` → `[다시 신청]` · `withdrawn` · `approved` → 빈 목록. 같은 문서의 1단 후보는 `submitted`에서 `[승인, 반려]`이고 `[회수]`가 없다
    - (통합, CXF-B-F01 — 기안자 = 지금 담당, `approvals-lifecycle.test.ts`, 바꾼 결재선 설정은 `afterEach`에서 원래 값으로 되돌린다) ① 팀장 본인 승인(01 결정표 W5): `self_approval` = `self_approve`로 저장한 뒤 팀장이 낸 문서(`submitted`, 1단 후보 = 기안자 단독)에서 기안자의 가능 행동이 정확히 `[승인, 회수]`(이 순서 — 1차 · 2차)이고 `반려`가 없다. 기안자의 `rejectDocument`(맞는 version · 유효한 사유) → `NotCurrentHolderError` · reason `not_holder` · 상태 · version · 단계 기록 그대로. 기안자의 `approveDocument` → 성공(1단 `self_approved`) 뒤 가능 행동이 `[회수]`. 같은 조건의 두 번째 문서에서 기안자의 `withdrawDocument` → 성공 · `withdrawn`. ② 대표 폴백 기안자(01 W8 — 04.1-01 픽스처 「1~3단 빈 자리 + 기안자 대표」, 대표 계급 재직자 = 기안자 한 명): 가능 행동이 정확히 `[승인, 회수]`이고 `반려`가 없다 · 기안자 `rejectDocument` → `NotCurrentHolderError` · 기안자 `approveDocument` → 성공 · 같은 조건의 두 번째 문서에서 기안자 `withdrawDocument` → 성공
    - (원자성, Codex HIGH) 반려 · 회수 · 다시 신청 각각에 `deps.appendActionLog`가 예외를 던지게 주입하면 호출이 실패하고 인스턴스 상태 · `version` · 단계 행 · 차수 행 · 신청 칸 · `action_log` 행 수가 호출 전과 같다
    - (통합, B-A1 — `approvals-lifecycle.test.ts`, 바꾼 노출 설정은 `afterEach`에서 되돌린다) 1단 결재자(팀장) 계급의 `approval.value` 노출을 기존 노출 설정 도메인 함수로 끈 뒤 팀장의 `approveDocument` 결과를 `projectActionResult(팀장, …)`에 넣은 값(= `approveAction` 반환값)의 `JSON.stringify`에 2단 담당 이름이 없고 `documentId` · `final: false`는 있다. 같은 조건의 `rejectDocument` 결과에는 기안자 이름이 없다. 마지막 결재자 계급의 `leave.value`를 끄면 최종 승인 결과에 차감 일수(`deductedDays`)가 없고 `final: true`는 있다. 노출을 켜 둔 대조 사례에서는 이름 · 일수가 있다(검출기 — 공허한 통과 방지)
    - (권한, Codex HIGH 02 — `leave-permission.test.ts`) `createRole`로 만든 권한 행 없는 계급의 사용자가 `submitLeave` · `previewLeaveBalance`를 직접 부르면 둘 다 `ForbiddenError`이고 `leave_requests` · `approval_instances` 행 수와 번호 카운터가 그대로다. 같은 사람이 `leave` write를 가진 동안 낸 신청이 반려된 뒤 `setPermissionCell`로 write를 끄면 `resubmitLeave`가 `ForbiddenError`이고 차수 2가 생기지 않는다. 그 상태에서도 자기 진행 중 신청의 `withdrawDocument`는 성공한다(계획 가정 4). (CX-W1) 같은 반려 문서에서 기안자의 `getApprovalView` 가능 행동이 write를 끄기 전에는 `[다시 신청]`, 끈 뒤에는 빈 목록이고, 같은 사람의 진행 중 문서는 끈 뒤에도 `[회수]`다. write를 다시 켜면 반려 문서가 다시 `[다시 신청]`이다
  </behavior>
  <action>
① `domain/approvals/index.ts`에 `rejectDocument(viewer, {instanceId, expectedVersion, reason})` — 사유를 trim해 1~500자 검증(UI-SPEC 문구 `사유 없음 · 한 줄 적어 주세요` / `사유가 500자를 넘음 · 줄여 주세요`) → 아래 공용 전이 함수(트랜잭션 안에서 walkRoute로 viewer가 후보인지 재판정 → `nextStep(…, "reject")` → version 조건 갱신 → 그다음 단계에 반려 기록) → 같은 tx 안 행동 로그. `withdrawDocument(viewer, {instanceId, expectedVersion})` — 기안자만, `nextStep(…, "withdraw")`가 허용하는 상태에서만. `resubmitDocument(viewer, prepared, {instanceId, expectedVersion}, tx)` — 기안자만, `rejected`에서만, 트랜잭션 **전에** `prepareSubmission`으로 다시 읽은 결재선(다시 신청 시점 설정 · 소속)으로 `current_round + 1` 차수와 단계 행을 만들고 `nextStep(…, "resubmit")`. 세 함수 모두 04.1-01의 submit·approve와 같은 트랜잭션·잠금·로그 모양을 쓴다 — 복붙한 세 벌이 아니라 내부 공용 전이 함수 하나(`approveDocument`도 이 함수로 옮긴다)가 **고정 순서(CEO-6)**를 지킨다: 트랜잭션 **전에** 필요한 읽기(스냅숏 · 다시 신청이면 `loadRouteConfig()`와 기안자의 그날 소속 — 04.1-01의 `prepareSubmission` 재사용, CEO-2) → 트랜잭션 안에서 `tx`로 인스턴스·차수·단계 읽기 → (1) version 불일치면 충돌 갈래로 간다(후보·기안자 판정보다 먼저 — CEO-6 진 쪽 문구 유지). 이 갈래의 오류 종류는 **관련자 판정으로 가른다(ENG-6 · D1)** — 관련자면 상세 충돌 오류, 아니면 `NotCurrentHolderError`(`지금 담당이 아님 · 새로 고침`) + reason `not_holder`. 이 task에서는 갈래 자리를 한 함수(`refuseStale(…)` 같은 내부 함수 하나)로 만들어 두고, 관련자 판정 `isApprovalParty`와 상세 문구 조립은 Task 3 ①②가 그 함수 안에 채운다(Task 2 시점의 통합 테스트는 불일치 갈래의 문구를 단언하지 않는다) → (2) **사건별 종결 검사(CX-B1)** — `approved` · `withdrawn`은 모든 사건(승인 · 반려 · 회수 · 다시 신청)에 종결이라 거부, `rejected`는 **기안자의** `resubmit`만 빼고 종결이라 승인 · 반려 · 회수와 기안자가 아닌 사람의 다시 신청은 여기서 거부하고, 기안자의 `resubmit`만 (3)으로 보낸다(반려에서 나가는 유일한 전이가 `nextStep("rejected", "resubmit")`). 검사는 「(상태, 사건, viewer가 기안자인가) → 종결인가」 한 함수로 두고 `nextStep` 허용 표와 같은 표에서 읽는다 — 상태만 보고 「종결이면 거부」하는 무조건 검사를 두지 않는다(그러면 다시 신청이 전부 여기서 막힌다). 거부 문구 갈래(관련자 판정)는 Task 3 ② (2)가 채운다 → (3) 후보(승인·반려 — 반려 후보는 기안자를 뺀다, CXF-B-F01) 또는 기안자(회수·다시 신청) 판정 → (4) 결과를 메모리에서 계산하고 `nextStep`. **walkRoute는 04.1-01의 필수 입력 `at`을 늘 넘긴다(X-1)**: 네 전이의 후보 판정(3)과 관련자 재료 `currentHolderIds`는 트랜잭션 전 스냅숏으로 부른 `at: "before_action"` 결과이고, `at: "after_approval"`은 승인의 (4) 결과 계산 한 곳뿐이다(다시 신청의 새 차수 결재선은 04.1-01 `prepareSubmission`의 `before_action` 그대로). 행동 전 계산의 고아 최종은 막힘과 같아(D2) 반려 후보가 없고 `NotCurrentHolderError`다 — 회수는 기안자 판정만이라 막힘 · 고아 최종에서도 된다 → (5) **상태 UPDATE 먼저**(version 조건, 0행이면 다시 읽어 충돌 오류) → (6) 단계 기록 · 폴백 행 · 새 차수와 단계 행 쓰기 → (7) **같은 tx 안에서** `recordActionInTx(viewer, entry, tx, gate, deps)`로 행동 로그(켜짐 여부 `gate`는 트랜잭션 전에 `loadActionLogGate()`로 읽는다 — 04.1-01 규약, Codex HIGH 원자성). 커밋 뒤 풀 연결로 `recordAction`을 부르는 갈래를 두지 않는다(04.1-01 금지 항목). 거부 갈래는 던지기 직전 `log.info("approval.refused", {instanceId, viewerId, reason, expectedVersion, actualVersion})`(reason = `conflict` · `not_holder` · `final` · `not_drafter` · `invalid_state`, 사유 글자 없음 — CEO-18. version 불일치인데 관련자가 아니면 `conflict`가 아니라 `not_holder` — ENG-6). 이 순서라 다시 신청 두 건이 겹쳐도 두 번째는 UPDATE 0행으로 멈추고 `UNIQUE(instance_id, round)` 위반이 나지 않는다.

② `domain/leave/resubmit.ts`의 `resubmitLeave(viewer, {leaveId, expectedVersion, input}, deps?)` — `countLeaveQuarters`로 다시 검증(회계연도 판정의 「오늘」은 `seoulToday(deps.now)` — CEO-11) → 트랜잭션 전 읽기(`prepareSubmission`) → 한 트랜잭션에서 신청 행 칸 갱신(번호 유지 — `repositories/leave-requests.ts`에 칸 갱신 함수 추가) + `resubmitDocument`. 이 파일을 `domain/leave/index.ts`에 연결하지 않는다(04.1-03과 파일을 나누기 위해 — 액션이 직접 import한다). 대신 이 파일 맨 위에서 `./index`를 side-effect로 import해 이 파일만 적재된 콜드 경로에서도 연차 종류가 등록되게 한다(CEO-3 — `index.ts`는 `resubmit.ts`를 import하지 않으므로 순환이 없다).

③ **연차 쓰기 권한의 도메인 강제(Codex HIGH 02)**: `domain/leave/access.ts` 끝에 `canWriteLeave(viewer, deps?)`(`can(viewer, "leave", "write")` 결과 그대로 — ④-b의 연차 종류 `canResubmit`도 이 함수, CX-W1)와 `assertLeaveWrite(viewer, deps?)`(`canWriteLeave`가 거짓이면 `ForbiddenError("연차 신청 권한이 없습니다.")` — `registerPerson` 첫 줄과 같은 모양, `deps.can` 주입)를 더하고 `submitLeave`(`domain/leave/index.ts`) · `resubmitLeave`(`resubmit.ts`) · `previewLeaveBalance`(`balance-service.ts`)의 **첫 줄**에서 부른다 — 어떤 읽기·트랜잭션보다 먼저라 거부되면 행이 생기지 않는다. `domain/leave/index.ts` · `balance-service.ts`에서 고치는 것은 import 한 줄과 호출 한 줄뿐이다(`index.ts`만 연차 종류 등록의 `canResubmit: canWriteLeave` 한 줄을 더 가진다 — ④-b · CX-W1). `withdrawDocument`는 기안자 판정만, 승인·반려는 후보 판정만 한다(계획 가정 4 · 금지 항목). 04.1-01 · 04.1-03의 기존 통합 테스트가 기안자 계급에 `leave` write를 주지 않아 새 판정에 걸리면, 그 테스트의 준비 단계에 `seedApprovalsLeave()`(계급 5종 × leave view·write) 호출을 더하는 것만 허용한다 — 단언은 바꾸지 않는다. `test/integration/leave-permission.test.ts`가 behavior의 권한 줄을 단언한다.

④ 액션: `rejectAction`(approvals) · `withdrawLeaveAction` · `resubmitLeaveAction`(leave)을 더하고 각 `actions.registry.ts`에 등록한다(메뉴 `approvals`·동작 `approve` / 메뉴 `leave`·동작 `write`). `rejectAction` · `resubmitLeaveAction`은 Task 1 ④의 `projectActionResult`를 지난 값을 돌려주고 등록 `dtoName`은 `ApprovalActionResultDto`다(B-A1 — `withdrawLeaveAction`은 이름 · 일수 없는 `회수 · 결재 멈춤`이라 투영할 필드가 없다). `resubmitLeaveAction`이 있는 `app/(app)/leave/actions.ts`도 `document-kinds`를 import한다(가드 테스트 조건). 화면 버튼은 04.1-05가 확인 모달과 함께 붙인다 — 이 task의 사용자 가치는 통합 테스트로 증명되는 서버 동작이다.

④-b **기안자 행동을 결재선 보기에 더한다(X-5)**: `domain/approvals/index.ts`의 `getApprovalView`가 돌려주는 가능 행동 목록에 기안자 갈래를 더한다. **기안자가 지금 담당이기도 하면(본인 승인 W5 · 대표 폴백 기안자 W8 — 04.1-01 결정표) 목록은 두 자격의 합집합에서 `[승인, 회수]`(이 순서 — 승인이 1차, 회수가 2차)이고 기안자에게 `반려`는 주지 않는다**(회수가 같은 목적 — 오케스트레이터 결정, CXF-B-F01). 가능 행동 계산은 `domain/approvals/index.ts` 안 한 함수(`possibleActions(…)` 같은 내부 함수)로 두어 04.1-05의 `listMyInbox` 상세(결재함 PC 행 · 폰 결재 시트)도 같은 함수 결과를 싣는다(CXF2-B-RF01). 서버 판정도 같다: 공용 전이 (3)의 반려 후보 판정은 기안자를 뺀다(기안자의 `rejectDocument`는 `NotCurrentHolderError` · reason `not_holder`), 승인 후보 판정은 01 결정표 그대로다 — viewer가 기안자이고 상태가 `submitted` · `in_review`면 `회수`(walkRoute 결과가 막힘 · 행동 전 고아 최종이어도 — D2), `rejected`면 `다시 신청` — 단 **종류의 다시 신청 권한이 있을 때만(CX-W1)**: `domain/approvals/kinds.ts`의 종류 정의에 선택 필드 `canResubmit?: (viewer: Viewer) => Promise<boolean>`를 더하고, `getApprovalView`는 이 필드가 있고 참일 때만 `다시 신청`을 넣는다(없는 종류 — 04.1-01 `test_memo` — 는 `다시 신청` 없음). 연차 종류는 `domain/leave/index.ts`의 등록에 `canResubmit: canWriteLeave`를 넘긴다 — `canWriteLeave`는 ③의 `assertLeaveWrite`가 부르는 같은 판정 함수라 목록과 `resubmitLeave` 첫 줄이 한 함수로 정해진다(`leave` write가 거둬진 기안자는 반려 문서에서 가능 행동이 빈 목록이고, 진행 중 문서의 `회수`는 권한과 무관하게 그대로 — 계획 가정 4). 판정은 `withdrawDocument` · `resubmitDocument`의 기안자 · 상태 판정과 같은 규칙(같은 `nextStep` 허용 표)을 쓴다 — 목록과 서버 판정이 따로 놀지 않게. `withdrawn`은 끝 상태라 `다시 신청`이 없다(truth 「회수는 끝 상태」 · `resubmitDocument`는 `rejected`에서만). 04.1-05는 이 목록만 그린다.

⑤ `test/integration/approvals-lifecycle.test.ts`가 behavior의 반려·회수·다시 신청 줄 전부(비후보 반려 세 사례 · 로그 스파이 · 로그 실패 주입 세 사례 포함)를 단언한다. 단 「고아 최종 문서의 기안자 회수 성공」 사례만은 `approvals-route-fixed.test.ts`의 04.1-01 describe 「담당 소멸 뒤 고아 최종 — 막힘으로 다룬다(ENG-3 · D2)」 안에 `setupOrphanFinal()`로 더한다 — 픽스처를 복사하지 않고 그 describe의 기존 단언은 고치지 않는다(X-2 — 한 곳).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-lifecycle.test.ts test/integration/leave-permission.test.ts test/integration/approvals-route-fixed.test.ts test/integration/leave-balance.test.ts test/integration/leak-scan.test.ts test/integration/approvals-concurrency.test.ts test/integration/approvals-inbox-projection.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 다시 신청 뒤 차수 1 기록이 사라지거나 번호가 바뀌거나, 비후보 반려가 기록을 남기거나 version을 바꾸거나, 로그 실패 주입 뒤 상태·version·행이 바뀌어 있거나, 권한 없는 직접 호출이 성공하거나 행을 남기거나, (B-A1) 노출을 끈 결재자의 투영 결과 JSON에 다음 담당 · 기안자 이름이나 차감 일수가 있거나 켠 대조 사례에 없거나, 새 판정 · `approveDocument`의 공용 전이 이동 때문에 04.1-01 · 04.1-03 통합 테스트(01의 동시 제출 · 승인 경주 · 결재함 투영 — B-R1)가 회귀하면 여기서 깨진다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/document-kinds-import.test.ts test/unit/import-cycles.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>`resubmitLeaveAction`을 부르는 app 파일이 `document-kinds`를 import하지 않았거나, `resubmit.ts` ↔ `index.ts` 순환이 생겼거나, eslint 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - 반려·회수·다시 신청이 각각 행동 로그 한 건을 남긴다는 단언과, 로그 실패 주입 시 세 동작 모두 아무 행도 바뀌지 않는다는 단언이 있다(Codex HIGH 원자성)
    - (B-A1) `grep -c "projectActionResult(" "app/(app)/approvals/actions.ts" "app/(app)/leave/actions.ts"`이 파일마다 2 이상이고, `approvals-lifecycle.test.ts`에 `approval.value` · `leave.value`를 끈 결재자의 투영 결과에 이름 · 일수가 없는 사례와 켠 대조 사례가 있다
    - `domain/approvals/index.ts`에서 `recordAction(` 직접 호출이 0건이고 `recordActionInTx`만 쓴다(`grep -v '^\s*//' domain/approvals/index.ts | grep -c "recordAction("`이 0)
    - (Codex HIGH 02) `assertLeaveWrite`가 `submitLeave` · `resubmitLeave` · `previewLeaveBalance` 세 함수 본문의 첫 문장이다 · `leave-permission.test.ts`의 직접 호출 거부 · 행 0건 단언이 녹색이다
    - 사유 검증(빈 값 · 공백 · 501자)이 서버에서 거부된다는 단언이 있다
    - `approved`에서 회수 거부 단언이 있다
    - (D2 · X-2) `approvals-route-fixed.test.ts`의 describe 「담당 소멸 뒤 고아 최종 — 막힘으로 다룬다(ENG-3 · D2)」 안에 `setupOrphanFinal()`을 부르는 「기안자 회수 성공」 사례(대표 반려 `NotCurrentHolderError` · 기안자 회수 `withdrawn` · 1단 기록 보존)가 있다 · 도우미 정의는 한 곳이다(`grep -rlE "(function|const) setupOrphanFinal" test/integration | wc -l`이 1 — 픽스처 복사 없음)
    - (X-5) 기안자 가능 행동 사례(`[회수]` · `[다시 신청]` · 빈 목록)와 X-2 사례의 기안자 `[회수]` 단언이 있다
    - (CXF-B-F01) 기안자 = 지금 담당 두 사례(팀장 본인 승인 W5 · 대표 폴백 기안자 W8)가 가능 행동 `[승인, 회수]` 정확 일치 · 기안자 반려 거부 · 승인 성공 · 회수 성공을 단언한다
    - (X-1) `domain/approvals/index.ts`의 모든 `walkRoute(` 호출에 `at:`이 있고 `"after_approval"`은 승인 결과 계산 한 곳뿐이다 — SUMMARY에 호출 지점과 `at` 값을 표로 적는다
    - 다시 신청 뒤 번호 동일 · 차수 2 · 차수 1 반려 사유 보존 · 설정 변경이 차수 2에만 반영이 단언된다
    - (CX-B1) 공용 전이 함수의 종결 검사가 사건을 입력으로 받는다 — `rejected`에서 기안자의 지금 version `resubmitLeave` 성공 사례와, 같은 `rejected`에서 승인 · 반려 · 회수 거부(행 불변) 사례 · `approved` · `withdrawn`의 다시 신청 거부 사례가 `approvals-lifecycle.test.ts`에 있다. SUMMARY의 (1)~(6) 순서 표에 (2)의 (상태, 사건) 종결 표를 적는다
    - `domain/leave/index.ts` · `domain/leave/balance-service.ts`의 `. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" && git diff "$PLAN_BASE"..HEAD` 변경이 `assertLeaveWrite` import 한 줄과 호출 한 줄뿐이다(`index.ts`는 여기에 `canWriteLeave` import와 등록의 `canResubmit: canWriteLeave` 한 줄만 더 — CX-W1)
    - (CX-W1) `domain/leave/access.ts`의 `assertLeaveWrite` 본문이 `canWriteLeave(`를 부르고 `can(viewer, "leave", "write"` 직접 호출은 `canWriteLeave` 안 한 곳뿐이다(`grep -v '^\s*//' domain/leave/access.ts | grep -c 'can(viewer, "leave", "write"'`이 1) · `leave-permission.test.ts`에 write 끈 뒤 반려 문서 가능 행동 빈 목록 · 진행 중 문서 `[회수]` 유지 단언이 있다
    - 비후보 반려 세 사례가 각각 단계 기록 0건 · version 불변을 단언한다(CEO-8)
    - `domain/approvals/index.ts`에 전이 순서를 한 곳에서 지키는 공용 함수가 있고 approve · reject · withdraw · resubmit이 모두 그것을 부른다 — SUMMARY에 함수 이름과 (1)~(6) 순서를 적는다(CEO-6)
    - `domain/leave/resubmit.ts` 첫 import 묶음에 `./index` side-effect import가 있다(CEO-3)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>반려(사유 필수) · 회수(최종 전만) · 다시 신청(번호 유지 · 새 차수 고정)이 전이와 로그 한 트랜잭션으로 서버에서 동작하고, `leave` write 없는 사용자의 직접 호출이 도메인에서 거부된다는 것이 통합 테스트로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 동시 조작 — 두 순서 모두 하나만 성공하고 진 쪽은 누가 · 언제 · 무엇을 했는지 받는다</name>
  <files>domain/approvals/conflict-message.ts, domain/approvals/index.ts, test/unit/domain/approvals/conflict-message.test.ts, test/integration/approvals-concurrency.test.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 205행(거부 — 동시 처리 문구 원문 다섯)
    - `.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md` 224–238행(Pattern 3) · 335–344행(낙관적 잠금 코드 예)
    - `test/integration/quote-lines-conflict.test.ts` — 같은 행을 두 요청이 잡는 통합 테스트 모양(두 순서 · `Promise.all` 경주)
    - `test/integration/document-counters-concurrency.test.ts` — 동시 실행 경주의 결정적 단언 모양
  </read_first>
  <behavior>
    - (단위) `buildConflictMessage({status: "withdrawn", actorName: "박서연", at})` → `박서연이 14:01에 회수함 · 새로 고침`, 승인 `이수아가 14:01에 승인함 · 새로 고침`, 반려 `김팀장이 14:01에 반려함 · 새로 고침`, 최종 승인 상태에서 회수 시도 `최종 승인됨 · 새로 고침`, 후보 아님 `지금 담당이 아님 · 새로 고침`, (B-C1) 다시 신청 뒤 지금 상태 `{status: "submitted", round: 2, actorName: "박서연", at}` → 정확히 `박서연이 14:01에 다시 신청함 · 새로 고침`(같은 입력에 `round: 1` → `지금 담당이 아님 · 새로 고침`). 받침 없는 이름은 `가`, 있는 이름은 `이`, 한글이 아닌 끝 글자는 `이(가)` 없이 `이`로 고정(규칙 한 줄 주석)
    - (통합) 순서 A: 결재자가 version 1로 승인 → 기안자가 같은 version 1로 회수 → 회수는 `…승인함 · 새로 고침`으로 거부, 상태 `in_review`
    - (통합) 순서 B: 기안자가 version 1로 회수 → 결재자가 version 1로 승인 → 승인은 `…회수함 · 새로 고침`으로 거부, 상태 `withdrawn`, 단계 처리 기록 0건
    - (통합) 승인↔반려도 같은 두 순서
    - (통합) `Promise.all`로 승인과 회수를 동시에 20번 반복해 매번 정확히 하나만 성공하고 단계 기록이 1건 이하
    - (통합) 같은 단계 후보 둘이 동시에 승인하면 하나만 기록되고 다른 쪽은 `{이름}이/가 HH:MM에 승인함 · 새로 고침`
    - (통합) 같은 사람이 같은 version으로 승인을 두 번 보내면 두 번째가 충돌이고 단계 기록은 1건
    - (통합, CEO-6) 순서 B의 진 쪽 문구가 정확히 `{기안자}이/가 HH:MM에 회수함 · 새로 고침`이다 — `지금 담당이 아님`이 아니다(진 쪽 결재자는 지금 차수 담당이라 관련자이고, 관련자에게는 version 확인이 후보 판정보다 먼저다 — ENG-6 · D1. 관련자가 아닌 사람의 불일치는 아래 사례처럼 `지금 담당이 아님`)
    - (단위, ENG-6 · D1) `isApprovalParty(viewerId, {drafterId, actedByIds, currentHolderIds})`: 기안자 → true · 차수 1에서만 처리한 사람(차수 2 진행 중) → true · 지금 차수 단계의 담당 → true · 이전 차수에서 담당이었으나 처리하지 않았고 지금 차수 담당이 아닌 사람 → false · 무관한 사람 → false · 빈 집합들 + 무관한 사람 → false
    - (통합, ENG-6 · D1 — 무관한 사용자의 옛 version 호출) 1단 팀장이 승인해 version이 2가 된 문서에서, 이 결재선의 어느 단계 담당도 아니고 처리한 적도 없는 로그인 기획 PM이 인스턴스 id와 옛 version 1로 `approveDocument` · `rejectDocument`(유효한 사유) · `withdrawDocument`를 각각 부르면: 셋 다 `NotCurrentHolderError`이고 `message`가 정확히 `지금 담당이 아님 · 새로 고침`이며, 잡은 오류를 `handleServerError`에 넣은 반환 문자열도 같은 값이다. 메시지에 팀장 이름 · `/\d{2}:\d{2}/` · `승인함` · `반려함` · `회수함` · `최종 승인됨`이 없다. `log.info("approval.refused", …)`가 reason `not_holder`로 불리고 상태 · version · 단계 기록 수가 그대로다. (CX-W4) 네 번째 전이도 공개 진입점으로 같은 describe에서 단언한다 — 같은 기획 PM(`seedApprovalsLeave()`로 `leave` write 있음 — `assertLeaveWrite`를 통과해 전이 갈래까지 간다)이 유효한 다시 신청 입력(다른 평일 기간)과 옛 version 1로 `resubmitLeave({leaveId, expectedVersion: 1, input})`를 부르면 `NotCurrentHolderError` · 정확히 `지금 담당이 아님 · 새로 고침` · `handleServerError` 통과 뒤 같은 값 · 이름/`HH:MM`/상태 낱말 없음 · reason `not_holder`이고, 신청 행의 칸(종류 · 시작일 · 종료일 · 단위 · 비고) · 번호 · `current_round` · `approval_routes` 차수 수 · version이 호출 전과 같다. 같은 문서가 최종 승인된 뒤(`approved`)에 같은 사람이 옛 version으로 불러도, **지금 version**으로 불러도 `최종 승인됨`이 아니라 `지금 담당이 아님 · 새로 고침`이다(기안자가 지금 version으로 회수하면 기존대로 `최종 승인됨 · 새로 고침`)
    - (통합, ENG-6 · D1 — 관련자는 상세 문구) 같은 문서에서 옛 version 1로: 기안자의 `approveDocument` → `팀장이름이/가 HH:MM에 승인함 · 새로 고침` · 1단 팀장(처리자)의 두 번째 승인 → 같은 상세 문구 · 2단 본부 책임자(지금 담당)의 승인 → 같은 상세 문구(세 부류가 각각 상세 문구를 받는다)
    - (통합, ENG-6 · D1 · X-3 — 폴백 후보는 관련자) 1단 팀장 × 기안자 팀 · 2·3단 켜짐이나 담당 없음 · 4단 꺼짐 · 대표 계급 재직자 둘인 문서에서 팀장이 승인해 version 2가 되고 지금 자리가 대표 폴백(폴백 단계 행 0개)일 때: 대표 한 명이 옛 version 1로 `approveDocument` → `ApprovalConflictError` `{팀장}이/가 HH:MM에 승인함 · 새로 고침`(상세 문구 — 결재선 단계 행에 대표가 없어도 지금 폴백 후보라 관련자) · 단계 기록 · version 그대로 · reason `conflict`. 같은 문서에 무관한 기획 PM이 옛 version 1로 부르면 `지금 담당이 아님 · 새로 고침` · reason `not_holder`
    - (통합, CEO-6 폴백 경합) 1~3단이 빈 자리이고 대표 계급 두 명이 폴백 후보인 문서에서 두 대표가 같은 version으로 `Promise.all` 승인 → 정확히 하나 성공, 다른 쪽은 `{먼저 승인한 대표}이/가 HH:MM에 승인함 · 새로 고침`(원시 오류·23505 아님), `is_fallback` 단계 행 1개
    - (통합, CEO-6 다시 신청 중복 · B-C1) 반려된 문서에 기안자가 같은 version으로 `resubmitLeave`를 `Promise.all` 두 번 → 정확히 하나 성공, 다른 쪽은 `ApprovalConflictError` 정확히 `{기안자}이/가 HH:MM에 다시 신청함 · 새로 고침`(진 쪽도 기안자라 관련자 — (1) 불일치 · (5) 0행 어느 갈래로 져도 같은 문구), 차수 2의 `approval_routes` 행 1개
    - (통합, CX-B1 — 반려 상태의 사건별 종결 문구) 1단 팀장이 반려한 문서(`rejected`)에서 **지금 version**으로: 기안자의 `withdrawDocument` · 반려한 팀장의 `approveDocument` · `rejectDocument`(유효한 사유) → 각각 `ApprovalConflictError` 정확히 `{팀장}이/가 HH:MM에 반려함 · 새로 고침`(관련자 — 상태 문구) · reason `final`. 무관한 기획 PM(`leave` write 있음)의 `approveDocument` · `rejectDocument` · `withdrawDocument` · `resubmitLeave`(유효한 입력) → 넷 다 정확히 `지금 담당이 아님 · 새로 고침` · reason `not_holder` · 메시지에 팀장 이름 · `/\d{2}:\d{2}/` · `반려함`이 없다. 모든 거부 뒤 상태 `rejected` · version · 단계 기록 수 · 차수 수 · 신청 칸이 그대로다. 이어 기안자가 같은 지금 version으로 `resubmitLeave` → 성공(차수 2 — 종결 예외는 기안자의 `resubmit` 하나)
  </behavior>
  <action>
① `domain/approvals/conflict-message.ts`의 `buildConflictMessage` — 순수 함수. 입력은 현재 상태 · 지금 차수 · 마지막으로 바꾼 사람 이름 · 바꾼 시각(서울 시간 `HH:MM`) · 시도한 동작. 상태 → 문구는 상태 타입 전체를 다루는 `switch`이고 `default`는 `never` 검사로 끝난다 — 새 상태가 생기면 컴파일 오류다(B-C1): `in_review` → `{이름}이/가 HH:MM에 승인함` · `approved` → `최종 승인됨` · `rejected` → `{이름}이/가 HH:MM에 반려함` · `withdrawn` → `{이름}이/가 HH:MM에 회수함` · `submitted`이고 차수 > 1 → `{기안자}이/가 HH:MM에 다시 신청함`(다시 신청이 이긴 뒤의 지금 상태 — `updated_by` = 기안자. UI-SPEC 205행에 없는 새 문구 하나다) · `submitted`이고 차수 1 → `지금 담당이 아님`(제출 직후라 옛 version이 없어 정상 흐름에서 오지 않는다 — 문구를 새로 만들지 않는다), 모두 뒤에 ` · 새로 고침`. 조사 판정은 마지막 글자의 유니코드 한글 음절 받침으로 한다(작은 헬퍼 하나, 이 파일 안). 같은 파일에 순수 함수 `isApprovalParty(viewerId, {drafterId, actedByIds, currentHolderIds})`(ENG-6 · D1 — 기안자 · 모든 차수 `acted_by` · 지금 차수 단계들의 지금 해석한 담당 집합(기안자 제외 전 담당) 중 하나면 true)를 둔다. 입력 집합은 호출자가 만든다 — `actedByIds`는 트랜잭션 안에서 `tx`로 읽은 전 차수 단계 행, `currentHolderIds`는 트랜잭션 전에 읽은 스냅숏으로 부른 04.1-01 `walkRoute(… at: "before_action")` 결과 그대로(X-1) — 지금 차수 단계마다 해석한 담당의 합집합에, 지금 자리가 대표 폴백이면 폴백 단계 행이 아직 없어도 폴백 후보(대표 계급 재직자 전원)를 더한 것(후보 판정과 같은 해석 — X-3). 호출자가 합집합을 따로 만들지 않는다.

② `domain/approvals/index.ts`의 공용 전이 함수에서 오류 문구를 이렇게 가른다(ENG-6 · D1 — `lib/actions/handle-server-error.ts:24`가 `UserFacingError` 문구를 그대로 화면에 보내므로 관련자가 아닌 사람에게 가는 오류에는 이름·시각·상태가 들어가면 안 된다):
- **(1) version 불일치(Task 2의 갈래 자리)**: `isApprovalParty`가 true면 인스턴스와 `updated_by` 사람 이름을 읽어 `buildConflictMessage`로 만든 `ApprovalConflictError`(UserFacingError 하위), false면 `NotCurrentHolderError`(`지금 담당이 아님 · 새로 고침`) + `log.info("approval.refused", {…, reason: "not_holder"})`. 종결 상태여도 관련자가 아니면 `최종 승인됨`이 아니라 이 일반 문구다(상태도 새지 않게). 승인 · 반려 · 회수 · 다시 신청 네 전이가 모두 이 한 갈래를 지난다.
- **(5) version 조건 UPDATE 0행**: 이 지점의 viewer는 (3) 후보·기안자 판정을 통과했으므로 관련자다 — 다시 읽은 행으로 상세 `ApprovalConflictError`.
- **(2) 종결 상태(version 일치)**: 종결 판정은 사건별이다(CX-B1 — Task 2 ① (2)): `approved` · `withdrawn`은 모든 사건에, `rejected`는 기안자의 `resubmit`을 뺀 모든 사건(승인 · 반려 · 회수 · 기안자 아닌 사람의 다시 신청)에 종결이다. 기안자의 `rejected` → `resubmit`만 이 갈래에 오지 않고 (3)으로 간다. 종결로 걸린 경우 version은 작은 정수라 맞힐 수 있으므로 같은 규칙을 건다 — 관련자면 지금 상태의 `buildConflictMessage` 문구(`approved` → `최종 승인됨 · 새로 고침` 등 기존대로 · `rejected` → `{반려자}이/가 HH:MM에 반려함 · 새로 고침` · `withdrawn` → `{기안자}이/가 HH:MM에 회수함 · 새로 고침` — 모두 UI-SPEC 205행 기존 문구, 새 문구 없음), 아니면 `NotCurrentHolderError` + `not_holder`(ENG-6 — 상태도 관련자에게만. 04.1-01 ④ (2)와 같은 규칙 — X-4).
- 후보 재판정(3)에서 떨어진 경우는 `지금 담당이 아님 · 새로 고침`.
- **재시도 코드를 두지 않는다**(금지 항목).

③ 트랜잭션 격리: 판정(walkRoute)과 갱신이 한 트랜잭션 안에 있고 갱신은 version 조건 UPDATE 한 문장이다 — 두 트랜잭션이 같은 version을 읽고 둘 다 판정을 통과해도 UPDATE는 하나만 1행을 얻는다. 단계 처리 기록·폴백 행·새 차수 행은 **반드시 그 UPDATE가 1행을 얻은 뒤에만** 쓴다(Task 2의 고정 순서 (5)→(6) — CEO-6). 「단계를 먼저 쓰고 0행이면 롤백」 갈래는 쓰지 않는다: 그 갈래에서는 두 경쟁자가 폴백 행·새 차수 행을 먼저 INSERT해 UNIQUE 위반(23505)이 원시 오류로 새기 때문이다. 진 쪽은 UPDATE에서 행 잠금을 기다렸다가 0행을 받고, 다시 읽은 행으로 충돌 문구를 만든다.

④ `test/unit/domain/approvals/conflict-message.test.ts`와 `test/integration/approvals-concurrency.test.ts`(04.1-01이 만든 파일 — 동시 제출 describe 아래에 describe를 더한다)가 behavior를 단언한다. 경주 테스트는 결정적이어야 한다 — 「정확히 하나 성공」과 「기록 1건 이하 / 폴백 행 1개 / 차수 2 한 행」만 단언하고 어느 쪽이 이기는지는 단언하지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/approvals/conflict-message.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 "No test files found"가 나온다 — `isApprovalParty` 여섯 사례 중 하나라도 틀리거나(ENG-6), 다시 신청 뒤 상태(`submitted` · 차수 2)의 문구가 정확히 `박서연이 14:01에 다시 신청함 · 새로 고침`이 아니거나 `undefined` · 다른 동사가 나오면(B-C1) 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-concurrency.test.ts test/integration/approvals-lifecycle.test.ts</automated>
    <fails_when>무관한 사용자의 옛 version 호출(다시 신청 포함 — CX-W4)이 이름·시각·상태가 담긴 문구를 받거나 신청 칸 · 차수를 바꾸거나(ENG-6), 반려 문서에서 기안자의 지금 version 다시 신청이 종결 검사에 막히거나 승인 · 반려 · 회수가 통과하면(CX-B1), 두 순서 중 하나라도 진 쪽이 성공하거나 문구가 다르거나, 경주 20회 중 한 번이라도 둘 다 성공하거나 단계 기록이 2건이 되거나, 폴백 경합·다시 신청 중복에서 23505 원시 오류가 나거나 폴백 행·차수 2 행이 2개가 되거나, 다시 신청 중복의 진 쪽 문구가 정확히 `{기안자}이/가 HH:MM에 다시 신청함 · 새로 고침`이 아니면(B-C1) failed가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm test</automated>
    <fails_when>세 계층(단위 → 통합 → E2E) 중 어느 하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 승인↔회수 · 승인↔반려 각각 두 순서(총 네 사례)가 통합 테스트에 있다
    - 경주 반복 테스트와 후보 둘 동시 승인 테스트가 있다
    - 대표 폴백 경합(폴백 행 1개)과 다시 신청 중복(차수 2 한 행) 테스트가 있다(CEO-6)
    - (CX-B1) 반려 문서의 지금 version 사례 — 관련자 셋(기안자 회수 · 팀장 승인 · 팀장 반려)의 `…반려함 · 새로 고침` 정확 일치 · 무관한 PM 네 전이의 `지금 담당이 아님 · 새로 고침` · 이어서 기안자 다시 신청 성공 — 가 `approvals-concurrency.test.ts`에 있다
    - `grep -rn --exclude=error.tsx "retry" domain/approvals app/\(app\)/approvals app/\(app\)/leave`가 0건이다(자동 재시도 금지 검사 — 04.1-05 · 04.1-06의 `error.tsx`가 쓰는 Next 16.3 오류 경계 표준 인자 `retry`는 사용자가 누르는 `다시 시도`라 대상이 아니어서 뺀다, B-C3)
    - 충돌 문구 다섯 모양이 UI-SPEC 205행 원문과 글자 단위로 같다(단위 테스트가 문자열 전체를 비교) · 여섯째 `{기안자}이/가 HH:MM에 다시 신청함 · 새로 고침`(B-C1 — UI-SPEC에 없는 새 문구)도 단위와 다시 신청 중복 통합에서 문자열 전체를 비교한다 · `buildConflictMessage`의 상태 분기가 `never` 검사로 끝난다(`grep -c "never" domain/approvals/conflict-message.ts`이 1 이상)
    - (ENG-6 · D1 · CX-W4) 무관한 사용자의 옛 version 승인 · 반려 · 회수 · 다시 신청(`resubmitLeave` 공개 진입점 — 신청 칸 · 차수 불변 포함) 네 사례가 `지금 담당이 아님 · 새로 고침` 정확 일치 · `handleServerError` 통과 뒤 같은 값 · 이름/`HH:MM`/상태 낱말 부재 · reason `not_holder`를 단언하고, 관련자 네 부류(기안자 · 처리자 · 지금 담당 · 단계 행 없는 지금 폴백 후보 — X-3)의 상세 문구 사례가 있다 · `isApprovalParty` 단위 여섯 사례가 있다
    - (ENG-6) `domain/approvals/index.ts`에서 `buildConflictMessage(` 호출이 `isApprovalParty` 판정 갈래 안에만 있다 — SUMMARY에 두 판정 지점((1) 불일치 · (2) 종결)과 (5) 0행 지점을 적는다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>동시 조작이 두 순서·경주 모두에서 한 쪽만 성공하고, 진 쪽이 서버 값으로 만든 한 줄을 받는다는 것이 통합 테스트로 증명된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 폼 → 서버 액션 | 인스턴스 id · version · 사유 · 연차 칸이 클라이언트에서 온다 |
| 세션 사용자 → 결재 권한 | 메뉴 권한이 아니라 결재선 후보 여부가 권한이다 |
| 기안자 → 회수·다시 신청 | 기안자만 할 수 있고 상태 제약이 있다 |
| 두 사람의 동시 요청 → 한 문서 상태 | 같은 version을 본 두 요청이 겹친다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-08 | Elevation of Privilege | `rejectAction` · `approveAction` | high | mitigate | 매 요청 트랜잭션 안에서 후보 재판정. 비후보 세 명(기안자 · 무관한 PM · 차례 전 대표)의 맞는-version 반려 거부 · 기록 0건 · version 불변을 Task 2 통합 테스트가 단언한다(CEO-8 — 승인 쪽은 04.1-01 Task 3) |
| T-04.1-09 | Elevation of Privilege | `withdrawLeaveAction` · `resubmitLeaveAction` | high | mitigate | 도메인이 기안자 id와 viewer를 비교하고 `nextStep`이 상태를 제한한다. 비기안자·잘못된 상태 거부 단언 |
| T-04.1-10 | Tampering | 동시 승인·회수·반려·다시 신청 | high | mitigate | 고정 순서(version → 종결(사건별 — `rejected`는 기안자의 다시 신청만 통과, CX-B1) → 후보/기안자 → UPDATE 먼저 → 단계·폴백·차수 행). 두 순서 · 경주 20회 · 후보 둘 동시 승인 · 대표 폴백 경합(폴백 행 1개) · 다시 신청 중복(차수 2 한 행) 통합 테스트(CEO-6) |
| T-04.1-11 | Tampering / DoS | 반려 사유 · 비고 입력 | medium | mitigate | zod와 도메인 양쪽에서 trim · 1~500자(사유) · 500자(비고). React 기본 이스케이프만 쓰고 `dangerouslySetInnerHTML` 0건 |
| T-04.1-12 | Information Disclosure | `/leave/[id]` 직접 URL | high | mitigate | `getLeave`가 기안자·후보·처리자 외에는 null → `notFound()`. E2E가 차례 아닌 사람의 404를 단언한다 |
| T-04.1-12b | Information Disclosure | 충돌·종결 거부 문구(`ApprovalConflictError` → `handleServerError` → 화면) | high | mitigate | (ENG-6 · D1) 상세 문구(처리자 이름 · 시각 · 상태)는 `isApprovalParty`가 true인 관련자(기안자 · 모든 차수 처리자 · 지금 차수 담당 · 지금 폴백 후보 — X-3)에게만. 그 밖은 version 불일치든 종결 상태든 `지금 담당이 아님 · 새로 고침` + reason `not_holder`. 무관한 사용자의 옛 version · 지금 version 호출 통합 테스트가 문구 정확 일치와 이름·`HH:MM`·상태 낱말 부재를 `handleServerError` 통과 뒤 값으로 단언한다(Task 3) |
| T-04.1-12c | Information Disclosure | 네 액션 반환값(토스트 재료 — 다음 담당 이름 · 기안자 이름 · 차감 일수) | medium | mitigate | (B-A1) `projectActionResult`가 이름 · 일수를 등록 DTO `ApprovalActionResultDto`(`approval.value` · `leave.value`)로 `project()`한 뒤에만 돌려준다 — 미리보기 · 상세의 CX-R3 투영을 액션 반환 경로가 우회하지 않는다. 노출을 끈 결재자의 투영 결과에 이름 · 일수가 없음 + 켠 대조 통합 사례(Task 2) · 네 액션의 호출 grep 수락 기준 |
| T-04.1-13 | Elevation of Privilege | `/leave/new` · `submitLeaveAction` · `resubmitLeaveAction` · 미리보기 | high | mitigate | 페이지 `can(leave, write)` 게이트 + 도메인 첫 줄 `assertLeaveWrite`(`submitLeave` · `resubmitLeave` · `previewLeaveBalance`) — `authedActionClient`는 로그인만 보므로 도메인이 막는다. 권한 없는 계급의 직접 호출 거부 · 행 0건 통합 단언(`leave-permission.test.ts`, Codex HIGH 02) |
| T-04.1-57 | Information Disclosure | 등록되지 않은 새 서버 액션 | medium | mitigate | `action-registry-completeness` 단위 가드 — `app/**/actions.ts` 전부에서 출발해 액션 이름 ⊆ 옆 등록 이름, 등록 파일 없는 액션 파일은 이유가 적힌 예외 목록(`account/actions.ts` 하나)과 정확히 같을 때만 통과. 빠지면 누수 스캔이 그 액션을 조용히 건너뛰는 틈을 닫는다(Codex MEDIUM · ENG-19) |
| T-04.1-14 | Repudiation | 반려·회수·다시 신청 | high | mitigate | 같은 tx 안 `recordActionInTx` 세 종류 — 커밋된 전이에는 로그가 반드시 있고 로그 실패는 전이를 롤백한다(로그 실패 주입 세 사례, Codex HIGH 원자성). 단계 행의 처리자·시각·사유가 두 번째 기록 |
| T-04.1-15 | Spoofing | 서버 액션 CSRF | low | accept | Next 서버 액션의 기본 Origin 검사와 세션 쿠키 SameSite(Phase 1 설정)를 그대로 쓴다 — 이 플랜에서 바꾸지 않는다 |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test` 세 계층 전부 통과
- 통합 3개(lifecycle · concurrency · leave-permission) · 단위 4개(conflict-message · document-kinds-import · action-registry-completeness · e2e/leave-dates) · E2E 1개가 존재하고 녹색
- `. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- db/ ui/ domain/rules domain/action-log ui/next-turn` 비어 있음
- `. "$(git rev-parse --git-dir)/gsd-04.1-02.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음
- 독립 DOM 감사(별도 에이전트, `CI=true`, 폭 1280 · 1024 · 375)가 `/leave/new` · `/leave/[id]` · `/approvals`에서 가로 스크롤 0을 실측
</verification>

<success_criteria>
- 신청 → 결재함 → 승인 → 최종 승인이 화면에서 끝까지 된다
- 반려 · 회수 · 다시 신청이 서버에서 규칙대로 동작한다
- 동시 조작이 두 순서 모두 한 쪽만 성공하고 진 쪽이 무슨 일이 있었는지 받는다 — 그 상세는 관련자만 받고, 무관한 사용자는 옛 version으로 불러도 `지금 담당이 아님 · 새로 고침`만 받는다(ENG-6)
- 새 액션이 전부 누수 스캔과 행동 로그에 들어갔고, 로그는 전이와 한 트랜잭션이다
- `leave` write 없는 사용자는 액션을 직접 불러도 신청 · 다시 신청 · 미리보기를 할 수 없다
- E2E 날짜가 실행하는 날과 무관하게 유효하다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 화면 | `/leave/new`(첫 형태) · `/leave/[id]`(머리 + KvList) · `/approvals`(내 결재 · 처리함 · PC 승인) |
| 서버 액션 | `submitLeaveAction` · `withdrawLeaveAction` · `resubmitLeaveAction` · `approveAction` · `rejectAction`(+ 두 `actions.registry.ts`) |
| 함수 | `rejectDocument` · `withdrawDocument` · `resubmitDocument` · `resubmitLeave` · `projectActionResult` + DTO `ApprovalActionResultDto`(`domain/approvals/dto.ts` — 액션 반환값 투영, B-A1) · `buildConflictMessage` · `isApprovalParty`(관련자 판정 — ENG-6) · `leaveStatusDisplay` · 공용 전이 함수(고정 순서 · 같은 tx 로그, CEO-6) · `assertLeaveWrite`(`domain/leave/access.ts`) · `leaveWeekdayRange`(`test/e2e/leave-dates.ts`) |
| 종류 등록 | `app/(app)/document-kinds.ts`(결재 모듈을 부르는 모든 app 진입점이 import, CEO-3) |
| 운영 로그 | `approval.refused` 사유 코드 `not_drafter` · `invalid_state` 추가(CEO-18) |
| 메뉴·권한 | `leave` 메뉴 키 · 계급 5종 view·write 기본 허용 |
| 디자인 문서 | DECISIONS.md A2 기록 · SYSTEM.md §7-5 · §1-2 개정 |
| 테스트 | `test/integration/approvals-lifecycle.test.ts` · `test/integration/approvals-concurrency.test.ts`(04.1-01 파일에 describe 추가) · `test/integration/leave-permission.test.ts` · `test/unit/domain/approvals/conflict-message.test.ts` · `test/unit/document-kinds-import.test.ts` · `test/unit/action-registry-completeness.test.ts` · `test/unit/e2e/leave-dates.test.ts` · `test/e2e/leave-approval.spec.ts` |
| 변경된 기존 심볼 | `MENUS`(끝에 leave) · 결재함 자리표시 페이지 → 개인 결재함 · `approveDocument`(공용 전이 함수로 이동) · `submitLeave` · `previewLeaveBalance`(첫 줄 `assertLeaveWrite`) |


## Review Dispositions Ledger

### Round 1 — 1b86370

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| `leave:write`가 페이지에만 있고 Server Action에는 없음(L69@1b86370) | HIGH | Plan 02, Task 2 ③: `assertLeaveWrite`를 `submitLeave` · `resubmitLeave` · `previewLeaveBalance` 첫 줄에서 · `leave-permission.test.ts` 직접 호출 거부 + 행 0건 · 새 truth · T-04.1-13 상향. 회수는 기안자 판정만(계획 가정 4), 승인·반려는 후보 판정(기존 금지 항목) |
| 반려·회수·재신청도 커밋 뒤 로그(L71@1b86370) | HIGH | Plan 02, Task 2 ①: 공용 전이 함수 (7) 같은 tx `recordActionInTx` + 트랜잭션 전 `loadActionLogGate` · 로그 실패 주입 세 사례 · `recordAction(` 직접 호출 0건 수락 기준 · T-04.1-14 상향 |
| 누수 스캔이 「모든 액션이 등록됨」을 증명하지 않음(L70@1b86370) | MEDIUM | Plan 02, Task 1 ⑩: `test/unit/action-registry-completeness.test.ts`(`actions.ts` 액션 이름 ⊆ 등록 이름 · 검출기 사례) · T-04.1-57 |
| E2E 날짜가 연말에 불가능(L187@1b86370 — 04.1-06 지적이지만 헬퍼는 이 플랜이 먼저 만든다) | HIGH | Plan 02, Task 1 ⑦⑨: `test/e2e/leave-dates.ts` `leaveWeekdayRange`(그해 3월 첫 월요일 + 주 오프셋, 해를 넘지 않음) + 단위 테스트(1월 1일 · 6월 30일 · 12월 31일 · 윤년) · 이 플랜 E2E의 날짜 리터럴 0건. 04.1-05 · 04.1-06 E2E가 같은 헬퍼를 쓴다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Codex 지적은 전부 반영했다 |

### Round 2 — plan-eng-review 2026-09-24(`docs/designs/plant8-erp-phase04.1-eng-review-260924.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-6 충돌 문구가 권한 판정보다 먼저 나감(`handle-server-error.ts:24`가 `UserFacingError` 문구를 그대로 보냄) | BLOCKER | 공통 정의 D1: 관련자(기안자 · 모든 차수 처리자 · 지금 차수 담당)만 상세 문구, 그 밖은 `지금 담당이 아님 · 새로 고침` + `not_holder`. version 불일치 · 종결 상태 두 지점에 `isApprovalParty`. 새 truth · Task 2 ① (1) · Task 3 ①② · CEO-6 순서 B 문구 유지(진 쪽은 관련자) · 무관한 사용자 옛/지금 version 통합 · 관련자 세 부류 통합 · `isApprovalParty` 단위 · T-04.1-12b |
| ENG-19 누수 스캔 열거가 registry에서 시작 | MEDIUM | Task 1 ⑩: `app/**/actions.ts`에서 출발 · 등록 파일 없는 파일은 예외 목록(`account/actions.ts` → `changePasswordAction`)과 정확히 같을 때만 · 검출기 (a)~(d) · T-04.1-57 |

### Round 3 — plan-eng-review 2회차 2026-09-25 — 4078bd8(`docs/designs/plant8-erp-phase04.1-eng-review2-260925.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B-A1(T7) 액션 반환값(다음 담당 이름 · 기안자 이름 · 차감 일수)이 `approval.value`/`leave.value` 투영을 거치지 않아 CX-R3로 막은 누수를 다시 연다 — 누수 스캔은 dtoName 등록만 검사(`leak-scan.test.ts:138-140`) | 경고 | Plan 02, Task 1 ④: `domain/approvals/dto.ts`에 DTO `ApprovalActionResultDto` + `projectActionResult`(이름 · 일수만 투영, 구조 값은 그대로) · 네 액션이 이 함수를 지나 반환 · 등록 dtoName · 빠진 조각을 뺀 토스트 문구 다섯. Task 2 ④ · behavior 통합(노출 끈 결재자 → 반환 JSON에 이름 · 일수 없음 + 켠 대조) · verify fails_when · 수락 기준 grep · 새 truth · key_link · T-04.1-12c. 04.1-05 truth(토스트)에 줄인 문구 한 줄 |
| B-A3(T10) document-kinds 가드가 `import type` · `"use client"`까지 잡아 클라이언트 번들에 서버 코드를 끌어올 위험 | P2 | Plan 02, Task 1 ⑧: (a) 검출 정규식이 `import type` 제외(`import-cycles.test.ts` 관례) · (d) `"use client"` 파일은 (a) 건너뛰고 런타임 import 0건 단언 · 순수 판정 함수 + 검출기 네 사례(클라이언트 타입 import 오탐 없음 포함) · truth · artifact · verify fails_when · 수락 기준 |
| B-C1(T11) `buildConflictMessage`에 다시 신청 뒤 상태(`submitted` · 차수 > 1) 문구가 없음 | P2 | Plan 02, Task 3 ①: `never` 검사로 끝나는 상태 전수 `switch` · `{기안자}이/가 HH:MM에 다시 신청함 · 새로 고침` · 차수 1 `submitted`는 기존 일반 문구. behavior 단위 정확 일치 + 다시 신청 중복 통합 정확 일치 · truth(CEO-6) · verify fails_when 둘 · 수락 기준. 새 문구의 UI-SPEC 205행 메모는 cross-notes-B.md로 넘김 |
| B-C5(P3) 참조 브랜치 `origin/claude/gsd-progress-e1nzgu`가 로컬에 없음 | P3 | Plan 02, Task 1 read_first: `git fetch origin claude/gsd-progress-e1nzgu` 먼저(실측 `git branch -r`) |
| B-R1(P3) `approveDocument`를 공용 전이로 옮기는 Task 2 verify에 01의 동시성 · 결재함 투영 테스트가 없음 | P3 | Plan 02, Task 2 verify 통합 명령에 `approvals-concurrency.test.ts` · `approvals-inbox-projection.test.ts` 추가 · fails_when 문구 |
| B-C3(P3, 04.1-05) 오류 경계가 `retry`로 바뀌면 이 플랜 Task 3의 자동 재시도 금지 grep과 부딪힘 | P3 | Plan 02, Task 3 수락 기준 grep에 `--exclude=error.tsx` — 금지 대상(자동 재시도)과 Next 표준 오류 경계 인자를 가른다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-C2(P3) 차감 일수 출처가 종류 경계 밖 | 설계 선택(종류 `describeDocuments` 요약에서 가져오기)이라 한 문장 사실 교정이 아니다 — 실행자가 SUMMARY에 출처를 적고, 종류 분기가 생기면 금지 항목(종류 이름 분기)이 lint · 리뷰에서 잡는다. Phase 5 지출결의 합류 때 다시 본다 |
| B-C6(P3) `resubmitLeave`가 판정 전에 신청 칸을 쓰고 막힘 오류가 관련자 판정보다 먼저 나감 | 결과는 롤백으로 맞고(confidence 5), 순서 재배치는 새 테스트가 필요하다 — 이번 회차 P3는 한 문장 교정만 반영 |
| B-C7(P3) E2E 픽스처 이름이 모두 `E2E Employee`라 이름 단언이 공허 | 새 픽스처 코드가 필요하다 — 04.1-06의 C-07(고유 이름 `registerPerson`) 처리와 함께 볼 후보로 남긴다 |
| B-C8(P3) 승인 뒤 결재함 리렌더 방법 미기재 | `revalidatePath` vs 04.1-05 `router.refresh()` 중 한 곳을 고르는 설계 선택 — 04.1-05가 `router.refresh()`로 정해 두었고 E2E(`처리함`으로 옮겨 감)가 결과를 단언한다 |
| B-T5(P3) `/leave/new` 404 게이트를 grep으로만 확인 | 새 E2E 사례가 필요하다 — 도메인 거부는 `leave-permission.test.ts`가 증명한다. 04.1-06 E2E와 합칠 후보 |
| B-P2(P3) `leave` 메뉴 추가로 매 요청 권한 조회 1회 증가 | 무시 가능 수준(리뷰 판정) — 기록만 |
| Codex B-NEW03(참고) 본문과 승인 `version`의 동일 스냅숏 계약 없음 | Codex 자체 참고 · 트랜잭션 안 version 재검증이 데이터 손상을 막는다. 조회 구현이 아직 없어 이번 회차에 강제하지 않는다 |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-02-SUMMARY.md` when done
</output>
