---
phase: 04.1-approvals-leave
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - db/schema/auth.ts
  - db/schema/leave.ts
  - db/migrations/meta/_journal.json
  - repositories/users.ts
  - repositories/org-snapshot.ts
  - repositories/leave-adjustments.ts
  - repositories/leave-usage.ts
  - domain/leave/balance.ts
  - domain/leave/balance-service.ts
  - domain/leave/dto.ts
  - domain/people/index.ts
  - domain/settings/keys.ts
  - test/unit/domain/leave/balance.test.ts
  - test/unit/domain/leave/first-year-accrual.test.ts
  - test/integration/leave-balance.test.ts
  - test/integration/people.test.ts
autonomous: true
requirements: [LEAV-01]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "잔고는 저장하지 않고 매번 계산한다 — 부여(grant) 목록과 최종 승인된 신청·진행 중 신청·조정 기록에서 순수 함수가 만든다. 같은 문서를 몇 번 조회하든, 최종 승인 요청이 두 번 오든 이중 차감이 없다(LEAV-01 idempotency)"
    - "잔고의 기본 명사는 「부여(grant) — 출처 · 일수 · 유효 시작일 · 소멸일」이다. 회계연도 연차와 입사 첫해 월차는 같은 명사의 두 출처이고, 조정도 출처 하나다. 차감은 신청 시작일에 유효한 부여 중 소멸이 가장 빠른 것부터, 같으면 월차부터 한다(D-96 · 05-CONTEXT Discretion · assumption-delta promote)"
    - "입사일이 있는 사람은 입사일 + k개월(k = 1..11)마다 월차 1일이 적립되고(근속 첫 1년 기준 — 12월 31일에서 자르지 않는다, 사용자 결정 2026-09-24 11:42), 적립일은 그달에 같은 날이 없으면 그달 말일이다(1월 31일 입사 → 2월 28/29일). 월차는 전부 입사 다음 회계연도 12월 31일에 소멸한다(D-96 · LEAV-01 boundary)"
    - "퇴직일이 있으면 월차 적립은 퇴직일까지만이다 — 적립일 ≤ 퇴직일인 적립만 세고, 퇴직일 뒤에는 `asOf`가 늦어도 월차가 쌓이지 않는다(D-97 · Codex HIGH). `buildLeaveGrants`가 `resignationDate`를 입력으로 받는다"
    - "입사 다음 해처럼 월차와 회계연도 연차가 함께 남아 있어도 두 잔고는 언제나 **따로** 보인다 — 잔고 DTO 두 명세(본인·관리자용 · 신청용) 어디에도 월차 + 연차 합계 필드가 없고, 잔고 문자열(잔고 줄 · 잔고 행 · 퇴직 줄) 어디에도 둘을 더한 `남음`이 없다(사용자 요구 2026-09-24 11:43 · D-96 「회계연도 연차와는 별도 잔고로 보인다」)"
    - "회계연도 Y의 연차 부여는 순수 함수 `annualGrantQuarters({fiscalYear, hireDate, resignationDate, annualDays})` 한 곳이 정한다(annualDays = 이력형 설정 `leave.annual_days`의 Y-01-01 시점 값): 입사 연도 H 이하 → 0(입사 첫해는 월차만) · Y = H+1 → 근무 기간 비례 = 0.25일 단위 올림 `ceil(annualDays×4 × 근무일수(H) / H의 날수)` 쿼터(근무일수 = 입사일 ~ H-12-31 양끝 포함, H의 날수 365·366 — 1월 1일 입사면 전액, 10월 1일 평년 입사 → 92일 → 16쿼터 = 4일) · Y ≥ H+2 → 전액(사용자 결정 2026-09-24 21:45 KST 카드 B — R1). 퇴직일이 있으면 퇴직 연도 다음 해부터 연차 부여가 없다(ENG-4 — 입사 연도 안 퇴직이면 H+1 비례 부여도 없다). 입사일이 없으면 연차는 매년 전액이고 월차 줄은 `월차 계산 불가 · 입사일 없음`이다"
    - "이월은 없다 — 회계연도 Y의 남은 연차는 Y+1 잔고에 더해지지 않는다. `leave.annual_days`는 이력형이고, 04.1-04의 공통 검증 `validateEffectiveFrom`(형식 `YYYY-01-01` · 적용 연도 ≥ `seoulToday()`의 연도)이 일반 저장(`addHistorizedValue`)과 설정 JSON 가져오기(`importSettings`) **두 경로 모두**에서 지난 연도 행 추가를 막으므로 지난 연도 잔고가 소급해 바뀌지 않는다. 지난 연도 정정은 연차 조정(`addLeaveAdjustment`, `fiscalYear` = 그 연도)으로만 한다(ENG-5 · RESEARCH Pitfall 4 · LEAV-01 — 가드 구현·회귀 테스트는 04.1-04, 이 플랜은 Y-01-01 asOf 읽기 쪽)"
    - "종일 1 · 반차 0.5 · 반반차 0.25는 신청의 `days_quarters`로 차감되고 재택(0)은 기록만 된다. 계산은 끝까지 정수 1/4일이다 — 소수 오차가 없다(LEAV-01 precision)"
    - "잔여를 넘는 신청은 막지 않는다 — 연차 남음이 음수가 될 수 있고, 결재 중 신청과 이번 신청을 배분 규칙대로 나눴을 때 유효한 부여를 넘는 만큼이 `잔여 초과 N일` 경고 재료로 DTO에 실린다(LEAV-01 · 입력 §5). 판정에 두 잔고를 더한 값을 화면·DTO에 싣지 않는다"
    - "「남음」의 정의는 하나다(ENG-12 · D3): 연차·월차 각 줄의 남음 = 부여(+조정) − 사용(최종 승인됨) − 결재 중(진행 중 신청의 그 버킷 배분분). 잔고 줄 `연차 15일 · 사용 3.5일 · 결재 중 1일 · 남음 10.5일`의 10.5 = 15 − 3.5 − 1이다. 잔고 행에서는 `결재 중`이 이 문서를 뺀 다른 진행 중 신청이므로 행의 남음도 이 문서를 빼지 않은 값이고 `이번 신청`은 따로 보인다. 요약 객체 · DTO · 문자열이 같은 정의를 쓰고, 한 픽스처(D3 픽스처)가 DTO 값과 문자열을 같이 단언한다"
    - "월차 줄(ENG-11 · D4)은 조회 기준일이 근속 첫 1년(입사일 ~ 입사일+1년 전날) 안이거나 조회 연도에 월차 적립이 하나라도 있으면 존재하고, 값이 0이어도 `월차 남음 0일`(잔고 행) · `월차 적립 0일 · 사용 0일 · 결재 중 0일 · 남음 0일 · {소멸일} 소멸`(잔고 줄)로 보인다. 입사일이 없으면 `월차 계산 불가 · 입사일 없음`, 그 밖(근속 1년 지나고 그 해 적립 없음)이면 월차 줄이 없다(DTO `monthly` = null · `monthlyRemaining` = null). 12월 15일 입사자는 다음 해 1월 5일에 `월차 남음 0일`을 본다. 두 남음을 더한 숫자는 여전히 금지"
    - "잔고 행 1행은 `연차 남음 N일`(D3 정의) · 월차 줄이 있을 때(D4) `월차 남음 N일` · `결재 중 N일`(이 문서를 뺀 같은 사람의 다른 진행 중 신청 합) · `이번 신청 N일`이다 — 남은 일수는 잔고별 숫자이고 합계가 없다. 행의 두 남음은 다른 문서의 진행 중 신청(그 잔고 배분분)만 빼고 **이번 신청은 빼지 않는다** — 이번 신청은 `이번 신청`과 2행으로 따로 보인다(ENG-12 · D3). 입사일이 없으면 1행에 `월차 남음` 칸이 없다(`월차 계산 불가 · 입사일 없음`은 잔고 줄에만). 2행 `차감 예정 월차 N일 · 연차 N일`은 이 신청을 소멸 빠른 순(같은 소멸일이면 월차 먼저)으로 나눈 결과라 어느 잔고에서 빠지는지가 보인다 — 2행은 1행에 `월차 남음` 칸이 있을 때만 있고(UI-SPEC 「월차가 있을 때만」 = D4 월차 줄), 그때는 한쪽이 0이어도 두 칸을 다 쓴다(`차감 예정 월차 0일 · 연차 0.5일`). 신청용 DTO의 `over`는 늘 있는 정수 쿼터이고 초과가 없으면 0이며, 3행 `잔여 초과 N일`은 `over` > 0일 때만 있다(UI-SPEC Copywriting 잔고 행 · Assumptions #14 — 11:43 사용자 요구로 개정)"
    - "같은 사람의 신청 두 건이 동시에 최종 승인돼도 잔고는 두 신청 일수의 합만큼만 줄어든다 — 잔고 행이 없으니 잃어버린 갱신이 없다(LEAV-01 concurrency)"
    - "관리자는 사람마다 입사일·퇴직일을 넣고, 연차·월차 잔고를 부호 있는 0.25일 단위 + 필수 사유로 조정한다. 조정 기록은 추가만 된다 — 수정·삭제 함수가 리포지토리에 없다. 조정 행과 그 행동 로그는 한 트랜잭션이다(04.1-01 `recordActionInTx` — 로그가 실패하면 조정도 남지 않는다)(05-CONTEXT Discretion 「결근 차감은 관리자 수동 조정」 · Codex HIGH 원자성)"
    - "사람 등록의 입사일은 계정을 만들기 전에 형식을 검증하고, 계정 생성 뒤 입사일 저장이 인프라 오류로 실패하면 발령 실패와 같은 보상(방금 만든 계정 보관 + 안내 문구)을 탄다 — 입사일 없는 새 계정이 조용히 남지 않는다(Codex MEDIUM · `registerPerson`의 기존 「부분 상태 없음」 규약)"
    - "퇴직일이 있으면 잔고는 `퇴직 2026-10-31 · 남은 연차 3.5일 · 월차 0일` 한 줄로만 보이고 금액은 어디에도 계산하지 않는다(D-97). 잔고 읽기는 받은 연도를 그대로 쓴다 — 퇴직 줄은 **조회 연도 = 퇴직 연도**일 때만 그 해 요약으로 나오고, 다른 연도는 그 해의 보통 잔고(연차 줄 · D4 월차 줄)다(CXF2 — 04.1-06의 섹션 연도 한 값과 맞춘다). 퇴직 연도 다음 해의 연차 부여는 없다 — 퇴직자를 다음 해로 조회해도 새 연차 15일이 생기지 않고 연차 부여 0인 그 해 잔고가 보인다(ENG-4)"
    - "조정의 계산 계약(ENG-13): 연차 조정은 `fiscalYear` Y가 필수이고 입력 날짜와 무관하게 유효 Y-01-01 · 소멸 Y-12-31(그 해 연차 줄에만 붙는다 — 지난 연도 정정 경로). 월차 조정은 `fiscalYear`가 없고 유효 = max(입력한 날(서울), 입사일) · 소멸 = 월차 소멸일(입사 다음 해 12-31)이며, 입사일이 없거나 입력한 날이 월차 소멸일 뒤면 거부된다. 양수 조정은 그 창의 부여(출처 adjustment)다. 음수 조정은 같은 버킷 · 같은 창의 부여 합에서만 빠지는 차감 기록이고 다른 버킷으로 넘어가지 않는다 — 모자라면 그 버킷 남음이 음수가 된다(월차 `남음 -1일`도 그대로). 배분에서 음수 조정은 같은 날 신청보다 먼저 처리된다. 표시는 연차 조정 합이 연차 줄 `조정 N일`, 월차 조정 합이 `월차 적립`에 합산이다"
    - "(CX-R2) 관리자 화면의 조정 기록은 도메인 읽기 `listLeaveAdjustmentsForUser(viewer, { userId, fiscalYear })`로만 나간다 — 관리자 사람 상세 연차 섹션의 잔고와 같은 **보기** 권한 판정 `can(viewer, \"admin.people\", \"view\")`(`getLeaveBalanceForUser`와 같은 판정 · `/admin/people` 페이지 게이트와 같은 메뉴·동작, 없으면 `ForbiddenError`)을 첫 문장에서 하고 — 조정 쓰기 권한을 요구하지 않아 보기 권한만 있는 관리자도 잔고·기록을 본다(CX-R2 오케스트레이터 정정) — `LEAVE_ADJUSTMENT_DTO_SPEC`(`id` · `kind` · `quarters` · `reason` · `createdAt` · `createdByName`, 전부 `leave.value`)으로 `project()`한 `LeaveAdjustmentDTO[]`만 돌려준다. 명세는 `registerDto`로 등록되어 누수 스캔 생성기가 케이스를 만든다. 04.1-06 페이지는 이 함수만 부르고 `repositories/`를 import하지 않는다"
    - "(CX-B2) 잔고 조회 네 함수는 `deps.now`를 받고 「오늘」을 공개 입구의 `seoulToday(deps?.now)` 한 번에서만 얻는다 — 결재함 상세처럼 다른 모듈이 부르는 신청 잔고도 넘겨받은 `now` 기준으로 계산되어 주입한 날짜의 값과 같다"
    - "퇴직일이 지난 사람(퇴직일 < 오늘)은 결재선 후보 스냅숏에서 빠진다 — 퇴직일 당일까지는 후보다(오케스트레이터 결정 3의 「담당 소멸」 경로). 「오늘」·기본 회계연도·월차 적립 기준일은 `lib/dates.ts` `seoulToday()`의 서울 날짜다(CEO-11)"
    - "결재자·신청 미리보기가 받는 잔고 DTO(`LEAVE_REQUEST_BALANCE_DTO_SPEC`)에는 연차 남음 · 월차 남음(D4의 월차 줄이 없거나 입사일이 없으면 null — ENG-11) · 결재 중 · 이번 신청 · 차감 예정 · 초과만 있고 합계 남음 · 입사일 · 퇴직일 키가 없다 — 기안자의 발표 전 퇴직일이 결재자 RSC 페이로드로 새지 않는다. 입사일·퇴직일은 본인·관리자용 잔고 DTO에만 있다(CEO-9)"
    - "`balance-service.ts`는 문서 보임 규칙을 `domain/leave/access.ts`에서 가져오고 `domain/leave/index.ts`를 import하지 않는다 — 04.1-05가 `index.ts`에서 `balance-service.ts`를 불러도 순환이 없고 import-cycles 테스트가 이 플랜 끝에서 녹색이다(CEO-4)"
    - "잔고 문자열(잔고 줄 · 월차 줄 · 잔고 행 세 줄 · 퇴직 줄)은 `domain/leave/balance.ts`의 순수 함수가 UI-SPEC Copywriting 원문 그대로 만든다 — 화면 넷(S1 · S2 · S3/S5 · S9)이 같은 함수를 쓴다"
    - statement: "근속 첫 1년 밖이고 조회 연도에 월차 적립도 없으면 월차 줄을 그리지 않고(소멸일이 지난 다음 해가 이 경우다), 근속 첫 1년 안이면 적립 0이어도 `월차 남음 0일`로 그린다 — `—`로 채우지 않는다(UI-SPEC UI Considerations partial S1 월차 없음 · ENG-11 D4)"
      verification: backstop
  prohibitions:
    - statement: "사람별 잔고 숫자를 표에 저장해 두고 승인·조정 때 더하고 빼서는 안 된다. 저장된 잔고는 동시 승인에서 갱신을 잃고, 설정·입사일을 고쳤을 때 과거와 어긋나며, 이중 차감을 막을 장치를 따로 만들어야 한다"
      status: resolved
      verification: test
    - statement: "연차 일수를 부동소수로 계산해서는 안 된다(0.1 + 0.2 문제). 저장·계산·비교는 정수 1/4일이고 소수는 표시 직전에만 만든다"
      status: resolved
      verification: test
    - statement: "퇴직 정산 금액(연차 수당)을 계산하거나 보여서는 안 된다 — D-97이 범위 밖으로 잠갔다"
      status: resolved
      verification: test
    - statement: "월차 남음과 연차 남음을 더한 한 숫자(`남음 N일` 합계 · `remaining`/`total` 같은 합계 필드)를 DTO·문자열 함수·화면 어디에도 만들어서는 안 된다 — 입사 다음 해 직원에게 입사일 기준 월차와 회계연도 연차가 한 숫자로 합쳐 보이면 소멸일이 다른 두 잔고를 구분할 수 없다(사용자 요구 2026-09-24 11:43)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/leave/balance.ts"
      provides: "순수 함수 — 회계연도 연차 부여량(입사 다음 해 비례 R1 · 퇴직 다음 해 0 ENG-4) · 부여 목록 만들기 · 소멸 빠른 순 배분 · 요약 · 잔고 문자열"
      exports: ["annualGrantQuarters", "checkLeaveAdjustment", "buildLeaveGrants", "allocateLeave", "summarizeLeaveBalance", "formatBalanceLines", "formatBalanceRow", "formatResignationLine"]
    - path: "domain/leave/balance-service.ts"
      provides: "잔고 조회(본인 · 관리자 · 문서 결재자 — 전부 `deps.now` 주입, CX-B2) · 신청 미리보기 · 조정 추가 · 조정 기록 조회(`LeaveAdjustmentDTO[]`, CX-R2)"
      exports: ["getMyLeaveBalance", "getLeaveBalanceForUser", "getLeaveBalanceForRequest", "previewLeaveBalance", "addLeaveAdjustment", "listLeaveAdjustmentsForUser"]
    - path: "repositories/leave-adjustments.ts"
      provides: "조정 기록 추가·목록(행마다 작성자 이름 — CX-R2) — 수정·삭제 함수 없음"
      exports: ["insertLeaveAdjustment", "listLeaveAdjustments"]
    - path: "test/unit/domain/leave/first-year-accrual.test.ts"
      provides: "D-96 월차 적립일·말일 보정·최대 11일·소멸일 · 퇴직일 적립 중단 · 입사 다음 해 연차 비례 부여(R1 다섯 사례) · 퇴직 다음 해 연차 0(ENG-4) · 월차 0일 줄 표시(ENG-11) · 입사 다음 해 두 잔고 분리 증명"
      min_lines: 60
    - path: "test/integration/leave-balance.test.ts"
      provides: "이력형 연차 일수 · 이월 없음 · 월차부터 차감 · 초과 경고 · 조정(로그 실패 시 롤백 · 월차 조정 거부 두 경우 ENG-13) · 퇴직(퇴직 줄은 조회 연도 = 퇴직 연도일 때만 · 다른 연도는 보통 잔고 — ENG-4 · CXF2) · D3 픽스처 DTO·문자열 · 10월 입사자 다음 해 분리 잔고(연차 비례 4일 R1) 증명"
      min_lines: 80
  key_links:
    - from: "domain/leave/balance-service.ts"
      to: "domain/settings/keys.ts"
      via: "LEAVE_ANNUAL_DAYS를 회계연도 1월 1일 시점으로 getSettingValue asOf 조회"
      pattern: "LEAVE_ANNUAL_DAYS"
    - from: "domain/leave/balance-service.ts"
      to: "domain/leave/dto.ts"
      via: "04.1-01이 먼저 등록한 연차 잔고 DTO 명세(본인·관리자)로 project()"
      pattern: "LEAVE_BALANCE_DTO_SPEC"
    - from: "domain/leave/balance-service.ts"
      to: "domain/leave/dto.ts"
      via: "getLeaveBalanceForRequest · previewLeaveBalance는 입사일·퇴직일 없는 신청용 명세로 project()(CEO-9)"
      pattern: "LEAVE_REQUEST_BALANCE_DTO_SPEC"
    - from: "domain/leave/balance-service.ts"
      to: "domain/leave/dto.ts"
      via: "listLeaveAdjustmentsForUser가 조정 행을 LEAVE_ADJUSTMENT_DTO_SPEC으로 project() — 04.1-06 페이지의 유일한 조정 기록 출처(CX-R2)"
      pattern: "LEAVE_ADJUSTMENT_DTO_SPEC"
    - from: "domain/leave/balance-service.ts"
      to: "domain/leave/access.ts"
      via: "문서 보임 규칙 canSeeLeaveDocument — index.ts를 import하지 않아 순환이 없다(CEO-4)"
      pattern: "canSeeLeaveDocument"
    - from: "domain/leave/balance-service.ts"
      to: "domain/approvals/tx-log.ts"
      via: "addLeaveAdjustment가 조정 행과 행동 로그를 같은 tx에 쓴다(04.1-01 recordActionInTx — Codex HIGH 원자성)"
      pattern: "recordActionInTx"
    - from: "repositories/org-snapshot.ts"
      to: "db/schema/auth.ts"
      via: "퇴직일이 지난 사람을 후보에서 뺀다"
      pattern: "resignation"
---

<objective>
**As a** 연차를 쓰는 직원과 연차를 관리하는 경영관리 담당자, **I want to** 회계연도 연차와 입사 첫해 월차가 규칙대로 쌓이고 승인된 연차가 소멸이 빠른 것부터 빠지며, 관리자가 입사일·퇴직일·조정으로 예외를 바로잡기를, **so that** 잔고 숫자를 사람이 엑셀로 따로 맞추지 않아도 되고, 잔여를 넘는 신청은 막히지 않되 누구나 넘는다는 사실을 본다.

Task 1은 이 플랜의 **트레이서**다 — 입사일이 있는 직원의 월차 적립 → 연차 최종 승인 → 월차부터 차감 → 잔고 조회까지 스키마·리포지토리·도메인·설정을 관통해 통합 테스트로 증명한다. 잔고를 보여 주는 화면(S1 잔고 줄 · S2/S3/S5 잔고 행 · S9 관리자 연차 섹션)은 04.1-05·04.1-06이 이 플랜의 함수·문자열을 그대로 그린다.

Purpose: ROADMAP 기준 4(차감 · 연 1회 설정 · 이월 없음 · 초과 경고 · 입사 첫해 월차 D-96 · 퇴직 D-97)의 계산 전부가 이 플랜이다.

Output: `users` 두 열 · `leave_adjustments` 표, 순수 잔고 함수와 서비스, `leave.annual_days` 이력형 키, 단위 2 · 통합 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md
@.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@.planning/phases/04.1-approvals-leave/04.1-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다 — 잔고는 입력·출력이 분명한 계산이라 사례표를 먼저 쓴다. 실패가 나면 원인을 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름을 적는다.
- 이 플랜은 04.1-01 다음으로 마이그레이션을 만드는 **유일한** 플랜이다(04.1-02 · 04.1-04 · 04.1-05 · 04.1-06은 스키마를 바꾸지 않는다). `pnpm db:generate`만 쓰고 파일 이름·`_journal.json`을 손대지 않는다(오케스트레이터 결정 7a).
- **마이그레이션 번호(조정자 11:53 규칙 — 페이즈별 번호 범위 지정 폐지)**: 브랜치에서는 `pnpm db:generate`가 주는 번호를 그대로 쓴다 — 그 번호는 임시다. 병합 직전 04.1-07 Task 1이 「origin/main 병합 → 04.1 자신의 `.sql`과 `meta/NNNN_snapshot.json`(각 플랜 SUMMARY `migration_manifest`의 정확한 목록만) 삭제 → `pnpm db:generate`」로 재생성해 번호가 main 마지막 + 1이 된다. 어떤 테스트·검사·SUMMARY도 번호 범위를 단언하거나 보고하지 않는다. 파일 이름을 손으로 바꾸거나 `_journal.json`을 손으로 고치는 일은 이 페이즈 내내 금지다.
- `domain/settings/keys.ts`는 파일 끝에 덧붙이기만 한다. `domain/leave/index.ts`는 건드리지 않는다(04.1-02와 겹치지 않게 — 새 서비스는 `balance-service.ts`에 두고 호출자가 직접 import한다). `balance-service.ts`는 `./index`를 import하지 않는다 — 보임 규칙은 04.1-01의 `domain/leave/access.ts`에서 가져온다(CEO-4). `app/(app)/admin/people/*`는 이 플랜에서 건드리지 않는다(화면은 04.1-06). `domain/people/index.ts`의 입사일·퇴직일 함수는 Phase 3 사람 도메인 규약(쓰기 뒤 `recordAction` · 계정 생성과는 「부분 상태 없음」 보상)을 따른다 — 이 플랜에서 트랜잭션 안 로그로 바꾸는 것은 새 표 `leave_adjustments`에 쓰는 `addLeaveAdjustment`뿐이다(04.1-01 `recordActionInTx`).
- **「빈 DB」는 명령으로 만든다(CEO-16)**: `scripts/dev-db.sh`는 DB를 지우지 않는다. 빈 DB 증명과 재생성 뒤 적용은 `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`, 개발 DB `erp`는 04.1-01 실행 노트의 psql DROP/CREATE 한 줄 뒤 `pnpm db:migrate`.
- **「오늘」(CEO-11)**: 스냅숏 기준일 · 월차 적립 기준일 · 기본 회계연도는 `seoulToday(deps.now)`에서만 얻는다. 날짜 문자열 산술(말일 보정 등)은 UTC 자정 기준 연산이라 시간대 영향이 없다 — 「지금이 며칠인가」만 서울 날짜로 정한다.
- `docs/inputs/phase-05-approval.md` §5와 D-96·D-97이 정본이다 — 이 플랜의 계획 가정은 그 빈칸만 채운다.
- 첫 task를 시작하기 전에 `git rev-parse HEAD` 값을 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도 남긴다). 이 플랜의 diff 기준 검사는 전부 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다 — 커밋한 뒤의 맨 `git diff`는 늘 비어 있어 검사가 공허해진다.
- **실행 순서(공유 DB)**: 이 페이즈의 일곱 플랜은 한 줄로 이어진 체인이다(웨이브마다 플랜 하나: 01 → **03** → 02 → 04 → 05 → 06 → 07). 이 플랜은 마이그레이션을 만들고 DB를 다시 만드는(`scripts/dev-db.sh`) 플랜이라 04.1-01 바로 뒤, 04.1-02보다 앞에 둔다. 로컬 Postgres(`scripts/dev-db.sh`의 고정 DB `erp` · `erp_test`)는 모든 플랜이 공유하고 통합 테스트 setup이 매 테스트 전 전 표를 비우므로, 두 플랜이 동시에 돌지 않는 것은 이 순서가 보장한다 — 잠금·새 도구를 두지 않는다. 원인 모를 「행이 사라짐」 실패가 나면 다른 실행이 같은 DB를 쓰고 있지 않은지 먼저 확인한다(`systematic-debugging`).
</execution_notes>

<assumption_delta_decision>
- **Trigger:** D-96(입사 첫해 월차, Reversibility: costly)이 「사람당 잔고 하나(회계연도 연차)」를 「잔고 둘(연차 · 월차)」로 바꾼다 — 단수 → 복수(pluralization). 감지기는 이 페이즈 문구에서 신호를 잡지 못했지만(`detected: false`) 오케스트레이터 지시로 결정을 기록한다.
- **Primary noun:** `LeaveGrant`(부여) — `{ source: "annual" | "monthly" | "adjustment", bucket: "annual" | "monthly", amountQuarters, validFrom, expiresOn }`. 연차·월차·조정이 전부 이 명사 하나의 출처다.
- **Decision:** `promote` — 「회계연도 연차 잔고」를 기본 모델로 두고 월차를 옆에 덧붙이지 않는다. 부여 목록을 기본 명사로 올리고, 회계연도 연차는 「출처 = annual, 유효 = Y-01-01 ~ Y-12-31」인 부여 하나로 내린다. 차감 배분은 출처를 모르는 한 규칙(신청 시작일에 유효 · 소멸 빠른 순 · 같으면 월차)이다.
- **Why:** 덧붙이기(연차 필드 + 월차 필드)로 가면 차감 순서·만료·조정이 출처마다 따로 구현되고, 다음 출처(예: 포상 휴가)가 오면 세 번째 필드가 생긴다. 부여 목록이면 새 출처는 부여를 만드는 함수 하나다.
- **Invariant test (채택):** `balance.test.ts`에 「세 출처(연차·월차·조정)가 전부 같은 buildLeaveGrants → allocateLeave 경로로 계산된다 — 출처별 분기 없이 요약이 부여 목록만으로 재현된다」 사례를 둔다. 미래 페이즈가 출처 전용 경로를 다시 만들면 이 사례가 빨간색이 된다.
</assumption_delta_decision>

<probe_fallback>
이 플랜이 닫는 미해결 엣지: **LEAV-01 idempotency**(파생 계산 — 첫 truth) · **LEAV-01 concurrency**(동시 승인 두 건 — 「동시에 최종 승인돼도」 truth). LEAV-01 boundary·precision은 04.1-01(신청 일수)과 이 플랜(적립일 말일 보정 · 정수 1/4일 배분)이 함께 닫는다.

**계획 가정(D-96·D-97·입력 §5의 빈칸 — 뒤집으면 `domain/leave/balance.ts` 함수 하나만 바뀐다)**
1. (R1 — **사용자 결정 2026-09-24 21:45 KST(카드 B 「근무 기간 비례」)**, 옛 「Y+1부터 연차 전액」 가정을 대체) 입사 연도 H에는 회계연도 연차를 주지 않는다(월차만 — 기존). 입사 다음 해 H+1의 1월 1일 연차 부여 = 0.25일 단위 올림(annual_days(H+1) × 근무일수(H) / H의 날수). 근무일수(H) = 입사일 ~ H-12-31(양끝 포함), H의 날수 = 365 또는 366, annual_days(H+1) = 이력형 설정 `leave.annual_days`의 (H+1)-01-01 시점 값. 계산은 정수 1/4일만 쓴다: `quarters = ceil(annualDays×4 × worked / daysInYear)`를 정수 나눗셈 `(a×b + c − 1) div c`로 한다(부동소수 금지 — 금지 항목). 올림인 이유: 법정 일수보다 적어지지 않게(직원에게 불리하지 않은 쪽). 1월 1일 입사면 전액, H+2부터는 그 해 설정값 전액. 퇴직 규칙(가정 9)이 우선한다 — H-12-31 이전 퇴직이면 H+1 부여 없음. 이 규칙은 `domain/leave/balance.ts`의 `annualGrantQuarters` 한 함수에만 있다
2. 월차 적립 = 입사일 + k개월(k = 1..11)의 같은 날(없으면 말일), 적립은 그날 0시부터 유효. 11일 상한은 「입사 첫해」 기간 전체에 걸친다(달력 연도에서 자르지 않는다) — **사용자 결정 2026-09-24 11:42(타이핑 「아니 입사일 기준으로 변경해」): 근속 첫 1년 기준, D-96 문구 「그해」는 근속 첫해를 뜻한다.** Codex HIGH(달력 연도에서 잘라야 한다)는 이 결정으로 기각됐다. 근로기준법 제60조 제2항의 1년 미만 근로자 월 1일 해석과 같다
3. 신청의 배분은 신청 시작일에 유효한 부여만 쓴다 — 아직 적립되지 않은 월차를 미리 쓰지 않고, 모자라면 연차 부여로 넘어가며 거기서도 모자라면 연차 남음이 음수가 된다(초과 경고, 막지 않음). 그 해 연차 부여가 없어도(입사 첫해 · 설정 0일) 부여 0인 그 해 연차 자리로 넘겨 음수 남음과 `over`가 생긴다(CX-R02)
4. 연도 중 퇴직해도 그해 연차를 비례로 깎지 않는다 — D-97이 「잔여 일수만 보여 준다」고만 했고 정산 계산은 범위 밖. 퇴직 때 법정(입사일 기준) 대조 계산은 D-97 연차 수당 정산과 함께 이 페이즈 밖이다(R1 — Deferred 표)
5. 퇴직일 당일까지는 결재선 후보다(퇴직일 < 오늘이면 제외 — 오늘 = `seoulToday()`, CEO-11)
7. 월차 적립은 적립일 ≤ 퇴직일인 것만 센다 — 퇴직일이 적립일과 같은 날이면 그 1일은 적립된다(당일 포함, 가정 5의 「퇴직일 당일까지」와 같은 경계). 연차 부여는 가정 4대로 깎지 않는다(Codex HIGH 퇴직 경계)
8. 입사 다음 해처럼 월차와 연차가 함께 있으면 두 잔고를 따로 보여 주고, 신청마다 `차감 예정 월차 N일 · 연차 N일`로 어느 잔고에서 빠지는지 보인다 — 배분 규칙(소멸 빠른 순 · 같으면 월차 먼저)은 그대로다(사용자 요구 2026-09-24 11:43)
6. `leave.annual_days` 기본값 15(이력형, 시드는 2000-01-01 — 기존 이력형 시드 규약). 적용 시작일 1월 1일 강제와 지난 연도 거부(일반 저장 · JSON 가져오기 두 경로, 공통 검증 `validateEffectiveFrom` — ENG-5)는 04.1-04가 레지스트리에서 한다 — 이 플랜은 키에 `effectiveFromRule: "year_start"`만 적는다. 이 플랜의 테스트가 이력 행을 넣을 때는 리포지토리 `insertHistorizedValue`(테스트 픽스처)로 직접 넣는다 — 도메인 `addHistorizedValue`로 넣으면 04.1-04 뒤 과거 연도 가드가 벽시계 연도에 따라 테스트를 깨뜨린다
9. (ENG-4) 퇴직일이 있으면 퇴직 연도 다음 해부터 회계연도 연차 부여가 없다(`annualGrantQuarters`가 0 — 부여 목록에 그 해 연차가 없다). 퇴직 연도 자체의 부여는 가정 4대로 깎지 않는다(그 해가 H+1이면 R1 비례값 그대로). 퇴직 줄(`formatResignationLine`)은 조회 연도가 퇴직 연도일 때만 그 해 요약으로 만든다 — 다른 연도를 조회하면 그 해의 보통 잔고(퇴직 다음 해면 연차 부여 0)를 돌려준다. 서비스는 받은 연도를 바꾸지 않는다(CXF2 — 04.1-06이 섹션 연도 하나를 서버에서 정해 잔고 · 기록 · 연차 조정 `fiscalYear`에 같이 쓴다)
10. (ENG-13 조정 계약) 연차 조정: `fiscalYear` 필수(지난 연도 정정 허용 — ENG-5의 과거 정정 경로), 유효 Y-01-01 ~ Y-12-31, 입력 날짜와 무관. 입력 검증(버킷별 필수값 · 입사일 · 월차 소멸)은 순수 함수 `checkLeaveAdjustment`에 두어 DB 없이 단위 테스트한다. 월차 조정: `fiscalYear` null, 유효 = max(입력한 날의 서울 날짜, 입사일) ~ 월차 소멸일(입사 다음 해 12-31) — 입사일이 없으면 `입사일 없음 · 먼저 입사일 넣기`, 입력한 날이 월차 소멸일 뒤면 `월차 {소멸일} 소멸 · 연차로 조정하기`로 거부. 양수 조정은 그 창의 부여(출처 adjustment). 음수 조정은 같은 버킷 · 같은 창의 부여 합에서만 빠지는 차감 기록(창 안의 개별 부여는 소멸일이 같아 어느 것에서 빠지든 결과가 같다) — 다른 버킷으로 넘어가지 않고, 모자라면 그 버킷 남음이 음수다. `allocateLeave`는 차감 기록과 신청을 (날짜, 조정 먼저, id) 순으로 한 줄로 훑는다. DB는 `(bucket = 'annual') = (fiscal_year IS NOT NULL)` CHECK로 이 계약의 모양을 강제한다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 입사일이 있는 직원의 월차 적립 → 연차 최종 승인 → 월차부터 차감 → 잔고 조회 (스키마 · 리포지토리 · 도메인 · 설정)</name>
  <files>db/schema/auth.ts, db/schema/leave.ts, repositories/users.ts, repositories/org-snapshot.ts, repositories/leave-adjustments.ts, repositories/leave-usage.ts, domain/leave/balance.ts, domain/leave/balance-service.ts, domain/leave/dto.ts, domain/people/index.ts, domain/settings/keys.ts, test/integration/leave-balance.test.ts, test/integration/people.test.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-CONTEXT.md`의 D-96 · D-97 · Claude's Discretion 원문
    - `docs/inputs/phase-05-approval.md` §5 — 연차 원문(종류 · 회계연도 · 이월 없음 · 연 1회 설정 · 초과 경고)
    - `.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md` 325–332행(Pitfall 4 — 이력형 설정과 연 1회의 시점)
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 206–207·213행(잔고 줄 · 잔고 행 · 퇴직 문구 원문) · 492–513행(Assumptions #12·#13·#14)
    - `db/schema/auth.ts` · `db/schema/leave.ts`(04.1-01 산출물) · `db/schema/org.ts`(date 문자열 모드 선례)
    - `repositories/users.ts` · `repositories/org-snapshot.ts`(04.1-01 산출물) · `repositories/approvals.ts`(인스턴스 상태를 읽는 방법)
    - `domain/leave/dto.ts` · `domain/leave/days.ts`(04.1-01 산출물 — `formatLeaveDays`) · `domain/leave/access.ts`(04.1-01 — `canSeeLeaveDocument`) · `lib/dates.ts`(04.1-01 — `seoulToday`)
    - `test/unit/import-cycles.test.ts` 1–40행 — 런타임 순환이 Cloud Run Job을 죽이는 이유(CEO-4)
    - `domain/people/index.ts`(전문) — `registerPerson` 검증 순서와 보상 처리(「부분 상태 없음」 주석 · `register_person_rollback`), 쓰기 권한 판정에 쓰는 메뉴·동작
    - `test/integration/people.test.ts` 78–93행 — 발령 실패 보상 사례(입사일 저장 실패 사례가 같은 모양을 따른다)
    - `domain/approvals/tx-log.ts`(04.1-01 — `loadActionLogGate` · `recordActionInTx`) · `db/schema/corp-cards.ts` 11–40행(`check()` 선례)
    - `domain/settings/registry.ts`의 `getSettingValue(def, {asOf})` · `domain/settings/keys.ts` 끝 60행
  </read_first>
  <behavior>
    - (통합) 입사일 2026-03-10인 직원을 기준일 2026-09-24로 조회하면 월차 적립 6일(04-10 · 05-10 · 06-10 · 07-10 · 08-10 · 09-10), 소멸일 2027-12-31, 2026 연차 부여 0(입사 연도)
    - (통합) 그 직원이 2026-09-28~09-29 종일(2일)을 신청해 최종 승인되면 월차 사용 2일 · 남음 4일, 연차는 그대로
    - (통합) 조회를 세 번 반복하고 같은 문서에 승인을 한 번 더 시도(거부됨)해도 숫자가 같다
    - (통합) 입사일이 없는 직원은 2026 연차 = `leave.annual_days` 기본값 15, 월차 상태 = 입사일 없음
    - (통합) 퇴직일 2026-09-20인 팀장은 2026-09-24 기준 결재선 후보가 아니고, 퇴직일 2026-09-24이면 후보다
    - (통합) 입사일 2026-03-10 · 퇴직일 2026-06-15인 직원을 2026-09-24로 조회하면 월차 적립 3일(04-10 · 05-10 · 06-10)이고 07-10 이후 적립이 없다
    - (통합) `registerPerson`에 입사일 `2026-13-01`을 주면 계정 생성 전에 거부되고(사용자 수 불변), 입사일 저장이 인프라 오류로 실패하면 방금 만든 사용자가 보관 상태이고 오류 문구가 보관·입사일 입력을 안내한다
  </behavior>
  <action>
① **스키마**: `db/schema/auth.ts` `users`에 `hire_date`·`resignation_date`(date 문자열 모드, null 허용) 두 열을 **추가만** 한다. `db/schema/leave.ts`에 `leave_adjustments`(id · `user_id` FK · `bucket` text(`annual`|`monthly`) · `fiscal_year` int null(연차 조정에만) · `amount_quarters` int(0 아님) · `reason` text · `created_by` FK · `created_at`)를 더하고, 04.1-01과 같은 `check()` 선례로 `bucket IN ('annual','monthly')` · `amount_quarters <> 0` · `(bucket = 'annual') = (fiscal_year IS NOT NULL)` 세 CHECK를 단다(Codex MEDIUM DB CHECK — 04.1-01 표들과 같은 기준 · 셋째는 ENG-13 조정 계약: 연차 조정만 회계연도를 갖는다). `pnpm db:generate` → `bash scripts/dev-db.sh && pnpm db:migrate`.

② **리포지토리**: `repositories/users.ts`에 입사일·퇴직일 갱신 함수(viewer 첫 인자). `repositories/org-snapshot.ts`의 스냅숏 쿼리에 「퇴직일이 null이거나 asOf 이상」 조건을 더한다(계획 가정 5 — 호출자는 asOf로 `seoulToday()`를 넘긴다, CEO-11). `repositories/leave-adjustments.ts` — `insertLeaveAdjustment`(선택 `createdAt` — 주어지지 않으면 DB 기본값) · `listLeaveAdjustments(viewer, userId)`만(수정·삭제 없음 — 금지 항목·truth). 목록 행에는 작성자 이름 `createdByName`을 `users` 조인으로 같은 쿼리 한 번에 싣는다(CX-R2 — 조정 기록 DTO 재료). `repositories/leave-usage.ts` — 한 사람의 연차 신청과 그 결재 인스턴스 상태(`approved` · 진행 중)를 쿼리 한 번으로 읽는다(회수·반려는 제외).

③ **순수 함수** `domain/leave/balance.ts`(DB·설정 import 없음, 전부 정수 1/4일): `annualGrantQuarters({fiscalYear, hireDate, resignationDate, annualDays})` → 그 회계연도 연차 부여 쿼터(정수): 퇴직일이 있고 Y > 퇴직 연도 → 0(ENG-4 · 계획 가정 9) · 입사일 없음 → `annualDays×4` · Y ≤ 입사 연도 H → 0 · Y = H+1 → `(annualDays×4 × worked + daysInYear(H) − 1) div daysInYear(H)`(worked = 입사일 ~ H-12-31 양끝 포함 · 윤년 366 — R1 · 계획 가정 1) · 그 밖 → `annualDays×4`. 04.1-06 E2E도 입사 다음 해 연차 기대값을 이 함수로 계산한다(리터럴 금지 — D5). `buildLeaveGrants({hireDate, resignationDate, fiscalYears, annualDaysByYear, adjustments, asOf})` → 부여 목록(연차: Y-01-01~Y-12-31, 양은 `annualGrantQuarters` — 0이면 그 해 연차 부여를 목록에 넣지 않는다 · 월차: 입사일 + k개월(k = 1..11, 근속 첫 1년 — 사용자 결정 11:42)의 적립일마다 1일, 적립일 ≤ `asOf`이고 **퇴직일이 있으면 적립일 ≤ 퇴직일**인 것만(계획 가정 7 · Codex HIGH 퇴직 경계), 소멸 = 입사 다음 해 12-31 · 조정: 계획 가정 10의 계약 — 연차 조정은 `fiscalYear` 창, 월차 조정은 `createdOn`(입력한 날의 서울 날짜 — 서비스가 `created_at`에서 만든다) 창, 양수는 부여 · 음수는 같은 버킷 창의 차감 기록(ENG-13)). `allocateLeave(grants, requests)` → 차감 기록과 신청을 (날짜, 조정 먼저, id) 순으로 훑어 신청은 시작일에 유효한 부여에 소멸 빠른 순(같으면 월차)으로 배분하고 모자라면 해당 회계연도 연차 부여를 음수로 넘긴다(계획 가정 3). **넘길 연차 부여가 목록에 없는 해**(입사 첫해 · `annualGrantQuarters` 0 · 연차 설정 0일 — 위에서 목록에 넣지 않은 해)에는 신청 시작일의 회계연도 Y에 부여 0인 연차 자리(Y-01-01 ~ Y-12-31)를 결과 안에서만 두고 그 자리로 넘긴다 — 부여 목록 자체는 바꾸지 않는다. 결과는 신청마다 `{monthly, annual, annualFiscalYear, over}`(정수 쿼터 — `annual`은 초과분 포함, `over` = 유효한 부여를 넘은 양)이고, 요약의 Y 연차 줄은 부여 0 · 사용/결재 중에 넘긴 양이 들어가 남음이 음수다(D3 정의 그대로). 그래서 「부여가 없는 해의 초과」도 연도별 배분 결과와 `over`로 기록된다(CX-R02). 음수 조정은 자기 버킷 창에서만 빠지고 넘기지 않는다(ENG-13). `summarizeLeaveBalance(…)` → 연차 줄(부여 · 조정 · 사용 · 결재 중 · 남음)과 월차 줄(적립(월차 조정 합산) · 사용 · 결재 중 · 남음 · 소멸일 | 입사일 없음 | 없음)을 **서로 다른 두 객체**로 돌려준다 — 두 남음을 더한 필드를 만들지 않는다(사용자 요구 11:43 · 금지 항목). 남음은 줄마다 `부여(+조정) − 사용 − 결재 중` 하나의 정의다(ENG-12 · D3). 월차 줄의 있고 없음은 D4 규칙(근속 첫 1년 안 · 또는 조회 연도에 적립 있음 → 0이어도 있음)으로 정한다(ENG-11). 이 task는 behavior 네 줄이 도는 만큼만 채우고 전 사례는 Task 3가 굳힌다.

④ **설정**: `domain/settings/keys.ts` 끝에 `LEAVE_ANNUAL_DAYS`(key `leave.annual_days` · 이력형 · `z.coerce.number().int().min(0).max(366)` · 라벨 `연차 일수` · 힌트 `회계연도(1월 시작)마다 부여 · 이월 없음` · namespace `연차` · default 15 · `effectiveFromRule: "year_start"`)를 덧붙이고 `SETTING_DEFS`에 올린다.

⑤ **서비스** `domain/leave/balance-service.ts`(`./index`를 import하지 않는다 — CEO-4): `getMyLeaveBalance(viewer, {fiscalYear?}, deps?)`(연도 기본값 = `seoulToday(deps.now)`의 연도 — CEO-11) · `getLeaveBalanceForUser(viewer, userId, {fiscalYear}, deps?)`(사람 관리 보기 권한 — `domain/people`이 쓰는 메뉴와 같은 판정) · `getLeaveBalanceForRequest(viewer, leaveId, deps?: { now?: Date })`(문서를 볼 수 있는 사람만 — 04.1-01 `domain/leave/access.ts`의 `canSeeLeaveDocument` 재사용, 「이번 신청 · 결재 중(이 문서 제외) · 남음 · 차감 예정 나눔 · 초과」) · `previewLeaveBalance(viewer, input, deps?: { now?: Date })`(저장 전 신청의 같은 계산 — 신청 폼용). **「오늘」 주입(CX-B2)**: 네 조회 함수의 `deps`는 전부 `now?: Date`를 받고 「오늘」(월차 적립 기준일 `asOf` · D4 근속 첫 1년 판정 · 기본 회계연도)은 공개 입구에서 `seoulToday(deps?.now)` **한 번**만 만든다 — 실제 시계 기본값은 이 입구에서만 쓰이고, 안쪽 계산 도우미(요청 잔고 재료 계산 등)는 `now`나 `today`를 인자로 받을 뿐 스스로 시계를 읽지 않는다. 그래서 04.1-05의 일괄 조회(`loadDetails(ids, { visible, now })` → `getLeaveBalancesForRequests`)가 `listMyInbox` · `getApprovalView`의 `deps.now`를 끝까지 넘기면 결재함 상세의 잔고도 주입한 날짜 기준이다. 전부 `LEAVE_ANNUAL_DAYS`를 각 회계연도 1월 1일 시점 `asOf`로 읽는다. **DTO 명세를 둘로 나눈다(CEO-9)**: 본인·관리자용(`getMyLeaveBalance` · `getLeaveBalanceForUser`)은 04.1-01 `domain/leave/dto.ts`의 `LEAVE_BALANCE_DTO_SPEC`(퇴직 줄에 필요한 입사일·퇴직일을 여기에만 둔다), 결재자·미리보기용(`getLeaveBalanceForRequest` · `previewLeaveBalance`)은 새 `LEAVE_REQUEST_BALANCE_DTO_SPEC`(`annualRemaining` · `monthlyRemaining`(D4의 월차 줄이 없거나 입사일이 없으면 null, 줄이 있으면 0도 0 — ENG-11) · `pending` · `thisRequest` · `plannedDeduction {monthly, annual}` · `over` 필드만 — 입사일·퇴직일 필드와 두 잔고를 합친 필드가 명세에 **없다**)로 `project()`한다(CEO-9 · 사용자 요구 11:43). 본인·관리자용 명세도 연차 줄 · 월차 줄 두 객체로만 싣고 합계 필드를 두지 않는다. `getMyLeaveBalance` · `getLeaveBalanceForUser`는 받은 `fiscalYear`의 요약을 그대로 싣는다 — 퇴직일이 있고 그 연도가 퇴직 연도일 때만 퇴직 줄 재료를 싣고, 다른 연도면 그 해의 보통 잔고다(ENG-4 · 계획 가정 9 · CXF2). 두 명세 모두 `registerDto` + `leave.value`. `domain/people/index.ts`에 `setHireDate` · `setResignationDate`(쓰기 권한 · 날짜 형식 · 퇴직일 ≥ 입사일 검증 · 행동 로그 `document_update` entity `user` detail 필드명)와 `registerPerson` 입력의 선택 인자 `hireDate`를 더한다 — 기존 검증 순서 (a)에 넣어 **계정 생성 전에** `YYYY-MM-DD` 실재 날짜인지 검증하고(틀리면 `ValidationError` — 계정이 생기지 않는다), 계정 생성 뒤 저장은 `RegisterPersonDeps`에 새 주입점 `saveHireDate`(기본값 = ②의 `repositories/users.ts` 입사일 갱신 함수 — 권한 판정·로그는 등록 흐름이 이미 했다)로 부르며, 그 저장이 인프라 오류로 실패하면 발령 실패와 같은 보상(`archive(SYSTEM_VIEWER, …)` · detail `register_person_rollback`)을 태우고 `UserFacingError`(`계정 발급은 됐으나 입사일 저장이 실패해 보관함으로 보냈습니다 · 보관함에서 복원한 뒤 사람 상세에서 입사일을 넣으세요`)를 던진다(Codex MEDIUM 입사일 원자성 — 기존 「부분 상태 없음」 규약 그대로). 필수화는 04.1-06의 폼·액션이 한다 — 도메인은 입사일 없는 기존 계정(`월차 계산 불가 · 입사일 없음` 상태)을 계속 받아야 한다.

⑥ `test/integration/leave-balance.test.ts` 첫 describe가 behavior의 잔고·후보·퇴직 적립 줄을 단언한다(신청·승인은 04.1-01 서비스로, 사람·발령은 도메인 함수로). `test/integration/people.test.ts`에 입사일 두 사례(형식 거부 — 사용자 수 불변 · `saveHireDate` 주입 실패 — 보관 상태)를 기존 발령 실패 사례 옆에 더한다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leave-balance.test.ts test/integration/people.test.ts test/integration/approvals-route-fixed.test.ts</automated>
    <fails_when>마이그레이션 적용이 실패하거나, 트레이서 describe의 숫자(월차 6 · 사용 2 · 남음 4 · 연차 15 · 퇴직자 적립 3)가 하나라도 다르거나, 입사일 두 사례(형식 거부 · 저장 실패 보관)가 실패하거나, 기존 사람·결재선 통합 테스트가 회귀한다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/settings test/unit/import-cycles.test.ts</automated>
    <fails_when>registry-coverage가 `leave.annual_days`를 참조 없음으로 잡거나, `balance-service.ts` ↔ `domain/leave/index.ts` 순환이 생겨 import-cycles가 실패하거나, failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(repository-viewer-param 포함) 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - 트레이서 describe가 녹색이고 잔고 숫자가 저장된 열이 어느 표에도 없다(`grep -rn "balance" db/schema`가 0건)
    - `domain/leave/balance.ts`가 `@/db`·`@/repositories`·`@/domain/settings`를 import하지 않는다
    - `repositories/leave-adjustments.ts`에 update·delete 호출이 없다(`grep -cE "\.update\(|\.delete\(" repositories/leave-adjustments.ts`가 0)
    - `domain/leave/index.ts` · `app/(app)/admin/people/*` diff가 비어 있다
    - (CEO-4) `domain/leave/balance-service.ts`가 연차 모듈 입구(`./index` · `.` · `@/domain/leave`)를 import하지 않는다(`grep -cE "from \"(\./index|\.|@/domain/leave)\"" domain/leave/balance-service.ts`가 0)
    - (CEO-9) `domain/leave/dto.ts`에 `LEAVE_REQUEST_BALANCE_DTO_SPEC`이 있고 그 명세 블록에 `hireDate`·`resignationDate`가 없다
    - (CEO-11) `grep -rnE "getFullYear\(\)|new Date\(\)\.toISOString" domain/leave repositories/org-snapshot.ts`가 0건이다
    - `buildLeaveGrants` 입력 타입에 `resignationDate`가 있다(`grep -c "resignationDate" domain/leave/balance.ts`가 1 이상)
    - `db/schema/leave.ts`의 `leave_adjustments` 정의에 `check(`가 3개 있다(버킷 · 0 아님 · 연차만 회계연도 — ENG-13)
    - (R1) `domain/leave/balance.ts`가 `annualGrantQuarters`를 export한다(`grep -cE "export (function|const) annualGrantQuarters" domain/leave/balance.ts`가 1) — 입력·출력이 정수 쿼터이고 일(日) 단위 소수를 만들지 않는다
    - `domain/people/index.ts`에서 입사일 검증이 `createAccount` 호출보다 앞 줄에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>입사일로 월차가 쌓이고 최종 승인된 연차가 월차부터 빠지는 잔고가 저장 없이 계산되어 조회되고, 퇴직자는 결재선 후보에서 빠진다</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 스키마 확정 — 마이그레이션 적용 · SQL 린트 · journal 가드</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `db/migrations/meta/_journal.json`(전문) · `test/unit/db/migration-journal.test.ts`(04.1-01 산출물)
    - `.squawk.toml` — squawk가 막는 규칙(열 추가는 null 허용이라 잠금 위험이 작다)
  </read_first>
  <action>
① Task 1 스키마 위에서 `pnpm db:generate`를 다시 돌려 새 파일이 생기지 않음을 확인한다. ② **실제로 빈 DB를 만들어**(CEO-16) `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`로 전부 적용하고(Task 1 도중 마이그레이션을 다시 만들었으면 개발 DB `erp`도 psql 한 줄로 다시 만든다) `pnpm lint:sql`을 통과시킨다 — 위반이면 스키마 TS를 고쳐 재생성하고 SQL을 손으로 고치지 않는다. ③ journal 가드 단위 테스트(스냅숏 존재 · 고아 스냅숏 없음 포함 — 04.1-01)가 새 항목 포함 녹색인지 확인한다. ④ **마이그레이션 manifest**: SUMMARY 머리(frontmatter)에 04.1-01과 같은 모양의 `migration_manifest:` 목록(이 플랜이 만든 journal tag · SQL 경로 · `meta/NNNN_snapshot.json` 경로)을 적는다 — 04.1-07 Task 1이 병합 때 이 목록에 있는 파일만 지운다(Codex HIGH 07 · 11:48 규칙). 번호 범위는 적지도 판정하지도 않는다(조정자 11:53). 이 task는 코드를 새로 쓰지 않는다 — 실패하면 `systematic-debugging`으로 Task 1 스키마를 고친다.
  </action>
  <verify>
    <automated>G=$(pnpm db:generate 2&gt;&amp;1); RC=$?; printf '%s\n' "$G"; test "$RC" -eq 0 &amp;&amp; grep -q "No schema changes" &lt;&lt;&lt;"$G"</automated>
    <fails_when>`pnpm db:generate`가 0이 아닌 코드로 끝나거나(출력에 문구가 있어도 — 종료 코드를 `RC`로 따로 본다, 04.1-07 CX-R10과 같은 모양, CXF-A-F02), 출력에 "No schema changes"가 없다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>빈 `erp_test` 재생성 · 전체 마이그레이션 적용 · squawk · journal 가드 중 하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 플랜이 만든 SQL 파일이 하나이고 그 스냅숏 파일이 하나이며 이름이 drizzle-kit이 준 그대로다
    - `_journal.json` 변경이 끝에 항목 하나 추가뿐이다(`git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json`의 `-` 줄이 닫는 괄호 정리 외에 없다)
    - SUMMARY frontmatter에 `migration_manifest:`가 있고 tag · SQL · 스냅숏 세 경로가 실제 파일과 같다
    - `pnpm lint:sql` 경고·오류 0, journal 가드 녹색
  </acceptance_criteria>
  <done>잔고 스키마가 DB에 적용되었고 재생성 차이가 0이며 journal 가드가 녹색이고, 04.1-07이 지울 파일 목록이 SUMMARY에 있다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-96 · D-97 · 입력 §5 · R1 사례표로 잔고를 굳힌다 — 적립 경계 · 퇴직 적립 중단 · 입사 다음 해 비례 연차 · 퇴직 다음 해 0 · 배분 순서 · 두 잔고 분리 · 남음 정의 · 월차 0일 줄 · 조정 계약(원자적) · 초과 · 문자열</name>
  <files>domain/leave/balance.ts, domain/leave/balance-service.ts, domain/leave/dto.ts, test/unit/domain/leave/balance.test.ts, test/unit/domain/leave/first-year-accrual.test.ts, test/integration/leave-balance.test.ts</files>
  <read_first>
    - Task 1 산출물 `domain/leave/balance.ts` · `domain/leave/balance-service.ts`(전문)
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` Copywriting의 잔고 줄 · 잔고 행(11:43 개정 — `연차 남음` · `월차 남음` 분리) · 퇴직 · 조정 오류 문구 원문(문자열 단언의 정본 — `grep -n "잔고 행\|남음" 04.1-UI-SPEC.md`로 위치를 찾는다)
    - `domain/approvals/tx-log.ts`(04.1-01 — `loadActionLogGate` · `recordActionInTx`) · `test/integration/approvals-route-fixed.test.ts`의 「행동 로그 실패 주입 — 전이와 함께 롤백」 describe(04.1-01 — 같은 주입 모양)
  </read_first>
  <behavior>
    - (월차) 입사 2026-01-31 → 적립일 02-28 · 03-31 · 04-30 …, 2월은 말일 보정. 입사 2024-02-29 → 2024-03-29 … 2025-01-29(11번째). 입사 2026-10-01 → 11번째 적립 2027-09-01, 소멸 2027-12-31, 2028-01-01 기준 월차 줄 없음
    - (월차) 입사 전날·입사 당일 기준 적립 0, 입사 + 1개월 당일 기준 1
    - (퇴직) 입사 2026-03-10 · 퇴직 2026-06-15 → 2026-09-24 기준 적립 3(07-10 이후 없음). 퇴직 2026-06-10(적립일 당일) → 3(당일 포함 — 계획 가정 7). 퇴직 2026-06-09 → 2. 퇴직(2026-04-01)이 입사 + 1개월 전이면 적립 0이고, 퇴직자라 잔고는 퇴직 줄 한 줄 `퇴직 2026-04-01 · 남은 연차 0일 · 월차 0일`이다 — `월차 적립 0일 …` 줄은 없다(D-97 — 퇴직 연도를 조회하면 퇴직 줄이 월차 줄을 대신한다. 다른 연도 조회는 D4 월차 줄 규칙 그대로, CXF2)
    - (R1 비례 — `annualGrantQuarters`, annualDays 15) 입사 2026-01-01 → 2027 연차 60쿼터(15일, 전액) · 입사 2026-10-01(평년) → 근무 92일 → `ceil(60×92/365)` = 16쿼터(4일) · 윤년 입사 2028-07-02 → 근무 183일/366일 → 30쿼터(7.5일 — 365로 잘못 나누면 31쿼터 7.75일이 되어 깨진다) · 윤년 2028-01-01 입사 → 2029 연차 60쿼터(366/366) · 입사 2026-12-31 → 근무 1일 → 1쿼터(0.25일) · 입사 연도 안 퇴직(입사 2026-03-10 · 퇴직 2026-11-30) → 2027 연차 0(부여 목록에 2027 연차 없음)
    - (R1 경계) 입사 2026-10-01 → 2026 연차 0(입사 연도) · 2028 연차 60쿼터(H+2 전액) · 입사일 없음 → 매년 60쿼터 · 이력형 값 × 비례: 2027-01-01 시점 annualDays 16이면 입사 2026-10-01 → `ceil(64×92/365)` = 17쿼터(4.25일 — annual_days(H+1)을 쓴다는 증명)
    - (ENG-4 퇴직 다음 해) 입사일 없음 · 퇴직 2026-10-31 → 2026 연차 60쿼터(퇴직 연도는 깎지 않음 — 가정 4) · 2027 연차 0이고 `buildLeaveGrants(fiscalYears [2026, 2027])` 목록에 2027 연차 부여가 없다. 입사 2026-10-01 · 퇴직 2027-05-31 → 2027 연차 16쿼터(비례 그대로) · 2028 연차 0
    - (ENG-11 · D4 월차 줄) 입사 2026-12-15 · 기준 2027-01-05(근속 첫 1년 안, 적립 0) → 월차 줄 `월차 적립 0일 · 사용 0일 · 결재 중 0일 · 남음 0일 · 2027-12-31 소멸`, 신청용 DTO `monthlyRemaining` = 0(null 아님). 같은 사람 2027 연차 = 근무 17일 → `ceil(60×17/365)` = 3쿼터(0.75일)이고, 2027-01-05 반차(0.5일) 신청의 잔고 행은 `연차 남음 0.75일 · 월차 남음 0일 · 결재 중 0일 · 이번 신청 0.5일` / `차감 예정 월차 0일 · 연차 0.5일`(초과 없음 — `over` 0, 3행 없음). 같은 사람을 2028-03-15에 보면(근속 1년 지남 · 2028 적립 없음 · 2028 연차 전액 15) 반차 잔고 행은 `연차 남음 15일 · 결재 중 0일 · 이번 신청 0.5일`이고 2행이 없다. 입사 2026-10-01 · 기준 2027-12-15(근속 1년 지남, 2027년 적립 있음) → 월차 줄 있음. 입사 2024-06-01 · 기준 2026-09-24(근속 1년 지남, 2026년 적립 없음) → 월차 줄 없음(`monthly` null · 잔고 행에 `월차 남음` 없음). 입사일 없음 → `월차 계산 불가 · 입사일 없음`, 신청용 `monthlyRemaining` null
    - (분리 — 사용자 요구 11:43 · R1 비례 반영) 입사 2026-10-01 직원을 2027-03-15 기준으로 보면 연차 줄 부여 4(2027 연차 — 비례 16쿼터) · 남음 4와 월차 줄 적립 5(2026-11-01 · 12-01 · 2027-01-01 · 02-01 · 03-01) · 남음 5가 **따로** 나온다. 요약 객체·잔고 행·잔고 줄 어디에도 두 남음의 합 `9일`이 없고, 잔고 행은 `연차 남음 4일 · 월차 남음 5일 · 결재 중 0일 · 이번 신청 6일`이다
    - (분리 + 차감 순서) 같은 직원의 2027-03-15 시작 6일 종일 신청: 월차(2027-12-31 소멸)와 2027 연차(2027-12-31 소멸)가 같은 소멸일 → 월차 먼저 → `차감 예정 월차 5일 · 연차 1일`
    - (배분 · CX-R02 입사 첫해 초과) 입사 2026-03-10 직원이 2026-05-20 시작 3일 종일(최종 승인 · 기준일 `asOf` 2026-05-20): 그날까지 적립 2(04-10 · 05-10) → 월차 2 + 연차 1, 2026 연차 부여 0이라 연차 남음 -1 → 초과 경고 재료. 부여 목록에 2026 연차 부여가 없는 상태에서 `allocateLeave` 결과의 그 신청 = `{monthly 8, annual 4, annualFiscalYear 2026, over 4}`이고, `summarizeLeaveBalance`의 2026 연차 줄 = {부여 0 · 조정 0 · 사용 4 · 결재 중 0 · 남음 -4}쿼터 · 월차 줄 남음 0
    - (CX-R02 연차 설정 0일) 입사일 없음 · 2026-01-01 시점 annualDays 0 → `annualGrantQuarters` 0이라 부여 목록에 2026 연차가 없다. 2026-03-02(월) 종일 1일 결재 중 신청: `allocateLeave` 결과 `{monthly 0, annual 4, annualFiscalYear 2026, over 4}` · 요약 2026 연차 줄 {부여 0 · 사용 0 · 결재 중 4 · 남음 -4}쿼터 · 그 신청의 신청용 DTO `annualRemaining` 0(이 문서를 빼지 않은 값 — D3) · `monthlyRemaining` null(입사일 없음) · `over` 4 → 잔고 행 3행 `잔여 초과 1일`
    - (배분) 2027년 신청은 월차(2027-12-31 소멸)와 2027 연차(2027-12-31 소멸)가 같은 소멸일 → 월차 먼저
    - (정밀) 반반차 4건 = 1일, 반차 1 + 반반차 2 = 1일, 재택은 어떤 부여에서도 빠지지 않는다
    - (이월 없음) 입사일 없는(또는 2025년 이전 입사) 직원의 2026 연차 남음 5일이어도 2027 연차 = 2027-01-01 시점 설정 값 그대로
    - (이력형) 2027-01-01부터 16일을 추가해도 2026 잔고의 부여는 15 그대로(이력 행은 리포지토리 `insertHistorizedValue`로 넣는다 — 계획 가정 6)
    - (조정) 연차 -1(사유 `무단 결근`) → 연차 줄에 `조정 -1일`, 월차 +1 → 월차 적립에 합산. 0 · 0.3 · 사유 공백은 거부
    - (ENG-13 월차 음수 조정) 입사 2026-10-01 · 월차 -1(입력 2027-03-01) · 기준 2027-03-15 → 월차 적립 4 · 남음 4, 연차 4 그대로. 같은 날 6일 신청 → `차감 예정 월차 4일 · 연차 2일`. 월차 -6(입력 2027-03-01)이면 월차 남음 `-1일`이고 연차 남음 4는 그대로다(음수 조정은 다른 버킷으로 넘어가지 않는다)
    - (ENG-13 소멸 뒤 조정) 입사일 없는 직원에게 2027-02-01에 2026 연차 +1(`fiscalYear` 2026)을 넣으면 2026 연차 줄의 조정 · 남음만 +1이고 2027 연차 줄은 그대로다(이월 없음 — 지난 연도 정정 경로). 입사 2026-10-01 직원의 월차 조정: `checkLeaveAdjustment`가 입력한 날 2027-12-31(소멸일 당일)은 통과, 2028-01-02는 `월차 2027-12-31 소멸 · 연차로 조정하기`로 거부
    - (ENG-13 입사일 없는 사람) 연차 -1(2026) → `연차 15일 · 조정 -1일 · 사용 0일 · 결재 중 0일 · 남음 14일`. 월차 조정은 `checkLeaveAdjustment`가 `입사일 없음 · 먼저 입사일 넣기`로 거부하고 월차 줄은 `월차 계산 불가 · 입사일 없음` 그대로. 연차 조정에 `fiscalYear`가 없거나 월차 조정에 `fiscalYear`가 있으면 거부(DB CHECK와 같은 모양)
    - (ENG-12 · D3 픽스처 — 한 입력으로 DTO와 문자열을 같이 단언) 입사 2026-10-01 · 기준 2027-03-15 · annualDays 15. 신청 A 최종 승인 2027-02-08~02-12 종일 5일(그날 유효 월차 4 → 월차 4 + 연차 1) · B 결재 중 2027-03-16 종일 1일(월차 1) · C 결재 중 2027-03-17 오전 반차 0.5일(월차 소진 → 연차 0.5). 요약 DTO: 연차 {부여 16 · 조정 0 · 사용 4 · 결재 중 2 · 남음 10}쿼터, 월차 {적립 20 · 사용 16 · 결재 중 4 · 남음 0}쿼터. 잔고 줄 `연차 4일 · 사용 1일 · 결재 중 0.5일 · 남음 2.5일` / `월차 적립 5일 · 사용 4일 · 결재 중 1일 · 남음 0일 · 2027-12-31 소멸`(D4 — 0이어도 줄 있음). C의 신청용 DTO: `annualRemaining` 12 · `monthlyRemaining` 0 · `pending` 4(B만 — C 제외) · `thisRequest` 2 · `plannedDeduction` {monthly 0, annual 2} · `over` 0, 잔고 행 `연차 남음 3일 · 월차 남음 0일 · 결재 중 1일 · 이번 신청 0.5일` / `차감 예정 월차 0일 · 연차 0.5일`(초과 줄 없음). 모든 남음이 `부여(+조정) − 사용 − 결재 중`으로 재현된다
    - (통합, CX-R2 조정 기록 조회) 입사 2026-10-01 직원에게 관리자가 `addLeaveAdjustment`로 연차 -1(`fiscalYear` 2026, 사유 `무단 결근`, `deps.now` 2026-11-02T01:00Z) · 월차 +1(사유 `보상`, `deps.now` 2026-11-03T01:00Z) · 연차 +1(`fiscalYear` 2027, `deps.now` 2027-02-01T01:00Z)을 넣은 뒤 `listLeaveAdjustmentsForUser(관리자, { userId, fiscalYear })`를 부르면: 2026 → [월차 +1, 연차 -1](`createdAt` 내림차순, `quarters` 4 · -4) · 2027 → [연차 +1, 월차 +1](월차 창이 2027-12-31까지) · 2028 → []. 모든 항목의 `Object.keys`가 정확히 `["id", "kind", "quarters", "reason", "createdAt", "createdByName"]`이고 `createdByName` = 관리자 이름이다. `admin.people` **보기만** 있고 쓰기는 없는 계급(테스트에서 `createRole` 뒤 `upsertPermission(SYSTEM_VIEWER, { roleId, menu: "admin.people", action: "view", allowed: true })`와 `upsertVisibility(SYSTEM_VIEWER, { roleId, infoItem: "leave.value", visible: true })` — 기존 통합 테스트의 권한·노출 부여 모양, `@/repositories/permissions`. 새 계급은 노출 행이 없어 이것이 없으면 투영이 빈 객체다, CX2-01)의 사용자가 부르면 같은 2026 목록을 받고 항목마다 여섯 키가 **값과 함께** 관리자 결과와 같다(`kind` · `quarters` · `reason` · `createdByName` 값 단언, `createdAt`은 주입한 시각), `admin.people` 보기 권한이 없는 기획 PM이 부르면 `ForbiddenError`다(CX-R2 정정). 이어 보기 권한은 둔 채 그 계급의 `leave.value`만 거짓으로 바꾸면(`upsertVisibility`) 같은 호출이 오류 없이 같은 개수의 항목을 돌려주되 항목마다 `Object.keys`가 `[]`이고(여섯 키 `id` · `kind` · `quarters` · `reason` · `createdAt` · `createdByName` 모두 없음) `JSON.stringify(결과)`에 사유(`무단 결근` · `보상`)와 관리자 이름이 없다 — 투영이 실제로 걸린다는 증명(CXF2-A-FF02)
    - (통합, 조정 원자성) `addLeaveAdjustment`에 `deps.appendActionLog`가 예외를 던지게 주입하면 호출이 실패하고 `leave_adjustments` 행 수와 잔고가 그대로다(Codex HIGH 원자성)
    - (문자열 — UI-SPEC 원문, 요약 객체를 직접 만들어 넣는 표시 함수 사례) `연차 15일 · 사용 3.5일 · 결재 중 1일 · 남음 10.5일`(D3: 10.5 = 15 − 3.5 − 1) / 조정 있으면 `연차 15일 · 조정 -1일 · 사용 …` / `월차 적립 4일 · 사용 1일 · 결재 중 0일 · 남음 3일 · 2027-12-31 소멸`(D3: 3 = 4 − 1 − 0) / `월차 계산 불가 · 입사일 없음` / 잔고 행 `연차 남음 10.5일 · 월차 남음 3일 · 결재 중 1일 · 이번 신청 3일`(D4의 월차 줄이 없으면 `연차 남음 10.5일 · 결재 중 1일 · 이번 신청 3일`) · `차감 예정 월차 2일 · 연차 1일` · `잔여 초과 1.5일` / 재택이면 잔고 행 없음(null) / `퇴직 2026-10-31 · 남은 연차 3.5일 · 월차 0일`
    - (불변식 — assumption-delta) 연차·월차·조정 세 출처가 같은 부여 목록 경로로 계산되어, 부여 목록만 넘겨도 요약이 재현된다
    - (통합) 같은 사람의 두 신청을 `Promise.all`로 동시에 최종 승인하면 사용이 두 일수의 합이다
    - (통합) 문서의 결재자는 그 기안자의 잔고 행을 받고, 무관한 사람은 null을 받는다
    - (통합, CEO-9) 기안자에게 입사일·퇴직일이 둘 다 있어도 결재자가 받은 `getLeaveBalanceForRequest` 결과의 `Object.keys`에 `hireDate`·`resignationDate`가 없다. 같은 사람을 관리자가 `getLeaveBalanceForUser`로 보면 퇴직 줄 재료가 있다
    - (통합, 11:43 · R1) 입사 2026-10-01 직원(`deps.now` = 2027-03-15)의 `getMyLeaveBalance`와 `previewLeaveBalance`(6일 신청) 결과에서 연차 남음 4(비례 16쿼터) · 월차 남음 5가 서로 다른 필드이고, `Object.keys`가 명세의 필드 목록과 정확히 같으며(합계 필드 없음), 어떤 숫자 필드도 두 남음의 합(9일 = 36쿼터)과 같지 않다
    - (통합, ENG-12 · D3) 위 D3 픽스처를 실제 신청·승인(04.1-01 서비스, `deps.now` 주입)으로 만들고 `getMyLeaveBalance` DTO 값 → `formatBalanceLines` 문자열, 결재자의 `getLeaveBalanceForRequest(C)` DTO 값 → `formatBalanceRow` 문자열을 한 테스트 안에서 같이 단언한다
    - (통합, CX-B2 주입 시계) 입사 2026-10-01 직원이 2027-03-16 종일 1일을 신청한(결재 중) 문서를 1단 결재자가 `getLeaveBalanceForRequest(viewer, leaveId, { now: new Date("2027-03-15T03:00:00Z") })`로 보면 `monthlyRemaining` 20(적립 5 — 2026-11-01 ~ 2027-03-01) · `annualRemaining` 16이고, `{ now: new Date("2027-01-15T03:00:00Z") }`로 보면 `monthlyRemaining` 12(적립 3) · `annualRemaining` 16이다. 두 결과가 각각 같은 재료로 순수 함수(`buildLeaveGrants` → `allocateLeave` → `summarizeLeaveBalance`, `asOf` = 그 서울 날짜)에 계산한 값과 같다 — 실행 시점의 실제 시계(2026년)를 읽으면 입사 전이라 두 값이 모두 틀려 이 사례가 깨진다
    - (통합, ENG-4 · CXF2 퇴직자를 퇴직 연도가 아닌 해로 조회) 입사일 없음 · 퇴직 2026-10-31 · 2026년 승인 연차 3일인 사람을 관리자가 `deps.now` = 2027-03-01로 `getLeaveBalanceForUser`하면: `fiscalYear` 2026 → 퇴직 줄 재료가 2026 요약(`퇴직 2026-10-31 · 남은 연차 12일 · 월차 0일`) · `fiscalYear` 2027 → 퇴직 줄 재료가 없고 2027 보통 잔고 연차 줄 {부여 0 · 사용 0 · 결재 중 0 · 남음 0}(2027 연차 부여 15가 어디에도 없다) · `fiscalYear` 2025 → 퇴직 줄 없이 2025 보통 잔고(연차 부여 60쿼터)
    - (통합, ENG-13) 입사일 없는 사람의 월차 `addLeaveAdjustment`와 입사 2026-10-01 직원의 월차 소멸 뒤(`deps.now` = 2028-01-02) 월차 `addLeaveAdjustment`는 `ValidationError`이고 `leave_adjustments` 행 수가 그대로다
  </behavior>
  <action>
① behavior의 순수 사례를 `first-year-accrual.test.ts`(월차 적립·경계·소멸·퇴직 적립 중단·R1 비례 부여 사례·ENG-4 퇴직 다음 해·D4 월차 줄·입사 다음 해 분리)와 `balance.test.ts`(배분·정밀·이월·이력·조정·ENG-13 조정 계약 세 사례·D3 픽스처·분리 잔고 행·차감 예정·문자열·불변식)에 먼저 적어 실패를 확인하고 `balance.ts`를 고쳐 초록으로 만든다. 날짜 더하기는 문자열 `YYYY-MM-DD` 입력을 받아 UTC 기준 날짜 연산으로 하고(시간대가 날짜를 하루 밀지 않게), 말일 보정 헬퍼는 이 파일 안에 둔다. 근무일수·해의 날수(윤년)도 같은 UTC 날짜 연산으로 센다(R1). `annualGrantQuarters`는 Task 1이 만든 모양을 R1 사례표로 굳히고, `checkLeaveAdjustment({bucket, fiscalYear, amountQuarters, createdOn, hireDate})`(ENG-13 — 오류 문구 또는 null)를 이 파일에 더한다.

② 문자열 함수 `formatBalanceLines` · `formatBalanceRow` · `formatResignationLine`은 UI-SPEC 원문을 글자 단위로 만든다(`formatLeaveDays` 재사용 — `3.5일`, `0.25일`, 음수 `-1일`). 화면이 문자열을 다시 조립하지 않게 줄 배열(줄마다 톤 `default`·`muted`·`warning`)로 돌려준다. `formatBalanceRow` 1행은 `연차 남음 N일` · (D4의 월차 줄이 있을 때 — 0이어도 — `월차 남음 N일`, ENG-11) · `결재 중 N일`(이 문서 제외) · `이번 신청 N일`이고(두 남음은 이 문서를 빼지 않은 값 — D3), 2행 `차감 예정 월차 N일 · 연차 N일`은 1행에 `월차 남음` 칸이 있을 때만(0 칸도 씀), 3행 `잔여 초과 N일`은 `over` > 0일 때만이다(입사일 없음 → 월차 칸·2행 없음) — 두 남음을 더한 값을 만드는 코드 경로가 없다(사용자 요구 11:43 · UI-SPEC 개정 문구).

③ `addLeaveAdjustment(viewer, {userId, bucket, fiscalYear, amountDays, reason})`를 서비스에 더한다 — 입력 일수를 0.25 배수 정수 쿼터로 바꾸고(아니면 `일수는 0.25 단위 · 0.5처럼 적어 주세요`), 0이면 거부, 사유 trim 빈 값이면 `사유 비어 있음 · 사유 적기`. 쓰기 권한은 setHireDate와 같은 판정. 트랜잭션 전에 04.1-01 `loadActionLogGate()`로 켜짐 여부를 읽고, `withTransaction` 안에서 `insertLeaveAdjustment(viewer, row, tx)`와 `recordActionInTx(viewer, {actionType: "document_create", entity: "leave_adjustment", …}, tx, gate, deps)`(키 이름은 `actionType` — `recordAction`이 `entry.actionType`을 검사하고 `recordActionInTx`는 entry를 그대로 넘긴다, CX-R03)를 함께 쓴다 — 로그가 실패하면 조정 행도 롤백된다(Codex HIGH 원자성). `deps.appendActionLog`를 받아 테스트가 실패를 주입한다. 트랜잭션 전에 대상자의 입사일을 읽고 `checkLeaveAdjustment`로 버킷별 계약(연차 = `fiscalYear` 필수 · 월차 = `fiscalYear` 없음 · 입사일 필요 · 입력한 날(`seoulToday(deps.now)`) ≤ 월차 소멸일)을 검증해 어기면 `ValidationError`(`입사일 없음 · 먼저 입사일 넣기` · `월차 {소멸일} 소멸 · 연차로 조정하기`)를 던진다 — 행이 생기지 않는다(ENG-13 · 계획 가정 10). 서비스는 조정 행을 부여 목록에 넘길 때 `created_at`을 서울 날짜 `createdOn`으로 바꾼다. 조정 행의 `created_at`은 `deps?.now`를 `insertLeaveAdjustment`의 선택 `createdAt`으로 넘겨 쓰고, 없으면 DB 기본값이다(서비스에서 시계를 읽지 않는다 — CX-B2 · CX-R2 테스트가 날짜를 고정한다).

③-2 **조정 기록 조회(CX-R2)** `listLeaveAdjustmentsForUser(viewer, { userId, fiscalYear })`를 같은 서비스 파일에 더한다. 첫 문장은 **보기** 권한 판정이다 — `can(viewer, "admin.people", "view")`가 거짓이면 `ForbiddenError`(`getLeaveBalanceForUser`와 같은 메뉴·동작 · 관리자 사람 상세 연차 섹션의 잔고를 여는 판정과 같다). 조정 쓰기(`addLeaveAdjustment`의 사람 관리 쓰기)를 요구하지 않는다 — 보기 권한만 있는 관리자도 잔고와 기록을 본다(CX-R2 정정). `listLeaveAdjustments(viewer, userId)` 행 중 그 회계연도 잔고 줄에 들어가는 것만 남긴다 — 연차 조정은 `fiscal_year = Y`, 월차 조정은 유효 창(입력한 날 ~ 월차 소멸일)이 Y와 겹치는 것(입력한 날의 연도 ≤ Y ≤ 입사 연도 + 1). `createdAt` 내림차순. 행마다 `project(viewer, row, LEAVE_ADJUSTMENT_DTO_SPEC)`로 `LeaveAdjustmentDTO`(`id` · `kind`(`annual`|`monthly` — `bucket`에서) · `quarters`(`amount_quarters`) · `reason` · `createdAt` · `createdByName`)를 만든다 — `userId` · `createdBy` id · `fiscalYear`는 싣지 않는다. `domain/leave/dto.ts`에 `LEAVE_ADJUSTMENT_DTO_SPEC`(여섯 필드 전부 `leave.value`)과 `LeaveAdjustmentDTO` 타입을 더하고 `registerDto({ name: "leaveAdjustment", … })`로 등록한다 — `test/integration/leak-scan.test.ts`가 이미 `@/domain/leave`(→ `./dto`)를 import하므로 누수 스캔 생성기가 `leaveAdjustment·…` 케이스를 저절로 만든다(leak-scan 파일은 고치지 않는다).

④ 통합 테스트에 둘째 describe(동시 승인 · 결재자 잔고 행 · 무관한 사람 null · 주입 시계 두 날짜(CX-B2) · 조정 · 조정 기록 조회(CX-R2) · 조정 로그 실패 주입 · 월차 조정 거부 두 경우(ENG-13) · 퇴직자 연도별 조회(퇴직 연도만 퇴직 줄 — ENG-4 · CXF2) · 이력형 연도 분리 · D3 픽스처 DTO·문자열(ENG-12) · 10월 입사자 다음 해 분리 잔고(R1 비례 4일))를 더한다. 이력 행은 리포지토리 `insertHistorizedValue`로 넣는다(계획 가정 6 — 04.1-04 과거 연도 가드와 벽시계 연도에 묶이지 않게).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/leave</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 문자열 하나라도 UI-SPEC 원문과 다르면 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leave-balance.test.ts</automated>
    <fails_when>동시 승인 합계 · 결재자 잔고 행 · 무관한 사람 null · 주입 시계 두 날짜의 결재자 잔고(CX-B2) · 조정 · 조정 기록 조회의 연도 거름 · 키 목록 · 보기 전용 관리자 허용 · 보기 권한 없음 거부(CX-R2) · 조정 로그 실패 시 롤백 · 월차 조정 거부(ENG-13) · 퇴직(퇴직 연도 조회만 퇴직 줄 · 2025/2027 조회는 보통 잔고 — ENG-4 · CXF2) · 이력형 분리 · D3 픽스처(ENG-12) · 10월 입사자 두 잔고 분리(연차 4 · 월차 5 · 합계 필드·값 없음) 중 하나라도 실패한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm test:unit &amp;&amp; pnpm test:integration</automated>
    <fails_when>단위·통합 프로젝트 전체에서 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 말일 보정(1월 31일 · 윤년 2월 29일) · 11번째 적립 · 소멸 다음 날 사례가 단위 테스트에 있다
    - 같은 소멸일에서 월차 먼저 사례가 있다
    - assumption-delta 불변식 사례가 있다
    - 문자열 단언이 UI-SPEC 206·207·213행의 예시를 전부 포함한다
    - `grep -rn "parseFloat\|toFixed" domain/leave`가 표시 함수 밖에서 0건이다
    - 연차 수당·금액 계산 함수가 없다(`grep -rniE "allowance|payout|수당" domain/leave`가 0건)
    - 결재자 DTO에 `hireDate`·`resignationDate` 키가 없다는 통합 단언이 있다(CEO-9)
    - 퇴직 적립 중단 세 사례(퇴직 뒤 · 적립일 당일 · 전날)가 단위 테스트에 있다(Codex HIGH)
    - 입사 2026-10-01 · 기준 2027-03-15 사례가 단위와 통합 둘 다에 있고, 연차 남음 4(R1 비례)와 월차 남음 5를 따로 단언하며 합계(9)가 없음을 단언한다(사용자 요구 11:43)
    - (R1) 단위 테스트에 비례 부여 다섯 사례(1월 1일 입사 전액 · 10월 1일 평년 4일 · 윤년 2028-07-02 7.5일 · 12월 31일 입사 0.25일 · 입사 연도 안 퇴직 부여 없음)와 이력형 16일 × 비례 17쿼터 사례가 있다. 입사일이 있는 직원의 입사 다음 해 연차를 15일(60쿼터)로 단언하는 사례가 없다(1월 1일 입사 전액 사례만 예외)
    - (ENG-4 · CXF2) 퇴직 다음 해 연차 0 단위 사례와, 퇴직자를 퇴직 연도(퇴직 줄) · 그 앞뒤 해(보통 잔고, 퇴직 줄 없음)로 조회하는 통합 사례가 있다
    - (ENG-11) 입사 2026-12-15 · 기준 2027-01-05의 `월차 남음 0일` 사례와 근속 1년 지나고 적립 없는 해의 월차 줄 없음 사례가 있다
    - (ENG-12) D3 픽스처가 단위(요약 → 문자열)와 통합(DTO → 문자열) 둘 다에서 DTO 값과 문자열을 한 테스트 안에서 단언한다
    - (ENG-13) 월차 음수 조정 · 소멸 뒤 조정 · 입사일 없는 사람 세 단위 사례가 있고, `addLeaveAdjustment` 본문이 트랜잭션 전에 `checkLeaveAdjustment`를 부른다
    - `domain/leave/dto.ts` 두 잔고 명세와 `domain/leave/balance.ts`에 합계 필드 이름이 없다(`grep -cE "totalRemaining|combinedRemaining|remainingTotal" domain/leave/dto.ts domain/leave/balance.ts`가 0)
    - `addLeaveAdjustment` 본문에 `recordActionInTx`가 있고 `recordAction(`이 없다 · 그 인자 객체의 키가 `actionType`이다(`grep -c 'type: "document_create"' domain/leave/balance-service.ts`가 0이고 `grep -c 'actionType: "document_create"' domain/leave/balance-service.ts`가 1 — CX-R03)
    - (CX-R02) `allocateLeave` 단위 사례에 「부여 목록에 그 해 연차가 없는 초과」 두 사례(입사 첫해 · 연차 설정 0일)가 있고 연도별 배분 결과(`annualFiscalYear`)와 `over`를 단언한다
    - (CX-B2) `domain/leave/balance-service.ts`에 `new Date(`가 0건이다(`grep -v '^\s*//' domain/leave/balance-service.ts | grep -c "new Date("`이 0 — 시계는 `seoulToday(deps?.now)` 입구에서만) · `getLeaveBalanceForRequest` · `previewLeaveBalance` 시그니처에 `deps`가 있다
    - (CX-R2) `domain/leave/dto.ts`에 `name: "leaveAdjustment"` 등록이 한 번이다(`grep -c 'name: "leaveAdjustment"' domain/leave/dto.ts`가 1) · `listLeaveAdjustmentsForUser` 본문 첫 판정이 `can(viewer, "admin.people", "view")`이고 쓰기 판정을 부르지 않는다(실행자가 SUMMARY에 판정 줄을 인용한다)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>D-96(근속 첫 1년 · 사용자 결정 11:42) · D-97(퇴직 적립 중단 포함) · 입력 §5의 규칙과 계획 가정이 사례표로 고정되고, 입사 다음 해 직원의 월차·연차가 두 잔고로 따로 보이며, 화면 넷이 그대로 쓸 잔고 문자열이 개정된 원문과 일치한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 → 입사일·퇴직일·조정 | 잔고와 결재선 후보를 바꾸는 입력이다 |
| 잔고 조회자 → 다른 사람의 잔고 | 본인 · 관리자 · 그 문서의 결재자만 봐야 한다 |
| 설정(연차 일수) → 과거 잔고 | 값을 바꿔도 지난 연도가 소급해 바뀌면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-16 | Elevation of Privilege | `setHireDate` · `setResignationDate` · `addLeaveAdjustment` | high | mitigate | 도메인에서 사람 관리 쓰기 권한을 판정하고 거부 시 예외. 통합 테스트가 기획 PM 계급의 호출 거부를 단언한다 |
| T-04.1-17 | Information Disclosure | 잔고 조회 서비스 | high | mitigate | 본인 · 사람 관리 보기 권한 · 그 문서를 볼 수 있는 결재자만. 나머지는 null. DTO는 `leave.value` 정보 항목으로 project(). 결재자·미리보기는 입사일·퇴직일 필드가 명세에 없는 `LEAVE_REQUEST_BALANCE_DTO_SPEC`만 받는다 — 발표 전 퇴직일이 결재자에게 새지 않는다는 키 부재 통합 단언(CEO-9) |
| T-04.1-57 | Information Disclosure | 조정 기록 목록(사유 · 작성자) | medium | mitigate | 도메인 `listLeaveAdjustmentsForUser`만 내보낸다 — 잔고와 같은 보기 판정 `can(viewer, "admin.people", "view")`(없으면 `ForbiddenError` — 쓰기는 요구하지 않는다) + `LEAVE_ADJUSTMENT_DTO_SPEC` `project()`(여섯 필드 `leave.value`, 사람 id 없음) + `registerDto`로 누수 스캔 케이스. 04.1-06 페이지는 리포지토리를 직접 부르지 않는다(eslint `app` → `repositories` 금지). 키 목록 · 보기 전용 허용 · 보기 없음 거부 통합 사례(CX-R2) |
| T-04.1-18 | Tampering | 조정 일수 입력 | medium | mitigate | 0.25 배수 정수 쿼터 변환 · 0 거부 · 사유 필수를 도메인에서 검증. 부동소수 계산 금지(금지 항목) |
| T-04.1-19 | Repudiation | 조정·입사일·퇴직일 | medium | mitigate | 조정은 추가 전용 표(수정·삭제 함수 없음) + `created_by` + DB CHECK(버킷 · 0 아님). 조정은 `recordActionInTx`로 행과 로그가 한 tx(로그 실패 주입 → 행 0건 통합 단언). 입사일·퇴직일은 Phase 3 사람 도메인 규약대로 쓰기 뒤 `recordAction` |
| T-04.1-20 | Tampering | 이력형 연차 일수 | medium | mitigate | 회계연도 1월 1일 asOf로만 읽는다. 1월 1일 강제와 지난 연도 행 거부는 04.1-04 공통 검증 `validateEffectiveFrom`이 일반 저장 · JSON 가져오기 두 경로에 건다(ENG-5 — 가져오기의 우회 경로 차단). 지난 연도 정정은 연차 조정으로만. 이력 분리 통합 테스트 |
| T-04.1-21 | Tampering | 동시 승인과 잔고 | medium | mitigate | 잔고를 저장하지 않고 파생 계산 — 잃어버린 갱신이 원천적으로 없다. 동시 승인 합계 통합 테스트 |
| T-04.1-54 | Tampering | 퇴직 뒤 월차 적립 | high | mitigate | `buildLeaveGrants`가 `resignationDate`를 받아 적립일 ≤ 퇴직일만 센다. 퇴직 뒤 · 당일 · 전날 단위 사례(Codex HIGH) |
| T-04.1-55 | Information Disclosure | 두 잔고 합계 표시(오인) | medium | mitigate | DTO 두 명세와 문자열 함수에 합계 필드·경로가 없다. 10월 입사자 다음 해 사례가 단위·통합에서 연차 4(R1 비례) · 월차 5 분리와 합계(9일) 부재를 단언(사용자 요구 11:43) |
| T-04.1-56 | Tampering | 사람 등록 중 입사일 누락 | medium | mitigate | 입사일 형식을 계정 생성 전에 검증하고, 저장 실패는 발령 실패와 같은 보관 보상 — 두 통합 사례(Codex MEDIUM) |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` 통과
- `bash scripts/dev-db.sh && pnpm db:reset:test && pnpm db:migrate && pnpm test:unit && pnpm test:integration` 통과(기존 스위트 회귀 없음 · import-cycles 녹색 — CEO-4)
- `pnpm db:generate` 재실행 결과 "No schema changes"
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml domain/leave/index.ts "app/(app)"` 비어 있음
</verification>

<success_criteria>
- 연차·월차·조정이 부여 목록 하나로 계산되고 소멸 빠른 순으로 차감된다
- 입사 첫해 월차가 D-96대로(근속 첫 1년 — 사용자 결정 11:42) 적립·소멸하고 퇴직일 뒤에는 쌓이지 않으며 퇴직자는 잔여만 보인다(D-97)
- 입사 다음 해 직원에게 월차와 연차가 두 잔고로 따로 보이고 합계 숫자가 어디에도 없다(사용자 요구 11:43)
- 입사 다음 해 연차가 근무 기간 비례(0.25일 올림)로 부여되고, 퇴직 다음 해에는 연차가 생기지 않으며 퇴직 줄은 퇴직 연도 기준이다(R1 · ENG-4)
- 남음 정의 하나(D3)와 월차 0일 줄(D4)이 DTO·문자열에서 같고, 조정은 유효 창·소멸·음수 차감 규칙대로 계산된다(ENG-11 · ENG-12 · ENG-13)
- 잔고가 저장되지 않아 이중 차감·잃어버린 갱신이 없다
- 네 화면이 쓸 잔고 문자열이 UI-SPEC 원문과 같다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표·열 | `leave_adjustments` · `users.hire_date` · `users.resignation_date` |
| 마이그레이션 | drizzle-kit이 이름 붙인 SQL 1개 + 그 `meta/NNNN_snapshot.json`(브랜치 임시 번호 — 병합 직전 04.1-07 Task 1이 SUMMARY `migration_manifest`의 파일만 지우고 main 마지막 + 1로 재생성, 번호 범위 지정 없음 — 조정자 11:53) |
| 함수 | `buildLeaveGrants` · `allocateLeave` · `summarizeLeaveBalance` · `formatBalanceLines` · `formatBalanceRow` · `formatResignationLine` · `getMyLeaveBalance` · `getLeaveBalanceForUser` · `getLeaveBalanceForRequest` · `previewLeaveBalance` · `addLeaveAdjustment` · `listLeaveAdjustmentsForUser`(CX-R2) · `setHireDate` · `setResignationDate` · `insertLeaveAdjustment` · `listLeaveAdjustments` |
| 설정 키 | `leave.annual_days`(이력형, 기본 15, 적용 시작일 규칙 year_start) |
| 테스트 | `test/unit/domain/leave/balance.test.ts` · `test/unit/domain/leave/first-year-accrual.test.ts` · `test/integration/leave-balance.test.ts` · `test/integration/people.test.ts`(입사일 두 사례 추가) |
| 기록 | SUMMARY frontmatter `migration_manifest`(tag · SQL · 스냅숏 경로) |
| DTO | `LEAVE_BALANCE_DTO_SPEC`(본인·관리자 — 입사일·퇴직일 포함, 연차 줄·월차 줄 두 객체) · `LEAVE_REQUEST_BALANCE_DTO_SPEC`(결재자·미리보기 — `annualRemaining` · `monthlyRemaining` 분리, 입사일·퇴직일·합계 없음, CEO-9 · 11:43) · `LEAVE_ADJUSTMENT_DTO_SPEC`(`leaveAdjustment` — 조정 기록 여섯 필드, `leave.value`, CX-R2) |
| 변경된 기존 심볼 | `users` 표(두 열) · `registerPerson`(선택 인자 hireDate · 계정 전 검증 · 주입점 `saveHireDate` · 저장 실패 보관 보상) · `RegisterPersonDeps` · `listOrgSnapshot`(퇴직 조건, asOf = `seoulToday`) · 연차 잔고 DTO 명세(필요한 필드) |

## Review Dispositions Ledger

### Round 1 — 1b86370

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 퇴직일이 부여 계산 입력에 없어 퇴직 뒤에도 월차가 쌓임(L98@1b86370) | HIGH | Plan 03, Task 1 ③: `buildLeaveGrants({hireDate, resignationDate, …})` · 적립일 ≤ 퇴직일(계획 가정 7 · 당일 포함) · Task 1 트레이서 통합 사례 · Task 3 단위 세 사례(뒤 · 당일 · 전날) · T-04.1-54 |
| 입사일 저장이 사람 등록 원자성 바깥(L99@1b86370) | MEDIUM | Plan 03, Task 1 ⑤: 계정 생성 전 형식 검증 + 주입점 `saveHireDate` 실패 시 기존 발령 실패와 같은 보관 보상(「부분 상태 없음」 규약) · `people.test.ts` 두 사례 · T-04.1-56 |
| 조정도 행동 로그와 원자적이어야 함(L40·L71@1b86370과 같은 문제 — 새 표 쓰기) | HIGH | Plan 03, Task 3 ③: `addLeaveAdjustment`가 `loadActionLogGate` + `withTransaction` 안 `recordActionInTx` · 로그 실패 주입 → 행 0건 통합 사례 |
| `leave_adjustments` 문자열·수치 열 DB CHECK(L41@1b86370과 같은 기준) | MEDIUM | Plan 03, Task 1 ①: `bucket IN ('annual','monthly')` · `amount_quarters <> 0` |
| 입사 다음 해 월차·연차가 합쳐 보이지 않게(사용자 요구 2026-09-24 11:43, 조정자 전달) | — | Plan 03 truths(분리 잔고 · 잔고 행 정의 · DTO 필드) · 새 금지 항목 · Task 1 ③⑤(두 객체 · `annualRemaining`/`monthlyRemaining`) · Task 3 ②(잔고 행 1행 분리 · 2행 차감 예정) · 10월 입사자 2027-03-15 단위·통합 사례 · T-04.1-55. 화면 쪽은 04.1-05(결재자 패널) · 04.1-06(목록 머리 · 신청 창), 문구는 UI-SPEC Copywriting 개정 |
| 마이그레이션 번호 범위 문구(조정자 11:53 — 번호 범위 지정 폐지) | — | 실행 노트 · Artifacts의 번호 범위 문구를 「브랜치는 받은 번호 · 병합 직전 재생성 = main 마지막 + 1」 규칙으로 교체, Task 2 ④ `migration_manifest` |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| Codex HIGH D-96 — 정본 「입사한 해 · 그해 최대 11일」과 모순, 달력 연도에서 잘라야 함(L97@1b86370) | Rejected per user decision (11:42 typed): service-year interpretation kept; D-96 wording '그해' means the first service year. 계획 가정 2와 월차 truth가 이 결정을 인용한다 |
| 퇴직일은 로그인 종료가 아님 — 세션 게이트는 `archivedAt`만 거부(L100@1b86370) | Deferred — D-97은 잔여 표시만 잠갔고 로그인 종료 규칙은 이 페이즈 범위 밖이다. 운영 절차 「퇴직 처리 = 퇴직일 입력 + 계정 보관」을 04.1-07 공개 체크리스트에 적는다(04.1-07 Task 3). 퇴직일 경과 자동 보관은 사용자 결정이 필요한 새 기능이라 이 플랜에 넣지 않는다 |

### Round 2 — /plan-eng-review 2026-09-24(docs/designs/plant8-erp-phase04.1-eng-review-260924.md)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| R1 입사 다음 해 연차 부여량(사용자 결정 21:45 KST 카드 B 「근무 기간 비례」 — D5) | — | 계획 가정 1 교체 · 연차 부여 truth · 순수 함수 `annualGrantQuarters`(Task 1 ③) · 단위 사례(1월 1일 전액 · 10월 1일 평년 4일 · 윤년 2028-07-02 7.5일 · 12월 31일 0.25일 · 입사 연도 안 퇴직 0 · 이력형 16일 × 비례 17쿼터) · 10월 입사자 예시 15 → 4, 합계 20 → 9 |
| 4 퇴직 뒤 해에도 연차가 새로 생김(04.1-03:202) | BLOCKER | 계획 가정 9 · 퇴직 truth · `annualGrantQuarters`(Y > 퇴직 연도 → 0) · 서비스의 퇴직 줄 기준 연도 = 퇴직 연도(Task 1 ⑤) · 단위 사례 · 통합 사례(2027 조회에 2026 기준 퇴직 줄) |
| 5 「지난 연도 잔고 불변」 미보장 — 과거 연도 1월 1일 통과 · JSON 가져오기 우회(registry.ts:137-139 · export.ts:116,140) | BLOCKER | 가드 구현·회귀 테스트는 04.1-04(`validateEffectiveFrom` 두 경로). 이 플랜: 이월 truth가 가드를 인용 · 계획 가정 6(테스트 이력 행은 리포지토리 픽스처로) · T-04.1-20 |
| 11 월차 0일 표시(04.1-03:44) | MEDIUM | D4 — 월차 줄 truth · backstop 문구 · DTO `monthlyRemaining`(0 vs null) · `formatBalanceRow` · 단위 사례(12월 15일 입사 · 근속 1년 지난 해) |
| 12 잔고 「남음」 정의 충돌(04.1-03:44 vs 280) | MEDIUM | D3 — 남음 truth(한 정의) · 잔고 행 truth(이번 신청은 빼지 않음) · D3 픽스처가 단위·통합에서 DTO 값과 문자열을 같이 단언 |
| 13 조정의 계산 계약 없음(04.1-03:202) | MEDIUM | 조정 계약 truth · 계획 가정 10 · 셋째 DB CHECK · `checkLeaveAdjustment` · 단위 세 사례(월차 음수 · 소멸 뒤 · 입사일 없음) · 통합 거부 사례 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 퇴직 때 법정(입사일 기준) 연차 대조 계산(R1 카드 B의 뒤따르는 일) | Deferred — 회계연도 비례 부여가 법정 입사일 기준보다 적은지의 대조와 수당 정산은 D-97 연차 수당 정산과 함께 이 페이즈 밖이다. 이 플랜은 금액·법정 대조를 계산하지 않는다(금지 항목) |

### Round 3 — Codex 1회차(docs/designs/phase04.1-revision-260924/codex-round1-A.md · -C.md · triage)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| R02 그 해 연차 부여가 없을 때 초과 배분 계약 없음 | WARNING | Task 1 ③ `allocateLeave` — 부여 0인 그 해 연차 자리로 넘김 · 결과 `{monthly, annual, annualFiscalYear, over}` · 계획 가정 3 · Task 3 단위 두 사례(입사 첫해 · 연차 설정 0일)(CX-R02) |
| R03 조정 로그 인자 키 `type` → `actionType` | WARNING | Task 3 ③ 호출 인자 · 수락 기준 grep(CX-R03) |
| B2 결재함 상세 잔고의 「오늘」 전달 경로 없음(03 쪽) | BLOCKER | Task 1 ⑤ 네 조회 함수 `deps.now` · 입구 한 번 `seoulToday` · truth · Task 3 주입 시계 두 날짜 통합 사례 · `new Date(` 0건 수락 기준(CX-B2) |
| C-R2 조정 기록을 페이지가 리포지토리에서 직접 읽음(03 쪽) | BLOCKER | Task 3 ③-2 `listLeaveAdjustmentsForUser` · `LEAVE_ADJUSTMENT_DTO_SPEC` 등록 · 보기 판정 `admin.people` view(정정 — 쓰기 아님) · Task 1 ② 작성자 이름 조인 · 통합 사례 · T-04.1-57(CX-R2) |
| R04 음수 월차 조정의 창 | — | 근거 불일치(triage EVIDENCE-MISMATCH) — 변경 없음 |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-03-SUMMARY.md` when done
</output>
