---
phase: 04.1-approvals-leave
plan: 03
subsystem: leave
tags: [leave, balance, grants, monthly-accrual, adjustments, drizzle, postgres, tracer, tdd]
plan_base: 81974c52736da9f5351a80d5978c42e3dfed4cd3
plan_head_before: 81974c52736da9f5351a80d5978c42e3dfed4cd3

requires:
  - phase: 04.1-approvals-leave
    plan: 01
    provides: "leave_requests · 결재 엔진(approveDocument) · canSeeLeaveDocument(access.ts) · recordActionInTx/loadActionLogGate · seoulToday · LEAVE_BALANCE_DTO_SPEC(인터페이스만)"
provides:
  - "users.hire_date · users.resignation_date + CHECK users_resignation_on_or_after_hire_check(NOT VALID) · leave_adjustments 표(추가 전용, CHECK 3개) + 마이그레이션 1개"
  - "순수 domain/leave/balance.ts — annualGrantQuarters · buildLeaveGrants · allocateLeave · summarizeLeaveBalance · requestBalanceOf · resignationBalanceOf · checkLeaveAdjustment · formatBalanceLines · formatBalanceRow · formatResignationLine · balanceFiscalYears"
  - "domain/leave/balance-service.ts — getMyLeaveBalance · getLeaveBalanceForUser · getLeaveBalanceForRequest · previewLeaveBalance · addLeaveAdjustment · listLeaveAdjustmentsForUser (전부 deps.now 입구)"
  - "DTO 명세 LEAVE_BALANCE_DTO_SPEC(채움) · LEAVE_REQUEST_BALANCE_DTO_SPEC · LEAVE_ADJUSTMENT_DTO_SPEC(registerDto 3건)"
  - "domain/people setHireDate · setResignationDate · registerPerson 선택 인자 hireDate(주입점 saveHireDate)"
  - "설정 키 leave.annual_days(이력형, 기본 15, effectiveFromRule year_start)"
  - "lib/dates.ts seoulDateToUtcDate · lib/pg-errors.ts isCheckViolation · 결재선 스냅숏 퇴직일 조건"
affects: [04.1-02, 04.1-04, 04.1-05, 04.1-06, 04.1-07]

actuals:
  tokens: 51196
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "잔고 = 부여(grant) 목록 하나 — 연차 · 월차 · 조정이 같은 명사의 출처, 배분은 출처를 모르는 한 규칙(시작일에 유효 · 소멸 빠른 순 · 같으면 월차)"
    - "음수 조정 = 같은 버킷 · 같은 소멸일 창의 차감 기록, (날짜, 조정 먼저, id) 순으로 신청과 한 줄로 훑음"
    - "공개 입구에서 seoulToday(deps?.now) 한 번 → 안쪽 도우미는 today 인자만"
    - "이력형 설정 asOf Date는 seoulDateToUtcDate 한 도우미로만"

key-files:
  created:
    - db/migrations/0018_brown_aaron_stack.sql
    - db/migrations/meta/0018_snapshot.json
    - domain/leave/balance.ts
    - domain/leave/balance-service.ts
    - repositories/leave-adjustments.ts
    - repositories/leave-usage.ts
    - test/integration/leave-balance.test.ts
    - test/unit/domain/leave/balance.test.ts
    - test/unit/domain/leave/first-year-accrual.test.ts
    - test/unit/domain/leave/balance-fixture.ts
  modified:
    - db/schema/auth.ts
    - db/schema/leave.ts
    - db/migrations/meta/_journal.json
    - domain/leave/dto.ts
    - domain/people/index.ts
    - domain/settings/keys.ts
    - lib/dates.ts
    - lib/pg-errors.ts
    - repositories/org-snapshot.ts
    - repositories/users.ts
    - test/integration/people.test.ts
    - test/unit/lib/dates.test.ts

migration_manifest:
  - journal_tag: "0018_brown_aaron_stack"
    sql: "db/migrations/0018_brown_aaron_stack.sql"
    snapshot: "db/migrations/meta/0018_snapshot.json"
    hand_insert: "(a) 15~17행 — SET LOCAL lock_timeout = '1s'; · SET LOCAL statement_timeout = '5s'; · --> statement-breakpoint (첫 ALTER TABLE \"users\" ADD COLUMN \"hire_date\" 앞) · (b) 23행 끝 — users_resignation_on_or_after_hire_check CHECK 문장 끝에 NOT VALID. 04.1-07 재생성 때 같은 두 삽입을 다시 한다"

key-decisions:
  - "요청 잔고 행의 `over`는 그 신청 자신이 유효한 부여를 넘은 양이다(다른 결재 중 신청의 초과는 그 신청의 `over`)"
  - "`결재 중`(잔고 행)은 이 문서를 뺀 그 회계연도의 다른 진행 중 신청 합이다"
  - "월차 조정은 asOf와 무관하게 적립 합에 들어가고, D4 월차 줄 존재 판정만 `유효 시작일 ≤ 조회 기준일`을 본다"
  - "음수 조정이 창의 부여를 넘으면 모자란 만큼은 배분에서 버리고 요약에서 그 버킷 남음이 음수가 된다(다른 버킷으로 넘기지 않음)"
  - "previewLeaveBalance는 입력이 아직 신청이 되지 않으면(날짜 없음 등) null"
  - "조정 입력 오류 문구 중 UI-SPEC에 없는 두 줄(계획 가정): 0일 `일수가 0 · 빼려면 -1처럼 적기` · 버킷·연도 불일치 `잔고와 연도가 맞지 않음 · 다시 고르기`"
  - "setHireDate · setResignationDate는 null(비우기)을 받는다 — 퇴직일이 비면 재직"

requirements-completed: [LEAV-01]

coverage:
  - id: D1
    description: "트레이서 — 입사일 있는 직원의 월차 적립 → 종일 2일 최종 승인 → 월차부터 차감 → 반복 조회 · 재승인 시도에도 같은 잔고, 퇴직자 결재선 후보 제외"
    requirement: "LEAV-01"
    verification:
      - kind: integration
        ref: "test/integration/leave-balance.test.ts#트레이서 — 월차 적립 → 연차 최종 승인 → 월차부터 차감 → 잔고 조회"
        status: pass
    human_judgment: false
  - id: D2
    description: "입사일·퇴직일 권한 · 역전 · 로그 · DB CHECK 23514 · 경합 10회, registerPerson 입사일 형식 거부 · 저장 실패 보관"
    requirement: "LEAV-01"
    verification:
      - kind: integration
        ref: "test/integration/people.test.ts#입사일·퇴직일 — 권한 · 역전 · 로그 · DB CHECK(A-04 · A2-02)"
        status: pass
    human_judgment: false
  - id: D3
    description: "D-96 · D-97 · R1 · ENG-4 · ENG-11 · A2-01 · 11:43 분리 · 배분 · 정밀 · 이월 없음 · 이력형 · ENG-13 조정 계약 · D3 픽스처 · 문자열 · 불변식"
    requirement: "LEAV-01"
    verification:
      - kind: unit
        ref: "test/unit/domain/leave/first-year-accrual.test.ts · test/unit/domain/leave/balance.test.ts"
        status: pass
    human_judgment: false
  - id: D4
    description: "서비스 — 동시 승인 합 · 결재자 잔고 행/무관한 사람 null · CEO-9 키 부재 · CX-B2 주입 시계 · D3 DTO+문자열 · A2-01 배분 범위 · 이력형 분리 · 퇴직자 연도별(보관 뒤 포함) · 조정 기록(CX-R2) · 조정 원자성 · ENG-13/T3 거부 · A-04"
    requirement: "LEAV-01"
    verification:
      - kind: integration
        ref: "test/integration/leave-balance.test.ts"
        status: pass
    human_judgment: false

duration: 37min
completed: 2026-09-26
status: complete
---

# Phase 04.1 Plan 03: 연차 잔고(부여 목록 · 월차 · 조정) Summary

**저장된 잔고 없이 부여 목록(회계연도 연차 · 입사 첫해 월차 · 조정) 하나에서 소멸 빠른 순으로 차감해 계산하는 연차 잔고 — 입사 다음 해 비례 연차(0.25일 올림), 퇴직 적립 중단, 두 잔고 분리 문자열, 원자적 관리자 조정까지**

## Performance

- **Duration:** 37분
- **Started:** 2026-09-26T15:54:54Z
- **Completed:** 2026-09-26T16:31:52Z
- **Tasks:** 3 (커밋 3)
- **Files modified:** 22
- **PLAN_BASE:** `81974c52736da9f5351a80d5978c42e3dfed4cd3`
- **실행자 모델:** Opus — 플랜 `estimate.confidence: low`(잔고 규칙 · 배분 · 조정 계약의 사례가 많고 서로 얽힘)라 오케스트레이터가 Opus로 띄웠다

## Accomplishments
- 잔고를 어느 표에도 저장하지 않는다 — 조회마다 `buildLeaveGrants → allocateLeave → summarizeLeaveBalance`. 동시 최종 승인 두 건은 사용 = 두 일수의 합(잃어버린 갱신 없음), 재조회 · 재승인 시도에도 같은 숫자
- 월차: 입사일 + k개월(말일 보정), 적립일 ≤ 조회 기준일 · ≤ 퇴직일, 전부 입사 다음 해 12-31 소멸. 연차: 입사 연도 0 · 다음 해 비례(윤년 366) · 그 뒤 전액 · 퇴직 다음 해 0
- 연차 줄과 월차 줄은 서로 다른 두 객체이고, DTO 두 명세 · 문자열 함수 어디에도 합계 필드 · 경로가 없다(10월 입사자 사례가 단위 · 통합에서 9일 부재를 단언)
- 조정: 권한 → 버킷 → 계약 검증을 트랜잭션 전에 끝내고, 조정 행과 행동 로그가 한 tx(로그 실패 주입 → 행 0건 · 잔고 불변)
- 입사일·퇴직일: 앱 검증 + DB CHECK(23514 → 같은 `ValidationError`). 경합 10회에서 매번 정확히 하나만 성공
- 결재자 · 미리보기 DTO에는 입사일 · 퇴직일 키가 없다(CEO-9)

## Task Commits

1. **Task 1 ① 스키마 + 마이그레이션** - `f75ee77` (feat) — 확인: `db:generate` "No schema changes" rc 0 · `lint:sql` 0 issues · `dev-db.sh && db:migrate` applied
2. **Task 1 ②~⑥ 트레이서** - `9bf3e71` (feat) — RED(모듈·함수·열 없음 8건 + import 실패) → GREEN: 통합 3파일(leave-balance · people · approvals-route-fixed) 38건 · settings/import-cycles 단위 39건 · lint · typecheck 0 · `TZ=Asia/Seoul` dates 5건. ⚡ Tracer verified end-to-end — expanding
3. **Task 2 스키마 확정** — 새 diff 없음(아래 편차 1). 빈 `erp_test` 재생성 → 전체 migrate applied → `lint:sql` 0 issues → journal 가드 7건 녹색 · `db:generate` rc 0 "No schema changes"
4. **Task 3 사례표 + 조정** - `9383686` (feat) — RED(단위 66건 중 30건 assertion 실패 · 통합 17건 실패) → GREEN 단위 66 · 통합 18

## 인용(수락 기준)

- 마이그레이션 손 삽입(삽입 전 생성본과의 차이 — `diff 생성본 최종본`):
  ```
  14a15,17
  > SET LOCAL lock_timeout = '1s';
  > SET LOCAL statement_timeout = '5s';
  > --> statement-breakpoint
  20c23
  < ... OR "users"."resignation_date" >= "users"."hire_date");
  > ... OR "users"."resignation_date" >= "users"."hire_date") NOT VALID;
  ```
- `_journal.json` diff는 끝 항목 하나 추가(`0018_brown_aaron_stack`)와 닫는 괄호 정리뿐
- CX-R2 `listLeaveAdjustmentsForUser` 첫 판정(쓰기 판정 없음): `if (!(await (deps?.can ?? defaultCan)(viewer, PEOPLE_MENU, "view"))) {` — `PEOPLE_MENU = "admin.people"`, `can(viewer, "admin.people", "view")`와 같은 판정에 테스트 주입점만 더한 모양
- 조정 로그 인자 키: `actionType: "document_create"` 1건 · `type: "document_create"` 0건 · `recordAction(` 0건(`recordActionInTx`만)
- `balance-service.ts`: `new Date(` 0건 · `seoulDateToUtcDate(` 1건 · 연차 모듈 입구 import 0건
- `lib/dates.ts` · `lib/pg-errors.ts` diff의 `-` 줄 0(파일 끝 덧붙임뿐)
- `git diff --stat PLAN_BASE..HEAD -- package.json pnpm-lock.yaml domain/leave/index.ts "app/(app)"` 비어 있음
- 경합 사례 돌연변이 확인: `isCheckViolation` 변환을 지우면 경합 사례가 원시 DB 오류로 깨진다(실행 뒤 원복) — CHECK 경로가 실제로 타진다

## Decisions Made

frontmatter `key-decisions` 참고.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - 막힘] Task 2에 커밋할 diff가 없음**
- **Found during:** Task 2
- **Issue:** 플랜 Task 1 ①이 `pnpm db:generate`와 손 삽입까지 하므로 Task 2의 `_journal.json` 변경은 이미 Task 1 스키마 커밋에 들어갔다
- **Fix:** Task 2는 검증만(빈 DB 재생성 · migrate · squawk · journal 가드 · 재생성 차이 0) 하고 빈 커밋을 만들지 않았다
- **Committed in:** f75ee77

**2. [Rule 2] `checkLeaveAdjustment` 입력을 `amountDays` · `reason`으로**
- **Issue:** 플랜은 입력을 `amountQuarters`로 적었지만 behavior의 「0 · 0.3 · 사유 공백 거부」 단위 사례를 순수 함수에서 증명하려면 일수 · 사유가 필요하다
- **Fix:** `checkLeaveAdjustment({bucket, fiscalYear, amountDays, reason, createdOn, hireDate})` → `{field, message} | null`. 서비스는 통과 뒤 `amountDays × 4`(정수 보장)를 저장
- **Committed in:** 9383686

**3. [Rule 2] 칸 이름을 가진 조정 오류 클래스**
- **Issue:** T3가 「칸 `bucket`의 `ValidationError`」를 요구하는데 `domain/people`의 `ValidationError`에는 칸이 없고 `domain/leave/index.ts`의 `LeaveValidationError`는 CEO-4 때문에 import할 수 없다
- **Fix:** `balance-service.ts`에 `LeaveAdjustmentValidationError extends ValidationError`(`field`) — `instanceof ValidationError`가 그대로 참
- **Committed in:** 9383686

**4. [Rule 2] artifacts 목록 밖의 순수 export 5개 · 테스트 픽스처 1개**
- **Issue:** 서비스와 단위 사례가 같은 계산을 써야 한다(배분 연도 범위 · 요청 잔고 행 · 퇴직 줄 재료 · 월차 소멸일)
- **Fix:** `balanceFiscalYears` · `requestBalanceOf` · `resignationBalanceOf` · `monthlyExpiresOn` · 타입들을 `balance.ts`에 두고 서비스가 부른다. 단위 두 파일의 공용 파이프라인은 `test/unit/domain/leave/balance-fixture.ts`(수집 대상 아님)
- **Committed in:** 9bf3e71 · 9383686

**5. [Rule 2] `setHireDate` · `setResignationDate`가 null을 받고 대상 없음은 `UserNotFoundError`**
- **Issue:** 퇴직일을 비워 재직으로 되돌리는 경로가 없으면 오입력을 고칠 수 없다
- **Fix:** 두 함수의 값 인자 타입 `string | null`, 대상 행이 없으면 기존 `UserNotFoundError`
- **Committed in:** 9bf3e71

---

**Total deviations:** 5 (Rule 2 4 · Rule 3 1)
**Impact on plan:** 범위 확장 없음. 모두 플랜 사례를 순수 함수에서 증명하거나 기존 오류 계약을 지키기 위한 모양 조정이다.

## Issues Encountered

- TDD 기록: Task 1에서 트레이서에 필요한 만큼(`requestBalanceOf` 포함 — 트레이서 범위의 `getLeaveBalanceForRequest`가 쓴다)만 구현했으므로 Task 3 첫 RED 실행에서 단위 66건 중 36건은 이미 통과했다(적립 경계 · 퇴직 적립 중단 · 같은 소멸일 월차 먼저 · 입사 첫해 초과 등 Task 1이 만든 동작). 나머지 30건이 assertion으로 실패한 뒤 구현했다. 통합 Task 3 사례의 RED는 서비스가 새 계약(조정 입력)을 아직 넘기지 않아 난 `input.adjustments is not iterable`였다(assertion RED 아님)
- 한 번의 `ls` 글롭 불일치(rc 2)가 hook의 실패 관문을 켜 `systematic-debugging`을 불렀다 — 원인은 없는 파일 글롭이었고 테스트 실패가 아니었다

## 검증 게이트(최종 커밋 직전, 빈 erp_test 재생성 뒤)

- `pnpm typecheck` rc 0
- `pnpm lint` rc 0
- `pnpm lint:sql` rc 0 — 0 issues in 19 files
- `pnpm test:unit` rc 0 — 106파일 · 1452건(import-cycles 포함)
- `pnpm test:integration` rc 0 — 60파일 · 1809건(`db:reset:test` → erp_test migrate 뒤)
- `TZ=Asia/Seoul pnpm vitest run --project unit test/unit/lib/dates.test.ts` — 5건 통과
- `pnpm db:generate` rc 0 · "No schema changes"
- E2E: 이 플랜은 화면이 없어 돌리지 않음

## 건너뛴 절차(오케스트레이터 지시)

- execute-plan.md의 STATE.md · ROADMAP.md · REQUIREMENTS.md 갱신과 루트 HANDOFF.json은 건너뛰었다(Phase 4 세션 소유 — 오케스트레이터가 처리)
- 이 플랜 task에는 Codex 교차 검토 단계가 없어 Opus 대체 검토를 띄우지 않았다 — Codex 한도(9/29) — 한도 풀리면 Codex 재확인 필요
- 푸시하지 않았다(오케스트레이터가 한다)
- PR #90 사용자 결정 대기 6건(반차 시간 처음 값 · 멈춘 문서 끝 줄 · 결재함 승인 동작 · 상태 그룹 순서 · 제목 구분자 · 반차 종료일 거부 문구)에 의존하는 작업은 없었다 — 잔고 계산 · 문자열은 그 여섯과 무관하다

## Known Stubs

없음. 04.1-01이 남긴 스텁(`LEAVE_BALANCE_DTO_SPEC` 인터페이스만)은 이 플랜이 채웠다.

## Threat Flags

없음 — 새 함수는 플랜 threat_model(T-04.1-16 ~ 57 · 54b · SC) 범위 안이고 액션 · 라우트 같은 새 네트워크 입구는 없다(화면 · 액션은 04.1-05 · 04.1-06).

## User Setup Required

None.

## Next Phase Readiness

- 04.1-05 · 04.1-06은 `balance-service.ts`를 직접 import하고(연차 입구 `index.ts`를 거치지 않는다), 화면 문자열은 `formatBalanceLines` · `formatBalanceRow`(재택 → null) · `formatResignationLine`을 그대로 그린다. 조정 폼 칸 오류는 `LeaveAdjustmentValidationError.field`
- 04.1-06 `월차` 옵션 빼기 문구 = `checkLeaveAdjustment`의 거부 문구(`입사일 없음 · 먼저 입사일 넣기` · `월차 {소멸일} 소멸 · 연차로 조정하기`)
- 04.1-07 재생성: `migration_manifest`의 두 파일만 지우고 두 손 삽입(SET LOCAL 블록 · CHECK 끝 NOT VALID)을 다시 한다
- 04.1-04: `leave.annual_days`의 `effectiveFromRule: "year_start"`는 표시만 있다 — `validateEffectiveFrom` 가드는 04.1-04 몫

## Self-Check: PASSED

- 생성 파일 10개 존재 확인, 커밋 3개(f75ee77 · 9bf3e71 · 9383686) `git log`에 있음
