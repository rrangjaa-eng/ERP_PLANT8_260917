---
phase: 04.1-approvals-leave
plan: 04
type: execute
wave: 4
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - domain/settings/registry.ts
  - domain/settings/export.ts
  - domain/approvals/settings-warnings.ts
  - domain/approvals/settings-options.ts
  - app/(app)/admin/settings/page.tsx
  - app/(app)/admin/settings/settings-form-client.tsx
  - app/(app)/admin/settings/settings.module.css
  - test/unit/settings/effective-from-rule.test.ts
  - test/unit/domain/approvals/settings-warnings.test.ts
  - test/integration/settings-approval-route.test.ts
  - test/integration/settings-export.test.ts
  - test/e2e/settings-approval-route.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [ADMN-04, EXP-04]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "관리자는 `/admin/settings`의 `연차 결재선` 섹션에서 자기 승인 · 단계별 사용 · 담당 계급 · 조직 범위 · 특정 부서를 바꾸고, 바꾼 값은 **새로 제출하는 문서부터** 적용된다 — 진행 중 문서의 단계는 그대로다(ADMN-04 · EXP-04 · ROADMAP 기준 2)"
    - "`N단 사용`을 끄면(규칙 on/off) 그 뒤 제출되는 문서의 결재선에 그 단계 행이 없고, 켜 두면 있다. 자기 승인을 `본인 승인`으로 바꾸면 그 뒤 제출되는 문서부터 기안자가 담당인 단계를 기안자가 직접 승인한다(ADMN-04 · 입력 §3)"
    - "섹션은 새 화면이 아니라 설정 레지스트리가 자동으로 그린다 — 17칸이 한 섹션에 세로로 서고 탭·접기가 없다(UI-SPEC S8 · UI Considerations overflow S8)"
    - "`담당 계급`·`특정 부서` select의 옵션은 서버가 화면을 그릴 때 지금의 계급·조직 목록으로 채운다 — 저장값은 id, 보이는 값은 이름, 첫 옵션은 `—`(빈 값)다(UI-SPEC U3 해소 · 오케스트레이터 결정 5)"
    - "옵션 목록은 `admin.settings` 보기 권한만으로 전부 나온다 — `listApprovalRouteOptions`가 설정 보기 판정 뒤 리포지토리에서 계급·본부의 id·이름만 읽고 사람 관리 권한(`scopeFor`)이나 정보 노출 투영을 거치지 않는다. 사람 관리 권한 없이 설정 권한만 가진 계급도 계급·본부 목록을 빠짐없이 본다(Codex MEDIUM)"
    - "`자기 승인`은 `건너뜀`/`본인 승인`, `조직 범위`는 `기안자 팀`/`기안자 본부`/`전사`/`특정 부서`로 보인다 — 저장값(enum 키)이 화면에 날것으로 나오지 않는다"
    - "조직 범위가 특정 부서인데 부서가 `—`이거나 그 부서가 없어졌으면 그 단계의 `특정 부서` 칸 아래에 `부서 없음 · 이 단계는 빈 자리로 건너뜀`이 `--warning`으로 보이고 저장은 된다(UI-SPEC S8 error 행 · partial S8 backstop 행)"
    - "`연차 일수`(이력형)의 적용 시작일이 실재하는 `YYYY-01-01`이 아니면(`2027-03-01` · `garbage-01-01` · `2027-1-1` 포함) `적용 시작일은 1월 1일 · 2027-01-01처럼 적어 주세요`로 DB에 닿기 전에 거부된다 — 이 규칙은 `effectiveFromRule: \"year_start\"`를 단 모든 이력형 키에 레지스트리가 일반 규칙으로 건다(Codex MEDIUM — 접미사 검사가 아니라 날짜 해석)"
    - "지난 연도 잔고는 소급해 바뀌지 않는다(ENG-5 — 04.1-03 이월 truth의 보장): `year_start` 키의 적용 시작 연도가 `seoulToday(deps.now)`의 연도보다 앞이면 `지난 연도는 바꿀 수 없습니다 · 지난 잔고는 사람 상세의 연차 조정으로 고쳐 주세요`로 거부된다. 형식·1월 1일·지난 연도 판정은 공통 검증 함수 `validateEffectiveFrom(def, effectiveFrom, today)` 하나에 있고 **두 쓰기 경로**가 모두 부른다 — 일반 저장 `addHistorizedValue`(지금 `domain/settings/registry.ts:137-139`는 스키마 파싱 직후 날짜 검사 없이 `insertHistorizedValue`를 부른다)와 설정 JSON 가져오기 `importSettings`(지금 `domain/settings/export.ts:116`은 값 스키마만 보고 `:140` `applySettingsImport`로 `addHistorizedValue`를 거치지 않고 직접 쓴다). 가져오기에서 지난 연도 항목은 그 날짜에 대상 환경에서 이미 유효한 값(기존 행 또는 기본값)과 같을 때만(아무 날짜의 값도 바꾸지 않는 무변화 행 — 예: 모든 내보내기에 들어 있는 시드 기준 행 `2000-01-01`) 통과하고, 값이 다르면 전체 가져오기가 거부된다. 올해 1월 1일은 허용된다. 규칙 없는 이력형 키(세율 등)의 동작은 그대로다"
    - "같은 값을 다시 저장해도 결과가 같고(단순값 upsert), 저장은 기존대로 `settings_change` 행동 로그에 남는다(ADMN-04 idempotency)"
    - "테스트 전용 두 번째 문서 종류는 설정 화면에 나타나지 않는다 — 섹션은 등록된 설정 키로만 생긴다(UI-SPEC S8)"
    - "관리자가 네 단계를 모두 꺼도 결재 없이 승인되는 연차는 없다 — 그 뒤 제출한 연차는 상태 `submitted`, 지금 단계 = 대표 폴백 자리로 멈춘다. 04.1-01 T3의 폴백 규칙(차수 승인 0건 → 대표)을 설정 저장 경로로 증명한다(CEO-1)"
    - "설정 E2E는 공유 `erp_test`의 결재선을 바꾼 채 두지 않는다 — 바꾼 값은 `finally`에서 도메인 함수로 원래 값을 다시 쓰고, 스펙 마지막 테스트가 복원을 화면에서 확인한다. 이 스펙은 `desktop` → `mobile-375`가 모두 끝난 뒤 도는 별도 Playwright 프로젝트 `desktop-settings`에서만 돌고 `desktop`은 이 스펙을 무시한다. 다른 스펙이 기대하는 단계 수를 바꾸는 단계 on/off는 E2E에서 건드리지 않는다(CEO-14)"
    - "설정 화면의 문서 종류 등록은 `app/(app)/document-kinds.ts` import 한 줄로만 보장한다 — `page.tsx`가 연차 모듈을 직접 import하지 않고, 04.1-02의 가드 단위 테스트가 녹색이다(CEO-3)"
    - statement: "두 관리자가 같은 결재선 키를 거의 동시에 바꾸면 둘 다 오류 없이 끝나고 그 키의 행은 하나, 값은 둘 중 마지막 커밋이다. 그 사이에 제출된 문서는 제출 순간 SELECT 한 문장(04.1-01 `findSimpleValues`)으로 읽은 17키 — 한 커밋 시점의 값 한 벌 — 로 고정되고, 그 뒤 어느 쪽 저장에도 단계 행이 바뀌지 않는다. 설정 화면은 칸마다 즉시 저장이라 「여러 칸 묶음 저장」의 원자성은 없다 — 두 칸 사이에 제출된 문서는 중간 조합(그 자체로 유효한 값)을 받고, 어떤 조합이든 폴백 규칙 때문에 결재 없이 승인되지 않는다(ADMN-04 concurrency · Codex HIGH)"
      verification: test
  prohibitions:
    - statement: "결재 단계 전용 커스텀 폼이나 JSON 편집 칸을 만들어서는 안 된다 — 설정 레지스트리의 필드 단위 키가 화면을 그린다(RESEARCH Don't Hand-Roll · UI-SPEC S8)"
      status: resolved
      verification: test
    - statement: "설정 화면 코드에 계급 이름·문서 종류 이름으로 분기하는 코드를 두어서는 안 된다(D-36) — 옵션은 키 정의의 `optionLabels`·`dynamicOptions`, 경고는 등록된 종류의 `routeSettings`로만 들어온다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/settings/registry.ts"
      provides: "effectiveFromRule 일반 규칙(적용 시작일 1월 1일 강제 · 지난 연도 거부 — 공통 검증 `validateEffectiveFrom`, ENG-5)"
      contains: "validateEffectiveFrom"
    - path: "domain/settings/export.ts"
      provides: "설정 JSON 가져오기의 이력형 항목에 같은 공통 검증을 건다 — 지난 연도는 무변화 행만 통과(ENG-5)"
      contains: "validateEffectiveFrom"
    - path: "test/integration/settings-export.test.ts"
      provides: "가져오기 경로 회귀 — 지난 연도 값 변경 거부(전부 또는 없음) · 무변화 지난 행 + 미래 행 통과 · 1월 1일 아닌 날짜 거부 · 올해 허용 · 기존 왕복 (a)(b) 녹색(ENG-5)"
    - path: "domain/approvals/settings-options.ts"
      provides: "설정 화면 결재선 동적 옵션 — admin.settings 보기 판정 뒤 계급·본부 id·이름(Codex MEDIUM)"
      exports: ["listApprovalRouteOptions"]
    - path: "domain/approvals/settings-warnings.ts"
      provides: "등록된 문서 종류의 결재선 설정에서 「특정 부서 없음」 경고를 키별로 계산"
      exports: ["listApprovalRouteSettingWarnings"]
    - path: "test/integration/settings-approval-route.test.ts"
      provides: "설정 변경 → 새 제출만 반영 · 진행 중 무영향 · 단계 끄기 · 자기 승인 전환 · 네 단계 모두 끔 → 대표 폴백(CEO-1) · 두 관리자 동시 저장 + 제출 경주 · 설정 보기 권한만으로 옵션 전부 증명"
      min_lines: 90
    - path: "test/e2e/settings-approval-route.spec.ts"
      provides: "설정 화면 섹션 · 이름 옵션 · 경고 스모크 · `finally` 복원과 복원 확인 테스트(CEO-14)"
      min_lines: 35
    - path: "playwright.config.ts"
      provides: "`desktop-settings` 프로젝트(testMatch = 설정 결재선 스펙 · dependencies = mobile-375) · `desktop` testIgnore에 같은 스펙(CEO-14)"
      contains: "desktop-settings"
  key_links:
    - from: "domain/settings/export.ts"
      to: "domain/settings/registry.ts"
      via: "importSettings가 이력형 항목마다 addHistorizedValue와 같은 validateEffectiveFrom을 부른다 — 가져오기가 적용 시작일 규칙을 우회하지 못한다(ENG-5)"
      pattern: "validateEffectiveFrom"
    - from: "app/(app)/admin/settings/page.tsx"
      to: "domain/settings/registry.ts"
      via: "키 정의의 optionLabels·dynamicOptions를 읽어 {value, label} 옵션을 만든다"
      pattern: "dynamicOptions"
    - from: "app/(app)/admin/settings/page.tsx"
      to: "domain/approvals/settings-options.ts"
      via: "dynamicOptions 목록은 설정 보기 판정만 거친 이 함수에서 한 번 읽는다(scopeFor·투영 없음)"
      pattern: "listApprovalRouteOptions"
    - from: "app/(app)/admin/settings/page.tsx"
      to: "domain/approvals/settings-warnings.ts"
      via: "키별 경고를 보기 모델에 싣는다"
      pattern: "listApprovalRouteSettingWarnings"
    - from: "domain/approvals/settings-warnings.ts"
      to: "domain/approvals/kinds.ts"
      via: "등록된 종류의 routeSettings만 순회한다(종류 이름 분기 없음)"
      pattern: "routeSettings"
    - from: "app/(app)/admin/settings/page.tsx"
      to: "app/(app)/document-kinds.ts"
      via: "문서 종류 등록 보장 import 한 줄(CEO-3 — 경고 계산이 등록된 종류를 순회하기 전에)"
      pattern: "document-kinds"
    - from: "playwright.config.ts"
      to: "playwright.config.ts"
      via: "`desktop-settings`가 `dependencies: [\"mobile-375\"]`로 다른 모든 스펙 뒤에 돈다(CEO-14 — 기존 mobile-after-desktop 직렬화와 같은 방식)"
      pattern: "desktop-settings"
---

<objective>
**As a** 결재 규칙을 관리하는 경영관리 담당자, **I want to** 설정 화면에서 결재 단계를 켜고 끄고, 단계마다 담당 계급·조직 범위·특정 부서를 이름으로 고르고, 문서 종류별 자기 승인을 바꾸기를, **so that** 조직이 바뀔 때 개발자 없이 결재선을 고치고, 이미 올라간 문서는 흔들리지 않는다.

Task 1은 이 플랜의 **트레이서**다 — 관리자가 화면에서 값을 고르는 경로(이름 옵션)부터 다음 제출의 결재선까지 설정 화면 · 레지스트리 · 결재 엔진을 관통해 E2E와 통합 테스트로 증명한다. Task 2는 경고와 이력형 적용 시작일 규칙으로 넓힌다.

Purpose: ROADMAP 기준 2의 후반(ADMN-04 — 관리자가 결재 단계·담당 계급·조직 범위·규칙 on/off를 바꾸고 진행 중 문서는 영향 없음)이 이 플랜이다. 이 페이즈의 ADMN-04는 **결재 부분만(partially covered — 결재 부분)** 이다 — 알림 시점·대상은 Phase 7/04.2, 세율·수식은 Phase 5 몫이다(ROADMAP 04.1 기준 2 · REQUIREMENTS 추적표).

Output: 레지스트리의 옵션 이름·동적 옵션·적용 시작일 규칙 배선, 결재선 경고 함수, 설정 화면 두 파일 소폭 수정, Playwright 프로젝트 `desktop-settings` 한 항목, 단위 2 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md
@.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md
@.planning/phases/04.1-approvals-leave/04.1-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다. 실패가 나면 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름을 적는다.
- **`domain/settings/keys.ts`를 고치지 않는다** — 결재선 17키의 라벨·힌트·optionLabels·dynamicOptions는 04.1-01이 최종형으로 적었고, 이 플랜보다 앞선 04.1-03이 그 파일 끝에 `leave.annual_days`를 덧붙였다. 이 플랜의 테스트는 `leave.annual_days` 대신 테스트 안에서 만든 이력형 키 정의로 적용 시작일 규칙을 증명한다. 단 가져오기 경로(ENG-5)는 `SETTING_DEFS`에 등록된 키만 받으므로 그 통합 테스트는 04.1-03의 `LEAVE_ANNUAL_DAYS`를 import해서 쓴다(읽기만 — `keys.ts` diff는 여전히 비어 있다).
- **과거 연도 가드와 테스트 시계(ENG-5)**: 가드는 「올해」를 `seoulToday(deps.now)`로 정한다. 이 플랜의 새 테스트는 전부 `deps.now`를 주입한다 — 벽시계 연도에 묶인 단언을 만들지 않는다. 기존 테스트가 `year_start` 키에 도메인 `addHistorizedValue`로 행을 넣으면 해가 바뀔 때 깨지므로, 그런 호출이 있으면 `deps.now` 주입이나 리포지토리 픽스처로 바꾼다(04.1-03은 리포지토리 `insertHistorizedValue` 픽스처를 쓰도록 계획됨 — `grep -rn "LEAVE_ANNUAL_DAYS" test`로 확인). 시드 경로(`domain/seed/index.ts`의 `seedHistorizedValue`)는 기본값과 같은 `2000-01-01` 기준 행을 없을 때만 넣어 어떤 날짜의 값도 바꾸지 않으므로 가드 대상이 아니다.
- `app/(app)/admin/settings/settings-form-client.tsx`는 Phase 4 04-09도 고치는 파일이다(병합 위험) — **select 옵션 렌더와 경고 한 줄**만 바꾸고 다른 줄은 건드리지 않는다. diff를 최소로 유지한다.
- 화면 검증 순서(CLAUDE.md): 싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`) → 수정 → 전체 게이트 한 번. 새 색·토큰 0개 — `--warning`은 기존 토큰이다.
- 첫 task를 시작하기 전에 `git rev-parse HEAD` 값을 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도 남긴다). 이 플랜의 diff 기준 검사는 전부 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다 — 커밋한 뒤의 맨 `git diff`는 늘 비어 있어 검사가 공허해진다.
- **설정 E2E 격리(CEO-14)**: 설정 결재선은 모든 연차 스펙이 공유하는 전역 값이다. 이 플랜의 E2E는 (1) 단계 on/off 대신 다른 스펙이 기대에 쓰지 않는 `자기 승인`으로 저장 유지를 증명하고, (2) 바꾼 값은 `finally`에서 되돌리며, (3) 별도 프로젝트 `desktop-settings`(다른 모든 스펙 뒤)에서만 돈다. `playwright.config.ts`는 `projects` 배열에 한 항목을 더하고 `desktop`의 `testIgnore`를 배열로 바꾸는 것만 한다 — 기존 주석·`webServer`·env 블록은 한 글자도 바꾸지 않는다. 이 스펙만 빨리 돌릴 때는 `--project desktop-settings --no-deps`를 쓴다(기존 주석의 `--no-deps` 관례).
- **실행 순서(공유 DB)**: 이 페이즈의 일곱 플랜은 한 줄로 이어진 체인이다(웨이브마다 플랜 하나: 01 → 03 → 02 → **04** → 05 → 06 → 07). 이 플랜은 04.1-02가 끝난 뒤 시작하고 04.1-05는 이 플랜이 끝난 뒤 시작한다. 로컬 Postgres(`scripts/dev-db.sh`의 고정 DB `erp` · `erp_test`)는 모든 플랜이 공유하고 통합 테스트 setup이 매 테스트 전 전 표를 비우므로, 두 플랜이 동시에 돌지 않는 것은 이 순서가 보장한다 — 잠금·새 도구를 두지 않는다. 원인 모를 「행이 사라짐」 실패가 나면 다른 실행이 같은 DB를 쓰고 있지 않은지 먼저 확인한다(`systematic-debugging`).
</execution_notes>

<probe_fallback>
이 플랜이 닫는 미해결 엣지: **ADMN-04 idempotency**(같은 값 재저장 — 「같은 값을 다시 저장해도」 truth) · **ADMN-04 concurrency**(두 관리자 동시 저장 + 제출 경주 — 통합 테스트로 증명하는 truth, 04.1-01의 한 문장 스냅숏 위). UI-SPEC 미해결 **U3**(설정 동적 옵션)을 해소한다 — 오케스트레이터 결정 5(레지스트리의 가장 작은 확장)를 따라 `SettingDef` 선택 필드(04.1-01이 타입만 더함)를 화면 쪽에서 해석한다.

**계획 가정**
1. 동적 옵션 목록은 보관되지 않은 계급·본부만 싣고, `admin.settings` 보기 권한만 요구한다(사람 관리 권한과 무관 — 설정 화면을 여는 사람이 곧 옵션을 고르는 사람이다). 저장된 id가 목록에 없으면(보관됨) 그 값을 이름 대신 `(보관됨)` 한 옵션으로 보여 주어 저장값이 조용히 바뀌지 않게 한다
2. 동적 옵션 값의 서버 측 존재 검증은 하지 않는다 — 없는 id는 결재 엔진에서 빈 자리가 되어 건너뛰거나 대표에게 간다(결재 없이 통과하지 않는다). 관리자 전용 입력이라 위협 수준 low(T-04.1-24)
3. 경고는 「조직 범위 = 특정 부서」인데 부서가 빈 값이거나 목록에 없을 때만 낸다. 다른 조합(예: 계급만 비어 있음)은 설계상 유효한 값이라 경고하지 않는다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 관리자가 설정 화면에서 이름으로 고른 결재선 값이 다음 제출부터 적용되고 진행 중 문서는 그대로 (화면 · 레지스트리 · 결재 엔진)</name>
  <files>domain/approvals/settings-options.ts, app/(app)/admin/settings/page.tsx, app/(app)/admin/settings/settings-form-client.tsx, test/integration/settings-approval-route.test.ts, test/e2e/settings-approval-route.spec.ts, playwright.config.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 370–378행(S8) · 219–220행(설정 라벨·힌트·오류·경고 원문) · 459행(overflow S8)
    - `app/(app)/admin/settings/page.tsx`(전문 96행) — `buildSections`와 보기 모델
    - `app/(app)/admin/settings/settings-form-client.tsx`(전문 275행) — enum select가 원시값을 라벨로 쓰는 자리, `SettingsFieldDescriptorView` 타입, 필드 아래 오류 한 줄 자리
    - `app/(app)/admin/settings/actions.ts`(전문 58행) — `setSimpleSettingAction`
    - `domain/settings/registry.ts`(04.1-01이 더한 선택 필드 셋) · `domain/settings/keys.ts`의 `approval_route.leave.*` 17키(04.1-01)
    - `domain/org/index.ts` 105–120행(본부 목록이 `scopeFor(viewer, "org_unit")`를 거쳐 사람 관리 권한에 묶이는 이유) · `domain/permissions/roles.ts` 75–95행(계급 목록의 정보 노출 투영) — **이 둘을 옵션 목록에 쓰지 않는다**(Codex MEDIUM)
    - `repositories/roles.ts`의 `listRoles(viewer, {includeArchived})` · `repositories/org-units.ts`의 `listOrgUnits(viewer, {scope})`(`Scope` = `{rows, includeArchived}`) — 옵션 함수가 직접 쓰는 리포지토리
    - `app/(app)/document-kinds.ts` · `test/unit/document-kinds-import.test.ts`(04.1-02 T1 ⑧ — `@/domain/approvals` 하위 경로를 import하는 app 파일은 `document-kinds`도 import)
    - `repositories/settings.ts`의 `findSimpleValues`(04.1-01 — 17키 한 문장 읽기) · `test/integration/approvals-concurrency.test.ts`의 멈춘 트랜잭션 · 경주 사례(04.1-01 T3 — 같은 모양을 설정 저장 경로로 한 번 더)
    - `domain/approvals/index.ts` · `domain/approvals/kinds.ts`(04.1-01) — submitDocument가 설정을 읽는 시점
    - `test/e2e/settings.spec.ts` — 설정 화면 E2E 모양과 관리자 픽스처
    - `test/e2e/fixtures.ts` — E2E가 도메인 함수를 직접 부르는 모양(복원에 같은 모양을 쓴다)
    - `playwright.config.ts`(전문 약 105행) — `projects` 배열, `MOBILE_SPEC_PATTERN`, mobile-375의 `dependencies: ["desktop"]` 직렬화 주석(CEO-14가 같은 방식을 한 칸 더 잇는다)
    - `domain/approvals/route.ts`(04.1-01 T3) — 폴백 규칙(차수 승인 0건 → 대표) 한 곳
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` CEO-1(132–137행) · CEO-14(449–454행)
  </read_first>
  <behavior>
    - (통합) 기본 설정으로 문서 A를 제출(단계 4) → `approval_route.leave.step2.enabled`를 거짓으로 저장 → 문서 B 제출 → B의 단계는 3개(팀장 · 경영관리본부 · 대표), A는 여전히 4개이고 A의 지금 단계 해석도 그대로
    - (통합) `approval_route.leave.self_approval`을 `self_approve`로 저장한 뒤 팀장이 기안한 문서 C는 1단 후보가 기안자 본인이고, 저장 전 제출한 문서 D(같은 팀장 기안)는 1단을 건너뛴 채 그대로다
    - (통합) `step2.role_id`를 다른 계급 id로 저장하면 그 뒤 문서만 2단 계급이 바뀐다. 같은 값을 두 번 저장해도 결과가 같다
    - (통합 · CEO-1) `step1`~`step4`의 `enabled`를 모두 거짓으로 저장 → 팀원이 연차 제출 → 상태 `submitted`(`approved` 아님), 지금 단계 = 대표 폴백 자리(`is_fallback`), 지금 담당 후보 = 대표 계급 재직자. 대표가 승인해야 `approved`가 된다
    - (통합 · Codex HIGH ADMN-04 concurrency) 관리자 둘이 `setSettingValue`로 `step2.role_id`를 각각 Y · Z로 저장하는 호출과 `submitLeave` 여섯 건을 `Promise.all`로 10번 겹쳐 돌리면: 오류 0건 · 그 키의 설정 행 1개 · 최종 값 ∈ {Y, Z} · 각 문서의 2단 계급 ∈ {X, Y, Z}(X = 시작 값)이고, 각 문서의 단계 행을 제출 직후에 읽은 값과 경주가 끝난 뒤 다시 읽은 값이 같다(사후 영향 없음)
    - (통합 · Codex MEDIUM) `admin.settings` view만 있고 `admin.people` 권한이 없는 계급의 사용자가 `listApprovalRouteOptions`를 부르면 보관되지 않은 계급 전부 · 본부 전부가 id·이름으로 나오고(`listRoles` · `listOrgUnits`와 개수 같음), `admin.settings` view가 없으면 `ForbiddenError`다
    - (E2E) 관리자가 `/admin/settings`를 열면 `연차 결재선` 섹션에 `자기 승인` select의 보이는 옵션이 `건너뜀`·`본인 승인`이고, `1단 담당 계급` select 옵션에 `팀장`이 이름으로 있으며, `3단 특정 부서`의 선택값이 `경영관리본부`다. 날것 키(`self_approve`·`drafter_team`·`role-team-lead`)가 화면 글자에 없다
    - (E2E · CEO-14) `자기 승인`을 `본인 승인`으로 바꾸면 즉시 저장되고 새로 고쳐도 `본인 승인`이다. 테스트는 `finally`에서 원래 값(`skip`)을 도메인 함수로 다시 쓴다. 스펙은 `N단 사용` 체크를 누르지 않는다(다른 연차 스펙의 기대 단계 수가 바뀐다)
    - (E2E · CEO-14) 스펙의 **마지막** 테스트가 화면을 다시 열어 `자기 승인`이 `건너뜀`임을 확인한다 — 앞 테스트의 복원 증명
  </behavior>
  <action>
① **옵션 함수(Codex MEDIUM)** `domain/approvals/settings-options.ts`의 `listApprovalRouteOptions(viewer, deps?)`: `can(viewer, "admin.settings", "view")`가 거짓이면 `ForbiddenError`, 참이면 `repositories/roles.ts` `listRoles(viewer, {includeArchived: true})`와 `repositories/org-units.ts` `listOrgUnits(viewer, {scope: {rows: "all", includeArchived: true}})`를 한 번씩 읽어 `{roles, orgUnits}`를 각각 `{id, name, archived}` 배열로만 돌려준다 — 조직·계급 도메인의 목록 함수(사람 관리 권한 범위 판정·정보 노출 투영)를 거치지 않는다. 보관된 항목은 계획 가정 1의 `(보관됨)` 표시에만 쓰고 새 선택지로는 싣지 않는다. `page.tsx`의 `buildSections`: 키 정의에 `optionLabels`가 있으면 기술자의 정적 옵션을 `{value, label}`로 바꾸고, `dynamicOptions`가 `roles`면 계급 목록, `org_units`면 본부 목록을 이 함수 결과에서 `{value: id, label: 이름}`으로 만든 뒤 맨 앞에 `{value: "", label: "—"}`를 둔다(계획 가정 1의 보관됨 처리 포함). `page.tsx`는 `@/domain/approvals/settings-options`를 import하므로 이 task에서 `import "@/app/(app)/document-kinds"` 한 줄도 더한다(CEO-3 가드 조건 (a) — 연차 모듈을 직접 import하지 않는다). 보기 모델에 선택 필드 `options`를 더한다. 목록 읽기는 섹션을 그릴 때 한 번씩만(키마다 다시 읽지 않는다). 계급·문서 종류 이름으로 분기하지 않는다(금지 항목).

② `settings-form-client.tsx`: 보기 모델에 `options`가 있으면 기술자 종류(`string`이든 `enum`이든)와 무관하게 select를 그리고 `option`의 보이는 글자를 `label`로 쓴다. 기존 enum select가 원시값을 쓰던 자리만 바꾼다 — 저장 액션 호출·즉시 저장(change) 동작은 그대로.

③ 통합 테스트 `test/integration/settings-approval-route.test.ts`가 behavior 통합 여섯 줄을 단언한다(설정 저장은 `setSettingValue` 도메인 함수로, 제출은 `submitLeave`로). 동시 저장 경주 사례는 새 잠금·새 코드를 만들지 않는다 — 04.1-01의 한 문장 스냅숏과 기존 upsert가 이 truth를 이미 만족하는지 증명하는 테스트다. 실패하면 이 플랜에서 덧대지 말고 `systematic-debugging`으로 04.1-01의 `findSimpleValues` · `getSimpleSettingValues` 경로를 고친다. 권한 사례의 계급은 `createRole` + `setPermissionCell`로 테스트 안에서 만든다. 네 단계 모두 끔 사례(CEO-1)는 폴백 규칙을 새로 만들지 않는다 — 04.1-01 T3가 만든 규칙이 설정 경로에서도 걸리는지만 단언한다. 실패하면 이 플랜에서 규칙을 덧대지 말고 `systematic-debugging`으로 04.1-01의 `route.ts`를 고친다.

④ E2E `test/e2e/settings-approval-route.spec.ts`: behavior E2E 세 줄(CEO-14). 바꾼 값 복원은 `finally` 안에서 `setSettingValue`를 도메인으로 직접 불러 한다(`fixtures.ts` 모양 — 화면이 깨진 채 끝나도 복원된다. 설정 레지스트리에 프로세스 캐시가 없어 서버가 즉시 새 값을 읽는다). 복원 확인 테스트는 파일 맨 끝에 둔다(파일 안 테스트는 선언 순서로 돈다 — `fullyParallel: false`). 기존 `settings.spec.ts`가 회귀하지 않아야 한다.

⑤ `playwright.config.ts`(CEO-14): `projects`에 `desktop-settings` 항목 하나를 더한다 — `testMatch`는 설정 결재선 스펙 파일 이름, `dependencies: ["mobile-375"]`, 주석 두 줄(공유 `erp_test`의 전역 결재선을 바꾸는 스펙이라 다른 모든 스펙 뒤에 돈다 · 스펙 안 `finally` 복원과 함께 쓰는 이중 장치). `desktop`의 `testIgnore`를 `[MOBILE_SPEC_PATTERN, 설정 결재선 스펙 파일 이름]` 배열로 바꾼다. 다른 줄은 바꾸지 않는다(execution_notes).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/settings-approval-route.test.ts test/integration/settings.test.ts</automated>
    <fails_when>설정 변경 뒤 진행 중 문서의 단계 수·계급이 바뀌거나, 새 문서에 변경이 반영되지 않거나, 네 단계를 모두 끈 뒤 제출한 연차가 `approved`이거나 지금 단계가 대표 폴백이 아니거나(CEO-1), 동시 저장 경주에서 오류·행 2개·{X, Y, Z} 밖 계급·제출 뒤 단계 행 변화가 한 번이라도 나오거나, 설정 보기 권한만 가진 사용자의 옵션 목록이 리포지토리 목록보다 짧거나, 기존 설정 통합 테스트가 회귀하면 failed가 1 이상이다</fails_when>
    <automated>! pnpm playwright test --list --project desktop --no-deps | grep -q "settings-approval-route" &amp;&amp; pnpm playwright test --list --project desktop-settings --no-deps | grep -q "settings-approval-route"</automated>
    <fails_when>`desktop` 목록에 설정 결재선 스펙이 나오거나(병렬 워커에서 전역 결재선을 바꿈) `desktop-settings` 목록에 없으면 종료 코드가 0이 아니다(CEO-14)</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/settings-approval-route.spec.ts --project desktop-settings --no-deps &amp;&amp; pnpm playwright test test/e2e/settings.spec.ts --project desktop --no-deps</automated>
    <fails_when>playwright failed가 1 이상이다 — 화면에 날것 키가 보이거나, `자기 승인` 즉시 저장이 새로 고침 뒤 유지되지 않거나, 마지막 복원 확인 테스트에서 `자기 승인`이 `건너뜀`이 아니면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `settings-form-client.tsx` diff가 select 옵션 렌더 부분에 국한된다(`git diff --stat "$PLAN_BASE"..HEAD -- "app/(app)/admin/settings/settings-form-client.tsx"`의 변경 줄 수가 30 이하)
    - `page.tsx`·`settings-form-client.tsx`에 `role-`·`leave`·`approval_route` 문자열 분기가 없다(`grep -v '^\s*//' "app/(app)/admin/settings/page.tsx" "app/(app)/admin/settings/settings-form-client.tsx" | grep -cE "role-|approval_route"`이 0)
    - `domain/settings/keys.ts` diff가 이 플랜에서 비어 있다
    - (CEO-1) 통합 테스트에 네 단계 모두 끔 사례가 있고 녹색이다 — 이 플랜에서 `domain/approvals/route.ts` diff가 비어 있다(`git diff --stat "$PLAN_BASE"..HEAD -- domain/approvals/route.ts`가 비어 있음 — 규칙은 04.1-01 몫)
    - (CEO-14) `grep -c "finally" test/e2e/settings-approval-route.spec.ts`이 1 이상이고, `grep -cE "[1-4]단 사용" test/e2e/settings-approval-route.spec.ts`이 0이다
    - (CEO-14) `grep -c "desktop-settings" playwright.config.ts`이 1 이상이고 `git diff "$PLAN_BASE"..HEAD -- playwright.config.ts`의 변경이 `projects` 배열 안에 국한된다(`webServer` 블록 불변 — `D=$(git diff "$PLAN_BASE"..HEAD -- playwright.config.ts) && printf '%s\n' "$D" | grep -cE "^[-+].*(webServer|DATABASE_URL|BETTER_AUTH|RATE_LIMIT)"`이 0 — git이 실패하면 `&&`에서 멈춰 공허한 통과가 없다)
    - (Codex MEDIUM) `domain/approvals/settings-options.ts`가 `scopeFor` · `@/domain/org` · `@/domain/permissions/roles`를 import하지 않는다(`grep -cE "scopeFor|@/domain/org|@/domain/permissions/roles" domain/approvals/settings-options.ts`가 0) · `page.tsx`에 `document-kinds` import가 있다
    - (Codex HIGH) 동시 저장 + 제출 경주 사례가 통합 테스트에 있고 녹색이다
    - 통합 여섯 사례와 E2E 세 사례가 녹색이다
  </acceptance_criteria>
  <done>관리자가 이름으로 고른 결재선 값이 다음 제출부터만 적용되고, 단계를 모두 꺼도 대표 폴백으로 결재가 남으며, 설정 E2E가 다른 스펙과 겹치지 않는 자리에서 돌고 값을 되돌린다는 것이 화면과 엔진 양쪽에서 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 「부서 없음」 경고와 이력형 적용 시작일 규칙(1월 1일 · 지난 연도 거부 — 일반 저장과 JSON 가져오기 두 경로)</name>
  <files>domain/settings/registry.ts, domain/settings/export.ts, test/integration/settings-export.test.ts, domain/approvals/settings-warnings.ts, app/(app)/admin/settings/page.tsx, app/(app)/admin/settings/settings-form-client.tsx, app/(app)/admin/settings/settings.module.css, test/unit/settings/effective-from-rule.test.ts, test/unit/domain/approvals/settings-warnings.test.ts, test/e2e/settings-approval-route.spec.ts</files>
  <read_first>
    - `domain/settings/registry.ts` 50–60행(`RegistryDeps`) · 68–91행(`getSettingValue` — asOf 유효값, 행이 없으면 기본값) · 120–160행(`addHistorizedValue`와 deps 주입 — 137–139행이 스키마 파싱 직후 날짜 검사 없이 저장한다)
    - `domain/settings/export.ts` 71–145행(`ImportDeps` · `importSettings` — 116행은 이력 항목의 값 스키마만 보고, 140행 `applySettingsImport`가 `addHistorizedValue`를 거치지 않고 직접 쓴다) · `repositories/settings.ts`의 `applySettingsImport`(이미 있는 (key, effectiveFrom)은 `onConflictDoNothing`으로 건너뜀 — 멱등)
    - `test/integration/settings-export.test.ts`(전문 — (a) 빈 환경 왕복 · (b) 두 번 가져오기 멱등 · (c) 전부 또는 없음. `test/integration/setup.ts`가 매 테스트 전 `seedMasterData`로 모든 이력형 키의 `2000-01-01` 기준 행을 넣으므로 내보내기에 늘 그 행이 있다)
    - `lib/dates.ts`(04.1-01 — `seoulToday`)
    - `test/unit/settings/` 안의 registry 단위 테스트 하나 — deps 주입으로 DB 없이 시험하는 모양
    - `domain/approvals/kinds.ts`(04.1-01 — `routeSettings` 선택 필드)
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 220행(경고 원문) · 443행(S8 error 행 — 경고는 저장을 막지 않는다)
    - `app/(app)/admin/settings/settings.module.css` 55–70행(`.hint` · `.error` — 경고 한 줄 클래스가 같은 모양을 따른다)
  </read_first>
  <behavior>
    - (단위) `effectiveFromRule: "year_start"`인 테스트용 이력형 정의에 `2027-03-01`을 넣으면 `적용 시작일은 1월 1일 · 2027-01-01처럼 적어 주세요`로 거부되고 저장 함수가 호출되지 않는다. `garbage-01-01` · `2027-1-1` · `0000-01-01`도 같은 문구로 거부된다(Codex MEDIUM — DB까지 가지 않는다). `2027-01-01`은 저장된다. 규칙이 없는 이력형 정의는 어떤 날짜든 기존대로 저장된다
    - (단위 · ENG-5 일반 저장 경로 회귀) `deps.now` = 2026-09-24T03:00:00Z(서울 2026-09-24)로 `year_start` 테스트 정의에 `2025-01-01`을 넣으면 `지난 연도는 바꿀 수 없습니다 · 지난 잔고는 사람 상세의 연차 조정으로 고쳐 주세요`로 거부되고 `insertHistorizedValue` 주입 함수가 호출되지 않는다. `2026-01-01`(올해) · `2027-01-01`은 저장된다. 서울 연도 경계: `deps.now` = 2026-12-31T15:00:00Z(서울 2027-01-01 00:00)면 `2026-01-01`이 거부되고, 2026-12-31T14:59:59Z면 통과한다. 규칙 없는 이력형 정의는 `2020-03-01`도 기존대로 저장된다
    - (단위 · ENG-5) `validateEffectiveFrom`을 직접 부른 사례표 — 규칙 없음 → null · `2027-03-01` → 형식(1월 1일) · `2025-01-01`(오늘 2026-09-24) → 지난 연도 · `2026-01-01` → null
    - (통합 · ENG-5 가져오기 경로 회귀, `test/integration/settings-export.test.ts`, 전부 `deps.now` = 2026-09-24T03:00:00Z) (e) `leave.annual_days` `[{2025-01-01, 20}]`와 유효한 단순 키 값 하나를 함께 가져오면 `ImportValidationError`이고 issues에 `지난 연도`가 있으며, `listSettingHistory(LEAVE_ANNUAL_DAYS)`와 그 단순 키 값이 전과 같다(전부 또는 없음). (f) `[{2000-01-01, 15}, {2027-01-01, 16}]`은 통과 — `2000-01-01`은 그날 이미 유효한 값(시드 15)과 같은 무변화 행이라 허용되고 이력에는 `2027-01-01` 행만 늘어난다. (g) `[{2026-03-01, 16}]`은 `적용 시작일은 1월 1일` 문구로 거부(지금까지 가져오기는 1월 1일 규칙도 건너뛰었다). (h) `[{2026-01-01, 16}]`(올해)은 통과. 기존 (a) 빈 환경 왕복 · (b) 멱등 · (c) 전부 또는 없음 사례가 그대로 녹색이다 — 내보내기에 늘 들어 있는 `2000-01-01` 시드 행이 무변화 규칙으로 통과하기 때문
    - (단위) 등록된 종류의 결재선 설정이 `3단 조직 범위 = 특정 부서`, `3단 특정 부서 = ""`이면 경고 맵에 3단 부서 키 → `부서 없음 · 이 단계는 빈 자리로 건너뜀`. 부서 id가 본부 목록에 없어도 같은 경고. 조직 범위가 `기안자 팀`이면 부서 칸이 비어도 경고 없음. `routeSettings`가 없는 종류는 순회하지 않는다
    - (E2E) 관리자가 `3단 특정 부서`를 `—`로 바꾸면 그 칸 아래에 경고 한 줄이 보이고 새로 고쳐도 값이 `—`로 저장되어 있다(저장은 막히지 않는다). 테스트는 `finally`에서 원래 부서 id(경영관리본부)를 도메인 함수로 다시 쓴다(CEO-14 — 비운 3단은 다른 연차 스펙의 4단 기대를 깬다)
    - (E2E · CEO-14) 마지막 복원 확인 테스트가 `3단 특정 부서` 선택값 = `경영관리본부`도 확인한다
    - (단위 · CEO-3) 04.1-02의 가드 테스트가 `admin/settings/page.tsx`를 조건 (a)에 걸린 파일로 잡고 녹색이다
  </behavior>
  <action>
① **공통 검증(ENG-5)** `domain/settings/registry.ts`에 `validateEffectiveFrom(def, effectiveFrom, today)`를 export한다 — `def.effectiveFromRule !== "year_start"`면 null(규칙 없는 키의 동작은 한 글자도 바꾸지 않는다). 규칙이 있으면 적용 시작일을 **날짜로 해석**한다: `^(\d{4})-(\d{2})-(\d{2})$` 형식, 연도 1 이상, 월 = `01` · 일 = `01`(1월 1일은 모든 해에 실재하므로 형식·연도·월일 세 검사로 충분하다) — 어긋나면 `{reason: "format", message: "적용 시작일은 1월 1일 · 2027-01-01처럼 적어 주세요"}`(접미사 검사 금지 — Codex MEDIUM). 형식이 맞고 연도 < `today`의 연도면 `{reason: "past_year", message: "지난 연도는 바꿀 수 없습니다 · 지난 잔고는 사람 상세의 연차 조정으로 고쳐 주세요"}`, 아니면 null. `today`는 호출자가 `seoulToday(deps.now)`로 만든 `YYYY-MM-DD`다(`RegistryDeps`·`ImportDeps`에 `now?: Date`를 더한다 — 04.1-01 `lib/dates.ts` 재사용, 새 날짜 코드 없음).

①-a **일반 저장 경로** `addHistorizedValue`: 권한 판정 뒤, 저장(지금 138–139행 `insertHistorizedValue`) 전에 `validateEffectiveFrom(def, input.effectiveFrom, seoulToday(deps?.now))`을 부르고 결과가 있으면 `UserFacingError` 하위 새 오류 클래스로 그 `message`를 던진다 — `insertHistorizedValue`가 불리지 않는다.

①-b **가져오기 경로** `domain/settings/export.ts` `importSettings`: 이력 항목 루프에서 값 스키마 검사(지금 116행) 뒤에 같은 `validateEffectiveFrom`을 부른다. `format`이면 issue(`'${key}'(${effectiveFrom}) ` + message). `past_year`면 그 항목이 **무변화 행**인지 본다 — `getSettingValue(def, {asOf: 그 날짜}, deps)`(대상 환경에서 그날 이미 유효한 값: 기존 행 또는 기본값)와 파싱한 값이 구조적으로 같으면(`node:util` `isDeepStrictEqual` — 새 의존성 없음) 통과, 다르면 issue. 무변화 행은 그 날짜 이후 어느 날의 유효값도 바꾸지 않으므로 「지난 연도 잔고 불변」이 선다 — 이 예외가 없으면 모든 내보내기에 들어 있는 시드 `2000-01-01` 행 때문에 기존 왕복 사례 (a)가 깨진다. issue가 하나라도 있으면 기존대로 `ImportValidationError`로 아무것도 쓰지 않는다(140행 `applySettingsImport` 앞). 지난 연도 정정은 가져오기가 아니라 04.1-03 `addLeaveAdjustment`(연차 조정, `fiscalYear` = 그 연도)로 한다.

② `domain/approvals/settings-warnings.ts`의 `listApprovalRouteSettingWarnings(viewer)`: 등록된 문서 종류 중 `routeSettings`가 있는 것만 순회해 단계마다 조직 범위 값과 부서 값을 읽고, 「특정 부서인데 비었거나 본부 목록에 없음」이면 그 단계 부서 키 → 경고 문구를 맵에 담아 돌려준다(계획 가정 3). 순수 판정 부분을 분리해 단위 테스트가 DB 없이 돌게 한다(`deps` 주입 모양).

③ `page.tsx`가 경고 맵을 한 번 읽어 보기 모델의 선택 필드 `warning`에 싣고(문서 종류 등록은 T1이 더한 `@/app/(app)/document-kinds` import 한 줄로 보장된다 — 04.1-02 ⑧ · CEO-3. 연차 모듈을 이 파일에서 직접 import하지 않는다), `settings-form-client.tsx`는 `warning`이 있으면 그 칸 아래에 경고 한 줄을 그린다 — `settings.module.css`에 기존 `.error`와 같은 모양에 `color: var(--warning)`만 다른 `.warning` 클래스를 더해 쓴다(Codex LOW — 새 토큰 없음, 기존 규칙은 고치지 않는다). 이력형 추가의 서버 오류는 기존 오류 자리에 그대로 나온다.

④ 단위 두 파일과 E2E 두 사례(경고 · 복원 확인 확장)를 behavior대로 더한다. 3단 부서 복원은 T1 ④와 같은 `finally` + 도메인 함수 모양이다(CEO-14).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/settings test/unit/domain/approvals/settings-warnings.test.ts test/unit/document-kinds-import.test.ts test/unit/import-cycles.test.ts</automated>
    <fails_when>(ENG-5) 지난 연도 일반 저장이 통과하거나 `insertHistorizedValue`가 불리거나, 서울 연도 경계 사례가 UTC 연도로 판정되면 failed가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/settings-export.test.ts test/integration/settings.test.ts</automated>
    <fails_when>(ENG-5) 값이 다른 지난 연도 항목의 가져오기가 통과하거나 일부만 적용되거나, 무변화 지난 행 + 미래 행 가져오기가 거부되거나, 1월 1일이 아닌 항목이 가져오기로 들어가거나, 기존 (a)(b)(c) 왕복·멱등·원자성 사례가 회귀하면 failed가 1 이상이다</fails_when>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 규칙 없는 이력형 키의 기존 동작이 바뀌면 기존 registry 테스트가, `page.tsx`가 `@/domain/approvals/settings-warnings`를 import하면서 `document-kinds`를 import하지 않으면 가드 테스트가(CEO-3) 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/settings-approval-route.spec.ts --project desktop-settings --no-deps &amp;&amp; pnpm playwright test test/e2e/settings.spec.ts --project desktop --no-deps</automated>
    <fails_when>playwright failed가 1 이상이다 — 경고가 안 보이거나, 저장이 막히거나, 마지막 복원 확인 테스트에서 `3단 특정 부서`가 `경영관리본부`가 아니면(CEO-14) 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - 적용 시작일 규칙이 레지스트리의 일반 규칙이고 키 이름을 모른다(`grep -c "leave.annual_days" domain/settings/registry.ts`가 0)
    - 경고가 저장을 막지 않는다는 E2E 단언이 있다
    - `settings-form-client.tsx`의 이 플랜 전체 diff가 50줄 이하다
    - 새 색 토큰이 없다(`git diff "$PLAN_BASE"..HEAD -- docs/design/tokens.css app/globals.css`가 비어 있다) · `settings.module.css` diff가 `.warning` 규칙 추가뿐이고 `var(--warning)`을 쓴다
    - (ENG-5) 공통 검증이 한 곳이고 두 경로가 부른다: `grep -c "export function validateEffectiveFrom" domain/settings/registry.ts`가 1 · `grep -c "validateEffectiveFrom" domain/settings/export.ts`가 1 이상 · `addHistorizedValue` 본문에서 `validateEffectiveFrom` 호출이 `insertHistorizedValue` 호출보다 앞 줄이다
    - (ENG-5) `test/integration/settings-export.test.ts`에 지난 연도 거부 · 무변화 통과 · 1월 1일 아닌 날짜 거부 · 올해 허용 네 사례가 있고 전부 `deps.now`를 주입한다 · `test/unit/settings/effective-from-rule.test.ts`에 지난 연도 저장 거부와 서울 연도 경계 사례가 있다
    - `domain/settings/registry.ts`에 `endsWith("-01-01")`이 없다(`grep -c 'endsWith("-01-01")' domain/settings/registry.ts`가 0) · 단위 테스트에 `garbage-01-01` 사례가 있다
    - (CEO-3) `grep -c "document-kinds" "app/(app)/admin/settings/page.tsx"`이 1 이상이고(T1에서 더함) `grep -c 'import "@/domain/leave"' "app/(app)/admin/settings/page.tsx"`이 0이다
    - (CEO-14) `grep -c "finally" test/e2e/settings-approval-route.spec.ts`이 2 이상이다(자기 승인 · 3단 부서)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>특정 부서가 빈 결재선 단계에 경고가 보이고(저장은 됨), 이력형 연차 일수가 올해 이후의 1월 1일에만 적용되도록 레지스트리의 공통 검증이 일반 저장과 JSON 가져오기 두 경로에서 강제한다 — 지난 연도 잔고가 어느 경로로도 소급해 바뀌지 않는다(ENG-5)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 브라우저 → 설정 저장 액션 | 결재선 설정 값이 클라이언트에서 온다 |
| 설정 → 진행 중 문서 | 설정이 이미 제출된 문서에 새어 들어가면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-22 | Elevation of Privilege | 설정 저장 | high | mitigate | 기존 `admin.settings` write 판정(레지스트리 `setSettingValue`·`addHistorizedValue`)을 그대로 쓴다 — 이 플랜은 판정을 우회하는 경로를 만들지 않는다. 기존 settings 통합 테스트가 회귀 방지 |
| T-04.1-23 | Tampering | 진행 중 문서 결재선 | medium | mitigate | 제출 때 단계 행으로 고정(04.1-01). 설정 변경 뒤 진행 중 무영향을 통합 테스트 세 사례가 단언 |
| T-04.1-24 | Tampering | 동적 옵션 값(계급·부서 id) | low | accept | 관리자 전용 입력. 없는 id는 엔진에서 빈 자리 → 건너뜀/대표 폴백으로 처리되어 결재 없이 통과하지 않는다(계획 가정 2). 모든 단계가 빈 자리가 되어도 04.1-01의 「차수 승인 0건 → 대표 폴백」 규칙(CEO-1 · T-04.1-41)이 걸리므로 이 문장이 참이다 |
| T-04.1-46 | Elevation of Privilege | 설정으로 결재선 비우기(네 단계 모두 끔) | high | mitigate | 무결재 승인 경로가 없다 — 04.1-01 T3 폴백 규칙을 이 플랜 T1 통합 사례가 설정 저장 경로(`setSettingValue` → `submitLeave`)로 증명한다: 상태 `submitted`, 지금 단계 = 대표 폴백(CEO-1) |
| T-04.1-25 | Tampering | 이력형 연차 일수 적용 시작일 | high | mitigate | 공통 검증 `validateEffectiveFrom`(형식 · 연도 · 01-01 · 적용 연도 ≥ `seoulToday` 연도)을 일반 저장 `addHistorizedValue`와 JSON 가져오기 `importSettings` **두 경로**가 저장 전에 부른다(ENG-5 — 가져오기가 `applySettingsImport`로 직접 쓰던 우회 차단). 가져오기의 지난 연도 항목은 그날 유효값과 같은 무변화 행만 통과. 연도 중간·지난 연도 적용으로 과거 잔고가 흔들리는 것과 `garbage-01-01`이 DB 오류가 되는 것을 막는다. 두 경로 회귀 테스트(단위 · 통합) |
| T-04.1-26 | Repudiation | 결재선 설정 변경 | low | mitigate | 기존 `settings_change` 행동 로그 경로를 그대로 탄다(이 플랜이 바꾸지 않음) |
| T-04.1-58 | Tampering | 두 관리자 저장 사이의 제출(설정 혼합) | high | mitigate | 제출은 04.1-01 `findSimpleValues` 한 문장으로 17키를 읽어 한 커밋 시점 값 한 벌을 고정. 동시 저장 + 제출 경주 10회 통합 사례가 행 1개 · 값 범위 · 사후 불변을 단언(Codex HIGH). 칸별 즉시 저장 사이의 중간 조합은 유효 값이고 폴백 규칙(T-04.1-46)으로 무결재 승인이 없다 |
| T-04.1-59 | Information Disclosure / DoS | 설정 화면 동적 옵션 | low | mitigate | `listApprovalRouteOptions`가 `admin.settings` view 판정 뒤 id·이름·보관 여부만 돌려준다 — 사람 관리 권한 없는 설정 관리자에게 빈 목록이 가지 않고, 이름 외 필드는 실리지 않는다(Codex MEDIUM) |
| T-04.1-47 | Tampering | 공유 `erp_test`의 전역 결재선(테스트 무결성) | low | mitigate | 설정 E2E가 바꾼 값을 `finally`에서 되돌리고, 별도 프로젝트 `desktop-settings`(다른 모든 스펙 뒤)에서만 돈다. 복원 확인 테스트와 `--list` 검사가 증명(CEO-14) |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test:unit && pnpm test:integration` 통과
- `pnpm playwright test test/e2e/settings-approval-route.spec.ts --project desktop-settings --no-deps` · `pnpm playwright test test/e2e/settings.spec.ts --project desktop --no-deps` 통과
- `pnpm playwright test --list --project desktop --no-deps`에 설정 결재선 스펙이 없고 `--project desktop-settings --no-deps`에 있다(CEO-14)
- `pnpm test:e2e:ci` 한 번 녹색 — 리셋 + CI 빌드로 `desktop` → `mobile-375` → `desktop-settings` 순서 전부를 한 실행에서 돌려, 설정 스펙이 연차 스펙들보다 뒤에 돈다는 것을 실측한다(CEO-14)
- `git diff --stat "$PLAN_BASE"..HEAD -- domain/settings/keys.ts db/ package.json pnpm-lock.yaml` 비어 있음
- 독립 DOM 감사(별도 에이전트, `CI=true`, 폭 1280 · 375)가 `/admin/settings` `연차 결재선` 섹션 17칸의 세로 배치 · 날것 키 0 · 경고 한 줄을 실측
</verification>

<success_criteria>
- 관리자가 설정 화면에서 결재 단계·계급·범위·부서·규칙 on/off·자기 승인을 이름으로 바꾼다
- 바꾼 값이 새 제출에만 반영되고 진행 중 문서는 그대로다
- 비어 있는 특정 부서가 경고로 보인다
- 연차 일수 이력이 1월 1일에만, 지난 연도가 아닌 해에만 시작된다 — 일반 저장과 JSON 가져오기 모두(ENG-5)
- 결재 단계를 모두 꺼도 연차는 대표에게 간다(CEO-1)
- 설정 E2E가 다른 스펙의 결재선 기대를 흔들지 않는다(CEO-14)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 레지스트리 동작 | `effectiveFromRule: "year_start"` 강제(공통 검증 `validateEffectiveFrom` — 1월 1일 · 지난 연도 거부, 일반 저장 · JSON 가져오기 두 경로, ENG-5) · `optionLabels`·`dynamicOptions` 화면 해석 |
| Playwright 프로젝트 | `desktop-settings`(설정 결재선 스펙 전용 · `dependencies: ["mobile-375"]`) · `desktop` `testIgnore` 배열화(CEO-14) |
| 함수 | `listApprovalRouteSettingWarnings` · `listApprovalRouteOptions`(`domain/approvals/settings-options.ts`) |
| 화면 변경 | `/admin/settings` 섹션 `연차 결재선`(이름 옵션 · 경고) · `연차`(이력형 규칙) |
| 테스트 | `test/unit/settings/effective-from-rule.test.ts` · `test/unit/domain/approvals/settings-warnings.test.ts` · `test/integration/settings-approval-route.test.ts` · `test/integration/settings-export.test.ts`(가져오기 경로 네 사례 추가 — ENG-5) · `test/e2e/settings-approval-route.spec.ts` |
| 변경된 기존 심볼 | `addHistorizedValue`(규칙 검사 — 날짜 해석 · 지난 연도) · `importSettings`(이력 항목 공통 검증 · 무변화 지난 행만 통과) · `RegistryDeps`·`ImportDeps`(`now`) · `buildSections`(옵션·경고) · 설정 select 렌더 · 설정 `page.tsx`의 `document-kinds` import(CEO-3) · `settings.module.css`(`.warning` 추가) |
| 증명된 동시성 | 두 관리자 동시 저장 + 제출 경주 — 행 1개 · 마지막 커밋 · 문서 단계 사후 불변(Codex HIGH) |
| 증명된 불변식 | 네 단계 모두 끔 → 대표 폴백, 무결재 승인 없음(CEO-1 — 규칙은 04.1-01) |


## Review Dispositions Ledger

### Round 1 — 1b86370

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ADMN-04 동시성 보장이 구현·시험되지 않음 — 각 설정이 독립 upsert(L127@1b86370) | HIGH | 구현은 04.1-01 Task 1 ②⑥(`findSimpleValues` 한 문장 → 한 커밋 시점 한 벌). 이 플랜은 backstop truth를 test로 올리고 Task 1 ③에 두 관리자 동시 저장 + 제출 여섯 건 × 10회 경주(행 1개 · 최종 값 ∈ {Y, Z} · 단계 사후 불변) · T-04.1-58. 칸별 즉시 저장 UI라 「여러 칸 묶음 저장」 원자성은 없음을 truth에 명시 — 중간 조합은 유효 값이고 폴백 규칙이 무결재 승인을 막는다 |
| 설정 관리자에게 동적 옵션이 비거나 부분 투영(L128@1b86370) | MEDIUM | Plan 04, Task 1 ①: `listApprovalRouteOptions` — `admin.settings` view 판정 뒤 리포지토리 `listRoles` · `listOrgUnits` 직접(scopeFor·투영 없음) · 설정 권한만 가진 계급의 목록 완전성 통합 사례 · T-04.1-59 |
| `endsWith("-01-01")`은 날짜 검증이 아님(L129@1b86370) | MEDIUM | Plan 04, Task 2 ①: 형식 정규식 · 연도 ≥ 1 · 월일 01-01 해석, `garbage-01-01` · `2027-1-1` · `0000-01-01` 단위 사례, `endsWith` 0건 수락 기준 |
| 경고 CSS 파일이 변경 목록에 없음(L130@1b86370) | LOW | Plan 04, Task 2: `settings.module.css`를 files에 · `.warning`(`var(--warning)`) 추가만 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Codex 지적은 전부 반영했다. 규칙 없는 이력형 키의 적용 시작일 형식 검사(액션의 `z.string().min(1)`)는 Phase 3 기존 동작이고 지적 범위 밖이라 이 플랜이 바꾸지 않는다 |

### Round 2 — /plan-eng-review 2026-09-24(docs/designs/plant8-erp-phase04.1-eng-review-260924.md)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 5 「지난 연도 잔고는 소급해 바뀌지 않는다」 미보장 — `year_start`가 과거 연도 1월 1일을 통과시키고, JSON 가져오기(`domain/settings/export.ts:116,140`)가 `addHistorizedValue`(`domain/settings/registry.ts:137-139`)를 거치지 않아 검사를 우회 | BLOCKER | truth 추가 · Task 2 ①(공통 검증 `validateEffectiveFrom`) · ①-a 일반 저장 · ①-b 가져오기(지난 연도는 무변화 행만 — 기존 왕복 (a) 보존) · 단위(지난 연도 저장 거부 · 서울 연도 경계) · 통합(가져오기 네 사례) · T-04.1-25 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 지난 연도 가드를 규칙 없는 이력형 키(부가세율 등)로 넓히기 | 범위 밖 — ENG-5는 연차 잔고 불변(`year_start` 키)이 대상이다. 세율 이력은 Phase 5 소관이고, 기존 `settings-export.test.ts` (a)가 `TAX_VAT_RATE`의 `2026-01-01` 행을 넣으므로 넓히면 해가 바뀔 때 기존 테스트가 깨진다 |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-04-SUMMARY.md` when done
</output>
