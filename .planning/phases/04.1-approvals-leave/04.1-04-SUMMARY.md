---
phase: 04.1-approvals-leave
plan: 04
subsystem: settings
tags: [settings-registry, approvals, admin-settings, playwright, effective-date]

requires:
  - phase: 04.1-01
    provides: 결재선 17키(optionLabels · dynamicOptions · 키 스키마) · findSimpleValues 한 문장 스냅숏 · 대표 폴백 규칙 · lib/dates seoulToday
  - phase: 04.1-02
    provides: app/(app)/document-kinds.ts 등록 한 곳과 가드 테스트(CEO-3)
  - phase: 04.1-03
    provides: LEAVE_ANNUAL_DAYS(effectiveFromRule year_start) · 연차 조정(지난 연도 정정 경로)
provides:
  - listApprovalRouteOptions · routeActiveWhen · isSettingActive (domain/approvals/settings-options.ts)
  - listApprovalRouteSettingWarnings (domain/approvals/settings-warnings.ts)
  - validateEffectiveFrom 공통 검증 + EffectiveFromRuleError (domain/settings/registry.ts)
  - 설정 화면 이름 옵션 · 비활성 칸 · 경고 한 줄
  - Playwright 프로젝트 desktop-settings
affects: [04.1-05, 04.1-06, 04.1-07, Phase 5 문서 종류 등록, 설정 가져오기 운영]

plan_head_before: 5cdd5fb471e48d030f21223f4d72e3a04b86695e
actuals:
  tokens: 12299
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - "키 정의의 optionLabels · dynamicOptions를 page.tsx가 {value,label} 옵션으로 해석 — 화면 코드에 키 · 계급 이름 분기 없음"
    - "켜짐 조건(activeWhen)은 등록된 문서 종류의 routeSettings에서 파생, 서버가 저장값으로 판정해 보기 모델 disabled로 내림"
    - "이력형 적용 시작일 규칙 = validateEffectiveFrom 한 곳, 저장 · 가져오기 · 취소 세 경로가 부름"

key-files:
  created:
    - domain/approvals/settings-options.ts
    - domain/approvals/settings-warnings.ts
    - test/unit/domain/approvals/settings-options.test.ts
    - test/unit/domain/approvals/settings-warnings.test.ts
    - test/unit/settings/effective-from-rule.test.ts
    - test/integration/settings-approval-route.test.ts
    - test/e2e/settings-approval-route.spec.ts
  modified:
    - app/(app)/admin/settings/page.tsx
    - app/(app)/admin/settings/settings-form-client.tsx
    - app/(app)/admin/settings/settings.module.css
    - domain/settings/registry.ts
    - domain/settings/export.ts
    - test/integration/settings-export.test.ts
    - playwright.config.ts

key-decisions:
  - "동적 옵션의 빈 값 라벨은 키 정의 optionLabels[\"\"](계급 키 = 계급 무관), 없으면 —(부서 키) — 동적 옵션 종류로 가른 것과 같은 결과를 레지스트리 값으로 낸다"
  - "저장값이 지금 목록(보관 제외)에 없으면 (보관됨) 한 옵션을 덧붙여 저장값이 조용히 바뀌지 않게 한다"
  - "가져오기의 지난 연도 항목은 그날 유효값과 isDeepStrictEqual로 같을 때만 통과(시드 2000-01-01 왕복 유지)"
  - "이력 취소는 모든 이력형 키에 정규형 실재 날짜를 요구하고 「이미 적용됨」을 서울 날짜로 판정"

patterns-established:
  - "설정 E2E 격리: 전역 설정을 바꾸는 스펙은 desktop-settings(mobile-375 뒤) + finally 도메인 복원 + 마지막 복원 확인 테스트"

requirements-completed: [ADMN-04, EXP-04]

coverage:
  - id: D1
    description: "관리자가 설정 화면에서 결재선 값을 이름으로 고르고, 바꾼 값은 새 제출에만 반영(진행 중 불변 · 단계 끄기 · 자기 승인 전환 · 멱등 저장)"
    requirement: "ADMN-04"
    verification:
      - kind: integration
        ref: "test/integration/settings-approval-route.test.ts"
        status: pass
      - kind: e2e
        ref: "CI=true pnpm exec playwright test test/e2e/settings-approval-route.spec.ts --project desktop-settings --no-deps"
        status: pass
    human_judgment: false
  - id: D2
    description: "네 단계를 모두 꺼도 대표 폴백으로 결재가 남는다(CEO-1) · 두 관리자 동시 저장 + 제출 경주 10회(Codex HIGH)"
    requirement: "EXP-04"
    verification:
      - kind: integration
        ref: "test/integration/settings-approval-route.test.ts#네 단계를 모두 꺼도 · 두 관리자 동시 저장"
        status: pass
    human_judgment: false
  - id: D3
    description: "효과 없는 칸 비활성(CX-W2) · 특정 부서 없음 경고(저장은 됨)"
    requirement: "ADMN-04"
    verification:
      - kind: unit
        ref: "test/unit/domain/approvals/settings-options.test.ts · settings-warnings.test.ts"
        status: pass
      - kind: e2e
        ref: "test/e2e/settings-approval-route.spec.ts"
        status: pass
    human_judgment: false
  - id: D4
    description: "연차 일수 적용 시작일 규칙(1월 1일 · 지난 연도 거부) — 저장 · JSON 가져오기 · 취소 세 경로(ENG-5 · B-NEW01 · B-NEW02)"
    requirement: "ADMN-04"
    verification:
      - kind: unit
        ref: "test/unit/settings/effective-from-rule.test.ts"
        status: pass
      - kind: integration
        ref: "test/integration/settings-export.test.ts#(e)-(i)"
        status: pass
    human_judgment: false
  - id: D5
    description: "연차 결재선 섹션 17칸 세로 배치 · 날것 키 0 · 경고 한 줄의 화면 일관성"
    verification: []
    human_judgment: true
    rationale: "CLAUDE.md §6 — 독립 에이전트의 CI=true DOM 감사(1280 · 375)와 /design-review가 판정한다"

duration: 41min
completed: 2026-09-26
status: complete
---

# Phase 04.1 Plan 04: 설정 화면 결재선 · 적용 시작일 규칙 Summary

**설정 레지스트리의 optionLabels · dynamicOptions로 `연차 결재선` 17칸을 이름 select · 비활성 칸 · 부서 없음 경고로 그리고, 이력형 `year_start` 적용 시작일을 공통 검증 하나로 저장 · 가져오기 · 취소 세 경로에서 강제**

PLAN_BASE: `5cdd5fb471e48d030f21223f4d72e3a04b86695e`

## Performance

- **Duration:** 41 min (2026-09-26T20:26:44Z → 21:07:27Z)
- **Tasks:** 2/2
- **Files:** 7 created · 7 modified

## Accomplishments

- `listApprovalRouteOptions`: `admin.settings` view 판정 뒤 리포지토리 `listRoles` · `listOrgUnits`에서 id · 이름 · 보관 여부만(scopeFor · 투영 없음). 설정 권한만 가진 계급도 목록 전부를 본다(통합 증명)
- `routeActiveWhen` · `isSettingActive`: 단계별 계급 · 범위 · 부서 12키의 켜짐 조건. 판정은 저장값을 지우지 않는다(단위 · 통합 값 보존 사례)
- 설정 화면: `자기 승인` `건너뜀`/`본인 승인`, `조직 범위` 네 이름, `담당 계급` 첫 옵션 `계급 무관`, `특정 부서` 첫 옵션 `—`, 꺼진 단계 · 특정 부서 아닌 범위의 칸은 네이티브 `disabled`, 특정 부서 없음은 칸 아래 `--warning` 한 줄
- 통합: 새 제출만 반영 · 진행 중 불변 · 자기 승인 전환(route.self_approval 고정) · 2단 계급 변경 멱등(행 1개 · settings_change 2건) · 네 단계 모두 끔 → `submitted` + 대표 폴백 + 담당 `최대표` → 대표 승인 뒤 `approved` · 두 관리자 동시 저장 + 제출 6건 × 10회 경주(오류 0 · 행 1 · 값 ∈ {Y,Z} · 2단 ∈ {X,Y,Z} · 사후 불변)
- `validateEffectiveFrom`: 정규식 날짜 해석(접미사 검사 아님) · 1월 1일 · 서울 오늘 연도 이후. `addHistorizedValue` · `importSettings` · `cancelHistorizedValue`가 저장 · 삭제 전에 부른다. 취소는 규칙 없는 키에도 정규형 실재 날짜 요구 + 서울 날짜 「이미 적용됨」
- Playwright `desktop-settings` 프로젝트(`dependencies: ["mobile-375"]`), `desktop` `testIgnore` 배열화 — `webServer` · env 블록 불변

## Task Commits

1. **Task 1: 트레이서 — 이름 옵션 · 다음 제출부터 적용** — `7841710` (feat)
2. **Task 2a: 적용 시작일 규칙 세 경로** — `fd89f01` (feat)
3. **Task 2b: 부서 없음 경고** — `69b8ed4` (feat)

## Verification (fresh runs, exit codes)

| Gate | Result |
|---|---|
| `pnpm typecheck` | 0 |
| `pnpm lint` | 0 |
| `pnpm lint:sql` | 0 |
| `pnpm test:unit` | 0 — 117 files · 1549 tests passed |
| `pnpm test:integration` | 0 — 64 files · 1900 tests passed (Unhandled 0) |
| `pnpm build` | 0 (Task 1) |
| `CI=true pnpm exec playwright test test/e2e/settings-approval-route.spec.ts --project desktop-settings --no-deps` | 0 — 4 passed |
| `pnpm playwright test test/e2e/settings.spec.ts --project desktop --no-deps` | 4 passed |
| `pnpm test:e2e:ci`(리셋 + CI 빌드, desktop → mobile-375 → desktop-settings) | 0 — 373 passed |
| `--list` 검사(desktop에 settings.spec 있음 · 설정 결재선 스펙 없음 · desktop-settings에 있음) | rc 0 |
| `pnpm db:generate` | No schema changes |

B-NEW02 거부 근거: `export.ts` 단순 키 값 스키마 검사 `issues.push(\`'${key}' 값이 스키마를 만족하지 않습니다.\`)` — 04.1-01 `ROUTE_ORG_UNIT_ID_SCHEMA`(`""` 또는 uuid)가 `not-a-uuid`를 거른다(통합 (i)).

Acceptance greps: `keys.ts` · `db/` · `package.json` · `pnpm-lock.yaml` · `domain/approvals/route.ts` · `tokens.css` · `globals.css` diff 비어 있음 · `settings-form-client.tsx` 22줄(18+/4-) · page/client의 `role-|approval_route` 0 · settings-options의 `scopeFor|@/domain/org|@/domain/permissions/roles` 0 · `endsWith("-01-01")` 0 · `dateOnly(new Date())` 0 · `leave.annual_days` in registry 0 · `finally` 3 · `[1-4]단 사용` 0 · playwright diff의 webServer/env 줄 0.

## Deviations from Plan

### 문구 명사형 변환(hard constraint 3 · SYSTEM.md §8-3)

| 플랜 문구 | 코드 문구 |
|---|---|
| `적용 시작일은 1월 1일 · 2027-01-01처럼 적어 주세요` | `적용 시작일은 1월 1일만 · 2027-01-01처럼 적기` |
| `지난 연도는 바꿀 수 없습니다 · 지난 잔고는 사람 상세의 연차 조정으로 고쳐 주세요` | `지난 연도 변경 불가 · 지난 잔고는 사람 상세의 연차 조정으로 고치기` |
| `적용 시작일은 2027-01-01처럼 적어 주세요`(취소 정규형) | `적용 시작일은 2027-01-01처럼 적기` |
| `부서 없음 · 이 단계는 빈 자리로 건너뜀` | 그대로(이미 명사형) |

`error-copy-noun-style.test.ts` 녹색. (registry · export는 이 스캐너의 개발자 전용 제외 대상이지만 화면에 나가는 문구라 변환했다.)

### 그 밖

1. **[구조] Task 2를 커밋 둘로** — 적용 시작일 규칙(`fd89f01`)과 경고(`69b8ed4`)는 의도가 달라 CLAUDE.md §5 「한 커밋 한 의도」를 따랐다.
2. **[판단] 빈 값 라벨** — 플랜은 「`dynamicOptions`가 roles면 계급 무관, org_units면 —」. 04.1-01이 계급 키에 `optionLabels: { "": "계급 무관" }`을 이미 적어 두어 `optionLabels[""] ?? "—"`로 같은 결과를 낸다(분기 대신 레지스트리 값).
3. **[판단] (보관됨)** — 보관된 id뿐 아니라 목록에 전혀 없는 id도 `(보관됨)` 한 옵션으로 남긴다(select가 첫 옵션으로 조용히 바뀌는 것을 막는 같은 목적).
4. **[Rule 2] 경고 함수 권한** — `listApprovalRouteSettingWarnings`도 `admin.settings` view를 판정한다(옵션 함수와 같은 규칙).
5. **[Rule 2] 가져오기 비교 실패** — 그날 유효값 조회가 던지면(기본값 없는 `year_start` 키) 무변화로 보지 않고 issue로 거부한다.
6. **disabled 적용 자리** — 켜짐 조건이 붙는 12키는 모두 select로 그려져 select에만 `disabled`를 건다.
7. **TDD 메모** — 통합 테스트의 설정 → 제출 사례들은 04.1-01이 이미 만든 동작의 증명이라 처음부터 녹색이었다(플랜 ③이 의도한 모양). RED는 단위(`settings-options` · `effective-from-rule` · `settings-warnings` 모듈 부재/실패 16건)와 E2E(날것 `skip` 표시 · 경고 없음) · 가져오기 (e)(g)에서 확인했다.

## Deferred / Notes

- UI-SPEC S8은 `특정 부서` 힌트 `조직 범위가 특정 부서일 때만`을 적지만 04.1-01 `keys.ts`의 `*_ORG_UNIT_ID` 키에 hint가 없다. 이 플랜은 `keys.ts` 수정 금지라 그대로 두었다 — DOM 감사 · 디자인 리뷰에서 필요 여부 판단.
- 이 플랜 실행 중 Codex 검토 호출은 없었다(한도 풀리면 Codex 재확인 필요 — 오케스트레이터가 독립 Opus 리뷰로 대체).
- 공유 상태 파일(STATE.md · ROADMAP.md · REQUIREMENTS.md · HANDOFF.json)은 hard constraint 1에 따라 쓰지 않았다 — execute-plan의 state/roadmap/requirements 갱신 단계 생략.

## Known Stubs

None.

## Threat Flags

None — 새 표면은 `admin.settings` view 뒤의 읽기 두 함수뿐이고 플랜 위협 모델(T-04.1-59 · T-04.1-25)에 있다.

## DOM 감사 대상

- `/admin/settings` 폭 1280 · 375: `연차 결재선` 섹션 17칸 세로 · 탭/접기 없음 · 날것 키(`self_approve` · `drafter_team` · `role-*` · uuid) 글자 0 · 1단/2단/4단 `특정 부서` disabled(기본 설정) · 3단 `특정 부서` 활성 = `경영관리본부` · `3단 담당 계급` = `계급 무관`
- 3단 `특정 부서` = `—` 상태: 칸 아래 `부서 없음 · 이 단계는 빈 자리로 건너뜀`(`--warning`, 저장됨). 감사 뒤 값 복원 필요
- `연차` 섹션 `연차 일수` 이력 추가: `2027-03-01` → `저장 실패 · 적용 시작일은 1월 1일만 · 2027-01-01처럼 적기`, 지난 연도 → `저장 실패 · 지난 연도 변경 불가 · …`

## Self-Check: PASSED
