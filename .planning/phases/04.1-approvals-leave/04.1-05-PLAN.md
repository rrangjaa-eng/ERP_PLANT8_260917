---
phase: 04.1-approvals-leave
plan: 05
type: execute
wave: 5
depends_on: ["04.1-02", "04.1-03", "04.1-04"]
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - ui/approval-route/ApprovalRoute.tsx
  - ui/approval-route/ApprovalRoute.module.css
  - ui/confirm-dialog/ConfirmDialog.tsx
  - ui/confirm-dialog/ConfirmDialog.module.css
  - domain/approvals/kinds.ts
  - domain/approvals/index.ts
  - domain/leave/index.ts
  - domain/leave/balance-service.ts
  - app/(app)/approvals/page.tsx
  - app/(app)/approvals/inbox-table.tsx
  - app/(app)/approvals/approval-sheet.tsx
  - app/(app)/approvals/loading.tsx
  - app/(app)/approvals/loading.module.css
  - app/(app)/approvals/error.tsx
  - app/(app)/leave/[id]/page.tsx
  - app/(app)/leave/[id]/document-actions.tsx
  - app/(app)/leave/[id]/loading.tsx
  - app/(app)/leave/[id]/error.tsx
  - app/(app)/leave/new/leave-form.tsx
  - test/e2e/mobile-leave-approval.spec.ts
  - test/e2e/leave-document.spec.ts
  - test/e2e/settings-approval-route.spec.ts
  - test/e2e/leave-a11y.spec.ts
  - test/integration/approvals-inbox-projection.test.ts
  - test/integration/leave-balance.test.ts
  - test/unit/domain/approvals/document-kind-registry.test.ts
autonomous: true
requirements: [EXP-05, EXP-03, EXP-04, LEAV-01]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "결재자는 폰(375)에서 `/approvals` 행을 탭해 결재 시트를 열고 종류·기간·일수·비고·잔고·기안·결재선을 본 뒤 `승인`(첫 포커스, 2차의 두 배 폭)으로 승인하고, 그 행은 `처리함`으로 옮겨 간다(EXP-05 · ROADMAP 기준 3 E2E). 시트는 `내 결재` 행에만 열린다 — 폰에서 `처리함` 행을 탭하면 그 문서 화면 `/leave/[id]`(종류가 준 `href`)로 이동하고 시트가 열리지 않는다. 처리함 행에는 상세를 미리 읽지 않는다(ENG-16 — 읽기 전용 상세를 더하는 것보다 작은 쪽인 문서 링크를 골랐다: 04.1-02가 이미 문서 칸 링크를 두었고 결재함 읽기 수가 늘지 않는다)"
    - "폰 시트의 `반려`는 결재 시트를 닫고 반려 확인 시트를 연다(중첩 없음) — 사유 칸이 첫 포커스이고, 사유가 비면 1차가 `aria-disabled`이며 `사유 없음 · 한 줄 적어 주세요`가 보인다. 확인 시트가 닫히면 목록 행으로 포커스가 돌아간다(UI-SPEC S5·S6 · interaction 행)"
    - "폰에서 `승인`을 두 번 빠르게 눌러도 한 번만 처리된다 — 시트·문서 행동 줄·확인 다이얼로그의 버튼은 공용 `ui/button`의 `pending`을 써서 첫 누름 뒤 같은 줄의 버튼이 실제 `disabled`(라벨 뒤 `…`)이고, 늦게 도착한 두 번째 요청은 서버 version 충돌로 기록을 남기지 않는다. `aria-disabled`는 사유 없음 같은 「막힘 이유」에만 쓴다(EXP-05 idempotency · Codex LOW)"
    - "`/leave/[id]` 행동 줄 · 결재함 PC 행 · 폰 결재 시트는 서버 가능 행동 목록을 그대로 그린다 — 지금 담당 = 1차 `승인`(`Ctrl+Enter`) + 2차 `반려` / 기안자이면서 지금 담당(본인 승인 · 대표 폴백 기안자) = 1차 `승인` + 2차 `회수`, `반려` 없음(CXF-B-F01 · CXF2-B-RF01) / 기안자 · 결재 중 = 2차 `회수`만 / 기안자 · 반려 = 고쳐 쓰는 폼 + 1차 `연차 다시 신청`(`Ctrl+Enter`) / 그 밖 = 행동 줄 없음(UI-SPEC S3 populated)"
    - "결재선은 `ui/approval-route` 한 컴포넌트가 그린다 — 제출 뒤에는 단계마다 `사람 단계 이름 · 결과 시각` 한 줄, 처리한 단계는 저장된 처리자, 지금·남은 단계는 표시 시점 담당(보는 사람이면 `(나)` · `내 결재` — 진행 중(`submitted` · `in_review`) 문서만. 종결 문서(`approved` · `rejected` · `withdrawn`)는 저장된 처리 기록만 그리고 지금 단계 · `담당 없음` 줄이 없다, A-01), 반려 단계는 다음 줄에 사유, 자리가 비면 `— 경영관리 · 담당 없음`. 끝 줄은 해당할 때만 `팀장 단계 건너뜀(자기 승인 없음)` · `회수 09-19 10:00`(EXP-04 · UI-SPEC S7)"
    - "제출 뒤 설정을 바꿔도 문서 화면의 결재선은 제출 때 고정된 단계 그대로 그려진다 — 화면이 EXP-04의 증거다"
    - "문서 화면·결재 시트의 `잔고` 행은 04.1-03의 `formatBalanceRow` 그대로다 — 1행 `연차 남음 N일` · `월차 남음 N일` · `결재 중 N일` · `이번 신청 N일`, 2행 `차감 예정 월차 N일 · 연차 N일`, 초과면 `잔여 초과 N일`(`--warning` 600), 재택이면 행 자체가 없다. `월차 남음` 칸은 조회 기준일이 기안자의 근속 첫 1년(입사일 ~ 입사일+1년 전날) 안이거나 조회 연도에 월차 부여(적립)가 하나라도 있으면 있고 값이 0이어도 `월차 남음 0일`로 보인다 — 입사일이 없거나 근속 1년이 지나 그해 월차 부여가 없으면 칸이 없다(ENG-11 · D4). 두 `남음`은 잔고별로 부여(+조정) − 사용(승인됨) − 결재 중(진행 중 신청)이다 — 이 행의 `결재 중`은 이 문서를 뺀 같은 사람의 다른 진행 중 신청 합이고 두 남음도 바로 그 몫(배분 규칙대로 그 잔고에 나눈 것)만 빼며, 이번 신청은 `이번 신청`·2행 `차감 예정`으로 따로 보인다(ENG-12 · D3). 기안자가 입사 다음 해라 월차·연차가 함께 있어도 결재자는 두 잔고를 따로 보고 합계 숫자를 보지 않는다(LEAV-01 · 입력 §5 결재 옆판 · 사용자 요구 2026-09-24 11:43)"
    - "「결재 옆판」은 옆 패널이 아니다 — 폰 = 결재 시트, PC = 문서 화면이고 이 자리 매김이 SYSTEM.md에 A3로 먼저 기록된 뒤 코드가 따른다(UI-SPEC Assumptions #2 · A3)"
    - "PC 결재함 `내 결재` 행의 3차 `반려`는 반려 확인 모달(폭 `--modal-w` 480)을 열고, 3차 `승인`은 확인 없이 즉시다. 700px 경계에서 같은 확인 슬롯이 모달 ↔ 하단 시트로 바뀐다"
    - "동시 처리로 거부되면 누른 자리 옆(결재함 행 · 문서 행동 줄 · 시트·모달 막힘 자리)에 서버가 만든 한 줄 + 3차 `새로 고침`(`router.refresh()`)이 보이고 시트·모달은 닫히지 않는다. 자동 재시도는 없다(UI-SPEC error 동시 처리 행)"
    - "결재함·문서 화면 로딩은 PageHeader + 뼈대 행 3개(반짝임 없음 · 300ms 지연 표시 · 진행 바 없음)이고 — 지연은 `loading.module.css`의 `animation-delay: 300ms` 한 번 나타남(`opacity` 0 → 1, 반복 없음)으로 만들어 300ms 안에 끝나는 스트리밍에서는 뼈대가 보이지 않는다(Codex LOW) — 로드 실패는 `결재함을 불러오지 못했습니다 · 다시 시도` / `연차 문서를 불러오지 못했습니다 · 다시 시도` 한 줄 + 2차 `다시 시도`다(UI-SPEC loading·error 행)"
    - "성공 토스트는 누른 1차 라벨 · 결과다 — `승인 · 결재 요청됨 → 이수아` · `승인 · 최종 승인 · 3일 차감`(재택은 `승인 · 최종 승인`) · `반려 · 박서연에게 돌아감` · `회수 · 결재 멈춤` · `연차 다시 신청 · 결재 요청됨 → 김팀장`. 되돌리기 버튼이 없다. 이름 · 일수 조각은 액션 반환값이 등록 DTO 투영을 지난 뒤에만 오고, 빠지면 그 조각을 뺀 문구(`승인 · 결재 요청됨` · `승인 · 최종 승인` · `반려 · 기안자에게 돌아감` 등)다 — 화면은 받은 필드로만 조립한다(04.1-02 ④ · B-A1)"
    - "`ui/confirm-dialog`는 실행 시점 main에 있으면 그것을 쓰고, 없으면 Phase 4 `04-UI-SPEC.md` S16 계약 그대로 같은 경로에 최소형으로 만든다(오케스트레이터 결정 8)"
    - "결재 시트·문서 화면의 잔고 행 재료는 `getLeaveBalanceForRequest`(입사일·퇴직일 키가 명세에 없는 신청용 DTO — 04.1-03)에서만 온다. 종류의 `loadDetails`는 본인·관리자용 잔고 함수를 부르지 않고, 기안자에게 입사일·퇴직일이 있어도 결재자가 받는 상세 직렬화 결과에 그 날짜 문자열이 없다(CEO-9)"
    - "결재함 한 번 그리기는 `listMyInbox(viewer, { withDetails: true })` 한 번이다 — `내 결재` 항목의 상세(종류 `loadDetails` · 잔고 · 결재선 · 가능 행동)까지 04.1-01의 요청 단위 노출 메모 하나로 읽어, 20건이어도 `findVisibility`가 2회 이하이고, 종류의 상세는 항목 id 목록을 한 번에 받는 `loadDetails` 한 번이다. 연차 행 읽기는 요청당 **정확히 2회**이고 항목 수와 무관하다 — 04.1-01 `listMyInbox`의 목록 요약 `describeDocuments` 일괄 읽기 1회 + 상세 `loadDetails` 일괄 읽기 1회(행마다 읽는 N+1 없음, CXF2-B-RF02). 그리고 잔고 재료(사용 · 조정 읽기)는 항목 수가 아니라 **서로 다른 기안자 수**만큼, 연차 일수 설정 읽기는 서로 다른 회계연도 수만큼이다. 페이지가 항목마다 따로 상세를 부르지 않는다(CEO-17 · Codex MEDIUM). 이 truth의 계수 범위는 「노출 · 연차 행 · 사용 · 조정 · 설정」 다섯 읽기다 — 사용자 이름 · 문서 보임 판정 · 결재선(인스턴스 · 차수 · 단계) 읽기는 04.1-01 `listMyInbox`의 기존 읽기라 이 truth가 세지 않는다(ENG-18 — 전체 SQL 계수 주입을 새로 두지 않고 문구를 좁혔다)"
    - "종류가 준 상세는 엔진이 그 종류가 등록한 `detailDto`(`registerDto`로 등록 — 누수 스캔 대상, 필드마다 정보 항목)로 `project()`한 뒤에만 결재함에 싣는다. 순서가 고정이다(ENG-17): 종류의 `loadDetails`는 문자열 행이 아니라 **구조 필드**(종류 · 기간 · 일수 · 비고 · 잔고 재료 · 기안 …)를 돌려주고 → 엔진이 정보 항목별로 `project()` → 종류의 순수 함수 `buildDetailRows(projected)`가 **투영된 필드만으로** `{title, subtitle, rows: {label, value, tone}[]}` 문자열 행을 만든다. 그래서 보이지 않는 정보 항목의 값은 어떤 행 문자열에도 들어갈 수 없다. 새 문서 종류(Phase 5)가 `loadDetails`에서 무엇을 돌려도 등록되지 않았거나 숨긴 필드가 결재자 페이로드로 새지 않는다(Codex MEDIUM · ENG-17)"
    - "결재 모듈을 부르는 app 파일(`/approvals` · `/leave/[id]`)은 `app/(app)/document-kinds.ts`를 import하고 04.1-02의 가드 단위 테스트가 녹색이다(CEO-3). `loadDetail` → `balance-service` → `access.ts` 한 방향이라 `import-cycles.test.ts`가 이 플랜 끝에 녹색이다(CEO-4)"
    - statement: "결재함 · 결재 시트 · 반려/회수 확인이 폭 1280 · 1024 · 375에서 가로 스크롤 0, 행 높이 ≥ 44(폰), 시트 본문 세로 스크롤 · 행동 줄 고정, 700 경계에서 모달(480) ↔ 시트 전환(UI-SPEC responsive backstop 행 S1·S4·S5 · S6)"
      verification: backstop
    - statement: "결재 시트·확인 다이얼로그가 열릴 때 포커스(결재 시트 = 승인, 반려 = 사유 칸, 회수 = 1차), `Esc` 닫힘, 닫힐 때 트리거 복귀, 포커스 트랩, axe 위반 0(UI-SPEC a11y backstop 행 S5·S6)"
      verification: backstop
    - statement: "결재선 목록이 이름 10자 · 부서명 12자 · 사유 200자에서 한 단계 한 줄 규칙을 지키고 사유만 줄바꿈된다(UI-SPEC long-text backstop 행 S7)"
      verification: backstop
    - statement: "결재함 `<table>`에 숨긴 caption `결재함`, 그룹 머리글은 `ui/table` 기존 렌더 그대로, 상태는 글자로 전달되고 행 링크는 `Tab`으로 닿는다. 결재선 담당이 빈 단계는 S7 규칙 그대로 표시되고, 결재함 그룹 0건은 머리글째 없으며 1건·여러 건이 같은 행 템플릿이다(UI-SPEC a11y S1·S4 · partial S4 · zero-one-many S4 backstop 행)"
      verification: backstop
  prohibitions:
    - statement: "결재 옆판을 화면 옆 패널(사이드바)로 만들어서는 안 된다 — SYSTEM.md §0 「패널 없음」 · §6-1 「사이드바 없음」이다"
      status: resolved
      verification: test
    - statement: "행동 줄을 클라이언트가 상태 문자열을 보고 고르게 해서는 안 된다 — 서버의 `getApprovalView`가 돌려준 가능 행동만 그린다. 클라이언트 분기는 권한 밖 버튼을 보여 주는 누수가 된다"
      status: resolved
      verification: test
    - statement: "화면 하나만을 위한 새 색·서체·radius·그림자를 만들어서는 안 된다 — 토큰은 `docs/design/tokens.css`에서만, 벗어나면 DECISIONS.md → SYSTEM.md 순서로 고친다"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/approval-route/ApprovalRoute.tsx"
      provides: "결재선 표시 — 제출 전 한 줄 · 제출 뒤 단계 목록(그리기만, 해석은 서버)"
      exports: ["ApprovalRoute"]
    - path: "app/(app)/approvals/approval-sheet.tsx"
      provides: "폰 결재 시트(S5)"
    - path: "app/(app)/leave/[id]/document-actions.tsx"
      provides: "서버가 고른 행동 줄 넷 · 반려/회수 확인 · 다시 신청 폼"
    - path: "test/e2e/mobile-leave-approval.spec.ts"
      provides: "폰 375 신청 → 결재 시트 승인 · 반려 · 두 번 탭 E2E(EXP-05)"
      min_lines: 60
    - path: "test/e2e/leave-document.spec.ts"
      provides: "문서 화면 행동 줄 넷 · 회수 · 다시 신청 · 동시 처리 거부 표시"
      min_lines: 50
    - path: "test/integration/approvals-inbox-projection.test.ts"
      provides: "(04.1-01 파일에 describe 추가) 상세 포함 결재함 20건 노출 조회 2회 이하 · 연차 행 읽기 정확히 2회(목록 요약 1 + 상세 1, 항목 수와 무관 — CXF2-B-RF02) · 잔고 재료 읽기 = 기안자 수(CEO-17 · Codex MEDIUM — 계수 범위 노출·행·사용·조정·설정, ENG-18) · 결재자 상세 직렬화에 기안자 입사일·퇴직일 없음(CEO-9) · 10월 입사 기안자의 다음 해 잔고 행이 연차·월차 분리(11:43) · 결재자 계급에 `leave.value`를 숨기면 비고 값이 어떤 행 문자열에도 없음(ENG-17)"
      contains: "withDetails"
    - path: "app/(app)/approvals/loading.module.css"
      provides: "뼈대 300ms 지연 표시(animation-delay) — 결재함 · 문서 · 04.1-06 목록 로딩이 같이 쓴다(Codex LOW)"
      contains: "animation-delay"
  key_links:
    - from: "app/(app)/approvals/approval-sheet.tsx"
      to: "domain/approvals/kinds.ts"
      via: "종류가 준 loadDetails 행(엔진이 detailDto로 투영한 것)을 그린다 — 결재함은 연차를 모른다"
      pattern: "loadDetails"
    - from: "app/(app)/leave/[id]/page.tsx"
      to: "domain/leave/balance-service.ts"
      via: "getLeaveBalanceForRequest → formatBalanceRow"
      pattern: "getLeaveBalanceForRequest"
    - from: "app/(app)/leave/[id]/document-actions.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "반려·회수 확인"
      pattern: "ConfirmDialog"
    - from: "app/(app)/approvals/page.tsx"
      to: "domain/approvals/index.ts"
      via: "listMyInbox(viewer, { withDetails: true }) 한 번 — 상세까지 같은 노출 메모(CEO-17)"
      pattern: "withDetails"
    - from: "domain/leave/index.ts"
      to: "domain/leave/balance-service.ts"
      via: "연차 loadDetails의 잔고 = getLeaveBalancesForRequests(신청용 DTO · 기안자별 재료 메모 · 받은 visible 메모 전달, CEO-9 · CEO-17)"
      pattern: "getLeaveBalancesForRequests"
    - from: "domain/approvals/index.ts"
      to: "domain/permissions/project.ts"
      via: "withDetails가 종류의 detailDto로 project()한 뒤에만 상세를 싣는다(Codex MEDIUM)"
      pattern: "detailDto"
    - from: "domain/approvals/index.ts"
      to: "domain/approvals/kinds.ts"
      via: "project() 결과만 종류의 buildDetailRows에 넘겨 문자열 행을 만든다 — 원시 loadDetails 결과는 행 조립에 닿지 않는다(ENG-17)"
      pattern: "buildDetailRows"
---

<objective>
**As a** 현장에 나가 있는 결재자와 연차를 낸 직원, **I want to** 폰에서 결재함 한 줄을 눌러 근거(기간 · 일수 · 잔고 · 결재선)를 본 뒤 승인·반려하고, 직원은 문서 화면에서 지금 누구 차례인지 보고 회수하거나 반려된 문서를 고쳐 다시 내기를, **so that** 결재가 사무실 PC 앞에서만 되는 일이 아니고, 누구의 무엇이 멈춰 있는지 화면이 말해 준다.

Task 1은 이 플랜의 **트레이서**다 — 폰 결재 시트 경로 하나(행 탭 → 시트 → 승인 → 처리함)를 디자인 개정 A3 · `ui/approval-route` · 종류의 상세 행 · 결재함 화면 · 모바일 E2E까지 관통시킨다. Task 2는 반려·회수·다시 신청 화면과 확인 다이얼로그로, Task 3은 다섯 상태·접근성·독립 DOM 감사로 넓힌다.

Purpose: ROADMAP 기준 3의 「결재자는 결재 대기함에서 폰으로 승인·반려」와 「연차 신청 → 결재자 폰 승인 흐름이 Playwright E2E로 CI에 있다」가 이 플랜이다(EXP-05). 04.1-02가 만든 서버 동작 전부에 화면이 붙는다.

Output: `ui/approval-route` · `ui/confirm-dialog`(main에 없을 때만), 결재 시트, 문서 화면 행동 줄, 로딩·오류 경로, SYSTEM.md A3, E2E 3.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md
@.planning/phases/04.1-approvals-leave/04.1-02-SUMMARY.md
@.planning/phases/04.1-approvals-leave/04.1-03-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다(E2E·단위를 먼저 적는다). 실패가 나면 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 독립 DOM 감사를 서브에이전트에 맡길 때 프롬프트에 세 스킬 이름과 `CI=true`를 적는다.
- UI 작업 전에 `docs/DESIGN.md` §4 절차를 읽는다(CLAUDE.md 프론트엔드). SYSTEM.md 개정은 `docs/design/DECISIONS.md` 먼저 → `SYSTEM.md` → 코드.
- 화면 검증 순서: 싼 게이트(lint · typecheck · build) → 독립 DOM 감사(실행자가 아닌 별도 에이전트, `CI=true`, 폭 1280 · 1024 · 375 실측, 스크린샷 육안 금지) → 수정 → 전체 게이트 한 번.
- `ui/confirm-dialog`: 시작 전에 `git fetch origin main && git ls-tree origin/main -- ui/confirm-dialog`로 main에 있는지 본다. 있으면 **그 파일을 쓰고 이 플랜에서 만들지 않는다**(files_modified의 두 파일은 조건부). 없으면 로컬 `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`의 S16 · 150행 계약대로 최소형으로 만든다(같은 문서가 로컬에 있다 — 원격 브랜치 참조는 로컬에 없다, `git branch -r` = `origin/main` · `origin/claude/phase-04.1-plan-1iqtwe`뿐, B-C5). 그 브랜치의 구현 파일은 `git fetch origin claude/gsd-progress-e1nzgu` 뒤 `git show origin/claude/gsd-progress-e1nzgu:ui/confirm-dialog/ConfirmDialog.tsx`로 보고, 있으면 그 모양을 그대로 따른다(뒤에 머지되는 쪽이 앞의 것을 쓴다). `ui/button`은 고치지 않는다(Phase 4 04-46 소관).
- 건드리지 않는 파일: `ui/next-turn/*` · 첫 화면 · `ui/shell/*`(04.1-06) · `domain/rules/register.ts`.
- 첫 task를 시작하기 전에 `git rev-parse HEAD` 값을 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도 남긴다). 이 플랜의 diff 기준 검사는 전부 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다 — 커밋한 뒤의 맨 `git diff`는 늘 비어 있어 검사가 공허해진다.
</execution_notes>

<probe_fallback>
이 플랜이 닫는 미해결 엣지: **EXP-05 idempotency**(폰 두 번 탭 — 「두 번 빠르게 눌러도」 truth). EXP-05 concurrency는 04.1-02 Task 3의 서버 증명 위에 이 플랜이 거부 표시(「동시 처리로 거부되면」 truth)를 얹는다. UI-SPEC UI Considerations에서 이 플랜 표면(S3 · S4 · S5 · S6 · S7)의 covered 행은 truths로, backstop 행은 backstop 항목 넷으로 옮겼다 — 버린 행은 없다. U2(담당 없음 표시)는 04.1-01의 엔진 규칙(오케스트레이터 결정 3)을 이 플랜의 `ApprovalRoute`가 그린다.

**계획 가정**
1. 결재 시트의 본문 행은 문서 종류가 준다 — `loadDetails(viewer, documentIds, deps)`가 한 번에 id → **구조 필드** 객체를 돌려주고, 엔진이 `detailDto`로 투영한 뒤 종류의 `buildDetailRows(투영 결과)`가 `{title, subtitle, rows}`를 만든다(Task 1 ③ 순서 — 구조 필드 → 투영 → 문자열 행, ENG-17 · CXF4-B-F4-01) — 결재함 화면이 연차를 모르게 하려는 것이고 Phase 5 지출결의가 같은 자리에 붙는다. 결재함 페이지는 `listMyInbox(viewer, { withDetails: true })` 한 번으로 `내 결재` 항목의 이 값을 서버에서 미리 읽는다 — 항목마다 따로 부르지 않아 노출 메모 하나를 공유하고, 잔고 재료는 기안자별로 한 번만 읽는다(CEO-17 · Codex MEDIUM). 문서 화면(`/leave/[id]`) 하나짜리 경로는 같은 함수에 id 하나를 넘긴다
2. 반려된 문서의 고쳐 쓰는 폼은 04.1-02의 `leave-form.tsx`를 「다시 신청」 모드로 쓴다(번호 유지 · 1차 라벨 `연차 다시 신청`) — 두 벌의 폼을 만들지 않는다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 폰 375에서 결재함 행 탭 → 결재 시트(근거 · 잔고 · 결재선) → 승인 → 처리함 (A3 · 결재선 컴포넌트 · 종류 상세 · 모바일 E2E)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, ui/approval-route/ApprovalRoute.tsx, ui/approval-route/ApprovalRoute.module.css, domain/approvals/kinds.ts, domain/approvals/index.ts, domain/leave/index.ts, domain/leave/balance-service.ts, app/(app)/approvals/page.tsx, app/(app)/approvals/inbox-table.tsx, app/(app)/approvals/approval-sheet.tsx, test/unit/domain/approvals/document-kind-registry.test.ts, test/integration/approvals-inbox-projection.test.ts, test/integration/leave-balance.test.ts, test/e2e/mobile-leave-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 320–369행(S4 · S5 · S6 · S7) · 205·207·210·211·214행(동시 처리 · 잔고 행 · 결재선 · 결재함 행 · 토스트 원문) · 410–412행(A3) · 419–489행(UI Considerations)
    - `docs/design/system/sheet-modal.html` — 폰 결재 시트 `#sheet-approve` · 결재함 표 실물
    - `docs/design/SYSTEM.md` §6-1(315–356행) · §6-3(380–413행) · §7-8(834–845행) · §7-9 · §10(1031–1042행)
    - `docs/DESIGN.md` §4
    - `ui/shell/MoreSheet.tsx` · `ui/shell/MoreSheet.module.css` — 이미 있는 하단 시트 구현(스크림 · 포커스 트랩 · Esc)의 모양
    - `app/(app)/approvals/page.tsx` · `app/(app)/approvals/inbox-table.tsx`(04.1-02 산출물)
    - `domain/approvals/kinds.ts` · `domain/approvals/index.ts`(`getApprovalView` 표시 목록) · `domain/leave/index.ts` · `domain/leave/balance-service.ts` · `domain/leave/balance.ts`(`formatBalanceRow`)
    - `playwright.config.ts`(mobile-375 프로젝트는 `mobile-*.spec.ts`) · `test/e2e/leave-approval.spec.ts`(04.1-02 — 전용 본부·팀 픽스처 모양) · `test/e2e/mobile-org.spec.ts`
    - 04.1-01 T4 산출물: `domain/approvals/index.ts`의 요청 단위 노출 메모(`listMyInbox`의 두 번째 인자 `deps.findVisibility`) · `test/integration/approvals-inbox-projection.test.ts`(세는 `findVisibility` 주입 모양)
    - `domain/leave/dto.ts`(`LEAVE_REQUEST_BALANCE_DTO_SPEC` — 04.1-03, 입사일·퇴직일 없음) · `domain/leave/access.ts`(`canSeeLeaveDocument` — 04.1-01) · `test/unit/import-cycles.test.ts`
    - `app/(app)/document-kinds.ts` · `test/unit/document-kinds-import.test.ts`(04.1-02 T1 ⑧)
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` CEO-9(306–311행) · CEO-17(Section 7)
    - `domain/permissions/project.ts`(`project(viewer, row, spec, deps)` — 명세 밖 키는 싣지 않는다) · `domain/permissions/dto-registry.ts`(`registerDto`) · `test/unit/domain/approvals/document-kind-registry.test.ts`(04.1-01)
    - `repositories/leave-usage.ts` · `repositories/leave-adjustments.ts`(04.1-03 — 기안자 한 사람 단위 읽기) · `ui/button/Button.tsx`(`pending` = 실제 `disabled` + `…`)
    - `test/e2e/leave-dates.ts`(04.1-02 — `leaveWeekdayRange`)
  </read_first>
  <behavior>
    - (E2E mobile-375) 기안자(입사일 없음 — 월차 부여 없음)가 폰에서 `/leave/new`로 종일 3평일(`leaveWeekdayRange(today, {week: 2, weekdays: 3})` — `today`는 테스트 맨 앞에서 `seoulToday()`를 한 번 불러 얻은 값, CXF2-B-RF03 · 실행하는 날과 무관하게 그해 안)을 신청 → 팀장이 폰으로 로그인해 `/approvals`의 행(가로 스크롤 0)을 탭하면 결재 시트가 열리고 제목 `연차 · 종일 MM-DD ~ MM-DD`, 부제 `{번호} · {기안자}`, 행 `잔고`의 1행이 `연차 남음 …일 · 결재 중 0일 · 이번 신청 3일`(월차 칸 없음 — 입사일이 없어 D4의 「근속 첫 1년」·「그해 월차 부여」 둘 다 아님, ENG-11), 결재선 목록의 1단이 `{팀장}(나) 팀장 · 내 결재`다. 첫 포커스가 `승인`
    - (E2E) `승인`을 누르면 토스트 `승인 · 결재 요청됨 → {본부 책임자}`, 시트가 닫히고 그 행이 `처리함` 그룹에 있다
    - (E2E, ENG-16) 승인 직후 `처리함`의 그 행을 탭하면 URL이 그 문서의 `/leave/[id]`가 되고 결재 시트(`dialog`)가 열리지 않으며, 문서 화면 머리 번호가 그 행의 번호와 같다
    - (E2E) 두 번째 문서에서 `승인`을 빠르게 두 번 누르면 첫 누름 뒤 버튼이 `disabled`이고 처리 기록이 한 건이다(스펙이 도메인 조회로 단계 처리 기록 수를 센다)
    - (통합, CEO-17 · Codex MEDIUM) 기안자 4명이 낸 진행 중 연차 20건(대표가 지금 단계 후보)이 있을 때 세는 주입(`findVisibility` · 연차 행 읽기 · 사용 읽기 · 조정 읽기 · 설정 읽기)을 넣은 `listMyInbox(대표, { withDetails: true, … })` 한 번의 호출에서 `findVisibility` ≤ 2 · 연차 행 읽기 = 2(CXF2-B-RF02 — 04.1-01 `describeDocuments` 목록 요약 1 + `loadDetails` 상세 1. 계수 주입은 두 경로의 연차 행 리포지토리 읽기를 모두 센다. 대표의 `처리함`은 0건인 픽스처) · 사용 읽기 = 4 · 조정 읽기 = 4 · 연차 일수 설정 읽기 ≤ 회계연도 수이고, `내 결재` 20건 모두에 상세(제목 · 행 · 결재선 표시 목록 · 가능 행동 · version)가 있으며 결과는 주입 없이 부른 결과와 같다. 같은 픽스처를 진행 중 5건(기안자 4명)으로 줄여도 연차 행 읽기는 2 그대로다(N+1 없음 — CXF2-B-RF02). 이 사례가 세는 것은 이 다섯 읽기뿐이다 — 사용자 · 보임 판정 · 결재선 조회 수는 단언하지 않는다(ENG-18 — truth 문구와 같은 범위)
    - (단위, Codex MEDIUM — `document-kind-registry.test.ts`) 테스트 전용 종류의 `loadDetails`가 명세 밖 키 `hireDate: "2026-10-01"`과 옛 모양의 문자열 행 `rows: [{label, value: "ROWLEAK-7c1"}]`을 함께 돌려도 엔진이 싣는 상세의 `JSON.stringify`에 `2026-10-01` · `ROWLEAK-7c1`이 없다(`rows`는 `detailDto` 명세 밖이라 투영에서 떨어지고, 행은 `buildDetailRows`만 만든다). `loadDetails`가 있는데 `detailDto` 또는 `buildDetailRows`가 없는 종류는 `registerDocumentKind`에서 예외다(`loadDetails`가 없는 04.1-01 `test_memo`는 그대로 등록된다)
    - (단위, ENG-17 — 숨긴 정보 항목, `document-kind-registry.test.ts`) 테스트 전용 종류의 `detailDto`가 `note` 필드를 정보 항목 `test.secret`에, `days` 필드를 `test.value`에 묶고, `buildDetailRows`가 `note`와 `days`를 둘 다 행 문자열에 넣도록(`비고 ${note ?? "—"}` · `${days}일`) 작성돼 있다. 주입한 `visible`이 `test.secret`만 거부하면 엔진 결과의 모든 `rows[].value` · `title` · `subtitle` 어디에도 `note` 원값 `SECRET-NOTE-9f3`이 없고(`JSON.stringify` 전체에도 없음) `days` 값은 행에 있다. 같은 종류에 `visible`이 전부 허용하면 `SECRET-NOTE-9f3`이 행에 나타난다(검출기 사례 — 공허한 통과 방지)
    - (통합, 11:43 · R1/D5 · ENG-12/D3 — `leave-balance.test.ts`) 입사 2026-10-01 기안자(`deps.now` = 2027-03-15)의 2027-03-15 시작 6평일 신청(다른 진행 중 신청 없음)을 결재자가 `withDetails`로 받으면: 2027 연차 부여는 D5 비례값 4일(2026 근무 92일 → ceil(60×92/365) = 16 quarters)이고 월차 적립은 5일(2026-11-01 ~ 2027-03-01)이다. 잔고 행 1행이 `연차 남음 4일 · 월차 남음 5일 · 결재 중 0일 · 이번 신청 6일`, 2행이 `차감 예정 월차 5일 · 연차 1일`(소멸일이 같아 월차 먼저)이고, 6 ≤ 월차 5 + 연차 4라 `잔여 초과` 줄이 없다. 같은 픽스처의 신청용 DTO 값도 함께 단언한다 — DTO는 04.1-03 정의대로 **정수 1/4일(쿼터)**이다(CXF-B-F02 — 일 단위 기대값은 화면 문자열에만): `annualRemaining` 16 · `monthlyRemaining` 20 · `pending` 0 · `thisRequest` 24 · `plannedDeduction` {monthly 20, annual 4} · `over` 정확히 0. 상세 직렬화 어디에도 두 남음을 더한 `9일`이 독립 숫자로 없다(`/(?<![\d.])9일/` 불일치)
    - (통합, ENG-11 · D4 — `leave-balance.test.ts`) 입사 2026-12-15 기안자(`deps.now` = 2027-01-05, 근속 첫 1년 안 · 아직 월차 적립 0)의 반차(0.5일) 신청을 결재자가 `withDetails`로 받으면 잔고 행 1행이 `연차 남음 0.75일 · 월차 남음 0일 · 결재 중 0일 · 이번 신청 0.5일`이다(2027 연차 = D5 비례 — 2026 근무 17일 → ceil(60×17/365) = 3 quarters). `월차 남음 0일` 칸이 있고, 행 전체는 같은 입력의 04.1-03 `formatBalanceRow` 결과와 같다(2행 유무는 그 함수 규칙대로). 그 신청을 회수한 뒤 같은 사람이 2028-03-15(수) 반차를 신청하고 `deps.now` = 2028-03-15(근속 1년 지남 · 2028 월차 부여 없음)로 보면 1행이 `연차 남음 15일 · 결재 중 0일 · 이번 신청 0.5일`이고 `월차 남음` 칸이 없다(H+2 = 설정값 전액 — D5)
    - (통합, CX-B2 — `leave-balance.test.ts`, 시계 주입이 상세 잔고까지 닿는다) 위 입사 2026-10-01 기안자의 6평일 신청(2027-03-15 시작)을 결재자가 `listMyInbox(결재자, { withDetails: true, now: 2027-03-15 })`로 받은 상세 `잔고` 행이 같은 결재자의 `getLeaveBalanceForRequest(결재자, 그 id, { now: 2027-03-15 })` DTO → `formatBalanceRow` 결과와 글자 단위로 같다. 같은 호출을 `now: 2027-04-15`로 하면 상세 1행이 `월차 남음 6일`(2027-04-01 적립 1일 더함)을 담고 그날로 계산한 `formatBalanceRow` 결과와 같으며 2027-03-15 행과 다르다 — 두 주입 날짜가 서로 다른 값을 내므로 중간 층이 벽시계로 떨어지면 둘 중 하나가 틀린다(실행 날짜와 무관)
    - (단위, CX-B2 — `document-kind-registry.test.ts`) 엔진의 `withDetails` 경로에 `now` = 2031-01-02를 주입하면 테스트 전용 종류의 `loadDetails`가 받은 `deps.now`가 그 값(같은 시각)이고, 주입하지 않으면 `deps.now`가 `undefined`다(엔진이 시계를 직접 읽지 않는다)
    - (통합, CEO-9) 기안자에게 입사일·퇴직일을 둘 다 넣은 뒤 1단 결재자가 받은 `withDetails` 상세를 `JSON.stringify`하면 그 두 날짜 문자열이 없다. 같은 상세의 `잔고` 행은 `formatBalanceRow` 결과와 같다
    - (통합, CXF2-B-RF01 — `approvals-inbox-projection.test.ts`, 바꾼 설정은 `afterEach`에서 되돌린다) `self_approval` = `self_approve`로 저장한 뒤 팀장이 낸 문서가 `listMyInbox(팀장, { withDetails: true })`의 `내 결재`에 있고 그 항목의 가능 행동이 정확히 `[승인, 회수]`(반려 없음)이며 같은 문서의 `getApprovalView(팀장)` 가능 행동과 같다. 같은 결재함의 다른 사람 문서 항목은 `[승인, 반려]`다
    - (통합, ENG-17 — 실제 연차 종류, `approvals-inbox-projection.test.ts`) 비고 `비고-고유-5d2`로 낸 신청에서, 1단 결재자 계급의 `leave.value` 노출을 끄면(기존 노출 설정 도메인 함수로) 그 결재자의 `withDetails` 상세 `JSON.stringify`에 `비고-고유-5d2`가 없고 어떤 `rows[].value`에도 없다. 노출을 다시 켜면 비고 행에 나타난다. 결재 가능 여부(가능 행동 · version)는 노출과 무관하게 그대로다
  </behavior>
  <action>
① **A3 개정 먼저**: `docs/design/DECISIONS.md`에 A3(UI-SPEC 410–412행 — 제출 뒤 결재선 = 단계별 목록 · 「결재 옆판」 = 폰 결재 시트 + PC 문서 화면, 옆 패널 없음)를 기록하고 `docs/design/SYSTEM.md` §6-3 결재선 문단 · §6-1 결재함 문단(그룹 `내 결재`/`처리함` · PC 행 3차 승인·반려 · 폰 행 탭 → 결재 시트) · §7-8을 그 문장대로 고친다.

② `ui/approval-route/ApprovalRoute.tsx`(ui 계층 — domain·app import 금지): 두 모드. `line` = §6-3 제출 전 한 줄(`기안자 → 사람 단계 → … · 결재 규칙`, 기안자 700, `→` `--faint`, 단계 이름 `--fs-sm --muted`, 끝에 자기 승인 건너뜀 한 마디). `list` = `<ol>`(번호 표시 없음) 단계마다 `[사람] [단계 이름] · [결과 글자] [시각]`, 반려 사유 다음 줄(`keep-all`), 끝 줄 해당할 때만. 입력은 서버가 해석한 표시 목록(사람 문자열 · 단계 이름 · 결과 {글자, kind} · 시각 · 사유 · 끝 줄)이고 컴포넌트는 그리기만 한다. 색은 kind → 의미 토큰(status-display가 준 값)만.

③ `domain/approvals/kinds.ts`의 종류 정의에 선택 필드 `loadDetails(viewer, documentIds, deps: { visible, now? })`(`now`는 엔진이 받은 `deps.now`를 그대로 넘긴 값 — CX-B2)(id → **구조 필드** 객체 맵 — 문자열 행이 아니다, 계획 가정 1)와, `loadDetails`가 있으면 **둘 다 필수**인 `detailDto`(`DtoSpec` — 필드마다 정보 항목, 종류 모듈이 `registerDto`로도 등록해 누수 스캔이 본다) · `buildDetailRows(projected: Partial<Dto>) → {title, subtitle, rows: {label, value, tone}[]}`(순수 함수)를 더한다(Codex MEDIUM · ENG-17). 엔진(`withDetails`)의 순서는 고정이다: 원시 구조 필드 → `project(viewer, raw, kind.detailDto, { visible: 메모 })`로 **정보 항목별 투영** → 투영 결과만 `buildDetailRows`에 넘겨 문자열 행 생성 → 결과 행은 `{label, value, tone}` 문자열 칸만 남겨 싣는다. 원시 결과는 `buildDetailRows`에 닿지 않는다(타입도 `Partial<Dto>`) — 문자열 행을 만든 **뒤에** 거르는 모양(행 모양·최상위 키만 막는 것)은 행 문자열 안의 숨긴 값을 못 막으므로 쓰지 않는다(ENG-17). `domain/leave/index.ts`의 연차 종류가 이를 구현한다 — 연차 행을 id 목록 한 쿼리로 읽고 구조 필드(종류 · 기간 · 일수 · 비고 · 잔고 재료(`getLeaveBalancesForRequests`, 재택이면 없음) · 기안 이름 · 기안일)를 돌려주며, `buildDetailRows`가 투영된 필드로 제목 · 부제 · 행(종류 · 기간 · 일수 · 비고 · 잔고(`formatBalanceRow`) · 기안)을 만든다. 연차 `detailDto`의 정보 항목은 기존 것만 쓴다 — 연차 필드는 04.1-03 신청용 잔고 DTO와 같은 `leave.value`, 기안 이름·기안일은 04.1-01 결재함 DTO와 같은 `approval.value`(새 정보 항목 없음). 잔고는 **신청용 DTO 함수 하나만** 부른다 — `domain/leave/index.ts`는 본인·관리자용 잔고 함수 둘(입사일·퇴직일을 싣는 `LEAVE_BALANCE_DTO_SPEC` 쪽)을 import하지 않는다(CEO-9). `domain/leave/balance-service.ts`에 `getLeaveBalancesForRequests(viewer, leaveIds, deps?: { visible, now?, … })`를 더한다(CX-B2 — 연차 종류의 `loadDetails`는 받은 `now`를 이 함수에 그대로 넘기고, 이 함수는 **04.1-03의 신청 잔고 읽기(Task 1 ⑤ — `getLeaveBalanceForRequest`의 계산 경로)를 재사용해** `now`를 그대로 넘긴다. 이 함수 안에는 `new Date(` · 자체 `seoulToday(` 호출이 없다 — 「오늘」(적립 기준일 · 회계연도)은 04.1-03 규약대로 그 읽기 입구의 `seoulToday(deps?.now)` 호출당 한 번에서만 나오고 문서마다 다시 읽지 않는다. `now`가 없을 때만 그 한 곳이 벽시계다. 중간 층(`withDetails` · `loadDetails`)도 시계를 직접 읽지 않는다) — 보임 규칙을 문서마다 확인한 뒤 **기안자별로 한 번** 사용·조정을 읽고(요청 안 지역 메모), 연차 일수 설정은 회계연도별로 한 번 읽어 04.1-03의 순수 함수로 문서마다 잔고 행 재료를 만든다. 04.1-03의 신청 잔고 읽기 본체를 id 목록을 받는 모양으로 넓혀(기안자별 한 번 읽기) `getLeaveBalanceForRequest`(id 하나)와 이 함수(id 목록)가 둘 다 그 본체를 부른다 — 계산 경로와 「오늘」 한 곳이 하나다(CX-B2 · 04.1-03 테스트 불변 · 03의 주입 시계 두 날짜 사례 그대로 녹색 — 첫 줄의 `assertLeaveWrite`는 `previewLeaveBalance`에만 있고 이 조회 함수들에는 없다). 결과는 `LEAVE_REQUEST_BALANCE_DTO_SPEC`(연차 남음 · 월차 남음 분리, 합계 없음 — 04.1-03)로 `project()`한다. `balance-service.ts`는 여전히 `./index`를 import하지 않는다(CEO-4).
`domain/approvals/index.ts`(CEO-17): `listMyInbox`의 두 번째 인자 객체(04.1-01의 `deps` 자리 — `findVisibility`가 있는 그 객체)에 선택 필드 `withDetails`를 더한다 — 참이면 `내 결재` 항목마다 종류의 보임 규칙을 확인한 뒤, 종류별로 항목 id 목록을 모아 `loadDetails(viewer, ids, { visible: 메모, now: deps.now })`를 한 번 부르고(CX-B2 — `listMyInbox` · `getApprovalView`는 자기 `deps.now`를 스냅숏 기준일과 상세 읽기에 같은 값으로 내려보낸다) 그 결과를 문서마다 `detailDto`로 투영한 뒤 `buildDetailRows(투영 결과)`로 행을 만들어(ENG-17 — 투영이 행 생성보다 먼저) 결재선 표시 목록 · 가능 행동 · version과 함께 붙인다. 가능 행동은 04.1-02 ④-b의 `getApprovalView`와 **같은 함수**(`possibleActions`)로 만든다 — 기안자가 지금 담당이면 `[승인, 회수]`(CXF2-B-RF01). 결재선 표시 목록 · 가능 행동의 재료는 `listMyInbox`가 `mine`을 가를 때 이미 계산한 결재선 결과(04.1-01 — 필수 입력 `at: "before_action"`)를 그대로 쓰고, 상세를 붙이려고 결재선을 다시 계산하지 않는다(X-1 — 고아 최종은 막힘과 같아 애초에 `mine`에 없다, D2). 메모는 04.1-01이 이 호출 안에서 만든 **같은** 지역 메모다(새 메모·모듈 전역 캐시 금지). 종류마다 `loadDetails`를 `내 결재` 항목 id 목록으로 **한 번** 부른다. 항목 하나짜리 상세 함수를 따로 export하지 않는다 — 페이지가 항목마다 부르는 모양을 막는다. 테스트: `test/integration/approvals-inbox-projection.test.ts`(04.1-01 파일)에 `describe` 하나로 CEO-17 · CEO-9 · ENG-17(`leave.value` 숨김) 줄, `test/unit/domain/approvals/document-kind-registry.test.ts`에 `detailDto` 투영 줄과 ENG-17 숨긴 정보 항목 줄(검출기 사례 포함), `test/integration/leave-balance.test.ts`에 11:43 분리 잔고 줄(R1/D5 비례 연차 · D3 문자열+DTO)과 D4 월차 0일 줄을 더한다. 잔고 숫자(연차 비례값 · 월차 적립)는 04.1-03의 순수 함수가 정한다 — 이 테스트의 리터럴이 04.1-03 결과와 다르면 이 플랜이 아니라 04.1-03 규칙과 대조해 원인을 찾는다(`systematic-debugging`).

④ `app/(app)/approvals/page.tsx`가 `listMyInbox(viewer, { withDetails: true })` 한 번으로 `내 결재` 항목의 상세를 미리 읽어 `inbox-table.tsx`에 넘기고(04.1-02가 넣은 `document-kinds` import는 그대로 둔다 — CEO-3), `<700px`에서 `내 결재` 행 탭이 `approval-sheet.tsx`를 연다. `처리함` 행 탭은 시트를 열지 않고 그 행의 문서 링크(종류가 준 `href` → `/leave/[id]`, 04.1-02가 문서 칸에 둔 그 링크)로 이동한다 — 처리함 행에는 상세를 미리 읽지 않으므로 시트 재료가 없다(ENG-16 — 읽기 전용 상세 대신 더 작은 변경인 문서 링크를 골랐다. `withDetails`의 계수 · 투영 경로가 그대로다). 두 그룹의 행 탭 갈래는 서버가 넘긴 그룹 값(`mine`/`processed`)으로 정하고 상태 문자열을 보지 않는다. 시트: 머리(제목 `--fs-lg` + 부제 + 닫기 x 20) 고정 · 본문(KvList 행 + `ApprovalRoute` list, 최대 85vh 스크롤) · 행동 줄 고정(항목의 서버 가능 행동 목록을 그대로 — 담당이면 2차 `반려` + 1차 `승인`, 기안자이면서 담당이면 2차 `회수`(→ 04.1-05 Task 2의 회수 확인) + 1차 `승인`이고 `반려` 없음, CXF2-B-RF01. 높이 `--touch-min`, 1차가 두 배 폭, 첫 포커스 = 승인). 시트의 두 버튼은 `ui/button` `Button`이고 1차를 누르면 두 버튼 모두 `pending`(실제 `disabled` + 라벨 뒤 `…`, Codex LOW)이며, 성공하면 토스트 · 시트 닫힘 · `router.refresh()`, 충돌이면 시트 막힘 자리에 서버 문구 + 3차 `새로 고침`(시트는 닫히지 않음). 시트 골격은 `MoreSheet`의 스크림·포커스 트랩·Esc 모양을 따르되 `ui/shell`을 고치지 않는다(이 파일 안에서 같은 모양으로). 반려 버튼은 Task 2가 확인 시트와 연결한다.

⑤ `test/e2e/mobile-leave-approval.spec.ts`(파일 이름이 `mobile-`로 시작 → mobile-375 프로젝트): behavior E2E 네 줄(처리함 행 탭 → 문서 화면 — ENG-16 포함). 픽스처는 04.1-02 E2E의 전용 본부·팀 모양을 따른다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint(ui 경계 규칙 — ui가 domain/app을 import하면 error)·stylelint·tsc·next build 중 하나라도 error를 출력한다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/import-cycles.test.ts test/unit/document-kinds-import.test.ts test/unit/domain/approvals/document-kind-registry.test.ts</automated>
    <fails_when>종류가 돌려준 명세 밖 값(`hireDate` · 옛 모양 행 문자열 `ROWLEAK-7c1`)이 엔진 결과에 남거나 `loadDetails`만 있고 `detailDto`·`buildDetailRows` 없는 종류가 등록되거나(Codex MEDIUM), 숨긴 정보 항목의 값 `SECRET-NOTE-9f3`이 어느 행 문자열에라도 나타나거나 전부 허용한 검출기 사례에서 나타나지 않으면(ENG-17), SYSTEM.md 개정이 문서 구조 테스트를 깨거나, domain/approvals ↔ domain/leave 또는 `index.ts` ↔ `balance-service.ts` 순환이 생기거나(CEO-4), 결재 모듈을 부르는 app 파일이 `document-kinds`를 import하지 않으면(CEO-3) failed가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-inbox-projection.test.ts test/integration/leave-balance.test.ts</automated>
    <fails_when>상세 포함 결재함의 `findVisibility` 호출이 3회 이상이거나 연차 행 읽기가 20건 · 5건 픽스처 어느 쪽에서든 정확히 2가 아니거나(CXF2-B-RF02) 사용·조정 읽기가 기안자 수(4)를 넘거나(CEO-17 · Codex MEDIUM), 결재자 상세 직렬화에 기안자 입사일·퇴직일 문자열이 있거나(CEO-9), 10월 입사 기안자의 잔고 행이 `연차 남음 4일 · 월차 남음 5일 · 결재 중 0일 · 이번 신청 6일`이 아니거나 합계 `9일`이 있거나 두 잔고가 한 칸이거나 문자열과 DTO 값이 어긋나면(11:43 · R1/D5 · D3), 12월 중순 입사 기안자의 1월 잔고 행에 `월차 남음 0일`이 없으면(D4), 주입한 `now`(2027-03-15 · 2027-04-15)로 받은 결재함 상세 잔고 행이 그날로 계산한 `formatBalanceRow` 결과와 다르면(CX-B2), `leave.value`를 숨긴 결재자의 상세에 비고 값이 있으면(ENG-17), 04.1-01·03의 기존 투영·잔고 사례가 회귀하면 failed가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/mobile-leave-approval.spec.ts --project mobile-375</automated>
    <fails_when>playwright failed가 1 이상이다 — 시트 첫 포커스가 승인이 아니거나, 잔고 행 문자열이 다르거나, 두 번 탭에 처리 기록이 2건이거나, 처리함 행 탭이 시트를 열거나 문서 화면으로 가지 않으면(ENG-16) 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - DECISIONS.md A3 기록이 SYSTEM.md 개정과 같은 커밋이거나 앞서고, 둘 다 `ApprovalRoute` 커밋보다 앞선다
    - `ui/approval-route/*`에 `@/domain`·`@/app`·`@/repositories` import가 없다
    - 결재 시트가 연차 도메인을 직접 부르지 않는다 — 본문 행은 종류의 `loadDetails`가 준다(`grep -c "@/domain/leave" "app/(app)/approvals/approval-sheet.tsx"`이 0)
    - 시트 행동 줄 1차 폭이 2차의 두 배이고 높이가 `--touch-min`이다(CSS에 토큰 이름으로)
    - (CEO-9) `grep -cE "getLeaveBalanceForUser|getMyLeaveBalance" domain/leave/index.ts`이 0이다
    - (CEO-17) `app/(app)/approvals/page.tsx`에 `withDetails`가 있고, `domain/approvals/index.ts`에 모듈 최상위 `new Map(` 선언이 없다(`grep -nE "^(export )?const [A-Za-z_]+ = new Map\(" domain/approvals/index.ts`가 0줄)
    - (CEO-4) `grep -cE "from \"(\./index|\.|@/domain/leave)\"" domain/leave/balance-service.ts`이 0이다
    - (Codex MEDIUM) `domain/approvals/index.ts`의 `withDetails` 경로에 `detailDto`와 `project(`가 있고, 단수형(항목마다 부르는 모양) 호출이 0건이다(`grep -cE "loadDetail\(" domain/approvals/index.ts`가 0 — 복수형 `loadDetails(`는 걸리지 않는다)
    - (Codex LOW) `approval-sheet.tsx`가 `@/ui/button`을 import하고 `pending`을 넘긴다
    - (11:43) E2E 잔고 행 기대 문자열이 `연차 남음`으로 시작하고, 스펙 어디에도 월차·연차를 더한 기대값이 없다
    - (ENG-17) `domain/approvals/index.ts`에서 `buildDetailRows(` 호출의 인자가 `project(` 결과다 — 원시 `loadDetails` 결과를 `buildDetailRows`에 넘기는 줄이 없다(SUMMARY에 두 줄의 순서를 인용한다) · `domain/approvals/kinds.ts`의 `buildDetailRows` 매개변수 타입이 `Partial<…>` 투영 타입이다
    - (ENG-16) `inbox-table.tsx`에서 시트를 여는 핸들러가 `mine` 그룹 행에만 걸리고, `processed` 행은 문서 `href` 링크다
    - (CXF2-B-RF01) `approval-sheet.tsx` · `inbox-table.tsx`의 행동 버튼은 항목의 가능 행동 목록에서만 나온다 — 두 파일에 상태 문자열 비교가 없다(`grep -cE "status ===|status==" "app/(app)/approvals/approval-sheet.tsx" "app/(app)/approvals/inbox-table.tsx"`가 파일마다 0)
    - (ENG-18) SUMMARY의 쿼리 수 보고가 「노출 · 연차 행 · 사용 · 조정 · 설정」 다섯 계수와 그 값만 적고, 사용자 · 보임 판정 · 결재선 조회는 계수 밖이라고 한 줄로 밝힌다
    - (CX-B2) `domain/approvals/index.ts`의 `loadDetails(` 호출 인자에 `now`가 있고(`grep -cE "loadDetails\(.*now" domain/approvals/index.ts` ≥ 1), 연차 종류 `loadDetails`가 `getLeaveBalancesForRequests`에 받은 `now`를 넘긴다 — SUMMARY에 두 호출 줄을 인용한다. 04.1-03 수락 기준이 이 플랜 뒤에도 녹색이다 — `grep -v '^\s*//' domain/leave/balance-service.ts | grep -c "new Date("`이 0이고, `seoulToday(` 호출 수가 04.1-03 끝의 수와 같다(이 플랜이 시계 읽기를 더하지 않는다 — `D=$(git diff "$PLAN_BASE"..HEAD -- domain/leave/balance-service.ts) && printf '%s\n' "$D" | grep -c "^+.*seoulToday("`이 0 — git이 실패하면 `&&`에서 멈춘다)
    - (R1/D5) `leave-balance.test.ts`의 10월 입사 사례 기대값에 `연차 남음 15일`이 없다(`grep -c "연차 남음 15일 · 월차 남음 5일" test/integration/leave-balance.test.ts`이 0)
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재자가 폰에서 결재함 행을 탭해 근거를 보고 승인하는 흐름이 모바일 E2E로 CI에 있고, 그 상세는 노출 조회 2회 이하 · 입사일·퇴직일 없이 실린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 반려 · 회수 · 다시 신청 화면 — 확인 다이얼로그와 서버가 고른 행동 줄 넷</name>
  <files>ui/confirm-dialog/ConfirmDialog.tsx, ui/confirm-dialog/ConfirmDialog.module.css, app/(app)/leave/[id]/page.tsx, app/(app)/leave/[id]/document-actions.tsx, app/(app)/leave/new/leave-form.tsx, app/(app)/approvals/inbox-table.tsx, app/(app)/approvals/approval-sheet.tsx, test/e2e/leave-document.spec.ts, test/e2e/mobile-leave-approval.spec.ts, test/e2e/settings-approval-route.spec.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 286–319행(S3 전문 — 행동 줄 네 갈래) · 356–361행(S6) · 189–194·204·215–217행(1차·2차 라벨 · 막힘 · 제자리 결과 · 반려/회수 확인 원문)
    - Phase 4 확인 다이얼로그 계약: 로컬 `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md` 150행과 S16 절(`grep -n "S16"`으로 위치 확인), 그 브랜치에 `ui/confirm-dialog`가 있는지는 `git fetch origin claude/gsd-progress-e1nzgu` 뒤 `git show origin/claude/gsd-progress-e1nzgu:ui/confirm-dialog/ConfirmDialog.tsx`(B-C5 — 원격 참조가 로컬에 없다)
    - `docs/design/SYSTEM.md` §7-1(657–673행 — 1차·2차·3차, `aria-disabled`, 막힘 이유) · §7-8 · §7-9(`Ctrl+Enter`·`Esc`)
    - `app/(app)/leave/[id]/page.tsx` · `app/(app)/leave/new/leave-form.tsx`(04.1-02) · `app/(app)/leave/actions.ts` · `app/(app)/approvals/actions.ts`(04.1-02 — reject · withdraw · resubmit 액션)
  </read_first>
  <behavior>
    - (E2E desktop) 1단 결재자가 `/leave/[id]`를 열면 1차 `승인` + 2차 `반려`가 있고 `Ctrl+Enter`로 승인된다. 기안자가 같은 문서를 열면 2차 `회수`만 있다. 관계없는 사람은 404
    - (E2E) 결재자가 `반려`를 누르면 모달(폭 480) 제목 `연차 반려` · 부제 `{번호} · {기안자} · 종일 … · 3일` · 사유 칸 첫 포커스, 사유가 비면 1차 `aria-disabled` + `사유 없음 · 한 줄 적어 주세요`, 사유를 적고 `Ctrl+Enter`면 토스트 `반려 · {기안자}에게 돌아감`
    - (E2E) 기안자가 반려된 문서를 열면 고쳐 쓰는 폼(값 채워짐)과 1차 `연차 다시 신청`이 있고, 날짜를 고쳐 내면 토스트 `연차 다시 신청 · 결재 요청됨 → {팀장}` · 번호 그대로 · 결재선 목록에 새 차수가 보인다
    - (E2E) 기안자가 결재 중 문서에서 `회수` → 확인 모달 결과 줄 `결재 멈춤 · {팀장}의 결재함에서 빠짐`(이미 승인한 단계가 있으면 둘째 줄 `팀장 승인 기록은 남음`) → 토스트 `회수 · 결재 멈춤`, 머리 태그 `회수`, 행동 줄 사라짐, KvList에 `잔고` 행이 없다(CXF-B-F03 — 회수 전에는 있었다)
    - (E2E, CXF-B-F03) 모든 단계가 승인된 문서(머리 태그 `승인` — 스펙이 각 단계 담당으로 도메인 `approveDocument`를 불러 만든다)를 기안자와 마지막 결재자가 열면 KvList에 `잔고` 행이 없다. 같은 스펙의 결재 중 문서에는 `잔고` 행이 있다(대조). (A-01) 같은 최종 승인 문서의 결재선 목록에 `담당 없음`이 없고 단계마다 저장된 처리자와 `승인` 결과가 보인다
    - (E2E desktop-settings, CXF-B-F01 — `test/e2e/settings-approval-route.spec.ts`의 마지막 복원 확인 테스트 **앞**, 전역 `자기 승인`을 바꾸므로 직렬 `desktop-settings` 프로젝트에서만) `try` 안에서 `self_approval` = `self_approve`를 도메인으로 저장하고 전용 팀의 팀장 픽스처가 연차를 낸다(`leaveWeekdayRange(today, {week: 9, weekdays: 2})` — 테스트 맨 앞 `seoulToday()` 한 번, CXF2-B-RF03). 팀장이 `/leave/[id]`를 열면 행동 줄이 1차 `승인` + 2차 `회수`이고 `반려` 버튼이 없다. (CXF2-B-RF01) 승인 전에 `/approvals`의 PC `내 결재` 그 행에도 `승인` · `회수`만 있고 `반려`가 없으며, 같은 테스트에서 `page.setViewportSize({width: 375, height: 800})`로 바꿔 그 행을 탭하면 결재 시트 행동 줄이 1차 `승인` + 2차 `회수`이고 `반려`가 없다(시트를 닫고 다시 데스크톱 폭으로). `승인`을 누르면 토스트 뒤 행동 줄이 2차 `회수` 하나다. `finally`에서 `self_approval`을 `skip`으로 되돌린다. (대표 폴백 기안자 W8은 E2E 공유 DB에 다른 대표 픽스처가 있어 후보가 기안자 한 명이 될 수 없으므로 04.1-02 통합 사례가 증명한다 — 화면은 같은 서버 목록을 그리므로 렌더 경로는 이 사례와 같다)
    - (E2E) 결재자가 문서를 연 채로 기안자가 회수하면, 결재자의 `승인`이 행동 줄 옆에 `{기안자}이/가 HH:MM에 회수함 · 새로 고침`을 보이고 다이얼로그가 있으면 닫히지 않는다
    - (E2E desktop) 결재함 PC 행 3차 `반려`(담당인 행 — 서버 목록에 `반려`가 있을 때)가 같은 반려 모달을 연다
    - (E2E mobile-375) 결재 시트의 `반려`가 결재 시트를 닫고 반려 확인 시트(하단)를 열며, 확인 시트를 닫으면 목록 행으로 포커스가 돌아간다
  </behavior>
  <action>
① **확인 다이얼로그**: main에 `ui/confirm-dialog`가 있으면 그것을 import만 한다. 없으면 Phase 4 S16 계약(네이티브 `<dialog>` `showModal()`, ≥700 폭 `--modal-w` 모달 / <700 하단 시트, 슬롯 = 제목 · 부제 · 결과 줄 0~3 · 확인 근거 한 칸 · 막힘 이유 자리(1차 왼쪽) · 2차(1차 라벨에 「취소」가 있으면 `닫기 Esc`, 아니면 `취소 Esc`) · 1차 + `Ctrl+Enter`)대로 **이 페이즈가 쓰는 슬롯만** 최소형으로 만든다 — 목록형 시트 등 쓰지 않는 갈래는 만들지 않는다. 포커스: 열릴 때 확인 근거 칸이 있으면 그 칸, 없으면 1차. 닫힐 때 트리거 복귀.

② `app/(app)/leave/[id]/page.tsx`: `document-kinds` import를 유지·보장하고(CEO-3 가드 조건), `getApprovalView`가 준 가능 행동 **목록을 그대로** `document-actions.tsx`에 넘긴다(금지 항목 — 클라이언트가 상태로 고르지 않는다. 기안자가 지금 담당이기도 하면 서버 목록이 `[승인, 회수]` 합집합이다 — 04.1-02 ④-b · CXF-B-F01). `getApprovalView`의 결재선은 04.1-01의 `at: "before_action"` 계산이라 막힘과 고아 최종(D2)이 같게 온다 — 지금 단계는 `ApprovalRoute`가 `— {단계 이름} · 담당 없음`으로 그리고, 결재자 쪽 행동 줄은 없으며 기안자 쪽은 `회수` 갈래 그대로다. 화면은 결재선을 따로 계산하거나 상태로 「최종」을 해석하지 않는다(X-1). 이 walkRoute 계산은 진행 중(`submitted` · `in_review`) 문서에만 있다 — 종결 상태(`approved` · `rejected` · `withdrawn`)의 결재선은 04.1-01 `getApprovalView`가 walkRoute 없이 저장된 단계 처리 기록만으로 주므로 화면에 지금 단계 · `담당 없음` 줄이 없고 끝난 문서를 열어도 `route_blocked` 로그가 생기지 않는다(A-01 — 판정은 01 몫, 화면은 받은 목록만 그린다). KvList에 `잔고` 행(재택이면 없음)과 `결재선` 행(`ApprovalRoute` list)을 더한다. `잔고` 행은 진행 중(`submitted` · `in_review`) 문서에만 있다 — 서버 페이지가 결재 보기의 상태가 진행 중일 때만 신청 잔고를 읽어 넘기고, 승인 · 반려 · 회수된 문서에서는 읽지도 그리지도 않는다(UI-SPEC 315행 · CXF-B-F03). 폰에서 행동 줄은 하단 탭 위에 고정, 본문 아래 여백 = 탭 44 + 행동 줄.

③ `document-actions.tsx`: 서버 목록의 행동을 **전부** 그린다 — 갈래를 하나로 고르지 않는다(CXF-B-F01). 담당 = 1차 `승인`(`Ctrl+Enter`, 즉시) + 2차 `반려`(→ 반려 확인) / 기안자 = 지금 담당(목록 `[승인, 회수]`) = 1차 `승인` + 2차 `회수`, `반려` 없음 / 기안자·결재 중 = 2차 `회수`(→ 회수 확인, 결과 줄은 서버가 준 「이미 승인한 단계」 여부로) / 기안자·반려(가능 행동에 `다시 신청`이 있을 때만 — `leave` write가 거둬진 기안자에게는 서버가 이 행동을 빼므로 폼 · 1차 `연차 다시 신청`이 그려지지 않고 행동 줄이 없다. 화면은 권한을 따로 보지 않는다, 04.1-02 ④-b · CX-W1) = `leave-form.tsx`를 다시 신청 모드로(값 채움 · 1차 `연차 다시 신청` · 액션 `resubmitLeaveAction` · 결재선은 `ApprovalRoute` line으로 다시 신청 시점 미리보기) / 그 밖 = 없음. 버튼은 전부 `ui/button` `Button`이고 누르면 그 줄의 버튼이 모두 `pending`(실제 `disabled` + `…` — Codex LOW, `aria-disabled`는 사유 없음 같은 막힘 이유에만), 성공 토스트는 truths의 문구, 충돌은 누른 자리 옆 한 줄 + 3차 `새로 고침`. 확인 다이얼로그의 1차도 같은 `pending`을 쓴다.

④ `leave-form.tsx`에 다시 신청 모드 prop을 더한다(초기값 · 1차 라벨 · 제출 액션만 바뀜 — 04.1-06이 같은 파일에 잔고 행 · 결재선 한 줄 · 입력 버리기를 더하므로 구조를 크게 바꾸지 않는다).

⑤ `inbox-table.tsx`의 PC `내 결재` 행은 그 항목의 서버 가능 행동 목록을 그대로 그린다 — 목록에 `반려`가 있을 때만 3차 `반려`(→ 같은 반려 확인), `회수`가 있으면 3차 `회수`(→ 회수 확인)를 더한다(기안자이면서 담당인 행 = `승인` + `회수`, `반려` 없음 — CXF2-B-RF01). 행 컴포넌트는 상태 · 기안자 여부를 보지 않는다. `approval-sheet.tsx`의 `반려`가 결재 시트를 닫고 반려 확인 시트를 연다(중첩 없음, 닫히면 목록 행으로 포커스). 시트의 `회수`(기안자이면서 담당인 항목 — CXF2-B-RF01)도 같은 모양으로 회수 확인 시트를 연다.

⑥ E2E: `test/e2e/leave-document.spec.ts`(desktop) behavior의 desktop 줄(desktop-settings · mobile-375 표시 줄 제외 — CXF-B-F03 두 줄 포함), `test/e2e/settings-approval-route.spec.ts`에 CXF-B-F01 줄(04.1-04 파일 — 마지막 복원 확인 테스트 앞에 넣고 그 테스트가 여전히 파일 맨 끝이다), `mobile-leave-approval.spec.ts`에 마지막 줄. 신청·다시 신청 날짜는 전부 04.1-02의 `leaveWeekdayRange(today, {week, weekdays})`로 만든다 — `today`는 **테스트마다 맨 앞에서 `seoulToday()`를 한 번** 불러 얻고, 그 테스트의 신청 · 다시 신청 · 기대 문자열이 전부 이 값 하나를 쓴다(신청과 다시 신청 사이에 서울 날짜 · 연도가 바뀌어도 한 사례가 두 날짜에서 나오지 않게, CXF2-B-RF03. 04.1-06의 `onStableSeoulDay`보다 먼저 도는 플랜이라 그 헬퍼는 쓰지 않는다)(이 스펙은 `week` 4~8을 쓴다 — 04.1-02 · 모바일 스펙과 기간이 겹치지 않게, Codex HIGH 06). 날짜 리터럴을 쓰지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/document-kinds-import.test.ts test/unit/import-cycles.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>`/leave/[id]` 쪽 app 파일이 결재 모듈을 부르면서 `document-kinds`를 import하지 않거나(CEO-3), import 순환이 생기거나(CEO-4), eslint·stylelint·tsc·next build 중 하나라도 error를 출력하면 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/leave-document.spec.ts test/e2e/leave-approval.spec.ts --project desktop &amp;&amp; pnpm playwright test test/e2e/settings-approval-route.spec.ts --project desktop-settings --no-deps</automated>
    <fails_when>playwright failed가 1 이상이다 — 행동 줄 갈래 · 반려 사유 막힘 · 다시 신청 번호 유지 · 동시 처리 거부 표시 중 하나라도 다르거나, 승인 · 회수된 문서에 `잔고` 행이 있거나(CXF-B-F03), 최종 승인 문서의 결재선에 `담당 없음`이 보이거나(A-01), 본인 승인 팀장의 행동 줄이 `승인` + `회수`가 아니거나 `반려`가 있거나, 마지막 복원 확인 테스트가 `건너뜀`을 못 보면(CXF-B-F01) 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/mobile-leave-approval.spec.ts --project mobile-375</automated>
    <fails_when>playwright failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 문서 화면 행동 줄이 서버 가능 행동 목록 그대로 그려진다 — 합집합 `[승인, 회수]` 포함(CXF-B-F01)(클라이언트 컴포넌트에 상태 문자열 비교가 없다 — `grep -cE "status ===|status==" "app/(app)/leave/[id]/document-actions.tsx"`이 0)
    - (CXF-B-F03) `app/(app)/leave/[id]/page.tsx`가 신청 잔고를 진행 중 상태일 때만 읽는다 — SUMMARY에 그 조건 줄을 인용한다 · 승인 · 회수 문서의 `잔고` 행 부재 E2E가 녹색이다
    - (CXF-B-F01) `settings-approval-route.spec.ts`의 마지막 테스트가 여전히 복원 확인이고, 본인 승인 사례가 `finally`에서 `skip`을 되돌린다(`grep -c "self_approve" test/e2e/settings-approval-route.spec.ts`이 1 이상 · 04.1-04의 `grep -cE "[1-4]단 사용"` 0건 기준 유지)
    - (CX-W1) 다시 신청 폼은 서버 가능 행동의 `다시 신청`으로만 열린다 — `document-actions.tsx`에 권한 판정 호출이 없다(`grep -cE "canWriteLeave|can\(" "app/(app)/leave/[id]/document-actions.tsx"`이 0). write 회수 뒤 가능 행동 빈 목록은 04.1-02 `leave-permission.test.ts`가 단언한다
    - 확인 다이얼로그를 main에서 가져왔는지 새로 만들었는지가 SUMMARY에 적혔고, 새로 만들었다면 S16 계약 대조표(슬롯별 있음/없음)가 SUMMARY에 있다
    - 폼이 한 벌이다(`app/(app)/leave` 아래 `*form*.tsx` 파일이 `new/leave-form.tsx` 하나)
    - `ui/button` diff가 비어 있다 · `document-actions.tsx`가 `@/ui/button`의 `pending`을 쓴다(Codex LOW)
    - (X-1) 화면 층이 결재선을 직접 계산하지 않는다 — `grep -rn "domain/approvals/route" app ui | wc -l`이 0(막힘 · 고아 최종의 `담당 없음` 판정은 04.1-01 통합 describe 「담당 소멸 뒤 고아 최종」이, 기안자 회수는 04.1-02 Task 2가 증명한다)
    - (CXF2-B-RF03) 두 스펙과 `settings-approval-route.spec.ts`에서 `seoulToday()` 호출 수가 파일마다 `test(` 수 이하다(`for f in test/e2e/leave-document.spec.ts test/e2e/mobile-leave-approval.spec.ts test/e2e/settings-approval-route.spec.ts; do [ "$(grep -c 'seoulToday()' "$f")" -le "$(grep -cE '^\s*test\(' "$f")" ] || echo "$f"; done`가 아무것도 출력하지 않는다 — 테스트마다 맨 앞 한 번)
    - (Codex HIGH 06) `grep -cE '"20[0-9]{2}-[0-9]{2}-[0-9]{2}"' test/e2e/leave-document.spec.ts test/e2e/mobile-leave-approval.spec.ts`가 0이고 두 스펙이 `leaveWeekdayRange`를 쓴다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>반려(사유 필수) · 회수 · 다시 신청이 확인 다이얼로그와 서버가 고른 행동 줄로 화면에서 되고, 동시 처리 거부가 누른 자리에 보인다</done>
</task>

<task type="auto">
  <name>Task 3: 다섯 상태와 접근성 — 로딩 · 오류 경로, axe, 독립 DOM 감사</name>
  <files>app/(app)/approvals/loading.tsx, app/(app)/approvals/loading.module.css, app/(app)/approvals/error.tsx, app/(app)/leave/[id]/loading.tsx, app/(app)/leave/[id]/error.tsx, test/e2e/leave-a11y.spec.ts</files>
  <read_first>
    - `docs/design/SYSTEM.md` §7-7(805–833행) · §10(1031–1042행) · §11(1043행~)
    - `app/(app)/error.tsx` · `app/(app)/projects/loading.tsx`(기존 선례 — fallback을 즉시 그린다. 지연 장치가 없으므로 이 플랜이 CSS로 만든다, Codex LOW)
    - `test/e2e/a11y.spec.ts` — axe 호출 모양(이 파일은 고치지 않는다)
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 435–444·466–471·484–488행(loading · error · backstop 행)
  </read_first>
  <action>
① `app/(app)/approvals/loading.module.css`: 뼈대 감싸개 클래스 `.delayed` — 처음 `opacity: 0`, 한 번만 도는 키프레임(끝 `opacity: 1`)을 `animation-delay: 300ms` · `animation-fill-mode: forwards` · 길이 0으로 건다(반짝임·반복·진행 바 없음, 새 토큰 없음). `app/(app)/approvals/loading.tsx` · `app/(app)/leave/[id]/loading.tsx`: 이 클래스로 감싼 PageHeader + 표 머리글(또는 KvList) 뼈대 + `--surface` 행 3개 — 300ms 안에 끝나는 스트리밍에서는 뼈대가 눈에 띄지 않는다. 04.1-06의 `/leave` 로딩도 같은 CSS 모듈을 import한다.

② `error.tsx` 둘: 본문 자리 `--danger` 한 줄(`결재함을 불러오지 못했습니다` / `연차 문서를 불러오지 못했습니다`) + 2차 `다시 시도`(`retry` — Next 16.3 표준 인자, 서버 데이터를 다시 가져온다. 다시 가져오지 않는 다른 인자를 쓰면 DB 일시 오류 뒤 같은 오류가 반복된다: `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md` 117–157행. 모양은 기존 `app/(app)/projects/error.tsx`의 `ListEmpty tone="error"` + `retry`, B-C3). 사과·느낌표 없음.

③ `test/e2e/leave-a11y.spec.ts`(desktop + 같은 파일 안 375 뷰포트 한 사례): `/approvals` · 결재 시트 열린 상태 · 반려 확인 열린 상태 · `/leave/[id]`에서 axe 위반 0, 결재함 caption `결재함`, 시트·확인의 첫 포커스 · Esc 닫힘 · 트리거 복귀를 단언한다.

④ **독립 DOM 감사**: 이 task의 실행자가 아닌 별도 서브에이전트(모델을 명시)에게 `CI=true`로 프로덕션 빌드를 띄워 `/approvals` · 결재 시트 · 반려/회수 확인 · `/leave/[id]`를 폭 1280 · 1024 · 375에서 DOM으로 실측하게 한다 — 가로 스크롤 0 · 폰 행 높이 ≥ 44 · 시트 본문 세로 스크롤과 행동 줄 고정 · 700 경계 모달↔시트 · 이름 10자/부서명 12자/사유 200자 줄바꿈. 스크린샷 육안 판정 금지. 감사 결과를 SUMMARY에 표로 남기고 위반을 고친 뒤 전체 게이트를 **한 번** 돌린다(CLAUDE.md 화면 검증 순서).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/leave-a11y.spec.ts</automated>
    <fails_when>playwright failed가 1 이상이다 — axe 위반이 하나라도 있거나 포커스 단언이 실패하면 여기서 깨진다</fails_when>
    <automated>pnpm test:e2e:ci</automated>
    <fails_when>`CI=true` 프로덕션 빌드 E2E 전체(desktop → mobile-375 → desktop-settings)에서 failed가 1 이상이거나 「did not run」이 1 이상이거나 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 로딩 두 파일에 반짝임 애니메이션·진행 바가 없다(`grep -rniE "shimmer|progress" "app/(app)/approvals/loading.tsx" "app/(app)/leave/[id]/loading.tsx"`이 0)
    - (Codex LOW) `grep -c "animation-delay: 300ms" "app/(app)/approvals/loading.module.css"`이 1이고 `infinite`가 없으며, 두 `loading.tsx`가 이 모듈을 import한다
    - 독립 DOM 감사 표(표면 × 폭 × 판정)가 SUMMARY에 있고 위반 0으로 끝났다
    - `test/e2e/a11y.spec.ts` diff가 비어 있다
    - (B-C3) 두 `error.tsx`가 `retry`를 쓰고 `reset`을 쓰지 않는다(`grep -c "reset" "app/(app)/approvals/error.tsx" "app/(app)/leave/[id]/error.tsx"`이 파일마다 0)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재 화면들이 다섯 상태·접근성 계약을 지키고, 독립 DOM 감사와 `CI=true` E2E 전체가 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 → 행동 줄 | 가능 행동이 서버에서만 정해져야 한다 |
| 사용자 입력(사유) → 화면 | 다른 사람의 사유가 결재선 목록에 그려진다 |
| 결재함 → 문서 상세 미리 읽기 | 결재자가 볼 수 없는 문서 상세가 실리면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-27 | Elevation of Privilege | 문서 화면 행동 줄 | high | mitigate | 행동 갈래를 서버 `getApprovalView`가 고르고 클라이언트는 그리기만(금지 항목). 액션은 04.1-02의 서버 재판정을 그대로 탄다 — 숨긴 버튼을 되살려 호출해도 거부 |
| T-04.1-28 | Information Disclosure | 결재 시트 상세 미리 읽기 | high | mitigate | `listMyInbox`의 `withDetails`가 항목마다 종류의 보임 규칙(기안자 · 후보 · 처리자 — `canSeeLeaveDocument`)을 먼저 확인하고, 잔고는 입사일·퇴직일 필드가 명세에 없는 `getLeaveBalanceForRequest`(신청용 DTO)만 탄다 — 기안자의 발표 전 퇴직일이 결재자 RSC 페이로드로 가지 않는다(CEO-9). 통합이 상세 직렬화에 두 날짜가 없음을, E2E가 관계없는 사람의 404를 단언 |
| T-04.1-29 | Tampering (XSS) | 반려 사유 · 비고 렌더 | medium | mitigate | React 텍스트 노드로만 렌더 — `dangerouslySetInnerHTML` 0건(`grep -rn dangerouslySetInnerHTML ui/approval-route app/\(app\)/leave app/\(app\)/approvals`가 0) |
| T-04.1-30 | Tampering | 두 번 탭 · 동시 처리 | medium | mitigate | `ui/button` `pending`(실제 `disabled`) + 서버 version 충돌(04.1-02). 모바일 E2E가 두 번 탭에 `disabled`와 기록 1건을 단언 |
| T-04.1-31 | Denial of Service | 결재함 상세 미리 읽기 | low | mitigate | 노출 조회는 요청 단위 메모 하나로 20건에 2회 이하, 종류 상세는 `loadDetails` 한 번(연차 행 읽기는 목록 요약 1 + 상세 1 = 요청당 2회, 항목 수와 무관 — CXF2-B-RF02), 잔고 재료는 기안자 수만큼 — 세는 주입으로 통합 단언(CEO-17 · Codex MEDIUM). 계수 범위는 노출 · 행 · 사용 · 조정 · 설정 다섯 읽기이고 사용자 · 보임 판정 · 결재선 조회는 04.1-01의 기존 읽기라 세지 않는다(ENG-18 — 30명 규모 · LOW라 전체 SQL 계수 주입을 두지 않고 문구를 좁혔다) |
| T-04.1-60 | Information Disclosure | 새 문서 종류의 `loadDetails` 반환값 | medium | mitigate | 종류는 구조 필드만 돌려주고 → 엔진이 `detailDto`(등록 DTO — 필드마다 정보 항목, 누수 스캔 대상)로 정보 항목별 `project()` → 투영 결과만 종류의 `buildDetailRows`가 문자열 행으로 만든다. 행 문자열 안에 숨긴 항목 값이 들어갈 경로가 없다. `loadDetails`가 있는데 `detailDto`·`buildDetailRows`가 없으면 등록 거부. 명세 밖 키 탈락 · 숨긴 정보 항목 값이 어떤 행 문자열에도 없음(단위 + 실제 연차 종류 `leave.value` 숨김 통합) 사례(Codex MEDIUM · ENG-17) |
| T-04.1-61 | Information Disclosure(오인) | 결재자 잔고 행 | low | mitigate | 입사 다음 해 기안자의 월차·연차를 따로 싣고 합계를 싣지 않는다 — 10월 입사 기안자 통합 사례(사용자 요구 11:43 · 비례 연차 4일 + 월차 5일, 합 `9일` 부재 — R1/D5) |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(확인 다이얼로그·시트는 네이티브 `<dialog>`). `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test:unit && pnpm test:integration` 통과 — 그 안에서 `document-kinds-import` 가드(CEO-3) · `import-cycles`(CEO-4) · `approvals-inbox-projection`(CEO-17 · CEO-9) 녹색
- `pnpm test:e2e:ci`(CI=true 프로덕션 빌드, desktop → mobile-375 → desktop-settings) 통과
- 독립 DOM 감사 위반 0(SUMMARY 표)
- `git diff --stat "$PLAN_BASE"..HEAD -- ui/button ui/shell ui/next-turn package.json pnpm-lock.yaml` 비어 있음
</verification>

<success_criteria>
- 결재자가 폰에서 근거를 보고 승인·반려한다(EXP-05)
- 문서 화면이 지금 누구 차례인지, 누가 언제 처리했는지를 결재선 목록으로 보여 준다(EXP-04)
- 회수·다시 신청·동시 처리 거부가 화면에서 규칙대로 보인다(EXP-03)
- 잔고 행이 결재 옆판(폰 시트 · PC 문서 화면)에 연차 남음 · 월차 남음(따로 — 근속 첫 1년이면 0일도 보임, D4) · 결재 중 · 이번 신청으로 보이고, 남음은 부여(+조정) − 사용 − 결재 중이다(LEAV-01 · 11:43 · D3) — 입사일·퇴직일은 결재자에게 가지 않는다(CEO-9)
- 결재함 한 번이 노출표를 정보 항목 수만큼만, 상세를 한 번에, 잔고 재료를 기안자 수만큼만 읽는다(CEO-17 · Codex MEDIUM — 계수 범위 노출·행·사용·조정·설정, ENG-18)
- 새 문서 종류의 상세도 등록 DTO로 정보 항목별 투영을 거친 뒤에만 문자열 행이 되어 결재자에게 간다(Codex MEDIUM · ENG-17)
- 폰 결재함의 처리함 행은 시트 대신 문서 화면으로 간다(ENG-16)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컴포넌트 | `ui/approval-route/ApprovalRoute`(line · list) · `ui/confirm-dialog/ConfirmDialog`(main에 없을 때만) · `app/(app)/approvals/approval-sheet.tsx` · `app/(app)/leave/[id]/document-actions.tsx` |
| 화면 | `/approvals`(폰 행 탭 → 시트 · PC 반려) · `/leave/[id]`(행동 줄 넷 · 잔고 · 결재선) · 로딩·오류 경로 넷(`loading.module.css` 300ms 지연) |
| 함수 | `listMyInbox`의 선택 `withDetails`(상세까지 같은 노출 메모 · `detailDto` 투영 — CEO-17 · Codex MEDIUM) · 종류 정의 `loadDetails(viewer, ids, deps?)`(구조 필드) + 필수 `detailDto` · `buildDetailRows`(투영 뒤 행 생성 — ENG-17)(연차 구현 — 신청용 잔고 DTO만, CEO-9) · `getLeaveBalancesForRequests`(기안자별 재료 메모) · `getLeaveBalanceForRequest`(id 하나 위임) |
| 디자인 문서 | DECISIONS.md A3 · SYSTEM.md §6-3 · §6-1 · §7-8 개정 |
| 테스트 | `test/e2e/mobile-leave-approval.spec.ts` · `test/e2e/leave-document.spec.ts` · `test/e2e/leave-a11y.spec.ts` · `test/integration/approvals-inbox-projection.test.ts`(04.1-01 파일에 상세 describe 추가) · `test/unit/domain/approvals/document-kind-registry.test.ts`(detailDto 사례 추가) · `test/integration/leave-balance.test.ts`(결재자 분리 잔고 사례 추가) |
| 변경된 기존 심볼 | `leave-form.tsx`(다시 신청 모드) · 결재함 표(폰 탭 · PC 반려) · 문서 종류 정의(선택 필드 loadDetails · 필수 detailDto · buildDetailRows — ENG-17) · 결재함 폰 처리함 행(시트 → 문서 링크, ENG-16) |


## Review Dispositions Ledger

### Round 1 — 1b86370

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| generic `loadDetail`이 등록 DTO 투영을 우회(L157@1b86370) | MEDIUM | Plan 05, Task 1 ③: 종류 필수 `detailDto`(registerDto) + 엔진이 `project()` 뒤 행은 문자열 칸만 · 명세 밖 키 탈락 단위 사례 · `loadDetails`만 있고 `detailDto` 없는 종류 등록 거부 · T-04.1-60 |
| 「20건에 조회 2회 이하」가 visibility만 측정(L158@1b86370) | MEDIUM | Plan 05, Task 1 ③: `loadDetails` 일괄(연차 행 쿼리 1회) + `getLeaveBalancesForRequests` 기안자별 재료 메모 · 세는 주입 통합 단언(노출 ≤ 2 · 행 1 · 사용·조정 = 기안자 수 4 · 설정 ≤ 연도 수) · truth 개정 |
| 300ms 지연 로딩의 실제 메커니즘 없음(L159@1b86370) | LOW | Plan 05, Task 3 ①: `app/(app)/approvals/loading.module.css` `animation-delay: 300ms` 한 번 나타남(04.1-06 `/leave` 로딩도 재사용) · 수락 기준 grep |
| 승인 시트가 `aria-disabled`만 명시(L160@1b86370) | LOW | Plan 05, Task 1 ④ · Task 2 ③: 시트 · 문서 행동 줄 · 확인 1차가 `ui/button` `pending`(실제 `disabled`), `aria-disabled`는 막힘 이유에만 · E2E가 두 번 탭 `disabled` 단언 |
| E2E 날짜가 연말에 불가능(L187@1b86370 — 이 플랜 E2E에도 해당) | HIGH | Plan 05, Task 1 behavior · Task 2 ⑥: 04.1-02 `leaveWeekdayRange`만 사용(주 오프셋 분리), 날짜 리터럴 0건 수락 기준 |
| 결재자 옆판에 입사 다음 해 월차·연차가 합쳐 보이지 않게(사용자 요구 2026-09-24 11:43, 조정자 전달) | — | Plan 05 truths(잔고 행 1행 분리 · 2행 차감 예정) · Task 1 behavior(10월 입사 기안자 통합 · 모바일 E2E `연차 남음`) · T-04.1-61 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Codex 지적은 전부 반영했다 |

### Round 2 — plan-eng-review 2026-09-24(`docs/designs/plant8-erp-phase04.1-eng-review-260924.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-11 월차 0일 표시 | MEDIUM | truth 잔고 행 · Task 1 behavior D4 사례(12-15 입사 · 1월 `월차 남음 0일` · 근속 1년 뒤 칸 없음) — 공통 정의 D4 |
| ENG-12 「남음」 정의 | MEDIUM | truth 잔고 행 · 성공 기준에 D3(부여(+조정) − 사용 − 결재 중, 행의 결재 중 = 이 문서 제외) · 10월 입사 사례가 문자열과 DTO 값을 같이 단언 |
| ENG-16 폰 처리함 행 | MEDIUM | 문서 링크를 골랐다(읽기 전용 상세보다 작음 — 기존 문서 칸 링크 재사용, 읽기 수 불변). truth · Task 1 ④ · 승인 직후 처리함 행 탭 E2E |
| ENG-17 행 문자열 속 값 | MEDIUM | `loadDetails` = 구조 필드 → 정보 항목별 `project()` → `buildDetailRows(projected)` 순서 고정 · 숨긴 항목 단위(검출기 포함) + `leave.value` 숨김 통합 · T-04.1-60 |
| ENG-18 쿼리 계수 범위 | LOW | 문구를 좁혔다(전체 SQL 계수 주입보다 작음) — truth · behavior · T-04.1-31 · 성공 기준 |
| R1 입사 다음 해 연차(사용자 결정 21:45 카드 B · D5) | — | 10월 입사 사례를 비례 연차 4일로 다시 계산 — `연차 남음 4일 · 월차 남음 5일 · 결재 중 0일 · 이번 신청 6일` / `차감 예정 월차 5일 · 연차 1일` · 잔여 초과 없음 · 합 `9일` 부재 |

### Round 3 — plan-eng-review 2회차 2026-09-25 — 4078bd8(`docs/designs/plant8-erp-phase04.1-eng-review2-260925.md`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| A-01(T5, 05 몫) 종결 문서도 walkRoute를 돌려 끝난 문서마다 가짜 `route_blocked` · `담당 없음` | 경고 | 판정(`getApprovalView` · `canSeeLeaveDocument` 상태 조건 + `log.warn` 0회 통합)은 04.1-01 몫(다른 플래너). Plan 05, Task 2 ②: walkRoute 결재선은 진행 중(`submitted` · `in_review`)에만, 종결 상태는 저장된 처리 기록만 · `route_blocked` 없음 한 줄 · truth(결재선 컴포넌트) · CXF-B-F03 E2E에 최종 승인 문서 결재선 `담당 없음` 부재 단언 · verify fails_when |
| B-A1(T7, 05 몫) 액션 반환값 투영 — 토스트 문구 | 경고 | 투영은 04.1-02 ④. Plan 05 truth(성공 토스트)에 「이름 · 일수 조각은 투영을 지난 뒤에만, 빠지면 그 조각을 뺀 문구 — 받은 필드로만 조립」 한 줄 |
| B-C3(P3) 오류 경계 재시도가 `reset`으로 적힘 | P3 | Plan 05, Task 3 ②: `retry`(Next 16.3 문서 `error.md` 117–157행 · 기존 `projects/error.tsx` 모양 — 실측 확인) · 수락 기준 grep. 04.1-02 Task 3의 재시도 금지 grep은 `error.tsx`를 뺀다(04.1-02 Round 3) |
| B-C5(P3) 참조 브랜치가 로컬에 없음 | P3 | Plan 05, execution_notes · Task 2 read_first: 로컬 `.planning/phases/04-project-quote-ledger/04-UI-SPEC.md`(150행 실측) 1순위 · 구현 파일은 `git fetch origin claude/gsd-progress-e1nzgu` 뒤 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B-C4(P3) ConfirmDialog `aria-disabled` 막힘이 `ui/button` API와 안 맞음 | 다이얼로그가 막힘 이유 슬롯을 직접 그리는 구현 방식 선택이고 새 E2E(B-T6)가 따라온다 — 이번 회차 P3는 한 문장 교정만. 실행자가 S16 계약대로 막힘 자리를 다이얼로그 안에 둔다(Task 2 ①이 이미 「막힘 이유 자리(1차 왼쪽)」 슬롯을 요구) |
| B-T4(P3) 옛 탭 충돌 표시 E2E가 문서 화면에만 있고 결재함 PC 행은 없음 | 새 E2E 사례 — 서버 문구는 04.1-02 통합이 증명하고 렌더는 `ui/table` `cellIssue` 기존 경로다 |
| B-T6(P3) 사유 없음 + `Ctrl+Enter` 요청 0건 E2E 없음 | 새 E2E 사례 — B-C4와 같이 다음 회차 후보 |
| B-C7(P3) E2E 픽스처 이름이 모두 같아 이름 단언이 공허(05:195) | 새 픽스처 코드 — 04.1-02 Round 3 Deferred와 같은 이유 |
| Codex B-NEW03(참고) 본문과 승인 `version`의 동일 스냅숏 계약 없음(05:277) | 04.1-02 Round 3 Deferred와 같은 이유 |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-05-SUMMARY.md` when done
</output>
