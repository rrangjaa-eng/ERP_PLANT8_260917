---
phase: 04.1-approvals-leave
plan: 06
type: execute
wave: 6
depends_on: ["04.1-03", "04.1-05"]
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - ui/shell/role-menu.ts
  - ui/shell/TopBar.tsx
  - test/unit/ui/role-menu.test.ts
  - app/(app)/leave/page.tsx
  - app/(app)/leave/year-param.ts
  - app/(app)/leave/leave-table.tsx
  - app/(app)/leave/loading.tsx
  - app/(app)/leave/error.tsx
  - domain/leave/index.ts
  - repositories/leave-requests.ts
  - test/integration/leave-earliest-year.test.ts
  - app/(app)/leave/new/page.tsx
  - app/(app)/leave/new/leave-form.tsx
  - app/(app)/leave/actions.ts
  - app/(app)/leave/actions.registry.ts
  - app/(app)/admin/people/[id]/page.tsx
  - app/(app)/admin/people/[id]/leave-section.tsx
  - app/(app)/admin/people/[id]/actions.ts
  - app/(app)/admin/people/[id]/actions.registry.ts
  - app/(app)/admin/people/person-form.tsx
  - app/(app)/admin/people/actions.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/leave-list.spec.ts
  - test/e2e/mobile-leave-list.spec.ts
  - test/e2e/mobile-shell.spec.ts
  - test/e2e/leave-dates.ts
  - test/unit/e2e/leave-dates.test.ts
  - test/e2e/admin-person-leave.spec.ts
  - test/e2e/people.spec.ts
  - test/e2e/mobile-people.spec.ts
  - test/e2e/admin-master-list-first.spec.ts
  - test/e2e/corp-cards.spec.ts
  - test/e2e/mobile-corp-cards.spec.ts
  - test/e2e/single-column.spec.ts
autonomous: true
requirements: [LEAV-01, EXP-05]

estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원은 계정 그룹 「연차」(폰 「더보기」 시트 `내 정보 · 연차 · 설정 · 로그아웃`, PC 사용자 메뉴 `관리 · 내 정보 · 연차 · 로그아웃` — 「관리」는 권한 있을 때만 맨 위, SYSTEM.md §6-0 (a)와 `TopBar.tsx` 합성 순서 그대로)로 `/leave`에 들어온다 — 1차 메뉴 다섯과 폰 하단 탭 넷은 그대로이고, 항목은 `leave` view 권한이 있을 때만 서버가 보낸다. E2E가 관리자·비관리자 두 경우의 PC 메뉴 순서를 정확히 단언한다(UI-SPEC S10 · A1 · Codex MEDIUM)"
    - "`/leave` 머리에 잔고 줄(`연차 15일 · 사용 3.5일 · 결재 중 1일 · 남음 10.5일`, 조정이 있으면 `조정 -1일`, 둘째 줄 월차는 조회 기준일이 근속 첫 1년 안이거나 조회 연도에 월차 부여가 하나라도 있으면 값이 0이어도 `월차 적립 0일 · 사용 0일 · 결재 중 0일 · 남음 0일 · {소멸일} 소멸`로 있고(D4), 입사일이 없으면 `월차 계산 불가 · 입사일 없음`)이 있고 합계 행은 없다 — 잔고 줄이 합계다. `남음` = 부여(+조정) − 사용(승인됨) − 결재 중(D3). 입사 다음 해 직원은 연차 줄(입사 연도 근무 기간 비례 부여 — 04.1-03 `annualGrantQuarters`, R1/D5)과 월차 줄을 **따로** 보고, 두 `남음`을 더한 숫자는 머리 어디에도 없다(UI-SPEC S1 · A4 · Assumptions #18 · 사용자 요구 2026-09-24 11:43)"
    - "`/leave` 목록은 상태 그룹 `반려 → 결재 중 → 승인 → 회수` 순서, 그룹 안은 시작일 오름차순, 빈 그룹은 머리글째 없고 하나뿐이어도 머리글을 그린다. 행 전체가 `/leave/[id]` 링크다(UI-SPEC S1 populated · zero-one-many)"
    - "이번 회계연도 신청이 0건이면 `{올해} 회계연도에 신청한 연차가 없습니다 · 연차 신청`(3차 → `/leave/new`)이 보이고 머리 1차 `연차 신청`은 렌더되지 않으며 잔고 줄은 남는다. `leave` write가 없는 사람(보기만)에게는 머리 1차와 EMPTY 3차 `연차 신청`이 둘 다 없다 — EMPTY는 앞 문장만(CX-R5). 지난 연도(`?year={올해-1}`) 0건은 `… · 올해 보기`다(UI-SPEC S1 empty). 연도 규칙(D6 · ENG-8): `?year=`가 정수이고 2000 ≤ year ≤ 올해이면 그 연도를 그대로 보여 준다(신청이 없어도 — 그때가 위 지난 연도 EMPTY). 미래 연도 · 형식 오류 · 범위 밖만 올해로 바꾼다. 연도 select 옵션 = min(가장 이른 신청 연도, 요청 연도) ~ 올해 — 가장 이른 신청 연도는 도메인 읽기 `earliestMyLeaveYear(viewer)`의 `min(fiscal_year)` 쿼리 한 번이다(해마다 조회하지 않는다, C-04 · C-P1). 여기서 「올해」는 `seoulToday()`(04.1-01 `lib/dates.ts`)의 연도다 — 서울 1월 1일 00:00~09:00에도 새해가 기본 연도다(CEO-11)"
    - "`/leave/new`에서 `연차 신청`을 빠르게 두 번 누르거나 `Ctrl+Enter`를 연달아 쳐도 새 신청은 한 건이다 — 제출 중에는 1차가 `ui/button`의 `pending`(`연차 신청…` · 비활성)이고 두 번째 누름·`Ctrl+Enter`는 아무것도 하지 않는다. 서버 멱등키·새 인덱스는 두지 않는다 — 혹시 생긴 중복은 기안자가 회수한다(CEO-12)"
    - "새 E2E의 기대값은 실행 날짜와 다른 스펙의 데이터에 묶이지 않는다 — 연도는 스펙이 `seoulToday()`에서 계산하고 신청 날짜는 04.1-02의 `leaveWeekdayRange(seoulToday(), …)`로만 만들어 1월 1일에도 12월 31일에도 그해 안의 유효한 평일이다(Codex HIGH 06), 잔고 기대값을 단언하는 스펙은 그 스펙 전용 픽스처 사용자(입사일 없음 · 신청 기록 없음, 04.1-02 E2E의 전용 본부·팀 모양)를 만든다. 새 E2E 세 파일에 네 자리 연도 리터럴이 없다(CEO-15). 날짜로 기대값을 만드는 사례는 `onStableSeoulDay`(`test/e2e/leave-dates.ts`) 안에서 그 헬퍼가 준 날짜만 쓴다 — 사례가 도는 동안 서울 날짜가 바뀌어 실패하면 새 날짜로 한 번만 다시 돌고, 날짜가 그대로인데 실패하면 그대로 실패한다(스펙과 E2E 서버는 같은 호스트 시계 — 운영 코드에 테스트용 시계 입구를 만들지 않는다. 달·해 경계 계산은 04.1-03 순수 함수의 주입 날짜 단위 테스트가 맡는다, CX-R8)"
    - "신청 폼은 종류에 따라 칸이 바뀐다 — 종일·재택은 시작·종료, 반차·반반차는 하루 + `오전`/`오후`. 날짜를 고르면 일수 힌트(`이번 신청 3일 · 주말 2일 제외` / `이번 신청 0.5일` / `재택 · 차감 없음`), `잔고` 행(`연차 남음 · 월차 남음(근속 첫 1년 안이거나 그 해 월차 부여가 있을 때 — 0이어도 `월차 남음 0일`, D4 · 따로) · 결재 중 · 이번 신청`, 2행 `차감 예정 월차 N일 · 연차 N일`, `잔여 초과 N일` 경고 — 막지 않음, 재택이면 없음 · 합계 숫자 없음 — 11:43), 제출 전 결재선 한 줄(`박서연 → 김팀장 팀장 → … · 결재 규칙`, 자기 승인 건너뜀이면 끝 한 마디)이 서버 계산으로 갱신된다(UI-SPEC S2 · 입력 §5 신청 창). 결재선 한 줄은 04.1-01 `previewRoute`의 `RoutePreviewDTO`만 그린다 — viewer 계급에서 결재 정보(`approval.value`)가 숨겨져 있으면 이름 없이 단계 이름만(`팀장 → 경영관리 → 대표`), 계급 정보(`role.value`)까지 숨겨져 있으면 04.1-01이 준 `N단` 이름만(`1단 → 2단 → …`)이고 화면이 이름을 따로 구하지 않는다(CX-R3 · CX2-02)"
    - "신청 폼은 `Ctrl+Enter`로 제출하고 `Esc`·`취소`는 입력이 있으면 `입력 버리기` 확인(`연차 신청 · N칸`)을, 없으면 바로 `/leave`로 간다. 필수 칸이 비면 `시작일 · 종료일 2칸 비어 있음 · 시작일 적기` / `날짜 비어 있음 · 날짜 적기`와 그 칸으로 가는 3차가 보이고, 제출 실패는 1차 옆 `신청하지 못했습니다 · 종료일 1칸` · 입력값 보존이다(UI-SPEC Copywriting · S2 error·interaction)"
    - "같은 날짜 겹침 신청은 막지 않는다(UI-SPEC Assumptions #17)"
    - "관리자는 `/admin/people/[id]` `연차` 섹션에서 입사일·퇴직일을 blur로 즉시 저장하고, 잔고(S1과 같은 문자열 · 퇴직일이 있고 섹션 연도 = 퇴직 연도일 때만 `퇴직 2026-10-31 · 남은 연차 3.5일 · 월차 0일` — 다른 연도는 그 연도 보통 잔고 줄, 04.1-03 DTO의 퇴직 줄 재료 유무로만 가른다, CXF3-C-F3-01)를 보고, `연차 조정 추가`(잔고 연차/월차 · 부호 있는 0.25일 · 필수 사유)로 조정한다. 조정 기록 표는 최신이 위이고 수정·삭제가 없다(UI-SPEC S9 · D-96 · D-97). 조정 기록은 04.1-03 도메인 읽기 `listLeaveAdjustmentsForUser(viewer, {userId, fiscalYear: 섹션 연도})`의 `LeaveAdjustmentDTO[]`만 그린다 — 페이지가 `repositories/`를 부르지 않고, 월차 조정은 유효 창이 겹치는 해마다 · 연차 조정은 그 `fiscalYear`에만 나온다. 그 읽기의 권한 판정은 사람 상세 연차 섹션을 보는 읽기 권한이다 — 보기 권한만 있으면 잔고·기록만(읽기 전용 표), 조정 추가 폼은 쓰기 권한이 있을 때만(CX-R2). 조정 폼 잔고 Select는 `연차`가 기본이고, `월차`는 04.1-03 `checkLeaveAdjustment`가 오늘(`seoulToday()`) 기준으로 거부할 때(입사일 없음 · 월차 소멸) 옵션에서 빠지며(공용 `ui/select`는 옵션 비활성을 받지 않는다 — 고치지 않는다) 그 거부 문구가 Select 아래 한 줄로 보인다. `월차`를 고른 채 쓸 수 없게 되면 선택값이 `연차`로 돌아간다(CX-R5 · CX2-03). (S9-FY) 연차 조정은 섹션이 지금 보여 주는 회계연도를 `fiscalYear`로 넘긴다 — 연도 select는 새로 두지 않는다. 섹션 연도 = 서버가 **한 번** 정한 값 하나 — `?year=`가 D6 규칙으로 유효하면 그 연도, 없거나 미래·형식 오류·범위 밖이면 퇴직일이 있는 사람은 min(퇴직 연도, `seoulToday()`의 연도), 그 밖은 `seoulToday()`의 연도(`resolveLeaveYear`의 대체 연도 인자) — 이고 잔고 읽기 · 조정 기록 읽기 · 연차 조정 `fiscalYear` · 부제가 모두 이 값 하나를 쓴다(CXF2-C-F2-01). `잔고` 옆에 S1 부제 문구 `{Y} 회계연도`로 보인다. 지난 연도 정정은 `잔고` 옆 링크 `{V-1} 회계연도`(V = 보는 연도, V-1 ≥ 2000일 때 — 지난 연도를 보는 중이면 `?year={올해}`를 명시한 `올해 보기` 링크도 — 지난해 퇴직자의 기본 연도는 퇴직 연도라 `?year` 없는 링크는 제자리다, CXF3-C-F3-02)로 그 연도를 연 뒤 조정한다 — URL을 손으로 고치지 않는다(CX-R6). `{Y-1}`을 보는 중 연차 조정을 더하면 `fiscal_year` {Y-1} 행이 생기고 올해 잔고는 그대로다(이월 없음). 월차 조정은 `fiscalYear`를 넘기지 않는다(04.1-03 ENG-13 · DB CHECK)"
    - "사람 등록 폼에 `입사일`이 필수다 — 비면 `입사일 비어 있음 · 입사일 적기`로 등록이 막힌다(UI-SPEC S9 · D-96 전제). 형식 검증과 저장 실패 보상은 도메인 `registerPerson`이 한다(04.1-03) — 도메인은 입사일 없는 기존 계정 상태(`월차 계산 불가 · 입사일 없음`)를 계속 받는다. 등록 폼을 제출하는 기존 E2E 넷(`admin-master-list-first` · `corp-cards` · `mobile-corp-cards` · `single-column`)도 등록 단계의 입사일 한 줄로 이 플랜 안에서 녹색이다 — 07 전체 게이트에서 처음 깨지지 않는다(C-01)"
    - "신청 미리보기 액션도 `leave` write가 있어야 한다 — `previewLeaveAction`은 등록 동작이 `write`이고 첫 문장이 `assertLeaveWrite`(04.1-02)라, 로그인만 한 사용자가 직접 불러도 자기 잔고와 결재자 이름을 받지 못한다(Codex MEDIUM)"
    - "SYSTEM.md A1(계정 그룹 「연차」)은 `role-menu.ts` 수정과 **같은 커밋**이고, A4(연차 목록·신청을 §6-1·§6-3 적용 목록에 올림 · 잔고 줄이 있는 목록은 합계 행 없음)는 목록 코드보다 먼저 DECISIONS.md → SYSTEM.md 순서로 들어간다"
    - "연차 목록(`/leave`) 로딩은 뼈대 행(반짝임·진행 바 없음), 로드 실패는 `연차를 불러오지 못했습니다 · 다시 시도` 한 줄 + 2차 `다시 시도`다. 사람 상세 연차 섹션은 페이지와 같은 서버 렌더라 따로 로딩 구간이 없고(UI-SPEC loading S9 행 — 서버 컴포넌트 초기 렌더에 값이 이미 있다), 그 읽기 실패는 새 경계를 만들지 않고 기존 라우트 오류 경계 `app/(app)/error.tsx`(`화면을 불러오지 못했습니다 · 다시 시도`)가 받는다(CX-R9)"
    - statement: "연차 목록이 폭 1280 · 1024 · 375에서 가로 스크롤 0, 폰 P1 3열(종류·기간 · 일수 · 상태) + 접힌 줄 `신청 09-15`, 행 높이 ≥ 44, 숨긴 caption `내 연차`(UI-SPEC responsive·a11y backstop 행 S1)"
      verification: backstop
    - statement: "신청 폼·문서 화면의 폰 행동 줄이 하단 탭 위에 고정되고 본문 마지막 칸이 가려지지 않는다(UI-SPEC responsive backstop 행 S2·S3)"
      verification: backstop
    - statement: "조정 기록 표가 최신순 · 합계 행 없음 · 0건/1건/여러 건이 같은 템플릿이고, 이름 10자 · 사유 500자에서 줄바꿈만 하며 겹치지 않는다(UI-SPEC populated S9 · zero-one-many S9 · long-text S1·S9·S10 backstop 행)"
      verification: backstop
  prohibitions:
    - statement: "연차를 1차 메뉴 여섯째나 폰 하단 탭에 넣어서는 안 된다 — D-22 「다섯 고정」과 탭 넷을 바꾸지 않는다(UI-SPEC A1 · Assumptions #1)"
      status: resolved
      verification: test
    - statement: "연차 목록에 합계 행을 두어서는 안 된다 — 잔고 줄과 같은 합을 두 번 쓰게 된다(P0 · A4)"
      status: resolved
      verification: test
    - statement: "잔여를 넘는 신청의 1차 버튼을 막아서는 안 된다 — 경고만 보이고 신청은 된다(입력 §5)"
      status: resolved
      verification: test
    - statement: "이 플랜의 화면 코드가 「오늘」·기본 연도를 호스트 시계(UTC)의 연도·ISO 문자열로 정해서는 안 된다 — `seoulToday()`만 쓴다(CEO-11)"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/leave/page.tsx"
      provides: "내 연차 목록 — 잔고 줄 · 연도 select · 상태 그룹"
      contains: "requireSession"
    - path: "app/(app)/leave/new/leave-form.tsx"
      provides: "완성형 신청 폼 — 종류별 칸 · 일수 힌트 · 잔고 행 · 결재선 한 줄 · 입력 버리기"
    - path: "app/(app)/admin/people/[id]/leave-section.tsx"
      provides: "관리자 사람 상세 연차 섹션(S9)"
    - path: "test/e2e/mobile-leave-list.spec.ts"
      provides: "폰 「더보기」 → 연차 → 목록 → 신청 흐름"
      min_lines: 30
    - path: "test/e2e/admin-person-leave.spec.ts"
      provides: "입사일 blur 저장 · 조정 추가 · 조정 연도 = 보고 있는 회계연도(S9-FY) · 퇴직 줄 · 등록 입사일 필수(날짜는 seoulToday 상대 — CEO-15)"
      min_lines: 40
    - path: "test/e2e/leave-list.spec.ts"
      provides: "전용 픽스처 사용자의 목록·EMPTY·그룹 순서 · 연도 규칙(D6) · PC 메뉴 정확한 순서 · 입사 다음 해 사용자의 두 잔고 분리(목록 머리 · 신청 창 — 연차는 `annualGrantQuarters` + `formatLeaveDays`로 계산한 비례값, R1/D5) · 첫 1년 월차 0 줄(D4) · 신청 폼 · 두 번 제출에 한 건(CEO-12 · CEO-15 · 11:43)"
      min_lines: 80
  key_links:
    - from: "ui/shell/role-menu.ts"
      to: "app/(app)/layout.tsx"
      via: "allowedMenus에 leave가 있을 때만 계정 그룹에 연차 — layout은 이미 MENUS 전부를 can()으로 계산한다"
      pattern: "leave"
    - from: "app/(app)/leave/new/leave-form.tsx"
      to: "app/(app)/leave/actions.ts"
      via: "previewLeaveAction — 일수 힌트 · 잔고 행 · 결재선 한 줄을 서버가 계산"
      pattern: "previewLeaveAction"
    - from: "app/(app)/leave/actions.ts"
      to: "domain/leave/access.ts"
      via: "previewLeaveAction 첫 문장 assertLeaveWrite — previewRoute보다 먼저(Codex MEDIUM)"
      pattern: "assertLeaveWrite"
    - from: "app/(app)/leave/actions.ts"
      to: "domain/approvals/index.ts"
      via: "04.1-01 previewRoute → RoutePreviewDTO(결재 정보 투영 뒤 — 숨김이면 단계 이름만). 이 플랜은 정의하지 않고 부르기만 한다(CX-R3)"
      pattern: "previewRoute"
    - from: "app/(app)/admin/people/[id]/page.tsx"
      to: "domain/leave"
      via: "listLeaveAdjustmentsForUser → LeaveAdjustmentDTO[]만 — repositories/ 직접 호출 없음(CX-R2)"
      pattern: "listLeaveAdjustmentsForUser"
    - from: "test/integration/leak-scan.test.ts"
      to: "app/(app)/admin/people/[id]/actions.registry.ts"
      via: "파일 끝 side-effect import"
      pattern: "people/\\[id\\]/actions.registry"
    - from: "app/(app)/leave/page.tsx"
      to: "lib/dates.ts"
      via: "기본 연도 · 「올해」 = seoulToday()의 연도(CEO-11)"
      pattern: "seoulToday"
    - from: "app/(app)/leave/new/leave-form.tsx"
      to: "ui/button/Button.tsx"
      via: "1차 pending — 제출 중 두 번째 누름·Ctrl+Enter 무시(CEO-12)"
      pattern: "pending"
---

<objective>
**As a** 연차를 내는 직원과 사람을 관리하는 경영관리 담당자, **I want to** 계정 메뉴에서 내 연차로 들어가 올해 잔고와 신청 상태를 한눈에 보고, 신청 창에서 이번 신청이 잔고를 넘는지와 누가 결재할지를 미리 보며, 관리자는 입사일·퇴직일·조정으로 잔고를 바로잡기를, **so that** 인트라넷보다 적게 묻고 적게 틀리는 연차 화면이 되고, 입사 첫해 월차가 입사일 하나로 자동으로 맞는다.

Task 1은 이 플랜의 **트레이서**다 — 셸 진입점(계정 그룹 「연차」) → `/leave` 목록(잔고 줄 · 상태 그룹) → `연차 신청`까지 디자인 개정 A1·A4 · 셸 · 목록 화면 · 잔고 서비스를 관통시킨다. Task 2는 신청 폼 완성형, Task 3은 관리자 사람 상세 연차 섹션과 등록 입사일이다. 병합 직전 마이그레이션 재생성과 페이즈 전체 게이트는 04.1-07이다.

Purpose: ROADMAP 기준 4의 화면 쪽(신청 창에 남은/결재 중/이번 신청 일수 경고 · 입사 첫해 월차 · 퇴직 잔여)과 UI-SPEC S1 · S2 · S9 · S10이 이 플랜이다.

Output: 셸 진입점 · 연차 목록 · 완성형 신청 폼 · 관리자 연차 섹션 · 등록 입사일, SYSTEM.md A1 · A4, E2E 3 + 등록 폼을 제출하는 기존 E2E 여섯 갱신(C-01) · 가장 이른 신청 연도 읽기와 그 통합 테스트(C-04).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md
@.planning/phases/04.1-approvals-leave/04.1-03-SUMMARY.md
@.planning/phases/04.1-approvals-leave/04.1-05-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다. 실패가 나면 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 독립 DOM 감사 서브에이전트 프롬프트에도 세 스킬 이름과 `CI=true`를 적는다.
- UI 작업 전에 `docs/DESIGN.md` §4를 읽는다. SYSTEM.md 개정은 DECISIONS.md 먼저 → SYSTEM.md → 코드. **A1은 `role-menu.ts`와 같은 커밋**이어야 한다(`test/unit/ui/role-menu.test.ts`가 SYSTEM.md 문장을 읽어 대조한다).
- 화면 검증 순서: 싼 게이트 → 독립 DOM 감사(별도 에이전트 · `CI=true` · 폭 1280 · 1024 · 375 실측 · 스크린샷 육안 금지) → 수정 → 전체 게이트 한 번.
- 병합 위험 파일(Phase 4 04-27이 고침): `app/(app)/admin/people/actions.ts` — 등록 zod에 `hireDate` 한 칸과 그 전달만 더하고 다른 줄은 건드리지 않는다. 새 관리자 액션은 `app/(app)/admin/people/[id]/actions.ts`(새 파일)에 둔다. `test/integration/leak-scan.test.ts`는 끝에 덧붙이기만.
- **날짜·픽스처(CEO-11 · CEO-15 · Codex HIGH 06)**: 화면의 「오늘」·기본 연도는 `seoulToday()`에서만 나온다. 새 E2E는 연도를 `seoulToday()`에서 계산하고 신청 날짜는 `test/e2e/leave-dates.ts`의 `leaveWeekdayRange(seoulToday(), {week, weekdays})`로만 만든다(이 플랜의 스펙은 `week` 10~20을 쓴다 — 04.1-02 · 04.1-05 스펙과 기간이 겹치지 않게)(스펙이 `@/lib/dates`를 import — `fixtures.ts`가 도메인을 import하는 것과 같은 방식), 잔고를 단언하는 사례는 스펙 안에서 `createFixtureUser`로 만든 전용 사용자를 쓴다 — 시드 사용자(기획 PM 등)는 다른 스펙이 연차를 내므로 기대값이 흔들린다.
- 건드리지 않는 파일: 첫 화면 「내 차례」 블록 · `ui/next-turn/*`(오케스트레이터 결정 9) · `ui/shell`의 `role-menu.ts`·`TopBar.tsx` 밖 파일.
- 첫 task를 시작하기 전에 `git rev-parse HEAD` 값을 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도 남긴다). 이 플랜의 diff 기준 검사는 전부 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다 — 커밋한 뒤의 맨 `git diff`는 늘 비어 있어 검사가 공허해진다. (C-05) Bash 호출 사이에 셸 변수가 남지 않으므로 값은 `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-04.1-06.env"`로 파일에 쓰고(`.git` 안이라 `git status`에 안 잡힌다), `$PLAN_BASE`를 쓰는 검사는 전부 `. "$(git rev-parse --git-dir)/gsd-04.1-06.env" && test -n "$PLAN_BASE" &&`로 시작한다 — 값이 비면 `git diff ..HEAD`가 `HEAD..HEAD`(빈 diff)가 되어 오류 없이 공허하게 통과한다(실측: 빈 `PLAN_BASE`로 `git diff --stat "$PLAN_BASE"..HEAD`가 rc 0 · 빈 출력).
</execution_notes>

<probe_fallback>
이 플랜 표면(S1 · S2 · S9 · S10)의 UI Considerations covered 행은 truths로, backstop 행(responsive S1·S2 · long-text S1·S9·S10 · populated/zero-one-many S9)은 backstop 항목 셋으로 옮겼다 — 버린 행은 없다. 미해결 엣지는 04.1-01~05가 전부 닫았고 이 플랜에 새로 남은 것은 없다.

**계획 가정**
1. (D6 · ENG-8) `/leave?year=`가 정수이고 2000 ≤ year ≤ 올해이면 그 연도를 그대로 보여 준다(신청이 없으면 지난 연도 EMPTY `{연도} 회계연도에 신청한 연차가 없습니다 · 올해 보기`). 미래 연도 · 형식 오류 · 범위 밖(2000 미만)만 올해로 바꾼다. 연도 select 옵션 = min(그 사람의 가장 이른 신청 연도(없으면 올해), 요청 연도) ~ 올해 — select 범위는 표시 옵션일 뿐 조회 허용 범위가 아니다. 가장 이른 신청 연도는 Task 1 ③의 `earliestMyLeaveYear`(쿼리 한 번)가 준다(C-04). 미래 연도 옵션은 없다. 「올해」 = `seoulToday()`의 연도(CEO-11)
2. 신청 폼 미리보기는 칸이 바뀔 때 서버 액션 한 번(`previewLeaveAction`)으로 일수 힌트 · 잔고 행 · 결재선 한 줄을 함께 받는다 — 다시 받는 동안 이전 값을 유지하고 그 칸만 바뀐다(UI-SPEC loading S2 행). 디바운스는 날짜·종류 선택(change) 단위라 두지 않는다. 응답 순서는 Next 16의 서버 액션 순차 전송이 보장한다 — 옛 응답이 최신 값을 덮지 않는다(CX-R4)
3. 기존 사람의 입사일은 상세에서 채운다 — 이 페이즈는 기존 사람의 입사일을 일괄로 넣지 않는다(데이터 이전 없음, 2026-09-23 결정)
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 계정 그룹 「연차」 → `/leave` 잔고 줄 · 상태 그룹 → `연차 신청` (A1 · A4 · 셸 · 목록)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, ui/shell/role-menu.ts, ui/shell/TopBar.tsx, test/unit/ui/role-menu.test.ts, app/(app)/leave/page.tsx, app/(app)/leave/year-param.ts, app/(app)/leave/leave-table.tsx, app/(app)/leave/loading.tsx, app/(app)/leave/error.tsx, domain/leave/index.ts, repositories/leave-requests.ts, test/integration/leave-earliest-year.test.ts, test/e2e/leave-list.spec.ts, test/e2e/mobile-leave-list.spec.ts, test/e2e/mobile-shell.spec.ts, test/e2e/leave-dates.ts, test/unit/e2e/leave-dates.test.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 230–255행(S1) · 390–397행(S10) · 402–404·414–415행(A1 · A4) · 188·196–197·206·212행(목록 CTA · EMPTY · 잔고 줄 · 목록 행 문구)
    - `docs/design/SYSTEM.md` §6-0(270–314행 — 계정 그룹 · 더보기 시트 문단) · §6-1(315–356행) · §6-3 머리 · §7-3(708–782행 — 폰 P1·P2·P3)
    - `ui/shell/role-menu.ts`(전문) · `ui/shell/TopBar.tsx` 25–60행(PC 사용자 메뉴가 accountGroup에서 무엇을 거르는지) · `test/unit/ui/role-menu.test.ts`(SYSTEM.md 「계정」 그룹 문장을 읽는 정규식)
    - (ENG-7) `test/e2e/mobile-shell.spec.ts` 139–155행 — 기대 계정 그룹을 `roleMenu({roleId: DEFAULT_ROLE_ID, allowedMenus: []})`로 만든다(04.1-02가 기본 계급에 `leave` view를 시드하므로 실제 시트엔 `연차`가 있어 반드시 깨진다). `test/e2e/user-menu.spec.ts`는 항목을 `allTextContents()`로 읽어 개수 ≥ 3과 키보드 이동만 보므로 같은 패턴이 아니다 — 고치지 않고 회귀 실행만 한다
    - `app/(app)/layout.tsx` — allowedMenus 계산(고치지 않는다)
    - `app/(app)/projects/page.tsx` · Phase 4의 목록 표 선례 — 그룹 머리글 · 필터 줄 · EMPTY 두 갈래
    - `domain/leave/index.ts`(`listMyLeave` — 권한 판정 모양을 `earliestMyLeaveYear`가 그대로 따른다) · `repositories/leave-requests.ts`(04.1-01 — `leave_requests.fiscal_year` · `drafter_id` 열, 조회 함수 모양) · `domain/leave/balance-service.ts`(`getMyLeaveBalance`) · `domain/leave/balance.ts`(`formatBalanceLines`) · `app/(app)/leave/status-display.ts`
    - `lib/dates.ts`(`seoulToday` — 04.1-01) · `test/e2e/fixtures.ts`(`createFixtureUser`) · `test/e2e/leave-approval.spec.ts`(04.1-02 — 전용 본부·팀 픽스처 모양) · `test/e2e/leave-dates.ts`(04.1-02 — `leaveWeekdayRange`) · `test/unit/e2e/leave-dates.test.ts`(04.1-02 — 기준일 주입 단위 테스트 모양, CX-R8)
    - `domain/leave/balance.ts`(04.1-03 — `annualGrantQuarters({fiscalYear, hireDate, resignationDate, annualDays})`(정수 1/4일 비례 부여) · `buildLeaveGrants` · `summarizeLeaveBalance` · `formatBalanceLines` · `formatBalanceRow`: 스펙이 기대 문자열을 계산한다) · `domain/leave/days.ts`(04.1-01 — `formatLeaveDays(quarters)`) · `domain/settings/registry.ts`(`getSettingValue`) + 04.1-03 `LEAVE_ANNUAL_DAYS`(스펙이 `{Y}-01-01` 시점 연차 일수를 읽는다) · `domain/people/index.ts`(`setHireDate` — 스펙이 전용 사용자에게 입사일을 넣는다)
    - `app/(app)/approvals/loading.module.css`(04.1-05 — 300ms 지연 `.delayed`)
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` CEO-11 · CEO-15(456–460행)
  </read_first>
  <behavior>
    - (단위) `roleMenu({roleId, allowedMenus: [..., "leave"]})`의 계정 그룹이 `내 정보 · 연차 · 설정 · 로그아웃`이고, `leave`가 없으면 `내 정보 · 설정 · 로그아웃`이다. 1차 메뉴와 하단 탭은 두 경우 같다. 계정 그룹 이름이 SYSTEM.md 문장과 일치한다
    - (E2E desktop · CEO-15) 이 스펙 전용 픽스처 사용자(입사일 없음 · 신청 기록 없음)가 PC 사용자 메뉴에서 `연차`를 누르면 `/leave`이고 머리 부제 `{Y} 회계연도`(Y = 스펙이 `seoulToday()`로 계산한 연도), 잔고 줄 1행 `연차 15일 · 사용 0일 · 결재 중 0일 · 남음 15일`, 2행 `월차 계산 불가 · 입사일 없음`, 0건 EMPTY `{Y} 회계연도에 신청한 연차가 없습니다 · 연차 신청`, 머리 1차 `연차 신청`이 없다
    - (E2E desktop) 같은 전용 사용자(전용 팀 발령)의 신청 두 건(하나 반려, 하나 결재 중 — 날짜는 `leaveWeekdayRange(seoulToday(), {week: 10, weekdays: 2})` · `{week: 11, weekdays: 2}`, Codex HIGH 06)이 있으면 그룹 머리글이 `반려` → `결재 중` 순서이고 합계 행이 없다
    - (E2E desktop · D6 · ENG-8) 같은 전용 사용자(올해 신청만 있음)가 `?year={Y-1}`로 들어가면 올해로 바뀌지 않고 부제 `{Y-1} 회계연도` · EMPTY `{Y-1} 회계연도에 신청한 연차가 없습니다 · 올해 보기`(3차 → `/leave`)이고 연도 select 옵션이 정확히 `{Y-1}` · `{Y}`(선택값 `{Y-1}`)다. `?year={Y+1}`(미래) · `?year=abc`(형식 오류) · `?year=1999`(범위 밖)는 셋 다 부제 `{Y} 회계연도`와 올해 목록(그룹 `반려` · `결재 중`)이다
    - (E2E desktop · 11:43 · R1/D5) 입사일 `{Y-1}-10-01`(Y = `seoulToday()`의 연도 — 스펙이 계산, 리터럴 없음)인 전용 사용자의 `/leave` 머리: 1행이 정확히 `연차 {P} · 사용 0일 · 결재 중 0일 · 남음 {P}`이고 — P는 스펙이 `formatLeaveDays(annualGrantQuarters({fiscalYear: Y, hireDate: "{Y-1}-10-01", resignationDate: null, annualDays}))`로 계산한 문자열(annualDays = `getSettingValue(LEAVE_ANNUAL_DAYS)`의 `{Y}-01-01` 시점 값 · 입사 연도 근무 기간 비례 부여 — 스펙에 숫자로 적지 않는다, R1/D5 · 메모 9) — 2행이 스펙이 04.1-03 순수 함수(`buildLeaveGrants` → `summarizeLeaveBalance` → `formatBalanceLines`)로 같은 입력(입사일 · `seoulToday()`)에서 만든 월차 줄(`월차 적립 N일 · … · 남음 N일 · {Y}-12-31 소멸`)과 같으며(같은 호출의 1행도 위 1행과 같다 — 두 계산이 어긋나면 RED), 페이지 글자 어디에도 두 남음을 더한 `남음 {P + N}일`(`formatLeaveDays`로 쿼터 합을 표시)이 없다 — 1년 중 어느 날에 돌아도 두 잔고가 다 있다(연차는 {Y}-01-01 비례 부여, 월차 적립 ≥ 3, 소멸 {Y}-12-31). 비례 규칙 자체의 사례(1월 1일 · 10월 1일 평년 · 윤년 · 12월 31일 · 입사 연도 안 퇴직)는 04.1-03 단위 테스트가 맡는다
    - (E2E desktop · D4) 입사일 = `seoulToday()`인 전용 사용자(근속 첫 1년 · 월차 적립 0)의 `/leave` 머리 2행이 비어 있지 않고, 스펙이 같은 순수 함수로 만든 월차 줄(`월차 적립 0일 · 사용 0일 · 결재 중 0일 · 남음 0일 · {소멸일} 소멸`)과 같다 — 0이어도 월차 줄이 사라지지 않는다
    - (E2E desktop · Codex MEDIUM) 관리자 계정의 PC 사용자 메뉴 항목이 정확히 `관리 · 내 정보 · 연차 · 로그아웃` 순서이고, `관리` 권한이 없는 전용 사용자는 정확히 `내 정보 · 연차 · 로그아웃`이다(배열 전체 비교)
    - (E2E mobile-375) 「더보기」 시트의 계정 그룹에 `연차`가 있고, 누르면 `/leave`, 목록 폰 P1 세 열과 접힌 줄 `신청 MM-DD`, 가로 스크롤 0
    - (E2E mobile-375 · ENG-7) 기존 `mobile-shell.spec.ts` 「시트 안 계정 그룹 항목이 role-menu.ts의 accountGroup과 같다」가 기대값을 실제 권한으로 만든다 — layout과 같은 계산(`MENUS` 각각에 `can({id: <임의 문자열>, roleId: DEFAULT_ROLE_ID}, key, "view")`)으로 allowedMenus를 구해 `roleMenu`에 넘기고, 그 allowedMenus에 `leave` 키가 있음을 먼저 단언한다(시드가 바뀌어 `leave`가 빠지면 공허한 통과 대신 실패 — 항목 이름은 기존 주석대로 스펙에 박지 않는다). `role-menu.ts`가 `연차`를 넣은 뒤 옛 기대값(`allowedMenus: []`)으로 두면 실제 시트와 달라 RED다
    - (E2E) `leave` view 권한을 뺀 계급(권한표에서 끔)의 사람은 계정 그룹에 `연차`가 없고 `/leave`가 404
    - (E2E desktop · CX-R5) `leave` view만 켜고 write를 끈 스펙 전용 계급(권한표 도메인 함수로 만든다 — 정보 노출 행은 만들지 않는다. 이 사례는 잔고 값을 단언하지 않으므로 `leave.value` 노출에 기대지 않는다, CXF-C-F06)의 사람(신청 0건)이 `/leave`를 열면 EMPTY가 `{Y} 회계연도에 신청한 연차가 없습니다`뿐이고 페이지 어디에도 `연차 신청` 링크·버튼이 없다 — 양성 대조는 둘째 줄(write 있는 전용 사용자의 EMPTY `… · 연차 신청`). 옛 구현(write 판정 없이 0건 여부만)이면 RED
    - (통합 · C-04 — `test/integration/leave-earliest-year.test.ts`) `earliestMyLeaveYear(viewer)`: 신청이 없는 사용자 → `null` · 본인 신청이 두 회계연도(스펙이 04.1-01 `submitLeave`에 `deps.now`를 주입해 만든다)에 있으면 이른 쪽 · 다른 사용자의 더 이른 신청은 세지 않는다 · 리포지토리 조회 주입 카운터로 호출 1회(해마다 조회하지 않는다, C-P1)
    - (단위 · CX-R8 — `test/unit/e2e/leave-dates.test.ts`) `onStableSeoulDay(body, {today})`에 날짜 함수를 주입한다(벽시계 없음): 날짜가 d0 그대로이고 body가 통과하면 body를 d0로 한 번만 · body가 실패하고 날짜가 그대로면 그 오류를 다시 던지고 한 번만 · body가 실패하는 동안 날짜가 d1로 바뀌었으면 body를 d1로 한 번 더 불러 그 결과를 돌려준다 · 둘째 실행도 실패하면 던진다(세 번 돌지 않는다) · body가 통과했으면 날짜가 바뀌었어도 다시 돌지 않는다
  </behavior>
  <action>
① **A4 먼저**: DECISIONS.md에 A4(UI-SPEC 414–415행)를 기록하고 SYSTEM.md §6-1 제목 괄호에 `연차 목록`, 규칙 「머리에 잔고 한 줄이 있는 목록은 합계 행을 두지 않는다 — 같은 합을 두 번 쓰지 않는다(P0)」, §6-3 머리에 「연차 신청도 이 템플릿(종류에 따라 칸이 바뀐다)」 한 줄을 더한다.

② **A1 + 셸(같은 커밋)**: DECISIONS.md에 A1(402–404행)을 기록하고 SYSTEM.md §6-0 (a) · 「더보기」 시트 문단 · §7-8 「더보기」 시트 줄의 계정 그룹을 `내 정보 · 연차 · 설정 · 로그아웃`으로 고친다. 같은 커밋에서 `ui/shell/role-menu.ts`의 `buildAccountGroup`이 viewer를 받아 `allowedMenus`에 `leave`가 있을 때만 `내 정보` 다음에 `{kind: "link", label: "연차", href: "/leave"}`를 넣는다(계급 이름 분기 없음). PC 사용자 메뉴는 `TopBar.tsx`가 `adminMenu` 뒤에 accountGroup(설정 제외)을 붙이는 기존 합성 그대로 `관리 · 내 정보 · 연차 · 로그아웃`이 된다 — 이 순서가 SYSTEM.md §6-0 (a)(「관리」는 내 정보 · 로그아웃 위)와 UI-SPEC S10(개정됨)과 같다. 거르는 규칙이 `연차`를 빠뜨리면 그 거르는 줄만 고치고 합성 순서는 바꾸지 않는다. E2E가 순서를 배열로 단언한다(Codex MEDIUM). `role-menu.test.ts`는 두 경우(leave 있음/없음)를 단언하도록 고친다 — SYSTEM.md 문장에서 이름을 읽는 기존 방식은 유지.

③ **목록** `app/(app)/leave/page.tsx`: `requireSession` → `leave` view 판정이 거짓이면 `notFound()` → `?year` 검색 파라미터(D6 · ENG-8 — 없으면 올해. 정수이고 2000 ≤ year ≤ 올해이면 그 연도 그대로, 신청이 없어도 올해로 바꾸지 않는다. 미래 · 형식 오류 · 2000 미만만 올해로. 연도 select 옵션 = min(가장 이른 신청 연도, 요청 연도) ~ 올해 — 옵션 범위로 조회를 거르지 않는다, 계획 가정 1. 「올해」는 `seoulToday()`의 앞 네 글자, 호스트 시계의 연도 함수나 ISO 문자열 자르기로 구하지 않는다, CEO-11. (C-04 · C-P1) 「가장 이른 신청 연도」는 새 도메인 읽기 `earliestMyLeaveYear(viewer, deps?)`(`domain/leave/index.ts` — 권한 판정은 `listMyLeave`와 같고 viewer 본인 신청만 센다, 다른 사람 id를 받지 않는다 · 반환 `number | null` · `deps.findEarliestLeaveFiscalYear` 주입 가능 — 통합 테스트가 호출 수를 센다)가 `repositories/leave-requests.ts`의 새 함수 `findEarliestLeaveFiscalYear(drafterId)`(`leave_requests`의 `drafter_id` = 본인 행의 `min(fiscal_year)` 쿼리 **한 번**, 행이 없으면 null)로 준다 — null이면 올해. `listMyLeave`를 해마다 부르지 않고 `listMyLeave` 결과 DTO(04.1-01)도 바꾸지 않는다) → `getMyLeaveBalance` + `listMyLeave` + `earliestMyLeaveYear` 병렬 → PageHeader(`연차` · 부제 `{연도} 회계연도`) → 잔고 줄(`formatBalanceLines` 그대로, 숫자 700, 톤은 함수가 준 값) → 필터 줄(연도 select 200 · 오른쪽 1차 `연차 신청` — 올해 0건이면 렌더하지 않음, `leave` write가 없어도 렌더하지 않음 — EMPTY 3차 `연차 신청`도 같은 판정, write 판정은 서버가 view 판정과 같은 방식으로 하고 목록·EMPTY에 넘긴다, CX-R5) → `leave-table.tsx`(`ui/table` 그룹 머리글 = 상태, 순서 고정, 그룹 안 시작일 오름차순, 열 = 번호(P3) · 종류·기간(P1) · 일수(P1) · 상태(P1, status-display 글자) · 신청일(P2 — 폰 접힌 줄 `신청 MM-DD`) · 비고(P3 한 줄 말줄임), 행 전체가 `/leave/[id]` 링크, 숨긴 caption `내 연차`, 합계 행 없음). EMPTY 두 갈래. `loading.tsx` · `error.tsx`는 04.1-05의 두 파일과 같은 모양(문구 `연차를 불러오지 못했습니다`) — `error.tsx`의 2차 `다시 시도`는 Next 16.3 오류 경계 인자 `retry`를 부른다(`reset` 아님 — `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md` · `app/(app)/projects/error.tsx` 선례 · 04.1-05 B-C3와 같다) — 로딩은 04.1-05의 `app/(app)/approvals/loading.module.css` `.delayed`를 import해 300ms 지연을 같이 쓴다(Codex LOW). `?year` 해석(D6)은 순수 함수 `resolveLeaveYear(raw, thisYear)` → 보여 줄 연도(정수)로 `app/(app)/leave/year-param.ts`에 두고 page.tsx가 부른다 — Task 3 사람 상세 연차 섹션이 같은 함수를 쓴다(S9-FY — 규칙 한 곳). select 옵션 계산은 page.tsx에 남긴다.

④ E2E 두 파일이 behavior의 새 E2E 줄을 전부 단언한다(연도 규칙 D6 · 입사 다음 해 비례 연차 R1/D5 · 첫 1년 월차 0 줄 D4 포함). 입사 다음 해 연차 기대값은 스펙이 04.1-03 `annualGrantQuarters({fiscalYear, hireDate, resignationDate, annualDays})`(정수 쿼터) + 04.1-01 `formatLeaveDays`로 계산한다 — `15`나 비례 결과를 숫자로 적지 않는다(R1/D5 · 메모 9). 입사일 없는 전용 사용자의 `연차 15일`은 비례를 계산할 수 없어 그대로다. (ENG-7) `test/e2e/mobile-shell.spec.ts` 150행의 기대값 계산을 behavior대로 실제 권한 계산으로 바꾸고 그에 필요한 import(`can` · `MENUS`)와 `leave` 키 단언만 더한다 — 그 테스트의 다른 줄과 다른 테스트는 건드리지 않는다. `test/e2e/user-menu.spec.ts`는 항목을 화면에서 읽으므로 고치지 않는다(회귀 실행만). 권한을 끄는 사례는 권한표 도메인 함수로 그 스펙 전용 계급을 만들어서 한다(시드 계급의 권한을 바꾸지 않는다 — 다른 스펙을 깨지 않게). 잔고·EMPTY 사례는 스펙 안에서 만든 전용 사용자로, 연도·날짜는 `seoulToday()`에서 계산해 기대 문자열을 만든다(CEO-15 — 네 자리 연도를 스펙에 적지 않는다). (CX-R8) `test/e2e/leave-dates.ts`에 `onStableSeoulDay(body, deps?: { today?: () => string })`(기본 `today` = `seoulToday`)를 더한다 — `d0 = today()`로 `body(d0)`를 돌리고, 실패했을 때 `today()`가 d0와 다르면 `body(새 날짜)`를 한 번만 다시 돌며, 같으면 그 오류를 그대로 던진다. 날짜로 기대값을 만드는 E2E 사례(연도 · 입사일 · 월차 줄 · 소멸일)는 본문을 이 헬퍼 안에 두고 헬퍼가 준 날짜만 쓴다 — 본문이 전용 사용자 생성부터 단언까지를 담아 다시 돌 때 앞 실행의 기록과 섞이지 않는다. 기존 `leaveWeekdayRange`와 그 테스트는 바꾸지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts test/unit/design-system-docs.test.ts test/unit/page-auth-guard.test.ts test/unit/e2e/leave-dates.test.ts</automated>
    <fails_when>계정 그룹이 SYSTEM.md 문장과 어긋나거나(A1과 role-menu.ts가 같은 커밋이 아니면 중간 커밋에서 깨진다), 새 page.tsx에 세션 검사가 없거나, `onStableSeoulDay`가 주입한 날짜 바뀜에 다시 돌지 않거나 같은 날 실패를 삼키면(CX-R8) failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/leave-list.spec.ts test/e2e/mobile-leave-list.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/user-menu.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/admin-nav.spec.ts</automated>
    <fails_when>playwright failed가 1 이상이다 — 새 스펙이 실패하거나, `mobile-shell.spec.ts`의 계정 그룹 기대값이 실제 권한(`leave` 포함)으로 만들어지지 않았거나(ENG-7), 계정 그룹에 `연차`가 늘어 PC 메뉴 키보드 이동(`keyboard-nav`)·관리 메뉴(`admin-nav`)가 회귀하거나(C-GAP-3), `?year={Y-1}`가 올해로 바뀌거나(D6), 입사 다음 해 연차가 비례값이 아니거나(R1/D5), 첫 1년 월차 0 줄이 사라지거나(D4), view만 있는 계급에 `연차 신청`이 보이면(CX-R5) 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leave-earliest-year.test.ts</automated>
    <fails_when>failed가 1 이상이다 — `earliestMyLeaveYear`가 신청 없는 사용자에게 null이 아니거나, 두 회계연도 중 이른 쪽이 아니거나, 다른 사용자의 신청을 세거나, 리포지토리 조회가 1회가 아니면(해마다 조회 — C-P1) 깨진다(C-04)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --format=%H -n1 -- ui/shell/role-menu.ts`와 SYSTEM.md A1 문장이 들어간 커밋이 같다
    - DECISIONS.md의 A4 기록 커밋이 `app/(app)/leave/page.tsx` 첫 커밋보다 앞선다
    - `role-menu.ts`에 계급 id 문자열 분기가 새로 생기지 않았다(추가된 줄에 계급 id 문자열 리터럴이 없다: `. "$(git rev-parse --git-dir)/gsd-04.1-06.env" && test -n "$PLAN_BASE" && D=$(git diff "$PLAN_BASE"..HEAD -- ui/shell/role-menu.ts) && printf '%s\n' "$D" | grep '^+' | grep -cE '"role-[a-z-]+"'`이 0 — 값 파일이 없거나 `PLAN_BASE`가 비면 `test -n`에서, git이 실패하면 `&&`에서 멈춰 공허한 통과가 없다(빈 `PLAN_BASE`는 git 오류 없이 `HEAD..HEAD` 빈 diff가 된다 — C-05))
    - 목록에 합계 행 컴포넌트·prop이 없다
    - 1차 메뉴 · 하단 탭 배열이 이 플랜에서 바뀌지 않았다(role-menu.test.ts가 두 경우 같음을 단언)
    - (CEO-11) `grep -c "seoulToday" "app/(app)/leave/page.tsx"`이 1 이상이고 `grep -cE "getFullYear\(|toISOString\(" "app/(app)/leave/page.tsx"`이 0이다
    - (CEO-15) `cat test/e2e/leave-list.spec.ts test/e2e/mobile-leave-list.spec.ts | grep -cE "\b20[0-9]{2}\b"`이 0이다(연도는 계산값)
    - (Codex HIGH 06) 두 스펙이 `leaveWeekdayRange`로만 날짜를 만든다(`grep -c "leaveWeekdayRange" test/e2e/leave-list.spec.ts`이 1 이상)
    - (11:43) 입사 다음 해 사용자 사례가 월차 줄과 연차 줄을 따로 단언하고 합계 `남음` 부재를 단언한다
    - (R1/D5 · 메모 9) 입사 다음 해 연차 기대값이 비례 부여 함수 계산값이다(`grep -c "annualGrantQuarters" test/e2e/leave-list.spec.ts`와 `grep -c "formatLeaveDays" test/e2e/leave-list.spec.ts`이 둘 다 1 이상) — 입사일 있는 사용자 사례에 `연차 15일` · `남음 15일` 리터럴 기대값이 없다(15는 입사일 없는 사용자 사례에만)
    - (D4) 입사일 = 오늘인 사용자 사례가 월차 줄 존재와 `월차 적립 0일`로 시작함을 단언한다
    - (D6 · ENG-8) `?year={Y-1}` EMPTY · 옵션 `{Y-1}`~`{Y}` 사례와 `?year={Y+1}` · `abc` · `1999`가 올해로 바뀌는 사례가 있고 녹색이다
    - (ENG-7) `grep -c "allowedMenus: \[\]" test/e2e/mobile-shell.spec.ts`이 0이고 `grep -c "MENUS" test/e2e/mobile-shell.spec.ts`이 1 이상이다 — `test/e2e/user-menu.spec.ts`는 이 플랜 diff에 없다(`git diff --stat "$PLAN_BASE"..HEAD -- test/e2e/user-menu.spec.ts`가 비어 있음)
    - (Codex MEDIUM) PC 메뉴 순서 단언이 배열 전체 비교다
    - (CX-R5) view만 있는 계급의 `/leave`에 `연차 신청`이 없다는 E2E 사례가 있고 녹색이다
    - (CX-R9) `grep -c "연차를 불러오지 못했습니다" "app/(app)/leave/error.tsx"`이 1 이상이고 `app/(app)/leave/loading.tsx`가 있다 · (B-C3) `grep -c "retry" "app/(app)/leave/error.tsx"`이 1 이상이다 — 뼈대·오류 한 줄 truth는 `/leave`에만 걸린다(사람 상세 섹션은 Task 3 수락 기준)
    - (CX-R8) `grep -c "onStableSeoulDay" test/e2e/leave-list.spec.ts test/e2e/mobile-leave-list.spec.ts`이 파일마다 1 이상이고(`admin-person-leave.spec.ts`는 Task 3이 만들므로 Task 3 수락 기준이 본다 — CXF-C-F05), `pnpm vitest run --project unit test/unit/e2e/leave-dates.test.ts`가 녹색이다(첫 검증 명령에 포함) — 헬퍼 다섯 사례 중 「날짜 바뀜 → 다시 돎」은 헬퍼를 넣기 전 RED를 확인했다
    - (C-04 · C-P1) `grep -c "earliestMyLeaveYear" "app/(app)/leave/page.tsx"`이 1 이상이고 `grep -c "listMyLeave(" "app/(app)/leave/page.tsx"`이 정확히 1(해마다 부르지 않는다)이며 `grep -c "min(" repositories/leave-requests.ts`이 1 이상이다 · 넷째 검증 명령(`leave-earliest-year.test.ts`)이 녹색이다 · D6 E2E의 select 옵션 정확 비교(`{Y-1}` · `{Y}`)가 녹색이다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>직원이 계정 그룹 「연차」로 들어가 올해 잔고(연차·월차 따로)와 상태별 신청을 보고 신청으로 넘어가는 경로가 PC·폰 E2E로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 신청 폼 완성형 — 종류별 칸 · 일수 힌트 · 잔고 행 · 결재선 한 줄 · 입력 버리기 · `Ctrl+Enter`</name>
  <files>app/(app)/leave/new/page.tsx, app/(app)/leave/new/leave-form.tsx, app/(app)/leave/actions.ts, app/(app)/leave/actions.registry.ts, test/e2e/leave-list.spec.ts, test/e2e/mobile-leave-list.spec.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 256–285행(S2 전문) · 189·201–203·207–209·218행(폼 CTA · 오류 · 막힘 · 잔고 행 · 일수 힌트 · 결재선 한 줄 · 입력 버리기 원문) · 433·436·439·449·451·456·460·463행(S2 상태 행)
    - `docs/design/SYSTEM.md` §6-3(380–413행) · §7-15(972–1006행) · §7-9
    - `app/(app)/leave/new/leave-form.tsx`(04.1-02 · 04.1-05 산출물 — 다시 신청 모드 유지) · `ui/confirm-dialog/ConfirmDialog.tsx` · `ui/approval-route/ApprovalRoute.tsx`(line 모드)
    - Phase 4 입력 버리기(DR-27) 선례: `git show origin/claude/gsd-progress-e1nzgu --stat | grep -i "discard\|dirty"` 또는 `ui/form`의 기존 dirty 처리 — 있으면 같은 모양
    - `domain/approvals/index.ts`(04.1-01 `previewRoute(viewer, {kind}, deps?)` → `RoutePreviewDTO` — `drafterName` · 단계 `holderNames`는 `approval.value`, 단계 `label` · `skipped`는 `role.value`에 묶인 등록 DTO. **고치지 않는다**, CX-R3) · `node_modules/next/dist/docs/01-app/02-guides/server-actions.md` 26–30행(「Sequential dispatch on the client」, CX-R4) · `domain/leave/balance-service.ts`(`previewLeaveBalance`) · `domain/leave/days.ts`
    - `ui/button/Button.tsx` — 기존 `pending` prop(`…` 표시 · 비활성). **고치지 않는다**
    - `domain/leave/access.ts`(`assertLeaveWrite` — 04.1-02) · `test/e2e/leave-dates.ts`(`leaveWeekdayRange`)
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` CEO-12(393–398행)
  </read_first>
  <behavior>
    - (E2E desktop · CEO-15 · Codex HIGH 06) 전용 픽스처 사용자(입사일 없음)로 종일 월~다음 주 월(`leaveWeekdayRange(seoulToday(), {week: 12, weekdays: 6})`) 선택 → 힌트 `이번 신청 6일 · 주말 2일 제외`, 잔고 행 1행 `연차 남음 15일 · 결재 중 0일 · 이번 신청 6일`(월차 칸 없음), 결재선 한 줄이 기안자 이름으로 시작하고 `· 결재 규칙`으로 끝난다
    - (E2E desktop · 11:43 · R1/D5) 입사일 `{Y-1}-10-01` 전용 사용자가 같은 모양의 6일(`{week: 13, weekdays: 6}`)을 고르면 잔고 행 1행이 `연차 남음 {P} · 월차 남음 N일 · 결재 중 0일 · 이번 신청 6일`, 2행이 `차감 예정 월차 a일 · 연차 b일`(초과면 3행 `잔여 초과 c일`)이고 — P(입사 연도 근무 기간 비례 부여, `일`까지 든 문자열)는 Task 1과 같은 `formatLeaveDays(annualGrantQuarters({fiscalYear: Y, hireDate: "{Y-1}-10-01", resignationDate: null, annualDays}))`(메모 9), N · a · b(· c)와 행 전체는 스펙이 04.1-03 순수 함수(`buildLeaveGrants` → `allocateLeave` → `formatBalanceRow`)로 같은 입력에서 계산한 값이며 스펙에 숫자로 적지 않는다 — 신청 창 어디에도 `남음 {P + N}일`이 없다
    - (E2E desktop · D4) 입사일 = `seoulToday()`인 전용 사용자(근속 첫 1년 · 월차 적립 0)가 `반차`(`{week: 15, weekdays: 1}`)를 고르면 잔고 행 1행에 `월차 남음 0일`이 있고, 행 전체가 스펙이 같은 순수 함수로 계산한 `formatBalanceRow` 결과와 같다 — 월차 칸이 0이라고 사라지지 않는다(미리보기만 보고 제출하지 않는다)
    - (E2E) 종류를 `반차`로 바꾸면 날짜 칸이 하나 + `오전`/`오후`가 되고 힌트 `이번 신청 0.5일`, `재택`이면 힌트 `재택 · 차감 없음`이고 잔고 행이 없다(결재선 한 줄은 있다)
    - (E2E) 연차 남음 15일(입사일 없음 사용자)에 평일 16일(`{week: 14, weekdays: 16}`)을 고르면 3행 `잔여 초과 1일`(`--warning`)이 보이고 `연차 신청`은 눌리며 신청된다
    - (E2E) 날짜를 비우고 `Ctrl+Enter` → `시작일 · 종료일 2칸 비어 있음 · 시작일 적기`와 3차 `시작일 적기`(누르면 그 칸 포커스)
    - (E2E) 비고를 적은 뒤 `Esc` → 확인 `입력 버리기`(부제 `연차 신청 · 1칸`) → `입력 버리기` → `/leave`. 아무것도 안 적고 `Esc`면 바로 `/leave`
    - (E2E mobile-375) 폰에서 행동 줄이 하단 탭 위에 고정되고 비고 칸이 가려지지 않는다
    - (E2E desktop · CX-R3 · CX2-02) 스펙 전용 계급 A(권한표 도메인 함수로 `leave` view · write를 켜고, 정보 노출표 도메인 함수로 `role.value` 노출 행만 만든다 — `approval.value`는 행이 없어 숨김, CEO-10. 새 계급은 `staffDefault`의 노출을 받지 않으므로 `role.value`를 스펙이 명시로 켠다)의 사람(전용 팀 발령)이 `/leave/new`에서 날짜(`{week: 16, weekdays: 2}`)를 고르면 결재선 한 줄이 스펙이 같은 사람을 viewer로 04.1-01 `previewRoute`를 불러 받은 `RoutePreviewDTO`의 단계 `label`만으로 만든 줄(`팀장 → 경영관리 → 대표` 모양)로 시작하고, 그 사람 이름과 전용 팀장 이름이 신청 폼 영역 글자 어디에도 없다. 양성 대조: 같은 방식으로 만든 기본 계급 기안자의 결재선 한 줄이 그 기안자의 고유 이름으로 시작하고 전용 팀장의 고유 이름을 담는다. 이름을 투영 밖에서 구해 싣는 구현이면 RED. (C-07) 이 사례와 아래 계급 B 사례의 기안자 · 전용 팀장은 스펙이 도메인 `registerPerson(SYSTEM_VIEWER, {name, email, roleId, teamId, effectiveFrom})`(반환 `userId` · `tempPassword`로 로그인)으로 사례마다 고유 이름(짧은 난수 접미 — 기안자와 팀장이 서로 다르다)을 줘 만든다 — `createFixtureUser`는 이름이 전부 `E2E Employee`라 기안자·팀장 이름이 같아 누수와 정상이 구분되지 않는다(`test/e2e/fixtures.ts`는 고치지 않는다). 이름 부재 단언의 범위는 신청 폼 영역 locator다 — 셸 상단 바는 로그인한 사람 이름을 늘 보이므로 범위 밖
    - (E2E desktop · CX2-02) 스펙 전용 계급 B(`leave` view · write만 — 노출 행 없음, `approval.value` · `role.value` 둘 다 숨김)의 사람(전용 팀 발령)이 같은 날짜를 고르면 결재선 한 줄이 스펙이 같은 사람을 viewer로 부른 `previewRoute`의 DTO 단계 `label`로 만든 줄 — 04.1-01의 대체 이름 `1단 → 2단 → …` — 로 시작하고, 그 사람 이름 · 전용 팀장 이름(둘 다 `registerPerson` 고유 이름 — C-07) · 계급 이름(`팀장`)이 신청 폼 영역 글자 어디에도 없으며 자기 승인 건너뜀 한 마디도 없다(`skipped`도 숨김)
    - (E2E desktop · CX-R4) 입사일 없음 전용 사용자가 종일로 `leaveWeekdayRange(seoulToday(), {week: 18, weekdays: 1})`의 시작~끝을 골라 힌트 `이번 신청 1일`을 본 뒤, 스펙이 `page.route`로 다음 미리보기 요청(`next-action` 헤더가 있는 POST)의 응답을 `route.fetch()`로 받아 쥔 채 종료를 `{week: 18, weekdays: 2}`의 끝으로, 이어 `{week: 18, weekdays: 3}`의 끝으로 바꾸고 쥔 응답을 푼다. (CXF2-C-F2-03) 응답을 쥔 동안 둘째 요청이 나가지 않았음을 먼저 단언한다 — 셋째 입력 뒤 `page.evaluate`로 애니메이션 프레임 한 번과 태스크 한 번을 돌린 다음(그 전에 시작된 요청의 `request` 이벤트는 이 왕복보다 먼저 온다) 미리보기 요청 수가 1이다. 그다음 쥔 응답을 풀고, 미리보기 응답이 둘 다 온 뒤(`expect.poll`로 응답 수 2) 힌트가 마지막 입력의 `이번 신청 3일`이다. 미리보기를 서버 액션 밖 `fetch` · Route Handler · 병렬 호출로 옮긴 구현은 쥔 동안 요청 수가 2라 끝나는 순서와 무관하게 RED다. 시간 지연(`waitForTimeout`)을 쓰지 않는다
    - (E2E desktop · CEO-12) 유효한 칸을 채우고 `Ctrl+Enter`를 연달아 두 번 치면 새 신청은 한 건이다 — 스펙이 전후로 `listMyLeave` 도메인 조회로 건수를 센다. 1차를 빠르게 두 번 클릭(`dblclick`)한 사례도 한 건이다. 누른 직후 1차 라벨이 `연차 신청…`이고 비활성이다
  </behavior>
  <action>
① (CX-R3) 결재선 미리보기는 04.1-01이 만든 `previewRoute(viewer, {kind}, deps?)` → `RoutePreviewDTO`(제출과 같은 조립 함수 · 저장 없음 · 결재 정보 투영 뒤 — 04.1-01이 투영 켬/끔 테스트와 누수 스캔 등록을 맡는다)를 부르기만 한다. 이 플랜은 `domain/approvals/`에 결재선 함수를 정의하지 않는다.

② `app/(app)/leave/actions.ts`에 `previewLeaveAction`(입력 칸 전체, 저장 없음) — **첫 문장이 `assertLeaveWrite(viewer)`**(04.1-02 · `previewRoute`는 결재 모듈 함수라 연차 권한을 모르므로 액션이 먼저 막는다, Codex MEDIUM) → `countLeaveQuarters` 결과(힌트 · 주말 제외 수 · 칸 오류) + `previewLeaveBalance`(→ `formatBalanceRow`, 재택이면 null — 연차·월차 남음 분리, 합계 없음) + `previewRoute`가 준 `RoutePreviewDTO`를 그대로 한 응답으로(이름을 더하거나 다른 곳에서 다시 구하지 않는다 — CX-R3). `actions.registry.ts`에 등록(메뉴 `leave` · 동작 `write` — 신청 창 전용이고 권한 판정이 write이므로 누수 스캔 분류도 같게 둔다).

③ `leave-form.tsx` 완성: 종류 Select 변경 시 칸 구성 전환(반차·반반차 = 날짜 하나 + 오전/오후) · 날짜·종류 change마다 `previewLeaveAction` 한 번(받는 동안 이전 값 유지 — 계획 가정 2) · 날짜 칸 아래 `Form.Hint` 일수 힌트 · `잔고` 행(1~3행, 3행 `--warning` 600, 1차는 막지 않음 — 금지 항목) · 제출 전 결재선 한 줄(`ApprovalRoute` line — 응답의 `RoutePreviewDTO`만 그린다: 단계마다 `holderNames`가 있으면 `{이름} {label}`, 투영에서 빠졌으면 `label`만, `drafterName`이 없으면 첫 단계부터, `skipped`면 끝 한 마디 — `role.value`가 숨겨지면 04.1-01이 `label`을 `N단`으로 주고 `skipped`는 빠지므로 화면은 받은 그대로 그린다, CX-R3 · CX2-02) · 1차 `연차 신청` + kbd `Ctrl+Enter`(폰은 kbd 없음 — 제출 중에는 `ui/button`의 기존 `pending`으로 `…` + 비활성, `Ctrl+Enter` 처리기는 제출 중이면 아무것도 하지 않는다. 두 keydown이 한 렌더 사이에 올 수 있으므로 제출 중 여부는 렌더 상태만이 아니라 동기로 바뀌는 ref로도 막는다 — CEO-12. ref와 `pending`은 누른 즉시 켠다 — 서버 액션 순차 전송 때문에 제출 요청이 대기 중인 미리보기 뒤에 줄을 서도 1차는 누른 직후 `연차 신청…`이다, C-P2. 서버 멱등키·새 인덱스·스키마 변경은 두지 않는다) · 2차 `취소`와 `Esc` = 입력이 있으면 `ConfirmDialog`로 `입력 버리기`(부제 `연차 신청 · N칸`) · 필수 막힘 문구와 그 칸으로 가는 3차 · 제출 실패 한 줄 · (B-A1) 신청 · 다시 신청 성공 토스트는 액션이 돌려준 투영된 결과 필드만으로 조립한다 — 이름 · 일수 조각이 투영에서 빠졌으면 그 조각을 빼(`연차 신청 · 결재 요청됨` · `연차 다시 신청 · 결재 요청됨`) 폼이 이름 · 일수를 따로 구하지 않는다(04.1-02 ④ · UI-SPEC Copywriting 토스트 메모). 04.1-05의 다시 신청 모드는 그대로 동작해야 한다(같은 폼 — 미리보기도 같이 쓴다). (CX-R4) 미리보기는 서버 액션 `previewLeaveAction`으로만 부른다 — Next 16은 서버 액션을 클라이언트마다 하나씩 보내므로(read_first 문서 「Sequential dispatch on the client」) 응답이 요청 순서대로 적용되어 늦은 옛 응답이 최신 입력의 결과를 덮지 못한다. 미리보기를 Route Handler · `fetch` · `Promise.all`로 옮기거나 병렬화하지 않고, 순번 가드를 따로 두지 않는다 — behavior의 CX-R4 E2E가 이 보장을 지킨다.

④ `app/(app)/leave/new/page.tsx`: 머리 줄(번호 없음 — 제출 때 부여) · 폰 행동 줄 고정 · 본문 아래 여백.

⑤ E2E 두 파일에 behavior를 더한다. (C-07) 결재선 이름을 단언하는 사례(계급 A · 계급 B · 양성 대조)의 사람은 `registerPerson`(고유 이름)으로 만들고 이름 부재 단언은 신청 폼 영역 locator 안에서만 한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/document-kinds-import.test.ts test/unit/import-cycles.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>`previewRoute`를 부르는 `app/(app)/leave` 파일이 `document-kinds`를 import하지 않거나(CEO-3), import 순환이 생기거나, eslint·stylelint·tsc·next build 중 하나라도 error를 출력하면 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/leave-list.spec.ts test/e2e/mobile-leave-list.spec.ts test/e2e/leave-document.spec.ts test/e2e/leave-approval.spec.ts</automated>
    <fails_when>playwright failed가 1 이상이다 — 힌트·잔고 행·결재선 한 줄 문자열이 다르거나, `approval.value`가 숨겨진 계급의 결재선 한 줄에 이름이 있거나(CX-R3), 늦게 푼 옛 미리보기 응답이 최신 힌트를 덮거나(CX-R4), 입사 다음 해 사용자의 잔고 행에 두 잔고의 합이 보이거나 `월차 남음`이 없거나, 초과 신청이 막히거나, 두 번 누름·`Ctrl+Enter` 두 번에 신청이 2건 생기거나(CEO-12), 다시 신청 모드가 회귀하면 여기서 깨진다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/approvals-route-fixed.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>제출 경로의 결재선 고정 테스트가 회귀하거나 새 액션이 누수 스캔에 없으면 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - (CX-R3) 이 플랜 diff에 `domain/approvals/` 변경이 없다(`git diff --stat "$PLAN_BASE"..HEAD -- domain/approvals`가 비어 있음) — 미리보기는 04.1-01 `previewRoute`만 부르고, `approval.value` 숨김 계급 사례(이름 없음)와 양성 대조가 녹색이다
    - (CX-R4) 미리보기 응답을 붙잡았다 푸는 E2E 사례가 녹색이고 `grep -cE "waitForTimeout" test/e2e/leave-list.spec.ts`이 0이다 · `grep -cE "fetch\(|Promise\.all" "app/(app)/leave/new/leave-form.tsx"`이 0이다
    - 초과 경고가 있어도 1차가 눌린다 — E2E가 `toBeEnabled()`로 단언한다(`ui/button`은 `aria-disabled`를 쓰지 않고 `disabled`만 쓰므로 `aria-disabled` 부재 단언은 공허하다, C-11)
    - (C-07) 결재선 이름 사례 셋의 사람을 `registerPerson`으로 만든다(`grep -c "registerPerson" test/e2e/leave-list.spec.ts`이 1 이상) — 계급 A · B의 이름 부재와 양성 대조의 고유 이름 포함이 녹색이다
    - (Codex MEDIUM) `previewLeaveAction` 본문 첫 문장이 `assertLeaveWrite`이고 등록 동작이 `write`다(`grep -A3 "name: \"previewLeaveAction\"" "app/(app)/leave/actions.registry.ts" | grep -c 'action: "write"'`이 1)
    - (Codex HIGH 06 · 11:43) `grep -cE '"20[0-9]{2}-[0-9]{2}-[0-9]{2}"' test/e2e/leave-list.spec.ts test/e2e/mobile-leave-list.spec.ts`가 0이고, 입사 다음 해 신청 창 사례가 `월차 남음`과 `연차 남음`을 따로 단언하며 합계 부재를 단언한다
    - (R1/D5 · D4 · 메모 9) 입사 다음 해 신청 창 사례의 `연차 남음` 기대값이 `annualGrantQuarters` + `formatLeaveDays` 계산값이고(`연차 남음 15일` 리터럴은 입사일 없는 사용자 사례에만), 입사일 = 오늘 사용자 사례가 `월차 남음 0일`을 단언한다
    - 폼 파일이 하나다(`find "app/(app)/leave" -name "*form*.tsx"`가 1줄)
    - (CEO-12) 두 번 제출 E2E 두 사례(`Ctrl+Enter` 두 번 · `dblclick`)가 있고 녹색이다. `git diff --stat "$PLAN_BASE"..HEAD -- ui/button db/`가 비어 있다(버튼·스키마 불변)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>신청 폼이 종류별 칸 · 일수 힌트 · 잔고 행(초과 경고, 막지 않음) · 결재선 미리보기 · 입력 버리기 · `Ctrl+Enter`를 갖춘 완성형이고, 두 번 눌러도 신청은 한 건이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 관리자 사람 상세 `연차` 섹션과 등록 폼 입사일 필수</name>
  <files>app/(app)/admin/people/[id]/page.tsx, app/(app)/admin/people/[id]/leave-section.tsx, app/(app)/admin/people/[id]/actions.ts, app/(app)/admin/people/[id]/actions.registry.ts, app/(app)/admin/people/person-form.tsx, app/(app)/admin/people/actions.ts, test/integration/leak-scan.test.ts, test/e2e/admin-person-leave.spec.ts, test/e2e/people.spec.ts, test/e2e/mobile-people.spec.ts, test/e2e/admin-master-list-first.spec.ts, test/e2e/corp-cards.spec.ts, test/e2e/mobile-corp-cards.spec.ts, test/e2e/single-column.spec.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md` 380–388행(S9 전문) · 195·199·201·213·221행(조정 CTA · 조정 EMPTY · 관리자 오류 · 퇴직 · S9 라벨 원문)
    - `app/(app)/admin/people/[id]/page.tsx` · `app/(app)/admin/people/[id]/person-detail-client.tsx` — 「계급 변경」 select의 즉시 저장 선례와 KvList · 발령 이력 표 자리
    - `app/(app)/admin/people/actions.ts` · `app/(app)/admin/people/actions.registry.ts` · `app/(app)/admin/people/person-form.tsx` — 등록 zod · 폼 칸 모양(Phase 3 관리자 폼 — `ui/form` 이관은 Phase 7, 칸 하나만 현재 모양대로)
    - `domain/people/index.ts`(`setHireDate` · `setResignationDate` · `registerPerson` hireDate — 04.1-03. (C-N01) `PersonDto`(46–55행)에는 입사일·퇴직일이 없다 — 이 플랜은 넓히지 않고 두 날짜는 잔고 DTO에서 읽는다, ③) · `domain/leave/balance-service.ts`(`getLeaveBalanceForUser`(fiscalYear 인자 — `LEAVE_BALANCE_DTO_SPEC`이 `hireDate` · `resignationDate`를 싣는다) · `addLeaveAdjustment(viewer, {userId, bucket, fiscalYear, amountDays, reason})` — 연차는 `fiscalYear` 필수 · 월차는 없음, DB CHECK · ENG-13) · (CX-R2) `domain/leave`의 `listLeaveAdjustmentsForUser(viewer, {userId, fiscalYear})`(04.1-03 — `LeaveAdjustmentDTO[]`: id · kind · quarters · reason · createdAt · createdByName, 권한 판정은 사람 상세 연차 섹션 읽기 권한(조정 쓰기가 아니다), 월차 조정은 유효 창이 겹치는 해마다 · 연차 조정은 그 `fiscalYear`에만 — 페이지가 부르는 **유일한** 조정 읽기) · `checkLeaveAdjustment`(04.1-03 순수 — 월차 비활성 이유, CX-R5) · `repositories/leave-adjustments.ts`(`listLeaveAdjustments` — **스펙만** 새 행의 `fiscalYear`를 읽는다. 페이지·섹션은 import하지 않는다 — `eslint.config.mjs`가 app → repositories를 막는다)
    - `app/(app)/leave/year-param.ts`(Task 1 — `resolveLeaveYear`, S9-FY)
    - (C-01) 등록 폼을 **제출하는** 스펙 전부 — `grep -rln 'getByRole("button", { name: "사람 등록" }).click' test/e2e`(제출 버튼 셀렉터 — 스펙은 URL이 아니라 목록 머리의 링크 `사람 등록`을 눌러 폼을 열므로 옛 `people?new=1` grep은 0건이었다). 2026-09-25 실측 여섯 파일: `test/e2e/people.spec.ts:32` · `test/e2e/mobile-people.spec.ts:31` · `test/e2e/admin-master-list-first.spec.ts:108` · `test/e2e/corp-cards.spec.ts:27` · `test/e2e/mobile-corp-cards.spec.ts:34` · `test/e2e/single-column.spec.ts:104`(각 줄 앞의 이름·이메일·계급 채우기 뒤가 입사일 한 줄 자리). 링크만 누르고 제출하지 않는 `mobile-admin-master-list-first.spec.ts` · `single-column.spec.ts` 등록 폼 루프(140행)는 해당 없다. 실행 시점에 grep 결과가 이 목록과 다르면 멈추고 보고한다
    - `lib/dates.ts`(`seoulToday`) · `domain/leave/balance.ts`(순수 `buildLeaveGrants` · `summarizeLeaveBalance` · `formatBalanceLines` — 스펙이 기대 문자열을 만드는 데 쓴다)
  </read_first>
  <behavior>
    - (E2E desktop · CEO-15) 관리자가 이 스펙 전용 사람의 상세 `연차` 섹션에서 입사일에 H(= `seoulToday()`의 달에서 6개월 앞 달의 1일 — 일자를 1로 고정해 월말·일자 차이를 없앤다)를 적고 칸을 벗어나면 저장되고(새로 고침 뒤 유지), 잔고 둘째 줄이 스펙이 같은 입력(입사일 H · 기준일 `seoulToday()`)으로 04.1-03 순수 함수를 불러 만든 월차 줄과 같다(`월차 적립 ` 으로 시작 — 적립 수를 스펙에 숫자로 적지 않는다. 근속 첫 1년 사용자라 적립이 몇이든 월차 줄이 있다 — D4)
    - (E2E) 조정 폼에서 잔고 `연차` · 일수 `-1` · 사유 `무단 결근` → `연차 조정 추가` → 조정 기록 표 맨 위에 새 행, 잔고 줄에 `조정 -1일`, 폼이 빈다(토스트 없음). (S9-FY) `?year` 없이 연 섹션은 `잔고` 옆에 `{Y} 회계연도`이고, 스펙이 `listLeaveAdjustments`로 읽은 새 행의 `fiscalYear`가 Y(= `seoulToday()`의 연도)다
    - (E2E · S9-FY) 이 사례 전용 사람(입사일 없음 · 퇴직일 없음 — 다른 사례의 퇴직 줄과 섞이지 않게)의 상세를 `?year` 없이 연 뒤 `잔고` 옆 링크 `{Y-1} 회계연도`를 누르면(CX-R6 — URL을 손으로 고치지 않는다) 주소가 `?year={Y-1}`이고 `잔고` 옆이 `{Y-1} 회계연도`이며 `올해 보기` 링크가 있고, 연차 `1` · 사유 `지난해 정정`을 더하면 새 행의 `fiscalYear`가 {Y-1}이며 그 화면 연차 줄이 스펙이 같은 입력(`fiscalYear` {Y-1} · 조정 +1)으로 `formatBalanceLines`에서 만든 줄과 같다. 이어 `올해 보기`를 누르면(CX-R6 — 주소 `?year={Y}`, CXF3-C-F3-02) `{Y} 회계연도`이고 올해 연차 줄에 `조정`이 없다(지난 연도 조정이 올해로 넘어오지 않는다 — 이월 없음). `?year={Y+1}` · `?year=abc`는 `{Y} 회계연도`로 연다(D6과 같은 함수)
    - (E2E · S9-FY) 근속 첫 1년 사람(입사일 H)에게 잔고 `월차` · 일수 `1` · 사유를 더하면 새 행의 `fiscalYear`가 null이고 월차 줄 적립이 1일 늘어난다 — `?year={Y-1}`로 보는 중이어도 월차 행은 `fiscalYear` null이다(월차는 연도를 넘기지 않는다). 「적립이 1일 늘어난다」는 스펙이 같은 입력(월차 조정 +1)으로 `formatBalanceLines`에서 만든 월차 줄과 같다는 단언이다 — 조정이 `적립`에 더해지는지 따로 쓰이는지는 04.1-03 문자열 계약이 정하고 06이 숫자·낱말을 새로 정하지 않는다(C-14)
    - (E2E) 사유를 비우면 1차가 막히고 `사유 비어 있음 · 사유 적기`, 일수 `0.3`이면 `일수는 0.25 단위 · 0.5처럼 적어 주세요`
    - (E2E · CXF2-C-F2-01 · C-N01 — 퇴직일은 첫 읽기(올해 잔고 DTO)에서 얻는다, ③) 퇴직일 `{Y-1}-06-30`(스펙이 계산)인 전용 사람의 상세를 `?year` 없이 열면 `잔고` 옆이 `{Y-1} 회계연도`이고, 연차 `1` · 사유 `퇴직 정정`을 더하면 스펙이 읽은 새 행의 `fiscalYear`가 {Y-1}이며 같은 화면의 퇴직 줄이 스펙이 같은 입력(조정 +1)으로 `formatResignationLine`에서 만든 줄과 같다 — 보이는 연도와 조정 연도가 어긋나면 RED. `?year=abc`로 열어도 `{Y-1} 회계연도`다. (CXF3-C-F3-01 · C-F3-02) 이어 `올해 보기`를 누르면 주소가 `?year={Y}`이고 부제 `{Y} 회계연도` · `퇴직` 줄 없음 · 연차 줄이 스펙이 04.1-03 순수 함수로 만든 {Y} 보통 잔고 줄(퇴직 연도 다음 해라 부여 0)과 같으며, 연차 `1`을 더하면 새 행의 `fiscalYear`가 {Y}다. `?year={Y-2}`(퇴직 연도 전)로 열면 `퇴직` 줄 없이 {Y-2} 보통 잔고 줄이다
    - (E2E · CEO-15 · CXF3-C-F3-01) 퇴직일 `{Y}-12-28`(올해 퇴직 — 실행 달과 무관하게 올해, 스펙이 계산)을 넣으면 기본 섹션 연도 = 퇴직 연도라 잔고 자리가 스펙이 `formatResignationLine`으로 만든 `퇴직 {Y}-12-28 · 남은 연차 …일 · 월차 …일` 한 줄과 같고 금액 표시가 없다
    - (E2E · CXF3-C-F3-01) 퇴직일 `{Y+1}-03-31`(다음 해 퇴직)인 전용 사람은 기본 섹션 연도가 min(퇴직 연도, 올해) = {Y}라 `퇴직` 줄이 없고 {Y} 보통 잔고 줄(스펙이 순수 함수로 계산)이다. `?year={Y+1}`은 미래라 D6대로 {Y}다
    - (E2E) 조정 기록이 0건이면 `연차 조정 기록이 없습니다`(행동 없음)
    - (E2E) 사람 등록 폼에서 입사일을 비우고 등록하면 `입사일 비어 있음 · 입사일 적기`로 막히고, 채우면 등록되며 상세 입사일에 그 값이 있다. 입사일 칸에는 네이티브 `required`가 없다 — 있으면 브라우저 말풍선이 제출을 먼저 막아 서버 문구가 나오지 않는다(SYSTEM.md §7-15 「검증 관문은 서버 하나」, C-13)
    - (E2E · C-01) 등록 폼을 제출하는 기존 스펙 넷(`admin-master-list-first` · `corp-cards` · `mobile-corp-cards` · `single-column`)과 `people` · `mobile-people`이 등록 단계의 입사일 한 줄(`getByLabel("입사일").fill(…)` — 이미 지난 날짜)로 녹색이다. 그 한 줄이 없으면 등록이 `입사일 비어 있음`으로 막혀 각 스펙의 `초기 비밀번호 — {이메일}`을 찾지 못해 RED다
    - (E2E · CX-R2 · CXF-C-F06) 사람 관리 보기 권한만 있는 스펙 전용 계급(권한표 도메인 함수로 만들고, 정보 노출표 도메인 함수로 `leave.value` 노출 행을 명시로 켠다 — 새 계급은 노출 행이 없어 켜지 않으면 조정 DTO가 빈 객체다)은 `연차` 섹션에 입력 칸·조정 추가 폼이 없고 잔고 줄과 조정 기록 표(읽기 전용)가 있으며 그 표에 `무단 결근`이 보인다(보기 권한만 있으면 잔고·기록만). 노출 대조: 같은 권한에 `leave.value` 노출 행을 만들지 않은 스펙 전용 계급은 `무단 결근`이 그 응답 HTML(`page.content()`) 어디에도 없다(CXF-C-F06). 음성 대조: 사람 상세 연차 섹션 읽기 권한이 없는 계급은 섹션이 없고(페이지 자체가 막히면 404) `무단 결근`이 그 응답 HTML(`page.content()`) 어디에도 없다. 권한과 무관하게 기록을 읽어 넘기거나 보기만 있는 계급에 추가 폼을 그리는 구현이면 RED
    - (E2E · CX-R5 · C-02) 조정 폼 잔고 Select의 처음 선택값이 `연차`다 — 제어값 `annual`이라 공용 `ui/select`가 늘 첫 옵션으로 그리는 빈 `—`가 선택된 채로 열리지 않는다. 입사일 없는 전용 사람은 Select의 `—`를 뺀 옵션이 `연차` 하나뿐이고(옵션 글자 배열이 정확히 `["—", "연차"]` — `—`는 공용 Select가 늘 그리고 이 플랜은 Select를 고치지 않는다 · `월차` 옵션 없음 — CX2-03) Select 아래 한 줄이 스펙이 `checkLeaveAdjustment({bucket: "monthly", fiscalYear: null, amountQuarters: 4, createdOn: 오늘, hireDate: null})`에서 받은 문구와 같다. 입사일 `{Y-2}-01-01`(월차 소멸일 `{Y-1}-12-31`이 지남 — 스펙이 계산)인 전용 사람도 `월차` 옵션이 없고 같은 함수가 준 소멸 문구가 보인다. 입사일 없는 사람에게 입사일 H를 blur로 넣으면 서버 재렌더로 `월차` 옵션이 다시 나타나고 한 줄이 사라진다(양성 대조). 그 상태에서 `월차`를 고른 뒤 입사일을 `{Y-2}-01-01`로 blur 저장하면 `월차` 옵션이 빠지고 선택값이 `연차`로 돌아간다(CX2-03). 날짜는 `onStableSeoulDay`가 준 오늘만 쓴다(CX-R8)
    - (E2E · C-02) 잔고 Select에서 `—`를 고르고 일수 `1` · 사유를 채워 `연차 조정 추가`를 누르면 서버가 거부한다 — Select 아래 칸 오류 한 줄 `잔고 비어 있음 · 잔고 고르기`(SYSTEM.md §7-15 「… 고르기」 틀), 스펙이 `listLeaveAdjustments`로 센 그 사람의 조정 행 수가 그대로다. Select에 네이티브 `required`가 없어 브라우저가 먼저 막지 않는다(서버 판정을 실제로 탄다)
  </behavior>
  <action>
① `app/(app)/admin/people/[id]/actions.ts`(새 파일, `authedActionClient`): `setHireDateAction` · `setResignationDateAction` · `addLeaveAdjustmentAction` — 도메인 함수(04.1-03)를 부르고 칸 오류를 칸별로 돌려준다. (S9-FY) `addLeaveAdjustmentAction` 입력은 `{userId, bucket, amountDays, reason, fiscalYear?}`이고 zod가 연차면 `fiscalYear` 정수 2000 ≤ y ≤ `seoulToday()`의 연도를 요구, 월차면 `fiscalYear`가 없어야 한다(있으면 거부 — 04.1-03 `checkLeaveAdjustment`와 DB CHECK와 같은 모양). 상한은 스키마를 만들 때가 아니라 호출 때 계산한다 — `.refine`처럼 요청마다 `seoulToday()`를 읽는다(모듈 최상위 `.max(…)`로 굳히면 새해를 넘긴 인스턴스가 옛 상한을 쓴다, C-12). (C-02) `bucket`은 `annual` · `monthly` 둘만 받고, 빈 값(Select의 `—`)은 칸 오류 `잔고 비어 있음 · 잔고 고르기`로 거부한다. 도메인 `addLeaveAdjustment`에 그대로 넘긴다 — `fiscalYear`는 섹션이 받은 섹션 연도 prop 그대로다(CXF2-C-F2-01). (CXF2-C-F2-02) 세 액션은 성공 뒤 `revalidatePath`로 `/admin/people/{userId}` 경로를 무효화한다 — `app/(app)/admin/people/actions.ts` `changePersonRoleAction`(40–42행)과 같은 모양 — 그래서 잔고 줄 · 조정 기록 · `월차` 옵션이 새로 고침 없이 서버 재렌더로 바뀐다. `actions.registry.ts`(새 파일)에 메뉴 `admin.people` · 동작 `write`로 등록. `test/integration/leak-scan.test.ts` **파일 끝에** 이 등록 파일 import 한 줄.

② `leave-section.tsx`(client): 2px 섹션 선 + 제목 `연차`, `.single-column` 720. 입사일 · 퇴직일 날짜 입력 280(blur 즉시 저장 — 계급 변경 select 선례와 같은 방식, 칸 아래 한 줄 오류) · `잔고`(04.1-03 DTO에 퇴직 줄 재료가 있으면 — 섹션 연도 = 퇴직 연도일 때만 — `formatResignationLine`, 없으면 `formatBalanceLines`. 화면이 퇴직일로 따로 분기하지 않는다, CXF3-C-F3-01) · 조정 폼(`Form` + `Form.Actions`: 잔고 Select 200 `연차`/`월차` · 일수 짧은 칸 280 `inputMode="decimal"` 힌트 `빼려면 -1처럼` · 사유 긴 칸 480 필수 · 1차 `연차 조정 추가`) · 조정 기록 표(컨테이너 폭, 최신이 위, 열 날짜(P2) · 잔고(P2) · 일수(P1) · 사유(P1) · 입력(P3), 수정·삭제 버튼 없음, 0건 EMPTY). 쓰기 권한이 없으면 입력·조정 추가 폼을 렌더하지 않고 조정 기록 표는 읽기 전용으로 그린다(서버가 판정해 prop으로 넘긴다 — 보기 권한만 있으면 잔고·기록만, CX-R2). 조정 기록 표는 `LeaveAdjustmentDTO`만 그린다 — 날짜 = `createdAt`의 서울 날짜 · 잔고 = `kind` · 일수 = `formatLeaveDays(quarters)`(부호 포함) · 사유 = `reason` · 입력 = `createdByName`. (CX-R5 · CX2-03 · C-02) 잔고 Select는 처음 선택값이 제어값 `annual`(`연차`)이고 — 공용 Select가 늘 첫 옵션으로 그리는 빈 `—`는 그대로 둔다(공용 컴포넌트 불변) · 브라우저 네이티브 필수 속성을 두지 않는다 — `—`를 고른 제출은 ①의 서버 zod가 거부한다. 서버가 넘긴 `monthlyBlockedReason`이 있으면 `월차` 옵션을 옵션 목록에서 빼고 그 문구를 Select 아래 `Form.Hint` 한 줄로 보인다 — 공용 `ui/select/Select.tsx`는 옵션 비활성을 받지 않고 이 플랜은 고치지 않는다. 선택값이 `월차`인데 `monthlyBlockedReason`이 생기면(입사일 재저장 뒤 재렌더) 선택값을 `연차`로 되돌린다. 새 문구는 없다 — 04.1-03 `checkLeaveAdjustment`의 거부 문구 그대로다. (CX-R6) `잔고` 옆 `{Y} 회계연도` 뒤에 3차 링크 `{Y-1} 회계연도`(→ `?year={Y-1}`, Y-1 ≥ 2000일 때)와, 지난 연도를 보는 중이면 3차 링크 `올해 보기`(→ `?year={올해}` 명시 — 퇴직자의 기본 연도가 퇴직 연도라 `?year`를 빼면 제자리다, CXF3-C-F3-02)를 둔다 — `{N} 회계연도` · `올해 보기`는 S1의 기존 문구다. (S9-FY) 섹션은 서버가 넘긴 연도 `year` prop을 `잔고` 옆 `{Y} 회계연도`로 보이고, 조정 제출 때 잔고 Select가 `연차`면 `fiscalYear: year`를, `월차`면 `fiscalYear` 없이 보낸다 — 연도 select는 두지 않는다(지난 연도 정정 = 그 연도를 `?year=`로 연 뒤 조정).

③ `[id]/page.tsx`: 기존 KvList·계급 변경 아래, 발령 이력 표 위에 섹션을 넣고 잔고·조정 목록을 서버에서 읽어 넘긴다. (CX-R2) 조정 목록은 `listLeaveAdjustmentsForUser(viewer, {userId, fiscalYear: 섹션 연도})`만 부르고 — viewer가 섹션을 볼 수 있으면 늘 부른다(판정은 섹션 읽기 권한과 같다) — `repositories/`를 import하지 않는다. (CX-R5) 같은 자리에서 `monthlyBlockedReason = checkLeaveAdjustment({bucket: "monthly", fiscalYear: null, amountQuarters: 4, createdOn: seoulToday(), hireDate})`(문구 또는 null)를 계산해 섹션에 넘긴다 — 입사일을 blur로 저장하면 서버 재렌더로 다시 계산된다. (CX-R9) 섹션 전용 `loading.tsx` · `error.tsx`나 섹션 오류 경계를 새로 만들지 않는다 — 읽기 실패는 기존 `app/(app)/error.tsx`가 받는다(UI-SPEC loading S9 행). 섹션 연도 = `resolveLeaveYear(searchParams.year, seoulToday()의 연도, 대체 연도)`(Task 1 `year-param.ts` — D6과 같은 규칙. 선택 셋째 인자 대체 연도는 `?year`가 없거나 무효일 때 쓰는 값이고 기본값은 올해라 `/leave`는 그대로다. 사람 상세는 퇴직일이 있으면 min(퇴직 연도, 올해)를 넘긴다 — CEO-11 · S9-FY · CXF2-C-F2-01)를 **한 번** 계산하고 그 값 하나를 `getLeaveBalanceForUser`의 `fiscalYear` · `listLeaveAdjustmentsForUser`의 `fiscalYear` · 섹션 `year` prop(조정 `fiscalYear`와 부제)에 같이 넘긴다 — 따로 계산하지 않는다. (C-N01 · 읽기 순서 고정) 대체 연도에 필요한 퇴직일은 `getPerson`의 `PersonDto`에 없으므로(이 플랜은 넓히지 않는다) 순서는 **① 첫 읽기 `getLeaveBalanceForUser(viewer, userId, {fiscalYear: 올해})`**(올해 = `seoulToday()`의 연도) → 그 DTO의 `hireDate` · `resignationDate`로 대체 연도(퇴직일이 있으면 min(퇴직 연도, 올해), 없으면 올해)를 정하고 `resolveLeaveYear`로 섹션 연도 → **② 섹션 연도가 올해와 다를 때만** `getLeaveBalanceForUser(…, {fiscalYear: 섹션 연도})`를 한 번 더 부르고(같으면 첫 읽기를 그대로 잔고로 쓴다 — 잔고 읽기는 최대 2회) → **③** `listLeaveAdjustmentsForUser(…, {fiscalYear: 섹션 연도})`. 입사일 · 퇴직일 칸 초기값과 `monthlyBlockedReason`의 `hireDate`도 첫 읽기의 값이다. 두 날짜는 04.1-03 `LEAVE_BALANCE_DTO_SPEC`에 조회 연도와 무관하게 실린다(퇴직 연도가 아닌 해로 읽어도 `resignationDate`가 있다 — 04.1-03 ⑤의 C-N01 문장과 퇴직자 연도별 통합 사례의 `resignationDate` 단언, 대조 메모 `docs/designs/phase04.1-eng-review2-260925/cross-notes-C.md`). viewer 계급이 `leave.value`를 숨겨 두 날짜가 투영에서 빠지면 대체 연도는 올해다(둘째 읽기 없음). 리포지토리를 직접 부르지 않는다. 기존 영역은 건드리지 않는다.

④ 등록: `person-form.tsx`에 `입사일` 날짜 칸 하나(현재 폼 모양 그대로 — 단 네이티브 `required`는 두지 않는다: 브라우저 말풍선이 서버 문구 `입사일 비어 있음 · 입사일 적기`를 가린다, SYSTEM.md §7-15 · C-13), `app/(app)/admin/people/actions.ts`의 등록 zod에 `hireDate` 필수(비면 `입사일 비어 있음 · 입사일 적기`)와 `registerPerson` 전달 **만** 더한다(병합 위험 — 다른 줄 불변). (C-01) read_first grep이 찾은 등록 폼 **제출** 스펙 여섯 — `test/e2e/people.spec.ts` · `test/e2e/mobile-people.spec.ts` · `test/e2e/admin-master-list-first.spec.ts` · `test/e2e/corp-cards.spec.ts` · `test/e2e/mobile-corp-cards.spec.ts` · `test/e2e/single-column.spec.ts` — 각각의 등록 단계(계급 `selectOption` 뒤 · 제출 버튼 `사람 등록` 클릭 앞)에 입사일 입력 **한 줄**(`await page.getByLabel("입사일").fill("2026-01-01");` — 이미 지난 날짜, `single-column`의 발령일과 같은 값)만 더한다. 그 스펙의 다른 줄은 건드리지 않는다.

⑤ `test/e2e/admin-person-leave.spec.ts`가 behavior 줄 전부를 단언한다(조정 행의 `fiscalYear`는 스펙(테스트 코드)만 04.1-03 `listLeaveAdjustments`로 읽는다 — DTO에 `fiscalYear`가 없다, S9-FY · CX-R2). 대상 사람은 스펙 안에서 만든 전용 사람이고, 날짜·기대 문자열은 `seoulToday()`와 04.1-03 순수 함수로 계산한다(CEO-15 — 네 자리 연도를 스펙에 적지 않는다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>새 `actions.registry.ts`가 leak-scan에 import되지 않았거나 lint·tsc·build가 error를 출력한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts test/integration/people.test.ts test/integration/leave-balance.test.ts</automated>
    <fails_when>새 액션의 메뉴·동작·DTO가 레지스트리에 없거나 사람·잔고 통합 테스트가 회귀하면 failed가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/admin-person-leave.spec.ts test/e2e/people.spec.ts test/e2e/mobile-people.spec.ts test/e2e/admin-people-detail-link.spec.ts test/e2e/admin-master-list-first.spec.ts test/e2e/corp-cards.spec.ts test/e2e/mobile-corp-cards.spec.ts test/e2e/single-column.spec.ts</automated>
    <fails_when>playwright failed가 1 이상이다 — 입사일 필수화로 등록 폼을 제출하는 기존 스펙 여섯(`people` · `mobile-people` · `admin-master-list-first` · `corp-cards` · `mobile-corp-cards` · `single-column`) 중 하나라도 입사일 한 줄이 빠져 `초기 비밀번호 — …`를 못 찾거나(C-01 — 옛 명령은 뒤의 넷을 돌리지 않아 06이 거짓 녹색으로 끝나고 07 전체 게이트에서 넷이 한꺼번에 깨졌다), 연차 섹션이 `single-column`의 단일 기둥 단언을 깨거나, 새 섹션 단언(`—` 선택 거부 · 퇴직자 기본 섹션 연도 포함 — C-02 · C-N01)이 실패하면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/(app)/admin/people/actions.ts`의 이 플랜 diff가 등록 zod의 `hireDate` 줄과 전달 줄뿐이다(`D=$(git diff "$PLAN_BASE"..HEAD -- "app/(app)/admin/people/actions.ts") && printf '%s\n' "$D" | grep -c '^[+-][^+-]'`이 1 이상 6 이하)
    - 조정 기록 표에 수정·삭제 버튼이 없다
    - 금액·수당 표시가 섹션에 없다(`grep -cE "₩|수당|[0-9]원" "app/(app)/admin/people/[id]/leave-section.tsx"`이 0)
    - `test/integration/leak-scan.test.ts` 변경이 파일 끝 한 줄 추가뿐이다
    - (CEO-15) `grep -cE "\b20[0-9]{2}\b" test/e2e/admin-person-leave.spec.ts`이 0이다
    - (CX-R2) `grep -c "repositories/" "app/(app)/admin/people/[id]/page.tsx" "app/(app)/admin/people/[id]/leave-section.tsx"`이 파일마다 0이고 `grep -c "listLeaveAdjustmentsForUser" "app/(app)/admin/people/[id]/page.tsx"`이 1 이상이다 · 보기만 있는 계급이 읽기 전용 기록 표(`무단 결근`)를 보고 추가 폼이 없다는 E2E와 권한 없는 계급의 음성 대조가 녹색이다
    - (CX-R5 · CX2-03) `월차` 옵션 없음 두 사례(입사일 없음 · 소멸 지남) · 입사일을 넣으면 옵션이 다시 나타나는 사례 · 고른 `월차`가 쓸 수 없게 되면 `연차`로 돌아가는 사례가 녹색이다 · `git diff --stat "$PLAN_BASE"..HEAD -- ui/select`가 비어 있다 · 거부 문구는 스펙이 `checkLeaveAdjustment`로 받은 값이다(문구 리터럴 없음)
    - (CX-R6) `grep -c "올해 보기" "app/(app)/admin/people/[id]/leave-section.tsx"`이 1 이상이고, `지난해 정정` 사례가 `page.goto`의 `?year=`가 아니라 링크 누르기로 그 연도를 연다
    - (CX-R8 · CXF-C-F05) `grep -c "onStableSeoulDay" test/e2e/admin-person-leave.spec.ts`이 1 이상이다
    - (CXF2-C-F2-01) `grep -c "resolveLeaveYear(" "app/(app)/admin/people/[id]/page.tsx"`이 정확히 1이고(섹션 연도를 한 번만 계산) 퇴직자 `?year` 없음 사례가 녹색이다
    - (CXF2-C-F2-02) `grep -c "revalidatePath(" "app/(app)/admin/people/[id]/actions.ts"`이 3 이상이고, 입사일 blur · 조정 추가 E2E가 `page.reload()` 없이 잔고 줄 · 기록 · `월차` 옵션 변화를 단언한다(헬퍼는 Task 1이 만든 것을 쓴다)
    - (CX-R9) `find "app/(app)/admin/people/[id]" -name "loading.tsx" -o -name "error.tsx"`가 빈 출력이다(섹션 전용 경계 없음 — UI-SPEC loading S9 행)
    - (C-01) 등록 폼 제출 스펙 여섯 전부에 입사일 한 줄이 있다 — `F=$(grep -rl 'getByRole("button", { name: "사람 등록" }).click' test/e2e) && test "$(printf '%s\n' "$F" | wc -l)" -eq 6 && M=$(grep -L 'getByLabel("입사일")' $F; true) && test -z "$M"`(여섯 파일 · 빠진 파일 없음) · 셋째 검증 명령이 네 스펙(`admin-master-list-first` · `corp-cards` · `mobile-corp-cards` · `single-column`)을 포함해 녹색이다 · 네 스펙의 이 플랜 diff가 입사일 한 줄씩뿐이다(`. "$(git rev-parse --git-dir)/gsd-04.1-06.env" && test -n "$PLAN_BASE" && D=$(git diff "$PLAN_BASE"..HEAD -- test/e2e/admin-master-list-first.spec.ts test/e2e/corp-cards.spec.ts test/e2e/mobile-corp-cards.spec.ts test/e2e/single-column.spec.ts) && printf '%s\n' "$D" | grep -c '^[+-][^+-]'`이 4)
    - (C-02) 잔고 Select의 처음 선택값 `연차` · 입사일 없는 사람의 옵션 글자 배열 `["—", "연차"]` · `—` 선택 제출 거부(칸 오류 한 줄 · 조정 행 수 불변) E2E가 녹색이다 · `grep -v '^[[:space:]]*//' "app/(app)/admin/people/[id]/leave-section.tsx" | grep -cw "required"`이 0이다(주석 줄 제외)
    - (C-N01) `grep -c "getLeaveBalanceForUser(" "app/(app)/admin/people/[id]/page.tsx"`이 1 이상 2 이하이고, 이 플랜 diff에 `domain/people/index.ts` 변경이 없다(`PersonDto`를 넓히지 않았다 — `. "$(git rev-parse --git-dir)/gsd-04.1-06.env" && test -n "$PLAN_BASE" && D=$(git diff --stat "$PLAN_BASE"..HEAD -- domain/people/index.ts) && test -z "$D"`) · 퇴직 `{Y-1}-06-30` 사람의 `?year` 없음 · `?year=abc` 사례(부제 `{Y-1} 회계연도`)가 녹색이다
    - (C-13) 등록 폼 입사일 칸에 네이티브 `required`가 없고 `입사일 비어 있음 · 입사일 적기` E2E가 녹색이다
    - (S9-FY) `grep -c "resolveLeaveYear" "app/(app)/admin/people/[id]/page.tsx"`이 1 이상이고 `leave-section.tsx`에 연도 select가 없다(`grep -cE "name=\"year\"|label=\"연도\"" "app/(app)/admin/people/[id]/leave-section.tsx"`이 0). `?year={Y-1}` 조정 → `fiscalYear` {Y-1} · 월차 조정 → `fiscalYear` null 두 E2E 사례가 있고 녹색이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>관리자가 사람 상세에서 입사일·퇴직일·조정으로 잔고를 바로잡고(연차 조정은 보고 있는 회계연도에 들어간다), 새 사람은 입사일 없이 등록되지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 브라우저 → 사람 상세 연차 액션 | 입사일 · 퇴직일 · 조정 일수 · 사유 |
| 직원 브라우저 → 신청 미리보기 액션 | 칸 값이 서버 계산에 들어간다(저장 없음) |
| 셸 → 연차 진입점 | 권한 없는 사람에게 항목이 보이면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-32 | Elevation of Privilege | `setHireDateAction` · `setResignationDateAction` · `addLeaveAdjustmentAction` · 조정 기록 읽기 | high | mitigate | 도메인이 사람 관리 쓰기 권한을 판정(04.1-03), 화면은 쓰기 권한이 없으면 폼을 렌더하지 않는다. 조정 기록은 페이지가 `repositories/`가 아니라 04.1-03 `listLeaveAdjustmentsForUser`(섹션 읽기 권한 판정 · `LeaveAdjustmentDTO` 투영 · 누수 스캔 등록)로만 읽는다 — 보기만 있는 계급은 읽기 전용 기록, 권한 없는 계급에는 기록·사유가 없다(E2E, CX-R2). 액션은 `admin.people` write로 등록되어 누수 스캔 액션 축에 들어간다 |
| T-04.1-33 | Information Disclosure | `previewLeaveAction` | medium | mitigate | 첫 문장 `assertLeaveWrite` — 로그인만 한 사용자는 잔고·결재자 이름을 받지 못한다(Codex MEDIUM, 도메인 거부는 04.1-02 `leave-permission.test.ts`). 본인 잔고만 계산(`previewLeaveBalance`는 viewer 본인), 결재선 미리보기는 04.1-01 `RoutePreviewDTO` 그대로 — 결재자·기안자 이름은 viewer 계급이 `approval.value`를 통과할 때만 싣고, 숨김이면 단계 이름만이다(04.1-01 투영 켬/끔 테스트 · 이 플랜 E2E 숨김 계급 사례, CX-R3). 다른 사람 id를 받지 않는다 |
| T-04.1-34 | Information Disclosure | `/leave` · 계정 그룹 진입점 | medium | mitigate | `leave` view 판정으로 404와 항목 비노출을 서버가 한다. E2E가 권한 끈 계급의 404와 항목 부재를 단언 |
| T-04.1-35 | Tampering | 조정 일수 · 날짜 입력 | medium | mitigate | zod + 도메인 이중 검증(0.25 단위 · 0 거부 · 사유 필수 · 날짜 형식 · 퇴직일 ≥ 입사일 · (S9-FY) 연차 `fiscalYear` 정수 2000~올해 필수 · 월차 `fiscalYear` 있으면 거부 — 폼 값을 바꿔 미래 연도나 월차 연도를 보내도 서버가 막는다) |
| T-04.1-36 | Denial of Service | 미리보기 액션 호출 빈도 | low | accept | change 단위 호출(타이핑마다가 아님), 사용자 10~30명 — 디바운스를 두지 않는다(계획 가정 2) |
| T-04.1-48 | Tampering | 신청 두 번 누르기 → 같은 연차 문서 둘(결재자 결재함에 중복) | medium | mitigate | 1차 `pending` + 동기 ref로 제출 중 두 번째 누름·`Ctrl+Enter` 무시, E2E 두 사례가 신청 1건을 단언(CEO-12). 서버 멱등키는 두지 않는다 — 남은 위험(두 탭에서 따로 제출)은 기안자 회수로 정리되고 결재 없이 승인되지 않으므로 수용 |
| T-04.1-62 | Information Disclosure(오인) | 목록 머리 · 신청 창 잔고 | low | mitigate | 입사 다음 해 사용자의 연차·월차를 따로 그리고 합계를 그리지 않는다 — 두 E2E 사례가 합계 부재를 단언(사용자 요구 11:43) |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test:unit && pnpm test:integration` 통과
- `pnpm test:e2e:ci`(CI=true, desktop → mobile-375 → desktop-settings) 통과 — 기존 셸·사람 스펙 회귀 없음
- 새 E2E 세 파일에 네 자리 연도 리터럴이 없다(`cat test/e2e/leave-list.spec.ts test/e2e/mobile-leave-list.spec.ts test/e2e/admin-person-leave.spec.ts | grep -cE "\b20[0-9]{2}\b"`이 0 — CEO-15)
- 독립 DOM 감사(별도 에이전트, `CI=true`, 폭 1280 · 1024 · 375)가 `/leave` · `/leave/new` · `/admin/people/[id]` 연차 섹션에서 가로 스크롤 0 · 폰 행 높이 ≥ 44 · 행동 줄 고정을 실측, 위반 0을 SUMMARY 표로
- `git diff --stat "$PLAN_BASE"..HEAD -- ui/next-turn ui/button package.json pnpm-lock.yaml` 비어 있음
</verification>

<success_criteria>
- 직원이 계정 그룹에서 내 연차로 들어가 잔고와 상태를 한눈에 본다
- 신청 창이 연차 남음 · 월차 남음(따로) · 결재 중 · 이번 신청과 차감 예정 · 초과 경고, 결재선을 미리 보여 주고 초과를 막지 않는다 — 두 잔고의 합은 어디에도 없다(11:43)
- E2E가 실행하는 날과 무관하게 유효한 날짜로 돈다(Codex HIGH 06)
- 관리자가 입사일·퇴직일·조정으로 잔고를 바로잡고, 새 사람은 입사일과 함께 등록된다
- 신청을 두 번 눌러도 한 건이다(CEO-12) · 새해 첫 아홉 시간에도 기본 연도가 새해다(CEO-11)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 화면 | `/leave`(잔고 줄 · 연도 · 상태 그룹) · `/leave/new`(완성형) · `/admin/people/[id]` 연차 섹션 · 사람 등록 폼 입사일 |
| 셸 | 계정 그룹 「연차」(leave view 권한 조건) |
| 서버 액션 | `previewLeaveAction`(write · 첫 문장 `assertLeaveWrite`) · `setHireDateAction` · `setResignationDateAction` · `addLeaveAdjustmentAction`(+ `admin/people/[id]/actions.registry.ts`) |
| 함수 | (결재선 함수 없음 — 결재선 미리보기는 04.1-01 `previewRoute` → `RoutePreviewDTO`를 부르기만, CX-R3) · `earliestMyLeaveYear`(`domain/leave/index.ts`) + `findEarliestLeaveFiscalYear`(`repositories/leave-requests.ts` — `min(fiscal_year)` 한 번, C-04) · 테스트 헬퍼 `onStableSeoulDay`(`test/e2e/leave-dates.ts`, CX-R8) |
| 디자인 문서 | DECISIONS.md A1 · A4 · SYSTEM.md §6-0 · §6-1 · §6-3 · §7-8 개정 |
| 테스트 | `test/e2e/leave-list.spec.ts` · `test/e2e/mobile-leave-list.spec.ts` · `test/e2e/admin-person-leave.spec.ts` · `test/integration/leave-earliest-year.test.ts`(C-04) · 갱신 `test/e2e/people.spec.ts` · `test/e2e/mobile-people.spec.ts` · `test/e2e/admin-master-list-first.spec.ts` · `test/e2e/corp-cards.spec.ts` · `test/e2e/mobile-corp-cards.spec.ts` · `test/e2e/single-column.spec.ts`(입사일 한 줄씩 — C-01) · `test/e2e/mobile-shell.spec.ts`(계정 그룹 기대값을 실제 권한으로 — ENG-7) · `test/unit/ui/role-menu.test.ts` |
| 변경된 기존 심볼 | `buildAccountGroup`(viewer 인자) · 등록 zod(`hireDate` 필수) · `leave-form.tsx`(완성형 · 제출 중 `pending` + ref 이중 제출 방지, CEO-12) |
| 날짜 규약 | `/leave` 기본 연도 = `seoulToday()`의 연도(CEO-11) · 사람 상세 기본 섹션 연도 = 퇴직일이 있으면 min(퇴직 연도, `seoulToday()`의 연도), 없으면 `seoulToday()`의 연도(CXF2-C-F2-01 · CXF4-C-F4-01) · `?year=` 2000~올해는 그대로, 미래·형식 오류·범위 밖만 기본 연도로(D6 · ENG-8 — `resolveLeaveYear` 한 함수, `/leave`와 사람 상세 공용 — 사람 상세는 대체 연도 인자) · 사람 상세 연차 조정의 `fiscalYear` = 보고 있는 연도, 월차 조정은 연도 없음(S9-FY) |
| E2E 격리 | 잔고 단언 스펙은 전용 픽스처 사용자 · 연도는 `seoulToday()` 계산 · 신청 날짜는 `leaveWeekdayRange`(CEO-15 · Codex HIGH 06) · 입사 다음 해 전용 사용자(입사일 `{Y-1}-10-01`)로 두 잔고 분리 단언(11:43 — 연차는 `annualGrantQuarters` + `formatLeaveDays`로 계산한 비례값, R1/D5) · 입사일 = 오늘 전용 사용자로 월차 0 줄 단언(D4) |


## Review Dispositions Ledger

### Round 1 — 1b86370

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E2E 날짜 계산이 연말에 불가능(L187@1b86370) | HIGH | 헬퍼는 04.1-02 Task 1 ⑨(`leaveWeekdayRange` + 네 기준일 단위 테스트). 이 플랜 Task 1 · Task 2의 모든 신청 날짜가 헬퍼로 바뀌고(`week` 10~20), 날짜 리터럴 0건 수락 기준 |
| PC 메뉴 순서가 truth와 TopBar 합성 순서가 다름(L188@1b86370) | MEDIUM | truth · UI-SPEC S10을 SYSTEM.md §6-0 (a)와 TopBar 합성 순서(`관리 · 내 정보 · 연차 · 로그아웃`)로 맞춤 · Task 1 E2E가 관리자 · 비관리자 두 경우를 배열 전체로 단언 |
| `previewLeaveAction`도 registry 등록만으로 권한이 생기지 않음(L190@1b86370) | MEDIUM | Task 2 ②: 첫 문장 `assertLeaveWrite` · 등록 동작 `write` · 도메인 `previewLeaveBalance`도 04.1-02에서 같은 판정 · T-04.1-33 |
| 입사 다음 해 월차·연차가 합쳐 보이지 않게(사용자 요구 2026-09-24 11:43, 조정자 전달) | — | truths(목록 머리 · 신청 창) · Task 1(목록 머리 두 줄 분리 · 합계 부재 E2E) · Task 2(신청 창 `연차 남음` · `월차 남음` · 차감 예정 E2E) · T-04.1-62 · UI-SPEC Copywriting 잔고 행 · Assumptions #14 개정 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| UI에서만 입사일을 필수화 — 도메인 `registerPerson`은 선택값(L189@1b86370) | Partially addressed — 원자성 부분은 04.1-03 Task 1 ⑤(계정 생성 전 형식 검증 · 저장 실패 보관 보상)가 닫았다. 도메인 필수화는 하지 않는다: 입사일 없는 기존 계정이 있고(데이터 이전 없음 — 계획 가정 3), D-96 · UI-SPEC이 `월차 계산 불가 · 입사일 없음` 상태를 정의했으며, 계정은 `registerPerson`을 거치지 않고도 생긴다 — 계정 CLI(`scripts/account-cli.ts:87`)와 E2E 픽스처(`test/e2e/fixtures.ts` `createFixtureUser`)는 `createAccount`를 부르고 입사일을 모른다. 그래서 `registerPerson`만 필수화해도 입사일 없는 계정 상태는 없어지지 않는다(ENG-25 — 근거 정정, 결론 불변). 새 등록 화면·액션은 필수다 |

### Round 2 — eng review 2026-09-24(`docs/designs/plant8-erp-phase04.1-eng-review-260924.md`)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG-7 `mobile-shell.spec.ts:150`이 기대 계정 그룹을 `allowedMenus: []`로 만들어 `연차`가 든 실제 시트와 어긋남 | BLOCKER | Task 1 files · behavior · ④ · 수락 기준: 기대값을 layout과 같은 권한 계산으로, `leave` 키 단언. `user-menu.spec.ts`는 화면에서 읽어 같은 패턴이 아님을 확인 — 회귀 실행만 |
| ENG-8 연도 범위 규칙과 `?year={Y-1}` EMPTY E2E 모순 | BLOCKER | D6로 truth · 계획 가정 1 · Task 1 ③ · E2E(지난 연도 EMPTY · 미래/형식 오류/범위 밖 → 올해 · select 옵션) 통일 |
| R1/D5 입사 다음 해 연차 15일 가정 | 사용자 결정 | Task 1 · Task 2 입사 `{Y-1}-10-01` 사례의 연차 기대값을 비례 계산값 P로(수정 회차 2에서 계산 함수를 `annualGrantQuarters` + `formatLeaveDays`로 명시 — 아래) · 합계 금지 `남음 {P + N}일` · 입사일 없는 사용자는 15 유지 |
| D4 첫 1년 월차 0 줄 | MEDIUM | truths · Task 1 · Task 2에 입사일 = 오늘 사용자의 `월차 적립 0일` 줄 · `월차 남음 0일` E2E · Task 3 첫 1년 사용자 줄 존재 명시 |
| ENG-25 Deferred 근거 문장(계정 CLI는 `createAccount`) | LOW | 위 Round 1 Deferred 근거만 정정 — 결론 불변 |

### Round 3 — 수정 회차 2 교차 메모(2026-09-24)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 메모 9 — E2E의 입사 다음 해 연차 계산 함수가 04.1-03 export 이름과 어긋남(`buildLeaveGrants`만 적힘) | MEDIUM | Task 1 · Task 2 behavior · ④ · 수락 기준: P = `formatLeaveDays(annualGrantQuarters({fiscalYear, hireDate, resignationDate, annualDays}))`(annualDays = `LEAVE_ANNUAL_DAYS`의 `{Y}-01-01` 값) · 월차 줄·잔고 행은 기존 `buildLeaveGrants` 경로 · grep 수락 기준 교체 |
| 메모 10 — `addLeaveAdjustment`는 연차에 `fiscalYear`가 필요한데 S9에 연도 칸이 없음 | BLOCKER | 오케스트레이터 결정(S9-FY): 섹션이 보여 주는 연도를 넘긴다 · 연도 select 없음. truth · Task 3 ①~③ · behavior 두 줄(`?year={Y-1}` → `fiscal_year` {Y-1} · 월차 → null) · 수락 기준 · T-04.1-35 · 연도 해석은 Task 1 `resolveLeaveYear` 공용 |

### Round 4 — Codex 1회차 샤드 C(2026-09-25)

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CX-R2 조정 기록을 페이지가 `repositories/`로 읽음 · 투영 없음 | BLOCKER | 잠근 인터페이스: 04.1-03 `listLeaveAdjustmentsForUser` → `LeaveAdjustmentDTO[]`만(권한 = 섹션 읽기 — 오케스트레이터 결정: 보기만 있으면 잔고·기록만). truth · key_link · Task 3 read_first · ② · ③ · ⑤ · 보기만 계급 E2E(읽기 전용 표 · 추가 폼 없음) + 권한 없는 계급 음성 대조 · 수락 기준 grep · T-04.1-32 |
| CX-R3 `previewRoute`가 이름을 투영 없이 돌려줌 | BLOCKER | 잠근 인터페이스: 04.1-01이 `previewRoute` → `RoutePreviewDTO`를 소유 — 이 플랜은 정의를 빼고(`domain/approvals/index.ts`를 files에서 뺌) DTO만 그린다. truth · key_link · Task 2 ① · ② · ③ · 숨김 계급 E2E + 양성 대조 · 수락 기준 · T-04.1-33 |
| CX-R4 미리보기 응답 순서 | WARNING | 더 작은 수단: Next 16 서버 액션 순차 전송(문서 인용)이 응답 순서를 보장 — 가드 코드 없이 서버 액션 밖 병렬화 금지 · 응답을 쥐었다 푸는 E2E(지연 없음) · 계획 가정 2 |
| CX-R5 불가능한 선택지(보기만 사용자의 `연차 신청` · 월차 조정) | WARNING | `/leave` CTA · EMPTY 3차를 write 판정으로 · 조정 Select 기본 `연차` · `월차`는 `checkLeaveAdjustment` 거부 시 옵션에서 빠짐(CX2-03 — 공용 Select 불변) + 기존 거부 문구 한 줄 · 쓸 수 없게 된 `월차` 선택은 `연차`로 · E2E 넷 |
| CXF3-C-F3-01 퇴직 줄 표시 계약이 03과 다름 · 「두 달 뒤 퇴직」이 11·12월에 깨짐 | BLOCKER | 03 계약 그대로: DTO 퇴직 줄 재료가 있을 때(섹션 연도 = 퇴직 연도)만 퇴직 줄, 그 밖은 그 연도 보통 잔고 · E2E를 올해 퇴직 `{Y}-12-28` · 다음 해 퇴직 `{Y+1}-03-31` · 지난해 퇴직의 앞뒤 연도 사례로 |
| CXF3-C-F3-02 퇴직자의 `올해 보기`가 제자리 | WARNING | 링크가 `?year={올해}`를 명시 · 지난해 퇴직자 링크 이동 뒤 부제·잔고·조정 연도 = 올해 E2E |
| CXF2-C-F2-01 퇴직자의 보이는 잔고 연도와 조정 연도가 어긋남 | BLOCKER | 섹션 연도를 서버가 한 번 정함(`?year` 유효 → 그 연도 · 퇴직자 → min(퇴직 연도, 올해) · 그 밖 올해 — `resolveLeaveYear` 대체 연도 인자) · 잔고 · 기록 · 조정 `fiscalYear` · 부제가 같은 값 · 퇴직자 `?year` 없음 E2E |
| CXF2-C-F2-02 날짜·조정 액션 뒤 재렌더 없음 | WARNING | 세 액션 `revalidatePath` — 기존 `changePersonRoleAction` 모양 · grep · 새로 고침 없는 E2E 단언 |
| CXF2-C-F2-03 순서 E2E가 병렬 구현을 못 잡음 | WARNING | 응답을 쥔 동안 둘째 요청이 없음을 먼저 단언(요청 수 1) → 풀고 마지막 입력 힌트 |
| CXF-C-F05 Task 1 수락 기준이 Task 3의 스펙 파일을 먼저 요구 | WARNING | Task 1 `onStableSeoulDay` grep을 Task 1 스펙 둘로 줄이고 `admin-person-leave.spec.ts` grep은 Task 3 수락 기준으로 |
| CXF-C-F06 보기만 계급 E2E가 `leave.value` 노출을 명시하지 않음 | WARNING | Task 3 보기만 계급은 `leave.value` 노출 행을 명시로 켜고, 노출 없는 계급 대조(사유 부재)를 더함 · Task 1 보기만 계급은 노출에 기대지 않음을 명시 |
| CX2-02 미리보기 E2E가 `role.value` 없이 단계 이름을 기대 | BLOCKER | 계급 A는 스펙이 `role.value` 노출 행을 명시로 만든다(`approval.value`만 숨김) · 계급 B(둘 다 숨김) → 04.1-01 대체 이름 `1단 → 2단 …`, 이름·계급 이름·건너뜀 없음 E2E · truth · Task 2 ③ |
| CX-R6 지난 연도 정정이 URL 손 편집 | WARNING | S9-FY(연도 select 없음)를 지키며 `잔고` 옆 링크 `{Y-1} 회계연도` · `올해 보기`(기존 S1 문구) · 지난해 정정 E2E가 링크로 연다 |
| CX-R8 스펙·서버의 독립 벽시계 | WARNING | `onStableSeoulDay`(주입 날짜 단위 다섯 사례) — 사례 중 서울 날짜가 바뀌어 실패하면 새 날짜로 한 번만 다시. 운영 코드에 시계 입구 없음 · 경계 계산은 04.1-03 단위 |
| CX-R9 사람 상세 섹션 로딩·오류 약속이 구현 없음 | WARNING | truth를 UI-SPEC loading S9 행에 맞춤(서버 렌더 · 기존 `app/(app)/error.tsx`) · 섹션 전용 경계 없음 수락 기준 · `/leave` 쪽 파일 확인 |

### Round 5 — 4078bd8

eng review 2회차(`docs/designs/plant8-erp-phase04.1-eng-review2-260925.md` — 증거 `docs/designs/phase04.1-eng-review2-260925/eng-C.md` · `judge.md`). 이 플랜 몫: T1 · T4 · T12 · T13 · T16 · T17(VALIDATION) + C-05의 06 부분 + P3 일부.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| C-01 입사일 필수화가 등록 폼을 제출하는 기존 E2E 넷(`admin-master-list-first:108` · `corp-cards:27` · `mobile-corp-cards:34` · `single-column:104`)을 깨는데 read_first grep(`people?new=1`, 0건)이 못 찾음(L19@4078bd8) | BLOCKER | Plan 06, Task 3: read_first grep → `grep -rln 'getByRole("button", { name: "사람 등록" }).click' test/e2e`(제출 버튼 셀렉터 — 지시문의 `name: "사람 등록" }).click`도 같은 여섯 파일을 내지만 링크 클릭까지 잡아 제출 단계로 좁혔다. 실측 여섯 = people · mobile-people + 넷, 링크만 누르는 `mobile-admin-master-list-first` · `single-column` 등록 폼 루프는 해당 없음) · 네 스펙을 frontmatter `files_modified` · Task 3 `<files>` · ④(입사일 한 줄씩) · 셋째 검증 명령 · behavior · 수락 기준(여섯 파일 전부 입사일 한 줄 · 네 스펙 diff가 한 줄씩 — 현재 트리에서 RED 확인) · truth에 더함 |
| C-N01 = C-03 섹션 연도에 필요한 퇴직일의 출처가 연도를 먼저 요구(L25@4078bd8) | WARNING | Task 3 ③ 읽기 순서 고정(지시 기본안): 첫 읽기 `getLeaveBalanceForUser(…, {fiscalYear: 올해})` → DTO `hireDate` · `resignationDate`로 대체 연도 → `resolveLeaveYear` 한 번 → 섹션 연도 ≠ 올해일 때만 둘째 읽기(최대 2회) → 조정 목록. 입력 칸 초기값 · `monthlyBlockedReason`도 첫 읽기 값 · `leave.value` 숨김이면 대체 연도 올해 · read_first `PersonDto`(넓히지 않음) · behavior 태그 · 수락 기준(`getLeaveBalanceForUser(` 1~2 · `domain/people` diff 없음 · 퇴직자 기본 연도 E2E). 전제인 03 계약(「퇴직 연도가 아닌 해로 읽어도 두 날짜를 싣는다」)은 03 소유자가 같은 회차에 04.1-03 ⑤ C-N01 문장 · 퇴직자 연도별 통합 사례의 `resignationDate` 단언으로 명시했다 — 대조 기록 `docs/designs/phase04.1-eng-review2-260925/cross-notes-C.md` |
| C-02 공용 `ui/select`는 늘 빈 `—` 옵션을 먼저 그림(L38@4078bd8) | P2 | Task 3 behavior · ① · ② · 수락 기준: 계약을 「`—`를 뺀 옵션이 `연차` 하나」(옵션 글자 배열 `["—", "연차"]`)로 · 처음 선택값 = 제어값 `annual` · 네이티브 `required` 없음 · `—` 선택 제출은 서버 zod가 `잔고 비어 있음 · 잔고 고르기`(SYSTEM.md §7-15 틀)로 거부 · 조정 행 수 불변 E2E. 공용 Select 불변 결정은 그대로 |
| C-04 「가장 이른 신청 연도」 도메인 읽기가 files 밖(L39@4078bd8) · C-P1 해마다 조회(L80@4078bd8) | P2 | Task 1 ③: 새 `earliestMyLeaveYear(viewer, deps?)`(`domain/leave/index.ts`) → `findEarliestLeaveFiscalYear`(`repositories/leave-requests.ts`, `min(fiscal_year)` 한 번 — `leave_requests.fiscal_year` 열이 01에 있어 `extract(year …)`보다 단순). `listMyLeave` DTO(01 소유)는 바꾸지 않는다 · files_modified · 새 통합 테스트 `leave-earliest-year.test.ts`(null · 이른 쪽 · 남의 신청 제외 · 조회 1회) + 넷째 검증 명령 · 수락 기준(`listMyLeave(` 1회 · `min(`) · truth · 계획 가정 1 |
| C-07 E2E 픽스처 이름이 전부 `E2E Employee`라 이름 누수 단언이 판별력 없음(L42@4078bd8) | P2 | Task 2 behavior(계급 A · 계급 B · 양성 대조) · ⑤ · 수락 기준: 사례별 기안자 · 전용 팀장을 도메인 `registerPerson(SYSTEM_VIEWER, …)` 고유 이름으로 만들고 단언 범위를 신청 폼 영역으로(셸 상단 바 제외). `test/e2e/fixtures.ts` 불변 |
| C-05(06 부분) `PLAN_BASE`가 Bash 호출 사이에 남지 않음 — 빈 값이면 `git diff ..HEAD`가 오류 없이 빈 diff(L40@4078bd8) | P2 | 실행 노트: `.git/gsd-04.1-06.env`에 쓰고 `$PLAN_BASE`를 쓰는 검사는 전부 `. <파일> && test -n "$PLAN_BASE" &&`로 시작(실측 — 빈 값 rc 0 · 빈 출력). Task 1 role-menu 수락 기준의 「git이 실패하면 멈춘다」 문장이 사실과 달라 가드를 붙여 정정 |
| GAP-1 VALIDATION에 06 행 누락(L43@4078bd8) | P2 | `04.1-VALIDATION.md` Per-Task 표에 행 추가(CEO-12 두 번 제출 · 등록 입사일 필수 · 기존 등록 스펙 여섯 · 퇴직자 기본 섹션 연도 · CX2-02 · A1 role-menu · PC 메뉴 배열 · C-04 · C-02) — `/gsd-validate-phase` 위임 대신 행을 채움 |
| C-11 `aria-disabled` 부재 단언이 공허(L74@4078bd8) | P3 | Task 2 수락 기준: `toBeEnabled()` 단언으로(한 문장 정정) |
| C-12 zod 연도 상한이 모듈 로드 때 굳을 수 있음(L75@4078bd8) | P3 | Task 3 ①: 상한은 호출 때 `.refine`로 계산(한 문장) |
| C-13 네이티브 `required`가 서버 zod 문구를 가림(L76@4078bd8) | P3 | Task 3 ④ · behavior · 수락 기준: 입사일 칸 · 잔고 Select에 `required` 없음(SYSTEM.md §7-15 근거) |
| C-14 월차 조정이 「적립」에 더해지는지는 03의 문자열 계약(L77@4078bd8) | P3 | Task 3 behavior: 기대값을 `formatBalanceLines`로 계산해 단언(한 문장) |
| C-GAP-3 계정 그룹 증가로 `keyboard-nav` · `admin-nav` 회귀가 06 검증에 없음(L78@4078bd8) | P3 | Task 1 셋째 검증 명령에 두 스펙 추가 · fails_when |
| C-P2 제출이 대기 중 미리보기 뒤에 줄을 섬(L81@4078bd8) | P3 | Task 2 ③: ref · `pending`은 누른 즉시 켠다(한 문장) |
| (교차 메모 B) B-C3 오류 경계 재시도 인자가 `reset`으로 적힘 — Next 16.3 표준은 `retry`(L59@4078bd8) | P3 | Task 1 ③: `/leave` `error.tsx`가 `retry`를 부른다(04.1-05와 같은 모양 · 문서 경로) · 수락 기준 grep 한 줄. 04.1-02의 자동 재시도 금지 grep은 `--exclude=error.tsx`라 부딪치지 않는다 |
| (교차 메모 B) B-A1 액션 반환값이 투영을 우회 — 06이 토스트를 다시 조립하면 받은 필드만으로(L28@4078bd8) | WARNING | Task 2 ③: 신청 · 다시 신청 토스트는 투영된 결과 필드만으로 조립, 빠진 조각은 뺀다(한 문장 — 반환 투영 자체와 그 통합 사례는 04.1-02 ④) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| C-09 `ui/table`에 행 전체 링크 기능이 없음(L72@4078bd8) | 한 문장 사실 정정이 아니다 — 「행 전체가 `/leave/[id]` 링크」는 UI-SPEC S1 truth라 한 칸 링크 + 폰 `onRowTap`(기존 선례)로 바꿀지 `ui/table`을 넓힐지는 화면 결정이다. `/plan-design-review 04.1`에서 정한다 |
| C-GAP-4 `/leave` loading/error가 grep 수락 기준뿐(L79@4078bd8) | 새 렌더 E2E가 필요한 P3 — 이번 회차 강제 아님. grep 수락 기준 · 04.1-05의 같은 모양 파일로 유지 |
| C-P3 새 E2E로 desktop 실행 시간 증가(L82@4078bd8) | P3 · confidence 4 — 조치 없음. 07 전체 게이트 결과에 시간이 드러난다 |
| C-P4 레이아웃 메뉴 권한 조회 1회 증가(L83@4078bd8) | P3 · confidence 4 — 기존 주석(10~30명 규모 개별 호출 수용) 범위 안, 조치 없음 |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-06-SUMMARY.md` when done
</output>
