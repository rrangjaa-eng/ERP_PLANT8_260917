---
phase: 04.1-approvals-leave
plan: 07
type: execute
wave: 7
depends_on: ["04.1-04", "04.1-06"]
files_modified:
  - db/migrations/meta/_journal.json
  - docs/ARCHITECTURE.md
  - TODOS.md
autonomous: true
requirements: [EXP-03, EXP-04, EXP-05, LEAV-01, ADMN-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "병합 직전에 04.1이 만든 마이그레이션(SQL · 스냅숏 · journal 항목)을 지우고 `origin/main`을 병합한 뒤 `pnpm db:generate`로 main의 마지막 마이그레이션 **뒤에** 04.1 마이그레이션을 다시 만든다 — 파일 이름·`_journal.json`을 손으로 고치지 않는다(오케스트레이터 결정 7a·7c)"
    - "조정자가 main 기준 최종 번호 0017~0020을 04.1에 예약했다(Phase 4 = 0011~0016 · 04.2 = 0021~0024 · 04.3 = 0025~0029) — 04.1은 마이그레이션을 최대 네 개 만든다. 브랜치에서 만든 임시 번호(0011·0012일 수 있음)는 재생성으로 main 마지막 항목 뒤 번호가 되고, 최종 tag와 그것이 0017~0020 안인지가 SUMMARY에 보고된다. 다른 병렬 페이즈가 먼저 병합돼 번호가 0017~0020 밖이 되면 그것은 예상된 결과로 보고만 하고 손으로 이름을 바꾸지 않는다"
    - "재생성 뒤 `_journal.json`은 idx가 빈틈없고 tag가 idx로 시작하며 `when`이 엄격히 증가한다 — 04.1 항목의 `when`이 main의 마지막 적용 시각보다 늦어 drizzle `migrate()`가 조용히 건너뛰지 않는다(건너뛰기 판정은 `drizzle-orm/pg-core/dialect.js:57-62`에 있다 — `scripts/migrate-runner.ts`는 `migrate()`를 부르기만 한다)"
    - "병합된 트리에서 빈 DB부터 전 마이그레이션이 적용되고, 연차 한 건이 신청 → 결재 → 최종 승인까지 간다(병합 트레이서)"
    - "병합된 트리에서 `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `pnpm build` · 단위 · 통합 · `CI=true` E2E 전체가 녹색이다 — 페이즈 완료 판정은 로컬 dev가 아니라 `CI=true`다(CLAUDE.md)"
    - "Phase 4와 공유하는 덧붙이기 전용 파일(`domain/permissions/menus.ts` · `domain/permissions/info-items.ts` · `domain/settings/keys.ts` · `test/integration/leak-scan.test.ts` · SYSTEM.md · DECISIONS.md)의 병합 충돌은 양쪽을 모두 남기는 것으로만 푼다. 의미 판단이 필요한 충돌은 추측하지 않고 멈춰 보고한다"
    - "이 페이즈는 금지 파일(`domain/rules/register.ts` · `ui/next-turn/*` · 견적 줄·프로젝트 상태·차수·계약 파일)을 병합 기준점 대비 한 줄도 바꾸지 않았다(오케스트레이터 결정 9)"
    - "병합 시작 판정은 태그 하나가 아니라 Phase 4 **완료**다 — `origin/main`의 ROADMAP이 Phase 4를 `[x]`로 표시하고, Phase 4 SUMMARY들이 적은 마지막 마이그레이션 tag가 `origin/main`의 `_journal.json`에 있다. 판정 명령이 그 tag를 출력하고 SUMMARY에 옮긴다(CEO-19)"
    - "「빈 DB부터」 증명은 실제로 빈 DB에서 한다 — `pnpm db:reset:test`로 `erp_test`를 지우고 다시 만든 뒤 적용하고, 옛 번호가 적용된 개발 DB `erp`는 psql DROP/CREATE 뒤 다시 적용한다. `scripts/dev-db.sh`는 없는 DB만 만든다(CEO-16)"
    - "`docs/ARCHITECTURE.md`에 병합된 트리의 마지막 §4-N 다음 번호로 「결재 모듈 계약(Phase 04.1 → Phase 5)」 절이 20줄 이하로 있다 — 종류 등록 계약 · 고정 결재선/담당 재해석 · 전이 순서 · 잠근 트랜잭션 밖 선읽기 · 결재함 노출 규칙 · 무결재 승인 없음이 Phase 5가 읽을 한 곳에 있고, docs-limits 테스트가 녹색이다(CEO-21)"
    - "04.1-07 SUMMARY에 공개 전 점검표(전 재직자 입사일 입력 · 04.2 알림 전까지 결재자는 매일 결재함 확인 · 새 계급을 만들면 정보 노출표에서 결재·연차 정보 켜기)와 미결 항목 「공개 시점(바로 vs 04.2 알림 뒤)은 배포 때 사용자 결정」이 있다(CEO-20 — 기본값 A)"
    - "새 계급의 결재·연차 정보 노출 한계가 SUMMARY의 알려진 한계와 `TODOS.md` 항목 하나로 남는다 — 04.1-01 T4의 한계 기록 테스트가 근거다(CEO-10 — 기본값 A, `domain/permissions/roles.ts` 불변)"
    - "SUMMARY 운영 메모에 `approval.route_blocked`(warn)은 대표 계급·조직을 고치면 풀리고 기안자는 회수할 수 있다는 것, `approval.refused`(info)의 사유 코드별 의미가 한 문단으로 있다(CEO-18)"
    - "Post-build 순서에 `/design-review`와 나란히 Codex 적대적 디자인 검토(`codex exec`)가 있다 — 실행할 수 없으면 그 사실을 보고하고 다른 검토자로 대체하지 않는다(CEO-23)"
  prohibitions:
    - statement: "Phase 4가 main에서 완료되기 전(ROADMAP `[x]`와 마지막 Phase 4 tag가 main journal에 있기 전)에 04.1을 먼저 병합해서는 안 된다 — 그러면 04.1이 Phase 4의 번호를 차지하고 Phase 4가 재생성해야 한다. 이 순서 결정은 사용자의 것이다(CEO-19)"
      status: resolved
      verification: test
    - statement: "`git push`·`git push --force`를 하지 않는다 — PR 생성·병합은 Post-build `/ship`의 몫이다(CLAUDE.md)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/migrations/meta/_journal.json"
      provides: "main 항목 + 04.1 재생성 항목 하나, 가드 테스트 녹색"
      contains: "entries"
    - path: "docs/ARCHITECTURE.md"
      provides: "§4-N 결재 모듈 계약(Phase 04.1 → Phase 5) 20줄 이하(CEO-21)"
      contains: "registerDocumentKind"
    - path: "TODOS.md"
      provides: "새 계급 정보 노출 켜기 항목(CEO-10)"
      contains: "정보 노출표"
  key_links:
    - from: "db/migrations/meta/_journal.json"
      to: "test/unit/db/migration-journal.test.ts"
      via: "재생성 결과가 idx · tag · when 불변식을 지킨다"
      pattern: "idx"
    - from: "docs/ARCHITECTURE.md"
      to: "test/unit/docs-limits.test.ts"
      via: "§4-N 추가 뒤에도 문서 줄 수 상한 안(CEO-21)"
      pattern: "결재 모듈 계약"
---

<objective>
**As a** 이 저장소를 병합하는 사람, **I want to** 04.1 브랜치가 Phase 4가 먼저 들어간 main 위에서 마이그레이션 번호·순서 충돌 없이 그대로 돌아가는 상태로 끝나기를, **so that** 운영 DB에 적용할 때 04.1 마이그레이션이 조용히 건너뛰어지거나 번호가 겹치는 사고가 없고, Post-build(`/review` → `/qa` → `/cso` → `/ship`)가 병합 가능한 트리에서 시작한다.

Task 1은 이 플랜의 **트레이서**다 — main 병합 → 마이그레이션 재생성 → 빈 DB 적용 → 연차 한 건 끝까지를 병합된 트리에서 한 번 관통한다. Task 2는 페이즈 전체 게이트(`CI=true` 전체 · 독립 DOM 감사 · 금지 파일 확인 · 미룬 항목 기록)다.

Purpose: 오케스트레이터 결정 7c(병합 직전 재생성)와 RESEARCH Pitfall 1(병렬 브랜치 마이그레이션 번호·스냅숏 충돌)을 닫는다.

Output: main 위에 재생성된 04.1 마이그레이션 하나, 병합 커밋, 전체 게이트 결과와 미룬 항목 목록(SUMMARY).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md
@.planning/phases/04.1-approvals-leave/04.1-06-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 시작 전에 Superpowers `verification-before-completion` 스킬을 **호출**한다 — 이 플랜의 모든 판정은 명령의 실제 출력으로 한다. 병합 충돌·테스트 실패가 나면 원인을 쫓기 전에 `systematic-debugging`을 호출한다. 새 코드를 써야 하는 수정이 생기면 먼저 `test-driven-development`를 호출한다. 서브에이전트(독립 DOM 감사)에게도 세 스킬 이름과 `CI=true`를 적는다.
- `git push`를 하지 않는다. 병합 커밋까지만 만든다.
- **번호 예약**: 조정자가 main 기준 최종 번호 **0017~0020**을 04.1에 예약했다(Phase 4 = 0011~0016 · 04.2 = 0021~0024 · 04.3 = 0025~0029). 04.1은 마이그레이션을 최대 네 개 만든다 — 지금 계획은 둘(04.1-01 · 04.1-03)이고 Task 1이 그 둘을 하나로 재생성한다.
- 병렬 머지 시 drizzle _journal.json 재번호 필요
- **브랜치 번호와 최종 번호 맞추기**: 이 플랜 전에 브랜치에서 만든 파일은 브랜치 journal 기준으로 `pnpm db:generate`가 준 번호를 달고 있다(main이 0010에서 끝났을 때 만들었으므로 0011·0012일 수 있다) — 이 번호는 임시다. Task 1이 그 파일을 지우고 main의 마지막 항목 뒤로 재생성한다 — Phase 4의 0011~0016이 main에 있고 04.2·04.3이 아직 병합되지 않았으면 0017이 된다. 다른 병렬 페이즈(04.2 · 04.3)가 먼저 병합됐으면 재생성이 04.1을 main 마지막 항목 뒤(0017~0020 밖)로 옮기는데, 이는 예상된 결과이고 SUMMARY에 보고한다 — 손으로 이름을 바꾸지 않는다. 파일 이름을 손으로 바꾸거나 `_journal.json`을 손으로 고치는 일은 계속 금지다(생성물을 이력에서 복원하는 Task 1 ②는 손 편집이 아니다).
- 첫 task를 시작하기 전에 `git fetch origin main` 뒤 `git merge-base HEAD origin/main` 값을 `PHASE_BASE`로 적어 둔다(SUMMARY 머리에도). diff 기준 검사는 전부 `"$PHASE_BASE"`부터다.
- **빈 DB 만들기(CEO-16)**: `scripts/dev-db.sh`는 없는 DB만 만들고 있는 DB를 비우지 않는다. 테스트 DB는 `pnpm db:reset:test`(erp_test DROP/CREATE), 개발 DB는 `PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp WITH (FORCE)' -c 'CREATE DATABASE erp OWNER erp' && pnpm db:migrate`(`scripts/reset-test-db.sh`와 같은 모양 — 새 스크립트를 만들지 않는다). 옛 04.1 번호가 적용된 DB에 재생성 파일을 증분 적용하면 drizzle이 Phase 4 항목을 조용히 건너뛰거나 「already exists」로 실패한다.
- **ARCHITECTURE·TODOS는 병합 뒤에 쓴다**: Phase 4 04-32가 `docs/ARCHITECTURE.md`에 §4-8을 더하므로, 결재 모듈 계약 절(CEO-21)과 `TODOS.md` 항목(CEO-10)은 Task 1의 병합이 끝난 트리에서 Task 2가 더한다 — 병합 충돌과 번호 겹침을 피한다.
</execution_notes>

<probe_fallback>
이 플랜은 새 동작을 만들지 않는다 — 04.1-01~06의 truths가 병합된 트리에서도 참인지 다시 증명한다.

**Deferred(이 페이즈 밖 — 두 묶음으로 나눠 SUMMARY에 그대로 옮긴다)**

(a) 범위 밖으로 지정된 것(coordinator brief, 사용자가 확인한 범위) — 정확히 둘:
- 지출결의 화면·문서 종류(결의서 작성 · 결재 옆판의 지출 행) — Phase 5. 04.1의 `registerDocumentKind` · `routeSettings` · `loadDetail`에 종류 하나를 더하는 일이다
- 회사 대납 세율 문제(코드 기본 8.8% 대 실무 22% gross-up; 경품 22%는 사용자 확인) — Phase 5/11

(b) 다른 페이즈 범위로 이미 정해진 것(ROADMAP이 근거 — 줄인 범위가 아니다):
- ADMN-04 알림 시점·대상(결재 요청 · 반려 · 최종 승인 통지 포함) — Phase 7/04.2. 근거: ROADMAP 04.1 기준 2가 ADMN-04를 결재 단계·담당 계급·조직 범위·규칙 on/off로만 적는다(PR #49, 사용자가 병합) · REQUIREMENTS 추적표 ADMN-04 행이 이미 나눠 적었다
- ADMN-04 세율·수식 설정 — Phase 5. 근거: 위와 같다(ROADMAP 04.1 기준 2 · REQUIREMENTS 추적표)
- 첫 화면 「내 차례」 블록의 `[결재]` 행 — Phase 4 병합 뒤. 근거: ROADMAP 04.1 「Depends on」이 Phase 4 파일을 건드리지 못하게 한다(`ui/next-turn` · 첫 화면은 04-08/04-09가 바꾼다)
- 공휴일 제외 일수 계산 — Phase 04.2 공휴일 표(ADMN-11) 뒤. 근거: 공휴일 표는 PR #50으로 04.2로 옮겨졌다. 그때까지 일수는 평일만 세고 힌트가 그 일수를 보여 준다(UI-SPEC Assumptions #4)

ADMN-04는 이 페이즈에서 **결재 부분만(partially covered — 결재 부분)** 덮인다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 병합 트레이서 — origin/main 병합 → 04.1 마이그레이션 재생성 → 빈 DB 적용 → 연차 한 건 끝까지</name>
  <precondition>`git fetch origin main` 뒤 Phase 4가 main에서 **완료**됐다 — `origin/main`의 ROADMAP이 `- [x] **Phase 4:`이고, `origin/main`의 Phase 4 SUMMARY들이 적은 마이그레이션 tag 중 가장 큰 것이 `origin/main`의 `_journal.json`에 있다(첫 검증 명령이 그 tag를 출력한다 — CEO-19). 아니면 이 task를 시작하지 않고 멈춰 「04.1과 Phase 4 중 어느 쪽을 먼저 병합할지」를 사용자 결정으로 넘긴다(오케스트레이터 결정 7c)</precondition>
  <reversibility rating="costly">04.1 마이그레이션 번호가 바뀐다 — 옛 번호로 이미 적용한 로컬 DB는 다시 만들어야 한다: `erp_test`는 `pnpm db:reset:test`, 개발 DB `erp`는 execution_notes의 psql DROP/CREATE 한 줄 뒤 `pnpm db:migrate`(`bash scripts/dev-db.sh`는 있는 DB를 지우지 않으므로 이 용도로 쓸 수 없다 — CEO-16). 운영·스테이징에는 아직 적용되지 않았으므로 되돌릴 수 없는 결정은 아니다</reversibility>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-RESEARCH.md` 286–300행(Pitfall 1 — 재생성 절차와 경고 신호)
    - `node_modules/drizzle-orm/pg-core/dialect.js` 50–70행 — `when`이 오래된 항목을 건너뛰는 판정(`scripts/migrate-runner.ts`는 `migrate()`를 부르기만 한다 — 판정은 여기 없다)
    - `scripts/reset-test-db.sh` · `scripts/dev-db.sh` 77–86행(없는 DB만 만든다 — CEO-16)
    - `git show origin/main:.planning/ROADMAP.md`의 Phase 4 줄 · `git ls-tree -r --name-only origin/main -- .planning/phases/04-project-quote-ledger`의 SUMMARY 목록(CEO-19)
    - `test/unit/db/migration-journal.test.ts`(04.1-01)
    - `.planning/phases/04.1-approvals-leave/04.1-PATTERNS.md`의 병합 위험 표 — 파일별로 어느 쪽이 무엇을 덧붙였는지
    - 04.1-01 · 04.1-03 SUMMARY — 04.1이 만든 마이그레이션 파일 이름 두 개
  </read_first>
  <action>
① `PHASE_BASE` 기준으로 04.1이 **추가한** 마이그레이션 파일을 찾는다(`git diff --name-only --diff-filter=A "$PHASE_BASE"..HEAD -- db/migrations` — SQL 둘 · 스냅숏 둘). SUMMARY에 목록을 적는다.

② 커밋 하나로 그 파일들을 지우고 `db/migrations/meta/_journal.json`을 `PHASE_BASE` 시점 내용으로 되돌린다(`git show "$PHASE_BASE":db/migrations/meta/_journal.json`의 출력으로 파일을 덮는다 — 생성물을 이력에서 복원하는 것이지 손 편집이 아니다). 커밋 제목 `chore: drop 04.1 migrations before regenerating on main`, 본문 한국어로 이유 한 줄.

③ `git merge origin/main`. 충돌은 이 순서로 푼다 — (a) 덧붙이기 전용 파일(truth 다섯째 목록)은 양쪽 덧붙임을 모두 남긴다 · (b) `ui/confirm-dialog/*`가 양쪽에 있으면 main 쪽을 채택하고 04.1 사용처를 main의 prop 이름에 맞춘다 · (c) `app/(app)/admin/settings/settings-form-client.tsx` · `app/(app)/admin/people/actions.ts` · `domain/seed/index.ts`는 양쪽 변경을 합치되 04.1 쪽 변경은 04.1 플랜들이 적은 최소 diff(옵션 렌더 · 경고 한 줄 · hireDate 한 칸 · 시드 한 줄)만 다시 얹는다 · (d) 그 밖에 한쪽 의미를 골라야 하는 충돌이 나오면 **추측하지 않고** 병합을 중단(`git merge --abort`)하고 충돌 파일과 양쪽 의도를 SUMMARY에 적어 멈춘다.

④ 병합된 트리에서 `pnpm db:generate` — main의 마지막 번호 다음 번호로 04.1 마이그레이션 **하나**가 생긴다(04.1 스키마 변경 전체가 한 파일). 생긴 tag(예: `0017_…`)를 SUMMARY에 적고, 그 번호가 예약 범위 0017~0020 안인지 한 줄로 판정한다 — 밖이면(04.2·04.3이 먼저 병합되거나 Phase 4가 계획보다 많은 마이그레이션으로 끝남) 그 사실과 main의 마지막 tag를 적는다. 번호가 밖이어도 손으로 고치지 않는다. 빈 DB 증명은 **실제로 빈** `erp_test`에서 한다: `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`(CEO-16). 개발 DB `erp`는 execution_notes의 DROP/CREATE 뒤 `pnpm db:migrate`. 이어 `pnpm lint:sql` · journal 가드 테스트. 커밋 제목 `chore: regenerate 04.1 migration on top of main`.

⑤ 병합 트레이서: `pnpm db:reset:test` 뒤 결재선 고정 통합 테스트와 PC 신청→승인 E2E를 병합된 트리에서 돌린다(통합 global-setup이 빈 `erp_test`에 전부 다시 적용 — CEO-16).
  </action>
  <verify>
    <automated>git fetch origin main &amp;&amp; R=$(git show origin/main:.planning/ROADMAP.md) &amp;&amp; printf '%s\n' "$R" | grep -qE '^- \[x\] \*\*Phase 4:' &amp;&amp; L=$(git ls-tree -r --name-only origin/main -- .planning/phases/04-project-quote-ledger) &amp;&amp; S=$(for f in $(printf '%s\n' "$L" | grep -- '-SUMMARY\.md$'); do git show "origin/main:$f" || exit 1; done) &amp;&amp; T=$(printf '%s\n' "$S" | grep -oE '\b0[0-9]{3}_[a-z0-9_]+' | sort -u | tail -1) &amp;&amp; test -n "$T" &amp;&amp; J=$(git show origin/main:db/migrations/meta/_journal.json) &amp;&amp; printf '%s' "$J" | grep -q "\"tag\": \"$T\"" &amp;&amp; echo "Phase 4 last migration tag on main: $T"</automated>
    <fails_when>종료 코드가 0이 아니거나 마지막 줄 `Phase 4 last migration tag on main: …`이 없다 — git이 origin/main을 읽지 못했거나, main ROADMAP이 Phase 4를 완료로 표시하지 않았거나, Phase 4 SUMMARY가 적은 마지막 tag가 main journal에 없다(CEO-19). precondition 불충족이므로 멈추고 출력(찾은 tag 또는 빈 값)을 붙여 사용자에게 병합 순서를 묻는다</fails_when>
    <automated>pnpm db:generate 2&gt;&amp;1 | tee /dev/stderr | grep -q "No schema changes" &amp;&amp; bash scripts/dev-db.sh &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>재생성 뒤 스키마와 마이그레이션이 어긋나 "No schema changes"가 없거나, 방금 비운 `erp_test`에 전 마이그레이션 적용이 실패하거나(CEO-16), squawk · journal 가드 중 하나라도 실패한다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/approvals-route-fixed.test.ts &amp;&amp; pnpm playwright test test/e2e/leave-approval.spec.ts --project desktop</automated>
    <fails_when>병합된 트리의 빈 `erp_test`에서 트레이서 통합 테스트 또는 PC 신청→승인 E2E가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=A origin/main..HEAD -- db/migrations`(병합 뒤 main 트리 대비 — `PHASE_BASE` 기준이면 병합으로 들어온 Phase 4 파일까지 잡힌다)에 04.1 SQL이 정확히 하나이고, 그 번호가 main의 마지막 번호 + 1이다
    - SUMMARY에 첫 검증 명령이 출력한 Phase 4 마지막 tag(CEO-19), 재생성된 04.1 최종 tag와 main의 마지막 tag가 있고, 최종 번호가 예약 범위 0017~0020 안인지 「안」/「밖 — 이유」 한 줄 판정이 있다
    - `db/migrations/meta/_journal.json`의 04.1 항목 `when`이 main 마지막 항목의 `when`보다 크다(journal 가드가 증명)
    - 병합 커밋이 있고 `git log origin/main..HEAD --merges`가 1줄 이상이다
    - `git status --porcelain`이 비어 있다(충돌 흔적 · 생성 부산물 없음)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>04.1이 Phase 4가 들어간 main 위에서 마이그레이션 하나로 재생성되어 빈 DB에 적용되고, 연차 한 건이 병합된 트리에서 끝까지 간다</done>
</task>

<task type="auto">
  <name>Task 2: 페이즈 전체 게이트 — 결재 모듈 계약 문서화 · `CI=true` 전체 · 독립 DOM 감사 · 금지 파일 확인 · 미룬 항목·공개 점검표 기록</name>
  <files>db/migrations/meta/_journal.json, docs/ARCHITECTURE.md, TODOS.md</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-VALIDATION.md`(요구사항 → 테스트 표 · Manual-Only)
    - `.planning/ROADMAP.md` Phase 04.1 성공 기준 1~5
    - 04.1-01~06 SUMMARY — 각 플랜의 독립 DOM 감사 표와 계획 가정 · 04.1-01 T4의 새 계급 한계 기록 테스트 이름
    - 병합된 트리의 `docs/ARCHITECTURE.md` — `## 4-N.` 절 목록(`grep -n "^## " docs/ARCHITECTURE.md`)과 §4-2 · §4-6(계약 절 문체 선례), Phase 4가 더한 §4-8(잠근 트랜잭션 규약) · `test/unit/docs-limits.test.ts`(문서 줄 수 상한)
    - `TODOS.md`(항목 형식 — What · Why · Context · Effort · Priority · Depends on)
    - `lib/log.ts` · 04.1-01 T3 ⑦(`approval.route_blocked` · `approval.refused` 필드와 사유 코드)
    - `docs/designs/plant8-erp-phase04.1-ceo-review-260924.md` CEO-10 · CEO-18 · CEO-20 · CEO-21 · CEO-23
  </read_first>
  <action>
⓪ **결재 모듈 계약 절(CEO-21)**: 병합된 트리의 `docs/ARCHITECTURE.md`에서 마지막 `## 4-N.` 절 바로 뒤(`## 5.` 앞)에 다음 번호로 `## 4-N. 결재 모듈 계약(Phase 04.1 → Phase 5)`을 **20줄 이하**로 더한다(04-32가 §4-8을 넣었으면 §4-9). 기존 절 문체(짧은 글머리)로 다음만 적는다 — (1) 종류 등록: `registerDocumentKind({kind, label, loadRouteConfig, href, describeDocuments, routeSettings?, loadDetail?})`, 새 종류는 `app/(app)/document-kinds.ts`에 import 한 줄을 더한다(가드 단위 테스트가 빠진 진입점을 잡는다) · (2) 결재선은 제출 때 단계 행으로 고정되고 담당은 표시·처리 시점에 다시 해석한다 · (3) 전이 순서는 version → 종료 상태 → 후보 판정 → UPDATE 먼저 → 단계 행, 거부는 `approval.refused` 사유 코드로 남는다 · (4) 설정·소속 스냅숏은 트랜잭션을 열기 전에 읽는다(`prepareSubmission` — §4-8 잠근 트랜잭션 규약 준수) · (5) 결재함은 `scopeFor()`를 쓰지 않고 후보·처리자 기준, 노출은 요청 단위 메모 · (6) 차수 승인 0건으로 끝나지 않는다(대표 폴백) · 한 차수 한 사람 한 승인 · (7) 「오늘」은 `seoulToday()`. 이 절 밖의 ARCHITECTURE 줄은 바꾸지 않는다.

⓪-b **알려진 한계(CEO-10)**: `TODOS.md`에 절 하나(`## Phase 04.1 알려진 한계(2026-09-24 CEO 리뷰)`)와 항목 하나를 기존 형식대로 더한다 — What: 새 계급을 만들면 정보 노출표에서 결재 정보·연차 정보를 켠다(안 켜면 그 계급 결재자의 결재함 줄이 비어 승인할 수 없다) · Why: `createRole`은 노출 행을 만들지 않고 노출은 기본 숨김이다 · Context: 04.1-01 T4 한계 기록 테스트, `domain/permissions/roles.ts`는 이 페이즈 금지 파일 · Effort S · Priority P2 · Depends on: 노출 모델 결정(사용자). 다른 절은 건드리지 않는다.

① 전체 게이트를 **한 번** 돌린다(CLAUDE.md 화면 검증 순서의 마지막 단계): `pnpm lint && pnpm typecheck && pnpm lint:sql && pnpm build` → `bash scripts/dev-db.sh && pnpm db:reset:test && pnpm test:unit && pnpm test:integration`(통합은 방금 비운 `erp_test`에서 — CEO-16) → `pnpm test:e2e:ci`. 실패하면 `systematic-debugging`으로 원인을 찾고 고친 뒤(새 코드면 TDD) 게이트를 처음부터 다시 돈다.

② 독립 DOM 감사: 실행자가 아닌 별도 서브에이전트(모델 명시)에게 병합된 트리의 `CI=true` 프로덕션 빌드로 04.1 표면 전부(`/leave` · `/leave/new` · `/leave/[id]` · `/approvals` · 결재 시트 · 반려/회수 확인 · `/admin/settings` 두 섹션 · `/admin/people/[id]` 연차 섹션 · 셸 계정 그룹)를 폭 1280 · 1024 · 375에서 DOM 실측하게 한다 — 04.1-02·05·06 플랜의 backstop 항목 전부(가로 스크롤 0 · 행 높이 ≥ 44 · 행동 줄 고정 · 700 경계 모달↔시트 · 긴 이름·사유 줄바꿈 · caption · 포커스). 결과 표를 SUMMARY에 남긴다. 위반이 있으면 고친 뒤 ①을 한 번 더.

③ ROADMAP 성공 기준 1~5와 요구사항 다섯(EXP-03 · EXP-04 · EXP-05 · LEAV-01 · ADMN-04의 결재 부분 — ADMN-04는 partially covered로 적는다)을 증명한 테스트 파일에 한 줄씩 대응시킨 표를 SUMMARY에 쓴다. VALIDATION.md Manual-Only(실제 폰 기기 승인·반려)는 스테이징 확인 항목으로 남긴다.

④ `<probe_fallback>`의 Deferred 목록을 (a)·(b) 두 묶음 그대로 SUMMARY에 옮기고, 다음 단계가 `/gsd-verify-work` → Post-build `/review` → `/qa`(+ `/design-review`와 나란히 **Codex 적대적 디자인 검토** — `codex exec`로 `04.1-UI-SPEC.md`와 병합된 04.1 화면 코드를 검토한다. `codex exec`를 실행할 수 없으면 그 사실과 오류를 보고하고 **다른 검토자로 대체하지 않는다**, CEO-23) → `/cso`(권한·결재·외부 입력을 건드렸으므로 필수) → `/ship`임을 SUMMARY 끝에 적는다(CLAUDE.md — 건너뛰지 않는다).

⑤ SUMMARY에 세 묶음을 더 적는다 — (a) **공개 전 점검표(CEO-20 · 기본값 A)**: 관리자가 전 재직자의 입사일을 `/admin/people/[id]`에 채운다(입사일이 없으면 올해 입사자도 연차 15일을 받는다) · 04.2 알림이 들어오기 전까지 결재자는 매일 `결재`를 연다고 공지한다 · 새 계급을 만들면 정보 노출표에서 결재·연차 정보를 켠다(CEO-10). 그리고 미결 항목 한 줄 「공개 시점(바로 공개 vs 04.2 알림 뒤 공개)은 배포 때 사용자가 정한다」 · (b) **알려진 한계(CEO-10)**: 새 계급 노출 한계 — 근거 테스트 이름과 ⓪-b의 TODOS 항목 · (c) **운영 메모(CEO-18)**: `approval.route_blocked`(warn — 인스턴스 id · 종류 · 차수 · 단계만)는 대표 계급 재직자나 조직을 고치면 풀리고 그동안 기안자는 회수할 수 있다 · `approval.refused`(info)의 사유 코드 `conflict` · `not_holder` · `final` · `not_drafter` · `invalid_state`가 각각 무엇인지 한 줄씩.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql &amp;&amp; pnpm build</automated>
    <fails_when>어느 하나라도 error를 출력하고 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm test:unit &amp;&amp; pnpm test:integration</automated>
    <fails_when>단위·통합 프로젝트 어느 쪽이든 failed가 1 이상이다 — docs-limits가 ⓪의 새 절로 상한을 넘으면 여기서도 깨진다</fails_when>
    <automated>pnpm test:e2e:ci</automated>
    <fails_when>`CI=true` 프로덕션 빌드 E2E(desktop → mobile-375 → desktop-settings)에서 failed가 1 이상이거나 「did not run」이 1 이상이거나 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts &amp;&amp; grep -q "registerDocumentKind" docs/ARCHITECTURE.md &amp;&amp; awk '/^## 4-[0-9]+\. 결재 모듈 계약/{f=1;next} f&amp;&amp;/^## /{exit} f{n++} END{exit !(f &amp;&amp; n&lt;=20)}' docs/ARCHITECTURE.md &amp;&amp; grep -q "정보 노출표" TODOS.md</automated>
    <fails_when>종료 코드가 0이 아니다 — ARCHITECTURE가 줄 수 상한을 넘었거나, 결재 모듈 계약 절이 없거나 본문이 20줄을 넘거나(CEO-21), `TODOS.md`에 새 계급 노출 항목이 없다(CEO-10)</fails_when>
    <automated>OUT=$(git log --no-merges --name-only --format= "$PHASE_BASE"..HEAD --not origin/main -- domain/rules/register.ts ui/next-turn domain/action-log package.json pnpm-lock.yaml) &amp;&amp; test -z "$OUT"</automated>
    <fails_when>종료 코드가 0이 아니다 — git이 실패했거나(`PHASE_BASE` 미설정 등), 04.1 자신의 커밋(병합으로 들어온 main 커밋 제외)이 금지 파일·의존성 파일을 바꿨다</fails_when>
  </verify>
  <acceptance_criteria>
    - 다섯 검증 명령이 전부 초록이다
    - 독립 DOM 감사 표(표면 × 폭 × 판정)가 SUMMARY에 있고 위반 0이다
    - 성공 기준 1~5 × 테스트 파일 대응표가 SUMMARY에 있다
    - Deferred 목록과 Post-build 다음 단계가 SUMMARY에 있고, Post-build 단계에 Codex 적대적 디자인 검토(`codex exec`, 대체 금지 문구 포함)가 `/design-review` 옆에 있다(CEO-23)
    - SUMMARY에 공개 전 점검표 세 줄과 미결 「공개 시점 — 사용자 결정」 한 줄(CEO-20), 알려진 한계(CEO-10), 운영 메모(CEO-18)가 있다
    - `git diff "$PHASE_BASE"..HEAD -- docs/ARCHITECTURE.md TODOS.md`에서 04.1이 바꾼 줄이 새 절·새 항목 안에만 있다(병합으로 들어온 main 변경 제외 — `git diff origin/main..HEAD -- docs/ARCHITECTURE.md TODOS.md`의 `-` 줄이 0)
    - `git push` 기록이 없다(`git reflog` 확인 결과를 SUMMARY에 한 줄)
  </acceptance_criteria>
  <done>병합된 트리에서 전체 게이트가 `CI=true`로 녹색이고, 성공 기준·요구사항이 테스트에 대응되며, 미룬 항목과 Post-build 순서가 기록되었다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 마이그레이션 → 운영 DB | 병합 뒤 journal 순서가 운영 적용 순서다 |
| 두 브랜치의 병합 | Phase 4와 04.1이 같은 파일에 덧붙였다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-37 | Tampering | 마이그레이션 번호·순서 | high | mitigate | 병합 시작은 Phase 4 완료(ROADMAP `[x]` + 마지막 Phase 4 tag가 main journal에 있음) 판정 뒤에만(CEO-19). 병합 직전 04.1 마이그레이션을 지우고 main 위에서 재생성 — `when`이 main보다 늦고 번호가 겹치지 않는다. journal 가드 테스트 + `pnpm db:reset:test`로 실제로 비운 DB에 전체 적용해 증명(CEO-16) |
| T-04.1-38 | Tampering | 병합 충돌 해소 | high | mitigate | 덧붙이기 전용 파일은 양쪽 보존, 의미 판단이 필요한 충돌은 병합 중단 후 보고(추측 금지). 병합된 트리에서 전체 스위트 `CI=true` |
| T-04.1-39 | Elevation of Privilege | 병합으로 섞인 권한 시드·메뉴 | medium | mitigate | 병합 뒤 누수 스캔(액션·DTO·내보내기 축)과 권한 시드 통합 테스트를 다시 돈다 — 두 브랜치의 새 메뉴·액션이 전부 등록되었는지 확인 |
| T-04.1-40 | Repudiation | 강제 푸시로 이력 손실 | medium | mitigate | 이 플랜은 push를 하지 않는다(금지 항목). PR·병합은 `/ship` |
| T-04.1-49 | Tampering | 입사일 없는 재직자의 잔고(과다 부여) · 알림 없는 첫 주 결재 지연 | medium | mitigate | 공개 전 점검표(전 재직자 입사일 입력 · 결재자 매일 확인 공지)를 SUMMARY에 남기고, 공개 시점은 배포 때 사용자가 정한다(CEO-20 기본값 A). 새 사람은 06이 입사일을 필수로 받는다 |
| T-04.1-50 | Repudiation | 막힌 결재선 · 거부된 결재의 운영 가시성 | low | mitigate | 01·02의 `approval.route_blocked` · `approval.refused` 로그(id·코드만 — T-04.1-42)를 SUMMARY 운영 메모가 해석 절차와 함께 넘긴다(CEO-18) |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. 04.1 커밋 범위에서 `package.json`·`pnpm-lock.yaml` 변경 없음을 Task 2 네 번째 검증이 확인한다 |
</threat_model>

<verification>
- 첫 검증 명령이 Phase 4 마지막 tag를 출력(CEO-19)
- 병합된 트리에서 `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `pnpm build` 통과
- `bash scripts/dev-db.sh && pnpm db:reset:test && pnpm test:unit && pnpm test:integration` 통과(CEO-16)
- `pnpm test:e2e:ci` 통과(desktop → mobile-375 → desktop-settings)
- `pnpm vitest run --project unit test/unit/docs-limits.test.ts` 통과 · ARCHITECTURE 결재 모듈 계약 절 20줄 이하(CEO-21) · TODOS 새 계급 항목(CEO-10)
- SUMMARY에 공개 전 점검표 · 미결 공개 시점(CEO-20) · 운영 메모(CEO-18) · Post-build Codex 검토(CEO-23)
- journal 가드 녹색, 04.1 SQL 하나, 번호 = main 마지막 + 1, 최종 tag와 0017~0020 안/밖 판정이 SUMMARY에 있음
- 금지 파일·의존성 파일 04.1 커밋 범위 변경 0
</verification>

<success_criteria>
- 04.1이 Phase 4가 들어간 main 위에서 마이그레이션 충돌 없이 돈다
- 페이즈 전체가 `CI=true` 게이트와 독립 DOM 감사를 통과한다
- 성공 기준 1~5와 요구사항 다섯이 테스트 파일에 대응된다
- 미룬 항목과 Post-build 순서가 기록되어 다음 사람이 이어받는다
- Phase 5가 결재 모듈 계약을 ARCHITECTURE 한 곳에서 읽는다(CEO-21)
- 공개 전 점검표와 운영 메모로 첫 주 운영 위험이 넘겨진다(CEO-20 · CEO-18 · CEO-10)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | main 위에서 재생성된 04.1 SQL 1개(옛 두 개 대체) |
| 커밋 | `chore: drop 04.1 migrations before regenerating on main` · 병합 커밋 · `chore: regenerate 04.1 migration on top of main` |
| 기록 | SUMMARY — Phase 4 마지막 tag(CEO-19) · 전체 게이트 결과 · 독립 DOM 감사 표 · 성공 기준 대응표 · Deferred 목록 · Post-build 순서(Codex 적대적 디자인 검토 포함, CEO-23) · 공개 전 점검표와 미결 공개 시점(CEO-20) · 알려진 한계(CEO-10) · 운영 메모(CEO-18) |
| 문서 | `docs/ARCHITECTURE.md` §4-N 결재 모듈 계약(Phase 04.1 → Phase 5, CEO-21) · `TODOS.md` 새 계급 정보 노출 항목(CEO-10) |

<output>
Create `.planning/phases/04.1-approvals-leave/04.1-07-SUMMARY.md` when done
</output>
