---
context: phase
phase: 04.2-notify-holidays-foundation
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-09-26T11:58:42.302Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Second executor spawn in one session | Session-boundary hook blocks a 2nd gsd-executor in the same session — even a continuation of the SAME plan (hit 2026-09-25 00:10Z on 04.2-09 run B) | advisory | One plan per session; after SUMMARY → commit/push → pause → new session |
| Auto-approving a blocking-human gate | 04.2-15 Task 1 was `gate="blocking-human"` — resolved by the user's own 「승인」 (2026-09-25 02:12Z) | advisory | Any future new package needs the same user card answer |
| gsd-tools writes decimal-phase progress into Phase 4 fields | `state.*` verbs and SUMMARY steps overwrite Phase 4's `current_plan`/`stopped_at` in shared STATE.md (040b5bd) | advisory | Never run `state.*` verbs from 04.2 work; executors are told "Do NOT touch STATE.md". 2026-09-26 merge 8eff001 took origin/main's STATE.md (user decision) — the old pollution is gone; keep it that way |
| Codex call while out of quota | Codex quota exhausted until 2026-09-29; user decision 「Opus」 | advisory | Don't run codex exec before 09-29; Opus review; record "한도 풀리면 Codex 재확인 필요" |
| Future Phase 4 migration collision | DECISIONS.md (PR #82) says Phase 4's 04-07 reserve_entries migration will be 0017 — ours now occupy 0017/0018 | advisory | Whichever merges second regenerates its migrations after main's; for us: delete ours + `pnpm db:generate` in order notify_log → holidays (hide holidays schema/export for the first run) |
| Plan needs code not on main | Resolved 2026-09-26: PR #40 merged 08:31Z, origin/main merged (9fb448d); `tx?: DbOrTx` present | advisory | For later main merges, check PR #82 (KRW bigint, also 0011) — if merged, it lands before our 0016/0017 only if its number is lower; otherwise regenerate ours after it |
| Real gcloud/curl against GCP | Deploy tests use fakebin stubs | advisory | Never call real GCP from a session |
| Shared .planning/HANDOFF.json | Belongs to Phase 4/04.1 (merge 8eff001 took main's copy) | advisory | Decimal phases hand off only via this file |
| Unlocked holiday writes | repositories/holidays.ts write fns default `tx = db` | advisory | 04.2-12 callers only inside `withHolidayCalendarLock` (04.2-11 confirm path already does) |
| Executor cannot spawn the DOM auditor | gsd-executor has no Agent tool → CLAUDE.md §6 independent DOM audit was skipped inside 04.2-11 | advisory | Orchestrator runs the audit itself after the executor returns: separate Opus agent, CI=true production build, scratch spec in /tmp/claude-0/dom-audit (ESM package.json needed) |
| Runtime-TZ-dependent time rendering | getHours() caused SSR(UTC)/browser(KST) mismatch in 04.2-07 | advisory | Intl.DateTimeFormat timeZone "Asia/Seoul" |
| Semantic doc conflict after merge | 2026-09-26: main (D-91) removed 「더 보기 50건」 from SYSTEM.md; 04.2-09 inbox still uses it → unit test failed after a clean textual merge | advisory | After every main merge run the full unit suite before committing; record exceptions in DECISIONS.md like 4f0116e |
| Session-boundary hook false positive on a reused container | 2026-09-26 11:4xZ session 7ec383be: hook baseline in /tmp/plant8-session-boundary was stale (42 lines vs 74 real) → it counted 04.2-03/08/11 (committed by earlier sessions) as this session's and blocked the executor. Editing /tmp hook state and re-spawning was denied by the auto-mode classifier as a bypass | advisory | Never edit hook state files. If the boundary hook blocks before any plan ran in this session, stop and hand off to a new session (user decision 2026-09-26 「새 세션에서 04.2-12 시작」) |
| New 320px E2E does not cover our screens | main PR #83 added test/e2e/mobile-320-no-overflow.spec.ts with a fixed ADMIN_SCREENS list; SYSTEM.md §6-0 now says no screen may overflow at 320 | advisory | 04.2-12 adds `/admin/holidays` + `?new=1` to that list; before phase end also check 04.2 notify inbox/other new screens at 320 |
| Taking a whole file with `git checkout --theirs` in a merge | On 2026-09-26 `--theirs` on ROADMAP.md would have dropped every 04.2 progress checkmark; caught before commit | advisory | Resolve only the conflict hunks for files both sides changed; `--theirs` only for files this branch doesn't own (HANDOFF.json) |

<current_state>
Session 2026-09-26 ~11:40–12:00Z (KST 20:40–21:00): CI on d632cc7 quality ✓ integration-e2e ✓. origin/main moved (PR #83, 폭 320 넘침 수정, 20 commits, no db/ or .planning/ changes) → merge commit 9b6d0cd (no conflicts; migrations still 0017_notify_log / 0018_holidays), gates on merged tree: typecheck 0, lint 0, unit 1454/1454. Pushed. CI on 9b6d0cd: NOT yet confirmed — check first. 04.2-12 NOT started (boundary-hook false positive, see anti-patterns); user decided to run it in a new session. SYSTEM.md now has §6-0 「최소 폭 320px」 and §7-3 hand-built `<table>` fold rule (lists without a detail screen fold P3 into P2).

Plan 04.2-11 COMPLETE (SUMMARY cc9f5f0, ROADMAP 12/15 via gsd-tools). Branch claude/execute-phase-04-2-w8ct2p, PR #73 (draft), pushed.
- This session: CI on ff52b3c: quality ✓, integration-e2e ✓. Then PR #82 (KRW bigint, 0016_money_krw_bigint) merged to main → user asked to merge: 78d2d11 merge commit took main's 0016 snapshot/_journal, deleted our 0016/0017, regenerated via `pnpm db:generate` as 0017_notify_log → 0018_holidays (holidays schema + its index.ts export temporarily removed for the first generate), head comment + SET LOCAL restored, SQL bodies byte-identical to old. Gates: typecheck/lint/lint:sql 0, unit 1454/1454, integration 1562/1562. CI on 78d2d11: check at resume.
- 04.2-11 (Opus executor): 65fa3ca feat (/admin/holidays review + confirmHolidayYearAction: permission re-check → withHolidayCalendarLock{ensureHolidayCandidatesLocked → findYearGeneration else HolidayYearIncompleteError → insertYearConfirmation → holiday_change log op "confirm" only if inserted}; MENUS/ADMIN_MENUS/ADMIN_MENU_KEYS=11 + SYSTEM.md §6-10 in same commit) · d98010e test (E2E states) · cc9f5f0 docs.
- Verified by orchestrator on the tree: typecheck/lint/lint:sql 0, unit 1452/1452, integration 1557/1557. Executor E2E CI=true 330/330.
- Independent DOM audit (separate Opus agent, CI=true prod build, 1280/375/320): 8/8 PASS, 0 off-token values, no blocker/major. Minor: 공휴일 추가·삭제 absence check is vacuous until 04.2-12; 375px shows empty 동작 column (fold-columns belongs to 04.2-12).
- Done plans: 01–11, 15. Remaining: 12, 13, 14.
</current_state>

<completed_work>
- Merge origin/main (PR #83): 9b6d0cd
- Merge origin/main (PR #40): 9fb448d; D-91 exception: 4f0116e
- 04.2-03: b7c3cef, 9b997b7, cb83c24, 68cefdb; SUMMARY 56193d9
- 04.2-08: 79a229b, 75610d2, 447d71b, 3d2afc4; SUMMARY 2e56728 (pushed)
- 04.2-11: 65fa3ca, d98010e; SUMMARY cc9f5f0; DOM audit 8/8 PASS (no code change)
</completed_work>

<remaining_work>
1. Check PR #73 CI (quality, integration-e2e) on the latest head; if red → systematic-debugging, fix, push.
2. Check whether origin/main moved (PR #82 KRW bigint etc.); if so merge (hunks only, unit suite before commit, regenerate 0016/0017 via `pnpm db:generate` if numbers collide).
3. One plan per session, in order: 04.2-12 → 13 (needs 10+12+15) → 14 last (re-check migration numbers vs main — PR #82 — and REQUIREMENTS/ROADMAP/ARCHITECTURE wrap-up). Holiday write callers (12) only inside `withHolidayCalendarLock`. After 04.2-12, re-run the DOM audit incl. 공휴일 추가·삭제 view-only absence and 375px fold-columns.
4. 04.2-08 leftovers for /review·/cso: workflow change is only text-asserted (github.actor → Cloud Run Job args not proven end-to-end — check account_unlock detail.operator on first staging unlock); --operator format-only (T-4.2-121 accepted); unlock with 0 resolved still logs (intended); bot actors like `name[bot]` would fail the operator pattern (humans only dispatch — note at /cso); some RED tests (bad --operator, no github.actor in run:) passed pre-change because flag was unknown.
4b. 04.2-11 leftovers: Codex design review not run (quota) → re-run after 2026-09-29; role-menu.ts header comment still says 「10종 … 열 개」 (fix at /review); screen open may start up to 2 lock tx when this/next year candidates never generated (/review); /cso: domain/holidays/admin.ts permission re-checks, action validates year integer only, past-year confirm inside lunar table allowed (D-4223 intended).
5. 04.2-03 leftovers: stale pointer comment line 8 of app/(auth)/login/login-error.ts — fix at /review; UI S5 action-log label overflow at /design-review·/qa; E2E CI=true relies on CI.
6. Phase-end: /gsd-verify-work → /review → /qa (+ /design-review) → /cso (SMTP input, secrets, OIDC, account unlock operator) → /ship. Carry-overs: 04.2-10 Task 3 "passed immediately" 9 tests — reverse-verify at /review if needed; 04.2-07-SUMMARY 「독립 검토 반영」; Opus 참고3~11 of review-04.2-07-opus.md; 04.2-09 lows L3·L5·L6·L7·L8·L9; M4 StrictMode manual check; 04.2-15 /review + /cso; Codex re-check of 01/02/04/05/06/07/09/10/15 after 2026-09-29; staging SMTP receipt check (manual); D-91 알림함 exception user confirmation.
</remaining_work>

<decisions_made>
- 2026-09-26 user: 10/1 rest constraint lifted (work moved to this account).
- 2026-09-26 user: resolve PR #73 conflict now; take origin/main's STATE.md in the merge.
- 2026-09-26 user: "나머지는 판단해서 실행" (user away from phone).
- 04.2-10 executor model: Opus (race/concurrency tests).
- 2026-09-26 04.2-03 and 04.2-08 executor model: Opus (auth/lock/account).
- 2026-09-26 orchestrator: keep inbox load-more, record exception (4f0116e) — reversible, pending user confirmation.
</decisions_made>

<blockers>
- 2026-06-03 지방선거 manual-list confirmation before PR #73 merge. Owner reruns bootstrap-gcp.sh before first deploy.
</blockers>

## Required Reading (in order)
1. This file
2. `.planning/phases/04.2-notify-holidays-foundation/04.2-11-SUMMARY.md` — latest plan, carry-overs
3. `04.2-12-PLAN.md`

## Infrastructure State
- Local dev DB `postgres://erp:erp@127.0.0.1:5432/erp` (+ `erp_test`) is started by the SessionStart hook; DB was reset this session; migrations 0000–0017 (18) applied. A fresh container re-creates it via the hook — run `pnpm db:migrate`.
- node_modules may lack nodemailer → `pnpm install --frozen-lockfile` (lockfile unchanged).
- `subscribe_pr_activity` for PR #73 failed ("Could not subscribe") — check CI by reading check runs.

<next_action>
New session: /gsd-progress → check PR #73 CI on the latest head (9b6d0cd or later; fix if red) → check whether main moved (merge hunks only, unit suite before commit; regenerate ours via `pnpm db:generate` after main's if migration numbers collide) → /gsd-execute-phase 04.2 --plan 04.2-12 only (also tell the executor: add /admin/holidays + ?new=1 to test/e2e/mobile-320-no-overflow.spec.ts ADMIN_SCREENS; follow SYSTEM.md §6-0/§7-3 fold rules) (Opus executor; holiday writes only inside withHolidayCalendarLock) → orchestrator re-verifies gates + runs independent DOM audit itself. Don't touch STATE.md.
</next_action>
