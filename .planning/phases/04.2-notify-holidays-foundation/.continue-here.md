---
context: phase
phase: 04.2-notify-holidays-foundation
task: 0
total_tasks: 0
status: paused
last_updated: 2026-09-24T23:12:26.814Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-approving a blocking-human gate | 04.2-15 Task 1 is `gate="blocking-human"` (package identity before `pnpm add`); execute-phase carve-out forbids auto-approve even when the user is asleep | advisory | Install only after the user's own answer on the card (「승인」) — a coordinator note is not approval |
| Codex call while out of quota | Codex quota exhausted until 2026-09-29; calls fail | advisory | Use Opus for reviews; record "한도 풀리면 Codex 재확인 필요" |
| Second plan / post-plan fix in the same session | Session-boundary hook blocks spawning executors once a plan's SUMMARY exists — even for review fixes of that same plan (hit 2026-09-24 23:10Z) | advisory | Put review fixes in the NEXT session's first step, before the next plan |
| Real gcloud/curl against GCP | Deploy tests use test/unit/deploy/fakebin stubs | advisory | Never call real GCP from a session; bootstrap re-run is the owner's job |
| Shared .planning/HANDOFF.json | Belongs to Phase 4 | advisory | Decimal phases hand off only via this file |
| Unlocked holiday writes | repositories/holidays.ts write functions default `tx = db` | advisory | 04.2-11/12 callers must call them only inside `withHolidayCalendarLock` |
| Runtime-TZ-dependent time rendering | 04.2-07 inbox rendered time with getHours() → SSR (Cloud Run UTC) vs browser (KST) hydration mismatch; CI is UTC so E2E can't see it | advisory | Always format user-visible times with Intl.DateTimeFormat timeZone "Asia/Seoul" (precedent domain/holidays/business-day.ts) |

<current_state>
04.2-01, 02, 04, 05, 06, 07 complete (SUMMARYs exist). 6/15 plans done. Branch claude/execute-phase-04-2-w8ct2p (draft PR #73), head 8365b21 pushed; CI green on c24d04c, CI for 8365b21 pending. STATE.md/ROADMAP.md/REQUIREMENTS.md untouched (decimal phase; 04.2-07 SUMMARY frontmatter records requirements-completed NOTI-01 for later).
04.2-07 has 2 BLOCKING review findings NOT yet fixed (see remaining_work) — fix them first next session.
04.2-15 still PARKED at Task 1 (blocking-human nodemailer card in thread "04.2-15", root cmsg_01YBkj3cD9fvNQycDPSt9u5L9gzXmkMzyYZJimYgy6kY2c). Nothing installed.
Uncommitted: ` M .claude/gates/phase-04.2.log` (hook-appended line; hook-managed, left as is).
</current_state>

<completed_work>
- 04.2-07 (this session, Sonnet executor): ce0acc9 (tracer: tick→badge 1→open CTE→badge 0; fixed raw-SQL timestamp parsing), b03bc30 (8 boundary integration tests), 125ba1a (13 unit + 4 E2E badge-state tests), 8365b21 (SUMMARY).
- Fresh verification 2026-09-24 23:10Z (orchestrator): pnpm lint 0, typecheck 0, lint:sql 0, unit unread-count 13/13, integration notify-inbox 9/9. Executor: CI=true E2E notify-inbox+user-menu+mobile-shell+a11y 154/154.
- Independent DOM audit (Sonnet, CI=true, prod build): /mnt/project-files/04.2-gates/dom-audit-04.2-07.md — FAIL 0 (badge states, tokens, inset line, same-time rows split, axe 0 PC/mobile, 375px). N/A: empty state & 「더 보기」 keyboard (deferred to 04.2-09 by plan).
- Opus independent review (Codex stand-in): /mnt/project-files/04.2-gates/review-04.2-07-opus.md — 막는 문제 2건 (below). Authorization, one-statement CTE snapshot, ms cursor, badge race: confirmed OK.
</completed_work>

<remaining_work>
- FIRST (before any new plan): fix 04.2-07 review findings with TDD (RED proven), one commit each `fix(04.2-07): ...`:
  - B1: app/(app)/notifications/inbox-list.tsx:22-23 uses getHours()/getMinutes() → format with Intl.DateTimeFormat timeZone "Asia/Seoul"; add unit test that fails under TZ=UTC.
  - B2: test/e2e/notify-inbox.spec.ts:117-135 "실패 때 이전 값 유지" asserts before the abort happens → wait for the aborted request (requestfailed) before asserting; prove it catches a catch→null regression.
  - 참고1: openInbox raw SQL binds JS Date → pass `.toISOString()` + `::timestamp` like the other queries.
  - 참고2: add explicit ORDER BY (created_at desc, id desc) to the CTE's final SELECT.
  - Then full gate once (lint, typecheck, lint:sql, unit, integration notify-inbox + notify-tick one at a time, CI=true E2E notify-inbox/user-menu/mobile-shell/a11y), append "## 독립 검토 반영" to 04.2-07-SUMMARY (DOM audit FAIL 0; Opus B1/B2 fixed + shas; 참고3~8 deferred to phase-end review; "Codex 디자인 검토: 한도로 미실행 — 한도 풀리면 Codex 재확인 필요"), push.
  - Deferred to phase-end code review: Opus 참고3 (read_at SET vs returned value TZ conversions), 4 (cross-tick test doesn't catch splitting into two statements), 5 (zod cursor schema duplicated in test), 6 (plan's 3 E2E + full accessible-name assertion missing), 7 (inset rule wording differs from plan; matches UI-SPEC — record deviation), 8 (badge CSS literals 600/1px/0 instead of tokens).
- Next plan after the fixes: next incomplete plan in wave order (wave 2 done after 07 → wave 3; 04.2-10 needs 04.2-15). Check `phase-plan-index` for the lowest runnable plan (e.g. 04.2-08/09).
- Phase-end: 04.2-05 Opus 참고#1 (default-config wiring test); /cso required (auth); /design-review for UI plans (04.2-07 included); Codex design review for 04.2-07 Task 3 owed after quota reset.
- 04.2-15: after 「승인」 → Task 2/3. 04.2-10 depends on it.
- 04.2-14 regenerates migrations (0011_notify_log, 0012_holidays) after merging main.
</remaining_work>

<decisions_made>
- 04.2-07 executor used Sonnet per coordinator (UI plan) with Opus independent review; gstack /browse not used — DOM audit ran via repo Playwright CI=true (deviation reported).
- 04.2-06: a year with a `holiday_year_generations` row is never regenerated.
- 04.2-15 Task 1 NOT auto-approved (blocking-human; CLAUDE.md §5 new dependency approval).
- 04.2-02 holiday list is a PROVISIONAL DEFAULT; user's answer overrides it.
</decisions_made>

<blockers>
- 04.2-15 Task 1: user's answer on the nodemailer card (blocks 04.2-15 and 04.2-10 only).
- Before PR #73 merges, the user must confirm the 04.2-02 holiday items.
- Owner must re-run `bash scripts/bootstrap-gcp.sh --project plant8-509002 --github-repo rrangjaa-eng/ERP_PLANT8_260917` once before the first deploy of this code.
- After next deploy, run the scheduler job once per environment and confirm first success log.
- Codex re-check of 04.2-01/02/04/05/06/07 after 2026-09-29 (한도 풀리면 Codex 재확인 필요).
</blockers>

## Required Reading (in order)
1. /mnt/project-files/04.2-gates/review-04.2-07-opus.md (B1, B2, 참고1, 참고2)
2. .planning/phases/04.2-notify-holidays-foundation/04.2-07-SUMMARY.md
3. The next plan's PLAN.md (only after the fixes are pushed — and it belongs to the session after, per the one-plan rule if the hook treats the fixes as a unit)

## Infrastructure State
- Local dev DB via session hook. Migrations 0011_notify_log and 0012_holidays on branch only.
- package.json / pnpm-lock.yaml unchanged.

<context>
Sequential execution (isolation auto-degrades). Plans touching DB/security use an Opus executor; UI plans Sonnet + Opus check; after each plan an Opus independent review replaces Codex; UI plans also get an independent CI=true DOM audit.
</context>

<next_action>
Start with: /gsd-progress, then fix the 04.2-07 review findings (B1, B2, 참고1, 참고2) with TDD and push `git push origin HEAD:claude/execute-phase-04-2-w8ct2p`. If the session-boundary hook allows, continue with the next plan; otherwise hand off again.
</next_action>
