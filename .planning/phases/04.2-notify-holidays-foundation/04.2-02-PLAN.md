---
phase: 04.2-notify-holidays-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - domain/holidays/lunar-table.ts
  - domain/holidays/rules.ts
  - domain/holidays/business-day.ts
  - test/unit/holidays/rules.test.ts
  - test/unit/holidays/business-day.test.ts
autonomous: true
requirements: [ADMN-11]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`generateHolidayRules(year)`가 관공서의 공휴일에 관한 규정 제2조(법정 공휴일 — 1월 1일 · 설날 연휴 사흘 · 3·1절 · 어린이날 · 부처님오신날 · 현충일 · 광복절 · 추석 연휴 사흘 · 개천절 · 한글날 · 기독탄신일)와 제3조(대체공휴일)로 그 해 후보를 날짜 오름차순·날짜 유일로 돌려준다. 선거일·임시공휴일은 규칙이 만들지 않는다(관리자 수동 — 04.2-07)"
    - "2026년 규칙 결과는 정확히 19행이다: 01-01 · 02-16 · 02-17 · 02-18 · 03-01 · 03-02(대체) · 05-05 · 05-24 · 05-25(대체) · 06-06 · 08-15 · 08-17(대체) · 09-24 · 09-25 · 09-26 · 10-03 · 10-05(대체) · 10-09 · 12-25"
    - "2027년 규칙 결과는 정확히 20행이다: 01-01 · 02-06 · 02-07 · 02-08 · 02-09(대체) · 03-01 · 05-05 · 05-13 · 06-06 · 08-15 · 08-16(대체) · 09-14 · 09-15 · 09-16 · 10-03 · 10-04(대체) · 10-09 · 10-11(대체) · 12-25 · 12-27(대체)"
    - "내장 음력 표(2025~2035)의 설날·부처님오신날·추석 양력 날짜는 Node ICU 단기력(`ko-KR-u-ca-dangi`, `Asia/Seoul`)으로 되읽으면 음력 1월 1일·4월 8일·8월 15일이다 — 표를 손으로 고칠 때 틀리면 단위 테스트가 실패한다"
    - "음력 표 마지막 해가 KST 올해 + 2 이상이 아니면 단위 테스트가 실패한다(표 갱신 알림 — 조용히 틀린 해를 계산하지 않게)"
    - "addBusinessDays(d, N)은 N=0이면 d가 영업일일 때 d 자신이고, 연말을 넘는 계산(12/31 + 1영업일)은 다음 해 표(확정 또는 후보)를 읽는다. 내장 음력 표 범위 밖 연도 요청은 조용히 틀린 값 대신 오류를 낸다"
    - "설·추석 연휴가 일요일·다른 공휴일과 겹치면 규정 제3조대로 그 다음 첫 비공휴일 평일에 대체공휴일이 생기고, 같은 날짜 두 공휴일은 표에 한 행(날짜 유니크)만 남는다"
    - "영업일 계산은 KST 달력 날짜(시각 없음)로 하며 UTC 변환으로 하루 밀리지 않는다 — 단위 테스트가 KST 자정 전후 입력을 덮는다"
  artifacts:
    - path: "domain/holidays/lunar-table.ts"
      provides: "2025~2035 설날·부처님오신날·추석 양력 날짜 표와 범위 상수"
      exports: ["LUNAR_HOLIDAY_TABLE", "LUNAR_TABLE_FIRST_YEAR", "LUNAR_TABLE_LAST_YEAR"]
    - path: "domain/holidays/rules.ts"
      provides: "규정 제2·3조 후보 생성 순수 함수 · 구분 값·라벨 · 범위 오류"
      exports: ["generateHolidayRules", "RuleHoliday", "HOLIDAY_KINDS", "HolidayKind", "HOLIDAY_KIND_LABELS", "LunarTableRangeError"]
    - path: "domain/holidays/business-day.ts"
      provides: "KST 날짜 문자열 · 영업일 판정 · N영업일 전/후 순수 함수"
      exports: ["toKstDate", "isBusinessDay", "addBusinessDays", "HolidayLookup"]
  key_links:
    - from: "domain/holidays/rules.ts"
      to: "domain/holidays/lunar-table.ts"
      via: "`generateHolidayRules`가 그 해 음력 세 날짜를 표에서 읽고, 없으면 `LunarTableRangeError`"
      pattern: "LUNAR_HOLIDAY_TABLE"
    - from: "domain/holidays/business-day.ts"
      to: "HolidayLookup"
      via: "날짜마다 그 해의 공휴일 집합을 조회 함수로 받는다 — 연말을 넘으면 다음 해 집합을 요청한다"
      pattern: "HolidayLookup"
---

<objective>
**As a** 경영관리와 관리자, **I want to** 법정 규칙으로 해마다 공휴일 후보가 정확히 계산되고 영업일·N영업일 전/후가 한 함수로 나오기를, **so that** 260907 「알림만 목요일 고정」처럼 지급일·마감·알림이 서로 다른 달력을 쓰는 일이 다시 생기지 않는다.

(스토리는 ROADMAP 04.2 Goal 줄과 기준 1에서 끌어왔다.)

순수 함수 플랜이다 — DB·화면 없이 규칙과 계산만 만든다. 공휴일 표(DB)와 지연 생성·tick 연결은 04.2-05, 관리자 화면은 04.2-07이 이 함수 위에 붙는다.

Purpose: ADMN-11 「법정 공휴일 규칙으로 매년 후보가 자동 생성」과 「영업일·N영업일 전/후 계산은 도메인 함수이고 단위 테스트가 설·추석 연휴와 대체공휴일을 덮는다」(ROADMAP 기준 1). CONTEXT 이미 확정된 입력 「공휴일 표」·「한 표 원칙」, Claude's Discretion 「음력 변환 데이터는 코드 내장 표」.

Output: 음력 표 · 규칙 생성 · 영업일 함수 · 단위 테스트 둘.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개(`korean-lunar-calendar`는 RESEARCH가 기각했다 — 쓰지 않는다). `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — 「Code Examples」의 규정 제2·3조 규칙 스케치, Assumption A1(Grep `A1`)
# docs/inputs/phase-07-schedule-notify.md §1(공휴일 표)
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 ADMN-11 boundary(#1)·adjacency(#2)·precision(#5)이다(#1의 「다음 해 표를 읽는다」의 DB 쪽은 04.2-05가 다시 증명한다).

**계획 단계 가정·정정(표면화):**

- **RESEARCH 정정 둘(플래너가 ICU·규정으로 확인):** (1) 2027년 설날은 2월 7일(일)이다 — RESEARCH의 「2월 6일」은 틀렸다(Node 22 ICU 78 단기력과 KST 합삭 시각 기준). 그래서 2027 설 연휴는 02-06(토)·02-07(일)·02-08(월)이고 일요일이 끼어 02-09(화)가 대체공휴일이다. UI-SPEC S2-b 와이어프레임의 2027 행도 이 날짜로 읽는다(예시 그림이다). (2) 3·1절은 대체공휴일 대상이다(2021년 개정 제3조 — 국경일 전부) — 그래서 2026-03-01(일) → 03-02(월) 대체공휴일이 생긴다. RESEARCH의 「3·1절 제외」는 틀렸다.
- **A1(사람 확인 남김):** 위 두 해의 날짜는 ICU·규정 조문으로 계산한 값이고 공식 월력요항(한국천문연구원·인사혁신처 고시)과 대조하지 못했다(연구 세션의 네트워크 차단). 실행자는 테스트 기대값을 굳히기 전에 공식 고시와 대조할 수 있으면 대조하고, 못 하면 SUMMARY의 「사람 확인 필요」에 두 해 목록을 그대로 올린다(VALIDATION Manual-Only 첫 줄). 2026-06-03 지방선거일은 규칙이 아니라 관리자 수동 추가다(선거일 — 제2조 제10호의2).
- **D-4210 대체 규칙의 범위:** 대체공휴일은 규칙 공휴일(제2조)에서만 생긴다. 관리자가 넣은 임시공휴일·선거일은 대체공휴일을 만들지 않는다. 근로자의 날(5/1)은 관공서 공휴일 규정에 없어 규칙에 넣지 않는다 — 회사가 쉬면 관리자가 임시공휴일로 넣는다(가정 — SUMMARY에 적고 /plan-eng-review에서 확인).
- **대체일 규칙(제3조):** (가) 국경일 넷(3·1절·광복절·개천절·한글날)·어린이날·부처님오신날·기독탄신일이 토·일이면 대체 (나) 설·추석 연휴 사흘 중 일요일이 끼면 대체(토요일은 해당 없음) (다) (가)·(나)의 날이 다른 공휴일과 같은 날이면 대체 (라) 대체일은 그 공휴일(연휴면 연휴 마지막 날) 다음의 첫 평일(월~금)이면서 공휴일·이미 정한 대체일이 아닌 날 (마) 1월 1일·현충일·선거일은 대체하지 않는다. 대체가 필요한 날마다 대체일 하나이고 날짜순으로 정한다.
- **이름:** `1월 1일` · `설날 연휴`(전날·다음날) · `설날` · `3·1절` · `어린이날` · `부처님오신날` · `현충일` · `광복절` · `추석 연휴` · `추석` · `개천절` · `한글날` · `기독탄신일`. 대체일의 이름은 원래 공휴일 이름(연휴는 `설날`·`추석`)이고 구분이 `substitute`다(UI-SPEC S2-c). 같은 날짜 두 공휴일은 한 행, 이름은 ` · `로 잇는다(예: 2025-05-05 `어린이날 · 부처님오신날`).
- **N=0과 비영업일:** `addBusinessDays(d, 0)`은 d가 영업일이면 d, 아니면 다음 영업일이다. N < 0은 N영업일 전이다.
- **연도 조회:** 영업일 함수는 동기 순수 함수라 공휴일 집합을 `HolidayLookup = (year: number) => ReadonlySet<string>`로 받는다. DB에서 미리 읽어 넘기는 일은 04.2-05의 `calendar.ts`가 한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 2026년 규칙 후보 → 영업일 계산 한 길(2026-02-13 금 + 1영업일 = 설 연휴·주말을 건너 02-19 목)</name>
  <files>domain/holidays/lunar-table.ts, domain/holidays/rules.ts, domain/holidays/business-day.ts, test/unit/holidays/rules.test.ts, test/unit/holidays/business-day.test.ts</files>
  <read_first>
    - `domain/action-log/record.ts` 10~70행 — `as const` 배열 + 라벨 `Record` 모양(구분 값·라벨의 본보기)
    - `lib/actions/user-facing-error.ts` — `UserFacingError`(범위 오류의 부모)
    - `.planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md` 규정 규칙 스케치(Grep `제3조`)
    - `.planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md` S2-c(이름·구분 규칙, Grep `### S2-c`)
  </read_first>
  <behavior>
    - `generateHolidayRules(2026)`의 날짜 목록이 must_haves의 19개와 같고, 03-02·05-25·08-17·10-05 네 행만 `kind: "substitute"`다
    - `addBusinessDays("2026-02-13", 1, lookup)` = `"2026-02-19"` (lookup은 2026 규칙 날짜 집합)
    - `isBusinessDay("2026-09-25", lookup)` 거짓(추석) · `isBusinessDay("2026-10-07", lookup)` 참
  </behavior>
  <action>
① `test/unit/holidays/rules.test.ts`·`business-day.test.ts`(신규)에 behavior 셋을 먼저 쓴다(RED).

② `domain/holidays/lunar-table.ts`(신규): `LUNAR_HOLIDAY_TABLE: Readonly<Record<number, { seollal: string; buddhasBirthday: string; chuseok: string }>>`에 2025~2035 열한 해의 양력 날짜(`YYYY-MM-DD`)를 넣는다 — 값: 2025 `01-29`·`05-05`·`10-06` / 2026 `02-17`·`05-24`·`09-25` / 2027 `02-07`·`05-13`·`09-15` / 2028 `01-27`·`05-02`·`10-03` / 2029 `02-13`·`05-20`·`09-22` / 2030 `02-03`·`05-09`·`09-12` / 2031 `01-23`·`05-28`·`10-01` / 2032 `02-11`·`05-16`·`09-19` / 2033 `01-31`·`05-06`·`09-08` / 2034 `02-19`·`05-25`·`09-27` / 2035 `02-08`·`05-15`·`09-16`(각 해의 연도를 앞에 붙인다). `LUNAR_TABLE_FIRST_YEAR = 2025`, `LUNAR_TABLE_LAST_YEAR = 2035`. 파일 머리 주석 두 줄: 출처(Node ICU 단기력으로 계산, 공식 월력요항 대조는 A1) · 갱신 방법(해를 더하면 ICU 대조 테스트가 검사한다).

③ `domain/holidays/rules.ts`(신규): `HOLIDAY_KINDS = ["statutory", "substitute", "temporary", "election"] as const` · `HolidayKind` · `HOLIDAY_KIND_LABELS: Record<HolidayKind, string>` = `법정 공휴일`·`대체공휴일`·`임시공휴일`·`선거일`(UI-SPEC 카피 계약) · `RuleHoliday = { date: string; name: string; kind: "statutory" | "substitute" }` · `class LunarTableRangeError extends UserFacingError`(메시지 `{연도}년 후보를 만들지 못했습니다 · 음력 표에 없는 해 · 음력 표 갱신 필요`, `year` 필드) · `generateHolidayRules(year: number): RuleHoliday[]`. 이 Task에서는 법정 공휴일 전부 + 대체 규칙 (가)의 토·일 가지까지만 구현해 2026이 초록이 되게 한다(연휴 일요일·겹침·병합은 Task 2).

④ `domain/holidays/business-day.ts`(신규): `HolidayLookup` 타입 · `toKstDate(instant: Date): string`(`Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Seoul", year: "numeric", month: "2-digit", day: "2-digit" })`) · `isBusinessDay(date: string, lookup: HolidayLookup): boolean`(요일은 날짜 문자열을 UTC 정오로 만든 `Date`의 `getUTCDay()` — 로컬 시간대와 무관) · `addBusinessDays(date: string, n: number, lookup: HolidayLookup): string`. 날짜 산술은 UTC 자정 `Date`의 `setUTCDate`로만 하고 로컬 시간대 메서드를 쓰지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/holidays</automated>
    <fails_when>failed 개수가 1 이상이거나 "No test files found"가 나온다 — 2026 목록이 19행과 다르거나, 영업일 계산이 설 연휴·주말을 건너뛰지 못했다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(경계 — domain은 domain·repositories·lib만) 또는 tsc error로 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run --project unit test/unit/holidays` 초록, 2026 19행 단언이 날짜 문자열 배열 비교(`toEqual`)다
    - `domain/holidays/*.ts`에 `getDay()`·`getDate()`·`setDate(` 같은 로컬 시간대 메서드가 없다(`grep -nE "\.(getDay|getDate|setDate|getMonth|getFullYear)\(" domain/holidays/*.ts` 출력 없음)
    - 새 의존성 없음(`git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음)
  </acceptance_criteria>
  <done>2026년 후보와 그 위의 영업일 계산이 한 길로 맞는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 대체공휴일 규칙 전부 — 연휴 속 일요일·다른 공휴일과 겹침·같은 날 병합 · 2025·2027 전체 목록 · 표 전 해 ICU 대조 · 범위 오류 · 표 만료 알림</name>
  <files>domain/holidays/rules.ts, test/unit/holidays/rules.test.ts</files>
  <read_first>
    - `domain/holidays/rules.ts`·`domain/holidays/lunar-table.ts`(Task 1 결과)
    - 이 플랜 `<probe_fallback>` 「대체일 규칙(제3조)」·「이름」
  </read_first>
  <behavior>
    - 2027 = must_haves의 20행(02-09·08-16·10-04·10-11·12-27이 substitute) — 설 연휴 속 일요일(02-07) → 02-09
    - 2025: 03-01(토) → 03-03 대체 · 05-05 한 행 이름 `어린이날 · 부처님오신날` → 05-06 대체 · 추석 연휴 10-05(일)~10-07 → 10-08 대체(10-03 개천절·10-09 한글날과 겹치지 않는 첫 평일)
    - 표의 모든 해(2025~2035): 날짜 중복 없음 · 대체일은 모두 월~금 · 대체일은 다른 규칙 공휴일과 겹치지 않음 · 결과가 날짜 오름차순
    - 표의 모든 해: `Intl.DateTimeFormat("ko-KR-u-ca-dangi", { timeZone: "Asia/Seoul", month: "numeric", day: "numeric" }).formatToParts`로 세 날짜를 되읽으면 (1,1)·(4,8)·(8,15)
    - `generateHolidayRules(2024)`·`(2036)` → `LunarTableRangeError`(메시지 그대로)
    - 표 만료 알림: `LUNAR_TABLE_LAST_YEAR >= Number(toKstDate(new Date()).slice(0, 4)) + 2`
  </behavior>
  <action>
① behavior 여섯 줄을 `rules.test.ts`에 더한다(RED). ICU 대조 케이스에서 달·날 부분이 숫자 문자열이 아니면(예: 윤달 표기) 그 해를 실패로 보고 `systematic-debugging`으로 원인을 적는다 — 테스트를 느슨하게 하지 않는다.

② `generateHolidayRules`를 `<probe_fallback>` 규칙 (가)~(마)대로 마무리한다: 먼저 법정 공휴일을 날짜별로 모아 같은 날은 한 행으로 합치고(이름 ` · `), 대체가 필요한 날(토·일 가지 · 연휴 일요일 가지 · 겹침 가지)을 날짜순으로 훑으며 대체일을 하나씩 정한다(이미 정한 대체일도 「공휴일」로 친다). 대체일 행의 이름은 원래 공휴일 이름, 연휴면 `설날`/`추석`이다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/holidays/rules.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 2025·2027 목록이 다르거나(특히 2027-02-09, 2025-03-03·05-06·10-08), 같은 날 두 행이 남았거나, 음력 표 값이 ICU 단기력과 어긋나거나, 범위 밖 해가 오류 없이 빈 목록을 돌려줬거나, 표 만료 알림이 빠졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 2025·2026·2027 목록 단언이 각각 날짜·구분 쌍 배열의 `toEqual`이다
    - ICU 대조가 `LUNAR_TABLE_FIRST_YEAR`부터 `LUNAR_TABLE_LAST_YEAR`까지 모든 해를 돈다(반복 범위를 상수로 — 해를 더하면 자동으로 검사된다)
    - 범위 밖 해의 오류 메시지가 `{연도}년 후보를 만들지 못했습니다 · 음력 표에 없는 해 · 음력 표 갱신 필요` 글자 그대로다
  </acceptance_criteria>
  <done>규정 제3조의 세 가지 대체 가지와 같은 날 병합이 세 해 전체 목록으로 증명되고, 표 밖의 해는 조용히 틀리지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 영업일 함수 경계 — KST 자정 전후 · N=0 · N영업일 전 · 연말 넘김 · 모르는 해</name>
  <files>domain/holidays/business-day.ts, test/unit/holidays/business-day.test.ts</files>
  <read_first>
    - `domain/holidays/business-day.ts`(Task 1 결과)
  </read_first>
  <behavior>
    - `toKstDate(new Date("2026-10-06T14:59:59Z"))` = `"2026-10-06"` · `toKstDate(new Date("2026-10-06T15:00:00Z"))` = `"2026-10-07"` — 함수가 `Asia/Seoul`을 명시하므로 결과는 프로세스 시간대와 무관하다(테스트는 두 순간만 단언한다)
    - `addBusinessDays("2026-10-07", 0)` = `"2026-10-07"` · `addBusinessDays("2026-10-10", 0)`(토) = `"2026-10-12"`
    - `addBusinessDays("2026-10-06", -1)` = `"2026-10-02"`(10-05 대체·10-03 토·10-04 일을 건너뜀)
    - `addBusinessDays("2026-12-31", 1)` = `"2027-01-04"` — lookup이 2027을 요청한다(호출된 해 목록에 2027이 있음을 단언)
    - lookup이 모르는 해에 대해 던지면 그 오류가 그대로 전파된다(빈 집합으로 삼키지 않는다)
  </behavior>
  <action>
① behavior 다섯 줄을 `business-day.test.ts`에 더한다(RED). lookup은 `generateHolidayRules(year)`의 날짜 집합을 돌려주는 테스트용 함수이고, 호출된 해를 기록한다.

② 실패하는 케이스만 `business-day.ts`를 고친다. 비영업일 N=0은 다음 영업일로 굴린다(`<probe_fallback>`). 함수 안에서 lookup 결과를 해마다 한 번만 부르도록 지역 캐시를 둔다(같은 해를 반복 조회하지 않게 — 04.2-05가 DB 읽기를 lookup 뒤에 둘 때 비용이 된다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/holidays/business-day.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — KST 자정 경계에서 하루 밀렸거나, 비영업일 N=0이 그 날을 돌려줬거나, 음수 N이 공휴일을 세었거나, 연말을 넘길 때 다음 해 집합을 요청하지 않았거나, 모르는 해의 오류를 삼켰다</fails_when>
  </verify>
  <acceptance_criteria>
    - behavior 다섯 줄이 이름 있는 `it`로 있다
    - `pnpm vitest run --project unit test/unit/holidays` 전체 초록
  </acceptance_criteria>
  <done>영업일·N영업일 전/후가 KST 날짜로만 계산되고 연말·모르는 해에서 조용히 틀리지 않는다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 코드 내장 음력 표 → 영업일 계산 | 표 값 하나가 틀리면 그 해 지급일·마감·알림 날짜가 조용히 틀린다(Phase 7 소비) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-10 | Tampering | `LUNAR_HOLIDAY_TABLE` 값 | medium | mitigate | 표 전 해를 Node ICU 단기력으로 되읽는 단위 테스트(Task 2) — 손 편집 실수가 CI에서 실패한다. 공식 고시 대조는 A1 사람 확인 |
| T-4.2-11 | Denial of Service | 표 범위 밖 해 | medium | mitigate | 범위 밖 해는 `LunarTableRangeError`(빈 목록 금지) + 마지막 해가 올해 + 2 미만이면 테스트 실패(Task 2) |
| T-4.2-12 | Tampering | KST/UTC 변환 | low | mitigate | 날짜 문자열 + UTC 산술만 쓰고 로컬 시간대 메서드를 금지(Task 1 Grep 수용 기준 · Task 3 경계 테스트) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개(음력 변환 라이브러리 기각 — RESEARCH Package Legitimacy Audit) — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/holidays` 초록
- `pnpm lint` · `pnpm typecheck` 통과
- `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음

</verification>

<success_criteria>

- 규정 제2·3조 후보가 2025·2026·2027 전체 목록과 2025~2035 성질 검사로 증명되고, 음력 표는 ICU로 교차 검증된다
- 영업일·N영업일 전/후가 KST 날짜로만 계산되며 연말·표 밖의 해에서 조용히 틀리지 않는다
- A1(공식 고시 대조)이 SUMMARY의 사람 확인 항목으로 남는다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 상수 | `LUNAR_HOLIDAY_TABLE`(2025~2035) · `LUNAR_TABLE_FIRST_YEAR` · `LUNAR_TABLE_LAST_YEAR` · `HOLIDAY_KINDS` · `HOLIDAY_KIND_LABELS` |
| 타입 | `RuleHoliday` · `HolidayKind` · `HolidayLookup` |
| 함수(순수) | `generateHolidayRules(year)` · `toKstDate(instant)` · `isBusinessDay(date, lookup)` · `addBusinessDays(date, n, lookup)` |
| 오류 | `LunarTableRangeError`(`UserFacingError` 하위, 메시지 = UI-SPEC S2 음력 표 오류 문구) |
| 테스트 | `test/unit/holidays/rules.test.ts` · `test/unit/holidays/business-day.test.ts` |
| 결정·정정 | D-4210(대체는 규칙 공휴일에서만 · 근로자의 날 미포함) · RESEARCH 정정(2027 설날 2/7 · 3·1절 대체 대상) · A1 사람 확인 |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-02-SUMMARY.md` when done
</output>
