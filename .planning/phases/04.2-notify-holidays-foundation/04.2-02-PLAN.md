---
phase: 04.2-notify-holidays-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - domain/holidays/lunar-table.ts
  - domain/holidays/rules.ts
  - domain/holidays/business-day.ts
  - test/unit/holidays/rules.test.ts
  - test/unit/holidays/business-day.test.ts
  - test/unit/holidays/official-calendar.ts
autonomous: false
requirements: [ADMN-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`generateHolidayRules(year, { blockers })`가 관공서의 공휴일에 관한 규정 제2조(법정 공휴일)와 제3조(대체공휴일)로 그 해 후보를 날짜 오름차순·날짜 유일로 돌려준다. 법정 공휴일 목록은 코드 안 **버전 있는 데이터**(`STATUTORY_HOLIDAYS` — 항목마다 날짜 규칙·대체 대상 여부·효력 시작 해·효력 끝 해)이고 규칙 함수는 그 해에 효력 있는 항목만 쓴다. 선거일·임시공휴일은 규칙이 만들지 않는다(관리자 수동 — 04.2-12 · 초기 목록 — 아래)"
    - "2026·2027 규칙 결과(법정·대체)는 Task 2 체크포인트에서 사람이 공식 출처(law.go.kr 현행 규정 · 우주항공청/한국천문연구원 월력요항)로 확인한 목록과 날짜·구분이 같다 — 기대 목록은 `test/unit/holidays/official-calendar.ts` 한 곳에 출처와 확인 날짜를 적어 두고, 개수를 계획에 박아 두지 않는다(Codex #1)"
    - "노동절(5월 1일)·제헌절(7월 17일)의 법정 공휴일 여부·효력 시작 해·대체공휴일 대상 여부는 **확인 전 가정(A1-b)**이다 — Task 2 체크포인트 답이 오기 전에는 `STATUTORY_HOLIDAYS`에 넣지도 빼지도 않고 기대 목록을 굳히지 않는다"
    - "대체일은 **막는 날**을 따로 받는다: 대체일을 정할 때 규칙 공휴일·이미 정한 대체일에 더해 `blockers`(수동 임시공휴일·선거일 날짜)도 공휴일로 치고 건너뛴다. 대체를 **일으키는** 날은 규정 제3조의 법정 공휴일뿐이다(수동 날짜는 대체를 일으키지 않는다 — D-4210, Codex #2)"
    - "대체일 검색은 연말에서 멈추지 않는다 — 12월 공휴일의 대체일 자리가 막혀 있으면 다음 해 1월의 첫 비공휴일 평일이 대체일이고(결과에 다음 해 날짜가 들어간다), 그 검색은 다음 해의 법정 공휴일(1월 1일 등)도 공휴일로 친다. 예: 2027-12-27~31이 막히면 2027 성탄절 대체일은 2028-01-03이다(Codex 2차 #5)"
    - "초기 수동 공휴일 목록(`INITIAL_MANUAL_HOLIDAYS` — 이 시스템을 켜기 전에 지난 선거일·임시공휴일, 예: 2026-06-03 지방선거일)은 체크포인트에서 확인한 값만 담긴 코드 안 데이터이고, 04.2-06의 후보 생성이 규칙 후보와 함께 넣는다(Codex #16)"
    - "내장 음력 표(2025~2035)의 설날·부처님오신날·추석 양력 날짜는 Node ICU 단기력(`ko-KR-u-ca-dangi`, `Asia/Seoul`)으로 되읽으면 음력 1월 1일·4월 8일·8월 15일이다 — 표를 손으로 고칠 때 틀리면 단위 테스트가 실패한다. ICU 대조는 음력 변환만 검증하고 법정 공휴일 목록은 검증하지 않는다"
    - "음력 표 마지막 해가 KST 올해 + 2 이상이 아니면 단위 테스트가 실패한다(표 갱신 알림 — 조용히 틀린 해를 계산하지 않게)"
    - "addBusinessDays(d, N)은 N=0이면 d가 영업일일 때 d 자신이고, 연말을 넘는 계산(12/31 + 1영업일)은 다음 해 표(확정 또는 후보)를 읽는다. 내장 음력 표 범위 밖 연도 요청은 조용히 틀린 값 대신 오류를 낸다"
    - "설·추석 연휴가 일요일·다른 공휴일과 겹치면 규정 제3조대로 그 다음 첫 비공휴일 평일에 대체공휴일이 생기고, 같은 날짜 두 공휴일은 표에 한 행(날짜 유니크)만 남는다"
    - "영업일 계산은 KST 달력 날짜(시각 없음)로 하며 UTC 변환으로 하루 밀리지 않는다 — 단위 테스트가 KST 자정 전후 입력을 덮는다"
  artifacts:
    - path: "domain/holidays/lunar-table.ts"
      provides: "2025~2035 설날·부처님오신날·추석 양력 날짜 표와 범위 상수"
      exports: ["LUNAR_HOLIDAY_TABLE", "LUNAR_TABLE_FIRST_YEAR", "LUNAR_TABLE_LAST_YEAR"]
    - path: "domain/holidays/rules.ts"
      provides: "버전 있는 법정 공휴일 데이터 · 규정 제2·3조 후보 생성 순수 함수(막는 날 인자) · 초기 수동 목록 · 구분 값·라벨 · 범위 오류"
      exports: ["STATUTORY_HOLIDAYS", "INITIAL_MANUAL_HOLIDAYS", "generateHolidayRules", "RuleHoliday", "HOLIDAY_KINDS", "HolidayKind", "HOLIDAY_KIND_LABELS", "LunarTableRangeError"]
    - path: "domain/holidays/business-day.ts"
      provides: "KST 날짜·시각 문자열 · 영업일 판정 · N영업일 전/후 순수 함수"
      exports: ["toKstDate", "formatKstMinute", "formatKstTime", "isBusinessDay", "addBusinessDays", "HolidayLookup"]
    - path: "test/unit/holidays/official-calendar.ts"
      provides: "체크포인트에서 확인한 2026·2027 공식 목록(출처 URL · 확인 날짜 머리 주석)"
  key_links:
    - from: "domain/holidays/rules.ts"
      to: "domain/holidays/lunar-table.ts"
      via: "`generateHolidayRules`가 그 해 음력 세 날짜를 표에서 읽고, 없으면 `LunarTableRangeError`"
      pattern: "LUNAR_HOLIDAY_TABLE"
    - from: "domain/holidays/business-day.ts"
      to: "HolidayLookup"
      via: "날짜마다 그 해의 공휴일 집합을 조회 함수로 받는다 — 연말을 넘으면 다음 해 집합을 요청한다"
      pattern: "HolidayLookup"
    - from: "test/unit/holidays/rules.test.ts"
      to: "test/unit/holidays/official-calendar.ts"
      via: "2026·2027 전체 목록 단언의 기대값은 이 파일 한 곳에서만 온다"
      pattern: "official-calendar"
---

<objective>
**As a** 경영관리와 관리자, **I want to** 법정 규칙으로 해마다 공휴일 후보가 정확히 계산되고 영업일·N영업일 전/후가 한 함수로 나오기를, **so that** 260907 「알림만 목요일 고정」처럼 지급일·마감·알림이 서로 다른 달력을 쓰는 일이 다시 생기지 않는다.

(스토리는 ROADMAP 04.2 Goal 줄과 기준 1에서 끌어왔다.)

순수 함수 플랜이다 — DB·화면 없이 규칙과 계산만 만든다. 공휴일 표(DB)와 지연 생성·tick 연결은 04.2-06, 관리자 화면은 04.2-11·12가 이 함수 위에 붙는다.

Purpose: ADMN-11 「법정 공휴일 규칙으로 매년 후보가 자동 생성」과 「영업일·N영업일 전/후 계산은 도메인 함수이고 단위 테스트가 설·추석 연휴와 대체공휴일을 덮는다」(ROADMAP 기준 1). CONTEXT 이미 확정된 입력 「공휴일 표」·「한 표 원칙」, Claude's Discretion 「음력 변환 데이터는 코드 내장 표」.

Output: 음력 표 · 버전 있는 법정 공휴일 데이터와 규칙 생성 · 초기 수동 목록 · 영업일·KST 함수 · 공식 목록 기대값 파일 · 단위 테스트 둘. **사람 확인 체크포인트 하나(Task 2 — 자동 승인 불가).**

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개(`korean-lunar-calendar`는 RESEARCH가 기각했다 — 쓰지 않는다). `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — 「Code Examples」의 규정 제2·3조 규칙 스케치, Assumption A1(Grep `A1`)
# docs/inputs/phase-07-schedule-notify.md §1(공휴일 표)
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 ADMN-11 boundary(#1)·adjacency(#2)·precision(#5)이다(#1의 「다음 해 표를 읽는다」의 DB 쪽은 04.2-06이 다시 증명한다).

**계획 단계 가정·정정(표면화):**

- **RESEARCH 정정 둘(플래너가 ICU·규정으로 확인):** (1) 2027년 설날은 2월 7일(일)이다 — RESEARCH의 「2월 6일」은 틀렸다(Node 22 ICU 78 단기력과 KST 합삭 시각 기준). 그래서 2027 설 연휴는 02-06(토)·02-07(일)·02-08(월)이고 일요일이 끼어 02-09(화)가 대체공휴일이다. UI-SPEC S2-b 와이어프레임의 2027 행도 이 날짜로 읽는다(예시 그림이다). (2) 3·1절은 대체공휴일 대상이다(2021년 개정 제3조 — 국경일 전부) — 그래서 2026-03-01(일) → 03-02(월) 대체공휴일이 생긴다. RESEARCH의 「3·1절 제외」는 틀렸다. 두 정정도 Task 2 체크포인트의 공식 목록으로 다시 확인한다.
- **A1-b(Codex #1 — 확인 전 가정):** Codex는 2026년 개정으로 노동절(5/1)·제헌절(7/17)이 관공서 공휴일이 되었고 그래서 2027년에 05-03·07-19 대체공휴일이 생긴다고 지적했다(출처: law.go.kr lsiSeq=285779 개정문, 우주항공청 2027 월력요항 plcyBrfNo=431). 플래너 세션에서 두 출처는 네트워크 차단(egress 거부)으로 열리지 않았고, 플래너도 Codex도 기억만으로는 믿을 수 없다. 그래서 (가) 법정 공휴일 목록을 효력 시작·끝 해가 있는 데이터로 두고 (나) 두 날의 포함 여부·효력 해·대체 대상 여부를 Task 2 체크포인트에서 사람이 공식 출처로 확인한 뒤에만 데이터와 기대 목록에 넣으며 (다) 계획·테스트 어디에도 확인 전 개수(예: 「2026년 19행」)를 박지 않는다. RESEARCH A1(월력요항 대조)도 같은 체크포인트에서 닫는다.
- **D-4210 대체 규칙의 범위(개정 — Codex #2):** 대체를 **일으키는** 날은 제3조가 정한 법정 공휴일뿐이다 — 관리자가 넣은 임시공휴일·선거일은 대체공휴일을 만들지 않는다. 그러나 대체일은 「다음의 첫 번째 비공휴일」이므로 임시공휴일·선거일은 대체일 자리를 **막는다** — `generateHolidayRules(year, { blockers })`의 `blockers`가 그 날짜 집합이다. DB의 수동 행을 막는 날로 넘기는 일과 수동 행이 나중에 생겼을 때 미래 대체일을 다시 계산하는 일은 04.2-06이다.
- **대체일 규칙(제3조):** (가) 국경일(3·1절·광복절·개천절·한글날 — 체크포인트 결과에 따라 제헌절)·어린이날·부처님오신날·기독탄신일(·체크포인트 결과에 따라 노동절)이 토·일이면 대체 (나) 설·추석 연휴 사흘 중 일요일이 끼면 대체(토요일은 해당 없음) (다) (가)·(나)의 날이 다른 공휴일과 같은 날이면 대체 (라) 대체일은 그 공휴일(연휴면 연휴 마지막 날) 다음의 첫 평일(월~금)이면서 규칙 공휴일·이미 정한 대체일·`blockers`가 아닌 날 — 검색이 12월 31일을 넘으면 다음 해 1월로 이어지고, 그때는 다음 해의 효력 있는 법정 공휴일 날짜도 공휴일로 친다(양력 항목은 늘, 음력 항목은 다음 해가 음력 표 안일 때 — 표 밖인데 검색이 다음 해 1월 20일(설 연휴가 올 수 있는 가장 이른 날)에 닿으면 `LunarTableRangeError(year + 1)`. Codex 2차 #5) (마) 1월 1일·현충일·선거일은 대체하지 않는다. 대체가 필요한 날마다 대체일 하나이고 날짜순으로 정한다. 대체 대상 여부는 `STATUTORY_HOLIDAYS` 항목의 `substitutable` 칸이 정한다.
- **D-4221 초기 수동 목록(Codex #16):** 9월 배포 시점에 이미 지난 2026-06-03 지방선거일은 관리자 화면의 「내일 이후만」 규칙 때문에 넣을 수 없다. 그래서 확인된 지난 선거일·임시공휴일을 `INITIAL_MANUAL_HOLIDAYS`(`{ date, name, kind: "temporary" | "election" }[]`) 코드 데이터로 두고 04.2-06의 후보 생성이 규칙 후보와 한 문장으로 넣는다(계산보다 먼저). 이후의 대화형 추가는 미래 날짜만이다(04.2-12). 목록 내용은 체크포인트 답으로만 채운다.
- **이름:** `1월 1일` · `설날 연휴`(전날·다음날) · `설날` · `3·1절` · `어린이날` · `부처님오신날` · `현충일` · `광복절` · `추석 연휴` · `추석` · `개천절` · `한글날` · `기독탄신일`(체크포인트 결과에 따라 `노동절`·`제헌절`). 대체일의 이름은 원래 공휴일 이름(연휴는 `설날`·`추석`)이고 구분이 `substitute`다(UI-SPEC S2-c). 같은 날짜 두 공휴일은 한 행, 이름은 ` · `로 잇는다(예: 2025-05-05 `어린이날 · 부처님오신날`).
- **N=0과 비영업일:** `addBusinessDays(d, 0)`은 d가 영업일이면 d, 아니면 다음 영업일이다. N < 0은 N영업일 전이다.
- **연도 조회:** 영업일 함수는 동기 순수 함수라 공휴일 집합을 `HolidayLookup = (year: number) => ReadonlySet<string>`로 받는다. 조회 함수가 모르는 해에 던지는 오류는 그대로 전파한다 — 04.2-06의 `calendar.ts`가 그 오류를 받아 그 해만 읽고 다시 계산한다(지연 적재 — Codex #15).
- **KST 표시 도우미:** `formatKstMinute`(`YYYY-MM-DD HH:MM`)·`formatKstTime`(`HH:MM`)을 `toKstDate`와 같은 방식(`Asia/Seoul` 명시)으로 여기 둔다 — 알림함(04.2-09)·공휴일 화면(04.2-11)·시스템 상태(04.2-13)가 같은 함수를 쓴다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 규칙 후보 → 영업일 계산 한 길(2026-02-13 금 + 1영업일 = 설 연휴·주말을 건너 02-19 목)과 KST 경계</name>
  <files>domain/holidays/lunar-table.ts, domain/holidays/rules.ts, domain/holidays/business-day.ts, test/unit/holidays/rules.test.ts, test/unit/holidays/business-day.test.ts</files>
  <read_first>
    - `domain/action-log/record.ts` 10~70행 — `as const` 배열 + 라벨 `Record` 모양(구분 값·라벨의 본보기)
    - `lib/actions/user-facing-error.ts` — `UserFacingError`(범위 오류의 부모)
    - `.planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md` 규정 규칙 스케치(Grep `제3조`)
    - `.planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md` S2-c(이름·구분 규칙, Grep `### S2-c`)
  </read_first>
  <behavior>
    - `generateHolidayRules(2026)`의 결과에 설 연휴 02-16·02-17·02-18과 추석 연휴 09-24·09-25·09-26이 있고 03-02가 `kind: "substitute"`다(부분 단언 — 전체 목록은 Task 3)
    - `addBusinessDays("2026-02-13", 1, lookup)` = `"2026-02-19"` (lookup은 2026 규칙 날짜 집합)
    - `isBusinessDay("2026-09-25", lookup)` 거짓(추석) · `isBusinessDay("2026-10-07", lookup)` 참
    - `toKstDate(new Date("2026-10-06T14:59:59Z"))` = `"2026-10-06"` · `toKstDate(new Date("2026-10-06T15:00:00Z"))` = `"2026-10-07"` — 함수가 `Asia/Seoul`을 명시하므로 결과는 프로세스 시간대와 무관하다 · `formatKstMinute(new Date("2026-09-24T00:00:00Z"))` = `"2026-09-24 09:00"` · `formatKstTime` 같은 순간 = `"09:00"`
    - `addBusinessDays("2026-10-07", 0)` = `"2026-10-07"` · `addBusinessDays("2026-10-10", 0)`(토) = `"2026-10-12"`
    - `addBusinessDays("2026-10-06", -1)` = `"2026-10-02"`(10-05 대체·10-03 토·10-04 일을 건너뜀)
    - `addBusinessDays("2026-12-31", 1)` = `"2027-01-04"` — lookup이 2027을 요청한다(호출된 해 목록에 2027이 있음을 단언)
    - lookup이 모르는 해에 대해 던지면 그 오류가 그대로 전파된다(빈 집합으로 삼키지 않는다)
  </behavior>
  <action>
① `test/unit/holidays/rules.test.ts`·`business-day.test.ts`(신규)에 behavior를 먼저 쓴다(RED). lookup은 `generateHolidayRules(year)`의 날짜 집합을 돌려주는 테스트용 함수이고, 호출된 해를 기록한다. 이 Task의 단언은 노동절·제헌절(A1-b)에 기대지 않는 날짜만 쓴다.

② `domain/holidays/lunar-table.ts`(신규): `LUNAR_HOLIDAY_TABLE: Readonly<Record<number, { seollal: string; buddhasBirthday: string; chuseok: string }>>`에 2025~2035 열한 해의 양력 날짜(`YYYY-MM-DD`)를 넣는다 — 값: 2025 `01-29`·`05-05`·`10-06` / 2026 `02-17`·`05-24`·`09-25` / 2027 `02-07`·`05-13`·`09-15` / 2028 `01-27`·`05-02`·`10-03` / 2029 `02-13`·`05-20`·`09-22` / 2030 `02-03`·`05-09`·`09-12` / 2031 `01-23`·`05-28`·`10-01` / 2032 `02-11`·`05-16`·`09-19` / 2033 `01-31`·`05-06`·`09-08` / 2034 `02-19`·`05-25`·`09-27` / 2035 `02-08`·`05-15`·`09-16`(각 해의 연도를 앞에 붙인다). `LUNAR_TABLE_FIRST_YEAR = 2025`, `LUNAR_TABLE_LAST_YEAR = 2035`. 파일 머리 주석 두 줄: 출처(Node ICU 단기력으로 계산, 공식 월력요항 대조는 Task 2 체크포인트) · 갱신 방법(해를 더하면 ICU 대조 테스트가 검사한다).

③ `domain/holidays/rules.ts`(신규): `HOLIDAY_KINDS = ["statutory", "substitute", "temporary", "election"] as const` · `HolidayKind` · `HOLIDAY_KIND_LABELS: Record<HolidayKind, string>` = `법정 공휴일`·`대체공휴일`·`임시공휴일`·`선거일`(UI-SPEC 카피 계약) · `RuleHoliday = { date: string; name: string; kind: "statutory" | "substitute" }` · `class LunarTableRangeError extends UserFacingError`(메시지 `{연도}년 후보를 만들지 못했습니다 · 음력 표에 없는 해 · 음력 표 갱신 필요`, `year` 필드) · `STATUTORY_HOLIDAYS`(항목 `{ name; rule: { month; day } | { lunar: "seollal" | "buddhasBirthday" | "chuseok" }; span?: "eve-and-next"; substitutable: boolean; fromYear: number; toYear: number | null }` — 이 Task에는 확인이 필요 없는 오래된 항목만 넣는다: 1월 1일·설날 연휴·3·1절·어린이날·부처님오신날·현충일·광복절·추석 연휴·개천절·한글날·기독탄신일, `fromYear`는 표 첫 해 2025) · `generateHolidayRules(year: number, opts?: { blockers?: ReadonlySet<string> }): RuleHoliday[]`. 이 Task에서는 법정 공휴일 전부 + 대체 규칙 (가)의 토·일 가지까지만 구현해 behavior가 초록이 되게 한다(연휴 일요일·겹침·병합·막는 날은 Task 3).

④ `domain/holidays/business-day.ts`(신규): `HolidayLookup` 타입 · `toKstDate(instant: Date): string`(`Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Seoul", year: "numeric", month: "2-digit", day: "2-digit" })`) · `formatKstMinute(instant: Date): string` · `formatKstTime(instant: Date): string`(둘 다 `Asia/Seoul` 명시, `hourCycle: "h23"`) · `isBusinessDay(date: string, lookup: HolidayLookup): boolean`(요일은 날짜 문자열을 UTC 정오로 만든 `Date`의 `getUTCDay()` — 로컬 시간대와 무관) · `addBusinessDays(date: string, n: number, lookup: HolidayLookup): string`. 날짜 산술은 UTC 자정 `Date`의 `setUTCDate`로만 하고 로컬 시간대 메서드를 쓰지 않는다. 함수 안에서 lookup 결과를 해마다 한 번만 부르도록 지역 캐시를 둔다. 비영업일 N=0은 다음 영업일로 굴린다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/holidays</automated>
    <fails_when>failed 개수가 1 이상이거나 "No test files found"가 나온다 — 설 연휴·추석·3·1절 대체가 규칙 결과에 없거나, 영업일 계산이 설 연휴·주말을 건너뛰지 못했거나, KST 자정 경계에서 하루 밀렸거나, 비영업일 N=0이 그 날을 돌려줬거나, 연말을 넘길 때 다음 해 집합을 요청하지 않았거나, 모르는 해의 오류를 삼켰다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(경계 — domain은 domain·repositories·lib만) 또는 tsc error로 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run --project unit test/unit/holidays` 초록, behavior 여덟 줄이 이름 있는 `it`로 있다
    - `domain/holidays/*.ts`에 `getDay()`·`getDate()`·`setDate(` 같은 로컬 시간대 메서드가 없다(`grep -nE "\.(getDay|getDate|setDate|getMonth|getFullYear|getHours)\(" domain/holidays/*.ts` 출력 없음)
    - `STATUTORY_HOLIDAYS`에 `노동절`·`제헌절` 항목이 아직 없다(`grep -c "노동절\|제헌절" domain/holidays/rules.ts`가 0 — 체크포인트 전)
    - 새 의존성 없음(`git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음)
  </acceptance_criteria>
  <done>규칙 후보와 그 위의 영업일 계산이 한 길로 맞고, KST 경계·N=0·음수·연말·모르는 해에서 조용히 틀리지 않는다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: 공식 달력 확인 — 현행 규정 제2·3조(노동절·제헌절 포함 여부·효력 해·대체 대상) · 2026·2027 월력요항 · 초기 수동 공휴일 (blocking-human, 자동 승인 불가)</name>
  <what-built>Task 1까지: 음력 표와 오래된 법정 공휴일 항목만 든 규칙·영업일 함수. 플래너 세션에서는 law.go.kr·kasa.go.kr이 네트워크 차단으로 열리지 않았다 — 실행자 세션에서 먼저 WebFetch로 두 출처를 열어 보고, 열리면 그 본문을 사람에게 함께 보인다(열리지 않으면 그 사실을 적는다). 어느 경우든 목록을 굳히는 것은 사람의 확인 뒤다.</what-built>
  <how-to-verify>
    1. law.go.kr 「관공서의 공휴일에 관한 규정」 현행 본문(Codex 인용: https://www.law.go.kr/lsInfoP.do?lsiSeq=285779&viewCls=lsRvsDocInfoR) — 제2조 목록에 노동절(5월 1일)·제헌절(7월 17일)이 있는지, 있다면 시행일(효력 시작 해)과 제3조 대체공휴일 대상에 드는지 본다. 3·1절이 대체 대상인지(2021년 개정)도 본다.
    2. 우주항공청(또는 한국천문연구원) 2026·2027 월력요항(Codex 인용: https://www.kasa.go.kr/prog/plcyBrf/brief/kor/sub01_01_04/view.do?plcyBrfNo=431) — 두 해의 공휴일·대체공휴일 날짜와 이름 전체 목록을 받아 적는다(설날 2027-02-07 · 2027-02-09 대체 · 2026-03-02 대체 포함 여부 확인).
    3. 이미 지난 2026년 선거일·임시공휴일(예: 2026-06-03 제9회 전국동시지방선거)과 2027년에 이미 지정된 임시공휴일이 있는지 본다 — 초기 수동 목록의 내용이다.
  </how-to-verify>
  <resume-signal>세 가지 답을 적는다: ① 노동절·제헌절 각각 「법정 공휴일 아님」 또는 「법정 공휴일 · 효력 시작 YYYY · 대체 대상 예/아니오」 ② 2026·2027 공식 목록(날짜 · 이름 · 법정/대체)과 출처 URL ③ 초기 수동 목록(날짜 · 이름 · 선거일/임시공휴일). 출처를 확인할 수 없으면 「보류」라고 적는다 — 보류면 Task 3을 시작하지 않고 멈춘다</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 확인된 달력으로 규칙 마무리 — 버전 있는 데이터 · 대체 가지 전부(연휴 속 일요일·겹침·같은 날 병합·막는 날) · 2026·2027 공식 목록 대조 · 표 전 해 ICU 대조 · 범위 오류 · 표 만료 알림 · 초기 수동 목록</name>
  <files>domain/holidays/rules.ts, test/unit/holidays/rules.test.ts, test/unit/holidays/official-calendar.ts</files>
  <precondition>Task 2 체크포인트에 사람의 답(보류 아님)이 있다 — 없으면 이 Task를 시작하지 않는다</precondition>
  <read_first>
    - `domain/holidays/rules.ts`·`domain/holidays/lunar-table.ts`(Task 1 결과)
    - Task 2 체크포인트의 답 세 가지
    - 이 플랜 `<probe_fallback>` 「대체일 규칙(제3조)」·「D-4210」·「D-4221」·「이름」
  </read_first>
  <behavior>
    - 2026·2027: `generateHolidayRules(year)`의 날짜·구분 쌍 배열이 `official-calendar.ts`의 그 해 목록(체크포인트 답 ②에서 수동 행을 뺀 것)과 `toEqual`이다
    - 2027 설 연휴 속 일요일(02-07) → 02-09 대체(공식 목록과 같은지로 확인)
    - 2025: 03-01(토) → 03-03 대체 · 05-05 한 행 이름 `어린이날 · 부처님오신날` → 05-06 대체 · 추석 연휴 10-05(일)~10-07 → 10-08 대체(10-03 개천절·10-09 한글날과 겹치지 않는 첫 평일)
    - 막는 날(Codex #2): 2027 개천절 10-03(일)의 대체일 10-04에 대해 `blockers = {"2027-10-04"}` → 그 해 결과에 10-04가 없고 개천절 대체가 10-05다 · `blockers`는 대체를 일으키지 않는다(수동 날짜가 토·일이어도 대체 행이 생기지 않는다)
    - 교차 연도(Codex 2차 #5): `generateHolidayRules(2027, { blockers: 2027-12-27~12-31 다섯 날 })` → 기독탄신일 대체 행이 `{ date: "2028-01-03", kind: "substitute" }`이고 결과의 마지막 행이다 · 같은 막는 날 + `"2028-01-03"` → `"2028-01-04"` · 막는 날이 없으면 결과는 2027 안의 날짜뿐이다
    - 표의 모든 해(2025~2035): 날짜 중복 없음 · 대체일은 모두 월~금 · 대체일은 다른 규칙 공휴일과 겹치지 않음 · 결과가 날짜 오름차순
    - 표의 모든 해: `Intl.DateTimeFormat("ko-KR-u-ca-dangi", { timeZone: "Asia/Seoul", month: "numeric", day: "numeric" }).formatToParts`로 세 날짜를 되읽으면 (1,1)·(4,8)·(8,15)
    - 효력 해: `STATUTORY_HOLIDAYS`의 어떤 항목이 `fromYear > year`면 그 해 결과에 없다(체크포인트가 새 항목을 넣었으면 그 항목으로, 아니면 테스트용 가짜 항목 목록을 주입하지 않고 데이터 자체의 `fromYear`로 단언한다)
    - `generateHolidayRules(2024)`·`(2036)` → `LunarTableRangeError`(메시지 그대로)
    - 표 만료 알림: `LUNAR_TABLE_LAST_YEAR >= Number(toKstDate(new Date()).slice(0, 4)) + 2`
    - `INITIAL_MANUAL_HOLIDAYS`: 모든 항목 날짜가 ISO 날짜이고 구분이 `temporary`·`election`이며 규칙 결과와 날짜가 겹치지 않는다
  </behavior>
  <action>
① `test/unit/holidays/official-calendar.ts`(신규)에 체크포인트 답 ②를 그대로 옮긴다 — `OFFICIAL_2026`·`OFFICIAL_2027`(`{ date, name, kind: "statutory" | "substitute" }[]`)와 머리 주석(출처 URL · 확인한 날짜 · 확인한 사람 「사용자」). 이 파일 밖의 테스트·계획 어디에도 해마다의 행 개수를 적지 않는다. behavior를 `rules.test.ts`에 더한다(RED). ICU 대조 케이스에서 달·날 부분이 숫자 문자열이 아니면(예: 윤달 표기) 그 해를 실패로 보고 `systematic-debugging`으로 원인을 적는다 — 테스트를 느슨하게 하지 않는다.

② 체크포인트 답 ①대로 `STATUTORY_HOLIDAYS`를 고친다 — 노동절·제헌절이 법정 공휴일이면 그 이름·날짜·`substitutable`·`fromYear`(시행 해)로 항목을 더하고, 아니면 더하지 않고 데이터 머리 주석에 「확인 결과 법정 공휴일 아님(출처·날짜)」 한 줄을 남긴다. 체크포인트 답 ③을 `INITIAL_MANUAL_HOLIDAYS`에 넣는다(답이 「없음」이면 빈 배열과 그 사실 주석).

③ `generateHolidayRules`를 `<probe_fallback>` 규칙 (가)~(마)대로 마무리한다: 그 해 효력 있는 항목만 쓰고, 먼저 법정 공휴일을 날짜별로 모아 같은 날은 한 행으로 합치고(이름 ` · `), 대체가 필요한 날(토·일 가지 · 연휴 일요일 가지 · 겹침 가지)을 날짜순으로 훑으며 대체일을 하나씩 정한다(이미 정한 대체일과 `blockers`도 「공휴일」로 친다). 대체일 검색은 연말을 넘어 다음 해 1월로 이어진다 — `<probe_fallback>` (라)의 다음 해 법정 날짜 처리 그대로(per Codex 2차 #5). 대체일 행의 이름은 원래 공휴일 이름, 연휴면 `설날`/`추석`이다.

④ 공식 목록과 규칙 결과가 어긋나면 규칙·데이터를 고친다 — 기대값(공식 목록)을 규칙에 맞춰 고치지 않는다. 어긋남의 원인이 규정 해석이면 멈추고 SUMMARY에 두 목록의 차이를 적어 보고한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/holidays</automated>
    <fails_when>failed 개수가 1 이상이다 — 2026·2027 규칙 결과가 공식 목록과 다르거나(특히 2027-02-09, 체크포인트가 확인한 노동절·제헌절과 그 대체일), 2025 대체(03-03·05-06·10-08)가 틀렸거나, 막는 날을 건너뛰지 않았거나 막는 날이 대체를 일으켰거나, 12월 대체일 검색이 연말에서 멈췄거나 다음 해 1월 1일을 대체일로 골랐거나, 같은 날 두 행이 남았거나, 음력 표 값이 ICU 단기력과 어긋나거나, 효력 전 해에 항목이 나왔거나, 범위 밖 해가 오류 없이 빈 목록을 돌려줬거나, 표 만료 알림이 빠졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc error로 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 2026·2027 목록 단언의 기대값이 `official-calendar.ts`에서만 온다(`grep -n "OFFICIAL_202" test/unit/holidays/rules.test.ts`가 두 이름을 보인다)
    - `grep -rnE "(19|20)(행|개|rows)" test/unit/holidays/*.ts domain/holidays/*.ts` 출력이 비어 있다(개수 하드코딩 없음)
    - `official-calendar.ts` 머리 주석에 출처 URL과 확인 날짜가 있다
    - ICU 대조가 `LUNAR_TABLE_FIRST_YEAR`부터 `LUNAR_TABLE_LAST_YEAR`까지 모든 해를 돈다(반복 범위를 상수로 — 해를 더하면 자동으로 검사된다)
    - 범위 밖 해의 오류 메시지가 `{연도}년 후보를 만들지 못했습니다 · 음력 표에 없는 해 · 음력 표 갱신 필요` 글자 그대로다
  </acceptance_criteria>
  <done>규정 제3조의 대체 가지·같은 날 병합·막는 날이 공식 목록 두 해와 2025 목록·2025~2035 성질 검사로 증명되고, 법정 목록은 확인된 효력 해로 버전이 매겨지며, 표 밖의 해는 조용히 틀리지 않는다</done>
  <reversibility rating="costly">법정 목록·대체 규칙이 틀린 채 배포되면 그 해의 생성된 행이 DB에 남는다 — 04.2-06 D-4205대로 이미 생성된 해는 다시 만들지 않으므로 바로잡으려면 마이그레이션이 필요하다. 그래서 이 Task는 체크포인트 뒤에만 기대 목록을 굳힌다</reversibility>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 코드 내장 음력 표·법정 목록 → 영업일 계산 | 값 하나가 틀리면 그 해 지급일·마감·알림 날짜가 조용히 틀린다(Phase 7 소비) |
| 외부 공식 달력 → 기대 목록 | 사람이 옮겨 적은 목록이 테스트의 정답이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-10 | Tampering | `LUNAR_HOLIDAY_TABLE` 값 | medium | mitigate | 표 전 해를 Node ICU 단기력으로 되읽는 단위 테스트(Task 3) — 손 편집 실수가 CI에서 실패한다 |
| T-4.2-11 | Denial of Service | 표 범위 밖 해 | medium | mitigate | 범위 밖 해는 `LunarTableRangeError`(빈 목록 금지) + 마지막 해가 올해 + 2 미만이면 테스트 실패(Task 3) |
| T-4.2-12 | Tampering | KST/UTC 변환 | low | mitigate | 날짜 문자열 + UTC 산술만 쓰고 로컬 시간대 메서드를 금지(Task 1 Grep 수용 기준 · 경계 테스트) |
| T-4.2-13 | Tampering | 법정 공휴일 목록(노동절·제헌절) | high | mitigate | 확인 전 가정은 데이터·기대값에 넣지 않는다 · blocking-human 체크포인트가 공식 출처로 확인(Task 2) · 기대값은 출처·날짜가 적힌 파일 한 곳 · 개수 하드코딩 금지 Grep(Task 3) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개(음력 변환 라이브러리 기각 — RESEARCH Package Legitimacy Audit) — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/holidays` 초록
- `pnpm lint` · `pnpm typecheck` 통과
- `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음
- Task 2 체크포인트 답(출처 URL·확인 날짜)이 SUMMARY에 그대로 있다

</verification>

<success_criteria>

- 규정 제2·3조 후보가 사람이 공식 출처로 확인한 2026·2027 목록, 2025 목록, 2025~2035 성질 검사로 증명되고, 음력 표는 ICU로 교차 검증된다
- 노동절·제헌절은 확인된 효력 해로만 데이터에 들어간다(또는 확인 결과와 함께 빠진다)
- 대체일은 수동 공휴일을 막는 날로 건너뛰고, 수동 날짜는 대체를 일으키지 않는다
- 영업일·N영업일 전/후가 KST 날짜로만 계산되며 연말·표 밖의 해에서 조용히 틀리지 않는다

</success_criteria>

## Review Dispositions (Codex 적대 계획 검토 2026-09-24)

| # | 처리 | 어디에 |
|---|------|--------|
| 1 (high) | 반영(확인 조건부) | 출처 두 곳이 플래너 세션에서 egress 차단 — 법정 목록을 효력 해 있는 데이터로 · A1-b 가정 표면화 · blocking-human 체크포인트(Task 2) · 기대 목록 파일 한 곳 · 개수 하드코딩 제거(must_haves의 19/20행 문장 삭제) |
| 2 (high) | 반영 | 「일으키는 날」과 「막는 날」 분리 — `generateHolidayRules(year, { blockers })`(Task 1·3) · 두 삽입 순서와 재계산은 04.2-06 |
| 15 (medium) | 반영 | 조회 오류를 그대로 전파하는 계약을 04.2-06의 지연 적재가 쓴다 |
| 16 (medium) | 반영 | `INITIAL_MANUAL_HOLIDAYS`(체크포인트로 확인한 지난 선거일·임시공휴일) — 04.2-06이 계산 전에 넣는다 |

### Review Dispositions (Codex 2차)

| Codex 2차 # | 처리 | 한 줄 |
|---|---|---|
| 5 (medium) — 규칙 쪽 | 반영 | 대체일 검색이 연말을 넘어 다음 해 1월로 이어지고 다음 해 법정 날짜를 공휴일로 친다 · 2027 성탄절 → 2028-01-03/01-04 단위 테스트(Task 3). `origin_year`와 원래 해 재계산은 04.2-06·12 |

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 상수·데이터 | `LUNAR_HOLIDAY_TABLE`(2025~2035) · `LUNAR_TABLE_FIRST_YEAR` · `LUNAR_TABLE_LAST_YEAR` · `STATUTORY_HOLIDAYS`(효력 해 있는 법정 목록) · `INITIAL_MANUAL_HOLIDAYS` · `HOLIDAY_KINDS` · `HOLIDAY_KIND_LABELS` |
| 타입 | `RuleHoliday` · `HolidayKind` · `HolidayLookup` |
| 함수(순수) | `generateHolidayRules(year, { blockers })` · `toKstDate(instant)` · `formatKstMinute(instant)` · `formatKstTime(instant)` · `isBusinessDay(date, lookup)` · `addBusinessDays(date, n, lookup)` |
| 오류 | `LunarTableRangeError`(`UserFacingError` 하위, 메시지 = UI-SPEC S2 음력 표 오류 문구) |
| 테스트 | `test/unit/holidays/rules.test.ts` · `test/unit/holidays/business-day.test.ts` · `test/unit/holidays/official-calendar.ts`(공식 목록 기대값) |
| 결정·정정 | D-4210(개정 — 일으키는 날/막는 날) · D-4221(초기 수동 목록) · A1-b(노동절·제헌절 — 체크포인트) · RESEARCH 정정(2027 설날 2/7 · 3·1절 대체 대상) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-02-SUMMARY.md` when done
</output>
