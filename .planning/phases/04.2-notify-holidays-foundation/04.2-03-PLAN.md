---
phase: 04.2-notify-holidays-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - domain/action-log/record.ts
  - domain/auth/hooks.ts
  - domain/auth/accounts.ts
  - domain/auth/locked-message.ts
  - domain/auth/lockout.ts
  - app/(auth)/login/login-error.ts
  - test/integration/action-log.test.ts
  - test/integration/lockout.test.ts
  - test/unit/login-error.test.ts
  - test/unit/action-log/record.test.ts
autonomous: true
requirements: [D-712]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "계정이 잠기는 순간(로그인 실패 N번째) 행동 로그에 `account_lock` 한 행이 남고, 관리자 잠금 해제는 `account_unlock` 한 행을 남긴다(D-712 · ROADMAP 04.2 기준 5)"
    - "`account_lock`·`account_unlock`은 `CORE_ACTION_TYPES`·`ACTION_TYPE_LABELS`(`계정 잠금`·`잠금 해제`)·`ALWAYS_ON_ACTION_TYPES` 세 곳에 함께 있다 — 설정으로 끌 수 없는 보안 기록이다"
    - "D-712 기록은 지금 `recordAction(viewer, entry)` 두 인자로 부른다 — Phase 4 04-32의 `RecordActionDeps.tx?` 확장과 그대로 맞는 모양이고, 두 호출 지점 모두 트랜잭션 밖이라 tx를 넘길 곳이 없다(새 시그니처를 만들지 않는다)"
    - "로그인 실패가 임계값 N-1회면 잠금 행동 로그가 없고, N번째 실패에서 잠금 행동 로그가 1건 남는다"
    - "잠금 안내 문구의 분 값은 설정 레지스트리의 잠금 시간 설정을 읽고, 설정이 기본값(15)이면 지금 문구와 같다 — 코드에 「15분」 고정 문자열이 남지 않는다"
    - "잠금 행동 로그의 행위자는 시스템(actorId null)이고 대상 이메일은 detail에 남으며, 해제 로그의 행위자는 해제한 관리자다"
    - "잠금 안내 문구는 정수 분으로 표시한다"
    - "이미 잠긴 계정에 순차로 실패가 더 와도 잠금 행동 로그는 늘지 않는다(잠긴 뒤 before 훅이 거부)"
    - statement: "동시 로그인 실패 경합에서는 잠금 행동 로그가 드물게 2건 남을 수 있으나(at-least-once, hooks.ts 주석) 0건은 없다"
      verification: backstop
    - "UI S5-lock-message/empty — 잠기지 않은 로그인 실패에는 잠금 문구가 나오지 않는다(기존 동작)"
    - "UI S5-lock-message/loading — 로그인 제출 대기 표시는 기존 로그인 폼 그대로이고 잠금 문구는 응답 뒤에만 나온다"
    - "UI S5-lock-message/error — 설정 `auth.lockout.window_minutes`를 20으로 바꾸면 잠금 문구가 `… 20분 뒤 다시 시도하거나 …`가 되고, 로그인 화면이 그 문구를 잠금 오류로 인식한다(일반 오류로 떨어지지 않는다)"
    - "UI S5-lock-message/partial — 남은 대기 시간은 표시하지 않는다 — 문구의 N은 설정값(최대 대기) 하나뿐이다"
    - "UI S5-lock-message/overflow — 문구는 기존 로그인 폼 오류 자리(FormAlert)에서 줄바꿈되고 로그인 폼 폭을 넘지 않는다"
    - "UI S5-lock-message/long-text — N 외의 글자는 기존 문구와 같다: `로그인 시도가 너무 많습니다. {N}분 뒤 다시 시도하거나 관리자에게 문의하세요.`"
  artifacts:
    - path: "domain/auth/locked-message.ts"
      provides: "잠금 문구를 분 숫자 하나로 만드는 함수와 문구 판정 — import 없는 잎 모듈"
      exports: ["lockedMessage", "isLockedMessage"]
    - path: "domain/action-log/record.ts"
      provides: "핵심 행동 종류 두 개 추가(세 배열 함께)"
      contains: "account_lock"
    - path: "test/integration/action-log.test.ts"
      provides: "잠금 N-1/N/N+1 · 해제 행위자 통합"
  key_links:
    - from: "domain/auth/hooks.ts"
      to: "domain/action-log/record.ts"
      via: "`after` 훅의 잠금 가지가 `recordAction(SYSTEM_VIEWER, { actionType: \"account_lock\", detail })`"
      pattern: "account_lock"
    - from: "domain/auth/accounts.ts"
      to: "domain/action-log/record.ts"
      via: "`unlockAccount`가 `recordAction(viewer, { actionType: \"account_unlock\", detail })`"
      pattern: "account_unlock"
    - from: "domain/auth/hooks.ts"
      to: "domain/auth/locked-message.ts"
      via: "`before` 훅이 `lockoutConfig()`의 `windowMinutes`로 `lockedMessage(windowMinutes)`를 던진다"
      pattern: "lockedMessage"
    - from: "app/(auth)/login/login-error.ts"
      to: "domain/auth/locked-message.ts"
      via: "403 + `isLockedMessage(message)`면 그 문구를 그대로 보인다"
      pattern: "isLockedMessage"
---

<objective>
**As a** 관리자와 감사 담당, **I want to** 계정이 잠기고 풀린 일이 행동 로그에 남고 잠금 안내 문구가 실제 잠금 설정값을 말하기를, **so that** 서버 JSON 로그를 뒤지지 않고도 누가 언제 잠겼고 누가 풀었는지 확인할 수 있고, 관리자가 잠금 시간을 바꿔도 직원이 틀린 안내를 보지 않는다.

(스토리는 ROADMAP 04.2 Goal 줄 「계정 잠금·해제가 행동 로그에 남는다」와 기준 5에서 끌어왔다.)

Purpose: D-712(Phase 1 성공 기준 2의 미이행분) — 잠금·해제 행동 로그와, 코디네이터 결정 「잠금 안내 문구는 설정값을 읽어 표시」.

Output: 핵심 행동 종류 두 개(세 배열 함께) · 두 기록 지점 · 문구 함수와 판정 · 로그인 화면 판정 교체 · 통합·단위 테스트.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개. 행동 로그 화면(`app/(app)/admin/action-log`)은 고치지 않는다 — 라벨 배열만 늘리면 화면이 따라온다(UI-SPEC S5).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-PATTERNS.md — 「domain/action-log/record.ts (modified …)」·「recordAction tx-aware call」·「domain/auth/locked-message.ts」 절(Grep)
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — S5(Grep `## S5`)
# `git show origin/claude/gsd-progress-e1nzgu:domain/action-log/record.ts` — 04-32의 `RecordActionDeps.tx?` 모양(읽기만, 이 플랜은 04-32를 구현하지 않는다)
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 D-712 여섯 줄(#26~#31, #31은 backstop)과 UI-SPEC S5 여섯 줄이다. 금지 회상에서 「잠금 문구가 남은 시간이나 계정 존재 여부를 드러내는 것」은 정규 보안(사용자 열거 — OWASP)으로 넘겼다 — S5/partial truth와 기존 원칙(문구는 계정 존재와 무관하게 같다)이 덮는다.

**계획 단계 결정(표면화):**

- **D-4207 행위자 표현:** 잠금 — 로그인 전이라 행위자가 없으므로 `SYSTEM_VIEWER`(기존 `recordAction`이 `actorId: null`로 적는다) + `detail: { email, threshold, windowMinutes }`. 해제 — 호출자 viewer + `detail: { email, resolved }`. 관리자 화면 해제는 아직 없고 지금 유일한 호출자는 `scripts/account-cli.ts`(SYSTEM_VIEWER)라 CLI 해제는 행위자가 `—`(null)로 남는다 — 통합 테스트는 관리자 viewer로 불러 행위자 = 그 관리자를 단언한다. `entity`는 `"user"`, `entityId`는 잠금 대상 사용자 id가 있으면 그 id, 없으면(없는 이메일 — 잠금은 계정 존재와 무관하게 동작한다) `null`.
- **04-32 시그니처:** 두 호출 지점 모두 트랜잭션 밖이다(`after` 훅 · `unlockAccount`). 그래서 `recordAction(viewer, entry)` 두 인자로 부르고 deps를 넘기지 않는다 — 04-32가 main에 들어와도 이 호출은 그대로 맞는다(ROADMAP 04.3 절의 「04-32가 main에 들어오기 전에는 지금 시그니처로 부른다」와 같은 규칙). `RecordActionDeps`에 tx를 스스로 더하지 않는다.
- **세 배열 + 누수 스캔:** 세 배열은 한 커밋에서 함께 고친다(04-RESEARCH Pitfall 2). `test/integration/leak-scan.test.ts`는 행동 종류 목록을 갖지 않고 이 플랜은 새 액션·DTO를 만들지 않아 **고칠 것이 없다** — 대신 Task 1 검증에서 돌려 초록임을 확인한다(코디네이터 메모 1의 「leak-scan 함께」를 확인으로 이행). 04.2의 새 레지스트리 import는 04.2-07이 더한다.
- **문구 판정:** `login-error.ts`의 정확 대조(`===`)는 숫자가 바뀌면 깨진다. 잎 모듈에 `isLockedMessage(text)` — 고정 접두어 `로그인 시도가 너무 많습니다. `와 고정 접미어 `분 뒤 다시 시도하거나 관리자에게 문의하세요.` 사이가 1자리 이상 숫자뿐인지 — 를 두고 로그인 화면이 그것을 쓴다(better-auth의 다른 403 영문 메시지는 계속 일반 오류로 접힌다 — /review L-3 회귀 방지). 옛 상수 이름은 쓰는 곳이 없어지므로 잎 모듈과 `lockout.ts` 재수출에서 함께 지운다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 로그인 N번째 실패 한 번이 행동 로그 `account_lock` 한 행(행위자 null · detail에 이메일)으로 끝까지 간다</name>
  <files>domain/action-log/record.ts, domain/auth/hooks.ts, test/integration/action-log.test.ts, test/unit/action-log/record.test.ts</files>
  <read_first>
    - `domain/action-log/record.ts` 10~140행 — 세 배열 · `recordAction` · `SYSTEM_VIEWER` → `actorId: null`
    - `domain/auth/hooks.ts` 55~125행 — `before`(잠금 판정·거부)와 `after`(실패 기록 → `countOpenFailures` → `isLocked`이면 `log.info("auth.lockout")`)
    - `test/integration/lockout.test.ts` 1~35행 — `signIn(email, password, ip)` 도우미 · 계정 만들기 · 고유 `x-client-ip`와 rate limit 주석
    - `test/integration/action-log.test.ts` — `queryActionLog(SYSTEM_VIEWER, { actionType })` 사용 모양
    - `test/unit/action-log/record.test.ts` — 부분집합·라벨 단언 모양
  </read_first>
  <behavior>
    - 잠금 임계값 기본(설정 `auth.lockout.threshold`)에서 N-1회 실패 → `queryActionLog(SYSTEM_VIEWER, { actionType: "account_lock" })` 0행
    - N번째 실패 → 1행 · `actorId` null · `detail.email` = 그 이메일 · `detail.threshold`·`detail.windowMinutes`가 숫자
    - N+1번째 시도(잠긴 뒤 — `before` 훅이 403) → 여전히 1행
    - 단위: `account_lock`·`account_unlock`이 `CORE_ACTION_TYPES`에 있고 `ACTION_TYPE_LABELS`가 `계정 잠금`·`잠금 해제`이며 `ALWAYS_ON_ACTION_TYPES`에 있다
  </behavior>
  <action>
① `test/integration/action-log.test.ts`에 `describe("계정 잠금·해제 행동 로그 (D-712)")`를 더하고 behavior 세 통합 케이스를 쓴다(RED). 로그인 도우미는 `lockout.test.ts`의 `signIn` 모양을 이 파일 안 지역 함수로 둔다(기존 파일을 공용화하지 않는다). 블록마다 고유 `x-client-ip`, 블록당 로그인 10회 이하(rate limit 주석). `test/unit/action-log/record.test.ts`에 단위 케이스 하나를 더한다.

② `domain/action-log/record.ts`의 세 배열에 **항목만** 더한다: `CORE_ACTION_TYPES` 끝에 `"account_lock"`, `"account_unlock"` · `ACTION_TYPE_LABELS`에 `account_lock: "계정 잠금"`, `account_unlock: "잠금 해제"` · `ALWAYS_ON_ACTION_TYPES` 끝에 둘(주석 한 줄: 「D-712: 계정 잠금·해제는 보안 기록이라 끌 수 없다」). 다른 줄을 고치지 않는다(Phase 4 브랜치도 이 파일을 고친다 — 머지 충돌 면을 줄인다).

③ `domain/auth/hooks.ts` `after` 훅의 `isLocked` 가지에서 기존 `log.info("auth.lockout", …)`는 두고 그 뒤에 `await recordAction(SYSTEM_VIEWER, { actionType: "account_lock", entity: "user", entityId: 사용자 id 또는 null, detail: { email, threshold, windowMinutes } })`를 더한다(D-4207). 사용자 id는 이미 가진 값이 없으면 `findUserByEmail(SYSTEM_VIEWER, email)`로 찾는다(before 훅이 쓰는 저장소 함수 — 새 저장소 함수를 만들지 않는다). 기존 at-least-once 주석은 그대로 둔다(엣지 #31 backstop의 근거).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/action-log.test.ts test/integration/lockout.test.ts test/integration/leak-scan.test.ts test/integration/action-log-query.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — N-1회에 잠금 행이 생겼거나, N번째에 0행이거나, 잠긴 뒤 시도가 행을 더했거나, 행위자가 null이 아니거나, detail에 이메일이 없거나, 새 종류가 행동 로그 조회·누수 스캔을 깨뜨렸다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/action-log &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>세 배열 단언 실패(한 곳만 고침) 또는 `Record<CoreActionType, string>` 누락으로 tsc error가 나서 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "account_lock" domain/action-log/record.ts`가 3 이상(세 배열)이다
    - 통합: N-1 → 0행 · N → 1행(actorId null, detail.email) · N+1 → 1행
    - `test/integration/leak-scan.test.ts`는 diff가 없고 초록이다(`git diff --stat HEAD -- test/integration/leak-scan.test.ts` 출력 없음)
    - `RecordActionDeps` 타입에 이 플랜이 더한 필드가 없다(`git diff HEAD -- domain/action-log/record.ts`에 `RecordActionDeps` 줄 변경 없음)
  </acceptance_criteria>
  <done>잠금이 행동 로그에 정확히 한 번(순차 기준) 남고 행위자는 시스템이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 잠금 해제 로그 · 잠금 문구가 설정값을 말한다(20분 설정 → 20분 문구 → 로그인 화면이 잠금으로 인식)</name>
  <files>domain/auth/accounts.ts, domain/auth/locked-message.ts, domain/auth/lockout.ts, domain/auth/hooks.ts, app/(auth)/login/login-error.ts, test/integration/action-log.test.ts, test/integration/lockout.test.ts, test/unit/login-error.test.ts</files>
  <read_first>
    - `domain/auth/accounts.ts` 95~110행 — `unlockAccount(viewer, email)`(can → resolveOpenFailures → `log.info("auth.unlock")`)
    - `domain/auth/locked-message.ts` 전체 — 잎 모듈 제약 주석(클라이언트 번들)
    - `domain/auth/lockout.ts` — `lockoutConfig()` · 38행 재수출
    - `app/(auth)/login/login-error.ts` — `loginErrorMessage` · /review L-3 주석
    - `test/unit/login-error.test.ts` — 기존 케이스(잠금 문구 · 다른 403 영문 메시지)
    - `domain/settings/registry.ts` 94행 `setSettingValue(viewer, def, value, deps?)` · `domain/settings/keys.ts` 24행 `AUTH_LOCKOUT_WINDOW_MINUTES`
  </read_first>
  <behavior>
    - 통합: 잠긴 계정을 관리자 viewer(`admin.people` write)로 `unlockAccount` → `account_unlock` 1행 · `actorId` = 그 관리자 id · `detail` = `{ email, resolved: 열린 실패 수 }`
    - 통합: `setSettingValue(관리자, AUTH_LOCKOUT_WINDOW_MINUTES, 20)` 뒤 잠긴 계정의 로그인 응답 403 본문 메시지 = `로그인 시도가 너무 많습니다. 20분 뒤 다시 시도하거나 관리자에게 문의하세요.` · 설정 기본값이면 분 숫자가 기본값과 같다
    - 단위: `lockedMessage(20)` = 위 20분 문구 · `lockedMessage(15)`가 옛 문구와 글자 하나까지 같다 · `isLockedMessage(lockedMessage(7))` 참 · `isLockedMessage("Invalid origin")` 거짓 · 숫자 자리가 비었거나 숫자가 아니면 거짓
    - 단위: `loginErrorMessage({ status: 403, message: lockedMessage(20) })` = 그 문구 · `{ status: 403, message: "Invalid origin" }` = 일반 오류 · `{ status: 401, message: lockedMessage(20) }` = 일반 오류
  </behavior>
  <action>
① behavior의 통합 두 케이스(해제는 `action-log.test.ts`, 20분 문구는 `lockout.test.ts`)와 단위 케이스(`test/unit/login-error.test.ts`의 고정 문자열 두 곳을 `lockedMessage(15)`로 바꾸고 새 케이스 추가)를 먼저 쓴다(RED).

② `domain/auth/locked-message.ts`: 잎 모듈(import 없음)을 유지하며 옛 상수를 `lockedMessage(minutes: number): string`(정수 분 — `Math.trunc` 뒤 문장에 넣는다)과 `isLockedMessage(text: string): boolean`(`<probe_fallback>` 접두어·접미어 사이 숫자 판정)으로 바꾼다. 파일 머리 주석의 「남은 시간은 표시하지 않는다」는 남긴다. `domain/auth/lockout.ts` 38행 재수출을 새 두 이름으로 바꾸거나 쓰는 곳이 없으면 지운다.

③ `domain/auth/hooks.ts` `before` 훅: 이미 읽은 `windowMinutes`로 `new APIError("FORBIDDEN", { message: lockedMessage(windowMinutes) })`. `app/(auth)/login/login-error.ts`: `error?.status === 403 && typeof error.message === "string" && isLockedMessage(error.message)`면 그 메시지, 아니면 `GENERIC_ERROR`. 파일 머리 주석의 「문구를 정확히 대조한다」 문장은 「고정 접두어·접미어로 대조한다(분 숫자는 설정값)」로 한 줄만 고친다.

④ `domain/auth/accounts.ts` `unlockAccount`: 기존 `log.info("auth.unlock")` 뒤에 `await recordAction(viewer, { actionType: "account_unlock", entity: "user", entityId: 사용자 id 또는 null, detail: { email, resolved } })`(D-4207 — 트랜잭션 밖이라 deps 없음).

⑤ 이 플랜이 고친 `domain`·`app` 파일에 분 숫자가 박힌 잠금 문장이 남지 않았는지 Grep으로 확인한다(테스트 파일의 기대값은 예외).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/login-error.test.ts test/unit/lockout.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 설정 분 숫자가 문구에 반영되지 않았거나, 옛 기본 문구와 한 글자라도 달라졌거나, 다른 403 영문 메시지가 잠금 문구로 새었거나(/review L-3 회귀), 숫자 없는 문장이 잠금으로 인식됐다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/action-log.test.ts test/integration/lockout.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 해제 로그가 없거나 행위자가 관리자가 아니거나, 20분 설정 뒤에도 403 메시지가 기본 분이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>잎 모듈이 import를 얻어 클라이언트 번들 경계(lib/env)가 깨졌거나 옛 상수를 아직 import하는 곳이 있어 eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "15분" domain app --include=*.ts --include=*.tsx | grep -v "//"` 출력이 비어 있다
    - `grep -c "^import" domain/auth/locked-message.ts`가 0이다(잎 모듈)
    - `grep -rn "LOCKED_MESSAGE" domain app lib` 출력이 비어 있다
    - 통합: 해제 → `account_unlock` 1행(actorId = 관리자) · 20분 설정 → 403 메시지에 `20분`
  </acceptance_criteria>
  <done>잠금 해제가 행위자와 함께 남고, 잠금 문구의 분 숫자가 설정값을 따르며 로그인 화면이 그 문구를 계속 잠금으로 알아본다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로그인 요청 → better-auth 훅 | 인증 전 입력(이메일)이 행동 로그 detail에 들어간다 |
| 서버 403 메시지 → 로그인 화면 | 서버가 준 문장이 판정을 지나 사용자에게 그대로 보인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-20 | Repudiation | 계정 잠금·해제 | medium | mitigate | `account_lock`·`account_unlock`을 끌 수 없는 핵심 행동으로 기록(세 배열 + 통합 N-1/N/N+1 · 해제 행위자) |
| T-4.2-21 | Information Disclosure | 잠금 문구 | medium | mitigate | 문구는 계정 존재와 무관하게 같고 남은 시간이 없다 — N은 설정 최대값 하나(S5/partial). better-auth의 다른 403 영문 메시지는 계속 일반 오류로 접힌다(단위 — /review L-3 회귀) |
| T-4.2-22 | Tampering | 행동 로그 detail | low | accept | detail의 이메일은 로그인 폼 입력 그대로다 — 행동 로그 화면은 기존처럼 텍스트로만 보인다(HTML 해석 없음, React 이스케이프). 새 표시 경로를 만들지 않는다 |
| T-4.2-23 | Denial of Service | 잠금 로그 쓰기 실패 | low | accept | `recordAction` 실패는 기존 `login` 기록과 같이 감싸지 않는다(같은 DB — 여기서 실패하면 바로 앞 실패 기록도 실패한다, hooks.ts 기존 주석) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/login-error.test.ts test/unit/lockout.test.ts test/unit/action-log` 초록
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/action-log.test.ts test/integration/lockout.test.ts test/integration/leak-scan.test.ts test/integration/action-log-query.test.ts` 초록
- `pnpm lint && pnpm typecheck && pnpm build` 통과

</verification>

<success_criteria>

- 계정 잠금·해제가 행동 로그에 남고(ROADMAP 04.2 기준 5), 행위자 표현이 D-4207대로다
- 잠금 문구가 설정값을 말하고 로그인 화면이 계속 잠금 오류로 알아본다
- Phase 4 04-32가 main에 들어와도 두 호출 지점을 고칠 필요가 없다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 핵심 행동 종류 | `account_lock`(라벨 `계정 잠금`) · `account_unlock`(라벨 `잠금 해제`) — `CORE_ACTION_TYPES`·`ACTION_TYPE_LABELS`·`ALWAYS_ON_ACTION_TYPES` |
| 함수(신규) | `lockedMessage(minutes)` · `isLockedMessage(text)`(`domain/auth/locked-message.ts`, 잎 모듈) |
| 지운 이름 | 옛 잠금 문구 상수와 `domain/auth/lockout.ts`의 그 재수출(쓰는 곳 없음) |
| 기록 지점 | `domain/auth/hooks.ts` `after` 잠금 가지 · `domain/auth/accounts.ts` `unlockAccount` |
| 테스트 | `test/integration/action-log.test.ts`(D-712 describe) · `test/integration/lockout.test.ts`(20분 문구) · `test/unit/login-error.test.ts` · `test/unit/action-log/record.test.ts` |
| 결정 | D-4207(행위자 표현) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-03-SUMMARY.md` when done
</output>
