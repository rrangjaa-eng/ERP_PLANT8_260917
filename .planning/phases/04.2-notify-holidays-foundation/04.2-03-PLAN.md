---
phase: 04.2-notify-holidays-foundation
plan: 03
type: execute
wave: 3
depends_on: [04.2-06]
files_modified:
  - domain/action-log/record.ts
  - repositories/login-attempts.ts
  - domain/auth/lockout.ts
  - domain/auth/hooks.ts
  - domain/auth/locked-message.ts
  - app/(auth)/login/login-error.ts
  - test/integration/action-log.test.ts
  - test/integration/lockout.test.ts
  - test/unit/login-error.test.ts
  - test/unit/action-log/record.test.ts
autonomous: true
requirements: [D-712]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "계정이 잠기는 순간(로그인 실패 N번째) 행동 로그에 `account_lock` 한 행이 남는다(D-712 · ROADMAP 04.2 기준 5). 잠금 해제의 `account_unlock` 기록은 04.2-08이다"
    - "`account_lock`·`account_unlock`·`holiday_change`는 `CORE_ACTION_TYPES`·`ACTION_TYPE_LABELS`(`계정 잠금`·`잠금 해제`·`공휴일 변경`)·`ALWAYS_ON_ACTION_TYPES` 세 곳에 함께 있다 — 설정(`action_log.optional_types`)으로 끌 수 없는 기록이다(D-4220 · Codex #12)"
    - "N번째 실패의 실패 기록 INSERT · 열린 실패 수 세기 · 잠금 행동 로그 INSERT는 한 트랜잭션이다 — 잠금 행동 로그 쓰기가 실패하면 그 실패 기록도 롤백되어 「잠겼는데 잠금 기록 0건」이 생기지 않고, 다음 실패가 다시 N번째가 되어 잠금 기록을 남긴다(Codex #12)"
    - "행동 로그 쓰기는 Phase 4 04-32의 tx 인자 `recordAction(viewer, entry, { tx })`로 한다 — 이 플랜은 `RecordActionDeps`를 스스로 늘리지 않는다. 04-32가 origin/main에 없으면 실행을 멈추고 보고한다(D-712 · CONTEXT 「04-32의 tx 인자 시그니처를 따른다」)"
    - "이메일 키 잠금의 `tx.execute`는 04.2-06이 `db/client.ts`에서 `\"delete\" | \"execute\"`까지 넓힌 공용 `DbOrTx`로 컴파일된다 — 이 플랜은 공용 타입 파일을 고치지 않고 지역 타입도 만들지 않는다(Codex 2차 #7)"
    - "같은 이메일의 실패 기록 트랜잭션은 그 이메일 키의 트랜잭션 advisory 잠금으로 줄을 선다 — 동시 실패 둘이 서로의 미커밋 행을 못 봐 둘 다 N-1을 세는 일이 없다"
    - "실패 기록 트랜잭션 안에서는 그 트랜잭션의 `tx`로 묶인 호출만 한다 — 사용자 조회(`findUserByEmail`)와 잠금 설정 읽기(`lockoutConfig()`)는 `withTransaction` 전에 끝나, 잠금을 쥔 트랜잭션이 전역 풀에서 두 번째 연결을 잡는 경로가 없다. 풀에 빈 연결이 1개뿐이어도 N번째 실패(잠김 가지)가 5초 안에 끝나고 `account_lock` 1행을 남긴다(eng E5 — 풀 5개가 같은 이메일 대기로 다 차면 교착)"
    - "로그인 실패가 임계값 N-1회면 잠금 행동 로그가 없고, N번째 실패에서 잠금 행동 로그가 1건 남는다"
    - "잠금 안내 문구의 분 값은 설정 레지스트리의 잠금 시간 설정을 읽고, 설정이 기본값(15)이면 지금 문구와 같다 — 코드에 「15분」 고정 문자열이 남지 않는다"
    - "잠금 행동 로그의 행위자는 시스템(actorId null)이고 대상 이메일은 detail에 남으며, 해제 로그의 행위자는 해제한 관리자다"
    - "잠금 안내 문구는 정수 분으로 표시한다"
    - "이미 잠긴 계정에 순차로 실패가 더 와도 잠금 행동 로그는 늘지 않는다(잠긴 뒤 before 훅이 거부)"
    - statement: "동시 로그인 실패 경합에서는 잠금 행동 로그가 드물게 2건 남을 수 있으나(at-least-once, hooks.ts 주석) 0건은 없다"
      verification: backstop
    - "잠금 문구의 분 값은 설정 `auth.lockout.window_minutes`(`AUTH_LOCKOUT_WINDOW_MINUTES`)에서 온다 — `lockoutConfig()`의 `windowMinutes`"
    - "UI S5-lock-message/empty — 잠기지 않은 로그인 실패에는 잠금 문구가 나오지 않는다(기존 동작)"
    - "UI S5-lock-message/loading — 로그인 제출 대기 표시는 기존 로그인 폼 그대로이고 잠금 문구는 응답 뒤에만 나온다"
    - "UI S5-lock-message/error — 설정 `auth.lockout.window_minutes`를 20으로 바꾸면 잠금 문구가 `… 20분 뒤 다시 시도하거나 …`가 되고, 로그인 화면이 그 문구를 잠금 오류로 인식한다(일반 오류로 떨어지지 않는다)"
    - "UI S5-lock-message/partial — 남은 대기 시간은 표시하지 않는다 — 문구의 N은 설정값(최대 대기) 하나뿐이다"
    - "UI S5-lock-message/overflow — 문구는 기존 로그인 폼 오류 자리(FormAlert)에서 줄바꿈되고 로그인 폼 폭을 넘지 않는다"
    - "UI S5-lock-message/long-text — N 외의 글자는 기존 문구와 같다: `로그인 시도가 너무 많습니다. {N}분 뒤 다시 시도하거나 관리자에게 문의하세요.`"
  artifacts:
    - path: "domain/auth/locked-message.ts"
      provides: "잠금 문구를 분 숫자 하나로 만드는 함수와 문구 판정 — import 없는 잎 모듈"
      exports: ["lockedMessage", "isLockedMessage"]
    - path: "domain/auth/lockout.ts"
      provides: "실패 기록·세기·잠금 로그를 한 트랜잭션으로 묶는 `recordLoginFailure`"
      exports: ["recordLoginFailure"]
    - path: "repositories/login-attempts.ts"
      provides: "세 함수의 선택 `tx` 인자 · 이메일 키 advisory 잠금"
      exports: ["lockLoginEmail"]
    - path: "domain/action-log/record.ts"
      provides: "핵심 행동 종류 세 개 추가(세 배열 함께)"
      contains: "holiday_change"
    - path: "test/integration/action-log.test.ts"
      provides: "잠금 N-1/N/N+1 · 감사 쓰기 실패 롤백 · 동시 실패 · 빈 연결 1개 풀 교착 회귀(eng E5) 통합"
  key_links:
    - from: "domain/auth/hooks.ts"
      to: "domain/auth/lockout.ts"
      via: "`after` 훅의 실패 가지가 `recordLoginFailure(email, ip)` 한 번을 부른다"
      pattern: "recordLoginFailure"
    - from: "domain/auth/lockout.ts"
      to: "domain/action-log/record.ts"
      via: "`withTransaction` 안에서 `recordAction(SYSTEM_VIEWER, { actionType: \"account_lock\", … }, { tx })`"
      pattern: "account_lock"
    - from: "domain/auth/hooks.ts"
      to: "domain/auth/locked-message.ts"
      via: "`before` 훅이 `lockoutConfig()`의 `windowMinutes`로 `lockedMessage(windowMinutes)`를 던진다"
      pattern: "lockedMessage"
    - from: "app/(auth)/login/login-error.ts"
      to: "domain/auth/locked-message.ts"
      via: "403 + `isLockedMessage(message)`면 그 문구를 그대로 보인다"
      pattern: "isLockedMessage"
---

<objective>
**As a** 관리자와 감사 담당, **I want to** 계정이 잠긴 일이 행동 로그에 빠짐없이 남고 잠금 안내 문구가 실제 잠금 설정값을 말하기를, **so that** 서버 JSON 로그를 뒤지지 않고도 누가 언제 잠겼는지 확인할 수 있고, 관리자가 잠금 시간을 바꿔도 직원이 틀린 안내를 보지 않는다.

(스토리는 ROADMAP 04.2 Goal 줄 「계정 잠금·해제가 행동 로그에 남는다」와 기준 5에서 끌어왔다.)

Purpose: D-712(Phase 1 성공 기준 2의 미이행분)의 잠금 쪽 — 상태 변경과 감사 기록을 한 트랜잭션으로(Codex #12), 코디네이터 결정 「잠금 안내 문구는 설정값을 읽어 표시」, 그리고 이 페이즈가 쓰는 끌 수 없는 행동 종류 세 개(잠금 해제 04.2-08 · 공휴일 변경 04.2-11·12가 쓴다).

Output: 핵심 행동 종류 세 개(세 배열 함께) · 실패 기록 트랜잭션 함수 · 저장소 tx 인자 · 문구 함수와 판정 · 로그인 화면 판정 교체 · 통합·단위 테스트.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개. 행동 로그 화면(`app/(app)/admin/action-log`)은 고치지 않는다 — 라벨 배열만 늘리면 화면이 따라온다(UI-SPEC S5).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-PATTERNS.md — 「domain/action-log/record.ts (modified …)」·「recordAction tx-aware call」·「domain/auth/locked-message.ts」 절(Grep)
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — S5(Grep `## S5`)
# lib/db-transaction.ts — `withTransaction<T>(fn: (tx: DbOrTx) => Promise<T>)`(이미 main에 있다)
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 D-712 여섯 줄(#26~#31, #31은 backstop — #28의 해제 쪽은 04.2-08이 다시 증명한다)과 UI-SPEC S5 여섯 줄이다. 금지 회상에서 「잠금 문구가 남은 시간이나 계정 존재 여부를 드러내는 것」은 정규 보안(사용자 열거 — OWASP)으로 넘겼다 — S5/partial truth와 기존 원칙(문구는 계정 존재와 무관하게 같다)이 덮는다.

**계획 단계 결정(표면화):**

- **D-4207 행위자 표현:** 잠금 — 로그인 전이라 행위자가 없으므로 `SYSTEM_VIEWER`(기존 `recordAction`이 `actorId: null`로 적는다) + `detail: { email, threshold, windowMinutes }`. `entity`는 `"user"`, `entityId`는 그 이메일의 사용자 id가 있으면 그 id, 없으면(없는 이메일 — 잠금은 계정 존재와 무관하게 동작한다) `null`. 해제 쪽 표현은 04.2-08이다.
- **D-4220 감사 기록의 원자성(개정 — Codex #12):** 첫 계획은 04-32가 main에 없다는 이유로 두 인자 호출(트랜잭션 밖)을 택했다. Codex #12 — 잠금이 커밋된 뒤 행동 로그 쓰기가 실패하면 잠금 기록이 0건이고, 다음 시도는 `before` 훅이 먼저 거부해 영영 고쳐지지 않는다. 그래서 새 도메인 함수 `recordLoginFailure(email, ip, deps?)`(`domain/auth/lockout.ts`)가 `withTransaction(tx => …)` 안에서 ① 이메일 키 advisory 잠금 ② 실패 기록 INSERT ③ 열린 실패 수 세기 ④ 잠겼으면 `recordAction(SYSTEM_VIEWER, { actionType: "account_lock", … }, { tx })`를 한다. 행동 로그 INSERT가 실패하면 ②도 롤백된다. 행동 종류 세 개는 모두 `ALWAYS_ON_ACTION_TYPES`에 넣는다 — `document_*`처럼 설정으로 꺼지면 원자성이 무의미하다.
- **advisory 잠금(①이 필요한 이유):** 트랜잭션 안에서는 동시 실패 둘이 서로의 미커밋 INSERT를 못 본다 — 임계값 5, 커밋된 실패 3에서 동시 실패 둘이 각각 4를 세고 둘 다 잠금 로그를 안 남긴 채 커밋되면 계정은 잠겼는데 기록이 0건이다(지금의 트랜잭션 밖 코드는 INSERT가 바로 커밋되어 뒤에 센 쪽이 둘 다 본다). 그래서 `lockLoginEmail(viewer, email, tx)`가 `pg_advisory_xact_lock(hashtextextended('login_attempts:' || email, 0))`(기다리는 잠금 — 같은 이메일의 실패만 줄 세운다)을 먼저 잡는다. 결과는 at-least-once보다 강한 정확히 1회지만 엣지 #31 문장(「드물게 2건 … 0건은 없다」)은 그대로 참이라 backstop 문장을 바꾸지 않는다. `hooks.ts`의 at-least-once 주석은 「이메일 키 잠금으로 줄을 서 정확히 1회」로 한 줄 고친다(이 플랜이 바꾼 동작이라 주석도 이 플랜 몫).
- **트랜잭션 안에서 두 번째 연결 금지(eng E5 · Codex 최종 B #1):** 첫 계획은 잠김 가지에서 `findUserByEmail(SYSTEM_VIEWER, email)`을 트랜잭션 콜백 **안에서** 불렀다. 그 함수는 전역 `db`를 쓰므로(`repositories/users.ts` 10행) 풀에서 연결을 하나 더 잡는다. 풀은 `DB_POOL_MAX` 기본 5(`lib/env.ts` 54행)라 같은 이메일 실패 다섯이 겹치면 넷은 연결을 쥔 채 advisory 잠금을 기다리고, 잠금을 쥔 하나는 여섯 번째 연결을 기다려 서로 풀리지 않는다(04.2-10이 풀 대기 한도를 더하면 교착 대신 실패 기록이 롤백된다 — 둘 다 틀리다). 그래서 사용자 조회는 **트랜잭션을 열기 전에** 한 번 한다(잠길지는 트랜잭션 안에서만 알 수 있으므로 실패마다 조회한다 — 이메일 색인 한 줄 읽기). `repositories/users.ts`에 `tx` 인자를 더하는 길은 파일을 하나 더 고쳐야 해 택하지 않는다. 콜백 안의 나머지는 모두 `tx`로 묶인다: `lockLoginEmail`·`recordAttempt`·`countOpenFailures`(③의 `tx` 인자)와 `recordAction(…, { tx })` — `account_lock`은 `ALWAYS_ON_ACTION_TYPES`라 `recordAction`이 설정 조회를 아예 부르지 않는다(main `domain/action-log/record.ts` 「끌 수 없는 종류는 설정 조회를 아예 부르지 않고」). 회귀 테스트는 풀 크기를 바꾸지 않고 「연결 1개 풀」과 같은 상태를 만든다: `db/client.ts`가 내보내는 `pool`에서 `pool.options.max - 1`개를 `pool.connect()`로 미리 잡아 두면 남은 연결이 1개뿐이라, 콜백 안에서 연결을 하나 더 잡는 순간 영영 기다린다.
- **04-32 전제:** `lib/db-transaction.ts`의 `withTransaction`은 이미 main에 있다. `RecordActionDeps.tx?: DbOrTx`(04-32)는 이 계획을 쓰는 시점에 origin/main에 없다(Phase 4 브랜치 `origin/claude/gsd-progress-e1nzgu`에만 있다). 그래서 Task 1 `<precondition>`이 확인하고, 없으면 `git fetch origin main && git merge origin/main`을 한 번 해 보고 그래도 없으면 멈춘다 — 04-32를 이 플랜에서 다시 구현하지 않는다(CONTEXT D-712 「04-32의 tx 인자 시그니처를 따른다」). 이 전제는 04.2-08·11·12도 막는다.
- **저장소 tx 인자:** `recordAttempt`·`countOpenFailures`·`resolveOpenFailures`에 마지막 선택 인자 `tx: DbOrTx = db`를 더한다(04-32의 `appendActionLog(viewer, row, tx)`와 같은 모양). `resolveOpenFailures`의 tx는 04.2-08(해제)이 쓴다.
- **공용 트랜잭션 타입(Codex 2차 #7):** main의 `DbOrTx`(`db/client.ts` 48행)는 `Pick<typeof db, "insert" | "update" | "select">`라 `tx.execute`가 컴파일되지 않고, 04-32의 `lib/db-transaction.ts` 주석은 「DbOrTx 타입 자체는 넓히지 않고」라고 적는다. 그래서 공용 타입을 한 곳에서 한 번 넓히는 일은 `db/client.ts`를 소유한 04.2-06이 하고(`"delete" | "execute"` 추가 — 04.2-06 truth), 이 플랜은 그 결과만 쓴다(depends_on 04.2-06 → wave 3). `withTransaction`은 드리즐 트랜잭션을 그대로 넘기므로 시그니처를 바꿀 필요가 없다. Phase 4 쪽 주석 한 줄의 정리는 04.2-14 머지 단계가 한다.
- **세 배열 + 누수 스캔:** 세 배열은 한 커밋에서 함께 고친다(04-RESEARCH Pitfall 2). `test/integration/leak-scan.test.ts`는 행동 종류 목록을 갖지 않고 이 플랜은 새 액션·DTO를 만들지 않아 고칠 것이 없다 — Task 1 검증에서 돌려 초록임을 확인한다.
- **문구 판정:** `login-error.ts`의 정확 대조(`===`)는 숫자가 바뀌면 깨진다. 잎 모듈에 `isLockedMessage(text)` — 고정 접두어 `로그인 시도가 너무 많습니다. `와 고정 접미어 `분 뒤 다시 시도하거나 관리자에게 문의하세요.` 사이가 1자리 이상 숫자뿐인지 — 를 두고 로그인 화면이 그것을 쓴다(better-auth의 다른 403 영문 메시지는 계속 일반 오류로 접힌다 — /review L-3 회귀 방지). 옛 상수 이름은 쓰는 곳이 없어지므로 잎 모듈과 `lockout.ts` 재수출에서 함께 지운다.
</probe_fallback>

## Review Dispositions

| Codex # | 처리 | 한 줄 |
|---|---|---|
| 12 (medium) — 잠금 쪽 | 반영 | 실패 기록·세기·잠금 로그를 한 트랜잭션(`recordAction(…, { tx })` — 04-32) + 이메일 키 advisory 잠금 + 세 종류 모두 끌 수 없음. 감사 쓰기 실패 롤백·동시 실패를 통합 테스트로 증명(Task 1). 해제 쪽은 04.2-08, 공휴일 쪽은 04.2-11·12 |
| eng E5 (blocker) — 풀 교착 | 반영 | 사용자 조회·설정 읽기를 `withTransaction` 전으로 옮겨 콜백 안에는 `tx` 호출만 남긴다 · 빈 연결 1개만 남긴 풀에서 N번째 실패가 5초 안에 끝나는 통합 회귀(Task 1) |
| Codex 2차 7 (medium) — `tx.execute` | 반영 | 공용 `DbOrTx`를 04.2-06이 `db/client.ts`에서 `"delete" \| "execute"`까지 넓히고(파일 소유 04.2-06), 이 플랜은 depends_on 04.2-06 · wave 3으로 옮겨 그 타입을 쓴다 · precondition이 넓힌 타입을 확인한다 · Phase 4(04-32) 주석과의 조율은 04.2-14 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 로그인 N번째 실패 한 번이 한 트랜잭션 안에서 행동 로그 `account_lock` 한 행(행위자 null · detail에 이메일)으로 끝까지 간다</name>
  <files>domain/action-log/record.ts, repositories/login-attempts.ts, domain/auth/lockout.ts, domain/auth/hooks.ts, test/integration/action-log.test.ts, test/unit/action-log/record.test.ts</files>
  <precondition>`git show origin/main:domain/action-log/record.ts | grep -q "tx?: DbOrTx"`가 성공하거나, 실패하면 `git fetch origin main && git merge --no-edit origin/main` 뒤 `grep -q "tx?: DbOrTx" domain/action-log/record.ts`가 성공한다. 둘 다 실패하면(04-32가 아직 main에 없다) 이 Task를 시작하지 않고 「04-32 대기로 04.2-03·08·11·12 보류」를 보고한다 — 04-32를 여기서 구현하지 않는다(D-712). 또 04.2-06이 공용 타입을 넓혀 `grep -q '"execute"' db/client.ts`가 성공한다(Codex 2차 #7) — 실패하면 04.2-06 대기로 멈춘다</precondition>
  <read_first>
    - `domain/action-log/record.ts` 전체 — 세 배열 · `RecordActionDeps.tx?`(04-32) · `recordAction` · `SYSTEM_VIEWER` → `actorId: null`
    - `repositories/login-attempts.ts` 전체 — `countOpenFailures`·`recordAttempt`·`resolveOpenFailures`가 `db`를 직접 쓰는 모양
    - `repositories/action-log.ts` — 04-32의 `appendActionLog(viewer, row, tx)` 선택 tx 인자 모양(따라 할 모양)
    - `domain/auth/hooks.ts` 55~130행 — `before`(잠금 판정·거부)와 `after`(실패 기록 → `countOpenFailures` → `isLocked`이면 `log.info("auth.lockout")`) · at-least-once 주석
    - `domain/auth/lockout.ts` — `lockoutConfig()` · `windowStart` · `isLocked`
    - `lib/db-transaction.ts` — `withTransaction`
    - `db/client.ts` — 내보내는 `pool`(`max: env.DB_POOL_MAX`) · `repositories/users.ts` 10행 `findUserByEmail`(전역 `db` — eng E5)
    - `test/integration/lockout.test.ts` 1~35행 — `signIn(email, password, ip)` 도우미 · 계정 만들기 · 고유 `x-client-ip`와 rate limit 주석
    - `test/integration/action-log.test.ts` — `queryActionLog(SYSTEM_VIEWER, { actionType })` 사용 모양
  </read_first>
  <behavior>
    - 잠금 임계값 기본(설정 `auth.lockout.threshold`)에서 N-1회 실패(`signIn`) → `queryActionLog(SYSTEM_VIEWER, { actionType: "account_lock" })` 0행
    - N번째 실패 → 1행 · `actorId` null · `detail.email` = 그 이메일 · `detail.threshold`·`detail.windowMinutes`가 숫자
    - N+1번째 시도(잠긴 뒤 — `before` 훅이 403) → 여전히 1행
    - 감사 쓰기 실패: 열린 실패 N-1에서 `recordLoginFailure(email, ip, { recordAction: 던지는 가짜 })` → 오류가 전파되고 `countOpenFailures`가 여전히 N-1(실패 기록도 롤백) · 이어서 주입 없이 `recordLoginFailure` 한 번 → `account_lock` 1행
    - 동시 실패: 열린 실패 N-2에서 `Promise.all([recordLoginFailure(e), recordLoginFailure(e)])` → 열린 실패 N · `account_lock` 행이 1건 이상(0건 아님)
    - 풀 교착 회귀(eng E5 — 연결 1개 풀과 같은 상태): 열린 실패 N-1인 **존재하는** 사용자 이메일에서 `pool.options.max - 1`개 연결을 `pool.connect()`로 잡아 둔 채 `recordLoginFailure(e)`를 5초 타이머와 `Promise.race` → 타이머보다 먼저 `{ locked: true }`로 끝난다 · 잡아 둔 연결을 `finally`에서 모두 `release()`한 뒤 `account_lock` 1행이고 그 `entityId`가 그 사용자 id다(트랜잭션 전 조회가 쓰였다)
    - 단위: `account_lock`·`account_unlock`·`holiday_change`가 `CORE_ACTION_TYPES`에 있고 `ACTION_TYPE_LABELS`가 `계정 잠금`·`잠금 해제`·`공휴일 변경`이며 `ALWAYS_ON_ACTION_TYPES`에 있다
  </behavior>
  <action>
① `test/integration/action-log.test.ts`에 `describe("계정 잠금 행동 로그 (D-712)")`를 더하고 behavior 여섯 통합 케이스를 쓴다(RED). 풀 교착 케이스는 `pool`을 `@/db/client`에서 가져오고(다른 통합 테스트도 `@/db/client`를 import한다), 타이머가 이기면 「두 번째 풀 연결을 잠금 트랜잭션 안에서 잡았다(eng E5)」 메시지로 실패시키며, 타이머는 `clearTimeout`으로 치운다. 이 케이스는 사용자 조회를 트랜잭션 안에 둔 구현에서 반드시 타이머로 실패해야 한다 — RED 단계에서 그 실패를 한 번 본다. 로그인 도우미는 `lockout.test.ts`의 `signIn` 모양을 이 파일 안 지역 함수로 둔다(기존 파일을 공용화하지 않는다). 블록마다 고유 `x-client-ip`와 고유 이메일, 블록당 로그인 10회 이하(rate limit 주석) — 감사 실패·동시 실패 두 케이스는 `signIn`이 아니라 `recordLoginFailure`를 직접 불러 rate limit에 걸리지 않는다. `test/unit/action-log/record.test.ts`에 단위 케이스 하나를 더한다.

② `domain/action-log/record.ts`의 세 배열에 **항목만** 더한다(D-4220): `CORE_ACTION_TYPES` 끝에 `"account_lock"`, `"account_unlock"`, `"holiday_change"` · `ACTION_TYPE_LABELS`에 `account_lock: "계정 잠금"`, `account_unlock: "잠금 해제"`, `holiday_change: "공휴일 변경"` · `ALWAYS_ON_ACTION_TYPES` 끝에 셋(주석 한 줄: 「04.2 D-712·D-4220: 계정 잠금·해제와 공휴일 변경은 상태 변경과 한 트랜잭션으로 남기는 기록이라 끌 수 없다」). 다른 줄을 고치지 않는다(Phase 4 브랜치도 이 파일을 고친다 — 머지 충돌 면을 줄인다).

③ `repositories/login-attempts.ts`: `recordAttempt`·`countOpenFailures`·`resolveOpenFailures`에 마지막 선택 인자 `tx: DbOrTx = db`를 더하고 본문의 `db.`를 `tx.`로 바꾼다(다른 줄 그대로). 새 함수 `lockLoginEmail(viewer: Viewer, email: string, tx: DbOrTx): Promise<void>` — `tx.execute(sql\`select pg_advisory_xact_lock(hashtextextended(${"login_attempts:" + email}, 0))\`)`(값은 바인딩 파라미터 — 문자열 이어 붙이기 SQL 금지).

④ `domain/auth/lockout.ts`에 `recordLoginFailure(email: string, ip: string | null, deps?: { recordAction?: typeof recordAction; now?: () => Date }): Promise<{ locked: boolean }>` — **트랜잭션을 열기 전에** `lockoutConfig()`를 읽고 `findUserByEmail(SYSTEM_VIEWER, email)`(before 훅이 쓰는 기존 함수 — 고치지 않는다)로 `entityId = user?.id ?? null`을 정한다(eng E5 — `<probe_fallback>` 「트랜잭션 안에서 두 번째 연결 금지」). 그다음 `withTransaction(async (tx) => { lockLoginEmail → recordAttempt(…, tx) → countOpenFailures(…, tx) → isLocked면 recordAction(SYSTEM_VIEWER, { actionType: "account_lock", entity: "user", entityId, detail: { email, threshold, windowMinutes } }, { tx }) })`(D-4207·D-4220). 콜백 안에서는 `tx`를 받는 함수만 부른다 — 전역 `db`를 쓰는 저장소 함수·설정 읽기를 콜백 안에 두지 않는다. 잠겼으면 커밋 뒤 기존 `log.info("auth.lockout", { email, threshold, windowMinutes })`를 그대로 남긴다.

⑤ `domain/auth/hooks.ts` `after` 훅: 성공 가지(기존 `recordAttempt` → `resolveOpenFailures` → `login` 기록)는 동작을 바꾸지 않되 `recordAttempt`를 성공 가지 안으로 옮기고, 실패 가지는 `await recordLoginFailure(email, ip)` 한 줄로 바꾼다. at-least-once 주석은 `<probe_fallback>`대로 한 줄 고친다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/action-log.test.ts test/integration/lockout.test.ts test/integration/leak-scan.test.ts test/integration/action-log-query.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — N-1회에 잠금 행이 생겼거나, N번째에 0행이거나, 잠긴 뒤 시도가 행을 더했거나, 행위자가 null이 아니거나, detail에 이메일이 없거나, 감사 쓰기 실패 뒤 실패 기록이 남았거나, 동시 실패에서 잠금 행이 0건이거나, 빈 연결 1개만 남긴 풀에서 N번째 실패가 5초 안에 끝나지 않았거나(잠금 트랜잭션 안에서 두 번째 풀 연결을 잡음 — eng E5), 새 종류가 행동 로그 조회·누수 스캔을 깨뜨렸다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/action-log test/unit/lockout.test.ts &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>세 배열 단언 실패(한 곳만 고침) 또는 `Record<CoreActionType, string>` 누락으로 tsc error가 나거나, domain이 db를 직접 import해 boundaries 규칙이 깨져 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "holiday_change" domain/action-log/record.ts`가 3 이상(세 배열)이고 `account_lock`도 3 이상이다
    - `grep -c "{ tx }" domain/auth/lockout.ts`가 1 이상이다
    - `grep -c "pg_advisory_xact_lock" repositories/login-attempts.ts`가 1이다
    - 통합: N-1 → 0행 · N → 1행(actorId null, detail.email) · N+1 → 1행 · 감사 실패 → 열린 실패 N-1 유지 · 동시 → 1건 이상 · 빈 연결 1개 풀 → 5초 안에 `{ locked: true }` + `account_lock` 1행(entityId = 사용자 id)(eng E5)
    - `awk '/findUserByEmail\(/{if(!f)f=NR} /withTransaction\(/{if(!w)w=NR} END{exit !(f && w && f<w)}' domain/auth/lockout.ts` 종료 코드 0 — 사용자 조회 호출이 트랜잭션 여는 줄보다 앞이다(eng E5)
    - `git diff HEAD -- domain/action-log/record.ts`에 `RecordActionDeps` 줄 변경이 없다(04-32 시그니처를 그대로 쓴다)
    - `test/integration/leak-scan.test.ts`는 diff가 없고 초록이다
  </acceptance_criteria>
  <done>잠금이 행동 로그에 정확히 한 번(순차 기준) 남고, 감사 쓰기가 실패하면 잠금 자체가 커밋되지 않으며, 행위자는 시스템이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 잠금 문구가 설정값을 말한다(20분 설정 → 20분 문구 → 로그인 화면이 잠금으로 인식)</name>
  <files>domain/auth/locked-message.ts, domain/auth/lockout.ts, domain/auth/hooks.ts, app/(auth)/login/login-error.ts, test/integration/lockout.test.ts, test/unit/login-error.test.ts</files>
  <read_first>
    - `domain/auth/locked-message.ts` 전체 — 잎 모듈 제약 주석(클라이언트 번들)
    - `domain/auth/lockout.ts` — 38행 부근 옛 상수 재수출
    - `app/(auth)/login/login-error.ts` — `loginErrorMessage` · /review L-3 주석
    - `test/unit/login-error.test.ts` — 기존 케이스(잠금 문구 · 다른 403 영문 메시지)
    - `domain/settings/registry.ts` `setSettingValue(viewer, def, value, deps?)` · `domain/settings/keys.ts` `AUTH_LOCKOUT_WINDOW_MINUTES`
  </read_first>
  <behavior>
    - 통합: `setSettingValue(관리자, AUTH_LOCKOUT_WINDOW_MINUTES, 20)` 뒤 잠긴 계정의 로그인 응답 403 본문 메시지 = `로그인 시도가 너무 많습니다. 20분 뒤 다시 시도하거나 관리자에게 문의하세요.` · 설정 기본값이면 분 숫자가 기본값과 같다 · 잠기지 않은 실패(N-1회째)의 응답은 잠금 문구가 아니다
    - 단위: `lockedMessage(20)` = 위 20분 문구 · `lockedMessage(15)`가 옛 문구와 글자 하나까지 같다 · `lockedMessage(20.7)`의 분 숫자는 20(정수) · `isLockedMessage(lockedMessage(7))` 참 · `isLockedMessage("Invalid origin")` 거짓 · 숫자 자리가 비었거나 숫자가 아니면 거짓
    - 단위: `loginErrorMessage({ status: 403, message: lockedMessage(20) })` = 그 문구 · `{ status: 403, message: "Invalid origin" }` = 일반 오류 · `{ status: 401, message: lockedMessage(20) }` = 일반 오류
  </behavior>
  <action>
① behavior의 통합 케이스(`lockout.test.ts` — 설정은 테스트 끝에 기본값으로 되돌린다)와 단위 케이스(`test/unit/login-error.test.ts`의 고정 문자열 두 곳을 `lockedMessage(15)`로 바꾸고 새 케이스 추가)를 먼저 쓴다(RED).

② `domain/auth/locked-message.ts`: 잎 모듈(import 없음)을 유지하며 옛 상수를 `lockedMessage(minutes: number): string`(정수 분 — `Math.trunc` 뒤 문장에 넣는다)과 `isLockedMessage(text: string): boolean`(`<probe_fallback>` 접두어·접미어 사이 숫자 판정)으로 바꾼다. 파일 머리 주석의 「남은 시간은 표시하지 않는다」는 남긴다. `domain/auth/lockout.ts`의 옛 상수 재수출을 지운다(쓰는 곳이 없어진다).

③ `domain/auth/hooks.ts` `before` 훅: 이미 읽은 `lockoutConfig()`의 `windowMinutes`(설정 `auth.lockout.window_minutes`)로 `new APIError("FORBIDDEN", { message: lockedMessage(windowMinutes) })`. `app/(auth)/login/login-error.ts`: `error?.status === 403 && typeof error.message === "string" && isLockedMessage(error.message)`면 그 메시지, 아니면 `GENERIC_ERROR`. 파일 머리 주석의 「문구를 정확히 대조한다」 문장은 「고정 접두어·접미어로 대조한다(분 숫자는 설정값)」로 한 줄만 고친다.

④ 이 플랜이 고친 `domain`·`app` 파일에 분 숫자가 박힌 잠금 문장이 남지 않았는지 Grep으로 확인한다(테스트 파일의 기대값은 예외).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/login-error.test.ts test/unit/lockout.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 설정 분 숫자가 문구에 반영되지 않았거나, 옛 기본 문구와 한 글자라도 달라졌거나, 소수 분이 그대로 나왔거나, 다른 403 영문 메시지가 잠금 문구로 새었거나(/review L-3 회귀), 숫자 없는 문장이 잠금으로 인식됐다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/lockout.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 20분 설정 뒤에도 403 메시지가 기본 분이거나, 잠기지 않은 실패에 잠금 문구가 나왔다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>잎 모듈이 import를 얻어 클라이언트 번들 경계(lib/env)가 깨졌거나 옛 상수를 아직 import하는 곳이 있어 eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "15분" domain app --include=*.ts --include=*.tsx | grep -v "//"` 출력이 비어 있다
    - `grep -c "^import" domain/auth/locked-message.ts`가 0이다(잎 모듈)
    - `grep -rn "LOCKED_MESSAGE" domain app lib` 출력이 비어 있다
    - 통합: 20분 설정 → 403 메시지에 `20분`
  </acceptance_criteria>
  <done>잠금 문구의 분 숫자가 설정값을 따르고 로그인 화면이 그 문구를 계속 잠금으로 알아본다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로그인 요청 → better-auth 훅 | 인증 전 입력(이메일)이 행동 로그 detail과 advisory 잠금 키에 들어간다 |
| 서버 403 메시지 → 로그인 화면 | 서버가 준 문장이 판정을 지나 사용자에게 그대로 보인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-20 | Repudiation | 계정 잠금 | medium | mitigate | `account_lock`을 끌 수 없는 핵심 행동으로, 실패 기록과 한 트랜잭션에서 기록(D-4220) — 감사 쓰기 실패 롤백·동시 실패 0건 없음을 통합 테스트로 증명 |
| T-4.2-21 | Information Disclosure | 잠금 문구 | medium | mitigate | 문구는 계정 존재와 무관하게 같고 남은 시간이 없다 — N은 설정 최대값 하나(S5/partial). better-auth의 다른 403 영문 메시지는 계속 일반 오류로 접힌다(단위 — /review L-3 회귀) |
| T-4.2-22 | Tampering | 행동 로그 detail · advisory 키 | low | mitigate | detail의 이메일은 로그인 폼 입력 그대로 텍스트로만 보인다(React 이스케이프). advisory 잠금 키는 바인딩 파라미터로 넘겨 SQL에 이어 붙이지 않는다(Task 1 ③) |
| T-4.2-23 | Denial of Service | 같은 이메일 실패 직렬화 · 풀 고갈 | medium | mitigate | advisory 잠금은 같은 이메일의 실패끼리만 줄을 세우고 트랜잭션 끝에 풀린다. 기다리는 쪽이 풀 연결을 모두 쥐어도 잠금을 쥔 트랜잭션은 두 번째 연결을 잡지 않아(사용자 조회·설정 읽기는 트랜잭션 전 — eng E5) 반드시 끝나고 차례로 풀린다 — 빈 연결 1개 풀 통합 회귀가 증명(Task 1). 공격자가 한 이메일로 동시 실패를 몰아도 그 이메일은 곧 잠긴다(before 훅이 트랜잭션 전에 거부) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/login-error.test.ts test/unit/lockout.test.ts test/unit/action-log` 초록
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/action-log.test.ts test/integration/lockout.test.ts test/integration/leak-scan.test.ts test/integration/action-log-query.test.ts` 초록
- `pnpm lint && pnpm typecheck && pnpm build` 통과

</verification>

<success_criteria>

- 계정 잠금이 상태 변경과 같은 트랜잭션으로 행동 로그에 남고(ROADMAP 04.2 기준 5), 행위자 표현이 D-4207대로다
- 잠금 문구가 설정값을 말하고 로그인 화면이 계속 잠금 오류로 알아본다
- 이 페이즈가 쓰는 끌 수 없는 행동 종류 세 개가 세 배열에 함께 있다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 핵심 행동 종류 | `account_lock`(라벨 `계정 잠금`) · `account_unlock`(라벨 `잠금 해제`) · `holiday_change`(라벨 `공휴일 변경`) — `CORE_ACTION_TYPES`·`ACTION_TYPE_LABELS`·`ALWAYS_ON_ACTION_TYPES` |
| 함수(신규) | `recordLoginFailure(email, ip, deps?)`(`domain/auth/lockout.ts`) · `lockLoginEmail(viewer, email, tx)`(`repositories/login-attempts.ts`) · `lockedMessage(minutes)` · `isLockedMessage(text)`(`domain/auth/locked-message.ts`, 잎 모듈) |
| 시그니처 변경 | `recordAttempt`·`countOpenFailures`·`resolveOpenFailures`에 선택 `tx` 마지막 인자 |
| 지운 이름 | 옛 잠금 문구 상수와 `domain/auth/lockout.ts`의 그 재수출(쓰는 곳 없음) |
| 기록 지점 | `domain/auth/hooks.ts` `after` 실패 가지 → `recordLoginFailure` |
| 테스트 | `test/integration/action-log.test.ts`(D-712 잠금 describe — 감사 실패 롤백·동시 실패·빈 연결 1개 풀 교착 회귀 포함) · `test/integration/lockout.test.ts`(20분 문구) · `test/unit/login-error.test.ts` · `test/unit/action-log/record.test.ts` |
| 외부 전제 | Phase 4 04-32(`RecordActionDeps.tx`)가 origin/main에 있어야 한다 |
| 결정 | D-4207(행위자 표현) · D-4220(감사 기록 원자성·끌 수 없음) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-03-SUMMARY.md` when done
</output>
