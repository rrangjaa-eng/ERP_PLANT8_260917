---
phase: 04.2-notify-holidays-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy.sh
  - infra/names.sh
  - scripts/bootstrap-gcp.sh
  - infra/monitoring/tick-stale.json.tpl
  - test/unit/deploy/deploy-sh.test.ts
  - test/unit/deploy/bootstrap-sh.test.ts
  - test/unit/deploy/fakebin/gcloud
  - test/unit/deploy/fakebin/curl
  - docs/OPERATIONS.md
  - test/unit/db/migration-journal.test.ts
autonomous: true
requirements: [NOTI-04]
user_setup:
  - service: gcp
    why: "배포자 서비스 계정에 Cloud Scheduler 관리 역할과 스케줄러 서비스 계정 사용 권한을 주려면 프로젝트 소유자가 bootstrap-gcp.sh를 다시 돌려야 한다(배포자는 IAM 정책을 스스로 바꾸지 못한다)"
    dashboard_config:
      - task: "이 플랜이 머지된 뒤 첫 배포 전에 `bash scripts/bootstrap-gcp.sh --project plant8-509002 --github-repo rrangjaa-eng/ERP_PLANT8_260917`를 소유자 계정으로 한 번 실행(멱등 — 이미 있는 리소스는 건너뛴다)"
        location: "Cloud Shell 또는 소유자 gcloud 로그인 터미널"

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scripts/deploy.sh`가 서비스 배포 뒤 Cloud Scheduler 잡 `plant8-{env}-notify-tick`을 만들거나 고친다 — 매일 `0 9 * * *` `Asia/Seoul`, `POST ${SERVICE_URL}/internal/notify-tick`, OIDC 서비스 계정 `plant8-{env}-scheduler@…`, OIDC audience = `SERVICE_URL`, 재시도 0, 시도 마감 `--attempt-deadline=180s`(ROADMAP 04.2 기준 4 · 07-CONTEXT 입력 §4)"
    - "서비스 환경 변수에 `NOTIFY_TICK_SCHEDULER_SA`(스케줄러 서비스 계정 이메일)가 들어가고 audience는 이미 있는 `BETTER_AUTH_URL=${SERVICE_URL}`과 같은 값이다"
    - "배포하는 셸에 `NOTIFY_TICK_OIDC_DISABLED`가 있으면 deploy.sh가 어떤 gcloud 호출보다 먼저 exit 2로 거부한다(Issue 6)"
    - "배포 스모크가 토큰 없는 `POST /internal/notify-tick`이 401인지 확인한다 — 404면 Cloud Run 엣지가 경로를 먹은 것(`/healthz` 전례), 2xx면 검증이 꺼진 것이라 둘 다 SmokeFailed다(D-4214)"
    - "tick 멈춤 경보(`tick-stale`)가 켜지고 기간이 25시간이다 — 하루 한 번 tick이라 23시간 30분이면 매일 tick 직전 30분 동안 거짓 경보가 난다(D-4211). 조건은 `conditionAbsent`가 아니라 `conditionThreshold` 하나다(eng E1 — 부재 조건의 기간 상한 23.5시간을 넘길 수 없다): 로그 지표를 `alignmentPeriod` `89700s` · `ALIGN_SUM` · `REDUCE_SUM`(`groupByFields` 없음 — 리비전별 시계열을 하나로)으로 더한 값이 `COMPARISON_LT` 1이면 참(성공 0건) + `duration` `300s` + `evaluationMissingData: EVALUATION_MISSING_DATA_ACTIVE`(있던 시계열이 끊기면 참). 정렬 기간 + duration = 90000초(25시간) — Google 한도(정렬 기간과 duration의 **합** ≤ 25시간, eng R2-1)를 넘지 않는다. 한 번도 생긴 적 없는 시계열은 이 경보가 잡는다고 주장하지 않는다 — 환경마다 이 페이즈 첫 배포 뒤 잡을 한 번 수동 실행하고 첫 성공 로그를 확인해 시계열을 만든다(첫 성공 확인, eng R2-2)"
    - "스케줄러 서비스 계정은 bootstrap-gcp.sh(소유자 실행)가 만들고 배포자에게 그 계정 사용 권한과 `cloudscheduler.admin`만 준다 — 배포자에게 서비스 계정 관리 권한을 주지 않는다(D-4212)"
    - "OPERATIONS.md가 스케줄러 잡 수동 실행(남은 건 같은 날 잇기)·SMTP 확인 경로(Workspace 관리자에게 확인할 값 목록)·새 경보 행을 적고 300줄 이하다"
    - "drizzle 저널 가드(`test/unit/db/migration-journal.test.ts` — idx 연속 · tag 접두어 = 0 채운 idx · when 엄격 증가 · SQL 파일 존재)가 wave 1에 있고 지금 main의 저널에서 초록이다 — 04.2-06의 마이그레이션 검증과 04.2-14의 통합 절차가 기댄다. 04.1이 먼저 만들었으면 고치지 않고 돌리기만 한다(Codex 2차 #10)"
  artifacts:
    - path: "test/unit/db/migration-journal.test.ts"
      provides: "drizzle 저널 가드 — 04.2-06·04.2-14가 기댄다"
    - path: "scripts/deploy.sh"
      provides: "`ensure_scheduler` · OIDC 우회 변수 거부 · 스모크 401 확인 · cloudscheduler API"
      contains: "ensure_scheduler"
    - path: "infra/names.sh"
      provides: "`scheduler_sa` · `scheduler_job` 이름 도우미 · 바뀐 `policy_tick`"
      contains: "scheduler_sa"
    - path: "infra/monitoring/tick-stale.json.tpl"
      provides: "켜진 25시간 성공 0건 경보(threshold · 결측=참 — `conditionAbsent` 없음)"
      contains: "EVALUATION_MISSING_DATA_ACTIVE"
  key_links:
    - from: "scripts/deploy.sh"
      to: "app/internal/notify-tick/route.ts"
      via: "스케줄러 잡 URI `${SERVICE_URL}/internal/notify-tick` + 스모크 401 확인(04.2-01의 라우트)"
      pattern: "/internal/notify-tick"
    - from: "scripts/deploy.sh"
      to: "lib/env.ts"
      via: "서비스 env `NOTIFY_TICK_SCHEDULER_SA` — 04.2-05의 `schedulerConfigFromEnv`가 읽어 04.2-01의 검증기에 기대 이메일로 넘긴다"
      pattern: "NOTIFY_TICK_SCHEDULER_SA"
    - from: "infra/monitoring/tick-stale.json.tpl"
      to: "domain/notify/tick.ts"
      via: "로그 지표 `notify.tick` ok 참(04.2-01의 성공 로그 한 줄)의 `alignmentPeriod` 89700s 합이 1 미만이거나 있던 시계열이 끊기면 `duration` 300s 뒤 경보(합 25시간)"
      pattern: "89700s"
---

<objective>
**As a** 운영자(대표·관리자), **I want to** 배포 한 번으로 매일 09:00 KST 스케줄러가 검증된 호출로 tick을 부르고 tick이 멈추면 경보가 오기를, **so that** 알림이 조용히 끊기지 않고 누군가 검증을 끈 채 배포하는 일도 없다.

(스토리는 ROADMAP 04.2 기준 4 「스케줄러 잡·서비스 계정은 deploy.sh가 만들고, OIDC 검증을 끄는 환경 변수가 있으면 배포가 거부된다」에서 끌어왔다.)

Purpose: NOTI-04의 배포 몫 — 스케줄러 잡·서비스 계정·배포 거부·엣지 경로 확인(CONTEXT 「계획이 정할 것」의 `/internal/notify-tick`이 Cloud Run 엣지에서 살아남는지)·경보 켜기·운영 문서.

Output: deploy.sh 네 변경 · 이름 도우미 · bootstrap 권한 · 경보 템플릿 · 단위 테스트 · OPERATIONS.md.

**실행 규율:** 구현 전 superpowers `test-driven-development`(가짜 gcloud 테스트 먼저), 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 실제 GCP 명령을 실행하지 않는다(단위 테스트는 가짜 gcloud·curl만). 프로젝트 번호 12자리 숫자를 문서에 쓰지 않는다(docs-limits 단언). 새 의존성 0개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md

# 범위 Read:
# scripts/deploy.sh — main(653~677행) · ensure_apis(153행) · deploy_service(400~500행, 440행 env_vars) · smoke(539~585행) · ensure_alerts(586~618행)
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — Cloud Run 엣지 `/healthz` 전례·OIDC audience(Grep `healthz`)
</context>

<probe_fallback>
이 플랜은 edge 보고서 행을 떠맡지 않는다(배포 경로 — NOTI-04 기준 4의 배포 문장과 18A 경보). 

**계획 단계 결정·가정(표면화):**

- **D-4211 경보 기간:** 기존 템플릿은 `conditionAbsent` 84600초(23시간 30분)인데 tick은 하루 한 번(24시간 간격)이다 — 켜는 순간 매일 08:30~09:00에 거짓 경보가 난다. 창을 25시간으로 바꾸고 이름도 `[env] notify tick stale 25h`로 바꾼다(`policy_tick` · 템플릿 `displayName` 두 곳). 기존 비활성 `…23h30m` 정책은 deploy.sh가 이름으로 찾지 못해 남는다 — 지우는 일은 OPERATIONS.md에 콘솔 한 줄 안내로 둔다(자동 삭제를 만들지 않는다). 비영업일 tick도 `notify.tick` ok 참을 남기므로(04.2-01) 주말·연휴에도 경보가 울리지 않는다.
- **25시간 창을 만드는 방식(eng E1 · Codex 최종 A #1):** 첫 계획은 `conditionAbsent.duration`을 `90000s`로 올렸는데, 부재 조건의 기간 상한은 23.5시간이라(Codex가 인용한 공식 문서 `monitoring/alerts/metric-absence` — 이 세션에서 그 페이지는 네트워크 차단으로 직접 확인하지 못했다) 배포의 정책 생성·갱신이 `INVALID_ARGUMENT`로 실패한다. 그래서 부재 조건을 쓰지 않고 `conditionThreshold` 하나로 바꾼다. 기대는 한도는 모두 공식 Cloud Monitoring API 참조(discovery 문서 `monitoring.googleapis.com/$discovery/rest?version=v3`, revision 20260917 — REST 참조 `projects.alertPolicies`와 같은 본문)에서 확인했다: `Aggregation.alignmentPeriod` 「최소 60초 … 알림 정책은 최대 90,000초(25시간)」 · `MetricThreshold.duration` 「분의 배수만」 · `evaluationMissingData` 「`duration`이 60초 이상일 때만」 · `EVALUATION_MISSING_DATA_ACTIVE` 「평가할 데이터가 없으면 조건을 참으로 본다」. 조건 모양: 필터는 기존 그대로(`__METRIC__` · `cloud_run_revision`) · `aggregations` 한 개 `{ alignmentPeriod: "89700s", perSeriesAligner: "ALIGN_SUM", crossSeriesReducer: "REDUCE_SUM" }`(`groupByFields` 없음) · `comparison: "COMPARISON_LT"` · `thresholdValue: 1` · `duration: "300s"` · `evaluationMissingData: "EVALUATION_MISSING_DATA_ACTIVE"`.
  - **합 한도(eng R2-1 · Codex r2-B #1):** Google 한도 문서(`monitoring/quotas#alerting` 주석 2 — Codex 인용, 이 세션에서는 네트워크 차단으로 직접 열지 못했다)는 정렬 기간과 duration의 **합**을 25시간으로 묶는다. 확인하지 못한 한도는 더 엄격한 쪽으로 읽는다: 정렬 기간 89700초(24시간 55분) + duration 300초 = 90000초. duration을 0으로 줄이면 결측 처리를 쓸 수 없으므로(`evaluationMissingData`는 duration 60초 이상 — API 참조) 정렬 기간을 줄인다. 하루 한 번 09:00 tick이라 24시간 55분 창은 tick이 55분 늦어도 전날 성공을 창 안에 둔다. 단위 가드는 각 값이 아니라 **조건마다 합**(`alignmentPeriod` 최댓값 + `duration` ≤ 90000초)을 단언한다.
  - **덮는 경로와 덮지 않는 경로(eng R2-2 · Codex r2-B #2):** (가) **성공 0건** — 로그 기반 카운터가 0을 기록하면 합 0 < 1로 참. (나) **있던 시계열이 끊김** — 성공 로그가 멈춰 창에 점이 없어지면 결측=참(API: `evaluationMissingData`는 데이터가 멈출 때의 평가 방식). (다) **한 번도 생긴 적 없는 시계열** — `scripts/deploy.sh` 607~610행은 지표 정의만 만들고 시계열은 첫 성공 로그가 들어와야 생긴다. 이 조건이 그 경우에 사건을 연다는 문서 근거가 없으므로 주장하지 않는다(부재 조건을 더하는 길은 기간이 23.5시간 이하라 하루 한 번 tick에서 매일 거짓 경보가 나 택하지 않는다). 대신 **첫 성공 확인**으로 막는다: 환경마다 이 페이즈 첫 배포 뒤 잡을 한 번 수동 실행하고, 성공 로그를 읽기 명령으로 확인한다(`<verification>` — 사람 확인, 결과를 SUMMARY에). 그 뒤로는 시계열이 있어 (가)·(나)가 덮는다.
  - **새 리비전:** `REDUCE_SUM`(`groupByFields` 없음)이 리비전별 시계열을 하나로 합치므로, 배포로 옛 리비전의 시계열이 끊겨도 합친 시계열은 새 리비전의 성공으로 이어져 거짓 경보가 없다(단위 단언). 결과: 마지막 성공 뒤 25시간에 경보, 성공 로그가 들어오면 닫힌다.
- **D-4212 서비스 계정 생성 주체:** 런타임 SA 선례대로 bootstrap-gcp.sh가 `plant8-{env}-scheduler`를 만들고 배포자에게 그 계정의 `iam.serviceAccountUser`(잡 생성 때 OIDC 계정으로 지정하려면 필요)를 준다. 배포자 프로젝트 역할에는 `cloudscheduler.admin`만 더한다. 서비스는 이미 `--allow-unauthenticated`라 `run.invoker` 바인딩은 필요 없다(검증은 앱 안 — 04.2-01·05). 그래서 소유자가 bootstrap을 한 번 다시 돌려야 한다(`user_setup`).
- **D-4214 스모크 401:** 토큰 없는 POST는 앱이 401을 돌려줘야 한다. 404 = 엣지가 경로를 먹음 · 2xx/5xx = 검증 꺼짐 또는 라우트 실패 — 모두 실패로 본다. 스모크는 tick을 실제로 돌리지 않는다(토큰 없음).
- **스케줄러 위치:** `--location="$REGION"`(asia-northeast3). Cloud Scheduler가 서울 리전을 지원한다는 가정 — 실제 첫 배포에서 `INVALID_ARGUMENT`가 나면 가장 가까운 지원 리전으로 바꾸고 OPERATIONS에 적는다(가정 — SUMMARY에 남긴다).
- **재시도 0:** 스케줄러 재시도는 끈다(`--max-retry-attempts=0`). tick은 멱등이라 재시도가 해롭지는 않지만, 409(잠금 경합)를 재시도로 겹쳐 부를 이유가 없다. 남은 건은 운영자가 같은 날 수동 실행으로 잇는다(D-4202).
- **시도 마감 180초(Codex #9):** 스케줄러 HTTP 시도 마감의 기본값(180초)에 기대지 않고 `--attempt-deadline=180s`로 적는다. Cloud Run 요청 제한은 기본 300초(deploy.sh가 `--timeout`을 주지 않는다 — 고치지 않는다). 첫 계획의 「이메일 예산 120초 + nodemailer 시간 제한 40초 < 180초」 증명은 틀렸다(Codex 2차 #6 — `socketTimeout`은 유휴 한도이고 DNS·평가·DB 시간이 빠졌다). 끝에서 끝까지의 요청 예산은 04.2-10이 정한다: 기준 시각은 핸들러 진입, `NOTIFY_TICK_BUDGET_MS` 160초 안에서만 새 묶음을 선점하고, 한 통은 `AbortSignal` 마감 30초(+2초 경주)로 끊기며, DB 트랜잭션은 `lock_timeout`·`statement_timeout`으로 묶여 마지막 묶음까지 170초 안이다. 이 플랜이 고정하는 것은 그 계산이 기대는 바깥 두 값 — 시도 마감 180초와 Cloud Run 300초 — 뿐이다.
- **저널 가드 소유(Codex 2차 #10):** `test/unit/db/migration-journal.test.ts`를 첫 계획은 04.2-05(wave 2)가 만들었는데 같은 wave의 04.2-06이 그 파일을 검증에서 돌려 「No test files found」가 날 수 있었다. 04.2-01은 파일 예산(14)이 차 있어 wave 1인 이 플랜 Task 3이 만든다 — 04.2-06은 이 플랜에 의존한다. 내용: `db/migrations/meta/_journal.json`을 읽어 `entries[i].idx === i`(0..n-1 연속), `tag`가 `String(idx).padStart(4, "0") + "_"`로 시작, `when`이 앞 항목보다 엄격히 큼, `tag + ".sql"` 파일이 `db/migrations/`에 있음. 실행 시점에 파일이 이미 있으면(04.1이 같은 경로를 만든다) 고치지 않고 돌리기만 한다. 파일 번호를 이 가드가 정하지 않는다 — 번호는 생성기가 붙이고 머지 직전에 다시 만든다(04.2-14).
- **SMTP 확인 경로(ROADMAP 기준 3 「계획 단계에서 Workspace SMTP 릴레이의 앱 비밀번호·발송 한도를 확인한다」):** 값은 사용자 몫이다(D-711). 이 플랜은 OPERATIONS.md에 확인할 항목 목록(발송 주소 · 인증 방식 앱 비밀번호 vs SMTP 릴레이 · 일 발송 한도 · 포트 587 STARTTLS)과 Secret Manager에 넣는 명령 자리(값 없음)만 적는다. 네 값 중 하나라도 `__unset__`이면 앱은 이메일을 끄고 시스템 상태에 「미설정」을 보인다(04.2-10·13).
</probe_fallback>

## Review Dispositions

| Codex # | 처리 | 한 줄 |
|---|---|---|
| 9 (medium) — 실행 마감 | 반영(부분) | 스케줄러 시도 마감을 `--attempt-deadline=180s`로 고정(Task 1). 요청 예산 계산은 04.2-10(2차 #6으로 개정), 조건 평가 경계는 Phase 7 |
| Codex 2차 6 (medium) — 이 플랜의 예산 문장 | 반영 | 「120초 + 40초 < 180초」 증명을 지우고 04.2-10의 끝에서 끝 예산(핸들러 진입 기준 160초 · 발송 마감 30초 · DB 문장 한도)을 가리킨다 — 이 플랜은 시도 마감 180초·Cloud Run 300초만 고정 |
| Codex 2차 10 (medium) | 반영 | 저널 가드 `test/unit/db/migration-journal.test.ts`를 wave 1인 이 플랜 Task 3이 만든다(04.2-05에서 옮김 · 04.2-01은 파일 14개로 예산이 참) — 04.2-06이 이 플랜에 의존한다 |
| 10 (medium) — audience | 반영 | 잡 audience = `SERVICE_URL` = 서비스 env `BETTER_AUTH_URL` — 앱이 둘 중 하나라도 비면 `not_configured`로 거부하는 쪽은 04.2-01·05 |
| eng E1 (blocker) — 부재 조건 25시간 | 반영 | `conditionAbsent` 90000s(상한 23.5시간 초과 → 배포 실패)를 `conditionThreshold` 하나로 교체: 25시간 `ALIGN_SUM`·`REDUCE_SUM` 합 < 1(성공 0건) + `EVALUATION_MISSING_DATA_ACTIVE`(시계열 없음). 한도는 공식 API 참조로 확인 · 템플릿 단위 단언(두 경로 + 모든 템플릿 한도) · 배포 뒤 첫 경보 열림→수동 실행으로 닫힘을 사람 확인(Task 2 · `<verification>`) — 아래 R2가 일부 대체 |
| eng R2-1 (blocker) — 정렬 기간 + duration 합 | 반영 | 합 한도(≤ 25시간, 확인 못 해 엄격하게 읽음)에 맞춰 `alignmentPeriod` 89700s + `duration` 300s = 90000s · 단위 가드가 조건마다 **합**을 단언(Task 2) |
| eng R2-2 (blocker) — 생긴 적 없는 시계열 | 반영(b) | 첫 배포 전 경보가 열린다는 주장을 지우고, 환경마다 첫 배포 뒤 잡 수동 실행 + 성공 로그 읽기로 시계열을 만드는 **첫 성공 확인**을 따로 둔다 · 검증을 「생긴 적 없음(첫 성공 확인)」과 「있던 시계열이 끊김(결측=참 단언)」으로 나눈다 · 부재 조건은 매일 거짓 경보라 더하지 않는다 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 배포 한 번이 스케줄러 잡을 OIDC 계정·audience·09:00 KST로 만들고 서비스에 기대 이메일을 넘긴다(가짜 gcloud로 끝까지)</name>
  <files>scripts/deploy.sh, infra/names.sh, test/unit/deploy/fakebin/gcloud, test/unit/deploy/deploy-sh.test.ts</files>
  <read_first>
    - `scripts/deploy.sh` 400~500행(`deploy_service` — `SERVICE_URL` 확정·440행 `env_vars`) · 653~677행(`main` 순서)
    - `infra/names.sh` 28~38행 — 이름 도우미 모양과 주석
    - `test/unit/deploy/fakebin/gcloud` — `case "$ARGS"` 분기 · 상태 파일 · 처리하지 않는 호출은 실패(새 하위 명령은 분기를 더해야 한다)
    - `test/unit/deploy/deploy-sh.test.ts` 1~140행 — `deploy()` 도우미 · 로그 줄 단언 모양 · 시나리오 1(새 프로젝트)·2(기존 서비스)
  </read_first>
  <behavior>
    - 시나리오 1(잡 없음): 로그에 `scheduler jobs describe plant8-staging-notify-tick`이 있고 그 뒤 `scheduler jobs create http plant8-staging-notify-tick` 한 줄 — 인자에 `--schedule=0 9 * * *` · `--time-zone=Asia/Seoul` · `--uri=https://plant8-staging-…/internal/notify-tick` · `--http-method=POST` · `--oidc-service-account-email=plant8-staging-scheduler@` · `--oidc-token-audience=https://plant8-staging-…`(끝 슬래시 없음) · `--max-retry-attempts=0` · `--attempt-deadline=180s` · `--location=asia-northeast3`
    - 시나리오 2(잡 있음 — 상태 파일): `create`가 아니라 `update http` 한 줄, 같은 인자
    - `run deploy plant8-staging` 줄의 `--set-env-vars`에 `NOTIFY_TICK_SCHEDULER_SA=plant8-staging-scheduler@` 가 있다
    - 순서: `ensure_scheduler`의 첫 호출이 `run deploy plant8-staging` 줄보다 뒤다(SERVICE_URL 확정 뒤)
  </behavior>
  <action>
① behavior 넷을 `deploy-sh.test.ts`의 두 시나리오 describe에 더한다(RED). 가짜 gcloud에 분기를 더한다: `scheduler jobs describe …`(상태 파일 `scheduler-job`이 있으면 성공, 없으면 exit 1) · `scheduler jobs create http …` · `scheduler jobs update http …`(둘 다 성공). 시나리오 2 준비에 상태 파일 `scheduler-job`을 만든다.

② `infra/names.sh`에 `scheduler_sa() { echo "plant8-$1-scheduler"; }`와 `scheduler_job() { echo "plant8-$1-notify-tick"; }`를 기존 도우미 모양(한 줄 + 오른쪽 주석)으로 더한다.

③ `scripts/deploy.sh`: (가) 서비스 env `env_vars`(440행) 끝에 `,NOTIFY_TICK_SCHEDULER_SA=$(scheduler_sa "$ENV")@${PROJECT}.iam.gserviceaccount.com`을 더한다(다른 항목은 그대로). (나) 새 함수 `ensure_scheduler()` — `STAGE=ensure_scheduler`, 잡 이름·SA 이메일을 이름 도우미로 만들고, `run gcloud scheduler jobs describe "$job" --location="$REGION" --project="$PROJECT"`가 성공하면 `update http`, 아니면 `create http`(인자는 behavior 첫 줄 그대로 + `--project`). 기존 함수들처럼 `run` 래퍼를 지나 `--dry-run`에서는 출력만 한다. (다) `main`에서 `deploy_service` 바로 뒤, `ensure_alerts` 앞에 `ensure_scheduler`를 둔다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/deploy-sh.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 잡 인자(시각·시간대·URI·OIDC 계정·audience·재시도·위치)가 다르거나, 잡이 있을 때 create를 불렀거나, 서비스 env에 기대 이메일이 없거나, 잡을 SERVICE_URL 확정 전에 만들었거나, 가짜 gcloud가 새 하위 명령을 처리하지 못해 기존 시나리오가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "ensure_scheduler" scripts/deploy.sh`가 2 이상(정의 + main 호출)이다
    - `grep -n "oidc-token-audience" scripts/deploy.sh`의 값이 `"$SERVICE_URL"`이다(경로 없는 서비스 URL — 04.2-01의 audience와 같다)
    - 기존 deploy-sh 테스트가 전부 초록이다
  </acceptance_criteria>
  <done>배포가 매일 09:00 KST 검증 호출 잡을 만들거나 고치고, 앱은 누가 불러야 하는지 안다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 거부·스모크·API·경보·bootstrap — 우회 변수 exit 2 · 토큰 없는 POST 401 스모크 · cloudscheduler API · 25시간 경보 켜기(threshold · 결측=참) · 스케줄러 SA와 배포자 권한</name>
  <files>scripts/deploy.sh, infra/names.sh, infra/monitoring/tick-stale.json.tpl, scripts/bootstrap-gcp.sh, test/unit/deploy/deploy-sh.test.ts, test/unit/deploy/bootstrap-sh.test.ts, test/unit/deploy/fakebin/gcloud, test/unit/deploy/fakebin/curl</files>
  <read_first>
    - `scripts/deploy.sh` 57~130행(`parse_args`·`require_clean_tree` — exit 2 거부 모양) · 153~160행(`ensure_apis`) · 539~585행(`smoke` — dry-run 가지 · `smoke_failed`) · 586~645행(`ensure_alerts`·`_upsert_policy` — 표시 이름으로 찾기)
    - `infra/monitoring/tick-stale.json.tpl` — `enabled: false` · `conditionAbsent` · `duration: 84600s`(이 조건 종류를 threshold로 바꾼다 — eng E1) · `infra/monitoring/5xx-ratio.json.tpl` — 기존 `conditionThreshold`·`aggregations` 모양(따라 할 모양)
    - `scripts/bootstrap-gcp.sh` 103~130행 — (c) SA 루프 · (d) 배포자 역할 루프 · 런타임 SA의 `iam.serviceAccountUser` 바인딩
    - `test/unit/deploy/bootstrap-sh.test.ts` — 순서 단언 · names.sh 상수 일치 단언(bootstrap은 names.sh를 source하지 않는다)
    - `test/unit/deploy/fakebin/curl` — 스모크 3종 흉내
  </read_first>
  <behavior>
    - `NOTIFY_TICK_OIDC_DISABLED=1`을 넣은 환경으로 deploy → exit 2 · stderr에 `NOTIFY_TICK_OIDC_DISABLED` 포함 · 가짜 gcloud 로그 비어 있음
    - 스모크: 가짜 curl의 `/internal/notify-tick` POST 응답이 401이면 통과 · 404면 SmokeFailed(메시지에 `edge`) · 200이면 SmokeFailed(메시지에 `OIDC`)
    - `services enable` 줄에 `cloudscheduler.googleapis.com`
    - 경보 템플릿(`infra/monitoring/tick-stale.json.tpl`을 읽어 `JSON.parse`): `enabled` 참 · 표시 이름 `[__ENV__] notify tick stale 25h` · 조건이 정확히 한 개이고 어느 조건에도 `conditionAbsent`가 없다(eng E1) · **성공 0건 경로** — `conditionThreshold.aggregations`가 정확히 `[{ alignmentPeriod: "89700s", perSeriesAligner: "ALIGN_SUM", crossSeriesReducer: "REDUCE_SUM" }]` · `comparison` `COMPARISON_LT` · `thresholdValue` 1 · **있던 시계열이 끊기는 경로** — `evaluationMissingData` `EVALUATION_MISSING_DATA_ACTIVE` · `duration` `300s` · **새 리비전 거짓 경보 없음** — `groupByFields`가 없다(리비전별 시계열을 하나로 합침) · `policy_tick staging` = `[staging] notify tick stale 25h`. (한 번도 생긴 적 없는 시계열은 이 템플릿의 단언 대상이 아니다 — `<verification>` 첫 성공 확인이 따로 덮는다, eng R2-2)
    - API 한도 가드(`infra/monitoring/*.json.tpl` 전부): 모든 `alignmentPeriod`가 60초 이상 · 모든 조건 `duration`이 60의 배수 · **조건마다 `alignmentPeriod` 최댓값(없으면 0) + `duration` ≤ 90000초**(합 한도 — eng R2-1, 각 값만 보지 않는다) · tick 템플릿은 그 합이 정확히 90000초 · `conditionAbsent`가 있으면 그 `duration`이 120초 이상 84600초 이하(84600초 상한은 가정 — Codex 인용 문서, `<probe_fallback>` 「25시간 창을 만드는 방식」). 한도를 넘는 값으로 바꾸면 이 단언이 먼저 빨갛게 된다 — RED 단계에서 `duration`을 `600s`로 바꿔 합 단언이 실패하는 것을 한 번 보고 되돌린다
    - bootstrap: `iam service-accounts create plant8-staging-scheduler`·`plant8-prod-scheduler`(없을 때) · 배포자 역할에 `roles/cloudscheduler.admin` · 두 스케줄러 SA에 배포자 `roles/iam.serviceAccountUser` · 배포자 역할에 `iam.serviceAccountAdmin`이 **없다** · names.sh `scheduler_sa`와 bootstrap의 이름이 같다(상수 일치 단언 확장)
  </behavior>
  <action>
① behavior 여섯 줄을 두 테스트 파일에 더하고(RED — 경보 템플릿·API 한도 두 줄은 `deploy-sh.test.ts`에 `describe("tick-stale 경보 템플릿 (D-4211 · eng E1)")`로, 저장소의 템플릿 파일을 직접 읽어 `JSON.parse`한다), 가짜 curl에 `*"/internal/notify-tick"*` 분기(상태 파일 `notify-tick` 값, 기본 401)를 더한다.

② `scripts/deploy.sh`: (가) `parse_args` 직후(어떤 gcloud 호출보다 앞) 검사 — `NOTIFY_TICK_OIDC_DISABLED`가 비어 있지 않으면 `echo "refusing to deploy: NOTIFY_TICK_OIDC_DISABLED is set (OIDC verification must stay on outside local)" >&2; exit 2`. 기존 거부 함수 모양을 따라 작은 함수 `refuse_oidc_bypass`로 두고 `main`에서 부른다. (나) `ensure_apis`의 목록에 `cloudscheduler.googleapis.com`을 더한다. (다) `smoke`에 네 번째 검사 — `curl -s -o /dev/null -w '%{http_code}' -X POST "${target}/internal/notify-tick"`; `401`이면 통과, `404`면 `smoke_failed "/internal/notify-tick returned 404 — Cloud Run edge ate the path"`, 그 밖이면 `smoke_failed "/internal/notify-tick returned $code without a token (expected 401 — OIDC verification must be on)"`. dry-run 가지에도 같은 호출을 `run`으로 더한다.

③ `infra/monitoring/tick-stale.json.tpl`(D-4211 · eng E1): `enabled` 참 · 정책 `displayName` `[__ENV__] notify tick stale 25h` · 조건 하나를 `conditionAbsent`에서 `conditionThreshold`로 바꾼다 — 조건 `displayName` `notify tick successes < 1 in 25h`, `filter`는 지금 값 그대로, `aggregations` 한 개(`alignmentPeriod` `89700s` · `perSeriesAligner` `ALIGN_SUM` · `crossSeriesReducer` `REDUCE_SUM` · `groupByFields` 없음), `comparison` `COMPARISON_LT`, `thresholdValue` 1, `duration` `300s`(정렬 기간과의 합 90000초 — eng R2-1), `evaluationMissingData` `EVALUATION_MISSING_DATA_ACTIVE`(근거·덮는 경로와 덮지 않는 경로는 `<probe_fallback>` 「25시간 창을 만드는 방식」). `conditionAbsent` 키는 파일에 남기지 않는다. `infra/names.sh` `policy_tick`을 `"[$1] notify tick stale 25h"`로. `scripts/deploy.sh`의 `_upsert_policy`·로그 지표 만들기는 고치지 않는다(같은 치환·같은 지표 — 정책 JSON만 바뀐다). 배포 시점 검증은 두 가지다: API가 거부하면 `set -euo pipefail`과 `STAGE=ensure_alerts`로 배포가 그 단계에서 멈추고(조용히 넘어가지 않는다), 한 번도 생긴 적 없는 시계열은 배포 뒤 첫 성공 확인이 따로 막는다(`<verification>` — eng R2-2).

④ `scripts/bootstrap-gcp.sh`: (c) SA 루프에 런타임 SA 옆으로 `plant8-${env}-scheduler`(표시 이름 `PLANT8 ERP ${env} notify scheduler`)를 없을 때만 만든다 · (d) 배포자 역할 목록에 `cloudscheduler.admin`을 더한다 · 런타임 SA 바인딩 루프 옆에 스케줄러 SA의 배포자 `iam.serviceAccountUser` 바인딩을 더한다(D-4212). 그 밖의 줄은 고치지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy</automated>
    <fails_when>failed 개수가 1 이상이다 — 우회 변수가 있는데 gcloud가 한 번이라도 불렸거나, 스모크가 404·200을 통과시켰거나, 경보가 여전히 꺼져 있거나 23h30m이거나, 경보가 아직 부재 조건이거나 합 < 1(성공 0건)·결측=참(있던 시계열이 끊김) 중 하나가 빠졌거나 `REDUCE_SUM`이 없거나 `groupByFields`가 있거나, 어느 조건의 정렬 기간 + duration 합이 90000초를 넘었거나(각 값만 맞고 합이 넘는 경우 포함 — eng R2-1) 분 배수가 아니거나, bootstrap이 스케줄러 SA·역할을 빠뜨렸거나 배포자에게 SA 관리 권한을 줬거나, 이름 상수가 names.sh와 어긋났다</fails_when>
    <automated>bash -n scripts/deploy.sh &amp;&amp; bash -n scripts/bootstrap-gcp.sh &amp;&amp; bash -n infra/names.sh</automated>
    <fails_when>셸 문법 오류로 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "cloudscheduler.googleapis.com" scripts/deploy.sh`가 1이다
    - `grep -n "\"enabled\"" infra/monitoring/tick-stale.json.tpl`이 `true`를 보인다
    - `grep -c "EVALUATION_MISSING_DATA_ACTIVE" infra/monitoring/tick-stale.json.tpl`이 1이고 `grep -c "\"89700s\"" infra/monitoring/tick-stale.json.tpl`이 1이며 `grep -c "\"300s\"" infra/monitoring/tick-stale.json.tpl`이 1이다(eng E1 · R2-1 — 합 90000초)
    - `node -e 'JSON.parse(require("fs").readFileSync("infra/monitoring/tick-stale.json.tpl","utf8"))'` 종료 코드 0(템플릿이 올바른 JSON)
    - `grep -c "serviceAccountAdmin" scripts/bootstrap-gcp.sh`가 0이다
    - `pnpm vitest run --project unit test/unit/deploy` 전체 초록
  </acceptance_criteria>
  <done>검증을 끈 배포는 시작도 못 하고, 배포 뒤 엣지·검증 상태가 스모크로 확인되며, 한 번이라도 성공한 뒤 성공한 tick이 25시간 없으면(성공 0건이든 시계열이 끊겼든) GCP 한도 안의 조건으로 경보가 온다 — 한 번도 성공하지 않은 환경은 첫 성공 확인이 막는다</done>
</task>

<task type="auto">
  <name>Task 3: OPERATIONS.md — 스케줄러 운영(수동 실행·남은 건) · SMTP 확인 경로 · 경보 행 · bootstrap 재실행 · drizzle 저널 가드</name>
  <files>docs/OPERATIONS.md, test/unit/db/migration-journal.test.ts</files>
  <read_first>
    - `docs/OPERATIONS.md` §6 「경보 3개」(120~135행) · 배포 절 · 220행 부근(rate_limits 정리 문장)
    - `test/unit/docs-limits.test.ts` — 300줄 상한 · 12자리 숫자 금지 · 필수 토큰
    - `db/migrations/meta/_journal.json` — `entries[]`의 `idx`·`tag`·`when` 모양 · `test/unit/db/` 기존 테스트 하나(파일 읽기·vitest 모양)
  </read_first>
  <action>
① §6 경보 표의 tick 행을 `| notify tick 25h 미성공 | 스케줄러 잡(`plant8-{env}-notify-tick`) 실행 기록과 Cloud Logging `notify.tick` 확인 → 401이면 `NOTIFY_TICK_SCHEDULER_SA`·잡의 OIDC 계정·audience 대조, 409면 다른 실행 중(다음 날 자동) · 새 환경·첫 배포 뒤에는 잡을 한 번 수동 실행해 첫 성공을 확인한다(성공이 한 번도 없으면 이 경보가 울리지 않는다) · 옛 `…stale 23h30m` 비활성 정책은 콘솔에서 지워도 된다 |`로 바꾼다(한 줄 — 「Phase 7까지 꺼 둔다」는 옛 대응 문구를 대체).

② 새 소절 「알림 발송 스케줄러」(8줄 이내): 잡 이름·시각(매일 09:00 KST — 비영업일에는 앱이 보내지 않고 `비영업일`로 기록)·수동 실행 `gcloud scheduler jobs run plant8-{env}-notify-tick --location=asia-northeast3`(응답 `remaining`이 0보다 크면 같은 날 한 번 더 실행해 잇는다 — 이미 메일을 받은 사람은 다음 영업일로 미뤄진다)·스케줄러 SA는 bootstrap이 만든다(권한이 바뀐 이 페이즈 뒤 소유자가 bootstrap 재실행 1회)·배포 셸에 `NOTIFY_TICK_OIDC_DISABLED`가 있으면 배포가 거부된다.

③ 새 소절 「이메일(SMTP) 확인 경로」(8줄 이내): Workspace 관리자에게 확인할 네 가지 — 발송 주소(`SMTP_FROM`) · 인증 방식(앱 비밀번호 또는 Workspace SMTP 릴레이) · 사용자별 일 발송 한도 · 호스트·포트(587, STARTTLS). 확인된 값은 Secret Manager 기존 비밀 네 개(`smtp-host-{env}`·`smtp-user-{env}`·`smtp-password-{env}`·`smtp-from-{env}`)에 새 버전으로 넣는다(명령 자리만 — 값을 문서에 적지 않는다). 네 값이 모두 채워지기 전에는 앱이 이메일을 보내지 않고 시스템 상태 「이메일」이 `미설정`이다(D-711). 첫 설정 뒤 스테이징에서 본인에게 1통 받아 확인(VALIDATION Manual-Only).

④ 220행 「`rate_limits` 행 정리는 Phase 7 tick에 붙인다」는 고치지 않는다(이 페이즈 범위 밖 — Phase 7 몫 그대로).

⑤ **저널 가드**(per Codex 2차 #10): `test/unit/db/migration-journal.test.ts`가 없을 때만 `<probe_fallback>` 「저널 가드 소유」의 네 단언으로 만든다(있으면 diff 없이 돌리기만 한다). 먼저 단언 하나를 일부러 뒤집어(예: `when` 비교 방향) 실패하는 것을 한 번 보고 되돌린다 — 가드가 실제로 저널을 읽는다는 확인. 번호를 고정하는 단언(특정 태그 이름·개수)을 넣지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — OPERATIONS.md가 300줄을 넘었거나(04.1·04.3도 이 문서를 늘린다 — 이 플랜 추가분은 20줄 이하로 둔다) 12자리 숫자를 적었거나 필수 토큰을 지웠다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 파일이 없어 「No test files found」가 났거나, 지금 main의 저널에서 idx 연속·tag 접두어·when 증가·SQL 파일 존재 중 하나가 실패했다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "notify-tick" docs/OPERATIONS.md`가 2 이상이다
    - `grep -n "enabled: false" docs/OPERATIONS.md`에 tick 행이 없다
    - `git diff --numstat HEAD -- docs/OPERATIONS.md`의 추가 줄 수가 20 이하다
    - 문서에 SMTP 비밀번호·호스트 값이 없다(자리 표시만)
    - `test -f test/unit/db/migration-journal.test.ts`가 참이고 `grep -c "_journal.json" test/unit/db/migration-journal.test.ts`가 1 이상이다
  </acceptance_criteria>
  <done>운영자가 스케줄러를 수동으로 돌리고, 경보에 대응하고, SMTP 값을 어디서 확인해 어디에 넣는지 문서 한 곳에서 알며, 저널 가드가 wave 1에서 초록이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포자 셸 환경 → deploy.sh | 셸에 남은 변수가 운영 서비스 설정에 섞일 수 있다 |
| Cloud Scheduler → 서비스 | 스케줄러가 서비스 계정 이름으로 OIDC 토큰을 받아 호출한다 |
| 배포자 SA 권한 | GitHub Actions가 쓰는 계정 — 권한이 넓을수록 저장소 침해의 피해가 크다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-30 | Elevation of Privilege | OIDC 우회 변수 | high | mitigate | deploy.sh가 첫 gcloud 호출 전에 exit 2(Task 2) + 앱 `lib/env.ts` 비로컬 부팅 거부(04.2-05) — 이중 차단 |
| T-4.2-31 | Spoofing | 스케줄러 호출자 | high | mitigate | 잡의 OIDC 계정 = 전용 `plant8-{env}-scheduler` · audience = 서비스 URL · 앱이 같은 이메일을 기대(`NOTIFY_TICK_SCHEDULER_SA`). 스모크가 토큰 없는 호출 401을 확인(D-4214) |
| T-4.2-32 | Elevation of Privilege | 배포자 SA | medium | mitigate | 스케줄러 SA는 소유자가 bootstrap으로 만들고 배포자에게는 `cloudscheduler.admin` + 그 SA의 `serviceAccountUser`만(D-4212) — `iam.serviceAccountAdmin` 없음(단위 단언) |
| T-4.2-33 | Denial of Service | tick 멈춤 | medium | mitigate | 25시간 성공 0건 경보 켜기(D-4211 · eng E1·R2 — threshold 합 < 1 + 결측=참, 정렬 기간 + duration 합 90000초 · 템플릿 단위 단언) · 생긴 적 없는 시계열은 환경별 첫 성공 확인으로 막음 · 스케줄러 재시도 0 + 멱등 tick |
| T-4.2-34 | Information Disclosure | SMTP 값 | medium | mitigate | 문서에는 비밀 이름·명령 자리만, 값은 Secret Manager(기존 `_ensure_secret` — 기본값 `__unset__`) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/deploy test/unit/docs-limits.test.ts test/unit/db/migration-journal.test.ts` 초록
- `bash -n scripts/deploy.sh && bash -n scripts/bootstrap-gcp.sh && bash -n infra/names.sh` 통과
- 실제 스케줄러 → 배포된 `/internal/notify-tick` 호출 성공은 사람 확인(VALIDATION Manual-Only 셋째 줄 — 배포 뒤 잡 수동 실행 → 시스템 상태 「알림 발송」 시각 갱신)
- 경보 검증은 경로별로 따로 한다(eng E1 · R2-2):
  - **있던 시계열이 끊김 · 성공 0건 · 새 리비전:** 단위 단언(Task 2 — 결측=참 · 합 < 1 · `groupByFields` 없음 · 합 한도 90000초)
  - **정책을 GCP가 받아들임:** 배포가 `ensure_alerts` 단계를 통과한다(거부면 그 단계에서 배포가 멈춘다 — 사람이 배포 로그로 확인)
  - **한 번도 생긴 적 없는 시계열 — 첫 성공 확인(사람 확인, 스테이징·프로덕션 각각 이 페이즈 첫 배포 때 한 번):** ① `gcloud scheduler jobs run plant8-{env}-notify-tick --location=asia-northeast3` ② `gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="plant8-{env}" AND jsonPayload.event="notify.tick" AND jsonPayload.ok=true' --freshness=1h --limit=1 --format='value(timestamp)'`가 ① 뒤의 시각 한 줄을 출력한다(로그 읽기만 — DB 명령 아님). 빈 출력이면 이 경보는 그 환경에서 울릴 수 없는 상태이므로 원인을 찾을 때까지 이 플랜을 끝난 것으로 보지 않는다. 결과를 SUMMARY에 적는다. 첫 배포 전·첫 성공 전에 경보 사건이 열린다고 기대하지 않는다

</verification>

<success_criteria>

- 배포 한 번으로 매일 09:00 KST 검증 호출 잡이 생기고, 검증을 끈 배포는 거부되며, 엣지·검증 상태가 스모크로 확인된다
- tick 멈춤 경보가 거짓 경보 없이 켜진다
- 운영자 문서가 스케줄러 수동 실행과 SMTP 확인 경로를 담고 300줄 상한 안이다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 셸 함수(신규) | `ensure_scheduler` · `refuse_oidc_bypass`(`scripts/deploy.sh`) |
| 이름 도우미 | `scheduler_sa` · `scheduler_job`(신규) · `policy_tick`(→ `[env] notify tick stale 25h`)(`infra/names.sh`) |
| GCP 리소스(배포 시) | Cloud Scheduler 잡 `plant8-{env}-notify-tick`(매일 09:00 Asia/Seoul, POST, OIDC) · 서비스 계정 `plant8-{env}-scheduler`(bootstrap) · API `cloudscheduler.googleapis.com` · 경보 `[env] notify tick stale 25h`(켜짐) |
| 서비스 환경 변수 | `NOTIFY_TICK_SCHEDULER_SA`(배포가 넣는다) |
| 배포 거부 | 셸 `NOTIFY_TICK_OIDC_DISABLED` → exit 2 |
| 스모크 | `POST /internal/notify-tick` 토큰 없음 = 401 |
| 문서 | `docs/OPERATIONS.md` §6 tick 행 · 「알림 발송 스케줄러」 · 「이메일(SMTP) 확인 경로」 |
| 테스트 | `test/unit/db/migration-journal.test.ts`(drizzle 저널 가드 — 04.2-05에서 옮김, Codex 2차 #10) |
| 사람 할 일 | 소유자 bootstrap-gcp.sh 재실행 1회(`user_setup`) |
| 결정 | D-4211 · D-4212 · D-4214 |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-04-SUMMARY.md` when done
</output>
