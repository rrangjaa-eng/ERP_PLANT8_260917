---
phase: 04.2-notify-holidays-foundation
plan: 05
type: execute
wave: 2
depends_on: [04.2-01]
files_modified:
  - lib/env.ts
  - lib/oidc.ts
  - app/internal/notify-tick/handle.ts
  - test/unit/lib/oidc.test.ts
  - test/unit/env.test.ts
  - test/integration/notify-tick-auth.test.ts
  - test/unit/db/migration-journal.test.ts
autonomous: true
requirements: [NOTI-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/internal/notify-tick`의 기본 설정(주입 없음)은 `schedulerConfigFromEnv(env)`에서 온다 — audience = `BETTER_AUTH_URL`, 기대 호출자 = `NOTIFY_TICK_SCHEDULER_SA`, 검증 끄기 = `APP_ENV === \"local\"`이고 `NOTIFY_TICK_OIDC_DISABLED === \"1\"`일 때만"
    - "audience나 기대 이메일 중 하나라도 비어 있으면 검증은 켜진 채 `not_configured` 401이다 — 실제 기본 핸들러를 테스트 환경 그대로 불러 확인한다(Codex #10)"
    - "거부 사유는 정확히 네 가지다 — `missing_token`(헤더 없음·Bearer 아님) · `not_configured` · `invalid_token`(다른 키 서명·끝 슬래시 audience·다른 issuer·만료) · `unexpected_caller`(다른 이메일·`email_verified` 거짓·`email_verified` 없음·`email` 없음). 모든 거부는 401이고 tick은 돌지 않는다(`notification_log`·`notify_tick_runs` 행 0)"
    - "거부 로그 `notify.tick_unauthorized`는 `reason`만 싣는다 — 토큰·이메일·페이로드를 로그에 쓰지 않는다"
    - "로컬 밖(`APP_ENV`가 `local`이 아님)에서 `NOTIFY_TICK_OIDC_DISABLED`가 있으면 환경 변수 파싱이 실패해 서버가 뜨지 않는다 — deploy.sh의 exit 2(04.2-04)와 이중 차단"
    - "재생 보장은 「인증된 호출 + 발생 단위 멱등」이다: 유효 기간 안의 같은 토큰을 다시 보내면, 후보가 그대로면 `sent: 0`이고, 그사이 새 발생이 생겼으면 그 발생만 처리된다 — 요청 단위 재생 거부는 요구되지 않았다(Codex #11)"
    - "drizzle 저널 가드(`test/unit/db/migration-journal.test.ts` — idx 연속 · tag 접두어 = 0 채운 idx · when 엄격 증가 · SQL 파일 존재)가 초록이다 — 04.1이 먼저 만들었으면 고치지 않고 돌리기만 한다"
  artifacts:
    - path: "app/internal/notify-tick/handle.ts"
      provides: "`schedulerConfigFromEnv` · 기본 설정 연결"
      exports: ["handleNotifyTick", "schedulerConfigFromEnv"]
    - path: "lib/env.ts"
      provides: "`NOTIFY_TICK_SCHEDULER_SA` · `NOTIFY_TICK_OIDC_DISABLED` · 비로컬 거부"
      contains: "NOTIFY_TICK_OIDC_DISABLED"
    - path: "test/integration/notify-tick-auth.test.ts"
      provides: "거부 행렬 통합 · 실제 기본 설정 · 재생 두 경우"
    - path: "test/unit/db/migration-journal.test.ts"
      provides: "drizzle 저널 가드 — 04.2-14의 통합 절차가 기댄다"
  key_links:
    - from: "app/internal/notify-tick/handle.ts"
      to: "lib/env.ts"
      via: "`schedulerConfigFromEnv(env)` — `BETTER_AUTH_URL` · `NOTIFY_TICK_SCHEDULER_SA` · `NOTIFY_TICK_OIDC_DISABLED` · `APP_ENV`"
      pattern: "schedulerConfigFromEnv"
    - from: "scripts/deploy.sh"
      to: "lib/env.ts"
      via: "배포가 서비스 env `NOTIFY_TICK_SCHEDULER_SA`를 넣는다(04.2-04) — 이 플랜이 그 이름을 읽는다"
      pattern: "NOTIFY_TICK_SCHEDULER_SA"
---

<objective>
**As a** 운영자, **I want to** 검증 설정이 빠졌거나 잘못된 호출이 오면 tick이 절대 돌지 않고, 로컬 밖에서는 검증을 끌 방법이 없기를, **so that** 공개 Cloud Run 주소에 누가 POST를 보내도 알림이 만들어지거나 메일이 나가지 않는다.

(스토리는 ROADMAP 04.2 기준 4 「OIDC 검증을 끄는 환경 변수가 있으면 배포가 거부된다」와 NOTI-04 「검증된 스케줄러 호출만」에서 끌어왔다.)

Purpose: NOTI-04의 인증 몫 완성 — 04.2-01의 트레이서가 연 검증기 위에 거부 경로 전부·환경 변수 연결·실제 기본 설정 실측(Codex #10)·재생 보장의 정확한 진술과 테스트(Codex #11). 04.2-14의 마이그레이션 통합이 기대는 저널 가드도 여기서 만든다.

Output: 환경 변수 두 개와 비로컬 거부 · `schedulerConfigFromEnv` · 검증기 거부 행렬 단위 · 인증 통합 테스트 파일 · 저널 가드.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개(google-auth-library는 이미 있다 — 04.2-01). 네트워크 인증서 조회를 테스트에서 부르지 않는다(항상 `getCerts` 주입).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-01-SUMMARY.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — Security Domain STRIDE 표(OIDC 위조·재생)(Grep `STRIDE`)
</context>

<probe_fallback>
이 플랜은 edge 보고서 행을 새로 떠맡지 않는다(NOTI-04 행은 04.2-01). 금지 회상의 「OIDC 검증 없이 tick이 도는 것」(OWASP 인증 — 04.2-01 T-4.2-01이 정규 보안으로 넘긴 것)의 거부 경로 전부를 이 플랜이 증명한다.

**계획 단계 결정(표면화):**

- **`schedulerConfigFromEnv(source)`:** 04.2-01의 기본 설정은 기대 이메일이 `null`로 닫혀 있었다. 이 플랜이 `handle.ts`에 순수 함수 `schedulerConfigFromEnv(source: Pick<Env, "APP_ENV" | "BETTER_AUTH_URL" | "NOTIFY_TICK_SCHEDULER_SA" | "NOTIFY_TICK_OIDC_DISABLED">)`를 두고 기본 설정을 `schedulerConfigFromEnv(env)`로 바꾼다. 빈 문자열은 `null`로 친다(`BETTER_AUTH_URL`이 로컬에서 선택값이라 빈 값이 들어올 수 있다 — Codex #10). 검증 끄기는 `APP_ENV === "local" && NOTIFY_TICK_OIDC_DISABLED === "1"`일 때만 참이다.
- **실제 기본 설정 실측(Codex #10):** 테스트는 `deps.config`를 주입하지 않고 기본 핸들러를 부른다. 통합 테스트 환경에는 `NOTIFY_TICK_SCHEDULER_SA`가 없으므로 올바르게 서명된 토큰이라도 `not_configured` 401이어야 한다 — 「주입한 설정에서만 안전한」 상태를 잡는다.
- **재생(Codex #11):** 04.2-01 T-4.2-02를 「인증된 호출 + 발생 단위 멱등」으로 고쳤다. 이 플랜은 그 진술을 테스트로 고정한다 — 같은 토큰 두 번(후보 그대로 → `sent: 0`), 발생 하나를 더한 뒤 같은 토큰(→ `sent: 1`, 새 발생만). 요청 단위 재생 방지(jti 원장 등)는 요구되지 않았고 만들지 않는다.
- **저널 가드:** `test/unit/db/migration-journal.test.ts`는 실행 시점에 파일이 없을 때만 만든다(04.1이 같은 경로를 만든다 — 있으면 고치지 않고 돌리기만 한다). 내용: `db/migrations/meta/_journal.json`을 읽어 `entries[i].idx === i`(0..n-1 연속), `tag`가 `String(idx).padStart(4, "0") + "_"`로 시작, `when`이 앞 항목보다 엄격히 큼, `tag + ".sql"` 파일이 `db/migrations/`에 있음.
</probe_fallback>

## Review Dispositions

| Codex # | 처리 | 한 줄 |
|---|---|---|
| 10 (medium) | 반영 | `schedulerConfigFromEnv`가 빈 audience·빈 이메일을 `null`로 만들고 검증기가 `not_configured`로 거부 · 실제 기본 핸들러를 주입 없이 불러 401 확인 · `email_verified` 없음 케이스(Task 1·2) |
| 11 (medium) | 반영 | 보장을 「인증된 호출 + 발생 단위 멱등」으로 진술 · 재생 두 경우(후보 그대로 · 새 발생) 통합 테스트(Task 2) · 요청 단위 재생 거부는 요구 밖이라 만들지 않음 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 환경 변수에서 기본 설정까지 한 길: 테스트 환경 그대로의 기본 핸들러가 올바른 토큰에도 `not_configured` 401을 돌려주고 tick이 돌지 않는다</name>
  <files>lib/env.ts, app/internal/notify-tick/handle.ts, test/unit/env.test.ts, test/integration/notify-tick-auth.test.ts</files>
  <read_first>
    - `lib/env.ts` — 선택값 도우미 · 비로컬 `superRefine` 가지 · `ENV_KEYS`
    - `app/internal/notify-tick/handle.ts` · `lib/oidc.ts`(04.2-01 결과) — `NotifyTickHandlerDeps.config` 모양 · 기본 설정 자리
    - `test/support/notify-tick.ts`(04.2-01 결과) — `createTestSigner` · `createTestConditionKind` · `testCandidate`
    - `test/integration/notify-tick.test.ts` 준비부(04.2-01 결과) — 사용자 만들기·핸들러 호출 모양(이 플랜은 새 파일에 쓴다 — 04.2-01의 파일을 고치지 않는다)
    - `test/unit/env.test.ts` — 비로컬 superRefine 단언 모양
  </read_first>
  <behavior>
    - 통합(기본 설정): `handleNotifyTick(요청, { verify: 테스트 인증서를 쓰는 verifySchedulerToken, runTick: 테스트 조건 종류(발생 1건) })` — `config`는 주입하지 않는다 — 에 테스트 서명기의 올바른 토큰 → 401 `{ error: "unauthorized" }` · `log.warn("notify.tick_unauthorized", { reason: "not_configured" })` 1회 · `notification_log`·`notify_tick_runs` 행 0
    - 단위(`schedulerConfigFromEnv`): `{ APP_ENV: "staging", BETTER_AUTH_URL: "https://a.example", NOTIFY_TICK_SCHEDULER_SA: "s@x.iam.gserviceaccount.com" }` → `{ audience: "https://a.example", schedulerSa: "s@…", oidcDisabled: false }` · `BETTER_AUTH_URL` 빈 문자열 → `audience: null` · `APP_ENV: "local"` + `NOTIFY_TICK_OIDC_DISABLED: "1"` → `oidcDisabled: true` · `APP_ENV: "local"` + `"true"` → `false` · `APP_ENV: "staging"` + `"1"` → `false`
    - env: `APP_ENV=staging` + `NOTIFY_TICK_OIDC_DISABLED=1` → 파싱 오류(메시지에 변수 이름) · `APP_ENV=local` + 같은 값 → 통과 · 두 변수 모두 없어도 통과(선택값)
  </behavior>
  <action>
① `test/integration/notify-tick-auth.test.ts`(신규)에 첫 behavior를 쓰고(RED — 지금 기본 설정의 이메일이 `null`이라 이미 401일 수 있다: 그러면 `schedulerConfigFromEnv`가 없어 import 단계에서 실패하는 단위 케이스가 RED 역할을 한다), `test/unit/env.test.ts`에 env 세 케이스를 더한다. `schedulerConfigFromEnv` 단위 케이스는 `test/unit/lib/oidc.test.ts`(신규 — Task 2가 이어 쓴다)에 둔다.

② `lib/env.ts`: `NOTIFY_TICK_SCHEDULER_SA`(선택 문자열 — 스케줄러 서비스 계정 이메일)·`NOTIFY_TICK_OIDC_DISABLED`(선택 문자열)를 기존 선택값 도우미로 더하고 `ENV_KEYS`에도 더한다(항목만 추가). 비로컬 `superRefine` 가지에 「`NOTIFY_TICK_OIDC_DISABLED`가 있으면 오류 — 메시지 `NOTIFY_TICK_OIDC_DISABLED is only allowed when APP_ENV=local`」 한 가지를 더한다.

③ `app/internal/notify-tick/handle.ts`: `<probe_fallback>`의 `schedulerConfigFromEnv(source)`를 내보내고 기본 `config`를 `schedulerConfigFromEnv(env)`로 바꾼다. `oidcDisabled`가 참이면 검증을 건너뛰고 `log.warn("notify.tick_oidc_disabled")`를 한 번 남긴다(로컬에서만 가능 — env가 막는다). 다른 흐름(401·409·200·500)은 04.2-01 그대로다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/env.test.ts test/unit/lib/oidc.test.ts</automated>
    <fails_when>failed 개수가 1 이상이거나 "No test files found"가 나온다 — 비로컬에서 우회 변수가 통과했거나, 빈 audience가 `null`이 되지 않았거나, `"1"` 외의 값이나 비로컬에서 검증이 꺼졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-tick-auth.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 주입 없는 기본 핸들러가 올바른 토큰에 200을 돌려줬거나(기본 설정이 열려 있다), 거부 사유가 `not_configured`가 아니거나, tick 행이 생겼다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "NOTIFY_TICK_OIDC_DISABLED" lib/env.ts`가 2 이상(정의 + 비로컬 거부)이다
    - `grep -n "schedulerConfigFromEnv(env)" app/internal/notify-tick/handle.ts`가 한 줄이다
    - 통합: 주입 없는 기본 설정 + 올바른 토큰 → 401 `not_configured` · 행 0
  </acceptance_criteria>
  <done>실제 배포 설정이 빠진 상태의 서버는 올바른 토큰에도 tick을 돌리지 않고, 로컬 밖에서 검증을 끌 수 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 거부 행렬 전부 · 재생 두 경우 · 로그 비노출 · 저널 가드</name>
  <files>lib/oidc.ts, app/internal/notify-tick/handle.ts, test/unit/lib/oidc.test.ts, test/integration/notify-tick-auth.test.ts, test/unit/db/migration-journal.test.ts</files>
  <read_first>
    - `lib/oidc.ts`(04.2-01 결과) — `verifySchedulerToken` 순서와 네 사유
    - `node_modules/google-auth-library/build/src/auth/oauth2client.d.ts` — `verifySignedJwtWithCertsAsync(jwt, certs, requiredAudience, issuers?, maxExpiry?)`
    - `db/migrations/meta/_journal.json` — `idx`·`tag`·`when` 모양
  </read_first>
  <behavior>
    - 단위(`verifySchedulerToken`, 기대 `{ audience: "https://notify.test.invalid", email: "scheduler@test.invalid" }`): 기대 audience `null` · 기대 이메일 `null` · 기대 audience 빈 문자열 → `not_configured`(세 케이스) · `Authorization` 없음 · `Basic …` → `missing_token` · 다른 키로 서명 · audience가 `https://notify.test.invalid/`(끝 슬래시) · issuer `https://evil.example` · 만료된 exp → `invalid_token` · 이메일이 다른 서비스 계정 · `email_verified: false` · `email_verified` 칸 없음 · `email` 칸 없음 → `unexpected_caller` · 바른 토큰 → `{ ok: true }`
    - 통합(설정 주입 `{ audience, schedulerSa, oidcDisabled: false }`): 토큰 없음 → 401 · 경고 로그 1회(`reason: "missing_token"`) · 행 0 · 다른 이메일 토큰 → 401(`unexpected_caller`) · 행 0
    - 통합 재생: 바른 토큰 T로 발생 1건 → `{ sent: 1 }` · 같은 T 다시(발생 그대로) → `{ sent: 0, skipped: 1 }` · 테스트 조건 종류 원천에 발생 1건 추가 뒤 같은 T → `{ sent: 1, skipped: 1 }` · 원장 행 2
    - 로그 비노출: 위 모든 통합 케이스에서 `log.warn`·`log.info`·`log.error` 스파이의 인자 JSON 문자열에 토큰 문자열·`scheduler@test.invalid`·다른 호출자 이메일이 없다
    - 저널 가드: 현재 `_journal.json`에 대해 초록
  </behavior>
  <action>
① behavior 단위 케이스를 `test/unit/lib/oidc.test.ts`에, 통합 케이스를 `test/integration/notify-tick-auth.test.ts`에 더한다(RED). 테스트 토큰은 `createTestSigner()`(04.2-01 — `test/support/notify-tick.ts`)로 만들고 몸 필드를 덮어써 각 거부 케이스를 만든다(인증서는 항상 `getCerts` 주입).

② 실패하는 케이스만 `lib/oidc.ts`·`handle.ts`를 고쳐 초록으로 만든다. `email_verified`는 `=== true`만 통과(칸 없음·문자열 `"true"` 거부). 로그 필드는 `reason`만 싣는다.

③ `test/unit/db/migration-journal.test.ts`: 파일이 없을 때만 `<probe_fallback>`의 네 단언으로 만든다. 있으면 diff 없이 돌리기만 한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/lib/oidc.test.ts test/unit/db/migration-journal.test.ts</automated>
    <fails_when>failed 개수가 1 이상이거나 "No test files found"가 나온다 — 거부 사유가 뒤바뀌었거나(예: 끝 슬래시 audience가 통과, `email_verified` 없음이 통과), 검증기가 네트워크 인증서를 불렀거나, 저널 idx·tag·when이 어긋났다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-tick-auth.test.ts test/integration/notify-tick.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 거부된 호출에 tick이 돌아 행이 생겼거나, 재생이 같은 발생을 두 번 넣었거나 새 발생을 막았거나, 로그 인자에 토큰·이메일이 실렸거나, 04.2-01의 트레이서 테스트가 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/oidc.ts`의 반환 사유가 정확히 네 가지다(`missing_token`·`not_configured`·`invalid_token`·`unexpected_caller`)
    - 통합 재생: 같은 토큰 세 번 → 원장 행 2(발생 2) · 두 번째 응답 `sent: 0`
    - 로그 비노출 단언이 통합 파일에 있다(`grep -c "scheduler@test.invalid" test/integration/notify-tick-auth.test.ts`가 1 이상이고 `not.toContain` 단언과 함께 쓰인다)
    - `test/unit/db/migration-journal.test.ts`가 있고 초록이다
  </acceptance_criteria>
  <done>위조·잘못된 호출자·설정 누락은 모두 401이고 tick은 돌지 않으며, 재생의 보장 범위가 테스트로 고정되고, 로그에 호출자 정보가 남지 않는다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → `/internal/notify-tick` | Cloud Run 서비스는 공개(`--allow-unauthenticated`)라 누구나 POST할 수 있다 — 앱 안 OIDC 검증이 유일한 문이다 |
| 서비스 환경 변수 → 검증 설정 | 배포가 넣은 값(또는 빠진 값)이 검증의 기대값이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-110 | Spoofing | 기대값 누락 | high | mitigate | audience·이메일 중 하나라도 비면 `not_configured` — 실제 기본 핸들러를 주입 없이 불러 확인(Task 1, Codex #10) |
| T-4.2-111 | Spoofing | 위조·다른 호출자 토큰 | high | mitigate | 서명·audience(끝 슬래시 포함)·issuer·exp·이메일·`email_verified === true` 거부 행렬 단위 + 통합(Task 2) |
| T-4.2-112 | Tampering | 재생된 토큰 | medium | accept | 보장은 「인증된 호출 + 발생 단위 멱등」 — 재생은 정상 tick과 같게 동작하고 원장 유니크 키로 같은 알림을 두 번 만들지 못한다(Task 2 재생 테스트). 요청 단위 재생 거부는 요구 밖(Codex #11) |
| T-4.2-113 | Elevation of Privilege | 검증 끄기 변수 | high | mitigate | 로컬 밖 env 파싱 실패(Task 1) + deploy.sh exit 2(04.2-04) — 로컬에서도 `"1"`만 인정 |
| T-4.2-114 | Information Disclosure | 거부 로그 | medium | mitigate | 로그 필드는 `reason`뿐 — 토큰·이메일이 로그 인자에 없음을 통합 스파이로 단언(Task 2) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/env.test.ts test/unit/lib/oidc.test.ts test/unit/db/migration-journal.test.ts` 초록
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/notify-tick-auth.test.ts test/integration/notify-tick.test.ts` 초록
- `pnpm lint && pnpm typecheck` 통과

</verification>

<success_criteria>

- 기본 설정·거부 경로·재생 보장이 테스트로 고정되고, 설정이 빠진 서버는 닫힌 쪽으로 실패한다
- 로컬 밖에서 검증을 끌 수 없다
- 저널 가드가 있어 04.2-14의 마이그레이션 통합이 번호 어긋남을 잡을 수 있다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 환경 변수 | `NOTIFY_TICK_SCHEDULER_SA`(선택) · `NOTIFY_TICK_OIDC_DISABLED`(선택 — 로컬 밖이면 파싱 오류)(`lib/env.ts`) |
| 함수(신규) | `schedulerConfigFromEnv(source)`(`app/internal/notify-tick/handle.ts`) |
| 로그 이벤트 | `notify.tick_oidc_disabled`(로컬 우회 때 경고 1줄) |
| 테스트 | `test/integration/notify-tick-auth.test.ts`(신규) · `test/unit/lib/oidc.test.ts`(신규) · `test/unit/env.test.ts`(케이스 추가) · `test/unit/db/migration-journal.test.ts`(없을 때만 신규) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-05-SUMMARY.md` when done
</output>
