---
phase: 04.2-notify-holidays-foundation
plan: 05
subsystem: auth
tags: [notify-tick, oidc, google-auth-library, env, cloud-scheduler, zod]

requires:
  - phase: 04.2-01
    provides: "handleNotifyTick(검증 → tick → 200/401/409/500) · verifySchedulerToken 네 사유 · test/support 서명기"
  - phase: 04.2-04
    provides: "deploy.sh가 서비스 env NOTIFY_TICK_SCHEDULER_SA를 넣고, NOTIFY_TICK_OIDC_DISABLED면 exit 2"
provides:
  - "schedulerConfigFromEnv(source) — audience = BETTER_AUTH_URL, 기대 호출자 = NOTIFY_TICK_SCHEDULER_SA, 빈 값은 null, 검증 끄기는 APP_ENV=local + \"1\"일 때만"
  - "handle.ts 기본 설정 = schedulerConfigFromEnv(env) · oidcDisabled면 검증 건너뜀 + notify.tick_oidc_disabled 경고"
  - "lib/env.ts NOTIFY_TICK_SCHEDULER_SA · NOTIFY_TICK_OIDC_DISABLED(선택) · 로컬 밖 NOTIFY_TICK_OIDC_DISABLED면 파싱 실패"
  - "거부 행렬 단위 · 인증 통합(기본 설정 실측, 거부, 재생 두 경우, 로그 비노출) 테스트"
affects: [04.2-10, 04.2-13, 04.2-14, deploy, phase-7-NOTI-03]

actuals:
  tokens: 5950
  tasks: 2
  commits: 4
plan_head_before: ca4c546caa4e0e254a75017ec5dafaba929c104c

tech-stack:
  added: []
  patterns:
    - "서비스 env → 검증 설정은 순수 함수(schedulerConfigFromEnv)로 두고 모듈 로드 때 한 번 적용한다"
    - "보안 거부 경로 테스트는 돌연변이(검증 줄 제거·로그에 토큰 싣기)로 테스트가 잡는지 확인한다"

key-files:
  created:
    - test/integration/notify-tick-auth.test.ts
    - test/unit/notify-tick/oidc.test.ts
  modified:
    - lib/env.ts
    - app/internal/notify-tick/handle.ts
    - test/unit/env.test.ts
    - test/support/notify-tick.ts

key-decisions:
  - "schedulerConfigFromEnv는 `||`로 빈 문자열을 null로 만든다 — 빈 audience·빈 SA면 검증기가 not_configured로 닫는다"
  - "oidcDisabled 경고는 호출마다 한 줄(notify.tick_oidc_disabled) — 로컬에서만 가능하므로 소음 걱정이 없다"
  - "재생 보장은 「인증된 호출 + 발생 단위 멱등」 그대로 — 요청 단위 재생 거부(jti 원장)는 만들지 않았다(Codex #11)"
  - "단위 테스트 파일은 test/unit/lib/ 대신 test/unit/notify-tick/oidc.test.ts — eslint boundaries가 test/unit/lib/*를 lib 계층으로 분류한다"

patterns-established:
  - "test/unit/<계층 이름>/ 아래에 둔 테스트는 그 계층 경계 규칙을 받는다 — app·test를 import하는 테스트는 계층 이름이 아닌 폴더에 둔다"

requirements-completed: [NOTI-04]

coverage:
  - id: D1
    description: "주입 없는 기본 핸들러가 테스트 환경 그대로(NOTIFY_TICK_SCHEDULER_SA 없음)에서 올바른 토큰에도 not_configured 401을 돌려주고 tick 행이 0이다"
    requirement: NOTI-04
    verification:
      - kind: integration
        ref: "test/integration/notify-tick-auth.test.ts#기대 호출자 이메일이 없으면 올바르게 서명된 토큰에도 not_configured 401이고 tick은 돌지 않는다"
        status: pass
    human_judgment: false
  - id: D2
    description: "schedulerConfigFromEnv — audience·SA 연결, 빈 값 null, 검증 끄기는 local + \"1\"일 때만"
    requirement: NOTI-04
    verification:
      - kind: unit
        ref: "test/unit/notify-tick/oidc.test.ts#schedulerConfigFromEnv"
        status: pass
    human_judgment: false
  - id: D3
    description: "로컬 밖에서 NOTIFY_TICK_OIDC_DISABLED가 있으면 env 파싱 실패(부팅 불가), 로컬에서는 통과, 두 변수 모두 선택값"
    requirement: NOTI-04
    verification:
      - kind: unit
        ref: "test/unit/env.test.ts#APP_ENV=staging에 NOTIFY_TICK_OIDC_DISABLED가 있으면 throw한다"
        status: pass
      - kind: unit
        ref: "test/unit/env.test.ts#APP_ENV=local이면 NOTIFY_TICK_OIDC_DISABLED가 있어도 파싱 성공한다"
        status: pass
    human_judgment: false
  - id: D4
    description: "검증기 거부 행렬 — missing_token·not_configured·invalid_token·unexpected_caller 네 사유, 인증서 항상 주입"
    requirement: NOTI-04
    verification:
      - kind: unit
        ref: "test/unit/notify-tick/oidc.test.ts#verifySchedulerToken 거부 행렬"
        status: pass
    human_judgment: false
  - id: D5
    description: "거부 호출은 401이고 tick이 돌지 않으며, 로그 인자에 토큰·이메일이 없다 · 같은 토큰 재생은 발생 단위 멱등"
    requirement: NOTI-04
    verification:
      - kind: integration
        ref: "test/integration/notify-tick-auth.test.ts#notify-tick 거부 경로(설정 주입)"
        status: pass
      - kind: integration
        ref: "test/integration/notify-tick-auth.test.ts#후보 그대로면 sent 0, 새 발생이 생기면 그 발생만 처리된다"
        status: pass
    human_judgment: false
  - id: D6
    description: "실제 배포 환경에서 스케줄러 잡 한 번 수동 실행 → 첫 성공 로그 확인(NOTIFY_TICK_SCHEDULER_SA가 이제 연결됨)"
    verification: []
    human_judgment: true
    rationale: "실제 GCP(Cloud Scheduler OIDC 토큰·Cloud Run env)는 세션에서 부르지 않는다 — 소유자가 배포 뒤 환경마다 확인해야 한다"

duration: 9min
completed: 2026-09-24
status: complete
---

# Phase 04.2 Plan 05: notify-tick OIDC 설정 연결 · 거부 행렬 Summary

**`/internal/notify-tick`의 기본 설정을 `schedulerConfigFromEnv(env)`(audience = `BETTER_AUTH_URL`, 기대 호출자 = `NOTIFY_TICK_SCHEDULER_SA`, 빈 값은 닫힘)로 연결했다. 로컬 밖에서는 `NOTIFY_TICK_OIDC_DISABLED`가 있으면 부팅이 실패한다. 거부 네 사유·재생 보장·로그 비노출은 단위 19건과 통합 5건으로 고정했다.**

## Performance

- **Duration:** 약 9분
- **Started:** 2026-09-24T22:11:23Z
- **Completed:** 2026-09-24T22:20:30Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- 04.2-04 배포가 넣는 `NOTIFY_TICK_SCHEDULER_SA`를 핸들러가 읽는다. 이 플랜 전에는 라우트가 항상 `not_configured` 401이었고, 이제 배포된 스케줄러 호출이 통과할 수 있다.
- 설정이 빠지거나 빈 값이면 닫힌 쪽으로 실패한다. 주입 없는 기본 핸들러를 테스트 환경 그대로 불러, 올바른 토큰에도 401이 나오고 tick 행이 0인지 확인했다(Codex #10).
- OIDC 검증 끄기를 두 겹으로 막았다. 로컬 밖이면 env 파싱에서 부팅이 실패하고, 04.2-04의 deploy.sh exit 2도 막는다. 로컬에서도 값이 정확히 `"1"`일 때만 꺼진다.
- 재생 보장을 「인증된 호출 + 발생 단위 멱등」으로 테스트에 고정했다. 같은 토큰을 다시 보내면 후보가 그대로일 때 `sent: 0`이고, 새 발생이 생기면 그 발생만 처리된다(Codex #11).

## Task Commits

1. **Task 1 (tracer): 환경 변수 → 기본 설정 한 길**
   - `9cea818` test — RED(단위 7건 실패: `schedulerConfigFromEnv is not a function`·env 값 undefined·비로컬 throw 없음, 통합 1건 실패: oidcDisabled 401 ≠ 200)
   - `0491195` feat — GREEN
   - `a2f85b9` test — 단위 테스트 파일 이동(아래 이탈 1)
2. **Task 2: 거부 행렬 · 재생 · 로그 비노출** — `5ceee83` test

**Plan metadata:** 이 SUMMARY 커밋

## Files Created/Modified

- `lib/env.ts` — 선택값 두 개 추가, `ENV_KEYS`에 항목 추가, 비로컬 `superRefine`에 `NOTIFY_TICK_OIDC_DISABLED is only allowed when APP_ENV=local` 추가
- `app/internal/notify-tick/handle.ts` — `schedulerConfigFromEnv` 내보냄, 기본 설정 연결, oidcDisabled 분기(경고 한 줄)
- `test/integration/notify-tick-auth.test.ts`(신규) — 기본 설정 실측, oidcDisabled, 거부 두 경우, 재생, 로그 비노출
- `test/unit/notify-tick/oidc.test.ts`(신규) — `schedulerConfigFromEnv` 4건과 `verifySchedulerToken` 거부 행렬 15건
- `test/unit/env.test.ts` — 케이스 3건 추가, 저장·복원 키 목록에 새 변수 2개 추가
- `test/support/notify-tick.ts` — 서명기의 타입만 넓혔다(`email` 생략 가능, `email_verified` 문자열 허용)

## TDD 기록

- **Task 1:** RED를 먼저 확인했다. 기본 설정 통합 케이스는 04.2-01 기본값(`schedulerSa: null`)에서 이미 401이었다. 플랜이 예상한 대로 import 단계의 단위 실패가 RED 역할을 했다. 이 케이스가 실제로 잡는지는 돌연변이로 확인했다. 기본 SA를 `"scheduler@test.invalid"`로 바꾸자 `expected 200 to be 401`로 실패했다.
- **Task 2:** 04.2-01 검증기가 이미 행렬을 만족해 테스트가 처음부터 초록이었다. 플랜 ②는 「실패하는 케이스만 고친다」고 했으므로 프로덕션 코드는 바꾸지 않았다. 대신 돌연변이 8종으로 테스트가 잡는지 확인했고, 모두 해당 테스트 하나 이상이 실패했다. 돌연변이 뒤 파일은 원래대로 되돌렸다(diff 없음).
  - `lib/oidc.ts`: 빈 audience를 null로만 판정, `email_verified` truthy 허용, issuer 목록 제거, 이메일 비교 제거, Bearer 외 방식 허용
  - `handle.ts`: 거부 로그에 authorization 싣기, 거부 로그에 기대 이메일 싣기, 거부 뒤 tick 실행

## Decisions Made

- 빈 문자열은 `||`로 null 처리했다(`BETTER_AUTH_URL`이 로컬에서 선택값이라 빈 값이 들어올 수 있다).
- oidcDisabled 경고는 호출마다 한 줄 남긴다. 로컬에서만 켤 수 있어 소음 문제가 없다.
- 요청 단위 재생 거부(jti 원장)는 만들지 않았다. 요구 범위 밖이다(Codex #11, T-4.2-112 accept).
- 체크포인트가 없는 플랜이라 기본값을 고를 일이 없었다. 트레이서 게이트는 interactive + `end-of-phase` + 자동 검증만 있는 경우라, 검증을 다시 돌려 통과한 뒤 Task 2로 넘어갔다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 단위 테스트 경로 `test/unit/lib/oidc.test.ts` → `test/unit/notify-tick/oidc.test.ts`**
- **Found during:** Task 1 (커밋 뒤 eslint)
- **Issue:** eslint boundaries 요소 패턴 `lib/**`가 `test/unit/lib/*`를 lib 계층으로 분류한다. lib → app(`handle.ts`), lib → test(`test/support`) import가 금지라 `pnpm lint`가 실패했다.
- **Fix:** 파일을 `git mv`로 옮겼다(내용 변경 없음). eslint 설정은 건드리지 않았다.
- **Files modified:** test/unit/notify-tick/oidc.test.ts
- **Verification:** `pnpm lint` exit 0, 단위 4/4
- **Committed in:** `a2f85b9`
- 플랜 `<verify>`의 `test/unit/lib/oidc.test.ts` 경로는 새 경로로 바꿔 실행했다.

**2. [Rule 3 - Blocking] `test/support/notify-tick.ts` 서명기 타입 확장(플랜 files 목록 밖)**
- **Found during:** Task 2
- **Issue:** 「email 칸 없음」과 「email_verified 문자열 "true"」 케이스를 만들려면 `sign`의 `email: string` 필수와 `email_verified?: boolean` 타입을 넓혀야 했다. 넓히지 않으면 캐스트가 필요하다.
- **Fix:** `sign(claims: TestTokenClaims & { aud: string })`, `email_verified?: boolean | string`으로 바꿨다. 런타임 동작은 같고, 기존 호출부는 그대로 컴파일된다.
- **Verification:** typecheck 0, notify-tick 통합 27/27
- **Committed in:** `5ceee83`

**3. [Rule 2 - 테스트 추가] oidcDisabled 분기 통합 케이스**
- 새 분기(검증 건너뜀 + 경고)는 플랜 behavior에 테스트 항목이 없었다. TDD를 지키려고 통합 1건을 더했다(`9cea818`).

---

**Total deviations:** 3 (경로 이동 1, 테스트 지원 타입 1, 테스트 추가 1)
**Impact on plan:** 모두 lint 통과와 테스트 표현을 위한 것이다. 범위는 늘지 않았다.

## Issues Encountered

- Task 1 GREEN 커밋 명령을 `;`로 이어 eslint 결과와 상관없이 커밋이 먼저 들어갔다. eslint 오류는 모두 RED 커밋의 테스트 파일에 있었다(미사용 `AUDIENCE`는 Task 2에서 사용, boundaries는 이탈 1로 처리). 프로덕션 코드 커밋 `0491195` 자체는 eslint가 깨끗했다.

## 최종 검증 (2026-09-24 22:19Z)

- `pnpm lint` exit 0 · `pnpm typecheck` exit 0 · `pnpm lint:sql` 0 issues(13 files)
- `CI=true pnpm test:unit`: 80 files, 834/834 통과
- 통합: `notify-tick-auth.test.ts` 5/5, `notify-tick.test.ts` 27/27(함께 돌려 32/32)
- `git diff --stat ca4c546 HEAD -- package.json pnpm-lock.yaml` 출력 없음. 새 의존성 0개(T-4.2-SC)
- 수용 기준: `grep -c NOTIFY_TICK_OIDC_DISABLED lib/env.ts` = 5 · `schedulerConfigFromEnv(env)` 1줄(handle.ts:28) · oidc.ts 사유 정확히 네 가지 · 통합 파일 `scheduler@test.invalid` 2회, `not.toContain` 3회

## User Setup Required

None. 새 env는 배포(04.2-04)가 이미 넣는다.

## Next Phase Readiness

- 배포 뒤 환경마다 스케줄러 잡을 한 번 수동 실행해 첫 성공 로그를 확인해야 한다(소유자 몫. 이 플랜으로 확인이 가능해졌다).
- 다음 플랜은 04.2-07(알림함 UI)이다.
- 한도 풀리면 Codex 재확인 필요(04.2-05 포함, 2026-09-29 이후).
- 페이즈 끝 게이트: 인증 경로를 건드렸으므로 `/cso`는 필수다.

## Self-Check: PASSED

- 파일 존재: lib/env.ts, app/internal/notify-tick/handle.ts, test/integration/notify-tick-auth.test.ts, test/unit/notify-tick/oidc.test.ts, test/unit/env.test.ts, test/support/notify-tick.ts
- 커밋 존재: 9cea818, 0491195, a2f85b9, 5ceee83

---
*Phase: 04.2-notify-holidays-foundation*
*Completed: 2026-09-24*
