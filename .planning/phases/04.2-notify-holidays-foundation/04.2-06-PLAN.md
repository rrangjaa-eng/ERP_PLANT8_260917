---
phase: 04.2-notify-holidays-foundation
plan: 06
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - app/(app)/layout.tsx
  - ui/shell/Shell.tsx
  - ui/shell/TopBar.tsx
  - ui/shell/TopBar.module.css
  - ui/shell/BottomTabs.tsx
  - ui/shell/MoreSheet.tsx
  - ui/shell/role-menu.ts
  - app/(app)/notifications/page.tsx
  - app/(app)/notifications/loading.tsx
  - app/(app)/notifications/inbox-list.tsx
  - app/(app)/notifications/actions.ts
  - app/(app)/notifications/notifications.module.css
  - domain/notify/inbox.ts
  - repositories/notifications.ts
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - test/unit/ui/role-menu.test.ts
  - test/integration/notify-inbox.test.ts
  - test/e2e/notify-inbox.spec.ts
  - test/e2e/mobile-notify-inbox.spec.ts
autonomous: true
requirements: [NOTI-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tick(테스트 조건 종류) → 상단 바 배지 1 → 알림함 열람 → 같은 화면에서 배지 0 흐름이 Playwright E2E로 CI에 있다(ROADMAP 04.2 기준 2)"
    - "알림함 `/notifications`는 로그인한 모든 사용자가 쓰는 개인 화면이다 — 권한표 메뉴가 아니고, 페이지는 자기 세션 가드를 갖고, 목록·안 읽은 수·읽음 처리는 받는 사람 = `viewer.id`만 다룬다(D-4208)"
    - "알림함을 열면 연 순간까지 생긴 그 사람의 안 읽은 알림이 모두 읽음이 되고, 이번 방문 동안은 연 순간 안 읽음이던 행에 인셋 선이 남는다(UI-SPEC S1-d · D-4206)"
    - "SYSTEM.md가 UI-SPEC 수정 제안 #2~#7(§7-12 표기·계약 · §4-3 인셋 허용 · §6-8 다섯 항목 · §7-11 B1·B2와 우선순위 · §6-0 (a) 메뉴와 배지 · §6 레이아웃 한 줄)을 더하고 DECISIONS.md에 「Phase 04.2 알림함·공휴일 UI 계약」 한 항목이 먼저 들어간다 — #1(§6-10 공휴일)은 `ADMIN_MENUS`와 같은 커밋이어야 해서 04.2-07이 넣는다"
    - "같은 시각에 생긴 알림 두 건은 알림함에서 각각 한 줄로 보이고 합쳐지지 않는다(알림함은 건별, D-706)"
    - "안 읽은 알림이 0건이면 배지를 그리지 않고, 알림이 0건이면 알림함은 「알림이 없습니다」 한 줄이다(SYSTEM §7-12)"
    - statement: "알림 제목·본문의 긴 한국어·특수문자·HTML 문자는 이스케이프되어 글자 그대로 보이고 알림함 줄 레이아웃을 깨지 않는다"
      verification: backstop
    - "이미 읽은 알림을 다시 읽음 처리해도 read_at이 바뀌지 않고 오류가 없으며, 배지 수는 recipient = viewer이고 read_at IS NULL인 건수다"
    - "알림함을 여는 순간의 읽음 처리는 그 시점까지 생성된 건만 대상이며, 그 사이 tick이 넣은 새 알림은 안 읽음으로 남는다"
    - "UI S1-badge/empty — 안 읽은 알림이 0건이면 트리거 안에 배지 요소와 스크린 리더 글자가 렌더되지 않는다"
    - "UI S1-badge/loading — 배지는 레이아웃 서버 렌더에서 건수와 함께 나오며 클라이언트 로딩 표시(뼈대·스피너)가 없다"
    - "UI S1-badge/error — 안 읽은 건수 조회가 실패하면 배지를 렌더하지 않고 셸은 정상 렌더된다(서버 로그만 남는다)"
    - "UI S1-badge/populated — 안 읽은 알림 3건이면 트리거가 `박서연 3`으로 보이고 접근 가능 이름은 「박서연 · 안 읽은 알림 3건」이다"
    - "UI S1-badge/partial — 알림함을 열어 읽음 처리한 직후 같은 화면에서 배지가 사라진다(E2E 배지 1 → 열람 → 배지 0)"
    - statement: "UI S1-badge/overflow — 폰 375px에서 긴 이름 + 배지가 트리거 폭을 넘으면 이름이 말줄임되고 배지는 잘리지 않으며 트리거가 44×44 이상이다"
      verification: backstop
    - "UI S1-badge/zero-one-many — 0 → 배지 없음, 1~99 → 숫자 그대로, 100 이상 → `99+`"
    - statement: "UI S1-badge/long-text — 한글·영문 혼용 20자 이름에서도 배지 숫자가 한 줄(nowrap)로 트리거 안에 보인다"
      verification: backstop
    - "UI S1-menu-item/empty — 안 읽은 알림이 0이면 메뉴 항목 라벨이 숫자 없이 `알림함`이다"
    - "UI S1-menu-item/loading — 메뉴 항목은 서버가 넘긴 건수로 즉시 렌더되고 메뉴를 열 때 추가 조회·로딩 표시가 없다"
    - "UI S1-menu-item/error — 건수 조회 실패 시 항목은 `알림함`(숫자 없음)으로 남고 링크는 동작한다"
    - "UI S1-menu-item/populated — PC 사용자 메뉴 순서가 `관리`(권한 시) · `알림함 N` · `내 정보` · `로그아웃`이고 폰 「더보기」 계정 그룹 순서가 `알림함 N` · `내 정보` · `설정` · `로그아웃`이다"
    - "UI S1-menu-item/partial — 관리 권한이 없는 계급은 PC 메뉴 첫 항목이 `알림함`이고 빈 자리가 없다"
    - "UI S1-menu-item/overflow — 메뉴 항목 수가 하나 늘어도 기존 행 높이·안쪽 포커스 링 그대로 목록이 세로로 늘어날 뿐 스크롤·잘림이 없다"
    - "UI S1-menu-item/zero-one-many — 라벨은 0이면 `알림함`, 1~99면 `알림함 N`, 100 이상이면 `알림함 99+`다"
    - "UI S1-menu-item/long-text — 라벨 최대 길이는 `알림함 99+`로 고정이라 메뉴 폭 안에서 한 줄이다"
    - "UI S1-inbox-list/empty — 알림 0건이면 `알림이 없습니다` 한 줄만 보이고 다음 한 수 버튼과 표 머리글이 없다(Copywriting S1 Empty)"
    - "UI S1-inbox-list/loading — 300ms를 넘는 로딩에서만 `--surface` 뼈대 3행이 보이고 스피너·반짝임이 없다"
    - "UI S1-inbox-list/error — 조회 실패 시 목록 자리에 Copywriting S1 Error 한 줄과 2차 `다시 시도`가 보인다. 읽음 처리 실패는 화면 오류 없이 배지만 남는다"
    - "UI S1-inbox-list/populated — 행은 날짜 그룹 머리글(올해 `MM-DD`) 아래 최신순이고, 연 시점에 안 읽음이던 행만 첫 칸에 `inset 2px 0 0 var(--line-strong)`과 스크린 리더 전용 `안 읽음 · `가 있다"
    - "UI S1-inbox-list/partial — 이메일 발송이 실패한 행에만 내용 칸 두 번째 줄 `이메일 발송 실패`(`--fs-sm --muted`)가 있고 미설정·성공 행에는 없다"
    - "UI S1-inbox-list/overflow — 51건 이상이면 처음 50건 아래 3차 `더 보기 50건`이 있고 페이지 번호·가로 스크롤이 없다"
    - "UI S1-inbox-list/zero-one-many — 0건 → EMPTY 한 줄, 1건 → 그룹 머리글 1 + 행 1, 여러 날 → 날짜별 그룹 머리글이 각각 하나"
    - statement: "UI S1-inbox-list/long-text — 40자 이상 알림 내용이 `keep-all`로 줄바꿈되고 말줄임되지 않으며 375px에서 가로 스크롤이 없다"
      verification: backstop
  artifacts:
    - path: "app/(app)/notifications/page.tsx"
      provides: "알림함 화면(세션 가드 · 50건 · 다섯 상태)"
    - path: "app/(app)/notifications/inbox-list.tsx"
      provides: "날짜 그룹 표 · 연 시점 기준 인셋 · 열면 읽음 · 더 보기"
    - path: "app/(app)/notifications/actions.ts"
      provides: "본인 범위 액션 둘(읽음 처리 · 더 보기)"
      exports: ["markInboxReadAction", "loadMoreInboxAction"]
    - path: "domain/notify/inbox.ts"
      provides: "본인 알림 목록 · 안 읽은 수 · 연 시점까지 읽음 처리"
      exports: ["listMyNotifications", "countMyUnread", "markMyNotificationsRead", "INBOX_RETENTION_DAYS"]
    - path: "ui/shell/role-menu.ts"
      provides: "accountGroup 맨 앞 `알림함` · 건수 표기 함수"
      exports: ["NOTIFICATIONS_HREF", "unreadCountLabel"]
  key_links:
    - from: "app/(app)/layout.tsx"
      to: "domain/notify/inbox.ts"
      via: "`countMyUnread(viewer)`(실패는 로그 + null) → `Shell unreadCount`"
      pattern: "countMyUnread"
    - from: "ui/shell/TopBar.tsx"
      to: "ui/shell/role-menu.ts"
      via: "`unreadCountLabel(n)`으로 배지·메뉴 항목 글자 · `NOTIFICATIONS_HREF`로 알림함 항목을 가른다"
      pattern: "unreadCountLabel"
    - from: "app/(app)/notifications/inbox-list.tsx"
      to: "app/(app)/notifications/actions.ts"
      via: "처음 마운트 때 `markInboxReadAction({ openedAt })` → 성공하면 `router.refresh()`(배지 갱신)"
      pattern: "markInboxReadAction"
    - from: "test/unit/ui/role-menu.test.ts"
      to: "docs/design/SYSTEM.md"
      via: "「계정」 그룹 문장 목록 = accountGroup 라벨 집합(`알림함` 포함)"
      pattern: "알림함"
---

<objective>
**As a** 직원, **I want to** 상단 바의 내 이름 옆 숫자로 안 읽은 알림을 보고 알림함을 열어 건별로 확인하기를, **so that** 하루 한 번 생기는 알림을 메뉴를 뒤지지 않고 알아채고, 연 뒤에는 숫자가 사라진다.

(스토리는 ROADMAP 04.2 Goal 줄 「직원은 앱 알림함(미읽음 배지)…으로 알림을 받고」와 기준 2에서 끌어왔다.)

Purpose: NOTI-01 — 앱 안 알림함과 미읽음 배지, 건별 행, 읽음 처리. SYSTEM.md §7-12 계약 + UI-SPEC S1이 채운 빈칸. CONTEXT Claude's Discretion의 「읽음 처리 방식·보관 기간」(D-4206).

Output: 셸 배지·메뉴 항목 · 알림함 화면(다섯 상태) · 본인 범위 액션 둘 · DECISIONS 한 항목 + SYSTEM.md 수정 제안 #2~#7 · 통합·E2E(PC·폰).

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 화면은 `docs/design/SYSTEM.md`와 `docs/design/tokens.css`의 토큰만 쓴다(새 색·서체·radius 금지 — 배지는 §7-5 상태 태그 모양). 화면 검증 순서는 CLAUDE.md 「싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`) → 수정 → 전체 게이트 한 번」. UI 완료 판정은 페이즈 끝 `/design-review` → `/qa`. 새 의존성 0개. `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-01-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — S1(145~217행) · Copywriting S1 행 · 「SYSTEM.md 수정 제안」(536~547행) · 가정 U-7·U-8·U-9(Grep `U-7`)
# docs/design/SYSTEM.md — §6-0 (a)(Grep `### 6-0`) · §7-12(874행~, Grep `### 7-12`) · §4-3 인셋 허용 목록(Grep `인셋`) · §6-8 · §7-11 · §7-3 그룹 머리글 행
# docs/DESIGN.md §4 — 새 화면 절차
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 NOTI-01 adjacency(#8)·empty(#9)·encoding(#10, backstop)·idempotency(#12)·concurrency(#13)이고 ordering(#11)은 04.2-01이 저장소 조회로 증명했다(이 화면은 그 순서를 그대로 쓴다). UI-SPEC UI Considerations의 S1-badge·S1-menu-item·S1-inbox-list 24줄(backstop 넷)을 떠맡는다.

**계획 단계 결정(표면화):**

- **D-4208 알림함은 권한표 메뉴가 아니다:** PATTERNS는 `notifications` 액션 레지스트리 등록을 제안했지만, 레지스트리 항목은 `MENUS` 키를 요구하고(누수 스캔 단언) `MENUS` 항목은 권한표에서 계급별로 끌 수 있게 된다 — 그러면 `accountGroup`이 계급에 따라 갈라져 `role-menu.test.ts`의 「accountGroup은 계급과 무관하다」와 UI-SPEC S1-c 「로그인한 모든 사용자」가 깨진다. 그래서 `/account`(내 정보) 선례를 따른다: 두 액션은 `authedActionClient`를 지나고 `ctx.viewer.id`만 다루며 레지스트리·`MENUS`·DTO 등록을 하지 않는다. 누수 방지는 도메인 함수가 받는 사람 인자를 아예 받지 않는 것으로 한다(04.2-01 T-4.2-05). Phase 7이 알림 내용에 민감 정보를 넣게 되면 그 조건 종류가 문장을 만들 때 노출표를 지난다(Phase 7 몫).
- **D-4206 읽음·보관:** 열면 모두 읽음(UI-SPEC U-8). 읽음 처리는 화면이 서버 렌더 시각 `openedAt`을 넘겨 `created_at <= openedAt AND read_at IS NULL`만 고친다(엣지 #13). `read_at`에는 앱 시각을 넣는다(연 시점 비교가 같은 시계로 되게). 인셋 규칙: `readAt === null || readAt >= openedAt`인 행 — `router.refresh()` 뒤 다시 받은 행과 `더 보기`로 받은 행에도 같은 규칙이 맞는다. `openedAt`은 클라이언트 상태에 한 번만 담아 새로 고침된 props가 바꾸지 못하게 한다. 보관 창 90일(04.2-01 상수).
- **SYSTEM.md 반영 범위:** 코디네이터 메모 4는 일곱 수정 제안을 한 문서 작업으로 넣으라고 했다. #1(§6-10 표 · 「관리자 화면 10개 → 11개」)만 04.2-07로 뗀다 — UI-SPEC #1 스스로 「`ADMIN_MENUS`와 같은 커밋」을 요구하고, `role-menu.test.ts`가 §6-10 표와 `ADMIN_MENUS`를 원소 단위로 대조하며 `/admin/holidays` 페이지 파일의 존재까지 확인하기 때문이다. 나머지 #2~#7과 DECISIONS 항목은 여기 Task 2 한 작업이다. 모두 **덧붙이거나 표기만 고친다**(Phase 4·04.1·04.3도 SYSTEM.md를 고친다).
- **E2E의 tick:** E2E 준비는 앱과 같은 DB에 in-process로 `runTick({ conditionKinds: [테스트 종류], isBusinessDay: async () => true })`를 부른다(요일과 무관하게). Playwright 워커가 병렬이라 다른 파일의 tick과 advisory lock이 겹칠 수 있다 — 준비 도우미는 `status: "locked"`면 짧게 다시 부른다(최대 20회). 후보의 `entityId`는 테스트마다 `randomUUID()`로 만든다(중복 키 충돌 없음).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — tick 한 번 → 상단 바 배지 1 → /notifications 열람(읽음 처리) → 같은 화면 배지 0 (E2E로 끝까지)</name>
  <files>app/(app)/layout.tsx, ui/shell/Shell.tsx, ui/shell/TopBar.tsx, ui/shell/TopBar.module.css, app/(app)/notifications/page.tsx, app/(app)/notifications/inbox-list.tsx, app/(app)/notifications/actions.ts, domain/notify/inbox.ts, repositories/notifications.ts, test/integration/notify-inbox.test.ts, test/e2e/notify-inbox.spec.ts</files>
  <read_first>
    - `app/(app)/layout.tsx` — 세션 · `can()`으로 `allowedMenus` · `Shell` props
    - `ui/shell/Shell.tsx` · `ui/shell/TopBar.tsx`(24~160행 — 트리거 버튼 · `userName` 렌더 · PC 메뉴 항목) · `ui/shell/TopBar.module.css`
    - `ui/status-tag/StatusTag.tsx`·`.module.css` — §7-5 태그 모양(배지가 따를 글자·테두리 규칙)
    - `app/globals.css` 60행 `.sr-only`
    - `app/(app)/account/actions.ts` — 레지스트리 없는 본인 범위 액션 선례(D-4208)
    - `lib/viewer.ts` — `requireSession()` · `test/unit/page-auth-guard.test.ts` — 모든 `(app)` 페이지의 세션 가드 규칙
    - `domain/notify/inbox.ts`·`repositories/notifications.ts`(04.2-01 결과)
    - `test/e2e/fixtures.ts` — `createFixtureUser` · `test/e2e/user-menu.spec.ts` — 로그인·트리거 찾기
    - `test/support/notify-test-condition.ts`(04.2-01)
    - `docs/design/SYSTEM.md` §7-12 · UI-SPEC S1-a·S1-d
  </read_first>
  <behavior>
    - 통합: A에게 알림 3건 → `countMyUnread(A)` 3 · `markMyNotificationsRead(A, { openedAt })` → 3건 읽음 · 다시 부르면 0건이고 `read_at` 그대로(엣지 #12) · openedAt 뒤에 넣은 1건은 안 읽음으로 남는다(엣지 #13) · B의 행은 A의 호출로 바뀌지 않는다
    - E2E(PC): 새 사용자 + tick 1건 → 로그인 → 트리거 접근 가능 이름에 `안 읽은 알림 1건` · 보이는 배지 `1` → `/notifications`로 이동 → 행 `테스트 알림 · {id}`에 스크린 리더 `안 읽음 · ` · 읽음 처리 뒤 같은 화면에서 트리거에 배지 요소가 없다 · 새로 고침하면 인셋 표시가 사라진다
  </behavior>
  <action>
① `test/integration/notify-inbox.test.ts`(신규)와 `test/e2e/notify-inbox.spec.ts`(신규)에 behavior를 쓴다(RED). E2E 준비 도우미는 스펙 파일 안에 두고 `<probe_fallback>` 「E2E의 tick」대로 잠금 경합이면 다시 부른다.

② 도메인·저장소: `repositories/notifications.ts`에 `markRead(viewer, { recipientId, openedAt, now })` — `recipient_id = recipientId AND created_at <= openedAt AND read_at IS NULL`을 `read_at = now`로, 바뀐 행 수 반환. `domain/notify/inbox.ts`에 `markMyNotificationsRead(viewer, { openedAt }, deps?)`(받는 사람 = `viewer.id`, `now` = 앱 시각).

③ 액션 `app/(app)/notifications/actions.ts`(신규, `"use server"`): `markInboxReadAction` — `authedActionClient` + zod `{ openedAt: z.string().datetime() }` → `markMyNotificationsRead(ctx.viewer, …)`. 레지스트리 import를 두지 않는다(D-4208 — 파일 머리 주석 두 줄로 이유를 적는다).

④ 화면 `app/(app)/notifications/page.tsx`(신규): `dynamic = "force-dynamic"` · `requireSession()` · `PageHeader` `알림함`(부제 없음) · `openedAt = new Date().toISOString()` · `listMyNotifications(viewer, { limit: 50 })` → `InboxList`에 행과 `openedAt`. `inbox-list.tsx`(신규, 클라이언트): 읽기용 `<table>` + `<caption className="sr-only">알림함</caption>` · 머리글 `내용`·`시각` · `openedAt`을 `useState`에 한 번 담는다 · 인셋 규칙은 `<probe_fallback>` D-4206 · 인셋 행 첫 칸에 `<span className="sr-only">안 읽음 · </span>` · 마운트 때 한 번 `markInboxReadAction` → 성공하면 `router.refresh()`, 실패하면 화면에 아무것도 띄우지 않는다(서버 로그는 액션 클라이언트가 남긴다). 이 Task는 날짜 그룹·더 보기·다섯 상태 없이 행만 그린다(Task 3).

⑤ 셸: `app/(app)/layout.tsx`에서 `countMyUnread(viewer)`를 try로 부르고 실패하면 `log.warn("notify.unread_count_failed", { message })` + `null` → `Shell`의 새 prop `unreadCount: number | null` → `TopBar`. `TopBar` 트리거 안 이름 뒤에 `unreadCount`가 1 이상일 때만 `<span aria-hidden="true" className={styles.badge}>{글자}</span>`와 `<span className="sr-only"> · 안 읽은 알림 {n}건</span>`(글자는 1~99 그대로, 100 이상 `99+` — Task 2가 공용 함수로 옮긴다). `TopBar.module.css`의 `.badge`: `font-size: var(--fs-xs)` · `font-weight: 600` · `border: 1px solid currentColor` · `padding: 1px 6px` · `border-radius: 0` · `color: var(--bar-fg)` · `font-variant-numeric: tabular-nums` · `white-space: nowrap` · `flex-shrink: 0` · 이름과 간격 `var(--s-2)` · 이름 쪽은 말줄임(`min-width: 0; overflow: hidden; text-overflow: ellipsis`). tokens.css에 없는 값·색을 쓰지 않는다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-inbox.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 읽음 재처리가 read_at을 바꿨거나, openedAt 뒤의 새 알림까지 읽음이 됐거나, 다른 사람의 행이 바뀌었다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint(경계·stylelint 토큰 규칙)·tsc·build 중 하나가 0이 아닌 코드로 끝난다 — 토큰 밖 값을 썼거나 액션이 `authedActionClient`를 지나지 않았거나 페이지 세션 가드가 없다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/user-menu.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 배지가 1로 보이지 않았거나, 열람 뒤 같은 화면에서 배지가 남았거나, 기존 사용자 메뉴 E2E가 트리거 이름 변경으로 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run --project unit test/unit/page-auth-guard.test.ts` 초록(새 페이지에 세션 가드)
    - `grep -n "registerAction\|actions.registry" "app/(app)/notifications/actions.ts"` 출력이 비어 있다(D-4208)
    - E2E: 배지 `1` → 열람 → 배지 요소 0개(`CI=true`)
    - `TopBar.module.css`의 새 규칙이 `var(--…)` 토큰과 `0`·`1px` 테두리만 쓴다(stylelint 초록)
  </acceptance_criteria>
  <done>tick이 만든 알림이 배지로 보이고, 알림함을 열면 읽음이 되어 같은 화면에서 배지가 사라진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 메뉴 항목 「알림함 N」(PC 사용자 메뉴 · 폰 더보기 계정 그룹) + DECISIONS 한 항목 + SYSTEM.md 수정 제안 #2~#7</name>
  <files>ui/shell/role-menu.ts, ui/shell/TopBar.tsx, ui/shell/BottomTabs.tsx, ui/shell/MoreSheet.tsx, ui/shell/Shell.tsx, docs/design/DECISIONS.md, docs/design/SYSTEM.md, test/unit/ui/role-menu.test.ts</files>
  <read_first>
    - `ui/shell/role-menu.ts` 12~60행(타입) · 153~160행 `buildAccountGroup` · 210행 `roleMenu`
    - `test/unit/ui/role-menu.test.ts` 30~160행 · 300~340행 — SYSTEM.md 「계정」 그룹 문장 대조 · 「accountGroup은 계급과 무관」 · `ADMIN_MENU_KEYS`(이 Task는 고치지 않는다)
    - `ui/shell/MoreSheet.tsx` · `ui/shell/BottomTabs.tsx` — 계정 그룹 렌더 · 「내 차례 N」 라벨 붙이는 방식
    - `docs/design/DECISIONS.md` 머리글 형식(`## YYYY-MM-DD — 제목`) · `test/unit/design-system-docs.test.ts`(DECISIONS 머리글 개수 단언 · SYSTEM.md 절 존재 단언)
    - `docs/design/SYSTEM.md` §6-0 (a) · §7-8 「계정」 그룹 문장(Grep `「계정」`) · §7-12 · §4-3 · §6-8 · §7-11 · §6 레이아웃 목록
    - UI-SPEC 「SYSTEM.md 수정 제안」 #2~#7 · 가정 U-1~U-10
  </read_first>
  <behavior>
    - 단위: `roleMenu(ADMIN).accountGroup` 라벨 = `알림함` · `내 정보` · `설정` · `로그아웃` 순서이고 SYSTEM.md 「계정」 그룹 문장 목록과 같다 · 직원 계급도 같다
    - 단위: `unreadCountLabel(0)` null · `(1)` `"1"` · `(99)` `"99"` · `(100)` `"99+"`
    - E2E(PC): 알림 1건 있는 직원 → 사용자 메뉴 첫 항목 `알림함 1`(관리 권한 없음) · 관리자 → `관리` 다음 `알림함 1`
    - E2E(폰): 더보기 시트 계정 그룹 `알림함 1` · `내 정보` · `설정` · `로그아웃`
  </behavior>
  <action>
① 단위 케이스를 `role-menu.test.ts`에 더하고(RED) E2E 두 줄을 `notify-inbox.spec.ts`·`mobile-notify-inbox.spec.ts`(신규 — 폰 스펙 파일 이름 규칙 `mobile-*.spec.ts`)에 더한다.

② **DECISIONS.md를 먼저**: 끝에 `## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약` 항목 하나 — UI-SPEC U-1~U-10 요약(각 한 줄: 고른 것 · 이유)과 「이탈이 아니라 SYSTEM.md가 비워 둔 자리를 채움」 한 줄, #1은 04.2-07이 `ADMIN_MENUS`와 함께 반영한다는 한 줄. 같은 날짜 머리글을 세는 단언이 있으면 이 항목이 그것을 깨지 않는지 확인하고, 깨지면 단언을 고치지 말고 SUMMARY에 적은 뒤 멈춰 묻는다(다른 페이즈의 기록 개수 단언일 수 있다).

③ **SYSTEM.md(덧붙이거나 표기만):** #2 §7-12 제목의 「구현은 Phase 7」 → 「구현은 Phase 04.2」 + S1-a(배지 자리·모양·`99+`·스크린 리더 이름) · S1-b(메뉴 순서) · S1-c(인셋 `--line-strong` · 이메일 실패 보조 줄 · 50건 더 보기) · S1-d(열면 모두 읽음) 계약 문장 · #3 §4-3 인셋 허용 목록에 `inset 2px 0 0 --line-strong`(알림함 안 읽은 행) 한 줄 · #4 §6-8 세 항목 → 다섯 항목(`알림 발송`·`이메일`) + 배너가 겹치면 DB 커넥션이 먼저 · #5 §7-11 예시에 B1·B2 + 우선순위 「경고 > 안내, 같은 등급이면 그 화면의 조건이 먼저」 + 「관리」 인덱스는 여러 관리자 조건을 받는 자리 · #6 §6-0 (a) 사용자 메뉴 `관리 · 알림함 N · 내 정보 · 로그아웃` + 트리거 배지, 그리고 §7-8 「계정」 그룹 문장 목록 맨 앞에 `알림함` · #7 §6 레이아웃 목록에 「공휴일 화면은 새 템플릿이 아니다 — §6-1 + §7-15 조립」 한 줄. 기존 문장을 다시 쓰지 않는다.

④ **코드:** `role-menu.ts` — `export const NOTIFICATIONS_HREF = "/notifications"` · `buildAccountGroup` 맨 앞에 `{ kind: "link", label: "알림함", href: NOTIFICATIONS_HREF }` · `export function unreadCountLabel(count: number | null): string | null`(0·null → null, 1~99 그대로, 100 이상 `99+`). 라벨 문자열은 `알림함` 그대로 두고 건수는 표면이 붙인다(라벨 대조 테스트가 계급·건수와 무관하게 맞게). `TopBar`: 배지 글자를 `unreadCountLabel`로 바꾸고, PC 메뉴 항목 중 `href === NOTIFICATIONS_HREF`인 항목 라벨 뒤에 공백 하나 + 건수(있을 때). `Shell` → `BottomTabs` → `MoreSheet`에 `unreadCount`를 넘겨 계정 그룹의 같은 항목에 같은 방식으로 붙인다(하단 탭 자체에는 배지를 달지 않는다 — UI-SPEC S1-a).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts test/unit/design-system-docs.test.ts test/unit/ui/system-md-compliance.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — accountGroup과 SYSTEM.md 「계정」 문장이 어긋났거나(같은 커밋 규칙 위반), accountGroup이 계급에 따라 갈라졌거나, 건수 표기 경계(0·99·100)가 틀렸거나, SYSTEM.md 절 머리글을 깨뜨렸거나, DECISIONS 머리글 단언이 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/mobile-notify-inbox.spec.ts test/e2e/user-menu.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 메뉴 순서가 UI-SPEC S1-b와 다르거나, 폰 시트에 건수가 없거나, 기존 셸·키보드 E2E가 새 항목으로 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "알림함" docs/design/SYSTEM.md`가 반영 전보다 늘었고 `grep -n "구현은 Phase 7" docs/design/SYSTEM.md`에 §7-12 제목 줄이 없다
    - `grep -c "^## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약" docs/design/DECISIONS.md`가 1이다
    - `git diff --numstat HEAD -- docs/design/SYSTEM.md`의 삭제 줄 수가 추가 줄 수보다 훨씬 적다(표기 수정 몇 줄만 — 검토로 확인하고 SUMMARY에 두 수를 적는다)
    - 단위·E2E 초록(`CI=true`)
  </acceptance_criteria>
  <done>PC·폰 두 메뉴가 `알림함 N`을 UI-SPEC 순서로 보이고, SYSTEM.md·DECISIONS가 그 계약을 먼저 기록한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 알림함 목록 완성 — 날짜 그룹 머리글 · 이메일 발송 실패 보조 줄 · 50건 + 더 보기 · 로딩 뼈대 · 빈/오류 상태 · 긴 글 · 폰 375</name>
  <files>app/(app)/notifications/inbox-list.tsx, app/(app)/notifications/page.tsx, app/(app)/notifications/actions.ts, app/(app)/notifications/loading.tsx, app/(app)/notifications/notifications.module.css, domain/notify/inbox.ts, test/integration/notify-inbox.test.ts, test/e2e/notify-inbox.spec.ts, test/e2e/mobile-notify-inbox.spec.ts</files>
  <read_first>
    - `app/(app)/projects/loading.tsx` — 뼈대 규약(제목만 · `aria-hidden` · 부제 없음 이유)
    - `ui/list-empty/ListEmpty.tsx` — `tone="error"` · `action` 생략(DECISIONS 2026-09-20)
    - `docs/design/SYSTEM.md` §7-3 그룹 머리글 행 · §2-4 날짜 표기 · §7-12 다섯 상태
    - `app/(app)/notifications/*`(Task 1·2 결과)
  </read_first>
  <behavior>
    - 통합: 55건 → `listMyNotifications(A, { limit: 50, offset: 0 })` 50건 · `offset: 50` 5건 · 91일 전 행은 목록·안 읽은 수 둘 다에서 빠진다 · 같은 `created_at` 두 행은 두 줄(id 내림차순)
    - E2E: 알림 0건 → `알림이 없습니다` 한 줄 · 표 머리글·버튼 없음
    - E2E: 서로 다른 KST 날짜 두 날에 걸친 알림 → 날짜 그룹 머리글 둘(올해면 `MM-DD`) · 행 시각 `HH:MM`
    - E2E: `email_status = 'failed'` 행에만 `이메일 발송 실패` 보조 줄 · `pending`/`sent`/`skipped_no_smtp` 행에는 없다
    - E2E: 51건 → 행 50개 + 3차 `더 보기 50건` → 누르면 51번째 행이 보이고, 그 행도 연 시점 기준으로 인셋이 있다
    - E2E: 내용에 `<b>굵게</b> & "따옴표"`가 든 알림 → 화면에 글자 그대로 보이고 `b` 요소가 생기지 않는다
    - E2E(폰 375): 40자 이상 내용이 줄바꿈되고 문서 가로 스크롤 폭이 뷰포트를 넘지 않는다(`scrollWidth <= clientWidth`)
  </behavior>
  <action>
① behavior를 통합·E2E에 먼저 쓴다(RED). E2E에서 이메일 상태·생성 시각을 다르게 만들려면 tick 뒤 테스트 DB에서 해당 행만 고치는 준비 도우미를 스펙 안에 둔다(프로덕션 코드에 테스트용 경로를 만들지 않는다).

② `loadMoreInboxAction`(`actions.ts`): `authedActionClient` + zod `{ offset: z.number().int().min(0) }` → `listMyNotifications(ctx.viewer, { limit: 50, offset })`. `inbox-list.tsx`: 행을 KST 날짜로 묶어 그룹 머리글 행(`<tr>` 안 `<th scope="colgroup" colSpan={2}>`, 올해 `MM-DD` · 다른 해 `YYYY-MM-DD`) · 시각 칸 `HH:MM`(KST, `tabular-nums`, nowrap, 오른쪽 정렬) · 내용 칸 줄바꿈 허용(`word-break: keep-all; overflow-wrap: anywhere`) · `emailStatus === "failed"`면 내용 칸 둘째 줄 `이메일 발송 실패`(`--fs-sm`·`--muted`) · 목록이 50의 배수로 찼을 때만 3차 `더 보기 50건`. 내용은 React 텍스트 노드로만 넣는다(HTML 주입 경로 없음 — 엣지 #10 backstop).

③ 상태: 0건 → `ListEmpty message="알림이 없습니다"`(action 생략, 표 없음) · 조회 실패 → 페이지가 잡아 `ListEmpty tone="error" message="불러오지 못했습니다 · 다시 시도"` + 2차 `다시 시도`(새로 고침) · `loading.tsx`(신규) — `PageHeader` `알림함` + `--surface` 뼈대 3행(`aria-hidden`), 스피너·반짝임 없음(projects 선례).

④ `notifications.module.css`(신규): 인셋 `box-shadow: inset 2px 0 0 var(--line-strong)` · 그룹 머리글 `--fs-sm --muted` 600 · 위 12px(토큰 간격) · 아래 1px `--line-strong` — 모두 tokens.css 토큰만.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-inbox.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 페이지 경계(50)·보관 창(90일)·같은 시각 두 행 규칙이 틀렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>stylelint(토큰 밖 값)·eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/mobile-notify-inbox.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 빈·오류 문구가 카피 계약과 다르거나, 그룹 머리글·보조 줄·더 보기가 빠졌거나, HTML 글자가 요소로 해석됐거나, 375px에서 가로 스크롤이 생겼거나, axe 위반이 생겼다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "dangerouslySetInnerHTML" "app/(app)/notifications"` 출력이 비어 있다
    - 카피 네 문구(`알림이 없습니다` · `불러오지 못했습니다 · 다시 시도` · `이메일 발송 실패` · `더 보기 50건`)가 화면 코드에 글자 그대로 한 번씩 있다(Grep)
    - 통합·E2E(`CI=true`) 초록 · 독립 DOM 감사(별도 에이전트, `CI=true`)가 S1 24줄을 판정해 SUMMARY에 결과를 붙인다(backstop 넷은 감사 판정 또는 human_needed)
  </acceptance_criteria>
  <done>알림함이 §7-12·UI-SPEC S1의 다섯 상태와 날짜 그룹·실패 보조 줄·더 보기를 갖추고 폰에서도 가로 스크롤 없이 읽힌다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 본인 범위 액션 둘 | 클라이언트가 보낸 `openedAt`·`offset`이 서버 쿼리 조건이 된다 |
| 알림 내용(조건 종류가 만든 문장) → 화면 | Phase 7 조건 종류가 문서 이름 등 사용자 입력을 문장에 넣을 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-50 | Information Disclosure | 알림함 목록·배지 | high | mitigate | 도메인 함수가 받는 사람 인자를 받지 않고 `viewer.id`만 쓴다 · 통합에서 B의 행 불변 · 페이지 세션 가드(page-auth-guard 단위) |
| T-4.2-51 | Tampering | `markInboxReadAction` | medium | mitigate | zod로 `openedAt` 형식 검사 · 조건이 `recipient_id = viewer.id`라 미래 시각을 보내도 자기 행만 읽음이 된다(피해는 자기 배지뿐) |
| T-4.2-52 | Tampering | 알림 내용 XSS | medium | mitigate | 텍스트 노드로만 렌더 · `dangerouslySetInnerHTML` 금지(Grep) · E2E가 HTML 글자를 그대로 보는지 단언 |
| T-4.2-53 | Denial of Service | `loadMoreInboxAction` | low | mitigate | `limit` 50 고정(클라이언트가 못 바꾼다) · `offset` 정수 ≥ 0 · 보관 창 90일 |
| T-4.2-54 | Elevation of Privilege | 레지스트리 밖 액션 | low | accept | `/account` 선례와 같은 본인 범위 — `authedActionClient`(세션 필수) 경유, 누수 스캔 대상이 아닌 이유를 파일 머리 주석과 D-4208에 남긴다 |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`
- 단위: `pnpm vitest run --project unit test/unit/ui test/unit/design-system-docs.test.ts test/unit/page-auth-guard.test.ts`
- 통합: `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/notify-inbox.test.ts`
- 독립 DOM 감사(별도 에이전트, `CI=true`) → 수정 → E2E 한 번: `CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/mobile-notify-inbox.spec.ts test/e2e/user-menu.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/a11y.spec.ts`
- 페이즈 끝 UI 판정은 `/design-review` → `/qa`(이 플랜이 대신하지 않는다)

</verification>

<success_criteria>

- tick → 배지 1 → 열람 → 배지 0이 E2E로 CI에 있다(ROADMAP 04.2 기준 2)
- 알림함이 §7-12 + UI-SPEC S1 계약(배지·메뉴 순서·인셋·날짜 그룹·실패 보조 줄·더 보기·다섯 상태)을 지키고 다른 사람의 알림을 보이지 않는다
- SYSTEM.md 수정 제안 #2~#7과 DECISIONS 항목이 반영됐다(#1은 04.2-07)

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 라우트·화면 | `/notifications`(`app/(app)/notifications/page.tsx` · `loading.tsx` · `inbox-list.tsx` · `notifications.module.css`) |
| Server Action(본인 범위, 레지스트리 없음) | `markInboxReadAction`({ openedAt }) · `loadMoreInboxAction`({ offset }) |
| 함수 | `markMyNotificationsRead`(도메인) · `markRead`(저장소) · `listMyNotifications`에 `offset` · `unreadCountLabel` · 상수 `NOTIFICATIONS_HREF` |
| 셸 props | `Shell`·`TopBar`·`BottomTabs`·`MoreSheet`의 `unreadCount: number \| null` · 트리거 배지 `.badge` · accountGroup 맨 앞 `알림함` |
| 로그 이벤트 | `notify.unread_count_failed`(warn) |
| 문서 | `docs/design/DECISIONS.md` `## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약` · `docs/design/SYSTEM.md` 수정 제안 #2~#7 |
| 테스트 | `test/integration/notify-inbox.test.ts` · `test/e2e/notify-inbox.spec.ts` · `test/e2e/mobile-notify-inbox.spec.ts` · `test/unit/ui/role-menu.test.ts`(확장) |
| 결정 | D-4206(읽음·보관) · D-4208(알림함은 권한표 메뉴가 아니다) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-06-SUMMARY.md` when done
</output>
