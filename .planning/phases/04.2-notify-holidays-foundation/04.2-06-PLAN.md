---
phase: 04.2-notify-holidays-foundation
plan: 06
type: execute
wave: 2
depends_on: [04.2-01, 04.2-02, 04.2-04]
files_modified:
  - db/schema/holidays.ts
  - db/schema/index.ts
  - db/client.ts
  - db/migrations/*_holidays.sql
  - db/migrations/meta/*_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/holidays.ts
  - domain/holidays/candidates.ts
  - domain/holidays/calendar.ts
  - domain/notify/tick.ts
  - test/integration/holidays.test.ts
  - test/integration/notify-tick.test.ts
autonomous: true
requirements: [ADMN-11, NOTI-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "공휴일 표(`holidays`)는 날짜마다 한 행(유니크 `holidays_date_key`)이고 구분은 `statutory`·`substitute`·`temporary`·`election` 넷 중 하나다. 대체공휴일 행만 `origin_year`(그 대체일을 낳은 공휴일의 해)를 갖는다(CHECK). 연도 확정은 별도 표 `holiday_year_confirmations`(연도 PK)에, 그 해 후보 생성 완료 표시는 `holiday_year_generations`(연도 PK)에 한 행이다"
    - "영업일 판정·N영업일 계산의 DB 쪽 진입점은 `domain/holidays/calendar.ts` 하나이고, 그 함수가 필요한 해의 후보를 먼저 보장한 뒤 표를 읽어 04.2-02의 순수 함수에 넘긴다(한 표 원칙 — CONTEXT 이미 확정된 입력)"
    - "tick의 기본 영업일 판정이 이 함수로 바뀐다 — 04.2-01의 월~금 판정 자리를 바꿔 끼우고 비공개 KST 도우미를 지운다"
    - "tick이 올해 후보를 보장하고 다음 해 후보 생성도 시도한다 — 다음 해가 실패해도(예: 음력 표 밖) tick은 계속되고 경고 로그 `holiday.candidates_failed { year }` 한 줄을 남긴다(D-4205)"
    - "두 연결이 같은 해 후보를 동시에 만들어도 행이 중복되지 않는다(유니크 날짜 + ON CONFLICT DO NOTHING)"
    - "후보 생성·수동 추가·삭제·미래 대체일 재계산·연도 확정은 모두 같은 트랜잭션 advisory 잠금(`lockHolidayCalendar` — `pg_advisory_xact_lock(HOLIDAYS_LOCK_KEY)`, 달력 전체 하나)을 잡은 트랜잭션 안에서만 표를 바꾼다 — 두 재계산이 READ COMMITTED에서 서로의 새 대체일을 못 보고 대체일을 둘 남기는 일이 없다(Codex 2차 #4)"
    - "그 해 후보 생성이 끝났다는 판단은 `holiday_year_generations`의 그 해 행 하나로 한다 — 행이 없으면 법정 행이 일부 있어도 규칙이 기대하는 행 전부를 DO NOTHING으로 채우고 같은 잠금 트랜잭션에서 표시 행을 넣는다(법정 행 하나 = 생성 완료로 보지 않는다 — Codex 2차 #14 잔여)"
    - "대체일 검색이 연말을 넘으면 다음 해 1월의 대체일이 되고 그 행의 `origin_year`는 앞 해다. 막는 날은 그 해 1월 1일부터 다음 해 12월 31일까지의 수동 날짜다. 수동 날짜가 바뀐 해 Y의 재계산은 `origin_year` Y-1과 Y 둘을 다시 계산한다(Codex 2차 #5)"
    - "후보 생성은 그 해의 수동 날짜(DB의 `temporary`·`election` 행 + `INITIAL_MANUAL_HOLIDAYS`)를 대체일을 **막는 날**로 넘긴다 — 수동 날짜가 대체일 자리를 차지하면 대체공휴일은 그다음 첫 비공휴일로 간다(D-4210 · Codex #2)"
    - "`INITIAL_MANUAL_HOLIDAYS`(체크포인트로 확인한 지난 선거일·임시공휴일)는 그 해 후보를 처음 만들 때 규칙 후보와 같은 INSERT 한 문장으로 들어간다 — 이 시스템을 켜기 전의 공휴일도 영업일 계산에 빠지지 않는다(D-4221 · Codex #16)"
    - "수동 공휴일이 생기거나 지워진 해의 **미래** 대체공휴일(오늘 이후)은 같은 트랜잭션 안에서 다시 계산된다(`recomputeFutureSubstitutes` — 그 `origin_year`의 미래 대체 행만 지우고 다시 넣는다) — 지난 대체일은 소급해 바꾸지 않는다. 두 삽입 순서(수동 먼저 → 후보 생성 / 후보 먼저 → 수동 추가 + 재계산)가 같은 표를 만든다(Codex #2)"
    - "두 트랜잭션이 수동 2027-10-04·10-05(개천절 대체일 10-06)를 동시에 하나씩 지우면(장벽으로 두 번째가 첫 번째 커밋 전에 잠금을 요청함을 확인) 최종 2027 대체 행은 개천절 몫 10-04 하나다(Codex 2차 #4)"
    - "공유 트랜잭션 타입 `DbOrTx`(`db/client.ts`)가 `delete`·`execute`를 포함한다 — 04.2-03(`tx.execute`)·이 플랜(`tx.delete`·잠금)·04.2-11·12가 같은 타입을 쓴다(Codex 2차 #7)"
    - "N영업일 계산은 필요한 해만 그때그때 읽는다 — 계산이 실제로 닿지 않은 해를 미리 요청하지 않아 표 첫 해(2025)·마지막 해(2035) 안의 계산은 표 밖의 해 때문에 실패하지 않고, 계산이 실제로 표 밖의 해로 넘어갈 때만 `LunarTableRangeError`다. |n|의 가정 한도를 두지 않는다(Codex #15)"
    - "공휴일에 기한이 된 발생은 다음 영업일 tick에서 나간다(발생 계약 — 04.2-01 · Codex #3)"
    - "addBusinessDays(d, N)은 N=0이면 d가 영업일일 때 d 자신이고, 연말을 넘는 계산(12/31 + 1영업일)은 다음 해 표(확정 또는 후보)를 읽는다. 내장 음력 표 범위 밖 연도 요청은 조용히 틀린 값 대신 오류를 낸다"
    - "공휴일 표에 그 해 행이 없으면 계산 전에 규칙 후보를 생성해 채운 뒤 계산한다 — 빈 표를 '공휴일 없음'으로 계산하지 않는다"
    - "후보 생성을 같은 해에 두 번 돌려도 행 수가 같고, 이미 확정되었거나 수동 추가된 행을 덮어쓰지 않는다"
    - "tick의 영업일 판정은 KST 날짜 기준이며 tick 날이 공휴일·주말이면 {sent: 0}으로 끝난다(D-709)"
    - "통합 테스트의 연도별 기대 행은 04.2-02의 `test/unit/holidays/official-calendar.ts`(체크포인트로 확인한 공식 목록)와 `INITIAL_MANUAL_HOLIDAYS`에서만 온다 — 행 개수를 테스트·계획에 박아 두지 않는다(Codex #1)"
  artifacts:
    - path: "db/schema/holidays.ts"
      provides: "`holidays`(`origin_year` 포함) · `holiday_year_confirmations` · `holiday_year_generations` 표"
      exports: ["holidays", "holidayYearConfirmations", "holidayYearGenerations"]
    - path: "db/client.ts"
      provides: "`DbOrTx`에 `delete`·`execute`를 더한다(한 줄 — Codex 2차 #7)"
      exports: ["DbOrTx"]
    - path: "repositories/holidays.ts"
      provides: "후보·수동 행 삽입 · 해별 읽기 · 관리자 화면용 읽기·쓰기(04.2-11·12가 쓴다) — 쓰기 함수는 선택 `tx`"
      exports: ["HOLIDAYS_LOCK_KEY", "lockHolidayCalendar", "findYearGeneration", "insertYearGeneration", "insertHolidayRows", "findHolidayDates", "findManualDates", "listHolidaysForYear", "listHolidayYears", "findYearConfirmation", "insertYearConfirmation", "insertManualHoliday", "findHolidayByDate", "deleteHolidayById", "deleteFutureSubstitutes"]
    - path: "domain/holidays/candidates.ts"
      provides: "달력 잠금 트랜잭션 · 그 해 후보 지연 생성(멱등, 생성 표시, 막는 날 · 초기 수동 목록) · 미래 대체일 재계산(원래 해 기준)"
      exports: ["withHolidayCalendarLock", "ensureHolidayCandidates", "ensureHolidayCandidatesLocked", "recomputeFutureSubstitutes"]
    - path: "domain/holidays/calendar.ts"
      provides: "표를 읽는 영업일 판정·N영업일 계산(해를 필요할 때만 읽음)"
      exports: ["isBusinessDayKst", "addBusinessDaysKst", "loadHolidayLookup", "HolidayYearNotLoadedError"]
  key_links:
    - from: "domain/holidays/calendar.ts"
      to: "domain/holidays/candidates.ts"
      via: "읽기 전에 그 해 `ensureHolidayCandidates(year)` — 계산이 모르는 해에 닿으면 `HolidayYearNotLoadedError`를 받아 그 해만 보장·읽고 다시 계산"
      pattern: "HolidayYearNotLoadedError"
    - from: "domain/holidays/candidates.ts"
      to: "domain/holidays/rules.ts"
      via: "`generateHolidayRules(year, { blockers })` 결과와 `INITIAL_MANUAL_HOLIDAYS`를 `insertHolidayRows` 한 번으로 넣는다"
      pattern: "blockers"
    - from: "domain/notify/tick.ts"
      to: "domain/holidays/calendar.ts"
      via: "`TickDeps.isBusinessDay` 기본값 = `isBusinessDayKst`"
      pattern: "isBusinessDayKst"
---

<objective>
**As a** 경영관리와 모든 직원, **I want to** 공휴일 표가 비어 있어도 그 해 규칙 후보와 지난 임시공휴일이 먼저 채워지고 알림 tick과 영업일 계산이 그 표 하나를 읽기를, **so that** 추석·대체공휴일·선거일에 알림이 나가지 않고 Phase 7의 지급일·마감이 같은 달력을 쓴다.

(스토리는 ROADMAP 04.2 Goal 줄 「영업일 계산은 이 표 하나를 읽는다」와 기준 1·4(D-709)에서 끌어왔다.)

Purpose: ADMN-11의 표·지연 생성·수동 날짜와 대체일의 관계(Codex #2·#16), NOTI-04 precision(공휴일 tick → sent 0 · D-709)과 공휴일 기한 발생의 이어받기(Codex #3), 필요한 해만 읽는 N영업일 계산(Codex #15), CONTEXT 「계획이 정할 것」의 「공휴일 후보를 매년 만드는 계기」. 관리자 화면(04.2-11·12)이 쓸 저장소 함수도 여기서 만든다.

Output: 두 표와 생성 마이그레이션 · 저장소 · 후보 보장과 재계산 · 표를 읽는 영업일 함수 · tick 연결 · 통합 테스트.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개. 관리자 화면·확정·수동 추가·삭제의 도메인과 화면은 04.2-11·12다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-01-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-02-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-PATTERNS.md — 「db/schema/holidays.ts」 절(Grep)
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — lazy `ensureHolidayCandidates` · Pitfall 4(과거 날짜 소급)(Grep)
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 ADMN-11 boundary(#1 — DB 쪽)·empty(#3)·idempotency(#6)와 NOTI-04 precision(#23)이다. ADMN-11 concurrency(#7)의 후보 생성 몫은 여기서 증명하고, 확정 동시 클릭 몫과 그 줄의 원문 lift는 04.2-11이다.

**계획 단계 결정·가정(표면화):**

- **D-4205 후보를 만드는 계기(지연 생성):** 해마다 따로 돌리는 작업을 두지 않는다. (1) 영업일 판정·계산이 어떤 해를 처음 읽을 때(`calendar.ts`가 그 해를 보장) (2) tick이 올해를 보장하고 다음 해를 시도(실패는 경고만) (3) 관리자 공휴일 화면 진입(올해·다음 해 — 04.2-11). 배포 때 만들지 않는다(마이그레이션·시드가 날짜에 기대지 않게). 그래서 해가 바뀌어도 첫 tick이 새 해를 채운다.
- **보장의 기준과 한 문장 삽입(개정 — Codex 2차 #14 잔여):** 생성 완료의 기준은 `holiday_year_generations`의 그 해 행이다(첫 계획의 「`statutory` 행이 하나라도 있으면 생성된 것」은 버린다 — 법정 행이 일부만 있는 해를 완성된 해로 오인한다). 빠른 길: 잠금 없이 `findYearGeneration(year)`가 행을 돌려주면 건너뛴다. 없으면 `withHolidayCalendarLock` 안에서 `ensureHolidayCandidatesLocked(year, tx)`: 표시 행을 다시 읽고(이중 확인) 여전히 없으면 (가) 막는 날 = 그 해 1월 1일부터 다음 해 12월 31일까지의 DB 수동 날짜(`findManualDates`, 같은 tx) ∪ 같은 범위의 `INITIAL_MANUAL_HOLIDAYS` 날짜 (나) `generateHolidayRules(year, { blockers })` (다) 규칙 후보 전부(대체 행은 `origin_year = year`) + 그 해의 `INITIAL_MANUAL_HOLIDAYS` 행(`created_by` null)을 `insertHolidayRows` **한 번**으로 넣는다(`ON CONFLICT (date) DO NOTHING` — 이미 있는 행은 남고 빠진 행만 들어간다. 같은 날짜에 관리자 수동 행이 먼저 있으면 그 행이 남는다, D-4221) (라) `insertYearGeneration(year, tx)`. 표시 행이 생긴 해의 규칙 행은 다시 만들지 않는다 — 규칙이 틀렸던 해를 바로잡아야 하면 그때 마이그레이션으로 한다(가정 — SUMMARY에 남긴다).
- **D-4210 막는 날 재계산(Codex #2 · 개정 — Codex 2차 #5):** `recomputeFutureSubstitutes(originYear, { today }, tx)` — 부르는 쪽이 달력 잠금을 잡은 트랜잭션 안에서: 그 해의 표시 행이 없으면 아무것도 하지 않는다(아직 생성되지 않은 해 — 나중 생성이 그때의 막는 날을 읽는다). 있으면 (가) `origin_year = originYear`이고 `date > today`인 `substitute` 행을 지우고(`deleteFutureSubstitutes`) (나) 그 해 1월 1일~다음 해 12월 31일의 수동 날짜(DB, 같은 tx)를 막는 날로 `generateHolidayRules(originYear, { blockers })`를 다시 돌려 (다) 결과 중 `substitute`이고 `date > today`인 행만 `insertHolidayRows(…, tx)`로 넣는다. 지난 대체일은 건드리지 않는다(RESEARCH Pitfall 4 — 소급 금지). 수동 날짜 d가 바뀌면 부르는 쪽(04.2-12)은 d의 해 Y에 대해 `origin_year` Y-1과 Y를 차례로 재계산한다 — 12월 공휴일의 대체일이 다음 해 1월로 넘어간 경우를 잡는다(첫 계획의 「성탄절 대체일은 늘 12월 안」 가정은 버린다: 12-27~31을 수동으로 막으면 2027 성탄절 대체일이 2028-01-03이다). 이 플랜은 저장소 수준에서 두 순서를 증명한다: 2027 개천절(10-03 일)의 대체일은 10-04(월)인데, (1) 10-04 수동 행 먼저 → 2027 후보 생성 → 개천절 대체일 10-05 (2) 2027 후보 먼저(대체일 10-04) → 10-04 대체 행을 지우고 수동 행 삽입 + 재계산 → 대체일 10-05. 두 결과의 2027 행 집합이 같다.
- **달력 잠금(Codex 2차 #4):** 트랜잭션이 롤백을 주지만 두 재계산 사이의 순서는 주지 않는다 — 한 트랜잭션이 10-05를 지우고 대체일 10-05를 새로 넣는 동안 다른 트랜잭션의 대체일 삭제문이 기다렸다가 READ COMMITTED에서 그 새 행을 못 보고 지나가면 대체일이 둘 남는다. 그래서 `repositories/holidays.ts`에 `HOLIDAYS_LOCK_KEY`(파일 상단 정수 상수 — 04.2-01의 tick 잠금 키·04.2-10의 이메일 선점 키와 다른 값, `grep -rn "advisory" repositories`로 충돌 확인)와 `lockHolidayCalendar(viewer, tx)` — `SET LOCAL lock_timeout = '5s'` 뒤 `select pg_advisory_xact_lock(HOLIDAYS_LOCK_KEY)`(기다리는 잠금 — 시도형이 아니다: 수동 추가·삭제는 건너뛰면 안 된다)를 두고, `domain/holidays/candidates.ts`에 `withHolidayCalendarLock<T>(fn: (tx: DbOrTx) => Promise<T>, deps?)` — `withTransaction` 안에서 잠금을 잡은 뒤 `fn(tx)`. 표를 바꾸는 모든 길(후보 생성 · 04.2-12 추가·삭제와 재계산 · 04.2-11 확정)이 이 함수 안에서만 돈다. 잠금 범위는 연도가 아니라 달력 전체 하나다 — 원래 해 Y-1·Y 재계산이 두 해에 걸치고, 관리자 변경은 드물어 직렬화 비용이 없다. 잠금 대기는 `lock_timeout` 5s로 묶인다(tick 경로가 후보 생성을 거쳐도 무한 대기하지 않는다 — 04.2-10 실행 예산).
- **공유 트랜잭션 타입(Codex 2차 #7):** `db/client.ts`의 `DbOrTx = Pick<typeof db, "insert" | "update" | "select">`에 `"delete" | "execute"`를 더한다(그 한 줄만). `lib/db-transaction.ts`의 `withTransaction(fn: (tx: DbOrTx) => …)`은 `db.transaction`의 tx가 두 메서드를 이미 가지므로 고칠 것이 없다(계획 단계에서 넓힌 타입으로 `tsc --noEmit` 통과를 확인했다). 소유는 이 플랜이다 — 04.2-03(`tx.execute`)은 이 플랜 뒤(wave 3)로, 04.2-11·12는 이미 뒤다. 04.2-01의 지역 `NotifyTx`는 04.2-01이 이 플랜보다 먼저 돌므로 그대로 둔다. **Phase 4 조율:** 04-32는 「DbOrTx 타입은 넓히지 않는다」를 택해 `lib/db-transaction.ts`의 `withTimeoutConversion` 주석과 `test/integration/tx-safety.test.ts`의 `ExecutableTx` 캐스트로 우회한다. 이 변경은 멤버를 더하기만 해서 04-32 코드와 충돌하지 않는다 — 합칠 때 `db/client.ts` 충돌이 나면 두 쪽 멤버의 합집합을 남긴다. 04-32의 주석 한 줄(「DbOrTx 타입 자체는 넓히지 않고」)이 사실과 달라지는 것은 04.2-14 Task 1이 머지 뒤 그 줄만 고친다. `ExecutableTx` 캐스트는 무해해서 건드리지 않는다(SUMMARY에 언급).
- **표 모양:** `holidays` — `id` uuid PK(`defaultRandom`) · `date` date NOT NULL 유니크(`holidays_date_key`) · `name` text NOT NULL · `kind` text NOT NULL + CHECK(넷) · `origin_year` integer NULL + CHECK `holidays_origin_year_check`(`kind = 'substitute'`이면 NOT NULL, 아니면 NULL) · `created_by` text NULL FK `users.id`(규칙 행·초기 수동 목록은 null) · `created_at` timestamp NOT NULL 기본 now. 해별 조회는 `date` 범위(`>= YYYY-01-01 AND < (YYYY+1)-01-01`)로 하고 `year` 칸을 따로 두지 않는다(다음 해 1월로 넘어간 대체일은 그 날짜의 해 목록에 보인다). `holiday_year_confirmations` — `year` integer PK · `confirmed_at` timestamp NOT NULL 기본 now · `confirmed_by` text NOT NULL FK `users.id`. `holiday_year_generations` — `year` integer PK · `generated_at` timestamp NOT NULL 기본 now. 확정·생성 표를 04.2-11이 아니라 여기서 만드는 이유: 마이그레이션 파일을 둘(04.2-01 · 이 플랜)로 묶어 병렬 머지 저널 충돌 면을 줄인다.
- **마이그레이션 번호:** 고정 번호나 번호 범위를 쓰지 않는다(코디네이터 규칙). 브랜치에서는 `pnpm db:generate --name holidays`가 정한 번호를 그대로 쓴다. 머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다(04.2-14 — 결과는 main의 마지막 idx + 1).
- **필요한 해만 읽기(Codex #15):** 04.2-02의 순수 함수는 lookup이 모르는 해에 던진 오류를 그대로 전파한다. `calendar.ts`의 lookup은 읽지 않은 해를 요청받으면 `HolidayYearNotLoadedError(year)`를 던지고, `addBusinessDaysKst`·`isBusinessDayKst`는 그 오류를 받아 그 해만 보장·읽은 뒤 처음부터 다시 계산한다(해마다 한 번 — 다시 계산 횟수는 실제로 닿은 해의 수). 그 해의 보장이 `LunarTableRangeError`면 그대로 전파한다. 첫 계산은 기준 날짜의 해 하나만 읽는다.
- **관리자 저장소 함수를 여기 두는 이유:** 04.2-11·12는 파일 예산이 차 있고, 같은 표의 저장소를 한 플랜이 만들어야 두 화면 플랜이 같은 파일을 동시에 고치지 않는다. 쓰기 함수(`insertHolidayRows`·`insertYearGeneration`·`insertYearConfirmation`·`insertManualHoliday`·`deleteHolidayById`·`deleteFutureSubstitutes`)와 그 트랜잭션 안 읽기(`findManualDates`·`findHolidayByDate`·`findYearGeneration`)는 마지막 선택 인자 `tx: DbOrTx = db`를 받는다(04-32 `appendActionLog(viewer, row, tx)` 모양). `lockHolidayCalendar(viewer, tx)`는 tx가 필수다. `insertYearConfirmation`·`insertManualHoliday`·`insertYearGeneration`은 `ON CONFLICT DO NOTHING … RETURNING`으로 삽입 여부를 돌려준다. 첫 계획의 `countStatutoryHolidays`는 생성 표시로 대체되어 만들지 않는다.
</probe_fallback>

## Review Dispositions

| Codex # | 처리 | 한 줄 |
|---|---|---|
| 1 (high) — DB 쪽 | 반영 | 통합 기대값은 `official-calendar.ts`·`INITIAL_MANUAL_HOLIDAYS`에서만 온다 — 개수를 박지 않는다(Task 1·2) |
| 2 (high) | 반영 | 후보 생성이 수동 날짜를 막는 날로 넘김 · `recomputeFutureSubstitutes`(미래만) · 두 삽입 순서 2027-10-04 통합(Task 1·3). 도메인 추가·삭제 연결은 04.2-12 |
| 3 (high) — 공휴일 쪽 | 반영 | 공휴일 기한 발생 → 다음 영업일 tick 통합(Task 2) |
| 15 (medium) | 반영 | `HolidayYearNotLoadedError`로 닿은 해만 읽음 · |n| 가정 제거 · 표 첫 해·마지막 해 · 수동 휴일이 빽빽한 달력 통합(Task 2) |
| 16 (medium) | 반영 | `INITIAL_MANUAL_HOLIDAYS`를 첫 후보 생성과 한 문장으로 넣음(Task 1) |

### Review Dispositions (Codex 2차)

| Codex 2차 # | 처리 | 한 줄 |
|---|---|---|
| 4 (high) | 반영 | 달력 전체 advisory 잠금 `lockHolidayCalendar` + `withHolidayCalendarLock` — 생성(이중 확인)·재계산·(04.2-11·12의) 확정·추가·삭제가 모두 그 안에서만 쓴다 · 장벽으로 겹침을 확인하는 동시 삭제 통합이 정확한 최종 날짜 집합을 단언(Task 3) |
| 5 (medium) | 반영 | `holidays.origin_year` · 막는 날 = 그 해~다음 해 말 수동 날짜 · 재계산은 원래 해 단위(Y-1·Y는 04.2-12가 부름) · 2027 성탄절 → 2028-01-03 교차 연도 통합(Task 3) |
| 7 (medium) | 반영 | 공유 `DbOrTx`에 `delete`·`execute`를 더한다(`db/client.ts` 한 줄 — 이 플랜 소유). `withTransaction`은 그대로 컴파일된다. 04.2-03을 이 플랜 뒤로 옮긴다. Phase 4(04-32) 조율은 「공유 트랜잭션 타입」 줄 |
| 10 (medium) | 반영 | 저널 가드 파일을 04.2-04 Task 3(wave 1)이 만든다 — 이 플랜은 04.2-04에 의존한다(파일 없음 → 「No test files found」가 생길 수 없다) |
| 14 잔여 (partial) | 반영 | 생성 표시 표 `holiday_year_generations` — 법정 행 개수가 아니라 표시 행으로 완결을 판단 · 법정 행 하나만 있는 해를 채우는 통합(Task 2). 04.2-11 확정도 표시 행을 검사한다 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 빈 공휴일 표에서 2026-09-25(추석) tick이 그 해 후보(규칙 + 초기 수동 목록, 막는 날 반영)를 먼저 채우고 비영업일로 sent 0에 끝난다(표 → 영업일 함수 → tick 한 길)</name>
  <files>db/schema/holidays.ts, db/schema/index.ts, db/client.ts, db/migrations/*_holidays.sql, db/migrations/meta/*_snapshot.json, db/migrations/meta/_journal.json, repositories/holidays.ts, domain/holidays/candidates.ts, domain/holidays/calendar.ts, domain/notify/tick.ts, test/integration/notify-tick.test.ts</files>
  <read_first>
    - `db/schema/login-attempts.ts`(단순 표 본보기) · `db/schema/revenue-entries.ts`(`date` 칸) · `db/schema/action-log.ts`(`users.id` FK)
    - `db/schema/notifications.ts`·`repositories/notifications.ts`·`domain/notify/tick.ts`(04.2-01 결과 — `TickDeps.isBusinessDay` 자리·비공개 KST 도우미)
    - `domain/holidays/rules.ts`·`domain/holidays/business-day.ts`(04.2-02 결과 — `generateHolidayRules(year, { blockers })`·`INITIAL_MANUAL_HOLIDAYS`·`HolidayLookup`·`toKstDate`·`isBusinessDay`·`addBusinessDays`)
    - `test/unit/holidays/official-calendar.ts`(04.2-02 결과 — `OFFICIAL_2026`·`OFFICIAL_2027`)
    - `test/integration/notify-tick.test.ts`(04.2-01 — 조건 종류·호출 도우미)
    - `db/client.ts` — `DbOrTx`
  </read_first>
  <behavior>
    - 빈 `holidays` 표 · `runTick({ conditionKinds: [발생 1건], now: 2026-09-25T00:00:00Z })` → `{ status: "ok", sent: 0, skipped: 0, remaining: 0, businessDay: false }` · 평가 함수 호출 0회 · `notify_tick_runs.business_day` 거짓 · `holidays`의 2026 행(날짜·구분)이 `OFFICIAL_2026` ∪ 2026년 `INITIAL_MANUAL_HOLIDAYS`와 같다
    - 같은 준비로 2026-10-05T00:00:00Z(대체공휴일 월) → sent 0 · 2026-10-07T00:00:00Z(수) → sent 1
    - 2026년 `INITIAL_MANUAL_HOLIDAYS`의 날짜(체크포인트 값)에 tick → sent 0(초기 수동 목록이 판정에 들어간다)
    - 첫 tick 뒤 `holiday_year_generations`에 2026 행이 있다(생성 표시가 후보 삽입과 같은 트랜잭션에서 생긴다)
  </behavior>
  <action>
① `test/integration/notify-tick.test.ts`에 `describe("공휴일 tick (D-709)")`로 behavior 네 줄을 쓴다(RED) — `runTick`을 기본 `isBusinessDay`로 부른다(주입하지 않는다 — 이 플랜의 연결 자체를 증명한다). 기대 행은 `official-calendar.ts`와 `INITIAL_MANUAL_HOLIDAYS`에서 계산한다(개수 상수 금지). `INITIAL_MANUAL_HOLIDAYS`에 2026 항목이 없으면(체크포인트 답이 없음이었으면) 세 번째 줄은 `it.skip`이 아니라 「목록이 비었음」을 단언하는 케이스로 바꾸고 SUMMARY에 적는다.

② 공유 타입(per Codex 2차 #7): `db/client.ts`의 `DbOrTx` 한 줄에 `"delete" | "execute"`를 더한다 — `<probe_fallback>` 「공유 트랜잭션 타입」. 그 밖의 줄은 건드리지 않는다. 스키마 `db/schema/holidays.ts`(신규)를 `<probe_fallback>` 「표 모양」대로 세 표(`holidays` — `origin_year`·CHECK 포함 · `holiday_year_confirmations` · `holiday_year_generations`)로 만들고 `db/schema/index.ts`에 세 표를 내보낸다(항목만). `pnpm db:generate --name holidays`로 생성하고(번호·파일 이름을 손으로 정하지 않는다) 기존 마이그레이션과 같은 락 타임아웃 한 쌍 머리말(선례는 Grep `SET LOCAL lock_timeout` in `db/migrations`)과 한 줄 머리 주석(「Phase 04.2 — 공휴일 표·연도 확정. 머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다」)을 더한다. 신규 표뿐이라 `-- rollback-floor:` 표시는 없다.

③ `repositories/holidays.ts`(신규, 첫 인자 viewer): 이 Task에서는 `HOLIDAYS_LOCK_KEY` · `lockHolidayCalendar(viewer, tx)`(per Codex 2차 #4 — `<probe_fallback>` 「달력 잠금」) · `findYearGeneration(viewer, year, tx = db)` · `insertYearGeneration(viewer, year, tx = db)` → boolean · `insertHolidayRows(viewer, rows, tx = db)`(`onConflictDoNothing({ target: holidays.date })`, 삽입된 행 수 반환 — 행에 `originYear` 선택 칸) · `findHolidayDates(viewer, years)` · `findManualDates(viewer, { from, to }, tx = db)`(`kind in ('temporary','election')`)만 만든다.

④ `domain/holidays/candidates.ts`(신규): `withHolidayCalendarLock(fn, deps?)` · `ensureHolidayCandidatesLocked(year: number, tx: DbOrTx, deps?)` · `ensureHolidayCandidates(year: number, deps?)`(빠른 길 → 없으면 `withHolidayCalendarLock(tx => ensureHolidayCandidatesLocked(year, tx))`) — `<probe_fallback>` 「보장의 기준과 한 문장 삽입」 그대로(per Codex 2차 #14 잔여). `DbOrTx` 타입은 `repositories/document-counters.ts`의 재수출로 가져온다(domain → db 경계 규칙). 범위 밖이면 `LunarTableRangeError`가 그대로 전파된다(표시 행도 생기지 않는다 — 같은 트랜잭션). 행위자는 `SYSTEM_VIEWER`.

⑤ `domain/holidays/calendar.ts`(신규): `HolidayYearNotLoadedError`(필드 `year`) · `loadHolidayLookup(years: number[], deps?)` — 해마다 보장 → `findHolidayDates` 한 번 → 해별 `Set`으로 묶은 `HolidayLookup`(모르는 해 → `HolidayYearNotLoadedError`) · `isBusinessDayKst(date: string, deps?)` — 그 해 하나만 읽어 `isBusinessDay` · `addBusinessDaysKst(date, n, deps?)` — 이 Task에서는 기준 해 하나를 읽고 `HolidayYearNotLoadedError`를 받으면 그 해를 더해 다시 계산하는 되풀이까지 넣는다(Task 2가 경계를 증명한다).

⑥ `domain/notify/tick.ts`: `TickDeps.isBusinessDay` 기본값을 `isBusinessDayKst`로 바꾸고, KST 오늘을 04.2-02의 `toKstDate(now)`로 만든다 — 04.2-01의 비공개 KST 도우미와 월~금 기본 판정을 지운다(이 변경이 쓸모없게 만든 것만). 잠금 트랜잭션을 열기 **전에** 판정을 끝낸다(후보 삽입이 tick 잠금 트랜잭션을 늘리지 않게).
  </action>
  <verify>
    <automated>pnpm db:generate --name holidays &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>생성·squawk·저널 가드 중 하나가 0이 아닌 코드로 끝난다 — 락 타임아웃 한 쌍 누락, idx 빈칸, when 역행, SQL 파일 누락(두 번째 생성 실행에서 「No schema changes」가 나오면 정상이다)</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-tick.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 추석·대체공휴일·초기 수동 날짜 tick이 알림을 만들었거나, 빈 표를 「공휴일 없음」으로 계산했거나, 2026 행이 공식 목록과 다르거나, 04.2-01의 케이스(주말·상한·잠금·발생 계약)가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls db/migrations/*_holidays.sql`이 파일 하나를 보이고 첫 SQL 문장이 `SET LOCAL lock_timeout = '1s';`다
    - `domain/notify/tick.ts`에 `isBusinessDayKst`가 있고 04.2-01의 비공개 KST 도우미 이름이 남지 않았다(SUMMARY에 지운 이름을 적는다)
    - 공휴일 행 기대값이 공식 목록에서 계산된다 — `grep -c "OFFICIAL_2026" test/integration/notify-tick.test.ts`가 1 이상이고, 공휴일 행 수를 숫자 상수로 단언하는 줄이 없다(검토 — 알림 행 수 단언은 해당 없음)
    - 통합: 2026-09-25 · 2026-10-05 tick sent 0 · 2026-10-07 sent 1
    - `grep -c '"delete" | "execute"' db/client.ts`가 1이고 `git diff --stat -- db/client.ts`가 1줄 변경이다(per Codex 2차 #7)
    - `grep -c "countStatutoryHolidays" repositories/holidays.ts domain/holidays/candidates.ts`가 모두 0이다(생성 표시로 대체)
  </acceptance_criteria>
  <done>공휴일 표를 읽는 영업일 판정이 tick까지 한 길로 이어지고, 빈 표는 규칙 후보와 지난 임시공휴일로 채워진 뒤 읽힌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 후보 보장의 멱등·동시성·수동 행 보존 · 필요한 해만 읽기(표 경계·빽빽한 달력) · 범위 오류 · tick의 다음 해 시도 · 공휴일 기한 → 다음 영업일</name>
  <files>domain/holidays/candidates.ts, domain/holidays/calendar.ts, domain/notify/tick.ts, test/integration/holidays.test.ts, test/integration/notify-tick.test.ts</files>
  <read_first>
    - `domain/holidays/candidates.ts`·`calendar.ts`(Task 1 결과)
    - `db/client.ts`(`pool` — 동시성 테스트에 두 연결이 필요)
    - `test/support/notify-tick.ts`(04.2-01 — `createTestConditionKind(source)` 발생 목록 원천)
  </read_first>
  <behavior>
    - 빈 표에서 `isBusinessDayKst("2027-02-09")` 거짓(설 대체) — 2027 행이 `OFFICIAL_2027` ∪ 2027년 `INITIAL_MANUAL_HOLIDAYS`와 같다
    - `ensureHolidayCandidates(2026)` 두 번 → 2026 행 집합 그대로
    - 법정 행이 일부만 있는 해(Codex 2차 #14 잔여): 저장소로 2028 `statutory` 행 하나(`2028-01-01 신정`)만 넣고 표시 행은 없는 상태에서 `ensureHolidayCandidates(2028)` → 2028 행 집합이 한 번에 생성한 것과 같고 `holiday_year_generations`에 2028 행이 생긴다(법정 행 하나를 완성된 해로 오인하지 않는다)
    - 수동 행 먼저: 저장소로 `2026-06-03 선거일 kind=election created_by=관리자`를 넣은 뒤 보장 → 그 행의 구분·이름·만든 사람 그대로 · 규칙 날짜와 같은 날에 수동 행이 있으면 수동 행이 남는다(예: 2026-12-25에 `temporary` 행 → 보장 뒤 그 날짜 행 1개, 구분 `temporary`)
    - 확정된 해: `holiday_year_confirmations`에 2026 행이 있어도 보장은 행을 바꾸지 않는다
    - 두 연결 동시 `ensureHolidayCandidates(2027)`(`Promise.all`) → 2027 행 집합이 한 번 생성한 것과 같고 오류 없음
    - `addBusinessDaysKst("2026-12-31", 1)` = `"2027-01-04"`이고 2027 행이 생긴다
    - 표 경계(Codex #15): `isBusinessDayKst("2025-01-02")`·`addBusinessDaysKst("2025-01-02", 0)`은 2024를 요청하지 않고 성공한다 · `addBusinessDaysKst("2035-06-01", 0)`은 2036을 요청하지 않고 성공한다(주입한 적재 함수가 받은 해 목록으로 단언) · `addBusinessDaysKst("2035-12-31", 1)` → `LunarTableRangeError` · `addBusinessDaysKst("2025-01-02", -1)` → `LunarTableRangeError`
    - 빽빽한 달력: 2026-12-01~2026-12-31의 평일 전부를 수동 `temporary` 행으로 넣은 뒤 `addBusinessDaysKst("2026-11-30", 1)`이 2027년 첫 영업일이다(해를 넘겨 다시 읽는다 — |n| 한도 없음)
    - `ensureHolidayCandidates(2036)` → `LunarTableRangeError` · 2036 행 0
    - tick: `now` = 2035-10-10(수 — 다음 해 2036은 음력 표 밖)으로 부르면 tick은 끝까지 가고 경고 로그 `holiday.candidates_failed { year: 2036 }` 1회 · 2026-10-07 tick 뒤 2027 행이 있다
    - 공휴일 기한(Codex #3): 발생 1건(reference_date 2026-09-24 — 추석 연휴) → 2026-09-24·2026-09-25 tick sent 0 → 2026-09-28(월) tick sent 1(같은 발생 원천, `now`만 옮김)
  </behavior>
  <action>
① `test/integration/holidays.test.ts`(신규)에 behavior의 보장·계산 줄을, `notify-tick.test.ts`에 tick 두 줄을 쓴다(RED). 동시성은 서로 다른 풀 연결에서 도는지 `pool.totalCount`로 먼저 확인한다(풀 최대가 2 미만이면 테스트가 스스로 실패 — 가짜 통과 방지). 표 경계 케이스는 `calendar.ts` deps의 적재 함수를 감싼 스파이로 요청된 해 목록을 본다.

② 실패하는 것만 고친다. tick의 다음 해 시도: 영업일 판정 뒤 `ensureHolidayCandidates(올해 + 1)`을 try로 감싸 실패하면 `log.warn("holiday.candidates_failed", { year, message })`만 남긴다(올해 보장은 판정이 이미 했다 — 올해 실패는 tick 실패다: 표 밖의 해에 조용히 월~금으로 계산하지 않는다).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/holidays.test.ts test/integration/notify-tick.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 두 번 보장이 행을 늘렸거나, 법정 행이 일부만 있는 해를 채우지 않았거나, 수동·확정 행을 덮어썼거나, 동시 보장이 유니크 위반 오류를 냈거나, 연말을 넘길 때 다음 해를 읽지 않았거나, 표 첫 해·마지막 해 안의 계산이 표 밖의 해를 요청해 실패했거나, 빽빽한 달력에서 해를 넘기지 못했거나, 표 밖의 해가 빈 목록으로 통과했거나, 다음 해 실패가 tick을 멈췄거나, 공휴일 기한 발생이 다음 영업일에 나가지 않았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(경계·저장소 viewer 인자) 또는 tsc error로 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - behavior 열두 줄이 이름 있는 `it`로 있다
    - 동시성 케이스가 풀 연결 수를 먼저 단언한다
    - `grep -n "holiday.candidates_failed" domain/notify/tick.ts`가 1줄이다
    - `grep -n "250" domain/holidays/calendar.ts` 출력이 비어 있다(|n| 가정 한도 없음)
  </acceptance_criteria>
  <done>후보 생성이 몇 번, 몇 곳에서 동시에 불려도 표가 하나로 맞고, 사람이 넣은 행은 남으며, 계산은 실제로 닿는 해만 읽고, 공휴일에 기한이 된 알림은 다음 영업일에 나간다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 관리자 화면용 저장소와 미래 대체일 재계산 — 두 삽입 순서(2027-10-04)가 같은 표를 만들고, 동시 삭제가 대체일을 둘 남기지 않고, 해를 넘은 대체일을 원래 해로 다시 계산한다</name>
  <files>repositories/holidays.ts, domain/holidays/candidates.ts, test/integration/holidays.test.ts</files>
  <read_first>
    - `repositories/holidays.ts`·`domain/holidays/candidates.ts`(Task 1·2 결과)
    - `lib/db-transaction.ts` — `withTransaction`
    - `repositories/action-log.ts` — 선택 tx 마지막 인자 모양(04-32)
  </read_first>
  <behavior>
    - 저장소: `listHolidaysForYear(viewer, 2026)`은 날짜 오름차순 · `listHolidayYears(viewer)`는 행이 있는 해 오름차순 · `insertYearConfirmation(viewer, { year, confirmedBy }, tx)` 두 번째는 `false` · `insertManualHoliday` 같은 날짜 두 번째는 `false` · `findHolidayByDate` · `deleteHolidayById` 없는 id → `null`
    - 순서 1: 2027 행 없음 → `insertManualHoliday(2027-10-04 temporary)` → `ensureHolidayCandidates(2027)` → 개천절 대체일이 2027-10-05
    - 순서 2: `ensureHolidayCandidates(2027)`(대체일 2027-10-04) → 한 트랜잭션에서 10-04 대체 행 삭제 + `insertManualHoliday(2027-10-04 temporary)` + `recomputeFutureSubstitutes(2027, { today: "2026-09-24" }, tx)` → 대체일이 2027-10-05 · 순서 1과 2027 행 집합(날짜·구분)이 같다
    - 수동 행 삭제 뒤 재계산: 순서 2 결과에서 10-04 수동 행을 지우고 재계산 → 대체일이 다시 2027-10-04
    - 소급 금지: `today`가 대체일보다 뒤면(예: `today: "2027-10-06"`) 재계산은 그 대체일을 지우거나 옮기지 않는다
    - 재계산 도중 오류(주입한 `insertHolidayRows`가 던짐) → 트랜잭션 롤백 · 대체 행 그대로
    - 동시 삭제(Codex 2차 #4): 2027 후보 + 수동 2027-10-04·10-05(재계산 뒤 개천절 대체일 10-06) → 두 `withHolidayCalendarLock` 트랜잭션이 각각 10-05·10-04 수동 행을 지우고 `recomputeFutureSubstitutes(2027, { today: "2026-09-24" }, tx)` · 장벽: 첫 트랜잭션이 잠금을 잡고 삭제한 뒤 두 번째가 잠금 요청을 보냈다는 신호(`pg_stat_activity`에서 두 번째 연결의 `wait_event_type = 'Lock'`을 확인)를 받을 때까지 멈춘다 → 둘 다 커밋 뒤 2027 `substitute` 중 `origin_year = 2027`이고 10월 첫 주인 행이 정확히 `2027-10-04` 하나다(10-05·10-06 대체 행 없음)
    - 교차 연도(Codex 2차 #5): 2027·2028 후보 + 수동 2027-12-27~12-31(재계산 `origin_year` 2027) → 2027 성탄절 대체일이 `2028-01-03`, `origin_year = 2027` → 수동 2028-01-03 추가 + `origin_year` 2027·2028 재계산 → 성탄절 대체일 `2028-01-04` → 그 수동 행 삭제 + 같은 재계산 → 다시 `2028-01-03`
    - 표시 행 없는 해: `recomputeFutureSubstitutes(2031, …)`(2031 표시 행 없음) → 행 변화 0
  </behavior>
  <action>
① behavior 아홉 줄을 `test/integration/holidays.test.ts`에 `describe("관리자 저장소 · 미래 대체일 재계산 (D-4210)")`로 쓴다(RED). 쓰기 트랜잭션은 `withHolidayCalendarLock`으로 연다. 동시 삭제 케이스는 두 트랜잭션이 서로 다른 풀 연결에서 도는지 먼저 단언하고, 장벽은 테스트 콜백 안의 `Promise`(첫 트랜잭션이 삭제 뒤 `await` — 두 번째 연결이 잠금 대기에 들어간 것을 `pg_stat_activity`로 폴링해 확인한 뒤 풀어 준다, 최대 5초 뒤 테스트 실패)로 만든다 — 두 트랜잭션이 실제로 겹쳤음을 증명하지 못하면 테스트가 스스로 실패한다(순차 실행으로 가짜 통과 방지). 기대 날짜 집합은 `generateHolidayRules`로 계산하지 않고 위 날짜를 그대로 적는다(2027 달력은 04.2-02 체크포인트로 확인한 값 — 개천절 10-03 일요일).

② `repositories/holidays.ts`에 `<probe_fallback>` 「관리자 저장소 함수를 여기 두는 이유」의 나머지 함수를 더한다: `listHolidaysForYear(viewer, year)`(날짜 오름차순, 만든 사람 이름을 `users`와 LEFT JOIN — 규칙·초기 행은 null) · `listHolidayYears(viewer)`(행이 있는 해 오름차순 — `extract(year from date)` 고유값) · `findYearConfirmation(viewer, year, tx = db)` · `insertYearConfirmation(viewer, row, tx = db)` → boolean · `insertManualHoliday(viewer, row, tx = db)` → boolean · `findHolidayByDate(viewer, date, tx = db)` · `deleteHolidayById(viewer, id, tx = db)` → 지운 행 또는 null(`RETURNING`) · `deleteFutureSubstitutes(viewer, { originYear, after }, tx = db)`(`kind = 'substitute' AND origin_year = $originYear AND date > $after` — per Codex 2차 #5, 날짜의 해로 고르지 않는다). 날짜 범위·조건은 모두 바인딩 파라미터.

③ `domain/holidays/candidates.ts`에 `recomputeFutureSubstitutes(originYear: number, opts: { today: string }, tx: DbOrTx, deps?)`를 `<probe_fallback>` 「D-4210 막는 날 재계산」대로 더한다. 이 함수는 트랜잭션도 잠금도 스스로 열지 않는다 — 부르는 쪽(04.2-12의 추가·삭제)이 `withHolidayCalendarLock` 안에서 부른다(per Codex 2차 #4). 원래 해 Y-1·Y를 둘 다 부르는 책임도 부르는 쪽이다(per Codex 2차 #5 — 이 플랜의 교차 연도 테스트가 그 호출 순서를 그대로 쓴다).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/holidays.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 두 삽입 순서가 다른 표를 만들었거나, 수동 행이 대체일을 막지 못했거나, 동시 삭제가 대체일을 둘 남겼거나 두 트랜잭션의 겹침을 증명하지 못했거나, 해를 넘은 대체일이 원래 해 재계산으로 돌아오지 않았거나, 삭제 뒤 대체일이 돌아오지 않았거나, 지난 대체일을 소급해 옮겼거나, 재계산 오류가 반쯤 지운 표를 남겼거나, 중복 삽입·없는 id 삭제가 오류를 던졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(저장소 viewer 첫 인자 규칙) 또는 tsc error로 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 저장소 쓰기 함수 여섯 개가 마지막 선택 인자 `tx`를 받는다(`grep -c "tx: DbOrTx = db" repositories/holidays.ts`가 9 이상)
    - 통합: 순서 1·순서 2의 2027 행 집합이 같고 개천절 대체일이 2027-10-05 · 동시 삭제 뒤 개천절 대체일이 2027-10-04 하나 · 교차 연도 2028-01-03 → 01-04 → 01-03
    - `recomputeFutureSubstitutes` 안에 `withTransaction`·`withHolidayCalendarLock` 호출이 없다(Grep — 부르는 쪽이 연다)
    - `grep -c "pg_advisory_xact_lock" repositories/holidays.ts`가 1이다(달력 잠금 한 곳)
  </acceptance_criteria>
  <done>수동 공휴일이 언제 들어오든 미래 대체공휴일이 규정대로 다시 계산되고, 관리자 화면 두 플랜이 같은 저장소를 고치지 않고 쓸 수 있다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 코드 규칙 → 공휴일 표 | 지연 생성이 사람 확인 전 후보를 계산 입력으로 쓴다(D-705 — 의도된 동작, 배너는 04.2-13) |
| 동시 요청 → 공휴일 표 | tick·화면·계산이 같은 해를 동시에 처음 읽을 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-40 | Tampering | 수동·확정 행 덮어쓰기 | medium | mitigate | 보장은 `ON CONFLICT (date) DO NOTHING` 삽입만 하고 수정·삭제를 하지 않는다. 재계산은 미래 `substitute` 행만 지운다(Task 2·3 통합) |
| T-4.2-41 | Denial of Service | 동시 후보 생성 | low | mitigate | 유니크 날짜 + DO NOTHING — 두 연결 동시 보장 테스트 |
| T-4.2-42 | Tampering | 표 밖의 해를 월~금으로 계산 | medium | mitigate | 계산이 실제로 표 밖의 해에 닿으면 `LunarTableRangeError`(빈 목록 금지) · 올해 보장 실패는 tick 실패(500 — 경보) · 다음 해 실패는 경고 로그만 |
| T-4.2-43 | Repudiation | 규칙 행·초기 수동 행 출처 | low | accept | 규칙 행과 초기 수동 목록은 `created_by` null로 출처가 「규칙·초기 목록」임을 남긴다. 이 생성은 사람 행동이 아니라 행동 로그에 남기지 않는다(사람의 추가·삭제·확정 기록은 04.2-11·12) |
| T-4.2-44 | Tampering | 재계산 중 부분 실패 | medium | mitigate | 재계산은 부르는 쪽 트랜잭션 안에서만 돈다 — 도중 오류는 전부 롤백(Task 3 통합) |
| T-4.2-45 | Tampering | 동시 재계산이 대체일을 둘 남김 | high | mitigate | 모든 표 변경이 `withHolidayCalendarLock`(`pg_advisory_xact_lock`) 안에서만 — 장벽 동시 삭제 통합이 정확한 최종 집합을 단언(Task 3). 잠금 대기는 `lock_timeout` 5s |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm lint:sql` · `pnpm lint` · `pnpm typecheck` 통과
- `pnpm vitest run --project unit test/unit/db/migration-journal.test.ts test/unit/holidays` 초록
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/holidays.test.ts test/integration/notify-tick.test.ts` 초록

</verification>

<success_criteria>

- 공휴일 표가 규칙 후보·초기 수동 목록으로 지연 생성되고, 영업일 판정·계산과 tick이 그 표 하나를 읽는다
- 추석·대체공휴일 tick이 아무것도 보내지 않고, 그날 기한이 된 알림은 다음 영업일에 나간다(D-709)
- 수동 공휴일과 대체공휴일의 관계가 두 삽입 순서 모두에서 규정대로다
- 04.2-11·12(관리자 화면)와 Phase 7(지급일·마감)이 `calendar.ts`·`candidates.ts`·저장소 함수만 불러 같은 달력을 쓸 수 있다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표(신규) | `holidays`(id · date 유니크 `holidays_date_key` · name · kind CHECK · origin_year + CHECK `holidays_origin_year_check` · created_by · created_at) · `holiday_year_confirmations`(year PK · confirmed_at · confirmed_by) · `holiday_year_generations`(year PK · generated_at) |
| 마이그레이션 | `db/migrations/*_holidays.sql` + snapshot + `_journal.json` 항목(번호는 생성기 — 04.2-14가 머지 직전 재생성) |
| 바뀐 공유 타입 | `DbOrTx`(`db/client.ts`) — `"delete" | "execute"` 추가(Codex 2차 #7, Phase 4 04-32와 합칠 때 멤버 합집합) |
| 상수(신규) | `HOLIDAYS_LOCK_KEY`(`repositories/holidays.ts`) |
| 저장소 함수(신규) | `lockHolidayCalendar` · `findYearGeneration` · `insertYearGeneration` · `insertHolidayRows` · `findHolidayDates` · `findManualDates` · `listHolidaysForYear` · `listHolidayYears` · `findYearConfirmation` · `insertYearConfirmation` · `insertManualHoliday` · `findHolidayByDate` · `deleteHolidayById` · `deleteFutureSubstitutes` |
| 도메인 함수(신규) | `withHolidayCalendarLock` · `ensureHolidayCandidates` · `ensureHolidayCandidatesLocked` · `recomputeFutureSubstitutes` · `loadHolidayLookup` · `isBusinessDayKst` · `addBusinessDaysKst` |
| 오류(신규) | `HolidayYearNotLoadedError`(`calendar.ts`, 필드 `year`) |
| 바뀐 심볼 | `TickDeps.isBusinessDay` 기본값 → `isBusinessDayKst` · tick의 KST 오늘 → `toKstDate`(04.2-01 비공개 도우미 제거) |
| 로그 이벤트 | `holiday.candidates_failed`(warn, year) |
| 테스트 | `test/integration/holidays.test.ts`(신규) · `test/integration/notify-tick.test.ts`(공휴일 tick describe) |
| 결정 | D-4205(지연 생성 계기) · D-4210(막는 날 재계산 — DB 쪽) · D-4221(초기 수동 목록 — DB 쪽) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-06-SUMMARY.md` when done
</output>
