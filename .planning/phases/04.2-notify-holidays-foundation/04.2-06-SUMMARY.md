---
phase: 04.2-notify-holidays-foundation
plan: 06
subsystem: database
tags: [postgres, drizzle, holidays, business-day, advisory-lock, notify-tick]
status: complete

requires:
  - phase: 04.2-01
    provides: runTick · TickDeps.isBusinessDay 주입 자리 · withDeadlineTransaction/DbDeadlineError
  - phase: 04.2-02
    provides: generateHolidayRules(year, { blockers }) · INITIAL_MANUAL_HOLIDAYS · isBusinessDay/addBusinessDays/toKstDate · LunarTableRangeError · official-calendar.ts
  - phase: 04.2-04
    provides: test/unit/db/migration-journal.test.ts 저널 가드
provides:
  - holidays(origin_year·CHECK) · holiday_year_confirmations · holiday_year_generations 표와 0012_holidays 마이그레이션
  - DbOrTx에 delete·execute(공유 트랜잭션 타입)
  - repositories/holidays.ts — 달력 잠금·마감 트랜잭션, 생성 표시, 법정·수동 삽입, 대체 삽입(ON CONFLICT 없음), 빠른 길 읽기, 관리자 화면용 읽기·쓰기
  - domain/holidays/candidates.ts — withHolidayCalendarLock · ensureHolidayCandidates(Locked) · recomputeFutureSubstitutes(비공개 allocateSubstitutes)
  - domain/holidays/calendar.ts — isBusinessDayKst · addBusinessDaysKst · loadHolidayLookup · HolidayYearNotLoadedError
  - tick 기본 영업일 판정 = isBusinessDayKst, 다음 해 후보 시도(holiday.candidates_failed 경고)
affects: [04.2-03, 04.2-10, 04.2-11, 04.2-12, 04.2-14, phase-7]

actuals:
  tokens: 35258
  tasks: 3
  commits: 3
plan_head_before: f259c89881f82b6124a28a622b5f37d53d4c3e82

tech-stack:
  added: []
  patterns:
    - "달력 전체 advisory 잠금(pg_advisory_xact_lock 420_601) 안에서만 공휴일 표를 바꾼다"
    - "대체일 배정: 생성된 원래 해 오름차순 · 점유 집합 하나 · ON CONFLICT 없는 INSERT(충돌 = 23505 롤백)"
    - "계산이 모르는 해에 닿으면 HolidayYearNotLoadedError → 그 해만 보장·읽고 다시 계산"

key-files:
  created:
    - db/schema/holidays.ts
    - db/migrations/0012_holidays.sql
    - db/migrations/meta/0012_snapshot.json
    - repositories/holidays.ts
    - domain/holidays/candidates.ts
    - domain/holidays/calendar.ts
    - test/integration/holidays.test.ts
  modified:
    - db/client.ts
    - db/schema/index.ts
    - db/migrations/meta/_journal.json
    - domain/notify/tick.ts
    - test/integration/notify-tick.test.ts

key-decisions:
  - "HOLIDAYS_LOCK_KEY = 420_601 (tick 잠금 420_401과 다른 값, 리포 안 다른 advisory 키 없음)"
  - "생성 표시 행이 있는 해의 규칙 행은 다시 만들지 않는다 — 규칙이 틀렸던 해는 마이그레이션으로 바로잡는다(가정)"
  - "tick은 잠금 트랜잭션을 열기 전에 영업일을 판정하고 다음 해 후보를 시도한다(실패는 경고만, 올해 실패는 tick 실패)"
  - "addBusinessDaysKst 재시도 때 적재 함수에 누적 해 목록을 넘긴다(그 해들을 다시 읽음 — 재시도 수는 닿은 해 수)"

requirements-completed: [ADMN-11, NOTI-04]

duration: 13min
completed: 2026-09-24
---

# Phase 04.2 Plan 06: 공휴일 표 · 지연 후보 생성 · 표를 읽는 영업일 계산과 tick 연결 Summary

**공휴일 표 세 개와 달력 advisory 잠금 안의 지연 후보 생성(초기 수동 목록 · 점유 집합 대체일 배정 · 미래 대체일 재계산), 닿은 해만 읽는 영업일 계산, 이 판정으로 바꾼 notify tick**

## Performance

- **Duration:** 약 13분(측정 시작 21:48:58Z → 22:01Z)
- **Tasks:** 3/3
- **Files:** 12개(생성 7 · 수정 5)

## Accomplishments

- 빈 표에서 추석(2026-09-25)·대체공휴일(10-05)·초기 선거일(06-03) tick이 sent 0이고, 2026 행이 `OFFICIAL_2026` ∪ 2026 `INITIAL_MANUAL_HOLIDAYS`와 같다. 수요일(10-07)은 sent 1
- 공휴일 기한 발생(09-24)이 09-24·09-25에는 안 나가고 09-28(월) tick에서 나간다. 2035-10-10 tick은 2036 실패를 `holiday.candidates_failed { year: 2036 }` 경고 1회로 넘긴다
- 두 삽입 순서(2027-10-04)와 두 생성 순서(2032·2033)가 같은 표를 만든다. 장벽 동시 삭제(`pg_stat_activity` 잠금 대기 확인) 뒤 개천절 대체일은 10-04 하나다. 교차 연도(2028-01-03 → 01-04 → 01-03), 사슬(01-28/02-02 ↔ 02-02/02-03), Y+2, 23505 충돌, 삭제 뒤 실패 롤백(E6)도 확인했다

## Task Commits

1. **Task 1: 트레이서 — 표 → 영업일 함수 → tick** - `c3e7706` (feat)
2. **Task 2: 멱등·동시성·경계·다음 해 시도·공휴일 기한** - `1f8ff8d` (feat)
3. **Task 3: 관리자 저장소 · 미래 대체일 재계산** - `3f96415` (feat)

## Files Created/Modified

- `db/schema/holidays.ts` - 세 표(`holidays_date_key` · `holidays_kind_check` · `holidays_origin_year_check`)
- `db/migrations/0012_holidays.sql` - 생성기 번호 0012 · 락 타임아웃 한 쌍 머리말 · 머리 주석
- `db/client.ts` - `DbOrTx`에 `"delete" | "execute"` (1줄)
- `repositories/holidays.ts` - 상수 셋 · 잠금 · 마감 트랜잭션 · 저장소 함수 17개
- `domain/holidays/candidates.ts` - 잠금 도우미 · 후보 보장 · 대체일 배정 · 재계산
- `domain/holidays/calendar.ts` - 표를 읽는 영업일 판정·N영업일 계산
- `domain/notify/tick.ts` - 기본 판정 교체, 판정을 잠금 트랜잭션 앞으로, 다음 해 시도. 지운 비공개 도우미: `kstDateOf`, `isKstWeekday`
- `test/integration/holidays.test.ts` - 보장·계산 10개 + 관리자 저장소·재계산 12개
- `test/integration/notify-tick.test.ts` - `공휴일 tick (D-709)` 6개

## Decisions Made

frontmatter `key-decisions`를 따랐다. 04.2-02 기본값(공휴일 규칙 · 임시 목록 · 공식 달력 fixture)은 바꾸지 않았다.

## Deviations from Plan

### 계획과 다르게 한 점

**1. [Rule 1 - Bug] tick 안의 `businessDay` 이중 선언**
- **Found during:** Task 1
- **Issue:** 판정을 잠금 트랜잭션 앞으로 옮기자 잠금 결과를 푸는 줄의 `businessDay`와 이름이 겹쳐 tsc 오류가 났다
- **Fix:** 잠금 콜백이 돌려주는 값에서 `businessDay`를 빼고 바깥 변수를 쓴다(동작 같음)
- **Files modified:** domain/notify/tick.ts
- **Commit:** c3e7706

**2. 작은 형태 차이**
- `withHolidayCalendarLock(fn)`에 `deps?` 인자를 두지 않았다. 계획에 쓰임새가 없어서다
- 순서 1과 순서 2는 비교 대상이라 한 `it`에 묶었다. 그래서 Task 3 behavior 13줄이 `it` 12개다
- 동시 삭제 테스트는 풀 최대 ≥ 3을 단언한다(두 트랜잭션 + `pg_stat_activity` 폴링 연결)
- Task 2 테스트 중 10개는 쓰자마자 통과했다. Task 1 action이 그 함수들을 먼저 만들게 되어 있어서다. 변이 검사로 확인했다: 계산이 앞뒤 해를 미리 읽게 바꾸면 경계·빽빽한 달력 테스트 2개가 실패한다. 이중 확인을 빼는 변이는 결과로 드러나지 않는다(두 번째 생성이 같은 행으로 멱등하게 재배정한다). 대신 잠금 직렬화는 Task 3 장벽 테스트가 증명한다

**Total deviations:** 1건 자동 수정(Rule 1) + 형태 차이 4건. 범위 확장은 없다.

## 참고 (계획이 SUMMARY에 남기라고 한 것)

- 2026 `INITIAL_MANUAL_HOLIDAYS`가 비어 있지 않아(2026-06-03 선거일) 세 번째 tick 케이스를 계획대로 두었다
- `test/integration/tx-safety.test.ts`의 `ExecutableTx` 캐스트(04-32)는 이 브랜치에 없어 건드리지 않았다. 04-32 주석 한 줄은 04.2-14 Task 1이 고친다
- 머지 전에는 04.2-14가 0012 마이그레이션·스냅숏·journal 항목을 다시 생성한다

## Verification (실제 실행 결과)

- `pnpm lint` exit 0(기존 boundaries 사용 중단 경고만) · `pnpm typecheck` exit 0 · `pnpm lint:sql` 0 issues / 13 files
- `pnpm test:unit` 79 files · 812 tests passed(저널 가드 포함)
- 통합(파일 하나씩): holidays 22/22 · notify-tick 27/27 · migrate-runner 3/3 · db-bootstrap 2/2
- `pnpm db:generate --name holidays` 재실행 → "No schema changes" · `git diff f259c89 HEAD -- package.json pnpm-lock.yaml` 비어 있음(새 의존성 0)

## Known Stubs

없다.

## Next Phase Readiness

- 04.2-03(`tx.execute`), 04.2-10(실행 예산), 04.2-11·12(관리자 화면), Phase 7이 `calendar.ts`·`candidates.ts`·저장소 함수를 불러 쓸 수 있다
- 한도 풀리면 Codex 재확인 필요(2026-09-29 이후)

## Self-Check: PASSED

- 파일 7개 생성 확인, 커밋 c3e7706 · 1f8ff8d · 3f96415가 git log에 있다
