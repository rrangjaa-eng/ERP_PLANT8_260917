---
phase: 04.2-notify-holidays-foundation
plan: 08
type: execute
wave: 4
depends_on: [04.2-03]
files_modified:
  - domain/auth/accounts.ts
  - scripts/account-cli.ts
  - .github/workflows/account.yml
  - test/unit/account-cli.test.ts
  - test/unit/deploy/workflows.test.ts
  - test/integration/account-cli.test.ts
  - test/integration/action-log.test.ts
autonomous: true
requirements: [D-712]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "관리자가 잠금을 풀면 행동 로그에 `account_unlock` 한 행이 남는다(D-712 · ROADMAP 04.2 기준 5)"
    - "잠금 행동 로그의 행위자는 시스템(actorId null)이고 대상 이메일은 detail에 남으며, 해제 로그의 행위자는 해제한 관리자다"
    - "지금 사용자가 실제로 쓸 수 있는 해제 경로는 GitHub Actions `account.yml`(workflow_dispatch) → Cloud Run Job → `scripts/account-cli.ts unlock`이고, 그 경로의 「해제한 관리자」는 워크플로를 실행한 GitHub 계정(`github.actor`)이다 — CLI가 `--operator`로 받아 `detail.operator`에 남기고 `actorId`는 null(시스템 실행)이다(D-4222 · Codex #13)"
    - "`unlock`은 `--operator` 없이는 사용법 오류(exit 2)다 — 해제 기록에 누가 풀었는지가 빠지는 실행이 없다"
    - "세션이 있는 관리자 viewer가 도메인 `unlockAccount`를 부르면 `actorId` = 그 관리자다(관리자 화면이 생길 때 쓸 계약 — 지금은 화면이 없다)"
    - "열린 실패 해소(`resolveOpenFailures`)와 해제 행동 로그 INSERT는 한 트랜잭션이다 — 로그 쓰기가 실패하면 해소도 롤백되어 계정은 잠긴 채이고 「풀렸는데 해제 기록 0건」이 없다(D-4220 · Codex #12)"
    - "해제 트랜잭션 안에서는 `tx`로 묶인 호출만 한다 — 권한 검사와 사용자 조회(`findUserByEmail`)는 `withTransaction` 전에 끝나, 해소 UPDATE의 행 잠금을 쥔 트랜잭션이 전역 풀에서 두 번째 연결을 잡지 않는다. 풀에 빈 연결이 1개뿐이어도 해제가 5초 안에 끝난다(eng E5와 같은 교착 — 04.2-03)"
  artifacts:
    - path: "domain/auth/accounts.ts"
      provides: "`unlockAccount(viewer, email, opts?)` — 해소 + 해제 로그 한 트랜잭션 · `detail.operator`"
      contains: "account_unlock"
    - path: "scripts/account-cli.ts"
      provides: "`unlock --email <e> --operator <github 계정>`"
      contains: "--operator"
    - path: ".github/workflows/account.yml"
      provides: "unlock 인자에 `--operator,${github.actor}`(env 경유)"
      contains: "github.actor"
    - path: "test/integration/account-cli.test.ts"
      provides: "실제 CLI 프로세스로 해제 → 행동 로그 한 행(operator) 통합"
  key_links:
    - from: ".github/workflows/account.yml"
      to: "scripts/account-cli.ts"
      via: "`ARGS=\"$ARGS,--operator,$INPUT_OPERATOR\"`(unlock일 때) — `INPUT_OPERATOR: ${{ github.actor }}`"
      pattern: "--operator"
    - from: "scripts/account-cli.ts"
      to: "domain/auth/accounts.ts"
      via: "`unlockAccount(SYSTEM_VIEWER, email, { operator })`"
      pattern: "operator"
    - from: "domain/auth/accounts.ts"
      to: "domain/action-log/record.ts"
      via: "`withTransaction` 안에서 `recordAction(viewer, { actionType: \"account_unlock\", … }, { tx })`"
      pattern: "account_unlock"
---

<objective>
**As a** 감사 담당, **I want to** 계정 잠금이 풀린 일이 누가 풀었는지와 함께 행동 로그에 빠짐없이 남기를, **so that** 실제로 쓰는 해제 경로(GitHub Actions → CLI)에서도 「풀었는데 기록이 없다」나 「기록은 있는데 누가 풀었는지 모른다」가 생기지 않는다.

(스토리는 ROADMAP 04.2 기준 5 「계정 잠금·해제가 행동 로그에 남는다」에서 끌어왔다.)

Purpose: D-712의 해제 쪽 — 해소와 로그의 원자성(Codex #12), 실제 호출 경로의 운영자 신원 보존(Codex #13).

Output: 해제 도메인의 트랜잭션·detail · CLI `--operator` · 워크플로 인자 · 실제 CLI 통합 테스트.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 새 의존성 0개. 관리자 해제 화면은 만들지 않는다(요구 밖).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-03-SUMMARY.md
@CLAUDE.md
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 D-712 encoding(#28)의 해제 쪽이다(잠금 쪽은 04.2-03).

**계획 단계 결정(표면화):**

- **D-4222 CLI 경로의 운영자 신원(Codex #13):** 첫 계획은 관리자 viewer를 직접 주입한 통합 테스트로 「해제 로그의 행위자는 관리자」를 증명했지만, 사용자가 실제로 쓸 수 있는 해제 경로는 CLI(`SYSTEM_VIEWER`)뿐이라 그 테스트는 쓰이지 않는 길을 증명했다. 관리자 해제 화면을 새로 만드는 대신(요구 밖) 실제 경로의 신원을 보존한다: `account.yml`이 GitHub가 정하는 `github.actor`(워크플로를 실행한 계정 — 저장소 쓰기 권한자만 dispatch할 수 있다)를 env `INPUT_OPERATOR`로 받아 unlock일 때만 `--args`에 `--operator,<계정>`을 붙이고, CLI가 `unlockAccount(SYSTEM_VIEWER, email, { operator })`로 넘겨 `detail.operator`에 남긴다. `actorId`는 사람 사용자 id가 아니므로 null이다(users 표에 없는 값을 FK에 넣지 않는다 — `recordAction` 기존 규칙). 행동 로그 화면은 detail을 이미 보여 주므로 화면을 고치지 않는다.
- **operator 형식:** `--args`가 쉼표로 인자를 나누므로 쉼표·공백이 든 값은 인자를 깨뜨린다. CLI가 `^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38})$`(GitHub 계정 이름 규칙)만 받고 나머지는 사용법 오류(exit 2)다. 워크플로 쪽은 기존 규약대로 입력을 env로만 넘긴다(셸 인젝션 방지 — T-1-32).
- **원자성(D-4220):** `unlockAccount`가 `withTransaction(tx => …)` 안에서 `resolveOpenFailures(SYSTEM_VIEWER, email, "admin_unlock", tx)`(04.2-03이 더한 tx 인자) → `recordAction(viewer, { actionType: "account_unlock", entity: "user", entityId, detail: { email, resolved, operator } }, { tx })`. 권한 검사(`admin.people` write)는 트랜잭션 전 그대로이고, `entityId`를 정하는 `findUserByEmail(SYSTEM_VIEWER, email)`도 **트랜잭션을 열기 전에** 한 번 부른다(eng E5 — 그 함수는 전역 `db`를 써서 풀 연결을 하나 더 잡는다. 콜백 안에서 부르면 해소 UPDATE의 행 잠금을 쥔 채 두 번째 연결을 기다리게 되고, 같은 이메일을 기다리는 쪽이 풀을 다 차지하면 풀리지 않는다 — 04.2-03 「트랜잭션 안에서 두 번째 연결 금지」와 같은 규칙). 콜백 안에는 `tx`를 받는 두 호출만 둔다. 열린 실패가 0건이어도 해제 행동은 기록한다(관리자가 명령을 실행한 사실). 커밋 뒤 기존 `log.info("auth.unlock", …)`는 그대로 둔다.
- **04-32 전제:** 04.2-03 Task 1의 `<precondition>`과 같다 — 04.2-03이 끝났으면 이미 충족돼 있다.
</probe_fallback>

## Review Dispositions

| Codex # | 처리 | 한 줄 |
|---|---|---|
| 12 (medium) — 해제 쪽 | 반영 | 해소 + 해제 로그 한 트랜잭션 · 로그 쓰기 실패 시 계정이 잠긴 채 남는지 통합(Task 1) |
| 13 (medium) | 반영 | 실제 경로(account.yml → CLI) 운영자 신원 `github.actor` → `--operator` → `detail.operator` · 실제 CLI 프로세스 통합 테스트(Task 1·2). 관리자 해제 화면은 요구 밖이라 만들지 않음 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 실제 CLI 프로세스 `unlock --email … --operator …` 한 번이 한 트랜잭션에서 열린 실패를 풀고 행동 로그 `account_unlock` 한 행(operator)을 남긴다</name>
  <files>domain/auth/accounts.ts, scripts/account-cli.ts, test/unit/account-cli.test.ts, test/integration/account-cli.test.ts, test/integration/action-log.test.ts</files>
  <precondition>04.2-03이 끝나 `repositories/login-attempts.ts`의 `resolveOpenFailures`가 선택 `tx` 인자를 받고 `domain/action-log/record.ts`에 `account_unlock`이 있다(`grep -q "account_unlock" domain/action-log/record.ts`)</precondition>
  <read_first>
    - `domain/auth/accounts.ts` 95~110행 — `unlockAccount(viewer, email)`(can → resolveOpenFailures → `log.info("auth.unlock")`)
    - `scripts/account-cli.ts` 전체 — `ParsedArgs` · `parseArgs` 플래그 루프 · `run`의 unlock 가지 · `UsageError` exit 2
    - `test/unit/account-cli.test.ts` 60~80행 — 「unlock: --email만 파싱한다」
    - `test/integration/account-cli.test.ts` 1~60행 — `runAccountCli(args)`(`spawnSync("node", ["--import", "tsx", "scripts/account-cli.ts", …])`)
    - `domain/auth/lockout.ts`(04.2-03 결과 — `recordLoginFailure`로 잠금 만들기)
    - `lib/db-transaction.ts` — `withTransaction`
  </read_first>
  <behavior>
    - 통합(실제 CLI): 잠긴 계정(04.2-03 `recordLoginFailure`를 임계값만큼) → `runAccountCli(["unlock", "--email", e, "--operator", "octo-admin"])` 종료 0 → `account_unlock` 1행 · `actorId` null · `detail` = `{ email: e, resolved: 열린 실패 수, operator: "octo-admin" }` · 그 이메일의 열린 실패 0
    - 통합(도메인): 관리자 viewer(`admin.people` write)로 `unlockAccount(admin, e)` → `account_unlock` 1행 · `actorId` = 그 관리자 id · `detail.operator` null
    - 통합(원자성): 잠긴 계정 · `unlockAccount(admin, e, { recordAction: 던지는 가짜 })` → 오류 전파 · 열린 실패 수 그대로(여전히 잠김) · `account_unlock` 0행
    - 통합(풀 교착 회귀 — eng E5, 연결 1개 풀과 같은 상태): 잠긴 **존재하는** 사용자 이메일에서 `pool`(`@/db/client`)의 `pool.options.max - 1`개 연결을 `pool.connect()`로 잡아 둔 채 `unlockAccount(admin, e)`를 5초 타이머와 `Promise.race` → 타이머보다 먼저 끝난다 · 잡아 둔 연결을 `finally`에서 모두 `release()`한 뒤 `account_unlock` 1행이고 `entityId`가 그 사용자 id다
    - 단위: `parseArgs(["unlock", "--email", "a@b.c", "--operator", "octo-admin"])` = `{ cmd: "unlock", email: "a@b.c", operator: "octo-admin" }` · `--operator` 없음 → `UsageError` · `--operator` 값 `"a,b"`·`"-x"`·`""`·40자 → `UsageError` · `create`·`reset`에 `--operator` → `UsageError`(unlock 전용)
  </behavior>
  <action>
① behavior를 먼저 쓴다(RED): 통합 도메인 케이스(원자성·풀 교착 회귀 포함)는 `test/integration/action-log.test.ts`에 `describe("잠금 해제 행동 로그 (D-712)")`로(풀 교착 케이스는 04.2-03의 같은 파일 잠금 쪽 케이스와 같은 모양 — 타이머가 이기면 「두 번째 풀 연결을 해제 트랜잭션 안에서 잡았다(eng E5)」로 실패, `clearTimeout`으로 치움), 실제 CLI 케이스는 `test/integration/account-cli.test.ts`에, 단위는 `test/unit/account-cli.test.ts`에(기존 「unlock: --email만 파싱한다」 케이스는 `--operator`를 더한 기대값으로 고친다 — 동작이 바뀌었으므로).

② `domain/auth/accounts.ts`: `unlockAccount(viewer, email, opts?: { operator?: string; recordAction?: typeof recordAction })` — `<probe_fallback>` 「원자성」대로. 사용자 id는 기존 `findUserByEmail(SYSTEM_VIEWER, email)`로 권한 검사 뒤·`withTransaction` 전에 찾고 없으면 `entityId` null(eng E5). 트랜잭션 콜백 안에서는 `resolveOpenFailures(…, tx)`와 `recordAction(…, { tx })`만 부른다 — 전역 `db`를 쓰는 저장소 함수를 콜백 안에 두지 않는다.

③ `scripts/account-cli.ts`: 플래그 루프에 `--operator`를 더하고 `ParsedArgs`의 unlock 가지를 `{ cmd: "unlock"; email: string; operator: string }`로 바꾼다. `<probe_fallback>` 「operator 형식」 검사 · unlock에 없으면 `UsageError("unlock에는 --operator가 필요합니다.")` · create·reset에 있으면 `UsageError`. `run`의 unlock 가지가 `unlockAccount(SYSTEM_VIEWER, parsed.email, { operator: parsed.operator })`를 부르고 출력 줄은 그대로(`account unlocked: … (resolved=…)`).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/account-cli.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — `--operator` 없는 unlock이 통과했거나, 쉼표·대시로 시작하는 값이 통과했거나, create·reset이 `--operator`를 받았다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/account-cli.test.ts test/integration/action-log.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 실제 CLI 해제 뒤 해제 행이 없거나 operator가 빠졌거나, 관리자 viewer 호출의 행위자가 관리자가 아니거나, 로그 쓰기 실패 뒤 계정이 풀려 있었거나, 빈 연결 1개만 남긴 풀에서 해제가 5초 안에 끝나지 않았다(해제 트랜잭션 안에서 두 번째 풀 연결을 잡음 — eng E5)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "{ tx }" domain/auth/accounts.ts`가 1 이상이다
    - 통합: 실제 CLI 프로세스 → `detail.operator = "octo-admin"` · 로그 실패 → 열린 실패 수 불변 · 빈 연결 1개 풀 → 5초 안에 해제 + `account_unlock` 1행(entityId = 사용자 id)(eng E5)
    - `awk '/^export async function unlockAccount/{u=1} u && /findUserByEmail\(/{if(!f)f=NR} u && /withTransaction\(/{if(!w)w=NR} END{exit !(f && w && f<w)}' domain/auth/accounts.ts` 종료 코드 0 — `unlockAccount` 본문 안에서 사용자 조회 호출이 트랜잭션 여는 줄보다 앞이다(같은 파일의 `createAccount`·`resetPassword`도 이 함수를 불러 파일 전체 대조는 뜻이 없다 — eng E5)
    - `pnpm lint && pnpm typecheck` 통과
  </acceptance_criteria>
  <done>실제로 쓰는 해제 경로가 누가 풀었는지와 함께 한 트랜잭션으로 기록된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: account.yml이 unlock에만 `--operator,${github.actor}`를 env 경유로 붙인다</name>
  <files>.github/workflows/account.yml, test/unit/deploy/workflows.test.ts</files>
  <read_first>
    - `.github/workflows/account.yml` 48~75행 — env 입력 · `ARGS` 조립
    - `test/unit/deploy/workflows.test.ts` 165~200행 — account.yml 텍스트 단언 모양(YAML 파서 없음)
  </read_first>
  <behavior>
    - 단위: account.yml에 `INPUT_OPERATOR: ${{ github.actor }}`가 env로 있다 · `ARGS` 조립에 unlock 조건 아래 `--operator,$INPUT_OPERATOR`가 있다 · `run:` 본문에 `${{ github.actor }}`가 직접 들어가지 않는다(env로만)
  </behavior>
  <action>
① behavior 세 단언을 `workflows.test.ts`의 `describe("account.yml")`에 더한다(RED).

② `account.yml`: 셸 단계 env에 `INPUT_OPERATOR: ${{ github.actor }}` 한 줄 · `ARGS` 조립에 `if [ "$INPUT_ACTION" = "unlock" ]; then ARGS="$ARGS,--operator,$INPUT_OPERATOR"; fi` 한 가지. 다른 줄(폴링·로그 질의·성공 판정)은 고치지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/workflows.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — operator가 env로 오지 않거나 `run:` 본문에 표현식이 직접 들어갔거나 unlock 외 동작에도 붙었거나 기존 account.yml 단언이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "github.actor" .github/workflows/account.yml`가 1이다(env 한 줄)
    - `git diff --numstat HEAD -- .github/workflows/account.yml`의 추가 줄이 5 이하다
  </acceptance_criteria>
  <done>GitHub Actions로 잠금을 풀면 실행한 계정이 CLI까지 전달된다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions dispatch → Cloud Run Job | 워크플로 입력과 `github.actor`가 CLI 인자가 된다 |
| CLI 인자 → 행동 로그 detail | `--operator` 값이 감사 기록에 그대로 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-120 | Repudiation | 계정 해제 | medium | mitigate | 해소 + 해제 로그 한 트랜잭션(`recordAction(…, { tx })`) · `account_unlock`은 끌 수 없음(04.2-03) · 실제 CLI 경로가 `github.actor`를 남김(Task 1·2) |
| T-4.2-121 | Spoofing | `--operator` 값 | medium | mitigate | 값은 GitHub가 정하는 `github.actor`에서만 온다(워크플로 입력 칸이 아님) — dispatch는 저장소 쓰기 권한자만. CLI를 직접 돌리는 사람(Cloud Run Job 실행 권한자)은 값을 바꿀 수 있으나 그 권한 자체가 운영자 권한이다 |
| T-4.2-122 | Tampering | `--args` 쉼표 분리·셸 인젝션 | medium | mitigate | operator 형식 검사(GitHub 계정 규칙 — 쉼표·공백·대시 시작 거부) · 워크플로는 env로만 넘김(`run:`에 표현식 없음 — 단위 단언) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/account-cli.test.ts test/unit/deploy/workflows.test.ts` 초록
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/account-cli.test.ts test/integration/action-log.test.ts` 초록
- `pnpm lint && pnpm typecheck` 통과

</verification>

<success_criteria>

- 계정 해제가 해소와 같은 트랜잭션으로 행동 로그에 남는다(ROADMAP 04.2 기준 5)
- 실제 해제 경로의 기록에 워크플로를 실행한 계정이 남는다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 시그니처 변경 | `unlockAccount(viewer, email, opts?: { operator?; recordAction? })` — 해소 + 로그 한 트랜잭션 |
| CLI 인자(신규) | `unlock --operator <github 계정>`(필수) |
| 워크플로 | `account.yml` env `INPUT_OPERATOR: ${{ github.actor }}` · unlock `--args`에 `--operator` |
| 행동 로그 detail | `account_unlock` — `{ email, resolved, operator }` |
| 테스트 | `test/integration/account-cli.test.ts`(실제 CLI 해제) · `test/integration/action-log.test.ts`(해제 describe) · `test/unit/account-cli.test.ts` · `test/unit/deploy/workflows.test.ts` |
| 결정 | D-4222(CLI 경로 운영자 신원) · D-4220(해제 쪽 원자성) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-08-SUMMARY.md` when done
</output>
