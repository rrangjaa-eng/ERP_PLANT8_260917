---
phase: 04.2-notify-holidays-foundation
plan: 09
type: execute
wave: 3
depends_on: [04.2-07]
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - ui/shell/role-menu.ts
  - ui/shell/TopBar.tsx
  - ui/shell/MoreSheet.tsx
  - test/unit/ui/role-menu.test.ts
  - app/(app)/notifications/page.tsx
  - app/(app)/notifications/inbox-list.tsx
  - app/(app)/notifications/loading.tsx
  - app/(app)/notifications/notifications.module.css
  - test/e2e/notify-inbox.spec.ts
  - test/e2e/mobile-notify-inbox.spec.ts
autonomous: true
requirements: [NOTI-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SYSTEM.md가 UI-SPEC 수정 제안 #2~#8(§7-12 표기·계약 · §4-3 인셋 허용 · §6-8 다섯 항목 · §7-11 B1·B2와 우선순위 · §6-0 (a) 메뉴와 배지 · §6 레이아웃 한 줄 · §7-8 목록 행 삭제 = 확인 없이 지우고 결과 줄 `{대상} 삭제됨` + 3차 `되돌리기` — #8은 2026-09-25 /plan-design-review #1에서 생겼다)을 더하고 DECISIONS.md에 「Phase 04.2 알림함·공휴일 UI 계약」 한 항목이 먼저 들어간다 — #1(§6-10 공휴일)은 `ADMIN_MENUS`와 같은 커밋이어야 해서 04.2-11이 넣고, #4·#5 가운데 D-4217 부분(§7-11 B2 결과 불명 변형 · §6-8 `이메일` 줄 설정/결과 분리와 결과 꼬리)은 04.2-13이 덧붙인다"
    - "SYSTEM.md·DECISIONS.md 변경은 커밋 전에 Codex(GPT) 적대적 검토를 한 번 거치고, 화면은 독립 DOM 감사 옆에 Codex 적대적 디자인 검토를 한 번 거친다 — 두 출력 원문이 SUMMARY에 붙고, codex가 실행되지 않으면 그 사실과 오류를 적고 다른 검토로 대신하지 않는다(소유자 지시)"
    - "폰 더보기 시트의 계정 그룹 건수는 04.2-07의 `useUnreadCount()` 컨텍스트에서 온다 — `Shell`·`BottomTabs` props를 고치지 않는다"
    - "안 읽은 알림이 0건이면 배지를 그리지 않고, 알림이 0건이면 알림함은 「알림이 없습니다」 한 줄이다(SYSTEM §7-12)"
    - statement: "알림 제목·본문의 긴 한국어·특수문자·HTML 문자는 이스케이프되어 글자 그대로 보이고 알림함 줄 레이아웃을 깨지 않는다"
      verification: backstop
    - "UI S1-menu-item/empty — 안 읽은 알림이 0이면 메뉴 항목 라벨이 숫자 없이 `알림함`이다"
    - "UI S1-menu-item/loading — 메뉴 항목은 서버가 넘긴 건수로 즉시 렌더되고 메뉴를 열 때 추가 조회·로딩 표시가 없다"
    - "UI S1-menu-item/error — 건수 조회 실패 시 항목은 `알림함`(숫자 없음)으로 남고 링크는 동작한다"
    - "UI S1-menu-item/populated — PC 사용자 메뉴 순서가 `관리`(권한 시) · `알림함 N` · `내 정보` · `로그아웃`이고 폰 「더보기」 계정 그룹 순서가 `알림함 N` · `내 정보` · `설정` · `로그아웃`이다"
    - "UI S1-menu-item/partial — 관리 권한이 없는 계급은 PC 메뉴 첫 항목이 `알림함`이고 빈 자리가 없다"
    - "UI S1-menu-item/overflow — 메뉴 항목 수가 하나 늘어도 기존 행 높이·안쪽 포커스 링 그대로 목록이 세로로 늘어날 뿐 스크롤·잘림이 없다"
    - "UI S1-menu-item/zero-one-many — 라벨은 0이면 `알림함`, 1~99면 `알림함 N`, 100 이상이면 `알림함 99+`다"
    - "UI S1-menu-item/long-text — 라벨 최대 길이는 `알림함 99+`로 고정이라 메뉴 폭 안에서 한 줄이다"
    - "UI S1-inbox-list/empty — 알림 0건이면 `알림이 없습니다` 한 줄만 보이고 다음 한 수 버튼과 표 머리글이 없다(Copywriting S1 Empty)"
    - "UI S1-inbox-list/loading — 300ms를 넘는 로딩에서만 `--surface` 뼈대 3행이 보이고 스피너·반짝임이 없다. `더 보기` 요청 중에는 기존 행이 남고 버튼만 비활성이다"
    - "UI S1-inbox-list/error — 조회 실패 시 목록 자리에 Copywriting S1 Error 한 줄과 2차 `다시 시도`가 보인다. `더 보기` 실패는 기존 행을 두고 버튼 자리에 `--fs-sm --danger` `불러오지 못했습니다` + 3차 `다시 시도`(같은 커서)다. 읽음 처리 실패는 화면 오류 없이 배지만 남는다"
    - "UI S1-inbox-list/populated — 행은 날짜 그룹 머리글(올해 `MM-DD`) 아래 최신순이고, 날짜마다 `<tbody>` + 첫 행 `<th scope=\"rowgroup\" colSpan={2}>`다. 연 시점에 안 읽음이던 행만 첫 칸에 `inset 2px 0 0 var(--line-strong)`과 스크린 리더 전용 `안 읽음 · `가 있다"
    - "UI S1-inbox-list/partial — 이메일 발송이 실패한 행에만 내용 칸 두 번째 줄 `이메일 발송 실패`(`--fs-sm --muted`)가 있고 미설정·성공·결과 불명(`unknown`·`sending`) 행에는 없다"
    - "UI S1-inbox-list/overflow — 51건 이상이면 처음 50건 아래 3차 `더 보기 50건`이 있고 페이지 번호·가로 스크롤이 없다. 마지막 쪽을 불러오면 버튼이 사라지고 포커스가 새로 붙은 첫 행으로 간다"
    - "UI S1-inbox-list/zero-one-many — 0건 → EMPTY 한 줄, 1건 → 그룹 머리글 1 + 행 1, 여러 날 → 날짜별 그룹 머리글이 각각 하나"
    - statement: "UI S1-inbox-list/long-text — 40자 이상 알림 내용이 `keep-all`로 줄바꿈되고 말줄임되지 않으며 375px에서 가로 스크롤이 없다"
      verification: backstop
  artifacts:
    - path: "ui/shell/role-menu.ts"
      provides: "accountGroup 맨 앞 `알림함` · 알림함 경로 상수"
      exports: ["NOTIFICATIONS_HREF"]
    - path: "app/(app)/notifications/inbox-list.tsx"
      provides: "날짜 그룹 표 · 이메일 실패 보조 줄 · 키셋 더 보기 · 인셋"
    - path: "app/(app)/notifications/loading.tsx"
      provides: "뼈대 3행"
    - path: "docs/design/DECISIONS.md"
      provides: "`## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약`"
      contains: "Phase 04.2 알림함·공휴일 UI 계약"
  key_links:
    - from: "test/unit/ui/role-menu.test.ts"
      to: "docs/design/SYSTEM.md"
      via: "「계정」 그룹 문장 목록 = accountGroup 라벨 집합(`알림함` 포함)"
      pattern: "알림함"
    - from: "ui/shell/MoreSheet.tsx"
      to: "ui/shell/unread-count.tsx"
      via: "`useUnreadCount().count` + `unreadCountLabel`로 계정 그룹 `알림함 N`"
      pattern: "useUnreadCount"
    - from: "app/(app)/notifications/inbox-list.tsx"
      to: "app/(app)/notifications/actions.ts"
      via: "`더 보기 50건` → 마지막 행 커서로 `loadMoreInboxAction({ cursor })`(04.2-07)"
      pattern: "loadMoreInboxAction"
---

<objective>
**As a** 직원, **I want to** PC 사용자 메뉴와 폰 더보기에서 「알림함 N」을 찾고 알림함을 날짜별로 읽기를, **so that** 배지를 못 봐도 메뉴에서 알림함에 닿고, 알림이 많아도 날짜·실패 여부를 한눈에 읽는다.

(스토리는 ROADMAP 04.2 Goal 줄 「직원은 앱 알림함(미읽음 배지)…으로 알림을 받고」에서 끌어왔다.)

Purpose: NOTI-01의 나머지 — SYSTEM.md §7-12 계약 + UI-SPEC S1-b·S1-c(메뉴 항목·목록 다섯 상태·날짜 그룹·실패 보조 줄·더 보기와 그 상태), 그리고 코디네이터 메모 4의 SYSTEM.md 수정 제안 반영(#2~#7 · 2026-09-25 추가 #8). 화면·문서 검토에 Codex 적대적 검토를 넣는다(소유자 지시 · 체커 claude_md_compliance).

Output: DECISIONS 한 항목 + SYSTEM.md #2~#8 · 메뉴 항목 두 곳 · 알림함 목록 완성 · 로딩 뼈대 · 단위·E2E(PC·폰) · Codex 검토 두 번의 기록.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`. 화면은 `docs/design/SYSTEM.md`와 `docs/design/tokens.css`의 토큰만 쓴다. 화면 검증 순서는 CLAUDE.md 「싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`) + Codex 적대적 디자인 검토 → 수정 → 전체 게이트 한 번」. UI 완료 판정은 페이즈 끝 `/design-review` → `/qa`. 새 의존성 0개. `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-07-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — S1-b·S1-c(165~200행) · Copywriting S1 행 · 「SYSTEM.md 수정 제안」(536~547행) · 가정 U-1~U-10(Grep `U-1`)
# docs/design/SYSTEM.md — §6-0 (a)(Grep `### 6-0`) · §7-12(Grep `### 7-12`) · §4-3 인셋 허용 목록(Grep `인셋`) · §6-8 · §7-11 · §7-3 그룹 머리글 행 · §7-8 「계정」 그룹 문장
# docs/DESIGN.md §4 — 새 화면 절차
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 NOTI-01 empty(#9)·encoding(#10, backstop)이다. UI-SPEC UI Considerations의 S1-menu-item 여덟 줄과 S1-inbox-list 여덟 줄(backstop 하나)을 떠맡는다.

**계획 단계 결정(표면화):**

- **SYSTEM.md 반영 범위:** 코디네이터 메모 4는 일곱 수정 제안을 한 문서 작업으로 넣으라고 했다. #1(§6-10 표 · 「관리자 화면 10개 → 11개」)만 04.2-11로 뗀다 — UI-SPEC #1 스스로 「`ADMIN_MENUS`와 같은 커밋」을 요구하고, `role-menu.test.ts`가 §6-10 표와 `ADMIN_MENUS`를 원소 단위로 대조하며 `/admin/holidays` 페이지 파일의 존재까지 확인하기 때문이다. 나머지 #2~#7과 DECISIONS 항목은 이 플랜이다. 모두 **덧붙이거나 표기만 고친다**(Phase 4·04.1·04.3도 SYSTEM.md를 고친다). #4·#5 가운데 D-4217 부분 — §7-11 B2의 「결과 불명」 변형과 §6-8 `이메일` 줄의 설정/결과 분리·결과 꼬리 문장 — 은 04.2-13이 같은 절에 덧붙인다(그 결정의 DECISIONS 항목과 한 커밋). 〔2026-09-25 추가: UI-SPEC 62d1392 「SYSTEM.md 수정 제안」 #8(목록 행 삭제 — 같은 값으로 다시 넣을 수 있는 행은 확인 없이 지우고 결과 줄 `{대상} 삭제됨` + 3차 `되돌리기`, 두 단계 인라인 확인은 되돌릴 수 없는 삭제에 남김)은 /plan-design-review #1이 만든 제안인데 맡은 플랜이 없어 이 플랜이 #2~#7과 같은 문서 작업으로 §7-8에 덧붙인다 — 화면 구현은 04.2-12〕
- **#6과 accountGroup은 같은 커밋:** `role-menu.test.ts`가 SYSTEM.md 「계정」 그룹 문장 목록과 accountGroup 라벨을 대조하므로 §6-0 (a)·§7-8 문장(#6)과 `buildAccountGroup`의 `알림함` 항목은 Task 1의 한 커밋이다.
- **건수는 컨텍스트에서:** 04.2-07의 `UnreadCountProvider`가 레이아웃 전체를 감싸므로 `TopBar`(PC 메뉴)와 `MoreSheet`(폰 계정 그룹)은 `useUnreadCount()`를 직접 읽는다 — `Shell`·`BottomTabs`에 prop을 새로 뚫지 않는다. 라벨 문자열은 `알림함` 그대로 두고 건수는 표면이 `unreadCountLabel`로 붙인다(라벨 대조 테스트가 계급·건수와 무관하게 맞게). 하단 탭 자체에는 배지를 달지 않는다(UI-SPEC S1-a).
- **더 보기:** 04.2-07의 목록·열기는 `{ rows, hasMore }`를 돌려준다(저장소가 `limit + 1`행을 읽는다). `더 보기 50건`은 `hasMore`일 때만 — 정확히 50건이면 버튼이 없다(S1-inbox-list/overflow 「51건 이상이면」). 누르면 마지막 행의 `(createdAt, id)`로 `loadMoreInboxAction`. 〔2026-09-25 보완 — /plan-design-review #5(R14, UI-SPEC 62d1392 S1-c): 요청 중에는 이미 보이는 행을 그대로 두고 버튼만 비활성 · 실패하면 행은 그대로이고 버튼 자리에 `--fs-sm --danger` `불러오지 못했습니다` + 3차 `다시 시도` — 실패는 행을 덧붙이지 않으므로 `다시 시도`는 같은 마지막 행 커서로 다시 부르고 중복 행이 생기지 않는다 · 성공하면 새 행을 아래에 붙이고, 새 `hasMore`가 거짓이면 버튼이 사라지고 포커스가 새로 붙은 첫 행(`tabIndex={-1}`)으로 간다(버튼이 사라져 포커스를 잃지 않게)〕
- **Codex 적대적 검토 두 번(소유자 지시):** (1) 문서 — Task 2 끝, 커밋 전에 SYSTEM.md·DECISIONS.md 작업 트리 diff를 UI-SPEC 수정 제안과 대조 (2) 화면 — Task 3의 독립 DOM 감사 옆. 명령은 `codex exec -s read-only -C /home/user/ERP_PLANT8_260917 "<프롬프트>" < /dev/null`이고 출력 원문을 SUMMARY에 붙인다. high는 고치거나 고치지 않는 이유를 한 줄씩 적는다. `codex`가 없거나 0이 아닌 코드로 끝나면 명령·종료 코드·stderr 끝 20줄을 「Codex 검토 미실행」으로 적고 **다른 검토로 대신하지 않는다**.
</probe_fallback>

## Review Dispositions

| Codex # / 검사 | 처리 | 한 줄 |
|---|---|---|
| checker claude_md_compliance(plan 10 → 화면 플랜 전부) | 반영 | 문서 diff Codex 검토(Task 2) + 화면 Codex 적대적 디자인 검토(Task 3) · 출력 원문 SUMMARY · 미실행이면 보고만 |
| 19 (medium) — 폰 시트 | 반영 | 폰 계정 그룹 건수도 04.2-07의 이동 갱신 컨텍스트를 읽는다(`MoreSheet` → `useUnreadCount`) |
| 20 (medium) — 더 보기 | 반영 | `더 보기 50건`이 04.2-07 키셋 커서를 쓴다 · 51번째 행의 인셋이 열기 결과(`readAt === openedAt`)로 맞는다(Task 3 E2E) |

### Review Dispositions (/plan-design-review · 2026-09-25 — 결정 원장 중 이 플랜 몫)

| # | 처리 | 한 줄 |
|---|---|---|
| 5 (medium) · R14 | 반영 | `더 보기` 요청 중(기존 행 유지·버튼 비활성) · 실패(버튼 자리 `불러오지 못했습니다` + 3차 `다시 시도`, 같은 커서) · 마지막 쪽(버튼 사라짐 · 새로 붙은 첫 행 포커스) — E2E가 `page.route` 지연·끊기로 단언(Task 3) |
| 13 (low) | 반영 | 날짜마다 `<tbody>` + 첫 행 `th` rowgroup `colSpan={2}` — 열 묶음 머리글을 쓰지 않는다 · E2E·acceptance 단언 교체(Task 3) |
| 17 (low) — 받는 사람 쪽 | 반영 | 결과 불명 행(`unknown`·`sending`)에는 보조 줄 없음 — E2E 대상 상태에 `unknown`을 더한다(Task 3). 관리자 쪽 문구는 04.2-13 |
| 1 → SYSTEM.md 제안 #8 | 반영(주인 없는 제안을 맡음) | 목록 행 삭제 패턴(확인 없이 지우고 결과 줄 `되돌리기`)을 §7-8에 덧붙인다(Task 2 ③) — 04.2-12 화면과 같은 규칙 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — DECISIONS 한 항목 → SYSTEM.md 「계정」 문장(#6) → accountGroup 맨 앞 `알림함` → PC 메뉴 `알림함 1` 클릭 → /notifications (E2E로 끝까지)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, ui/shell/role-menu.ts, ui/shell/TopBar.tsx, test/unit/ui/role-menu.test.ts, test/e2e/notify-inbox.spec.ts</files>
  <read_first>
    - `docs/design/DECISIONS.md` 머리글 형식(`## YYYY-MM-DD — 제목`) · `test/unit/design-system-docs.test.ts`(DECISIONS 머리글 개수 단언 · SYSTEM.md 절 존재 단언)
    - `docs/design/SYSTEM.md` §6-0 (a) · §7-8 「계정」 그룹 문장(Grep `「계정」`)
    - `ui/shell/role-menu.ts` 12~60행(타입) · `buildAccountGroup` · `roleMenu`
    - `test/unit/ui/role-menu.test.ts` — SYSTEM.md 「계정」 그룹 문장 대조 · 「accountGroup은 계급과 무관」 · `ADMIN_MENU_KEYS`(이 플랜은 고치지 않는다)
    - `ui/shell/TopBar.tsx`·`ui/shell/unread-count.tsx`(04.2-07 결과)
    - UI-SPEC 「SYSTEM.md 수정 제안」 #6 · 가정 U-1~U-10
  </read_first>
  <behavior>
    - 단위: `roleMenu(ADMIN).accountGroup` 라벨 = `알림함` · `내 정보` · `설정` · `로그아웃` 순서이고 SYSTEM.md 「계정」 그룹 문장 목록과 같다 · 직원 계급도 같다
    - E2E(PC): 알림 1건 있는 직원 → 사용자 메뉴 첫 항목 `알림함 1`(관리 권한 없음) → 누르면 `/notifications` · 관리자 → `관리` 다음 `알림함 1`
  </behavior>
  <action>
① 단위 케이스를 `role-menu.test.ts`에, PC E2E 두 줄을 `notify-inbox.spec.ts`에 더한다(RED).

② **DECISIONS.md를 먼저**: 끝에 `## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약` 항목 하나 — UI-SPEC U-1~U-10 요약(각 한 줄: 고른 것 · 이유)과 「이탈이 아니라 SYSTEM.md가 비워 둔 자리를 채움」 한 줄, #1은 04.2-11이 `ADMIN_MENUS`와 함께 반영한다는 한 줄, 배지·폰 시트 건수는 레이아웃이 아니라 클라이언트 경계가 이동마다 갱신한다는 한 줄(04.2-07 D-4219). 같은 날짜 머리글을 세는 단언이 있으면 이 항목이 그것을 깨지 않는지 확인하고, 깨지면 단언을 고치지 말고 SUMMARY에 적은 뒤 멈춰 묻는다(다른 페이즈의 기록 개수 단언일 수 있다).

③ **SYSTEM.md #6(덧붙이기):** §6-0 (a) 사용자 메뉴 `관리 · 알림함 N · 내 정보 · 로그아웃` + 트리거 배지 문장, §7-8 「계정」 그룹 문장 목록 맨 앞에 `알림함`. 기존 문장을 다시 쓰지 않는다.

④ **코드:** `role-menu.ts` — `export const NOTIFICATIONS_HREF = "/notifications"` · `buildAccountGroup` 맨 앞에 `{ kind: "link", label: "알림함", href: NOTIFICATIONS_HREF }`. `TopBar`: PC 메뉴 항목 중 `href === NOTIFICATIONS_HREF`인 항목 라벨 뒤에 `unreadCountLabel(useUnreadCount().count)`가 있으면 공백 하나 + 그 글자.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts test/unit/design-system-docs.test.ts test/unit/ui/system-md-compliance.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — accountGroup과 SYSTEM.md 「계정」 문장이 어긋났거나(같은 커밋 규칙 위반), accountGroup이 계급에 따라 갈라졌거나, SYSTEM.md 절 머리글을 깨뜨렸거나, DECISIONS 머리글 단언이 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/user-menu.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — PC 메뉴 순서가 UI-SPEC S1-b와 다르거나, `알림함 1`이 `/notifications`로 가지 않거나, 기존 사용자 메뉴 E2E가 새 항목으로 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약" docs/design/DECISIONS.md`가 1이다
    - `git log -1 --name-only`(이 Task 커밋)에 `docs/design/SYSTEM.md`와 `ui/shell/role-menu.ts`가 함께 있다
    - 단위·E2E 초록(`CI=true`)
  </acceptance_criteria>
  <done>PC 메뉴에서 `알림함 N`으로 알림함에 닿고, 그 계약이 DECISIONS·SYSTEM.md에 먼저 기록된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 폰 더보기 계정 그룹 `알림함 N` + SYSTEM.md 나머지 수정 제안(#2·#3·#4·#5·#7·#8) + 문서 Codex 적대적 검토</name>
  <files>ui/shell/MoreSheet.tsx, docs/design/SYSTEM.md, test/e2e/mobile-notify-inbox.spec.ts, test/unit/ui/role-menu.test.ts</files>
  <read_first>
    - `ui/shell/MoreSheet.tsx` — 계정 그룹 렌더 · 「내 차례 N」 라벨 붙이는 방식
    - `test/e2e/mobile-shell.spec.ts` — 폰 뷰포트·시트 열기
    - `docs/design/SYSTEM.md` §7-12 · §4-3 · §6-8 · §7-11 · §7-8(Grep `### 7-8`) · §6 레이아웃 목록
    - UI-SPEC 「SYSTEM.md 수정 제안」 #2·#3·#4·#5·#7·#8(62d1392)
  </read_first>
  <behavior>
    - 단위: `unreadCountLabel`을 쓰는 메뉴 라벨 조립이 0 → `알림함` · 1 → `알림함 1` · 99 → `알림함 99` · 100 → `알림함 99+`(`role-menu.test.ts` — 04.2-07 표기 함수와 메뉴 라벨의 결합)
    - E2E(폰 375): 알림 1건 → 더보기 시트 계정 그룹 `알림함 1` · `내 정보` · `설정` · `로그아웃` 순서 · 항목 높이가 이웃 항목과 같고 시트에 스크롤·잘림이 없다
    - E2E(폰): 건수 조회 실패 흉내는 할 수 없으므로 0건 사용자로 `알림함`(숫자 없음) · 링크 동작
  </behavior>
  <action>
① behavior를 먼저 쓴다(RED). 폰 스펙 파일 이름 규칙 `mobile-*.spec.ts`.

② `MoreSheet.tsx`: 계정 그룹의 `href === NOTIFICATIONS_HREF` 항목 라벨 뒤에 `unreadCountLabel(useUnreadCount().count)`가 있으면 공백 하나 + 그 글자. `BottomTabs`·`Shell`은 고치지 않는다.

③ **SYSTEM.md(덧붙이거나 표기만):** #2 §7-12 제목의 「구현은 Phase 7」 → 「구현은 Phase 04.2」 + S1-a(배지 자리·모양·`99+`·실제 수 스크린 리더 이름 · 경로 이동 때 다시 받기 — 받는 동안·실패 때 이전 값 유지) · S1-b(메뉴 순서) · S1-c(인셋 `--line-strong` · 이메일 실패 보조 줄(결과 불명 행에는 없음) · 날짜 `<tbody>` rowgroup 머리글 · 50건 더 보기와 그 상태) · S1-d(열면 모두 읽음 — 서버가 연 순간의 스냅샷만) 계약 문장 · #3 §4-3 인셋 허용 목록에 `inset 2px 0 0 --line-strong`(알림함 안 읽은 행) 한 줄 · #4 §6-8 세 항목 → 다섯 항목(`알림 발송`·`이메일`) + 배너가 겹치면 DB 커넥션이 먼저(`이메일` 줄의 설정/결과 분리·결과 꼬리 문장은 04.2-13) · #5 §7-11 예시에 B1·B2(B2 결과 불명 변형은 04.2-13) + 우선순위 「경고 > 안내, 같은 등급이면 그 화면의 조건이 먼저」 + 「관리」 인덱스는 여러 관리자 조건을 받는 자리 · #7 §6 레이아웃 목록에 「공휴일 화면은 새 템플릿이 아니다 — §6-1 + §7-15 조립」 한 줄 · #8 §7-8 끝에 목록 행 삭제 한 항목 — 같은 값으로 다시 넣을 수 있는 행의 삭제는 확인 없이 지우고 표 위 결과 줄 `{대상} 삭제됨` + 3차 `되돌리기`(화면 이동이 없어 토스트가 아니다, §7-6), 두 단계 인라인 확인은 되돌릴 수 없는 삭제에 남긴다(UI-SPEC 62d1392 #8 · /plan-design-review #1). 기존 문장을 다시 쓰지 않는다.

④ **문서 Codex 적대적 검토(커밋 전):** `codex exec -s read-only -C /home/user/ERP_PLANT8_260917 "적대적 문서 검토. git diff -- docs/design/SYSTEM.md docs/design/DECISIONS.md 가 .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md 「SYSTEM.md 수정 제안」 #2~#8과 가정 U-1~U-10을 빠짐없이, 덧붙이기·표기 수정만으로 반영했는지(#4·#5의 결과 불명·이메일 설정/결과 분리 부분은 04.2-13 몫이라 없어야 한다), 기존 규칙과 모순되거나 다른 페이즈의 문장을 다시 쓴 곳이 없는지, #8이 §7-8의 기존 확인 규칙(D-25)과 부딪치지 않는지, #1(§6-10)이 섞여 들어가지 않았는지 심각도(high/medium/low)와 파일:행으로 보고하라. 파일을 고치지 마라." < /dev/null` — `<probe_fallback>` 「Codex 적대적 검토 두 번」의 기록·미실행 규칙대로.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts test/unit/design-system-docs.test.ts test/unit/ui/system-md-compliance.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 메뉴 라벨 경계(0·99·100)가 틀렸거나, SYSTEM.md 절 머리글·단언 문장을 깨뜨렸다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/mobile-notify-inbox.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 폰 시트 계정 그룹 순서·건수가 다르거나, 시트가 스크롤·잘림을 보이거나, 기존 폰 셸·키보드 E2E가 새 항목으로 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "구현은 Phase 7" docs/design/SYSTEM.md`에 §7-12 제목 줄이 없다
    - `grep -c "삭제됨" docs/design/SYSTEM.md`와 `grep -c "rowgroup" docs/design/SYSTEM.md`가 각각 1 이상이다(#8 · #2 S1-c)
    - `git diff --numstat HEAD -- docs/design/SYSTEM.md`의 삭제 줄 수가 추가 줄 수보다 훨씬 적다(표기 수정 몇 줄만 — 두 수를 SUMMARY에 적는다)
    - `git diff --stat HEAD -- ui/shell/BottomTabs.tsx ui/shell/Shell.tsx` 출력이 비어 있다
    - SUMMARY에 「Codex 문서 검토」 절(출력 원문 또는 「미실행」 + 오류)이 있다
  </acceptance_criteria>
  <done>폰에서도 `알림함 N`이 보이고, SYSTEM.md가 알림함·배너 계약과 목록 행 삭제 되돌리기 규칙(#8)을 담으며, 문서 변경이 Codex 검토를 거쳤다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 알림함 목록 완성 — 날짜 그룹 머리글(날짜별 tbody) · 이메일 발송 실패 보조 줄 · 키셋 더 보기(요청 중·실패·마지막 쪽) · 로딩 뼈대 · 빈/오류 상태 · HTML 글자 · 폰 375 · Codex 디자인 검토</name>
  <files>app/(app)/notifications/inbox-list.tsx, app/(app)/notifications/page.tsx, app/(app)/notifications/loading.tsx, app/(app)/notifications/notifications.module.css, test/e2e/notify-inbox.spec.ts, test/e2e/mobile-notify-inbox.spec.ts</files>
  <read_first>
    - `app/(app)/projects/loading.tsx` — 뼈대 규약(제목만 · `aria-hidden` · 부제 없음 이유)
    - `ui/list-empty/ListEmpty.tsx` — `tone="error"` · `action` 생략(DECISIONS 2026-09-20)
    - `docs/design/SYSTEM.md` §7-3 그룹 머리글 행 · §2-4 날짜 표기 · §7-12 다섯 상태
    - `domain/holidays/business-day.ts`(04.2-02 — `formatKstMinute`·`formatKstTime`·`toKstDate`)
    - `app/(app)/notifications/*`(04.2-07 결과)
  </read_first>
  <behavior>
    - E2E: 알림 0건 → `알림이 없습니다` 한 줄 · 표 머리글·버튼 없음
    - E2E: 서로 다른 KST 날짜 두 날에 걸친 알림 → 날짜 그룹 머리글 둘(올해면 `MM-DD`) · 행 시각 `HH:MM` · 1건이면 그룹 머리글 1 + 행 1 · 표 안 `tbody`가 날짜 수만큼이고 각 `tbody`의 첫 행이 `th[scope="rowgroup"][colspan="2"]` 하나를 가지며 `th[scope="colgroup"]`는 0개(디자인 리뷰 #13)
    - E2E: `email_status = 'failed'` 행에만 `이메일 발송 실패` 보조 줄 · `pending`/`sending`/`unknown`/`sent`/`skipped_no_smtp` 행에는 없다(결과 불명 행에는 아무것도 쓰지 않는다 — 디자인 리뷰 #17)
    - E2E: 정확히 50건 → 행 50개 · `더 보기 50건` 없음 / 51건 → 행 50개 + 3차 `더 보기 50건` → 누르면 51번째 행이 보이고, 그 행도 이번 열기가 읽음 처리한 행이라 인셋이 있다
    - E2E(`더 보기` 상태 — 디자인 리뷰 #5 · R14): 51건 · `page.route`로 `더 보기` 요청(`Next-Action` 머리글이 있는 POST)을 1초 붙잡는 동안 행 50개 그대로 + `더 보기 50건` 비활성 → 끝나면 51번째 행이 붙고 버튼이 사라지며 포커스가 51번째 행(`toBeFocused`) / 51건 · 같은 요청을 `route.abort()`로 끊으면 행 50개 그대로 + 버튼 자리에 `불러오지 못했습니다` + 3차 `다시 시도`(`더 보기 50건`은 없다) → 라우트를 풀고 `다시 시도` → 51번째 행 하나가 붙고 행 id가 겹치지 않으며(같은 커서) 버튼이 사라지고 포커스가 51번째 행
    - E2E: 내용에 `<b>굵게</b> & "따옴표"`가 든 알림 → 화면에 글자 그대로 보이고 `b` 요소가 생기지 않는다
    - E2E(폰 375): 40자 이상 내용이 줄바꿈되고 문서 가로 스크롤 폭이 뷰포트를 넘지 않는다(`scrollWidth <= clientWidth`)
  </behavior>
  <action>
① behavior를 E2E에 먼저 쓴다(RED). 이메일 상태·생성 시각을 다르게 만들려면 tick 뒤 테스트 DB에서 해당 행만 고치는 준비 도우미를 스펙 안에 둔다(프로덕션 코드에 테스트용 경로를 만들지 않는다).

② `inbox-list.tsx`: 행을 KST 날짜(`toKstDate`)로 묶어 날짜마다 `<tbody>` 하나, 그 첫 행을 그룹 머리글 행(`<tr>` 안 `<th scope="rowgroup" colSpan={2}>` — 행 묶음의 머리글, 디자인 리뷰 #13 · UI-SPEC S1-c · 올해 `MM-DD` · 다른 해 `YYYY-MM-DD`)으로 둔다 · 시각 칸 `formatKstTime`(`tabular-nums`, nowrap, 오른쪽 정렬) · 내용 칸 줄바꿈 허용(`word-break: keep-all; overflow-wrap: anywhere`) · `emailStatus === "failed"`면 내용 칸 둘째 줄 `이메일 발송 실패`(`--fs-sm`·`--muted`) · `hasMore`일 때만 3차 `더 보기 50건`(누르면 마지막 행 커서로 `useAction(loadMoreInboxAction)` → 행 덧붙임 · 새 `hasMore`). `더 보기` 상태(`<probe_fallback>` 「더 보기」 2026-09-25 보완 · 디자인 리뷰 #5): 요청 중(`isPending`) 이미 보이는 행은 그대로이고 버튼만 `disabled` · 결과가 `serverError`·`validationErrors`이거나 요청이 던지면 행은 그대로이고 버튼 대신 그 자리에 `--fs-sm --danger` `불러오지 못했습니다` + 3차 `다시 시도`(같은 마지막 행 커서로 같은 액션을 다시 부른다 — 실패는 행을 덧붙이지 않으므로 중복이 생기지 않는다, 요청 중에는 `다시 시도`도 비활성) · 성공하면 실패 줄을 치우고 새 행을 붙이며, 새 `hasMore`가 거짓이면 버튼을 렌더하지 않고 새로 붙은 첫 행(`<tr tabIndex={-1}>` + ref)으로 포커스를 옮긴다. 인셋 규칙은 04.2-07 그대로. 내용은 React 텍스트 노드로만 넣는다(HTML 주입 경로 없음 — 엣지 #10 backstop).

③ 상태: 0건 → `ListEmpty message="알림이 없습니다"`(action 생략, 표 없음) · 조회 실패 → 페이지가 잡아 `ListEmpty tone="error" message="불러오지 못했습니다 · 다시 시도"` + 2차 `다시 시도`(새로 고침) · `loading.tsx`(신규) — `PageHeader` `알림함` + `--surface` 뼈대 3행(`aria-hidden`), 스피너·반짝임 없음(projects 선례). 읽음 처리 실패는 04.2-07 그대로 화면에 띄우지 않는다.

④ `notifications.module.css`(신규): 인셋 `box-shadow: inset 2px 0 0 var(--line-strong)` · 그룹 머리글 `--fs-sm --muted` 600 · 위 간격 토큰 · 아래 1px `--line-strong` — 모두 tokens.css 토큰만.

⑤ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`, S1-menu-item·S1-inbox-list 16줄 판정 — backstop 포함) → **Codex 적대적 디자인 검토**: `codex exec -s read-only -C /home/user/ERP_PLANT8_260917 "적대적 디자인 검토. 기준: docs/design/SYSTEM.md, docs/design/tokens.css, .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md 의 S1-b·S1-c와 Copywriting S1. 대상: git diff origin/main...HEAD -- ui/shell \"app/(app)/notifications\". 토큰 밖 값·계약 문구 불일치·상태 누락(빈·로딩·오류·부분·넘침·50/51건)·표 접근성(caption·날짜별 tbody·rowgroup scope)·더 보기 상태(요청 중 비활성·실패 줄과 같은 커서 다시 시도·마지막 쪽 포커스)·375px 넘침·HTML 해석 경로를 심각도(high/medium/low)와 파일:행으로 보고하라. 코드를 고치지 마라." < /dev/null` — `<probe_fallback>` 기록·미실행 규칙대로 → 수정 → 전체 게이트 한 번(아래 verify).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>stylelint(토큰 밖 값)·eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/mobile-notify-inbox.spec.ts test/e2e/user-menu.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 빈·오류 문구가 카피 계약과 다르거나, 그룹 머리글·보조 줄·더 보기가 빠졌거나 날짜 묶음이 `tbody` + rowgroup 머리글이 아니거나 50건에 버튼이 보였거나, 51번째 행 인셋이 틀렸거나, `더 보기` 요청 중 행이 사라지거나 버튼이 눌리거나, 실패 뒤 `불러오지 못했습니다`·`다시 시도`가 없거나 `다시 시도`가 행을 겹쳐 붙였거나, 마지막 쪽 뒤 포커스가 새로 붙은 첫 행이 아니거나, HTML 글자가 요소로 해석됐거나, 375px에서 가로 스크롤이 생겼거나, axe 위반이 생겼다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "dangerouslySetInnerHTML" "app/(app)/notifications"` 출력이 비어 있다
    - `grep -c 'scope="rowgroup"' "app/(app)/notifications/inbox-list.tsx"`가 1 이상이고 `grep -c 'scope="colgroup"' "app/(app)/notifications/inbox-list.tsx"`가 0이다(#13)
    - `grep -c "불러오지 못했습니다" "app/(app)/notifications/inbox-list.tsx"`와 `grep -c "tabIndex={-1}" "app/(app)/notifications/inbox-list.tsx"`가 각각 1 이상이다(#5 — 더 보기 실패 줄 · 마지막 쪽 포커스 대상)
    - 카피 네 문구(`알림이 없습니다` · `불러오지 못했습니다 · 다시 시도` · `이메일 발송 실패` · `더 보기 50건`)가 화면 코드에 글자 그대로 한 번씩 있다(Grep)
    - SUMMARY에 독립 DOM 감사 결과(backstop은 감사 판정 또는 human_needed)와 「Codex 디자인 검토」 절(출력 원문 또는 「미실행」 + 오류)이 있다
  </acceptance_criteria>
  <done>알림함이 §7-12·UI-SPEC S1의 다섯 상태와 날짜 그룹(날짜별 tbody + rowgroup)·실패 보조 줄·더 보기(요청 중·실패·마지막 쪽 포커스)를 갖추고 폰에서도 가로 스크롤 없이 읽히며, 화면 검토에 Codex 검토가 들어갔다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 알림 내용(조건 종류가 만든 문장) → 화면 | Phase 7 조건 종류가 문서 이름 등 사용자 입력을 문장에 넣을 수 있다 |
| 브라우저 → `loadMoreInboxAction` 커서 | 04.2-07이 검증 — 이 플랜은 마지막 행 값만 넘긴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-56 | Tampering | 알림 내용 XSS | medium | mitigate | 텍스트 노드로만 렌더 · `dangerouslySetInnerHTML` 금지(Grep) · E2E가 HTML 글자를 그대로 보는지 단언 |
| T-4.2-57 | Information Disclosure | 메뉴 건수 | low | mitigate | 건수는 04.2-07 컨텍스트(본인 `countMyUnread`)에서만 온다 — 메뉴가 따로 조회하지 않는다 |
| T-4.2-58 | Repudiation | SYSTEM.md 계약 변경 | low | mitigate | DECISIONS 항목을 먼저 쓰고, 문서 diff를 Codex 적대적 검토에 거친 뒤 커밋(Task 2) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`
- 단위: `pnpm vitest run --project unit test/unit/ui test/unit/design-system-docs.test.ts test/unit/page-auth-guard.test.ts`
- 독립 DOM 감사(별도 에이전트, `CI=true`) + Codex 적대적 디자인 검토(Task 3 ⑤ — 미실행이면 보고만) → 수정 → E2E 한 번: `CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/mobile-notify-inbox.spec.ts test/e2e/user-menu.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/a11y.spec.ts`
- 문서 Codex 적대적 검토(Task 2 ④)는 커밋 전
- 페이즈 끝 UI 판정은 `/design-review` + Codex 적대적 디자인 검토 → `/qa`(04.2-14 Post-build — 이 플랜이 대신하지 않는다)

</verification>

<success_criteria>

- PC·폰 두 메뉴가 `알림함 N`을 UI-SPEC 순서로 보인다
- 알림함이 §7-12 + UI-SPEC S1 계약(인셋·날짜별 tbody rowgroup 머리글·실패 보조 줄·더 보기와 그 상태·다섯 상태)을 지킨다
- SYSTEM.md 수정 제안 #2~#8과 DECISIONS 항목이 반영됐고(#1은 04.2-11, #4·#5의 D-4217 부분은 04.2-13), 문서·화면 모두 Codex 적대적 검토를 거쳤다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 화면 | `/notifications` 완성(`inbox-list.tsx` · `page.tsx` · `loading.tsx` · `notifications.module.css`) |
| 상수 | `NOTIFICATIONS_HREF`(`ui/shell/role-menu.ts`) · accountGroup 맨 앞 `알림함` |
| 셸 | PC 메뉴·폰 계정 그룹 `알림함 N`(`TopBar`·`MoreSheet`이 `useUnreadCount` 사용 — `Shell`·`BottomTabs` 불변) |
| 문서 | `docs/design/DECISIONS.md` `## 2026-09-24 — Phase 04.2 알림함·공휴일 UI 계약` · `docs/design/SYSTEM.md` 수정 제안 #2~#8 |
| 테스트 | `test/e2e/notify-inbox.spec.ts`(확장 — 날짜별 tbody·결과 불명 행·더 보기 요청 중·실패·마지막 쪽 포커스) · `test/e2e/mobile-notify-inbox.spec.ts`(신규) · `test/unit/ui/role-menu.test.ts`(확장) |
| 검토 기록 | SUMMARY 「Codex 문서 검토」 · 「Codex 디자인 검토」 절 |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-09-SUMMARY.md` when done
</output>
