---
phase: 04.2-notify-holidays-foundation
plan: 09
type: execute
wave: 4
depends_on: ["04.2-07", "04.2-08"]
files_modified:
  - repositories/notifications.ts
  - domain/system-status/index.ts
  - domain/holidays/admin.ts
  - app/(app)/admin/system-status/page.tsx
  - app/(app)/admin/page.tsx
  - test/unit/system-status.test.ts
  - test/integration/admin-banners.test.ts
  - test/e2e/system-status.spec.ts
  - test/e2e/holidays.spec.ts
autonomous: true
requirements: [NOTI-02, NOTI-04, ADMN-11]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "관리자 시스템 상태 화면의 기존 세 줄 아래에 `알림 발송`·`이메일` 두 줄이 더해진다 — 마지막 tick 시각·결과(18A)와 이메일 채널 상태(D-711)"
    - "다음 해(또는 올해) 공휴일이 확정 전이면 `admin.holidays` write 권한이 있는 사람에게 「관리」 인덱스에서 안내 배너 `{연도}년 공휴일 확정 전 · 규칙으로 만든 후보로 계산 중` + 3차 링크 `{연도}년 공휴일 검토`가 보인다 — 통합: 다음 해 미확정 → 관리자 배너(D-705)"
    - "이메일을 시도한 마지막 tick에 실패가 있으면 `admin.system-status` view 권한이 있는 사람에게 경고 배너 `이메일 발송 실패 N건 · YYYY-MM-DD HH:MM · 알림함으로만 전달됨`이 보이고, 다음 이메일 발송 tick에 실패가 0이면 사라진다(재시도 0이라 「미해결 실패 전체」를 조건으로 삼지 않는다 — UI-SPEC U-6)"
    - "배너 자리는 한 화면에 하나다 — `/admin`은 이메일 실패(경고)가 공휴일 확정 요청(안내)보다 앞서고, `/admin/system-status`는 DB 커넥션 경고가 이메일 실패보다 앞서며, `/admin/holidays`와 그 밖의 화면에는 이 두 배너가 없다(UI-SPEC S3)"
    - "그날 새 알림이 없는 사람에게는 메일을 보내지 않고, SMTP 환경 변수 4개 중 하나라도 비면 메일 0통·관리자 시스템 상태 「미설정」이다"
    - "UI S3-banners/empty — 두 조건이 모두 없으면 배너 요소가 렌더되지 않는다"
    - "UI S3-banners/loading — 배너는 서버 렌더에서 조건이 확인된 뒤에만 나오고 로딩 표시가 없다"
    - "UI S3-banners/error — 조건 조회가 실패하면 배너를 띄우지 않고 화면은 정상 렌더된다(서버 로그). 배너 자체는 ERROR 상태를 갖지 않는다(§7-11)"
    - "UI S3-banners/partial — `/admin`에서 두 조건이 함께면 경고 B2만 보이고, `/admin/system-status`에서는 DB 커넥션 배너가 B2를 밀어낸다. 한 화면 배너는 최대 1개다"
    - statement: "UI S3-banners/overflow — 폰 375px에서 배너 문구와 3차 링크가 줄바꿈되어 가로 스크롤 없이 보이고 링크 터치 목표가 44px 이상이다"
      verification: backstop
    - "UI S3-banners/long-text — 배너 글자는 Copywriting B1·B2 고정 문구 + 연도·건수·시각만 바뀌며 닫기 버튼이 없다"
    - "UI S4-status-rows/empty — tick 기록이 없으면 `알림 발송` 값이 `기록 없음 — 첫 알림 발송 전`, SMTP 값이 하나라도 비면 `이메일` 값이 muted 태그 `미설정`이다"
    - "UI S4-status-rows/loading — 시스템 상태는 서버 렌더라 새 두 줄도 기존 세 줄과 함께 나오고 별도 로딩 표시가 없다"
    - "UI S4-status-rows/error — 마지막 tick 기록 조회가 실패하면 그 줄 값만 muted 태그 `확인 불가`이고 나머지 줄은 정상이다"
    - "UI S4-status-rows/populated — 정상 tick이면 `알림 발송` 값이 `YYYY-MM-DD HH:MM · 알림 N건 · 중복 건너뜀 N건 · 남음 N건`, SMTP 설정 시 `이메일` 값이 `사용 중 · {SMTP_FROM}`이다"
    - "UI S4-status-rows/partial — 마지막 이메일 발송 tick에 실패가 있으면 `이메일` 값 뒤에 ` · 실패 N건 (시각)`이 붙고 `실패 N건`만 `--danger` 글자다"
    - "UI S4-status-rows/overflow — 새 줄은 single-column 720 `dl` 안에서 값이 줄바꿈되고 라벨 폭은 96(PC)/84(폰)이다"
    - "UI S4-status-rows/zero-one-many — 건수는 0도 `0건`으로 쓴다(`남음 0건`). 비영업일 tick은 `비영업일 · 보내지 않음`이다"
    - "UI S4-status-rows/long-text — 긴 SMTP_FROM 주소는 값 칸 안에서 `overflow-wrap: anywhere`로 줄바꿈되고 비밀번호·호스트·사용자는 표시되지 않는다"
  artifacts:
    - path: "domain/system-status/index.ts"
      provides: "시스템 상태에 `notify`·`email` 두 항목 · 이메일 실패 배너 판정"
      exports: ["SystemStatus", "StatusDeps", "getSystemStatus", "emailFailureBanner"]
    - path: "domain/holidays/admin.ts"
      provides: "공휴일 확정 요청 배너 판정(올해·다음 해 중 가장 이른 미확정 해)"
      exports: ["holidayConfirmationBanner"]
    - path: "repositories/notifications.ts"
      provides: "마지막 tick 실행 · 이메일을 시도한 마지막 tick 실행 읽기"
      exports: ["findLastTickRun", "findLastEmailTickRun"]
    - path: "app/(app)/admin/page.tsx"
      provides: "「관리」 인덱스 배너 자리(B2 → B1)"
    - path: "app/(app)/admin/system-status/page.tsx"
      provides: "`알림 발송`·`이메일` 줄 · 배너 자리(DB 커넥션 → B2)"
  key_links:
    - from: "app/(app)/admin/page.tsx"
      to: "domain/system-status/index.ts"
      via: "`emailFailureBanner(viewer)`가 있으면 B2, 없을 때만 `holidayConfirmationBanner(viewer)`로 B1"
      pattern: "emailFailureBanner"
    - from: "domain/system-status/index.ts"
      to: "repositories/notifications.ts"
      via: "`findLastTickRun`(알림 발송 줄) · `findLastEmailTickRun`(`email_sent + email_failed > 0`인 마지막 실행 — 이메일 줄 실패 꼬리와 B2)"
      pattern: "findLastEmailTickRun"
    - from: "domain/system-status/index.ts"
      to: "lib/email/sender.ts"
      via: "`smtpConfigFromEnv()`가 null이면 `미설정`, 아니면 `from`만 쓴다"
      pattern: "smtpConfigFromEnv"
---

<objective>
**As a** 시스템 관리자, **I want to** 시스템 상태에서 마지막 알림 발송과 이메일 채널 상태를 보고, 「관리」 첫 화면에서 공휴일 확정 요청과 이메일 발송 실패를 배너로 알아채기를, **so that** 스케줄러가 멈췄거나 SMTP가 실패하거나 다음 해 공휴일이 후보 상태로 계산되는 것을 사람이 먼저 안다.

(스토리는 ROADMAP 04.2 기준 3·4(「관리자 시스템 상태 화면에 마지막 tick 시각·결과」, 「발송 실패는 알림함과 관리자 배너에 남는다」)와 CONTEXT D-705 앞부분에서 끌어왔다. ROADMAP Goal 줄은 사용자 스토리 형식이 아니다 — 형식을 맞추려면 `/gsd mvp-phase 04.2`.)

Purpose: 18A(마지막 tick 시각·결과) · D-711(이메일 「미설정」) · NOTI-02(발송 실패 관리자 배너) · D-705 앞부분(다음 해 미확정 → 관리자 배너). UI-SPEC S3·S4.

Output: 시스템 상태 두 줄 · 배너 판정 함수 둘 · 두 화면의 배너 자리(우선순위) · 단위·통합·E2E.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 Skill 도구로 호출한다. 기존 `Banner`·`KvList`·`StatusTag`만 쓰고 tokens.css 토큰 밖 값을 만들지 않는다. SYSTEM.md §6-8·§7-11 문장은 04.2-06이 이미 반영했다 — 여기서는 고치지 않는다. 화면 검증 순서 「싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`) → 수정 → 전체 게이트 한 번」. 새 의존성 0개. `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-07-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-08-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — S3(321~343행) · S4(345~358행) · Copywriting 배너 B1·B2 · 시스템 상태 라벨·값 행 · 가정 U-4·U-5·U-6
# docs/design/SYSTEM.md §6-8 · §7-11(04.2-06이 더한 B1·B2·우선순위 문장)
</context>

<probe_fallback>
이 플랜이 떠맡는 UI-SPEC 줄은 S3-banners 6줄(backstop 하나)과 S4-status-rows 8줄이다. 엣지 NOTI-02 empty(#15)는 04.2-08이 발송 절반을 증명했고 여기서 「관리자 시스템 상태 「미설정」」 화면 절반을 증명한다(같은 문장을 truth로 다시 든다). #18의 「관리자 배너에 실패가 보인다」 절반은 B2가 덮는다.

**계획 단계 결정(표면화):**

- **「마지막 tick」 두 가지:** `알림 발송` 줄은 `notify_tick_runs`의 가장 최근 실행(잠금을 못 잡은 tick은 04.2-01 D-4204대로 기록이 없어 덮어쓰지 않는다). 이메일 실패 꼬리와 B2는 `email_sent + email_failed > 0`인 가장 최근 실행을 읽는다 — SMTP 미설정 tick·비영업일 tick·보낼 사람이 없던 tick은 「이메일을 시도한 tick」이 아니므로 B2 조건을 풀지도 새로 걸지도 않는다(UI-SPEC U-6). N은 실패한 메일 수(04.2-08 D-4213).
- **B1 대상 해(UI-SPEC U-4):** 올해·다음 해 가운데 확정 기록이 없는 가장 이른 해. 확인하는 사람은 `admin.holidays` **write** 권한자뿐(행동할 수 있는 사람만). B2는 `admin.system-status` **view** 권한자. 판정 함수는 권한이 없으면 null을 돌려준다(던지지 않는다 — 인덱스는 다른 권한으로 들어온 사람도 본다). 조회가 실패하면 `log.warn`(`admin.banner_failed`, `{ banner, message }`) 뒤 null이다(S3/error).
- **배너 자리 코드:** `/admin`은 B2가 있으면 B2만, 없으면 B1. `/admin/system-status`는 기존 DB 커넥션 배너가 있으면 그것만, 없으면 B2(링크 `시스템 상태 보기`는 뺀다). 우선순위는 코드 순서 한 줄로 두고 추상화를 만들지 않는다.
- **E2E 공유 상태:** Playwright 워커가 파일 단위로 병렬이고 두 프로젝트(PC·폰)가 한 테스트 DB를 쓴다. 공휴일 확정 기록과 이메일 실패 실행 기록을 바꾸는 E2E는 04.2-07의 `holidays.spec.ts` 한 파일(파일 안은 직렬)에 모은다. B2 준비는 테스트 DB에 실패 실행 한 행을 직접 넣고 끝에서 지운다. 그래서 `system-status.spec.ts`의 기존 「배너는 없다」 단언을 **DB 커넥션 배너 문구가 없다**로 좁힌다(다른 파일의 B2 준비와 겹쳐도 깨지지 않게 — 단언의 뜻은 그대로다). 올해(Y) 확정은 E2E가 건드리지 않는다(04.2-07은 Y+1만 확정한다) — 그래서 「관리」 인덱스의 B1은 E2E 동안 늘 `{Y}년`이다. 배너가 사라지는 조건은 통합에서 증명한다.
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — tick 실행 기록 → 저장소 → `getSystemStatus` → 시스템 상태 화면 `알림 발송`·`이메일` 두 줄 (E2E로 끝까지)</name>
  <files>repositories/notifications.ts, domain/system-status/index.ts, app/(app)/admin/system-status/page.tsx, test/unit/system-status.test.ts, test/e2e/system-status.spec.ts</files>
  <read_first>
    - `domain/system-status/index.ts` 전체 — `SystemStatus` 합 타입 · `StatusDeps` 주입 · 실패를 `unavailable`로 바꾸는 방식
    - `app/(app)/admin/system-status/page.tsx` 전체 — `KvList` 항목 · `StatusTag kind="muted"` `확인 불가` · 커넥션 배너
    - `test/unit/system-status.test.ts` 28~100행 — deps 가짜로 `getSystemStatus`를 단언하는 모양
    - `test/e2e/system-status.spec.ts` 19~45행 — 시스템 관리자 화면 단언 · 숨은 `role=alert` 리전 주석
    - `repositories/notifications.ts`(04.2-01·06·08 결과 — `notify_tick_runs` 쓰기) · `db/schema/notifications.ts`
    - `lib/email/sender.ts` `smtpConfigFromEnv`(04.2-08) · `domain/holidays/business-day.ts` `formatKstMinute`(04.2-07)
    - `ui/kv-list/KvList.tsx`(값 칸 줄바꿈 규칙)
  </read_first>
  <behavior>
    - 단위: 실행 기록 없음 → `notify.kind = "none"` · 영업일 실행 → `{ kind: "ok", at: "2026-09-24 09:00", businessDay: true, sent: 12, skipped: 3, remaining: 0 }` · 비영업일 실행 → `businessDay: false` · 읽기 함수가 던지면 `notify.kind = "unavailable"`이고 다른 항목은 정상
    - 단위: SMTP 설정 null → `email.kind = "unconfigured"` · 설정 있음 + 이메일 시도 실행 없음 → `{ kind: "configured", from, failure: null }` · 마지막 이메일 시도 실행 `email_failed = 3` → `failure: { count: 3, at }` · 읽기 함수가 던지면(설정 있음) `email.kind = "unavailable"` · 결과 어디에도 host·user·password가 없다
    - E2E(PC): 시스템 관리자 → `/admin/system-status` → 라벨 `알림 발송` 값이 `기록 없음 — 첫 알림 발송 전` 또는 `^\d{4}-\d{2}-\d{2} \d{2}:\d{2} · (알림 \d+건 · 중복 건너뜀 \d+건 · 남음 \d+건|비영업일 · 보내지 않음)$` · 라벨 `이메일` 값이 `미설정`(E2E 환경에는 SMTP 값이 없다) · DB 커넥션 배너 문구가 없다
  </behavior>
  <action>
① **RED.** `test/unit/system-status.test.ts`에 behavior 단위 케이스를 더하고, `test/e2e/system-status.spec.ts`의 시스템 관리자 테스트에 두 줄 단언을 더하며, 같은 테스트의 배너 부재 단언을 「DB 커넥션이 한도의」 문구가 없다는 단언으로 좁힌다(`<probe_fallback>` 「E2E 공유 상태」 — 다른 단언은 그대로).

② **저장소**(항목 추가): `findLastTickRun(viewer)` — `started_at` 내림차순 첫 행 또는 null · `findLastEmailTickRun(viewer)` — `email_sent + email_failed > 0`인 행 중 `started_at` 내림차순 첫 행 또는 null.

③ **도메인** `domain/system-status/index.ts`: `SystemStatus`에 `notify: { kind: "none" } | { kind: "ok"; at: string; businessDay: boolean; sent: number; skipped: number; remaining: number } | { kind: "unavailable" }`와 `email: { kind: "unconfigured" } | { kind: "configured"; from: string; failure: { count: number; at: string } | null } | { kind: "unavailable" }`를 더한다. `StatusDeps`에 `getLastTickRun`·`getLastEmailTickRun`·`smtpConfig`(기본 `smtpConfigFromEnv`)를 더하고, 기존 백업 읽기처럼 각각 try로 감싸 실패는 `unavailable`(조용히 삼키지 않고 기존과 같은 로그를 남긴다). 시각은 `formatKstMinute(started_at)`. SMTP 설정에서는 `from`만 꺼낸다.

④ **화면** `system-status/page.tsx`: `KvList` 항목 끝에 두 줄 — `알림 발송`: none → `기록 없음 — 첫 알림 발송 전` · ok + 영업일 → `{at} · 알림 {sent}건 · 중복 건너뜀 {skipped}건 · 남음 {remaining}건` · ok + 비영업일 → `{at} · 비영업일 · 보내지 않음` · unavailable → `<StatusTag kind="muted">확인 불가</StatusTag>`. `이메일`: unconfigured → `<StatusTag kind="muted">미설정</StatusTag>` · configured → `사용 중 · {from}`, `failure`가 있으면 ` · ` + `--danger` 글자 `실패 {count}건` + ` ({at})` · unavailable → `확인 불가` 태그. 숫자·시각은 `tabular-nums`, 긴 주소는 `overflow-wrap: anywhere`(기존 KvList 값 칸이 이미 그렇게 하지 않으면 이 화면 CSS 모듈 없이 인라인 클래스 대신 KvList가 쓰는 규칙을 확인하고 필요한 최소만 더한다 — 토큰 밖 값 금지). 기존 세 줄과 가드 세 줄은 건드리지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/system-status.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 실행 기록 없음·비영업일·읽기 실패·SMTP 미설정·실패 꼬리 가운데 하나의 모양이 틀렸거나, 결과에 SMTP host·user·password가 실렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/system-status.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — `알림 발송`·`이메일` 라벨이 없거나 값이 카피 계약 형식과 다르거나, 권한 없는 계급의 404 단언이 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - 카피 문구 `기록 없음 — 첫 알림 발송 전` · `비영업일 · 보내지 않음` · `미설정` · `사용 중 · ` · `중복 건너뜀`이 `app/(app)/admin/system-status/page.tsx`에 글자 그대로 있다(Grep)
    - `grep -n "host\|password\|SMTP_USER" "app/(app)/admin/system-status/page.tsx"` 출력이 비어 있다
    - 단위·E2E(`CI=true`) 초록
  </acceptance_criteria>
  <done>관리자가 시스템 상태에서 마지막 알림 발송의 시각·건수(또는 비영업일·기록 없음)와 이메일 채널이 미설정인지·실패가 있었는지를 본다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 배너 둘 — B1 공휴일 확정 요청(안내) · B2 이메일 발송 실패(경고), 「관리」 인덱스 B2 → B1, 시스템 상태 DB 커넥션 → B2</name>
  <files>domain/holidays/admin.ts, domain/system-status/index.ts, app/(app)/admin/page.tsx, app/(app)/admin/system-status/page.tsx, test/integration/admin-banners.test.ts, test/e2e/holidays.spec.ts</files>
  <read_first>
    - `app/(app)/admin/page.tsx` 전체 — 세션·권한 가드 · `adminIndexGroups` · 제목 위 자리
    - `ui/banner/Banner.tsx` — `kind="info"`는 `role="status"`, `kind="warning"`은 `role="alert"` · 닫기 버튼 없음
    - `domain/holidays/admin.ts`(04.2-07 결과 — `findYearConfirmation` 사용처 · 권한 상수) · `domain/system-status/index.ts`(Task 1 결과)
    - `test/e2e/holidays.spec.ts`(04.2-07 결과 — Y 계산 · 직렬 실행 · 확정 기록 정리 방식)
    - `lib/log.ts` — `log.warn` 모양
    - UI-SPEC S3 두 표 · Copywriting B1·B2 · 가정 U-4·U-5·U-6
  </read_first>
  <behavior>
    - 통합(`describe("holiday confirmation banner (D-705)")` — VALIDATION `-t "holiday"`): 올해 Y 확정 + Y+1 미확정 → `holidayConfirmationBanner(관리자)` = `{ year: Y+1 }`(다음 해 미확정 → 관리자 배너) · 둘 다 미확정 → `{ year: Y }` · 둘 다 확정 → null · `admin.holidays` view만 있는 viewer → null · 확정 기록 읽기가 던지면 null + `admin.banner_failed` 경고 로그
    - 통합(`describe("email failure banner")`): 이메일 시도 실행 없음 → null · 마지막 이메일 시도 실행 `email_failed = 3` → `{ count: 3, at }` · 그 뒤 `email_sent = 2`·`email_failed = 0` 실행 → null(다음 이메일 발송 tick에 실패 0이면 사라진다) · 그 뒤 이메일 0건 실행(SMTP 미설정 tick)이 더해져도 판정이 바뀌지 않는다 · `admin.system-status` view 없는 viewer → null
    - E2E(PC, `holidays.spec.ts`에 describe 추가): 시스템 관리자 → `/admin` → `role="status"` 배너 `{Y}년 공휴일 확정 전 · 규칙으로 만든 후보로 계산 중` + 링크 `{Y}년 공휴일 검토` → 누르면 `/admin/holidays?year={Y}`이고 그 화면에는 두 배너가 없다 · 테스트 DB에 `email_failed = 3` 실행 한 행을 넣으면 `/admin`은 `role="alert"` 배너 `이메일 발송 실패 3건 · … · 알림함으로만 전달됨` + 링크 `시스템 상태 보기` 하나만 보이고(B1 없음) `/admin/system-status`는 같은 배너를 링크 없이 보이며 `이메일` 줄은 그대로 `미설정` · 넣은 행을 지우면 `/admin`에 다시 B1 · 배너에 닫기 버튼이 없다
  </behavior>
  <action>
① behavior를 `test/integration/admin-banners.test.ts`(신규)와 `test/e2e/holidays.spec.ts`(04.2-07 파일에 describe 하나 추가 — 공유 상태를 한 직렬 파일에 모으는 이유를 describe 위 주석 두 줄로)에 먼저 쓴다(RED). E2E의 실패 실행 행은 `started_at = 지금`으로 넣고 `afterAll`에서 그 id만 지운다.

② **판정 함수:** `domain/holidays/admin.ts`에 `holidayConfirmationBanner(viewer, deps?)` → `{ year: number } | null` — write 권한이 없으면 null → KST 올해 Y·Y+1의 확정 기록 → 없는 가장 이른 해. `domain/system-status/index.ts`에 `emailFailureBanner(viewer, deps?)` → `{ count: number; at: string } | null` — `admin.system-status` view가 없으면 null → `findLastEmailTickRun` → `email_failed > 0`이면 `{ count, at: formatKstMinute(started_at) }`. 두 함수 모두 조회가 던지면 `log.warn("admin.banner_failed", { banner: "holiday" | "email", message })` 뒤 null.

③ **화면:** `app/(app)/admin/page.tsx` — 가드 뒤 `const emailBanner = await emailFailureBanner(viewer)`, 그것이 없을 때만 `holidayConfirmationBanner(viewer)`. `PageHeader` **위**에 하나만: B2 `<Banner kind="warning">이메일 발송 실패 {count}건 · {at} · 알림함으로만 전달됨 <Link href="/admin/system-status">시스템 상태 보기</Link></Banner>` 또는 B1 `<Banner kind="info">{year}년 공휴일 확정 전 · 규칙으로 만든 후보로 계산 중 <Link href="/admin/holidays?year={year}">{year}년 공휴일 검토</Link></Banner>`. 링크는 3차 링크 모양(기존 인덱스 링크 클래스·터치 목표 44px). `system-status/page.tsx` — 기존 커넥션 배너 식을 「커넥션 배너가 있으면 그것, 없고 B2가 있으면 링크 없는 B2」로 바꾼다(B2 판정은 이미 읽은 `status.email.failure`를 써서 두 번 조회하지 않는다). `/admin/holidays`와 다른 화면은 건드리지 않는다(U-5).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/admin-banners.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 다음 해 미확정인데 배너가 null이거나(D-705 통합 실패), 둘 다 확정인데 배너가 나오거나, 보기 전용에게 B1이 나오거나, 실패 뒤 성공 tick이 와도 B2가 남거나, SMTP 미설정 tick이 B2 조건을 바꾸거나, 조회 실패가 화면 오류로 번졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/system-status.spec.ts test/e2e/admin-nav.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 「관리」 인덱스에 배너가 둘 보였거나, B2가 있는데 B1이 보였거나, 시스템 상태의 B2에 링크가 남았거나, 배너 문구가 카피 계약과 다르거나, 공휴일 화면에 배너가 떴거나, 기존 관리자 탐색·a11y E2E가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "<Banner" "app/(app)/admin/page.tsx"`가 2이고 두 요소가 하나의 조건식(삼항 또는 `??`) 안에 있다(한 화면 최대 1개 — 검토)
    - 카피 문구 `규칙으로 만든 후보로 계산 중` · `알림함으로만 전달됨` · `시스템 상태 보기` · `공휴일 검토`가 `app/(app)/admin/page.tsx`에 글자 그대로 있다(Grep)
    - `grep -rn "ui/banner" "app/(app)/admin/holidays"` 출력이 비어 있다(공휴일 화면에는 배너 없음 — U-5)
    - 통합 `pnpm vitest run --project integration -t "holiday"`에 D-705 케이스가 잡힌다 · E2E(`CI=true`) 초록 · 독립 DOM 감사(별도 에이전트, `CI=true`)가 S3·S4 14줄을 판정해 SUMMARY에 붙인다(S3/overflow backstop은 감사 판정 또는 human_needed)
  </acceptance_criteria>
  <done>관리자가 「관리」 첫 화면에서 이메일 실패나 공휴일 확정 요청 가운데 하나를 우선순위대로 보고, 시스템 상태에서 같은 실패를 커넥션 경고 다음 순서로 본다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 권한표 → 배너·상태 줄 | 누가 운영 상태(발송 실패·SMTP 발신 주소·공휴일 미확정)를 보는지를 권한이 가른다 |
| 환경 변수 → 화면 | SMTP 설정 가운데 무엇이 화면에 나오는지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-90 | Information Disclosure | 시스템 상태 `이메일` 줄 | high | mitigate | 도메인이 SMTP 설정에서 `from`만 꺼낸다 · 화면 파일에 host·password·user 참조 없음(수용 기준 Grep) · 단위가 결과에 세 값이 없음을 단언 |
| T-4.2-91 | Information Disclosure | 배너 두 개 | medium | mitigate | B1은 `admin.holidays` write, B2는 `admin.system-status` view 권한자에게만 — 판정 함수가 권한을 검사하고 통합이 권한 없는 viewer의 null을 단언 |
| T-4.2-92 | Denial of Service | 「관리」 인덱스·시스템 상태 렌더 | low | mitigate | 배너·상태 조회 실패는 null/`확인 불가`로 바꾸고 경고 로그만 남긴다(화면은 렌더된다 — S3/error, S4/error) |
| T-4.2-93 | Repudiation | 운영 신호 | low | accept | 배너는 조건의 투영일 뿐 새 기록을 만들지 않는다 — 원천은 `notify_tick_runs`·`holiday_year_confirmations`와 행동 로그(04.2-07·08) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- 단위: `pnpm vitest run --project unit test/unit/system-status.test.ts`
- 통합: `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/admin-banners.test.ts`
- 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`
- 독립 DOM 감사(별도 에이전트, `CI=true`) → 수정 → E2E 한 번: `CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/system-status.spec.ts test/e2e/admin-nav.spec.ts test/e2e/a11y.spec.ts`
- 배포 뒤 스케줄러 수동 실행 → 시스템 상태 마지막 tick 갱신 확인은 VALIDATION 「Manual-Only」

</verification>

<success_criteria>

- 시스템 상태에 `알림 발송`·`이메일` 두 줄이 카피 계약대로 보이고 SMTP 비밀은 보이지 않는다(18A · D-711)
- 통합: 다음 해 미확정 → 관리자 배너(D-705) · 이메일 실패 → B2, 다음 이메일 tick 성공 → 사라짐
- 한 화면 배너 최대 1개, 우선순위는 UI-SPEC S3 표대로

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 타입 확장 | `SystemStatus.notify` · `SystemStatus.email` · `StatusDeps.getLastTickRun` · `StatusDeps.getLastEmailTickRun` · `StatusDeps.smtpConfig` |
| 함수 | `emailFailureBanner`(domain/system-status) · `holidayConfirmationBanner`(domain/holidays/admin.ts) · `findLastTickRun` · `findLastEmailTickRun`(저장소) |
| 화면 | `/admin` 배너 자리(B2 → B1) · `/admin/system-status` 두 줄 + 배너 자리(DB 커넥션 → B2) |
| 로그 이벤트 | `admin.banner_failed`(warn, banner · message) |
| 테스트 | `test/integration/admin-banners.test.ts`(D-705 「다음 해 미확정 → 관리자 배너」) · `test/unit/system-status.test.ts`(확장) · `test/e2e/system-status.spec.ts`(두 줄 · 커넥션 배너 단언 좁힘) · `test/e2e/holidays.spec.ts`(배너 describe) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-09-SUMMARY.md` when done
</output>
