---
phase: 04.2-notify-holidays-foundation
plan: 09
subsystem: notify
tags: [css, react, playwright, vitest, next-safe-action, accessibility]

requires:
  - phase: 04.2-07
    provides: "알림함 읽기 도메인(listMyNotifications·openMyInbox) · UnreadCountProvider · /notifications 화면 행만"
provides:
  - "날짜 그룹 머리글(rowgroup) 계약 정합 — 위 12px · 아래 1px(M1, CSS 특이도 버그 수정)"
  - "더 보기 요청 중·실패·재시도·마지막 쪽에서 포커스를 잃지 않는 목록(M2)"
  - "dev StrictMode에서도 마운트 열기 요청이 한 번만 나가는 InboxList(M4)"
  - "더 보기 실패 줄 role=\"status\" 실시간 알림"
  - "읽음 처리 POST 실패 시 화면 오류 없이 배지·인셋만 유지되는 것을 증명하는 E2E"
  - "결정성 있는 더 보기 실패 E2E(요청 특정 + fulfill 500, CI=true 3회 반복 확인)"
affects: [04.2-14]

actuals:
  tokens: 5193
  tasks: 1
  commits: 3
plan_head_before: 7be9b11

tech-stack:
  added: []
  patterns:
    - "CSS 특이도 버그: 범용 요소 선택자(`.table th`)가 같은 요소에 붙는 더 구체적인 클래스 규칙을 덮어쓸 때는 클래스만 올리지 말고 요소+클래스 조합(`.table th.groupHeader`)으로 특이도를 올린다 — 컴포넌트별 module.css라도 `.className` 단독 규칙은 죽은 규칙이 될 수 있다"
    - "next-safe-action의 hasErrored는 재실행(status=executing) 동안 항상 거짓이다 — 실패 상태를 요청 중에도 보여야 하면 훅의 파생 상태를 그대로 쓰지 말고 onError/onSuccess로 로컬 상태를 직접 관리한다"
    - "Button의 pending은 네이티브 disabled라 브라우저가 요청 중 포커스를 문서로 돌린다 — 요청이 끝난 뒤 남은 컨트롤에 ref로 포커스를 되돌리는 것은 컴포넌트를 고치지 않고도 컨테이너 ref + isExecuting 전이 감지 effect로 해결할 수 있다"
    - "dev StrictMode의 mount→cleanup→mount 이중 실행에서 부수효과가 있는(멱등하지 않은) 서버 액션을 한 번만 부르려면, '요청을 이미 시작했는가' 플래그와 '지금 진짜로 언마운트됐는가' 플래그를 분리한다 — 하나로 합치면 cleanup이 유일한 실제 요청까지 취소해 버린다"
    - "Playwright에서 서버 액션 실패를 흉내낼 때 route.abort()는 로컬호스트 keep-alive에서 간헐적으로 같은 요청을 재성공시킨다 — route.fulfill({status:500})가 결정적이다(단, requestfailed가 아니라 waitForResponse로 기다려야 한다)"

key-files:
  created:
    - test/unit/notifications/inbox-list-format-group-label.test.ts
  modified:
    - app/(app)/notifications/inbox-list.tsx
    - app/(app)/notifications/inbox-list.module.css
    - test/e2e/notify-inbox.spec.ts
    - test/unit/holidays/business-day.test.ts

key-decisions:
  - "M3(ERROR 한 줄에 「다시 시도」가 중복 보임)은 코드를 고치지 않고 DECISIONS 후보로만 남겼다 — 플랜 Task 3 ③과 acceptance_criteria grep이 `불러오지 못했습니다 · 다시 시도` 문자열을 글자 그대로 요구하고, 같은 패턴이 admin/permissions/page.tsx에도 이미 있어 이 화면 하나만 고치면 CLAUDE.md §6(화면 하나만 예외 금지)에 걸린다. 시스템 전체 카피 계약 변경은 Rule 4(아키텍처/계약 변경)로 별도 승인이 필요하다."
  - "L7(axe 대상에 /notifications 없음)은 test/e2e/a11y.spec.ts가 Task 3 <files> 범위 밖이고, 화면을 빼거나 넣는 일 자체가 그 파일의 계약(SCREENS 배열 고정 개수 단언)을 건드리는 별도 검증 절차라 이번 런에서 다루지 않았다."
  - "L3·L5·L6·L8·L9는 04.2-07 기존 코드 부채이거나(L3 두 줄 행 vertical-align, L5 TopBar 배지 리터럴, L6 LOADING 지연 없음, L8 min-height 무효) 문서 전용(L9 SYSTEM.md 순서 불일치)이라 이번 Task 3 사후 수정 범위에서 defer했다 — 04.2-14(Post-build)나 별도 후속 플랜에서 다룬다."

requirements-completed: []

coverage:
  - id: D1
    description: "그룹 머리글 padding-top 12px · border-bottom-width 1px(계약값)로 렌더된다(M1)"
    verification:
      - kind: e2e
        ref: "test/e2e/notify-inbox.spec.ts#서로 다른 KST 날짜 두 날 — 날짜마다 tbody + rowgroup 머리글 (computed style 단언)"
        status: pass
    human_judgment: false
  - id: D2
    description: "더 보기 성공 뒤 hasMore가 남으면 포커스가 버튼에 남는다(M2)"
    verification:
      - kind: e2e
        ref: "test/e2e/notify-inbox.spec.ts#101건 — 더 보기 성공 뒤에도 hasMore면 포커스가 버튼에 남는다(M2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "더 보기 실패 줄이 role=\"status\"이고, 재시도 요청 중에도 라벨이 「다시 시도」로 남고 비활성이다(M2/L1)"
    verification:
      - kind: e2e
        ref: "test/e2e/notify-inbox.spec.ts#51건 — 더 보기 요청 중·실패(다시 시도)·마지막 쪽 인셋과 포커스"
        status: pass
    human_judgment: false
  - id: D4
    description: "dev StrictMode에서도 openInboxAction이 마운트당 한 번만 실행된다(M4)"
    verification: []
    human_judgment: true
    rationale: "프로덕션 빌드(CI=true, next start)는 StrictMode 이중 실행을 하지 않아 이 회귀를 재현할 수 없다 — 코드 리뷰로만 검증했다(hasOpenedRef/unmountedRef 분리 패턴, 위 tech-stack 패턴 참고). dev 서버(pnpm dev)에서 직접 확인이 필요하면 사람이 한다."
  - id: D5
    description: "읽음 처리 POST가 실패해도 화면에 오류가 보이지 않고 배지·인셋이 유지된다"
    verification:
      - kind: e2e
        ref: "test/e2e/notify-inbox.spec.ts#읽음 처리 POST가 실패해도 화면 오류 없이 배지만 남는다(M4/S1-inbox-list error (c))"
        status: pass
    human_judgment: false
  - id: D6
    description: "51건 더 보기 실패 E2E가 결정적이다(M5) — 3회 반복 안정"
    verification:
      - kind: e2e
        ref: "test/e2e/notify-inbox.spec.ts#51건 — 더 보기 요청 중·실패(다시 시도)·마지막 쪽 인셋과 포커스 (CI=true 3회)"
        status: pass
    human_judgment: false

duration: 1h 10min
completed: 2026-09-25
status: complete
---

# Phase 04.2 Plan 09: 알림함 목록 완성 — 사후 수정(적대적 검토 반영) Summary

**독립 DOM 감사(13 PASS/3 human_needed) + Opus 적대적 디자인 검토(medium 5·low 9) 뒤 M1·M2·M4·L1·L2·L4를 코드로, M5를 테스트 원인 수정으로 반영하고 전체 E2E 게이트(167/167)를 한 번 통과시켰다**

## 이 SUMMARY의 범위

이 플랜의 Task 1~3은 이전 세션(Run A·Run B, `exec-04.2-09-runB.md`)에서 이미 커밋·완료됐다
(`f7afc35`·`d7af504`·`159b7d6`·`9b28471`·`2625060`·`e71a072`). 이 SUMMARY는 Task 3 ⑤(화면
검증 순서 — 독립 DOM 감사 + 적대적 디자인 검토 → 수정 → 전체 게이트 한 번)의 **마지막 단계**,
즉 감사·검토가 찾은 항목을 코드에 반영하고 전체 E2E를 한 번 더 돈 이번 세션의 작업만 기록한다.

## Codex 문서 검토

**미실행(한도, 2026-09-29까지 · 사용자 결정 「Opus」)** — 한도 풀리면 Codex 재확인 필요.
Task 2 ④ 몫은 이전 세션에서 Opus 적대적 검토(`/mnt/project-files/04.2-gates/review-04.2-09-docs-opus.md`)
로 대신했고, 그 발견(B-1~B-4 medium 4건 + R-1~R-7 low 7건)은 커밋 `9b28471`에서 전부 반영했다
(R-8·R-9는 검토 파일 자체가 「범위 밖 · 문제 없음」으로 판정해 반영하지 않았다).

## Codex 디자인 검토

**미실행(한도, 2026-09-29까지 · 사용자 결정 「Opus」)** — 한도 풀리면 Codex 재확인 필요.
Task 3 ⑤ 몫은 Opus 적대적 디자인 검토로 대신했다: `/mnt/project-files/04.2-gates/design-review-04.2-09-opus.md`
(high 0 · medium 5 · low 9 · 참고 9). 이번 세션에서 medium 5건 전부와 low 3건(L1·L2·L4)을
반영했다(아래 「반영 항목」·「반영하지 않은 low」참고).

## 독립 DOM 감사 결과

`/mnt/project-files/04.2-gates/dom-audit-04.2-09.md` — CI=true 프로덕션 빌드, 감사자(실행자
아님)가 임시 스펙 21개로 실측 후 삭제·`git status` clean 확인. 판정 대상 16줄 중:

- **13 PASS** — S1-menu-item(빈·로딩·populated·partial·overflow·zero/one/many·long-text) 7개,
  S1-inbox-list(빈·populated·partial·overflow·zero/one/many·long-text backstop) 6개.
- **3 human_needed(FAIL 아님)** — 모두 "브라우저에서 가로챌 네트워크 요청 자체가 없는 SSR 전용
  실패 경로"이거나 "로컬에서 강제 재현이 안 되는 타이밍 조건": (a) 메뉴 배지 조회 실패(SSR),
  (b) LOADING 뼈대의 300ms 지연 자체(로컬 DB가 너무 빨라 자연 발생을 못 봄, 강제 지연도
  스트리밍 특성상 재현 안 됨 — 소스 확인으로 대체), (c) 초기 조회 실패(SSR). **다만 감사자는
  (c)의 자매 항목인 「읽음 처리 실패」는 E2E로 확정 가능함을 증명했고(하드 goto로
  UnreadCountProvider 재조회 effect를 피함), 이번 세션에서 그 방법을 그대로 써서 E2E를
  추가했다(coverage D5).**
- FAIL 0건.

## 반영 항목

| ID | 심각도 | 내용 | 처리 | 근거 |
|---|---|---|---|---|
| M1 | medium | `.groupHeader`가 `.table th`보다 특이도가 낮아 죽은 규칙(위 12px·아래 1px 계약 미적용) | 반영 | `inbox-list.module.css` `.table th.groupHeader` + E2E computed-style 단언(D1) |
| M2 | medium | 더 보기 성공(더 있음)·실패·재시도에서 포커스를 잃음(Button pending=네이티브 disabled) | 반영 | `loadMoreContainerRef` + isExecuting 전이 감지 effect로 포커스 복구, 실패 줄 `role="status"` + E2E(D2·D3) |
| M3 | medium | ERROR 한 줄에 「다시 시도」가 두 번 보임 | **미반영(DECISIONS 후보)** | 플랜 acceptance_criteria가 문자열을 고정, admin/permissions에도 같은 패턴 존재(§6 화면 하나만 예외 금지) — key-decisions 참고 |
| M4 | medium | dev StrictMode가 openInboxAction을 두 번 불러 인셋·배지가 사라짐(정적 추론) | 반영 | `hasOpenedRef`/`unmountedRef` 분리 가드(D4, human_judgment — 아래 참고) |
| M5 | medium | 51건 더 보기 실패 E2E가 원인 불명 채 예외만 삼킴(1/3회 레이스) | 반영 | 요청을 `"cursor"` 본문으로 특정 + `route.fulfill({status:500})`로 교체, CI=true 3회 반복 안정 확인(D6) |
| L1 | low | 재시도 요청 중 hasErrored가 거짓이 되어 라벨이 「더 보기 50건」으로 잠깐 바뀜 | 반영 | `showRetry` 로컬 상태로 직접 관리 + E2E(D3) |
| L2 | low | 시각 열 머리글이 값 칸과 달리 왼쪽 정렬 | 반영 | `.table th.time` 추가 |
| L4 | low | §7-3 행 hover(`--surface`)가 빠짐 | 반영 | `.table tbody tr:hover td` 추가(people.module.css 선례) |
| L3·L5·L6·L8·L9 | low | 두 줄 행 정렬·배지 CSS 리터럴·LOADING 지연 없음·min-height 무효·SYSTEM.md 순서 불일치 | **미반영** | L3/L5/L6/L8은 04.2-07 기존 코드 부채, L9는 문서 전용 — key-decisions 참고 |
| L7 | low | axe 대상(`a11y.spec.ts`)에 `/notifications` 없음 | **미반영** | Task 3 `<files>` 범위 밖, SCREENS 배열 고정 개수 계약을 건드리는 별도 검증 — key-decisions 참고 |
| R1~R9 | 참고 | 판단 재료(커버리지 표 과장·이름 넘침 여유·hover 전파 등) | 검토만, 수정 불필요 | design-review-04.2-09-opus.md 참고 섹션 — 막는 문제 없음 |

## 싼 게이트

- `pnpm typecheck` → 0 errors
- `pnpm lint` → 0 errors(기존 boundaries deprecation 경고만, 무관)
- `pnpm lint:sql` → 0 issues(13 files)
- `pnpm vitest run --project unit` → **857/857 통과**(기존 854 + 신규 3개 테스트: `formatKstTime` KST 자정 경계 1개(사례 2건) + `formatGroupLabel` 2개)
- `pnpm build` → exit 0, `/notifications` 라우트 포함 정상 생성

## 전체 E2E 게이트(한 번)

`CI=true pnpm playwright test test/e2e/notify-inbox.spec.ts test/e2e/mobile-notify-inbox.spec.ts test/e2e/user-menu.spec.ts test/e2e/mobile-shell.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/a11y.spec.ts`

**167/167 통과**, exit 0(2.5분). 세부:
- `notify-inbox.spec.ts` 15/15(기존 13 + 신규 2: 101건 M2 테스트, 읽음 처리 실패 테스트)
- `mobile-notify-inbox.spec.ts` 3/3
- `user-menu.spec.ts`·`mobile-shell.spec.ts`·`keyboard-nav.spec.ts`·`a11y.spec.ts` — 나머지 149개 회귀 없음

추가로 M5 안정성 확인을 위해 `51건` 테스트만 CI=true로 **3회 반복** 실행 — 3/3 통과, 레이스
재현 없음.

## 커밋

| 커밋 | 유형 | 내용 |
|---|---|---|
| `156e9e7` | fix | M1·M2·M4·L1·L2·L4 화면 코드(`inbox-list.tsx`·`inbox-list.module.css`) |
| `cd2a842` | test | M1 계측·M2 포커스·M5 원인 수정·읽음 실패 E2E(`notify-inbox.spec.ts`) |
| `a344084` | test | formatKstTime 자정 경계·formatGroupLabel 연도 분기 단위 테스트 |

(이전 세션 커밋 `f7afc35`·`d7af504`·`159b7d6`·`9b28471`·`2625060`·`e71a072`는 이 SUMMARY 범위
밖 — `exec-04.2-09-runB.md` 참고.)

## SYSTEM.md numstat

이번 세션은 `docs/design/SYSTEM.md`를 건드리지 않았다(`git diff --numstat` 출력 없음) — Task 2
④의 SYSTEM.md 반영(Opus 문서 검토, 3/3·11/11)은 이전 세션 커밋 `9b28471`에서 이미 끝났다.

## Deviations from Plan

### 이번 세션에서 발견·수정(모두 Rule 1 — 버그)

**1. [Rule 1 - 버그] M1 CSS 특이도로 그룹 머리글 padding/border 계약 미적용**
- **발견:** Task 3 사후 Opus 적대적 디자인 검토
- **원인:** `.table th`(0,1,1)가 `.groupHeader`(0,1,0)보다 특이도가 높아 후자가 죽은 규칙이었다.
- **수정:** `.table th.groupHeader`로 특이도를 올리고 padding-top·border-bottom만 override.
- **파일:** `app/(app)/notifications/inbox-list.module.css`
- **검증:** E2E computed-style 단언(D1)
- **커밋:** `156e9e7`

**2. [Rule 1 - 버그] M2 더 보기 성공·실패·재시도에서 포커스 유실**
- **발견:** Opus 적대적 디자인 검토(정적 추론) → 이번 세션 E2E로 확정
- **원인:** `Button`의 `pending`이 네이티브 `disabled`라 브라우저가 요청 중 포커스를 문서로 돌리고, 요청이 끝나도 되돌리지 않았다.
- **수정:** 컨테이너 ref + `isExecuting` 전이 감지 effect로 요청 종료 뒤 남은 버튼에 포커스 복구. 실패 줄에 `role="status"` 추가.
- **파일:** `app/(app)/notifications/inbox-list.tsx`
- **검증:** E2E(D2·D3)
- **커밋:** `156e9e7`

**3. [Rule 1 - 버그] M4 dev StrictMode 이중 마운트가 openInboxAction을 두 번 호출**
- **발견:** Opus 적대적 디자인 검토(정적 추론, 코드 읽기로 확정 — 아래 검증 한계 참고)
- **원인:** 기존 `cancelled` 플래그만으로는 StrictMode의 mount→cleanup→mount를 버티지 못해, 두 번째 마운트가 서버를 다시 열어 첫 호출이 읽은 행의 readAt과 두 번째 호출의 openedAt이 어긋났다.
- **수정:** `hasOpenedRef`(실제 요청 시작 여부)와 `unmountedRef`(진짜 언마운트 여부)를 분리.
- **파일:** `app/(app)/notifications/inbox-list.tsx`
- **검증:** 코드 리뷰만(D4 — human_judgment: true, 프로덕션 빌드는 StrictMode 이중 실행을 하지 않아 E2E로 재현 불가). **CI=true 전체 E2E(167/167)에는 회귀 없음을 확인했다.**
- **커밋:** `156e9e7`

**4. [Rule 1 - 버그, L1] 재시도 요청 중 라벨이 잠깐 「더 보기 50건」으로 바뀜**
- **발견:** Opus 적대적 디자인 검토
- **원인:** next-safe-action의 `hasErrored`는 `status="executing"`이 되는 즉시 거짓이 되어, 렌더 분기가 실패 상태에서 정상 상태로 잠깐 넘어갔다.
- **수정:** `showRetry` 로컬 상태(onError/onSuccess에서 직접 설정)로 대체.
- **파일:** `app/(app)/notifications/inbox-list.tsx`
- **검증:** E2E(D3, 재시도 요청 중 라벨·비활성 단언)
- **커밋:** `156e9e7`

**5. [Rule 1 - 테스트 결함, M5] 51건 더 보기 실패 E2E가 결정적이지 않음**
- **발견:** Opus 적대적 디자인 검토 + 독립 DOM 감사(같은 원인을 각자 발견)
- **원인:** 첫 Next-Action POST를 가로채는 방식이 마운트 때 openInboxAction·배지 다시 받기와 섞였고, `route.abort()`가 로컬호스트 keep-alive에서 간헐적으로 같은 요청을 재성공시켰다.
- **수정:** 본문에 `"cursor"`가 있는 요청만 가로채고 `route.fulfill({status:500})`로 교체, 대기도 `requestfailed`→`waitForResponse`로 변경.
- **파일:** `test/e2e/notify-inbox.spec.ts`
- **검증:** CI=true 3회 반복 실행, 3/3 통과
- **커밋:** `cd2a842`

**6. [Rule 1 - CSS 특이도, L2] 시각 열 머리글이 값 칸과 다르게 왼쪽 정렬**
- 같은 종류(M1)의 특이도 문제 — `.table th.time` 추가.
- **커밋:** `156e9e7`

**7. [Rule 2 - 누락된 필수 기능, L4] §7-3 행 hover(`--surface`)가 빠짐**
- 목록 표 선례 7곳 모두 갖고 있는 시각 신호가 누락돼 있었다 — 추가.
- **커밋:** `156e9e7`

---

**총 편차:** 7건 자동 수정(버그 5·CSS 특이도 1·누락 기능 1). 모두 Opus 적대적 검토 또는 독립
DOM 감사가 먼저 찾은 항목이고, 계획 범위(Task 3 화면 검증 순서 ⑤) 안의 "감사·검토 → 수정"
단계 자체다 — 범위 확장(scope creep)이 아니다.

**미반영(별도 판단, 코드 변경 없음):** M3(카피 계약 충돌, DECISIONS 후보), L3·L5·L6·L7·L8·L9(기존
부채 또는 이 Task의 `<files>` 밖) — 위 "반영 항목" 표와 key-decisions 참고.

## Issues Encountered

없음 — 모든 자동 수정이 3회 이내 시도로 해결됐고, 전체 게이트(싼 게이트 + 전체 E2E)가 한 번에
통과했다.

## Next Phase Readiness

- 04.2-09는 이 SUMMARY로 완료 처리한다. STATE.md는 갱신하지 않는다(decimal-phase 규칙).
- 04.2-03/08/11/12는 여전히 PR #40(Phase 4, 04-32 tx 인자) 머지 대기.
- 04.2-15 Task 1(nodemailer 패키지 정체성)은 여전히 사용자 승인 대기 — 04.2-10·04.2-13도 그 뒤.
- 페이즈 끝(04.2-14) 때 `/design-review` + Codex 적대적 디자인 검토(한도 풀리면) + `/qa`가
  이 플랜이 대신하지 않은 항목(M3 DECISIONS 확정, L3·L5·L6·L7·L8·L9)을 마저 다뤄야 한다.
- Codex 재확인(문서·화면, 04.2-01/02/04/05/06/07/09) — 2026-09-29 이후.

---
*Phase: 04.2-notify-holidays-foundation*
*Plan: 09 (사후 수정 세션)*
*Completed: 2026-09-25*
