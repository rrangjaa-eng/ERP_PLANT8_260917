---
phase: 04.2-notify-holidays-foundation
plan: 10
type: execute
wave: 3
depends_on: [04.2-05, 04.2-06, 04.2-07, 04.2-15]
files_modified:
  - domain/notify/email-phase.ts
  - domain/notify/tick.ts
  - repositories/notifications.ts
  - app/internal/notify-tick/handle.ts
  - test/support/fake-email-sender.ts
  - test/integration/notify-email.test.ts
  - test/integration/notify-tick.test.ts
  - db/client.ts
  - repositories/settings.ts
autonomous: true
requirements: [NOTI-02]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "같은 알림이 이메일로도 간다 — 받는 사람마다 KST 하루 한 통으로 묶고(D-706), 알림함에는 건별로 남는다. 한 사람에게 3건 → 메일 1통 · 알림함 3건(D-711 테스트 원문)"
    - "이메일 채널은 `SMTP_HOST`·`SMTP_USER`·`SMTP_PASSWORD`·`SMTP_FROM` 넷이 모두 채워졌을 때만 켜진다(D-711 기능 플래그 = 환경 변수 채움 여부). 하나라도 비면 메일 0통이고 그 알림 행은 `skipped_no_smtp`로 남아 나중에 SMTP를 채워도 지난 알림을 몰아 보내지 않는다"
    - "`skipped_no_smtp` 표시는 발송 시도가 아니다 — `email_attempted_at`을 찍지 않고 하루 한 통 판정에 세지 않는다. 그래서 SMTP가 꺼진 채 tick이 돈 날 SMTP를 켜면 그날 새로 생긴 알림은 그날 메일로 간다(Codex #17)"
    - "묶음 메일의 제목·본문은 04.2-15의 `composeDigest`가 만든다(UI-SPEC 카피 계약 D-706 — 모양 단언은 04.2-15 단위 테스트)"
    - "이메일은 받는 사람 묶음 **하나씩** 짧은 트랜잭션으로 선점한다 — 선점 트랜잭션은 04.2-01 알림함 삽입과 **같은 키**의 기다리는 advisory 잠금(`pg_advisory_xact_lock(NOTIFY_TICK_LOCK_KEY)`)을 잡고, 오늘(KST) `sending`·`sent`·`failed`·`unknown` 기록이 없고 **가장 최근 영업일 실행 기록의 `incomplete_recipient_ids`**(04.2-01 — 같은 선점 트랜잭션 안에서 읽는다)에 없는 받는 사람 한 명의 `pending` 행을 `sending` + `email_attempted_at`으로 바꾸고 실행 기록 `email_claimed`를 1 늘린 뒤 커밋한다. 발송은 트랜잭션 밖이다(D-4203 개정 · Codex #4 · Codex 2차 #3 · 3차 추가 #1)"
    - "평가가 실패한 tick 뒤에는 아무에게도 메일을 선점하지 않는다 — 선점 트랜잭션이 읽는 가장 최근 영업일 실행 기록의 `evaluation_failed`(04.2-01 — 조건 종류 하나라도 던진 tick)가 참이면 선점 0이고 행은 `pending`으로 남는다. 모든 종류가 성공한 다음 tick 뒤에 다시 선점해, A의 알림 3건 중 1건만 들어간 뒤 평가가 실패해도 A에게는 3건이 다 들어간 뒤 한 통이 간다(Codex 4차 E3)"
    - "겹친 tick에서도 반쯤 들어간 묶음을 보내지 않는다 — tick A의 이메일 단계가 B 발송에서 멈춘 사이 tick B가 C의 알림을 일부만 넣으면(B 실행 기록 `incomplete_recipient_ids = {C}`), 풀려난 A의 다음 선점은 C를 고르지 않는다. C의 알림이 다 들어간 tick 뒤에 C에게 한 통이 간다(Codex 2차 #3)"
    - "미완 목록과 선점하는 행은 한 시점의 DB에서 온다 — 선점이 알림함 삽입과 같은 잠금을 잡으므로, 선점이 미완 목록을 읽고 행을 고르는 사이에 다른 tick의 삽입이 커밋될 수 없다. tick B가 잠금을 쥔 채 C를 반쯤 넣는 동안 A의 선점은 잠금을 기다렸다가 B의 실행 기록 `{C}`를 읽고 C를 고르지 않는다(Codex 3차 추가 #1)"
    - "보관된 사람(`users.archived_at IS NOT NULL`)은 메일을 받지 않는다 — 선점이 후보를 고르는 조건에 `users.archived_at IS NULL`이 있어 보관된 사람은 후보에서 빠지고(가장 오래된 pending이어도 다른 사람의 선점을 막지 않는다), 그 사람의 `pending` 행은 상태·`email_attempted_at` 모두 그대로 남아 복원되면 그 뒤 tick의 묶음으로 간다. 알림함은 `lib/viewer.ts`가 이미 막으므로 메일이 유일한 우회로였다(Codex 최종 B)"
    - "여러 날 쌓인 `pending` 행도 한 통에 모이되 메일은 커지지 않는다 — 선점은 그 사람의 `pending` 행 전부를 잡고(D-706 — 알림을 한 통에 모은다), 본문은 04.2-15 `composeDigest`가 최신 먼저로 `DIGEST_MAX_LISTED`(20)건만 `- ` 줄로 적고 나머지는 `외 {M}건은 알림함에서 확인` 한 줄로 센다(제목·첫 줄의 N은 전체 수, 첫 줄 날짜는 발송일). 알림함에는 모두 건별로 있다(Codex 1차 #9 잔여 · 3차) 〔2026-09-25 개정 — /plan-design-review #15(R10 보완, UI-SPEC 62d1392 Copywriting 묶음 이메일 본문): 알림함처럼 최신 먼저 — 묶음 행은 `created_at DESC, id DESC`로 넘기고 `composeDigest`는 받은 순서의 앞 20건(= 최신 20건)을 적으며, 첫 줄 날짜는 발송일(tick의 KST 날짜)이다. 앞 판의 「생성 순서 앞 20건」은 여러 날 쌓이면 가장 오래된 20건만 보여 새 알림이 「외 M건」에 묻혔다〕"
    - "발송 결과는 04.2-15 포트의 세 결과대로 기록된다 — `sent` → 행 `sent` · `email_sent` +1, `rejected` → 행 `failed` · `email_failed` +1, `indeterminate` → 행 `unknown` · `email_unknown` +1. 발송기가 예외를 던지면(포트 계약 위반) `indeterminate`·`UNKNOWN`으로 본다 — 갔을 수도 있는 메일을 확정 실패로 적지 않는다. 결과와 건수는 한 트랜잭션으로 커밋되고, 이메일 단계가 끝나면 실행 기록 `email_finished_at`을 찍는다(D-4216 · Codex 2차 #2)"
    - "선점 뒤 결과 기록 전에 프로세스가 죽으면(발송 전 · SMTP 수락 뒤 · 결과 기록 전 어디서든) 그 묶음은 `sending`으로 남는다. 결과 불명은 두 가지다: `unknown` 행(모호한 SMTP 끊김·발송 마감)과 선점 시각이 10분 넘게 지난 `sending` 행. 둘 다 자동으로 다시 보내지 않는다(같은 날 판정에 세어진다)(D-4216 · D-4217)"
    - "`findUnresolvedEmail(viewer, { now, unknownAfterMs, visibleDays })`(도메인 상수 `EMAIL_UNKNOWN_AFTER_MS` 10분 · `EMAIL_UNKNOWN_VISIBLE_DAYS` 7일을 인자로 받는다 — 저장소는 도메인을 import하지 않는다)는 실행 기록이 아니라 알림 행에서 결과 불명 묶음(받는 사람 × 선점 시각)을 센다 — `email_attempted_at >= now - 7일`인 `unknown` 행과 `email_attempted_at < now - 10분`인 `sending` 행. 그래서 끊긴 실행 A 뒤에 성공한 실행 B가 와도 A의 결과 불명은 10분 뒤부터 7일 동안 보인다(Codex 2차 #1)"
    - "`findLastEmailOutcome(viewer)`는 `email_claimed > 0`인 가장 최근 실행 기록의 `{ at, failed }`만 돌려준다 — 실패 수는 UI-SPEC U-6(「이메일을 보낸 마지막 tick」) 그대로이고, 결과 불명은 위 함수가 따로 센다"
    - "tick 요청 하나는 벽시계 예산 안에서 끝난다 — 예산의 기준 시각은 라우트 핸들러 진입(`handleNotifyTick`)이고, 요청 경로의 대기는 모두 묶인다: 풀 연결 대기 5초(`db/client.ts` `connectionTimeoutMillis`) · 토큰 검증 10초(`OIDC_VERIFY_DEADLINE_MS` 경주) · tick 경로의 DB 문장 하나하나는 자기 트랜잭션의 `SET LOCAL statement_timeout = '5s'`(공휴일 읽기·달력 잠금 04.2-06, tick 잠금 04.2-01, 상한 설정 읽기는 tick 잠금 트랜잭션 안, 이메일 트랜잭션 이 플랜) · tick 경로의 트랜잭션은 모두 BEGIN부터 COMMIT·ROLLBACK까지 04.2-01 `withDeadlineTransaction`의 클라이언트 마감 안에서 돌고 넘으면 연결을 파기한다(연결을 잡은 뒤 응답이 멈춰도 끝난다 — Codex 4차 E4). 트랜잭션 몫은 풀 대기 5 + 마감이다(tick 잠금 5 + 25 · 선점·결과 5 + 25 · 공휴일 빠른 길 5 + 5 · 생성 5 + 35 · 끝 표시 5 + 5). 이메일 단계 전 구간의 최악은 150초(토큰 10 + 영업일 판정 60 + 다음 해 시도 50 + tick 잠금 30 — 셈은 `<probe_fallback>` 「실행 예산」)이고, 이메일 단계는 `경과 + EMAIL_TX_WORST_MS(30초 = 풀 대기 5 + 트랜잭션 마감 25) + 발송 마감(EMAIL_SEND_DEADLINE_MS 30초) + 2초 + EMAIL_TX_WORST_MS ≤ NOTIFY_TICK_BUDGET_MS(160초)`일 때만 새 묶음을 선점한다. 발송은 `AbortSignal` 마감으로 끊기고(04.2-15), 발송기가 마감을 무시해도 이메일 단계가 마감 + 2초에 `indeterminate`·`DEADLINE`으로 끝낸다. 끝 표시는 10초(풀 대기 5 + 트랜잭션 마감 5) 안이다. 그래서 요청은 어느 경우든 170초 안 < 스케줄러 시도 마감 180초(04.2-04) < Cloud Run 300초다(Codex 2차 #6 · 3차)"
    - "발송 실패는 자동 재시도하지 않는다(0회, D-4213). 실패한 묶음의 알림 행은 `email_status = 'failed'`로 남아 알림함 보조 줄(04.2-09)과 관리자 배너(04.2-13)가 읽고, 실행 기록 `email_failed`가 그 tick의 실패 메일 수다"
    - "이메일 단계를 붙이면 SMTP가 없는 테스트 환경의 tick은 새 행을 `skipped_no_smtp`로 바꾼다 — 04.2-01 Task 1의 `email_status = 'pending'` 단언을 이 플랜이 `skipped_no_smtp`로 명시해 고친다(Codex 2차 #9)"
    - "한 사람의 같은 날 알림 N건은 메일 1통, 두 사람이면 2통이며 묶음은 recipient_id로만 나뉘어 다른 사람의 항목이 섞이지 않는다"
    - "그날 새 알림이 없는 사람에게는 메일을 보내지 않고, SMTP 환경 변수 4개 중 하나라도 비면 메일 0통·관리자 시스템 상태 「미설정」이다"
    - "묶음 메일 안 항목 순서는 알림 생성 시각 순이다 〔2026-09-25 #15: 최신 먼저 — 생성 시각 내림차순, 같은 시각이면 id 내림차순(알림함과 같은 순서)〕"
    - "같은 날 tick이 다시 돌아도(remaining 이어받기·재호출) 이미 보낸 (받는 사람, KST 날짜) 묶음 메일은 다시 보내지 않는다"
    - statement: "SMTP 발송이 tick 도중 예외로 끊기면 그 사람 묶음은 실패로 기록되고 알림함 항목은 그대로 남으며 관리자 배너에 실패가 보인다 — 재시도는 계획이 정한 정책대로만 한다"
      verification: backstop
    - "위 줄(엣지 #18 원문)의 「예외로 끊김」 중 서버가 거부했거나 메시지를 보내기 전에 끊긴 경우(`rejected`)가 「실패」다. 메시지를 보낸 뒤 확답 없이 끊긴 경우(`indeterminate` — RFC 5321 §4.5.3.2.6)는 실패가 아니라 `unknown`으로 기록되고 관리자 화면에 「결과 불명」으로 보인다 — 둘 다 알림함 항목은 그대로이고 재시도는 0회다(Codex 2차 #2가 엣지 원문의 범위를 좁힌다)"
    - "SMTP 비밀번호·호스트·사용자·받는 사람 주소·알림 내용은 로그에 실리지 않는다 — 발송 실패·결과 불명 로그는 04.2-15 `emailErrorCode` 허용 목록 안의 코드(`EAUTH`·`ECONNECTION`·`ETIMEDOUT`·`ESOCKET`·`EENVELOPE`·`EMESSAGE`·`ETLS`·`EDNS`·`DEADLINE`, 그 밖은 `UNKNOWN`)와 건수만 싣고 예외 메시지 원문을 싣지 않는다(Codex #5)"
  prohibitions:
    - statement: "한 통의 묶음 메일에 다른 받는 사람의 알림 항목이 섞여 들어가서는 안 된다 — 결재·지급 대상 같은 남의 업무 정보가 엉뚱한 사람의 메일함으로 새고 되돌릴 수 없다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/notify/email-phase.ts"
      provides: "한 묶음씩 선점 → 마감 있는 발송 → 세 결과 기록 · 요청 예산"
      exports: ["runEmailPhase", "NOTIFY_TICK_BUDGET_MS", "EMAIL_SEND_DEADLINE_MS", "EMAIL_TX_WORST_MS", "EMAIL_UNKNOWN_AFTER_MS", "EMAIL_UNKNOWN_VISIBLE_DAYS"]
    - path: "repositories/notifications.ts"
      provides: "이메일 선점(tick 잠금 키 · 미완 목록과 평가 실패 표시는 실행 기록에서) · 결과 기록 · 이메일 단계 끝 · 마지막 실패 · 결과 불명 묶음 — 쓰기 트랜잭션은 모두 클라이언트 마감(`withDeadlineTransaction`)"
      exports: ["markPendingEmailSkipped", "claimNextEmailBundle", "recordEmailOutcome", "finishEmailPhase", "findLastEmailOutcome", "findUnresolvedEmail", "NOTIFY_SHORT_TX_DEADLINE_MS"]
    - path: "db/client.ts"
      provides: "풀 연결 대기 5초 — 두 Pool 생성 경로에 `connectionTimeoutMillis`(Phase 4 04-32와 같은 상수·같은 줄, Codex 3차 #6)"
    - path: "repositories/settings.ts"
      provides: "`findSimpleValue(viewer, key, tx?)` 선택 tx — tick 잠금 트랜잭션 안에서 상한 설정을 읽는다(04-32와 같은 모양, Codex 3차 #6)"
      exports: ["findSimpleValue"]
    - path: "test/support/fake-email-sender.ts"
      provides: "보낸 메일을 모으고 받는 사람별로 결과(`rejected`·`indeterminate`)·예외·지연·마감 무시를 주입하는 가짜 발송기"
      exports: ["createFakeEmailSender"]
  key_links:
    - from: "app/internal/notify-tick/handle.ts"
      to: "domain/notify/tick.ts"
      via: "핸들러 진입 시각을 `runTick({ requestStartedAtMs })`로 넘긴다 — 예산의 기준"
      pattern: "requestStartedAtMs"
    - from: "domain/notify/tick.ts"
      to: "domain/notify/email-phase.ts"
      via: "잠금 트랜잭션 커밋 뒤, 영업일이고 발송기가 있으면 `runEmailPhase({ runId, dayStart, kstDate, requestStartedAtMs })` · 발송기가 null이면 `markPendingEmailSkipped`"
      pattern: "runEmailPhase"
    - from: "domain/notify/email-phase.ts"
      to: "repositories/notifications.ts"
      via: "`claimNextEmailBundle`(자기 마감 트랜잭션 · tick 잠금 키의 기다리는 잠금 · 최근 영업일 실행 기록의 미완 목록과 평가 실패 표시) → 마감 있는 발송 → `recordEmailOutcome`(행 + 실행 기록 한 트랜잭션) → `finishEmailPhase`"
      pattern: "claimNextEmailBundle"
    - from: "domain/notify/tick.ts"
      to: "repositories/settings.ts"
      via: "기본 `batchMax(tx)`가 `getSettingValue(NOTIFY_TICK_BATCH_MAX, undefined, { findSimpleValue: (v, k) => findSimpleValue(v, k, tx) })` — 상한 읽기가 tick 잠금 트랜잭션의 문장 한도 안에서 돈다"
      pattern: "findSimpleValue"
    - from: "domain/notify/tick.ts"
      to: "lib/email/sender.ts"
      via: "`TickDeps.emailSender` 기본값 `emailSenderFromEnv()`(04.2-15) — null이면 `skipped_no_smtp` 가지"
      pattern: "emailSenderFromEnv"
---

<objective>
**As a** 직원, **I want to** 그날 생긴 내 알림을 하루 한 통의 이메일로도 받기를, **so that** 앱을 열지 않은 날에도 결재·지급 같은 할 일을 놓치지 않고, 메일이 실패하거나 결과를 모르게 되어도 알림함과 관리자 화면에 그 사실이 남는다.

(스토리는 ROADMAP 04.2 Goal 줄 「하루 한 통 묶음 이메일로 알림을 받고」와 기준 3에서 끌어왔다. ROADMAP Goal 줄은 사용자 스토리 형식이 아니다 — 형식을 맞추려면 `/gsd mvp-phase 04.2`.)

Purpose: NOTI-02 — 이메일 채널(D-706 묶음 · D-711 SMTP 미설정 동작 · 가짜 SMTP 테스트 세 시나리오). CONTEXT Claude's Discretion의 「재시도 횟수」와 「계획이 정할 것」의 「상한과 하루 한 통을 함께 지키는 방식」의 이메일 절반. Codex #4(끊긴 발송의 결과 불명 기록)·#5(로그 비노출)·#9(실행 예산)·#17(미설정 표시가 하루 한 통을 막지 않음)과 Codex 2차 #1(결과 불명이 새 실행에 가려짐)·#2(모호한 SMTP 결과)·#3(겹친 tick의 낡은 미완 목록)·#6(요청 예산)·#9(04.2-01 단언)을 반영한다. 발송 어댑터·묶음 메일 모양·새 의존성 체크포인트는 04.2-15(wave 1)로 떼어 냈다.

Output: 이메일 단계(한 묶음씩 · 마감 있는 발송 · 세 결과) · 결과 기록·결과 불명 저장소 · 핸들러의 예산 기준 시각 · 가짜 발송기 · 통합 테스트.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 Skill 도구로 호출한다. 새 의존성 0개(`nodemailer`는 04.2-15가 체크포인트를 지나 이미 넣었다). 테스트는 실제 SMTP에 연결하지 않는다(가짜 발송기). 시크릿·실제 주소를 코드·테스트에 넣지 않는다(`@test.invalid`). `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-01-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-06-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-07-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-15-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — tick 흐름도(140~170행) · Anti-Patterns(313~318행) · Open Question 3(436~438행)
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 NOTI-02 adjacency(#14)·empty(#15)·ordering(#16)·idempotency(#17)·concurrency(#18, backstop)이다. #15의 「관리자 시스템 상태 「미설정」」 화면 절반과 #18의 「관리자 배너」 절반은 04.2-13이 화면에서 증명한다(이 플랜은 그 화면이 읽을 행·실행 기록·`findLastEmailOutcome`·`findUnresolvedEmail`을 만든다). 금지 회상 하나(다른 사람 항목 섞임 금지)를 통합 테스트로 묶는다. 정규 보안 쪽 회상 「SMTP 비밀 노출」은 truth 한 줄과 위협 T-4.2-81로 덮는다.

**계획 단계 결정(표면화):**

- **D-4203 하루 한 통을 지키는 방식(개정 — Codex #4·#9):** 첫 계획은 tick 잠금 트랜잭션 안에서 받는 사람 전부를 `failed`로 먼저 선점하고 커밋 뒤 보냈다. Codex #4 — 선점 뒤 프로세스가 죽으면 실행 기록 건수가 0인 채 메일이 영영 막히고 관리자 배너도 그 실행을 못 찾으며, SMTP가 수락한 뒤 죽으면 실제로는 갔을 메일이 「실패」로 남는다. Codex #9 — 전부를 한 번에 선점하면 요청 마감을 넘겨도 선점은 이미 되돌릴 수 없다. 그래서 이메일 단계(`domain/notify/email-phase.ts`)는 tick 잠금 트랜잭션이 **커밋된 뒤** 돈다: 예산 안에서 「`claimNextEmailBundle` → 없으면 끝 → 발송 → `recordEmailOutcome`」을 되풀이하고 끝에 `finishEmailPhase(runId)`. `claimNextEmailBundle(viewer, { runId, dayStart, now, deadlineMs? })`는 자기 짧은 클라이언트 마감 트랜잭션(04.2-01 `withDeadlineTransaction(deadlineMs ?? NOTIFY_TX_DEADLINE_MS 25초)` — BEGIN~COMMIT·ROLLBACK 전체, Codex 4차 E4)에서 `SET LOCAL lock_timeout = '5s'`·`statement_timeout = '5s'` 뒤 `pg_advisory_xact_lock(NOTIFY_TICK_LOCK_KEY)`(04.2-01 알림함 삽입의 `pg_try_advisory_xact_lock`과 **같은 키**의 기다리는 잠금 — 선점끼리, 그리고 선점과 알림함 삽입 트랜잭션을 한 줄로 세운다. 선점 전용 키는 두지 않는다)을 잡고, 같은 트랜잭션에서 `business_day = true`인 가장 최근 실행 기록(`id` 내림차순 첫 행)의 `incomplete_recipient_ids`를 읽은 뒤(Codex 2차 #3 — 메모리 속 목록은 겹친 tick에서 낡는다. tick 잠금이 inbox 트랜잭션을 한 번에 하나로 줄 세우므로 `id` 순서 = 커밋 순서이고, 알림 행과 그 미완 목록은 같은 커밋으로 함께 보인다. Codex 3차 추가 #1 — 첫 수정은 선점 전용 잠금만 잡아, READ COMMITTED에서 미완 목록을 읽은 문장과 행을 고르는 문장 사이에 다른 tick이 C의 일부 행과 `{C}`를 커밋하면 뒤 문장은 새 행을 보면서 낡은 목록을 썼다. 같은 잠금을 잡으면 그 사이에 삽입 트랜잭션이 커밋될 수 없다. 대가 둘: 선점 트랜잭션이 잠금을 쥔 짧은 동안 새로 온 tick의 시도형 잠금은 실패해 409로 끝나고(스케줄러가 다시 부른다 — 04.2-04), 선점은 다른 tick의 삽입 트랜잭션을 `lock_timeout` 5초까지만 기다린다 — 넘으면 이메일 단계가 오류로 끝나고 선점하지 못한 사람은 `pending` 그대로 다음 tick이 잇는다), 그 실행 기록의 `evaluation_failed`가 참이면(04.2-01 — 그 tick에서 조건 종류 하나라도 평가가 실패해 누구의 알림이 덜 들어갔는지 모른다) 아무도 고르지 않고 빈 결과를 돌려준다(Codex 4차 E3 — 모든 종류가 성공한 다음 tick의 실행 기록이 최신이 되면 다시 고른다. 같은 잠금 뒤에서 읽으므로 겹친 tick의 실패 표시도 놓치지 않는다), 아니면 「`pending` 행이 있고 · 보관되지 않았고(`users.archived_at IS NULL` — Codex 최종 B. 후보를 고르는 조건 안에서 `users`를 이어 거른다. 고른 뒤에 거르면 가장 오래된 pending인 보관된 사람이 매번 뽑혀 빈 결과로 루프가 끝나 다른 사람이 막힌다. 행 선점 문장 안의 조건이라 선점 트랜잭션의 문장 다섯은 그대로) · 오늘 `email_attempted_at >= dayStart`이면서 `email_status in ('sending','sent','failed','unknown')`인 행이 없고 · 그 미완 목록에 없는」 받는 사람 **한 명**(가장 오래된 pending 행 순)을 골라 그 사람의 `pending` 행 전부를 `sending` + `email_attempted_at = now`로 바꾸고 `email_claimed`를 1 늘린 뒤 `users.email`과 행 목록(`created_at`, `id` 순)을 돌려준다. 그래서 (가) 두 tick이 같은 사람을 동시에 선점할 수 없고(선점 잠금 + `sending`이 오늘 기록으로 세어진다), (나) 발송 도중 죽어도 「보냈다」·「실패했다」를 거짓 기록하지 않으며 다시 보내지도 않고, (다) SMTP 대기가 DB 트랜잭션을 붙잡지 않고, (라) 예산을 넘기면 아직 선점하지 않은 사람은 손대지 않은 채 남는다. 오늘 이미 메일을 받은 사람에게 같은 날 뒤 tick이 새 알림을 넣으면 그 행은 `pending`으로 남아 **다음 영업일 9시 묶음**에 들어간다. 보관된 사람의 `pending` 행은 선점되지 않아 상태·`email_attempted_at`이 그대로이고(`setUserArchived`는 행과 주소를 지우지 않는다 — `repositories/users.ts`), 복원되면 그 뒤 첫 tick의 묶음에 들어간다. 이 행들은 `findUnresolvedEmail`(`unknown`·10분 지난 `sending`만 센다)·`findLastEmailOutcome`(`email_claimed > 0` 실행)·실행 기록 email 건수 어디에도 잡히지 않아 결과 불명·실패로 잘못 보이지 않는다. SMTP 미설정 가지의 `markPendingEmailSkipped`는 보관 여부와 무관하게 `pending` 전부를 표시한다(어느 쪽도 메일 0통 — 복원·SMTP 켬 뒤 지난 알림을 몰아 보내지 않는 D-711 동작 그대로). 비영업일 tick은 이메일 단계도 돌지 않는다(D-709). 잠금을 못 잡은 tick(409)도 이메일 단계를 돌지 않는다.
- **D-4216 결과 기록과 결과 불명(개정 — Codex 2차 #1·#2):** `recordEmailOutcome(viewer, { runId, ids, outcome: "sent" | "failed" | "unknown", deadlineMs? })`는 한 클라이언트 마감 트랜잭션(`withDeadlineTransaction(deadlineMs ?? NOTIFY_TX_DEADLINE_MS)` · `lock_timeout`·`statement_timeout` 5s)에서 그 행들을 `sending` → `outcome`으로, 실행 기록 `email_sent`·`email_failed`·`email_unknown` 중 하나를 1 늘린다. 포트 결과 대응: `sent` → `sent`, `rejected` → `failed`, `indeterminate` → `unknown`(04.2-15 — DATA를 받은 서버의 확답이 끊긴 메일은 갔을 수 있다, RFC 5321 §4.5.3.2.6). 발송기가 예외를 던지면(포트 계약 위반) `unknown`·`UNKNOWN` — 갔을 수 있는 메일을 확정 실패로 적지 않는다. `recordEmailOutcome` 자체가 던지면(DB 장애 — 프로세스가 죽은 것과 같은 상태) 이메일 단계는 그 오류를 그대로 전파하고 `email_finished_at`을 찍지 않는다 — 행은 `sending`으로 남는다. 클라이언트 마감(`DbDeadlineError`)도 같은 가지다: 선점·결과 트랜잭션의 COMMIT 응답을 기다리다 마감이 오면 커밋 여부를 모르지만, 어느 쪽이든 행은 `sending`(선점만 커밋) 또는 결과 값으로 남고 `sending`은 10분 뒤 결과 불명으로 보이며 다시 보내지 않는다(Codex 4차 E4). **결과 불명의 표시는 실행 기록과 떨어진다(Codex 2차 #1):** 첫 계획은 `email_claimed > 0`인 가장 최근 실행 하나에서 결과 불명을 셌는데, 09:00 실행 A가 끊기고 09:01 실행 B가 다른 사람에게 성공하면 09:11에 A는 더는 「가장 최근」이 아니어서 한 번도 보이지 않는다. 그래서 `findUnresolvedEmail(viewer, { now, unknownAfterMs, visibleDays })` → `{ bundles: number; since: Date | null }` — 알림 행에서 `(email_status = 'unknown' OR (email_status = 'sending' AND email_attempted_at < now - EMAIL_UNKNOWN_AFTER_MS(10분)))`이고 `email_attempted_at >= now - EMAIL_UNKNOWN_VISIBLE_DAYS(7일)`인 행의 고유 `(recipient_id, email_attempted_at)` 수와 가장 이른 `email_attempted_at`. `findLastEmailOutcome(viewer)` → `{ at: started_at; failed: email_failed } | null`(`email_claimed > 0`인 가장 최근 실행 — 실패 수는 UI-SPEC U-6 그대로). **「명시적으로 풀 때까지」 대신 7일 창(작은 방법):** 리뷰는 결과 불명을 사람이 명시적으로 풀 때까지 남기라고 권한다. 그런 확인 컨트롤(버튼·서버 액션·행동 로그 종류)은 경고를 치우려고 관리자에게 결정 하나를 더 시키므로 만들지 않는다(사용성 원칙 — 확인 컨트롤보다 조용한 상태, 04.2-13에서 열린 질문을 닫았다). 대신 끊긴 실행 뒤 몇 번의 성공과 무관하게 7일(영업일 다섯 번 이상의 tick) 동안 보이고, 그 뒤에는 조용히 빠진다. 결과 불명 묶음은 자동으로 다시 보내지 않는다(받는 사람 메일함 확인이 먼저 — 두 번 받는 것이 안 받는 것보다 나은지 판단은 사람 몫, D-4213).
- **D-4213 재시도 0회:** RESEARCH OQ3 권고대로 자동 재시도는 없다. 알림함 행은 이미 성공했고, Phase 7의 실제 조건은 여전히 참이면 다음 회차(`round`)로 다시 나온다. 실패 수는 메일(받는 사람) 단위로 센다 — 실행 기록 `email_sent`·`email_failed`, 관리자 배너 「이메일 발송 실패 N건」의 N이 이것이다.
- **SMTP 미설정(Codex #17):** 발송기가 null이면 이메일 단계 대신 `markPendingEmailSkipped(viewer)` — `pending` 전부를 `skipped_no_smtp`로 바꾸되 `email_attempted_at`은 찍지 않는다. 하루 한 통 판정은 `sending`·`sent`·`failed`·`unknown`만 세므로 그날 SMTP를 켜면 그 뒤 새 알림은 그날 나간다.
- **실행 예산(개정 — Codex #9 · Codex 2차 #6 · 3차 #6 잔여):** 첫 계획의 증명(「예산 120초 + nodemailer 시간 제한 합 40초 < 180초」)은 틀렸다 — `socketTimeout`은 유휴 한도라 조금씩 오는 응답이 한 통을 끝없이 붙잡고, DNS·이메일 단계 전의 평가·DB 시간이 빠졌으며, 가짜 시계 테스트는 루프가 멈추는 것만 보였다. 2차 수정은 발송을 묶었지만 이메일 단계 전 읽기와 풀 연결 대기를 묶지 않고 04-32에 기댔다(3차). 개정: (1) **기준 시각** = `handleNotifyTick` 진입(`performance.now()`) — 토큰 검증(Google 인증서 조회)·영업일 판정·알림함 삽입 시간이 모두 예산에 든다. 핸들러가 `runTick({ requestStartedAtMs })`로 넘기고 `TickDeps.monotonicNow`(기본 `performance.now`)로 잰다. (2) **모든 대기의 한도(이 페이즈 안에서 — 04-32에 기대지 않는다):** 풀 연결 대기는 `db/client.ts`의 `connectionTimeoutMillis` 5초(두 Pool 생성 경로 — 04-32 커밋 b395483과 같은 상수 이름 `POOL_CONNECTION_TIMEOUT_MS = 5000`·같은 자리라 합칠 때 한 벌로 겹친다). tick 경로의 DB 문장은 모두 자기 트랜잭션의 `SET LOCAL statement_timeout = '5s'`·`lock_timeout = '5s'` 아래서 돈다 — 공휴일 빠른 길 읽기와 달력 잠금 트랜잭션(04.2-06), tick 잠금 트랜잭션(04.2-01 — 5s로 낮춤), 상한 설정 읽기(이 플랜이 tick 잠금 트랜잭션 안으로 옮긴다 — `findSimpleValue(viewer, key, tx?)`, 04-32와 같은 모양), 이메일 트랜잭션 넷(이 플랜). 토큰 검증은 핸들러가 `OIDC_VERIFY_DEADLINE_MS = 10_000` 타이머와 경주시킨다(인증서 조회는 부작용 없는 GET이라 늦은 응답을 버려도 된다 — 넘으면 500). **연결을 잡은 뒤의 응답 정지(Codex 4차 E4):** 서버 쪽 한도는 연결이 응답을 멈추면 끊지 못하고(설치된 `pg`의 `query_timeout` 기본 꺼짐 — `node_modules/pg/lib/defaults.js` 76행), Drizzle은 BEGIN·COMMIT·오류 뒤 ROLLBACK 응답도 기다린다 — 그래서 tick 경로의 트랜잭션은 모두 04.2-01 `withDeadlineTransaction(deadlineMs, …)`으로 연다: BEGIN부터 COMMIT·ROLLBACK까지 전부가 마감 하나 안이고, 넘으면 그 연결을 풀에서 빼며 파기해(`release(err)` → pg가 기다리던 쿼리의 소켓을 끊는다) `DbDeadlineError`로 끝난다. 마감 값은 아래 (3)·(4)·(6)의 트랜잭션 몫에서 풀 대기 5초를 뺀 값이라 셈이 그대로다. (3) **이메일 단계 전 최악(초):** 토큰 10 · 영업일 판정 60(빠른 길 읽기 10[풀 5 + 마감 5] + 그 해 생성 트랜잭션 40[풀 5 + 마감 35 — 잠금 대기 5 · 문장 여섯 × 5, 04.2-06] + 날짜 읽기 10[풀 5 + 마감 5]) · 다음 해 시도 50(빠른 길 10 + 생성 40) · tick 잠금 트랜잭션 30(풀 5 + 마감 25 — 잠금 시도 · 상한 읽기 · 기존 키 · 삽입 · 실행 기록, 04.2-01) = 150. BEGIN·SET·COMMIT·ROLLBACK도 각 트랜잭션의 마감 안이다. 조건 종류가 평가 문장을 더하는 Phase 7이 이 합을 다시 잰다. (4) **선점 조건** = `monotonicNow() - requestStartedAtMs + EMAIL_TX_WORST_MS + EMAIL_SEND_DEADLINE_MS + 2_000 + EMAIL_TX_WORST_MS ≤ NOTIFY_TICK_BUDGET_MS` — `NOTIFY_TICK_BUDGET_MS = 160_000` · `EMAIL_SEND_DEADLINE_MS = 30_000` · `EMAIL_TX_WORST_MS = 30_000`(선점·결과 트랜잭션 각각 — 풀 대기 5 + 클라이언트 마감 `NOTIFY_TX_DEADLINE_MS` 25[문장 다섯 이하 × 5: 선점은 잠금 대기 · 미완 목록과 평가 실패 표시 · 행 선점 · 건수 · 주소 읽기]). 조건을 넘기면 남은 받는 사람은 `pending` 그대로 다음 tick으로 간다. (5) **한 통의 마감** = `AbortSignal.timeout(EMAIL_SEND_DEADLINE_MS)`를 발송기에 넘긴다(04.2-15가 DNS·연결·TLS·로그인·데이터를 모두 이 안에서 끝내고 `close()`한다). 발송기가 신호를 무시해도 이메일 단계가 발송 프라미스를 `마감 + 2초` 타이머와 경주시켜 `indeterminate`·`DEADLINE`으로 기록하고 다음으로 간다. (6) **끝 표시** `finishEmailPhase`·`markPendingEmailSkipped`는 각각 풀 대기 5 + 클라이언트 마감 `NOTIFY_SHORT_TX_DEADLINE_MS` 5(문장 하나) = 10초. 그래서 마지막 선점은 경과 68초 안에서만 시작하고(선점 30 + 발송 32 + 결과 30 = 92 → 160) 끝 표시까지 170초, 이메일 단계 전이 최악 150초면 선점 없이 끝 표시까지 160초 — 어느 경우든 170초 < 180초(스케줄러 시도 마감, 04.2-04) < 300초(Cloud Run 요청 제한). 알림함 삽입 쪽 경계는 04.2-01의 엄격한 상한, 조건 평가 쪽 경계는 Phase 7(04.2-01 Review Dispositions #9).
- **묶음 크기(Codex 1차 #9 잔여 · 3차):** 선점은 그 사람의 `pending` 행 전부를 잡는다(여러 날 쌓였어도 — D-706 「알림을 한 통에 모은다」를 행 상한으로 쪼개지 않는다). 메일 크기는 04.2-15 `composeDigest`가 묶는다: 받은 순서(선점이 넘기는 `created_at DESC, id DESC` — 최신 먼저, 2026-09-25 #15)의 앞 `DIGEST_MAX_LISTED`(20)건만 `- ` 줄로 적고 나머지는 `외 {M}건은 알림함에서 확인` 한 줄 — 본문 줄 수는 22 + 머리·꼬리로 고정이고 한 줄의 길이는 알림 내용(조건 종류가 만든 한 줄 — 04.2-15가 줄바꿈을 공백으로 편다)이다. 선점 UPDATE가 건드리는 행 수는 문장 한도 5초가 묶는다. 묶음 메일 모양은 CONTEXT Claude's Discretion이지만 UI-SPEC 카피 계약에 적혀 있어, 그 한 줄은 04.2-12 「UI-SPEC 변경 요청」으로 디자인 게이트에 넘긴다(04.2-15 Task 2 precondition).
- **로그 비노출(Codex #5):** 발송 결과에서 로그에 남기는 것은 포트 결과의 `code`(04.2-15 `emailErrorCode`를 이미 거친 값)와 결과 종류뿐이다 — `log.error("notify.email_failed", { code })` · `log.warn("notify.email_unknown", { code })`. 발송기가 던진 예외는 `emailErrorCode(err)`만 남긴다. 예외 메시지·응답 문자열은 받는 사람 주소·호스트·사용자·서버 응답을 담을 수 있어 어떤 로그에도 싣지 않는다.
- **메일 모양 · 어댑터 · 새 의존성:** 04.2-15(wave 1)로 옮겼다 — 이 플랜은 `composeDigest`·`emailSenderFromEnv`·`EmailSender`를 가져다 쓰기만 한다. 서비스 URL은 `BETTER_AUTH_URL`.
- **04.2-01 단언 고치기(Codex 2차 #9):** 04.2-01 Task 1의 트레이서 통합은 새 행을 `email_status = 'pending'`으로 단언한다 — 이 플랜의 tick은 SMTP가 없는 테스트 환경(발송기 null)에서 `markPendingEmailSkipped`를 돌리므로 그 값이 `skipped_no_smtp`가 된다. Task 1이 그 한 단언을 `skipped_no_smtp`로 바꾸고 이유를 테스트 주석 한 줄로 남긴다(그 밖의 04.2-01 케이스는 고치지 않는다).
</probe_fallback>

## Review Dispositions

| Codex # | 처리 | 한 줄 |
|---|---|---|
| 4 (high) | 반영 | 한 묶음씩 선점(`sending` + `email_claimed`) · 결과와 건수 한 트랜잭션 · `email_finished_at` · 결과 불명 = 끊긴 실행(자동 재발송 없음) · `findLastEmailOutcome`은 `email_claimed > 0`로 찾음 · 발송 전/수락 뒤/기록 전 끊김과 SMTP가 막힌 동안의 동시 tick 통합(Task 2·3). 화면 표시는 04.2-13 |
| 5 (high) | 반영 | 로그는 허용 목록 오류 코드와 건수만 · 받는 사람·호스트·사용자·비밀번호·본문을 담은 예외를 주입해 로그 부재 단언(Task 2) · 어댑터 쪽 비노출은 04.2-15 |
| 9 (medium) — 이메일 쪽 | 반영(2차 #6으로 개정) | 선점은 핸들러 진입 기준 요청 예산 안에서만(첫 계획의 120초 증명은 2차 #6이 반박 — `<probe_fallback>` 「실행 예산」) · 미완 받는 사람 제외(04.2-01) · 예산 초과 시 남은 사람은 `pending`(Task 3). 스케줄러 마감 고정은 04.2-04 |
| 17 (medium) | 반영 | `skipped_no_smtp`는 `email_attempted_at` 없이 · 하루 한 통 판정은 `sending`·`sent`·`failed`·`unknown`만 · 미설정 tick → SMTP 켬 → 같은 날 새 알림 발송 통합(Task 2) |

### Review Dispositions (Codex 2차)

| Codex 2차 # | 처리 | 한 줄 |
|---|---|---|
| 1 (high) | 반영(작은 방법) | 결과 불명을 실행 기록이 아니라 알림 행에서 센다(`findUnresolvedEmail` — `unknown` 행 + 10분 지난 `sending` 행, 7일 창) · 끊긴 A → 성공 B → +11분에 보임 · +7일에 빠짐 통합(Task 3). 「명시적으로 풀 때까지」는 반려 — 확인 컨트롤은 UI-SPEC 밖 새 화면 요소·액션·행동 로그라 이 수정에서 만들지 않고, 04.2-13에 /plan-design-review 열린 질문으로 남긴다 — 3차에서 닫음: 사용성 원칙으로 만들지 않는다(D-4216 · 04.2-13) |
| 2 (high) — 원장 쪽 | 반영 | 포트 세 결과(04.2-15) → 행 `sent`·`failed`·`unknown` + 실행 기록 `email_unknown` · 발송기 예외는 `unknown` · 엣지 #18 원문은 그대로 두고 범위를 좁히는 truth 한 줄 · DATA 뒤 끊김(가짜 발송기 `indeterminate`) 통합(Task 3) |
| 3 (medium) | 반영 | 선점 트랜잭션이 최근 영업일 실행 기록의 `incomplete_recipient_ids`를 읽는다(메모리 속 `excludeRecipientIds` 삭제) · 겹친 tick A/B와 반쯤 들어간 C 통합(Task 3) |
| 5 (medium) — 크기 | 반영 | 어댑터·묶음 메일·의존성 체크포인트를 04.2-15(wave 1)로 분리 — 이 플랜은 7파일 · 체크포인트 없음 |
| 6 (medium) | 반영 | 핸들러 진입 기준 요청 예산 160초 · 한 통 벽시계 마감 30초(`AbortSignal`, 무시해도 +2초 경주) · 이메일 트랜잭션 `lock_timeout`·`statement_timeout` 5s · 예산 식과 170초 < 180초 증명(`<probe_fallback>`) · 마감을 무시하는 발송기·흐르는 시계 통합(Task 3). 조금씩 응답하는 실제 소켓 테스트는 04.2-15 |
| 9 (medium) — 단언 모순 | 반영 | 04.2-01 Task 1의 `email_status = 'pending'` 단언을 Task 1이 `skipped_no_smtp`로 명시해 고친다 · `test/integration/notify-tick.test.ts`를 이 플랜 파일에 넣음 |

### Review Dispositions (Codex 3차)

| Codex 3차 # | 처리 | 한 줄 |
|---|---|---|
| 추가 #1 (medium) — 미완 목록과 선점 행의 시점 | 반영(같은 잠금) | 선점 전용 키를 없애고 선점이 04.2-01 삽입과 같은 `NOTIFY_TICK_LOCK_KEY`를 기다리는 형으로 잡는다 — 잠금을 쥔 동안 삽입 트랜잭션이 커밋될 수 없어 미완 목록과 행이 한 시점이다. 한 문장 CTE는 장벽 테스트를 걸 자리가 없어 쓰지 않았다. 대가(선점 중 새 tick 409 · 선점 잠금 대기 5초 한도)는 `<probe_fallback>` D-4203. 장벽 테스트: B가 잠금을 쥔 채 멈춤 → A의 선점이 advisory 대기로 보임 → B가 C 일부 + `{C}` 커밋 → A가 C를 고르지 않음(Task 3) |
| NEW #6 잔여 (medium) — 요청 예산 | 반영(이 페이즈 안에서) | 04-32에 기대지 않고 풀 대기 5초(`db/client.ts` — 04-32와 같은 상수·같은 줄, 더하기만) · 토큰 검증 10초 경주 · 상한 설정 읽기를 tick 잠금 트랜잭션 안으로(`findSimpleValue` tx — 04-32와 같은 모양) · tick 경로 모든 트랜잭션 5s(04.2-01을 30s → 5s · 04.2-06 공휴일 읽기) · `EMAIL_TX_WORST_MS` 30초 · 최악 합 170초 < 180초 · 풀·문장(`holidays`·`settings_simple` 잠금)·토큰 지연 통합(Task 3). 합칠 때는 04.2-14 Task 1 ② |
| Old #9 잔여 (medium) — 묶음 크기 | 반영(본문 상한) | 선점은 여러 날의 `pending` 전부(D-706 — 한 통에 모은다, 행 상한으로 쪼개지 않는다) · 메일은 04.2-15 `composeDigest`가 앞 20건 + `외 {M}건은 알림함에서 확인` · 사흘 45건 통합(Task 2) · 2026-09-25 #15: 앞 20건 = 최신 20건(묶음 행 `created_at DESC, id DESC`) · 첫 줄은 발송일. 04.2-01의 거짓 처리 줄은 04.2-01이 고친다 |

### Review Dispositions (Codex 4차 · eng review 2026-09-24)

| # | 처리 | 한 줄 |
|---|---|---|
| E3 (blocker) — 평가 실패 뒤 반쯤 찬 묶음 | 반영 | 선점이 같은 잠금 뒤에서 가장 최근 영업일 실행 기록의 `evaluation_failed`(04.2-01)를 읽어 참이면 선점 0 — 전원 보류, 모든 종류가 성공한 다음 tick 뒤 재개(D-4203) · `부분 삽입 → 평가 실패 → 복구` 통합(Task 3) |
| E4 (blocker) — 멈춘 DB 응답 · 예산 | 반영 | 이메일 트랜잭션 넷을 04.2-01 `withDeadlineTransaction`으로(선점·결과 25초 · 끝 표시 5초) · 「BEGIN·SET·COMMIT은 세지 않는다」를 지우고 모든 트랜잭션 몫을 풀 5 + 마감으로 다시 적음(합 150·170초 그대로) · `TickDeps.txDeadlineMs` → 이메일 단계 · 결과 기록 응답 정지 통합(Task 3). 도우미 자체의 BEGIN·ROLLBACK·COMMIT 정지 테스트는 04.2-01 Task 2 |

### Review Dispositions (Codex 최종 전체 리뷰)

| # | 처리 | 한 줄 |
|---|---|---|
| B (blocker) — 보관된 사람에게 메일 | 반영(리뷰 제안 그대로) | 선점 후보 조건에 `users.archived_at IS NULL`(행 선점 문장 안 — 문장 수 그대로) · 고른 뒤가 아니라 고르는 조건 안에서 걸러 다른 사람을 막지 않음 · 보관된 사람의 `pending`은 그대로, 복원 뒤 발송 · `findUnresolvedEmail`·`findLastEmailOutcome`·실행 기록 건수는 영향 없음(D-4203) · 「보관」 통합(Task 2) · T-4.2-82 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 검증된 호출(핸들러 진입 시각) → tick → 받는 사람 A의 알림 3건 → 한 묶음 선점(sending) → 마감 있는 발송 1통(가짜 발송기 `sent`) → 결과 기록(sent · email_sent 1) → 이메일 단계 끝 표시, SMTP 미설정이면 0통 + `skipped_no_smtp`(시도 시각 없음)</name>
  <files>domain/notify/email-phase.ts, domain/notify/tick.ts, repositories/notifications.ts, app/internal/notify-tick/handle.ts, test/support/fake-email-sender.ts, test/integration/notify-email.test.ts, test/integration/notify-tick.test.ts, db/client.ts, repositories/settings.ts</files>
  <precondition>04.2-15가 끝나 `lib/email/sender.ts`에 `SendOutcome`·`emailSenderFromEnv`가, `domain/notify/digest.ts`에 `composeDigest`가 있다(`grep -q "SendOutcome" lib/email/sender.ts && grep -q "composeDigest" domain/notify/digest.ts`)</precondition>
  <read_first>
    - `domain/notify/tick.ts` 전체(04.2-01·06 결과 — `TickDeps` · `withNotifyTickLock` 콜백 흐름 · 실행 기록 · `TickResult.incompleteRecipientIds`·`runId` · `notify.tick` 로그)
    - `repositories/notifications.ts` 전체(04.2-01·07 결과 — `insertTickRun` · `withNotifyTickLock`의 지역 tx 타입과 `SET LOCAL` · `NOTIFY_TICK_LOCK_KEY` · `NOTIFY_TX_DEADLINE_MS`) · `db/deadline-transaction.ts`(04.2-01 — `withDeadlineTransaction` · `DbDeadlineError`)
    - `db/schema/notifications.ts` — `email_status` CHECK 여섯 값 · `email_attempted_at` · 실행 기록 `email_claimed`·`email_sent`·`email_failed`·`email_unknown`·`email_finished_at`·`incomplete_recipient_ids`
    - `lib/email/sender.ts`·`domain/notify/digest.ts`(04.2-15 결과 — `EmailSender.send(message, { signal })` · `SendOutcome` · `emailErrorCode` · `composeDigest`)
    - `app/internal/notify-tick/handle.ts`(04.2-01·05 결과 — `NotifyTickHandlerDeps.runTick` 호출 자리)
    - `test/integration/notify-tick.test.ts` · `test/support/notify-tick.ts`(04.2-01 — 사용자 만들기 · 테스트 조건 종류 주입 · KST 수요일 `now` · Task 1의 `email_status = 'pending'` 단언)
    - `db/client.ts`(두 Pool 생성 경로) · `repositories/settings.ts` `findSimpleValue` · `domain/settings/registry.ts` 68행 `getSettingValue(def, opts?, deps?)`의 `findSimpleValue` 주입 자리 · 04-32 커밋 `git show b395483 -- db/client.ts` · `git show c9e70ea -- repositories/settings.ts`(같은 줄로 옮길 원본)
  </read_first>
  <behavior>
    - 통합: 가짜 발송기 + 테스트 조건 종류(A에게 3건) + KST 수요일 → tick 200 `{ sent: 3 }` · 메일 1통(to = A의 이메일, 제목 `PLANT8 알림 3건`) · 발송기가 받은 `signal`이 중단되지 않은 `AbortSignal`이다 · A의 세 행 `email_status = 'sent'`·`email_attempted_at` 있음 · 실행 기록 `email_claimed = 1`·`email_sent = 1`·`email_failed = 0`·`email_unknown = 0`·`email_finished_at` 있음 · `listMyNotifications(A)` 3건
    - 통합: `emailSender: null`(SMTP 미설정) → 메일 0통 · 세 행 `skipped_no_smtp` · 세 행 `email_attempted_at` null · 실행 기록 `email_claimed = 0`
    - 통합(핸들러 기준 시각): `handleNotifyTick`을 부르면 `runTick`이 받은 `requestStartedAtMs`가 핸들러 진입 때 잰 값이다(주입한 `monotonicNow`로 단언)
    - 04.2-01 트레이서 단언 고침(Codex 2차 #9): `test/integration/notify-tick.test.ts`의 새 행 `email_status` 기대값이 `skipped_no_smtp`이고 그 파일 전체가 초록이다
  </behavior>
  <action>
① **RED.** `test/integration/notify-email.test.ts`(신규)에 behavior를 쓴다. 가짜 발송기 `test/support/fake-email-sender.ts`(신규): `createFakeEmailSender(opts?: { outcomeFor?: ReadonlyMap<string, SendOutcome>; throwFor?: ReadonlyMap<string, Error>; blockFor?: ReadonlyMap<string, Promise<void>>; ignoreSignal?: boolean })` → `{ sender: EmailSender; sent: EmailMessage[] }` — 받는 사람 주소별로 정해진 결과를 돌려주거나(기본 `sent`), 오류를 던지거나, 프라미스가 풀릴 때까지 기다린다. 기다리는 동안 `signal`이 중단되면 `{ outcome: "indeterminate", code: "DEADLINE" }`로 끝난다(04.2-15 계약과 같게). `ignoreSignal`이면 신호를 보지 않는다(Task 3의 경주 테스트용). 통합은 04.2-01 도우미로 사용자를 만들고 이메일은 `@test.invalid` 주소다. `test/integration/notify-tick.test.ts`의 04.2-01 트레이서 단언 한 줄을 `skipped_no_smtp`로 바꾸고 주석 한 줄(「04.2-10 이메일 단계 — SMTP 없는 테스트 환경」)을 단다(per Codex 2차 #9 — 다른 줄은 고치지 않는다).

② **저장소**(`repositories/notifications.ts`에 함수만 더한다, 첫 인자 viewer): `markPendingEmailSkipped(viewer)` — `pending` 전부 → `skipped_no_smtp`(`email_attempted_at`을 건드리지 않는다) · `claimNextEmailBundle(viewer, { runId, dayStart, now })` — 잠금은 04.2-01의 `NOTIFY_TICK_LOCK_KEY`를 `pg_advisory_xact_lock`(기다리는 형)으로 잡는다(선점 전용 키를 새로 두지 않는다 — per Codex 3차 추가 #1) · `recordEmailOutcome(viewer, { runId, ids, outcome })` · `finishEmailPhase(viewer, { runId, now })` — `<probe_fallback>` D-4203·D-4216 그대로(per Codex 2차 #2·#3 · 4차 E3 — 선점은 가장 최근 영업일 실행 기록의 `evaluation_failed`가 참이면 빈 결과), 네 함수 모두 각자 04.2-01의 `withDeadlineTransaction`으로 자기 트랜잭션을 열고(선점·결과는 `deadlineMs ?? NOTIFY_TX_DEADLINE_MS`, 끝 표시·미설정 표시는 새 상수 `NOTIFY_SHORT_TX_DEADLINE_MS = 5_000` — per Codex 4차 E4) 첫 문장으로 `SET LOCAL lock_timeout = '5s'`·`SET LOCAL statement_timeout = '5s'`(per Codex 2차 #6). `dayStart`는 KST 오늘 0시에 해당하는 순간이다.

③ **이메일 단계** `domain/notify/email-phase.ts`(신규): 상수 `NOTIFY_TICK_BUDGET_MS = 160_000` · `EMAIL_SEND_DEADLINE_MS = 30_000` · `EMAIL_TX_WORST_MS = 30_000`(풀 대기 5 + 트랜잭션 마감 25 — per Codex 3차 #6 · 4차 E4) · `EMAIL_UNKNOWN_AFTER_MS = 600_000` · `EMAIL_UNKNOWN_VISIBLE_DAYS = 7`(마지막 둘은 저장소 결과 불명 조회가 쓴다 — 도메인 상수를 인자로 넘긴다) · `runEmailPhase(input: { runId; kstDate; dayStart; requestStartedAtMs }, deps: { sender: EmailSender; serviceUrl: string; now: () => Date; monotonicNow: () => number; sendDeadlineMs?: number; txDeadlineMs?: number; claim?; recordOutcome?; finish? })`(`txDeadlineMs`는 선점·결과 트랜잭션의 `deadlineMs`로 넘긴다 — per Codex 4차 E4) → `{ emailSent, emailFailed, emailUnknown }` — `<probe_fallback>` D-4203의 되풀이와 「실행 예산」(4)·(5)(per Codex 2차 #6). 이 Task에서는 성공 길(`sent` → `sent`)과 선점 조건 식까지 만들고, `rejected`·`indeterminate`·예외·마감 경주는 Task 2·3이 behavior로 몰아붙인다. 끝에 `finish`.

④ **tick** `domain/notify/tick.ts`: `TickDeps`에 `emailSender: EmailSender | null`(기본 `emailSenderFromEnv()`)·`serviceUrl: string`(기본 `env.BETTER_AUTH_URL`)·`requestStartedAtMs: number`(기본 `runTick` 진입 때의 `monotonicNow()`)·`monotonicNow: () => number`(기본 `performance.now`)를 더한다. 잠금 콜백이 커밋된 뒤, 영업일이고 `status: "ok"`일 때만: 발송기가 null이면 `markPendingEmailSkipped`, 있으면 `runEmailPhase({ runId, kstDate, dayStart, requestStartedAtMs }, …)`(04.2-01의 `TickDeps.txDeadlineMs`를 이메일 단계 deps의 `txDeadlineMs`로도 넘긴다 — per Codex 4차 E4). `notify.tick` 로그에 `emailSent`·`emailFailed`·`emailUnknown`을 더한다. HTTP 응답 모양 `{ sent, skipped, remaining }`은 바꾸지 않는다. `TickResult.incompleteRecipientIds`는 이메일 단계에 넘기지 않는다(선점이 실행 기록에서 읽는다 — per Codex 2차 #3). `TickDeps.batchMax`를 `(tx) => Promise<number>`로 바꾸고 잠금 콜백 안에서 그 tx로 부른다 — 기본값은 `getSettingValue(NOTIFY_TICK_BATCH_MAX, undefined, { findSimpleValue: (v, k) => findSimpleValue(v, k, tx) })`(풀 연결을 하나 더 잡지 않고 tick 잠금 트랜잭션의 문장 한도 안에서 읽는다 — per Codex 3차 #6. 테스트가 주입하는 `async () => 3`은 그대로 맞는다).

⑤ **핸들러** `app/internal/notify-tick/handle.ts`: 함수 첫 줄에서 `const requestStartedAtMs = performance.now()`(deps로 바꿀 수 있게 `NotifyTickHandlerDeps.monotonicNow`)를 재고, 성공 가지의 `runTick()` 호출을 `runTick({ requestStartedAtMs })`로 바꾼다(per Codex 2차 #6 — 예산 기준이 토큰 검증 시간을 포함한다). 토큰 검증 호출은 `OIDC_VERIFY_DEADLINE_MS = 10_000`(파일 상단 상수, `NotifyTickHandlerDeps.oidcDeadlineMs`로 줄일 수 있다) 타이머와 경주시켜, 넘으면 오류를 던져 기존 예외 가지(500 `tick_failed`)로 보낸다(per Codex 3차 #6 — 인증서 조회는 부작용 없는 GET). 그 밖의 줄은 고치지 않는다.

⑥ **공용 파일 두 곳(더하기만 — Phase 4 04-32와 같은 줄, per Codex 3차 #6):** `db/client.ts` — 파일 상단 상수 `POOL_CONNECTION_TIMEOUT_MS = 5000`과 두 `new Pool({…})`에 `connectionTimeoutMillis: POOL_CONNECTION_TIMEOUT_MS` 한 줄씩(04-32 커밋 b395483과 같은 이름·값·자리 — `git show b395483 -- db/client.ts`로 확인하고 그대로 옮긴다). `repositories/settings.ts` — `findSimpleValue(viewer, key, tx?: DbOrTx)`로 선택 인자를 더하고 본문의 `db`를 `(tx ?? db)`로(04-32 커밋 c9e70ea와 같은 모양 — `git show c9e70ea -- repositories/settings.ts`). 두 파일의 다른 줄은 고치지 않는다. 합칠 때의 처리는 04.2-14 Task 1 ②.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-email.test.ts test/integration/notify-tick.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 3건이 메일 3통으로 나갔거나, 행이 sent로 바뀌지 않았거나, 실행 기록 email_claimed·email_sent가 1이 아니거나 email_finished_at이 비었거나, SMTP 미설정인데 메일이 나갔거나 skipped 행에 시도 시각이 찍혔거나, 핸들러 진입 시각이 runTick에 가지 않았거나, 04.2-01 트레이서의 `email_status` 단언을 고치지 않아 notify-tick 통합이 깨졌거나, 기존 tick 통합(응답 모양·상한·발생 계약·공휴일)이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint(경계 — domain이 lib만 import · 저장소 viewer 인자)·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력이 비어 있다(이 플랜의 새 의존성 0개)
    - `grep -c "excludeRecipientIds" domain/notify/email-phase.ts domain/notify/tick.ts repositories/notifications.ts`가 모두 0이다
    - `grep -c "requestStartedAtMs" app/internal/notify-tick/handle.ts`가 1 이상이고 `grep -c "OIDC_VERIFY_DEADLINE_MS" app/internal/notify-tick/handle.ts`가 1 이상이다
    - `grep -c "connectionTimeoutMillis: POOL_CONNECTION_TIMEOUT_MS" db/client.ts`가 2이고 `git diff --stat -- db/client.ts repositories/settings.ts`가 더한 줄 위주다(두 파일의 기존 줄 삭제는 `repositories/settings.ts`의 import · `findSimpleValue` 시그니처 · 본문 세 줄뿐 — 04-32 c9e70ea와 같은 줄, 검토)
    - `grep -n "pg_advisory_xact_lock\|pg_try_advisory_xact_lock" repositories/notifications.ts`의 모든 줄이 `NOTIFY_TICK_LOCK_KEY`를 쓴다(선점 전용 키 없음 — 검토, per Codex 3차 추가 #1)
    - `grep -v '^\s*//' repositories/notifications.ts | grep -c "db\.transaction("`이 0이고 `grep -c "withDeadlineTransaction" repositories/notifications.ts`가 5 이상이다(tick 잠금 + 이메일 트랜잭션 넷이 모두 클라이언트 마감 안 — per Codex 4차 E4)
    - `grep -n "email_status" test/integration/notify-tick.test.ts`에 `pending` 기대 단언이 남지 않았다(`skipped_no_smtp`로 바뀜 — 검토)
    - 통합: A 3건 → 메일 1통 · 알림함 3건 · 미설정 → 0통(D-711 테스트 원문 두 시나리오)
  </acceptance_criteria>
  <done>SMTP가 설정되면 tick이 받는 사람마다 한 통을 한 묶음씩 선점해 마감 안에서 보내고 결과와 건수를 함께 기록하며, 설정이 없으면 메일 없이 알림함으로만 가고, 예산의 기준 시각이 핸들러 진입이다</done>
  <reversibility rating="costly">D-706 발송 단위(사람 × KST 날짜)가 선점·결과 기록·실행 기록 email 건수·관리자 배너 N의 단위가 된다. 바꾸려면 D-4203 선점 조건과 04.2-13 배너 문구의 뜻이 함께 바뀐다</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 격리·순서·멱등·다음 영업일 · 확정 실패(재시도 0 · 로그 비노출) · 미설정 뒤 같은 날 켬 · SMTP가 막힌 동안의 동시 tick · 비영업일·잠금 경합</name>
  <files>domain/notify/email-phase.ts, domain/notify/tick.ts, repositories/notifications.ts, test/integration/notify-email.test.ts, test/support/fake-email-sender.ts</files>
  <read_first>
    - `domain/notify/email-phase.ts` · `tick.ts` · `repositories/notifications.ts`(Task 1 결과)
    - `test/integration/notify-email.test.ts` · `test/support/fake-email-sender.ts`(Task 1 결과)
    - `domain/notify/inbox.ts`(04.2-07) — `listMyNotifications` DTO의 `emailStatus`(알림함 보조 줄이 읽는 값)
    - 엣지 #14~#18 원문(이 플랜 must_haves)
  </read_first>
  <behavior>
    - 격리(엣지 #14 · 금지 회상): A 2건 · B 1건 → 메일 2통 · A의 메일 본문에 B의 내용 문자열이 없고 그 반대도 같다 · 각 메일의 to가 그 사람 주소뿐이다
    - 빈 사람(엣지 #15): 알림이 없는 C에게는 메일이 없다 · SMTP 미설정(발송기 null)이면 메일 0통 · 실행 기록 `email_claimed = 0`
    - 순서(엣지 #16 · 2026-09-25 #15): A의 세 행 `created_at`을 테스트 DB에서 서로 다르게 고친 뒤 본문 `- ` 줄 순서가 생성 시각 **내림차순**(최신 먼저 — 알림함과 같은 순서)이다 · 두 행의 `created_at`을 같게 두면 id가 큰 행이 먼저다
    - 멱등(엣지 #17): 같은 `now`로 tick을 한 번 더 → 메일 0통 추가 · 같은 날 뒤 tick에 A의 새 알림 1건 → A에게 메일 추가 없음 · 그 행 `pending` · 다음 영업일 `now`로 tick → A에게 1통(그 pending 1건 + 그날 새 건)
    - 확정 실패(엣지 #18 backstop · Codex #5): `outcomeFor = { B의 주소 → { outcome: "rejected", code: "EAUTH" } }` → A 성공 · B의 행 `failed`이고 `listMyNotifications(B)`의 `emailStatus`가 `failed` · 실행 기록 `email_sent = 1`·`email_failed = 1` · `notify.email_failed`의 필드가 `{ code: "EAUTH" }`뿐 · 같은 날 재호출해도 B에게 다시 보내지 않는다(재시도 0)
    - 로그 비노출(Codex #5): `throwFor = { B의 주소 → Error 객체(message에 B의 주소 · SMTP 호스트 · SMTP 사용자 · 비밀번호 문자열 · 알림 본문 문자열을 모두 담고 code "EAUTH") }` → 로그 스파이의 모든 인자 JSON에 그 다섯 문자열이 하나도 없다 · B의 행은 `unknown`(발송기 예외 = 포트 계약 위반 → 결과 불명, `<probe_fallback>` D-4216)
    - 미설정 뒤 켬(Codex #17): 발송기 null로 tick(A 1건 → `skipped_no_smtp`) → 같은 날 A에게 새 알림 1건 + 가짜 발송기로 tick → A에게 1통(새 건만 — 본문 항목 1개)
    - 동시 tick(Codex #4): `blockFor = { A의 주소 → 대기 }`로 tick 1이 A 발송에서 멈춘 사이 tick 2 → tick 2는 A를 선점하지 않는다(A 행 `sending` 유지 · tick 2 실행 기록 `email_claimed = 0`, B가 있으면 B만) → 대기를 풀면 A 1통 · A에게 모두 합쳐 1통
    - 비영업일·잠금 경합: KST 토요일 tick → 발송기 호출 0회 · 409 잠금 경합 tick → 이메일 단계 없음
    - 많이 쌓인 행(Codex 1차 #9 잔여 · 3차): A의 `pending` 알림 행 45건을 KST 사흘(월·화·수 — 그 사이 tick 없음)에 걸친 서로 다른 `created_at`으로 테스트 DB에 직접 넣고 04.2-01 도우미의 KST 수요일 `now`로 tick → A에게 메일 1통 · 제목 `PLANT8 알림 45건` · 본문 첫 줄이 `{수요일 KST 날짜} 알림 45건`(발송일 — 가장 오래된 월요일 날짜가 아니다) · `- ` 줄이 정확히 20개이고 가장 최근 20건(수요일 쪽)이 최신 먼저다 — 월요일 행은 하나도 줄에 없다 · 그 다음 줄이 `외 25건은 알림함에서 확인` · 45행 모두 `sent` · 실행 기록 `email_claimed = 1`·`email_sent = 1`
    - 보관(Codex 최종 B): A 1건·B 1건 `pending`을 테스트 DB에 직접 넣되 A의 `created_at`이 더 이르다(가장 오래된 pending 순으로는 A가 먼저 뽑힌다) → `setUserArchived(관리자, A, true)` → 다음 영업일 tick(04.2-01 도우미의 KST 수요일 `now`) → 메일 B 1통만 · A의 행 `pending` 그대로 · `email_attempted_at` null · 실행 기록 `email_claimed = 1`·`email_sent = 1` → `setUserArchived(관리자, A, false)`(복원) → 같은 날 다음 tick → A에게 1통
  </behavior>
  <action>
① behavior를 `test/integration/notify-email.test.ts`에 먼저 쓴다(RED). 로그 단언은 `vi.spyOn(log, "error" | "warn" | "info")`로 모든 호출 인자를 `JSON.stringify`해 본다. 생성 시각 조작은 테스트 DB 직접 UPDATE로 한다(프로덕션 코드에 테스트 경로를 만들지 않는다). 동시 tick은 두 번째 풀 연결이 필요하다(풀 최대 2 미만이면 스스로 실패하게 먼저 단언).

② 실패하는 것만 고친다 — 기대 위치: `claimNextEmailBundle`의 받는 사람 조건(오늘 `sending`·`sent`·`failed`·`unknown` 기록 제외 · 보관된 사람 제외 `users.archived_at IS NULL`을 후보 조건 안에 — per Codex 최종 B · 가장 오래된 pending 순)과 행 정렬, 이메일 단계의 받는 사람별 묶기(`recipientId` 하나로만 — 주소·이름으로 묶지 않는다), 결과 대응(`rejected` → `failed`, 발송기 예외 → `unknown`·`emailErrorCode(err)` — per Codex 2차 #2), 선점이 날짜와 무관하게 그 사람의 `pending` 전부를 잡아 묶음 행을 `created_at DESC, id DESC`(최신 먼저 — 2026-09-25 #15)로 돌려주고 `composeDigest`(04.2-15 — 받은 순서의 앞 20건 + `외 {M}건`, `kstDate`는 tick의 KST 날짜 = 발송일)에 전부 넘기는지(per D-706 · Codex 3차 — 행 수로 묶음을 쪼개지 않는다), 로그(`notify.email_failed { code }` · `notify.email_unknown { code }`), 비영업일·409 가지에서 이메일 단계가 돌지 않음. 재시도 루프·재발송 큐를 만들지 않는다(D-4213).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-email.test.ts test/integration/notify-inbox.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 테스트가 타임아웃으로 끝난다 — 한 메일에 다른 사람의 항목이 섞였거나(금지 회상 위반), 알림 없는 사람에게 메일이 갔거나, 본문 순서가 최신 먼저(생성 시각 내림차순, 같으면 id 내림차순)가 아니거나, 같은 날 두 번째 메일이 갔거나, 거부된 사람의 행이 failed가 아니거나 알림함에서 사라졌거나 다시 보냈거나, 발송기 예외가 failed로 기록됐거나, 로그 인자에 주소·호스트·사용자·비밀번호·본문이 실렸거나, 미설정 뒤 켠 날 새 알림이 막혔거나, 동시 tick이 같은 사람을 두 번 선점했거나, 보관된 사람에게 메일이 갔거나 그 행이 `pending`에서 바뀌었거나 보관된 사람 때문에 B의 메일이 막혔거나 복원 뒤 A에게 메일이 가지 않았거나, 비영업일·409에 발송기가 불렸거나, 사흘 쌓인 45건이 두 통 이상으로 나갔거나 본문 `- ` 줄이 20개가 아니거나 그 20건이 가장 최근 20건이 아니거나 첫 줄 날짜가 발송일이 아니거나 `외 25건은 알림함에서 확인` 줄이 없거나 45행 중 하나라도 `sent`가 아니다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "retry\|재시도" domain/notify/tick.ts domain/notify/email-phase.ts` 결과가 D-4213을 설명하는 주석뿐이다(재시도 루프·횟수 변수 없음 — 검토로 확인)
    - `grep -n "notify.email_failed\|notify.email_unknown" domain/notify/email-phase.ts`의 로그 필드가 `code`뿐이다
    - 발송 예외의 메시지 원문이 어떤 로그 인자에도 들어가지 않는다(검토 — `domain/notify/email-phase.ts`의 `log.` 호출 인자 확인, SUMMARY에 결과)
    - `grep -n "archived_at\|archivedAt" repositories/notifications.ts`가 `claimNextEmailBundle`의 후보 조건 안에서 나온다(검토 — 보관된 사람 제외, per Codex 최종 B)
    - behavior 열 시나리오가 이름 있는 `it`로 있다
  </acceptance_criteria>
  <done>한 통에는 한 사람의 알림만 최신 먼저(최대 20줄) 들어가고, 같은 날 두 번째 메일은 없으며, 확정 실패는 기록되고 다시 보내지 않고, 로그에 SMTP·받는 사람 정보가 새지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 결과 불명 — 모호한 SMTP 끊김(`unknown`) · 끊긴 실행(`sending`) · 새 실행에 가려지지 않는 결과 불명 조회(10분 · 7일) · 겹친 tick의 미완 목록과 선점–삽입 줄 세우기 · 요청 예산(풀·문장·토큰 대기 한도)과 발송 마감</name>
  <files>domain/notify/email-phase.ts, domain/notify/tick.ts, repositories/notifications.ts, app/internal/notify-tick/handle.ts, test/integration/notify-email.test.ts, test/support/fake-email-sender.ts</files>
  <read_first>
    - `domain/notify/email-phase.ts` · `repositories/notifications.ts` · `test/integration/notify-email.test.ts` · `test/support/fake-email-sender.ts`(Task 1·2 결과)
    - `test/support/notify-tick.ts`(04.2-01 — 상한 주입 · 발생 목록 원천)
    - 이 플랜 `<probe_fallback>` D-4216 · 「실행 예산」
  </read_first>
  <behavior>
    - 모호한 끊김(Codex 2차 #2): `outcomeFor = { A → { outcome: "indeterminate", code: "ECONNECTION" } }` → A의 행 `unknown` · 실행 기록 `email_unknown = 1`·`email_failed = 0` · `email_finished_at` 있음 · `findLastEmailOutcome`의 `failed` 0 · `findUnresolvedEmail({ now: 시도 + 1분 })`의 `bundles` 1(`unknown`은 10분을 기다리지 않는다) · 같은 날 재호출해도 A에게 다시 보내지 않는다 · 로그 `notify.email_unknown { code: "ECONNECTION" }`
    - 끊긴 실행(Codex #4 · D-4216): (가) 발송 성공 뒤 결과 기록이 던지게 주입(`recordOutcome` 대체) → tick이 오류로 끝남 · 가짜 발송기 `sent` 1통 · A의 행 `sending` · 실행 기록 `email_claimed = 1`, 결과 셋 모두 0, `email_finished_at` null (나) 발송기가 던지고 결과 기록도 던지게 주입(발송 전 끊김과 같은 저장 상태) → 가짜 `sent` 0통 · 같은 저장 상태 · 두 경우 모두 같은 날 다음 tick이 A에게 보내지 않는다
    - 가려지지 않음(Codex 2차 #1): 09:00 실행 A가 (가)처럼 끊김 → 09:01 실행 B가 다른 사람 C에게 성공(`email_claimed = 1`, `email_sent = 1`) → `findLastEmailOutcome()`은 B(`failed` 0)지만 `findUnresolvedEmail({ now: 09:05 })`의 `bundles` 0 · `{ now: 09:11 }`의 `bundles` 1, `since` = A의 선점 시각 · `{ now: A 선점 + 7일 + 1분 }`의 `bundles` 0
    - 겹친 tick의 미완 목록(Codex 2차 #3): 상한 주입. tick A(C 없음 · 미완 없음)의 이메일 단계가 B의 발송에서 멈춘다(`blockFor`) → 그 사이 tick B가 C의 알림 3건 중 2건만 넣는다(상한 — B 실행 기록 `incomplete_recipient_ids = {C}`) → A의 대기를 풀면 A의 다음 선점은 C를 고르지 않는다(C 행 `pending` 유지 · 가짜 발송기가 C에게 보낸 메일 0통) → C의 나머지가 들어간 tick D(같은 날, 미완 없음) → C에게 1통 · 본문 항목 3개
    - 예산(Codex 2차 #6): `monotonicNow`를 호출마다 25초씩 흐르게 주입하고 받는 사람 넷 → 경과가 `NOTIFY_TICK_BUDGET_MS - (2 × EMAIL_TX_WORST_MS + EMAIL_SEND_DEADLINE_MS + 2초)`를 넘는 순간부터 선점하지 않는다 · 선점되지 않은 사람의 행은 `pending` 그대로 · `email_finished_at` 있음 · 기준 시각이 이미 예산 끝(`requestStartedAtMs`가 170초 전)이면 선점 0
    - 발송 마감: `sendDeadlineMs: 200` · `blockFor = { A → 풀리지 않는 대기 }` → 가짜 발송기가 신호를 받아 `indeterminate`·`DEADLINE` → A의 행 `unknown` · `ignoreSignal: true`면 이메일 단계의 경주가 마감 + 2초(주입 가능 — 테스트는 `sendDeadlineMs + 50ms`)에 `unknown`·`DEADLINE`으로 기록하고 다음 사람으로 간다 · 둘 다 1초 안에 끝난다
    - 선점–삽입 줄 세우기(Codex 3차 추가 #1 — 읽은 미완 목록 뒤에 C가 커밋되는 창): 상한 주입. tick A의 이메일 단계가 X 발송에서 멈춘다(`blockFor`) → tick B를 시작하고 B의 테스트 조건 종류 `evaluate`가 `deferred`를 기다리게 해 B가 tick 잠금을 쥔 채 멈춘다(04.2-01 엣지 #25 도우미 — B는 C의 알림 3건 중 2건만 넣을 참) → X의 대기를 풀면 A는 결과를 기록하고 다음 선점을 시작한다 → 다른 연결에서 `pg_stat_activity`에 `wait_event_type = 'Lock'`·`wait_event = 'advisory'`인 백엔드가 1개 보일 때까지 기다린다(3초 한도 — A의 선점이 잠금을 기다리는 중) → B의 `deferred`를 푼다(A의 `lock_timeout` 5초 안) → B가 C의 행 2건과 `incomplete_recipient_ids = {C}`를 커밋한 뒤 A의 선점이 잠금을 얻는다 → A와 B의 이메일 단계 모두 C를 선점하지 않는다(C 행 2건 `pending` · C에게 메일 0통) → 같은 날 tick D가 C의 셋째 건을 넣으면(미완 없음) C에게 1통 · 본문 항목 3개. 풀 최대 3 미만이면 스스로 실패하게 먼저 단언한다
    - 풀 대기 한도(Codex 3차 #6): 테스트가 `db/client.ts`의 `pool`에서 `pool.connect()`로 풀 최대만큼 연결을 잡아 둔 채 `handleNotifyTick`(가짜 `verify`)을 부른다 → 7초 안에 500 `tick_failed` · 실행 기록 0행 · 알림 행 0행 · 잡아 둔 연결을 풀고 다시 부르면 200
    - 문장 대기 한도(Codex 3차 #6): 다른 연결이 트랜잭션 안에서 `LOCK TABLE holidays IN ACCESS EXCLUSIVE MODE`를 쥔 채 `handleNotifyTick` → 10초 안에 500 · 실행 기록 0행 · 알림 행 0행 → 잠금을 풀고 따로, 그 해 공휴일이 생성된 뒤 다른 연결이 `LOCK TABLE settings_simple IN ACCESS EXCLUSIVE MODE`를 쥔 채 `handleNotifyTick`(`batchMax` 주입 없이 기본값 — 상한 설정 읽기가 tick 잠금 트랜잭션 안에서 돈다) → 10초 안에 500 · 실행 기록 0행(tick 잠금 트랜잭션이 되돌려졌다) · 알림 행 0행
    - 토큰 검증 마감(Codex 3차 #6): `verify`가 끝나지 않는 프라미스를 돌려주고 `oidcDeadlineMs: 50` → 1초 안에 500 `tick_failed` · 주입한 `runTick`이 한 번도 불리지 않았다
    - 평가 실패 보류(Codex 4차 E3 — 부분 삽입 → 평가 실패 → 복구): 가짜 발송기 · 같은 날 KST 수요일. tick 1(상한 1, 종류 X가 A에게 3건) → A 1건 · 실행 기록 `incomplete_recipient_ids = {A}` · 메일 0통 → tick 2(X가 던지고 종류 Y가 B에게 1건, 상한 10) → B 1건 · 실행 기록 `evaluation_failed` 참 · `incomplete_recipient_ids` 빈 배열 · 메일 0통(A 1건·B 1건 모두 `pending` · 그 실행 `email_claimed = 0`) → tick 3(X 복구, 상한 10) → A 나머지 2건 · `evaluation_failed` 거짓 → A에게 1통(본문 항목 3개) · B에게 1통 · 그날 A에게 간 메일은 모두 합쳐 1통
    - 결과 기록 응답 정지(Codex 4차 E4 — 클라이언트 마감이 요청을 끝낸다): `handleNotifyTick`의 `runTick`에 `txDeadlineMs: 300`(04.2-01 deps — 선점·결과 트랜잭션에도 같은 값) · 받는 사람 A 1건 · `blockFor = { A → 대기 }`로 A 발송에서 멈춘 사이 다른 연결이 트랜잭션 안에서 `LOCK TABLE notification_log IN ACCESS EXCLUSIVE MODE`를 쥔다 → 대기를 풀면 A의 결과 기록이 응답을 못 받고 → 대기를 푼 뒤 1.5초 안에(서버 `lock_timeout` 5초보다 먼저) 500 `tick_failed` → 잠금을 풀고 `pg_locks`에서 테스트 연결이 아닌 백엔드의 `notification_log` 잠금이 사라질 때까지(6초 한도 — 파기한 연결의 서버 백엔드가 COMMIT 없이 끝났다는 신호) 기다린 뒤 A의 행 `sending` · 실행 기록 `email_finished_at` null · 같은 날 다음 tick이 A에게 다시 보내지 않는다(D-4216 — DB 오류 = 프로세스가 죽은 것과 같은 상태)
  </behavior>
  <action>
① behavior를 `test/integration/notify-email.test.ts`에 먼저 쓴다(RED). 끊긴 실행은 `runEmailPhase`·`runTick`의 deps 주입(결과 기록 함수 대체)으로만 만든다 — 프로세스를 죽이거나 테스트 전용 분기를 넣지 않는다. 시각이 필요한 조회는 `now` 인자로, 선점 시각은 테스트 DB 직접 UPDATE로 옮긴다. 겹친 tick은 두 번째 풀 연결이 필요하다(풀 최대 2 미만이면 스스로 실패하게 먼저 단언). 경주 테스트의 grace는 `runEmailPhase` deps(`sendGraceMs`, 기본 2_000)로만 줄인다. 대기 한도 세 케이스(풀·문장·토큰)는 실제 5초 한도를 기다리므로 그 `it`에만 시간 제한 20초를 준다 — 한도 상수를 테스트용으로 줄이는 경로를 프로덕션 코드에 만들지 않는다(`oidcDeadlineMs`는 Task 1의 핸들러 deps). 테이블 잠금은 테스트가 따로 연 `pg` 클라이언트(`new Client`)의 트랜잭션으로 쥐고 `finally`에서 되돌린다 — 풀 대기 케이스가 잡은 연결도 `finally`에서 푼다.

② 실패하는 것만 고친다 — 기대 위치: 결과 대응(`indeterminate` → `unknown` · `email_unknown` — per Codex 2차 #2), `findUnresolvedEmail(viewer, { now, unknownAfterMs, visibleDays })`와 `findLastEmailOutcome(viewer)`의 새 모양(per Codex 2차 #1 — 04.2-13이 읽는다), `claimNextEmailBundle`이 같은 트랜잭션에서 최근 영업일 실행 기록의 `incomplete_recipient_ids`를 읽는 자리(per Codex 2차 #3), 선점 전 예산 식과 발송 `AbortSignal.timeout(sendDeadlineMs)` · 마감 + grace 경주(per Codex 2차 #6), 결과 기록 오류 전파(`finish`를 부르지 않는다), 선점이 `NOTIFY_TICK_LOCK_KEY`의 기다리는 잠금 뒤에서 미완 목록과 행을 읽는지(per Codex 3차 추가 #1), 풀 대기·문장 한도·토큰 검증 경주가 오류를 기존 500 가지로 보내고 실행 기록을 남기지 않는지(per Codex 3차 #6 — Task 1 ⑤·⑥이 만든 한도를 몰아붙인다), `claimNextEmailBundle`이 같은 잠금 뒤에서 가장 최근 영업일 실행 기록의 `evaluation_failed`를 읽고 참이면 빈 결과를 돌려주는지(per Codex 4차 E3), 이메일 트랜잭션이 `txDeadlineMs`를 받아 클라이언트 마감으로 끝나고 `DbDeadlineError`가 결과 기록 오류 가지(전파 · `finish` 안 부름)로 가는지(per Codex 4차 E4). 테이블 잠금은 대기 한도 케이스와 같게 테스트가 따로 연 `pg` `Client`로 쥐고 `finally`에서 되돌린다.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/notify-email.test.ts test/integration/notify-tick.test.ts test/integration/notify-inbox.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 테스트가 타임아웃으로 끝난다 — 모호한 끊김이 failed로 기록됐거나 다시 보내졌거나, 끊긴 실행이 failed·sent로 기록됐거나 다시 보내졌거나, 뒤의 성공한 실행이 끊긴 실행의 결과 불명을 가렸거나 7일 뒤에도 남았거나, 겹친 tick에서 반쯤 들어간 C의 묶음이 선점됐거나, 선점이 tick 잠금을 기다리지 않아 `pg_stat_activity`에 advisory 대기가 보이지 않았거나 B가 C를 커밋한 뒤 C가 선점됐거나, 예산을 넘겨 선점했거나, 마감이 지난 발송(신호를 무시한 발송기 포함)이 이메일 단계를 붙잡았거나, 풀이 모두 잡힌 동안 7초 안에 500이 오지 않았거나, `holidays`·`settings_simple` 잠금 동안 10초 안에 500이 오지 않았거나 실행 기록·알림 행이 남았거나, 끝나지 않는 토큰 검증이 1초 안에 500으로 끝나지 않았거나 `runTick`이 불렸거나, 평가가 실패한 tick 뒤에 누구에게든 메일이 선점됐거나 A에게 3건보다 적은 묶음이 갔거나 복구 tick 뒤에 A·B에게 메일이 가지 않았거나, 결과 기록 응답이 멈춘 동안 1.5초 안에 500이 오지 않았거나 A의 행이 `sending`이 아니거나 다시 보내졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - behavior 열 시나리오가 이름 있는 `it`로 있다
    - `grep -n "incomplete_recipient_ids\|incompleteRecipientIds" repositories/notifications.ts`가 `claimNextEmailBundle` 안에서 나온다(검토 — 메모리 속 목록 인자 없음)
    - `grep -n "AbortSignal.timeout" domain/notify/email-phase.ts`가 1줄 이상이다
    - `grep -c "EMAIL_PHASE_BUDGET_MS" domain/notify/email-phase.ts`가 0이다(첫 계획의 틀린 예산 상수 없음)
    - `grep -n "evaluation_failed" repositories/notifications.ts`가 `claimNextEmailBundle` 안에서 나온다(검토 — 평가 실패 보류, per Codex 4차 E3)
  </acceptance_criteria>
  <done>갔는지 모르는 메일은 실패가 아니라 결과 불명으로 남아 뒤의 성공과 무관하게 관리자 화면이 7일 동안 찾을 수 있고, 겹친 tick도 평가가 실패한 tick도 반쯤 찬 묶음을 보내지 않으며, DB 응답이 멈춰도 요청은 스케줄러 마감 전에 끝난다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → 회사 Google SMTP | 인증 정보(환경 변수)와 알림 내용이 네트워크를 건넌다 |
| 알림 원장 → 받는 사람 메일함 | 한 번 나간 메일은 되돌릴 수 없다 |
| SMTP 서버 응답 → 앱 로그 | 오류 메시지가 주소·호스트·사용자·서버 응답을 담을 수 있다 |
| 조건 종류가 만든 문장 → 메일 본문 | Phase 7 조건 종류가 문서 이름 등 사용자 입력을 넣을 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-80 | Information Disclosure | 묶음 메일 받는 사람 | high | mitigate | 묶음은 `recipient_id` 하나로만 가르고 to는 그 사람의 `users.email`뿐 · 통합이 교차 본문 문자열 부재를 단언(금지 회상) |
| T-4.2-81 | Information Disclosure | SMTP 인증 정보 · 오류 메시지 | high | mitigate | 값은 환경 변수(Secret Manager)에만 · 코드·테스트에 없음 · 실패·결과 불명 로그는 허용 목록 오류 코드뿐 — 다섯 비밀 문자열을 담은 예외 주입 뒤 로그 부재 단언(Task 2, Codex #5) · 어댑터 쪽은 04.2-15 T-4.2-150 · 시스템 상태는 `SMTP_FROM`만 보인다(04.2-13) |
| T-4.2-82 | Information Disclosure | 보관된 사람의 남은 `pending` 알림 · `users.email` | high | mitigate | `claimNextEmailBundle`의 후보 조건에 `users.archived_at IS NULL`(보관은 행·주소를 지우지 않고 알림함만 `lib/viewer.ts`가 막는다) · 보관된 사람의 행은 `pending` 그대로 · 통합 「보관」이 B 1통만 · A 행 `pending` · 복원 뒤 A 1통을 단언(Task 2, Codex 최종 B). 남는 창: 보관 전에 이미 선점된 묶음의 발송 중(≤ `EMAIL_SEND_DEADLINE_MS` + 2초)에 보관되면 그 한 통은 나간다 — 보관 직전에 나간 메일과 구별되지 않아 accept |
| T-4.2-84 | Denial of Service | 멈춘 SMTP · 긴 발송 · 느린 DB · 연결 뒤 멈춘 DB 응답 · 풀 고갈 · 느린 인증서 조회 | medium | mitigate | tick 경로 트랜잭션은 모두 04.2-01 `withDeadlineTransaction` — BEGIN~COMMIT·ROLLBACK 전체가 클라이언트 마감 안, 넘으면 연결 파기(결과 기록 응답 정지 통합 — Task 3, Codex 4차 E4) · 핸들러 진입 기준 요청 예산 160초 안에서만 선점 · 한 통 벽시계 마감 30초(`AbortSignal`) + 무시해도 +2초 경주 · 풀 연결 대기 5초(`db/client.ts` `POOL_CONNECTION_TIMEOUT_MS`) · 토큰 검증 10초 경주(`OIDC_VERIFY_DEADLINE_MS`) · tick 경로 모든 트랜잭션 `lock_timeout`·`statement_timeout` 5s(상한 설정 읽기는 tick 잠금 트랜잭션 안) · 이메일 단계 전 최악 150초 · 요청 최악 170초 < 180초 · 발송은 트랜잭션 밖 · 재시도 0(Task 3 예산·마감·풀·문장·토큰 통합, Codex 2차 #6 · 3차 #6) |
| T-4.2-85 | Repudiation | 발송 결과 | high | mitigate | 행마다 `email_status`(`unknown` = 모호한 SMTP 결과, 10분 지난 `sending` = 끊긴 실행)·`email_attempted_at` · 실행 기록 `email_claimed`·`email_sent`·`email_failed`·`email_unknown`·`email_finished_at` · 결과와 건수 한 트랜잭션 · 결과 불명은 알림 행에서 세어 뒤의 성공에 가려지지 않음(Task 3, D-4216 · Codex 2차 #1·#2) |
| T-4.2-86 | Tampering | 같은 사람 두 번 발송 · 반쯤 찬 묶음 발송 | medium | mitigate | 선점이 알림함 삽입과 같은 `NOTIFY_TICK_LOCK_KEY`의 기다리는 advisory 잠금을 잡는다(선점끼리 · 선점과 삽입 커밋을 한 줄로) + 오늘 `sending`·`sent`·`failed`·`unknown` 기록 제외 + 최근 영업일 실행 기록의 미완 목록과 평가 실패 표시(`evaluation_failed` 참이면 선점 0 — Codex 4차 E3, 평가 실패 보류 통합 Task 3)를 그 잠금 뒤에서 읽음 — SMTP가 막힌 동안의 동시 tick(Task 2)·겹친 tick 미완 목록·선점–삽입 줄 세우기 장벽(Task 3) 통합(Codex 3차 추가 #1) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — `git diff --stat HEAD -- package.json pnpm-lock.yaml`이 비어 있다(`nodemailer` 체크포인트는 04.2-15) |

</threat_model>

<verification>

- 통합: `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/notify-email.test.ts test/integration/notify-tick.test.ts test/integration/notify-inbox.test.ts`
- 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`
- 의존성: `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음(이 플랜의 새 의존성 0개)
- 실제 Workspace SMTP 수신 확인은 VALIDATION 「Manual-Only」 — SMTP 값이 정해진 뒤 스테이징에서(D-711)

</verification>

<success_criteria>

- 한 사람 3건 → 메일 1통 · 알림함 3건, SMTP 미설정 → 0통, 발송 거부 → 실패 기록(D-711 세 시나리오 — 「발송 예외」 중 확정 거부)
- 묶음은 받는 사람별 · 최신 먼저(생성 시각 내림차순 — 2026-09-25 #15) · 같은 날 한 번 · 재시도 0(엣지 #14~#18)
- 끊긴 발송은 결과 불명으로 남고 다시 나가지 않으며 관리자 화면이 찾을 수 있다 · 로그에 SMTP·받는 사람 정보가 없다 · 이메일 단계가 실행 마감 안에서 끝난다
- 갔는지 모르는 메일(모호한 SMTP 끊김·끊긴 실행)은 실패가 아닌 결과 불명으로 남고 뒤의 성공한 실행에 가려지지 않는다 · 겹친 tick이 반쯤 찬 묶음을 보내지 않는다
- 선점은 알림함 삽입과 한 줄로 서서 미완 목록과 행을 한 시점에서 읽고 평가가 실패한 tick 뒤에는 선점하지 않는다 · 풀·문장·토큰 대기와 연결 뒤 멈춘 DB 응답이 모두 묶여 요청이 170초 안에 끝난다 · 여러 날 쌓인 알림도 한 통에 모이되 본문은 20줄에서 멈춘다(Codex 3차)
- 이 플랜의 새 의존성 0개

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈 | `domain/notify/email-phase.ts`(`runEmailPhase` · `NOTIFY_TICK_BUDGET_MS` · `EMAIL_SEND_DEADLINE_MS` · `EMAIL_TX_WORST_MS` · `EMAIL_UNKNOWN_AFTER_MS` · `EMAIL_UNKNOWN_VISIBLE_DAYS`) — 포트·어댑터·`composeDigest`는 04.2-15 |
| 핸들러 | `handleNotifyTick`이 진입 시각을 `runTick({ requestStartedAtMs })`로 넘김 · `NotifyTickHandlerDeps.monotonicNow` · `OIDC_VERIFY_DEADLINE_MS`(10초 토큰 검증 경주) · `NotifyTickHandlerDeps.oidcDeadlineMs` |
| 공용(더하기만 — 04-32와 같은 줄) | `db/client.ts` `POOL_CONNECTION_TIMEOUT_MS`(5000 · 두 Pool의 `connectionTimeoutMillis`) · `repositories/settings.ts` `findSimpleValue(viewer, key, tx?)` |
| 저장소 | `markPendingEmailSkipped` · `claimNextEmailBundle`(`NOTIFY_TICK_LOCK_KEY`의 기다리는 잠금 · 미완 목록과 `evaluation_failed`는 실행 기록에서) · 네 쓰기 트랜잭션 모두 `withDeadlineTransaction`(`NOTIFY_TX_DEADLINE_MS` · 새 `NOTIFY_SHORT_TX_DEADLINE_MS` 5_000) · `recordEmailOutcome`(`sent`·`failed`·`unknown`) · `finishEmailPhase` · `findLastEmailOutcome`(`{ at, failed }`) · `findUnresolvedEmail`(`{ bundles, since }`) |
| tick | `TickDeps.emailSender` · `TickDeps.serviceUrl` · `TickDeps.requestStartedAtMs` · `TickDeps.monotonicNow` · `TickDeps.batchMax(tx)`(tick 잠금 트랜잭션 안 상한 읽기) · 커밋 뒤 이메일 단계 |
| 로그 이벤트 | `notify.email_failed`(error, code) · `notify.email_unknown`(warn, code) · `notify.tick`에 `emailSent`·`emailFailed`·`emailUnknown` |
| 테스트 | `test/support/fake-email-sender.ts`(`createFakeEmailSender` — 결과·예외·지연·마감 무시 주입) · `test/integration/notify-email.test.ts` · `test/integration/notify-tick.test.ts`(04.2-01 트레이서 `email_status` 단언 → `skipped_no_smtp`) |
| 결정 | D-4203(개정 — 한 묶음씩 선점 · 미완 목록은 실행 기록 · 다음 영업일로 미룸) · D-4213(재시도 0 · 메일 단위 실패 수) · D-4216(개정 — 세 결과 기록 · 결과 불명은 알림 행에서 7일) · 요청 예산(160초 · 발송 마감 30초 · 트랜잭션 최악 `EMAIL_TX_WORST_MS` 30초 · 요청 최악 170초) · 묶음 크기(선점은 `pending` 전부 · 메일은 앞 20건 + `외 {M}건` — 04.2-15) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-10-SUMMARY.md` when done
</output>
