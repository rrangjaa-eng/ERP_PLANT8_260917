---
phase: 04.2-notify-holidays-foundation
plan: 10
subsystem: notify
status: complete
tags: [email, digest, notify-tick, deadline, advisory-lock, vitest]

requires:
  - phase: 04.2-01
    provides: "tick 잠금(NOTIFY_TICK_LOCK_KEY) · 실행 기록 incomplete_recipient_ids·evaluation_failed · withDeadlineTransaction"
  - phase: 04.2-06
    provides: "공휴일 판정 트랜잭션의 문장 한도"
  - phase: 04.2-15
    provides: "EmailSender 포트(sent·rejected·indeterminate) · emailSenderFromEnv · composeDigest · emailErrorCode"
provides:
  - "runEmailPhase — 예산 안에서 한 묶음씩 선점 → AbortSignal 마감 + grace 경주 발송 → 세 결과 기록 → 끝 표시"
  - "claimNextEmailBundle — tick 잠금 키의 기다리는 잠금 뒤에서 최근 영업일 실행 기록의 미완 목록·평가 실패 표시를 읽고, 보관된 사람을 뺀 한 명의 pending 전부를 sending으로"
  - "recordEmailOutcome · finishEmailPhase · markPendingEmailSkipped — 모두 withDeadlineTransaction + lock/statement_timeout 5s"
  - "findUnresolvedEmail(viewer, { now, unknownAfterMs, visibleDays }) → { bundles, since } — 알림 행에서 센 결과 불명(unknown · 10분 지난 sending, 7일 창)"
  - "findLastEmailOutcome(viewer) → { at, failed } | null — email_claimed > 0인 가장 최근 실행(U-6)"
  - "핸들러 진입 시각 → runTick({ requestStartedAtMs }) · 토큰 검증 10초 경주 · 풀 연결 대기 5초"
affects: [04.2-13, 04.2-14]

tech-stack:
  added: []
  patterns:
    - "발송은 트랜잭션 밖 — 선점·결과를 각각 짧은 클라이언트 마감 트랜잭션으로 나눠, 끊겨도 sending으로 남고 거짓 sent·failed를 적지 않는다"
    - "신호를 무시하는 외부 호출은 마감 + grace 타이머와 Promise.race, 진 프라미스의 늦은 거부는 삼킨다"
    - "결과 불명 표시는 실행 기록이 아니라 알림 행에서 센다 — 뒤의 성공한 실행에 가려지지 않는다"

key-files:
  created:
    - domain/notify/email-phase.ts
    - test/support/fake-email-sender.ts
    - test/integration/notify-email.test.ts
  modified:
    - domain/notify/tick.ts
    - repositories/notifications.ts
    - app/internal/notify-tick/handle.ts
    - db/client.ts
    - repositories/settings.ts
    - test/integration/notify-tick.test.ts

key-decisions:
  - "D-4203(개정) 한 묶음씩 선점 — 선점은 알림함 삽입과 같은 tick 잠금 키를 기다리는 형으로 잡고 미완 목록·평가 실패 표시를 실행 기록에서 같은 트랜잭션으로 읽는다"
  - "D-4216(개정) 세 결과 — rejected → failed, indeterminate·발송기 예외·마감 → unknown, 결과 기록 오류는 전파하고 끝 표시를 하지 않는다(행 sending). 결과 불명은 알림 행에서 10분 · 7일 창으로 센다"
  - "D-4213 재시도 0회 — 실패·결과 불명 모두 다시 보내지 않는다"
  - "요청 예산 — 핸들러 진입 기준 160초 안에서만 선점, 발송 마감 30초 + grace 2초, 요청 최악 170초 < 스케줄러 180초"
  - "TickDeps.recordOutcome — 끊긴 실행 테스트를 위한 결과 기록 대체 주입(플랜이 정한 deps 주입, 테스트 전용 분기 아님)"

requirements-completed: []

duration: "Task 1·2 2026-09-25 세션 + Task 3 2026-09-26 세션"
completed: 2026-09-26

actuals:
  tokens: 16074
  tasks: 3
  commits: 7
plan_head_before: cc156229e3d2baf72a24bc96c901f465d632849e
---

# Phase 04.2 Plan 10: 하루 한 통 묶음 이메일 Summary

**받는 사람별 KST 하루 한 통 묶음 이메일 — tick 잠금 뒤 한 묶음씩 선점, AbortSignal 마감 + 2초 경주 발송, sent·failed·unknown 세 결과 기록, 결과 불명은 알림 행에서 10분·7일 창으로 조회, 요청은 핸들러 진입 기준 170초 안에 끝남**

## Performance

- **완료:** 2026-09-26 (Task 1·2는 2026-09-25 세션, Task 3은 사용자 재개 지시로 2026-09-26 세션)
- **Tasks:** 3/3
- **Files:** 코드 9개(생성 3 · 수정 6), `.planning/` 제외 +1,382/−18줄(Task 1~3 합)

## Accomplishments

- 한 사람 3건 → 메일 1통 · 알림함 3건, SMTP 미설정 → 0통 · `skipped_no_smtp`(시도 시각 없음), 확정 거부 → `failed`(D-711 세 시나리오)
- 묶음 격리(다른 사람 항목 섞임 금지 회상) · 최신 먼저 20줄 + 「외 M건」 · 같은 날 두 번째 메일 없음 · 보관된 사람 제외
- 결과 불명(모호한 SMTP 끊김 · 끊긴 실행)은 실패가 아니라 `unknown`/`sending`으로 남고 다시 보내지 않으며, `findUnresolvedEmail`이 뒤의 성공과 무관하게 10분 뒤부터 7일 동안 센다
- 겹친 tick·평가 실패 tick 뒤에는 반쯤 찬 묶음을 선점하지 않는다(선점이 tick 잠금 뒤에서 미완 목록·평가 실패 표시를 읽음 — `pg_stat_activity` advisory 대기로 실측)
- 풀 대기 5초 · 문장 한도 5초 · 토큰 검증 10초 · 결과 기록 응답 정지(클라이언트 마감) 모두 500 `tick_failed`로 끝나고 기록을 남기지 않는다

## Task Commits

1. **Task 1: 이메일 트레이서** — RED `1a0f4b6` (test) · GREEN `2fc358e` (feat)
2. **Task 2: 묶음 규칙 · 확정 거부 · 로그 비노출** — RED `f5b9c3b` (test) · GREEN `5827554` (feat)
   - 일시 정지 인계 `fc1becd` (wip: `.continue-here.md`)
3. **Task 3: 결과 불명 · 겹친 tick · 요청 예산 · 대기 한도** — RED `c5b06ff` (test) · GREEN `17b325c` (feat)

## RED 증거 (TDD)

| Task | RED 커밋 | 결과 | 비고 |
|---|---|---|---|
| 1 | `1a0f4b6` | 대상 3건 모두 단언 실패(`check tdd-red-evidence` = RED_EVIDENCE_OK) | 인계 기록 · 커밋 본문 |
| 2 | `f5b9c3b` | 14건 중 2건 실패(거부가 unknown으로 기록 · 발송기 예외가 tick을 던짐) | 12건은 Task 1 선점 조건이 이미 충족해 처음부터 통과 |
| 3 | `c5b06ff` | 14건 중 5건 실패 · 9건 바로 통과 | 아래 |

**Task 3 RED 실패 5건(모두 기능 부재 사유):**
- 모호한 끊김 — `TypeError: findLastEmailOutcome is not a function`
- 끊긴 실행 (가)·(나), 가려지지 않음 — `promise resolved … instead of rejecting`(runTick이 결과 기록 대체를 받지 않음)
- 신호를 무시하는 발송기의 마감 + grace — `Test timed out in 3000ms`(`sendGraceMs` 경주 없음)

**Task 3 바로 통과 9건(Task 1·2 코드가 이미 만든 동작 — 정직하게 기록):** 겹친 tick의 미완 목록 · 선점–삽입 줄 세우기 · 평가 실패 보류 · 예산 · 신호를 받는 발송기의 발송 마감 · 풀 대기 한도 · 문장 대기 한도 · 토큰 검증 마감 · 결과 기록 응답 정지. 이 9건은 RED를 본 적이 없으므로 회귀 방지 테스트로서의 효력은 검증되지 않았다(구현을 되돌려 실패를 확인하는 역검증은 하지 않았다).

## 최종 게이트 (Task 3 GREEN 뒤, 2026-09-26)

| 게이트 | 결과 |
|---|---|
| `pnpm typecheck` · `pnpm lint` · `pnpm lint:sql` | 모두 exit 0 |
| 단위(vitest unit) 전체 | 84 파일 · 880/880 통과 |
| 통합 notify-email | 28/28 |
| 통합 notify-tick | 27/27 |
| 통합 notify-inbox | 10/10 |
| 통합 notify-tick-auth(핸들러를 건드려 추가) | 5/5 |
| `pnpm build` | exit 0 |
| `git diff --stat HEAD -- package.json pnpm-lock.yaml` | 빈 출력(새 의존성 0) |

통합 파일은 DB를 공유해 한 파일씩 돌렸다. 로컬 DB가 이미 떠 있어 플랜 verify의 `scripts/dev-db.sh && pnpm db:migrate`는 다시 돌리지 않았다.

**Task 3 acceptance grep:**
- `incomplete_recipient_ids` · `evaluation_failed` — `repositories/notifications.ts` 312~321행, `claimNextEmailBundle` 안(메모리 속 목록 인자 없음)
- `archived_at` — 329행, `claimNextEmailBundle` 후보 조건 안
- `AbortSignal.timeout` — `domain/notify/email-phase.ts` 67행(1줄)
- `EMAIL_PHASE_BUDGET_MS` — 0
- `notify.email_failed` · `notify.email_unknown` 로그 인자는 `{ code }`뿐. `email-phase.ts`의 `log.` 호출은 이 두 줄뿐이고 예외 메시지 원문은 어떤 로그 인자에도 들어가지 않는다(검토)
- behavior 열 시나리오는 모두 이름 있는 `it`로 있다(Task 3 14건: 모호한 끊김 · 끊긴 실행 (가) · (나) · 가려지지 않음 · 겹친 tick 미완 목록 · 선점–삽입 줄 세우기 · 평가 실패 보류 · 예산 · 발송 마감 2건(신호 받음/무시) · 풀 대기 · 문장 대기 · 토큰 검증 마감 · 결과 기록 응답 정지)

## Files Created/Modified

- `domain/notify/email-phase.ts` — `runEmailPhase`와 예산 상수, 발송 마감 + grace 경주(`sendGraceMs` 기본 2초)
- `repositories/notifications.ts` — 선점 · 결과 기록 · 끝 표시 · 미설정 표시 · `findLastEmailOutcome` · `findUnresolvedEmail`
- `domain/notify/tick.ts` — 잠금 커밋 뒤 영업일 이메일 단계, `batchMax(tx)`, `TickDeps.emailSender`·`serviceUrl`·`requestStartedAtMs`·`monotonicNow`·`recordOutcome`
- `app/internal/notify-tick/handle.ts` — 진입 시각 전달, `OIDC_VERIFY_DEADLINE_MS` 경주
- `db/client.ts` — `POOL_CONNECTION_TIMEOUT_MS` 5000(04-32와 같은 줄)
- `repositories/settings.ts` — `findSimpleValue(viewer, key, tx?)`
- `test/support/fake-email-sender.ts` — 결과·예외·대기·신호 무시 주입
- `test/integration/notify-email.test.ts` — Task 1~3 통합 28건
- `test/integration/notify-tick.test.ts` — 04.2-01 트레이서 `email_status` 단언 → `skipped_no_smtp`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] node_modules에 nodemailer가 없음(두 세션 모두)**
- **발견:** Task 1 세션과 Task 3 RED 첫 실행(`Cannot find package 'nodemailer/lib/mail-composer'`)
- **조치:** `pnpm install --frozen-lockfile` — package.json·pnpm-lock.yaml 변경 없음
- **커밋:** 없음(작업 트리 변화 없음)

### 계획 안의 구현 선택(편차 아님, 기록)

- **끊긴 실행의 결과 기록 대체:** 플랜이 정한 「결과 기록 함수 대체」를 `TickDeps.recordOutcome`으로 `runTick` → `runEmailPhase`에 넘겼다(기본은 `recordEmailOutcome`). 테스트 전용 분기는 없다.
- **발송 마감 테스트는 `runEmailPhase` 직접 호출:** `sendDeadlineMs`·`sendGraceMs`는 `runEmailPhase` deps에만 있고 `TickDeps`로 넓히지 않았다(요청받지 않은 설정 가능성 금지). 테스트가 실행 기록 1행과 pending 행을 직접 넣고 부른다.
- **`findUnresolvedEmail`의 7일 창·10분 기준**은 인자(`visibleDays`·`unknownAfterMs`)로 받고, 도메인 상수 `EMAIL_UNKNOWN_VISIBLE_DAYS`·`EMAIL_UNKNOWN_AFTER_MS`를 호출자(04.2-13)가 넘긴다.

**Total deviations:** 1 auto-fixed(Rule 3, 환경). **Impact:** 범위 변화 없음.

## Codex

Codex 생략(한도) — 한도 풀리면 Codex 재확인 필요(04.2-10 전체, 특히 Task 3의 경주·결과 불명 조회).

## Issues / 후속

- **STATE.md는 건드리지 않았다**(gsd-tools `state.*` 동사가 Phase 4 필드에 소수 페이즈 진행을 써 넣는 알려진 문제). 04.2-10 진행은 ROADMAP(gsd-tools `roadmap update-plan-progress`)에만 반영한다.
- **NOTI-02는 완료로 표시하지 않았다** — 관리자 배너·시스템 상태(04.2-13)와 04.2-14가 남아 있어 `requirements mark-complete`는 하지 않았다.
- 04.2-13은 `findUnresolvedEmail`·`findLastEmailOutcome`을 읽는다(이 플랜이 모양을 확정).
- 실제 Workspace SMTP 수신 확인은 VALIDATION 「Manual-Only」 — 스테이징에서.
- Task 3 바로 통과 9건의 역검증(구현을 되돌려 실패 확인)은 하지 않았다 — `/review` 때 필요하면 확인.

## Known Stubs

없음(스캔: 수정 파일에 placeholder·TODO·빈 값 흐름 없음).

## User Setup Required

없음(SMTP 네 값은 04.2-15 · 04.2-13 몫).

## Self-Check: PASSED

- 파일: `domain/notify/email-phase.ts` · `test/support/fake-email-sender.ts` · `test/integration/notify-email.test.ts` 존재
- 커밋: `1a0f4b6` · `2fc358e` · `f5b9c3b` · `5827554` · `fc1becd` · `c5b06ff` · `17b325c` 모두 `git log`에 있음(`git rev-list --count cc15622..HEAD` = 7)
