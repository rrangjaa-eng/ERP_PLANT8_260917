---
phase: 04.2-notify-holidays-foundation
plan: 11
type: execute
wave: 4
depends_on: [04.2-03, 04.2-06, 04.2-09]
files_modified:
  - domain/permissions/menus.ts
  - ui/shell/role-menu.ts
  - docs/design/SYSTEM.md
  - test/unit/ui/role-menu.test.ts
  - test/e2e/admin-nav.spec.ts
  - domain/holidays/admin.ts
  - app/(app)/admin/holidays/page.tsx
  - app/(app)/admin/holidays/holidays.module.css
  - app/(app)/admin/holidays/actions.ts
  - app/(app)/admin/holidays/actions.registry.ts
  - app/(app)/admin/holidays/confirm-year.tsx
  - test/integration/leak-scan.test.ts
  - test/integration/holidays-admin.test.ts
  - test/e2e/holidays.spec.ts
autonomous: true
requirements: [ADMN-11]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "관리자는 「관리」 인덱스 「마스터」 그룹 끝의 `공휴일`로 `/admin/holidays`에 가서 올해·다음 해 후보 표를 보고 `{연도}년 공휴일 확정`을 눌러 그 해를 확정한다 — Playwright E2E로 CI에 있다(ROADMAP 04.2 기준 1)"
    - "`admin.holidays` view가 없는 계급에게 `/admin/holidays`는 404이고, view만 있고 write가 없으면 확정 버튼·`공휴일 추가` 링크·`삭제`가 DOM에 없다(비활성이 아니라 없다 — UI-SPEC S2-a). 도메인 함수도 같은 권한을 다시 검사한다"
    - "연도 확정은 달력 잠금 트랜잭션(`withHolidayCalendarLock` — 04.2-06) 하나에서 (가) 그 해 후보를 잠금 안에서 보장하고(`ensureHolidayCandidatesLocked` — 음력 표 밖이면 오류) (나) 그 해의 생성 표시 행(`holiday_year_generations`)이 있는지 확인하고 — 없으면 `HolidayYearIncompleteError` (다) 확정 기록을 멱등 삽입하고 (라) 실제로 들어갔을 때만 행동 로그 `holiday_change`(detail `op: \"confirm\"`)를 같은 tx로 쓴다 — 로그 쓰기가 실패하면 확정도 롤백되어 다시 누르면 확정과 로그가 함께 남는다(D-4220 · D-4223 · Codex #12·#14 · Codex 2차 #4·#14 잔여)"
    - "액션을 화면 밖에서 직접 불러도 음력 표 밖의 해(예: 2036)는 확정되지 않고 오류이며, 수동 행 하나만 있던 해나 법정 행이 일부만 있는 해(생성 표시 없음)를 확정하면 그 해의 법정 후보가 먼저 빠짐없이 채워지고 생성 표시가 생긴 뒤 확정된다 — 「법정 행 하나」를 완성된 달력으로 보지 않고, 생성 표시 없이 확정된 해가 없다(Codex #14 · Codex 2차 #14 잔여)"
    - "공휴일 변경 행동 로그는 끌 수 없는 종류 `holiday_change`(라벨 `공휴일 변경` — 04.2-03)이고 `action_log.optional_types` 설정과 무관하게 남는다(Codex #12)"
    - "SYSTEM.md §6-10 표 「마스터」 줄 끝에 `공휴일`이 있고 절 제목의 「관리자 화면 10개」가 「11개」이며, `ADMIN_MENUS`·`MENUS`·`ADMIN_MENU_KEYS`가 같은 커밋에서 11개로 맞는다(UI-SPEC SYSTEM.md 수정 제안 #1)"
    - "화면 검토에 Codex(GPT) 적대적 디자인 검토 한 번이 들어가고 그 출력 원문이 SUMMARY에 붙는다 — codex가 실행되지 않으면 그 사실과 오류를 적고 다른 검토로 대신하지 않는다(소유자 지시)"
    - "관리자 공휴일 표는 날짜 오름차순이고 같은 날짜 행은 존재하지 않는다"
    - "두 요청이 같은 해 후보 생성을 동시에 해도 유니크 제약(ON CONFLICT DO NOTHING)으로 중복 행이 생기지 않고, 확정을 동시에 두 번 눌러도 확정 기록은 한 번만 남는다"
    - "UI S2-holiday-table/empty — 올해·다음 해는 화면 진입 시 후보가 생성돼 행이 항상 있다. 음력 표 범위 밖 해는 EMPTY가 아니라 Copywriting S2 Error(음력 표) 줄이다"
    - "UI S2-holiday-table/loading — 서버 컴포넌트 초기 렌더에 값이 들어 있어 클라이언트 로딩 상태가 없다(§7-7 관리자 마스터 화면)"
    - "UI S2-holiday-table/error — 조회 실패 시 표 자리에 Copywriting S2 Error(조회) 한 줄 + 2차 `다시 시도`가 보인다"
    - "UI S2-holiday-table/populated — 날짜 오름차순 행이 날짜·요일·이름·구분·동작 칸으로 보이고 머리글은 흰 바탕 `<table>` + 숨긴 caption `{연도}년 공휴일`이다"
    - "UI S2-holiday-table/partial — 확정 전 연도는 상태 줄 `후보 · 규칙으로 만든 N일 · 확정 전` + 1차 확정 버튼, 확정 연도는 `확정 · 시각 이름 · N일`이고 버튼이 없다 〔사용성 원칙으로 대체(04.2-12 「UI-SPEC 변경 요청」 R7, /plan-design-review에서 확정): 확정 전 상태 줄은 `후보 · N일`이다 — 「확정 전」은 `후보`와 1차 확정 버튼이 이미 말하고 「규칙으로 만든」은 설명이라 뺀다. 1차 확정 버튼과 확정 연도 줄은 그대로. 앞의 옛 줄 모양은 화면·E2E에 쓰지 않는다〕"
    - 'UI S2-holiday-table/zero-one-many — 연도 링크는 행이 있는 연도 전부를 오름차순으로 보이고 현재 연도는 링크가 아닌 글자(`aria-current="page"`)다. 한 해가 1행이어도 같은 행 템플릿이다'
    - statement: "UI S2-holiday-table/long-text — `제22대 국회의원 선거일` 같은 긴 이름이 이름 칸에서 줄바꿈되고 말줄임되지 않는다(PC·375px)"
      verification: backstop
    - "UI S2-confirm/empty — 확정 버튼은 확정 전 연도 + write 권한 + 폼 닫힘일 때만 DOM에 있고 그 밖에는 없다"
    - "UI S2-confirm/loading — 대기 중 라벨이 `{연도}년 공휴일 확정…`이고 비활성이다"
    - "UI S2-confirm/error — 실패 시 원래 라벨로 돌아오고 옆에 `확정하지 못했습니다 · 다시 시도`(`--fs-sm --danger`)가 보인다"
    - "UI S2-confirm/partial — 두 관리자가 동시에 확정해도 둘 다 성공 모양을 보고 상태 줄에는 먼저 확정한 사람·시각이 남는다"
    - "UI S2-confirm/long-text — 라벨 길이는 `2027년 공휴일 확정…`으로 고정이라 PC에서 상태 줄과 한 줄에 들어간다"
  artifacts:
    - path: "app/(app)/admin/holidays/page.tsx"
      provides: "공휴일 관리 화면(연도 링크 · 연도 상태 줄 · 표 · 다섯 상태) — 폼·삭제는 04.2-12"
    - path: "app/(app)/admin/holidays/actions.ts"
      provides: "확정 Server Action"
      exports: ["confirmHolidayYearAction"]
    - path: "app/(app)/admin/holidays/actions.registry.ts"
      provides: "확정 액션의 누수 스캔 등록(menu `admin.holidays`, write, dtoName null)"
    - path: "domain/holidays/admin.ts"
      provides: "화면 데이터 한 번에 읽기 · 연도 확정(한 트랜잭션 · 범위·완결 검사 · 멱등) — 권한 재검사와 행동 로그"
      exports: ["loadHolidayAdmin", "confirmHolidayYear", "HolidayAdminView", "HolidayRowView", "HolidayForbiddenError", "HolidayYearIncompleteError"]
  key_links:
    - from: "app/(app)/admin/holidays/page.tsx"
      to: "domain/holidays/admin.ts"
      via: "`loadHolidayAdmin(viewer, { requestedYear })`가 올해·다음 해 후보를 보장한 뒤 표·상태 줄 데이터를 준다"
      pattern: "loadHolidayAdmin"
    - from: "domain/holidays/admin.ts"
      to: "domain/action-log/record.ts"
      via: "`withHolidayCalendarLock` 안에서 `recordAction(viewer, { actionType: \"holiday_change\", … }, { tx })`"
      pattern: "holiday_change"
    - from: "domain/holidays/admin.ts"
      to: "domain/holidays/candidates.ts"
      via: "확정이 `withHolidayCalendarLock(tx => …)` 안에서 `ensureHolidayCandidatesLocked(year, tx)` → `findYearGeneration(viewer, year, tx)`를 부른다(Codex 2차 #4·#14 잔여)"
      pattern: "withHolidayCalendarLock"
    - from: "ui/shell/role-menu.ts"
      to: "docs/design/SYSTEM.md"
      via: "`ADMIN_MENUS` 「마스터」 끝 `공휴일` ↔ §6-10 표(단위 테스트 원소 대조)"
      pattern: "admin.holidays"
    - from: "test/integration/leak-scan.test.ts"
      to: "app/(app)/admin/holidays/actions.registry.ts"
      via: "부작용 import로 액션 등록을 스캔 대상에 넣는다(leak-scan-coverage 단언)"
      pattern: "admin/holidays/actions.registry"
---

<objective>
**As a** 경영관리 관리자, **I want to** 법정 규칙으로 만든 올해·다음 해 공휴일 후보를 한 화면에서 보고 연도 단위로 확정하기를, **so that** 지급일·마감·알림이 사람이 확인한 달력으로 계산되고, 확정 기록과 그 행동 로그가 어긋나지 않는다.

(스토리는 ROADMAP 04.2 Goal 줄 「관리자가 법정 규칙으로 생성된 공휴일 후보를 검토·확정하고」와 기준 1에서 끌어왔다. ROADMAP Goal 줄은 사용자 스토리 형식이 아니다 — 형식을 맞추려면 `/gsd mvp-phase 04.2`.)

Purpose: ADMN-11 화면 몫의 앞 절반 — 메뉴·후보 검토·연도 확정. UI-SPEC S2-a·S2-b·S2-e와 SYSTEM.md 수정 제안 #1. Codex #12(확정과 로그의 원자성·끌 수 없음)·#14(확정의 범위·완결 검사). 수동 추가·삭제·폰 칸 접기는 04.2-12.

Output: `admin.holidays` 메뉴 키와 「관리」 인덱스 항목 · `/admin/holidays` 화면(표·상태 줄·다섯 상태) · 확정 액션(누수 스캔 등록) · 도메인 권한 재검사·원자적 행동 로그 · 통합·E2E.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 Skill 도구로 호출한다. 화면은 `docs/design/SYSTEM.md`와 `docs/design/tokens.css` 토큰만 쓴다(새 색·서체·radius 금지). 새 템플릿을 만들지 않고 §6-1 목록 + §7-3으로 조립한다(본보기 `admin/corp-cards/page.tsx`). 화면 검증 순서는 「싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`) + Codex 적대적 디자인 검토 → 수정 → 전체 게이트 한 번」. UI 완료 판정은 페이즈 끝 `/design-review` → `/qa`. 새 의존성 0개. `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-03-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-06-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — S2(220~318행) · Copywriting S2 행 · 가정 U-1·U-2·U-3·U-10 · 「SYSTEM.md 수정 제안」 #1
# .planning/phases/04.2-notify-holidays-foundation/04.2-PATTERNS.md — 44~152행(holidays page·actions 본보기)
# docs/design/SYSTEM.md — §6-10(Grep `### 6-10`) · §6-1 · §7-1 · §7-3
</context>

<probe_fallback>
이 플랜이 떠맡는 엣지 행은 ADMN-11 ordering(#4)과 concurrency(#7)이다 — #7의 앞 절반(후보 동시 생성)은 04.2-06 `holidays.test.ts`가 저장소 수준에서 이미 증명했고, 여기서는 화면 데이터 경로(`loadHolidayAdmin` 두 번 동시)와 확정 절반을 증명한다. UI-SPEC UI Considerations의 S2-holiday-table 일곱 줄(overflow 제외 — 폰 칸 접기와 함께 04.2-12)과 S2-confirm 다섯 줄(overflow 제외 — 04.2-12)을 떠맡는다.

**계획 단계 결정(표면화):**

- **D-4220 공휴일 행동 로그(개정 — Codex #12):** 첫 계획은 마스터 화면 선례대로 `document_create`·`document_delete`·`document_update`를 트랜잭션 밖에서 썼다. Codex #12 — 확정이 커밋된 뒤 로그 쓰기가 실패하면 다시 눌러도 「이미 확정」이라 로그가 영영 빠지고, `document_*`는 `action_log.optional_types` 설정으로 꺼질 수 있다. 그래서 04.2-03이 더한 끌 수 없는 종류 `holiday_change`(라벨 `공휴일 변경`)를 쓰고 `detail.op`로 `confirm`·`add`·`delete`를 가른다. 확정 로그는 `entity: "holiday_year"`, `entityId: String(year)`, `detail: { op: "confirm", year, count }`. 호출은 `recordAction(viewer, entry, { tx })`(04-32 — 04.2-03 전제가 이미 충족).
- **D-4223 확정의 범위·완결 검사(Codex #14 · 개정 — Codex 2차 #4·#14 잔여):** `confirmHolidayYear(viewer, year, deps?: { recordAction?; ensure? })` — write 검사 → `withHolidayCalendarLock(tx => { (deps.ensure ?? ensureHolidayCandidatesLocked)(year, tx) — 음력 표 밖이면 LunarTableRangeError가 그대로 나간다, 멱등 ; findYearGeneration(viewer, year, tx)가 null이면 HolidayYearIncompleteError ; inserted = insertYearConfirmation(viewer, { year, confirmedBy }, tx) ; inserted면 recordAction(…, { tx }) })`. 완결의 기준은 04.2-06의 생성 표시 행이다 — 첫 계획의 「법정 행이 하나 이상」은 법정 행이 일부만 있는 해를 완성된 달력으로 오인했다(Codex 2차 #14 잔여). 보장과 확인이 같은 잠금 트랜잭션이라 그 사이에 다른 변경이 끼지 않는다(Codex 2차 #4). 액션 입력 검사는 정수 모양만 하지만 도메인이 범위와 완결을 다시 본다 — 화면에 보이지 않는 해를 직접 불러도 같은 규칙이다. 확정할 수 있는 해를 올해·다음 해로 더 좁히지 않는다(요구 밖 — 범위와 완결이 Codex #14의 요구다).
- **확정 실패 흉내(개정 — Codex 2차 #9):** 확정이 스스로 후보를 보장하므로 「확정 직전 법정 행을 DB에서 지워 실패시키기」는 보장이 행을 다시 만들어 실패하지 않는다. 화면의 실패 상태는 E2E가 Playwright `page.route`로 확정 액션 요청을 끊어(`route.abort()`) 만든다 — 04.2-12와 같은 방식이고 프로덕션 코드에 실패 주입 경로를 만들지 않는다. 도메인의 `HolidayYearIncompleteError` 경로는 통합이 아무것도 하지 않는 가짜 `ensure`를 주입해 증명한다.
- **확정은 멱등 삽입:** `insertYearConfirmation`은 `ON CONFLICT (year) DO NOTHING RETURNING`(04.2-06)이고, 행이 실제로 들어갔을 때만 행동 로그를 쓴다. 뒤에 누른 사람도 오류 없이 성공 모양을 보고 상태 줄은 먼저 확정한 사람을 읽는다(UI-SPEC S2-e).
- **연도 선택:** `?year`는 행이 있는 해 목록 안의 값만 받고, 아니면 기본 연도(올해·다음 해 중 확정 전인 가장 이른 해, 둘 다 확정이면 올해)로 떨어진다 — 주소창으로 아무 해나 넣어 후보를 만들지 못하게 한다. 후보 보장은 올해·다음 해 두 해만 한다(04.2-06 D-4205의 화면 진입 계기).
- **SYSTEM.md #1은 여기:** UI-SPEC #1 스스로 「`ADMIN_MENUS`와 같은 커밋」을 요구하고 `role-menu.test.ts`가 §6-10 표·`ADMIN_MENUS`·`/admin/<name>/page.tsx` 존재를 함께 대조하므로 메뉴 키·표·페이지 파일이 한 커밋이다(나머지 #2~#7은 04.2-09). DECISIONS 항목은 04.2-09가 이미 #1을 예고했다.
</probe_fallback>

## Review Dispositions

| Codex # / 검사 | 처리 | 한 줄 |
|---|---|---|
| 12 (medium) — 확정 쪽 | 반영 | 확정 기록 + `holiday_change` 로그 한 트랜잭션(`{ tx }`) · 끌 수 없는 종류 · 로그 쓰기 실패 뒤 재시도가 둘 다 남기는지 통합(Task 1). 추가·삭제 쪽은 04.2-12 |
| 14 (medium) | 반영 | `confirmHolidayYear`가 음력 표 범위(후보 보장)와 법정 행 존재를 확정과 같은 트랜잭션에서 검사 · 2036·수동 행만 있던 해를 액션으로 직접 부르는 통합(Task 1) |
| checker claude_md_compliance(plan 10 → 화면 플랜 전부) | 반영 | 독립 DOM 감사 옆 Codex 적대적 디자인 검토 한 번 · 출력 원문 SUMMARY · 미실행이면 보고만(Task 2) |
| Codex 2차 14 잔여 — 「법정 행 하나 = 완성」 | 반영 | 완결 기준을 04.2-06 생성 표시 행(`findYearGeneration`)으로 바꿨다 · 법정 행이 일부만 있는 2030을 확정하면 빠진 법정 행이 채워지고 표시가 생긴 뒤 확정되는 통합 · 가짜 no-op `ensure` → `HolidayYearIncompleteError` 통합(Task 1). 저장소·생성 쪽의 일부 채워진 해 테스트는 04.2-06 |
| Codex 2차 4 (high) — 확정 쪽 | 반영 | 확정의 보장·완결 확인·삽입·로그가 `withHolidayCalendarLock` 한 트랜잭션(D-4223 개정). 잠금·장벽 테스트는 04.2-06·04.2-12 |
| Codex 2차 9 (medium) — 확정 실패 E2E | 반영 | 법정 행 삭제로 실패를 일으키던 E2E를 `page.route` + `route.abort()`로 바꿨다(Task 2) — 확정이 후보를 다시 만들어 옛 방식은 실패하지 않는다 |
| Codex 2차 7 (medium) — 이 플랜의 tx 사용 | 반영(04.2-06 소유) | 확정은 `withHolidayCalendarLock`이 주는 `DbOrTx`만 쓴다 — 공용 타입 확장(`delete`·`execute`)은 04.2-06이 `db/client.ts`를 소유해 한다(depends_on 04.2-06) |

### Review Dispositions (코디네이터 — 사용성 원칙)

| 항목 | 처리 | 한 줄 |
|---|---|---|
| 화면 안내 글자 최소 | 반영 | 확정 전 연도 상태 줄을 `후보 · {N}일`로 줄인다(「규칙으로 만든」 설명과 「확정 전」 중복 제거 — 상태·건수만). UI-SPEC 문구라 04.2-12 「UI-SPEC 변경 요청」 R7로 디자인 게이트에 넘기고 Task 1 precondition이 반영을 확인한다. 확정은 확인 모달 없는 1차 버튼 하나 그대로(UI-SPEC S2-e — 이미 원칙에 맞다) |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 「관리」 인덱스 `공휴일` → /admin/holidays 다음 해 후보 표 → `{연도}년 공휴일 확정`(한 트랜잭션 확정 + `holiday_change` 로그) → 상태 줄 `확정 · 시각 이름 · N일` (E2E로 끝까지)</name>
  <files>domain/permissions/menus.ts, ui/shell/role-menu.ts, docs/design/SYSTEM.md, test/unit/ui/role-menu.test.ts, test/e2e/admin-nav.spec.ts, domain/holidays/admin.ts, app/(app)/admin/holidays/page.tsx, app/(app)/admin/holidays/holidays.module.css, app/(app)/admin/holidays/actions.ts, app/(app)/admin/holidays/actions.registry.ts, app/(app)/admin/holidays/confirm-year.tsx, test/integration/leak-scan.test.ts, test/integration/holidays-admin.test.ts, test/e2e/holidays.spec.ts</files>
  <precondition>04.2-03이 끝나 `domain/action-log/record.ts`에 `holiday_change`가 있고(`grep -q "holiday_change" domain/action-log/record.ts`) `RecordActionDeps.tx`(04-32)가 있다 · 04.2-06이 끝나 `repositories/holidays.ts`에 `insertYearConfirmation`·`findYearGeneration`이 있고 `domain/holidays/candidates.ts`에 `withHolidayCalendarLock`·`ensureHolidayCandidatesLocked`가 있다(`grep -q "findYearGeneration" repositories/holidays.ts && grep -q "ensureHolidayCandidatesLocked" domain/holidays/candidates.ts`) · 디자인 게이트가 04.2-12 「UI-SPEC 변경 요청」 R7(연도 상태 줄 `후보 · {N}일`)을 적용했다(`grep -c "규칙으로 만든 [N0-9{]" .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 0) — 아니면 멈추고 보고한다. 게이트가 다른 줄 모양으로 정했으면 그 모양으로 이 플랜의 S2-holiday-table/partial 주석 · Task 1 behavior·⑤ · Task 2 acceptance를 함께 고친 뒤 시작한다</precondition>
  <read_first>
    - `domain/permissions/menus.ts` 20~35행(admin.* 항목 모양) · `domain/seed/index.ts` 130~145행(시스템 관리자는 `MENUS` 전부를 받는다 — 시드 수정 불필요)
    - `ui/shell/role-menu.ts` `ADMIN_MENUS`와 그룹 상수 · `test/unit/ui/role-menu.test.ts`(§6-10 표 해석 · `ADMIN_MENU_KEYS` · 길이 단언 · 페이지 파일 존재 단언) · `test/unit/admin-menu-registry.test.ts`(MENUS ↔ 인덱스 href 대조 — 고칠 필요 없음)
    - `test/e2e/admin-nav.spec.ts` 50~110행(사용자 메뉴에 없어야 할 라벨 목록 · 인덱스에 있어야 할 라벨 목록)
    - `docs/design/SYSTEM.md` §6-10 절 제목과 그룹 표(Grep `### 6-10`)
    - `app/(app)/admin/corp-cards/page.tsx`(전체) · `actions.ts` · `actions.registry.ts` — 세션·`can` 게이트, 읽기용 표, 레지스트리 분리
    - `app/(app)/admin/system-status/page.tsx` — `StatusTag` 글자 변형 사용처
    - `domain/corp-cards/index.ts` 1~40행 — `UserFacingError` 하위 오류, 권한 재검사 모양
    - `repositories/holidays.ts`(`findYearGeneration` · `insertYearConfirmation`) · `domain/holidays/candidates.ts`(`withHolidayCalendarLock` · `ensureHolidayCandidates` · `ensureHolidayCandidatesLocked` — 04.2-06) · `domain/holidays/rules.ts`(`HOLIDAY_KIND_LABELS` · `LunarTableRangeError`) · `domain/holidays/business-day.ts`(`toKstDate` · `formatKstMinute`) — 04.2-02·06 결과
    - `test/integration/leak-scan.test.ts` 1~40행 · `test/unit/leak-scan-coverage.test.ts`(모든 `actions.registry.ts`가 import돼야 한다)
    - `test/e2e/fixtures.ts` · `test/e2e/code-tables-write-gate.spec.ts`(임시 계급 + 권한 칸 켜기)
    - UI-SPEC S2-a·S2-b·S2-e
  </read_first>
  <behavior>
    - 단위(role-menu): `ADMIN_MENU_KEYS` 11개 · §6-10 표 「마스터」 항목이 `사람 · 거래처 · 법인카드 마스터 · 코드표 · 공휴일` 순서이고 `adminIndexGroups` 결과와 원소 단위로 같다 · `app/(app)/admin/holidays/page.tsx`가 있다
    - 통합: 빈 표에서 `loadHolidayAdmin(관리자, {})`를 `Promise.all`로 두 번 → 오류 없음, 올해·다음 해 행 날짜가 모두 서로 다르고 오름차순(엣지 #4·#7 앞 절반)
    - 통합: `confirmHolidayYear(관리자A, Y)`와 `confirmHolidayYear(관리자B, Y)`를 `Promise.all` → 둘 다 성공 · `holiday_year_confirmations` 행 1개 · 행동 로그 `holiday_change`(`detail.op = "confirm"`) 1줄 · 확정자는 둘 중 하나(엣지 #7 뒤 절반)
    - 통합(원자성): `confirmHolidayYear(관리자, Y, { recordAction: 던지는 가짜 })` → 오류 · 확정 행 0 · 이어서 주입 없이 다시 → 확정 행 1 · 로그 1(Codex #12)
    - 통합(설정 무관): `action_log.optional_types`를 빈 목록으로 바꾼 뒤 확정 → 로그 1(끌 수 없음)
    - 통합(범위·완결 — Codex #14): `confirmHolidayYearAction({ year: 2036 })`(액션을 직접 부른다) → 실패(`LunarTableRangeError` 문구) · 2036 확정 행 0 · 2030에 수동 행 1개만 넣은 뒤 `confirmHolidayYear(관리자, 2030)` → 성공이고 확정 뒤 2030 생성 표시 행이 있고 2030 법정 행 날짜 집합이 `generateHolidayRules(2030)`의 법정 날짜 집합과 같다
    - 통합(일부 채워진 해 — Codex 2차 #14 잔여): 테스트가 2029 법정 행 하나(1월 1일)만 DB에 직접 넣고 생성 표시는 넣지 않는다 → `confirmHolidayYear(관리자, 2029)` → 성공 · 2029 법정 행 날짜 집합이 `generateHolidayRules(2029)`의 법정 날짜 집합과 같다(빠진 행이 채워졌다) · 생성 표시 행 1 · 확정 행 1
    - 통합(완결 실패): 아무것도 하지 않는 가짜 `ensure`를 주입하고 생성 표시가 없는 해(2031)를 확정 → `HolidayYearIncompleteError` · 2031 확정 행 0 · 로그 0
    - 통합: `admin.holidays` view 없는 viewer → `loadHolidayAdmin`이 `HolidayForbiddenError` · view만 있는 viewer → `confirmHolidayYear`가 `HolidayForbiddenError`
    - E2E(PC): 시스템 관리자 → `/admin` → 링크 `공휴일` → `/admin/holidays?year={Y+1}` → 캡션 `{Y+1}년 공휴일` · 머리글 `날짜` `요일` `이름` `구분` `동작` · 상태 줄 `후보 · N일`(사용성 원칙 R7) → `{Y+1}년 공휴일 확정` 클릭 → 버튼이 사라지고 상태 줄이 `확정 · YYYY-MM-DD HH:MM {이름} · N일` · 토스트 없음
    - E2E(PC): 보기만 가진 임시 계급 → 같은 화면에서 확정 버튼 0개 · 권한 없는 계급 → 404
  </behavior>
  <action>
① **RED 먼저.** `test/unit/ui/role-menu.test.ts`의 `ADMIN_MENU_KEYS`에 `admin.holidays`를 더하고 길이 단언을 11로 바꾼다(항목·숫자만 — 다른 단언은 그대로). `test/e2e/admin-nav.spec.ts`의 두 라벨 목록에 `공휴일`을 더하고 주석과 테스트 이름의 「10개」를 「11개」로 고친다. `test/integration/holidays-admin.test.ts`(신규)와 `test/e2e/holidays.spec.ts`(신규)에 behavior를 쓴다. E2E는 KST 올해 Y를 테스트 안에서 계산하고, `beforeAll`에서 테스트 DB의 `holiday_year_confirmations`에서 Y+1 행만 지워 되풀이 실행에도 「확정 전」에서 시작하게 한다(프로덕션 코드에 되돌리기 경로를 만들지 않는다 — U-2). 같은 파일의 테스트는 직렬로 돈다 — 확정 상태를 바꾸는 E2E는 이 파일 밖에 두지 않는다.

② **메뉴 키와 SYSTEM.md #1(같은 커밋).** `domain/permissions/menus.ts` MENUS에 `{ key: "admin.holidays", label: "공휴일" }` 한 항목. `ui/shell/role-menu.ts` `ADMIN_MENUS`의 코드표 다음(「마스터」 그룹 끝)에 `{ key: "admin.holidays", label: "공휴일", href: "/admin/holidays", group: ADMIN_GROUP_MASTER }`. `docs/design/SYSTEM.md` §6-10: 그룹 표 「마스터」 줄 끝에 ` · 공휴일`, 절 제목(또는 본문)의 관리자 화면 개수 표기를 하나 늘려 `11개`로 쓴다. 다른 문장은 다시 쓰지 않는다. 시드는 고치지 않는다.

③ **도메인** `domain/holidays/admin.ts`(신규): 메뉴 상수 `admin.holidays`, `class HolidayForbiddenError extends UserFacingError`, `class HolidayYearIncompleteError extends UserFacingError`(문구 `{연도}년 후보가 아직 없습니다 · 다시 시도`). `loadHolidayAdmin(viewer, { requestedYear?: number }, deps?)` — `can(viewer, "admin.holidays", "view")` 거짓이면 던진다 → KST 올해 Y(`toKstDate`)와 Y+1마다 `ensureHolidayCandidates`를 try로 부르고 `LunarTableRangeError`면 그 해의 오류 문구를 모은다(그 밖의 오류는 던진다) → `listHolidayYears` · 해마다 확정 여부 → 선택 연도(`<probe_fallback>` 「연도 선택」) → `listHolidaysForYear`와 확정 기록. 반환 `HolidayAdminView = { years: { year, confirmed }[]; year; rows: HolidayRowView[]; confirmation: { confirmedAt, confirmedByName } | null; count; candidateError: string | null }`, `HolidayRowView = { id; date; monthDay(MM-DD); weekday(월~일 한 글자); name; kind; kindLabel; deletable }` — `deletable`은 구분이 `temporary`·`election`이고 날짜가 KST 오늘보다 뒤일 때만 참(04.2-12가 쓴다). `confirmHolidayYear(viewer, year, deps?: { recordAction?; ensure? })` — `<probe_fallback>` D-4223 그대로(per Codex 2차 #4 — `withHolidayCalendarLock` 안에서만, per Codex 2차 #14 잔여 — 완결은 `findYearGeneration`으로). `DbOrTx`는 04.2-06이 넓힌 공용 타입(`repositories/document-counters.ts` 재수출)을 쓴다(Codex 2차 #7).

④ **액션** `app/(app)/admin/holidays/actions.ts`(신규, `"use server"`): `confirmHolidayYearAction` — `authedActionClient` + zod `{ year: z.number().int() }` → `confirmHolidayYear(ctx.viewer, year)` → `revalidatePath("/admin/holidays")`, 반환 `{ ok: true }`. 파일 끝에 `import "./actions.registry"`. `actions.registry.ts`(신규): `registerAction({ name: "confirmHolidayYearAction", menu: "admin.holidays", action: "write", dtoName: null })`. `test/integration/leak-scan.test.ts` import 목록에 `import "@/app/(app)/admin/holidays/actions.registry";` 한 줄만 더한다.

⑤ **화면** `app/(app)/admin/holidays/page.tsx`(신규): `dynamic = "force-dynamic"` · `getSession()` 없으면 `redirect("/login")` · view 없으면 `notFound()` · `canWrite = await can(viewer, "admin.holidays", "write")` · `loadHolidayAdmin`. 순서: `PageHeader` `공휴일` → 필터 줄(왼쪽 연도 목록: 현재 연도는 `aria-current="page"` 글자, 나머지는 `?year=` 3차 링크) → 연도 상태 줄(`--fs-sm --muted`, 상태 낱말 `StatusTag` 글자 변형: 확정 전 `후보` + ` · {N}일`(설명 꼬리 없음 — 사용성 원칙, 04.2-12 R7), 확정 `확정`(`--success`) + ` · {formatKstMinute} {이름} · {N}일`) + 확정 전·`canWrite`·`candidateError` 없음일 때만 `ConfirmYear` → 표(`<table>` + `<caption className="sr-only">{연도}년 공휴일</caption>`, 머리글 흰 바탕, 칸 날짜(`MM-DD`, `tabular-nums`, nowrap) · 요일 · 이름(줄바꿈 허용, `word-break: keep-all`) · 구분(글자만) · 동작(이 플랜에서는 비어 있고 `canWrite`가 없으면 칸 자체가 없다)). `candidateError`가 선택 연도의 것이면 표 자리에 `ListEmpty tone="error"` 그 문구(버튼 없음), 조회 자체가 실패하면 페이지가 잡아 `ListEmpty tone="error" message="공휴일을 불러오지 못했습니다 · 다시 시도"` + 2차 `다시 시도`. `confirm-year.tsx`(신규, 클라이언트): 1차 `{연도}년 공휴일 확정` · 대기 `{연도}년 공휴일 확정…` 비활성 · 실패하면 원래 라벨 + 옆 `--fs-sm --danger` `확정하지 못했습니다 · 다시 시도` · 성공하면 `router.refresh()`(토스트 없음, §7-6). `holidays.module.css`(신규): tokens.css 토큰만.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts test/unit/admin-menu-registry.test.ts test/unit/leak-scan-coverage.test.ts test/unit/page-auth-guard.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — §6-10 표와 `ADMIN_MENUS`가 원소 단위로 어긋났거나(같은 커밋 규칙 위반), `ADMIN_MENU_KEYS`가 11이 아니거나, 새 `actions.registry.ts`가 누수 스캔에 import되지 않았거나, 새 페이지에 세션 가드가 없다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/holidays-admin.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 동시 화면 로드가 중복 날짜를 만들었거나, 동시 확정이 확정 행·행동 로그를 두 개 남겼거나, 로그 쓰기 실패 뒤 확정이 남았거나 재시도가 로그를 못 남겼거나, 설정으로 로그가 꺼졌거나, 2036이 확정됐거나 생성 표시 없이 확정됐거나 일부만 채워진 해의 빠진 법정 행이 확정 뒤에도 없거나, 권한 없는 viewer가 읽거나 확정했거나, 누수 스캔이 새 등록을 거부했다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint(경계·stylelint 토큰)·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/admin-nav.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 인덱스에서 `공휴일`로 닿지 못했거나, 확정 뒤 상태 줄이 `확정 · … · N일`로 바뀌지 않았거나, 보기 전용 계급에게 확정 버튼이 보였거나, 기존 관리자 탐색 E2E가 11번째 항목으로 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "admin.holidays" domain/permissions/menus.ts ui/shell/role-menu.ts test/unit/ui/role-menu.test.ts`가 파일마다 1 이상이다
    - `grep -n "관리자 화면 11개" docs/design/SYSTEM.md`가 한 줄 이상이고 `grep -n "관리자 화면 10개" docs/design/SYSTEM.md` 출력이 비어 있다
    - `grep -n "admin/holidays/actions.registry" test/integration/leak-scan.test.ts`가 한 줄이다
    - `grep -c "document_update\|document_create\|document_delete" domain/holidays/admin.ts`가 0이다(공휴일은 `holiday_change`만)
    - 통합: 동시 확정 → 확정 행 1 · 로그 1(엣지 #7) · 로그 실패 → 확정 0 · 2036 → 오류(Codex #14) · 일부 채워진 2029 → 법정 행 채움 + 확정 · no-op 보장 → `HolidayYearIncompleteError`(Codex 2차 #14 잔여)
    - `grep -v '^\s*//' domain/holidays/admin.ts | grep -c "withHolidayCalendarLock"`가 1 이상이고 `grep -v '^\s*//' domain/holidays/admin.ts | grep -c "findYearGeneration"`가 1 이상이다
  </acceptance_criteria>
  <done>관리자가 「관리」에서 공휴일 화면에 닿아 다음 해 후보를 보고 확정할 수 있고, 확정과 그 로그는 함께만 남으며, 범위 밖·미완성 해는 확정되지 않는다</done>
  <reversibility rating="costly">`holiday_change` 로그 모양(`detail.op`)이 행동 로그 화면·내보내기에 남는다 — 바꾸면 이미 쌓인 행과 다른 모양이 섞인다</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 표·상태 줄의 다섯 상태 — 음력 표 밖 오류 줄 · 조회 실패 · 연도 링크 · 긴 이름 · 확정 대기·실패 · Codex 디자인 검토</name>
  <files>app/(app)/admin/holidays/page.tsx, app/(app)/admin/holidays/holidays.module.css, app/(app)/admin/holidays/confirm-year.tsx, test/e2e/holidays.spec.ts</files>
  <read_first>
    - `app/(app)/admin/holidays/*`(Task 1 결과)
    - `ui/list-empty/ListEmpty.tsx` — `tone="error"` · `action` 생략
    - UI-SPEC S2-b·S2-e · Copywriting S2 행
  </read_first>
  <behavior>
    - E2E: 연도 링크가 행이 있는 해 전부 오름차순 · 현재 연도는 링크가 아닌 `aria-current="page"` 글자 · 한 해가 1행이어도 같은 행 템플릿
    - E2E: `?year=1999`(행이 없는 해) → 기본 연도로 떨어진다(후보 생성 없음 — 1999 행 0)
    - E2E: `제22대 국회의원 선거일` 같은 긴 이름 행(테스트 DB 직접 삽입) → 이름 칸에서 줄바꿈 · 말줄임 없음
    - E2E: 확정 대기 중 라벨 `{연도}년 공휴일 확정…` 비활성 · 확정 실패(테스트가 `page.route`로 확정 액션 요청을 끊는다 — `route.abort()`, Codex 2차 #9) → 원래 라벨 + `확정하지 못했습니다 · 다시 시도` · 그 해 확정 행 0(DB로 확인) · 끊기를 푼 뒤 다시 누르면 확정된다
    - 통합 수준 확인은 Task 1 — 이 Task는 화면 상태만
  </behavior>
  <action>
① behavior를 `test/e2e/holidays.spec.ts`에 먼저 쓴다(RED). 실패 흉내는 Playwright `page.route`로 확정 액션의 POST 요청만 `route.abort()`하는 것으로만 한다(per Codex 2차 #9 — 확정이 후보를 스스로 보장하므로 법정 행을 DB에서 지우는 방식은 실패를 만들지 못한다. 프로덕션 코드에 실패 주입 경로를 만들지 않는다). 가로채기는 그 테스트 안에서 `page.unroute`로 푼다.

② 실패하는 것만 고친다.

③ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`, S2-holiday-table 일곱 줄·S2-confirm 다섯 줄 판정 — backstop 포함) → **Codex 적대적 디자인 검토(소유자 지시)**: `codex exec -s read-only -C /home/user/ERP_PLANT8_260917 "적대적 디자인 검토. 기준: docs/design/SYSTEM.md(§6-1·§6-10·§7-1·§7-3·§7-6), docs/design/tokens.css, .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md 의 S2-a·S2-b·S2-e와 Copywriting S2. 대상: git diff origin/main...HEAD -- \"app/(app)/admin/holidays\" ui/shell/role-menu.ts docs/design/SYSTEM.md. 토큰 밖 값·계약 문구 불일치·상태 누락(빈·로딩·오류·부분·넘침·음력 표 밖)·권한별 DOM 노출(보기 전용에게 버튼)·표 접근성(caption·aria-current)을 심각도(high/medium/low)와 파일:행으로 보고하라. 코드를 고치지 마라." < /dev/null` — 출력 원문을 SUMMARY 「Codex 디자인 검토」 절에 붙이고, high는 전체 게이트 전에 고치거나 고치지 않는 이유를 한 줄씩 적는다. `codex`가 없거나 0이 아닌 코드로 끝나면 명령·종료 코드·stderr 끝 20줄을 「Codex 디자인 검토 미실행」으로 적고 **다른 검토로 대신하지 않는다** → 수정 → 전체 게이트 한 번(아래 verify).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>stylelint(토큰 밖 값)·eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/admin-nav.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 연도 링크·aria-current·기본 연도 규칙이 틀렸거나, 없는 해에 후보가 생겼거나, 긴 이름이 말줄임됐거나, 확정 대기·실패 문구가 카피 계약과 다르거나, axe 위반이 생겼다</fails_when>
  </verify>
  <acceptance_criteria>
    - 카피 문구 `확정하지 못했습니다 · 다시 시도` · `공휴일을 불러오지 못했습니다 · 다시 시도` · `후보`가 화면 코드에 글자 그대로 있고(Grep), `grep -c "규칙으로 만든" "app/(app)/admin/holidays/page.tsx"`가 0이다(연도 상태 줄 `후보 · {N}일` — 사용성 원칙 R7)
    - SUMMARY에 독립 DOM 감사 결과(backstop은 감사 판정 또는 human_needed)와 「Codex 디자인 검토」 절(출력 원문 또는 「미실행」 + 오류)이 있다
  </acceptance_criteria>
  <done>공휴일 표와 확정 버튼이 UI-SPEC S2의 상태를 모두 계약대로 보이고, 화면 검토에 Codex 검토가 들어갔다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `confirmHolidayYearAction` | 클라이언트가 보낸 연도가 공휴일 표(영업일 계산의 단일 출처)의 확정 상태를 바꾼다 |
| 권한표 → 화면·도메인 | `admin.holidays` view/write가 화면 노출과 쓰기를 가른다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-70 | Elevation of Privilege | `confirmHolidayYearAction` | high | mitigate | 화면은 write가 없으면 버튼을 렌더하지 않고, 도메인이 `can(viewer, "admin.holidays", "write")`를 다시 검사해 `HolidayForbiddenError` — 통합이 보기 전용 viewer의 직접 호출을 거부하는지 단언 |
| T-4.2-71 | Tampering | 범위 밖·미완성 해 확정 | medium | mitigate | 도메인이 음력 표 범위(잠금 안 후보 보장)와 생성 표시 행을 확정과 같은 `withHolidayCalendarLock` 트랜잭션에서 검사(D-4223) — 액션 직접 호출·일부 채워진 해·no-op 보장 통합(Codex #14 · Codex 2차 #14 잔여) |
| T-4.2-72 | Repudiation | 연도 확정 | medium | mitigate | 끌 수 없는 `holiday_change` 로그를 확정과 한 트랜잭션으로(실제로 들어갔을 때만 1줄) — 로그 실패 롤백·설정 무관 통합(D-4220) |
| T-4.2-73 | Information Disclosure | `/admin/holidays` | low | mitigate | view 없으면 `notFound()`(D-17) + `loadHolidayAdmin` 권한 재검사. 화면이 보이는 사람 정보는 확정한 관리자 이름 하나 |
| T-4.2-75 | Denial of Service | `?year=` 임의 연도 | low | mitigate | 화면의 후보 생성은 올해·다음 해만, `?year`는 행이 있는 해 목록 안의 값만 받는다 |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`
- 단위: `pnpm vitest run --project unit test/unit/ui test/unit/admin-menu-registry.test.ts test/unit/leak-scan-coverage.test.ts test/unit/page-auth-guard.test.ts`
- 통합: `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/holidays-admin.test.ts test/integration/holidays.test.ts test/integration/leak-scan.test.ts`
- 독립 DOM 감사(별도 에이전트, `CI=true`) + Codex 적대적 디자인 검토(Task 2 ③ — 미실행이면 보고만) → 수정 → E2E 한 번: `CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/admin-nav.spec.ts test/e2e/mobile-admin-nav.spec.ts test/e2e/a11y.spec.ts`
- 페이즈 끝 UI 판정은 `/design-review` + Codex 적대적 디자인 검토 → `/qa`(04.2-14 Post-build — 이 플랜이 대신하지 않는다)

</verification>

<success_criteria>

- 관리자가 「관리」 → `공휴일`에서 후보 표를 보고 연도를 확정하는 흐름이 E2E로 CI에 있다(ROADMAP 04.2 기준 1)
- 동시 확정은 확정 기록·행동 로그 한 줄, 표는 날짜 오름차순·중복 없음(엣지 #4·#7), 확정과 로그는 함께만 남는다
- SYSTEM.md §6-10과 `ADMIN_MENUS`·`MENUS`·단위 테스트가 11개로 함께 맞는다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 메뉴 키 | `admin.holidays`(`domain/permissions/menus.ts`) · `ADMIN_MENUS` 「마스터」 끝 `공휴일` → `/admin/holidays` |
| 라우트·화면 | `/admin/holidays`(`page.tsx` · `holidays.module.css` · `confirm-year.tsx`) · 검색 파라미터 `year` |
| Server Action | `confirmHolidayYearAction`({ year }) — 레지스트리 menu `admin.holidays`, write, dtoName null |
| 도메인 | `domain/holidays/admin.ts`: `loadHolidayAdmin` · `confirmHolidayYear` · 타입 `HolidayAdminView` · `HolidayRowView` · 오류 `HolidayForbiddenError` · `HolidayYearIncompleteError` |
| 행동 로그 | `holiday_change`(`detail.op = "confirm"`, entity `holiday_year`) — 확정과 한 트랜잭션 |
| 문서 | `docs/design/SYSTEM.md` §6-10 「마스터」 `공휴일` · 「관리자 화면 11개」(수정 제안 #1) |
| 테스트 | `test/integration/holidays-admin.test.ts` · `test/e2e/holidays.spec.ts` · `test/unit/ui/role-menu.test.ts`(11개) · `test/e2e/admin-nav.spec.ts`(`공휴일`) · `test/integration/leak-scan.test.ts`(import 한 줄) |
| 검토 기록 | SUMMARY 「Codex 디자인 검토」 절 |
| 결정 | D-4220(공휴일 로그 — 끌 수 없는 종류·한 트랜잭션) · D-4223(확정 범위·완결 검사 — 개정: 달력 잠금 안, 생성 표시 기준) · 확정 멱등 삽입 · 연도 선택 규칙 · 확정 실패 E2E는 `page.route` 끊기 |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-11-SUMMARY.md` when done
</output>
