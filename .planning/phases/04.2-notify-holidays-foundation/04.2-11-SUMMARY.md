---
phase: 04.2-notify-holidays-foundation
plan: 11
subsystem: ui
tags: [holidays, admin, server-action, action-log, advisory-lock, playwright]

requires:
  - phase: 04.2-03
    provides: 끌 수 없는 행동 로그 종류 holiday_change · recordAction({ tx })
  - phase: 04.2-06
    provides: withHolidayCalendarLock · ensureHolidayCandidates(Locked) · findYearGeneration · insertYearConfirmation · listHolidaysForYear · listHolidayYears
  - phase: 04.2-09
    provides: SYSTEM.md 수정 제안 #2~#8 반영(#1은 이 플랜) · 「관리」 인덱스 셸 구조
provides:
  - 메뉴 키 admin.holidays(MENUS · ADMIN_MENUS 「마스터」 끝 「공휴일」) · SYSTEM.md §6-10 「관리자 화면 11개」
  - /admin/holidays 화면(연도 링크 · 연도 상태 줄 · 읽기용 표 · 조회 실패·음력 표 밖 오류 줄 · 확정 버튼)
  - confirmHolidayYearAction(누수 스캔 등록, menu admin.holidays write, dtoName null)
  - domain/holidays/admin.ts — loadHolidayAdmin · confirmHolidayYear · HolidayAdminView · HolidayRowView(deletable 포함, 04.2-12가 쓴다) · HolidayForbiddenError · HolidayYearIncompleteError
affects: [04.2-12, 04.2-14, Phase 7 지급일 파생]

actuals:
  tokens: 11850
  tasks: 2
  commits: 2
plan_head_before: ff52b3cb8c8a4991702155114565b47eb112e6a3

tech-stack:
  added: []
  patterns:
    - "연도 확정 = withHolidayCalendarLock 한 트랜잭션(보장 → 생성 표시 확인 → 멱등 삽입 → 삽입됐을 때만 로그)"
    - "확정 버튼은 useAction 대신 액션을 직접 await하고 try/catch — 연결 끊김(route.abort)도 실패 줄로 보인다. 성공이면 pending을 유지한 채 router.refresh()로 버튼이 사라진다"

key-files:
  created:
    - domain/holidays/admin.ts
    - app/(app)/admin/holidays/page.tsx
    - app/(app)/admin/holidays/holidays.module.css
    - app/(app)/admin/holidays/actions.ts
    - app/(app)/admin/holidays/actions.registry.ts
    - app/(app)/admin/holidays/confirm-year.tsx
    - test/integration/holidays-admin.test.ts
    - test/e2e/holidays.spec.ts
  modified:
    - domain/permissions/menus.ts
    - ui/shell/role-menu.ts
    - docs/design/SYSTEM.md
    - test/unit/ui/role-menu.test.ts
    - test/e2e/admin-nav.spec.ts
    - test/integration/leak-scan.test.ts

key-decisions:
  - "D-4220 적용: 확정 로그는 holiday_change, entity holiday_year, entityId String(year), detail { op: confirm, year, count } — count는 같은 tx에서 읽은 그 해 행 수"
  - "D-4223 적용: confirmHolidayYear는 write 재검사 뒤 withHolidayCalendarLock 안에서 ensure(기본 ensureHolidayCandidatesLocked) → findYearGeneration(없으면 HolidayYearIncompleteError) → insertYearConfirmation → inserted일 때만 recordAction({ tx })"
  - "loadHolidayAdmin은 올해·다음 해만 후보를 보장하고 LunarTableRangeError만 그 해 오류 문구로 모은다(다른 오류는 던져 화면의 조회 실패 줄로 간다). ?year는 행이 있는 해 목록 안의 값만 받는다"
  - "SYSTEM.md §6-10은 표 행·절 제목에 더해 ASCII 도식의 「마스터」 목록에도 「공휴일」 한 줄을 넣었다(표와 도식이 어긋나지 않게)"
  - "role-menu.test.ts는 ADMIN_MENU_KEYS·길이 단언 외에 KEY_BY_LABEL(라벨↔키 짝 표)에도 공휴일을 더하고 테스트 이름의 「10개」를 「11개」로 바꿨다 — 짝 표 길이가 항목 수와 같아야 하는 단언이라 빼면 실패한다"

patterns-established:
  - "Server Action을 통합 테스트에서 직접 부를 때: vi.hoisted 세션 홀더 + vi.mock('@/lib/viewer') + vi.mock('next/cache')"

requirements-completed: [ADMN-11]

coverage:
  - id: D1
    description: "「관리」 인덱스 「마스터」 끝 공휴일 → /admin/holidays, MENUS·ADMIN_MENUS·SYSTEM.md §6-10이 11개로 함께 맞는다"
    requirement: ADMN-11
    verification:
      - kind: unit
        ref: "test/unit/ui/role-menu.test.ts"
        status: pass
      - kind: e2e
        ref: "test/e2e/admin-nav.spec.ts (CI=true)"
        status: pass
    human_judgment: false
  - id: D2
    description: "연도 확정 — 한 잠금 트랜잭션 · 멱등 · 로그 원자성 · 끌 수 없음 · 음력 표 밖/미완성 해 거부 · 권한 재검사"
    requirement: ADMN-11
    verification:
      - kind: integration
        ref: "test/integration/holidays-admin.test.ts (10건)"
        status: pass
    human_judgment: false
  - id: D3
    description: "관리자가 다음 해 후보를 보고 확정하면 버튼이 사라지고 상태 줄이 「확정 · 시각 이름 · 공휴일 N일」로 바뀐다 · 보기 전용은 버튼·동작 칸 없음 · 권한 없음 404"
    requirement: ADMN-11
    verification:
      - kind: e2e
        ref: "test/e2e/holidays.spec.ts (CI=true)"
        status: pass
    human_judgment: false
  - id: D4
    description: "표·확정 버튼 상태(연도 링크·aria-current·기본 연도 규칙·긴 이름 줄바꿈 PC/375·확정 대기·실패 줄)"
    requirement: ADMN-11
    verification:
      - kind: e2e
        ref: "test/e2e/holidays.spec.ts 「공휴일 표·확정 버튼의 상태」 (CI=true, 뮤테이션으로 검출력 확인)"
        status: pass
    human_judgment: true
    rationale: "플랜의 독립 DOM 감사(별도 에이전트, CI=true)와 Codex 적대적 디자인 검토가 이 실행에서 돌지 않았다 — 아래 절 참고. SYSTEM.md 시각 일관성은 페이즈 끝 /design-review가 판정해야 한다"

duration: 29min
completed: 2026-09-26
status: complete
---

# Phase 04.2 Plan 11: 공휴일 관리 화면 · 연도 확정 Summary

**`/admin/holidays`에서 올해·다음 해 후보 표를 보고 연도를 확정한다. 확정은 달력 advisory 잠금 트랜잭션 하나에서 후보 보장, 생성 표시 확인, 멱등 확정 삽입, 끌 수 없는 `holiday_change` 로그까지 처리한다. 메뉴 키는 `admin.holidays`이고 SYSTEM.md §6-10은 11개가 됐다**

## Performance

- **Duration:** 29분
- **Started:** 2026-09-26T10:10:19Z
- **Completed:** 2026-09-26T10:39:02Z
- **Tasks:** 2/2
- **Files:** 14개(생성 8 · 수정 6)

## Accomplishments

- 「관리」 → 「마스터」 끝 `공휴일`로 진입한다. `MENUS`·`ADMIN_MENUS`·`ADMIN_MENU_KEYS`와 SYSTEM.md §6-10(표·도식·「관리자 화면 11개」)은 한 커밋(`65fa3ca`)에서 바꿨다.
- `confirmHolidayYear`는 권한을 다시 검사한 뒤 `withHolidayCalendarLock` 안에서 네 단계를 거친다.
  - `ensureHolidayCandidatesLocked`: 음력 표 밖이면 `LunarTableRangeError`
  - `findYearGeneration`: 없으면 `HolidayYearIncompleteError`
  - `insertYearConfirmation`: `ON CONFLICT DO NOTHING`
  - 행이 실제로 들어갔을 때만 `recordAction({ tx })`
- 통합 테스트 10건이 다음을 증명한다.
  - 동시 확정은 확정 행 1개와 로그 1줄만 남긴다.
  - 로그가 실패하면 확정도 0이고, 다시 누르면 확정과 로그가 함께 남는다.
  - `optional_types`를 비워도 로그가 남는다.
  - 액션으로 2036을 직접 불러도 오류이고 확정 행은 0이다.
  - 수동 행만 있던 2030과 법정 행이 일부만 있던 2029는 법정 행이 채워진 뒤 확정된다.
  - 보장 함수가 아무것도 하지 않으면 `HolidayYearIncompleteError`다.
  - 권한이 없으면 두 함수 모두 거부한다.
- 화면 구성: 연도 링크(현재 연도는 `aria-current="page"` 글자), 상태 줄, caption이 있는 흰 머리글 표, 오류 줄 두 가지.
  - 상태 줄: 확정 전 `후보 · 공휴일 {N}일`, 확정 뒤 `확정 · {YYYY-MM-DD HH:MM} {이름} · 공휴일 {N}일`. 시각은 `formatKstMinute`(Intl `Asia/Seoul`)로 쓴다.
  - 오류 줄: 조회 실패, 음력 표 밖 해.
  - 확정 버튼은 write 권한이 있고 확정 전인 연도에만 DOM에 있다.

## Task Commits

1. **Task 1: 트레이서 — 메뉴 · 도메인 · 액션 · 화면 · 통합 · E2E** — `65fa3ca` (feat)
2. **Task 2: 표·상태 줄 다섯 상태 E2E** — `d98010e` (test)

**Plan metadata:** 이 SUMMARY 커밋(docs)

## TDD 기록

- **Task 1 RED:**
  - 단위: `role-menu.test.ts`가 11개 단언 5건에서 AssertionError로 실패했다.
  - 통합: 도메인·액션을 `throw new Error("not implemented")` 뼈대로만 두고 돌려 10/10이 실패했다. 뼈대는 커밋하지 않았고 구현으로 바로 덮었다.
  - E2E(`holidays.spec.ts` Task 1분)는 구현 전에 작성했지만 **구현 전에 실행해 보지는 않았다.** 그때 라우트가 없어 404로 실패할 상태였을 뿐, 실패를 관찰한 기록은 없다.
- **Task 1 GREEN:**
  - 단위 82/82
  - 통합 942/942(`holidays-admin` + `leak-scan`)
  - CI=true E2E 7/7
- **Task 2:** Task 1의 action ⑤가 이미 이 상태들을 구현해서 새 E2E 8/8이 처음부터 통과했다. 그래서 테스트가 실제로 결함을 잡는지 뮤테이션으로 확인했다. 구현을 일부러 깨뜨리고 각 테스트가 실패하는 것을 본 뒤 `git checkout`으로 되돌렸다.
  - 현재 연도를 링크로 바꿈 → 연도 링크 테스트 실패
  - `.name`에 nowrap·ellipsis → 긴 이름(PC) 테스트 실패
  - 실패 줄 제거 → 확정 대기·실패 테스트 실패
  - `?year` 검사 제거 → 1999 테스트 실패

## 게이트 결과 (최종 코드 기준, 이 실행에서 직접 돌림)

| 게이트 | 결과 |
|---|---|
| `pnpm typecheck` | exit 0 |
| `pnpm lint`(eslint + stylelint) | exit 0. 기존 boundaries 사용 중단 경고만 있음 |
| `pnpm lint:sql` | exit 0, 18개 파일에서 0건 |
| 단위 `pnpm vitest run --project unit` | 104개 파일 · 1452/1452 통과 |
| 통합 `pnpm vitest run --project integration` | 60개 파일 · 1557/1557 통과 |
| E2E `CI=true pnpm playwright test holidays admin-nav mobile-admin-nav a11y` | 330/330 통과(4.9분, webServer가 `pnpm build && pnpm start`로 프로덕션 빌드) |
| 새 의존성 | 0개(`package.json`·`pnpm-lock.yaml` 변경 없음) |

## 독립 DOM 감사 — 미실행

플랜 Task 2 ③은 **별도 에이전트**가 `CI=true`로 DOM을 실측 감사하게 한다(CLAUDE.md §6). 이 실행 환경의 실행자에게는 에이전트를 띄우는 도구가 없어 감사를 돌리지 못했다. 실행자가 스스로 한 E2E를 감사로 대신하지 않았다.

- 오케스트레이터나 페이즈 끝 `/design-review`가 S2-holiday-table 일곱 줄과 S2-confirm 다섯 줄을 판정해야 한다.
- backstop인 long-text 줄은 E2E(PC·375px)가 계산된 스타일과 scrollWidth로 단언하지만, 감사 판정으로는 **human_needed**로 남긴다.

## Codex 디자인 검토 — 미실행

Codex 사용 한도가 2026-09-29까지 소진돼 있다. 사용자가 「Opus」로 결정했고 오케스트레이터가 `codex exec` 실행을 금지해서 플랜 Task 2 ③의 명령을 돌리지 않았다. 따라서 명령·종료 코드·stderr는 없다. 다른 검토로 대신하지 않았다. **한도가 풀리면 Codex 재확인이 필요하다**(대상: `app/(app)/admin/holidays` · `ui/shell/role-menu.ts` · `docs/design/SYSTEM.md`).

## Decisions Made

frontmatter key-decisions 참고. 추가 설명:

- 확정 버튼(`confirm-year.tsx`)은 `useAction`을 쓰지 않는다. 액션을 직접 `await`하고 `try/catch`로 감싼다. 이렇게 해야 네트워크가 끊겼을 때(`route.abort`)도 실패 줄이 확실히 보인다.
- 성공하면 `pending`을 유지한 채 `router.refresh()`를 부른다. 다시 그린 화면에서 버튼이 사라질 때까지 라벨이 원래대로 돌아가 깜빡이지 않는다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] role-menu.test.ts의 `KEY_BY_LABEL`에도 `공휴일` 추가**
- **Found during:** Task 1 RED
- **Issue:** 플랜은 「`ADMIN_MENU_KEYS` 항목·숫자만」 바꾸라고 했다. 그런데 같은 파일의 라벨↔키 짝 표는 길이가 전체 항목 수와 같아야 한다고 단언한다.
- **Fix:** 짝 표에 `공휴일: "admin.holidays"` 한 줄을 더하고, 테스트 이름의 「10개」를 「11개」로 바꿨다.
- **Files modified:** test/unit/ui/role-menu.test.ts
- **Committed in:** 65fa3ca

**2. [Rule 2 - Consistency] SYSTEM.md §6-10 ASCII 도식에도 `공휴일` 한 줄 추가**
- **Found during:** Task 1 ②
- **Issue:** 표만 고치면 같은 절의 도식(마스터 목록)과 어긋난다.
- **Fix:** 도식의 「코드표」 아래에 「공휴일」 한 줄만 넣었다. 문장은 다시 쓰지 않았다.
- **Committed in:** 65fa3ca

---

**Total deviations:** 2 auto-fixed (Rule 3 1건 · Rule 2 1건)
**Impact on plan:** 둘 다 같은 커밋 규칙(§6-10 ↔ ADMIN_MENUS)을 지키려는 최소 변경이다. 범위는 넓어지지 않았다.

## Issues Encountered

- 통합 실행이 2분을 넘겨 멈춘 것처럼 보였다. `pg_stat_activity`를 확인하니 시드 INSERT가 진행 중이었다. `leak-scan.test.ts`의 테스트 932건이 건마다 TRUNCATE와 시드를 반복해서 느린 것일 뿐 멈춘 게 아니었고, 942/942 통과로 끝났다.

## Known Stubs

- `app/(app)/admin/holidays/page.tsx`: write 권한이 있으면 「동작」 칸이 보이지만, 이 플랜에서는 모든 행의 칸이 비어 있다(`<td />`). 플랜이 명시한 의도된 상태이고, `삭제`·`공휴일 추가`·폰 칸 접기는 04.2-12가 채운다. `HolidayRowView.deletable`은 이미 계산해 둔다.
- 교차 페이즈 결함 대장(`.planning/WINDOWS.md`)에는 적지 않았다. 오케스트레이터가 `.planning` 쓰기를 SUMMARY로 한정했기 때문이다.

## Threat Flags

없음. 새 표면은 `confirmHolidayYearAction` 하나이고 플랜 threat_model T-4.2-70~75가 모두 다룬다. 통합 테스트로 70·71·72·73을, E2E와 `?year` 규칙으로 73·75를 확인했다.

## /review · /cso · /design-review로 넘길 것

- `/design-review` → `/qa`: 위 「독립 DOM 감사 — 미실행」과 「Codex 디자인 검토 — 미실행」 항목.
- `/cso`(권한·외부 입력): 도메인이 `admin.holidays` view/write를 다시 검사하는지, 액션 입력이 정수 모양만 검사하고 나머지는 도메인이 판단하는지 확인한다.
  - 참고로 `confirmHolidayYear`는 연도 범위를 올해·다음 해로 좁히지 않는다(플랜 D-4223 의도). 쓰기 권한이 있는 사람은 음력 표 안의 과거 해(예: 2025)도 확정할 수 있다.
- `/review`: `loadHolidayAdmin`이 화면을 열 때마다 달력 잠금 트랜잭션을 최대 두 번 연다. 올해·다음 해의 생성 표시가 없을 때만 그렇고, 빠른 길은 잠금이 없다.
  - `role-menu.ts` 머리 주석의 「관리자 화면 10종 … 열 개 전부 확인」 문장은 이제 11개와 맞지 않는다. 요청받지 않은 주석 수정이라 고치지 않았다.

## Next Phase Readiness

04.2-12(수동 추가·삭제·되돌리기·폰 칸 접기)를 시작할 수 있다. 그 플랜이 쓸 `HolidayRowView.deletable`, 「동작」 칸 자리, `withHolidayCalendarLock` 확정 선례가 준비돼 있다.

## Self-Check: PASSED

- 생성 파일 8개 존재 확인
- 커밋 `65fa3ca`·`d98010e` 존재 확인
- acceptance grep 전부 통과
  - `admin.holidays` 파일마다 1 이상
  - 「관리자 화면 11개」 1줄 · 「10개」 0줄
  - leak-scan import 1줄
  - `document_*` 0
  - `withHolidayCalendarLock`·`findYearGeneration` 각 2
  - 카피 세 문구 존재 · 「규칙으로 만든」 0 · 「 · 공휴일 」 2
