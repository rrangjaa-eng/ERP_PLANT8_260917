---
phase: 04.2-notify-holidays-foundation
plan: 12
type: execute
wave: 5
depends_on: [04.2-11]
files_modified:
  - domain/holidays/admin.ts
  - app/(app)/admin/holidays/page.tsx
  - app/(app)/admin/holidays/holidays.module.css
  - app/(app)/admin/holidays/actions.ts
  - app/(app)/admin/holidays/actions.registry.ts
  - app/(app)/admin/holidays/holiday-form.tsx
  - app/(app)/admin/holidays/delete-holiday.tsx
  - app/(app)/admin/holidays/delete-undo.tsx
  - app/(app)/admin/holidays/added-toast.tsx
  - test/unit/ui/admin-master-list-first.test.ts
  - test/unit/ui/holiday-undo.test.ts
  - test/integration/holidays-admin.test.ts
  - test/e2e/holidays.spec.ts
  - test/e2e/mobile-holidays.spec.ts
autonomous: true
requirements: [ADMN-11]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "관리자는 `/admin/holidays`의 `공휴일 추가`(`?new=1`)로 임시공휴일·선거일만, KST 내일 이후 날짜로만 넣고, 지울 수 있는 행은 그 두 구분 가운데 날짜가 KST 오늘보다 뒤인 행뿐이다 — 규칙 행(법정 공휴일·대체공휴일)은 관리자가 고치거나 지울 수 없다(UI-SPEC U-3). Playwright E2E로 CI에 있다(ROADMAP 04.2 기준 1)"
    - "공휴일 추가·삭제는 달력 잠금 트랜잭션(`withHolidayCalendarLock` — 04.2-06) 하나에서 (가) 수동 행을 넣거나 지우고 (나) 바뀐 날짜의 해 Y에 대해 원래 해(`origin_year`) Y-1부터 표 마지막 해까지의 **미래** 대체공휴일을 한 번에 다시 정하고(`recomputeFutureSubstitutes(Y - 1, …)` 한 번 — 12월 공휴일의 대체일이 다음 해로 넘어간 경우와 옮겨진 대체일이 뒤 해의 대체일을 밀어내는 사슬을 잡는다 · Codex 3차 추가 #2) (다) 끌 수 없는 행동 로그 `holiday_change`(detail `op: \"add\"`·`\"delete\"`, 날짜·이름·구분)를 같은 tx로 쓴다 — 로그 쓰기가 실패하면 행 변경과 재계산이 함께 롤백된다(D-4220 · Codex #2·#12 · Codex 2차 #4·#5)"
    - "미래 대체공휴일 날짜에 임시공휴일을 넣으면 그 날은 임시공휴일이 되고 대체공휴일은 규칙대로 다음 비공휴일로 옮겨진다(예: 2027-10-04 → 개천절 대체일 2027-10-05). 그 임시공휴일을 지우면 대체공휴일이 다시 10-04로 돌아온다 — 두 방향 모두 04.2-06의 두 삽입 순서와 같은 표를 만든다(Codex #2)"
    - "해를 넘는 대체일도 같은 경로로 움직인다: 수동 2027-12-27~12-31이 있으면 2027 기독탄신일 대체일은 2028-01-03(`origin_year = 2027`)이고, 관리자가 2028-01-03을 넣으면 그 대체일이 2028-01-04로 옮겨지며, 그 행을 지우면 다시 2028-01-03이다(Codex 2차 #5)"
    - "같은 해의 수동 행 둘(2027-10-04·10-05, 개천절 대체일 10-06)을 두 관리자가 동시에 지우면 두 트랜잭션이 실제로 겹친 뒤에도(장벽으로 증명) 끝난 표의 2027 개천절 대체일은 정확히 2027-10-04 하나다 — 대체일이 둘 남지 않는다(Codex 2차 #4)"
    - "같은 행을 동시에 두 번 지우면 둘 다 오류 없이 끝나고 행은 사라지며 삭제 로그는 한 줄이다. 같은 날짜를 동시에 두 번 넣으면 하나는 성공하고 하나는 `이미 공휴일입니다(…)` 오류다(Codex #12)"
    - "삭제에는 확인 단계가 없다 — 3차 `삭제`를 누르면 바로 지우고, 서버에서 다시 그린 표(재계산으로 옮겨진 대체공휴일 포함, 토스트 없음) 위 한 줄에 `{YYYY-MM-DD} {이름} 삭제됨` + 3차 `되돌리기`가 남는다. `되돌리기`는 지운 행과 같은 날짜·이름·종류로 기존 추가 경로(`addHolidayAction` → 행 + 대체일 재계산 + `holiday_change` op `add`, 한 트랜잭션)를 다시 부른다. 화면의 1차 버튼은 늘 하나다(SYSTEM.md §7-1 「1차 1개」) — UI-SPEC S2-f(62d1392) · /plan-design-review #1 사용자 결정 「되돌리기」(2026-09-25) · #8. D-4224(확인 문구)는 이 결정으로 폐기됐다"
    - "공휴일 추가 폼은 고를 것을 줄인다 — 날짜 칸은 날짜 입력(`type=\"date\"`)이고 `min` = KST 내일, `max` = `{LUNAR_TABLE_LAST_YEAR}-12-31`(지금 `2035-12-31`)이라 달력 선택이 그 범위로 좁혀지며, `?new=1`로 열리면 날짜 칸에 포커스가 있다. `noValidate`라 키보드로는 범위 밖 값을 적을 수 있으므로 서버 칸 오류(형식 · 오늘이나 과거 · 음력 표 밖 해 `{연도}년은 음력 표에 없습니다 · {마지막 해}년 이전 날짜를 적어 주세요` · 중복)가 마지막 관문이다. 종류는 `임시공휴일`이 미리 골라져 있어 빈 칸이 되지 않는다 — 막힘 이유는 날짜·이름만 센다(사용성 원칙 · R5·R6 · 디자인 리뷰 #9·#14)"
    - "화면 검토에 Codex(GPT) 적대적 디자인 검토 한 번이 들어가고 그 출력 원문이 SUMMARY에 붙는다 — codex가 실행되지 않으면 그 사실과 오류를 적고 다른 검토로 대신하지 않는다(소유자 지시)"
    - "UI S2-holiday-table/overflow — 폰 375px에서 P1(날짜·이름·동작)만 칸으로 남고 `요일 · 구분`이 행 아래 접힌 줄로 가며 가로 스크롤이 없다"
    - "UI S2-add-form/empty — 폼이 열리면 날짜 칸에 포커스가 있고, 필수 칸이 비면 1차 `공휴일 추가`가 비활성이며 옆에 `추가할 수 없음 — 날짜 · 이름 2칸 · 날짜 적기`와, 그 칸으로 포커스를 옮기는 3차 다음 한 수 버튼이 있다. 종류는 `임시공휴일`이 골라져 있다"
    - "UI S2-add-form/loading — 제출 중 라벨이 `공휴일 추가…`이고 폼의 다른 버튼도 비활성이며 300ms를 넘으면 상단 2px 진행 바가 보인다"
    - "UI S2-add-form/error — 서버 칸 오류(날짜 형식·오늘이나 과거·음력 표 밖 해·중복)는 해당 칸 아래 `원인 · 다음 행동`으로 보이고 입력값이 남는다. 폼 전체 실패는 Actions 이유 자리에 `추가하지 못했습니다 · 다시 시도`다"
    - "UI S2-add-form/partial — 일부 칸만 채우면 비활성 이유가 남은 빈 칸 수와 이름만 센다(예: `이름 1칸`)"
    - "UI S2-add-form/overflow — 폰 375px에서 라벨이 위로 가고 칸이 전폭이며 폼 폭은 PC에서 720(single-column) 이하다. 날짜 칸 `min`은 KST 내일, `max`는 음력 표 마지막 해의 12-31이다"
    - "UI S2-add-form/long-text — 이름 칸은 480px 긴 칸이고 긴 입력은 칸 안에서 가로로 이어지며 서버가 받은 이름 전체가 표에 줄바꿈으로 보인다"
    - "UI S2-confirm/overflow — 폰에서 버튼이 상태 줄 아래로 내려가 전폭이 되고 보이는 높이는 40 이상, 누르는 영역은 44×44 이상이다"
    - "누르는 영역 — 폰 375px에서 확정·필터 줄 `공휴일 추가`·폼 1차 `공휴일 추가`·`취소`·`삭제`·`되돌리기`·연도 링크의 누르는 영역이 모두 44×44 이상이다. 보이는 높이가 40인 버튼은 보이는 높이를 그대로 두고 누르는 영역만 넓힌다(UI-SPEC Spacing 「터치 목표」 · 디자인 리뷰 #7)"
    - "UI S2-delete/empty — 규칙 행·과거 행·오늘 행은 동작 칸이 비어 있고 write 권한이 없으면 동작 칸 자체가 없다"
    - "UI S2-delete/loading — 누른 행의 3차 라벨이 `삭제…`이고 비활성이다. 되돌리기 대기 중에는 `되돌리기…`이고 비활성이다"
    - "UI S2-delete/error — 삭제 실패 시 행이 남고 그 행 동작 칸에 `삭제하지 못했습니다 · 다시 시도`가 보인다. 되돌리기 실패는 결과 줄이 `되돌리지 못했습니다 · …`로 바뀐다"
    - "UI S2-delete/populated — 3차 `삭제`를 누르면 확인 없이 지워지고 연도 상태 줄 아래에 `{날짜} {이름} 삭제됨` + 3차 `되돌리기`가 보이며 포커스가 `되돌리기`로 간다"
    - "UI S2-delete/partial — 성공하면 화면을 서버에서 다시 그려 지운 행이 사라지고 재계산으로 바뀐 대체공휴일 행이 함께 보이며, 상태 줄 N은 다시 그린 그 해 행 수다. 토스트는 없다. `되돌리기`를 누르면 같은 날짜·이름·종류로 행이 돌아오고 결과 줄이 사라진다"
    - statement: "UI S2-delete/overflow — 폰 375px에서 결과 줄이 줄바꿈되어 가로 스크롤 없이 보이고 `삭제`·`되돌리기`의 누르는 영역이 44×44 이상이다"
      verification: backstop
    - "UI S2-delete/zero-one-many — 삭제는 행마다 따로다. 결과 줄은 마지막으로 지운 한 건만 보이고, 다른 행을 지우면 그 행의 줄로 바뀐다"
    - "UI S2-delete/long-text — 결과 줄은 긴 공휴일 이름을 그대로 넣고 줄바꿈하며 말줄임하지 않는다"
    - "결과 줄 수명 — 시간이 지나도 사라지지 않고, 연도를 바꾸거나 공휴일 추가 폼을 열거나 화면을 떠나면 사라진다. `되돌리기`가 거절되면(그사이 그 날짜가 오늘 이전이 됐거나 다른 공휴일이 들어섰다) 결과 줄이 `되돌리지 못했습니다 · {원인}`이 되고 `되돌리기`가 사라진다. 연결·서버 실패면 `되돌리지 못했습니다 · 다시 시도`이고 `되돌리기`가 남는다(UI-SPEC S2-f)"
  prohibitions:
    - statement: "지난 날짜(오늘 포함)의 공휴일 행은 관리자 화면이나 액션을 통해 추가·삭제·수정되어서는 안 되고, 규칙으로 만든 행(법정 공휴일·대체공휴일)은 관리자가 직접 지우거나 고칠 수 없다 — 규칙 행이 바뀌는 경로는 수동 공휴일 추가·삭제와 같은 트랜잭션에서 **미래(내일 이후)** 대체공휴일을 규칙대로 다시 계산하는 것 하나뿐이다. 이미 계산된 영업일(지급일·마감·알림 발송일)이 소급해서 바뀌지 않게 한다(RESEARCH Pitfall 4 · UI-SPEC U-3 · Codex #2)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/holidays/admin.ts"
      provides: "수동 추가 · 수동 미래 행 삭제 — 권한 재검사, 한 트랜잭션(행 변경 + 미래 대체일 재계산 + `holiday_change` 로그)"
      exports: ["addHoliday", "deleteHoliday", "PastHolidayDateError", "DuplicateHolidayError", "HolidayNotDeletableError"]
    - path: "app/(app)/admin/holidays/actions.ts"
      provides: "추가 · 삭제 Server Action(확정은 04.2-11)"
      exports: ["addHolidayAction", "deleteHolidayAction"]
    - path: "app/(app)/admin/holidays/holiday-form.tsx"
      provides: "`?new=1` 추가 폼(열리면 날짜 칸 포커스 · 날짜 입력 `min` 내일 · `max` 음력 표 마지막 해 12-31 · 종류 기본 `임시공휴일` · 막힘 이유 + 다음 한 수 · 칸 오류 · 제출 중)"
    - path: "app/(app)/admin/holidays/delete-holiday.tsx"
      provides: "확인 없는 행 `삭제`(대기 `삭제…` · 실패 문구는 그 행에만)"
    - path: "app/(app)/admin/holidays/delete-undo.tsx"
      provides: "표 위 결과 줄 `{날짜} {이름} 삭제됨` + 3차 `되돌리기`(같은 추가 경로) · 되돌리기 실패 문구 순수 함수"
      exports: ["DeleteUndoSection", "useDeleteUndo", "undoFailure"]
    - path: "app/(app)/admin/holidays/added-toast.tsx"
      provides: "추가 성공 토스트 한 개"
  key_links:
    - from: "domain/holidays/admin.ts"
      to: "domain/holidays/candidates.ts"
      via: "추가·삭제가 `withHolidayCalendarLock(tx => …)` 안에서만 돈다(Codex 2차 #4)"
      pattern: "withHolidayCalendarLock"
    - from: "domain/holidays/admin.ts"
      to: "domain/holidays/candidates.ts"
      via: "잠금 트랜잭션 안에서 `recomputeFutureSubstitutes(Y - 1, { today }, tx)` 한 번 — 원래 해 Y-1부터 표 끝까지(Codex 2차 #5 · 3차 추가 #2)"
      pattern: "recomputeFutureSubstitutes"
    - from: "domain/holidays/admin.ts"
      to: "domain/action-log/record.ts"
      via: "`recordAction(viewer, { actionType: \"holiday_change\", entity: \"holiday\", detail: { op, … } }, { tx })`"
      pattern: "holiday_change"
    - from: "app/(app)/admin/holidays/page.tsx"
      to: "app/(app)/admin/holidays/holiday-form.tsx"
      via: "`showForm = canWrite && newParam === \"1\"`일 때만 `<HolidayForm`(목록 우선 규칙)"
      pattern: "HolidayForm"
    - from: "test/unit/ui/admin-master-list-first.test.ts"
      to: "app/(app)/admin/holidays/page.tsx"
      via: "holidays 케이스 한 항목(행동 링크 `공휴일 추가`가 `new=1`)"
      pattern: "공휴일 추가"
    - from: "app/(app)/admin/holidays/delete-undo.tsx"
      to: "app/(app)/admin/holidays/actions.ts"
      via: "`되돌리기`가 삭제 액션이 돌려준 `{ date, name, kind }`로 `addHolidayAction`을 부른다 — 되돌리기 전용 경로를 만들지 않는다(#1)"
      pattern: "addHolidayAction"
---

<objective>
**디자인 게이트 입력(처리됨 — 2026-09-25):** 본문 「UI-SPEC 변경 요청」 표(R1~R14)는 페이즈 전체의 요청 목록이다 — 줄마다 소유 플랜·Task가 적혀 있다(04.2-07·09·11·12·13·15). /plan-design-review(결정 원장 `docs/designs/plant8-erp-phase04.2-design-review-260925.md`)가 이 표를 받아 `04.2-UI-SPEC.md` 개정본(커밋 62d1392)에 반영했고, 그 결정은 이 플랜과 소유 플랜의 truth·behavior·acceptance에 이미 옮겨졌다(표의 「상태」 칸). 실행자는 UI-SPEC을 고치지 않는다 — 소유 플랜의 precondition이 표의 「적용 확인」 명령으로 반영 여부를 다시 본다.

**As a** 경영관리 관리자, **I want to** 정부가 발표한 임시공휴일·선거일을 미래 날짜로만 더하고 잘못 넣은 수동 행을 빼기를, **so that** 지급일·마감·알림이 같은 달력으로 계산되고 대체공휴일은 법 규칙대로 따라 움직이며, 과거 계산은 소급해서 흔들리지 않는다.

(스토리는 ROADMAP 04.2 Goal 줄 「관리자가 법정 규칙으로 생성된 공휴일 후보를 검토·확정하고」와 기준 1에서 끌어왔다. ROADMAP Goal 줄은 사용자 스토리 형식이 아니다 — 형식을 맞추려면 `/gsd mvp-phase 04.2`.)

Purpose: ADMN-11 화면 몫의 뒤 절반 — 수동 추가·수동 미래 행 삭제·폰 칸 접기. UI-SPEC S2-c·S2-d·S2-f·S2-g와 S2-holiday-table·S2-confirm의 overflow 줄. Codex #2(도메인 연결 — 추가·삭제가 미래 대체일을 다시 계산)·#12(추가·삭제 로그의 원자성·끌 수 없음·동시 중복 삭제)·#16(대화형 편집은 미래만 — 초기 달력은 04.2-06). Codex 2차 #4(달력 잠금 안의 추가·삭제 — 장벽 동시 삭제 통합)·#5(원래 해 Y-1·Y 재계산 — 해 넘김 대체일)·#12(삭제 결과는 다시 그린 표가 보인다). Codex 3차 추가 #2(재계산은 Y-1부터 표 끝까지 한 호출). /plan-design-review 2026-09-25 #1(확인 대신 되돌리기 — 사용자 결정)·#7·#8·#9·#10·#11·#12·#14.

Output: 추가·삭제 도메인(한 트랜잭션) · 두 액션(누수 스캔 등록) · `?new=1` 폼 · 확인 없는 삭제 + 결과 줄 `되돌리기`(같은 추가 경로) · 추가 토스트 · 폰 375 칸 접기와 44×44 누르는 영역 · 단위·통합·E2E(PC·폰) · Codex 디자인 검토 기록.

**실행 규율:** 구현 전 superpowers `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 「완료」 전 `verification-before-completion`을 Skill 도구로 호출한다. 화면은 `docs/design/SYSTEM.md`와 `docs/design/tokens.css` 토큰만 쓴다(새 색·서체·radius 금지). 새 템플릿을 만들지 않고 §6-1 목록 + `?new=1` 토글(D-39) + §7-15 폼 + §7-3 칸 접기로 조립한다(본보기 `admin/corp-cards/page.tsx`). 삭제는 `admin/archive/delete-to-archive.tsx`의 두 단계 확인을 본뜨지 않는다(UI-SPEC S2-f). 화면 검증 순서는 「싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`) + Codex 적대적 디자인 검토 → 수정 → 전체 게이트 한 번」. UI 완료 판정은 페이즈 끝 `/design-review` → `/qa`. 새 의존성 0개. `any` 금지.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-06-SUMMARY.md
@.planning/phases/04.2-notify-holidays-foundation/04.2-11-SUMMARY.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md — Spacing 「터치 목표」(93행) · S2(226~344행, 특히 S2-f 312~334행) · Copywriting S2 행(403~426행) · 가정 U-2·U-3·U-10
# docs/design/SYSTEM.md — §6-1 · §7-1(위계 「1차 1개」) · §7-3 칸 접기 · §7-6 토스트 규칙 · §7-15 폼 PARTIAL
</context>

<probe_fallback>
이 플랜은 엣지 행을 새로 떠맡지 않는다(ADMN-11 #4·#7은 04.2-11). UI-SPEC UI Considerations의 S2-add-form 여섯 줄 · S2-delete 여덟 줄(backstop 하나) · S2-holiday-table/overflow · S2-confirm/overflow를 떠맡는다(폰 칸 접기와 함께). 금지 회상 하나(소급 변경 금지 — Codex #2에 맞춰 문장을 다듬었다)를 도메인 거부 + 통합 테스트로 묶는다.

**계획 단계 결정(표면화):**

- **D-4209 삭제는 보관함이 아니라 바로 지운다(UI-SPEC U-10):** 수동 공휴일은 다시 넣기 쉽고, 보관함 경로는 보관 대상 레지스트리 등록과 복원 규칙(복원이 과거 날짜를 되살리면 소급 변경)이 더 필요하다. 원래 값은 `holiday_change` 로그(op `delete`)에 남는다.
  - **2026-09-25 개정 — /plan-design-review #1 사용자 결정 「되돌리기」 · #8:** 확인 단계를 두지 않는다(CLAUDE.md §7 「확인 대신 되돌리기, 확인은 되돌릴 수 없는 일에만」 — 같은 값으로 다시 넣으면 원래대로 돌아오는 일이다. 두 단계 인라인 확인은 화면에 1차 버튼을 하나 더 만들어 SYSTEM.md §7-1 「1차 1개」와도 어긋났다). 3차 `삭제` → 바로 삭제 → `router.refresh()`로 서버가 표와 상태 줄을 다시 그린다(옮겨진 대체공휴일 포함, 토스트 없음 — §7-6) → 상태 줄과 표 사이 한 줄 `{YYYY-MM-DD} {이름} 삭제됨` + 3차 `되돌리기`(`--fs-sm --muted`, 간격 `--s-2`, `role="status"`), 포커스는 `되돌리기`. `되돌리기`는 삭제 액션이 돌려준 원래 `{ date, name, kind }`로 기존 `addHolidayAction`을 부른다 — 행 + 대체일 재계산 + `holiday_change` op `add`가 한 트랜잭션이고, 되돌리기 전용 액션·로그 종류를 만들지 않는다. 대기·실패·거절·줄 수명은 UI-SPEC S2-f 그대로(must_haves 「결과 줄 수명」).
  - 결과 줄 상태는 URL이 아니라 클라이언트 상태(`delete-undo.tsx`의 `DeleteUndoSection`)가 들고 있다 — 링크로 만든 가짜 결과 줄이 관리자에게 `되돌리기`(= 임의 공휴일 추가)를 누르게 할 수 없다(T-4.2-74). `page.tsx`는 이 섹션을 `key={연도 + 폼 열림 여부}`로 감싸 연도를 바꾸거나 폼을 열면 줄이 사라지게 하고, 화면을 떠나면 언마운트로 사라진다. 동시 중복 삭제에서 뒤 사람이 받는 `{ deleted: false }`는 이 사람이 지운 것이 없으므로 결과 줄 없이 다시 그리기만 한다.
- **D-4224 삭제 확인 문구 — 2026-09-25 폐기(#1로 대체):** 확인 단계가 없어져 확인 문구도 없다. 앞선 판(설명 꼬리 → 꼬리 없는 확인 문구)의 근거 가운데 남는 것은 하나다 — 재계산 결과는 성공 뒤 서버에서 다시 그린 표가 보여 준다(Codex 2차 #12). 옛 확인 문구·옛 영업일 꼬리는 화면·E2E에 쓰지 않는다.
- **확정에 확인이 없는 이유(디자인 리뷰 #11):** 04.2에서 확정은 계산된 값을 바꾸지 않고 확정 기록과 B1 배너 해제만 하며 확정 뒤에도 수동 행 추가·삭제가 되므로 확인 단계를 두지 않는다 — 확정이 지급일을 다시 파생하게 되는 Phase 7에서 확인이나 되돌리기를 다시 본다(UI-SPEC S2-e · U-2).
- **D-4220 공휴일 로그(개정 — Codex #12, 04.2-11과 같은 규칙):** 추가·삭제는 끌 수 없는 `holiday_change`(04.2-03)로, `entity: "holiday"`, `entityId`는 행 id, `detail: { op: "add" | "delete", date, name, kind }`. 호출은 `recordAction(viewer, entry, { tx })`(04-32). 행 변경·재계산·로그가 한 트랜잭션이다.
- **D-4210 도메인 연결(Codex #2 · 개정 — Codex 2차 #4·#5):** 04.2-06이 저장소 수준에서 「수동 날짜는 대체일을 막는다」·두 삽입 순서·달력 잠금·해 넘김 재계산을 증명했다. 여기서는 사람이 쓰는 경로가 그 규칙을 부른다. 두 함수 모두 표를 04.2-06의 `withHolidayCalendarLock(tx => …)` 안에서만 바꾼다 — 첫 계획의 맨 `withTransaction`은 두 재계산 사이의 순서를 주지 않아 동시 삭제가 대체일을 둘 남길 수 있었다(Codex 2차 #4). 아래 Y는 추가·삭제한 날짜의 해다.
  - 추가: write 검사 → 구분·날짜 검사 → `withHolidayCalendarLock`: `ensureHolidayCandidatesLocked(Y, tx)`(멱등 — 후보가 없는 해에 대체일만 생기지 않게. 음력 표 밖 해는 `LunarTableRangeError`가 그대로 나가고 액션이 날짜 칸 오류 `{Y}년은 음력 표에 없습니다 · {LUNAR_TABLE_LAST_YEAR}년 이전 날짜를 적어 주세요`로 바꾼다 — 디자인 리뷰 #9) → `findHolidayByDate(viewer, date, tx)`: 그 날짜 행이 **미래 대체공휴일**이면(원래 해가 Y든 Y-1이든) 그 행을 지운다(규칙이 옮길 자리) · 법정 공휴일이나 수동 행이면 `DuplicateHolidayError(기존 이름)` → `insertManualHoliday(…, tx)`가 거짓이면 다시 찾아 같은 중복 오류(잠금이 동시 삽입을 직렬화하므로 안전망) → `recomputeFutureSubstitutes(Y - 1, { today }, tx)` 한 번 → 로그.
  - 삭제: write 검사 → `withHolidayCalendarLock`: `deleteHolidayById(viewer, id, tx)`(RETURNING) → null이면 이미 지워진 것이라 로그 없이 `{ deleted: false }`(동시 중복 삭제 — 뒤 사람은 잠금을 기다린 뒤 null을 받아 성공 모양) → 지운 행이 수동 구분이 아니거나 날짜 ≤ KST 오늘이면 `HolidayNotDeletableError`를 던져 트랜잭션째 되돌린다 → 지운 날짜의 해 Y에 대해 `recomputeFutureSubstitutes(Y - 1, …)` 한 번 → 로그 → `{ deleted: true, date, name, kind }`(지운 행의 원래 값 — 결과 줄과 `되돌리기`가 쓴다).
  - 원래 해 Y-1부터 다시 정한다(Codex 2차 #5 — 첫 계획의 「12월 기독탄신일 대체일은 12월 안」 가정은 버린다): 12-27~31이 수동으로 막히면 2027 기독탄신일 대체일은 2028-01-03이다. 대체 행의 `origin_year`(04.2-01·04.2-06)로 고르므로 2028년 날짜를 바꿔도 2027의 의무를 다시 계산할 수 있다. 2차의 Y-1·Y 두 번은 Y+1 이후를 놓쳤다 — 옮겨진 대체일이 다른 원래 해의 대체일 자리를 밀어내면 그 뒤 해도 다시 정해야 한다(Codex 3차 추가 #2). 그래서 한 호출 `recomputeFutureSubstitutes(Y - 1, …)`가 04.2-06 「대체일 배정」대로 원래 해 Y-1부터 `LUNAR_TABLE_LAST_YEAR`까지 생성된 해 전부를 점유 집합 하나로 다시 정하고, 충돌이 생기면 던져 추가·삭제 전체가 되돌려진다. 표시 행이 없는 해는 건너뛴다(04.2-06).
  - 재계산 함수는 `deps.recompute`(기본 `recomputeFutureSubstitutes`)로 주입할 수 있다 — Task 2의 장벽 동시 삭제 통합이 첫 트랜잭션을 잠금 안에서 멈추게 하는 데만 쓴다(프로덕션 경로는 기본값).
  - U-3과의 관계: 관리자는 규칙 행을 직접 지우거나 고치지 못한다. 미래 대체일이 옮겨지는 것은 법 규칙(공휴일법 제3조 — 대체일은 다음 비공휴일)이 입력 변화에 따라 다시 적용되는 것이고, 지난 대체일은 건드리지 않는다.
- **추가 폼의 결정 줄이기(사용성 원칙 — 신규 · 2026-09-25 보완 — 디자인 리뷰 #9·#14):** 날짜 칸은 `TextField type="date"`(선례 `app/(app)/admin/people/person-form.tsx`)이고 `page.tsx`가 `min`(KST 오늘(`toKstDate`)의 다음 날)과 `max`(`{LUNAR_TABLE_LAST_YEAR}-12-31` — `domain/holidays/lunar-table.ts`)를 계산해 넘긴다 — 달력 선택이 그 범위로 좁혀진다. `noValidate`라 `min`·`max`는 제출을 막지 않고 키보드로는 범위 밖 값을 적을 수 있으므로, 서버의 네 칸 오류(형식 · 오늘이나 과거 · 음력 표 밖 해 · 중복)가 마지막 관문이다. `?new=1`로 열리면 날짜 칸에 포커스를 둔다(키보드로 바로 입력 — 막힘 줄은 §7-1 규칙이라 그대로 보인다). 자리표시자는 날짜 입력에 보이지 않아 두지 않는다. 종류 `Select`는 `임시공휴일`을 미리 고르고 빈 옵션 `—`를 두지 않는다 — 두 종류는 대체일 계산에서 똑같이 쓰이고 구분 글자만 다르다. 그래서 막힘 이유는 날짜·이름 두 칸만 센다(R5·R6).
- **누르는 영역(디자인 리뷰 #7):** 보이는 높이는 §7-1대로(폰 확정 버튼 40) 두고, 확정·필터 줄 `공휴일 추가`·폼 1차 `공휴일 추가`·`취소`·`삭제`·`되돌리기`·연도 링크의 누르는 영역을 폰에서 44×44(`--touch-min`) 이상으로 넓힌다(투명 확장 영역, 새 토큰 없음). E2E는 보이는 상자가 아니라 누르는 영역을 잰다 — 요소 중심에서 가로·세로 ±21px 지점을 `document.elementFromPoint`로 찍어 모두 그 요소(또는 그 자손)가 나오는지 본다.
- **대화형 편집은 미래만(Codex #16):** 2026-06-03 지방선거 같은 이미 지난 초기 달력은 04.2-06의 검토된 초기 목록(`INITIAL_MANUAL_HOLIDAYS`)이 넣는다. 화면·액션의 미래 전용 규칙은 그대로다.
- **폼 성공 토스트:** 폼은 `?new=1`일 때만 렌더되고(목록 우선 단위 테스트 규칙) 성공하면 `?year={Y}&added={날짜}`로 이동한다. 화면은 `added`가 ISO 날짜이고 그 해 행에 있을 때만 작은 클라이언트 토스트(`공휴일 추가 · {날짜} 추가됨`)를 띄우고, 토스트가 닫히면 `router.replace`로 `added`를 뗀다.
- **실패 흉내:** 화면의 「추가하지 못했습니다」·「삭제하지 못했습니다」·「되돌리지 못했습니다 · 다시 시도」 상태는 E2E가 Playwright `page.route`로 그 액션 요청을 끊어(`route.abort()`) 만든다 — 프로덕션 코드에 실패 주입 경로를 만들지 않는다. 되돌리기 거절은 E2E가 삭제 뒤 테스트 DB에 같은 날짜의 다른 행을 직접 넣어 만든다.
</probe_fallback>

## Review Dispositions

| Codex # / 검사 | 처리 | 한 줄 |
|---|---|---|
| 2 (high) — 도메인 쪽 | 반영 | `addHoliday`·`deleteHoliday`가 같은 트랜잭션에서 `recomputeFutureSubstitutes`를 부르고, 미래 대체일 자리에 넣은 임시공휴일이 대체일을 옮기며 지우면 되돌리는지 두 방향 통합(Task 1·2). 저장소·생성 쪽은 04.2-06 |
| 12 (medium) — 추가·삭제 쪽 | 반영 | 행 변경 + 재계산 + `holiday_change` 로그 한 트랜잭션 · 끌 수 없는 종류 · 로그 쓰기 실패 롤백 · 동시 중복 삭제 로그 1줄 통합(Task 1·2). 확정 쪽은 04.2-11 |
| 16 (medium) — 대화형 편집 쪽 | 반영 | 대화형 추가·삭제의 미래 전용 규칙 유지(금지 회상 통합). 지난 초기 달력은 04.2-06 `INITIAL_MANUAL_HOLIDAYS` |
| checker claude_md_compliance(plan 10 → 화면 플랜 전부) | 반영 | 독립 DOM 감사 옆 Codex 적대적 디자인 검토 한 번 · 출력 원문 SUMMARY · 미실행이면 보고만(Task 3) |
| Codex 2차 4 (high) — 추가·삭제 쪽 | 반영 | 추가·삭제가 `withHolidayCalendarLock` 안에서만 표를 바꾼다(D-4210 개정) · 장벽(`pg_stat_activity` 잠금 대기 확인)으로 겹침을 증명한 동시 삭제 통합이 정확한 최종 대체일 `2027-10-04` 하나를 단언(Task 2). 잠금·생성 쪽은 04.2-06 |
| Codex 2차 5 (medium) — 추가·삭제 쪽 | 반영(3차에 개정 — `Y - 1` 한 호출로 표 끝까지) | 바뀐 날짜의 해 Y에 대해 원래 해 Y-1·Y를 재계산 · 「성탄절 대체일은 늘 12월 안」 문장 삭제 · 12월 막는 날 → 2028-01-03 대체일 → 1월 추가·삭제 두 방향 통합(Task 1·2) |
| Codex 2차 7 (medium) — 이 플랜의 `tx.delete` | 반영(04.2-06 소유) | 공용 `DbOrTx`를 `"delete" \| "execute"`까지 넓히는 일은 04.2-06이 `db/client.ts`를 소유해 한다 — 이 플랜은 그 타입과 `withHolidayCalendarLock`만 쓰고 공용 타입 파일을 고치지 않는다(depends_on 04.2-11 → 04.2-06) |
| Codex 2차 12 (medium) | 반영 · 2026-09-25 #1로 대체(확인 단계 없음 — 남는 것은 「다시 그린 표가 재계산 결과를 보인다」) | 삭제 확인 문구를 모든 경우에 참인 중립 문구로(D-4224, /plan-design-review에서 확정) · S2-delete populated·partial truth는 원문을 인용하고 대체 동작을 적었다 · 화면 코드·E2E가 새 문구를 쓰고 옛 문구가 화면 코드에 없음을 Grep · UI-SPEC 파일 변경은 아래 「UI-SPEC 변경 요청」 · 「알려진 문구 한계」·「열린 문구 질문」 삭제 |
| 탐침 커버리지 S2-delete zero-one-many | 반영(인용) | `ui-probe-coverage.json`의 초안 문장(「한 번에 한 행만」)은 뒤에 나온 `ui-probe-resolutions.json`과 UI-SPEC 490행이 「행마다 따로」로 바꿨다 — truth가 UI-SPEC 문장을 따르고 초안 문장은 대체된 것으로 인용만 한다 |

### Review Dispositions (Codex 3차 · 코디네이터)

| 항목 | 처리 | 한 줄 |
|---|---|---|
| Codex 3차 추가 #2 · NEW #5 잔여 — 추가·삭제 쪽 | 반영 | 추가·삭제가 `recomputeFutureSubstitutes(Y - 1, …)`를 **한 번** 부른다 — 04.2-06이 원래 해 Y-1부터 표 끝까지 점유 집합 하나로 다시 정하고 충돌은 던져 전체 롤백(D-4210 · truth · Task 1·2 · T-4.2-79). 사슬·두 생성 순서·Y+2 통합은 04.2-06 |
| 코디네이터 — UI-SPEC 넘겨주기 | 반영 | `<objective>` 첫 문단에 「이 표는 /plan-design-review가 실행 전에 UI-SPEC에 적용하는 입력」을 적고, 표를 페이즈 전체 목록(R1~R10 · 소유 플랜·Task · 적용 확인 명령)으로 바꿨다. 실행자는 UI-SPEC을 고치지 않는다 |
| 코디네이터 — 사용성 원칙 | 반영(이 플랜 몫) · 2026-09-25 일부 대체 | 삭제 확인에서 설명 꼬리를 뺀다(D-4224 개정 · R1~R4 — 뒤에 #1이 확인 단계 자체를 없앴다) · 날짜 칸을 `type="date"` + `min` 내일로(달력 선택을 좁힘 · R5) · 종류 기본 `임시공휴일`(결정 하나 줄임 · 막힘 이유 2칸 · R6). 막힘 이유 + 다음 한 수는 SYSTEM.md 규칙(§7-15)이라 남긴다 |

### Review Dispositions (/plan-design-review · 2026-09-25 — 결정 원장 #1~#19 중 이 플랜 몫)

| # | 처리 | 한 줄 |
|---|---|---|
| 1 (high, 사용자 결정 「되돌리기」) | 반영 | 두 단계 인라인 확인·`DeleteToArchive` 본뜨기를 없앤다 · 바로 삭제 → 다시 그린 표 → 결과 줄 `{날짜} {이름} 삭제됨` + 3차 `되돌리기`(같은 추가 경로, `holiday_change` op `add`) · 대기·실패·거절·줄 수명(D-4209 개정 · must_haves · Task 2) · 1차 버튼 늘 하나 |
| 7 (medium) | 반영 | 보이는 높이 40은 그대로, 누르는 영역 44×44를 확정·추가·취소·삭제·되돌리기·연도 링크 모두에서 `elementFromPoint`로 잰다(Task 3 — 40px 합격선 삭제) |
| 8 (medium) | 반영 | 삭제 성공 = 서버에서 다시 그린 표(대체공휴일 이동 포함), 토스트 없음 · U-10은 `D-4209 바로 삭제, 원래 값은 holiday_change 로그` + 되돌리기 |
| 9 (medium) | 반영 | 날짜 칸 `max` = `{LUNAR_TABLE_LAST_YEAR}-12-31` · 음력 표 밖 칸 오류(액션이 `LunarTableRangeError`를 바꾼다 — 키보드 입력이 `max`를 넘으므로 서버 관문) · 「고를 수 없다」 문장 삭제(Task 1·3) |
| 10 (medium) | 반영(소유 04.2-11) | 연도 상태 줄 `후보 · 공휴일 {N}일` · 확정 줄 `… · 공휴일 {N}일` — 이 플랜의 삭제·되돌리기 E2E도 같은 모양으로 N을 본다 |
| 11 (medium) | 반영 | 확정에 확인이 없는 이유 한 문장(`<probe_fallback>`) |
| 12 (low) | 반영 | R4 적용 확인을 새 성공 문구가 **있는지** 보는 grep으로 바꿨다(R 표) |
| 14 (low) | 반영 | `?new=1`이면 날짜 칸 포커스(Task 3) |

## UI-SPEC 변경 요청 (페이즈 전체 — 2026-09-25 /plan-design-review가 처리, 반영 커밋 62d1392 · 이력으로 남긴다 · 이 플랜은 UI-SPEC을 고치지 않는다)

이 표는 디자인 게이트의 입력이었다(`<objective>` 첫 문단). 게이트가 줄마다 판정해 `04.2-UI-SPEC.md`를 고쳤고(62d1392 「개정 이력」), 판정은 「상태」 칸에 있다. 줄은 지우지 않는다. 소유 플랜의 precondition이 「적용 확인」을 돌린다(`$U` = `.planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`). 04.2-09의 SYSTEM.md §7-11 B1·B2 예시는 게이트가 고친 UI-SPEC 문구를 옮겨 적는다(별도 줄 없음).

| # | 소유 플랜 · Task | UI-SPEC 위치 | 지금 | 바꿀 내용 | 이유 | 적용 확인 | 상태(2026-09-25 · 62d1392) |
|---|---|---|---|---|---|---|---|
| R1 | 04.2-12 T2 | S2-f 첫 줄(305행) | 예시 확인 문구 「… 삭제 · (이 날이 영업일로 돌아간다는 옛 문구)」 | `2027-06-03 제9회 전국동시지방선거 삭제` + 1차 `공휴일 삭제` + 2차 `취소` | D-4224 — 옛 문구는 대체일이 돌아오는 삭제에서 거짓 · 설명 꼬리는 사용성 원칙으로 뺀다 | R1~R3 공통: `grep -c "다시 영업일이 됩니다" $U`가 0 | 대체됨 — #1(확인 없이 삭제 + 결과 줄 `되돌리기`, S2-f). 적용 확인은 그대로 참 |
| R2 | 04.2-12 T2 | Copywriting 삭제 확인 행(391행) | `{YYYY-MM-DD} {이름} 삭제 · ` + 옛 문구 | `{YYYY-MM-DD} {이름} 삭제` (1차 `공휴일 삭제`·대기 `공휴일 삭제…`·2차 `취소`는 그대로) | 같음 | 위와 같음 | 대체됨 — #1. Copywriting은 `삭제 결과 줄` · `되돌리기 실패` 행으로 바뀜 |
| R3 | 04.2-12 T2 | UI Considerations populated · S2-delete(487행) | 옛 확인 문구 | 같은 행의 확인 문구만 `{날짜} {이름} 삭제` | 같음 | 위와 같음 | 대체됨 — #1(populated는 `확인 없이 지워지고 … 삭제됨 + 되돌리기`) |
| R4 | 04.2-12 T2 | UI Considerations partial · S2-delete(488행) | 「그 행만 사라지고 다른 행과 연도 상태 줄의 N이 하나 줄어든다」 | 「성공하면 화면을 서버에서 다시 그려 지운 행이 사라지고 재계산으로 바뀐 대체공휴일 행이 함께 보이며, 상태 줄 N은 다시 그린 그 해 행 수다. 토스트는 없다」 | 재계산이 다른 행을 옮긴다(Codex 2차 #12) | `grep -c "재계산으로 바뀐 대체공휴일 행이 함께 보" $U`가 2 이상(S2-f 성공 줄 + partial 행 — 새 문구가 **있는지** 본다, 디자인 리뷰 #12) | 적용 · 확장(#8 — S2-f 성공 줄 · S2-g SUCCESS · U-10도 함께) |
| R5 | 04.2-12 T1·T3 | S2-d 표 날짜 행(284행) · S2-b 도식 날짜 칸(235행) | `TextField`, 자리표시자 `2027-06-03` | `TextField type="date"`, 자리표시자 없음, `min` = KST 내일. 칸 오류 문구 세 줄은 서버 관문으로 그대로 | 사용성 원칙 — 입력 형식으로 이끈다 | `grep -c 'type="date"' $U`가 1 이상 · 보완분: `grep -c "음력 표에 없습니다 · " $U`가 1 이상 | 적용 · 보완(#9 — `max` = 음력 표 마지막 해 12-31, 음력 표 밖 칸 오류, 「틀린 선택 불가능」 문장 삭제 · #14 날짜 칸 포커스) |
| R6 | 04.2-12 T1·T3 | S2-d 표 종류 행(285행) · S2-d 막힘 예(289행) · Copywriting 막힘 행(384행) | 첫 옵션 `—` · 예 `… 날짜 · 종류 · 이름 3칸 · 날짜 적기` · 「종류가 첫 빈 칸이면 …」 문장 | `임시공휴일`(기본 선택) · `선거일`, 빈 옵션 없음 · 예 `추가할 수 없음 — 날짜 · 이름 2칸 · 날짜 적기` · 종류 문장 삭제 | 사용성 원칙 — 기본값으로 결정 하나를 줄인다(두 종류는 계산에서 같다) | `grep -c "종류 고르기" $U`가 0 | 적용 |
| R7 | 04.2-11 T1 | S2-b 도식(241행) · S2-b 연도 상태 줄(257행) · S2-g PARTIAL(317행) · Copywriting 연도 상태 줄(381행) · UI Considerations partial · S2-holiday-table(468행) | `후보 · 규칙으로 만든 {N}일 · 확정 전` | `후보 · {N}일`(확정 연도 줄은 그대로) | 사용성 원칙 — 「확정 전」은 `후보`와 1차 확정 버튼이 이미 말하고, 「규칙으로 만든」은 설명이다. 남는 정보는 상태와 건수 | `grep -c "규칙으로 만든 [N0-9{]" $U`가 0 · 수정분: `grep -c "후보 · 공휴일 {N}일" $U`가 1 이상 | 적용 · 문구 수정(#10 — `후보 · 공휴일 {N}일`, 확정 줄도 `… · 공휴일 {N}일`) |
| R8 | 04.2-13 T3 | Copywriting 배너 B1(404행) | `{연도}년 공휴일 확정 전 · 규칙으로 만든 후보로 계산 중` + 3차 링크 | `{연도}년 공휴일 확정 전` + 3차 링크 `{연도}년 공휴일 검토` | 사용성 원칙 — 설명 꼬리 없이 상태와 다음 한 수(링크)만 | `grep -c "후보로 계산 중" $U`가 0 | 적용 |
| R9 | 04.2-13 T1·T3 | Copywriting 배너 B2(405행) | `이메일 발송 실패 {N}건 · {시각} · 알림함으로만 전달됨` + 3차 링크 | `이메일 발송 실패 {N}건 · {YYYY-MM-DD HH:MM}` + 3차 링크 `시스템 상태 보기` | 사용성 원칙 — 건수·시각만. 「알림함으로만 전달됨」은 결과 불명이 있을 때 사실과 다를 수도 있다(D-4217) | `grep -c "알림함으로만 전달됨" $U`가 0 | 대체됨 — R11(결과 불명 변형 · 시각은 괄호). 적용 확인은 그대로 참 |
| R10 | 04.2-15 T2(04.2-10 T2가 통합) | Copywriting 묶음 이메일 본문(411행) | 알림마다 `- {알림 내용}` 한 줄(상한 없음) | 20건을 넘으면 생성 순서 앞 20건만 `- ` 줄로 쓰고 바로 다음 줄 `외 {M}건은 알림함에서 확인` — 제목·첫 줄의 N은 전체 수 | Codex 1차 #9 잔여 · 3차 — 여러 날 쌓인 `pending`을 한 통에 모아도 메일 크기가 묶인다(D-706 유지) | `grep -c "알림함에서 확인" $U`가 1 이상 · 보완분: `grep -c "최신 먼저" $U`가 1 이상 | 적용 · 보완(#15 — 알림함처럼 **최신 먼저** 20건, 첫 줄 날짜는 발송일. 처음 요청의 「생성 순서 앞 20건」을 뒤집었다) |
| R11 | 04.2-13 T1·T2·T3 | S3 B2 조건·사라지는 조건 · Copywriting B2 · S4 `이메일` 값 · U-6 | 결과 불명 없음 · B2 시각이 `· {시각}` | B2 조건에 결과 불명(선점 뒤 7일 창, D-4217) · 문구 `이메일 발송 실패 {F}건 ({실패 시각}) · 결과 불명 {U}건 ({가장 이른 시각})`, 0건 조각 생략, 시각은 모든 변형에서 괄호 · S4 꼬리 ` · 결과 불명 {U}건 ({시각})`(본문 색) | 디자인 리뷰 #2 — D-4217이 UI-SPEC에 없었다 | `grep -c "결과 불명" $U`가 3 이상 | 신규 · 적용 |
| R12 | 04.2-13 T1·T3 | S4 `이메일` 값 · S3 「어느 화면에」 표 | 미설정이면 꼬리 없음(04.2-13 옛 「설정이 있을 때만」) | 설정 부분(`미설정` / `사용 중 · {주소}`)과 결과 꼬리를 따로 — 미설정이어도 `미설정 · 실패 3건 (…)`. 결과 조회 실패면 설정 부분은 두고 꼬리만 ` · 확인 불가` | 디자인 리뷰 #3 — B2 → 시스템 상태 경로에서 실패가 사라졌다 | `grep -c "미설정 · 실패" $U`가 1 이상 | 신규 · 적용 |
| R13 | 04.2-07 T1·T3 | S1-a 갱신 문장 · UI Considerations S1-badge loading·error | 「레이아웃(서버)이 이동마다 센다」 | 첫 값은 레이아웃이 서버에서 세고, 경로가 바뀔 때마다 `UnreadCountProvider`가 다시 받는다 · 받는 동안·실패 때 이전 값 유지(첫 서버 조회 실패는 배지 없음) | 디자인 리뷰 #4 — D-4219와 달랐다 | `grep -c "이전 값을 유지" $U`가 1 이상 | 신규 · 적용(D-4219 개정 — 04.2-07) |
| R14 | 04.2-09 T3 | S1-c `더 보기 50건` 상태 · UI Considerations S1-inbox-list | 성공 경로만 | 요청 중 기존 행 유지·버튼 비활성 · 실패하면 버튼 자리에 `불러오지 못했습니다` + 3차 `다시 시도`(같은 커서) · 마지막 쪽이면 버튼이 사라지고 포커스는 새로 붙은 첫 행 | 디자인 리뷰 #5 | `grep -c "새로 붙은 첫 행" $U`가 1 이상 | 신규 · 적용 |

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — `공휴일 추가` → `?new=1` 폼 → 미래 선거일 한 건 → 한 트랜잭션(행 + 미래 대체일 재계산 + `holiday_change` op add) → 표에 새 행 + 토스트 (E2E로 끝까지)</name>
  <files>domain/holidays/admin.ts, app/(app)/admin/holidays/page.tsx, app/(app)/admin/holidays/holidays.module.css, app/(app)/admin/holidays/actions.ts, app/(app)/admin/holidays/actions.registry.ts, app/(app)/admin/holidays/holiday-form.tsx, app/(app)/admin/holidays/added-toast.tsx, test/unit/ui/admin-master-list-first.test.ts, test/integration/holidays-admin.test.ts, test/e2e/holidays.spec.ts</files>
  <precondition>04.2-11이 끝나 `domain/holidays/admin.ts`에 `confirmHolidayYear`·`HolidayRowView.deletable`이 있고(`grep -q "confirmHolidayYear" domain/holidays/admin.ts`) 04.2-06의 달력 잠금과 재계산이 있다(`grep -q "withHolidayCalendarLock" domain/holidays/candidates.ts && grep -q "recomputeFutureSubstitutes" domain/holidays/candidates.ts`) · 디자인 게이트가 본문 「UI-SPEC 변경 요청」 R5·R6(날짜 입력 · 종류 기본값)과 #9(음력 표 밖 칸 오류)를 적용했다(`grep -c 'type="date"' .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 1 이상, `grep -c "종류 고르기" .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 0, `grep -c "음력 표에 없습니다 · " .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 1 이상 — 62d1392에서 참) — 거짓이면 멈추고 보고한다</precondition>
  <read_first>
    - `domain/holidays/admin.ts` · `app/(app)/admin/holidays/*` — 04.2-11 결과(권한 검사 모양, `HolidayForbiddenError`, 잠금 안의 확정, 표 칸)
    - `domain/holidays/candidates.ts`(`withHolidayCalendarLock` · `ensureHolidayCandidatesLocked(year, tx)` · `recomputeFutureSubstitutes(fromOriginYear, { today }, tx)` — 뒤 둘은 트랜잭션도 잠금도 스스로 열지 않는다 · 재계산은 원래 해 `fromOriginYear`부터 표 끝까지) · `repositories/holidays.ts`(`findHolidayByDate` · `insertManualHoliday` → boolean · `deleteHolidayById` → 행 또는 null — 모두 선택 `tx`, 대체 행의 `originYear`) · `domain/holidays/rules.ts`(`HOLIDAY_KIND_LABELS` · `LunarTableRangeError`) · `domain/holidays/lunar-table.ts`(`LUNAR_TABLE_LAST_YEAR`) · `domain/holidays/business-day.ts`(`toKstDate`)
    - `domain/action-log/record.ts` — `recordAction(viewer, entry, { tx })` · `test/integration/holidays.test.ts` — 04.2-06의 장벽 동시 삭제·교차 연도 케이스(같은 준비·같은 기대 날짜를 도메인 경로로 다시 쓴다)
    - `app/(app)/admin/corp-cards/card-form.tsx` — `useAction` · `result.validationErrors?.<칸>?._errors?.[0]` · `FormAlert` · 제출 중 라벨
    - `ui/form/Form.tsx`(`Form.Actions` · 칸 폭 `short`/`select`/`long`) · `ui/input/TextField.tsx` · `ui/select/Select.tsx` · `ui/button/Button.tsx` · `ui/toast/Toast.tsx`(토스트 한 개·4초)
    - `test/unit/ui/admin-master-list-first.test.ts` 20~65행 — 목록 우선 규칙 단언(`new?: string` · `newParam === "1"` · `show… &&` 뒤 폼 태그 · 행동 링크 `new=1`)
    - `lib/pg-errors.ts` `isUniqueViolation` · next-safe-action `returnValidationErrors`(설치 버전 `node_modules/next-safe-action/package.json` 확인)
    - UI-SPEC S2-c · S2-d · Copywriting S2 행
  </read_first>
  <behavior>
    - 통합: `addHoliday(관리자, { date: 오늘(KST), kind: "temporary", name: "x" })` → `PastHolidayDateError`(문구 `오늘이나 지난 날짜입니다 · 내일 이후 날짜를 적어 주세요`) · 어제 → 같은 오류 · 내일 → 행 1 + 행동 로그 `holiday_change` 1줄(`detail.op = "add"`, `date`·`name`·`kind`)
    - 통합: 법정 행이 있는 날짜(`now` 주입 2026-09-24 기준 2027-10-03)에 추가 → `DuplicateHolidayError` 문구 `이미 공휴일입니다(개천절) · 다른 날짜를 적어 주세요` · 같은 날짜 동시 추가 두 번(`Promise.all`) → 하나 성공, 하나 같은 중복 오류 · 로그 1줄
    - 통합: 구분 `statutory`·`substitute`로 추가 → 거부(도메인도 두 구분만 받는다) · 보기 전용 viewer → `HolidayForbiddenError`
    - 통합(음력 표 밖 해 — 디자인 리뷰 #9): `addHoliday(관리자, { date: "{LUNAR_TABLE_LAST_YEAR + 1}-01-05", kind: "temporary", name: "x" }, { now })` → `LunarTableRangeError` · 그 해 행 0 · 로그 0(키보드로 `max`를 넘긴 입력의 서버 관문)
    - 통합(Codex #2): `now` 2026-09-24 · 2027 후보 보장(개천절 대체일 2027-10-04) → `addHoliday(관리자, { date: "2027-10-04", kind: "temporary", name: "임시공휴일 테스트" }, { now })` → 2027-10-04 행은 `temporary` · 개천절 대체일 행이 2027-10-05 · 2027년 행 집합(날짜·구분)이 04.2-06 「순서 1」 결과와 같다
    - 통합(해 넘김 — Codex 2차 #5): `now` 2026-09-24 · `addHoliday`로 2027-12-27~12-31 임시공휴일 다섯 건 → 2027 기독탄신일 대체 행이 `2028-01-03`, `origin_year = 2027` → `addHoliday(관리자, { date: "2028-01-03", kind: "temporary", name: "임시공휴일 테스트" }, { now })` → 2028-01-03 행은 `temporary` · 기독탄신일 대체 행(`origin_year = 2027`)이 `2028-01-04` 하나
    - 통합(원자성 — Codex #12): 위와 같은 준비에서 `addHoliday(…, { now, recordAction: 던지는 가짜 })` → 오류 · 2027-10-04 행은 여전히 `substitute` · 10-05 행 없음 · 2027 행 수 그대로
    - 통합(설정 무관): `action_log.optional_types`를 빈 목록으로 바꾼 뒤 추가 → 로그 1줄
    - 단위(목록 우선): holidays 케이스 — `new?: string` · `newParam === "1"` · `show… && <HolidayForm` · 행동 링크 `공휴일 추가`가 `new=1`
    - E2E(PC): `/admin/holidays?year={Y+1}` → 3차 `공휴일 추가` → 폼(날짜 짧은 칸 `type="date"` · 그 `min`이 KST 내일 · 종류 select `임시공휴일`(미리 골라짐)/`선거일` — 빈 옵션 없음 · 이름 긴 칸) → `{Y+1}-07-07` 선거일 `제22대 국회의원 선거일 테스트` → `?year={Y+1}`로 이동 · 폼 닫힘 · 토스트 `공휴일 추가 · {Y+1}-07-07 추가됨` · 새 행 이름이 말줄임 없이 보인다 · 폼이 열린 동안 확정 버튼이 DOM에 없다
  </behavior>
  <action>
① **RED 먼저.** behavior를 `test/integration/holidays-admin.test.ts`·`test/e2e/holidays.spec.ts`(04.2-11이 만든 파일에 describe를 더한다)와 단위에 쓴다. 날짜가 걸린 통합은 모두 `now`를 주입해 실행 날짜와 무관하게 한다. E2E는 `beforeAll`에서 테스트 DB `holiday_year_confirmations`의 `{Y+1}` 행을 지우고(04.2-11과 같은 방식 — 같은 파일의 확정 E2E 뒤에도 `후보`에서 시작) `{Y+1}-07-07` 행을 지워 되풀이 실행에도 깨끗하게 시작한다. `admin-master-list-first.test.ts`의 `cases`에 `{ name: "공휴일 (holidays)", pagePath: [...admin, "holidays", "page.tsx"], actionLabel: "공휴일 추가", formComponent: "HolidayForm" }` 한 항목만 더한다.

② **도메인** `domain/holidays/admin.ts`(항목 추가 — 04.2-11 코드는 고치지 않는다): 오류 클래스 `PastHolidayDateError`·`DuplicateHolidayError`(생성자가 기존 이름을 받아 `이미 공휴일입니다({이름}) · 다른 날짜를 적어 주세요`)·`HolidayNotDeletableError`(이 Task에서는 선언만, Task 2가 쓴다) — 모두 `UserFacingError`. `addHoliday(viewer, { date, kind, name }, deps?: { now?: Date; recordAction?; recompute? })` — `<probe_fallback>` D-4210 「추가」 순서 그대로(per Codex 2차 #4 — `withHolidayCalendarLock` 안에서 `ensureHolidayCandidatesLocked(Y, tx)` · 행 확인·삽입 · per Codex 2차 #5 · 3차 추가 #2 — `recomputeFutureSubstitutes(Y - 1, …)` 한 번), 로그는 D-4220 모양(`op: "add"`). `DbOrTx`는 04.2-06이 넓힌 공용 타입(`repositories/document-counters.ts` 재수출)을 쓰고 공용 타입 파일은 고치지 않는다(Codex 2차 #7). 오늘은 `toKstDate(deps.now ?? new Date())`. 반환 `{ id, date, year }`. 소급 금지는 화면이 아니라 이 함수가 지킨다(액션을 직접 불러도 막힌다).

③ **액션**(항목 추가): `addHolidayAction` — zod `{ date: ISO 날짜 정규식 + 실재 날짜(메시지 `날짜 형식이 아닙니다 · 2027-06-03처럼 적어 주세요`), kind: z.enum(["temporary", "election"]), name: z.string().trim().min(1) }` → `addHoliday(ctx.viewer, …)` · `PastHolidayDateError`·`DuplicateHolidayError`는 `returnValidationErrors(schema, { date: { _errors: [error.message] } })`로 날짜 칸 아래로 보내고, `LunarTableRangeError`는 같은 자리에 폼 전용 문구 `{연도}년은 음력 표에 없습니다 · {LUNAR_TABLE_LAST_YEAR}년 이전 날짜를 적어 주세요`(UI-SPEC 카피 계약 — 오류 객체의 S2-g 문구가 아니다, 디자인 리뷰 #9)로 보낸다 · `revalidatePath("/admin/holidays")` · 반환 `{ date, year }`. `actions.registry.ts`에 `registerAction({ name: "addHolidayAction", menu: "admin.holidays", action: "write", dtoName: null })` 한 줄.

④ **화면:** `page.tsx` — `searchParams`에 `new?: string`·`added?: string`, `const showForm = canWrite && newParam === "1"`, 필터 줄 오른쪽 3차 `공휴일 추가`(href `?year={연도}&new=1`, 폼이 열려 있으면 숨김), 폼은 PageHeader 바로 아래 `{showForm ? <HolidayForm year={연도} cancelHref=… /> : null}`, 폼이 열려 있으면 확정 버튼을 렌더하지 않는다(§7-1 1차 하나). `added`가 ISO 날짜이고 그 해 행에 있으면 `<AddedToast date=… year=… />`. `holiday-form.tsx`(신규, 클라이언트): `Form`(single-column, `noValidate`) — 날짜 `TextField type="date"` 짧은 칸 · `min`(KST 오늘의 다음 날)과 `max`(`{LUNAR_TABLE_LAST_YEAR}-12-31`)는 `page.tsx`가 계산해 prop으로 넘긴다(자리표시자 없음 — `<probe_fallback>` 「추가 폼의 결정 줄이기」, 사용성 원칙 R5 · 디자인 리뷰 #9) · 종류 `Select` select 폭 옵션 `임시공휴일`(기본 선택)·`선거일` — 빈 옵션을 두지 않는다(R6) · 이름 `TextField` 긴 칸(자리표시자 없음) · `Form.Actions`: 1차 `공휴일 추가` · 2차 링크 `취소`(→ `?year=`) · 서버 칸 오류는 칸 아래 · 성공하면 `router.push("/admin/holidays?year={year}&added={date}")`. 막힘 이유·다음 한 수·제출 중·전체 실패 상태는 Task 3. `added-toast.tsx`(신규, 클라이언트): `Toast` 한 개 `공휴일 추가 · {date} 추가됨`, 닫히면 `router.replace("/admin/holidays?year={year}")`. CSS는 tokens.css 토큰만.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/holidays-admin.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 오늘·과거 날짜나 규칙 구분이 추가됐거나, 중복 오류가 기존 이름을 싣지 않았거나, 동시 추가가 두 행·두 로그를 남겼거나, 미래 대체일 자리에 넣은 임시공휴일이 대체일을 옮기지 못했거나, 1월 추가가 전년도 기독탄신일 대체일(`origin_year` 2027)을 옮기지 못했거나, 로그 쓰기 실패 뒤 행이나 재계산 결과가 남았거나, 설정으로 로그가 꺼졌거나, 새 등록이 누수 스캔에서 거부됐다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/admin-master-list-first.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 폼이 `?new=1` 없이 렌더되거나 `show…` 판정 없이 렌더되거나, 행동 링크 `공휴일 추가`가 등록 모드를 열지 않는다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint(경계)·stylelint(토큰 밖 값)·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/holidays.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 폼에서 넣은 선거일이 표에 없거나, 토스트 문구가 카피 계약과 다르거나, 폼이 열린 동안 확정 버튼이 있었거나, 04.2-11의 확정 흐름이 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' domain/holidays/admin.ts | grep -c "recomputeFutureSubstitutes"`가 1 이상이고 `grep -v '^\s*//' domain/holidays/admin.ts | grep -c "withHolidayCalendarLock"`가 2 이상이다(확정·추가)
    - `grep -c "document_update\|document_create\|document_delete" domain/holidays/admin.ts`가 0이다(공휴일은 `holiday_change`만)
    - 카피 문구 `오늘이나 지난 날짜입니다 · 내일 이후 날짜를 적어 주세요` · `날짜 형식이 아닙니다 · 2027-06-03처럼 적어 주세요` · `공휴일 추가 · `가 도메인·액션·화면 코드에 글자 그대로 있고, `grep -c "년은 음력 표에 없습니다 · " "app/(app)/admin/holidays/actions.ts"`가 1 이상이다(Grep · #9)
    - 통합: 2027-10-04 추가 → 개천절 대체일 10-05 · 2028-01-03 추가 → 2027 기독탄신일 대체일 2028-01-04 · 로그 실패 → 10-04는 여전히 대체공휴일(Codex #2·#12 · Codex 2차 #5)
    - `addHoliday`·`deleteHoliday` 안의 재계산 호출이 각각 한 번이고 인자가 `Y - 1`이다(검토 — `Y`로 두 번째 호출하는 줄이 없다, per Codex 3차 추가 #2)
  </acceptance_criteria>
  <done>관리자가 폼으로 미래 선거일을 넣으면 표에 새 행과 토스트가 보이고, 그 추가는 대체일 재계산·행동 로그와 함께만 남는다</done>
  <reversibility rating="reversible">수동 행은 같은 화면에서 지울 수 있고 추가 사실은 `holiday_change` 로그에 남는다. 재계산은 미래 대체일만 바꾼다</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 수동 미래 행 삭제(확인 없음) + 결과 줄 `되돌리기` — 한 트랜잭션(삭제 + 미래 대체일 재계산 + `holiday_change` op delete) · 되돌리기는 같은 추가 경로(op add) · 동시 중복 삭제 로그 1줄 · 소급 삭제 거부</name>
  <files>domain/holidays/admin.ts, app/(app)/admin/holidays/page.tsx, app/(app)/admin/holidays/holidays.module.css, app/(app)/admin/holidays/actions.ts, app/(app)/admin/holidays/actions.registry.ts, app/(app)/admin/holidays/delete-holiday.tsx, app/(app)/admin/holidays/delete-undo.tsx, test/unit/ui/holiday-undo.test.ts, test/integration/holidays-admin.test.ts, test/e2e/holidays.spec.ts</files>
  <precondition>디자인 게이트가 UI-SPEC S2-f를 #1(확인 없이 삭제 + 결과 줄 `되돌리기`)로 개정했다 — `grep -c "다시 영업일이 됩니다" .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 0, `grep -c "삭제됨" .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 1 이상, `grep -c "되돌리지 못했습니다 · " .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 1 이상, `grep -c "재계산으로 바뀐 대체공휴일 행이 함께 보" .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md`가 2 이상(R4 — 새 문구가 있다, 62d1392에서 참). 아니면 멈추고 보고한다</precondition>
  <read_first>
    - UI-SPEC S2-f(312~334행) 전체 · S2-b 「화면 순서」·「1차 버튼은 화면에 하나만」 · Copywriting 삭제·삭제 결과 줄·되돌리기 실패 행 · U-10
    - `app/(app)/admin/corp-cards/card-form.tsx` — `useAction` 결과 모양(`result.data` · `result.serverError` · `result.validationErrors?.<칸>?._errors?.[0]`)
    - `ui/button/Button.tsx`(`variant` · `pending`) — 삭제·되돌리기는 `variant="tertiary"`
    - `test/unit/ui/unread-count.test.ts`(04.2-07 — `"use client"` 파일에서 내보낸 순수 함수를 단위로 증명하는 선례)
    - Task 1 결과(`domain/holidays/admin.ts` 잠금 안 추가 모양 · `deps.recompute` · `addHolidayAction`의 칸 오류 모양)
    - `test/integration/holidays.test.ts` — 04.2-06의 장벽(`pg_stat_activity` 폴링) 도우미 모양
  </read_first>
  <behavior>
    - 통합(금지 회상): 규칙 행(법정·대체) 삭제 → `HolidayNotDeletableError` · 행 그대로 · 테스트가 DB에 직접 넣은 지난 날짜 수동 행과 오늘 날짜 수동 행 삭제 → 같은 오류 · 행 그대로 · 미래 수동 행 삭제 → 행 사라짐 + 반환 `{ deleted: true, date, name, kind }`(지운 행의 원래 값) + 로그 `holiday_change` 1줄(`detail.op = "delete"`, 원래 `date`·`name`·`kind`) · 보기 전용 viewer → `HolidayForbiddenError`
    - 통합(Codex #12): 같은 미래 수동 행을 `Promise.all`로 두 번 삭제 → 둘 다 오류 없음 · 하나는 `{ deleted: true, … }`, 하나는 `{ deleted: false }` · 행 0 · 삭제 로그 1줄
    - 통합(Codex #12): `deleteHoliday(…, { recordAction: 던지는 가짜 })` → 오류 · 행 그대로
    - 통합(Codex #2 되돌림): `now` 2026-09-24 · Task 1의 2027-10-04 임시공휴일 추가 뒤 그 행 삭제 → 개천절 대체일 행이 다시 2027-10-04 · 2027-10-05 행 없음 · 2027 행 집합이 추가 전과 같다
    - 통합(해 넘김 되돌림 — Codex 2차 #5): Task 1 해 넘김 준비 뒤 2028-01-03 임시공휴일 삭제 → 기독탄신일 대체 행(`origin_year = 2027`)이 다시 `2028-01-03` 하나 · 2028-01-04 대체 행 없음
    - 통합(장벽 동시 삭제 — Codex 2차 #4): `now` 2026-09-24 · `addHoliday`로 2027-10-04·10-05 임시공휴일(개천절 대체일 10-06) → 두 `deleteHoliday`(10-05 · 10-04)를 동시에 부른다. 첫 호출의 `deps.recompute`는 장벽을 기다린 뒤 진짜 `recomputeFutureSubstitutes`를 부르는 감싼 함수다 · 장벽은 두 번째 연결이 잠금 대기에 들어간 것(`pg_stat_activity`의 `wait_event_type = 'Lock'`)을 폴링으로 확인한 뒤 풀린다(5초 안에 못 보면 테스트 실패 — 순차 실행 가짜 통과 방지) → 둘 다 끝난 뒤 `origin_year = 2027`인 개천절 대체 행이 정확히 `2027-10-04` 하나 · 삭제 로그 2줄
    - 통합(되돌리기 — #1): `now` 2026-09-24 · 2027-10-04 임시공휴일 `임시공휴일 테스트` 추가(대체일 10-05) → 삭제(대체일 10-04로 돌아옴) → 삭제가 돌려준 `{ date, name, kind }` 그대로 `addHoliday(관리자, { date, name, kind }, { now })` → 2027-10-04 행이 같은 이름·`temporary`로 돌아오고 개천절 대체일이 다시 2027-10-05 · 2027 행 집합이 삭제 전과 같다 · 그 행의 `holiday_change` 로그가 add → delete → add 세 줄이고 마지막 줄 `detail`이 같은 `date`·`name`·`kind`
    - 통합(되돌리기 거절 — #1): 삭제 뒤 같은 날짜에 다른 이름의 수동 행을 `addHoliday`로 넣고 되돌리기 → `DuplicateHolidayError`(문구 `이미 공휴일입니다({다른 이름}) · 다른 날짜를 적어 주세요`) · 삭제 뒤 `now`를 지운 날짜 당일로 옮겨 되돌리기 → `PastHolidayDateError` · 두 경우 모두 로그가 늘지 않는다
    - 단위(`test/unit/ui/holiday-undo.test.ts` — `delete-undo.tsx`의 순수 함수 `undoFailure(result)`): `{ data: { date, year } }` → null · `{ validationErrors: { date: { _errors: ["오늘이나 지난 날짜입니다 · 내일 이후 날짜를 적어 주세요"] } } }` → `{ text: "되돌리지 못했습니다 · 오늘이나 지난 날짜입니다", retry: false }` · `["이미 공휴일입니다(설날) · 다른 날짜를 적어 주세요"]` → `{ text: "되돌리지 못했습니다 · 이미 공휴일입니다(설날)", retry: false }` · `{ serverError: "…" }` · 던짐(`null`로 넘김) → `{ text: "되돌리지 못했습니다 · 다시 시도", retry: true }`
    - E2E(PC): 테스트 DB에 `{Y+1}-07-08` `임시공휴일 테스트 가`·`{Y+1}-07-09` `임시공휴일 테스트 나`를 직접 넣고 시작 → 규칙 행 동작 칸은 비어 있고 두 수동 행에만 3차 `삭제` → 가 행 `삭제` → 그 행 라벨 `삭제…` 비활성, 나 행 `삭제`는 그대로 → 확인 단계 없이 서버에서 다시 그린 표에서 가 행이 사라지고 상태 줄이 `후보 · 공휴일 {N}일`(N은 다시 그린 그 해 행 수 — 이 두 날짜는 대체일과 무관해 삭제 전보다 1 작다) · 토스트 없음 · 상태 줄과 표 사이 `role="status"` 한 줄 `{Y+1}-07-08 임시공휴일 테스트 가 삭제됨` + 3차 `되돌리기`, 포커스가 `되돌리기` · 그동안 화면의 1차 버튼은 `{Y+1}년 공휴일 확정` 하나 → 나 행 `삭제` → 결과 줄이 나 행의 줄 하나로 바뀐다 → `되돌리기` → 라벨 `되돌리기…` 비활성 → 결과 줄이 사라지고 나 행이 같은 이름·`임시공휴일`로 돌아오며 포커스가 그 행의 `삭제` · 토스트 없음
    - E2E(PC, 줄 수명): 가 행 삭제 뒤 연도 링크로 다른 해에 갔다 돌아오면 결과 줄이 없다 · 다시 삭제한 뒤 필터 줄 `공휴일 추가`로 폼을 열면 결과 줄이 없다
    - E2E(PC, 실패): `page.route`로 삭제 요청을 끊으면 행이 남고 그 행 동작 칸의 `삭제` 옆에 `삭제하지 못했습니다 · 다시 시도` → 끊기를 풀고 `삭제`를 다시 누르면 지워진다 · 되돌리기 요청을 끊으면 결과 줄이 `되돌리지 못했습니다 · 다시 시도`이고 `되돌리기`가 남는다 · 삭제 뒤 테스트 DB에 같은 날짜 `다른 공휴일 테스트` 행을 직접 넣고 `되돌리기` → 결과 줄 `되돌리지 못했습니다 · 이미 공휴일입니다(다른 공휴일 테스트)`이고 `되돌리기`가 없다
  </behavior>
  <action>
① behavior를 먼저 쓴다(RED). 지난 날짜·오늘 날짜 수동 행은 통합 테스트가 DB에 직접 넣는다(프로덕션 코드에 과거 날짜 입력 경로를 만들지 않는다). 장벽 동시 삭제는 두 호출이 서로 다른 풀 연결에서 도는지 먼저 단언하고, 기대 날짜는 계산하지 않고 위 값을 그대로 적는다(per Codex 2차 #4). E2E `beforeAll`은 테스트 DB `holiday_year_confirmations`의 `{Y+1}` 행을 지우고(04.2-11과 같은 방식 — 같은 파일의 확정 E2E 뒤에도 `후보`에서 시작), `{Y+1}-07-08`·`-07-09` 행을 지우고 다시 넣고, `afterAll`은 두 날짜와 거절 준비 행을 지운다. E2E의 상태 줄 N 기대값은 그때그때 그 해 행 수를 DB로 세어 쓴다.

② **도메인** `deleteHoliday(viewer, id, deps?: { now?: Date; recordAction?; recompute? })` — `<probe_fallback>` D-4210 「삭제」 순서 그대로(`withHolidayCalendarLock` 안 · 지운 날짜의 해 Y에 대해 `recomputeFutureSubstitutes(Y - 1, …)` 한 번 — per Codex 2차 #4·#5 · 3차 추가 #2), 로그는 D-4220 모양(`op: "delete"`, 지운 행의 원래 값). 반환 `{ deleted: true; date; name; kind } | { deleted: false }`. 되돌리기용 도메인 함수는 만들지 않는다 — Task 1의 `addHoliday`가 그 경로다(per D-4209 개정 · #1).

③ **액션** `deleteHolidayAction` — zod `{ id: z.string().uuid() }` → `deleteHoliday(ctx.viewer, id)` → `revalidatePath("/admin/holidays")` · 반환은 도메인 반환 그대로. `actions.registry.ts`에 `registerAction({ name: "deleteHolidayAction", menu: "admin.holidays", action: "write", dtoName: null })` 한 줄. 되돌리기 액션을 따로 두지 않는다(`addHolidayAction` 재사용).

④ **화면(per D-4209 개정 · UI-SPEC S2-f):** `delete-undo.tsx`(신규, 클라이언트): `DeleteUndoSection({ children })`이 결과 줄 상태(`{ date, name, kind } | null` · 되돌리기 대기 · 실패 문구 · 돌아온 행에 줄 포커스 날짜)를 들고, 상태가 있으면 children(표) **위**에 `role="status"` 한 줄 `{date} {name} 삭제됨` + 3차 `Button` `되돌리기`(대기 `되돌리기…` 비활성)를 그린다(`--fs-sm --muted`, 간격 `--s-2`, 줄바꿈 허용·말줄임 없음). 줄이 새로 설 때 포커스는 `되돌리기`. `useDeleteUndo()`는 행 버튼이 쓰는 `show(값)`과 포커스 날짜를 내준다. `되돌리기`는 `addHolidayAction({ date, kind, name })`을 부르고 — 성공이면 줄을 지우고 돌아올 행의 날짜를 기억한 뒤 `router.refresh()`(토스트 없음) — 실패면 순수 함수 `undoFailure(result)`(내보낸다)가 만든 `{ text, retry }`로 줄 글자를 `--danger` 문구로 바꾸고 `retry`가 거짓이면 `되돌리기`를 치운다(원인은 칸 오류 문구의 ` · ` 앞부분). `delete-holiday.tsx`(신규, 클라이언트): 3차 `Button` `삭제` 하나 — 누르면 바로 `deleteHolidayAction({ id })`, 대기 `삭제…` 비활성(그 행만), 실패면 그 행 동작 칸에 `--fs-sm --danger` `삭제하지 못했습니다 · 다시 시도`(같은 `삭제`로 다시), `deleted: true`면 `show({ date, name, kind })` 뒤 `router.refresh()`, `deleted: false`면 `router.refresh()`만. 마운트될 때 자기 날짜가 포커스 날짜와 같으면 스스로 포커스를 받고 그 값을 지운다(되돌리기 뒤 포커스). 확인 단계·두 번째 버튼을 두지 않는다 — 공용 두 단계 보관 삭제 컴포넌트는 import하지도 고치지도 않는다(acceptance Grep이 두 파일에서 그 이름을 찾는다). `page.tsx` 동작 칸 — `canWrite`이고 `row.deletable`이면 `<DeleteHoliday id=… date=… name=… kind=… />`, 아니면 빈 칸(`canWrite`가 없으면 04.2-11처럼 칸 자체가 없다). 표를 `<DeleteUndoSection key={`${year}:${showForm ? "form" : "list"}`}>`로 감싼다 — 연도를 바꾸거나 폼을 열면 줄이 사라지고, 화면을 떠나면 언마운트로 사라진다(UI-SPEC S2-f 줄 수명). CSS는 tokens.css 토큰만이고 `삭제`·`되돌리기`의 폰 누르는 영역은 Task 3이 44×44로 맞춘다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/holiday-undo.test.ts</automated>
    <fails_when>failed 개수가 1 이상이거나 "No test files found"가 나온다 — 거절(칸 오류)이 `retry: true`였거나, 연결·서버 실패가 `되돌리기`를 치웠거나, 원인이 칸 오류 문구의 앞부분이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/holidays-admin.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 규칙 행·지난 수동 행·오늘 수동 행이 지워졌거나(금지 회상 위반), 동시 중복 삭제가 오류를 냈거나 로그를 두 줄 남겼거나, 로그 쓰기 실패 뒤 행이 사라졌거나, 삭제 뒤 대체일(같은 해·해 넘김)이 되돌아오지 않았거나, 장벽이 겹침을 확인하지 못했거나 겹친 두 삭제 뒤 개천절 대체일이 `2027-10-04` 하나가 아니거나, 삭제가 원래 값을 돌려주지 않았거나, 되돌리기가 같은 날짜·이름·구분과 대체일 자리를 되살리지 못했거나 op `add` 로그를 남기지 않았거나, 거절된 되돌리기가 행·로그를 남겼거나, 보기 전용이 지웠거나, 새 등록이 누수 스캔에서 거부됐다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint(토큰 밖 값)·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/holidays.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 규칙 행에 `삭제`가 보였거나, 삭제가 확인 단계를 거쳤거나, 결과 줄·대기 라벨·실패·거절 문구가 카피 계약과 다르거나, 결과 줄이 둘 이상이거나 연도 변경·폼 열기 뒤에도 남았거나, 포커스가 `되돌리기`·돌아온 행의 `삭제`로 가지 않았거나, 되돌리기 뒤 행이 돌아오지 않았거나, 삭제·되돌리기 뒤 토스트가 떴거나, 상태 줄 N이 다시 그린 그 해 행 수와 다르거나, 1차 버튼이 둘 이상 보였다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "보관함" "app/(app)/admin/holidays/delete-holiday.tsx" "app/(app)/admin/holidays/delete-undo.tsx"` 출력이 비어 있다(D-4209 — 보관을 약속하지 않는다)
    - 확인 단계가 없다: `grep -c "DeleteToArchive\|delete-to-archive\|취소" "app/(app)/admin/holidays/delete-holiday.tsx" "app/(app)/admin/holidays/delete-undo.tsx"`가 파일마다 0이고, `grep -c 'variant="primary"' "app/(app)/admin/holidays/delete-holiday.tsx" "app/(app)/admin/holidays/delete-undo.tsx"`가 파일마다 0이다(삭제·되돌리기는 3차 — 1차 버튼 하나, #1)
    - 카피 문구 `삭제하지 못했습니다 · 다시 시도` · `삭제…` · ` 삭제됨` · `되돌리기…` · `되돌리지 못했습니다 · `가 화면 코드에 글자 그대로 있고, `grep -c "다시 영업일이" "app/(app)/admin/holidays/delete-holiday.tsx" test/e2e/holidays.spec.ts`가 파일마다 0이다
    - `grep -c "addHolidayAction" "app/(app)/admin/holidays/delete-undo.tsx"`가 1 이상이고 `grep -c "registerAction" "app/(app)/admin/holidays/actions.registry.ts"`가 셋(확정·추가·삭제)이다 — 되돌리기 전용 액션 없음
    - `grep -v '^\s*//' domain/holidays/admin.ts | grep -c "withHolidayCalendarLock"`가 3 이상이다(확정·추가·삭제 — Codex 2차 #4)
    - 통합: 소급 변경 거부(오늘·과거 추가 — Task 1, 규칙 행·지난·오늘 수동 행 삭제)가 모두 오류이고 행 수가 그대로다 · 동시 중복 삭제 로그 1줄 · 장벽 동시 삭제 뒤 개천절 대체일 `2027-10-04` 하나 · 되돌리기 뒤 로그 add → delete → add
  </acceptance_criteria>
  <done>관리자가 미래 수동 행을 확인 없이 바로 지우고, 다시 그린 표 위 결과 줄의 `되돌리기`로 같은 값을 같은 추가 경로에서 되살리며, 삭제·되돌리기는 달력 잠금 안의 대체일 재계산(원래 해 Y-1부터 표 끝까지)·행동 로그와 함께만 남고, 겹친 동시 삭제도 대체일을 둘 남기지 않으며, 규칙 행과 지난 날짜는 액션 직접 호출로도 지워지지 않는다</done>
  <reversibility rating="reversible">D-4209 바로 삭제 — 결과 줄 `되돌리기`나 같은 폼으로 다시 넣을 수 있고 삭제 사실·원래 값은 `holiday_change` 로그 detail에 남는다. 보관함 경로가 필요해지면 액션과 결과 줄만 바꾼다(UI-SPEC U-10)</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 추가 폼 상태(날짜 칸 포커스·막힘 이유·다음 한 수·칸 오류·제출 중·전체 실패) · 폰 375 칸 접기 · 누르는 영역 44×44 · 독립 DOM 감사 + Codex 적대적 디자인 검토</name>
  <files>app/(app)/admin/holidays/page.tsx, app/(app)/admin/holidays/holidays.module.css, app/(app)/admin/holidays/holiday-form.tsx, test/e2e/holidays.spec.ts, test/e2e/mobile-holidays.spec.ts</files>
  <read_first>
    - 막힘 이유 + 다음 한 수 3차 버튼 선례: Grep `추가할 수 없음\|적기` in `app/` (없으면 SYSTEM.md §7-1·§7-15 PARTIAL 문장만 따른다)
    - 300ms 넘는 대기의 상단 2px 진행 바: Grep 기존 폼의 진행 바 구현(새로 만들지 않는다)
    - `test/e2e/mobile-corp-cards.spec.ts` — 폰 375 칸 접기·가로 스크롤 단언 방식 · 그 화면 CSS의 칸 접기 방식
    - 누르는 영역 선례: `ui/button/Button.module.css` 폰 `.tertiary`(`min-width`·`min-height: var(--touch-min)` — 3차 `삭제`·`되돌리기`는 이미 44×44) · `app/(app)/admin/corp-cards/corp-cards.module.css` `.toggle`(짧은 글자 링크를 패딩으로 넓힘) · `docs/design/tokens.css` `--touch-min`
    - `app/(app)/admin/holidays/*`(Task 1·2 결과) · `domain/holidays/lunar-table.ts`(`LUNAR_TABLE_LAST_YEAR`)
    - UI-SPEC 「터치 목표」(Spacing 절) · S2-b(연도 상태 줄·폰 확정 버튼) · S2-c(폰 칸 접기) · S2-d(날짜 칸 포커스·`min`·`max`) · S2-e · S2-f(폰 결과 줄) · Copywriting S2 「칸 오류: 음력 표 밖 해」
  </read_first>
  <behavior>
    - E2E(PC): `?year={Y+1}&new=1`로 열면 날짜 칸에 포커스가 있다(`toBeFocused` — 디자인 리뷰 #14) · 빈 폼(종류는 `임시공휴일`이 골라져 있다) → 1차 비활성 + `추가할 수 없음 — 날짜 · 이름 2칸 · ` + 3차 `날짜 적기` → 누르면 날짜 칸에 포커스 · 날짜만 채우면 `이름 1칸 · 이름 적기` · 종류 select에 빈 옵션이 없다 · 1차의 `aria-describedby`가 이유 줄을 가리킨다(사용성 원칙 R6)
    - E2E(PC): 날짜 칸 `min`이 KST 내일이고 `max`가 `${LUNAR_TABLE_LAST_YEAR}-12-31`이다(`LUNAR_TABLE_LAST_YEAR`는 `domain/holidays/lunar-table.ts`에서 import — 숫자를 적지 않는다) · 오늘 날짜를 `fill`로 넣고 제출(`noValidate`라 `min`이 막지 않는다) → 날짜 칸 아래 `오늘이나 지난 날짜입니다 · 내일 이후 날짜를 적어 주세요` + 입력값 유지 · 규칙 행 날짜(`{Y+1}-10-03`) → `이미 공휴일입니다(개천절) · 다른 날짜를 적어 주세요` · 키보드로 `max`를 넘긴 `{L+1}-01-05`(L = `LUNAR_TABLE_LAST_YEAR`)를 `fill`로 넣고 제출 → 날짜 칸 아래 `{L+1}년은 음력 표에 없습니다 · {L}년 이전 날짜를 적어 주세요` + 입력값 유지 + 표 행 수 그대로(서버 관문 — 디자인 리뷰 #9). (`날짜 형식이 아닙니다` 칸 오류는 E2E에서 뺀다 — 날짜 입력은 형식이 틀린 값을 제출하지 않는다. 그 zod 문구는 Task 1 acceptance Grep이 지킨다 — R5)
    - E2E(PC): 제출 중 라벨 `공휴일 추가…` + `취소` 비활성 · `page.route`로 요청을 끊으면 이유 자리에 `추가하지 못했습니다 · 다시 시도` · 폼 폭 720 이하
    - E2E(폰 375, `mobile-holidays.spec.ts`): 표 칸은 날짜·이름·동작 셋이고 각 행 아래 접힌 줄 `{요일} · {구분}`(라벨 없음) · 문서 `scrollWidth <= clientWidth` · 확정 버튼(확정 전 연도)은 상태 줄 아래 전폭이고 **보이는 높이**(`getBoundingClientRect().height`)가 40 이상 · 폼 라벨이 칸 위에 있고 칸이 전폭 · 긴 이름 행을 지운 뒤 결과 줄 `… 삭제됨` + `되돌리기`가 줄바꿈되어 가로 스크롤 없음
    - E2E(폰 375, 누르는 영역 — 디자인 리뷰 #7): 확정 · 필터 줄 `공휴일 추가` · 폼 1차 `공휴일 추가` · `취소` · `삭제` · `되돌리기` · 연도 링크 각각에서 요소 중심 기준 (±21.5, ±21.5) 네 점의 `document.elementFromPoint`가 그 요소나 그 자손이다 — 보이는 상자가 아니라 누르는 영역(44×44 이상)을 잰다(UI-SPEC 「터치 목표」)
  </behavior>
  <action>
① behavior를 먼저 쓴다(RED). 실패 흉내는 `page.route`의 `route.abort()`로만 한다.

② **폼 상태** `holiday-form.tsx`: `?new=1`로 열리면 날짜 칸에 포커스를 둔다(날짜 `TextField`에 `autoFocus` — 폼은 `showForm`일 때만 렌더되므로 연 순간에만 걸린다 · UI-SPEC S2-d · 디자인 리뷰 #14) · 날짜 칸 `min`·`max`는 Task 1이 `page.tsx`에서 넘긴 prop 그대로(`max` = `{LUNAR_TABLE_LAST_YEAR}-12-31`) · 빈 칸이 있으면 1차 비활성 + 옆 이유 `추가할 수 없음 — {빈 칸 이름들 · 로 이음} {N}칸 · ` + 3차 버튼 `{첫 빈 칸} 적기`(누르면 그 칸 포커스 — 세는 칸은 날짜·이름뿐이다: 종류는 기본값이 있어 빈 칸이 되지 않는다, R6), 1차의 `aria-describedby`가 이유 줄을 가리킨다 · 제출 중 1차 라벨 `공휴일 추가…`이고 폼의 다른 버튼도 비활성 · 300ms 넘는 대기의 상단 2px 진행 바는 기존 폼 방식 그대로 · 칸 오류 밖의 실패는 이유 자리에 `추가하지 못했습니다 · 다시 시도`. 음력 표 밖 해 칸 오류는 Task 1의 액션이 날짜 칸 `validationErrors`로 보내므로 폼은 다른 칸 오류와 같은 자리에 그대로 쓴다.

③ **폰 칸 접기·누르는 영역** `page.tsx`·`holidays.module.css`: 요일·구분 칸은 375px에서 숨기고 이름 칸 안 둘째 줄 `{요일} · {구분}`(`--fs-sm --muted`)을 폰에서만 보인다 — `mobile-corp-cards` 선례의 CSS 방식. 확정 버튼은 폰에서 상태 줄 아래 전폭이고 **보이는 높이 40을 그대로 둔다**. 폼은 폰에서 라벨 위·칸 전폭. 누르는 영역(UI-SPEC 「터치 목표」 · 디자인 리뷰 #7): 폰(`max-width: 699.98px`)에서 확정 버튼·폼 1차 `공휴일 추가`·`취소`처럼 보이는 높이가 40인 버튼은 보이는 상자를 그대로 두고 투명 확장 영역(`position: relative` + `::before`를 위아래로 넓혀 `min-height: var(--touch-min)` 만큼 덮기)으로 누르는 영역만 44×44 이상으로 넓힌다 · 연도 링크·필터 줄 `공휴일 추가` 같은 짧은 글자 링크는 corp-cards `.toggle` 선례대로 `inline-flex` + `min-width`·`min-height: var(--touch-min)` + 좌우 `--s-2` 패딩 · 3차 `삭제`·`되돌리기`는 공유 `Button` 폰 `.tertiary`가 이미 44×44이므로 CSS를 더하지 않고 E2E로만 잰다. 새 토큰·새 수치 없이 tokens.css 값만 쓴다(stylelint).

④ **화면 검증 순서(CLAUDE.md):** 싼 게이트 → 독립 DOM 감사(별도 에이전트, `CI=true`, 이 플랜의 UI 줄 전부 판정 — backstop·누르는 영역 포함) → **Codex 적대적 디자인 검토(소유자 지시)**: `codex exec -s read-only -C /home/user/ERP_PLANT8_260917 "적대적 디자인 검토. 기준: docs/design/SYSTEM.md(§6-1·§7-1·§7-3·§7-6·§7-8·§7-15·§10), docs/design/tokens.css, .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md 의 터치 목표·S2-b·S2-c·S2-d·S2-e·S2-f·S2-g, 가정 U-3·U-10, Copywriting S2. 대상: git diff origin/main...HEAD -- \"app/(app)/admin/holidays\". 토큰 밖 값·계약 문구 불일치·상태 누락(빈·로딩·오류·부분·넘침·긴 글자)·권한별 DOM 노출·폰 375 가로 스크롤·누르는 영역 44x44·폼 접근성(aria-describedby·포커스 이동)을 심각도(high/medium/low)와 파일:행으로 보고하라. 추가로 판단하라: 확인 없이 지운 뒤 서버에서 다시 그린 표와 결과 줄('{날짜} {이름} 삭제됨' + 3차 되돌리기)이 대체일이 그 날로 돌아오는 삭제·다른 대체일 행이 옮겨지는 삭제·해를 넘는 대체일 삭제에서 오해를 부르지 않는지, 포커스가 되돌리기로 갔다가 되돌린 행의 삭제로 돌아오는지, 결과 줄이 연도 변경·폼 열기에서 사라지는지, 화면의 1차 버튼이 늘 하나인지, 삭제·되돌리기 뒤 상태 줄 N이 서버에서 다시 그린 행 수와 맞는지, 날짜 입력의 포커스·min(내일)·max(음력 표 마지막 해 12-31)과 종류 기본값이 키보드만으로 쓸 수 있는지. 코드를 고치지 마라." < /dev/null` — 출력 원문을 SUMMARY 「Codex 디자인 검토」 절에 붙이고, high는 전체 게이트 전에 고치거나 고치지 않는 이유를 한 줄씩 적는다(문구를 바꿔야 한다는 판단이면 고치지 않고 SUMMARY에 적어 페이즈 끝 `/design-review`로 넘긴다 — S2 문구는 디자인 게이트(62d1392)가 정한 계약이다). `codex`가 없거나 0이 아닌 코드로 끝나면 명령·종료 코드·stderr 끝 20줄을 「Codex 디자인 검토 미실행」으로 적고 **다른 검토로 대신하지 않는다** → 수정 → 전체 게이트 한 번(아래 verify).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>stylelint(토큰 밖 값)·eslint·tsc·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui</automated>
    <fails_when>failed 개수가 1 이상이다 — 목록 우선 규칙·관리자 메뉴 대조·되돌리기 실패 문구(`undoFailure`)가 깨졌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/mobile-holidays.spec.ts test/e2e/admin-nav.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — `?new=1`에서 날짜 칸에 포커스가 없거나, 날짜 칸 `max`가 음력 표 마지막 해 12-31이 아니거나, 막힘 이유·다음 한 수·칸 오류(음력 표 밖 해 포함)·제출 중·전체 실패 문구가 카피 계약과 다르거나, 다음 한 수가 포커스를 옮기지 않거나, 375px에서 가로 스크롤이 생겼거나(결과 줄 포함) 접힌 줄이 없거나, 폰 확정 버튼이 전폭이 아니거나 보이는 높이가 40 미만이거나, 확정·`공휴일 추가` 둘·`취소`·`삭제`·`되돌리기`·연도 링크 가운데 누르는 영역이 44×44 미만인 것이 있거나, axe 위반이 생겼다</fails_when>
  </verify>
  <acceptance_criteria>
    - 카피 문구 `추가할 수 없음 — ` · `추가하지 못했습니다 · 다시 시도` · `공휴일 추가…`가 `holiday-form.tsx`에 글자 그대로 있고(Grep), `grep -c 'type="date"' "app/(app)/admin/holidays/holiday-form.tsx"`가 1 이상, `grep -c "autoFocus" "app/(app)/admin/holidays/holiday-form.tsx"`가 1 이상이며 `grep -c "종류 고르기" "app/(app)/admin/holidays/holiday-form.tsx"`가 0이다(사용성 원칙 R5·R6 · #14)
    - `grep -c "LUNAR_TABLE_LAST_YEAR" test/e2e/holidays.spec.ts`가 1 이상이고 `grep -c "년은 음력 표에 없습니다 · " test/e2e/holidays.spec.ts`가 1 이상이다(#9)
    - `grep -c "touch-min" "app/(app)/admin/holidays/holidays.module.css"`가 1 이상이다(#7)
    - `test -f test/e2e/mobile-holidays.spec.ts`가 참이고 `grep -c "scrollWidth" test/e2e/mobile-holidays.spec.ts`와 `grep -c "elementFromPoint" test/e2e/mobile-holidays.spec.ts`가 각각 1 이상이다
    - SUMMARY에 독립 DOM 감사 결과(backstop은 감사 판정 또는 human_needed) · 「Codex 디자인 검토」 절(출력 원문 또는 「미실행」 + 오류)이 있다
  </acceptance_criteria>
  <done>추가 폼(열면 날짜 칸 포커스 · `min`·`max` · 음력 표 밖 칸 오류)과 확인 없는 삭제·결과 줄이 UI-SPEC S2의 상태를 모두 계약대로 보이고, 폰 375에서 가로 스크롤 없이 읽히며 누르는 영역이 모두 44×44 이상이고(보이는 높이 40은 그대로), 화면 검토에 Codex 검토가 들어갔다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `addHolidayAction`·`deleteHolidayAction` | 클라이언트가 보낸 날짜·구분·이름·id가 공휴일 표(영업일 계산의 단일 출처)를 바꾸고, 같은 트랜잭션에서 미래 대체일이 다시 계산된다 |
| 권한표 → 화면·도메인 | `admin.holidays` view/write가 화면 노출과 쓰기를 가른다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-74 | Tampering | 공휴일 이름 XSS · `added` 파라미터 · 삭제 결과 줄 | low | mitigate | 이름·결과 줄(`{날짜} {이름} 삭제됨`)·되돌리기 실패 문구·토스트는 React 텍스트 노드로만 렌더(`dangerouslySetInnerHTML` 없음). `added`는 ISO 날짜 정규식 + 그 해 행에 있을 때만 쓴다. 결과 줄과 `되돌리기`가 다시 넣을 값은 URL이 아니라 그 브라우저에서 방금 성공한 `deleteHolidayAction` 응답(클라이언트 상태)에서만 온다 — 만든 링크로 관리자에게 임의 공휴일 추가를 누르게 할 수 없고, `되돌리기`도 `addHolidayAction`의 write 검사·날짜·중복 검사를 그대로 거친다(2026-09-25 #1) |
| T-4.2-76 | Elevation of Privilege | `addHolidayAction`·`deleteHolidayAction` | high | mitigate | 화면은 write가 없으면 링크·`삭제`를 렌더하지 않고, 도메인 두 함수가 `can(viewer, "admin.holidays", "write")`를 다시 검사해 `HolidayForbiddenError` — 통합이 보기 전용 viewer의 직접 호출을 거부하는지 단언 |
| T-4.2-77 | Tampering | 공휴일 표 소급 변경 | high | mitigate | 도메인이 오늘·과거 날짜 추가, 규칙 구분 추가, 규칙 행·지난·오늘 수동 행 삭제를 거부(금지 회상 — 통합). 삭제 거부는 트랜잭션째 되돌린다. 규칙 행 수정 경로 자체가 없다 |
| T-4.2-78 | Repudiation | 공휴일 추가·삭제 | medium | mitigate | 끌 수 없는 `holiday_change` 로그를 행 변경과 한 트랜잭션으로(삭제는 원래 값 detail) — 로그 실패 롤백·동시 중복 삭제 1줄·설정 무관 통합(D-4220) |
| T-4.2-79 | Tampering | 미래 대체일 재계산 · 동시 변경 | medium | mitigate | 재계산은 추가·삭제의 달력 잠금 트랜잭션(`withHolidayCalendarLock`) 안에서만, 원래 해 Y-1부터 표 끝까지의 내일 이후 대체일만 지우고 원래 해 순서로 다시 넣는다(04.2-06 `recomputeFutureSubstitutes` — 충돌은 던져 전체 롤백) — 두 방향·해 넘김 통합이 지난 대체일 불변과 결과 표를, 장벽 동시 삭제 통합이 대체일이 둘 남지 않음을 단언(Codex 2차 #4·#5 · 3차 추가 #2) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 새 패키지 0개 — `git diff --stat HEAD -- package.json pnpm-lock.yaml` 출력 없음 |

</threat_model>

<verification>

- 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`
- 단위: `pnpm vitest run --project unit test/unit/ui test/unit/leak-scan-coverage.test.ts test/unit/page-auth-guard.test.ts`(`test/unit/ui/holiday-undo.test.ts` 포함)
- 통합: `bash scripts/dev-db.sh && pnpm db:migrate && pnpm vitest run --project integration test/integration/holidays-admin.test.ts test/integration/holidays.test.ts test/integration/leak-scan.test.ts`
- 독립 DOM 감사(별도 에이전트, `CI=true`) + Codex 적대적 디자인 검토(Task 3 ④ — 미실행이면 보고만) → 수정 → E2E 한 번: `CI=true pnpm playwright test test/e2e/holidays.spec.ts test/e2e/mobile-holidays.spec.ts test/e2e/admin-nav.spec.ts test/e2e/mobile-admin-nav.spec.ts test/e2e/a11y.spec.ts`
- 페이즈 끝 UI 판정은 `/design-review` + Codex 적대적 디자인 검토 → `/qa`(04.2-14 Post-build — 이 플랜이 대신하지 않는다)

</verification>

<success_criteria>

- 수동 추가는 임시공휴일·선거일 + 내일 이후만, 삭제는 수동 미래 행만 — 소급 변경은 도메인이 거부한다
- 추가·삭제는 달력 잠금 안에서 미래 대체일 재계산(원래 해 Y-1부터 표 끝까지 한 호출)·`holiday_change` 로그와 한 트랜잭션이고, 동시 중복 삭제는 로그 한 줄, 겹친 동시 삭제는 대체일 하나다(Codex #2·#12 · Codex 2차 #4·#5)
- 폼(열면 날짜 칸 포커스 · `min`·`max` · 음력 표 밖 칸 오류)·확인 없는 삭제 + 결과 줄 `되돌리기`·폰 칸 접기·누르는 영역 44×44가 UI-SPEC S2 계약대로 E2E(PC·폰)로 CI에 있다
- 삭제에는 확인 단계가 없고(D-4224 폐기 · D-4209 개정 — 2026-09-25 #1) `되돌리기`가 같은 날짜·이름·종류를 기존 추가 경로로 다시 넣어 행과 대체일이 돌아오며 `holiday_change` add 로그가 남는다 — UI-SPEC S2-f(62d1392) → 플랜 → 단위·통합·E2E가 같은 문구를 쓴다. 화면의 1차 버튼은 늘 하나다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 라우트·화면 | `/admin/holidays`에 `holiday-form.tsx` · `delete-holiday.tsx` · `delete-undo.tsx`(`DeleteUndoSection` 결과 줄 + `되돌리기` · `undoFailure`) · `added-toast.tsx` 추가 · 검색 파라미터 `new` · `added` · 폰 375 칸 접기 · 폰 누르는 영역 44×44 |
| Server Action | `addHolidayAction`({ date, kind, name }) · `deleteHolidayAction`({ id }) — 레지스트리 menu `admin.holidays`, write, dtoName null |
| 도메인 | `domain/holidays/admin.ts`: `addHoliday` · `deleteHoliday` · 오류 `PastHolidayDateError` · `DuplicateHolidayError` · `HolidayNotDeletableError` |
| 행동 로그 | `holiday_change`(`detail.op = "add"`·`"delete"`, entity `holiday`) — 행 변경·재계산과 한 트랜잭션 |
| 테스트 | `test/integration/holidays-admin.test.ts`(추가·삭제·원자성·동시·재계산 두 방향·해 넘김·장벽 동시 삭제·음력 표 밖 해·되돌리기 재추가·되돌리기 거절) · `test/unit/ui/holiday-undo.test.ts`(신규 — `undoFailure`) · `test/e2e/holidays.spec.ts`(폼·삭제·결과 줄·되돌리기·실패) · `test/e2e/mobile-holidays.spec.ts`(신규 — 칸 접기·결과 줄·누르는 영역) · `test/unit/ui/admin-master-list-first.test.ts`(holidays 케이스) |
| 검토 기록 | SUMMARY 「Codex 디자인 검토」 |
| UI-SPEC 변경 요청 | 본문 표 R1~R14 — 페이즈 전체(소유 플랜·Task 표시 · 적용 확인 명령 · 상태) · /plan-design-review가 62d1392에서 처리(이력), 실행자는 UI-SPEC을 고치지 않는다 |
| 결정 | D-4210 도메인 연결(개정 — 달력 잠금 안 추가·삭제, 원래 해 Y-1부터 표 끝까지 한 호출로 재계산) · D-4220(공휴일 로그 — 끌 수 없는 종류·한 트랜잭션) · D-4209(개정 2026-09-25 #1 — 확인 없이 바로 삭제, 원래 값은 `holiday_change` 로그 + 결과 줄 `되돌리기`) · D-4224(2026-09-25 폐기 — 확인 단계가 없어졌다) · 추가 폼 결정 줄이기(날짜 입력 `min` 내일 · `max` 음력 표 마지막 해 12-31 · 열면 날짜 칸 포커스 · 종류 기본 `임시공휴일`) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-12-SUMMARY.md` when done
</output>
