---
phase: 04.2-notify-holidays-foundation
plan: 13
subsystem: ui
tags: [system-status, banner, notify, email, holidays, admin, playwright]
status: complete

requires:
  - phase: 04.2-10
    provides: findLastEmailOutcome · findUnresolvedEmail · EMAIL_UNKNOWN_AFTER_MS · EMAIL_UNKNOWN_VISIBLE_DAYS
  - phase: 04.2-12
    provides: /admin/holidays 화면 · holidays.spec.ts 직렬 파일 · UI-SPEC R8·R9 적용
  - phase: 04.2-15
    provides: smtpConfigFromEnv
provides:
  - repositories/notifications.ts — findLastTickRun
  - domain/system-status — SystemStatus.notify · email · emailOutcome · emailFailureBanner · emailFailureBannerFrom · emailFailureBannerText
  - domain/holidays/admin.ts — holidayConfirmationBanner
  - /admin 배너 자리(B2 → B1) · /admin/system-status 두 줄 + 배너 자리(DB 커넥션 → B2, 링크 없음)
  - DECISIONS.md D-4217 · SYSTEM.md §6-8·§7-11 덧붙임
affects: [04.2-14, /design-review, /qa]

actuals:
  tokens: 14151
  tasks: 3
  commits: 4
plan_head_before: 5556db6b3232abb70ac99c7e527adb943188c47c

tech-stack:
  added: []
  patterns:
    - "설정과 결과를 가른다: SystemStatus.email(지금의 SMTP 설정 — from만)과 emailOutcome(과거 발송 결과)은 다른 항목, 화면 값은 설정 부분 + 결과 꼬리"
    - "배너 판정 = 순수 함수(emailFailureBannerFrom) 하나 + 권한 검사 래퍼(emailFailureBanner). 시스템 상태는 이미 읽은 결과로 같은 순수 함수를 부른다(두 번 조회하지 않는다)"
    - "배너 판정 함수는 권한이 없으면 null, 조회가 실패하면 admin.banner_failed 경고 뒤 null — 던지지 않는다"
    - "한 화면 배너 1개: 조건식 한 줄(삼항)로 우선순위를 둔다 — 추상화 없음"

key-files:
  created:
    - app/(app)/admin/system-status/system-status.module.css
    - test/integration/admin-banners.test.ts
  modified:
    - docs/design/DECISIONS.md
    - docs/design/SYSTEM.md
    - repositories/notifications.ts
    - domain/system-status/index.ts
    - domain/holidays/admin.ts
    - app/(app)/admin/system-status/page.tsx
    - app/(app)/admin/page.tsx
    - app/(app)/admin/admin-index.module.css
    - test/unit/system-status.test.ts
    - test/e2e/system-status.spec.ts
    - test/e2e/holidays.spec.ts

key-decisions:
  - "D-4217 문서 계약을 코드보다 먼저 커밋(61318b5) — DECISIONS 한 항목 + SYSTEM.md §6-8·§7-11 각 한 줄 덧붙임, 삭제 줄 0"
  - "failedAt은 findLastEmailOutcome이 돌려주는 그 tick의 started_at(KST 분) — 04.2-10 저장소 그대로, 새 읽기 함수를 만들지 않음"
  - "failedAt은 failed > 0일 때만 채우고, unknownSince는 bundles > 0일 때만 채운다(0건 조각의 시각은 쓰지 않는다)"
  - "`실패 N건` --danger 글자는 StatusTag(text 변형은 --fs-xs로 글자가 작아짐) 대신 화면 CSS 모듈 .failed { color: var(--danger) }. 숫자·시각은 tabular-nums. 긴 주소 줄바꿈은 body의 overflow-wrap: anywhere가 이미 맡음"
  - "배너 3차 링크 .bannerLink는 공휴일 화면 .yearLink와 같은 모양, 폰(<700)에서 min-height var(--touch-min)"
  - "holidayConfirmationBanner deps는 { now, findConfirmation } — 조회 실패 경로를 통합에서 주입해 단언"

requirements-completed: [NOTI-02, NOTI-04, ADMN-11]

metrics:
  completed: 2026-09-26
---

# Phase 04.2 Plan 13: 시스템 상태 알림 발송·이메일 줄 + 관리자 배너 B1·B2 Summary

시스템 상태에 `알림 발송`(마지막 tick)·`이메일`(설정 부분 + 실패·결과 불명 꼬리, 미설정에도 꼬리) 두 줄을 더하고, 「관리」 인덱스에 이메일 실패/결과 불명 경고(B2) → 공휴일 확정 요청(B1) 순의 배너 하나, 시스템 상태에 DB 커넥션 → 링크 없는 B2 배너 하나를 같은 판정 함수로 붙였다(D-4217 문서 계약 먼저).

## 커밋

| Task | 커밋 | 내용 |
|---|---|---|
| 1 ⓪ | 61318b5 | docs: D-4217 DECISIONS 항목 + SYSTEM.md §6-8·§7-11 덧붙임 |
| 1 | 1d1a4d5 | feat: findLastTickRun · getSystemStatus notify/email/emailOutcome · emailFailureBannerFrom/Text · 시스템 상태 두 줄 · 단위·E2E |
| 2 | bbb36e1 | feat: holidayConfirmationBanner · emailFailureBanner · 통합 admin-banners.test.ts(11) |
| 3 | 5b083e1 | feat: /admin 배너 자리(B2 → B1) · 시스템 상태 배너 자리(DB → B2) · holidays.spec.ts 배너 describe |

## TDD

- Task 1 단위: 새 케이스 17개가 먼저 실패(`emailFailureBannerText is not a function` 등) → 구현 → 29/29.
- Task 2 통합: 11개가 먼저 실패(`holidayConfirmationBanner is not a function`) → 구현 → 11/11.
- Task 3 E2E: 배너 describe가 먼저 실패(`2026년 공휴일 확정 전` 배너 요소 없음, CI=true) → 구현 → 통과.
- 예외(정직하게): Task 1의 `system-status.spec.ts` 두 줄 단언은 구현 전에 따로 돌려 실패를 보지 않았다(단위 RED로 같은 기능의 실패를 확인한 뒤 구현하고 E2E는 구현 뒤 처음 돌림). 두 줄 라벨은 구현 전 화면에 없었으므로 그 단언은 구현 전이라면 실패했을 것이지만, 관찰 증거는 없다.

## 게이트 (verification-before-completion — 모두 이 세션에서 새로 실행)

| 게이트 | 결과 |
|---|---|
| `pnpm typecheck` | 0 |
| `pnpm lint` (eslint·stylelint) | 0 |
| `pnpm lint:sql` | 0 |
| 단위 `pnpm vitest run --project unit` | 106 파일 · 1482/1482 |
| 통합 `pnpm vitest run --project integration` | 63 파일 · 1609/1609 |
| 통합 `-t "holiday"` (admin-banners) | D-705 케이스 5개 잡힘 |
| E2E `CI=true` holidays·system-status·admin-nav·a11y·mobile-320 | 342 passed (5.2m) — 프로덕션 빌드 |
| E2E `CI=true` 전체 스위트 | 392 passed (5.9m), 실패·flaky·skip 0, 종료 코드 0 — 프로덕션 빌드 |

## 수용 기준 Grep

- `<Banner` in app/(app)/admin/page.tsx = 2, 한 삼항 안 ✓
- `공휴일 확정 전`·`시스템 상태 보기`·`공휴일 검토` in admin/page.tsx ✓ · `이메일 발송 실패`·`이메일 결과 불명` in domain/system-status/index.ts ✓ · 두 페이지가 `emailFailureBannerText`를 부름 ✓
- `후보로 계산 중|알림함으로만|메일함 확인` = 파일마다 0 ✓ · `ui/banner` in app/(app)/admin/holidays = 없음 ✓
- 시스템 상태 page.tsx의 카피 6개 글자 그대로 ✓ · `host|password|SMTP_USER` 없음 ✓ · `findLastEmailTickRun` 0 ✓
- 단위의 괄호 시각 문자열 1 · `건 · 2026-09-2` 0 ✓
- `D-4217` in DECISIONS.md ✓ · SYSTEM.md `결과 불명` 5회(≥2) ✓ · 문서 diff 삭제 줄 0 ✓
- package.json·pnpm-lock.yaml 변경 없음(새 의존성 0, T-4.2-SC) ✓

## Codex 문서 검토

미실행 — Codex 한도 소진(2026-09-29까지). 사용자·코디네이터 결정(「Codex는 9/29까지 Opus 대체 후 '재확인 필요' 기록」)으로 `codex exec`를 돌리지 않았고, Opus 대체 적대적 문서 검토(대상: `git show 61318b5 -- docs/design/DECISIONS.md docs/design/SYSTEM.md`)는 오케스트레이터가 실행해 이 절에 덧붙인다. 한도가 풀리면 Codex 재확인 필요.

**Opus 대체 적대적 문서 검토 결과(오케스트레이터, 2026-09-26, 읽기 전용 Opus 에이전트):** high 0 · medium 1 · low 6.
- [medium] DECISIONS.md D-4217 ③ 색 규칙이 ①(B2)·②(§6-8)와 나란해 B2 배너 글자에도 `--danger`를 칠하는 것처럼 읽힌다 → **반영** eebb97d(범위를 「②의 §6-8 `이메일` 줄 안에서」로 밝히고 B2는 경고 배너 색 그대로라고 덧붙임). 구현은 처음부터 이 뜻대로였다(독립 DOM 감사로 확인).
- [low] SYSTEM.md 922/923 — 「실패만」 변형은 기존 922행에만 있어 세 변형이 두 행에 걸쳐 있다 · 기존 문장은 고치지 않는 규칙이라 그대로 둔다.
- [low] SYSTEM.md 923 — B2 시각 형식(`YYYY-MM-DD HH:MM`) 미기재 · UI-SPEC에 있음, 보류.
- [low] SYSTEM.md 598 — 결과 불명 꼬리가 「마지막 tick」 문장 안에 있어 7일 창이 안 읽힐 수 있다 · UI-SPEC S4 원문과 같은 구조, 보류(/review에서 재검토).
- [low] DECISIONS.md 937 — 제목 날짜 2026-09-24가 앞 항목(2026-09-26) 뒤에 온다 · 계획이 지정한 제목, 882행 선례, 그대로 둔다.
- [low] DECISIONS.md 938 — 결정자 줄에 Codex 미실행·Opus 대체 사실이 없다 · 이 절과 eebb97d 커밋 메시지에 기록.
- [low] SYSTEM.md 597 — 기존 문장이 값 전체를 설정 여부처럼 적는다 · 598 덧붙임이 보완, 기존 문장 수정 금지라 그대로.
판정: high 0 — 커밋 전 필수 수정 없음. **Codex 재확인 필요(2026-09-29 이후).**

## Codex 디자인 검토

미실행 — Codex 한도 소진(2026-09-29까지). 사용자·코디네이터 결정으로 Opus 대체 적대적 디자인 검토(대상: `app/(app)/admin/page.tsx` · `app/(app)/admin/system-status/`)를 오케스트레이터가 실행해 이 절에 덧붙인다. 한도가 풀리면 Codex 재확인 필요.

**Opus 대체 적대적 디자인 검토 결과(오케스트레이터, 2026-09-26, 읽기 전용 Opus 에이전트):** high 0 · medium 0 · low 4 — 통과.
- 확인: 한 화면 배너 1개(/admin 삼항 B2→B1, system-status DB→B2), B2 링크는 /admin에만, 권한(B1 admin.holidays write · B2 admin.system-status view), role(B1 status · B2/DB alert), SMTP는 `from`만 노출, 조회 실패 시 `admin.banner_failed` + null, 시간대 Intl Asia/Seoul, 새 CSS 값 모두 tokens.css 토큰.
- [low] domain/system-status/index.ts:204-207 — system-status 화면의 결과 조회 실패 로그 키는 `status.email_outcome_unavailable`이라 `admin.banner_failed` 모니터링에서 빠진다 · 계획(235행)은 판정 함수 두 개에만 요구, 보류(/review).
- [low] index.ts:96,98 · system-status/page.tsx:52,57 — 시각이 null이면 빈 괄호 `()` 가능 · 저장소상 bundles>0이면 사실상 null 아님, 보류(/review).
- [low] admin-index.module.css `.bannerLink` 폰 44px로 배너 마지막 줄만 높아짐 · 계약상 44px 필수, 보류(/design-review).
- [low] admin/page.tsx:39-40 — B2 조회 실패(null) 시 B1이 대신 나옴 · S3/error를 B2 한 칸 기준으로 읽으면 허용, 보류(/review).
**Codex 재확인 필요(2026-09-29 이후).**

## 독립 DOM 감사

오케스트레이터가 실행(gsd-executor에는 에이전트 생성 도구가 없다 — .continue-here 반패턴 표). 판정 대상: S3-banners 6줄(overflow backstop — 폰 375·320 가로 스크롤 없음, `.bannerLink` 44px), S4-status-rows 8줄. 참고: `mobile-320-no-overflow.spec.ts`는 이미 `/admin`·`/admin/system-status`를 재지만 그때 DB 상태는 B1(안내)뿐이라 **B2 상태의 폰 넘침은 E2E가 재지 않는다** — 감사에서 B2 행(실행 기록 한 행)을 넣고 재야 한다.

**결과(오케스트레이터가 띄운 별도 Opus 에이전트, 2026-09-26, CI=true와 같은 프로덕션 빌드 `next build` + `next start`, DOM 실측 — 스크린샷 육안 판정 없음):** blocker 0 · major 0 · minor 0 — 모든 칸 PASS.
- 상태: A(B1만) · B(B2 실패만) · C(B2 실패+결과 불명) · D(DB 커넥션 배너가 B2를 밀어냄 — `STATUS_CONN_BANNER_RATIO`로 실제 화면을 만들어 측정) · E(조건 없음) × 폭 1280/700/375/320.
- 문구·role·배너 1개 이하·닫기 버튼 0·링크 href 정확, 폰(375·320) 링크 높이 44px, 모든 상태에서 scrollWidth = innerWidth(가로 스크롤 없음 — **S3/overflow backstop 판정 PASS**, B2 상태 폰 넘침 포함).
- `이메일` 줄 값 정확(`미설정 · 실패 3건 (…)`, C에서 ` · 결과 불명 1건 (…)` 꼬리, D에서 `사용 중 · {긴 주소} · 실패 3건 (…)`), `--danger`(rgb 155,28,28)는 「실패 N건」 span 하나뿐, 긴 SMTP_FROM은 overflow-wrap:anywhere로 칸 안(720/폰 16px 여백), outerHTML에 SMTP host·user·password 0건, 라벨 폭 96/96/84/84px, `알림 발송` 줄 `YYYY-MM-DD HH:MM · 알림 N건 · 중복 건너뜀 0건 · 남음 0건`.
- 넣은 DB 행·감사 계정 모두 정리, 추적 파일 변경 없음. 스크래치: /tmp/claude-0/dom-audit.
- 오케스트레이터 재검증(같은 날, 합쳐진 트리 eebb97d 기준): typecheck/lint/lint:sql 0, unit 1482/1482, integration 1609/1609.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 기존 단위 케이스에 새 읽기 스텁을 넣음**
- **Found during:** Task 1
- **Issue:** `getSystemStatus`가 새 기본 읽기(`findLastTickRun`·`findLastEmailOutcome`·`findUnresolvedEmail`)를 부르면 기존 단위 케이스가 DB에 닿는다.
- **Fix:** `quietDeps`(null·0 스텁)를 기존 `getSystemStatus` 호출 여섯 곳에 펼쳐 넣음. 기존 단언은 그대로.
- **Files modified:** test/unit/system-status.test.ts · **Commit:** 1d1a4d5

**2. [Rule 1 - Lint] 통합 테스트 헬퍼의 any 추론**
- `vi.spyOn` 반환형을 그대로 받아 no-unsafe-* 5건 → 인자 타입을 `{ mock: { calls: unknown[][] } }`로 좁힘. bbb36e1.

**3. 시스템 상태 page.tsx 헬퍼 위치**
- 새 값 함수 두 개(`notifyValue`·`emailValue`)를 기존 「아래 세 줄은 손대지 않는다」 주석 **위**에 두어 그 주석이 여전히 페이지 함수에 붙게 했다(외과적 변경).

**4. 문서 커밋을 코드 커밋과 분리**
- 플랜 ⓪ 「문서 먼저」를 별도 docs 커밋(61318b5)으로 지켰다. 코드 커밋보다 앞선다.

### 플랜과 다르게 판단한 것

- `mobile-320-no-overflow.spec.ts`에 B2 상태를 더하지 않았다 — 플랜 files_modified·behavior에 없고 S3/overflow는 backstop(감사 판정)이다. 위 「독립 DOM 감사」에 감사 몫으로 적었다.

## Known Stubs

없음.

## Threat Flags

없음 — 새 엔드포인트·쓰기 없음. SMTP 설정에서 `from`만 화면에 간다(단위가 host·user·password 부재를 단언, T-4.2-90). 배너는 판정 함수가 권한을 보고(T-4.2-91 — 통합이 권한 없는 viewer의 null을 단언), 조회 실패는 null + 경고(T-4.2-92).

## 오케스트레이터 다음 할 일

1. Opus 적대적 문서 검토(D-4217 diff) · Opus 적대적 디자인 검토 → 위 두 절에 덧붙임, high는 전체 게이트 전에 수정.
2. 독립 DOM 감사(별도 Opus 에이전트, `CI=true`, 1280/700/375/320) — B1·B2(실패 · 결과 불명 · DB 커넥션 밀어냄) 상태 각각, `.bannerLink` 폰 44px.
3. 2026-09-29 뒤 Codex 문서·디자인 재확인.

## Self-Check: PASSED

파일 2개(created) 존재 · 커밋 61318b5 · 1d1a4d5 · bbb36e1 · 5b083e1 존재.
