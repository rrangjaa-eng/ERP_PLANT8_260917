---
phase: 04.2-notify-holidays-foundation
plan: 14
type: execute
wave: 7
depends_on: [04.2-01, 04.2-02, 04.2-03, 04.2-04, 04.2-05, 04.2-06, 04.2-07, 04.2-08, 04.2-09, 04.2-10, 04.2-11, 04.2-12, 04.2-13, 04.2-15]
files_modified:
  - db/migrations/*.sql
  - db/migrations/meta/*_snapshot.json
  - db/migrations/meta/_journal.json
  - test/unit/db/migration-journal.test.ts
  - lib/db-transaction.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [ADMN-11, NOTI-01, NOTI-02, NOTI-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "머지 직전 브랜치는 `origin/main`을 합친 상태이고, `db/migrations` 아래에서 main과 다른 것은 생성기가 main의 마지막 항목 뒤에 다시 만든 이 페이즈 마이그레이션 **한 벌**(SQL 하나 · snapshot 하나 · 저널 항목 하나 — idx는 main의 마지막 idx + 1)뿐이다. 이 페이즈가 앞서 만든 초안 마이그레이션과 그 snapshot·저널 항목은 남지 않는다"
    - "재생성 절차는 「머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다」이고, 지울 파일은 이름 글롭이 아니라 **목록**으로 정한다 — 머지 전 `merge-base..HEAD`에서 이 페이즈가 더한 `db/migrations` 경로 가운데 main에 없는 경로. main에도 있는 경로(같은 번호 snapshot 충돌)는 지우지 않고 main 내용으로 되돌린다(Codex #6·#8)"
    - "한 번의 실행은 main 커밋 하나에 고정된다 — 맨 먼저 `git fetch origin main` 뒤 `MAIN=$(git rev-parse origin/main)`을 `$(git rev-parse --git-dir)/phase-04.2-main-sha`에 적고, 목록 만들기·합치기·되돌림·비교·업그레이드 DB·리허설이 모두 그 `$MAIN`을 쓴다(`origin/main`을 다시 읽지 않는다). 실행 도중 main이 앞서가도 이번 실행은 `$MAIN` 기준으로 끝나고, 새 main은 절차를 처음(fetch → 고정)부터 다시 돌려 반영한다. 리허설은 고정 M1=`$LAST^`로 한 번, M2=`$LAST`로 처음부터 한 번 돌려 main이 앞서간 경우에도 main snapshot이 남는지 확인한다(Codex 2차 #8)"
    - "`db/migrations` 전체를 고정한 main(`$MAIN`)으로 되돌리고 목록의 파일을 지운 직후 `git diff \"$MAIN\" -- db/migrations` 출력과 추적되지 않은 파일이 모두 비어 있다 — 그 뒤에만 생성한다. 같은 번호 snapshot 충돌을 일부러 만든 임시 작업 트리 리허설에서도 main의 snapshot이 그대로 남는다(Codex #6)"
    - "같은 절차를 두 번 돌리면 두 번째도 같은 목록 규칙으로 첫 번째 결과(재생성 한 벌)를 지우고 다시 만들며, 두 결과의 SQL 파일 이름과 내용이 같다(Codex #8)"
    - "재생성한 마이그레이션은 버리는 DB 두 곳에서 적용된다 — (가) 빈 DB에 전부 (나) `$MAIN` 작업 트리로 main 마이그레이션까지 적용한 DB에 이 브랜치 마이그레이션을 더 적용. 두 DB 모두 `drizzle.__drizzle_migrations` 행 수 = 저널 항목 수이고 표 다섯(`notification_log`·`notify_tick_runs`·`holidays`·`holiday_year_confirmations`·`holiday_year_generations`)과 `notification_log_dedup_key`·`holidays_date_key`·두 CHECK·`notification_log_recipient_created_idx`가 있다. 확인 뒤 두 DB를 지운다(Codex #7)"
    - "Task 1·3의 통합·E2E 게이트도 초안이 적용된 기존 개발 DB(`erp`)나 통합 DB(`erp_test`)를 쓰지 않고, 게이트마다 새로 만든 버리는 DB `erp_0402_gate_test`(E2E global-setup의 `_test` 접미어 보호를 통과하는 이름)에 `DATABASE_URL`을 덮어써 빈 상태에서 마이그레이션한 뒤 돌리고, 끝나면(실패해도) 지운다 — 초안의 적용 기록이 남은 DB에서 재생성 마이그레이션이 「이미 있는 표」로 실패하거나 조용히 건너뛰는 일이 게이트 결과를 가리지 않는다(Codex #7 잔여)"
    - "이 브랜치가 공유 환경(스테이징·프로덕션)에 배포된 적이 있으면 재생성하지 않는다 — 초안 마이그레이션이 이미 그 DB의 적용 기록이라 합쳐 다시 만들면 적용 기록과 파일이 어긋난다. 배포되지 않았다는 증거는 가지 이름이 아니라 커밋으로 잡는다: `deploy.yml`은 main push(스테이징)와 `workflow_dispatch`(스테이징은 실행한 ref, 프로덕션은 스테이징이 서빙 중인 SHA만 승격)로만 돈다. 그래서 `git rev-list \"$MAIN\"..HEAD`의 모든 커밋이 `deploy.yml` 실행 기록(이벤트 무관, `headSha`)에 하나도 없으면 Actions 경로로는 배포되지 않았고, 프로덕션은 스테이징을 거쳐야 하므로 함께 배제된다. 한계 — 사람이 로컬에서 `scripts/deploy.sh`를 직접 돌린 배포는 Actions 기록에 없어 이 증거로 배제되지 않는다. 그 한계를 SUMMARY에 그대로 적고, 사람에게 묻는 것으로 메운다: 실행자는 재생성 전에 멈추고 사용자에게 「이 브랜치(이 페이즈 커밋)를 `scripts/deploy.sh`로 직접 배포한 적이 없는가」를 물어, 받은 답을 그대로 SUMMARY 「배포 이력 증거」에 적는다 — 「없다」가 아니면(있다 · 모른다 · 답 없음) 멈춘다. 스테이징·프로덕션 DB의 `__drizzle_migrations`를 직접 읽어 확인하지 않는다 — 공유 DB 직접 명령은 CLAUDE.md 금지다(Codex 3차의 「권위 있는 마이그레이션 증거」 요구는 이 이유로 반려). 커밋 하나라도 기록에 있거나 `gh`를 쓸 수 없으면 멈추고 보고한다(Codex #7 · Codex 2차 #7 잔여 · 3차)"
    - "재생성한 마이그레이션 첫머리에 락 타임아웃 한 쌍과 머리 주석 「머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다」가 글자 그대로 있고 `pnpm lint:sql`이 초록이다"
    - "`test/unit/db/migration-journal.test.ts`(04.1과 공용 — 두 번째로 머지하는 쪽이 중복을 없앤다)가 저널 idx 0..n-1 연속 · tag 접두어 = 0 채운 idx · `when` 엄격 증가 · 항목마다 SQL 파일 존재를 단언하고 초록이다"
    - "REQUIREMENTS 추적표의 ADMN-11·NOTI-01·NOTI-02·NOTI-04 네 줄이 Phase 04.2이고, ROADMAP Coverage 표에 `04.2 | 4` 행이 생기고 7 행이 `EXP-11, EXP-12, NOTI-03` 3개가 되며 합계는 86 그대로다(옮긴 것 + 남긴 것 = 원래 7개)"
    - "`docs/ARCHITECTURE.md`의 환경 변수 표에 `NOTIFY_TICK_SCHEDULER_SA`·`NOTIFY_TICK_OIDC_DISABLED`가 있고 SMTP 줄과 §10 「이후 페이즈」의 이메일·알림 tick 표기가 Phase 04.2로 고쳐진다(표기만 — 300줄 이하)"
    - "`.planning/STATE.md`의 현재 페이즈 값은 바뀌지 않는다(Phase 4가 다른 세션에서 실행 중)"
    - "합친 뒤 공용 `DbOrTx`(`db/client.ts`)는 Phase 4와 이 페이즈 멤버의 합집합이고(`\"delete\" | \"execute\"` 포함 — 04.2-06), 04-32의 `lib/db-transaction.ts`에 「DbOrTx 타입 자체는 넓히지 않고」 주석이 있으면 그 한 줄만 04.2-06이 넓혔다는 사실로 고친다(없으면 그 파일을 건드리지 않는다). 04.2-10이 더한 `db/client.ts`의 `POOL_CONNECTION_TIMEOUT_MS`(5000)·두 `connectionTimeoutMillis` 줄과 `repositories/settings.ts`의 `findSimpleValue(viewer, key, tx?)`는 04-32(b395483 · c9e70ea)와 같은 줄이라 합칠 때 한 벌로 겹친다 — 충돌이 나면 main 쪽을 받고, 합친 뒤 상수가 5000이고 두 Pool에 그 줄이 있으며 `findSimpleValue`가 `tx`를 받는지 확인한다. 하나라도 아니면 04.2-10 예산(풀 대기 5초 · 상한 읽기가 tick 잠금 트랜잭션 안)이 깨진 것이라 멈추고 보고한다(Codex 2차 #7·#6 · 3차 #6)"
    - "전체 게이트 `CI=true pnpm test`와 `pnpm build`가 합친 main 위에서 한 번 초록이다"
    - "이 플랜 뒤 Post-build는 `/review` → `/qa` → `/design-review` + Codex(GPT) 적대적 디자인 검토 → `/cso`(필수 — 인증·외부 입력) → `/ship` 순서이고 하나도 건너뛰지 않는다. Codex 디자인 검토가 실행되지 않으면 그 사실과 오류를 보고하고 다른 검토로 대신하지 않는다(소유자 지시)"
  artifacts:
    - path: "db/migrations/meta/_journal.json"
      provides: "main 항목 + 이 페이즈 재생성 항목 하나(연속 idx · 증가하는 when)"
    - path: "test/unit/db/migration-journal.test.ts"
      provides: "저널 가드(04.1과 공용)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "추적표 네 줄 → Phase 04.2 · 페이즈별 개수 줄"
    - path: ".planning/ROADMAP.md"
      provides: "Coverage 표 04.2 행 · 7 행 3개 · Phase 목록 04.2 요약 줄"
    - path: "docs/ARCHITECTURE.md"
      provides: "환경 변수 두 줄 · SMTP·§10 표기 수정"
  key_links:
    - from: "db/migrations/meta/_journal.json"
      to: "test/unit/db/migration-journal.test.ts"
      via: "가드가 저널을 읽어 연속 idx·증가 when·SQL 파일 존재를 단언"
      pattern: "_journal.json"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "추적표 네 줄(Phase 04.2) = Coverage 표 04.2 행의 네 ID"
      pattern: "04.2"
---

<objective>
**As a** 이 페이즈를 main에 넣는 사람, **I want to** 병렬로 진행한 Phase 4·04.1·04.3의 마이그레이션 위에 이 페이즈 마이그레이션을 생성기로 다시 만들어 붙이고 추적표·문서 표기를 맞춘 뒤 전체 게이트를 한 번 통과시키기를, **so that** drizzle 마이그레이터가 이 페이즈 표를 조용히 건너뛰지 않고(저널 `when` 순서) main의 snapshot을 잃지 않으며, 요구사항 추적이 한 페이즈에 하나씩 맞는다.

(스토리는 ROADMAP 04.2 「계획 단계에서 정할 것」의 마이그레이션·`_journal.json` 충돌·추적표 이동 줄과 코디네이터 메모 2·5, 그리고 코디네이터의 프로젝트 공통 마이그레이션 규칙에서 끌어왔다. ROADMAP Goal 줄은 사용자 스토리 형식이 아니다 — 형식을 맞추려면 `/gsd mvp-phase 04.2`.)

Purpose: [BLOCKING] 스키마 적용(스키마 게이트) · 병렬 머지 규칙 · REQUIREMENTS/ROADMAP 추적 · ARCHITECTURE 표기 · 전체 게이트. Codex #6(snapshot 충돌)·#7(버리는 DB 두 곳 · 배포된 초안 재생성 금지)·#8(글롭 대신 목록 · 두 번 돌리기).

Output: 합친 main 위의 재생성 마이그레이션 한 벌 · 저널 가드 초록 · 두 DB 적용 확인 · 추적표·Coverage 표·Phase 목록 줄 · ARCHITECTURE 환경 변수·표기 · `CI=true` 전체 초록.

**실행 규율:** superpowers `verification-before-completion`을 Skill 도구로 호출한 뒤에만 「완료」를 말한다. 실패를 쫓기 전 `systematic-debugging`. `git push --force`·rebase로 이력 고쳐 쓰기 금지 — 합치기(merge)만 한다. `.planning/STATE.md`는 건드리지 않는다. 프로덕션 DB에는 어떤 명령도 보내지 않는다(버리는 DB는 로컬 dev Postgres 안에서만 만들고 지운다). 이 플랜 뒤 Post-build는 `/review` → `/qa` → `/design-review` + **Codex(GPT) 적대적 디자인 검토** → `/cso` → `/ship`이고 하나도 건너뛰지 않는다 — 이 페이즈는 인증(잠금 문구·OIDC)과 외부 입력(스케줄러 토큰)을 건드려 `/cso`가 필수다(CLAUDE.md). `/ship` 전에 main에 새 마이그레이션이 들어왔으면 Task 1·2를 다시 돈다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-notify-holidays-foundation/04.2-CONTEXT.md
@CLAUDE.md

# 범위 Read:
# .planning/phases/04.2-notify-holidays-foundation/04.2-RESEARCH.md — Pitfall 1(마이그레이션 저널 `when`) · Anti-Patterns 「Hand-numbering」
# .planning/ROADMAP.md — Phase 04.2 절 · Coverage 표(Grep `## Coverage`) · Phase 목록(Grep `- \[ \] \*\*Phase 4:`)
# .planning/REQUIREMENTS.md — 추적표(Grep `| NOTI-01 |` · `| ADMN-11 |`) · Coverage 블록(Grep `By phase`)
</context>

<probe_fallback>
이 플랜은 엣지 행·UI 줄을 새로 떠맡지 않는다 — 앞 플랜들이 떠맡은 31개 엣지와 72개 UI 줄 전부를 합친 main 위의 전체 게이트 한 번으로 다시 확인한다.

**계획 단계 결정(표면화):**

- **D-4215 마이그레이션 병합 규칙(개정 — 코디네이터 공통 규칙 · Codex #6·#7·#8):** 번호를 미리 잡아 두지 않는다. 브랜치에서는 `pnpm db:generate`가 정한 번호를 그대로 쓰고(04.2-01 · 04.2-06), 머지 직전에 이 플랜이 「머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다」를 한다. drizzle `migrate()`는 저널에서 마지막 적용 항목보다 `when`이 늦은 항목만 적용하고(RESEARCH Pitfall 1), 04.1의 저널 가드는 idx 연속을 강제하므로 번호 틈이나 손으로 붙인 번호는 실패한다. 두 초안을 한 벌로 합치는 것은 이 페이즈 표가 모두 신규라 되돌림 경계가 같기 때문이다.
  - **main 고정(Codex 2차 #8):** 첫 계획은 목록을 그때의 `origin/main`으로 만든 뒤 합치기 단계에서 fetch해, 그사이 main에 들어온 snapshot이 삭제 목록에 남아 되돌림 직후 지워질 수 있었다. 그래서 절차의 **첫 단계**가 `git fetch origin main` → `MAIN=$(git rev-parse origin/main)` → `printf '%s\n' "$MAIN" > "$(git rev-parse --git-dir)/phase-04.2-main-sha"`이고, 아래 모든 단계가 `MAIN=$(cat "$(git rev-parse --git-dir)/phase-04.2-main-sha")`로 같은 값을 읽는다. 합치기는 `git merge --no-edit "$MAIN"`이다(두 번째 fetch 없음). main이 앞서간 것을 반영하려면 절차를 처음부터 다시 돈다 — 고정 파일을 새로 쓰고 목록도 새로 만든다.
  - **지울 목록(글롭 금지 — Codex #8):** 머지 **전에** `BASE=$(git merge-base HEAD "$MAIN")`로 `git diff --name-only --diff-filter=A "$BASE" HEAD -- db/migrations`(이 페이즈가 더한 경로)와 `git ls-tree -r --name-only "$MAIN" -- db/migrations`(main 경로)를 정렬해 `comm -23`으로 「이 페이즈만 가진 경로」를, `comm -12`로 「main과 같은 경로(충돌)」를 만든다. 두 목록과 `$MAIN`을 `"$(git rev-parse --git-dir)/phase-04.2-migrations.txt"`에 적고 SUMMARY에도 붙인다. 이 규칙은 초안 두 벌이든 이미 한 번 재생성한 한 벌이든 똑같이 잡는다(이름에 기대지 않는다).
  - **되돌리기(Codex #6):** 합친 뒤 `git checkout "$MAIN" -- db/migrations`로 **트리 전체**를 main으로 되돌리고, 「이 페이즈만 가진 경로」만 `git rm`한다. 충돌 경로는 지우지 않는다 — 이미 main 내용이다. 그 직후 `git diff --quiet "$MAIN" -- db/migrations`와 `git ls-files --others --exclude-standard db/migrations`(빈 출력)로 확인한 뒤에만 생성한다.
  - **배포된 초안은 합치지 않는다(Codex #7 · 2차 #7 잔여):** 증거는 가지 이름 필터가 아니라 커밋이다. `git rev-list "$MAIN"..HEAD`(이 브랜치에만 있는 커밋)를 `gh run list --workflow deploy.yml --limit 1000 --json databaseId,event,headSha,headBranch,createdAt`의 `headSha` 집합과 대조해 교집합이 비어 있어야 한다(이벤트 무관 — push·workflow_dispatch 둘 다). 근거: `deploy.yml`의 트리거는 main push(스테이징)와 `workflow_dispatch`뿐이고, 프로덕션 dispatch는 스테이징이 서빙 중인 SHA만 승격하므로 스테이징을 거치지 않은 커밋은 프로덕션에 갈 수 없다. 한계: 로컬에서 사람이 `scripts/deploy.sh`를 직접 돌린 배포와 삭제된 실행 기록은 이 증거로 배제되지 않는다 — SUMMARY 「배포 이력 증거」 절에 조회한 실행 수·가장 오래된 `createdAt`·브랜치 커밋 수·교집합(빈 목록)·이 한계를 적는다. 교집합이 비어 있지 않거나 `gh`를 쓸 수 없으면 확인하지 못했다고 보고하고 멈춘다(추측으로 넘어가지 않는다). **사람에게 묻기(Codex 3차):** 로컬 직접 배포는 기록이 없으므로 실행자가 Task 1 첫 동작 전에 멈추고 사용자에게 묻는다 — 「이 브랜치의 커밋을 `scripts/deploy.sh`로 스테이징·프로덕션에 직접 배포한 적이 있습니까?(있음/없음)」. 받은 답을 고치지 않고 SUMMARY 「배포 이력 증거」에 적는다. 「없음」일 때만 진행한다. 리뷰가 권한 「공유 DB의 `__drizzle_migrations`로 권위 있게 확인」은 하지 않는다 — 스테이징·프로덕션 DB를 읽는 것도 공유 DB 직접 명령이고 CLAUDE.md 「프로덕션 DB 직접 명령」 금지에 걸린다. 그 위험은 문서화한 한계 + 사람의 답으로 남긴다.
  - **게이트 DB(Codex #7 잔여):** 개발 DB `erp`와 통합 DB `erp_test`에는 초안 마이그레이션이 적용돼 있어 재생성 마이그레이션을 거기에 적용하면 「이미 있는 표」로 실패하거나 결과가 가려진다. Task 1·3의 통합·E2E 게이트는 `erp_0402_gate_test`를 `DROP DATABASE IF EXISTS … WITH (FORCE)` → `CREATE DATABASE … OWNER erp`로 새로 만들고(`scripts/reset-test-db.sh`와 같은 psql 접속), 그 DB의 `DATABASE_URL`을 덮어써(`pnpm db:migrate`의 `--env-file-if-exists`는 이미 있는 환경 변수를 덮지 않는다) 마이그레이션·테스트를 돈 뒤, 성공·실패와 무관하게 지운다. 이름 끝 `_test`는 E2E global-setup의 개발 DB 보호(이름이 `_test`로 끝나지 않으면 지우지 않는다)를 통과시키기 위한 것이다. 로컬 개발 DB `erp`는 게이트에 쓰지 않는다 — 로컬 개발을 이어 가려면 그 DB를 다시 만들어야 한다는 한 줄만 SUMMARY에 적는다(로컬 전용).
  - **공용 트랜잭션 타입(Codex 2차 #7 조율):** 04.2-06이 `db/client.ts`의 `DbOrTx`에 `"delete" | "execute"`를 더했다. 합칠 때 그 줄이 충돌하면 두 쪽 멤버의 합집합으로 푼다. 04-32의 `lib/db-transaction.ts`에 「DbOrTx 타입 자체는 넓히지 않고」 주석이 main에서 들어왔으면, 합친 뒤 그 주석 한 줄만 「DbOrTx는 04.2-06이 delete·execute까지 넓혔다」 뜻으로 고친다(코드 줄은 고치지 않는다). 주석이 없으면 파일을 건드리지 않는다.
- **저널 가드 공용 파일:** `test/unit/db/migration-journal.test.ts`는 04.1도 같은 경로로 만든다. 합칠 때 두 쪽이 모두 추가했으면(add/add 충돌) main 쪽 내용을 남긴다 — 두 번째로 머지하는 쪽이 중복을 없앤다(코디네이터 메모 2).
- **drizzle-kit 질문:** 재생성 때 drizzle-kit이 「표를 새로 만들었나, 다른 표 이름을 바꿨나」 같은 대화형 질문을 하면(이 페이즈 표는 모두 신규라 정상이면 묻지 않는다) 답을 고르지 말고 멈춰서 SUMMARY에 그 질문을 적고 보고한다.
- **`.planning/` 문서 편집:** REQUIREMENTS·ROADMAP 수정은 코디네이터 메모 5와 ROADMAP 04.2 「계획 단계에서 정할 것」이 요구한 계획 산출물이다(PR #49·#50 선례 — 옮긴 요구사항 + 남긴 요구사항 = 원래). 이 두 파일 밖의 `.planning/` 파일, 특히 `STATE.md`는 고치지 않는다.
</probe_fallback>

## Review Dispositions

| Codex # / 검사 | 처리 | 한 줄 |
|---|---|---|
| 6 (high) | 반영 | 머지 전 목록(`comm`) · 트리 전체를 main으로 되돌린 뒤 이 페이즈만 가진 경로만 삭제 · 되돌림 직후 main과 차이 0 확인 · 같은 번호 snapshot 충돌 임시 작업 트리 리허설(Task 1·2) |
| 7 (high) | 반영 | 버리는 DB 두 곳(빈 DB · main 작업 트리로 올린 DB) 적용 + `__drizzle_migrations` 행 수 · 제약 확인 뒤 삭제 · 배포된 브랜치는 재생성 금지(`gh run list` 전제)(Task 1·2) |
| 8 (medium) | 반영 | 이름 글롭 대신 경로 목록 · 초안·재생성본 어느 쪽이든 같은 규칙 · 절차 두 번 실행 뒤 SQL 이름·내용 동일(Task 2) · 수용 기준에 글롭 없음 |
| checker claude_md_compliance(최종 플랜 Post-build) | 반영 | Post-build 목록에 `/design-review` + Codex 적대적 디자인 검토(명령·미실행 시 보고) — objective · verification · truth |
| 7 잔여 (Codex 2차 부분 수정) — 게이트 DB | 반영 | Task 1·3의 통합·E2E 게이트를 새로 만든 버리는 DB `erp_0402_gate_test`에서 돌리고 끝에 지운다 — 초안이 적용된 `erp`·`erp_test`를 쓰지 않는다 |
| 7 잔여 (Codex 2차 부분 수정) — 배포 이력 증거 | 반영 | 가지 이름 필터 대신 브랜치 전용 커밋 전부를 `deploy.yml` 실행 기록의 `headSha`와 대조(이벤트 무관) · 프로덕션은 스테이징 서빙 SHA만 승격하는 근거 · 로컬 `deploy.sh` 직접 실행은 배제 못 한다는 한계를 SUMMARY에 명시 |
| Codex 2차 8 (medium) | 반영 | 맨 먼저 fetch → `$MAIN` 고정 파일 → 목록·합치기·되돌림·비교·업그레이드·리허설 모두 `$MAIN` · 리허설을 M1=`$LAST^`·M2=`$LAST` 두 번 돌려 main이 앞서간 경우를 재현(Task 1·2) |
| Codex 2차 7 (medium) — Phase 4 조율 | 반영 | 합칠 때 `DbOrTx` 멤버 합집합 · 04-32 주석 한 줄 조건부 정정(`lib/db-transaction.ts` 소유 추가) · `connectionTimeoutMillis` Grep 기록(04.2-10 예산 가정) |

### Review Dispositions (Codex 3차)

| Codex 3차 # | 처리 | 한 줄 |
|---|---|---|
| Old #7 잔여 (medium) — 배포 이력 | 일부 반영 · 「권위 있는 마이그레이션 증거」는 반려 | 문서화한 한계(로컬 `deploy.sh` · 삭제된 실행 기록)는 그대로 두고, Task 1 시작 전에 멈춰 사용자에게 직접 배포 여부를 묻고 답을 SUMMARY에 원문으로 남긴다(「없음」일 때만 진행). 리뷰가 권한 스테이징·프로덕션 `__drizzle_migrations` 확인은 공유 DB 직접 명령이라 CLAUDE.md 금지 — 하지 않는다 |
| NEW #6 잔여 (medium) — 합칠 때 | 반영 | 04.2-10이 04-32와 같은 줄로 더한 `db/client.ts` 풀 대기 상수·두 줄과 `findSimpleValue` tx — 충돌 시 main 쪽, 합친 뒤 값 5000·두 줄·tx를 Grep하고 어긋나면 멈춘다(「Grep만 기록」을 대체) |

<tasks>

<task type="tracer">
  <name>Task 1: [BLOCKING] 스키마 적용 — main 고정 → 배포 이력 확인(커밋 대조) → 머지 전 목록 → 고정 main 합치기 → 트리 전체 되돌림 + 목록 삭제 → `pnpm db:generate` 한 벌 → 빈 DB 적용 → 저널 가드 초록</name>
  <files>db/migrations/*.sql, db/migrations/meta/*_snapshot.json, db/migrations/meta/_journal.json, test/unit/db/migration-journal.test.ts, lib/db-transaction.ts</files>
  <precondition>이 브랜치에만 있는 커밋이 `deploy.yml`로 배포된 적이 없다 — `git fetch origin main` 뒤 `git rev-list origin/main..HEAD`의 커밋과 `gh run list --workflow deploy.yml --limit 1000 --json headSha --jq '.[].headSha'` 출력의 교집합이 비어 있다(이벤트 무관). 교집합이 있거나 `gh`를 쓸 수 없으면 멈추고 보고한다(Codex #7 · 2차 #7 잔여 — 근거와 한계는 `<probe_fallback>` 「배포된 초안은 합치지 않는다」) · 사용자가 이 브랜치를 `scripts/deploy.sh`로 직접 배포한 적이 없다고 답했다 — 실행자는 이 Task의 첫 동작 전에 멈추고 `<probe_fallback>` 「사람에게 묻기」의 질문을 한다. 답이 「없음」이 아니면(있음 · 모름 · 답 없음) 멈추고 보고한다(Codex 3차 — 공유 DB를 읽어 확인하지 않는다)</precondition>
  <read_first>
    - `db/migrations/meta/_journal.json` — 합치기 전과 뒤(main 항목 · 이 페이즈 항목의 idx·tag·when)
    - 이 페이즈가 만든 초안 마이그레이션 SQL 머리(04.2-01 · 04.2-06 — 락 타임아웃 한 쌍과 머리 주석 모양). 경로는 목록에서 얻는다
    - 기존 마이그레이션 하나의 첫 세 줄(Grep `SET LOCAL lock_timeout` in `db/migrations`) — 락 타임아웃 선례
    - `drizzle.config.ts` · `scripts/migrate-runner.ts`(`migrationsFolder`는 `process.cwd()` 기준) · `package.json` 스크립트 `db:generate`·`db:migrate`·`lint:sql` · `.squawk.toml`
    - `test/unit/db/migration-journal.test.ts`(04.2-04가 만들었거나 main에서 온 것)
    - `db/schema/notifications.ts` · `db/schema/holidays.ts` · `db/schema/index.ts` — 재생성이 표 다섯을 모두 담는지 확인할 목록
    - `.github/workflows/deploy.yml` 1~40행 — 트리거(main push · `workflow_dispatch` target/sha)와 프로덕션 승격 가드(배포 이력 증거의 근거)
    - `scripts/reset-test-db.sh` · `test/integration/global-setup.ts` · `test/e2e/global-setup.ts` — psql 접속 모양 · `DATABASE_URL ??=` · `_test` 접미어 보호(게이트 DB)
    - 합친 뒤 `db/client.ts` `DbOrTx` 줄 · `lib/db-transaction.ts` 주석(04-32)
  </read_first>
  <action>
① **고정 · 증거 · 목록(머지 전):** 먼저 `<probe_fallback>` 「main 고정」대로 `git fetch origin main` → `$MAIN`을 고정 파일에 적는다(per Codex 2차 #8 — 이 뒤로 `origin/main`을 다시 읽지 않는다). 이어서 「배포된 초안은 합치지 않는다」의 커밋 대조를 `$MAIN` 기준으로 다시 해 SUMMARY 「배포 이력 증거」 절(실행 수 · 가장 오래된 `createdAt` · 브랜치 커밋 수 · 교집합 · 한계)을 쓴다. 그 뒤 「지울 목록」대로 두 목록을 `$MAIN` 기준으로 만들고 파일과 SUMMARY에 적는다. 「이 페이즈만 가진 경로」가 SQL 둘·snapshot 둘 이하가 아니면(예상 밖 경로가 있으면) 멈추고 보고한다.

② **합치기:** `git merge --no-edit "$MAIN"`(fetch를 다시 하지 않는다 · rebase·force 금지). `db/migrations` 아래 충돌은 ③이 트리 전체를 main으로 되돌리므로 따로 고르지 않는다. `test/unit/db/migration-journal.test.ts`가 add/add 충돌이면 main 쪽 내용을 받는다. 그 밖의 충돌(공유 등록부 `domain/action-log/record.ts`·`domain/settings/keys.ts`·`test/integration/leak-scan.test.ts`·`ui/shell/TopBar.tsx`·`docs/design/SYSTEM.md`·`docs/design/DECISIONS.md`)은 **양쪽 항목을 모두 남기는** 쪽으로만 푼다 — 한쪽 항목을 지우거나 문장을 다시 쓰지 않는다. `db/client.ts`의 `DbOrTx` 줄이 충돌하면 두 쪽 멤버의 합집합으로 푼다. 합친 뒤 `<probe_fallback>` 「공용 트랜잭션 타입」대로 `lib/db-transaction.ts`의 04-32 주석을 조건부로 한 줄 고치고(per Codex 2차 #7), 04.2-10이 04-32와 같은 줄로 더한 두 공용 파일을 확인한다(per Codex 3차 #6): `db/client.ts`·`repositories/settings.ts`가 충돌하면 main 쪽을 받는다(같은 이름·같은 값이라 잃는 것이 없다) → `grep -c "POOL_CONNECTION_TIMEOUT_MS = 5000" db/client.ts`가 1 · `grep -c "connectionTimeoutMillis: POOL_CONNECTION_TIMEOUT_MS" db/client.ts`가 2 · `grep -c "tx?: DbOrTx" repositories/settings.ts`가 1 이상. 하나라도 아니면 04.2-10 예산이 깨진 것이라 멈추고 보고한다(여기서 값을 고치지 않는다). 세 결과와 푼 파일 목록을 SUMMARY에 적는다.

③ **되돌림 + 삭제:** `git checkout "$MAIN" -- db/migrations` → 「이 페이즈만 가진 경로」를 `git rm -q`로 지운다(충돌 경로는 지우지 않는다) → `git diff --quiet "$MAIN" -- db/migrations` 종료 코드 0 · `git ls-files --others --exclude-standard db/migrations` 빈 출력을 확인한다. 둘 중 하나라도 아니면 생성하지 않고 멈춘다.

④ **재생성:** `pnpm db:generate --name phase_04_2`. 생성된 SQL이 `notification_log`·`notify_tick_runs`·`holidays`·`holiday_year_confirmations`·`holiday_year_generations` 다섯 표와 04.2-01·06이 정한 제약·색인(`notification_log_dedup_key` · `notification_log_recipient_created_idx` · `holidays_date_key` · 두 CHECK)을 모두 담는지 확인한다. 첫 줄 앞에 `SET LOCAL lock_timeout = '1s';`, `SET LOCAL statement_timeout = '5s';`, statement-breakpoint, 그리고 한 줄 머리 주석 「Phase 04.2 — 알림 원장·tick 실행 기록·공휴일 표. 머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다」를 넣는다(SQL 파일만 고친다 — 저널·snapshot은 생성기 출력 그대로). 대화형 질문이 나오면 `<probe_fallback>` 「drizzle-kit 질문」대로 멈춘다.

⑤ **빈 DB 적용:** 로컬 dev Postgres(`pnpm db:dev`)에 버리는 DB `erp_0402_fresh`를 만들고(`CREATE DATABASE`) `DATABASE_URL=<그 DB> pnpm db:migrate` → `drizzle.__drizzle_migrations` 행 수 = `_journal.json` 항목 수 · `information_schema.tables`에 표 다섯 · `pg_constraint`/`pg_indexes`에 ④의 제약·색인 다섯이 있음을 조회해 SUMMARY에 붙이고 `DROP DATABASE`. 이어서 `pnpm lint:sql` · 저널 가드 · 게이트 DB(`<probe_fallback>` 「게이트 DB」 — 새로 만든 `erp_0402_gate_test`, 끝에 지움)에서 통합 네 파일. 초안이 적용된 개발 DB `erp`·통합 DB `erp_test`에는 `pnpm db:migrate`를 돌리지 않는다(per Codex #7 잔여).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 저널 idx가 끊겼거나, tag 접두어가 idx와 다르거나, 이 페이즈 항목의 when이 main 마지막 항목보다 작거나, SQL 파일이 없는 항목이 있다</fails_when>
    <automated>pnpm lint:sql</automated>
    <fails_when>squawk가 0이 아닌 코드로 끝난다 — 재생성 파일에 락 타임아웃이 빠졌거나 금지 패턴이 생겼다</fails_when>
    <automated>M=$(cat "$(git rev-parse --git-dir)/phase-04.2-main-sha") &amp;&amp; git add -A db/migrations &amp;&amp; S=$(git diff --cached --name-status "$M" -- db/migrations) &amp;&amp; test "$(printf '%s\n' "$S" | cut -f1 | sort | tr -d '\n')" = "AAM"</automated>
    <fails_when>종료 코드가 0이 아니다 — 고정 파일이 없거나, 스테이징 기준으로 고정한 main과의 차이가 「새 SQL 하나 · 새 snapshot 하나 · 저널 수정 하나」가 아니다: 초안이 남았거나, 생성이 두 벌을 만들었거나, main의 다른 마이그레이션 파일이 바뀌었다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; export PGPASSWORD=erp G=postgres://erp:erp@127.0.0.1:5432/erp_0402_gate_test &amp;&amp; psql -h 127.0.0.1 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp_0402_gate_test WITH (FORCE)' -c 'CREATE DATABASE erp_0402_gate_test OWNER erp' &amp;&amp; (DATABASE_URL=$G pnpm db:migrate &amp;&amp; DATABASE_URL=$G pnpm vitest run --project integration test/integration/notify-tick.test.ts test/integration/holidays.test.ts test/integration/notify-email.test.ts test/integration/notify-inbox.test.ts); R=$?; psql -h 127.0.0.1 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp_0402_gate_test WITH (FORCE)'; test $R -eq 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 새로 만든 게이트 DB에 재생성 마이그레이션이 처음부터 적용되지 않았거나(표·제약 누락), 합친 main 위에서 tick·공휴일·이메일·알림함 통합이 깨졌다(초안이 적용된 `erp`·`erp_test`는 쓰지 않는다 — Codex #7 잔여)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY에 고정한 `$MAIN` 값 · 「이 페이즈만 가진 경로」·「main과 같은 경로(충돌)」 두 목록과 되돌림 직후 `git diff --quiet "$MAIN" -- db/migrations` 종료 코드 0 기록이 있다(Codex 2차 #8)
    - SUMMARY 「배포 이력 증거」 절에 조회한 실행 수 · 가장 오래된 `createdAt` · 브랜치 커밋 수 · 빈 교집합 · 한계(로컬 `deploy.sh` 직접 실행은 배제 못 함)가 있다(Codex 2차 #7 잔여)
    - SUMMARY 「배포 이력 증거」 절에 사용자에게 한 질문과 받은 답(「없음」)이 원문 그대로 있다 · 스테이징·프로덕션 DB에 보낸 명령이 없다(Codex 3차)
    - 재생성 SQL(`git diff --cached --name-only --diff-filter=A "$MAIN" -- 'db/migrations/*.sql'`의 한 줄)에 `번호나 _journal.json을 손으로 고치지 않는다`와 `SET LOCAL lock_timeout`이 각각 1회 있다
    - SUMMARY에 `erp_0402_fresh`의 `__drizzle_migrations` 행 수 = 저널 항목 수와 표 다섯 · 제약·색인 다섯 조회 결과가 있고 그 DB는 지워졌다 · `psql -h 127.0.0.1 -U erp -d postgres -Atc "select count(*) from pg_database where datname like 'erp_0402_%'"`가 0이다
    - SUMMARY에 `POOL_CONNECTION_TIMEOUT_MS = 5000`(1) · `connectionTimeoutMillis: POOL_CONNECTION_TIMEOUT_MS`(2) · `tx?: DbOrTx`(settings) Grep 결과와 `lib/db-transaction.ts` 주석 처리(고침·해당 없음)가 있다(Codex 3차 #6)
    - `git diff --name-only HEAD -- .planning/STATE.md` 출력이 비어 있다
  </acceptance_criteria>
  <done>합친 main 위에 이 페이즈 마이그레이션이 한 벌로 다시 생성되어 빈 DB에 적용되고, main의 마이그레이션 파일은 한 바이트도 바뀌지 않았으며, 저널 가드가 그 순서를 지킨다</done>
  <reversibility rating="costly">재생성 뒤 배포되면 마이그레이션 파일 이름·저널 항목이 운영 DB의 적용 기록이 된다 — 배포 전까지는 같은 절차로 다시 만들 수 있고, 배포 뒤에는 새 마이그레이션으로만 고친다</reversibility>
</task>

<task type="auto">
  <name>Task 2: 절차 검증 — main 작업 트리로 올린 DB에 업그레이드 적용 · 절차 두 번째 실행 동일성 · 같은 번호 snapshot 충돌 리허설</name>
  <files>db/migrations/*.sql, db/migrations/meta/*_snapshot.json, db/migrations/meta/_journal.json</files>
  <read_first>
    - Task 1 SUMMARY 기록(두 목록 · 재생성 파일 이름)
    - `scripts/migrate-runner.ts` — `process.cwd()` 기준 `db/migrations`
  </read_first>
  <action>
① **커밋:** Task 1 결과를 한 커밋으로 남긴다(`chore: regenerate phase 04.2 migration on main`, 본문 한국어). 이후 비교는 이 커밋 기준이다.

② **업그레이드 DB(Codex #7):** `git worktree add "$(git rev-parse --git-dir)/wt-main" "$MAIN"`(고정 파일의 값 — per Codex 2차 #8) → 그 작업 트리에 `node_modules`를 심볼릭 링크로 잇는다(설치하지 않는다) → 버리는 DB `erp_0402_upgrade`를 만들고 그 작업 트리에서 `DATABASE_URL=<그 DB> pnpm db:migrate`(main 마이그레이션만) → 저장소로 돌아와 같은 DB에 `pnpm db:migrate`(이 페이즈 한 벌만 더 적용) → `__drizzle_migrations` 행 수 = 이 브랜치 저널 항목 수 · 제약·색인 다섯 조회 → SUMMARY에 붙이고 `DROP DATABASE` · `git worktree remove`.

③ **두 번째 실행(Codex #8):** 재생성 SQL을 `"$(git rev-parse --git-dir)/phase-04.2-first.sql"`로 복사한다. Task 1 ②~④를 같은 `$MAIN`으로 한 번 더 돈다(고정 파일을 다시 쓰지 않는다 — 같은 main에 대한 반복이 같은 결과인지 보는 단계다. 목록은 새로 만든다 — 이번 「이 페이즈만 가진 경로」는 재생성 한 벌이어야 한다. 합치기는 이미 합쳐져 있어 할 일이 없다). 머리 주석을 다시 넣은 뒤 새 SQL의 파일 이름이 첫 번째와 같고 `cmp`로 내용이 같은지 확인한다. snapshot의 `id`·`prevId`와 저널 `when`은 생성마다 달라지므로 비교하지 않고, 확인 뒤 `git checkout HEAD -- db/migrations`와 `git clean -fdq -- db/migrations`로 ①의 커밋 상태로 돌린다.

④ **충돌 리허설(Codex #6 · 2차 #8):** `LAST=$(git log -1 --format=%H "$MAIN" -- db/migrations/meta/_journal.json)`(고정한 main에서 마지막으로 마이그레이션을 더한 커밋)의 **부모**에서 버리는 작업 트리를 연다 — `git worktree add -b rehearsal-04.2 "$(git rev-parse --git-dir)/wt-rehearsal" "$LAST^"`. 그 작업 트리에서 `$LAST`가 더한 snapshot과 **같은 경로**에 가짜 snapshot 파일(내용 `{"rehearsal":true}`)과 가짜 SQL 한 개(`db/migrations/rehearsal_fake.sql`, 내용 `SELECT 1;`)를 더해 커밋하고 그 커밋을 `FAKE`로 적는다 — 이것이 「이 페이즈가 더한 경로가 main이 더한 경로와 겹침」(add/add) 상태다. 그 작업 트리에서 Task 1 ①~③(고정 → 목록 → 합치기 → 되돌림 + 삭제)을 두 번 돈다. 고정 값은 `git fetch` 대신 리허설 값으로 직접 쓴다(작업 트리마다 `git rev-parse --git-dir`가 달라 고정 파일이 따로 있다). (가) **M1 = `$LAST^`**(main이 아직 그 snapshot을 더하기 전): 두 가짜 경로가 모두 「이 페이즈만 가진 경로」이고 되돌림 뒤 `git diff --quiet "$LAST^" -- db/migrations`가 0이다. 확인 뒤 그 버리는 작업 트리 안에서만 `git reset -q --hard "$FAKE"`로 되돌린다. (나) **M2 = `$LAST`**(main이 앞서가 같은 경로 snapshot을 더한 뒤 — 처음부터 다시 고정): 충돌 목록에 그 snapshot 경로가 있고, 합치기 충돌은 ③의 되돌림이 풀며, 되돌림 뒤 `git diff --quiet "$LAST" -- db/migrations`가 0이고 그 snapshot 내용이 `$LAST`의 것과 같다 — M1 목록을 M2 합치기에 쓰지 않았기 때문에 main snapshot이 남는다는 것이 이 리허설의 요점이다. 결과를 SUMMARY에 적고 `git worktree remove --force`와 `git branch -D rehearsal-04.2`로 지운다(원격에 올리지 않는다).
  </action>
  <verify>
    <automated>M=$(cat "$(git rev-parse --git-dir)/phase-04.2-main-sha") &amp;&amp; git diff --quiet HEAD -- db/migrations &amp;&amp; test -z "$(git ls-files --others --exclude-standard db/migrations)" &amp;&amp; cmp "$(git rev-parse --git-dir)/phase-04.2-first.sql" "$(git diff --name-only --diff-filter=A "$M" HEAD -- 'db/migrations/*.sql')"</automated>
    <fails_when>종료 코드가 0이 아니다 — 두 번째 실행 뒤 작업 트리가 ①의 커밋과 다르거나, 첫 번째 재생성 SQL과 커밋된 재생성 SQL이 다르거나, 재생성 SQL이 한 개가 아니다</fails_when>
    <automated>W=$(git worktree list) &amp;&amp; B=$(git branch --list rehearsal-04.2) &amp;&amp; test -z "$(printf '%s\n' "$W" | grep -e wt-main -e wt-rehearsal)" &amp;&amp; test -z "$B"</automated>
    <fails_when>종료 코드가 0이 아니다 — 리허설·업그레이드용 작업 트리나 임시 가지가 남았다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY에 `erp_0402_upgrade`의 `__drizzle_migrations` 행 수 = 저널 항목 수와 제약·색인 다섯 조회 결과가 있고 그 DB는 지워졌다
    - SUMMARY에 두 번째 실행의 목록(재생성 한 벌)과 `cmp` 동일 결과가 있다
    - SUMMARY에 충돌 리허설 두 번(M1=`$LAST^` · M2=`$LAST`)의 목록 · M2 충돌 목록의 main snapshot 경로 · 두 번 모두 되돌림 뒤 고정 main과 차이 0 · M2 snapshot 내용 동일 결과가 있다(Codex 2차 #8)
  </acceptance_criteria>
  <done>재생성 절차가 업그레이드 경로에서도 적용되고, 두 번 돌려도 같은 결과이며, 같은 번호 snapshot 충돌에서도 main의 snapshot을 지우지 않음이 확인됐다</done>
</task>

<task type="auto">
  <name>Task 3: 추적표·Coverage·Phase 목록 줄(REQUIREMENTS·ROADMAP) · ARCHITECTURE 환경 변수·표기 · 전체 게이트 한 번</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` 추적표 `| NOTI-01 |`~`| NOTI-04 |` · `| ADMN-11 |` 줄과 `**Coverage:**` 블록(`By phase` 줄) — 합친 main 기준
    - `.planning/ROADMAP.md` `## Coverage` 표 · Phase 목록(`- [ ] **Phase 4:` 줄과 그 근처의 04.1·04.3·04.5 요약 줄이 main에 이미 있는지) · Phase 04.2 절 Goal 줄
    - `docs/ARCHITECTURE.md` §9 환경 변수 표 · §10 「이후 페이즈가 교체·추가하는 지점」(Grep `## 9` · `## 10`)
    - `lib/env.ts` — 04.2-05가 더한 `NOTIFY_TICK_SCHEDULER_SA`·`NOTIFY_TICK_OIDC_DISABLED`
    - `test/unit/docs-limits.test.ts` — ARCHITECTURE.md 300줄 한도 · 12자리 숫자 금지
  </read_first>
  <action>
① **REQUIREMENTS:** 추적표의 `| NOTI-01 | Phase 7 |`·`| NOTI-02 | Phase 7 |`·`| NOTI-04 | Phase 7 |`·`| ADMN-11 | Phase 7 |` 네 줄의 페이즈 칸만 `Phase 04.2`로 바꾼다(`NOTI-03`은 Phase 7 그대로). `By phase` 줄에서 `7 (7)`을 `7 (3)`으로 바꾸고 `4 (…)` 바로 뒤에 `04.2 (4)`를 넣는다(04.1·04.3·04.5 항목이 main에 이미 있으면 그 순서에 맞춰 끼운다). 합계 86은 그대로다. 파일 끝 `*Last updated:*` 줄 위에 한 줄 `*2026-09-24: Phase 7에서 Phase 04.2로 ADMN-11·NOTI-01·NOTI-02·NOTI-04 이동(PR #50 분리 — 옮긴 4 + 남긴 3 = 원래 7)*`을 더한다.

② **ROADMAP:** `## Coverage` 표에 `| 04.2 | 4 | ADMN-11, NOTI-01, NOTI-02, NOTI-04 |` 행을 4 행 뒤(04.1 행이 있으면 그 뒤)에 넣고 7 행을 `| 7 | 3 | EXP-11, EXP-12, NOTI-03 |`로 바꾼다. **Total** 86은 그대로다. Phase 목록에서 `- [ ] **Phase 4:` 줄 뒤(04.1 요약 줄이 있으면 그 뒤)에 `- [ ] **Phase 04.2: 알림·공휴일 기반** - 법정 규칙 공휴일 후보·연도 확정·임시공휴일(ADMN-11), 알림함·미읽음 배지(NOTI-01), 하루 한 통 묶음 이메일(SMTP 미설정 시 알림함만, NOTI-02), OIDC 검증 `/internal/notify-tick`·advisory lock·배치 상한(NOTI-04), 계정 잠금·해제 행동 로그(D-712)` 한 줄을 넣는다. Phase 04.2 절의 `**Plans:**`·플랜 목록은 계획 단계가 이미 채웠으니 건드리지 않는다. 다른 절·표는 다시 쓰지 않는다(04.1·04.3도 같은 표를 고친다 — 항목만 더한다).

③ **ARCHITECTURE:** §9 표의 SMTP 줄 용도를 `이메일 — 넷 다 채워져야 켜짐(Phase 04.2, D-711)`으로 고치고, 그 아래에 두 줄 — `NOTIFY_TICK_SCHEDULER_SA` | `/internal/notify-tick` OIDC 기대 호출자(스케줄러 서비스 계정 이메일) | deploy.sh가 주입 · `NOTIFY_TICK_OIDC_DISABLED` | 로컬 전용 OIDC 우회(`1`) — 비로컬이면 부팅 거부·deploy.sh 거부 | `.env.local`만. §10의 `**Phase 7:**` 항목을 두 항목으로 나눈다: `**Phase 04.2:**` 이메일 채널(SMTP 4개 실사용)·알림 tick(`/internal/notify-tick`, tick 정체 경보 켜짐)·공휴일 표·영업일 함수 · `**Phase 7:**` 실제 알림 조건 종류·규칙 관리·지급일·마감이 영업일 함수를 씀. 경보가 꺼져 있다는 옛 괄호 문구는 지운다. 300줄 한도를 넘지 않는다.

④ **전체 게이트 한 번:** `pnpm lint && pnpm typecheck && pnpm lint:sql && pnpm build` → 게이트 DB(`<probe_fallback>` 「게이트 DB」 — 새로 만든 `erp_0402_gate_test`에 `DATABASE_URL`을 덮어써 `pnpm db:migrate` 뒤 `CI=true pnpm test`, 끝나면 성공·실패와 무관하게 지운다 — per Codex #7 잔여). 실패하면 `systematic-debugging`으로 원인을 찾아 해당 플랜 범위 안에서 고치고 전체 게이트를 다시 돈다(플레이크로 넘기지 않는다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — ARCHITECTURE.md가 300줄을 넘었거나 12자리 숫자가 들어갔다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; export PGPASSWORD=erp G=postgres://erp:erp@127.0.0.1:5432/erp_0402_gate_test &amp;&amp; psql -h 127.0.0.1 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp_0402_gate_test WITH (FORCE)' -c 'CREATE DATABASE erp_0402_gate_test OWNER erp' &amp;&amp; (DATABASE_URL=$G pnpm db:migrate &amp;&amp; DATABASE_URL=$G CI=true pnpm test); R=$?; psql -h 127.0.0.1 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp_0402_gate_test WITH (FORCE)'; test $R -eq 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 새로 만든 게이트 DB 위에서 합친 main의 단위·통합·E2E(프로덕션 빌드) 가운데 하나라도 실패했다(초안이 적용된 `erp`·`erp_test`는 쓰지 않는다 — Codex #7 잔여)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql &amp;&amp; pnpm build</automated>
    <fails_when>eslint·tsc·squawk·build 중 하나가 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| (NOTI-01|NOTI-02|NOTI-04|ADMN-11) \| Phase 04\.2 \|" .planning/REQUIREMENTS.md`가 4이고 `grep -cE "^\| NOTI-03 \| Phase 7 \|" .planning/REQUIREMENTS.md`가 1이다
    - `grep -n "^| 04.2 | 4 | ADMN-11, NOTI-01, NOTI-02, NOTI-04 |" .planning/ROADMAP.md`가 한 줄이고 `grep -n "^| 7 | 3 | EXP-11, EXP-12, NOTI-03 |" .planning/ROADMAP.md`가 한 줄이며 `**Total** | **86**`이 그대로다
    - `grep -c "NOTIFY_TICK_SCHEDULER_SA\|NOTIFY_TICK_OIDC_DISABLED" docs/ARCHITECTURE.md`가 2 이상이다
    - `grep -n "enabled: false" docs/ARCHITECTURE.md` 출력이 비어 있다
    - `git diff --name-only HEAD -- .planning/STATE.md` 출력이 비어 있다
    - `CI=true pnpm test`·`pnpm build` 한 번 초록 — SUMMARY에 통과 수와 실행 시각을 적는다
  </acceptance_criteria>
  <done>추적표·Coverage 표·Phase 목록·ARCHITECTURE가 Phase 04.2를 가리키고, 합친 main 위에서 전체 게이트가 한 번 초록이다</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이 브랜치 ↔ main | 병렬 페이즈가 같은 저널·snapshot·공유 등록부를 고친다 |
| 마이그레이션 → 공유 DB(스테이징·프로덕션) | 배포가 마이그레이션 Job으로 적용한다 — 적용 기록(`__drizzle_migrations`)과 파일이 어긋나면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4.2-100 | Tampering | `_journal.json` 순서 | high | mitigate | main을 합친 뒤 생성기로 재생성(D-4215) · 저널 가드(연속 idx · 증가 when · 파일 존재) · 빈 DB·업그레이드 DB 두 곳 적용 확인 — 조용히 건너뛴 마이그레이션이 운영에서 표 없음 오류가 되는 것을 막는다 |
| T-4.2-101 | Denial of Service | 마이그레이션 잠금 | medium | mitigate | 재생성 파일 첫머리 `lock_timeout 1s`·`statement_timeout 5s` · `pnpm lint:sql` |
| T-4.2-102 | Tampering | 공유 등록부 병합 | medium | mitigate | 충돌은 양쪽 항목을 모두 남기는 쪽으로만 푼다 · 푼 파일 목록을 SUMMARY에 · 전체 게이트(누수 스캔·행동 로그 세 배열 단언 포함) 한 번 |
| T-4.2-103 | Tampering | main snapshot 삭제 | high | mitigate | 지울 파일은 머지 전 목록(이 페이즈만 가진 경로)뿐 · 트리 전체를 main으로 되돌린 직후 main과 차이 0 확인 · 충돌 리허설(Codex #6) |
| T-4.2-104 | Tampering | 배포된 초안 재생성 | high | mitigate | 브랜치 전용 커밋과 `deploy.yml` 실행 기록 `headSha`의 교집합이 비어 있고 사용자가 `scripts/deploy.sh` 직접 배포가 없다고 답했을 때만 재생성 — 아니면 멈추고 보고(Codex #7 · 2차 #7 잔여 · 3차). 남는 위험(삭제된 실행 기록 · 사람의 기억 착오)은 SUMMARY 한계로 적는다. 스테이징·프로덕션 DB에는 읽기 명령도 보내지 않는다(CLAUDE.md 금지) |
| T-4.2-105 | Tampering | 목록·합치기 기준 main 불일치 | medium | mitigate | 첫 단계에서 `$MAIN`을 고정 파일에 적고 모든 단계가 그 값만 쓴다 · M1·M2 리허설(Codex 2차 #8) |
| T-4.2-SC | Tampering | npm installs | high | mitigate | 이 플랜은 패키지를 더하지 않는다 — 합친 뒤 `package.json` 차이는 main과 04.2-15의 두 줄(`nodemailer`·`@types/nodemailer`)뿐 |

</threat_model>

<verification>

- `pnpm vitest run --project unit test/unit/db/migration-journal.test.ts test/unit/docs-limits.test.ts`
- `pnpm lint:sql`
- 게이트 DB `erp_0402_gate_test`(새로 만들고 끝에 지움)에서 `pnpm db:migrate` → `CI=true pnpm test`(Task 3 verify 명령 그대로)
- `pnpm lint && pnpm typecheck && pnpm build`
- 이 플랜 뒤(건너뛰지 않는다): `/gsd-verify-work` → Post-build `/review` → `/qa` → `/design-review` + **Codex 적대적 디자인 검토** `codex exec -s read-only -C /home/user/ERP_PLANT8_260917 "적대적 디자인 검토. 기준: docs/design/SYSTEM.md, docs/design/tokens.css, docs/design/DECISIONS.md 의 Phase 04.2 항목, .planning/phases/04.2-notify-holidays-foundation/04.2-UI-SPEC.md 전체. 대상: git diff origin/main...HEAD -- app ui docs/design. 화면 사이 일관성(배지·메뉴·알림함·공휴일·배너·시스템 상태)·토큰 밖 값·계약 문구 불일치·상태 누락·권한별 DOM 노출·폰 375 넘침을 심각도(high/medium/low)와 파일:행으로 보고하라. 코드를 고치지 마라." < /dev/null`(출력 원문을 `/design-review` 결과와 함께 남긴다 — `codex`가 없거나 0이 아닌 코드로 끝나면 명령·종료 코드·stderr 끝 20줄을 보고하고 다른 검토로 대신하지 않는다) → `/cso`(필수) → `/ship`

</verification>

<success_criteria>

- 합친 main 위에 이 페이즈 마이그레이션 한 벌(머리 주석 「머지 직전 origin/main 머지 → 이 페이즈의 마이그레이션·스냅숏·journal 항목 삭제 → pnpm db:generate 재실행. 번호나 _journal.json을 손으로 고치지 않는다」)이 빈 DB와 업그레이드 DB에 적용되고, main의 마이그레이션 파일은 그대로다
- 절차를 두 번 돌려도 같은 SQL이고, 같은 번호 snapshot 충돌에서도 main snapshot이 남는다
- REQUIREMENTS·ROADMAP 추적이 04.2 네 개 + 7 세 개 = 원래 일곱 개, 합계 86
- `CI=true pnpm test` 한 번 초록 · STATE.md 불변 · Post-build 목록에 Codex 적대적 디자인 검토가 있다

</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/<생성기 번호>_phase_04_2.sql`(재생성 한 벌, 번호는 생성기 — main의 마지막 idx + 1) + snapshot + 저널 항목 — 04.2-01·06의 두 초안을 대체 |
| 테스트 | `test/unit/db/migration-journal.test.ts`(04.1과 공용, 중복 제거) |
| 계획 문서 | `.planning/REQUIREMENTS.md` 추적표 네 줄 · `By phase` 줄 · 이동 기록 한 줄 · `.planning/ROADMAP.md` Coverage 04.2 행 · 7 행 · Phase 목록 04.2 줄 |
| 문서 | `docs/ARCHITECTURE.md` §9 `NOTIFY_TICK_SCHEDULER_SA` · `NOTIFY_TICK_OIDC_DISABLED` · SMTP 용도 · §10 Phase 04.2/7 항목 |
| 검증 기록 | SUMMARY: 고정 `$MAIN` · 배포 이력 증거(커밋 대조·한계) · 두 목록 · 되돌림 차이 0 · 빈 DB·업그레이드 DB 조회 · 게이트 DB 생성·삭제 · 두 번째 실행 `cmp` · 충돌 리허설 M1·M2 · `connectionTimeoutMillis` Grep |
| 코드 주석 | `lib/db-transaction.ts` 04-32 주석 한 줄(해당할 때만 — `DbOrTx`를 04.2-06이 넓혔다는 사실) |
| 고정 파일(로컬) | `$(git rev-parse --git-dir)/phase-04.2-main-sha` · `phase-04.2-migrations.txt` · `phase-04.2-first.sql` — 커밋하지 않는다 |
| 결정 | D-4215(개정 — 머지 직전 재생성 · main 고정 · 목록 삭제 · 트리 전체 되돌림 · 배포된 초안 금지(커밋 대조) · 게이트는 버리는 DB · 공유 파일은 양쪽 항목 유지) |

<output>
Create `.planning/phases/04.2-notify-holidays-foundation/04.2-14-SUMMARY.md` when done
</output>
