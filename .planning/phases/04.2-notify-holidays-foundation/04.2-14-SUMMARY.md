---
phase: 04.2-notify-holidays-foundation
plan: 14
subsystem: database
tags: [drizzle, migrations, postgres, requirements-tracking, architecture-docs]

requires:
  - phase: 04.2-01
    provides: notify_tick 트레이서 · notification_log 초안 스키마
  - phase: 04.2-06
    provides: holidays·holiday_year_confirmations·holiday_year_generations 초안 스키마 · DbOrTx delete·execute 확장
  - phase: 04.2-13
    provides: 이 플랜 직전까지 완료된 04.2 전체 구현(14/15 plans)
provides:
  - "합친 origin/main(8dbe98f) 위에서 재생성한 이 페이즈 마이그레이션 한 벌 (db/migrations/0017_phase_04_2.sql · meta/0017_snapshot.json · journal 항목 하나) — 04.2-01·06의 두 초안(0017_notify_log·0018_holidays)을 대체"
  - ".planning/REQUIREMENTS.md 추적표: ADMN-11·NOTI-01·NOTI-02·NOTI-04 → Phase 04.2 (NOTI-03은 Phase 7 유지)"
  - ".planning/ROADMAP.md Coverage 04.2 행 · 7 행 축소 · Phase 목록 04.2 요약 줄"
  - "docs/ARCHITECTURE.md §9 NOTIFY_TICK_SCHEDULER_SA·NOTIFY_TICK_OIDC_DISABLED · §10 Phase 04.2/7 분리"
  - "lib/db-transaction.ts 04-32 주석 정정(DbOrTx를 04.2-06이 delete·execute까지 넓혔다는 사실)"
affects: [04.2-post-build, phase-4-merge-coordination]

actuals:
  tokens: 26709
  tasks: 3
  commits: 3
plan_head_before: 7287381f1f7d577d5f1227fbd45d0d1722cf2878

tech-stack:
  added: []
  patterns:
    - "머지 직전 재생성: 병렬 페이즈가 같은 저널을 건드릴 때는 번호를 미리 정하지 않고, 머지 직전에 이 페이즈의 마이그레이션·스냅숏·journal 항목만 지운 뒤 pnpm db:generate로 한 번에 다시 만든다(번호·journal은 생성기 출력 그대로)"
    - "지울 목록은 이름 글롭이 아니라 merge-base 기준 `comm -23`/`comm -12`로 만든 경로 목록 — main과 겹치는 snapshot 경로는 지우지 않고 main 내용으로 되돌린다"
    - "한 실행은 fetch 시점에 고정한 main SHA 하나에 묶인다(고정 파일 `$(git rev-parse --git-dir)/phase-04.2-main-sha`) — 중간에 origin/main을 다시 읽지 않는다"
    - "게이트는 초안이 적용된 기존 dev/통합 DB(erp·erp_test)를 쓰지 않고 매번 새로 만들고 끝에 지우는 버리는 DB(erp_0402_*)에서 돈다"

key-files:
  created:
    - db/migrations/0017_phase_04_2.sql
    - db/migrations/meta/0017_snapshot.json
  modified:
    - db/migrations/meta/_journal.json
    - lib/db-transaction.ts
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - docs/ARCHITECTURE.md

key-decisions:
  - "재생성한 마이그레이션 한 벌(0017_phase_04_2.sql)이 04.2-01의 notify_log와 04.2-06의 holidays 초안 둘을 대체한다 — drizzle-kit generate 한 번이 0016 이후 스키마 diff 전체(표 5개)를 하나의 파일로 묶었다"
  - "배포 이력 증거(자동 수집)와 별개로, 사용자 질문 「이 브랜치의 마이그레이션 초안이 스테이징·프로덕션에 적용된 적이 있나요?」에 대한 답 '없음'을 PR #73 코멘트(id 5847663575, 2026-09-26T15:55:44Z)에서 재확인한 뒤 Task 1을 진행했다"
  - "lib/db-transaction.ts의 04-32 주석 한 줄만 04.2-06이 DbOrTx에 delete·execute를 넓혔다는 사실로 고쳤다 — 코드 줄은 그대로"
  - "REQUIREMENTS·ROADMAP의 04.2 요약 줄은 04.2-CONTEXT/ROADMAP Phase 04.2 절 원문을 그대로 옮겼다(새로 요약하지 않음)"

requirements-completed: [ADMN-11, NOTI-01, NOTI-02, NOTI-04]

metrics:
  duration: ~34min
  completed: 2026-09-26
status: complete
---

# Phase 04.2 Plan 14: 마이그레이션 재생성·추적표 이동·ARCHITECTURE 표기·전체 게이트 Summary

합친 origin/main(8dbe98f) 위에서 04.2-01·06 두 초안 마이그레이션을 지우고 `pnpm db:generate`로 한 벌(`0017_phase_04_2.sql`)로 다시 만들어 빈 DB·업그레이드 DB 두 곳에 적용을 확인했고, 절차를 두 번 돌려 같은 결과를 냈으며 같은 번호 snapshot 충돌 리허설에서도 main의 snapshot이 지워지지 않음을 검증했다. REQUIREMENTS·ROADMAP 추적표를 Phase 04.2로 옮기고 ARCHITECTURE.md에 알림 환경 변수 두 줄을 더한 뒤, 새로 만든 버리는 DB에서 `CI=true pnpm test`(unit 1482 · integration 1609 · E2E 392)를 한 번에 초록으로 통과시켰다.

## Performance

- **Duration:** ~34분 (세션 시작 ~2026-09-26T16:05Z 추정 — Task 3 커밋 16:38:17Z 기준)
- **Started:** 2026-09-26T16:05Z(추정)
- **Completed:** 2026-09-26T16:39Z
- **Tasks:** 3/3
- **Files modified:** 9 (db/migrations 3 · lib/db-transaction.ts 1 · .planning 2 · docs 1 · 삭제 2)

## Accomplishments

- 병렬 페이즈(Phase 4·04.1)와 공유하는 `db/migrations` 저널을 머지 직전 재생성 절차로 안전하게 합쳤다 — main의 기존 17개 마이그레이션 파일은 한 바이트도 바뀌지 않았고(`git diff --quiet` 확인), 새 항목 하나(idx 17)만 늘었다
- 절차 결정론(같은 절차를 두 번 돌려도 같은 SQL)과 충돌 안전성(같은 번호 snapshot이 main에 먼저 생기는 경우에도 main의 snapshot이 지워지지 않음)을 실제 리허설로 증명했다
- REQUIREMENTS·ROADMAP·ARCHITECTURE 세 문서를 Phase 04.2 실제 산출물에 맞게 갱신하고 전체 게이트(lint·typecheck·lint:sql·build·`CI=true pnpm test`)를 합친 main 위에서 한 번에 통과시켰다

## Task Commits

1. **Task 1+2 ①: 마이그레이션 재생성 + db-transaction.ts 주석 정정** - `a5a143a` (chore) — Task 1(스키마 적용)의 산출물을 Task 2 action①의 지시대로 한 커밋에 담았다. Task 2의 나머지 action(②업그레이드 DB ③두 번째 실행 동일성 ④충돌 리허설)은 검증 전용이라 추가 커밋이 없다.
2. **Task 3: REQUIREMENTS·ROADMAP 이동 + ARCHITECTURE 표기 + 전체 게이트** - `19a5610` (docs)

**Plan metadata:** `[이 커밋 — 아래 참고]` (docs: SUMMARY)

_Note: 이 플랜은 tdd="true"가 아니다(마이그레이션 재생성·문서 갱신) — RED/GREEN 사이클 없음._

## Files Created/Modified

- `db/migrations/0017_phase_04_2.sql` - 재생성 마이그레이션 한 벌(표 5개·제약·색인 전부, 머리 주석 + lock_timeout 쌍)
- `db/migrations/meta/0017_snapshot.json` - drizzle-kit 생성 스냅숏
- `db/migrations/meta/_journal.json` - idx 17 항목 추가(main의 idx 0~16은 그대로)
- `lib/db-transaction.ts` - 04-32 주석 한 줄 정정(DbOrTx가 04.2-06으로 delete·execute까지 넓어졌다는 사실)
- `.planning/REQUIREMENTS.md` - ADMN-11·NOTI-01·NOTI-02·NOTI-04 → Phase 04.2, By phase 줄, 이동 기록 한 줄
- `.planning/ROADMAP.md` - Coverage 04.2 행 · 7 행 축소 · Phase 목록 04.2 요약 줄
- `docs/ARCHITECTURE.md` - §9 NOTIFY_TICK_SCHEDULER_SA·NOTIFY_TICK_OIDC_DISABLED·SMTP 표기 · §10 Phase 04.2/7 분리(268줄, 300줄 한도 안)
- 삭제: `db/migrations/0017_notify_log.sql`(→ 0017_phase_04_2.sql로 재생성) · `db/migrations/0018_holidays.sql` · 두 초안의 meta snapshot

## Task 1 — 스키마 적용 상세

### 고정 · 배포 이력 증거

- **고정 `$MAIN`:** `8dbe98f10c8bc325664cbde36231b106fdfea66d` (`git fetch origin main` → `.git/phase-04.2-main-sha`에 기록, 이후 모든 단계가 이 값만 참조)
- **배포 이력 증거 (`deploy.yml` 전체 실행 기록, 필터 없는 `gh api --paginate` 페이지 전체 수집):**
  - `T0`(조회 전 total_count) = 80
  - `T1`(조회 후 total_count) = 80
  - `N`(모은 tsv의 고유 run id 수) = 80 — `T0 = T1 = N`이므로 완전한 이력
  - 가장 오래된 실행: run id 35340209834, `created_at` = 2026-09-18T11:33:23Z, event=push, branch=main
  - 이 브랜치 전용 커밋 수(`git rev-list "$MAIN"..HEAD`, Task 1 시작 시점 측정) = 55개
  - 가장 이른 브랜치 커밋 시각(`git log --reverse --format=%cI "$MAIN"..HEAD | head -1`) = 2026-09-25T02:47:23+00:00
  - 교집합(`git rev-list "$MAIN"..HEAD`의 SHA 집합 ∩ `deploy.yml` 실행 기록 `head_sha` 집합) = **빈 집합** (Actions 경로로는 배포되지 않았다)
  - **한계:** 로컬에서 사람이 `scripts/deploy.sh`를 직접 돌린 배포와 지워진 실행 기록은 이 증거로 배제되지 않는다
  - **사용자 질문과 답(원문):** 「이 브랜치의 마이그레이션 초안이 스테이징·프로덕션에 적용된 적이 있나요? (Actions 수동 배포·deploy.sh 직접 실행 등 경로 무관)」 → **없음**. 출처: GitHub PR #73(`rrangjaa-eng/ERP_PLANT8_260917`) 코멘트 id `5847663575`, 작성 시각 `2026-09-26T15:55:44Z`, 원문: "[코디네이터 지시] 결정 (사용자 결정, 2026-09-27 01:0x KST) — 04.2 마이그레이션 초안(0017_notify_log·0018_holidays) 배포 여부: **없음**. Actions 외 경로(로컬 deploy.sh 등)로 스테이징·프로덕션에 적용한 적 없음. 04.2-14 Task 1(마이그레이션 재생성)을 계획대로 진행한다." — 이 실행 세션에서 GitHub API로 직접 재확인했다(스테이징·프로덕션 DB에는 어떤 명령도 보내지 않았다)
  - `cut -f1 .git/phase-04.2-deploy-runs.tsv | sort -u | wc -l` = 80 = `T0` = `T1` (재확인 완료)

### 머지 전 목록 (merge-base 기준, `comm` 정렬 비교)

```
MAIN=8dbe98f10c8bc325664cbde36231b106fdfea66d
PHASE_ONLY (이 페이즈만 가진 경로):
  db/migrations/0017_notify_log.sql
  db/migrations/0018_holidays.sql
  db/migrations/meta/0017_snapshot.json
  db/migrations/meta/0018_snapshot.json
CONFLICTING (main과 같은 경로): 없음
```

- SQL 둘·snapshot 둘 — 예상 범위 안(멈추지 않고 진행)

### 합치기 · 되돌림 · 재생성

- `git merge --no-edit "$MAIN"` → **Already up to date**(이 세션 시작 전에 이미 origin/main 8dbe98f가 반영된 상태였다 — `merge-base(HEAD, $MAIN) == $MAIN`)
- 공용 트랜잭션 타입 확인(Codex 3차 #6): `grep -c "POOL_CONNECTION_TIMEOUT_MS = 5000" db/client.ts` = 1, `grep -c "connectionTimeoutMillis: POOL_CONNECTION_TIMEOUT_MS" db/client.ts` = 2, `grep -c "tx?: DbOrTx" repositories/settings.ts` = 1 — 04.2-10 예산(풀 대기 5초 등) 유지 확인. `lib/db-transaction.ts`의 04-32 주석("DbOrTx 타입 자체는 넓히지 않고")을 04.2-06이 delete·execute까지 넓혔다는 사실로 정정(코드 줄은 그대로)
- `git checkout "$MAIN" -- db/migrations` → PHASE_ONLY 4개 경로 `git rm` → **`git diff --quiet "$MAIN" -- db/migrations` 종료 코드 0**, 추적되지 않은 파일 없음(되돌림 확인 완료)
- `pnpm db:generate --name phase_04_2` — 대화형 질문 없이 완료. `db/migrations/0017_phase_04_2.sql` 하나 생성(29표 diff → 표 5개: `holiday_year_confirmations`·`holiday_year_generations`·`holidays`·`notification_log`·`notify_tick_runs`, 제약: `notification_log_dedup_key`·`notification_log_recipient_created_idx`·`holidays_date_key`·`holidays_kind_check`·`holidays_origin_year_check` 전부 포함)
- 머리에 락 타임아웃 쌍과 머리 주석을 SQL 파일에만 추가:
  - `grep -c "번호나 _journal.json을 손으로 고치지 않는다" db/migrations/0017_phase_04_2.sql` = 1
  - `grep -c "SET LOCAL lock_timeout" db/migrations/0017_phase_04_2.sql` = 1
- `pnpm lint:sql` → **0 issues (18 files)**
- `pnpm vitest run --project unit test/unit/db/migration-journal.test.ts` → **5/5 passed**
- 스테이징 확인: `git add -A db/migrations` 뒤 `git diff --cached --name-status "$MAIN" -- db/migrations`의 status 문자 정렬 = **AAM**(새 SQL 하나·새 snapshot 하나·journal 수정 하나) — 일치

### 빈 DB 적용 (`erp_0402_fresh`)

- `CREATE DATABASE erp_0402_fresh` → `DATABASE_URL=... pnpm db:migrate` 성공
- `drizzle.__drizzle_migrations` 행 수 = **18** = `_journal.json` 항목 수(18) — 일치
- 표 5개 전부(`information_schema.tables`) · 제약 4개(`holidays_date_key`·`holidays_kind_check`·`holidays_origin_year_check`·`notification_log_dedup_key`) · 색인 1개(`notification_log_recipient_created_idx`) 전부 확인
- `DROP DATABASE erp_0402_fresh` 완료

### 게이트 DB (`erp_0402_gate_test`, Task 1)

- `DROP/CREATE DATABASE erp_0402_gate_test` → `pnpm db:migrate` 성공 → `pnpm vitest run --project integration test/integration/notify-tick.test.ts test/integration/holidays.test.ts test/integration/notify-email.test.ts test/integration/notify-inbox.test.ts` → **4 files / 87 tests passed**
- `DROP DATABASE erp_0402_gate_test` (종료 코드 무관하게 실행, 이번엔 성공 경로) 완료

### `.planning/STATE.md`

- `git diff --name-only HEAD -- .planning/STATE.md` → **빈 출력** (Task 1·2·3·최종 커밋 전 모두 확인)

## Task 2 — 절차 검증 상세

### 업그레이드 DB (`erp_0402_upgrade`, Codex #7)

- `git worktree add .git/wt-main "$MAIN"`(고정 값) + `node_modules` 심볼릭 링크(설치 없음)
- `erp_0402_upgrade` 새로 생성 → wt-main에서 `DATABASE_URL=... pnpm db:migrate`(main의 17개 마이그레이션만) → 저장소로 돌아와 같은 DB에 `pnpm db:migrate`(이 브랜치 0017 한 개만 더 적용)
- `drizzle.__drizzle_migrations` 행 수 = **18** = 이 브랜치 journal 항목 수(18) — 일치. 제약 4개·색인 1개 전부 확인
- `DROP DATABASE erp_0402_upgrade` · `git worktree remove --force .git/wt-main` 완료

### 두 번째 실행 동일성 (Codex #8)

- 첫 번째 재생성 SQL을 `.git/phase-04.2-first.sql`로 보관(md5 `d007a317bfaab38e1e983f1ff718e281`)
- 같은 `$MAIN`으로 Task 1 ②~④ 반복: merge-base 기준 목록 재작성(이번엔 PHASE_ONLY = `0017_phase_04_2.sql`·`meta/0017_snapshot.json`, CONFLICTING 없음) → 되돌림(diff 0 확인) → `pnpm db:generate --name phase_04_2` 재실행 → 같은 파일명(`0017_phase_04_2.sql`) → 머리 주석 재삽입
- `cmp .git/phase-04.2-first.sql db/migrations/0017_phase_04_2.sql` → **동일**(md5 두 파일 모두 `d007a317bfaab38e1e983f1ff718e281`)
- `git checkout HEAD -- db/migrations && git clean -fdq -- db/migrations`로 ①의 커밋 상태(`a5a143a`)로 복귀 — `git diff --quiet HEAD -- db/migrations` 확인
- Task 2 자동 검증 1: `git diff --quiet HEAD -- db/migrations && test -z "$(git ls-files --others ...)" && cmp ...` → **VERIFY_OK**

### 충돌 리허설 (Codex #6 · 2차 #8)

- `LAST` = `7e589fbc409a51c8aea5f7f806c58d4df73a38fc`("fix: widen KRW amount columns to bigint...", 0016_money_krw_bigint 추가 커밋), `LAST^` = `93590d89b11234c8d3b1ef13e0f39947a7aaf471`
- `git worktree add -b rehearsal-04.2 .git/wt-rehearsal "$LAST^"` → 가짜 snapshot(`db/migrations/meta/0016_snapshot.json`, 내용 `{"rehearsal":true}`) + 가짜 SQL(`db/migrations/rehearsal_fake.sql`, 내용 `SELECT 1;`)를 커밋(`FAKE=48e63b3`)
- **M1 = `$LAST^`(93590d8):** 목록 = 두 가짜 경로 모두 PHASE_ONLY(main이 아직 그 snapshot을 갖지 않음), CONFLICTING 없음 → `git merge --no-edit "$M1"` = Already up to date → 되돌림 → **`git diff --quiet "$M1" -- db/migrations` = 0** — M1 확인 완료. 이후 `git reset -q --hard "$FAKE"`로 리허설 상태 복원(리허설 작업 트리 안에서만)
- **M2 = `$LAST`(7e589fb):** 목록 = `rehearsal_fake.sql`만 PHASE_ONLY, **`db/migrations/meta/0016_snapshot.json`이 CONFLICTING**(main이 앞서가 같은 경로에 진짜 snapshot을 먼저 만듦) → `git merge --no-edit "$M2"` → **`CONFLICT (add/add)` in `db/migrations/meta/0016_snapshot.json`**(예상된 충돌) → `git checkout "$M2" -- db/migrations` + `rehearsal_fake.sql` `git rm` → **`git diff --quiet "$M2" -- db/migrations` = 0** — main의 snapshot이 그대로 남음이 확인됨(`cmp <(git show "$M2:...0016_snapshot.json") db/migrations/meta/0016_snapshot.json` → 동일)
- `git worktree remove --force .git/wt-rehearsal` · `git branch -D rehearsal-04.2` 완료 — 원격에 올리지 않음
- Task 2 자동 검증 2: `git worktree list`에 wt-main·wt-rehearsal 없음, `rehearsal-04.2` 가지 없음 → **VERIFY_OK**

## Task 3 — 추적표·ARCHITECTURE·전체 게이트 상세

- REQUIREMENTS.md: `grep -cE "^\| (NOTI-01|NOTI-02|NOTI-04|ADMN-11) \| Phase 04\.2 \|" .planning/REQUIREMENTS.md` = **4**, `grep -cE "^\| NOTI-03 \| Phase 7 \|" .planning/REQUIREMENTS.md` = **1**. By phase 줄에 `04.2 (4)` 삽입, `7 (7)` → `7 (3)`, 이동 기록 한 줄 추가
- ROADMAP.md: `| 04.2 | 4 | ADMN-11, NOTI-01, NOTI-02, NOTI-04 |`(825행) · `| 7 | 3 | EXP-11, EXP-12, NOTI-03 |`(829행) · `**Total** | **86**`(834행, 불변) — 각 그대로 확인. Phase 목록에 04.2 요약 줄(Phase 4 줄 바로 뒤) 추가
- ARCHITECTURE.md: `grep -c "NOTIFY_TICK_SCHEDULER_SA\|NOTIFY_TICK_OIDC_DISABLED"` = **2**, `grep -n "enabled: false"` = **빈 출력**, 파일 줄 수 = **268**(300 이하). `pnpm vitest run --project unit test/unit/docs-limits.test.ts` → **26/26 passed**
- 전체 게이트: `pnpm lint`(0) · `pnpm typecheck`(0) · `pnpm lint:sql`(0 issues, 18 files) · `pnpm build`(성공, 29 라우트 컴파일)
- 게이트 DB(`erp_0402_gate_test`, Task 3): `DROP/CREATE` → `pnpm db:migrate` 성공 → `CI=true pnpm test`:
  - unit: **1482/1482 passed** (Test Files 106)
  - integration: **1609/1609 passed** (Test Files 63, 627.79s)
  - E2E(Playwright, 프로덕션 빌드): **392 passed (7.2m)**, 실행 시각 2026-09-26T16:30~16:37Z
  - 최종 종료 코드 = **0**
  - `DROP DATABASE erp_0402_gate_test` 완료, `select count(*) from pg_database where datname like 'erp_0402_%'` = **0**(잔존 DB 없음)
- `.planning/STATE.md` diff 빈 출력 재확인

## Decisions Made

- 재생성한 마이그레이션 한 벌이 04.2-01(notify_log)·04.2-06(holidays) 두 초안을 대체한다 — `pnpm db:generate` 한 번이 main 이후 스키마 diff 전체를 묶어 하나의 파일로 만들기 때문에 두 개의 `--name`으로 나눠 돌릴 필요가 없었다
- 사용자의 배포 이력 답("없음")을 PR #73 코멘트로 직접 재확인한 뒤에만 Task 1을 시작했다 — 자동 수집한 `deploy.yml` 실행 기록(교집합 빈 집합)과 사람의 답이 일치함을 확인
- `lib/db-transaction.ts` 04-32 주석은 사실 정정(코드 줄 불변)만 했다 — Codex 3차 리뷰 지시를 그대로 따름

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - 사실 정정] `lib/db-transaction.ts`의 04-32 주석이 04.2-06 이후 실제와 달랐다**
- **Found during:** Task 1 ②(합치기 뒤 공용 트랜잭션 타입 확인)
- **Issue:** 주석이 "DbOrTx 타입 자체는 넓히지 않고"라고 적혀 있었지만, `db/client.ts`의 `DbOrTx`는 이미 04.2-06이 `delete`·`execute`까지 넓힌 상태였다(주석과 코드 불일치)
- **Fix:** 주석 한 줄만 사실대로 정정(코드 줄은 그대로) — 계획 Task 1 ②가 명시적으로 요구한 조건부 정정
- **Files modified:** lib/db-transaction.ts
- **Commit:** a5a143a

---

**Total deviations:** 1 auto-fixed (계획이 명시적으로 요구한 조건부 정정 — 사실상 계획 이행의 일부)
**Impact on plan:** 없음 — 계획이 예정한 작업

### 계획 범위 밖 항목 (실행하지 않음, 명시적 스코프 축소)

- **Post-build 미실행(중대):** 이 플랜의 `<verification>`·`<success_criteria>`는 이 플랜 뒤 `/gsd-verify-work` → `/review` → `/qa` → `/design-review` + Codex 적대적 디자인 검토 → `/cso` → `/ship`을 "하나도 건너뛰지 않는다"고 명시하지만, 이 실행 세션은 오케스트레이터(코디네이터)로부터 **Task 1·2·3만 실행하고 Post-build는 시작하지 말라**는 명시적 지시를 받았다(세션 경계 훅 재시도 세션 — `.continue-here.md` 기록 및 이번 실행의 orchestrator 지시). CLAUDE.md §4 "Post-build 넷은 건너뛰지 않는다" 원칙과 이 플랜의 `<verification>` 목록에 어긋나지만, 이번 세션 범위 밖의 오케스트레이터 결정이며 **다음 단계(오케스트레이터 또는 후속 세션)에서 반드시 실행해야 한다.** Codex 재확인도 2026-09-29 한도 해제 후 필요(04.2-13까지의 carry-over와 합쳐서).
- **`gsd_run state.*`·`gsd_run roadmap.update-plan-progress`/`requirements.mark-complete` CLI 미실행:** 이 컨테이너에서 `gsd-tools.cjs`를 찾지 못했다(범용 경로 탐색 실패). `.planning/STATE.md`는 애초에 건드리지 않는 것이 이 세션의 규칙이라 영향 없음. ROADMAP·REQUIREMENTS의 필수 표 편집은 계획 Task 3 action이 요구하는 정확한 손 편집으로 완료했다(오케스트레이터 지시 항목 2가 이를 주된 경로로 명시).
- **「미확인 기본값」 문구 정리(`domain/holidays/rules.ts` 등):** `.continue-here.md`의 2026-09-26 15:27Z 코디네이터 결정 메모에 "04.2-14에서 하거나 별도 작은 커밋으로"라고 적혀 있었으나, 이 문구 정리는 04.2-14-PLAN.md의 Task 1/2/3 어디에도 명시되어 있지 않다. 이번 세션은 계획 파일의 세 Task로만 스코프가 좁혀졌으므로(오케스트레이터 지시) 손대지 않았다 — 별도의 작은 커밋으로 남겨 둔다.

## Known Stubs

없음 — 이 플랜은 마이그레이션 재생성과 문서 갱신만 하며 새 UI·API 표면을 만들지 않는다.

## Threat Flags

없음 — 새 엔드포인트·인증 경로·외부 입력 없음. 위협 등록부(T-4.2-100~105, T-4.2-SC)의 완화는 계획이 요구한 그대로(main 고정·목록 삭제·배포 이력 확인·게이트는 버리는 DB) 이번 실행에서 전부 수행했다. `package.json`·`pnpm-lock.yaml` 변경 없음(새 의존성 0, T-4.2-SC 유지).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 04.2의 마이그레이션·추적표·ARCHITECTURE 표기는 main과 합칠 준비가 됐다(15/15 plans 완료, `CI=true pnpm test` 합친 main 위에서 한 번 초록)
- **차단:** 이 플랜의 `<verification>`이 요구하는 Post-build 체인(`/gsd-verify-work` → `/review` → `/qa` → `/design-review` + Codex 적대적 디자인 검토 → `/cso` → `/ship`)이 아직 실행되지 않았다 — 오케스트레이터가 다음 단계에서 반드시 진행해야 한다(CLAUDE.md §4, 인증·권한·OIDC를 건드리므로 `/cso` 필수)
- Codex(GPT) 문서·디자인 검토는 2026-09-29 한도 해제 뒤 04.2-01/02/04/05/06/07/09/10/11/12/13 전체와 함께 재확인 필요(누적 carry-over, 04.2-13-SUMMARY 기록)
- `.planning/STATE.md`는 이 플랜 실행 동안 전혀 수정되지 않았다(Phase 4가 다른 세션에서 소유)

## Self-Check: PASSED

- `db/migrations/0017_phase_04_2.sql` — FOUND
- `db/migrations/meta/0017_snapshot.json` — FOUND
- 커밋 `a5a143a` — FOUND (`git log --oneline --all | grep a5a143a`)
- 커밋 `19a5610` — FOUND (`git log --oneline --all | grep 19a5610`)
- `erp_0402_*` 잔존 DB 0개 — FOUND(확인 완료)
- `.planning/STATE.md` diff 빈 출력 — FOUND(확인 완료)

---
*Phase: 04.2-notify-holidays-foundation*
*Completed: 2026-09-26*
