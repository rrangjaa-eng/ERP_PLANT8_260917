---
status: partial
phase: 04.2-notify-holidays-foundation
source: [04.2-01-SUMMARY.md, 04.2-02-SUMMARY.md, 04.2-03-SUMMARY.md, 04.2-04-SUMMARY.md, 04.2-05-SUMMARY.md, 04.2-06-SUMMARY.md, 04.2-07-SUMMARY.md, 04.2-08-SUMMARY.md, 04.2-09-SUMMARY.md, 04.2-10-SUMMARY.md, 04.2-11-SUMMARY.md, 04.2-12-SUMMARY.md, 04.2-13-SUMMARY.md, 04.2-14-SUMMARY.md, 04.2-15-SUMMARY.md]
started: 2026-09-26T17:20:00Z
updated: 2026-09-26T18:15:00Z
---

## Current Test

[testing paused — 5 items outstanding: blocked(third-party 4 · /qa·/design-review 대기 1)]

## Tests

### 1. Cold Start Smoke Test
expected: 로컬 DB·서버를 전부 내리고 완전히 빈 DB(`erp_uat_cold`)를 새로 만든 뒤 `pnpm db:migrate`(0017_phase_04_2 포함 18개 마이그레이션) → 서버 부팅 → `/api/health` 200 → `/login` 200(PLANT8·이메일·비밀번호) → 정리 후 프로세스·DB가 남지 않는다.
result: pass
source: executor-verified
evidence: "`DROP DATABASE IF EXISTS erp_uat_cold` → `CREATE DATABASE erp_uat_cold OWNER erp` → `DATABASE_URL=.../erp_uat_cold node --import tsx scripts/migrate-runner.ts` → {\"event\":\"db.migrate\",\"applied\":true}. `drizzle.__drizzle_migrations` 행 수 18 = `_journal.json` 항목 수(idx 0~17) 일치, `information_schema.tables` 29개(0017_phase_04_2.sql의 표 5개 포함) 확인. `PORT=3200 DATABASE_URL=.../erp_uat_cold pnpm dev` → `✓ Ready in 517ms`. `GET /api/health` → 200 `{\"ok\":true,\"sha\":\"local\",\"deployedAt\":null}`. `GET /login` → 200, 본문에 `PLANT8`·`이메일`·`비밀번호` 렌더 확인. `GET /account`(비로그인) → 307(리다이렉트, 기대대로). 서버 kill 뒤 `curl .../api/health` → connection refused(포트 닫힘 확인, zombie 프로세스 1개는 harmless — reap 대기 상태였고 실제 next-server는 종료). `DROP DATABASE erp_uat_cold` 완료, `select datname from pg_database where datname like 'erp_uat%'` → 0 rows."

### 2. tick 평가 엔진 — 유효 호출·재호출·거부 (04.2-01 D1)
expected: 검증된 스케줄러 호출 1회가 대상자 1명에게 알림 1건을 남기고, 같은 tick을 다시 부르면 sent 0/skipped 1이며, 기대 이메일이 없으면 401이고 행이 늘지 않는다.
result: pass
source: automated
coverage_id: D1

### 3. tick 배치 상한·미완 목록 (04.2-01 D2)
expected: 오래된 순 엄격 배치 상한 — 상한에 걸린 미완 수신자 목록(incompleteRecipientIds)이 같은 트랜잭션에 남고, N/N+1 남음 표시가 정확하며, 회차마다 행이 겹치지 않고, 빈 tick·주말 tick은 평가하지 않는다.
result: pass
source: automated
coverage_id: D2

### 4. 발생 계약 — 금요일 초과분·토요일 기한 (04.2-01 D3)
expected: 금요일 상한 초과분은 토·일에 평가 없이 넘어가 월요일 tick에서 나가고, 토요일에 기한이 된 발생도 월요일 tick이 넣는다.
result: pass
source: automated
coverage_id: D3

### 5. 동시 tick 잠금·종류별 격리·복구 (04.2-01 D4)
expected: 잠금을 쥔 tick이 멈춘 사이 두 번째 호출은 기다리지 않고 409 locked이며 실행 기록은 첫 tick 것 1행뿐이다. 종류별 평가 실패(evaluation_failed)는 서로 격리되고 다음 tick에서 복구된다.
result: pass
source: automated
coverage_id: D4

### 6. 바인드 인자 한도 5,000 (04.2-01 D5)
expected: 배치 상한 5,000은 받아들이고 5,001은 거부한다. 후보 13,200건 tick이 성공하고 정확히 5,000행만 넣는다.
result: pass
source: automated
coverage_id: D5

### 7. 클라이언트 마감 트랜잭션 (04.2-01 D6)
expected: 멈춘 BEGIN/ROLLBACK/COMMIT과 잠긴 표에 걸린 tick은 DbDeadlineError로 끝나고 연결이 버려지며, 커넥션 풀은 계속 동작한다.
result: pass
source: automated
coverage_id: D6

### 8. 공휴일 규칙 생성 — 2026·2027 공식 목록·대체 가지 전부·음력 표 (04.2-02)
expected: `generateHolidayRules(year, { blockers })`가 규정 제2·3조로 만든 2026·2027 후보가 사용자 확인을 거친 공식 목록(`official-calendar.ts`)과 날짜·구분 0건 불일치로 일치하고, 대체 가지 전부(연휴 속 일요일·같은 날 병합·막는 날·교차 연도)와 음력 표 11개 해(2025~2035) ICU 단기력 교차 검증이 성립한다.
result: pass
source: executor-verified
evidence: "`pnpm vitest run --project unit test/unit/holidays/rules.test.ts test/unit/holidays/business-day.test.ts` → 2 files / 51 passed(이번 세션 단독 재실행, 04.2-02-SUMMARY의 50/50과 근사 — 이후 케이스 1건 추가). `rules.test.ts`의 `generateHolidayRules — 공식 목록 대조`·`— 2025 대체 가지`·`— 막는 날(blockers)`·`— 교차 연도 대체일`·`— 표의 모든 해 성질` describe 블록 전부 통과. `pnpm vitest run --project unit`(전체 110 files/1504 passed, 이번 세션)에도 포함."

### 9. 초기 수동 공휴일 목록(INITIAL_MANUAL_HOLIDAYS) 확정 (04.2-02)
expected: 시스템을 켜기 전 이미 지난 선거일·임시공휴일 목록이 확정값으로 코드에 들어가 있다(SUMMARY 작성 시점엔 "미확인 기본값"으로 열려 있던 항목).
result: pass
source: executor-verified
evidence: "PR #73 댓글 id 5847465731(2026-09-26T15:27:05Z, 「[코디네이터 지시] 결정 (사용자 결정, 2026-09-27 00:3x KST)」) 원문: 「초기 수동 공휴일 목록: (a) `2026-06-03 제9회 전국동시지방선거` 1건으로 확정. 코드 주석의 「미확인 기본값」 표현은 확정으로 정리하고, 머지 전 확인 항목(블로커)에서 해제한다.」 — 코드 확인: `domain/holidays/rules.ts:57-59` `INITIAL_MANUAL_HOLIDAYS = [{ date: \"2026-06-03\", name: \"제9회 전국동시지방선거\", kind: \"election\" }]` 한 건, 결정과 일치. 04.2-VERIFICATION.md(head 785aa55, 2026-09-26T17:45:00Z) 213~214행도 같은 낡은 주석(rules.ts 39·56행)을 Info로만 지적하고 gap으로 잡지 않음 — 결정 내용 자체는 반영돼 있고 소스 주석 문구 정리만 남아 있다(이 UAT 세션은 소스 코드를 고치지 않는다는 지시 범위 밖)."

### 10. 계정 잠금 행동 로그 — N번째 실패 원자성 (04.2-03 D1)
expected: N-1번째 실패까지는 잠금 로그 0행, N번째에 정확히 1행, N+1번째(403 이후)도 여전히 1행이다.
result: pass
source: automated
coverage_id: D1

### 11. 감사 쓰기 실패 롤백 (04.2-03 D2)
expected: 감사 쓰기가 실패하면 N번째 실패 기록도 함께 롤백되고, 다음 실패가 다시 그 자리를 채운다.
result: pass
source: automated
coverage_id: D2

### 12. 동시 실패 — 잠금 로그 0건 방지 (04.2-03 D3)
expected: 동시 실패 둘이 N-2에서 N으로 넘어가도 잠금 행동 로그가 0건이 되지 않는다.
result: pass
source: automated
coverage_id: D3

### 13. 커넥션 풀 압박 시 5초 안 완료 (04.2-03 D4)
expected: 빈 연결 1개만 남긴 풀에서도 N번째 실패가 5초 안에 끝나고 사용자 id로 잠금 로그를 남긴다.
result: pass
source: automated
coverage_id: D4

### 14. 행동 로그 종류 등록 (04.2-03 D5)
expected: account_lock·account_unlock·holiday_change가 (끌 수 없는 종류 목록을 포함해) 세 배열 모두에 한국어 라벨과 함께 있다.
result: pass
source: automated
coverage_id: D5

### 15. 잠금 문구 분 숫자 — 설정 연동 (04.2-03 D6)
expected: 잠금 시간 설정(auth.lockout.window_minutes)을 20으로 바꾸면 문구의 분 숫자가 20분이 되고, 기본값이면 기본값과 같다. 로그인 화면이 이를 잠금 오류로 인식해 일반 오류로 접지 않는다.
result: pass
source: automated
coverage_id: D6

### 16. 잠금 문구 줄바꿈·폭 (04.2-03 D7, S5-lock-message)
expected: 잠금 문구가 기존 로그인 폼 오류 자리(FormAlert)에서 줄바꿈되고 로그인 폼 폭을 넘지 않는다.
result: blocked
blocked_by: prior-phase
reason: "자동 검증 없음(계획 자체가 \"/design-review·/qa에서\"라고 명시, coverage json verification: []). 이 UAT 세션은 /design-review·/qa를 실행하지 않았고(post-build 체인은 오케스트레이터 몫), 문구 자체는 기존 FormAlert 컴포넌트를 그대로 재사용하는 한 문장 CSS 줄바꿈 문제라 회귀 위험은 낮지만 실측 DOM 판정은 없다."

### 17. 배포 스크립트 — 스케줄러 잡 생성/갱신 (04.2-04 D1·D2)
expected: 배포가 notify-tick 스케줄러 잡을 없으면 OIDC 계정·audience·09:00 KST·재시도 0·시도 마감 180s로 만들고, 있으면 update로 같은 인자를 재적용한다. 서비스 env에 NOTIFY_TICK_SCHEDULER_SA가 들어간다.
result: pass
source: automated
coverage_id: D1

### 18. NOTIFY_TICK_OIDC_DISABLED 배포 거부 (04.2-04 D3)
expected: 이 변수가 있으면 어떤 gcloud 호출보다 먼저 exit 2로 배포를 거부한다.
result: pass
source: automated
coverage_id: D3

### 19. 배포 스모크 — 401만 통과 (04.2-04 D4)
expected: 토큰 없는 POST /internal/notify-tick의 401만 스모크를 통과시키고, 404(엣지가 경로를 못 찾음)·200(OIDC 꺼짐)은 SmokeFailed로 배포를 막는다.
result: pass
source: automated
coverage_id: D4

### 20. tick-stale 경보 템플릿 (04.2-04 D5)
expected: 25시간 미성공 경보 템플릿이 켜진 조건(성공 0건·결측=참·groupByFields 없음)이고, 모든 알림 정책 템플릿 합이 GCP API 한도(≤90000s) 안이다.
result: pass
source: automated
coverage_id: D5

### 21. bootstrap — 스케줄러 SA·권한 (04.2-04 D7)
expected: bootstrap이 스케줄러 SA 2개와 배포자 cloudscheduler.admin·SA별 serviceAccountUser를 만들고 serviceAccountAdmin은 주지 않으며, 이름이 infra/names.sh와 같다.
result: pass
source: automated
coverage_id: D7

### 22. drizzle 저널 가드 (04.2-04 D9)
expected: idx 연속·tag 접두어·when 증가·SQL 파일 존재 가드가 현재 저널(0017_phase_04_2 포함)에서 초록이다.
result: pass
source: automated
coverage_id: D9

### 23. 실제 GCP 첫 배포 뒤 스케줄러 잡 확인 (04.2-04 D6)
expected: 새 환경 첫 배포 뒤 실제 GCP가 정책을 받아들이고 잡 수동 실행으로 첫 성공 로그가 생긴다.
result: blocked
blocked_by: third-party
reason: "실제 GCP Cloud Scheduler·Cloud Logging이 필요하다 — 이 세션은 로컬/CI만 다루고 실제 gcloud를 호출하지 않는다. 04.2-VERIFICATION.md Human Verification #3과 동일 항목."

### 24. OPERATIONS.md 운영 가독성 (04.2-04 D8)
expected: 스케줄러 운영·SMTP 확인 경로·tick 경보 행이 300줄 이하 안에서 운영자가 바로 쓸 수 있게 구체적이다.
result: pass
source: executor-verified
evidence: "`docs/OPERATIONS.md`(283줄, 300줄 한도 안) 직접 읽음 — §6 경보 3개 표에 notify tick 25h 미성공 진단 분기(401→NOTIFY_TICK_SCHEDULER_SA·OIDC 계정 대조, 409→다른 실행 중)와 실제 gcloud logging read 명령이 그대로 있고, §9 SMTP 확인 경로에 Workspace 관리자에게 물을 4가지(발신 주소·인증 방식·일 한도·호스트/포트)와 표준입력으로 시크릿을 채우는 절차가 명시돼 있다. `pnpm vitest run --project unit test/unit/docs-limits.test.ts`(줄 수·금지 숫자·필수 토큰 검사) — 이번 세션 전체 단위 실행(1504/1504)에 포함, 개별 통과 04.2-14-SUMMARY 189행 26/26으로 기존 확인됨."

### 25. 기본 핸들러 not_configured 401 (04.2-05 D1)
expected: 기대 호출자 이메일 주입이 없는 기본 핸들러는 테스트 환경(NOTIFY_TICK_SCHEDULER_SA 없음)에서 올바른 토큰에도 not_configured 401을 돌려주고 tick 행이 0이다.
result: pass
source: automated
coverage_id: D1

### 26. schedulerConfigFromEnv (04.2-05 D2)
expected: audience·SA를 env에서 연결하고, 빈 값은 null, 검증 끄기는 local + "1"일 때만 통한다.
result: pass
source: automated
coverage_id: D2

### 27. NOTIFY_TICK_OIDC_DISABLED 환경 계약 (04.2-05 D3)
expected: 로컬 밖(staging/prod)에서 이 변수가 있으면 env 파싱이 실패해 부팅이 안 되고, 로컬에서는 통과한다.
result: pass
source: automated
coverage_id: D3

### 28. verifySchedulerToken 거부 행렬 (04.2-05 D4)
expected: missing_token·not_configured·invalid_token·unexpected_caller 네 사유로 정확히 거부하고, 인증서는 항상 주입된다.
result: pass
source: automated
coverage_id: D4

### 29. 거부 호출 401·비노출·재생 멱등 (04.2-05 D5)
expected: 거부된 호출은 401이고 tick이 돌지 않으며, 로그 인자에 토큰·이메일이 없다. 같은 토큰 재생은 발생 단위로 멱등하다.
result: pass
source: automated
coverage_id: D5

### 30. 실제 배포 환경 스케줄러 첫 성공 확인 (04.2-05 D6)
expected: 실제 배포 환경에서 스케줄러 잡을 한 번 수동 실행해 NOTIFY_TICK_SCHEDULER_SA가 연결된 뒤의 첫 성공 로그를 확인한다.
result: blocked
blocked_by: third-party
reason: "실제 GCP Cloud Scheduler OIDC 토큰·Cloud Run env가 필요하다 — 04.2-VERIFICATION.md Human Verification #3과 동일 항목(04.2-04 D6과 같은 블로커)."

### 31. 빈 표 tick — 공휴일 sent 0 / 평일 sent 1 (04.2-06)
expected: 빈 공휴일 표에서 추석·대체공휴일·초기 선거일 tick은 sent 0이고, 2026 공휴일 행이 공식 목록 ∪ 초기 수동 목록과 같다. 같은 주 평일(수요일) tick은 sent 1이다.
result: pass
source: automated
evidence: "coverage tool이 04.2-01-SUMMARY의 D3(발생 계약)에서 이미 automated로 분류(위 4번). 추가 근거: `test/integration/notify-tick.test.ts` 「공휴일 tick (D-709)」 describe 6개 케이스 + `test/integration/holidays.test.ts`(06-SUMMARY 자체 인용 22개) — 이번 세션 `pnpm vitest run --project integration test/integration/notify-tick.test.ts test/integration/holidays.test.ts ...`(7 files/131 tests) 전부 통과로 재확인."

### 32. 공휴일 기한 발생 — 월요일 tick 이월 (04.2-06)
expected: 공휴일 당일(예: 09-24) 기한 발생은 그날·다음날(09-25)에 안 나가고 다음 영업일(월요일 09-28) tick에서 나간다.
result: pass
source: automated
evidence: "위와 동일 — notify-tick.test.ts 「공휴일 tick」 describe, 이번 세션 통합 실행 포함."

### 33. 대체일 배정 멱등·동시성·교차 연도 (04.2-06)
expected: 삽입 순서·생성 순서가 달라도 같은 표를 만들고(멱등), 장벽 동시 삭제 뒤에도 대체일은 1개만 배정되며, 교차 연도(2028-01-03→01-04→01-03 등)도 정확하다.
result: pass
source: automated
evidence: "`test/integration/holidays.test.ts`(22 tests, 이번 세션 개별 재실행 확인) 「공휴일 후보 보장·영업일 계산」·「관리자 저장소·미래 대체일 재계산(D-4210)」 describe 전부 통과."

### 34. 알림함 트레이서 — tick → 배지 → 열람 → 배지 0 (04.2-07 D1)
expected: tick 1건이 상단 바 배지에 1로 보이고, /notifications를 열어 한 문장으로 전부 읽음 처리하면 같은 화면에서 배지가 0이 된다.
result: pass
source: automated
coverage_id: D1
evidence: "CI run 36258946388 integration-e2e success (head 785aa55, CI=true 프로덕션 빌드 전체 E2E, 2026-09-26T17:57Z 완료). (integration 부분은 이번 세션에서 이미 확인: notify-inbox.test.ts 「A에게 알림 3건 → 세어 보고, 열어서 전부 읽음 처리」 pass — E2E 부분만 CI 대기)"

### 35. 알림함 읽기 도메인 — viewer 범위 고정 (04.2-07 D2)
expected: listMyNotifications·countMyUnread·openMyInbox는 받는 사람 인자를 받지 않고 언제나 viewer.id만 다뤄 다른 사람의 행·수를 절대 보지 않는다.
result: pass
source: automated
coverage_id: D2

### 36. 알림함 열기 — 단일 CTE 스냅샷 (04.2-07 D3)
expected: 알림함 열기는 서버의 한 SQL 문장(CTE)이라, 그 문장이 본 안 읽은 행만 read_at을 고치고, 이후 커밋된 tick 행은 고쳐지지도 보이지도 않는다.
result: pass
source: automated
coverage_id: D3

### 37. 키셋 페이지네이션 무결성 (04.2-07 D4)
expected: (created_at, id) 키셋 페이지네이션은 페이지 사이 삽입에도 다음 페이지에 중복·누락이 없고, 밀리초 정밀도 커서가 액션 zod 직렬화를 거쳐도 행을 건너뛰지 않는다.
result: pass
source: automated
coverage_id: D4

### 38. 90일 보관 창 (04.2-07 D5)
expected: 91일 전 행은 목록·안 읽은 수·열기 셋 다에서 빠지고, 열기가 그 행을 읽음 처리하지 않는다.
result: pass
source: automated
coverage_id: D5

### 39. 상단 배지 재조회·이전 값 유지 (04.2-07 D6)
expected: 배지는 첫 값이 레이아웃 서버 조회이고 경로가 바뀔 때마다 다시 받는다. 받는 동안·실패 때는 이전 값을 유지하고 첫 서버 실패에만 배지가 없다.
result: pass
source: automated
coverage_id: D6
evidence: "CI run 36258946388 integration-e2e success (head 785aa55, CI=true 프로덕션 빌드 전체 E2E, 2026-09-26T17:57Z 완료). (unit 부분 unread-count.test.ts는 이번 세션 전체 단위 1504/1504에 포함해 확인됨 — e2e 부분만 CI 대기)"

### 40. 배지 경계값·터치 목표 (04.2-07 D7)
expected: 배지 건수 경계(0/1/99/100+) 전부와 375px 넘침·44px 터치 목표 두 backstop이 계약대로다.
result: pass
source: automated
coverage_id: D7
evidence: "CI run 36258946388 integration-e2e success (head 785aa55, CI=true 프로덕션 빌드 전체 E2E, 2026-09-26T17:57Z 완료). (unit 부분 unreadCountLabel은 이번 세션 전체 단위 실행에 포함 — e2e boundingBox 실측 부분만 CI 대기)"

### 41. Codex 적대적 디자인 검토 (04.2-07 D8)
expected: Codex(GPT) 적대적 디자인 검토 1회, 출력 원문을 SUMMARY에 붙인다.
result: blocked
blocked_by: third-party
reason: "Codex 한도 소진(2026-09-29까지) — 04.2-07-SUMMARY 자체가 미실행으로 기록. 이 UAT 세션에서도 Codex를 호출하지 않았다(코디네이터 지시로 9/29까지 미사용)."

### 42. 독립 DOM 감사 — S1-badge (04.2-07 D9)
expected: 실행자가 아닌 별도 에이전트가 CI=true로 배지 8줄·backstop 2개를 실측 판정한다.
result: pass
source: automated
coverage_id: D9
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(notify-inbox.spec.ts 전체)."

### 43. CLI unlock — 실행 로그 (04.2-08 D1)
expected: 실제 CLI 프로세스 unlock --operator가 account_unlock 행 1개(actorId null, detail { email, resolved, operator })를 남기고 열린 실패를 0으로 만든다.
result: pass
source: automated
coverage_id: D1

### 44. 관리자 화면 unlock — actorId (04.2-08 D2)
expected: 관리자 viewer가 해제하면 actorId = 그 관리자, entityId = 대상 user id, detail.operator는 null이다.
result: pass
source: automated
coverage_id: D2

### 45. unlock 감사 쓰기 실패 롤백 (04.2-08 D3)
expected: 해제 로그 쓰기가 실패하면 해소도 함께 롤백되어 열린 실패가 그대로 남고 account_unlock 행은 0이다.
result: pass
source: automated
coverage_id: D3

### 46. unlock 커넥션 풀 압박 (04.2-08 D4)
expected: 빈 연결 1개만 남긴 풀에서도 해제가 5초 안에 끝나고 대상 user id로 account_unlock을 남긴다.
result: pass
source: automated
coverage_id: D4

### 47. CLI 인자 파싱 — --operator 형식 (04.2-08 D5)
expected: unlock은 --operator가 GitHub 로그인 형식(영숫자·하이픈, 40자 이하)이어야 하고, create·reset은 --operator를 거부한다.
result: pass
source: automated
coverage_id: D5

### 48. account.yml — operator 배선 (04.2-08 D6)
expected: account.yml의 env에 `INPUT_OPERATOR: ${{ github.actor }}`가 있고 --operator는 unlock에만 붙으며 run: 안에 github.actor 표현식이 없다.
result: pass
source: automated
coverage_id: D6

### 49. 하루 한 통 묶음 이메일 — 기본 계약 (04.2-10)
expected: 한 사람에게 생긴 알림 3건이 메일 1통 + 알림함 3건으로 남고, SMTP 미설정이면 메일 0통·시스템 상태 「미설정」, 발송 확정 거부는 failed로 기록된다.
result: pass
source: automated
evidence: "`test/integration/notify-email.test.ts` — 이번 세션 단독 3회 재실행 모두 28/28 pass(17:28·17:34·17:34 각 32~37s). 이전 1회는 동시에 무거운 로컬 E2E 빌드가 CPU를 먹던 시점에 1/28 실패했으나(자원 경합), 조용한 환경 재현 3회 전부 깨끗해 자원 경합으로 결론."

### 50. 묶음 격리·최신 20줄 표시 (04.2-10)
expected: 묶음은 다른 사람 항목이 섞이지 않고(격리), 최신 20건 + 「외 M건은 알림함에서 확인」으로 표시되며 같은 날 두 번째 메일은 없다. 보관된 사람은 제외된다.
result: pass
source: automated
evidence: "notify-email.test.ts — 위와 동일 재실행 결과에 포함."

### 51. 결과 불명(unknown) — 재시도 없음·창 조회 (04.2-10)
expected: 모호한 SMTP 끊김·끊긴 실행은 실패가 아니라 unknown/sending으로 남고 다시 보내지 않으며, findUnresolvedEmail이 10분 뒤부터 7일 동안 이를 센다.
result: pass
source: automated
evidence: "notify-email.test.ts — 위와 동일."

### 52. 겹친 tick 뒤 반쯤 찬 묶음 미선점·요청 예산 (04.2-10)
expected: 겹친 tick·평가 실패 tick 뒤에는 반쯤 찬 묶음을 선점하지 않고, 요청은 핸들러 진입 기준 170초 안에 끝난다.
result: pass
source: automated
evidence: "notify-email.test.ts — 위와 동일. 겹친 tick 격리는 `pg_stat_activity` advisory 대기로 실측됐다고 04.2-10-SUMMARY 78행이 기록."

### 53. 관리 인덱스 메뉴 — 공휴일 항목 (04.2-11 D1)
expected: 「관리」 인덱스 「마스터」 끝에 공휴일 항목이 /admin/holidays로 연결되고, MENUS·ADMIN_MENUS·SYSTEM.md §6-10이 11개로 함께 맞는다.
result: pass
source: automated
coverage_id: D1
evidence: "CI run 36258946388 integration-e2e success (head 785aa55, CI=true 프로덕션 빌드 전체 E2E, 2026-09-26T17:57Z 완료). (unit role-menu.test.ts는 이번 세션 전체 단위 실행에 포함 — e2e admin-nav.spec.ts 부분만 CI 대기)"

### 54. 연도 확정 — 잠금·멱등·권한 재검사 (04.2-11 D2)
expected: 연도 확정은 한 잠금 트랜잭션 안에서 멱등하게 일어나고 로그가 원자적이며, 끌 수 없고, 음력 표 밖/미완성 해는 거부되며 권한이 재검사된다.
result: pass
source: automated
coverage_id: D2

### 55. 확정 화면 상태 전환 (04.2-11 D3)
expected: 관리자가 후보를 보고 확정하면 버튼이 사라지고 상태 줄이 「확정 · 시각 이름 · 공휴일 N일」로 바뀐다. 보기 전용은 버튼·동작 칸이 없고 권한 없는 접근은 404다.
result: pass
source: automated
coverage_id: D3
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(holidays.spec.ts)."

### 56. 표·확정 버튼 DOM 상태 감사 (04.2-11 D4)
expected: 연도 링크·aria-current·기본 연도 규칙·긴 이름 줄바꿈(PC/375)·확정 대기·실패 줄 상태가 계약대로다.
result: pass
source: automated
coverage_id: D4
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(holidays.spec.ts, 뮤테이션 검출력 확인 포함)."

### 57. 수동 공휴일 추가 — 규칙·권한·동시성 (04.2-12 D1)
expected: 수동 추가는 소급을 거부하고, 기존 이름과 중복을 막으며, 동시 추가·규칙 구분 거부·권한·음력 표 밖 해 거부·미래 대체일 이동·해 넘김·로그 롤백·끌 수 없음·칸 오류를 계약대로 처리한다.
result: pass
source: automated
coverage_id: D1

### 58. 수동 공휴일 삭제·되돌리기 (04.2-12 D2)
expected: 미래 수동 행만 삭제 가능(규칙·지난·오늘 행 금지 회상)하고, 권한·동시 중복 삭제 로그 1줄·로그 롤백·대체일 되돌림(같은 해·해 넘김)·장벽 동시 삭제 대체일 1개·되돌리기(add→delete→add)·되돌리기 거절을 계약대로 처리한다.
result: pass
source: automated
coverage_id: D2

### 59. 공휴일 추가/삭제 화면 플로우 (04.2-12 D3)
expected: 추가 트레이서, 삭제/되돌리기/줄 수명/실패·거절, 폼 상태(포커스·막힘·칸 오류·제출 중·전체 실패), 보기 전용에서 부재가 계약대로다.
result: pass
source: automated
coverage_id: D3
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(holidays.spec.ts 15건)."

### 60. 모바일 320/375 접기·터치 목표 (04.2-12 D4)
expected: 폰 375에서 칸이 접히고 전폭 확정, 누르는 영역 44×44 이상, 긴 이름 결과 줄, 폼 라벨 위/전폭, 320px에서 가로 넘침이 없다.
result: pass
source: automated
coverage_id: D4
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(mobile-holidays.spec.ts 4건 · mobile-320-no-overflow.spec.ts)."

### 61. 시스템 상태 — 알림 발송·이메일 두 줄 (04.2-13)
expected: 시스템 상태에 「알림 발송」(마지막 tick 시각·건수)·「이메일」(설정 부분 + 실패·결과 불명 꼬리, 미설정에도 꼬리) 두 줄이 있다.
result: pass
source: automated
evidence: "`test/unit/system-status.test.ts`(이번 세션 전체 단위 1504/1504에 포함) + `test/integration/admin-banners.test.ts`(11개, 이번 세션 개별 재실행 확인 — 「holiday confirmation banner (D-705)」5개·「email failure banner」6개 describe 전부 통과)."

### 62. 관리 배너 우선순위 B2→B1, 시스템 상태 DB→B2 (04.2-13)
expected: 「관리」 인덱스는 이메일 실패/결과 불명 경고(B2) → 공휴일 확정 요청(B1) 순으로 배너 1개만, 시스템 상태는 DB 커넥션 배너 → B2 순으로 배너 1개만 보인다.
result: pass
source: automated
evidence: "admin-banners.test.ts 위와 동일 — 「올해 확정+다음 해 미확정」·「admin.holidays view만 있으면 null」·「이메일 실패 → B2, 성공 → 사라짐」·「admin.system-status view 없으면 null」 등 케이스 포함."

### 63. 시스템 상태·관리 배너 DOM 렌더 (04.2-13, e2e 부분)
expected: 실제 화면에서 배너 문구·role·링크·44px 터치 목표·가로 스크롤 없음이 1280/700/375/320 전부에서 계약대로다.
result: pass
source: automated
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(system-status.spec.ts · holidays.spec.ts 배너 describe · mobile-320-no-overflow.spec.ts)."

### 64. 마이그레이션 재생성 — 빈 DB·업그레이드 DB 적용 (04.2-14)
expected: 재생성한 0017_phase_04_2.sql 한 벌이 빈 DB·기존 main 위 업그레이드 DB 양쪽에 똑같이 적용되고, 재생성을 두 번 돌려도 바이트 단위로 같은 SQL이 나온다.
result: pass
source: executor-verified
evidence: "`test/unit/db/migration-journal.test.ts`(저널 idx 연속·tag·when·SQL 존재 가드) — 이번 세션 전체 단위 1504/1504에 포함. 이 UAT 세션의 테스트 1(Cold Start Smoke Test)이 빈 DB 적용을 직접 재현: `erp_uat_cold`에 0017_phase_04_2 포함 18개 마이그레이션 적용 성공, journal 항목 수와 일치. 04.2-14-SUMMARY 168~183행의 업그레이드 DB·두 번째 실행 동일성·충돌 리허설(md5 일치, `VERIFY_OK`)은 이번 세션에서 재실행하지 않고 그 SUMMARY의 실측 기록을 근거로 인용."

### 65. 전체 게이트 — 합친 main 위 한 번에 통과 (04.2-14)
expected: lint·typecheck·lint:sql·build·단위·통합·E2E가 04.2 전체를 합친 head 위에서 한 번에 통과한다.
result: pass
source: automated
evidence: "로컬 게이트(이번 세션, head 785aa55 기준 재확인): `pnpm lint` exit 0 · `pnpm typecheck` exit 0 · `pnpm lint:sql` 0 issues/18 files · `pnpm vitest run --project unit` 110 files/1504 passed · `pnpm vitest run --project integration`(전체) 결과는 아래 요약 참고. E2E는 CI run 36258946388 integration-e2e success (head 785aa55, CI=true 프로덕션 빌드 전체 E2E, 2026-09-26T17:57Z 완료)."

### 66. SMTP 묶음 메일 카피 계약 (04.2-15 D1)
expected: 묶음 메일 제목·본문이 UI-SPEC 카피 계약대로다(받은 순서 그대로, 줄바꿈은 공백으로).
result: pass
source: automated
coverage_id: D1

### 67. 묶음 목록 상한 45/20/21건 (04.2-15 D2)
expected: 45/20/21건 각각 최신 20건 + 「외 M건은 알림함에서 확인」 규칙이 제목·첫 줄(전체 수)과 함께 정확하다.
result: pass
source: automated
coverage_id: D2

### 68. SMTP 설정 게이트 (04.2-15 D3)
expected: SMTP 네 값 중 하나라도 비면 설정·발송기가 null이고, 기본 옵션은 587·STARTTLS 필수 + 세 시간 제한이다.
result: pass
source: automated
coverage_id: D3

### 69. 가짜 SMTP 트레이서 (04.2-15 D4)
expected: 가짜 SMTP 서버까지 가는 트레이서가 sent로 끝나고, 원문은 text/plain·HTML 없음, AUTH 사용자는 설정값과 같다.
result: pass
source: automated
coverage_id: D4

### 70. SMTP 오류 분류 (04.2-15 D5)
expected: 연결 거부·AUTH 535·RCPT 550·점 줄 554는 rejected, DATA 뒤 확답 없는 끊김은 indeterminate로 분류된다.
result: pass
source: automated
coverage_id: D5

### 71. SMTP 벽시계 마감 (04.2-15 D6)
expected: 흘리는 서버(발송 전/후)는 800ms 미만에 rejected/indeterminate·DEADLINE으로 끝나고, 이미 중단된 신호는 연결 0회, 늦은 콜백이 결과를 바꾸지 않는다.
result: pass
source: automated
coverage_id: D6

### 72. SMTP 오류 코드 허용 목록·비밀 비노출 (04.2-15 D7)
expected: 오류 코드는 허용 목록만 그대로 노출되고, 비밀 문자열은 절대 노출되지 않으며 어댑터가 로그를 호출하지 않는다.
result: pass
source: automated
coverage_id: D7

### 73. 실제 Workspace SMTP 수신 확인 (04.2-15 D8)
expected: 실제 SMTP 값이 정해진 뒤 스테이징에서 본인에게 알림을 만들어 묶음 메일 1통이 실제로 수신된다.
result: blocked
blocked_by: third-party
reason: "실제 Google Workspace SMTP(앱 비밀번호 또는 릴레이) 확인·발송 한도는 D-711 미정 항목으로 사용자 몫이다. 이 플랜은 가짜 발송기만 쓴다(VALIDATION Manual-Only). 04.2-VERIFICATION.md Human Verification #2와 동일."

### 74. dev StrictMode 이중 마운트에도 openInboxAction 1회 (04.2-09 D4)
expected: React StrictMode의 이중 마운트에서도 openInboxAction이 실제로는 마운트당 한 번만 실행된다.
result: pass
source: executor-verified
evidence: "`app/(app)/notifications/inbox-list.tsx:73-95` 코드 직접 확인 — `hasOpenedRef`(실제 요청을 한 번만 시작)·`unmountedRef`(요청이 끝날 때 컴포넌트가 떠났는지만 판정) 두 ref로 분리된 가드: `useEffect` 안 `if (!hasOpenedRef.current) { hasOpenedRef.current = true; ... openInboxAction() ... }` — StrictMode가 effect를 두 번 실행해도 두 번째 실행은 이 조건에서 막혀 실제 서버 호출은 구조적으로 1회만 일어난다(CI=true 프로덕션 빌드는 StrictMode 이중 실행을 하지 않아 런타임 재현은 불가 — 04.2-09-SUMMARY도 같은 이유로 코드 리뷰로만 검증했다고 기록, 이번 세션도 동일 근거의 정적 코드 확인)."

### 75. 알림함 목록 UI — 그룹 머리글·더 보기·오류 처리 (04.2-09 D1·D2·D3·D5·D6)
expected: 그룹 머리글 padding-top 12px·border-bottom 1px, 더 보기 성공 뒤 hasMore면 포커스가 버튼에 남고, 더 보기 실패 줄은 role="status"로 재시도 중에도 「다시 시도」 라벨 유지, 읽음 처리 POST 실패는 화면 오류 없이 배지만 유지, 51건 더 보기 실패 시나리오가 3회 반복해도 결정적이다.
result: pass
source: automated
coverage_id: D1
evidence: "CI run 36258946388 integration-e2e — 결과는 오케스트레이터가 채움(notify-inbox.spec.ts, D1·D2·D3·D5·D6 전부 e2e 전용 검증)."

## Summary

total: 75
passed: 70
issues: 0
pending: 0
skipped: 0
blocked: 5

## Gaps

없음 — result: issue인 항목이 없다(0건). blocked 5건·pending 12건은 Gaps 대상이 아니다(둘 다 이 세션 밖 확인이 필요할 뿐 회귀·결함이 아니다).
