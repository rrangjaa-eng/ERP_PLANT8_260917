---
context: phase
phase: 04.3-qr-certificate-intake
task: 0
total_tasks: 13
status: paused
last_updated: 2026-09-25T00:35:06.898Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: D-01 한 세션 한 관문. 직전 세션은 설계 `/cso` 지적 반영을 끝냈다(862f237). 다음 세션은 **`/review`만** 한다(계획 문서 대상, 커밋·푸시·인계까지). 그 뒤 draft PR 준비 완료 표시.
- [ ] CONSTRAINT: Codex 사용 한도(2026-09-29 07:13 전후 재개). 교차 검토는 Opus 독립 검토, Fable은 페이즈당 최종 전체 검토 한 번만. 모든 검토 기록에 "Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요". `codex` 호출 금지.
- [ ] CONSTRAINT: 이 페이즈는 계획만. 실행 금지. 실행 시작은 Phase 4(PR #40)가 main에 머지된 뒤(E3-01). STATE.md 현재 페이즈를 바꾸지 않는다. `.planning/`은 GSD 도구·에이전트로만 고친다. 공용 `.planning/HANDOFF.json`은 Phase 4 것이라 덮어쓰지 않는다.
- [ ] CONSTRAINT: 모든 답·상태·카드는 한국어, 시각은 KST.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 게이트 리뷰 세션에서 반영 시작 | 리뷰 뒤 같은 세션에서 gsd-planner를 띄우면 세션 경계 훅에 막힘 | advisory | 리뷰 세션은 보고서 커밋·푸시·인계까지만 |
| 병렬 플래너 간 문구 어긋남 | 샤드끼리 수치·문구를 다르게 적을 위험 | advisory | 공통 brief에 고정 계약(수치·HMAC 키·전제 문장)을 박고 샤드는 인용만(/mnt/project-files/04.3-gates/cso-apply-brief.md) |
| 교차 검토가 잡은 실행 시 깨짐 | 1회차에 KMS 권한 축소 뒤 deploy.sh 확인 명령이 권한 거부(B4), 시드 변경이 앞 웨이브 통합 테스트를 깸(B5) | advisory | 권한·시드를 바꾸는 반영은 그 권한/값을 전제로 한 다른 플랜·스크립트를 grep으로 모두 찾아 대조 |
| 검사기의 외부 파일 오판 | 검사기가 Phase 4 04-49 플랜이 없다고 했으나 origin/claude/gsd-progress-e1nzgu에 있음 | advisory | Phase 4 파일은 반드시 `git show origin/claude/gsd-progress-e1nzgu:<path>`로 확인 |

<current_state>
설계 `/cso` 지적 반영 완료. 브랜치 `claude/plan-phase-04-3-9wian4`, draft PR #74, 반영 커밋 862f237.
- 반영 대상: 04.3-02·03·04·06·07·08·09·10·11·12·13 + UI-SPEC(04는 교차 B5·tooManyWinners 때문에 추가).
- 검토: Opus 검사기 r1(막는 0·경고 1) → r2 통과, Opus 교차 r1(막는 5) → r2(막는 1) → r3 「막는 문제 없음」. 기록 `/mnt/project-files/04.3-gates/cso-apply-{brief,checker-r1,checker-r2,cross-r1,cross-r2,cross-r3}.md`.
- 13개 플랜 모두 `verify.plan-structure` valid, 오류·경고 0.
</current_state>

<completed_work>
- 계획 13개, UI-SPEC 승인, 최종 전체 검토 1~4회차, `/plan-eng-review` + 반영, `/plan-design-review` + 반영, 설계 `/cso`(184df28)
- 설계 `/cso` 반영 + Opus 검사기 2회·교차 3회(862f237, 이번 세션)
</completed_work>

<remaining_work>
1. **다음 세션: `/review`**(계획 문서 전체 대상) → draft PR 준비 완료 표시.
2. Codex 한도 풀리면(9/29): 최종 4회차, 엔지니어링 검토 P1·P2와 반영 1·2회차, 디자인 검토(D-3·D-7·D-10), 디자인 반영 B-1·B-4·N-4, 설계 `/cso` 리포트 전체와 이번 반영(cso-apply r1~r3 대상)을 Codex로 재확인.
3. 남은 참고(막지 않음, 교차 r3): R3-1(06:190 ⓓ 재현 방법), R3-4(06:216·:191 표현), R3-5(UI-SPEC:261 `block` 뜻), R3-6(02:472 상수 grep이 import 줄에도 맞음). `/review` 때 함께 보면 된다.
</remaining_work>

<decisions_made>
- (기본값, 사용자 확인 대기) E3-03 서명 이미지 GCS 유지, E3-04 12·08 범위 현 계획 유지. 바꾸지 않았다.
- E3-10 비활동 120분 유지(법무 확인 필요).
- `cert.setup`은 박서연이 속한 시스템 관리자 계급에 세 권한을 명시적으로 켠다(공유 계급 변경 예외 사유는 02 ledger).
- tooManyWinners 문구는 새로 만들지 않고 I2 문구 「당첨자 {N}명 · 500명까지 줄이기」 재사용(UI-SPEC I3), 요청의 새 줄 전체에 표시.
- 09 verify는 cert-events.test.ts만, 07 회귀는 13의 `CI=true pnpm test`가 덮는다.
- IP 해시: 순수 함수 `certIpHash(secret, eventId, ip)`, 메시지 `eventId:ipKey(ip)`(IP 없으면 "unknown"), ipKey는 IPv4 원문·IPv4-mapped는 IPv4로·IPv6 /64·그 밖은 원문.
- 이전 세션 결정(D-10 폭 판정, 색 규칙 X-4, DR-5, 미룸 X-6·N-10·N-12·N-13)은 그대로.
</decisions_made>

<blockers>
- Codex 한도(외부): 9/29 전후까지, Opus 대체 중.
- Phase 4(PR #40) main 머지 전에는 실행 불가(E3-01).
- 사람 확인: 법무(120분·파기 기산일·동의 전문), 소유자(취급자 지정, 머지 전 bootstrap 실행으로 KMS 권한 정리 확인).
</blockers>

## Required Reading (in order)
1. 이 파일
2. `docs/designs/plant8-erp-phase04.3-cso-design-260925.md` — 이번에 반영한 판정
3. `/mnt/project-files/04.3-gates/cso-apply-cross-r3.md` — 마지막 교차 결과와 남은 참고

## Infrastructure State
- 작업 트리 깨끗함(이 파일 커밋 전 기준).

<next_action>
Start with: 새 세션에서 `/review`로 04.3 계획 문서(862f237 기준) 검토 → 보고서 커밋·푸시·인계. Codex 대신 Opus 교차, 한도 풀리면 Codex 재확인 필요.
</next_action>
