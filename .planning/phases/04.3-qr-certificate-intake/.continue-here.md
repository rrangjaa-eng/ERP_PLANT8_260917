---
context: phase
phase: 04.3-qr-certificate-intake
task: 0
total_tasks: 13
status: paused
last_updated: 2026-09-24T21:03:47.537Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: D-01 한 세션 한 관문. 직전 세션은 `/plan-eng-review`만 했다. 다음 세션은 엔지니어링 검토 지적 반영(`/gsd-plan-phase 04.3` 수정 모드 + 플랜 체커)만 한다. 끝나면 인계 → 새 세션에서 `/plan-design-review` → 그다음 새 세션에서 설계 `/cso`.
- [ ] CONSTRAINT: Codex 사용 한도(2026-09-29 07:13 전후 재개). 사용자 결정 「Opus」: 한도 동안 교차 검토는 Opus 독립 검토, Fable은 페이즈당 최종 전체 검토 한 번만. 모든 검토 기록에 "Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요"를 남긴다. `codex` 호출 금지.
- [ ] CONSTRAINT: 이 페이즈는 계획만. 실행(`/gsd-execute-phase`) 금지. 실행 시작은 Phase 4(PR #40)가 main에 머지된 뒤(E3-01). STATE.md 현재 페이즈를 바꾸지 않는다. `.planning/`은 GSD 도구·에이전트로만 고친다. 공용 `.planning/HANDOFF.json`은 Phase 4 것이라 덮어쓰지 않는다(04.3 인계는 이 파일만).
- [ ] CONSTRAINT: 모든 답·상태·카드는 한국어, 시각은 KST.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 검토 약속만 하고 실행 안 됨 | 긴 작업자가 시작 못 하고 멈춘 사례(04-28) | blocking | 작업자·긴 명령을 띄운 뒤 몇 분 안에 결과 파일이 생기고 커지는지 확인, 아니면 다시 띄움 |
| 컨테이너 재시작으로 작업자 중단 | 이번 세션에서 1차 검토 작업자 3개가 재시작으로 멈춤(결과 파일은 이미 완성돼 있었음) | advisory | 작업자가 결과를 파일에 틈틈이 저장하게 하고, 재시작 뒤 파일 끝(부록 섹션 유무)으로 완성 여부 판단 |
| 게이트 기록 커밋 차단 | `.claude/gates/phase-04.log` 변경 커밋이 자동 분류기에 막힘 | advisory | 훅이 쓴 게이트 기록은 사용자가 허용할 때까지 작업 트리에만 둔다(우회 금지) |

<current_state>
Phase 04.3 「QR 확인증 접수」 `/plan-eng-review`가 끝났다. 브랜치 `claude/plan-phase-04-3-9wian4`, 보고서 커밋 953e599 (`docs/designs/plant8-erp-phase04.3-eng-review-260925.md`).
- 지적 35건: P1 2 · P2 13 · P3 20, critical gap 1(E3-16 파기 Job 누락 시 알림 없음). Eng Review 상태 ISSUES OPEN.
- 1차 검토 Opus 4명(A 01·02·13 / B 03·04·05 / C 06·07·08 / D 09~12), 교차 검토 Opus 1명(Codex 대체). 원본: `/mnt/project-files/04.3-gates/eng-first-pass-{A,B,C,D}.md`, `eng-outside-opus.md`.
- 교차 검토 P1 4건 판정: E3-01 Phase 4 공통 파일 충돌(유지), E3-07 스테이징 게이트·부트스트랩 순서(유지), E3-08 행사 한도(P2 강등), E3-33 병렬 테스트 DB 공유(기각, 훅 D-04로 실행이 직렬).
- 사용자 수면 중이라 33건은 추천 기본값 자동 적용. 사용자 결정 2건(E3-03 서명 저장 위치, E3-04 12·08 범위)은 기본값 「현 계획 유지」로 진행.
- `.claude/gates/phase-04.log`에 plan-eng-review 한 줄이 추가됐으나 커밋은 분류기가 막아 작업 트리에만 있다.
</current_state>

<completed_work>
- 계획 13개, UI-SPEC 승인, 최종 전체 검토 1~4회차(이전 세션)
- `/plan-eng-review` 보고서 작성·커밋·푸시(이번 세션)
</completed_work>

<remaining_work>
1. 새 세션: 보고서 「## 다음 세션 반영 지시」(보고서 387행부터)를 `/gsd-plan-phase 04.3` 수정 모드로 반영 → 플랜 체커(Opus) + 교차 확인(Opus, Codex 대체).
2. 그다음 새 세션: `/plan-design-review`(UI-SPEC 있음, 필수).
3. 그다음: 설계 `/cso`(인증·암호화·외부 입력) → `/review` → draft PR.
4. Codex 한도가 풀리면(9/29) 최종 4회차와 이번 엔지니어링 검토 P1·P2를 Codex로 재확인.
</remaining_work>

<decisions_made>
- (이전) qrcode 의존성 승인, 누적 잠금 20회·담당자만 해제, 262,144 B 한도(T-04.3-182), 08 v1 키 유지, deploy.sh는 KMS 버전을 만들지 않음.
- (이번, 자동 기본값) 실행은 Phase 4 main 머지 뒤. 스테이징에만 기능 게이트 허용·스테이징 스모크 추가·bootstrap은 머지 전 PR 브랜치에서. 행사 한도는 P2로 로그·명단 비례 한도 추가(수치는 `/cso`). tx 로그 도우미 하나로 합침. 나머지는 보고서 Decision ledger 참고.
- (사용자 대기) E3-03 서명 저장 GCS 유지(기본값) 대 DB bytea, E3-04 12·08 범위 현 계획 유지(기본값) 대 축소·분리.
</decisions_made>

<blockers>
- Codex 한도(외부): 9/29 전후까지, Opus 대체 중.
- Phase 4(PR #40) main 머지 전에는 실행 불가(E3-01).
</blockers>

## Required Reading (in order)
1. `docs/designs/plant8-erp-phase04.3-eng-review-260925.md` — 「다음 세션 반영 지시」, 「Decision ledger」, 「사용자 확인 필요(아침)」
2. `04.3-CONTEXT.md` — 잠긴 결정 D-11xx
3. 수정할 플랜만 범위 Read(02·03·04·06·07·09·10·13 위주)

## Critical Anti-Patterns (do NOT repeat these)
- 전체 E2E를 필터 없이 돌리는 검증 명령 → `--no-deps --workers=1 --project=...`
- 한 세션에서 관문 둘 이상 → D-01 훅이 막는다

## Infrastructure State
- 실행 중인 서비스 없음.

## 소유자가 할 설정 (실행 전)
- 배포 SA의 KMS 역할 확인(문서마다 현황이 다름 — E3 보고서 참고), `scripts/bootstrap-gcp.sh`를 **머지 전에** 다시 실행.
- v2 키 교체 전에 KMS 새 primary 버전 만들기.

<context>
계획 품질은 여러 회차 외부 검토로 수렴했다. 엔지니어링 검토는 주로 Phase 4와의 공통 파일 기준 맞추기, 스테이징 확인 경로, 몇몇 테스트·에러 처리 공백을 짚었다. 범위 밖: `domain/projects/index.ts:258` 동시 생성 교착(다른 스레드 담당).
</context>

<next_action>
Start with: 새 세션에서 `/gsd-progress`로 재개 → `/gsd-plan-phase 04.3` 수정 모드로 엔지니어링 검토 지적 반영(체커·교차 확인 모두 Opus, 기록에 "한도 풀리면 Codex 재확인 필요").
</next_action>
