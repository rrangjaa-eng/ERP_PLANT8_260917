---
context: phase
phase: 04.3-qr-certificate-intake
task: 0
total_tasks: 13
status: paused
last_updated: 2026-09-25T01:51:47.181Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: D-01 한 세션 한 관문. 직전 세션은 계획 문서 `/review` 지적 반영을 끝냈다(e3202fe). 이 페이즈의 계획 관문은 모두 끝났다 — 남은 것은 머지 준비와 Codex 재확인뿐이다.
- [ ] CONSTRAINT: Codex 사용 한도(2026-09-29 07:13 전후 재개). 그 전에는 `codex` 호출 금지. 모든 검토 기록에 "Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요".
- [ ] CONSTRAINT: 이 페이즈는 계획만. 실행 금지. 실행 시작은 Phase 4(PR #40)가 main에 머지된 뒤(E3-01). STATE.md 현재 페이즈를 바꾸지 않는다. `.planning/`은 GSD 도구·에이전트로만 고친다. 공용 `.planning/HANDOFF.json`은 Phase 4 것이라 덮어쓰지 않는다.
- [ ] CONSTRAINT: 모든 답·상태·카드는 한국어, 시각은 KST.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 게이트 리뷰 세션에서 반영 시작 | 리뷰 뒤 같은 세션에서 gsd-planner를 띄우면 세션 경계 훅에 막힘 | advisory | 리뷰 세션은 보고서 커밋·푸시·인계까지만 |
| 병렬 샤드가 작업 트리를 건드림 | 샤드 C가 기준 확인용으로 `git stash`/`pop`을 잠깐 돌려 다른 샤드의 미커밋 편집이 1초 사라졌다(복구됨) | advisory | 샤드 지시에 「git stash 금지」를 넣는다 |
| 검토 전제 오류 | 적대 검토가 「잠금 대기 시간 제한 없음」이라 했지만 Phase 4 `withTransaction`에 `lock_timeout` 5초가 이미 있다 | advisory | 검토 전제는 `git show origin/claude/gsd-progress-e1nzgu:<path>`로 먼저 확인 |

<current_state>
계획 문서 `/review` 지적 반영 완료. 브랜치 `claude/plan-phase-04-3-9wian4`, draft PR #74, 반영 커밋 e3202fe.
- 반영: P1 1 · P2 6 · P3 19(요약표는 18이라 적었지만 본문 기준 19). id 표 `/mnt/project-files/04.3-gates/review-apply-brief.md`.
- 검증: Opus 검사기 1회차(경고 1) → 2회차 PASSED · Opus 교차 1회차 「막는 문제 없음」(참고 9) → 2회차 「막는 문제 없음」. 보고서 `/mnt/project-files/04.3-gates/review-apply-{checker,cross}-r{1,2}.md`.
- 13개 플랜 모두 `verify.plan-structure` valid(오류·경고 0).
</current_state>

<completed_work>
- 계획 13개, UI-SPEC 승인, 최종 전체 검토 1~4회차, /plan-eng-review·/plan-design-review·설계 /cso와 각 반영
- 계획 문서 /review(1d927bd)와 그 반영(e3202fe)
</completed_work>

<remaining_work>
1. 머지 준비: 문서 PR이라 /qa 대신 사용자가 GitHub에서 Squash and merge. 머지 전 소유자가 `scripts/bootstrap-gcp.sh`를 다시 돌려 KMS 권한을 정리(main 푸시가 스테이징 자동 배포).
2. Codex 한도 풀리면(9/29): 최종 4회차, 엔지니어링·디자인 검토와 반영, 설계 /cso 반영, /review와 그 반영(e3202fe)을 Codex로 재확인.
3. 남은 참고(막지 않음): 02:454 순서 검사는 deps 타입을 함수 머리에 인라인으로 쓰면 첫 줄에 걸릴 수 있음(동작 테스트 02:376이 잡음) · 08:225 grep은 `let keys` 꼴을 못 잡음(resetModules 단위 테스트 08:184가 잡음) · 로컬 dev에서 `APP_DATA_KEY_v1` 없으면 `/api/health` 503(08 ⑤-b에 적음) · 08 OPERATIONS +5줄 예산이 빠듯함(넘치면 멈추고 12 예산을 옮김).
</remaining_work>

<decisions_made>
- (기본값, 사용자 확인 대기) E3-03 서명 이미지 GCS 유지, E3-04 12·08 범위 현 계획 유지, E3-10 비활동 120분 유지. 바꾸지 않았다.
- /review 반영 기본값(사용자 결정 아님, 기술 선택): 07 표본은 `seedSubmittedCert`(07은 웨이브 4 유지) · 권한 없는 사람은 `touchPrivacySession` 머리에서 걸러 비활동 시계를 건드리지 않음 · 두 번째 `lock_timeout`은 두지 않고 잠금 전 속도 판정만 추가 · 헬스 왕복 실패는 기존 503(응답 필드 추가 없음) · 평문 시크릿은 유예 뒤 destroy + 런타임 바인딩 제거 · 백업 7일·PITR 복원 뒤 purge 한 번 · Cloud Run 요청 로그의 `/c/{token}`은 받아들인 잔여 위험.
- OPERATIONS.md 줄 예산: 05 +5 · 08 +5 · 12 +18 · 13 +2(합 30, main 270줄 전제).
</decisions_made>

<blockers>
- Codex 한도(외부): 9/29 전후까지, Opus 대체 중.
- Phase 4(PR #40) main 머지 전에는 실행 불가(E3-01).
- 사람 확인: 법무(120분·파기 기산일·동의 전문), 소유자(취급자 지정, 머지 전 bootstrap 실행으로 KMS 권한 정리).
</blockers>

## Required Reading (in order)
1. 이 파일
2. `/mnt/project-files/04.3-gates/review-apply-cross-r2.md` — 마지막 교차 판정과 남은 참고

## Infrastructure State
- 작업 트리 깨끗함(이 파일 커밋 전 기준). PR #74 CI는 8945590에서 초록.

<next_action>
Start with: 사용자에게 PR #74 머지 준비(문서 PR, GitHub Squash)를 알리고, 9/29 뒤 Codex 재확인 스레드를 연다. Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요.
</next_action>
