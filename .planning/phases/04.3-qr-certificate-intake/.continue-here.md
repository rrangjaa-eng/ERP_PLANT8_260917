---
context: phase
phase: 04.3-qr-certificate-intake
task: 0
total_tasks: 13
status: paused
last_updated: 2026-09-24T22:06:33.589Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: D-01 한 세션 한 관문. 직전 세션은 엔지니어링 검토 지적 반영(`/gsd-plan-phase 04.3` 수정 모드 + Opus 검사기 + Opus 교차)만 했다. 다음 세션은 `/plan-design-review`만 한다. 끝나면 인계 → 새 세션에서 설계 `/cso`.
- [ ] CONSTRAINT: Codex 사용 한도(2026-09-29 07:13 전후 재개). 한도 동안 교차 검토는 Opus 독립 검토, Fable은 페이즈당 최종 전체 검토 한 번만. 모든 검토 기록에 "Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요". `codex` 호출 금지.
- [ ] CONSTRAINT: 이 페이즈는 계획만. 실행 금지. 실행 시작은 Phase 4(PR #40)가 main에 머지된 뒤(E3-01, 각 플랜 frontmatter `execution_gate`). STATE.md 현재 페이즈를 바꾸지 않는다. `.planning/`은 GSD 도구·에이전트로만 고친다. 공용 `.planning/HANDOFF.json`은 Phase 4 것이라 덮어쓰지 않는다.
- [ ] CONSTRAINT: 모든 답·상태·카드는 한국어, 시각은 KST.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 병렬 플래너 간 이름·정책 어긋남 | 1회차에 샤드 3개가 tx 로그 대비책 정책·execution_gate 키·위협 id·테스트 도우미 수정 범위를 서로 다르게 적음 | advisory | 병렬 플래너엔 공통 이름표(`/mnt/project-files/04.3-gates/eng-apply-brief.md`)를 주고, 끝나면 샤드 간 우려 목록을 검사기·교차 검토에 입력 |
| 게이트 기록 커밋 차단 | `.claude/gates/phase-04.log` 변경 커밋이 자동 분류기에 막힘 | advisory | 훅이 쓴 게이트 기록은 사용자가 허용할 때까지 작업 트리에만 둔다(우회 금지) |

<current_state>
엔지니어링 검토(보고서 `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`, 35건) 반영 완료. 브랜치 `claude/plan-phase-04-3-9wian4`, draft PR #74.
- 72829e6: 1회차 반영(플래너 Opus 3명 병렬, 01~05·06~09·10~13). 각 플랜에 Review Dispositions Ledger.
- f3c7ab9: 2회차 수정 8건(교차 막는 문제 X1 = 06이 `seedSubmittedCert` 호출 줄을 안 고치던 문제, 10~13 execution_gate, T-04.3-193 재번호, 40자 기준 재계산, tx 로그 대비책 정책 통일, Phase 4 줄 번호 하드코딩 제거, ConfirmDialog 병합 목록, 03 문장).
- 검사기(Opus) 2회차: 막는 문제 0·경고 0·참고 1. 교차(Opus, Codex 대체) 2회차: 「막는 문제 없음」. 기록: `/mnt/project-files/04.3-gates/eng-apply-{checker,opus-cross}-r{1,2}.md`.
- 프로브(verify 경로·fails_when) 228건 모두 severity none.
</current_state>

<completed_work>
- 계획 13개, UI-SPEC 승인, 최종 전체 검토 1~4회차(이전 세션)
- `/plan-eng-review` 보고서(953e599)
- 엔지니어링 검토 반영 + Opus 검사기·교차 2회차 통과(이번 세션)
</completed_work>

<remaining_work>
1. 새 세션: `/plan-design-review`(UI-SPEC 있음, 필수). 넘겨받은 항목:
   - 보고서 「/plan-design-review에 넘김」: I2 길이 셀 문장(E3-21), 만들기 serverError 줄(E3-22), throttled 문구(E3-08).
   - 10 ④-0: Phase 4 ConfirmDialog에 `primary.failure` 추가(DECISIONS→SYSTEM §7-17→컴포넌트) — 실행 전 확정(10 ledger).
   - Phase 4 ConfirmDialog × 이름 `닫기`(폰만) 대 UI-SPEC 「취소」, UI-SPEC ⌘ 글리프 → Ctrl.
   - 11: Phase 4 SYSTEM §8-5(「…할 수 없음 —」 접두 금지) 대 UI-SPEC 인쇄 문구 충돌.
   - 03: 잠금 해제 때 포커스 규칙.
2. 그다음 새 세션: 설계 `/cso`. 입력 목록은 04.3-13 Task 「/cso 입력」 7행(E3-08·10·12·13·15·16·35).
3. 그다음: `/review` → draft PR 준비 완료 표시.
4. Codex 한도 풀리면(9/29): 최종 4회차, 엔지니어링 검토 P1·P2, 이번 반영 1·2회차를 Codex로 재확인.
</remaining_work>

<decisions_made>
- (기본값, 사용자 확인 대기) E3-03 서명 이미지 GCS 유지(대 DB bytea), E3-04 12·08 범위 현 계획 유지(대 축소·분리). 각 플랜 ledger에 "deferred — user check pending (default kept)".
- tx 로그: Phase 4 `recordAction({tx})` 사용, 없을 때만 02의 `insertActionLogTx`(repositories/cert-action-log.ts) 대비책 — 모든 플랜 한 정책(보고서 E3-02 지시 유지).
- 스테이징에만 `CERT_FEATURE_ALLOWED=true`, bootstrap은 PR 머지 전 PR 브랜치에서, 13에 스테이징 스모크.
</decisions_made>

<blockers>
- Codex 한도(외부): 9/29 전후까지, Opus 대체 중.
- Phase 4(PR #40) main 머지 전에는 실행 불가(E3-01).
</blockers>

## Required Reading (in order)
1. 이 파일
2. `/mnt/project-files/04.3-gates/eng-apply-opus-cross-r2.md` — 참고 지적 N1~N3(다음 반영 때 함께): N1 13:147 `grep -q 'failure'`는 10 ④-0이 건너뛰어지면 거짓 실패 → 10 SUMMARY 조건부로. N2 11:350 대비책 문장 불필요. N3 참고.
3. `04.3-UI-SPEC.md`, `docs/design/SYSTEM.md`(Phase 4 머지 전이면 `origin/claude/gsd-progress-e1nzgu` 판)

## Infrastructure State
- 작업 트리 깨끗함(커밋 f3c7ab9 푸시됨).

<next_action>
Start with: 새 세션에서 `/plan-design-review` 04.3 (Opus 1차 + Opus 교차, Codex 대체 기록).
</next_action>
