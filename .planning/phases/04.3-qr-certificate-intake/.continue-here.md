---
context: phase
phase: 04.3-qr-certificate-intake
plan: 2
total_plans: 13
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-09-26T13:25:00Z
---

# 04.3 인계 — 04.3-02 Task 2부터

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 공유 상태 파일 덮어쓰기 | Phase 4를 다른 계정이 병행 실행 중이라 `.planning/STATE.md`·`.planning/HANDOFF.json`·`.planning/state.json`을 고치면 머지 때 서로 덮는다. 사용자 지시로 04.3은 이 셋을 건드리지 않는다 | advisory | 실행자 프롬프트에 「STATE/HANDOFF/state.json/ROADMAP 수정 금지, `state.*`·`roadmap update-plan-progress` 동사 금지」를 적는다. 진행 기록은 이 파일과 각 플랜 SUMMARY로만 남긴다 |
| 게이트 기록 직접 쓰기 | `.claude/gates/phase-04.3.log`가 없어 실행 훅이 막혔고, 에이전트가 직접 쓰려다 권한 검사에 거부됐다. 사용자가 GitHub 웹에서 직접 커밋했다(89a9963) | advisory | `.claude/gates/`·`.claude/hooks/`·`.claude/settings*.json`은 에이전트가 쓰지 않는다. 막히면 멈추고 사용자에게 넣을 내용을 준다 |
| 세션당 실행자 하나 | 훅 `plant8-skill-gate.sh`가 한 세션에서 `gsd-executor`를 두 번째로 띄우는 것을 막는다(같은 플랜의 다음 태스크 포함). 04.3-02는 태스크별로 실행자를 나눠 띄웠기 때문에 Task 1 뒤 이 세션에서 Task 2를 띄우지 못했다 | advisory | 한 세션에서 실행자를 한 번만 띄운다. 04.3-02 남은 부분은 Task 2와 Task 3을 **한 실행자에** 함께 맡기고(SUMMARY까지), 컨텍스트가 모자라면 그 실행자가 멈춘 지점을 이 파일에 적어 다음 세션으로 넘긴다. 실행자를 우회해 SendMessage로 이어 가지 않는다 |

<current_state>
04.3-02(웨이브 2) Task 1(기반)은 끝나 푸시했다(6a68173). Task 2(트레이서)·Task 3(굳히기)·SUMMARY가 남았다.
브랜치 `claude/trusting-brahmagupta-k08auz`, PR rrangjaa-eng/ERP_PLANT8_260917#88(draft).
origin/main은 01196b5 그대로라 이번 세션에 머지할 것이 없었다. 714e09b의 CI는 quality·integration-e2e 모두 통과. 6a68173 CI는 이 인계 시점에 실행 중이다.
</current_state>

<completed_work>
04.3-02 Task 1 — 커밋(오래된 순)
- 163d612 test: 기능 게이트·로그 원문 제거 실패 테스트(RED)
- c268541 feat: 확인증 스키마 다섯 표 + 마이그레이션 `0017_cert_intake.sql`
- 7d726e3 feat: 기능 게이트(`isCertFeatureEnabled` · `assertCertFeatureEnabled` · 설정 `CERT_ENABLED` · env `CERT_FEATURE_ALLOWED`) + Playwright `cert-setup`/`certs` 격리 프로젝트
- 0a4ec6c feat: 스테이징 배포에만 `CERT_FEATURE_ALLOWED=true`
- 7e903ec fix: 서버 액션 미처리 오류 로그에서 message 제거(name·code·constraint만)
- fef7997 fix: migration-journal 테스트 no-unsafe-assignment 린트
- 6a68173 docs: 0018 주석의 DEFERRABLE 문구 중복 제거(acceptance grep -c = 1)
- `0018_cert_winners_unique_deferrable.sql`(custom): UNIQUE 제약을 `CREATE UNIQUE INDEX` → `ADD CONSTRAINT … UNIQUE USING INDEX … DEFERRABLE INITIALLY IMMEDIATE`로 바꾼다(괄호 형태는 squawk가 막음). 빈 DB에 처음부터 적용, squawk 0건, `condeferrable=true condeferred=false` 확인
- 검증(오케스트레이터 재확인): 단위 13파일 124 통과, typecheck·lint 0 오류, `playwright test --list` 354(cert 스펙 아직 없음)

SUMMARY용 RED 기록
- `test/unit/certs/feature.test.ts`: `Cannot find package '@/domain/certs/feature'`
- `test/unit/actions/handle-server-error.test.ts`(개인정보 케이스): 로그에 `김하늘`·`01048217730`·`테헤란로`가 그대로 있었다
- `test/unit/deploy/deploy-sh.test.ts`: 스테이징 `run deploy plant8-staging` 줄에 `CERT_FEATURE_ALLOWED=true` 없음(프로덕션 「없음」 케이스는 가드라 처음부터 녹색)
- `test/unit/db/migration-journal.test.ts`: main에 없어 새로 만든 가드라 처음부터 녹색

편차(SUMMARY에 옮길 것)
1. 커밋 제목이 플랜 ⑧의 `test(04.3): …`가 아니라 `test(04.3-02): …`다. acceptance의 정확 문자열 검색은 실패하지만 순서(RED 커밋이 스키마 커밋보다 먼저)는 지켰다 — 판정은 「RED가 먼저」로 한다
2. `migration-journal.test.ts`를 새로 만들었다(플랜이 예상한 경우 중 하나)
3. Rule 1 두 건: JSON.parse 결과 `as` 캐스팅(린트), 0018 머리 주석 문구만 수정(SQL 무변경)
</completed_work>

<remaining_work>
- 04.3-02 Task 2(트레이서: 설정 → domain → 리포지토리 → 공개 액션 → 외부 화면 `/c/[token]` → E2E `mobile-cert-intake.spec.ts` 한 경로, `test/e2e/helpers/cert.ts`·`cert.setup.ts`, 통합 `cert-intake`·`cert-crypto`)
- 04.3-02 Task 3(굳히기) + `04.3-02-SUMMARY.md`
- 04.3-03 ~ 04.3-13(웨이브 3~7)
- 페이즈 끝: /gsd-verify-work → /review → /qa → /design-review → /cso(주민번호 암호화·외부 입력이 있어 필수) → /ship
</remaining_work>

<decisions_made>
- 세션당 플랜 하나 + 세션당 실행자 하나(훅). 04.3-02 남은 Task 2·3은 다음 세션에서 실행자 하나에 함께 맡긴다
- 플랜마다 시작 전·푸시 전에 origin/main을 머지 커밋으로 반영한다(rebase·force push 금지)
- 마이그레이션은 0017·0018. main에 새 마이그레이션이 들어와 번호가 겹치면 `db/migrations/`를 origin/main과 같게 되돌린 뒤 플랜 C-규약(E3-31) 절차대로 두 파일을 다시 만든다(0018 본문은 Task 1 ②-b 그대로)
- Codex는 2026-09-29 07:13 KST까지 쓰지 않는다(Opus로 대체하고 「한도 풀리면 Codex 재확인」을 기록)
- 머지는 사용자가 채팅에 「머지해」라고 쳤을 때만 한다
- executor는 순차 모드(워크트리 없음), model 명시(config executor_model = sonnet)
</decisions_made>

<blockers>
- 없음
</blockers>

## Required Reading (in order)
1. PR #88의 최신 「[코디네이터 지시]」 댓글 — 작업 중 10분마다 「[진행 보고]」(지금 하는 일 / 마지막 푸시 커밋 / 막힌 것), 끝나면 「[완료 보고]」. 댓글 쓰기 전·푸시 전마다 최신 지시를 다시 읽는다
2. `.planning/phases/04.3-qr-certificate-intake/04.3-02-PLAN.md` — Task 2(334~479행)·Task 3(481~548행)·verification
3. `.planning/phases/04.3-qr-certificate-intake/04.3-01-SUMMARY.md` — 「04.3-01 대응표」

## Infrastructure State
- 로컬 개발 DB: 세션 시작 훅이 `postgres://erp:erp@127.0.0.1:5432/erp`(+ `erp_test`)를 띄운다. 새 세션이면 `pnpm db:migrate`로 0017·0018을 적용한다
- 게이트 기록: `.claude/gates/phase-04.3.log`에 plan-eng-review·plan-design-review가 있다(사용자 커밋 89a9963)

<next_action>
Start with: PR #88 최신 코디네이터 지시 읽기 → `git fetch origin && git merge origin/main`(머지 커밋) → `/gsd-execute-phase 04.3 --plan 04.3-02`로 실행자 **하나**에 Task 2·3과 SUMMARY를 맡긴다(Task 1은 끝났다고 알리고 위 completed_work의 RED 기록·편차를 넘긴다. STATE/HANDOFF/state.json/ROADMAP 수정 금지 조건을 프롬프트에 넣는다)
</next_action>
