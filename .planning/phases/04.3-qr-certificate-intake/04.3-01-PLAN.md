---
phase: 04.3-qr-certificate-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - docs/design/system/external-cert.html
autonomous: true
requirements: [CERT-01, CERT-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS 추적표에서 CERT-01·CERT-02는 Phase 04.3, CERT-03·CERT-04는 Phase 11에 속하고, ROADMAP Coverage 표의 합계는 86 그대로다 — 옮긴 둘과 남긴 둘을 더하면 원래 넷이다"
    - "CERT-01 본문이 D-1101(경품만 · 계좌·금액 칸 없음)과 D-1102(행사당 QR 하나 · 사람마다 1회 · 링크는 기한·전원 제출·담당자 닫기로 닫힘)·D-1103(목록에서 이름 고르기 + 전화번호 뒤 4자리 확인)을 말한다"
    - "ROADMAP Phase 목록에 Phase 04.3 요약 줄이 있고, Phase 11 요약 줄은 지출결의 연결(CERT-03)·세금 제안(CERT-04)·플래그 켜기만 말한다"
    - "DECISIONS.md에 D-a(이름 고르기·전화번호 확인 단계, 구별 표시 포함)와 D-b(관리 인덱스 `확인증` 그룹) 두 항목이 표 형식으로 있고, SYSTEM.md가 그 뒤에 고쳐졌다(DESIGN.md §4 순서)"
    - "SYSTEM.md §6-5가 E2 이름 고르기 · E3 전화번호 확인(짧은 잠김 · 누적 잠김 — 누적 한도는 서버 고정 상수 20, 설정 아님) · E4 입력 · E5 제출됨 · E6-a/b/c를 정본으로 갖고, 수령자 화면 금지어 둘(원천징수·지급명세서)을 규칙으로 적는다"
    - "SYSTEM.md §6-5의 이름 목록 규칙이 「한 행 = 한 사람 · 가린 이름이 겹치거나 구별 표시가 있는 행만 2행 `{경품명} {수량}개( · {구별 표시})` · 공개 모양이 같은 당첨자는 등록·저장 때 서버가 거부하고 담당자가 구별 표시(10자 · 숫자 3개 이상 이어짐 금지 · 당첨자 이름 금지)를 적는다」이고, 전화번호 뒤 4자리는 한 사람과만 대조한다(오케스트레이터 결정 2026-09-24, 묶음 행 없음)"
    - "SYSTEM.md 개정 ②③⑤⑥⑦⑧⑨⑩⑪⑫⑬이 각 절에 들어가 있다 — ④(§6-10)는 `role-menu.ts`·단위 테스트와 한 커밋이어야 해서 04.3-09가, ⑫의 §7-3 (아) 행동 셀 한 줄은 `ui/table` 키 처리와 한 슬라이스라 04.3-10이 한다"
    - "실물 `external-cert.html`에 장면 `#pick` `#verify` `#wrong` `#locked` `#locked-hard` `#submitted-slot` `#closed` `#notfound`가 있고, 본문에 금지어 둘이 없으며 이름 칸 자리표시자가 없다"
  prohibitions:
    - statement: "SYSTEM.md·실물을 고치지 않고 화면 코드가 먼저 서서는 안 된다. 외부 수령자 화면의 단계 추가·글자 크기·서명 계약은 전부 이 플랜의 개정이 먼저이고, 화면 하나만의 예외로 두는 길은 없다(CLAUDE.md 프론트엔드 규칙)"
      status: resolved
      verification: test
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CERT-01 새 문구 · 추적표 CERT-01/02 → Phase 04.3 · By phase 줄"
      contains: "| CERT-01 | Phase 04.3 | Pending |"
    - path: ".planning/ROADMAP.md"
      provides: "Coverage 표 04.3 행 · Phase 11 행 · Phase 목록 요약 줄 둘"
      contains: "| 04.3 | 2 | CERT-01, CERT-02 |"
    - path: "docs/design/DECISIONS.md"
      provides: "D-a · D-b 두 결정 항목"
      contains: "확인증"
    - path: "docs/design/SYSTEM.md"
      provides: "§6-5 개정 ①-a~l · §7-7 ② · §6-2 ③ · §7-5 ⑤ · §2-4 ⑥ · §7-1/§7-2 ⑦ · §7-15 ⑧ · §6-6 ⑨⑬ · §8 ⑩ · §7-3 ⑪ · ⑫(바) — ⑫(아)는 04.3-10"
      contains: "이름을 골라 주세요"
    - path: "docs/design/system/external-cert.html"
      provides: "새 장면 여덟 + ①-c·e·g·h·j·k·l 반영"
      contains: "#pick"
  key_links:
    - from: "docs/design/DECISIONS.md"
      to: "docs/design/SYSTEM.md"
      via: "D-a·D-b가 먼저 적히고 SYSTEM 개정이 그 번호를 가리킨다"
      pattern: "D-1103"
    - from: "docs/design/SYSTEM.md"
      to: "docs/design/system/external-cert.html"
      via: "§6-5 문장과 실물 장면이 같은 문구를 쓴다"
      pattern: "다시 제출할 수 없습니다"
---

<objective>
**As a** 경품 담당자·경영관리와 이 페이즈를 실행할 에이전트, **I want to** 요구사항·로드맵의 CERT 줄과 디자인 시스템 문서가 이 페이즈의 잠긴 결정(D-1101~D-1103·D-1106~D-1108)과 같은 말을 하게 하고 싶다, **so that** 화면 코드가 설 때 정본(REQUIREMENTS·SYSTEM.md·실물)이 이미 맞춰져 있어 실행자가 해석하지 않는다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다. Phase 4 선례대로 Goal의 행위자(담당자 · 경품 당첨자 · 경영관리)에서 이야기를 끌어냈다 — 새 이야기를 지어내지 않았다.

이 플랜은 **코드를 만들지 않는다.** 첫 태스크는 오케스트레이터가 정한 대로 REQUIREMENTS·ROADMAP 정렬이다(CLAUDE.md는 `.planning/` 수동 편집을 금하지만, 이 정렬은 ROADMAP 04.3 절이 「계획 단계에서 함께 맞춘다」고 적은 명시적 계획 태스크라 예외다 — GSD 도구에 이 동사가 없다). 둘째·셋째 태스크는 `docs/DESIGN.md` §4 순서(DECISIONS → SYSTEM → 실물)로 `04.3-UI-SPEC.md` 「SYSTEM.md · DECISIONS.md 개정 목록」을 옮긴다.

**UI-SPEC을 읽을 때의 규칙:** `04.3-UI-SPEC.md`는 Codex 디자인 검토를 반영해 최종 승인됐다(커밋 d138427). 그 문서가 정본이다. 특히 **같은 모양의 당첨자를 한 행으로 묶지 않는다** — 담당자가 행사 만들기·상세에서 짧은 **구별 표시**(담당자가 적는 글, 개인정보 아님)를 적어야 저장되고, 그 표시가 외부 목록 행에 보이며, 전화번호 뒤 4자리는 한 사람과만 대조한다(오케스트레이터 결정 2026-09-24 · UI-SPEC A11). 수령자 문의 전화는 행사를 만들 때 설정에서 복사한 행사별 사본이다(A12).

Purpose: 뒤 플랜 열둘이 같은 정본을 읽게 한다. 추적표가 틀린 채로 실행하면 `/gsd-verify-work`가 CERT-01·02를 Phase 11 몫으로 잘못 센다.
Output: REQUIREMENTS·ROADMAP 정렬, DECISIONS 두 항목, SYSTEM.md 개정 열세 묶음(④ 제외), 실물 장면 여덟(`#locked-hard` 포함).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@CLAUDE.md
</context>

<probe_fallback>
이 페이즈에는 평범한 `SPEC.md`가 없어 spec-less 프로브 폴백이 돌았다. 요구사항 원문이 한국어라 분류기의 영어 큐가 걸리지 않으므로(Phase 4 `04-01-PLAN.md` 선례와 같은 도구 한계), 두 요구사항 × 여덟 범주 = **16건**을 계획이 직접 풀어 각 플랜의 `must_haves.truths`에 옮겼다. **조용히 버린 항목은 0건이다.**

| # | 요구 × 범주 | 수용 기준(요약) | 판정 | 사는 플랜 |
|---|---|---|---|---|
| 1 | CERT-01 × boundary | 링크 마감 시각 정각에 닫힘 · 5번째 틀림에 잠김 · 수량 1 이상 · 2020-10-01 전후 검증번호 판정(발급 시점은 번호에 없으므로 생년월일로 거부하지 않고, 그 전 출생의 불일치는 한 번 되묻는다 — CONTEXT 31행) | test | 04.3-02 · 04.3-03 · 04.3-04 |
| 2 | CERT-01 × adjacency | 마지막 한 자리 제출이 같은 트랜잭션에서 링크를 닫는다 · 가린 이름이 이웃과 겹치면 경품 2행, 경품까지 같으면 구별 표시 | test | 04.3-06 · 04.3-03 · 04.3-04 |
| 3 | CERT-01 × empty | 당첨자 0명 행사는 만들 수 없다 · 서명은 잉크가 288픽셀(1040×400 내보내기 기준, `countInkPixels`) 미만이면 서명이 아니다 | test | 04.3-04 · 04.3-06 |
| 4 | CERT-01 × encoding | 이름을 NFC로 정규화한 뒤 가리고 정렬한다 · 공백은 자리를 지킨다 | test | 04.3-02 |
| 5 | CERT-01 × ordering | 실제 이름 가나다순 → 같으면 등록 순서, 가림은 정렬 뒤 | test | 04.3-03 |
| 6 | CERT-01 × precision | 잠금 해제 시각은 분 단위 올림으로 보인다(일찍 눌러 다시 잠기지 않게) · 전화번호는 숫자만 저장 | test | 04.3-03 · 04.3-02 |
| 7 | CERT-01 × idempotency | 같은 멱등 키의 두 번째 확인·제출은 첫 결과를 돌려주고 세거나 저장하지 않는다 | test | 04.3-03 · 04.3-06 |
| 8 | CERT-01 × concurrency | 같은 자리 동시 제출 둘 중 하나만 성공 · 마지막 두 자리 동시 제출에도 링크는 한 번 닫힌다 | test | 04.3-06 |
| 9 | CERT-02 × boundary | 파기 기한(제출 연도 + 1 + 보존 연수의 3월 1일 00:00 KST) 1ms 전은 남고 정각은 지운다 | test | 04.3-12 |
| 10 | CERT-02 × adjacency | 파기는 개인정보 칸만 비우고 줄(확인증 번호·행사·경품·수량·제출 일시·연결)은 남긴다 · DB 먼저, 파일 나중 | test | 04.3-12 |
| 11 | CERT-02 × empty | 현장 수령의 주소 없음은 I4에 칸이 없고 인쇄물에 `—` · 파기된 확인증의 전체 보기는 복호화하지 않고 거부 | test | 04.3-07 · 04.3-11 · 04.3-12 |
| 12 | CERT-02 × encoding | 주민등록번호는 `v1:` 암호문과 가린 값(`930412-2******`)으로만 저장되고 평문이 DB·행동 로그·서버 로그에 없다 | test | 04.3-02 · 04.3-07 |
| 13 | CERT-02 × ordering | 전체 보기는 기록이 먼저, 복호화가 나중 — 기록이 실패하면 평문이 나가지 않는다 | test | 04.3-07 |
| 14 | CERT-02 × precision | 제출 연도는 KST로 셈한다(2026-12-31 23:30 KST 제출 = 2026년분) | test | 04.3-12 |
| 15 | CERT-02 × idempotency | 파기를 다시 돌려도 안전하다 — 이미 지운 줄은 건너뛰고, 파일 삭제가 실패한 줄은 다음 실행이 다시 지운다 | test | 04.3-12 |
| 16 | CERT-02 × concurrency | 동시 정정은 버전 충돌로 거부된다(덮어쓰기 없음) · 파기와 겹친 전체 보기는 실패로 닫힌다 | test(정정) · backstop(파기 경합) | 04.3-07 · 04.3-12 |

**금지 항목(Prohibitions):** 주입·경로 순회 같은 canon 보안 항목은 `/cso`와 기존 eslint·`.squawk.toml`이 소유한다. 이 페이즈가 민팅하는 것은 그 도구들이 잡지 못하는 제품 고유의 must-NOT이고, 플랜마다 `must_haves.prohibitions`에 **디스크립터 없이**(wired check 미지정) 적어 검증 시점에 `flagged-unverified`로 떨어지게 한다.

**UI Considerations 옮김:** `04.3-UI-SPEC.md` `## UI Considerations`(본표) · 「상태 점검 보강」의 covered 항목은 문자열 truth로, backstop 항목은 `{statement, verification: backstop}`로 그 화면을 만드는 플랜에 옮겼다. 본표의 「묶음 행」「구별할 수 없는 자리」 두 줄은 위 오케스트레이터 결정(구별 표시)으로 바뀐 형태로 옮겼다(04.3-03 · 04.3-04 · 04.3-10). backstop 전부는 04.3-13의 독립 DOM 감사·실기기 확인이 판정한다.
</probe_fallback>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS·ROADMAP 정렬 — CERT-01/02를 Phase 04.3으로, CERT-01 문구를 D-1101·D-1102·D-1103에 맞춤</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` 「Phase 04.3」 절 전문(Goal · Requirements · 성공 기준 1~5 · 끝 문단의 「계획 단계에서 정할 것」) — 이 태스크가 맞출 대상의 정본
    - 같은 파일 「Phase 11」 절(Goal · Requirements `CERT-03, CERT-04` · 끝 문단) — Phase 11이 남기는 몫
    - 같은 파일 머리의 Phase 목록(`- [ ] **Phase N: …**` 줄들)과 `## Coverage` 표 · 그 아래 「하드 제약 대조」 앞까지
    - `.planning/REQUIREMENTS.md` 「기타소득 확인증 (CERT)」 절(CERT-01~04)과 추적표의 CERT 네 줄, 맨 아래 `**Coverage:**` · `**By phase:**` · `*Last updated:*` 줄
    - `.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md` 「Phase Boundary」의 CERT-01 대체 두 줄(D-1101 · D-1102)과 D-1101~D-1103 본문
  </read_first>
  <action>
① `.planning/REQUIREMENTS.md`의 CERT-01 줄 전체(체크박스와 굵은 ID는 그대로)를 다음 본문으로 바꾼다 — 「경품(현물) 수령자가 행사 QR(행사당 하나, 여러 명이 같이 씀)을 폰으로 찍고 로그인 없이 들어와 담당자가 미리 넣은 당첨자 목록에서 자기 이름을 고르고 전화번호 뒤 4자리로 확인한 뒤, 본인 정보(이름·주민등록번호·연락처, 택배면 주소)와 개인정보 동의·터치 서명을 제출한다. 수령자 화면에는 경품명 한 줄만 보이고 금액·세금·계좌 칸은 없다(D-1101). 한 사람은 한 번만 제출하고 제출하면 그 사람 자리만 닫히며, 링크 전체는 만료 시간(기본 72시간)이 지나거나 당첨자가 모두 제출하거나 담당자가 닫으면 닫힌다(D-1102·D-1103)」. 옛 문구의 세 표현(ROADMAP 04.3 절 끝 문단이 이름을 댄 셋 — 계좌 입력, 지출결의에서 채워 보여 주기, 한 번 쓰는 링크)은 새 본문에 남기지 않는다. CERT-02·03·04 본문은 건드리지 않는다(CERT-04 문구 조정은 Phase 11 계획 몫이라 ROADMAP Phase 11 절이 이미 적었다).

② 같은 파일 추적표의 CERT-01·CERT-02 두 줄의 페이즈 칸을 `Phase 04.3`으로 바꾼다(`| CERT-01 | Phase 04.3 | Pending |`). CERT-03·04는 `Phase 11` 그대로 둔다.

③ 같은 파일 `**By phase:**` 줄: `4 (11)` 항목 바로 뒤(04.1·04.2 항목이 main에 이미 있으면 그 뒤)에 ` · 04.3 (2)`를 끼우고 `11 (4)`를 `11 (2)`로 바꾼다. `**Coverage:**` 세 줄(86 · 86 · 0)은 그대로다 — 합이 바뀌지 않는다. `*Last updated:*` 줄은 오늘 날짜로 「2026-09-24 — CERT-01·02 → Phase 04.3(D-1101·D-1102·D-1103 문구), Phase 11은 CERT-03·04 (86/86 mapped)」 한 줄로 바꾼다.

④ `.planning/ROADMAP.md` `## Coverage` 표: `| 4 | 11 | …` 행 바로 뒤(04.1·04.2 행이 main에 이미 있으면 그 뒤)에 `| 04.3 | 2 | CERT-01, CERT-02 |` 행을 더하고, `| 11 | 4 | …` 행을 `| 11 | 2 | CERT-03, CERT-04 |`로 바꾼다. `| **Total** | **86** | |` 행과 표 위 문장은 그대로다.

⑤ 같은 파일 머리의 Phase 목록: Phase 4 줄 바로 뒤(04.1·04.2 줄이 있으면 그 뒤)에 `- [ ] **Phase 04.3: QR 확인증 접수 (INSERTED)** - 행사 QR 하나 + 당첨자 목록 등록, 수령자가 로그인 없이 이름 고르기·전화번호 뒤 4자리 확인·본인 정보·동의·터치 서명 제출(CERT-01), 주민등록번호 앱 단 암호화(설계 /cso 뒤 KMS 봉투 승격)·경영관리만 전체 보기·열람 기록·경영관리 정정·세무 보관용 인쇄물·보존 기간 뒤 파기(CERT-02), 프로덕션은 플래그로 꺼 둠 — /cso 두 번(D-1107)` 한 줄을 더한다. Phase 11 줄 본문은 `경품 지출결의 화면에서 행사 QR 만들기·제출된 확인증 자동 연결(CERT-03), 회사 대납 세금 제안·확정(CERT-04), 인쇄물의 지출결의·금액·세금 칸 채우기, 프로덕션 플래그 켜기 — 접수·암호화·열람·파기는 Phase 04.3, /cso 통과가 활성화 조건`으로 바꾼다(체크박스 · 굵은 머리 `**Phase 11: 기타소득 확인증**` 그대로).

⑥ 건드리지 않는 곳: ROADMAP 15행 부근의 과거 CEO 리뷰 문장 · Progress 표(GSD 도구가 갱신한다) · Phase 04.3 절 본문과 `**Plans:**` 줄(계획 커밋 때 오케스트레이터가 갱신한다) · STATE.md(다른 세션이 Phase 4를 실행 중이다 — 현재 페이즈를 바꾸지 않는다).

⑦ 커밋: `docs(04.3): align CERT-01/02 traceability to Phase 04.3` + 한국어 본문(무엇을 옮겼고 합계가 86 그대로임).
  </action>
  <verify>
    <automated>grep -q '^| CERT-01 | Phase 04.3 | Pending |$' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| CERT-02 | Phase 04.3 | Pending |$' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| CERT-03 | Phase 11 | Pending |$' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| CERT-04 | Phase 11 | Pending |$' .planning/REQUIREMENTS.md &amp;&amp; test "$(grep -c '^| CERT-0[12] | Phase 11 ' .planning/REQUIREMENTS.md)" = 0 &amp;&amp; grep -q '^| 04.3 | 2 | CERT-01, CERT-02 |$' .planning/ROADMAP.md &amp;&amp; grep -q '^| 11 | 2 | CERT-03, CERT-04 |$' .planning/ROADMAP.md &amp;&amp; test "$(grep -c '^| 11 | 4 |' .planning/ROADMAP.md)" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 추적표 네 줄 중 하나가 목표 형태가 아니거나, CERT-01/02가 아직 Phase 11에 남았거나, Coverage 행이 목표와 다르다</fails_when>
    <automated>awk -F'|' '/^## Coverage/{f=1;next} /^\*\*하드 제약/{f=0} f &amp;&amp; $2 ~ /^ [0-9]+(\.[0-9]+)? $/ {s+=$3} END{exit !(s==86)}' .planning/ROADMAP.md</automated>
    <fails_when>종료 코드가 0이 아니다 — Coverage 표의 페이즈 행 개수 합이 86이 아니다(옮긴 둘과 남긴 둘이 원래 넷과 맞지 않는다)</fails_when>
    <automated>test "$(grep '^- \[ \] \*\*CERT-01\*\*' .planning/REQUIREMENTS.md | grep -c '1회성\|미리 채워져\|1회 제출 후 만료')" = 0 &amp;&amp; test "$(grep '^- \[ \] \*\*Phase 11:' .planning/ROADMAP.md | grep -c '1회성')" = 0 &amp;&amp; grep -q '^- \[ \] \*\*Phase 04.3: QR 확인증 접수 (INSERTED)\*\*' .planning/ROADMAP.md &amp;&amp; grep '^- \[ \] \*\*Phase 11:' .planning/ROADMAP.md | grep -q 'CERT-03'</automated>
    <fails_when>CERT-01 줄이나 Phase 11 요약 줄에 옛 표현이 남았거나, Phase 04.3 요약 줄이 없거나, Phase 11 줄이 CERT-03을 말하지 않는다</fails_when>
    <automated>grep '^\*\*By phase:\*\*' .planning/REQUIREMENTS.md | grep -q '04.3 (2)' &amp;&amp; grep '^\*\*By phase:\*\*' .planning/REQUIREMENTS.md | grep -q '11 (2)'</automated>
    <fails_when>By phase 줄에 `04.3 (2)` 또는 `11 (2)`가 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - 추적표에 `| CERT-01 | Phase 04.3 | Pending |` · `| CERT-02 | Phase 04.3 | Pending |` · `| CERT-03 | Phase 11 | Pending |` · `| CERT-04 | Phase 11 | Pending |` 네 줄이 있고, `grep -c '^| CERT-0[12] | Phase 11 ' .planning/REQUIREMENTS.md`가 0이다
    - CERT-01 줄에 `D-1101`·`D-1102`와 `전화번호 뒤 4자리`가 있고, 옛 표현(`1회성` · `미리 채워져` · `1회 제출 후 만료`) 셋 중 어느 것도 없다
    - ROADMAP Coverage 표에 `| 04.3 | 2 | CERT-01, CERT-02 |`와 `| 11 | 2 | CERT-03, CERT-04 |`가 있고, `grep -c '^| 11 | 4 |' .planning/ROADMAP.md`가 0이며, 페이즈 행 합이 86이다(awk 단언)
    - ROADMAP Phase 목록에 Phase 04.3 줄이 있고 Phase 11 줄에 `CERT-03`·`CERT-04`가 있으며 `1회성`이 없다
    - `git diff --stat .planning/STATE.md`가 비어 있다
  </acceptance_criteria>
  <done>추적표·Coverage·요약 줄이 CERT-01/02 = Phase 04.3, CERT-03/04 = Phase 11로 맞고 합계 86이 유지된다</done>
</task>

<task type="auto">
  <name>Task 2: DECISIONS D-a·D-b → SYSTEM.md §6-5(개정 ①-a~l)·§7-7(②) → 실물 장면 여덟</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, docs/design/system/external-cert.html</files>
  <read_first>
    - `docs/DESIGN.md` §4(기존 시스템에 화면·규칙을 더하는 절차 — DECISIONS → SYSTEM → 실물)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「SYSTEM.md · DECISIONS.md 개정 목록」 절 전문(D-a · D-b · 표 ①-a~⑬ · 실물 갱신 문단) — 이 태스크가 옮길 원문
    - 같은 문서 「Copywriting Contract」의 E 표(외부 수령자 화면 문구 전부)와 「화면 계약」의 E 절(E1~E6) — §6-5에 들어갈 규칙·문장의 정본
    - 같은 문서 「가정」 표(A1~A12) — D-a가 적을 이유·버린 대안
    - `docs/design/DECISIONS.md` 전문 — 기존 항목 형식(# · 결정 · 이유 · 버린 대안 표)과 2026-09-18 「외부 수령자 화면 실물 확정」 항목(#2~#6)
    - `docs/design/SYSTEM.md` §6-5(456~490행 부근 전문) · §7-7(805행 부근, 「외부 수령자 화면」 행) · §10(1031행 부근, 「산문·외부 수령자 화면은 15px 이상」 · 「키보드 전 행동」)
    - `docs/design/system/external-cert.html` 전문(171행) — 장면 전환 방식(머리 주석의 해시 목록 + `s-*` 클래스 + 끝의 스크립트)과 고칠 자리(46·47·58·112·124행 부근)
    - `test/unit/design-system-docs.test.ts` 전문 — SYSTEM.md를 읽어 단언하는 기존 테스트(고친 뒤에도 녹색이어야 한다)
  </read_first>
  <action>
① `docs/design/DECISIONS.md` 끝에 2026-09-24 날짜 절을 만들고 두 항목을 기존 표 형식으로 더한다.
  - **D-a 「외부 수령자 화면에 이름 고르기 · 전화번호 확인 단계를 더한다(D-1103, Phase 04.3)」** — 행: UI-SPEC 개정 ①의 각 줄과 가정 A1~A5·A12. **구별 표시 결정을 한 행으로 적는다(오케스트레이터 결정 2026-09-24):** 가린 이름과 경품 2행까지 같은 당첨자가 둘 이상이면 담당자가 행사 만들기·상세에서 짧은 구별 표시(담당자가 적는 글, 이름·전화번호 조각 금지)를 적어야 저장되고, 그 표시가 외부 목록 행에 보이며, 전화번호 뒤 4자리는 사용자가 고른 한 사람과만 대조한다. 버린 대안에 UI-SPEC D-a가 든 것 전부 + 「같은 모양의 행을 한 행으로 묶고 서버가 묶음 전원과 4자리 대조(한 사람을 확정하지 못하는 자리가 생기고 그 사람의 제출 경로가 화면 밖으로 나간다)」를 더한다.
  - **D-b 「관리 인덱스에 `확인증` 그룹(Phase 04.3 임시 진입, 플래그 뒤)」** — UI-SPEC D-b 원문 그대로(버린 대안 셋 포함).

② `docs/design/SYSTEM.md` §6-5를 개정 ①-a~①-l대로 고친다. 핵심:
  - ①-a 머리 문장(당첨자를 등록할 때 사람마다 · QR 행사당 하나) + E2·E3 와이어프레임 두 개(UI-SPEC E 절 그림에서 「묶음」 예시 줄은 빼고 구별 표시가 보이는 행 한 줄로 바꾼다) + `main` 최대 폭 480
  - ①-b 새 줄: 이름 가림 규칙 · 겹침 규칙 · **구별 표시 규칙**(가린 이름과 경품 2행까지 같으면 담당자가 적은 구별 표시로 가른다, 없으면 담당자 쪽 저장이 막힌다) · 가나다순 · 제출 여부 미표시(E6-a는 전화번호 확인 뒤에만) · 목록 전부 · 행 = 전폭 버튼 48 · 뒤 4자리 칸 규칙(틀리면 값 지움 + 남은 횟수, 한 사람과만 대조) · 잠김 문장과 해제 시각(서버 값) · **누적 잠김**(사용자 결정 2026-09-24 「누적 잠금 넣기로 해」: 당첨자 한 줄의 틀림을 짧은 잠김과 따로 누적으로 센다 · 한도 = **서버 고정 상수 20, 설정 아님** · 짧은 잠김 풀림 · 맞음으로 줄지 않고 같은 멱등 키 재전송은 세지 않음 · 담당자 I3 「잠금 풀기」로만 0 · 20 = 5 × 4라 넷째 짧은 잠김 자리에서 걸리고 둘이 겹치면 누적 잠김이 앞선다 · `남은 횟수 {n}번`의 `{n}`은 짧은 잠김까지만 · 해제 시각 · 카운트다운 · 누적 횟수 없음 · 클라이언트가 풀지 않음 · 문장은 UI-SPEC Copywriting 「E3 누적 잠김」 두 줄뿐(셋째 줄 없음) · 버튼 아래 잠김 알림 묶음 `<div tabindex="-1">`(`aria-live` 없음 — 포커스 이동이 알린다)에 들어가고 그 안에 2차 「잠금 확인」 버튼을 두며 누적 잠김으로 들어오는 모든 길에서 포커스가 그 묶음으로 간다 · **재확인**(자동 = `visibilitychange`/`pageshow`, 조용함 · 수동 = 「잠금 확인」)의 결과는 넷 — 닫힘(어느 잠금보다 앞서 E6-b) · 누적 잠김 · 짧은 잠김(해제 시각 · 한도 — 누적 잠김으로 바로 들어온 사람에게도 짧은 잠김 문장이 선다) · 열림 · 수동으로 눌러 누적 잠김이거나 받지 못하면 포커스는 묶음, 실패 줄 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요`는 묶음 안에 서고 「잠금 확인」의 `aria-describedby`가 가리킨다 · 짧은 잠김으로 바뀌면 같은 묶음에 짧은 잠김 줄을 그리고 묶음으로 · 열림이면 뒤 4자리 칸으로 · 비활성 1차의 `aria-describedby`는 두 `<p>` id뿐 · 3차 「다른 이름 고르기」는 살아 있다) · 카운트다운 없음 · 잠금 풀림 복귀 · E2 행 누름 실패 줄 · E3 **결과 불명** 줄(`확인 결과를 받지 못했습니다 · 다시 눌러 주세요`) · 서버 왕복 20초와 응답 두 갈래(확정 판정 여덟 / **결과 불명** = 연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가) · 멱등 키는 시도 단위(결과 불명이고 입력이 그대로일 때만 같은 키로 재전송, 키는 확정 판정을 받을 때 끝남) — 이 상태의 이름은 UI-SPEC ①-b 그대로 「결과 불명」 하나만 쓴다(다른 낱말로 바꾸지 않는다, §7-7 ERROR 칸도 같다) · 링크 닫힘이면 E6-b · 확인 상태는 메모리만 · 값의 주인 · 기록 항목 둘(뒤로 = E2) · 단계별 포커스와 `document.title`
  - ①-c 다섯 상태: SUCCESS 첫 줄을 UI-SPEC ①-c의 새 문장(`제출되었습니다 · 다시 제출할 수 없습니다`)으로, 옛 「만료(이미 제출된 링크 재진입)」 줄을 E6-a로 교체, E6-b(사유 셋) · E6-c 추가, 연락 줄에 `{문의 전화}`
  - ①-d 이름·연락처 칸 비움 + 이유 한 줄 · ①-e 이름 칸 자리표시자 삭제 · ①-f 「전문 보기」 제자리 펼침 · ①-g 동의 요약 새 문장(목적 = 세무 신고와 경품 전달, 수집 항목에 서명, 보존 기산점 문장, 동의 거부 문장)과 **수령자 화면 금지어 둘(`원천징수` · `지급명세서`)을 규칙으로 명시** · ①-h 택배 부제 `--fs-md`(한 문장이라 `--fs-xs` → `--fs-md`, UI-SPEC :111 · :199) · ①-i 서명 캔버스 계약(색 · 3px · `touch-action: none` · 비율 좌표 보존 · `pointercancel` · 잉크 288픽셀 이상(1040×400 내보내기, `countInkPixels` — 획 길이 합이 아니다) · 키보드로 그리기 · `role="application"` + `.sr-only` 설명 · 바탕 투명 PNG 한 번 전송 · 서명의 뜻 = 수령자가 칸에 직접 그은 획) · ①-j 동의 라벨 `--fs-md`와 「이 화면의 글자 크기는 넷뿐」 한 줄 · ①-k 문장은 `--fs-md` · ①-l 문의 전화 연락 경로(행사에 저장된 번호 사본만 읽으므로 행사가 있는 외부 화면에는 언제나 번호가 있다 — **번호 없는 대체 문장은 두지 않는다**(UI-SPEC :160), 번호가 없는 화면은 행사를 모르는 E1 첫 진입 실패 · E6-c 둘뿐, §8-5 예외가 아님을 한 줄)
  - 옛 §6-5 동의 요약 문장의 목적 표현과 SUCCESS 옛 문장은 이 절에서 사라져야 한다(아래 acceptance가 파일 전체에서 0건을 단언한다)

③ 같은 파일 §7-7 「컴포넌트별 다섯 상태」 표의 「외부 수령자 화면」 행: SUCCESS 칸을 ①-c 문장으로, PDF 링크 언급 삭제(v2 CERT-05), ERROR 칸에 「결과 불명(연결 끊김 · 20초 · 5xx · 해석 불가) = 결과를 모름 · 같은 키로 다시 — 키는 확정 판정을 받을 때만 끝남」 한 줄(개정 ② 원문 그대로). EMPTY 「해당 없음」 유지.

④ `docs/design/system/external-cert.html`: 머리 주석의 장면 목록에 `#pick` `#verify` `#wrong` `#locked` `#locked-hard`(누적 잠김 — UI-SPEC 「실물 갱신」 · E3 재확인 계약: 1차 「확인」 아래 알림 묶음 `<div tabindex="-1">`(`aria-live` 없음) 안에 Copywriting 「E3 누적 잠김」 두 줄을 각각 id가 있는 `<p>` 둘로 · 2차 「잠금 확인」 버튼 · 그 아래 실패 줄 `<p id … hidden>확인 결과를 받지 못했습니다 · 다시 눌러 주세요</p>`(필요할 때만 보인다 — 실물에서는 `hidden`으로 두고, 「잠금 확인」 버튼의 `aria-describedby`가 이 줄 id를 가리킨다). 비활성 1차 「확인」의 `aria-describedby`는 두 `<p>` id뿐이다(묶음 · 실패 줄 id 없음). 해제 시각 · 카운트다운 · 남은 횟수 줄 없음 · 3차 「다른 이름 고르기」는 살아 있다) `#submitted-slot` `#closed` `#notfound`를 더하고(옛 `#expired`는 `#submitted-slot`으로 이름을 바꾼다) 같은 방식(`s-*` 클래스 + 끝 스크립트)으로 각 장면의 마크업을 만든다. `#pick`에는 가린 이름 행 · 겹침 2행 · 구별 표시가 보이는 행 · 목록 아래 문의 줄을 각각 한 번 이상 둔다. ①-c(E5 문장 · E6-a/b/c) · ①-e(이름 칸 자리표시자 속성 삭제) · ①-g(동의 요약 새 문장 — 본문에서 금지어 둘이 사라진다) · ①-h(`.lbl-sub` `--fs-md`) · ①-j(`.consent label` `--fs-md`) · ①-k(`.ferr` `.blocked` `.consent small` `.state .who` `--fs-md`) · ①-l(문의 줄 · 연락 줄의 `tel:` 링크)을 반영하고 `.inp` 좌우 패딩을 `var(--s-2)`로 고친다(개정 ⑦ (a)). 새 색·크기·radius 리터럴을 만들지 않는다 — 토큰만 쓴다. 머리 주석의 설명 문장(2026-09-18 사용자 결정 기록)은 역사 기록이라 그대로 둔다.

⑤ 커밋 둘: `docs(04.3): record D-a/D-b design decisions` → `docs(04.3): revise SYSTEM §6-5/§7-7 and external-cert mockup for name pick + phone check`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/ui/system-md-compliance.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 요약 줄의 failed 개수가 1 이상이거나 "No test files found"가 나온다 — SYSTEM.md를 읽는 기존 단언이 개정으로 깨졌다</fails_when>
    <automated>test "$(grep -c '제출되었습니다 · 이 링크는 닫혔습니다' docs/design/SYSTEM.md)" = 0 &amp;&amp; test "$(grep -c '원천징수 신고와' docs/design/SYSTEM.md)" = 0 &amp;&amp; grep -q '이름을 골라 주세요' docs/design/SYSTEM.md &amp;&amp; grep -q '구별 표시' docs/design/SYSTEM.md &amp;&amp; grep -q '누적 잠김' docs/design/SYSTEM.md &amp;&amp; grep -q '다시 제출할 수 없습니다' docs/design/SYSTEM.md &amp;&amp; test "$(grep -c '결과 불명' docs/design/SYSTEM.md)" -ge 2 &amp;&amp; test "$(grep -c '응답 없음' docs/design/SYSTEM.md)" = 0</automated>
    <fails_when>옛 SUCCESS 문장이나 옛 동의 목적 표현이 파일 어디에든 남았거나, 새 E2 라벨·구별 표시 규칙·누적 잠김 규칙·새 SUCCESS 문장 중 하나가 없거나, 서버 왕복 상태 이름이 §6-5 ①-b와 §7-7 두 곳에 「결과 불명」으로 들어가지 않았거나 다른 이름(UI-SPEC에 없는 표현)이 섞였다</fails_when>
    <automated>for s in pick verify wrong locked locked-hard submitted-slot closed notfound; do grep -q "#$s" docs/design/system/external-cert.html || exit 1; done; test "$(grep -c 'placeholder="김하늘"' docs/design/system/external-cert.html)" = 0 &amp;&amp; test "$(sed -n '/<body/,$p' docs/design/system/external-cert.html | grep -c '원천징수\|지급명세서')" = 0 &amp;&amp; grep -q 'consent label{font-size:var(--fs-md)' docs/design/system/external-cert.html &amp;&amp; grep -q '잠금 확인' docs/design/system/external-cert.html &amp;&amp; grep -q '확인 결과를 받지 못했습니다 · 다시 눌러 주세요' docs/design/system/external-cert.html</automated>
    <fails_when>새 장면 여덟 중 하나라도 없거나, 이름 칸 자리표시자가 남았거나, `<body>` 아래(수령자에게 보이는 부분)에 금지어가 있거나, 동의 라벨이 `--fs-md`가 아니거나, `#locked-hard`의 「잠금 확인」 버튼 · 재확인 실패 줄이 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/DECISIONS.md`에 D-a·D-b 두 항목이 표 형식으로 있고 D-a에 구별 표시 행과 「묶음 행」 버린 대안이 있다
    - `grep -c '제출되었습니다 · 이 링크는 닫혔습니다' docs/design/SYSTEM.md`가 0이다(§6-5 · §7-7 둘 다 고쳐졌다)
    - `grep -c '원천징수 신고와' docs/design/SYSTEM.md`가 0이다
    - §6-5에 `이름을 골라 주세요` · `전화번호 뒤 4자리` · `구별 표시` · `누적 잠김`(고정 상수 20 · 설정 아님 · `aria-live` 없는 묶음 — 포커스 이동이 알린다) · `원천징수` 금지 규칙 문장 · `role="application"` · `288`이 있다
    - `grep -c '결과 불명' docs/design/SYSTEM.md`가 2 이상(§6-5 ①-b · §7-7 ERROR 칸)이고 `grep -c '응답 없음' docs/design/SYSTEM.md`가 0이다(UI-SPEC ①-b · ② 낱말 그대로)
    - `external-cert.html`에 새 장면 여덟(`#locked-hard` 포함)이 있고 `<body>` 아래에 금지어 둘이 0건이며 `placeholder="김하늘"`이 0건이다
    - `#locked-hard` 장면이 E3 재확인 계약 모양이다: 알림 묶음에 `tabindex="-1"`이 있고 `aria-live`가 없으며, 그 안에 id 있는 `<p>` 둘 · 「잠금 확인」 버튼 · `hidden`인 실패 줄 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요`가 있고, 「잠금 확인」의 `aria-describedby`가 실패 줄 id를, 비활성 1차의 `aria-describedby`가 두 `<p>` id만 가리킨다(SUMMARY에 그 마크업을 인용한다)
    - `git diff --stat docs/design/tokens.css`가 비어 있다(새 토큰 없음)
    - 두 단위 테스트가 녹색이다
  </acceptance_criteria>
  <done>DECISIONS 두 항목 → SYSTEM §6-5·§7-7 → 실물 순서로 외부 수령자 화면의 새 정본이 서 있다</done>
</task>

<task type="auto">
  <name>Task 3: SYSTEM.md 나머지 개정 — ③ §6-2 · ⑤ §7-5 · ⑥ §2-4 · ⑦ §7-1/§7-2 · ⑧ §7-15 · ⑨⑬ §6-6 · ⑩ §8 · ⑪⑫ §7-3</name>
  <files>docs/design/SYSTEM.md</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「SYSTEM.md 개정」 표의 ②~⑬ 행(④ 제외) — 옮길 원문
    - `docs/design/SYSTEM.md` §2-4(157행 부근 형식 표) · §6-2(357행 부근, 끝의 적용 줄) · §6-6(491행 부근, 기타소득 확인증 줄) · §7-1(657행 부근) · §7-2(674행 부근) · §7-3 (마)·(바)(708행 부근부터) · §7-5(791행 부근 의미 목록) · §7-15(972행 부근 다섯 상태) · §8(1007행 부근 규칙 7 용어)
    - `docs/design/DECISIONS.md` — Task 2가 더한 D-a·D-b(이 태스크의 개정도 그 결정 번호를 가리킨다)
    - `ui/table/RowSheet.tsx` 머리 주석(「행동은 그 문서가 생기는 페이즈가 더한다」) — ⑫ 문장의 근거
    - `ui/table/use-dirty-storage.ts` 머리 주석 — ⑪이 바꾸는 규칙이 지금 어디에 배선돼 있는지
  </read_first>
  <action>
UI-SPEC 개정 표의 문장을 각 절에 넣는다. 새 규칙 문장만 더하고 주변 문장은 고치지 않는다(CLAUDE.md 「Surgical Changes」).

- **③ §6-2 끝 적용 줄:** 화면 안 QR 이미지 = 160×160, `--fg` on `--bg`, radius 0, 조용한 영역 4모듈, 닫힌 링크의 QR은 렌더하지 않는다.
- **⑤ §7-5 의미 목록:** 낱말 다섯 — `접수 중` `--accent` · `닫힘` `--muted` · `제출됨` `--success` · `미제출` `--muted` · `잠김` `--warning`(Phase 4 보강 줄과 같은 형식).
- **⑥ §2-4 형식 표:** 두 줄 — `주민등록번호(가림)` = `930412-2******`(앞 6 + 성별 1 + `*` 6) · `전화번호` = `010-4821-7730`(하이픈, tabular-nums).
- **⑦ §7-1 · §7-2:** (a) `Button`·`TextField`의 `size="external"`(높이 `--s-12` 48 · `--fs-md` · 폭 100% · Button은 이유 줄이 버튼 아래) — §7-2 「48(외부 수령자)」를 variant로 이름 붙인 것, 외부 입력 패딩도 `0 8px` · (b) `disabledReason`에 id + `aria-describedby` 자동 연결 · (c) `pending`에 `.sr-only` `처리 중` — §7-1 진행 중 줄에 「보조기술에는 `처리 중`」 한 줄. 코드 변경은 04.3-02(size)·04.3-03((b)(c))이 한다 — 이 태스크는 문서만.
- **⑧ §7-15 다섯 상태:** SUCCESS에 「정정 폼 = 버튼 자리 결과 텍스트 + 입력은 새 값 기준으로 계속 편집」 한 줄, ERROR에 「정정 폼 버전 충돌 = 거부 + `… 먼저 고침 · 다시 불러오기`, 병합 없음」 한 줄.
- **⑨ §6-6 기타소득 확인증:** 연결 전(Phase 11 이전) 칸 규칙 — 지출결의 번호 · 프로젝트 · 지급일 · 금액 셋 = `—`, 원천징수 내역 2행 · 한글 금액 줄 없음, 경품 건의 계좌 `—`, 현장 수령의 주소 `—`, 항목 = 행사 이름 + 2행 당첨일.
- **⑬ §6-6 인쇄 규칙 세 줄 + 한 줄:** 인쇄 라우트 화면 바탕 `--surface` · 종이 외곽 `1px --line`(실물 미리보기 리터럴은 옮기지 않는다) / 서명 이미지(바탕 투명 PNG)는 `filter: brightness(0)`로 `--print-ink` 검정 / 자동 `window.print()`는 서명 `decode()` 뒤에만, 실패면 ERROR / 화면에서 전체 값을 펼칠 수 있는 개인정보 칸은 그 화면의 `@media print`에서 저장된 마스킹 값만 보인다.
- **⑩ §8 규칙 7 용어:** `확인증 행사`(이벤트 X) · `당첨자`(수령자 명단 X) · `전달`(현장 수령 / 택배) · `링크 닫기`(링크 만료 X — 만료는 기한이 지난 경우만) · `구별 표시`(비고·메모 X — 외부 목록에 보이는 담당자 글).
- **⑪ §7-3 (마):** 개인정보 칸(이름·전화번호·주민등록번호 등)이 든 편집 표는 브라우저 저장소에 보관하지 않는다 — `beforeunload` 경고만, 「복원 / 버림」 줄 없음. 확인증 당첨자 표가 첫 적용.
- **⑫ §7-3 (바):** RowSheet는 보기 전용 — 셀 편집·저장 행동은 없고, 3차 한 개(다른 화면으로 가는 링크, 또는 확인 시트를 여는 줄 행동 — 확인증의 「잠금 풀기」)를 본문 아래에 둘 수 있다(`action` 한 자리). 코드 변경은 04.3-10이 한다. **⑫의 §7-3 (아) 한 줄(행동 셀 = 로빙 셀 하나 · 안의 3차 `tabindex="-1"` · 셀에서 `Enter`/`Space` = 누름)은 이 태스크에서 하지 않는다** — `ui/table` 키 처리 · 단위 테스트와 한 슬라이스라 04.3-10 Task 2 ⓪-b가 DECISIONS 항목과 함께 한다(④가 04.3-09로 간 것과 같은 이유, Codex r3 C-1).
- **④(§6-10)는 이 태스크에서 하지 않는다** — `role-menu.ts`·`test/unit/ui/role-menu.test.ts`와 한 커밋이어야 표 파싱 테스트가 한순간도 깨지지 않으므로 04.3-09 Task 1이 한다.

커밋: `docs(04.3): revise SYSTEM tokens-free rules for certificates (③⑤⑥⑦⑧⑨⑩⑪⑫⑬)` + 한국어 본문(④가 04.3-09로 간 이유 한 줄).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/ui/system-md-compliance.test.ts test/unit/ui/role-menu.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 role-menu 테스트가 깨지면 §6-10 표를 건드린 것이다</fails_when>
    <automated>grep -q '930412-2\*\*\*\*\*\*' docs/design/SYSTEM.md &amp;&amp; grep -q '접수 중' docs/design/SYSTEM.md &amp;&amp; grep -q 'size="external"' docs/design/SYSTEM.md &amp;&amp; grep -q 'brightness(0)' docs/design/SYSTEM.md &amp;&amp; grep -q '브라우저 저장소에 보관하지 않는다' docs/design/SYSTEM.md &amp;&amp; grep -q '처리 중' docs/design/SYSTEM.md &amp;&amp; test -z "$(git diff docs/design/tokens.css)"</automated>
    <fails_when>개정 ⑥·⑤·⑦·⑬·⑪·⑦(c) 중 하나의 대표 문장이 없거나 `tokens.css`가 바뀌었다</fails_when>
  </verify>
  <acceptance_criteria>
    - §2-4 형식 표에 `930412-2******`와 `010-4821-7730` 두 줄이 있다
    - §7-5 의미 목록에 낱말 다섯(`접수 중` · `닫힘` · `제출됨` · `미제출` · `잠김`)이 있다
    - §7-1/§7-2에 `size="external"` · `aria-describedby` · `처리 중`이 있다
    - §6-6에 연결 전 칸 규칙(`—`)과 `brightness(0)` · `decode()` · `@media print` 줄이 있다
    - §7-3 (마)에 개인정보 칸 표 보관 금지 줄, (바)에 3차 한 개(링크 또는 확인 시트를 여는 줄 행동) 줄이 있다
    - §8 용어에 `확인증 행사` · `당첨자` · `전달` · `링크 닫기` · `구별 표시`가 있다
    - §6-10 정본 표의 diff가 없다(④는 04.3-09)
    - 세 단위 테스트가 녹색이다
  </acceptance_criteria>
  <done>④를 뺀 SYSTEM 개정 전부가 들어가 있고 기존 SYSTEM 단언 테스트가 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 계획 문서 → 실행자 | REQUIREMENTS·SYSTEM.md가 틀리면 실행자가 틀린 정본을 구현한다(보안 규칙 포함) |
| 디자인 정본 → 수령자 화면 | §6-5의 금지어·노출 규칙이 외부 화면 문구의 유일한 출처다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-02 | Information Disclosure | §6-5 목록 규칙(공유 링크로 보이는 것) | high | mitigate | §6-5에 「가린 이름 + 필요할 때만 경품 2행 + 필요할 때만 구별 표시(개인정보 아님)」 · 「제출 여부 미표시」를 정본 규칙으로 적는다. 실제 판정은 04.3-03·04.3-04의 서버 코드와 테스트가 한다 |
| T-04.3-22 | Information Disclosure | §6-6 · I4 브라우저 인쇄 | high | mitigate | ⑬ 「펼칠 수 있는 개인정보 칸은 `@media print`에서 저장된 마스킹 값만」을 시스템 규칙으로 올려 화면 하나의 선택이 아니게 한다 |
| T-04.3-26 | Information Disclosure | §7-3 (마) 편집 표 보관 | medium | mitigate | ⑪ 「개인정보 칸 표는 브라우저 저장소에 보관하지 않는다」를 규칙으로 올린다(04.3-04·04.3-10이 `useDirtyStorage`를 부르지 않는다) |
| T-04.3-27 | Repudiation | 추적표 | low | mitigate | 옮긴 둘 + 남긴 둘 = 원래 넷, 합계 86을 awk 단언으로 고정한다 |
</threat_model>

<verification>
- 네 개의 REQUIREMENTS/ROADMAP 검증 명령이 전부 종료 코드 0
- `test/unit/design-system-docs.test.ts` · `test/unit/ui/system-md-compliance.test.ts` · `test/unit/ui/role-menu.test.ts` 녹색
- `git diff --stat .planning/STATE.md docs/design/tokens.css`가 비어 있다
</verification>

<success_criteria>
- CERT-01/02가 Phase 04.3, CERT-03/04가 Phase 11에 속하고 합계 86이다
- 외부 수령자 화면·내부 화면·인쇄물의 새 규칙이 SYSTEM.md에 먼저 서 있어 04.3-02부터의 화면 코드가 정본을 읽는다
- 구별 표시 결정(묶음 행 없음)이 DECISIONS·SYSTEM에 적혀 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 요구사항 정렬 | `.planning/REQUIREMENTS.md` CERT-01 새 문구 · 추적표 CERT-01/02 → 04.3 · By phase 줄 |
| 로드맵 정렬 | `.planning/ROADMAP.md` Coverage 04.3 행 · 11 행 · Phase 목록 요약 줄 둘 |
| 디자인 결정 | `docs/design/DECISIONS.md` D-a(이름 고르기·전화번호 확인·구별 표시) · D-b(관리 인덱스 `확인증` 그룹) |
| 디자인 정본 | `docs/design/SYSTEM.md` §6-5 ①-a~l · §7-7 ② · §6-2 ③ · §7-5 ⑤ · §2-4 ⑥ · §7-1/§7-2 ⑦ · §7-15 ⑧ · §6-6 ⑨⑬ · §8 ⑩ · §7-3 ⑪⑫ |
| 실물 | `docs/design/system/external-cert.html` 장면 `#pick` `#verify` `#wrong` `#locked` `#locked-hard` `#submitted-slot` `#closed` `#notfound` |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-01-SUMMARY.md` when done
</output>
