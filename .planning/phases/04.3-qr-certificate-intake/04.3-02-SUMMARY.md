---
phase: 04.3-qr-certificate-intake
plan: 02
subsystem: external-intake
tags: [tracer, next-safe-action, drizzle, playwright, rrn-validation, signature-canvas, aes-gcm]

# Dependency graph
requires:
  - phase: 04.3-01
    provides: "SYSTEM.md §6-5 정본 · DECISIONS.md D-a/D-b · external-cert.html 실물 · 04.3-01 대응표"
provides:
  - "스키마 다섯 표 + 마이그레이션 둘(0017_cert_intake · 0018_cert_winners_unique_deferrable)"
  - "규약 C1(기능 게이트) · C2(오류 로그 개인정보 제거) · C3(서명 업로드 의도 표) · C4(E2E 격리 + 공용 도우미)"
  - "공개 domain 진입점 domain/certs/intake.ts(loadIntake·selectWinner·verifyLast4·submitCertificate)"
  - "수령자 QR 진입 → 이름 고르기 → 전화번호 확인 → 입력·서명 제출 → 자리 닫힘 375px E2E(certs 프로젝트)"
  - "주민등록번호 검증번호·세기·외국인·경계 전체 규칙(domain/certs/rrn.ts)"
  - "test/e2e/helpers/cert.ts 공용 도우미(createCertEvent·withCertFeatureOff·seedSubmittedCert·signaturePngFixture) — 뒤 플랜(03·04·06·07·09·10·11·13)이 그대로 쓴다"
affects: [04.3-03, 04.3-04, 04.3-05, 04.3-06, 04.3-07, 04.3-09, 04.3-10, 04.3-11, 04.3-12, 04.3-13]

# Actuals (#2632)
actuals:
  tokens: 43737
  tasks: 3
  commits: 19
  plan_head_before: ac4fbc4e5c7673699bcccfcf19262938eec73fbd

tech-stack:
  added: []
  patterns:
    - "공개 흐름 domain은 SYSTEM_VIEWER + 토큰 해시/행사 id 범위 좁히기만 쓰고 can·visible·scopeFor를 import하지 않는다(정적 테스트로 고정, public-route-boundary.test.ts)"
    - "증표 없음·불일치·시간 지남·동의 불일치는 모두 같은 kind(expiredProof)로 합쳐 공격자가 원인을 구별하지 못하게 한다"
    - "제출 트랜잭션 순서: 서식·로그 스위치를 의도 행·put보다 먼저 읽는다(E3-27, 고아 객체 방지) → 행사 잠금 → markWinnerSubmitted(0행이면 AlreadySubmittedSignal로 롤백) → allocateDocumentNumber → insertSubmission → deleteSignatureUploadIntent → recordAction({tx}) 순서로 한 tx"
    - "규약 C4 공용 도우미(test/e2e/helpers/cert.ts)는 domain을 직접 불러 표본을 만들고 @playwright/test를 import하지 않아 Vitest 통합 테스트도 재사용한다"

key-files:
  created:
    - domain/certs/format.ts
    - domain/certs/rrn.ts
    - domain/certs/roster-display.ts
    - domain/certs/consent.ts
    - domain/certs/events.ts
    - domain/certs/intake.ts
    - repositories/cert-events.ts
    - repositories/cert-winners.ts
    - repositories/cert-submissions.ts
    - lib/storage/signature-store.ts
    - "app/c/[token]/page.tsx"
    - "app/c/[token]/not-found.tsx"
    - "app/c/[token]/actions.ts"
    - "app/c/[token]/intake-flow.tsx"
    - "app/c/[token]/signature-pad.tsx"
    - "app/c/[token]/intake.module.css"
    - test/e2e/helpers/cert.ts
    - test/e2e/cert.setup.ts
    - test/e2e/mobile-cert-intake.spec.ts
    - test/integration/cert-intake.test.ts
    - test/integration/cert-crypto.test.ts
    - test/integration/cert-schema.test.ts
    - test/unit/certs/format.test.ts
    - test/unit/certs/rrn.test.ts
    - test/unit/certs/roster-display.test.ts
    - test/unit/certs/public-route-boundary.test.ts
    - test/unit/e2e/playwright-config.test.ts
  modified:
    - domain/settings/keys.ts
    - domain/document-numbering/index.ts
    - domain/permissions/menus.ts
    - db/client.ts
    - lib/db-transaction.ts
    - lib/actions/client.ts
    - lib/actions/zod-error-message.ts
    - eslint.config.mjs
    - next.config.ts
    - ui/button/Button.tsx
    - ui/button/Button.module.css
    - ui/input/TextField.tsx
    - ui/input/TextField.module.css
    - test/integration/document-numbering.test.ts
    - test/unit/actions/zod-error-message.test.ts
    - test/unit/ui/button.test.ts

key-decisions:
  - "증표 무효 사유 통합: 증표 없음·해시 불일치·시간 지남·동의 값 불일치를 모두 expiredProof 하나로 합쳤다(plan 원문은 「증표 없음·불일치는 거부」만 이름을 짓지 않았다 — 공격자가 「틀린 증표」와 「지난 증표」를 구별하지 못하게 하는 것이 의도에 더 맞다고 판단)"
  - "cert.setup.ts 담당자 계정은 고정 이메일(cert-manager-e2e@example.test)로 findUserByEmail 뒤 없으면만 만든다(멱등, 여러 번 실행돼도 중복 계정이 안 생긴다)"
  - "signaturePngFixture()·화면의 signature-pad.tsx 둘 다 PNG를 만들지만 경로가 다르다: 테스트 픽스처는 Node zlib.deflateSync + 직접 청크 조립, 화면은 브라우저 canvas.toDataURL(둘 다 표준 PNG라 서버의 isValidSignaturePng 검사(시그니처 바이트 + 184,320바이트 이하)를 똑같이 통과한다)"

patterns-established:
  - "당첨자 UNIQUE(event_id, name, phone) 지연 가능 제약은 이후 재생성에서 사라지면 test/integration/cert-schema.test.ts가 잡는다(코디네이터 지시로 Task 1 커밋의 빈 커버리지를 이 플랜에서 백필)"

requirements-completed: [CERT-01, CERT-02]

coverage:
  - id: D1
    description: "확인증 스키마 다섯 표 + 마이그레이션 둘(생성 + 당첨자 UNIQUE DEFERRABLE) 적용, 당첨자 UNIQUE가 DEFERRABLE INITIALLY IMMEDIATE임을 커밋된 테스트로 고정"
    requirement: "CERT-02"
    verification:
      - kind: integration
        ref: "test/integration/cert-schema.test.ts — condeferrable=true·condeferred=false"
        status: pass
      - kind: other
        ref: "pnpm lint:sql — 0 issues in 19 files"
        status: pass
    human_judgment: false
  - id: D2
    description: "규약 C1 두 겹 기능 게이트(환경 변수 CERT_FEATURE_ALLOWED + 설정 cert.enabled) — 스테이징만 환경 변수가 실리고 프로덕션은 없음, cert.enabled 기본 꺼짐"
    requirement: "CERT-01"
    verification:
      - kind: unit
        ref: "test/unit/certs/feature.test.ts(Task 1) · test/unit/deploy/deploy-sh.test.ts(Task 1)"
        status: pass
      - kind: integration
        ref: "test/integration/cert-intake.test.ts > 규약 C1 domain 두 겹째(설정 꺼짐)"
        status: pass
    human_judgment: false
  - id: D3
    description: "수령자 QR 진입 → 이름 고르기 → 전화번호 뒤 4자리 확인 → 입력·서명 제출 → 자리 닫힘, 새로 고침 뒤 같은 자리 재확인 시 이미 제출하셨습니다 — 375px Playwright E2E로 CI(certs 프로젝트, CI=true)에 있다"
    requirement: "CERT-01"
    verification:
      - kind: e2e
        ref: "test/e2e/mobile-cert-intake.spec.ts(375px, certs 프로젝트) — dev 모드 5/5, CI=true 5/5"
        status: pass
    human_judgment: false
  - id: D4
    description: "규약 C1 직접 POST — 켜진 상태에서 잡은 Next-Action 요청을 기능 끈 채 재전송하면 결과 없음·DB 변화 없음, 범위 밖 재전송은 정상"
    requirement: "CERT-01"
    verification:
      - kind: e2e
        ref: "test/e2e/mobile-cert-intake.spec.ts > 규약 C1 직접 POST"
        status: pass
    human_judgment: false
  - id: D5
    description: "주민등록번호 평문이 DB 어느 text 칸에도 없다 · rrn_encrypted가 v1: 접두 암호문 · rrn_masked 형식 · token_hash가 SHA-256 hex"
    requirement: "CERT-02"
    verification:
      - kind: integration
        ref: "test/integration/cert-crypto.test.ts(4개)"
        status: pass
    human_judgment: false
  - id: D6
    description: "주민등록번호 서버 규칙(형식·세기·미래 날짜 거부, 검증번호는 2020-10 이전 발급 추정 시에만 판정하고 거부하지 않음) 및 공개 목록 정렬·가림·겹침·구별 표시 규칙"
    requirement: "CERT-01"
    verification:
      - kind: unit
        ref: "test/unit/certs/rrn.test.ts(14) · test/unit/certs/format.test.ts(27) · test/unit/certs/roster-display.test.ts(10)"
        status: pass
    human_judgment: false
  - id: D7
    description: "공개 경로 경계 — app/c/**가 lib/viewer·domain/permissions/를 import하지 않고 publicActionClient는 app/c/ 바깥에서 쓰이지 않으며 domain/certs/intake.ts가 can·visible·scopeFor를 import하지 않는다"
    requirement: "CERT-02"
    verification:
      - kind: unit
        ref: "test/unit/certs/public-route-boundary.test.ts(3)"
        status: pass
    human_judgment: false
  - id: D8
    description: "동시 제출(풀 크기+1건)이 풀 교착 없이 20초 안에 전부 저장 · 제출 로그가 제출 tx 안에서 실패하면 롤백되고 같은 증표로 재제출 시 저장됨(E3-02) · 서식 읽기 실패가 고아 서명 객체를 남기지 않음(E3-27)"
    requirement: "CERT-02"
    verification:
      - kind: integration
        ref: "test/integration/cert-intake.test.ts > 풀 교착 없음 · E3-02 제출 로그 같은 tx · E3-27 순서"
        status: pass
    human_judgment: false
  - id: D9
    description: "확인증 E2E 프로젝트 격리(cert-setup·certs가 desktop·mobile-375 뒤에만 돌고 확인증 스펙이 certs에서만 잡힘) 및 당첨자 UNIQUE DEFERRABLE — Task 1 커밋(7d726e3·c268541)의 커버리지 공백을 코디네이터 지시로 백필"
    verification:
      - kind: unit
        ref: "test/unit/e2e/playwright-config.test.ts(5)"
        status: pass
      - kind: other
        ref: "pnpm playwright test --list | grep mobile-cert-intake — [certs]만 잡힘"
        status: pass
    human_judgment: false

# Metrics
duration: 세션 1회(연속 실행, Task 1은 이전 세션)
completed: 2026-09-26
status: complete
---

# Phase 04.3 Plan 02: QR 확인증 수령자 인테이크 트레이서 Summary

**수령자가 행사 QR 링크로 로그인 없이 들어와 이름을 고르고 전화번호 뒤 4자리로 확인한 뒤 개인정보·서명을 제출하는 한 경로 전체(스키마 → 설정 → domain → 리포지토리 → 공개 액션 → 외부 화면 → 375px Playwright E2E)를 세우고, 주민등록번호 검증번호·경계 규칙과 평문 부재·공개 경계 증명으로 굳혔다.**

## Performance

- **Tasks:** 3(Task 1은 이전 세션 완료·커밋됨 — 이 SUMMARY가 세 태스크 전부를 문서화한다)
- **Files created:** 27 · **Files modified:** 16 (총 44파일, +3354/-9줄)
- **Commits:** 16(이 세션) — Task 1은 별도 8커밋(163d612~ac4fbc4, 이전 세션)

## Accomplishments

- 확인증 공개 흐름의 유일한 domain 진입점 `domain/certs/intake.ts`(loadIntake·selectWinner·verifyLast4·submitCertificate)를 세우고, 리포지토리 셋(`cert-events`·`cert-winners`·`cert-submissions`)·서명 저장소 포트(`lib/storage/signature-store.ts`, 로컬 드라이버)·행사 등록(`domain/certs/events.ts`)을 함께 만들었다
- 규약 C1(기능 게이트)~C4(E2E 격리) 위에서 트레이서 한 경로를 완성했다 — 서식·로그 스위치를 서명 업로드보다 먼저 읽는 순서(E3-27), 제출 로그를 제출 트랜잭션 안에서 쓰는 순서(E3-02), 동시 제출 풀 교착 방지(T-04.3-140)를 통합 테스트로 고정했다
- 외부 수령자 화면(`app/c/[token]/`)을 SYSTEM.md §6-5 그대로 세우고(E2 이름 고르기 → E3 전화번호 확인 → E4 입력·서명 → E5 제출됨/E6 계열), 375px Playwright E2E(`mobile-cert-intake.spec.ts`)로 정상 제출·재확인·규약 C1 직접 POST·플래그 꺼짐·잘못된 토큰 다섯 시나리오를 CI(certs 프로젝트, `CI=true`)까지 통과시켰다
- 주민등록번호 서버 규칙(성별 코드 → 세기, 미래 날짜 거부, 검증번호는 2020-10 이전 발급 추정 시에만 판정하고 거부하지 않음)과 가림·전화 형식·공개 경계·평문 부재를 단위·통합 테스트로 굳혔다(Task 3)
- 코디네이터 지시(PR #88)로 Task 1의 두 커밋(Playwright 프로젝트 격리·당첨자 UNIQUE DEFERRABLE)이 ad-hoc `<verify>` 명령으로만 확인되고 커밋된 회귀 테스트가 없던 공백을 백필했다

## Task Commits

Task 1(스키마·마이그레이션·규약 C1·C2·C4·journal 가드)은 이전 세션에서 커밋됐다(8개, `163d612`~`ac4fbc4` — 자세한 내용은 인계 문서 `.continue-here.md` 참고). 이 세션이 이어서 만든 19개:

1. `dc35bf3` test — Task 1 백필(playwright-config·cert-schema, 코디네이터 지시)
2. `9abc381` test — cert format/rrn/roster-display 실패 테스트(RED)
3. `08a0ae9` feat — cert format/rrn/roster-display 구현(GREEN)
4. `dcaf0ac` test — zod custom issue passthrough 실패 테스트(RED)
5. `6c6d091` fix — koreanZodErrorMessage custom 분기(GREEN)
6. `dc28d66` test — cert 문서 번호 서식 실패 테스트(RED)
7. `53fca39` feat — 설정 키·문서 번호·메뉴·DbOrTx 넓히기(GREEN)
8. `acf11ae` test — recipient intake tracer 통합 실패 테스트(RED)
9. `bd0e708` feat — 리포지토리·서명 저장소·events/intake domain(GREEN)
10. `709b7dc` feat — publicActionClient·eslint 허용 목록·next.config headers
11. `3c5bd4e` test — Button/TextField external 실패 테스트(RED)
12. `781f6fa` feat — Button/TextField external 구현(GREEN)
13. `72726f5` feat — 외부 수령자 화면(page·actions·intake-flow·signature-pad)
14. `0e48adc` test — mobile E2E(cert.setup.ts + mobile-cert-intake.spec.ts)
15. `de435d7` test — Task 3 rrn/consent/boundary 나머지 통합·단위 테스트
16. `5693e3d` fix — 서명 「다시 쓰기」 버튼 배선(Rule 2)
17. `0fbf68e` docs — 이 SUMMARY(1차 커밋, 16개 시점)
18. `52756d9` fix — recordAction/@playwright-test 리터럴 acceptance grep 정합
19. `cb014d4` fix — domain/certs/intake.ts의 lib/viewer 거짓 양성 주석 정정

## RED 기록

- `test/unit/certs/format.test.ts`: `Cannot find package '@/domain/certs/format'`
- `test/unit/certs/rrn.test.ts`(Task 2 범위): `Cannot find package '@/domain/certs/rrn'`
- `test/unit/certs/rrn.test.ts`(Task 3 전체 규칙로 재작성): `930412-2123458` 기대값이 `checkDigit: "match"`인데 Task 2 구현은 `"notApplicable"`만 내 실패(외국인·9·0·경계 케이스도 함께 실패)
- `test/unit/certs/roster-display.test.ts`: `Cannot find package '@/domain/certs/roster-display'`
- `test/unit/actions/zod-error-message.test.ts`(custom 케이스): `expected '입력값이 올바르지 않습니다 · 값을 확인해 주세요' to be '전화번호 형식이 아닙니다 · 02-1234-5678처럼 적어 주세요'`
- `test/integration/document-numbering.test.ts`(cert counterKey): `document-numbering: counterKey 'cert'의 서식 설정이 등록되지 않았습니다.`
- `test/integration/cert-intake.test.ts`(정상 제출 등 전부): `Cannot find package '@/domain/certs/events'`
- `test/unit/ui/button.test.ts`(size external): `expected '<span class="_wrap_...">...' to contain '_wrapExternal_...'`(styles.wrapExternal이 아직 없음)
- `test/unit/ui/text-field.test.ts`(size external): 같은 이유로 `styles.external`이 아직 없음

## Files Created/Modified

domain 계층: `domain/certs/{format,rrn,roster-display,consent,events,intake}.ts` · `domain/settings/keys.ts`(여덟 키 추가) · `domain/document-numbering/index.ts`(cert 서식) · `domain/permissions/menus.ts`(certs.events)
리포지토리·인프라: `repositories/cert-{events,winners,submissions}.ts` · `lib/storage/signature-store.ts` · `db/client.ts`(DbOrTx delete·execute) · `lib/db-transaction.ts`(주석) · `lib/actions/client.ts`(publicActionClient) · `lib/actions/zod-error-message.ts`(custom 분기) · `eslint.config.mjs` · `next.config.ts`(headers)
화면: `app/c/[token]/{page,not-found,actions,intake-flow,signature-pad}.tsx` · `intake.module.css` · `ui/button/Button.{tsx,module.css}` · `ui/input/TextField.{tsx,module.css}`(size="external")
테스트: `test/e2e/{cert.setup.ts,mobile-cert-intake.spec.ts,helpers/cert.ts}` · `test/integration/cert-{intake,crypto,schema}.test.ts` · `test/integration/document-numbering.test.ts`(cert 케이스 추가) · `test/unit/certs/{format,rrn,roster-display,public-route-boundary}.test.ts` · `test/unit/e2e/playwright-config.test.ts` · `test/unit/actions/zod-error-message.test.ts`(custom 케이스) · `test/unit/ui/{button,text-field}.test.ts`

## Decisions Made

- 증표 무효 사유(없음·불일치·시간 지남·동의 값 불일치)를 전부 `{kind: "expiredProof"}` 하나로 합쳤다 — plan 원문이 「거부」라고만 쓴 부분을 이렇게 구체화했다(위 key-decisions와 동일 근거).
- cert.setup.ts의 담당자 계정은 고정 이메일로 조회 뒤 없을 때만 생성해 멱등하게 만들었다.
- 서명 PNG는 테스트 픽스처(Node zlib)와 실제 화면(브라우저 canvas)이 서로 다른 경로로 만들지만 둘 다 표준 PNG라 서버 검사를 동일하게 통과한다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - 누락된 핵심 기능] koreanZodErrorMessage가 커스텀 refine 메시지를 뭉갬**
- **Found during:** CERT_CONTACT_PHONE 설정 스키마 작성(Task 2 ④)
- **Issue:** `describeIssue`의 switch에 `"custom"` 분기가 없어 `.refine()`이 지어 붙인 문장이 default 문구("입력값이 올바르지 않습니다 · 값을 확인해 주세요")로 뭉개졌다. CERT_CONTACT_PHONE의 "전화번호 형식이 아닙니다 · 02-1234-5678처럼 적어 주세요"가 화면에 나가지 않았을 것이다.
- **Fix:** `case "custom": return issue.message;` 한 줄 추가(lib/actions/zod-error-message.ts)
- **Files modified:** lib/actions/zod-error-message.ts, test/unit/actions/zod-error-message.test.ts
- **Verification:** 새 단위 테스트 통과, 기존 3케이스 diff 없음
- **Committed in:** dcaf0ac(test) → 6c6d091(fix)

**2. [Rule 2 - 누락된 핵심 기능] SYSTEM.md §6-5 「다시 쓰기」 버튼 누락**
- **Found during:** 화면 커밋 뒤 Known-Stubs 자체 점검
- **Issue:** signature-pad.tsx는 `clear()` 핸들 메서드를 노출했지만 intake-flow.tsx가 그 버튼을 렌더하지 않았다 — 서명한 뒤 다시 그릴 방법이 없었다.
- **Fix:** 서명이 있을 때만 3차 「다시 쓰기」 버튼을 더해 `signatureRef.current?.clear()`를 부른다.
- **Files modified:** app/c/[token]/intake-flow.tsx
- **Verification:** typecheck·lint 녹색, 375px E2E(dev·CI=true 각 5/5) 재확인
- **Committed in:** 5693e3d

**3. [Rule 1 - 버그] 통합 테스트 자체의 오입력(행사 경계 케이스)**
- **Found during:** Task 3 행사 경계 테스트 작성
- **Issue:** `makeEvent({ name: "행사A" })`처럼 불렀는데 도우미의 `name` 오버라이드는 **당첨자** 이름 자리라 "김하늘" 대신 "행사A"라는 당첨자가 생겨 `winnerIdOf(..., "김하늘")`가 못 찾았다.
- **Fix:** `name` 오버라이드를 빼고(행사 이름은 createCertEvent가 항상 접미사를 붙여 유일하다) 필요한 경우만 `phone`을 오버라이드했다.
- **Files modified:** test/integration/cert-intake.test.ts
- **Verification:** 18/18 통과 확인

**4. [코디네이터 지시 — 백필] Task 1 커밋 7d726e3·c268541의 커버리지 공백**
- **Found during:** 세션 중 코디네이터 지시(PR #88) 수신 — "Task 1 커밋에 적절한 테스트가 있는지 확인하고, 얕으면 백필해 SUMMARY에 기록하라"
- **Issue:** Playwright `cert-setup`/`certs` 프로젝트 격리(7d726e3)와 당첨자 UNIQUE DEFERRABLE(c268541)이 계획의 `<verify>` ad-hoc 명령(수동 `grep`·`pnpm playwright test --list`·`node -e` pg 조회)으로만 확인됐고 커밋된 회귀 테스트가 없었다.
- **Fix:** `test/unit/e2e/playwright-config.test.ts`(5케이스)와 `test/integration/cert-schema.test.ts`(1케이스)를 더했다. 둘 다 이미 있는 동작을 고정하는 백필이라 처음부터 녹색이었다(TDD RED 대상이 아니다 — 새 행동이 아니라 기존 행동의 회귀 방지).
- **Committed in:** dc35bf3

**5. [Rule 1 - 버그] SUMMARY 작성 뒤 acceptance grep 재확인에서 발견한 리터럴 불일치 둘**
- **Found during:** 최종 verification 단계에서 Task 2 `<acceptance_criteria>`의 정확한 grep 문자열을 다시 돌려봄
- **Issue A:** `domain/certs/intake.ts`가 `recordAction` import를 `as defaultRecordAction`으로 별칭하고 호출도 여러 줄로 나눠 적어 `grep -q "recordAction(SYSTEM_VIEWER" domain/certs/intake.ts`(대문자 R 없이, 한 줄)가 실패했다(실제 동작은 이미 맞았다 — 리터럴 검사만 어긋났다).
- **Issue B:** `test/e2e/helpers/cert.ts` 머리 주석이 "이 파일은 `@playwright/test`를 import하지 않는다"를 설명하며 그 패키지 이름 자체를 코드에 남겨 `grep -c "@playwright/test" test/e2e/helpers/cert.ts`가 1을 냈다(실제 import는 없었다).
- **Issue C:** 같은 파일의 domain 진입점 주석이 "lib/viewer를 import하지 않는다"고 설명하며 `grep -rEn "lib/viewer|domain/permissions" app/c domain/certs/intake.ts`(0줄 기대)를 깨뜨렸다.
- **Fix:** import 별칭을 없애고 호출을 한 줄로(Issue A), 주석에서 패키지·경로 리터럴을 산문으로 풀어 썼다(Issue B·C). 동작 변화 없음.
- **Files modified:** domain/certs/intake.ts, test/e2e/helpers/cert.ts
- **Verification:** 세 grep 전부 재확인 통과, typecheck·lint 녹색, cert-intake·cert-crypto·cert-schema 통합 23/23, mobile E2E(dev) 5/5 재확인
- **Committed in:** 52756d9(recordAction·@playwright/test) → cb014d4(lib/viewer)

---

**Total deviations:** 5(Rule 2 × 2, Rule 1 × 2, 코디네이터 지시 백필 × 1 — 5번은 Rule 1으로 집계)
**Impact on plan:** 모두 정확성·완결성에 필요한 수정이었고 범위 확대는 없었다.

## Known Stubs

- **키보드로 서명 그리기(Space/Enter 펜 대기/떼기, 방향키 8px 이동, `aria-live` 상태 알림)** — `app/c/[token]/signature-pad.tsx`는 Pointer Events(마우스·터치·펜)만 구현했다. UI-SPEC ①-i·SYSTEM.md §6-5가 요구하는 접근성 기능이지만, 04.3-06이 `domain/certs/signature-ink.ts`(잉크 픽셀 판정 공유 모듈)를 만들며 서명 계약 전체를 다시 다루기로 계획돼 있어 이 트레이서에서는 포인터 입력까지만 완성했다. 04.3-06 전까지는 키보드 전용 사용자가 서명을 남길 방법이 없다.
- **뒤 4자리 짧은 잠김·누적 잠김 UI, 멱등 키 재생 UI** — 계획이 명시적으로 04.3-03 몫으로 미룬 기능 빈자리(SYSTEM.md §6-5의 「짧은 잠김」·「누적 잠김」·「재확인」 문단). domain의 `verifyLast4`는 이 서명을 이미 받지만(idemKey 인자) Task 2 범위에서는 사용하지 않는다(`void idemKey`).
- **서명 「서명 있음」 판정(잉크 픽셀 288개 이상)** — 이 트레이서는 서버에서 PNG 시그니처·크기만 확인한다(`isValidSignaturePng`). 04.3-06이 `inspectSignaturePng`·`countInkPixels`로 대체할 예정(계획에 명시된 예정된 교체이지 결함이 아니다).

이 셋 모두 계획이 명시적으로 뒤 플랜(04.3-03·06) 몫으로 분류한 「기능 빈자리」이지 이 플랜의 목표(정상 제출 경로 완성) 달성을 막지 않는다.

## Threat Flags

없음 — 이 플랜이 만든 새 표면(공개 라우트·리포지토리·서명 저장소)은 모두 plan의 `<threat_model>`에 이미 등록돼 있다.

## Issues Encountered

- **통합 테스트와 E2E 동시 실행 충돌(E3-33 위반 자체 발견·수정):** 전체 통합 스위트(`pnpm vitest run --project integration`, 수백 개 파일)를 백그라운드로 돌려 둔 채 Playwright E2E(`cert-setup`)를 실행했더니 두 프로세스가 같은 `erp_test` DB를 동시에 건드려 `seedMasterData`가 `visibility_matrix_role_id_roles_id_fk` FK 위반으로 실패했다. 근본 원인은 통합 테스트의 `beforeEach` TRUNCATE가 E2E `globalSetup`이 방금 시드한 계급 행을 지운 것 — 계획의 실행 조건(E3-33 "통합·E2E는 한 번에 하나")을 실제로 어긴 것이었다. 백그라운드 통합 프로세스를 종료(`pkill`)하고 두 스위트를 차례로(E2E 먼저, 그다음 전체 통합 스위트) 다시 돌려 각각 깨끗하게 통과함을 확인했다 — 최종 SUMMARY의 수치는 이 재실행 결과다.
- **세션 중 코디네이터 지시 수신(PR #88):** RED→GREEN 커밋 순서·Task 1 커버리지 백필·커밋 제목 규칙을 지시받아 그대로 따랐다(위 Deviations 4번, 커밋 목록 참고). `test/integration/cert-intake.test.ts`는 Task 2 범위와 Task 3 범위가 한 파일에 이어 붙어 있어, Task 3분 커밋 전에 파일 끝의 두 describe 블록만 잘라내 Task 2 버전으로 되돌린 뒤 커밋하고 다시 이어 붙이는 방식으로 커밋 경계를 맞췄다.

## User Setup Required

None — 외부 서비스 설정 없음.

## Next Phase Readiness

- 04.3-03(틀림·잠금·멱등)·04.3-04(행사 만들기 내부 화면)·04.3-06(폼 검사·서명 계약 확정)·04.3-07(경영관리 열람)·04.3-09~13이 이 플랜의 규약 C1~C4, `test/e2e/helpers/cert.ts` 공용 도우미, `domain/certs/intake.ts` 결과 유니온을 그대로 이어 쓸 수 있다.
- 04.3-06은 signature-pad.tsx의 키보드 입력과 `inspectSignaturePng`/`countInkPixels` 교체를 맡는다(Known Stubs 참고).
- 04.3-03은 `verifyLast4`의 `idemKey` 인자를 실제로 쓰기 시작하고 짧은/누적 잠김을 더한다.
- 남은 위험: 이 세션은 기존 `desktop`·`mobile-375` 전체 E2E 스위트를 재실행하지 않았다(시간 제약) — Button·TextField의 새 `size` prop과 eslint 허용 목록 추가는 기존 호출부에 diff가 없고 단위 테스트(button 9/9·text-field 2/2)로 회귀를 확인했지만, 전체 E2E 재확인은 다음 검증 단계(`/gsd-verify-work` 또는 오케스트레이터의 페이즈 종료 검증)에서 한 번은 돌아볼 가치가 있다.

## Self-Check: PASSED

모든 생성 파일이 디스크에 있음을 확인했다(위 「Files Created/Modified」 목록 27개 전부 `find`/`ls`로 존재 확인). 19개 커밋 전부 `git log --oneline`에 존재(`git rev-list --count ac4fbc4..HEAD` = 19). 단위 1377/1377 · 통합 1477/1477(전체 스위트, 격리 재실행) · 통합(cert 세 파일 재확인) 23/23 · E2E 확인증 스펙 5/5(dev 모드, 2회 재확인) · 5/5(`CI=true`) · `pnpm build`·`pnpm lint`·`pnpm typecheck`·`pnpm lint:sql` 전부 종료 코드 0(마지막 두 fix 커밋 이후 재실행 포함).

---
*Phase: 04.3-qr-certificate-intake*
*Plan: 02*
*Completed: 2026-09-26*
