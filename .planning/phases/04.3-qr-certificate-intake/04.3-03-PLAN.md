---
phase: 04.3-qr-certificate-intake
plan: 03
type: execute
wave: 3
depends_on: ["04.3-02"]
files_modified:
  - domain/settings/keys.ts
  - domain/certs/verify-lock.ts
  - domain/certs/intake.ts
  - repositories/cert-winners.ts
  - app/c/[token]/actions.ts
  - ui/button/Button.tsx
  - ui/button/Button.module.css
  - app/c/[token]/intake-flow.tsx
  - app/c/[token]/flow-rules.ts
  - app/c/[token]/error.tsx
  - app/c/[token]/intake.module.css
  - test/unit/certs/verify-last4.test.ts
  - test/unit/certs/flow-rules.test.ts
  - test/unit/ui/button-a11y.test.ts
  - test/integration/cert-verify.test.ts
  - test/e2e/mobile-cert-verify.spec.ts
  - test/e2e/helpers/cert.ts   # seedSubmittedCert의 verifyLast4 호출 줄만 — ④ (a)가 입력에 ip를 더한다(04.3-02 C4 규칙, E3-32)
autonomous: true
requirements: [CERT-01]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "전화번호 뒤 4자리가 틀리면 칸이 `--danger` 테두리 + `aria-invalid`가 되고 칸 아래 `전화번호 뒤 4자리가 맞지 않습니다 · 다시 적어 주세요 · 남은 횟수 {n}번`(`aria-live`)이 서며 입력값은 지워지고 포커스는 칸에 남는다"
    - "틀림 한도(설정 `cert.verify.max_attempts`, 기본 5)에 닿으면 그 자리가 잠금 시간(설정 `cert.verify.lock_minutes`, 기본 3분) 동안 잠긴다 — 입력·1차 비활성, 제출 줄 `틀린 번호가 {한도}번 들어와 확인이 잠겼습니다 · {HH:mm}부터 다시 해 주세요`(누가 틀렸는지 말하지 않는다) + 문의 2행. 한도·시각은 서버 값이고 클라이언트에 숫자가 박혀 있지 않다"
    - "잠금은 당첨자 자리 단위다(IP 단위 아님). 잠긴 자리를 E2에서 다시 고르면 E3가 곧바로 잠김 상태로 선다. 잠금이 풀린 뒤 첫 틀림은 1부터 다시 센다. 맞히면 **짧은 잠김의** 틀림 횟수(`failed_attempts`)가 0이 된다 — 누적 셈은 줄지 않는다(아래)"
    - "누적 잠김(소유자 결정 2026-09-24 21:56 「누적 잠금 넣기로 해」): 자리마다 틀림을 `cumulative_failed_attempts`로도 센다 — 짧은 잠김이 3분 뒤 풀려도 · 맞혀도 0이 되지 않고, 같은 멱등 키의 재전송은 세지 않으며, 담당자의 I3 「잠금 풀기」(04.3-10)로만 0이 된다. 누적이 **고정 상수 20**(`VERIFY_HARD_LOCK_THRESHOLD` — 설정 키가 아니다)에 닿는 틀림은 그 자리에 `hard_locked_at`(잠근 순간)을 찍고 `hardLocked`를 돌려준다. 20 = 4 × 5라 기본값에서는 넷째 짧은 잠김이 걸릴 자리에서 걸리고, 둘이 함께 걸리면 누적 잠김이 앞선다(짧은 잠김 해제 시각을 쓰지 않는다). 누적 잠긴 자리는 맞든 틀리든 판정하지 않고 `hardLocked`다(세지 않음 · 맵에 넣지 않음)"
    - "E3 `남은 횟수 {n}번`의 `{n}`은 짧은 잠김까지 남은 횟수뿐이다 — 누적 한도·누적 횟수는 응답에도 화면에도 없다(UI-SPEC Copywriting E3 틀림)"
    - "E3 누적 잠김 상태(UI-SPEC E3 「누적 잠김」): 칸 값 지움 · 입력·1차 비활성 · 해제 시각·카운트다운·횟수 없음 · 클라이언트 풀림 타이머 없음. 1차 바로 뒤의 누적 잠김 묶음 `<div id tabindex=\"-1\">`(`aria-live` 없음 — 포커스 이동만으로 읽힌다, 두 번 읽힘 방지 · Codex UI 7차)에 UI-SPEC Copywriting 「E3 누적 잠김」 두 줄(1행 이유 · 2행 담당자·문의 전화 `tel:` 줄 — 각각 id를 가진 `<p>` · 풀린 뒤 할 일을 적은 셋째 줄은 없다)과 2차 「잠금 확인」 · 실패했을 때만 그리는 실패 줄이 들어가고, 1차는 `disabledReason` 없이 호출부 `aria-describedby`로 **두 `<p>`의 id 둘만** 받는다(묶음 id도 실패 줄 id도 아니다 — 4차 E3 계약 · `Button` 개발 경고는 호출부 `aria-describedby`를 이유로 인정 — 개정 ⑦ (b)). **복구 길(글 대신 동작 — 잠금 다시 확인):** 누적 잠김인 동안 화면이 다시 보일 때(`visibilitychange` visible · `pageshow`)와 묶음 안 2차 「잠금 확인」을 누를 때, 고른 자리의 **잠금 상태만** 서버에 다시 묻는다(`recheckLockAction` — 답은 넷 중 하나뿐: `closed{reason}`(링크 단위 닫힘 — 어떤 잠김보다 앞선다) · `hardLocked` · `shortLocked{unlockAt, remainingSec, limit}`(`limit` = 짧은 잠김 한도 설정값 — 곧바로 누적 잠김으로 들어온 사람도 기존 짧은 잠김 문구를 그릴 수 있게) · `open`. 개인별 제출 여부는 싣지 않아 이 길로는 E6-a에 가지 않는다 — E6-a는 확인을 통과한 `verifyLast4`로만). `closed` → E6-b(기존 닫힘 처리 · E6-b 포커스 규칙 그대로) · `open` → 보통 확인 입력이 살아나고 포커스 = 칸 · `shortLocked` → 같은 `tabindex=-1` 묶음 안에 서버 해제 시각의 짧은 잠김 줄(각각 id를 가진 `<p>`)을 그리고 포커스 = 묶음 + 자동 풀림 마감 — 이때 1차는 `disabledReason` 문자열 없이 `aria-describedby` = 그 두 `<p>` id(버튼 아래 같은 글이 두 번 서지 않게, UI-SPEC 6차 손질 3) · `hardLocked` → 글을 그대로 둔다. **포커스·알림(4차 E3 계약):** 보임 이벤트의 다시 확인은 상태가 바뀔 때(`closed` · `open` · `shortLocked`)만 위대로 포커스를 옮기고 `hardLocked` · 실패는 조용하다(포커스·글 불변). 포커스 규칙과 실패 줄 규칙은 따로다(UI-SPEC 6차 손질 2): 「잠금 확인」 누름의 결과가 `hardLocked`면 실패 줄 없이 포커스만 묶음으로 옮기고(`aria-live` 없이 포커스 이동으로 읽힌다), 실패·결과 불명이면 포커스를 묶음으로 옮기고 묶음 안에 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요` 한 줄을 세우며(1차는 비활성 그대로) 그 줄이 있는 동안만 「잠금 확인」의 `aria-describedby`가 그 줄 id를 가리킨다. 버튼은 살아 있어 다시 누르고, **누르는 즉시(응답 전)** 줄과 그 연결이 함께 지워지며 성공 응답이면 지워진 채다. 다시 확인은 읽기라 어떤 셈도 더하지 않고 한 번에 하나만 나간다. 누적 잠김으로 들어오는 모든 길(마지막 틀림 응답 · 재전송 응답 · E2에서 누적 잠긴 자리 고름)에서 포커스가 그 묶음으로 간다. 3차 「다른 이름 고르기」는 살아 있다(복구 길)"
    - "잠금 풀림: 서버가 준 남은 초로 잡은 마감이 지나면(또는 `visibilitychange` visible · `pageshow` 때 지났으면) 잠김 줄·오류 표시가 지워지고 입력이 다시 살아나며 1차는 원래 막힘 이유로 돌아간다. 포커스가 문서 밖(`body`)이거나 잠김 묶음 안(묶음 자체 · 지워지는 요소) · 1차 「전화번호 확인」 위(풀린 뒤에도 칸이 비어 비활성 — /plan-design-review D-8)이면 칸으로 옮기고, 사용자가 다른 곳(문의 줄 `tel:` 등)으로 옮겨 뒀으면 그대로 둔다(UI-SPEC 6차 손질 1 · E3 「잠금 풀림」). 판정은 여전히 서버다"
    - "E3 1차 막힘 이유 `뒤 4자리 숫자를 적으면 확인할 수 있습니다`(첫 진입 · 풀린 뒤)는 `--muted`(`reasonTone: info` — 외부 화면의 손대지 않은 막힘, §6-5 개정 ①-m ②)이고, 짧은 잠김 1행은 `--danger`(`block`)다 — 수령자의 첫 화면에 붉은 글자가 서지 않는다(X-4 · 반영 검사 r1 B-2)"
    - "전화번호 뒤 4자리는 사용자가 고른 **한 사람**과만 대조한다 — 행 하나가 여러 사람을 가리키는 일이 없다(오케스트레이터 결정 2026-09-24, 구별 표시). 남의 자리로 확인을 통과할 수 없다"
    - "E3 확인 응답이 20초 안에 오지 않거나 네트워크·서버가 실패하면 제출 줄에 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요` — 1차는 살아 있고 적은 4자리는 남으며 남은 횟수를 말하지 않는다. 다시 누르면 같은 멱등 키가 가서(키는 확정 판정을 받을 때만 끝난다) 서버는 첫 요청이 상태를 바꾼 결과(틀림 · 맞음)면 그것을 돌려주고 두 번 세지 않으며, 아니면 지금 다시 판정한다. 4자리를 고치면 새 키다"
    - "E2 행 누름(`selectWinner`)이 실패하면 E2에 남고 목록 라벨 아래 `이름을 불러오지 못했습니다 · 잠시 뒤 다시 골라 주세요`(`role=\"alert\"`)가 서며 목록은 다시 살아나고 포커스는 누른 행에 남는다. 다음 행 누름에서 지워진다"
    - "E2 행 누름·E3 확인 도중 링크가 닫혔으면(기한 · 모두 제출 · 담당자가 닫음) 화면 전체가 E6-b(서버가 준 사유의 문장)로 바뀌고 고른 이름·적은 4자리는 버려진다. 클라이언트는 기한 시각을 스스로 재지 않는다"
    - "제출 여부는 목록에도, 행을 고르는 것에도 드러나지 않는다 — E6-a `이미 제출하셨습니다`는 그 자리의 뒤 4자리를 맞힌 뒤에만 선다(UI Considerations 보강 partial E2)"
    - "한 행 = 한 사람이다. 가린 이름이 겹치거나 구별 표시가 있는 행만 2행 `{경품명} {수량}개( · {구별 표시})`를 늘 보이고, 같은 모양의 행은 저장 때 막혀 E2에 둘 서지 않는다(UI Considerations zero-one-many E2 · A11)"
    - "목록이 50명을 넘어도 한 목록 스크롤이고 페이지·검색이 없다(UI Considerations overflow E2)"
    - "목록 아래 `목록에 이름이 없으면 PLANT8 경영관리 {문의 전화}에 전화해 주세요`(번호는 `tel:` 3차, `tel:` 값은 숫자만)와 잠김 2행은 행사의 사본 번호(`cert_events.contact_phone`)로 늘 선다 — 행사를 만든 뒤 설정을 바꾸거나 비워도 그 행사 화면의 번호는 그대로이고, 번호 없는 대체 문장은 없다(UI-SPEC ①-l · A12)"
    - "외부 화면의 모든 서버 왕복은 상단 2px `--accent` 진행 바(300ms 안에 끝나면 안 보임)와 버튼 라벨 뒤 `…` + `.sr-only` `처리 중` + 다른 버튼 비활성으로 진행 중을 알린다(UI Considerations loading E)"
    - "`Button`의 비활성 이유 연결(`useId` id + `aria-describedby`, 호출부 `aria-describedby`와 합침)과 `aria-disabled`는 Phase 4(⑦ · DR-11)가 이미 한 것이고 Phase 4 `test/unit/ui/button.test.ts`가 녹색 그대로다. 이 플랜이 더하는 것은 둘뿐이다 — `pending`이면 `.sr-only` `처리 중`이 읽히고(04.3-⑦(c)), 개발 경고는 `disabledReason`도 호출부 `aria-describedby`도 없을 때만 난다(04.3-⑦(b) — E3-01). 기존 호출부의 모양은 그대로다"
    - "단계가 바뀔 때 포커스가 옮겨진다(E2 → E3 = 4자리 칸, E3 → E2 = 방금 고른 행)와 `document.title`이 `{단계} · 기타소득 지급 확인`으로 바뀐다. 브라우저 기록 항목은 둘뿐이다 — E2 → E3는 `pushState`, E3 → 뒤 단계는 `replaceState`, 뒤로 가기는 언제나 E2이고 메모리의 E3·E4 상태를 지운다"
    - "고아 기록 항목 규칙(UI-SPEC E3 「뒤로 가기」): 화면이 E2가 아닌 기록 항목(`history.state.step`이 `verify`·`form` 등) 위에 있는데 메모리에 그 단계가 없으면 — 앞으로 가기(`popstate`) · 첫 마운트(새로 고침) · bfcache 복원(`pageshow`의 `persisted`) 어느 경우든 — E3·E4 메모리를 비우고 E2를 그린 뒤 `history.back()`으로 E2 항목에 돌아간다. `replaceState`로 그 항목을 E2로 바꾸지 않는다(E2 항목이 둘이 된다)"
    - "throttled(아래 속도 제한) 응답은 확정 판정이 아니다(키에 묶이지 않는다) — 화면 처리는 결과 불명과 같고(4자리 유지 · 같은 멱등 키 · 1차 살아 있음) 문장만 UI-SPEC Copywriting 「E3 확인 잠시 멈춤」 `확인이 잠시 멈췄습니다 · 잠시 뒤 다시 눌러 주세요`다(/plan-design-review D-3)"
    - "CERT-01 × boundary: 틀림 한도 5에서 5번째 틀림이 곧바로 잠김을 돌려준다(4번째는 `남은 횟수 1번`)"
    - "CERT-01 × ordering: 목록은 실제 이름 가나다순(같으면 등록 순서)으로 정렬된 뒤 가려진다 — 가린 모양으로 정렬하지 않는다"
    - "CERT-01 × precision: 잠금 해제 시각은 분 단위로 올림해 보인다(해제 18:44:10 → `18:45`) — 보인 시각에 다시 눌러 잠김을 또 받지 않는다. 남은 초는 올림 정수다"
    - "CERT-01 × idempotency(확인): 상태를 바꾼 결과(틀림 셈 · 맞음)는 **키마다** 그 자리 행에 남는다(키 해시 → 결과, 보관 60분) — 같은 키가 다시 오면 끼어든 다른 키가 있어도(A 처리 → B 처리 → A 재전송) 첫 결과를 돌려주고 틀림 횟수를 더하지 않는다. 동시에 온 같은 키 둘도 한 번만 센다. 키는 해시로만 저장되고 그 자리에만 묶인다. 60분이 지난 항목은 **읽을 때도** 없는 것으로 본다(쓸 때 정리만이 아니다 — 61분 뒤 재전송 = 새 시도). 쓸 때마다 60분 지난 항목을 빼고 `ok` 항목만 20개로 묶는다(넘치면 가장 오래된 `ok`부터 뺀다) — `wrong` 항목은 개수로 빼지 않고 60분 나이로만 빠진다(속도 제한 셈이 이 `wrong` 항목을 세므로 캐시 축출이 셈을 줄이지 않는다 — 최종 리뷰 B1). `wrong` 항목은 60분 보관 · 15분 창이라 행사 전체에서 최대 약 **4 × `eventMissLimit(명단 수)`개**(= 4×L, 기본 `max(40, ceil(명단 × 0.5))`)로 묶여 맵이 끝없이 크지 않다(교차 B1 — 보관이 창의 4배라 상한도 4배다)"
    - "CERT-01 × idempotency(증표): 맞음 결과의 재생은 **처음 발급한 그 증표와 원래 만료 시각**을 돌려주고 자리의 현재 증표를 바꾸지 않는다 — 늦게 온 재전송이 브라우저가 이미 받은 더 새 증표를 무효로 만들지 않는다(UI-SPEC E1 「맞음(확인 증표, 원래 만료 시각 그대로)」). 재생용 증표는 `encrypt()` 암호문으로만 두고 평문 증표는 어디에도 저장하지 않는다"
    - "CERT-01 × idempotency(재생 불가 — Codex B-5 결정): 맞음 항목의 암호문을 `decrypt`하지 못하면(키 회전 등) 그 재전송은 「재생 불가」다 — 새로 판정하거나 새 증표를 발급하지 않고(자리의 더 새 증표를 덮어쓰지 않는다) 확정 판정 `expiredProof`를 돌려줘 화면이 E3 칸 아래 UI-SPEC 「확인 시간 지남」 줄로 다시 확인하게 한다. 항목은 60분 보관 뒤 저절로 빠지므로 04.3-08 키 회전은 이 맵을 다시 쓰지 않는다"
    - "CERT-01 × idempotency(제출 뒤 재생): 맞음 항목을 재생하기 전에 자리의 `submitted_at`을 본다 — 그새 제출됐으면 증표 대신 `submitted`(E6-a)를 돌려준다(키 주인은 이미 4자리를 맞힌 사람이라 제출 여부를 알려도 된다)"
    - "CERT-01 × idempotency(누적 잠김): 누적 20에 닿은 틀림은 상태를 바꾼 결과라 키에 묶인다 — 같은 키가 다시 오면 다시 세지 않는다. 응답의 잠김 여부는 응답을 만드는 순간의 자리 상태로 붙인다(UI-SPEC E1 「멱등 키」): 자리가 아직 누적 잠김이면 같은 `hardLocked`, 그새 담당자가 풀었으면 지금 셈으로 만든 `wrong{remaining}`(다시 세지 않음). 자리 행을 잠근 안에서 **지금의 누적 잠김이 어떤 재생보다 앞선다** — 맞음 항목의 재전송도 자리가 지금 누적 잠겼으면 증표·`submitted` 대신 `hardLocked`이고 셈은 그대로다(A 맞음 → 다른 요청들이 누적 잠금 → A 재전송 = `hardLocked`)"
    - "CERT-01 × rate limit(C5 · 설계 /cso 2026-09-25 확정 — E3-08 · D-16): `verifyLast4`는 행사 행을 잠근 뒤 판정 전에 지난 **15분**(`VERIFY_RATE_WINDOW_MINUTES`)의 틀림 수를 센다 — 그 행사 전체가 `eventMissLimit(명단 수)`(순수 함수 — 기본 `max(40, ceil(명단 × 0.5))`, E3-08) 이상이거나 같은 IP가 그 행사에서 **20번** 이상이면 판정하지 않고 `throttled`를 돌려준다(틀림을 세지 않고 키에 묶지 않는다). IP는 원문이 아니라 `ipHash`(HMAC-SHA256, 키는 `BETTER_AUTH_SECRET`에서 HKDF-SHA256으로 파생한 32바이트 — 라벨 `\"cert-ip-v1\"`, `lib/crypto.ts`의 데이터 키와 무관해 04.3-08 KMS 재작성이 다시 쓰지 않는다, E3-15)로 묶고, 해시 입력은 `ipKey(ip)`(순수 — IPv4는 원문 · IPv6는 앞 /64 prefix, M-3)다 — 같은 /64 안에서 IPv6 주소를 바꿔도 IP 한도를 우회하지 못한다. 막힐 때마다 서버 로그 `log.warn(\"cert.verify_throttled\", {scope, eventId})` 한 줄이 남아 운영자가 원인을 안다(IP · IP 해시 · 이름 없음). 15분 창은 `timestamptz`로 비교해 프로세스 시간대(KST)와 무관하다(E3-34). 한도·창은 `domain/certs/verify-lock.ts`의 이름 붙은 상수 · 함수이고 수치는 설계 `/cso`가 확정했다(더 이상 미정이 아니다). IP 원문은 DB·로그 어디에도 남지 않는다"
    - "트랜잭션 안 연결 규칙(E3-09 — 04.3-02 ⑩ 규칙을 넓힘): `verifyLast4`의 설정 · 플래그(`cert.verify.max_attempts` · `cert.verify.lock_minutes` · 동의 묶음에 쓰는 `cert.retention.years` · 기능 플래그)는 행사 행 잠금 tx를 열기 **전에** 읽어 값으로 넘기고, 잠근 콜백 안의 DB 호출은 전부 `tx`다 — 풀 크기 + 1건의 동시 확인이 `다른 저장이 끝나지 않음`이나 시간 초과 없이 판정된다. Phase 4 04-32가 설정 조회를 tx로 돌릴 수 있어도 규칙은 그대로다"
    - "C1: 공개 액션 넷(`selectWinnerAction` · `verifyLast4Action` · `submitCertificateAction` · `recheckLockAction`)은 본문에서 가장 먼저 `assertCertFeatureEnabled()`를 부른다 — 기능이 꺼진 상태에서 페이지를 거치지 않고 액션을 직접 POST해도 판정·셈·저장이 일어나지 않는다(E2E 직접 POST 테스트)"
    - "CERT-01 × concurrency(확인): 같은 자리에 틀린 요청이 동시에 여럿 와도 틀림 횟수가 정확히 센다(행 잠금) — 한도를 넘는 동시 요청으로 잠금을 건너뛸 수 없다"
    - "CERT-01 × 누적 잠김 공격: 짧은 잠김 넷을 지나며(시각 주입) 틀린 번호 20번을 넣으면 20번째가 `hardLocked`이고 그 뒤로는 3분이 지나도 · 맞는 4자리여도 `hardLocked`다. 중간에 맞혀도 누적은 줄지 않고, 멱등 재전송은 누적을 더하지 않으며, 누적 19에서 동시에 온 틀림 둘은 정확히 한 번 누적 잠김을 건다(통합 테스트)"
    - statement: "E3에서 4자리를 다 넣기 전에는 「전화번호 확인」이 서버를 부르지 않는다(비활성 + 이유) — 빈 칸·3자리 모두(UI Considerations 보강 empty·partial E3)"
      verification: backstop
    - statement: "첫 진입 서버 렌더가 5xx로 실패하면 E6-c(404)가 아니라 UI-SPEC E1 「첫 진입 실패」가 선다(`app/c/[token]/error.tsx`) — 상단 바 + 제목 `기타소득 지급 확인`(부제 없음) + 결과 블록 `확인증 화면을 불러오지 못했습니다 · 잠시 뒤 다시 시도해 주세요` + 3차 「다시 시도」(`location.reload()`, 1차 없음) · 포커스 = 결과 블록 첫 줄(`tabindex=-1`) · `document.title` `오류 · 기타소득 지급 확인`"
      verification: backstop
  prohibitions:
    - statement: "E2 목록과 행 고르기(`selectWinner` 응답)가 누가 제출했는지 드러내서는 안 된다. 제출한 자리도 같은 모양의 행으로 남고, 행을 골랐을 때의 응답도 제출 여부와 무관하게 같다 — 뒤 4자리를 맞힌 사람에게만 E6-a가 선다(A4 · DECISIONS 9/18 #6)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/certs/verify-lock.ts"
      provides: "틀림 횟수 · 잠금 · 해제 · 분 올림 표시 · 누적 잠김(고정 20) · 속도 제한 한도 · 키별 결과 보관 정리의 순수 판정"
      exports: ["evaluateVerifyAttempt", "lockStatus", "verifyBudgetExceeded", "eventMissLimit", "ipKey", "certIpHash", "pruneIdemEntries", "VERIFY_HARD_LOCK_THRESHOLD"]
    - path: "app/c/[token]/flow-rules.ts"
      provides: "고아 기록 항목 판정 · 응답 분류(확정 판정 여덟 / 결과 불명 — throttled 포함)의 순수 함수"
      exports: ["resolveHistoryEntry", "isDefiniteResult"]
    - path: "test/unit/certs/verify-last4.test.ts"
      provides: "한도 경계 · 잠금 뒤 재시작 · 성공 초기화(짧은 셈만) · 누적 20 경계 · 누적 잠김 우선 · `lockStatus`(누적 잠김 우선) · 분 올림 · 속도 제한 경계(15분 창 · 행사 `max(40, ceil(n×0.5))` — 1→40 · 500→250 · IP 20) · `ipKey`(IPv4 통과 · IPv6 /64 압축·완전 표기 · IPv4-매핑, M-3 · 교차 B3) · 보관 60분 정리 · 맵 `ok` 20개 상한(`wrong`은 나이로만)"
      min_lines: 30
    - path: "test/integration/cert-verify.test.ts"
      provides: "동시 틀림 계수 · 키별 멱등 재생(A→B→A · 동시 같은 키) · 증표 재생 불변 · 재생 불가(복호 실패) · 제출 뒤 재생 · 누적 잠김 공격(짧은 잠김 넷 · 맞음 비초기화 · 재전송 비계수 · 동시) · 누적 잠김이 맞음 재생보다 앞섬 · 61분 재전송 = 새 시도(읽을 때 만료) · 틀림 비축출(B1) · 속도 제한 공격 · 제출 자리 비노출 · selectWinner 잠김 증명 · 잠금 다시 확인(닫힘 우선 · `limit`)"
      min_lines: 80
    - path: "test/e2e/mobile-cert-verify.spec.ts"
      provides: "겹침 2행·구별 표시 · 틀림 · 잠김 · 풀림 · 누적 잠김(묶음 포커스 · 1차 설명 = 두 `<p>` id · 잠금 다시 확인 — 보임 이벤트 · 「잠금 확인」 · 누름 실패 포커스 · 짧은 잠김 전환 포커스 · 누적 잠긴 채 닫힘 → E6-b) · 응답 없음 · 문의 줄 · 새로 고침 고아 항목 · 기능 꺼짐 직접 POST E2E"
      min_lines: 40
  key_links:
    - from: "domain/certs/intake.ts"
      to: "domain/certs/verify-lock.ts"
      via: "verifyLast4가 행 잠금 안에서 순수 판정을 부른다"
      pattern: "evaluateVerifyAttempt"
    - from: "domain/certs/intake.ts"
      to: "domain/settings/keys.ts"
      via: "한도·잠금 시간은 설정 키"
      pattern: "CERT_VERIFY_MAX_ATTEMPTS"
    - from: "app/c/[token]/intake-flow.tsx"
      to: "app/c/[token]/actions.ts"
      via: "재시도는 같은 멱등 키로 같은 액션을 다시 부른다"
      pattern: "idemKey"
    - from: "domain/certs/intake.ts"
      to: "repositories/cert-winners.ts"
      via: "행사 행 → 자리 행 순서로 잠근 뒤 지난 15분 틀림 수를 세어 속도 제한을 판정한다"
      pattern: "countRecentMisses"
    - from: "app/c/[token]/actions.ts"
      to: "lib/certs/feature-guard.ts"
      via: "공개 액션 넷이 본문 첫 줄에서 기능 게이트를 부른다(C1)"
      pattern: "assertCertFeatureEnabled"
---

<objective>
**As a** 경품 당첨자, **I want to** 목록에서 내 자리를 헷갈리지 않고 고르고, 뒤 4자리를 잘못 적었을 때 몇 번 남았는지·언제 다시 할 수 있는지·누구에게 전화할지를 알고 싶다, **so that** 막히지 않고 확인을 마치고, 링크를 주운 다른 사람은 내 자리를 쉽게 통과하지 못한다.

04.3-02 트레이서의 E2·E3를 완성한다 — 자리 단위 잠금(설정 두 키), 자리 단위 누적 잠김(고정 20, 소유자 결정 2026-09-24 21:56 「누적 잠금 넣기로 해」 — 푸는 쪽 I3 「잠금 풀기」는 04.3-10), 키별 멱등 재생(증표 불변 · 재생 불가 시 재확인), 행사·IP 속도 제한(C5), 공개 액션 기능 게이트(C1), 응답 없음·실패·닫힘 전환, 문의 줄, 포커스·기록 항목(고아 항목 규칙 포함), 첫 진입 오류 화면, `Button` 접근성 개정 ⑦ (b)(c) 중 Phase 4에 없는 두 조각(개발 경고 조건 · `처리 중`). 목록의 겹침 2행·구별 표시 판정은 트레이서의 `roster-display.ts`가 이미 한다 — 이 플랜은 그 결과를 E2 화면·E2E로 고정한다.

**UI-SPEC:** 최신 승인본(`2c6277b` + 2026-09-25 `/plan-design-review` 반영 — 이 파일의 현재 내용)이 정본이다(X-11) — 같은 모양의 행은 묶지 않고 담당자의 구별 표시로 가르며(A11), 4자리는 한 사람과만 대조하고, 멱등 키는 확정 판정을 받을 때만 끝나며 서버는 상태를 바꾼 결과만 재생한다(E1).

Purpose: D-1107이 설계 `/cso`에 특히 보라고 한 「뒤 4자리 추측 공격」의 방어선(잠금·누적 잠김·서버 판정·멱등·속도 제한)을 코드와 공격 테스트로 세운다. 자리당 누적 상한(C5)은 소유자 결정으로 확정됐다 — 누적 20번 틀림 → 담당자가 풀 때까지 잠김(고정 상수, 설정 아님). 이 플랜은 세고 잠그는 쪽(서버 규칙 · E3 누적 잠김 상태)을, 04.3-10은 푸는 쪽(I3 「잠금 풀기」)을 맡는다. 칸 이름 `cumulative_failed_attempts` · `hard_locked_at`은 04.3-02 스키마가 만든다. 남는 위험은 위협 모델 T-04.3-03 · T-04.3-110에 수치로 적는다.
Output: 설정 키 둘, 순수 잠금·누적 잠김·속도 제한 판정(명단 비례 행사 한도 포함), `verifyLast4`/`selectWinner` 완성, 공개 액션 게이트, E2·E3 화면 완성(누적 잠김 상태 포함), 오류 경계, 단위 3 · 통합 1 · E2E 1.

**실행 조건(E3-01 · E3-33):** Phase 4 PR #40(`origin/claude/gsd-progress-e1nzgu`)이 main에 머지된 뒤에만 시작하고, 웨이브 첫 단계는 `git fetch origin && git merge origin/main`이다(리베이스 · 강제 푸시 금지). 같은 웨이브(04.3-04 · 05)와 통합 · E2E를 동시에 돌리지 않는다(세션당 플랜 하나 — `erp_test` · 포트 3100 공유). Phase 4 부품이 기준이다: `ui/button`은 이미 `aria-disabled`(네이티브 `disabled` 아님 — 비활성 1차도 포커스된다) · 이유 id 연결 · `reasonTone`을 갖고, `withTransaction`은 `lock_timeout` 5초와 풀 연결 한도 5초의 시간 초과를 `다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장`으로 바꾼다.

**태스크 나눔(E3-05):** 화면 태스크를 둘로 나눴다 — Task 2a(기본 E2·E3 + Phase 4 `Button` 확인)와 Task 2b(누적 잠김 묶음 · 잠금 다시 확인). 플랜의 파일 경계는 그대로이고 2b는 2a가 선 뒤 같은 파일을 넓힌다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md
@.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 자리 단위 잠금 · 누적 잠김 · 키별 멱등 재생 · 속도 제한 · 공개 액션 게이트 — 순수 판정 + verifyLast4/selectWinner 완성(서버)</name>
  <files>domain/settings/keys.ts, domain/certs/verify-lock.ts, domain/certs/intake.ts, repositories/cert-winners.ts, app/c/[token]/actions.ts, test/unit/certs/verify-last4.test.ts, test/integration/cert-verify.test.ts, test/e2e/helpers/cert.ts</files>
  <precondition>Phase 4가 main에 머지돼 있고 이 브랜치가 웨이브 첫 단계에서 그 main과 04.3-02를 머금었다 — `git cat-file -e origin/main:ui/confirm-dialog/ConfirmDialog.tsx`가 성공하고 `git merge-base --is-ancestor origin/main HEAD`가 참이며 `test/e2e/helpers/cert.ts`에 `seedSubmittedCert`가 있다(E3-01 · E3-32 — 아니면 멈추고 보고한다)</precondition>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md` Pattern 5(자리 단위 잠금 — 행 `FOR UPDATE` + 순수 함수)와 Pitfalls
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 E1 「서버 왕복 공통」·「멱등 키」 세 줄(340~345행 — 상태를 바꾼 결과만 묶음 · 맞음은 원래 만료 시각 그대로 · 잠금은 절대 시각) · E3(틀림 · 잠김 · 잠금 풀림 · 응답 없음 · 맞음)
    - `domain/certs/intake.ts` · `lib/certs/feature-guard.ts` · `repositories/cert-winners.ts` · `db/schema/cert-winners.ts` · `app/c/[token]/actions.ts`(04.3-02 산출물 전문) — `failed_attempts` · `locked_until` · `cumulative_failed_attempts` · `hard_locked_at`(`timestamp` null — 저장소 관례대로 `withTimezone` 없음, 04.3-02가 권위) · `verify_idem_outcome`(jsonb — 04.3-02가 정한 맵 모양: 키 해시 → 틀림 `{o, r, ip, at}` / 맞음 `{o, r, p, until, ip, at}`) · `verify_proof_hash` · `verified_until` · `submitted_at` 칸, `assertCertFeatureEnabled` 이름과 반환 모양(C1)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` E3 「누적 잠김」 문단 · E1 「멱등 키」의 누적 잠김 줄 — 누적 잠김 판정·재전송 규칙(20 고정 · 누적 잠김 우선 · 응답 순간 상태)
    - `domain/settings/keys.ts`(04.3-02가 더한 확인증 키 뒤) — 이어 붙일 자리와 형식
    - `lib/db-transaction.ts`(Phase 4 판) — `withTransaction`: 콜백 전 `SET LOCAL lock_timeout = '5s'` · 잠금(55P03) · 풀 연결 시간 초과(`db/client.ts` `connectionTimeoutMillis` 5초)를 `다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장`(UserFacingError)으로 바꾼다 — tx 안에서 전역 `db`를 부르면 이 문장이나 시간 초과로 드러난다(E3-09)
    - `repositories/quote-lines.ts` 머리 함수(`findQuoteLinesByIds(viewer, ids, tx: DbOrTx = db)`) — 저장소 관례 「viewer 첫 · tx 마지막」(E3-24 — 이 플랜의 새 함수는 기본값 없이 `tx`를 마지막에 받는다)
    - `lib/log.ts` — `warn(event, fields)`(E3-08 경고 줄)
    - `test/e2e/helpers/cert.ts`(04.3-02 C4) — `seedSubmittedCert()`가 `verifyLast4`를 부르는 한 줄(④ (a)가 입력을 바꾸면 이 줄을 같은 커밋에서 맞춘다 — 표본을 만드는 방법은 그대로)
    - `lib/client-ip.ts` — 클라이언트 IP 규칙(`clientIp(headers)` — 원본 포워딩 헤더는 여기서만 읽는다)
    - `lib/crypto.ts` 머리 주석과 `encrypt`/`decrypt` — 재생용 증표 암호문, 복호 실패는 예외(fail-closed). IP 해시 키(`certIpHashKey`)는 이 파일의 데이터 키에서 파생하지 **않는다**(E3-15 — 04.3-08 KMS 재작성과 계약이 엮이지 않게)
    - `lib/env.ts`의 `env.BETTER_AUTH_SECRET`(스키마상 optional — 길이 검증은 `APP_ENV !== "local"`일 때만, `lib/env.ts:57`·`:85` — 로컬에는 없을 수 있다, 교차 note 3) · Node `crypto.hkdfSync` 문서(`node_modules/next/dist/docs/`가 아니라 Node 내장 — `hkdfSync("sha256", secret, Buffer.alloc(0), "cert-ip-v1", 32)`로 `certIpHashKey` 32바이트를 파생. `salt` 인자는 필수라 빈 `Buffer.alloc(0)`을 쓴다. 반환은 `ArrayBuffer`라 `createHmac`에 넘기기 전에 `Buffer.from(...)`으로 감싼다(TS `BinaryLike`가 `ArrayBuffer`를 받지 않는다) · E3-15 · M-1(b))
    - `node:net`의 `isIPv4`/`isIPv6`(M-3 · 교차 B3 — `ipKey`의 IPv6 정규화 기준)
    - `node_modules/next/dist/docs/`에서 `headers` 함수 문서 — Server Action 안에서 요청 헤더를 읽는 이 버전의 방법(CLAUDE.md 「This is NOT the Next.js you know」)
    - `test/integration/lockout.test.ts` — 로그인 잠금 통합 테스트 선례(동시 요청 단언 방식)
    - `test/unit/lockout.test.ts` — 순수 잠금 판정 단위 테스트 선례
  </read_first>
  <behavior>
    - `evaluateVerifyAttempt({failedAttempts: 0, lockedUntil: null, cumulativeFailed: 0, hardLockedAt: null, now, maxAttempts: 5, lockMinutes: 3, matched: false})` → `{outcome: "wrong", failedAttempts: 1, cumulativeFailed: 1, remaining: 4}`(`remaining`은 짧은 잠김까지만)
    - 4번 틀린 상태에서 또 틀림 → `{outcome: "locked", lockedUntil: now + 3분, unlockAtDisplay: 분 올림, remainingSeconds: 180}`
    - 잠긴 동안(now < lockedUntil) → 맞든 틀리든 `{outcome: "locked"}`, 상태 불변
    - 잠금이 지난 뒤(now ≥ lockedUntil) 틀림 → `failedAttempts: 1`(0부터 다시), `lockedUntil: null`
    - 맞음 → `{outcome: "ok", failedAttempts: 0, lockedUntil: null}`이고 `cumulativeFailed`는 입력 그대로(맞음이 누적을 줄이지 않는다)
    - 모든 틀림은 `cumulativeFailed`를 1 더한다 — 짧은 잠김이 지난 뒤 `failedAttempts`가 0부터 다시 셀 때도 누적은 이어진다
    - `cumulativeFailed: 18`에서 틀림 → `{outcome: "wrong", cumulativeFailed: 19}` · `cumulativeFailed: 19`에서 틀림 → `{outcome: "hardLocked", cumulativeFailed: 20, hardLockedAt: now}`(경계 = 20 「이상」)
    - `failedAttempts: 4 · cumulativeFailed: 19`에서 틀림(짧은 잠김과 누적 잠김이 함께) → `hardLocked`이고 `lockedUntil`을 쓰지 않는다(누적 잠김이 앞선다)
    - `hardLockedAt`이 있으면 맞든 틀리든 · 짧은 잠김 시각이 지났든 → `{outcome: "hardLocked"}`, 상태 불변(세지 않음)
    - `VERIFY_HARD_LOCK_THRESHOLD === 20`(설정에서 읽지 않는 상수)
    - 해제 18:44:10 → 표시 `18:45` · 해제 18:45:00 → 표시 `18:45`(이미 정각이면 그대로)
    - `eventMissLimit(rosterSize)`(E3-08 · 설계 /cso 확정 — 기본 `max(40, ceil(rosterSize × 0.5))`): 1 → 40 · 80 → 40 · 81 → 41 · 500 → 250
    - `verifyBudgetExceeded({eventMisses, ipMisses, rosterSize})`: `rosterSize: 10` — `eventMisses: 39, ipMisses: 19` → false · `eventMisses: 40` → true · `ipMisses: 20` → true(경계는 「이상」) · `rosterSize: 500` — `eventMisses: 249` → false · `250` → true
    - `ipKey(ip)`(순수, M-3 — IPv6 CGNAT/모바일 우회 방지, 교차 B3 · R2-1 규칙 — `net.isIPv4`면 원문 · 그 밖은 `%zone` 제거 → **먼저 소문자로 바꾼 뒤**(교차 R2-1 — 매핑 판정이 소문자 변환보다 앞서면 대문자·16진 매핑 표기를 놓친다) `::`를 8개 그룹으로 펴 **파싱한 값**으로 판정: 앞 5개 그룹(80비트)이 전부 0이고 6번째 그룹이 `ffff`면 IPv4-매핑이라 뒤 32비트를 그 IPv4 주소로 씀(문자열 `::ffff:` 부분 일치가 아니라 값 판정이라 점 표기·16진 표기 매핑을 모두 잡는다) · 매핑이 아니면 각 그룹 앞 0을 없앤 앞 4개 그룹만 `:`로 이음 · `isIPv6`도 아니면(쓰레기 XFF 값 등) 원문 그대로 — 옛 「어느 쪽도 아니면 원문」 갈래를 유지, R3-3, 예외를 던지지 않는다): `ipKey("211.34.56.78")` → `"211.34.56.78"` · `ipKey("not-an-ip")` → `"not-an-ip"`(쓰레기 XFF 값, R3-3) · `ipKey("::ffff:211.34.56.78")` → `"211.34.56.78"` · `ipKey("::FFFF:1.2.3.4") === ipKey("1.2.3.4")`(대문자 접두, 교차 R2-1 — 소문자 변환이 매핑 판정보다 먼저라 걸린다) · `ipKey("::ffff:102:304") === ipKey("1.2.3.4")`(16진 매핑 표기, 교차 R2-1 — 값 판정이라 문자열 부분 일치가 아니어도 잡는다) · `ipKey("2001:db8::a:b:c:d") === ipKey("2001:db8::e:f:1:2")`(같은 /64, 압축 표기의 생략된 0 그룹이 같은 키로 펴진다) · `ipKey("2001:DB8:0:0:1::1") === ipKey("2001:db8::2")`(대소문자·앞 0·완전 표기 대 압축 표기가 같은 /64면 같음) · `ipKey("2001:db8:1::1")`과 `ipKey("2001:db8:2::1")`(서로 다른 /64)은 서로 다른 값 — 단순한 `ip.split(":").slice(0,4)`나 소문자 변환 전에 문자열 `::ffff:`만 찾는 구현은 이 케이스들에서 실패한다(교차 B3·R2-1 — 「단순 구현이 테스트를 통과하지 못하게」가 이 케이스 셋의 목적이다)
    - `certIpHash(secret, eventId, ip)`(순수, 교차 R2-2 — `ipKey`·HKDF·HMAC 전부 이 함수 안에서 하고 hex 앞 32자를 돌려준다): (골든 벡터 — 교차 note 3, 04.3-08의 KMS 재작성이 이 계약을 건드리지 못하게 값으로 고정) 고정 `secret`(32자 이상 리터럴) · 고정 `eventId` · 고정 IP 하나로 `certIpHash(secret, eventId, ip)`를 부르면 테스트에 박아 둔 고정 hex 문자열과 정확히 같다 — 이 값이 바뀌면 HKDF 라벨·해시 함수·메시지 구성 중 하나가 바뀐 것이다 · `certIpHash("", eventId, ip)`(빈 `secret`)는 던진다(조용히 빈 키로 해시하지 않는다)
    - `pruneIdemEntries(entries, now)` → `at`이 now − 60분보다 이른 항목만 빠진다(60분 정각은 남는다) · 60분 안의 `ok` 항목이 21개면 `at`이 가장 이른 `ok` 하나가 빠져 20개가 된다(`VERIFY_IDEM_MAX_ENTRIES = 20` — 21번째 `ok`가 가장 오래된 `ok`를 밀어낸다) · (B1) 가장 오래된 `wrong` 1개 + `ok` 21개 → `wrong` 1 + `ok` 20(개수 상한은 `ok`만 밀어내고 60분 안의 `wrong`은 개수와 무관하게 남는다)
    - `lockStatus({hardLockedAt: t, lockedUntil: now + 3분, now})` → `{kind: "hardLocked"}`(누적 잠김이 앞선다) · `{hardLockedAt: null, lockedUntil: now + 170초, now}` → `{kind: "shortLocked", unlockAt: lockedUntil, remainingSec: 170}` · `{hardLockedAt: null, lockedUntil: now − 1초, now}` · `lockedUntil: null` → `{kind: "open"}`
    - (통합) 같은 자리에 틀린 요청 6개를 동시에 보내면 결과는 `wrong` 4개 + `locked` 2개(또는 `wrong` 4 · 잠금을 거는 `locked` 1 · 이미 잠긴 `locked` 1)이고 DB의 잠금이 정확히 한 번 걸린다
    - (통합) 같은 멱등 키로 틀린 요청을 두 번 → 두 응답이 같고 `failed_attempts`는 1이다 · 다른 키 → 2
    - (통합, 끼어든 키) 키 A 틀림 → 키 B 틀림 → 키 A 재전송 → A의 두 응답이 같고(`remaining` 같음) `failed_attempts`는 2다(A를 다시 세지 않는다)
    - (통합, 동시 같은 키) 같은 키 A의 틀린 요청 둘을 `Promise.all`로 → 두 응답이 같고 `failed_attempts`는 1이다
    - (통합, 잠금을 건 키) 다섯 번째 틀림으로 잠금을 건 키를 잠긴 동안 다시 보내면 같은 해제 시각의 `locked`이고 `failed_attempts`·`locked_until`이 그대로다 · 해제 시각 뒤 그 키를 다시 보내도 다시 세지 않는다(저장된 결과, 남은 초 0)
    - (통합) 잠금 때문에 `locked`만 받은(상태를 바꾸지 않은) 키로 해제 시각 뒤 맞는 4자리를 다시 보내면 옛 `locked`를 재생하지 않고 지금 판정해 `ok`다 · 잠긴 동안 같은 키를 다시 보내도 `locked_until`이 늘지 않는다
    - (통합, 증표 불변) 키 A로 맞음 → 증표 P1 · 만료 T1 → 키 A 재전송 → 같은 P1 · 같은 T1이고 자리의 `verify_proof_hash`는 그대로다 · 그 뒤 키 B로 맞음 → P2 → 늦게 온 키 A 재전송 → P1 · T1을 돌려주고 자리의 `verify_proof_hash`는 여전히 P2의 해시다(재생이 증표를 바꾸지 않는다)
    - (통합, 재생 불가 — Codex B-5) 키 A로 맞음 → P1 → 테스트가 그 자리 맵의 A 항목 `p`를 다른 키로 만든 암호문(또는 깨진 문자열)으로 바꿔 복호가 실패하게 함 → 키 B로 맞음 → P2 → 키 A 재전송 → `{kind: "expiredProof"}`이고 자리의 `verify_proof_hash`는 여전히 P2의 해시 · `verified_until` · `failed_attempts` · 맵이 그대로다(새 증표를 발급하지 않는다) · 그 뒤 새 키 C로 맞는 4자리 → 정상 `ok`
    - (통합, 제출 뒤 재생) 키 A로 맞음 → 그 자리를 제출됨으로 만든 뒤(`submitted_at` 채움) 키 A 재전송 → 증표가 아니라 `submitted`
    - (통합, 누적 잠김 공격 — 짧은 잠김 넷) 한 자리에 틀림 5번 → `locked` → 시각을 해제 뒤로 주입 → 5번 → `locked` → 한 번 더 → 5번 → `locked` → 마지막 5번의 넷째까지 `wrong`, 다섯째(누적 20번째) → `hardLocked`(짧은 잠김이 아님)이고 `hard_locked_at`이 찍히고 `locked_until`은 새로 쓰이지 않는다 → 시각을 1시간 뒤로 주입해 맞는 4자리 → 여전히 `hardLocked` · 증표 없음
    - (통합, 맞음은 누적을 줄이지 않는다) 틀림 5 → 해제 뒤 맞음(`ok`, `failed_attempts` 0 · `cumulative_failed_attempts` 5 그대로) → 틀림 15번(짧은 잠김 해제마다 시각 주입) → 15번째 = 누적 20 → `hardLocked`
    - (통합, 재전송은 누적을 더하지 않는다) 누적 18에서 키 X 틀림 → 19 → 키 X 재전송 두 번 → 여전히 19, 응답 같음 · 누적 19에서 키 Y 틀림 → `hardLocked` → 키 Y 재전송 → 같은 `hardLocked`, `cumulative_failed_attempts` 20 그대로 · 테스트가 담당자 풀기를 흉내 내 두 셈과 `hard_locked_at`을 비운 뒤 키 Y 재전송 → 다시 세지 않고 `wrong{remaining: 5}`(응답 순간 상태 — UI-SPEC E1 「멱등 키」)
    - (통합, 누적 잠김 동시) 누적 19인 자리에 서로 다른 키의 틀림 둘을 `Promise.all`로 → 하나는 누적 잠김을 거는 `hardLocked`(누적 20), 다른 하나는 이미 잠긴 `hardLocked`(세지 않음)이고 `cumulative_failed_attempts`가 정확히 20이다
    - (통합, 읽을 때 만료) 키 A 틀림 → 그 사이 그 자리에 아무 쓰기도 없이 시각을 61분 뒤로 주입 → 키 A 재전송 → 저장된 응답이 아니라 새 시도로 판정되고 `failed_attempts`가 1 더해진다(맵에 A 항목이 아직 남아 있어도 읽을 때 없는 것으로 본다) · 60분 정각이면 재생이다
    - (통합, 누적 잠김이 재생보다 앞섬) 키 A로 맞음(증표 P1) → 다른 키들의 틀림으로 그 자리를 누적 잠금(누적 19로 맞춘 뒤 한 번 틀림) → 키 A 재전송 → 증표가 아니라 `hardLocked`이고 `failed_attempts` · `cumulative_failed_attempts` · `hard_locked_at` · 맵 · `verify_proof_hash`가 그대로다 · 같은 순서에서 그 자리가 제출됐어도 `submitted`가 아니라 `hardLocked`
    - (통합, 맵 상한) 한 자리에 60분 안에 서로 다른 키 21개로 맞는 4자리를 보내면(맞음은 틀림 셈·속도 제한에 들지 않아 막히지 않는다 — 성공 항목이 맵을 끝없이 키우던 구멍) 맵 항목이 20개이고 첫 키 항목이 없다 — 첫 키를 다시 보내면 재생이 아니라 새 판정(새 증표)이다
    - (통합, 틀림은 축출되지 않는다 — 최종 리뷰 B1) IP X가 자리 S에 틀림 1번 → IP Y가 자리 S에 서로 다른 새 키 20개로 맞는 4자리(맵 = `wrong` 1 + `ok` 20 — 옛 규칙이면 21번째 항목이 그 틀림을 밀어냈다) → IP X가 같은 행사 다른 자리들에 틀림 19번 → IP X의 다음 시도 → `throttled`(자리 S의 첫 틀림이 여전히 IP 셈 20에 든다) · 자리 S 맵에 그 `wrong` 항목이 남아 있고 `countRecentMisses`의 `eventMisses`가 20이다
    - (통합) 저장된 맵의 어느 값에도 증표 평문 · 4자리 · IP 원문이 없다
    - (통합, 공격 — 같은 IP) 한 IP가 같은 행사의 여러 자리에 틀린 번호 20번을 넣은 뒤 21번째 → `throttled`, 그 자리의 `failed_attempts`와 맵이 그대로 · 같은 IP의 맞는 4자리도 `throttled`(판정하지 않는다) · 다른 IP는 정상 판정
    - (통합, 공격 — 여러 IP) 서로 다른 IP 40개가 한 행사에 틀린 번호를 하나씩 넣은 뒤 41번째 IP → `throttled` · 다른 행사는 영향 없음
    - (통합, 공격 — 동시) 한 IP가 서로 다른 자리 30곳에 틀린 요청을 `Promise.all`로 → 판정된 `wrong`/`locked`가 정확히 20개이고 나머지는 `throttled`다(행사 행 잠금으로 한도를 넘는 동시 판정이 없다)
    - (통합) 15분이 지나면(시각 주입) 같은 IP가 다시 판정받는다
    - (통합, 명단 비례 한도 — E3-08) 당첨자 120명 행사(한도 60): 서로 다른 IP 60개가 틀림 하나씩 → 61번째 IP → `throttled`(고정 40이었으면 41번째에서 막혔다) · 당첨자 10명 행사는 41번째에서 `throttled`
    - (통합, 운영 가시성 — E3-08) `throttled`를 돌려줄 때마다 `log.warn`이 `"cert.verify_throttled"`와 `{scope: "event" | "ip", eventId}`로 한 번 불리고(`vi.spyOn(log, "warn")`), 인자 전체(JSON)에 IP 원문 · IP 해시 · 이름 · 4자리가 없다
    - (통합, 시간대 — E3-34) `process.env.TZ = "Asia/Seoul"`로 바꾼 채(끝나면 되돌린다) 14분 전 틀림은 셈에 들고 16분 전 틀림은 들지 않는다 — `at`을 `timestamptz`로 비교한다(`::timestamp`면 9시간 어긋나 이 케이스가 실패한다)
    - (통합) 제출한 자리를 틀린 4자리로 확인 → `wrong`(제출 여부를 흘리지 않는다) · 맞힌 4자리 → `submitted`
    - (통합) 잠긴 자리를 `selectWinner` → `{kind: "ok", locked: {limit, unlockAtDisplay, remainingSeconds}}` · 누적 잠긴 자리 → `{kind: "ok", hardLocked: true}`(`locked` 블록 없음 · 숫자 없음) · 제출한 자리를 `selectWinner` → 제출하지 않은 자리와 **같은 키 집합**의 응답(누적 잠긴 제출 자리와 누적 잠긴 미제출 자리도 같은 키 집합)
    - (통합, 잠금 다시 확인 — Codex UI 7차) `recheckWinnerLock`: 누적 잠긴 자리 → `{kind: "hardLocked"}` · 짧은 잠김 자리 → `{kind: "shortLocked", unlockAt, remainingSec, limit}`(해제 시각은 저장된 절대 시각 · `limit` = 설정 `cert.verify.max_attempts` 값 — 누적 잠김에서 곧바로 온 자리도 같은 값) · 잠김 없는 자리 → `{kind: "open"}` · **제출된 자리와 제출하지 않은 자리의 응답이 같은 키 집합**이고 제출된 · 잠김 없는 자리도 `{kind: "open"}`(제출을 흘리지 않는다) · **행사가 닫혔으면(기한 · 모두 제출 · 담당자가 닫음) 누적 잠긴 자리든 짧은 잠김 자리든 `{kind: "closed", reason}`**(링크 단위 닫힘이 어떤 잠김보다 앞선다 — 이유는 `selectWinner`·`verifyLast4`의 `closed`와 같은 값이고 개인별 제출 여부는 싣지 않는다) · 누적 잠긴 자리를 세 번 물어도 자리 행(`failed_attempts` · `cumulative_failed_attempts` · `hard_locked_at` · `locked_until` · 맵 · `updated_at`)이 그대로 · 다른 행사의 자리 id → `notFound` · 기능 꺼짐 → 판정 없이 거부
    - (통합) 누적 잠긴 제출 자리에 맞는 4자리 → `submitted`가 아니라 `hardLocked`(잠김 판정이 제출 판정보다 먼저 — 제출 여부를 흘리지 않는다)
    - (통합) 링크가 닫힌 뒤(기한 지남) `verifyLast4` → `closed`, 틀림 횟수 변화 없음
    - (통합, C1) 기능이 꺼지면 `selectWinner` · `verifyLast4`가 판정 없이 거부되고 자리 행이 그대로다
  </behavior>
  <action>
① **설정 키(공유 등록부 — 끝에 더한다):** `CERT_VERIFY_MAX_ATTEMPTS`(`cert.verify.max_attempts`, 정수 1~20, 기본 5) · `CERT_VERIFY_LOCK_MINUTES`(`cert.verify.lock_minutes`, 정수 1~60, 기본 3). 네임스페이스 「확인증」. CONTEXT 「이미 확정된 입력」의 5회 · 3분이 기본값이다. `SETTING_DEFS`에 싣는다.

② **RED → GREEN `domain/certs/verify-lock.ts`(순수):** 위 `behavior` 단위 케이스를 `test/unit/certs/verify-last4.test.ts`에 **먼저** 쓰고 실패를 확인한 뒤 구현한다. `evaluateVerifyAttempt(input)`(입력 = `failedAttempts` · `lockedUntil` · `cumulativeFailed` · `hardLockedAt` · `now` · `maxAttempts` · `lockMinutes` · `matched`) · `lockStatus({lockedUntil, hardLockedAt, now})`(→ `hardLocked` · `shortLocked{unlockAt, remainingSec}` · `open` — `hardLockedAt`이 있으면 짧은 잠김 시각과 무관하게 `hardLocked`, 4차 E3 계약) · 분 올림 표시(`HH:mm`, Asia/Seoul) · 남은 초(올림 정수, 0 미만은 0). **누적 잠김(소유자 결정 2026-09-24 21:56):** `export const VERIFY_HARD_LOCK_THRESHOLD = 20` — 설정 키가 아니고 `SETTING_DEFS`에 싣지 않는다(짧은 잠김 두 설정과 달리 고정). 판정 순서는 ⓐ `hardLockedAt` 있음 → `hardLocked`(불변) ⓑ `now < lockedUntil` → `locked`(불변) ⓒ 맞음 → `ok`(짧은 셈만 0, 누적 그대로) ⓓ 틀림 → 짧은 셈(해제 뒤면 0부터) + 1 · 누적 + 1 → 누적 ≥ 20이면 `hardLocked`(`hardLockedAt = now`, `lockedUntil`은 쓰지 않는다 — 누적 잠김이 앞선다) · 아니고 짧은 셈 ≥ 한도면 `locked` · 아니면 `wrong{remaining}`(짧은 잠김까지 남은 수만). 같은 파일에 C5 속도 제한 상수(이름을 붙여 설계 `/cso`가 2026-09-25에 확정했다, E3-08 · D-16) — `VERIFY_RATE_WINDOW_MINUTES = 15`(옛 `VERIFY_BUDGET_WINDOW_MINUTES = 60`을 대체) · `VERIFY_MISS_BUDGET_PER_EVENT = 40` · `VERIFY_MISS_BUDGET_PER_IP = 20` · `VERIFY_IDEM_RETENTION_MINUTES = 60`(재생 보관 — 속도 제한 창과 다른 값이라 바뀌지 않는다) — 과 맵 상한 `VERIFY_IDEM_MAX_ENTRIES = 20`, `verifyBudgetExceeded({eventMisses, ipMisses})` · `pruneIdemEntries(entries, now)`(60분 지난 항목을 뺀 뒤 `ok` 항목이 20개를 넘으면 `at`이 이른 `ok`부터 뺀다 — `wrong` 항목은 60분 나이로만 빠지고 개수로 빼지 않는다(B1) · ④ (d)의 읽기와 (h)의 쓰기가 같은 함수를 쓴다). **`ipKey(ip)`(순수, M-3 · 교차 B3 · R2-1):** `node:net`의 `isIPv4`면 원문 그대로. 그 밖은 `%zone`을 떼고 **먼저 소문자로 바꾼 뒤**(교차 R2-1 — 매핑 판정이 소문자 변환보다 앞서면 대문자·16진 매핑 표기를 놓친다) `::`를 8개 그룹으로 편다(RFC 5952 역). 편 8개 그룹의 **파싱된 값**으로 IPv4-매핑을 판정한다 — 앞 5개 그룹(80비트)이 전부 0이고 6번째 그룹이 `ffff`면 매핑이라 뒤 32비트를 IPv4 주소 문자열로 반환(문자열 `::ffff:` 부분 일치가 아니라 값 판정이므로 점 표기(`::ffff:1.2.3.4`)와 16진 표기(`::ffff:102:304`)를 모두 잡는다). 매핑이 아니면 각 그룹의 앞 0을 없앤 앞 4개 그룹만 `:`로 이어 /64 prefix로 쓴다. `isIPv6`도 아니면(쓰레기 XFF 값 등) 원문 그대로 반환한다(R3-3 — 옛 「어느 쪽도 아니면 원문」 갈래 유지, 예외를 던지지 않는다). 단순한 `split(":").slice(0,4)`나 소문자 변환 전에 문자열 `::ffff:`만 찾는 구현은 생략된 0 그룹이 낀 압축 표기나 대문자·16진 매핑 표기에서 틀리므로 `::`를 실제로 펴고 값으로 판정하는 단계가 있어야 한다. 단위 테스트 일곱(IPv4 통과 · IPv4-매핑 점 표기 → IPv4 · IPv4-매핑 대문자 접두 → IPv4(R2-1) · IPv4-매핑 16진 표기 → IPv4(R2-1) · 압축 표기 두 개가 같은 /64 · 완전 표기와 압축 표기가 같은 /64 · 서로 다른 /64는 다른 값). **`certIpHash(secret, eventId, ip)`(순수, 교차 R2-2):** `ipKey`·HKDF·HMAC 전부 이 함수 안에서 하고 hex 앞 32자를 돌려준다 — `secret`이 없으면(빈 문자열 · undefined) HKDF 파생 전에 분명한 오류를 던진다. 골든 벡터(고정 `secret`·`eventId`·IP → 고정 hex)와 빈 `secret` 오류 케이스를 이 함수를 직접 불러 시험한다(`env` import·`vi.stubEnv` 불필요).

③ **`repositories/cert-winners.ts`(viewer 첫 인자 = `SYSTEM_VIEWER`, where에 행사 id와 자리 id를 함께 — 04.3-02 경계 규칙):**
  - `lockEventRow(viewer, eventId, tx)`(E3-24 — 저장소 관례 「viewer 첫 · tx 마지막」, `tx`에 기본값을 두지 않는다: 잠금은 트랜잭션 밖에서 뜻이 없다) — 행사 행 `FOR UPDATE`. 04.3-06 제출 · 04.3-10도 이 이름과 인자 순서로 같은 순서(행사 → 자리)로 잠근다(04.3-07 · 12는 행사 행을 잠그지 않는다 — A7 Round 2) — 교착을 막는 순서 규칙을 함수 머리 주석에 적는다(`repositories/cert-events.ts`는 같은 웨이브의 04.3-04가 고치므로 이 플랜은 건드리지 않는다).
  - 자리 한 행 `FOR UPDATE` 읽기.
  - `countRecentMisses(viewer, {eventId, ipHash, since}, tx)`(E3-24 — tx 마지막 · 기본값 없음) → `{eventMisses, ipMisses, rosterSize}` — 그 행사 자리들의 `verify_idem_outcome` 맵을 `jsonb_each`로 펼쳐 `since` 이후의 틀림 항목을 세고, 같은 쿼리에서 그 행사의 파기되지 않은 당첨자 수(`rosterSize`, E3-08)를 센다. **시각 비교는 `(value->>'at')::timestamptz >= ${since.toISOString()}::timestamptz`로 적는다(E3-34)** — `::timestamp` 캐스트는 ISO 문자열의 `Z`를 버려 프로세스 시간대가 KST면 창이 9시간 어긋난다(CI · Cloud Run은 UTC라 테스트 없이는 드러나지 않는다 — 위 behavior 시간대 케이스).
  - 판정 결과(`failed_attempts` · `locked_until` · `cumulative_failed_attempts` · `hard_locked_at` · 맵 전체 · 증표 해시 · `verified_until`)를 한 번에 쓰는 함수. 이 함수는 `version`을 올리지 않는다(04.3-02 규칙 — 확인 시도·잠금은 담당자 편집이 아니다). 두 셈을 0으로 되돌리는 쓰기는 이 플랜에 없다(04.3-10 「잠금 풀기」 몫).

④ **`domain/certs/intake.ts` `verifyLast4` — 키별 결과 보관(Codex #9):** 04.3-02의 `verify_idem_outcome`(jsonb) 칸을 **키 해시 → 항목** 맵으로 쓴다. 항목 = `{o: "wrong", r: 그때 돌려준 응답(잠금을 건 틀림이면 locked 응답), ip: IP 해시, at}` 또는 `{o: "ok", r: 그때 돌려준 응답(증표 제외 — 경품 줄 · 전달 · `consent{version, retentionYears}`(04.3-02) · `version`), p: encrypt(증표), until: 원래 만료 시각, ip, at}`. 맵에는 개인정보 · 4자리 · 증표 평문 · IP 원문이 없다. 맵 모양은 04.3-02가 정한 그대로다(04.3-02가 단일 키 칸을 두지 않고 이 맵만 둔다). 누적 잠김을 건 틀림도 `o: "wrong"` 항목이다(`r` = 그때 돌려준 `hardLocked` 응답) — 그래서 속도 제한의 틀림 셈에도 들어간다. 순서:
  - (a) 입력 zod — 4자리 숫자 · 멱등 키(base64url 22~64자) · `ip`(액션이 넘긴 문자열 또는 null). 기능 게이트는 액션이 먼저 불렀다(⑥) — domain도 04.3-02 그대로 플래그를 다시 본다. 이 입력 변경으로 `test/e2e/helpers/cert.ts` `seedSubmittedCert()`의 `verifyLast4` 호출 줄이 바뀌어야 하면 같은 커밋에서 그 한 줄만 맞춘다(`ip`는 null — 04.3-02 C4 규칙, E3-32).
  - (a0) **트랜잭션 전에 읽기(E3-09 — 04.3-02 ⑩ 「트랜잭션 안 연결 규칙」을 넓힘):** 설정 · 플래그는 (c)의 `withTransaction`을 열기 **전에** 읽어 값으로 넘긴다 — 기능 플래그(`isCertFeatureEnabled`) · `CERT_VERIFY_MAX_ATTEMPTS` · `CERT_VERIFY_LOCK_MINUTES` · 맞음 때 `offered_*`에 묶는 `CERT_RETENTION_YEARS`(과 코드 상수 `CERT_CONSENT_VERSION`). 잠근 콜백 안의 DB 호출은 전부 `tx`를 받는다(`lockEventRow` · 자리 `FOR UPDATE` · `countRecentMisses` · 판정 쓰기). Phase 4 04-32로 설정 조회를 tx에 태울 수 있어도 이 규칙은 그대로 둔다 — 잠근 트랜잭션을 짧게 두고, 한 곳의 실수가 풀 교착으로 번지지 않게.
  - (b) 토큰 → 행사 id.
  - (c) `withTransaction` 안에서 `lockEventRow` → 자리 행 `FOR UPDATE`(이 순서만). 열림 판정(`closed_at` · 기한)은 **행사 행을 잠근 뒤 읽은 값으로** 한다 — 닫힘이면 세지 않고 `closed`(맵에 넣지 않는다). 잠그기 전에 읽은 값으로 판정하지 않는다(닫기와 경합해도 닫힌 뒤 증표가 나가지 않는다).
  - (d0) **지금의 누적 잠김이 재생보다 먼저(4차 결정 — Codex r3-B NOTE 1):** 자리 행을 잠근 뒤 `hard_locked_at`이 있으면 맵을 보지 않고 `hardLocked` — 맞음 항목의 재전송이어도 증표·`submitted`를 돌려주지 않고, 셈 · 맵 · 증표 어느 것도 바꾸지 않는다(누적 잠김 우선 계약이 재생에도 선다 — 이 순서의 교차 케이스는 ④ 구현 전에 통합 테스트로 먼저 써서 실패를 본다).
  - (d) **재생:** 맵을 `pruneIdemEntries(맵, now)`로 거른 뒤(60분 지난 항목은 **읽을 때도** 없는 것으로 본다 — 쓸 때만 정리하면 쓰기가 없던 자리의 61분 된 항목이 재생된다) 이 키 해시가 있으면 쓰기 없이(셈 · 맵 · 증표 어느 것도 바꾸지 않는다) 그 항목으로 응답한다:
    - `wrong` 항목: 저장된 응답이 짧은 잠김(`locked`)이면 해제 시각은 저장된 절대 시각, 남은 초만 지금 다시 계산. 저장된 응답이 `hardLocked`이면 **응답 순간 상태**로 붙인다(UI-SPEC E1 「멱등 키」 누적 잠김 줄) — 자리의 `hard_locked_at`이 아직 있으면 `hardLocked`, 그새 담당자가 풀었으면(비었으면) 지금 셈으로 만든 `wrong{remaining}`. 그 밖의 `wrong`은 저장된 응답 그대로.
    - `ok` 항목: ① 자리의 `submitted_at`이 채워졌으면 증표 대신 `submitted`(E6-a — 키 주인은 이미 맞힌 사람이다). ② 아니면 `decrypt(p)`를 시도한다 — 성공하면 저장된 `r`에 붙여 **처음 발급한 증표 · 원래 `until` · 그때의 동의 버전·보존 기간·`version`**을 돌려주고 자리의 `verify_proof_hash`·`verified_until`을 건드리지 않는다. ③ **복호가 실패하면(키 회전 등 — Codex B-5 결정) 「재생 불가」:** 새로 판정하지 않고 새 증표도 발급하지 않으며(자리의 더 새 증표를 덮어쓰지 않는다) 확정 판정 `{kind: "expiredProof"}`를 돌려준다 — 화면은 E3 칸 아래 UI-SPEC 「확인 시간 지남」 줄로 다시 확인하게 하고 키는 끝난다(다음 누름 = 새 키 = 정상 판정). 항목은 60분 보관 뒤 (h)가 빼므로 04.3-08 키 회전이 이 맵을 다시 쓸 필요가 없다(04.3-08은 바꾸지 않는다).
  - (e) **누적 잠김은 (d0)에서 이미 걸렀다:** `hard_locked_at`이 있으면 `hardLocked` — 맞든 틀리든 판정하지 않고 세지 않고 맵에 넣지 않는다(상태를 바꾸지 않은 판정 — 담당자가 푼 뒤의 같은 키는 지금 다시 판정된다). 재생이 없으면 짧은 잠김: 잠겨 있으면(now < `locked_until`) `locked` — 세지 않고 맵에 넣지 않는다(해제 시각은 절대 시각이라 재전송으로 늘지 않는다, UI-SPEC E1). 두 잠김 판정은 제출 판정보다 먼저다(제출 여부를 흘리지 않는다).
  - (f) **속도 제한(C5, 설계 /cso 2026-09-25 확정 수치 — E3-08 · D-16 · M-3 · E3-15):** `ipHash = certIpHash(env.BETTER_AUTH_SECRET, eventId, ip)`(②의 순수 함수, 교차 R2-2 — HKDF·HMAC·`ipKey`·`ip`가 없을 때의 `"unknown"` 처리 전부 이 함수 안에서 한다. **메시지는 `eventId + ":" + (ip ? ipKey(ip) : "unknown")`다**(교차 R3-2 — 골든 벡터로 값을 고정하는 계약이 이 형식에 묶인다): `ip`가 없으면(로컬 · XFF 없는 경로) 함수 안에서 `"unknown"` 한 칸에 모여 그 요청들이 IP 한도를 나눠 쓴다 — Cloud Run은 항상 XFF가 있어 실사용에서는 안 걸린다, 코드 주석 한 줄로 남긴다). `certIpHash`는 `secret`이 없으면(로컬 — `env.BETTER_AUTH_SECRET`이 optional) HKDF 파생 전에 분명한 오류를 던진다(조용히 빈 키로 해시하지 않는다) — 파생·HMAC은 함수 안에 있어 `lib/crypto.ts`의 데이터 키와 무관하다(04.3-08 KMS 재작성이 이 값을 다시 쓰지 않는다). `ipKey(ip)`가 IPv6는 /64로 묶고 IPv4는 원문을 쓴 뒤 해시하므로 행사 사이에 같은 사람을 이을 수 없고 DB만으로는 원본 IP를 되돌릴 수 없다(키 있는 가명 — T-04.3-80). `countRecentMisses(SYSTEM_VIEWER, {eventId, ipHash, since: now − 15분}, tx)` → `verifyBudgetExceeded({eventMisses, ipMisses, rosterSize})`면 `{kind: "throttled"}` — 판정 · 셈 · 맵 기록 없음. **행사 한도는 명단 비례(E3-08):** `eventMissLimit(rosterSize)` 순수 함수(`max(40, ceil(rosterSize × 0.5))` — 명단 최대 500(04.3-04 · 10)에서 정상 오타만으로 행사 전체가 멈추지 않게). **IP 한도는 15분에 20번**(`VERIFY_MISS_BUDGET_PER_IP`). **운영 가시성(E3-08):** `throttled`를 돌려줄 때 `log.warn("cert.verify_throttled", {scope, eventId})` 한 줄(`scope` = 막은 한도 `"event"` 또는 `"ip"`) — IP 원문 · IP 해시 · 이름 · 4자리를 싣지 않는다. 창 15분 · 행사 `max(40, ceil(n×0.5))` · IP 20은 설계 `/cso`가 확정한 이름 붙은 상수다(정상 오타율 × 명단 크기). throttled 화면 문구를 「n분 뒤 다시 · 문의」로 바꿀지는 이 플랜이 정하지 않는다(→ `/plan-design-review` — UI-SPEC 승인 계약 그대로 「결과 불명」 줄).
  - (g) `evaluateVerifyAttempt`(맞음 = 그 한 사람의 전화번호 끝 4자리). 제출된 자리는 맞았을 때만 `submitted`(쓰기 없음), 틀렸으면 제출하지 않은 자리와 똑같이 센다. 맞음 → 32바이트 증표 새로 발급, 자리에 해시와 `verified_until`(지금 + `VERIFY_PROOF_TTL_MINUTES`)을 쓰고(04.3-02가 같은 순간에 쓰는 `offered_consent_version` · `offered_retention_years` 묶음도 그대로 함께 쓴다 — 이 플랜이 없애지 않는다), `failed_attempts`만 0으로 하고 `cumulative_failed_attempts`는 그대로 두며 맵에 `ok` 항목. 틀림 → 짧은 셈·누적 셈·잠금(`locked_until` 또는 누적 20이면 `hard_locked_at`)을 쓰고 맵에 `wrong` 항목. 두 셈 모두 자리 행 `FOR UPDATE` 안에서만 더한다(동시 틀림이 누적 잠김을 건너뛰지 못한다).
  - (h) 쓸 때마다 `pruneIdemEntries`로 60분 지난 항목을 빼고 `ok` 항목이 20개(`VERIFY_IDEM_MAX_ENTRIES`)를 넘으면 가장 오래된 `ok`부터 뺀다 — 맞음 항목은 틀림 셈·속도 제한에 들지 않아 시간 창만으로는 맵이 묶이지 않는다(Codex r3-B 추가 주석 2). 빠진 키의 재전송은 새 시도다(맞음이면 새 증표 — 그 키 주인은 이미 맞힌 사람이다). **`wrong` 항목은 개수 상한으로 빼지 않는다(최종 리뷰 B1):** `countRecentMisses`(③)가 이 맵의 `wrong` 항목으로 행사 `eventMissLimit`(기본 40) · IP 20 한도를 세므로, 캐시 축출이 15분 창 안의 틀림을 지우면 한도가 새 버린다 — `wrong`은 60분 나이로만 빠지고(재생 보관 — 속도 제한 창(15분)보다 길다), 그 수는 행사 한도(15분에 최소 40번 — 한도에 닿으면 `throttled`라 맵에 넣지 않는다)에 묶인다. **교차 계약(04.3-10):** 담당자의 이름·전화 수정이 이 맵을 비울 때도 `ok` 항목(증표 재생)만 지우고 `wrong` 항목은 남긴다 — 그 수정과 테스트(「담당자 수정 뒤에도 틀림이 셈에 든다」)는 04.3-10 몫이다.
  - 응답: `wrong{remaining}` · `locked{limit, unlockAtDisplay, remainingSeconds}` · `hardLocked`(숫자·시각 없음 — 누적 한도·누적 횟수를 싣지 않는다) · `ok{…, proof, verifiedUntil, version}` · `submitted{…}` · `closed{…}` · `expiredProof`(재생 불가) · `throttled`. `ok`의 `version`은 증표를 발급한 순간 그 자리 행의 `version`이다(담당자 편집만 올린다) — 04.3-06 제출이 행을 잠근 뒤 이 값과 비교해, 확인 뒤 담당자가 경품·전달을 고쳤으면 옛 화면으로 저장하지 않는다(Codex #7). 재생은 저장된 응답의 `version`을 그대로 돌려준다. 시각은 기존 deps 주입 관례로 `now`를 받는다(테스트가 60분 경과를 주입). 멱등 키 형식 · 보관 60분 · 속도 제한 한도는 설계 `/cso` 검토 대상으로 SUMMARY에 적는다.

⑤ **`selectWinner`:** 누적 잠긴 자리(`hard_locked_at` 있음)면 `hardLocked: true`만(해제 시각·횟수 없음, `locked` 블록 없음 — 누적 잠김이 앞선다), 짧은 잠김이면 `locked` 블록을 싣는다. 제출 여부는 싣지 않는다 — 제출한 자리와 안 한 자리의 응답 키 집합이 같다. 고르기는 읽기라 속도 제한을 걸지 않는다(추측할 비밀이 없다 — 목록은 이미 공개).

⑤-b **`recheckWinnerLock(token, winnerId)`(잠금 다시 확인 — Codex UI 7차 계약):** E3 누적 잠김 화면의 복구 길이 부르는 읽기 전용 판정. 토큰 → 행사 id → 자리(행사 id + 자리 id, 없으면 `notFound`) → 먼저 행사 열림 판정(`selectWinner`와 같은 함수 · 같은 사유 값) — 닫혔으면 어떤 잠김이든 `closed{reason}`(링크 단위 닫힘이 앞선다 — 닫힌 행사는 담당자도 풀 수 없어 누적 잠긴 채 닫히면 이 길이 E6-b에 닿아야 한다, Codex UI 8차) → 아니면 `lockStatus({lockedUntil, hardLockedAt, now})`(②)로 `hardLocked` · `shortLocked{unlockAt, remainingSec, limit}`(`limit` = 설정 `CERT_VERIFY_MAX_ATTEMPTS` 값 — 기존 짧은 잠김 문구 `틀린 번호가 {한도}번 들어와 …`를 그리게, 누적 잠김에서 곧바로 온 사람은 이 값을 받은 적이 없다) · `open` 중 하나. 이 넷**만** 돌려준다 — 개인별 제출 여부 · 셈 · 이름을 싣지 않고 쓰지 않는다(잠금 없음, 속도 제한 없음 — `selectWinner`와 같은 읽기). 공개 액션 `recheckLockAction`(⑥의 게이트 규칙 그대로)이 이것 하나만 부른다.

⑥ **`app/c/[token]/actions.ts`(C1):** 네 공개 액션(`selectWinnerAction` · `verifyLast4Action` · `submitCertificateAction` · `recheckLockAction`) 본문의 **첫 문장**이 `await assertCertFeatureEnabled()`(`lib/certs/feature-guard.ts`, 04.3-02 — 꺼짐이면 `notFound()`를 던진다)인지 확인하고 없으면 더한다. `verifyLast4Action`만 `clientIp(await headers())`(`lib/client-ip.ts` · `next/headers`)로 IP를 읽어 domain에 넘긴다 — IP를 로그에 쓰지 않는다. 액션은 여전히 domain 함수 하나만 부르고 결과 유니온을 그대로 돌려준다. `submitCertificateAction`의 속도 제한은 두지 않는다 — 제출은 증표가 있어야 하고(추측 대상이 아니다) 본문 한도가 이미 있다.

⑦ **통합 `test/integration/cert-verify.test.ts`:** 위 `behavior`의 통합 케이스 전부(공격 케이스 넷 포함). 4차에 더한 넷(누적 잠김이 맞음 재생보다 앞섬 · 읽을 때 만료 · 맵 상한 · 잠금 다시 확인의 닫힘 우선·`limit`)과 최종 리뷰 B1의 틀림 비축출 케이스는 ④ (d0)·(d)·(h)와 ⑤-b를 고치기 전에 먼저 써서 실패를 확인한다(RED — 틀림 비축출은 개수 상한이 `wrong`까지 밀어내는 구현에서 `throttled`가 아니라 판정이 나와 실패한다). 동시 요청은 `Promise.all`로 보낸다(통합 프로젝트는 파일 병렬 없음 — 한 파일 안의 동시성만 본다). IP는 domain 호출 인자로 바꿔 가며 넣는다.

⑧ 커밋 `feat(04.3): slot lockout and idempotent phone check` · `feat(04.3): rate limit phone check per event and ip` 식으로 의도별로 + 한국어 본문(한도·잠금 시간 기본값의 출처, 키별 보관 방식, 증표를 재생에서 바꾸지 않는 이유, 속도 제한 한도).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/verify-last4.test.ts test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-verify.test.ts test/integration/cert-intake.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 동시 틀림·동시 공격 케이스의 **셈이 틀리면**(판정 수 ≠ 20 · 잠금 두 번) 행 잠금(자리 또는 행사)이 빠졌다. 같은 케이스가 **`다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장` 거부나 시간 초과로** 실패하면 잠근 tx 안에서 전역 `db`(설정 읽기 등)를 불러 풀이 막힌 것이다(E3-09 — ④ (a0)대로 tx 전에 읽는다). 끼어든 키·증표 불변 케이스가 실패하면 키별 보관이 아니고, 시간대 케이스가 실패하면 `at` 비교가 `timestamptz`가 아니다(E3-34)</fails_when>
    <automated>test "$(grep -v '^\s*//' 'app/c/[token]/actions.ts' | grep -c 'await assertCertFeatureEnabled(')" -ge 4 &amp;&amp; pnpm vitest run --project unit test/unit/certs/public-route-boundary.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 공개 액션 넷 중 기능 게이트를 부르지 않는 것이 있거나(C1), `lib/client-ip`를 더하며 공개 경계 금지 import를 끌어왔다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/certs/verify-lock.ts`가 DB·설정을 import하지 않는 순수 모듈이고 속도 제한 상수 넷이 이름으로 있다
    - 한도 경계(4번째 = 남은 1번, 5번째 = 잠김)와 속도 제한 경계(39 · 40 / 19 · 20) 케이스가 단위 테스트에 있다
    - `ipKey`의 IPv4 통과 · IPv6 /64(압축 표기 포함, 대소문자·앞 0 무관) · IPv4-매핑(점 표기 `::ffff:1.2.3.4` · 대문자 접두 · 16진 표기 `::ffff:102:304`, 교차 R2-1 — 소문자 변환 뒤 파싱된 값으로 판정) · 서로 다른 /64 구별 케이스가 단위 테스트에 있고 녹색이다(M-3 · 교차 B3) · `certIpHash(secret, eventId, ip)`(순수, 교차 R2-2)로 고정 비밀·이벤트·IP → 고정 hex 골든 벡터 케이스가 있고 녹색이다 · `certIpHash`에 빈 `secret`을 주면 명확한 오류를 던지는 케이스가 있다
    - 동시 틀림 6개 케이스와 동시 공격(자리 30곳 → 판정 정확히 20) 케이스가 통합 테스트에 있고 녹색이다
    - 같은 키 재생 · 끼어든 키(A→B→A) · 동시 같은 키가 횟수를 더하지 않음을 단언하는 케이스가 있다
    - 맞음 재생이 같은 증표 · 같은 만료를 돌려주고 자리의 `verify_proof_hash`를 바꾸지 않음을 단언하는 케이스가 있다
    - 저장된 맵에 증표 평문 · IP 원문이 없음을 단언하는 케이스가 있다
    - `selectWinner`의 제출 자리·미제출 자리 응답 키 집합이 같음을 단언하는 케이스가 있다
    - 새 설정 키 둘이 `SETTING_DEFS`에 있고 registry-coverage 테스트가 녹색이다 — 누적 한도 20은 설정 키가 아니다(`grep -v '^\s*//' domain/settings/keys.ts | grep -ci "hard_lock\|cumulative"`가 0)
    - 누적 잠김 통합 케이스 다섯(짧은 잠김 넷을 지나 20번째 = `hardLocked` · 맞음이 누적을 줄이지 않음 · 재전송이 누적을 더하지 않음과 풀린 뒤 재전송 · 누적 19 동시 틀림 둘 · 누적 잠긴 제출 자리에 맞는 4자리 = `hardLocked`)이 있고 녹색이다
    - 재생 불가(복호 실패) 케이스가 `expiredProof`를 받고 자리의 `verify_proof_hash`가 더 새 증표의 해시 그대로임을 단언한다 · 제출 뒤 맞음 재생이 `submitted`를 받는다
    - 재생 순서 세 케이스(A 맞음 → 남이 누적 잠금 → A 재전송 = `hardLocked` · 셈 불변 / 61분 재전송 = 새 시도 / 21번째 `ok`가 가장 오래된 `ok`를 밀어냄)와 `lockStatus`의 `hardLockedAt` 우선 단위 케이스가 있고 녹색이다
    - (B1) `wrong` 1 + 새 키 `ok` 20 뒤에도 그 틀림이 IP·행사 셈에 드는 통합 케이스와, 개수 상한이 `ok`만 밀어내는 `pruneIdemEntries` 단위 케이스가 있고 녹색이다
    - `recheckWinnerLock`이 닫힌 행사에서 누적 잠긴 자리에도 `closed{reason}`을 돌려주고, `shortLocked`에 `limit`이 있음을 통합 테스트가 단언한다
    - `repositories/cert-winners.ts`의 두 함수가 `lockEventRow(viewer, eventId, tx)` · `countRecentMisses(viewer, {…}, tx)` 순서이고 `tx`에 기본값이 없다 — `grep -nE "lockEventRow\(viewer, tx|countRecentMisses\(viewer, tx" repositories/cert-winners.ts domain/certs/intake.ts`가 0줄(E3-24)
    - `countRecentMisses`의 SQL에 `::timestamptz`가 있고 `'at')::timestamp ` 꼴(시간대 없는 캐스트)이 없으며, 시간대 케이스가 녹색이다(E3-34)
    - `eventMissLimit` 경계 단위 케이스(1 · 80 · 81 · 500)와 명단 비례 · `cert.verify_throttled` 경고 통합 케이스가 녹색이고, 경고 인자에 IP 원문 · IP 해시가 없다(E3-08)
    - `grep -n "sha256(eventId" domain/certs/intake.ts`가 0줄이다 — IP 해시가 평문 SHA-256 솔트가 아니라 `BETTER_AUTH_SECRET` HKDF 파생 키의 HMAC-SHA256이다(E3-15)
    - `domain/certs/intake.ts` `verifyLast4`에서 설정 읽기가 `withTransaction(` 앞에 있다 — 콜백 안에 `getSettingValue(`가 없다(E3-09)
  </acceptance_criteria>
  <done>자리 단위 잠금·누적 잠김(고정 20)·키별 멱등 재생(증표 불변 · 재생 불가 시 재확인)·행사/IP 속도 제한·제출 비노출·공개 액션 게이트가 서버에서 판정되고 단위·통합 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: E2·E3 기본 화면 — 문의 줄 · 실패 줄 · 틀림·잠김·풀림 · 응답 없음 · 닫힘 전환 · 포커스·기록 항목 · 첫 진입 오류 · Phase 4 Button 확인(E3-05 · E3-01)</name>
  <files>ui/button/Button.tsx, ui/button/Button.module.css, app/c/[token]/intake-flow.tsx, app/c/[token]/flow-rules.ts, app/c/[token]/error.tsx, app/c/[token]/intake.module.css, test/unit/ui/button-a11y.test.ts, test/unit/certs/flow-rules.test.ts, test/e2e/mobile-cert-verify.spec.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「Copywriting Contract」 E 표(E1 첫 진입 실패 · E2 · E3 · E4 확인 시간 지남 · E6-b 행) · 「화면 계약」 E1(로딩 · 첫 진입 실패 · 서버 왕복 공통 · 포커스 이동) · E2 · E3 · 「뒤로 가기」 줄(고아 항목은 `history.back()`) · E6 — 줄 번호 대신 절 이름으로 찾는다
    - `test/e2e/helpers/cert.ts` · `test/e2e/cert.setup.ts`(04.3-02 산출물, C4) — `createCertEvent`(고유 이름 행사) · `withCertFeatureOff`(기능 끄기 범위) · `CERT_E2E_CONTACT_PHONE`, 기능·문의 전화는 setup이 한 번 켠다. 확인증 스펙은 이름에 `cert`가 들어가 `certs` 프로젝트(workers 1)에서 돈다. 이 태스크는 읽기만 한다
    - `docs/design/SYSTEM.md` §6-5(04.3-01 개정판) · §7-1(Phase 4 ⑦ 「이유가 있는 비활성과 진행 중 버튼은 네이티브 `disabled`가 아니라 `aria-disabled="true"`다」 줄 + 04.3-01이 더한 호출부 `aria-describedby` 한 줄 · 진행 중 `처리 중` 줄) · §10 포커스
    - `ui/button/Button.tsx`(Phase 4 판 전문) — 이미 있는 것: `useId` 이유 id가 호출부 `aria-describedby`와 합쳐짐 · 비활성/진행 중 = `aria-disabled="true"`(포커스 가능, `handleClick`이 `preventDefault`) · `reasonTone` · 진행 중 `…`(`aria-hidden`). 없는 것: `.sr-only` `처리 중` · 호출부 `aria-describedby`를 이유로 인정하는 개발 경고 예외
    - `test/unit/ui/button.test.ts`(Phase 4) — aria-disabled · 이유 연결 · pending · 호출부 describedby 합치기를 이미 단언한다(이 태스크의 새 단위 파일은 이 케이스들을 다시 쓰지 않는다)
    - `app/globals.css`의 `.sr-only`(전역 클래스 — 새로 만들지 않는다)
    - `app/c/[token]/intake-flow.tsx`(04.3-02 산출물 전문) — 단계 상태기계
    - `app/(app)/error.tsx` — 오류 경계 선례
    - `test/e2e/mobile-cert-intake.spec.ts`(04.3-02 산출물) — 헬퍼 사용 방식 재사용
    - Playwright `page.clock` 안내와 `toBeDisabled()`(`aria-disabled="true"`도 비활성으로 본다) — 잠금 풀림을 기다리지 않고 시계를 앞당긴다 · 1차 비활성은 네이티브 `disabled` 속성이 아니라 이 단언으로 본다
  </read_first>
  <behavior>
    - (단위) `pending` 버튼 마크업에 `.sr-only` `처리 중`이 있고 `…`는 `aria-hidden` 그대로다(04.3-⑦(c))
    - (단위, 경고 예외 — 04.3-⑦(b)) 비활성 `Button`에 `disabledReason` 없이 호출부 `aria-describedby="lock-box"`만 주면 개발 경고(`console.warn`)가 나지 않고 버튼 `aria-describedby`가 `lock-box`다 · 둘 다 없으면 경고가 한 번 난다(기존 계약)
    - (단위, Phase 4 회귀) Phase 4 `test/unit/ui/button.test.ts`가 고치지 않은 채 녹색이다(이유 id 연결 · `aria-disabled` · 호출부 describedby 합치기 — 이 태스크가 다시 구현하지 않는다)
    - (단위) `resolveHistoryEntry({stateStep: "verify", memoryStep: null})` → `{render: "E2", back: true}` · `{stateStep: "form", memoryStep: null}` → 같음 · `{stateStep: null, memoryStep: null}` → `{render: "E2", back: false}` · `{stateStep: "verify", memoryStep: "verify"}` → `{render: "E3", back: false}`
    - (단위) `isDefiniteResult`: 확정 판정 여덟 — `wrong` · `locked` · `hardLocked` · `ok` · `submitted` · `closed` · `expiredProof` · 입력 거부(`validationErrors`) → true · `throttled` · `serverError` · `undefined` · 모르는 `kind` → false
    - (E2E) 당첨자 `김민수`·`김문수`(같은 경품 · 구별 표시 `A`/`B`)와 `김하늘` · `이도윤`인 행사 → 목록에 `김*수` 두 행이 각자 2행 `{경품명} {수량}개 · A`/`· B`로 보이고 `김*늘` · `이*윤`은 한 줄이다
    - (E2E) 스펙이 자기 행사의 `cert_events.contact_phone`만 DB에서 `0315550000`으로 바꾸면(전역 설정은 건드리지 않는다 — C4) 목록 아래 문의 줄과 `tel:` 링크가 `031-555-0000`(`tel:0315550000`)이다 — 화면이 설정이 아니라 행사 사본을 읽는다
    - (E2E) `김*늘` → `0000` → `남은 횟수 4번` · 칸 비워짐 · 포커스가 칸
    - (E2E) 네 번 더 틀림(마지막 틀림은 1차 「전화번호 확인」을 눌러 보낸다 — 가장 흔한 길, D-8) → `틀린 번호가 5번 들어와 확인이 잠겼습니다 · HH:mm부터 다시 해 주세요`(한도·시각은 응답 값) · 칸 비활성 · 1차는 `toBeDisabled()`(= `aria-disabled="true"`, 네이티브 `disabled` 속성을 단언하지 않는다)
    - (E2E, 막힘 이유 색 — X-4 · 반영 검사 r1 B-2) `김*늘`을 고른 첫 E3에서 1차 이유 글이 정확히 `뒤 4자리 숫자를 적으면 확인할 수 있습니다`이고 그 요소의 `color`가 `rgb(78, 93, 89)`(`--muted` `#4E5D59` — `info`) · 잠긴 뒤 1행 이유(`틀린 번호가 5번 들어와 …`)의 `color`가 `rgb(155, 28, 28)`(`--danger` `#9B1C1C` — `block`) · 풀린 뒤 이유가 다시 `rgb(78, 93, 89)`(`toHaveCSS`)
    - (E2E) 잠긴 뒤 `page.clock`으로 응답의 남은 초 + 1초를 앞당기면(기본 잠금 시간 그대로 — 설정을 바꾸지 않는다) 입력이 다시 살아나고 1차가 원래 막힘 이유로 돌아가며 포커스가 칸이다(잠긴 동안 포커스가 `aria-disabled` 1차 「전화번호 확인」 위였다 — 마지막 틀림을 1차 클릭으로 보냈다, D-8 · 1차 갈래) · 다시 잠근 뒤 포커스를 문의 줄 `tel:`에 옮겨 두고 앞당기면 포커스는 `tel:`에 그대로다(UI-SPEC 6차 손질 1)
    - (E2E, `body` 갈래) 새 행사에서 마지막 틀림을 칸에서 `Enter`로 보내 잠근다 → 칸이 비활성이 되며 포커스가 빠져 `document.activeElement`가 `body`임을 먼저 단언 → `page.clock`으로 풀림 → 포커스가 칸(UI-SPEC E3 「잠금 풀림」 「문서 바깥이면 칸으로」 — 두 갈래를 한 케이스씩, 반영 검사 r1 B-3)
    - (E2E) 확인 요청을 `page.route`로 한 번 끊으면 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요` · 4자리 유지 · 다시 누르면 정상 결과
    - (E2E) 행사 한도를 넘긴 상태(db/client로 틀림 기록 채움)에서 4자리 → `확인이 잠시 멈췄습니다 · 잠시 뒤 다시 눌러 주세요` · 4자리 남음 · 1차 켜짐
    - (E2E) 목록이 열린 뒤 스펙이 자기 행사의 `closed_at`·`closed_reason`(`manual`)을 DB에 직접 채우고(닫기 domain은 04.3-10 몫) 행을 누르면 E6-b `담당자가 접수를 마쳤습니다 …`
    - (E2E) E3에서 브라우저 뒤로 → E2, 포커스는 방금 고른 행
    - (E2E, 고아 항목) E3에서 새로 고침 → E2가 서고 `history.state`의 단계가 E3가 아니며, 이어서 브라우저 앞으로 가기를 해도 E2다(E3 화면이 서지 않는다)
    - (E2E, C1 직접 POST) 기능이 켜진 상태에서 틀린 4자리의 실제 `verifyLast4` 요청 하나를 `page.waitForRequest`로 잡아 `Next-Action` 헤더와 본문을 저장 → `withCertFeatureOff` 범위 안에서 본문의 멱등 키 문자열만 새 키로 바꾼 요청(게이트가 없으면 새 틀림으로 세어질 요청)을 `page.request.post`로 보냄 → 그 자리의 `failed_attempts`가 1 그대로이고 맵 항목 수도 그대로이며 응답 본문에 `remaining` 판정이 없다 → 범위를 나와 기능이 다시 켜진 뒤 같은 요청은 정상 판정된다(요청 모양이 맞았음을 증명)
  </behavior>
  <action>
⓪ **RED 먼저:** 구현 전에 `test/unit/ui/button-a11y.test.ts`(①) · `test/unit/certs/flow-rules.test.ts`(③의 두 함수)의 `behavior` 단위 줄과 `test/e2e/mobile-cert-verify.spec.ts`의 이 태스크 `behavior` E2E 열 줄을 먼저 쓰고, 단위는 실패를, E2E는 없는 상태 문장·포커스에서 실패함을 확인한 뒤 ①~⑥으로 채운다. Phase 4 `test/unit/ui/button.test.ts`는 먼저 한 번 돌려 녹색인지 본다(기준선).

① **`ui/button` — Phase 4 구현 확인 + 두 조각만(04.3-⑦(b)(c), E3-01):** 이유 요소의 `useId` id · 호출부 `aria-describedby`와 합치기 · `aria-disabled`는 Phase 4에 이미 있으므로 다시 만들지 않는다(Phase 4 `button.test.ts`가 그 계약의 정본이다). 고치는 것 둘: (b) 개발 경고 조건을 「`disabled` · `pending` 아님 · `disabledReason`도 호출부 `aria-describedby`도 없음」으로 바꾼다 — 누적 잠김 1차가 알림 묶음을 이유로 삼는 쓰임(UI-SPEC E3 「누적 잠김」, Task 2b). 런타임 동작 불변. (c) `pending`이면 `…`(`aria-hidden` 그대로) 옆에 전역 `.sr-only` 클래스로 `처리 중`을 그린다. 주석은 01 SUMMARY 「04.3-01 대응표」의 표지(「04.3-⑦(b)」 · 「04.3-⑦(c)」)로 적는다 — Phase 4 주석의 「§7-1 개정 ⑦」과 헷갈리지 않게. `button-a11y.test.ts`는 Phase 4 테스트와 겹치는 케이스(이유 id 연결 · `aria-disabled`)를 다시 쓰지 않고 새 케이스 셋(경고 예외 · 경고 한 번 · `처리 중`)만 둔다. 단위 테스트는 `react-dom/server`로 마크업을 그려 단언한다. 기존 호출부의 보이는 모양은 바뀌지 않는다.

② **E2 완성(`intake-flow.tsx`):** 행 누름 중 누른 행 오른쪽 `…` + 목록 전체 비활성 · 300ms 넘으면 상단 진행 바 · 실패·20초 응답 없음 → 라벨 아래 실패 줄(`role="alert"`), 목록 재활성, 포커스 누른 행 · 응답 `closed` → E6-b · 응답에 `locked` 블록 → E3 잠김 상태로 곧바로 · 목록 아래 문의 줄(행사 사본 번호, `tel:` 3차). 목록 행 2행(`prizeLine` · `label`)은 트레이서가 그린 그대로. 응답의 `hardLocked: true` 갈래는 Task 2b가 더한다.

③ **RED → GREEN `app/c/[token]/flow-rules.ts`(순수, 브라우저 API 없음):** `resolveHistoryEntry({stateStep, memoryStep})`(고아 항목 판정) · `isDefiniteResult(result)`(확정 판정 여덟 가지 — `wrong` · `locked` · `hardLocked` · `ok` · `submitted` · `closed` · `expiredProof` · `validationErrors` — 만 true, `throttled`는 확정 판정이 아니다). 위 `behavior` 단위 줄을 `test/unit/certs/flow-rules.test.ts`에 먼저 쓰고 실패를 확인한 뒤 구현한다. 04.3-06 제출도 `isDefiniteResult`를 쓰고 제출 결과 종류를 더한다. `recheckOutcome`은 Task 2b.

④ **E3 기본 상태(`intake-flow.tsx`):**
  - **칸과 제출(교차 r2 R2-2 — `body` 갈래 E2E가 기댄다):** 뒤 4자리 칸과 1차 「전화번호 확인」(`type="submit"`)은 같은 `<form>` 안에 두고, 확인은 폼 `onSubmit` 하나로 보낸다(`preventDefault()` 먼저 · 1차에 따로 `onClick`을 달지 않는다). 그래서 칸에서 `Enter` = 1차를 누른 것과 같은 제출이다 — 1차는 네이티브 `disabled`가 아니라 `aria-disabled`라 브라우저 암묵 제출이 1차에 click을 보내고, 1차가 막힘이면 Phase 4 `Button`의 `handleClick`이 그 click을 취소한다(T-04-360).
  - 틀림: 칸 `--danger` · `aria-invalid` · 아래 줄 `aria-live="polite"` · 값 지움 · 포커스 유지.
  - 잠김: 칸 비활성 · 1차 비활성(Phase 4 `Button` — `aria-disabled`라 포커스는 남을 수 있다) · 제출 줄 두 줄, 한도·시각은 응답 값.
  - 풀림: 응답의 남은 초로 마감을 잡는다(받은 순간 기준 — 기기 시계 차이 무시) · 마감 도달 또는 `visibilitychange` visible · `pageshow`에서 지났으면 복귀 · 포커스가 `body`이거나 잠김 묶음 안(묶음 자체 · 지워지는 요소) · 1차 「전화번호 확인」 위(`aria-disabled` 1차 — 풀린 뒤에도 칸이 비어 비활성이라 거기 남을 이유가 없다, /plan-design-review D-8)이면 칸으로, 사용자가 다른 곳(문의 줄 `tel:` 등)에 둔 포커스는 그대로(UI-SPEC E3 「잠금 풀림」 — 문장 그대로 구현한다).
  - `expiredProof`(재생 불가): 칸 아래 UI-SPEC 「확인 시간 지남」 줄(`확인 시간이 지났습니다 · 전화번호 뒤 4자리를 다시 적어 주세요`), 칸·1차 살아 있음, 키는 끝난다(다음 누름 = 새 키).
  - 멱등 키: 요청마다 `crypto.getRandomValues`로 만들고, 키는 `isDefiniteResult`가 참인 응답을 받을 때만 끝난다 — 거짓(연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가 · `throttled`)이면 다음 누름에 같은 키를 다시 보낸다. 줄은 `throttled`면 UI-SPEC 「E3 확인 잠시 멈춤」 `확인이 잠시 멈췄습니다 · 잠시 뒤 다시 눌러 주세요`, 그 밖은 결과 불명 줄이다 — 4자리 유지 · 1차 살아 있음 · 칸 테두리 그대로 · 남은 횟수 없음 · 1차 `aria-describedby`는 둘 다 같다(D-3, `isDefiniteResult`는 바꾸지 않는다). 4자리를 바꾸면 새 키. 20초 타이머와 네트워크 실패를 같은 줄로.
  - `closed` → E6-b · `submitted` → E6-a. 4자리가 아니면 서버를 부르지 않는다(비활성 + 이유).
  - **막힘 이유의 색(X-4 · UI-SPEC Color 「막힘 이유의 색」 · §6-5 개정 ①-m ② — 반영 검사 r1 B-2):** 1차 막힘 이유 `뒤 4자리 숫자를 적으면 확인할 수 있습니다`(첫 진입 · 4자리 덜 참 · 풀린 뒤 — UI-SPEC Copywriting 「E3 1차 막힘 이유」)는 Phase 4 `Button`에 `reasonTone="info"`(`--muted`)로 넘기고, 짧은 잠김 1행 `disabledReason`(`틀린 번호가 {한도}번 들어와 확인이 잠겼습니다 · {18:45}부터 다시 해 주세요`)은 `block`(`--danger` — `Button` 기본값)이다. 같은 1차의 이유가 바뀌면 색도 그 문장을 따른다(잠김 → `block`, 풀림 → `info`). 결과 불명 · 확인 잠시 멈춤 줄은 `--danger` 그대로(UI-SPEC Copywriting).
  - `hardLocked` 응답의 누적 잠김 상태는 Task 2b가 더한다(이 태스크에서는 `isDefiniteResult`가 참인 확정 판정으로만 받는다).

⑤ **포커스·제목·기록 항목(UI-SPEC E3 「뒤로 가기」 그대로):**
  - 단계 전환 때 UI-SPEC E1 「포커스 이동」 표대로 옮기고 `document.title`을 바꾼다.
  - E2 → E3 `history.pushState({step: "verify"})` · E3 → E4·E5·E6 `replaceState` · 「다른 이름 고르기」 = `history.back()`.
  - `popstate`로 E2 항목에 오면 E3·E4 메모리를 비운다.
  - **고아 항목:** 세 시점 — `popstate`(앞으로 가기) · 첫 마운트(`useEffect` 한 번, 새로 고침 뒤 `history.state`가 남아 있다) · `pageshow`에서 `event.persisted`(bfcache 복원) — 모두 `resolveHistoryEntry({stateStep: history.state?.step ?? null, memoryStep})`를 부르고 `back`이면 E3·E4 메모리를 비우고 E2를 그린 뒤 `history.back()`. `replaceState`로 그 항목을 E2로 바꾸지 않는다(E2 항목이 둘이 된다). bfcache 복원은 메모리가 남아 있어도 확인 상태를 버린다(메모리 단계를 null로 보고 판정 — 떠났다 돌아온 화면에 주민등록번호 입력 단계를 되살리지 않는다).
  - URL은 바꾸지 않는다.

⑥ **`app/c/[token]/error.tsx`(UI-SPEC E1 「첫 진입 실패」 · Copywriting E1 행):** `"use client"` 오류 경계. 상단 바 + 제목 `기타소득 지급 확인`(부제 없음 — 행사를 모른다) + 결과 블록(2px 선 아래, §6-5 `.state`) 첫 줄 `확인증 화면을 불러오지 못했습니다 · 잠시 뒤 다시 시도해 주세요`(`--fs-md` 700, `tabindex=-1`) + 3차 「다시 시도」 → `location.reload()`(같은 URL — `reset()`이 아니다). 1차 없음. 마운트 때 포커스를 결과 블록 첫 줄로 옮기고 `document.title = "오류 · 기타소득 지급 확인"`. E6-c(404 · `not-found`)와 파일·문장을 섞지 않는다. 오류 객체의 메시지·digest를 화면에 그리지 않는다.

⑦ **E2E `test/e2e/mobile-cert-verify.spec.ts`(C4):** 이 태스크 `behavior`의 E2E 열 줄. 행사 준비는 `test/e2e/helpers/cert.ts`(04.3-02)의 `createCertEvent`만 쓴다(테스트마다 고유 이름 행사) — 기능·문의 전화(`CERT_E2E_CONTACT_PHONE`)는 `cert.setup.ts`가 한 번 켜 두므로 스펙이 전역 설정(`cert.enabled` · `cert.contact_phone` · `cert.verify.*`)을 직접 바꾸지 않고, 기능 끄기는 `withCertFeatureOff` 범위 안에서만 한다. 파일 이름에 `cert`가 있어 `certs` 프로젝트(workers 1)에서 돈다 — 스펙 머리에 `test.describe.configure({ mode: "serial" })`와, 프로젝트가 폰 폭을 주지 않으면 `test.use({ viewport: { width: 375, height: 800 } })`를 둔다. 목록 전체가 비었다는 식의 전역 단언을 하지 않는다(자기 행사만 본다). 스펙이 DB를 직접 고치는 것은 자기 행사 행(`contact_phone` · `closed_at`)뿐이다(Task 2b가 자리 칸 몇 개를 더한다). **1차 비활성은 `toBeDisabled()` 또는 `aria-disabled="true"`로만 단언한다** — Phase 4 `Button`은 네이티브 `disabled`를 쓰지 않는다(E3-01).

⑧ 커밋 `feat(04.3): complete name pick and phone check states` · `fix(04.3): button pending is announced and caller reason counts` · `feat(04.3): first-load error screen for recipient link` 식으로 의도별로, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/button-a11y.test.ts test/unit/ui/button.test.ts test/unit/certs/flow-rules.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 특히 Phase 4 `button.test.ts`가 실패하면 ①이 Phase 4의 이유 연결 · `aria-disabled` 계약을 건드렸다(E3-01)</fails_when>
    <automated>grep -q 'location.reload()' 'app/c/[token]/error.tsx' &amp;&amp; grep -q '오류 · 기타소득 지급 확인' 'app/c/[token]/error.tsx' &amp;&amp; grep -q '확인증 화면을 불러오지 못했습니다' 'app/c/[token]/error.tsx' &amp;&amp; grep -q 'history.back()' 'app/c/[token]/intake-flow.tsx' &amp;&amp; grep -q 'persisted' 'app/c/[token]/intake-flow.tsx'</automated>
    <fails_when>종료 코드가 0이 아니다 — 첫 진입 오류 화면이 UI-SPEC E1 문장·`location.reload()`·제목을 따르지 않거나, 고아 항목 규칙(`history.back()` · bfcache `persisted`)이 빠졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>error가 한 줄이라도 나오고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/mobile-cert-verify.spec.ts test/e2e/mobile-cert-intake.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 트레이서 스펙이나 기존 접근성 스펙이 `Button` 변경으로 깨지면 여기서 걸린다</fails_when>
  </verify>
  <acceptance_criteria>
    - `pending`의 `처리 중`과 호출부 `aria-describedby`만 준 비활성 `Button`의 경고 없음을 `button-a11y.test.ts`가 단언하고, 그 파일에 Phase 4 `button.test.ts`와 같은 이유 id 연결 케이스가 없으며 두 파일 모두 녹색이다(E3-01)
    - `grep -n "localStorage\|sessionStorage" -r app/c`의 결과가 0줄이다
    - 잠김 문장의 숫자·시각이 응답 값에서 오고 `intake-flow.tsx`에 한도 숫자 리터럴이 없다(한도·분 값은 서버 응답 필드로만 그린다)
    - `resolveHistoryEntry` · `isDefiniteResult`(여덟 가지 true · `throttled` → false 포함) 단위 케이스가 있고 녹색이다
    - UI-SPEC E3 「잠금 풀림」(6차 손질 ① + D-8)을 E2E가 단언한다: 짧은 잠김이 풀릴 때 포커스가 1차 「전화번호 확인」 위면 칸(마지막 틀림을 1차 클릭으로 보낸 케이스 — D-8) · `body`면 칸(마지막 틀림을 칸 `Enter`로 보내 잠긴 동안 `document.activeElement`가 `body`임을 먼저 단언한 케이스) · 다른 곳(`tel:`)이면 그대로 — 두 갈래가 한 케이스씩 있다(반영 검사 r1 B-3)
    - E3 첫 진입 1차 막힘 이유가 `reasonTone="info"`이고(`grep -n 'reasonTone' 'app/c/[token]/intake-flow.tsx'`가 1줄 이상 — 04.3-06이 E4 몫을 더하기 전에도) E2E가 첫 진입 이유를 정확 일치 + `color` `rgb(78, 93, 89)`로, 짧은 잠김 1행을 `rgb(155, 28, 28)`로 단언한다(X-4 · 반영 검사 r1 B-2)
    - E2E가 throttled 줄 `확인이 잠시 멈췄습니다 · 잠시 뒤 다시 눌러 주세요`를 정확 일치로 단언하고, 그때 4자리가 남고 1차가 켜져 있다(D-3)
    - E2E의 1차 비활성 단언이 `toBeDisabled()` 또는 `aria-disabled`이고 네이티브 `disabled` 속성 단언이 없다(E3-01)
    - E2E가 이 태스크 `behavior`의 E2E 열 줄(새로 고침 고아 항목 · 기능 꺼짐 직접 POST 포함)을 전부 단언하고 녹색이다
    - E2E 스펙이 전역 설정 키를 직접 쓰지 않는다(`grep -cE "cert\\.(enabled|contact_phone|verify)" test/e2e/mobile-cert-verify.spec.ts`가 0 — 헬퍼 호출만)
    - 기존 `a11y.spec.ts`가 녹색이다
  </acceptance_criteria>
  <done>E2·E3의 기본 상태(겹침·문의·실패·틀림·잠김·풀림·응답 없음·닫힘·뒤로·고아 항목)와 첫 진입 오류 화면이 서고, Phase 4 `Button` 위에 두 조각(경고 예외 · `처리 중`)만 더해졌으며, 공개 액션 게이트가 직접 POST로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: 누적 잠김 묶음 · 잠금 다시 확인 — E3 누적 잠김 상태와 복구 길(E3-05)</name>
  <files>app/c/[token]/intake-flow.tsx, app/c/[token]/flow-rules.ts, app/c/[token]/intake.module.css, test/unit/certs/flow-rules.test.ts, test/e2e/mobile-cert-verify.spec.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「Copywriting Contract」 **E3 누적 잠김** 행 · 「화면 계약」 E3 「누적 잠김」 문단(알림 묶음 · 포커스 · 복구 길)
    - Task 2a가 만든 `app/c/[token]/intake-flow.tsx` · `flow-rules.ts` · `test/e2e/mobile-cert-verify.spec.ts` — 이 태스크가 넓힐 자리(같은 파일, 같은 규칙)
    - Task 1의 `recheckWinnerLock` · `recheckLockAction`(답 넷) · `selectWinner`의 `hardLocked: true`
    - `ui/button/Button.tsx`(Task 2a 뒤 판) — 호출부 `aria-describedby`만 준 비활성 1차는 경고 없이 그 id만 가리킨다
  </read_first>
  <behavior>
    - (단위) `recheckOutcome(result, trigger)`(잠금 다시 확인 응답 → `{next, focus}`, `trigger` = `"visible"` | `"button"` — 4차 E3 계약): `{kind: "closed", reason}` → 두 방아쇠 모두 `{next: "closed", focus: "step"}`(E6-b · 단계 전환 포커스 규칙 그대로 — 잠김보다 앞선다) · `{kind: "shortLocked", unlockAt, remainingSec, limit}` → 두 방아쇠 모두 `{next: "shortLock", focus: "group"}` · `{kind: "open"}` → 두 방아쇠 모두 `{next: "open", focus: "input"}` · `{kind: "hardLocked"}` → 버튼이면 `{next: "stay", focus: "group"}`, 보임이면 `{next: "stay", focus: "none"}` · `serverError` · 연결 끊김(`undefined`) · 모르는 `kind` → 버튼이면 `{next: "networkError", focus: "group"}`, 보임이면 `{next: "stay", focus: "none"}` · `{kind: "submitted"}` 같은 잠금 밖 응답도 `"open"`이 아니라 모르는 응답으로 본다(이 길은 E6-a로 가지 않는다)
    - (E2E, 누적 잠김) 스펙이 자기 행사의 한 자리 `cumulative_failed_attempts`만 DB에서 19로 바꾼 뒤(20번을 화면으로 넣지 않는다 — 서버 짧은 잠김 3분을 기다리지 않기 위해) 그 자리 → `0000` → UI-SPEC Copywriting 「E3 누적 잠김」 두 줄과 2차 「잠금 확인」이 묶음(`tabindex="-1"`, **`aria-live` 없음**) 안에 있고 `document.activeElement`가 그 묶음이며 · 칸 값이 비고 칸이 비활성 · 1차는 `toBeDisabled()`(= `aria-disabled="true"`) · 1차 `aria-describedby`가 두 `<p>`의 id **둘만**이고(묶음 id · 실패 줄 id가 아니다 — 4차 E3 계약) · 실패 줄이 DOM에 없으며 · 제출 줄 어디에도 `HH:mm`·`남은 횟수`·`20`이 없으며 · 문의 줄 `tel:` 링크가 있고 · 3차 「다른 이름 고르기」가 살아 있다 → `page.clock`으로 10분을 앞당겨도 입력이 살아나지 않는다 → 「다른 이름 고르기」 → E2 → 같은 행 → E3가 곧바로 누적 잠김 상태이고 포커스가 묶음이다
    - (E2E, 잠금 다시 확인 · 아직 잠김) 화면 숨김 → 보임(`page.evaluate`로 `document.visibilityState`를 `hidden` → `visible`로 바꾸고 `visibilitychange`를 쏜다 — Playwright에 탭 숨김 API가 없다) → 묶음 두 줄 · 포커스(보임 이벤트 전 자리 그대로 — 자동 다시 확인은 조용하다) · 자리 행 그대로 → 포커스를 칸 밖 다른 곳(문의 줄 `tel:`)에 둔 뒤 「잠금 확인」을 누르면 같은 두 줄(글이 다시 쓰이지 않는다)이고 `document.activeElement`가 묶음이고 실패 줄이 없다(누름의 `hardLocked` = 실패 줄 없이 묶음으로 포커스)
    - (E2E, 담당자 풀기 + 남이 제출 — E6-a 없음) 숨긴 동안 스펙이 그 자리의 네 칸(`failed_attempts` 0 · `locked_until` null · `cumulative_failed_attempts` 0 · `hard_locked_at` null)을 비우고(담당자 「잠금 풀기」 흉내 — 04.3-10은 더 늦은 웨이브) `submitted_at`을 채운 뒤(다른 기기의 진짜 수령자가 제출한 흉내) 보임 이벤트 → 묶음이 사라지고 칸이 살아나며 1차는 4자리 전 막힘 이유 · 포커스 = 칸 · 화면 어디에도 E6-a 문장(`이미 제출`)이 없다 · 다시 확인하는 동안 `verifyLast4` 요청이 나가지 않았다(앞서 잡은 확인 요청의 `Next-Action` id가 다시 보이지 않는다) · 두 셈 0 그대로 → 같은 이벤트를 `pageshow`로 한 번 더 쏴도 셈 불변
    - (E2E, 누적 → 풀림 → 새 짧은 잠김) 새 자리를 누적 잠김으로 만든 뒤 스펙이 네 칸을 비우고 짧은 잠김(`failed_attempts` 5 · `locked_until` = 지금 + 3분)을 넣는다 → 「잠금 확인」 → 같은 `tabindex="-1"` 묶음 안에 짧은 잠김 줄(`틀린 번호가 5번 들어와 …` — 한도는 다시 확인 응답의 `limit` · 서버 해제 시각 `HH:mm`) · 칸 비활성 · 1차 `toBeDisabled()` · `document.activeElement`가 그 묶음 · 1차 `aria-describedby`가 그 짧은 잠김 두 `<p>` id뿐이고 1차 아래에 `disabledReason` 요소가 없다(같은 글 두 번 없음) → `page.clock`으로 남은 초 + 1초를 앞당기면 입력이 다시 살아나고 포커스가 칸이다(묶음 안에 있던 포커스 → 칸, 자동 풀림 마감)
    - (E2E, 버튼 실패 → 다시 누름) 누적 잠김에서 다시 확인 요청을 `page.route`로 한 번 끊고 「잠금 확인」 → 묶음 안 버튼 아래 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요` 한 줄 · `document.activeElement`가 묶음 · 「잠금 확인」의 `aria-describedby`가 그 줄의 id · 1차 `aria-describedby`는 여전히 두 `<p>` id뿐 · 버튼은 살아 있다 → 다시 확인 요청을 `page.route`로 붙잡아 둔 채 다시 누르면 응답 전에 이미 실패 줄이 DOM에 없고 「잠금 확인」 `aria-describedby`에 그 id가 없다 → 요청을 놓아 정상 응답(`hardLocked`) 뒤에도 줄·연결 없음 · 포커스 = 묶음. 보임 이벤트로 나간 요청을 끊으면 화면에 아무 줄도 생기지 않고 포커스도 그대로다
    - (E2E, 누적 잠긴 채 닫힘 → E6-b) 새 자리를 누적 잠김으로 만든 뒤 스펙이 자기 행사의 `closed_at`·`closed_reason`(`manual`)을 채우고 「잠금 확인」 → E6-b `담당자가 접수를 마쳤습니다 …`(링크 단위 닫힘이 잠김보다 앞선다) · 포커스는 E6-b 단계 전환 규칙 그대로 · E6-a 문장 없음. 새 행사 하나에서 같은 준비 뒤 「잠금 확인」 대신 보임 이벤트를 쏴도 E6-b다
  </behavior>
  <action>
⓪ **RED 먼저:** `test/unit/certs/flow-rules.test.ts`에 `recheckOutcome` 단위 줄을, `test/e2e/mobile-cert-verify.spec.ts`에 이 태스크 `behavior`의 E2E 여섯 줄을 먼저 더하고, 단위는 실패를, E2E는 누적 잠김 묶음 · 포커스가 없어 실패함을 확인한 뒤 ①~③으로 채운다. Task 2a의 케이스는 고치지 않는다.

① **E2의 `hardLocked: true`(`intake-flow.tsx`):** `selectWinner` 응답에 `hardLocked: true`가 있으면 E3 누적 잠김 상태로 곧바로(포커스 = 알림 묶음, ③).

② **RED → GREEN `flow-rules.ts` `recheckOutcome(result, trigger)`**(잠금 다시 확인 응답 → `{next, focus}` — `next` = `closed` · `open` · `shortLock` · `stay` · `networkError`, `focus` = `step` · `input` · `group` · `none`. 실패 · 모르는 응답은 버튼이면 `networkError` + `group`, 보임 이벤트면 `stay` + `none` · 누름의 `hardLocked`는 `stay` + `group` — 4차 E3 계약의 포커스 규칙을 이 한 함수에 둔다).

③ **E3 누적 잠김과 복구 길(`intake-flow.tsx`):**
  - **누적 잠김(`hardLocked` 응답 또는 `selectWinner`의 `hardLocked: true` — UI-SPEC E3 「누적 잠김」):** 칸 값 지움 · 칸·1차 비활성 · `--danger` 글자. 누적 잠김일 때 1차 바로 뒤에 묶음 `<div id tabindex="-1">`을 그린다 — **`aria-live`를 달지 않는다**(포커스 이동만으로 읽힌다 — 알림과 포커스가 겹쳐 두 번 읽히지 않게, Codex UI 7차). 그 안에 UI-SPEC Copywriting 「E3 누적 잠김」의 두 줄(각각 id를 가진 `<p>`)과 2차 「잠금 확인」(아래 복구 길), 그리고 실패했을 때만 그리는 실패 줄을 둔다 — 1행 이유, 2행 담당자·문의 전화(`{문의 전화}`는 행사 사본 번호의 `tel:` 3차 — 짧은 잠김 2행과 같은 모양). 풀린 뒤 할 일을 적은 줄은 두지 않는다 — 아래 복구 길이 동작으로 대신한다. 문장은 UI-SPEC 문자열을 그대로 옮긴다(이 플랜이 새 문장을 만들지 않는다 — 실행 때 UI-SPEC 최종본과 다르면 UI-SPEC을 따른다). 이때 1차는 `disabledReason` 문자열을 받지 않고 호출부 `aria-describedby`로 **두 `<p>`의 id 둘만**(공백으로 이어) 받는다 — Task 2a ①의 경고 예외가 이 쓰임을 인정한다. 묶음 id를 주면 「잠금 확인」 라벨과 실패 줄이 설명에 섞이므로 묶음 id도 실패 줄 id도 주지 않는다(4차 E3 계약). `role="alert"` 없음(SYSTEM §10). 포커스는 누적 잠김으로 들어오는 모든 길(마지막 틀림 응답 · 재전송 응답 · E2에서 누적 잠긴 자리 고름)에서 묶음으로. 해제 시각·카운트다운·횟수를 그리지 않고 풀림 마감 타이머를 잡지 않는다(클라이언트는 풀지 않는다). 3차 「다른 이름 고르기」는 살아 있다 — 담당자가 푼 뒤 다시 고르면 서버가 보통 E3를 준다.
  - **누적 잠김 복구 길 — 잠금 다시 확인(UI-SPEC E3 「누적 잠김」 · Codex UI 7차 계약):** 두 방아쇠 — ① 누적 잠김 상태일 때만 `visibilitychange`(visible) · `pageshow`를 듣고 화면이 다시 보이면 조용히, ② 묶음 안 2차 「잠금 확인」 누름(진행 중 `pending`). 둘 다 `recheckLockAction`(Task 1 ⑤-b — 닫힘 또는 잠금 상태만 답한다)을 부르고, 진행 중이면 다음 방아쇠는 무시한다(한 번에 하나). 응답은 ②의 `recheckOutcome(result, trigger)`로만 가르고 포커스도 그 `focus`대로만 옮긴다(`none`이면 옮기지 않는다 — 보임 이벤트의 `hardLocked` · 실패는 조용하다): `closed` → E6-b(기존 닫힘 처리 그대로 — 고른 이름·적은 4자리를 버리고 단계 전환 포커스 규칙) / `open` → 묶음을 걷고 보통 확인 입력을 살리며 1차는 4자리 전 막힘 이유 · 포커스 = 칸 / `shortLock` → **같은 `tabindex=-1` 묶음 안에** 기존 짧은 잠김 두 줄(각각 id를 가진 `<p>` · 한도 = 응답 `limit` · 서버 해제 시각 `HH:mm`)을 그리고 1차는 `disabledReason` 문자열 없이 호출부 `aria-describedby` = 그 두 `<p>` id(UI-SPEC 6차 손질 3) · 포커스 = 묶음 · 기존 자동 풀림 마감을 잡는다(풀리면 Task 2a의 풀림 규칙대로 칸이 살아난다) / `stay` → 글을 그대로 두고(묶음을 다시 쓰지 않는다) `focus`가 `group`이면(누름의 `hardLocked`) 실패 줄 없이 포커스만 묶음으로 옮긴다 / `networkError` → 묶음 안 「잠금 확인」 아래 `확인 결과를 받지 못했습니다 · 다시 눌러 주세요` 한 줄(E3가 쓰는 문구 그대로 · 1차는 비활성 그대로)을 그리고 그 줄이 있는 동안만 「잠금 확인」 `aria-describedby`에 그 줄 id를 준다(숨긴 요소를 가리키면 설명으로 읽혀서 줄이 없을 땐 주지 않는다) · 포커스 = 묶음 · 버튼은 살아 있어 다시 누르고, **누르는 즉시(응답 전)** 줄과 「잠금 확인」 `aria-describedby` 연결을 함께 지우며 성공 응답이면 지워진 채 둔다(UI-SPEC 6차 손질 2). 이 길은 **E6-a로 가지 않는다** — 응답에 개인별 제출 여부가 없고, E6-a는 확인을 통과한 `verifyLast4`로만 간다(남이 제출한 뒤 풀린 자리도 입력이 살아나고, 맞는 4자리를 넣어야 E6-a). 다시 확인은 읽기라 셈 · 맵 · 증표를 건드리지 않고(Task 1 통합 단언) `verifyLast4`를 부르지 않는다 — 몇 번 물어도 누적이 늘지 않는다. 누적 잠김을 벗어나면 두 듣기를 뗀다.

④ **E2E(Task 2a ⑦의 규칙 그대로):** 스펙이 DB를 직접 고치는 것은 Task 2a의 자기 행사 행에 더해 자기 행사의 당첨자 자리의 `cumulative_failed_attempts`(누적 잠김 케이스) · 네 칸 비우기(담당자 풀기 흉내) · `submitted_at`(남의 제출 흉내) · 짧은 잠김 두 칸(`failed_attempts` · `locked_until`)뿐이다. 1차 비활성은 `toBeDisabled()` 또는 `aria-disabled`로만 단언한다(E3-01).

⑤ 커밋 `feat(04.3): cumulative lock state and lock recheck on phone check` + 한국어 본문.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/flow-rules.test.ts test/unit/ui/button-a11y.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — `recheckOutcome`의 방아쇠별 표가 계약과 다르다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>error가 한 줄이라도 나오고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test test/e2e/mobile-cert-verify.spec.ts test/e2e/a11y.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이거나 종료 코드가 0이 아니다 — 누적 잠김 묶음 · 포커스 · 1차 설명 연결 · 다시 확인 갈래 중 하나가 계약과 다르거나, Task 2a의 E2E가 이 태스크 변경으로 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `recheckOutcome`(네 갈래 · 방아쇠별 실패 처리 · 잠금 밖 응답 거부) 단위 케이스가 있고 녹색이다
    - 누적 잠김 묶음에 `aria-live`가 없다(`intake-flow.tsx`의 누적 잠김 묶음 요소 · E2E가 속성 부재를 단언)
    - E2E가 잠금 다시 확인 다섯(아직 잠김 → 그대로 · 누름이면 포커스 = 묶음 · 풀림 + 남의 제출 → 입력 · E6-a 없음 · 풀림 뒤 새 짧은 잠김 → 같은 묶음 안 짧은 잠김 줄 + 포커스 = 묶음 + 자동 풀림 · 버튼 실패 → 묶음 안 실패 줄 + 포커스 = 묶음 + 「잠금 확인」 `aria-describedby` = 그 줄 + 다시 누름 · 누적 잠긴 채 닫힘 → E6-b)과 다시 확인 동안 `verifyLast4` 요청 0 · 셈 불변을 단언한다
    - 누적 잠김 1차의 `aria-describedby`가 두 `<p>` id뿐임(묶음 id · 실패 줄 id 없음)을 E2E가 단언하고, `recheckOutcome`의 `{next, focus}` 표(방아쇠별)를 단위 테스트가 단언한다
    - UI-SPEC 6차 손질 ②③을 E2E가 단언한다: ② 누름의 `hardLocked` = 실패 줄 없음 · 실패 = 묶음 포커스 + 실패 줄 + 「잠금 확인」 연결, 다시 누르는 즉시(응답 전) 줄·연결이 지워짐 ③ 묶음 안 짧은 잠김에서 1차 `aria-describedby` = 짧은 잠김 두 `<p>` id · `disabledReason` 요소 없음
    - 누적 잠김 화면에 시각·카운트다운이 없다 — `intake-flow.tsx`의 누적 잠김 갈래가 풀림 마감 타이머를 잡지 않고, E2E가 `page.clock` 10분 뒤에도 입력이 비활성임과 포커스가 알림 묶음임을 단언한다
    - E2E가 이 태스크 `behavior`의 E2E 여섯 줄(누적 잠긴 채 닫힘 → E6-b 포함)을 전부 단언하고, Task 2a의 열 줄과 합쳐 열여섯 줄이 녹색이다
  </acceptance_criteria>
  <done>E3 누적 잠김 상태(묶음 · 포커스 · 1차 설명)와 잠금 다시 확인 복구 길(보임 이벤트 · 「잠금 확인」 · 닫힘 우선 · E6-a 없음)이 375px E2E로 고정된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 수령자 폰 → `verifyLast4` | 4자리 · 행 id · 멱등 키가 신뢰할 수 없는 입력이다. 틀림 횟수·잠금은 서버만 판정한다 |
| 공유 QR을 가진 누구나 → E2 목록 | 행사 링크는 여러 명이 같이 쓴다(D-1102) — 목록에 보이는 것이 곧 링크를 주운 사람에게 보이는 것이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-03 | Spoofing | 뒤 4자리 추측(자리 하나 겨냥) | high | mitigate | 자리 단위 잠금(한도 5 · 3분, 설정)을 행 `FOR UPDATE` 안에서 서버가 판정하고, 멱등 재생은 횟수를 더하지 않으며, 제출 여부는 맞힌 뒤에만 보인다. **C5 속도 제한(이 플랜, 설계 /cso 2026-09-25 확정):** 행사 행을 잠근 뒤 지난 15분 틀림이 행사 전체 `eventMissLimit(명단 수)`번(기본 `max(40, ceil(명단 × 0.5))` — E3-08) · 같은 IP 20번 이상이면 판정하지 않고 `cert.verify_throttled` 경고(`scope` · `eventId`만)를 남긴다(동시 요청으로 넘을 수 없음을 통합 공격 테스트가 증명). **C5 자리당 누적 상한(이 플랜, 소유자 결정 2026-09-24 21:56):** 누적 20번 틀림 → 담당자가 I3 「잠금 풀기」(04.3-10)로 풀 때까지 잠김 — 짧은 잠김 해제·맞음·재전송으로 줄지 않고, 누적 셈도 자리 행 `FOR UPDATE` 안에서만 더한다(누적 19 동시 틀림 테스트). **남는 위험(수치 — 설계 `/cso` 확정, E3-08):** 자리 하나를 겨냥한 공격자는 담당자가 풀기 전까지 최대 20번만 추측한다 → 자리당 성공 확률 약 0.2%(20 / 1만) — 행사 기간(72시간)·IP 수와 무관하게 묶인다. 행사 단위로 보면 당첨자 각자가 독립적으로 추측당할 수 있어 성공 확률의 상한은 `1 − 0.998ⁿ`(n = 당첨자 수)이다 — 30명이면 약 5.8~6%. 성공하면 공격자가 그 사람 이름으로 확인을 통과해 증표를 받고 뒤 제출(04.3-06)까지 마쳐 그 사람 몫의 택배 경품을 가로챌 수 있다(피해). 담당자가 풀 때마다 다시 20번이 생기므로, 풀기 전에 전화한 사람이 그 자리의 수령자인지 담당자가 확인하는 것이 운영 전제다(설계 `/cso`가 운영 안내로 확인). 누적 상한이 없던 때의 수치(IP 하나 약 7% · IP 여럿 약 22%)는 더 이상 남는 위험이 아니다 |
| T-04.3-02 | Information Disclosure | E2 목록 · `selectWinner` 응답 | high | mitigate | 제출 여부를 목록·행 고르기 응답에 싣지 않고(키 집합 동일 단언), E6-a는 4자리를 맞힌 뒤에만. 구별 표시는 담당자 글이고 04.3-04가 개인정보 조각을 거부한다 |
| T-04.3-24 | Tampering | 멱등 키 재생 | medium | mitigate | 키는 해시로만 저장되고 그 자리 행의 맵에만 묶인다 — 다른 자리에 같은 키를 보내면 새 요청으로 센다. 키마다 결과를 60분 보관해 끼어든 키 · 동시 같은 키도 두 번 세지 않고 누적 셈도 더하지 않는다. `ok` 재생은 처음 증표와 원래 만료를 돌려주고 자리의 현재 증표를 바꾸지 않는다 — 재생 증표는 `encrypt()` 암호문으로만 보관하고, 자리의 현재 증표 해시와 다르면 제출에 쓸 수 없다(04.3-06). 복호가 실패한 재생(키 회전)은 새로 판정·발급하지 않고 `expiredProof`로 끝나 더 새 증표를 덮어쓰지 않는다(Codex B-5, 통합 테스트) |
| T-04.3-29 | Tampering | 클라이언트 잠금 우회 | medium | mitigate | 잠금 해제 타이머는 화면만 되살린다. 일찍 눌러도 서버가 `locked`를 다시 준다(통합 테스트) |
| T-04.3-30 | Denial of Service | 남의 자리 일부러 잠그기 · 행사 한도 소진 | medium | accept | 링크를 가진 사람이 남의 자리를 3분씩 잠글 수 있고(담당자 풀기까지 가는 누적 잠김은 T-04.3-110), 틀린 번호 `eventMissLimit(명단 수)`번(기본 최소 40 — 명단에 비례, E3-08)으로 그 행사 전체의 확인을 **공격이 멈춘 뒤에도 최대 15분** 더 멈출 수 있다(창이 15분 슬라이딩이라 공격이 이어지는 한 막힘도 이어지고, 멈추면 그로부터 최대 15분 뒤 다시 열린다 — 멈춘 동안 수령자 화면은 결과 불명 줄 — 다시 누르면 한도가 풀린 뒤 판정된다, N-8 · 교차 R-3). 공격자는 현장 QR을 가져야 하고, 막힌 수령자에게는 목록 아래 문의 전화가 보이고, 멈출 때마다 `cert.verify_throttled` 경고가 남아 운영자가 원인을 찾을 수 있다(E3-08). 한도 수치(창 15분 · 행사 `max(40, ceil(n×0.5))`)는 설계 `/cso`가 2026-09-25 확정했다. throttled 문구를 「n분 뒤 다시 · 문의」로 바꿀지는 `/plan-design-review`에 넘긴다(UI-SPEC 계약) |
| T-04.3-110 | Denial of Service | 남의 자리 누적 잠그기(QR을 가진 누구나 틀린 번호 20번으로 자리를 담당자 풀기 전까지 잠글 수 있다) | medium | accept | 누적 잠김은 추측 방어(T-04.3-03)의 대가로 받아들인다(교차 B-1 — 창 15분·IP 20 수치로 재계산). 완화: ① 행사·IP 속도 제한이 한 IP의 틀림을 **15분에 20번** · 행사 전체를 **15분에 `eventMissLimit(명단 수)`번**(기본 `max(40, ceil(명단 × 0.5))`)으로 묶는다 — IP 하나로 빈 자리 하나를 누적 잠그는 데는 **약 15분**이면 충분하다(15분 IP 한도 20 = 누적 문턱 20, 한 창에서 다 쓸 수 있다). 행사 전체로도 15분마다 `eventMissLimit ÷ 20`곳(시간당은 그 4배 — 예: 명단 30명이면 한도 40 → 15분에 2곳 · 시간당 약 8곳, 명단 500명이면 한도 250 → 15분에 12곳 · 시간당 약 50곳)을 넘지 못하고, 명단 전체를 모두 누적 잠그려면 명단 크기 × 20회의 틀림이 필요해 30명 행사도 약 3.75시간 걸린다 · ② 담당자가 I3 「잠금 풀기」로 즉시 푼다(04.3-10) · ③ 수령자 화면은 담당자·문의 전화와 다시 고르기 안내를 보여 준다(E3 누적 잠김 2·3행). 잠긴 수·풀기 요청이 많으면 속도 제한 수치를 조정한다(설계 /cso 2026-09-25 확정 — 창 15분 · IP 20 · 행사 `max(40, ceil(n×0.5))`) |
| T-04.3-112 | Information Disclosure | 잠금 다시 확인(`recheckLockAction`) | medium | mitigate | 링크를 가진 누구나 부를 수 있는 읽기라, 답에 제출 여부가 섞이면 E6-a를 확인 없이 흘린다. 답은 넷(`closed{reason}` · `hardLocked` · `shortLocked{unlockAt, remainingSec, limit}` · `open`)뿐이고 개인별 제출 · 셈 · 이름을 싣지 않는다 — 제출된 자리와 안 한 자리의 응답 키 집합이 같음을 통합 테스트가, 풀린 뒤 남이 제출한 자리에서 E6-a가 서지 않음을 E2E가 단언한다. 잠금 상태는 이미 `selectWinner`가 주는 것과 같고, `closed{reason}`은 링크 단위라 이미 `selectWinner`·`verifyLast4`의 E6-b가 누구에게나 주는 것이며, `limit`은 짧은 잠김 문구에 이미 보이는 설정값이라 새로 드러나는 것이 없다. 쓰기가 없어 셈을 늘리지 않고(행 불변 단언), C1 게이트가 첫 줄이다 |
| T-04.3-145 | Tampering | 속도 제한 셈 지우기(맵 개수 상한 · 담당자 수정으로 틀림 항목이 빠짐) | medium | mitigate | 속도 제한은 자리 맵의 `wrong` 항목을 센다 — 개수 상한은 `ok` 항목만 밀어내고 `wrong`은 60분 나이로만 빠지며(④ (h)), 04.3-10의 이름·전화 수정도 `ok`만 지운다(교차 계약). `wrong` 1 + 새 키 `ok` 20 뒤에도 한도가 걸림을 통합 테스트가 단언한다(담당자 수정 쪽 테스트는 04.3-10) |
| T-04.3-80 | Information Disclosure | 속도 제한용 IP | low | mitigate | **키 있는 가명 · DB 단독 역산 불가(E3-15 · M-3, 설계 /cso 2026-09-25 확정):** IP 원문은 저장·로그하지 않고 `ipKey(ip)`(IPv4 원문 · IPv6 /64 prefix, M-3)를 `BETTER_AUTH_SECRET`에서 HKDF로 파생한 키의 HMAC-SHA256 앞 32자로 바꿔 자리 맵의 `wrong` 항목에 둔다(행사 사이 연결 불가). 옛 안(솔트한 SHA-256 단독 해시)은 IPv4 공간이 작고 솔트가 같은 DB에 있어 DB만 읽어도 역산 가능한 가명이었지만, HMAC 키가 DB 밖의 애플리케이션 비밀(`BETTER_AUTH_SECRET`)에 있어 DB 유출만으로는 되돌릴 수 없다(그 비밀까지 함께 유출돼야 역산된다). `wrong` 항목은 그 자리에 다음 쓰기가 있을 때만 60분 나이로 빠지므로 04.3-12 파기까지 남을 수 있다. 통합 테스트가 맵에 IP 원문이 없고 `cert.verify_throttled` 경고 인자에 IP 원문 · 해시가 없음을 단언한다 |
| T-04.3-16 | Elevation of Privilege | 기능 꺼진 공개 액션 직접 호출 | high | mitigate | 공개 액션 넷(잠금 다시 확인 포함)이 본문 첫 줄에서 `assertCertFeatureEnabled()`를 부른다(C1). 페이지를 거치지 않는 직접 POST가 판정·셈을 일으키지 않음을 E2E가 증명한다 |
</threat_model>

<verification>
- 단위 `verify-last4` · `flow-rules` · `button-a11y` · `registry-coverage` · `public-route-boundary` 녹색
- 통합 `cert-verify`(공격 케이스 포함) · `cert-intake` 녹색
- E2E `mobile-cert-verify` · `mobile-cert-intake` · `a11y` 녹색
- Phase 4 단위 `test/unit/ui/button.test.ts` 녹색 — 04.3-⑦(b)(c) 뒤에도 Phase 4 계약(`aria-disabled` · 이유 연결) 그대로(E3-01)
- 통합 · E2E는 한 번에 하나 — 같은 웨이브의 04.3-04 · 04.3-05와 동시에 돌리지 않는다(E3-33)
- `pnpm lint && pnpm typecheck && pnpm build` 녹색
</verification>

<success_criteria>
- 뒤 4자리 틀림·잠금·풀림·누적 잠김이 서버 판정 그대로 화면에 선다
- 누적 20번 틀림이 짧은 잠김 넷을 지나도 · 맞혀도 · 재전송해도 정확히 20에서 자리를 잠그고, 담당자 풀기 전에는 풀리지 않는다
- 응답 없음 뒤 재시도가 두 번 세지 않고, 끼어든 키·늦은 재전송·복호 실패 재전송도 두 번 세거나 증표를 바꾸지 않는다
- 행사·IP 속도 제한이 동시 요청에도 정확히 걸린다
- 목록과 행 고르기가 제출 여부를 흘리지 않는다
- 기능이 꺼지면 공개 액션을 직접 불러도 아무 일도 일어나지 않는다
- 행사 한도가 명단 크기에 비례하고(`eventMissLimit`), 한도가 걸릴 때마다 `cert.verify_throttled` 경고가 남는다(E3-08)
- 남는 추측 공격 위험(자리당 약 0.2% · 행사 단위 최대 약 6%, 30명 기준)과 누적 잠그기 방해 위험(T-04.3-110 — 15분에 IP 하나당 자리 1개 · 행사 전체 시간당 약 `eventMissLimit ÷ 5`곳)이 위협 모델에 수치로 남는다(설계 `/cso` 2026-09-25 확정 — 더 이상 미정이 아니다)
- 「정상 오타율 × 명단 크기」가 행사 한도에 닿는지(명단 최대 500명 · UI-SPEC A2 가정 「수~수십 명」과의 틈) — 설계 `/cso`가 창 15분 · 행사 `max(40, ceil(n×0.5))` · IP 20으로 확정했다(E3-08 · D-16, 2026-09-25). IP 한도 20이 현장 와이파이 · 통신사 CGNAT 공유 IP에서 정상 이용자를 멈추지 않는지는 `/qa` 실사용 확인 대상이다
- 증표 유효 시간 `VERIFY_PROOF_TTL_MINUTES`(30분)는 설계 `/cso`가 **연장 없이 그대로 유지**로 확정했다(X-10, 2026-09-25) — `expiredProof` 뒤 E3 재확인 → E4 메모리 값 되살아남 E2E는 04.3-06이 갖는다(교차 M-2). 이 플랜은 만료 시각을 쓰는 이 상수 이름만 확정 표시로 둔다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 설정 키 | `cert.verify.max_attempts`(기본 5) · `cert.verify.lock_minutes`(기본 3) |
| 함수 | `evaluateVerifyAttempt` · `lockStatus` · `verifyBudgetExceeded` · `pruneIdemEntries`(순수) · `ipKey`(순수, M-3) · `resolveHistoryEntry` · `isDefiniteResult` · `recheckOutcome`(순수, 화면) · `recheckWinnerLock` · `recheckLockAction` · `verifyLast4`·`selectWinner` 완성 · `eventMissLimit`(순수) · `lockEventRow(viewer, eventId, tx)` · `countRecentMisses(viewer, {…}, tx)`(리포지토리 — tx 마지막) |
| 상수 | `VERIFY_HARD_LOCK_THRESHOLD`(20, 고정 — 설정 아님) · `VERIFY_RATE_WINDOW_MINUTES`(15, 옛 `VERIFY_BUDGET_WINDOW_MINUTES`=60을 대체) · `VERIFY_MISS_BUDGET_PER_EVENT`(40) · `VERIFY_MISS_BUDGET_PER_IP`(20) · `VERIFY_IDEM_RETENTION_MINUTES`(60, 재생 보관 — 속도 제한 창과 별개) · `VERIFY_IDEM_MAX_ENTRIES`(20 — `ok` 항목만) |
| 결과 종류 | `hardLocked`(누적 잠김 — 확정 판정) · `expiredProof`(재생 불가 — 확정 판정) · `throttled`(확정 판정 아님 — 처리는 결과 불명과 같고 문장만 UI-SPEC 「E3 확인 잠시 멈춤」, D-3) |
| 컴포넌트 변경 | `ui/button/Button.tsx` — Phase 4 구현(`aria-disabled` · 이유 `aria-describedby`) 위에 개발 경고 예외(호출부 `aria-describedby`) · `pending` `.sr-only` `처리 중`만(04.3-⑦(b)(c)) |
| 로그 이벤트 | `cert.verify_throttled`(`log.warn`, `{scope, eventId}` — 개인정보 · IP 없음, E3-08) |
| 라우트 파일 | `app/c/[token]/error.tsx`(UI-SPEC E1 첫 진입 실패) |
| 테스트 | `test/unit/certs/verify-last4.test.ts` · `test/unit/certs/flow-rules.test.ts` · `test/unit/ui/button-a11y.test.ts` · `test/integration/cert-verify.test.ts` · `test/e2e/mobile-cert-verify.spec.ts` |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(/plan-eng-review E3 — 반영 지시 L387–L455@953e599, 04.3-03 항목 L404–L409). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---|---|---|
| E3-01 Phase 4 부품 전제(Button `aria-disabled` · `withTransaction` 5초 · 전역 `.sr-only`) | P1 | `execution_gate` · 목표 「실행 조건」 · Task 1 `<precondition>`. Task 2a ①을 「Phase 4 구현 확인 + 두 조각(04.3-⑦(b)(c))」으로 줄이고 Phase 4 `button.test.ts`를 verify · verification에 넣음. E2E 잠금 단언을 `aria-disabled` 전제(`toBeDisabled()`)로 고침 |
| E3-05 Task 2 과대 | P3 | Task 2를 2a(기본 E2·E3 + Button 확인) · 2b(누적 잠김 묶음 · 잠금 다시 확인)로 나눔 — 파일 경계 불변, estimate `tasks: 3` |
| E3-08 행사 한도 · throttled 가시성 | P2 | truth · Task 1 behavior(경계 1·100·101·500 · 명단 120 → 36 · 경고 케이스) · ④ (f) `eventMissLimit` · `log.warn("cert.verify_throttled", {scope, eventId})` · acceptance · T-04.3-03/30/110 · success_criteria 「정상 오타율 × 명단 크기」 · 산출물 표. 수치 → 설계 /cso에서 확정 |
| E3-09 설정 읽기 tx 밖 | P2 | Task 1 ④ (a0) 「트랜잭션 전에 읽기」 · 통합 `fails_when`에 「거부 · 시간 초과 = tx 안 전역 `db`」 |
| E3-15 IP 해시 서술 | P3 | T-04.3-80을 「역산 가능한 가명 · 파기까지 보관」으로 고침 · ④ (f) 같은 서술. HMAC → 설계 /cso에서 확정 |
| E3-24 리포지토리 인자 순서 | P3 | ③ `lockEventRow(viewer, eventId, tx)` · `countRecentMisses(viewer, {…}, tx)`(기본값 없음) · acceptance grep · 산출물 표 |
| E3-34 `at` 시간대 캐스트 | P3 | ③ `::timestamptz` 비교 명시 · `TZ=Asia/Seoul` 통합 케이스 · acceptance · `fails_when` |
| E3-32 E2E 헬퍼 | P2 | `verifyLast4` 입력에 `ip`를 더하므로 `test/e2e/helpers/cert.ts`의 `seedSubmittedCert` 호출 줄을 같은 커밋에서 고침(`files_modified`에 등재 — 04.3-02 C4 규칙) |
| E3-33 통합 · E2E 직렬 | P2 | `execution_gate.serial_tests` · verification 한 줄 |

### Review Feedback Deferred

| Concern | Reason |
|---|---|
| E3-03 | deferred — user check pending (default kept) |
| E3-04 | deferred — user check pending (default kept) |
| E3-08 throttled 문구(「n분 뒤 다시 · 문의」로 바꿀지) | UI-SPEC 승인 계약 — `/plan-design-review`에 넘김(L454). 이 플랜은 현재 「결과 불명」 줄 유지 |
| 풀림 순간 포커스가 `aria-disabled` 1차 위에 있을 때의 규칙(E3-01 여파) | Phase 4 `aria-disabled`는 포커스를 받으므로 UI-SPEC 포커스 규칙의 전제가 바뀜 — Task 2a는 「다른 곳」으로 다루고 문구 · 규칙 확정은 `/plan-design-review`에 넘김 |
| 행사 · IP 한도 수치(E3-08) · IP 해시 HMAC(E3-15) | → 설계 /cso에서 확정(L455) |

### Round 2 — 72829e6

> 출처: 04.3 eng-apply 재검토 r1 — Opus 플랜 체커(`eng-apply-checker-r1`, W/I 번호) · 교차 검토(`eng-apply-opus-cross-r1`, X/A 번호). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 A7 — `lockEventRow`를 07 · 12도 부른다는 문장(07 · 12는 부르지 않음, grep 0) | advisory | Task 2 `<interfaces>` `lockEventRow` 줄을 「04.3-06 제출 · 04.3-10」으로 좁히고 07 · 12는 행사 행을 잠그지 않는다고 적었다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 3 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3·D-7·D-10). 문장 정본은 UI-SPEC이고 이 플랜은 그 문장을 옮겨 쓴다.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| D-3 · X-2 throttled 문장 — L48@1c3a27b · L186@1c3a27b | 경고 · 막는 문제 | UI-SPEC 새 행 「E3 확인 잠시 멈춤」 `확인이 잠시 멈췄습니다 · 잠시 뒤 다시 눌러 주세요` — throttled truth 전체 교체(보고서 문장) · Task 2a ④ 멱등 키 줄(문장만 다르고 처리 같음, `isDefiniteResult` 불변) · Task 2a behavior E2E 「행사 한도를 넘긴 상태 → 4자리 남음 · 1차 켜짐」 · 산출물 표 결과 종류. **Supersedes Round 1 Deferred: E3-08 throttled 문구** — `/plan-design-review` D-3 확정 — 전용 문장, 처리는 결과 불명과 같음 |
| D-8 풀림 순간 포커스가 `aria-disabled` 1차 위 — L94@1c3a27b | 경고 | 잠금 풀림 truth · Task 2a ④ 풀림 줄에 `· 1차 「전화번호 확인」 위` 추가하고 「→ /plan-design-review」 넘김 문장 삭제 · behavior의 `Enter` 우회 삭제(마지막 틀림을 1차 클릭으로) · E2E 「1차 클릭으로 잠긴 뒤 풀림 → 포커스가 칸」. **Supersedes Round 1 Deferred: 풀림 순간 포커스가 `aria-disabled` 1차 위에 있을 때의 규칙** — D-8 확정 |
| D-16 현장 와이파이 · CGNAT와 IP 한도 — L128@1c3a27b | 참고 | success_criteria 「정상 오타율 × 명단 크기」 줄 끝에 IP 한도 10 · 공유 IP 점검 한 구절 — 수치는 /cso |
| X-10 E4 앱 전환 · 증표 유효 시간 — L220@1c3a27b | 참고 | success_criteria에 `VERIFY_PROOF_TTL_MINUTES` 조건 한 줄(30분 이상 · 입력 중 연장 여부 → /cso). 앱 전환 뒤 복귀는 UI-SPEC UI Considerations backstop · `/qa` 실기기 항목 |
| X-11 플랜의 「최종 승인본(커밋 d138427)」 — L224@1c3a27b | 참고 | objective 「UI-SPEC」 줄을 「최신 승인본(`2c6277b` + 2026-09-25 `/plan-design-review` 반영 — 이 파일의 현재 내용)」으로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| X-6 E3 상태기계 표 하나로 — L204@1c3a27b | 모양만 바꾸는 제안이라 이번 라운드에서 세 곳을 다시 쓰면 새 어긋남 위험이 더 크다 — UI-SPEC 「디자인 검토 반영」 절에 보류 이유 기록. 이 플랜의 E3 truth · action은 UI-SPEC 문장을 그대로 옮긴다 |
| 행사 · IP 한도 수치 · 증표 유효 시간 값(D-16 · X-10) | → 설계 /cso에서 확정 — 이 플랜은 점검 조건만 넘긴다 |

### Round 4 — 66a98e1

> 출처: 디자인 반영 검사 r1 — `design-apply-checker-r1.md`(Opus 플랜 체커) · `design-apply-cross-r1.md`(교차 검토 B-1~B-4 · N-1~N-14). **교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요.** 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 B-2 — X-4가 E3를 그리는 이 플랜에 옮겨지지 않음(`Button` 기본 `block`이라 첫 화면이 붉다) | 막는 문제 | 새 truth(E3 이유 색) · Task 2a ④ 「막힘 이유의 색」 줄(첫 진입 · 풀린 뒤 `뒤 4자리 숫자를 적으면 확인할 수 있습니다` = `reasonTone="info"`, 짧은 잠김 1행 = `block`) · behavior E2E 한 줄(정확 일치 + `rgb(78, 93, 89)` / `rgb(155, 28, 28)`) · acceptance 한 줄 |
| 교차 B-3 — D-8 뒤 E2E 서술 모순(1차 클릭으로 잠갔는데 포커스가 `body`였다고 적음 · 같은 케이스 두 번 · `body` 갈래 검증 없음) | 막는 문제 | behavior 풀림 줄 괄호를 「포커스가 `aria-disabled` 1차 위였다(1차 갈래)」로 · 옛 중복 줄을 칸 `Enter` 길(`body` 갈래 — `document.activeElement === body`를 먼저 단언)로 바꿈 · acceptance 풀림 줄이 두 갈래를 한 케이스씩 단언한다고 적음 |
| 교차 r2(`design-apply-cross-r2.md` @c9a728d — 교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요) R2-2 — `body` 갈래 E2E가 「칸에서 `Enter` = 제출」에 기대는데 Task 2a action에 폼 · 암묵 제출 줄이 없음 | 참고 | Task 2a ④ 첫 줄 「칸과 제출」: 칸과 1차(`type="submit"`)는 같은 `<form>` · 폼 `onSubmit` 하나로 보냄 · 막힘이면 Phase 4 `Button` `handleClick`이 암묵 제출 click을 취소(T-04-360) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 N-10 — 짧은 잠김 진입 알림이 정해지지 않음(1차 클릭 길에서 포커스는 1차에 남고 `aria-describedby` 대상만 바뀜) | 새 접근성 결정이다(누적 잠김처럼 묶음 포커스로 통일하면 이 플랜의 1차 갈래 E2E 전제와 UI-SPEC E3 계약이 함께 바뀐다) — 한 줄 수정이 아니다. UI-SPEC 「디자인 검토 반영」 절에 기록했고 Codex 재확인 때 정한다 |
| 교차 N-13 — E3 막힘 이유를 내부 꼴(`뒤 4자리 비어 있음 · 뒤 4자리 적기`)로 바꾸면 예외가 필요 없음 | 낮음. §6-5 확정 문장 `…채우면 제출할 수 있습니다`와 같은 꼴로 개정 ①-m ①이 예외를 적었다 — 바꾸면 확정 문장과 실물까지 다시 연다. 그대로 둔다 |
| 교차 N-8 — throttled 「잠시 뒤」와 최대 60분 멈춤 | 수치 · 문의 줄 여부는 설계 `/cso` 몫 — 04.3-13 「설계 `/cso` 입력 목록」 N-8 행으로 넘겼다 |

### Round 5 — 184df28

> 출처: `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`(설계 `/cso` — 제작자 판정 + 교차, 5건 blocking은 교차 수정안 채택). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-08 창·행사 한도 확정 | blocking(수치 미정) | 창 `VERIFY_RATE_WINDOW_MINUTES = 15`(옛 `VERIFY_BUDGET_WINDOW_MINUTES = 60` 대체) · `eventMissLimit = max(40, ceil(n×0.5))` — truth · key_links · behavior(eventMissLimit 경계 1·80·81·500, verifyBudgetExceeded 경계, 공격 케이스 6개, TZ 케이스) · action ②·④(f) · acceptance · T-04.3-03/30/110 · success_criteria · 산출물 표 전부 갱신 |
| D-16 IP 한도 확정 | blocking(수치 미정) | IP 한도 10→20/15분(경계 19→false·20→true) — 위와 같은 지점 전부 갱신, success_criteria 「IP 한도 20」 |
| N-8 throttled 「공격이 멈춘 뒤」 조건(교차 R-3) | 참고 | T-04.3-30에 「공격이 멈춘 뒤에도 최대 15분」 문구 추가 — throttled 화면 문구(D-3)는 바꾸지 않는다 |
| M-3 IPv6 /64 우회(신규 발견) | medium | truth · action ②(`ipKey` 순수 함수, 단위 테스트 2개) · action ④(f) · 산출물 표 exports·함수 · T-04.3-80 |
| E3-15 IP 해시 HMAC 키 출처 확정(교차 M-1 (b)) | blocking(계약 미정) | `ipHash = hmacSha256(HKDF(BETTER_AUTH_SECRET, "cert-ip-v1"), eventId + ipKey(ip))` — read_first(`lib/env.ts` · HKDF) · action ④(f) · truth · acceptance(`sha256(eventId` grep 0) · T-04.3-80 disposition 「키 있는 가명 · DB 단독 역산 불가」로 교체 |
| T-04.3-110/교차 B-1 위협 문장이 옛 60분 수치로 과대평가 | low(문서 정합) | 재계산해 교체: IP 하나 약 15분에 자리 1개 · 행사 전체 15분에 `eventMissLimit÷20`곳(30명 시간당 약 8곳) · 30명 전체 잠그는 데 약 3.75시간. T-04.3-03 「남는 위험」에 행사 단위 확률 `1−0.998ⁿ`(30명 약 6%)과 피해 문장(택배 경품 가로채기) 추가 |
| X-10 증표 30분 담당 이관(교차 M-2) | 참고 | success_criteria — 04.3-03은 `VERIFY_PROOF_TTL_MINUTES`(30분, 연장 없음) 상수 확정 표시만 남기고, `expiredProof` → E4 되살아남 E2E 소유는 04.3-06으로 명시 |
| 교차 r1(`cso-apply-cross-r1.md`) B1 — 이 라운드가 새로 쓴 idempotency truth(64)가 옛 「60분 `eventMissLimit` — `max(30, ceil(n×0.3))`」 문구를 그대로 남겨 T-04.3-110의 4× 재계산과 어긋남 | blocking | truth(64)를 「`wrong` 항목은 60분 보관·15분 창이라 최대 약 4 × `eventMissLimit(명단 수)`개(= 4×L, 기본 `max(40, ceil(명단 × 0.5))`)」로 다시 써 T-04.3-110과 같은 수(보관이 창의 4배라 상한도 4배) |
| 교차 B2 — acceptance(옛 「20곳 → 10」)가 behavior(211, 「30곳 → 20」)와 어긋남 | blocking | acceptance(271)를 「자리 30곳 → 판정 정확히 20」으로 고쳐 behavior와 맞춤 |
| 교차 B3 — `ipKey` 스펙이 `net.isIP` 갈래·IPv4-매핑·`::` 전개 규칙 없이 단순 `split(":").slice(0,4)`로도 통과할 만큼 느슨함 | blocking | `ipKey`(187·226·244·270)를 `net.isIPv4`/`isIPv6` 갈래 + IPv4-매핑(`::ffff:`) + `::` 8그룹 전개(RFC 5952 역) + 대소문자·앞 0 정규화로 다시 쓰고, 압축/완전 표기 동치 · 서로 다른 /64 구별 단위 케이스 넷 + 골든 벡터(고정 비밀·이벤트·IP → 고정 `ipHash` hex) 하나를 더함(단순 slice 구현은 이 케이스들에서 떨어진다) |
| 교차 note 3 — HKDF 세부(salt·Buffer 감싸기)와 키 없을 때 처리 미확정 | 참고 | ④(f)에 `crypto.hkdfSync("sha256", secret, Buffer.alloc(0), "cert-ip-v1", 32)` → `Buffer.from(...)` 감싸기 명시 · `BETTER_AUTH_SECRET` 없으면 파생 전에 분명한 오류를 던진다는 문장 추가 · acceptance(270)에 골든 벡터·오류 케이스 추가 |
| 체커 r1 I-2 — Round 1 줄(511, E3-08)의 「경계 1·100·101·500 · 명단 120 → 36」이 이번 라운드 behavior(185, `1→40·80→40·81→41·500→250`)와 다른 옛 수치 | info | Round 1 줄은 append-only라 그대로 두고 여기 새 줄로 정정: 확정 경계는 185(`eventMissLimit`: 1→40·80→40·81→41·500→250)와 213(명단 120명 한도 60, 61번째 `throttled`) — Round 1의 「120→36 · 1·100·101·500」은 옛 계산이고 이 줄이 대체한다 |
| 교차 r2(`cso-apply-cross-r2.md`) R2-1 — `ipKey` 매핑 판정이 소문자 변환보다 앞서(옛 226 순서) `::FFFF:1.2.3.4`(대문자)와 16진 매핑 `::ffff:102:304`가 매핑으로 안 잡혀 /64로 잘리는 문제 | blocking | 순서를 「먼저 소문자 → `::` 8그룹 전개 → 파싱된 값(앞 80비트 0 + 다음 16비트 `ffff`)으로 매핑 판정」으로 바꿈(187·226) — 문자열 `::ffff:` 부분 일치가 아니라 값 판정이라 대문자·16진 표기 모두 잡는다. 단위 케이스 둘 추가(대문자 접두 · 16진 매핑) |
| 교차 R2-2 — 골든 벡터·「비밀 없음 → 오류」 테스트를 붙일 순수 함수가 없어 `env` 모듈 재로딩(`vi.stubEnv`)이 필요해지는 문제 | blocking | `certIpHash(secret, eventId, ip)` 순수 함수를 새로 내보내(exports 85, action ② · behavior 187) `ipKey`·HKDF·HMAC을 그 안에 모으고, ④(f)는 `certIpHash(env.BETTER_AUTH_SECRET, eventId, ip)`를 부르도록 고침(244) — 골든 벡터·빈 `secret` 오류 케이스가 이 함수를 직접 불러 `env` import 없이 시험한다 |
| 교차 r3(`cso-apply-cross-r3.md`) R3-2 · R3-3 — `certIpHash` 리팩터로 HMAC 메시지 형식이 본문에서 사라짐(R3-2) · `ipKey`가 `isIPv6`도 아닌 쓰레기 XFF 값에서 「어느 쪽도 아니면 원문」 갈래 없이 예외를 던질 수 있음(R3-3) | 참고 | (f)에 메시지 형식 `eventId + ":" + (ip ? ipKey(ip) : "unknown")` 한 줄 복원(244) · `ipKey`에 `isIPv6`도 아니면 원문 반환하는 갈래 복원 + 단위 케이스 하나 추가(187·226) |

### Review Feedback Deferred
| Concern | Reason |
|--------|--------|
| (없음) | 이 라운드에서 04.3-03에 걸린 지적은 모두 반영했다 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-03-SUMMARY.md` when done
</output>
