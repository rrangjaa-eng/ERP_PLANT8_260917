---
phase: 04.3-qr-certificate-intake
plan: 03
subsystem: external-intake
status: complete
tags: [lockout, idempotency, rate-limit, hkdf, hmac, next-safe-action, playwright, a11y, history-api]

requires:
  - phase: 04.3-02
    provides: "트레이서 한 경로(domain/certs/intake.ts · 공개 액션 · E2~E6 화면) · cert_winners 잠금 칸(failed_attempts · locked_until · cumulative_failed_attempts · hard_locked_at · verify_idem_outcome) · 규약 C1~C4 · test/e2e/helpers/cert.ts"
provides:
  - "자리 단위 짧은 잠김(설정 cert.verify.max_attempts 5 · cert.verify.lock_minutes 3) + 누적 잠김(고정 20, VERIFY_HARD_LOCK_THRESHOLD)"
  - "키별 멱등 재생(맵 = 키 해시 → 항목, 60분 보관, ok만 20개 상한) — 증표 불변 · 재생 불가 시 expiredProof · 제출 뒤 submitted · 누적 잠김이 재생보다 앞섬"
  - "행사 · IP 속도 제한(15분 · 행사 max(40, ceil(n×0.5)) · IP 20) + 잠금 전 빠른 거부(AX-P2) + cert.verify_throttled 경고"
  - "IP 키 있는 가명 certIpHash(BETTER_AUTH_SECRET HKDF 'cert-ip-v1' → HMAC-SHA256 앞 32자, IPv6 /64 · IPv4-매핑 값 판정)"
  - "recheckWinnerLock + recheckLockAction(닫힘 · 누적 잠김 · 짧은 잠김{limit} · 열림 넷만)"
  - "repositories/cert-winners.ts: lockEventRow(viewer, eventId, tx) · lockWinnerInEvent · countRecentMisses(…, tx) · countRecentMissesUnlocked · writeWinnerVerifyState"
  - "E2 · E3 완성 화면(틀림 · 짧은 잠김 · 풀림 · 결과 불명 · 잠시 멈춤 · 닫힘 E6-b · 누적 잠김 묶음 · 잠금 다시 확인) · 기록 항목(pushState/replaceState/고아 항목) · 단계별 포커스 · document.title"
  - "app/c/[token]/error.tsx(E1 첫 진입 실패) · flow-rules.ts(resolveHistoryEntry · isDefiniteResult · recheckOutcome)"
  - "Button pending의 .sr-only 「처리 중」(04.3-⑦(c))"
affects: [04.3-04, 04.3-06, 04.3-08, 04.3-10, 04.3-12, 04.3-13]

actuals:
  tokens: 36300
  tasks: 3
  commits: 7
  plan_head_before: 70eef26ad758d804ff84655fb93065fa82aca566

tech-stack:
  added: []
  patterns:
    - "verifyLast4 순서: 설정 · IP 해시 tx 전 읽기 → 잠금 없는 빠른 셈(throttled면 tx 없이 반환) → withTransaction(행사 행 → 자리 행 FOR UPDATE → 닫힘 → 누적 잠김 → 키별 재생 → 짧은 잠김 → 잠근 뒤 다시 센 속도 제한 → 한 사람과만 대조 → 한 번의 쓰기)"
    - "확인증 공개 쓰기의 잠금 순서 규칙: 행사 행 → 자리 행(lockEventRow 머리 주석)"
    - "외부 화면 서버 왕복은 액션을 직접 부르고 20초 withDeadline으로 감싼다 — 확정 판정일 때만 멱등 키를 끝낸다(isDefiniteResult)"
    - "domain은 db를 import하지 못하므로 VerifyIdemEntry 타입을 repositories/cert-winners.ts에서 다시 내보낸다"

key-files:
  created:
    - domain/certs/verify-lock.ts
    - "app/c/[token]/flow-rules.ts"
    - "app/c/[token]/error.tsx"
    - test/unit/certs/verify-last4.test.ts
    - test/unit/certs/flow-rules.test.ts
    - test/unit/ui/button-a11y.test.ts
    - test/integration/cert-verify.test.ts
    - test/e2e/mobile-cert-verify.spec.ts
  modified:
    - domain/settings/keys.ts
    - domain/certs/intake.ts
    - repositories/cert-winners.ts
    - "app/c/[token]/actions.ts"
    - "app/c/[token]/intake-flow.tsx"
    - "app/c/[token]/intake.module.css"
    - "app/c/[token]/page.tsx"
    - ui/button/Button.tsx
    - test/integration/cert-intake.test.ts
    - test/e2e/helpers/cert.ts

key-decisions:
  - "verifyLast4 서명에 ip를 필수 위치 인자로 더했다(token, rowId, last4, idemKey, ip, now) — 기본값을 두면 액션이 IP를 빠뜨려도 조용히 'unknown' 한 칸으로 모인다. 기존 호출부(cert-intake 통합 24줄 · seedSubmittedCert 한 줄)는 ip null로 맞췄다"
  - "selectWinner · verifyLast4의 링크 닫힘은 notFound가 아니라 closed{reason, at}을 돌려준다(UI-SPEC E2 · E3 「링크 닫힘 → E6-b」)"
  - "recheckWinnerLock의 shortLocked에 unlockAtDisplay를 더 싣는다 — 분 올림 표시는 서버 한 곳(verify-lock.ts, node:crypto를 import해 클라이언트가 못 씀)에서만 만든다. 새 정보는 없다(같은 시각의 표시형)"
  - "E6-b 문장을 UI-SPEC Copywriting 그대로 ClosedResult 하나로 모아 진입(page.tsx)과 서버 왕복이 같은 블록을 그린다 — 옛 page.tsx 문장(「담당자가 링크를 닫았습니다」 등)이 UI-SPEC과 달랐다"
  - "단계별 document.title 이름: 이름 고르기 · 전화번호 확인 · 확인증 입력 · 제출됨 · 이미 제출 · 링크 닫힘(UI-SPEC은 꼴과 예 하나만 준다)"

patterns-established:
  - "잠금 전 빠른 거부(AX-P2): 잠금 없는 셈과 잠근 뒤 셈이 같은 내부 SQL 함수(queryRecentMisses) 하나를 쓴다 — ::timestamptz 비교가 한 곳뿐"
  - "E2E page.clock은 목록이 선 뒤에 설치한다(탐색 전에 설치하면 dev 스트리밍 렌더가 멈춘다)"

requirements-completed: [CERT-01]

coverage:
  - id: D1
    description: "자리 단위 짧은 잠김(설정 두 키) · 누적 잠김(고정 20) · 키별 멱등 재생(증표 불변 · 재생 불가 · 제출 뒤 · 누적 잠김 우선 · 읽을 때 만료 · ok 20개 상한)이 서버에서 판정되고 동시 요청에도 정확히 센다"
    requirement: "CERT-01"
    verification:
      - kind: unit
        ref: "test/unit/certs/verify-last4.test.ts(43)"
        status: pass
      - kind: integration
        ref: "test/integration/cert-verify.test.ts(42) — 동시 틀림 6 · A→B→A · 동시 같은 키 · 증표 불변 · 재생 불가 · 누적 잠김 공격 5종 · 61분 재전송 · 맵 상한"
        status: pass
    human_judgment: false
  - id: D2
    description: "행사 · IP 속도 제한(15분 · max(40, ceil(n×0.5)) · IP 20) · 잠금 전 빠른 거부로 풀 고갈 없음 · 틀림 비축출(B1) · cert.verify_throttled 경고(IP · 해시 없음) · TZ=Asia/Seoul timestamptz 비교"
    requirement: "CERT-01"
    verification:
      - kind: integration
        ref: "test/integration/cert-verify.test.ts > 속도 제한 · 잠금 전 빠른 거부 · 풀 고갈 없음(AX-P2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "IP 가명 ipKey(IPv6 /64 · IPv4-매핑 값 판정 · 쓰레기 값 원문) · certIpHash 골든 벡터 · 빈 비밀 거부"
    requirement: "CERT-01"
    verification:
      - kind: unit
        ref: "test/unit/certs/verify-last4.test.ts > ipKey · certIpHash"
        status: pass
    human_judgment: false
  - id: D4
    description: "E2 · E3 화면 — 겹침 2행 · 행사 사본 문의 줄 · 틀림 · 잠김(1차 클릭 · 칸 Enter 두 갈래) · 풀림 포커스 · 막힘 이유 색 · 결과 불명 · 잠시 멈춤 · 닫힘 E6-b · 뒤로 · 고아 항목 · 기능 꺼짐 직접 POST · 누적 잠김 묶음 · 잠금 다시 확인 다섯 갈래(375px)"
    requirement: "CERT-01"
    verification:
      - kind: e2e
        ref: "test/e2e/mobile-cert-verify.spec.ts(16) — dev 3회 연속 16/16, CI=true 체인 안에서 통과"
        status: pass
      - kind: unit
        ref: "test/unit/certs/flow-rules.test.ts(29) · test/unit/ui/button-a11y.test.ts(4) · test/unit/ui/button.test.ts(Phase 4, 무수정 녹색)"
        status: pass
    human_judgment: true
  - id: D5
    description: "첫 진입 5xx 오류 화면(E1) — 결과 블록 · 다시 시도 = location.reload() · 제목 · 포커스"
    requirement: "CERT-01"
    verification:
      - kind: other
        ref: "verify 둘째 줄 grep(location.reload() · 오류 · 기타소득 지급 확인 · 확인증 화면을 불러오지 못했습니다 · history.back() · persisted) 통과"
        status: pass
    human_judgment: true

duration: "약 3시간 10분"
completed: 2026-09-26
---

# Phase 04.3 Plan 03: 전화번호 뒤 4자리 확인 — 잠금 · 누적 잠김 · 멱등 · 속도 제한 · E2/E3 화면 Summary

**자리 단위 짧은 잠김(5회 · 3분 설정) + 누적 잠김(고정 20) + 키별 멱등 재생(증표 불변) + 행사·IP 속도 제한(HKDF-HMAC IP 가명, 잠금 전 빠른 거부)을 서버 판정으로 세우고, E2·E3 화면을 틀림·잠김·풀림·결과 불명·누적 잠김 묶음·잠금 다시 확인·고아 기록 항목·첫 진입 오류까지 완성했다.**

## Performance

- **Started:** 2026-09-26T18:00Z
- **Completed:** 2026-09-26T21:10Z(대략)
- **Tasks:** 3/3(Task 1 · 2a · 2b)
- **Commits:** 7(`git rev-list --count 70eef26..HEAD`)

## Accomplishments

- **Task 1(서버):** `domain/certs/verify-lock.ts` 순수 판정(누적 잠김 → 짧은 잠김 → 맞음 → 틀림, 누적 20이 짧은 잠김보다 앞섬 · `lockStatus` · 분 올림 표시 · `eventMissLimit` · `verifyBudgetScope/Exceeded` · `ipKey` · `certIpHash` · `pruneIdemEntries`), 설정 키 둘, `verifyLast4` 재작성(tx 전 읽기 · 잠금 전 빠른 거부 · 행사 행 → 자리 행 잠금 · 재생 · 잠근 뒤 속도 제한 · 한 번의 쓰기), `selectWinner` 잠김 표시, `recheckWinnerLock`, 공개 액션 넷 C1 게이트 · IP 전달
- **Task 2a(화면):** E2 실패 줄 · 진행 바 · 닫힘 전환, E3 한 폼(칸 Enter = 1차) · 틀림 · 짧은 잠김 · 풀림 포커스 규칙 · 결과 불명/잠시 멈춤(같은 멱등 키) · 막힘 이유 색, 기록 항목(push/replace/back · 고아 항목 세 시점), 단계별 포커스 · 제목, `error.tsx`, `Button` `처리 중`
- **Task 2b(화면):** 누적 잠김 알림 묶음(aria-live 없음 · 1차 설명 = 두 `<p>` id · 타이머 없음) · 잠금 다시 확인(보임 이벤트 · 「잠금 확인」, 닫힘 우선, E6-a로 가지 않음, 실패 줄 · 즉시 지움)

## Task Commits

1. `6a684aa` test(04.3-03): failing tests for slot lockout, idempotent replay and rate limit
2. `7b2f6a6` feat(04.3-03): slot lockout, idempotent phone check and per-event/ip rate limit
3. `aa77880` test(04.3-03): failing tests for name pick and phone check screen states
4. `3102f6a` fix(04.3-03): noun-style message for missing ip hash secret
5. `da88148` feat(04.3-03): complete name pick and phone check states
6. `739b3b0` test(04.3-03): failing tests for cumulative lock group and lock recheck
7. `2b8780e` feat(04.3-03): cumulative lock state and lock recheck on phone check

## RED 기록

- 단위 `verify-last4.test.ts`: `Cannot find package '@/domain/certs/verify-lock'`(구현 전) → GREEN 43/43(설정 키 포함)
- 통합 `cert-verify.test.ts`(구현 전): 모듈 없음으로 파일 실패. **(b1) 잠금 전 빠른 판정을 넣기 전** 두 AX-P2 케이스가 실제로 실패했다:
  - 잠금 전 빠른 거부: `AssertionError: expected 'timeout' to deeply equal { kind: 'throttled' }`(행사 행 잠금을 기다려 2초 안에 답이 없었다)
  - 풀 고갈 없음: `AssertionError: expected false to be true`(쥔 트랜잭션을 놓은 뒤 A 요청이 전부 throttled가 아니었다 — 잠금 대기열에서 5초 lock_timeout 거부)
  - (b1) 추가 뒤 42/42 · cert-intake 포함 74/74
- 단위 `button-a11y.test.ts`: `expected '<span class="_wrap_…' to contain '<span class="sr-only">처리 중</span>'`. 경고 예외 두 케이스는 Phase 4가 이미 지키던 조건이라 처음부터 녹색(회귀 고정)
- 단위 `flow-rules.test.ts`: `Cannot find package '@/app/c/[token]/flow-rules'`, 2b 추가분은 `recheckOutcome` 없음(TS2305)
- E2E 2a: 둘째 케이스 `toBeFocused`(E3 진입 포커스 없음) 실패 → serial로 나머지 건너뜀. 2b: `lockGroup toHaveCount(1)` 실패(묶음 없음)

## 검증 수치(마지막 커밋 2b8780e 기준)

- `pnpm typecheck` 0 · `pnpm lint` 0 · `pnpm lint:sql` 0 issues(19 files)
- 단위 전체 `pnpm vitest run --project unit` **1476/1476**
- 통합 전체 `pnpm vitest run --project integration` **1535/1535**(61 files, 직렬)
- `CI=true pnpm playwright test --project=cert-setup --project=certs`(desktop · mobile-375 의존 포함 전체 체인, 프로덕션 빌드) **392/392**(8.4m). 첫 실행은 `mobile-320-no-overflow.spec.ts` 관리자 화면 `/admin/archive` 스트리밍 대기 시간 초과 1건(부하 플레이크 — 단독 재실행 3/3 통과, 이 플랜과 무관한 화면) → 재실행 전부 통과
- `mobile-cert-verify.spec.ts` dev 모드 3회 연속 16/16
- acceptance grep: 게이트 호출 4 · (b1)이 `withTransaction`보다 앞 · `countRecentMisses(` 남음 · keys.ts에 hard_lock/cumulative 0 · `lockEventRow(viewer, tx` 0 · `sha256(eventId` 0 · `::timestamptz` 2(한 SQL 안 두 곳) · `'at')::timestamp ` 0 · `lib/db-transaction.ts` diff 없음 · `app/c`에 localStorage/sessionStorage 0 · E2E 스펙에 `cert.(enabled|contact_phone|verify)` 0

## .continue-here 이관 항목(/review → 04.3-03) 닫힘

| 항목 | 닫힘 | 어떻게 |
|---|---|---|
| verifyLast4 시도 제한 · 잠금 · 누적 잠금 | 닫힘 | Task 1 — 짧은 잠김(설정 5회 · 3분) · 누적 20 · 자리 행 FOR UPDATE 안에서만 셈, 통합 공격 테스트 |
| 남은 횟수 · 잠김 문구 | 닫힘 | Task 2a — `남은 횟수 {n}번`(짧은 잠김까지만) · `틀린 번호가 {한도}번 … {HH:mm}부터` · 누적 잠김 두 줄, 숫자 · 시각은 모두 서버 응답 값 |
| 증표 재발급 정책 | 닫힘 | 같은 키 재전송은 처음 증표 · 원래 만료를 그대로(자리 증표 해시 불변), 새 키의 맞음만 새 증표를 발급해 자리 증표를 바꾼다. 복호 실패는 발급 없이 expiredProof, 그새 제출이면 submitted, 누적 잠김이면 hardLocked |
| 단계별 포커스 · title · history | 닫힘 | Task 2a — UI-SPEC E1 포커스 표 · `{단계} · 기타소득 지급 확인` · E2→E3 pushState / 뒤 단계 replaceState / 「다른 이름 고르기」 = history.back() / 고아 항목(popstate · 첫 마운트 · bfcache) |
| 03 위협 모델: verify 외 공개 액션의 DoS | 수용 위험으로 기록 | 아래 「위협 메모」 |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] verifyLast4 서명 변경으로 기존 호출부 수정(files_modified 밖)**
- **Found during:** Task 1
- **Issue:** ip를 필수 위치 인자로 더하자 `test/integration/cert-intake.test.ts`의 호출 24줄이 맞지 않음(플랜은 helper 한 줄만 적음)
- **Fix:** 모두 `ip = null`을 넘기게 기계적으로 고침(`…, past)` → `…, null, past)` 포함). 표본 만드는 방법은 그대로
- **Commit:** 6a684aa

**2. [Rule 1 - Bug] 누적 잠김 공격 테스트의 locked_until 단언이 계약과 반대였음**
- **Found during:** Task 1 GREEN
- **Issue:** 테스트가 「누적 잠김 뒤에도 셋째 잠금 시각이 남아 있다」고 단언 — 실제 계약은 풀린 잠금 뒤 첫 틀림이 locked_until을 지우고 누적 잠김은 새로 쓰지 않는다(단위 계약 「잠금이 지난 뒤 틀림 → lockedUntil null」)
- **Fix:** `lockedUntil` null · `failedAttempts` 5로 단언(systematic-debugging으로 원인 확인)
- **Commit:** 7b2f6a6

**3. [Rule 1 - Bug] 내부 오류 문구 명사형 검사 위반**
- **Issue:** `certIpHash` 비밀 없음 오류가 높임말 종결 → `test/unit/error-copy-noun-style.test.ts` 실패
- **Fix:** `BETTER_AUTH_SECRET 없음 · IP 해시 키 파생 불가` · **Commit:** 3102f6a

**4. [Rule 1 - Bug] 진입 E6-b 문장이 UI-SPEC과 달랐다(page.tsx, files_modified 밖)**
- **Issue:** page.tsx의 닫힘 문장(「담당자가 링크를 닫았습니다」 등)이 UI-SPEC Copywriting E6-b(「담당자가 접수를 마쳤습니다 · 확인이 필요하면 …」)와 다름 — 서버 왕복의 E6-b와 두 벌이 될 뻔함
- **Fix:** `ClosedResult` 하나로 모아 page.tsx도 쓰게 함(기존 regression-2 E2E 녹색) · **Commit:** da88148

**5. [Rule 3] domain → db import 경계**
- **Issue:** domain이 `@/db/schema/cert-winners` 타입을 import하면 boundaries 규칙 위반
- **Fix:** repositories/cert-winners.ts에서 `VerifyIdemEntry` · `VerifyIdemOutcomeMap` 타입 재내보내기 · **Commit:** 7b2f6a6

**6. E2E 조정(테스트 쪽, 제품 동작 불변)**
- `page.clock.install()`을 목록이 선 뒤로 옮김 — 탐색 전에 설치하면 dev 스트리밍 렌더가 멈춰 본문이 비었다(같은 조건에서 설치 위치만 바꿔 확인)
- 틀림마다 응답을 기다린 뒤 다음 입력(진행 중에 적은 값이 응답의 「값 지움」에 덮였다)
- 새로 고침 고아 항목: 앞 문서로의 history.back() 동안 실행 문맥이 바뀌어 읽기를 재시도
- 2b 「아직 잠김」: 보임 이벤트의 다시 확인이 끝난 뒤 누름(진행 중 방아쇠 무시 계약과의 테스트 경합)
- **플랜과 UI-SPEC의 어긋남:** 플랜 behavior는 「잠근 뒤 포커스를 문의 줄 tel:에 두고 풀리면 tel:에 그대로」인데, E3의 tel:은 짧은 잠김 2행 안에만 있고 UI-SPEC E3 「잠금 풀림」은 그 줄을 지우며 지워질 요소 안의 포커스는 칸으로 보낸다. 정본(UI-SPEC)을 따르고 「다른 곳」 갈래는 「다른 이름 고르기」에 포커스를 둬서 단언했다

### 설계 판단(플랜 · UI-SPEC이 정하지 않은 자리)
- `lockEventRow`를 cert-winners.ts에 새로 둠(cert-events.ts는 04.3-04 몫이라 건드리지 않음) — 기존 제출은 cert-events의 `lockEventForUpdate`를 그대로 쓰며 잠그는 행 · 순서는 같다
- recheck `shortLocked`에 `unlockAtDisplay`를 더 실음(위 key-decisions)
- E2에서 짧은 잠김 자리를 고르면 포커스 = 1차(칸이 비활성이라 칸에 둘 수 없음, UI-SPEC은 누적 잠김 자리만 묶음으로 정함)
- 칸 아래 틀림 줄의 `aria-live="polite"`는 TextField를 고치지 않고 칸을 감싼 `<div aria-live="polite">`로 줬다(TextField가 오류 `<p>`를 조건부로 그려 live 영역을 그 안에 둘 수 없다)

## Known Stubs

없음. E4의 「확인 시간 지남」 되돌림(IntakeForm `onExpired` → E2)은 04.3-06 몫(플랜 success_criteria · 교차 M-2)으로 그대로 두었다.

## 알려진 틈 · 남은 위험

- **진행 중 「다른 버튼 비활성」 일부 미적용:** E3 확인 진행 중 3차 「다른 이름 고르기」는 비활성으로 바뀌지 않는다(1차는 pending, E2 행은 전부 비활성). UI Considerations loading E의 「다른 버튼 비활성」을 04.3-06(E4 제출 진행 중과 함께) 또는 /review에서 닫을 것
- **IP 없음 한 칸:** XFF가 없는 경로(로컬)는 행사마다 `unknown` 한 칸이 IP 한도 20을 나눠 쓴다(Cloud Run은 늘 XFF — 코드 주석)
- **dev 모드 Cache-Control:** `mobile-cert-intake.spec.ts`의 `cache-control: no-store` 단언은 dev 서버에서 `no-cache, must-revalidate`로 실패한다(이 플랜 전 코드에서도 같음 — error.tsx를 빼고 재현) · CI=true(프로덕션 빌드)에서는 통과
- **Codex:** 이 플랜 실행에 Codex를 쓰지 않았다(2026-09-29 07:13 KST까지 한도). 플랜의 교차 검토 자리(디자인 D-3 · D-7 · D-10 · 설계 /cso 교차)는 「Codex 대신 Opus 독립 검토 — 한도 풀리면 Codex 재확인」 그대로 남는다. 실행 결과 코드에 대한 독립 검토는 오케스트레이터의 DOM 감사 · /review 몫

## 위협 메모(설계 /cso · /review 확인 대상)

- **멱등 키 형식:** base64url 22~64자(클라이언트는 `crypto.randomUUID()` 36자) · 보관 60분 · ok 20개 상한 · 속도 제한 15분/행사 max(40, ceil(n×0.5))/IP 20 — 설계 /cso 확정값 그대로
- **수용 위험(.continue-here 이관):** 확인(verifyLast4) 밖의 공개 액션 — `selectWinner`(읽기, 목록은 이미 공개) · `recheckLockAction`(읽기, 제출 여부 없음) · `submitCertificate`(증표 필요, 본문 한도) — 에는 속도 제한이 없다. 링크를 가진 사람이 이 읽기를 빠르게 되풀이해 DB 읽기 부하를 줄 수 있으나 추측할 비밀이 없고 쓰기가 없다 → DoS는 받아들인 위험(인프라 요청 한도 · Cloud Run 동시성으로 묶임)
- 새 공개 액션 `recheckLockAction`은 플랜 위협 모델 T-04.3-112 그대로(답 넷 · 쓰기 없음 · C1 첫 줄) — 플랜 밖 새 표면 없음

## Review fixes(DOM 감사 · Opus 독립 검토 — Codex 대신, 한도 풀리면 Codex 재확인)

근거: 독립 DOM 감사(CI=true, 1280/375/320, 계산 스타일 실측) FAIL 6건 · Opus 독립 코드 리뷰 Medium 2건. TDD — `3e2d1f7`(RED: E2E 7건 실패 · 통합 4건 실패 확인) → `6621e05`(GREEN).

| # | 결함 | 수정 | 회귀 테스트 |
|---|---|---|---|
| F1 | 외부 1차 버튼이 `.btn`(뒤 선언)에 져 PC 32 · 폰 40px · 12px | `ui/button/Button.module.css` `.external` → `.btn.external`(명시도로 이김, 내부 버튼 불변) | E2E: 375 · 1280 모두 48px · 15px, 내부(로그인) 1차 1280 = 32px · 375 = 40px · 12px |
| F2 | `.input:focus-visible`이 `.inputError` 테두리를 덮음 | `.inputError, .inputError:focus-visible { --danger }` — SYSTEM §7-2에 내부 예외가 없어 공통 수정, 포커스 링 유지 | E2E: 틀림 직후 포커스 중 border = `--danger` · outline `--focus` solid, 내부 설정 쉼표 칸도 같음 · 오류를 지우면 --danger 아님 |
| F3 | 외부 칸 오류 12px | `TextField` `size="external"`일 때 `.errorExternal`(`--fs-md`) | E2E: 외부 15px · 내부 칸 오류 12px · 내부 칸 높이 32px |
| F4 | 이름 행 대기 중 `.sr-only` 「처리 중」 없음 | 행 버튼에 `…` 뒤 `.sr-only` 처리 중 | E2E: selectWinner POST를 붙잡고 행 안 sr-only 텍스트 |
| F5 · M1 | 확인 대기 중 3차 살아 있음 · 뒤로 가기 뒤 늦은 verify 응답이 E3/E4를 다시 그리고 `replaceState`로 E2 항목을 덮음 | 3차 `disabled={busy}`(aria-disabled, 설명 = 1차 「처리 중」) + `verify()` await 뒤 `stepRef.current`가 같은 rowId의 verify이고 `pendingKeyRef` 키가 같을 때만 반영(아니면 setStep · replaceState 없이 버림) | E2E: 대기 중 aria-disabled=true → 응답 뒤 풀림. 맞는 4자리 응답을 붙잡은 채 뒤로 → 풀기 → E2 유지 · E4 없음 · 제목 · `history.state` · `history.length` · URL 그대로 |
| F6 | 닫힌 링크 첫 진입 제목이 「이름 고르기」 | `page.tsx` 정적 `metadata` → `generateMetadata`(Next 16 문서 확인), `loadIntake`를 React `cache`로 감싸 한 요청에 한 번 읽기 | E2E: 닫힌 행사 첫 진입 제목 「링크 닫힘 · 기타소득 지급 확인」 |
| M2 | 셈을 올리지 않는 요청(누적 · 짧은 잠김 자리)이 행사 행 FOR UPDATE 대기열에 서서 풀을 붙듦 | `verifyLast4` (b0): 잠금 없는 읽기로 닫힘 → 누적 잠김 → (같은 키 재생 항목이 없을 때만) 짧은 잠김을 곧바로 반환(기존 응답 모양, 서버 해제 시각). 정본 판정은 잠근 뒤 그대로 | 통합: 누적 잠긴 자리 동시 확인 DB_POOL_MAX×3 동안 다른 행사 loadIntake 5초 안 · 전부 hardLocked / 짧은 잠김은 행을 남이 쥔 동안 locked · 셈 불변 / 닫힌 행사 누적 잠긴 자리 = closed / 닫힌 행사 + 한도 초과 = closed / 같은 키 재전송은 짧은 잠김 빠른 판정보다 재생이 앞섬 |

- **selectWinner 늦은 응답:** 대기 중 모든 행이 네이티브 disabled라 다른 행을 누를 수 없고, E2는 첫 기록 항목이라 뒤로 가기 대상이 앱 밖이다 → M1 같은 엇갈림이 생기지 않아 고치지 않았다.
- **Low #1(빠른 거부가 닫힘 판정보다 앞섬) — 함께 닫힘:** M2 (b0)이 닫힘을 빠른 거부(throttled)보다 먼저 본다(통합 테스트 「닫힌 행사가 한도를 넘었어도 closed」).
- **수용 위험(M2 잔여):** 같은 멱등 키 재전송 · 없는(또는 파기된) 행 id 폭주는 여전히 잠금 전 빠른 판정을 지나 행사 행 대기열에 선다(재전송은 재생 규칙상 잠근 뒤에만 판정 가능, 없는 행은 셈이 오르지 않음). 막으려면 행사 행 잠금 전 per-IP 요청 수 제한이 필요 — /cso 기록.
- **이관(/review · 04.3-06) — Low 8건(고치지 않음):** #2 빠른 거부가 멱등 재생보다 앞섬 · #3 recheckWinnerLock의 notFound가 닫힘보다 앞섬 · #4 저장된 wrong/locked 재생이 지금 자리 상태와 다를 수 있음 · #5 `notFound`에 화면 갈래가 없어 결과 불명 줄 반복 · #6 4자리 A→B→A 되돌리면 옛 키 재사용 · #7 보임 이벤트 재확인 중 「잠금 확인」 누름이 조용히 무시 · #8 시간대 통합 테스트가 `process.env.TZ`만 바꿈(PG 세션 TZ 경로 미검증) · #9 누적 20 · 행사 한도 조합의 설계 잔여(/cso 확정값, 기록만).
- **전체 게이트(수정 뒤 한 번):** typecheck · lint · lint:sql exit 0 · unit 112 파일 1476/1476 · integration 61 파일 1540/1540 · build exit 0 · `CI=true playwright --project=cert-setup --project=certs`(전체 체인) 367 통과 · 1 실패 · 31 미실행 — 실패는 `mobile-320-no-overflow.spec.ts` 「시스템 관리자가 여는 화면 전부」 30초 타임아웃(관리 화면 순회, 이 수정과 무관한 체인 부하 플레이크: 단독 재실행 `--project=mobile-375 --no-deps` 3/3 통과). 그 실패로 의존 체인이 멈춰 못 돈 cert-setup · certs는 `--no-deps --project=cert-setup --project=certs`로 따로 돌려 31/31 통과.

## Next Phase Readiness

- 04.3-06(제출)은 `lockEventRow` · `isDefiniteResult`(제출 결과 종류 추가) · `ok.version`(증표 발급 순간 자리 version)을 그대로 쓸 수 있다
- 04.3-10(잠금 풀기)은 네 칸(failed_attempts · locked_until · cumulative_failed_attempts · hard_locked_at)을 비우면 되고, 맵은 `ok` 항목만 지운다(`wrong`은 속도 제한 셈 — 교차 계약)
- 04.3-08(키 회전)은 맵을 다시 쓰지 않는다(재생 불가 → expiredProof, 60분 뒤 저절로 빠짐)

## Self-Check: PASSED

- 생성 파일 8개 존재 확인, 커밋 7개(6a684aa · 7b2f6a6 · aa77880 · 3102f6a · da88148 · 739b3b0 · 2b8780e) `git log`에 존재
- 공유 상태 파일(STATE.md · HANDOFF.json · state.json · ROADMAP.md · CLAUDE.md · .claude/gates/*) 무수정
