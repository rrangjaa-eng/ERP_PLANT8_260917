---
phase: 04.3-qr-certificate-intake
plan: 04
type: execute
wave: 3
depends_on: ["04.3-02"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - domain/certs/qr.ts
  - domain/certs/winner-rules.ts
  - domain/certs/events.ts
  - repositories/cert-events.ts
  - domain/permissions/menus.ts
  - domain/permissions/info-items.ts
  - app/(app)/certs/events/create-form-rules.ts
  - app/(app)/certs/events/page.tsx
  - app/(app)/certs/events/loading.tsx
  - app/(app)/certs/events/error.tsx
  - app/(app)/certs/events/events-table.tsx
  - app/(app)/certs/events/event-create-form.tsx
  - app/(app)/certs/events/actions.ts
  - app/(app)/certs/events/actions.registry.ts
  - app/(app)/certs/events/events.module.css
  - app/(app)/certs/events/create-entry.tsx
  - app/(app)/certs/events/[id]/page.tsx
  - app/(app)/certs/events/[id]/loading.tsx
  - app/(app)/certs/events/[id]/qr-section.tsx
  - app/(app)/certs/events/[id]/winners-view.tsx
  - app/(app)/certs/events/[id]/event-detail.module.css
  - test/integration/leak-scan.test.ts
  - test/unit/certs/winner-rules.test.ts
  - test/unit/certs/create-form-rules.test.ts
  - ui/table/types.ts
  - ui/table/Table.tsx
  - test/unit/ui/table-header-describedby.test.ts
  - test/integration/cert-events.test.ts
  - test/e2e/cert-events.spec.ts
autonomous: true
requirements: [CERT-01]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 160000
  raw_tokens: 160000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "담당자가 「관리」가 아니라 주소 `/certs/events`에서 행사 목록을 보고(진입 링크는 04.3-09), 「행사 만들기」로 행사 이름 · 당첨일 두 칸과 당첨자 표(이름 · 전화번호 · 경품명 · 수량 · 전달 · 구별 표시)를 한 번에 저장하면 행사 상세로 이동하고 QR 하나와 링크가 생긴다(D-1102 · D-1103, ROADMAP 04.3 기준 2)"
    - "행사 만들기는 한 트랜잭션이다 — 두 칸 + 표 전체가 전부 저장되거나 전부 거부되고, 거부될 때 표의 오류 셀이 고정되고 합계 행에 `오류 N칸 · 전부 거부`가 선다(UI Considerations error I2)"
    - "당첨자 셀 검사: 전화번호 형식 · 수량 1 이상 정수 · 전달 `현장`/`택배` · 빈 필수 칸 · 같은 이름·전화번호 두 번 · 이름 40자 넘음 · 경품명 80자 넘음(E3-21 — 앞뒤 공백을 뗀 길이, 41자 · 81자부터) — 각각 UI-SPEC Copywriting의 셀 오류 문장으로 그 셀에 선다. 길이를 넘은 값도 `serverError`가 아니라 그 셀의 오류다(zod가 먼저 거부하지 않는다)"
    - "**구별 표시 규칙(오케스트레이터 결정 2026-09-24):** 공개 모양(가린 이름 + 2행)이 같은 줄이 둘 이상이면 그 줄들의 구별 표시 셀이 오류 `수령자 목록에 {김*수 · 갤럭시 탭 S10 1개}가 {2}줄입니다 · 구별 표시를 서로 다르게 적어 주세요(예: 오전 조)`가 되고 저장이 막힌다(묶음 행 없음). 구별 표시는 10자 이하이고 숫자 3개 이상 이어짐과 **그 행사에 등록된 당첨자 이름 아무것이나**를 담을 수 없다 — 서버가 거부하고 셀 오류 문장은 UI-SPEC Copywriting I2 그대로다(UI-SPEC A11)"
    - "구별 표시 검사는 우회 입력을 정규화한 뒤 한다(UI-SPEC E2 354행): 숫자 사이에 공백·하이픈·점이 끼어도 이어진 숫자로 센다(`7-7-3-0` · `7 7 3` · `7.7.3` → 거부) · 이름 포함은 구별 표시와 당첨자 이름 **둘 다** 공백을 모두 뺀 NFC 문자열로 비교한다(`김 하늘` · `김하 늘` → 당첨자 `김하늘`이 있으면 거부)"
    - "설정 `수령자 문의 전화`가 비어 있으면 행사를 만들 수 없다 — 설정 화면을 열 수 있는 사람에게는 `수령자 문의 전화 없음 · 설정 확인증 탭에서 채움`, 없는 사람(기획 PM)에게는 `수령자 문의 전화 없음 · 등록은 경영관리`(비활성 이유에 접두 없음 — SYSTEM §8 규칙 5 · /plan-design-review D-7). 만든 행사는 그때의 번호를 사본으로 가진다(UI-SPEC A12)"
    - "I2·I3 당첨자 표의 `수령자 화면` 열은 서버와 같은 가림 함수(`roster-display.ts`)로 계산한 E2 행 모양(가린 이름 + 2행)을 저장 전에 보이고, 섹션 라벨 아래 상시 힌트 `구별 표시는 수령자 목록에 그대로 보입니다 · 이름·전화번호는 쓰지 마세요`가 `구별 표시` 열 머리글의 설명으로 연결된다 — **셀을 편집하는 동안에는 그 편집 입력(`<input>`)의 `aria-describedby`도 같은 힌트 id를 가리킨다**(머리글만이 아니라 입력에 있어야 편집 중 읽힌다)"
    - "당첨자가 0명이면 만들 수 없다 — 표 EMPTY `당첨자가 없습니다 · 첫 줄 만들기 Ctrl+Enter` + 1차 막힘 `당첨자 없음 · 첫 줄 만들기`(UI Considerations empty I2 · CERT-01 × empty · D-6 · D-7)"
    - "행사 만들기 폼의 당첨일은 처음부터 오늘(KST)로 채워져 있고(자리표시자 `2026-09-18`은 형식 예로 남는다) 입력 버리기 `{N}`의 처음 값도 오늘이다 — 그래서 첫 화면의 1차 막힘은 `행사 이름 비어 있음 · 행사 이름 적기`다(UI-SPEC Copywriting 「I2 폼 제목 · 칸」 · 「I2 1차 막힘」, CLAUDE.md §7 · /plan-design-review D-11 · X-1)"
    - "1차 막힘 이유의 색은 UI-SPEC Color 「막힘 이유의 색」 규칙 하나를 따른다 — I2는 내부 폼이라 막힘 이유가 전부 `reasonTone: \"block\"`(`--danger`)이다(`… 비어 있음 · … 적기` · `당첨자 없음 · 첫 줄 만들기` · `당첨자 {N}명 · 500명까지 줄이기` · `수령자 문의 전화 없음 · …` — SYSTEM §7-15 `Form.Actions` 막힘 이유 `--danger`와 그 예 `클라이언트 · 담당 PM 2칸 비어 있음 · 클라이언트 고르기`, 첫 화면 `행사 이름 비어 있음 · 행사 이름 적기`도 `block`). `info`는 외부 화면(①-m ②)과 `바뀐 칸 없음`에만 있다(/plan-design-review X-4 · 반영 검사 r1 B-1)"
    - "**표 오류 셀은 1차를 막지 않는다(SYSTEM §7-3 DR-5):** 고정된 오류 셀이 남아 있어도 1차 「행사 만들기」는 켜져 있고, 누르면 서버를 부르지 않고 첫 오류로 포커스를 옮긴다 — 폼 칸(행사 이름 · 당첨일) 오류가 있으면 그 칸, 아니면 표의 첫 오류 셀(Phase 4 `Table`의 `firstIssueSignal`, 04-47). 오류 사실은 오류 셀과 합계 행 `오류 N칸 · 전부 거부`가 말한다(UI-SPEC Copywriting 「I2 1차 막힘」 끝 · 반영 검사 r1 B-4)"
    - "당첨자 표의 미저장 편집은 브라우저 저장소에 보관하지 않는다 — `beforeunload` 경고만 쓰고 「복원 / 버림」 줄이 없다(개정 ⑪)"
    - "「취소 Esc」는 처음 값과 다른 칸 수 N이 0이면 곧바로 목록, 1 이상이면 `입력 버리기` 확인(`행사 만들기 · {N}칸`)을 연다. 확인은 Phase 4 공용 `ui/confirm-dialog`의 `ConfirmDialog`다(SYSTEM §7-17 · `app/(app)/projects/project-form.tsx` 선례 — 화면 파일 안에 `<dialog>`를 따로 만들지 않는다, E3-01)"
    - "**만들기 제출 실패 갈래 · 멱등(E3-22):** 결과 불명(연결 실패 · 20초 · 5xx · `serverError` · 해석 불가 응답)이면 합계 행 자리에 `--danger` 한 줄이 서고 편집 값은 그대로이며 1차가 다시 켜진다. 폼은 처음 열 때 만든 `requestId` 하나를 성공할 때까지 다시 보내므로, 첫 요청이 서버에서 이미 행사를 만들었어도 다시 누른 요청은 같은 행사를 돌려주고 행사(QR)가 둘 생기지 않는다(`cert_events.create_request_id` UNIQUE). 한 요청의 당첨자는 500줄까지다(04.3-10과 같은 상한). 줄 문장은 UI-SPEC Copywriting 「I2 만들기 실패」 `만들었는지 확인하지 못했습니다 · 다시 눌러 주세요` 그대로다(/plan-design-review D-2). **확정 거부 둘은 이 줄로 가지 않고 보내기 전에 막는다**(다시 눌러도 같은 거부라 「다시 눌러」가 사실이 아니다): 500줄 넘음 → 1차 막힘 `당첨자 {N}명 · 500명까지 줄이기`(`block`) · 요청 크기 한도 → `lib/actions/payload-size.ts`의 `checkPayloadSize`(서버 미들웨어와 같은 함수 · 같은 한도)가 거부하면 요청을 보내지 않고 같은 자리에 그 `reason` 문장(I3 「일괄 저장」과 같다, 반영 검사 r1 N-1)"
    - "행사 목록은 보는 사람 범위로 걸러진다 — 확인증 제출 내용 보기 권한(`certs.submissions` view)이 있으면 전부, 없으면 자기가 만든 행사만. 남의 행사 상세는 404다"
    - "행사 목록: 그룹 머리글 `접수 중` → `닫힘`, 그룹 안 당첨일 내림차순, 열 행사 · 당첨일 · 담당 · 제출 `3/12` · 상태(색 글자), 행 전체가 상세 링크. 0건이면 `확인증 행사가 없습니다 · 행사 만들기` 한 줄(1024 미만에서는 3차 없음 — D-10)(UI Considerations empty I1)"
    - "행사 상세(읽기): 제목 = 행사 이름 + 상태 태그, 부제 = 당첨일 · 담당 · 마감. 접수 중이면 QR 160×160(`--fg` on `--bg`, 조용한 영역 4모듈) + 전체 링크 + 3차 「링크 복사」(→ `링크 복사됨` 4초), 닫힘이면 QR·링크를 렌더하지 않고 닫힘 사유 한 줄"
    - "행사 상세 당첨자 표의 제출 열은 `제출됨` + `MM-dd HH:mm` · `미제출` · `잠김` + `HH:mm까지`이고 합계 행이 `합계 · 12명 · 제출 3명`이다(UI Considerations populated I3)"
    - "1024 미만(좁은 PC · 폰)에서는 행사 만들기(1차 · `?new=1` 폼 · EMPTY의 3차)를 렌더하지 않는다 — 폭 판정은 Phase 4 `useEditableWidth()`(`ui/table/use-editable-width.ts`) 하나다(UI-SPEC I2 「1024 미만(좁은 PC · 폰): 만들기 없음」 · SYSTEM §6 ⑷ DR-36 · /plan-design-review D-10). 폰(<700)에서 목록·상세 표는 칸 접기로 선다"
    - "기능이 꺼져 있으면(`assertCertFeatureEnabled()` — env 게이트 + 설정, C1) `/certs/events`(`?new=1` 포함)와 `/certs/events/[id]`는 셸 안 §6-9 404이고, `createCertEventAction`은 본문 첫 줄의 같은 게이트로 거부된다 — 페이지를 거치지 않은 직접 POST도 행사를 만들지 못한다(E2E 직접 POST 테스트)"
    - "행사 이름·경품명·주소·링크 URL은 줄바꿈되고(`keep-all` + `overflow-wrap: anywhere`) 전화·일시 숫자는 줄바꿈되지 않는다(UI Considerations long-text)"
    - "목록·상세 로딩은 라우트 `loading.tsx` 두 개(`/certs/events` · `/certs/events/[id]`)가 그리는 §7-7 표 뼈대(머리글 · 합계 + `--surface` 3행, `aria-hidden`)이고 스피너가 없다 — `app/(app)/projects/loading.tsx` 선례 그대로(UI Considerations loading I1·I3)"
    - "QR은 서버가 `qrcode`로 만든 SVG(`await renderQrSvg(link)` — 비동기, E3-25)를 인라인으로 그리고 모듈 색은 `currentColor`라 CSS 토큰 `--fg`·`--bg`만 쓴다 — 색 리터럴이 없고 외부 QR API를 부르지 않는다. 접근 이름은 `{행사 이름} 확인증 QR`(`role=\"img\"` + `aria-label`)"
    - "CERT-01 × boundary(수량): 수량 0 · 음수 · 소수 · 빈 값은 셀 오류이고 1은 통과한다"
    - "새 DTO(행사 목록 · 행사 상세 · 당첨자)와 액션이 누수 스캔 생성기에 등록된다(ROADMAP 04.3 기준 5) — 등록 검사만으로는 실제 응답을 보지 않으므로, 정보 노출(`cert_winner.value`)을 끈 계급으로 **실제** `getEventDetail` · `listEvents` · `createEvent` 반환값과 상세 페이지 HTML(RSC 페이로드 포함)에 당첨자 이름·전화번호가 한 글자도 없음을 따로 단언한다(Codex #22) — HTML 대조는 HTTP 200이고 행사 이름이 보이는 응답에서만 한다(임시 계급에 `cert_event.value`를 켠다 — codex-final3-B 3)"
    - statement: "행사 목록 불러오기 실패가 표 자리 `--danger` 한 줄 + 2차 「다시 시도」로 서는지(UI Considerations 보강 error I1)"
      verification: backstop
    - statement: "일부만 제출된 행사의 제출 열 `3/12`가 `제출 3명 / 전체 12명`으로 읽히는지(접근 이름)(UI Considerations 보강 partial I1)"
      verification: backstop
    - statement: "폰(<700)에서 목록·상세 표가 칸 접기(P1 열 + 접힌 줄)로 서고 표 컨테이너와 문서 모두 `scrollWidth <= clientWidth`인지(UI Considerations 보강 overflow I1·I2·I3)"
      verification: backstop
    - statement: "당첨자 1명 행사와 여러 명 행사에서 합계 행·저장 문구가 어색하지 않은지(UI Considerations 보강 zero-one-many I2)"
      verification: backstop
    - statement: "「행사 만들기」 진행 중 버튼 라벨 뒤 `…` + 다른 1차 비활성(UI Considerations 보강 loading I2)"
      verification: backstop
  prohibitions:
    - statement: "당첨자 이름·전화번호가 든 편집 표의 미저장 값이 브라우저 저장소(localStorage·sessionStorage·IndexedDB)에 남아서는 안 된다 — 로그아웃·계정 전환·파기 뒤에도 남는 사본이 처음부터 없어야 한다(개정 ⑪)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/certs/winner-rules.ts"
      provides: "당첨자 줄 묶음 검사(셀 오류) — 형식 · 중복 · 구별 표시 필요 · 구별 표시 내용 규칙. 04.3-10이 재사용"
      exports: ["validateWinnerRows"]
    - path: "domain/certs/qr.ts"
      provides: "링크 → 인라인 SVG(currentColor, 여백 4모듈) — `async renderQrSvg(link): Promise<string>`(E3-25)"
      exports: ["renderQrSvg"]
    - path: "app/(app)/certs/events/event-create-form.tsx"
      provides: "I2 — Form 두 칸 + 폼 밖 편집 표 + Form.Actions(form 속성으로 제출)"
      min_lines: 80
    - path: "app/(app)/certs/events/loading.tsx"
      provides: "I1 목록 §7-7 표 뼈대(머리글 · 합계 + --surface 3행)"
      min_lines: 15
    - path: "app/(app)/certs/events/[id]/loading.tsx"
      provides: "I3 상세 §7-7 표 뼈대"
      min_lines: 15
    - path: "test/integration/cert-events.test.ts"
      provides: "만들기 검사 · 한 트랜잭션 · 범위 필터 · 상세 404 · 플래그 꺼짐 · 노출 끔 실제 반환값 비노출 증명"
      min_lines: 60
    - path: "test/e2e/cert-events.spec.ts"
      provides: "만들기(붙여넣기 · 구별 표시 오류 · 편집 입력 힌트 연결) → 상세 QR · 노출 끔 HTML 비노출 · 기능 꺼짐 404 · 직접 POST 거부 E2E"
      min_lines: 50
  key_links:
    - from: "domain/certs/winner-rules.ts"
      to: "domain/certs/roster-display.ts"
      via: "구별되지 않는 줄 판정은 외부 목록과 같은 함수(findIndistinguishable)"
      pattern: "findIndistinguishable"
    - from: "domain/certs/events.ts"
      to: "domain/permissions/can.ts"
      via: "목록 범위 — certs.submissions view면 전부, 아니면 만든 사람"
      pattern: "certs.submissions"
    - from: "test/integration/leak-scan.test.ts"
      to: "app/(app)/certs/events/actions.registry.ts"
      via: "새 액션 레지스트리를 누수 스캔이 import한다(append-only)"
      pattern: "certs/events/actions.registry"
    - from: "app/(app)/certs/events/event-create-form.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "「입력 버리기」 확인은 Phase 4 공용 확인 모달(§7-17)을 쓴다(E3-01)"
      pattern: "ConfirmDialog"
    - from: "domain/certs/events.ts"
      to: "db/schema/cert-events.ts"
      via: "만들기 멱등 — 같은 requestId는 같은 행사(create_request_id UNIQUE, E3-22)"
      pattern: "create_request_id|createRequestId"
    - from: "app/(app)/certs/events/actions.ts"
      to: "lib/certs/feature-guard.ts"
      via: "만들기 액션과 두 페이지가 첫 줄에서 기능 게이트를 부른다(C1)"
      pattern: "assertCertFeatureEnabled"
---

<objective>
**As a** 기획 PM(행사 담당자), **I want to** 행사 이름·당첨일과 당첨자 명단을 엑셀에서 붙여 넣어 한 번에 행사를 만들고, 상세 화면에서 QR을 띄워 현장에 걸고 누가 제출했는지 보고 싶다, **so that** 행사장에서 QR 하나로 당첨자 전원의 확인증을 받는다.

이 페이즈의 만들기 진입점은 내부 행사 화면이다(ROADMAP 04.3 기준 2 · CONTEXT Phase Boundary — Phase 11이 경품 지출결의 화면으로 옮긴다). 이 플랜은 목록(I1) · 만들기(I2) · 상세 읽기(I3의 머리 · QR · 당첨자 읽기 표)를 만든다. 상세의 편집 · 링크 닫기 · 「제출 내용」 링크는 04.3-10, 「관리」 인덱스 진입 줄은 04.3-09다.

**의존성:** `qrcode`(+ 개발 의존성 `@types/qrcode`)를 더한다 — 사용자 승인 2026-09-24(「qrcode 패키지 추가 해」, 오케스트레이터 사실 1). 이유 한 줄: QR 인코딩(Reed-Solomon · 마스크 선택)은 직접 만들지 않는다(RESEARCH Don't Hand-Roll). RESEARCH Package Legitimacy Audit가 `qrcode`를 SUS(다운로드 수 확인 불가 — 도구 한계)로 적었으므로, 설치 태스크가 저장소 주소·버전을 자동으로 대조하고 정확한 버전으로 고정한다(오케스트레이터 지시로 사람 확인 체크포인트는 두지 않는다 — 아래 위협 모델 T-04.3-SC).

Purpose: D-1102 「행사당 QR 하나」와 D-1103 「담당자가 당첨자 목록을 미리 넣는다」의 담당자 쪽 절반이다. 구별 표시 규칙이 여기서 저장을 막아야 외부 목록에 같은 모양의 행이 서지 않는다.
Output: 의존성 하나(+ 타입), QR·당첨자 검사 순수 모듈, 만들기 화면 판정 순수 도우미, 목록·만들기·상세 읽기 화면, DTO 셋 · 액션 등록, 단위 2 · 통합 1 · E2E 1. 태스크 넷(checker-B W6 — 옛 Task 1을 「의존성 + 순수 모듈」과 「domain · 등록」으로 나눴다), 모든 행동 태스크는 실패 테스트를 먼저 쓴다(checker-B W4). `certs.events` 권한은 04.3-09가 시드하기 전이라 이 플랜의 테스트가 스스로 준다 — 통합은 `role-pm`에 주고(테스트마다 TRUNCATE + 시드로 되돌아간다), E2E는 공유 `role-pm`을 건드리지 않고 임시 계급 · 임시 사용자를 쓴다(codex-B 3 · checker-B B2 · 4차 W-1).

**범위(checker-B W-3 — 받아들임):** 파일 27개 · 태스크 넷으로 15개 기준을 넘지만 순수 모듈 → domain · 등록 → 목록·만들기 화면 → 상세 · E2E가 한 세로 조각(담당자가 행사를 만들고 QR을 띄우기까지)이라 나누지 않는다 — 나누면 어느 쪽도 혼자서는 담당자가 쓸 수 있는 것을 만들지 못한다(04.3-07 · 08 · 10과 같은 판단).

**실행 조건(E3-01 · E3-33):** Phase 4 PR #40(`origin/claude/gsd-progress-e1nzgu`)이 main에 머지된 뒤에만 시작하고, 웨이브 첫 단계는 `git fetch origin && git merge origin/main`이다(리베이스 · 강제 푸시 금지). 같은 웨이브(04.3-03 · 05)와 통합 · E2E를 동시에 돌리지 않는다(세션당 플랜 하나 — `erp_test` · 포트 3100 공유). Phase 4 부품이 기준이다: 「입력 버리기」 확인은 `ui/confirm-dialog`(§7-17), `ui/table/Table.tsx`는 Phase 4 판(머리글 `<th>` 위치가 옛 main과 다르다 — 줄 번호가 아니라 `<thead>` 안 `<th`로 찾는다), `ui/button`은 `aria-disabled`(비활성 1차도 포커스된다). 문장이 새로 필요한 두 곳(이름 · 경품명 길이 셀 오류 — E3-21, 만들기 실패 줄 — E3-22)은 `/plan-design-review`가 UI-SPEC I2에 넣은 문장을 쓴다(Task 3 `<precondition>`).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md
@.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md
@.planning/phases/04.3-qr-certificate-intake/04.3-PATTERNS.md
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: qrcode 설치(버전 대조·고정) · QR SVG · 당첨자 줄 검사(순수 모듈)</name>
  <files>package.json, pnpm-lock.yaml, domain/certs/qr.ts, domain/certs/winner-rules.ts, test/unit/certs/winner-rules.test.ts</files>
  <precondition>Phase 4가 main에 머지돼 있고 이 브랜치가 웨이브 첫 단계에서 그 main과 04.3-02를 머금었다 — `git cat-file -e origin/main:ui/confirm-dialog/ConfirmDialog.tsx`가 성공하고 `git merge-base --is-ancestor origin/main HEAD`가 참이며 `test -f ui/table/use-editable-width.ts && grep -q 'useEditableWidth' ui/table/use-editable-width.ts`가 종료 코드 0이고(Phase 4 04-49 폭 판정 — D-10) `db/schema/cert-events.ts`에 `create_request_id`가 있다(E3-01 · E3-22 — 하나라도 아니면 멈추고 보고한다. 폭 판정을 새로 만들지 않는다)</precondition>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md` 「Standard Stack」 · 「Package Legitimacy Audit」(`qrcode` 행) · 「Don't Hand-Roll」
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` Copywriting I 표(I2 셀 오류) · E2 354행 — 구별 표시 우회 입력 정규화(공백·하이픈·점 · 공백 뺀 이름)
    - `domain/certs/roster-display.ts` · `domain/certs/format.ts`(04.3-02 산출물 전문) — `findIndistinguishable` · `normalizePhone`
  </read_first>
  <behavior>
    - `validateWinnerRows([{이름 "김하늘", 전화 "010-4821-7730", 경품 "갤럭시 탭 S10", 수량 "1", 전달 "현장"}])` → 오류 없음, 정규화된 줄(전화 숫자만 · 수량 1 · 전달 `onsite`)
    - 전화 `02-123-4567` → 그 셀 오류 · 수량 `0`/`-1`/`1.5`/빈 값 → 그 셀 오류 · 전달 `퀵` → 그 셀 오류 · 이름 빈 값 → 그 셀 오류
    - 같은 이름·전화번호 두 줄 → 두 줄의 이름 셀(또는 전화 셀) 오류
    - `김민수`·`김문수`(같은 경품·수량, 구별 표시 없음) → 두 줄의 구별 표시 셀 오류(코드 `shapeDuplicate`, 모양 `김*수 · 스타벅스 기프티콘 2개` · 줄 수 2를 함께) · 한 줄만 `오전 조` → 통과 · 둘 다 `오전 조` → 두 줄 오류
    - 구별 표시 `3시 추첨` → 통과(숫자 하나) · `123조` → 오류(숫자 3개 이어짐) · `12시 조` → 통과(숫자 둘) · 다른 줄 당첨자 이름 `김하늘` 포함 → 오류(그 줄 이름만이 아니라 행사 전체 이름) · 10자 → 통과 · 11자 → 오류
    - (우회 숫자) `7-7-3-0` → `labelDigits` · `7 7 3` → `labelDigits` · `7.7.3` → `labelDigits` · `1-2 조` → 통과(숫자 둘) · `3시 1조` → 통과(숫자 사이에 글자가 있으면 끊긴다)
    - (우회 이름) 당첨자 `김하늘`이 있을 때 구별 표시 `김 하늘` → `labelName` · `김하 늘` → `labelName` · 당첨자 이름이 `김 하늘`(공백 포함)로 등록돼도 구별 표시 `김하늘` → `labelName` · NFD로 적은 `김하늘` → `labelName`(NFC 비교)
    - (길이 — E3-21) 이름 40자 → 통과 · 41자 → 그 셀 `nameTooLong` · 앞뒤 공백을 붙인 40자(` ` + 40자 + ` `) → 통과(뗀 길이로 센다) · 경품명 80자 → 통과 · 81자 → 그 셀 `prizeTooLong`
    - 줄 0개 → 「당첨자 없음」 오류
    - (단위 안의 한 줄 — E3-25) `await renderQrSvg(link)`의 결과가 `<svg`로 시작하고 `currentColor`를 쓰며 `#` 색 리터럴이 없다(`renderQrSvg`는 `Promise<string>`을 돌려준다)
  </behavior>
  <action>
(checker-B W6 — 옛 Task 1을 둘로 나눴다: 이 태스크는 의존성과 순수 모듈만, DB·권한·액션은 Task 2.)

① **의존성(사용자 승인 2026-09-24):** 설치 전에 `pnpm view qrcode version repository.url` · `pnpm view @types/qrcode version repository.url`을 돌려 `qrcode`의 저장소가 `github.com/soldair/node-qrcode`이고 `@types/qrcode`가 DefinitelyTyped임을 확인한다 — 다르면 멈추고 SUMMARY에 적은 뒤 보고한다(다른 패키지를 대신 깔지 않는다). 확인되면 `pnpm add --save-exact qrcode@<확인한 버전>` · `pnpm add -D --save-exact @types/qrcode@<확인한 버전>`. 두 버전과 저장소 주소를 SUMMARY에 적는다. 다른 의존성은 더하지 않는다(`@google-cloud/storage` 금지 — 오케스트레이터 사실 1).

② **`domain/certs/qr.ts` `async renderQrSvg(link): Promise<string>`(E3-25 — `qrcode`의 `toString`이 비동기라 이 함수도 비동기다, 부르는 곳은 전부 `await`):** `qrcode`의 SVG 문자열 출력(오류 정정 M · 여백 4모듈)을 받아 어두운 모듈 채움을 `currentColor`로, 밝은 바탕을 투명으로 바꿔 돌려준다(CSS가 `color: var(--fg)`·`background: var(--bg)`를 준다 — 색 리터럴 금지, CLAUDE.md). 입력은 서버가 만든 링크뿐이다.

③ **`domain/certs/winner-rules.ts` `validateWinnerRows(rows, context)`(순수):**
  - 입력 = 화면 표의 문자열 줄들(+ 상세 편집 때는 이미 저장된 줄들 — 04.3-10이 `context`로 넘긴다). 출력 = `{ ok: true, rows: 정규화된 줄 }` 또는 `{ ok: false, cellErrors: [{rowKey, column, code}] }`.
  - 형식: `normalizePhone` 재사용(당첨자 전화는 휴대전화 계약 — 04.3-02). 구별되지 않는 줄: `findIndistinguishable`(04.3-02) 재사용.
  - 구별 표시 내용 규칙(UI-SPEC E2 354행 · Codex #18): 빈 값 허용 · 앞뒤 공백 제거 뒤 1~10자 · **숫자 이어짐**은 공백·하이픈·점(`[\s\-.]`)을 지운 문자열에서 숫자 3개 이상 이어짐으로 판정 · **이름 포함**은 구별 표시와 그 행사의 등록된 당첨자 이름(이번 표의 줄 + `context`의 저장된 줄) 둘 다 NFC 정규화 후 모든 공백(`\s`)을 지운 문자열로 비교해 하나라도 포함되면 거부(빈 이름은 비교하지 않는다). 정규화는 판정에만 쓰고 저장값은 입력(앞뒤 공백 제거)을 그대로 둔다.
  - 길이(E3-21): 이름은 앞뒤 공백을 뗀 `length`가 40을 넘으면 `nameTooLong`, 경품명은 80을 넘으면 `prizeTooLong`(04.3-02 zod의 이름 1~40자 · 경품명 1~80자와 같은 셈 — 그 한도를 이 함수가 셀 오류로 판정하고, Task 2 ③ (a)가 zod에서 뺀다). 붙여넣기는 입력의 `maxLength`를 거치지 않으므로 이 검사가 정본이다.
  - 오류 `code`(`required` · `phoneFormat` · `quantity` · `delivery` · `duplicatePerson` · `nameTooLong` · `prizeTooLong` · `shapeDuplicate` · `labelTooLong` · `labelDigits` · `labelName`)와 `shapeDuplicate`의 모양 문자열 · 줄 수만 돌려주고, 문장은 화면이 UI-SPEC Copywriting 「I2 셀 오류」 11종(기존 9종 + 길이 둘 `40자를 넘습니다 · 40자 안으로 줄여 주세요` · `80자를 넘습니다 · 80자 안으로 줄여 주세요` — E3-21 · /plan-design-review D-1)에서 고른다.

④ **테스트(RED 먼저):** ①의 설치 뒤, ②·③을 쓰기 **전에** `test/unit/certs/winner-rules.test.ts`에 위 `behavior` 전부(우회 숫자 · 우회 이름 · `renderQrSvg` 한 줄)를 쓰고 실패(모듈 없음)를 확인한다 → ②·③으로 녹색.

⑤ 커밋: `chore(04.3): add qrcode dependency (user-approved)` → `test(04.3): failing winner row rules` → `feat(04.3): winner row rules with distinguishing label`, 본문 한국어.
  </action>
  <verify>
    <automated>node -e 'const p=require("./package.json");const v=(p.dependencies||{}).qrcode||"";const t=(p.devDependencies||{})["@types/qrcode"]||"";process.exit(/^\d+\.\d+\.\d+$/.test(v)&amp;&amp;/^\d+\.\d+\.\d+$/.test(t)?0:1)'</automated>
    <fails_when>종료 코드가 1이다 — `qrcode`가 dependencies에 없거나, `@types/qrcode`가 devDependencies에 없거나, 버전이 `^`·`~` 범위로 들어갔다(정확한 버전 고정 위반)</fails_when>
    <automated>pnpm vitest run --project unit test/unit/certs/winner-rules.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - `package.json`의 `qrcode`·`@types/qrcode`가 정확한 버전(범위 기호 없음)이고 SUMMARY에 저장소 주소 대조 결과가 있다
    - `grep -c "google-cloud/storage" package.json`이 0이다
    - `validateWinnerRows`가 DB·설정을 import하지 않는 순수 모듈이고 구별 표시 규칙 케이스 넷(없음 · 한쪽만 · 둘 다 같음 · 내용 위반)과 우회 입력 케이스(`7-7-3-0` · `7 7 3` · `김 하늘` · 공백 든 등록 이름 · NFD)가 단위 테스트에 있다
    - 길이 경계 케이스(이름 40 · 41 · 공백 붙인 40, 경품명 80 · 81)가 단위 테스트에 있고 코드가 `nameTooLong` · `prizeTooLong`이다(E3-21)
    - `domain/certs/qr.ts`의 `renderQrSvg`가 `async`이고(`grep -qE 'export async function renderQrSvg' domain/certs/qr.ts`) 단위 테스트가 `await`한다(E3-25)
    - git 기록에서 실패 테스트 커밋이 구현 커밋보다 앞선다
  </acceptance_criteria>
  <done>`qrcode`가 고정 버전으로 들어오고, QR SVG와 당첨자 줄 검사(구별 표시 우회 입력 포함)가 단위 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 행사 목록·상세·만들기 domain(범위 필터 · 기능 게이트 · 실제 반환값 비노출) · DTO·액션 등록</name>
  <files>domain/certs/events.ts, repositories/cert-events.ts, domain/permissions/menus.ts, domain/permissions/info-items.ts, app/(app)/certs/events/actions.ts, app/(app)/certs/events/actions.registry.ts, test/integration/leak-scan.test.ts, test/integration/cert-events.test.ts</files>
  <read_first>
    - `domain/certs/events.ts` · `repositories/cert-events.ts` · `lib/certs/feature-guard.ts`(04.3-02 산출물 전문) — `assertCertFeatureEnabled`의 반환 모양(C1 — 모든 액션·라우트의 첫 줄)
    - `db/schema/cert-events.ts`(04.3-02 산출물) — `create_request_id`(uuid null) · UNIQUE `cert_events_create_request_id_unique`(E3-22 — 이 플랜은 스키마 · 마이그레이션을 다시 만들지 않는다) · `lib/pg-errors.ts` `isUniqueViolation(error, constraint)` · `lib/actions/client.ts`의 `checkPayloadSize`(요청 크기 한도 — 넘으면 `UserFacingError`라 화면에는 `serverError`로 온다)
    - `domain/vendors/index.ts`(전문) — `DtoSpec` · `registerDto` · `project()` · 오류 클래스 · `deps` 주입
    - `domain/permissions/project.ts` 18~36행 · `domain/permissions/info-items.ts`(전문) · `domain/permissions/menus.ts`(전문) · `domain/permissions/can.ts`(행이 없거나 `allowed`가 거짓이면 거짓)
    - `app/(app)/admin/vendors/actions.ts`(전문) · `actions.registry.ts`(전문) — Server Action 규약 · 레지스트리 분리
    - `test/integration/leak-scan.test.ts` 1~30행 — import 줄을 더할 자리(끝에 한 줄만 더한다, 공유 등록부)
    - `test/unit/leak-scan-coverage.test.ts` — 모든 `app/**/actions.registry.ts`가 누수 스캔에 import돼야 한다
    - `test/integration/setup.ts` — 매 테스트 전 TRUNCATE + `seedMasterData`(테스트가 준 권한은 다음 테스트 전에 사라진다)
    - `repositories/permissions.ts` `upsertPermission` · `test/integration/revenue-entries.test.ts` 51~56행 — 테스트 안에서 계급 권한을 주는 선례
    - `test/integration/projects-list.test.ts` — 범위 필터 통합 테스트 선례
    - `test/integration/quote-line-visibility.test.ts` — `upsertVisibility(SYSTEM_VIEWER, {roleId, infoItem, visible: false})`로 노출을 끄고 실제 반환값을 단언하는 선례(끝에 되돌린다)
  </read_first>
  <behavior>
    - (통합) `createEvent`가 오류 셀이 하나라도 있으면 행사도 당첨자도 만들지 않는다(전부 거부) — 행사 표·당첨자 표 행 수 불변
    - (통합) PM 계급 viewer(만든 사람)는 자기 행사만 목록에 보고 남의 행사 상세는 `notFound` · `certs.submissions` view가 있는 viewer는 전부 본다
    - (통합) 플래그 꺼짐 → 목록·상세·만들기 전부 `notFound`/거부
    - (통합, 실제 반환값 비노출 — Codex #22) 테스트 계급의 `cert_winner.value` 노출을 끈 뒤 그 계급 viewer로 `createEvent` 반환값 · `listEvents` · `getEventDetail`을 부르면, 결과 전체를 `JSON.stringify`한 문자열에 픽스처 당첨자 이름(`김하늘`)·전화번호(`01048217730` · `010-4821-7730`)가 없고 당첨자 DTO에 `name` · `phone` 키가 없다 · 노출을 켠 계급은 같은 호출에서 이름·전화가 보인다(대조) · 끝에서 노출을 되돌린다
    - (통합) 설정 `cert.contact_phone`이 비었을 때 `getCreateGate`가 시스템 관리자에게 `{contactMissing: true, canOpenSettings: true}`, 기획 PM에게 `{contactMissing: true, canOpenSettings: false}`를 준다
    - (통합) 상세 DTO의 링크가 `createEvent`가 돌려준 링크와 같다(토큰 암호문 왕복) · 닫힌 행사 DTO에는 링크·QR 필드가 없다
    - (통합, PM 권한 전제) PM 케이스는 테스트가 `role-pm`에 `certs.events` 보기·쓰기를 스스로 준 뒤에만 돈다 — 준 줄을 빼면 PM `listEvents`가 `notFound`(권한 없음과 범위 밖이 같은 모양)
    - (통합, 길이 — E3-21) 당첨자 이름 41자 · 경품명 81자인 줄 → `{kind: "invalid"}`의 셀 오류 `nameTooLong` · `prizeTooLong`(던지지 않는다 — zod 거부가 아니다) · 행사 · 당첨자 행 수 불변
    - (통합, 멱등 — E3-22) 같은 `requestId`로 `createEvent`를 두 번 부르면 두 번째는 첫 번째와 같은 `{eventId, link}`를 돌려주고 행사 행 1 · 당첨자 행은 한 벌 · 같은 `requestId`로 동시에 둘(`Promise.all`) → 둘 다 같은 `eventId` · 행사 행 1 · `requestId`가 없으면(헬퍼 · 테스트 직접 호출) 부를 때마다 새 행사
    - (통합, 상한 — E3-22) 당첨자 501줄 → 거부(행사 · 당첨자 행 수 불변) · 500줄 → 만들어진다
  </behavior>
  <action>
① **PM 권한 전제(codex-B 3 · checker-B B2):** `role-pm`의 `certs.events` 보기·쓰기는 04.3-09가 시드한다 — 이 플랜(wave 3)이 돌 때는 아직 없다(`domain/seed/index.ts`는 PM에 `projects`만 주고, `can()`은 행이 없으면 거짓). 그래서 `test/integration/cert-events.test.ts`의 PM 케이스 `describe`는 `beforeEach`(setup의 TRUNCATE + 시드 뒤에 돈다)에서 `upsertPermission(SYSTEM_VIEWER, {roleId: "role-pm", menu: "certs.events", action: "view", allowed: true})` · 같은 모양 `action: "write"` 두 줄로 스스로 준다. 되돌리기는 두지 않는다 — 다음 테스트 전에 setup이 TRUNCATE + 시드한다. 이 플랜은 시드(`domain/seed`)를 고치지 않는다 — 04.3-09가 `insertPermissionIfAbsent`로 같은 값을 넣으므로 09 뒤에도 이 두 줄은 무해하다.

② **테스트(RED 먼저):** ③~⑥을 쓰기 **전에** `test/integration/cert-events.test.ts`에 위 `behavior` 전부를 쓰고 실패를 확인한다. **실제 반환값 비노출**은 `quote-line-visibility.test.ts` 선례대로 `upsertVisibility(SYSTEM_VIEWER, {roleId: 테스트 계급, infoItem: "cert_winner.value", visible: false})`로 끄고 `afterEach`/`finally`에서 되돌린다 — 누수 스캔(`leak-scan.test.ts`)은 등록·타입만 보므로 이 케이스가 실제 응답의 증거다.

③ **`domain/certs/events.ts`:**
  - (a) `createEvent`: 첫 줄에서 `isCertFeatureEnabled()`(`domain/certs/feature.ts`)가 거짓이면 `{kind: "notFound"}` — domain은 `notFound()`를 던지지 않고 결과로 돌려준다(액션·페이지는 따로 `lib/certs/feature-guard.ts`의 `assertCertFeatureEnabled()`를 먼저 부른다 — C1 이중 방어). 당첨자 검사를 `validateWinnerRows`로 바꾸고, 셀 오류가 있으면 아무것도 쓰지 않고 `{kind: "invalid", cellErrors, fieldErrors}`(행사 이름 1~80자 · 당첨일 필수는 `fieldErrors`). **zod는 줄 모양만(E3-21 · E3-22):** 04.3-02 zod의 당첨자 줄 내용 규칙(이름 1~40자 · 경품명 1~80자 · 전화 · 수량 · 전달)을 zod에서 빼 `validateWinnerRows`의 셀 오류로만 판정한다 — zod가 먼저 거부하면 `serverError`라 어느 칸인지 알 수 없다. zod에는 줄 칸의 문자열 타입과 당첨자 배열 `.max(500)`(04.3-10과 같은 상한)만 남기고, 요청 전체 크기는 `authedActionClient`의 `checkPayloadSize`가 묶는다. **멱등(E3-22):** 입력에 선택 칸 `requestId`(zod uuid)를 더한다 — 화면은 늘 보내고, 헬퍼(`test/e2e/helpers/cert.ts`)와 테스트 직접 호출은 비워 두므로 04.3-02 C4 헬퍼 호출 줄은 바뀌지 않는다. `requestId`가 있으면 기능 게이트 · 권한 뒤, 검사 전에 `findEventByCreateRequest(viewer, {requestId, createdBy: viewer.id})`로 찾아 있으면 그 행사의 `{eventId, link}`(토큰 암호문 복호)를 그대로 돌려준다. 없으면 행사 행에 `create_request_id`를 채워 만들고, 동시 요청으로 `isUniqueViolation(error, "cert_events_create_request_id_unique")`이면 그 트랜잭션은 되돌려진 것이므로 트랜잭션 **밖에서** 다시 찾아 기존 값을 돌려준다(다시 찾아도 없으면 — 다른 사람의 같은 키 — 그대로 던진다). `ON CONFLICT`로 조용히 넘기지 않는다(당첨자 줄까지 한 트랜잭션이라 행사 행만 건너뛸 수 없다). 트레이서의 `contactMissing`은 그대로. **성공 반환값은 04.3-02 계약 `{eventId, link}` 그대로다(4차 — 이름을 하나로 맞춘다: `eventId`, `id`가 아니다).** 당첨자 행을 싣지 않으므로 투영할 개인정보가 없고, 상세 DTO는 `getEventDetail`만 돌려준다 — 그래서 `test/e2e/helpers/cert.ts`(`createCertEvent` → `{eventId, …}`)와 그 호출부는 바뀌지 않는다(이 플랜이 반환 이름을 바꾸지 않으므로 헬퍼 갱신 소유도 생기지 않는다). 만들기 화면은 받은 `eventId`로 상세(`/certs/events/{eventId}`)로 간다. 실제 반환값 비노출 테스트는 이 반환값에도 이름·전화가 없음을 그대로 본다.
  - (b) `getCreateGate(viewer)` → `{contactMissing, canOpenSettings}`(`canOpenSettings` = `admin.settings` 메뉴 view 권한) — 화면이 1차 막힘 문장 둘 중 하나를 고른다(UI-SPEC Copywriting I2 1차 막힘).
  - (c) `listEvents(viewer)` · `getEventDetail(viewer, eventId)`: 첫 줄 `isCertFeatureEnabled()` 거짓 → `notFound` → `can(viewer, "certs.events", "view")` 거짓 → `notFound` → 범위 = `can(viewer, "certs.submissions", "view")`면 전부, 아니면 `created_by = viewer.id`.
  - (d) 목록 항목 = id · 이름 · 당첨일 · 담당(만든 사람 이름) · 제출 수 · 전체 수 · 상태(접수 중/닫힘 — `closed_at` 또는 기한 지남) · 닫힘 사유·시각.
  - (e) 상세 = 목록 항목 + 마감 · 접수 중이면 `link`(토큰 `decrypt`)와 `qrSvg`(`await renderQrSvg(link)` — E3-25) · 당첨자 줄(이름 · 전화 표시형 · 경품 · 수량 · 전달 · 구별 표시 · 제출 상태 · 제출 시각 · 잠금 해제 시각 · 버전). 닫힌 행사에는 `link`·`qrSvg` 키 자체가 없다.
  - (f) DTO 셋(`CertEventListDto` · `CertEventDetailDto` · `CertWinnerDto`)을 `DtoSpec` + `registerDto`로 등록한다 — 당첨자 이름·전화는 정보 항목 `cert_winner.value`, 행사 칸은 `cert_event.value`. 행을 돌려주는 모든 반환 경로가 `project()`를 거친다(`createEvent`의 `{eventId, link}`는 행이 아니다 — (a)).

④ **`repositories/cert-events.ts`:** 목록 쿼리(범위 서술자 · 제출 수 집계를 한 쿼리로) · 상세 쿼리(행사 + 당첨자) · `findEventByCreateRequest(viewer, {requestId, createdBy}, tx: DbOrTx = db)`(E3-22 — `create_request_id` + `created_by`로 한 행). viewer 첫 인자 · tx 마지막. `repositories/cert-winners.ts`는 고치지 않는다(같은 웨이브의 04.3-03이 고친다).

⑤ **공유 등록부(항목만 더한다):** `domain/permissions/menus.ts` 끝에 `{ key: "certs.submissions", label: "확인증 제출 내용" }` · `domain/permissions/info-items.ts` 끝에 `{ key: "cert_event.value", label: "확인증 행사 정보", staffDefault: true }` · `{ key: "cert_winner.value", label: "당첨자 정보", staffDefault: true }`.

⑥ **액션:** `app/(app)/certs/events/actions.ts`(`"use server"`, `authedActionClient`) `createCertEventAction` — 본문 **첫 문장** `await assertCertFeatureEnabled()`(C1 — domain도 다시 보지만 액션 진입점에서 먼저 막는다), 그다음 domain `createEvent` 하나만 부른다. `actions.registry.ts`에 `registerAction({name: "createCertEventAction", menu: "certs.events", action: "write", dtoName: null})`(반환이 `{eventId, link}`라 행 DTO를 내보내지 않는다 — 액션 축은 `dtoName: null`을 받는다, `app/(app)/admin/permissions/actions.registry.ts` 선례). `test/integration/leak-scan.test.ts`의 import 목록 끝에 `@/domain/certs/events`와 `@/app/(app)/certs/events/actions.registry` 두 줄을 더한다(등록 축 검사 — 실제 반환값 검사는 ②가 따로 한다).

⑦ 커밋: `test(04.3): failing certificate event scope and leak cases` → `feat(04.3): certificate event list and detail with scope`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 새 `actions.registry.ts`가 누수 스캔에 import되지 않았다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-events.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 누수 스캔 등록 축이 깨졌거나, 노출을 끈 계급의 실제 반환값에 이름·전화가 있거나, 범위 필터가 틀렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 「오류 셀 하나면 행사·당첨자 행 수 불변」을 단언한다
    - 통합 테스트가 PM(만든 사람) · 남의 행사 · `certs.submissions` 보유자 세 경우의 범위를 단언한다
    - PM 케이스의 `certs.events` 권한은 테스트 파일 안의 `upsertPermission`에서 온다(`grep -c 'upsertPermission' test/integration/cert-events.test.ts`가 1 이상) · `domain/seed/index.ts`의 diff가 없다
    - 통합 테스트가 `cert_winner.value` 노출을 끈 계급으로 `createEvent` · `listEvents` · `getEventDetail`의 실제 반환값에 이름·전화 문자열이 없음을 단언하고, 켠 계급과 대조한다
    - `app/(app)/certs/events/actions.ts`의 `createCertEventAction` 본문 첫 문장이 `await assertCertFeatureEnabled()`다
    - 누수 스캔 import 두 줄이 더해졌고 `leak-scan-coverage`·`leak-scan`이 녹색이다
    - 통합 테스트가 길이 초과를 `invalid` 셀 오류로(E3-21), 같은 `requestId` 순차 · 동시 두 번을 행사 행 1로, 501줄을 거부로 단언한다(E3-22)
    - 이 플랜의 커밋이 `db/schema/` · `db/migrations/`를 고치지 않는다(`create_request_id`는 04.3-02가 만들었다 — E3-22 · E3-31)
  </acceptance_criteria>
  <done>행사 목록·상세·만들기 domain이 범위 필터·기능 게이트·실제 반환값 비노출 · 길이 셀 오류 · 만들기 멱등과 함께 통합 테스트로 고정되고, PM 권한은 09의 시드를 기다리지 않고 테스트가 스스로 준다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: I1 행사 목록 + I2 행사 만들기 화면 — Form 두 칸 + 폼 밖 편집 표 + 붙여넣기 + 입력 버리기</name>
  <files>app/(app)/certs/events/create-form-rules.ts, test/unit/certs/create-form-rules.test.ts, ui/table/types.ts, ui/table/Table.tsx, test/unit/ui/table-header-describedby.test.ts, app/(app)/certs/events/page.tsx, app/(app)/certs/events/loading.tsx, app/(app)/certs/events/error.tsx, app/(app)/certs/events/events-table.tsx, app/(app)/certs/events/event-create-form.tsx, app/(app)/certs/events/create-entry.tsx, app/(app)/certs/events/events.module.css</files>
  <precondition>`.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` Copywriting I 표에 `/plan-design-review`가 넣은 문장 셋이 있다 — 당첨자 이름 길이 셀 오류 · 경품명 길이 셀 오류(E3-21) · 만들기 실패 줄(서버 거부 · 연결 실패, E3-22). I2 행을 읽어 확인하고, 없으면 문장을 지어내지 않고 멈추고 보고한다 · 그리고 `test -f ui/table/use-editable-width.ts && grep -q 'useEditableWidth' ui/table/use-editable-width.ts`가 종료 코드 0이다(Phase 4 04-49가 만든 폭 판정 — 아니면 폭 판정을 새로 만들지 않고 멈추고 보고한다, D-10) · `grep -q 'firstIssueSignal' ui/table/Table.tsx`가 종료 코드 0이다(Phase 4 04-47 첫 오류 이동 — 아니면 멈추고 보고한다, DR-5)</precondition>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 I1 · I2 전문(그림 · 구조 · 편집 표 열 · 붙여넣기 · 개정 ⑪ · 한 트랜잭션 · 취소 Esc · 폰 규칙 · 다섯 상태)과 Copywriting I 표
    - `docs/design/SYSTEM.md` §6-1(목록 = 원장 · `?new=1` 토글) · §7-3 (가)~(사) · §7-15 · §7-7 · §3(폼 720 · 표는 컨테이너 폭)
    - `app/(app)/projects/page.tsx` · `app/(app)/projects/project-form.tsx`(Phase 4 판) — 목록 + `?new=1` 등록 폼 토글 · 입력 버리기(DR-27) 선례: Escape 갈래의 `event.defaultPrevented` 확인 → `event.preventDefault()`(04-46 편차 주석) → `ConfirmDialog`를 `<form>` 밖 형제로 렌더
    - `ui/confirm-dialog/ConfirmDialog.tsx`(Phase 4, SYSTEM §7-17) — `open` · `onClose` · `title` · `subtitle` · `primary {label, onConfirm}`, 2차 라벨은 `secondaryLabelFor`가 「취소」로 정하고 `Esc` 단축 표시를 단다, 열리면 1차에 포커스 · 닫히면 연 자리로 돌아간다(E3-01)
    - `app/(app)/projects/[id]/quote-table.tsx` — `ui/table` 편집 표 사용 선례(단 이 화면은 `useDirtyStorage`를 부르지 않는다 — 개정 ⑪)
    - `ui/table/Table.tsx` · `ui/table/types.ts` · `ui/table/parse-tsv.ts` · `ui/form/Form.tsx` · `ui/page-header/PageHeader.tsx` · `ui/toast/Toast.tsx` · `ui/list-empty/` — 쓰기만 하고 고치지 않는다. **예외 하나(최종 리뷰 B2):** `ui/table/types.ts`의 `TableColumn.header`는 문자열뿐이고 `Table.tsx`(Phase 4 판)의 `<thead>` 안 머리글 `<th`에는 설명 속성을 넘길 통로가 없다(줄 번호가 아니라 `grep -n '<th' ui/table/Table.tsx`로 찾는다 — Phase 4가 이 파일을 고쳤다, E3-01) — ⓪-b가 선택 필드 하나만 더한다
    - `app/(app)/admin/vendors/page.tsx` — 세션·`can` 게이트 · `notFound()` 관례
    - `app/(app)/error.tsx` — 오류 경계 선례
    - `ui/table/use-editable-width.ts`(Phase 4 04-49가 만든다 — 04-49 frontmatter `files_modified` · artifacts · Task 2 ①, 04-30은 엔지 r2 E2-02 분할로 이 파일을 04-49에 넘겼다. `useEditableWidth()`, 1024 폭 판정 하나 · 서버 스냅숏 참 · 04-42 리저브 대장도 같은 훅을 쓴다) · UI-SPEC Color 「막힘 이유의 색」(`reasonTone` 규칙 — X-4 · 반영 검사 r1 B-1)
    - `ui/table/Table.tsx`의 `firstIssueSignal`(Phase 4 04-47 — 1차가 서버를 부르지 않고 첫 오류 셀로 옮기는 신호, SYSTEM §7-3 DR-5) · `app/(app)/projects/[id]/quote-table.tsx`에서 그 신호를 올리는 자리(`grep -n firstIssueSignal`) · `lib/actions/payload-size.ts`(`checkPayloadSize` — server-only import가 없어 클라이언트 부품이 부를 수 있다)
    - `app/(app)/projects/loading.tsx`(전문) — §7-7 표 뼈대 `loading.tsx` 선례(부제를 렌더하지 않는 이유 주석 포함)
    - `app/(auth)/login/login-error.ts` · `test/unit/login-error.test.ts` — 화면 폴더 안 순수 도우미 + 단위 테스트 선례
  </read_first>
  <behavior>
    - (입력 버리기 N — UI-SPEC Copywriting 「I2 입력 버리기 확인」 셈법) `countChangedCells(처음, 지금)`: 아무것도 안 바꿈 → 0 · 행사 이름만 적음 → 1 · 두 칸 + 새 줄에 이름·전화 → 4 · 기본값 `수량 1` · `전달 현장`만 든 새 줄 → 0 · 새 줄 수량을 2로 → 1 · Task 4 E2E의 붙여넣기 3줄(`김하늘 … 1 현장` · `김민수 … 2 택배` · `김문수 … 2 택배`) → 13(첫 줄의 `1` · `현장`은 기본값과 같아 빼고 3 + 5 + 5) · 적었다가 처음 값으로 되돌린 칸 → 0 · 당첨일 처음 값 = 오늘(KST) 그대로 → 0 · 당첨일을 다른 날로 → 1(D-11)
    - (1차 막힘 이유 — 한 번에 하나, 이 순서 · UI-SPEC Copywriting 「I2 1차 막힘」 그대로, D-7 · X-4) `createBlockReason({contactMissing, canOpenSettings, emptyFields, rowCount})` → `{text, tone}` 또는 `null` — `emptyFields: string[]`는 빈 폼 칸 이름을 화면 순서대로(`["행사 이름", "당첨일"]`), 오류 셀 수는 입력이 아니다(DR-5 — 오류 셀은 1차를 막지 않는다, 반영 검사 r1 B-4): 문의 전화 없음 + 설정 열 수 있음 → `수령자 문의 전화 없음 · 설정 확인증 탭에서 채움`(`block`) · 설정 못 엶 → `수령자 문의 전화 없음 · 등록은 경영관리`(`block`) · 빈 칸 둘 → `행사 이름 · 당첨일 2칸 비어 있음 · 행사 이름 적기`(`block`) · 행사 이름만 빔 → `행사 이름 비어 있음 · 행사 이름 적기`(`block`) · 당첨일만 빔 → `당첨일 비어 있음 · 당첨일 적기`(`block`) · 줄 0 → `당첨자 없음 · 첫 줄 만들기`(`block`) · 줄 501 → `당첨자 501명 · 500명까지 줄이기`(`block`, 반영 검사 r1 N-1) · 줄 500 · 칸 다 참 → `null` · 막힘 없음 → `null`(1차 켜짐) · 어느 결과에도 `tone: "info"`가 없다(내부 폼 — 반영 검사 r1 B-1) · 어느 문장에도 `수 없음`이 없다(「못 한다」 접두 금지 — §8 규칙 5 · D-7)
    - (단위, `ui/table` 머리글 설명 — 최종 리뷰 B2) `renderToStaticMarkup(createElement(Table, …))`로 그린 표에서 `headerDescribedBy: "hint-id"`를 준 열의 `<th>`에 `aria-describedby="hint-id"`가 있고, 주지 않은 열의 `<th>`에는 `aria-describedby` 속성이 없다(기존 표의 머리글 마크업이 그대로다)
    - (서버 거부 고정) `pinCellErrors(cellErrors)`: 코드 11종(길이 둘 `nameTooLong` · `prizeTooLong` 포함 — E3-21)이 UI-SPEC Copywriting I2 셀 오류 문장으로 바뀌고(`shapeDuplicate`는 모양 · 줄 수를 넣은 `수령자 목록에 {모양}가 {N}줄입니다 · …`) `{rowKey}:{column}` 키로 모인다 · 합계 문장 `오류 {N}칸 · 전부 거부`의 N = 오류 셀 수
    - (제출 응답 가르기 — E3-22) `createSubmitOutcome(response)`: `{data: {eventId, link}}` → `{kind: "success", eventId}` · `{data: {kind: "invalid", cellErrors, fieldErrors}}` → `invalid`(셀 오류 그대로) · `{data: {kind: "contactMissing"}}` → `contactMissing`(만드는 사이 설정이 비워진 경합) · `{serverError: "…"}`(요청 크기 한도 등) · `{validationErrors: …}` · `"unreachable"`(액션 호출이 던짐 — 연결 실패) · 모양을 알 수 없는 응답 → 모두 `failed`
  </behavior>
  <action>
⓪ **테스트(RED 먼저 — checker-B W4):** 화면 코드(①~⑥)를 쓰기 **전에** `test/unit/certs/create-form-rules.test.ts`에 위 `behavior` 전부를 쓰고 실패(모듈 없음)를 확인한다 → `app/(app)/certs/events/create-form-rules.ts`(순수 — React · DB · 설정을 import하지 않는다, `app/(auth)/login/login-error.ts` 선례)에 `countChangedCells` · `createBlockReason` · `pinCellErrors` · `createSubmitOutcome`(E3-22)을 만들어 녹색. 화면(③)은 이 넷만 불러 N · 막힘 이유 · 오류 셀 · 제출 응답 갈래를 정한다 — 같은 판정을 화면 안에 다시 쓰지 않는다. 문장은 UI-SPEC Copywriting 그대로 이 모듈에 둔다(화면에 따로 적지 않는다). 막힘 이유는 한 번에 하나만 보인다 — 사람이 고를 것이 없게 가장 먼저 풀어야 할 것 하나만 말한다.

⓪-b **`ui/table` 머리글 설명 한 속성(최종 리뷰 B2 — RED 먼저):** `ui/table`을 고치기 **전에** `test/unit/ui/table-header-describedby.test.ts`(`.test.ts` — unit 프로젝트는 node 환경이라 JSX 대신 `react`의 `createElement` + `react-dom/server`의 `renderToStaticMarkup`로 `Table`을 그린다)에 위 `behavior`의 머리글 줄을 쓰고, 필드가 없어 `<th>`에 속성이 없는 단언 실패를 확인한다(vitest는 타입을 검사하지 않는다 — 필드를 더한 뒤 `pnpm typecheck`도 녹색이어야 한다) → `ui/table/types.ts` `TableColumn`에 선택 필드 `headerDescribedBy?: string`(JSDoc 한 줄 — 머리글 `<th>`의 `aria-describedby`)을 더하고 `Table.tsx`의 머리글 `<th>`에 `aria-describedby={column.headerDescribedBy}`만 더한다(값이 없으면 React가 속성을 그리지 않는다) → 녹색. 다른 prop · 렌더 · CSS · 기존 필드 이름은 바꾸지 않는다 — **더하기만 한다.** 04.3-10(웨이브 5)도 `ui/table/types.ts` · `Table.tsx`를 고치므로, 이 변경은 기존 줄을 옮기거나 지우지 않는 추가 두 줄로 둬서 04.3-10이 그 위에 그대로 얹게 한다. 커밋은 `test(04.3): failing table header describedby` → `feat(04.3): table header can carry describedby`.

① **`page.tsx`(서버):** `requireSession` → `await assertCertFeatureEnabled()`(C1 — env 게이트 + 설정, 꺼짐이면 셸 안 404) → `listEvents(viewer)`가 `notFound`면 `notFound()`. PageHeader `확인증 행사` + 표 위 행동 줄 오른쪽 1차 「행사 만들기」(이 목록에는 필터가 없다 — UI-SPEC I1 · X-8)(→ `?new=1`, 쓰기 권한이 있을 때만, 폼이 열려 있으면 렌더하지 않는다). `?new=1`이면 `getCreateGate`를 부르고 `<EventCreateForm>`을 위에, 목록을 아래에 둔다(§6-1 토글).

①-b **`loading.tsx`(Codex #25):** `app/(app)/projects/loading.tsx`와 같은 모양 — PageHeader 제목 `확인증 행사`만(부제 없음, 선례 주석의 이유) + `aria-hidden` 표(숨긴 `<caption>` · 열 머리글 다섯 · `--surface` 뼈대 행 3개 · 합계 행 자리). 스피너·반짝임 없음(§7-7). 클래스는 `events.module.css`에 토큰으로.

② **`events-table.tsx`:** 읽기 표(흰 머리글, `<table>` + 숨긴 `<caption>`), 열 행사(P1) · 당첨일(P2) · 담당(P2) · 제출 `3/12`(P1, 우측 정렬, 접근 이름 `제출 3명 / 전체 12명`) · 상태(P1, 색 글자 `접수 중` `--accent` / `닫힘` `--muted`). 그룹 머리글 `접수 중` → `닫힘`, 그룹 안 당첨일 내림차순(domain이 정렬해 준 순서를 그대로). 행 전체가 상세 링크. EMPTY 한 줄(1024 이상에만 3차 「행사 만들기」 — ④, D-10). 폰 칸 접기(P2 접힌 줄 `2026-09-13 · 박서연`).

③ **`event-create-form.tsx`(`"use client"`) — UI-SPEC I2 그대로, 아래 순서로 만든다(`create-entry.tsx` = ④의 1024 판정 조각):**
  - (a) **뼈대:** `Form`(`<form id="cert-event-new">`, 720)에는 `행사 이름`(긴 칸 480, 자리표시자 없음) · `당첨일`(짧은 칸 280, **초기값 오늘(KST)** — 서버가 렌더 때 KST 날짜를 넘겨 `useState` 초기값과 `countChangedCells`의 「처음 값」으로 쓴다, 자리표시자 `2026-09-18`은 형식 예로 남긴다 — D-11)만. 섹션 라벨 `당첨자` + `ui/table` 편집 표 + `Form.Actions`는 `<form>` **밖** 형제(컨테이너 폭).
  - (b) **편집 표 열:** 행 번호 · 이름 · 전화번호 · 경품명 · 수량(기본 1) · 전달(기본 `현장`, 셀 편집은 네이티브 `<select>` 두 값, 붙여넣기는 글자 `현장`/`택배`) · 구별 표시(선택, 빈 값 기본, 입력 10자 제한 — 서버 검사는 그대로). 이름 편집 입력은 `maxLength={40}`, 경품명은 `maxLength={80}`(E3-21 — 붙여넣기는 이 제한을 거치지 않으므로 넘은 값은 서버 셀 오류 `nameTooLong` · `prizeTooLong`로 선다). 엑셀 범위 붙여넣기 순서 = 이름 · 전화번호 · 경품명 · 수량 · 전달(· 구별 표시). 저장 전 줄은 Delete로 지운다(확인 없음). 합계 행 `합계 · N명`. 힌트 줄(1024 이상) `이동 Tab ↑↓←→ · 복사 Ctrl+C · 붙여넣기 Ctrl+V · 취소 Esc · 새 줄 Ctrl+Enter`(UI-SPEC I2 그림 그대로 — 조합 키는 `Ctrl+키`, 맥 글리프 없음, D-6).
  - (c) **`수령자 화면` 계산 열**(읽기 전용, 입력 없음): 서버와 같은 `domain/certs/roster-display.ts`(`buildPublicRows`/`publicShape` — 순수 모듈이라 클라이언트 부품이 import한다)로 지금 표 전체를 계산해 1행 가린 이름 + 2행이 있으면 `--fs-sm --muted` 2행(E2 목록 행과 같은 모양).
  - (d) **구별 표시 상시 힌트와 연결(체커 경고):** 섹션 라벨 `당첨자` 바로 아래 한 줄 `구별 표시는 수령자 목록에 그대로 보입니다 · 이름·전화번호는 쓰지 마세요`(`--fs-sm --muted`, `useId`로 만든 id를 가진 `<p>`). 그 id를 ① `구별 표시` 열 머리글의 `aria-describedby`(열 정의의 `headerDescribedBy` — ⓪-b가 더한 필드)와 ② **구별 표시 열의 `editCell`이 그리는 편집 `<input>`의 `aria-describedby`**에 준다 — `ui/table`의 `editCell(row, ctx)`는 화면이 입력 요소를 직접 그리므로 입력 쪽은 `ui/table`을 고치지 않고 화면 쪽에서 속성을 단다. 셀 오류가 있으면 그 오류 줄 id와 공백으로 잇는다.
  - (e) **1차 막힘:** 1차 비활성 + 이유 한 줄 = ⓪의 `createBlockReason`의 `text`, 색 = 그 `tone`을 Phase 4 `Button`의 `reasonTone`에 그대로(설정 `수령자 문의 전화`가 비었는지 · 설정을 열 수 있는지는 `getCreateGate`가 준다). 화면이 문장 · 색을 고르지 않는다(D-7 · X-4 — I2의 `tone`은 늘 `block`, 반영 검사 r1 B-1). **오류 셀이 있을 때(DR-5 — 반영 검사 r1 B-4):** 고정된 폼 칸 오류(`fieldErrors`)나 표 오류 셀은 막힘 이유가 아니라 1차를 끄지 않는다(다른 막힘이 없으면 켜진 채). 그 상태에서 누르면(버튼 · 폼 칸 `Enter` 모두) 제출 핸들러가 서버를 부르지 않고 첫 오류로 포커스를 옮긴다 — 폼 칸 오류가 있으면 그 칸(`행사 이름` → `당첨일` 순), 아니면 표의 `firstIssueSignal`을 올려 Phase 4 `Table`이 첫 오류 셀로 옮긴다(`quote-table.tsx` 선례 · 새 포커스 코드를 만들지 않는다). 오류 셀 고정 · 풀림은 Phase 4 `ui/table` 동작 그대로다.
  - (f) **제출(E3-22):** 1차 「행사 만들기」는 `type="submit" form="cert-event-new"` — 제출 핸들러가 두 칸 + 표 상태 + `requestId`를 `createCertEventAction` 한 번으로 보낸다. `requestId`는 폼이 처음 그려질 때 `crypto.randomUUID()`로 한 번 만들고(`useState` 초기값) 성공할 때까지 같은 값을 보낸다 — 첫 요청이 서버에서 행사를 만든 뒤 응답만 끊겨도 다시 누른 요청이 같은 행사를 돌려받는다. 보내기 전(오류 셀이 없을 때): `checkPayloadSize`(`lib/actions/payload-size.ts` — 서버 미들웨어와 같은 함수 · 같은 한도)가 `ok`가 아니면 요청을 보내지 않고 합계 행 저장 결과 자리에 그 `reason`(`요청이 너무 큽니다 · {N}KB > 256KB 한도 · 나눠서 저장해 주세요`, `--danger`)을 세우고 값을 그대로 둔다 — 결과 불명 문장이 아니다(확정 거부, 반영 검사 r1 N-1 · UI-SPEC 「I2 만들기 실패」 끝). 500줄 넘음은 (e)의 1차 막힘이 먼저 막는다. 응답은 ⓪의 `createSubmitOutcome`으로만 가른다: `invalid` → `pinCellErrors`로 셀 오류 고정 + 합계 행 `오류 N칸 · 전부 거부` · `contactMissing` → 1차 막힘 이유를 문의 전화 문장으로(`createBlockReason`, 기존 문장) · `failed` → 합계 행 오른쪽 저장 결과 자리에 `--danger` 한 줄 `만들었는지 확인하지 못했습니다 · 다시 눌러 주세요`(UI-SPEC Copywriting 「I2 만들기 실패」, 합계 행 `aria-live="polite"` — D-2), 편집 값 유지, 1차 다시 켬 · 성공 → `/certs/events/{eventId}`로 이동 + 토스트 `행사 만들기 · {행사 이름} · 당첨자 N명`(기존 토스트 전달 방식).
  - (g) **미저장 편집(개정 ⑪):** 브라우저 저장소에 두지 않는다(`useDirtyStorage`를 부르지 않는다). dirty면 `beforeunload` 경고만.
  - (h) **「취소 Esc」(E3-01 — Phase 4 `ui/confirm-dialog`):** 내부 컨트롤(셀 편집 · 열린 select)이 `Esc`를 먼저 쓴다(`event.defaultPrevented`면 아무것도 하지 않는다). 그 밖에서는 `event.preventDefault()`를 먼저 부르고(Phase 4 `project-form.tsx` 04-46 편차 — 안 부르면 같은 keydown이 방금 연 확인을 다시 닫는다), N(⓪의 `countChangedCells` — UI-SPEC 「I2 입력 버리기 확인」의 셈법)이 0이면 목록, 1 이상이면 `ConfirmDialog`를 연다(`title` `입력 버리기` · `subtitle` `행사 만들기 · {N}칸` · `primary` 라벨 `입력 버리기` → 목록 · 2차 `취소`와 `Esc` 표시 · 열리면 1차 포커스 · 닫히면 연 자리로는 부품이 한다). 확인은 `<form>` · 표 밖 형제로 렌더한다(Phase 4 선례). 제출 중에는 폼의 `Esc`를 무시한다. 네이티브 대화 상자 요소를 화면 파일에 따로 두지 않는다.

④ **1024 미만(좁은 PC · 폰) — 만들기 없음(UI-SPEC I2 「1024 미만(좁은 PC · 폰): 만들기 없음」 · SYSTEM §6 ⑷ DR-36 · D-10):** 폭 판정은 Phase 4 `useEditableWidth()`(`ui/table/use-editable-width.ts` — Phase 4 04-49, `matchMedia("(min-width: 1024px)")`, 서버 스냅숏 참) **하나**다 — 화면 CSS 미디어 쿼리로 두 번째 폭 판정을 만들지 않는다. `event-create-form.tsx`는 이 훅이 거짓이면 폼을 그리지 않고 목록만 남긴다(1024 미만에서 `?new=1`로 들어오면 목록이 선다) — 폼 부품 안에서 `null`을 돌려 부품을 떼지 않는다(편집 중 창이 좁아져도 입력 상태가 남아 1024 이상으로 돌아오면 그대로 다시 선다 · `beforeunload` 경고도 남는다, 반영 검사 r1 N-4). 서버 `page.tsx`의 머리 1차 「행사 만들기」와 `events-table.tsx` EMPTY의 3차는 같은 훅을 부르는 작은 클라이언트 조각(`events.module.css`와 같은 폴더)으로 감싸 1024 미만에서 렌더하지 않는다(EMPTY는 앞 문장만). 폰(<700) 칸 접기는 §7-3 레이아웃 그대로다.

⑤ **`error.tsx`:** 목록 조회 실패 → 표 자리 `확인증 행사를 불러오지 못했습니다 · 다시 시도`(`--danger`) + 2차 「다시 시도」(`reset()`).

⑥ **`events.module.css`:** 토큰만. 행사 이름·경품명 줄바꿈(`keep-all` + `overflow-wrap: anywhere`), 숫자 `nowrap`.

⑦ 행사 이름 칸은 수령자 화면 부제에 그대로 나간다 — 화면에 안내 문구로 쓰지 않는다(§8-5). 이 사실은 SUMMARY에만 적는다.

⑧ 커밋 `test(04.3): failing create form rules` → `feat(04.3): certificate event list and create screen`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/create-form-rules.test.ts test/unit/ui/table-header-describedby.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>test "$(grep -rn 'useDirtyStorage\|localStorage\|sessionStorage\|indexedDB' 'app/(app)/certs' | wc -l)" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 확인증 화면 어딘가가 브라우저 저장소를 쓴다(개정 ⑪ 위반)</fails_when>
    <automated>F='app/(app)/certs/events/event-create-form.tsx'; grep -q 'ui/confirm-dialog/ConfirmDialog' "$F" &amp;&amp; grep -q 'createSubmitOutcome' "$F" &amp;&amp; grep -q 'randomUUID' "$F" &amp;&amp; test "$(grep -v '^[[:space:]]*//' "$F" | grep -c '<dialog')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 「입력 버리기」가 Phase 4 `ConfirmDialog`를 쓰지 않거나 화면 파일에 네이티브 대화 상자가 남았거나(E3-01), 제출 응답을 `createSubmitOutcome`으로 가르지 않거나 멱등 `requestId`를 만들지 않는다(E3-22)</fails_when>
    <automated>D='app/(app)/certs/events'; test "$(grep -nF -e "$(printf '\342\214\230')" -e '수 없음' "$D/create-form-rules.ts" "$D/event-create-form.tsx" "$D/create-entry.tsx" "$D/events-table.tsx" "$D/page.tsx" "$D/error.tsx" | wc -l)" = 0 &amp;&amp; test "$(grep -rn '1024' 'app/(app)/certs' --include=*.css | wc -l)" = 0 &amp;&amp; grep -q 'useEditableWidth' "$D/event-create-form.tsx" &amp;&amp; grep -q 'useEditableWidth' "$D/create-entry.tsx" &amp;&amp; grep -q 'reasonTone' "$D/event-create-form.tsx" &amp;&amp; grep -q 'firstIssueSignal' "$D/event-create-form.tsx" &amp;&amp; grep -q 'checkPayloadSize' "$D/event-create-form.tsx"</automated>
    <fails_when>종료 코드가 0이 아니다 — 이 태스크가 만드는 I1 · I2 파일(여섯 개 — `[id]/`는 04.3-10이 `제출된 자리 · 고칠 수 없음`을 둘 수 있어 보지 않는다, 반영 검사 r1 N-7)에 맥 커맨드 글리프(U+2318, 패턴은 바이트로 적었다)나 「못 한다」 접두 문장(`수 없음`)이 남았거나(D-6 · D-7), CSS에 1024 미디어 쿼리로 두 번째 폭 판정을 만들었거나 만들기 폼 · 진입 조각이 `useEditableWidth`를 부르지 않거나(D-10), 1차 막힘 색을 `reasonTone`으로 넘기지 않거나(X-4), 오류 셀이 남았을 때 첫 오류로 옮기는 `firstIssueSignal`을 쓰지 않거나(DR-5 · B-4), 보내기 전 요청 크기 판정 `checkPayloadSize`를 부르지 않는다(N-1)</fails_when>
  </verify>
  <acceptance_criteria>
    - `create-form-rules.ts`가 React·DB·설정을 import하지 않고, 단위 테스트(입력 버리기 N · 막힘 이유 순서 · 서버 오류 고정)가 git 기록에서 화면 코드보다 먼저 커밋됐다
    - `event-create-form.tsx`가 `countChangedCells` · `createBlockReason` · `pinCellErrors` · `createSubmitOutcome`과 `ui/confirm-dialog/ConfirmDialog`를 import하고, 화면 파일에 네이티브 대화 상자 요소가 없다(E3-01 · E3-22)
    - `createSubmitOutcome` 단위 케이스(성공 · `invalid` · `contactMissing` · `serverError` · `validationErrors` · 연결 실패 · 해석 불가)가 있고 `failed` 갈래에서 화면이 값을 지우지 않고 같은 `requestId`로 다시 보낸다(E3-22)
    - 이름 · 경품명 편집 입력에 `maxLength` 40 · 80이 있다(E3-21)
    - `app/(app)/certs/`에서 브라우저 저장소 API·`useDirtyStorage` 사용이 0건이다
    - 1차 「행사 만들기」가 `form="cert-event-new"`로 폼 밖에서 제출한다
    - `ui/form` · `ui/page-header` 디렉터리의 diff가 없고, `ui/table`의 diff는 `types.ts`의 선택 필드 `headerDescribedBy` 하나와 `Table.tsx` 머리글 `<th>`의 `aria-describedby` 한 속성뿐이다(더한 줄만 — `feat(04.3): table header can carry describedby` 커밋을 `git show --numstat <그 커밋> -- ui/table`로 보면 삭제 수가 0 · 기존 필드 이름 변경 없음)
    - `test/unit/ui/table-header-describedby.test.ts`가 git 기록에서 `ui/table` 변경보다 먼저 커밋됐고(RED) 녹색이다 · 구별 표시 열 머리글이 힌트 id를 `headerDescribedBy`로 받는다
    - 1024 미만에서 `useEditableWidth()`가 만들기 1차 · 폼 · EMPTY 3차를 거두고(Task 4 E2E (k) 1000px), `app/(app)/certs` CSS에 `1024` 미디어 쿼리가 없다(두 번째 폭 판정 없음 — D-10)
    - `createBlockReason` 단위 케이스가 UI-SPEC Copywriting 「I2 1차 막힘」 문장과 `tone`을 그대로 단언하고(전부 `block` — `info` 결과 없음, 반영 검사 r1 B-1) 입력에 오류 셀 수가 없으며(DR-5 · B-4) 501줄 → `당첨자 501명 · 500명까지 줄이기` · 500줄 → `null`을 단언하고(N-1), 1차 `Button`이 그 `tone`을 `reasonTone`으로 받는다(D-7 · X-4)
    - 오류 셀이 남은 채 1차를 누르면 만들기 요청이 나가지 않고 포커스가 첫 오류로 간다(Task 4 E2E (b) — SYSTEM §7-3 DR-5 · B-4) · 요청 크기 한도는 보내기 전에 `checkPayloadSize`로 막고 결과 불명 문장을 쓰지 않는다(N-1)
    - `grep -c "$(printf '\342\214\230')" .planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md`가 0이고(맥 커맨드 글리프 없음) 이 태스크가 만드는 I1 · I2 파일 여섯 개에 그 글리프 · `수 없음`이 0건이다(D-6 · D-7 · 범위는 N-7)
    - `app/(app)/certs/events/loading.tsx`가 있고 `aria-hidden` 표 뼈대를 그리며 스피너가 없다
    - `page.tsx`가 `assertCertFeatureEnabled`를 부른다(`grep -q assertCertFeatureEnabled 'app/(app)/certs/events/page.tsx'`)
    - 구별 표시 편집 입력에 힌트 id가 `aria-describedby`로 걸린다(Task 4 E2E가 단언)
    - 단위 테스트와 세 게이트가 녹색이다
  </acceptance_criteria>
  <done>담당자가 목록에서 행사 만들기로 두 칸 + 당첨자 표를 한 번에 저장할 수 있고, 로딩은 표 뼈대이며, 미저장 편집은 브라우저 저장소에 남지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: I3 행사 상세(읽기) — 머리 · QR 섹션(링크 복사) · 당첨자 읽기 표 + 목록→만들기→상세 E2E</name>
  <files>app/(app)/certs/events/[id]/page.tsx, app/(app)/certs/events/[id]/loading.tsx, app/(app)/certs/events/[id]/qr-section.tsx, app/(app)/certs/events/[id]/winners-view.tsx, app/(app)/certs/events/[id]/event-detail.module.css, test/e2e/cert-events.spec.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 I3(그림 · 머리 · QR 섹션 접수 중/닫힘 · 당첨자 표 제출 열 · 합계 행 · 폰) · Copywriting I3 행들
    - `docs/design/SYSTEM.md` §6-2(상세 · 04.3-01의 ③ QR 줄) · §7-5(04.3-01의 ⑤ 낱말 다섯) · §7-1 SUCCESS(라벨 자리 결과 4초)
    - `app/(app)/projects/[id]/page.tsx` — 상세 머리(PageHeader + 상태 태그 + 부제) 선례
    - `ui/status-tag/StatusTag.tsx` · `ui/table/RowSheet.tsx`(보기 전용 — 이 태스크는 고치지 않는다, `action` 자리는 04.3-10)
    - `domain/certs/events.ts` `getEventDetail`(Task 2 산출물)
    - `test/e2e/fixtures.ts`(`createFixtureUser`) · `test/e2e/projects-list.spec.ts`(데스크톱 목록 E2E 선례)
    - `test/e2e/code-tables-write-gate.spec.ts` · `test/e2e/permissions-grid.spec.ts:15-18`(공유 계급을 바꾸지 않는 규칙) — 임시 계급 선례(`insertRole` · `setPermissionCell` · `setRoleArchived`) · `repositories/permissions.ts`(`upsertVisibility`) · `repositories/users.ts`(`findUserByEmail`) · `domain/viewer.ts`(`Viewer = {id, roleId}`) — (f-0)·(f)의 임시 계급 권한 주기와 viewer 만들기
    - `test/e2e/helpers/cert.ts` · `test/e2e/cert.setup.ts`(04.3-02 산출물, C4) — `createCertEvent`(고유 이름 행사) · `withCertFeatureOff`(기능 끄기 범위) · `CERT_E2E_CONTACT_PHONE`, 기능·문의 전화는 setup이 한 번 켠다. 스펙 이름에 `cert`가 들어가 `certs` 프로젝트(workers 1)에서 돈다. 읽기만 한다
    - `app/(app)/projects/loading.tsx` — 상세 뼈대도 같은 선례
  </read_first>
  <action>
⓪ **E2E 먼저(RED — checker-B W4):** ①~④-b를 쓰기 **전에** ⑤의 `test/e2e/cert-events.spec.ts`를 전부 쓰고 돌려, 상세 화면 단언((e) QR · 링크 · 합계)이 실패함을 확인한다(Task 3의 만들기 화면 단언은 이미 녹색일 수 있다). 그다음 ①~④-b로 녹색.

① **`[id]/page.tsx`(서버):** `requireSession` → `await assertCertFeatureEnabled()`(C1, 꺼짐이면 셸 안 404) → `getEventDetail`이 `notFound`면 `notFound()`. PageHeader: 제목 = 행사 이름 + `StatusTag`(`접수 중` `--accent` / `닫힘` `--muted`), 부제 = `당첨일 {날짜} · 담당 {이름} · 마감 {일시}`. 머리 2차 「링크 닫기」는 04.3-10이 더한다(이 태스크는 자리만 비워 둔다 — 빈 버튼을 두지 않는다).

② **`qr-section.tsx`:** 섹션 라벨 `QR`(2px 선으로 시작). 접수 중 = DTO의 `qrSvg`를 인라인으로(`role="img"` · `aria-label="{행사 이름} 확인증 QR"` · 160×160 · `color: var(--fg)` · `background: var(--bg)` · radius 0) + 링크 줄(전체 URL, `--fs-sm`, `overflow-wrap: anywhere`) + 3차 「링크 복사」(`navigator.clipboard.writeText` → 라벨 자리 `링크 복사됨` `--success` 4초 뒤 원래 라벨, 토스트 없음 · 실패 → `복사하지 못했습니다 · 링크를 길게 눌러 복사해 주세요`). 닫힘 = QR·링크를 렌더하지 않고 `--fs-sm --muted` 한 줄(Copywriting 「I3 닫힌 QR 섹션」 사유 셋). QR 내려받기·인쇄 버튼 없음(A8). 인라인 SVG는 서버가 만든 문자열만 넣는다(사용자 입력이 섞이지 않는다 — 링크는 서버 토큰).

③ **`winners-view.tsx`:** 흰 머리글 읽기 표(편집 표로 바꾸는 일은 04.3-10), 열 이름 · 전화번호(가리지 않음, A6) · 경품명 · 수량 · 전달 · 구별 표시 · 수령자 화면(I2와 같은 `roster-display.ts` 계산 열) · 제출. 제출 열: `제출됨`(`--success`) + 2행 `MM-dd HH:mm` · `미제출`(`--muted`) · `잠김`(`--warning`) + 2행 `HH:mm까지`(서버가 준 해제 시각). 합계 행 `합계 · N명 · 제출 M명`. 폰: 칸 접기 — P1 이름 · 수량 · 제출, P2 접힌 줄 `{전화} · {경품} · {전달}`(구별 표시가 있으면 ` · {구별 표시}`를 붙인다), 행 탭 → `RowSheet`(보기 전용).

④ **`event-detail.module.css`:** 토큰만. QR 크기 160 고정(폰도 160).

④-b **`[id]/loading.tsx`(Codex #25):** 목록 `loading.tsx`와 같은 결 — 제목 자리만 있는 PageHeader(부제 없음) + `aria-hidden` 당첨자 표 뼈대(머리글 · `--surface` 3행 · 합계 행 자리). QR 자리는 그리지 않는다(없는 것을 있는 척하지 않는다). 스피너 없음.

⑤ **E2E `test/e2e/cert-events.spec.ts`(데스크톱 폭, C4 — `test/e2e/helpers/cert.ts`만 쓴다):** 파일 이름에 `cert`가 있어 `certs` 프로젝트(workers 1)에서 돈다 · 스펙 머리 `test.describe.configure({ mode: "serial" })`. 행사 이름은 테스트마다 고유하게(`createCertEvent` 규칙 — 화면으로 만드는 행사도 같은 규칙의 이름). 기능·문의 전화는 `cert.setup.ts`가 켜 두므로 전역 설정(`cert.enabled` · `cert.contact_phone`)을 스펙이 직접 바꾸지 않고, 기능 끄기는 `withCertFeatureOff` 범위 안에서만. **목록이 비었다는 전역 단언을 하지 않는다**(다른 스펙이 남긴 행사가 있을 수 있다 — Codex #21) — 목록 단언은 자기 행사 행만 찾아서 한다. 흐름:
  - (a) 시스템 관리자 픽스처로 로그인 → (기능·문의 전화는 `cert.setup.ts`가 이미 켰다) → `/certs/events` → 「행사 만들기」 → 행사 이름·당첨일 → 첫 셀에 TSV 붙여넣기(`김하늘 010-4821-7730 갤럭시 탭 S10 1 현장` · `김민수 010-1111-2222 스타벅스 기프티콘 2 택배` · `김문수 010-3333-4444 스타벅스 기프티콘 2 택배`).
  - (b) 「행사 만들기」 → 구별 표시 셀 오류 둘(문장 `수령자 목록에 김*수 · 스타벅스 기프티콘 2개가 2줄입니다 · …`) + 합계 행 `오류 2칸 · 전부 거부` → **DR-5(반영 검사 r1 B-4):** 1차 「행사 만들기」가 `not.toBeDisabled()`이고 이유 줄이 없다 → 1차를 다시 누르면 `createCertEventAction` 요청이 0개(`page.on("request")`로 `Next-Action` 헤더 요청을 센다)이고 `document.activeElement`가 첫 오류 셀(`김민수` 줄의 `구별 표시` 셀 — 그 셀의 `aria-invalid="true"`)이다.
  - (c) **힌트 연결(체커 경고):** 구별 표시 셀을 편집 상태로 열면 그 `<input>`의 `aria-describedby`가 상시 힌트 `<p>`의 id를 포함하고, 그 id의 글이 `구별 표시는 수령자 목록에 그대로 보입니다 · 이름·전화번호는 쓰지 마세요`다 · `구별 표시` 열 머리글도 같은 id를 가리킨다.
  - (d) 구별 표시에 `7-7-3` → 셀 오류(`labelDigits` 문장) · `김 하늘` → 셀 오류(`labelName` 문장) → `오전 조`·`오후 조`로 고침 → 그 두 줄의 `수령자 화면` 열 2행이 `스타벅스 기프티콘 2개 · 오전 조` / `… · 오후 조`.
  - (e) 「행사 만들기」 → 상세로 이동 · 토스트 · QR(`role=img` 이름) · 링크 줄 · 당첨자 셋 `미제출` · 합계 `합계 · 3명 · 제출 0명` → 목록으로 돌아가 `접수 중` 그룹에서 **자기 행사 이름의 행**을 찾아 제출 `0/3`.
  - (f-0) **임시 계급 · 임시 사용자(4차 W-1 — 공유 `role-pm`을 바꾸지 않는다):** E2E는 스펙 사이에 DB를 초기화하지 않고 여러 프로젝트가 같은 DB를 쓰므로, 공유 계급 `role-pm`의 권한을 바꾸면 다른 스펙의 「기획 PM은 404」 가정이 깨진다(저장소 규칙 `test/e2e/permissions-grid.spec.ts:15-18`). 그래서 (f-0-b)·(f)를 담은 `describe`의 `beforeAll`이 `test/e2e/code-tables-write-gate.spec.ts`와 같은 모양으로 — `insertRole(SYSTEM_VIEWER, {id: "role-e2e-cert-" + randomUUID(), name: "E2E 임시 계급 " + 끝 12자, sortOrder: 99})` → `setPermissionCell(SYSTEM_VIEWER, {roleId: 그 id, menu: "certs.events", action, allowed: true})`를 `view` · `write` 둘에 → **`upsertVisibility(SYSTEM_VIEWER, {roleId: 그 id, infoItem: "cert_event.value", visible: true})`로 행사 칸 노출을 켠다(codex-final3-B 3):** 새 계급에는 노출 행이 하나도 없고 `domain/permissions/visible.ts:21`은 행이 없으면 숨김으로 판정한다(`staffDefault`는 시드가 `role-pm`에 넣는 값일 뿐 `insertRole`은 노출 행을 만들지 않는다) — 켜지 않으면 (f)의 상세가 행사 칸까지 숨긴 채 서거나 오류 화면이 되어, 당첨자 칸만 끄고 켜는 대조가 성립하지 않는다 → `createFixtureUser({roleId: 그 id})`로 임시 사용자를 만든다. `afterAll`(`finally`)이 `setRoleArchived(SYSTEM_VIEWER, 그 id, true)`로 보관한다(하드 DELETE 금지 — 같은 선례의 규칙 · 보관된 계급은 목록·권한표에서 빠진다 · 임시 사용자는 고유 이메일이라 남아도 다른 스펙과 겹치지 않는다). 임시 계급에는 `certs.submissions` 보기를 주지 않는다. **범위 근거:** 「자기 행사만」 범위는 `role-pm`에 묶인 규칙이 아니라 `listEvents`의 권한 규칙(② (c) — `certs.submissions` 보기가 있으면 전부, 없으면 `created_by = viewer.id`)이라 `certs.events`만 가진 임시 계급이 PM과 같은 범위를 탄다 — `role-pm`이 필요 없다. 시드(`domain/seed`)는 고치지 않는다.
  - (f-0-b) **EMPTY(격리된 방법):** (f-0)의 임시 사용자(자기 행사만 보는 범위라 다른 스펙의 행사가 보이지 않는다)로 로그인한 두 번째 컨텍스트에서 `/certs/events` → EMPTY 한 줄 `확인증 행사가 없습니다 · 행사 만들기`. 전역 목록이 비었다는 가정이 아니라 범위 규칙으로 비어 있음을 만든다.
  - (f) **실제 HTML 비노출(Codex #22):** 같은 임시 사용자를 만든 사람으로 하는 행사를 **한 경로로만** 만든다(checker-B W7): `findUserByEmail(SYSTEM_VIEWER, 픽스처 email)`의 `id`로 viewer `{id, roleId: 임시 계급 id}`를 만들어 domain `createEvent(그 viewer, {이름 · 당첨일 · 당첨자 줄(김하늘 010-4821-7730 …)})`를 스펙에서 직접 부른다 — (f-0)이 준 `certs.events` 쓰기 권한으로 통과하고 `created_by`가 그 사용자가 된다(`createCertEvent` 도우미는 만든 사람을 받지 않으므로 쓰지 않는다 · `db/client`로 행을 직접 넣지 않는다 — 토큰 암호화를 건너뛴 행이 생긴다). 행사 이름은 `createCertEvent`와 같은 고유 접미사 규칙. 그 사용자로 로그인한 두 번째 컨텍스트에서 — `cert_winner.value` 노출을 **임시 계급에서** 끈 뒤(`upsertVisibility` — 공유 계급의 노출도 건드리지 않는다, 임시 계급은 끝에서 보관된다) `page.request.get("/certs/events/{id}")`의 응답 본문(서버 HTML + 인라인 RSC 페이로드)에 `김하늘` · `01048217730` · `010-4821-7730`이 없다 · 노출을 켜면 같은 요청 본문에 `김하늘`이 있다(대조). **두 응답 모두 당첨자 칸을 보기 전에 `response.status()`가 200이고 본문에 그 행사 이름이 있음을 먼저 단언한다(codex-final3-B 3)** — 404 · 500 · 권한 오류 화면에도 당첨자 이름은 없으므로, 이 두 단언 없이는 오류 화면이 「숨김 성공」으로 통과한다(행사 이름은 (f-0)에서 켠 `cert_event.value`로 보인다).
  - (g) **기능 꺼짐(C1):** `withCertFeatureOff` 범위 안에서 `/certs/events`가 404 화면 · 범위를 나오면 다시 보인다.
  - (h) **직접 POST(C1):** 기능이 켜진 상태에서 실제 `createCertEventAction` 요청 하나를 `page.waitForRequest`로 잡아(`Next-Action` 헤더와 본문 저장) → `withCertFeatureOff` 범위 안에서 본문의 행사 이름을 새 고유 이름으로, `requestId`를 새 `randomUUID()`로 바꿔(E3-22 — 잡은 요청의 키를 그대로 쓰면 이미 만든 행사를 돌려받아 판정이 흐려진다) `page.request.post` → 그 이름의 행사가 DB에 없다 → 범위를 나와 같은 본문을 **두 번** 보내면 그 이름의 행사가 DB에 정확히 하나다(요청 모양이 맞았음과 같은 `requestId` 재전송이 행사를 하나만 만든다는 것을 함께 증명, 만든 행사는 자기 것이라 남겨도 된다).
  - (i) 「입력 버리기」(E3-01): 한 칸 적고 「취소 Esc」 → `getByRole("dialog", {name: "입력 버리기"})`(Phase 4 `ConfirmDialog`)의 부제 `행사 만들기 · 1칸` · 포커스가 1차 → 「입력 버리기」 → 목록.
  - (j) **응답 끊김 뒤 다시 누르기(E3-22):** 만들기 폼을 채운 뒤 `page.route`로 첫 `createCertEventAction` 요청만 서버에 보내고(`route.fetch()`) 응답은 버린다(`route.abort()`) → 합계 행 저장 결과 자리에 `만들었는지 확인하지 못했습니다 · 다시 눌러 주세요`(UI-SPEC Copywriting 「I2 만들기 실패」 — D-2) · 편집 값 그대로 · 1차 켜짐 → 경로를 풀고 1차를 다시 누르면 상세로 이동하고, 그 행사 이름의 행사가 DB에 정확히 하나다(같은 `requestId`가 첫 요청이 만든 행사를 돌려받았다).
  - (k) **1024 미만 만들기 없음(D-10):** `page.setViewportSize({width: 1000, height: 800})`로 `/certs/events?new=1` → 폼(`#cert-event-new`)과 1차 「행사 만들기」가 없고 목록이 선다 · (f-0)의 임시 사용자 컨텍스트(자기 행사 0건)에서 같은 폭이면 EMPTY `확인증 행사가 없습니다`만 있고 3차 「행사 만들기」 링크가 없다 · 1280 폭으로 되돌리면 1차가 다시 선다.
  - (l) **당첨일 초기값 · 첫 막힘(D-11 · D-7 · X-4):** 「행사 만들기」를 열자마자 `당첨일` 칸 값이 오늘의 KST 날짜(`new Intl.DateTimeFormat("sv-SE", {timeZone: "Asia/Seoul"}).format(new Date())`)이고, 1차가 `toBeDisabled()` · 이유 글이 정확히 `행사 이름 비어 있음 · 행사 이름 적기`이고 그 요소의 `color`가 `rgb(155, 28, 28)`(`--danger` `#9B1C1C` — 내부 폼은 `block`, 반영 검사 r1 B-1 · `toHaveCSS`)다 · 당첨일을 건드리지 않고 「취소 Esc」 → 확인 없이 곧바로 목록(N = 0).

⑥ 커밋 `test(04.3): failing e2e for event create and detail` → `feat(04.3): certificate event detail with qr`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>error가 한 줄이라도 나오고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-events.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이거나 종료 코드가 0이 아니다</fails_when>
    <automated>test "$(grep -rnE '#[0-9a-fA-F]{3,8}\b' 'app/(app)/certs' --include=*.css | wc -l)" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 확인증 화면 CSS에 색 리터럴이 있다(토큰만 써야 한다)</fails_when>
    <automated>test -f 'app/(app)/certs/events/loading.tsx' &amp;&amp; test -f 'app/(app)/certs/events/[id]/loading.tsx' &amp;&amp; grep -q 'assertCertFeatureEnabled' 'app/(app)/certs/events/page.tsx' &amp;&amp; grep -q 'assertCertFeatureEnabled' 'app/(app)/certs/events/[id]/page.tsx' &amp;&amp; grep -q 'await assertCertFeatureEnabled(' 'app/(app)/certs/events/actions.ts' &amp;&amp; test "$(grep -cE 'cert\.(enabled|contact_phone)' test/e2e/cert-events.spec.ts)" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 로딩 경계가 없거나, 두 페이지·만들기 액션 중 기능 게이트를 부르지 않는 곳이 있거나(C1), E2E가 전역 설정을 직접 바꾼다(C4)</fails_when>
  </verify>
  <acceptance_criteria>
    - 닫힌 행사의 상세 HTML에 QR SVG와 링크 문자열이 없음을 E2E 또는 통합 테스트가 단언한다(Task 2 통합의 DTO 키 부재 단언으로 충족 가능)
    - QR에 접근 이름 `{행사 이름} 확인증 QR`이 있다
    - E2E가 붙여넣기 → 구별 표시 오류(모양 중복 · `7-7-3` · `김 하늘`) → 오류가 남은 채 1차 누름(요청 0개 · 첫 오류 셀 포커스 — SYSTEM §7-3 DR-5, 반영 검사 r1 B-4) → 편집 입력 힌트 연결 → 고침 → 만들기 → 상세 QR → 목록의 자기 행사 `0/3` → 노출 끔 HTML 비노출(대조 포함) → 기능 꺼짐 404 → 직접 POST 거부(+ 같은 본문 두 번 → 행사 하나) → 입력 버리기(`ConfirmDialog`) → 응답 끊김 뒤 다시 누르기 → 행사 하나를 단언하고 녹색이다(E3-01 · E3-22)
    - E2E (l)이 첫 막힘 이유 `행사 이름 비어 있음 · 행사 이름 적기`를 정확 일치로, 그 색을 `toHaveCSS("color", "rgb(155, 28, 28)")`(`--danger` — 내부 폼 `block`)로 단언한다(반영 검사 r1 B-1)
    - E2E는 공유 `role-pm`의 권한·노출을 바꾸지 않는다(`grep -v '^\s*//' test/e2e/cert-events.spec.ts | grep -c 'role-pm'`가 0) — 임시 계급(`insertRole` + `setPermissionCell`)과 임시 사용자를 쓰고 `afterAll`에서 계급을 보관한다 · (f)의 행사는 domain `createEvent`(임시 계급 viewer) 한 경로로만 만든다
    - (codex-final3-B 3) 임시 계급에 `cert_event.value` 노출을 켜고(`grep -v '^\s*//' test/e2e/cert-events.spec.ts | grep -c 'cert_event.value'`가 1 이상), (f)의 끔 · 켬 두 응답 모두 HTTP 200과 행사 이름을 당첨자 칸 단언보다 먼저 단언한다
    - E2E 스펙의 EMPTY 단언은 임시 사용자 컨텍스트(범위상 자기 행사 0건)에서만 하고, 시스템 관리자 목록이 비었다고 단언하지 않는다 · 전역 설정 키를 직접 쓰지 않는다(`grep -cE "cert\\.(enabled|contact_phone)" test/e2e/cert-events.spec.ts`가 0)
    - `app/(app)/certs/events/[id]/loading.tsx`가 있고 `[id]/page.tsx`가 `assertCertFeatureEnabled`를 부른다
    - `app/(app)/certs/` CSS에 색 리터럴이 0건이다
  </acceptance_criteria>
  <done>담당자가 행사를 만든 직후 상세에서 QR과 링크를 보고 복사할 수 있고, 당첨자별 제출 상태가 보인다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 담당자 브라우저 → 만들기 액션 | 붙여 넣은 명단(이름 · 전화 · 경품 · 수량 · 전달 · 구별 표시)이 신뢰할 수 없는 입력이다 |
| 내부 화면 → 외부 목록 | 행사 이름·경품명·구별 표시는 공유 링크를 가진 누구에게나 보인다 |
| npm 레지스트리 → 빌드 | 새 의존성 `qrcode` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-SC | Tampering | `qrcode` · `@types/qrcode` 설치 | high | mitigate | 사용자 승인(2026-09-24) 패키지명 그대로만 깐다. 설치 전 `pnpm view`로 저장소 주소(`soldair/node-qrcode` · DefinitelyTyped)를 대조하고 다르면 멈춘다, 정확한 버전 고정(`--save-exact`) + lockfile 무결성 해시 커밋. RESEARCH Audit의 SUS 판정(다운로드 수 확인 불가 — 도구 한계)에 대한 사람 확인 체크포인트는 오케스트레이터 지시로 두지 않는다 — 계획 게이트·설계 `/cso`가 이 결정을 볼 수 있게 SUMMARY에 대조 결과를 남긴다 |
| T-04.3-23 | Information Disclosure | 구별 표시(외부 목록에 보임) | medium | mitigate | 10자 이하 · 숫자 3개 이상 이어짐 금지(공백·하이픈·점을 끼운 `7-7-3-0` 같은 우회도 이어진 것으로 판정) · 그 행사 당첨자 이름 포함 금지(양쪽 모두 NFC + 공백 제거 뒤 비교 — `김 하늘` 우회 차단)를 서버가 검사하고, 편집 표 위 상시 힌트(머리글과 편집 입력 모두 `aria-describedby`)와 `수령자 화면` 미리 보기 열이 공개 모양을 저장 전에 보인다. 남는 위험(별명 · 이름 일부 · 소속)은 A11 그대로 설계 `/cso`가 본다 |
| T-04.3-81 | Information Disclosure | 행사 상세 DTO · 액션 반환 · RSC 페이로드 | high | mitigate | 행을 돌려주는 모든 반환 경로가 `project()`를 거치고(만들기 액션은 행 없이 `{eventId, link}`만 돌려준다), 노출(`cert_winner.value`)을 끈 계급으로 실제 domain 반환값(통합)과 실제 상세 페이지 HTML/RSC 본문(E2E)에 이름·전화가 없음을 대조 테스트로 단언한다 — 등록 여부만 보는 누수 스캔에 기대지 않는다(Codex #22). E2E 대조는 임시 계급에 `cert_event.value`를 켜고 두 응답 모두 HTTP 200 + 행사 이름을 먼저 단언해, 오류 화면이 비노출 성공으로 오인되지 않게 한다(codex-final3-B 3) |
| T-04.3-19 | Information Disclosure | 행사 목록·상세 범위 | medium | mitigate | `certs.submissions` view 보유자만 전부, 나머지는 만든 사람만. 목록과 상세가 같은 범위 서술자를 쓰고 남의 상세는 404(통합 테스트) |
| T-04.3-12a | Information Disclosure | 상세의 링크·QR 재표시 | medium | mitigate | 토큰은 `encrypt()` 암호문에서만 복원하고 접수 중일 때만 DTO에 싣는다. 닫힌 행사는 링크·QR 키 자체가 없다(닫힌 QR이 다시 퍼지지 않게) |
| T-04.3-31 | Tampering | 인라인 SVG | low | mitigate | SVG는 서버가 `qrcode`로 서버 토큰 링크에서만 만든다 — 사용자 입력이 SVG 문자열에 섞이지 않는다 |
| T-04.3-26 | Information Disclosure | 편집 표 브라우저 보관 | medium | mitigate | `useDirtyStorage`·브라우저 저장소 API 0건을 grep 게이트로 고정(개정 ⑪) |
| T-04.3-16 | Elevation of Privilege | 기능 꺼진 내부 라우트 · 액션 직접 호출 | high | mitigate | 두 페이지와 `createCertEventAction`이 첫 줄에서 `assertCertFeatureEnabled()`(env 게이트 + 설정, C1)를 부르고 domain도 다시 본다. 페이지 404(E2E)와 별도로, 페이지를 거치지 않은 직접 POST가 행사를 만들지 못함을 E2E가 증명한다 |
| T-04.3-74 | Tampering | 만들기 재전송 · 동시 요청(행사 · QR 이중 생성) | medium | mitigate | 폼이 한 번 만든 `requestId`를 성공까지 다시 보내고, `cert_events.create_request_id` UNIQUE가 같은 키의 두 번째 행사를 막는다 — 동시 요청의 unique 위반은 트랜잭션 밖에서 다시 찾아 같은 행사를 돌려준다(E3-22). 찾기는 `created_by = viewer.id`로 묶여 남의 키로 남의 링크를 받지 못한다. 통합(순차 · 동시)과 E2E(응답 끊김 뒤 다시 누르기 · 같은 본문 두 번)가 행사 하나를 단언한다 |
| T-04.3-75 | Denial of Service | 한 요청의 당첨자 수 | low | mitigate | zod 당첨자 배열 `.max(500)`(04.3-10과 같은 상한, E3-22) + `authedActionClient`의 `checkPayloadSize`. 501줄 거부를 통합 테스트가 단언한다 |
</threat_model>

<verification>
- `package.json` 버전 고정 검사 녹색 · `@google-cloud/storage` 0건
- 단위 `winner-rules`(우회 입력 · 길이 경계 · `await renderQrSvg` 포함) · `create-form-rules`(`createSubmitOutcome` 포함) · `leak-scan-coverage` 녹색
- 통합 `cert-events`(노출 끔 실제 반환값 · 길이 셀 오류 · 만들기 멱등 · 500줄 상한 포함) · `leak-scan` 녹색
- E2E `cert-events.spec.ts`(힌트 연결 · HTML 비노출 · 직접 POST · `ConfirmDialog` 입력 버리기 · 응답 끊김 뒤 다시 누르기 포함) 녹색
- 통합 · E2E는 한 번에 하나 — 같은 웨이브의 04.3-03 · 04.3-05와 동시에 돌리지 않는다(E3-33)
- `pnpm lint && pnpm typecheck && pnpm build` 녹색
- 확인증 화면의 브라우저 저장소 사용 0건 · CSS 색 리터럴 0건 · 로딩 경계 둘 존재 · 기능 게이트 셋(두 페이지 + 액션)
</verification>

<success_criteria>
- 담당자가 행사 하나에 당첨자 명단을 붙여 넣어 한 번에 만들고 QR을 받는다
- 외부 목록에서 구별되지 않는 줄은 저장 단계에서 막힌다
- 목록·상세가 보는 사람 범위로 걸러지고 플래그 뒤에 있다
- 이름 · 경품명이 길면 어느 칸인지 셀 오류로 보이고(E3-21), 만들기가 서버 거부 · 연결 실패로 끝나면 값이 남은 채 한 줄이 서며 다시 눌러도 행사(QR)가 하나만 생긴다(E3-22)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 의존성 | `qrcode`(dependencies, 정확한 버전) · `@types/qrcode`(devDependencies) |
| 메뉴 · 정보 항목 | `certs.submissions` · `cert_event.value` · `cert_winner.value` |
| 함수 | `renderQrSvg`(async — E3-25) · `validateWinnerRows`(길이 코드 둘 포함) · `listEvents` · `getEventDetail` · `createEvent`(셀 오류 · `requestId` 멱등) · `findEventByCreateRequest`(리포지토리) · `createSubmitOutcome`(화면 순수) |
| 스키마 칸 사용 | `cert_events.create_request_id`(04.3-02가 만든 칸 — 이 플랜은 스키마 · 마이그레이션을 고치지 않는다, E3-22 · E3-31) |
| DTO | `CertEventListDto` · `CertEventDetailDto` · `CertWinnerDto` |
| 액션 | `createCertEventAction`(`certs.events` write) |
| 라우트 | `/certs/events`(+ `?new=1`) · `/certs/events/[id]`(읽기) · 각 `loading.tsx` 뼈대 |
| 테스트 | `test/unit/certs/winner-rules.test.ts` · `test/integration/cert-events.test.ts` · `test/e2e/cert-events.spec.ts` |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(/plan-eng-review E3 — 반영 지시 L387–L455@953e599, 04.3-04 항목 L410–L414). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---|---|---|
| E3-01 Phase 4 부품 기준 · 입력 버리기 확인 중복 구현 — L392 · L394 · L414@953e599 | P1 | frontmatter `execution_gate` · objective 「실행 조건」 · Task 1 `<precondition>`. 「입력 버리기」를 Phase 4 `ui/confirm-dialog`로(truth · Task 3 read_first · (h) · verify · acceptance · E2E (i) · key_link) — 옛 「main에 생기면 옮기는 후속 항목」 두 곳 삭제. `Table.tsx:228` 줄 번호를 Phase 4 판 `<th` grep으로 |
| E3-21 이름 40자 · 경품명 80자 길이 코드 없음 — L411 | P2 | truth · Task 1 ③ `nameTooLong` · `prizeTooLong`(뗀 길이) + 경계 40/41 · 80/81 단위 · Task 2 ③ (a) zod에서 줄 내용 규칙을 빼 셀 오류로만 + 통합 케이스 · Task 3 `maxLength` · `pinCellErrors` 11종 |
| E3-22 만들기 serverError · 연결 실패 갈래 없음 · 재전송 이중 행사 — L413 | P2 | truth · Task 2 `requestId`(선택 — 헬퍼 불변) · `findEventByCreateRequest` · unique 위반 뒤 트랜잭션 밖 다시 찾기 · zod `.max(500)` · 통합(순차 · 동시 · 501줄) · Task 3 `createSubmitOutcome` 단위 · (f) 실패 줄 · 값 유지 · 1차 다시 켬 · verify · E2E (h) 같은 본문 두 번 · (j) 응답 끊김 뒤 다시 누르기 · T-04.3-74 · 75 · key_link |
| E3-25 `renderQrSvg` 비동기 — L412 | P3 | Task 1 ② `async renderQrSvg(link): Promise<string>` · behavior · acceptance · Task 2 ③ (e) `await` · artifacts |
| E3-31 스키마 변경 규칙 | P3 | `create_request_id`는 04.3-02가 미리 만든 칸 — Task 1 precondition · Task 2 acceptance 「이 플랜은 `db/schema/` · `db/migrations/`를 고치지 않는다」 |
| E3-33 통합 · E2E 직렬 | P2 | `execution_gate.serial_tests` · objective · verification 한 줄 |

### Review Feedback Deferred

| Concern | Reason |
|---|---|
| E3-03 | deferred — user check pending (default kept) |
| E3-04 | deferred — user check pending (default kept) |
| E3-21 길이 셀 오류 문장 · E3-22 만들기 실패 줄 문장 | UI-SPEC 승인 계약 — `/plan-design-review`에 넘김(L454). 이 플랜은 코드 · 갈래만 만들고, Task 3 `<precondition>`이 UI-SPEC I2에 문장이 없으면 멈춘다(문장을 지어내지 않는다) |

### Round 2 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3·D-7·D-10). 문장 정본은 UI-SPEC이고 이 플랜은 그 문장을 옮겨 쓴다.

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---|---|---|
| D-1 I2 길이 셀 오류 문장 없음 — L33@1c3a27b | 막는 문제 | UI-SPEC Copywriting 「I2 셀 오류」에 `40자를 넘습니다 · 40자 안으로 줄여 주세요` · `80자를 넘습니다 · 80자 안으로 줄여 주세요` — Task 1 ③ 문장 인용 · `pinCellErrors` 11종. **Supersedes Round 1 Deferred: E3-21 길이 셀 오류 문장** — 이제 UI-SPEC에 있다 |
| D-2 I2 만들기 실패 줄 없음 — L40@1c3a27b | 막는 문제 | UI-SPEC 새 행 「I2 만들기 실패」 `만들었는지 확인하지 못했습니다 · 다시 눌러 주세요`(`--danger` · 합계 행 `aria-live="polite"`) — E3-22 truth · Task 3 (f) · E2E (j) 문장 단언. **Supersedes Round 1 Deferred: E3-22 만들기 실패 줄 문장** — 이제 UI-SPEC에 있다 |
| D-6 맥 커맨드 글리프 · 힌트 줄 저장 중복 — L73@1c3a27b | 경고 | truth · Task 3 (b) 힌트 줄 `이동 Tab ↑↓←→ · 복사 Ctrl+C · 붙여넣기 Ctrl+V · 취소 Esc · 새 줄 Ctrl+Enter` · EMPTY `당첨자가 없습니다 · 첫 줄 만들기 Ctrl+Enter` · 새 verify가 행사 화면 코드의 글리프 0건을 본다 |
| D-7 「못 한다」 접두 문장(§8 규칙 5) — L82@1c3a27b | 경고 | Task 3 behavior `createBlockReason` → `{text, tone}`: `수령자 문의 전화 없음 · 설정 확인증 탭에서 채움` / `… · 등록은 경영관리`(block) · `{칸} 비어 있음 · {칸} 적기`(info) · `당첨자 없음 · 첫 줄 만들기`(info) · `오류 N칸 · 고친 뒤 다시`(block) — 새 verify가 접두 문장 0건을 본다 |
| D-10 700~1023 폭 편집 표(DR-36) — L105@1c3a27b | 경고 | 폭 판정은 Phase 4 `useEditableWidth()`(`ui/table/use-editable-width.ts`, 04-49) 하나 — Task 3 ④ · 새 조각 `create-entry.tsx` · precondition(훅이 main에 없으면 멈춤) · 1024 미만 truth · E2E (k) 1000px/1280px · verify가 CSS 1024 쿼리 0건. 보고서의 「폰과 같은 CSS 방식」 대신 이 훅을 쓴다(두 번째 폭 판정을 만들지 않기 위해 — 코디네이터 지시, 반환 보고에 충돌로 적음) |
| D-11 · X-1 당첨일 기본값 — L110@1c3a27b · L182@1c3a27b | 경고 · 막는 문제 | truth · Task 3 (a) 초기값 오늘(KST, 서버가 넘김) · 자리표시자 `2026-09-18` 유지(D-11) · 첫 막힘 `행사 이름 비어 있음 · 행사 이름 적기` · `countChangedCells` 날짜 경우 · E2E (l) |
| X-3 §8-5 안심 꼬리 — L190@1c3a27b | 막는 문제 | 이 플랜 문장에 안심 꼬리 없음 — 만들기 실패 줄은 「다시 눌러 주세요」에서 끝난다(UI-SPEC ①-m: 외부 E4만 예외) |
| X-4 정상 상태를 오류 색으로(`reasonTone`) — L194@1c3a27b | 막는 문제 | truth(UI-SPEC Color 「막힘 이유의 색」 규칙) · Task 3 (e) `tone` → `reasonTone` · acceptance 단위 단언 · verify `reasonTone` grep |
| X-8 EMPTY 1차 · 3차 중복 · 「필터 줄」 — L212@1c3a27b | 참고 | Task 3 ① 「표 위 행동 줄(§6-1 — 필터 없음)」 · ② EMPTY 3차는 §7-7대로 두되 1024 이상에서만 |
| X-12 QR 현장 게시 — L228@1c3a27b | 참고 | 범위 그대로 — 이 플랜은 이미 인라인 SVG(벡터)라 크게 인쇄해도 깨지지 않는다. UI-SPEC A8이 그 사실과 「포스터 인쇄는 Phase 11」을 적는다. 플랜 변경 없음 |

### Review Feedback Deferred

| Concern | Reason |
|---|---|
| X-6 E3 상태기계 표 하나로 — L204@1c3a27b | 이 플랜 범위 밖(E3는 04.3-03 · 04.3-06). UI-SPEC 「디자인 검토 반영」 절에 보류 이유 기록 |
| E3-03 · E3-04 | 사용자 결정 그대로(서명 이미지 GCS · 12 · 08 범위 유지) — 이번 라운드에서 바꾸지 않는다 |

### Round 3 — 66a98e1

> 출처: 디자인 반영 검사 r1 — `design-apply-checker-r1.md`(Opus 플랜 체커: 막는 문제 0 · 경고 3) · `design-apply-cross-r1.md`(교차 검토 — B-1~B-4 · N-1~N-14). **교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요.** 앞 라운드 표는 고치지 않는다(append-only). 문장 정본은 UI-SPEC이다.

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---|---|---|
| 체커 W2 — `useEditableWidth` 출처(04-30이 만든다는 지적) | 경고 | 증거로 04-49 유지: Phase 4 `04-49-PLAN.md` frontmatter `files_modified`(`ui/table/use-editable-width.ts`) · artifacts(`exports: ["useEditableWidth"]`) · Task 2 ①이 만든다. `04-30-PLAN.md`는 엔지 r2 E2-02 분할로 이 파일을 04-49에 넘겼다(04-30 「모듈(04-49로 옮김)」 표 · Output 줄 「폭 규칙 … 04-49」 · frontmatter에 이 파일 없음). Task 3 read_first에 근거와 04-42(리저브 대장 — 같은 훅 사용)를 적었다 |
| 체커 W3 — 폭 훅이 없을 때 멈춤 조건 | 경고 | Task 1 · Task 3 `<precondition>`에 `test -f ui/table/use-editable-width.ts && grep -q 'useEditableWidth' …` — 아니면 멈추고 보고(폭 판정을 새로 만들지 않는다) |
| 교차 B-1 — I2 내부 폼 막힘 이유를 `info`로 넓힘(SYSTEM §7-15 · §7-1 U-4와 어긋남, 화면 하나만의 예외) | 막는 문제 | truth(`reasonTone` 줄) · Task 3 behavior `createBlockReason` 전부 `block`(`info` 결과 없음) · (e) · acceptance · E2E (l) `toHaveCSS("color", "rgb(155, 28, 28)")`. UI-SPEC Color 「막힘 이유의 색」이 `info`를 `바뀐 칸 없음`과 외부 E3 · E4(①-m ②)로 좁혔다 |
| 교차 B-4 — I2 「오류 N칸 · 고친 뒤 다시」가 1차를 끔(SYSTEM §7-3 DR-5와 어긋남) | 막는 문제 | 새 truth(표 오류 셀은 1차를 막지 않는다) · behavior `createBlockReason` 입력에서 오류 셀 수 제거 · (e) 누르면 서버를 부르지 않고 첫 오류로(폼 칸 → 표 `firstIssueSignal`, Phase 4 04-47) · Task 3 precondition `firstIssueSignal` 확인 · verify grep · E2E (b) 요청 0개 · 첫 오류 셀 포커스 |
| 교차 N-1 — I2에 클라이언트 500줄 상한 없음 · 확정 거부에 「다시 눌러」 | 참고 | 1차 막힘 `당첨자 {N}명 · 500명까지 줄이기`(`block`, 서버 zod `.max(500)`과 같은 수) · (f) 보내기 전 `checkPayloadSize`(서버 미들웨어와 같은 함수) — 넘으면 보내지 않고 그 `reason` 문장 · 만들기 실패 줄은 결과 불명(연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가)에만 · truth · behavior 501/500 · verify grep |
| 교차 N-4 — 편집 중 폭이 1024 아래로 줄 때 | 참고 | ④에 한 구절: 폼 부품이 `null`을 돌려 떼지 않는다 — 입력 상태가 남아 1024 이상으로 돌아오면 다시 선다(만들기 없음 D-10 결정은 그대로) |
| 교차 N-7 — `수 없음` grep이 `[id]/`까지 훑음 | 참고 | Task 3 verify를 이 태스크가 만드는 I1 · I2 파일 여섯 개로 좁혔다(fails_when 갱신) · acceptance 같은 범위 |

### Review Feedback Deferred

| Concern | Reason |
|---|---|
| 교차 N-12 — QR 이미지 저장 길이 Phase 11 전까지 없음 | 범위 결정(A8 · X-12)은 그대로. 사용자에게 알릴 한 줄은 오케스트레이터 보고로 넘긴다 — 플랜 변경 없음 |
| `createSubmitOutcome`의 `validationErrors` → `failed` | 화면이 보내는 요청에서 zod가 거부할 수 있는 것은 500줄 상한 하나이고 이제 보내기 전에 막으므로 화면에서 닿지 않는다. 갈래 모양은 그대로 둔다 |
| E3-03 · E3-04 | 사용자 결정 그대로 — 이번 라운드에서 바꾸지 않는다 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-04-SUMMARY.md` when done
</output>
