---
phase: 04.3-qr-certificate-intake
plan: 06
type: execute
wave: 4
depends_on: ["04.3-03", "04.3-04"]
files_modified:
  - domain/certs/intake.ts
  - domain/certs/signature-png.ts
  - domain/certs/signature-ink.ts
  - repositories/cert-submissions.ts
  - repositories/cert-events.ts
  - app/c/[token]/actions.ts
  - app/c/[token]/intake-flow.tsx
  - app/c/[token]/flow-rules.ts
  - app/c/[token]/signature-pad.tsx
  - app/c/[token]/signature-geometry.ts
  - app/c/[token]/rrn-fields.tsx
  - app/c/[token]/rrn-paste.ts
  - app/c/[token]/consent-block.tsx
  - domain/certs/consent.ts
  - app/c/[token]/intake.module.css
  - test/unit/certs/signature-geometry.test.ts
  - test/unit/certs/signature-png.test.ts
  - test/unit/certs/rrn-paste.test.ts
  - test/unit/certs/flow-rules.test.ts
  - test/integration/cert-submit.test.ts
  - test/integration/cert-intake.test.ts
  - test/e2e/mobile-cert-submit.spec.ts
  - test/e2e/helpers/cert.ts   # seedSubmittedCert의 submitCertificate 호출 줄만 — Task 1 ⑤-b가 입력에 winnerVersion을 싣는다(04.3-02 C4 규칙, X1)
autonomous: true
requirements: [CERT-01, CERT-02]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 180000
  raw_tokens: 180000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "수령자가 E4에서 이름 · 주민등록번호 · (택배면 주소) · 연락처 · 동의 · 서명을 채우고 「확인증 제출」을 누르면 E5 `제출되었습니다 · 다시 제출할 수 없습니다`가 서고 그 사람 자리만 닫힌다 — 같은 링크의 다른 당첨자는 계속 제출할 수 있다(D-1102 · ROADMAP 04.3 기준 3)"
    - "CERT-01 × adjacency: 마지막 미제출 자리가 제출되는 트랜잭션 안에서 링크가 `all_submitted`로 닫힌다 — 그 뒤 누가 링크를 열어도 E6-b `당첨자 모두 제출했습니다 · …`"
    - "CERT-01 × idempotency(제출): 결과 불명 뒤 같은 제출 키로 다시 보내면 서버는 첫 저장 결과(E5)를 돌려주고 두 번 저장하지 않는다 — 첫 저장이 링크를 닫았어도 재전송은 E6-b가 아니라 E5를 받는다. 칸 오류 · 확인 시간 지남 · 링크 닫힘 같은 상태를 바꾸지 않은 판정은 키에 묶지 않고 다시 판정한다(UI-SPEC E1 「멱등 키」)"
    - "제출 여부 비노출(Codex #6): 증표 확인 **전에** 제출 여부가 드러나는 응답은 「저장된 제출과 **같은 제출 키**의 재생(E5)」 하나뿐이다 — 키 해시가 다르면 제출된 자리와 제출되지 않은 자리가 같은 순서(닫힘 → 증표 확인)를 밟아, 증표가 없거나 틀린 요청은 두 자리 모두 같은 `expiredProof`(같은 키 집합)를 받는다. E6-a(`alreadySubmitted`)는 유효한 증표를 가진 요청에만 나간다"
    - "잠근 뒤 재판정(Codex #7): 저장 트랜잭션은 행사 행 → 자리 행 순서로 `FOR UPDATE`를 잡은 **뒤** 닫힘(`closed_at` · 기한) · 제출 여부 · 증표 해시·만료 · 자리 `version`(확인 응답이 준 값과 같은가 — 담당자가 확인 뒤 경품·전달을 고쳤으면 다름) · 동의 버전·보존 기간을 잠긴 값으로 다시 판정하고, 하나라도 달라졌으면 저장하지 않는다(닫힘 → E6-b · 증표·`version`·동의 불일치 → `expiredProof`). 잠그기 전에 읽은 값으로 저장 여부를 정하지 않는다"
    - "CERT-01 × concurrency: 같은 자리에 서로 다른 키의 제출 둘이 동시에 오면 하나만 저장되고 다른 하나는 E6-a(`이미 제출하셨습니다`)를 받으며, 진 쪽이 올린 서명 객체는 지워진다. 마지막 두 자리가 동시에 제출돼도 링크는 정확히 한 번 닫힌다"
    - "C3 업로드 의도: 서명 객체를 올리기 **전에** `insertSignatureUploadIntent(SYSTEM_VIEWER, key)`가 커밋되고, 저장 트랜잭션 안에서 `deleteSignatureUploadIntent(SYSTEM_VIEWER, key, tx)`로 지워진다. 저장하지 않기로 판정되면(트랜잭션은 확정적으로 끝났다) `store.delete`를 부르고 그 삭제가 성공했을 때만 의도 줄을 지운다 — 삭제가 실패하면 의도 줄이 남아 04.3-12가 24시간 뒤 객체를 지운다. 트랜잭션이 예외로 끝나면 의도 줄은 요청이 지우지 않는다(아래 커밋 결과 불명 줄 — 롤백으로 되살아난 줄은 04.3-12가 치운다). 고아 객체가 파기 경로 밖에 남지 않는다"
    - "커밋 결과 불명(codex-final3-B 1): 저장 트랜잭션이 예외로 끝나도 서명 객체를 곧바로 지우지 않는다 — `withTransaction`(`lib/db-transaction.ts:9`)이 그대로 부르는 Drizzle은 `commit` 응답 실패도 예외로 던지므로(`node_modules/drizzle-orm/node-postgres/session.js:187`) 커밋이 이미 들어갔을 수 있다. 행사 → 자리 순서로 잠근 짧은 트랜잭션에서 이 요청의 객체 키를 `signature_key`로 가진 제출 줄을 찾아 — 있으면 객체를 두고 그 줄로 `saved`(E5)를 돌려주고, 없으면 객체를 지운 뒤 예외를 다시 던지며(의도 줄은 04.3-12에 맡긴다), 조회마저 실패하면 아무것도 지우지 않고 예외를 다시 던진다. 확인증이 가리키는 서명 객체가 보상 삭제로 사라지는 일이 없다"
    - "CERT-01 × empty — 잉크 측정 하나(checker-B W5): 「서명 있음」은 클라이언트와 서버가 **같은 함수 · 같은 상수**로 판정한다 — 제출할 1040×400 PNG(논리 좌표 × 2, 굵기 6)에서 알파가 0이 아닌 픽셀 수(`countInkPixels`, `domain/certs/signature-ink.ts`)가 `SIGNATURE_MIN_INK_PIXELS = 288`(= 겹치지 않은 논리 24 선 × 배율 2 × 굵기 6) 이상. 클라이언트는 획을 끝낼 때마다 자기 내보내기 이미지를 세고, 모자라면 제출 줄 이유가 계속 `서명을 해 주세요`다. 서버는 받은 PNG를 풀어 같은 함수로 세고 모자라면 칸 오류 `signature`(Codex #14) — 같은 자리를 왕복한 낙서처럼 획 길이는 길어도 잉크가 모자란 서명을 화면이 받아 놓고 서버가 거절하는 어긋남이 없다. 1040×400 · 8비트 RGBA · 비인터레이스가 아니거나 PNG가 아니거나 청크 CRC가 하나라도 틀리거나 마지막 청크가 IEND가 아니면(인쇄 04.3-11의 `decode()`가 실패할 본문) 역시 `signature`이고 저장소에 올리기 전에 거부된다"
    - "서명 크기 상한(Codex #27): `SIGNATURE_MAX_PNG_BYTES = 184,320`(180 KiB) — base64로 245,760바이트이고 나머지 칸을 zod 최대 길이·3바이트 글자로 채워도 액션 본문이 `MAX_ACTION_PAYLOAD_BYTES`(262,144) 아래다(경계 테스트). 서명 문자열의 zod 최대 길이가 이 상한의 base64 길이라, 넘으면 본문 한도 오류가 아니라 칸 오류 `signature`가 된다"
    - "C1: `submitCertificateAction` 본문 첫 문장이 `await assertCertFeatureEnabled()`(`lib/certs/feature-guard.ts`)이고, 기능이 꺼지면 페이지를 거치지 않은 직접 POST도 저장·업로드를 일으키지 않는다(E2E)"
    - "동의 묶음(04.3-02): E4의 `{보존 기간}`은 확인 응답의 `consent.retentionYears`로 그리고, 제출은 `consentVersion` · `retentionYears`를 그대로 돌려보낸다. 주민등록번호 검증번호 불일치는 `rrnRecheck`로 한 번 되묻고(주민등록번호 오류 줄), 값을 바꾸지 않고 다시 누르면 `rrnRecheckConfirmed: true`를 실어 저장된다 — 값을 바꾸면 그 표시는 풀린다. 표시는 되물음을 받은 주민등록번호 **값**에 묶여, 그 뒤 결과 불명이 끼어도 같은 값이면 유지된다 — 결과 불명 재전송은 같은 키 · 같은 본문(`rrnRecheckConfirmed: true` 포함)이라 서버가 다시 되묻지 않는다(codex-final3-B 2)"
    - "E4 제출 막힘 이유는 **빈 칸만** 나열하고(`이름 · 주민등록번호 · … · 서명을 채우면 제출할 수 있습니다`), 서명만 남으면 `서명을 해 주세요`다. 칸 오류가 오면 칸 아래 원인 · 다음 행동과 제출 줄 요약이 서고 포커스는 시각 순서상 첫 오류 칸으로 간다(UI Considerations partial·error E4) — domain `invalid`든 액션 스키마 거절(next-safe-action `validationErrors`)이든 같은 갈래라, 서명 길이 초과도 결과 불명이 아니라 서명 칸 오류다"
    - "제출 결과 불명(연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가)이면 제출 줄 `제출됐는지 확인하지 못했습니다 · 다시 눌러 주세요 · 적은 내용은 남아 있습니다`, 1차는 살아 있고 값은 그대로다(UI Considerations error E4)"
    - "E4 제출 중 경합: 그새 자리가 제출됨 → E6-a · 그새 링크가 닫힘 → E6-b(값 버림) · 확인 시간 지남 → E3 칸 아래 `확인 시간이 지났습니다 · 전화번호 뒤 4자리를 다시 적어 주세요`이고, 같은 자리의 확인을 다시 통과하면 적은 값(서명 획 포함)이 되살아난다. E2로 가거나 E6으로 가면 값은 버려진다(UI-SPEC E4 「값의 주인」)"
    - "주민등록번호 두 칸: 앞 칸 끝에서 쳐 넣어 6자리가 찰 때만 포커스가 뒤 칸으로 간다 · 앞 칸에 13자리(하이픈 있든 없든)를 붙여 넣으면 6 · 7로 나뉘어 채워지고 포커스는 뒤 칸 끝 · 그 밖의 길이는 앞 칸에 숫자만 남긴다. 두 칸은 `role=group`으로 묶이고 칸마다 접근 이름이 있다"
    - "E4 경품 블록 바로 아래 `경품이나 받는 방법이 다르면 제출하기 전에 PLANT8 경영관리 {문의 전화}에 전화해 주세요`(행사 사본 번호, `tel:` 3차)가 선다(UI Considerations error E4 경품)"
    - "「전문 보기」는 동의 블록 안 제자리 펼침이다 — 라벨이 「전문 접기」로 바뀌고 `aria-expanded`·`aria-controls`가 따라간다. 전문은 법무 확인 전 초안이고 금지어 둘(원천징수 · 지급명세서)이 없다(개정 ①-f · ①-g)"
    - "서명 캔버스는 고정 논리 영역 520×200을 배율 하나(`폭 ÷ 520`)로 보여 주고 입력을 영역 안으로 잘라 넣어, 줄이고 긋고 넓혀도 잘리는 획이 없다. 제출 PNG는 논리 영역에서 1040×400 바탕 투명으로 다시 그린다(UI Considerations interaction E4 서명 크기 변경)"
    - "서명은 키보드로도 그린다 — `Space`/`Enter` 펜 대기·떼기 · 방향키 논리 8(`Shift` 24, 두 키 = 대각선) · `Backspace` 마지막 획 지우기 · 펜 십자 · 키보드 포커스 때 보이는 키 안내 줄 · `aria-live` 상태 알림(펜 댐·뗌·가장자리·지움·서명이 들어갔습니다) · `role=application` · 접근 이름 `서명 칸`/`서명 칸 · 서명함`(UI Considerations interaction E4 키보드 그리기)"
    - "E5가 서면 메모리의 입력값·서명 획이 지워지고 포커스는 결과 블록 첫 줄, `document.title`은 `제출됨 · 기타소득 지급 확인`이다"
    - "제출 로그 원자성(최종 리뷰 B5): 행동 로그 `document_submit`은 제출을 저장하는 **같은 트랜잭션** 안에서 쓴다(커밋 뒤가 아니다) — 로그 INSERT가 실패하면 제출 전체가 롤백되고(제출 줄 · `submitted_at` · 자동 닫힘 없음 · 올린 객체는 제출 줄이 없음을 확인한 (g)가 지운다) 같은 키로 다시 보내면 새로 저장된다. 확인증은 저장됐는데 로그만 영구히 빠지는 일이 없다"
    - statement: "E4가 360px 폭과 200% 확대에서 주민등록번호 두 칸 · sticky 제출 줄 · 서명 칸이 가로 스크롤 없이 선다(UI Considerations overflow E4)"
      verification: backstop
    - statement: "iOS Safari · Android Chrome에서 서명 중 페이지가 스크롤되지 않고 획이 끊기지 않는다(UI Considerations interaction E4 서명 캔버스)"
      verification: backstop
    - statement: "E4 서명 완료 기준 다섯(키보드만으로 두 획 · 한 획 지우고 다시 긋기 · 알림과 이름 변화 듣기 · 제출 줄 이유에서 서명 빠짐 · 제출)과 VoiceOver·TalkBack 통과 제스처로 ①·③·⑤(UI Considerations interaction E4 서명 보조기술)"
      verification: backstop
  prohibitions:
    - statement: "제출값(주민등록번호 · 서명 획 포함)을 브라우저 저장소나 URL에 두어서는 안 된다 — 메모리에만 두고 E5·E6·E2 이동 때 지운다"
      status: resolved
      verification: test
    - statement: "한 자리가 두 번 저장되거나, 앞사람이 적은 주민등록번호·서명이 다른 자리의 제출로 넘어가서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "test/integration/cert-submit.test.ts"
      provides: "제출 재생 · 제출 여부 비노출 · 잠근 뒤 재판정 · 동시 제출 · 마지막 자리 자동 닫힘 · 업로드 의도 줄 · 커밋 결과 불명(객체 보존) · PNG·칸 검사"
      min_lines: 90
    - path: "domain/certs/signature-png.ts"
      provides: "서명 PNG 검사(순수) — 형식 · 청크 CRC · 끝 IEND · 크기 상한 · 압축 해제 상한 · 잉크 픽셀 하한"
      exports: ["inspectSignaturePng", "SIGNATURE_MAX_PNG_BYTES"]
    - path: "domain/certs/signature-ink.ts"
      provides: "잉크 측정 하나 — RGBA 바이트에서 알파가 0이 아닌 픽셀 수 · 하한 상수(import 없는 순수 모듈 — 서명 칸(클라이언트)과 PNG 검사(서버)가 같이 쓴다)"
      exports: ["countInkPixels", "SIGNATURE_MIN_INK_PIXELS"]
    - path: "test/unit/certs/signature-png.test.ts"
      provides: "빈 서명 · 점 · 잉크 288/287 경계 · 왕복 낙서 · `countInkPixels` · 형식 · CRC 틀림 · IEND 없음 · 180 KiB 경계 · 본문 한도 예산 · 압축 폭탄"
      min_lines: 50
    - path: "app/c/[token]/signature-geometry.ts"
      provides: "논리 영역 520×200 좌표 변환·잘라 넣기·키보드 이동·3×3 위치·1040×400 다시 그리기(순수) — 「서명 있음」은 여기서 재지 않는다(잉크 측정 하나, `signature-ink.ts`)"
      exports: ["toLogical", "moveCursor", "gridCellName"]
    - path: "app/c/[token]/rrn-paste.ts"
      provides: "13자리 붙여넣기 나누기 · 자동 이동 판정(순수)"
      exports: ["splitRrnPaste", "shouldAdvance"]
    - path: "test/e2e/mobile-cert-submit.spec.ts"
      provides: "375px 제출 · 키보드 서명 · 붙여넣기 · 결과 불명 재전송(주민등록번호 되물음 뒤 포함) · 자동 닫힘 E2E"
      min_lines: 60
  key_links:
    - from: "domain/certs/intake.ts"
      to: "repositories/cert-events.ts"
      via: "마지막 자리 제출이 같은 트랜잭션에서 행사를 all_submitted로 닫는다"
      pattern: "all_submitted"
    - from: "app/c/[token]/intake-flow.tsx"
      to: "app/c/[token]/actions.ts"
      via: "결과 불명 재전송은 같은 제출 키로 같은 액션을 부른다"
      pattern: "idemKey"
    - from: "app/c/[token]/signature-pad.tsx"
      to: "app/c/[token]/signature-geometry.ts"
      via: "포인터·키보드 입력 모두 같은 순수 좌표 함수를 거친다"
      pattern: "toLogical"
    - from: "domain/certs/intake.ts"
      to: "repositories/cert-submissions.ts"
      via: "업로드 전에 의도 줄을 커밋하고 저장 트랜잭션에서 지운다(C3)"
      pattern: "insertSignatureUploadIntent"
    - from: "domain/certs/intake.ts"
      to: "repositories/cert-winners.ts"
      via: "제출도 확인과 같은 순서(행사 → 자리)로 잠근다(04.3-03의 lockEventRow)"
      pattern: "lockEventRow"
    - from: "domain/certs/intake.ts"
      to: "domain/certs/signature-png.ts"
      via: "올리기 전에 빈 서명·형식·크기를 서버가 판정한다"
      pattern: "inspectSignaturePng"
---

<objective>
**As a** 경품 당첨자, **I want to** 폰에서 주민등록번호를 틀리지 않게 넣고 손가락이나 키보드로 서명한 뒤 한 번 눌러 제출하고, 연결이 끊겨도 다시 눌러 안전하게 끝내고 싶다, **so that** 확인증이 정확히 한 번 저장되고 내 자리만 닫혀 경품을 받을 수 있다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — Goal의 행위자(경품 당첨자)와 기준 3에서 이야기를 끌어냈다.

04.3-02 트레이서가 E4 폼의 뼈대와 저장 한 경로(업로드 의도 줄 C3 · 동의 묶음 · 주민등록번호 되묻기 포함)를 세웠다. 이 플랜은 E4 → E5를 UI-SPEC 「화면 계약」 E4 · E5 · E6 그대로 완성한다: 서버 쪽 제출 재생(멱등 — 같은 키 재생만 증표 전에) · 잠근 뒤 재판정 · 동시 제출 · 마지막 자리 자동 닫힘(D-1102) · 서명 PNG 검사(빈 서명 · 크기 상한), 화면 쪽 주민등록번호 두 칸 · 동의 제자리 펼침 · 경품 문의 줄 · 제출 결과 갈래 · 서명 캔버스 계약(개정 ①-i) 전부.

**UI-SPEC:** 최신 승인본(`2c6277b` + 2026-09-25 `/plan-design-review` 반영 — 이 파일의 현재 내용, X-11)이 정본이다. 문구는 Copywriting E 표를 그대로 쓴다.

Purpose: CERT-01의 「사람마다 한 번 제출 · 제출하면 그 사람 자리만 닫힘 · 모두 제출하면 링크가 닫힘」을 동시성과 재전송까지 포함해 서버가 보장하고, 서명 입력이 접근성 기준(§10 · §11)을 지키게 한다.
Output: `submitCertificate` 완성, 순수 모듈 둘, 화면 부품 셋, 단위 2 · 통합 1 · E2E 1.

**실행 조건(E3-01 · E3-33):** Phase 4 PR #40이 main에 머지된 뒤에만 시작하고, 웨이브 첫 단계는 `git merge origin/main`이다(리베이스 · 강제 푸시 금지). 로그는 Phase 4의 `recordAction(viewer, entry, { tx })`(`domain/action-log/record.ts` `RecordActionDeps.tx` — `repositories/action-log.ts` `appendActionLog(viewer, entry, tx?)`)를 기준으로 쓴다. 같은 웨이브 4의 04.3-07 · 08 · 09와도 통합 · E2E는 한 번에 하나다(세션당 플랜 하나). 이 문서의 「개정 ①-i · ⑦ (a)」 같은 번호는 UI-SPEC 개정 목록 번호다 — 04.3-01이 SYSTEM · DECISIONS 번호를 main 기준으로 다시 매겼으면 그 SUMMARY 대응표로 찾는다.

**범위(checker-B W-3 — 받아들임):** 파일 22개 · 태스크 셋으로 15개 기준을 넘지만 제출 서버 판정 → E4 폼 · E5 → 서명 캔버스 · 375px 제출 E2E가 한 세로 조각(수령자가 확인 뒤 서명해 제출하기까지)이라 나누지 않는다 — 나누면 어느 쪽도 혼자서는 수령자가 제출을 마치지 못한다(04.3-07 · 08 · 10과 같은 판단).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-02-SUMMARY.md
@.planning/phases/04.3-qr-certificate-intake/04.3-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 제출 완성(서버) — 같은 키 재생만 증표 전에 · 잠근 뒤 재판정 · 동시 제출 · 마지막 자리 자동 닫힘 · 업로드 의도 줄 · 서명 PNG 검사(빈 서명 · 크기 상한)</name>
  <files>domain/certs/intake.ts, domain/certs/signature-png.ts, domain/certs/signature-ink.ts, repositories/cert-submissions.ts, repositories/cert-events.ts, app/c/[token]/actions.ts, test/unit/certs/signature-png.test.ts, test/integration/cert-submit.test.ts, test/integration/cert-intake.test.ts, test/e2e/helpers/cert.ts</files>
  <precondition>(E3-01) Phase 4가 main에 있고 이 브랜치에 합쳐져 있다 — `git fetch origin && git merge origin/main` 뒤 `test -f ui/confirm-dialog/ConfirmDialog.tsx && git merge-base --is-ancestor origin/main HEAD`가 종료 코드 0, 아니면 시작하지 않고 멈춘다 · 그리고 `db/client.ts`의 `DbOrTx`가 `"delete"`를 담는다(`grep -q '"delete"' db/client.ts`) — 04.3-02 Task 2 ⑨가 `deleteSignatureUploadIntent(viewer, objectKey, tx?)`를 만들며 `DbOrTx`를 이미 `"delete"`까지 넓혀 두었다(소유는 02, RED→GREEN 기록도 02 SUMMARY에 있다, codex-B 2 — 이 플랜은 넓히지 않고 아래 롤백 테스트로 확인만 한다)</precondition>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 E1 「서버 왕복 공통」·「멱등 키」 세 줄, E2 349행(제출 여부는 확인 통과 뒤에만), E4 「서명」 388·395행(24 규칙 · 1040×400 · 굵기 6 · 바탕 투명) · 「제출 뒤 서버 결과에 따라」 줄, E6 머리 문단
    - `domain/certs/intake.ts`(04.3-02·03 산출물 전문) — `submitCertificate`의 지금 순서(⑩ — 의도 줄 → `store.put` → 트랜잭션) · 판별 유니온 이름(`expiredProof` · `rrnRecheck` 등) · 동의 묶음 비교 · 고아 정리 · `recordAction` 호출
    - `repositories/cert-submissions.ts`(04.3-02) — `insertSignatureUploadIntent(SYSTEM_VIEWER, key)` · `deleteSignatureUploadIntent(SYSTEM_VIEWER, key, tx)`
    - `repositories/cert-winners.ts`(04.3-03) — `lockEventRow(viewer, eventId, tx)`(행사 → 자리 잠금 순서 · tx 마지막 · 기본값 없음, E3-24 — 이 플랜의 호출부는 모두 이 순서로 부른다)
    - `test/e2e/helpers/cert.ts`(04.3-02 C4 · 04.3-03이 `verifyLast4` 줄을 맞춤) — `seedSubmittedCert()`가 `verifyLast4` 결과로 `submitCertificate`를 부르는 한 줄(⑤-b가 이 줄만 맞춘다 — 표본을 만드는 방법은 그대로)
    - `repositories/cert-events.ts` · `db/schema/cert-submissions.ts` · `db/schema/cert-events.ts` · `db/schema/cert-winners.ts` — `idempotency_key_hash` · `winner_id` UNIQUE · `closed_at` · `closed_reason` · `version` · `offered_consent_version` · `offered_retention_years` 칸
    - `app/c/[token]/actions.ts` — 공개 액션의 입력 스키마(제출 키 · 서명 base64 · `consentVersion` · `retentionYears` · `rrnRecheckConfirmed`)
    - `lib/actions/payload-size.ts` — `MAX_ACTION_PAYLOAD_BYTES = 262_144`와 `checkPayloadSize`(JSON 전체 바이트 — 순수 함수라 단위 테스트가 직접 부른다)
    - `lib/certs/feature-guard.ts`(04.3-02) — `assertCertFeatureEnabled()`
    - `test/integration/cert-intake.test.ts`(04.3-02) · `test/integration/cert-verify.test.ts`(04.3-03) — 증표를 얻는 순서(`selectWinner` → `verifyLast4`)와 동시 요청 단언 방식
    - `lib/db-transaction.ts` — `withTransaction`(9행 — Drizzle `db.transaction`을 그대로 부른다) · `node_modules/drizzle-orm/node-postgres/session.js` 180~195행(187행 `commit` 실패도 `catch`로 가 예외가 된다 — (g)의 커밋 결과 불명 갈래)
    - `domain/action-log/record.ts`(Phase 4 머지 뒤 main — 줄 번호 대신 이름으로 찾는다) — `ALWAYS_ON_ACTION_TYPES`(`document_submit`은 끌 수 있는 종류) · `RecordActionDeps`의 `appendActionLog` · `isActionTypeEnabled` · **`tx`**(04-32: 주면 로그 INSERT가 그 tx로 가고, `isActionTypeEnabled`를 주지 않으면 설정 조회도 그 tx로 간다) · `recordAction(viewer, entry, deps?)` · 설정 조회 실패 = 켬 — (h)의 같은 트랜잭션 로그
    - `repositories/action-log.ts` `appendActionLog(viewer, entry, tx?)`(Phase 4 — 선택 tx) · `repositories/cert-action-log.ts`(04.3-02 ⑨ — `RecordActionDeps`에 `tx`가 없을 때만 쓰는 대비 도우미 `insertActionLogTx(viewer, entry, tx)`. 읽기만 하고 이 플랜은 고치지 않는다, E3-02)
    - `eslint/rules/repository-viewer-param.mjs`(29행 — `repositories/**`의 export 함수는 첫 인자 이름이 `viewer`여야 한다, `eslint.config.mjs:77`에서 error) · `repositories/projects.ts` `insertProject(viewer, input, tx: DbOrTx = db)`(viewer 먼저 · tx 끝 규약) · `db/client.ts` `DbOrTx` — ④의 `findSubmissionBySignatureKey` 모양
  </read_first>
  <behavior>
    - (단위 `signature-png`) 투명 1040×400(잉크 0) → `{ok: false, reason: "blank"}` · 지름 6 점 하나(잉크 약 28) → `blank` · **잉크 픽셀 ≥ 288 경계(4차 — 획 길이가 아니라 잉크 픽셀로 센다):** 1040×400 PNG에 잉크 288픽셀(가로선 48×6 — `countInkPixels` 실측 288) → ok · 잉크 276픽셀(가로선 46×6 — 실측 276) → `blank` · 잉크 287픽셀(288픽셀 선에서 한 픽셀을 투명으로 — 실측 287) → `blank` · (왕복 낙서 — checker-B W5) 같은 자리를 네 번 오간 획(획 길이 합은 길어도 잉크는 가로선 한 줄 24×6 — 실측 144, 한 자리를 여러 번 그어도 한 번만 센다) → `blank` · `countInkPixels`: 알파 0만 → 0 · 알파 1인 픽셀 하나 → 1 · 1040×400 RGBA에 48×6 불투명 사각형 → 288
    - (단위) 1040×399 · 8비트 RGB(색 형식 2) · 16비트 · 인터레이스 · PNG 시그니처가 아닌 본문 · 잘린 IDAT → `format` · **(RED — 잉크는 충분한데 구조가 깨진 표본 둘, 04.3-02의 시그니처·IHDR 검사는 통과시킨다)** 잉크 288픽셀 이상 PNG에서 IDAT 청크의 CRC 한 바이트를 바꾼 본문 → `format` · 같은 PNG에서 끝 IEND 청크를 뗀 본문 → `format` · IEND 뒤에 바이트가 더 붙은 본문 → `format` · 184,320바이트(부속 청크로 채운 유효 PNG) → ok · 184,321바이트 → `tooLarge` · IDAT가 선언 크기(400 × (1 + 1040 × 4) = 1,664,400바이트)보다 크게 풀리는 본문 → `format`(그 이상 풀지 않는다)
    - (단위 — 본문 한도 예산) 모든 칸을 zod 최대 길이로 채우고(이름 40자 · 주소 200자 — 둘 다 3바이트 한글 · 키·증표 최대 길이) 서명에 184,320바이트 PNG의 base64를 넣은 입력 객체 → `checkPayloadSize` ok · 서명 base64 최대 길이 + 1 → zod가 칸 오류 `signature`로 거절한다(본문 한도 오류가 먼저 나지 않는다)
    - 증표를 가진 자리의 정상 제출 → 저장 성공(E5 결과) · `cert_submissions` 한 줄 · `cert_winners.submitted_at` 채워짐 · 행동 로그 `document_submit` 한 줄 · `cert_signature_uploads` 0줄(의도 줄이 저장 트랜잭션에서 지워짐)
    - 같은 제출 키로 다시 → 같은 결과(같은 확인증 번호 · 같은 제출 시각) · 줄 수 그대로 · 행동 로그 그대로 · 저장소 객체 수 그대로 · 의도 줄 0
    - (비노출 — Codex #6) 제출된 자리와 제출되지 않은 자리에 임의 키 + 임의 증표로 제출 → 두 응답이 모두 `expiredProof`이고 키 집합이 같다 · 제출된 자리에 **다른 키 + 제출 전에 받은 옛 증표**로 제출 → `expiredProof`(제출이 증표를 비웠다 — E6-a를 흘리지 않는다) · 어느 경우도 저장소에 객체가 올라가지 않고 의도 줄이 생기지 않는다
    - 같은 자리에 서로 다른 키 둘을 같은 유효 증표로 `Promise.all` → 저장 하나 + `alreadySubmitted`(E6-a) 하나(진 쪽은 증표를 통과한 요청이라 E6-a를 받는다) · 줄 하나 · 로컬 저장소에 그 자리의 객체가 정확히 하나 · 의도 줄 0
    - (잠근 뒤 재판정 — Codex #7) 증표 확인을 통과한 뒤 저장소 `put`이 끝나기 전에(주입한 저장소의 `put`이 신호를 기다리게 해 순서를 고정) ⓐ 행사를 `manual`로 닫으면 → `closed`, 제출 줄 없음, 올린 객체 지워짐, 의도 줄 0 · ⓑ 담당자 편집으로 그 자리 `version`을 올리면(전달 `onsite` → `parcel`) → `expiredProof`, 저장 없음 · ⓒ 그 자리의 증표 해시를 비우면 → `expiredProof` · ⓓ 기한을 지나게 하면 → `closed`
    - 확인 응답의 `version`과 다른 `winnerVersion`을 보내면 → `expiredProof` · 동의 버전·보존 기간이 자리에 묶인 값과 다르면 → `expiredProof`(04.3-02 규칙 유지)
    - (tx 삭제가 같은 트랜잭션 · 보상 삭제 — codex-B 2) 저장 트랜잭션이 `deleteSignatureUploadIntent(…, tx)`를 부른 **뒤** 커밋 전에 던지게 하면(주입 — `all_submitted` 닫기 함수가 던진다) 의도 줄 삭제도 롤백돼 그 줄이 되살아난다 → (g)가 제출 줄이 없음을 확인하고 주입한 저장소의 `delete`를 부른다 — 제출 줄 0 · 의도 줄은 `delete`가 성공하든 실패하든 정확히 1줄(예외 갈래는 의도 줄을 지우지 않는다 — 04.3-12가 24시간 뒤 치운다) · 저장소 객체는 `delete` 성공이면 0 / 실패면 1 · (판정 갈래 대조) 잠근 뒤 `closed`로 끝나는 요청(아래 ⓐ 모양)에서는 주입한 `delete`가 성공하면 의도 줄 0 / 실패하면 1
    - 미제출 둘 남은 행사에서 둘을 동시에 제출 → 둘 다 저장 · 행사 `closed_at` 한 번 채워짐 · `closed_reason = all_submitted` · 그 뒤 `loadIntake` → 닫힘(all_submitted)
    - 마지막 자리 제출 결과를 잃고 같은 키로 다시 → 링크가 이미 닫혔어도 E5 결과(E6-b 아님)
    - (마지막 자리 동시 같은 키 — 4차, Codex r3-B 추가 주석 3) 미제출 하나 남은 행사에서 같은 제출 키 · 같은 유효 증표의 제출 둘을 `Promise.all`로 보내되 주입한 저장소의 `put`이 두 요청 모두 끝난 뒤에야 풀리게 해(둘 다 잠그기 전 검사 (b)·(c)를 통과한 순서를 고정) → 두 응답이 모두 `saved`이고 같은 확인증 번호 · 같은 제출 시각이다(뒤 요청이 E6-b `closed`가 아니다) · 제출 줄 1 · 행사 `closed_reason = all_submitted`로 한 번 닫힘 · 저장소에 그 자리 객체가 정확히 하나(진 요청이 올린 객체는 지워짐) · 의도 줄 0
    - 빈 서명 PNG(투명) · 1040×400이 아닌 PNG · 184,320바이트 초과 · PNG가 아닌 본문 · 잉크는 충분하나 청크 CRC가 틀린 PNG · IEND가 없는 PNG → 칸 오류 `signature`, 저장소에 아무것도 올라가지 않고 의도 줄도 없음
    - 택배 자리에 주소 없음 → 칸 오류 `address` · 현장 자리에 주소가 와도 저장값은 null
    - 칸 오류를 받은 키로 고친 값을 다시 보내면 옛 칸 오류를 재생하지 않고 새로 판정해 저장한다(상태를 바꾸지 않은 판정은 키에 묶지 않는다)
    - 증표 없이·만료된 증표로 → 확인 시간 지남 결과, 저장 없음
    - (C1) 기능이 꺼지면 `submitCertificate`가 저장·업로드·의도 줄 없이 거부된다
    - (로그 원자성 — 최종 리뷰 B5) 주입한 로그 INSERT(`deps.appendActionLog` — `recordAction`의 `appendActionLog` 주입 자리로 넘어간다, E3-02)가 던지게 하고 정상 제출 → 예외(결과 불명 갈래) · 제출 줄 0 · `cert_winners.submitted_at` null · 행사 `closed_at` 그대로(마지막 자리여도 자동 닫힘 롤백) · `document_submit` 로그 0 · 제출 줄이 없음을 확인한 (g)가 올린 객체를 지워 저장소 객체 0 · 의도 줄 1(예외 갈래 — 04.3-12가 치운다) · 자리의 증표 해시 그대로 → 주입을 걷고 같은 제출 키로 다시 → `saved` · 제출 줄 1 · 로그 1 · 의도 줄은 앞 요청 것 1줄 그대로(새 요청의 의도 줄은 저장 트랜잭션이 지웠다)
    - **(커밋 결과 불명 — codex-final3-B 1, RED)** 주입한 `deps.withTransaction`이 **첫 호출에서만** 실제 `withTransaction`으로 커밋까지 끝낸 뒤 `commit` 응답 유실 예외를 던지게 하고(`session.js:187`의 `commit` 실패 재현 — 두 번째 호출부터는 실제 함수 그대로) 정상 제출 → 응답 `saved`(예외가 아니다) · 제출 줄 1 · 그 줄의 `signature_key` 객체가 로컬 저장소에 **있다** · 의도 줄 0 · 로그 1 → 같은 제출 키로 다시 → `saved` · 같은 확인증 번호 · 객체 그대로 · 줄 수 그대로. ①에서 먼저 써, 예외면 무조건 `store.delete`인 04.3-02 순서에서 객체가 사라져 실패함을 본다(RED) · (조회도 실패) 같은 커밋 뒤 예외에 주입한 `deps.findSubmissionBySignatureKey`까지 던지게 하면 → 원래 예외(결과 불명) · 객체 1 · 의도 줄 0 · 제출 줄 1 → 주입을 걷고 같은 키로 다시 → `saved` · 커밋 **전** 예외(위 `all_submitted` 주입)에 조회도 던지면 → 원래 예외 · 객체 1 · 의도 줄 1 · 제출 줄 0(04.3-12가 치운다)
    - (로그 끔) 설정 `ACTION_LOG_OPTIONAL_TYPES`에서 `document_submit`을 빼면 제출은 `saved`이고 로그 0줄이다(설정은 트랜잭션을 열기 **전에** 읽는다 — 행사·자리 잠금을 쥔 채 풀에서 두 번째 연결을 잡지 않는다)
  </behavior>
  <action>
① **RED:** 위 `behavior`의 단위 줄을 `test/unit/certs/signature-png.test.ts`에, 통합 줄을 `test/integration/cert-submit.test.ts`에 먼저 쓴다(04.3-02의 `cert-intake.test.ts`와 겹치는 정상 경로는 다시 쓰지 않는다). 서명 PNG 표본은 테스트 안의 작은 인코더(`node:zlib` + CRC — 새 의존성 없음)로 만든다: 투명 바탕 1040×400 8비트 RGBA에 알파 255 가로선(굵기 6 · 길이 = 논리 길이 × 2)을 찍는다 — 투명 PNG는 이제 **빈 서명 표본**이고 정상 제출 표본은 잉크 288픽셀 이상(가로선 48×6 이상)이 든 PNG다. 184,320바이트 경계 표본은 유효 PNG에 부속 청크(`tEXt`)를 붙여 정확한 크기로 맞춘다. 잠근 뒤 재판정 케이스의 담당자 편집은 04.3-10 전이라 테스트가 `db/client`로 자리 행을 직접 고친다.

② **RED → GREEN `domain/certs/signature-png.ts`(순수, `node:zlib`만):**
  - **`domain/certs/signature-ink.ts`(import 없음 — 클라이언트 부품도 불러 쓴다, `node:zlib`를 끌어오지 않게 PNG 모듈과 나눈다):** `SIGNATURE_MIN_INK_PIXELS = 288` · `countInkPixels(rgba: Uint8Array | Uint8ClampedArray): number`(4바이트마다 알파가 0이 아닌 픽셀 수). 잉크 판정은 이 둘뿐이다 — 서버의 PNG 검사와 Task 3의 서명 칸이 같은 것을 부른다(checker-B W5 「잉크 측정 하나」).
  - 상수: `SIGNATURE_MAX_PNG_BYTES = 184_320`(180 KiB — base64 245,760바이트 + 나머지 칸 최대치가 `MAX_ACTION_PAYLOAD_BYTES` 262,144 아래, 예산은 단위 테스트가 `checkPayloadSize`로 증명) · `SIGNATURE_BASE64_MAX_LENGTH = Math.ceil(SIGNATURE_MAX_PNG_BYTES / 3) * 4`.
  - `inspectSignaturePng(bytes)` → `{ok: true} | {ok: false, reason: "tooLarge" | "format" | "blank"}`: 크기 → 시그니처 8바이트 → 청크 순회(길이 경계 검사, IHDR 첫 청크 · **청크마다 CRC-32(형식 4바이트 + 데이터)를 `node:zlib`의 `crc32`로 계산해 청크 끝 4바이트와 비교 — 하나라도 다르면 `format`** · **IEND를 만나면 순회를 끝내고 그 뒤에 남은 바이트가 없어야 하며, IEND 없이 본문이 끝나도 `format`** — 잉크가 충분해도 깨진 PNG를 받아 두면 인쇄(04.3-11)의 `decode()`가 실패하므로 저장 전에 거른다. `zlib.crc32`는 Node 22.2부터 있다 — `package.json` `engines` `>=22.12` · `Dockerfile` `node:24-slim`이라 새 의존성이 없다) → IHDR = 1040 × 400 · 비트 깊이 8 · 색 형식 6 · 압축 0 · 필터 0 · 인터레이스 0 → IDAT 이어 붙여 `inflateSync(…, { maxOutputLength: 400 × (1 + 1040 × 4) })`(넘으면 `format` — 압축 폭탄 방어) → 풀린 길이가 정확히 그 값인지 → 줄마다 필터 0~4(Sub · Up · Average · Paeth, 픽셀당 4바이트)를 되돌린 줄 바이트를 `countInkPixels`로 세어 더한다 → `SIGNATURE_MIN_INK_PIXELS` 미만이면 `blank`. 부속 청크(첫 글자 소문자)는 건너뛴다.
  - 04.3-02가 둔 200KB 상수와 시그니처·IHDR 검사는 이 함수 하나로 바꾼다(`reason`은 어느 것이든 칸 오류 `signature` 하나로 돌려준다 — 화면 문장은 그대로).

③ **`submitCertificate` 순서(`domain/certs/intake.ts`) — 04.3-02 ⑩의 순서를 유지하며 아래로 정리한다:**
  - (a) 플래그(domain은 `isCertFeatureEnabled` 결과로 거부 — 액션은 따로 가드를 먼저 부른다) · 토큰 → 행사 id → 자리 찾기(행사 id + 자리 id).
  - (b) **같은 키 재생(증표 전에 허용되는 유일한 판정):** 그 자리의 제출 줄이 있고 `idempotency_key_hash`가 이 요청의 제출 키 해시와 **같으면** 저장된 결과로 E5를 다시 만들어 돌려준다(닫힘 판정보다 먼저 — 마지막 자리 재전송이 E6-b로 빠지지 않게). 같지 않으면 제출 여부를 여기서 말하지 않고 다음으로 간다(Codex #6).
  - (c) 닫힘(→ `closed`, E6-b — 링크 닫힘은 누구에게나 보이는 공개 상태다) → 증표 해시 일치 + `verified_until` 안(→ `expiredProof`) → `winnerVersion`이 자리 `version`과 같음 · 동의 버전·보존 기간이 자리에 묶인 값과 같음(→ `expiredProof`). **제출된 자리는 제출 때 증표 해시가 비워졌으므로 다른 키의 요청은 여기서 `expiredProof`가 된다 — 제출되지 않은 자리의 틀린 증표와 같은 응답이다.**
  - (d) 칸 검사(→ `invalid` · 주민등록번호 검증번호 불일치는 04.3-02의 `rrnRecheck` 한 번 규칙 그대로) → `inspectSignaturePng`(→ `invalid` `signature`).
  - (e) **C3:** `insertSignatureUploadIntent(SYSTEM_VIEWER, key)`를 커밋한 뒤 `store.put`.
  - (f) `withTransaction`: `lockEventRow(SYSTEM_VIEWER, eventId, tx)`(04.3-03 — 인자 순서 viewer · eventId · tx, E3-24) → 자리 행 `FOR UPDATE` → **잠긴 값으로 다시 판정(Codex #7) — 순서가 계약이다:** ⓐ **같은 키 재생 먼저(4차 — Codex r3-B 추가 주석 3):** 자리 `submitted_at`이 있고 그 제출 줄의 제출 키 해시가 이 요청과 같으면 저장된 결과로 `saved`를 재생한다 — 닫힘 판정보다 먼저라, 마지막 자리의 성공 제출과 동시에 온 같은 키 중복(둘 다 (b)·(c)를 잠그기 전에 통과했다)이 자동 닫힘 뒤 잠금을 얻어도 E6-b가 아니라 `saved`를 받는다(이 요청은 저장하지 않았으므로 (g)가 이 요청이 올린 객체를 지운다) · ⓑ 행사 `closed_at`·기한(→ `closed`) · ⓒ 자리 `submitted_at`이 있고 키가 다르면 `alreadySubmitted`(이 요청은 (c)에서 증표를 통과했으므로 E6-a를 알려도 된다) · 증표 해시·만료(→ `expiredProof`) · `version`(→ `expiredProof`) · 동의 묶음(→ `expiredProof`). 모두 같으면 자리 조건부 갱신(`submitted_at IS NULL`) → 문서 번호 → 제출 줄 삽입(제출 키 해시 포함, 증표 해시 비움) → `deleteSignatureUploadIntent(SYSTEM_VIEWER, key, tx)` → 같은 행사의 파기되지 않은 미제출 자리 수가 0이면 행사를 `closed_at = now` · `closed_reason = all_submitted`로 닫는다(`closed_at`이 비어 있을 때만) → **같은 tx로 제출 로그((h))** → 커밋.
  - (g) **판정 갈래 — (f)가 저장하지 않기로 판정하고 끝나면**(`closed` · `alreadySubmitted` · `expiredProof` · 잠근 뒤 같은 키 재생 — 트랜잭션은 확정적으로 끝났고 이 요청의 객체는 어느 제출 줄도 가리키지 않는다): `store.delete(key)` → 성공(404 포함)일 때만 의도 줄을 지운다 · 실패면 의도 줄을 남긴다(04.3-12가 24시간 뒤 지운다).
    **예외 갈래 — (f)가 예외로 끝나면(커밋 결과 불명, codex-final3-B 1):** 예외만 보고 객체를 지우지 않는다. `withTransaction`(`lib/db-transaction.ts:9`)은 Drizzle `db.transaction`을 그대로 부르고, Drizzle은 `commit` 응답 실패도 `catch`로 보내 예외를 던진다(`node_modules/drizzle-orm/node-postgres/session.js:187`) — DB에는 커밋이 들어갔는데 응답만 잃었을 수 있다. 저장 트랜잭션은 `deps.withTransaction`(기본값 `lib/db-transaction.ts`의 `withTransaction`)으로 열어 테스트가 커밋 뒤 예외를 주입할 수 있게 한다. 예외를 받으면 새 짧은 트랜잭션에서 `lockEventRow(SYSTEM_VIEWER, eventId, tx)`(E3-24) → 자리 행 `FOR UPDATE`(저장과 같은 잠금 순서 — 실패한 트랜잭션이 아직 커밋 중이면 그 잠금이 풀릴 때까지 기다려 확정된 상태를 읽는다) → `deps.findSubmissionBySignatureKey(SYSTEM_VIEWER, eventId, key, tx)`(④)로 이 요청의 객체 키를 `signature_key`로 가진 제출 줄을 찾는다:
      · **있으면** 커밋이 들어간 것이다 — 객체를 두고 예외를 삼킨 뒤 그 줄로 (b)와 같은 `saved`(E5)를 돌려준다(로그 · 자동 닫힘 · 의도 줄 삭제도 같은 트랜잭션이라 이미 반영됐다).
      · **없으면** 롤백된 것이다 — `store.delete(key)`(실패해도 그대로 진행) 후 원래 예외를 다시 던진다(결과 불명 → 같은 키 재전송이 새로 저장한다). 이 갈래는 의도 줄을 지우지 않는다 — 롤백으로 되살아난 줄은 04.3-12가 24시간 뒤 객체 삭제(404 포함) → 줄 삭제로 치운다(의도 줄은 저장 트랜잭션 안에서만 지워지므로, 줄이 남았다는 것 자체가 「커밋되지 않음」의 기록이다).
      · **조회마저 실패하면** 아무것도 지우지 않고 원래 예외를 다시 던진다 — 커밋됐으면 트랜잭션이 의도 줄을 지웠고 객체는 제출 줄이 가리키며, 롤백됐으면 의도 줄이 남아 04.3-12가 치운다(어느 쪽도 고아 객체나 끊어진 참조가 남지 않는다).
  - (h) **제출 로그는 같은 트랜잭션 안에서(최종 리뷰 B5 · E3-02 — 자체 tx 로그 도우미를 만들지 않는다):** 커밋 뒤에 쓰면 (b)의 같은 키 재생이 곧바로 성공을 돌려주므로 로그 INSERT 실패가 영구 누락이 된다. 그래서 (f)의 마지막 단계로 Phase 4의 `recordAction(SYSTEM_VIEWER, {actionType: "document_submit", entity: "cert_submission", entityId: 새 제출 id, detail: {eventId}}, { tx, isActionTypeEnabled: async () => submitLogEnabled, appendActionLog: deps.appendActionLog })`를 부른다 — 종류 검사 · `actorId` 매핑 · 설정 끔 판정은 `recordAction` 것을 그대로 쓰고, INSERT는 `recordAction`이 `appendActionLog(viewer, entry, tx)`로 같은 tx에 보낸다. `deps.appendActionLog`는 `submitCertificate`의 주입 자리(타입 `RecordActionDeps["appendActionLog"]`, 기본값 없음 = `recordAction`의 기본 `appendActionLog`)이고 테스트가 던지는 것으로 바꾼다. `submitLogEnabled`는 `withTransaction`을 열기 **전에** `getSettingValue(ACTION_LOG_OPTIONAL_TYPES)`로 한 번 읽어 값으로 넘긴다(실패하면 켬 — `record.ts`의 fail-open과 같은 방향 · E3-09와 같은 규칙: 04-32가 설정 조회를 tx로 돌릴 수 있어도 설정 · 플래그는 tx 전에 읽는다). **대비책(실행 때 `grep -q 'tx?: DbOrTx' domain/action-log/record.ts`가 실패할 때만 — Phase 4 머지 뒤라 정상이면 쓰지 않는다):** 04.3-02가 만든 `insertActionLogTx`를 `@/repositories/cert-action-log`에서 import해 `{ appendActionLog: deps.appendActionLog ?? ((viewer, entry) => insertActionLogTx(viewer, entry, tx)), isActionTypeEnabled: async () => submitLogEnabled }`로 넘긴다(테스트 주입 자리는 같은 `deps.appendActionLog` 하나) — 이 플랜은 그 파일도 `repositories/action-log.ts`도 고치지 않고 같은 이름의 함수를 따로 만들지 않는다(어느 쪽을 썼는지 SUMMARY에 한 줄). 로그가 던지면 트랜잭션 전체가 롤백되고 (g)가 올린 객체를 지운 뒤 예외를 다시 던진다(화면은 결과 불명 → 같은 키 재전송이 새로 저장한다). 이 교차 케이스(로그 실패 주입 → 전체 롤백)는 ①에서 다른 통합 케이스와 함께 먼저 써서, 커밋 뒤 로그인 04.3-02 구현에서 제출 줄이 남아 실패함을 본다(RED — 04.3-02가 E3-02로 처음부터 tx 안 로그를 썼으면 RED가 없는 것이 정상이고 SUMMARY에 「02가 이미 tx 안 · 회귀 테스트만」으로 적는다).
  - 결과 `kind`: `saved`(E5 — 확인의 `submitted`(E6-a)와 이름을 나눈다. 04.3-02 트레이서는 저장 결과를 `submitted`로 둔다(02 ⑩ 8 · 02 behavior 정상 제출) — 여기서 `saved`로 바꾸고 화면 갈래도 같이 바꾼다. **이름을 바꾸는 이 플랜이 04.3-02의 `test/integration/cert-intake.test.ts` 기대값 갱신을 맡는다(4차 note 3b):** `submitCertificate` 결과를 단언하는 줄만 — 정상 제출 · `rrnRecheckConfirmed: true` 제출의 `submitted` → `saved`, 「같은 자리 두 번째 제출」은 그 케이스가 쓰는 키대로 같은 키면 `saved`(같은 확인증 번호) · 다른 키면 `expiredProof`(제출이 증표를 비웠다 — (c)), 제출 행 1개 단언은 그대로. `verifyLast4`의 `submitted`(E6-a) 기대값은 바꾸지 않는다. 이름 바꾸기 전에 그 테스트를 먼저 고쳐 실패를 본다(RED)) · `alreadySubmitted`(E6-a) · `closed` · `expiredProof` · `invalid` · `rrnRecheck`. **상태를 바꾸지 않은 판정은 키와 함께 저장하지 않는다.**

④ **리포지토리:** `repositories/cert-submissions.ts`에 자리 id로 제출 줄(제출 키 해시 · 확인증 번호 · 제출 시각 · 제출 이름)을 읽는 함수(의도 줄 두 함수는 04.3-02 것을 쓴다)와 ③ (g)의 `findSubmissionBySignatureKey(viewer, eventId, signatureKey, tx: DbOrTx)`(행사 id + `signature_key` 일치로 같은 모양의 줄 하나 또는 null — (b)의 재생 결과를 그대로 만들 수 있는 칸, 첫 인자 `viewer` — 저장소 린트 규칙), `repositories/cert-events.ts`에 미제출 수 세기 · `all_submitted`로 닫기 함수를 더한다(행사 행 잠금은 04.3-03의 `lockEventRow(viewer, eventId, tx)`를 쓴다 — 같은 함수를 두 번 만들지 않는다). where 절에 행사 id를 반드시 건다(04.3-02 경계 규칙). 행동 로그를 쓰는 저장소 함수는 이 플랜에서 만들지 않는다(③ (h) — `recordAction(…, { tx })`, E3-02).

⑤ **`app/c/[token]/actions.ts`:** `submitCertificateAction` 본문 첫 문장 `await assertCertFeatureEnabled()`(04.3-03이 확인했으면 그대로 — C1). 입력 스키마: 제출 키(base64url 22~64자) 필수 · 서명 문자열 `max(SIGNATURE_BASE64_MAX_LENGTH)`(넘으면 본문 한도가 아니라 칸 오류 `signature` — Codex #27. 이 거절은 domain `invalid`가 아니라 next-safe-action 결과의 `validationErrors`(기본 모양 `validationErrors.signature._errors` — `app/(app)/admin/vendors/vendor-form.tsx:136`과 같다)로 오고, 화면이 Task 2 ④(e)에서 칸 오류로 바꾼다 — 액션이 따로 가공하지 않는다) · `winnerVersion`(정수 ≥ 1) · `consentVersion` · `retentionYears` · `rrnRecheckConfirmed`(04.3-02). 결과 유니온을 그대로 돌려준다(액션은 domain 하나만 부른다).

⑤-b **표본 도우미 호출 줄(04.3-02 C4 규칙 · X1):** ③ (c)가 domain 제출 입력에 `winnerVersion`을 필수로 더하므로 `test/e2e/helpers/cert.ts` `seedSubmittedCert()`의 `submitCertificate` 호출 줄을 **같은 커밋에서** 맞춘다 — 그 도우미가 이미 받은 `verifyLast4` 결과(`ok`)의 `version`(04.3-03)을 `winnerVersion`으로 싣고, 이 플랜이 입력을 더 바꾸면(제출 키 모양 등) 그 값도 같은 줄에서 맞춘다. 표본을 만드는 방법(domain 직접 호출 · 제출 줄은 `winner_id`로 DB에서 읽어 성공 판정 — 결과 `kind` 이름에 기대지 않는다)과 옵션 · 반환 모양은 그대로이고 Playwright 테스트 러너 패키지를 import하지 않는다(Vitest 통합 테스트가 이 파일을 import한다). 이 한 줄 없이 입력을 바꾸면 도우미가 던져 같은 웨이브의 04.3-07과 뒤 플랜 10 · 11 · 12 표본이 전부 빨강이 된다.

⑥ 커밋 `feat(04.3): server-side signature png inspection` · `feat(04.3): idempotent submit with lock-time recheck and auto-close` 식으로 의도별로, 본문 한국어(같은 키 재생만 증표 전에 두는 이유 · 잠근 뒤 재판정 · 행사 행 잠금으로 닫힘이 한 번인 이유 · 180 KiB 상한 계산).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/signature-png.test.ts test/unit/certs/public-route-boundary.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 특히 본문 한도 예산 케이스가 실패하면 180 KiB 상한과 262,144바이트 한도가 어긋났다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-submit.test.ts test/integration/cert-intake.test.ts test/integration/cert-verify.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 동시 제출 케이스가 실패하면 조건부 갱신이나 행사 행 잠금이 빠졌고, 비노출 케이스가 실패하면 증표 전에 제출 여부가 새고, 잠근 뒤 재판정 케이스가 실패하면 잠그기 전 값으로 저장했다</fails_when>
    <automated>grep -v '^\s*//' 'app/c/[token]/actions.ts' | grep -q 'SIGNATURE_BASE64_MAX_LENGTH' &amp;&amp; test "$(grep -v '^\s*//' domain/certs/intake.ts | grep -cE '200[_ ]?(KB|\* ?1024|000)')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 서명 문자열의 zod 상한이 상수를 쓰지 않거나, 04.3-02의 200KB 상한이 남아 두 상한이 공존한다</fails_when>
    <automated>pnpm exec eslint repositories/cert-submissions.ts repositories/cert-events.ts domain/certs/intake.ts domain/certs/signature-png.ts domain/certs/signature-ink.ts 'app/c/[token]/actions.ts'</automated>
    <fails_when>종료 코드가 0이 아니다 — 특히 `plant8/repository-viewer-param`(첫 인자가 `viewer`가 아닌 저장소 export)이 걸렸다</fails_when>
    <automated>test "$(grep -rlE 'export (async )?function [A-Za-z]*ActionLogTx' repositories | grep -v 'repositories/cert-action-log.ts' | wc -l)" = 0 &amp;&amp; grep -v '^\s*//' domain/certs/intake.ts | grep -q 'recordAction(' &amp;&amp; test "$(grep -rn 'lockEventRow(' domain/certs/intake.ts | grep -cE 'lockEventRow\([^,]+, *tx')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 04.3-02의 대비 파일 밖에 tx 로그 도우미가 새로 정의됐거나(E3-02 — 도우미는 하나뿐이어야 한다), 제출 경로가 `recordAction`을 부르지 않거나, `lockEventRow`를 옛 순서(viewer 다음 tx)로 부른다(E3-24)</fails_when>
    <automated>grep -v '^\s*//' test/e2e/helpers/cert.ts | grep -q 'winnerVersion' &amp;&amp; test "$(grep -c '@playwright/test' test/e2e/helpers/cert.ts)" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 표본 도우미의 `submitCertificate` 호출이 `winnerVersion`을 싣지 않아 `seedSubmittedCert()`가 던지거나(X1 — 04.3-02 C4 규칙 위반), 도우미가 Playwright 러너를 import해 Vitest 통합 테스트(04.3-12)가 못 쓴다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/certs/signature-png.ts`가 `node:zlib`와 `./signature-ink` 밖의 모듈을 import하지 않는 순수 모듈이고 `SIGNATURE_MAX_PNG_BYTES = 184_320`이 있다 · `domain/certs/signature-ink.ts`가 import 없이 `SIGNATURE_MIN_INK_PIXELS = 288` · `countInkPixels`를 내보낸다
    - 통합 테스트가 「tx 삭제 뒤 롤백 → 의도 줄 되살아나 1줄로 남음(예외 갈래) · 보상 삭제 성공이면 객체 0 / 실패면 객체 1」을 단언한다(`DbOrTx`의 `delete`가 실제로 같은 트랜잭션임을 증명)
    - (codex-final3-B 1) 커밋 뒤 `commit` 응답 유실 주입 케이스가 있고 녹색이다 — 응답 `saved` · 제출 줄의 `signature_key` 객체가 저장소에 남음 · 같은 키 재전송 `saved`(같은 확인증 번호) · 조회 실패 주입 두 케이스(커밋 뒤: 객체 1 · 의도 줄 0 / 커밋 전: 객체 1 · 의도 줄 1)가 원래 예외를 다시 던진다 · SUMMARY에 04.3-02 순서에서 이 케이스가 실패한 RED 실행 결과가 있다 · `grep -cE 'export (async )?function findSubmissionBySignatureKey' repositories/cert-submissions.ts`가 1이다
    - 빈 서명 · 점 · 잉크 288/287 경계(실측 수) · 형식 넷 · CRC 틀림 · IEND 없음 · 184,320/184,321 경계 · 압축 폭탄 · 본문 한도 예산 케이스가 단위 테스트에 있다
    - 재생 케이스가 줄 수 · 행동 로그 수 · 저장소 객체 수 · 의도 줄 0이 그대로임을 단언한다
    - 비노출 케이스(제출 자리 · 미제출 자리의 임의 증표 → 같은 `expiredProof` · 같은 키 집합, 제출 자리의 옛 증표 → `expiredProof`)가 있다
    - 잠근 뒤 재판정 케이스 넷(닫힘 · `version` · 증표 · 기한)이 순서를 고정한 주입 저장소로 있고 녹색이다
    - 동시 제출 두 케이스(같은 자리 · 마지막 두 자리)가 있고 녹색이다
    - 마지막 자리 재전송이 E5를 받는 케이스가 있다
    - (판정 갈래) 보상 삭제 실패 때 의도 줄이 남고 성공 때 지워지는 케이스가 있다 · (예외 갈래) 의도 줄은 삭제 성공 · 실패 어느 쪽에서도 1줄로 남는다
    - (B5) 로그 INSERT 실패 주입 → 제출 줄 0 · `submitted_at` null · 자동 닫힘 없음 · 로그 0 · 같은 키 재전송 `saved` + 로그 1 케이스가 있고 녹색이다 · `domain/certs/intake.ts`의 제출 경로에서 `document_submit` 로그가 `withTransaction` 콜백 안에 있다(커밋 뒤 `recordAction` 호출이 없다)
    - (lint) `pnpm lint`의 eslint 단계를 이 태스크가 고친 파일에 돌린 `pnpm exec eslint repositories/cert-submissions.ts repositories/cert-events.ts domain/certs/intake.ts domain/certs/signature-png.ts domain/certs/signature-ink.ts 'app/c/[token]/actions.ts'`가 종료 코드 0이다(`plant8/repository-viewer-param` 오류 0 — `findSubmissionBySignatureKey`의 첫 인자 `viewer`를 판정한다)
    - (E3-02) 제출 로그가 `recordAction(…, { tx, … })`로 같은 트랜잭션에 쓰이고, `repositories/`에 04.3-02의 `repositories/cert-action-log.ts` 밖의 tx 로그 도우미 정의가 0개다(verify 넷째 줄) · 대비책을 썼는지 SUMMARY에 한 줄
    - (E3-24) `domain/certs/intake.ts`의 `lockEventRow` 호출이 모두 `(SYSTEM_VIEWER, eventId, tx)` 순서다(verify 넷째 줄)
    - (X1) `test/e2e/helpers/cert.ts`가 이 플랜의 `files_modified`에 있고 이 플랜의 도우미 변경은 `seedSubmittedCert()`의 `submitCertificate` 호출 줄(`winnerVersion` 등 새 입력)뿐이다 · 04.3-02의 표본 도우미 통합 케이스(`test/integration/cert-intake.test.ts`의 `seedSubmittedCert()` · `seedSubmittedCert({ delivery: "parcel", extraWinners })`)가 verify 둘째 줄에서 녹색이다
    - 서명 PNG 단위 테스트에 잉크 충분 + CRC 틀림 · IEND 없음 · IEND 뒤 바이트 세 표본이 `format`으로 있고, 통합 테스트가 CRC 틀림 · IEND 없음 제출에서 저장소 객체 0 · 의도 줄 0을 단언한다(저장 전 거부)
    - `domain/certs/intake.ts`의 제출 경로가 `can`·`visible`·`scopeFor`를 import하지 않는다(04.3-02 경계 단위 테스트가 녹색)
  </acceptance_criteria>
  <done>한 자리는 정확히 한 번 저장되고, 재전송은 첫 결과를 받으며, 제출 여부는 증표 전에 같은 키 재생으로만 드러나고, 저장 여부는 잠근 뒤의 값으로 정해지며, 빈 서명과 상한 넘는 서명은 서버가 거부하고, 마지막 제출이 링크를 한 번 닫는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E4 폼 완성 — 주민등록번호 두 칸 · 동의 제자리 펼침 · 경품 문의 줄 · 막힘 이유 · 제출 결과 갈래 · E5</name>
  <files>app/c/[token]/rrn-paste.ts, app/c/[token]/rrn-fields.tsx, app/c/[token]/consent-block.tsx, domain/certs/consent.ts, app/c/[token]/intake-flow.tsx, app/c/[token]/flow-rules.ts, app/c/[token]/intake.module.css, test/unit/certs/rrn-paste.test.ts, test/unit/certs/flow-rules.test.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 E4 전문(값의 주인 · 이름 칸 비움 · 경품 블록 · 주민등록번호 · 동의 · 제출 줄 · 제출 뒤 결과) · E5 · E6 · Copywriting E 표(E4 ~ E6-b 줄)
    - `docs/design/SYSTEM.md` §6-5(04.3-01이 개정한 폼 · 다섯 상태 · 글자 크기 넷) · §7-2(외부 48 · 패딩) · §10
    - `docs/design/system/external-cert.html` — 폼 · 동의 · 결과 블록 실물 마크업
    - `app/c/[token]/intake-flow.tsx` · `app/c/[token]/intake.module.css`(04.3-02·03 산출물 전문) — 단계 상태기계 · 결과 불명 처리(E3) · 기록 항목 규칙
    - `ui/input/TextField.module.css` · `ui/button/Button.module.css` — `.inputExternal` · `.errorExternal` · `.wrapExternal`(개정 ⑦ (a)) — 화면 전용 부품은 `composes`로 가져온다
    - `.planning/phases/04.3-qr-certificate-intake/04.3-02-SUMMARY.md` — 동의 요약 문구 · `{보존 기간}`은 확인 응답의 `consent.retentionYears`(04.3-02 — `loadIntake`는 더 이상 보존 기간을 주지 않는다) · `CERT_CONSENT_VERSION`(`domain/certs/consent.ts` — 전문을 바꾸면 올린다) · `rrnRecheck` 결과와 `rrnRecheckConfirmed` 입력
    - `app/c/[token]/flow-rules.ts`(04.3-03) — `isDefiniteResult`에 제출 결과 종류를 더할 자리
    - `app/(app)/admin/vendors/vendor-form.tsx` 136행 — next-safe-action 결과의 `result.validationErrors?.<칸>?._errors?.[0]` 읽는 모양(`lib/actions/client.ts`가 기본 오류 모양을 쓴다)
  </read_first>
  <behavior>
    - `splitRrnPaste("9304122123458")` → `{front: "930412", back: "2123458"}` · `"930412-2123458"` → 같음 · `"93041"` → `{front: "93041", back: null}` · `"abc930412"` → `{front: "930412", back: null}`(숫자만, 앞 칸 6자리까지)
    - `shouldAdvance({inputType: "insertText", caretAtEnd: true, length: 6})` → true · `deleteContentBackward` → false · 캐럿이 중간 → false · 길이 5 → false
    - (단위 `flow-rules`) `isDefiniteResult`: 04.3-03의 확정 판정 여덟(`wrong` · `locked` · `hardLocked` · `ok` · `submitted` · `closed` · `expiredProof` · `validationErrors`)은 그대로 true(**`hardLocked` · `expiredProof` 포함 — 지우지 않는다**) + 제출 결과 `saved`(E5) · `alreadySubmitted` · `invalid` · `rrnRecheck`를 더해 열두 가지 true · `throttled` · `serverError` · `undefined` · 모르는 `kind`에 false
    - (단위 `flow-rules` — 되물음 표시는 값에 묶인다, codex-final3-B 2, RED) `nextRrnRecheckConfirmed({armedRrn: "9304122123458", rrn: "9304122123458"})` → true · `rrn: "9304122123459"`(값이 바뀜) → false · `armedRrn: null`(되물음을 받은 적 없음) → false. `armedRrn`은 `rrnRecheck` 응답을 받은 요청이 보낸 주민등록번호(앞 6 + 뒤 7을 이은 13자리)이고 인자에 직전 결과가 없다 — 그래서 사이에 결과 불명이 끼어도 값이 같으면 true다(「직전 결과가 `rrnRecheck`일 때만」으로 판정하면 결과 불명 뒤 false가 되어 같은 키의 본문이 바뀌고 서버가 다시 되묻는다)
    - (단위 `flow-rules` — 스키마 거절을 칸 오류로, RED) `submitOutcomeFromValidationErrors({signature: {_errors: ["…"]}})` → `{kind: "invalid", fields: ["signature"]}`(서명 길이 초과 = 서명 칸 오류, 결과 불명이 아니다) · 주민등록번호 뒤 칸과 이름이 함께 오면 → `fields`가 E4 시각 순서(`name` 먼저, 두 주민등록번호 칸은 `rrn` 하나로) · 화면에 칸이 없는 키만 오면(`idemKey` · `winnerVersion` 등 클라이언트가 만든 값) → `null`
  </behavior>
  <action>
① **RED → GREEN `rrn-paste.ts`(순수):** `splitRrnPaste` · `shouldAdvance`와 `test/unit/certs/rrn-paste.test.ts`. 같은 방식으로 `flow-rules.ts`의 `isDefiniteResult`에 제출 결과 종류를 더하고 `nextRrnRecheckConfirmed` · `submitOutcomeFromValidationErrors`를 더한다(`test/unit/certs/flow-rules.test.ts`에 위 세 줄을 먼저 써 실패를 본다 — 04.3-03 케이스는 그대로 둔다). `submitOutcomeFromValidationErrors(validationErrors)`는 액션 입력 스키마의 칸 이름을 domain `invalid`의 칸 이름(`name` · `rrn` · `address` · `phone` · `consent` · `signature` — 04.3-02 `{kind: "invalid", fields}`와 같은 이름)으로 바꿔 `_errors`가 있는 칸만 E4 시각 순서로 모은다. zod 메시지는 화면에 쓰지 않는다(문장은 칸별 Copywriting 문장 — `invalid`와 같다).

② **`rrn-fields.tsx`(`"use client"`, 화면 전용 부품 「주민등록번호 두 칸」):** 보이는 라벨 `주민등록번호`가 `role="group"` · `aria-labelledby`의 이름. 앞 칸(`aria-label="주민등록번호 앞 6자리"`, `inputmode=numeric`, maxlength 6, placeholder `930412`) - 뒤 칸(`aria-label="주민등록번호 뒤 7자리"`, `type=password`, `inputmode=numeric`, maxlength 7, `autocomplete="off"`, placeholder `•••••••`). 자동 이동은 `shouldAdvance`일 때만 · 붙여넣기는 `splitRrnPaste`(뒤 칸 끝으로 포커스). 오류 때 두 칸 `--danger` 테두리 + `aria-invalid`, 칸 아래 한 줄의 id를 두 칸의 `aria-describedby`에. CSS는 `composes: input inputExternal` · `composes: error errorExternal`(개정 ⑦ (a)).

③ **`consent-block.tsx`:** 서명 바로 위, 위아래 1px `--line`. 체크박스 20×20(라벨 전체가 클릭 영역, 44 확보) + 라벨 `개인정보 수집·이용에 동의합니다`(`--fs-md`) + 요약(Copywriting E4 동의 요약, 택배면 수집 항목에 주소, `{보존 기간}` = 확인 응답의 `consent.retentionYears` — 04.3-02 동의 묶음) + 3차 「전문 보기」 ↔ 「전문 접기」(`aria-expanded` · `aria-controls`) — 요약 아래 제자리 펼침(`--fs-md --fg`, 행간 1.6, `max-width: 64ch`). **전문 초안**을 이 파일에 쓴다: 근거법령 · 수집 항목(이름 · 주민등록번호 · 주소(택배일 때) · 연락처 · 서명 이미지 · 담당자가 등록한 이름과 전화번호) · 목적(기타소득 세무 신고와 경품 전달) · 보존 기간(기산점 = 제출한 해가 끝나고 법정 신고기한이 지난 날, `{보존 기간}`년) · 파기 · 제공처(국세청 세무 신고 목적만) · 거부 권리와 불이익. 수령자 화면 금지어 둘은 쓰지 않는다(개정 ①-g). 전문을 04.3-02 초안에서 바꿨으면 `CERT_CONSENT_VERSION`(`domain/certs/consent.ts`)을 올린다(동의 묶음이 옛 버전으로 제출되는 것을 막는다). **법무 확인 전 초안**이라는 사실과 확인 후속 항목을 SUMMARY에 적는다(법무 확인은 사람이 한다 — 이 플랜의 태스크가 아니다).

④ **`intake-flow.tsx` E4 완성 — 아래 순서로:**
  - (a) **폼 모양:** 경품 블록(`tabindex=-1`, 라벨 `경품` · 값 `{경품명} {수량}개`) + 바로 아래 문의 줄(행사 사본 번호, `tel:` 3차) · 이름·연락처 칸은 비워 둔다(A3) · 택배 자리만 주소 칸 + 부제 `택배로 보내 드립니다`.
  - (b) **막힘 이유:** 제출 줄 = **빈 칸만** 나열(서명만 남으면 `서명을 해 주세요`). 색은 UI-SPEC Color 「막힘 이유의 색」 규칙 그대로 — 빈 칸 나열은 Phase 4 `Button`의 `reasonTone="info"`(`--muted`), 서버 거부 요약(`invalid` · 주민등록번호 오류)과 결과 불명 줄만 `block`(`--danger`)이다(/plan-design-review X-4 · 개정 ①-m ②).
  - (c) **보내는 값:** 두 칸 · 서명 PNG · 제출 키 + 확인 응답에서 받은 `version`(→ `winnerVersion`) · `consent.version`(→ `consentVersion`) · `consent.retentionYears`(→ `retentionYears`)를 그대로 되돌려 보낸다(Codex #7 · 04.3-02 동의 묶음 — 서버가 잠근 뒤 비교). `rrnRecheckConfirmed` = `nextRrnRecheckConfirmed({armedRrn, rrn: 지금 두 칸 값})`(codex-final3-B 2) — `armedRrn`은 메모리 상태로, `rrnRecheck` 응답을 받으면 그 요청이 보낸 주민등록번호로 정하고 결과 불명(연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가)이나 다른 결과로는 바꾸지 않으며 E5 · E6 · E2로 값을 버릴 때 함께 버린다. 그래서 되물음 뒤 값을 그대로 두고 보낸 요청이 결과 불명이면, 다시 누른 요청은 (d)의 규칙대로 같은 키이고 본문도 같다(`rrnRecheckConfirmed: true` 유지) — 서버가 다시 되묻지 않는다. 값을 바꾸면 비교가 어긋나 false가 되고(표시가 풀린다) 키도 새로 만든다.
  - (d) **제출 키:** E3와 같은 규칙 — `isDefiniteResult`가 참인 응답을 받을 때만 끝남 · 거짓이면 같은 키 · 값을 고치면 새 키.
  - (e) **결과 갈래:** `saved` → E5 · `invalid` → 칸 아래 문장 + 제출 줄 요약(Copywriting E4 주민등록번호·연락처 오류) + 포커스는 시각 순서상 첫 오류 칸 · **액션 결과에 `validationErrors`가 있으면**(zod 거절 — 서명 길이 초과 등, 결과 불명이 아니라 확정 판정이다: `isDefiniteResult`가 이미 참) `submitOutcomeFromValidationErrors`로 바꿔 `invalid`와 **같은 갈래**(칸 아래 문장 · 제출 줄 요약 · 첫 오류 칸 포커스 — 서명이면 서명 칸)로 보낸다 · 그 함수가 `null`이면(화면에 칸이 없는 클라이언트 값만 거절 — 수령자가 고칠 칸이 없다) 결과 불명 제출 줄을 쓴다(새 문장을 만들지 않는다) · `rrnRecheck` → 주민등록번호 오류 줄(04.3-02가 정한 기존 E4 주민등록번호 오류 문장 자리) + 포커스 앞 칸, 값 유지 · `expiredProof` → E3(칸 아래 `확인 시간이 지났습니다 · …`) · `alreadySubmitted` → E6-a · `closed` → E6-b(값 버림) · 결과 불명 → 제출 줄 `제출됐는지 확인하지 못했습니다 · 다시 눌러 주세요 · 적은 내용은 남아 있습니다`.
  - (f) **값의 주인:** E4 값(서명 획 포함)은 확정된 자리 id에 묶어 메모리에 두고, 확인 시간 지남으로 E3에 갔다가 같은 자리 확인을 다시 통과하면 되살리며(새 확인 응답의 `version` · `consent`로 (c)의 값을 바꾼다 — 경품·전달이 바뀌었으면 폼이 새 값으로 선다), E2 · E6로 가면 버린다.
  - (g) **기록 항목:** E3 → E4 · E4 → E5/E6 · E4 → E3 모두 `replaceState`(항목은 둘뿐, 04.3-03 규칙과 고아 항목 규칙 그대로).
  - (h) **E5:** 폼·경품 블록·제출 줄을 걷고 결과 블록 세 줄(행사 사본 번호) · 포커스 첫 줄(`tabindex=-1`) · `aria-live="polite"` · 메모리 값 지움 · `document.title` `제출됨 · 기타소득 지급 확인`. 브라우저 저장소 API를 부르지 않는다.

⑤ 커밋 `feat(04.3): complete recipient form, consent and submit outcomes`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/rrn-paste.test.ts test/unit/certs/flow-rules.test.ts test/unit/certs/public-route-boundary.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>test "$(grep -rn 'localStorage\|sessionStorage\|indexedDB' 'app/c' | wc -l)" = 0 &amp;&amp; test "$(grep -rhoE '원천징수|지급명세서' 'app/c' | wc -l)" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 외부 화면이 브라우저 저장소를 쓰거나 수령자 화면 금지어가 들어갔다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `rrn-fields.tsx`에 `role="group"`과 두 `aria-label`(`주민등록번호 앞 6자리` · `주민등록번호 뒤 7자리`)이 있다
    - `consent-block.tsx`에 `aria-expanded`와 `전문 접기`가 있다
    - `intake-flow.tsx`에 `제출됐는지 확인하지 못했습니다`와 `확인 시간이 지났습니다`가 있다
    - `intake-flow.tsx`가 제출에 `winnerVersion` · `consentVersion` · `retentionYears` · `rrnRecheckConfirmed`를 싣는다(`grep -c` 넷 모두 1 이상)
    - `flow-rules` 단위 테스트에 제출 결과 종류와 `nextRrnRecheckConfirmed` 케이스(값 같음 true · 값 바뀜 false · 되물음 없음 false — 인자에 직전 결과가 없다)가 있다
    - `flow-rules` 단위 테스트에 `submitOutcomeFromValidationErrors` 케이스 셋(서명 길이 초과 → `invalid` `["signature"]` · 여러 칸 시각 순서 · 칸 없는 키만 → `null`)이 있고 녹색이다 · `intake-flow.tsx`가 제출 결과의 `validationErrors`를 이 함수로 넘긴다(`grep -q 'submitOutcomeFromValidationErrors' 'app/c/[token]/intake-flow.tsx'`)
    - `app/c` 아래 브라우저 저장소 API 사용 0건 · 금지어 0건
    - E4 제출 막힘 이유(빈 칸 나열 · `서명을 해 주세요`)의 1차 `Button`이 `reasonTone="info"`를 받고, `block`은 서버 거부 요약 · 결과 불명 줄에만 쓰인다(X-4 — `grep -c 'reasonTone' 'app/c/[token]/intake-flow.tsx'`가 1 이상)
  </acceptance_criteria>
  <done>E4 폼이 UI-SPEC E4 그대로 서고, 제출 결과 여섯 갈래가 각자의 화면으로 이어지며, 값은 메모리에서 주인을 따라 살거나 버려진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 서명 캔버스 계약(개정 ①-i) — 논리 영역 · 키보드 그리기 · 상태 알림 + 375px 제출 E2E</name>
  <files>app/c/[token]/signature-geometry.ts, app/c/[token]/signature-pad.tsx, test/unit/certs/signature-geometry.test.ts, test/e2e/mobile-cert-submit.spec.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 E4 「서명(개정 ①-i)」 전문(모양 · 획 보존 · 서명 있음 판정 · 키보드·보조기술 · 보내기) · Copywriting E4 서명 칸 · 키 안내 줄 · 서명 상태 알림
    - `app/c/[token]/signature-pad.tsx`(04.3-02 산출물 — 논리 영역 저장 · 배율 · PNG 다시 그리기가 이미 있다)
    - `docs/design/system/external-cert.html` `.sig` 부분 — 기준선 위치 · 자리표시 · 「다시 쓰기」 자리
    - `test/e2e/mobile-cert-intake.spec.ts`(04.3-02) · `test/e2e/mobile-cert-verify.spec.ts`(04.3-03) — 375px 폭 설정 · 증표 얻기 · `page.route`·`page.clock` · 직접 POST 사용 방식
    - `test/e2e/helpers/cert.ts` · `test/e2e/cert.setup.ts`(04.3-02, C4) — `createCertEvent` · `withCertFeatureOff` · `CERT_E2E_CONTACT_PHONE`. 이 태스크는 읽기만 한다 — 제출 입력에 맞춘 `seedSubmittedCert()`의 `submitCertificate` 호출 줄은 Task 1 ⑤-b가 이미 고쳤다(04.3-02 C4 규칙 · X1 — 표본 방법은 그대로, 같은 웨이브의 04.3-07은 읽기만 한다)
    - `playwright.config.ts` — 이름에 `cert`가 든 스펙은 `certs` 프로젝트(workers 1)에서 돈다(04.3-02) — 폰 폭은 스펙이 `test.use`로 준다
  </read_first>
  <behavior>
    - `toLogical({x: 208, y: 80}, cssWidth 416)` → 배율 0.8 → `{x: 260, y: 100}` · 영역 밖 `{x: -10, y: 500}` → `{x: 0, y: 200}`(잘라 넣기)
    - 캔버스 폭이 416 → 208 → 832로 바뀌어도 논리 점은 그대로이고 화면 점 = 논리 × 새 배율(원은 원)
    - `moveCursor({x: 24, y: 155}, "ArrowRight", {shift: false})` → `{x: 32, y: 155}` · `shift` → 24 이동 · 두 방향키 → 대각선 · 가장자리에서 더 가면 그대로 + `edge: true`
    - `gridCellName({x: 260, y: 100})` → `가운데` · `{x: 10, y: 190}` → `왼쪽 아래`
    - (E2E, 375px) 증표를 얻어 E4 → 이름·연락처 비어 있음 → 앞 칸에 `930412-2123458` 붙여넣기 → 두 칸 채워짐 · 포커스 뒤 칸 → 캔버스에 짧은 점 하나 → 제출 줄 `서명을 해 주세요` → 같은 자리를 짧게 네 번 왕복한 낙서(획 길이는 논리 24를 넘지만 잉크가 모자란 획 — checker-B W5) → 제출 줄 여전히 `서명을 해 주세요`(서버와 같은 잉크 판정) → 「다시 쓰기」 → 키보드로 캔버스 포커스 → 키 안내 줄 보임 → `Space` · 방향키 여러 번 · `Space` → 상태 알림 `서명이 들어갔습니다`와 접근 이름 `서명 칸 · 서명함` → `Backspace` → `마지막 획을 지웠습니다 · 획 {n}개` → 다시 긋기 → 동의 · 제출 → E5 세 줄 · 포커스 첫 줄 · 제목 `제출됨 · 기타소득 지급 확인`
    - (E2E) 첫 제출 요청을 `page.route`로 응답 전에 끊어(서버는 처리하게 두고 응답만 버림) → 결과 불명 문장 · 값 유지 → 다시 누름 → E5, DB 제출 줄 하나
    - (E2E — 되물음 뒤 결과 불명, codex-final3-B 2) 검증번호가 틀린 주민등록번호(`930412-2123459` — 맞는 검증번호는 8이라 `validateRrn`이 `mismatch`로 본다)로 제출 → 주민등록번호 오류 줄(`rrnRecheck`) → 두 칸을 그대로 두고 다시 누른 요청을 `page.route`로 서버에 넘기지 않고 `route.abort()`(서버에 닿지 않는다) → 결과 불명 문장 · 값 유지 → 다시 누름 → 끊긴 요청과 다시 보낸 요청의 본문(`request.postData()`)에서 읽은 제출 키가 같고 둘 다 `rrnRecheckConfirmed`가 true → 주민등록번호 오류 줄이 다시 서지 않고 E5 · DB 제출 줄 하나
    - (E2E) 둘 남은 행사에서 한 자리 제출 뒤 다른 브라우저 컨텍스트로 마지막 자리 제출 → 첫 컨텍스트가 링크를 다시 열면 E6-b `당첨자 모두 제출했습니다 · …`
    - (E2E) 택배 자리는 주소 칸과 부제 `택배로 보내 드립니다`가 있고, 현장 자리는 없다
    - (E2E) 경품 블록 아래 문의 줄의 `tel:` 값이 숫자만이다
    - (E2E, C1 직접 POST) 기능이 켜진 상태에서 실제 제출 요청 하나를 `page.route`로 서버에 넘기지 않고 잡아(`Next-Action` 헤더와 본문 저장 — 이 요청은 서버에 닿지 않는다) → `withCertFeatureOff` 범위 안에서 같은 헤더·본문으로 `page.request.post` → 그 자리의 제출 줄이 없고 `cert_signature_uploads` 줄도 없으며 로컬 저장소에 그 자리 객체가 없다 → 범위를 나와 같은 요청을 보내면 저장된다(요청 모양이 맞았음을 증명)
  </behavior>
  <action>
① **RED → GREEN `signature-geometry.ts`(순수, 브라우저 API 없음):** `LOGICAL_WIDTH = 520` · `LOGICAL_HEIGHT = 200` · `BASELINE_Y = 155` · `toLogical(point, cssWidth)`(배율 `cssWidth ÷ 520` 하나를 x·y에, 영역 안으로 잘라 넣기) · `toScreen` · `moveCursor`(8 / `Shift` 24 / 두 키 대각선, 영역 밖으로 안 나감, 가장자리 표시) · `gridCellName`(3×3, 가운데는 `가운데`) · `exportScale = 2`(1040×400). 위 `behavior` 단위 케이스를 `test/unit/certs/signature-geometry.test.ts`에 먼저 쓴다. 획 길이로 「서명 있음」을 재는 함수는 만들지 않는다(잉크 측정 하나 — ②).

①-b **E2E 먼저(RED — 최종 리뷰 B4):** ②(서명 UI)를 쓰기 **전에** ③의 규칙대로 `test/e2e/mobile-cert-submit.spec.ts`에 위 `behavior` E2E 줄 전부를 쓰고 돌려, 서명 UI 단언 — 키보드 그리기(캔버스 포커스 · 키 안내 줄 · `Space`/방향키 획 · `Backspace`) · 상태 알림(`서명이 들어갔습니다` · `마지막 획을 지웠습니다 · 획 {n}개`)과 접근 이름 `서명 칸 · 서명함` · 잉크 판정(점 하나 · 왕복 낙서 뒤 제출 줄 `서명을 해 주세요`) — 이 04.3-02 캔버스에서 실패함을 확인하고 실패한 단언 줄을 SUMMARY에 적는다(RED). 제출 흐름 단언(붙여넣기 · 결과 불명 재전송 · 자동 닫힘 · 직접 POST)은 Task 1·2 뒤라 이미 녹색일 수 있다. 커밋 `test(04.3): failing e2e for signature canvas contract`. 그다음 ②로 녹색.

② **`signature-pad.tsx`:** UI-SPEC E4 「서명」 전문 그대로 — `<canvas>` 폭 100% · `aspect-ratio: 520 / 200` · `1px --line-ui` 테두리 · 기준선(논리 y 155, `1px dashed --line`, 좌우 `--s-4` 안쪽) · 빈 칸일 때만 `여기에 서명`(`--fs-sm --faint`) · 선 색 `getComputedStyle`의 `--fg` · 화면 3px · 비트맵 = CSS 크기 × `devicePixelRatio` · `ResizeObserver`로 다시 그림 · `pointercancel`/`pointerleave` = 획 끝. 키보드: `tabindex=0` · `role="application"` · `aria-label` 두 상태 · `aria-describedby` → 키 안내 줄 id(`:focus-visible`일 때만 보이는 줄 + 같은 id 안 `.sr-only` 통과 제스처 문장) · 펜 십자 8px `1px --fg`(처음 논리 (24, 155)) · `Space`/`Enter` 펜 · 방향키 · `Backspace` 마지막 획 지우기(펜을 댄 채면 그 획을 지우고 펜을 뗀다) · `Tab`은 떠난다. `.sr-only` `aria-live="polite"` 한 곳이 Copywriting 「E4 서명 상태 알림」의 여섯 문장을 알린다(이동 한 칸마다 알리지 않는다). 「다시 쓰기」(잉크가 조금이라도 있으면, 44 높이) → 비우고 포커스 캔버스 · `서명을 모두 지웠습니다`. PNG: 오프스크린 1040×400에 모든 점 × 2 · 굵기 6px · 바탕 투명 · 기준선·자리표시·십자 없음. **「서명 있음」(checker-B W5 — 잉크 측정 하나):** 획이 끝날 때마다(포인터 뗌 · 키보드 펜 뗌 · `Backspace`) 그 오프스크린 이미지의 `getImageData(…).data`를 Task 1의 `countInkPixels`로 세어 `SIGNATURE_MIN_INK_PIXELS` 이상일 때만 참 — 둘 다 `domain/certs/signature-ink.ts`에서 import하고 이 부품에 숫자를 따로 두지 않는다. 제출은 같은 오프스크린 이미지를 PNG로 보내므로 서버가 세는 픽셀과 같다(움직이는 동안에는 세지 않는다). 역할 설명 속성은 두지 않는다(이름과 같은 말이 두 번 읽힌다, UI-SPEC E4). 색·크기는 토큰만(새 색 금지).

③ **E2E `test/e2e/mobile-cert-submit.spec.ts`(C4) — ①-b가 ②보다 먼저 쓰는 스펙의 규칙:** 위 `behavior` E2E 줄 전부. 행사는 `createCertEvent`로 테스트마다 고유 이름으로 만들고(당첨자 옵션 — 택배 · 둘 남은 행사 — 은 헬퍼 인자로, 헬퍼가 받지 않는 것은 스펙이 자기 행사 행만 `db/client`로 고친다), 증표는 화면 흐름(E2 → E3)으로 얻는다. 기능·문의 전화는 `cert.setup.ts`가 켜 두므로 전역 설정을 스펙이 바꾸지 않고, 기능 끄기는 `withCertFeatureOff` 안에서만. 스펙 머리 `test.describe.configure({ mode: "serial" })` · 프로젝트가 폰 폭을 주지 않으면 `test.use({ viewport: { width: 375, height: 800 } })`. 결과 불명 재현은 `page.route`로 액션 요청을 서버에 넘긴 뒤(`route.fetch()`) 응답 대신 `route.abort()`. 전역 목록·전역 행 수를 단언하지 않는다(자기 행사·자기 자리만).

④ 커밋 `test(04.3): failing e2e for signature canvas contract`(①-b) → `feat(04.3): signature canvas contract with keyboard drawing`(②), 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/signature-geometry.test.ts test/unit/certs/signature-png.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/mobile-cert-submit.spec.ts test/e2e/mobile-cert-intake.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `signature-pad.tsx`에 `role="application"` · `aria-live` · `aspect-ratio`(또는 CSS 모듈의 `aspect-ratio: 520 / 200`)가 있고 `aria-roledescription`이 없다
    - `signature-geometry.ts`가 `window`·`document`를 참조하지 않는다
    - 크기 변경 불변 케이스가 단위 테스트에 있다 · `signature-pad.tsx`가 `countInkPixels`와 `SIGNATURE_MIN_INK_PIXELS`를 `domain/certs/signature-ink`에서 import하고(`grep -q 'signature-ink' 'app/c/[token]/signature-pad.tsx'`) 획 길이 판정 함수가 없다(`grep -c 'strokeLength\|hasSignature' 'app/c/[token]/signature-geometry.ts'`가 0)
    - E2E가 왕복 낙서에서 제출 줄이 `서명을 해 주세요`로 남음을 단언한다
    - (B4) git 기록에서 `test/e2e/mobile-cert-submit.spec.ts`의 실패 커밋(`test(04.3): failing e2e for signature canvas contract`)이 `signature-pad.tsx`를 고친 커밋보다 앞서고, SUMMARY에 서명 UI 단언의 RED 실행 결과가 있다
    - E2E가 키보드 서명 · 붙여넣기 · 결과 불명 재전송 · 주민등록번호 되물음 뒤 결과 불명 재전송(같은 제출 키 · `rrnRecheckConfirmed` true 유지 · E5) · 자동 닫힘 · 기능 꺼짐 직접 POST 여섯 흐름을 단언하고 녹색이다
    - E2E 스펙이 전역 설정 키를 직접 쓰지 않는다(`grep -cE "cert\\.(enabled|contact_phone)" test/e2e/mobile-cert-submit.spec.ts`가 0)
  </acceptance_criteria>
  <done>서명은 손가락·키보드 어느 쪽으로도 같은 논리 영역에 그려지고 잘리지 않으며, 375px 제출 흐름이 재전송·자동 닫힘까지 E2E로 고정된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 수령자 폰 → `submitCertificate` | 이름 · 주민등록번호 · 주소 · 연락처 · 서명 PNG · 제출 키 모두 신뢰할 수 없는 입력이다 |
| 공유 링크를 가진 여러 사람 → 같은 행사 | 동시 제출이 같은 행사 행·같은 자리에 겹칠 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-07 | Tampering | 같은 자리 이중 제출 · 경합 | high | mitigate | `lockEventRow`(행사 → 자리 잠금 순서, 04.3-03) + 자리 `FOR UPDATE` + 자리 조건부 갱신(`submitted_at IS NULL`) + `winner_id` UNIQUE. 진 쪽 객체는 보상 삭제한다. 동시 제출 통합 테스트 두 케이스(같은 자리 · 마지막 두 자리) |
| T-04.3-36 | Repudiation | 서명의 주인 | medium | mitigate | 제출은 그 자리의 증표(뒤 4자리 확인 뒤 30분)를 가져야 하고, E4 값은 확정된 자리 id와 확인 응답의 `version`에 묶여 다른 자리 · 바뀐 자리로 넘어가지 않는다. 제출 뒤 서명은 고칠 수 없다(D-1106, 04.3-05 덮어쓰기 금지) |
| T-04.3-37 | Denial of Service | 서명 PNG | medium | mitigate | 액션 본문 `MAX_ACTION_PAYLOAD_BYTES` 262,144(04.3-02) + zod `max(SIGNATURE_BASE64_MAX_LENGTH)` + `inspectSignaturePng`의 `SIGNATURE_MAX_PNG_BYTES` 184,320 · 시그니처 · IHDR(1040×400 · 8비트 RGBA · 인터레이스 없음) 검사. 저장소에 올리기 전에 거부한다. 본문 한도 예산은 단위 테스트가 `checkPayloadSize`로 증명한다 |
| T-04.3-38 | Information Disclosure | 브라우저에 남는 값 | high | mitigate | 입력값·서명 획은 메모리에만, E5·E6·E2 이동 때 지운다. 외부 경로 전체에서 브라우저 저장소 API 0건(verify 음성 검사). 뒤 칸은 `type=password` |
| T-04.3-39 | Tampering | 제출 키 재생 | low | mitigate | 재생은 그 자리에 저장된 제출의 키 해시와 같을 때만 E5를 돌려준다(증표 전에 허용되는 유일한 판정) — 다른 자리·다른 키는 재생되지 않는다. 상태를 바꾸지 않은 판정은 키에 묶지 않는다. 키는 해시로만 저장 |
| T-04.3-40 | Spoofing | 자동 닫힘 악용 | low | accept | 마지막 자리를 남이 제출하면 링크가 닫힌다 — 남의 자리 제출은 그 자리의 뒤 4자리 확인이 막고(04.3-03), 닫힌 뒤에도 E6-b에 문의 전화가 있다 |
| T-04.3-82 | Denial of Service / Tampering | 빈 서명 · 압축 폭탄 PNG | medium | mitigate | 서버가 클라이언트 「서명 있음」 판정을 믿지 않는다 — `inspectSignaturePng`가 IDAT를 `maxOutputLength` 1,664,400바이트로 풀고(넘으면 `format`) 알파가 0이 아닌 픽셀이 `SIGNATURE_MIN_INK_PIXELS` 288 미만이면 `blank`. 클라이언트 「서명 있음」도 같은 `countInkPixels` · 같은 상수라 화면이 받은 서명을 서버가 거절하는 어긋남이 없다(checker-B W5). 경계(24/23 · 점 · 투명 · 왕복 낙서)와 압축 폭탄은 단위 테스트 |
| T-04.3-83 | Information Disclosure | 제출 여부 노출(Codex #6) · 잠그기 전 값으로 저장(Codex #7) | high | mitigate | 증표 확인 전에는 같은 키 재생만 판정하고 제출 여부를 말하지 않는다 — 제출된 자리는 증표 해시가 비워져 다른 키 요청이 미제출 자리의 틀린 증표와 같은 `expiredProof`를 받는다(키 집합까지 같음을 단언). 트랜잭션 안에서 잠긴 값(닫힘 · 기한 · 증표 · `version` · 동의 묶음)으로 다시 판정한다 — `put`을 멈춘 주입 저장소로 순서를 고정한 통합 테스트 넷 |
| T-04.3-84 | Information Disclosure | 고아 서명 객체(C3) | medium | mitigate | 업로드 전에 의도 줄(`insertSignatureUploadIntent`)을 커밋하고 저장 트랜잭션이 `deleteSignatureUploadIntent(…, tx)`로 지운다. 저장하지 않기로 판정된 요청은 `store.delete`가 성공(404 포함)해야만 의도 줄을 지우고, 실패하면 남겨 04.3-12가 24시간 뒤 지운다. 저장 트랜잭션이 예외로 끝난 요청은 의도 줄을 지우지 않는다(T-04.3-190). 통합 테스트가 성공 · 재생 · 경합 · 보상 삭제 실패 · 예외 갈래 모두에서 의도 줄 수를 단언한다 |
| T-04.3-190 | Tampering / Repudiation | 커밋 결과 불명 뒤 보상 삭제(codex-final3-B 1 — `commit` 응답만 잃은 정상 제출의 서명을 지움) | high | mitigate | 예외만으로 객체를 지우지 않는다(③ (g) 예외 갈래). `lockEventRow` → 자리 `FOR UPDATE`로 실패한 트랜잭션이 끝나기를 기다린 뒤 `findSubmissionBySignatureKey`로 이 객체 키의 제출 줄을 찾아 — 있으면 객체를 두고 `saved`, 없으면 지우고 예외, 조회 실패면 아무것도 지우지 않는다(의도 줄은 저장 트랜잭션 안에서만 지워지므로 04.3-12가 남은 줄로 고아만 치운다). 커밋 뒤 응답 유실 주입 통합 테스트(RED 먼저)가 객체 보존과 같은 키 재전송 `saved`를 단언한다 |
| T-04.3-146 | Repudiation | 제출 로그 누락(커밋 뒤 로그 INSERT 실패 — 같은 키 재전송은 곧바로 성공을 재생해 로그가 영구히 빠짐) | medium | mitigate | `document_submit`을 제출 저장과 같은 트랜잭션에서 쓴다(③ (h) — `recordAction(…, { tx })`, E3-02). 로그가 실패하면 제출 전체가 롤백되고 재전송이 새로 저장한다 — 로그 실패 주입 통합 테스트가 제출 줄 0 · 로그 0 → 재전송 뒤 1 · 1을 단언한다 |
| T-04.3-164 | Tampering / Repudiation | 구조가 깨진 서명 PNG(잉크는 충분 · 청크 CRC 틀림 · IEND 없음 · IEND 뒤 바이트) | medium | mitigate | `inspectSignaturePng`가 청크마다 CRC-32(`node:zlib` `crc32`)를 대조하고 마지막 청크가 IEND이며 그 뒤 바이트가 없을 때만 받는다 — 저장소에 올리기 전에 칸 오류 `signature`. 인쇄(04.3-11 `decode()`)에서 풀리지 않는 서명이 확인증 증거로 남지 않는다. 단위 세 표본 + 통합 두 케이스(객체 0 · 의도 줄 0) |
| T-04.3-165 | Repudiation | 제출 로그 INSERT가 트랜잭션 밖으로 샘 · tx 로그 도우미가 둘로 갈라짐 | low | mitigate | 로그는 Phase 4 `recordAction(…, { tx })` 하나로 쓴다 — INSERT가 같은 tx로 가고, 설정 판정은 tx 전에 읽은 값(`isActionTypeEnabled`)이라 잠금을 쥔 채 풀의 두 번째 연결을 잡지 않는다. 이 플랜은 tx 로그 도우미를 정의하지 않는다(04.3-02의 대비 파일 밖 정의 0 — Task 1 verify 음성 검사, E3-02). 로그 실패 주입 통합 테스트가 전체 롤백을 증명한다 |
| T-04.3-16 | Elevation of Privilege | 기능 꺼짐 뒤 제출 액션 직접 POST(C1) | high | mitigate | `submitCertificateAction` 첫 문장 `await assertCertFeatureEnabled()` + domain의 `isCertFeatureEnabled` 거부. 통합 테스트(저장·업로드·의도 줄 없음)와 E2E 직접 POST(`withCertFeatureOff`) |
</threat_model>

<verification>
- 단위 `signature-png` · `flow-rules` · `rrn-paste` · `signature-geometry` · `public-route-boundary` 녹색(본문 한도 예산 케이스 포함)
- 통합 `cert-submit` · `cert-intake` · `cert-verify` 녹색(비노출 · 잠근 뒤 재판정 넷 · 보상 삭제 실패 · 커밋 결과 불명 · C1 케이스 포함)
- `domain/certs/intake.ts`에 200KB 상한이 남지 않고 `app/c/[token]/actions.ts`의 서명 zod 상한이 `SIGNATURE_BASE64_MAX_LENGTH`다
- E2E `mobile-cert-submit` · `mobile-cert-intake` 녹색
- `pnpm lint && pnpm typecheck && pnpm build` 녹색
- backstop 셋(360px·200% · 실기기 캔버스 · 보조기술)은 04.3-13의 독립 DOM 감사와 사람 확인이 판정한다
- 통합 · E2E는 같은 웨이브의 다른 플랜과 동시에 돌리지 않는다 — 세션당 플랜 하나(E3-33, `erp_test` · 포트 3100 공유)
</verification>

<success_criteria>
- 한 자리 한 번 저장 · 재전송 안전 · 마지막 제출이 링크를 한 번 닫는다
- 증표 확인 전에는 제출 여부가 새지 않고, 저장은 잠긴 값으로 다시 판정한 뒤에만 일어난다
- 서버가 빈 서명 · 184,320바이트 초과 · 형식이 틀린 PNG를 업로드 전에 거부하고, 최대 입력이 262,144바이트 본문 한도 안에 든다
- 업로드된 서명 객체는 확인증이나 의도 줄 둘 중 하나에 반드시 기록되고, 커밋 결과 불명이어도 확인증이 가리키는 객체는 지워지지 않는다(C3 · T-04.3-190)
- 기능이 꺼지면 제출 경로 전체가 404이고 직접 POST도 아무것도 저장하지 않는다(C1)
- E4 폼과 서명 캔버스가 UI-SPEC E4 계약 그대로다
- 제출값이 브라우저 저장소·URL에 남지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `submitCertificate` 완성(재생 · 비노출 · 잠근 뒤 재판정 · 의도 줄 · 경합 · 자동 닫힘 · PNG 검사) |
| 상수 | `SIGNATURE_MAX_PNG_BYTES = 184_320` · `SIGNATURE_BASE64_MAX_LENGTH`(`domain/certs/signature-png.ts`) · `SIGNATURE_MIN_INK_PIXELS = 288`(`domain/certs/signature-ink.ts`) |
| 순수 모듈 | `domain/certs/signature-png.ts`(`inspectSignaturePng`) · `domain/certs/signature-ink.ts`(`countInkPixels` — 클라이언트 · 서버 공용) · `app/c/[token]/signature-geometry.ts` · `app/c/[token]/rrn-paste.ts` · `app/c/[token]/flow-rules.ts` 확장(`isDefiniteResult` 제출 결과 · `nextRrnRecheckConfirmed`) |
| 화면 부품 | `app/c/[token]/rrn-fields.tsx` · `app/c/[token]/consent-block.tsx` · `signature-pad.tsx` 완성 |
| 문서 초안 | 개인정보 수집·이용 동의 전문(법무 확인 전 초안, `consent-block.tsx`) |
| 테스트 | `test/unit/certs/signature-png.test.ts` · `test/unit/certs/flow-rules.test.ts`(확장) · `test/unit/certs/signature-geometry.test.ts` · `test/unit/certs/rrn-paste.test.ts` · `test/integration/cert-submit.test.ts` · `test/e2e/mobile-cert-submit.spec.ts` |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(/plan-eng-review E3 — 반영 지시 L387–L455@953e599). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-02 tx 로그 도우미가 둘(06 자체 도우미 · 07 사본) — L419@953e599 · L48@953e599 | P2 | Plan 06, Task 1: ③ (h)를 `recordAction(…, { tx, isActionTypeEnabled, appendActionLog })`로 바꾸고 자체 도우미 정의를 뺐다(④ · read_first · 대비책은 04.3-02 `repositories/cert-action-log.ts`의 `insertActionLogTx` import만) · 실패 주입 자리 `deps.appendActionLog` · verify 넷째 줄(02 대비 파일 밖 도우미 정의 0) · acceptance (E3-02) · T-04.3-146 · T-04.3-165 |
| E3-24 `lockEventRow` 인자 순서(tx 마지막) — L420@953e599 · L201@953e599 | P3 | Plan 06, Task 1: read_first · ③ (f) · (g) 예외 갈래 · ④에서 `lockEventRow(SYSTEM_VIEWER, eventId, tx)` · verify 넷째 줄이 옛 순서 호출 0을 단언 · acceptance (E3-24) |
| E3-01 Phase 4 머지 뒤 시작 · 웨이브 첫 단계 `git merge origin/main` · Phase 4 부품(`recordAction({tx})`) 기준 — L392@953e599 · L394@953e599 | P1 | frontmatter `execution_gate` · Task 1 `<precondition>`(Phase 4 머지 확인 — 없으면 멈춤) · objective 「실행 조건」 · Task 1 read_first(`record.ts` · `action-log.ts`를 Phase 4 main 기준 이름으로) · 개정 번호는 04.3-01 대응표로 찾는다는 한 줄 |
| E3-33 통합 · E2E 한 번에 하나(세션당 플랜 하나) — L393@953e599 | P3 | frontmatter `execution_gate.serial_tests` · objective · `<verification>` 마지막 줄 |
| E3-09 설정 · 플래그는 tx 전에 읽는다(03 소유 규칙을 06 제출 경로에도) — L111@953e599 | P2 | Plan 06, Task 1 ③ (h): `submitLogEnabled`를 tx 전에 읽어 값으로 넘긴다 — 04-32가 설정 조회를 tx로 돌릴 수 있어도 규칙 유지 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| E3-03 서명 이미지 저장 위치(GCS 대 DB bytea) — 06 (e)(g) 커밋 불명 복구 · C3 의도 줄에 걸림 — L59@953e599 · L459@953e599 | deferred — user check pending (default kept): 기본값 GCS 유지로 계획을 그대로 둔다. 사용자가 DB를 고르면 05를 빼고 02 · 06 · 11 · 12를 재계획한다 |

### Round 2 — 72829e6

> 출처: 04.3 eng-apply 재검토 r1 — Opus 플랜 체커(`eng-apply-checker-r1`, W/I 번호) · 교차 검토(`eng-apply-opus-cross-r1`, X/A 번호). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 X1 — 06이 제출 입력(`winnerVersion`)을 바꾸는데 표본 도우미를 「읽기만」으로 둠(04.3-02 C4 규칙 위반 → 07 · 10 · 11 · 12 표본 전부 빨강) | BLOCKING | frontmatter `files_modified`에 `test/e2e/helpers/cert.ts`(호출 줄만) · Task 1 `<files>` · read_first · 새 ⑤-b(같은 커밋에서 `seedSubmittedCert()`의 `submitCertificate` 호출 줄만 — 확인 결과 `version` → `winnerVersion`, 표본 방법 · 옵션 · 반환 그대로) · verify 마지막 줄(`winnerVersion` 있음 · `@playwright/test` import 0 + `fails_when`) · acceptance (X1): 02 표본 도우미 통합 케이스가 verify 둘째 줄에서 녹색 · Task 3 read_first 「읽기만」 문장을 「Task 1 ⑤-b가 이미 고쳤다」로. 같은 웨이브 4에서 이 파일을 `files_modified`에 둔 다른 플랜은 없다(07 · 09는 읽기만) |
| 체커 I1 · 교차 A2 — tx 로그 부재 정책 | info | 오케스트레이터 결정으로 이 플랜의 ③ (h) 대비책(04.3-02 `insertActionLogTx` import)을 그대로 둔다 — 10 · 11 · 12 · 01 · 13이 같은 정책으로 맞춰졌다. A2의 「없으면 멈춤으로 통일」은 채택하지 않는다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 3 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3·D-7·D-10). 문장 정본은 UI-SPEC이고 이 플랜은 그 문장을 옮겨 쓴다.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-4 정상 상태를 오류 색으로(`reasonTone`) — L194@1c3a27b | 막는 문제 | Task 2 ③ (b) 빈 칸 나열 = `reasonTone="info"`, 서버 거부 요약 · 결과 불명만 `block` · acceptance 한 줄 |
| X-3 §8-5 안심 꼬리 — L190@1c3a27b | 막는 문제 | E4 결과 불명 꼬리 `적은 내용은 남아 있습니다`와 막힘 이유 `…채우면 제출할 수 있습니다`는 UI-SPEC 개정 ①-m ①의 외부 화면 예외 두 꼴로 확정 — 이 플랜 문장 그대로(변경 없음) |
| X-11 「최종 승인본(커밋 d138427)」 — L224@1c3a27b | 참고 | objective 「UI-SPEC」 줄을 최신 승인본(`2c6277b` + 2026-09-25 `/plan-design-review` 반영)으로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| X-10 E4 값 메모리 · 앱 전환 뒤 복귀 — L220@1c3a27b | 결정은 그대로(메모리만 — 보안상 맞다, 검토자도 유지 권고). 앱 전환 뒤 복귀는 UI-SPEC UI Considerations backstop · `/qa` 실기기 항목, 증표 유효 시간 조건은 04.3-03 success_criteria가 `/cso`에 넘긴다 — 이 플랜 코드 변경 없음 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-06-SUMMARY.md` when done
</output>
