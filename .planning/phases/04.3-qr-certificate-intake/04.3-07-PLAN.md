---
phase: 04.3-qr-certificate-intake
plan: 07
type: execute
wave: 4
depends_on: ["04.3-03", "04.3-04"]
files_modified:
  - domain/permissions/info-items.ts
  - domain/settings/keys.ts
  - domain/action-log/record.ts
  - domain/certs/review.ts
  - domain/certs/privacy-session.ts
  - repositories/cert-review.ts
  - repositories/privacy-session-activity.ts
  - lib/viewer.ts
  - app/(app)/certs/submissions/[id]/page.tsx
  - app/(app)/certs/submissions/[id]/review-form.tsx
  - app/(app)/certs/submissions/[id]/rrn-field.tsx
  - app/(app)/certs/submissions/[id]/actions.ts
  - app/(app)/certs/submissions/[id]/actions.registry.ts
  - app/(app)/certs/submissions/[id]/review.module.css
  - test/integration/leak-scan.test.ts
  - test/integration/cert-crypto.test.ts
  - test/integration/cert-review.test.ts
  - test/e2e/cert-review.spec.ts
autonomous: true
requirements: [CERT-02]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 160000
  raw_tokens: 160000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "경영관리(`certs.submissions` 보기 권한)가 I4 `/certs/submissions/[id]`를 열면 주민등록번호는 기본 `930412-2******`(앞 6 + 성별 1 + `*` 6)이고 화면 DOM 어디에도 전체 번호가 없다. 권한 없는 사람(대표 · 기획 PM 포함)과 플래그 꺼짐은 404다(ROADMAP 04.3 기준 4)"
    - "「전체 보기」는 메뉴 `certs.submissions` 보기 권한과 정보 항목 `cert.rrn_unmasked`가 **둘 다** 있는 사람에게만 서버가 보내고(메뉴를 거둔 뒤 정보 항목만 남은 사람이 액션을 직접 불러도 거부 · 복호화 0번 · 로그 0줄, codex r2 C4), 누를 때마다 서버가 **행동 로그 `mask_reveal`을 먼저 기록한 뒤** 복호화한다 — 기록이 실패하면 평문이 나가지 않는다(CERT-02 × ordering, vendors 선례). 그 기록은 잠금을 쥔 같은 트랜잭션 연결로 쓰므로 풀 한도만큼 동시에 눌러도 서로를 기다리며 멈추지 않는다(codex r2 C5). 복호화할 암호문은 한 트랜잭션 안에서 `FOR SHARE`로 잠근 행에서 읽고 그 자리에서 `purged_at`을 다시 확인한다 — 파기(04.3-12의 UPDATE)는 그 잠금이 풀린 뒤에만 칸을 비우므로 「읽은 뒤 파기 커밋 → 메모리의 암호문 복호화」가 생기지 않는다(codex #13, 경합 테스트는 04.3-12)"
    - "정보 항목 `cert_submission.value`를 끄면 I4 DTO는 `project()`가 모든 값 칸을 걸러 비고 I4는 404다 — 메뉴 보기 권한이 있어도 이름 · 가린 번호 · 연락처 · 주소 · 서명이 응답에 실리지 않고, 정정 액션도 거부한다(codex r2 C3)"
    - "C1: 기능 게이트가 꺼져 있으면 I4 페이지와 세 액션(`revealCertRrnAction` · `reopenCertRrnAction` · `correctCertSubmissionAction`)이 모두 첫 줄 `assertCertFeatureEnabled()`(`lib/certs/feature-guard.ts`)에서 not-found로 끝나고(로그인한 사람에게는 게이트가 꺼지면 권한과 무관하게 404. 로그인하지 않은 요청은 `app/(app)/layout.tsx:22`의 `requireSession()`이 페이지보다 먼저 `/login`으로 보낸다 — 모든 `(app)` 경로와 같은 이동이고 데이터가 나가지 않으므로 받아들인다, Codex r3 C-2), domain 함수 넷도 맨 앞에서 `isCertFeatureEnabled()`를 보고 not-found/거부로 끝난다 — 페이지를 거치지 않고 액션 엔드포인트에 직접 POST해도 평문 · 로그 · 변경이 없다(codex #2 · UI-SPEC 「서버 액션 전부 거부」)"
    - "「가리기」는 언제나 있다. 고친 값이 없으면 평문을 메모리에서 지우고 기본 텍스트로 돌아가고, 고친 값이 있으면 입력 칸을 내리고 `{930412-1******} · 저장 안 함` + 「전체 보기」를 보인다 — 이 「전체 보기」도 서버가 권한을 다시 확인하고 `mask_reveal`을 기록한 뒤에만 연다(UI-SPEC I4)"
    - "화면이 가려지거나(`visibilitychange` hidden) 이 화면에서 개인정보취급자 비활동 시간(설정 `cert.privacy.idle_minutes`, 기본 120분, 10~120)이 지나면 「가리기」를 저절로 누른 것과 같다(고친 값은 보존)"
    - "개인정보취급자 세션: 확인증 개인정보 경로(I4 · 전체 보기 · 정정 · 인쇄)에서 마지막 활동 뒤 비활동 시간이 지난 세션은 서버가 끊고 로그인으로 보낸다 — 다른 화면의 세션 수명(30일)은 그대로다. 이 세션에 개인정보 경로 활동 기록이 아직 없으면 **첫 방문 시각을 시작점으로 기록하고 끊지 않는다** — 09시에 로그인해 지출결의에서 계속 일하던 경영관리가 11시 넘어 I4를 처음 열어도 ERP 전체가 로그아웃되지 않는다. 로그인 3시간 뒤 첫 방문은 통과하고, 그 뒤 개인정보 경로에서 한도(기본 120분)를 넘겨 쉬면 끊긴다(E3-10 — codex #23의 「로그인 시각 기준」을 바꾼다. 끊는 범위(세션 전체 대 개인정보 경로만 재인증)는 → 설계 /cso에서 확정)"
    - "D-1106: 경영관리가 이름 · 연락처 · 주소(택배만) · 주민등록번호를 고쳐 「고친 내용 저장」하면 버튼 자리에 `저장됨 · {바뀐 칸 이름 나열} · HH:mm`이 서고 입력은 새 값 기준으로 계속 고칠 수 있다. 저장마다 행동 로그 `cert_correct`(끌 수 없는 핵심 로그)에 누가 · 언제 · 어느 칸을 남기고, 값(특히 주민등록번호 평문)은 로그에 없다. 정정 UPDATE와 그 로그 INSERT는 한 트랜잭션이라 로그 쓰기가 실패하면 정정도 되돌아가고 버전도 그대로다(C6 · codex #11)"
    - "주민등록번호는 「전체 보기」 뒤에만 입력 칸이 생긴다 — 전체 보기 권한 없이 주민등록번호를 고칠 길이 화면에도 서버에도 없다(서버가 권한 없는 주민등록번호 변경을 거부). 서버는 주민등록번호를 바꾸는 정정에 쓰기 권한과 **함께** 전체 보기와 같은 두 판정(메뉴 `certs.submissions` 보기 **그리고** `cert.rrn_unmasked`)을 요구한다 — 보기를 거둔 뒤 쓰기만 남은 직원이 기존 id · version으로 직접 보내도 거부된다(codex final C1)"
    - "CERT-02 × concurrency(정정): 다른 직원이 먼저 저장했으면 거부되고 `저장하지 못했습니다 · {이름}가 {HH:mm}에 먼저 고침 · 다시 불러오기`가 서며, 값은 남고 「다시 불러오기」만 새 값을 가져온다 — 덮어쓰기·병합이 없다"
    - "서명은 읽기 전용 이미지(폭 320 · 비율 520/200, 폰 전폭)와 `수령자 서명 · 고칠 수 없음`이고 어떤 컨트롤도 없다. 이미지를 못 불러오면 `서명 이미지를 불러오지 못했습니다 · 다시 시도`(3차)가 이미지 자리에 선다(UI Considerations error I4 서명)"
    - "현장 수령 확인증은 I4에 주소 칸이 없다(UI Considerations empty I4 · CERT-02 × empty 절반)"
    - "브라우저 인쇄(`Ctrl+P`)를 하면 화면 내용은 모두 숨고 `이 화면은 인쇄하지 않습니다 · 머리의 「인쇄」 버튼으로 확인증을 열어 주세요` 한 줄만 찍힌다(UI Considerations error I4 브라우저 인쇄)"
    - "CERT-02 × encoding: 정정 뒤에도 `cert_submissions`의 주민등록번호는 `v1:` 암호문과 가린 값뿐이고 행동 로그 표의 어느 칸에도 13자리 평문·뒤 7자리가 없다"
    - "CERT-02 × empty(파기 절반): 파기된 확인증(`purged_at` 있음)은 I4가 404이고 전체 보기는 복호화하지 않고 거부한다"
    - statement: "I4 「전체 보기」 · 「고친 내용 저장」 진행 중 버튼 라벨 뒤 `…`와 다른 1차 비활성이 보인다(UI Considerations 보강 loading I4)"
      verification: backstop
  prohibitions:
    - statement: "주민등록번호 평문이 행동 로그의 detail · 서버 로그 · 브라우저 인쇄물에 들어가서는 안 된다(D-1106 · D-1108)"
      status: resolved
      verification: test
    - statement: "`decrypt()`가 `mask_reveal` 기록보다 먼저 불려서는 안 된다 — 전체 보기 경로는 `revealRrn` 하나뿐이다"
      status: resolved
      verification: test
    - statement: "서명 이미지를 바꾸거나 지우는 정정 경로가 있어서는 안 된다(D-1106)"
      status: resolved
      verification: test
    - statement: "대표 계급(`role-ceo`)이 I4 · 전체 보기 · 재열람 · 정정을 열어서는 안 된다 — 권한표 · 정보 노출표에서 켜 두어도 domain이 계급으로 먼저 거부한다(CONTEXT 「전체 보기는 경영관리만(대표도 불가)」 · UI-SPEC I4 진입 「대표 불가」 · codex #5)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/certs/review.ts"
      provides: "I4 조회(`project()` 투영) · 전체 보기(기록 먼저, 같은 tx) · 고친 값 재열람 기록 · 정정(버전 · 칸별 로그)"
      exports: ["getSubmissionForReview", "revealRrn", "recordRrnReopen", "correctSubmission", "isCertPrivacyBarredRole", "CERT_SUBMISSION_REVIEW_DTO_SPEC"]
    - path: "domain/certs/privacy-session.ts"
      provides: "개인정보취급자 비활동 판정 · 세션 끊기"
      exports: ["touchPrivacySession"]
    - path: "test/integration/cert-review.test.ts"
      provides: "정정 로그(칸 이름만) · 주민등록번호 정정 권한 · 버전 충돌 · 비활동 만료"
      min_lines: 60
    - path: "test/e2e/cert-review.spec.ts"
      provides: "I4 가림 · 전체 보기 로그 · 가리기 · 정정 · 동시 정정 · 브라우저 인쇄 · 404"
      min_lines: 50
  key_links:
    - from: "domain/certs/review.ts"
      to: "domain/action-log/record.ts"
      via: "전체 보기는 mask_reveal, 정정은 cert_correct — 둘 다 끌 수 없는 핵심 로그"
      pattern: "cert_correct"
    - from: "app/(app)/certs/submissions/[id]/page.tsx"
      to: "domain/certs/privacy-session.ts"
      via: "개인정보 경로마다 비활동 판정을 먼저 한다"
      pattern: "touchPrivacySession"
    - from: "app/(app)/certs/submissions/[id]/actions.ts"
      to: "lib/certs/feature-guard.ts"
      via: "C1 — 세 액션의 첫 줄이 assertCertFeatureEnabled()"
      pattern: "assertCertFeatureEnabled"
    - from: "domain/certs/review.ts"
      to: "domain/action-log/record.ts"
      via: "C6 — 정정 UPDATE와 cert_correct 로그를 한 트랜잭션에 · 전체 보기의 mask_reveal도 FOR SHARE를 쥔 같은 tx로 — Phase 4 recordAction(…, { tx })(codex r2 C5 · E3-02)"
      pattern: "\\{ tx"
    - from: "domain/certs/review.ts"
      to: "domain/permissions/project.ts"
      via: "I4 DTO는 project()로 투영 — cert_submission.value가 꺼지면 칸이 실리지 않는다(codex r2 C3)"
      pattern: "project\\(viewer"
    - from: "test/integration/leak-scan.test.ts"
      to: "app/(app)/certs/submissions/[id]/actions.registry.ts"
      via: "새 액션·DTO가 누수 스캔에 등록된다(ROADMAP 04.3 기준 5)"
      pattern: "certs/submissions"
---

<objective>
**As a** 경영관리(개인정보취급자), **I want to** 제출된 확인증을 가린 채로 확인하고, 필요할 때만 기록을 남기며 주민등록번호 전체를 보고, 틀린 칸을 직접 고치고 싶다, **so that** 세무 신고에 맞는 값을 남기면서 누가 언제 무엇을 보고 고쳤는지 추적되고 평문이 화면·로그·종이에 새지 않는다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — Goal의 행위자(경영관리)와 기준 4 · 5에서 이야기를 끌어냈다.

UI-SPEC 「화면 계약」 I4를 만든다: 서버 렌더 폼(이름 · 연락처 · 주소) · 주민등록번호 가림/전체 보기/가리기/고친 값 재열람 · 읽기 전용 경품 · 동의 · 서명 · 「고친 내용 저장」(버전 · 동시 정정) · 자동 가리기 · 브라우저 인쇄 차단 한 줄. 서버는 `revealRrn`(vendors `revealAccountNumber` 순서 그대로: 권한 → 기록 → 복호화) · `correctSubmission`(D-1106) · 개인정보취급자 비활동 세션(CONTEXT 「이미 확정된 입력」 2시간, 설정 10~120분)을 더한다. 머리의 「인쇄」 버튼과 인쇄 라우트는 04.3-11, I3의 「제출 내용」 진입 링크는 04.3-10이다.

**결정(재량, 설계 `/cso`가 다시 본다):** ① 비활동 판정은 확인증 개인정보 경로(I4 · 전체 보기 · 정정 · 인쇄)에서의 마지막 활동을 기준으로 하고, 넘으면 세션 전체를 끊는다(플래그 뒤 — 플래그가 꺼진 동안 이 경로가 없다). 활동 행이 없으면 **첫 방문 시각을 시작점으로 기록하고 끊지 않는다**(E3-10) — codex #23이 고른 「로그인 시각(`sessions.created_at`) 기준」은 로그인 뒤 다른 화면에서 계속 일한 경영관리를 개인정보 화면 첫 방문에서 ERP 전체 로그아웃시켰다(다른 탭의 저장 안 한 작업도 잃는다). 대가로 로그인한 지 오래된 세션도 첫 개인정보 접근에서 새 한도를 얻는다 — 이 교환과 끊는 범위(세션 전체 대 개인정보 경로만 재인증)는 → 설계 /cso에서 확정(T-04.3-17). 전 화면 활동 추적은 레이아웃이 클라이언트 이동마다 다시 돌지 않아 믿을 수 없다. ② 지금 앱에는 §6-9 「세션 만료」 변종 화면이 없어, 끊긴 세션은 기존 `requireSession`과 같이 로그인으로 보낸다. ③ 정정 로그는 핵심 로그 종류 `cert_correct`(끌 수 없음)를 새로 둔다 — 기존 `document_update`는 설정으로 끌 수 있어 D-1106 「고칠 때마다」를 보장하지 못한다. 공유 등록부라 항목만 더한다. ④ 대표 차단은 권한표 기본값에 맡기지 않고 domain의 `isCertPrivacyBarredRole(viewer)`(`viewer.roleId === "role-ceo"` — `domain/permissions/roles.ts`의 영구 식별자)로 I4 · 전체 보기 · 재열람 · 정정 · 인쇄(04.3-11) · 「제출 내용」 링크(04.3-10)를 먼저 거른다(codex #5). ⑤ 정정과 그 로그는 한 트랜잭션(C6) — Phase 4(04-32)의 `recordAction(viewer, entry, { tx })`로 쓴다(E3-02 — 이 플랜은 tx 로그 도우미를 만들지 않는다). 실행 때 `RecordActionDeps`에 `tx`가 없으면(`grep -q 'tx?: DbOrTx' domain/action-log/record.ts` 실패 — 시작 조건상 없어야 한다) 04.3-02가 만든 `insertActionLogTx`를 `@/repositories/cert-action-log`에서 import해 `appendActionLog` 주입 자리로 넘긴다(같은 이름의 함수를 따로 만들지 않는다 · 어느 쪽을 썼는지 SUMMARY에 한 줄). ⑥ C1: 세 액션과 domain 함수 넷의 첫 줄은 04.3-02의 `assertCertFeatureEnabled()`다 — I4 페이지는 `requireSession`보다도 먼저 부른다(게이트가 꺼져 있으면 로그인한 사람은 권한과 상관없이 404, checker-C I2. 로그인하지 않은 요청은 `app/(app)/layout.tsx:22` `requireSession()`이 먼저 `/login`으로 보내며 모든 `(app)` 경로와 같아 받아들인다 — 레이아웃은 고치지 않는다, Codex r3 C-2). ⑦ I4 DTO는 `registerDto`만으로 끝내지 않고 `project()`(`domain/permissions/project.ts`)로 투영한다 — `registerDto`는 누수 스캔용 선언일 뿐 요청 시점에 칸을 거르지 않는다(codex r2 C3). 모든 값 칸이 정보 항목 `cert_submission.value` 하나에 걸려 있으므로 그 항목이 꺼지면 투영 결과가 비고 I4는 404다(부분 화면 변종은 UI-SPEC에 없어서 만들지 않는다). 같은 이유로 정정도 그 항목이 안 보이면 거부한다. ⑧ 「전체 보기」 · 재열람은 `can(viewer, "certs.submissions", "view")`를 `visible(viewer, "cert.rrn_unmasked")`와 **함께** 본다 — `visible`과 `can`은 서로 독립(D-35)이라 메뉴를 거둔 사람이 정보 항목만 남아 액션을 직접 부르는 길을 막는다(codex r2 C4). ⑨ `revealRrn`의 `mask_reveal`은 `FOR SHARE` 잠금을 쥔 같은 트랜잭션 연결로 쓴다 — `recordAction(viewer, entry, { tx })`. tx 없이 부르면 전역 풀에서 두 번째 연결을 잡아, 풀 한도(`DB_POOL_MAX` 기본 5)만큼 동시에 누르면 모두가 서로를 기다린다(codex r2 C5 · checker-C W3). `mask_reveal` · `cert_correct`는 끌 수 없는 종류(`ALWAYS_ON_ACTION_TYPES`)라 `recordAction`이 설정 조회 없이 `appendActionLog(viewer, entry, tx)` 하나만 부른다 — tx 안 DB 호출이 그 tx뿐이다(E3-09와 같은 규칙). 종류 검사도 `recordAction` 것이다(`test/unit/action-log/record.test.ts`가 이미 덮는다).

**실행 조건(E3-01 · E3-33):** Phase 4 PR #40이 main에 머지된 뒤에만 시작하고, 웨이브 첫 단계는 `git merge origin/main`이다(리베이스 · 강제 푸시 금지). Phase 4 부품 — `recordAction({ tx })` · `ui/button`(비활성은 네이티브 `disabled`가 아니라 `aria-disabled` + `disabledReason`) — 을 기준으로 쓴다. 같은 웨이브 4의 04.3-06 · 08 · 09와도 통합 · E2E는 한 번에 하나다(세션당 플랜 하나).

**범위(checker-C W1 — 받아들임):** 이 플랜은 파일 19개로 15개를 넘지만 한 세로 조각(domain → 리포지토리 → 액션 → I4 화면 → E2E)이라 나누지 않는다 — 나누면 어느 쪽도 혼자서는 사용자가 쓸 수 있는 것을 만들지 못한다.

Purpose: CERT-02의 「열람은 권한자로 제한 · 열람 기록」과 D-1106 정정, 개인정보취급자 세션을 한 화면에서 닫는다.
Output: domain 모듈 둘, 리포지토리 둘(`cert-review` · `privacy-session-activity` — tx 로그 도우미는 만들지 않는다, E3-02), I4 화면, 액션 셋, 설정 키 하나, 정보 항목 둘, 핵심 로그 종류 하나, 통합 2 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-PATTERNS.md
@.planning/phases/04.3-qr-certificate-intake/04.3-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 전체 보기(기록 먼저) · 정정(버전 · 칸별 로그) · 비활동 세션 — domain·리포지토리·등록부</name>
  <files>domain/permissions/info-items.ts, domain/settings/keys.ts, domain/action-log/record.ts, domain/certs/review.ts, domain/certs/privacy-session.ts, repositories/cert-review.ts, repositories/privacy-session-activity.ts, test/integration/cert-crypto.test.ts, test/integration/cert-review.test.ts</files>
  <precondition>(E3-01) Phase 4가 main에 있고 이 브랜치에 합쳐져 있다 — `git fetch origin && git merge origin/main` 뒤 `test -f ui/confirm-dialog/ConfirmDialog.tsx && git merge-base --is-ancestor origin/main HEAD`가 종료 코드 0. 아니면 시작하지 않고 멈춘다</precondition>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-PATTERNS.md` 「`domain/certs/review.ts` — `revealRrn`」 · 「PII 암호화·마스킹 해제·행동 로그」
    - `domain/vendors/index.ts` 360~393행(`revealAccountNumber` — deps 주입 · 순서) · `test/integration/vendors.test.ts`의 마스킹 해제 케이스(기록 실패 주입 방식)
    - `domain/action-log/record.ts`(전문) — `CORE_ACTION_TYPES` · `ACTION_TYPE_LABELS` · `ALWAYS_ON_ACTION_TYPES` 세 배열을 **같이** 고친다(30행 주석) · `test/unit/action-log/record.test.ts`
    - `domain/permissions/info-items.ts`(전문) — `vendor.account_number_unmasked` 선례 · 끝에 더할 자리
    - `domain/settings/keys.ts`(04.3-02·03이 더한 확인증 키 뒤)
    - `db/schema/cert-submissions.ts` · `db/schema/cert-winners.ts` · `db/schema/privacy-session-activity.ts` · `db/schema/auth.ts`의 `sessions`(04.3-02 산출물) — `version` · `updated_by` · `purged_at` · `rrn_masked` 칸, 활동 표의 `ON DELETE CASCADE`
    - `domain/certs/rrn.ts` · `domain/certs/format.ts`(`validateRrn` · `maskRrn` · `normalizePhone`)
    - `test/integration/cert-crypto.test.ts`(04.3-02 — 평문 부재 단언 방식, 이어 붙일 자리)
    - `lib/certs/feature-guard.ts`(04.3-02 — `assertCertFeatureEnabled()`: 거짓이면 `notFound()`를 던진다 — 페이지 · 라우트 · 액션의 첫 줄, C1) · `domain/certs/feature.ts`(`isCertFeatureEnabled(deps?)` — domain 함수가 보고 `notFound` 유니온을 돌려준다 · 테스트에서 게이트를 끄는 방법) · `domain/permissions/roles.ts` 22~24행(`role-ceo` 영구 식별자)
    - (Phase 4 머지 뒤 main — 줄 번호 대신 이름으로) `repositories/action-log.ts` `appendActionLog(viewer, entry, tx?)` · `domain/action-log/record.ts` `RecordActionDeps`(`appendActionLog` · `isActionTypeEnabled` · `tx`)와 `recordAction(viewer, entry, deps?)`(actorId 매핑: `SYSTEM_VIEWER`면 null · `isCoreActionType` · `UnknownActionTypeError` · 끌 수 없는 종류는 설정 조회 없이 `appendActionLog(…, deps.tx)`) · `repositories/cert-action-log.ts`(04.3-02 대비 도우미 `insertActionLogTx` — ⑤ 결정의 대비책일 때만, 읽기만) · `lib/db-transaction.ts`
    - `domain/permissions/project.ts`(전문 — `DtoSpec` · `project()`가 칸마다 `visible()`을 부른다) · `domain/vendors/index.ts` 49~66행(`VENDOR_DTO_SPEC` + `registerDto({fields: SPEC.fields.map(…)})` — 스펙 하나로 투영과 누수 스캔 등록을 같이 하는 선례) · `lib/env.ts`의 `DB_POOL_MAX`(기본 5) · `db/client.ts` 38~41행(전역 풀 하나)
  </read_first>
  <behavior>
    - `revealRrn(권한 없는 viewer, id)` → 거부 · 행동 로그 0줄 · 복호화 함수 호출 0번(주입한 스파이)
    - `revealRrn(viewer, id)`(viewer = 테스트 계급에 `upsertPermission`/`upsertVisibility`로 켠 viewer — 시드 기본값 아님, E3-13) → `930412-2123458` · `mask_reveal` 정확히 1줄(entity `cert_submission`, detail에 번호 없음)
    - 기록 함수(`deps.appendActionLog` — `recordAction`의 주입 자리로 넘어간다, E3-02)가 던지도록 주입 → `revealRrn`이 던지고 복호화 호출 0번(기록 먼저)
    - (메뉴 거둠 · codex r2 C4) 테스트 계급에 정보 항목 `cert.rrn_unmasked`는 켜고 메뉴 `certs.submissions` 보기는 끈 viewer → `revealRrn` · `recordRrnReopen` 둘 다 거부 · 복호화 0번 · `mask_reveal` 0줄(액션은 이 domain 함수를 그대로 부르는 얇은 층이라 직접 POST도 여기서 막힌다 — 액션 모듈은 Vitest에서 import하지 않는다)
    - (풀 · codex r2 C5) `DB_POOL_MAX`(환경 값, 기본 5)개의 `revealRrn`을 동시에 부르고, 주입한 기록 함수(`deps.appendActionLog`)가 **모두가 기록 단계에 닿을 때까지 기다리는 장벽** 뒤에 받은 세 인자 그대로 실제 `appendActionLog(viewer, entry, tx)`를 부른다(셋째 인자 `tx`가 비어 있으면 곧바로 던진다 — `recordAction`이 tx를 넘기는지 확인) → 10초 안에 모두 끝나고 `mask_reveal`이 정확히 그 수만큼(각 연결이 잠금을 쥔 채 두 번째 연결을 기다리면 이 케이스가 시간 초과로 실패한다). Phase 4 `db/client.ts`의 풀 연결 대기 5초(`POOL_CONNECTION_TIMEOUT_MS`) 때문에 실패는 시간 초과 대신 연결 획득 오류로 나올 수도 있다 — 어느 쪽이든 실패다
    - (정보 항목 · codex r2 C3) `certs.submissions` 보기·쓰기는 켜고 `cert_submission.value`는 끈 viewer → `getSubmissionForReview`가 `notFound` · `correctSubmission`이 거부(아무것도 안 바뀜). 같은 행을 `project(viewer, row, CERT_SUBMISSION_REVIEW_DTO_SPEC)`로 투영하면 항목 꺼짐은 `{}`, 켜짐은 스펙의 모든 키가 있다
    - 파기된 확인증 → 거부 · 복호화 호출 0번
    - `recordRrnReopen`(고친 값 재열람) → 복호화 없이 `mask_reveal` 1줄
    - `correctSubmission`(연락처만, 맞는 version) → 저장 · 버전 +1 · `cert_correct` 1줄 detail `{fields: ["연락처"]}`(값 없음)
    - 주민등록번호를 바꾸는 정정을 `cert.rrn_unmasked`가 안 보이는 viewer가 보냄 → 거부, 아무것도 안 바뀜
    - (보기 회수 · codex final C1) 테스트 계급에 `certs.submissions` 쓰기 · `cert_submission.value` · `cert.rrn_unmasked`는 켜고 `certs.submissions` 보기만 끈 viewer가 기존 id · version으로 주민등록번호를 바꾸는 정정을 보냄 → 거부 · `rrn_encrypted` · `rrn_masked` · `version` 그대로 · `cert_correct` 0줄
    - 주민등록번호 정정 → `rrn_encrypted`가 `v1:`로 시작하는 새 값 · `rrn_masked` 갱신 · 행동 로그 표 모든 텍스트 칸에 새 13자리·뒤 7자리 없음
    - 틀린 version → `{kind: "conflict", byName, at}` · 아무것도 안 바뀜
    - 현장 수령 확인증에 주소를 보냄 → 칸 오류 · 택배는 주소 1~200자
    - (C6) 정정의 로그 INSERT가 던지도록 주입 → `correctSubmission`이 던지고, 확인증의 모든 칸 · `version` · `updated_by`가 저장 전 그대로 · `cert_correct` 0줄(되돌림 증명)
    - (C1) 기능 게이트를 끈 상태 → `getSubmissionForReview` · `revealRrn` · `recordRrnReopen` · `correctSubmission` 넷 다 not-found · 복호화 0번 · 행동 로그 0줄 · 확인증 칸 · 버전 그대로
    - (대표) 대표 계급(`role-ceo`)에 테스트가 `certs.submissions` 보기·쓰기와 정보 항목 `cert.rrn_unmasked` · `cert_submission.value`를 켠 뒤 → 넷 다 거부(조회는 not-found) · 복호화 0번 · 로그 0줄 · 아무것도 안 바뀜
    - (잠금) `revealRrn`의 행 읽기가 `FOR SHARE` 잠금 안이다 — 주입한 기록 함수가 멈춘 동안 다른 연결에서 `purged_at`을 채우는 UPDATE가 `lock_timeout` 짧게 걸고 시도하면 잠금 대기로 실패한다(파기와의 경합 전체는 04.3-12 통합 테스트)
    - `touchPrivacySession`: 활동 행 없음 + 세션 `created_at` 119분 전 → ok + 활동 행 생성(마지막 활동 = 지금) · **(E3-10) 로그인 3시간 뒤 첫 방문:** 활동 행 없음 + `created_at` 180분 전 → ok · `sessions` 행 그대로 · 활동 행 생성(마지막 활동 = 지금) → 그 뒤 121분 무활동(주입한 `now`) → 만료 + `sessions` 행 삭제 · `created_at` 29일 전 + 마지막 활동 10분 전 → ok · 마지막 활동 119분 전(설정 120) → ok + 갱신 · 121분 전 → 만료 + `sessions` 행 삭제(활동 행은 cascade로 사라짐) · 설정 10분으로 바꾸면 11분 전 → 만료
  </behavior>
  <action>
① **등록부(공유 — 항목만 끝에 더한다):** `domain/permissions/info-items.ts`에 `{ key: "cert_submission.value", label: "확인증 제출 내용", staffDefault: false }` · `{ key: "cert.rrn_unmasked", label: "주민등록번호 전체 보기", staffDefault: false }`. `domain/settings/keys.ts`에 `CERT_PRIVACY_IDLE_MINUTES`(`cert.privacy.idle_minutes`, 정수 10~120, 기본 120, 네임스페이스 「확인증」, 라벨 `개인정보취급자 비활동 만료(분)`) + `SETTING_DEFS`. `domain/action-log/record.ts` 세 배열에 `cert_correct`(라벨 `확인증 정정`, 끌 수 없음)를 같이 더한다.

② **RED:** 위 `behavior`의 전체 보기 케이스(메뉴 거둠 · 풀 장벽 포함)는 `test/integration/cert-crypto.test.ts` 끝에, 정정 · 비활동(E3-10 로그인 3시간 뒤 첫 방문 포함) · 정보 항목 꺼짐 케이스는 새 `test/integration/cert-review.test.ts`에 먼저 쓰고(tx 로그의 종류 거부는 `recordAction`의 것이라 `test/unit/action-log/record.test.ts`가 이미 덮는다 — 이 플랜은 그 케이스를 따로 쓰지 않는다, E3-02) 실행해 실패를 확인한다(구현 전). 제출 표본은 04.3-02의 domain 흐름(`selectWinner` → `verifyLast4` → `submitCertificate`)으로 만든다.

③ **리포지토리(모두 viewer 첫 인자, 트랜잭션을 받는 함수는 마지막 인자 `tx`):**
  - ③-a `repositories/cert-review.ts`: 확인증 한 줄 + 자리(등록 이름 · 전달 · 경품 · 수량) + 행사 이름 조회 · 전체 보기용 잠금 읽기(`rrn_encrypted` · `purged_at`을 `FOR SHARE`로, tx 안) · 버전 조건부 갱신(`version = 입력 AND purged_at IS NULL`, `updated_by` · `updated_at` · `version + 1`, tx 안, 바뀐 행 수 반환) · 마지막 정정자 이름·시각 조회.
  - ③-b **tx 로그 도우미를 만들지 않는다(E3-02):** 로그는 domain에서 Phase 4 `recordAction(viewer, entry, { tx, appendActionLog: deps?.appendActionLog })`로 쓴다(결정 ⑤ · ⑨ — `recordAction`이 종류 검사 · `actorId` 매핑 · 끌 수 없는 종류의 설정 조회 건너뛰기를 하고 INSERT를 그 tx로 보낸다). 대비책(결정 ⑤)일 때만 04.3-02의 `insertActionLogTx`를 import한다. 이 플랜은 `repositories/cert-action-log.ts` · `repositories/action-log.ts`를 고치지 않는다.
  - ③-c `repositories/privacy-session-activity.ts`: 활동 행 읽기 · upsert · 세션 행 삭제(better-auth `sessions`). 세션 생성(로그인) 시각을 읽는 함수는 두지 않는다(E3-10 — 첫 방문이 시작점).

④ **`domain/certs/review.ts`(네 함수 모두 맨 앞에서 `isCertFeatureEnabled()`가 거짓이면 `notFound`(조회) · 거부(나머지) — C1, domain은 `next/navigation`을 던지는 가드를 쓰지 않고 04.3-02처럼 유니온을 돌려준다. 다음 줄 `isCertPrivacyBarredRole(viewer)`면 거부 — 결정 ④):**
  - `isCertPrivacyBarredRole(viewer)`: `viewer.roleId === "role-ceo"`. 04.3-10 · 11이 import한다.
  - `CERT_SUBMISSION_REVIEW_DTO_SPEC: DtoSpec<CertSubmissionReviewRow, CertSubmissionReviewDto>`(export) — 값 칸 전부(id · 확인증 번호 · 행사 이름 · 제출 시각 · 제출 이름 · 등록 이름(제출 이름과 다를 때만) · 가린 주민등록번호 · 연락처(표시형) · 주소(택배만) · 전달 · 경품 줄 · 동의 시각 · 서명 이미지 data URL · version)의 `infoItem`이 `cert_submission.value`. `registerDto({name: "CertSubmissionReviewDto", fields: SPEC.fields.map(…)})`로 같은 스펙을 누수 스캔에 등록한다(vendors 선례 — 결정 ⑦).
  - `getSubmissionForReview(viewer, id)`: 게이트 → 대표 → `can(viewer, "certs.submissions", "view")` 거짓 · 없음 · 파기됨 → `notFound`. 행 + 서명(저장소에서 읽은 PNG의 data URL, 읽기 실패면 null — 화면은 오류 표시만)을 원본 객체 하나로 만든 뒤 **`project(viewer, 원본, CERT_SUBMISSION_REVIEW_DTO_SPEC)`** — 투영 결과가 빈 객체면(= `cert_submission.value`가 안 보임) `notFound`. 통과하면 투영 결과에 개인정보가 아닌 판정 값 셋을 붙여 돌려준다: `canReveal`(`visible(viewer, "cert.rrn_unmasked")` — 메뉴 보기는 앞에서 이미 통과) · `canCorrect`(`can(…, "write")`) · 비활동 분. 값 칸을 투영 밖에서 DTO에 싣지 않는다(codex r2 C3).
  - `revealRrn(viewer, id, deps?)`: `revealAccountNumber`와 같은 순서(권한 → 기록 → 복호화)를 **한 트랜잭션 안에서** 한다 — 게이트 → 대표 → 권한(`can(viewer, "certs.submissions", "view")` **그리고** `visible(viewer, "cert.rrn_unmasked")` — 결정 ⑧, 하나라도 거짓이면 거부 · 기록 · 복호화 없음) → `withTransaction(tx)`: ③-a 잠금 읽기(`FOR SHARE`) → 없음 · 파기됨이면 거부(기록 · 복호화 없음) → `recordAction(viewer, {actionType: "mask_reveal", entity: "cert_submission", entityId: id}, { tx, appendActionLog: deps?.appendActionLog })`(**같은 tx 연결** — 잠금을 쥔 채 전역 풀에서 두 번째 연결을 잡지 않는다, 결정 ⑨ · E3-02. `deps.appendActionLog`로 바꿔 끼울 수 있다 — 실패 주입 · 풀 장벽 테스트) → 잠금 읽기로 얻은 암호문을 `decrypt` → 커밋 → `000000-0000000` 형식. 기록 실패는 그대로 던진다(롤백 · 복호화 없음). 이 함수 안의 `recordAction(` 호출은 이 하나이고 반드시 `tx`를 넘긴다.
  - `recordRrnReopen(viewer, id)`: 게이트 → 대표 → 같은 권한 둘(`can` 그리고 `visible`, 결정 ⑧) → 행이 없거나 파기됐으면 거부 → `recordAction(viewer, {actionType: "mask_reveal", …})`(잠금을 쥐지 않으므로 전역 연결로 충분), 복호화 없음(고친 값을 다시 여는 「전체 보기」 — UI-SPEC I4).
  - `correctSubmission(viewer, id, input, deps?)` 순서:
    1. 게이트 → 대표 → `can(…, "write")` → `visible(viewer, "cert_submission.value")`(거짓이면 거부 — 볼 수 없는 값을 고치는 길을 두지 않는다, 결정 ⑦)
    2. 입력 zod(version · 이름 1~40 · 연락처 `normalizePhone` · 주소 1~200(택배만) · 주민등록번호(보냈을 때만, `validateRrn`)) — 서명은 입력 스키마에 없다
    3. 주민등록번호가 있으면 전체 보기와 **같은 두 판정** — `can(viewer, "certs.submissions", "view")` **그리고** `visible(viewer, "cert.rrn_unmasked")`(결정 ⑧, 하나라도 거짓이면 거부). 1의 `write`만으로는 부족하다 — 권한표는 보기와 쓰기를 따로 저장하므로(`domain/permissions/matrix.ts` `setPermissionCell`) 보기를 거둔 뒤 쓰기만 남은 직원이 기존 id · version으로 주민등록번호를 바꾸는 길이 열린다(codex final C1)
    4. 바뀐 칸 계산(주민등록번호는 보냈으면 바뀐 칸 — 비교를 위해 복호화하지 않는다)
    5. `withTransaction`(C6): 조건부 갱신 → 0행이면 롤백하고 `conflict` + 마지막 정정자 → 1행이면 같은 tx로 `recordAction(viewer, {actionType: "cert_correct", entity: "cert_submission", entityId, detail: {fields: [칸 이름]}}, { tx, appendActionLog: deps?.appendActionLog })`(E3-02 — `deps.appendActionLog`로 바꿔 끼울 수 있다, 실패 주입 테스트) → 커밋
    6. `{kind: "saved", fields, at, version, rrnMasked}`

⑤ **`domain/certs/privacy-session.ts` `touchPrivacySession(viewer, sessionId, now?)`:** 설정 `CERT_PRIVACY_IDLE_MINUTES` → 활동 행 → **활동 행이 없으면 지금을 마지막 활동으로 upsert하고 `ok`(첫 방문 = 시작점, 끊지 않는다 — E3-10, codex #23의 로그인 시각 기준을 바꾼다)** · 있으면 경과 = 지금 − 마지막 활동 → 경과 ≤ 한도면 활동 행을 upsert하고 `ok` · 넘으면 세션 행을 지우고 `expired`. 끊는 범위(세션 전체 대 개인정보 경로만 재인증)는 → 설계 /cso에서 확정(지금은 세션 전체 — 결정 ①). 이 함수는 04.3-11 인쇄 라우트도 부른다.

⑥ 커밋 `feat(04.3): record-first RRN reveal, audited corrections and privacy idle session`, 본문 한국어(기록 먼저 순서 · 잠금 읽기와 같은 tx의 `mask_reveal`(`recordAction({ tx })`) · 메뉴와 정보 항목을 함께 보는 전체 보기 · `project()` 투영 · `cert_correct`를 새로 둔 이유 · 정정과 로그의 한 트랜잭션 · 대표 차단 · 비활동 판정 범위와 첫 방문 = 시작점(E3-10)).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/action-log/record.test.ts test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-crypto.test.ts test/integration/cert-review.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 기록 실패 주입 케이스가 실패하면 복호화가 기록보다 먼저고, 풀 장벽 케이스가 시간 초과 · 연결 획득 오류로 실패하면 `recordAction`에 `tx`를 넘기지 않았으며, 로그인 3시간 뒤 첫 방문 케이스가 `expired`면 아직 로그인 시각 기준이다(E3-10)</fails_when>
    <automated>test "$(grep -rlE 'export (async )?function [A-Za-z]*ActionLogTx' repositories | grep -v 'repositories/cert-action-log.ts' | wc -l)" = 0 &amp;&amp; test "$(grep -v '^\s*//' domain/certs/review.ts | grep -cE 'recordAction\(')" -ge 3 &amp;&amp; test "$(grep -v '^\s*//' domain/certs/privacy-session.ts | grep -c 'created_at\|createdAt')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 04.3-02의 대비 파일 밖에 tx 로그 도우미가 새로 정의됐거나(E3-02), 전체 보기 · 재열람 · 정정 세 로그가 `recordAction`을 거치지 않거나, 비활동 판정이 아직 세션 로그인 시각을 읽는다(E3-10)</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/certs/review.ts`에서 `decrypt(`가 `revealRrn` 안에만 있고 그 앞 줄에 `tx`를 넘기는 `recordAction(` 호출이 있다(codex r2 C5 · E3-02 — `revealRrn` · `correctSubmission`의 `recordAction` 호출은 `{ tx` 를 싣는다: `grep -n '{ tx' domain/certs/review.ts`가 두 줄 이상)
    - 풀 한도만큼 동시 `revealRrn`(장벽 주입)이 10초 안에 모두 끝나는 케이스가 있다(codex r2 C5). tx 로그의 종류 거부 케이스는 두지 않는다 — `record.test.ts`가 덮는다(E3-02)
    - `repositories/`에 04.3-02의 `repositories/cert-action-log.ts` 밖의 tx 로그 도우미 정의가 0개이고, 이 플랜의 커밋이 `repositories/cert-action-log.ts` · `repositories/action-log.ts`를 고치지 않는다(E3-02) · 대비책을 썼는지 SUMMARY에 한 줄
    - 메뉴 `certs.submissions` 보기를 끄고 `cert.rrn_unmasked`만 켠 viewer로 `revealRrn` · `recordRrnReopen`이 거부 · 복호화 0번 · 로그 0줄인 케이스가 있다(codex r2 C4)
    - `domain/certs/review.ts`가 `project(viewer`로 I4 DTO를 만들고(`grep -n "project(viewer" domain/certs/review.ts`), `cert_submission.value`를 끈 viewer로 `getSubmissionForReview`가 `notFound` · `correctSubmission`이 거부 · 투영이 `{}`인 케이스가 있다(codex r2 C3)
    - 기록 실패 주입 · 권한 없음 · 파기됨 세 케이스가 복호화 호출 0번을 단언한다
    - 정정 로그 detail이 칸 이름 배열뿐임을 단언하는 케이스가 있다
    - `record.ts`의 `CORE_ACTION_TYPES` · `ACTION_TYPE_LABELS` · `ALWAYS_ON_ACTION_TYPES` 세 곳 모두에 `cert_correct`가 있다(`Record<CoreActionType, string>` 타입이라 라벨이 빠지면 typecheck가 실패한다 — 끌 수 없음 배열은 SUMMARY에 해당 줄을 인용한다)
    - 비활동 경계(한도 -1분 ok · +1분 만료)와 설정 변경 케이스가 있다 · (E3-10) 「로그인 3시간 뒤 첫 방문 → ok · 세션 행 그대로 · 활동 행 생성 → 121분 무활동 → 만료 + 세션 행 삭제」 케이스가 있고 녹색이다 · `domain/certs/privacy-session.ts`가 세션 `created_at`을 읽지 않는다
    - 정정 로그 실패 주입 → 확인증 칸 · 버전 불변을 단언하는 케이스가 있다(C6)
    - 보기만 끄고 쓰기 · `cert.rrn_unmasked`는 남긴 viewer의 주민등록번호 정정이 거부되고 아무것도 안 바뀌는 케이스가 있다(codex final C1)
    - 게이트 꺼짐에서 domain 함수 넷이 not-found · 복호화 0번 · 로그 0줄인 케이스가 있다(C1)
    - 대표 계급에 권한을 켠 채로 넷 다 거부되는 케이스가 있다(codex #5)
    - `domain/certs/review.ts`의 export 함수 넷이 각각 `isCertFeatureEnabled`를 부른다(`test "$(grep -c 'isCertFeatureEnabled(' domain/certs/review.ts)" -ge 4`)
  </acceptance_criteria>
  <done>전체 보기는 기록이 먼저이고, 정정은 칸 이름만 로그에 남기며, 개인정보 경로의 비활동 세션이 설정대로 끊긴다</done>
</task>

<task type="auto">
  <name>Task 2: I4 화면 — 가림/전체 보기/가리기/재열람 · 정정 폼 · 동시 정정 · 서명 · 자동 가리기 · 브라우저 인쇄 차단</name>
  <files>lib/viewer.ts, app/(app)/certs/submissions/[id]/page.tsx, app/(app)/certs/submissions/[id]/review-form.tsx, app/(app)/certs/submissions/[id]/rrn-field.tsx, app/(app)/certs/submissions/[id]/actions.ts, app/(app)/certs/submissions/[id]/actions.registry.ts, app/(app)/certs/submissions/[id]/review.module.css, test/integration/leak-scan.test.ts, test/e2e/cert-review.spec.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 I4 전문 · Copywriting I 표의 I4 줄 전부
    - `docs/design/SYSTEM.md` §6-3(폼 화면 · 폰 하단 고정 제출 줄) · §7-15(04.3-01 개정 ⑧ 정정 폼 SUCCESS · 버전 충돌) · §2-4(주민등록번호 가림 형식, 개정 ⑥) · §6-9
    - `app/(app)/admin/vendors/account-number.tsx`(전문) — `revealed` 상태 · 「가리기」는 서버 재호출 없음 · `canReveal`이 없으면 버튼 자체를 렌더하지 않음
    - `app/(app)/admin/vendors/actions.ts` · `actions.registry.ts`(전문) — 마스킹 해제 액션의 `dtoName: null` 예외 주석과 등록 방식
    - `lib/viewer.ts`(전문) · `lib/auth.ts` — 세션 id를 얻는 자리(`auth.api.getSession`의 `session.id`)
    - `ui/form/Form.tsx` · `ui/kv-list/KvList.tsx` · `ui/input/TextField.tsx` · `ui/button/Button.tsx` · `ui/page-header/PageHeader.tsx` · `ui/status-tag/StatusTag.tsx`
    - `test/integration/leak-scan.test.ts` 1~30행 · `test/unit/leak-scan-coverage.test.ts`
  </read_first>
  <action>
⓪ **E2E 먼저(RED — codex final C6, CLAUDE.md §5):** ①~⑥을 쓰기 **전에** Task 3 ①~③의 `test/e2e/cert-review.spec.ts`(격리 · 가림 · 전체 보기 로그 · 가리기 · 고친 값 재열람 · 정정 · 동시 정정 · 인쇄 차단 · 404 · 비활동 만료 · 게이트 꺼짐 직접 POST)를 전부 쓰고 Task 3 `<verify>`의 명령으로 돌려 실패(I4 라우트 · 액션 없음)를 확인한다 — 실패 이유 한 줄을 SUMMARY에 적고 `test(04.3): add failing certificate review E2E`로 커밋한다. 그다음 ①~⑥으로 채운다(04.3-04 Task 4 · 04.3-11 Task 3 선례).

① **`lib/viewer.ts`(더하기만):** `getSessionId(): Promise<string | null>` — 지금 요청의 better-auth 세션 id. 기존 함수의 모양은 바꾸지 않는다.

② **`page.tsx`(서버):** `await assertCertFeatureEnabled()`(C1 — **맨 첫 줄**, `requireSession`보다 먼저: 페이지 안에서는 게이트가 첫 판정이라 로그인한 사람은 권한 · 비활동 판정 전에 404를 받는다. 로그인하지 않은 요청은 이 줄에 닿기 전에 `app/(app)/layout.tsx:22`의 `requireSession()`이 `/login`으로 보낸다 — 페이지 안 순서로는 바꿀 수 없고, 모든 `(app)` 경로와 같은 이동이며 데이터가 나가지 않으므로 받아들인다. 레이아웃은 고치지 않는다, Codex r3 C-2) → `requireSession` → 세션 id로 `touchPrivacySession` → `expired`면 `redirect("/login")` → `getSubmissionForReview`가 `notFound`면 `notFound()`. PageHeader 제목 `기타소득 확인증 — {제출 이름}` + 태그 `제출됨`(`--success`), 부제 `{확인증 번호} · {행사 이름} · {제출 시각} 제출`. 머리 줄 폭 720. 「인쇄」는 04.3-11이 더한다(자리만 비워 둔다 — 빈 버튼 없음). metadata title `확인증 확인 · {제출 이름}`.

③ **`review-form.tsx`(`"use client"`, `Form` 한 열 720 · 라벨 96):** 이름(480, 제출값) + 다를 때만 `Form.Hint` `등록 이름 {등록 이름}` · 주민등록번호(`rrn-field.tsx`) · 연락처(280) · 주소(480, 택배만). 읽기 전용 `KvList`: 경품 `{경품명} {수량}개 · 현장 수령|택배` · 동의 `동의함 · {일시}` · 서명 이미지(CSS `width: 320px; aspect-ratio: 520 / 200; object-fit: contain`, 폰 `width: 100%`, 아래 1px `--line`, `alt="{제출 이름} 서명"`) + `수령자 서명 · 고칠 수 없음` — 서버가 이미지를 못 읽었거나 `onerror`면 이미지 자리에 `서명 이미지를 불러오지 못했습니다 · 다시 시도`(`--danger`, 3차 = `router.refresh()`). `Form.Actions` 1차 「고친 내용 저장」 — 바뀐 칸이 없으면 비활성 + `바뀐 칸 없음`(UI-SPEC Copywriting 「I4 1차 막힘」 · 막힘 이유의 색 `reasonTone="info"`, /plan-design-review D-7 · X-4 — Phase 4 `ui/button`의 `disabled` + `disabledReason` + `reasonTone` — 네이티브 `disabled`가 아니라 `aria-disabled`이고 이유 글자가 `aria-describedby`로 묶인다. 버튼을 고치지 않고 그대로 쓴다, E3-01). 성공 → 버튼 자리 `저장됨 · {칸 이름 · …} · HH:mm`(`--success`), 입력은 새 값을 기준으로 계속 편집, 토스트 없음. 실패 → 버튼 옆 이유(UI-SPEC 「I4 저장 실패」 `저장하지 못했습니다 · 주민등록번호 1칸` 꼴 — 안심 꼬리 없음, X-3) + 칸 아래 `Form.Error`(명사형, Copywriting I4), 값 유지. 충돌 → `저장하지 못했습니다 · {이름}가 {HH:mm}에 먼저 고침 · 다시 불러오기`(3차 → `router.refresh()`로 새 값, 내 고친 값 버림). 폰(<700): 라벨 위 · 칸 전폭 · 1차는 §6-3 폰 규칙.

④ **`rrn-field.tsx`(`"use client"`):** 기본 텍스트 `930412-2******` + 3차 「전체 보기」(`canReveal`일 때만). 누르면 `revealCertRrnAction` → 성공 시 `TextField`(280, `000000-0000000`, `.num`) + 3차 「가리기」 · 실패 `번호를 불러오지 못했습니다 · 다시 시도`. 「가리기」: 고친 값이 없으면 평문을 상태에서 지우고 기본 텍스트로(서버 호출 없음) · 고친 값이 있으면 칸을 내리고 `{고친 값의 가림} · 저장 안 함` + 「전체 보기」(→ `reopenCertRrnAction` 기록 성공 뒤에만 메모리의 고친 값으로 칸을 다시 연다). 자동 가리기: `visibilitychange` hidden · 이 화면 비활동 타이머(서버가 준 분) — 「가리기」와 같은 동작. 저장 성공 → 칸은 새 가림 텍스트로 즉시 돌아간다. 액션 결과가 세션 만료면 `/login`으로 이동.

⑤ **액션(`actions.ts`, `"use server"`, `authedActionClient`):** `revealCertRrnAction` · `reopenCertRrnAction` · `correctCertSubmissionAction` — 각각 순서가 같다: 첫 줄 `await assertCertFeatureEnabled()`(C1 — 꺼져 있으면 not-found, 페이지를 거치지 않은 직접 POST도 여기서 끝난다) → `touchPrivacySession`(만료면 `{kind: "sessionExpired"}`) → domain 함수 하나. `actions.registry.ts`: vendors 선례 그대로(마스킹 해제 둘은 `dtoName: null` 예외 주석, 정정은 `CertSubmissionReviewDto`), 메뉴 `certs.submissions`. `test/integration/leak-scan.test.ts` import 목록 끝에 `@/domain/certs/review`와 이 레지스트리 두 줄.

⑥ **`review.module.css`:** 토큰만. `@media print`에서 화면 내용 전부 `display: none`, 인쇄 전용 한 줄 `이 화면은 인쇄하지 않습니다 · 머리의 「인쇄」 버튼으로 확인증을 열어 주세요`만 보인다(화면에서는 숨김). 셸의 상단 바·하단 탭도 인쇄 미디어에서 숨는지 확인하고, 셸 CSS를 고쳐야 하면 고치지 말고 이 화면 루트에서 해결 방법을 SUMMARY에 적는다.

⑦ 커밋 `feat(04.3): certificate review and correction screen`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts test/unit/page-auth-guard.test.ts &amp;&amp; bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 새 레지스트리가 누수 스캔에 빠졌거나 페이지 인증 검사가 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - `page.tsx`에 `requireSession`과 `touchPrivacySession`이 있고 `touchPrivacySession`이 `getSubmissionForReview`보다 먼저다
    - `page.tsx`에서 `assertCertFeatureEnabled()` 호출이 `requireSession(` 호출보다 앞 줄이다(checker-C I2 — `P="app/(app)/certs/submissions/[id]/page.tsx"; test "$(grep -n 'assertCertFeatureEnabled()' "$P" | head -1 | cut -d: -f1)" -lt "$(grep -n 'requireSession(' "$P" | head -1 | cut -d: -f1)"`)
    - `actions.ts`의 세 액션 몸통 첫 줄이 `await assertCertFeatureEnabled()`다(`test "$(grep -c 'await assertCertFeatureEnabled()' "app/(app)/certs/submissions/[id]/actions.ts")" -ge 3`)
    - `rrn-field.tsx`에 `저장 안 함`과 `visibilitychange`가 있다
    - `review.module.css`에 `@media print`가 있다
    - `review-form.tsx`에 `바뀐 칸 없음`과 `reasonTone`이 있고 「못 한다」 접두 문장이 없다(`grep -c '저장할 수 없음' 'app/(app)/certs/submissions/[id]/review-form.tsx'`가 0 — 서명 캡션 `수령자 서명 · 고칠 수 없음`은 상태 표시라 그대로, D-7 · X-4)
    - UI-SPEC와 이 플랜의 I4 코드에 맥 커맨드 글리프가 없다 — `grep -c "$(printf '\342\214\230')" .planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md`가 0이고 `grep -rc "$(printf '\342\214\230')" 'app/(app)/certs/submissions'`의 합이 0(D-6)
    - 레지스트리가 누수 스캔 import 목록에 있고 leak-scan-coverage가 녹색이다
    - `test(04.3): add failing certificate review E2E` 커밋이 `feat(04.3): certificate review and correction screen` 커밋보다 먼저이고, SUMMARY에 ⓪ RED 실패 이유 한 줄이 있다(codex final C6)
  </acceptance_criteria>
  <done>I4가 UI-SPEC I4 그대로 서고 모든 개인정보 경로가 비활동 판정 뒤에만 열린다</done>
</task>

<task type="auto">
  <name>Task 3: I4 E2E — 가림 · 전체 보기 로그 · 가리기 · 정정 · 동시 정정 · 브라우저 인쇄 · 권한 404</name>
  <files>test/e2e/cert-review.spec.ts</files>
  <read_first>
    - `test/e2e/fixtures.ts` — `createFixtureUser({roleId})` · 로그인 헬퍼
    - `test/e2e/helpers/cert.ts`(04.3-02, C4 — 읽기만: `seedSubmittedCert()`(domain을 직접 불러 제출 표본을 만든다 — E3-32) · `createCertEvent` · `withCertFeatureOff(fn)` · `CERT_E2E_CONTACT_PHONE`, 기능 켜기 · 문의 전화는 `test/e2e/cert.setup.ts`가 한 번). E4 폼 조작으로 표본을 만들지 않는다 — 같은 웨이브의 04.3-06이 그 폼을 다시 쓴다
    - `playwright.config.ts` — C4의 `certs` 프로젝트(`*cert*.spec.ts`, 워커 1) — 이 스펙 이름이 잡힌다
    - 이 플랜 Task 1·2 산출물
  </read_first>
  <action>
아래 ①~③은 Task 2 ⓪이 구현 **전에** 쓰고 RED를 확인한 스펙의 내용이다(codex final C6). 이 태스크는 Task 2 구현 뒤 같은 스펙을 녹색으로 만든다 — 단언을 빼거나 약하게 바꾸지 않고, 구현과 어긋난 선택자만 고친다.

① **격리(C4):** 스펙 머리에 `test.describe.configure({ mode: "serial" })`. 기능 켜기 · 문의 전화는 `cert.setup.ts`에 맡기고 이 스펙은 `cert.enabled` · 문의 전화 설정을 직접 바꾸지 않는다. 행사는 `createCertEvent`로 테스트마다 고유 이름으로 만들고, 행사 목록이 비었다는 단언을 하지 않는다. 게이트를 끄는 케이스는 `withCertFeatureOff(fn)` 안에서만 한다(끝나면 도우미가 다시 켠다).

② **`test/e2e/cert-review.spec.ts`(데스크톱):** 전제: 이 권한은 시드 기본값이 아니라 `cert.setup`이 켠 것이다(E3-13) — `role-sysadmin`(박서연)의 `certs.submissions`(view · write) · `cert.rrn_unmasked` · `cert_submission.value`는 04.3-09의 시드 insert-if-absent 규칙 뒤로 배포마다 자동으로 켜지지 않고, `test/e2e/cert.setup.ts`(04.3-02)가 이 스펙보다 먼저 한 번 명시적으로 켠다. 이 스펙은 그 권한을 스스로 켜지 않는다. 제출 둘(현장 하나 · 택배 하나)은 `seedSubmittedCert()`(04.3-02 C4)만으로 만든다(E3-32 — 전달 방식은 그 헬퍼의 인자로 고른다. 헬퍼가 전달 방식을 받지 않으면 이 스펙에서 폼 조작이나 자체 domain 흐름으로 우회하지 말고 멈춰 04.3-02 헬퍼 계약을 먼저 고친다). 시스템 관리자로 I4 → 주민등록번호 `930412-2******` · 페이지 HTML에 13자리 없음 → 「전체 보기」 → 칸에 전체 번호 · DB 행동 로그 `mask_reveal` 1줄 → 「가리기」 → 가림 텍스트 · HTML에 13자리 없음 → 연락처 고침 → 「고친 내용 저장」 → `저장됨 · 연락처 · HH:mm` · DB `cert_correct` 1줄 detail에 칸 이름만 → 두 번째 브라우저 컨텍스트가 이름을 먼저 저장 → 첫 컨텍스트 저장 → `… 먼저 고침 · 다시 불러오기` → 「다시 불러오기」 → 새 이름. 현장 확인증은 주소 칸 없음 · 택배는 있음. 주민등록번호를 고쳐 「가리기」 → `{가림} · 저장 안 함` → 「전체 보기」 → 칸이 고친 값으로 다시 열림 · `mask_reveal`이 1줄 더. `page.emulateMedia({media: "print"})` → 보이는 텍스트가 인쇄 차단 한 줄뿐. 기획 PM 계정으로 같은 주소 → 404 화면. 비활동 만료: DB의 활동 행 `last_seen_at`을 121분 전으로 돌린 뒤 새로 고침 → 로그인 화면.

③ **직접 POST(C1 · codex #2):** 게이트가 켜진 동안 `page.on("request")`로 「전체 보기」 · 「전체 보기」(재열람) · 「고친 내용 저장」 세 요청의 URL · `Next-Action` 헤더 · 본문을 잡아 둔다 → `withCertFeatureOff` 안에서: 페이지를 거치지 않고 `page.request.post`로 세 요청을 그대로 다시 보낸다 → 각 응답 본문에 13자리 모양(`\d{6}-?\d{7}`)과 `saved` 결과가 없음 · DB의 `mask_reveal` · `cert_correct` 줄 수와 확인증 `version`이 다시 보내기 전과 같음 → 같은 게이트 꺼짐 상태에서 로그인한 시스템 관리자 · 기획 PM이 I4 주소 → 둘 다 404(게이트가 권한 판정보다 먼저, 도우미가 끝에서 다시 켠다). 로그인하지 않은 컨텍스트의 404는 단언하지 않는다 — `(app)` 레이아웃이 먼저 `/login`으로 보낸다(Codex r3 C-2).

④ 스펙에 바뀐 것이 있으면 커밋 `test(04.3): certificate review E2E`(없으면 커밋 없이 녹색 실행 결과만 SUMMARY에).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-review.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 스펙이 가림 상태의 HTML에 13자리가 없음을 두 번(처음 · 가리기 뒤) 단언한다
    - 동시 정정 · 브라우저 인쇄 · PM 404 · 게이트 꺼짐 404 · 비활동 만료 케이스가 있다
    - 게이트 꺼짐에서 세 액션을 직접 POST로 다시 보내 평문 · 로그 · 변경이 없음을 단언하는 케이스가 있다(C1)
    - 게이트 꺼짐에서 로그인한 시스템 관리자와 기획 PM이 I4를 열면 둘 다 404인 케이스가 있다(게이트가 권한 판정보다 먼저). 로그인하지 않은 컨텍스트는 `(app)` 레이아웃의 `/login` 이동이라 404를 단언하지 않는다(Codex r3 C-2)
    - 스펙이 설정을 직접 바꾸지 않는다(`! grep -n "setSettingValue" test/e2e/cert-review.spec.ts`)와 `mode: "serial"`이 있다(C4)
    - 스펙의 제출 표본이 `seedSubmittedCert(`로만 만들어진다 — `grep -c 'seedSubmittedCert(' test/e2e/cert-review.spec.ts`가 1 이상이고 스펙이 E4 폼 칸을 조작하지 않는다(`! grep -n '주민등록번호 앞 6자리' test/e2e/cert-review.spec.ts`, E3-32)
    - 스펙에 「전제: 이 권한은 시드 기본값이 아니라 cert.setup이 켠 것이다」 문구가 있고 스펙 자신이 `role-sysadmin`의 `certs.submissions` · `cert.rrn_unmasked` · `cert_submission.value`를 켜지 않는다(`! grep -nE "upsertPermission|upsertVisibility" test/e2e/cert-review.spec.ts`, E3-13)
  </acceptance_criteria>
  <done>I4의 열람·정정·동시성·인쇄 차단·권한이 브라우저에서 고정된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 경영관리 브라우저 → 정정·전체 보기 액션 | 인증된 직원이지만 권한·정보 항목은 서버가 매번 다시 판정한다 |
| 서버 → 브라우저 화면 | 평문은 「전체 보기」 응답에만 실리고, 기본 렌더·인쇄 미디어에는 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-08 | Repudiation | 전체 보기 | high | mitigate | `revealRrn` 하나만 복호화하고 순서는 권한 → `mask_reveal` 기록(끌 수 없음, 잠금을 쥔 같은 tx) → 복호화. 기록 실패 주입 테스트. 고친 값 재열람도 기록 |
| T-04.3-120 | Elevation of Privilege | `revealRrn` · `recordRrnReopen`(메뉴를 거둔 뒤 정보 항목만 남은 직원의 액션 직접 호출) | high | mitigate | 두 함수가 `can(viewer, "certs.submissions", "view")`와 `visible(viewer, "cert.rrn_unmasked")`를 **함께** 본다(D-35로 두 축이 독립이라 하나만 보면 우회된다). 메뉴 끔 + 정보 항목 켬 viewer로 둘 다 거부 · 복호화 0번 · 로그 0줄 통합 테스트(codex r2 C4) |
| T-04.3-121 | Denial of Service | `revealRrn`의 로그 쓰기(DB 풀) | medium | mitigate | `mask_reveal`을 `FOR SHARE`를 쥔 같은 tx 연결로 `recordAction(…, { tx })`에 쓴다(E3-02 — 끌 수 없는 종류라 설정 조회도 없다) — 전역 풀의 두 번째 연결을 잡지 않으므로 풀 한도(`DB_POOL_MAX`)만큼 동시 호출에도 서로를 기다리며 멈추지 않는다. 장벽 주입 동시 호출 통합 테스트(codex r2 C5 · checker-C W3) |
| T-04.3-122 | Information Disclosure | `getSubmissionForReview` · `correctSubmission`(정보 항목 `cert_submission.value` 꺼짐) | high | mitigate | I4 DTO를 `project()`로 투영해 항목이 꺼지면 값 칸이 실리지 않고 I4는 404, 정정도 거부한다. `registerDto`만으로는 요청 시점에 거르지 않는다. 항목 꺼짐 viewer 통합 테스트(codex r2 C3) |
| T-04.3-12 | Elevation of Privilege | I4 · 정정 · 전체 보기 | high | mitigate | 메뉴 `certs.submissions`(보기/쓰기) + 정보 항목 `cert.rrn_unmasked`를 서버가 매번 판정, 없으면 404/거부. 대표 계급은 권한표가 켜도 domain의 `isCertPrivacyBarredRole`이 먼저 거부한다(권한을 켠 대표 계급으로 넷 다 거부되는 통합 테스트) |
| T-04.3-13 | Tampering | 직원의 주민등록번호 정정(D-1106, D-1107 명시 항목) | high | mitigate | 주민등록번호 변경은 전체 보기 권한이 있어야 서버가 받고, 저장마다 `cert_correct`(끌 수 없음)에 누가·언제·어느 칸. 정정 UPDATE와 로그 INSERT는 한 트랜잭션(C6) — 로그 실패 주입 시 정정이 되돌아가는 통합 테스트. 서명은 입력 스키마에 없다 |
| T-04.3-150 | Elevation of Privilege | `correctSubmission`의 주민등록번호 변경(보기를 거두고 쓰기만 남긴 직원의 직접 호출) | high | mitigate | 주민등록번호가 입력에 있으면 `write`에 더해 전체 보기와 같은 `can(viewer, "certs.submissions", "view")` **그리고** `visible(viewer, "cert.rrn_unmasked")`를 본다(권한표가 보기·쓰기를 따로 저장하므로 `write`만 보면 우회된다). 보기 끔 + 쓰기 · 정보 항목 켬 viewer로 거부 · 무변경 · 로그 0줄 통합 테스트(codex final C1) |
| T-04.3-16 | Elevation of Privilege | 게이트 꺼진 동안 액션 직접 POST | high | mitigate | 세 액션 · 페이지의 첫 줄이 `assertCertFeatureEnabled()`(C1 — 페이지 안에서는 `requireSession`보다 먼저라 로그인한 사람은 권한과 무관하게 404. 로그인 전 요청은 `(app)` 레이아웃이 `/login`으로 보내 데이터가 나가지 않는다 — 모든 `(app)` 경로와 같은 이동이라 수용, Codex r3 C-2), domain 함수 넷은 맨 앞에서 `isCertFeatureEnabled()`. 통합(domain 넷) + E2E(잡아 둔 액션 요청을 게이트 꺼짐에서 다시 보내기 · 로그인한 관리자 · PM 404) |
| T-04.3-90 | Information Disclosure | 파기와 전체 보기 경합 | medium | mitigate | 전체 보기는 `FOR SHARE`로 잠근 행에서 파기 여부와 암호문을 함께 읽고 기록 → 복호화 → 커밋까지 잠금을 쥔다. 파기 UPDATE는 그 뒤에만 커밋된다(잠금 통합 테스트, 경합 전체는 04.3-12) |
| T-04.3-14 | Information Disclosure | 로그 · 인쇄 · 화면 잔류 | high | mitigate | 로그 detail은 칸 이름만. `@media print`가 화면 전체를 숨긴다. 자동 가리기(가려짐 · 비활동)와 저장 뒤 즉시 가림 |
| T-04.3-15 | Information Disclosure | 연락처 · 주소 평문 저장 | medium | accept | CONTEXT가 암호화 대상으로 정한 것은 주민등록번호뿐이다(RESEARCH Open Question 1 · Pitfall 5). 이 화면은 경영관리에게만 열리고 파기가 지운다(04.3-12). 암호화 여부는 설계 `/cso`가 정한다 — 정해지면 04.3-02 스키마와 이 리포지토리가 바뀐다 |
| T-04.3-17 | Spoofing | 방치된 개인정보취급자 세션 | medium | mitigate | 개인정보 경로마다 `touchPrivacySession` — 비활동 한도(기본 120분)를 넘으면 세션 행을 지워 끊는다. 활동 기록이 없는 첫 접근은 그 시각이 시작점이다(E3-10 — 로그인 시각 기준은 일하던 경영관리를 ERP 전체에서 로그아웃시켰다. 통합 테스트 「로그인 3시간 뒤 첫 방문 ok → 121분 무활동 만료」). 잔여 위험: 로그인한 지 오래된(최대 30일) 세션도 첫 개인정보 접근에서 새 한도를 얻는다 — 이 교환 · 판정 범위(개인정보 경로 기준) · 끊는 범위(세션 전체 대 개인정보 경로만 재인증)는 → 설계 /cso에서 확정 |
| T-04.3-41 | Tampering | 동시 정정 덮어쓰기 | medium | mitigate | 버전 조건부 갱신 — 0행이면 충돌을 돌려주고 병합·덮어쓰기 경로가 없다(통합 · E2E) |
</threat_model>

<verification>
- 단위 `record` · `registry-coverage` · `leak-scan-coverage` · `page-auth-guard` 녹색
- 통합 `cert-crypto` · `cert-review` · `leak-scan` 녹색
- E2E `cert-review` 녹색(표본은 `seedSubmittedCert()` — E3-32)
- 통합 · E2E는 같은 웨이브의 다른 플랜과 동시에 돌리지 않는다 — 세션당 플랜 하나(E3-33, `erp_test` · 포트 3100 공유)
- `pnpm lint && pnpm typecheck && pnpm build` 녹색
</verification>

<success_criteria>
- 전체 보기 = 게이트 + 대표 차단 + 메뉴 권한과 정보 항목 둘 다 + 기록 먼저(같은 tx) + 매번, 평문이 로그·인쇄·기본 화면에 없다
- `cert_submission.value`를 끄면 I4 DTO가 `project()`로 비워져 404이고 정정도 거부된다
- 정정은 칸 이름만 로그에 남기고(정정과 로그가 한 트랜잭션) 동시 정정을 거부한다
- 개인정보 경로가 비활동 한도 뒤 세션을 끊고, 활동 기록이 없는 첫 방문은 시작점으로 기록될 뿐 끊지 않는다(E3-10)
- 게이트가 꺼지면 액션 직접 POST도 평문 · 로그 · 변경 없이 끝난다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 라우트 | `/certs/submissions/[id]`(I4) |
| 함수 | `getSubmissionForReview` · `revealRrn` · `recordRrnReopen` · `correctSubmission` · `isCertPrivacyBarredRole` · `touchPrivacySession` · `getSessionId`(lib) — 로그는 Phase 4 `recordAction(…, { tx })`(C6 · E3-02, 새 로그 도우미 없음) |
| 액션 | `revealCertRrnAction` · `reopenCertRrnAction` · `correctCertSubmissionAction` |
| DTO | `CertSubmissionReviewDto` |
| 정보 항목 | `cert_submission.value` · `cert.rrn_unmasked` |
| 설정 키 | `cert.privacy.idle_minutes`(기본 120, 10~120) |
| 핵심 로그 종류 | `cert_correct`(확인증 정정, 끌 수 없음) |
| 테스트 | `test/integration/cert-review.test.ts` · `test/integration/cert-crypto.test.ts`(추가) · `test/e2e/cert-review.spec.ts` |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(/plan-eng-review E3 — 반영 지시 L387–L455@953e599). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-02 07 ③-b의 `recordAction` 재구현 사본 · 종류 거부 케이스 · verify grep — L422@953e599 · L48@953e599 | P2 | Plan 07: frontmatter `files_modified` · artifacts · key_links에서 `repositories/cert-action-log.ts` 제거 · 결정 ⑤ · ⑨ 재작성 · Task 1 ③-b(도우미를 만들지 않음) · ④ `revealRrn` · `correctSubmission`이 `recordAction(…, { tx, appendActionLog })` · behavior에서 종류 거부 케이스 삭제 · 풀 장벽 케이스는 유지(실제 `appendActionLog(viewer, entry, tx)` · tx 없으면 던짐) · verify 셋째 줄 + acceptance(02 대비 파일 밖 도우미 정의 0 · `recordAction(` 셋 · `{ tx` 두 줄) · T-04.3-121 · Artifacts 표 |
| E3-10 「비활동 2시간」이 로그인 시각 기준이라 첫 방문에 ERP 전체 로그아웃 — L423@953e599 · L114@953e599 | P2 | Plan 07: must_haves truth · 결정 ① · Task 1 behavior(「로그인 3시간 뒤 첫 방문 ok → 121분 무활동 만료」) · ③-c(로그인 시각 읽기 없음) · ⑤ `touchPrivacySession`(활동 행 없으면 지금으로 upsert · ok) · verify 셋째 줄 · acceptance · success_criteria · T-04.3-17(잔여 위험과 끊는 범위 → 설계 /cso에서 확정) |
| E3-32 07 E2E 표본을 E4 폼 조작으로 만듦(06이 같은 웨이브에서 폼을 고침) — L424@953e599 · L237@953e599 | P2 | Plan 07, Task 3: read_first · ②가 `seedSubmittedCert()`만 쓰고(전달 방식은 헬퍼 인자, 못 받으면 멈추고 02 계약부터) · acceptance(`seedSubmittedCert(` 1 이상 · E4 칸 조작 없음) · `<verification>` |
| E3-01 Phase 4 머지 뒤 시작 · 웨이브 첫 단계 `git merge origin/main` · Phase 4 부품(`recordAction({tx})` · `ui/button` aria-disabled) 기준 — L392@953e599 · L394@953e599 | P1 | frontmatter `execution_gate` · Task 1 `<precondition>`(Phase 4 머지 확인 — 없으면 멈춤) · objective 「실행 조건」 · Task 1 read_first(Phase 4 main 기준 이름) · Task 2 ③ 「고친 내용 저장」 비활성은 Phase 4 `ui/button` `disabled` + `disabledReason`(aria-disabled) 그대로 |
| E3-33 통합 · E2E 한 번에 하나(세션당 플랜 하나) — L393@953e599 | P3 | frontmatter `execution_gate.serial_tests` · objective · `<verification>` |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| E3-10 끊는 범위(세션 전체 대 개인정보 경로만 재인증)와 「오래된 세션도 첫 방문에 새 한도」 교환 — L455@953e599 | 설계 `/cso` 항목 — 계획은 세션 전체 끊기를 유지하고 T-04.3-17 · 결정 ①에 「→ 설계 /cso에서 확정」으로 남겼다(수치 · 범위를 여기서 정하지 않는다) |

### Round 2 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3·D-7·D-10). 문장 정본은 UI-SPEC이고 이 플랜은 그 문장을 옮겨 쓴다.

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---|---|---|
| D-6 맥 커맨드 글리프 → `Ctrl+키` — L73@1c3a27b | 경고 | 브라우저 인쇄 truth의 단축키 표기를 `Ctrl+P`로 · Task 2 acceptance에 UI-SPEC · I4 코드 글리프 0건(검증 줄 추가 지시) |
| D-7 「못 한다」 접두 문장(§8 규칙 5) — L82@1c3a27b | 경고 | Task 2 ③ 「고친 내용 저장」 막힘 이유를 `바뀐 칸 없음`(UI-SPEC 「I4 1차 막힘」)으로 · acceptance에 옛 접두 문장 0건 grep. 「인쇄」 막힘 이유 `저장 안 한 칸 {N} · 먼저 저장`은 04.3-11 몫(이 플랜은 자리만 비운다) |
| X-3 안심 꼬리 — L190@1c3a27b | 막는 문제 | Task 2 ③ 저장 실패 이유를 UI-SPEC 「I4 저장 실패」 `저장하지 못했습니다 · 주민등록번호 1칸` 꼴(꼬리 없음)로 |
| X-4 막힘 이유의 색(`reasonTone`) — L194@1c3a27b | 막는 문제 | 「고친 내용 저장」 막힘 = `reasonTone="info"`(UI-SPEC Color 「막힘 이유의 색」) · acceptance `reasonTone` 확인 |

### Review Feedback Deferred

| Concern | Reason |
|---|---|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 3 — (설계 /cso, 2026-09-25)

> 출처: `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`(설계 단계 /cso — 최종 판정). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---|---|---|
| E3-10 개인정보취급자 비활동 만료 기본값 — 세션 전체 대 개인정보 경로만 재인증, 120분 대 30분 | medium(결정 확정) | KEEP — 기본값 120분 · 세션 전체 끊기 유지(제작자 30분안은 개인정보 경로 밖에서 일하던 담당자를 ERP 전체에서 로그아웃시켜 기각). UI-SPEC:536·546 · CONTEXT:35 · 이 플랜의 truth · 결정 ① · T-04.3-17의 「2시간/120분」 문구는 변경 없음. 법무가 내부관리계획과 맞는지 확인(사람 확인 목록) |
| E3-13 시스템 관리자 시드가 `cert.rrn_unmasked` · `cert_submission.value` · 메뉴 `certs.submissions`를 배포마다 다시 켬(04.3-09가 insert-if-absent로 고침) — 이 플랜의 I4 E2E가 전제하는 담당자(박서연) 권한이 시드가 아니라 별도 준비에서 온다는 사실을 밝혀야 함 | medium(신규 발견 승격) | Task 3 ②에 「전제: 이 권한은 시드 기본값이 아니라 cert.setup이 켠 것이다(E3-13)」 문구 추가 · acceptance에 스펙이 권한을 스스로 켜지 않는다는 grep 추가 |

### Review Feedback Deferred
| Concern | Reason |
|---|---|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 4 — (cross r1, 2026-09-25)

> 출처: `cso-apply-cross-r1.md` B5. 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---|---|---|
| B5 — `revealRrn(권한 있는 viewer, id)` behavior가 어느 viewer인지 정하지 않아, 09의 시드 변경(E3-13) 뒤 실행자가 시드된 시스템 관리자로 읽으면 거짓이 된다 | 막는 문제 | behavior의 `revealRrn(권한 있는 viewer, id)`를 `revealRrn(viewer, id)`(viewer = 테스트 계급에 upsertPermission/upsertVisibility로 켠 viewer — 시드 기본값 아님, E3-13)로 못박음 |

### Review Feedback Deferred
| Concern | Reason |
|---|---|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적(B5)은 모두 반영했다 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-07-SUMMARY.md` when done
</output>
