---
phase: 04.3-qr-certificate-intake
plan: 08
type: execute
wave: 4
depends_on: ["04.3-05"]
files_modified:
  - lib/gcp/kms.ts
  - lib/crypto.ts
  - lib/env.ts
  - instrumentation.ts
  - scripts/rotate-key.ts
  - infra/names.sh
  - scripts/deploy.sh
  - docs/OPERATIONS.md
  - docs/ARCHITECTURE.md
  - test/unit/gcp/kms.test.ts
  - test/unit/crypto.test.ts
  - test/unit/env.test.ts
  - test/unit/deploy/deploy-sh.test.ts
  - test/unit/deploy/app-data-key-length.test.ts
autonomous: true
requirements: [CERT-02]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "스테이징·프로덕션의 데이터 키(`APP_DATA_KEY_v1`)는 Secret Manager에 평문이 아니라 Cloud KMS로 감싼 암호문(`app-data-key-v1-wrapped`)으로 있고, 앱은 뜰 때 한 번 KMS `decrypt`로 풀어 메모리에만 둔다(ROADMAP 04.3 기준 4 · Issue 7 · `COVERAGE.md` KMS INTEGRATE 하나)"
    - "`lib/crypto.ts`의 `encrypt()`/`decrypt()` 시그니처와 암호문 형식 `v1:<iv>:<tag>:<ct>`는 그대로다 — 승격 전에 만든 `v1:` 암호문(거래처 계좌번호 · 주민등록번호)이 승격 뒤에도 같은 키로 풀린다(데이터 키 값은 바뀌지 않고 보관 방식만 바뀐다)"
    - "감싼 키 변수가 설정됐는데 아직 풀지 못했으면 `encrypt`/`decrypt`가 즉시 던진다(실패로 닫힘 — 평문 저장·빈 값 통과 없음). KMS를 부르지 못하면 서버가 기동을 마치지 못해 배포 스모크에서 실패한다"
    - "로컬·테스트는 지금처럼 평문 `APP_DATA_KEY_v1` 환경 변수를 쓰고 KMS를 부르지 않는다"
    - "`scripts/deploy.sh`가 키링·대칭 키를 서울에 만들고, 런타임 서비스 계정에는 그 키의 `roles/cloudkms.cryptoKeyDecrypter`만 준다. 이미 평문 데이터 키가 있는 환경은 **그 값을** 감싸 옮기고(새 키를 만들지 않는다), 없는 환경은 새 32바이트를 만들어 바로 감싼다 — 평문은 파이프로만 흐르고 디스크에 쓰이지 않는다"
    - "서비스는 평문 데이터 키 시크릿 대신 감싼 시크릿과 KMS 키 이름을 받는다. 평문 시크릿은 스크립트가 지우지 않는다 — 운영자가 스테이징·프로덕션 복호화를 확인한 뒤 끈다(런북)"
  prohibitions:
    - statement: "런타임 서비스 계정이 KMS `encrypt`나 키 관리 권한을 가져서는 안 된다 — 감싸기는 배포 스크립트(운영자 권한)만 한다"
      status: resolved
      verification: test
    - statement: "`encrypt`/`decrypt`의 시그니처를 비동기로 바꾸거나 암호문 접두어를 바꿔서는 안 된다(CONTEXT 「이미 확정된 입력」 · ROADMAP 기준 4)"
      status: resolved
      verification: test
  artifacts:
    - path: "lib/gcp/kms.ts"
      provides: "Cloud KMS `cryptoKeys.decrypt` REST 어댑터(주입 가능한 request, 10초 시간 초과)"
      exports: ["decryptWithKms", "KmsUnavailableError"]
    - path: "lib/crypto.ts"
      provides: "loadDataKeys — 감싼 키를 한 번 풀어 메모리에 둠 · 기존 encrypt/decrypt 그대로"
      exports: ["encrypt", "decrypt", "loadDataKeys"]
    - path: "instrumentation.ts"
      provides: "Node 서버 기동 때 loadDataKeys를 한 번 부름"
      contains: "loadDataKeys"
    - path: "scripts/deploy.sh"
      provides: "ensure_kms_key · _ensure_wrapped_data_key · 서비스에 감싼 시크릿 연결"
      contains: "ensure_kms_key"
  key_links:
    - from: "instrumentation.ts"
      to: "lib/crypto.ts"
      via: "register()가 요청을 받기 전에 데이터 키를 푼다"
      pattern: "loadDataKeys"
    - from: "lib/crypto.ts"
      to: "lib/gcp/kms.ts"
      via: "감싼 키가 설정된 환경에서만 KMS decrypt를 부른다"
      pattern: "decryptWithKms"
    - from: "scripts/rotate-key.ts"
      to: "lib/crypto.ts"
      via: "CLI도 시작할 때 데이터 키를 푼다(instrumentation이 돌지 않는 경로)"
      pattern: "loadDataKeys"
---

<objective>
**As a** 경영관리(개인정보 책임자), **I want to** 주민등록번호를 푸는 데이터 키가 Secret Manager에 평문으로 있지 않고 KMS로 감싸져 있기를 바란다, **so that** 시크릿 하나가 새도 그것만으로 주민등록번호를 풀 수 없고, 기존 암호문은 그대로 읽힌다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — 기준 4(「설계 `/cso` 뒤 키 보관을 Cloud KMS 봉투 암호화로 승격하되 헬퍼 인터페이스와 암호문의 키 버전 접두어는 그대로」)에서 이야기를 끌어냈다.

오케스트레이터 사실: KMS 봉투 승격은 뒤 웨이브에서 하고 `lib/crypto.ts` 인터페이스와 `v1:` 접두어를 유지한다. 설계 `/cso`는 실행 전에 끝나므로 이 플랜은 그 결과를 전제로 W4에서 돈다. 설계 `/cso`가 이 방식을 바꾸라고 했으면 그 결과가 이긴다(SUMMARY에 적는다).

**방식:** 데이터 키 값은 그대로 두고 보관만 바꾼다. 배포 스크립트가 KMS 대칭 키로 데이터 키를 감싸 새 시크릿(`app-data-key-v1-wrapped`)에 두고, 서비스는 그 암호문과 KMS 키 이름을 환경으로 받는다. 앱은 Next.js `instrumentation.ts`의 `register()`(요청을 받기 전에 한 번, 비동기 가능 — `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md`)에서 KMS `decrypt`를 불러 메모리에 두고, `encrypt`/`decrypt`는 지금처럼 동기 함수로 그 키를 쓴다. 호출은 `COVERAGE.md` KMS 표의 INTEGRATE 하나(`cryptoKeys.decrypt`)뿐이고 이미 있는 `google-auth-library`로 REST를 부른다(새 의존성 없음).

Purpose: CERT-02의 「앱 단 암호화」를 키 보관까지 닫는다.
Output: KMS 어댑터, `loadDataKeys`, `instrumentation.ts`, 배포 단계 둘, 런북·아키텍처 한 줄씩, 단위 테스트.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/COVERAGE.md
@.planning/phases/04.3-qr-certificate-intake/04.3-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KMS decrypt 어댑터 + loadDataKeys(인터페이스·접두어 유지) + instrumentation 기동 + CLI</name>
  <files>lib/gcp/kms.ts, lib/crypto.ts, lib/env.ts, instrumentation.ts, scripts/rotate-key.ts, test/unit/gcp/kms.test.ts, test/unit/crypto.test.ts, test/unit/env.test.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/COVERAGE.md` KMS 표 — INTEGRATE는 `cryptoKeys.decrypt` 하나
    - `lib/crypto.ts`(전문) — `rawKeyFor` · `keyFor` · 오류 종류 · 머리 주석(평문·키를 로그·메시지에 넣지 않는다)
    - `lib/gcp/gcs.ts`(04.3-05 산출물) — 같은 REST 어댑터 모양(주입 가능한 request · 시간 초과 · 오류 메시지 규칙)
    - `lib/env.ts` 43~133행 — 스키마 · `APP_DATA_KEY_v*` 길이 refine(105행) · 이름 목록
    - `test/unit/crypto.test.ts` · `test/unit/env.test.ts` — 지금 단언
    - `scripts/rotate-key.ts`(전문) — 진입점 · `pathToFileURL` 실행 가드
    - `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md` — `register()` 계약 · `NEXT_RUNTIME` 분기
    - Google Cloud KMS REST `projects.locations.keyRings.cryptoKeys.decrypt` 요청·응답 모양(요청 `{ciphertext}` base64 · 응답 `{plaintext}` base64) — Context7 또는 `/browse`로 확인
  </read_first>
  <behavior>
    - `decryptWithKms({keyName: "projects/p/locations/asia-northeast3/keyRings/r/cryptoKeys/k", ciphertext: "QUJD", request})` → `POST https://cloudkms.googleapis.com/v1/projects/p/locations/asia-northeast3/keyRings/r/cryptoKeys/k:decrypt` 본문 `{"ciphertext":"QUJD"}` · 응답 `{plaintext: base64(32바이트)}` → 32바이트 `Buffer`
    - KMS 403·5xx·시간 초과 → `KmsUnavailableError`(메시지에 키 이름·암호문·평문 없음, 상태 코드만)
    - (crypto) 감싼 변수가 없으면 `loadDataKeys()`는 아무것도 부르지 않고, `encrypt`/`decrypt`는 지금처럼 평문 환경 변수 키를 쓴다(기존 단위 테스트 그대로 녹색)
    - (crypto) `APP_DATA_KEY_v1_WRAPPED` + `APP_DATA_KEY_KMS_KEY`가 있고 `loadDataKeys({unwrap: 가짜})` 전 → `encrypt`가 `MissingEncryptionKeyError`
    - (crypto) 가짜 unwrap이 키 K를 돌려준 뒤 → `encrypt("x")`가 `v1:`로 시작 · 같은 K를 평문 변수로 쓰던 시절의 `v1:` 암호문 표본이 풀린다(값 보존)
    - (crypto) unwrap 결과가 32바이트가 아니면 `loadDataKeys`가 `InvalidEncryptionKeyLengthError`로 던진다
    - (env) 감싼 변수가 있는데 KMS 키 이름이 없으면 부팅 검증 오류 · 같은 버전의 평문 변수와 감싼 변수가 비로컬에서 함께 있으면 부팅 검증 오류(평문 시크릿이 계속 붙어 있는 배포를 막는다)
  </behavior>
  <action>
① **RED:** 위 `behavior`를 `test/unit/gcp/kms.test.ts` · `test/unit/crypto.test.ts` · `test/unit/env.test.ts`에 먼저 쓴다. KMS는 주입한 가짜 `request`/`unwrap`으로만 — 네트워크 없음.

② **`lib/gcp/kms.ts`:** `decryptWithKms({ keyName, ciphertext, request? })` — 기본 `request`는 `GoogleAuth({ scopes: ["https://www.googleapis.com/auth/cloudkms"] })` 클라이언트, 시간 초과 10초, 상태 판정은 어댑터가 한다. 응답 `plaintext`를 base64로 풀어 `Buffer`. 실패는 `KmsUnavailableError`(상태 코드만), `log.warn("kms.decrypt_failed", { status })`. `COVERAGE.md` OPT-OUT 연산을 부르는 코드는 만들지 않는다.

③ **`lib/crypto.ts`:** 모듈 안에 풀린 키 저장소(버전 → `Buffer`)를 두고 `loadDataKeys(deps?)`를 더한다 — 버전마다 `APP_DATA_KEY_{v}_WRAPPED`가 있으면 `decryptWithKms`(또는 주입한 `unwrap`)로 풀어 32바이트를 확인하고 저장한다. `rawKeyFor`/`keyFor`의 판단 순서: 풀린 키가 있으면 그것 → 감싼 변수만 있고 아직 안 풀렸으면 `MissingEncryptionKeyError`(메시지 = 「키를 아직 풀지 않았습니다: APP_DATA_KEY_v1」) → 아니면 지금처럼 평문 변수. `highestAvailableVersion`도 감싼 변수를 셈에 넣는다. `encrypt`/`decrypt`의 시그니처·반환 형식·오류 종류는 바꾸지 않는다. 머리 주석에 봉투 방식 두 줄을 더한다.

④ **`lib/env.ts`:** `APP_DATA_KEY_KMS_KEY` · `APP_DATA_KEY_v1_WRAPPED` · `APP_DATA_KEY_v2_WRAPPED`(선택 문자열)를 스키마와 이름 목록에 더하고, refine 둘 — 감싼 변수가 있으면 KMS 키 이름 필수 · 비로컬에서 같은 버전의 평문과 감싼 변수를 함께 두지 않는다.

⑤ **`instrumentation.ts`(저장소 루트, 새 파일):** `export async function register()` — `process.env.NEXT_RUNTIME === "nodejs"`일 때만 `lib/crypto`를 동적 import해 `await loadDataKeys()`. 실패는 삼키지 않는다(서버가 뜨지 않아 배포 스모크가 잡는다 — `scripts/deploy.sh` `smoke`).

⑥ **`scripts/rotate-key.ts`:** 본문 시작에서 `await loadDataKeys()`(instrumentation이 돌지 않는 CLI 경로). 다른 줄은 건드리지 않는다.

⑦ 커밋 `feat(04.3): KMS envelope for app data key, interface unchanged`, 본문 한국어(값은 그대로 · 보관만 바뀜 · 실패로 닫힘).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/gcp/kms.test.ts test/unit/crypto.test.ts test/unit/env.test.ts test/unit/deploy/cli-bundle.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — instrumentation이 빌드를 깨면 여기서 잡힌다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-crypto.test.ts test/integration/vendors.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 로컬 평문 키 경로가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/crypto.ts`의 `export function encrypt(plaintext: string): string`과 `export function decrypt(stored: string): string` 시그니처가 그대로다
    - 승격 전 `v1:` 표본이 승격 뒤에도 풀리는 케이스가 단위 테스트에 있다
    - 감싼 변수 + 미로딩 → 던짐 케이스가 있다
    - `instrumentation.ts`가 `NEXT_RUNTIME`을 확인한다
  </acceptance_criteria>
  <done>데이터 키는 KMS로 감싼 형태로만 들어오고 기동 때 한 번 풀리며, 암호화 헬퍼의 모양과 기존 암호문은 그대로다</done>
</task>

<task type="auto">
  <name>Task 2: 배포 — KMS 키링·키 · 복호화 역할만 · 기존 데이터 키를 감싸 옮기기 · 서비스 연결 + 런북</name>
  <files>infra/names.sh, scripts/deploy.sh, test/unit/deploy/deploy-sh.test.ts, test/unit/deploy/app-data-key-length.test.ts, docs/OPERATIONS.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - `scripts/deploy.sh` 150~300행(`ensure_apis` · `_ensure_secret` · `ensure_secrets`) · 400~470행(`deploy_service`의 `secrets` · `env_vars`) · `main()` · 04.3-05가 더한 `ensure_cert_bucket`
    - `test/unit/deploy/deploy-sh.test.ts` · `test/unit/deploy/fakebin/gcloud` · `test/unit/deploy/app-data-key-length.test.ts`(전문 — 시드 바이트 수 대조 방식)
    - `infra/names.sh`
    - `docs/OPERATIONS.md` §9 시크릿 목록(179~206행 — 키 길이 · 회전 절차) · `docs/ARCHITECTURE.md` §4-4 앱단 암호화 계약 · §9 환경 변수 계약
    - Google Cloud 문서 `gcloud kms keyrings create` · `gcloud kms keys create`(`--purpose=encryption`) · `gcloud kms encrypt`(`--plaintext-file=-` · `--ciphertext-file=-`) · `gcloud kms keys add-iam-policy-binding` — 철자는 문서로 확인한다
  </read_first>
  <action>
① **`infra/names.sh`:** `kms_keyring() { echo "plant8-$1"; }` · `kms_key() { echo "app-data-key"; }` 두 줄.

② **`scripts/deploy.sh`:**
  - `ensure_apis`에 `cloudkms.googleapis.com`.
  - `ensure_kms_key()`: 키링(서울 `$REGION`)과 대칭 키(목적 encryption)가 없으면 만든다(있으면 건너뜀, 멱등). 런타임 서비스 계정에 **그 키에만** `roles/cloudkms.cryptoKeyDecrypter`를 바인딩한다.
  - `_ensure_wrapped_data_key <base> <seed_bytes>`(예 `app-data-key-v1 32`): 감싼 시크릿 `secret_name "${base}-wrapped" "$ENV"`에 ENABLED 버전이 있으면 끝. 없으면 — 평문 시크릿(`secret_name "$base" "$ENV"`)에 ENABLED 버전이 있으면 **그 값을** `gcloud secrets versions access latest`로 읽어, 없으면 `openssl rand -base64 "$seed_bytes"`로 만들어, 어느 쪽이든 파이프로 `gcloud kms encrypt`(평문·암호문 모두 표준 입출력)에 넘기고 그 출력을 base64로 감싼 시크릿의 새 버전으로 더한다. 평문을 변수·임시 파일에 두지 않는다. 런타임 서비스 계정에 감싼 시크릿 `secretAccessor`.
  - `ensure_secrets`에서 평문 데이터 키를 새로 만드는 줄을 `_ensure_wrapped_data_key app-data-key-v1 32` 호출로 바꾸고, `main()`에서 `ensure_kms_key`를 `ensure_secrets` 앞에 둔다.
  - `deploy_service`: `secrets`의 평문 데이터 키 연결을 `APP_DATA_KEY_v1_WRAPPED=<감싼 시크릿>:latest`로 바꾸고 `env_vars`에 `APP_DATA_KEY_KMS_KEY=projects/${PROJECT}/locations/${REGION}/keyRings/$(kms_keyring "$ENV")/cryptoKeys/$(kms_key)`.
  - 평문 시크릿을 지우거나 끄는 명령을 넣지 않는다(되돌릴 길을 남긴다 — 런북).

③ **테스트:** `deploy-sh.test.ts`에 — 키링·키 생성 인자(리전 · 목적) · 역할 바인딩이 `roles/cloudkms.cryptoKeyDecrypter`이고 대상이 그 키 · 평문 시크릿이 있는 경로에서 `secrets versions access`가 불리고 `openssl rand`가 불리지 않음 · 없는 경로에서는 `openssl rand` · 서비스 배포 인자에 `APP_DATA_KEY_v1_WRAPPED=`와 `APP_DATA_KEY_KMS_KEY=`가 있고 `APP_DATA_KEY_v1=`이 없음. `app-data-key-length.test.ts`는 새 호출 `_ensure_wrapped_data_key app-data-key-v1 <n>`의 `<n>`을 읽어 `APP_DATA_KEY_BYTES`와 대조하도록 정규식만 바꾼다.

④ **문서:** `docs/OPERATIONS.md` §9에 — 데이터 키는 감싼 시크릿 + KMS 키 · 승격 순서(스테이징 배포 → 거래처 계좌번호 전체 보기로 복호화 확인 → 평문 시크릿 버전 `disable` → 프로덕션 같은 순서) · 되돌리기(평문 시크릿 버전을 다시 켜고 이전 리비전으로 롤백) · 회전(`v2`도 같은 함수로 감싼다) 네 줄. `docs/ARCHITECTURE.md` §4-4에 봉투 한 줄 · §9에 새 환경 변수 셋. 두 문서 300줄 상한.

⑤ 커밋 `feat(04.3): wrap app data key with Cloud KMS in deploy`, 본문 한국어.
  </action>
  <reversibility rating="costly">서비스가 감싼 시크릿으로 바뀌어도 평문 시크릿은 남아 있어 이전 리비전 롤백으로 되돌릴 수 있다. 평문 시크릿을 끄는 일은 운영자가 런북대로 한다 — 이 태스크는 끄지 않는다</reversibility>
  <verify>
    <automated>bash -n scripts/deploy.sh &amp;&amp; pnpm vitest run --project unit test/unit/deploy/deploy-sh.test.ts test/unit/deploy/app-data-key-length.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>셸 문법 오류가 나거나, 종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>test "$(grep -v '^\s*#' scripts/deploy.sh | grep -cE 'roles/cloudkms\.(cryptoKeyEncrypter|cryptoKeyEncrypterDecrypter|admin)')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 런타임에 감싸기·관리 권한이 붙었다</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy.sh`에 `ensure_kms_key`와 `_ensure_wrapped_data_key`가 정의되고 `main()`/`ensure_secrets`에서 불린다
    - `deploy_service`의 시크릿 인자에 평문 데이터 키 연결이 없다(배포 테스트 단언)
    - 평문 시크릿이 있는 경로에서 새 키를 만들지 않음을 테스트가 단언한다
    - 두 문서가 300줄 이하다
  </acceptance_criteria>
  <done>배포 한 번으로 KMS 키가 서고 기존 데이터 키 값이 감싸져 옮겨지며, 서비스는 감싼 시크릿만 받는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 서버 → Cloud KMS | 런타임 서비스 계정 ADC로 `decrypt` 하나만 부른다 |
| 운영자 배포 → Secret Manager · KMS | 감싸기·키 생성은 배포 권한으로만 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-18 | Information Disclosure | 데이터 키 보관 | high | mitigate | 시크릿에는 KMS 암호문만. 풀려면 그 KMS 키의 decrypt 권한이 따로 필요하다. 평문 시크릿은 런북대로 끈다 |
| T-04.3-42 | Denial of Service | KMS 장애·권한 오류 | medium | mitigate | 기동 때 한 번만 부르고 실패하면 서버가 뜨지 않아 배포 스모크가 실패한다(이전 리비전이 트래픽을 계속 받는다). 요청마다 KMS를 부르지 않는다 |
| T-04.3-43 | Elevation of Privilege | 런타임 서비스 계정 | high | mitigate | 키 하나에 `cryptoKeyDecrypter`만. 감싸기·관리 역할이 붙으면 배포 테스트 음성 검사가 실패한다 |
| T-04.3-44 | Tampering | 승격 중 키 바뀜 | high | mitigate | 평문 시크릿이 있으면 그 값을 감싼다(새 키 생성 금지 — 테스트 단언). 승격 전 `v1:` 표본이 풀리는 단위 테스트 |
| T-04.3-45 | Information Disclosure | 평문의 디스크·로그 잔류 | medium | mitigate | 배포 스크립트는 파이프로만 넘기고, 어댑터·`loadDataKeys`의 오류·로그에 키 이름·암호문·평문이 없다 |
</threat_model>

<verification>
- 단위 `kms` · `crypto` · `env` · `deploy-sh` · `app-data-key-length` · `cli-bundle` · `docs-limits` 녹색
- 통합 `cert-crypto` · `vendors` 녹색(로컬 평문 키 경로 불변)
- `pnpm lint && pnpm typecheck && pnpm build` 녹색
</verification>

<success_criteria>
- 스테이징·프로덕션 배포가 감싼 데이터 키만 서비스에 넘긴다
- 헬퍼 인터페이스와 `v1:` 접두어가 그대로이고 기존 암호문이 풀린다
- 런타임은 KMS decrypt 하나만 할 수 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈 | `lib/gcp/kms.ts`(`decryptWithKms` · `KmsUnavailableError`) · `lib/crypto.ts` `loadDataKeys` |
| 파일 | `instrumentation.ts`(Next.js 기동 훅) |
| 환경 변수 | `APP_DATA_KEY_KMS_KEY` · `APP_DATA_KEY_v1_WRAPPED` · `APP_DATA_KEY_v2_WRAPPED` |
| 배포 단계 | `ensure_kms_key` · `_ensure_wrapped_data_key`(`scripts/deploy.sh`) · `kms_keyring` · `kms_key`(`infra/names.sh`) |
| GCP 리소스 | KMS 키링 `plant8-{환경}` · 키 `app-data-key`(서울) · 시크릿 `app-data-key-v1-wrapped-{환경}` · 키 IAM `roles/cloudkms.cryptoKeyDecrypter` |
| 테스트 | `test/unit/gcp/kms.test.ts` · `test/unit/crypto.test.ts`(추가) |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-08-SUMMARY.md` when done
</output>
