---
phase: 04.3-qr-certificate-intake
plan: 09
type: execute
wave: 4
depends_on: ["04.3-04"]
files_modified:
  - docs/design/SYSTEM.md
  - ui/shell/role-menu.ts
  - test/unit/ui/role-menu.test.ts
  - test/unit/admin-menu-registry.test.ts
  - domain/certs/feature.ts
  - app/(app)/layout.tsx
  - app/(app)/admin/page.tsx
  - domain/seed/index.ts
  - repositories/permissions.ts
  - test/integration/seed-permissions.test.ts
  - test/unit/certs/contact-phone-setting.test.ts
  - test/e2e/cert-admin-entry.spec.ts
autonomous: true
requirements: [CERT-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "플래그가 켜진 환경에서 `certs.events` 보기 권한이 있는 사람은 PC 사용자 메뉴·「더보기」 시트의 「관리」 한 줄 → `/admin` 인덱스의 넷째 그룹 `확인증` → 항목 `확인증 행사`(→ `/certs/events`)로 행사 화면에 간다 — 1차 메뉴 다섯은 그대로다(D-b · 개정 ④ · A9)"
    - "플래그가 꺼져 있으면 `확인증` 그룹이 통째로 없고, 관리자 메뉴가 `certs.events`뿐인 사람(기획 PM)에게는 「관리」 한 줄도 없다(UI Considerations error 「플래그 꺼짐이면 인덱스 그룹 없음」)"
    - "SYSTEM.md §6-10 정본 표·그림에 `확인증` 그룹 · `확인증 행사` 항목(플래그 조건 포함)이 있고 「관리자 화면 N개」 문구의 개수가 실제 항목 수와 같다 — 이 문서 개정과 `role-menu.ts`·단위 테스트가 한 커밋이다(개정 ④)"
    - "기획 PM 계급은 시드에서 `certs.events` 보기·쓰기를 기본으로 받는다(CONTEXT 재량 「만들 수 있는 사람 — 기획 PM·경영관리를 기본으로」) — 경영관리는 시드 계급이 아니라 권한표에서 켠다. `certs.submissions`와 `cert.rrn_unmasked`는 시스템 관리자 말고 누구에게도 기본으로 켜지지 않는다(대표 포함)"
    - "확인증 정보 항목 넷(`cert_event.value` · `cert_winner.value` · `cert_submission.value` · `cert.rrn_unmasked`)의 기획 PM 노출 행은 시드가 **없을 때만** 넣는다 — 관리자가 PM의 `cert_winner.value`를 끈 뒤 배포(재시드)해도 꺼진 채이고, PM이 보는 행사 상세(`getEventDetail`) 결과에 당첨자 이름 · 전화번호가 없다. 지금 시드는 `domain/seed/index.ts:173`에서 PM 노출 행을 매번 `upsertVisibility`로 쓰고 `repositories/permissions.ts:113`의 `onConflictDoUpdate`가 바뀐 값을 덮는다(codex final2 C). 시스템 관리자 행은 지금처럼 매번 켠다"
    - "설정 화면의 「확인증」 묶음에 `수령자 문의 전화`가 있고, 형식이 틀린 값을 적으면 `전화번호 형식이 아닙니다 · 02-1234-5678처럼 적어 주세요`가 오류 줄에 서며 저장된 값은 이전 그대로다. 비우기는 막지 않는다(UI Considerations error 설정 문의 전화 — 오류 줄 · 값 불변은 E2E, 빈 값 · 지역번호 · 대표번호 허용은 설정 스키마 단위 테스트. E2E는 공유 설정을 바꾸지 않는다, C4)"
    - "PM 시드 뒤에도 기존 `test/e2e/admin-nav.spec.ts`의 PM 단언(「관리」 없음 · `/admin` 404)은 고치지 않고 녹색이다 — `desktop` 프로젝트는 `cert-setup`보다 먼저 돌아 기능이 꺼진 상태라 `withCertMenusGated`가 확인증 메뉴를 걷는다(codex A2)"
    - "C4: 이 플랜의 E2E는 `test/e2e/helpers/cert.ts`로만 준비하고 `cert.enabled` · 문의 전화 설정을 직접 바꾸지 않는다. 게이트를 끄는 케이스는 도우미 `withCertFeatureOff(fn)` 안에서만 하고, 스펙은 `mode: \"serial\"`로 확인증 Playwright 프로젝트 `certs` 안에서 돈다(codex #21)"
  prohibitions:
    - statement: "1차 메뉴를 여섯으로 늘리거나 확인증 진입을 1차 메뉴·하단 탭에 두어서는 안 된다(셸 계약 D-22 · D-b 버린 대안)"
      status: resolved
      verification: test
  artifacts:
    - path: "ui/shell/role-menu.ts"
      provides: "ADMIN_MENUS 넷째 그룹 `확인증` · `certs.events`"
      contains: "확인증 행사"
    - path: "domain/certs/feature.ts"
      provides: "플래그가 꺼져 있으면 allowedMenus에서 확인증 메뉴를 걷어 내는 함수"
      exports: ["isCertFeatureEnabled", "withCertMenusGated"]
    - path: "test/e2e/cert-admin-entry.spec.ts"
      provides: "PM 「관리」 → 확인증 행사 · 게이트 꺼짐(헬퍼) · 설정 문의 전화 형식 오류(값 불변)"
      min_lines: 30
    - path: "test/unit/certs/contact-phone-setting.test.ts"
      provides: "수령자 문의 전화 설정 스키마 — 빈 값 · 지역번호 · 대표번호 허용, 틀린 형식 거부와 오류 문장"
      min_lines: 15
  key_links:
    - from: "app/(app)/layout.tsx"
      to: "domain/certs/feature.ts"
      via: "셸 메뉴 계산 전에 플래그로 확인증 메뉴를 거른다"
      pattern: "withCertMenusGated"
    - from: "app/(app)/admin/page.tsx"
      to: "domain/certs/feature.ts"
      via: "인덱스 그룹 계산 전에 같은 거르기"
      pattern: "withCertMenusGated"
    - from: "test/unit/ui/role-menu.test.ts"
      to: "docs/design/SYSTEM.md"
      via: "§6-10 정본 표를 읽어 adminIndexGroups와 대조"
      pattern: "확인증"
---

<objective>
**As a** 기획 PM·경영관리, **I want to** 주소를 외우지 않고 「관리」에서 확인증 행사 화면으로 가고 싶다, **so that** 행사 현장 전에 QR을 만들고 제출 상황을 볼 수 있다 — 기능이 꺼진 환경에서는 그 길 자체가 보이지 않는다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — Goal의 행위자(담당자)와 기준 2(「이 페이즈의 만들기 진입점은 내부 행사 화면」)에서 이야기를 끌어냈다.

`docs/design/DECISIONS.md` D-b(04.3-01이 적었다)와 UI-SPEC 개정 ④를 코드와 SYSTEM.md에 옮긴다. `ui/`는 `domain`을 import할 수 없으므로(D-26) 플래그 판정은 셸을 부르는 두 곳(`app/(app)/layout.tsx` · `app/(app)/admin/page.tsx`)이 `allowedMenus`를 거른 뒤 넘긴다 — `role-menu.ts`는 순수 함수로 남는다. PM 기본 권한은 Phase 4 `projects` 시드 선례(`insertPermissionIfAbsent`)를 따른다 — 권한표에서 이미 바꾼 값은 덮지 않는다. 확인증 정보 항목의 PM 노출 행도 같은 원칙으로 `insertVisibilityIfAbsent`(없을 때만)로 넣는다(codex final2 C — 04.3-04가 `cert_winner.value`를 `staffDefault: true`로 등록하므로 덮어쓰는 시드는 관리자가 끈 당첨자 정보를 다음 배포에 되살린다). **Phase 4 플랜 04-20이 시스템 관리자 밖 모든 계급의 노출 시드를 없을 때만 넣도록 고치는 중이다**(04-20 Task 1 ① — 같은 이름의 `insertVisibilityIfAbsent`). 04-20이 먼저 들어왔으면 그 함수와 시드 루프를 그대로 쓰고 이 플랜은 시드 · 리포지토리를 고치지 않되 회귀 테스트는 둔다.

Purpose: 04.3-04의 행사 화면에 가는 유일한 화면 안 경로(A9).
Output: SYSTEM §6-10 개정, `role-menu.ts` 넷째 그룹, 플래그 거르기, PM 시드, 설정 화면 확인, E2E 하나.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 개정 ④ — SYSTEM §6-10 · role-menu 넷째 그룹 · 단위 테스트(한 커밋) + 플래그 거르기</name>
  <files>docs/design/SYSTEM.md, ui/shell/role-menu.ts, test/unit/ui/role-menu.test.ts, test/unit/admin-menu-registry.test.ts, domain/certs/feature.ts, app/(app)/layout.tsx, app/(app)/admin/page.tsx</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「SYSTEM.md · DECISIONS.md 개정 목록」 ④ 줄 · Copywriting I 표 첫 줄(그룹 `확인증` · 항목 `확인증 행사`)
    - `docs/design/DECISIONS.md` D-b 항목(04.3-01 산출물)
    - `docs/design/SYSTEM.md` §6-10(610행 근처 — 정본 표 `| 그룹 | 항목 |` · 그림 · 「관리자 화면 10개」 문구) · §6-0 (a) · §7-8의 「관리」 한 줄 문장
    - `ui/shell/role-menu.ts`(전문 — `ADMIN_MENUS` · 그룹 상수 · `buildAdminMenu` · `adminIndexGroups` · 머리 주석의 개수)
    - `test/unit/ui/role-menu.test.ts`(전문 — §6-10 표 파서 · `ADMIN_MENU_KEYS` 복제 목록) · `test/unit/admin-menu-registry.test.ts`
    - `app/(app)/layout.tsx` · `app/(app)/admin/page.tsx`(전문 — `allowedMenus` 계산 두 곳)
    - `domain/certs/feature.ts`(04.3-02 산출물)
  </read_first>
  <behavior>
    - `adminIndexGroups({roleId: "role-pm", allowedMenus: ["projects", "certs.events"]})` → `[{label: "확인증", items: [{label: "확인증 행사", href: "/certs/events"}]}]` · `roleMenu(같은 viewer).adminMenu` → `[{label: "관리", href: "/admin"}]`
    - 시스템 관리자(모든 메뉴) → 그룹 넷, 순서 마스터 → 설정·권한 → 운영 기록 → 확인증, §6-10 표와 원소 단위로 같다
    - `allowedMenus`에 `certs.events`가 없으면 `확인증` 그룹이 없고, 다른 관리자 메뉴가 없으면 `adminMenu`가 빈 배열
    - `withCertMenusGated(["projects", "certs.events", "certs.submissions"])` → 플래그 꺼짐이면 `["projects"]` · 켜짐이면 그대로
    - MENUS의 `certs.events`가 인덱스에 `/certs/events`로 나타난다(admin-menu-registry 대조에 한 케이스)
  </behavior>
  <action>
① **RED:** 위 `behavior`를 `test/unit/ui/role-menu.test.ts`(복제 목록에 `certs.events` · PM 케이스)와 `test/unit/admin-menu-registry.test.ts`(확인증 한 케이스)에 먼저 쓴다. §6-10 표를 읽는 기존 파서가 넷째 그룹을 읽도록 표 행만 더하면 되는지 확인한다.

② **`docs/design/SYSTEM.md` §6-10:** 정본 표에 `| 확인증 | 확인증 행사 |` 행과 한 줄 「기능 게이트(환경 `CERT_FEATURE_ALLOWED` + 설정 `cert.enabled`, 04.3-02 `isCertFeatureEnabled`)가 꺼져 있으면 그룹째 없다 — Phase 04.3 임시 진입, Phase 11이 경품 지출결의 화면으로 옮긴다(D-b)」, 그림에 넷째 그룹, 절 제목·본문의 「관리자 화면 10개」를 실제 항목 수로 고친다. §6-0 (a)의 「세 그룹」 같은 개수 문장이 있으면 같이 맞춘다. **이 문서 수정 · `role-menu.ts` · 두 단위 테스트는 한 커밋이다(개정 ④).**

③ **`ui/shell/role-menu.ts`:** `ADMIN_GROUP_CERTS = "확인증"` · `ADMIN_MENUS` 끝에 `{ key: "certs.events", label: "확인증 행사", href: "/certs/events", group: ADMIN_GROUP_CERTS }` · `adminIndexGroups`의 그룹 순서 끝에 확인증. 머리 주석의 관리자 화면 개수 문장을 고친다(그 밖의 주석·코드는 건드리지 않는다). `buildAdminMenu`는 `ADMIN_MENUS`를 보므로 PM도 「관리」를 얻는다 — 로직 변경 없음.

④ **`domain/certs/feature.ts`:** `withCertMenusGated(allowedMenus)` — `isCertFeatureEnabled()`가 거짓이면 `certs.`로 시작하는 키를 걷어 낸 새 배열, 참이면 그대로.

⑤ **`app/(app)/layout.tsx` · `app/(app)/admin/page.tsx`:** `allowedMenus`를 만든 바로 뒤 `withCertMenusGated`를 한 번 거쳐 `roleMenu`/`adminIndexGroups`에 넘긴다. `admin/page.tsx` 머리 주석의 「관리자 화면 10개」 개수만 고친다.

⑥ 커밋 `feat(04.3): certificate group in admin index behind flag`(①~③ 한 커밋), 본문 한국어. ④⑤가 별도 커밋이어도 된다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/role-menu.test.ts test/unit/admin-menu-registry.test.ts test/unit/design-system-docs.test.ts test/unit/ui/admin-index-css.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — §6-10 표와 role-menu가 어긋났다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 특히 `ui`가 `domain`을 import하면 boundaries 규칙이 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - SYSTEM.md §6-10 표에 `확인증 행사` 행이 있다(`grep -q '| 확인증 | 확인증 행사 |' docs/design/SYSTEM.md`)
    - `ui/shell/role-menu.ts`가 `@/domain`을 import하지 않는다
    - `git log -1 --stat`(개정 ④ 커밋)에 SYSTEM.md · role-menu.ts · role-menu.test.ts가 함께 있다
    - PM 케이스와 플래그 거르기 케이스가 단위 테스트에 있다
  </acceptance_criteria>
  <done>「관리」 인덱스에 플래그 뒤 `확인증` 그룹이 서고, SYSTEM §6-10과 코드·테스트가 한 커밋으로 맞는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PM 기본 권한 · 확인증 노출 시드(없을 때만) + 진입·설정 E2E(플래그 켬/끔 · 문의 전화 형식 오류) + admin-nav 대조</name>
  <files>domain/seed/index.ts, repositories/permissions.ts, test/integration/seed-permissions.test.ts, test/unit/certs/contact-phone-setting.test.ts, test/e2e/cert-admin-entry.spec.ts</files>
  <precondition>`playwright.config.ts`의 `cert-setup` 프로젝트 `dependencies`에 `"desktop"`과 `"mobile-375"`가 있다(04.3-02, codex A2) — 없으면 멈추고 04.3-02를 먼저 고친다</precondition>
  <read_first>
    - `domain/seed/index.ts` 120~180행 — 시스템 관리자 전 메뉴 · `projects` PM 기본(`insertPermissionIfAbsent`) · 정보 항목 `staffDefault` · 노출 루프(165~179행 — PM 행을 매번 `upsertVisibility`, 173행)
    - `repositories/permissions.ts` 53~73행(`insertPermissionIfAbsent` — `onConflictDoNothing`) · 86~117행(`findVisibility` · `upsertVisibility`의 `onConflictDoUpdate`) — `insertVisibilityIfAbsent`가 이미 있는지(Phase 4 04-20이 먼저 들어왔는지) 여기서 본다
    - `.planning/phases/04-project-quote-ledger/04-20-PLAN.md` Task 1 ①(`insertVisibilityIfAbsent(viewer, row)` 계약 · 노출 루프 (b)(c)) — 이름 · 모양을 맞추기 위해 읽기만
    - `test/integration/seed-permissions.test.ts` — 시드 단언 방식
    - `test/integration/cert-events.test.ts`(04.3-04 — 기능 켜기 · 문의 전화 · PM 사용자 viewer · `createEvent` 준비 방식) · `domain/certs/events.ts`의 `createEvent` · `getEventDetail`(04.3-04)
    - `app/(app)/admin/settings/page.tsx` · `settings-form-client.tsx`(전문 — 네임스페이스 묶음 · 문자 칸 blur 저장 · `errorMessageOf`) · `lib/actions/zod-error-message.ts`
    - `domain/settings/keys.ts`의 `CERT_CONTACT_PHONE`(04.3-02 — zod 메시지 · 문의 전화 전용 검증 함수) · 04.3-02가 이미 그 스키마 단위 테스트를 뒀는지(겹치면 이 플랜의 단위 파일은 그 테스트에 없는 케이스만 둔다)
    - `test/e2e/fixtures.ts` — `createFixtureUser({roleId})` · 로그인
    - `test/e2e/helpers/cert.ts`(04.3-02, C4 — 읽기만: `createCertEvent` · `withCertFeatureOff(fn)`, 기능 켜기 · 문의 전화는 `test/e2e/cert.setup.ts`가 한 번) · `playwright.config.ts`의 `certs` 프로젝트(`*cert*.spec.ts`, 워커 1) · `cert-setup`의 `dependencies`(04.3-02 — `desktop` · `mobile-375` 뒤)
    - `test/e2e/admin-nav.spec.ts` 106~130행 — 기획 PM 케이스가 무엇을 보는지(사용자 메뉴의 「관리」 개수 0 · `/admin` 404) · `test/e2e/global-setup.ts`(매 실행 스키마를 비우고 시드 — `cert.enabled`는 기본 false로 돌아간다)
  </read_first>
  <behavior>
    - (통합) 시드 뒤 PM에 `certs.events` 보기·쓰기가 있고 `certs.submissions`가 없으며, 대표 계급에 확인증 메뉴·정보 항목이 하나도 없다
    - (통합) PM의 `certs.events`를 권한표에서 끈 뒤 시드를 다시 돌려도 꺼진 채다(`insertPermissionIfAbsent`)
    - (통합, 노출 재시드 회귀 — codex final2 C) 시드만 돈 DB에서(PM의 `certs.events` 보기 · 쓰기는 이 케이스 안에서 `upsertPermission`으로 준다 — 04.3-04 `cert-events.test.ts` 선례, 메뉴 시드와 무관하게 노출만 본다) PM 사용자 viewer가 `createEvent`로 당첨자 `김하늘` `010-4821-7730` 행사를 만들고 `getEventDetail` → 결과에 `김하늘`이 보인다(대조) → `upsertVisibility(SYSTEM_VIEWER, {roleId: DEFAULT_ROLE_ID, infoItem: "cert_winner.value", visible: false})`(관리자가 끈 것과 같은 행) → `seedMasterData(SYSTEM_VIEWER)` → `findVisibility(SYSTEM_VIEWER, DEFAULT_ROLE_ID, "cert_winner.value")`의 `visible`이 거짓 · 같은 viewer의 `getEventDetail` 결과 전체를 `JSON.stringify`한 문자열에 `김하늘` · `01048217730` · `010-4821-7730`이 없고 당첨자 DTO에 `name` · `phone` 키가 없다 · 시스템 관리자의 `cert_winner.value`는 재시드 뒤 참
    - (통합) 빈 DB의 첫 시드는 PM에게 `cert_event.value` · `cert_winner.value`를 켜고(`staffDefault: true`) `cert_submission.value` · `cert.rrn_unmasked`는 끈다
    - (단위) `CERT_CONTACT_PHONE` 스키마 — `""` · `02-1234-5678` · `031-123-4567` · `1588-1234` · `010-1234-5678` 허용(숫자만으로 저장) · `02-12` 거부와 메시지 `전화번호 형식이 아닙니다 · 02-1234-5678처럼 적어 주세요`
    - (E2E 켬) PM 사용자 메뉴 「관리」 → `/admin`에 그룹 `확인증`만 · 항목 `확인증 행사` → `/certs/events`
    - (E2E 끔) `withCertFeatureOff` 안에서 같은 PM에게 「관리」가 없고 `/admin` 404 · 시스템 관리자의 `/admin`은 그룹 셋
    - (E2E 설정) `02-12` blur → 오류 줄 문장 · 새로 고침 뒤 값 불변
    - (admin-nav 대조 — codex A2) 시드 변경 뒤에도 `desktop` 프로젝트의 admin-nav PM 케이스(「관리」 0개 · `/admin` 404)가 그대로 녹색이다
  </behavior>
  <action>
① **RED — 테스트 먼저(codex C7):**
  - `test/integration/seed-permissions.test.ts`: 위 `behavior`의 통합 두 줄을 단언한다.
  - `test/unit/certs/contact-phone-setting.test.ts`: `CERT_CONTACT_PHONE` 설정 정의의 스키마로 위 `behavior`의 단위 줄. 04.3-02가 이미 만든 스키마를 고정하는 테스트라 처음부터 녹색일 수 있다 — 그 경우 SUMMARY에 「특성 고정 테스트」로 적는다. 공유 설정 값은 건드리지 않는다.
  - `test/e2e/cert-admin-entry.spec.ts`: 아래 ③의 내용을 이 단계에서 쓴다.
  - 통합 테스트와 E2E 「게이트 켬」 케이스가 PM에게 `certs.events`가 없어서 실패하는 것을 확인한다(실패 이유가 권한 없음이어야 한다 — 다른 이유면 테스트를 고친다). 노출 재시드 케이스는 재시드 뒤 PM `cert_winner.value`가 다시 참이 되어(`domain/seed/index.ts:173` `upsertVisibility`) `visible` 단언 또는 `김하늘` 없음 단언에서 실패하는 것을 확인한다 — 04-20이 먼저 들어와 이미 녹색이면 RED가 없는 것이 정상이고 SUMMARY에 「04-20이 고침 · 회귀 테스트만 추가」로 적는다.

② **GREEN — `domain/seed/index.ts`:** `projects` PM 블록 바로 뒤에 같은 모양의 블록 — `DEFAULT_ROLE_ID`에 `certs.events` `view` · `write`를 `insertPermissionIfAbsent`(한국어 주석 두 줄: CONTEXT 재량 「기획 PM·경영관리를 기본으로」, 경영관리는 시드 계급이 아니라 권한표에서 켠다). `certs.submissions`와 정보 항목 `cert.rrn_unmasked` · `cert_submission.value`는 PM·대표에게 켜지 않는다(정보 항목은 `staffDefault: false`라 이미 꺼진다 — 확인만). ①의 통합 · E2E가 녹색이 된다.

②-b **GREEN — 확인증 노출 시드를 없을 때만(codex final2 C):**
  - `repositories/permissions.ts`: `insertVisibilityIfAbsent`가 **없으면**(04-20 아직 안 들어옴) `upsertVisibility` 바로 뒤에 더한다 — `insertPermissionIfAbsent`와 같은 모양(viewer 첫 인자 · `void viewer` · 입력은 `upsertVisibility`와 같은 `{roleId, infoItem, visible, updatedBy?}` · `onConflictDoNothing({target: [visibilityMatrix.roleId, visibilityMatrix.infoItem]})`, 한국어 주석 두 줄: 이미 있는 칸은 건드리지 않는다 · 관리자가 끈 노출을 시드가 되살리지 않기 위해). 이름 · 인자 모양은 04-20 Task 1 ①과 같게 둔다 — 04-20이 뒤에 들어오면 이 함수를 있는 그대로 쓴다(SUMMARY에 한 줄). **이미 있으면** 이 파일을 고치지 않는다.
  - `domain/seed/index.ts` 노출 루프(165~179행): 04-20이 이미 시스템 관리자 밖 행을 `insertVisibilityIfAbsent`로 바꿔 두었으면 고치지 않는다. 아니면 `DEFAULT_ROLE_ID` 행만, 키가 `cert_` 또는 `cert.`로 시작하는 확인증 항목일 때 `insertVisibilityIfAbsent`로, 그 밖의 항목은 지금 `upsertVisibility` 그대로 둔다(다른 항목의 덮어쓰기는 04-20 몫 — 이 플랜은 건드리지 않는다). 시스템 관리자 행은 `upsertVisibility` 그대로(전 항목 켬 — 04-20 (c)와 같은 원칙). 한국어 주석 한 줄: 관리자가 PM에게서 끈 확인증 정보(특히 당첨자 이름 · 전화번호)를 배포 시드가 되살리지 않는다. ①의 노출 재시드 케이스가 녹색이 된다.

③ **`test/e2e/cert-admin-entry.spec.ts`(데스크톱, C4 — ①에서 쓴다):**
  - 머리: `test.describe.configure({ mode: "serial" })`. 준비는 C4(기능 켜기 · 문의 전화는 `cert.setup.ts`). 스펙은 `cert.enabled` · 문의 전화 설정을 직접 바꾸지 않는다.
  - 게이트 켬: 기획 PM 계정 로그인 → 사용자 메뉴에 「관리」 → `/admin`에 그룹 `확인증`만 · 항목 `확인증 행사` → 누르면 `/certs/events`.
  - 게이트 끔: `withCertFeatureOff` 안에서 — 같은 PM의 사용자 메뉴에 「관리」가 없고 `/admin`이 404 · 시스템 관리자의 `/admin`에는 그룹 셋뿐(도우미가 끝에서 다시 켠다).
  - 설정 형식 오류(값을 바꾸지 않는 경로만): 시스템 관리자로 `/admin/settings` → 「확인증」 묶음에 `수령자 문의 전화` → 지금 값을 읽어 둔다 → `02-12` 입력 후 blur → 오류 줄에 `전화번호 형식이 아닙니다 · 02-1234-5678처럼 적어 주세요` → 새로 고침 → 읽어 둔 값 그대로. 올바른 값 저장 · 비우기는 E2E에서 하지 않는다(③ 단위 테스트가 맡는다 — 다른 확인증 스펙이 쓰는 공유 설정을 바꾸지 않기 위해).

④ **admin-nav 대조(codex A2):** `test/e2e/admin-nav.spec.ts`의 PM 케이스는 사용자 메뉴의 「관리」 개수 0과 `/admin` 404를 본다. ②로 PM이 `certs.events`를 갖게 되지만 `desktop` 프로젝트가 도는 동안은 기능이 꺼져 있다 — `global-setup.ts`가 매 실행 스키마를 비워 `cert.enabled`가 기본 false이고, `cert.enabled`를 켜는 `cert-setup`은 04.3-02의 `dependencies`로 `desktop` · `mobile-375`가 끝난 뒤에 돈다. 그래서 `withCertMenusGated`가 PM의 확인증 메뉴를 걷고 그 단언은 여전히 참이다 — 새 메뉴가 정당하게 보이는 상황이 아니므로 admin-nav 단언은 고치지 않는다. 기능이 켜진 PM의 「관리」는 ③의 「게이트 켬」이, 꺼진 PM은 「게이트 끔」이 본다. `desktop` 프로젝트로 admin-nav를 돌려 녹색을 확인한다(verify 셋째 줄). 빨가면 시드 순서나 `cert-setup` 의존이 어긋난 것이다 — admin-nav 단언을 바꾸지 말고 원인을 고친다.

⑤ 커밋 `feat(04.3): default PM access to certificate events`, `fix(04.3): keep revoked certificate visibility across reseed`(②-b — 고칠 것이 없었으면 회귀 테스트만 `test(04.3): certificate visibility reseed regression`), `test(04.3): admin entry and settings E2E`.
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/seed-permissions.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/certs/contact-phone-setting.test.ts &amp;&amp; pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-admin-entry.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No tests found"가 나온다 — `--no-deps`라 파일 거르기가 `cert-setup`에도 걸리므로 `cert.setup.ts`를 함께 넘긴다(의존 프로젝트는 파일 거르기와 상관없이 전부 돌아 전체 E2E가 된다)</fails_when>
    <automated>pnpm playwright test --project=desktop test/e2e/admin-nav.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — PM 시드가 기능 꺼진 상태의 「관리 없음 · /admin 404」 단언을 깼다(codex A2)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 특히 `repositories/permissions.ts`의 새 함수가 viewer 첫 인자 규칙(`eslint/rules/repository-viewer-param.mjs`)을 어겼다</fails_when>
  </verify>
  <acceptance_criteria>
    - 시드가 `insertPermissionIfAbsent`로 PM `certs.events`를 넣는다(덮어쓰는 `upsertPermission`이 아니다)
    - 시드 노출 루프에서 확인증 항목의 PM 행은 `insertVisibilityIfAbsent`로 들어가고 시스템 관리자 행은 `upsertVisibility` 그대로다(04-20이 먼저 들어왔으면 그 루프 그대로 — SUMMARY에 어느 쪽인지 한 줄) · `grep -c "export async function insertVisibilityIfAbsent" repositories/permissions.ts`가 1(중복 정의 없음)
    - 통합 테스트가 PM `cert_winner.value` 끔 → `seedMasterData` 재실행 → 노출 행이 꺼진 채이고 PM `getEventDetail` 결과에 `김하늘` · 전화번호가 없음을 단언하고, 끄기 전 같은 호출에 `김하늘`이 보이는 대조가 있다(codex final2 C)
    - 대표 계급에 확인증 권한이 없음을 통합 테스트가 단언한다
    - E2E가 게이트 켬/끔 두 상태와 설정 형식 오류(값 불변)를 단언하고 녹색이다
    - E2E가 공유 설정을 직접 바꾸지 않는다(`! grep -n "setSettingValue" test/e2e/cert-admin-entry.spec.ts`)와 `mode: "serial"`이 있다(C4)
    - 설정 스키마 단위 테스트가 빈 값 허용과 `02-12` 거부 문장을 단언한다
    - 시드 변경 커밋보다 통합 · E2E 테스트가 먼저 쓰였고 RED를 확인했다(SUMMARY에 실패 이유 한 줄 — codex C7)
    - `desktop` 프로젝트의 `test/e2e/admin-nav.spec.ts`가 시드 변경 뒤에도 녹색이고, 그 파일은 이 플랜에서 바뀌지 않는다(`git log --oneline -- test/e2e/admin-nav.spec.ts`에 04.3-09 커밋이 없다 — codex A2)
  </acceptance_criteria>
  <done>PM이 「관리」에서 행사 화면에 가고, 플래그가 꺼지면 길이 사라지며, 관리자가 PM에게서 끈 확인증 노출이 재시드에 되살아나지 않고, 수령자 문의 전화 설정이 형식을 지킨다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 셸 메뉴 계산 → 화면 | 메뉴는 편의일 뿐 — 모든 확인증 라우트는 자체적으로 권한·플래그를 다시 판정한다(04.3-04 · 07) |
| 시드 → 권한표 | 배포마다 시드가 돈다(멱등) — 관리자가 바꾼 값을 덮으면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-20 | Information Disclosure | 플래그 꺼짐 중 진입 경로 | low | mitigate | `withCertMenusGated`가 셸·인덱스 두 곳에서 확인증 메뉴를 걷는다. 라우트 자체도 플래그로 404(04.3-04) |
| T-04.3-46 | Elevation of Privilege | PM 기본 권한 | medium | mitigate | PM에는 `certs.events`(자기 행사 범위, 04.3-04)만. 제출 내용·주민등록번호 권한은 시스템 관리자 말고 기본으로 켜지지 않고, `insertPermissionIfAbsent`라 관리자가 끈 값을 시드가 되살리지 않는다(통합 테스트) |
| T-04.3-170 | Information Disclosure | 배포 시드의 확인증 노출 행(PM `cert_winner.value` 등) | high | mitigate | 확인증 정보 항목 넷의 PM 노출 행은 `insertVisibilityIfAbsent`(`onConflictDoNothing`)로 없을 때만 넣는다 — 관리자가 끈 당첨자 이름 · 전화번호 노출이 다음 배포에 되살아나지 않는다(지금 `seed/index.ts:173` `upsertVisibility` · `permissions.ts:113` `onConflictDoUpdate`). 04-20이 먼저 들어오면 그 루프로 같은 보장. 통합 회귀(끔 → 재시드 → 노출 행 거짓 · `getEventDetail`에 이름 · 전화 없음, 끄기 전 대조) |
| T-04.3-47 | Tampering | 문의 전화 설정 | low | mitigate | 서버 zod가 한국 전화번호 형식만 받아 숫자로 저장한다. 설정 변경은 기존 `settings_change` 로그에 남는다. 이미 만든 행사는 사본을 쓰므로 설정 변경이 열린 링크에 번지지 않는다(04.3-02) |
</threat_model>

<verification>
- 단위 `role-menu` · `admin-menu-registry` · `design-system-docs` · `admin-index-css` 녹색
- 통합 `seed-permissions`(확인증 노출 재시드 회귀 포함) · 단위 `contact-phone-setting` 녹색
- E2E `cert-admin-entry` 녹색(C4 헬퍼 · 공유 설정 직접 변경 없음)
- E2E `admin-nav`(`desktop`) 녹색 — PM 시드 뒤에도 기능 꺼진 상태의 「관리 없음 · /admin 404」 단언이 참(codex A2)
- `pnpm lint && pnpm typecheck` 녹색
</verification>

<success_criteria>
- 1차 메뉴 다섯 그대로, 「관리」 인덱스 넷째 그룹으로 행사 화면에 간다
- 플래그가 꺼지면 그룹·「관리」 한 줄(PM)이 사라진다
- SYSTEM §6-10 · role-menu · 테스트가 한 커밋으로 맞는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 디자인 문서 | `docs/design/SYSTEM.md` §6-10 넷째 그룹 `확인증`(개정 ④) |
| 셸 | `ui/shell/role-menu.ts` 그룹 `확인증` · 항목 `확인증 행사` |
| 함수 | `withCertMenusGated`(`domain/certs/feature.ts`) |
| 시드 | 기획 PM `certs.events` 보기·쓰기(없을 때만) · 확인증 정보 항목 PM 노출(없을 때만) |
| 함수(repository) | `insertVisibilityIfAbsent`(`repositories/permissions.ts` — 04-20이 먼저 만들었으면 그것) |
| 테스트 | `test/e2e/cert-admin-entry.spec.ts` · 단위·통합 케이스 추가 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-09-SUMMARY.md` when done
</output>
