---
phase: 04.3-qr-certificate-intake
plan: 10
type: execute
wave: 5
depends_on: ["04.3-04", "04.3-06", "04.3-07"]
files_modified:
  - domain/certs/events.ts
  - repositories/cert-events.ts
  - repositories/cert-winners.ts
  - domain/action-log/record.ts
  - test/unit/action-log/record.test.ts
  - app/(app)/certs/events/actions.ts
  - app/(app)/certs/events/actions.registry.ts
  - test/integration/cert-events-edit.test.ts
  - app/(app)/certs/events/winner-table-parts.tsx
  - app/(app)/certs/events/event-create-form.tsx
  - app/(app)/certs/events/[id]/page.tsx
  - app/(app)/certs/events/[id]/winners-section.tsx
  - app/(app)/certs/events/[id]/winners-view.tsx
  - app/(app)/certs/events/[id]/close-link-dialog.tsx
  - app/(app)/certs/events/[id]/unlock-outcome.ts
  - test/unit/certs/unlock-outcome.test.ts
  - app/(app)/certs/events/[id]/save-payload.ts
  - test/unit/certs/save-payload.test.ts
  - app/(app)/certs/events/[id]/event-detail.module.css
  - ui/table/RowSheet.tsx
  - ui/table/RowSheet.module.css
  - ui/table/use-grid-keyboard.ts
  - ui/table/types.ts
  - ui/table/Table.tsx
  - test/unit/ui/grid-action-cell.test.ts
  - ui/confirm-dialog/ConfirmDialog.tsx
  - test/unit/ui/confirm-dialog.test.ts
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - test/e2e/cert-events-edit.spec.ts
  - test/e2e/mobile-cert-events.spec.ts
autonomous: true
requirements: [CERT-01]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 190000
  raw_tokens: 190000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "행사 상세(I3)의 당첨자 표는 접수 중이고 보는 사람이 그 행사를 고칠 수 있을 때만 편집 표다 — 미제출 줄의 여섯 칸(이름 · 전화번호 · 경품명 · 수량 · 전달 · 구별 표시)은 편집, 제출된 줄의 여섯 칸은 읽기 전용이고 이유 `제출된 자리 · 고칠 수 없음`이 선다. 닫혔거나 보기만 가능하면 흰 머리글 읽기 표이고 1차·힌트 줄이 없다(UI Considerations partial I3 제출된 줄과 미제출 줄 혼재)"
    - "700~1023(좁은 PC)에서는 편집 가능한 행사도 I3 당첨자 표가 읽기 표다 — 셀 편집 · 새 줄 · 힌트 줄이 없고 1차 「일괄 저장 N」은 N ≥ 1일 때만 선다(1024 이상에서 고친 뒤 창이 좁아진 경우 — SYSTEM §7-3 「`N ≥ 1`이면 항상 렌더한다」, Phase 4 04-49 R1과 같은 규칙 · 반영 검사 r1 N-4), 머리 「링크 닫기」와 행동 열 3차(「잠금 풀기」 · 「제출 내용」 — 읽기 표의 보통 `Tab` 대상)는 남는다. 폭 판정은 Phase 4 견적 원장이 쓰는 판정 하나를 그대로 쓴다(SYSTEM §6 ⑷ DR-36 · /plan-design-review D-10)"
    - "담당자는 I3에서 새 줄(`Ctrl+Enter` — Phase 4 표기 D-94)로 당첨자를 더할 수 있고 더한 사람도 같은 링크로 제출한다 — 저장된 줄은 지우지 않는다(Delete는 저장 전 새 줄만, A7 · D-1102 「행사당 QR 하나」)"
    - "I3 일괄 저장은 전부 저장 또는 전부 거부다 — 셀 오류가 있으면 오류 셀이 고정되고 합계 행에 `오류 N칸 · 전부 거부`, 셀 오류 문장은 11종(`validateWinnerRows` — UI-SPEC Copywriting 「I2 셀 오류」) + `tooManyWinners` = `pinCellErrors` 12종이다 — 04.3-04 `pinCellErrors`를 그대로 쓰고 문장 매핑을 따로 만들지 않는다(UI Considerations error I2 · I3 편집 표의 I3 절반, /review RB-P2d)"
    - "미제출 두 줄의 이름 · 전화번호를 서로 맞바꾸는 일괄 저장은 성공한다 — 저장 트랜잭션만 당첨자 UNIQUE(`cert_winners_event_id_name_phone_unique`, 04.3-02가 `DEFERRABLE INITIALLY IMMEDIATE`로 건다)를 `SET CONSTRAINTS … DEFERRED`로 커밋 때 검사한다. 최종 명단에 같은 이름 · 전화번호 두 줄이 남으면 여전히 전부 거부되고 결과는 기존 셀 오류 `duplicatePerson`이다(커밋 때 23505도 같은 결과로 바뀐다 — codex final3 A1 · C2)"
    - "I3 저장에서도 수령자 화면 모양 중복을 막는다 — 제출된 줄을 포함한 어느 두 줄의 공개 모양이 같아지면 전부 거부되고 편집 가능한 그 줄들의 `구별 표시` 칸에 `수령자 목록에 {모양}가 {N}줄입니다 · 구별 표시를 서로 다르게 적어 주세요(예: 오전 조)`가 선다. 구별 표시의 10자 · 숫자 3개 이상 이어짐 · 그 행사 당첨자 이름(제출된 줄 포함) 규칙도 같다(UI Considerations error I2 · I3 모양 중복 · 구별 표시에 개인정보의 I3 절반 · A11)"
    - "편집 가능한 I3에는 당첨자 섹션 라벨 아래 상시 힌트 `구별 표시는 수령자 목록에 그대로 보입니다 · 이름·전화번호는 쓰지 마세요`(`구별 표시` 열 머리글의 `aria-describedby`)와 저장 전 표 상태로 계산한 `수령자 화면` 열이 있다 — I2와 같은 부품이다. `구별 표시` 셀을 편집하는 동안 그 편집 `<input>`도 같은 상시 힌트 id를 `aria-describedby`로 가리킨다(스크린 리더가 입력 중에 경고를 듣는다)"
    - "편집 중 그 줄이 제출됐으면 저장이 전부 거부되고 합계 행 `제출된 줄 1 · 전부 거부 · 그 줄 고친 값은 버림` — 그 줄만 서버 값·읽기 전용으로 돌아가고 다른 줄의 고친 값은 남으며 「덮어쓰기 / 그 값으로」가 없다. 편집 중 링크가 닫혔으면(기한 · 모두 제출 · 다른 직원) 저장이 거부되고 표가 읽기 표로 다시 그려지며 합계 행 `링크 닫힘 · 고친 {N}칸 저장 안 됨`(`aria-live`)이 선다(UI Considerations error I3 편집 중 그 줄 제출 · 링크 닫힘)"
    - "제출이 아닌 다른 직원의 수정과 부딪히면 SYSTEM §7-3 (나) 그대로다 — 실제로 바뀐 셀만 고정 · `다른 사람이 {HH:mm}에 {값}으로 바꿈 · 덮어쓰기 / 그 값으로` · 합계 행 `충돌 N줄 · 전부 거부`. 저장 요청은 **내가 고친 칸만** 싣고(그 칸의 `baseline` = 화면이 처음 읽은 값을 함께), 「실제로 바뀐 셀」은 그 보낸 칸 가운데 `baseline`과 지금 서버 값이 다른 셀이다 — 내가 이름 · 전화번호를, 다른 직원이 전화번호를 고쳤으면 전화번호만 고정되고 이름은 오판하지 않는다(codex final C3). 견줌은 정규형(전화 숫자만 · 전달 `onsite`/`parcel` · 수량 정수 · 글자 칸 NFC + 앞뒤 공백 제거)으로 하고 충돌 셀 값은 화면형(`010-4821-7730` · `현장`/`택배`)으로 돌려준다 — 내가 전화번호만, 다른 직원이 경품명만 고쳤으면 저장이 통과하고, 보내지 않은 칸이 저장형이라 형식 오류로 오판되지 않는다(E3-17). 내가 고치지 않은 칸은 보내지 않으므로 서버가 지금 값(다른 직원의 값)을 그대로 두고 충돌로 묻지 않는다 — 내가 이름만, 다른 직원이 전화번호만 고쳤으면 저장이 통과하고 둘 다 남는다(덮을 내 값이 없는 칸이라 「덮어쓰기」가 뜻이 없다 · CLAUDE.md §7 시스템이 판단할 수 있는 것은 묻지 않는다, codex final2 C). 당첨자 값(`cert_winner.value`)을 볼 수 없는 사람은 저장 자체가 거부되고 편집 표를 받지 않아 충돌 셀로 값이 새지 않는다(codex final C2). 수령자의 전화번호 확인 시도·잠금은 줄 버전을 올리지 않아 충돌을 만들지 않는다"
    - "「링크 닫기」(접수 중 + 편집 가능할 때만 머리 2차)는 Phase 4 공용 확인 부품 `ui/confirm-dialog`(SYSTEM §7-17 — PC 모달 · 폰 시트)를 연다 — 제목 `링크 닫기` · 부제 `{행사 이름} · 제출 3/12명` · 결과 줄 `미제출 9명은 더 제출할 수 없습니다 · 다시 열 수 없습니다` · 2차 `취소 Esc`(부품이 1차 라벨에서 파생) · 1차 `링크 닫기`, 열리면 포커스 = 1차. × 는 부품 그대로다(§7-17 — 폰 시트에서만 보이고 접근 이름 `닫기`, E3-01 — 1차 `링크 닫기`와의 이름 겹침은 /plan-design-review D-5로 부품 그대로 두고, 테스트 셀렉터를 `exact: true` 정확 일치로 쓴다). 성공하면 모달이 닫히고 토스트 `링크 닫기 · 미제출 N명` · QR과 링크 줄이 사라지고 `닫힘 · {일시} · {이름}이 닫음`. 실패하면 모달 안 1차 왼쪽 `닫지 못했습니다 · 다시 시도`이고 모달이 남으며 1차를 다시 누를 수 있다(부품의 막지 않는 실패 줄 — Task 2 ④-0, 색 `.reason` · 알림 역할 `alert`는 /plan-design-review D-4 확정). 다시 열기는 없다(UI Considerations error I3 링크 닫기 실패)"
    - "합계 행은 `합계 · N명 · 제출 0명`부터 전부 제출까지 같은 모양이고, 모두 제출되면 링크가 저절로 닫혀 QR 자리에 `닫힘 · {일시} · 모두 제출`이 선다(UI Considerations zero-one-many I3 제출 수)"
    - "C1: 기능 게이트가 꺼져 있으면 `saveCertWinnersAction` · `closeCertEventAction` · `unlockCertWinnerAction`이 첫 줄 `assertCertFeatureEnabled()`(`lib/certs/feature-guard.ts`)에서 not-found로 끝나고 domain `saveWinners` · `closeEvent` · `unlockWinner`도 맨 앞에서 `isCertFeatureEnabled()`를 보고 `notFound`를 돌려준다 — 페이지 없이 액션에 직접 POST해도 당첨자 · 셈 · 잠금 · 닫힘 · 행동 로그가 바뀌지 않는다(codex #2)"
    - "제출된 줄의 3차 「제출 내용」(→ `/certs/submissions/{id}`)은 서버가 확인증 정정 화면을 열 수 있는 사람(`certs.submissions` 보기이고 대표 계급이 아님 — 04.3-07 `isCertPrivacyBarredRole`)에게만 보낸다 — PM · 대표에게는 제출 상태와 시각만 보인다. 폰에서는 행 탭 → 보기 전용 `RowSheet`의 `action` 자리에 같은 링크가 선다(개정 ⑫)"
    - "당첨자의 이름이나 전화번호를 고치면 그 자리의 확인 증표(`verify_proof_hash` · `verified_until`)와 확인 멱등 기록 `verify_idem_outcome`의 맞음(`ok`) 항목이 지워진다 — 고치기 전에 확인을 통과한 사람은 다시 확인해야 제출할 수 있다. 틀림(`wrong`) 항목은 남아 속도 제한 셈(04.3-03 `countRecentMisses`)이 편집 뒤에도 그대로다(B1). 짧은 잠김 셈 · 누적 셈 · 누적 잠김은 편집으로 바뀌지 않는다(짧은 잠김은 담당자가 풀 수 없고, 누적 잠김은 「잠금 풀기」로만 풀린다)"
    - "누적 잠김(소유자 결정 2026-09-24 21:56 「누적 잠금 넣기로 해」): **행사가 접수 중일 때만** 미제출이고 `hard_locked_at`이 있는 줄은 제출 열이 `잠김` + 2행 `잠금 풀기 필요`다 — 서버가 `hardLockedAt`을 접수 중인 행사에서만 싣고, 닫힌 행사의 그 줄은 `미제출`이다(풀 방법이 없는 잠김 표시를 남기지 않는다, UI-SPEC:491 · checker-C W1). 그리고 서버가 **접수 중 + 그 행사 `certs.events` 쓰기(경영관리 포함) + 범위 안 + 미제출 + 누적 잠김**인 줄에만 `unlockable`을 실어 행동 열 3차 「잠금 풀기」를 보인다. 누르면 「링크 닫기」와 같은 확인 부품(Phase 4 `ui/confirm-dialog` — 당첨자 표 밖에 하나, E3-20)이 `잠금 풀기` / `{이름}` / `틀린 횟수를 모두 지웁니다` / `취소 Esc` · 1차 `잠금 풀기`로 열린다(UI-SPEC Copywriting 「I3 잠금 풀기 확인」 정본 — 3차 손질본, /plan-design-review D-9 · X-5)"
    - "「잠금 풀기」 요청은 줄 id와 그 줄의 잠금 표시(`hard_locked_at`, 서버가 화면에 준 값 — `YYYY-MM-DDTHH:mm:ss.sssZ` 한 모양만 받는다, E3-23)를 싣는다 — 서버는 쓰기 권한 · `cert_winner.value` 보임(못 보면 `forbidden` — `unlockable`과 같은 조건, E3-14) · 범위를 다시 보고, 그 줄이 **아직 같은 표시로 누적 잠김일 때만** `failed_attempts` · `locked_until` · `cumulative_failed_attempts` · `hard_locked_at`을 되돌리고, 같은 트랜잭션에서 끌 수 없는 새 로그 `cert_unlock` 한 줄(행사 id · 잠금 표시만 — 이름 · 전화번호 없음)을 Phase 4 `recordAction(viewer, entry, {tx})`로 쓴다(C6 · E3-02). 이미 그 표시로 풀린 뒤 같은 요청이 다시 오면 같은 성공을 돌려주고 로그를 더 쓰지 않으며 풀린 뒤 새로 쌓인 틀림을 지우지 않는다. 풀어도 줄 `version` · `updated_by` · `updated_at`은 그대로라 같은 줄의 미저장 편집도 「일괄 저장」이 그대로 저장한다"
    - "「잠금 풀기」 성공 → 확인 부품이 닫히고 토스트 `잠금 풀기 · {이름}` · 그 줄을 응답이 실은 **서버의 지금 상태**(`row`)로 다시 그린다 — 보통은 제출 열 `미제출` · 버튼 사라짐이고, 과거 풀기의 재전송인데 그새 다시 누적 잠김이면 `잠김` + `잠금 풀기 필요` + 새 잠금 표시의 「잠금 풀기」가 그대로 선다(화면이 성공만 보고 잠김을 지우지 않는다 — Codex UI 6차 #1) · 포커스는 그 줄 행동 셀(로빙 셀 — 버튼이 사라져도 셀은 남는다) · 그 줄과 다른 줄의 미저장 편집값은 그대로. 실패 → 모달 안 `풀지 못했습니다 · 다시 시도`, 결과 불명(연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가) → 모달 안 `풀었는지 확인하지 못했습니다 · 다시 눌러 주세요`이고 1차가 같은 요청(같은 잠금 표시)을 다시 보낸다. 짧은 잠김 줄에는 버튼이 없다(UI-SPEC I3 「잠금 풀기」)"
    - "I3 편집 표(grid)의 행동 셀은 §7-3 (아) 그대로 로빙 셀 하나다(UI-SPEC:493 · 개정 ⑫ (아) 한 줄 · Codex r3 C-1): 표 전체가 탭 정지 1개이고 방향키로 행동 셀에 닿는다 · 셀 안 3차(「잠금 풀기」 · 「제출 내용」)는 `tabindex=-1`이라 `Tab`이 닿지 않는다 · 셀에서 `Enter`/`Space` = 그 3차 누름 · 그 3차가 연 확인을 취소 · `Esc` · × · 성공으로 닫으면 포커스는 그 행동 셀(로빙 위치 그대로)로 돌아온다. 공용 표(Phase 4 `ui/table/use-grid-keyboard.ts` · `types.ts` · `Table.tsx` — 충돌 셀 키보드 `handleConflictKey`가 먼저인 순서 그대로, E3-01)가 이 셀을 받고, `docs/design/DECISIONS.md` 항목 → SYSTEM §7-3 (아) 한 줄이 코드보다 먼저 서며, 공용 표 커밋은 화면 커밋보다 먼저다(CLAUDE.md §6 · E3-06). 확인 부품은 표 밖에 하나라 모달 안에서 누른 `Enter` · 방향키가 셀로 올라가지 않는다 — 모달 1차 `Enter` 한 번 = 요청 한 번(E3-20). 읽기 표(grid 아님)의 「제출 내용」은 보통 `Tab` 대상 링크다"
    - "링크 닫기의 `status_change` 로그는 닫힘 UPDATE와 같은 트랜잭션에서 Phase 4 `recordAction(…, {tx})`로 쓰인다(E3-02) — 로그 쓰기가 실패하면 닫힘도 되돌아가 다시 시도할 수 있다(C6 · codex C6)"
    - "미저장 편집은 브라우저 저장소에 두지 않는다 — 떠나기 경고만 있다(개정 ⑪)"
    - "「일괄 저장」 요청(`{eventId, rows}`)은 바뀐 줄만 싣는다 — 저장된 줄은 `id` · `version`과 고친 칸 · 그 칸들의 `baseline`만(여섯 칸 전부가 아니다), 새 줄은 여섯 칸만(`baseline` · `id` · `version` 없음). 요청 한도는 `lib/actions/payload-size.ts:7`의 262,144 B이고 `lib/actions/client.ts:27` 미들웨어가 domain 전에 모든 액션에 같은 값으로 건다(액션별 한도 통로 없음). **지원 범위는 받아들인 한계다(codex final3 C1) — 유효한 500줄 저장이 전부 한도 안인 것은 아니다.** 한도 안이 보장되는 것: 서버 검사를 통과할 수 있는 최대 길이의 새 줄 500개(244,559 B) · 저장된 500줄의 한 칸씩 고침(이름 40자 170,059 B · 경품명 한글 60자 235,059 B — 고친 값과 그 칸의 `baseline`이 같은 길이인 모양으로 잰 값). 경품명 고침의 크기는 새 값과 `baseline` 글자 수의 합으로 정해진다 — 새 값이나 원래 값이 한글 약 60자보다 긴 500줄은 넘을 수 있다(`version` 10자리 최악 모양에서 둘 다 69자 262,059 B까지 안 · 둘 다 70자 265,059 B · 둘 다 80자 295,059 B · 원래 80자를 60자로 고침 265,059 B는 밖). 넘는 저장은 화면이 서버와 같은 `checkPayloadSize`로 **보내기 전에** 거부하고 합계 행에 기존 문장 `요청이 너무 큽니다 · {N}KB > 256KB 한도 · 나눠서 저장해 주세요`를 세운다 — 고친 값은 표에 그대로 남고 사용자가 나눠서 저장한다. 화면이 요청을 쪼개 보내지 않고(한 요청은 전부 저장 또는 전부 거부, codex final2 C) 액션별 한도도 바꾸지 않는다"
  prohibitions:
    - statement: "저장된 당첨자 줄을 지우는 경로(행 삭제 · 물리 삭제)를 두어서는 안 된다(A7)"
      status: resolved
      verification: test
    - statement: "제출된 줄의 값을 담당자 편집으로 바꾸어서는 안 된다(요청에 섞여 와도 배치 전체 거부)"
      status: resolved
      verification: test
    - statement: "닫힌 링크를 다시 여는 경로를 두어서는 안 된다"
      status: resolved
      verification: test
    - statement: "「잠금 풀기」가 짧은 잠김만 걸린 줄 · 제출된 줄 · 닫힌 행사의 줄을 풀거나, 잠금 표시가 다른 요청으로 셈을 지우거나, 한 번 적용된 풀기를 두 번 기록해서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/certs/events.ts"
      provides: "saveWinners · closeEvent · unlockWinner · 상세 DTO의 submissionId(권한자에게만) · hardLockedAt · unlockable"
      exports: ["saveWinners", "closeEvent", "unlockWinner", "getEventDetail"]
    - path: "repositories/cert-winners.ts"
      provides: "잠금 풀기 둘(줄 FOR UPDATE · 조건부 되돌리기) + 적용된 풀기 찾기 findCertUnlockLogTx(action_log 읽기 — UPDATE · DELETE 없음, E3-02)"
      contains: "findCertUnlockLogTx"
    - path: "ui/confirm-dialog/ConfirmDialog.tsx"
      provides: "Phase 4 공용 확인 부품 — 선택 primary.failure(1차를 막지 않는 실패 줄, SYSTEM §7-17 한 줄 · DECISIONS)만 더한다"
      contains: "failure"
    - path: "app/(app)/certs/events/[id]/winners-section.tsx"
      provides: "I3 당첨자 표 — 편집/읽기 전환 · 일괄 저장 · 충돌 · 편집 중 제출/닫힘"
      min_lines: 120
    - path: "app/(app)/certs/events/[id]/close-link-dialog.tsx"
      provides: "머리 2차 「링크 닫기」 + ui/confirm-dialog ConfirmDialog에 링크 닫기 문구 · closeCertEventAction · 토스트 · 다시 누를 수 있는 실패 줄"
      contains: "ConfirmDialog"
    - path: "app/(app)/certs/events/[id]/unlock-outcome.ts"
      provides: "잠금 풀기 응답 분류(순수) — unlocked · stale · closed · failed · unknown(결과 불명)"
      exports: ["unlockOutcome"]
    - path: "test/unit/certs/unlock-outcome.test.ts"
      provides: "응답 분류 단위 테스트(결과 불명 다섯 갈래 · 확정 실패 · row 없는 성공은 결과 불명)"
      min_lines: 30
    - path: "app/(app)/certs/events/[id]/save-payload.ts"
      provides: "일괄 저장 요청 만들기(순수) — 바뀐 줄 · 고친 칸 · 그 칸의 baseline만 + checkPayloadSize로 보내기 전 한도 판정"
      exports: ["buildWinnerSavePayload", "saveTooLargeReason"]
    - path: "test/unit/certs/save-payload.test.ts"
      provides: "칸 단위 요청 모양 · 500줄 경계(새 줄 최대 · 한 칸씩 고친 줄 · 넘는 요청 차단 문장)"
      min_lines: 30
    - path: "ui/table/RowSheet.tsx"
      provides: "선택 prop `action`(본문 아래 3차 링크 한 자리, 개정 ⑫)"
      contains: "action"
    - path: "ui/table/use-grid-keyboard.ts"
      provides: "행동 셀 Enter/Space 판정(순수 `gridEnterAction`) + `isActionCell` · `onActivateCell` — 개정 ⑫ §7-3 (아) 한 줄 · Phase 4 `conflictFocusTransition` 옆(E3-01)"
      exports: ["useGridKeyboard", "gridEnterAction"]
    - path: "test/unit/ui/grid-action-cell.test.ts"
      provides: "행동 셀 Enter/Space 판정 · Table 배선 단위 테스트(RED 먼저)"
      min_lines: 30
    - path: "test/integration/cert-events-edit.test.ts"
      provides: "편집 · 모양 중복 · 제출된 줄 · 충돌 · 닫힘 · 닫기 · 증표 무효화 · 권한"
      min_lines: 80
  key_links:
    - from: "domain/certs/events.ts"
      to: "domain/certs/winner-rules.ts"
      via: "saveWinners가 저장된 줄 전부(제출된 줄 포함)를 context로 validateWinnerRows에 넘긴다"
      pattern: "validateWinnerRows"
    - from: "app/(app)/certs/events/[id]/winners-section.tsx"
      to: "app/(app)/certs/events/actions.ts"
      via: "일괄 저장 한 번 = saveCertWinnersAction 한 번"
      pattern: "saveCertWinnersAction"
    - from: "app/(app)/certs/events/[id]/save-payload.ts"
      to: "lib/actions/payload-size.ts"
      via: "보내기 전 한도 판정을 서버 미들웨어와 같은 checkPayloadSize · 같은 한도 · 같은 문장으로(새 상수 없음)"
      pattern: "checkPayloadSize"
    - from: "app/(app)/certs/events/[id]/close-link-dialog.tsx"
      to: "app/(app)/certs/events/actions.ts"
      via: "1차 「링크 닫기」 = closeCertEventAction"
      pattern: "closeCertEventAction"
    - from: "app/(app)/certs/events/[id]/winners-section.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "「링크 닫기」와 같은 Phase 4 확인 부품을 표 밖 최상위에 하나만 그리고 여는 줄은 state로 넘긴다(UI-SPEC Component Inventory · E3-01 · E3-20)"
      pattern: "ui/confirm-dialog/ConfirmDialog"
    - from: "app/(app)/certs/events/actions.ts"
      to: "lib/certs/feature-guard.ts"
      via: "C1 — 세 새 액션의 첫 줄이 assertCertFeatureEnabled()"
      pattern: "assertCertFeatureEnabled"
    - from: "domain/certs/events.ts"
      to: "domain/action-log/record.ts"
      via: "C6 · E3-02 — 닫힘 UPDATE와 status_change, 셈 되돌리기와 cert_unlock을 각각 한 트랜잭션에 recordAction(viewer, entry, {tx})로"
      pattern: "recordAction"
    - from: "app/(app)/certs/events/[id]/winners-section.tsx"
      to: "app/(app)/certs/events/actions.ts"
      via: "줄 행동 「잠금 풀기」 확인 1차 = unlockCertWinnerAction(줄 id + 잠금 표시)"
      pattern: "unlockCertWinnerAction"
    - from: "app/(app)/certs/events/[id]/winners-section.tsx"
      to: "domain/certs/roster-display.ts"
      via: "`수령자 화면` 열을 서버와 같은 순수 함수로 계산(winner-table-parts 경유)"
      pattern: "roster-display|winner-table-parts"
    - from: "app/(app)/certs/events/[id]/winners-section.tsx"
      to: "ui/table/Table.tsx"
      via: "행동 열을 `actionCell: true`로 선언 → 표가 그 셀의 Enter/Space를 셀 안 3차 누름으로 넘긴다(UI-SPEC:493)"
      pattern: "actionCell"
    - from: "app/(app)/certs/events/[id]/winners-section.tsx"
      to: "ui/table/use-editable-width.ts"
      via: "1024 미만이면 편집 가능해도 읽기 표(편집 · 새 줄 · 힌트 줄 없음 · 일괄 저장은 N ≥ 1일 때만, 행동 열 3차 유지) — Phase 4 견적 원장과 같은 폭 판정 하나(D-10 · DR-36)"
      pattern: "useEditableWidth"
---

<objective>
**As a** 기획 PM·경영관리(행사 담당자), **I want to** 행사 현장에서 QR을 다시 만들지 않고 미제출 당첨자의 오타를 고치거나 빠진 사람을 더하고, 접수를 마칠 때 링크를 닫고 싶다, **so that** 수령자는 같은 QR로 계속 제출하고, 제출된 확인증은 누구도 담당자 화면에서 바꾸지 못한다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — Goal의 행위자(담당자)와 기준 1(「담당자가 닫으면 링크가 닫힌다」) · D-1102에서 이야기를 끌어냈다.

04.3-04가 만든 I3 읽기 화면에 UI-SPEC 「화면 계약」 I3의 나머지 — 미제출 줄 편집(§7-3 편집 표) · 새 줄 · 일괄 저장 · 편집 중 제출/닫힘 · §7-3 (나) 충돌 · 「링크 닫기」 · 「제출 내용」 3차 · 폰 `RowSheet` `action`(개정 ⑫) — 를 더한다. 셀 검사는 04.3-04의 `validateWinnerRows`를 저장된 줄 전부(제출된 줄 포함)를 `context`로 넘겨 재사용한다(오케스트레이터 결정 — 구별 표시 · 모양 유일성 · A11).

**결정(재량 · 소유자 결정 반영):** ① 담당자 편집 로그는 `document_update`(entity `cert_event`, detail = 바뀐 줄 수 · 더한 줄 수 — 이름·전화 없음, 끌 수 있는 종류라 지금처럼 커밋 뒤 `recordAction`), 링크 닫기 로그는 이미 있는 끌 수 없는 종류 `status_change`(detail `{from: "open", to: "closed", reason: "manual"}`)를 닫힘 UPDATE와 **같은 트랜잭션**에서 Phase 4 `recordAction(viewer, entry, {tx})`로 쓴다(C6 · codex C6 · E3-02 — `status_change` · `cert_unlock`은 `ALWAYS_ON_ACTION_TYPES`라 tx 안에서 설정을 읽지 않는다. tx 로그 사본 도우미 · 로그 종류 유니온은 두지 않는다 — main에 `tx`가 없을 때의 대비책은 04.3-02의 도우미 하나다. 커밋 뒤에 쓰면 로그가 한 번 실패한 닫힘은 재시도가 `alreadyClosed`로 끝나 영영 기록되지 않는다). 「잠금 풀기」 로그는 끌 수 없는 새 종류 `cert_unlock`(라벨 `확인증 잠금 풀기`) — 공유 등록부 `record.ts`의 세 배열에 항목만 끝에 더한다(append-only). ② 줄 `version`은 담당자 편집만 올린다(04.3-02 스키마 주석) — 수령자의 확인 시도도, 「잠금 풀기」도 담당자 저장을 충돌시키지 않는다. ③ 이름·전화번호를 고치면 그 자리의 확인 증표 · 확인 멱등 기록의 맞음 항목을 지운다(고치기 전 확인자가 다른 사람 자리로 제출하지 못하게) — 틀림 항목은 속도 제한 셈이라 남긴다(B1). 짧은 잠김 · 누적 셈은 편집으로 바뀌지 않는다 — 짧은 잠김은 담당자가 풀 수 없고(3분 뒤 저절로), 누적 잠김(소유자 결정 2026-09-24 21:56 「누적 잠금 넣기로 해」 — 고정 상수 20, 04.3-03이 세고 잠근다)은 이 플랜의 「잠금 풀기」로만 푼다. ④ 머리의 「링크 닫기」 자리는 `PageHeader`를 고치지 않고 `app/(app)/projects/[id]/quote-table.tsx`의 `.header` · `.titleBlock` · `.headerActions` 선례로 감싼다. ⑤ 확인은 Phase 4 공용 부품 `ui/confirm-dialog`의 `ConfirmDialog`(SYSTEM §7-17 — 네이티브 `<dialog>` · PC 모달 / 폰 시트) 하나를 「링크 닫기」와 「잠금 풀기」가 문구만 바꿔 같이 쓴다(UI-SPEC Component Inventory — 두 확인은 같은 부품 · E3-01). 화면 폴더에 확인 부품을 따로 만들지 않는다. 당첨자 표의 「잠금 풀기」 확인은 `winners-section` 최상위, 표 밖에 하나만 그리고 여는 줄은 state로 넘긴다 — 행동 셀 `td` 안에 그리면 모달 안 `Enter` · 방향키가 셀 `onKeyDown`으로 올라가 3차를 다시 누르거나 로빙 포커스를 옮긴다(E3-20). 부품에 없는 한 자리 — 1차를 막지 않는 실패 줄(「닫지 못했습니다 · 다시 시도」 뒤 1차를 다시 누른다) — 는 DECISIONS → SYSTEM §7-17 한 줄 → 부품 순서로 더한다(Task 2 ④-0, CLAUDE.md §6 — 화면 하나만의 예외를 만들지 않는다). 부품의 × 는 §7-17 그대로(`닫기`, 폰 시트에서만 보임)이고 2차 라벨은 부품이 파생한다(`취소 Esc`). ⑥ 「잠금 풀기」의 재전송 판정은 새 칸 없이 로그로 한다 — 셈 되돌리기와 `cert_unlock` 로그가 한 트랜잭션이라 「같은 줄 + 같은 잠금 표시의 `cert_unlock` 로그가 있다」 = 「그 풀기가 적용됐다」이다. 그래서 재전송은 쓰기 없이 같은 성공이고, 풀린 뒤 새로 쌓인 틀림 · 새 누적 잠김을 건드리지 않는다. 잠금 표시 비교는 밀리초 단위(`date_trunc('milliseconds', hard_locked_at)`)로 한다 — 화면에 준 ISO 문자열이 밀리초까지라서다. 요청의 잠금 표시는 액션 zod가 `z.string().datetime({ offset: false, precision: 3 })`(`YYYY-MM-DDTHH:mm:ss.sssZ` 한 모양 — zod 4.6.5로 실측)로 좁혀, 로그 `detail->>'lockStamp'`와의 문자열 비교가 표기 차이(오프셋 · 자릿수)로 어긋나지 않는다(E3-23). ⑦ 칸 단위 저장의 두 표기(E3-17): 화면 · 요청 · 셀 검사는 화면형(`010-4821-7730` · `현장`/`택배` · 수량 글자), DB는 저장형(숫자만 · `onsite`/`parcel` · 정수)이다. 병합은 DB 줄을 화면형으로 바꾼 뒤(`domain/certs/format.ts` `formatPhone` · 전달 라벨) 보낸 칸을 덮어 `validateWinnerRows`에 넣고, 충돌 견줌은 두 쪽을 정규형(`normalizePhone` · 전달 enum · 정수 · NFC + 앞뒤 공백 제거)으로 바꿔 하며, 충돌 `value`는 화면형으로 돌려준다.

**범위:** 파일이 31개로 15를 넘는다(codex final2 C의 요청 크기 대응으로 순수 모듈 · 단위 테스트 2개가 늘었다) — 편집 · 닫기 · 잠금 풀기가 domain부터 화면 · E2E까지 한 수직 슬라이스라 받아들이고 나누지 않는다(checker-C W1). 3차 수정에서 I3 행동 셀 키보드 계약(UI-SPEC:493 — 공용 표 세 파일 · 단위 테스트 · DECISIONS · SYSTEM §7-3 (아))이 이 플랜 안으로 들어와 6개가 늘었다(Codex r3 C-1 — 이월하지 않고 구현한다). 행동 셀을 처음 쓰는 화면이 이 플랜이라 공용 표 변경과 화면을 한 슬라이스로 둔다. eng-review로 화면 폴더 확인 부품 · 로그 도우미 파일이 빠지고 Phase 4 `ui/confirm-dialog` 부품과 그 단위 테스트가 들어와 수는 같다(E3-01 · E3-02).

**실행 조건(eng-review E3-01 · E3-33):** Phase 4 PR #40(`origin/claude/gsd-progress-e1nzgu`)이 main에 머지된 뒤에만 시작한다. 웨이브 첫 단계로 `git fetch origin && git merge origin/main`을 한다(리베이스 · 강제 푸시 금지 — 이미 합쳐져 있으면 `Already up to date`로 끝난다). Phase 4 부품(`ui/confirm-dialog` · `ui/table/Table.tsx` + `use-grid-keyboard`(Ctrl 단축키 D-94 · 충돌 셀 키보드) · `ui/button`의 `aria-disabled` · `recordAction(…, {tx})`)을 기준으로 쓴다. 같은 웨이브의 04.3-11과도 통합 · E2E는 한 번에 하나다(세션당 플랜 하나).

Purpose: CERT-01 × D-1102 「담당자가 닫으면 링크 전체가 닫힌다」와 현장 복구 경로(오타 · 빠진 사람).
Output: `saveWinners` · `closeEvent` · `unlockWinner` · 편집 표 · 링크 닫기 · 잠금 풀기 확인 · `RowSheet` `action` · 통합 · E2E(데스크톱 · 폰).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-04-SUMMARY.md
@.planning/phases/04.3-qr-certificate-intake/04.3-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saveWinners · closeEvent · unlockWinner · 상세 submissionId · 누적 잠김 DTO — domain · 리포지토리 · 액션 · 로그 종류 · 통합 테스트</name>
  <files>domain/certs/events.ts, repositories/cert-events.ts, repositories/cert-winners.ts, domain/action-log/record.ts, test/unit/action-log/record.test.ts, app/(app)/certs/events/actions.ts, app/(app)/certs/events/actions.registry.ts, test/integration/cert-events-edit.test.ts</files>
  <precondition>Phase 4가 main에 있고 이 브랜치에 합쳐져 있다 — `git fetch origin && git merge origin/main` 뒤 `test -f ui/confirm-dialog/ConfirmDialog.tsx && grep -q "conflictFocusTransition" ui/table/use-grid-keyboard.ts && test -f ui/table/use-editable-width.ts && grep -q "useEditableWidth" ui/table/use-editable-width.ts && grep -q "firstIssueSignal" ui/table/Table.tsx`가 종료 코드 0(E3-01 · 폭 판정은 Phase 4 04-49 — D-10 · 첫 오류 이동은 Phase 4 04-47 — ②-c DR-5, 04.3-04 Task 3과 같은 확인 · 교차 r2 R2-3). 아니면 시작하지 않고 멈춘다(폭 판정을 새로 만들지 않는다 — 04.3-04와 같은 멈춤 규칙). `domain/action-log/record.ts`의 `tx?: DbOrTx` 유무는 멈춤 조건이 아니다 — 없으면 04.3-02 ⑨-b 대비책 `insertActionLogTx`(`repositories/cert-action-log.ts`) 하나로 간다 — ④ 대비책 문단(E3-02 단일 정책, Round 2)</precondition>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 I3 당첨자 표 · 「링크 닫기」 불릿 · Copywriting I3 행들(편집 중 제출 · 닫힘 · 토스트 · 실패)
    - `domain/certs/events.ts` · `domain/certs/winner-rules.ts` · `repositories/cert-events.ts` · `repositories/cert-winners.ts`(04.3-02 · 03 · 04 · 06 산출물 전문) — `createEvent` · `getEventDetail` · `validateWinnerRows(rows, context)` · 범위 판정 · 행사 행 `FOR UPDATE` 함수
    - `app/(app)/certs/events/create-form-rules.ts`(04.3-04 — 순수, React·DB·설정 import 없음)의 `pinCellErrors(cellErrors)`(4-b가 `tooManyWinners` 렌더에 그대로 쓴다 — 04.3-04 Round 5로 12번째 코드가 됐다, E3-35)
    - `db/schema/cert-winners.ts` · `db/schema/cert-events.ts` · `db/schema/cert-submissions.ts` — `version` · `updated_by` · `closed_at` · `closed_reason` · `closed_by` · `verify_proof_hash` · `verified_until` · `verify_idem_outcome` · `failed_attempts` · `locked_until` · `cumulative_failed_attempts` · `hard_locked_at` 칸(04.3-02 — 누적 두 칸은 소유자 결정으로 02가 더한다)
    - `domain/certs/intake.ts` `verifyLast4`와 `VERIFY_HARD_LOCK_THRESHOLD`(04.3-03 — 누적 잠김을 세고 거는 쪽. 이 플랜은 푸는 쪽만) · `test/integration/cert-verify.test.ts`(04.3-03 — 시각 주입으로 짧은 잠김 넷을 지나 20번 틀리게 하는 누적 잠김 공격 준비 방식)
    - `domain/quotes/lines.ts` 285~480행 — 배치 저장의 줄 버전 충돌 모양(`SaveRejectedError` · `CellConflict`)을 **읽기만** 한다(오케스트레이터 사실: 견적 줄 코드는 고치지 않는다)
    - `app/(app)/certs/events/actions.ts` · `actions.registry.ts`(04.3-04 전문) · `domain/action-log/record.ts`(Phase 4 — 줄 번호 대신 `grep -n`으로 이름을 찾는다: 세 배열 `CORE_ACTION_TYPES` · `ACTION_TYPE_LABELS` · `ALWAYS_ON_ACTION_TYPES`(04.3-07의 `cert_correct` 뒤에 더한다) · `RecordActionDeps`의 `tx?: DbOrTx` · `recordAction(viewer, entry, deps?)` — `ALWAYS_ON_ACTION_TYPES`의 종류는 설정 조회를 건너뛰고 `deps.appendActionLog`가 있으면 그것으로 쓴다, `status_change`는 이미 끌 수 없는 종류) · `test/unit/action-log/record.test.ts` · `repositories/cert-action-log.ts`(04.3-02 대비 도우미 `insertActionLogTx` — ④ 대비책일 때만, 읽기만)
    - `db/schema/action-log.ts`(`entity` · `entity_id` · `detail` jsonb — `findCertUnlockLogTx`가 읽는다) · `repositories/action-log.ts` 1~10행(action_log 조회 선례 — 읽기만)
    - `domain/certs/format.ts`(04.3-02 — `normalizePhone` · `formatPhone` · `normalizeName`, 결정 ⑦ 두 표기 · E3-17) · `repositories/cert-winners.ts` `lockEventRow(viewer, eventId, tx)`(04.3-03 — tx 마지막 · 기본값 없음, E3-24. 실행 시점 04.3-03 SUMMARY의 시그니처를 따른다)
    - `test/integration/cert-events.test.ts` · `test/integration/cert-submit.test.ts`(04.3-04 · 06 — 행사 준비 방식) · `test/e2e/helpers/cert.ts` `seedSubmittedCert(opts?)`(04.3-02 C4 — 제출된 줄 표본을 domain으로 만든다, `extraWinners`로 미제출 줄을 함께. `@playwright/test`를 import하지 않아 통합 테스트도 import한다, E3-32)
    - `lib/certs/feature-guard.ts`(04.3-02 — `assertCertFeatureEnabled()`: 거짓이면 `notFound()`를 던진다 — 페이지 · 라우트 · 액션의 첫 줄, C1) · `domain/certs/feature.ts`(`isCertFeatureEnabled(deps?)` — domain 함수가 보고 `notFound` 유니온을 돌려준다 · 테스트에서 게이트를 끄는 방법) · `domain/certs/review.ts`의 `isCertPrivacyBarredRole`(04.3-07)
  </read_first>
  <behavior>
    - 미제출 줄 하나의 전화번호 고침 → `{kind: "saved"}` · 그 줄 `version` +1 · `updated_by` = 보는 사람 · 행동 로그 `document_update` 1줄(detail에 이름·전화 없음)
    - 새 줄 하나 더함 → 저장 · `sort_order` = 기존 최대 + 1 · 그 뒤 `loadIntake`의 목록에 그 사람이 있다
    - (경계 · 설계 /cso E3-35) 파기되지 않은 당첨자가 이미 499명(제출된 줄 포함)인 행사에 새 줄 1명을 더함 → `{kind: "saved"}`(총원 500, 04.3-04 생성 시 500줄 상한과 같은 수) · 이미 500명인 행사에 새 줄 1명을 더함 → `{kind: "invalid", cellErrors}`(그 새 줄의 `name` 셀 `{code: "tooManyWinners", total: 501}`) · 아무것도 쓰이지 않는다 · 500명인 행사에 새 줄 없이 기존 줄만 편집 → `{kind: "saved"}`(총원 불변이라 거부되지 않는다)
    - (checker note #10 반영 — 렌더 확인, 컴포넌트 단언, R2-B1로 `tone` 삭제) 500+1 케이스의 `cellErrors`를 04.3-04 `pinCellErrors`에 그대로 넘기면 `{"{그 줄 rowKey}:name": "당첨자 501명 · 500명까지 줄이기"}`이 나온다(`pinCellErrors`의 반환 모양은 문장 맵뿐이라 `tone` 키가 없다 — 셀 색은 기존 I2 셀 오류 표시를 그대로 따른다) — 통합 테스트가 도메인 응답 뒤 이 순수 함수 호출까지 이어 붙여 단언한다(별도 E2E/브라우저 렌더 없이 싼 값으로 화면 문구까지 고정)
    - 요청에 제출된 줄이 섞임 → `{kind: "submittedRows", rows}`(그 줄들의 서버 값) · 같은 요청의 다른 줄 편집도 쓰이지 않는다
    - 다른 직원이 먼저 고친 줄(버전 불일치, 미제출) → `{kind: "conflict", conflicts: [{rowId, version, cells: [{column, value}], updatedByName, updatedAt}]}` · 아무것도 쓰이지 않는다
    - (baseline · codex final C3 · 칸 단위 요청 — codex final2 C) 내가 이름 · 전화번호를 고치고, 그사이 다른 직원이 같은 줄 전화번호를 고쳐 저장 → 내 저장(옛 `version` · 보낸 칸 `name` · `phone`과 그 두 칸의 `baseline`) → `conflict` · 그 줄 `cells`는 `phone` 하나(값 = 다른 직원이 넣은 서버 전화번호)이고 `name`은 없다 · 아무것도 쓰이지 않는다 → 화면이 「그 값으로」 한 것처럼 전화번호를 빼고 `name` · `baseline.name`만 다시 보냄(버전은 옛 값 그대로) → `saved` · DB 그 줄 = 내 이름 + 다른 직원의 전화번호 · `version` = 서버 버전 + 1
    - (보내지 않은 칸 — codex final2 C) 내가 이름만 고치고(보낸 칸 `name` 하나), 그사이 다른 직원이 같은 줄 전화번호를 고쳐 저장 → 내 저장(옛 `version`) → 충돌 없이 `saved` · DB 그 줄 = 내 이름 + 다른 직원의 전화번호(보내지 않은 칸은 서버 값 그대로) · `version` = 서버 버전 + 1 · 요청에 없는 줄은 값 · `version` 불변 / 보낸 칸에 `baseline` 값이 없는 저장된 줄의 버전 불일치 → 그 칸 충돌(안전한 쪽) / 병합 결과로 검사한다: 내가 경품명만 고쳐 보냈는데 다른 직원이 바꾼 이름과 합쳐져 다른 줄과 모양이 같아지면 `invalid`(`shapeDuplicate`)
    - (두 표기 · E3-17) 한 줄의 전화번호만 화면형으로 고쳐 보냄(`baseline.phone`도 화면형) · 그사이 다른 직원이 같은 줄 경품명만 고쳐 저장 → 내 저장(옛 `version`) → 충돌 없이 `saved` · DB 그 줄 = 내 전화번호(숫자만) + 다른 직원의 경품명 / 전달만 `현장` → `택배`로 고쳐 보냄 → `saved` · DB `parcel` · 보내지 않은 전화 · 수량 칸이 병합 검사에서 `phoneFormat` · 수량 오류로 오판되지 않는다 / 내 전화 × 남의 전화 충돌 → 충돌 셀 `value`가 화면형 `010-XXXX-XXXX`(숫자만 모양 아님)
    - 불러온 뒤 수령자가 그 줄에서 틀린 번호를 넣음(`failed_attempts` 증가) → 담당자 저장은 충돌 없이 통과
    - 새 줄이 제출된 줄과 모양이 같음 → `{kind: "invalid", cellErrors}`에 새 줄의 `label` `shapeDuplicate`만(제출된 줄 쪽 오류 없음)
    - 구별 표시에 제출된 줄 당첨자 이름 → `labelName`
    - (맞교환 · codex final3 A1 · C2) 같은 이름에 구별 표시가 다른(`오전 조` · `오후 조`) 미제출 두 줄의 전화번호를 서로 바꿔 한 요청으로 저장 → `{kind: "saved"}` · DB 두 줄의 전화번호가 바뀌어 있다 · 두 줄 `version` +1 · 두 줄 증표가 비었다. 이름이 다른 두 줄의 이름 · 전화 조합을 통째로 맞바꿔도 같은 `saved`(최종 명단에 중복이 없으면 중간 순서와 무관하게 통과)
    - (최종 중복 · codex final3) 두 줄을 같은 이름 · 전화번호로 만들어 저장 → `{kind: "invalid", cellErrors}`에 `duplicatePerson`(4 검사) · 아무것도 쓰이지 않는다. 같은 요청을 검사가 늘 통과하는 `deps.validate`로 보내면 커밋에서 23505(`cert_winners_event_id_name_phone_unique`)가 나고 `saveWinners`는 던지지 않고 같은 `{kind: "invalid", cellErrors}`(보낸 이름 · 전화 줄의 `name` 셀 `duplicatePerson`) · 두 줄 값 · `version` 그대로 · `document_update` 0줄 · 결과를 `JSON.stringify`한 문자열에 픽스처 전화번호가 없다 — 미룬 제약이 최종 중복을 여전히 막는다
    - 행사가 닫힘(담당자 닫음 · 기한 지남 · 모두 제출) → `{kind: "closed"}` · 아무것도 쓰이지 않는다
    - 이름·전화번호를 고친 줄은 `verify_proof_hash` · `verified_until`이 비고 `verify_idem_outcome`에서 `ok` 항목만 사라진다(`failed_attempts` · `locked_until` · `cumulative_failed_attempts` · `hard_locked_at`은 그대로), 고치기 전 증표로 `submitCertificate` → 확인 시간 지남 판정
    - (B1 — 틀림 셈 보존) 04.3-03 `verifyLast4`로 한 줄에 틀림 1번(키 A)과 맞음 1번(키 B)을 넣고 담당자가 그 줄 전화번호를 고쳐 저장 → 맵에 키 A의 `wrong` 항목만 남고 키 B는 없다 · 같은 행사 · IP로 `countRecentMisses` → `eventMisses` · `ipMisses`가 편집 전과 같은 1 / 경품명만 고친 줄은 맵 전체가 그대로
    - 경품명만 고친 줄은 증표가 남는다
    - `closeEvent` → `closed_at` · `closed_reason = manual` · `closed_by` 채움 · `{kind: "closed", unsubmitted}` · `status_change` 1줄 · 두 번째 호출 → `{kind: "alreadyClosed"}` · 그 뒤 `loadIntake` → 닫힘(manual)
    - (C6 · codex C6) `closeEvent`의 로그 INSERT가 던지도록 deps로 주입 → `closeEvent`가 던지고 행사의 `closed_at` · `closed_reason` · `closed_by`가 null 그대로 · `status_change` 0줄 → 정상 deps로 다시 → `{kind: "closed"}` · `status_change` 정확히 1줄
    - 권한: 행사를 만들지 않았고 `certs.submissions` 보기가 없는 PM → `notFound` · `certs.events` 쓰기 없음 → `forbidden`
    - (정보 항목 · codex final C2) `certs.events` 쓰기 · 범위는 통과하고 `cert_winner.value`만 끈 테스트 계급(`upsertVisibility`, 끝에서 되돌림) → 다른 직원이 먼저 고쳐 버전이 어긋난 줄을 보내도 `saveWinners`가 `forbidden` · 아무것도 쓰이지 않음 · 결과 전체를 `JSON.stringify`한 문자열에 픽스처 이름 · 전화번호가 없다(충돌 셀 값이 새지 않음) · 같은 viewer의 `getEventDetail`은 `editable` 거짓
    - (C1) 기능 게이트를 끈 상태 → `saveWinners` · `closeEvent` · `unlockWinner` 셋 다 not-found · 당첨자 줄(셈 · 잠금 칸 포함) · 행사 닫힘 칸 · 행동 로그 변화 없음
    - `getEventDetail`: `certs.submissions` 보기가 있으면 제출된 줄에 `submissionId`, 없으면 그 키 자체가 없다 · 대표 계급에 `certs.submissions` 보기를 켜 둔 경우에도 그 키가 없다(04.3-07 `isCertPrivacyBarredRole`)
    - `getEventDetail` 누적 잠김: 접수 중 행사의 미제출 · 누적 잠김 줄에 `hardLockedAt`(ISO, 밀리초) · 짧은 잠김만 걸린 줄에는 `lockedUntil`만 · 제출된 줄에는 둘 다 없음 · **같은 누적 잠김 줄이라도 행사가 닫히면(담당자 닫음 · 기한 지남 · 모두 제출 각각) `hardLockedAt` 키 자체가 없고 `unlockable` 거짓**(checker-C W1 — 화면이 `미제출`을 그린다) · `unlockable`은 접수 중 + `certs.events` 쓰기 + 범위 안 + 미제출 + 누적 잠김일 때만 참(쓰기 없는 보기 전용 사람 · 닫힌 행사 · 짧은 잠김 줄 → 거짓)
    - (누적 잠김 끝까지, 소유자 결정) 04.3-03 `verifyLast4`로 시각을 주입해 짧은 잠김 넷을 지나며 20번 틀림 → `hardLocked` → `unlockWinner(viewer, eventId, {rowId, lockStamp: 그 줄 hardLockedAt})` → `{kind: "unlocked"}` · 네 칸(`failed_attempts` 0 · `locked_until` null · `cumulative_failed_attempts` 0 · `hard_locked_at` null) · 맞는 4자리 `verifyLast4` → `ok`
    - 잠금 풀기 로그: `cert_unlock` 정확히 1줄 · entity `cert_winner` · entityId = 줄 id · detail = `{eventId, lockStamp}`뿐(이름 · 전화번호 · 뒤 4자리 없음 — 테스트가 detail 키를 정확히 비교)
    - (E3-23) `lockStampSchema`(`domain/certs/events.ts`): 접수 중 누적 잠김 줄 DTO의 `hardLockedAt`(DB에서 읽어 만든 실제 값) → 통과 · 같은 순간의 `+09:00` 오프셋 표기 · 밀리초 없는 표기 · 마이크로초 6자리 · 날짜만 → 거부
    - 잠금 풀기는 `version` · `updated_by` · `updated_at`을 바꾸지 않는다 — 풀기 전에 읽은 `version`으로 같은 줄 경품명을 `saveWinners` → `saved`(충돌 없음)
    - 재전송: 같은 `{rowId, lockStamp}`로 두 번째 호출 → 같은 `{kind: "unlocked"}` · `cert_unlock` 여전히 1줄 · 첫 풀기 뒤 틀림 1번을 넣고(셈 1) 같은 요청을 다시 → `unlocked` · 셈 1 그대로(새 틀림을 지우지 않음)
    - 동시: 같은 요청 둘을 `Promise.all` → 둘 다 `unlocked` · `cert_unlock` 1줄
    - 거부: 짧은 잠김만 걸린 줄(`hard_locked_at` null, 적용된 풀기 로그 없음) → `{kind: "stale", row}` · 셈 그대로 / 잠금 표시가 다른 요청(1ms 다름) → `stale` · 셈 그대로 · 로그 0 / 행사가 닫힘 → `{kind: "closed"}` · 셈 그대로 / 쓰기 없음 → `forbidden` / 쓰기 · 범위는 통과하고 `cert_winner.value`만 끈 테스트 계급(`upsertVisibility`, 끝에서 되돌림) → `forbidden` · 네 칸 그대로 · 로그 0(E3-14) / 범위 밖 PM · 다른 행사의 줄 id → `notFound`
    - (C6) 잠금 풀기 로그 INSERT가 던지도록 주입 → `unlockWinner`가 던지고 네 칸이 풀기 전 그대로(되돌림) · `cert_unlock` 0줄
    - (단위 `record.test.ts`) `cert_unlock`이 `CORE_ACTION_TYPES` · `ALWAYS_ON_ACTION_TYPES`에 있고 라벨이 `확인증 잠금 풀기`
  </behavior>
  <action>
① **RED:** 위 `behavior`를 `test/integration/cert-events-edit.test.ts`에, `cert_unlock` 등록 케이스를 `test/unit/action-log/record.test.ts`에 먼저 쓰고 실패를 확인한다(04.3-03 · 04 · 06 통합 테스트의 준비 함수를 import해 재사용 — 새 픽스처 파일을 만들지 않는다. 제출된 줄이 든 행사는 `seedSubmittedCert({ extraWinners })`(04.3-02 C4)로 만든다 — 제출된 줄 + 미제출 줄을 한 번에 받고 제출 입력 모양을 이 플랜이 따라가지 않는다(E3-32). 누적 잠김 준비는 `verifyLast4` 끝까지 케이스 하나만 실제 20번 틀림으로 하고, 나머지는 이 테스트 파일 안에서 `db/client`로 네 칸을 직접 채운다). **맞교환 케이스의 RED 이유는 23505여야 한다** — ④를 `deferWinnerPersonUniqueTx` 호출 없이 먼저 녹색까지 쓴 뒤 이 케이스만 `duplicate key value violates unique constraint "cert_winners_event_id_name_phone_unique"`로 빨간 것을 SUMMARY 「RED 기록」에 옮기고 나서 호출을 더한다(계획 시점 Postgres 16 임시 표로 실측: 즉시 모드는 첫 UPDATE에서 23505, `SET CONSTRAINTS … DEFERRED` 뒤 맞교환은 통과, 최종 중복은 COMMIT에서 23505).

② **`repositories/cert-winners.ts`(viewer 첫 인자, where에 행사 id를 함께):**
  - ②-a 한 행사의 파기되지 않은 당첨자 전부를 트랜잭션 안에서 읽는 함수 · 조건부 갱신(`id` + `event_id` + `version = 입력` + `submitted_at IS NULL`, 여섯 칸 가운데 **받은 칸만**(보내지 않은 칸은 set에 넣지 않는다 — codex final2 C) + `version + 1` + `updated_by` + `updated_at`, 이름·전화가 바뀌면 확인 증표 두 칸 `verify_proof_hash` · `verified_until`을 null로 하고, 확인 멱등 맵 `verify_idem_outcome`에서는 **맞음(`o: "ok"`) 항목만** 지우고 틀림(`o: "wrong"`) 항목은 남긴다 — set 절에서 `jsonb_each`로 `value->>'o' = 'wrong'`인 항목만 다시 모으고 남는 항목이 없으면 null(읽는 쪽은 null을 빈 맵으로 본다, 04.3-02 맵 정본). 틀림 항목은 04.3-03 `countRecentMisses`의 속도 제한 셈이라 담당자 편집으로 지워지면 편집을 핑계로 틀림 셈이 0이 된다(B1 — 04.3-03의 20개 상한도 틀림 항목은 나이 60분으로만 거른다). 셈 · 잠금 칸은 건드리지 않는다) · 삽입(`sort_order`) 셋을 더한다. 넷째로 `deferWinnerPersonUniqueTx(viewer, tx)` — 그 tx에서 `SET CONSTRAINTS cert_winners_event_id_name_phone_unique DEFERRED` 한 문장만(`tx: DbOrTx`의 `tx.execute(sql…)` — `DbOrTx`의 `execute`는 04.3-02 Task 2 ⑨가 넓혔다, 이 플랜은 `db/client.ts`를 고치지 않는다. 04.3-02 ②-b가 이 제약을 `DEFERRABLE INITIALLY IMMEDIATE`로 걸었다 — 이 호출이 없는 쓰기는 즉시 검사 그대로). 제약 이름은 이 파일의 내보낸 상수 `CERT_WINNER_PERSON_UNIQUE` 하나로 두고 domain의 23505 판정도 이 상수를 쓴다(codex final3 A1 · C2). `updated_by` 사용자 표시 이름을 함께 읽는 조회를 둔다(충돌 문장의 「누가」 — null이면 행사를 만든 사람 이름). 삭제 함수는 만들지 않는다.
  - ②-b 잠금 풀기 둘: 한 줄을 `FOR UPDATE`로 읽는 함수(`id` + `event_id` — `submitted_at` · `purged_at` · `hard_locked_at` 포함) · 조건부 되돌리기(`id` + `event_id` + `submitted_at IS NULL` + `date_trunc('milliseconds', hard_locked_at) = 입력 잠금 표시` → `failed_attempts = 0` · `locked_until = null` · `cumulative_failed_attempts = 0` · `hard_locked_at = null`, **`version` · `updated_by` · `updated_at`은 set에 넣지 않는다** — 소유자 결정 「일괄 저장의 줄 버전을 올리지 않는다」) → 바뀐 행 수를 돌려준다.

②-c **적용된 풀기 찾기 — `repositories/cert-winners.ts`(E3-02 — 로그 종류 유니온을 더하지 않는다):** 읽기 함수 `findCertUnlockLogTx(viewer, {winnerId, lockStamp}, tx)` — `action_log`에서 `action_type = 'cert_unlock'` + `entity = 'cert_winner'` + `entity_id = winnerId` + `detail->>'lockStamp' = lockStamp`인 행이 있는지만 돌려준다(SELECT만 — UPDATE · DELETE 없음, append-only 그대로. `lockStamp`는 ⑤ zod가 한 모양으로 좁힌 값이고 기록도 같은 값이라 문자열 비교가 표기 차이로 어긋나지 않는다, E3-23). 로그 쓰기는 domain이 Phase 4 `recordAction(viewer, entry, {tx})`로 한다(④) — 이 플랜은 tx 로그 사본 도우미 · 로그 종류 유니온 · 04.3-02의 대비책 파일을 만들거나 고치지 않는다.

③ **`repositories/cert-events.ts`:** 닫기 조건부 갱신(`closed_at IS NULL` 행만 · `closed_at` · `closed_reason` · `closed_by`), 상세 조회에 제출된 줄의 확인증 id를 붙이는 조인(`cert_submissions.winner_id`).

④ **`domain/certs/events.ts`:**
  - `saveWinners(viewer, eventId, input, deps?)` — deps = `{validate}`(기본 `validateWinnerRows` — 커밋 때 중복 갈래를 통합 테스트가 결정적으로 밟는 주입 자리, `closeEvent` · `unlockWinner`의 deps 선례). 입력 `rows`는 바뀐 줄만이다(codex final2 C — 요청 크기). 새 줄 = 여섯 칸 `{name, phone, prizeName, quantity, delivery, label}` 전부(문자열, `id` · `version` · `baseline` 없음). 저장된 줄 = `{id, version, baseline, ...고친 칸}` — 여섯 칸 가운데 **고친 칸만** 싣고, `baseline`은 그 고친 칸들의 처음 읽은 값만 같은 키로 싣는다(화면이 불러온 DTO 값, 저장 · 충돌 해결 뒤 갱신 — `domain/quotes/lines.ts` 441~466행 `baseline` 선례, codex final C3). 보내지 않은 칸은 「고치지 않음」이다 — 서버는 그 칸을 3에서 읽은 지금 값으로 채워 병합한 줄로 검사 · 갱신하고, 갱신 set에는 보낸 칸만 넣는다(남이 바꾼 값을 덮지 않는다). 순서:
    1. `isCertFeatureEnabled()` 거짓이면 `notFound`(C1) → `can(viewer, "certs.events", "write")`(거짓 → `forbidden`) → `visible(viewer, "cert_winner.value")`(거짓 → `forbidden` — 볼 수 없는 당첨자 값을 고치는 길을 두지 않고, `conflict` · `submittedRows` 응답의 서버 값이 투영 없이 새지 않게 한다. `domain/quotes/lines.ts` 368행 「금액을 볼 수 없는 사람은 줄을 저장하지 못한다」 선례, codex final C2) → 범위(04.3-04 `getEventDetail`과 같은 판정, 밖이면 `notFound`)
    2. `withTransaction` 시작 → 행사 행 `FOR UPDATE` → 닫힘(닫힌 시각 있음 또는 지금 ≥ 기한)이면 `{kind: "closed"}` → `deferWinnerPersonUniqueTx`(②-a) — 6은 줄마다 조건부 UPDATE라, 두 줄의 이름 · 전화를 맞바꾸면 첫 UPDATE가 아직 안 바뀐 상대 줄과 즉시 검사에서 부딪힌다. 이 tx에서만 UNIQUE를 커밋 때 검사로 미뤄 최종 명단만 보게 한다(codex final3 A1 · C2)
    3. 저장된 줄 전부 읽기 → 입력 줄 중 저장된 쪽이 제출됨이면 모아 `{kind: "submittedRows", rows}`(그 줄의 서버 값 DTO)
    4. 3에서 읽은 서버 줄을 먼저 **화면형**으로 바꾼다(전화 `formatPhone` · 전달 `onsite`/`parcel` → `현장`/`택배` · 수량 글자 · 이름 · 경품명 · 구별 표시는 그대로 — `validateWinnerRows`는 I2와 같은 화면형 입력을 받는다, 결정 ⑦ · E3-17). 저장된 입력 줄은 그 화면형 서버 줄 위에 보낸 칸만 덮어 병합한 줄로 만든 뒤(요청의 `id`가 이 행사의 저장된 줄이 아니면 `notFound`), `deps.validate(병합한 입력 줄 + 새 줄, {savedRows: 입력에 없는 저장된 줄 전부 + 제출된 줄})`(기본 `validateWinnerRows` — context 줄의 표기는 04.3-04 정의를 따른다)가 오류면 `{kind: "invalid", cellErrors}`. 저장형 값을 그대로 넣으면 보내지 않은 전화 · 전달 칸이 형식 오류로 오판된다. 6의 갱신 값은 검사가 돌려준 정규화된 줄에서 보낸 칸만 가져온다
    4-b. **누적 상한(설계 /cso E3-35 — 이번 페이즈에서 확정, 04.3-04 생성 시 상한과 같은 수):** 3에서 읽은 저장된 줄(파기되지 않은 전부, 제출된 줄 포함) 수 + 이번 요청의 새 줄 수가 500을 넘으면 새 줄 없이 저장이 성립하지 않으므로, **이번 요청의 새 줄 전부(초과분 k줄만이 아니라 이번에 더한 새 줄 모두 — UI-SPEC:261 「그 새 줄들을 오류 셀로 고정한다」와 맞춘다, R2-4)** 각각의 `name` 셀에 `{code: "tooManyWinners", total: 병합 뒤 총원}`을 붙여 `{kind: "invalid", cellErrors}`로 거부한다(`shapeDuplicate`가 `shape`·`count`를 나르듯 `total`을 나른다). **화면 렌더는 UI-SPEC I3 문구 그대로다(checker note #10 반영 완료, 04.3-04 Round 5 · Shard A의 UI-SPEC 갱신과 짝):** `winners-section.tsx`/`winner-table-parts.tsx`가 이 응답을 04.3-04 `pinCellErrors`(공용, I2 · I3 共用 — 04.3-04가 12번째 코드로 갱신, 반환값은 문장 맵뿐이고 `tone` 키는 없다 — R2-B1)로 그대로 넘기면, `tooManyWinners` 셀은 UI-SPEC Copywriting 「I3 셀 오류」의 `당첨자 {N}명 · 500명까지 줄이기`(N = `total` — 04.3-04 I2 「1차 막힘」의 501줄 거부와 같은 문장을 셀 오류로 옮긴 것)로 뜬다. 새 문구를 이 플랜이 짓지 않는다 — 04.3-04가 이미 정한 문장을 그대로 쓴다. 4의 `deps.validate` 통과 여부와 무관하게 독립으로 본다(형식·중복 오류가 없어도 총원 초과면 거부) — 아무것도 쓰지 않는다.
    5. 미제출 줄의 버전 불일치를 모아, **보낸 칸마다** 그 칸의 `baseline`과 3에서 읽은 지금 서버 값을 견주어 **다른 칸(= 내가 고친 칸을 내가 읽은 뒤 다른 직원도 바꿈)만** `cells`에 담아 `{kind: "conflict", conflicts}`(codex final C3 — 보낸 값과 서버 값을 견주면 내가 고친 칸까지 충돌로 오판한다). 견줌은 두 쪽을 **정규형**으로 바꿔 한다 — 전화 `normalizePhone` · 전달 `현장`/`택배` ↔ `onsite`/`parcel` · 수량 정수 · 이름 `normalizeName` · 경품명 · 구별 표시 NFC + 앞뒤 공백 제거(`baseline`은 화면형, 서버 값은 저장형이라 그대로 견주면 같은 값도 다르다고 본다 — 내 전화만 고친 줄이 남의 경품명 변경과 거짓 충돌이 된다, E3-17). 충돌 셀 `value`는 화면형(`formatPhone` · 전달 라벨 · 수량 글자)으로 싣는다(충돌 문장 `{값}`에 저장형이 보이지 않게). 내가 고쳤어도 서버 값이 `baseline`과 같은 칸은 충돌이 아니다. 보내지 않은 칸은 견주지 않는다 — 서버 값이 그대로 남으므로 덮이는 것이 없다(codex final2 C). 보낸 칸에 `baseline` 값이 없으면 그 칸을 충돌로 본다(견적 441행 폴백의 칸 단위판 — 안전한 쪽). 버전만 다르고 충돌 칸이 하나도 없는 줄은 충돌이 아니고, 6의 조건부 갱신은 그 줄의 서버 버전(3에서 읽은 값 — 행사 행 `FOR UPDATE` 아래라 그사이 바뀌지 않는다)을 조건으로 쓴다
    6. 갱신(set = 보낸 칸만 + `version + 1` · `updated_by` · `updated_at`, 이름 · 전화번호가 보낸 칸이고 서버 값과 정규형으로 다를 때만 ②-a의 증표 지우기) · 삽입 → 커밋
    7. 커밋 뒤 `recordAction(viewer, {actionType: "document_update", entity: "cert_event", entityId: eventId, detail: {changedRows, addedRows}})` → `{kind: "saved", detail: 새 상세 DTO}`
    거부 판정 다섯(2~5 · 4-b)은 아무것도 쓰지 않는다(전부 저장 또는 전부 거부).
    **커밋 때 중복(codex final3 A1 · C2):** 미룬 제약의 최종 중복은 콜백 안이 아니라 `withTransaction`의 COMMIT에서 던져진다(`node_modules/drizzle-orm/node-postgres/session.js` 185~191행 — commit 실패 → rollback → 다시 던짐). 그래서 2~6을 담은 `withTransaction(...)` 호출 전체를 감싸, 오류 자신 또는 `cause`(DrizzleQueryError가 pg 오류를 싣는 자리)의 `code === "23505"`이고 `constraint === CERT_WINNER_PERSON_UNIQUE`일 때만 기존 중복 결과 `{kind: "invalid", cellErrors}`로 돌려준다 — 셀은 요청 줄 가운데 이름 또는 전화번호를 실은 줄(새 줄은 전부)의 `name` 칸, 코드는 04.3-04 셀 오류 `duplicatePerson` 그대로(화면은 기존 「같은 사람 두 번」 문장). pg 오류의 `detail`(이름 · 전화번호가 든다)은 읽지 않는다(C2). 다른 오류는 그대로 던진다. 거부라 7의 로그는 쓰지 않는다. 정상 흐름에서는 4의 `duplicatePerson` 검사가 먼저 막으므로 이 갈래는 방어선이고, 통합 테스트는 `deps.validate` 주입으로 커밋까지 보낸다.
  - `closeEvent(viewer, eventId, deps?)` — deps = `{appendActionLog?}`(실패 주입용 — `recordAction`의 같은 이름 deps로 그대로 넘긴다, E3-02). 순서: `isCertFeatureEnabled()` 거짓이면 `notFound`(C1) → 같은 권한·범위 → `withTransaction`: 행사 행 `FOR UPDATE` → 이미 닫힘(닫힌 시각 또는 기한 지남)이면 `{kind: "alreadyClosed"}` → 조건부 갱신(`manual` · `closed_by = viewer.id`) → 파기되지 않은 미제출 자리 수 → **같은 tx로** `recordAction(viewer, {actionType: "status_change", entity: "cert_event", entityId: eventId, detail: {from: "open", to: "closed", reason: "manual"}}, {tx, appendActionLog: deps?.appendActionLog})`(C6 · codex C6 · E3-02 — 끌 수 없는 종류라 설정을 읽지 않는다. 던지면 닫힘까지 롤백되어 재시도가 `alreadyClosed`에 막히지 않는다) → 커밋 → `{kind: "closed", unsubmitted}`. 다시 여는 함수는 두지 않는다.
  - `unlockWinner(viewer, eventId, {rowId, lockStamp}, deps?)` — 소유자 결정 「누적 잠금」의 푸는 쪽. deps = `{appendActionLog?}`(`closeEvent`와 같다). 순서:
    1. `isCertFeatureEnabled()` 거짓이면 `notFound`(C1) → `can(viewer, "certs.events", "write")`(거짓 → `forbidden` — 경영관리도 이 권한으로 들어온다) → `visible(viewer, "cert_winner.value")`(거짓 → `forbidden` — `saveWinners` · `unlockable`과 같은 조건, E3-14) → `saveWinners`와 같은 범위 판정(밖이면 `notFound`)
    2. `withTransaction` → 04.3-03 `lockEventRow(viewer, eventId, tx)`(행사 행 `FOR UPDATE` — tx 마지막, E3-24) → 줄 `FOR UPDATE`(②-b, 그 행사의 줄이 아니면 `notFound`) — `verifyLast4` · 제출과 같은 「행사 → 자리」 잠금 순서라 교착이 없고, 풀기와 동시에 온 틀림은 둘 중 하나가 먼저 끝난 상태를 본다
    3. `findCertUnlockLogTx`(②-c)로 같은 `{rowId, lockStamp}`의 적용된 풀기가 있으면 **쓰기 없이** `{kind: "unlocked", row}` — 재전송 · 동시 요청의 둘째(줄 `FOR UPDATE`가 줄 세운다). 그새 새로 쌓인 틀림 · 새 누적 잠김은 건드리지 않는다(결정 ⑥)
    4. 행사가 닫힘(닫힌 시각 또는 기한 지남)이면 `{kind: "closed"}`
    5. 줄이 제출됨 · 파기됨이거나 `hard_locked_at`이 없거나 밀리초 단위로 `lockStamp`와 다르면 `{kind: "stale", row}`(그 줄의 지금 상태 DTO — 쓰기 없음)
    6. 조건부 되돌리기(②-b) → 같은 tx로 `recordAction(viewer, {actionType: "cert_unlock", entity: "cert_winner", entityId: rowId, detail: {eventId, lockStamp}}, {tx, appendActionLog: deps?.appendActionLog})`(C6 · E3-02 — ④-b로 끌 수 없는 종류라 설정을 읽지 않는다. 던지면 되돌리기까지 롤백) → 커밋 → `{kind: "unlocked", row}`
    `row` = 그 줄 하나의 `CertWinnerDto`(제출 열 · 잠금 · `unlockable`을 화면이 그 줄만 다시 그리는 데 쓴다). 로그 detail · 오류 메시지에 이름 · 전화번호 · 뒤 4자리를 넣지 않는다.
  - **대비책(E3-02 단일 정책 — 04.3-02 · 06 · 07과 같다, Round 2):** 실행 때 `grep -q 'tx?: DbOrTx' domain/action-log/record.ts`가 실패할 때만 — Phase 4 머지 뒤라 정상이면 쓰지 않는다. `closeEvent` · `unlockWinner`의 `recordAction` 세 번째 인자를 `{ appendActionLog: deps?.appendActionLog ?? ((v, e) => insertActionLogTx(v, e, tx)) }`로 바꾸고 04.3-02가 만든 `insertActionLogTx`를 `@/repositories/cert-action-log`에서 import한다 — 같은 tx · 같은 롤백 계약(C6)이고 테스트 주입 자리는 같은 `deps.appendActionLog` 하나다. 이 플랜은 그 파일도 `repositories/action-log.ts`도 고치지 않고 같은 일을 하는 함수를 따로 만들지 않는다. 그 파일이 없으면(04.3-02가 대비책을 쓰지 않았는데 지금 `tx`가 없다) 멈추고 보고한다. 어느 쪽을 썼는지 SUMMARY에 한 줄.
  - `getEventDetail`: `can(viewer, "certs.submissions", "view")`가 참이고 `isCertPrivacyBarredRole(viewer)`(04.3-07)가 거짓일 때만 제출된 줄 DTO에 `submissionId`를 싣는다(아니면 키 자체가 없다 — 대표는 I4를 열 수 없으므로 링크도 받지 않는다). 편집 가능 여부 `editable`(접수 중 + 쓰기 권한 + `cert_winner.value` 보임 — `saveWinners`가 거부할 표를 편집 표로 내지 않는다, codex final C2)을 DTO에 싣고 화면은 이 값만 본다. 미제출 줄에는 잠금 상태를 싣는다 — 누적 잠김이고 **행사가 접수 중이면** `hardLockedAt`(`Date#toISOString()` 그대로 — 밀리초 · `Z`, `lockStampSchema`를 통과하는 「잠금 풀기」 요청의 잠금 표시, E3-23). 행사가 닫혔으면(닫힌 시각 있음 또는 지금 ≥ 기한) `hardLockedAt`을 싣지 않는다 — 닫힌 뒤에는 풀 방법이 없으므로 화면이 `미제출`만 그리게 한다(UI-SPEC:491 「접수 중일 때만」 · checker-C W1 · Codex r3 C-3). 아니고 짧은 잠김 중이면 `lockedUntil`(04.3-04 그대로), 그리고 `unlockable` = `editable` + 미제출 + `hardLockedAt` 있음(UI-SPEC I3 「잠금 풀기」 — 서버가 정한다). 제출된 줄에는 잠금 값을 싣지 않는다(UI-SPEC 「`잠김`은 미제출 줄에만」).
  - `CertWinnerDto` · `CertEventDetailDto`에 새 필드(`submissionId` · `hardLockedAt` · `unlockable` · `editable`)가 생기면 기존 `DtoSpec`에 항목만 더한다(정보 항목은 04.3-04의 `cert_winner.value` · `cert_event.value` 그대로).

④-b **`domain/action-log/record.ts`(공유 등록부 — 항목만 끝에 더한다):** `CORE_ACTION_TYPES` 끝(04.3-07의 `cert_correct` 뒤)에 `cert_unlock`, `ACTION_TYPE_LABELS`에 `cert_unlock: "확인증 잠금 풀기"`, `ALWAYS_ON_ACTION_TYPES`에 `cert_unlock` — 세 배열을 같은 커밋에서(한국어 주석 한 줄: 누적 잠김 해제는 끌 수 없다 — 소유자 결정 2026-09-24). 기존 항목의 순서 · 값은 바꾸지 않는다.

⑤ **액션(`actions.ts`, `authedActionClient`):** `saveCertWinnersAction` · `closeCertEventAction` · `unlockCertWinnerAction` — 각각 첫 줄 `await assertCertFeatureEnabled()`(C1 — 페이지 없이 직접 POST해도 여기서 not-found) 뒤 domain 함수 하나만 부른다. 입력 zod(`{eventId, rows}`)는 줄 수 상한(500)과 칸 길이 상한(이름 50 · 경품명 80 · 구별 표시 20 — 서버 검사가 10자를 따로 본다)만 건다. 줄의 여섯 칸은 각각 선택이고 `baseline`은 같은 여섯 칸 · 같은 상한의 선택 객체다(빠뜨리면 zod가 벗겨 모든 충돌이 폴백이 된다, codex final C3). `superRefine` 둘(codex final2 C 칸 단위 요청): `id`가 없는 새 줄은 여섯 칸 전부가 있고 `version` · `baseline`이 없다 · `id`가 있는 저장된 줄은 `version`과 고친 칸 하나 이상이 있고 `baseline`의 키가 보낸 칸의 키와 같다 — 어기면 `validationErrors`. 요청 크기는 이 액션이 따로 보지 않는다 — `lib/actions/client.ts:27` 미들웨어가 모든 액션에 262,144 B(`lib/actions/payload-size.ts:7`)를 domain 전에 건다. `checkPayloadSize(payload, maxBytes)`는 한도 인자를 받지만 그 강제 자리가 인자 없이 부르고 `"use server"` 파일은 `authedActionClient` 하나로만 감싸야 하므로(`eslint/rules/require-action-client.mjs`) 액션별 한도를 줄 통로가 없다 — 공용 진입점을 이 플랜이 넓히지 않고, 화면이 같은 함수로 보내기 전에 막는다(Task 2 ⓪-c). 잠금 풀기 입력은 `{eventId: uuid, rowId: uuid, lockStamp: lockStampSchema}` — `lockStampSchema`는 `domain/certs/events.ts`가 내보내는 `z.string().datetime({ offset: false, precision: 3 })`다(`YYYY-MM-DDTHH:mm:ss.sssZ` 한 모양 — 오프셋 · 밀리초 없음 · 마이크로초는 `validationErrors`, E3-23. `"use server"` 파일은 async 함수만 내보내므로 스키마는 domain에 둔다). `actions.registry.ts`에 세 항목(메뉴 `certs.events` · `write` · `dtoName`은 04.3-04 선례대로 `CertEventDetailDto`, 잠금 풀기는 `CertWinnerDto`, 닫기는 DTO가 없으면 레지스트리 규약의 예외 주석 방식). `test/integration/leak-scan.test.ts`는 이미 이 모듈을 import한다(04.3-04) — 고치지 않는다.

⑥ **GREEN → 커밋** `feat(04.3): edit unsubmitted winners, close link and unlock hard-locked rows`, 본문 한국어(결정 ①~③ · ⑥ · ⑦ · 소유자 결정 인용).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/action-log/record.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — `cert_unlock`이 끌 수 없는 종류로 등록되지 않았다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-events-edit.test.ts test/integration/cert-events.test.ts test/integration/cert-submit.test.ts test/integration/cert-verify.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 제출된 줄 · 닫힘 · 충돌 케이스에서 한 줄이라도 쓰였으면 전부 저장 또는 전부 거부가 깨졌고, 재전송 케이스에서 `cert_unlock`이 2줄이면 풀기 멱등이 깨졌다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>! grep -rn "append[C]ertActionLogTx" domain/certs/events.ts repositories/cert-winners.ts &amp;&amp; test "$(grep -rlE 'export (async )?function [A-Za-z]*ActionLogTx' domain repositories | grep -v 'repositories/cert-action-log.ts' | wc -l)" = 0 &amp;&amp; { ! grep -q 'tx?: DbOrTx' domain/action-log/record.ts || ! grep -v '^\s*//' domain/certs/events.ts | grep -q 'insert[A]ctionLogTx'; } &amp;&amp; grep -q "precision: 3" domain/certs/events.ts &amp;&amp; grep -q 'visible(viewer, "cert_winner.value")' domain/certs/events.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 07의 옛 사본 도우미를 부르거나 04.3-02 대비 파일 밖에 tx 로그 도우미가 정의됐거나, `record.ts`에 `tx`가 있는데도 대비책을 import했거나(E3-02 단일 정책), 잠금 표시 zod가 한 모양으로 좁혀지지 않았거나(E3-23), 당첨자 값 보임 판정이 없다(E3-14)</fails_when>
  </verify>
  <acceptance_criteria>
    - `repositories/cert-winners.ts`에 행 삭제 호출이 없다(`! grep -nE "db\.delete\(|tx\.delete\(" repositories/cert-winners.ts`)
    - 통합 테스트에 `submittedRows` · `conflict` · `closed` · `alreadyClosed` · 증표 무효화 · 확인 시도 뒤 무충돌 · 권한 셋 케이스가 있다
    - `cert_winner.value`를 끈 viewer의 `saveWinners`가 `forbidden`이고 결과에 픽스처 이름 · 전화번호가 없는 케이스(codex final C2)와, 내가 이름 · 전화번호를, 다른 직원이 전화번호를 고친 줄의 충돌 셀이 `phone` 하나이고 해결 뒤 저장이 내 이름 + 서버 전화번호인 케이스(codex final C3)와, 내가 이름만 보낸 줄이 다른 직원의 전화번호 변경과 충돌 없이 병합되는 케이스(codex final2 C)가 통합 테스트에 있다 · `domain/certs/events.ts`의 `saveWinners`가 `visible(viewer, "cert_winner.value")`를 부르고 입력 줄 타입에 `baseline`이 있으며 여섯 칸이 저장된 줄에서 선택이다 · 당첨자 갱신 set이 보낸 칸만 담는다(SUMMARY에 그 리포지토리 함수의 set 절을 인용한다)
    - 이름 · 전화번호 편집 뒤 `verify_idem_outcome`에 `wrong` 항목이 남고 `ok` 항목이 없으며 편집 전 틀림이 `countRecentMisses`의 셈에 그대로 드는 케이스가 통합 테스트에 있다(B1)
    - 행동 로그 detail에 당첨자 이름·전화번호가 없음을 통합 테스트가 단언한다(`document_update` · `status_change` · `cert_unlock` 셋 다)
    - `domain/certs/events.ts`가 `validateWinnerRows`를 저장된 줄 context와 함께 부른다
    - 두 줄 맞교환 → `saved` 케이스와, 최종 중복이 검사 단계 · 커밋 단계(`deps.validate` 주입) 둘 다에서 `invalid`/`duplicatePerson` · 쓰기 0 · 로그 0인 케이스가 통합 테스트에 있다 · SUMMARY 「RED 기록」에 맞교환 케이스의 23505 실패 줄이 있다 · `repositories/cert-winners.ts`에 `SET CONSTRAINTS`가 정확히 한 곳이고 `saveWinners` 말고는 `deferWinnerPersonUniqueTx`를 부르지 않는다(`grep -rn "deferWinnerPersonUniqueTx" domain/ app/`이 정의 파일 밖에서 `domain/certs/events.ts` 한 곳)
    - 게이트 꺼짐에서 `saveWinners` · `closeEvent` · `unlockWinner`가 아무것도 쓰지 않는 케이스와, 대표 계급에 권한을 켜도 `submissionId`가 없는 케이스가 통합 테스트에 있다
    - 세 새 액션 몸통 첫 줄이 `await assertCertFeatureEnabled()`다(액션 파일에서 `saveCertWinnersAction` · `closeCertEventAction` · `unlockCertWinnerAction` 정의 바로 안쪽 — SUMMARY에 세 줄을 인용한다)
    - 이 플랜의 코드에 사라진 칸 이름이 없다(`! grep -rn "verify_idem_key_hash\|verifyIdemKeyHash" domain/certs/events.ts repositories/cert-winners.ts test/integration/cert-events-edit.test.ts`)
    - `closeEvent` · `unlockWinner`가 로그를 tx 안의 `recordAction(…, {tx, …})`로 쓴다(SUMMARY에 두 호출 줄을 인용한다 · E3-02) — 커밋 뒤에 따로 쓰지 않고, 로그 실패 주입(`deps.appendActionLog`) 시 닫힘 롤백 케이스(C6)가 통합 테스트에 있다
    - 누적 잠김 끝까지(`verifyLast4` 20번 → 풀기 → 맞음 `ok`) · 재전송(로그 1줄 · 새 틀림 보존) · 동시 둘 · 잠금 표시 불일치 · 짧은 잠김만 · 닫힘 · 버전 불변(풀기 전 `version`으로 저장 성공) · 로그 실패 롤백 케이스가 통합 테스트에 있다
    - 잠금 풀기 되돌리기의 set 절에 `version`이 없다(SUMMARY에 그 리포지토리 함수의 set 절을 인용한다) · `record.ts` 세 배열에 `cert_unlock`이 함께 있다(typecheck가 라벨 누락을 잡는다)
    - 닫힌 행사(닫힘 사유 셋 각각)의 누적 잠김 미제출 줄 DTO에 `hardLockedAt` 키가 없고 `unlockable`이 거짓인 케이스가 통합 테스트에 있다(checker-C W1 — RED에서 먼저 실패를 본다)
    - 두 표기 케이스(내 전화만 × 남의 경품명 → `saved` · 전달만 → `saved` · 충돌 `value` 화면형)가 통합 테스트에 있고 `domain/certs/events.ts`가 `normalizePhone` · `formatPhone`을 부른다(`grep -nE "normalizePhone|formatPhone" domain/certs/events.ts`, E3-17)
    - `cert_winner.value`를 끈 viewer의 `unlockWinner`가 `forbidden` · 쓰기 0 · 로그 0인 케이스가 통합 테스트에 있다(E3-14)
    - `lockStampSchema`가 `domain/certs/events.ts`에서 `datetime({ offset: false, precision: 3 })`로 내보내지고 `unlockCertWinnerAction` 입력이 그것을 쓰며(`grep -n "lockStampSchema" "app/(app)/certs/events/actions.ts"`), DTO `hardLockedAt` 통과 · 변형 넷 거부 케이스가 통합 테스트에 있다(E3-23)
    - 제출된 줄 표본이 `seedSubmittedCert`로 만들어진다(`grep -n "seedSubmittedCert" test/integration/cert-events-edit.test.ts`, E3-32) · `repositories/cert-action-log.ts`가 이 플랜의 diff에 없다(E3-02)
    - (설계 /cso E3-35) 누적 500명 상한 경계 케이스 둘(499+1 → `saved` · 500+1 → `invalid`/`{code: "tooManyWinners", total: 501}`)이 통합 테스트에 있고, `domain/certs/events.ts`의 `saveWinners`에 4-b 누적 상한 검사가 있다(SUMMARY에 그 조건문을 인용한다) — 500명인 행사의 기존 줄 편집만(새 줄 없음)은 거부되지 않는 케이스도 있다
    - (checker note #10 반영, R2-B1로 `tone` 단언 제거) 500+1 케이스의 `cellErrors`를 04.3-04 `pinCellErrors`로 렌더한 결과가 정확히 `당첨자 501명 · 500명까지 줄이기`임을 통합 테스트가 단언한다(반환값은 문장뿐 — `tone` 키 없음) — 새 문구를 짓지 않고 04.3-04가 UI-SPEC 그대로 정한 문장과 글자 단위로 같다
  </acceptance_criteria>
  <done>담당자 저장이 제출된 줄 · 닫힌 링크 · 다른 직원 수정 · 모양 중복에서 전부 거부되고, 링크 닫기가 닫힘과 같은 트랜잭션에서 한 번만 기록되며, 고친 이름·전화는 다시 확인을 요구하고, 누적 잠김 줄은 같은 잠금 표시의 「잠금 풀기」 한 번으로만 풀리며 재전송은 같은 성공 · 로그 1줄이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: I3 편집 표 · 편집 중 제출/닫힘 · 충돌 · 공용 확인 부품(링크 닫기 · 잠금 풀기) · 누적 잠김 줄 · RowSheet action · 행동 셀 키보드(개정 ⑫ (바)(아))</name>
  <files>app/(app)/certs/events/winner-table-parts.tsx, app/(app)/certs/events/event-create-form.tsx, app/(app)/certs/events/[id]/page.tsx, app/(app)/certs/events/[id]/winners-section.tsx, app/(app)/certs/events/[id]/winners-view.tsx, app/(app)/certs/events/[id]/close-link-dialog.tsx, app/(app)/certs/events/[id]/unlock-outcome.ts, test/unit/certs/unlock-outcome.test.ts, app/(app)/certs/events/[id]/save-payload.ts, test/unit/certs/save-payload.test.ts, app/(app)/certs/events/[id]/event-detail.module.css, ui/table/RowSheet.tsx, ui/table/RowSheet.module.css, ui/table/use-grid-keyboard.ts, ui/table/types.ts, ui/table/Table.tsx, test/unit/ui/grid-action-cell.test.ts, ui/confirm-dialog/ConfirmDialog.tsx, test/unit/ui/confirm-dialog.test.ts, docs/design/DECISIONS.md, docs/design/SYSTEM.md, test/e2e/cert-events-edit.spec.ts, test/e2e/mobile-cert-events.spec.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 I3 전부(그림 · 당첨자 표 불릿 · 「링크 닫기」 · 폰 · 다섯 상태) · Copywriting 핵심 요소 Destructive confirmation · Copywriting I3 행들
    - `docs/design/SYSTEM.md` §7-3 (가)~(사)(750행 근처 — (나) 충돌 문장 · (마) 개정 ⑪ · (바) 개정 ⑫) · §7-7 · §7-8 시트
    - `app/(app)/certs/events/event-create-form.tsx`(04.3-04 전문 — 편집 표 열 · 셀 오류 문장 선택 · `수령자 화면` 계산 열 · 상시 힌트 · 떠나기 경고 · 확인 모달)
    - `app/(app)/certs/events/[id]/page.tsx` · `winners-view.tsx` · `qr-section.tsx` · `event-detail.module.css`(04.3-04 전문)
    - `app/(app)/projects/[id]/quote-table.tsx` 880~960행(머리 `.header` 감싸기 · 일괄 저장 버튼 · 충돌 셀 「덮어쓰기 / 그 값으로」 · 합계 행 저장 결과) · `app/(app)/projects/[id]/project-detail.module.css` 1~30행 — **읽기만**
    - `ui/table/RowSheet.tsx` · `RowSheet.module.css`(전문) · `ui/toast/Toast.tsx` · `test/e2e/quote-table.spec.ts` 186~210행(시트 안 button이 「닫기」 하나뿐이라는 단언 — `action`은 링크라 이 단언이 그대로 녹색이어야 한다)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` I3 「잠금 풀기」 불릿 · 폰 불릿 · Copywriting 행 「I3 제출 셀」 · 「I3 누적 잠김 줄 행동」 · 「I3 잠금 풀기 확인」 · 「I3 잠금 풀기 토스트」 · 「I3 잠금 풀기 실패」 · 「I3 잠금 풀기 결과 불명」 — **실행 시점 문구를 그대로 옮긴다**(이 UI-SPEC은 Codex UI 6차 지적으로 손질 중이다. 아래 따옴표 문구와 다르면 UI-SPEC이 이긴다)
    - Phase 4 `ui/table/use-grid-keyboard.ts` 전문(13~30행 `GridKeyboardHandlers` · 31~38행 `UseGridKeyboardParams` · 52~80행 순수 `conflictFocusTransition`(04-28 충돌 셀 키보드) · 135행 Ctrl 조합(D-94 — `metaKey`를 보지 않는다) · 171~176행 `Enter`/`Space`가 편집 셀만 받는 자리) · `ui/table/types.ts` 15~41행(`TableColumn`) · `ui/table/Table.tsx` 116행(훅 배선) · 167~190행(로빙 DOM 포커스 `td[data-grid-focus]`) · 194~215행(`handleConflictKey` — 충돌 3차 `data-issue-action`) · 336~395행(셀 `tabIndex` 로빙 · `onClick` · `onFocus` · `onKeyDown` — `handleConflictKey`가 먼저, 그다음 `keyboardState.handleKeyDown`) · `test/unit/ui/conflict-focus.test.ts`(Phase 4 — 순수 함수 + 소스 단언 선례, E3-01). 줄 번호는 Phase 4 head `0db3ea2` 기준이고, 머지 뒤 다르면 이름으로 찾는다
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 493행 끝의 키보드 문장 · 591행 개정 ⑫의 「(아)에 한 줄」 원문 · 804행 결정 2 — **행동 셀 키보드 계약의 정본**
    - `docs/design/SYSTEM.md` §7-3 (아)(779~781행) · `docs/design/DECISIONS.md` 끝 두 항목(형식: `## 날짜 — 제목` · **결정** · **왜** · **버린 대안** · **범위**) · `docs/DESIGN.md` §4
    - `test/unit/ui/toast-timer.test.ts` 머리 주석(이 저장소에 React 렌더 테스트 러너가 없다 — 새 의존성 없이 순수 함수 + 소스 단언으로 고정하는 선례)
    - Phase 4 `ui/confirm-dialog/ConfirmDialog.tsx` · `ConfirmDialog.module.css` 전문(`primary` = `label` · `shortcut`(기본 `Ctrl+Enter`) · `onConfirm` · `pending` · `disabledReason`(1차를 `aria-disabled`로 막는다) · `reasonTone` · `nextStep` · 1차 왼쪽 막힘 자리 `.reason`(`grep -n 'styles.reason' ui/confirm-dialog/ConfirmDialog.tsx`로 찾은 위치 — 줄 번호를 전제하지 않는다) · × `aria-label="닫기"`(폰 시트에서만 보임) · 열 때 `document.activeElement`를 붙잡아 닫힐 때 되돌리고 없어졌으면 `h1` · 진행 중 `Esc` · 가림막 무시) · `test/unit/ui/confirm-dialog.test.ts`(`react-dom/server` `renderToStaticMarkup` 슬롯 단언 선례) · `docs/design/SYSTEM.md` §7-17(마지막 불릿 「서버 거부 문자열은 막힘 자리(1차 왼쪽, `block`)에 그대로 붙고 다이얼로그는 닫히지 않는다」) · `ui/table/RowSheet.tsx` 43~44행(`onClose` 뒤 포커스를 연 요소로 되돌린다)
    - `app/c/[token]/intake-flow.tsx`와 `app/c/[token]/flow-rules.ts`(04.3-03 — 20초 응답 없음을 결과 불명으로 접는 방식 · `isDefiniteResult` 모양. 이 플랜은 import하지 않고 같은 방식만 따른다)
    - `lib/actions/payload-size.ts`(전문 — `MAX_ACTION_PAYLOAD_BYTES` 262,144 · `checkPayloadSize`의 `reason` 문장 · server-only import가 없어 화면에서도 부를 수 있다) · `lib/actions/client.ts` 15~31행(미들웨어가 모든 액션에 기본 한도로 부르는 자리)
    - Phase 4 `ui/table/use-editable-width.ts`(04-49가 만든다 — 04-49 frontmatter `files_modified` · artifacts · Task 2 ①, 04-30은 엔지 r2 E2-02 분할로 넘겼다 · `useEditableWidth` — **읽기만**) · `app/(app)/projects/[id]/quote-table.tsx`에서 그 훅을 부르는 자리(`grep -n "useEditableWidth"`) · `docs/design/SYSTEM.md` §6-1 열 많은 표 ⑷(1024 미만 편집 표 = 보기 전용 · 힌트 줄 없음, DR-36 · DR-24) — ②-g0(/plan-design-review D-10)
    - Task 3 ①②(아래 — 이 태스크 맨 앞에서 두 스펙을 먼저 쓴다)
  </read_first>
  <behavior>
    - (단위 `unlock-outcome.test.ts`) `unlockOutcome(result)`: `{data: {kind: "unlocked", row}}` → `unlocked`(row 그대로) · `{data: {kind: "stale", row}}` → `stale`(row 그대로) · `{data: {kind: "closed"}}` → `closed` · `{data: {kind: "forbidden"}}` · `{data: {kind: "notFound"}}` · `validationErrors`가 있는 결과 → `failed`(확정 실패)
    - 결과 불명 → `unknown`: `undefined`(연결 끊김 · 20초 · 5xx로 호출부가 결과를 못 받음) · `serverError`가 있는 결과 · `data` 없음 · 모르는 `kind` · `row`가 빠진 `unlocked`/`stale`(해석 불가)
    - (단위 `grid-action-cell.test.ts` — 행동 셀, UI-SPEC:493) `gridEnterAction(pos, {isEditableCell, isActionCell})`: 편집 셀 → `"edit"` · 행동 셀(편집 아님) → `"activate"` · 둘 다 아님 → `"none"` · `isActionCell`을 넘기지 않은 기존 호출 → 편집 셀 `"edit"`, 나머지 `"none"`(기존 표 동작 그대로)
    - (같은 파일 — 소스 단언, 렌더 러너 없음) `use-grid-keyboard.ts`의 `Enter`/`" "` 분기가 `gridEnterAction`을 부르고 `"activate"`에서 `onActivateCell`을 부른다 · `Table.tsx`가 `isActionCell`을 `column.actionCell`로 넘기고 `onActivateCell`에서 셀 안 `button:not([data-issue-action]), a[href]` 하나를 누른다(충돌 3차는 건너뜀) · `types.ts` `TableColumn`에 선택 필드 `actionCell?: boolean`
    - (단위 `test/unit/ui/confirm-dialog.test.ts` — Phase 4 파일에 케이스 둘, ④-0) `primary.failure`만 → 그 글자가 `role="alert"` 요소로 1차 왼쪽에 있고 1차 버튼에 `aria-disabled`가 없다 · `disabledReason`과 `failure`가 함께 → `disabledReason`만 보이고 1차가 `aria-disabled="true"`
    - (단위 `save-payload.test.ts` — 요청 모양, codex final2 C) `buildWinnerSavePayload(eventId, rows)`: 안 고친 저장된 줄은 `rows`에 없다 · 이름만 고친 저장된 줄 → `{id, version, name, baseline: {name}}`(다른 다섯 칸 키 없음) · 「그 값으로」로 값 = `baseline`이 된 칸은 고친 칸이 아니다 · 새 줄 → 여섯 칸만(`id` · `version` · `baseline` 키 없음) · 결과 키는 `eventId` · `rows` 둘뿐
    - (같은 파일 — 500줄 경계, 한글 한 글자 3 B · `version` 2147483647 · uuid `id` · 고친 칸의 `baseline`은 새 값과 같은 길이의 다른 한글로 계산) 새 줄 500개(이름 40자(04.3-02 zod 이름 1~40자 · 04.3-04 `nameTooLong` 40 — Round 2에서 50자 가정을 고침) · `010-1234-5678` · 경품명 80자 · 수량 `999` · `택배` · 구별 표시 10자 — 서버 검사를 통과할 수 있는 최대, 244,559 B) → `saveTooLargeReason(payload)`가 null · 이름 40자를 고친 저장된 줄 500개(170,059 B) → null · 경품명 60자를 고친 저장된 줄 500개(235,059 B) → null · 69자(262,059 B) → null · 70자(265,059 B) → `reason` · 80자(295,059 B) → `checkPayloadSize`의 `reason` 문장 그대로(`요청이 너무 큽니다 · 289KB > 256KB 한도 · 나눠서 저장해 주세요`) — 이 넷이 받아들인 한계(한 칸씩 고친 500줄은 경품명 새 값 · 원래 값 모두 한글 약 60자까지 보장, 둘 다 69자가 최악 모양의 마지막 안쪽)를 고정한다(codex final3 C1)
  </behavior>
  <action>
**E2E 먼저(RED — codex final2 C · CLAUDE.md §5):** 아래 ⓪~⑦(화면 · 공용 표 · 확인 부품 구현)을 쓰기 **전에** Task 3 ⓪~②의 두 스펙 `test/e2e/cert-events-edit.spec.ts` · `test/e2e/mobile-cert-events.spec.ts`(편집 저장 · 모양 중복 · 요청 너무 큼 · 편집 중 제출/닫힘 · 구별 표시 접근성 · 링크 닫기 확인 모달 · 닫기 실패 · 모두 제출 · 권한 · 충돌 · 잠금 풀기 · 결과 불명 재전송 · 행동 셀 키보드 · 닫힌 행사 누적 잠김 · 보기 전용 · 직접 POST · 폰 칸 접기 · 시트)를 전부 쓰고 Task 3 `<verify>`의 명령으로 돌려 실패를 확인한다. 실패 이유는 구현 없음(04.3-04의 읽기 표뿐이라 편집 셀 · 「일괄 저장」 · 「링크 닫기」 · 「잠금 풀기」 · 확인 부품을 찾지 못함)이어야 한다 — 준비 오류(로그인 · 행사 만들기 · `db/client` 채우기 · Task 1 domain 호출)면 스펙을 고친다. 두 스펙은 `mode: "serial"`이라 첫 실패 뒤 케이스는 건너뛰므로 각 파일의 첫 실패 이유를 SUMMARY에 한 줄씩 적고 `test(04.3): add failing event detail edit E2E`로 커밋한다. 그다음 ⓪부터 채운다(04.3-07 Task 2 ⓪ 선례).

⓪ **RED:** `test/unit/certs/unlock-outcome.test.ts`를 위 `behavior`대로 먼저 쓰고 `pnpm vitest run --project unit test/unit/certs/unlock-outcome.test.ts`가 실패(모듈 없음)하는 것을 본다. 이어 `app/(app)/certs/events/[id]/unlock-outcome.ts`(순수, 브라우저 API 없음 — 입력 타입은 `unlockCertWinnerAction`의 결과 타입 | `undefined`, `any` 없이 좁힘)로 GREEN.

⓪-b **행동 셀 키보드 계약(UI-SPEC:493 · 개정 ⑫ (아) · Codex r3 C-1) — 정본 → RED → 공용 표 순서(CLAUDE.md §6 · `docs/DESIGN.md` §4). Phase 4 `Table` · `use-grid-keyboard` 기준이고, 이 ⓪-b의 커밋은 ⓪-c · ①~⑦의 어떤 화면 커밋보다 먼저다(E3-06 · E3-01 — 공용 표 변경은 화면 없이 단위 테스트만으로 녹색이 되므로 먼저 따로 커밋해 화면 커밋에 섞이지 않게 한다):**
  - ⓪-b-1 `docs/design/DECISIONS.md` 끝에 `## 2026-09-24 — §7-3 (아) 행동 셀: 3차 하나만 든 셀도 로빙 셀 하나` 항목을 기존 형식으로 더한다 — **결정**: 3차 하나만 든 행동 셀은 로빙 셀 하나 · 안의 3차 `tabindex="-1"` · 셀에서 `Enter`/`Space` = 그 3차 누름 · 그 3차가 연 모달이 닫히면 포커스는 그 셀. **왜**: 표 전체가 탭 정지 1개라는 (아) 계약을 지키면서 셀 안 버튼을 누를 길이 필요하다(확인증 I3 「잠금 풀기」 · 「제출 내용」이 첫 적용). **버린 대안**: 셀 안 버튼을 보통 `Tab` 정지로(탭 정지가 줄 수만큼 늘어 (아)가 깨진다) · 버튼만 `tabindex="-1"`(셀에서 누를 길이 없다). **범위**: SYSTEM.md §7-3 (아), `ui/table/use-grid-keyboard.ts` · `types.ts` · `Table.tsx`.
  - ⓪-b-2 `docs/design/SYSTEM.md` §7-3 (아) 문단(781행) 바로 뒤에 UI-SPEC 개정 ⑫ 「(아)에 한 줄」 원문 그대로 한 줄: 「3차 하나만 든 행동 셀은 로빙 셀 하나다 — 안의 3차는 `tabindex="-1"`, 셀에서 `Enter`/`Space` = 그 3차 누름, 그 3차가 연 모달이 닫히면 포커스는 그 셀로」. 주변 문장은 고치지 않는다. (04.3-01의 ⑫는 (바)만 하고 (아) 한 줄은 공용 표 코드와 같은 슬라이스라 이 플랜이 한다 — 04.3-01 Task 3 ⑫에 적혀 있다.)
  - ⓪-b-3 **RED:** `test/unit/ui/grid-action-cell.test.ts`를 위 `behavior` 마지막 두 불릿대로 먼저 쓰고 `pnpm vitest run --project unit test/unit/ui/grid-action-cell.test.ts`가 실패(`gridEnterAction` export 없음)하는 것을 본다. 이 저장소에는 키 이벤트를 흘리는 렌더 러너가 없고(Phase 4 `confirm-dialog.test.ts`의 `renderToStaticMarkup`은 정적 HTML뿐) 새 의존성은 승인 사항이라(`test/unit/ui/toast-timer.test.ts` 머리 주석 · `conflict-focus.test.ts` 선례) 판정은 순수 함수로, 배선은 소스 단언으로 고정하고 실제 키 동작은 Task 3 E2E가 증명한다.
  - ⓪-b-4 **GREEN — `ui/table/use-grid-keyboard.ts`:** 순수 export `gridEnterAction(pos, {isEditableCell, isActionCell?}): "edit" | "activate" | "none"`(편집 셀이 앞선다) — 같은 파일의 Phase 4 순수 함수 `conflictFocusTransition` 옆에 둔다. `UseGridKeyboardParams`(31~38행)에 선택 `isActionCell?: (pos) => boolean`, `GridKeyboardHandlers`(13~30행)에 선택 `onActivateCell?: (pos, cell: HTMLElement) => void`(한국어 주석 한 줄 — §7-3 (아) 행동 셀). 171~176행 `Enter`/`" "` 분기는 `gridEnterAction`의 결과로 `"edit"`이면 지금처럼 `preventDefault` 뒤 `onEnterEdit`, `"activate"`면 `preventDefault` 뒤 `onActivateCell(pos, event.currentTarget)`, `"none"`이면 아무것도 하지 않는다. 135행 Ctrl 조합(D-94 — `metaKey` 없음) · 다른 키 · 편집 중 분기는 그대로.
  - ⓪-b-5 **`ui/table/types.ts`:** `TableColumn`에 선택 필드 `actionCell?: boolean`(주석: 3차 하나만 든 행동 셀 — 셀 렌더가 안의 3차에 `tabIndex={-1}`을 준다 · 셀에서 Enter/Space = 그 3차 누름, §7-3 (아) 개정 ⑫). **`ui/table/Table.tsx`:** 훅(116행)에 `isActionCell: (pos) => columns[pos.col]?.actionCell === true`, 핸들러 `onActivateCell: (_pos, cell) => cell.querySelector<HTMLElement>("button:not([data-issue-action]), a[href]")?.click()`를 더한다(셀이 비어 있으면 — 버튼이 사라진 줄 — 아무 일도 없다 · Phase 4 충돌 3차 `data-issue-action`은 건너뛴다). 셀 `onKeyDown`의 순서 — `handleConflictKey`가 먼저, 그다음 `keyboardState.handleKeyDown` — 는 그대로 둔다(E3-01). `enableGridKeyboard`를 켜지 않은 표 · `actionCell`이 없는 열은 동작이 그대로다(견적 원장 · 행사 목록 회귀 없음).
  - GREEN(`grid-action-cell.test.ts` + Phase 4 `conflict-focus.test.ts` — 같은 `Enter` 분기를 건드리므로 함께 본다)을 본 뒤 커밋 `feat(ui): action cell activation in grid keyboard`(본문 한국어 — DECISIONS 항목 · SYSTEM (아) 한 줄과 같은 커밋이어도 되고, 문서가 먼저인 두 커밋이어도 된다). **이 커밋은 ⓪-c · ①~⑦보다 먼저다**(E3-06 — 수용 기준이 `git log --reverse --format=%s`의 순서로 본다).

⓪-c **일괄 저장 요청 만들기 — 칸 단위 + 보내기 전 한도(codex final2 C):**
  - **RED:** `test/unit/certs/save-payload.test.ts`를 위 `behavior`의 요청 모양 · 500줄 경계 두 불릿대로 먼저 쓰고 `pnpm vitest run --project unit test/unit/certs/save-payload.test.ts`가 실패(모듈 없음)하는 것을 본다. 경계 표본은 테스트 안에서 만든다(고친 값은 한글 `경`을 칸 길이만큼 · 그 칸의 `baseline`은 같은 길이의 한글 `나`(값 ≠ `baseline`이라 고친 칸으로 실린다) · 줄마다 다른 uuid · `version` 2147483647) — 숫자(244,559 · 170,059 · 235,059 · 262,059 · 265,059 · 295,059 B)는 이 모양으로 잰 값이다(계획 시점 `node`로 재측정 — 같은 값).
  - **GREEN — `app/(app)/certs/events/[id]/save-payload.ts`(순수, 브라우저 API 없음 · `any` 없음):** `buildWinnerSavePayload(eventId, rows)` — 화면 줄 상태(저장된 줄 = `id` · `version` · 여섯 칸 값 · 여섯 칸 `baseline`, 새 줄 = 여섯 칸 값)에서 값 ≠ `baseline`인 칸이 있는 저장된 줄만 `{id, version, ...그 칸들, baseline: {그 칸들의 baseline}}`으로, 새 줄은 여섯 칸만 담아 `{eventId, rows}`를 돌려준다(다른 키를 더하지 않는다 — 경계 숫자가 이 모양 기준이다). `saveTooLargeReason(payload)` — `lib/actions/payload-size.ts`의 `checkPayloadSize(payload)`를 그대로 불러 `ok`면 null, 아니면 그 `reason`(새 한도 상수 · 새 문장을 만들지 않는다 — 서버 미들웨어와 같은 함수 · 같은 한도라 화면이 통과시킨 요청은 서버도 통과시킨다). 액션별 한도는 쓰지 않는다(Task 1 ⑤ — 통로 없음).
  - 저장을 나눠 보내는 코드를 두지 않는다(전부 저장 또는 전부 거부). 넘으면 보내지 않고 기존 문장 「나눠서 저장해 주세요」를 세우며 고친 값은 표에 그대로 남는다 — 사용자가 나눠서 저장한다(받아들인 한계, codex final3 C1 · T-04.3-182). 액션별 한도는 바꾸지 않는다.

① **`winner-table-parts.tsx`(I2 · I3 공용):** 04.3-04가 `event-create-form.tsx` 안에 둔 당첨자 편집 표의 열 정의 · 셀 오류 `code` → 문장 선택(11종(`validateWinnerRows`) + `tooManyWinners` = `pinCellErrors` 12종 — 04.3-04 `pinCellErrors`를 그대로 import해 쓰고 이 파일에 `code` → 문장 표를 새로 두지 않는다, `nameTooLong` · `prizeTooLong`(E3-21) · `tooManyWinners`(E3-35)가 빠지지 않게, /review RB-P2d) · `수령자 화면` 계산 열(`roster-display.ts`) · 구별 표시 상시 힌트(`<p id>` + 머리글 `aria-describedby`)를 이 파일로 옮기고 `event-create-form.tsx`는 import만 바꾼다(I2 동작·문구 변경 없음 — 04.3-04 E2E가 그대로 녹색). 04.3-04가 이미 별도 파일로 뒀으면 그 파일을 그대로 쓰고 이 단계는 건너뛴다(SUMMARY에 적는다). 이 공용 부품에서 `구별 표시` 셀의 편집 `<input>`은 편집 중에 상시 힌트와 같은 id를 `aria-describedby`로 가리킨다(I2 · I3 공통 — 힌트 id는 부품이 한 번 만들어 머리글과 편집 입력이 같이 쓴다).

② **`winners-section.tsx`(`"use client"`):**
  - ②-a 전환: 서버가 준 상세 DTO를 받아 `editable`이면 §7-3 편집 표(머리글 `--g-100`), 아니면 `winners-view.tsx` 읽기 표를 그린다 — **페이지는 두 경우 모두 이 부품 하나를 그린다**(라우터 새로 고침 뒤에도 편집 중 닫힘 안내 상태가 남게).
  - ②-b 셀 단계(서버 판정 그대로): 미제출 줄 여섯 칸 편집 · 제출된 줄 여섯 칸 읽기 전용 + 이유 `제출된 자리 · 고칠 수 없음` · `수령자 화면` · 제출 두 열은 늘 읽기 전용.
  - ②-c 표 조작: 새 줄 `Ctrl+Enter`(Phase 4 `use-grid-keyboard` `onNewRow` — D-94 Ctrl 전용, 기본 수량 1 · 전달 현장) · Delete는 저장 전 새 줄만 · 붙여넣기 · 힌트 줄 · 1차 「일괄 저장 N」 + kbd `Ctrl+S`(§7-3 (사) — Phase 4 SYSTEM · UI-SPEC 모두 `Ctrl+키` 표기, /plan-design-review D-6 · E3-01). 힌트 줄은 UI-SPEC I3 그대로 `이동 Tab ↑↓←→ · 복사 Ctrl+C · 붙여넣기 Ctrl+V · 취소 Esc · 새 줄 Ctrl+Enter` — 1차 kbd로 보이는 `Ctrl+S`는 힌트 줄에 또 적지 않는다(DR-31 · D-6). 비활성 1차는 Phase 4 `Button`의 `disabled` + `disabledReason`(`aria-disabled` — 네이티브 `disabled`가 아니다). 편집이 0칸이면 1차 비활성 이유는 UI-SPEC Copywriting 「I3 1차 「일괄 저장」 막힘(편집 0칸)」 그대로 `바뀐 칸 없음`이고 `reasonTone: "info"`(`--muted` — 저장할 편집이 없는 정상 상태, SYSTEM §7-1 U-4 · UI-SPEC Color 「막힘 이유의 색」 · 교차 X-4 · 반영 검사 r1 W1). 고정된 오류 · 충돌 셀은 막힘 이유가 아니다 — 남아 있어도 1차는 켜진 채이고 누르면(버튼 · `Ctrl+S`) 서버를 부르지 않고 Phase 4 `Table`의 `firstIssueSignal`(04-47)로 첫 오류 셀에 포커스를 옮긴다(SYSTEM §7-3 DR-5 — 04.3-04 I2와 같다, 반영 검사 r1 B-4).
  - ②-d 제출 열·행동 열(머리글은 보이는 글자 없이 `.sr-only` `행동` — UI-SPEC I3): 제출 열은 Copywriting 「I3 제출 셀」 그대로 — `제출됨` + 2행 시각 · `미제출` · `잠김` + 2행 `HH:mm까지`(DTO `lockedUntil`, 짧은 잠김) · `잠김` + 2행 `잠금 풀기 필요`(DTO `hardLockedAt`, 누적 잠김, `--fs-sm --muted`). `잠김`은 미제출 줄에만 — 제출된 줄 DTO에는 잠금 값이 없다(Task 1). 행동 열은 `actionCell: true`(⓪-b-5)로 선언한 로빙 셀 하나이고(편집 표는 `enableGridKeyboard` — 방향키로 닿고 탭 정지를 늘리지 않는다) 안에 3차 하나까지만, 그 3차는 `tabIndex={-1}`이다(셀에서 `Enter`/`Space` = 그 3차 누름, UI-SPEC:493): 제출된 줄에 DTO `submissionId`가 있을 때만 링크 「제출 내용」(→ `/certs/submissions/{id}`) · DTO `unlockable`이 참인 줄에만 버튼 「잠금 풀기」(②-h). 화면은 `unlockable`만 보고 권한 · 접수 중 · 잠금 여부를 다시 계산하지 않는다(서버가 정한다) — 짧은 잠김 줄 · 잠기지 않은 줄 · 보기만 가능한 사람에게는 버튼이 없다.
  - ②-e 저장 응답별:
  - 줄 상태(codex final C3): 저장된 줄마다 `version`과 함께 `baseline`(처음 읽은 여섯 칸 값 — 상세 DTO에서 만든다, `quote-table.tsx`의 `baselineFromDto` 선례)을 화면 안에 든다. 「일괄 저장」은 ⓪-c `buildWinnerSavePayload`로 바뀐 줄 · 고친 칸 · 그 칸들의 `baseline`만 보낸다(여섯 칸 `baseline` 전부를 보내지 않는다 · 새 줄은 여섯 칸만, codex final2 C). 보내기 전에 `saveTooLargeReason`이 null이 아니면 요청을 보내지 않고 합계 행에 그 문장(`요청이 너무 큽니다 · {N}KB > 256KB 한도 · 나눠서 저장해 주세요`, `--danger`)을 세우며 고친 값 · 오류 셀은 그대로 둔다 — 결과 불명 문장이 아니다(보내지 않았으므로)
  - `saved` → 표를 새 DTO로(각 줄 `version` · `baseline`도 새 DTO 값으로) · 합계 행 `저장됨 N줄 HH:mm`
  - `invalid` → 오류 셀 고정 · `오류 N칸 · 전부 거부`
  - `conflict` → §7-3 (나): 실제로 바뀐 셀만 고정 · `다른 사람이 {HH:mm}에 {값}으로 바꿈 · 덮어쓰기 / 그 값으로`(3차 둘 — 덮어쓰기 = 그 셀의 `baseline`만 서버 값으로 바꾸고 내 값 유지, 그 값으로 = 그 셀의 값과 `baseline`을 서버 값으로 — 값 = `baseline`이라 고친 칸이 아니게 되어 다음 요청에서 빠진다. 줄 `version`은 옛 값 그대로 둔다 — 다음 저장에서 서버가 보낸 칸의 `baseline`과 지금 값을 다시 견주므로 고른 칸은 충돌이 아니고, 아직 고르지 않은 칸 · 그새 또 바뀐 칸만 다시 선다, codex final C3) · 합계 행 `충돌 N줄 · 전부 거부`
  - `submittedRows` → 그 줄들만 서버 값 + 읽기 전용으로 바꾸고 그 줄의 편집값을 버림 · 다른 줄 편집값 유지 · 합계 행 `제출된 줄 {n} · 전부 거부 · 그 줄 고친 값은 버림` · 「덮어쓰기 / 그 값으로」 없음
  - `closed` → 편집값을 모두 버리고 읽기 표로 · 합계 행 `링크 닫힘 · 고친 {N}칸 저장 안 됨`(`aria-live="polite"`, N = 버린 셀 수) · 라우터 새로 고침으로 머리 태그·QR 자리를 닫힘 모양으로
  - 결과 불명(연결 끊김 · `serverError`) → §7-3 저장 실패 문장(프로젝트 원장과 같은 문장), 값 유지
  - ②-f 떠나기: 미저장 편집은 브라우저 저장소 API로 보관하지 않고 dirty면 `beforeunload` 경고만(개정 ⑪ — 04.3-04 I2와 같다).
  - ②-g0 **좁은 PC(/plan-design-review D-10 · SYSTEM §6 ⑷ DR-36 · DR-24):** 700~1023: 읽기 표 · 힌트 줄 없음 · 1차 「일괄 저장 N」은 N ≥ 1일 때만 선다(1024 이상에서 고친 뒤 창이 좁아진 경우 — SYSTEM §7-3 「`N ≥ 1`이면 항상 렌더한다」, Phase 4 04-49 R1과 같은 규칙 · 반영 검사 r1 N-4), 행동 열 3차 유지 — 판정은 Phase 4 `ui/table/use-editable-width.ts`의 `useEditableWidth()`(04-49 — 1024 이상이면 참 · 서버 스냅숏 참) 하나다(견적 원장 `quote-table.tsx`가 부르는 같은 훅 — `grep -n "useEditableWidth" "app/(app)/projects/[id]/quote-table.tsx"`로 쓰는 법을 본다). 훅이 없으면 Task 1 `<precondition>`이 이미 멈췄다 — 다른 이름 · 다른 판정을 찾아 쓰지 않는다(04.3-04와 같은 멈춤 규칙, 반영 검사 r1 N-5). `winners-section`은 `editable`이어도 이 판정이 거짓이면 편집 표 대신 읽기 표 모양으로 그린다 — `enableGridKeyboard` · 셀 편집 · 새 줄 · Delete · 붙여넣기 · 힌트 줄이 없고(편집 상태 · N은 그대로 두어 고친 셀은 `ui/table` dirty 모양으로 보이고 1차 「일괄 저장 N」은 N ≥ 1일 때만 선다 — 안내 글자 없음), 행동 열은 `actionCell` 없이 셀 안 3차(「잠금 풀기」 · 「제출 내용」)가 보통 `Tab` 대상이다(`tabIndex={-1}`은 grid일 때만 — UI-SPEC:493 끝 문장). 「잠금 풀기」 확인은 ②-h · ④-b 그대로 표 밖 하나, 머리 「링크 닫기」는 그대로 남는다. 폰(<700) 칸 접기는 ②-g 그대로. 화면 폴더에 폭 판정(미디어 쿼리 구독 훅 · 1024 폭 미디어 쿼리)을 새로 두지 않는다(CLAUDE.md §6 — 화면 하나만의 예외 금지).
  - ②-g 폰(<700): 칸 접기 — P1 이름 · 수량 · 제출, P2 접힌 줄 `{전화} · {경품} · {전달}`(구별 표시가 있으면 ` · {구별 표시}`), 셀 편집 없음, 행 탭 → `RowSheet`(보기 전용 본문 + `action` 자리에 3차 하나: 제출된 줄이고 `submissionId`가 있으면 「제출 내용」 링크, `unlockable`인 줄이면 「잠금 풀기」 버튼). 폰 순서(UI-SPEC I3 폰 불릿): 「잠금 풀기」 → 대상을 미뤄 두고 `RowSheet`를 먼저 닫는다(`RowSheet`는 `onClose` 뒤 포커스를 그 줄로 되돌린다 — Phase 4 `RowSheet.tsx` 43~44행) → `RowSheet`의 `onClose`에서 미뤄 둔 대상을 `unlockTarget`으로 옮겨 ④-b의 확인 부품(폰 <700은 §7-17 시트 모양)을 연다 → 부품이 열 때의 `document.activeElement`가 그 줄이라, 닫히면(취소 · `Esc` · × · 성공 · 실패 뒤 취소 모두) 포커스는 **그 줄**(`Table`의 `onRowTap` 접힌 줄 셀 — 누른 버튼은 이미 닫힌 `RowSheet` 안에 있다).
  - ②-h **「잠금 풀기」(소유자 결정 「누적 잠금」 · UI-SPEC I3 「잠금 풀기」 불릿):**
    - 누르면 ④-b의 표 밖 `ConfirmDialog` 하나를 문구만 바꿔 연다 — Copywriting 「I3 잠금 풀기 확인」(3차 손질본 정본 — /plan-design-review D-9 · X-5): 제목 `잠금 풀기` · 부제 `{이름}`(그 줄 당첨자 이름만) · 결과 줄 `틀린 횟수를 모두 지웁니다`(되돌릴 수 없는 것 하나만 — 잠김 · 다시 열림은 줄 표시와 버튼 이름이 이미 말한다) · 2차 `취소 Esc`(부품 파생) · 1차 `잠금 풀기`. 열리면 포커스 = 1차. PC에서 2차 · `Esc`로 닫으면 포커스는 그 줄의 행동 셀(로빙 위치 그대로, UI-SPEC:493) — 3차 `onClick`이 먼저 `event.currentTarget.closest("td")?.focus()`로 그 셀에 포커스를 둔 뒤 `unlockTarget = {rowId, name, lockStamp}`를 세운다. 부품은 열 때의 `document.activeElement`(= 그 셀)로 되돌린다(④-a — 마우스로 눌러 버튼이 포커스를 가졌어도, `Enter`/`Space`로 눌러도 같은 셀이고, 성공 뒤 버튼이 사라져도 셀은 남는다).
    - 요청: 1차 = `unlockCertWinnerAction({eventId, rowId, lockStamp})` — `lockStamp`는 확인 부품을 **열 때** 그 줄 DTO의 `hardLockedAt`을 붙잡아 둔 값이고, 결과 불명 뒤 다시 누르면 **같은 값**을 다시 보낸다(서버 재전송 판정, Task 1 결정 ⑥). 응답은 04.3-03 E3와 같은 방식으로 20초가 지나면 받지 못한 것으로 보고(`undefined`) `unlockOutcome`(⓪)으로 가른다. 진행 중: 부품의 `pending`(1차 `…` · 나머지 `aria-disabled` · `Esc` · 가림막 무시, ④-a).
    - 줄 다시 그리기(`unlocked` · `stale` 공통 — 성공 응답만 보고 `미제출`로 그리지 않는다, Codex UI 6차 #1): 응답 `row`(서버의 지금 상태)가 제출됨이면 `submittedRows`처럼 그 줄만 서버 값 + 읽기 전용으로 바꾸고 그 줄 편집값을 버린다. 아니면 그 줄의 **제출 · 잠금 칸(`submittedAt` · `lockedUntil` · `hardLockedAt` · `unlockable`)만** 바꾸고 그 줄 · 다른 줄의 미저장 편집값과 줄 `version`은 그대로 둔다(풀기는 `version`을 바꾸지 않으므로 같은 줄 편집도 다음 「일괄 저장」에서 그대로 저장된다). `row`가 여전히 누적 잠김(과거 풀기의 재전송 뒤 새로 잠김)이면 `잠김` + 새 잠금 표시의 「잠금 풀기」가 그대로 선다. 포커스는 PC에서 그 줄 행동 셀(로빙 셀 — 버튼이 사라져도 칸은 남는다), 폰에서 ②-g의 그 줄.
    - `unlocked` → 확인 부품 닫힘 + 토스트 `잠금 풀기 · {이름}`(Copywriting 「I3 잠금 풀기 토스트」) + 줄 다시 그리기.
    - `stale`(그새 다른 직원이 풀었거나 · 제출됐거나 · 다른 잠금 표시로 다시 잠김) → 확인 부품 닫힘 · 토스트 없음 · 줄 다시 그리기. 문구를 새로 만들지 않는다 — 바뀐 줄 모양이 곧 결과이고, 아직 잠겨 있으면 새 표시의 「잠금 풀기」가 다시 선다(재량 — UI-SPEC에 `stale` 문구가 없어 사용자가 할 결정을 늘리지 않는 쪽을 골랐다. SUMMARY에 적는다).
    - `closed` → 확인 부품 닫힘 + ②-e `closed`와 같은 처리(편집값 모두 버림 · 읽기 표 · 합계 행 `링크 닫힘 · 고친 {N}칸 저장 안 됨` · 라우터 새로 고침).
    - `failed` → `primary.failure` = `풀지 못했습니다 · 다시 시도`(Copywriting 「I3 잠금 풀기 실패」 — 1차 왼쪽, ④-0), 모달 유지 · 1차 다시 누름 가능.
    - `unknown` → `primary.failure` = `풀었는지 확인하지 못했습니다 · 다시 눌러 주세요`(Copywriting 「I3 잠금 풀기 결과 불명」), 모달 유지 · 1차가 같은 `{rowId, lockStamp}`를 다시 보낸다.
    - 키보드(UI-SPEC:493 · ⓪-b에서 공용 표가 받는다): 행동 셀은 로빙 셀 하나 · 셀 안 「잠금 풀기」 · 「제출 내용」은 `tabIndex={-1}` · 셀에서 `Enter`/`Space` = 그 3차 누름(표의 `onActivateCell`이 셀 안 3차를 누른다 — 이 화면은 따로 키 처리를 두지 않는다) · 확인을 취소 · `Esc` · × · 성공 · `stale` · 실패 뒤 취소로 닫으면 포커스는 그 행동 셀. 모달 1차에서 누른 `Enter`는 표 셀로 올라가지 않는다(④-b — 요청 한 번, 모달이 다시 열리지 않음, E3-20). 「제출 내용」 링크를 셀에서 `Enter`로 누르면 I4로 간다.

③ **`winners-view.tsx`:** 읽기 표에 행동 열(「제출 내용」 3차 링크, `submissionId`가 있을 때만)과 폰 `RowSheet` `action`을 더하고, 제출 열의 누적 잠김 2행 `잠금 풀기 필요`를 ②-d와 같은 규칙으로 그린다 — DTO `hardLockedAt`이 있을 때만이라 닫힌 행사(Task 1: 서버가 싣지 않음)의 그 줄은 `미제출`이고, 접수 중인데 보기만 가능한 사람에게는 `잠김` + `잠금 풀기 필요`가 버튼 없이 보인다. 읽기 표는 grid가 아니므로(`enableGridKeyboard` 없음) 행동 열에 `actionCell`을 쓰지 않고 「제출 내용」은 보통 `Tab` 대상 링크다(UI-SPEC:493 끝 문장). 읽기 표에는 「잠금 풀기」가 없다 — `unlockable`은 `editable`일 때만 참이라(Task 1) 그 줄은 늘 ②의 편집 표에 있다. 나머지 모양은 그대로.

④ **확인 부품 — Phase 4 `ui/confirm-dialog`의 `ConfirmDialog` 하나를 「링크 닫기」 · 「잠금 풀기」가 문구만 바꿔 같이 쓴다(결정 ⑤ · UI-SPEC Component Inventory 「「링크 닫기」 · 「잠금 풀기」 확인」 · 소유자 결정 「링크 닫기와 같은 `<dialog>`」 — 부품이 네이티브 `<dialog>`다 · E3-01). 화면 폴더에 확인 부품이나 `<dialog`를 새로 만들지 않는다:**
  - ④-0 **다시 누를 수 있는 실패 줄 — 정본 → RED → 부품(CLAUDE.md §6 · `docs/DESIGN.md` §4):** Phase 4 `ConfirmDialog`의 1차 왼쪽 자리는 `disabledReason`뿐이고 그 값이 있으면 1차가 `aria-disabled`라, 「닫지 못했습니다 · 다시 시도」 뒤 1차를 다시 누를 길이 없다(`grep -n 'disabledReason' ui/confirm-dialog/ConfirmDialog.tsx`로 찾은 1차 왼쪽 자리 · SYSTEM §7-17 마지막 불릿 — 줄 번호를 전제하지 않는다). 실행 시점에 같은 뜻의 자리(1차를 막지 않는 실패 줄)가 이미 있으면 그것을 쓰고 이 단계를 건너뛴다(SUMMARY에 그 이름과 줄을 적는다). 없으면 순서대로:
    - `docs/design/DECISIONS.md` 끝에 `## 2026-09-25 — §7-17 확인 모달: 다시 누를 수 있는 실패 줄`(기존 형식) — **결정**: `primary.failure?: string`을 1차 왼쪽 막힘 자리(`.reason`, `block` 색)에 `role="alert"`로 그리고 1차는 막지 않는다 · `disabledReason`이 있으면 그것만 보인다. **왜**: 결과가 확정되지 않았거나 다시 보내면 되는 실패(연결 끊김 · 결과 불명 · 일시 실패)는 같은 요청을 다시 보내는 것이 다음 행동이다(확인증 I3 「링크 닫기」 · 「잠금 풀기」가 첫 적용). 누른 뒤에 생기는 실패라 `role="alert"`(§6 로그인 실패 줄 선례)이고, 누르기 전 막힘 이유(`aria-describedby`, §10)와는 다르다. **버린 대안**: `disabledReason`에 실패 문장(1차가 막혀 다시 누를 수 없다) · 화면마다 모달 밖 줄(§7-17 「서버 거부 문자열은 막힘 자리」와 어긋나고 화면 하나만의 예외가 된다). **범위**: SYSTEM §7-17, `ui/confirm-dialog/ConfirmDialog.tsx`.
    - `docs/design/SYSTEM.md` §7-17 마지막 불릿(서버 거부 문자열) 바로 뒤에 한 줄: 「다시 보내면 되는 실패(연결 끊김 · 결과 불명)는 같은 자리에 `failure`로 붙고 1차는 막지 않는다 — 1차를 다시 누르는 것이 다음 행동이다」. 주변 문장은 고치지 않는다.
    - **RED:** Phase 4 `test/unit/ui/confirm-dialog.test.ts`(기존 파일 — `renderToStaticMarkup` 선례)에 `behavior`의 두 케이스를 더하고 `pnpm vitest run --project unit test/unit/ui/confirm-dialog.test.ts`가 실패하는 것을 본다.
    - **GREEN — `ui/confirm-dialog/ConfirmDialog.tsx`:** `ConfirmDialogPrimary`에 선택 `failure?: string`(한국어 주석 한 줄 — §7-17 다시 누를 수 있는 실패), 1차 왼쪽 자리에서 `disabledReason`이 없고 `failure`가 있으면 `<span className={styles.reason} role="alert">`로 그린다. 새 CSS 클래스 · 색 · 토큰 없음(`.reason` 그대로 — 색 `.reason`(=`block`, `--danger`) · `role="alert"`은 /plan-design-review D-4에서 실행 전 확정). 다른 슬롯 · 포커스 · 2차 라벨 파생 · 진행 중 동작은 그대로.
    - 커밋 `feat(ui): retryable failure line on confirm dialog`(본문 한국어 — DECISIONS · SYSTEM 한 줄과 같은 커밋이어도 되고 문서가 먼저인 두 커밋이어도 된다) — ⓪-b와 같이 ①~⑦의 화면 커밋보다 먼저다(E3-06과 같은 규칙).
  - ④-a **쓰는 법(두 확인 공통):** `open` · `onClose` · `title` · `subtitle` · `resultLines`(결과 줄 한 줄) · `primary: {label, onConfirm, pending, failure}`만 쓴다. 2차 `취소 Esc`는 부품이 1차 라벨에서 파생한다(`secondaryLabelFor` — 두 1차 라벨에 「취소」가 없어 `취소`). 1차 kbd는 부품 기본 `Ctrl+Enter`. × 는 부품 그대로(§7-17 — `aria-label="닫기"`, 폰 시트에서만 보임)이고 이 플랜이 이름을 바꾸지 않는다(결정 ⑤). 열리면 포커스 = 1차이고, 닫히면 부품이 **열 때의 `document.activeElement`**로 되돌린다(없어졌으면 `h1`) — 그래서 여는 쪽이 열기 전에 포커스 자리를 정한다(「링크 닫기」 = 여는 버튼, 「잠금 풀기」 = ②-h · ②-g). 진행 중 1차 `…` · 나머지 `aria-disabled` · `Esc` · 가림막 무시는 부품이 한다.
  - ④-b **그리는 자리(E3-20):** 「잠금 풀기」 확인은 `winners-section.tsx` 최상위 — 표(`Table`)의 형제 — 에 `ConfirmDialog` **하나**만 두고 여는 줄은 state(`unlockTarget = {rowId, name, lockStamp} | null`)로 넘긴다. 행동 셀 렌더(`td` 안)에 확인 부품을 두지 않는다 — React 합성 이벤트는 React 트리를 따라 올라가므로 `td` 안에 그린 모달에서 누른 `Enter` · 방향키가 셀 `onKeyDown`(`handleConflictKey` → `keyboardState.handleKeyDown`)에 닿아 셀 3차를 다시 누르거나 로빙 포커스를 옮긴다. 「링크 닫기」 확인도 머리의 `close-link-dialog.tsx` 안, 표 밖이다.
  - ④-c `close-link-dialog.tsx`(`"use client"`): 머리 2차 「링크 닫기」 버튼 + `ConfirmDialog`에 문구 — 제목 `링크 닫기` · 부제 `{행사 이름} · 제출 {제출}/{전체}명` · 결과 줄 `미제출 {n}명은 더 제출할 수 없습니다 · 다시 열 수 없습니다` · 2차 `취소 Esc`(부품 파생) · 1차 `링크 닫기`(Copywriting Destructive confirmation).
  - ④-d 「링크 닫기」 응답: `closed` → 모달 닫힘 + 토스트 `링크 닫기 · 미제출 {unsubmitted}명`(기존 토스트 전달 방식) + 라우터 새로 고침 / `alreadyClosed` → 모달 닫힘 + 새로 고침(토스트 없음) / 그 밖(실패 · 결과 불명) → `primary.failure` = `닫지 못했습니다 · 다시 시도`, 모달 유지 · 1차 다시 누름 가능(④-0 — `closeEvent`는 두 번째 호출이 `alreadyClosed`라 다시 눌러도 안전하다).
  - I3 폴더(`[id]/`)에는 `<dialog` 요소가 없다 — `close-link-dialog.tsx` · `winners-section.tsx`는 `@/ui/confirm-dialog/ConfirmDialog`를 import해 쓴다(두 확인이 같은 부품).

⑤ **`[id]/page.tsx` · `event-detail.module.css`:** `PageHeader`를 `.header` · `.titleBlock` · `.headerActions`(quote-table 선례 — 클래스는 이 화면 CSS에 새로, 토큰만)로 감싸고, DTO가 접수 중 + `editable`일 때만 `.headerActions`에 `close-link-dialog`를 둔다. 당첨자 섹션은 `winners-section` 하나.

⑥ **`ui/table/RowSheet.tsx` · `RowSheet.module.css`(개정 ⑫):** 선택 prop `action?: ReactNode` — 있으면 본문(`KvList`) 아래 한 자리에 그린다(위 여백 토큰, 새 색 없음). 머리 주석의 「행동 줄을 두지 않는다」 문장을 「셀 편집·저장 행동은 없다 · 3차 한 개(다른 화면으로 가는 링크, 또는 확인 시트를 여는 줄 행동)는 `action`으로 둘 수 있다(§7-3 (바) 개정 ⑫)」로 고친다(UI-SPEC 개정 ⑫ 문장). 다른 prop·동작은 그대로 — `action`을 주지 않는 견적 원장 시트는 button이 「닫기」 하나뿐인 채로 남는다.

⑦ 커밋 `feat(04.3): winner editing, link close and unlock on event detail`, 본문 한국어(소유자 결정 인용). `RowSheet` 변경은 별도 커밋 `feat(ui): optional action slot on row sheet`이어도 된다(별도면 이 화면 커밋보다 먼저 — ⓪-b · ④-0의 공용 부품 커밋과 같은 규칙, E3-06).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/unlock-outcome.test.ts test/unit/certs/save-payload.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 결과 불명 갈래(연결 끊김 · 20초 · 5xx · `serverError` · 해석 불가)가 확정 실패나 성공으로 새고 있거나, 저장 요청이 여섯 칸 `baseline`을 싣거나 500줄 경계에서 한도 판정이 어긋났다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/ui/grid-action-cell.test.ts test/unit/ui/conflict-focus.test.ts test/unit/ui/confirm-dialog.test.ts test/unit/design-system-docs.test.ts test/unit/ui/system-md-compliance.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이거나 "No test files found"가 나온다 — 행동 셀 Enter/Space 판정 · 공용 표 배선이 없거나, Phase 4 충돌 셀 키보드가 깨졌거나, 확인 부품의 실패 줄이 없거나 1차를 막거나, SYSTEM (아) · §7-17 한 줄이 기존 SYSTEM 단언을 깼다</fails_when>
    <automated>L="$(git log --reverse --format=%s)" &amp;&amp; printf '%s\n' "$L" | awk '/^feat\(ui\): action cell activation in grid keyboard$/{a=NR} /^feat\(04\.3\): winner editing, link close and unlock on event detail$/{s=NR} END{exit !(a &amp;&amp; s &amp;&amp; a &lt; s)}'</automated>
    <fails_when>종료 코드가 0이 아니다 — 공용 표 커밋이 없거나 화면 커밋보다 뒤다(E3-06)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint(`pnpm lint`가 둘 다 돈다)·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 새 색·리터럴 값은 stylelint가 잡는다</fails_when>
    <automated>pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-events.spec.ts &amp;&amp; pnpm playwright test --no-deps --project=desktop test/e2e/quote-table.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — I2 공용 부품 이동이나 RowSheet 변경이 기존 화면을 깼다</fails_when>
    <automated>cat "app/(app)/certs/events/winner-table-parts.tsx" "app/(app)/certs/events/[id]/winners-section.tsx" | grep -vE '^\s*(//|/?\*)' | grep -q 'pinCellErrors(' &amp;&amp; test "$(cat "app/(app)/certs/events/winner-table-parts.tsx" "app/(app)/certs/events/[id]/winners-section.tsx" | grep -vE '^\s*(//|/?\*)' | grep -c 'labelDigits')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — I2 · I3 공용 표가 04.3-04 `pinCellErrors`(11종 + `tooManyWinners` = 12종)를 부르지 않거나, 화면 파일에 셀 오류 `code` → 문장 표를 따로 만들었다(그런 표는 9종이든 12종이든 표시 번호 코드 이름을 담는다 — 두 파일에서 그 코드 이름이 0줄이어야 한다. 옛 「9종」 매핑이면 `nameTooLong` · `prizeTooLong` · `tooManyWinners` 문장이 빠진다, /review RB-P2d)</fails_when>
  </verify>
  <acceptance_criteria>
    - `winners-section.tsx`와 `winner-table-parts.tsx`에 브라우저 저장소 사용이 없다(`! grep -nE "localStorage|sessionStorage|useDirtyStorage" "app/(app)/certs/events/[id]/winners-section.tsx" "app/(app)/certs/events/winner-table-parts.tsx"`)
    - `ui/table/RowSheet.tsx`의 `RowSheetProps`에 선택 prop `action`이 있다
    - (/review RB-P2d) I2 · I3 셀 오류 문장은 04.3-04 `pinCellErrors` 하나에서 온다 — `winner-table-parts.tsx` · `winners-section.tsx`에 `code` → 문장 표가 없고(verify 마지막 줄 — 코드 이름 0줄), `nameTooLong` · `prizeTooLong` · `tooManyWinners` 셋 모두 I3에서 문장으로 뜬다(`tooManyWinners`는 Task 1 behavior의 `pinCellErrors` 렌더 단언)
    - `[id]/page.tsx`가 편집 가능 여부와 무관하게 당첨자 섹션으로 `winners-section`만 그린다
    - I3 확인 부품이 Phase 4 공용 부품 하나다: 화면 폴더에 `<dialog`가 없고(`! grep -rn "<dialog" "app/(app)/certs/events/[id]"`) `close-link-dialog.tsx`와 `winners-section.tsx`가 둘 다 `@/ui/confirm-dialog/ConfirmDialog`를 import한다(`grep -n "ui/confirm-dialog/ConfirmDialog"` 두 파일, E3-01)
    - `winners-section.tsx`의 `ConfirmDialog`는 표 밖 최상위에 하나다 — `grep -c "<ConfirmDialog" "app/(app)/certs/events/[id]/winners-section.tsx"`가 1이고 행동 셀 렌더 안에 없다(SUMMARY에 그 줄과 `unlockTarget` state 줄을 인용한다, E3-20)
    - ④-0: `ui/confirm-dialog/ConfirmDialog.tsx`에 선택 `failure`가 있고(`grep -n "failure" ui/confirm-dialog/ConfirmDialog.tsx`) DECISIONS §7-17 항목 · SYSTEM §7-17 한 줄이 같은 커밋이거나 먼저다 · `confirm-dialog.test.ts` 두 케이스의 RED/GREEN 기록이 SUMMARY에 있다 · 커밋 `feat(ui): retryable failure line on confirm dialog`이 화면 커밋보다 먼저다(실행 시점에 같은 뜻의 자리가 이미 있어 건너뛰었으면 그 자리 이름과 줄을 SUMMARY에 인용한다)
    - 두 화면 파일에 `⌘`가 없다(`! grep -n "⌘" "app/(app)/certs/events/[id]/winners-section.tsx" "app/(app)/certs/events/[id]/close-link-dialog.tsx"`, D-94 · E3-01)
    - 옮겨 올 정본에도 `⌘`가 없다 — UI-SPEC 계약 본문(과거 기록 절 `## Codex 디자인 검토 반영` 앞까지)에서 `awk '/^## Codex 디자인 검토 반영/{exit} {print}' .planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md | grep -c "⌘"`의 출력이 `0`(/plan-design-review D-6 — 0이 아니면 화면 문구를 옮기기 전에 멈추고 보고한다)
    - 좁은 PC(D-10 · DR-36): `winners-section.tsx`가 Phase 4 폭 판정 `useEditableWidth`를 쓴다(`grep -n "useEditableWidth" "app/(app)/certs/events/[id]/winners-section.tsx"`가 1줄 이상 — 다른 이름은 받지 않는다: 훅이 없으면 Task 1 `<precondition>`에서 멈췄다, 04.3-04와 같은 규칙 · N-5) · 화면 폴더에 새 폭 판정이 없다(`! grep -rnE "matchMedia|min-width: *1024" "app/(app)/certs/events/[id]"`)
    - `winners-section.tsx`가 「잠금 풀기」 버튼을 DTO `unlockable`로만 그리고(`grep -n "unlockable" "app/(app)/certs/events/[id]/winners-section.tsx"`) 권한 · 역할 이름을 화면에서 다시 판정하지 않는다(`! grep -nE "can\\(|roleId|role-" "app/(app)/certs/events/[id]/winners-section.tsx"`)
    - 잠금 풀기 요청이 확인 부품을 열 때 붙잡은 `lockStamp`를 결과 불명 뒤에도 그대로 다시 보낸다(SUMMARY에 붙잡는 줄과 보내는 줄을 인용한다)
    - `unlockOutcome` 단위 테스트가 ⓪에서 모듈 없이 실패한 기록과 GREEN 기록이 SUMMARY에 있다
    - `winner-table-parts.tsx`의 `구별 표시` 편집 입력이 상시 힌트 id를 `aria-describedby`로 가리킨다(머리글과 같은 id 변수 — SUMMARY에 두 줄을 인용한다)
    - 행동 셀 계약(UI-SPEC:493): `grep -n "gridEnterAction" ui/table/use-grid-keyboard.ts` · `grep -n "actionCell" ui/table/types.ts ui/table/Table.tsx "app/(app)/certs/events/[id]/winners-section.tsx"`가 각각 1줄 이상이고, `winners-section.tsx`의 「잠금 풀기」 · 「제출 내용」 3차에 `tabIndex={-1}`이 있다(SUMMARY에 두 줄 인용) · `grid-action-cell.test.ts`가 ⓪-b-3에서 export 없이 실패한 기록과 GREEN 기록이 SUMMARY에 있다
    - 이 플랜에 「`ui/table`을 고치지 않고 SUMMARY 편차로 남긴다」는 이월 경로가 없다 — 행동 셀 키보드는 이 태스크가 구현한다(Codex r3 C-1)
    - `docs/design/DECISIONS.md`에 §7-3 (아) 행동 셀 항목이 있고 `docs/design/SYSTEM.md` §7-3 (아)에 `3차 하나만 든 행동 셀은 로빙 셀 하나다`가 있다(`grep -n "로빙 셀 하나다" docs/design/SYSTEM.md`) — 공용 표 코드 커밋보다 먼저이거나 같은 커밋이다
    - `pnpm lint`(eslint + stylelint) 녹색
    - `winners-section.tsx`가 저장 요청 줄에 `baseline`을 싣고 「덮어쓰기 / 그 값으로」가 그 셀의 `baseline`을 서버 값으로 바꾼다(`grep -n "baseline" "app/(app)/certs/events/[id]/winners-section.tsx"` — SUMMARY에 두 자리를 인용한다, codex final C3)
    - 「일괄 저장」이 `buildWinnerSavePayload`로 요청을 만들고 보내기 전에 `saveTooLargeReason`을 본다(`grep -n "buildWinnerSavePayload\|saveTooLargeReason" "app/(app)/certs/events/[id]/winners-section.tsx"`가 2줄 이상) · `save-payload.ts`가 `checkPayloadSize`를 import하고(`grep -n "checkPayloadSize" "app/(app)/certs/events/[id]/save-payload.ts"`) 숫자 상수를 새로 두지 않는다(`! grep -nE "^(export )?const [A-Za-z_]+ *= *[0-9]" "app/(app)/certs/events/[id]/save-payload.ts"`) · `save-payload.test.ts`가 ⓪-c에서 모듈 없이 실패한 기록과 GREEN 기록이 SUMMARY에 있다(codex final2 C)
    - `test(04.3): add failing event detail edit E2E` 커밋이 이 태스크의 화면 구현 커밋(`feat(04.3): winner editing, link close and unlock on event detail`)보다 먼저이고, SUMMARY에 두 스펙의 첫 실패 이유가 한 줄씩 있다(codex final2 C · CLAUDE.md §5)
    - `git log --reverse --format=%s`에서 `feat(ui): action cell activation in grid keyboard`가 `feat(04.3): winner editing, link close and unlock on event detail`보다 앞이다(Task 2 `<automated>`, E3-06)
  </acceptance_criteria>
  <done>I3가 UI-SPEC 그대로 편집·저장·충돌·편집 중 제출/닫힘·링크 닫기와 누적 잠김 줄(`잠김` + `잠금 풀기 필요`) · 「잠금 풀기」 확인(Phase 4 `ui/confirm-dialog` 하나, 표 밖) · 성공/실패/결과 불명을 보이고, 풀기 뒤 그 줄은 서버의 지금 상태로 다시 그려지며, 행동 셀이 §7-3 (아) 로빙 셀로 `Enter`/`Space`에 3차를 누르고(DECISIONS → SYSTEM → 공용 표 순서), 기존 I2와 견적 원장 화면이 그대로다</done>
</task>

<task type="auto">
  <name>Task 3: I3 편집 · 링크 닫기 · 잠금 풀기 E2E(데스크톱) + 폰 칸 접기 · RowSheet 「제출 내용」 · 「잠금 풀기」 시트</name>
  <files>test/e2e/cert-events-edit.spec.ts, test/e2e/mobile-cert-events.spec.ts</files>
  <read_first>
    - `test/e2e/helpers/cert.ts`(04.3-02, C4 — **읽기만**, 이 파일은 고치지 않는다: `createCertEvent` · `withCertFeatureOff(fn)` · `seedSubmittedCert(opts?)`(제출된 줄 표본 — domain으로 제출 하나를 만들고 `extraWinners`로 미제출 줄을 함께, 돌려주는 값에 `eventId` · `winnerId` · `submissionId` · `extraWinnerIds`, E3-32) · `signaturePngFixture()`, 기능 켜기 · 문의 전화는 `test/e2e/cert.setup.ts`가 한 번) · `playwright.config.ts`의 `certs` 프로젝트(`*cert*.spec.ts`, 워커 1) · `test/e2e/cert-events.spec.ts`(04.3-04 — 행사 준비 · 로그인 방식)
    - `test/e2e/mobile-cert-submit.spec.ts`(04.3-06 — 서버 액션 요청을 `page.route`로 끊는 방식 · 서버는 처리하게 두고 응답만 버리는 결과 불명 재전송 방식)
    - Phase 4 `ui/confirm-dialog/ConfirmDialog.tsx`(모달은 `getByRole("dialog", { name: 제목 })` · 1차 kbd `Ctrl+Enter` · 2차 `취소 Esc` · × `닫기`는 폰 시트에서만 보인다)
    - `test/e2e/fixtures.ts` — `createFixtureUser({roleId})` · `domain/permissions/roles.ts` 108행 근처 `createRole`(권한표 · 노출표 행을 만들지 않는다) · `repositories/permissions.ts` `upsertPermission` · `upsertVisibility` · `domain/permissions/visible.ts`(행이 없으면 거부)
    - `test/integration/cert-events-edit.test.ts`(Task 1 — 누적 잠김 네 칸을 `db/client`로 직접 채우는 준비 방식. E2E도 같은 방식으로 줄을 누적 잠김 · 짧은 잠김으로 만든다 — 20번 틀리는 끝까지 경로는 Task 1 통합이 이미 증명한다)
  </read_first>
  <action>
아래 ⓪~②는 Task 2 맨 앞 「E2E 먼저」가 구현 **전에** 쓰고 RED를 확인한 스펙의 내용이다(codex final2 C). 이 태스크는 Task 2 구현 뒤 같은 스펙을 녹색으로 만든다 — 단언을 빼거나 약하게 바꾸지 않고, 구현과 어긋난 선택자만 고친다.

⓪ **격리(C4, 두 스펙 공통):** 전제: 이 권한은 시드 기본값이 아니라 `cert.setup`이 켠 것이다(E3-13) — 이 스펙이 시스템 관리자(박서연)로 「제출 내용」 · I4 접근을 확인하는 케이스(①의 권한 · 행동 셀 키보드 · ②)는 `role-sysadmin`의 `certs.submissions`(view) 권한이 04.3-09 시드 insert-if-absent 규칙 뒤로 배포마다 자동으로 켜지지 않고 `test/e2e/cert.setup.ts`(04.3-02)가 이 스펙보다 먼저 한 번 명시적으로 켠다는 전제이며, 이 스펙 자신은 그 권한을 켜지 않는다. 머리에 `test.describe.configure({ mode: "serial" })`. 기능 켜기 · 문의 전화는 `cert.setup.ts`에 맡기고 스펙은 공유 설정을 직접 바꾸지 않는다. 행사는 `createCertEvent`로 테스트마다 고유 이름으로 만들고 행사 목록 전체가 비었다거나 N개라는 단언을 하지 않는다. 게이트를 끄는 케이스는 `withCertFeatureOff(fn)` 안에서만 한다. **표본(E3-32):** 제출된 줄이 든 행사는 `seedSubmittedCert({ extraWinners })`로 만든다 — E4 폼을 조작하지 않는다. 이미 연 화면의 미제출 줄을 테스트 도중 제출해야 하는 케이스(「편집 중 제출」 하나)는 `seedSubmittedCert`가 기존 행사의 자리를 고르는 옵션을 받으면 그것으로 하고, 받지 않으면(실행 시점 `test/e2e/helpers/cert.ts`의 `opts` 타입으로 본다 — 04.3-02 C4에 요청해 둔 교차 샤드 항목) 도우미와 같은 domain 순서(`selectWinner` → `verifyLast4` → `submitCertificate`, 서명 `signaturePngFixture()`)를 스펙 안 함수 하나로 부르고 SUMMARY에 편차로 적는다. 어느 쪽이든 E4 폼은 조작하지 않는다. **모달(E3-01):** 확인 모달 단언은 `page.getByRole("dialog")` 안으로 좁히고, 단축키는 `Control+Enter` · `Control+S`(D-94)로 누른다. × 와 1차는 이름이 겹치므로(× `닫기` ⊂ 1차 `링크 닫기`) 두 버튼 셀렉터는 정확 일치로만 쓴다 — `getByRole("button", { name: "닫기", exact: true })` · `getByRole("button", { name: "링크 닫기", exact: true })`(Playwright 기본 부분 일치면 × 셀렉터가 1차를 잡는다, /plan-design-review D-5 — 부품 · SYSTEM §7-17은 그대로).

① **`test/e2e/cert-events-edit.spec.ts`(데스크톱, 행사는 `createCertEvent`, 제출된 줄은 ⓪ 표본 규칙):**
  - 편집 저장: 기획 PM이 자기 행사 I3 → **고치기 전(W1 · X-4):** 1차 「일괄 저장」이 `toBeDisabled()`이고 이유 요소의 글이 정확히 `바뀐 칸 없음`(`toHaveText("바뀐 칸 없음")` — 전체 일치)이며 `color`가 `rgb(78, 93, 89)`(`--muted` `#4E5D59` — `info`, `toHaveCSS`) → 미제출 줄 전화번호 고침 · `Control+Enter`로 새 줄 · 「일괄 저장」 → `저장됨 2줄 HH:mm` · 새로 고침 뒤 값 유지 · 외부 `/c/{token}` 목록에 새 사람이 보임
  - 모양 중복: 제출된 줄이 든 행사(⓪ 표본 규칙)에서 새 줄을 제출된 줄과 같은 모양으로 → 「일괄 저장」 → 새 줄 `구별 표시` 칸 오류 문장 · `오류 1칸 · 전부 거부` → **DR-5:** 오류가 남은 채 「일괄 저장」이 `not.toBeDisabled()`이고 다시 누르면 저장 액션 요청 0개(`page.on("request")`) · 포커스가 그 `구별 표시` 셀(`aria-invalid="true"`) → `오후 조` 적고 저장 → 저장됨
  - 요청 너무 큼(받아들인 한계 · codex final3 C1 · T-04.3-182): `createCertEvent`로 미제출 500줄 행사(경품명은 모든 줄 한글 80자 `가` — 원래 값이 짧으면 새 값 80자 + 짧은 `baseline` · `version` 1이라 한도 안(원래 값 한글 2자면 173,559 B)이어서 거부 경로를 밟지 못한다 · 가린 모양이 겹치지 않게 첫 · 끝 글자를 줄마다 바꾼 한글 세 글자 이름 · 줄마다 다른 휴대전화)를 만들고 PM이 I3 → 첫 줄 경품명 칸에서 경품명 열 500칸에 다른 한글 80자 경품명(`경` 80자) 500줄을 붙여넣기(`test/e2e/quote-table.spec.ts`의 `pasteIntoFocusedCell` 방식 — 합성 `ClipboardEvent`) → 「일괄 저장」(요청 290,559 B — `createCertEvent` 줄의 `version`은 1) → 합계 행에 `요청이 너무 큽니다 · 284KB > 256KB 한도 · 나눠서 저장해 주세요` · 저장 액션 요청이 나가지 않음(`page.on("request")`로 `Next-Action` 헤더 요청 0개) · 붙여 넣은 값이 표에 그대로 · DB 500줄의 경품명 · `version` 그대로 → 앞 250줄만 남기고 나머지 경품명 붙여넣기를 되돌린 상태(스펙이 원래 값을 다시 붙여넣기)로 「일괄 저장」(145,309 B) → `저장됨 250줄 HH:mm`(나눠서 저장 가능)
  - 편집 중 제출: 줄 A · B를 고친 상태에서 스펙이 A를 제출(⓪ 표본 규칙 — 도중 제출) → 「일괄 저장」 → `제출된 줄 1 · 전부 거부 · 그 줄 고친 값은 버림` · A 칸이 서버 값 + `제출된 자리 · 고칠 수 없음` · B의 고친 값 유지 · 다시 저장 → 저장됨
  - 편집 중 닫힘: 셀 둘 고친 상태에서 스펙이 domain으로 행사를 닫음 → 「일괄 저장」 → `링크 닫힘 · 고친 2칸 저장 안 됨` · 표가 읽기 표 · QR 이미지 없음 · 머리 태그 `닫힘`
  - 구별 표시 접근성: `구별 표시` 셀을 편집 상태로 연다 → 편집 입력의 `aria-describedby`가 상시 힌트 요소의 id와 같고 그 요소 글자가 `구별 표시는 수령자 목록에 그대로 보입니다 · 이름·전화번호는 쓰지 마세요`
  - 링크 닫기: 「링크 닫기」 → 모달(`page.getByRole("dialog", { name: "링크 닫기" })`) 제목·부제·결과 줄 문구 · 포커스 = 1차 · 모달 안 2차 `취소`(부품 파생) · 데스크톱에서 × 가 보이지 않는다(§7-17 — 폰 시트에서만) → 2차 「취소」 → 모달 닫힘 · 포커스가 「링크 닫기」 버튼으로 → 다시 열고 `Esc` → 모달 닫힘 → 다시 열고 1차 → 토스트 `링크 닫기 · 미제출 N명` · `닫힘 · … · {이름}이 닫음` · 「링크 닫기」 버튼 없음 · 외부 `/c/{token}` → E6-b(담당자가 닫음 문장)
  - 닫기 실패: 닫기 액션 요청을 `page.route`로 끊음 → 모달 안 `닫지 못했습니다 · 다시 시도` · 모달 유지 · 1차에 `aria-disabled`가 없다(④-0) · 끊기 해제 뒤 1차 → 성공
  - 모두 제출: `seedSubmittedCert()`(당첨자 한 명이 제출 — 모두 제출) 행사 → I3 `닫힘 · … · 모두 제출` · 합계 행 `합계 · 1명 · 제출 1명`(E3-32 — 도중 제출 없이 표본만으로)
  - 권한(제출된 줄은 ⓪ 표본 규칙): 시스템 관리자에게는 제출된 줄에 「제출 내용」 링크(→ I4 제목), 기획 PM에게는 없음
  - 다른 직원 충돌: 두 브라우저 컨텍스트(PM · 시스템 관리자)가 같은 줄 경품명을 고쳐 차례로 저장 → 뒤 저장이 `다른 사람이 HH:mm에 … · 덮어쓰기 / 그 값으로` · `충돌 1줄 · 전부 거부` → 「그 값으로」 → 저장할 것 없음 상태. 다른 칸(codex final C3 — 화면이 `baseline`을 싣는다는 증거): PM이 한 줄 이름 · 전화번호를, 시스템 관리자가 같은 줄 전화번호를 고쳐 관리자가 먼저 저장 → PM 「일괄 저장」 → 그 줄에서 고정된 셀은 전화번호 하나이고 이름 셀은 오류 모양이 아니다 · PM이 고친 이름 값 유지 → 「그 값으로」 → 「일괄 저장」 → `저장됨 1줄 HH:mm` · 새로 고침 뒤 PM의 이름 + 관리자의 전화번호. 고치지 않은 칸(codex final2 C — 화면이 고친 칸만 보낸다는 증거): PM이 다른 줄의 이름만, 관리자가 그 줄 전화번호를 고쳐 관리자가 먼저 저장 → PM 「일괄 저장」 → 충돌 없이 `저장됨 1줄 HH:mm` · 새로 고침 뒤 PM의 이름 + 관리자의 전화번호 · PM 저장 요청 본문(`page.on("request")`)의 그 줄에 `phone` 키가 없다
  - 잠금 풀기(소유자 결정 「누적 잠금」): 스펙이 `db/client`로 한 줄 C를 누적 잠김(`hard_locked_at` · `cumulative_failed_attempts` 20 · `failed_attempts` · `locked_until`), 다른 줄 D를 짧은 잠김(`locked_until` 미래 · `hard_locked_at` null)으로 만든다 → PM이 I3를 연다 → C 제출 열 `잠김` + `잠금 풀기 필요` · C 행동 셀에 「잠금 풀기」 · D 제출 열 `잠김` + `HH:mm까지` · D 행동 셀에 버튼 없음. 줄 B 경품명과 줄 C 경품명을 고쳐 둔다(미저장) → C 「잠금 풀기」 → 확인 부품 문구가 UI-SPEC Copywriting 「I3 잠금 풀기 확인」 그대로(제목 `잠금 풀기` · 부제가 C 이름과 정확히 같음 · 결과 줄 `틀린 횟수를 모두 지웁니다` 한 줄 · `취소 Esc` · 1차 `잠금 풀기` — D-9 · X-5) · 포커스 = 1차 → `Esc` → 닫힘 · 포커스가 C 행동 셀(`td`) → 다시 열고 1차 → 토스트 `잠금 풀기 · {C 이름}` · C 제출 열 `미제출` · 「잠금 풀기」 없음 · 포커스가 C 행동 셀 · B · C의 고친 경품명이 그대로 → 「일괄 저장」 → `저장됨 2줄 HH:mm`(풀기가 C `version`을 올리지 않아 충돌 없음) · DB C의 네 칸이 0/null · `cert_unlock` 1줄
  - 잠금 풀기 결과 불명 재전송: 새 누적 잠김 줄 E를 만들고 「잠금 풀기」 → 1차 요청을 `page.route`로 받아 `route.fetch()`로 서버에 보내 처리하게 두고 응답은 `route.abort()`로 버린다(04.3-06 방식) → 모달 안 `풀었는지 확인하지 못했습니다 · 다시 눌러 주세요` · 모달 유지 · DB E는 이미 풀림 · `cert_unlock` 1줄 → `page.unroute` 뒤 1차 다시 → 토스트 · E `미제출` · 두 요청 본문의 `lockStamp`가 같음(`page.on("request")`로 잡음) · DB `cert_unlock` 여전히 1줄(재전송이 두 번 기록하지 않음)
  - 행동 셀 키보드(UI-SPEC:493 · Codex r3 C-1): 누적 잠김 줄 G가 있는 행사를 PM이 I3로 연다 → G의 「잠금 풀기」 요소가 `tabindex="-1"` → 표 안 첫 셀에 포커스를 둔 뒤 `Tab` 한 번 → 포커스가 표 밖 요소로 나간다(표 안 탭 정지 1개 — 행동 셀 안 버튼에 닿지 않음) → `Shift+Tab`으로 표로 돌아와 방향키로 G 행동 셀(`td`)로 → `Enter` → 확인 부품 열림 · 포커스 = 1차 → `Esc` → `document.activeElement`가 G 행동 셀 → `Space` → 다시 열림 · 잠금 풀기 요청 0개(`Space`를 뗄 때 1차가 눌리지 않는다) → 2차 「취소 Esc」 → 포커스가 G 행동 셀 → **모달 `Enter` 한 번(E3-20):** 셀에서 `Enter`로 다시 열고 포커스가 1차인 채 `Enter` → 잠금 풀기 액션 요청이 정확히 1개(`page.on("request")`로 `Next-Action` 헤더 요청을 센다) · 토스트 `잠금 풀기 · {G 이름}` 뒤 `page.getByRole("dialog")`가 0개(모달이 다시 열리지 않는다) · 포커스가 G 행동 셀. 시스템 관리자로 제출된 줄 H(⓪ 표본 규칙)의 행동 셀에 방향키로 가서 `Enter` → I4 제목(「제출 내용」도 셀에서 누른다)
  - 닫힌 행사의 누적 잠김(checker-C W1): 누적 잠김 줄 J(스펙이 `db/client`로 네 칸을 채움)가 있는 행사를 PM이 I3로 열어 **닫기 전에** J 제출 칸이 `잠김` + `잠금 풀기 필요`임을 먼저 본다(이 긍정 단언이 있어 구현 전 RED에서 이 케이스도 빨갛다) → domain으로 닫음 → PM이 I3 → 읽기 표에서 J 제출 칸이 `미제출`이고 그 줄에 `잠김` · `잠금 풀기 필요` 글자와 「잠금 풀기」가 없다
  - 좁은 PC 1000(/plan-design-review D-10 · DR-36): `page.setViewportSize({ width: 1000, height: 800 })` 뒤 PM이 누적 잠김 줄 K(스펙이 `db/client`로 네 칸을 채움)가 든 접수 중 자기 행사 I3를 연다 → 표가 읽기 표로 선 뒤(수화 뒤 한 번 바뀐다 — 편집 셀이 사라질 때까지 기다린다) 셀을 눌러도 · `Enter`를 눌러도 편집 입력이 열리지 않음 · 1차 「일괄 저장」 없음 · 힌트 줄(`이동 Tab`) 없음 · 머리 「링크 닫기」 있음 · K 행동 열 「잠금 풀기」가 있고 `tabindex="-1"`이 아니다(읽기 표의 보통 `Tab` 대상) → 누르면 `getByRole("dialog", { name: "잠금 풀기" })` 열림 → `Esc` → 닫힘 · 표 컨테이너와 문서 `scrollWidth <= clientWidth` → **고친 뒤 좁아짐(N-4):** 뷰포트를 1280으로 되돌려 미제출 줄 경품명 한 칸을 고치고 폭을 1000으로 줄이면 표는 읽기 표(편집 입력 없음)이고 1차 「일괄 저장 1」이 있다 → 누르면 `저장됨 1줄 HH:mm`(SYSTEM §7-3 「`N ≥ 1`이면 항상 렌더」) → 케이스 끝에서 뷰포트를 원래 크기로 되돌린다
  - 보기만 가능한 사람: 스펙이 고유 이름의 새 계급을 `createRole`(`domain/permissions/roles.ts`)로 만들고 `upsertPermission`(`repositories/permissions.ts`)으로 `certs.events` 보기 · `certs.submissions` 보기만 켜고(`certs.events` 쓰기 없음 — 범위 판정을 통과하되 쓰기는 없는 사람, 공유 계급은 건드리지 않는다), `upsertVisibility`(같은 파일)로 정보 항목 `cert_event.value` · `cert_winner.value`를 켠 뒤(codex final C5 — `createRole`은 노출표 행을 만들지 않고 `visible()`은 행이 없으면 거부하므로, 켜지 않으면 04.3-04 투영이 행사 · 당첨자 값을 걸러 잠김 줄 단언이 성립하지 않는다) `createFixtureUser({roleId})`로 로그인해 같은 행사 I3 → 읽기 표 · 누적 잠김 줄에 `잠김` + `잠금 풀기 필요`는 보이고 「잠금 풀기」는 없다
  - 직접 POST(C1 · codex #2): 게이트가 켜진 동안 「일괄 저장」 · 1차 「링크 닫기」 · 1차 「잠금 풀기」 요청의 URL · `Next-Action` 헤더 · 본문을 `page.on("request")`로 잡아 둔다(닫기는 닫히기 전 새 행사에서, 잠금 풀기는 누적 잠김 줄에서 — 둘 다 요청만 잡고 `page.route`로 서버에 닿기 전에 끊어 실제로 닫히거나 풀리지 않게) → `withCertFeatureOff` 안에서 `page.request.post`로 세 요청을 그대로 다시 보낸다 → DB의 그 행사 당첨자 값 · `version` · 네 잠금 칸 · `closed_at` · 행동 로그 줄 수(`cert_unlock` 포함)가 다시 보내기 전과 같다(도우미가 끝에서 다시 켠다)
② **`test/e2e/mobile-cert-events.spec.ts`(폰 375):** 시스템 관리자로 `seedSubmittedCert({ extraWinners })` 행사(제출된 줄 + 미제출 줄)의 I3 → 표가 칸 접기(P2 접힌 줄에 구별 표시 붙음) · 셀 편집 없음 · 표 컨테이너와 문서 모두 `scrollWidth <= clientWidth` · 제출된 줄 탭 → 시트 본문 + 「제출 내용」 링크 → I4 · 미제출 줄 탭 → 시트에 링크 없음 · 「링크 닫기」 → 시트 모양 확인(§7-17 폰) · 시트 머리 × 의 접근 이름이 부품 그대로 `닫기` → × 로 닫힘 · 포커스가 「링크 닫기」 버튼. 잠금 풀기 시트(UI-SPEC I3 폰 불릿 순서): 스펙이 `db/client`로 미제출 줄 F를 누적 잠김으로 만든다 → F 탭 → `RowSheet` 본문 아래 「잠금 풀기」 → 누르면 `RowSheet`가 먼저 닫히고(시트 제목 요소 사라짐) 확인 시트가 열린다(시트 모양 · 포커스 = 1차) → × (`닫기`) → 확인 시트 닫힘 · 포커스가 F 줄(접힌 줄 셀) → 다시 F 탭 → 「잠금 풀기」 → 1차 → 토스트 `잠금 풀기 · {F 이름}` · F 제출 칸 `미제출` · 포커스가 F 줄 · 문서 `scrollWidth <= clientWidth`.
③ 스펙에 바뀐 것이 있으면 커밋 `test(04.3): event detail editing and link close E2E`(없으면 커밋 없이 녹색 실행 결과만 SUMMARY에).
  </action>
  <verify>
    <automated>pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-events-edit.spec.ts test/e2e/mobile-cert-events.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 편집 중 제출 · 편집 중 닫힘 · 닫기 실패 · 모두 제출 · 권한 · 충돌 · 직접 POST(게이트 꺼짐 — 저장 · 닫기 · 잠금 풀기 세 요청) 케이스가 데스크톱 스펙에 있다
    - 요청 너무 큼 케이스가 데스크톱 스펙에 있다: 500줄 × 경품명 한글 80자 → 기존 너무 큼 문장 · 저장 액션 요청 0개 · 고친 값 유지 · DB 그대로 → 일부만 남겨 저장 성공(`grep -n "256KB 한도" test/e2e/cert-events-edit.spec.ts`, codex final3 C1)
    - 행동 셀 키보드 케이스가 데스크톱 스펙에 있다: 셀 안 3차 `tabindex="-1"` · 표 안 탭 정지 1개(`Tab` 한 번에 표 밖) · 방향키로 행동 셀 → `Enter`/`Space`로 확인 열림 · `Esc` · 「취소 Esc」 뒤 `document.activeElement` = 그 행동 셀 · 「제출 내용」 셀 `Enter` → I4
    - 닫힌 행사의 누적 잠김 줄이 `미제출`이고 `잠금 풀기 필요` · 「잠금 풀기」가 없는 케이스가 데스크톱 스펙에 있다(checker-C W1)
    - 이름 · 전화번호 교차 충돌에서 고정된 셀이 전화번호 하나이고 해결 뒤 저장이 되는 케이스(codex final C3), 이름만 고친 줄이 남의 전화번호 변경과 충돌 없이 합쳐지고 요청에 `phone` 키가 없는 케이스(codex final2 C)와, 보기 전용 임시 계급에 `cert_event.value` · `cert_winner.value` 노출을 켜는 준비(`grep -n "upsertVisibility" test/e2e/cert-events-edit.spec.ts`, codex final C5)가 데스크톱 스펙에 있다
    - 잠금 풀기 케이스가 데스크톱 스펙에 있다: 누적 잠김 줄만 버튼 · 짧은 잠김 줄 버튼 없음 · 확인 문구 · `Esc` 뒤 포커스 복귀(그 줄 행동 셀) · 성공 토스트 · 포커스 = 그 줄 행동 셀 · 미저장 편집 유지 뒤 「일괄 저장」 충돌 없음 · 결과 불명 문장 → 같은 `lockStamp` 재전송 → `cert_unlock` 1줄 · 보기만 가능한 사람에게 버튼 없음
    - 폰 스펙이 잠금 풀기 순서(`RowSheet` 먼저 닫힘 → 확인 시트 → 닫히면 포커스 = 그 줄)를 단언한다
    - 확인 모달 단언이 `page.getByRole("dialog")` 안으로 좁혀져 있고, 2차가 `취소`(부품 파생)이며 폰 시트의 × 가 부품 그대로 `닫기`임을 폰 스펙이 단언한다(E3-01 — 이 플랜이 부품의 이름을 바꾸지 않는다) — × · 1차 단언은 `getByRole("button", { name: "닫기", exact: true })` · `{ name: "링크 닫기", exact: true }`로 정확 일치(부분 일치면 × 셀렉터가 1차를 잡는다, /plan-design-review D-5)
    - `test/e2e/cert-events-edit.spec.ts`에 「전제: 이 권한은 시드 기본값이 아니라 cert.setup이 켠 것이다」 문구가 있고 이 스펙이 `role-sysadmin`의 `certs.submissions`를 켜지 않는다(`! grep -n 'roleId: "role-sysadmin"' test/e2e/cert-events-edit.spec.ts` — ⓪-c 임시 계급의 `upsertPermission`은 별도 역할이라 걸리지 않는다, E3-13)
    - 두 스펙에서 이름이 `닫기` · `링크 닫기`인 버튼 셀렉터가 전부 `exact: true`다 — prettier가 옵션 객체를 여러 줄로 나눠도 맞게 공백을 접어서 본다(반영 검사 r1 N-6): `cat test/e2e/cert-events-edit.spec.ts test/e2e/mobile-cert-events.spec.ts | tr -s '[:space:]' ' ' | grep -oE 'getByRole\( ?"button", ?\{[^}]*\}' | grep -E 'name: "(링크 )?닫기"'`의 출력이 1줄 이상이고, 같은 파이프 끝에 `| grep -vc 'exact: true'`를 붙인 출력이 `0`이다(D-5). 버튼 셀렉터 묶음만 뽑아 본다 — 모달 셀렉터 `getByRole("dialog", { name: "링크 닫기" })`는 세지 않고, 키 순서가 `{ exact: true, name: … }`여도 맞게 본다(교차 r2 R2-1). 실패 조건: 묶음 출력이 0줄이거나 뒤 숫자가 1 이상이다(견본 확인: 올바른 표본 — 모달 셀렉터 · 두 키 순서 · prettier 줄 나눔 — 묶음 4줄 · `0`, 틀린 표본 — `exact` 없는 버튼 둘 — `2`)
    - 「모양 중복」 케이스가 오류가 남은 채 「일괄 저장」을 누르면 저장 액션 요청 0개 · 포커스가 그 오류 셀임을 단언한다(SYSTEM §7-3 DR-5 · 반영 검사 r1 B-4와 같은 규칙)
    - I3 편집 0칸 막힘 이유를 데스크톱 스펙이 정확 일치로 단언한다: `grep -n '바뀐 칸 없음' test/e2e/cert-events-edit.spec.ts`가 1줄 이상이고 그 단언이 `toHaveText`(전체 일치)이며 같은 요소의 `color`가 `rgb(78, 93, 89)`다(UI-SPEC Copywriting 「I3 1차 「일괄 저장」 막힘(편집 0칸)」 · 반영 검사 r1 W1)
    - 잠금 풀기 확인 문구 단언이 UI-SPEC 정본이다: `grep -n "틀린 횟수를 모두 지웁니다" test/e2e/cert-events-edit.spec.ts`가 1줄 이상이고 결과 줄을 한 줄 전체 일치로 단언한다 · 부제는 그 줄 이름과 전체 일치다(D-9 · X-5)
    - 좁은 PC 1000 케이스(D-10)가 데스크톱 스펙에 있다: 편집 입력 없음 · 편집 0칸이면 「일괄 저장」 없음 · 1280에서 고친 뒤 1000으로 줄이면 「일괄 저장 1」이 남고 저장된다(N-4 · SYSTEM §7-3) · 힌트 줄 없음 · 「링크 닫기」 있음 · 「잠금 풀기」가 `tabindex="-1"` 아님 · 확인 부품 열림 · `scrollWidth <= clientWidth`(`grep -n "width: 1000" test/e2e/cert-events-edit.spec.ts`)
    - 모달 1차 `Enter` → 잠금 풀기 요청 정확히 1개 · 모달이 다시 열리지 않음 · `Space`로 연 뒤 요청 0개 케이스가 데스크톱 스펙에 있다(E3-20)
    - 제출된 줄 표본이 `seedSubmittedCert`로 만들어지고(`grep -n "seedSubmittedCert" test/e2e/cert-events-edit.spec.ts test/e2e/mobile-cert-events.spec.ts` — 두 파일) E4 폼을 조작하지 않는다(`! grep -nE "주민등록번호 앞 6자리|서명 칸" test/e2e/cert-events-edit.spec.ts test/e2e/mobile-cert-events.spec.ts`, E3-32) · 도중 제출이 도우미 옵션이 아니면 SUMMARY에 편차 한 줄이 있다
    - 두 스펙에 `⌘` · `Meta+`가 없다(`! grep -nE "⌘|Meta\+" test/e2e/cert-events-edit.spec.ts test/e2e/mobile-cert-events.spec.ts`, D-94)
    - `구별 표시` 편집 입력의 `aria-describedby` = 상시 힌트 id를 데스크톱 스펙이 단언한다
    - 폰 스펙이 `scrollWidth <= clientWidth`를 표 컨테이너와 문서 둘 다 단언한다
    - `test/e2e/helpers/cert.ts`가 이 플랜의 diff에 없고, 두 스펙에 `mode: "serial"`이 있으며 공유 설정을 직접 바꾸지 않는다(`! grep -n "setSettingValue" test/e2e/cert-events-edit.spec.ts test/e2e/mobile-cert-events.spec.ts`)
  </acceptance_criteria>
  <done>I3 편집·닫기·잠금 풀기의 모든 경합 경로(결과 불명 재전송 · 게이트 꺼짐 직접 POST 포함)가 브라우저(데스크톱 · 폰 시트)에서 UI-SPEC 문구 그대로 보인다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 담당자 브라우저 → 저장 액션 | 줄 id · 버전 · 셀 값은 믿지 않는다 — 서버가 행사 범위 · 제출 여부 · 버전 · 셀 규칙을 다시 판정한다 |
| 담당자 편집 ↔ 수령자 제출 | 같은 자리를 두 쪽이 동시에 바꿀 수 있다 — 행사 행 잠금과 조건부 갱신이 한쪽만 이기게 한다 |
| 담당자 브라우저 → 잠금 풀기 액션 | 줄 id · 잠금 표시는 믿지 않는다 — 서버가 권한 · 범위 · 접수 중 · 미제출 · 같은 표시의 누적 잠김을 다시 판정한다. 풀기 ↔ 수령자 틀림도 행사 → 줄 잠금 순서로 한쪽씩 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-21 | Tampering | 제출된 확인증의 사후 변경 | high | mitigate | 담당자 저장은 제출된 줄이 섞이면 배치 전체를 거부하고, 갱신 where 절에 `submitted_at IS NULL`을 건다. 제출 내용 정정은 경영관리 화면(04.3-07)의 기록되는 경로 하나뿐이다 |
| T-04.3-48 | Elevation of Privilege | 남의 행사 편집 · 닫기 | medium | mitigate | `can(certs.events, write)` + 상세와 같은 범위 판정(만든 사람 또는 `certs.submissions` 보기). 통합 테스트 권한 셋 케이스 |
| T-04.3-49 | Spoofing | 고친 이름·전화번호로 남의 자리 제출 | high | mitigate | 이름·전화번호를 고치면 확인 증표와 확인 멱등 기록의 맞음 항목을 지운다 — 고치기 전 확인자는 다시 뒤 4자리를 맞혀야 한다(통합 테스트) |
| T-04.3-50 | Repudiation | 링크 닫기 · 당첨자 편집 | low | mitigate | 닫기는 끌 수 없는 `status_change`를 닫힘 UPDATE와 같은 tx에서 `recordAction(…, {tx})`로 `closed_by`와 함께(C6 · E3-02 — 로그 실패 주입 시 닫힘 롤백 통합 케이스), 편집은 `document_update`에 줄 수만 남긴다(개인정보 없음). 편집 로그는 설정으로 끌 수 있다 — 수령자 개인정보 정정(끌 수 없음)과 다른 층위라 수용하고 설계 `/cso`에 적는다 |
| T-04.3-51 | Information Disclosure | 「제출 내용」 링크 · submissionId | low | mitigate | 서버가 `certs.submissions` 보기 권한자(대표 계급 제외)에게만 DTO에 싣는다. 링크를 알아도 I4 자체가 게이트·대표 차단·권한·비활동 세션을 다시 판정한다(04.3-07) |
| T-04.3-16 | Elevation of Privilege | 게이트 꺼진 동안 편집 · 닫기 · 잠금 풀기 액션 직접 POST | high | mitigate | 세 액션의 첫 줄이 `assertCertFeatureEnabled()`(C1), domain 세 함수(`saveWinners` · `closeEvent` · `unlockWinner`)는 맨 앞에서 `isCertFeatureEnabled()`. 통합(게이트 꺼짐에서 쓰기 0 — 셈 · 잠금 칸 · 로그 포함) + E2E(잡아 둔 세 액션 요청을 게이트 꺼짐에서 다시 보내기) |
| T-04.3-125 | Elevation of Privilege | `unlockCertWinnerAction`(남의 행사 · 쓰기 없는 사람 · 다른 행사의 줄 id) | high | mitigate | `can(viewer, "certs.events", "write")`(없으면 `forbidden`) + `visible(viewer, "cert_winner.value")`(없으면 `forbidden` — `saveWinners` · `unlockable`과 같은 조건, E3-14) + `saveWinners`와 같은 범위 판정(밖이면 `notFound`) + 줄 조회 where에 `event_id`를 함께 — 다른 행사의 줄 id는 `notFound`. 화면은 서버가 정한 `unlockable`만 보고 버튼을 그린다(보기만 가능한 사람 · 닫힌 행사 · 짧은 잠김 줄에는 버튼이 없다). 통합 거부 케이스(항목 꺼짐 viewer 포함) + E2E 보기 전용 계급 |
| T-04.3-126 | Tampering | 옛 잠금 표시의 재전송 · 동시 요청으로 새 틀림 · 새 누적 잠김 지우기 | high | mitigate | 요청이 잠금 표시(`hard_locked_at`, 밀리초)를 싣고 조건부 되돌리기 where가 같은 표시일 때만 맞는다. 잠금 표시는 zod `datetime({ offset: false, precision: 3 })` 한 모양만 받아 로그 `detail->>'lockStamp'` 문자열 비교가 표기 차이로 어긋나지 않는다(E3-23 — 어긋나면 적용된 풀기의 재전송이 `unlocked` 대신 `stale`이 된다). 적용된 풀기는 같은 tx의 `cert_unlock` 로그로 판별해 재전송은 쓰기 없이 같은 성공(`findCertUnlockLogTx`) — 풀린 뒤 쌓인 틀림을 지우지 않는다. 행사 → 줄 `FOR UPDATE` 순서가 `verifyLast4` · 제출과 같아 교착 없이 줄 세운다. 통합(1ms 다른 표시 → `stale` · 재전송 뒤 셈 1 보존 · `Promise.all` 둘 → 로그 1줄) + E2E 결과 불명 재전송 |
| T-04.3-127 | Repudiation | 잠금 풀기 기록 누락 · 이중 기록 | medium | mitigate | 끌 수 없는 새 종류 `cert_unlock`(`record.ts` 세 배열 · `ALWAYS_ON_ACTION_TYPES`)을 되돌리기와 **같은 tx**에서 Phase 4 `recordAction(…, {tx})`로 쓴다(C6 · E3-02) — 로그가 실패하면 되돌리기도 롤백되고, 재전송은 로그를 더 쓰지 않는다. 통합(로그 실패 주입 → 네 칸 그대로 · 0줄, 재전송 → 1줄) + 단위 `record.test.ts` |
| T-04.3-128 | Tampering | 풀기가 줄 `version` · `updated_by`를 바꿔 담당자 저장 충돌 판정을 흐리거나 편집자 표시를 바꿈 | medium | mitigate | 되돌리기 set 절은 `failed_attempts` · `locked_until` · `cumulative_failed_attempts` · `hard_locked_at` 넷뿐이다(`version` · `updated_by` · `updated_at` 없음 — SUMMARY에 set 절 인용). 통합(풀기 전 `version`으로 저장 → `saved`) + E2E(같은 줄 미저장 편집이 풀기 뒤 「일괄 저장」으로 저장됨) |
| T-04.3-129 | Information Disclosure | `cert_unlock` 로그 detail · 오류 · 응답 DTO | low | mitigate | 로그 detail은 `{eventId, lockStamp}`뿐(이름 · 전화번호 · 뒤 4자리 없음 — 통합 테스트가 detail 키를 정확히 비교), 오류 메시지에 개인정보를 넣지 않는다. 응답 `row`는 기존 `CertWinnerDto`(`DtoSpec` · 정보 항목 `cert_winner.value`)로만 나가고 누수 스캔이 이 모듈을 이미 훑는다 |
| T-04.3-152 | Information Disclosure | `saveWinners`의 `conflict` · `submittedRows` 응답(투영 밖 서버 값) | high | mitigate | 저장 진입에서 `visible(viewer, "cert_winner.value")`가 거짓이면 `forbidden` — 쓰기 권한만 있고 당첨자 값을 못 보는 직원이 옛 버전으로 충돌을 일으켜 이름 · 전화번호를 받는 길을 막는다(견적 저장 368행 선례). `editable`도 같은 조건. 항목 꺼짐 viewer 통합 테스트(결과 문자열에 이름 · 전화 없음, codex final C2) |
| T-04.3-153 | Tampering | 담당자 저장의 충돌 판정(최초 조회값 없음) | medium | mitigate | 입력 줄이 고친 칸과 그 칸의 `baseline`(처음 읽은 값)을 싣고 서버가 보낸 칸 가운데 `baseline` ≠ 지금 값인 칸만 충돌로 돌려준다 — 내가 고친 칸의 오판 · 남이 바꾼 칸의 무심한 덮어쓰기를 막는다. 보낸 칸에 `baseline`이 없으면 그 칸 충돌. 견줌은 정규형, 충돌 값은 화면형이다(E3-17 — 표기 차이로 인한 거짓 충돌 · 충돌 문장의 저장형 노출 없음). 통합(이름 · 전화 교차 · 내 전화 × 남의 경품명 · 전달만) + E2E(두 컨텍스트, codex final C3) |
| T-04.3-171 | Denial of Service | 「일괄 저장」 요청 크기(262,144 B 미들웨어 한도) | medium | mitigate | 요청이 바뀐 줄 · 고친 칸 · 그 칸의 `baseline`만 싣는다(여섯 칸 `baseline` 전부를 싣던 모양은 유효한 500줄이 377,559 B로 한도를 넘었다). 서버 검사를 통과할 수 있는 최대 새 줄 500개 244,559 B · 한 칸씩 고친 500줄(경품명은 새 값 · `baseline` 둘 다 69자까지)이 한도 안임을 단위 경계 테스트가 고정한다. 넘는 요청은 화면이 서버와 같은 `checkPayloadSize`로 보내기 전에 막고 기존 문장을 세운다(결과 불명으로 오인하지 않음) · 저장을 나누지 않는다. 액션별 한도는 공용 진입점(`lib/actions/client.ts:27`)에 통로가 없어 쓰지 않는다(codex final2 C) |
| T-04.3-172 | Tampering | 칸 단위 요청의 병합(보내지 않은 칸) | medium | mitigate | 서버가 행사 행 `FOR UPDATE` 아래에서 읽은 지금 값을 화면형으로 바꿔(E3-17) 보내지 않은 칸을 채워 병합한 줄 전체로 `validateWinnerRows`(모양 중복 · 구별 표시 규칙)를 돌리고, 갱신 set에는 보낸 칸만 넣는다 — 남이 바꾼 칸을 덮지 않고 병합 결과가 규칙을 어기면 전부 거부. zod가 새 줄의 여섯 칸 필수 · 저장된 줄의 `baseline` 키 = 보낸 칸 키를 강제한다. 요청의 줄 id가 그 행사의 저장된 줄이 아니면 `notFound`. 통합(이름만 보낸 줄 병합 · 병합 뒤 모양 중복) |
| T-04.3-181 | Tampering | 일괄 저장의 두 줄 맞교환 · 커밋 때 최종 중복(당첨자 UNIQUE 지연) | medium | mitigate | `saveWinners` tx만 `deferWinnerPersonUniqueTx`로 `cert_winners_event_id_name_phone_unique`를 커밋 때 검사로 미룬다 — 맞교환은 통과하고, 최종 중복은 4의 `duplicatePerson` 검사가 먼저 막고 그래도 남으면 COMMIT의 23505를 같은 `invalid`/`duplicatePerson`으로 돌려준다(쓰기 0 · 로그 0). 판정은 SQLSTATE · 제약 이름만 보고 pg `detail`(이름 · 전화번호)은 읽지 않는다(C2). 통합 테스트: 맞교환 `saved` · 검사 단계 · 커밋 단계(`deps.validate` 주입) 거부 |
| T-04.3-182 | Denial of Service | 한도를 넘는 유효한 일괄 저장(500줄 × 새 값이나 원래 값이 한글 약 60자 넘는 경품명 고침) | low | accept | 받아들인 한계(codex final3 C1 — 오케스트레이터 결정): 보장 범위는 최대 길이 새 줄 500개(244,559 B)와 한 칸씩 고친 500줄(경품명은 새 값 · 원래 값(`baseline`) 모두 한글 약 60자까지, 최악 모양 둘 다 69자 262,059 B — 크기는 두 값 글자 수의 합으로 정해진다)이다. 그보다 큰 저장은 화면이 서버 미들웨어와 같은 `checkPayloadSize`로 보내기 전에 거부하고 기존 문장을 세우며 고친 값은 남는다 — 사용자가 나눠서 저장한다. 원자 저장 · 공용 262,144 B 한도 · 액션별 한도 없음은 그대로. 단위 경계(60 · 69 안 · 70 · 80 밖)와 E2E 거부 경로(원래 80자 → 새 80자 500줄 290,559 B · 요청 0개 · 값 유지 · 나눠 저장 성공)가 고정한다 |
| T-04.3-193 | Denial of Service | `saveWinners` 500줄 저장이 행사 행 `FOR UPDATE`를 쥔 채 줄마다 UPDATE · 행사당 누적 당첨자 수 상한 없음 | low | mitigate | A2 규모(행사당 수~수십 명)에서는 저장 동안 현장 확인 · 제출이 몇 초 기다리는 정도라 잠금 시간 한도 자체는 이번 페이즈에서 받아들인다. 설계 `/cso`가 누적 상한 쪽을 확정 — ④ 4-b가 병합 뒤 총원(파기되지 않은 저장된 줄 + 새 줄)이 500을 넘으면 `{kind: "invalid", cellErrors}`(`tooManyWinners`)로 거부해 무제한 증식을 막는다(04.3-04 생성 시 500줄 상한과 같은 수). 경계 통합 테스트(499+1 `saved` · 500+1 `invalid`)가 고정한다(E3-35) |
| T-04.3-154 | Spoofing | 담당자 편집으로 틀림 셈이 지워져 뒤 4자리 추측 속도 제한이 풀림 | medium | mitigate | 이름 · 전화 편집은 `verify_idem_outcome`의 `ok` 항목만 지우고 `wrong` 항목은 남긴다(04.3-03 상한도 `wrong`은 나이로만 거른다). 편집 뒤 `countRecentMisses` 보존 통합 테스트(B1) |
| T-04.3-52 | Information Disclosure | 구별 표시로 새는 개인정보(I3 새 줄) | medium | mitigate | I2와 같은 서버 규칙(10자 · 숫자 3개 이상 이어짐 · 그 행사 당첨자 이름 — 제출된 줄 포함)과 상시 힌트 · 공개 모양 미리 보기. 서버가 알아볼 수 없는 별명·소속은 A11대로 설계 `/cso`가 본다 |
</threat_model>

<verification>
- 단위 `test/unit/action-log/record.test.ts`(`cert_unlock` 끌 수 없는 종류 · 라벨) · `test/unit/certs/unlock-outcome.test.ts`(결과 불명 다섯 갈래) · `test/unit/certs/save-payload.test.ts`(칸 단위 요청 · 500줄 경계 · 넘는 요청 차단 문장) · `test/unit/ui/grid-action-cell.test.ts`(행동 셀 Enter/Space) · Phase 4 `test/unit/ui/conflict-focus.test.ts` · `test/unit/ui/confirm-dialog.test.ts`(막지 않는 실패 줄) · `design-system-docs` · `system-md-compliance` 녹색
- 통합 `cert-events-edit`(누적 잠김 끝까지 · 재전송 · 동시 · 표시 불일치 · 버전 불변 · 로그 실패 롤백 · 두 줄 맞교환 `saved` · 최종 중복 검사/커밋 단계 거부 · 두 표기(E3-17) · 풀기 항목 꺼짐 거부(E3-14) · 잠금 표시 한 모양(E3-23) 포함) · `cert-events` · `cert-submit` · `cert-verify` · `leak-scan` 녹색
- E2E `cert-events-edit`(잠금 풀기 · 결과 불명 재전송 · 모달 `Enter` 한 번 = 요청 한 번(E3-20) · 게이트 꺼짐 직접 POST 세 요청 · 요청 너무 큼 거부 경로 · 좁은 PC 1000 읽기 표(D-10) · × · 1차 `exact: true` 셀렉터(D-5) 포함) · `mobile-cert-events`(잠금 풀기 시트 순서 포함) · `cert-events` · `quote-table` 녹색
- `pnpm lint && pnpm typecheck` 녹색 · `app/(app)/certs/events/[id]` 안에 `<dialog`가 없고 두 확인이 Phase 4 `ui/confirm-dialog`를 쓴다 · 공용 부품 커밋(⓪-b · ④-0)이 화면 커밋보다 먼저다(E3-06) · 통합 · E2E는 한 번에 하나(세션당 플랜 하나, E3-33)
</verification>

<success_criteria>
- 미제출 줄 편집 · 새 줄 · 일괄 저장이 전부 저장 또는 전부 거부로 동작한다 — 요청은 바뀐 줄 · 고친 칸 · 그 칸의 `baseline`만 싣고, 최대 길이 새 줄 500개와 한 칸씩 고친 500줄(경품명은 새 값 · 원래 값 모두 한글 약 60자까지)이 요청 한도 안이며, 그보다 큰 저장은 받아들인 한계로 보내기 전에 기존 문장으로 막히고 고친 값은 남는다(단위 경계 + E2E 거부 경로)
- 편집 중 제출 · 닫힘 · 다른 직원 수정이 UI-SPEC 문구대로 거부된다
- 두 줄의 이름 · 전화번호 맞교환은 저장되고, 최종 중복은 커밋 때 제약까지 포함해 기존 `duplicatePerson`으로 거부된다
- 링크 닫기가 확인 모달 → 토스트 → 닫힘 모양으로 가고 다시 열 수 없으며, 닫힘과 `status_change`가 한 트랜잭션이다
- 누적 잠김 줄은 `잠김` + `잠금 풀기 필요`이고 접수 중 · 쓰기 권한자에게만 「잠금 풀기」가 선다 — 짧은 잠김 줄 · 보기 전용 · 닫힌 행사에는 없다
- 「잠금 풀기」는 「링크 닫기」와 같은 확인 부품으로 열리고, 같은 잠금 표시일 때만 네 칸을 되돌리며 `cert_unlock`을 같은 트랜잭션에서 정확히 한 번 쓴다 — 결과 불명 뒤 재전송은 같은 성공 · 로그 1줄 · 새 틀림 보존이고, 줄 `version`이 그대로라 미저장 편집도 저장된다
- 풀기 뒤 그 줄은 응답의 서버 지금 상태로 다시 그려진다(성공만 보고 잠김을 지우지 않는다) · 포커스는 PC 행동 셀 / 폰 그 줄
- 「제출 내용」은 정정 화면을 열 수 있는 사람(대표 제외)에게만 보인다(PC · 폰 시트)
- 게이트가 꺼지면 편집 · 닫기 · 잠금 풀기 액션 직접 POST도 아무것도 바꾸지 않는다
- 두 확인은 Phase 4 `ui/confirm-dialog` 하나를 표 밖에서 쓰고(모달 1차 `Enter` = 요청 한 번, E3-20), 실패 · 결과 불명 줄 뒤 1차를 다시 누를 수 있으며, 구별 표시 편집 입력이 상시 힌트를 `aria-describedby`로 가리킨다
- 칸 단위 저장이 화면형 · 저장형 표기 차이로 거짓 충돌 · 형식 오류를 내지 않고 충돌 값은 화면형으로 보인다(E3-17)
- I3 편집 표의 행동 셀은 §7-3 (아) 로빙 셀 하나다 — 안의 3차 `tabindex="-1"`, 셀에서 `Enter`/`Space` = 누름, 확인이 닫히면 포커스 = 그 셀. DECISIONS 항목과 SYSTEM (아) 한 줄이 공용 표 코드보다 먼저 섰다
- 닫힌 행사의 누적 잠김 줄은 `미제출`로 보이고 풀 방법 없는 `잠금 풀기 필요`가 남지 않는다
- 700~1023에서는 편집 가능한 행사도 읽기 표(편집 · 새 줄 · 힌트 줄 없음 · 「일괄 저장 N」은 N ≥ 1일 때만 — SYSTEM §7-3)이고 「링크 닫기」 · 행동 열 3차는 남는다 — 폭 판정은 Phase 4 것 하나(D-10 · DR-36)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| domain 함수 | `saveWinners` · `closeEvent` · `unlockWinner`(`domain/certs/events.ts`) · 상세 DTO `submissionId` · `hardLockedAt` · `unlockable` · `editable` |
| 리포지토리 | `repositories/cert-winners.ts` 잠금 풀기 둘(줄 `FOR UPDATE` · 조건부 되돌리기) · `findCertUnlockLogTx`(action_log 읽기 — 로그 쓰기는 `recordAction(…, {tx})`, E3-02) |
| 액션 | `saveCertWinnersAction` · `closeCertEventAction` · `unlockCertWinnerAction` |
| 화면 부품 | `winners-section.tsx`(표 밖 `ConfirmDialog` 하나) · `close-link-dialog.tsx` · `unlock-outcome.ts`(순수) · `save-payload.ts`(순수 — 칸 단위 저장 요청 · 보내기 전 한도) · `winner-table-parts.tsx`(I2 · I3 공용) |
| 공용 UI | Phase 4 `ConfirmDialog` 선택 `primary.failure`(다시 누를 수 있는 실패 줄) · `RowSheet` 선택 prop `action`(개정 ⑫ — 링크 또는 확인 시트를 여는 줄 행동) · 표 열 `actionCell` + `useGridKeyboard`의 `isActionCell` · `onActivateCell` · 순수 `gridEnterAction`(개정 ⑫ §7-3 (아) 행동 셀) |
| 디자인 정본 | `docs/design/DECISIONS.md` §7-3 (아) 행동 셀 · §7-17 실패 줄 항목 · `docs/design/SYSTEM.md` §7-3 (아) · §7-17 한 줄씩 |
| 행동 로그 | `document_update`(당첨자 편집) · `status_change`(링크 닫기, 같은 tx의 `recordAction`) · **새 끌 수 없는 종류 `cert_unlock`**(라벨 `확인증 잠금 풀기`, `record.ts` 세 배열 끝에 추가) |
| 테스트 | `test/unit/action-log/record.test.ts` · `test/unit/certs/unlock-outcome.test.ts` · `test/unit/ui/confirm-dialog.test.ts`(Phase 4 파일에 케이스 둘) · `test/integration/cert-events-edit.test.ts` · `test/e2e/cert-events-edit.spec.ts` · `test/e2e/mobile-cert-events.spec.ts` |
| 후속 | 설계 `/cso` — 행사당 누적 당첨자 상한은 이번 페이즈가 확정·구현했다(500명, `tooManyWinners`, T-04.3-193 mitigate · E3-35). 500줄 저장의 잠금 시간 한도(수치)는 A2 규모라 받아들인 한계로 남긴다. `/plan-design-review`(2026-09-25, 실행 전 확정) — UI-SPEC 단축키 표기 `Ctrl+키` = D-6 · × 이름 겹침 = D-5 부품 그대로 · exact 셀렉터 · ④-0 공용 `ConfirmDialog` `primary.failure` + SYSTEM §7-17 한 줄 — /plan-design-review D-4 확정(`.reason` 색 · `role="alert"`) · 좁은 PC 읽기 표 = D-10 · Codex 재확인(한도 2026-09-29 뒤 — 우선 D-3 · D-7 · D-10) |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(plan-eng-review 2026-09-25) — 반영 지시 L387–455@953e599(공통 L391–394, 04.3-10 항목 L432–440). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-01 Phase 4 머지 뒤 시작 · Phase 4 부품 기준(확인 모달 · 표 키보드 · Ctrl 단축키 · Button) | P1 | objective 「실행 조건」 · Task 1 `<precondition>` · 결정 ⑤(`ui/confirm-dialog`) · Task 2 read_first 줄 번호를 Phase 4 head `0db3ea2` 기준으로 · ②-c `Ctrl+Enter`/`Ctrl+S`(D-94) · 비활성 1차 `aria-disabled` · Task 3 ⓪ 모달 단언 `getByRole("dialog")` 안 · 수용 줄(`⌘` 없음) |
| E3-02 tx 로그 도우미가 둘(07 사본 포함) | P2 | 결정 ① · ②-c(종류 유니온 · 로그 도우미 파일 없음, `findCertUnlockLogTx`는 `repositories/cert-winners.ts`로) · ④ `closeEvent` · `unlockWinner`가 `recordAction(…, {tx, appendActionLog})` · truths · key_links · Task 1 `<automated>`(사본 도우미 없음) · T-04.3-50 · 127 |
| E3-06 ⓪-b 공용 표 변경은 화면과 떼어도 녹색 | P3 | ⓪-b 머리 · 커밋 줄 「⓪-c · ①~⑦보다 먼저」 · Phase 4 `use-grid-keyboard` 171~176행 · `Table.tsx` `handleConflictKey` 먼저 기준으로 다시 씀 · Task 2 `<automated>` `git log` 순서 검사 + 수용 줄 |
| E3-14 `unlockWinner`에 `cert_winner.value` 보임 판정 없음 | P3 | ④ `unlockWinner` 1단계 · behavior 거부 케이스 · Task 1 `<automated>` · 수용 줄 · truths · T-04.3-125 |
| E3-17 표시형 · 저장형을 섞어 비교 · 병합 | P2 | 결정 ⑦ · ④ 4단계(화면형 검사 입력) · 5단계(정규형 견줌 · 충돌 value 화면형) · 6단계(증표 지우기도 정규형) · behavior 두 표기 케이스(내 전화 × 남의 경품명 · 전달만 · 충돌 값 화면형) · 수용 줄 · truths · T-04.3-153 · 172 |
| E3-20 행동 셀 `td` 안 모달의 키가 셀 `onKeyDown`으로 올라감 | P2 | 결정 ⑤ · Task 2 ④-b(표 밖 최상위 하나 · `unlockTarget` state) · ②-h(3차 `onClick`이 셀에 포커스를 둔 뒤 연다) · ②-g(폰 — `RowSheet`가 닫힌 뒤 연다) · Task 3 키보드 케이스 「모달 `Enter` → 요청 1회 · 다시 안 열림」 + `Space` 요청 0 · 수용 줄 |
| E3-23 `lockStamp` 문자열 정규화 없음 | P3 | 결정 ⑥ · ⑤ `lockStampSchema` = `z.string().datetime({ offset: false, precision: 3 })`(zod 4.6.5 실측) · `getEventDetail` `toISOString()` · ②-c · behavior 한 모양 케이스 · 수용 줄 · T-04.3-126 |
| E3-32 E2E 제출 표본 | P2 | Task 1 ① RED · Task 3 read_first · ⓪ 표본 규칙 · 모양 중복 · 편집 중 제출 · 모두 제출(한 명 표본) · 권한 · 키보드 H · 폰 · 수용 줄 |
| E3-33 통합 · E2E는 한 번에 하나 | P3 | objective 「실행 조건」 · verification |
| E3-35 500줄 저장이 행사 행 잠금을 쥠 · 누적 상한 없음 | P3 | T-04.3-190 accept 줄 → 설계 /cso에서 확정 · 산출물 표 후속 |
| E3-24(연쇄 — 04.3-03 소관) `lockEventRow` 인자 순서 | P3 | ④ `unlockWinner` 2단계 · Task 1 read_first를 `(viewer, eventId, tx)`로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| E3-35 누적 당첨자 상한 · 저장 잠금 시간 한도의 수치 | → 설계 /cso에서 확정(수치를 이 플랜이 정하지 않는다). 이번 페이즈는 T-04.3-190 accept |
| (이 수정 중 발견) Phase 4 `ConfirmDialog`에는 1차를 막지 않는 실패 자리가 없다(`disabledReason`은 1차를 `aria-disabled`로 막는다) | Task 2 ④-0이 DECISIONS → SYSTEM §7-17 한 줄 → 부품 `primary.failure` 순서로 더한다(화면 하나만의 예외 금지). 자리 · 색 판단은 `/plan-design-review`에서 다시 본다 |
| (이 수정 중 발견) Phase 4 부품의 × 는 `닫기`(폰 시트에서만 보임)라 옛 계획의 「× = `취소`」가 사라지고, 폰 「링크 닫기」 시트에서 × `닫기`와 1차 `링크 닫기`의 이름이 겹친다 · UI-SPEC의 `⌘` 표기와 Phase 4 SYSTEM의 `Ctrl+키` 표기가 어긋난다 | UI-SPEC은 승인된 계약이라 이 수정에서 바꾸지 않는다 → `/plan-design-review`에 넘김. 이 플랜은 부품 · SYSTEM 그대로 쓴다 |
| (교차 샤드) 04.3-02 C4 `seedSubmittedCert(opts?)`에 기존 행사의 미제출 자리를 제출하는 옵션이 없다 | 영향은 「편집 중 제출」 한 케이스뿐이다. 04.3-02에 옵션 추가를 요청했고, 없으면 Task 3 ⓪ 대비책(도우미와 같은 domain 순서, E4 폼 조작 없음 · SUMMARY 편차)으로 간다 |


### Round 2 — 72829e6

> 출처: 04.3 eng-apply 재검토 r1 — Opus 플랜 체커(`eng-apply-checker-r1`, W/I 번호) · 교차 검토(`eng-apply-opus-cross-r1`, X/A 번호). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W1 · 교차 A3 — frontmatter `execution_gate` 없음 | warning | 01~09와 같은 `execution_gate`(start_after · wave_first_step · serial_tests) 블록을 frontmatter에 그대로 복사했다 |
| 체커 W3 · 교차 A1 — `T-04.3-190`이 06(커밋 불명 보상 삭제)과 겹침 | warning | 이 플랜의 500줄 잠금 DoS 위협을 미사용 번호 `T-04.3-193`으로 바꿨다(threat_model · 산출물 표 후속 · 04.3-13 E3-35 줄). Supersedes Round 1 Addressed/Deferred E3-35 행의 `T-04.3-190` 표기 — 이제 `T-04.3-193` |
| 체커 W2 · 교차 A6 — 500줄 페이로드 표본이 이름 50자 전제(서버 한도 40자, 02 zod · 04 `nameTooLong`) | warning | truth · behavior 경계 줄 · RED 줄 · threat_model의 숫자를 이름 40자로 다시 쟀다: 최대 새 줄 500개 259,559 → 244,559 B · 이름 고친 저장된 줄 500개 200,059 → 170,059 B(`node`로 재측정 — 줄당 한글 10자 × 3 B 차이). 경품명 경계(235,059 · 262,059 · 265,059 · 295,059 B)는 그대로 |
| 체커 I1 · 교차 A2 — tx 로그 부재 정책 갈림 | info | 오케스트레이터 결정(04.3-02 `insertActionLogTx` 대비책 하나로 통일): Task 1 `<precondition>`에서 `tx?: DbOrTx` 멈춤을 빼고 ④에 대비책 문단(`closeEvent` · `unlockWinner` — 같은 tx · 같은 주입 자리, 대비 파일이 없으면 멈춤) · read_first에 `repositories/cert-action-log.ts`(읽기만) · verify 셋째 줄을 「대비 파일 밖 정의 0 · `tx`가 있으면 대비책 import 0」으로 바꿨다. Phase 4 머지 확인은 그대로 멈춘다 |
| 교차 A4 — `record.ts` 「97행 · 131행」 · 「12~75행」, `ConfirmDialog` 「0db3ea2 240~253행」 줄 번호 | advisory | Task 1 read_first는 세 배열 · `RecordActionDeps` · `recordAction`을 이름(`grep -n`)으로, Task 2 read_first · ④-0은 `grep -n 'styles.reason'` · `grep -n 'disabledReason'`으로 찾게 했다 |
| 교차 A5 — ④-0이 Phase 4 공용 `ConfirmDialog` · SYSTEM §7-17에 `primary.failure`를 더함(리뷰 지시를 넘는 새 설계 결정) | advisory | ④-0은 그대로 둔다. → /plan-design-review에서 확정(실행 전) — 산출물 표 후속 줄에 같은 문구로 넘겼다(Round 1 Deferred 「다시 본다」를 이 행이 대신한다). Phase 4 쪽 변경과의 충돌은 04.3-13 ① 머지 규칙의 「양쪽 유지」 목록에 `ui/confirm-dialog/ConfirmDialog.tsx`와 그 단위 테스트를 더해 다룬다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 3 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review 2026-09-25 — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3 · D-7 · D-10). 앞 라운드 표는 고치지 않는다(append-only) — 보고서가 고치라고 한 앞 라운드 처분 칸(L63@1c3a27b 「:615 · :632」, L71@1c3a27b 「:616」)은 아래 Supersedes 행으로 대신한다.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| D-4 ④-0 `ConfirmDialog` `primary.failure` 실행 전 확정(L59–64@1c3a27b) | 경고 | truths 「링크 닫기」 줄(D-4 확정 괄호) · Task 2 ④-0 DECISIONS **왜** 끝 문장(`role="alert"` — §6 로그인 실패 줄 선례, 누르기 전 막힘 이유 §10과 다름) · ④-0 GREEN(`.reason` = `block` · `role="alert"` · 새 클래스 0) · 산출물 표 후속 줄. Supersedes Round 1 Deferred 「Phase 4 `ConfirmDialog`에는 1차를 막지 않는 실패 자리가 없다」의 「자리 · 색 판단은 `/plan-design-review`에서 다시 본다」 → 자리 · 색 = D-4 확정. Supersedes Round 2 Addressed 「교차 A5」의 「→ /plan-design-review에서 확정(실행 전)」 → /plan-design-review D-4 확정 |
| D-5 폰 시트 × `닫기`와 1차 `링크 닫기` 이름 겹침(L66–71@1c3a27b) | 경고 | 부품 · SYSTEM §7-17 그대로(선택지 A). truths 「링크 닫기」 줄 · Task 3 ⓪ 모달 규칙(× · 1차 `getByRole("button", …, exact: true)`) · Task 3 수용 줄 둘(보고서 L70 문구 덧붙임 + 두 스펙 grep) · verification · 산출물 표 후속. Supersedes Round 1 Deferred 「× `닫기` · 이름 겹침 · UI-SPEC 단축키 표기」 → D-5: 부품 · SYSTEM 그대로, 테스트 exact 일치. 단축키 표기 = D-6 |
| D-6 커맨드 기호(U+2318) 표기 → `Ctrl+키` · 힌트 줄 저장 중복(L73–80@1c3a27b) | 경고 | Task 2 ②-c(표기 `Ctrl+키` · 힌트 줄 문구 · DR-31 중복 금지) · Task 2 수용 줄(UI-SPEC 계약 본문에 그 기호 0 — 과거 기록 절 `## Codex 디자인 검토 반영` 뒤는 뺌) · 산출물 표 후속. 두 화면 파일 · 두 스펙의 그 기호 0 단언은 이미 있다(Task 2 · Task 3 수용 줄) |
| D-9 · X-5 「잠금 풀기」 확인 문구가 6차 손질 전 것(L100–103 · L198–200@1c3a27b) | 경고(X-5 막는 문제) | truths 누적 잠김 줄(부제 `{이름}` · 결과 줄 `틀린 횟수를 모두 지웁니다`) · Task 2 ②-h · Task 3 ① 잠금 풀기 E2E 단언 · Task 3 수용 줄(정본 결과 줄 grep · 부제 전체 일치) |
| D-10 700~1023 편집 표(DR-36, L105–108@1c3a27b) | 경고 | truths 새 줄 · Task 2 ②-g0(보고서 L108 문구 + Phase 4 `useEditableWidth`(04-49) · 화면 폴더 새 폭 판정 금지 · 없으면 멈춤) · Task 2 수용 줄(`useEditableWidth` grep · 새 폭 판정 0) · Task 3 ① 좁은 PC 1000 케이스 + 수용 줄 · verification · success_criteria |
| X-4 `reasonTone` 미지정 → 첫 화면 붉은 이유(L194–196@1c3a27b) | 막는 문제(교차) | Task 2 ②-c 「편집 없음 막힘 이유 = `reasonTone: "info"`(`--muted`)」 — 규칙 본문은 UI-SPEC Color 표(shard A)가 정본이고 이 플랜은 그대로 따른다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| D-15 (N3) Task 1 ④ 대비책 문단(`insertActionLogTx`) — Phase 4 `record.ts`에 `tx?: DbOrTx`가 이미 있어 쓰이지 않는 경로(L125–126@1c3a27b) | 보고서 결정 「편집 없음, 기록만」 — 정책 일관성(E3-02 단일 정책)용으로 문단을 그대로 둔다 |
| D-7 비활성 이유의 부정 접두 · D-3 throttled 문장 · D-11 당첨일 기본값 · D-1 · D-2 · D-8 · D-12 | 이 플랜에 해당 문구 · 화면이 없다(I3의 막힘 이유 `제출된 자리 · 고칠 수 없음`은 접두형이 아니다). UI-SPEC · 01~09 쪽은 shard A 몫 |
| X-6 ~ X-12 (참고) | UI-SPEC 쪽(shard A) 항목 — 이 플랜의 태스크 · 문구에 닿는 것이 없다 |

### Round 4 — 66a98e1

> 출처: 디자인 반영 검사 r1 — `design-apply-checker-r1.md`(Opus 플랜 체커: 경고 3) · `design-apply-cross-r1.md`(교차 검토 B-1~B-4 · N-1~N-14). **교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요.** 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W1 · 교차 N-3 — ②-c가 I3 편집 0칸 이유를 `info`로만 적고 문장 `바뀐 칸 없음`을 인용하지 않음 · 단언 없음 | 경고 | Task 2 ②-c가 UI-SPEC Copywriting 「I3 1차 「일괄 저장」 막힘(편집 0칸)」 `바뀐 칸 없음`을 인용 · Task 3 「편집 저장」 케이스 첫머리에 `toHaveText("바뀐 칸 없음")`(전체 일치) + `color` `rgb(78, 93, 89)` · acceptance 한 줄 |
| 체커 W2 — `useEditableWidth` 출처(04-30이 만든다는 지적) | 경고 | 증거로 04-49 유지: Phase 4 `04-49-PLAN.md` frontmatter `files_modified` · artifacts(`exports: ["useEditableWidth"]`) · Task 2 ①이 만든다. `04-30-PLAN.md`는 엔지 r2 E2-02 분할로 이 파일을 04-49에 넘겼다(04-30 「모듈(04-49로 옮김)」 · frontmatter에 없음). Task 2 read_first에 근거를 적었다 |
| 체커 W3 · 교차 N-5 — 폭 훅이 없을 때 멈춤 조건 없음 · ②-g0 `grep "DR-36" app/(app)/projects`(괄호가 따옴표 밖 — 구문 오류 · 0줄) · 수용 줄이 다른 이름을 허용 | 경고 | Task 1 `<precondition>`에 `test -f ui/table/use-editable-width.ts && grep -q "useEditableWidth" …`(04.3-04와 같은 멈춤) · ②-g0의 DR-36 grep과 대체 grep을 지우고 「훅 하나 · 없으면 precondition이 멈췄다」 · 좁은 PC 수용 줄이 다른 이름을 받지 않는다 |
| 교차 N-4 — 편집 뒤 폭이 1024 아래로 줄면 1차 「일괄 저장」이 사라져 편집 값이 갈 곳이 없음 | 참고 | SYSTEM §7-3 「화면 1차 「일괄 저장」은 … `N ≥ 1`이면 항상 렌더한다」(Phase 4 04-49 R1)를 따른다 — truth · key_link · ②-g0 · success_criteria · Task 3 좁은 PC 케이스(1280에서 고친 뒤 1000 → 「일괄 저장 1」 → 저장됨) · acceptance. UI-SPEC I3 당첨자 표 불릿과 UI Considerations 좁은 PC 행도 같은 문장으로 고쳤다 |
| (교차 B-4와 같은 규칙 — I3) 고정된 오류 · 충돌 셀이 남은 채 「일괄 저장」 | 경고(자체 발견) | 교차 B-4는 I2만 짚었지만 I3도 같은 SYSTEM §7-3 DR-5를 따라야 화면 하나만의 예외가 없다 — ②-c 한 줄(1차 켜진 채 · 서버 안 부름 · `firstIssueSignal`로 첫 오류 셀) · Task 3 「모양 중복」 케이스에 요청 0개 · 포커스 단언 |
| 교차 N-6 — `exact: true` grep이 한 줄 모양에 기댐(prettier 줄 나눔이면 거짓 실패) | 참고 | acceptance를 공백을 접어 보는 파이프(`tr -s '[:space:]' ' '` → `name: "(링크 )?닫기"…}` 묶음 추출 → `exact: true` 없는 묶음 0 · 묶음 1개 이상)로 바꿨다 |
| 교차 r2(`design-apply-cross-r2.md` @c9a728d — 교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요) R2-1 · R2-3 — `exact: true` 검사 파이프가 모달 셀렉터 `getByRole("dialog", { name: "링크 닫기" })`까지 잡아 올바른 코드가 거짓 실패 · 키 순서 `{ exact: true, name }`을 놓침 / Task 1 precondition에 `firstIssueSignal` 확인 없음(04.3-04와 비대칭) | 참고 | 수용 줄 파이프를 버튼 묶음(`getByRole\( ?"button", ?\{[^}]*\}`) 추출 → `name: "(링크 )?닫기"` 거르기 → `exact: true` 없는 묶음 0으로 바꾸고 실패 조건 문장을 더함(견본 확인: 올바른 표본 묶음 4줄 · `0` / 틀린 표본 `2` — 옛 파이프는 올바른 표본에서 `2`) · Task 1 `<precondition>`에 `grep -q "firstIssueSignal" ui/table/Table.tsx`(04.3-04 Task 3과 같은 확인) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 B-1 · B-2 · B-3 · B-4 · N-1 · N-2 · N-7 | 이 플랜의 화면 · 문구에 닿지 않는다(I2 · E3 — 04.3-03 · 04.3-04 · UI-SPEC에서 반영) |

### Round 5 — (설계 /cso, 2026-09-25)

> 출처: `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`(설계 단계 /cso — 최종 판정). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---|---|---|
| E3-35 누적 당첨자 상한 없음(Round 1 · 2에서 「→ 설계 /cso에서 확정」으로 남겨 뒀던 항목) — 설계 `/cso`가 500명 초과 시 `invalid` 거부로 확정 | low | ④ `saveWinners`에 4-b 누적 상한 검사(병합 뒤 총원 > 500 → `{kind: "invalid", cellErrors}`, `tooManyWinners`) · behavior 경계 줄(499+1 `saved` · 500+1 `invalid`) · 거부 판정 「다섯」으로 수 갱신 · acceptance 추가 · T-04.3-193(accept → **mitigate**) · Artifacts 표 후속 줄 갱신. **Supersedes Round 1 Addressed 「E3-35 …」의 `T-04.3-190 accept 줄 → 설계 /cso에서 확정` · Supersedes Round 1 Deferred 「E3-35 누적 당첨자 상한 · 저장 잠금 시간 한도의 수치」** — 누적 상한은 이제 이 플랜이 확정·구현했고(잠금 시간 한도는 여전히 받아들인 한계), 위협 번호는 Round 2에서 이미 `T-04.3-193`으로 옮겨졌다 |
| E3-13 시스템 관리자 시드가 `certs.submissions`를 배포마다 다시 켜지 않게 04.3-09가 바뀌면서, 이 플랜의 E2E가 전제하는 시스템 관리자의 「제출 내용」·I4 접근이 시드가 아니라 별도 준비에서 온다는 사실을 밝혀야 함 | medium(신규 발견 승격, 04.3-09 몫) | Task 3 ⓪에 「전제: 이 권한은 시드 기본값이 아니라 cert.setup이 켠 것이다(E3-13)」 문구 추가 · acceptance에 스펙이 `role-sysadmin`의 `certs.submissions`를 스스로 켜지 않는다는 grep 추가 |

### Review Feedback Deferred
| Concern | Reason |
|---|---|
| E3-35 500줄 저장의 잠금 시간 한도(수치) | A2 규모(행사당 수~수십 명)에서는 몇 초 대기라 이번 페이즈는 받아들인 한계로 남긴다(T-04.3-193 Mitigation Plan 그대로) |

### Round 6 — (checker r1, 2026-09-25)

> 출처: `cso-apply-checker-r1.md` 참고 #10 — `tooManyWinners` 화면 처리가 「`pinCellErrors`가 모르면」으로 미뤄져 있었다. 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---|---|---|
| 참고 #10 — 4-b가 새 코드 `tooManyWinners`의 화면 문구를 「`pinCellErrors`가 모르는 코드면 I2 1차 막힘과 같은 형식의 문장으로 고른다」로 미뤄 뒀는데, 이는 04.3-04 `pinCellErrors`(닫힌 11종 매핑)에 없는 코드이고 UI-SPEC에도 I3용 문장이 없어 화면에서 실제로 어떻게 뜨는지 정해지지 않았다 | info | **오케스트레이터 결정(2026-09-25, Shard A · 04.3-04 Round 5와 짝) — 새 문구 없이 확정:** Shard A가 UI-SPEC Copywriting 「I3 셀 오류」에 기존 I2 문구 `당첨자 {N}명 · 500명까지 줄이기`를 그대로 더하고, 04.3-04가 `pinCellErrors`에 `tooManyWinners`를 12번째 코드(+`total` 데이터)로 더했다. 4-b를 `{code: "tooManyWinners", total}`로 갱신하고 렌더 결과(`당첨자 501명 · 500명까지 줄이기`, `tone: "block"`)를 통합 테스트가 `pinCellErrors` 호출까지 이어 붙여 단언하도록 behavior · acceptance에 추가(싼 컴포넌트 단언 — 별도 E2E 없이 통합 테스트 안에서 순수 함수 호출) |

### Review Feedback Deferred
| Concern | Reason |
|---|---|
| (없음) | 이 라운드의 지적(참고 #10)은 오케스트레이터 결정으로 모두 반영했다 |

### Round 7 — (cross r2, 2026-09-25)

> 출처: `cso-apply-cross-r2.md` R2-B1 · R2-4 · 참고 9. 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---|---|---|
| R2-B1(정정 — Round 6 Addressed 행 · behavior:227 · acceptance:331의 `tone: "block"`을 바로잡는다) — `pinCellErrors`는 문장 맵만 돌려주고 `tone` 키가 없다 | 막는 문제 | behavior · acceptance · 4-b action에서 `tone: "block"` 삭제, 셀 색은 기존 I2 셀 오류 표시를 따른다고 명시 |
| R2-4 — 4-b(:276)가 「초과분만큼의 새 줄」만 오류로 고정했는데 UI-SPEC:261은 「그 새 줄들」(이번 요청의 새 줄 전부)을 고정한다고 적어 문서가 어긋남 | 막는 문제 | 4-b를 「이번 요청의 새 줄 전부」로 맞춤(UI-SPEC:261과 일치) |
| (참고, 코디네이터 요청) 편집 화면에 클라이언트 1차 막힘(501줄) 없음 · 동시 499+1 경합 처리가 명시돼 있지 않음 | info | **설계대로 클라이언트 1차 막힘 없음** — I3는 I2(만들기)와 달리 총원을 실시간으로 계산해 막을 필요가 없다. 서버(4-b)가 거부하고 그 결과가 셀 오류로 뜬다(§7-3 DR-5 그대로 — 오류 셀은 1차를 막지 않고, 눌러도 서버가 판정한다). **동시성은 이미 있는 잠금으로 덮인다** — `saveWinners` 2단계(:273 `withTransaction` 시작 → 행사 행 `FOR UPDATE`)가 같은 행사의 저장 요청을 직렬화하므로, 두 요청이 동시에 499+1을 보내도 뒤 요청은 앞 요청의 커밋(500명) 뒤에야 3에서 다시 읽어 4-b가 501로 판정해 거부한다 — 별도 잠금·테스트를 새로 만들지 않는다(기존 행사 행 잠금이 이미 막는다) |

### Review Feedback Deferred
| Concern | Reason |
|---|---|
| 참고 9 — 스테이징에 09 이전 시드로 한 번이라도 돈 DB는 시스템 관리자 행이 계속 켜져 있어 「지정 전에는 안 보이는 게 정상」이 성립하지 않을 수 있음(13:215) | 13 담당 샤드의 몫 — 이 플랜(10)은 13을 수정할 권한이 없다 |

### Round 8 — 1d927bd

> 출처: `docs/designs/plant8-erp-phase04.3-review-260925.md`(계획 문서 /review — 이 페이즈의 REVIEWS.md 대신). 계획 문서 /review 교차는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---|---|---|
| RB-P2d — truth(57) 「셀 오류 문장은 I2와 같은 9종」 · Task 2 ①(384) 「Copywriting I2 셀 오류 9종」이 04.3-04의 11종(`validateWinnerRows`) · `pinCellErrors` 12종과 어긋나, 공용 부품 실행자가 9종 매핑을 만들면 I3에서 `nameTooLong` · `prizeTooLong` · `tooManyWinners` 문장이 빠짐 — L222@1d927bd | P2 | 두 곳을 「11종(`validateWinnerRows`) + `tooManyWinners` = `pinCellErrors` 12종 — 04.3-04 `pinCellErrors`를 그대로 쓴다」로 바꿈 · Task 2 verify 마지막 줄(공용 표 · I3 화면이 `pinCellErrors(`를 부르고 따로 둔 코드 → 문장 표 없음) + fails_when · acceptance 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---|---|
| (없음) | 이 라운드에서 04.3-10에 걸린 지적은 모두 반영했다 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-10-SUMMARY.md` when done
</output>
