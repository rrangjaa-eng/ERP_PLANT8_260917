---
phase: 04.3-qr-certificate-intake
plan: 11
type: execute
wave: 5
depends_on: ["04.3-05", "04.3-07"]
files_modified:
  - domain/certs/review.ts
  - repositories/cert-review.ts
  - test/integration/cert-print.test.ts
  - app/print/certs/[id]/page.tsx
  - app/print/certs/[id]/loading.tsx
  - app/print/certs/[id]/print-sheet.tsx
  - app/print/certs/[id]/print-cert.module.css
  - test/unit/page-auth-guard.test.ts
  - app/(app)/certs/submissions/[id]/page.tsx
  - app/(app)/certs/submissions/[id]/review-form.tsx
  - app/(app)/certs/submissions/[id]/review.module.css
  - test/e2e/cert-print.spec.ts
autonomous: true
requirements: [CERT-02]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "경영관리(`certs.submissions` 보기)는 I4 머리의 2차 「인쇄」(프린터 아이콘 16 + 글자)로 새 탭에 세무 보관용 인쇄물 `/print/certs/{id}`를 연다 — 템플릿은 `print-cert.html` 그대로(A4 세로 · 세로선 없음 · 색 토큰 셋)이고 새 줄·새 블록이 없다(D-1108)"
    - "인쇄물의 주민등록번호는 `930412-2******` 가린 값뿐이다 — 인쇄 라우트는 복호화하지 않고 `mask_reveal`을 남기지 않는다(D-1108 · T-04.3-22)"
    - "이 페이즈가 채우는 칸: 확인증 번호 · 항목(행사 이름 + 2행 `당첨일 {날짜}`) · 소득 종류 `기타소득` · 경품 `{경품명} {수량}개` · 수령자 표(이름 · 가린 주민등록번호 · 주소 · 연락처 · 계좌 `—`) · 수령자 이름 · 제출 일시 · 서명 · 출력 시각 · 바닥줄. 지출결의 번호(`지출결의 —`) · 프로젝트 · 지급일 · 지급액 · 원천징수 · 실지급액은 `—`이고 원천징수 내역 2행과 한글 금액 줄은 렌더하지 않는다(UI Considerations partial P1 Phase 11 칸 · ⑨ · A10)"
    - "현장 수령 확인증의 주소는 `—`, 계좌는 늘 `—`다(UI Considerations empty I4 주소 · P1 주소/계좌의 P1 절반)"
    - "서명 이미지는 `filter: brightness(0)`로 검정으로 찍힌다. 자동 `window.print()`는 서명 이미지 `decode()`가 성공한 뒤에만 한 번 불리고, 서명을 못 불러오면(없음 · decode 거부) 인쇄하지 않고 화면에 `인쇄물을 만들지 못했습니다 · 다시 시도`(2차 「다시 시도」)가 선다(UI Considerations error I4 · P1 서명 이미지 실패의 P1 절반 · ⑬)"
    - "준비 표시(`data-ready`)가 붙기 전이나 ERROR 때 사용자가 브라우저 메뉴로 인쇄하면 확인증 본문은 숨고 `인쇄물이 아직 준비되지 않았습니다 · 화면이 다 뜬 뒤 다시 인쇄해 주세요` 한 줄만 찍힌다 — 서명 없는 종이가 나가지 않는다(UI Considerations error I4 브라우저 인쇄 · P1 준비 전 수동 인쇄의 P1 절반)"
    - "I4에 고친 칸이 있으면 「인쇄」가 비활성이고 이유 `인쇄할 수 없음 — 저장 안 한 칸 {N} · 먼저 저장`이 선다(UI Considerations error I4 동시 정정 · 고친 채 인쇄의 인쇄 절반)"
    - "인쇄 라우트는 로그인 · 플래그 · `certs.submissions` 보기 · 개인정보취급자 비활동 세션을 자체적으로 판정한다 — 권한 없음 · 플래그 꺼짐 · 파기됨 · 없음은 404, 비활동 만료는 로그인으로(UI Considerations error 권한 없음 · 플래그 꺼짐의 인쇄 라우트 절반)"
    - "인쇄 라우트 화면 바탕은 `--surface`, 종이 외곽은 `1px --line`이고 인쇄 미디어에서는 외곽이 없다(개정 ⑬ — 실물 미리보기 리터럴을 옮기지 않는다)"
    - statement: "40자 행사 이름 + 긴 도로명 주소 + 서명에서 인쇄물이 A4 한 장(`1 / 1`)에 들어가는지 — 인쇄 미리보기로 확인(UI Considerations long-text P1 A4 한 장 — 04.3-13 사람 확인)"
      verification: backstop
  prohibitions:
    - statement: "인쇄 라우트가 주민등록번호를 복호화하거나 전체 번호를 인쇄물·HTML에 싣어서는 안 된다(D-1108)"
      status: resolved
      verification: test
    - statement: "서명 이미지가 준비되지 않은 상태로 확인증 본문이 인쇄되어서는 안 된다(⑬)"
      status: resolved
      verification: test
  artifacts:
    - path: "app/print/certs/[id]/page.tsx"
      provides: "P1 인쇄 라우트(서버) — 세션 · 플래그 · 비활동 · 권한 · 가린 값만"
      contains: "touchPrivacySession"
    - path: "app/print/certs/[id]/print-sheet.tsx"
      provides: "서명 decode → data-ready → window.print 한 번 · 실패 ERROR"
      min_lines: 60
    - path: "domain/certs/review.ts"
      provides: "getCertificatePrint — 복호화 없는 인쇄 DTO"
      exports: ["getCertificatePrint"]
    - path: "test/e2e/cert-print.spec.ts"
      provides: "인쇄 자동 호출 · 서명 실패 · 준비 전 인쇄 · 권한 · 가린 번호 · 「인쇄」 비활성"
      min_lines: 50
  key_links:
    - from: "app/print/certs/[id]/page.tsx"
      to: "domain/certs/privacy-session.ts"
      via: "getCertificatePrint 전에 touchPrivacySession, 만료면 /login"
      pattern: "touchPrivacySession"
    - from: "app/(app)/certs/submissions/[id]/review-form.tsx"
      to: "app/print/certs/[id]/page.tsx"
      via: "「인쇄」 → 새 탭 `/print/certs/{id}`"
      pattern: "/print/certs/"
    - from: "test/unit/page-auth-guard.test.ts"
      to: "app/print"
      via: "인쇄 라우트 페이지도 자체 세션 검사를 갖는지 훑는다"
      pattern: "app/print"
---

<objective>
**As a** 경영관리(세무 보관 담당), **I want to** 수령자가 제출한 확인증을 세무 보관용 종이 한 장으로 인쇄하고 싶다, **so that** 기타소득 확인 서류를 서명과 함께 보관하되 주민등록번호 전체가 종이에 나가지 않는다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — Goal의 「경영관리는 … 세무 보관용 인쇄물을 연다」(기준 5 · D-1108)에서 이야기를 끌어냈다.

UI-SPEC P1 · Copywriting P · SYSTEM §6-6(개정 ⑨ ⑬)을 만든다. 인쇄 라우트는 셸 밖(`app/print/…`)이라 `(app)` 그룹의 페이지 인증 검사 테스트가 훑지 않는다 — 그 테스트를 인쇄 라우트까지 넓힌다. I4(04.3-07)의 머리에 「인쇄」를 더하는데, `PageHeader`에는 행동 자리가 없으므로(D-25) `app/(app)/projects/[id]/quote-table.tsx`처럼 클라이언트 폼이 머리 줄을 그리고 저장 안 한 칸 수로 비활성을 정한다.

**결정(재량, 설계 `/cso`가 다시 본다):** ① 인쇄는 가린 주민등록번호만 싣고 복호화하지 않으므로 `mask_reveal`을 남기지 않는다(D-1108 문장 그대로). 이름·연락처·주소는 I4와 같은 권한 뒤에서 보이며 인쇄 자체의 행동 로그는 두지 않는다 — I4 열람도 로그가 없는 것과 같은 층위. ② 인쇄 라우트도 개인정보 경로라 `touchPrivacySession`을 부른다(04.3-07 결정 ①).

Purpose: CERT-02 「경영관리가 세무 보관용 인쇄물을 연다」의 인쇄 절반.
Output: `getCertificatePrint` · `/print/certs/[id]` · I4 「인쇄」 · 통합 · E2E · 인증 검사 테스트 확장.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@.planning/phases/04.3-qr-certificate-intake/04.3-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getCertificatePrint — 복호화 없는 인쇄 DTO + 통합 테스트</name>
  <files>domain/certs/review.ts, repositories/cert-review.ts, test/integration/cert-print.test.ts</files>
  <read_first>
    - `domain/certs/review.ts` · `repositories/cert-review.ts`(04.3-07 전문 — `getSubmissionForReview` 권한·파기·플래그 순서 · 서명 data URL 읽기 · `CertSubmissionReviewDto` 등록 방식)
    - `test/integration/cert-crypto.test.ts` · `test/integration/cert-review.test.ts`(04.3-07 — 복호화 호출 0번을 단언하는 방식 · 제출 준비 함수)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 P1 · Copywriting P
    - `domain/certs/format.ts`(04.3-02 — 전화 표시형 · 날짜 형식)
  </read_first>
  <behavior>
    - 시스템 관리자 · 택배 확인증 → DTO: 확인증 번호 · 행사 이름 · 당첨일 · 경품명 · 수량 · 제출 이름 · `rrnMasked` · 연락처 표시형 · 주소 · 제출 시각 · 서명 data URL(PNG) · 출력 시각(KST `yyyy-MM-dd HH:mm`)
    - 현장 확인증 → `address`가 null
    - DTO 전체를 JSON으로 만들어도 13자리 주민등록번호 모양(`\d{6}-?\d{7}`)이 없다 · 복호화 호출 0번 · `mask_reveal` 행 0줄
    - 기획 PM(`certs.submissions` 보기 없음) → `notFound` · 파기됨(테스트가 `purged_at`을 직접 채운다 — 파기 함수는 같은 웨이브 04.3-12 몫) → `notFound` · 없는 id → `notFound` · 플래그 꺼짐 → `notFound`
    - 저장소에 서명 객체가 없음 → DTO의 서명이 null(오류 표시는 화면 몫)
  </behavior>
  <action>
① **RED:** 위 `behavior`를 `test/integration/cert-print.test.ts`에 먼저 쓴다(04.3-07 통합 테스트의 준비 함수 · 복호화 호출 세기 방식을 그대로).

② **`repositories/cert-review.ts`:** 04.3-07의 확인증 한 줄 조회가 행사 당첨일(`won_on`)을 읽지 않으면 그 칸만 더한다(기존 함수 모양 유지 — 필요하면 인쇄 전용 조회 함수를 따로 둔다).

③ **`domain/certs/review.ts` `getCertificatePrint(viewer, id)`:** `getSubmissionForReview`와 같은 순서(플래그 → `can(viewer, "certs.submissions", "view")` → 행 → 파기) · 가린 번호는 저장된 `rrn_masked`만 쓴다 · 서명은 같은 저장소 포트로 읽어 data URL(읽기 실패·없음 → null) · 출력 시각은 서버 지금(KST). `lib/crypto`를 이 함수 경로에서 쓰지 않는다. DTO `CertificatePrintDto`를 `registerDto`로 등록(정보 항목 `cert_submission.value` — 04.3-07과 같은 항목). 누수 스캔은 이 모듈을 이미 import한다(04.3-07) — `leak-scan.test.ts`를 고치지 않는다.

④ **GREEN → 커밋** `feat(04.3): masked certificate print data`, 본문 한국어(결정 ①).
  </action>
  <verify>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/cert-print.test.ts test/integration/cert-review.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 복호화 호출이 1번 이상이거나 DTO에 13자리 모양이 있으면 D-1108 위반이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `getCertificatePrint`의 몸통에 `decrypt` 호출이 없다(통합 테스트의 호출 0번 단언이 증거)
    - 통합 테스트가 PM · 파기됨 · 없음 · 플래그 꺼짐 넷을 `notFound`로 단언한다
    - `CertificatePrintDto`가 `registerDto`로 등록돼 누수 스캔이 녹색이다
  </acceptance_criteria>
  <done>인쇄 DTO가 가린 번호와 서명만으로 만들어지고 권한·파기·플래그를 지킨다</done>
</task>

<task type="auto">
  <name>Task 2: P1 인쇄 라우트 — 템플릿 · 서명 decode 뒤 자동 인쇄 · ERROR · 준비 전 인쇄 차단 · 인증 검사 테스트 확장</name>
  <files>app/print/certs/[id]/page.tsx, app/print/certs/[id]/loading.tsx, app/print/certs/[id]/print-sheet.tsx, app/print/certs/[id]/print-cert.module.css, test/unit/page-auth-guard.test.ts</files>
  <read_first>
    - `docs/design/system/print-cert.html`(전문 108행 — `.sheet` · `.top` · `.rule` · `dl` · `.amt` · `.decl` · `.sign` · `.foot` · `@page`)
    - `docs/design/SYSTEM.md` §6-6(493~531행 — 인쇄 규칙 · 개정 ⑨ ⑬) · §7-7 컴포넌트별 표 「인쇄 라우트」 행(825행 근처)
    - `docs/design/tokens.css` 55~62행 · 179~200행(인쇄 토큰 · `@media print` 접힘)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「화면 계약」 P1 · Copywriting P
    - `app/(app)/certs/submissions/[id]/page.tsx`(04.3-07 — `requireSession` → 플래그 → `getSessionId` → `touchPrivacySession` → 조회 순서)
    - `test/unit/page-auth-guard.test.ts`(전문)
    - `node_modules/next/dist/docs/` 안 `loading.js` · `not-found` 파일 규약 문서(Next 16 — `params`는 Promise)
  </read_first>
  <action>
① **`page.tsx`(서버, `params`는 Promise):** `requireSession` → 플래그 꺼짐이면 `notFound()` → 세션 id로 `touchPrivacySession` → `expired`면 `redirect("/login")` → `getCertificatePrint`가 `notFound`면 `notFound()`. metadata title `확인증 인쇄 · {확인증 번호}`. 서버가 모든 값을 문자열로 만들어 `print-sheet`에 넘긴다(가린 번호 · 전화 표시형 · 날짜 형식 · `—`).

② **`print-sheet.tsx`(`"use client"`):** `print-cert.html`의 구조와 클래스 역할을 그대로 옮긴다(새 줄·새 블록 없음). 칸: 머리 `PLANT8` + `출력 {출력 시각}` · 제목 `기타소득 지급 확인증` · 부제 `{확인증 번호}` + `지출결의 —` · 지급 내용(`프로젝트 —` · `항목 {행사 이름}` + 2행 `당첨일 {날짜}` · `소득 종류 기타소득` · `경품 {경품명} {수량}개` · `지급일 —`) · 수령자(`이름` · `주민등록번호 {가림}` · `주소 {주소 또는 —}` · `연락처` · `계좌 —`) · 금액 블록(`지급액 —` · `원천징수 —` · `실지급액 —` — 원천징수 내역 2행과 한글 금액 줄은 **그리지 않는다**) · 확인 문장 `위 금액을 기타소득으로 지급받았음을 확인합니다.` · 서명 줄(`지급자 PLANT8` + `—` · `수령자 {제출 이름}` + `{제출 일시} 제출` · 서명 이미지(`alt="{제출 이름} 서명"`, `filter: brightness(0)`) + `서명`) · 바닥줄 `PLANT8 ERP` + `1 / 1`. 서명이 null이면 곧바로 ERROR. 서명 `<img>`가 붙으면 `decode()` → 성공 시 라우트 루트 요소에 `data-ready` 표시 → `window.print()` **한 번**(개발 모드 이중 효과에도 한 번이 되게 ref로 막는다). `decode()` 거부 · `onerror` → ERROR: 화면 한 줄 `인쇄물을 만들지 못했습니다 · 다시 시도` + 2차 「다시 시도」(= 라우트 다시 불러오기), `window.print()`를 부르지 않는다. 인쇄 미디어 전용 한 줄 `인쇄물이 아직 준비되지 않았습니다 · 화면이 다 뜬 뒤 다시 인쇄해 주세요`는 `data-ready`가 없을 때만 인쇄 미디어에 보이고, 그때 `.sheet`는 인쇄 미디어에서 숨는다(화면에서는 이 한 줄이 늘 숨는다).

③ **`print-cert.module.css`:** 실물 CSS를 토큰으로만 옮긴다 — 인쇄 템플릿은 `--print-ink` · `--print-rule` · `--print-line` 셋만(§6-6), A4 세로 · 여백은 실물 값 그대로, 세로선 없음, `break-inside: avoid`. 화면: 바탕 `--surface`, 종이 외곽 `1px solid var(--line)`(실물 15행의 미리보기 리터럴 두 값은 옮기지 않는다 — 개정 ⑬). 인쇄 미디어: 외곽 없음 · 버튼·ERROR 줄 숨김. 새 색·새 radius 없음.

④ **`loading.tsx`:** §7-7 인쇄 라우트 LOADING — 빈 A4 틀(같은 외곽) + 상단 진행 바(기존 진행 바 모양 재사용, 없으면 `--accent` 2px 막대 토큰만), 스피너 없음.

⑤ **`test/unit/page-auth-guard.test.ts`:** `(app)` 그룹과 같은 검사를 `app/print` 아래 모든 `page.tsx`에도 건다(훑을 목록이 1개 이상인지도 단언). 기존 케이스·문장은 그대로.

⑥ 커밋 `feat(04.3): certificate print route`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/page-auth-guard.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 인쇄 라우트 페이지에 자체 세션 검사가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/print/certs/[id]/page.tsx`에 `requireSession`과 `touchPrivacySession`이 있고 `touchPrivacySession`이 `getCertificatePrint`보다 먼저다
    - `print-cert.module.css`에 16진 색 리터럴이 없다(`! grep -nE "#[0-9A-Fa-f]{3,6}\b" "app/print/certs/[id]/print-cert.module.css"`)
    - `print-sheet.tsx`에 `brightness(0)`(또는 CSS 모듈 쪽) · `decode()` · `data-ready`가 있다
    - `page-auth-guard` 테스트가 `app/print`를 훑는다
  </acceptance_criteria>
  <done>인쇄 라우트가 템플릿 그대로 값을 채우고, 서명이 준비된 뒤에만 한 번 인쇄를 부르며, 준비 전 인쇄는 본문을 숨긴다</done>
</task>

<task type="auto">
  <name>Task 3: I4 「인쇄」(저장 안 한 칸이면 비활성) + 인쇄 E2E</name>
  <files>app/(app)/certs/submissions/[id]/page.tsx, app/(app)/certs/submissions/[id]/review-form.tsx, app/(app)/certs/submissions/[id]/review.module.css, test/e2e/cert-print.spec.ts</files>
  <read_first>
    - `app/(app)/certs/submissions/[id]/page.tsx` · `review-form.tsx` · `review.module.css`(04.3-07 전문 — 머리 · 고친 칸 셈 · `@media print` 차단 한 줄)
    - `app/(app)/projects/[id]/quote-table.tsx` 886~905행 · `project-detail.module.css` 1~30행(머리 `.header` 감싸기 선례 — 읽기만)
    - `ui/button/Button.tsx`(`disabledReason` · 2차 변형) · `ui/table/RowSheet.tsx`(인라인 SVG 아이콘 선례)
    - `test/e2e/cert-review.spec.ts`(04.3-07 — 제출 준비 · 로그인 · 플래그 방식)
    - `lib/storage/signature-store.ts`(04.3-02 · 05 — 로컬 드라이버에 객체를 넣는 함수)
  </read_first>
  <action>
① **`page.tsx` · `review-form.tsx` · `review.module.css`:** 머리 줄(제목 `기타소득 확인증 — {제출 이름}` · 태그 `제출됨` · 부제)을 `review-form`이 그리게 옮기고(`page.tsx`는 값만 넘긴다), `.header` · `.titleBlock` · `.headerActions`(quote-table 선례, 토큰만)의 행동 자리에 2차 「인쇄」 — 인라인 SVG 프린터 아이콘 16(`aria-hidden`) + 글자 `인쇄`. 누르면 새 탭으로 `/print/certs/{id}`(opener 없이). 고친 칸 수 N ≥ 1이면 비활성 + 이유 `인쇄할 수 없음 — 저장 안 한 칸 {N} · 먼저 저장`(N = 04.3-07의 1차 막힘이 세는 같은 값). 04.3-07의 인쇄 차단 한 줄과 나머지 동작은 그대로.

② **`test/e2e/cert-print.spec.ts`(데스크톱):** 스펙 안에서 domain 흐름으로 택배 확인증 하나 · 현장 확인증 하나를 만든다. `page.addInitScript`로 `window.print`를 호출 횟수를 세는 함수로 바꾼다.
  - I4 → 「인쇄」 → 새 탭(`context.waitForEvent("page")`) → 제목 `기타소득 지급 확인증` · 부제의 확인증 번호 · `지출결의 —` · `930412-2******` 모양 · 페이지 HTML에 13자리 번호 없음 · `계좌 —` · `지급액 —` · 한글 금액 줄 없음 · 루트에 `data-ready` · `window.print` 호출 1번 · DB `mask_reveal` 0줄
  - 현장 확인증 인쇄 → `주소 —`
  - 서명 실패: 스펙이 그 확인증의 서명 키를 망가진 PNG 바이트를 넣은 새 객체 키로 바꾼다 → 인쇄 라우트 → `인쇄물을 만들지 못했습니다 · 다시 시도` · `window.print` 0번 · `page.emulateMedia({media: "print"})` → 보이는 글자가 `인쇄물이 아직 준비되지 않았습니다 · 화면이 다 뜬 뒤 다시 인쇄해 주세요`뿐
  - 서명 없음: 서명 키를 없는 객체 키로 → 같은 ERROR · 인쇄 0번
  - I4에서 연락처를 고친 채 → 「인쇄」 비활성 + `인쇄할 수 없음 — 저장 안 한 칸 1 · 먼저 저장` → 저장 → 활성
  - 기획 PM이 `/print/certs/{id}` → 404 · 플래그 끔 → 시스템 관리자도 404(끝에서 다시 켬) · 비활동 만료(활동 행 시각을 121분 전으로) → 로그인 화면
③ 커밋 `feat(04.3): print button on certificate review`, `test(04.3): certificate print E2E`.
  </action>
  <verify>
    <automated>pnpm playwright test test/e2e/cert-print.spec.ts test/e2e/cert-review.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 서명 실패 케이스에서 `window.print` 호출이 1 이상이면 ⑬ 위반이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 서명 실패 · 서명 없음 · 준비 전 인쇄 미디어 · 「인쇄」 비활성 · PM 404 · 플래그 404 · 비활동 만료 케이스가 스펙에 있다
    - 04.3-07의 `cert-review.spec.ts`가 그대로 녹색이다(머리 이동이 I4를 깨지 않았다)
  </acceptance_criteria>
  <done>경영관리가 I4에서 인쇄물을 열고, 서명이 준비된 종이만 인쇄되며, 저장 안 한 칸이 있으면 인쇄하지 못한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `/print/certs/[id]` | 셸 밖 라우트 — 레이아웃 보호가 없으므로 페이지가 세션 · 플래그 · 권한 · 비활동을 스스로 판정한다 |
| 서버 → 종이 | 인쇄물은 화면 밖으로 나가는 사본 — 가린 번호만 싣는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-22 | Information Disclosure | 인쇄물의 주민등록번호 | high | mitigate | 인쇄 DTO는 저장된 가린 값만 쓰고 복호화 경로가 없다(통합 테스트 호출 0번 · E2E HTML 13자리 없음). I4의 브라우저 인쇄는 04.3-07이 한 줄로 막는다 |
| T-04.3-53 | Elevation of Privilege | 셸 밖 인쇄 라우트 | high | mitigate | 페이지 자체의 `requireSession` → 플래그 → `touchPrivacySession` → `certs.submissions` 보기 판정, 밖이면 404. `page-auth-guard` 테스트가 `app/print`까지 훑는다 |
| T-04.3-54 | Repudiation | 서명 없는 확인증 종이 | medium | mitigate | 자동 인쇄는 서명 `decode()` 뒤 한 번만, 준비 전·ERROR 때 수동 인쇄는 본문을 숨긴다(E2E 서명 실패 · 없음 · 인쇄 미디어) |
| T-04.3-55 | Information Disclosure | 인쇄 열람 기록 없음 | low | accept | 가린 번호만 나가고 I4 열람과 같은 권한 뒤에 있다. 인쇄마다 로그를 남길지는 설계 `/cso`가 본다 |
| T-04.3-56 | Information Disclosure | 파기된 확인증 인쇄 | medium | mitigate | 파기됨이면 404 — 파기(04.3-12)가 칸을 비운 뒤 빈 종이가 나가지 않는다 |
</threat_model>

<verification>
- 통합 `cert-print` · `cert-review` · `leak-scan` 녹색
- 단위 `page-auth-guard` 녹색
- E2E `cert-print` · `cert-review` 녹색
- `pnpm lint && pnpm typecheck && pnpm build` 녹색
</verification>

<success_criteria>
- I4 「인쇄」 → 새 탭 인쇄물이 템플릿 그대로 값을 채우고 가린 번호만 보인다
- 서명이 준비된 뒤에만 한 번 인쇄되고, 실패·준비 전에는 종이에 본문이 나가지 않는다
- 권한 없음 · 플래그 꺼짐 · 파기됨은 404, 비활동 만료는 로그인
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 라우트 | `/print/certs/[id]`(셸 밖 · `loading.tsx` 포함) |
| domain 함수 · DTO | `getCertificatePrint` · `CertificatePrintDto` |
| 화면 변경 | I4 머리 2차 「인쇄」(저장 안 한 칸이면 비활성) |
| 테스트 | `test/integration/cert-print.test.ts` · `test/e2e/cert-print.spec.ts` · `page-auth-guard`의 `app/print` 확장 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-11-SUMMARY.md` when done
</output>
