---
phase: 04.3-qr-certificate-intake
plan: 12
type: execute
wave: 6
depends_on: ["04.3-06", "04.3-07", "04.3-08", "04.3-10"]
files_modified:
  - domain/certs/purge.ts
  - repositories/cert-purge.ts
  - domain/action-log/record.ts
  - test/unit/action-log/record.test.ts
  - test/unit/certs/purge-deadline.test.ts
  - test/integration/purge-expired-certs.test.ts
  - scripts/purge-certs.ts
  - scripts/build-cli.mjs
  - package.json
  - test/unit/purge-certs-cli.test.ts
  - test/unit/deploy/cli-bundle.test.ts
  - scripts/deploy.sh
  - test/unit/deploy/deploy-sh.test.ts
  - docs/OPERATIONS.md
autonomous: true
requirements: [CERT-02]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "제출된 확인증은 파기 기한 = (제출 시각의 KST 연도 + 1 + 보존 연수)년 3월 1일 00:00 KST에 파기 대상이 된다 — 기한 1ms 전에는 남고 기한 정각에는 파기된다(제출한 해 종료 + 법정 신고기한(다음 해 2월 말) + 보존 기간, CONTEXT 「파기 시점」 · CERT-02 × boundary)"
    - "보존 연수는 확인증에 저장된 값(`cert_submissions.retention_years` — 확인 때 그 자리에 묶여 동의 요약에 보인 `offered_retention_years`를 제출이 복사한 것, 04.3-02)이다 — 설정을 나중에 바꿔도 이미 제출된 확인증의 기한이 바뀌지 않는다"
    - "파기는 행을 지우지 않는다 — 확인증의 이름 · 주민등록번호(암호문 · 가린 값) · 연락처 · 주소를 비우고 `purged_at`을 채우며, 그 자리의 이름 · 전화번호 · 구별 표시도 비운다. 확인증 번호 · 행사 · 경품 · 수량 · 제출 시각 · 연결 칸은 세무 기록으로 남는다(CONTEXT 「파기 시 줄은 남긴다」 · RESEARCH Pitfall 1)"
    - "파기된 자리(제출분 · 미제출 명단 모두)는 확인 흔적과 누적 잠김도 비운다 — `verify_idem_outcome`(IP 해시가 든 멱등 결과 맵) · `verify_proof_hash` · `verified_until` · `hard_locked_at` · `locked_until`은 null, `cumulative_failed_attempts` · `failed_attempts`는 not null 칸이라 0(짧은 잠김 셈과 해제 시각도 남기지 않는다 — checker-C INFO). 그래서 파기된 미제출 자리는 누적 잠김이 아니어서 04.3-10의 「잠금 풀기」 대상이 되지 않는다(checker-B W2 · 누적 잠금 결정)"
    - "파기는 라이브 DB 기준이다 — Cloud SQL 자동 백업(`scripts/deploy.sh` `ensure_sql_instance`의 `--retained-backups-count=7`, 하루 한 번 — PITR을 켠 환경이면 그 로그도. Phase 4 `ensure_sql_instance`는 `--enable-point-in-time-recovery`를 주지 않는다)에는 비운 개인정보(주민등록번호 암호문 포함 — 데이터 키도 남아 풀린다)가 남으므로 런북이 「백업 보관 7일이 지나야 완전 파기」를 적는다. 파기 전 시점의 백업으로(켠 경우 PITR 포함) 복원하면 파기된 행이 조용히 되살아나므로(서명 객체는 이미 없는데 `signature_key`는 채워진 채), 복원 뒤에는 곧바로 `purge-certs --apply`를 한 번 돌린다 — 기한이 지난 행이 다시 비워지고 없는 객체는 이미 지워진 것으로 친다(AX-3)"
    - "DB를 먼저 비우고 서명 파일을 나중에 지운다 — 파일 삭제가 끝날 때까지 `signature_key`가 삭제 대기 표시로 남고, 다음 실행이 대기 중인 파일을 다시 지운다. 없는 객체는 이미 지워진 것으로 친다(CERT-02 × idempotency)"
    - "C3: 업로드 의도 표 `cert_signature_uploads`에 24시간 넘게 남은 행(업로드는 됐지만 제출이 저장되지 않은 객체 — 프로세스 종료 · 응답 유실 · 보상 삭제 실패)은 적용 실행이 그 객체를 저장소에서 지운 뒤 행을 지운다(04.3-02의 C3 도우미 `listStaleSignatureUploadIntents` · `deleteSignatureUploadIntent`를 그대로 쓴다). 객체 삭제가 실패하면 행이 남아 다음 실행이 다시 지운다. 미리 보기는 수만 센다(codex #12)"
    - "파기와 주민등록번호 전체 보기는 직렬화된다 — 전체 보기가 행을 `FOR SHARE`로 잠근 동안(04.3-07) 파기 UPDATE는 기다렸다가 그 뒤에 칸을 비우고, 파기가 먼저 커밋되면 전체 보기는 복호화 없이 거부된다(경합 통합 테스트 · codex #13)"
    - "파기를 다시 돌려도 이미 파기된 줄은 다시 세지 않는다 — 두 번째 실행의 새 파기 수는 0이다"
    - "제출하지 않은 당첨자의 이름 · 전화번호 · 구별 표시는 행사가 닫힌 뒤(담당자 닫음 · 모두 제출 시각 · 기한 중 이른 것) 30일에 비워진다 — 30일은 코드 상수이고 설계 `/cso`가 다시 본다(CONTEXT 재량 「제출하지 않은 명단의 파기 시점」 · CERT-02 × empty)"
    - "파기된 확인증은 경영관리 화면이 404이고 전체 보기가 거부된다(04.3-07의 `purged_at` 판정) — 인쇄 라우트도 같은 판정을 쓴다(04.3-11이 자기 테스트로 확인한다, 이 플랜의 의존 목록에 없어 부르지 않는다)"
    - "파기는 기능 플래그와 무관하게 돈다 — 플래그를 꺼도 보존 기간이 지난 개인정보는 지워진다"
    - "파기 스크립트는 기본이 미리 보기(dry-run)다 — 대상 수만 보이고 아무것도 바꾸지 않는다. `--apply`일 때만 비우고, 그 실행 한 번이 끌 수 없는 행동 로그 `확인증 파기` 한 줄(개수만, 개인정보 없음)로 남는다. 이 로그는 칸 비우기와 **같은 트랜잭션**에서 쓰인다 — 로그 쓰기가 실패하면 비우기도 되돌아간다(C6 · Phase 4 `recordAction(…, {tx})`, E3-02)"
    - "파기 리포지토리 함수는 모두 첫 인자가 `viewer`이고 domain이 `SYSTEM_VIEWER`를 넘긴다 — 저장소 lint 규칙 `repository-viewer-param`을 그대로 통과한다(codex #19)"
    - "스테이징·프로덕션에서 파기는 Cloud Run Job `purge-certs`를 사람이 실행한다 — 확정(E3-16 교차 B-5 (나)안 — 설계 `/cso`): 이 페이즈는 Cloud Scheduler 자동 실행 단계를 두지 않고 사람 월 1회 + 35일 감시 런북을 유지하며, Scheduler 자동화는 Phase 7로 이관한다(Phase 4 D19-8과 일치). Job은 데이터 키를 받지 않고 서명 버킷 이름만 받는다"
  prohibitions:
    - statement: "파기가 확인증 · 당첨자 행을 물리 삭제해서는 안 된다(세무 기록 보존 · 저장소 불변식)"
      status: resolved
      verification: test
    - statement: "파기 실행 기록(행동 로그 · 서버 로그)에 이름 · 전화번호 · 주민등록번호 · 주소가 들어가서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/certs/purge.ts"
      provides: "파기 기한 순수 함수 둘 + runCertPurge(미리 보기 · 적용)"
      exports: ["certPurgeDeadline", "unsubmittedRosterPurgeAt", "runCertPurge"]
    - path: "repositories/cert-purge.ts"
      provides: "후보 조회 · 칸 비우기(UPDATE) · 삭제 대기 파일 조회·정리 — 모두 viewer 첫 인자, 행 삭제 없음"
    - path: "scripts/purge-certs.ts"
      provides: "파기 CLI — 기본 미리 보기, `--apply`로 적용"
      exports: ["parseArgs", "UsageError"]
    - path: "test/integration/purge-expired-certs.test.ts"
      provides: "기한 경계 · KST 연도 경계 · 행 보존 · 파일 대기 재시도 · 멱등 · 명단 30일 · 미리 보기 · 로그"
      min_lines: 90
  key_links:
    - from: "scripts/deploy.sh"
      to: "scripts/build-cli.mjs"
      via: "Job `purge-certs`가 부르는 `dist/cli/purge-certs.mjs`가 번들 outputs에 있다(cli-bundle 테스트)"
      pattern: "purge-certs.mjs"
    - from: "domain/certs/purge.ts"
      to: "lib/storage/signature-store.ts"
      via: "DB 커밋 뒤 같은 포트의 delete로 서명 객체를 지운다"
      pattern: "getSignatureStore"
    - from: "domain/certs/purge.ts"
      to: "domain/action-log/record.ts"
      via: "적용 실행 한 번 = cert_purge 한 줄(개수만), 칸 비우기와 같은 트랜잭션 — `recordAction(SYSTEM_VIEWER, …, {tx})`(C6 · Phase 4 04-32 · E3-02)"
      pattern: "recordAction"
    - from: "domain/certs/purge.ts"
      to: "repositories/cert-submissions.ts"
      via: "C3 — 24시간 넘은 업로드 의도를 04.3-02 도우미로 읽고, 객체를 지운 뒤 같은 도우미로 행을 지운다"
      pattern: "listStaleSignatureUploadIntents"
---

<objective>
**As a** 경영관리(개인정보 보관 책임자), **I want to** 보존 기간이 지난 확인증의 개인정보와 제출하지 않은 당첨자 명단이 정해진 날에 확실히 지워지기를 바란다, **so that** 세무 기록(건수 · 연결)은 남기면서 법이 허용한 기간보다 오래 개인정보를 들고 있지 않는다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — Goal의 「보존 기간 뒤 파기를 지킨다」(기준 4)에서 이야기를 끌어냈다.

CONTEXT 「파기 시점 = 제출한 해 종료 + 법정 신고기한 + 보존 기간(설정, 기본 5년) · 이름 · 전화 · 주민등록번호 · 주소 · 서명 이미지를 지우고 줄은 남긴다 · DB 먼저, 파일 나중 · 보관함 예외」를 구현한다. CONTEXT 재량 둘(파기 실행 방식 · 서명 저장 위치)은 04.3-05(비공개 버킷)와 이 플랜(사람이 실행하는 Cloud Run Job)으로 정했고, 재량 「제출하지 않은 명단의 파기 시점」은 30일 상수로 정해 설계 `/cso`에 넘긴다.

**결정(재량, 설계 `/cso`가 다시 본다):** ① 기한 = (제출 KST 연도 + 1 + 보존 연수)-03-01 00:00 KST — 기타소득 지급명세서 제출기한(다음 해 2월 말) 다음 날부터 보존 기간을 센다. ② 보존 연수는 확인증의 `retention_years`(04.3-02 — 확인 때 묶인 `offered_retention_years`를 제출이 복사) — 파기는 설정을 읽지 않는다. ③ 파일 삭제 대기는 새 칸 없이 파기된 줄(`purged_at` 있음)에 남은 `signature_key`로 표시한다 — 삭제가 끝나면 비운다(스키마 변경 없음). ④ 미제출 명단은 행사가 닫힌 뒤 30일. ⑤ 실행은 Cloud Scheduler 없이 사람이 Job을 실행한다 — 이 저장소에 스케줄러가 아직 없고, 기한이 연 1회(3월 1일)라 월 1회 실행으로 늦어도 한 달 안에 지워진다. ⑥ 실행 기록은 끌 수 없는 새 핵심 로그 종류 `cert_purge`(라벨 `확인증 파기`) — 공유 등록부라 세 배열에 항목만 더한다. 이 로그는 칸 비우기와 한 트랜잭션(C6 — Phase 4 04-32의 `recordAction(…, {tx})`를 처음부터 쓴다. 자체 tx 로그 도우미를 두지 않는다, E3-02)이라 detail은 트랜잭션 안에서 아는 개수(`submissions` · `winners` · `filesQueued`)뿐이고, 커밋 뒤 파일 삭제 결과(`filesDeleted` · `filesPending` · `orphansDeleted`)는 반환값과 서버 로그 `purge_certs.done`에 남는다. ⑦ 고아 서명(C3): 04.3-02가 제출 전 업로드 의도 행을 넣고 제출 트랜잭션에서 지우므로, 24시간 넘게 남은 의도 행은 저장되지 않은 업로드다 — 파기 적용 실행이 객체를 지우고 행을 지운다. ⑧ 리포지토리는 저장소 규칙대로 viewer 첫 인자이고 domain이 `SYSTEM_VIEWER`를 넘긴다(codex #19).

웨이브 6: 04.3-10(웨이브 5)도 `domain/action-log/record.ts`에 `cert_unlock`을 더하므로 같은 웨이브에서 같은 파일을 고치지 않게 그 뒤에 돈다(`depends_on`에 04.3-10). 04.3-13은 웨이브 7.

**실행 조건(eng-review E3-01 · E3-33):** Phase 4 PR #40(`origin/claude/gsd-progress-e1nzgu`)이 main에 머지된 뒤에만 시작한다. 웨이브 첫 단계로 `git fetch origin && git merge origin/main`을 한다(리베이스 · 강제 푸시 금지 — 이미 합쳐져 있으면 `Already up to date`로 끝난다). Phase 4 부품 가운데 이 플랜이 쓰는 것은 `recordAction(…, {tx})`(`domain/action-log/record.ts`의 `RecordActionDeps.tx` · `appendActionLog` 주입)다. 통합 테스트는 다른 플랜과 한 번에 하나만 돈다(세션당 플랜 하나).

Purpose: CERT-02 「보존 기간 뒤 파기」와 D-1107 설계 감사가 볼 파기 실행 경로.
Output: 파기 domain · 리포지토리 · CLI · Cloud Run Job · 런북 · 단위 · 통합 테스트.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-CONTEXT.md
@.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md
@.planning/phases/04.3-qr-certificate-intake/04.3-05-SUMMARY.md
@.planning/phases/04.3-qr-certificate-intake/04.3-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 파기 기한 순수 함수 · runCertPurge · cert_purge 로그 종류 — 단위 · 통합 테스트</name>
  <precondition>Phase 4가 main에 있고 이 브랜치에 합쳐져 있다 — `git fetch origin && git merge origin/main` 뒤 `test -f ui/confirm-dialog/ConfirmDialog.tsx`가 종료 코드 0(E3-01). 아니면 시작하지 않고 멈춘다. `domain/action-log/record.ts`의 `tx?: DbOrTx` 유무는 멈춤 조건이 아니다 — 없으면 04.3-02 ⑨-b 대비책 `insertActionLogTx`(`repositories/cert-action-log.ts`) 하나로 간다 — ② 순서 4 대비책(E3-02 단일 정책, Round 2)</precondition>
  <files>domain/certs/purge.ts, repositories/cert-purge.ts, domain/action-log/record.ts, test/unit/action-log/record.test.ts, test/unit/certs/purge-deadline.test.ts, test/integration/purge-expired-certs.test.ts</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md` 「Pitfall 1」(356~363행) · Security Domain 파기 줄(528행)
    - `domain/archive/index.ts` 10~20행 — 물리 삭제 문장 0건 불변식
    - `db/schema/cert-submissions.ts` · `db/schema/cert-winners.ts` · `db/schema/cert-events.ts`(04.3-02 산출물 — `retention_years` · `signature_key` · `purged_at` · `closed_at` · `expires_at`)
    - `lib/storage/signature-store.ts`(04.3-02 · 05 — `getSignatureStore` · `delete`의 404 = 성공)
    - `domain/action-log/record.ts`의 세 배열(`CORE_ACTION_TYPES` · `ACTION_TYPE_LABELS` · `ALWAYS_ON_ACTION_TYPES` — 줄 번호 대신 `grep -n`으로 찾는다. 04.3-10의 `cert_unlock` 항목 바로 뒤에 더한다. 이 파일을 04.3-10도 고치므로 이 플랜은 웨이브 6에서 그 뒤에 돈다) · `test/unit/action-log/record.test.ts`
    - `domain/certs/review.ts`(04.3-07 — `purged_at` 판정 위치 · `revealRrn`의 `FOR SHARE` 잠금 읽기와 deps(기록 · 복호화 주입). 04.3-11은 의존 목록에 없어 그 함수에 기대지 않는다) · `domain/viewer.ts` 14행 `SYSTEM_VIEWER` · `lib/db-transaction.ts`
    - `db/schema/cert-winners.ts`의 확인 흔적 · 누적 잠김 칸(04.3-02 — `verify_idem_outcome` jsonb · `verify_proof_hash` · `verified_until` · `cumulative_failed_attempts` integer not null 기본 0 · `hard_locked_at` timestamp null) · 04.3-10의 잠금 풀기 리포지토리 함수(누적 잠김 칸을 되돌리는 set 절 모양 — 파기는 그 칸에 더해 확인 흔적 셋도 비운다)
    - `domain/action-log/record.ts`의 `recordAction(viewer, entry, deps?)`와 `RecordActionDeps`(Phase 4 04-32 — `tx`를 주면 로그 INSERT가 그 tx로 돈다 · `appendActionLog` 주입 자리 · 끌 수 없는 종류는 설정 조회를 건너뛴다) · `repositories/action-log.ts` `appendActionLog(viewer, entry, tx?)` — C6 · E3-02. `repositories/cert-action-log.ts`는 04.3-02의 대비책(04-32가 없을 때만 생기는 `insertActionLogTx` 하나)이고 이 플랜은 대비책일 때만 import한다(읽기만 — ② 순서 4, E3-02 단일 정책)
    - `test/e2e/helpers/cert.ts`의 `seedSubmittedCert()`(04.3-02 C4 — domain 확인 → 제출로 제출 표본 하나를 만든다, E3-32). 이 통합 테스트가 import하므로 그 파일이 `@playwright/test`를 import하지 않는지 먼저 본다 — import하면 04.3-02 SUMMARY가 적은 표본 함수 위치를 따르고 SUMMARY에 편차 한 줄을 남긴다
    - `repositories/cert-submissions.ts`의 C3 도우미(04.3-02 — `listStaleSignatureUploadIntents(viewer, olderThan)` · `deleteSignatureUploadIntent(viewer, objectKey, tx?)`) · 업로드 의도 표 `cert_signature_uploads`
    - `eslint/rules/repository-viewer-param.mjs` 29~35행(첫 인자가 이름 `viewer`인 식별자여야 한다 — 주석 예외 없음)
    - `test/integration/cert-submit.test.ts` · `test/integration/cert-review.test.ts`(제출 준비 함수 · 저장소 가짜 주입 방식)
  </read_first>
  <behavior>
    - `certPurgeDeadline(제출 2020-06-01 10:00 KST, 5)` → 2026-03-01 00:00 KST
    - KST 연도 경계: 제출 2020-12-31 23:30 KST(UTC 14:30) → 2026-03-01 · 제출 2021-01-01 00:10 KST(UTC 2020-12-31 15:10) → 2027-03-01
    - `unsubmittedRosterPurgeAt({closedAt: T, expiresAt: T+2일})` → T+30일 · `{closedAt: null, expiresAt: E}` → E+30일 · `{closedAt: E+1일, expiresAt: E}` → E+30일(이른 쪽)
    - (통합) now = 기한 − 1ms → 그 확인증 그대로 · now = 기한 → 확인증의 이름 · 주민 암호문 · 가린 값 · 연락처 · 주소가 null · `purged_at` = now · 확인증 번호 · 행사 · 제출 시각은 그대로 · 그 자리의 이름 · 전화 · 구별 표시 null · 두 표의 행 수 변화 없음
    - (통합 · 확인 흔적 · 누적 잠김, checker-B W2) 제출분 자리(`seedSubmittedCert()`의 domain 확인 → 제출 흐름으로 만들어 `verify_idem_outcome` · `verify_proof_hash` · `verified_until`이 채워진 상태 — E3-32)와 미제출 명단 자리(테스트가 db로 직접 `verify_idem_outcome` 한 항목 · `cumulative_failed_attempts = 20` · `hard_locked_at`을 채운 누적 잠김 상태)를 둘 다 파기 → 두 자리 모두 `verify_idem_outcome` · `verify_proof_hash` · `verified_until` · `hard_locked_at` · `locked_until`이 null이고 `cumulative_failed_attempts` · `failed_attempts`가 0(미제출 자리는 파기 전에 `failed_attempts = 4` · `locked_until` 미래 시각도 채워 둔다) · 기한 1ms 전 실행에서는 일곱 칸이 그대로
    - (통합) 보존 연수 사본: 확인(보존 5년 묶임) → 설정을 7로 바꿈 → 제출 → 확인증 `retention_years` = 5이고 기한도 5년 기준 · 제출 뒤 설정을 다시 바꿔도 기한 불변
    - (통합) 파일: 적용 뒤 저장소에 그 객체가 없고 `signature_key`가 null · 가짜 저장소의 delete가 실패하도록 주입 → 칸은 비워지고 `signature_key`는 남음 · 결과 `filesPending: 1` → 정상 저장소로 다시 실행 → 객체 지워지고 `signature_key` null · 새 파기 수 0
    - (통합) 멱등: 적용을 두 번 → 두 번째 `{submissions: 0, winners: 0}`
    - (통합) 미리 보기: `apply: false` → 대상 수만 돌려주고 DB · 저장소 · 행동 로그 변화 없음
    - (통합) 명단: 담당자가 T에 닫은 행사의 미제출 자리는 T+30일 − 1ms에 남고 T+30일에 비워짐 · 같은 행사의 제출된 자리는 이 규칙에 걸리지 않음 · 닫힌 적 없이 기한만 지난 행사는 기한 기준
    - (통합) 파기 뒤 `getSubmissionForReview` → `notFound` · `revealRrn` → 거부
    - (통합 · 경합, codex #13) ① `revealRrn`에 기록 함수를 주입해 기록 도중 멈추게 한다(잠금을 쥔 상태) → 같은 확인증이 기한을 넘긴 `now`로 `runCertPurge({apply: true})`를 시작 → 짧게 기다려도 파기가 끝나지 않는다 → 기록을 풀어 주면 전체 보기가 파기 전 번호를 돌려주고 그 뒤 파기가 끝나 칸이 비워진다 → 다시 `revealRrn` → 거부 · 복호화 0번. ② 반대 순서: 파기가 먼저 커밋 → `revealRrn`이 복호화 호출 0번으로 거부
    - (통합) 플래그 꺼짐에서도 파기가 돈다
    - (통합 · C6) 적용 실행 → `cert_purge` 1줄, detail = 개수 셋(`submissions` · `winners` · `filesQueued`)뿐 · 반환값은 다섯(`submissions` · `winners` · `filesDeleted` · `filesPending` · `orphansDeleted`) · 미리 보기 → 0줄 · 로그 INSERT가 던지도록 주입(`deps.appendActionLog` — `recordAction`의 주입 자리로 그대로 넘긴다) → `runCertPurge`가 던지고 확인증 · 자리 칸과 `purged_at`이 파기 전 그대로(되돌림 증명) · 저장소 `delete`가 한 번도 불리지 않음
    - (통합 · C3 고아, codex #12) 업로드 의도 행 둘: 25시간 전(저장소에 객체 있음) · 23시간 전(객체 있음) → 적용 → 첫째만 객체와 행이 지워지고 둘째는 그대로 · `orphansDeleted: 1` · 저장소 delete 실패 주입이면 행이 남고 `filesPending`에 센다 · 미리 보기는 수만 센다
    - (lint) `repositories/cert-purge.ts`의 export 함수가 모두 첫 인자 `viewer` — `pnpm lint`가 `repository-viewer-param`으로 확인
    - (단위) `cert_purge`가 `CORE_ACTION_TYPES` · `ALWAYS_ON_ACTION_TYPES`에 있고 라벨이 `확인증 파기`
  </behavior>
  <action>
① **RED:** 위 `behavior`의 순수 함수 케이스를 `test/unit/certs/purge-deadline.test.ts`, 로그 종류 케이스를 `test/unit/action-log/record.test.ts`, 나머지를 `test/integration/purge-expired-certs.test.ts`에 먼저 쓴다. 통합 테스트는 `now`와 저장소를 인자로 넣는다(시계를 흉내 내지 않는다). 제출 표본은 `seedSubmittedCert()`(04.3-02 C4)로만 만든다(E3-32 — 표본 만드는 길을 한 곳에 둔다). 예외는 보존 연수 사본 케이스 하나다 — 확인과 제출 사이에 설정을 바꿔야 해서 domain 확인 · 제출을 직접 부른다(SUMMARY에 그 이유 한 줄).

② **`domain/certs/purge.ts`:**
  - `certPurgeDeadline(submittedAt, retentionYears)` — 제출 시각을 KST로 읽은 연도 Y → `Y + 1 + retentionYears`년 3월 1일 00:00 KST(Date). `unsubmittedRosterPurgeAt({closedAt, expiresAt})` — 닫힌 시각과 기한 중 이른 것 + `UNSUBMITTED_ROSTER_RETENTION_DAYS`(= 30, 한국어 주석으로 「설계 `/cso`가 다시 본다」). 둘 다 순수 함수(시간대는 기존 `domain/certs/format.ts`의 KST 처리 방식을 따른다).
  - `runCertPurge({now, apply}, deps?)` — deps = 리포지토리 · 저장소 포트 · `appendActionLog`(기본값 없음 = `recordAction`의 기본 INSERT, 실패 주입용 — `recordAction`의 `deps.appendActionLog`로 그대로 넘긴다. 자체 tx 로그 도우미를 만들지 않는다 — 04.3-02의 대비 도우미는 순서 4의 대비책일 때만 import한다, E3-02). 리포지토리 호출의 viewer는 모두 `SYSTEM_VIEWER`. 기능 플래그를 보지 않는다. 순서:
    1. 거친 후보 조회 — 확인증: `purged_at IS NULL` + 제출 시각이 now − 1년보다 이름 · 미제출 자리: `submitted_at IS NULL` + `purged_at IS NULL` + 행사의 닫힌 시각 또는 기한이 now − 30일 이전 · 고아 의도: `listStaleSignatureUploadIntents(SYSTEM_VIEWER, now − 24시간)`(04.3-02 C3 도우미)
    2. 순수 함수로 확인증 · 자리 후보를 정확히 거른다
    3. `apply`가 거짓이면 개수 · 삭제 대기 파일 수 · 고아 의도 수만 돌려준다(쓰기 없음)
    4. 참이면 `withTransaction` 한 번: 확인증 칸 비우기(`purged_at IS NULL` 조건부 — 04.3-07 전체 보기의 `FOR SHARE` 잠금이 있으면 여기서 기다린다) · 그 확인증들의 자리 칸 비우기 · 미제출 자리 칸 비우기 · 같은 tx로 `recordAction(SYSTEM_VIEWER, {actionType: "cert_purge", entity: "cert_submission", detail: {submissions, winners, filesQueued}}, {tx, appendActionLog: deps?.appendActionLog})`(C6 · E3-02 — Phase 4 04-32의 `tx` 인자라 INSERT가 이 tx 연결로 돌고, 실패하면 비우기까지 롤백. `SYSTEM_VIEWER`라 `actorId`는 null) → 커밋. **대비책(E3-02 단일 정책 — 04.3-02 · 06 · 07 · 10과 같다, Round 2):** 실행 때 `grep -q 'tx?: DbOrTx' domain/action-log/record.ts`가 실패할 때만 세 번째 인자를 `{appendActionLog: deps?.appendActionLog ?? ((v, e) => insertActionLogTx(v, e, tx))}`로 바꾸고 04.3-02가 만든 `insertActionLogTx`를 `@/repositories/cert-action-log`에서 import한다(같은 tx · 같은 롤백 계약 · 주입 자리는 같은 `deps.appendActionLog`). 그 파일을 고치거나 같은 일을 하는 함수를 따로 만들지 않고, 그 파일이 없으면 멈추고 보고한다. 어느 쪽을 썼는지 SUMMARY에 한 줄
    5. 커밋 뒤 삭제 대기 파일 전부(`purged_at` 있음 + `signature_key` 있음 — 이번 실행 것과 지난 실패분)에 저장소 `delete` → 성공(없음 포함)이면 그 줄의 `signature_key`를 비움, 실패면 `filesPending`에 세고 계속
    6. 고아 의도마다 저장소 `delete` → 성공(없음 포함)이면 `deleteSignatureUploadIntent(SYSTEM_VIEWER, 키)`로 그 의도 행을 지움(`orphansDeleted`), 실패면 행을 남기고 `filesPending`에 센다(C3)
    7. `{submissions, winners, filesDeleted, filesPending, orphansDeleted}`를 돌려준다
    오류 메시지 · 로그에 이름 · 전화 · 주소 · 번호 · 객체 키를 넣지 않는다(id도 개수로만).

③ **`repositories/cert-purge.ts`(모든 export 함수의 첫 인자 `viewer` — `repository-viewer-param` 규칙, 호출자는 `SYSTEM_VIEWER`만. 트랜잭션을 받는 함수는 마지막 인자 `tx`):** 위 후보 조회 둘(확인증 · 미제출 자리 — 고아 의도 조회 · 행 삭제는 04.3-02 C3 도우미를 쓰고 이 파일에 두지 않는다), 칸 비우기 셋(**UPDATE로 칸만 비운다** — 확인증: 이름 · `rrn_encrypted` · `rrn_masked` · 연락처 · 주소 null, `purged_at`, `version + 1` / 자리(제출분 · 미제출 명단 두 함수 모두): 이름 · 전화 · 구별 표시 · `verify_idem_outcome` · `verify_proof_hash` · `verified_until` · `hard_locked_at` · `locked_until` null, `cumulative_failed_attempts` · `failed_attempts` 0, `purged_at` — checker-B W2 · 누적 잠금 결정 · checker-C INFO(짧은 잠김 두 칸도). 자리의 set 절 칸 목록은 두 함수가 모듈 안 상수 하나를 같이 쓴다), 삭제 대기 조회, `signature_key` 비우기(`purged_at IS NOT NULL` 조건). 머리 주석: 「파기 전용 · 권한 판정 없음 · domain/certs/purge.ts만 SYSTEM_VIEWER로 부른다」. 행을 지우는 문장을 쓰지 않는다.

④ **`domain/action-log/record.ts`(공유 등록부 — 항목만 끝에):** `CORE_ACTION_TYPES` 끝(04.3-10의 `cert_unlock` 뒤)에 `cert_purge`, `ACTION_TYPE_LABELS`에 `cert_purge: "확인증 파기"`, `ALWAYS_ON_ACTION_TYPES`에 `cert_purge` — 세 배열을 같은 커밋에서(한국어 주석 한 줄: CERT-02 파기 실행 기록은 끌 수 없다). `recordAction`은 `ALWAYS_ON_ACTION_TYPES`의 종류면 설정 조회를 건너뛰고 곧바로 쓰므로(`record.ts`의 `recordAction` 몸통) 이 배열 등록만으로 ② 순서 4의 `cert_purge` 쓰기가 tx 안에서 설정 표를 읽지 않고 통과한다.

⑤ **GREEN → 커밋** `feat(04.3): purge expired certificate personal data`, 본문 한국어(결정 ①~④ · ⑥~⑧ · 전체 보기와의 직렬화).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/certs/purge-deadline.test.ts test/unit/action-log/record.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 KST 연도 경계 케이스가 실패하면 UTC 연도로 계산했다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/purge-expired-certs.test.ts test/integration/cert-review.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 특히 행 수가 줄었거나 기한 1ms 전 줄이 비워졌으면 파기 계약 위반이고, 경합 케이스가 실패하면 파기와 전체 보기가 직렬화되지 않았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 특히 `repository-viewer-param`이 `repositories/cert-purge.ts`를 잡으면 viewer 첫 인자가 빠졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `repositories/cert-purge.ts`에 행 삭제 호출이 없다(`! grep -nE "db\.delete\(|tx\.delete\(|DELETE FROM" repositories/cert-purge.ts`)
    - `domain/certs/purge.ts`가 기능 게이트를 보지 않는다(`! grep -nE "isCertFeatureEnabled|assertCertFeatureEnabled" domain/certs/purge.ts`)
    - 통합 테스트가 두 표의 행 수 불변 · 기한 경계 두 쪽 · 파일 대기 재시도 · 멱등 · 미리 보기 무변화 · 로그 detail 개수만 · 로그 실패 시 비우기 롤백(C6) · 고아 의도 둘(C3) · 파기와 전체 보기 경합 두 순서를 단언한다
    - 통합 테스트가 파기된 제출분 · 미제출 자리 둘 다에서 `verify_idem_outcome` · `verify_proof_hash` · `verified_until` · `hard_locked_at` · `locked_until` null과 `cumulative_failed_attempts` · `failed_attempts` 0을 단언한다(checker-B W2 · checker-C INFO) · `repositories/cert-purge.ts`에 일곱 칸의 Drizzle 필드 이름(저장소 관례 camelCase)이 모두 있다(`for c in verifyIdemOutcome verifyProofHash verifiedUntil cumulativeFailedAttempts hardLockedAt failedAttempts lockedUntil; do grep -q "$c" repositories/cert-purge.ts || exit 1; done`)
    - `record.ts`의 `cert_purge`가 `cert_unlock` 뒤에 있다(04.3-10 뒤 웨이브 6 — 같은 파일을 두 플랜이 같은 웨이브에서 고치지 않는다)
    - `record.ts`의 세 배열에 `cert_purge`가 함께 있다(typecheck 통과가 라벨 누락을 잡는다)
    - 파기 로그가 Phase 4 `recordAction(…, {tx})` 하나로 쓰인다(E3-02): `grep -n "recordAction(" domain/certs/purge.ts`가 1줄 이상이고 `! grep -rn "append[C]ertActionLogTx" domain/certs/purge.ts repositories/cert-purge.ts test/integration/purge-expired-certs.test.ts`(07의 옛 사본 도우미 이름 0곳) · `repositories/cert-action-log.ts` 밖의 tx 로그 도우미 정의 0(`test "$(grep -rlE 'export (async )?function [A-Za-z]*ActionLogTx' domain repositories | grep -v 'repositories/cert-action-log.ts' | wc -l)" = 0`) · `record.ts`에 `tx?: DbOrTx`가 있으면 `domain/certs/purge.ts`가 02 대비 도우미를 import하지 않는다(`{ ! grep -q 'tx?: DbOrTx' domain/action-log/record.ts || ! grep -v '^\s*//' domain/certs/purge.ts | grep -q 'insert[A]ctionLogTx'; }` — Round 2 단일 정책)
    - 제출 표본이 `seedSubmittedCert()`에서 온다(E3-32): `grep -n "seedSubmittedCert" test/integration/purge-expired-certs.test.ts`가 1줄 이상
  </acceptance_criteria>
  <done>보존 기한이 지난 확인증과 닫힌 지 30일 지난 미제출 명단의 개인정보가 행을 남긴 채 비워지고, 서명 파일은 DB 뒤에 지워지며 실패분은 다음 실행이 마저 지운다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 파기 CLI(기본 미리 보기 · `--apply`) + CLI 번들 등록</name>
  <files>scripts/purge-certs.ts, scripts/build-cli.mjs, package.json, test/unit/purge-certs-cli.test.ts, test/unit/deploy/cli-bundle.test.ts</files>
  <read_first>
    - `scripts/rotate-key.ts`(전문 — 인자 규약 · `UsageError` · 직접 실행 판정 · `closeDb` · `log.info` · 종료 코드)
    - `scripts/build-cli.mjs`(전문 — `entryPoints` · `outputs` 두 배열 · next import 검사)
    - `test/unit/deploy/cli-bundle.test.ts`(전문 — `cliEntriesCalledByDeploy`가 `--args=dist/cli/…`만 읽는다) · `test/unit/account-cli.test.ts` 1~30행(CLI 인자 단위 테스트 모양)
    - `package.json` scripts의 `db:rotate-key` 줄(실행 방식 선례)
  </read_first>
  <behavior>
    - `parseArgs([])` → `{apply: false}` · `parseArgs(["--apply"])` → `{apply: true}`
    - `parseArgs(["--apply=true"])` → `UsageError`(등호 결합 거부) · `parseArgs(["--force"])` → `UsageError`(모르는 플래그)
    - cli-bundle: deploy.sh가 `--command=node,dist/cli/<이름>.mjs` 형태로 부르는 진입점도 번들 outputs에 있어야 한다(account · purge-certs 둘 다 잡힌다)
  </behavior>
  <action>
① **RED:** `test/unit/purge-certs-cli.test.ts`에 위 `parseArgs` 케이스, `test/unit/deploy/cli-bundle.test.ts`에 `--command=node,dist/cli/…` 형태를 읽는 파서 확장과 「`dist/cli/purge-certs.mjs`가 번들에 있다」 케이스를 먼저 쓴다(기존 케이스·문장은 그대로).

② **`scripts/purge-certs.ts`:** `rotate-key.ts`와 같은 모양 — `UsageError` · `parseArgs(argv)`(등호 결합 거부 · `--apply`만 인식 · 그 밖의 `--` 토큰은 거부) · 직접 실행일 때만 `runCertPurge({now: new Date(), apply})` → `log.info("purge_certs.done", {mode: "dry-run" | "apply", ...반환 개수 다섯})` → `closeDb` → 종료 0, 실패는 메시지만 stderr(개인정보 없음) → 종료 1. 파일 머리 한국어 주석: 기본이 미리 보기인 이유(RESEARCH 보안 표 「dry-run 필수」) · 실행 주기는 OPERATIONS §14.

③ **`scripts/build-cli.mjs`:** `entryPoints`에 `scripts/purge-certs.ts`, `outputs`에 `dist/cli/purge-certs.mjs`를 같은 순서로 더하고 머리 주석의 Job 개수 문장을 고친다.

④ **`package.json`:** `db:rotate-key`와 같은 방식의 스크립트 `certs:purge`(로컬 미리 보기용 — 적용은 `pnpm certs:purge -- --apply`).

⑤ **GREEN → 커밋** `feat(04.3): certificate purge CLI with dry-run default`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/purge-certs-cli.test.ts test/unit/deploy/cli-bundle.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다</fails_when>
    <automated>pnpm build:cli</automated>
    <fails_when>종료 코드가 0이 아니거나 「imports "next"」 오류가 나온다 — 파기 CLI가 Next.js에 기대면 Job에서 돌지 않는다</fails_when>
  </verify>
  <acceptance_criteria>
    - `dist/cli/purge-certs.mjs`가 빌드 뒤 존재한다(`test -f dist/cli/purge-certs.mjs`)
    - `parseArgs`가 인자 없음을 미리 보기로 읽는다(단위 테스트)
    - cli-bundle 테스트가 `--command=node,dist/cli/…` 형태를 읽는다
  </acceptance_criteria>
  <done>파기 CLI가 번들에 들어가고, 인자 없이 돌리면 아무것도 바꾸지 않는다</done>
</task>

<task type="auto">
  <name>Task 3: Cloud Run Job `purge-certs`(사람이 실행) + 배포 테스트 + OPERATIONS 런북</name>
  <files>scripts/deploy.sh, test/unit/deploy/deploy-sh.test.ts, docs/OPERATIONS.md</files>
  <read_first>
    - `scripts/deploy.sh` 305~356행(`deploy_jobs` · `job_common` · account Job의 `--command=node,dist/cli/…` 선례와 그 주석) · 04.3-05가 더한 `cert_bucket` 사용처 · 04.3-08이 더한 KMS 줄(데이터 키가 서비스에만 가는지)
    - `infra/names.sh`의 `job_name` · `cert_bucket`(04.3-05)
    - `test/unit/deploy/deploy-sh.test.ts` 91~95행(`jobLine`) · 380~415행(「Job 4개」 케이스)
    - `docs/OPERATIONS.md` §4 배포 런북 · §7 계정 운영(Job 실행 명령 모양) · §8 최초 1회 부트스트랩(04.3-05가 고친 순서 — 머지 전 소유자 실행, E3-07) · 마지막 절 뒤 끝(`grep -n '^## ' docs/OPERATIONS.md | tail -1` — 지금 main은 §13까지지만 남은 Phase 4 플랜이 절을 더할 수 있어 번호를 전제하지 않는다, A4) · `test/unit/docs-limits.test.ts`(300줄 상한 · 필수 토큰)
    - `scripts/bootstrap-gcp.sh`의 04.3-05 절(런타임 서비스 계정에 서명 버킷의 객체 권한을 주는 줄 — 파기 Job의 `delete`가 이 권한으로 돈다)
    - (AX-3) `scripts/deploy.sh` `ensure_sql_instance`의 `gcloud beta sql instances create` 인자(`--backup` · `--retained-backups-count=7`) — 줄 번호가 아니라 함수 이름으로 찾는다. 파기 뒤에도 자동 백업에 비운 값이 남는 기간의 근거 · `--enable-point-in-time-recovery`가 없는지(교차 r1 참고 7 — Phase 4 판에는 없다)
  </read_first>
  <action>
① **RED 먼저(codex final C6 · CLAUDE.md §5) — `test/unit/deploy/deploy-sh.test.ts`:** ②를 쓰기 **전에** 「Job 4개」 케이스를 Job 5개로 고치고(케이스 이름 문장도), `purge-certs` 줄이 `--command=node,dist/cli/purge-certs.mjs` · `CERT_SIGNATURE_BUCKET=` · `BETTER_AUTH_SECRET=`을 갖고 `APP_DATA_KEY` · `DB_ADMIN_PASSWORD` · `--args=`를 갖지 않으며, 로그에 `run jobs execute plant8-staging-purge-certs`가 없음을 단언한다. `pnpm vitest run --project unit test/unit/deploy/deploy-sh.test.ts`를 돌려 새 단언이 `purge-certs` Job 줄 없음으로 실패하고 다른 케이스는 녹색인지 확인한다(실패 이유 한 줄을 SUMMARY에).

② **`scripts/deploy.sh` `deploy_jobs`(GREEN):** account Job 뒤에 `run gcloud run jobs deploy "$(job_name "$ENV" purge-certs)"` — `job_common` · `--command=node,dist/cli/purge-certs.mjs`(인자를 `execute --args=--apply`로 줄 수 있게 account 선례와 같은 모양) · `--set-env-vars="${common_env},CERT_SIGNATURE_BUCKET=$(cert_bucket "$ENV" "$PROJECT")"` · `--set-secrets`는 `BETTER_AUTH_SECRET`만(데이터 키 · 감싼 키 · KMS 키 이름을 주지 않는다 — 파기는 복호화하지 않는다). 한국어 주석 두 줄: 배포는 Job을 만들기만 하고 실행하지 않는다 · 실행 절차는 OPERATIONS 「확인증 파기」 절. `jobs execute`를 더하지 않는다. ①의 테스트를 다시 돌려 녹색을 본다.

③ **`docs/OPERATIONS.md` 끝에 새 절 `## {N}. 확인증 파기 (CERT-02)` — N = 실행 때 main을 머금은 OPERATIONS의 마지막 절 번호 + 1(`grep -oE '^## [0-9]+' docs/OPERATIONS.md | tail -1`로 찾는다 — 번호를 미리 정하지 않는다, A4):** 무엇을 지우고 무엇을 남기는지 한 줄 · 기한 계산 한 줄(제출 연도 + 1 + 보존 연수, 3월 1일 KST · 미제출 명단은 닫힌 뒤 30일) · 실행: 매월 첫 영업일과 3월 1일 뒤 첫 영업일에 `gcloud run jobs execute {job_name} --region … --wait`로 미리 보기 → 로그 `purge_certs.done`의 개수를 확인 → `--args=--apply`로 적용 · `filesPending`이 0이 아니면 다음 실행이 다시 지운다(파기된 확인증의 서명과 24시간 넘은 업로드 의도의 고아 서명 둘 다 — `orphansDeleted`) · 결과는 행동 로그 `확인증 파기`(비운 개수)와 Job 로그 `purge_certs.done`(파일 결과)에서 본다 · **실행 누락 감시(E3-16):** 마지막 `purge_certs.done`(또는 `gcloud run jobs executions list --job {job_name} --region … --limit 1`의 마지막 실행)이 35일보다 오래됐으면 한 달 실행을 빠뜨린 것이다 — 곧바로 미리 보기부터 돌린다 · **첫 적용 전 확인(E3-16):** 이 Job을 처음 `--apply`로 돌리기 전에 소유자가 04.3-05의 절이 든 `scripts/bootstrap-gcp.sh`를 그 환경에 돌렸는지(머지 전 PR 브랜치에서 한 번 — OPERATIONS §8 · E3-07. 런타임 서비스 계정의 서명 버킷 객체 삭제 권한이 여기서 생긴다 — 없으면 `filesPending`만 쌓인다) 확인하고, 기록이 없으면 다시 돌린다 · 경영관리 직원에게는 권한표에서 `확인증 제출 내용` 보기·쓰기와 정보 항목 둘(주민등록번호 전체 · 확인증 제출 정보)을 켠다(시드 계급이 아니다). **실행 주기 확정(E3-16 교차 B-5 (나)안 — 설계 `/cso`):** 절 본문에 문장 그대로 「사람 월 1회 + 35일 감시 런북 유지, Scheduler 자동화는 Phase 7 이관」을 넣는다(Cloud Scheduler 관련 실행 단계는 이 절에 두지 않는다). **백업과 복원(AX-3) — 두 줄을 넣는다:** ① 「백업 보관 7일이 지나야 완전 파기」 — 파기는 라이브 DB의 칸을 비우지만 Cloud SQL 자동 백업(하루 한 번 · 7개 보관, `deploy.sh` `ensure_sql_instance`)에는 비운 값이 남는다(데이터 키도 남아 풀린다). **PITR은 확인된 경우에만 적는다(교차 r1 참고 7):** Phase 4 `ensure_sql_instance`에는 `--enable-point-in-time-recovery`가 없다 — 실행자는 `grep -n 'point-in-time' scripts/deploy.sh`로 다시 보고, 두 환경을 `gcloud sql instances describe {인스턴스} --project … --format='value(settings.backupConfiguration.pointInTimeRecoveryEnabled)'`로 볼 수 있으면 결과를 SUMMARY에 적는다(권한이 없으면 「미확인」). 켜져 있다고 확인된 환경이 있을 때만 ①에 「PITR 로그도」 한 마디를 더한다 ② 「백업 복원 뒤에는 곧바로 `purge-certs --apply`를 한 번 돌린다」 — 파기 전 시점으로 복원하면 파기된 행이 되살아나고 서명 객체는 이미 없는데 `signature_key`만 남으므로, 곧바로 적용 실행 한 번으로 기한이 지난 행을 다시 비운다(없는 객체는 이미 지워진 것으로 친다 — 이 플랜 truths). **OPERATIONS 줄 예산(검사기 r1 W1 — 네 플랜 합 30줄: 04.3-05 +5 · 08 +5 · 12 +18 · 13 +2, 각 플랜 끝 상한선 = 300 − 뒤 플랜 예산 합):** 이 플랜은 순증 **+18줄 이하**(절 앞 빈 줄 1 · 제목 1 · 빈 줄 1 · 본문 15)이고, 끝난 뒤 `wc -l docs/OPERATIONS.md`의 줄 수가 **298 이하**다(04.3-13의 2줄을 남긴다 · verify 마지막 줄). 줄 수는 `wc -l`로 재고, 줄 수를 맞추려 한 줄에 몰아 쓰지 않는다(기존 절과 같은 폭 — 약 90칸에서 줄바꿈). **본문 15줄의 틀(이 순서 · 이 줄 수 이하):** ⓐ 지우는 것 · 남기는 것 + 기한 계산 2줄 · ⓑ 실행(미리 보기 → `purge_certs.done` 개수 확인 → `--args=--apply`) · `filesPending` 재시도(`orphansDeleted`) · 결과 보는 곳 3줄 · ⓒ 실행 누락 감시(35일) 2줄 · ⓓ 첫 적용 전 `bootstrap-gcp.sh` 확인 + 경영관리 직원 권한 3줄 · ⓔ 실행 주기 확정 문장 1줄 · ⓕ 백업 보관 7일 2줄 · ⓖ 복원 뒤 `--apply` 2줄(「복원」과 `--apply`는 같은 줄). **시작 줄 수 + 18이 298을 넘으면(main이 Phase 4 뒤 늘었거나 앞 플랜이 예산을 덜 남겼다) 이 순서로만 줄이고, 맞으면 거기서 멈춘다:** ① ⓑ의 「결과 보는 곳」 문구를 뺀다(실행 줄의 `purge_certs.done`이 이미 가리킨다 — 행동 로그 `확인증 파기`는 ⓐ 끝에 두 낱말로) ② ⓓ의 경영관리 직원 권한을 「권한: `확인증 제출 내용` 보기·쓰기 + 정보 항목 둘(시드 계급 아님)」로 줄인다 ③ ⓕ의 괄호(하루 한 번 · 7개 · 함수 이름)를 뺀다. **지키는 줄(줄이지 않는다):** ⓐ 기한 계산 규칙 · ⓑ 실행 명령 모양 · ⓒ 「35일」 · ⓓ `bootstrap-gcp.sh` · ⓔ 「사람 월 1회 + 35일 감시 런북 유지, Scheduler 자동화는 Phase 7 이관」 · ⓕ 「백업 보관 7일이 지나야 완전 파기」 · ⓖ 「복원」 + `--apply` 같은 줄(verify 둘째~넷째 줄이 찾는다). **12는 main 절 · 04.3-05 · 08이 더한 줄을 줄이지 않는다**(E3-01 · 04.3-13 acceptance — 줄였다면 이 페이즈 절뿐). ③까지 줄여도 넘으면 멈추고 시작 · 끝 줄 수를 SUMMARY에 적어 오케스트레이터에 보고한다 — 즉석으로 다른 절을 깎지 않는다.

④ 커밋 `feat(04.3): purge-certs Cloud Run Job and runbook`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/deploy-sh.test.ts test/unit/deploy/cli-bundle.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — Job 줄에 데이터 키가 붙었거나 OPERATIONS가 300줄을 넘었다</fails_when>
    <automated>test "$(grep -cE '^## [0-9]+\. 확인증 파기' docs/OPERATIONS.md)" = 1 &amp;&amp; grep -oE '^## [0-9]+\.' docs/OPERATIONS.md | tr -dc '0-9\n' | awk 'NR>1 &amp;&amp; $1!=p+1{exit 1}{p=$1}' &amp;&amp; awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -q "35일" &amp;&amp; awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -q "bootstrap-gcp.sh"</automated>
    <fails_when>종료 코드가 0이 아니다 — 「확인증 파기」 절이 없거나 둘이거나, 절 번호가 앞 절 + 1이 아니어서(main 마지막 번호를 보지 않고 번호를 박았다 — A4) 번호가 비거나 겹치거나, 그 절 런북에 실행 누락 감시(35일) 줄이나 첫 적용 전 bootstrap 확인 줄이 없다(E3-16)</fails_when>
    <automated>awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -q "Phase 7" &amp;&amp; test "$(grep -v '^\s*#' scripts/deploy.sh | grep -ciE 'cloudscheduler|scheduler jobs')" = 0</automated>
    <fails_when>종료 코드가 0이 아니다 — 「확인증 파기」 절에 Scheduler 자동화를 Phase 7로 이관한다는 문장이 없거나, `deploy.sh`에 Cloud Scheduler 자원·API가 이 페이즈의 단계로 들어갔다(E3-16 교차 B-5 (나)안 — 사람 실행만, Scheduler 없음)</fails_when>
    <automated>awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -q "백업 보관 7일" &amp;&amp; awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep "복원" | grep -q -- "--apply"</automated>
    <fails_when>종료 코드가 0이 아니다 — 「확인증 파기」 절에 「백업 보관 7일이 지나야 완전 파기」 줄이 없거나, 백업 복원 뒤 곧바로 `purge-certs --apply`를 한 번 돌린다는 줄(「복원」과 `--apply`가 같은 줄)이 없다(AX-3)</fails_when>
    <automated>test "$(cat docs/OPERATIONS.md | wc -l)" -le 298 &amp;&amp; test "$(awk '/^## [0-9]+\. 확인증 파기/{f=1} /^## /&amp;&amp;!/확인증 파기/{f=0} f' docs/OPERATIONS.md | wc -l)" -le 17</automated>
    <fails_when>종료 코드가 0이 아니다 — OPERATIONS가 이 플랜 끝 상한선 298줄을 넘어 04.3-13의 2줄을 먹었거나, 「확인증 파기」 절(제목부터 다음 절 전까지)이 17줄(제목 · 빈 줄 · 본문 15)을 넘었다(검사기 r1 W1 — ③의 본문 틀이나 줄이기 순서 ①~③을 따르지 않았다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy.sh`에 `purge-certs` Job 배포 줄이 있고 그 Job의 실행 줄은 없다(deploy-sh 테스트 단언) · `cloudscheduler` API·자원 관련 줄이 없다(E3-16 — Scheduler는 이 페이즈 단계가 아니다)
    - `docs/OPERATIONS.md`의 마지막 절이 `## {main 마지막 번호 + 1}. 확인증 파기`이고(verify 둘째 줄 — 절 하나 · 번호 연속) 300줄 이하다(verify 첫 줄의 `docs-limits.test.ts`)
    - 「확인증 파기」 절에 실행 누락 감시 줄과 첫 적용 전 bootstrap 재실행 확인 줄이 있다(E3-16): `awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -c "35일"`이 1 이상이고 `awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -c "bootstrap-gcp.sh"`가 1 이상
    - 「확인증 파기」 절에 「사람 월 1회 + 35일 감시 런북 유지, Scheduler 자동화는 Phase 7 이관」 문장(그대로) 또는 같은 뜻이 있다(설계 `/cso` 교차 B-5 (나)안 확정): `awk '/^## [0-9]+\. 확인증 파기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -c "Phase 7"`이 1 이상
    - SUMMARY에 ① RED 실행(`purge-certs` Job 줄 없음으로 실패)이 ② 구현보다 먼저 기록돼 있다(codex final C6)
    - (AX-3) 「확인증 파기」 절에 「백업 보관 7일이 지나야 완전 파기」 줄과 「백업 복원 뒤에는 곧바로 `purge-certs --apply`를 한 번」 줄이 있다(verify 넷째 줄 — 절 안에서만 찾는다) · PITR 사용 여부(두 환경 · 또는 「미확인」)가 SUMMARY에 있고 문서의 PITR 문구는 켜졌다고 확인된 경우에만 있다(교차 r1 참고 7)
    - (검사기 r1 W1) OPERATIONS가 이 플랜 끝에 298줄 이하이고 「확인증 파기」 절(제목부터 끝까지)이 17줄 이하다(verify 마지막 줄) · 시작 · 끝 줄 수와 줄이기 ①~③을 썼는지가 SUMMARY에 있다
  </acceptance_criteria>
  <done>스테이징·프로덕션에 데이터 키 없는 파기 Job이 만들어지고, 사람이 런북대로 미리 보기 → 적용을 실행할 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파기 Job → DB · 버킷 | 사람이 실행하는 시스템 작업 — 런타임 서비스 계정의 권한(DB · 그 버킷의 objectUser)만 쓴다 |
| DB 커밋 ↔ 파일 삭제 | 두 저장소가 따로 커밋된다 — 사이에서 실패하면 파일이 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-57 | Tampering | 파기가 세무 기록까지 지움 | high | mitigate | UPDATE로 개인정보 칸만 비우고 행 · 확인증 번호 · 연결 칸을 남긴다. 리포지토리에 행 삭제 문장 없음(수락 기준 grep) · 통합 테스트 행 수 불변 |
| T-04.3-58 | Information Disclosure | 보존 기간을 넘긴 개인정보 | medium | mitigate | 기한 순수 함수 + 경계 테스트 · 동의 때의 보존 연수 사본 · 플래그와 무관하게 실행. **사람 의존, Phase 7에서 자동화**(E3-16 교차 B-5 (나)안 확정, Phase 4 D19-8과 일치) — 사람이 실행하는 월 1회 주기라 최대 한 달 늦을 수 있고, 실행을 잊으면 알림 없이 남는다. 이 플랜은 런북에 「마지막 `purge_certs.done`이 35일보다 오래됐으면 확인」 · 「첫 적용 전 bootstrap 재실행 확인」 두 줄을 둔다. 자동 감시(시스템 상태 화면의 마지막 `cert_purge` 시각 · 알림)와 Cloud Scheduler 기반 자동 실행은 이 페이즈 범위 밖이며 Phase 7 스케줄러 기반으로 이관한다(이 저장소에 아직 스케줄러가 없다 — bootstrap에 API·SA가 없음) |
| T-04.3-59 | Information Disclosure | 파기 뒤 남은 서명 파일 · DB에 없는 고아 서명 | medium | mitigate | DB 먼저 · 파일 나중, 삭제 대기 표시(`signature_key`)로 다음 실행이 다시 지우고 결과에 `filesPending`을 보인다. 업로드 의도 표(C3)에 24시간 넘게 남은 객체를 지우고 실패하면 행을 남겨 다시 지운다(통합 테스트). 버킷은 소프트 삭제 0 · 버전 관리 없음(04.3-05). **백업(AX-3):** 파기는 라이브 DB 기준이라 Cloud SQL 자동 백업(7개 · 하루 한 번 — PITR을 켠 환경이면 그 로그도, Phase 4 `ensure_sql_instance`는 켜지 않는다 · 교차 r1 참고 7)에는 비운 개인정보가 남는다 — 런북에 ① 「백업 보관 7일이 지나야 완전 파기」(잔여 위험으로 받아들임 — 백업을 줄이거나 끄지 않는다) ② 「백업 복원 뒤 곧바로 `purge-certs --apply` 한 번」(복원으로 되살아난 기한 지난 행을 다시 비우고, 없는 서명 객체는 이미 지워진 것으로 친다). 두 줄은 Task 3 verify가 절 안에서 확인한다 |
| T-04.3-90 | Information Disclosure | 파기와 전체 보기 경합 | medium | mitigate | 전체 보기의 `FOR SHARE` 잠금(04.3-07)과 파기의 조건부 UPDATE가 행 잠금으로 직렬화된다 — 두 순서의 경합 통합 테스트 |
| T-04.3-60 | Elevation of Privilege | 파기 Job 권한 | medium | mitigate | Job은 데이터 키 · 감싼 키 · KMS 이름을 받지 않는다(deploy-sh 단언). 버킷 권한은 04.3-05의 버킷 수준 objectUser 그대로 |
| T-04.3-61 | Information Disclosure | 파기 실행 기록 | low | mitigate | 행동 로그 detail과 서버 로그는 개수 넷뿐(통합 테스트) |
| T-04.3-131 | Information Disclosure | 파기 뒤 자리에 남는 확인 흔적 · 누적 잠김 | medium | mitigate | 자리 비우기 두 함수가 같은 칸 목록으로 `verify_idem_outcome`(IP 해시) · `verify_proof_hash` · `verified_until` · `hard_locked_at` · `locked_until`을 null로, `cumulative_failed_attempts` · `failed_attempts`를 0으로 만든다 — 파기된 자리에 확인 시도 흔적이 남지 않고 「잠금 풀기」 대상에서도 빠진다. 제출분 · 미제출 두 자리의 통합 단언 |
| T-04.3-62 | Repudiation | 누가 언제 파기했나 | low | mitigate | 적용 실행마다 끌 수 없는 `cert_purge` 한 줄을 칸 비우기와 같은 트랜잭션으로 — Phase 4 `recordAction(…, {tx})`(C6 · E3-02 — 로그 실패 시 비우기 롤백 테스트) + Cloud Run Job 실행 기록(누가 execute했는지는 GCP 감사 로그) |
</threat_model>

<verification>
- 단위 `purge-deadline` · `record` · `purge-certs-cli` · `cli-bundle` · `deploy-sh` · `docs-limits` 녹색
- 통합 `purge-expired-certs` · `cert-review` 녹색
- `pnpm build:cli` 성공, `pnpm lint && pnpm typecheck` 녹색
</verification>

<success_criteria>
- 기한 정각에 개인정보가 비워지고 1ms 전에는 남는다
- 행과 세무 칸은 남고, 서명 파일은 DB 뒤에 지워지며 실패분은 재실행이 마저 지운다
- 파기된 자리에는 확인 흔적(멱등 결과 맵 · 증표 · 증표 기한)과 누적 잠김이 남지 않는다
- 24시간 넘은 업로드 의도의 고아 서명이 지워지고, 파기 로그는 비우기와 한 트랜잭션이다
- 파기와 전체 보기가 직렬화되고, 파기 리포지토리가 lint 규칙을 통과한다
- CLI는 기본이 미리 보기이고, Job은 데이터 키 없이 만들어져 사람이 런북대로 실행한다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| domain 함수 | `certPurgeDeadline` · `unsubmittedRosterPurgeAt` · `runCertPurge`(`domain/certs/purge.ts`) |
| 상수 | `UNSUBMITTED_ROSTER_RETENTION_DAYS = 30`(설계 `/cso` 확인 대상) |
| 행동 로그 종류 | `cert_purge` · 라벨 `확인증 파기`(끌 수 없음) |
| CLI · 스크립트 | `scripts/purge-certs.ts` · `dist/cli/purge-certs.mjs` · `pnpm certs:purge` |
| Cloud Run Job | `{접두어}-{환경}-purge-certs`(배포는 만들기만, 실행은 사람) |
| 문서 | `docs/OPERATIONS.md` 마지막 절 「확인증 파기」(번호 = main 마지막 절 + 1) |
| 테스트 | `test/integration/purge-expired-certs.test.ts` · `test/unit/certs/purge-deadline.test.ts` · `test/unit/purge-certs-cli.test.ts` |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(plan-eng-review 2026-09-25) — 반영 지시 L387–455@953e599(공통 L391–394, 04.3-12 항목 L444–447), 사용자 확인 L457–468@953e599. 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-02 tx 로그 도우미가 둘(06의 tx 로그 INSERT 도우미 · 07의 사본 도우미)이고 07 쪽은 `recordAction`을 다시 구현한 사본 | P2 | 결정 ⑥ · truths · key_links · Task 1 read_first · ② deps(`appendActionLog` 주입만) · 순서 4가 `recordAction(SYSTEM_VIEWER, …, {tx, appendActionLog})` · ④(always-on은 설정 조회 없음) · Task 1 수용 줄(`recordAction(` 있음 · 두 도우미 이름 0곳) · T-04.3-62 |
| E3-16 파기 Job을 사람이 월 1회 실행 — 잊으면 알림 없이 남고, 런타임 SA 버킷 삭제 권한 전제(bootstrap 재실행)가 없다 | P3 | Task 3 read_first(§8 · bootstrap 05 절) · ③ 런북 두 줄(35일 감시 · 첫 적용 전 bootstrap 확인) · Task 3 수용 줄 + `<automated>` awk 검사 · T-04.3-58에 「→ 설계 /cso에서 확정」 |
| E3-32 표본은 `seedSubmittedCert()` | P2 | Task 1 read_first · ① RED(표본은 도우미로만 · 보존 연수 사본 케이스만 예외와 이유) · behavior 확인 흔적 케이스 · Task 1 수용 줄 grep |
| E3-01 Phase 4 머지 뒤 시작 · 웨이브 첫 단계 main 머지 · Phase 4 부품 기준 | P1 | objective 「실행 조건」 · Task 1 `<precondition>` |
| E3-33 통합 · E2E는 한 번에 하나 | P3 | objective 「실행 조건」 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| E3-04 12 · 08의 범위와 시점(12를 미제출 명단 파기 + 미리 보기 CLI로 줄이기) | deferred — user check pending (default kept). 기본값 「현 계획 유지」라 범위를 줄이지 않는다. 사용자가 줄이기를 고르면 이 플랜을 다시 계획한다 |
| E3-03 서명 이미지 저장 위치(GCS 대 DB bytea) | deferred — user check pending (default kept). 기본값 GCS 유지. DB를 고르면 「DB 먼저 · 파일 나중」 · 삭제 대기 표시 · C3 고아 정리가 사라져 이 플랜을 다시 계획한다 |
| E3-16 자동 감시(시스템 상태 화면에 마지막 `cert_purge` 시각 · 알림) | TODO/후속 — 이 페이즈는 런북 두 줄까지. 자동화 여부와 주기는 설계 `/cso`(D-1107)에서 확정(T-04.3-58) |

### Round 2 — 72829e6

> 출처: 04.3 eng-apply 재검토 r1 — Opus 플랜 체커(`eng-apply-checker-r1`, W/I 번호) · 교차 검토(`eng-apply-opus-cross-r1`, X/A 번호). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W1 · 교차 A3 — frontmatter `execution_gate` 없음 | warning | 01~09와 같은 `execution_gate` 블록을 frontmatter에 복사했다 |
| 체커 I1 · 교차 A2 — tx 로그 부재 정책 갈림 | info | 오케스트레이터 결정(04.3-02 `insertActionLogTx` 대비책 하나로 통일): Task 1 `<precondition>`에서 `tx?: DbOrTx` 멈춤을 빼고 ② deps 설명 · 순서 4에 대비책(같은 tx · 같은 주입 자리, 대비 파일이 없으면 멈춤) · read_first(대비책일 때만 import) · acceptance를 「옛 사본 이름 0 · 대비 파일 밖 정의 0 · `tx`가 있으면 대비책 import 0」으로 바꿨다 |
| 교차 A4 — OPERATIONS 「## 14.」 · 「§13 뒤」 하드코딩(Phase 4가 §14를 쓰면 엉뚱한 절을 봄) · `record.ts` 「10~75행」 | advisory | Task 3 read_first · ③을 「끝에 새 절 `## {N}. 확인증 파기` — N = main 마지막 절 번호 + 1」로, verify 둘째 줄을 절 제목 grep(절 하나 · 번호 연속 · 그 절 안의 35일 · bootstrap 줄)로, acceptance · 산출물 표 · ② 주석의 「§14」를 절 제목으로 바꿨다. 300줄 상한은 verify 첫 줄의 `docs-limits.test.ts`가 그대로 판정한다. Task 1 read_first의 `record.ts` 줄 번호는 배열 이름 grep으로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 3 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review 2026-09-25 — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3 · D-7 · D-10). 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| (없음) | — | 이 라운드에서 이 플랜을 고친 지적은 없다(화면 없는 파기 Job 플랜) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| D-15 (N3) 대비책 문장(Task 1 `<precondition>` · ② deps · 순서 4의 04.3-02 ⑨-b `insertActionLogTx` 대비책) — Phase 4 `record.ts`에 `tx?: DbOrTx`가 이미 있어 쓰이지 않는 경로(L125–126@1c3a27b) | 보고서 결정 「편집 없음, 기록만」 — E3-02 단일 정책의 일관성을 위해 그대로 둔다 |
| D-1 ~ D-14 · D-16 · X-1 ~ X-12 | 이 플랜의 태스크 · 문구에 닿는 것이 없다(UI 문구 · 확인 부품 · 편집 표 없음) |

### Round 4 — 184df28

> 출처: `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`(설계 `/cso` 최종 판정 2026-09-25 — 최종 판정 표 E3-16, 항목별 근거 요약, 반영할 플랜 목록 04.3-12). 설계 `/cso` 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-16 교차 B-5 (나)안 채택 — 이 페이즈는 사람 월 1회 + 35일 감시 런북 유지, Scheduler 자동화는 Phase 7로 이관(Phase 4 D19-8과 일치). Cloud Scheduler 자동 실행 단계를 두지 않는다 | medium | truths(50행) · Task 3 ③ OPERATIONS 절 본문에 확정 문장 추가 · verify 신규(Phase 7 문구 · `deploy.sh`에 Scheduler 자원 없음) · acceptance 둘 · T-04.3-58 disposition을 「사람 의존, Phase 7에서 자동화」로 교체 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| Supersedes Round 1 Deferred: E3-16 자동 감시(시스템 상태 화면 · 알림) · 주기 · 자동화 결정 | 설계 `/cso`가 확정했다 — 사람 월 1회 + 35일 감시(현행 유지), 자동화 자체는 Phase 7 스케줄러 기반으로 이관(이 페이즈 범위 밖으로 확정, TODO 아님) |

### Round 5 — 1d927bd

> 출처: `docs/designs/plant8-erp-phase04.3-review-260925.md`(계획 문서 `/review` 요약 — 원문 `/mnt/project-files/04.3-gates/review-adversarial-opus.md`). 반영 지시 `/mnt/project-files/04.3-gates/review-apply-brief.md`의 기본값 6(AX-3)을 따랐다. 계획 문서 /review 교차는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| AX-3(L339@1d927bd) — 파기(칸 비우기) 뒤에도 Cloud SQL 자동 백업(`--retained-backups-count=7`)과 PITR 로그에 주민등록번호 암호문 등이 남고, 파기 전 백업으로 복원하면 파기된 행이 조용히 되살아난다(서명 객체는 없는데 `signature_key`만 남음) | P3 | 기본값 6 적용: truths 새 줄(파기는 라이브 DB 기준 · 백업 7일 · 복원 뒤 곧바로 `--apply`) · Task 3 read_first(`ensure_sql_instance` 백업 인자, 이름 참조) · ③ OPERATIONS 「확인증 파기」 절에 두 줄 ① 「백업 보관 7일이 지나야 완전 파기」 ② 「백업 · PITR 복원 뒤에는 곧바로 `purge-certs --apply`를 한 번」 · verify 새 줄(절 안에서 두 줄 확인) + fails_when · acceptance · T-04.3-59에 백업 · PITR 잔여 위험과 복원 절차 |
| 검사기 r1 W1(교차 r1 참고 8) — `docs/OPERATIONS.md` 300줄 상한을 05 · 08 · 12 · 13이 나눠 쓰는데 줄 예산이 없고, 웨이브 6의 12는 「전체 300줄 이하」뿐이라 넘치면 실행자가 무엇을 줄일지 즉석 판단 | WARNING | 네 플랜 공통 예산(05 +5 · 08 +5 · 12 +18 · 13 +2 = 30, 끝 상한선 = 300 − 뒤 예산 합). 이 플랜: Task 3 ③에 순증 +18 · 끝 298 이하 · 본문 15줄의 틀(ⓐ~ⓖ 줄 수) · 넘칠 때 줄이는 대상 ①~③을 정확히 · 지키는 줄 목록 · main · 05 · 08 줄은 줄이지 않음 · 그래도 넘으면 멈추고 보고 · verify 마지막 줄(`wc -l` ≤ 298 · 절 17줄 이하) + fails_when · acceptance. 검사기·교차 1회차 지적 반영 — Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요 |
| 교차 r1 참고 7 — truth · 런북 문장의 「PITR 로그」가 Phase 4 `ensure_sql_instance`에 `--enable-point-in-time-recovery`가 없어 과장일 수 있음 | 참고 | truths · read_first · ③ ① · T-04.3-59를 「자동 백업(켠 경우 PITR 로그도)」로 좁히고 ② 문구를 「백업 복원 뒤」로 · ③에 PITR 확인 절차(`deploy.sh` grep + 볼 수 있으면 `gcloud sql instances describe … pointInTimeRecoveryEnabled`, SUMMARY 기록 · 권한 없으면 「미확인」 · 확인된 경우에만 문서에 PITR 문구) · acceptance · fails_when 문구 갱신(verify 명령은 「복원」 + `--apply`라 그대로). 검사기·교차 1회차 지적 반영 — Codex 대신 Opus, 한도 풀리면 Codex 재확인 필요 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 지적(AX-3)은 반영했다. E3-04(12 · 08 범위)는 반영 지시대로 그대로 둔다 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-12-SUMMARY.md` when done
</output>
