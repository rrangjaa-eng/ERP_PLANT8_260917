---
phase: 04.3-qr-certificate-intake
plan: 13
type: execute
wave: 7
depends_on: ["04.3-01", "04.3-02", "04.3-03", "04.3-04", "04.3-05", "04.3-06", "04.3-07", "04.3-08", "04.3-09", "04.3-10", "04.3-11", "04.3-12"]
files_modified:
  - db/migrations/*_cert_intake.sql
  - db/migrations/meta/*_snapshot.json
  - db/migrations/meta/_journal.json
  - docs/ARCHITECTURE.md
  - test/e2e/cert-dom-audit.spec.ts
  - test/e2e/mobile-cert-dom-audit.spec.ts
autonomous: true
requirements: [CERT-01, CERT-02]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "브랜치가 origin/main을 머지한 상태이고, 이 페이즈의 스키마가 마이그레이션 하나(`*_cert_intake.sql`)로 main 마지막 번호 + 1에 있다 — 예약 번호 범위 없이 `pnpm db:generate`가 정한 번호이고, `test/unit/db/migration-journal.test.ts`(idx 연속 · tag = idx · `when` 엄격 증가)가 녹색이다"
    - "재생성은 `db/migrations/`를 origin/main과 정확히 같게 되돌린 뒤 이 페이즈 것만 새로 만든 결과다 — main 파일은 하나도 지워지거나 바뀌지 않았고(journal은 main 항목 뒤에 하나만 붙었다) 새 스냅샷 `prevId`가 main 마지막 스냅샷 `id`다"
    - "개발 DB `erp`를 건드리지 않고, 확인 전용 빈 DB(`erp_migcheck`)와 테스트 DB에 마이그레이션 전체가 0번부터 적용되며 `pnpm lint:sql`이 녹색이다 — `drizzle-kit push`를 쓰지 않았다"
    - "`docs/ARCHITECTURE.md`가 이 페이즈의 경계 넷(로그인 없는 `/c/[token]` 경로와 `publicActionClient` · 공개 domain이 권한 판정을 부르지 않는 규칙 · 서명 저장소 포트(로컬/GCS) · 파기 Job)을 적고 KMS 봉투 절(04.3-08)을 가리키며 300줄 이하다"
    - "UI Considerations backstop 가운데 브라우저로 잴 수 있는 항목 전부를 실행자가 아닌 별도 에이전트가 `CI=true`에서 DOM 실측으로 판정했고, 실패 항목을 고친 뒤 감사 스펙이 녹색이다 — 판정 표가 SUMMARY에 있다(CLAUDE.md 화면 검증 순서)"
    - "전체 게이트(`CI=true pnpm test` — 단위 → 통합 → E2E, 프로덕션 빌드)를 이 플랜에서 한 번 돌려 녹색이다"
    - "사람만 판정할 수 있는 항목(실기기 서명 · 보조기술 · 실제 인쇄 미리보기 · 카메라 QR 스캔 · 동의 전문 법무 확인)이 페이즈 끝 사람 확인 목록으로 SUMMARY에 있다"
    - "SUMMARY의 Post-build 순서가 오케스트레이터 단계(`/review` → `/design-review` + `codex` 스킬 Codex(GPT) 디자인 검토 → `/qa` → `/cso` → `/ship`)로 적혀 있고, 계획을 다시 검토할 때도 Codex 검토를 넣는다는 사용자 규칙(2026-09-24)이 함께 있다 — 이 넷은 실행자 태스크가 아니다"
  prohibitions:
    - statement: "마이그레이션 번호를 손으로 바꾸거나 journal · 스냅샷을 손으로 편집해서는 안 된다(생성기만 쓴다)"
      status: resolved
      verification: test
    - statement: "`git push --force` · 리베이스 뒤 강제 푸시를 해서는 안 된다(머지만)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/migrations/meta/_journal.json"
      provides: "main 항목 뒤에 이 페이즈 항목 하나"
      contains: "cert_intake"
    - path: "test/e2e/cert-dom-audit.spec.ts"
      provides: "데스크톱 DOM 감사(진행 표시 · 접근 이름 · 1명/여러 명 문구 · 인쇄 한 장)"
      min_lines: 40
    - path: "test/e2e/mobile-cert-dom-audit.spec.ts"
      provides: "폰 DOM 감사(가로 스크롤 없음 · E4 360px · 200% 확대 · E3 빈 칸 서버 호출 없음)"
      min_lines: 40
  key_links:
    - from: "db/migrations/meta/_journal.json"
      to: "test/unit/db/migration-journal.test.ts"
      via: "idx 연속 단언 — 비는 번호나 손으로 바꾼 번호는 실패한다"
      pattern: "migration-journal"
    - from: "test/e2e/mobile-cert-dom-audit.spec.ts"
      to: "test/e2e/helpers/cert.ts"
      via: "감사 스펙도 규약 C4 도우미로 행사를 만들고 `certs` 프로젝트(워커 1)에서만 돈다 — 폰 폭은 파일 머리 `test.use` viewport 375"
      pattern: "helpers/cert"
---

<objective>
**As a** 이 페이즈를 머지할 개발 담당자, **I want to** 확인증 기능 전체가 최신 main 위에서 하나의 마이그레이션과 녹색 게이트로 서 있기를 바란다, **so that** Post-build(`/review` → `/qa` → `/cso` → `/ship`)가 머지 충돌 · 번호 충돌 · 화면 결함 없이 시작한다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — 이 플랜은 Goal 전체(기준 1~5)를 머지 가능한 상태로 닫는 마무리라 행위자를 개발 담당자로 두었다.

세 가지를 한다: ① **머지 직전 마이그레이션 재생성**(예약 번호 범위 없음 — `db/migrations/`를 origin/main과 정확히 같게 되돌리고 main 것은 하나도 지우지 않은 채 이 페이즈 것만 다시 만들어 main 마지막 + 1로 두고, 개발 DB `erp`가 아닌 별도의 빈 DB에서 확인한다. RESEARCH Pitfall 4 · PATTERNS 공유 레지스트리 절이 같은 규칙을 적는다) ② **독립 DOM 감사**(CLAUDE.md 「싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번」) ③ **전체 게이트 한 번 + 사람 확인 목록**. 설계 `/cso` · `/review` · Post-build `/cso` · `/design-review` · `/qa` · `/ship`은 이 플랜의 태스크가 아니다(오케스트레이터가 부르는 단계) — SUMMARY에 넘길 항목과 순서만 모은다. **사용자 규칙(2026-09-24): 화면 디자인을 검토하는 단계에는 Codex(GPT) 검토를 반드시 넣는다** — Post-build `/design-review`는 gstack `/design-review`와 `codex` 스킬의 Codex 디자인 검토를 한 단계로 함께 돌리고, 계획을 다시 검토할 때(`/plan-design-review` 등 계획 게이트)도 같은 방식으로 Codex 검토를 넣는다. 이 규칙은 SUMMARY의 Post-build 순서에 그대로 적는다(Task 3 ②).

Purpose: CERT-01 · CERT-02 전체를 머지 가능한 상태로 확정한다.
Output: 재생성된 마이그레이션 하나, ARCHITECTURE 요약, DOM 감사 스펙 둘, 녹색 전체 게이트, 사람 확인 목록.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] origin/main 머지 → 이 페이즈 마이그레이션 재생성 → 새 DB에 적용 + ARCHITECTURE 요약</name>
  <files>db/migrations/*_cert_intake.sql, db/migrations/meta/*_snapshot.json, db/migrations/meta/_journal.json, docs/ARCHITECTURE.md</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-02-SUMMARY.md` — 이 페이즈 마이그레이션 파일 이름 · 스냅샷 파일 이름 · 머리의 `SET LOCAL` 두 줄
    - `.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md` 「Pitfall 4」(`id`/`prevId` 체인 · `when` 순서 · main 복원 뒤 재생성 규칙)
    - `db/migrations/meta/_journal.json` · `test/unit/db/migration-journal.test.ts`(04.1 또는 04.3-02 산출물)
    - `scripts/reset-test-db.sh`(전문 — `erp_test`만 지우고 만드는 방식, 이 태스크의 확인 DB가 따라 할 형태) · `scripts/dev-db.sh` · `package.json`의 `db:generate` · `db:migrate`(`--env-file-if-exists=.env.local` — 이미 있는 `DATABASE_URL`을 덮지 않는다) · `db:reset:test` · `lint:sql`
    - `db/migrations/meta/0010_snapshot.json` 머리 — `id` / `prevId` 체인의 모양
    - `docs/ARCHITECTURE.md` 목차 · 04.3-08이 고친 §4-4 · §9 · `test/unit/docs-limits.test.ts`
    - 모든 04.3 SUMMARY의 「공유 등록부에 더한 항목」 줄 — 충돌을 풀 때 이 페이즈 쪽 항목 목록
  </read_first>
  <action>
① **머지:** `git fetch origin` → `git merge origin/main`(리베이스 · 강제 푸시 금지). 충돌 해결 규칙: 추가 전용 등록부(`domain/action-log/record.ts` 세 배열 · `domain/settings/keys.ts` · `domain/permissions/info-items.ts` · `domain/permissions/menus.ts` · `test/integration/leak-scan.test.ts` import 목록)는 **양쪽 항목을 모두 남긴다**(main 쪽 먼저, 이 페이즈 쪽 뒤). `db/migrations/` 아래의 충돌은 어느 쪽으로도 손으로 풀지 않는다 — ②가 디렉터리째 main 것으로 되돌린다. 그 밖의 파일에서 충돌이 나면 **멈추고** 충돌 파일 목록과 양쪽 의도를 SUMMARY에 적어 사람에게 넘긴다(추측으로 풀지 않는다).

② **재생성 — main을 먼저 온전히 복원하고, 이 페이즈 것만 새로 만든다.** 파일 경로만으로 「이 페이즈 것」을 고르지 않는다(양쪽이 같은 번호로 생성했으면 경로가 같다 — 스냅샷은 `id`/`prevId` 체인으로 이어진다, `0010_snapshot.json` 머리):
  - (a) 기록: 재생성 전 `git diff --name-status origin/main -- db/migrations`와 04.3-02 SUMMARY의 마이그레이션 · 스냅샷 파일 이름을 SUMMARY에 옮긴다(나중에 대조용)
  - (b) 복원: `git checkout origin/main -- db/migrations` — main의 SQL · 스냅샷 · `_journal.json`이 main 내용 그대로 돌아온다(같은 경로의 이 페이즈 파일은 main 내용으로 덮인다). 그 뒤 `git ls-tree -r --name-only origin/main -- db/migrations`에 **없는** 파일만 `db/migrations/` 아래에서 지운다(이 브랜치가 더한 파일뿐이다). main에 있는 파일은 하나도 지우거나 고치지 않는다
  - (c) 복원 확인: `git diff --quiet origin/main -- db/migrations`가 종료 코드 0이고 `git ls-files --others --exclude-standard db/migrations`가 빈 출력이다 — 이 순간 `db/migrations/`가 main과 바이트 단위로 같다
  - (d) 생성: `pnpm db:generate --name cert_intake`(drizzle-kit이 main 마지막 스냅샷과 현재 스키마의 차이로 이 페이즈 SQL 하나 · 스냅샷 하나 · journal 항목 하나를 만든다) → 새 SQL 머리에 04.3-02와 같은 `SET LOCAL lock_timeout` · `statement_timeout` 두 줄을 다시 더한다. 번호를 손으로 바꾸지 않는다. `drizzle-kit push`를 쓰지 않는다
  - (e) 체인 확인: 새 항목 idx = main 마지막 idx + 1 · 새 `when` > main 마지막 `when` · 새 스냅샷 `prevId` = main 마지막 스냅샷 `id`(`node -e`로 두 JSON을 읽어 비교하고 값을 SUMMARY에 적는다) · `git diff origin/main -- db/migrations/meta/_journal.json`이 항목 하나를 **더하기만** 한다(`-` 줄 없음)
  - (f) 내용 대조: 생성된 SQL이 04.3-02 SUMMARY의 표 · 칸 · 인덱스 · CHECK와 같은지(빠진 것 · 생긴 것 없음)를 (a)에서 기록한 옛 SQL(`git show HEAD:<옛 경로>`)과 번호를 빼고 비교해 SUMMARY에 적는다

③ **[BLOCKING] 별도의 빈 DB에서 처음부터 적용 — 개발 DB `erp`는 지우지도 다시 만들지도 않는다.** 확인 전용 DB `erp_migcheck`를 `scripts/reset-test-db.sh`와 같은 방식(`DROP DATABASE IF EXISTS erp_migcheck WITH (FORCE)` → `CREATE DATABASE erp_migcheck OWNER erp`)으로 만들고 `DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_migcheck pnpm db:migrate`로 마이그레이션 전체를 0번부터 적용한다(`db:migrate`의 `.env.local`은 이미 있는 `DATABASE_URL`을 덮지 않는다) → `pnpm db:reset:test` → `pnpm lint:sql` → `pnpm vitest run --project unit test/unit/db/migration-journal.test.ts`. 확인이 끝나면 `erp_migcheck`만 지운다. 하나라도 실패하면 다음 태스크로 가지 않는다. 개발 DB `erp`에는 옛 번호의 적용 기록이 남아 있을 수 있다 — 고치지 않고 SUMMARY에 「개발 DB는 개발자가 원할 때 직접 다시 만든다」 한 줄만 적는다.

④ **`docs/ARCHITECTURE.md`:** 확인증 경계 요약(10줄 안팎) — 로그인 없는 `/c/[token]` 경로는 `proxy.ts`를 거치지 않고 `publicActionClient`(세션 없음 · 본문 한도)만 쓴다 · 공개 domain(`domain/certs/intake.ts`)은 `can` · `visible` · `scopeFor`를 부르지 않고 토큰 · 행사 id · 자리 id로 where를 좁힌다 · 서명 저장소 포트(로컬 드라이버 / GCS REST, 버킷 수준 권한) · 인쇄 라우트 `/print/…`는 셸 밖이고 페이지가 스스로 인증한다 · 파기는 사람이 실행하는 Cloud Run Job · 데이터 키 봉투 암호화는 04.3-08이 고친 절을 가리킨다. 문서 300줄 이하.

⑤ 커밋 둘: 머지 커밋(기본 메시지 + 본문 한국어 한 줄로 충돌 해결 요약), `chore(04.3): regenerate cert_intake migration on top of main` · `docs(04.3): certificate boundaries in architecture`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/db/migration-journal.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — journal idx가 비었거나(예약 번호를 썼다) ARCHITECTURE가 300줄을 넘었다</fails_when>
    <automated>PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp_migcheck WITH (FORCE)' -c 'CREATE DATABASE erp_migcheck OWNER erp' &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_migcheck pnpm db:migrate &amp;&amp; pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니다 — 별도의 빈 DB에 마이그레이션 전체가 0번부터 적용되지 않았거나 squawk가 위험한 문장을 찾았다</fails_when>
    <automated>node -e 'const cp=require("child_process");const fs=require("fs");const d=cp.execSync("git diff --name-status origin/main -- db/migrations",{encoding:"utf8"}).trim().split("\n").map((l)=>l.split(/\s+/));const add=d.filter((x)=>x[0]==="A").map((x)=>x[1]);const oth=d.filter((x)=>x[0]!=="A");const okFiles=add.length===2&amp;&amp;add.some((f)=>/cert_intake\.sql$/.test(f))&amp;&amp;add.some((f)=>/meta\/\d+_snapshot\.json$/.test(f))&amp;&amp;oth.length===1&amp;&amp;oth[0][0]==="M"&amp;&amp;oth[0][1]==="db/migrations/meta/_journal.json";const m=JSON.parse(cp.execSync("git show origin/main:db/migrations/meta/_journal.json",{encoding:"utf8"}));const w=JSON.parse(fs.readFileSync("db/migrations/meta/_journal.json","utf8"));const okJ=w.entries.length===m.entries.length+1&amp;&amp;m.entries.every((e,i)=>JSON.stringify(e)===JSON.stringify(w.entries[i]))&amp;&amp;w.entries.at(-1).tag.includes("cert_intake");process.exit(okFiles&amp;&amp;okJ?0:1)'</automated>
    <fails_when>main의 마이그레이션 파일이 하나라도 지워졌거나(`D`) 바뀌었거나(journal 말고 `M`), journal에서 main 항목이 빠지거나 바뀌었거나, 이 페이즈가 더한 파일이 SQL 하나 · 스냅샷 하나가 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --merges -1`이 origin/main 머지 커밋이다
    - `db/migrations/`에 `cert_intake`가 든 SQL이 정확히 하나다(`ls db/migrations/*cert_intake*.sql | wc -l`이 1)
    - `_journal.json`의 마지막 항목 tag에 `cert_intake`가 있고, 그 스냅샷의 `prevId`가 main 마지막 스냅샷의 `id`다(SUMMARY에 두 값)
    - `git diff --name-status origin/main -- db/migrations`가 `A` 둘(이 페이즈 SQL · 스냅샷)과 `M` 하나(`_journal.json`)뿐이다 — main 파일의 `D` · 다른 `M`이 0이고, journal의 main 항목 전부가 순서·내용 그대로 앞에 있고 뒤에 이 페이즈 항목 하나만 붙었다(verify의 `node -e` 비교)
    - 이 태스크의 어느 명령도 `DROP DATABASE` 대상에 `erp`(개발 DB)를 쓰지 않았다 — 확인 DB는 `erp_migcheck`, 테스트 DB는 기존 `pnpm db:reset:test`(`erp_test`)뿐이다
  </acceptance_criteria>
  <done>최신 main 위에 이 페이즈 마이그레이션 하나가 연속 번호로 서고, 새 DB에 적용된다</done>
</task>

<task type="auto">
  <name>Task 2: 싼 게이트 → 독립 DOM 감사(별도 에이전트 · CI=true) → 수정</name>
  <files>test/e2e/cert-dom-audit.spec.ts, test/e2e/mobile-cert-dom-audit.spec.ts</files>
  <read_first>
    - 모든 04.3 PLAN의 `must_haves` 중 `verification: backstop` 항목 — 감사 목록의 원본. 수를 여기 적어 두지 않는다(플랜이 고쳐지면 바뀐다): 실행 시점에 `grep -n -B2 "verification: backstop" .planning/phases/04.3-qr-certificate-intake/04.3-*-PLAN.md`로 모으고(이 플랜 자신의 항목은 뺀다) 모은 목록과 개수를 SUMMARY 판정 표 머리에 옮긴다
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「UI Considerations」 backstop 행 · 「상태 점검 보강」 backstop 행
    - `playwright.config.ts`(`CI=true`일 때 프로덕션 빌드 · 04.3-02가 더한 `cert-setup` · `certs` 프로젝트와 `CERT_SPEC_PATTERN`) · `test/e2e/helpers/cert.ts`(규약 C4 도우미) · `test/e2e/cert-events.spec.ts` · `test/e2e/mobile-cert-submit.spec.ts`(준비 · 로그인 · 요청 지연 방식)
  </read_first>
  <action>
① **싼 게이트:** `pnpm lint && pnpm typecheck && pnpm build`. 실패하면 고친 뒤 ②로.

② **독립 감사(실행자가 하지 않는다):** 서브에이전트를 `model: sonnet`으로 **명시해** 띄우고, 프롬프트에 `verification-before-completion` 스킬 사용과 아래 목록 · 판정 규칙(스크린샷 육안 금지, DOM 수치와 요청 수로만 판정, `CI=true`)과 규약 C4(스펙 이름에 `cert`가 들어가 `certs` 프로젝트에서만 돈다 · 행사는 `createCertEvent`로 이름이 겹치지 않게 만든다 · 전역 확인증 설정을 스스로 바꾸지 않는다 · 목록 전체가 비었다는 단언을 하지 않는다 · 폰 스펙은 머리에 `test.use({ viewport: { width: 375, height: 800 } })` · 두 파일 모두 `test.describe.configure({ mode: "serial" })`)를 넣는다. 감사 에이전트가 두 스펙을 쓰고 `CI=true pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-dom-audit.spec.ts test/e2e/mobile-cert-dom-audit.spec.ts`(04.3-02 규약 C4의 빠른 형태 — 의존 프로젝트 `desktop` · `mobile-375` 전체는 Task 3 전체 게이트가 한 번만 돌린다)로 돌려 항목별 통과/실패와 실측값을 표로 돌려준다.
  - 폰(`mobile-cert-dom-audit.spec.ts`, 375): I1 · I2 · I3 표 컨테이너와 문서의 `scrollWidth <= clientWidth` · E4를 폭 360과 폭 180(= 200% 확대)에서 같은 단언 + 주민등록번호 두 칸 · sticky 제출 줄 · 서명 칸이 뷰포트 안 · E3에서 빈 칸 · 3자리 상태로 「전화번호 확인」을 눌러도 서버 액션 요청 0건(요청 수 셈) + 비활성 이유 문장
  - 데스크톱(`cert-dom-audit.spec.ts`): I1 제출 셀의 `.sr-only` `제출 3명 / 전체 12명`이 셀 안에 있고 보이는 `3/12`가 `aria-hidden` · 당첨자 1명 행사와 여러 명 행사에서 I2 합계 행 · 저장 문구(`합계 · 1명` 등) · 「행사 만들기」 · I4 「전체 보기」 · 「고친 내용 저장」 요청을 늦췄을 때 버튼 라벨 뒤 `…` + 다른 1차 비활성 · P1을 `page.pdf({preferCSSPageSize: true})`로 뽑아 40자 행사 이름 + 긴 도로명 주소에서 페이지 수 1
  - 코드 확인 항목(브라우저로 유도하기 어려움 — 감사 표에 「코드 확인 + `/qa` 실측 필요」로 적는다): E1 `app/c/[token]/error.tsx`와 I1 `app/(app)/certs/events/error.tsx`가 있고 UI-SPEC 문장 그대로인지
  - 감사 밖(사람 · 보안 감사): 실기기 서명 · 보조기술 · 파기와 전체 보기 경합 — Task 3의 사람 확인 · `/cso`로 넘긴다

③ **수정:** 실패 항목마다 원인 → 수정(이 페이즈가 만든 화면 파일 안에서만, 토큰만 — 새 색 · radius 없음) → 감사 스펙만 다시 돌려 녹색. 한 항목 수정이 SYSTEM.md와 어긋나야만 풀리면 고치지 말고 SUMMARY에 적어 `/design-review`로 넘긴다. 감사 표(항목 · 원본 플랜 · 실측값 · 통과/실패 · 수정 커밋)를 SUMMARY에 둔다.

④ 커밋 `test(04.3): independent DOM audit specs`, 수정은 항목마다 `fix(04.3): …`.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>CI=true pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-dom-audit.spec.ts test/e2e/mobile-cert-dom-audit.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 감사 항목 중 고치지 못한 것이 남았다</fails_when>
  </verify>
  <acceptance_criteria>
    - 감사 스펙 둘이 서브에이전트가 쓴 것이다(SUMMARY에 에이전트 모델 · 프롬프트 요지 · 판정 표)
    - 폰 스펙이 E4 폭 360과 180 두 경우를 모두 잰다
    - 데스크톱 스펙이 P1 PDF 페이지 수 1을 단언한다
  </acceptance_criteria>
  <done>잴 수 있는 backstop 전부가 DOM 수치로 녹색이고, 잴 수 없는 항목은 넘길 곳이 정해져 있다</done>
</task>

<task type="auto">
  <name>Task 3: 전체 게이트 한 번(CI=true) + 페이즈 끝 사람 확인 목록</name>
  <files>test/e2e/cert-dom-audit.spec.ts</files>
  <read_first>
    - 모든 04.3 SUMMARY의 후속 항목(네이티브 dialog → `ui/confirm-dialog` · `recordAction` tx 인자(04-32 뒤) · 동의 전문 법무 확인 · 설계 `/cso` 열린 질문)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「가정」 표(A1~A12 — 설계 `/cso` · Phase 11이 다시 볼 결정)
  </read_first>
  <action>
① **전체 게이트 한 번:** `pnpm db:reset:test` 뒤 `CI=true pnpm test`(단위 → 통합 → E2E, 프로덕션 빌드) + `pnpm lint:sql`. 실패하면 `systematic-debugging` 스킬로 원인을 찾아 고친 뒤 **실패한 스위트만** 다시 돌리고, 마지막에 전체를 한 번 더 돌린다(고친 게 없으면 다시 돌리지 않는다). 이 태스크가 파일을 고쳤으면 그 파일을 SUMMARY에 적는다(이 태스크의 `files`는 감사 스펙 보정 가능성만 적었다).

② **SUMMARY에 넘길 목록:**
  - 사람 확인(아래 `human-check`)
  - 설계/구현 `/cso` 열린 질문: 뒤 4자리 추측 · 잠금 증가 · 당첨자 명단 전화번호와 수령자 연락처 · 주소 암호화(RESEARCH Open Question 1 · T-04.3-15 — 설계 `/cso`로 미뤄 둔 결정) · 미제출 명단 30일 · 확인 증표 수명 30분 · 멱등 키 모양과 보관 · 개인정보 비활동 범위 · 대표 계급 차단 · 파기 주기 · 구별 표시에 남는 개인정보 · 환경 게이트 `CERT_FEATURE_ALLOWED`를 켜는 절차(Phase 11 몫, 규약 C1) · 주민등록번호 검증번호 불일치를 되묻고 받는 규칙(04.3-02) · 공통 오류 로그 축소의 디버깅 영향(규약 C2) · 인쇄 열람 로그
  - 후속: 네이티브 dialog → `ui/confirm-dialog` · `recordAction` tx 인자(04-32가 main에 들어온 뒤) · 동의 전문 법무 확인 · §6-9 세션 만료 화면 부재로 로그인 이동 · 개발 DB `erp`는 개발자가 원할 때 직접 다시 만든다(Task 1 ③)
  - **Post-build 순서(오케스트레이터 단계 — 이 플랜의 실행 태스크가 아니다):** `/review` → `/design-review`(gstack `/design-review`와 **`codex` 스킬의 Codex(GPT) 디자인 검토를 같은 단계에서 함께** 돌리고 두 결과를 합쳐 고친다 — 사용자 규칙 2026-09-24 「화면 디자인을 검토할 때는 GPT 검토를 꼭 넣는다」) → `/qa` → `/cso`(필수 — 인증 · 암호화 · 외부 입력) → `/ship`. `/ship` 전에 main이 더 움직였으면 Task 1 ②③을 다시 한다. 계획을 다시 검토하는 일이 생기면(`/plan-design-review` 등 계획 게이트, 이 페이즈 PLAN 개정) 같은 규칙으로 Codex 검토를 넣는다(같은 사용자 규칙 — 「계획할 때도」)
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test &amp;&amp; pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 어느 스위트든 failed 개수가 1 이상이다</fails_when>
    <human-check>iOS Safari와 Android Chrome 실기기에서 E4 서명: 긋는 동안 페이지가 스크롤되지 않고 획이 끊기지 않는다 · 폭을 바꾸는 회전 뒤에도 획이 잘리지 않는다</human-check>
    <human-check>VoiceOver(iOS) · TalkBack(Android) 통과 제스처로 E4 서명 완료 기준 ①(키보드 두 획 대신 통과 제스처로 그리기) · ③(「서명이 들어갔습니다」 알림과 `서명 칸 · 서명함`) · ⑤(제출)이 된다</human-check>
    <human-check>I4 「인쇄」 → 실제 인쇄 대화상자 미리보기에서 40자 행사 이름 · 긴 도로명 주소 확인증이 A4 한 장이고 서명이 검정으로 찍힌다</human-check>
    <human-check>I3 화면의 QR을 폰 카메라로 찍으면 `/c/{token}` E2가 열린다(스테이징 URL)</human-check>
    <human-check>동의 전문 초안(근거법령 · 보존 기간 기산점 · 제공처)을 법무가 확인했거나, 확인 전이라 프로덕션 플래그를 켜지 않는다는 사실이 기록돼 있다</human-check>
  </verify>
  <acceptance_criteria>
    - `CI=true pnpm test`가 한 번(수정이 있었으면 마지막 한 번 더) 녹색이고 그 요약(스위트별 통과 수)이 SUMMARY에 있다
    - SUMMARY에 사람 확인 다섯 · `/cso` 열린 질문 · 후속 · Post-build 순서가 있다
    - SUMMARY의 Post-build 순서 줄에 `/design-review`와 `codex`(Codex 디자인 검토)가 한 단계로 함께 있고, 계획 재검토에도 Codex 검토를 넣는다는 줄이 있다(`grep -c 'codex' .planning/phases/04.3-qr-certificate-intake/04.3-13-SUMMARY.md`가 1 이상)
  </acceptance_criteria>
  <done>전체 게이트가 녹색이고, 사람 확인과 Post-build로 넘길 항목이 한 곳에 모였다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이 브랜치 ↔ origin/main | 다른 페이즈가 같은 등록부 · 마이그레이션 체인을 고친다 — 머지에서 한쪽 항목이 사라지면 권한 · 로그 · 누수 검사가 조용히 빠진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-63 | Tampering | 등록부 머지 충돌 해결 | high | mitigate | 추가 전용 등록부는 양쪽 항목을 모두 남기는 규칙 하나 · 그 밖의 충돌은 멈추고 사람에게. `registry-coverage` · `leak-scan-coverage` · `record` 단위 테스트가 전체 게이트에서 빠진 항목을 잡는다 |
| T-04.3-64 | Denial of Service | 마이그레이션 체인 | medium | mitigate | 생성기로만 재생성(`prevId` = main 마지막 `id`) · 새 DB에 처음부터 적용 · journal 연속 테스트 · squawk |
| T-04.3-65 | Elevation of Privilege | 감사 없이 끝난 화면 결함 | low | mitigate | 실행자가 아닌 에이전트의 DOM 실측 감사와 사람 확인 목록 — 보안 판정 자체는 Post-build `/cso`(이 플랜의 태스크가 아니다) |
</threat_model>

<verification>
- 단위 `migration-journal` · `docs-limits` 녹색, 새 DB에 `pnpm db:migrate` · `pnpm lint:sql` 녹색
- `CI=true` 감사 스펙 둘 녹색
- `CI=true pnpm test` 한 번 녹색
- 사람 확인 다섯이 SUMMARY 목록에 있다
</verification>

<success_criteria>
- 최신 main 위에 마이그레이션 하나 · 연속 번호
- 잴 수 있는 backstop 전부가 독립 DOM 감사로 녹색
- 전체 게이트 한 번 녹색, Post-build로 넘길 목록 완비
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/*_cert_intake.sql`(main 마지막 + 1, 재생성) · 스냅샷 · journal 항목 |
| 문서 | `docs/ARCHITECTURE.md` 확인증 경계 요약 |
| 감사 스펙 | `test/e2e/cert-dom-audit.spec.ts` · `test/e2e/mobile-cert-dom-audit.spec.ts` |
| 넘김 목록 | 사람 확인 다섯 · `/cso` 열린 질문 · 후속 · Post-build 순서(`/design-review` + Codex 디자인 검토 포함, SUMMARY) |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-13-SUMMARY.md` when done
</output>
