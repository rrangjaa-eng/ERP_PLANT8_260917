---
phase: 04.3-qr-certificate-intake
plan: 13
type: execute
wave: 7
depends_on: ["04.3-01", "04.3-02", "04.3-03", "04.3-04", "04.3-05", "04.3-06", "04.3-07", "04.3-08", "04.3-09", "04.3-10", "04.3-11", "04.3-12"]
files_modified:
  - db/migrations/*_cert_intake.sql
  - db/migrations/*_cert_winners_unique_deferrable.sql
  - db/migrations/meta/*_snapshot.json
  - db/migrations/meta/_journal.json
  - docs/ARCHITECTURE.md
  - docs/OPERATIONS.md
  - test/e2e/cert-dom-audit.spec.ts
  - test/e2e/mobile-cert-dom-audit.spec.ts
autonomous: true
requirements: [CERT-01, CERT-02]
execution_gate:
  start_after: "Phase 4 PR #40(origin/claude/gsd-progress-e1nzgu)이 main에 머지된 뒤에만 시작한다(E3-01)"
  wave_first_step: "웨이브 첫 단계로 git merge origin/main — 리베이스 · 강제 푸시 금지(E3-01)"
  serial_tests: "같은 웨이브 안에서도 통합 · E2E는 한 번에 하나 — 세션당 플랜 하나(E3-33)"

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "브랜치가 origin/main을 머지한 상태이고, 이 페이즈의 스키마가 04.3-02의 마이그레이션 둘 — 생성 `*_cert_intake.sql`(main 마지막 번호 + 1)과 drizzle-kit `--custom` `*_cert_winners_unique_deferrable.sql`(+ 2, 당첨자 UNIQUE를 `DEFERRABLE INITIALLY IMMEDIATE`로) — 로 있다. 예약 번호 범위 없이 둘 다 `pnpm db:generate`가 정한 번호이고, 적용된 DB의 `cert_winners_event_id_name_phone_unique`가 `condeferrable = true` · `condeferred = false`이며, `test/unit/db/migration-journal.test.ts`(idx 연속 · tag = idx · `when` 엄격 증가)가 녹색이다"
    - "재생성은 `db/migrations/`를 origin/main과 정확히 같게 되돌린 뒤 이 페이즈 것만 새로 만든 결과다 — main 파일은 하나도 지워지거나 바뀌지 않았고(journal은 main 항목 뒤에 `cert_intake` · `cert_winners_unique_deferrable` 둘만 이 순서로 붙었다) `cert_intake` 스냅샷 `prevId`가 main 마지막 스냅샷 `id`이고 custom 스냅샷 `prevId`가 `cert_intake` 스냅샷 `id`다"
    - "개발 DB `erp`를 건드리지 않고, 확인 전용 빈 DB(`erp_migcheck`)와 테스트 DB에 마이그레이션 전체가 0번부터 적용되며 `pnpm lint:sql`이 녹색이다 — `drizzle-kit push`를 쓰지 않았다"
    - "`docs/ARCHITECTURE.md`가 이 페이즈의 경계 넷(로그인 없는 `/c/[token]` 경로와 `publicActionClient` · 공개 domain이 권한 판정을 부르지 않는 규칙 · 서명 저장소 포트(로컬/GCS) · 파기 Job)을 적고 KMS 봉투 절(04.3-08)을 가리키며 300줄 이하다 — 넘으면 main 쪽 절이 아니라 이 페이즈 요약을 줄인다(E3-01)"
    - "UI Considerations backstop 가운데 브라우저로 잴 수 있는 항목 전부를 실행자가 아닌 별도 에이전트가 `CI=true`에서 DOM 실측으로 판정했고, 실패 항목을 고친 뒤 감사 스펙이 녹색이다 — 판정 표가 SUMMARY에 있다(CLAUDE.md 화면 검증 순서)"
    - "전체 게이트(`CI=true pnpm test` — 단위 → 통합 → E2E, 프로덕션 빌드)를 이 플랜에서 한 번 돌려 녹색이다"
    - "사람만 판정할 수 있는 항목(실기기 서명 · 보조기술 · 실제 인쇄 미리보기 · 카메라 QR 스캔 · 동의 전문 법무 확인 · **스테이징 스모크** — 확인증 개인정보취급자 지정(E3-13) → 행사 만들기 → 폰 제출 → I4 서명 읽기 → 파기 미리 보기, 가짜 인물만)이 페이즈 끝 사람 확인 목록으로 SUMMARY에 있다 — 스테이징에서만 환경 게이트가 허용되므로(04.3-02 · E3-07) 스모크가 실제 GCS · KMS 경로를 한 번 거친다"
    - "SUMMARY의 PR 체크리스트에 「머지 **전에** 소유자가 PR 브랜치의 `scripts/bootstrap-gcp.sh`를 한 번 돌린다(머지 = 스테이징 자동 배포)」 줄과 그 뒤 KMS 정리(E3-12) 확인 줄이 있다(E3-07)"
    - "설계 `/cso` 입력 목록(아래 「설계 `/cso` 입력 목록」 절 — E3-08 · E3-10 · E3-12 · E3-13 · E3-15 · E3-16 · E3-35 · D-16 · X-10 · N-8)이 모두 「확정 — `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`」로 이 플랜에 있다 — 남은 항목이 없다(eng-review T11, 설계 `/cso`가 전부 확정했다)"
    - "SUMMARY의 Post-build 순서가 오케스트레이터 단계(`/review` → `/design-review` + `codex` 스킬 Codex(GPT) 디자인 검토 → `/qa` → `/cso` → `/ship`)로 적혀 있고, 계획을 다시 검토할 때도 Codex 검토를 넣는다는 사용자 규칙(2026-09-24)이 함께 있다 — 이 넷은 실행자 태스크가 아니다"
  prohibitions:
    - statement: "마이그레이션 번호를 손으로 바꾸거나 journal · 스냅샷을 손으로 편집해서는 안 된다(생성기만 쓴다)"
      status: resolved
      verification: test
    - statement: "`git push --force` · 리베이스 뒤 강제 푸시를 해서는 안 된다(머지만)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/migrations/meta/_journal.json"
      provides: "main 항목 뒤에 이 페이즈 항목 둘(`cert_intake` → `cert_winners_unique_deferrable`)"
      contains: "cert_winners_unique_deferrable"
    - path: "test/e2e/cert-dom-audit.spec.ts"
      provides: "데스크톱 DOM 감사(진행 표시 · 접근 이름 · 1명/여러 명 문구 · 인쇄 한 장)"
      min_lines: 40
    - path: "test/e2e/mobile-cert-dom-audit.spec.ts"
      provides: "폰 DOM 감사(가로 스크롤 없음 · E4 360px · 200% 확대 · E3 빈 칸 서버 호출 없음)"
      min_lines: 40
  key_links:
    - from: "db/migrations/meta/_journal.json"
      to: "test/unit/db/migration-journal.test.ts"
      via: "idx 연속 단언 — 비는 번호나 손으로 바꾼 번호는 실패한다"
      pattern: "migration-journal"
    - from: "test/e2e/mobile-cert-dom-audit.spec.ts"
      to: "test/e2e/helpers/cert.ts"
      via: "감사 스펙도 규약 C4 도우미로 행사를 만들고 `certs` 프로젝트(워커 1)에서만 돈다 — 폰 폭은 파일 머리 `test.use` viewport 375"
      pattern: "helpers/cert"
---

<objective>
**As a** 이 페이즈를 머지할 개발 담당자, **I want to** 확인증 기능 전체가 최신 main 위에서 하나의 마이그레이션과 녹색 게이트로 서 있기를 바란다, **so that** Post-build(`/review` → `/qa` → `/cso` → `/ship`)가 머지 충돌 · 번호 충돌 · 화면 결함 없이 시작한다.

ROADMAP Goal 줄은 사용자 이야기 형식이 아니다 — 이 플랜은 Goal 전체(기준 1~5)를 머지 가능한 상태로 닫는 마무리라 행위자를 개발 담당자로 두었다.

세 가지를 한다: ① **머지 직전 마이그레이션 재생성**(예약 번호 범위 없음 — `db/migrations/`를 origin/main과 정확히 같게 되돌리고 main 것은 하나도 지우지 않은 채 이 페이즈 것만 다시 만들어 main 마지막 + 1로 두고, 개발 DB `erp`가 아닌 별도의 빈 DB에서 확인한다. RESEARCH Pitfall 4 · PATTERNS 공유 레지스트리 절이 같은 규칙을 적는다) ② **독립 DOM 감사**(CLAUDE.md 「싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번」) ③ **전체 게이트 한 번 + 사람 확인 목록**. 설계 `/cso` · `/review` · Post-build `/cso` · `/design-review` · `/qa` · `/ship`은 이 플랜의 태스크가 아니다(오케스트레이터가 부르는 단계) — SUMMARY에 넘길 항목과 순서만 모은다. **사용자 규칙(2026-09-24): 화면 디자인을 검토하는 단계에는 Codex(GPT) 검토를 반드시 넣는다** — Post-build `/design-review`는 gstack `/design-review`와 `codex` 스킬의 Codex 디자인 검토를 한 단계로 함께 돌리고, 계획을 다시 검토할 때(`/plan-design-review` 등 계획 게이트)도 같은 방식으로 Codex 검토를 넣는다. 이 규칙은 SUMMARY의 Post-build 순서에 그대로 적는다(Task 3 ②).

**실행 조건(eng-review E3-01 · E3-33):** 이 페이즈 실행은 Phase 4 PR #40(`origin/claude/gsd-progress-e1nzgu`)이 main에 머지된 뒤에 시작했고, 웨이브마다 첫 단계로 `git merge origin/main`을 해 왔다 — 그래서 ①의 머지는 마지막 웨이브의 증분 머지다(Phase 4의 `ui/button` · `ui/confirm-dialog` · `ui/table` · `recordAction(…, {tx})`는 앞 플랜들이 이미 기준으로 썼다). 리베이스 · 강제 푸시 금지. 통합 · E2E는 다른 세션과 겹치지 않게 한 번에 하나다(세션당 플랜 하나 — 이 저장소 훅 D-04가 세션당 실행 에이전트 디스패치를 한 번만 허용한다). 로컬에서 두 세션을 동시에 돌리면 `erp_test`와 포트 3100을 함께 써서 서로 깨진다 — 그렇게 하지 않는다.

Purpose: CERT-01 · CERT-02 전체를 머지 가능한 상태로 확정한다.
Output: 재생성된 마이그레이션 둘(생성 `cert_intake` + custom `cert_winners_unique_deferrable`), ARCHITECTURE 요약, DOM 감사 스펙 둘, 녹색 전체 게이트, 사람 확인 목록.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] origin/main 머지 → 이 페이즈 마이그레이션 둘 재생성 → 새 DB에 적용 + ARCHITECTURE 요약 · OPERATIONS 한 줄</name>
  <precondition>Phase 4가 main에 있다 — `git fetch origin` 뒤 `git show origin/main:ui/confirm-dialog/ConfirmDialog.tsx >/dev/null`가 종료 코드 0(E3-01). 아니면 멈춘다. `domain/action-log/record.ts`의 `tx?: DbOrTx` 유무는 멈춤 조건이 아니다 — 없으면 04.3-02 ⑨-b 대비책 `insertActionLogTx`(`repositories/cert-action-log.ts`) 하나로 간다(E3-02 단일 정책, Round 2) — 이 플랜은 로그를 쓰지 않고, 대비책을 쓴 플랜이 있으면 SUMMARY 후속에 한 줄로 옮긴다(앞 웨이브들이 이 조건으로 시작했어야 한다)</precondition>
  <files>db/migrations/*_cert_intake.sql, db/migrations/*_cert_winners_unique_deferrable.sql, db/migrations/meta/*_snapshot.json, db/migrations/meta/_journal.json, docs/ARCHITECTURE.md, docs/OPERATIONS.md</files>
  <read_first>
    - `.planning/phases/04.3-qr-certificate-intake/04.3-02-SUMMARY.md` — 이 페이즈 마이그레이션 둘의 파일 이름 · 스냅샷 파일 이름 · 머리의 `SET LOCAL` 두 줄 · custom 마이그레이션 본문(04.3-02 Task 1 ②-b — DROP → UNIQUE INDEX → `ADD CONSTRAINT … UNIQUE USING INDEX … DEFERRABLE INITIALLY IMMEDIATE`)
    - `04.3-02-PLAN.md` Task 1 ②-b와 그 verify의 `pg_constraint` 확인 명령(`condeferrable` · `condeferred`) — ③이 그대로 다시 쓴다
    - `.planning/phases/04.3-qr-certificate-intake/04.3-RESEARCH.md` 「Pitfall 4」(`id`/`prevId` 체인 · `when` 순서 · main 복원 뒤 재생성 규칙)
    - `db/migrations/meta/_journal.json` · `test/unit/db/migration-journal.test.ts`(04.1 또는 04.3-02 산출물)
    - `scripts/reset-test-db.sh`(전문 — `erp_test`만 지우고 만드는 방식, 이 태스크의 확인 DB가 따라 할 형태) · `scripts/dev-db.sh` · `package.json`의 `db:generate` · `db:migrate`(`--env-file-if-exists=.env.local` — 이미 있는 `DATABASE_URL`을 덮지 않는다) · `db:reset:test` · `lint:sql`
    - `db/migrations/meta/0010_snapshot.json` 머리 — `id` / `prevId` 체인의 모양
    - `docs/ARCHITECTURE.md` 목차 · 04.3-08이 고친 §4-4 · §9 · Phase 4가 더한 절(main) · `docs/OPERATIONS.md` §8(04.3-05) · §12 설정 가져오기 · 04.3-12가 끝에 더한 「확인증 파기」 절(번호는 `grep -n '^## [0-9]*\. 확인증 파기' docs/OPERATIONS.md`로 찾는다 — 전제하지 않는다, A4) · `test/unit/docs-limits.test.ts`(두 문서 300줄 상한)
    - `domain/settings/export.ts` 90~97행(가져오기가 등록되지 않은 키를 `등록되지 않은 키: {key}`로 모으고 아무것도 쓰지 않는다) · 04.3-02 Task 1의 `SETTING_DEFS` 끝 `...(env.CERT_FEATURE_ALLOWED === "true" ? [CERT_ENABLED] : [])`(게이트가 꺼진 환경에는 `cert.enabled`가 등록되지 않는다)
    - 모든 04.3 SUMMARY의 「공유 등록부에 더한 항목」 줄 — 충돌을 풀 때 이 페이즈 쪽 항목 목록
  </read_first>
  <action>
① **머지:** `git fetch origin` → `git merge origin/main`(리베이스 · 강제 푸시 금지). 충돌 해결 규칙: 추가 전용 등록부(`domain/action-log/record.ts` 세 배열 · `domain/settings/keys.ts` · `domain/permissions/info-items.ts` · `domain/permissions/menus.ts` · `test/integration/leak-scan.test.ts` import 목록)는 **양쪽 항목을 모두 남긴다**(main 쪽 먼저, 이 페이즈 쪽 뒤). 운영 문서 둘(`docs/ARCHITECTURE.md` · `docs/OPERATIONS.md`)도 같은 규칙을 **절 단위로** 쓴다 — 양쪽 절을 모두 남기고 main 절이 먼저, 이 페이즈 절이 뒤(절 번호가 겹치면 이 페이즈 절 번호만 뒤로 민다 · main 절의 본문은 고치지 않는다, E3-01). 합친 뒤 300줄을 넘으면 ④ 규칙대로 이 페이즈 쪽을 줄인다. **Phase 4 공용 확인 부품 `ui/confirm-dialog/ConfirmDialog.tsx`**(04.3-10 ④-0이 `primary.failure?` 슬롯을 더했다 — Phase 4 쪽도 아직 고칠 수 있는 부품, A5)와 그 단위 테스트 `test/unit/ui/confirm-dialog.test.ts`도 **양쪽 유지**다 — main 쪽 변경을 모두 남기고 이 페이즈의 `failure` 슬롯 · 케이스를 그 위에 다시 얹는다(같은 슬롯을 main이 다른 이름으로 이미 만들었으면 멈추고 사람에게 넘긴다 — 04.3-10 ④-0 「같은 뜻의 자리가 있으면 그것을 쓴다」 규칙). `db/migrations/` 아래의 충돌은 어느 쪽으로도 손으로 풀지 않는다 — ②가 디렉터리째 main 것으로 되돌린다. 그 밖의 파일에서 충돌이 나면 **멈추고** 충돌 파일 목록과 양쪽 의도를 SUMMARY에 적어 사람에게 넘긴다(추측으로 풀지 않는다).

② **재생성 — main을 먼저 온전히 복원하고, 이 페이즈 것만 새로 만든다.** 파일 경로만으로 「이 페이즈 것」을 고르지 않는다(양쪽이 같은 번호로 생성했으면 경로가 같다 — 스냅샷은 `id`/`prevId` 체인으로 이어진다, `0010_snapshot.json` 머리):
  - (a) 기록: 재생성 전 `git diff --name-status origin/main -- db/migrations`와 04.3-02 SUMMARY의 마이그레이션 둘 · 스냅샷 둘의 파일 이름을 SUMMARY에 옮긴다(나중에 대조용 — custom SQL 본문은 (d)가 옛 파일(`git show HEAD:<옛 custom 경로>`)에서 그대로 옮긴다)
  - (b) 복원: `git checkout origin/main -- db/migrations` — main의 SQL · 스냅샷 · `_journal.json`이 main 내용 그대로 돌아온다(같은 경로의 이 페이즈 파일은 main 내용으로 덮인다). 그 뒤 `git ls-tree -r --name-only origin/main -- db/migrations`에 **없는** 파일만 `db/migrations/` 아래에서 지운다(이 브랜치가 더한 파일뿐이다). main에 있는 파일은 하나도 지우거나 고치지 않는다
  - (c) 복원 확인: `git diff --quiet origin/main -- db/migrations`가 종료 코드 0이고 `git ls-files --others --exclude-standard db/migrations`가 빈 출력이다 — 이 순간 `db/migrations/`가 main과 바이트 단위로 같다
  - (d) 생성 — 둘을 이 순서로(04.3-02 Task 1 ② · ②-b와 같다): ⑴ `pnpm db:generate --name cert_intake`(drizzle-kit이 main 마지막 스냅샷과 현재 스키마의 차이로 SQL 하나 · 스냅샷 하나 · journal 항목 하나를 만든다) → 새 SQL 머리에 04.3-02와 같은 `SET LOCAL lock_timeout` · `statement_timeout` 두 줄을 다시 더한다 ⑵ 곧바로 `pnpm db:generate --custom --name cert_winners_unique_deferrable`(drizzle-kit이 다음 번호의 빈 SQL · `prevId`가 ⑴ 스냅샷 `id`인 스냅샷 · journal 항목 하나를 만든다 — `node_modules/drizzle-kit/bin.cjs`의 `writeResult`가 custom에도 스냅샷을 쓴다) → 빈 SQL을 (a)에서 기록한 옛 custom SQL 본문과 **같은 본문**(머리 두 줄 + DROP → UNIQUE INDEX → `USING INDEX … DEFERRABLE INITIALLY IMMEDIATE`)으로 채운다. SQL 본문 말고는 손대지 않는다 — 번호 · journal · 스냅샷은 생성기 값 그대로(예약 번호 범위 없음). `drizzle-kit push`를 쓰지 않는다
  - (e) 체인 확인: `cert_intake` 항목 idx = main 마지막 idx + 1 · custom 항목 idx = 그 + 1 · `when`이 main 마지막 < `cert_intake` < custom · `cert_intake` 스냅샷 `prevId` = main 마지막 스냅샷 `id` · custom 스냅샷 `prevId` = `cert_intake` 스냅샷 `id`(`node -e`로 JSON을 읽어 비교하고 값을 SUMMARY에 적는다) · journal은 줄 단위 diff로 판정하지 않는다 — 항목을 하나 더하면 main 마지막 항목의 `}`가 `},`로 바뀌어 정상이어도 지운 줄이 생긴다. main 항목 전부가 순서·내용 그대로 앞에 있고 뒤에 이 페이즈 항목 둘(`cert_intake` → `cert_winners_unique_deferrable`)만 붙었는지를 verify의 `node -e` 비교(기존 항목 순서·내용 대조)로만 판정한다
  - (f) 내용 대조: 생성된 `cert_intake` SQL이 04.3-02 SUMMARY의 표 · 칸 · 인덱스 · CHECK와 같은지(빠진 것 · 생긴 것 없음)를, custom SQL은 본문이 한 글자도 다르지 않은지를 (a)에서 기록한 옛 SQL(`git show HEAD:<옛 경로>`)과 번호를 빼고 비교해 SUMMARY에 적는다

③ **[BLOCKING] 별도의 빈 DB에서 처음부터 적용 — 개발 DB `erp`는 지우지도 다시 만들지도 않는다.** 확인 전용 DB `erp_migcheck`를 `scripts/reset-test-db.sh`와 같은 방식(`DROP DATABASE IF EXISTS erp_migcheck WITH (FORCE)` → `CREATE DATABASE erp_migcheck OWNER erp`)으로 만들고 `DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_migcheck pnpm db:migrate`로 마이그레이션 전체를 0번부터 적용한다(`db:migrate`의 `.env.local`은 이미 있는 `DATABASE_URL`을 덮지 않는다) → 같은 `DATABASE_URL`로 04.3-02 Task 1 verify의 `pg_constraint` 확인 명령을 돌려 `cert_winners_event_id_name_phone_unique`가 `condeferrable = true` · `condeferred = false`인지 본다(재생성에서 custom 절이 빠지면 여기서 잡힌다) → `pnpm db:reset:test` → `pnpm lint:sql` → `pnpm vitest run --project unit test/unit/db/migration-journal.test.ts`. 확인이 끝나면 `erp_migcheck`만 지운다. 하나라도 실패하면 다음 태스크로 가지 않는다. 개발 DB `erp`에는 옛 번호의 적용 기록이 남아 있을 수 있다 — 고치지 않고 SUMMARY에 「개발 DB는 개발자가 원할 때 직접 다시 만든다」 한 줄만 적는다.

④ **`docs/ARCHITECTURE.md`:** 확인증 경계 요약(10줄 안팎) — 로그인 없는 `/c/[token]` 경로는 `proxy.ts`를 거치지 않고 `publicActionClient`(세션 없음 · 본문 한도)만 쓴다 · 공개 domain(`domain/certs/intake.ts`)은 `can` · `visible` · `scopeFor`를 부르지 않고 토큰 · 행사 id · 자리 id로 where를 좁힌다 · 서명 저장소 포트(로컬 드라이버 / GCS REST, 버킷 수준 권한) · 인쇄 라우트 `/print/…`는 셸 밖이고 페이지가 스스로 인증한다 · 파기는 사람이 실행하는 Cloud Run Job · 데이터 키 봉투 암호화는 04.3-08이 고친 절을 가리킨다. 문서 300줄 이하 — `test/unit/docs-limits.test.ts`가 300줄 상한을 넘었다고 하면 **이 페이즈 요약을 줄인다**(main 쪽 절 · Phase 4가 더한 절은 줄이지 않는다, E3-01). `docs/OPERATIONS.md`가 넘으면 같은 규칙으로 이 페이즈의 「확인증 파기」 절(04.3-12) · §8 추가분(04.3-05)을 줄인다.

④-b **`docs/OPERATIONS.md` §12 설정 가져오기 끝에 한 줄(E3-29):** 「게이트가 꺼진 환경(`CERT_FEATURE_ALLOWED`가 없는 프로덕션)은 게이트가 켜진 환경(스테이징)에서 내보낸 파일의 `cert.enabled`를 `등록되지 않은 키: cert.enabled`로 거부하고 아무것도 쓰지 않는다 — 의도한 동작이다(꺼진 기능의 스위치가 프로덕션에 들어가지 않는다) · 그 키를 파일에서 지우고 다시 가져온다」. 코드는 고치지 않는다.

⑤ 커밋 둘: 머지 커밋(기본 메시지 + 본문 한국어 한 줄로 충돌 해결 요약), `chore(04.3): regenerate cert_intake and deferrable unique migrations on top of main` · `docs(04.3): certificate boundaries in architecture and settings import note`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/db/migration-journal.test.ts test/unit/docs-limits.test.ts test/unit/ui/confirm-dialog.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — journal idx가 비었거나(예약 번호를 썼다) ARCHITECTURE · OPERATIONS가 300줄을 넘었거나, `ConfirmDialog` 머지에서 main 쪽 변경이나 04.3-10의 실패 줄 케이스(④-0을 건너뛰었으면 그 자리의 케이스) 중 하나가 빠졌다(A5 — 양쪽 유지)</fails_when>
    <automated>awk '/^## [0-9]+\. 설정 가져오기/{f=1;next} /^## /{f=0} f' docs/OPERATIONS.md | grep -q "cert.enabled"</automated>
    <fails_when>종료 코드가 0이 아니다 — §12 설정 가져오기에 「게이트 꺼진 환경은 `cert.enabled`를 거부(의도)」 줄이 없다(E3-29)</fails_when>
    <automated>PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp_migcheck WITH (FORCE)' -c 'CREATE DATABASE erp_migcheck OWNER erp' &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_migcheck pnpm db:migrate &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_migcheck node -e 'const {Client}=require("pg");const c=new Client({connectionString:process.env.DATABASE_URL});c.connect().then(()=>c.query("select condeferrable, condeferred from pg_constraint where conname = $1",["cert_winners_event_id_name_phone_unique"])).then((r)=>{c.end();const x=r.rows[0];if(!x||x.condeferrable!==true||x.condeferred!==false){console.error("not deferrable-immediate: "+JSON.stringify(r.rows));process.exit(1)}})' &amp;&amp; pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니다 — 별도의 빈 DB에 마이그레이션 전체가 0번부터 적용되지 않았거나, 적용된 당첨자 UNIQUE가 `DEFERRABLE INITIALLY IMMEDIATE`가 아니거나(custom 재생성 누락), squawk가 위험한 문장을 찾았다</fails_when>
    <automated>node -e 'const cp=require("child_process");const fs=require("fs");const d=cp.execSync("git diff --name-status origin/main -- db/migrations",{encoding:"utf8"}).trim().split("\n").filter(Boolean).map((l)=>l.split(/\s+/));const add=d.filter((x)=>x[0]==="A").map((x)=>x[1]);const oth=d.filter((x)=>x[0]!=="A");const sqls=add.filter((f)=>/\.sql$/.test(f));const snaps=add.filter((f)=>/meta\/\d+_snapshot\.json$/.test(f));const okFiles=add.length===4&amp;&amp;sqls.length===2&amp;&amp;sqls.some((f)=>/cert_intake\.sql$/.test(f))&amp;&amp;sqls.some((f)=>/cert_winners_unique_deferrable\.sql$/.test(f))&amp;&amp;snaps.length===2&amp;&amp;oth.length===1&amp;&amp;oth[0][0]==="M"&amp;&amp;oth[0][1]==="db/migrations/meta/_journal.json";const m=JSON.parse(cp.execSync("git show origin/main:db/migrations/meta/_journal.json",{encoding:"utf8"}));const w=JSON.parse(fs.readFileSync("db/migrations/meta/_journal.json","utf8"));const okJ=w.entries.length===m.entries.length+2&amp;&amp;m.entries.every((e,i)=>JSON.stringify(e)===JSON.stringify(w.entries[i]))&amp;&amp;w.entries.at(-2).tag.includes("cert_intake")&amp;&amp;w.entries.at(-1).tag.includes("cert_winners_unique_deferrable");const snap=(t)=>JSON.parse(fs.readFileSync("db/migrations/meta/"+t.split("_")[0]+"_snapshot.json","utf8"));const okChain=okJ&amp;&amp;snap(w.entries.at(-2).tag).prevId===snap(m.entries.at(-1).tag).id&amp;&amp;snap(w.entries.at(-1).tag).prevId===snap(w.entries.at(-2).tag).id;const body=okJ?fs.readFileSync("db/migrations/"+w.entries.at(-1).tag+".sql","utf8"):"";const okBody=body.includes("DEFERRABLE INITIALLY IMMEDIATE")&amp;&amp;body.includes("lock_timeout");console.log(JSON.stringify({okFiles,okJ,okChain,okBody}));process.exit(okFiles&amp;&amp;okJ&amp;&amp;okChain&amp;&amp;okBody?0:1)'</automated>
    <fails_when>main의 마이그레이션 파일이 하나라도 지워졌거나(`D`) 바뀌었거나(journal 말고 `M`), journal에서 main 항목이 빠지거나 바뀌었거나, 이 페이즈가 더한 파일이 SQL 둘(`cert_intake` · `cert_winners_unique_deferrable`) · 스냅샷 둘이 아니거나, journal 끝 두 항목이 `cert_intake` → custom 순서가 아니거나, 스냅샷 체인(main 마지막 → `cert_intake` → custom)이 끊겼거나, custom SQL에 `DEFERRABLE INITIALLY IMMEDIATE` · `lock_timeout`이 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --merges -1`이 origin/main 머지 커밋이다
    - `db/migrations/`에 `cert_intake`가 든 SQL이 정확히 하나이고(`ls db/migrations/*cert_intake*.sql | wc -l`이 1) `cert_winners_unique_deferrable`이 든 SQL도 정확히 하나다(`ls db/migrations/*cert_winners_unique_deferrable*.sql | wc -l`이 1)
    - `_journal.json`의 마지막 항목 tag에 `cert_winners_unique_deferrable`, 그 앞 항목 tag에 `cert_intake`가 있고, `cert_intake` 스냅샷 `prevId` = main 마지막 스냅샷 `id` · custom 스냅샷 `prevId` = `cert_intake` 스냅샷 `id`다(SUMMARY에 네 값)
    - `erp_migcheck`에서 `pg_constraint`의 `cert_winners_event_id_name_phone_unique`가 `condeferrable = true` · `condeferred = false`였다(verify 둘째 명령, 04.3-02와 같은 확인)
    - `git diff --name-status origin/main -- db/migrations`가 `A` 넷(이 페이즈 SQL 둘 · 스냅샷 둘)과 `M` 하나(`_journal.json`)뿐이다 — main 파일의 `D` · 다른 `M`이 0이고, journal의 main 항목 전부가 순서·내용 그대로 앞에 있고 뒤에 이 페이즈 항목 둘만 `cert_intake` → custom 순서로 붙었다(verify의 `node -e` 비교)
    - 이 태스크의 어느 명령도 `DROP DATABASE` 대상에 `erp`(개발 DB)를 쓰지 않았다 — 확인 DB는 `erp_migcheck`, 테스트 DB는 기존 `pnpm db:reset:test`(`erp_test`)뿐이다
    - 머지에서 `docs/ARCHITECTURE.md` · `docs/OPERATIONS.md`가 충돌했으면 main 절이 먼저 · 이 페이즈 절이 뒤로 모두 남았다(SUMMARY에 두 문서의 절 순서 목록) · 두 문서 모두 300줄 이하(docs-limits) — 줄였다면 줄인 쪽이 이 페이즈 절뿐이다(`git diff origin/main -- docs/ARCHITECTURE.md docs/OPERATIONS.md`에 main 절 본문의 삭제 줄이 없다, E3-01)
    - `docs/OPERATIONS.md` §12에 `cert.enabled` 거부(의도) 줄이 있다(E3-29 — verify 둘째 명령)
    - (A5, 04.3-10 SUMMARY가 ④-0을 건너뛰지 않았을 때만) 머지 뒤 `ui/confirm-dialog/ConfirmDialog.tsx`에 `failure`가 있고(`grep -q 'failure' ui/confirm-dialog/ConfirmDialog.tsx`) `test/unit/ui/confirm-dialog.test.ts`가 main 쪽 케이스와 04.3-10 케이스 모두로 녹색이다(verify 첫 명령) — 충돌이 났으면 SUMMARY에 양쪽 유지로 푼 방법 한 줄 · 건너뛰었으면 SUMMARY가 인용한 같은 뜻의 자리 이름으로 grep한다(/plan-design-review D-13)
  </acceptance_criteria>
  <done>최신 main 위에 이 페이즈 마이그레이션 둘(생성 + 당첨자 UNIQUE 지연 가능 custom)이 연속 번호로 서고, 새 DB에 적용되어 제약이 `DEFERRABLE INITIALLY IMMEDIATE`다</done>
</task>

<task type="auto">
  <name>Task 2: 싼 게이트 → 독립 DOM 감사(별도 에이전트 · CI=true) → 수정</name>
  <files>test/e2e/cert-dom-audit.spec.ts, test/e2e/mobile-cert-dom-audit.spec.ts</files>
  <read_first>
    - 모든 04.3 PLAN의 `must_haves` 중 `verification: backstop` 항목 — 감사 목록의 원본. 수를 여기 적어 두지 않는다(플랜이 고쳐지면 바뀐다): 실행 시점에 `grep -n -B2 "verification: backstop" .planning/phases/04.3-qr-certificate-intake/04.3-*-PLAN.md`로 모으고(이 플랜 자신의 항목은 뺀다) 모은 목록과 개수를 SUMMARY 판정 표 머리에 옮긴다
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「UI Considerations」 backstop 행 · 「상태 점검 보강」 backstop 행
    - `playwright.config.ts`(`CI=true`일 때 프로덕션 빌드 · 04.3-02가 더한 `cert-setup` · `certs` 프로젝트와 `CERT_SPEC_PATTERN`) · `test/e2e/helpers/cert.ts`(규약 C4 도우미) · `test/e2e/cert-events.spec.ts` · `test/e2e/mobile-cert-submit.spec.ts`(준비 · 로그인 · 요청 지연 방식)
  </read_first>
  <action>
① **싼 게이트:** `pnpm lint && pnpm typecheck && pnpm build`. 실패하면 고친 뒤 ②로.

② **독립 감사(실행자가 하지 않는다):** 서브에이전트를 `model: sonnet`으로 **명시해** 띄우고, 프롬프트에 `verification-before-completion` 스킬 사용과 아래 목록 · 판정 규칙(스크린샷 육안 금지, DOM 수치와 요청 수로만 판정, `CI=true`)과 규약 C4(스펙 이름에 `cert`가 들어가 `certs` 프로젝트에서만 돈다 · 행사는 `createCertEvent`로 이름이 겹치지 않게 만든다 · 전역 확인증 설정을 스스로 바꾸지 않는다 · 목록 전체가 비었다는 단언을 하지 않는다 · 폰 스펙은 머리에 `test.use({ viewport: { width: 375, height: 800 } })` · 두 파일 모두 `test.describe.configure({ mode: "serial" })`)를 넣는다. 감사 에이전트가 두 스펙을 쓰고 `CI=true pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-dom-audit.spec.ts test/e2e/mobile-cert-dom-audit.spec.ts`(04.3-02 규약 C4의 빠른 형태 — 의존 프로젝트 `desktop` · `mobile-375` 전체는 Task 3 전체 게이트가 한 번만 돌린다)로 돌려 항목별 통과/실패와 실측값을 표로 돌려준다.
  - 폰(`mobile-cert-dom-audit.spec.ts`, 375): I1 · I3 표 컨테이너와 문서의 `scrollWidth <= clientWidth` · **I2는 폰에서 숨는다(UI-SPEC I2 「1024 미만(좁은 PC · 폰): 만들기 없음」 · 04.3-04 · /plan-design-review D-10)** — `?new=1`로 들어가면 1차 「행사 만들기」와 당첨자 표가 보이지 않고(`toBeHidden` — 요소 없음 또는 `display: none`) 행사 목록(I1)이 보인다를 단언한다(숨은 I2 표의 너비는 재지 않는다 — 숨은 요소는 `0 <= 0`으로 뜻 없이 통과한다) · E4를 폭 360과 폭 180(= 200% 확대)에서 같은 단언 + 주민등록번호 두 칸 · sticky 제출 줄 · 서명 칸이 뷰포트 안 · E3에서 빈 칸 · 3자리 상태로 「전화번호 확인」을 눌러도 서버 액션 요청 0건(요청 수 셈) + 비활성 이유 문장
  - 데스크톱(`cert-dom-audit.spec.ts`): I2 당첨자 표 컨테이너의 `scrollWidth <= clientWidth`(I2 너비는 여기서만 잰다) · I1 제출 셀의 `.sr-only` `제출 3명 / 전체 12명`이 셀 안에 있고 보이는 `3/12`가 `aria-hidden` · 당첨자 1명 행사와 여러 명 행사에서 I2 합계 행 · 저장 문구(`합계 · 1명` 등) · 「행사 만들기」 · I4 「전체 보기」 · 「고친 내용 저장」 요청을 늦췄을 때 버튼 라벨 뒤 `…` + 다른 1차 비활성 · P1을 `page.pdf({preferCSSPageSize: true})`로 뽑아 40자 행사 이름 + 긴 도로명 주소에서 페이지 수 1 · 좁은 PC 폭 1000(/plan-design-review D-10 · SYSTEM §6 ⑷ DR-36): I2 `?new=1` → 1차 「행사 만들기」 · 당첨자 표가 보이지 않고(`toBeHidden`) I1 목록이 보인다 · 편집 가능한 I3 → 편집 입력 · 「일괄 저장」(편집 0칸) · 힌트 줄이 없고 「링크 닫기」가 있다 · 문서 `scrollWidth <= clientWidth`
  - 코드 확인 항목(브라우저로 유도하기 어려움 — 감사 표에 「코드 확인 + `/qa` 실측 필요」로 적는다): E1 `app/c/[token]/error.tsx`와 I1 `app/(app)/certs/events/error.tsx`가 있고 UI-SPEC 문장 그대로인지
  - 감사 밖(사람 · 보안 감사): 실기기 서명 · 보조기술 · 파기와 전체 보기 경합 — Task 3의 사람 확인 · `/cso`로 넘긴다

③ **수정:** 실패 항목마다 원인 → 수정(이 페이즈가 만든 화면 파일 안에서만, 토큰만 — 새 색 · radius 없음) → 감사 스펙만 다시 돌려 녹색. 한 항목 수정이 SYSTEM.md와 어긋나야만 풀리면 고치지 말고 SUMMARY에 적어 `/design-review`로 넘긴다. 감사 표(항목 · 원본 플랜 · 실측값 · 통과/실패 · 수정 커밋)를 SUMMARY에 둔다.

④ 커밋 `test(04.3): independent DOM audit specs`, 수정은 항목마다 `fix(04.3): …`.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>CI=true pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-dom-audit.spec.ts test/e2e/mobile-cert-dom-audit.spec.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed 개수가 1 이상이다 — 감사 항목 중 고치지 못한 것이 남았다</fails_when>
  </verify>
  <acceptance_criteria>
    - 감사 스펙 둘이 서브에이전트가 쓴 것이다(SUMMARY에 에이전트 모델 · 프롬프트 요지 · 판정 표)
    - 폰 스펙이 E4 폭 360과 180 두 경우를 모두 잰다
    - 폰 스펙은 I2 비노출(1차 「행사 만들기」 · 당첨자 표 숨김)과 목록 표시를 단언하고 I2 표 너비를 재지 않으며, I2 표 `scrollWidth <= clientWidth`는 데스크톱 스펙에만 있다
    - 데스크톱 스펙이 P1 PDF 페이지 수 1을 단언한다
    - 데스크톱 스펙이 폭 1000(좁은 PC)에서 I2 비노출 · I3 읽기 표(편집 입력 · 「일괄 저장」(편집 0칸) · 힌트 줄 없음, 「링크 닫기」 있음)를 단언하고, I2 표 너비는 1024 이상 폭에서만 잰다(/plan-design-review D-10 — `grep -n "width: 1000" test/e2e/cert-dom-audit.spec.ts`가 1줄 이상)
  </acceptance_criteria>
  <done>잴 수 있는 backstop 전부가 DOM 수치로 녹색이고, 잴 수 없는 항목은 넘길 곳이 정해져 있다</done>
</task>

<task type="auto">
  <name>Task 3: 전체 게이트 한 번(CI=true) + 페이즈 끝 사람 확인 목록</name>
  <files>test/e2e/cert-dom-audit.spec.ts</files>
  <read_first>
    - 모든 04.3 SUMMARY의 후속 항목(동의 전문 법무 확인 · 설계 `/cso` 열린 질문 · 편차) — `ui/confirm-dialog` · `recordAction(…, {tx})`는 Phase 4가 main에 들어온 뒤 시작해 처음부터 썼으므로 옮기는 후속이 없다(E3-01 — 단, 04.3-02 · 06 · 07 · 10 · 12 SUMMARY 중 「대비책 씀」이 있으면 `recordAction(…, {tx})`로 옮기는 후속 한 줄만 둔다, E3-02 단일 정책)
    - 이 플랜의 「설계 `/cso` 입력 목록」 절(아래 — E3-08 · E3-10 · E3-12 · E3-13 · E3-15 · E3-16 · E3-35 · D-16 · X-10 · N-8)과 설계 `/cso` 결과 반영 기록
    - `.github/workflows/deploy.yml`(main push = 스테이징 자동 배포 · `workflow_dispatch` `target=staging`은 실행한 브랜치의 `github.sha`를 올린다 · 프로덕션 승격 입력 `sha`를 비우면 스테이징이 서빙 중인 SHA를 그대로 올린다) · `docs/OPERATIONS.md` §8(04.3-05 · 08이 고친 부트스트랩 순서) · 04.3-12 SUMMARY(파기 Job 이름 · `purge_certs.done` 모양)
    - `.planning/phases/04.3-qr-certificate-intake/04.3-UI-SPEC.md` 「가정」 표(A1~A12 — 설계 `/cso` · Phase 11이 다시 볼 결정)
  </read_first>
  <action>
① **전체 게이트 한 번:** `pnpm db:reset:test` 뒤 `CI=true pnpm test`(단위 → 통합 → E2E, 프로덕션 빌드) + `pnpm lint:sql`. 실패하면 `systematic-debugging` 스킬로 원인을 찾아 고친 뒤 **실패한 스위트만** 다시 돌리고, 마지막에 전체를 한 번 더 돌린다(고친 게 없으면 다시 돌리지 않는다). 이 태스크가 파일을 고쳤으면 그 파일을 SUMMARY에 적는다(이 태스크의 `files`는 감사 스펙 보정 가능성만 적었다).

② **SUMMARY에 넘길 목록:**
  - 사람 확인(아래 `human-check`)
  - 설계/구현 `/cso` 열린 질문: 이 플랜의 「설계 `/cso` 입력 목록」(E3-08 · D-16 · N-8 · M-3 · E3-15 · E3-10 · E3-12 · E3-13 · E3-35 · E3-16 · X-10)은 설계 `/cso`가 모두 확정했다(아래 절 — 결과와 반영 플랜이 그 표에 있다, 남는 항목 없음). 그 밖에 이번 설계 `/cso`가 다루지 않아 여전히 열린 항목만 옮긴다: 당첨자 명단 전화번호와 수령자 연락처 · 주소 암호화(RESEARCH Open Question 1 · T-04.3-15) · 미제출 명단 30일 상수 재검토 · 멱등 키 모양과 보관 · 대표 계급 차단 · 구별 표시에 남는 개인정보 · 환경 게이트 `CERT_FEATURE_ALLOWED`를 켜는 절차(Phase 11 몫, 규약 C1) · 주민등록번호 검증번호 불일치를 되묻고 받는 규칙(04.3-02) · 공통 오류 로그 축소의 디버깅 영향(규약 C2) · 인쇄 열람 로그
  - 후속: 동의 전문 법무 확인 · 비활동 세션 만료 기본값 120분이 내부관리계획과 맞는지 법무 확인(E3-10 KEEP 확정 — 값은 바뀌지 않는다, 법무 확인만 남는다) · 파기 기한 기산일(3월 1일, 04.3-12:96) 법무 확인 · PATTERNS 「Phase 4와 겹치는 것은 공유 등록부 5개뿐」 전제 정정(실제로는 Phase 4가 비등록부 파일 19개를 고쳤다 — Button · Table · grid 키보드 · ConfirmDialog · `db/client.ts` · `record.ts` · SYSTEM · DECISIONS · ARCHITECTURE, eng-review E3-01. `.planning/` 산출물은 플래너가 고친다) · 파기 실행 누락 감시(E3-16 확정 — 사람 월 1회 + 35일 감시(현행 유지), Scheduler 자동화는 Phase 7 이관) · Codex 재확인(eng-review T12 — 이 페이즈 계획 검토는 Codex 한도로 Opus가 대신했다. 한도가 풀리는 2026-09-29 뒤 eng-review P1 · P2와 「설계 `/cso` 입력 목록」을 Codex로 다시 확인하고 결과를 `docs/designs/plant8-erp-phase04.3-eng-review-260925.md` 부록에 더한다 · /plan-design-review도 Opus 교차였으므로 D-3 · D-7 · D-10 · B-1 · B-4 · N-4(설계 단계 우선순위, cso-design-260925.md 「Codex 재확인 필요 목록」 그대로)를 먼저 Codex로 다시 확인하고 결과를 `docs/designs/plant8-erp-phase04.3-design-review-260925.md` 부록에 더한다 · 설계 `/cso`도 Codex 대신 Opus 교차였으므로 `docs/designs/plant8-erp-phase04.3-cso-design-260925.md` 전체(우선 E3-12 · E3-13 · E3-16 · T-04.3-110)를 Codex로 다시 확인하고 결과를 그 문서 부록에 더한다) · §6-9 세션 만료 화면 부재로 로그인 이동 · 개발 DB `erp`는 개발자가 원할 때 직접 다시 만든다(Task 1 ③)
  - **PR 체크리스트(`/ship`이 PR 본문에 옮긴다 — E3-07):** ☐ 머지 **전에** 소유자가 PR 브랜치의 `scripts/bootstrap-gcp.sh`를 한 번 돌렸다(OPERATIONS §8 — 머지 = main push = 스테이징 자동 배포라 머지 뒤에는 틈이 없다. 버킷 · KMS 역할이 없으면 `deploy.sh`가 확인증 버킷 확인(04.3-05)에서 멈추고 다른 스레드의 main 배포도 함께 막힌다) ☐ bootstrap 실행 뒤 KMS 정리(E3-12 — 프로젝트 단위 `cloudkms.admin` **없음** · 키 단위 `cryptoKeyEncrypterDecrypter` · `cloudkms.viewer` · 런타임 `cryptoKeyDecrypter` 있음)를 04.3-08 `user_setup` 확인 명령들로 다시 봤다 ☐ 확인증 개인정보취급자를 지정했다(E3-13 — 아래 스테이징 스모크 ⓪-b, 교차 B5·R2-9 — 새 DB는 세 권한이 꺼진 채 시작하니 취급자 계급에 켠다 · 04.3-09 이전부터 있던 DB는 insert-if-absent가 기존 true를 끄지 않으니 sysadmin 등 취급자가 아닌 계급의 세 권한을 소유자가 직접 끈다) ☐ 스테이징 스모크(아래 사람 확인) 통과 · 스테이징을 main 머리로 되돌림 ☐ 프로덕션에는 환경 게이트가 없고(04.3-02 — `deploy.sh`는 staging에만 `CERT_FEATURE_ALLOWED=true`) `cert.enabled`는 기본 꺼짐 그대로(Phase 11이 켠다) ☐ 설계 `/cso` 입력 목록이 모두 확정 — 남는 항목 없음(아래 「설계 `/cso` 입력 목록」 절)
  - **통합 · E2E 직렬(E3-33):** 전체 게이트 · 감사 스펙 · 각 플랜의 통합 · E2E는 다른 세션과 겹치지 않게 한 번에 하나로 돌렸다(세션당 플랜 하나) — SUMMARY에 한 줄
  - **Post-build 순서(오케스트레이터 단계 — 이 플랜의 실행 태스크가 아니다):** `/review` → `/design-review`(gstack `/design-review`와 **`codex` 스킬의 Codex(GPT) 디자인 검토를 같은 단계에서 함께** 돌리고 두 결과를 합쳐 고친다 — 사용자 규칙 2026-09-24 「화면 디자인을 검토할 때는 GPT 검토를 꼭 넣는다」) → `/qa` → `/cso`(필수 — 인증 · 암호화 · 외부 입력) → `/ship`. `/ship` 전에 main이 더 움직였으면 Task 1 ②③을 다시 한다. 계획을 다시 검토하는 일이 생기면(`/plan-design-review` 등 계획 게이트, 이 페이즈 PLAN 개정) 같은 규칙으로 Codex 검토를 넣는다(같은 사용자 규칙 — 「계획할 때도」)
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm test &amp;&amp; pnpm lint:sql</automated>
    <fails_when>종료 코드가 0이 아니거나 어느 스위트든 failed 개수가 1 이상이다</fails_when>
    <human-check>iOS Safari와 Android Chrome 실기기에서 E4 서명: 긋는 동안 페이지가 스크롤되지 않고 획이 끊기지 않는다 · 폭을 바꾸는 회전 뒤에도 획이 잘리지 않는다 · E4를 채우는 도중 다른 앱으로 갔다가 돌아오면 어떻게 되는지 관찰해 적는다(탭이 다시 불리면 E2부터 — 값을 메모리에만 두는 결정은 그대로, 결과는 `/qa` · 설계 `/cso` 증표 유효 시간 항목으로 넘긴다, 교차 X-10)</human-check>
    <human-check>VoiceOver(iOS) · TalkBack(Android) 통과 제스처로 E4 서명 완료 기준 ①(키보드 두 획 대신 통과 제스처로 그리기) · ③(「서명이 들어갔습니다」 알림과 `서명 칸 · 서명함`) · ⑤(제출)이 된다</human-check>
    <human-check>I4 「인쇄」 → 실제 인쇄 대화상자 미리보기에서 40자 행사 이름 · 긴 도로명 주소 확인증이 A4 한 장이고 서명이 검정으로 찍힌다</human-check>
    <human-check>스테이징 스모크(E3-07 — **가짜 인물만**: 실존하지 않는 이름 · 서버 검사 규칙만 통과하는 가짜 주민등록번호 · 가짜 주소 · 실제 가입자가 아닌 휴대전화 번호. 실존 인물의 개인정보를 스테이징에 넣지 않는다): ⓪ 준비 — 소유자가 PR 브랜치의 `scripts/bootstrap-gcp.sh`를 돌렸다(PR 체크리스트 첫 줄) → 04.3-08 `user_setup` 확인 명령들로 KMS 정리(E3-12 — 프로젝트 단위 `cloudkms.admin` 없음 · 키 단위 역할 있음)를 다시 봤다 → Actions `deploy` 워크플로를 PR 브랜치에서 `target=staging`으로 실행(그 브랜치 머리가 스테이징에 올라가고 `deploy.sh --env staging`만 `CERT_FEATURE_ALLOWED=true`를 싣는다 — 04.3-02) → 스테이징 관리자 > 설정에서 `cert.enabled`를 켠다 ⓪-b **확인증 개인정보취급자 지정(E3-13, 소유자)** — 스테이징 경영관리 계급 담당자(또는 04.3-02 `cert.setup`이 명시적으로 켠 대상)를 확인증 취급자로 지정했는지 확인한다(시드 기본값이 아니다). **교차 B5·R2-9(정정) — 새 DB(insert-if-absent가 처음 넣는 값)에서는 sysadmin 계급의 세 권한(`certs.submissions`·`cert.rrn_unmasked`·`cert_submission.value`)이 꺼진 채 시작하므로, 취급자로 지정할 계급에 이 셋을 권한 화면에서 켠 뒤 스모크를 진행한다. 04.3-09 시드 이전부터 있던(이미 켜진 값을 가진) 스테이징·프로덕션 DB에서는 insert-if-absent가 기존 true 값을 끄지 않으므로 sysadmin이 그 셋을 그대로 유지한다 — 이 경우 소유자가 권한 화면에서 취급자가 아닌 계급의 세 권한을 직접 꺼야 하고(E3-13: 이후 재배포가 그 선택을 되돌리지 않는다), 켜져 있다고 해서 문제가 있는 것은 아니다**: ① I2에서 가짜 당첨자 둘로 행사 만들기 ② I3 화면의 QR을 실기기 폰 카메라로 찍으면 `/c/{token}` E2가 열린다(스테이징 URL) → 이름 고르기 · 뒤 4자리 → E4 제출 ③ I4에서 서명 이미지가 보인다(05 GCS 드라이버 · 버킷 IAM의 실제 읽기) · 「전체 보기」 한 번(08 KMS로 푼 데이터 키의 실제 복호화) ④ `gcloud run jobs execute plant8-staging-purge-certs --region {리전} --wait`(인자 없음 = 미리 보기) → Job 로그 `purge_certs.done`이 `mode: "dry-run"`이고 아무것도 비우지 않았다 ⑤ 정리 — `cert.enabled`를 끄고, PR을 곧바로 머지하지 않으면 Actions `deploy`를 main에서 `target=staging`으로 다시 실행해 스테이징을 main 머리로 되돌린다(프로덕션 승격에서 `sha`를 비우면 스테이징이 서빙 중인 SHA를 그대로 올리므로 PR 브랜치 SHA를 남겨 두지 않는다 — `.github/workflows/deploy.yml`)</human-check>
    <human-check>동의 전문 초안(근거법령 · 보존 기간 기산점 · 제공처)을 법무가 확인했거나, 확인 전이라 프로덕션 플래그를 켜지 않는다는 사실이 기록돼 있다. 설계 `/cso` 확정 사항 중 법무가 볼 둘도 같이 확인한다 — 비활동 세션 만료 기본값 **120분**(KEEP)이 내부관리계획과 맞는지(E3-10) · 파기 기한 기산일이 **3월 1일**(제출 연도 + 1 + 보존 연수)인지(04.3-12)</human-check>
  </verify>
  <acceptance_criteria>
    - `CI=true pnpm test`가 한 번(수정이 있었으면 마지막 한 번 더) 녹색이고 그 요약(스위트별 통과 수)이 SUMMARY에 있다
    - SUMMARY에 사람 확인 다섯(스테이징 스모크 포함) · `/cso` 열린 질문 · 후속 · PR 체크리스트 · 통합 · E2E 직렬 한 줄 · Post-build 순서가 있다
    - SUMMARY의 PR 체크리스트에 머지 전 소유자 bootstrap 줄이 있다(`grep -n "bootstrap-gcp.sh" .planning/phases/04.3-qr-certificate-intake/04.3-13-SUMMARY.md`가 1줄 이상, E3-07)
    - SUMMARY의 후속에 Codex 재확인(2026-09-29 뒤) 줄이 있고, 네이티브 dialog를 `ui/confirm-dialog`로 옮기는 후속 · `recordAction` tx 인자를 나중에 맞추는 후속이 없다(E3-01 — 처음부터 Phase 4 부품을 썼다. 04.3 SUMMARY 중 「대비책 씀」이 있을 때만 그 전환 후속 한 줄이 있다 — E3-02 단일 정책)
    - SUMMARY의 Post-build 순서 줄에 `/design-review`와 `codex`(Codex 디자인 검토)가 한 단계로 함께 있고, 계획 재검토에도 Codex 검토를 넣는다는 줄이 있다(`grep -c 'codex' .planning/phases/04.3-qr-certificate-intake/04.3-13-SUMMARY.md`가 1 이상)
  </acceptance_criteria>
  <done>전체 게이트가 녹색이고, 사람 확인과 Post-build로 넘길 항목이 한 곳에 모였다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이 브랜치 ↔ origin/main | 다른 페이즈가 같은 등록부 · 마이그레이션 체인을 고친다 — 머지에서 한쪽 항목이 사라지면 권한 · 로그 · 누수 검사가 조용히 빠진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-63 | Tampering | 등록부 머지 충돌 해결 | high | mitigate | 추가 전용 등록부는 양쪽 항목을 모두 남기는 규칙 하나 · 그 밖의 충돌은 멈추고 사람에게. `registry-coverage` · `leak-scan-coverage` · `record` 단위 테스트가 전체 게이트에서 빠진 항목을 잡는다 |
| T-04.3-64 | Denial of Service | 마이그레이션 체인 | medium | mitigate | 생성기로만 재생성(`prevId` = main 마지막 `id`) · 새 DB에 처음부터 적용 · journal 연속 테스트 · squawk |
| T-04.3-65 | Elevation of Privilege | 감사 없이 끝난 화면 결함 | low | mitigate | 실행자가 아닌 에이전트의 DOM 실측 감사와 사람 확인 목록 — 보안 판정 자체는 Post-build `/cso`(이 플랜의 태스크가 아니다) |
| T-04.3-191 | Information Disclosure | 스테이징 스모크 데이터(E3-07 — 스테이징에서만 게이트 허용) | medium | mitigate | 스모크는 가짜 인물만 쓴다(사람 확인 문장에 명시 — 실존 이름 · 주민등록번호 · 주소 · 번호 금지). 끝나면 `cert.enabled`를 끄고 스테이징을 main 머리로 되돌린다. 스테이징 `cert.enabled` 기본값은 꺼짐이고 프로덕션에는 환경 게이트가 없다(04.3-02 deploy 단위 테스트). 스테이징에 남은 가짜 제출분의 파기 시점은 설계 `/cso`에서 확정 |
| T-04.3-192 | Denial of Service | 머지 = 스테이징 자동 배포인데 버킷 · KMS 역할이 없음(E3-07) | medium | mitigate | PR 체크리스트 첫 줄 「머지 **전에** 소유자가 PR 브랜치의 `scripts/bootstrap-gcp.sh`를 한 번」 — 빠뜨리면 `deploy.sh`가 확인증 버킷 확인에서 멈추고 다른 스레드의 main 배포도 막힌다. 순서는 OPERATIONS §8(04.3-05)과 같은 문장 |
</threat_model>

<verification>
- 단위 `migration-journal` · `docs-limits` 녹색, 새 DB에 `pnpm db:migrate` · `pnpm lint:sql` 녹색
- `CI=true` 감사 스펙 둘 녹색
- `CI=true pnpm test` 한 번 녹색
- 사람 확인 다섯이 SUMMARY 목록에 있다
</verification>

<success_criteria>
- 최신 main 위에 마이그레이션 둘(생성 + custom) · 연속 번호 · 당첨자 UNIQUE `DEFERRABLE INITIALLY IMMEDIATE`
- 잴 수 있는 backstop 전부가 독립 DOM 감사로 녹색
- 전체 게이트 한 번 녹색, Post-build로 넘길 목록 완비 — PR 체크리스트(머지 전 소유자 bootstrap) · 스테이징 스모크(가짜 인물만) · 설계 `/cso` 입력 목록 · Codex 재확인 후속 포함
- 운영 문서 둘은 main 절이 먼저 · 이 페이즈 절이 뒤로 합쳐졌고 300줄 이하이며, §12에 `cert.enabled` 가져오기 거부(의도) 줄이 있다
</success_criteria>

## 설계 `/cso` 입력 목록 (eng-review T11)

D-1107의 설계 `/cso`(계획 게이트 뒤 · 실행 전)가 이 목록 전부를 확정했다 — `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`(2026-09-25, 최종 판정 표). 아래 각 줄은 「확정 — `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`」와 최종 값 · 반영 플랜을 적는다. 남는 항목은 없다(Task 3 ②의 `/cso` 열린 질문은 이 목록 밖의 다른 항목만 옮긴다). **설계 `/cso` 교차 검토도 Codex 대신 Opus였다(한도 2026-09-29까지) — 한도가 풀리면 이 문서 전체와 eng-review P1 · P2를 Codex로 다시 확인한다(T12, Task 3 ② 후속).**

| id | 질문 | 확정 — `docs/designs/plant8-erp-phase04.3-cso-design-260925.md` |
|---|---|---|
| E3-08 | 행사 · IP 틀림 한도 수치 · throttled 해제 시각 표시 여부 | 확정 — 창 **15분**, 행사 한도 `max(40, ceil(명단×0.5))`/15분. 해제 시각·`tel:` 줄 없음, D-3 문장 그대로. T-04.3-110 위험 문장·맵 상한 4×L 재계산, 행사 단위 확률(1−0.998ⁿ) 주석 추가 → 04.3-03 |
| E3-10 | 개인정보 비활동 만료 때 끊는 범위 — 세션 전체 대 개인정보 경로만 | 확정 — 교차 B-2 **KEEP 기본 120분**(세션 전체, 문구 변경 없음). 법무가 내부관리계획 부합 확인 필요(Task 3 사람 확인) → 04.3-07 |
| E3-12 | 배포자 `roles/cloudkms.admin`(프로젝트 단위) 축소 | 확정 — (가)안 + 교차 B-3: 키 단위 `cryptoKeyEncrypterDecrypter` + 키 단위 `cloudkms.viewer`, bootstrap이 프로젝트 단위 `cloudkms.admin` 제거 · `cloudkms.googleapis.com` API도 bootstrap에서 켬. T-04.3-91 → mitigate → 04.3-08 |
| E3-13 | 시스템 관리자 계급이 개인정보취급자 범위에 드는지 — 시드가 매번 다시 켜는 문제 | 확정 — 제작자 방향(insert-if-absent) 채택, medium 신규 발견으로 승격 → 04.3-02 · 07 · 09 · 10 · 11 |
| E3-15 | IP 해시 `sha256(eventId + ip)`는 역산 가능한 가명 — HMAC으로 바꿀지 | 확정 — HMAC-SHA256, 키는 `BETTER_AUTH_SECRET`에서 HKDF 파생(교차 M-1 (b)) → 04.3-03 · 04.3-08 |
| E3-16 | 파기 Job 실행 누락 감시 · 주기 · 자동화 | 확정 — 교차 B-5 (나): 이 페이즈는 **사람 월 1회 + 35일 감시 런북 유지**, Scheduler 자동화는 **Phase 7 이관**(D19-8과 일치). T-04.3-58 「사람 의존, Phase 7에서 자동화」 → 04.3-12 |
| E3-35 | 행사당 누적 당첨자 상한(500 넘으면 처리) | 확정 — 누적 500 상한 초과 시 **invalid** → 04.3-10 |
| D-16 | IP당 한도가 행사장 와이파이 · CGNAT 공유 IP에 걸리는 문제 | 확정 — IP 한도 **20회/15분/행사**(19→통과, 20→throttled) → 04.3-03 |
| N-8 | throttled 「잠시 뒤」 문구와 실제 멈춤 시간 차이 · 문의 줄 여부 | 확정 — 문구 유지(제작자 안), T-04.3-30에 「공격이 멈춘 뒤」 조건 명시(교차 R-3) → 04.3-03 |
| X-10 | 확인 증표 유효 시간 — 연장 여부 | 확정 — **30분 유지, 연장 없음**. 담당은 04.3-03이 아니라 **04.3-06**(교차 M-2, expiredProof E2E 케이스 추가) → 04.3-06(주요) · 04.3-03(상수 확정 표시) |

새 설계 발견(이 목록에 없던 항목, 참고): **M-3**(IPv6는 해시 전 /64 prefix로 묶는다 `ipKey(ip)` → 04.3-03) · **E3-13 부속**(배포 시드 재실행이 개인정보취급자 해제를 조용히 되살리는 medium 신규 발견 → 04.3-09, 위 E3-13과 같은 반영) · **T-04.3-110 불일치**(창 60→15분 재계산 누락, 위 E3-08에 포함).

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/*_cert_intake.sql`(main 마지막 + 1) · `*_cert_winners_unique_deferrable.sql`(+ 2, drizzle-kit `--custom`) — 둘 다 재생성 · 스냅샷 둘 · journal 항목 둘 |
| 문서 | `docs/ARCHITECTURE.md` 확인증 경계 요약 |
| 감사 스펙 | `test/e2e/cert-dom-audit.spec.ts` · `test/e2e/mobile-cert-dom-audit.spec.ts` |
| 문서 | `docs/OPERATIONS.md` §12 `cert.enabled` 가져오기 거부(의도) 한 줄 |
| 넘김 목록 | 사람 확인 다섯(스테이징 스모크 포함) · `/cso` 열린 질문 · 후속 · PR 체크리스트 · Post-build 순서(`/design-review` + Codex 디자인 검토 포함, SUMMARY) · 이 플랜의 「설계 `/cso` 입력 목록」 |

## Review Dispositions Ledger

> 출처: `docs/designs/plant8-erp-phase04.3-eng-review-260925.md`(plan-eng-review 2026-09-25) — 반영 지시 L387–455@953e599(공통 L391–394, 04.3-13 항목 L448–453, 설계 `/cso` 넘김 L455), Implementation Tasks T11 · T12(L530–533@953e599). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요.

### Round 1 — 953e599

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E3-01 Phase 4 겹침 파일(비등록부 19개)을 마지막 13에서 한 번에 머지 → 멈춤 · 중복 구현 | P1 | objective 「실행 조건」(Phase 4 main 머지 뒤 시작 · 웨이브마다 main 머지라 ①은 증분) · Task 1 `<precondition>` · ① 「양쪽 유지」에 `docs/ARCHITECTURE.md` · `docs/OPERATIONS.md`(절 단위, main 절 먼저) · ④ 300줄 넘으면 이 페이즈 요약을 줄임 · Task 1 수용 줄 · Task 3 read_first · ② 후속에서 dialog 옮기기 · tx 인자 후속을 지우고 PATTERNS 「5개뿐」 정정을 SUMMARY로 |
| E3-07 스테이징에서 기능이 꺼져 13 사람 확인이 불가능 · 부트스트랩 시점(머지 = 자동 배포) | P1 | Task 3 스테이징 스모크 human-check(행사 → 폰 제출 → I4 서명 · 전체 보기 → 파기 미리 보기, 가짜 인물만, 끝나면 스테이징을 main 머리로) · ② PR 체크리스트 첫 줄 「머지 전 소유자 bootstrap」 · truths 두 줄 · Task 3 수용 줄 · T-04.3-191 · T-04.3-192 |
| E3-29 게이트 꺼진 환경이 `cert.enabled` 가져오기를 「등록되지 않은 키」로 거부 — 운영자가 이유를 모름 | P3 | Task 1 ④-b OPERATIONS §12 한 줄(의도 · 다음 행동) · files · verify `<automated>` awk + `<fails_when>` · 수용 줄 |
| E3-33 같은 웨이브 병렬 실행이 `erp_test` · 포트 3100을 함께 쓴다는 주장 | P3 | objective 「실행 조건」(세션당 플랜 하나 · 로컬 두 세션 동시 실행 금지) · Task 3 ② SUMMARY 한 줄 |
| T11 설계 `/cso` 입력 목록(E3-08 · E3-10 · E3-12 · E3-13 · E3-15 · E3-16 · E3-35) | P3 | 「설계 `/cso` 입력 목록」 절(7행, 각 줄 「→ 설계 /cso에서 확정」 · 계획의 현재 기본값과 자리) · truths · Task 3 ② `/cso` 열린 질문이 그 절을 옮긴다 |
| T12 Codex 한도가 풀리면(2026-09-29 뒤) P1 · P2를 Codex로 재확인 | P3 | 「설계 `/cso` 입력 목록」 머리 문장 · Task 3 ② 후속 줄 · 수용 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| E3-03 서명 이미지 저장 위치 · E3-04 12 · 08 범위와 시점 | deferred — user check pending (default kept). 이 플랜은 둘 다 기본값(GCS · 현 계획)으로 머지 · 감사 · 스모크를 한다. 사용자가 바꾸면 스모크 ③ · ④와 사람 확인이 바뀐다 |
| E3-08 · E3-10 · E3-12 · E3-13 · E3-15 · E3-16 · E3-35의 수치 · 범위 · 권한 결정 | 설계 `/cso`(D-1107) 몫 — 이 플랜은 입력 목록만 두고 정하지 않는다(→ 설계 /cso에서 확정) |

### Round 2 — 72829e6

> 출처: 04.3 eng-apply 재검토 r1 — Opus 플랜 체커(`eng-apply-checker-r1`, W/I 번호) · 교차 검토(`eng-apply-opus-cross-r1`, X/A 번호). 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W1 · 교차 A3 — frontmatter `execution_gate` 없음 | warning | 01~09와 같은 `execution_gate` 블록을 frontmatter에 복사했다 |
| 체커 I1 · 교차 A2 — tx 로그 부재 정책 갈림 | info | 오케스트레이터 결정(모든 플랜 한 정책 — 04.3-02 `insertActionLogTx` 대비책): Task 1 `<precondition>`에서 `origin/main` `record.ts`의 `tx?: DbOrTx` 멈춤을 뺐다(Phase 4 머지 확인은 그대로). Task 3의 「`recordAction` tx 후속 없음」 두 줄은 04.3 SUMMARY 중 「대비책 씀」이 있을 때만 전환 후속 한 줄을 둔다고 좁혔다 |
| 교차 A4 — `## 14.`(04.3-12 절) 하드코딩 · §12 범위 `## 12.`~`## 13.` | advisory | Task 1 read_first · ④ 줄이기 규칙에서 「§14」를 「확인증 파기」 절 제목으로, verify 둘째 줄을 절 제목(`설정 가져오기`) 범위 awk로 바꿨다 |
| 교차 A5 — 04.3-10 ④-0이 Phase 4 공용 `ConfirmDialog`에 `primary.failure`를 더해 Phase 4 쪽 변경과 충돌할 수 있음 | advisory | ① 머지 규칙의 「양쪽 유지」에 `ui/confirm-dialog/ConfirmDialog.tsx`와 `test/unit/ui/confirm-dialog.test.ts`를 더했다(main 쪽 변경 먼저 · `failure` 슬롯 · 케이스를 그 위에, main이 같은 뜻의 슬롯을 이미 만들었으면 멈춤) · verify 첫 줄에 그 단위 테스트를 더하고 `fails_when`을 고쳤다 · acceptance (A5) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적은 모두 반영했다 |

### Round 3 — 1c3a27b

> 출처: `docs/designs/plant8-erp-phase04.3-design-review-260925.md`(/plan-design-review 2026-09-25 — 주 검토 D-1~D-16 · 부록 교차 검토 X-1~X-12). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요(우선 D-3 · D-7 · D-10). 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| D-13 (N1) Task 1 (A5) `failure` grep이 04.3-10 ④-0을 건너뛴 경우를 모른다(L119–120@1c3a27b) | 참고 | Task 1 수용 줄 (A5)를 「04.3-10 SUMMARY가 ④-0을 건너뛰지 않았을 때만」 조건부로 · 건너뛰었으면 SUMMARY가 인용한 같은 뜻의 자리 이름으로 grep · verify 첫 명령 `fails_when`을 「실패 줄 케이스(④-0을 건너뛰었으면 그 자리의 케이스)」로 |
| D-16 현장 와이파이 · 통신사 CGNAT 공유 IP와 IP당 10회 한도(L128–130@1c3a27b) · 교차 X-2의 IP 한도 재조정 부분(L186–188@1c3a27b) | 참고 | 「설계 `/cso` 입력 목록」에 D-16 행(수치는 정하지 않음 → 설계 /cso에서 확정) · truths · Task 3 read_first의 id 목록에 D-16 |
| D-3 throttled 문장 확정 — 이 플랜의 E3-08 행 「문구는 `/plan-design-review`」가 낡음(L48–57@1c3a27b) | 경고 | E3-08 행을 D-3 확정 문장으로 고치고 남은 /cso 몫(해제 시각 표시 = D-3 선택지 C · 수치)만 남겼다 |
| D-10 좁은 PC — UI-SPEC I2 앵커 「폰(<700): 만들기 없음」이 바뀜(L105–108@1c3a27b) | 경고 | Task 2 ② 폰 감사의 UI-SPEC 인용을 새 앵커(「1024 미만(좁은 PC · 폰): 만들기 없음」)로 · 데스크톱 감사에 폭 1000 항목(I2 비노출 · I3 읽기 표) + 수용 줄 |
| X-10 E4 값 메모리 보관 · 앱 전환 · 증표 유효 시간(L220–222@1c3a27b) | 참고 | 「설계 `/cso` 입력 목록」에 X-10 행(증표 유효 시간 — 수치는 /cso) · Task 3 첫 사람 확인(실기기)에 「앱 전환 뒤 복귀」 관찰 한 줄(사람 확인 수는 다섯 그대로) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| X-7 구별 표시 상시 힌트(「결정은 /cso에 맡긴다」 선택지) | UI-SPEC 쪽(shard A) 결정이 먼저다. 이 플랜의 Task 3 ② `/cso` 열린 질문에 「구별 표시에 남는 개인정보」가 이미 있어 행을 따로 두지 않는다 |
| D-1 ~ D-9 · D-11 · D-12 · D-14 · D-15 · X-1 · X-3 ~ X-6 · X-8 · X-9 · X-11 · X-12 | 이 플랜의 태스크 · 문구에 닿는 것이 없다(UI-SPEC · 01~11 쪽). 감사 스펙 문구는 실행 시점 UI-SPEC을 따른다 |

### Round 4 — 66a98e1

> 출처: 디자인 반영 검사 r1 — `design-apply-checker-r1.md`(Opus 플랜 체커) · `design-apply-cross-r1.md`(교차 검토 B-1~B-4 · N-1~N-14). **교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요.** 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 N-8 — throttled 「잠시 뒤」 대 최대 60분 멈춤 · 문의 줄 없음 · 폰 CGNAT | 참고 | 「설계 `/cso` 입력 목록」에 N-8 행(수치 · 문의 줄 여부는 /cso — 이 플랜은 정하지 않는다) · truths와 Task 3 read_first의 id 목록에 N-8 |
| 교차 r2(`design-apply-cross-r2.md` @c9a728d — 교차 검토는 Codex 대신 Opus — 한도 풀리면 Codex 재확인 필요) R2-7 — 폭 1000 「일괄 저장」 없음은 편집 0칸일 때만 참(N-4 「`N ≥ 1`이면 항상 렌더」와 겹쳐 읽힘) | 참고 | 수용 줄과 Task 2 데스크톱 감사 항목 두 곳의 「일괄 저장」 뒤에 「(편집 0칸)」 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 B-1 ~ B-4 · N-1 ~ N-7 · N-9 ~ N-14 · 체커 W1 ~ W3 | 이 플랜의 태스크 · 문구에 닿지 않는다(UI-SPEC · 01 · 03 · 04 · 10에서 반영). 감사 스펙은 실행 시점 UI-SPEC 문구 · 색 규칙(I2 막힘 `block` · I3 좁은 PC 「일괄 저장 N」 N ≥ 1)을 따른다 |

### Round 5 — 184df28

> 출처: `docs/designs/plant8-erp-phase04.3-cso-design-260925.md`(설계 `/cso` 최종 판정 2026-09-25 — 최종 판정 표, 항목별 근거 요약, 사람 확인 목록, 반영할 플랜 목록 04.3-13). 설계 `/cso` 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 「설계 `/cso` 입력 목록」 10항목(E3-08 · E3-10 · E3-12 · E3-13 · E3-15 · E3-16 · E3-35 · D-16 · N-8 · X-10)이 모두 확정 — 남는 항목 없음 | high | truths(39행) · Task 3 ② 열린 질문 문단 재작성(목록 밖 항목만 옮김) · 「설계 `/cso` 입력 목록」 표 전면 교체(각 행 「확정 — 설계 `/cso`」 + 최종 값 + 반영 플랜) · 새 발견(M-3 · E3-13 부속 · T-04.3-110) 각주 추가 |
| 후속 목록 「배포자 KMS 역할 축소(E3-12 최소 권한 (가)안)」 | resolved | 04.3-08 Task 2 ④가 B-3 remove 단계(프로젝트 단위 `cloudkms.admin` 제거)를 반영했으므로 후속에서 지웠다 |
| 후속 목록 「파기 실행 누락 자동 감시(E3-16)」 | resolved | 「사람 월 1회 + 35일 감시(현행 유지), Scheduler 자동화는 Phase 7 이관」으로 교체 |
| 소유자 절차 — 확인증 개인정보취급자 지정(E3-13) · bootstrap을 머지 전에 돌려 KMS 정리(E3-12)가 적용됐는지 확인 | medium | PR 체크리스트에 KMS 정리 확인 · 취급자 지정 체크 항목 추가 · 스테이징 스모크 human-check에 ⓪(KMS 정리 확인) · ⓪-b(취급자 지정) 단계 추가 · truths(37행) |
| 법무 확인 — 비활동 120분(E3-10 KEEP) · 파기 기산일(3월 1일) | low | 동의 전문 human-check에 두 확인 항목 추가 · 후속 목록에 두 줄 추가 |
| Codex 재확인 목록에 설계 `/cso` 리포트 추가(T12) | low | Task 3 ② 후속 Codex 재확인 문단에 `plant8-erp-phase04.3-cso-design-260925.md` 추가(우선 E3-12 · E3-13 · E3-16 · T-04.3-110) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| Supersedes Round 1 Addressed: 「설계 `/cso` 입력 목록」이 정하지 않은 값들 | 값이 모두 정해졌다 — 표를 「확정」으로 교체했으므로 이 supersession은 Addressed로 기록(위 표 첫 행) |
| M-3(IPv6 /64) · E3-13 부속(시드 재켜짐) · T-04.3-110(창 재계산) — 이 목록에 없던 새 발견 | 「설계 `/cso` 입력 목록」 각주로만 기록 — 실제 반영은 04.3-03 · 04.3-09가 각자 플랜에서 한다(이 플랜의 태스크는 아니다) |

### Round 6 — (working-tree, cso-apply-cross-r1)

> 출처: `/mnt/project-files/04.3-gates/cso-apply-cross-r1.md`(설계 `/cso` 반영 r1 교차 검토 — Opus, Codex 대신, 한도 2026-09-29까지 — 한도 풀리면 Codex 재확인 필요). 커밋 전 작업 트리 diff에 대한 검토라 REVIEWS.md 커밋 sha가 없다. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| N-4 — Codex 재확인 우선순위 목록이 cso-design-260925.md 「Codex 재확인 필요 목록」의 D-3 · D-7 · D-10 · B-1 · B-4 · N-4 여섯 항목 중 N-4를 빠뜨림(D-3 · D-7 · D-10만 있었다) | low | Task 3 ② 후속 Codex 재확인 문단(204행)에 B-1 · B-4 · N-4를 더해 여섯 항목 전부로 맞췄다 |
| B5 — E3-13 뒤로 시드가 sysadmin 계급에 `certs.submissions`·`cert.rrn_unmasked`·`cert_submission.value`를 더 이상 자동으로 켜지 않아, 취급자 지정 전에는 스테이징/프로덕션에서 시스템 관리자도 확인증 목록·전체 보기가 비거나 막힌 것처럼 보인다 | low | 스테이징 스모크 human-check ⓪-b에 한 줄(취급자 지정 전 목록·전체 보기 안 보임은 정상 — 권한 화면에서 세 권한을 켠 뒤 진행) · PR 체크리스트의 취급자 지정 항목에 같은 안내 추가 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적(N-4)은 반영했다 |

### Round 7 — (working-tree, cso-apply-cross-r2)

> 출처: `/mnt/project-files/04.3-gates/cso-apply-cross-r2.md`(설계 `/cso` 반영 r2 교차 검토 — Opus, Codex 대신, 한도 2026-09-29까지 — 한도 풀리면 Codex 재확인 필요). 커밋 전 작업 트리 diff에 대한 검토라 REVIEWS.md 커밋 sha가 없다. 앞 라운드 표는 고치지 않는다(append-only).

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Note 9 — Round 6에서 더한 취급자 권한 안내(`:205`·`:215`)가 이미 시드된 DB에는 틀리다: insert-if-absent는 기존 true 행을 끄지 않으므로, 04.3-09 이전부터 있던 스테이징·프로덕션 DB는 sysadmin 계급이 세 권한(`certs.submissions`·`cert.rrn_unmasked`·`cert_submission.value`)을 그대로 유지한다 — "취급자 지정 전엔 안 보이는 게 정상"은 새 DB에만 참이다 | medium | `:215`(human-check ⓪-b)과 `:205`(PR 체크리스트)를 두 경우로 다시 씀 — 새 DB는 세 권한이 꺼진 채 시작(취급자 계급에 켬) · 04.3-09 이전부터 있던 DB는 insert-if-absent가 기존 true를 끄지 않으므로 sysadmin이 그대로 유지(소유자가 취급자가 아닌 계급의 세 권한을 권한 화면에서 직접 끔, E3-13 — 이후 재배포가 그 선택을 되돌리지 않는다) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 이 플랜에 걸린 지적(Note 9)은 반영했다. R2-5(08:261 줄 번호 인용)는 04.3-08 소관 |

<output>
Create `.planning/phases/04.3-qr-certificate-intake/04.3-13-SUMMARY.md` when done
</output>
