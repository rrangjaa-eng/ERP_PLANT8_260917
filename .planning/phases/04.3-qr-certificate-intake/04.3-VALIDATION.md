---
phase: "04.3"
slug: "qr-certificate-intake"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-24"
---

# Phase 04.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase 04.3` from `04.3-RESEARCH.md` § Validation Architecture.
> The Per-Task Verification Map is filled by `/gsd-validate-phase` once PLAN.md files exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (`test/unit`, `test/integration`) + Playwright (`test/e2e`) |
| **Config file** | `vitest.config.ts` (2-project: `unit` / `integration`), `playwright.config.ts` |
| **Quick run command** | `pnpm vitest run --project unit` |
| **Full suite command** | `pnpm test` (단위 → 통합 → E2E) |
| **Estimated runtime** | ~90 seconds (unit ~15s; full suite needs `pnpm db:dev`) |

**Note:** 통합·E2E는 로컬 DB가 필요하다 (`pnpm db:dev`). 완료 판정은 `CI=true` — `playwright.config.ts`가 CI에서만 프로덕션 빌드를 쓴다 (CLAUDE.md).

---

## Sampling Rate

- **After every task commit:** Run `pnpm vitest run --project unit`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd-verify-work`:** Full suite must be green under `CI=true`, and both `/cso` audits (design + post-build) passed (D-1107)
- **Max feedback latency:** 15 seconds (unit project)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by `/gsd-validate-phase` once PLAN.md files exist) | | | | | | | | | |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Requirement → Test Map (from RESEARCH § Validation Architecture)

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| CERT-01 | 수령자 QR 진입 → 이름 선택 → 뒤 4자리 확인 → 입력 → 서명 제출 → 자리 닫힘 | E2E | `pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/mobile-cert-intake.spec.ts test/e2e/mobile-cert-verify.spec.ts test/e2e/mobile-cert-submit.spec.ts` (완료 판정은 의존 순서 그대로 `CI=true pnpm playwright test`) | ❌ 04.3-02 · 03 · 06이 만든다 |
| CERT-01 | 5회 실패 → 3분 잠금 판정(순수 함수) | unit | `pnpm vitest run test/unit/certs/verify-last4.test.ts` | ❌ W0 |
| CERT-01 | 사람당 1회 제출(재제출 거부) | integration | `pnpm vitest run test/integration/cert-intake.test.ts` | ❌ W0 |
| CERT-01 | 한도 넘은 확인은 잠금 전 `throttled` · 한도 초과 동시 확인 `DB_POOL_MAX`×3 동안 다른 행사 `loadIntake` 5초 안(AX-P2) | integration | `pnpm vitest run --project integration test/integration/cert-verify.test.ts` | ❌ W0 |
| CERT-01 | 잠금 · 풀 연결 시간 초과 `serverError`(`다른 저장이 끝나지 않음 · 잠시 뒤 다시 저장`)는 확정 판정이 아니다 → 결과 불명 · 같은 멱등 키로 재전송(AX-P2) | unit | `pnpm vitest run --project unit test/unit/certs/flow-rules.test.ts` | ❌ W0 |
| CERT-02 | 평문 주민등록번호가 DB에 없음 | integration | `pnpm vitest run test/integration/cert-crypto.test.ts` | ❌ W0 |
| CERT-02 | 비권한자 전체 보기 거부 | integration | `pnpm vitest run test/integration/cert-crypto.test.ts` | ❌ W0 |
| CERT-02 | 전체 보기 → `mask_reveal` 로그 1건 | integration | `pnpm vitest run test/integration/cert-crypto.test.ts` | ❌ W0 |
| CERT-02 | 권한 없는 사람(기획 PM · 대표)의 I4 · 인쇄 404가 비활동 시계를 건드리지 않는다 — `touchPrivacySession` `notAllowed` · 그 세션의 `privacy_session_activity` 0줄(RB-5) | integration · E2E | `pnpm vitest run --project integration test/integration/cert-review.test.ts` · `pnpm playwright test --no-deps --workers=1 --project=cert-setup --project=certs test/e2e/cert.setup.ts test/e2e/cert-review.spec.ts test/e2e/cert-print.spec.ts` | ❌ W0 (E2E는 04.3-07 · 11이 만든다) |
| CERT-02 | 기동 때 KMS로 푼 데이터 키를 다른 모듈 인스턴스(`vi.resetModules()` 뒤 다시 import한 `lib/crypto`)가 본다 — 키는 `globalThis[Symbol.for("plant8.appDataKeys")]` (RB-P1) | unit | `pnpm vitest run --project unit test/unit/crypto.test.ts` | ✅ (기존 파일 확장, 04.3-08) |
| CERT-02 | `/api/health`가 라우트 쪽 인스턴스에서 데이터 키 `encrypt` → `decrypt` 왕복, 실패면 `503 {ok:false}` · 응답 필드 `ok` · `sha` · `deployedAt` 불변 → 배포 스모크가 잡는다 (RB-P1) | integration | `pnpm vitest run --project integration test/integration/health.test.ts` | ✅ (기존 파일 확장, 04.3-08) |
| CERT-02 | 보존 기간 지난 건 파기 | integration | `pnpm vitest run test/integration/purge-expired-certs.test.ts` | ❌ W0 |
| CERT-02 | 제출하지 않은 명단 파기 | integration | `pnpm vitest run test/integration/purge-expired-certs.test.ts` | ❌ W0 |
| 기준 5 (D-1106) | 정정 시 행동 로그(평문 주민등록번호 미포함) | integration | `pnpm vitest run test/integration/cert-review.test.ts` | ❌ W0 |
| 기준 5 | 새 DTO(행사·당첨자·확인증)가 누수 스캔에 등록 | integration | `pnpm vitest run test/integration/leak-scan.test.ts` | ✅ (기존 파일 확장) |

---

## Wave 0 Requirements

- [ ] `test/unit/certs/verify-last4.test.ts` — 잠금 판정 순수 함수 (CERT-01)
- [ ] `test/integration/cert-intake.test.ts` — 토큰 조회·선택·확인·제출·1회 제한 (CERT-01)
- [ ] `test/integration/cert-verify.test.ts`(04.3-03) — 동시 틀림 · 멱등 재생 · 속도 제한 · 잠금 전 `throttled` · 풀 고갈 없음(AX-P2) (CERT-01)
- [ ] `test/unit/certs/flow-rules.test.ts`(04.3-03) — 고아 기록 항목 판정 · 응답 분류(확정 판정 / 결과 불명 — `throttled` · `serverError` 시간 초과 문장 → 결과 불명, AX-P2) (CERT-01)
- [ ] `test/integration/cert-crypto.test.ts` — 주민등록번호 암호화·마스킹 해제·행동 로그 (CERT-02)
- [ ] `test/integration/purge-expired-certs.test.ts` — 파기 (CERT-02)
- [ ] `test/integration/cert-review.test.ts` — 경영관리 정정·행동 로그 (D-1106) · 비활동 세션 — 권한 없는 사람은 활동 행 0줄(RB-5)
- [ ] `test/e2e/cert-review.spec.ts`(04.3-07) · `test/e2e/cert-print.spec.ts`(04.3-11) — 데스크톱 I4 · 인쇄 E2E, PM 404 · PM 세션 활동 행 0줄(RB-5) (CERT-02)
- [ ] `test/e2e/mobile-cert-intake.spec.ts`(04.3-02) · `test/e2e/mobile-cert-verify.spec.ts`(04.3-03) · `test/e2e/mobile-cert-submit.spec.ts`(04.3-06) — 로그인 없는 외부 흐름 E2E (CERT-01). `certs` 프로젝트(`*cert*.spec.ts`)와 `cert-setup` 의존이 있어야 돈다(04.3-02 규약 C4)
- Framework install: 불필요 (Vitest/Playwright 설치됨)

---

## Manual-Only Verifications

04.3-13 Task 3의 `human-check` 다섯과 같은 목록이다 — 한쪽을 바꾸면 다른 쪽도 바꾼다(RB-6). 지시 문장의 정본은 04.3-13이다.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 실기기 E4 서명(iOS Safari · Android Chrome) | CERT-01 | 터치 입력 · 스크롤 잠금 · 회전은 실제 기기에서만 판정된다 | 긋는 동안 페이지가 스크롤되지 않고 획이 끊기지 않는다 · 폭을 바꾸는 회전 뒤에도 획이 잘리지 않는다 · 다른 앱으로 갔다 돌아온 결과를 적는다(04.3-13 첫 human-check) |
| 보조기술(VoiceOver · TalkBack)로 E4 서명 · 제출 | CERT-01 | 스크린 리더 통과 제스처는 브라우저 자동화로 재현할 수 없다 | E4 완료 기준 ① · ③ · ⑤가 통과 제스처로 된다(04.3-13 둘째 human-check) |
| I4 「인쇄」 실제 인쇄 미리보기 | 기준 5 | 인쇄 대화상자의 쪽 나눔은 DOM 감사로 판정되지 않는다 | 40자 행사 이름 · 긴 도로명 주소 확인증이 A4 한 장이고 서명이 검정으로 찍힌다(04.3-13 셋째 human-check) |
| **스테이징 스모크** — 실제 폰 카메라로 행사 QR을 찍어 진입 → 제출 → I4 서명 읽기 · 「전체 보기」 → 파기 미리 보기(가짜 인물만) | CERT-01 · CERT-02 | 실제 GCS · KMS 경로와 카메라 스캔은 스테이징에서만 거친다 — 머지 전 KMS 경로(요청 경로의 데이터 키 · 런타임 decrypter)를 사람이 증명하는 단계. 배포 로그의 `smoke`가 `/api/health` `ok:true`로 통과했는지도 본다(04.3-08 — 라우트 인스턴스의 키 왕복, RB-P1) | 순서 그대로: ⓪ bootstrap · KMS 정리 확인 · 스테이징 배포(로그의 `smoke` 통과 확인) · `cert.enabled` 켜기 → ⓪-b 취급자 지정 → ① 행사 → ② 폰 카메라 QR → 제출 → ③ I4 서명 · 전체 보기 → ④ 파기 미리 보기 → ⑤ 정리(04.3-13 넷째 human-check) |
| 동의 전문 · 보존 규칙 법무 확인 | CERT-02 | 법률 판단 | 동의 전문 초안(근거법령 · 보존 기간 기산점 · 제공처)과 비활동 세션 만료 120분(E3-10) · 파기 기한 기산일 3월 1일을 법무가 확인했거나, 확인 전이라 프로덕션 플래그를 켜지 않는다는 사실이 기록돼 있다(04.3-13 다섯째 human-check) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
