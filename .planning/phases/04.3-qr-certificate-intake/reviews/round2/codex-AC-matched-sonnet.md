# Codex Round-2 findings — verified against repo (read-only)

Scope: only the final numbered MAJOR/MINOR lists at the end of codex-r2-A.txt (shard A: 04.3-01/02/13) and codex-r2-C.txt (shard C: 04.3-07…12). R1 FIXED/PARTIAL re-judgment tables were skipped per instructions.

| shard | # | Codex severity | title | verdict | evidence checked (file:line) | needed plan change |
|---|---|---|---|---|---|---|
| A | 1 | MAJOR | 개인정보 로그 수정과 "기존 테스트 유지"가 동시에 불가능 | CONFIRMED | 04.3-02-PLAN.md:400 ("기존 케이스는 그대로 두고 ... 더한다"); 04.3-02-PLAN.md:406 ("message를 빼고 ... 모든 액션에 공통으로 적용"); test/unit/actions/handle-server-error.test.ts:79-88 (asserts `loggedPayload.toContain("Failed query")`); lib/actions/handle-server-error.ts (current `log.error(..., {name, message: e.message})`); lib/log.ts (`write()` spreads `...fields` over `message: event`, so today `fields.message` — the raw SQL text — overwrites the outer message and is what the existing test actually catches). Removing `message` per line 406 removes "Failed query" from the JSON entirely, so the untouched old test at line 80-86 must fail. The two instructions are literally contradictory, not just in tension. | Explicitly replace/rewrite the old "원본 오류를 서버 로그에 남긴다" test to assert only safe metadata (name/code/constraint), instead of instructing to leave it untouched. |
| A | 2 | MAJOR | 확인증 Playwright 프로젝트가 기존 admin-nav E2E 전제를 깨뜨림 | CONFIRMED | 04.3-02-PLAN.md:260 (`cert-setup`/`certs` projects depend only on each other, not on `desktop`/`mobile-375`; `CERT_FEATURE_ALLOWED??="true"` set globally at config top); playwright.config.ts:99 (current: only `desktop→mobile-375` dependency chain exists, confirming no cert-project ordering yet); test/e2e/global-setup.ts:1-29 (single global seed/migration run shared by ALL projects against one `erp_test` DB); 04.3-09-PLAN.md:151 (seed grants PM `certs.events` view/write unconditionally, not gated); test/e2e/admin-nav.spec.ts:112-124 (asserts PM sees no "관리" and `/admin`→404). Since `cert-setup`'s `setSettingValue(cert.enabled, true)` runs in the same shared DB with no ordering dependency forcing it after `desktop`/`mobile-375` finish, and env-side gate is globally true, the cert admin-menu group can appear for PM while admin-nav.spec.ts (desktop project) is running concurrently, breaking its assertion. (The actual gating mechanism `withCertMenusGated` in 04.3-09-PLAN Task 1 is correctly designed — the bug is purely the cross-project race/ordering, not a permanent logic gap.) | Add an explicit Playwright `dependencies` chain so `cert-setup`/`certs` run only after `desktop` and `mobile-375` complete (or give the cert specs an isolated DB), and note this ordering requirement in the plan. |
| A | 3 | MAJOR (NEW) | 트레이서 직접 액션 호출 통합 테스트가 import 단계에서 막힘 | CONFIRMED | 04.3-02-PLAN.md:253 and :263 (require calling `app/c/[token]/actions.ts`'s three actions directly, no page, from `test/integration/cert-intake.test.ts`); lib/actions/client.ts:2 (`import { getSession } from "@/lib/viewer"` at module top, unconditional); lib/viewer.ts:1 (`import "server-only"`); vitest.config.ts (integration project has no alias/mock for `server-only`/`next/headers`); test/integration/leak-scan.test.ts:9-16 (repo's own comment: "actions.ts 자체는 use server → lib/actions/client.ts → lib/viewer.ts → server-only/next/headers 의존 체인이라 Vitest(node 환경)에서 import할 수 없다... 실측: Cannot find package 'server-only'"). Even if `app/c/[token]/actions.ts` only uses a not-yet-built `publicActionClient`, any export from `lib/actions/client.ts` still statically pulls in `lib/viewer.ts` at module load, so the same documented import failure applies. The plan gives no mock/alias setup to avoid it. | Either split `publicActionClient` into its own file that never statically imports `lib/viewer.ts`, add a `server-only`/`next/headers`/`getSession` mock for the integration project, or move this tracer assertion from a direct module-call test to a real HTTP POST test. |
| A | 4 | MINOR (NEW) | 01이 승인된 UI 계약(①-h, ①-l)을 이전 문구로 되돌림 | CONFIRMED | 04.3-01-PLAN.md:184 ("①-h 택배 부제 `--fs-sm`" and "①-l ... 값이 없으면 번호 없는 문장"); 04.3-01-PLAN.md:189 (external-cert.html action: "①-h(`.lbl-sub` `--fs-sm`)"); 04.3-UI-SPEC.md:111, :199, :565, :568, :728 (all five places consistently specify the parcel subtitle changes from `--fs-xs` → `--fs-md`, because it is a full sentence, never `--fs-sm`); 04.3-UI-SPEC.md:160 ("번호 없는 대체 문장은 두지 않는다" — no such fallback sentence is to exist, since settings validation blocks event creation without a valid contact phone); docs/design/system/external-cert.html:46 (current mockup still has `.lbl-sub{font-size:var(--fs-xs)...}`, the pre-plan state this task is meant to fix to `--fs-md`, not `--fs-sm`). Both the font-size value and the "no-number fallback sentence" instruction in 01-PLAN directly contradict the ratified UI-SPEC decisions it is supposed to carry over. | Fix 04.3-01-PLAN.md lines 184/189 to say `--fs-md` (not `--fs-sm`) for the parcel subtitle, and drop the "번호 없는 문장" instruction for ①-l per UI-SPEC:160. |
| A | 5 | MINOR (NEW) | RESEARCH에 폐기된 마이그레이션 번호 예약 지시가 남음 | CONFIRMED | 04.3-RESEARCH.md:59 (still under an "Open questions" list: "마이그레이션 번호: Phase 4가 0011~0016을 쓰므로 0017부터 ... 계획 때 정한다" — reserve/decide a number at plan time); 04.3-RESEARCH.md:382 (Pitfall 4, same doc, explicit "How to avoid: 번호를 예약하지 않는다" and full merge-time regenerate procedure); 04.3-PATTERNS.md:250 (repeats "번호는 예약하지 않는다"); scripts/migrate-runner.ts:54 (confirms `migrate()` applies only journal entries newer than the last-applied `when`, which is exactly why pre-reserving numbers is unsafe — matches Pitfall 4's own reasoning). Line 59 is a genuine leftover stale note contradicting the same document's own later, more authoritative decision. | Mark RESEARCH.md line 59 as superseded/strike it, pointing to Pitfall 4's decision (no number reservation; regenerate right before merge). |
| C | 1 | MAJOR | 키 회전 후 서비스가 v2 암호문을 읽지 못함 | CONFIRMED | 04.3-08-PLAN.md:216 (`deploy_service` secrets changed only to `APP_DATA_KEY_v1_WRAPPED=...:latest` + `APP_DATA_KEY_KMS_KEY` env var — no `APP_DATA_KEY_v2_WRAPPED` ever provisioned to the deployed Cloud Run service); 04.3-08-PLAN.md:230 (runbook only says the *operator's own ADC-based CLI* sets `APP_DATA_KEY_v1_WRAPPED`/`v2_WRAPPED`/`KMS_KEY` locally to run `pnpm db:rotate-key` — no step deploys v2 to the running service first); scripts/rotate-key.ts:52-72 (`rotateKey()` requires both keys and immediately re-encrypts every row to the newest version — i.e., DB ciphertext becomes v2 the moment the operator runs it, regardless of what the live service has); lib/crypto.ts:35-46,79 (`keyFor("v2")` throws `MissingEncryptionKeyError` if `env.APP_DATA_KEY_v2` is absent; `decrypt()` picks the key strictly by the ciphertext's version prefix); scripts/deploy.sh:441 region (no v2-wrapped secret entry in the service's `--set-secrets`). Sequence as written: operator rotates DB to v2 → live service (only has v1) now fails to decrypt any newly-rotated row. | Add an explicit plan step: deploy the service with both `APP_DATA_KEY_v1_WRAPPED` and `APP_DATA_KEY_v2_WRAPPED` present *before* running `db:rotate-key`, and only remove v1 from the service after confirming no v1 ciphertext remains. |
| C | 2 | MAJOR | 기존 PM 관리자 메뉴 E2E와 새 시드가 충돌 | CONFIRMED | Same evidence as shard A finding #2 (04.3-09-PLAN.md:151, 04.3-02-PLAN.md:260, playwright.config.ts:99, test/e2e/admin-nav.spec.ts:112, test/e2e/global-setup.ts). Duplicate finding across shards, same root cause and same verdict. | Same as A-2: sequence `cert-setup`/`certs` after `desktop`+`mobile-375`, or isolate DB. |
| C | 3 | MAJOR | `cert_submission.value` 정보항목을 꺼도 DTO가 그대로 반환됨 | CONFIRMED | 04.3-07-PLAN.md:178 (`getSubmissionForReview` builds `CertSubmissionReviewDto` by listing fields directly and calling `registerDto`, with no call to `project()`); 04.3-11-PLAN.md:127 (`getCertificatePrint` — identical pattern, `registerDto` only); domain/permissions/dto-registry.ts:14-18 (`registerDto` only pushes a static `{name, fields}` declaration into `DTO_REGISTRY` for leak-scan's static analysis — it performs no runtime filtering); domain/permissions/project.ts:19-32 (the actual runtime per-field `visible()` filtering lives only in `project()`); grep of 07-PLAN and 11-PLAN confirms neither ever calls `project(`. So turning off `cert_submission.value` in the visibility table has no runtime effect on what these two functions return — the field-level info-item permission is registered for the leak-scan test but never actually enforced at request time. | Route the DTO construction in both `getSubmissionForReview` and `getCertificatePrint` through `project()` (or an equivalent per-field `visible()` check) instead of just registering the spec with `registerDto`. |
| C | 4 | MAJOR (NEW) | 전체 보기/재열람 액션이 메뉴 권한(`can`)을 우회함 | CONFIRMED | 04.3-07-PLAN.md:179 (`revealRrn`/`recordRrnReopen`: "게이트 → 대표 → 권한(`visible(viewer, "cert.rrn_unmasked")`)" — only the info-item `visible()` check, no `can(viewer, "certs.submissions", "view")` call, unlike `getSubmissionForReview` on the line just above which does call `can()`); domain/permissions/visible.ts (module comment: "can()과 완전 독립(D-35)"); lib/actions/client.ts:26-38 (`authedActionClient` only checks payload size + `getSession()` login, no permission check at all). Since `visible` and `can` are independently-granted permission axes, a staff member who still has `cert.rrn_unmasked` visibility but has had `certs.submissions` menu access revoked could call `revealCertRrnAction`/`reopenCertRrnAction` directly with a known submission id and still receive the plaintext RRN. | Add `can(viewer, "certs.submissions", "view")` (or equivalent) as a required check in `revealRrn` and `recordRrnReopen`, and add a test for the "menu revoked, info-item still on" combination via direct action call. |
| C | 5 | MAJOR (NEW) | 전체 보기의 로그 기록이 별도 전역 연결을 써서 DB 풀 고갈 위험 | CONFIRMED | 04.3-07-PLAN.md:179 (`revealRrn`: inside `withTransaction` holding a `FOR SHARE` row lock, calls `recordAction(...)` explicitly annotated "기존 전역 연결" — i.e., a second, separate pool connection is acquired while the first is still checked out for the lock); repositories/action-log.ts:3,33 (`import { db } from "@/db/client"`; `appendActionLog` does `await db.insert(actionLog)...` against the single shared global pool, not the caller's `tx`); db/client.ts:38-41 (`pool`/`db` are one process-wide singleton `Pool`); lib/env.ts:54 (`DB_POOL_MAX: numberWithDefault(5)`, i.e. default max 5 connections). With pool max 5, five concurrent `revealRrn` calls each hold one connection via the transaction's lock and then each try to get a sixth connection from the same pool for the log insert — none can proceed, since all five are occupied by transactions waiting on that same pool. | Move the `recordAction`/`appendActionLog` call for `revealRrn` (and any other tx-held-lock caller) onto the same `tx` handle instead of the global pool, or release the lock before logging. |
| C | 6 | MAJOR (NEW) | 링크 닫기(`closeEvent`) 로그 실패가 영구 누락으로 이어짐 | CONFIRMED | 04.3-10-PLAN.md:170 (`closeEvent`: `withTransaction` → conditional close UPDATE → **커밋** → THEN "커밋 뒤 `recordAction(...)`" for `status_change`, i.e. logging happens strictly after commit, not atomically with the close); domain/action-log/record.ts:69 (`status_change` is in `ALWAYS_ON_ACTION_TYPES`, a log type described as never turned off); domain/action-log/record.ts:133 (`appendActionLog` call inside `recordAction`, executed via the module-level `defaultAppendActionLog`, independent of the caller's transaction). Since `closeEvent`'s own first check on retry is "이미 닫힘 ... `{kind: alreadyClosed}`" (short-circuits before any log attempt), if the post-commit `recordAction` call throws once, a retry never re-attempts the log write, so the close is permanently unrecorded for the always-on `status_change` history the plan itself calls non-turn-off-able. | Make the close UPDATE and the `status_change` log insert part of the same transaction (commit both together, or roll back the close if the log insert fails), instead of logging after commit. |
| C | 7 | MAJOR (NEW) | KMS 배포/PM 시드 태스크가 구현 후 테스트 순서(TDD 위반) | CONFIRMED | 04.3-08-PLAN.md: Task 2 (`<task type="auto">`, no `tdd="true"` marker) — action steps ①(line 205)-④ are all implementation (`infra/names.sh`, `deploy.sh`, `bootstrap-gcp.sh`, fake gcloud), and only step ⑤ (line 223) is "테스트:"; 04.3-09-PLAN.md Task 2 similarly has ① (seed change) before ② (`test/integration/seed-permissions.test.ts`, RED/GREEN not sequenced first); CLAUDE.md:104 ("TDD: 실패 테스트 → 최소 구현 → 리팩터. 실제 실행 확인 없이 '완료' 금지"), a repo-wide rule not scoped only to `tdd="true"`-tagged tasks. As literally ordered, both tasks write the implementation before the corresponding tests, which is implementation-first, not test-first. | Reorder both tasks' action steps so the test files are written and run RED before the corresponding implementation step, or explicitly mark these two tasks as an approved exception per CLAUDE.md's "말하고 승인" rule. |
| C | 8 | MINOR | 인쇄 서버 렌더 실패에 승인된 ERROR 화면이 없음 | CONFIRMED | 04.3-11-PLAN.md:161 (②-c ERROR handling covers only the client-side signature `decode()`/`onerror` failure, i.e., a JS-side branch inside `print-sheet.tsx`); 04.3-11-PLAN.md files list (only `page.tsx`, `loading.tsx`, `print-sheet.tsx`, `print-cert.module.css` — no `error.tsx`); repo check: `app/(app)/error.tsx` exists but the new route is `app/print/certs/[id]/page.tsx`, outside the `(app)` route group, so that boundary does not apply to it; no `app/print/**/error.tsx` exists or is planned; 04.3-UI-SPEC.md:540-542 (ERROR state contract includes "전체 보기 실패 한 줄" but the print-route-specific approved copy "인쇄물을 만들지 못했습니다 · 다시 시도" is only wired for the client-side signature-decode failure, not for a server-side data-fetch throw in `getCertificatePrint`). A server-side exception on this route would fall through to Next's default/root error handling instead of the approved copy. | Add an `app/print/certs/[id]/error.tsx` (or equivalent) rendering the approved "인쇄물을 만들지 못했습니다 · 다시 시도" copy for server-side render failures, separate from the client-side signature-decode ERROR branch. |
