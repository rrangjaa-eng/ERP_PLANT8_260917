---
phase: 04.4-restore-rehearsal-and-login-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/ops.ts
  - db/schema/index.ts
  - db/migrations/NNNN_restore_rehearsals.sql
  - db/migrations/meta/NNNN_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/restore-rehearsals.ts
  - domain/ops/restore-rehearsal.ts
  - domain/system-status/index.ts
  - app/(app)/admin/system-status/page.tsx
  - app/(app)/admin/system-status/restore-rehearsal-view.ts
  - scripts/restore-rehearsal-cli.ts
  - test/integration/restore-rehearsal.test.ts
  - test/unit/db/migration-journal.test.ts
  - test/unit/ops/restore-rehearsal-view.test.ts
  - test/unit/system-status.test.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "리허설 결과는 운영 DB 자신의 `restore_rehearsals` 표에 한 줄로 남는다 — 원본 환경(staging|production) · 성공 여부 · 실패 단계(restore|verify|cleanup, 성공이면 NULL) · 백업 id(NULL 가능) · 시작·종료 시각(timestamptz) · GitHub Actions 실행 URL(NULL 가능). 기록은 같은 컨테이너 이미지의 CLI `record` 하위 명령이 쓰고 사람은 DB에 명령하지 않는다(D8-08, ROADMAP 04.4 기준 2)"
    - "DB check 제약이 불변식을 지킨다: 원본은 staging·production 둘 중 하나이고, `succeeded = true`이면 `failed_stage IS NULL`, `succeeded = false`이면 `failed_stage IS NOT NULL`이면서 restore·verify·cleanup 중 하나다 — 성공×단계 전 조합(성공+NULL 허용 · 성공+단계 거부 · 실패+NULL 거부 · 실패+단계 허용 · 실패+모르는 단계 거부 · 원본 local 거부)을 도메인을 건너뛴 직접 SQL로 증명한다(Codex #17)"
    - "CLI `record`는 원본 환경을 인자로 받지 않고 컨테이너의 `APP_ENV`에서 정한다(staging → staging, prod → production, local → 사용 오류 종료 코드 2) — 스테이징 Job이 프로덕션 기록을 만들 경로가 없다. CLI 진입점은 DB·리포지토리·도메인 모듈을 정적으로 불러오지 않고, 인자·`APP_ENV` 검사를 통과한 뒤에만 동적으로 불러온다(Codex #11)"
    - "도메인 `recordRestoreRehearsal`은 쓰기 전에 거부한다: 성공⇔단계 불일치 · 모르는 단계 · 유한하지 않은 시각 · 종료 < 시작 · 숫자가 아닌 백업 id · `https://github.com/<owner>/<repo>/actions/runs/<숫자>` 모양이 아닌 실행 URL(Codex #17)"
    - "관리자 시스템 상태 화면의 「마지막 백업」 바로 다음 줄이 「복원 리허설」이다(UI-SPEC Component Inventory · 04.2와 상대 순서만 고정)"
    - "기록이 0행이면 값은 `리허설 기록 없음 — 첫 리허설 전`이고, 조회 자체가 실패하면(연결·권한·표 없음·잠금 대기 초과·읽은 행이 유효하지 않음 등 종류 불문) 그 행만 `StatusTag kind=\"muted\"` `확인 불가`이며 나머지 항목은 정상으로 보인다 — 두 경우를 하나로 뭉치지 않는다(UI-SPEC Copywriting Error/Empty · UI Considerations error·partial covered)"
    - "조회는 트랜잭션 한정 `SET LOCAL statement_timeout = '2000ms'` · `lock_timeout = '1000ms'`로 묶여 표가 잠겨 있어도 상태 화면이 멈추지 않고 그 행만 `확인 불가`가 된다(Codex #15). 도메인은 읽은 행을 쓰기와 같은 규칙으로 다시 검증해(원본·단계 조합 · 유한한 시각 · 종료 ≥ 시작 · 백업 id 숫자 · 실행 URL 모양) 어긋나면 그 행만 `확인 불가`로 만든다(Codex #16)"
    - "성공 기록은 `성공 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 12분` 모양이고, 실패 기록은 `실패 · 검증 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 4분 · 실행 기록`처럼 결과 바로 뒤에 단계 하나(복원·검증·정리), 맨 끝에 같은 탭으로 열리는 3차 링크 `실행 기록`이 붙는다. 백업 id가 NULL이면 `백업 …` 세그먼트와 그 구분자가 통째로 빠진다(UI-SPEC 복원 리허설 값 행 · populated covered). 링크·백업 id의 모양(CSS)과 브라우저 E2E·SYSTEM.md §6-8은 04.4-05가 붙인다"
    - "일시는 종료 시각을 `Asia/Seoul` 24시간 `YYYY-MM-DD HH:MM`으로, 소요 시간은 종료 − 시작이 60초 미만이면 `1분 미만`, 아니면 초를 버린 정수 `N분`으로 보인다. 여러 기록 중 종료 시각 내림차순 최신 1건(동시각이면 `seq` 내림차순)을 보인다"
    - "화면은 서버 렌더 스냅샷이다 — 로딩 구간·폴링·진행 중 상태를 추가하지 않는다(UI Considerations loading covered, Outside Review #10 반려 유지)"
    - "이 플랜의 마이그레이션 `NNNN_restore_rehearsals`의 번호는 db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1이다 — SQL·스냅숏·journal 이름과 idx를 손으로 바꾸지 않는다. 머지 직전 재생성은 04.4-06 Task 1이 한다(조정자 마이그레이션 규칙)"
    - "journal 가드 `test/unit/db/migration-journal.test.ts`는 main에 있으면 main 파일을 고치지 않고 쓰고, 없으면 04.1 브랜치(`origin/claude/phase-04.1-plan-1iqtwe` 또는 그 실행 브랜치)의 최신 커밋에 있는 파일을 글자 그대로 복사한다 — 이 페이즈가 따로 쓴 버전은 없다(조정자 규칙). 어디에도 없으면 만들지 않고 SUMMARY에 적는다"
  prohibitions:
    - statement: "리허설 결과를 기록하려고 사람이 운영 DB에 명령을 치는 경로를 만들어서는 안 된다 — 기록은 Cloud Run Job 안의 CLI만 쓴다(CLAUDE.md 금지 · ROADMAP 04.4 기준 2)"
      status: resolved
      verification: test
    - statement: "스테이징 기록이 프로덕션 증거로 보여서는 안 된다 — 각 환경의 화면은 자기 DB만 읽고, 원본 환경은 인자가 아니라 컨테이너 `APP_ENV`로 정해진다(Phase 8 기준 5)"
      status: resolved
      verification: test
    - statement: "`restore_rehearsals` 조회 실패·잠금 대기·유효하지 않은 행이 화면 전체 오류(공유 `app/(app)/error.tsx`)나 멈춤으로 번져서는 안 된다 — 조회는 시간 제한으로 묶이고 도메인·상태 합성이 자기 안에서 잡아 `확인 불가`로 흡수한다(UI-SPEC Error state, Codex #15·#16)"
      status: resolved
      verification: test
    - statement: "실행 URL을 검증 없이 `href`에 넣어서는 안 된다 — `https://github.com/<owner>/<repo>/actions/runs/<숫자>` 모양만 저장되고, 읽을 때도 같은 규칙으로 다시 확인한다"
      status: resolved
      verification: test
    - statement: "CLI가 인자·`APP_ENV` 검사를 통과하기 전에 DB 모듈(Cloud SQL 커넥터)을 불러와서는 안 된다 — `scripts/restore-rehearsal-cli.ts`에 `@/db`·`@/repositories`·`@/domain` 정적 import가 없다(Codex #11, 커넥터 미초기화 단위 테스트는 04.4-02)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/ops.ts"
      provides: "restore_rehearsals 표 + check 제약 둘"
      contains: "restore_rehearsals"
    - path: "db/migrations/NNNN_restore_rehearsals.sql"
      provides: "restore_rehearsals 생성 SQL — NNNN은 db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1"
      contains: "restore_rehearsals_stage_check"
    - path: "repositories/restore-rehearsals.ts"
      provides: "insertRestoreRehearsal · findLatestRestoreRehearsal (viewer 첫 인자)"
      exports: ["insertRestoreRehearsal", "findLatestRestoreRehearsal"]
    - path: "domain/ops/restore-rehearsal.ts"
      provides: "recordRestoreRehearsal · getLatestRestoreRehearsal · RestoreRehearsalRecord(행 타입이 아닌 도메인 타입)"
      exports: ["recordRestoreRehearsal", "getLatestRestoreRehearsal"]
    - path: "app/(app)/admin/system-status/restore-rehearsal-view.ts"
      provides: "formatRestoreRehearsal — 결과·원본·일시·백업 id·소요 시간·실행 링크 세그먼트"
      exports: ["formatRestoreRehearsal"]
    - path: "scripts/restore-rehearsal-cli.ts"
      provides: "record 하위 명령(parseArgs · main, account-cli 규약)"
      exports: ["parseArgs", "main", "UsageError"]
    - path: "test/integration/restore-rehearsal.test.ts"
      provides: "CLI record → DB → getSystemStatus 트레이서와 기록 없음·최신 선택 증명"
      min_lines: 60
  key_links:
    - from: "scripts/restore-rehearsal-cli.ts"
      to: "domain/ops/restore-rehearsal.ts"
      via: "record 하위 명령이 SYSTEM_VIEWER로 recordRestoreRehearsal을 부른다"
      pattern: "recordRestoreRehearsal"
    - from: "domain/system-status/index.ts"
      to: "domain/ops/restore-rehearsal.ts"
      via: "StatusDeps.getLatestRestoreRehearsal — try/catch로 unavailable 흡수"
      pattern: "getLatestRestoreRehearsal"
    - from: "app/(app)/admin/system-status/page.tsx"
      to: "app/(app)/admin/system-status/restore-rehearsal-view.ts"
      via: "KvList 「복원 리허설」 항목 값 조립"
      pattern: "formatRestoreRehearsal"
    - from: "db/schema/index.ts"
      to: "db/schema/ops.ts"
      via: "배럴 끝 export 한 줄 — test/integration/setup.ts가 배럴의 모든 표를 TRUNCATE한다"
      pattern: "ops"
---

<objective>
**As a** 관리자, **I want to** 시스템 상태 화면에서 마지막 복원 리허설이 언제·어느 원본으로·어느 백업으로·성공했는지(실패면 어느 단계에서) 한 줄로 보기를, **so that** 백업이 실제로 복원되는지 사람이 운영 DB에 손대지 않고 확인할 수 있다.

이 플랜은 D8-08의 「결과를 기록하고 상태 화면 「복원 리허설」 항목으로 보인다」 절반을 만든다. Task 1은 **트레이서**다 — CLI `record` → 도메인 → 리포지토리 → `restore_rehearsals` 표 → `getSystemStatus` → 상태 화면 한 줄(성공 기록 모양)을 한 번에 관통하고, 그 경로에 필요한 것만 구현한다. Task 2는 **빨간 테스트를 먼저 본 뒤** 쓰기·읽기 검증, 조회 시간 제한, `확인 불가` 흡수, 실패 문구(단계 · 1분 미만 · 백업 id 생략 · 실행 링크)로 넓힌다(Codex #20). 모양(CSS) · 브라우저 E2E · SYSTEM.md §6-8은 04.4-05가 붙인다(플랜 파일 수 15 이하 — 계획 검사 scope_sanity).

Purpose: ROADMAP 04.4 기준 2(결과가 상태 화면에 보인다 · 기록 없음 · 확인 불가 · 사람이 운영 DB에 명령하지 않는다)와 OPS-06의 「복원 리허설」 항목(CONTEXT 「분리로 생긴 조정」).
Output: `restore_rehearsals` 표와 마이그레이션 `NNNN_restore_rehearsals`(db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1), 리포지토리·도메인·CLI `record`, 상태 화면 한 줄, 테스트.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다 — 모든 task가 실패 테스트 먼저다. **각 task는 자기가 구현하는 behavior의 테스트를 먼저 쓰고 빨간 것을 본 뒤 구현한다** — Task 1은 트레이서 경로에 필요한 것만 구현하고, Task 2의 behavior(입력·읽기 검증 · 시간 제한 · 오류 흡수 · 실패 문구)는 Task 2에서 그 테스트가 빨간 것을 확인한 뒤 구현한다(Codex #20). 테스트·마이그레이션이 실패하면 원인을 쫓기 전에 `systematic-debugging`을 호출한다. 커밋하거나 「완료」라고 말하기 전에 `verification-before-completion`을 호출하고 verify 명령의 실제 출력으로 판정한다. 서브에이전트에 위임하면 그 프롬프트에도 세 스킬 이름과 `model`을 명시한다(CLAUDE.md [Build]).
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도). diff 기준 검사는 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다.
- **실행 순서(A4)**: 이 페이즈의 여섯 플랜은 한 줄 체인이다(01 → 02 → 03 → 04 → 05 → 06, 웨이브마다 플랜 하나). 실행 세션마다 자기 컨테이너와 자기 로컬 Postgres를 쓰므로 다른 페이즈와 `erp`·`erp_test`를 나누지 않고, 페이즈 안에서는 이 체인이 직렬화한다(Codex #13 반려 근거 — 아래 Review Dispositions Ledger).
- **마이그레이션 번호(조정자 규칙)**: 이 플랜의 마이그레이션 번호 NNNN은 db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1이다. `pnpm db:generate --name restore_rehearsals`가 만든 SQL·스냅숏·journal 항목을 **이름·idx 그대로** 둔다 — 손으로 바꾸지 않는다. 머지 직전(04.4-06 Task 1)에 이 페이즈의 마이그레이션을 전부 지우고 main 위에서 하나로 다시 만든다. `drizzle-kit push` 금지. 로컬 개발 DB에만 `pnpm db:migrate`, 스테이징·프로덕션 적용 없음(배포는 `/ship` 뒤 `deploy.yml`).
- **덧붙이기 전용(A2)**: `db/schema/index.ts`는 끝에 한 줄만 더한다. 공유 등록부(`domain/action-log/record.ts` · `domain/settings/keys.ts` · `test/integration/leak-scan.test.ts`)는 이 플랜이 건드리지 않는다. 상태 화면·`domain/system-status`에는 항목을 **더하기만** 하고 기존 세 항목과 가드(`getSession`/`redirect`/`notFound`)는 한 글자도 바꾸지 않는다 — 04.2가 같은 곳에 「마지막 tick」「이메일 미설정」을 더한다.
- **금지(A3)**: 견적 줄·프로젝트·`document_counters`·demo 행 파일을 건드리지 않는다. `.planning/STATE.md`를 고치지 않는다.
- 새 의존성 없음. `git push` 하지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 엣지 프로브가 비었다(EDGE_ABSENT=1). 이 플랜이 스스로 적은 엣지 truth: **empty 대 확인 불가 분리**(truth 6) · **잠금 대기·유효하지 않은 행**(truth 7) · **최신 1건 선택과 동시각 순서**(truth 9) · **원본 환경 위조 불가**(truth 3) · **DB 수준 불변식 전 조합**(truth 2) · **쓰기 거부 행렬**(truth 4). OPS-03 프로브 행은 분류되지 않은 채(unresolved) 남았고, 이 플랜은 그중 「결과 기록·표시」 절반을 닫는다 — 나머지(모양·E2E·문서는 04.4-05, 복원·검증·정리·운영 문서는 04.4-02 · 04.4-03, 실제 1회는 04.4-06)가 닫는다.

**계획 가정(뒤집히면 해당 함수만 바뀐다)**
1. 결과 저장소는 GCS 파일이 아니라 운영 DB 한 줄이다(RESEARCH A5, CONTEXT Discretion) — 새 의존성(`@google-cloud/storage`)·버킷이 필요 없고, 화면이 같은 DB에서 읽는다
2. 실행 식별자는 run id가 아니라 검증된 전체 URL(`run_url`)로 저장한다 — 화면이 저장소 이름을 몰라도 된다
3. 실패 단계는 영어 코드(restore·verify·cleanup)로 저장하고 화면에서 복원·검증·정리로 바꾼다
4. ROADMAP 기준 2의 「리허설 없음」은 UI-SPEC(승인됨)의 정확한 문구 `리허설 기록 없음 — 첫 리허설 전`으로 구현한다
5. 백업 id는 숫자만 허용한다(GCP 자동 백업 id 형태) — 아니면 도메인이 거부한다
6. 마이그레이션 번호: db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1(조정자 규칙). 이 브랜치에서는 번호를 고르지도 바꾸지도 않는다
7. 조회 시간 제한 값은 `statement_timeout` 2초 · `lock_timeout` 1초다(트랜잭션 한정 `SET LOCAL`). 커넥션 풀 획득 대기는 묶지 않는다 — 기존 「DB 커넥션」 항목과 같은 풀을 쓰므로 풀이 고갈되면 이미 화면 전체가 멈춘 상태이고, 이 페이즈가 만드는 새 위험이 아니다(Codex #15 범위)
8. CLI를 DB 없는 진입점으로 두고 DB 모듈을 동적으로 불러와도 `tsx`(통합 테스트의 자식 프로세스)와 esbuild 번들(04.4-02 `build:cli`)이 둘 다 동작한다고 본다 — 번들 쪽 확인은 04.4-02 Task 2가 한다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — CLI `record` 한 번이 `restore_rehearsals` 한 줄이 되고 상태 화면 「복원 리허설」에 보인다 (DB · 리포지토리 · 도메인 · 상태 합성 · 화면 · CLI)</name>
  <files>db/schema/ops.ts, db/schema/index.ts, db/migrations/NNNN_restore_rehearsals.sql, db/migrations/meta/NNNN_snapshot.json, db/migrations/meta/_journal.json, repositories/restore-rehearsals.ts, domain/ops/restore-rehearsal.ts, domain/system-status/index.ts, app/(app)/admin/system-status/page.tsx, app/(app)/admin/system-status/restore-rehearsal-view.ts, scripts/restore-rehearsal-cli.ts, test/integration/restore-rehearsal.test.ts, test/unit/db/migration-journal.test.ts, test/unit/ops/restore-rehearsal-view.test.ts, test/unit/system-status.test.ts</files>
  <precondition>로컬 Postgres가 떠 있다(`bash scripts/dev-db.sh`가 성공한다) — 통합 테스트와 `pnpm db:migrate`의 전제</precondition>
  <reversibility rating="costly">새 표의 칼럼 모양(원본 enum · 실패 단계 코드 · run_url)은 운영 DB에 배포되면 바꾸는 데 마이그레이션이 하나 더 든다. 아직 어디에도 배포되지 않았고 병합 직전 재생성이 있으므로 되돌릴 수 없는 결정은 아니다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 101–144행(Copywriting · 복원 리허설 값 행 · 필수 데이터 항목) — 정확한 문구와 칼럼 목록
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md` — 파일별 가장 가까운 선례
    - `db/schema/action-log.ts` — `bigint(..., {mode:"number"}).primaryKey().generatedByDefaultAsIdentity()` seq 선언(squawk prefer-identity)
    - `db/schema/corp-cards.ts` — `check("이름", sql\`...\`)` 선례(도메인 순수 함수 + DB check)
    - `db/schema/index.ts` — 배럴 끝 줄
    - `repositories/system-status.ts` · `repositories/users.ts` — viewer 첫 인자 규약(`plant8/repository-viewer-param`)과 `void viewer`
    - `domain/system-status/index.ts`(전문 85행) — `SystemStatus` · `StatusDeps` · db 필드의 try/catch + `log.warn("status.db_unavailable")` 모양
    - `app/(app)/admin/system-status/page.tsx`(전문 77행) — `KvList` items 배열과 「마지막 백업」 삼항식, 가드(건드리지 않음)
    - `scripts/account-cli.ts`(전문) — `UsageError` · 엄격한 `parseArgs`(`--플래그 값` 쌍만) · `main()`의 `closeDb` finally · 종료 코드 2/1 · `import.meta.url` 가드
    - `test/integration/account-cli.test.ts` 1–60행 — `spawnSync("node", ["--import","tsx", ...])`로 CLI를 자식 프로세스로 돌리는 모양과 env(비로컬 `APP_ENV`에 필요한 `BETTER_AUTH_SECRET` 32자 이상·`BETTER_AUTH_URL`)
    - `test/integration/setup.ts` — 배럴의 모든 표를 매 테스트 전 TRUNCATE(새 표도 자동 포함)
    - `lib/env.ts` 44–52행 — `APP_ENV` enum(local·staging·prod)
    - `db/client.ts` 38–43행 — `pool`을 top-level await로 만든다(`CLOUD_SQL_CONNECTION_NAME`이 있으면 커넥터 초기화) — CLI가 DB 모듈을 정적으로 부르면 안 되는 이유
    - `eslint/rules/no-row-type-escape.mjs` 1–30행 — domain export 함수가 `…Row` 타입을 반환하면 안 된다
    - `test/unit/system-status.test.ts`(전문) — deps 주입 모양, 기존 테스트가 새 dep 없이 부르는 곳
  </read_first>
  <behavior>
    - 통합 1: `restore_rehearsals`가 비어 있으면 `getSystemStatus(관리자).restoreRehearsal`이 `{kind:"none"}`이다
    - 통합 2(트레이서): `APP_ENV=staging`으로 CLI `record --succeeded true --backup-id 1758684000000 --started-at 2026-09-23T18:02:00Z --finished-at 2026-09-23T18:14:00Z --run-url https://github.com/o/r/actions/runs/1`을 자식 프로세스로 돌리면 종료 코드 0이고, 곧이어 `getSystemStatus`가 `{kind:"recorded"}` + source `staging` · succeeded true · failedStage null · 같은 backupId·시각을 돌려준다
    - 통합 3: 종료 시각이 다른 두 기록 중 늦은 쪽, 종료 시각이 같으면 나중에 넣은(seq가 큰) 쪽이 선택된다
    - 통합 4(DB 불변식 행렬 — 도메인을 거치지 않은 직접 SQL): (성공, NULL) 허용 · (성공, `verify`) 거부 · (실패, NULL) 거부 · (실패, `verify`) 허용 · (실패, `bogus`) 거부 · 원본 `local` 거부(Codex #17)
    - 통합 5: `APP_ENV=local`로 `record`를 돌리면 종료 코드 2이고 행이 생기지 않는다
    - 통합 6(인자 규약 — CLI 모듈이 DB를 부르지 않으므로 `parseArgs`를 테스트 프로세스 안에서 직접 부른다): 모르는 플래그 · 값 없는 플래그 · `--succeeded maybe` · 필수(`--succeeded` · `--started-at` · `--finished-at`) 누락 · 하위 명령 없음 → `UsageError`
    - 단위(보기, 트레이서 모양): 성공 + 백업 id + 12분 → head `성공 · 스테이징 · 2026-09-24 03:14` · backupId `1758684000000` · duration `12분` · runUrl null; 종료 UTC `2026-09-23T15:30:00Z` → 일시 `2026-09-24 00:30`(KST 날짜 경계)
    - 단위(상태 합성): 기존 테스트는 새 dep `getLatestRestoreRehearsal: () => Promise.resolve(null)`만 주입해 그대로 녹색이고 `restoreRehearsal`이 `{kind:"none"}`이다; 권한 없는 viewer는 NotAdminError이고 새 dep을 부르지 않는다
    - journal 가드(있을 때): 가져온 파일이 지금 트리에서 녹색이다
  </behavior>
  <action>
① **journal 가드(조정자 규칙 — 이 페이즈는 따로 쓰지 않는다)**: `git fetch origin` 뒤 (a) `git show origin/main:test/unit/db/migration-journal.test.ts`가 있으면 그 파일을 **고치지 않고** 같은 경로에 둔다 (b) 없으면 `for b in $(git branch -r --list 'origin/claude/phase-04.1*'); do git log -1 --format='%ct %H' "$b" -- test/unit/db/migration-journal.test.ts; done | sort -rn | head -1`로 04.1 브랜치(`origin/claude/phase-04.1-plan-1iqtwe` 또는 그 실행 브랜치) 중 이 파일을 담은 **가장 최근 커밋**을 찾아 `git show <그 커밋>:test/unit/db/migration-journal.test.ts`를 글자 그대로 복사한다 (c) 어디에도 없으면 만들지 않는다 — 단언을 직접 골라 쓴 별도 버전을 만들지 않는다. SUMMARY에 출처(main · 04.1 커밋 해시 · 없음)를 적는다. 가져온 파일이 이 브랜치에서 빨갛면(예: 04.1 스키마를 전제) 고치지 말고 `systematic-debugging` 뒤 SUMMARY에 적어 조정자에게 넘긴다. prevId 사슬 검사는 이 가드에 더하지 않는다(Codex #7 — 페이즈 간 제안으로 SUMMARY에만 적는다).

② **실패 테스트 먼저(Codex #20)**: behavior의 통합 1~6을 `test/integration/restore-rehearsal.test.ts`에, 보기 두 경우를 `test/unit/ops/restore-rehearsal-view.test.ts`에, 상태 합성 두 경우를 `test/unit/system-status.test.ts`에 쓴다(기존 테스트의 단언은 바꾸지 않고 새 dep 주입만 더한다 — 객체 전체 비교가 있으면 기대값에 `restoreRehearsal: {kind:"none"}`을 더한다). CLI는 `test/integration/account-cli.test.ts`의 `spawnSync` 모양을 따라 `scripts/restore-rehearsal-cli.ts`를 돌리고, env에 `DATABASE_URL`(erp_test) · `APP_ENV` · 32자 이상 `BETTER_AUTH_SECRET` · `BETTER_AUTH_URL`을 준다. 표 비우기는 `test/integration/setup.ts`가 한다. 돌려서 빨간 것(표·모듈 없음)을 확인한다.

③ **스키마(D8-08)**: 새 파일 `db/schema/ops.ts`에 `restore_rehearsals`를 정의한다 — `seq`(bigint identity PK, action-log 선례) · `source` text NOT NULL · `succeeded` boolean NOT NULL · `failed_stage` text NULL · `backup_id` text NULL · `started_at`·`finished_at` `timestamp(..., { withTimezone: true })` NOT NULL(UI-SPEC #11 — 이 표만 timestamptz) · `run_url` text NULL. check 제약 둘: `restore_rehearsals_source_check` = `source in ('staging','production')`, `restore_rehearsals_stage_check` = `(succeeded and failed_stage is null) or (not succeeded and failed_stage is not null and failed_stage in ('restore','verify','cleanup'))` — `NULL IN (…)`은 NULL이고 check는 NULL을 통과시키므로 `is not null`을 반드시 적는다(Codex #17). 파일 머리 주석에 D8-08과 「각 환경이 자기 DB에만 쓴다」를 한 줄로 적는다. `db/schema/index.ts` 끝에 export 한 줄.

④ **[BLOCKING] 마이그레이션(조정자 규칙)**: (a) `pnpm db:generate --name restore_rehearsals` — drizzle-kit이 `NNNN_restore_rehearsals.sql` · `meta/NNNN_snapshot.json` · journal 마지막 항목을 만든다. NNNN은 db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1 — **이름·idx를 손으로 바꾸지 않는다**. 실제 번호를 SUMMARY에 적는다 (b) `pnpm db:generate` 재실행이 "No schema changes" (c) `pnpm lint:sql` (d) `pnpm db:migrate`(로컬 개발 DB `erp`만) (e) journal 가드가 있으면 녹색. 통합 테스트는 `test/integration/global-setup.ts`가 `erp_test`에 적용한다. `drizzle-kit push`·스테이징·프로덕션 적용 금지.

⑤ **리포지토리**: `repositories/restore-rehearsals.ts` — `insertRestoreRehearsal(viewer, values)`와 `findLatestRestoreRehearsal(viewer)`(`finished_at` 내림차순 → `seq` 내림차순, limit 1, 없으면 null). 두 함수 모두 viewer 첫 인자(`void viewer`, `repositories/system-status.ts` 모양). 조회 시간 제한은 Task 2.

⑥ **도메인**: `domain/ops/restore-rehearsal.ts` — 도메인 타입 `RestoreRehearsalRecord`(source `"staging"|"production"` · succeeded · failedStage `"restore"|"verify"|"cleanup"|null` · backupId · startedAt · finishedAt · runUrl)를 정의하고 행을 이 타입으로 옮겨 반환한다(no-row-type-escape). `recordRestoreRehearsal(viewer, input)`은 이 task에서는 삽입까지만 하고(입력 검증은 Task 2가 빨간 테스트를 본 뒤 더한다), `getLatestRestoreRehearsal(viewer)`는 최신 1건 또는 null(읽기 검증도 Task 2). 권한 검사는 두지 않는다 — 호출자는 CLI(SYSTEM_VIEWER)와 이미 `can()`을 거친 `getSystemStatus`뿐이다.

⑦ **상태 합성**: `domain/system-status/index.ts`의 `SystemStatus`에 `restoreRehearsal`(`{kind:"none"}` | `{kind:"recorded"; record}` | `{kind:"unavailable"}` — 타입은 셋을 다 선언)을, `StatusDeps`에 `getLatestRestoreRehearsal`을 **더한다**. `can()` 판정 뒤에만 부른다. 이 task는 none/recorded만 만든다(오류 흡수 try/catch는 Task 2). 기존 필드·판정 순서는 바꾸지 않는다.

⑧ **화면(트레이서 모양)**: `app/(app)/admin/system-status/restore-rehearsal-view.ts`의 순수 함수 `formatRestoreRehearsal(record)` → `{ head, backupId, duration, runUrl }`. 이 task는 성공 기록 모양만: head `성공 · <원본> · <일시>`(원본 스테이징·프로덕션, 일시는 종료 시각을 `Intl.DateTimeFormat("en-CA", {timeZone:"Asia/Seoul", hour12:false, ...}).formatToParts`로 연·월·일·시·분을 뽑아 `YYYY-MM-DD HH:MM`으로 직접 조립 — 로캘 문자열 그대로 쓰지 않는다), duration `N분`(초 버림), runUrl null. 실패 head · 단계 낱말 · `1분 미만` · 백업 id 생략 · 실행 링크는 Task 2. `page.tsx`의 `KvList` items에서 「마지막 백업」 항목 **바로 다음**에 `label: "복원 리허설"` 항목 하나: none → `리허설 기록 없음 — 첫 리허설 전`, recorded → head + ` · 백업 ` + id를 감싼 span + ` · ` + duration. 스타일 클래스는 04.4-05가 붙인다.

⑨ **CLI(DB 없는 진입점, Codex #11)**: `scripts/restore-rehearsal-cli.ts`는 `node:` 내장 모듈만 정적으로 import한다 — `@/db/*` · `@/repositories/*` · `@/domain/*`를 정적으로 부르지 않는다(`db/client.ts`가 top-level await로 커넥터를 만들기 때문). `account-cli.ts`의 `UsageError` · 엄격한 `parseArgs(argv)`(`--플래그 값` 쌍만) · 종료 코드(사용 오류 2 · 그 밖 1) · `import.meta.url` 가드 규약을 따르되, `main(argv = process.argv.slice(2))`는 (a) `parseArgs` (b) `process.env.APP_ENV` → 원본(staging → staging · prod → production · 그 밖 `UsageError`「로컬에서는 리허설 결과를 기록하지 않습니다」) 뒤에만 (c) `await import`로 `@/domain/viewer` · `@/domain/ops/restore-rehearsal` · `@/db/client`를 불러 `recordRestoreRehearsal(SYSTEM_VIEWER, …)`을 부르고, finally에서 db 모듈을 불러왔을 때만 `closeDb()`한다. 하위 명령 `record` 플래그: `--succeeded true|false`(필수) · `--failed-stage`(선택) · `--backup-id`(선택) · `--started-at`·`--finished-at`(ISO, 필수) · `--run-url`(선택). 원본 환경 플래그는 **두지 않는다**. 성공하면 한 줄(「복원 리허설 결과를 기록했습니다 …」)을 찍는다. 출력 문구는 「…니다」로 끝낸다.

⑩ ②의 테스트가 녹색이 될 때까지 구현한다. 커밋 제목 예 `feat: record restore rehearsal results and show them on system status`, 본문 한국어(D8-08).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ops/restore-rehearsal-view.test.ts test/unit/system-status.test.ts &amp;&amp; { test ! -f test/unit/db/migration-journal.test.ts || pnpm vitest run --project unit test/unit/db/migration-journal.test.ts; }</automated>
    <fails_when>트레이서 모양 문구(KST 경계 포함) · 상태 합성(none · 권한 판정 · 기존 테스트) 중 하나가 빨갛거나, journal 가드 파일이 있는데 빨갛다</fails_when>
    <automated>pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/restore-rehearsal.test.ts test/integration/system-status.test.ts</automated>
    <fails_when>squawk가 새 SQL을 거부하거나, 로컬 적용이 실패하거나, 트레이서 · 기록 없음 · 최신 선택 · DB 불변식 행렬 · local 거부 · 인자 규약 · 기존 상태 통합 테스트 중 하나가 빨갛다</fails_when>
    <automated>pnpm db:generate 2&gt;&amp;1 | grep -q "No schema changes" &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>스키마와 생성 마이그레이션이 어긋나 "No schema changes"가 없거나, 타입·린트(repository-viewer-param · no-row-type-escape · boundaries 포함)가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=A "$PLAN_BASE"..HEAD -- db/migrations`가 정확히 두 줄 — `db/migrations/NNNN_restore_rehearsals.sql` 하나와 같은 NNNN의 `db/migrations/meta/NNNN_snapshot.json` 하나 — 이고, `_journal.json` 마지막 항목의 `tag`가 그 `.sql` 파일 이름(확장자 제외)과 같다(drizzle-kit이 준 이름 그대로)
    - `D=$(git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json) && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 1을 출력한다(삭제 줄은 직전 마지막 항목의 닫는 `}`가 `},`로 바뀌는 한 줄뿐 — 앞 항목은 바뀌지 않고 마지막 항목만 더해졌다)
    - 그 SQL에 `restore_rehearsals_source_check`와 `restore_rehearsals_stage_check`가 있고, `timestamp with time zone`이 두 번 이상, `is not null`(대소문자 무시)이 stage check 안에 나온다
    - `grep -n "복원 리허설" "app/(app)/admin/system-status/page.tsx"`의 줄 번호가 `grep -n "마지막 백업"`의 줄 번호보다 크고, 두 항목 사이에 다른 `label:`이 없다
    - `D=$(git diff "$PLAN_BASE"..HEAD -- "app/(app)/admin/system-status/page.tsx") && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 0을 출력한다(삭제 줄 없음 — 기존 항목·가드를 더하기만 했다)
    - `grep -cE "^import .*[\"']@/(db|repositories|domain)/" scripts/restore-rehearsal-cli.ts`가 0이고(정적 DB 경로 없음), `grep -c "\"--source\"" scripts/restore-rehearsal-cli.ts`가 0이다
    - journal 가드 파일이 있으면 출처 파일과 글자 그대로 같다(`git show <출처>:test/unit/db/migration-journal.test.ts | diff - test/unit/db/migration-journal.test.ts`가 아무것도 출력하지 않는다), SUMMARY에 출처가 적혀 있다
    - 세 verify 명령이 전부 초록이다
  </acceptance_criteria>
  <done>CLI `record` 한 번으로 생긴 한 줄이 로컬에서 상태 합성 결과에 `recorded`로 나오고, 표가 비면 `none`이며, 화면 목록의 「마지막 백업」 바로 다음에 「복원 리허설」 항목이 렌더된다. DB가 성공×단계 전 조합을 지킨다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 검증 · 시간 제한 · 확인 불가 · 실패 문구 — 빨간 테스트를 먼저 보고 구현한다(쓰기 거부 · 읽기 검증 · 잠금 대기 · 오류 흡수 · 단계 · 1분 미만 · 백업 id 생략 · 실행 링크)</name>
  <files>domain/ops/restore-rehearsal.ts, repositories/restore-rehearsals.ts, domain/system-status/index.ts, app/(app)/admin/system-status/restore-rehearsal-view.ts, app/(app)/admin/system-status/page.tsx, test/integration/restore-rehearsal.test.ts, test/unit/ops/restore-rehearsal-view.test.ts, test/unit/system-status.test.ts</files>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 101–131행(문구 표 · 결과 정의 · 일시·소요 규칙 · Error state)
    - `domain/people/index.ts` 27–30행 — `ValidationError`(`UserFacingError`) 관례
    - `domain/system-status/index.ts`의 db 필드 try/catch + `log.warn("status.db_unavailable")` 모양
    - `node_modules/drizzle-orm/pg-core/columns/timestamp.js` 25–35행 — 지원 밖 값(`infinity` 등)이 예외 없이 Invalid Date가 되는 자리(읽기 검증이 필요한 이유, Codex #16)
    - `test/integration/system-status.test.ts` — 통합에서 `getSystemStatus`를 부르는 모양(GCP 조회 대역)
    - `test/integration/db-bootstrap.test.ts` 1–20행 — 테스트 안에서 `pg`의 `Pool`을 따로 여는 선례(잠금 테스트의 별도 연결)
  </read_first>
  <behavior>
    - 쓰기 거부(통합, `recordRestoreRehearsal`을 프로세스 안에서 직접): 실행 URL `javascript:alert(1)` · `http://github.com/o/r/actions/runs/1` · `https://github.com.evil.example/o/r/actions/runs/1` · `https://github.com/o/r/actions/runs/abc` · `https://github.com/o/r/actions/runs/1/x`; 시각 `new Date("not-a-date")` · 종료 < 시작; 백업 id `12a` · 빈 문자열 · `-1`; 성공 + 단계; 실패 + 단계 없음; 모르는 단계 → 전부 `ValidationError`이고 행 0개(Codex #17)
    - CLI 거부(통합, 자식 프로세스 둘): `--run-url javascript:alert(1)`, 그리고 `--finished-at`이 `--started-at`보다 이른 경우 → 종료 코드 ≠ 0이고 행 0개
    - 읽기 검증(통합): 직접 SQL로 종료 < 시작인 행, 또는 `started_at = 'infinity'`인 행을 넣으면 `getSystemStatus(관리자)`의 `restoreRehearsal`만 `{kind:"unavailable"}`이고 `db` 필드는 정상이다(Codex #16)
    - 잠금 대기(통합): 별도 `pg` 연결이 트랜잭션 안에서 `LOCK TABLE restore_rehearsals IN ACCESS EXCLUSIVE MODE`를 잡은 동안 `getSystemStatus(관리자)`가 5초 안에 끝나고 `restoreRehearsal`은 unavailable, `db`는 정상이다; 잠금을 풀면 다음 호출은 `{kind:"none"}`이다(Codex #15)
    - 상태(단위): `getLatestRestoreRehearsal`이 throw하면 `restoreRehearsal`은 unavailable이고 version·db·backup은 정상이다
    - 보기(단위): 실패 verify + 백업 id + 4분 + URL → head `실패 · 검증 · 스테이징 · 2026-09-24 03:14`, runUrl 그대로; 실패 restore + 백업 id null → backupId null(세그먼트 생략), runUrl 있음; 실패 cleanup → `정리`; 59초 → `1분 미만`, 60초 → `1분`, 119초 → `1분`; 원본 production → `프로덕션`; 성공인데 URL이 있으면 runUrl null
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: 위 behavior를 세 테스트 파일에 **더하고** 돌려서 빨간 것을 확인한다(Task 1은 이 동작들을 구현하지 않았다). 잠금 테스트의 별도 연결은 `pg`의 `Pool`을 `process.env.DATABASE_URL`로 직접 열고(`db-bootstrap.test.ts` 선례, 새 의존성 없음) 테스트 끝에 `ROLLBACK` · `end()`한다.

② **쓰기 검증(Codex #17)**: `recordRestoreRehearsal`이 삽입 전에 확인한다 — 원본 두 값 · 성공 ⇔ 단계 없음 · 단계 세 값 · 두 시각이 유한(`Number.isFinite(date.getTime())`) · 시작 ≤ 종료 · 백업 id는 `^\d+$` 또는 null · 실행 URL은 `^https://github\.com/[\w.-]+/[\w.-]+/actions/runs/\d+$` 또는 null. 어기면 `ValidationError`(`domain/people` 모양). 검증 함수 하나를 ③과 같이 쓴다.

③ **읽기 검증(Codex #16)**: `getLatestRestoreRehearsal`이 행을 도메인 타입으로 옮기면서 ②와 같은 규칙을 다시 확인하고, 어긋나면 도메인 오류를 던진다 — 상태 합성의 catch(⑤)가 그 행만 `확인 불가`로 바꾼다. 형식화(`formatRestoreRehearsal`)는 검증을 통과한 기록만 받는다.

④ **조회 시간 제한(Codex #15)**: `findLatestRestoreRehearsal`이 `db.transaction` 안에서 SELECT 전에 `SET LOCAL statement_timeout = '2000ms'`와 `SET LOCAL lock_timeout = '1000ms'`(값은 매개변수가 아닌 리터럴)를 실행한다 — 시간 초과는 오류로 던져진다. 풀 획득 대기는 묶지 않는다(probe_fallback 가정 7).

⑤ **오류 흡수**: `domain/system-status/index.ts`에서 `getLatestRestoreRehearsal` 호출을 db 필드와 같은 try/catch로 감싸 `log.warn("status.restore_rehearsal_unavailable", { message })` 후 `{kind:"unavailable"}`. 기존 필드·판정 순서는 그대로.

⑥ **실패 문구**: `formatRestoreRehearsal` — 실패 head `실패 · <단계> · <원본> · <일시>`(복원·검증·정리), duration은 60초 미만이면 `1분 미만`, runUrl은 실패이고 값이 있을 때만. `page.tsx`: unavailable → 기존 「마지막 백업」과 같은 `<StatusTag kind="muted">확인 불가</StatusTag>`, 백업 id가 null이면 ` · 백업 …` 세그먼트를 통째로 빼고, runUrl이 있으면 끝에 ` · ` + 같은 탭 `<a href={runUrl}>실행 기록</a>`(target 없음). CSS 클래스는 04.4-05가 붙인다.

⑦ 녹색이 될 때까지 구현. 커밋 둘(검증·시간 제한 · 실패 문구), 제목 예 `feat: validate and bound restore rehearsal reads` · `feat: format failed restore rehearsal rows per UI-SPEC`, 본문 한국어(D8-08 · UI-SPEC 문구 표).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ops/restore-rehearsal-view.test.ts test/unit/system-status.test.ts</automated>
    <fails_when>실패 문구 규칙(단계 · 1분 미만 · 생략 · 링크 조건) 또는 dep 오류의 `확인 불가` 흡수 중 하나가 빨갛다</fails_when>
    <automated>pnpm vitest run --project integration test/integration/restore-rehearsal.test.ts test/integration/system-status.test.ts</automated>
    <fails_when>쓰기 거부 행렬 · CLI 거부 · 읽기 검증 · 잠금 대기 5초 제한 · 기존 통합 테스트 중 하나가 빨갛다</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>타입·린트(repository-viewer-param · no-row-type-escape · boundaries 포함) 오류가 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "statement_timeout" repositories/restore-rehearsals.ts`와 `grep -c "lock_timeout" repositories/restore-rehearsals.ts`가 각각 1 이상이다
    - `page.tsx`에 `target=`이 없다(같은 탭)
    - 잠금 대기 테스트가 경과 시간 5초 미만을 단언하고 초록이다
    - 세 verify 명령이 초록이다
  </acceptance_criteria>
  <done>잘못된 입력은 쓰기 전에, 잘못된 행·잠긴 표는 읽을 때 그 행만 `확인 불가`로 막히고, UI-SPEC 문구 표의 여섯 상태가 테스트로 고정된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run Job 인자 → CLI `record` | 워크플로가 넘긴 문자열(성공 여부·단계·백업 id·시각·URL)이 DB에 들어간다 |
| DB 행 → 관리자 화면 | 저장된 `run_url`이 `href`로 렌더된다 |
| 관리자 화면 접근 | `can(viewer, "admin.system-status", "view")` 기존 가드 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Tampering | `run_url` → `href` | medium | mitigate | 도메인이 쓸 때와 읽을 때 모두 `^https://github\.com/<owner>/<repo>/actions/runs/<숫자>$`만 받고 React가 속성을 이스케이프한다. `javascript:` · `http:` · 호스트 위장 · 숫자 아닌 run id · 꼬리 경로를 쓰기 거부 행렬이 단언한다(Task 2) |
| T-04.4-02 | Spoofing | 기록의 원본 환경 | high | mitigate | CLI에 원본 플래그가 없고 `APP_ENV`로만 정한다(local 거부). DB check 제약이 두 값만 허용한다(직접 SQL 행렬). 각 환경의 Job은 자기 DB에만 연결된다(deploy.sh 고정) — Task 1 통합 4·5 · acceptance(`--source` 없음) |
| T-04.4-03 | Information Disclosure | 상태 화면 | low | accept | 기존 `can()` + 404 가드를 그대로 쓰고 새 경로를 만들지 않는다. 표시 값(백업 id·시각)은 비밀이 아니다 |
| T-04.4-04 | Denial of Service | 상태 화면 조회 | medium | mitigate | 새 조회는 `statement_timeout` 2초 · `lock_timeout` 1초로 묶이고, 조회 오류·시간 초과·유효하지 않은 행은 try/catch로 그 행만 `확인 불가`가 된다 — 잠금 테스트가 5초 안에 끝남을 단언한다(Task 2, Codex #15·#16). 풀 획득 대기는 기존 「DB 커넥션」과 같은 풀이라 새 위험이 아니다(accept) |
| T-04.4-05 | Tampering | `_journal.json` 순서 | high | mitigate | 브랜치 번호는 db:generate가 준 그대로(손 편집 없음), journal 가드는 main 또는 04.1 브랜치 파일을 글자 그대로 쓴다. 머지 직전 04.4-06 Task 1이 main 위에서 하나로 재생성하고 main 마지막+1 · 04.4 변경만 · "No schema changes" · 가드 녹색을 확인한다 |
| T-04.4-28 | Elevation of Privilege | CLI 진입점의 커넥터 초기화 | medium | mitigate | CLI가 인자·`APP_ENV` 검사를 통과하기 전에는 DB 모듈을 불러오지 않는다(정적 import 0 — acceptance grep). 커넥터 미초기화 단위 테스트는 04.4-02(Codex #11) |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개(RESEARCH Package Legitimacy Audit: 대상 없음). `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` 통과
- `pnpm test:unit` 전체 통과(journal 가드가 있으면 포함 · import-cycles 포함)
- `pnpm test:integration` 전체 통과(기존 `test/integration/system-status.test.ts` 회귀 없음)
- `pnpm db:generate` 재실행 결과 "No schema changes"
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml .planning/STATE.md domain/action-log domain/settings/keys.ts test/integration/leak-scan.test.ts` 비어 있음
</verification>

<success_criteria>
- CLI `record` → DB → 상태 화면 한 줄이 로컬에서 관통한다(트레이서)
- 기록 없음 · 성공 · 실패(단계 + 링크) · 확인 불가 네 표시가 UI-SPEC 문구 그대로다(모양·브라우저는 04.4-05)
- 원본 환경은 컨테이너 환경에서만 정해지고 DB가 성공×단계 전 조합을 한 번 더 지킨다
- 잘못된 입력은 쓰기 전에, 잘못된 행·잠긴 표는 읽을 때 그 행만 `확인 불가`로 막힌다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `restore_rehearsals`(check 제약 `restore_rehearsals_source_check` · `restore_rehearsals_stage_check`) |
| 마이그레이션 | `db/migrations/NNNN_restore_rehearsals.sql` + `meta/NNNN_snapshot.json` + journal 항목 — NNNN은 db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1(04.4-06 Task 1) |
| 함수 | `insertRestoreRehearsal` · `findLatestRestoreRehearsal` · `recordRestoreRehearsal` · `getLatestRestoreRehearsal` · `formatRestoreRehearsal` |
| 타입 | `RestoreRehearsalRecord` · `SystemStatus.restoreRehearsal` · `StatusDeps.getLatestRestoreRehearsal` |
| CLI | `scripts/restore-rehearsal-cli.ts` `record`(DB 없는 진입점 · Job 배포·번들은 04.4-02) |
| 화면 | `/admin/system-status` 「복원 리허설」 행(모양·E2E는 04.4-05) |
| 로그 이벤트 | `status.restore_rehearsal_unavailable` |
| 테스트 | `test/integration/restore-rehearsal.test.ts` · `test/unit/ops/restore-rehearsal-view.test.ts` · `test/unit/system-status.test.ts`(확장) · `test/unit/db/migration-journal.test.ts`(main 또는 04.1 파일 그대로, 있을 때) |

## Source Audit (페이즈 전체 — 여섯 플랜)

| SOURCE | ID | 항목 | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | 워크플로 하나로 최신 자동 백업 → 임시 인스턴스 복원·확인·삭제, 결과가 상태 화면에 | 01 · 02 · 03 · 05 · 06 | COVERED | 실제 1회는 06 Task 3(사람 트리거) |
| GOAL | — | 복원 절차(워크플로 실행법 + 사고 때 수동 복원)가 OPERATIONS.md에 | 03 | COVERED | §14(20줄 이하) + 링크한 `docs/RESTORE.md`(사고 복원 런북, Codex #9) |
| GOAL | — | 사람 목록에서 첫 로그인 전·임시 비밀번호 사용 중을 본다 | 04 · 05 | COVERED | 폰 쌓기·문서는 05 |
| REQ | OPS-03 | 자동 백업 · 복원 절차 문서 · 복원 리허설 1회 | 01~06 | COVERED | 자동 백업 자체는 Phase 1(deploy.sh), 이 페이즈는 확인법 문서 |
| REQ | 기준 1 | 이름 거부 · 사설망 확인 · 확인 실패에도 삭제 · 비용 수십 분 | 02 · 03 | COVERED | |
| REQ | 기준 2 | 실제 1회 · 화면 표시(원본 포함) · 기록 없음/확인 불가 · 사람이 운영 DB에 명령 안 함 | 01 · 05 · 06 | COVERED | 「리허설 없음」은 UI-SPEC 문구 `리허설 기록 없음 — 첫 리허설 전` |
| REQ | 기준 3 | OPERATIONS 복원 절 · PITR 여부 · 백업 설정 확인 | 03 | COVERED | PITR 끔(가정 A4), 복원 뒤 백업 설정 재확인 포함 |
| REQ | 기준 4 | 두 배지 · 새 칼럼 · sessions 미사용 · 강제 없음 · 사람별 쿼리 없음 · 통합 흐름 | 04 · 05 | COVERED | 사람별 쿼리 없음 = 정보 항목 판정 요청당 1회(Codex #8) |
| REQ | 기준 5 | 누수 스캔 등록 · 마이그레이션 번호 · 병합 직전 재생성 | 01 · 04 · 06 | COVERED | 번호는 db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1(조정자 규칙) |
| RESEARCH | OQ1 | 복원 대상 선행 생성 | 03 · 06 | COVERED | RESOLVED — API 정의(v1.d.ts)로 답함, 06 Task 3에서 실측 |
| RESEARCH | OQ2 | env 덮어쓰기로 임시 인스턴스 연결 | 02 · 06 | COVERED | RESOLVED — 거짓 성공 불가 설계 + 06 Task 3 실측 |
| RESEARCH | OQ3 | REQUIREMENTS 추적표 OPS-03 | 계획 단계 | COVERED | RESOLVED — 이 계획에서 REQUIREMENTS·ROADMAP 수정 |
| RESEARCH | Pitfall 1~5 | 선행 생성 · PITR · IAM 사용자 · OPERATIONS 한도 · journal 번호 | 03 · 03 · 03 · 03 · 01·06 | COVERED | |
| RESEARCH | A1~A7 | 가정 목록 | 01~06 probe_fallback | COVERED | 실측 대상은 06 Task 3 |
| CONTEXT | D8-07 | 사람 목록 표시(D-08 유지) | 04 · 05 | COVERED | |
| CONTEXT | D8-08 | 워크플로 복원 리허설 · 이름 거부 · 사설망 확인 · 결과 기록·표시 · 문서 | 01 · 02 · 03 · 05 · 06 | COVERED | |
| CONTEXT | Discretion | 결과 저장 위치 · 칼럼 모양 · 임시 이름·정리 · PITR | 01 · 04 · 03 · 03 | COVERED | DB 한 줄 · first_login_at · run id 이름 + 한 마감 단계(finalize) 정리 · PITR 끔 |
| CONTEXT | Deferred | 정기 리허설 스케줄 · issue #56 해결 | — | 제외 | 계획에 없음(의도) |

## Review Dispositions Ledger

### Round 1 — uncommitted (Codex GPT 적대적 계획 리뷰, 세션 scratchpad `codex-plan-final.md`, 검토 대상 계획 커밋 `7a1eca3`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #15 잠긴 표 조회가 상태 화면 전체를 멈출 수 있다 | medium | Plan 01, Task 2: `SET LOCAL statement_timeout`·`lock_timeout` + 잠금 통합 테스트(5초) → 그 행만 `확인 불가` |
| #16 읽은 행 검증이 없다(Invalid Date가 형식화에서 던짐) | medium | Plan 01, Task 2: 쓰기와 같은 검증 함수로 읽은 행 확인, `infinity`·역전 시각 행 통합 테스트 |
| #17 실패+NULL 단계를 check가 통과시킬 수 있고 거부 테스트가 빠졌다 | medium | Plan 01, Task 1: check에 `failed_stage is not null` 명시 + 직접 SQL 전 조합 행렬; Task 2: URL·시각·백업 id 쓰기 거부 행렬과 CLI 거부 |
| #20 Task 1이 구현하고 Task 2가 나중에 테스트한다 | low | Plan 01: Task 1을 트레이서 경로로 줄이고, 검증·시간 제한·흡수·실패 문구는 Task 2에서 빨간 테스트를 본 뒤 구현 |
| #11 CLI 가드 전에 커넥터가 초기화된다 | medium | Plan 01, Task 1: CLI를 DB 없는 진입점으로(정적 import 0, 검사 뒤 동적 import). 커넥터 미초기화 단위 테스트는 Plan 02 Task 1 |
| #6 예약 번호 병합 순서 위험 | high | 조정자 마이그레이션 규칙으로 대체(처음부터 예약 번호를 쓰는 방식 취소): 브랜치 번호는 db:generate가 준 그대로, 머지 직전 04.4-06이 main 마지막+1로 재생성 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| #7 journal 가드가 스냅숏 prevId 사슬을 보지 않는다 | 공유 가드는 main 또는 04.1 파일을 글자 그대로 쓴다(조정자 규칙) — 이 페이즈가 단언을 더하지 않는다. drizzle-kit 자체가 prevId 충돌이면 generate를 멈추고, 04.4-06의 "No schema changes" 확인이 어긋남을 잡는다. 페이즈 간 제안으로 SUMMARY·보고에만 적는다 |
| #13 플랜 직렬화가 다른 페이즈와의 DB 공유를 막지 못한다 | 반려 — 실행 세션(스레드)마다 자기 컨테이너와 자기 로컬 Postgres를 쓰므로 병렬 페이즈가 `erp_test`를 나누지 않는다. 페이즈 안에서는 웨이브마다 플랜 하나인 체인이 이미 직렬화한다 |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-01-SUMMARY.md` when done
</output>
