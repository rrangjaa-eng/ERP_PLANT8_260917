---
phase: 04.4-restore-rehearsal-and-login-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/ops.ts
  - db/schema/index.ts
  - db/migrations/0030_restore_rehearsals.sql
  - db/migrations/meta/0030_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/restore-rehearsals.ts
  - domain/ops/restore-rehearsal.ts
  - domain/system-status/index.ts
  - app/(app)/admin/system-status/page.tsx
  - app/(app)/admin/system-status/restore-rehearsal-view.ts
  - app/(app)/admin/system-status/system-status.module.css
  - scripts/restore-rehearsal-cli.ts
  - test/integration/restore-rehearsal.test.ts
  - test/unit/db/migration-journal.test.ts
  - test/unit/ops/restore-rehearsal-view.test.ts
  - test/unit/restore-rehearsal-cli.test.ts
  - test/unit/system-status.test.ts
  - test/e2e/system-status.spec.ts
  - docs/design/SYSTEM.md
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "리허설 결과는 운영 DB 자신의 `restore_rehearsals` 표에 한 줄로 남는다 — 원본 환경(staging|production) · 성공 여부 · 실패 단계(restore|verify|cleanup, 성공이면 NULL) · 백업 id(NULL 가능) · 시작·종료 시각(timestamptz) · GitHub Actions 실행 URL(NULL 가능). 기록은 같은 컨테이너 이미지의 CLI `record` 하위 명령이 쓰고 사람은 DB에 명령하지 않는다(D8-08, ROADMAP 04.4 기준 2)"
    - "DB check 제약이 불변식을 지킨다: 원본은 staging·production 둘 중 하나, 성공이면 실패 단계가 NULL이고 실패면 세 단계 중 하나다 — 도메인 검증을 건너뛴 삽입도 DB가 거부한다"
    - "CLI `record`는 원본 환경을 인자로 받지 않고 컨테이너의 `APP_ENV`에서 정한다(staging → staging, prod → production, local → 사용 오류 종료 코드 2) — 스테이징 Job이 프로덕션 기록을 만들 경로가 없다"
    - "관리자 시스템 상태 화면의 「마지막 백업」 바로 다음 줄이 「복원 리허설」이다(UI-SPEC Component Inventory · 04.2와 상대 순서만 고정)"
    - "기록이 0행이면 값은 `리허설 기록 없음 — 첫 리허설 전`이고, 조회 자체가 실패하면(연결·권한·표 없음 등 종류 불문) 그 행만 `StatusTag kind=\"muted\"` `확인 불가`이며 나머지 항목은 정상으로 보인다 — 두 경우를 하나로 뭉치지 않는다(UI-SPEC Copywriting Error/Empty · UI Considerations error·partial covered)"
    - "성공 기록은 `성공 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 12분` 모양이고, 실패 기록은 `실패 · 검증 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 4분 · 실행 기록`처럼 결과 바로 뒤에 단계 하나(복원·검증·정리), 맨 끝에 같은 탭으로 열리는 3차 링크 `실행 기록`이 붙는다. 백업 id가 NULL이면 `백업 …` 세그먼트와 그 구분자가 통째로 빠진다(UI-SPEC 복원 리허설 값 행 · populated covered)"
    - "일시는 종료 시각을 `Asia/Seoul` 24시간 `YYYY-MM-DD HH:MM`으로, 소요 시간은 종료 − 시작이 60초 미만이면 `1분 미만`, 아니면 초를 버린 정수 `N분`으로 보인다. 여러 기록 중 종료 시각 내림차순 최신 1건(동시각이면 `seq` 내림차순)을 보인다"
    - "`dd` 값은 줄바꿈되어도 잘리지 않고, 백업 id 숫자만 `white-space: nowrap` · `tabular-nums`라 중간에서 끊기지 않는다. `실행 기록` 링크는 사람 목록 「상세」 링크(`.detailLink`)와 같은 모양이고 폰(<700px)에서 히트 영역이 44×44 이상이다(UI-SPEC Responsive #21 · overflow covered)"
    - "화면은 서버 렌더 스냅샷이다 — 로딩 구간·폴링·진행 중 상태를 추가하지 않는다(UI Considerations loading covered, Outside Review #10 반려 유지)"
    - "SYSTEM.md §6-8 와이어프레임의 「마지막 백업」 바로 다음에 「복원 리허설」 줄이 있고, 본문·LOADING·EMPTY의 고정 개수 문구(「세 항목」·「뼈대 3줄」)가 개수와 무관한 표현으로 바뀌었다(UI-SPEC 갱신 ②)"
    - "이 페이즈의 첫 마이그레이션은 **처음부터** 예약 범위 안의 `0030_restore_rehearsals`다 — SQL `db/migrations/0030_restore_rehearsals.sql` · 스냅숏 `db/migrations/meta/0030_snapshot.json` · journal 항목 `idx: 30` · `tag: \"0030_restore_rehearsals\"`. 0011~0016(Phase 4 몫) 번호의 파일은 이 브랜치에 한 순간도 커밋되지 않는다(조정자 정정 B1)"
    - "`test/unit/db/migration-journal.test.ts`가 journal의 idx 유일 · tag 유일 · tag 접두 = 0 채운 idx · 배열 순서대로 `when` 엄격 증가 · tag마다 `.sql` 존재 · **meta/ 스냅숏 중 이름순 마지막 파일이 `when`이 가장 큰 항목의 스냅숏**임을 지킨다. idx 연속(빈틈 없음)은 요구하지 않는다 — 예약 범위 번호는 빈틈을 만든다(B3, main에 이미 있으면 그 파일을 그대로 쓴다)"
  prohibitions:
    - statement: "리허설 결과를 기록하려고 사람이 운영 DB에 명령을 치는 경로를 만들어서는 안 된다 — 기록은 Cloud Run Job 안의 CLI만 쓴다(CLAUDE.md 금지 · ROADMAP 04.4 기준 2)"
      status: resolved
      verification: test
    - statement: "스테이징 기록이 프로덕션 증거로 보여서는 안 된다 — 각 환경의 화면은 자기 DB만 읽고, 원본 환경은 인자가 아니라 컨테이너 `APP_ENV`로 정해진다(Phase 8 기준 5)"
      status: resolved
      verification: test
    - statement: "`restore_rehearsals` 조회 실패가 화면 전체 오류(공유 `app/(app)/error.tsx`)로 번져서는 안 된다 — 도메인이 자기 안에서 잡아 `확인 불가`로 흡수한다(UI-SPEC Error state)"
      status: resolved
      verification: test
    - statement: "실행 URL을 검증 없이 `href`에 넣어서는 안 된다 — `https://github.com/<owner>/<repo>/actions/runs/<숫자>` 모양만 저장된다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/ops.ts"
      provides: "restore_rehearsals 표 + check 제약 둘"
      contains: "restore_rehearsals"
    - path: "db/migrations/0030_restore_rehearsals.sql"
      provides: "restore_rehearsals 생성 SQL — 처음부터 예약 범위 번호 0030(journal idx 30)"
      contains: "restore_rehearsals_stage_check"
    - path: "repositories/restore-rehearsals.ts"
      provides: "insertRestoreRehearsal · findLatestRestoreRehearsal (viewer 첫 인자)"
      exports: ["insertRestoreRehearsal", "findLatestRestoreRehearsal"]
    - path: "domain/ops/restore-rehearsal.ts"
      provides: "recordRestoreRehearsal · getLatestRestoreRehearsal · RestoreRehearsalRecord(행 타입이 아닌 도메인 타입)"
      exports: ["recordRestoreRehearsal", "getLatestRestoreRehearsal"]
    - path: "app/(app)/admin/system-status/restore-rehearsal-view.ts"
      provides: "formatRestoreRehearsal — 결과·원본·일시·백업 id·소요 시간·실행 링크 세그먼트"
      exports: ["formatRestoreRehearsal"]
    - path: "scripts/restore-rehearsal-cli.ts"
      provides: "record 하위 명령(parseArgs · main, account-cli 규약)"
      exports: ["parseArgs", "main", "UsageError"]
    - path: "test/integration/restore-rehearsal.test.ts"
      provides: "CLI record → DB → getSystemStatus 트레이서와 기록 없음·최신 선택 증명"
      min_lines: 60
    - path: "test/unit/db/migration-journal.test.ts"
      provides: "journal idx · tag · when · .sql 가드"
      min_lines: 20
  key_links:
    - from: "scripts/restore-rehearsal-cli.ts"
      to: "domain/ops/restore-rehearsal.ts"
      via: "record 하위 명령이 SYSTEM_VIEWER로 recordRestoreRehearsal을 부른다"
      pattern: "recordRestoreRehearsal"
    - from: "domain/system-status/index.ts"
      to: "domain/ops/restore-rehearsal.ts"
      via: "StatusDeps.getLatestRestoreRehearsal — try/catch로 unavailable 흡수"
      pattern: "getLatestRestoreRehearsal"
    - from: "app/(app)/admin/system-status/page.tsx"
      to: "app/(app)/admin/system-status/restore-rehearsal-view.ts"
      via: "KvList 「복원 리허설」 항목 값 조립"
      pattern: "formatRestoreRehearsal"
    - from: "db/schema/index.ts"
      to: "db/schema/ops.ts"
      via: "배럴 끝 export 한 줄 — test/integration/setup.ts가 배럴의 모든 표를 TRUNCATE한다"
      pattern: "ops"
---

<objective>
**As a** 관리자, **I want to** 시스템 상태 화면에서 마지막 복원 리허설이 언제·어느 원본으로·어느 백업으로·성공했는지(실패면 어느 단계에서) 한 줄로 보기를, **so that** 백업이 실제로 복원되는지 사람이 운영 DB에 손대지 않고 확인할 수 있다.

이 플랜은 D8-08의 「결과를 기록하고 상태 화면 「복원 리허설」 항목으로 보인다」 절반을 만든다. Task 1은 **트레이서**다 — CLI `record` → 도메인 → 리포지토리 → `restore_rehearsals` 표 → `getSystemStatus` → 상태 화면 한 줄을 한 번에 관통한다. Task 2는 문구 규칙(실패 단계·1분 미만·백업 id 생략·실행 링크)과 모양(CSS), CLI 인자·상태 합성 단위 테스트로 넓힌다. Task 3은 브라우저 E2E와 SYSTEM.md §6-8을 맞춘다.

Purpose: ROADMAP 04.4 기준 2(결과가 상태 화면에 보인다 · 기록 없음 · 확인 불가 · 사람이 운영 DB에 명령하지 않는다)와 OPS-06의 「복원 리허설」 항목(CONTEXT 「분리로 생긴 조정」).
Output: `restore_rehearsals` 표와 마이그레이션 `0030_restore_rehearsals`(처음부터 예약 범위 번호), 리포지토리·도메인·CLI `record`, 상태 화면 한 줄, 테스트, SYSTEM.md §6-8.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다 — 모든 task가 실패 테스트 먼저다. 테스트·마이그레이션이 실패하면 원인을 쫓기 전에 `systematic-debugging`을 호출한다. 커밋하거나 「완료」라고 말하기 전에 `verification-before-completion`을 호출하고 verify 명령의 실제 출력으로 판정한다. 서브에이전트에 위임하면 그 프롬프트에도 세 스킬 이름과 `model`을 명시한다(CLAUDE.md [Build]).
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적어 둔다(SUMMARY 머리에도). diff 기준 검사는 `git diff "$PLAN_BASE"..HEAD -- <경로>`로 한다.
- **실행 순서(공유 DB, A4)**: 이 페이즈의 다섯 플랜은 한 줄 체인이다(01 → 02 → 03 → 04 → 05, 웨이브마다 플랜 하나). 통합·E2E가 같은 로컬 DB(`erp` · `erp_test`)를 쓰므로 동시에 돌지 않는 것을 이 순서가 보장한다.
- **마이그레이션 번호(B1, 조정자 정정)**: 조정자가 04.4에 0030~0034를 예약했다(Phase 4 = 0011~0016 · 04.1 = 0017~0020 · 04.2 = 0021~0024 · 04.3 = 0025~0029). 이 플랜의 마이그레이션은 **처음부터 0030**이다 — 0011 같은 다른 페이즈 번호로 만들어 두었다가 나중에 옮기지 않는다. 방법(Task 1 ④): `pnpm db:generate --name restore_rehearsals`(drizzle-kit이 journal 마지막 idx + 1 = 11로 파일 셋을 만든다) → **같은 task 안에서, 커밋 전에** SQL `0011_restore_rehearsals.sql` → `0030_restore_rehearsals.sql`, 스냅숏 `meta/0011_snapshot.json` → `meta/0030_snapshot.json`으로 이름을 바꾸고, `_journal.json`의 **마지막 항목 하나**에서 `idx`를 30, `tag`를 `0030_restore_rehearsals`로 바꾼다(`when`·`version`·`breakpoints`와 다른 항목은 그대로). `idx`까지 30으로 두는 이유: drizzle-kit 0.31.10은 다음 번호를 「journal 마지막 항목 idx + 1」로 정하므로(`node_modules/drizzle-kit/bin.cjs` 32926–32927행) 04.4-04의 다음 생성이 자동으로 0031이 된다. 스냅숏 이름을 바꾸는 이유: drizzle-kit은 `meta/`의 스냅숏을 **이름순 정렬한 마지막 파일**을 이전 상태로 쓴다(같은 파일 8135–8136행 · 19862–19870행). drizzle-orm `migrate()`는 파일을 `tag`로 찾고(`node_modules/drizzle-orm/migrator.js` 13–15행) 적용 여부를 `when`으로 판정하므로 번호 빈틈은 적용에 영향이 없다. `drizzle-kit push` 금지. 로컬 개발 DB에만 `pnpm db:migrate`, 스테이징·프로덕션 적용 없음(배포는 `/ship` 뒤 `deploy.yml`). 병합 직전 규칙은 04.4-05 Task 1(main 병합 → 자기 마이그레이션 삭제 → 재생성 → 다시 0030으로).
- **덧붙이기 전용(A2)**: `db/schema/index.ts`는 끝에 한 줄만 더한다. 공유 등록부(`domain/action-log/record.ts` · `domain/settings/keys.ts` · `test/integration/leak-scan.test.ts`)는 이 플랜이 건드리지 않는다. 상태 화면·`domain/system-status`에는 항목을 **더하기만** 하고 기존 세 항목과 가드(`getSession`/`redirect`/`notFound`)는 한 글자도 바꾸지 않는다 — 04.2가 같은 곳에 「마지막 tick」「이메일 미설정」을 더한다.
- **금지(A3)**: 견적 줄·프로젝트·`document_counters`·demo 행 파일을 건드리지 않는다. `.planning/STATE.md`를 고치지 않는다.
- 새 의존성 없음. `git push` 하지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 엣지 프로브가 비었다(EDGE_ABSENT=1). 이 플랜이 스스로 적은 엣지 truth: **empty 대 확인 불가 분리**(truth 5) · **최신 1건 선택과 동시각 순서**(truth 7) · **원본 환경 위조 불가**(truth 3) · **DB 수준 불변식**(truth 2). OPS-03 프로브 행은 분류되지 않은 채(unresolved) 남았고, 이 플랜은 그중 「결과 기록·표시」 절반을 닫는다 — 나머지(복원·검증·정리·문서·실제 1회)는 04.4-02 · 04.4-03 · 04.4-05가 닫는다.

**계획 가정(뒤집히면 해당 함수만 바뀐다)**
1. 결과 저장소는 GCS 파일이 아니라 운영 DB 한 줄이다(RESEARCH A5, CONTEXT Discretion) — 새 의존성(`@google-cloud/storage`)·버킷이 필요 없고, 화면이 같은 DB에서 읽는다
2. 실행 식별자는 run id가 아니라 검증된 전체 URL(`run_url`)로 저장한다 — 화면이 저장소 이름을 몰라도 된다
3. 실패 단계는 영어 코드(restore·verify·cleanup)로 저장하고 화면에서 복원·검증·정리로 바꾼다
4. ROADMAP 기준 2의 「리허설 없음」은 UI-SPEC(승인됨)의 정확한 문구 `리허설 기록 없음 — 첫 리허설 전`으로 구현한다
5. 백업 id는 숫자만 허용한다(GCP 자동 백업 id 형태) — 아니면 도메인이 거부한다
6. **(표시 가정 — 조정자 확인 필요) 번호 빈틈과 drizzle-kit**: 이름을 바꾼 0030 항목은 drizzle-kit·drizzle-orm 양쪽에서 동작한다(위 execution_notes의 코드 근거). **위험**: drizzle-kit이 이전 스냅숏을 「이름순 마지막 파일」로 고르므로, 예약 범위가 **번호 순서와 다르게** 병합되면(예: 04.4 = 0030이 먼저 main에 들어가고 Phase 4 = 0011~0016이 나중에 들어오면) 나중 페이즈의 스냅숏이 이름순에서 0030보다 앞서 이전 상태로 쓰이지 않고, 그 뒤 `pnpm db:generate`가 이미 있는 DDL을 다시 만든다. 그래서 journal 가드에 「이름순 마지막 스냅숏 = `when`이 가장 큰 항목의 스냅숏」 단언을 넣어 CI가 이 상태를 잡게 하고, 04.4-05 Task 1은 낮은 예약 범위 페이즈가 아직 main에 없으면 멈춰 병합 순서를 사용자에게 묻는다. **대안(되돌아갈 길)**: 조정자가 이 위험을 받아들이지 않으면 04.4는 병합 시점 main 마지막 번호 + 1(drizzle-kit 기본)을 쓰고 예약 범위를 포기한다 — 그 경우 04.4-05 Task 1 ④의 이름 바꾸기만 빼면 된다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — CLI `record` 한 번이 `restore_rehearsals` 한 줄이 되고 상태 화면 「복원 리허설」에 보인다 (DB · 리포지토리 · 도메인 · 상태 합성 · 화면 · CLI)</name>
  <files>db/schema/ops.ts, db/schema/index.ts, db/migrations/meta/_journal.json, repositories/restore-rehearsals.ts, domain/ops/restore-rehearsal.ts, domain/system-status/index.ts, app/(app)/admin/system-status/page.tsx, app/(app)/admin/system-status/restore-rehearsal-view.ts, scripts/restore-rehearsal-cli.ts, test/integration/restore-rehearsal.test.ts, test/unit/db/migration-journal.test.ts</files>
  <precondition>로컬 Postgres가 떠 있다(`bash scripts/dev-db.sh`가 성공한다) — 통합 테스트와 `pnpm db:migrate`의 전제</precondition>
  <reversibility rating="costly">새 표의 칼럼 모양(원본 enum · 실패 단계 코드 · run_url)은 운영 DB에 배포되면 바꾸는 데 마이그레이션이 하나 더 든다. 아직 어디에도 배포되지 않았고 병합 직전 재생성이 있으므로 되돌릴 수 없는 결정은 아니다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 101–144행(Copywriting · 복원 리허설 값 행 · 필수 데이터 항목) — 정확한 문구와 칼럼 목록
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md` — 파일별 가장 가까운 선례
    - `db/schema/action-log.ts` — `bigint(..., {mode:"number"}).primaryKey().generatedByDefaultAsIdentity()` seq 선언(squawk prefer-identity)
    - `db/schema/corp-cards.ts` — `check("이름", sql\`...\`)` 선례(도메인 순수 함수 + DB check)
    - `db/schema/index.ts` — 배럴 끝 줄
    - `repositories/system-status.ts` · `repositories/users.ts` — viewer 첫 인자 규약(`plant8/repository-viewer-param`)과 `void viewer`
    - `domain/system-status/index.ts`(전문 85행) — `SystemStatus` · `StatusDeps` · db 필드의 try/catch + `log.warn("status.db_unavailable")` 모양
    - `app/(app)/admin/system-status/page.tsx`(전문 77행) — `KvList` items 배열과 「마지막 백업」 삼항식, 가드(건드리지 않음)
    - `scripts/account-cli.ts`(전문) — `UsageError` · 엄격한 `parseArgs`(`--플래그 값` 쌍만) · `main()`의 `closeDb` finally · 종료 코드 2/1 · `import.meta.url` 가드
    - `test/integration/account-cli.test.ts` 1–60행 — `spawnSync("node", ["--import","tsx", ...])`로 CLI를 자식 프로세스로 돌리는 모양과 env(비로컬 `APP_ENV`에 필요한 `BETTER_AUTH_SECRET` 32자 이상·`BETTER_AUTH_URL`)
    - `test/integration/setup.ts` — 배럴의 모든 표를 매 테스트 전 TRUNCATE(새 표도 자동 포함)
    - `lib/env.ts` 44–52행 — `APP_ENV` enum(local·staging·prod)
    - `eslint/rules/no-row-type-escape.mjs` 1–30행 — domain export 함수가 `…Row` 타입을 반환하면 안 된다
  </read_first>
  <behavior>
    - 통합 1: `restore_rehearsals`가 비어 있으면 `getSystemStatus(관리자).restoreRehearsal`이 `{kind:"none"}`이다
    - 통합 2(트레이서): `APP_ENV=staging`으로 CLI `record --succeeded true --backup-id 1758684000000 --started-at 2026-09-23T18:02:00Z --finished-at 2026-09-23T18:14:00Z --run-url https://github.com/o/r/actions/runs/1`을 자식 프로세스로 돌리면 종료 코드 0이고, 곧이어 `getSystemStatus`가 `{kind:"recorded"}` + source `staging` · succeeded true · failedStage null · 같은 backupId·시각을 돌려준다
    - 통합 3: 종료 시각이 다른 두 기록 중 늦은 쪽, 종료 시각이 같으면 나중에 넣은(seq가 큰) 쪽이 선택된다
    - 통합 4: 도메인을 거치지 않은 직접 삽입이라도 성공 + 실패 단계 `verify`, 또는 원본 `local`이면 DB가 check 제약으로 거부한다
    - 통합 5: `APP_ENV=local`로 `record`를 돌리면 종료 코드 2이고 행이 생기지 않는다
    - 단위(journal 가드): idx 유일 · tag 유일 · tag가 `String(idx).padStart(4,"0") + "_"`로 시작 · 배열 순서대로 `when` 엄격 증가 · tag마다 `db/migrations/<tag>.sql` 존재 · `db/migrations/meta/`의 `*_snapshot.json`을 이름순 정렬한 마지막 파일이 `when`이 가장 큰 항목의 `<tag 앞 네 자리>_snapshot.json`이다. idx 연속은 요구하지 않는다(0010 다음 0030이 정상)
  </behavior>
  <action>
① **journal 가드(B3)**: `git fetch origin main` 뒤 `git show origin/main:test/unit/db/migration-journal.test.ts`가 있으면 그 내용을 그대로 쓴다 — 단 그 파일이 idx 연속(`entries[i].idx === i`)을 요구하면 0030 번호와 양립할 수 없으므로 **고치지 말고 멈춰** 조정자·사용자에게 알린다. 없으면(계획 시점 main에는 없다) 같은 경로에 새로 만든다 — `db/migrations/meta/_journal.json`과 `meta/` 스냅숏 목록을 읽어 behavior 「단위(journal 가드)」의 여섯 불변식을 단언한다. 지금 트리(0000~0010)에서 녹색이어야 한다.

② **실패 테스트 먼저**: `test/integration/restore-rehearsal.test.ts`에 behavior 통합 1~5를 쓴다. CLI는 `test/integration/account-cli.test.ts`의 `spawnSync` 모양을 따라 `scripts/restore-rehearsal-cli.ts`를 돌리고, env에 `DATABASE_URL`(erp_test) · `APP_ENV` · 32자 이상 `BETTER_AUTH_SECRET` · `BETTER_AUTH_URL`을 준다. 표 비우기는 `test/integration/setup.ts`가 이미 하므로 따로 지우지 않는다. 돌려서 실패(표·모듈 없음)를 확인한다.

③ **스키마(D8-08)**: 새 파일 `db/schema/ops.ts`에 `restore_rehearsals`를 정의한다 — `seq`(bigint identity PK, action-log 선례) · `source` text NOT NULL · `succeeded` boolean NOT NULL · `failed_stage` text NULL · `backup_id` text NULL · `started_at`·`finished_at` `timestamp(..., { withTimezone: true })` NOT NULL(UI-SPEC #11 — 이 표만 timestamptz) · `run_url` text NULL. check 제약 둘: `restore_rehearsals_source_check`(source ∈ staging·production), `restore_rehearsals_stage_check`(성공이면 failed_stage IS NULL, 실패면 restore·verify·cleanup 중 하나). 파일 머리 주석에 D8-08과 「각 환경이 자기 DB에만 쓴다」를 한 줄로 적는다. `db/schema/index.ts` 끝에 export 한 줄.

④ **[BLOCKING] 마이그레이션 — 처음부터 0030(B1 조정자 정정)**: (a) `pnpm db:generate --name restore_rehearsals` — drizzle-kit이 `0011_restore_rehearsals.sql` · `meta/0011_snapshot.json` · journal 마지막 항목(idx 11)을 만든다 (b) **커밋하기 전에** SQL을 `db/migrations/0030_restore_rehearsals.sql`로, 스냅숏을 `db/migrations/meta/0030_snapshot.json`으로 이름을 바꾸고(`mv`), `_journal.json` 마지막 항목의 `idx`를 30, `tag`를 `0030_restore_rehearsals`로 바꾼다 — 다른 항목·필드는 한 글자도 바꾸지 않는다 (c) `pnpm db:generate` 재실행이 "No schema changes"인지 확인(이름순 마지막 스냅숏이 0030이라 스키마와 일치해야 한다) (d) `pnpm lint:sql` (e) `pnpm db:migrate`(로컬 개발 DB `erp`만 — 0030이 적용된다) (f) journal 가드 녹색. 통합 테스트는 `test/integration/global-setup.ts`가 `erp_test`에 적용한다. 0011 번호 파일은 어떤 커밋에도 들어가지 않는다. `drizzle-kit push`·스테이징·프로덕션 적용 금지. 만약 (c)나 (e)가 이름 바꾸기 때문에 실패하면 되돌려 추측으로 고치지 말고 멈춰 SUMMARY와 함께 보고한다(probe_fallback 가정 6의 대안으로 갈지는 조정자 결정).

⑤ **리포지토리**: `repositories/restore-rehearsals.ts` — `insertRestoreRehearsal(viewer, values)`와 `findLatestRestoreRehearsal(viewer)`(`finished_at` 내림차순 → `seq` 내림차순, limit 1, 없으면 null). 두 함수 모두 viewer 첫 인자(`void viewer`, `repositories/system-status.ts` 모양).

⑥ **도메인**: `domain/ops/restore-rehearsal.ts` — 도메인 타입 `RestoreRehearsalRecord`(source `"staging"|"production"` · succeeded · failedStage `"restore"|"verify"|"cleanup"|null` · backupId · startedAt · finishedAt · runUrl)를 정의하고 행을 이 타입으로 옮겨 반환한다(행 타입 이름이 export 밖으로 나가지 않게 — no-row-type-escape). `recordRestoreRehearsal(viewer, input)`은 삽입 전에 검증한다: 원본 두 값 · 성공 ⇔ 실패 단계 없음 · 단계 세 값 · 시작 ≤ 종료 · 백업 id는 숫자만 · 실행 URL은 정규식 `^https://github\.com/[\w.-]+/[\w.-]+/actions/runs/\d+$`만. 어기면 기존 도메인 검증 오류 관례(`domain/people`의 `ValidationError` 모양)로 거부한다. `getLatestRestoreRehearsal(viewer)`는 최신 1건 또는 null. 권한 검사는 두지 않는다 — 호출자가 CLI(SYSTEM_VIEWER)와 이미 `can()`을 거친 `getSystemStatus`뿐이다.

⑦ **상태 합성**: `domain/system-status/index.ts`의 `SystemStatus`에 `restoreRehearsal` 필드(`{kind:"none"}` | `{kind:"recorded"; record}` | `{kind:"unavailable"}`)를, `StatusDeps`에 `getLatestRestoreRehearsal`을 **더한다**. db 필드와 같은 try/catch로 감싸 오류는 `log.warn("status.restore_rehearsal_unavailable", { message })` 후 unavailable. 기존 필드·판정 순서는 바꾸지 않는다.

⑧ **화면**: `app/(app)/admin/system-status/restore-rehearsal-view.ts`에 순수 함수 `formatRestoreRehearsal(record)`를 만든다 — 반환은 `{ head, backupId, duration, runUrl }`: head는 성공이면 `성공 · <원본> · <일시>`, 실패면 `실패 · <단계> · <원본> · <일시>`(원본 스테이징·프로덕션, 단계 복원·검증·정리, 일시는 종료 시각을 `Intl.DateTimeFormat("en-CA", {timeZone:"Asia/Seoul", hour12:false, ...}).formatToParts`로 연·월·일·시·분을 뽑아 `YYYY-MM-DD HH:MM`으로 직접 조립 — 로캘 문자열 그대로 쓰지 않는다), duration은 60초 미만 `1분 미만` 아니면 `N분`, runUrl은 실패이고 값이 있을 때만. `page.tsx`의 `KvList` items에서 「마지막 백업」 항목 **바로 다음**에 `label: "복원 리허설"` 항목 하나를 넣는다: none → `리허설 기록 없음 — 첫 리허설 전`, unavailable → 기존 「마지막 백업」과 같은 `<StatusTag kind="muted">확인 불가</StatusTag>`, recorded → head 뒤에 백업 id가 있으면 ` · 백업 ` + id를 감싼 span, duration, runUrl이 있으면 ` · ` + 같은 탭 `<a href={runUrl}>실행 기록</a>`(target 없음). 스타일 클래스는 Task 2가 붙인다.

⑨ **CLI**: `scripts/restore-rehearsal-cli.ts` — `account-cli.ts` 규약 그대로(`UsageError`, `parseArgs(argv)` 엄격, `main()` finally `closeDb`, 종료 코드 사용 오류 2 · 그 밖 실패 1, `import.meta.url` 가드). 하위 명령 `record`: `--succeeded true|false`(필수) · `--failed-stage`(선택) · `--backup-id`(선택) · `--started-at`·`--finished-at`(ISO, 필수) · `--run-url`(선택). 원본 환경 플래그는 **두지 않는다** — `APP_ENV`로 정한다(staging → staging, prod → production, local → `UsageError`「로컬에서는 리허설 결과를 기록하지 않습니다」). 성공하면 한 줄(「복원 리허설 결과를 기록했습니다 …」)을 찍는다. 출력 문구는 「…니다」로 끝낸다.

⑩ ②의 테스트가 녹색이 될 때까지 구현한다. 커밋 제목 예 `feat: record restore rehearsal results and show them on system status`, 본문 한국어(D8-08).
  </action>
  <verify>
    <automated>test -f db/migrations/0030_restore_rehearsals.sql &amp;&amp; test -f db/migrations/meta/0030_snapshot.json &amp;&amp; test -z "$(ls db/migrations/0011_* db/migrations/meta/0011_* 2&gt;/dev/null)" &amp;&amp; pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>0030 SQL·스냅숏이 없거나, 0011 번호 파일이 남아 있거나, journal 가드(idx·tag 유일 · tag 접두 = idx · `when` 증가 · `.sql` 존재 · 이름순 마지막 스냅숏 = 최신 항목)가 실패한다</fails_when>
    <automated>pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/restore-rehearsal.test.ts</automated>
    <fails_when>squawk가 새 SQL을 거부하거나, 로컬 적용이 실패하거나, 트레이서(CLI record → getSystemStatus recorded)·기록 없음·최신 선택·check 제약·local 거부 중 하나가 빨갛다</fails_when>
    <automated>pnpm db:generate 2&gt;&amp;1 | grep -q "No schema changes" &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>스키마와 생성 마이그레이션이 어긋나 "No schema changes"가 없거나, 타입·린트(repository-viewer-param · no-row-type-escape · boundaries 포함)가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=A "$PLAN_BASE"..HEAD -- db/migrations`가 정확히 `db/migrations/0030_restore_rehearsals.sql`과 `db/migrations/meta/0030_snapshot.json` 두 줄이다(0011 번호 파일이 어느 커밋에도 없다 — `git log --name-only --format= "$PLAN_BASE"..HEAD -- 'db/migrations/0011_*' 'db/migrations/meta/0011_*'`가 비어 있다)
    - `_journal.json` 마지막 항목이 `idx: 30` · `tag: "0030_restore_rehearsals"`이고, `D=$(git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json) && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 1을 출력한다(삭제 줄은 직전 마지막 항목의 닫는 `}`가 `},`로 바뀌는 한 줄뿐 — 앞 항목 0000~0010의 내용은 바뀌지 않고 마지막 항목만 더해졌다)
    - 그 SQL에 `restore_rehearsals_source_check`와 `restore_rehearsals_stage_check`가 있고 `timestamp with time zone`이 두 번 이상 나온다
    - `grep -n "복원 리허설" "app/(app)/admin/system-status/page.tsx"`의 줄 번호가 `grep -n "마지막 백업"`의 줄 번호보다 크고, 두 항목 사이에 다른 `label:`이 없다
    - `D=$(git diff "$PLAN_BASE"..HEAD -- "app/(app)/admin/system-status/page.tsx") && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 0을 출력한다(삭제 줄 없음 — 기존 항목·가드를 더하기만 했다)
    - `scripts/restore-rehearsal-cli.ts`에 `--source` 플래그 처리 분기가 없다(`grep -c "\"--source\"" scripts/restore-rehearsal-cli.ts`가 0)
    - 세 verify 명령이 전부 초록이다
  </acceptance_criteria>
  <done>CLI `record` 한 번으로 생긴 한 줄이 로컬에서 상태 합성 결과에 `recorded`로 나오고, 표가 비면 `none`이며, 화면 목록의 「마지막 백업」 바로 다음에 「복원 리허설」 항목이 렌더된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 문구·모양 확장 — 실패 단계 · 1분 미만 · 백업 id 생략 · 실행 링크 · 확인 불가 흡수 · CLI 인자 규약</name>
  <files>app/(app)/admin/system-status/restore-rehearsal-view.ts, app/(app)/admin/system-status/system-status.module.css, app/(app)/admin/system-status/page.tsx, test/unit/ops/restore-rehearsal-view.test.ts, test/unit/restore-rehearsal-cli.test.ts, test/unit/system-status.test.ts</files>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 79–97행(Color — `--accent`는 실패 행 링크 한 곳뿐) · 111–131행(문구 표와 규칙) · 167–172행(Responsive #21)
    - `app/(app)/admin/people/people.module.css` 78–96행 — `.detailLink`와 폰 미디어 쿼리(`max-width: 699.98px`, 44×44)
    - `test/unit/system-status.test.ts`(전문) — deps 주입 모양, 기존 테스트가 새 dep 없이 부르는 곳
    - `test/unit/account-cli.test.ts` — parseArgs 단위 테스트 모양
    - `docs/design/tokens.css` — `--fs-sm` · `--fw-medium` · `--accent` · `--underline-offset` · `--touch-min`이 실제로 있는지
  </read_first>
  <behavior>
    - 보기 1: 성공 + 백업 id + 12분 → head `성공 · 스테이징 · 2026-09-24 03:14`, backupId `1758684000000`, duration `12분`, runUrl null
    - 보기 2: 실패 verify + 백업 id + 4분 + URL → head `실패 · 검증 · 스테이징 · 2026-09-24 03:14`, runUrl 그대로
    - 보기 3: 실패 restore + 백업 id null → backupId null(세그먼트 생략), runUrl 있음; 실패 cleanup → `정리`
    - 보기 4: 소요 59초 → `1분 미만`, 60초 → `1분`, 119초 → `1분`; 원본 production → `프로덕션`
    - 보기 5: UTC 2026-09-23T15:30:00Z → `2026-09-24 00:30`(KST 날짜 경계), 성공 기록에 URL이 있어도 runUrl null
    - 상태 1: `getLatestRestoreRehearsal`이 throw하면 `restoreRehearsal`은 unavailable이고 다른 필드(version·db·backup)는 정상이다
    - 상태 2: 권한 없는 viewer는 여전히 NotAdminError(새 dep을 부르지 않는다)
    - CLI 1: 모르는 플래그 · 값 없는 플래그 · `--succeeded maybe` · 필수 누락 · 하위 명령 없음 → `UsageError`
  </behavior>
  <action>
① 실패 테스트 먼저: `test/unit/ops/restore-rehearsal-view.test.ts`(보기 1~5), `test/unit/system-status.test.ts`에 상태 1·2 케이스를 **더하고** 기존 테스트 호출에는 `getLatestRestoreRehearsal: () => Promise.resolve(null)`을 주입해 DB에 닿지 않게 한다(기존 단언은 바꾸지 않는다 — 객체 전체 비교가 있으면 새 필드를 기대값에 더한다), `test/unit/restore-rehearsal-cli.test.ts`(CLI 1). 실패를 확인한다.

② `formatRestoreRehearsal`을 behavior에 맞춘다(Task 1 구현이 이미 맞으면 테스트만 녹색이 된다).

③ 새 `app/(app)/admin/system-status/system-status.module.css`: `.runLink`는 `.detailLink`와 같은 다섯 속성(`--fs-sm` · `font-weight: var(--fw-medium)` · `color: var(--accent)` · underline · `text-underline-offset: var(--underline-offset)`)과 같은 폰 미디어 쿼리(`max-width: 699.98px`에서 inline-flex · 가운데 정렬 · `min-width`/`min-height: var(--touch-min)`), `.backupId`는 `white-space: nowrap` + `font-variant-numeric: tabular-nums`. 새 색·서체·radius·간격 값을 만들지 않는다(토큰만). `page.tsx`에서 백업 id span과 링크에 두 클래스를 붙인다.

④ 커밋 제목 예 `feat: format restore rehearsal row per UI-SPEC`, 본문 한국어(D8-08 · UI-SPEC 문구 표).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ops/restore-rehearsal-view.test.ts test/unit/restore-rehearsal-cli.test.ts test/unit/system-status.test.ts</automated>
    <fails_when>문구 규칙(단계·1분 미만·KST 경계·생략·링크 조건), 확인 불가 흡수, 권한 판정, CLI 인자 거부 중 하나가 빨갛다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm exec stylelint "app/(app)/admin/system-status/system-status.module.css"</automated>
    <fails_when>린트·타입 오류가 있거나 stylelint가 새 CSS에서 토큰 밖 색·서체·radius를 잡는다</fails_when>
  </verify>
  <acceptance_criteria>
    - `system-status.module.css`에 `#`로 시작하는 색 리터럴이 없다(`grep -cE "#[0-9a-fA-F]{3,6}" …`가 0)
    - `.runLink` 블록에 `var(--accent)`와 `var(--touch-min)`이 있다
    - `page.tsx`에 `target=`이 없다(같은 탭)
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>UI-SPEC 문구 표의 여섯 상태가 단위 테스트로 고정되고, 링크·백업 id 모양이 기존 토큰만으로 붙는다</done>
</task>

<task type="auto">
  <name>Task 3: 브라우저 E2E와 SYSTEM.md §6-8 — 기록 없음 → 성공 → 실패(실행 기록 링크)</name>
  <files>test/e2e/system-status.spec.ts, docs/design/SYSTEM.md</files>
  <read_first>
    - `test/e2e/system-status.spec.ts`(전문 40행) — 로그인 모양과 `getByText("확인 불가")` 단언(새 행이 「확인 불가」를 하나 더 만들면 strict 위반이 되는 자리)
    - `test/e2e/projects-list.spec.ts`에서 `@/db/client`를 import해 DB를 직접 준비하는 부분(선례)
    - `docs/design/SYSTEM.md` 566–587행(§6-8) — 와이어프레임 · 「세 항목」 · LOADING 「뼈대 3줄」 · EMPTY 「항상 세 항목」
    - `test/unit/design-system-docs.test.ts` 30–62행 — §6-8이 다섯 상태 낱말을 모두 담아야 한다
  </read_first>
  <action>
① `test/e2e/system-status.spec.ts`에 describe 하나를 **더한다**(기존 두 테스트는 그대로): 관리자 fixture로 로그인한 뒤 (a) `db.delete(restoreRehearsals)`로 표를 비우고 화면을 열면 「복원 리허설」 행 값이 `리허설 기록 없음 — 첫 리허설 전`, (b) `recordRestoreRehearsal(SYSTEM_VIEWER, …)`로 시작 2026-09-23T18:02:00Z · 종료 18:14:00Z · 백업 1758684000000 · 성공 · staging을 넣고 새로고침하면 값이 정확히 `성공 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 12분`이고 링크가 없다, (c) 더 늦은 종료 시각의 실패(verify, URL `https://github.com/o/r/actions/runs/42`)를 넣으면 값이 `실패 · 검증 · …`으로 시작하고 `실행 기록` 링크의 href가 그 URL이며 `target` 속성이 없다. 값 단언은 `dt` 「복원 리허설」 옆 `dd`로 좁힌다. 기존 테스트의 「확인 불가」 단언이 여전히 한 개만 잡히는지 확인한다(로컬은 조회가 성공하므로 새 행은 「확인 불가」가 아니다).

② `docs/design/SYSTEM.md` §6-8(D8-08, UI-SPEC 갱신 ②): 와이어프레임 「마지막 백업」 줄 바로 다음에 `│ 복원 리허설  성공 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 12분` 한 줄. 「세 항목:」 문장을 개수 없는 「항목:」 목록으로 바꾸고 끝에 복원 리허설(결과·원본·종료 시각·백업 id·소요 시간, 실패면 단계와 「실행 기록」 링크, 기록 없으면 `리허설 기록 없음 — 첫 리허설 전`)을 더한다. LOADING은 「항목마다 `--surface` 뼈대 한 줄」, EMPTY는 「항목이 항상 조회되고」로 개수를 뺀다. 다섯 상태 머리말(LOADING · EMPTY · ERROR · SUCCESS · PARTIAL)은 그대로 둔다. 다른 절은 건드리지 않는다(04.2가 같은 절에 항목을 더한다 — 개수 문구가 없으면 병합이 사소해진다).

③ 커밋 두 개(E2E · 문서), 제목 예 `test: e2e for restore rehearsal row` · `docs: add restore rehearsal line to SYSTEM.md 6-8`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm exec playwright test test/e2e/system-status.spec.ts --project desktop</automated>
    <fails_when>기록 없음 · 성공 정확 문구 · 실패 링크 href · target 없음 · 기존 두 테스트 중 하나가 실패한다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts</automated>
    <fails_when>§6-8이 다섯 상태 낱말 중 하나를 잃었거나 다른 문서 불변식이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '/### 6-8/,/### 6-9/p' docs/design/SYSTEM.md | grep -c "세 항목\|뼈대 3줄"`이 0이다
    - 같은 범위에서 `복원 리허설` 줄이 `마지막 백업` 줄 바로 다음 줄이다
    - `git diff "$PLAN_BASE"..HEAD --stat -- docs/design/SYSTEM.md`의 변경이 §6-8 안에만 있다(`git diff`의 hunk 머리 줄이 566±10행 범위)
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>브라우저에서 기록 없음 · 성공 · 실패(링크) 세 상태가 UI-SPEC 문구 그대로 보이고, SYSTEM.md §6-8이 새 줄과 개수 없는 문구를 갖는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run Job 인자 → CLI `record` | 워크플로가 넘긴 문자열(성공 여부·단계·백업 id·시각·URL)이 DB에 들어간다 |
| DB 행 → 관리자 화면 | 저장된 `run_url`이 `href`로 렌더된다 |
| 관리자 화면 접근 | `can(viewer, "admin.system-status", "view")` 기존 가드 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Tampering | `run_url` → `href` | medium | mitigate | 도메인이 `^https://github\.com/<owner>/<repo>/actions/runs/<숫자>$`만 저장하고 React가 속성을 이스케이프한다. `javascript:` 등은 저장 단계에서 거부된다 — 통합·단위 테스트가 거부를 단언한다 |
| T-04.4-02 | Spoofing | 기록의 원본 환경 | high | mitigate | CLI에 원본 플래그가 없고 `APP_ENV`로만 정한다(local 거부). DB check 제약이 두 값만 허용한다. 각 환경의 Job은 자기 DB에만 연결된다(deploy.sh 고정) — Task 1 통합 5·acceptance(`--source` 없음) |
| T-04.4-03 | Information Disclosure | 상태 화면 | low | accept | 기존 `can()` + 404 가드를 그대로 쓰고 새 경로를 만들지 않는다. 표시 값(백업 id·시각)은 비밀이 아니다 |
| T-04.4-04 | Denial of Service | 상태 화면 조회 | low | mitigate | 새 조회는 try/catch로 `확인 불가`에 흡수되어 화면 전체 오류가 되지 않는다(Task 2 상태 1) |
| T-04.4-05 | Tampering | `_journal.json` 순서 | high | mitigate | journal 가드 단위 테스트(idx·tag 유일 · 접두 · when · .sql · 이름순 마지막 스냅숏 = 최신 항목)가 CI 단위 단계에서 막고, 병합 직전 재생성(다시 0030)은 04.4-05 Task 1이 한다. 번호 순서와 다른 병합의 스냅숏 위험은 probe_fallback 가정 6 |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개(RESEARCH Package Legitimacy Audit: 대상 없음). `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` 통과
- `pnpm test:unit` 전체 통과(journal 가드 · design-system-docs · import-cycles 포함)
- `pnpm test:integration` 전체 통과(기존 `test/integration/system-status.test.ts` 회귀 없음)
- `pnpm exec playwright test test/e2e/system-status.spec.ts --project desktop` 통과
- `pnpm db:generate` 재실행 결과 "No schema changes"
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml .planning/STATE.md domain/action-log domain/settings/keys.ts test/integration/leak-scan.test.ts` 비어 있음
</verification>

<success_criteria>
- CLI `record` → DB → 상태 화면 한 줄이 로컬에서 관통한다(트레이서)
- 기록 없음 · 성공 · 실패(단계 + 링크) · 확인 불가 네 표시가 UI-SPEC 문구 그대로다
- 원본 환경은 컨테이너 환경에서만 정해지고 DB가 불변식을 한 번 더 지킨다
- SYSTEM.md §6-8이 고정 개수 없이 「복원 리허설」 줄을 갖는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `restore_rehearsals`(check 제약 `restore_rehearsals_source_check` · `restore_rehearsals_stage_check`) |
| 마이그레이션 | `db/migrations/0030_restore_rehearsals.sql` + `meta/0030_snapshot.json` + journal 항목(idx 30) — 처음부터 예약 범위 0030~0034 안. 04.4-05 Task 1이 병합 직전 재생성해 다시 0030에 둔다 |
| 함수 | `insertRestoreRehearsal` · `findLatestRestoreRehearsal` · `recordRestoreRehearsal` · `getLatestRestoreRehearsal` · `formatRestoreRehearsal` |
| 타입 | `RestoreRehearsalRecord` · `SystemStatus.restoreRehearsal` · `StatusDeps.getLatestRestoreRehearsal` |
| CLI | `scripts/restore-rehearsal-cli.ts` `record`(Job 배포·번들은 04.4-02) |
| 화면 | `/admin/system-status` 「복원 리허설」 행 · `system-status.module.css`(`.runLink` · `.backupId`) |
| 로그 이벤트 | `status.restore_rehearsal_unavailable` |
| 테스트 | `test/integration/restore-rehearsal.test.ts` · `test/unit/ops/restore-rehearsal-view.test.ts` · `test/unit/restore-rehearsal-cli.test.ts` · `test/unit/db/migration-journal.test.ts` · `test/unit/system-status.test.ts`(확장) · `test/e2e/system-status.spec.ts`(확장) |
| 문서 | SYSTEM.md §6-8 |

## Source Audit (페이즈 전체 — 다섯 플랜)

| SOURCE | ID | 항목 | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | 워크플로 하나로 최신 자동 백업 → 임시 인스턴스 복원·확인·삭제, 결과가 상태 화면에 | 01 · 02 · 03 · 05 | COVERED | 실제 1회는 05 Task 3(사람 트리거) |
| GOAL | — | 복원 절차(워크플로 실행법 + 사고 때 수동 복원)가 OPERATIONS.md에 | 03 | COVERED | §14, 20줄 이하 |
| GOAL | — | 사람 목록에서 첫 로그인 전·임시 비밀번호 사용 중을 본다 | 04 | COVERED | |
| REQ | OPS-03 | 자동 백업 · 복원 절차 문서 · 복원 리허설 1회 | 01~05 | COVERED | 자동 백업 자체는 Phase 1(deploy.sh), 이 페이즈는 확인법 문서 |
| REQ | 기준 1 | 이름 거부 · 사설망 확인 · 확인 실패에도 삭제 · 비용 수십 분 | 02 · 03 | COVERED | |
| REQ | 기준 2 | 실제 1회 · 화면 표시(원본 포함) · 기록 없음/확인 불가 · 사람이 운영 DB에 명령 안 함 | 01 · 05 | COVERED | 「리허설 없음」은 UI-SPEC 문구 `리허설 기록 없음 — 첫 리허설 전` |
| REQ | 기준 3 | OPERATIONS 복원 절 · PITR 여부 · 백업 설정 확인 | 03 | COVERED | PITR 끔(가정 A4) |
| REQ | 기준 4 | 두 배지 · 새 칼럼 · sessions 미사용 · 강제 없음 · 사람별 쿼리 없음 · 통합 흐름 | 04 | COVERED | |
| REQ | 기준 5 | 누수 스캔 등록 · 마이그레이션 번호 · 병합 직전 재생성 | 01 · 04 · 05 | COVERED | 번호는 처음부터 0030~0034(조정자 정정) |
| RESEARCH | OQ1 | 복원 대상 선행 생성 | 03 | COVERED | API 정의(v1.d.ts)로 답함, 05에서 실측 |
| RESEARCH | OQ2 | env 덮어쓰기로 임시 인스턴스 연결 | 02 · 05 | COVERED | 거짓 성공 불가 설계 + 실측 |
| RESEARCH | OQ3 | REQUIREMENTS 추적표 OPS-03 | 계획 단계 | COVERED | 이 계획에서 REQUIREMENTS·ROADMAP 수정 |
| RESEARCH | Pitfall 1~5 | 선행 생성 · PITR · IAM 사용자 · OPERATIONS 한도 · journal 번호 | 02 · 03 · 01 · 04 · 05 | COVERED | |
| RESEARCH | A1~A7 | 가정 목록 | 01~05 probe_fallback | COVERED | 실측 대상은 05 Task 3 |
| CONTEXT | D8-07 | 사람 목록 표시(D-08 유지) | 04 | COVERED | |
| CONTEXT | D8-08 | 워크플로 복원 리허설 · 이름 거부 · 사설망 확인 · 결과 기록·표시 · 문서 | 01 · 02 · 03 · 05 | COVERED | |
| CONTEXT | Discretion | 결과 저장 위치 · 칼럼 모양 · 임시 이름·정리 · PITR | 01 · 04 · 03 · 03 | COVERED | DB 한 줄 · first_login_at · run id 이름 + 두 겹 정리 · PITR 끔 |
| CONTEXT | Deferred | 정기 리허설 스케줄 · issue #56 해결 | — | 제외 | 계획에 없음(의도) |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-01-SUMMARY.md` when done
</output>
