---
phase: 04.4-restore-rehearsal-and-login-status
plan: 01
subsystem: database
tags: [drizzle, postgres, cli, system-status, restore-rehearsal]

requires:
  - phase: 04
    provides: getSystemStatus · StatusDeps 주입 · KvList 상태 화면 · account-cli 규약
provides:
  - "restore_rehearsals 표(CHECK 셋 + run_key UNIQUE), 마이그레이션 0017_restore_rehearsals(브랜치 임시 번호)"
  - "insertRestoreRehearsal(ON CONFLICT DO NOTHING + 저장된 결과 재조회) · findLatestRestoreRehearsal(획득 1초 · statement 2초 · lock 1초)"
  - "recordRestoreRehearsal · getLatestRestoreRehearsal — 쓰기·읽기 같은 검증 규칙, ValidationError"
  - "SystemStatus.restoreRehearsal(none | recorded | unavailable), 로그 이벤트 status.restore_rehearsal_unavailable"
  - "formatRestoreRehearsal — 성공·실패(단계)·1분 미만·백업 id 생략·실행 링크"
  - "scripts/restore-rehearsal-cli.ts record — DB 없는 진입점, APP_ENV로 원본, 종료 코드 0/1/2/4, stored_outcome= 줄"
affects: [04.4-02, 04.4-03, 04.4-05, 04.4-06]

actuals:
  tokens: 9500
  tasks: 2
  commits: 3
  plan_head_before: 97dfba7d280403b01cacb4d4b3c4b175ffecdc7b

tech-stack:
  added: []
  patterns:
    - "상태 화면 조회 하나만 공유 pool 설정을 바꾸지 않고 묶는 법: pool.connect()를 타이머와 경주시키고, 지면 늦게 얻을 연결에 release를 걸어 둔다 + drizzle(client).transaction 안 SET LOCAL statement_timeout·lock_timeout"
    - "CLI 진입점은 node: 내장만 정적 import, 인자·APP_ENV 검사 뒤 @/db·@/domain을 동적 import"

key-files:
  created:
    - db/schema/ops.ts
    - db/migrations/0017_restore_rehearsals.sql
    - db/migrations/meta/0017_snapshot.json
    - repositories/restore-rehearsals.ts
    - domain/ops/restore-rehearsal.ts
    - app/(app)/admin/system-status/restore-rehearsal-view.ts
    - scripts/restore-rehearsal-cli.ts
    - test/integration/restore-rehearsal.test.ts
    - test/unit/ops/restore-rehearsal-view.test.ts
  modified:
    - db/schema/index.ts
    - db/migrations/meta/_journal.json
    - domain/system-status/index.ts
    - app/(app)/admin/system-status/page.tsx
    - test/unit/system-status.test.ts

key-decisions:
  - "마이그레이션 번호 0017은 db:generate가 준 그대로(이름·idx 손 편집 없음). 머지 직전 04.4-06 Task 1이 main 위에서 재생성한다"
  - "KST 일시는 hour12:false 대신 hourCycle: \"h23\" — ICU 판에 따라 hour12:false가 자정을 \"24\"로 줄 수 있어 00–23을 고정했다(계획 예시와 다른 유일한 선택, KST 경계 테스트가 지킨다)"
  - "CLI parseArgs는 --failed-stage 값도 restore·verify·cleanup으로 좁힌다(타입을 도메인 입력에 맞추려고). 도메인 검증은 그대로 따로 한다"

requirements-completed: []

duration: 1 session
completed: 2026-09-26
---

# Phase 04.4 Plan 01: 복원 리허설 결과 기록·표시 Summary

**PLAN_BASE:** `97dfba7d280403b01cacb4d4b3c4b175ffecdc7b`

CLI `record` 한 번이 운영 DB 자신의 `restore_rehearsals` 한 줄이 되고, 관리자 시스템 상태 화면 「마지막 백업」 바로 다음 줄 「복원 리허설」에 보인다. 잘못된 입력은 쓰기 전에, 잘못된 행·잠긴 표·고갈된 풀은 읽을 때 그 행만 `확인 불가`로 막힌다.

## Commits

| Task | Commit | 내용 |
|---|---|---|
| 1 트레이서 | `620694e` | 표·마이그레이션 0017 · 리포지토리 · 도메인 · 상태 합성 · 화면 한 줄 · CLI record |
| 2 검증·시간 제한 | `16cc31b` | 쓰기·읽기 검증, 획득 1초 · statement 2초 · lock 1초, 확인 불가 흡수 |
| 2 실패 문구 | `26d351e` | 단계 · 1분 미만 · 백업 id 생략 · 같은 탭 「실행 기록」 링크 |

## TDD

- Task 1: 통합 1~8 · 보기 2 · 상태 합성 2를 먼저 쓰고 빨간 것(모듈·필드 없음)을 확인한 뒤 구현.
- Task 2: 쓰기 거부 18 · CLI 거부 3 · 읽기 검증 2 · 잠금 대기 · 풀 고갈 · 단위 실패 문구 9 · dep throw 1을 먼저 더해 빨간 것(검증 없음, 잠금 테스트는 60초 멈춤)을 확인한 뒤 구현. 「실패인데 URL 없음」 CLI 거부는 Task 1 시점에도 DB CHECK가 막아 이미 녹색이었다(도메인이 이제 쓰기 전에 막는다).

## journal 가드

출처 **없음**. `origin/main`에 `test/unit/db/migration-journal.test.ts`가 없고, 04.1 브랜치(`origin/claude/phase-04.1-plan-1iqtwe-e92rcy` · 실행 브랜치 `origin/claude/execute-phase-04-1`) 어느 커밋에도 없다(`git log -- test/unit/db/migration-journal.test.ts` 빈 결과). 규칙 (c)대로 만들지 않았다. 계획의 `origin/claude/phase-04.1-plan-1iqtwe`라는 정확한 이름의 브랜치는 원격에 없다.

**페이즈 간 제안(Codex #7, 여기서는 적기만 함)**: journal 가드가 생기면 스냅숏 `prevId` 사슬 검사를 함께 두는 것을 조정자에게 제안한다.

## Acceptance 메모

- 새 마이그레이션 파일은 정확히 둘(`0017_restore_rehearsals.sql` · `meta/0017_snapshot.json`), journal 마지막 항목 tag = `0017_restore_rehearsals`.
- **journal 삭제 줄 수가 계획 기대(1)와 달리 0**이다. 앞 항목은 그대로이고 새 항목만 더해졌지만, git diff가 직전 항목의 `}`를 `},`로 바꾼 한 줄이 아니라 `+    },` `+    {` … 로 렌더했기 때문이다(순수 추가). 04.4-06의 재생성 확인은 이 차이를 전제로 삼지 말 것.
- page.tsx 삭제 줄 0 · `target=` 0 · CLI 정적 `@/db|repositories|domain` import 0 · `--source` 0.
- `db/client.ts` · `package.json` · `pnpm-lock.yaml` · `.planning/STATE.md` · 공유 등록부 diff 없음.

## Verification (실제 출력)

- `pnpm typecheck` exit 0 · `pnpm lint` exit 0 · `pnpm lint:sql` 「Found 0 issues in 18 files」
- `pnpm db:generate` → 「No schema changes」
- `pnpm test:unit` → 98 files · 1315 tests 통과
- `pnpm test:integration` → 57 files · 1501 tests 통과(기존 `system-status` 통합 회귀 없음)

## Deviations

- hourCycle `h23`(위 key-decisions). 그 밖에 계획과 다른 구현은 없다.

## 다음

04.4-02 — CLI 번들(`build:cli`)·커넥터 미초기화 단위 테스트·Job 배포.
