---
phase: 04.4-restore-rehearsal-and-login-status
plan: 02
type: execute
wave: 2
depends_on: ["04.4-01"]
files_modified:
  - domain/ops/restore-check-tables.ts
  - domain/ops/restore-verify.ts
  - repositories/restore-verify.ts
  - scripts/restore-rehearsal-cli.ts
  - scripts/build-cli.mjs
  - scripts/deploy.sh
  - test/integration/restore-verify.test.ts
  - test/unit/ops/restore-check-tables.test.ts
  - test/unit/restore-rehearsal-cli.test.ts
  - test/unit/deploy/deploy-sh.test.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "복원본 확인은 GitHub 러너가 아니라 사설망에 닿는 Cloud Run Job `plant8-{env}-restore` 안에서 CLI `verify --target <연결 이름>`으로 한다(D8-08, ROADMAP 04.4 기준 1)"
    - "`verify`는 DB에 한 번이라도 질의하기 전에 대상이 임시 인스턴스인지 확인한다 — `--target`이 `^[a-z0-9-]+:[a-z0-9-]+:plant8-(staging|prod)-rehearsal-\\d+-\\d+$`에 맞고 컨테이너의 `CLOUD_SQL_CONNECTION_NAME`과 같아야 한다. 아니면 종료 코드 1(성공 거짓 보고 없음). 실행 시점 env 덮어쓰기가 안 먹어 원본에 붙었다면 이 비교가 실패해 「검증」 실패로 끝난다"
    - "마이그레이션 버전 일치는 **알려진 접두** 규칙이다: 복원본 `drizzle.__drizzle_migrations`의 적용 개수 n이 1 이상 · 이미지 journal 항목 수 이하이고, 마지막 `created_at`이 journal의 idx n−1 항목 `when`과 정확히 같다. 백업 뒤 배포로 이미지가 더 앞서 있는 것은 정상(개수 차이를 출력만 한다)이고, 복원본이 이미지보다 앞서거나 `when`이 어긋나면 실패다"
    - "핵심 표 확인은 한 곳(`RESTORE_CHECK_TABLES`)의 목록을 순회한다 — 각 표가 읽히는지(`count(*)`)를 보고, `requireRows`인 표는 0행이면 실패다. 운영 현재 행 수와 비교하지 않는다. 뒤 페이즈는 이 목록에 항목만 더한다(CONTEXT 「분리로 생긴 조정」)"
    - "목록의 표 이름은 전부 `db/schema` 배럴의 실제 표이고 중복이 없으며 `requireRows` 표가 하나 이상이다 — 표 이름은 `sql.identifier`로만 SQL에 들어간다"
    - "`deploy.sh`가 다섯 번째 Job `plant8-{env}-restore`(`--command=node,dist/cli/restore-rehearsal-cli.mjs`, 기존 `job_common`·`common_env`·`BETTER_AUTH_SECRET`)를 배포하고 `scripts/build-cli.mjs`가 그 번들을 만든다"
  prohibitions:
    - statement: "복원본이 실제로 읽히지 않았는데 `verify`가 성공(종료 코드 0)을 내서는 안 된다 — 대상 불일치·연결 실패·질의 오류·journal 읽기 실패는 전부 종료 코드 1이다"
      status: resolved
      verification: test
    - statement: "`verify`가 원본 인스턴스를 검증 대상으로 받아들여서는 안 된다 — 대상 이름이 rehearsal 패턴이 아니면 질의 전에 거부한다"
      status: resolved
      verification: test
    - statement: "표 이름을 문자열 이어 붙이기로 SQL에 넣어서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/ops/restore-check-tables.ts"
      provides: "RESTORE_CHECK_TABLES — { table, requireRows } 목록(뒤 페이즈가 덧붙인다)"
      exports: ["RESTORE_CHECK_TABLES"]
    - path: "domain/ops/restore-verify.ts"
      provides: "verifyRestoredDatabase(viewer, journal, deps?) — 마이그레이션 접두 · 표 확인 결과 목록"
      exports: ["verifyRestoredDatabase"]
    - path: "repositories/restore-verify.ts"
      provides: "readAppliedMigrations · countTableRows(sql.identifier)"
      exports: ["readAppliedMigrations", "countTableRows"]
    - path: "test/integration/restore-verify.test.ts"
      provides: "로컬 erp_test를 복원본 대역으로 쓴 verify 트레이서와 실패 경로"
      min_lines: 60
  key_links:
    - from: "scripts/restore-rehearsal-cli.ts"
      to: "domain/ops/restore-verify.ts"
      via: "verify 하위 명령이 대상 가드 뒤 verifyRestoredDatabase를 부르고 결과로 종료 코드를 정한다"
      pattern: "verifyRestoredDatabase"
    - from: "domain/ops/restore-verify.ts"
      to: "domain/ops/restore-check-tables.ts"
      via: "RESTORE_CHECK_TABLES 순회"
      pattern: "RESTORE_CHECK_TABLES"
    - from: "scripts/deploy.sh"
      to: "scripts/build-cli.mjs"
      via: "restore Job의 dist/cli/restore-rehearsal-cli.mjs가 번들 출력 목록에 있다(test/unit/deploy/cli-bundle.test.ts)"
      pattern: "restore-rehearsal-cli"
---

<objective>
**As a** 운영 담당 관리자, **I want to** 복원된 임시 DB가 마이그레이션 버전이 맞고 핵심 표가 읽히며 비어 있지 않은지를 사설망 안에서 자동으로 확인받기를, **so that** 「복원됐다」가 「실제로 쓸 수 있는 데이터로 복원됐다」를 뜻한다.

이 플랜은 D8-08의 「확인 항목은 최소한 마이그레이션 버전 일치와 핵심 표가 읽히고 비어 있지 않은지」와 「복원본 확인은 GitHub 러너가 아니라 사설망에 닿는 곳(Cloud Run Job)」을 만든다. Task 1은 **트레이서**다 — CLI `verify` → 대상 가드 → 도메인 → 리포지토리 → 실제 Postgres(로컬 `erp_test`를 복원본 대역으로)를 관통한다. Task 2는 Job 배포(`deploy.sh`)·번들(`build-cli.mjs`)과 목록·인자 단위 테스트로 넓힌다.

Purpose: ROADMAP 04.4 기준 1의 확인 부분. 실제 GCP 인스턴스 조작은 04.4-03(워크플로·스크립트)이 한다.
Output: 확인 표 목록, verify 도메인·리포지토리·CLI 하위 명령, 다섯 번째 Cloud Run Job 정의.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development`를 **호출**한다. 실패를 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출하고 verify 명령의 실제 출력으로 판정한다. 서브에이전트 프롬프트에도 세 스킬 이름과 `model`을 명시한다.
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적는다(SUMMARY 머리에도).
- 체인 순서(A4): 04.4-01 뒤, 04.4-03 앞. 로컬 DB를 04.4-01과 동시에 쓰지 않는다.
- 이 플랜은 스키마를 바꾸지 않는다 — 새 마이그레이션이 없다.
- `scripts/deploy.sh`는 `deploy_jobs()` 끝에 Job 하나를 **더하기만** 한다. 기존 네 Job과 다른 함수는 바꾸지 않는다. 이 플랜은 아무 환경에도 배포하지 않는다(배포는 `/ship` 병합 뒤 `deploy.yml`).
- 새 의존성 없음. `git push` 하지 않는다. `.planning/STATE.md`를 고치지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 엣지 truth를 이 플랜이 스스로 적었다: **대상 이름 가드가 질의보다 먼저**(truth 2) · **백업 뒤 배포 = 알려진 접두로 통과**(truth 3) · **0행 허용 표와 필수 표의 구분**(truth 4). OPS-03 프로브 행 중 「복원본 확인」 부분을 닫는다.

**계획 가정**
1. RESEARCH Open Question 2(A2 — 실행 시점 `--update-env-vars`만으로 다른 인스턴스에 붙는가): `db/client.ts`는 Cloud Run 소켓 사이드카가 아니라 커넥터 라이브러리(`CLOUD_SQL_CONNECTION_NAME` · IAM · PRIVATE)로 붙고, 런타임 SA는 프로젝트 수준 `cloudsql.client`·`instanceUser`를 가지므로 붙을 것으로 본다. 틀려도 truth 2의 비교가 원본 접속을 「검증」 실패로 바꾸므로 거짓 성공은 없다 — 04.4-05 Task 3의 실제 1회가 실측이다(스테이징 사전 스파이크는 두지 않는다: 기존 인스턴스에 붙는 시험은 원본 인스턴스를 대상으로 삼는 일이라 C2 정신에 어긋나고, 실패해도 안전한 설계라 비용 대비 이득이 없다)
2. A6(0행 오탐): `requireRows = true`는 `seedMasterData`가 채우는 표와 계정이 반드시 있는 `users`만 — users · roles · permission_matrix · visibility_matrix · code_items · org_units · teams. 읽히기만 보면 되는 표 — settings_simple · settings_historized · team_memberships · vendors · action_log. 실행자가 `domain/seed/index.ts`로 이 구분을 대조하고 다르면 SUMMARY에 적고 목록을 고친다
3. A11: 마이그레이션 일치는 `when`(= `created_at`) 비교다. SQL 해시는 비교하지 않는다(이미 적용된 파일을 손으로 고친 이력 — 0003·0004 — 이 거짓 실패를 만들 수 있다)
4. journal은 컨테이너 작업 디렉터리 기준 `db/migrations/meta/_journal.json`(Dockerfile이 `db/migrations`를 이미지에 복사, `scripts/migrate-runner.ts`가 같은 기준을 쓴다)
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — `verify --target` 한 번이 대상 가드 → 마이그레이션 접두 → 핵심 표 확인을 실제 Postgres에서 관통한다</name>
  <files>domain/ops/restore-check-tables.ts, domain/ops/restore-verify.ts, repositories/restore-verify.ts, scripts/restore-rehearsal-cli.ts, test/integration/restore-verify.test.ts</files>
  <precondition>04.4-01이 끝나 `scripts/restore-rehearsal-cli.ts`(record)가 있고 로컬 Postgres가 떠 있다</precondition>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md` 117–240행(아키텍처 다이어그램 · Pattern 1·2 · Anti-Patterns) · 318–340행(확인 표 레지스트리 예시)
    - `scripts/restore-rehearsal-cli.ts`(04.4-01) — 하위 명령 분기와 종료 코드 규약
    - `scripts/migrate-runner.ts` 40–60행 — `resolve(process.cwd(), "db/migrations")`
    - `node_modules/drizzle-orm/pg-core/dialect.js`에서 `__drizzle_migrations`를 만드는 부분(`grep -n "__drizzle_migrations" node_modules/drizzle-orm/pg-core/dialect.js`) — 스키마 `drizzle`, 칼럼 `hash`·`created_at`(bigint)
    - `db/client.ts` 1–40행 — 커넥터 경로와 `env.CLOUD_SQL_CONNECTION_NAME`
    - `domain/seed/index.ts` 98–232행 — 어떤 표가 시드로 채워지는지
    - `test/integration/account-cli.test.ts` 1–60행 — CLI 자식 프로세스 모양
    - `repositories/system-status.ts` — `db.execute(sql…)` + `void viewer`
  </read_first>
  <behavior>
    - 통합 1(트레이서): 마이그레이션·시드된 `erp_test`에서 도메인 `verifyRestoredDatabase(SYSTEM_VIEWER, journal)`을 불러 ok true, 마이그레이션 항목 「적용 N개 = journal N개」, 표 항목 전부 ok를 받는다. **한계(명시)**: 로컬에서는 CLI 성공 경로를 돌릴 수 없다 — env에 `CLOUD_SQL_CONNECTION_NAME`을 주면 `db/client.ts`가 커넥터를 만들기 때문이다. 그래서 트레이서는 CLI 가드(통합 4) + 도메인·리포지토리·실제 Postgres(통합 1)로 나뉘어 관통하고, CLI 성공 경로 전체는 04.4-05 Task 3의 실제 1회가 증명한다
    - 통합 2: `requireRows` 표 하나(`org_units`)를 비우면 ok false이고 그 표 항목만 실패다
    - 통합 3: journal을 한 항목 더 붙인 사본으로 넘기면(이미지가 앞선 상황) ok true이고 「이미지보다 1개 뒤」가 출력된다; journal 마지막 항목을 떼어 넘기면(복원본이 앞선 상황) 마이그레이션 항목 실패; 마지막 `when`을 1 바꾼 사본이면 실패
    - 통합 4: CLI `verify --target a:b:plant8-staging-rehearsal-1-1`을 env `CLOUD_SQL_CONNECTION_NAME` 없이 돌리면 종료 코드 1이고 「검증 대상이 임시 인스턴스가 아닙니다」 계열 문구가 나오며 DB 질의가 없다; `--target a:b:plant8-staging-db`도 종료 코드 1
    - 단위(도메인 deps 주입): `readAppliedMigrations`가 throw하면 ok false(예외가 밖으로 새지 않고 실패 항목이 된다)
  </behavior>
  <action>
① 실패 테스트 먼저: `test/integration/restore-verify.test.ts`에 behavior 통합 1~4를 쓴다(journal은 실제 `db/migrations/meta/_journal.json`을 읽어 사본을 조작한다). 실패를 확인한다.

② `domain/ops/restore-check-tables.ts`: `RESTORE_CHECK_TABLES`를 `{ table: string; requireRows: boolean }`의 읽기 전용 배열로 export한다 — probe_fallback 가정 2의 열두 표. 파일 머리 주석: 「뒤 페이즈는 이 목록에 항목만 더한다 · 운영 현재 행 수와 비교하지 않는다(D8-08)」.

③ `repositories/restore-verify.ts`: `readAppliedMigrations(viewer)` → `{ count, lastCreatedAt }`(`drizzle.__drizzle_migrations`의 `count(*)`와 `max(created_at)`, 숫자로 변환) · `countTableRows(viewer, table)` → 숫자(`sql.identifier(table)`로만 이름을 넣는다). 둘 다 viewer 첫 인자.

④ `domain/ops/restore-verify.ts`: `verifyRestoredDatabase(viewer, journal, deps?)` → `{ ok, checks: { name, ok, detail }[] }`. 마이그레이션 항목은 truth 3의 알려진 접두 규칙(n ≥ 1 · n ≤ journal 길이 · `lastCreatedAt === journal.entries[n-1].when`), detail에 적용 개수·마지막 tag·이미지와의 차이를 적는다. 표 항목은 `RESTORE_CHECK_TABLES` 순회(읽기 실패 → 실패 · `requireRows`인데 0 → 실패 · 그 밖 통과, detail에 행 수). 각 질의의 예외는 그 항목의 실패로 바꾸고 밖으로 던지지 않는다. `ok`는 모든 항목이 ok일 때만 true.

⑤ CLI `verify` 하위 명령(`scripts/restore-rehearsal-cli.ts`에 **더한다**, `record`는 그대로): `--target <연결 이름>` 필수. 순서 — (a) truth 2의 정규식과 `env.CLOUD_SQL_CONNECTION_NAME` 일치 확인, 어기면 종료 코드 1(`UsageError`가 아니라 검증 실패 — 워크플로가 단계 「검증」으로 기록해야 하므로) · (b) `resolve(process.cwd(), "db/migrations/meta/_journal.json")` 읽기(실패 → 1) · (c) `verifyRestoredDatabase(SYSTEM_VIEWER, journal)` · (d) 항목마다 한 줄(「통과」/「실패」 + detail)을 stdout에 찍고 마지막 줄 「복원본 확인을 통과했습니다」 또는 「복원본 확인에 실패했습니다」 · (e) ok면 0, 아니면 1. 출력 문구는 「…니다」로 끝낸다.

⑥ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: verify restored database from a cloud run job`, 본문 한국어(D8-08 확인 항목).
  </action>
  <verify>
    <automated>pnpm vitest run --project integration test/integration/restore-verify.test.ts</automated>
    <fails_when>정상 복원본 통과 · 필수 표 0행 실패 · 이미지 앞섬 통과 · 복원본 앞섬/when 어긋남 실패 · 대상 가드 종료 코드 1 중 하나가 어긋난다</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>타입 오류 또는 repository-viewer-param · no-row-type-escape · boundaries 위반이 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "sql.raw" repositories/restore-verify.ts`가 0이고 `grep -c "sql.identifier" repositories/restore-verify.ts`가 1 이상이다
    - `scripts/restore-rehearsal-cli.ts`에서 대상 가드 코드가 `verifyRestoredDatabase` 호출보다 앞선 줄에 있다
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>로컬 Postgres를 복원본 대역으로 `verify`의 판정(통과·필수 표 0행·버전 접두·대상 거부)이 전부 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Job·번들·목록 단위 테스트 — `plant8-{env}-restore` 배포 정의와 `RESTORE_CHECK_TABLES` 실재 확인</name>
  <files>scripts/deploy.sh, scripts/build-cli.mjs, test/unit/deploy/deploy-sh.test.ts, test/unit/ops/restore-check-tables.test.ts, test/unit/restore-rehearsal-cli.test.ts</files>
  <read_first>
    - `scripts/deploy.sh` 300–360행(`deploy_jobs` — `job_common` · `common_env` · account Job 주석과 `--command=node,dist/cli/account-cli.mjs`)
    - `test/unit/deploy/deploy-sh.test.ts` 380–420행(「Job 4개 모두 …」 `toHaveLength(4)`와 Job별 단언)
    - `scripts/build-cli.mjs` 1–30행(`entryPoints` · `outputs`)
    - `test/unit/deploy/cli-bundle.test.ts` — deploy.sh의 CLI 경로가 번들 출력에 있어야 한다
    - `test/integration/setup.ts` 9–15행 — 배럴의 `PgTable`을 모으는 방법(`is(value, PgTable)` · `getTableName`)
  </read_first>
  <behavior>
    - 목록 1: 모든 `table`이 `db/schema` 배럴의 실제 표 이름 집합에 있다
    - 목록 2: 표 이름이 중복되지 않고, 배열이 비어 있지 않으며, `requireRows` true가 하나 이상이다
    - CLI 1: `verify`에 `--target` 없음 · 모르는 플래그 → `UsageError`(종료 코드 2), 값 있는 `--target`은 파싱 성공
    - deploy 1: `deploy_jobs`의 `gcloud run jobs deploy` 줄이 다섯 개이고, restore Job은 `--command=node,dist/cli/restore-rehearsal-cli.mjs` · `common_env` · `BETTER_AUTH_SECRET` 시크릿을 갖고 `DB_ADMIN_PASSWORD`는 없다
  </behavior>
  <action>
① 실패 테스트 먼저: `test/unit/ops/restore-check-tables.test.ts`(목록 1·2, 배럴의 표 이름 모으기는 `test/integration/setup.ts` 방식), `test/unit/restore-rehearsal-cli.test.ts`에 CLI 1을 더한다, `test/unit/deploy/deploy-sh.test.ts`의 「Job 4개 모두 …」 테스트의 개수 단언을 4 → 5로 바꾸고(테스트 이름의 개수도 맞춘다) restore Job 단언(deploy 1)을 **더한다**. 실패를 확인한다.

② `scripts/deploy.sh` `deploy_jobs()` 끝(account Job 다음)에 `run gcloud run jobs deploy "$(job_name "$ENV" restore)"`를 더한다 — `"${job_common[@]}"` · `--command=node,dist/cli/restore-rehearsal-cli.mjs` · `--set-env-vars="$common_env"` · `--set-secrets="BETTER_AUTH_SECRET=${better_auth_secret}:latest"`. 바로 위에 주석 두 줄: 「account Job과 같이 스크립트 경로를 --command 둘째 항목에 둔다 — restore-rehearsal.yml이 execute --args로 record/verify를 넘긴다(D8-08). verify 실행만 CLOUD_SQL_CONNECTION_NAME을 임시 인스턴스로 덮어쓴다」. `infra/names.sh` `job_name` 주석의 목록에 restore를 더하는 것은 하지 않는다(요청 밖 수정 금지 — SUMMARY에 언급만).

③ `scripts/build-cli.mjs`의 `entryPoints`에 `scripts/restore-rehearsal-cli.ts`, `outputs`에 `dist/cli/restore-rehearsal-cli.mjs`를 끝에 더한다. 머리 주석의 「Job 4개」는 「Job 5개(…·restore)」로 맞춘다.

④ `pnpm build:cli`로 번들이 만들어지고 next를 끌어오지 않는지(스크립트 자체 검사) 확인한다. 커밋 제목 예 `feat: deploy restore cloud run job and bundle its cli`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ops/restore-check-tables.test.ts test/unit/restore-rehearsal-cli.test.ts test/unit/deploy/deploy-sh.test.ts test/unit/deploy/cli-bundle.test.ts</automated>
    <fails_when>목록에 없는 표·중복이 있거나, CLI 인자 규약이 깨졌거나, deploy.sh Job 수·restore Job 인자가 어긋나거나, 번들 출력 목록에 restore CLI가 없다</fails_when>
    <automated>pnpm build:cli</automated>
    <fails_when>esbuild 번들이 실패하거나 빌드 스크립트의 next 유입 검사가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "gcloud run jobs deploy" scripts/deploy.sh`가 PLAN_BASE보다 정확히 1 많다
    - `D=$(git diff "$PLAN_BASE"..HEAD -- scripts/deploy.sh) && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 0을 출력한다(삭제 줄 없음 — 더하기만)
    - `ls dist/cli/restore-rehearsal-cli.mjs`가 성공한다(빌드 산출물, 커밋하지 않음 — `.gitignore` 확인)
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>다섯 번째 Job과 그 번들이 정의되고, 확인 표 목록이 실제 스키마와 맞는다는 것이 테스트로 고정된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 워크플로 → Job 실행 인자·env 덮어쓰기 | `--target`과 `CLOUD_SQL_CONNECTION_NAME`이 어느 인스턴스를 읽을지 정한다 |
| Job → 임시 Cloud SQL(사설 IP, IAM) | 런타임 SA가 프로젝트 수준 권한으로 붙는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-06 | Spoofing | `verify` 대상 | high | mitigate | 질의 전에 `--target`이 rehearsal 패턴이고 env 연결 이름과 같은지 확인, 어기면 종료 코드 1 — 원본에 붙어도 성공으로 보고할 수 없다(Task 1 통합 4) |
| T-04.4-07 | Repudiation | 거짓 성공 | high | mitigate | 모든 질의 예외·journal 읽기 실패가 실패 항목이 되고 `ok`는 전 항목 통과일 때만 true(Task 1 통합 2·3·단위) |
| T-04.4-08 | Tampering | 표 이름 SQL 삽입 | medium | mitigate | 이름은 코드 상수 목록에서만 오고 `sql.identifier`로만 들어간다. 목록이 실제 스키마 표인지 단위 테스트가 확인한다 |
| T-04.4-09 | Information Disclosure | verify 출력 | low | accept | 출력은 표 이름·행 수·마이그레이션 tag뿐이다(행 내용·비밀 없음). Actions 로그에 남아도 된다 |
| T-04.4-10 | Elevation of Privilege | restore Job의 권한 | low | accept | 기존 런타임 SA·기존 시크릿(`BETTER_AUTH_SECRET`)만 쓰고 `DB_ADMIN_PASSWORD`는 주지 않는다(deploy 1 단언). 새 IAM 부여 없음 |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- `pnpm test:unit` 전체 통과(deploy-sh · cli-bundle · import-cycles 포함)
- `pnpm test:integration` 전체 통과
- `pnpm build:cli` 성공
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml db/migrations .planning/STATE.md` 비어 있음
</verification>

<success_criteria>
- `verify`가 복원본을 실제로 읽었을 때만 성공한다
- 백업 뒤 배포는 통과, 복원본 이상은 실패로 가른다
- 확인 표 목록이 한 곳에 있고 실제 스키마와 맞는다
- 다섯 번째 Job과 번들이 기존 네 Job과 같은 모양으로 정의된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 목록 | `RESTORE_CHECK_TABLES`(`domain/ops/restore-check-tables.ts`) |
| 함수 | `verifyRestoredDatabase` · `readAppliedMigrations` · `countTableRows` |
| CLI | `scripts/restore-rehearsal-cli.ts` `verify --target` |
| Cloud Run Job | `plant8-{env}-restore`(`scripts/deploy.sh` `deploy_jobs`) |
| 번들 | `dist/cli/restore-rehearsal-cli.mjs`(`scripts/build-cli.mjs`) |
| 테스트 | `test/integration/restore-verify.test.ts` · `test/unit/ops/restore-check-tables.test.ts` · `test/unit/restore-rehearsal-cli.test.ts`(확장) · `test/unit/deploy/deploy-sh.test.ts`(4 → 5) |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-02-SUMMARY.md` when done
</output>
