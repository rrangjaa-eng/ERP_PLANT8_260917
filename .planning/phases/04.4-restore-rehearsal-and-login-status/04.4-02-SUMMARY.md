---
phase: 04.4-restore-rehearsal-and-login-status
plan: 02
subsystem: infra
tags: [cloud-run-job, cli, drizzle, postgres, restore-rehearsal, esbuild]

requires:
  - phase: 04.4-01
    provides: scripts/restore-rehearsal-cli.ts record — node: 내장만 정적 import하는 DB 없는 진입점, 종료 코드 규약
provides:
  - "RESTORE_CHECK_TABLES — { table, requireRows } 12개(뒤 페이즈는 항목만 더한다)"
  - "verifyRestoredDatabase(viewer, journal, deps?) — 마이그레이션 접두 전체 비교 + 핵심 표 확인, 예외는 항목 실패"
  - "readAppliedMigrations(id 순 created_at 수열) · countTableRows(sql.identifier)"
  - "CLI verify --target <연결 이름> — rehearsal 패턴 + CLOUD_SQL_CONNECTION_NAME 일치 가드가 DB 모듈 로드 전, 통과 0 · 실패 1 · 사용 오류 2"
  - "Cloud Run Job plant8-{env}-restore(--command=node,dist/cli/restore-rehearsal-cli.mjs) · 번들 dist/cli/restore-rehearsal-cli.mjs"
affects: [04.4-03, 04.4-06]

actuals:
  tokens: 6800
  tasks: 2
  commits: 2
  plan_head_before: 37ecc193ec37079b1218f52ddee89857b237e359

tech-stack:
  added: []
  patterns:
    - "CLI 대상 가드는 검증 실패(종료 코드 1, TargetRejectedError)로 — 사용 오류(2)와 구분해 워크플로가 단계 「검증」으로 기록한다"
    - "도메인 단위 테스트는 vi.mock으로 리포지토리 모듈을 막고 deps를 주입한다(db/client 미로드)"

key-files:
  created:
    - domain/ops/restore-check-tables.ts
    - domain/ops/restore-verify.ts
    - repositories/restore-verify.ts
    - test/integration/restore-verify.test.ts
    - test/unit/ops/restore-verify.test.ts
    - test/unit/ops/restore-check-tables.test.ts
    - test/unit/restore-rehearsal-cli.test.ts
  modified:
    - scripts/restore-rehearsal-cli.ts
    - scripts/deploy.sh
    - scripts/build-cli.mjs
    - test/unit/deploy/deploy-sh.test.ts

key-decisions:
  - "settings_simple·settings_historized를 requireRows true로 올림(플랜 가정 2와 다름) — seedMasterData가 기본값이 있는 설정 키를 두 표 모두에 항상 채우고, 삭제 경로는 미래 적용분(deleteFutureHistorizedValue)뿐이라 시드된 기본 행이 남는다"
  - "verify 인자는 --target 하나만(다른 플래그 · 여분 토큰은 UsageError 2)"

patterns-established:
  - "복원본 확인 표는 RESTORE_CHECK_TABLES 한 곳 — 목록이 db/schema 실제 표만 담는지 단위 테스트가 고정"

requirements-completed: [OPS-03]

coverage:
  - id: D1
    description: "마이그레이션 접두 전체 비교(중복·빠짐·중간 바뀜·0개·복원본 앞섬 실패, 이미지 앞섬 통과) · 예외 흡수"
    requirement: OPS-03
    verification:
      - kind: unit
        ref: "test/unit/ops/restore-verify.test.ts"
        status: pass
      - kind: integration
        ref: "test/integration/restore-verify.test.ts"
        status: pass
  - id: D2
    description: "핵심 표 확인 — 필수 표 0행이면 그 항목만 실패, 픽스처 사용자가 있는 정상 복원본 통과"
    requirement: OPS-03
    verification:
      - kind: integration
        ref: "test/integration/restore-verify.test.ts"
        status: pass
  - id: D3
    description: "verify 대상 가드가 DB 모듈 로드 전 — 원본·불일치 대상 거부 시 db/client 미로드"
    requirement: OPS-03
    verification:
      - kind: unit
        ref: "test/unit/restore-rehearsal-cli.test.ts"
        status: pass
      - kind: other
        ref: "pnpm build:cli 뒤 번들 스모크(원본 연결 이름·원본 대상)"
        status: pass
  - id: D4
    description: "다섯 번째 Job plant8-{env}-restore 배포 정의와 번들"
    requirement: OPS-03
    verification:
      - kind: unit
        ref: "test/unit/deploy/deploy-sh.test.ts#Job 5개 모두"
        status: pass
  - id: D5
    description: "RESTORE_CHECK_TABLES가 실제 스키마 표·중복 없음·requireRows 하나 이상"
    requirement: OPS-03
    verification:
      - kind: unit
        ref: "test/unit/ops/restore-check-tables.test.ts"
        status: pass
---

# Phase 04.4 Plan 02: 복원본 확인(verify)과 restore Job Summary

PLAN_BASE: `37ecc193ec37079b1218f52ddee89857b237e359`

**복원 리허설 CLI `verify --target`이 임시 인스턴스만 받아 마이그레이션 접두 전체 비교와 핵심 표 12개 확인을 하고, 다섯 번째 Cloud Run Job `plant8-{env}-restore`와 그 번들을 정의했다.**

## Accomplishments
- 대상 가드(rehearsal 이름 패턴 + 컨테이너 연결 이름 일치)가 DB 모듈 로드 전에 돈다. 단위 테스트(모듈 미로드 표시)와 번들 스모크로 증명했다
- `__drizzle_migrations.created_at` 수열을 journal `when` 접두와 원소별로 비교한다. 이미지가 앞선 것은 「이미지보다 N개 뒤」로 통과한다
- 표 확인은 `RESTORE_CHECK_TABLES`를 돌며 `sql.identifier`로만 이름을 넣는다. 질의 예외는 그 항목의 실패가 된다
- `deploy.sh`에 restore Job을 더했다(삭제 줄 0, `gcloud run jobs deploy` 4 → 5). `build-cli.mjs` 출력에 번들을 더했다

## Task Commits
1. **Task 1: 트레이서 — verify 가드 → 접두 → 표 확인** - `49accb3` (feat)
2. **Task 2: Job·번들·목록 단위 테스트** - `b7b0519` (feat)

## TDD
- Task 1 RED: 도메인·통합은 모듈 없음, CLI는 `verify` 미지원(5 실패)을 확인한 뒤 구현
- Task 2 RED: deploy-sh「Job 5개」가 4개로 실패. `restore-check-tables.test.ts`는 목록이 Task 1에 이미 있어 처음부터 초록이다(목록 고정용 회귀 테스트)

## 번들 스모크(Task 2 acceptance)
```
$ CLOUD_SQL_CONNECTION_NAME=p:r:plant8-prod-db APP_ENV=prod node dist/cli/restore-rehearsal-cli.mjs verify --target p:r:plant8-prod-db
검증 대상이 임시 인스턴스가 아닙니다.
exit=1
```
커넥터·인증·연결 오류 줄 없음. `dist/cli/restore-rehearsal-cli.mjs`는 `.gitignore` 대상(커밋 안 함).

## 검증
- `pnpm typecheck` · `pnpm lint` exit 0
- `pnpm test:unit` 101 파일 · 1337 테스트 통과
- `pnpm test:integration` 58 파일 · 1506 테스트 통과
- `pnpm build:cli` 성공(next 유입 검사 포함)
- `git diff --stat 37ecc19..HEAD -- package.json pnpm-lock.yaml db/migrations .planning/STATE.md .planning/HANDOFF.json` 비어 있음

## Deviations from Plan
- **목록 조정(플랜이 허용한 대조):** `settings_simple`·`settings_historized`를 requireRows로 올렸다. 근거는 key-decisions에 있다

## 독립 검토
Codex 한도(2026-09-29 오전까지) 때문에 Opus 독립 검토로 대신했다. blocking 없음. **한도가 풀리면 Codex로 다시 확인해야 한다.** 참고 사항:
- 가드 정규식은 도메인 범위 프로젝트 ID(`example.com:proj`)를 거부한다. 안전한 쪽(실패)이고 현재 프로젝트 ID 모양에는 해당하지 않는다
- 범위 밖 오래된 주석(언급만, 고치지 않음): `scripts/deploy.sh` 302행 「세 Job 모두」, `infra/names.sh` 31행 `job_name` 목록에 restore가 빠짐

## Next Phase Readiness
- 04.4-03(워크플로)은 `gcloud run jobs execute plant8-{env}-restore --args=verify,--target,<연결 이름> --update-env-vars=CLOUD_SQL_CONNECTION_NAME=<연결 이름>`으로 부른다
- 한계: CLI 성공 경로 전체(실제 임시 인스턴스 접속)는 04.4-06 Task 3의 실제 1회가 증명한다

## Self-Check: PASSED
