---
phase: 04.4-restore-rehearsal-and-login-status
plan: 03
type: execute
wave: 3
depends_on: ["04.4-02"]
files_modified:
  - scripts/restore-rehearsal.sh
  - test/unit/deploy/restore-fakebin/gcloud
  - test/unit/deploy/restore-fakebin/sleep
  - test/unit/deploy/restore-rehearsal-sh.test.ts
  - .github/workflows/restore-rehearsal.yml
  - test/unit/deploy/restore-rehearsal-workflow.test.ts
  - docs/OPERATIONS.md
  - docs/RESTORE.md
  - test/unit/docs-limits.test.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`workflow_dispatch` 워크플로 `restore-rehearsal.yml` 하나(WIF, `account.yml` 패턴)가 원본의 최신 성공 자동 백업을 새 임시 Cloud SQL 인스턴스로 복원하고, 사설망 Job(`plant8-{env}-restore` `verify`)으로 확인하고, 임시 인스턴스를 지운 뒤, 마지막 정리 시도 **뒤에** 같은 Job의 `record`로 결과를 그 환경 자신의 DB에 한 번 남긴다(D8-08, ROADMAP 04.4 기준 1·2)"
    - "임시 인스턴스 이름은 워크플로가 만든다: `plant8-<env>-rehearsal-<GITHUB_RUN_ID>-<GITHUB_RUN_ATTEMPT>`. 이름이 `^plant8-(staging|prod)-rehearsal-[0-9]+-[0-9]+$`에 맞지 않거나, 환경 접두가 다르거나, `plant8-staging-db`·`plant8-prod-db`·원본 이름과 같으면 **gcloud 명령을 하나도 실행하기 전에** 실패한다"
    - "스크립트가 실행하는 모든 변경 명령(인스턴스 생성·백업 복원·사용자 생성·인스턴스 삭제)의 대상 인스턴스는 임시 이름 하나뿐이다 — 원본은 백업 목록 조회·디스크 크기 조회·`--backup-instance`로만 나온다"
    - "임시 인스턴스의 부재는 **정확한 이름으로 한 목록 조회가 성공하고 결과가 비었을 때만** 확인된 것이다. 조회·삭제의 다른 실패(권한 403 · 일시 오류 · 인증 만료)는 「알 수 없음」이고, 그 실행의 결과는 `cleanup` 실패이며 요약은 「남은 임시 인스턴스 확인 불가」다 — 절대 0으로 적지 않는다(Codex #2)"
    - "삭제 전에 그 인스턴스의 끝나지 않은 Cloud SQL 작업을 기다리고, 삭제 뒤 재조회로 부재를 확인하며, 부재가 확인될 때까지 최대 3번 시도한다. 생성·복원 뒤 RUNNABLE 폴링(10초 × 90)이 다 떨어지면 명시적 실패다. 정리는 두 곳에서 한다 — rehearse의 EXIT trap(1차)과 워크플로 `always()` 단계의 `finalize`(마지막 시도). 정리 시간은 따로 떼어 둔다: rehearse 단계 60분 · finalize 단계 25분 · 잡 90분(Codex #3)"
    - "rehearse는 무엇을 만들기 전에 그 환경의 `plant8-<env>-rehearsal-` 인스턴스를 조회한다 — 하나라도 있거나 조회가 실패하면 아무것도 만들지 않고 단계 `cleanup` 실패로 끝나며, 요약에 남은 이름(또는 「확인 불가」)과 `docs/RESTORE.md`의 정리 절차를 적는다(고아 점검, Codex #3)"
    - "결과 기록은 `finalize` 하나만 한다: 실행 id + 시도로 키가 붙은 상태 파일(`$RUNNER_TEMP/restore-rehearsal-<RUN_ID>-<ATTEMPT>.state`)을 읽어 마지막 정리 시도 뒤에 결과를 정하고 record Job을 **한 번만** 부른다 — 다시 돌려도 두 번 기록하지 않는다. EXIT trap은 기록하지 않는다. 상태 파일이 없으면 정리만 하고 기록 없이 종료 코드 1(「기록하지 않았습니다」), 기록 자체가 실패하면 종료 코드 1(「기록 실패」)이다(Codex #4)"
    - "결과 단계는 우선순위 하나로 정한다: **정리 > 복원 > 검증**(UI-SPEC 결과의 정의, Codex 2차 #2). 임시 부재가 확인되지 않으면 다른 단계가 먼저 실패했어도 `cleanup`, 아니면 restore(백업 없음·생성·복원·IAM 사용자 보정·폴링 소진)가 실패했으면 `restore`, 아니면 verify(확인 실패 · verify 전후 이미지 변경·읽기 불가)가 실패했으면 `verify`. 한 실행은 단계 하나만 기록한다. 성공은 복원 완료 AND 확인 통과 AND 임시 부재 확인 AND 이미지 불변일 때만이다"
    - "verify 직전에 `plant8-<env>-restore` Job의 이미지 참조와 그 digest를 읽어 상태 파일에 적고, finalize가 기록 직전에 다시 읽어 비교한다 — 다르거나 읽을 수 없으면 성공이 아니다. 요약에 `이미지 <참조>@<digest>`를 적는다(증거). 배포와 동시성 그룹을 나누지 않는 이유는 GitHub가 같은 그룹의 대기 중 실행을 새 실행으로 바꿔 대기 중이던 배포를 취소할 수 있기 때문이다(Codex #12)"
    - "production은 입력 `confirm_production`에 `plant8-prod-db`를 그대로 적어야만 돈다 — 워크플로 첫 단계(체크아웃·인증 전)와 스크립트 공통 준비에서 확인한다. 스크립트 규칙은 하나다: **gcloud를 부르는 모든 하위 명령(`rehearse` · `finalize`)이 첫 gcloud 전에 같은 공통 준비(환경 · 실행 id · 임시 이름 가드 · production 확인)를 거친다**; `guard`는 gcloud를 부르지 않는다. 워크플로는 `refs/heads/main`에서만 돌고 schedule·push·pull_request 트리거가 없다(C1)"
    - "위 main·확인 입력 검사는 **실수 방지 장치이지 권한 경계가 아니다** — WIF는 저장소로만 묶여 있고(`scripts/bootstrap-gcp.sh`) 배포 계정은 `roles/cloudsql.admin`을 가지므로, 실제 경계는 1인 소유 비공개 저장소의 write 권한이다. 이 잔여 위험을 수용하고 이 페이즈는 IAM을 바꾸지 않는다(Codex #1, Phase 8 후속: 리허설 전용 최소 권한 SA · WIF ref/workflow 속성 조건)"
    - "`docs/OPERATIONS.md` §14(머리글 포함 20줄 이하)가 백업 설정 · PITR 꺼짐 · 리허설 실행 · 결과 읽기를 담고, 고아 정리와 실제 사고 복원은 `docs/RESTORE.md`로 링크한다. `docs/RESTORE.md`의 사고 복원은 쓰기 중단(공개 접근 제거 · 스케줄러 일시정지 · 운영 워크플로 중지 · 실행 중 Job 대기) → 백업 선택 → 복원 → 정상 배포 경로로 migrate·seed(= 이미지·스키마 호환, 여기서 공개 접근이 다시 열린다) → 확인 → 스케줄러 재개·공지 → 백업 설정 재확인 순서다(Codex #9, ROADMAP 04.4 기준 3, OPS-07)"
  prohibitions:
    - statement: "원본 인스턴스(`plant8-staging-db`·`plant8-prod-db`)로 복원하거나, 원본을 수정·삭제하는 명령이 스크립트에 있어서는 안 된다(C2)"
      status: resolved
      verification: test
    - statement: "production 리허설이 사람의 명시적 확인 없이 돌아서는 안 된다 — 스케줄·push·PR 트리거, 확인 입력 없는 실행, main 밖 실행이 전부 막힌다(C1, 실수 방지 수준 — truth 11)"
      status: resolved
      verification: test
    - statement: "임시 인스턴스가 조용히 남아서는 안 된다 — 남거나 알 수 없으면 결과가 `cleanup` 실패이고 요약에 이름 또는 「확인 불가」가 보인다"
      status: resolved
      verification: test
    - statement: "조회 실패를 「없음」으로 취급해서는 안 된다 — 요약의 「남은 임시 인스턴스 0」은 목록 조회가 성공했을 때만 나온다"
      status: resolved
      verification: test
    - statement: "finalize 밖에서 결과를 기록하거나, 한 실행·시도에 두 번 기록해서는 안 된다"
      status: resolved
      verification: test
    - statement: "워크플로 `run:` 블록 안에 `${{ … }}` 식을 직접 넣어서는 안 된다 — 입력은 전부 `env:`로 넘긴다(T-1-32)"
      status: resolved
      verification: test
    - statement: "OPERATIONS.md·RESTORE.md에 실제 프로젝트 ID·프로젝트 번호·12자리 이상 숫자(예시 백업 id 포함)를 적어서는 안 된다(D-03)"
      status: resolved
      verification: test
  artifacts:
    - path: "scripts/restore-rehearsal.sh"
      provides: "guard · rehearse · finalize 하위 명령(set -Eeuo pipefail, infra/names.sh, 상태 파일)"
      contains: "rehearsal"
    - path: ".github/workflows/restore-rehearsal.yml"
      provides: "workflow_dispatch(env · confirm_production) + WIF + 스크립트 위임 + always() finalize"
      contains: "workflow_dispatch"
    - path: "test/unit/deploy/restore-rehearsal-sh.test.ts"
      provides: "가짜 gcloud로 행복 경로 호출 순서 · 가드 · 고아 점검 · 실패 단계 · 알 수 없는 정리 · 작업 대기·재시도 · 중단 뒤 finalize · 한 번 기록 · 이미지 변경 증명"
      min_lines: 200
    - path: "test/unit/deploy/restore-rehearsal-workflow.test.ts"
      provides: "워크플로 YAML 텍스트 단언"
      min_lines: 40
    - path: "docs/OPERATIONS.md"
      provides: "§14 백업·복원(20줄 이하, RESTORE.md 링크)"
      contains: "restore-rehearsal.yml"
    - path: "docs/RESTORE.md"
      provides: "리허설 실행·결과 읽기 · 고아 임시 인스턴스 찾기·지우기 · 사고 때 사람이 하는 복원 런북"
      contains: "remove-iam-policy-binding"
  key_links:
    - from: ".github/workflows/restore-rehearsal.yml"
      to: "scripts/restore-rehearsal.sh"
      via: "rehearse 단계와 always() finalize 단계가 스크립트에 위임"
      pattern: "restore-rehearsal.sh"
    - from: "scripts/restore-rehearsal.sh"
      to: "scripts/restore-rehearsal-cli.ts"
      via: "gcloud run jobs execute plant8-{env}-restore --args=verify,… (env 덮어쓰기, rehearse) / --args=record,… (덮어쓰기 없음, finalize만)"
      pattern: "jobs execute"
    - from: "docs/OPERATIONS.md"
      to: "docs/RESTORE.md"
      via: "§14가 고아 정리·사고 복원을 RESTORE.md로 링크"
      pattern: "RESTORE.md"
    - from: "scripts/restore-rehearsal.sh"
      to: "infra/names.sh"
      via: "sql_instance · job_name · DB_TIER · DB_VERSION · NETWORK"
      pattern: "names.sh"
---

<objective>
**As a** 운영 담당 관리자, **I want to** GitHub Actions에서 버튼 하나로 최신 자동 백업을 임시 인스턴스에 복원·확인·삭제하고 결과를 남기기를, **so that** 운영 DB를 건드리지 않고 「백업이 실제로 복원된다」를 증명하고, 사고 때 무엇을 해야 하는지 문서로 안다.

이 플랜은 D8-08의 워크플로 본체와 OPS-03의 복원 절차 문서를 만든다. Task 1은 **트레이서**다 — 스크립트 `rehearse` 한 번(고아 점검 → 백업 조회 → 임시 생성 → 복원 → IAM 사용자 보정 → 이미지 고정 → verify Job → EXIT 정리)과 이어지는 `finalize` 한 번(마지막 정리 → 이미지 재확인 → record Job 한 번 → 요약)을 가짜 gcloud 위에서 끝까지 관통한다. Task 2는 실패·중단·알 수 없는 정리·작업 대기·한 번 기록·이미지 변경·production 확인 경로로 넓힌다. Task 3은 워크플로 YAML과 문서(`docs/OPERATIONS.md` §14 · 새 `docs/RESTORE.md`)다.

Purpose: ROADMAP 04.4 기준 1(워크플로·이름 거부·사설망 확인·확인 실패에도 삭제)과 기준 3(문서). 실제 GCP 실행은 04.4-06 Task 3.
Output: `scripts/restore-rehearsal.sh`, `.github/workflows/restore-rehearsal.yml`, 전용 가짜 gcloud와 테스트, OPERATIONS §14, `docs/RESTORE.md`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-02-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development`를 **호출**한다. 각 task는 자기가 구현하는 behavior의 테스트를 먼저 쓰고 빨간 것을 본 뒤 구현한다(Codex #20) — Task 1은 행복 경로만 구현하고, 실패·정리 분기는 Task 2의 빨간 테스트 뒤에 구현한다. 실패를 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름과 `model`을 명시한다.
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적는다.
- **세션은 gcloud를 직접 쓰지 않는다**(CONTEXT 이미 확정된 입력). 이 플랜의 모든 테스트는 `test/unit/deploy/restore-fakebin/`의 가짜 gcloud로만 돈다 — 실제 GCP·스테이징·프로덕션에 닿는 명령을 실행하지 않는다. 가짜 gcloud는 처리하지 않는 호출에 종료 코드 1을 낸다(기존 `test/unit/deploy/fakebin/gcloud` 규약).
- 공유 가짜 `test/unit/deploy/fakebin/gcloud`는 고치지 않는다 — 전용 가짜를 새로 둔다(기존 deploy·rollback 테스트에 영향 없음).
- 이 플랜은 DB를 쓰지 않는다(단위 테스트만). 그래도 체인 순서(04.4-02 뒤, 04.4-04 앞)를 지킨다.
- `docs/OPERATIONS.md`는 지금 248줄, 상한 300줄이다. 04.2·04.3·Phase 8도 이 파일에 절을 더하므로 이 플랜은 **§14를 머리글 포함 20줄 이하**로 쓰고 파일 전체를 270줄 이하로 남긴다(D8). 긴 절차(고아 정리 · 사고 복원)는 새 `docs/RESTORE.md`(150줄 이하)에 두고 §14가 링크한다(Codex #9).
- 이 플랜은 IAM·WIF 설정을 바꾸지 않는다(`scripts/bootstrap-gcp.sh` 수정 금지 — Codex #1은 잔여 위험 수용 + Phase 8 후속).
- 새 의존성 없음(`actionlint`·`shellcheck` 도입 금지 — RESEARCH Environment Availability). `git push` 하지 않는다. `.planning/STATE.md`를 고치지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 엣지 truth를 이 플랜이 적었다: **가드가 첫 gcloud보다 먼저**(truth 2·10) · **변경 명령의 대상은 임시 이름뿐**(truth 3) · **부재는 성공한 목록 조회로만 확인**(truth 4) · **작업 대기·재시도·폴링 소진**(truth 5) · **고아 점검**(truth 6) · **한 번 기록하는 finalize**(truth 7) · **단계 우선순위**(truth 8) · **이미지 고정**(truth 9). OPS-03 프로브 행 중 「복원 워크플로」와 「문서」 부분을 닫는다.

**RESEARCH Open Questions 처리(C6) — 셋 다 RESOLVED**
1. OQ1 RESOLVED(복원 대상이 먼저 있어야 하는가): 문서 대신 설치된 API 정의로 답했다 — `node_modules/@googleapis/sqladmin/build/v1.d.ts`에서 `instances.restoreBackup`은 **대상 인스턴스 리소스의 메서드**이고 `restoreBackupContext{backupRunId, instanceId, project}`를 받는다. 그래서 대상은 먼저 있어야 하고, 스크립트는 생성 → RUNNABLE → 복원 순서다. 같은 파일의 `restoreInstanceSettings`(「새 인스턴스 복원에만」) 경로는 쓰지 않는다. docs.cloud.google.com은 이 세션에서 EGRESS_BLOCKED였다 — 04.4-06 Task 3의 실제 1회가 실측으로 확인한다
2. OQ2 RESOLVED: 04.4-02 probe_fallback 가정 1 — 거짓 성공 불가 설계 + 04.4-06 Task 3 실측
3. OQ3 RESOLVED(REQUIREMENTS 추적표): 계획 커밋 `7a1eca3`에서 이미 고쳤다(OPS-03 → Phase 04.4)

**계획 가정**
1. A3: 복원이 IAM DB 사용자(`plant8-<env>-runtime@<프로젝트>.iam`)를 그대로 가져온다고 가정하지 않는다 — 복원 뒤 `gcloud sql users list`로 보고 없으면 `--type=cloud_iam_service_account`로 만든다(deploy.sh `ensure_sql_db_users` 모양, 멱등)
2. A4: PITR은 켜지 않는다(비용·요구사항은 「최신 자동 백업 복원」). 문서에 「복원 단위는 하루 1회 자동 백업, 그 뒤 입력은 사라진다」를 적는다
3. A7: `gha-deployer`의 `roles/cloudsql.admin`이 인스턴스 생성·삭제·작업 조회를 포함한다(deploy.sh가 같은 계정으로 생성한다) — 실측은 04.4-06 Task 3
4. A12: 생성·복원 대기는 deploy.sh와 같은 RUNNABLE 폴링(10초 × 90)이되 **소진을 명시적 실패로** 다룬다(deploy.sh 루프는 소진을 거부하지 않는다 — Codex #3). 임시 인스턴스는 GA `gcloud sql instances create`(beta는 `--storage-auto-increase-limit` 때문에만 필요했다)
5. 임시 인스턴스 크기: 원본 `settings.dataDiskSizeGb`와 같은 HDD, `DB_TIER`·`DB_VERSION`·ENTERPRISE·ZONAL·사설 IP·IAM 인증 플래그, 자동 백업 끔(`--no-backup`), 삭제 보호 없음. 비용은 db-f1-micro 수십 분 수준(D-06)
6. 같은 환경에서 두 리허설이 겹치지 않게 환경별 `concurrency` 그룹을 두고, `deploy` 그룹과는 공유하지 않는다(truth 9의 이유)
7. 부재 확인: `gcloud sql instances list --filter="name=<임시>" --format='value(name)'`은 없으면 종료 코드 0과 빈 출력, 인증·권한·API 오류면 0이 아닌 종료 코드를 낸다고 본다. `instances describe`의 실패는 NOT_FOUND와 다른 오류를 가르지 못하므로 부재 판단에 쓰지 않는다(Codex #2)
8. 작업 대기: `gcloud sql operations list --instance=<임시> --filter="status!=DONE" --format='value(name)'` → 각 id에 `gcloud sql operations wait <id> --timeout=600`. 삭제 시도 사이 30초. finalize 한 번이 20분 안에 끝나도록 대기 상한을 둔다(단계 제한 25분)
9. 이미지: `gcloud run jobs describe <job> --format='value(spec.template.spec.template.spec.containers[0].image)'`로 참조를, `gcloud artifacts docker images describe <참조> --format='value(image_summary.digest)'`로 digest를 읽는다. 배포 계정은 이미지를 올리므로 읽기 권한이 있다고 본다. 형식 경로가 틀려 못 읽으면 「이미지 확인 불가」로 성공이 아니다(안전한 쪽) — 04.4-06 Task 3의 실제 1회가 확인한다. `jobs execute`는 이미지를 덮어쓸 수 없어 고정 대신 검출이다
10. 취소: GitHub는 취소 뒤에도 `always()` 단계를 돌린다고 본다. 러너 자체를 잃는 경우(강제 종료)는 이 두 정리가 못 막는다 — 다음 실행의 고아 점검이 잡아 크게 실패하고, `docs/RESTORE.md`가 사람의 찾기·지우기 절차를 준다(잔여 위험)
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — `rehearse` 한 번과 `finalize` 한 번이 가짜 gcloud 위에서 고아 점검 → 백업 → 생성 → 복원 → 이미지 고정 → verify → 정리 → 이미지 재확인 → record 한 번 → 요약을 끝까지 간다</name>
  <files>scripts/restore-rehearsal.sh, test/unit/deploy/restore-fakebin/gcloud, test/unit/deploy/restore-fakebin/sleep, test/unit/deploy/restore-rehearsal-sh.test.ts</files>
  <precondition>04.4-02가 끝나 Job 이름 `plant8-{env}-restore`와 CLI 인자 규약(`verify --target` · `record --succeeded …`)이 정해져 있다</precondition>
  <reversibility rating="costly">임시 인스턴스 이름 규칙과 가드 정규식은 한번 실제로 돌면 운영 콘솔에 그 이름의 흔적이 남는다(삭제된 이름은 한동안 재사용 불가). 실행 id가 들어가 겹치지 않으므로 바꾸는 데 드는 것은 코드 수정뿐이다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md` 117–237행(다이어그램 · Pattern 1·2) · 255–283행(Pitfall 1~3) · 341–355행(최신 성공 백업 id 조회 명령)
    - `scripts/deploy.sh` 186–260행(`ensure_sql_instance` 생성 플래그와 RUNNABLE 폴링, `ensure_sql_db_users`) · 300–360행(Job 이름·`--args` 규약)
    - `.github/workflows/account.yml`(전문) — `ENV_SHORT` 매핑, `jobs execute … --args … --wait --format='value(metadata.name)'`, `poll_logs`(12회 × 10초, severity 필터 없음), 실패 이유를 `$GITHUB_STEP_SUMMARY`에 남기는 모양
    - `scripts/promote-guard.sh` · `test/unit/deploy/promote-guard-sh.test.ts` — 가드 스크립트와 그 행동 테스트 모양
    - `test/unit/deploy/fakebin/gcloud` 1–60행 — `DEPLOY_FAKE_LOG` · `DEPLOY_FAKE_STATE` · 처리 안 된 호출 = 종료 코드 1 규약
    - `test/unit/deploy/rollback-sh.test.ts` 1–80행 — `spawnSync`로 스크립트를 돌리고 로그를 읽는 모양
    - `infra/names.sh` — `sql_instance` · `job_name` · `runtime_sa` · `DB_TIER` · `DB_VERSION` · `NETWORK`
  </read_first>
  <behavior>
    - 행복 경로(rehearse): env staging · RUN_ID 123 · ATTEMPT 1이면 가짜 gcloud 로그 순서가 (0) `sql instances list` 접두 `plant8-staging-rehearsal-` 조회(빈 결과) (1) `sql backups list --instance=plant8-staging-db` (2) `sql instances describe plant8-staging-db` 디스크 크기 (3) `sql instances create plant8-staging-rehearsal-123-1` (4) RUNNABLE 폴링 (5) `sql backups restore <id> --restore-instance=plant8-staging-rehearsal-123-1 --backup-instance=plant8-staging-db` (6) RUNNABLE 폴링 (7) `sql users list/create --instance=plant8-staging-rehearsal-123-1` (8) 연결 이름 조회 (9) `run jobs describe plant8-staging-restore` 이미지 + `artifacts docker images describe` digest (10) `run jobs execute plant8-staging-restore --args=verify,--target,<프로젝트>:<리전>:plant8-staging-rehearsal-123-1 --update-env-vars=CLOUD_SQL_CONNECTION_NAME=<같은 값>` (11) 로그 조회 (12) EXIT 정리: 정확한 이름 목록(있음) → `sql operations list --instance=plant8-staging-rehearsal-123-1` → `sql instances delete plant8-staging-rehearsal-123-1` → 정확한 이름 목록(빔). 로그에 `--args=record`가 **없다**. 종료 코드 0
    - 행복 경로(이어지는 finalize, 같은 `RUNNER_TEMP`): (13) 정확한 이름 목록(빔 — 삭제 호출 없음) (14) 이미지·digest 재조회(같음) (15) `run jobs execute plant8-staging-restore --args=record,--succeeded,true,--backup-id,<id>,…` — `--update-env-vars` 없음 (16) 접두 목록. 종료 코드 0
    - 요약 파일(`GITHUB_STEP_SUMMARY`)에 `성공` · 백업 id · 임시 인스턴스 이름 · `남은 임시 인스턴스 0` · `이미지 <참조>@<digest>` · 「기록했습니다」가 있다
    - record 인자의 `--run-url`은 `https://github.com/<GITHUB_REPOSITORY>/actions/runs/123`이고 `--started-at`·`--finished-at`은 `YYYY-MM-DDTHH:MM:SSZ` 모양이며 `--started-at`은 상태 파일에 rehearse가 적은 시각이다
    - 전 시나리오 공통 불변식(헬퍼 하나): 로그의 `instances create`·`instances delete`·`--restore-instance=`·`users create`·`operations wait` 대상은 임시 이름뿐이고, `--args=record`는 전체 로그에 많아야 한 번이다
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: `test/unit/deploy/restore-rehearsal-sh.test.ts`에 behavior 다섯 가지를 쓴다. `spawnSync("bash", ["scripts/restore-rehearsal.sh", "rehearse"])` 뒤 같은 env로 `finalize`, env는 `PATH=<restore-fakebin>:$PATH` · `DEPLOY_FAKE_LOG` · `DEPLOY_FAKE_STATE`(임시 디렉터리) · `RUNNER_TEMP`(임시 디렉터리) · `INPUT_ENV` · `PROJECT`(가짜 값 `test-project`) · `REGION` · `GITHUB_RUN_ID` · `GITHUB_RUN_ATTEMPT` · `GITHUB_SERVER_URL=https://github.com` · `GITHUB_REPOSITORY=o/r` · `GITHUB_STEP_SUMMARY`(임시 파일). 공통 불변식 단언은 헬퍼 하나로 만들어 Task 2의 모든 시나리오에서 다시 쓴다. 빨간 것을 확인한다.

② 전용 가짜 두 개 — **행복 경로에 필요한 호출만**(실패 손잡이는 Task 2가 빨간 테스트 뒤에 더한다). `test/unit/deploy/restore-fakebin/gcloud`(bash, 실행 비트): 모든 인자를 `$DEPLOY_FAKE_LOG`에 한 줄로 적고 — 인스턴스 목록(`--filter`의 `name=<정확>` 또는 `name~^<접두>`에 맞는 `$DEPLOY_FAKE_STATE` 파일 이름 출력, 없으면 빈 출력 + 종료 코드 0), 백업 목록(고정 id 한 줄), 원본 디스크 크기(`10`), 인스턴스 생성(`$DEPLOY_FAKE_STATE/<이름>` 파일 생성), 상태·연결 이름 조회(파일이 있으면 `RUNNABLE` / `<프로젝트>:<리전>:<이름>`), 복원, 사용자 목록·생성, 작업 목록(빈 출력), Job 이미지 조회(고정 참조), 이미지 digest(고정 `sha256:` 값), Job 실행(`--format` 요청 시 실행 이름 출력), 로그 조회(고정 두 줄), 인스턴스 삭제(파일 삭제). 처리하지 않는 호출은 종료 코드 1(기존 fakebin 규약). `test/unit/deploy/restore-fakebin/sleep`은 즉시 0으로 끝나는 가짜(폴링이 테스트를 느리게 하지 않게 — 스크립트에 대기 시간 설정 손잡이를 만들지 않는다).

③ `scripts/restore-rehearsal.sh`(D8-08 · C2): `set -Eeuo pipefail`, 스크립트 위치 기준으로 `infra/names.sh`를 source. 하위 명령 `guard <이름>` · `rehearse` · `finalize`.
**공통 준비(truth 10 — `rehearse`·`finalize`가 똑같이 거친다, 체커 지적의 한 규칙)**: `INPUT_ENV`를 staging → staging / production → prod로 바꾸고(그 밖은 종료 코드 2), `GITHUB_RUN_ID`·`GITHUB_RUN_ATTEMPT`가 숫자가 아니면 종료 코드 2, 원본 = `sql_instance`, 임시 = `plant8-<env>-rehearsal-<RUN_ID>-<ATTEMPT>`, 그 이름에 truth 2의 가드(정규식 · 환경 접두 · 두 운영 이름·원본과 다름)를 적용해 어기면 stderr에 이유를 적고 종료 코드 3, production이면 `CONFIRM_PRODUCTION`이 정확히 `plant8-prod-db`가 아닐 때 종료 코드 3 — **여기까지 gcloud 호출 0**. 상태 파일 경로 = `$RUNNER_TEMP/restore-rehearsal-<RUN_ID>-<ATTEMPT>.state`(`key=value` 줄, 읽을 때 `source`하지 않고 줄 단위로 파싱).
**`rehearse`**: 상태 파일에 `STARTED_AT` · `STAGE=started`를 쓴다 → EXIT trap 등록(정리 1차만, 기록하지 않는다) → 고아 점검(접두 목록, 행복 경로에서는 빈 결과) → 최신 성공 자동 백업 id(`--filter="type=AUTOMATED AND status=SUCCESSFUL" --sort-by=~startTime --limit=1 --format='value(id)'`) → 원본 디스크 크기 → probe_fallback 가정 5의 플래그로 GA `gcloud sql instances create` → RUNNABLE 폴링 → `gcloud sql backups restore <id> --restore-instance=<임시> --backup-instance=<원본> --quiet` → RUNNABLE 폴링 → IAM 사용자 보정(가정 1) → 연결 이름 조회 → 이미지 참조·digest를 상태 파일에 `IMAGE`·`DIGEST`로(가정 9) → `gcloud run jobs execute <job_name env restore> --region --project --args=verify,--target,<연결 이름> --update-env-vars=CLOUD_SQL_CONNECTION_NAME=<연결 이름> --wait --format='value(metadata.name)'` → 실행 이름으로 좁혀 로그를 account.yml `poll_logs`와 같은 규칙으로 찍는다 → 상태 파일에 `VERIFY_OK=1`. 각 단계 진입 때 `STAGE`와 `BACKUP_ID`를 상태 파일에 갱신한다.
**정리 함수 `cleanup_temp`(EXIT trap과 finalize가 같이 쓴다)**: 임시 이름을 다시 가드 → 정확한 이름 목록 → 있으면 `sql operations list --filter="status!=DONE"`의 각 작업을 기다리고 `gcloud sql instances delete <임시> --quiet` → 다시 정확한 이름 목록이 비면 상태 파일에 `CLEANUP=absent`. (Task 1은 행복 경로만 — 알 수 없음·재시도는 Task 2.)
**`finalize`**: 공통 준비 → 상태 파일 읽기 → `cleanup_temp`(마지막 시도) → 이미지·digest 재조회해 상태 파일 값과 비교 → 결과 판정(truth 8) → 상태 파일에 `RECORDED=1`이 없을 때만 record Job 실행 — `--args=record,--succeeded,<true|false>[,--failed-stage,<단계>][,--backup-id,<id>],--started-at,<STARTED_AT>,--finished-at,<지금 ISO>,--run-url,<URL>`, **env 덮어쓰기 없음**(그 환경 자신의 DB, C3) — 성공하면 `RECORDED=1` → 접두 목록(개수와 이름) → `GITHUB_STEP_SUMMARY`에 결과(성공/실패 · 단계) · 원본 · 백업 id · 임시 이름 · 소요 · `남은 임시 인스턴스 <개수>` · `이미지 <참조>@<digest>` · 「기록했습니다」를 한국어로 → 성공이고 기록됐을 때만 종료 코드 0.
출력·요약 문구는 「…니다」로 끝낸다. 변경 명령에 원본 이름을 넣는 줄이 없게 쓴다.

④ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: add restore rehearsal script with name guard and finalizer`, 본문 한국어(D8-08 · 이름 가드가 첫째 안전 조건 · 기록은 finalize 한 곳).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/restore-rehearsal-sh.test.ts</automated>
    <fails_when>행복 경로 호출 순서(rehearse · finalize) · rehearse에 record 없음 · record에 env 덮어쓰기 없음 · 요약 여섯 항목 · run URL·시각 모양 · 변경 명령 대상 = 임시 이름 · record 많아야 한 번 불변식 중 하나가 어긋난다</fails_when>
    <automated>bash -n scripts/restore-rehearsal.sh &amp;&amp; test -x test/unit/deploy/restore-fakebin/gcloud &amp;&amp; test -x test/unit/deploy/restore-fakebin/sleep</automated>
    <fails_when>스크립트에 문법 오류가 있거나 가짜 두 파일에 실행 비트가 없다(git에 `100755`로 들어가야 한다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "restore-instance=.?\\\$(SOURCE|source)|instances (delete|patch|restart).*(plant8-(staging|prod)-db|SOURCE)" scripts/restore-rehearsal.sh`가 아무 줄도 찾지 않는다
    - `grep -c "set -Eeuo pipefail" scripts/restore-rehearsal.sh`가 1이다
    - `grep -c -- "--args=record" scripts/restore-rehearsal.sh`가 1이다(기록 호출은 finalize 한 곳)
    - `git ls-files -s test/unit/deploy/restore-fakebin`의 두 파일 모드가 `100755`다
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>가짜 gcloud 위에서 리허설 한 번(rehearse + finalize)이 끝까지 가고, 변경 명령이 임시 이름에만 닿으며, 결과가 마지막 정리 뒤 그 환경의 record Job으로 한 번 넘어간다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 실패·중단·알 수 없는 정리·작업 대기·한 번 기록·이미지 변경·production 확인 경로</name>
  <files>test/unit/deploy/restore-rehearsal-sh.test.ts, test/unit/deploy/restore-fakebin/gcloud, scripts/restore-rehearsal.sh</files>
  <read_first>
    - `scripts/deploy.sh` 186–215행 — 빌려 온 RUNNABLE 루프가 소진을 거부하지 않는 부분과 `describe` 실패를 부재로 보는 부분(191행) — 이 플랜이 따라 하지 않을 모양
    - `scripts/bootstrap-gcp.sh` 95–125행 — WIF가 저장소로만 묶이고 배포 계정이 `cloudsql.admin`을 갖는 근거(truth 11, 수정 금지)
  </read_first>
  <behavior>
    - 가드 1: `guard plant8-staging-db` · `guard plant8-prod-db` · env staging에서 `guard plant8-prod-rehearsal-1-1` · `guard plant8-staging-rehearsal-x-1` · `guard plant8-staging-rehearsal-1-1-extra` → 종료 코드 ≠ 0, 가짜 로그 0줄
    - 가드 2: `GITHUB_RUN_ID` 빈 값 또는 `INPUT_ENV=prod-db` → `rehearse`와 `finalize` 둘 다 종료 코드 ≠ 0, 로그 0줄
    - 확인 1(한 규칙): env production · `CONFIRM_PRODUCTION` 없음 또는 `plant8-prod-DB` → `rehearse`와 `finalize` **둘 다** 종료 코드 3, 로그 0줄. 정확히 `plant8-prod-db`면 진행하고 원본은 `plant8-prod-db`, 임시는 `plant8-prod-rehearsal-…`, 공통 불변식 유지
    - 고아 1: 상태 디렉터리에 `plant8-staging-rehearsal-99-1`이 이미 있으면 rehearse는 생성·백업 조회 호출 없이 종료 코드 1; finalize는 그 이름을 **지우지 않고** record에 `--failed-stage,cleanup`, 요약에 그 이름과 `RESTORE.md`, 종료 코드 1
    - 고아 2: 접두 목록 조회가 실패(`FAKE_LIST_FAIL=503`)하면 생성 없이 종료 코드 1, 요약 「남은 임시 인스턴스 확인 불가」
    - 실패 1: `FAKE_NO_BACKUP=1` → 생성 호출 없음, record에 `--failed-stage,restore`, `--backup-id` 없음, 종료 코드 1
    - 실패 2: `FAKE_FAIL=create` · `restore` · `users` → 단계 restore, 임시가 만들어졌으면 그 이름으로 삭제, 종료 코드 1
    - 실패 3: `FAKE_FAIL=verify` → 단계 verify, 임시 삭제 호출 있음, 요약 `남은 임시 인스턴스 0`, 종료 코드 1
    - 정리 1~3(Codex #2 — 각각 따로): 임시를 만든 뒤 정확한 이름 목록이 `FAKE_LIST_FAIL=403`(PERMISSION_DENIED) · `503`(일시 오류) · `auth`(「Reauthentication failed」)로 실패하면 결과 `cleanup` 실패, 요약에 「확인 불가」가 있고 `남은 임시 인스턴스 0`은 **없다**, 종료 코드 1. 이 세 시나리오에서 목록 실패가 시작된 뒤의 임시 `instances describe`는 가짜가 `NOT_FOUND` 문구로 실패하게 둔다 — 스크립트가 describe 실패를 부재 근거로 쓰면 이 테스트가 깨진다
    - 정리 4(Codex #3): `FAKE_PENDING_OP=1` + `FAKE_DELETE_CONFLICT=1`(첫 삭제만 「operation in progress」로 실패) → 로그에서 `operations wait`가 첫 `instances delete`보다 앞서고, 삭제가 다시 시도되며, 부재가 확인돼 성공이다
    - 정리 5: `FAKE_DELETE_FAIL=always` → 삭제 3번 뒤 멈추고 단계 cleanup(앞선 성공을 덮음), 요약에 남은 임시 이름, 종료 코드 1
    - 폴링 소진: `FAKE_NEVER_RUNNABLE=1` → 상태 조회가 정확히 90번 뒤 단계 restore 실패(소진이 성공으로 넘어가지 않는다), 임시 삭제 시도
    - finalize 1(중단, Codex #3·#4): `FAKE_KILL_AFTER=create`(가짜가 생성 뒤 스크립트 셸을 `kill -KILL`) → rehearse의 EXIT trap이 못 돈다; 이어진 finalize가 임시를 지우고 부재를 확인한 뒤 record에 `--failed-stage,restore`, 종료 코드 1
    - finalize 2(한 번 기록): 성공 시나리오에서 finalize를 두 번 돌리면 `--args=record`는 로그에 한 번뿐이고 두 번째도 같은 종료 코드
    - finalize 3: 상태 파일 없이 finalize → 정확한 이름 정리만 하고 record 호출 없음, 요약 「기록하지 않았습니다」, 종료 코드 1
    - finalize 4: `FAKE_FAIL=record` → 리허설은 성공이어도 종료 코드 1, 요약에 「기록 실패」, 상태 파일에 `RECORDED` 없음
    - 이미지 1(Codex #12): `FAKE_IMAGE_CHANGE=1`(두 번째 digest 조회가 다른 값) → record에 `--succeeded,false,--failed-stage,verify`, 요약에 두 digest, 종료 코드 1; `FAKE_IMAGE_FAIL=2`(두 번째 조회 실패) → 같은 결과에 「이미지 확인 불가」
    - 겹친 실패(우선순위 정리 > 복원 > 검증, UI-SPEC 결과의 정의 · Codex 2차 #2 — 각각 따로): ⓐ `FAKE_FAIL=verify` + `FAKE_DELETE_FAIL=always` → record에 `--failed-stage,cleanup` 하나(verify 아님) · ⓑ `FAKE_FAIL=restore` + 생성 뒤 `FAKE_LIST_FAIL=503` → `--failed-stage,cleanup` · ⓒ `FAKE_FAIL=restore` + `FAKE_IMAGE_CHANGE=1` → `--failed-stage,restore` · ⓓ `FAKE_FAIL=verify` + `FAKE_IMAGE_CHANGE=1` → `--failed-stage,verify`. 네 경우 모두 로그에 `--failed-stage`가 정확히 한 번, 종료 코드 1
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: 위 behavior를 Task 1 테스트 파일에 **더한다**(각 시나리오 끝에 공통 불변식 헬퍼). 빨간 것을 확인한다.

② 가짜 gcloud에 실패 손잡이를 더한다: `FAKE_NO_BACKUP` · `FAKE_FAIL=<create|restore|users|verify|record>` · `FAKE_LIST_FAIL=<403|503|auth>`(목록 호출이 각각 `PERMISSION_DENIED` · `503 UNAVAILABLE` · `Reauthentication failed` 문구를 stderr에 찍고 종료 코드 1, 접두 목록에도 같은 규칙 — 고아 2는 rehearse 첫 목록부터, 정리 1~3은 생성 뒤 목록부터 실패하도록 카운터 파일로 조절) · `FAKE_PENDING_OP`(작업 목록이 id 하나를 한 번 내고 `operations wait`가 지운다) · `FAKE_DELETE_CONFLICT`(첫 삭제만 실패) · `FAKE_DELETE_FAIL=always` · `FAKE_NEVER_RUNNABLE`(상태 조회가 늘 `PENDING_CREATE`) · `FAKE_KILL_AFTER=create`(생성 파일을 만든 뒤 `ps -o ppid=,args=`로 조상을 거슬러 올라가 `restore-rehearsal.sh`를 실행 중인 bash 프로세스 전부를 `kill -KILL` — 명령 치환 서브셸이어도 스크립트 본체가 죽어 EXIT trap이 돌지 않는다. vitest 프로세스는 건드리지 않는다) · `FAKE_IMAGE_CHANGE` · `FAKE_IMAGE_FAIL=<n번째>`. 처리하지 않는 호출 = 종료 코드 1 규약은 그대로.

③ 스크립트를 behavior에 맞게 고친다:
- `cleanup_temp`: 정확한 이름 목록이 **성공하고 비었을 때만** `CLEANUP=absent`. 목록이 실패하면 `CLEANUP=unknown`(더 시도하지 않고 멈춘다). 있으면 작업 대기(가정 8) → 삭제 → 재목록, 부재가 확인될 때까지 최대 3번(사이 30초 — 가짜 `sleep`), 끝까지 남으면 `CLEANUP=present`.
- RUNNABLE 폴링: 90번 뒤에도 RUNNABLE이 아니면 명시적 실패(단계 restore, 이유 「대기 시간을 넘겼습니다」).
- 고아 점검: 접두 목록이 비어 있지 않거나 실패하면 `STAGE=cleanup` · `ORPHANS=<이름들|unknown>`을 상태 파일에 쓰고 아무것도 만들지 않고 종료 코드 1. 고아는 지우지 않는다(이 실행의 임시가 아니므로 — 사람이 `docs/RESTORE.md` 절차로 지운다).
- finalize 판정(truth 8, 우선순위 정리 > 복원 > 검증 — 위에서부터 처음 맞는 하나만): ① `CLEANUP`이 absent가 아니거나 `STAGE`가 cleanup(고아)이면 cleanup ② 아니면 `STAGE`가 restore면 restore ③ 아니면 `STAGE`가 verify이거나, `VERIFY_OK=1`인데 digest가 다르거나 읽기 실패면 verify ④ 그 밖에만 성공. 판정은 finalize 한 곳에만 두고 rehearse는 먼저 실패한 단계를 `STAGE`에 적기만 한다. 상태 파일이 없으면 공통 준비의 임시 이름으로 `cleanup_temp`만 하고 요약 「상태 파일이 없어 결과를 기록하지 않았습니다」, 종료 코드 1. 기록 실패는 `RECORDED`를 쓰지 않고 요약 「기록 실패」, 종료 코드 1.
- 요약의 남은 개수: 접두 목록이 성공했을 때만 숫자, 실패하면 「확인 불가」.

④ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: cover restore rehearsal failure, cleanup and finalize paths`, 본문 한국어(C1 · C2 · Codex #2·#3·#4·#12).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/restore-rehearsal-sh.test.ts</automated>
    <fails_when>가드·확인 실패 시 gcloud가 한 번이라도 불리거나(rehearse·finalize 모두), 조회 실패가 「0」으로 적히거나, 작업 대기·삭제 재시도·폴링 소진·고아 점검·중단 뒤 finalize·한 번 기록·상태 파일 없음·기록 실패·이미지 변경 중 하나의 단계·요약·종료 코드가 어긋나거나, 겹친 실패 ⓐ~ⓓ에서 기록된 단계가 우선순위 정리 &gt; 복원 &gt; 검증과 다르거나 `--failed-stage`가 두 번 이상 나온다</fails_when>
    <automated>bash -n scripts/restore-rehearsal.sh</automated>
    <fails_when>스크립트에 문법 오류가 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - 테스트 파일의 시나리오 이름에 `403` · `503` · `Reauthentication` · `operations wait` · `90` · `kill` · `record` · `digest`가 각각 한 번 이상 나온다
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>모든 실패·중단·정리·확인 경로가 가짜 gcloud로 증명되고, 알 수 없는 정리는 절대 성공이나 「0」이 되지 않으며, 기록은 마지막 정리 뒤 한 번뿐이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 워크플로 YAML(`restore-rehearsal.yml`)과 문서 — OPERATIONS §14(20줄 이하) · `docs/RESTORE.md` 런북</name>
  <files>.github/workflows/restore-rehearsal.yml, test/unit/deploy/restore-rehearsal-workflow.test.ts, docs/OPERATIONS.md, docs/RESTORE.md, test/unit/docs-limits.test.ts</files>
  <read_first>
    - `.github/workflows/account.yml`(전문 143행) · `.github/workflows/verify.yml` — WIF 블록 원문, `env:` 매핑, 스크립트 위임 모양
    - `.github/workflows/deploy.yml` 1–40행 — 트리거(main push = 스테이징, workflow_dispatch target·sha = production)와 `concurrency: group: deploy`
    - `test/unit/deploy/workflows.test.ts` 1–40행 · 169–260행 — `readWorkflow` 텍스트 단언, 「입력을 env로 넘겨 셸 인젝션을 막는다」, 「실제 프로젝트 번호·이메일 등 식별자를 담지 않는다」, `environment:` 금지, 모든 워크플로 `drizzle-kit push` 부재
    - `test/unit/ci-guard.test.ts` — 모든 워크플로를 순회하는 검사
    - `docs/OPERATIONS.md` 1–10행(상한·D-03 식별자 금지) · 82–122행(§4 배포 런북 · §5 롤백) · 205–230행(§10 상태 화면 · §12의 「Job으로 존재한다」 문장)
    - `test/unit/docs-limits.test.ts` 39–82행 — 300줄 단언, `it.each` 토큰 배열, 12자리 숫자 금지
    - `scripts/deploy.sh` 186–200행(백업 플래그 원문 `--backup-start-time=18:00 --retained-backups-count=7`) · 650–680행(`run_migrate` · `run_seed` · `deploy_service`의 `--allow-unauthenticated` 순서)
  </read_first>
  <behavior>
    - 워크플로 1: `on:` 아래 트리거가 `workflow_dispatch` 하나 · `schedule:`/`push:`/`pull_request:`/`environment:` 부재 · 입력 `env`(staging 기본)와 `confirm_production` · permissions 두 줄
    - 워크플로 2: 첫 번째 `- ` 단계가 `refs/heads/main`과 `plant8-prod-db`를 담고 그 위치가 `actions/checkout`·`google-github-actions/auth`보다 앞 · WIF provider 줄이 account.yml의 같은 줄과 동일
    - 워크플로 3: `restore-rehearsal.sh rehearse` 단계에 `id: rehearse`와 `timeout-minutes: 60`, `restore-rehearsal.sh finalize` 단계에 `always()`·`steps.rehearse.outcome != 'skipped'`와 `timeout-minutes: 25`, 잡 `timeout-minutes: 90` · `concurrency` 그룹에 `restore-rehearsal`이 있고 `group: deploy`가 아니다
    - 워크플로 4: `run:` 블록 본문 어디에도 `${{` 없음 · 12자리 숫자·`@` 이메일 없음
    - 문서 1: OPERATIONS 토큰 `restore-rehearsal.yml` · `PITR` · `retained-backups-count` · `RESTORE.md`
    - 문서 2: `docs/RESTORE.md`가 150줄 이하이고 12자리 숫자가 없으며 토큰 `remove-iam-policy-binding` · `scheduler jobs pause` · `deploy.yml` · `retained-backups-count` · `operations list` · `-rehearsal-`를 담는다
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: 새 `test/unit/deploy/restore-rehearsal-workflow.test.ts`에 워크플로 1~4를, `test/unit/docs-limits.test.ts`의 OPERATIONS 토큰 배열 **끝에** 문서 1의 네 토큰을 더하고 새 `describe("docs/RESTORE.md")`에 문서 2를 **더한다**(기존 항목은 그대로). 빨간 것을 확인한다.

② `.github/workflows/restore-rehearsal.yml`(C1 · D8-08): 머리 주석에 「D8-08 복원 리허설 — 원본을 건드리지 않는다. main·production 확인 입력 검사는 실수 방지 장치이고 권한 경계가 아니다: WIF는 저장소로만 묶여 있어 실제 경계는 1인 소유 비공개 저장소의 write 권한이다(T-04.4-14, Codex #1 — Phase 8에서 리허설 전용 최소 권한 SA · WIF ref 조건 검토). GitHub Environments 미사용(D-05)」. `on:`은 `workflow_dispatch`만 — 입력 `env`(choice staging·production, 기본 staging, 필수)와 `confirm_production`(string, 기본 빈 값, 설명 「production일 때만 plant8-prod-db를 그대로 입력」). `permissions: contents: read, id-token: write`. `concurrency`는 `group: restore-rehearsal-${{ inputs.env }}` · `cancel-in-progress: false`, 바로 위 주석 「deploy 그룹과 공유하지 않는다 — 같은 그룹의 대기 중 실행은 새 실행으로 바뀌어 대기 중이던 배포가 취소될 수 있다. 대신 스크립트가 verify 전후 이미지 digest를 비교한다(Codex #12)」. 잡 하나, `runs-on: ubuntu-latest`, `timeout-minutes: 90`. 단계 순서 — (1) **첫 단계**: 체크아웃·인증 전, `env:`로 받은 `REF`(github.ref) · `INPUT_ENV` · `CONFIRM_PRODUCTION`을 검사해 main이 아니거나 production인데 확인이 정확히 `plant8-prod-db`가 아니면 이유를 찍고 종료 코드 1 (2) `actions/checkout@v4` (3) `google-github-actions/auth@v3` — account.yml과 **글자 그대로 같은** `workload_identity_provider`·`service_account` (4) `google-github-actions/setup-gcloud@v3` (5) `id: rehearse`, `timeout-minutes: 60`, `shell: bash`, `env:`(INPUT_ENV · CONFIRM_PRODUCTION · PROJECT · REGION), `run: bash scripts/restore-rehearsal.sh rehearse` (6) `if: ${{ always() && steps.rehearse.outcome != 'skipped' }}`, `timeout-minutes: 25`, 같은 env, `run: bash scripts/restore-rehearsal.sh finalize`. `run:` 본문에는 `${{`를 쓰지 않는다(GitHub 기본 env인 `GITHUB_RUN_ID` · `RUNNER_TEMP` 등은 러너가 이미 준다). `environment:` 키 · schedule · push · pull_request 없음. 실제 프로젝트 번호·이메일 없음(`vars.*`만).

③ `docs/RESTORE.md`(새 파일, 150줄 이하, D-03 — 실제 프로젝트 ID·번호·예시 백업 id 금지, 프로젝트는 `$GCP_PROJECT_ID` 표기, 리전·서비스 이름 표기는 OPERATIONS.md를 따른다). 세 절:
(가) **리허설 실행·결과 읽기** — Actions → `restore-rehearsal.yml` → main에서 Run workflow(env 기본 staging, production은 `confirm_production`에 `plant8-prod-db`). 결과는 그 환경의 `/admin/system-status` 「복원 리허설」 행(그 환경 자기 DB 기록만)과 Actions 요약(결과 · 단계 · 백업 id · 이미지 digest · 남은 임시 인스턴스). 기록 전에 끝난 실패(인증 실패 · 상태 파일 없음 · 기록 실패)는 Actions에만 남는다. 요약이 「확인 불가」면 정리가 확인되지 않은 것이다.
(나) **남은 임시 인스턴스 찾기·지우기**(Codex #3) — `gcloud sql instances list --filter="name~^plant8-<env>-rehearsal-" --project=$GCP_PROJECT_ID` → 이름이 `plant8-<env>-rehearsal-<숫자>-<숫자>`이고 `plant8-<env>-db`가 **아닌지** 대조 → `gcloud sql operations list --instance=<이름> --filter="status!=DONE"`가 빌 때까지 기다림(`gcloud sql operations wait <id>`) → `gcloud sql instances delete <이름>` → 목록 재조회로 부재 확인. 다음 리허설은 남은 인스턴스가 있으면 시작하지 않는다(고아 점검).
(다) **실제 사고 때 복원 — 사람이 한다(세션은 하지 않는다)**(Codex #9): 1. 쓰기 중단 — `gcloud run services remove-iam-policy-binding plant8-<env> --region=<리전> --member=allUsers --role=roles/run.invoker`(공개 접근 제거), `gcloud scheduler jobs list --location=<리전>`의 작업을 `gcloud scheduler jobs pause`, 운영 워크플로(account · 배포 · 리허설)를 돌리지 않고, 실행 중인 Cloud Run Job 실행이 끝나기를 기다린다 2. 백업 선택 — 리허설과 같은 `gcloud sql backups list` 필터로 최신 성공 자동 백업(또는 사고 직전의 것) 3. 복원 — `gcloud sql backups restore <백업 id> --restore-instance=plant8-<env>-db --backup-instance=plant8-<env>-db --project=$GCP_PROJECT_ID`(현재 데이터를 덮어쓴다 · PITR이 꺼져 있어 백업 뒤 입력은 사라진다) 4. 이미지·스키마 호환 — 백업은 서빙 중인 이미지보다 오래된 스키마일 수 있으므로 서비스를 바로 열지 않는다. 정상 배포 경로(`deploy.yml` — 스테이징은 main 재실행, production은 OPERATIONS §4 절차로 지금 서빙 중인 SHA)를 다시 돌려 migrate → seed를 거치게 한다; 그 배포의 서비스 배포 단계가 `--allow-unauthenticated`로 공개 접근을 **다시 연다**(재개점) 5. 확인 — 배포 스모크 · `/admin/system-status` · 관리자 로그인 · 최근 데이터 몇 건 대조; 이상하면 1의 공개 접근 제거를 다시 한다 6. 재개 — `gcloud scheduler jobs resume`, 사용자 공지(백업 시각 이후 입력은 다시 넣어야 한다) 7. 백업 설정 재확인 — 복원은 백업 설정을 기본값으로 되돌릴 수 있다: `gcloud sql instances describe plant8-<env>-db --format='value(settings.backupConfiguration)'`로 보고 다르면 `gcloud sql instances patch plant8-<env>-db --backup-start-time=18:00 --retained-backups-count=7`.

④ `docs/OPERATIONS.md` 끝에 `## 14. 백업·복원 (OPS-03)` 절을 **머리글 포함 20줄 이하**로 더한다(D8-08, ROADMAP 04.4 기준 3). 담을 것 — (a) 자동 백업 설정: `deploy.sh`가 `--backup-start-time=18:00`(UTC) · `--retained-backups-count=7`로 켠다, 확인 명령 (b) PITR은 꺼져 있다 — 복원 단위는 하루 1회 자동 백업 (c) 리허설 한 줄 요약(`restore-rehearsal.yml`, production 확인 입력, 임시 인스턴스 `plant8-<env>-rehearsal-<실행 id>-<시도>` → `plant8-<env>-restore` Job 확인 → 삭제 → 기록) (d) 결과는 `/admin/system-status`와 Actions 요약 (e) 고아 정리와 사고 복원은 [`docs/RESTORE.md`](RESTORE.md). §10 상태 화면 문장에 「복원 리허설(결과·원본·일시·백업 id·소요, 기록 없음/확인 불가)」을 더하고, §12의 「migrate·seed·account·db-bootstrap 넷만 … Job으로 존재한다」를 restore를 포함한 다섯으로 고친다 — 두 곳 모두 줄 수를 늘리지 않게 문단 안에서 고친다.

⑤ 녹색이 될 때까지. `wc -l docs/OPERATIONS.md docs/RESTORE.md`를 SUMMARY에 적는다. 커밋 두 개, 제목 예 `feat: add restore-rehearsal workflow_dispatch workflow` · `docs: add backup and restore runbook`, 본문 한국어(C1 · Codex #1·#9·#12).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/restore-rehearsal-workflow.test.ts test/unit/deploy/workflows.test.ts test/unit/ci-guard.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>워크플로 텍스트 단언(트리거 · 첫 단계 · WIF · 단계 시간 제한 · always finalize · 동시성 그룹 · env 매핑) 또는 기존 워크플로 검사가 실패하거나, OPERATIONS.md가 300줄을 넘거나 네 토큰 중 하나가 없거나, RESTORE.md가 150줄을 넘거나 토큰이 빠졌거나, 두 문서에 12자리 숫자가 들어갔다</fails_when>
    <automated>test "$(wc -l &lt; docs/OPERATIONS.md)" -le 270</automated>
    <fails_when>이 페이즈 몫 예산을 넘어 04.2·04.3·Phase 8이 쓸 자리가 30줄 미만으로 줄었다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s+(schedule|push|pull_request):" .github/workflows/restore-rehearsal.yml`이 0이다
    - `grep -c "environment:" .github/workflows/restore-rehearsal.yml`이 0이다
    - `diff <(grep "workload_identity_provider" .github/workflows/account.yml) <(grep "workload_identity_provider" .github/workflows/restore-rehearsal.yml)`이 아무것도 출력하지 않는다
    - `sed -n '/^## 14\./,$p' docs/OPERATIONS.md | wc -l`이 20 이하이고 그 범위에 `restore-rehearsal.yml` · `PITR` · `retained-backups-count` · `RESTORE.md`가 모두 있다
    - `docs/RESTORE.md`에서 `remove-iam-policy-binding`이 `backups restore`보다 앞 줄이고, `deploy.yml`이 `backups restore`보다 뒤 줄이며, `scheduler jobs resume`이 `deploy.yml`보다 뒤 줄이다(순서: 쓰기 중단 → 복원 → 정상 배포 경로 → 재개)
    - `git diff --stat "$PLAN_BASE"..HEAD -- scripts/bootstrap-gcp.sh`가 비어 있다(IAM 변경 없음)
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>워크플로가 main·사람 트리거·확인 입력(실수 방지)·WIF·always finalize 규칙을 텍스트로 지키고, 운영자가 OPERATIONS §14와 RESTORE.md만 보고 리허설 실행 · 결과 읽기 · 고아 정리 · 쓰기 중단부터 백업 설정 재확인까지의 사고 복원을 할 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사람(write 협업자) → `workflow_dispatch` 입력 | env · confirm_production 문자열 |
| GitHub 러너(WIF, `gha-deployer` = `roles/cloudsql.admin`) → Cloud SQL Admin API | 잘못된 대상이면 운영 인스턴스를 덮어쓸 수 있는 권한. WIF는 저장소로만 묶여 있다(`scripts/bootstrap-gcp.sh`) — 이 경계의 실제 통제는 저장소 write 권한이다 |
| 러너 → Cloud Run Job 실행(인자·env 덮어쓰기) | 어느 DB를 읽고 어디에 기록할지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-11 | Tampering | 원본 인스턴스 덮어쓰기·삭제 | critical | mitigate | 임시 이름을 스크립트가 만들고 첫 gcloud 전에 정규식·환경 접두·운영 이름 비교로 가드. 변경 명령은 임시 이름 변수만 쓴다. 고아 점검은 목록만 하고 지우지 않는다. 모든 시나리오에서 「변경 명령 대상 = 임시 이름」 불변식을 가짜 gcloud 로그로 단언(Task 1·2) |
| T-04.4-12 | Elevation of Privilege | 실수로 production 실행 | high | mitigate | **실수 방지 수준**: 트리거는 `workflow_dispatch`뿐, main만, production은 `plant8-prod-db` 정확 입력을 워크플로 첫 단계와 스크립트 공통 준비(`rehearse`·`finalize` 같은 규칙)에서 확인. 04.4-06 Task 3은 staging만 돌린다(Task 2 확인 1 · 워크플로 2). 악의적 우회는 T-04.4-14 |
| T-04.4-13 | Tampering | 워크플로 셸 인젝션 | high | mitigate | 입력은 `env:`로만 넘기고 `run:` 본문에 `${{`가 없다(워크플로 4). 스크립트는 입력을 enum·숫자로 검증하고 상태 파일을 `source`하지 않는다 |
| T-04.4-14 | Elevation of Privilege | 브랜치에서 고친 워크플로로 확인·main 검사 우회(Codex #1) | high | accept | 확인 입력과 main 검사는 **권한 부여가 아니라 실수 방지 장치**다. WIF는 저장소로만 묶여 있고(`scripts/bootstrap-gcp.sh` 100행) 배포 계정은 `cloudsql.admin`을 가지므로, 저장소 write 권한이 있으면 어느 브랜치에서든 production 권한을 얻는다 — 실제 경계는 1인 소유 비공개 저장소의 write 권한이다. 이 잔여 위험을 수용하고 이 페이즈는 IAM을 바꾸지 않는다. **Phase 8 후속**: 리허설 전용 최소 권한 SA 분리 · WIF 속성 조건(`assertion.ref`/`workflow_ref`)으로 main·지정 워크플로에만 토큰 발급. 워크플로 머리 주석과 SUMMARY에 같은 문장을 남긴다 |
| T-04.4-15 | Denial of Service (비용) | 남은 임시 인스턴스(Codex #2·#3·#4) | medium | mitigate | 부재는 성공한 정확한 이름 목록으로만 확인하고 조회 실패는 「알 수 없음」 = `cleanup` 실패(403 · 503 · 인증 만료 테스트). 삭제 전 끝나지 않은 작업 대기 · 최대 3번 삭제 재시도 · 폴링 소진은 명시적 실패. 정리 두 겹(EXIT trap + `always()` finalize), finalize에 별도 25분. 러너를 잃은 경우는 다음 실행의 고아 점검이 크게 실패시키고 `docs/RESTORE.md`가 찾기·지우기 절차를 준다(잔여) |
| T-04.4-16 | Repudiation | 누가 언제 돌렸나 · 기록 누락·중복 | low | mitigate | Actions 실행 기록(트리거한 사람)과 `run_url`이 기록 행에 남는다. 기록은 finalize 한 곳에서 실행 id + 시도당 한 번(`RECORDED` 표시 — finalize 2), 상태 파일 없거나 기록 실패는 종료 코드 1과 요약으로 드러난다(finalize 3·4) |
| T-04.4-17 | Information Disclosure | Actions 로그 | low | accept | verify 출력은 표 이름·행 수·tag뿐, 비밀·행 내용 없음. 이미지 digest는 비밀이 아니다. 실제 식별자는 `vars.*`로만 |
| T-04.4-29 | Tampering | verify와 record 사이 배포로 이미지 교체(Codex #12) | medium | mitigate | verify 직전 이미지 참조·digest를 상태 파일에 적고 finalize가 기록 직전 재조회해 다르거나 못 읽으면 성공이 아니다(이미지 1). 요약에 `이미지 <참조>@<digest>`. 배포 동시성 그룹 공유는 대기 중 배포 취소 위험 때문에 택하지 않았다 |
| T-04.4-30 | Tampering | 사고 복원 뒤 오래된 스키마에 새 이미지가 서빙(Codex #9) | high | mitigate | `docs/RESTORE.md`가 공개 접근 제거·스케줄러 정지로 쓰기를 멈추고, 복원 뒤 정상 배포 경로(migrate → seed → 서비스 배포가 공개 접근을 다시 연다)를 거쳐서만 재개하게 한다. 백업 설정 재확인 포함(Task 3 순서 단언) |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지·도구 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음 |
</threat_model>

<verification>
- `pnpm test:unit` 전체 통과(workflows · ci-guard · docs-limits · deploy-sh 포함)
- `pnpm lint` · `pnpm typecheck` 통과
- `bash -n scripts/restore-rehearsal.sh` 통과
- `wc -l docs/OPERATIONS.md` ≤ 270 · `wc -l docs/RESTORE.md` ≤ 150
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml test/unit/deploy/fakebin scripts/bootstrap-gcp.sh .planning/STATE.md` 비어 있음
</verification>

<success_criteria>
- 워크플로 하나가 원본을 건드리지 않고 복원·확인·삭제·기록을 한다(가짜 gcloud로 증명)
- 이름 가드와 production 확인(실수 방지)이 `rehearse`·`finalize` 모두에서 첫 gcloud 전에 멈춘다
- 확인 실패·중단에도 임시 인스턴스가 정확한 이름으로 지워지고, 남거나 알 수 없으면 `cleanup` 실패로 보인다
- 결과는 마지막 정리 뒤 한 번만 기록되고, 이미지가 바뀌면 성공이 아니다
- OPERATIONS.md §14(20줄 이하)와 RESTORE.md에 리허설·고아 정리·사고 복원 절차가 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 워크플로 | `.github/workflows/restore-rehearsal.yml`(입력 `env` · `confirm_production`) |
| 스크립트 | `scripts/restore-rehearsal.sh`(`guard` · `rehearse` · `finalize`) |
| 러너 상태 파일(실행 중에만) | `$RUNNER_TEMP/restore-rehearsal-<run id>-<attempt>.state` |
| GCP 리소스(실행 시 생성·삭제) | 임시 Cloud SQL `plant8-<env>-rehearsal-<run id>-<attempt>` |
| 테스트 도구 | `test/unit/deploy/restore-fakebin/gcloud` · `test/unit/deploy/restore-fakebin/sleep` |
| 테스트 | `test/unit/deploy/restore-rehearsal-sh.test.ts` · `test/unit/deploy/restore-rehearsal-workflow.test.ts` · `test/unit/docs-limits.test.ts`(토큰 4 + RESTORE.md 묶음) |
| 문서 | `docs/OPERATIONS.md` §14 백업·복원 · §10 · §12 한 문장씩 · `docs/RESTORE.md`(새 런북) |

## Review Dispositions Ledger

### Round 1 — uncommitted (Codex GPT 적대적 계획 리뷰, 세션 scratchpad `codex-plan-final.md`, 검토 대상 계획 커밋 `7a1eca3`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #1 production 확인이 WIF 신원으로 우회된다 | critical | 재구성하여 수용: truth 11 · T-04.4-14 · 워크플로 머리 주석이 확인 입력·main 검사를 **실수 방지 장치**로 규정하고, 실제 경계(1인 소유 비공개 저장소 write 권한)와 잔여 위험 수용을 명시. 이 페이즈 IAM 변경 없음(`bootstrap-gcp.sh` diff 비어 있음 단언). Phase 8 후속: 리허설 전용 최소 권한 SA · WIF ref/workflow 속성 조건 |
| #2 조회 실패를 부재로 취급 | high | Plan 03, Task 2: 부재는 성공한 정확한 이름 목록으로만, 실패는 「알 수 없음」 = `cleanup` 실패 · 「확인 불가」 요약. 가짜가 403 · 503 · 인증 만료를 따로 모델링하고 describe NOT_FOUND를 섞어 둔다 |
| #3 취소·시간 초과·진행 중 작업에서 정리가 보장되지 않음 | high | Plan 03, Task 2: 삭제 전 작업 대기 · 최대 3번 재시도 · 폴링 소진 명시 실패 · 시작 전 고아 점검(있거나 못 보면 크게 실패) · 단계별 시간 제한(60/25/90) · 중단(kill) 뒤 finalize 테스트. Task 3: RESTORE.md 고아 찾기·지우기 |
| #4 대체 정리가 기록 계약을 못 지킴 | high | Plan 03, Task 1·2: 기록은 실행 id + 시도로 키가 붙은 상태 파일을 읽는 `finalize` 하나가 마지막 정리 뒤 한 번만. 상태 파일 없음 → 기록 없이 종료 코드 1, 기록 실패 → 종료 코드 1. EXIT trap은 기록하지 않는다 |
| #9 사고 복원 런북이 오래된 스키마에 새 이미지를 올린다 | high | Plan 03, Task 3: `docs/RESTORE.md` — 쓰기 중단(공개 접근 제거 · 스케줄러 정지 · 워크플로 중지 · 실행 중 Job 대기) → 복원 → 정상 배포 경로로 migrate·seed(여기서 재개) → 확인 → 재개·공지 → 백업 설정 재확인. OPERATIONS §14는 20줄 안에서 링크 |
| #12 verify와 record가 다른 이미지를 쓸 수 있음 | medium | Plan 03, Task 1·2: verify 직전 이미지 참조·digest 고정, finalize가 기록 직전 비교(다르거나 못 읽으면 성공 아님), 요약에 digest. 배포 동시성 그룹 공유는 대기 중 배포가 취소될 수 있어 택하지 않음(워크플로 주석) |
| 체커 info(03 T1) | info | 한 규칙: gcloud를 부르는 모든 하위 명령(`rehearse` · `finalize`)이 첫 gcloud 전에 같은 공통 준비(production 확인 포함)를 거친다 — 확인 1이 두 하위 명령 모두를 검사 |
| #20 테스트가 구현 뒤에 온다 | low | Plan 03: 세 task 모두 ①이 실패 테스트 먼저, Task 1은 행복 경로만 구현 |
| UI-SPEC 확정본 3 · Codex 2차 #2 (결과 단계 우선순위) | high | Plan 03: truth 8과 Task 2 finalize 판정을 **정리 &gt; 복원 &gt; 검증** 한 규칙으로 고치고(판정은 finalize 한 곳), Task 2에 겹친 실패 ⓐ~ⓓ 테스트를 더함. 화면 쪽 복합 실패 문구 행은 Plan 01 Task 2 단위 테스트, 렌더는 Plan 05 Task 1 E2E |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-03-SUMMARY.md` when done
</output>
