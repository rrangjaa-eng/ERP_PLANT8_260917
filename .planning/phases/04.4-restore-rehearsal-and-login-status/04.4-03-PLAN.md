---
phase: 04.4-restore-rehearsal-and-login-status
plan: 03
type: execute
wave: 3
depends_on: ["04.4-02"]
files_modified:
  - scripts/restore-rehearsal.sh
  - test/unit/deploy/restore-fakebin/gcloud
  - test/unit/deploy/restore-fakebin/sleep
  - test/unit/deploy/restore-rehearsal-sh.test.ts
  - .github/workflows/restore-rehearsal.yml
  - test/unit/deploy/restore-rehearsal-workflow.test.ts
  - docs/OPERATIONS.md
  - test/unit/docs-limits.test.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`workflow_dispatch` 워크플로 `restore-rehearsal.yml` 하나(WIF, `account.yml` 패턴)가 원본의 최신 성공 자동 백업을 새 임시 Cloud SQL 인스턴스로 복원하고, 사설망 Job(`plant8-{env}-restore` `verify`)으로 확인하고, 임시 인스턴스를 지운 뒤, 같은 Job의 `record`로 결과를 그 환경 자신의 DB에 남긴다(D8-08, ROADMAP 04.4 기준 1·2)"
    - "임시 인스턴스 이름은 워크플로가 만든다: `plant8-<env>-rehearsal-<GITHUB_RUN_ID>-<GITHUB_RUN_ATTEMPT>`. 이름이 `^plant8-(staging|prod)-rehearsal-[0-9]+-[0-9]+$`에 맞지 않거나, 환경 접두가 다르거나, `plant8-staging-db`·`plant8-prod-db`·원본 이름과 같으면 **gcloud 명령을 하나도 실행하기 전에** 실패한다"
    - "스크립트가 실행하는 모든 변경 명령(인스턴스 생성·백업 복원·사용자 생성·인스턴스 삭제)의 대상 인스턴스는 임시 이름 하나뿐이다 — 원본은 백업 목록 조회·디스크 크기 조회·`--backup-instance`로만 나온다"
    - "확인이 실패해도(또는 생성·복원이 실패해도) 임시 인스턴스는 정확한 이름으로 지워진다 — 스크립트의 EXIT trap과 워크플로의 `always()` 정리 단계 두 곳에서. 정리는 다른 이름을 지우지 않고, 남은 `-rehearsal-` 인스턴스 이름과 개수를 요약에 적는다"
    - "결과 단계 규칙: 삭제가 실패하면 `cleanup`, 아니면 restore(백업 없음·생성·복원·IAM 사용자 보정 실패) 또는 verify 중 먼저 실패한 것. 성공은 복원 완료 AND 확인 통과 AND 삭제 성공일 때만이다(UI-SPEC 결과 정의 #6)"
    - "production은 입력 `confirm_production`에 `plant8-prod-db`를 그대로 적어야만 돈다 — 워크플로 첫 단계(체크아웃·인증 전)와 스크립트 양쪽에서 확인한다. 워크플로는 `refs/heads/main`에서만 돌고, schedule·push·pull_request 트리거가 없다(C1)"
    - "`docs/OPERATIONS.md`에 「백업·복원」 절이 있다: 자동 백업 설정 확인법(`deploy.sh` 보관 7개) · PITR이 꺼져 있어 복원 단위가 하루 1회 자동 백업이라는 사실 · 리허설 워크플로 실행법과 production 확인 입력 · 결과 읽는 법(상태 화면·Actions 요약, 기록 전 실패는 Actions에만) · 남은 임시 인스턴스 확인 · 실제 사고 때 사람이 하는 수동 복원. 파일은 300줄 이하다(ROADMAP 04.4 기준 3, OPS-07)"
  prohibitions:
    - statement: "원본 인스턴스(`plant8-staging-db`·`plant8-prod-db`)로 복원하거나, 원본을 수정·삭제하는 명령이 스크립트에 있어서는 안 된다(C2)"
      status: resolved
      verification: test
    - statement: "production 리허설이 사람의 명시적 확인 없이 돌아서는 안 된다 — 스케줄·push·PR 트리거, 확인 입력 없는 실행, main 밖 실행이 전부 막힌다(C1)"
      status: resolved
      verification: test
    - statement: "임시 인스턴스가 조용히 남아서는 안 된다 — 남으면 결과가 `cleanup` 실패이고 요약에 이름이 보인다"
      status: resolved
      verification: test
    - statement: "워크플로 `run:` 블록 안에 `${{ … }}` 식을 직접 넣어서는 안 된다 — 입력은 전부 `env:`로 넘긴다(T-1-32)"
      status: resolved
      verification: test
    - statement: "OPERATIONS.md에 실제 프로젝트 ID·프로젝트 번호·12자리 이상 숫자(예시 백업 id 포함)를 적어서는 안 된다(D-03)"
      status: resolved
      verification: test
  artifacts:
    - path: "scripts/restore-rehearsal.sh"
      provides: "guard · rehearse · cleanup 하위 명령(set -Eeuo pipefail, infra/names.sh)"
      contains: "rehearsal"
    - path: ".github/workflows/restore-rehearsal.yml"
      provides: "workflow_dispatch(env · confirm_production) + WIF + 스크립트 위임 + always() 정리"
      contains: "workflow_dispatch"
    - path: "test/unit/deploy/restore-rehearsal-sh.test.ts"
      provides: "가짜 gcloud로 행복 경로 호출 순서 · 가드 · 실패 단계 · 정리 증명"
      min_lines: 120
    - path: "test/unit/deploy/restore-rehearsal-workflow.test.ts"
      provides: "워크플로 YAML 텍스트 단언"
      min_lines: 40
    - path: "docs/OPERATIONS.md"
      provides: "§14 백업·복원(20줄 이하)"
      contains: "restore-rehearsal.yml"
  key_links:
    - from: ".github/workflows/restore-rehearsal.yml"
      to: "scripts/restore-rehearsal.sh"
      via: "rehearse 단계와 always() cleanup 단계가 스크립트에 위임"
      pattern: "restore-rehearsal.sh"
    - from: "scripts/restore-rehearsal.sh"
      to: "scripts/restore-rehearsal-cli.ts"
      via: "gcloud run jobs execute plant8-{env}-restore --args=verify,… (env 덮어쓰기) / --args=record,… (덮어쓰기 없음)"
      pattern: "jobs execute"
    - from: "scripts/restore-rehearsal.sh"
      to: "infra/names.sh"
      via: "sql_instance · job_name · DB_TIER · DB_VERSION · NETWORK"
      pattern: "names.sh"
---

<objective>
**As a** 운영 담당 관리자, **I want to** GitHub Actions에서 버튼 하나로 최신 자동 백업을 임시 인스턴스에 복원·확인·삭제하고 결과를 남기기를, **so that** 운영 DB를 건드리지 않고 「백업이 실제로 복원된다」를 증명하고, 사고 때 무엇을 해야 하는지 문서로 안다.

이 플랜은 D8-08의 워크플로 본체와 OPS-03의 복원 절차 문서를 만든다. Task 1은 **트레이서**다 — 스크립트 `rehearse` 한 번이 가드 → 백업 조회 → 임시 생성 → 복원 → IAM 사용자 보정 → verify Job → 삭제 → record Job → 요약을 가짜 gcloud 위에서 끝까지 관통한다. Task 2는 실패 경로·정리·production 확인과 워크플로 YAML로 넓힌다. Task 3은 `docs/OPERATIONS.md` 절이다.

Purpose: ROADMAP 04.4 기준 1(워크플로·이름 거부·사설망 확인·확인 실패에도 삭제)과 기준 3(문서). 실제 GCP 실행은 04.4-05 Task 3.
Output: `scripts/restore-rehearsal.sh`, `.github/workflows/restore-rehearsal.yml`, 전용 가짜 gcloud와 테스트, OPERATIONS §14.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-02-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development`를 **호출**한다. 실패를 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름과 `model`을 명시한다.
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적는다.
- **세션은 gcloud를 직접 쓰지 않는다**(CONTEXT 이미 확정된 입력). 이 플랜의 모든 테스트는 `test/unit/deploy/restore-fakebin/`의 가짜 gcloud로만 돈다 — 실제 GCP·스테이징·프로덕션에 닿는 명령을 실행하지 않는다. 가짜 gcloud는 처리하지 않는 호출에 종료 코드 1을 낸다(기존 `test/unit/deploy/fakebin/gcloud` 규약).
- 공유 가짜 `test/unit/deploy/fakebin/gcloud`는 고치지 않는다 — 전용 가짜를 새로 둔다(기존 deploy·rollback 테스트에 영향 없음).
- 이 플랜은 DB를 쓰지 않는다(단위 테스트만). 그래도 체인 순서(04.4-02 뒤, 04.4-04 앞)를 지킨다.
- `docs/OPERATIONS.md`는 지금 248줄, 상한 300줄이다. 04.2·04.3·Phase 8도 이 파일에 절을 더하므로 이 플랜은 **§14를 머리글 포함 20줄 이하**로 쓰고 파일 전체를 270줄 이하로 남긴다(D8).
- 새 의존성 없음(`actionlint`·`shellcheck` 도입 금지 — RESEARCH Environment Availability). `git push` 하지 않는다. `.planning/STATE.md`를 고치지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 엣지 truth를 이 플랜이 적었다: **가드가 첫 gcloud보다 먼저**(truth 2) · **변경 명령의 대상은 임시 이름뿐**(truth 3) · **실패 시에도 정확한 이름 정리**(truth 4) · **단계 우선순위**(truth 5) · **production 확인**(truth 6). OPS-03 프로브 행 중 「복원 워크플로」와 「문서」 부분을 닫는다.

**RESEARCH Open Questions 처리(C6)**
1. OQ1(복원 대상이 먼저 있어야 하는가): 문서 대신 설치된 API 정의로 답했다 — `node_modules/@googleapis/sqladmin/build/v1.d.ts`에서 `instances.restoreBackup`은 **대상 인스턴스 리소스의 메서드**이고 `restoreBackupContext{backupRunId, instanceId, project}`를 받는다. 그래서 대상은 먼저 있어야 하고, 스크립트는 생성 → RUNNABLE → 복원 순서다. 같은 파일의 `restoreInstanceSettings`(「새 인스턴스 복원에만」) 경로는 쓰지 않는다. docs.cloud.google.com은 이 세션에서 EGRESS_BLOCKED였다 — 불확실성은 04.4-05 Task 3 실측으로 닫는다
2. OQ2(env 덮어쓰기로 다른 인스턴스에 붙는가): 04.4-02 probe_fallback 가정 1 — 거짓 성공 불가 설계 + 실측
3. OQ3(REQUIREMENTS 추적표): 이 계획 단계에서 이미 고쳤다(OPS-03 → Phase 04.4)

**계획 가정**
1. A3: 복원이 IAM DB 사용자(`plant8-<env>-runtime@<프로젝트>.iam`)를 그대로 가져온다고 가정하지 않는다 — 복원 뒤 `gcloud sql users list`로 보고 없으면 `--type=cloud_iam_service_account`로 만든다(deploy.sh `ensure_sql_db_users` 모양, 멱등)
2. A4: PITR은 켜지 않는다(비용·요구사항은 「최신 자동 백업 복원」). 문서에 「복원 단위는 하루 1회 자동 백업, 그 뒤 입력은 사라진다」를 적는다
3. A7: `gha-deployer`의 `roles/cloudsql.admin`이 인스턴스 생성·삭제를 포함한다(deploy.sh가 같은 계정으로 생성한다) — 실측은 04.4-05 Task 3
4. A12: 생성·복원 대기는 deploy.sh와 같은 RUNNABLE 폴링(10초 × 90)으로 한다. 임시 인스턴스는 GA `gcloud sql instances create`(beta는 `--storage-auto-increase-limit` 때문에만 필요했다)
5. 임시 인스턴스 크기: 원본 `settings.dataDiskSizeGb`와 같은 HDD, `DB_TIER`·`DB_VERSION`·ENTERPRISE·ZONAL·사설 IP·IAM 인증 플래그, 자동 백업 끔(`--no-backup`), 삭제 보호 없음. 비용은 db-f1-micro 수십 분 수준(D-06)
6. 같은 환경에서 두 리허설이 겹치지 않게 `concurrency` 그룹을 둔다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — `restore-rehearsal.sh rehearse` 한 번이 가짜 gcloud 위에서 가드 → 백업 → 생성 → 복원 → verify → 삭제 → record → 요약을 끝까지 간다</name>
  <files>scripts/restore-rehearsal.sh, test/unit/deploy/restore-fakebin/gcloud, test/unit/deploy/restore-fakebin/sleep, test/unit/deploy/restore-rehearsal-sh.test.ts</files>
  <precondition>04.4-02가 끝나 Job 이름 `plant8-{env}-restore`와 CLI 인자 규약(`verify --target` · `record --succeeded …`)이 정해져 있다</precondition>
  <reversibility rating="costly">임시 인스턴스 이름 규칙과 가드 정규식은 한번 실제로 돌면 운영 콘솔에 그 이름의 흔적이 남는다(삭제된 이름은 한동안 재사용 불가). 실행 id가 들어가 겹치지 않으므로 바꾸는 데 드는 것은 코드 수정뿐이다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md` 117–237행(다이어그램 · Pattern 1·2) · 255–283행(Pitfall 1~3) · 341–355행(최신 성공 백업 id 조회 명령)
    - `scripts/deploy.sh` 186–260행(`ensure_sql_instance` 생성 플래그와 RUNNABLE 폴링, `ensure_sql_db_users`) · 300–360행(Job 이름·`--args` 규약)
    - `.github/workflows/account.yml`(전문) — `ENV_SHORT` 매핑, `jobs execute … --args … --wait --format='value(metadata.name)'`, `poll_logs`(12회 × 10초, severity 필터 없음), 실패 이유를 `$GITHUB_STEP_SUMMARY`에 남기는 모양
    - `scripts/promote-guard.sh` · `test/unit/deploy/promote-guard-sh.test.ts` — 가드 스크립트와 그 행동 테스트 모양
    - `test/unit/deploy/fakebin/gcloud` 1–60행 — `DEPLOY_FAKE_LOG` · `DEPLOY_FAKE_STATE` · 처리 안 된 호출 = 종료 코드 1 규약
    - `test/unit/deploy/rollback-sh.test.ts` 1–80행 — `spawnSync`로 스크립트를 돌리고 로그를 읽는 모양
    - `infra/names.sh` — `sql_instance` · `job_name` · `runtime_sa` · `DB_TIER` · `DB_VERSION` · `NETWORK`
  </read_first>
  <behavior>
    - 행복 경로: env staging · RUN_ID 123 · ATTEMPT 1이면 가짜 gcloud 로그 순서가 (1) `sql backups list --instance=plant8-staging-db` (2) `sql instances describe plant8-staging-db` 디스크 크기 (3) `sql instances create plant8-staging-rehearsal-123-1` (4) RUNNABLE 폴링 (5) `sql backups restore <id> --restore-instance=plant8-staging-rehearsal-123-1 --backup-instance=plant8-staging-db` (6) `sql users list/create --instance=plant8-staging-rehearsal-123-1` (7) `run jobs execute plant8-staging-restore --args=verify,--target,<프로젝트>:<리전>:plant8-staging-rehearsal-123-1 --update-env-vars=CLOUD_SQL_CONNECTION_NAME=<같은 값>` (8) `sql instances delete plant8-staging-rehearsal-123-1` (9) `run jobs execute plant8-staging-restore --args=record,--succeeded,true,…` — record 호출에는 `--update-env-vars`가 없다. 종료 코드 0
    - 요약 파일(`GITHUB_STEP_SUMMARY`)에 `성공` · 백업 id · 임시 인스턴스 이름 · `남은 임시 인스턴스 0`이 있다
    - record 인자의 `--run-url`은 `https://github.com/<GITHUB_REPOSITORY>/actions/runs/123`이고 `--started-at`·`--finished-at`은 `YYYY-MM-DDTHH:MM:SSZ` 모양이다
    - 전 시나리오 공통 불변식: 로그의 `instances create`·`instances delete`·`--restore-instance=`·`users create` 대상은 임시 이름뿐이다
  </behavior>
  <action>
① 전용 가짜 두 개를 먼저 만든다. `test/unit/deploy/restore-fakebin/gcloud`(bash, 실행 비트): 모든 인자를 `$DEPLOY_FAKE_LOG`에 한 줄로 적고, 이 스크립트가 쓰는 호출만 처리한다 — 백업 목록(`FAKE_NO_BACKUP=1`이면 빈 출력), 원본 디스크 크기(`10`), 인스턴스 생성(`$DEPLOY_FAKE_STATE/<이름>` 파일 생성), 상태 조회(파일이 있으면 `RUNNABLE`, 없으면 종료 코드 1), 연결 이름(`<프로젝트>:<리전>:<이름>`), 복원, 사용자 목록·생성, Job 실행(`--format` 요청 시 실행 이름 출력), 로그 조회(고정 두 줄), 인스턴스 삭제(파일 삭제), 인스턴스 목록(`$DEPLOY_FAKE_STATE`의 `-rehearsal-` 파일 이름). `FAKE_FAIL=<create|restore|users|verify|delete|record>`면 그 호출만 종료 코드 1. 처리하지 않는 호출은 종료 코드 1. `test/unit/deploy/restore-fakebin/sleep`은 즉시 0으로 끝나는 가짜(폴링이 테스트를 느리게 하지 않게 — 스크립트에 대기 시간 설정 손잡이를 만들지 않는다).

② 실패 테스트 먼저: `test/unit/deploy/restore-rehearsal-sh.test.ts`에 behavior 네 가지를 쓴다. `spawnSync("bash", ["scripts/restore-rehearsal.sh", "rehearse"])`, env는 `PATH=<restore-fakebin>:$PATH` · `DEPLOY_FAKE_LOG` · `DEPLOY_FAKE_STATE`(임시 디렉터리) · `INPUT_ENV` · `PROJECT`(가짜 값 `test-project`) · `REGION` · `GITHUB_RUN_ID` · `GITHUB_RUN_ATTEMPT` · `GITHUB_SERVER_URL=https://github.com` · `GITHUB_REPOSITORY=o/r` · `GITHUB_STEP_SUMMARY`(임시 파일). 공통 불변식 단언은 헬퍼 하나로 만들어 Task 2의 모든 시나리오에서 다시 쓴다. 실패를 확인한다.

③ `scripts/restore-rehearsal.sh`(D8-08 · C2): `set -Eeuo pipefail`, 스크립트 위치 기준으로 `infra/names.sh`를 source. 하위 명령 `guard <이름>` · `rehearse` · `cleanup`. 공통 준비 — `INPUT_ENV`를 staging → staging / production → prod로 바꾸고(그 밖은 종료 코드 2), `GITHUB_RUN_ID`·`GITHUB_RUN_ATTEMPT`가 숫자가 아니면 종료 코드 2, 원본 = `sql_instance`, 임시 = `plant8-<env>-rehearsal-<RUN_ID>-<ATTEMPT>`, 그 이름에 truth 2의 가드(정규식 · 환경 접두 · 두 운영 이름·원본과 다름)를 적용해 어기면 stderr에 이유를 적고 종료 코드 3 — **여기까지 gcloud 호출 0**. production이면 `CONFIRM_PRODUCTION`이 정확히 `plant8-prod-db`가 아닐 때 종료 코드 3(역시 gcloud 전).
`rehearse` — 시작 시각 기록 → EXIT trap 등록 → 최신 성공 자동 백업 id 조회(`--filter="type=AUTOMATED AND status=SUCCESSFUL" --sort-by=~startTime --limit=1 --format='value(id)'`, 비면 단계 restore 실패 「성공한 자동 백업이 없습니다」) → 원본 디스크 크기 조회 → probe_fallback 가정 5의 플래그로 GA `gcloud sql instances create` → RUNNABLE 폴링 → `gcloud sql backups restore <id> --restore-instance=<임시> --backup-instance=<원본> --quiet` → RUNNABLE 폴링 → IAM 사용자 보정(가정 1) → 연결 이름 조회 → `gcloud run jobs execute <job_name env restore> --region --project --args=verify,--target,<연결 이름> --update-env-vars=CLOUD_SQL_CONNECTION_NAME=<연결 이름> --wait --format='value(metadata.name)'` → 실행 이름으로 좁혀 로그를 account.yml `poll_logs`와 같은 규칙으로 읽어 Actions 로그에 찍는다(실패해도 같은 규칙). 어느 단계든 실패하면 그 단계(restore·verify)를 기억하고 trap으로 넘어간다.
EXIT trap(오류 종료 모드를 끄고 끝까지 간다) — (a) 임시 이름을 다시 가드한 뒤, 상태 조회가 성공하면(있으면) `gcloud sql instances delete <임시> --quiet`, 다시 조회해 없어졌는지 확인, 실패면 단계를 cleanup으로 덮는다 (b) 종료 시각 기록 (c) record Job 실행 — `--args=record,--succeeded,<true|false>[,--failed-stage,<단계>][,--backup-id,<id>],--started-at,<ISO>,--finished-at,<ISO>,--run-url,<URL>`, **env 덮어쓰기 없음**(그 환경 자신의 DB, C3) (d) `-rehearsal-` 인스턴스 목록(이름과 개수) (e) `GITHUB_STEP_SUMMARY`에 결과(성공/실패 · 단계) · 원본 · 백업 id · 임시 이름 · 소요 · `남은 임시 인스턴스 <개수>`(0이 아니면 이름) · 기록 성공 여부를 한국어로 적는다 (f) 성공이고 기록도 성공일 때만 종료 코드 0, 아니면 1.
`cleanup` — 공통 준비(확인 입력은 요구하지 않는다: 지우는 대상이 가드를 통과한 임시 이름 하나뿐이다) → 있으면 그 이름만 삭제 → 목록을 요약에 적는다 → 임시가 없으면 0.
출력·요약 문구는 「…니다」로 끝낸다. 변경 명령에 원본 이름을 넣는 줄이 없게 쓴다.

④ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: add restore rehearsal script with name guard and cleanup`, 본문 한국어(D8-08 · 이름 가드가 첫째 안전 조건).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/restore-rehearsal-sh.test.ts</automated>
    <fails_when>행복 경로 호출 순서 · record에 env 덮어쓰기 없음 · 요약 네 항목 · run URL·시각 모양 · 변경 명령 대상 = 임시 이름 불변식 중 하나가 어긋난다</fails_when>
    <automated>bash -n scripts/restore-rehearsal.sh &amp;&amp; test -x test/unit/deploy/restore-fakebin/gcloud &amp;&amp; test -x test/unit/deploy/restore-fakebin/sleep</automated>
    <fails_when>스크립트에 문법 오류가 있거나 가짜 두 파일에 실행 비트가 없다(git에 `100755`로 들어가야 한다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "restore-instance=.?\\\$(SOURCE|source)|instances (delete|patch|restart).*(plant8-(staging|prod)-db|SOURCE)" scripts/restore-rehearsal.sh`가 아무 줄도 찾지 않는다
    - `grep -c "set -Eeuo pipefail" scripts/restore-rehearsal.sh`가 1이다
    - `git ls-files -s test/unit/deploy/restore-fakebin`의 두 파일 모드가 `100755`다
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>가짜 gcloud 위에서 리허설 한 번이 끝까지 가고, 변경 명령이 임시 이름에만 닿으며, 결과가 그 환경의 record Job으로 넘어간다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 실패·정리·production 확인 경로와 워크플로 YAML</name>
  <files>test/unit/deploy/restore-rehearsal-sh.test.ts, test/unit/deploy/restore-fakebin/gcloud, scripts/restore-rehearsal.sh, .github/workflows/restore-rehearsal.yml, test/unit/deploy/restore-rehearsal-workflow.test.ts</files>
  <read_first>
    - `.github/workflows/account.yml`(전문 143행) · `.github/workflows/verify.yml` — WIF 블록 원문, `env:` 매핑, 스크립트 위임 모양
    - `test/unit/deploy/workflows.test.ts` 1–40행 · 169–260행 — `readWorkflow` 텍스트 단언, 「입력을 env로 넘겨 셸 인젝션을 막는다」, 「실제 프로젝트 번호·이메일 등 식별자를 담지 않는다」, `environment:` 금지, 모든 워크플로 `drizzle-kit push` 부재
    - `test/unit/ci-guard.test.ts` — 모든 워크플로를 순회하는 검사
  </read_first>
  <behavior>
    - 가드 1: `guard plant8-staging-db` · `guard plant8-prod-db` · env staging에서 `guard plant8-prod-rehearsal-1-1` · `guard plant8-staging-rehearsal-x-1` · `guard plant8-staging-rehearsal-1-1-extra` → 종료 코드 ≠ 0, 가짜 로그 0줄
    - 가드 2: `GITHUB_RUN_ID` 빈 값 또는 `INPUT_ENV=prod-db` → `rehearse` 종료 코드 ≠ 0, 로그 0줄
    - 확인 1: env production · `CONFIRM_PRODUCTION` 없음 또는 `plant8-prod-DB` → 종료 코드 ≠ 0, 로그 0줄. 정확히 `plant8-prod-db`면 진행하고 원본은 `plant8-prod-db`, 임시는 `plant8-prod-rehearsal-…`, 공통 불변식 유지
    - 실패 1: `FAKE_NO_BACKUP=1` → 생성 호출 없음, record에 `--failed-stage,restore`, `--backup-id` 없음, 종료 코드 1
    - 실패 2: `FAKE_FAIL=create` · `restore` · `users` → 단계 restore, 임시가 만들어졌으면 그 이름으로 삭제, 종료 코드 1
    - 실패 3: `FAKE_FAIL=verify` → 단계 verify, 임시 삭제 호출 있음, 요약 `남은 임시 인스턴스 0`, 종료 코드 1
    - 실패 4: `FAKE_FAIL=delete` → 단계 cleanup(앞선 성공을 덮음), 요약에 남은 임시 이름, 종료 코드 1
    - 실패 5: `FAKE_FAIL=record` → 리허설은 성공이어도 종료 코드 1, 요약에 「기록 실패」
    - 정리 1: 임시 상태 파일이 있으면 `cleanup`이 그 이름만 지우고 0; 없으면 삭제 호출 없이 0; 다른 `-rehearsal-` 파일이 있어도 지우지 않고 요약에 이름을 적는다
    - 워크플로: truth 6과 prohibitions 4의 텍스트 단언(아래 action ③)
  </behavior>
  <action>
① 실패 테스트 먼저: 위 behavior의 스크립트 시나리오를 Task 1 테스트 파일에 **더한다**(각 시나리오 끝에 공통 불변식 헬퍼). 새 `test/unit/deploy/restore-rehearsal-workflow.test.ts`에 워크플로 단언을 쓴다. 실패를 확인한다.

② 스크립트·가짜를 behavior에 맞게 고친다.

③ `.github/workflows/restore-rehearsal.yml`(C1 · D8-08): 머리 주석에 「D8-08 복원 리허설 — 원본을 건드리지 않는다, production은 확인 입력 필수, GitHub Environments 미사용(D-05), 실행 권한은 비공개 저장소 write 협업자(T-1-45)」. `on:`은 `workflow_dispatch`만 — 입력 `env`(choice staging·production, 기본 staging, 필수)와 `confirm_production`(string, 기본 빈 값, 설명 「production일 때만 plant8-prod-db를 그대로 입력」). `permissions: contents: read, id-token: write`. `concurrency` 그룹은 환경별, 진행 중 취소 없음. 잡 하나, `runs-on: ubuntu-latest`, `timeout-minutes: 90`. 단계 순서 — (1) **첫 단계**: 체크아웃·인증 전, `env:`로 받은 `REF`(github.ref) · `INPUT_ENV` · `CONFIRM_PRODUCTION`을 검사해 main이 아니거나 production인데 확인이 정확히 `plant8-prod-db`가 아니면 이유를 찍고 종료 코드 1 (2) `actions/checkout@v4` (3) `google-github-actions/auth@v3` — account.yml과 **글자 그대로 같은** `workload_identity_provider`·`service_account` (4) `google-github-actions/setup-gcloud@v3` (5) `id: rehearse`, `shell: bash`, `env:`(INPUT_ENV · CONFIRM_PRODUCTION · PROJECT · REGION), `run: bash scripts/restore-rehearsal.sh rehearse` (6) `if: ${{ always() && steps.rehearse.outcome != 'skipped' }}`, 같은 env, `run: bash scripts/restore-rehearsal.sh cleanup`. `run:` 본문에는 `${{`를 쓰지 않는다(GitHub 기본 env인 `GITHUB_RUN_ID` 등은 러너가 이미 준다). `environment:` 키 · schedule · push · pull_request 없음. 실제 프로젝트 번호·이메일 없음(`vars.*`만).

④ 워크플로 테스트 단언: `on:` 아래 트리거가 `workflow_dispatch` 하나 · `schedule:`/`push:`/`pull_request:`/`environment:` 부재 · 두 입력과 staging 기본값 · permissions 두 줄 · 첫 번째 `- ` 단계가 `refs/heads/main`과 `plant8-prod-db`를 담고 그 위치가 `google-github-actions/auth`보다 앞 · WIF provider 줄이 account.yml의 같은 줄과 동일 · `restore-rehearsal.sh rehearse`와 `always()`가 붙은 `restore-rehearsal.sh cleanup` · `timeout-minutes` · `concurrency` · `run:` 블록 본문 어디에도 `${{` 없음 · 12자리 숫자·`@` 이메일 없음.

⑤ 커밋 두 개(스크립트 실패 경로 · 워크플로), 제목 예 `feat: cover restore rehearsal failure and cleanup paths` · `feat: add restore-rehearsal workflow_dispatch workflow`, 본문 한국어(C1 · C2).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy/restore-rehearsal-sh.test.ts test/unit/deploy/restore-rehearsal-workflow.test.ts test/unit/deploy/workflows.test.ts test/unit/ci-guard.test.ts</automated>
    <fails_when>가드·확인 실패 시 gcloud가 한 번이라도 불리거나, 실패 단계·정리·요약·종료 코드 중 하나가 어긋나거나, 워크플로 텍스트 단언(트리거·첫 단계·WIF·env 매핑·always 정리) 또는 기존 워크플로 검사가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s+(schedule|push|pull_request):" .github/workflows/restore-rehearsal.yml`이 0이다
    - `grep -c "environment:" .github/workflows/restore-rehearsal.yml`이 0이다
    - `diff <(grep "workload_identity_provider" .github/workflows/account.yml) <(grep "workload_identity_provider" .github/workflows/restore-rehearsal.yml)`이 아무것도 출력하지 않는다
    - verify 명령이 초록이다
  </acceptance_criteria>
  <done>모든 실패·정리·확인 경로가 가짜 gcloud로 증명되고, 워크플로가 main·사람 트리거·확인 입력·WIF·always 정리 규칙을 텍스트로 지킨다</done>
</task>

<task type="auto">
  <name>Task 3: `docs/OPERATIONS.md` §14 백업·복원(20줄 이하)과 문서 한도 토큰</name>
  <files>docs/OPERATIONS.md, test/unit/docs-limits.test.ts</files>
  <read_first>
    - `docs/OPERATIONS.md` 1–10행(상한·D-03 식별자 금지) · 123–156행(§6 경보 · §7 계정 운영) · 205–230행(§10 상태 화면 · §12의 「Job으로 존재한다」 문장)
    - `test/unit/docs-limits.test.ts` 39–82행 — 300줄 단언, `it.each` 토큰 배열, 12자리 숫자 금지
    - `scripts/deploy.sh` 186–200행 — 백업 플래그 원문(`--backup-start-time=18:00 --retained-backups-count=7`)
  </read_first>
  <action>
① 실패 테스트 먼저: `test/unit/docs-limits.test.ts`의 OPERATIONS 토큰 배열 **끝에** `"restore-rehearsal.yml"` · `"PITR"` · `"retained-backups-count"` 세 개를 더한다(기존 항목은 그대로). 실패를 확인한다.

② `docs/OPERATIONS.md` 끝에 `## 14. 백업·복원 (OPS-03)` 절을 **머리글 포함 20줄 이하**로 더한다(D8-08, ROADMAP 04.4 기준 3). 담을 것 — (a) 자동 백업 설정: `deploy.sh`가 `--backup-start-time=18:00`(UTC) · `--retained-backups-count=7`로 켠다, 확인은 `gcloud sql instances describe plant8-<env>-db --format='value(settings.backupConfiguration)'` (b) PITR은 꺼져 있다 — 복원 단위는 하루 1회 자동 백업이고 그 뒤 입력은 복원되지 않는다 (c) 리허설: Actions → `restore-rehearsal.yml` → main에서 Run workflow, env 기본 staging. production은 `confirm_production`에 `plant8-prod-db`를 그대로 적어야만 돈다(사람만, 스케줄 없음). 최신 성공 자동 백업을 임시 인스턴스 `plant8-<env>-rehearsal-<실행 id>-<시도>`에 복원 → `plant8-<env>-restore` Job이 사설망에서 마이그레이션 버전·핵심 표를 확인 → 임시 인스턴스 삭제 → 결과 기록, 수십 분 (d) 결과 읽기: 그 환경의 `/admin/system-status` 「복원 리허설」 행(그 환경 자기 DB 기록만 — 스테이징 결과는 프로덕션 화면에 없다)과 Actions 실행 요약. 인증 실패처럼 기록 전에 끝난 실패는 Actions에만 남는다 (e) 요약의 「남은 임시 인스턴스」가 0이 아니면 콘솔에서 이름에 `-rehearsal-`가 든 인스턴스만 지운다 — 운영 인스턴스 이름과 대조한 뒤 (f) 실제 사고 때 수동 복원은 **사람이** 한다(세션은 하지 않는다): 쓰기 중단 공지 → 최신 성공 백업 id 조회(리허설 스크립트와 같은 `gcloud sql backups list` 필터) → `gcloud sql backups restore <백업 id> --restore-instance=plant8-<env>-db --backup-instance=plant8-<env>-db --project=$GCP_PROJECT_ID`(현재 데이터를 덮어쓴다 · 백업 뒤 입력은 사라진다) → 상태 화면·로그인 확인. 실제 프로젝트 ID·번호·예시 백업 id(13자리 숫자는 12자리 금지 단언에 걸린다)를 적지 않는다.

③ §10 상태 화면 문장에 「복원 리허설(결과·원본·일시·백업 id·소요, 기록 없음/확인 불가)」을 더하고, §12의 「migrate·seed·account·db-bootstrap 넷만 … Job으로 존재한다」를 restore를 포함한 다섯으로 고친다 — 두 곳 모두 줄 수를 늘리지 않게 문단 안에서 고친다.

④ `wc -l docs/OPERATIONS.md`가 270 이하인지 확인하고 SUMMARY에 줄 수를 적는다. 커밋 제목 예 `docs: add backup and restore runbook to OPERATIONS`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts test/unit/deploy/workflows.test.ts</automated>
    <fails_when>OPERATIONS.md가 300줄을 넘거나, 세 토큰 중 하나가 없거나, 12자리 숫자가 들어갔거나, `GCP_PROJECT_ID` 변수명이 사라졌다</fails_when>
    <automated>test "$(wc -l &lt; docs/OPERATIONS.md)" -le 270</automated>
    <fails_when>이 페이즈 몫 예산을 넘어 04.2·04.3·Phase 8이 쓸 자리가 30줄 미만으로 줄었다</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^## 14\./,$p' docs/OPERATIONS.md | wc -l`이 20 이하다
    - 그 범위에 `restore-rehearsal.yml` · `PITR` · `retained-backups-count` · `plant8-prod-db` · `-rehearsal-`가 모두 있다
    - `grep -cE "[0-9]{12}" docs/OPERATIONS.md`가 0이다
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>운영자가 OPERATIONS.md 한 절만 보고 백업 설정 확인 · 리허설 실행 · 결과 읽기 · 남은 인스턴스 정리 · 사고 때 수동 복원을 할 수 있고, 문서 한도가 지켜진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사람(write 협업자) → `workflow_dispatch` 입력 | env · confirm_production 문자열 |
| GitHub 러너(WIF, `gha-deployer` = `roles/cloudsql.admin`) → Cloud SQL Admin API | 잘못된 대상이면 운영 인스턴스를 덮어쓸 수 있는 권한 |
| 러너 → Cloud Run Job 실행(인자·env 덮어쓰기) | 어느 DB를 읽고 어디에 기록할지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-11 | Tampering | 원본 인스턴스 덮어쓰기·삭제 | critical | mitigate | 임시 이름을 스크립트가 만들고 첫 gcloud 전에 정규식·환경 접두·운영 이름 비교로 가드. 변경 명령은 임시 이름 변수만 쓴다. 모든 시나리오에서 「변경 명령 대상 = 임시 이름」 불변식을 가짜 gcloud 로그로 단언(Task 1·2) |
| T-04.4-12 | Elevation of Privilege | 실수로 production 실행 | high | mitigate | 트리거는 `workflow_dispatch`뿐, main만, production은 `plant8-prod-db` 정확 입력을 워크플로 첫 단계와 스크립트 양쪽에서 확인. 04.4-05 Task 3은 staging만 돌린다(Task 2 확인 1 · 워크플로 단언) |
| T-04.4-13 | Tampering | 워크플로 셸 인젝션 | high | mitigate | 입력은 `env:`로만 넘기고 `run:` 본문에 `${{`가 없다(워크플로 단언). 스크립트는 입력을 enum·숫자로 검증한다 |
| T-04.4-14 | Tampering | 브랜치에서 고친 워크플로로 가드 우회 | medium | accept | 기존 신뢰 경계 T-1-45(비공개 저장소 write 협업자만 실행 가능)를 그대로 쓴다. main 검사는 사고 방지용이지 악의적 협업자 방어가 아니다. GitHub Environment required reviewers는 쓸 수 없다(2026-09-18 결정 C) |
| T-04.4-15 | Denial of Service (비용) | 남은 임시 인스턴스 | medium | mitigate | EXIT trap + `always()` 정리 두 겹, 정확한 이름 삭제 후 재조회, 남은 `-rehearsal-` 인스턴스 이름·개수를 요약에 적고 삭제 실패는 `cleanup` 실패로 기록. `timeout-minutes: 90` |
| T-04.4-16 | Repudiation | 누가 언제 돌렸나 | low | accept | Actions 실행 기록(트리거한 사람)과 `run_url`이 기록 행에 남는다 |
| T-04.4-17 | Information Disclosure | Actions 로그 | low | accept | verify 출력은 표 이름·행 수·tag뿐, 비밀·행 내용 없음. 실제 식별자는 `vars.*`로만 |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지·도구 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음 |
</threat_model>

<verification>
- `pnpm test:unit` 전체 통과(workflows · ci-guard · docs-limits · deploy-sh 포함)
- `pnpm lint` · `pnpm typecheck` 통과
- `bash -n scripts/restore-rehearsal.sh` 통과
- `wc -l docs/OPERATIONS.md` ≤ 270
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml test/unit/deploy/fakebin .planning/STATE.md` 비어 있음
</verification>

<success_criteria>
- 워크플로 하나가 원본을 건드리지 않고 복원·확인·삭제·기록을 한다(가짜 gcloud로 증명)
- 이름 가드와 production 확인이 첫 gcloud 전에 멈춘다
- 확인 실패에도 임시 인스턴스가 정확한 이름으로 지워지고, 남으면 보인다
- OPERATIONS.md에 복원 절차가 20줄 이하로 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 워크플로 | `.github/workflows/restore-rehearsal.yml`(입력 `env` · `confirm_production`) |
| 스크립트 | `scripts/restore-rehearsal.sh`(`guard` · `rehearse` · `cleanup`) |
| GCP 리소스(실행 시 생성·삭제) | 임시 Cloud SQL `plant8-<env>-rehearsal-<run id>-<attempt>` |
| 테스트 도구 | `test/unit/deploy/restore-fakebin/gcloud` · `test/unit/deploy/restore-fakebin/sleep` |
| 테스트 | `test/unit/deploy/restore-rehearsal-sh.test.ts` · `test/unit/deploy/restore-rehearsal-workflow.test.ts` · `test/unit/docs-limits.test.ts`(토큰 3) |
| 문서 | `docs/OPERATIONS.md` §14 백업·복원 · §10 · §12 한 문장씩 |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-03-SUMMARY.md` when done
</output>
