---
phase: 04.4-restore-rehearsal-and-login-status
plan: 03
status: complete
subsystem: infra
tags: [restore-rehearsal, github-actions, cloud-sql, bash, runbook, fakebin]

requires:
  - phase: 04.4-01
    provides: "record CLI — --run-key · stored_outcome=<…> run_key=<…> 한 줄 · 같은 키 다른 결과 종료 코드 4"
  - phase: 04.4-02
    provides: "Cloud Run Job plant8-{env}-restore · verify --target <연결 이름> + CLOUD_SQL_CONNECTION_NAME 덮어쓰기"
provides:
  - "scripts/restore-rehearsal.sh — guard · rehearse · finalize(마감 하나 · gc() 래퍼 · 상태 파일 · 판정 정리 > 복원 > 검증)"
  - ".github/workflows/restore-rehearsal.yml — workflow_dispatch(env · confirm_production), WIF, 자기 동시성 그룹 restore-rehearsal, always() finalize"
  - "test/unit/deploy/restore-fakebin/{gcloud,sleep,date,timeout} — 실패 손잡이와 가짜 시계"
  - "docs/RESTORE.md — 리허설 실행·결과 읽기 · 고아 정리 · 사고 복원 런북"
  - "docs/OPERATIONS.md §14 백업·복원"
affects: [04.4-04, 04.4-05, 04.4-06]

tech-stack:
  added: []
  patterns:
    - "모든 gcloud를 gc <예비 초> 하나로 부르고 마감까지 남은 초로 timeout — 0 이하면 부르지 않고 124"
    - "가짜 timeout이 FAKE_UNDER_TIMEOUT을 넘기고 가짜 gcloud가 없으면 UNBOUNDED를 찍어, 모든 시나리오에서 timeout 밖 호출을 잡는다"
    - "스크립트 셸 kill은 가짜가 조상 프로세스를 거슬러 스크립트 셸만 골라 kill -KILL(EXIT trap이 못 도는 중단 흉내)"

key-files:
  created:
    - scripts/restore-rehearsal.sh
    - .github/workflows/restore-rehearsal.yml
    - docs/RESTORE.md
    - test/unit/deploy/restore-rehearsal-sh.test.ts
    - test/unit/deploy/restore-rehearsal-workflow.test.ts
    - test/unit/deploy/restore-fakebin/gcloud
    - test/unit/deploy/restore-fakebin/sleep
    - test/unit/deploy/restore-fakebin/date
    - test/unit/deploy/restore-fakebin/timeout
  modified:
    - docs/OPERATIONS.md
    - .github/workflows/ci.yml
    - .github/workflows/deploy.yml
    - test/unit/docs-limits.test.ts
    - test/unit/ci-guard.test.ts
    - test/unit/deploy/workflows.test.ts

key-decisions:
  - "OPERATIONS.md가 계획 실측(263줄)과 달리 270줄이라, §14를 머리글 포함 5줄로 줄여 파일 276줄 상한과 후속 페이즈 몫 24줄을 지켰다"
  - "마감 4의 「정리 몫이 떨어진 뒤 sql 호출 없음」은 정리용 호출(정확한 이름 목록 · 작업 · 삭제)로 읽었다 — 요약의 접두 목록은 계획대로 예비 0이라 그 뒤에 한 번 불린다"
  - "정리가 마감으로 끊기면 마지막으로 본 상태(present)로 멈춘다 — 다시 목록을 부르려다 거부돼 unknown이 되지 않게"

requirements-completed: [OPS-03]

duration: 약 45분
completed: 2026-09-26

actuals:
  tokens: 27700
  tasks: 3
  commits: 5
  plan_head_before: b2a922caac80f2e90f28275bafddb5603e86e210
---

# Phase 04.4 Plan 03: 복원 리허설 워크플로·스크립트·런북 Summary

PLAN_BASE: `b2a922caac80f2e90f28275bafddb5603e86e210`

**`restore-rehearsal.yml` 한 번이 최신 성공 자동 백업을 사설 IP 임시 인스턴스에 복원하고, restore Job으로 확인하고, 지운 다음 finalize가 같은 실행 키로 결과를 한 번 기록한다. 모든 gcloud 호출은 마감 하나까지 남은 초로 잘린다. 리허설 도중 배포가 끼면 시작 기준선 digest와 비교해 실패로 남긴다. 사고 복원 런북 `docs/RESTORE.md`는 공개 접근을 닫은 채 복원하고 확인한다.**

## Task Commits

| Task | Commit | 내용 |
|---|---|---|
| 1 트레이서 | `65e1c00` | rehearse(기준선 → 고아 점검 → 백업 → 생성 → 복원 → IAM 사용자 → verify → EXIT 정리) + finalize(정리 → 기록 직전 이미지 → record 한 번 → 기록 뒤 이미지 → 요약) 행복 경로 |
| 2 실패·정리·기록 | `7247f7e` | 가드·production 확인·고아·실패 단계·알 수 없는 정리·작업 대기·재시도·폴링 소진·kill 뒤 finalize·판정 표·한 번 기록·저장된 결과·마감 1~4·배포 겹침 ⓐ~ⓕ·ⓓ′·이미지 확인 불가·겹친 실패 |
| 3 워크플로 | `c000640` | `restore-rehearsal.yml` + 텍스트 단언 5개(예산 합 포함) |
| 3 문서 | `4929441` | `docs/RESTORE.md` · OPERATIONS §14·§10·§12 · WR-09 재포함 목록 · docs-limits |
| 게이트 수정 | `8b98c51` | 새 테스트 두 파일의 인덱스 접근을 좁혀 typecheck 통과(아래 Deviations 1) |

## TDD

- Task 1 RED: 스크립트가 없어 5개 모두 실패했다(`No such file or directory`). 최소 구현 뒤 5/5 통과.
- Task 2 RED: 시나리오와 가짜 손잡이를 먼저 더했다. 57개가 기대한 이유로 실패했다(timeout 밖 호출 `UNBOUNDED`, 가드·실패 분기 없음). 우연히 통과한 것은 8개였다. 구현 뒤 65/65 통과. 변이 검사로 목록 실패를 `absent`로 바꾸자 4개가 실패했고, 되돌린 뒤 다시 65/65 통과했다.
- Task 3 RED: 워크플로·RESTORE.md 파일이 없어 테스트 파일 둘이 실패했다. WR-09 두 건은 목록 항목이 없어 실패했다. 구현 뒤 4 파일 93/93 통과.

## 검증(실제 출력)

- `pnpm lint` exit 0 · `pnpm typecheck` exit 0
- `pnpm test:unit` → 103 files · 1425 tests 통과
- `bash -n scripts/restore-rehearsal.sh` 통과
- `wc -l docs/OPERATIONS.md docs/RESTORE.md` → 276 · 141. §14 = 5줄
- `git diff --stat b2a922c..HEAD -- package.json pnpm-lock.yaml test/unit/deploy/fakebin scripts/bootstrap-gcp.sh .planning/STATE.md db/migrations` 결과가 비어 있다. 새 의존성·마이그레이션·IAM 변경은 없다
- restore-fakebin 네 파일 모드 `100755`
- `--args=record` 1줄, 주석 밖 `instances delete` 1줄이고 둘 다 `--async`. 원본 대상 변경 grep 0줄, `SOURCE=` 정의 1줄
- 워크플로에는 schedule·push·pull_request·`environment:`가 없다. WIF 줄은 account.yml과 같다(diff 없음)
- gcloud는 테스트의 가짜로만 불렀다. 실제 GCP 명령은 실행하지 않았다

## Deviations from Plan

1. **[Rule 1 - Bug] 새 테스트의 typecheck 실패:** `noUncheckedIndexedAccess` 때문에 인덱스 읽기 9곳이 TS 오류였다. Task 1 커밋 시점에는 vitest만 돌렸고 typecheck는 돌리지 않았다. SUMMARY 전 게이트에서 발견해 `8b98c51`로 좁혔다. 단언은 바꾸지 않았다.
2. **OPERATIONS.md 기준 줄 수:** 계획은 263줄을 전제했지만 실측은 270줄이다. §13 Codex 절이 앞선 머지로 들어온 것으로 보이며, 얕은 이력이라 추적하지 못했다. §14를 12줄이 아니라 5줄(머리글 · 빈 줄 · 세 줄)로 써서 276줄 상한과 24줄 몫을 그대로 지켰다. 긴 설명은 RESTORE.md 1절에 두었다.
3. **마감 4 단언 해석:** 「정리 몫이 떨어진 뒤 `sql` 호출 없음」을 정리용 호출로 한정했다. 계획이 요약의 접두 목록을 예비 0으로 정해서, 그 목록은 정리 몫이 떨어진 뒤에 한 번 불린다. 테스트는 그 시점 이후의 sql 호출이 `name~^` 접두 목록뿐인지를 단언한다.
4. **가짜 손잡이 모양:** 「카운터 파일로 조절」 대신 `FAKE_LIST_FAIL_AFTER_CREATE=1`(생성 표시 파일)로 「생성 뒤부터 실패」를 흉내 냈다.
5. **TDD 순서:** 계획 Task 1 ③은 공통 준비 가드까지 적었다. 그러나 가드의 빨간 테스트가 Task 2에 있어 가드 분기는 Task 2에서 구현했다(실행 노트 「실패 분기는 Task 2의 빨간 테스트 뒤」를 따름).
6. **작은 추가:** 가드 통과 사례와 `GITHUB_RUN_ATTEMPT` 숫자 아님 사례 테스트를 더했다. verify 실패 때 `executions list`로 실행을 찾아 실패 로그를 stderr에 찍는다(account.yml 모양). 고아가 있거나 조회에 실패하면 rehearse도 요약에 한 줄 남긴다. `ci-guard.test.ts` 주석의 「5개」를 「6개」로 고쳤다(제목과 같은 목록).

## 독립 검토

**Codex 한도 때문에 Opus 독립 검토로 대신했다. 한도가 풀리면(2026-09-29 이후) Codex로 다시 확인해야 한다.** 이 실행기에는 서브에이전트를 띄우는 도구가 없었다. 그래서 이번 검토는 실행기 자신의 코드 대조와 변이 검사 1건뿐이다. **오케스트레이터가 별도 Opus 검토를 돌려야 독립 검토가 된다.** 자체 대조에서 막는 문제는 찾지 못했다. 참고 사항:
- 형식 경로(`spec.template.spec.template.spec.containers[0].image` · `image_summary.digest` · 실행 `spec.template.spec.containers[0].image`)와 `--async` 동작은 04.4-06 Task 3 실측으로 확인해야 한다. 경로가 틀리면 「이미지 확인 불가」 = 실패로 끝난다. 거짓 성공은 나지 않는다
- `sql instances list --filter=name=<임시>` 결과는 `grep -qxF`로 정확한 줄만 존재로 본다. gcloud 필터가 부분 일치로 동작해도 부재를 잘못 확정하지 않는다
- T-04.4-14 잔여 위험(WIF 저장소 범위 · cloudsql.admin)은 수용했다. 워크플로 머리 주석에 같은 문장을 남겼다. Phase 8 후속 과제다

## Known Stubs

없음.

## Next Phase Readiness

- 04.4-06 Task 3의 staging 실제 1회가 형식 경로 · `--async` · 복원 동기 종료(E-08)를 실측한다
- 한계: 러너 자체를 잃으면(강제 종료) 두 정리가 모두 못 돈다. 다음 실행의 고아 점검이 잡고, RESTORE.md 2절로 사람이 지운다

## Self-Check: PASSED

- 파일 9개(created) 존재 확인, 커밋 `65e1c00` · `7247f7e` · `c000640` · `4929441` · `8b98c51` 존재 확인
