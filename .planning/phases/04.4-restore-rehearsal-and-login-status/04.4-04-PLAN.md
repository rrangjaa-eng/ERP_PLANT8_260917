---
phase: 04.4-restore-rehearsal-and-login-status
plan: 04
type: execute
wave: 4
depends_on: ["04.4-03"]
files_modified:
  - db/schema/auth.ts
  - db/migrations/0031_users_first_login_at.sql
  - db/migrations/meta/0031_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/users.ts
  - domain/auth/hooks.ts
  - domain/people/index.ts
  - app/(app)/admin/people/person-status.ts
  - app/(app)/admin/people/page.tsx
  - app/(app)/admin/people/people.module.css
  - test/integration/people-login-status.test.ts
  - test/integration/first-login-backfill.test.ts
  - test/unit/people/person-status.test.ts
  - test/e2e/people.spec.ts
  - test/e2e/mobile-people.spec.ts
  - docs/design/SYSTEM.md
  - docs/design/DECISIONS.md
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사람 목록(`/admin/people`) 「상태」 칸이 두 칼럼에서 독립적으로 배지를 만든다: `first_login_at`이 NULL이면 `첫 로그인 전`, `password_is_temporary`가 참이면 `임시 비밀번호 사용 중` — 둘 다 `StatusTag kind=\"muted\" variant=\"text\"`, 둘이면 PC에서 `·`로 잇고, 없으면 `—`(D8-07, ROADMAP 04.4 기준 4, UI-SPEC 배지 조합 규칙 · zero-one-many covered)"
    - "보관된 사람은 `보관됨`만 보이고 두 로그인 배지는 억제된다. 정보 노출표에서 `person.value`를 못 보는 계급에게는 두 필드가 DTO에 없고(필드 부재) 배지를 그리지 않는다 — NULL/false로 취급하지 않는다(UI-SPEC partial covered)"
    - "`first_login_at`은 better-auth `after` 훅의 로그인 성공 분기에서 값이 NULL일 때만 한 번 채워진다 — `recordAction(\"login\")`과 별개의 직접 호출이라 `action_log.optional_types`에서 로그인을 꺼도 채워진다. 로그아웃·비밀번호 변경·관리자 재발급은 이 값을 지우지 않는다"
    - "통합 흐름: 발급 직후 → 두 배지 · 첫 로그인 → `첫 로그인 전` 사라짐 · 비밀번호 변경 → `임시 비밀번호 사용 중` 사라짐 · 관리자 재발급 → `임시 비밀번호 사용 중` 다시 보임(`첫 로그인 전`은 다시 보이지 않음). 비밀번호 변경 강제는 없다(D-08 유지)"
    - "사람 목록 조회에 사람별 쿼리를 더하지 않는다 — `listPeople` 한 번에서 `first_login_at`을 담은 SQL은 사람 수와 무관하게 정확히 1개(`listUsers`의 기존 select)다(issue #56)"
    - "마이그레이션이 기존 사용자를 채운다: 각자의 가장 이른 `action_log` login 행 시각(`pruned_at`과 무관) → 없고 `password_is_temporary = false`면 마이그레이션 시각 → 그 밖은 NULL. 이 SQL은 `-- 04.4 D8-07 first_login_at backfill` 표지 뒤에 있고 `pnpm lint:sql`을 통과한다(UI-SPEC 백필 규칙, B4)"
    - "새 두 필드 `firstLoginAt`·`passwordIsTemporary`는 `PERSON_DTO_SPEC`에서 `person.value`에 매핑되어 누수 스캔 DTO 축에 자동으로 들어간다(ROADMAP 04.4 기준 5)"
    - "폰(<700px)에서는 두 배지가 `·` 없이 세로로 쌓이고(간격 `--s-1`), 각 배지 글자는 줄바꿈되지 않는다. 「상태」 칸은 말줄임 없이 줄바꿈된다(UI-SPEC Responsive #19~20 · overflow covered). 목록은 서버 렌더라 새 로딩 구간이 없다"
    - "SYSTEM.md §7-5에 두 긴 배지 낱말이 길이 규칙 예외로 적히고, DECISIONS.md에 그 이유 항목이 있다(UI-SPEC 갱신 ①, CLAUDE.md 「화면 하나만 예외 금지」)"
  prohibitions:
    - statement: "비밀번호 변경을 강제하는 흐름(로그인 차단·강제 이동)을 더해서는 안 된다(D-08)"
      status: resolved
      verification: test
    - statement: "`first_login_at`을 로그아웃·비밀번호 변경·관리자 재발급·세션 정리에서 지우거나 덮어써서는 안 된다 — 쓰기는 NULL일 때만 하는 조건부 UPDATE 하나뿐이다"
      status: resolved
      verification: test
    - statement: "`sessions` 행을 「첫 로그인 전」 판정 근거로 써서는 안 된다(ROADMAP 04.4 기준 4)"
      status: resolved
      verification: judgment
    - statement: "사람 목록 표시를 위해 사람마다 도는 쿼리를 더해서는 안 된다(issue #56)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/auth.ts"
      provides: "users.first_login_at(timestamp, NULL 허용)"
      contains: "first_login_at"
    - path: "db/migrations/0031_users_first_login_at.sql"
      provides: "ADD COLUMN first_login_at + 표지 뒤 백필 두 문장(journal idx 31)"
      contains: "04.4 D8-07 first_login_at backfill"
    - path: "repositories/users.ts"
      provides: "setFirstLoginAtIfUnset(viewer, userId, at) — IS NULL 조건부 UPDATE"
      exports: ["setFirstLoginAtIfUnset"]
    - path: "app/(app)/admin/people/person-status.ts"
      provides: "personLoginStatus — 보관됨 / 배지 0·1·2개 판정(필드 부재 존중)"
      exports: ["personLoginStatus"]
    - path: "test/integration/people-login-status.test.ts"
      provides: "발급 → 로그인 → 변경 → 재발급 흐름, 로그인 로그 끔, 1회성, 쿼리 불변식"
      min_lines: 80
    - path: "test/integration/first-login-backfill.test.ts"
      provides: "마이그레이션의 표지 뒤 백필 SQL을 그대로 실행해 세 규칙 증명"
      min_lines: 40
  key_links:
    - from: "domain/auth/hooks.ts"
      to: "repositories/users.ts"
      via: "로그인 성공 분기에서 recordAction보다 먼저 setFirstLoginAtIfUnset(SYSTEM_VIEWER, actor.id, new Date())"
      pattern: "setFirstLoginAtIfUnset"
    - from: "domain/people/index.ts"
      to: "domain/permissions/info-items.ts"
      via: "PERSON_DTO_SPEC 두 필드 → person.value"
      pattern: "firstLoginAt"
    - from: "app/(app)/admin/people/page.tsx"
      to: "app/(app)/admin/people/person-status.ts"
      via: "상태 칸 렌더"
      pattern: "personLoginStatus"
---

<objective>
**As a** 관리자, **I want to** 사람 목록에서 누가 아직 첫 로그인 전인지·임시 비밀번호를 쓰는지 한눈에 보기를, **so that** 전환 전에 전 직원 로그인 상태를 확인하고(Phase 8 체크리스트) 필요한 사람에게만 재발급한다.

이 플랜은 D8-07의 표시 부분이다. Task 1은 **트레이서**다 — `users.first_login_at` → 로그인 훅 → 리포지토리 → DTO → 사람 목록 「상태」 칸 → 브라우저까지 한 번에 관통한다. Task 2는 기존 사용자 백필과 1회성·설정 무관·재발급·쿼리 수 엣지로 넓힌다. Task 3은 폰 쌓기와 SYSTEM.md §7-5·DECISIONS.md를 맞춘다.

Purpose: ROADMAP 04.4 기준 4와 기준 5의 DTO 부분.
Output: 새 칼럼 1개(마이그레이션 1개, 백필 포함), 훅 한 줄, DTO 두 필드, 상태 칸 배지, 테스트, 문서 두 곳.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development`를 **호출**한다. 실패를 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름과 `model`을 명시한다.
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적는다.
- 체인 순서(A4): 04.4-03 뒤, 04.4-05 앞. 이 플랜은 로컬 DB를 두 번 다시 만든다(Task 2) — 다른 플랜과 동시에 돌지 않는다.
- **마이그레이션(B1 조정자 정정 · B4)**: 이 플랜의 마이그레이션은 예약 범위 안의 **0031**이다. 04.4-01이 journal 마지막 항목을 `idx: 30`으로 두었으므로 `pnpm db:generate --name users_first_login_at`이 drizzle-kit 규칙(마지막 idx + 1)대로 `0031_users_first_login_at.sql` · `meta/0031_snapshot.json` · journal 항목(idx 31)을 **바로** 만든다 — 이름 바꾸기가 필요 없다. 생성 결과가 0031이 아니면(예: 0011이나 0012) 커밋하지 말고 멈춰 보고한다(04.4-01의 0030 설정이 빠진 것이다). 백필 SQL은 생성된 파일 **끝에** 손으로 덧붙인다(0003의 손 추가 선례) — 파일 이름·`_journal.json`·스냅숏은 이 플랜에서 손대지 않는다. 이미 적용된 파일에 덧붙이므로 로컬 두 DB를 다시 만든다(`bash scripts/dev-db.sh && pnpm db:migrate` · `pnpm db:reset:test`). `drizzle-kit push` 금지, 스테이징·프로덕션 적용 금지. 04.4-05 Task 1이 병합 직전에 04.4-01 것과 합쳐 하나로 재생성하고 이 백필 블록을 다시 붙인다 — 그래서 표지 줄이 정확해야 한다.
- **건드리지 않는 것**: 사람 목록의 조회 모양(`listUsers` 한 번 + 사람별 `toPersonDto`의 기존 팀·계급 조회)은 그대로다 — issue #56 자체를 고치지 않고(Deferred) 악화만 막는다. 기존 6열 표 재설계·폰 칸 접기 없음(UI-SPEC). `test/integration/leak-scan.test.ts`는 고치지 않는다(`domain/people` 모듈이 이미 등록된다). `.planning/STATE.md` 금지.
- 로그인 기록 지점은 이메일 로그인(`/sign-in/email`) `after` 훅 하나다 — 배포는 `AUTH_PROVIDER=email`로 고정(`scripts/deploy.sh` 440행). Google 로그인 경로는 이 페이즈 범위 밖이다(가정 3).
- 새 의존성 없음. `git push` 하지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 D8-07 프로브 행이 분류되지 않았다(unresolved). 이 플랜이 스스로 적은 엣지 truth: **1회성**(truth 3) · **설정 무관**(truth 3) · **재발급 뒤 표시**(truth 4) · **보관·필드 부재**(truth 2) · **백필 세 규칙**(truth 6) · **쿼리 수 불변**(truth 5).

**계획 가정**
1. 칼럼 모양은 「첫 로그인 시각」(`first_login_at`, 1회성)이다 — 「마지막 로그인 시각」은 매 로그인 쓰기가 생기고 D8-07 판정에 필요 없다(CONTEXT Discretion)
2. 재발급 뒤 「첫 로그인 전」은 다시 보이지 않는다(한 번이라도 로그인했으면). 한 번도 로그인 안 한 사람을 재발급하면 NULL이 그대로라 두 배지가 계속 보인다(UI-SPEC 가정 1)
3. 이메일 로그인만 기록한다. 배포 설정이 Google 로그인을 켜면 그 경로는 `first_login_at`을 채우지 않는다 — 그때 이 훅을 넓히는 것은 별개 작업이다
4. 로그인 로그가 꺼진 기간에 로그인했고 아직 임시 비밀번호인 기존 사용자는 백필로 NULL이 되어 「첫 로그인 전」으로 잘못 보일 수 있다 — 다음 로그인 때 스스로 고쳐진다(자가 치유). 비밀번호를 바꾼 사람은 백필 2단계가 채우므로 해당 없다
5. 보관된 사람의 배지 억제는 UI-SPEC이 확정했다(CONTEXT에 명시 없음) — 보관된 사람은 Phase 8 「전 직원 로그인 확인」 범위 밖
6. §7-5 「Phase 4 보강」 목록 줄을 직접 고치지 않고 바로 아래에 새 줄 하나로 두 낱말을 더한다 — Phase 4가 같은 줄을 고치므로 병합 충돌을 피한다(덧붙이기 전용 원칙)
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 새 계정은 두 배지, 한 번 로그인하면 「첫 로그인 전」이 사라진다 (칼럼 · 훅 · 리포지토리 · DTO · 상태 칸 · 브라우저)</name>
  <files>db/schema/auth.ts, db/migrations/meta/_journal.json, repositories/users.ts, domain/auth/hooks.ts, domain/people/index.ts, app/(app)/admin/people/person-status.ts, app/(app)/admin/people/page.tsx, test/integration/people-login-status.test.ts, test/e2e/people.spec.ts</files>
  <precondition>로컬 Postgres가 떠 있고, `db/migrations/meta/_journal.json` 마지막 항목이 04.4-01의 `idx: 30` · `0030_restore_rehearsals`이며 로컬 두 DB에 적용되어 있다</precondition>
  <reversibility rating="costly">`users`에 칼럼을 더하는 마이그레이션은 배포되면 되돌리는 데 마이그레이션이 하나 더 든다. 아직 배포 전이고 병합 직전 재생성이 있다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 145–172행(배지 조합 규칙 · 보관됨 · 정보 노출 매핑 · 백필 규칙 · 기록 지점 · 가정 1 · Responsive)
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md` 296–317행(첫 로그인 기록 코드 예시)
    - `db/schema/auth.ts` — `passwordIsTemporary`(D-08 주석)·`archivedAt` 선언 모양(이 표의 timestamp는 tz 없음)
    - `repositories/users.ts` — `setPasswordTemporary` · `setUserArchived`(IS NULL 조건부 UPDATE) · `listUsers`
    - `domain/auth/hooks.ts` 80–110행 — `after` 훅 성공 분기, `getSessionActor`, `SYSTEM_VIEWER`
    - `test/unit/session-hooks.test.ts` — 훅 단위 테스트가 모듈을 어떻게 흉내 내는지(새 리포지토리 호출이 기존 모의를 깨지 않게)
    - `domain/people/index.ts` 40–100행 — `PersonDto` · `PersonSource = UserRow & …` · `PERSON_DTO_SPEC` · `registerDto` · `listPeople`
    - `app/(app)/admin/people/page.tsx` 70–110행 — 「상태」 칸 현재 렌더
    - `test/integration/archive.test.ts` — `auth.api.signInEmail({ body, headers: new Headers({ [CLIENT_IP_HEADER]: nextTestIp() }) })` 모양
    - `test/e2e/people.spec.ts` · `test/e2e/fixtures.ts` — 로그인과 `createFixtureUser`
  </read_first>
  <behavior>
    - 통합 1: `createAccount` 직후 관리자 viewer의 `listPeople`에서 그 사람의 `firstLoginAt`이 null, `passwordIsTemporary`가 true
    - 통합 2(트레이서): 그 계정으로 `signInEmail` 성공 뒤 `firstLoginAt`이 null이 아니고 로그인 직전·직후 시각 사이다; `passwordIsTemporary`는 여전히 true
    - 통합 3: `person.value`를 못 보는 계급 viewer(노출 행을 끈 계급)의 `listPeople` 결과에는 두 키가 없다(`"firstLoginAt" in dto`가 false)
    - E2E 1: 관리자로 로그인해 `/admin/people`을 열면 로그인한 적 없는 새 fixture 사용자 행 「상태」 칸에 `첫 로그인 전`과 `임시 비밀번호 사용 중`이 둘 다 있고, 방금 로그인한 관리자 자신의 행에는 `첫 로그인 전`이 없다
  </behavior>
  <action>
① 실패 테스트 먼저: `test/integration/people-login-status.test.ts`에 통합 1~3, `test/e2e/people.spec.ts`에 E2E 1을 **더한다**(기존 테스트는 그대로). 실패를 확인한다.

② 스키마(D8-07): `db/schema/auth.ts` users에 `firstLoginAt: timestamp("first_login_at")`(NULL 허용, 이 표의 다른 시각과 같이 tz 없음) 한 칸과 주석 한 줄(「D8-07 첫 로그인 시각 — after 훅이 NULL일 때만 한 번 쓴다. sessions는 판정 근거가 아니다」).

③ **[BLOCKING] 마이그레이션**: `pnpm db:generate --name users_first_login_at`(ADD COLUMN 1개) → 생성 파일이 `db/migrations/0031_users_first_login_at.sql` · `db/migrations/meta/0031_snapshot.json`이고 journal 마지막 항목이 `idx: 31`인지 확인(아니면 멈춤) → `pnpm lint:sql` → `pnpm db:migrate`(로컬 `erp`만). `erp_test`는 통합 global-setup이 적용한다. `drizzle-kit push`·스테이징·프로덕션 적용 금지.

④ 리포지토리: `repositories/users.ts`에 `setFirstLoginAtIfUnset(viewer, userId, at)`를 **더한다** — `users.id = userId AND first_login_at IS NULL` 조건의 UPDATE 하나(`setUserArchived` 모양), 반환 없음.

⑤ 훅: `domain/auth/hooks.ts` 성공 분기에서 `actor`를 얻은 직후, `recordAction` **앞에** `await setFirstLoginAtIfUnset(SYSTEM_VIEWER, actor.id, new Date())`를 넣는다 — `recordAction`과 독립(설정으로 꺼지는 로그인 로그에 기대지 않는다). 기존 주석과 같은 이유로 감싸지 않고 await한다. 주석 한 줄(「D8-07 — 로그인 로그 설정과 무관하게 항상」).

⑥ DTO: `PersonDto`에 `firstLoginAt: Date | null`·`passwordIsTemporary: boolean`을, `PERSON_DTO_SPEC.fields`에 두 줄(`infoItem: "person.value"`)을 **끝에** 더한다. `listPeople`·`toPersonDto`의 조회 모양은 바꾸지 않는다(두 값은 이미 `listUsers`가 읽는 `UserRow`에 있다).

⑦ 표시: 새 `app/(app)/admin/people/person-status.ts`에 순수 함수 `personLoginStatus(person)`을 만든다 — 입력은 투영된 DTO(필드가 없을 수 있다). `archivedAt`이 참이면 `{ kind: "archived" }`, 아니면 `{ kind: "badges", badges }`(`firstLoginAt` 키가 있고 null이면 `첫 로그인 전`, `passwordIsTemporary` 키가 있고 true면 `임시 비밀번호 사용 중`, 이 순서). `page.tsx` 「상태」 칸: archived → 기존 `보관됨` 그대로, 배지 0개 → `—`, 그 밖 → 감싸는 span 안에 배지마다 `<StatusTag kind="muted" variant="text">`, 둘 사이에 ` · ` 구분자 span(`aria-hidden`). 폰 모양 클래스는 Task 3이 붙인다.

⑧ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: show first-login and temporary-password badges on people list`, 본문 한국어(D8-07 · D-08 유지 · issue #56 악화 없음).
  </action>
  <verify>
    <automated>pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/people-login-status.test.ts test/integration/leak-scan.test.ts test/integration/people.test.ts</automated>
    <fails_when>새 SQL이 squawk에 걸리거나, 발급·로그인 흐름·필드 부재 중 하나가 어긋나거나, 누수 스캔이 새 필드를 잡지 못하거나, 기존 사람 통합 테스트가 깨진다</fails_when>
    <automated>pnpm exec playwright test test/e2e/people.spec.ts --project desktop</automated>
    <fails_when>새 사용자 행에 두 배지가 없거나, 로그인한 관리자 행에 「첫 로그인 전」이 남아 있거나, 기존 사람 목록 E2E가 깨진다</fails_when>
    <automated>pnpm db:generate 2&gt;&amp;1 | grep -q "No schema changes" &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm vitest run --project unit test/unit/session-hooks.test.ts test/unit/db/migration-journal.test.ts</automated>
    <fails_when>스키마·마이그레이션 불일치, 타입·린트 오류, 훅 단위 테스트 회귀, journal 가드 실패 중 하나가 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=A "$PLAN_BASE"..HEAD -- db/migrations`가 정확히 `db/migrations/0031_users_first_login_at.sql`과 `db/migrations/meta/0031_snapshot.json` 두 줄이고, 그 SQL에 `ADD COLUMN "first_login_at"`이 있다
    - `domain/auth/hooks.ts`에서 `setFirstLoginAtIfUnset` 줄이 `recordAction(actor` 줄보다 앞선다
    - `D=$(git diff "$PLAN_BASE"..HEAD -- domain/people/index.ts) && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 0을 출력한다(삭제 줄 없음 — 기존 필드·조회를 더하기만 했다)
    - 세 verify 명령이 초록이다
  </acceptance_criteria>
  <done>새 계정은 목록에서 두 배지로 보이고, 이메일 로그인 한 번으로 「첫 로그인 전」이 사라지며, 브라우저에서 같은 결과가 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 백필과 엣지 — 기존 사용자 채우기 · 1회성 · 로그인 로그 끔 · 비밀번호 변경 · 재발급 · 쿼리 수 불변 · 판정 함수</name>
  <files>db/migrations/meta/_journal.json, test/integration/first-login-backfill.test.ts, test/integration/people-login-status.test.ts, test/unit/people/person-status.test.ts, app/(app)/admin/people/person-status.ts</files>
  <read_first>
    - `db/migrations/0003_permissions_masters_spine.sql` 70–95행 — 생성 SQL 끝에 손으로 덧붙인 문장과 `--> statement-breakpoint` 선례
    - `domain/action-log/record.ts` 1–140행 — `"login"`이 `ALWAYS_ON_ACTION_TYPES`에 없고 `action_log.optional_types`로 꺼진다
    - `domain/settings/keys.ts` 39–56행 · `domain/settings/registry.ts`의 `setSettingValue` — 설정 값 쓰기 모양
    - `domain/auth/password.ts` `finalizePasswordChange` · `domain/auth/accounts.ts` `resetPassword` — 변경·재발급이 칼럼에 하는 일
    - `test/integration/projects-list.test.ts`의 `vi.spyOn(pool, "query")` 부분 — 호출 인자에서 SQL 문자열을 꺼내는 방법
    - `test/integration/setup.ts` — 매 테스트 전 TRUNCATE + 재시드(설정 기본값도 되돌아간다)
  </read_first>
  <behavior>
    - 백필 1: 사용자 A(login 행 t2·t1, t1<t2, 하나는 `pruned_at` 있음) → A는 t1; 사용자 B(login 행 없음, 임시 false) → 실행 시각 근처(null 아님); 사용자 C(login 행 없음, 임시 true) → null; 사용자 D(이미 값 있음) → 그대로; 로그인이 아닌 행(`action_type`이 login이 아님)만 있는 사용자 E(임시 true) → null
    - 흐름 1: 로그인 두 번째에도 `firstLoginAt`이 첫 값 그대로(1회성)
    - 흐름 2: `finalizePasswordChange` 뒤 `passwordIsTemporary` false, `firstLoginAt` 그대로
    - 흐름 3: 그 뒤 `resetPassword`(관리자 재발급) → `passwordIsTemporary` true, `firstLoginAt` 그대로(null로 돌아가지 않음)
    - 흐름 4: `action_log.optional_types`에서 `login`을 뺀 상태로 새 계정이 로그인하면 login 행은 0개인데 `firstLoginAt`은 채워진다
    - 흐름 5: 사람 3명에서 `listPeople` 한 번 동안 SQL 문자열에 `first_login_at`이 든 호출이 정확히 1개다
    - 판정 1~6: 보관 + 두 조건 → archived; 0개 → 빈 배열; 첫 로그인 전만; 임시만; 둘 다(순서 고정); 두 키가 없는 객체 → 빈 배열(NULL로 오인하지 않음)
  </behavior>
  <action>
① 실패 테스트 먼저: 새 `test/integration/first-login-backfill.test.ts`(백필 1) — `db/migrations/*.sql` 중 표지 줄 `-- 04.4 D8-07 first_login_at backfill`을 가진 파일이 **정확히 하나**인지 단언하고, 표지 뒤 텍스트를 `--> statement-breakpoint`로 나눠 각 문장을 `db.execute(sql.raw(…))`로 실행한 뒤 A~E를 단언한다(준비 데이터는 사용자·action_log 행 직접 삽입, 표 비우기는 setup이 한다). `test/integration/people-login-status.test.ts`에 흐름 1~5를 더한다. 새 `test/unit/people/person-status.test.ts`에 판정 1~6. 실패(표지 없음 등)를 확인한다.

② 백필(D8-07, B4): Task 1이 만든 `db/migrations/0031_users_first_login_at.sql` **끝에** 표지 주석 줄 하나와 두 문장을 덧붙인다 — (a) `users`의 `first_login_at`을 `action_log`에서 `action_type = 'login'`인 행의 `actor_id`별 `min(occurred_at)`으로 채우되 `first_login_at IS NULL`인 행만(`pruned_at` 조건 없음) (b) 여전히 NULL이고 `password_is_temporary = false`인 행을 `now()`로 채운다. 두 문장 사이와 표지 앞에 `--> statement-breakpoint`를 둔다. 파일 이름·journal·스냅숏은 손대지 않는다. `pnpm lint:sql`.

③ 로컬 두 DB를 다시 만든다(이미 적용된 파일에 덧붙였으므로): `bash scripts/dev-db.sh && pnpm db:migrate` · `pnpm db:reset:test`.

④ `personLoginStatus`를 판정 1~6에 맞춘다(Task 1 구현이 맞으면 테스트만 녹색).

⑤ 커밋 두 개(백필 · 엣지 테스트), 제목 예 `feat: backfill first_login_at from login history` · `test: cover first-login write-once and reissue edges`, 본문 한국어(D8-07 · UI-SPEC 백필 규칙).
  </action>
  <verify>
    <automated>pnpm lint:sql &amp;&amp; bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/first-login-backfill.test.ts test/integration/people-login-status.test.ts</automated>
    <fails_when>표지가 없거나 둘 이상이거나, 백필 세 규칙(가장 이른 login · 임시 false → 실행 시각 · 그 밖 NULL) 또는 흐름 1~5(1회성 · 변경 · 재발급 · 로그인 로그 끔 · 쿼리 1개) 중 하나가 어긋난다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/people/person-status.test.ts test/unit/db/migration-journal.test.ts</automated>
    <fails_when>보관·필드 부재·0/1/2개 판정이 어긋나거나 journal 가드가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -l "04.4 D8-07 first_login_at backfill" db/migrations/*.sql | wc -l`이 1이다
    - `pnpm db:generate` 재실행이 "No schema changes"다(백필은 스냅숏에 영향 없음)
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>기존 사용자가 규칙대로 채워지고, 1회성·설정 무관·재발급·쿼리 수 불변이 통합 테스트로 고정된다</done>
</task>

<task type="auto">
  <name>Task 3: 폰 세로 쌓기 · SYSTEM.md §7-5 예외 · DECISIONS.md 기록</name>
  <files>app/(app)/admin/people/people.module.css, app/(app)/admin/people/page.tsx, test/e2e/mobile-people.spec.ts, docs/design/SYSTEM.md, docs/design/DECISIONS.md</files>
  <read_first>
    - `app/(app)/admin/people/people.module.css` 70–110행 — 폰 미디어 쿼리 모양(`max-width: 699.98px`)과 `--s-1` 사용 선례
    - `test/e2e/mobile-people.spec.ts` — `mobile-375` 프로젝트에서 사람 목록을 여는 모양
    - `docs/design/SYSTEM.md` 791–797행(§7-5 — 「표 상태 열에서는 색 글자만」 · 「Phase 4 보강」 · 「길이 규칙」)
    - `docs/design/DECISIONS.md` 끝 40행 — 항목 형식(`## 날짜 — 제목` · **결정** · **왜** · **버린 대안** · **범위** · `---`)
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 176–184행(갱신 ①) · 223–232행(반려된 대안 #15·#18)
  </read_first>
  <action>
① E2E 먼저: `test/e2e/mobile-people.spec.ts`에 테스트 하나를 **더한다** — 375 폭에서 로그인한 적 없는 fixture 사용자 행의 두 배지 상자(`boundingBox`)가 세로로 쌓이고(둘째의 top ≥ 첫째의 bottom − 1) 구분자 ` · `가 보이지 않는다. 실패를 확인한다.

② CSS(토큰만, 새 값 없음): `people.module.css`에 배지 묶음 클래스(PC는 인라인)와 구분자 클래스를 더하고, 기존 폰 미디어 쿼리 모양으로 묶음을 `display: flex; flex-direction: column; gap: var(--s-1)`, 구분자를 `display: none`으로 한다. `page.tsx`의 감싸는 span·구분자 span에 두 클래스를 붙인다.

③ `docs/design/SYSTEM.md` §7-5(D8-07, UI-SPEC 갱신 ①): 「길이 규칙」 줄 바로 다음에 새 줄 하나 — 「(Phase 04.4 보강) 사람 목록 「상태」 열의 `첫 로그인 전`·`임시 비밀번호 사용 중` → `--muted` 색 글자(의미 목록의 미착수·임시). D8-07이 잠근 업무 용어라 길이 규칙의 예외다. 둘이 함께면 PC는 `·`로 잇고 폰(<700px)은 세로로 쌓는다(DECISIONS.md <실행 날짜> 기록)」. 기존 「Phase 4 보강」·「길이 규칙」 줄은 고치지 않는다(probe_fallback 가정 6).

④ `docs/design/DECISIONS.md` 끝에 `---` 뒤 항목 하나: `## <실행 날짜> — §7-5 상태 태그 길이 예외: 「첫 로그인 전」·「임시 비밀번호 사용 중」`. **결정**: 두 문구를 그대로 `--muted` 색 글자로 쓰고 폰에서 세로로 쌓는다. **왜**: D8-07이 문구를 잠갔고 Phase 8 「전 직원 로그인 확인」이 이 낱말로 읽는다 — `완료(정산)`과 같은 「업무 용어로 잠긴 상태 값」. **버린 대안**: 네 글자로 줄인 새 낱말(`미접속`·`임시`) — 잠긴 문구와 달라지고 뜻이 흐려진다 / 정상 상태 배지 「로그인 확인됨」 — 정상에는 태그를 달지 않는 관행(UI-SPEC 반려 #15) / 계정 잠금 표시 — D8-07 범위 밖(반려 #18). **범위**: SYSTEM.md §7-5, `/admin/people` 「상태」 열.

⑤ 커밋 두 개(폰 모양 · 문서), 제목 예 `feat: stack login badges vertically on phones` · `docs: record 7-5 length exception for login badges`, 본문 한국어.
  </action>
  <verify>
    <automated>pnpm exec playwright test test/e2e/mobile-people.spec.ts --project mobile-375</automated>
    <fails_when>375 폭에서 두 배지가 한 줄에 있거나 구분자가 보이거나, 기존 폰 사람 목록 E2E가 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/stylelint-config.test.ts</automated>
    <fails_when>stylelint가 토큰 밖 값을 잡거나 SYSTEM.md·DECISIONS.md 문서 불변식(참조 토큰 실재 등)이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '/### 7-5/,/### 7-6/p' docs/design/SYSTEM.md`에 `첫 로그인 전`과 `임시 비밀번호 사용 중`이 있다
    - `D=$(git diff "$PLAN_BASE"..HEAD -- docs/design/SYSTEM.md) && printf '%s\n' "$D" | grep -cE '^-[^-]'`가 0을 출력한다(삭제 줄 없음 — 더하기만)
    - `grep -c "첫 로그인 전" docs/design/DECISIONS.md`가 1 이상이다
    - `people.module.css`의 새 규칙에 `#` 색 리터럴이 없다
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>폰에서 두 배지가 세로로 쌓이고, 시스템 이탈이 SYSTEM.md와 DECISIONS.md에 같이 기록된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로그인 요청 → better-auth `after` 훅 | 성공한 세션의 사용자 id로만 쓰기 |
| DTO 투영 → 계급별 화면 | 노출표가 `person.value`를 끈 계급은 새 필드를 받지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-18 | Information Disclosure | `PersonDto` 새 필드 | medium | mitigate | 두 필드를 `PERSON_DTO_SPEC`에서 `person.value`에 매핑 → `project()`가 못 보는 계급에게서 제거, 누수 스캔 DTO 축이 자동 확인(Task 1 통합 3 · leak-scan) |
| T-04.4-19 | Tampering | `first_login_at` 쓰기 | low | mitigate | 쓰는 곳은 훅의 성공 분기 하나, 세션 actor id, IS NULL 조건부 UPDATE(1회성 — Task 2 흐름 1~3) |
| T-04.4-20 | Denial of Service | 사람 목록 성능(issue #56) | medium | mitigate | 새 필드는 기존 `listUsers` select에서만 온다 — `first_login_at` SQL이 사람 수와 무관하게 1개(Task 2 흐름 5) |
| T-04.4-21 | Repudiation | 로그인 사실 | low | accept | 감사 기록은 기존 `action_log` login 행이 맡고, `first_login_at`은 표시용 요약이다 |
| T-04.4-22 | Tampering | `_journal.json` · 손 추가 백필 | medium | mitigate | journal 가드 + 표지 줄이 정확히 한 파일에 있다는 통합 단언 + 병합 직전 재생성(04.4-05) |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` 통과
- `pnpm test:unit` 전체 통과
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test:integration` 전체 통과(leak-scan · people · auth · archive 회귀 없음)
- `pnpm exec playwright test test/e2e/people.spec.ts --project desktop` · `pnpm exec playwright test test/e2e/mobile-people.spec.ts --project mobile-375` 통과
- `pnpm db:generate` 재실행 결과 "No schema changes"
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml test/integration/leak-scan.test.ts domain/action-log domain/settings/keys.ts .planning/STATE.md` 비어 있음
</verification>

<success_criteria>
- 사람 목록에서 첫 로그인 전·임시 비밀번호 사용 중이 기존 배지 규약으로 보인다
- 발급 → 로그인 → 변경 → 재발급 흐름이 통합 테스트로 고정된다
- 기존 사용자가 규칙대로 백필된다
- 사람별 쿼리가 늘지 않는다
- 시스템 이탈이 SYSTEM.md·DECISIONS.md에 함께 기록된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 칼럼 | `users.first_login_at`(timestamp, NULL 허용) |
| 마이그레이션 | `db/migrations/0031_users_first_login_at.sql` + `meta/0031_snapshot.json`(예약 범위 0030~0034 안) + 표지 `-- 04.4 D8-07 first_login_at backfill` 뒤 백필 두 문장 — 04.4-05가 병합 직전 0030과 합쳐 하나로 재생성해 다시 0030에 둔다 |
| 함수 | `setFirstLoginAtIfUnset` · `personLoginStatus` |
| DTO | `PersonDto.firstLoginAt` · `PersonDto.passwordIsTemporary`(`person.value`) |
| 화면 | `/admin/people` 「상태」 칸 배지(`첫 로그인 전` · `임시 비밀번호 사용 중`) · `people.module.css` 배지 묶음·구분자 |
| 테스트 | `test/integration/people-login-status.test.ts` · `test/integration/first-login-backfill.test.ts` · `test/unit/people/person-status.test.ts` · `test/e2e/people.spec.ts`(확장) · `test/e2e/mobile-people.spec.ts`(확장) |
| 문서 | SYSTEM.md §7-5 한 줄 · DECISIONS.md 항목 하나 |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-04-SUMMARY.md` when done
</output>
