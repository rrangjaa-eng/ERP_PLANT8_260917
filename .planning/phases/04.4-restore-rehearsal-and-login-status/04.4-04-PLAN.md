---
phase: 04.4-restore-rehearsal-and-login-status
plan: 04
type: execute
wave: 4
depends_on: ["04.4-03"]
files_modified:
  - db/schema/auth.ts
  - db/migrations/NNNN_users_first_login_at.sql
  - db/migrations/meta/NNNN_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/users.ts
  - domain/auth/hooks.ts
  - lib/auth.ts
  - domain/people/index.ts
  - app/(app)/admin/people/person-status.ts
  - test/integration/people-login-status.test.ts
  - test/integration/first-login-hook.test.ts
  - test/integration/first-login-upgrade.test.ts
  - test/unit/people/person-status.test.ts
  - test/unit/auth/first-login-session-hook.test.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`personLoginStatus`(사람 목록 「상태」 칸의 판정)가 두 칼럼에서 독립적으로 배지를 만든다: `first_login_at`이 NULL이면 `첫 로그인 전`, `password_is_temporary`가 참이면 `임시 비밀번호 사용 중`(이 순서), 없으면 빈 배열(화면은 `—`). 화면 렌더 — 둘 다 `StatusTag kind=\"muted\" variant=\"text\"`, PC `·` 연결, 폰 세로 쌓기·칸 접기 — 는 04.4-05가 이 판정을 받아 그린다(D8-07, ROADMAP 04.4 기준 4, UI-SPEC 배지 조합 규칙 · zero-one-many covered)"
    - "보관된 사람은 `보관됨`만 보이고 두 로그인 배지는 억제된다. 정보 노출표에서 `person.value`를 못 보는 계급에게는 두 필드가 DTO에 없고(필드 부재) 배지를 그리지 않는다 — NULL/false로 취급하지 않는다(UI-SPEC partial covered)"
    - "`first_login_at`의 **유일한 기록 지점**은 `lib/auth.ts`에 등록한 better-auth `databaseHooks.session.create.after`다(UI-SPEC 「기록 지점과 설정 무관성」, Codex 3차 #1) — 로그인 방식과 무관하게 세션이 만들어질 때 `session.userId`로 `first_login_at IS NULL`인 행만 갱신하는 조건부 UPDATE 한 번. `/sign-in/email` `after` 훅(`domain/auth/hooks.ts`)에서는 쓰지 않는다(그 훅은 이메일 외 경로에서 곧바로 돌아간다). `recordAction(\"login\")`·`action_log.optional_types` 설정에 기대지 않는다. 틀린 비밀번호 · 잠긴 계정 · 보관된 계정의 로그인 시도는 세션이 없으니 NULL을 남기고, 두 번째 로그인 · 로그아웃 · 비밀번호 변경(새 세션이 생겨도 no-op) · 관리자 재발급은 값을 바꾸지 않는다(Codex #18 — 실제 훅을 지나는 통합 테스트). 단위 테스트는 처리 함수를 `/callback/google` 경로 컨텍스트와 `null` 컨텍스트로 직접 부른다"
    - "통합 흐름: 발급 직후 → 두 배지 · 첫 로그인 → `첫 로그인 전` 사라짐 · 비밀번호 변경 → `임시 비밀번호 사용 중` 사라짐 · 관리자 재발급 → `임시 비밀번호 사용 중` 다시 보임(`첫 로그인 전`은 다시 보이지 않음). 비밀번호 변경 강제는 없다(D-08 유지)"
    - "사람 목록 조회의 SQL이 늘지 않는다: `listPeople` 한 요청 안에서 정보 항목별 노출 판정을 **한 번만** 하고(요청 단위 메모를 `project()`의 `deps.visible`로 넘기고, 그 메모를 묶은 같은 `project`를 `teamAtDate`의 기존 `deps.project`로도 넘겨 팀 투영까지 같은 메모를 쓴다 — 최종 검토 #3), **사람마다 팀 소속이 있는** 3명·6명에서 센 **전체** SQL 수가 변경 전 기준값 이하이며 `visibility_matrix` SQL 수는 사람 수와 무관하게 같다. 사람별 팀·계급 조회(issue #56 자체)는 그대로 둔다(Codex #8)"
    - "마이그레이션이 기존 사용자를 채운다(UI-SPEC 백필 규칙, Codex 2차 #1): (가) 가장 이른 `action_log` login 행 시각(`pruned_at`과 무관)과 (나) `login_attempts`에서 `success = true`이고 `email = lower(users.email)`인 가장 이른 `attempted_at` 중 **더 이른 것** → 둘 다 없고 `password_is_temporary = false`면 마이그레이션 시각 → 그 밖은 NULL. 남는 오표시(수동 삭제 · `login_attempts` 도입 전 로그인만 · 이메일 변경 · Google로만 로그인한 이력)는 계획 가정으로 적는다. 이 SQL은 `-- 04.4 D8-07 first_login_at backfill` 표지 뒤에 있고 `pnpm lint:sql`을 통과한다(UI-SPEC 백필 규칙, B4). 증명은 **업그레이드 픽스처**다 — 일회용 DB에 이 페이즈 앞 마이그레이션만 적용 → 옛 모양 사용자·로그 행 삽입 → 실제 migrator로 최종 journal 적용 → 백필·장부 확인 → 다시 적용해 멱등 확인(Codex #19)"
    - "새 두 필드 `firstLoginAt`·`passwordIsTemporary`는 `PERSON_DTO_SPEC`에서 `person.value`에 매핑되어 누수 스캔 DTO 축에 자동으로 들어간다(ROADMAP 04.4 기준 5)"
    - "마이그레이션 번호는 `NNNN` — db:generate가 준 번호(브랜치 임시)를 그대로 쓰고, 머지 직전 재생성(04.4-06)으로 main 마지막+1이 된다. 이 플랜은 이름을 바꾸지 않는다"
  prohibitions:
    - statement: "비밀번호 변경을 강제하는 흐름(로그인 차단·강제 이동)을 더해서는 안 된다(D-08)"
      status: resolved
      verification: test
    - statement: "`first_login_at`을 로그아웃·비밀번호 변경·관리자 재발급·세션 정리에서 지우거나 덮어써서는 안 된다 — 쓰기는 NULL일 때만 하는 조건부 UPDATE 하나뿐이다"
      status: resolved
      verification: test
    - statement: "`sessions` 행을 「첫 로그인 전」 판정 근거로 써서는 안 된다(ROADMAP 04.4 기준 4)"
      status: resolved
      verification: judgment
    - statement: "사람 목록 표시를 위해 사람마다 도는 쿼리를 더해서는 안 된다 — 노출 판정 포함 전체 SQL 수로 센다(issue #56, Codex #8)"
      status: resolved
      verification: test
    - statement: "로컬 DB 「다시 만들기」에 `scripts/dev-db.sh`를 써서는 안 된다 — 없는 DB만 만든다. 실제 DROP/CREATE와 빈 장부 확인을 쓴다(Codex #5)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/auth.ts"
      provides: "users.first_login_at(timestamp, NULL 허용)"
      contains: "first_login_at"
    - path: "db/migrations/NNNN_users_first_login_at.sql"
      provides: "ADD COLUMN first_login_at + 표지 뒤 백필 두 문장(NNNN = db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1)"
      contains: "04.4 D8-07 first_login_at backfill"
    - path: "repositories/users.ts"
      provides: "setFirstLoginAtIfUnset(viewer, userId, at) — IS NULL 조건부 UPDATE"
      exports: ["setFirstLoginAtIfUnset"]
    - path: "domain/auth/hooks.ts"
      provides: "recordFirstLogin(session, context, deps?) — 세션 생성 뒤 처리 함수(경로·컨텍스트 무관, 설정 무관). 기존 before/after 훅은 그대로"
      exports: ["recordFirstLogin"]
    - path: "test/unit/auth/first-login-session-hook.test.ts"
      provides: "처리 함수 직접 호출 — `/callback/google` 경로 · null 컨텍스트 · 로그 설정 조회가 실패해도 기록"
      min_lines: 30
    - path: "app/(app)/admin/people/person-status.ts"
      provides: "personLoginStatus — 보관됨 / 배지 0·1·2개 판정(필드 부재 존중)"
      exports: ["personLoginStatus"]
    - path: "test/integration/people-login-status.test.ts"
      provides: "DTO 두 필드 · 필드 부재 · 발급 → 로그인 → 변경 → 재발급 표시 흐름 · 팀 소속이 있는 사람 3명/6명 전체 SQL 수 기준값 비교"
      min_lines: 80
    - path: "test/integration/first-login-hook.test.ts"
      provides: "실제 better-auth 훅을 지나는 첫 로그인 기록 — 성공만 채움, 실패·잠김·보관은 NULL, 이후 로그인·로그아웃·변경·재발급·로그 끔에서 보존"
      min_lines: 60
    - path: "test/integration/first-login-upgrade.test.ts"
      provides: "일회용 DB 업그레이드 픽스처 — 앞 마이그레이션 → 옛 행 → 실제 migrator로 최종 journal → 백필·장부 → 재적용 멱등"
      min_lines: 60
  key_links:
    - from: "lib/auth.ts"
      to: "domain/auth/hooks.ts"
      via: "betterAuth({ databaseHooks: { session: { create: { after: recordFirstLogin } } } })"
      pattern: "databaseHooks"
    - from: "domain/auth/hooks.ts"
      to: "repositories/users.ts"
      via: "recordFirstLogin(session, context)이 setFirstLoginAtIfUnset(SYSTEM_VIEWER, session.userId, new Date())를 부른다(컨텍스트·경로와 무관)"
      pattern: "setFirstLoginAtIfUnset"
    - from: "domain/people/index.ts"
      to: "domain/permissions/info-items.ts"
      via: "PERSON_DTO_SPEC 두 필드 → person.value"
      pattern: "firstLoginAt"
    - from: "domain/people/index.ts"
      to: "domain/permissions/project.ts"
      via: "listPeople이 요청 단위 메모 visible을 묶은 project를 toPersonDto에 넘기고, 그 project가 project(…, { visible })를 부른다"
      pattern: "visible"
    - from: "domain/people/index.ts"
      to: "domain/org/index.ts"
      via: "toPersonDto가 같은 메모 project를 teamAtDate의 기존 deps.project로 넘긴다 — 팀 투영의 노출 판정도 요청당 한 번(최종 검토 #3, org 코드는 그대로)"
      pattern: "project: "
---

<objective>
**As a** 관리자, **I want to** 사람 목록에서 누가 아직 첫 로그인 전인지·임시 비밀번호를 쓰는지 한눈에 보기를, **so that** 전환 전에 전 직원 로그인 상태를 확인하고(Phase 8 체크리스트) 필요한 사람에게만 재발급한다.

이 플랜은 D8-07의 데이터·판정 부분이다. Task 1은 **트레이서**다 — 실제 better-auth 이메일 로그인 → 세션 생성 훅(`databaseHooks.session.create.after`) → 리포지토리 → `users.first_login_at` → DTO(요청 단위 노출 판정 메모 포함) → `personLoginStatus` 판정까지 한 번에 관통하고, 그 동작의 테스트(훅 엣지 · `/callback/google` 단위 · 판정 함수 · 전체 SQL 수)를 전부 먼저 쓴다. Task 2는 기존 사용자 백필(두 기록원 중 더 이른 것)을 업그레이드 픽스처로 먼저 빨갛게 만든 뒤 덧붙이고, 로컬 두 DB를 실제로 다시 만든다. 화면 렌더(배지 · 폰 칸 접기 · 접근성 관계)와 브라우저 E2E, SYSTEM.md·DECISIONS.md는 04.4-05가 한다(파일 수 한도).

Purpose: ROADMAP 04.4 기준 4의 데이터·판정과 기준 5의 DTO 부분.
Output: 새 칼럼 1개(마이그레이션 1개 `NNNN`, 백필 포함), 세션 생성 훅 등록과 처리 함수, DTO 두 필드와 노출 판정 메모, 판정 함수, 테스트.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development`를 **호출**한다. 각 task는 자기가 구현하는 behavior의 테스트를 먼저 쓰고 빨간 것을 본 뒤 구현한다(Codex #20). 실패를 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름과 `model`을 명시한다.
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적는다.
- 체인 순서(A4): 04.4-03 뒤, 04.4-05 앞. 이 플랜은 로컬 DB를 다시 만든다(Task 2) — 다른 플랜과 동시에 돌지 않는다.
- **마이그레이션(조정자 규칙 · B4)**: `pnpm db:generate --name users_first_login_at`이 준 번호를 **그대로** 쓴다(`NNNN` — db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1). 이름·번호를 바꾸지 않는다. 생성 결과는 SQL 하나 · 스냅숏 하나 · journal 항목 하나이고 SQL 이름과 journal 마지막 tag가 같아야 한다. 백필 SQL은 생성된 파일 **끝에** 손으로 덧붙인다(기존 파일 `db/migrations/0003_permissions_masters_spine.sql`의 손 추가 선례 — 이 페이즈의 번호가 아니다) — journal·스냅숏은 손대지 않는다. `drizzle-kit push` 금지, 스테이징·프로덕션 적용 금지. 04.4-06 Task 1이 병합 직전에 04.4-01 것과 합쳐 하나로 재생성하고 이 백필 블록을 다시 붙인다 — 그래서 표지 줄이 정확해야 한다.
- **로컬 DB 다시 만들기(Codex #5)**: `scripts/dev-db.sh`는 없는 DB만 만들고 비우지 않으므로 「다시 만들기」에 쓰지 않는다. 이 플랜의 실제 재생성 명령 — 개발 DB `PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp WITH (FORCE)' -c 'CREATE DATABASE erp OWNER erp' && pnpm db:migrate`, 테스트 DB `pnpm db:reset:test` 뒤 빈 장부 확인 `test -z "$(PGPASSWORD=erp psql -h 127.0.0.1 -U erp -d erp_test -tAc "select to_regclass('drizzle.__drizzle_migrations')")"`. 두 명령 모두 로컬 `127.0.0.1`만 가리킨다(프로덕션 DB 직접 명령 금지).
- **건드리지 않는 것**: 사람 목록의 조회 모양(`listUsers` 한 번 + 사람별 `toPersonDto`의 기존 팀·계급 조회)은 그대로다 — issue #56 자체를 고치지 않는다(Deferred). 바꾸는 것은 노출 판정을 요청 단위로 한 번만 하는 것뿐이다(Codex #8 — 새 필드가 사람당 판정 2번을 더하지 않게). 기존 6열 표 재설계 없음(UI-SPEC), 폰 칸 접기·행 머리글·`.peopleTable`은 04.4-05. `test/integration/leak-scan.test.ts`는 고치지 않는다(`domain/people` 모듈이 이미 등록된다). `.planning/STATE.md` 금지.
- 첫 로그인 기록 지점은 `lib/auth.ts`의 `databaseHooks.session.create.after` 하나다(UI-SPEC, 조정자 추가 지시 2) — 이메일 로그인·소셜 콜백 모두 세션 생성을 지난다. `domain/auth/hooks.ts`의 기존 `before`/`after` 훅 본문은 바꾸지 않고, 처리 함수 `recordFirstLogin`만 이 모듈에 **더한다**(`lib/auth.ts` 밖에 둔다 — `lib/auth.ts`는 better-auth 인스턴스를 만든다). 단, `domain/auth/hooks.ts`도 정적 import 사슬(`hooks.ts` 5·7행 → `repositories/login-attempts` · `repositories/users` 3행 → `@/db/client`)로 DB 모듈에 닿는다 — 그래서 단위 테스트는 의존성 주입만으로 「DB 없음」을 증명하지 못하고, hooks.ts를 import하기 **전에** `vi.mock("@/db/client", …)`로 단일 DB 진입점을 가짜로 바꾼다(Codex 2차 #7 — 새 모듈로 옮기지 않는 쪽을 택했다: 기존 기록 지점 결정과 파일 수를 그대로 두고, 모든 DB 경로가 `@/db/client` 하나를 지나므로 mock 하나로 충분하다).
- 새 의존성 없음. `git push` 하지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 D8-07 프로브 행이 분류되지 않았다(unresolved). 이 플랜이 스스로 적은 엣지 truth: **1회성 · 성공만 기록**(truth 3) · **설정 무관**(truth 3) · **재발급 뒤 표시**(truth 4) · **보관·필드 부재**(truth 2) · **백필 세 규칙과 업그레이드 경로**(truth 6) · **전체 SQL 수 불변**(truth 5). 폰 쌓기와 §7-5 예외는 04.4-05.

**계획 가정**
1. 칼럼 모양은 「첫 로그인 시각」(`first_login_at`, 1회성)이다 — 「마지막 로그인 시각」은 매 로그인 쓰기가 생기고 D8-07 판정에 필요 없다(CONTEXT Discretion)
2. 재발급 뒤 「첫 로그인 전」은 다시 보이지 않는다(한 번이라도 로그인했으면). 한 번도 로그인 안 한 사람을 재발급하면 NULL이 그대로라 두 배지가 계속 보인다(UI-SPEC 가정 1)
3. 기록 지점: better-auth 1.7.5의 `databaseHooks.session.create.after(session, context)`(`node_modules/@better-auth/core/dist/types/init-options.d.mts` 1263·1322–1324행)는 모든 세션 생성에서 한 번 돈다. 이 저장소에는 다른 세션 생성 경로가 없다(`plugins` 없음 · `disableSignUp: true` · 앱 코드에 `createSession(` 직접 호출 없음 — UI-SPEC 확인). 비밀번호 변경이 새 세션을 만들어도 이미 값이 있으면 no-op이다. 처리 함수의 예외는 감싸지 않는다(기존 훅과 같은 이유 — 조용한 실패보다 로그인 실패가 낫다) — 실행자가 better-auth가 이 훅의 예외를 어떻게 다루는지 `node_modules/better-auth`에서 확인해 SUMMARY에 적는다
4. 백필이 남기는 오표시(UI-SPEC 「가정 — 남는 오표시」, 전부 아직 임시 비밀번호인 사람에게만 · 다음 로그인 때 세션 훅이 채워 스스로 해소): ① 로그인 이력이 DB에서 수동으로 지워진 경우 ② 로그인이 `login_attempts` 도입(`0001_login_attempts`) 이전에만 있었던 경우 ③ 로그인 뒤 관리자가 이메일을 바꿔 과거 `login_attempts.email`과 맞지 않는 경우 ④ **Google로만 로그인한 이력** — 두 기록원 모두 이메일 로그인 경로에서만 쓰이므로 백필로 확정할 수 없다. 오늘 배포는 `AUTH_PROVIDER=email` 고정(`scripts/deploy.sh` 440행)이라 그런 이력은 없다. 비밀번호를 바꾼 사람은 백필 2단계가 채우므로 해당 없다
5. 보관된 사람의 배지 억제는 UI-SPEC이 확정했다(CONTEXT에 명시 없음) — 보관된 사람은 Phase 8 「전 직원 로그인 확인」 범위 밖
6. 노출 판정 메모는 요청 하나(`listPeople` 한 번) 안에서만 산다 — 같은 viewer · 같은 정보 항목이면 같은 답이므로 캐시 무효화 문제가 없다. 모듈 전역 캐시는 두지 않는다(계급 노출표를 바꾼 뒤 다음 요청이 옛 답을 받게 된다)
7. 업그레이드 픽스처의 「이 페이즈 앞 마이그레이션」은 journal에서 SQL이 `restore_rehearsals` 또는 `first_login_at`을 처음 언급하는 항목 **앞**까지다 — 브랜치에서는 main 분기점까지의 마이그레이션, 04.4-06 재생성 뒤에는 main 전체다. 같은 테스트가 두 시점 모두에서 뜻이 맞는다
8. 시각 형식: `users.*` · `action_log.occurred_at` · `login_attempts.attempted_at`은 모두 tz 없는 `timestamp`다(`db/schema/*.ts` 확인). 그래서 `first_login_at`도 tz 없는 `timestamp`로 두고(이 표의 다른 시각과 같다) 백필은 변환 없이 `least(min(occurred_at), min(attempted_at))`로 복사한다 — UI-SPEC의 「`first_login_at`의 timestamptz」 언급은 「계획 단계가 확인한다」로 위임된 것이고, 확인 결과 timestamp다. `login_attempts.email`은 기록 때 이미 소문자라(`getBodyEmail`) `login_attempts.email = lower(users.email)`로 맞춘다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 새 계정은 두 배지 판정, 한 번 로그인하면 「첫 로그인 전」이 사라진다 (칼럼 · 세션 생성 훅 · 리포지토리 · DTO · 판정 함수)</name>
  <files>db/schema/auth.ts, db/migrations/NNNN_users_first_login_at.sql, db/migrations/meta/NNNN_snapshot.json, db/migrations/meta/_journal.json, repositories/users.ts, domain/auth/hooks.ts, lib/auth.ts, domain/people/index.ts, app/(app)/admin/people/person-status.ts, test/integration/people-login-status.test.ts, test/integration/first-login-hook.test.ts, test/unit/people/person-status.test.ts, test/unit/auth/first-login-session-hook.test.ts</files>
  <precondition>로컬 Postgres가 떠 있고, `db/migrations/meta/_journal.json` 마지막 항목이 04.4-01의 `restore_rehearsals` 마이그레이션이며 로컬 두 DB에 적용되어 있다</precondition>
  <reversibility rating="costly">`users`에 칼럼을 더하는 마이그레이션은 배포되면 되돌리는 데 마이그레이션이 하나 더 든다. 아직 배포 전이고 병합 직전 재생성이 있다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 「사람 목록 배지 조합 규칙」 절 전체(보관됨 · 정보 노출 매핑 · 백필 규칙 · 남는 오표시 · **기록 지점과 설정 무관성** · **수용 조건 — 이메일 외 로그인 방식** · 가정 1)
    - `lib/auth.ts`(82행) — `betterAuth({ … hooks: { before, after }, socialProviders … })`, `databaseHooks`를 더할 자리
    - `node_modules/@better-auth/core/dist/types/init-options.d.mts` 1263행 · 1322–1324행 — `databaseHooks.session.create.after(session, context: GenericEndpointContext | null)` 서명
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md` 296–317행(첫 로그인 기록 코드 예시)
    - `db/schema/auth.ts` — `passwordIsTemporary`(D-08 주석)·`archivedAt` 선언 모양(이 표의 timestamp는 tz 없음)
    - `repositories/users.ts` — `setPasswordTemporary` · `setUserArchived`(IS NULL 조건부 UPDATE) · `listUsers`
    - `domain/auth/hooks.ts` 전문 — `before` 훅(보관·잠김 거부)이 실패·잠김·보관 시도에서 세션을 막는 이유, `SYSTEM_VIEWER` import 모양, `recordFirstLogin`을 더할 자리(기존 `before`/`after` 본문은 고치지 않는다 — `after`는 이메일 경로 전용이라 기록 지점이 아니다)
    - `test/integration/auth.test.ts` · `test/integration/lockout.test.ts` · `test/integration/archive.test.ts` — 실제 better-auth 훅을 지나는 로그인 성공·실패·잠김·보관 시도 모양(`auth.api.signInEmail({ body, headers: new Headers({ [CLIENT_IP_HEADER]: nextTestIp() }) })`)과 로그아웃 호출
    - `domain/people/index.ts` 40–106행 — `PersonDto` · `PERSON_DTO_SPEC`(여덟 필드) · `registerDto` · `toPersonDto`(사람마다 팀·계급 조회 뒤 `project`) · `listPeople` · `getPerson`
    - `domain/permissions/project.ts` 10–35행 — `ProjectDeps.visible`과 필드마다 `visibleFn(viewer, field.infoItem)` 호출(메모를 넣을 자리)
    - `domain/org/index.ts` 176–204행 — `TeamAtDateDeps.project`와 `teamAtDate`가 소속이 있을 때만 `projectFn(viewer, team, TEAM_DTO_SPEC)`을 부르는 곳(메모 `project`를 넘길 기존 경계 — 이 파일은 고치지 않는다)
    - `domain/permissions/visible.ts` · `repositories/permissions.ts` 86–100행 — 판정마다 노출표 조회 한 번(캐시 없음)
    - `test/integration/projects-list.test.ts` 80–110행 — `vi.spyOn(pool, "query")`로 SQL을 세는 방법
    - `domain/action-log/record.ts` 1–140행 · `domain/settings/keys.ts` 39–56행 · `domain/settings/registry.ts` `setSettingValue` — 로그인 로그 끄기
    - `domain/auth/password.ts` `finalizePasswordChange` · `domain/auth/accounts.ts` `resetPassword`
  </read_first>
  <behavior>
    - 통합 1: `createAccount` 직후 관리자 viewer의 `listPeople`에서 그 사람의 `firstLoginAt`이 null, `passwordIsTemporary`가 true
    - 통합 2(트레이서): 그 계정으로 `signInEmail` 성공 뒤 `firstLoginAt`이 null이 아니고 로그인 직전·직후 시각 사이다; `passwordIsTemporary`는 여전히 true
    - 통합 3: `person.value`를 못 보는 계급 viewer(노출 행을 끈 계급)의 `listPeople` 결과에는 두 키가 없다(`"firstLoginAt" in dto`가 false)
    - 통합 4(표시 흐름): 발급 → 로그인 → `finalizePasswordChange` → `resetPassword` 각 시점의 `listPeople` DTO가 truth 4와 같다
    - 쿼리 수(Codex #8, **변경 전에 쓴다**): 사람 3명과 6명을 만들고 **사람마다 발령일이 오늘 이하인 팀 소속 하나씩**을 넣은 뒤(최종 검토 #3 — 소속이 없으면 `teamAtDate`가 팀 투영 전에 null로 끝나 팀 투영의 노출 판정이 사람 수에 따라 느는 것을 못 잡는다; 기준값도 이 픽스처로 잰다) `vi.spyOn(pool, "query")`로 `listPeople` 한 번의 **전체** 호출 수와 `visibility_matrix`가 든 호출 수를 센다. 단언 — 전체 수 ≤ 변경 전 기준값(3명·6명 각각, PLAN_BASE에서 잰 상수), `visibility_matrix` 호출 수가 3명과 6명에서 같다
    - 훅(Codex #18, `test/integration/first-login-hook.test.ts`, 실제 better-auth 로그인 → 세션 생성 훅을 지난다): 틀린 비밀번호 로그인 → NULL · 잠긴 계정 로그인 시도 → NULL · 보관된 계정 로그인 시도 → NULL · 성공 → 채워짐 · 두 번째 성공 → 첫 값 그대로 · 로그아웃 → 그대로 · `finalizePasswordChange` → 그대로 · `resetPassword` → 그대로(null로 돌아가지 않음) · `action_log.optional_types`에서 `login`을 뺀 채 새 계정 로그인 → login 행 0개인데 값은 채워짐 · 처리 함수 `recordFirstLogin`을 `{ path: "/callback/google" }` 모양 컨텍스트로 직접 부르면 NULL인 사용자가 채워지고, 같은 사용자로 한 번 더 부르면 값이 그대로
    - 세션 훅 단위(`test/unit/auth/first-login-session-hook.test.ts`, UI-SPEC 수용 조건, `deps` 주입): 파일 맨 위(= hooks.ts import보다 먼저 끌어올려지는) `vi.mock("@/db/client", …)` 팩토리가 `db`·`pool`을 **어떤 메서드든 부르면 throw하는** 가짜로 바꾼다 — hooks.ts의 정적 사슬(`repositories/users` → `@/db/client`)이 실제 풀·커넥터를 만들지 못한다(Codex 2차 #7). 그 위에서: `recordFirstLogin({ userId: "u1", … }, { path: "/callback/google" } 모양 컨텍스트, deps)` → 주입한 기록 함수가 `(SYSTEM_VIEWER, "u1", Date)`로 한 번 불린다; 컨텍스트 `null` → 같다; `/sign-in/email` 컨텍스트 → 같다(경로로 거르지 않는다); 설정 조회(`@/domain/settings/registry`)를 `vi.mock`으로 throw하게 해도 기록 함수가 불린다(로그 설정과 무관); 네 경우 모두 가짜 `db`·`pool`의 메서드가 한 번도 불리지 않았다(throw가 없었다 — 주입한 deps 밖으로 DB에 닿지 않는다)
    - 판정 1~6(`test/unit/people/person-status.test.ts`): 보관 + 두 조건 → archived; 0개 → 빈 배열; 첫 로그인 전만; 임시만; 둘 다(순서 고정); 두 키가 없는 객체 → 빈 배열(NULL로 오인하지 않음)
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)** — 순서가 중요하다: (a) 쿼리 수 테스트를 **아무것도 바꾸기 전에** `test/integration/people-login-status.test.ts`에 쓰고, PLAN_BASE 코드로 3명·6명의 전체 호출 수를 한 번 재어 기준값 상수로 박는다(주석 「04.4-04 변경 전 측정값(PLAN_BASE <sha>)」, 잰 값을 SUMMARY에도). 이 시점 `visibility_matrix` 같음 단언은 빨갛다(지금은 사람당 판정이 돈다). (b) 통합 1~4, 훅 통합 전부, 세션 훅 단위, 판정 1~6을 쓴다(기존 테스트는 그대로). 빨간 것을 확인한다. 브라우저 E2E는 화면을 그리는 04.4-05가 먼저 쓴다.

② 스키마(D8-07): `db/schema/auth.ts` users에 `firstLoginAt: timestamp("first_login_at")`(NULL 허용, 이 표의 다른 시각과 같이 tz 없음) 한 칸과 주석 한 줄(「D8-07 첫 로그인 시각 — after 훅이 NULL일 때만 한 번 쓴다. sessions는 판정 근거가 아니다」).

③ **[BLOCKING] 마이그레이션**: `pnpm db:generate --name users_first_login_at`(ADD COLUMN 1개) → 준 번호 그대로(`NNNN`, 이름 바꾸기 없음) → SQL 이름과 journal 마지막 tag가 같은지 확인 → `pnpm lint:sql` → `pnpm db:migrate`(로컬 `erp`만). `erp_test`는 통합 global-setup이 적용한다. `drizzle-kit push`·스테이징·프로덕션 적용 금지.

④ 리포지토리: `repositories/users.ts`에 `setFirstLoginAtIfUnset(viewer, userId, at)`를 **더한다** — `users.id = userId AND first_login_at IS NULL` 조건의 UPDATE 하나(`setUserArchived` 모양), 반환 없음.

⑤ 세션 생성 훅(D8-07, UI-SPEC 「기록 지점과 설정 무관성」 · 조정자 추가 지시 2): `domain/auth/hooks.ts`에 `recordFirstLogin(session, context, deps?)`를 **더한다** — `context`를 보지 않고(경로·로그인 방식 무관) `await (deps?.setFirstLoginAtIfUnset ?? setFirstLoginAtIfUnset)(SYSTEM_VIEWER, session.userId, new Date())` 한 번. `recordAction`·설정을 부르지 않는다. 기존 `before`/`after` 훅 본문은 한 줄도 바꾸지 않는다(`/sign-in/email` `after` 훅에서 쓰지 않는다). `lib/auth.ts`의 `betterAuth({ … })`에 `databaseHooks: { session: { create: { after: recordFirstLogin } } }`를 **더한다**(기존 키는 그대로, import 한 줄 추가). 주석 한 줄(「D8-07 — 로그인 방식·로그인 로그 설정과 무관하게 모든 세션 생성에서 한 번. 유일한 기록 지점」). 실패·잠김·보관 시도는 세션이 만들어지지 않으므로 값이 남지 않는다(훅 통합이 증명).

⑥ DTO(Codex #8): `PersonDto`에 `firstLoginAt: Date | null`·`passwordIsTemporary: boolean`을, `PERSON_DTO_SPEC.fields`에 두 줄(`infoItem: "person.value"`)을 **끝에** 더한다(기존 여덟 줄은 그대로). `toPersonDto`에 선택 인자로 `projectFn: typeof project`(없으면 지금처럼 `project`)를 받아 자기 투영 `projectFn(viewer, source, PERSON_DTO_SPEC)`과 팀 조회 `defaultTeamAtDate(viewer, row.id, todayIsoDate(), { project: projectFn })` 둘 다에 쓴다 — `teamAtDate`의 기존 `deps.project` 경계를 그대로 쓰므로 `domain/org/index.ts`는 고치지 않는다(최종 검토 #3). `listPeople`은 요청 안에서 정보 항목 → `Promise<boolean>`을 담는 `Map` 하나로 기본 `visible`을 감싼 메모 함수를 만들고, 그 메모를 `deps.visible`로 얹어 `project`를 부르는 함수 하나(`typeof project`로 타입을 준다 — 받은 deps 위에 메모 `visible`)를 모든 `toPersonDto`에 넘긴다(probe_fallback 가정 6 — 모듈 전역 캐시 금지). `getPerson`은 그대로. 사람별 팀·계급 조회는 그대로다(issue #56 Deferred).

⑦ 표시: 새 `app/(app)/admin/people/person-status.ts`에 순수 함수 `personLoginStatus(person)`을 만든다 — 입력은 투영된 DTO(필드가 없을 수 있다). `archivedAt`이 참이면 `{ kind: "archived" }`, 아니면 `{ kind: "badges", badges }`(`firstLoginAt` 키가 있고 null이면 `첫 로그인 전`, `passwordIsTemporary` 키가 있고 true면 `임시 비밀번호 사용 중`, 이 순서). `page.tsx` 렌더는 04.4-05가 한다(이 플랜은 화면 파일을 고치지 않는다).

⑧ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: record first login from session creation and expose login status`, 본문 한국어(D8-07 · D-08 유지 · 세션 생성 훅이 유일한 기록 지점 · 노출 판정 요청 단위 메모로 SQL 수 불변).
  </action>
  <verify>
    <automated>pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/people-login-status.test.ts test/integration/first-login-hook.test.ts test/integration/leak-scan.test.ts test/integration/people.test.ts test/integration/auth.test.ts test/integration/lockout.test.ts test/integration/archive.test.ts</automated>
    <fails_when>새 SQL이 squawk에 걸리거나, DTO 흐름·필드 부재·전체 SQL 수(기준값 · 팀 소속이 있는 3명=6명 노출 판정 수)·훅 엣지(실패·잠김·보관 NULL, 이후 보존, 로그 끔) 중 하나가 어긋나거나, 누수 스캔이 새 필드를 잡지 못하거나, 기존 사람·인증 통합 테스트가 깨진다</fails_when>
    <automated>pnpm db:generate 2&gt;&amp;1 | grep -q "No schema changes" &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm vitest run --project unit test/unit/people/person-status.test.ts test/unit/auth/first-login-session-hook.test.ts test/unit/import-cycles.test.ts &amp;&amp; { test ! -f test/unit/db/migration-journal.test.ts || pnpm vitest run --project unit test/unit/db/migration-journal.test.ts; }</automated>
    <fails_when>스키마·마이그레이션 불일치, 타입·린트 오류, 판정 1~6 실패, 세션 훅 단위(`/callback/google` · null 컨텍스트 · 설정 무관) 실패, import 순환, (가드 파일이 있으면) journal 가드 실패 중 하나가 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=A "$PLAN_BASE"..HEAD -- db/migrations`가 정확히 두 줄(`db/migrations/<NNNN>_users_first_login_at.sql`과 `db/migrations/meta/<NNNN>_snapshot.json`, 같은 NNNN)이고, 그 SQL에 `ADD COLUMN "first_login_at"`이 있으며 journal 마지막 tag가 그 SQL 이름과 같다
    - `git diff "$PLAN_BASE"..HEAD -- domain/auth/hooks.ts`의 삭제 줄(`^-[^-]`)이 0개다(기존 `before`/`after` 훅은 그대로, `recordFirstLogin`만 더함)
    - `grep -c "databaseHooks" lib/auth.ts`가 1 이상이고 `grep -c "recordFirstLogin" lib/auth.ts`가 1 이상이다
    - `git diff "$PLAN_BASE"..HEAD -- domain/people/index.ts`의 삭제 줄(`^-[^-]`)이 4줄 이하이고, 그중 어느 것도 `PERSON_DTO_SPEC`의 기존 여덟 필드 줄이 아니다(`{ key: "`로 시작하는 삭제 줄 0개) — 바뀌는 것은 `toPersonDto` 서명 · `defaultTeamAtDate(` 호출(최종 검토 #3) · `project(` 호출 · `listPeople` 반환 줄뿐
    - `git diff "$PLAN_BASE"..HEAD -- domain/org/index.ts`가 아무것도 출력하지 않는다(팀 투영 메모는 기존 `deps.project` 경계로만 넘긴다, 최종 검토 #3)
    - 쿼리 수 테스트 파일에 기준값 상수와 그 측정 커밋 sha 주석이 있다
    - 세 verify 명령이 초록이다
  </acceptance_criteria>
  <done>새 계정은 판정에서 두 배지이고, 세션이 처음 만들어질 때(로그인 방식 무관) 한 번만 「첫 로그인 전」이 사라지며, 목록 SQL 수는 변경 전 이하이고 사람 수에 따라 노출 판정이 늘지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 백필 — 업그레이드 픽스처로 먼저 빨갛게, 표지 뒤 두 문장, 로컬 두 DB 실제 재생성</name>
  <files>db/migrations/NNNN_users_first_login_at.sql, test/integration/first-login-upgrade.test.ts</files>
  <read_first>
    - `db/migrations/0003_permissions_masters_spine.sql` 70–91행 — 생성 SQL 끝에 손으로 덧붙인 문장과 `--> statement-breakpoint` 선례
    - `test/integration/db-bootstrap.test.ts` 1–40행 · 70–110행 — `ADMIN_URL`(로컬 `postgres` DB) `new Pool`로 일회용 DB를 만들고 `DROP DATABASE … WITH (FORCE)`로 지우는 모양과 그 이유 주석
    - `test/integration/global-setup.ts` — `migrate(drizzle(pool), { migrationsFolder })` 호출 모양
    - `node_modules/drizzle-orm/pg-core/dialect.js` 40–70행 — migrator가 `created_at`(= journal `when`)으로 건너뛸 항목을 정한다(해시가 아니다)
    - `scripts/reset-test-db.sh` · `package.json`의 `db:reset:test` — 테스트 DB DROP/CREATE
    - `db/schema/auth.ts` · `db/schema/action-log.ts` · `db/schema/login-attempts.ts` — 옛 모양 행 삽입에 필요한 NOT NULL 칼럼(세 표의 시각은 모두 tz 없는 `timestamp`)
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 「계획 단계 필수 요구사항 — 백필 규칙」 · 「가정 — 남는 오표시」
  </read_first>
  <behavior>
    - 업그레이드 1(Codex #19, 일회용 DB `erp_upgrade_<무작위>`): 임시 폴더에 journal을 SQL이 `restore_rehearsals|first_login_at`을 처음 언급하는 항목 **앞**까지 잘라 그 SQL·스냅숏과 함께 복사 → 그 폴더로 `migrate()` → 옛 모양(칼럼 없음) 사용자와 `action_log`·`login_attempts` 행을 raw SQL로 삽입: A(login 행 t2·t1, t1<t2, 하나는 `pruned_at` 있음, 시도 기록 없음), B(기록 없음, 임시 false), C(기록 없음, 임시 true), E(login이 아닌 `action_log` 행과 `success = false` 시도만, 임시 true), F(`action_log` login 없음, `login_attempts` 성공 t3 · 이메일 대소문자 다름 — 사용자 `Fe@X.com`, 시도 `fe@x.com`, 임시 true), G(`action_log` login t5와 `login_attempts` 성공 t4, t4<t5, 임시 true), H(`action_log` login t6과 `login_attempts` 성공 t7, t6<t7) → 실제 `db/migrations`로 `migrate()`(실제 migrator) → A = t1, B = null 아님(실행 시각 근처), C = null, E = null, F = t3, G = t4(더 이른 쪽), H = t6; 장부 `drizzle.__drizzle_migrations` 행 수 = 실제 journal 항목 수
    - 업그레이드 2(멱등): 같은 DB에 실제 폴더로 `migrate()`를 한 번 더 → 오류 없음, 장부 행 수 그대로, A~H 값 그대로; 그 뒤 D(값을 직접 넣은 사용자)를 만들고 다시 `migrate()` → D 그대로
    - 표지 1: `db/migrations/*.sql` 중 표지 줄 `-- 04.4 D8-07 first_login_at backfill`을 가진 파일이 정확히 하나다
    - 뒷정리: `afterAll`이 일회용 DB를 `WITH (FORCE)`로 지운다(실패해도 다음 실행이 무작위 이름이라 충돌 없음)
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: `test/integration/first-login-upgrade.test.ts`에 behavior 전부를 쓴다. 일회용 DB는 `test/integration/db-bootstrap.test.ts`처럼 로컬 `ADMIN_URL`(`postgres://erp:erp@127.0.0.1:5432/postgres`) 풀로 만들고, 그 DB용 풀에 `drizzle(pool)`을 씌워 `migrate()`를 부른다(앱의 `@/db/client` 풀을 쓰지 않는다 — 대상 DB가 다르다). 빨간 것(표지 없음 · A~C 값 불일치)을 확인한다.

② 백필(D8-07, B4, UI-SPEC 백필 규칙 · 조정자 추가 지시 1): Task 1이 만든 `db/migrations/NNNN_users_first_login_at.sql` **끝에** 표지 주석 줄 하나와 두 문장을 덧붙인다 — (a) `first_login_at IS NULL`인 `users` 행을 두 기록원의 **더 이른 값**으로 채운다: `action_log`에서 `action_type = 'login'`인 행의 `actor_id`별 `min(occurred_at)`(`pruned_at` 조건 없음)과 `login_attempts`에서 `success = true`이고 `email = lower(users.email)`인 행의 `min(attempted_at)` — 둘 중 있는 값들의 `least(…)`(PostgreSQL `least`는 NULL을 무시한다), 둘 다 없으면 건드리지 않는다 (b) 여전히 NULL이고 `password_is_temporary = false`인 행을 `now()`로 채운다. 세 칼럼 모두 tz 없는 `timestamp`라 변환하지 않는다(probe_fallback 가정 8). 두 문장 사이와 표지 앞에 `--> statement-breakpoint`를 둔다. 파일 이름·journal·스냅숏은 손대지 않는다. `pnpm lint:sql`.

③ **로컬 두 DB 실제 재생성(Codex #5)** — 이미 적용된 파일에 덧붙였으므로 migrator는 `created_at`만 보고 건너뛴다. execution_notes의 명령으로 `erp`를 DROP/CREATE 뒤 `pnpm db:migrate`, `erp_test`는 `pnpm db:reset:test` 뒤 빈 장부 확인. `scripts/dev-db.sh`는 쓰지 않는다.

④ 녹색이 될 때까지. 커밋 제목 예 `feat: backfill first_login_at from login history`, 본문 한국어(D8-07 · UI-SPEC 백필 규칙 · 업그레이드 픽스처).
  </action>
  <verify>
    <automated>pnpm lint:sql &amp;&amp; pnpm vitest run --project integration test/integration/first-login-upgrade.test.ts</automated>
    <fails_when>표지가 없거나 둘 이상이거나, 앞 마이그레이션 → 옛 행 → 실제 migrator 경로에서 백필 규칙(두 기록원 중 더 이른 값 · 대소문자 무관 이메일 · 실패 시도 무시 · 임시 false → 실행 시각 · 그 밖 NULL)·장부 행 수·재적용 멱등 중 하나가 어긋난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; test -z "$(PGPASSWORD=erp psql -h 127.0.0.1 -U erp -d erp_test -tAc "select to_regclass('drizzle.__drizzle_migrations')")" &amp;&amp; PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp WITH (FORCE)' -c 'CREATE DATABASE erp OWNER erp' &amp;&amp; pnpm db:migrate &amp;&amp; pnpm vitest run --project integration test/integration/people-login-status.test.ts test/integration/first-login-hook.test.ts</automated>
    <fails_when>테스트 DB가 비워지지 않았거나(장부 표가 남음), 개발 DB 재생성·전체 마이그레이션이 실패하거나, 새 백필이 들어간 DB에서 Task 1 통합 테스트가 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -l "04.4 D8-07 first_login_at backfill" db/migrations/*.sql | wc -l`이 1이다
    - `pnpm db:generate` 재실행이 "No schema changes"다(백필은 스냅숏에 영향 없음)
    - 두 verify 명령이 초록이다
  </acceptance_criteria>
  <done>기존 사용자가 실제 업그레이드 경로(앞 마이그레이션 → 옛 행 → 실제 migrator)에서 두 기록원 중 더 이른 값 규칙대로 채워지고 재적용에도 그대로이며, 로컬 두 DB가 실제로 새로 만들어진 상태에서 전체가 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 세션 생성 → better-auth `databaseHooks.session.create.after` | 만들어진 세션의 `userId`로만 쓰기(요청 본문 값을 쓰지 않는다) |
| DTO 투영 → 계급별 화면 | 노출표가 `person.value`를 끈 계급은 새 필드를 받지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-18 | Information Disclosure | `PersonDto` 새 필드 | medium | mitigate | 두 필드를 `PERSON_DTO_SPEC`에서 `person.value`에 매핑 → `project()`가 못 보는 계급에게서 제거, 누수 스캔 DTO 축이 자동 확인(Task 1 통합 3 · leak-scan) |
| T-04.4-19 | Tampering | `first_login_at` 쓰기 | low | mitigate | 쓰는 곳은 `lib/auth.ts` `databaseHooks.session.create.after` → `recordFirstLogin` 하나, 세션의 `userId`, IS NULL 조건부 UPDATE. 실패·잠김·보관 시도는 세션이 없어 NULL, 이후 로그인·로그아웃·변경·재발급은 보존(Task 1 훅 통합 — 실제 better-auth 로그인), `/callback/google`·null 컨텍스트도 같은 경로(세션 훅 단위) |
| T-04.4-20 | Denial of Service | 사람 목록 성능(issue #56) | medium | mitigate | 노출 판정을 요청 단위로 한 번만(메모 — 팀 투영도 `teamAtDate`의 `deps.project`로 같은 메모, 최종 검토 #3) — 팀 소속이 있는 3명·6명 전체 SQL 수가 변경 전 기준값 이하이고 `visibility_matrix` SQL 수가 사람 수와 무관(Task 1 쿼리 수, Codex #8) |
| T-04.4-21 | Repudiation | 로그인 사실 | low | accept | 감사 기록은 기존 `action_log` login 행이 맡고, `first_login_at`은 표시용 요약이다 |
| T-04.4-22 | Tampering | `_journal.json` · 손 추가 백필 | medium | mitigate | 표지 줄이 정확히 한 파일에 있다는 통합 단언 + 실제 migrator 업그레이드 픽스처(Codex #19) + 병합 직전 재생성(04.4-06). journal 가드는 main 또는 04.1 파일이 있을 때만(04.4-01 규칙) |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개. `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` 통과
- `pnpm test:unit` 전체 통과
- 로컬 두 DB 실제 재생성(execution_notes 명령 + 빈 장부 확인) 뒤 `pnpm test:integration` 전체 통과(leak-scan · people · auth · lockout · archive 회귀 없음)
- 브라우저 E2E는 04.4-05(화면 렌더)가 맡는다 — 이 플랜의 판정은 통합·단위로 끝난다
- `pnpm db:generate` 재실행 결과 "No schema changes"
- `git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml test/integration/leak-scan.test.ts domain/action-log domain/settings/keys.ts domain/permissions .planning/STATE.md` 비어 있음
</verification>

<success_criteria>
- `personLoginStatus`가 첫 로그인 전·임시 비밀번호 사용 중을 UI-SPEC 배지 조합 규칙대로 정한다(렌더는 04.4-05)
- `first_login_at`은 세션 생성 훅 한 곳에서만, 로그인 방식·로그 설정과 무관하게 한 번 기록된다
- 발급 → 로그인 → 변경 → 재발급 흐름과 실패·잠김·보관 시도가 실제 훅을 지나는 통합 테스트로 고정된다
- 기존 사용자가 실제 업그레이드 경로에서 규칙대로 백필되고 재적용에 멱등이다
- 목록 전체 SQL 수가 변경 전 이하이고 노출 판정이 사람 수에 따라 늘지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 칼럼 | `users.first_login_at`(timestamp, NULL 허용) |
| 마이그레이션 | `db/migrations/NNNN_users_first_login_at.sql` + `meta/NNNN_snapshot.json`(NNNN = db:generate가 준 번호(브랜치 임시), 머지 직전 재생성으로 main 마지막+1) + 표지 `-- 04.4 D8-07 first_login_at backfill` 뒤 백필 두 문장 — 04.4-06이 병합 직전 04.4-01 것과 합쳐 하나로 재생성하고 백필을 다시 붙인다 |
| 함수 | `setFirstLoginAtIfUnset` · `recordFirstLogin`(세션 생성 훅 처리 함수, `lib/auth.ts` `databaseHooks.session.create.after`에 등록) · `personLoginStatus` · `listPeople` 안의 요청 단위 노출 판정 메모 |
| DTO | `PersonDto.firstLoginAt` · `PersonDto.passwordIsTemporary`(`person.value`) |
| 화면 | 없음 — `/admin/people` 렌더(배지 · 폰 칸 접기 · 접근성 관계 · `--lh-table`)는 04.4-05 |
| 테스트 | `test/integration/people-login-status.test.ts` · `test/integration/first-login-hook.test.ts` · `test/integration/first-login-upgrade.test.ts` · `test/unit/people/person-status.test.ts` · `test/unit/auth/first-login-session-hook.test.ts` |

## Review Dispositions Ledger

### Round 1 — uncommitted (Codex GPT 적대적 계획 리뷰, 세션 scratchpad `codex-plan-final.md`, 검토 대상 계획 커밋 `7a1eca3`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #5 「다시 만들기」라던 `dev-db.sh`가 개발 DB를 비우지 않는다 | high | Plan 04, Task 2: 개발 DB는 로컬 `psql` DROP/CREATE 뒤 `pnpm db:migrate`, 테스트 DB는 `pnpm db:reset:test` 뒤 빈 장부(`to_regclass`) 확인. `dev-db.sh`를 재생성으로 쓰지 않는다는 금지 추가 |
| #8 DTO 두 필드가 사람당 노출 판정 2번을 더한다 | high | Plan 04, Task 1: `listPeople` 요청 단위 메모를 `project()`의 `deps.visible`로 전달. 쿼리 수 테스트를 **변경 전에** 쓰고 3명·6명 전체 SQL을 PLAN_BASE 기준값과 비교, `visibility_matrix` 수가 사람 수와 무관함을 단언 |
| #18 훅 테스트로 지목한 파일이 인증과 무관하다 | medium | Plan 04, Task 1: `session-hooks.test.ts` 참조 삭제, 새 `test/integration/first-login-hook.test.ts`가 실제 better-auth 훅으로 실패·잠김·보관 → NULL, 성공 → 한 번, 이후 로그인·로그아웃·변경·재발급 → 보존, 로그 끔 → 채움 |
| #19 빈 DB·추출 SQL 테스트는 배포 업그레이드를 증명하지 않는다 | medium | Plan 04, Task 2: 일회용 DB에서 앞 마이그레이션 → 옛 행 → 실제 migrator로 최종 journal → 백필·장부 확인 → 재적용 멱등. 04.4-06이 재생성 뒤 같은 테스트를 다시 돌린다(분할 기준이 두 시점 모두에서 맞다 — 가정 7) |
| #20 테스트가 구현 뒤에 온다 | low | Plan 04: 판정 함수·훅 엣지·쿼리 수 테스트를 Task 1 ①로 옮기고, 백필은 Task 2에서 업그레이드 테스트를 먼저 빨갛게 |
| 체커 scope_sanity(17개 파일) | warning | 화면 파일(`page.tsx` · `people.module.css`) · E2E 둘 · SYSTEM.md · DECISIONS.md를 새 04.4-05로 옮겨, 세션 훅(`lib/auth.ts` · 단위 테스트)을 더하고도 14개 |
| UI-SPEC 최종판(조정자 추가 지시 1·2, UI 체커 재검 1·2) | — | 기록 지점을 `lib/auth.ts` `databaseHooks.session.create.after` → `recordFirstLogin` 하나로(이메일 `after` 훅에서 쓰지 않음), `/callback/google`·null 컨텍스트 단위 테스트. 백필을 `action_log` login과 `login_attempts` 성공(이메일 대소문자 무관) 중 더 이른 값으로, 남는 오표시 ①~④(Google 전용 이력 포함)를 가정 4로. 칼럼 형식은 tz 없는 `timestamp` — `users`·`sessions`·`accounts`·`action_log`·`login_attempts`의 모든 시각 칼럼과 같은 관례(가정 8) |

### Round 2 — uncommitted (Codex 2차 변경분 리뷰 · 계획 체커 2차)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Codex 2차 #7 · 계획 체커 2차 info (세션 훅 단위 테스트가 DI만으로 「DB 모듈 안 불림」을 증명 못함 · 0003 인용 행) | minor · info | Plan 04, Task 1: `hooks.ts` → `repositories/users` → `@/db/client` 정적 사슬을 인정하고, hooks.ts import 전에 `vi.mock("@/db/client")`로 호출 시 throw하는 가짜를 넣어 「DB에 닿지 않음」을 단언(새 모듈로 옮기지 않음 — 이유는 execution_notes). Task 2 read_first 인용을 `0003_permissions_masters_spine.sql` 70–91행으로 정정 |

### Final review — uncommitted (Codex 최종 전체 검토 1회, 28f90c5 기준 · Sonnet 코드 대조 확인)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 최종 #3 노출 판정 메모가 `teamAtDate` 자신의 팀 투영에 닿지 않고, 소속 없는 픽스처가 그것을 가린다 | minor | Plan 04, Task 1: truth · key_link · ⑥ — `toPersonDto`가 받은 같은 메모 `project`를 `teamAtDate`의 기존 `deps.project`로 넘긴다(`domain/org/index.ts`는 diff 0 — acceptance). 쿼리 수 테스트의 3명·6명 픽스처 모두 사람마다 팀 소속을 넣고 기준값도 그 픽스처로 잰다. `domain/people/index.ts` 삭제 줄 상한 3 → 4(`defaultTeamAtDate(` 호출 한 줄) |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-04-SUMMARY.md` when done
</output>
