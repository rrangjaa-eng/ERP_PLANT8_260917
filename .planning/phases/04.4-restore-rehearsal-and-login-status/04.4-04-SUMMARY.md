---
phase: 04.4-restore-rehearsal-and-login-status
plan: 04
status: complete
subsystem: auth
tags: [first-login, better-auth, database-hooks, migration, backfill, person-dto]
plan_base: c22208386b24d49f874ab9eaa89c30ae2e137561

requires:
  - phase: 04.4-03
    provides: "직전 플랜(체인 순서 A4) — 코드 의존 없음"
  - phase: "main PR #65 (quick 260924-hgx)"
    provides: "listPeople 묶음 조회 + 호출 한정 visibleCache, 조회 수 회귀 테스트"
provides:
  - "users.first_login_at(tz 없는 timestamp, NULL 허용) + 마이그레이션 0018_users_first_login_at(db:generate 번호, 머지 직전 재생성 대상) — SET LOCAL 머리 줄 + 표지 뒤 백필 두 문장"
  - "repositories/users.ts setFirstLoginAtIfUnset — IS NULL 조건부 UPDATE"
  - "domain/auth/hooks.ts recordFirstLogin — lib/auth.ts databaseHooks.session.create.after에 등록된 유일한 기록 지점, 기록 실패는 잡아 에러 로그(CEO-2)"
  - "PersonDto.firstLoginAt · PersonDto.passwordIsTemporary(person.value)"
  - "app/(app)/admin/people/person-status.ts personLoginStatus — 보관됨 / 첫 로그인 전 · 임시 비밀번호 사용 중(필드 부재 존중)"
affects: [04.4-05, 04.4-06]

tech-stack:
  added: []
  patterns:
    - "better-auth databaseHooks.session.create.after — 로그인 방식·경로와 무관하게 세션 생성마다 한 번"
    - "업그레이드 픽스처: journal을 이 페이즈 첫 항목 앞에서 잘라 일회용 DB에 적용 → 옛 행 → 실제 migrator로 최종 journal"

key-files:
  created:
    - db/migrations/0018_users_first_login_at.sql
    - db/migrations/meta/0018_snapshot.json
    - app/(app)/admin/people/person-status.ts
    - test/integration/people-login-status.test.ts
    - test/integration/first-login-hook.test.ts
    - test/integration/first-login-upgrade.test.ts
    - test/unit/people/person-status.test.ts
    - test/unit/auth/first-login-session-hook.test.ts
  modified:
    - db/schema/auth.ts
    - db/migrations/meta/_journal.json
    - repositories/users.ts
    - domain/auth/hooks.ts
    - lib/auth.ts
    - domain/people/index.ts
    - test/integration/people.test.ts

key-decisions:
  - "조회 수 기준값: PLAN_BASE c222083에서 #65 테스트의 1명 기준 전체 pool.query 수를 실측 9(#65 SUMMARY와 같음) — people.test.ts에 `expect(before).toBeLessThanOrEqual(9)` 한 줄만 더함"
  - "first-login-hook 통합 테스트는 실패 경로도 Response로 받으려고 auth.api.signInEmail 대신 auth.handler(Request)를 쓴다 — asResponse로도 before 훅의 APIError는 throw로 올라온다(lockout.test.ts와 같은 모양)"
  - "recordFirstLogin의 context 인자는 unknown으로 받는다 — 보지 않기 때문(경로 무관). better-auth 서명과 호환"
  - "Opus 검토 NIT 미반영: ① 단위 테스트의 설정 조회 throw mock은 플랜 behavior가 명시한 경우라 유지 ② userId 끌어올림은 session이 null이면 어차피 try 밖에서 throw해 보호 효과가 없어 되돌림 ③ 이메일 로그인에서 세션 훅이 sign-in after 미들웨어보다 먼저 돌아 recordAttempt 실패로 500이 나도 first_login_at은 채워질 수 있음 — 세션은 이미 만들어졌으므로 허용"

requirements-completed: [OPS-03]

duration: 약 40분
completed: 2026-09-26

actuals:
  tasks: 2
  commits: 3
---

# 04.4-04 SUMMARY — 첫 로그인 기록·사람 로그인 상태 판정

PLAN_BASE: `c22208386b24d49f874ab9eaa89c30ae2e137561`

## 한 일

- **Task 1 트레이서(1b30b97)** — 실제 better-auth 이메일 로그인 → `databaseHooks.session.create.after` → `recordFirstLogin` → `setFirstLoginAtIfUnset`(IS NULL 조건부 UPDATE) → `users.first_login_at` → `listPeople` DTO(`listUsers` 행 그대로, #65 목록 경로 무수정) → `personLoginStatus`. 테스트를 먼저 쓰고 빨간 것을 확인했다(칼럼 없음 · 함수 없음 · 모듈 없음).
- **Task 2 백필(fd899ad)** — 업그레이드 픽스처를 먼저 빨갛게(표지 없음 · A 값 null) 만든 뒤 표지 `-- 04.4 D8-07 first_login_at backfill` 뒤 두 문장을 덧붙였다. 로컬 두 DB를 실제로 DROP/CREATE해 다시 적용했다(`pnpm db:reset:test` 뒤 빈 장부 확인, `erp`는 psql DROP/CREATE 뒤 `pnpm db:migrate`).
- **검토 반영(e47f89f)** — Opus 독립 검토(Codex 대체) SHOULD 2건: 일회용 DB 풀 error 리스너(57P01), 관리 URL을 DATABASE_URL에서.

## better-auth의 훅 예외 처리(가정 3 확인)

`node_modules/better-auth/dist/db/with-hooks.mjs` 33–40행: `create.after`는 `queueAfterTransactionHook`에 넣어 트랜잭션 밖이면 바로, 안이면 커밋 뒤 실행하고 결과를 `await`한다. `onError` 같은 흡수 장치가 없어 **예외는 로그인 요청까지 올라간다** — 그래서 `recordFirstLogin`의 try/catch가 필요하고, 통합 테스트가 「기록 실패에도 200 · 세션 쿠키 · NULL · 에러 로그 한 번 → 다음 로그인에 채워짐」을 실제 경로로 확인했다.

## 검증

- `pnpm lint` 0 · `pnpm typecheck` 0 · `pnpm lint:sql` 0건 · `pnpm db:generate` "No schema changes"
- `pnpm test:unit` 전체 통과(exit 0) · 로컬 두 DB 재생성 뒤 `pnpm test:integration` 61 파일 1530 테스트 통과
- 수정 뒤 재확인: 단위 first-login-session-hook 4/4, 통합 first-login-upgrade + first-login-hook 10/10
- acceptance: 추가 마이그레이션 파일 2개(0018 SQL·스냅숏), `head -3` 머리 줄 일치, hooks.ts 삭제 줄은 `@/repositories/users` import 한 줄뿐, `domain/people/index.ts` 추가 4 · 삭제 0, 금지 경로(`domain/org` · `repositories/{roles,team-memberships,teams}` · leak-scan · settings keys · permissions · package.json · lock · STATE.md · HANDOFF.json) diff 없음, people.test.ts 삭제 0, 표지 파일 1개

## 이어질 일

- 04.4-05: `/admin/people` 렌더(배지 · 폰 칸 접기 · 접근성) · E2E · SYSTEM.md/DECISIONS.md — `personLoginStatus`를 받아 그린다
- 04.4-06: 마이그레이션 0017·0018을 main 마지막+1로 합쳐 재생성하고 머리 줄과 백필 블록을 다시 붙인다 — 그 뒤 `first-login-upgrade.test.ts`를 다시 돌린다
- **/cso 필요** — 인증 경로(better-auth 세션 생성 훅)를 건드렸다
- Codex 재확인 필요(한도 2026-09-29 이후): 1b30b97 · fd899ad · e47f89f
