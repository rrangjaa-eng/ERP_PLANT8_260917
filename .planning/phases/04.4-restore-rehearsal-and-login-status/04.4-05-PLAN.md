---
phase: 04.4-restore-rehearsal-and-login-status
plan: 05
type: execute
wave: 5
depends_on: ["04.4-04"]
files_modified:
  - db/migrations/0030_restore_rehearsals.sql
  - db/migrations/0031_users_first_login_at.sql
  - db/migrations/0030_restore_rehearsal_login_status.sql
  - db/migrations/meta/0030_snapshot.json
  - db/migrations/meta/0031_snapshot.json
  - db/migrations/meta/_journal.json
autonomous: false
requirements: [OPS-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "병합 직전에 04.4가 만든 마이그레이션(`0030_restore_rehearsals` · `0031_users_first_login_at`의 SQL 둘 · 스냅숏 둘 · journal 항목 둘)을 지우고 `origin/main`을 병합한 뒤 `pnpm db:generate --name restore_rehearsal_login_status`로 04.4 마이그레이션 **하나**를 다시 만들고, **다시 0030에 둔다** — drizzle-kit이 다른 번호로 만들면 SQL을 `0030_restore_rehearsal_login_status.sql`, 스냅숏을 `meta/0030_snapshot.json`으로 이름을 바꾸고 journal 마지막 항목의 `idx`를 30, `tag`를 같은 이름으로 바꾼다(04.4-01과 같은 방법, 조정자 정정 B1·B2). 04.4-04의 백필 블록(표지 `-- 04.4 D8-07 first_login_at backfill` 이하)을 그 끝에 다시 붙인다(ROADMAP 04.4 기준 5)"
    - "조정자가 04.4에 0030~0034를 예약했다(Phase 4 = 0011~0016 · 04.1 = 0017~0020 · 04.2 = 0021~0024 · 04.3 = 0025~0029). 병합 뒤 04.4의 마이그레이션은 정확히 하나이고 그 tag는 `0030_`으로 시작한다. main에 이미 0030 이상 번호가 있으면(다른 페이즈가 범위를 넘었거나 04.4 번호를 썼으면) 이름을 고르지 않고 멈춰 조정자·사용자에게 넘긴다"
    - "재생성 뒤 `_journal.json`은 idx·tag가 유일하고 tag 접두가 idx와 같으며, 배열 순서대로 `when`이 엄격히 증가하고(04.4 항목의 `when`이 main의 모든 항목보다 늦어 drizzle `migrate()`가 조용히 건너뛰지 않는다), `meta/`의 이름순 마지막 스냅숏이 `0030_snapshot.json`(= `when`이 가장 큰 항목)이다 — journal 가드가 증명한다. idx 빈틈(0016 → 0030 등)은 정상이다"
    - "병합된 트리에서 빈 DB부터 전 마이그레이션이 적용되고, 리허설 기록 → 상태 화면 한 줄, 새 계정 → 두 배지 → 로그인 → 한 배지가 다시 관통한다(병합 트레이서)"
    - "병합된 트리에서 싼 게이트(lint · typecheck · lint:sql · build) → 별도 에이전트의 `CI=true` 독립 DOM 감사 → 수정 → 전체 게이트 한 번(단위 · 통합 · `CI=true` E2E)이 녹색이다(CLAUDE.md 화면 검증 순서, D6)"
    - "이 페이즈는 금지 범위(견적 줄·프로젝트·`document_counters`·demo 행 · `.planning/STATE.md` · `package.json`/`pnpm-lock.yaml`)를 자기 커밋으로 한 줄도 바꾸지 않았고, 공유 등록부는 덧붙이기만 했다(A2·A3)"
    - "`/ship` 병합과 staging 배포 뒤, 사용자가 GitHub Actions에서 `restore-rehearsal.yml`을 main · env=staging으로 한 번 돌려 성공하고, 스테이징 `/admin/system-status` 「복원 리허설」 행이 `성공 · 스테이징 · <KST 일시> · 백업 <id> · N분`이며, 이름에 `plant8-staging-rehearsal-`가 든 인스턴스가 남아 있지 않다(ROADMAP 04.4 기준 2 「1회」, D8-08)"
    - "이 페이즈에서 production 리허설은 돌리지 않는다 — 전환 전 프로덕션 원본 성공 기록은 Phase 8 기준 5가 요구하고 Phase 8 체크리스트가 같은 워크플로를 사람 확인과 함께 돌린다"
  prohibitions:
    - statement: "04.4보다 낮은 예약 범위(Phase 4 · 04.1 · 04.2 · 04.3)를 쓰는 페이즈가 아직 main에 없는데 사용자 확인 없이 04.4를 먼저 병합해서는 안 된다 — drizzle-kit이 이전 스냅숏을 이름순 마지막 파일로 고르므로(04.4-01 probe_fallback 가정 6), 뒤에 들어오는 낮은 번호 페이즈의 스냅숏이 0030보다 앞에 정렬되어 그 페이즈의 재생성·다음 생성이 깨진다. 이 순서 결정은 사용자(조정자)의 것이다"
      status: resolved
      verification: test
    - statement: "`git push`·`git push --force`를 하지 않는다 — PR 생성·병합은 Post-build `/ship`의 몫이다(CLAUDE.md)"
      status: resolved
      verification: test
    - statement: "세션이 gcloud를 직접 쓰거나, 워크플로를 production으로 돌리거나, 임시 비밀번호를 Actions 로그에서 읽어서는 안 된다 — 실제 리허설은 사용자가 staging으로 트리거하고 스테이징 화면 확인도 사용자가 한다(C1 · C4)"
      status: resolved
      verification: judgment
    - statement: "실제 실행이 실패하면 같은 워크플로를 무작정 다시 돌려서는 안 된다 — `systematic-debugging`으로 원인을 찾고 gaps 플랜으로 고친 뒤 다시 한 번 돌린다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/migrations/0030_restore_rehearsal_login_status.sql"
      provides: "main 위에서 재생성해 다시 0030에 둔 04.4 마이그레이션 하나(백필 포함)"
      contains: "04.4 D8-07 first_login_at backfill"
    - path: "db/migrations/meta/_journal.json"
      provides: "main 항목 + 04.4 항목 하나(idx 30), journal 가드 녹색"
      contains: "entries"
  key_links:
    - from: "db/migrations/meta/_journal.json"
      to: "test/unit/db/migration-journal.test.ts"
      via: "재생성 결과가 idx · tag · when · .sql 불변식을 지킨다"
      pattern: "idx"
    - from: ".github/workflows/restore-rehearsal.yml"
      to: "app/(app)/admin/system-status/page.tsx"
      via: "실제 1회 실행의 record Job이 스테이징 DB에 쓴 줄이 스테이징 상태 화면에 보인다"
      pattern: "복원 리허설"
---

<objective>
**As a** 이 저장소를 병합하고 운영하는 사람, **I want to** 04.4 브랜치가 main 위에서 마이그레이션 충돌 없이 돌고 전체 게이트를 `CI=true`로 통과한 뒤, 병합·배포된 스테이징에서 복원 리허설이 실제로 한 번 성공하기를, **so that** OPS-03의 「복원 리허설 1회」가 코드가 아니라 실제 실행으로 증명되고, Phase 8이 같은 워크플로와 화면을 믿고 쓸 수 있다.

Task 1은 **트레이서**다 — main 병합 → 마이그레이션 하나로 재생성(+ 백필 재부착) → 빈 DB 적용 → 리허설 기록·로그인 배지를 병합된 트리에서 한 번 관통한다. Task 2는 페이즈 전체 게이트(싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번)와 금지 범위 확인이다. Task 3은 **사람이 트리거하는** 실제 staging 리허설 1회다(`checkpoint:human-action`, 자동 승인 불가).

Purpose: B2(병합 직전 재생성) · D8-08(실제 1회) · ROADMAP 04.4 기준 2·5.
Output: main 위에 재생성된 04.4 마이그레이션 하나, 병합 커밋, 게이트·감사 결과, 실제 실행 기록(SUMMARY).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-VALIDATION.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-01-SUMMARY.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-04-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 시작 전에 Superpowers `verification-before-completion`을 **호출**한다 — 이 플랜의 모든 판정은 명령의 실제 출력으로 한다. 병합 충돌·테스트 실패가 나면 원인을 쫓기 전에 `systematic-debugging`을 호출한다. 새 코드를 써야 하는 수정이 생기면 먼저 `test-driven-development`를 호출한다. 서브에이전트(독립 DOM 감사)에도 세 스킬 이름, `model`, `CI=true`를 적는다.
- 첫 task 전에 `git fetch origin main` 뒤 `git merge-base HEAD origin/main`을 `PHASE_BASE`로 적는다(SUMMARY 머리에도). 이 브랜치의 기준점은 계획 시점 `a206e9e`(PR #58)였고, 계획 중에 main이 PR #59(Phase 04.5 삽입 · SYSTEM.md/DECISIONS.md 디자인 수정)로 이미 앞서 나갔다 — 병합 충돌 후보로 적어 둔다.
- `git push` 하지 않는다. 병합 커밋까지만 만든다. `.planning/STATE.md`를 고치지 않는다.
- 이 플랜은 로컬 DB를 다시 만든다 — 다른 플랜과 동시에 돌지 않는다(체인 마지막).
- **Post-build(D10, CLAUDE.md — 건너뛰지 않는다)**: Task 2가 끝나면 Task 3 체크포인트에서 실행이 멈춘다. 그 사이에 사용자와 함께 `/review` → `/qa` → `/design-review`(UI 변경) → `/cso`(**필수** — 운영 인스턴스 권한·외부 입력(workflow 입력)·인증 훅을 건드렸다) → `/ship`을 실제로 호출한다. `/ship`이 병합하면 `deploy.yml` staging 잡이 새 이미지·`plant8-staging-restore` Job·마이그레이션을 스테이징에 올린다. 그 뒤에 Task 3을 한다.
</execution_notes>

<probe_fallback>
이 플랜은 새 동작을 만들지 않는다 — 04.4-01~04의 truths가 병합된 트리에서도 참인지 다시 증명하고, 실제 GCP에서 한 번 실측한다.

**실측으로 닫는 계획 가정(Task 3)**: A1 복원 전 대상 생성(API 정의로 답함) · A2 실행 시점 env 덮어쓰기로 임시 인스턴스 연결 · A3 복원 뒤 IAM DB 사용자 · A7 `gha-deployer`의 인스턴스 생성·삭제 권한 · A12 생성·복원 대기 시간. 하나라도 틀리면 워크플로가 실패로 끝나고(거짓 성공 없음) 임시 인스턴스는 지워진다 — 그때는 결과 요약·Actions 로그로 원인을 찾아 gaps 플랜을 만든다.

**왜 실제 실행이 병합 뒤인가(가정 A8)**: `workflow_dispatch`는 기본 브랜치에 있는 워크플로만 Actions 화면에 보인다. 또 스테이징에 `plant8-staging-restore` Job과 `restore_rehearsals` 표가 있어야 하는데, 둘은 main 병합 뒤 `deploy.yml`이 올린다. 기능 브랜치를 스테이징에 먼저 배포하면 재생성 **전** 마이그레이션이 스테이징에 적용되어 뒤에 번호·`when`이 어긋난다 — 그래서 하지 않는다.

**Deferred(이 페이즈 밖)**: 정기 리허설 스케줄(CONTEXT Deferred) · issue #56 자체 해결 · production 원본 리허설과 전환일 체크리스트(Phase 8) · Google 로그인 경로의 첫 로그인 기록(04.4-04 가정 3).
</probe_fallback>

<tasks>

<task type="tracer">
  <name>Task 1: 병합 트레이서 — origin/main 병합 → 04.4 마이그레이션 하나로 재생성(+ 백필 재부착) → 빈 DB 적용 → 기록·배지 관통</name>
  <files>db/migrations/meta/_journal.json</files>
  <precondition>`git fetch origin main` 뒤 `git show origin/main:db/migrations/meta/_journal.json`에 접두가 0030 이상인 tag가 없다. 그리고 낮은 예약 범위 페이즈(Phase 4 0011~0016 · 04.1 0017~0020 · 04.2 0021~0024 · 04.3 0025~0029) 중 main journal에 아직 없는 범위를 SUMMARY에 적는다 — 하나라도 없으면(계획 시점 main은 0010에서 끝나 넷 다 없다) 이 task를 시작하지 않고 멈춰 「04.4를 먼저 병합할지, 그 페이즈들을 기다릴지」를 사용자 결정으로 넘긴다(가정 6의 스냅숏 순서 위험을 함께 전한다). 그 페이즈 중 마이그레이션을 만들지 않은 페이즈가 있으면 사용자가 그 사실을 확인해 준 경우에만 진행한다</precondition>
  <reversibility rating="costly">04.4 마이그레이션 번호가 바뀐다 — 옛 번호로 적용된 로컬 DB는 `bash scripts/dev-db.sh`로 다시 만든다. 스테이징·프로덕션에는 아직 적용되지 않았으므로 되돌릴 수 없는 결정은 아니다</reversibility>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-RESEARCH.md` 284–295행(Pitfall 4 OPERATIONS 경합 · Pitfall 5 journal 번호)
    - `scripts/migrate-runner.ts` 40–60행 · `node_modules/drizzle-orm/pg-core/dialect.js`의 `created_at` 비교 부분 — `when`이 오래된 항목을 건너뛰는 판정
    - `test/unit/db/migration-journal.test.ts`
    - 04.4-01 · 04.4-04 SUMMARY — 이 페이즈가 만든 마이그레이션 tag 두 개와 백필 블록
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-PATTERNS.md`의 병합 위험 표(있으면)
  </read_first>
  <action>
① `PHASE_BASE` 기준으로 이 페이즈가 **추가한** 마이그레이션 파일을 찾는다(`git diff --name-only --diff-filter=A "$PHASE_BASE"..HEAD -- db/migrations` — `0030_restore_rehearsals.sql` · `0031_users_first_login_at.sql` · `meta/0030_snapshot.json` · `meta/0031_snapshot.json`). SUMMARY에 목록을 적는다. `0031_users_first_login_at.sql`에서 표지 줄 `-- 04.4 D8-07 first_login_at backfill`부터 끝까지를 스크래치 파일(저장소 밖)에 그대로 보관한다.

② 커밋 하나로 그 파일들을 지우고 `db/migrations/meta/_journal.json`을 `git show "$PHASE_BASE":db/migrations/meta/_journal.json` 출력으로 덮는다(생성물을 이력에서 복원 — 손 편집이 아니다). 제목 `chore: drop 04.4 migrations before regenerating on main`, 본문 한국어 한 줄.

③ `git merge origin/main`. 충돌은 이 순서로 푼다 — (a) 덧붙이기 전용(`db/schema/index.ts` · `test/unit/deploy/deploy-sh.test.ts`의 Job 수 · `scripts/build-cli.mjs` · `scripts/deploy.sh` `deploy_jobs` 끝 · `test/unit/docs-limits.test.ts` 토큰 · `docs/OPERATIONS.md` 절 · SYSTEM.md · DECISIONS.md · `domain/system-status/index.ts` 필드 · 상태 화면 `KvList` 항목)은 양쪽을 모두 남긴다 — 상태 화면은 「복원 리허설」이 「마지막 백업」 **바로 다음**이 되게 두고(04.2 항목은 그 뒤), Job 수 단언은 양쪽이 더한 수의 합으로 맞춘다 (b) `test/unit/db/migration-journal.test.ts`가 양쪽에 있으면 main 쪽을 채택한다(B3) (c) `docs/OPERATIONS.md`가 300줄을 넘게 되면 멈추고 보고한다(줄이는 판단은 사용자) (d) 그 밖에 한쪽 의미를 골라야 하는 충돌은 **추측하지 않고** `git merge --abort` 후 충돌 파일과 양쪽 의도를 SUMMARY에 적어 멈춘다.

④ 병합된 트리에서 `pnpm db:generate --name restore_rehearsal_login_status` — 04.4 마이그레이션 **하나**(`restore_rehearsals` 생성 + `users.first_login_at` 추가)가 drizzle-kit 규칙(main journal 마지막 idx + 1)의 번호로 생긴다. **커밋 전에 다시 0030에 둔다**(B1·B2 조정자 정정, 04.4-01 Task 1 ④와 같은 방법): 생성 번호가 0030이 아니면 SQL을 `db/migrations/0030_restore_rehearsal_login_status.sql`, 스냅숏을 `db/migrations/meta/0030_snapshot.json`으로 이름을 바꾸고 journal 마지막 항목의 `idx`를 30, `tag`를 `0030_restore_rehearsal_login_status`로 바꾼다(다른 항목·필드는 그대로). ①에서 보관한 백필 블록을 그 SQL 끝에 그대로 붙인다(앞에 `--> statement-breakpoint`). 그 뒤 `pnpm db:generate` 재실행이 "No schema changes"인지(이름순 마지막 스냅숏이 0030이고 스키마와 일치), journal `when`이 배열 순서대로 엄격히 증가하는지(가드) 확인하고, `bash scripts/dev-db.sh && pnpm db:migrate`(빈 DB부터 전부) · `pnpm db:reset:test` · `pnpm lint:sql`. 최종 tag와 main 마지막 tag를 SUMMARY에 적는다. 이름 바꾸기 뒤 "No schema changes"가 나오지 않으면 추측으로 고치지 말고 멈춰 보고한다(04.4-01 가정 6의 대안 — main 마지막 번호 + 1 사용 — 으로 갈지는 조정자 결정). 제목 `chore: regenerate 04.4 migration on top of main at 0030`.

⑤ 병합 트레이서: 리허설 기록·검증·첫 로그인·백필 통합 테스트와 상태 화면·사람 목록 E2E를 병합된 트리에서 돌린다.
  </action>
  <verify>
    <automated>J=$(git show origin/main:db/migrations/meta/_journal.json) &amp;&amp; printf '%s' "$J" | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8'));const n=j.entries.map(e=&gt;parseInt(e.tag.slice(0,4),10));const lower=[[11,16],[17,20],[21,24],[25,29]].filter(([a,b])=&gt;!n.some(x=&gt;x&gt;=a&amp;&amp;x&lt;=b));console.log('missing lower ranges:',JSON.stringify(lower));process.exit(n.some(x=&gt;x&gt;=30)||lower.length?1:0)"</automated>
    <fails_when>종료 코드가 0이 아니다 — origin/main을 읽지 못했거나, main에 0030 이상 번호가 이미 있거나, 낮은 예약 범위 중 main에 없는 것이 있다(출력의 missing lower ranges). precondition 불충족이므로 멈추고 사용자에게 병합 순서를 묻는다(마이그레이션을 만들지 않은 페이즈라고 사용자가 확인한 범위만 예외)</fails_when>
    <automated>test -f db/migrations/0030_restore_rehearsal_login_status.sql &amp;&amp; test -f db/migrations/meta/0030_snapshot.json &amp;&amp; pnpm db:generate 2&gt;&amp;1 | grep -q "No schema changes" &amp;&amp; bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db/migration-journal.test.ts</automated>
    <fails_when>재생성 뒤 스키마와 마이그레이션이 어긋나거나, 빈 DB 적용 · squawk · journal 가드 중 하나라도 실패한다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/restore-rehearsal.test.ts test/integration/restore-verify.test.ts test/integration/people-login-status.test.ts test/integration/first-login-backfill.test.ts &amp;&amp; pnpm exec playwright test test/e2e/system-status.spec.ts test/e2e/people.spec.ts --project desktop</automated>
    <fails_when>병합된 트리에서 기록·검증·첫 로그인·백필(표지가 정확히 한 파일) 통합 테스트나 상태 화면·사람 목록 E2E가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=A origin/main..HEAD -- db/migrations`가 정확히 `db/migrations/0030_restore_rehearsal_login_status.sql`과 `db/migrations/meta/0030_snapshot.json` 두 줄이다(0031 파일은 남아 있지 않다)
    - 그 SQL에 `restore_rehearsals`와 `first_login_at`과 표지 `-- 04.4 D8-07 first_login_at backfill`이 모두 있다
    - SUMMARY에 최종 tag(`0030_restore_rehearsal_login_status`) · main 마지막 tag · main에 있던 예약 범위 목록이 있다
    - `git log origin/main..HEAD --merges`가 1줄 이상이고 `git status --porcelain`이 비어 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>04.4가 main 위에서 마이그레이션 하나(백필 포함)로 재생성되어 빈 DB에 적용되고, 기록 → 화면 · 발급 → 배지가 병합된 트리에서 관통한다</done>
</task>

<task type="auto">
  <name>Task 2: 페이즈 게이트 — 싼 게이트 → 독립 DOM 감사(`CI=true`) → 수정 → 전체 게이트 한 번 · 금지 범위 확인</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-VALIDATION.md`(요구사항 → 테스트 표 · Manual-Only)
    - `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md` 79–97행(색) · 111–131행(문구) · 145–172행(배지 · 반응형) · 188–211행(UI Considerations)
    - `.planning/ROADMAP.md` Phase 04.4 성공 기준 1~5
    - `playwright.config.ts` — `CI=true`면 프로덕션 빌드, 프로젝트 `desktop` · `mobile-375`(`mobile-*.spec.ts`)
  </read_first>
  <action>
① **싼 게이트**: `pnpm lint && pnpm typecheck && pnpm lint:sql && pnpm build`. 실패하면 `systematic-debugging` → 고침 → 다시.

② **독립 DOM 감사(D6)**: 실행자가 아닌 별도 서브에이전트(`model: "opus"` — CLAUDE.md 「점검·판단·검토는 Opus」, 프롬프트에 세 Superpowers 스킬 이름과 「스크린샷 육안 판정 금지, DOM 실측만」)에게 맡긴다. 그 에이전트는 저장소에 커밋하지 않는 임시 spec(예: `test/e2e/zz-dom-audit-04-4.spec.ts`, 끝나면 지운다)을 만들어 `CI=true pnpm exec playwright test <그 파일> --project desktop`으로 **프로덕션 빌드**에서 폭 1280과 375(`test.use({ viewport })`)를 실측한다:
  (a) `/admin/system-status` — `dt` 「복원 리허설」이 `dt` 「마지막 백업」의 바로 다음 `dt`다 · 표가 빌 때 `dd` 텍스트가 정확히 `리허설 기록 없음 — 첫 리허설 전` · 실패 기록을 넣으면 링크 텍스트 `실행 기록`, `target` 없음, 계산된 색이 `--accent` 값, 밑줄, 375에서 상자 44×44 이상 · 백업 id span의 `white-space: nowrap` · `font-variant-numeric`에 `tabular-nums` · 가로 스크롤 0 · 새 행이 굵기·색으로 강조되지 않음(다른 `dd`와 같은 font-weight·color).
  (b) `/admin/people` — 로그인한 적 없는 사용자 행: 1280에서 두 배지가 한 줄(같은 top)이고 구분자 보임, 375에서 세로(둘째 top ≥ 첫째 bottom − 1)이고 구분자 `display: none` · 배지 계산 색이 `--muted` 값, font-size 11px · 보관된 사람 행은 `보관됨`만 · 배지 0개 행은 `—`.
  감사 결과를 표(화면 × 폭 × 항목 × 판정 × 실측값)로 받아 SUMMARY에 넣는다.

③ **수정**: 위반이 있으면 고친다(새 동작이면 TDD). 고친 뒤 ①만 다시 돈다.

④ **전체 게이트 한 번**: `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test:unit && pnpm test:integration && pnpm test:e2e:ci`. 실패하면 원인을 고친 뒤 ④를 다시(①~③은 UI를 고친 경우에만 다시).

⑤ **금지 범위(A2·A3)**: 이 페이즈 자신의 커밋(병합으로 들어온 main 커밋 제외)이 금지 경로를 바꾸지 않았는지 확인한다(verify 넷째). `wc -l docs/OPERATIONS.md` ≤ 300. 공유 등록부 `domain/action-log/record.ts` · `domain/settings/keys.ts` · `test/integration/leak-scan.test.ts` 변경이 0인지(이 페이즈는 그 셋에 덧붙일 것도 없었다).

⑥ ROADMAP 04.4 성공 기준 1~5 × 증명한 테스트 파일 대응표, 요구사항 OPS-03 대응, `<probe_fallback>` Deferred 목록, 그리고 다음 단계 「Task 3 체크포인트에서 멈춤 → `/review` → `/qa` → `/design-review` → `/cso`(필수) → `/ship` → staging 배포 확인 → Task 3 실제 리허설 → `/gsd-verify-work`」를 SUMMARY에 적는다. `git reflog`에 push 흔적이 없다는 한 줄도.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql &amp;&amp; pnpm build</automated>
    <fails_when>어느 하나라도 오류를 내고 0이 아닌 코드로 끝난다</fails_when>
    <automated>bash scripts/dev-db.sh &amp;&amp; pnpm db:migrate &amp;&amp; pnpm test:unit &amp;&amp; pnpm test:integration</automated>
    <fails_when>단위·통합 어느 쪽이든 failed가 1 이상이다</fails_when>
    <automated>pnpm test:e2e:ci</automated>
    <fails_when>`CI=true` 프로덕션 빌드 E2E(desktop + mobile-375)에서 failed가 1 이상이거나 종료 코드가 0이 아니다</fails_when>
    <automated>OUT=$(git log --no-merges --name-only --format= "$PHASE_BASE"..HEAD --not origin/main -- .planning/STATE.md package.json pnpm-lock.yaml domain/quotes domain/projects domain/document-numbering db/schema/quote-lines.ts db/schema/quote-revisions.ts db/schema/projects.ts db/schema/document-counters.ts repositories/quote-lines.ts repositories/quote-revisions.ts repositories/projects.ts repositories/document-counters.ts domain/action-log domain/settings/keys.ts test/integration/leak-scan.test.ts test/unit/deploy/fakebin) &amp;&amp; test -z "$OUT" &amp;&amp; test "$(wc -l &lt; docs/OPERATIONS.md)" -le 300</automated>
    <fails_when>종료 코드가 0이 아니다 — git이 실패했거나(`PHASE_BASE` 미설정 등), 이 페이즈 자신의 커밋이 금지·공유 경로를 바꿨거나, OPERATIONS.md가 300줄을 넘는다</fails_when>
  </verify>
  <acceptance_criteria>
    - 네 검증 명령이 전부 초록이다
    - 독립 DOM 감사 표(화면 × 폭 × 항목 × 판정 × 실측값)가 SUMMARY에 있고 위반 0이며, 감사를 맡은 에이전트의 모델이 적혀 있다
    - 감사용 임시 spec이 저장소에 남아 있지 않다(`git status --porcelain`이 비어 있다)
    - 성공 기준 1~5 × 테스트 파일 대응표, Deferred 목록, Post-build 순서가 SUMMARY에 있다
  </acceptance_criteria>
  <done>병합된 트리가 싼 게이트 → 독립 DOM 감사 → 전체 게이트 한 번을 `CI=true`로 통과하고, 금지 범위가 지켜졌으며, 다음 단계가 기록되었다</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: 실제 staging 복원 리허설 1회 — 사용자가 GitHub Actions에서 트리거한다</name>
  <read_first>
    - `docs/OPERATIONS.md` §14 백업·복원(04.4-03) — 사용자에게 줄 실행법과 같은 내용
    - 04.4-03 SUMMARY — 워크플로 입력 이름과 요약 항목
  </read_first>
  <action>사람만 할 수 있는 한 가지: 병합·배포가 끝난 뒤 GitHub Actions에서 `restore-rehearsal.yml`을 **main · env=staging**으로 한 번 실행한다. 사람이 하는 이유 — 이 프로젝트는 세션이 gcloud를 직접 쓰지 않고(CONTEXT), 실제 Cloud SQL 인스턴스를 만들고 지우는 운영 행동이며, `workflow_dispatch`는 기본 브랜치의 워크플로만 보인다(C5). production은 이 페이즈에서 돌리지 않는다(Phase 8).</action>
  <instructions>
    Claude가 이미 한 것: 워크플로·스크립트·Job·기록·화면·문서, 병합된 트리의 전체 게이트(Task 1~2).

    그 사이에 할 것(순서대로, CLAUDE.md Post-build — 건너뛰지 않는다): `/review` → `/qa` → `/design-review` → `/cso`(필수) → `/ship`(PR 생성·병합). 병합되면 GitHub Actions `deploy.yml`의 staging 잡이 성공했는지 확인한다(이 잡이 `plant8-staging-restore` Job을 배포하고 `restore_rehearsals` 표 마이그레이션을 적용한다).

    누가·어디서: 저장소 write 권한이 있는 사용자가 GitHub → Actions → 「restore-rehearsal」 → Run workflow → Branch `main` · env `staging` · confirm_production은 **비워 둔다** → Run.

    기다리는 동안: 수십 분 걸린다(임시 인스턴스 생성·복원·확인·삭제). 도중에 다시 누르지 않는다.

    확인할 것(넷):
    1. 실행이 초록(성공)으로 끝났다
    2. 실행 요약(Summary)에 `성공` · 백업 id · 임시 인스턴스 이름 `plant8-staging-rehearsal-<실행 id>-<시도>` · `남은 임시 인스턴스 0`이 보인다
    3. 스테이징 `/admin/system-status`(본인 관리자 계정으로 — Claude 검증 계정·임시 비밀번호를 세션에 넘기지 않는다)의 「복원 리허설」 행이 `성공 · 스테이징 · <KST 일시> · 백업 <id> · N분`이고 「마지막 백업」 바로 다음 줄이다
    4. GCP 콘솔 → SQL 인스턴스 목록에 이름에 `plant8-staging-rehearsal-`가 든 인스턴스가 없다

    실패하면: 다시 누르지 않는다. 실행 URL과 요약의 결과·단계를 알려 준다 — Claude가 `systematic-debugging`으로 원인을 찾고(요약·Actions 로그 읽기, 비밀 없음) gaps 플랜으로 고친 뒤 한 번 더 돌린다. 임시 인스턴스가 남았다고 요약에 나오면 OPERATIONS §14대로 그 이름만 지운다.
  </instructions>
  <verification>재개 신호로 받은 값을 SUMMARY에 기록한다: 실행 URL · 백업 id · 소요 시간 · 확인 넷의 결과 · 가정 A2·A3·A7·A12의 실측 판정(실행 로그 기준). 실행 URL이 GitHub MCP로 읽힌다면 그 실행의 conclusion이 success이고 로그에 「복원본 확인을 통과했습니다」가 있는지 읽기 전용으로 확인한다(이 워크플로 로그에는 비밀번호가 없다). 스테이징 화면 확인은 사용자의 확인 3을 그대로 적는다.</verification>
  <verify>
    <automated>grep -qE "https://github\.com/[^ ]+/actions/runs/[0-9]+" .planning/phases/04.4-restore-rehearsal-and-login-status/04.4-05-SUMMARY.md &amp;&amp; grep -q "남은 임시 인스턴스 0" .planning/phases/04.4-restore-rehearsal-and-login-status/04.4-05-SUMMARY.md &amp;&amp; grep -qE "성공 · 스테이징 · [0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}" .planning/phases/04.4-restore-rehearsal-and-login-status/04.4-05-SUMMARY.md</automated>
    <fails_when>SUMMARY에 실제 실행 URL · 「남은 임시 인스턴스 0」 · 스테이징 화면의 성공 문구 중 하나가 없다 — 실제 1회 성공이 기록되지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - 사용자 재개 신호에 실행 URL · 백업 id · 소요 시간이 있다
    - 확인 넷이 모두 「예」다
    - 가정 A2·A3·A7·A12의 실측 판정이 SUMMARY에 있다
    - verify 명령이 초록이다
  </acceptance_criteria>
  <resume-signal>「리허설 성공」 + 실행 URL + 백업 id + 소요 시간(분) + 확인 1~4 결과. 실패면 「리허설 실패」 + 실행 URL + 요약의 결과·단계</resume-signal>
  <done>staging 원본으로 복원 리허설이 실제로 한 번 성공했고, 스테이징 상태 화면에 그 결과가 보이며, 임시 인스턴스가 남지 않았다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 마이그레이션 → 운영 DB | 병합 뒤 journal 순서가 운영 적용 순서다 |
| 여러 병렬 브랜치의 병합 | Phase 4 · 04.1~04.3 · 04.5가 같은 파일에 덧붙인다 |
| 사용자 → 실제 GCP(staging) | 리허설 트리거는 사람이 하고 세션은 gcloud를 쓰지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-23 | Tampering | 마이그레이션 번호·순서 | high | mitigate | 병합 직전 04.4 마이그레이션을 지우고 main 위에서 하나로 재생성해 0030에 둔다 — `when`이 main보다 늦다. journal 가드(이름순 마지막 스냅숏 = 최신 항목 포함) + 재생성 뒤 "No schema changes" + 빈 DB 전체 적용 + 표지가 정확히 한 파일(Task 1). 낮은 예약 범위가 아직 main에 없으면 병합 순서를 사용자가 정한다(precondition) |
| T-04.4-24 | Tampering | 병합 충돌 해소 | high | mitigate | 덧붙이기 전용은 양쪽 보존, 의미 판단 충돌은 병합 중단 후 보고(추측 금지). 병합된 트리에서 전체 스위트 `CI=true`(Task 2) |
| T-04.4-25 | Elevation of Privilege | 실제 실행의 대상 | critical | mitigate | Task 3은 staging만, confirm_production 비움. production 경로는 워크플로·스크립트의 확인 입력이 막고 이 플랜은 그 경로를 쓰지 않는다. 원본 보호는 04.4-03 가드가 증명 |
| T-04.4-26 | Information Disclosure | 검증 계정 비밀번호 | medium | mitigate | 스테이징 화면 확인은 사용자 본인 계정으로 한다 — 세션이 Actions 로그의 임시 비밀번호를 읽지 않는다(C4) |
| T-04.4-27 | Repudiation | 강제 푸시로 이력 손실 | medium | mitigate | 이 플랜은 push를 하지 않는다. PR·병합은 `/ship` |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개 — Task 2 넷째 검증이 이 페이즈 커밋 범위의 `package.json`·`pnpm-lock.yaml` 변경 0을 확인한다 |
</threat_model>

<verification>
- 병합된 트리에서 `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `pnpm build` 통과
- 독립 DOM 감사(`CI=true`, 별도 에이전트) 위반 0
- `bash scripts/dev-db.sh && pnpm db:migrate && pnpm test:unit && pnpm test:integration` 통과
- `pnpm test:e2e:ci` 통과
- journal 가드 녹색, 04.4 SQL 하나(백필 포함)가 `0030_restore_rehearsal_login_status`, `when`이 main 모든 항목보다 늦음, 이름순 마지막 스냅숏 = 0030
- 금지·공유 경로 04.4 커밋 범위 변경 0, OPERATIONS.md ≤ 300줄
- 실제 staging 리허설 1회 성공이 SUMMARY에 기록됨(Task 3)
</verification>

<success_criteria>
- 04.4가 main 위에서 마이그레이션 충돌 없이 돈다
- 페이즈 전체가 `CI=true` 게이트와 독립 DOM 감사를 통과한다
- Post-build 넷(+ `/design-review`)을 거쳐 병합·배포된 뒤, staging 리허설이 실제로 한 번 성공하고 화면에 보인다
- 임시 인스턴스가 남지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | main 위에서 재생성해 다시 0030에 둔 04.4 SQL 1개 `0030_restore_rehearsal_login_status.sql` + `meta/0030_snapshot.json`(`restore_rehearsals` + `users.first_login_at` + 백필, 브랜치의 0030·0031 둘 대체) |
| 커밋 | `chore: drop 04.4 migrations before regenerating on main` · 병합 커밋 · `chore: regenerate 04.4 migration on top of main` |
| 운영 기록 | staging `restore_rehearsals` 첫 행(실제 실행) · Actions 실행 URL |
| 기록 | SUMMARY — 게이트 결과 · 독립 DOM 감사 표 · 성공 기준 대응표 · 실측 가정 판정 · Deferred · Post-build 순서 |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-05-SUMMARY.md` when done
</output>
