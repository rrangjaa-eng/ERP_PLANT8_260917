---
phase: 04.4-restore-rehearsal-and-login-status
plan: 05
type: execute
wave: 5
depends_on: ["04.4-04"]
files_modified:
  - app/(app)/admin/system-status/system-status.module.css
  - app/(app)/admin/system-status/page.tsx
  - test/e2e/system-status.spec.ts
  - app/(app)/admin/people/people.module.css
  - app/(app)/admin/people/page.tsx
  - test/e2e/people.spec.ts
  - test/e2e/mobile-people.spec.ts
  - docs/design/SYSTEM.md
  - docs/design/DECISIONS.md
  - test/unit/design-system-docs.test.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "상태 화면 「복원 리허설」 행(04.4-01이 만든 문구)이 브라우저에서 UI-SPEC 문구 그대로 보인다: 기록 없음 · 성공(`run_url`이 있어도 「실행 기록」과 그 앞 `·`가 함께 빠지고 `a` 요소가 없다 — 링크 생략 분기의 브라우저 증명) · 실패(단계 하나) · 겹친 실패의 저장된 `정리` 행. `run_url`이 NULL인 실패 행은 04.4-01의 `restore_rehearsals_failed_run_url_check`가 DB에서 막아 E2E로 넣을 수 없다 — 그 방어적 렌더는 04.4-01의 순수 함수 단위 테스트가 맡는다(UI 체커 7차). 링크는 같은 탭(`target` 없음)이고 `.detailLink`와 같은 다섯 속성(`--fs-sm` · `--fw-medium` · `--accent` · 밑줄 · `--underline-offset`), 폰에서 44×44 이상이다. 백업 id는 `white-space: nowrap` + `tabular-nums`다(D8-08, UI-SPEC Color · Responsive 2)"
    - "사람 목록 「상태」 칸이 `personLoginStatus`(04.4-04)를 받아 두 배지를 `StatusTag kind=\"muted\" variant=\"text\"`로 그린다 — PC(≥700px)는 ` · `로 이어 한 줄, 폰(<700px)은 구분자를 숨기고 세로로 쌓는다(`gap: var(--s-1)`). 보관된 사람은 `보관됨` 하나, 배지 0개는 `—`(D8-07, ROADMAP 04.4 기준 4)"
    - "폰(<700px)에서 사람 목록은 §7-3 칸 접기를 따른다: P1 = 이름 · 상태 · 동작, P2 = 이메일 · 계급 · 현재 소속(머리글·셀 숨김, 각 행 아래 접힌 줄 하나에 `값 · 값 · 값`), P3 없음. PC에서는 접힌 줄이 `display: none`이라 같은 값이 한 번만 노출된다(UI-SPEC Responsive)"
    - "이름 칸은 행마다 문서 안에서 유일한 `id`를 가진 `th scope=\"row\"`이고, 접힌 줄 셀은 `headers`로 그 id를 가리키며, 각 값 앞에 전역 `.sr-only`(`app/globals.css`) 필드 라벨 `이메일` · `계급` · `현재 소속`이 있다. 접힌 줄과 그 조상에 `aria-hidden`이 없다(UI-SPEC 접근성 관계 ①~④)"
    - "사람 목록 전용 수식 클래스 `.peopleTable`이 목록 표에만 붙고, 그 아래에만 새 규칙이 있다: 열 머리글 · 행 머리글 · 셀 · 접힌 줄 셀 네 곳의 `line-height: var(--lh-table)`, 행 머리글이 셀과 같은 값(`min-height: var(--row-min)` · `padding: var(--cell-pad-y) var(--cell-pad-x)` · `--fs-base` · `--fw-regular` · 왼쪽 정렬 · 얇은 `--line` 아래선, `white-space` 선언 없음), 행 머리글 hover `--surface`, 접힌 줄 셀 `padding: 0 var(--cell-pad-x) var(--cell-pad-y)`. 공유 선택자 변경은 `.table th` → `.table thead th` 하나뿐이고, 공유 `.table td`와 `.table tbody tr:hover td`는 그대로이며, 계급 화면 표(`roles-client.tsx`)의 계산 스타일은 바뀌지 않는다(UI-SPEC Typography 범위 한정 · 행 머리글 스타일, Codex 5차)"
    - "가장 긴 상태 조합(두 배지)과 공백 없는 영문 30자 이름을 가진 행이 있을 때, 폭 360px(배율 100%)과 폭 640px · `deviceScaleFactor` 2(= 1280px 200% 확대)에서 문서와 표 모두 `scrollWidth <= clientWidth`이고, 행 머리글의 계산 `white-space`가 `nowrap`이 아니며 굵기 400이다. 「상세」 링크와 보관 버튼은 44×44 이상이고 화면 안이다(UI-SPEC 검증 조건)"
    - "시스템 문서가 이 화면과 같다: SYSTEM.md §6-8 와이어프레임에 「복원 리허설」 줄이 「마지막 백업」 바로 다음에 있고 §6-8 본문 · LOADING · EMPTY 세 곳에 고정 개수 문구가 없다 · §7-5에 두 로그인 문구의 길이 예외 줄 · §7-3 P1 줄에 금액 열 없는 표 규칙 한 문장 · DECISIONS.md에 두 항목(§7-5 길이 예외와 그 위치 이탈 · §7-3 금액 열 없는 표의 P1)(UI-SPEC 갱신 ①②③, CLAUDE.md 「화면 하나만 예외 금지」)"
  prohibitions:
    - statement: "새 색 · 서체 · radius · 토큰을 만들어서는 안 된다 — 모든 값은 `docs/design/tokens.css` 변수다(stylelint, CLAUDE.md)"
      status: resolved
      verification: test
    - statement: "공유 `.table td` · `.table tbody tr:hover td` 규칙을 고치거나 `line-height`를 공유 `.table` 선택자에 두어서는 안 된다 — 계급 화면이 같은 스타일시트를 쓴다(Codex 5차)"
      status: resolved
      verification: test
    - statement: "접힌 줄 `tr`·`td`에 `aria-hidden`을 두어서는 안 된다 — 폰에서 P2 값의 유일한 출처다(`ui/table/Table.tsx`의 행 탭 없는 분기를 베끼지 않는다)"
      status: resolved
      verification: test
    - statement: "`ui/table/Table`(편집용 `role=\"grid\"`)이나 새 컴포넌트 · 카드 전환을 써서는 안 된다 — 같은 `table`에 CSS와 접힌 줄 `tr`만 더한다(UI-SPEC 구현 범위)"
      status: resolved
      verification: judgment
    - statement: "「복원 리허설」 행에 `--danger` · `--success`를 쓰거나 링크를 새 탭으로 열어서는 안 된다(UI-SPEC Color)"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/admin/system-status/system-status.module.css"
      provides: "`.runLink`(`.detailLink`와 같은 다섯 속성 + 폰 44×44) · `.backupId`(nowrap · tabular-nums)"
      contains: "runLink"
    - path: "app/(app)/admin/people/people.module.css"
      provides: "`.table thead th`로 좁힌 열 머리글 · `.peopleTable` 아래의 `--lh-table` 네 곳 · 행 머리글 · 행 머리글 hover · `.prioP2` · `.collapsedRow` · `.collapsedCell` · `.badges` · `.badgeSep` · 폰 미디어 쿼리 `max-width: 699.98px`"
      contains: "peopleTable"
    - path: "app/(app)/admin/people/page.tsx"
      provides: "배지 렌더 · 행 머리글 `th scope=\"row\"` · 접힌 줄 `tr`(`colSpan` · `headers` · `.sr-only` 라벨) · `styles.peopleTable`"
      contains: "personLoginStatus"
    - path: "test/e2e/system-status.spec.ts"
      provides: "기록 없음 · 성공(URL 있어도 링크 없음) · 실패 · 겹친 실패 행 문구와 링크 · 백업 id · 폰 44×44"
      min_lines: 60
    - path: "test/e2e/mobile-people.spec.ts"
      provides: "375 · 360 · 640@2에서 세로 쌓기 · 칸 접기 · 접근성 트리 · `headers` DOM 속성 · 행 머리글 · 넘침 없음 · 터치 목표"
      min_lines: 80
    - path: "test/unit/design-system-docs.test.ts"
      provides: "§6-8 개수 없는 문구 · §7-5 예외 줄 · §7-3 P1 문장 · DECISIONS 두 항목 단언"
      contains: "복원 리허설"
  key_links:
    - from: "app/(app)/admin/people/page.tsx"
      to: "app/(app)/admin/people/person-status.ts"
      via: "personLoginStatus(person) → archived | badges[]"
      pattern: "personLoginStatus"
    - from: "app/(app)/admin/people/page.tsx"
      to: "app/(app)/admin/people/people.module.css"
      via: "목록 표 className에 styles.table과 styles.peopleTable을 함께"
      pattern: "peopleTable"
    - from: "app/(app)/admin/system-status/page.tsx"
      to: "app/(app)/admin/system-status/system-status.module.css"
      via: "「실행 기록」 링크 styles.runLink · 백업 id span styles.backupId"
      pattern: "runLink"
    - from: "test/unit/design-system-docs.test.ts"
      to: "docs/design/SYSTEM.md"
      via: "§6-8 · §7-3 · §7-5 구간 단언"
      pattern: "6-8"
---

<objective>
**As a** 관리자, **I want to** 시스템 상태 화면에서 마지막 복원 리허설 결과를, 사람 목록에서 누가 아직 첫 로그인 전인지·임시 비밀번호를 쓰는지를 폰에서도 넘침 없이 읽기를, **so that** 전환 전 확인(Phase 8 체크리스트)을 화면 하나로 끝낸다.

이 플랜은 D8-07 · D8-08의 화면 마감이다. 데이터와 판정은 앞 플랜이 끝냈다 — 「복원 리허설」 문구는 04.4-01(`formatRestoreRehearsal` · `page.tsx` 항목), 로그인 상태 판정은 04.4-04(`personLoginStatus`). Task 1은 **트레이서**다 — 상태 화면 한 행을 E2E(브라우저) → `page.tsx` → 새 CSS 모듈까지 관통한다. Task 2는 사람 목록 렌더(배지 · 행 머리글 · 폰 칸 접기 · `.peopleTable`)를 E2E 먼저 쓰고 구현한다. Task 3은 SYSTEM.md · DECISIONS.md 갱신(UI-SPEC 완료 조건 ①②③)을 문서 테스트 먼저 쓰고 고친다.

Purpose: ROADMAP 04.4 기준 2(화면 표시)와 기준 4(사람 목록 표시)의 화면 부분, 그리고 UI-SPEC의 시스템 문서 갱신 완료 조건.
Output: 새 CSS 모듈 1개, 두 화면의 렌더 변경, E2E 세 파일 보강, SYSTEM.md 네 곳 · DECISIONS.md 두 항목, 문서 테스트.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-UI-SPEC.md
@.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-CONTEXT.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development`를 **호출**한다. 각 task는 자기 behavior의 테스트를 먼저 쓰고 빨간 것을 본 뒤 구현한다(Codex #20). 실패를 쫓기 전에 `systematic-debugging`, 커밋·「완료」 전에 `verification-before-completion`을 호출한다. 서브에이전트 프롬프트에도 세 스킬 이름과 `model`을 명시한다.
- 첫 task 전에 `git rev-parse HEAD`를 `PLAN_BASE`로 적는다(SUMMARY 머리에도). 이 값을 검증 명령에 리터럴로 넣는다 — 검증 명령은 새 셸에서 돌아 `$PLAN_BASE`가 비면 `..HEAD`가 빈 diff(`HEAD..HEAD`)가 되어 「비어 있다」 게이트가 공허하게 통과한다. 변수로 쓰면 명령 앞의 `: "${PLAN_BASE:?PLAN_BASE unset}" &&` 가드를 빼지 않는다(eng review E-20).
- 체인 순서(A4): 04.4-04 뒤, 04.4-06 앞. E2E는 로컬 `erp_test`를 쓴다(global-setup이 스키마를 다시 만든다) — 다른 플랜과 동시에 돌지 않는다.
- 화면을 고치기 전에 `docs/design/SYSTEM.md` §6-8 · §7-3 · §7-5와 `docs/DESIGN.md` §4를 읽는다(CLAUDE.md 프론트엔드). 토큰은 `docs/design/tokens.css`에서만 — `--lh-table`(1.5) · `--cell-pad-x` · `--cell-pad-y` · `--row-min` · `--touch-min` · `--s-1` · `--fw-regular` · `--fw-medium` · `--underline-offset`은 이미 있다.
- Next.js 16 규칙은 `node_modules/next/dist/docs/`에서 확인한다(서버 컴포넌트 page에 훅 없음).
- 화면 검증 순서(CLAUDE.md): 이 플랜은 싼 게이트(`pnpm lint` · `pnpm typecheck` · `pnpm build`)와 자기 E2E까지다. 별도 에이전트의 `CI=true` 독립 DOM 감사와 전체 게이트 한 번은 병합된 트리에서 04.4-06 Task 2가 한다 — 전체 게이트를 두 번 돌리지 않는다.
- **건드리지 않는 것**: `domain/` · `repositories/` · `db/` · `ui/` 공용 컴포넌트, `roles-client.tsx`, 04.4-01의 문구 함수(`restore-rehearsal-view.ts`), 04.4-04의 판정 함수(`person-status.ts`). `.planning/STATE.md` 금지. 새 의존성 없음. `git push` 하지 않는다.
</execution_notes>

<probe_fallback>
SPEC이 없어 엣지 프로브가 비었다. 이 플랜이 스스로 적은 엣지 truth: **링크 없는 행 — 성공 행(URL 있음)으로 증명, NULL 실패 행은 DB가 막음**(truth 1) · **배지 0·1·2개와 보관됨**(truth 2) · **PC/폰 두 배치와 접힌 줄 중복 없음**(truth 3) · **id 유일 · 필드 부재**(truth 4) · **공유 스타일시트의 다른 화면 불변**(truth 5) · **긴 이름·확대 넘침**(truth 6).

**계획 가정**
1. 행 머리글 id는 목록 순번으로 만든다(`people-row-<순번>-name`) — UI-SPEC의 `person-${person.id}-name`은 예시다. 노출표가 `person.value`를 끈 계급은 DTO에서 `id`가 빠지므로 id 기반이면 여러 행이 같은 id를 갖는다. 순번은 렌더마다 유일하다
2. 공백 없는 영문 30자 이름이 360px에서 넘치지 않으려면 행 머리글이 단어 중간에서 줄을 바꿀 수 있어야 한다 — 행 머리글과 접힌 줄 셀에 `overflow-wrap: anywhere`를 준다(UI-SPEC이 접힌 줄에 이미 쓰는 값. 색·서체·radius가 아니므로 시스템 이탈이 아니다). 행 머리글 속성 목록(UI-SPEC)에 없는 한 줄이라 SUMMARY에 적는다
3. 두 배지 사이 구분자는 배지 밖의 일반 텍스트 span이다(보조기기는 그대로 읽는다). 새 `aria-hidden`을 두지 않는다
4. §7-5 예외 줄은 UI-SPEC ①이 말한 「Phase 4 보강」 목록 줄이 아니라 그 다음 「길이 규칙」 줄 뒤의 **새 줄**에 둔다 — 「Phase 4 보강」 줄은 Phase 4 브랜치가 고치는 줄이라 같은 줄을 고치면 병합 충돌이 나고, 두 문구는 색 의미가 아니라 길이 규칙의 예외다. 이 위치 이탈을 DECISIONS.md 항목에 적는다(체커 info 04 T3)
5. E2E의 리허설 행은 `recordRestoreRehearsal(SYSTEM_VIEWER, …)`(04.4-01 도메인)을 테스트 프로세스에서 불러 넣는다(`test/e2e/fixtures.ts`가 `createAccount`를 부르는 것과 같은 결). 모든 행의 종료 시각을 같게 두면 최신 1건은 `seq` 내림차순으로 마지막에 넣은 행이다. 행마다 서로 다른 `runKey`(`<숫자>-<숫자>` 모양, 예 `9001-1` · `9002-1`)를 준다 — 04.4-01의 UNIQUE와 모양 검증 때문이다(같은 키면 두 번째 행이 조용히 버려진다), 실패 행에는 반드시 URL을 준다
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 상태 화면 「복원 리허설」 행 — 브라우저 E2E → page.tsx → 새 CSS 모듈 한 줄 관통</name>
  <files>test/e2e/system-status.spec.ts, app/(app)/admin/system-status/page.tsx, app/(app)/admin/system-status/system-status.module.css</files>
  <read_first>
    - `app/(app)/admin/system-status/page.tsx`(04.4-01이 더한 「복원 리허설」 항목 — 백업 id span · 「실행 기록」 링크)
    - `app/(app)/admin/system-status/restore-rehearsal-view.ts`(04.4-01 — `formatRestoreRehearsal` 반환 모양)
    - `domain/ops/restore-rehearsal.ts`(04.4-01 — `recordRestoreRehearsal` 입력 모양)
    - `app/(app)/admin/people/people.module.css` 75~96행(`.detailLink`와 그 폰 미디어 쿼리 — 복제할 다섯 속성)
    - `test/e2e/system-status.spec.ts`(기존 로그인 · 404 테스트 모양), `test/e2e/fixtures.ts`
    - UI-SPEC — **구현 기준은 표다**: 「복원 리허설 값 행」 문구 표 `04.4-UI-SPEC.md` 123–132행(성공 · 실패 · 겹친 실패 · run id 없음 · 기록 없음 — 문구의 정본) · Color 85–103행 · Responsive 2 210행. 주변의 리비전 이력·근거 문단은 참고만 한다
  </read_first>
  <behavior>
    - 기록 없음: 새 스키마에서 시스템 관리자로 들어가면 `dt` 「복원 리허설」이 `dt` 「마지막 백업」 바로 다음이고 그 `dd`가 정확히 `리허설 기록 없음 — 첫 리허설 전`
    - 성공: 성공 행(스테이징 · 종료 `2026-09-23T18:14:00Z` · 12분 · 백업 `1758684000000` · URL `https://github.com/plant8/erp/actions/runs/123455` — 실제 기록처럼 URL이 있다)을 넣으면 `dd` 텍스트가 정확히 `성공 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 12분`(끝이 `·`나 `실행 기록`이 아니다), `dd` 안에 `a` 요소 0개, 백업 id span의 계산 `white-space`가 `nowrap`이고 `font-variant-numeric`에 `tabular-nums`
    - 실패(URL 있음): 검증 실패 행 + `https://github.com/plant8/erp/actions/runs/123456` → 텍스트가 `실패 · 검증 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 4분 · 실행 기록`, 링크 `href`가 그 URL, `target` 속성 없음, 계산 색이 `--accent` 값, `text-decoration-line`이 `underline`, 굵기가 `--fw-medium` 값
    - 겹친 실패(UI-SPEC 문구 표 · Codex 2차 #2): 저장된 단계 `cleanup` + 9분 + URL → `실패 · 정리 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 9분 · 실행 기록`(단계 낱말 하나)
    - 폰(같은 파일 안 `test.use` 뷰포트 375×800): 위 링크의 상자가 44×44 이상이고 화면 안, 문서 `scrollWidth <= clientWidth`
    - 모든 행에 `--danger` · `--success` 계산 색이 없다(`dd`와 자손의 `color`가 다른 `dd`와 같거나 링크의 `--accent`)
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: `test/e2e/system-status.spec.ts`에 `test.describe.serial` 블록 하나를 **더한다**(기존 두 테스트는 그대로). 첫 테스트가 기록 없음을 보고, 이어서 각 테스트가 `recordRestoreRehearsal(SYSTEM_VIEWER, …)`로 행 하나를 넣고 화면을 다시 연다 — 종료 시각은 전부 `2026-09-23T18:14:00Z`로 같게 두고 시작 시각만 바꿔 소요 시간을 만든다(가정 5). 폰 확인은 그 블록 안 중첩 `test.describe`에 `test.use({ viewport: { width: 375, height: 800 } })`로 둔다(새 파일 없이 — `mobile-*` 패턴은 폰 프로젝트 전용이다). 토큰 값은 `getComputedStyle(document.documentElement).getPropertyValue("--accent")` 등으로 읽어 비교한다. 문구 · `a` 부재 · 링크 스타일이 빨간 것을 확인한다(04.4-01이 문구를 이미 만들었으면 문구 단언은 초록이어도 되고, 스타일 · 44×44 단언이 빨개야 한다).

② **CSS 모듈**: 새 `app/(app)/admin/system-status/system-status.module.css`에 머리 주석(SYSTEM.md §6-8 · UI-SPEC Color — `.detailLink`와 같은 모양)과 두 클래스를 둔다. `.runLink` = `font-size: var(--fs-sm)` · `font-weight: var(--fw-medium)` · `color: var(--accent)` · `text-decoration: underline` · `text-underline-offset: var(--underline-offset)`, 그리고 `@media (max-width: 699.98px)`에서 `display: inline-flex` · `align-items: center` · `justify-content: center` · `min-width: var(--touch-min)` · `min-height: var(--touch-min)`(people.module.css 86~96행과 같은 결). `.backupId` = `white-space: nowrap` · `font-variant-numeric: tabular-nums`. 토큰 밖 값 없음.

③ **page.tsx**: 모듈을 `styles`로 불러 04.4-01이 만든 백업 id span에 `className={styles.backupId}`, 「실행 기록」 링크에 `className={styles.runLink}`만 붙인다. 문구 · 세그먼트 생략 · `target` 없음은 04.4-01의 것을 그대로 둔다 — E2E가 URL 있는 성공 행에서 `·`가 남거나 빈 링크가 있으면 04.4-01의 `formatRestoreRehearsal`이 아니라 이 파일의 조립을 고친다(링크 세그먼트는 runUrl이 있을 때만 ` · `와 함께 붙는다, UI-SPEC 결과의 정의).

④ 녹색이 될 때까지 구현. 커밋 제목 예 `feat: style restore rehearsal row on system status`, 본문 한국어(D8-08 · UI-SPEC Color · 링크 생략 분기).
  </action>
  <verify>
    <automated>pnpm exec playwright test test/e2e/system-status.spec.ts --project desktop</automated>
    <fails_when>기록 없음 · 성공 · 실패 · 겹친 실패 행의 문구가 UI-SPEC과 한 글자라도 다르거나, URL 있는 성공 행에 `a`나 끝 `·`가 남거나, 링크가 새 탭이거나 `.detailLink`와 다른 스타일이거나, 375에서 44×44 미만이거나, 백업 id가 줄바꿈 가능하다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>stylelint가 토큰 밖 색·서체·radius를 찾거나 타입 오류가 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/(app)/admin/system-status/system-status.module.css`에 `runLink`와 `backupId`가 있고 `var(--touch-min)`이 2번 나온다
    - `page.tsx`에 `styles.runLink`와 `styles.backupId`가 각각 한 번 있다
    - `grep -c 'target=' 'app/(app)/admin/system-status/page.tsx'`가 0이다
    - `test/e2e/system-status.spec.ts`에 URL 있는 성공 행의 링크 없음 테스트와 `cleanup` 겹친 실패 행 테스트가 있고 둘 다 초록이다
  </acceptance_criteria>
  <done>상태 화면 「복원 리허설」 행의 네 표시가 브라우저에서 UI-SPEC 문구 그대로이고, 실행 기록 링크가 기존 3차 링크와 같은 모양 · 폰 44×44이며, 링크가 없어야 하는 행(성공)에는 링크 자리가 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 사람 목록 — 배지 렌더 · 행 머리글 · 폰 칸 접기 · `.peopleTable` 범위 한정</name>
  <files>test/e2e/people.spec.ts, test/e2e/mobile-people.spec.ts, app/(app)/admin/people/page.tsx, app/(app)/admin/people/people.module.css</files>
  <read_first>
    - `app/(app)/admin/people/page.tsx` 66~111행(6열 표 · 이름 `td` · 상태 칸 `보관됨`)
    - `app/(app)/admin/people/people.module.css` 41~65행(`.table` · `.table th` · `.table td` · hover — 공유 규칙)
    - `app/(app)/admin/people/roles/roles-client.tsx` 13행 · 126~133행(같은 스타일시트를 쓰는 계급 화면 표)
    - `app/(app)/admin/people/person-status.ts`(04.4-04 — `personLoginStatus` 반환 `archived | badges`)
    - `ui/table/Table.module.css` 145~189행(`.collapsedRow` · `.collapsedCell` · `.prio-p2` · 폰 미디어 쿼리 — 같은 결로 옮긴다)
    - `app/globals.css` 60행(`.sr-only`) · `docs/design/tokens.css`(`--lh-table` · `--cell-pad-*` · `--row-min` · `--fw-regular`)
    - `test/e2e/people.spec.ts` · `test/e2e/mobile-people.spec.ts`(로그인 · 「사람 등록」 폼으로 사람 만들기 모양)
    - UI-SPEC — **구현 기준은 표다**: 배지 조합 표 `04.4-UI-SPEC.md` 157–162행 + 보관됨 우선 164행(상태 칸 렌더의 정본) · 반응형/폰 칸 접기 188–210행(열 우선순위 표 190–198 · 접근성 관계 ①~④ · P1 구성 · 검증 조건 둘 · 폰 접근성 트리 · 배지 세로 쌓기 209) · Typography 66–84행(범위 한정). 주변의 리비전 이력·근거 문단은 참고만 한다
  </read_first>
  <behavior>
    - PC(people.spec.ts, desktop): 「사람 등록」으로 막 만든 사람 행의 상태 칸이 `첫 로그인 전 · 임시 비밀번호 사용 중` 한 줄(두 배지의 top이 같다), 로그인해 들어온 관리자 자신의 행에는 `첫 로그인 전`이 없다, 보관된 사람 행은 `보관됨`만
    - PC: 접힌 줄이 보이지 않고(`toBeHidden`) 표의 `ariaSnapshot()`에 그 사람의 이메일이 한 번만 나온다 · 이름은 `rowheader` 역할이다 · 행에 마우스를 올리면 행 머리글의 계산 배경이 같은 행 `td`와 같다
    - PC `--lh-table`: 목록 표의 `thead th` · `tbody th[scope="row"]` · `td`의 계산 `line-height ÷ font-size`가 `--lh-table` 값(오차 0.01)이고, 행 머리글의 굵기 400 · 왼쪽 정렬 · `white-space`가 `nowrap` 아님 · `font-size`가 같은 행 `td`와 같다
    - 계급 화면 불변(Codex 5차): `/admin/people/roles` 표의 `thead th`와 첫 `tbody td`의 `line-height ÷ font-size`가 `--lh-body` 값(오차 0.01)이고, `thead th`는 여전히 `nowrap` · `--fw-medium` · 굵은 아래선이다
    - 폰 375(mobile-people.spec.ts): 두 배지가 세로(둘째 top ≥ 첫째 bottom − 1) · 구분자 `display: none` · 이메일 · 계급 · 현재 소속 머리글과 셀이 숨고 접힌 줄이 보이며 그 보이는 텍스트가 `<이메일> · <계급> · <소속 또는 —>`(라벨은 화면에 안 보임 — 보이는 텍스트는 `.sr-only` 라벨 span을 뺀 텍스트로 잰다. `.sr-only`는 clip 방식이라 `innerText`·`toHaveText`에 라벨이 섞이므로 라벨을 지워 맞추지 않는다, eng review E-23)
    - 폰 360(`test.use` 뷰포트 360×800): 공백 없는 영문 30자 이름으로 등록한 사람(두 배지)이 있을 때 문서 · 표 · 그 행 머리글 모두 `scrollWidth <= clientWidth` · 행 머리글 계산 `white-space` ≠ `nowrap`, 굵기 400 · 접힌 줄 셀의 `getAttribute("headers")`가 바로 위 행 `th[scope="row"]`의 `id`와 같고 그 id가 문서에서 한 번만 나온다 · 접힌 줄과 조상에 `[aria-hidden]` 없음 · 접힌 줄의 `ariaSnapshot()`에 `이메일` · `계급` · `현재 소속` 라벨이 있다 · 「상세」 링크와 보관 버튼 상자가 44×44 이상이고 화면 안
    - 640 · `deviceScaleFactor` 2(= 1280px 200% 확대): 같은 행에서 문서 · 표 `scrollWidth <= clientWidth`, 접힌 줄이 보이고 P2 셀이 숨는다
    - 방어적 렌더 NULL+false(UI-SPEC 162행, 발생 경로 없음): 테스트는 **단위 1건뿐**이고 04.4-04 `test/unit/people/person-status.test.ts` 판정 3(배지가 `첫 로그인 전` 하나 — `임시 비밀번호 사용 중`과 구분자가 빠진다)이 그것이다. 이 task는 그 조합의 E2E · 단위 테스트를 더하지 않는다
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: 위 behavior를 `test/e2e/people.spec.ts`(PC · 계급 화면 불변)와 `test/e2e/mobile-people.spec.ts`(375 · 360 · 640@2 — 뒤 둘은 파일 안 중첩 `test.describe`의 `test.use`)에 **더한다**(기존 테스트는 그대로). 사람은 기존 테스트처럼 「사람 등록」 폼으로 만든다(발급 직후라 두 배지가 다 보인다). `headers` 연결은 접근성 트리에 나오지 않으므로 DOM 속성을 직접 읽는다(UI-SPEC 5차 :203). 토큰 값은 `document.documentElement`의 계산 스타일에서 읽는다. 빨간 것을 확인한다.

② **people.module.css**(모든 값은 토큰, 색·서체·radius 새 값 없음):
- 공유 규칙 변경은 **하나뿐**: 46행의 선택자 `.table th`를 `.table thead th`로 좁힌다(본문 그대로). 두 표의 `th`는 전부 `thead` 안이라 열 머리글 렌더는 그대로다. 공유 `.table td`와 `.table tbody tr:hover td`는 한 글자도 고치지 않는다.
- 새 수식 클래스 `.peopleTable`(사람 목록 표 전용, Codex 5차) 아래에만 새 규칙을 둔다: (a) `.peopleTable thead th, .peopleTable td, .peopleTable tbody th[scope="row"]`에 `line-height: var(--lh-table)` (b) `.peopleTable tbody th[scope="row"]`에 `min-height: var(--row-min)` · `padding: var(--cell-pad-y) var(--cell-pad-x)` · `font-size: var(--fs-base)` · `font-weight: var(--fw-regular)` · `text-align: left` · `border-bottom: var(--line-w) solid var(--line)` · `overflow-wrap: anywhere`(가정 2) — `white-space`는 선언하지 않는다 (c) `.peopleTable tbody tr:hover th`에 `background: var(--surface)` (d) 접힌 줄: `.peopleTable .collapsedRow`는 `display: none`, `.peopleTable .collapsedCell`은 `padding: 0 var(--cell-pad-x) var(--cell-pad-y)` · `font-size: var(--fs-sm)` · `font-weight: var(--fw-regular)` · `color: var(--muted)` · `line-height: var(--lh-table)` · `overflow-wrap: anywhere` (e) `.badges`는 PC에서 인라인(한 줄), `.badgeSep`은 보인다.
- `@media (max-width: 699.98px)`: `.peopleTable .prioP2`는 `display: none`, `.peopleTable .collapsedRow`는 `display: table-row`, `.badges`는 `display: flex` · `flex-direction: column` · `gap: var(--s-1)`, `.badgeSep`은 `display: none`.
- 머리 주석에 SYSTEM.md §7-3 칸 접기 · UI-SPEC 범위 한정(계급 화면이 같은 파일을 쓴다)을 한 줄씩.

③ **page.tsx**(서버 컴포넌트, 훅 없음):
- 목록 표의 `className`을 `styles.table`과 `styles.peopleTable` 둘로. 이메일 · 계급 · 현재 소속 `th`와 `td`에 `styles.prioP2`.
- `people.map`의 콜백에서 순번을 받아 행 머리글 id `people-row-<순번>-name`을 만들고(가정 1), 이메일 · 계급 이름 · 소속 값을 지금 셀과 **같은 식**으로 한 번 계산해 P2 셀과 접힌 줄이 같은 값을 쓰게 한다(계급은 지금의 `roleName ?? roleNameById… ?? "—"`, 소속은 `?? "—"`, 이메일은 지금처럼 그대로).
- 이름 셀을 `th scope="row"` + 그 id로 바꾼다.
- 상태 칸: `personLoginStatus(person)`이 `archived`면 기존 `보관됨` 그대로, 배지가 0개면 `—`, 1~2개면 `styles.badges` span 안에 배지마다 `StatusTag kind="muted" variant="text"`, 둘일 때만 사이에 `styles.badgeSep` span(` · `).
- 각 행 `tr` 바로 뒤(React `Fragment`에 `key`)에 접힌 줄 `tr`(`styles.collapsedRow`) 하나: 셀 하나가 `colSpan={6}` · `headers`=행 머리글 id · `styles.collapsedCell`이고, 내용은 `.sr-only` 라벨(`이메일 ` · `계급 ` · `현재 소속 `)을 각 값 앞에 둔 `값 · 값 · 값`. 이 `tr`·`td`에 `aria-hidden`을 두지 않는다(UI-SPEC ④).

④ 녹색이 될 때까지 구현. 커밋을 둘로 나눌 수 있다: `feat: show login status badges and phone fold on people list`(본문 한국어, D8-07 · UI-SPEC Responsive · Codex 3차 #2 · 5차).
  </action>
  <verify>
    <automated>pnpm exec playwright test test/e2e/people.spec.ts test/e2e/mobile-people.spec.ts</automated>
    <fails_when>배지 배치(PC 한 줄 · 폰 세로)나 칸 접기, 행 머리글 역할 · 스타일 · hover, `headers` 속성 · id 유일, 필드 라벨, `aria-hidden` 부재, `--lh-table` 실측, 계급 화면 불변, 360 · 640@2 넘침 없음, 터치 목표 중 하나라도 어긋난다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>stylelint가 토큰 밖 값을 찾거나 타입 오류가 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - `: "${PLAN_BASE:?PLAN_BASE unset}" && git diff "$PLAN_BASE"..HEAD -- 'app/(app)/admin/people/people.module.css'`의 삭제 줄(`^-[^-]`)이 정확히 하나이고 그 줄이 `-.table th {`다(공유 `.table td` · hover 불변)
    - `grep -n 'lh-table' 'app/(app)/admin/people/people.module.css'`의 모든 줄이 `.peopleTable` 선택자 블록 안이다(2줄 이상)
    - `page.tsx`에 `personLoginStatus` · `scope="row"` · `headers=` · `colSpan` · `sr-only` · `styles.peopleTable`이 있다
    - `: "${PLAN_BASE:?PLAN_BASE unset}" && git diff --stat "$PLAN_BASE"..HEAD -- 'app/(app)/admin/people/roles' ui` 가 비어 있다
    - 두 E2E 파일이 초록이다
  </acceptance_criteria>
  <done>사람 목록에 두 로그인 배지가 PC 한 줄 · 폰 세로로 보이고, 폰에서 칸 접기와 행 머리글 · `headers` · 숨은 라벨로 보조기기도 값의 주인을 알며, 긴 이름과 200% 확대에서도 넘치지 않고, 계급 화면은 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 시스템 문서 — SYSTEM.md §6-8 · §7-5 · §7-3 · DECISIONS.md 두 항목</name>
  <files>test/unit/design-system-docs.test.ts, docs/design/SYSTEM.md, docs/design/DECISIONS.md</files>
  <read_first>
    - `docs/design/SYSTEM.md` 566~590행(§6-8 — 와이어프레임 574행 「마지막 백업」 · 본문 578행 · LOADING 583행 · EMPTY 584행) · 732~734행(§7-3 P1 줄) · 791~797행(§7-5 · 「Phase 4 보강」 · 「길이 규칙」)
    - `docs/design/DECISIONS.md` 끝 40행(항목 형식: `## 날짜 — 제목` · **결정** · **왜** · **버린 대안** · **범위**)
    - `test/unit/design-system-docs.test.ts`(`section()` 도우미 · 기존 §6-8 단언)
    - UI-SPEC 「SYSTEM.md·DECISIONS.md 갱신」 ①②③
  </read_first>
  <behavior>
    - §6-8 구간(`### 6-8` ~ `### 6-9`)에 「복원 리허설」이 있고, 와이어프레임에서 「복원 리허설」 줄이 「마지막 백업」 줄 바로 다음이다
    - §6-8 구간에 고정 개수 문구가 없다: 정규식 `/세 항목|3줄/`에 맞는 곳이 0개
    - §7-5 구간(`### 7-5` ~ 다음 `### 7-6`)에 `첫 로그인 전`과 `임시 비밀번호 사용 중`이 있다
    - §7-3 구간(`### 7-3` ~ `### 7-4`)의 P1 줄에 「금액 열이 없는 표」 문장이 있다
    - DECISIONS.md에 §7-5 길이 예외 항목(두 문구 · 「Phase 4 보강」 대신 새 줄에 둔 이유)과 §7-3 금액 열 없는 표 항목(첫 적용: 사람 목록)이 있다
  </behavior>
  <action>
① **실패 테스트 먼저(Codex #20)**: `test/unit/design-system-docs.test.ts`에 위 behavior의 `describe` 하나를 **더한다**(기존 `section()` 도우미 사용, 기존 단언 그대로). 빨간 것을 확인한다. 이 테스트는 문서와 같은 커밋에 있어야 CI 경로 필터에 걸리지 않는다(파일 머리 주석).

② **SYSTEM.md §6-8**(UI-SPEC ②, 시스템 이탈 아님):
- 와이어프레임 「마지막 백업」 줄 바로 다음에 `│ 복원 리허설  성공 · 스테이징 · 2026-09-24 03:14 · 백업 1758684000000 · 12분` 한 줄.
- 578행 본문의 개수 낱말을 지우고 「항목:」으로 시작하게 한 뒤 목록 끝에 `복원 리허설(결과 · 실패 단계 · 원본 · 종료 시각 · 백업 id · 소요 시간, 실패이고 실행 기록이 있으면 링크)`을 더한다.
- 583행 LOADING은 「항목마다 `--surface` 뼈대 한 줄」로, 584행 EMPTY는 「항상 모든 항목이 조회되고」로 — 개수를 적지 않는다(04.2가 같은 절에 항목을 더해도 고칠 숫자가 없다).

③ **SYSTEM.md §7-5**(UI-SPEC ① · 가정 4): 「길이 규칙」 줄 바로 다음에 새 줄 — `(Phase 04.4 보강)` 사람 목록 「상태」 칸의 `첫 로그인 전` · `임시 비밀번호 사용 중`은 D8-07이 잠근 업무 용어라 길이 규칙의 예외이고, 의미는 `--muted`(대기·임시), `variant="text"`로만 쓴다. 「Phase 4 보강」 줄은 고치지 않는다.

④ **SYSTEM.md §7-3**(UI-SPEC ③): 733행 P1 줄 끝에 한 문장 — 금액 열이 없는 표는 비는 금액 자리를 상태와 행동 **둘 다**에 쓸 수 있다(최대 3열 그대로, 첫 적용: 사람 목록 — 읽기 전용 표라 행 탭 → 하단 시트가 없어 행동을 접으면 폰에서 상세로 갈 길이 없다).

⑤ **DECISIONS.md** 끝에 두 항목(날짜는 실행한 날, 기존 형식):
- 「§7-5 길이 예외: 첫 로그인 전 · 임시 비밀번호 사용 중」 — **결정**(두 문구를 예외로, `--muted` text) · **왜**(D8-07이 문구를 잠갔다 — `완료(정산)`과 같은 성격) · **버린 대안**(네 글자로 줄이기 — 잠긴 용어를 바꾼다 / UI-SPEC ①대로 「Phase 4 보강」 목록 줄에 넣기 — Phase 4 브랜치가 같은 줄을 고쳐 병합 충돌, 그리고 색 의미 목록이 아니라 길이 규칙의 예외라 새 줄이 맞다: **UI-SPEC 위치에서 벗어난 것을 여기 기록한다**) · **범위**(SYSTEM.md §7-5, `/admin/people`).
- 「§7-3 금액 열 없는 표의 P1」 — **결정**(금액 열이 없는 표는 비는 P1 자리를 상태와 행동 둘 다에, 최대 3열 그대로) · **왜**(읽기 전용 표에는 행 탭 → 하단 시트가 없어 행동을 접으면 폰에서 상세로 못 간다, 상태를 접으면 배지가 접힌 줄 작은 글자에 묻힌다) · **버린 대안**(상태를 접힌 줄로 / 행동을 접힌 줄로) · **범위**(SYSTEM.md §7-3, 첫 적용 사람 목록 · DECISIONS.md 2026-09-18 폰 전략의 보강).

⑥ 녹색이 될 때까지. 커밋 제목 예 `docs: record people list P1 and login status length exception`, 본문 한국어(D8-07 · D8-08 · UI-SPEC ①②③ · 체커 info 04 T3).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/stylelint-config.test.ts</automated>
    <fails_when>§6-8에 개수 문구가 남았거나 「복원 리허설」 줄이 「마지막 백업」 다음이 아니거나, §7-5 · §7-3 문장이나 DECISIONS 두 항목이 없거나, 기존 문서 단언이 깨진다</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>lint가 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `awk '/^### 6-8/,/^### 6-9/' docs/design/SYSTEM.md | grep -cE '세 항목|3줄'`이 0이다
    - `awk '/^### 6-8/,/^### 6-9/' docs/design/SYSTEM.md | grep -c '복원 리허설'`이 2 이상이다(와이어프레임 · 항목 목록)
    - `: "${PLAN_BASE:?PLAN_BASE unset}" && git diff "$PLAN_BASE"..HEAD -- docs/design/SYSTEM.md`의 삭제 줄이 578 · 583 · 584 · 733행의 옛 문장뿐이다(「Phase 4 보강」 줄 불변)
    - DECISIONS.md에 `§7-5`와 `첫 로그인 전`을 담은 항목, `§7-3`과 `금액 열`을 담은 항목이 하나씩 더해졌다
  </acceptance_criteria>
  <done>SYSTEM.md가 이 페이즈의 두 화면과 같고(§6-8 개수 없음 · 와이어프레임 · §7-5 예외 · §7-3 P1), DECISIONS.md가 두 이탈의 이유와 UI-SPEC 위치 이탈을 남긴다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB 행 → 상태 화면 `href` | 저장된 `run_url`이 링크가 된다(검증은 04.4-01이 쓸 때와 읽을 때 한다) |
| 투영된 DTO → 사람 목록 접힌 줄 | 접힌 줄은 같은 행 P2 셀과 같은 DTO 값을 한 번 더 그린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-31 | Information Disclosure | 접힌 줄 · 숨은 라벨 | low | mitigate | 접힌 줄 값은 같은 행 P2 셀과 같은 식(투영된 DTO)으로 한 번 계산해 둘 다에 쓴다 — 새 조회 · 새 필드 없음. 노출표가 끈 필드는 DTO에 없어 접힌 줄에도 없다(Task 2 ③) |
| T-04.4-32 | Tampering | 「실행 기록」 `href` | medium | transfer | URL 모양 검증은 04.4-01(T-04.4-01)이 쓰기·읽기 모두에서 한다. 이 플랜은 `className`만 붙이고 `target`을 두지 않는다(Task 1 수용 조건 `target=` 0개) |
| T-04.4-33 | Tampering | 공유 스타일시트 `people.module.css` | low | mitigate | 공유 선택자 변경은 `.table th` → `.table thead th` 하나, 새 규칙은 `.peopleTable` 아래에만 — 삭제 줄 하나 단언 + 계급 화면 계산 스타일 불변 E2E(Task 2) |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | 새 패키지 0개. `: "${PLAN_BASE:?PLAN_BASE unset}" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml` 비어 있음 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과(싼 게이트)
- `pnpm exec playwright test test/e2e/system-status.spec.ts test/e2e/people.spec.ts test/e2e/mobile-people.spec.ts` 초록
- `pnpm vitest run --project unit test/unit/design-system-docs.test.ts` 초록
- `: "${PLAN_BASE:?PLAN_BASE unset}" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml domain repositories db ui 'app/(app)/admin/people/roles' .planning/STATE.md` 비어 있음
- 독립 DOM 감사(`CI=true`)와 전체 게이트 한 번은 04.4-06 Task 2(병합된 트리)
</verification>

<success_criteria>
- 상태 화면 「복원 리허설」 행이 UI-SPEC 문구 · 링크 모양 · 폰 터치 목표 그대로이고 URL 없는 실패 행에 링크 자리가 없다
- 사람 목록에 두 로그인 배지가 PC · 폰 규칙대로 보이고 폰 칸 접기 · 행 머리글 · `headers` · 숨은 라벨이 있다
- 360px과 1280px 200% 확대에서 가로 넘침이 없고, 계급 화면 표는 그대로다
- SYSTEM.md · DECISIONS.md가 화면과 같다(§6-8 · §7-5 · §7-3, 두 결정)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| CSS | `app/(app)/admin/system-status/system-status.module.css`(새) · `app/(app)/admin/people/people.module.css`(`.table thead th` · `.peopleTable` 규칙 · 칸 접기 · 배지 쌓기) |
| 화면 | `/admin/system-status` 「복원 리허설」 행 모양 · `/admin/people` 배지 · 행 머리글 · 폰 접힌 줄 |
| 문서 | `docs/design/SYSTEM.md` §6-8 · §7-5 · §7-3 · `docs/design/DECISIONS.md` 두 항목 |
| 테스트 | `test/e2e/system-status.spec.ts` · `test/e2e/people.spec.ts` · `test/e2e/mobile-people.spec.ts` · `test/unit/design-system-docs.test.ts` |

## Source Coverage (this plan)

| Source | Item | Status |
|---|---|---|
| CONTEXT | D8-07 사람 목록 「첫 로그인 전」 · 「임시 비밀번호 사용 중」 표시 | COVERED(Task 2 렌더 · Task 3 §7-5; 데이터·판정은 04.4-04) |
| CONTEXT | D8-08 상태 화면 「복원 리허설」 항목 | COVERED(Task 1 모양 · Task 3 §6-8; 기록·문구는 04.4-01) |
| UI-SPEC | Color · Typography(`--lh-table` 네 곳, 범위 한정) · Spacing(`--cell-pad-*` · `--s-1`) · Responsive(칸 접기 · 접근성 관계 · 검증 조건 둘) · 갱신 ①②③ | COVERED(Task 1 · 2 · 3) |
| GOAL | ROADMAP 04.4 기준 2(화면 표시) · 기준 4(사람 목록 표시) | COVERED(화면 부분) |

## Review Dispositions Ledger

### Round 1 — uncommitted (Codex GPT 적대적 계획 리뷰, 세션 scratchpad `codex-plan-final.md`, 검토 대상 계획 커밋 `7a1eca3`)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 scope_sanity(01 19개 · 04 17개 파일) | warning | 화면 렌더 · E2E · SYSTEM.md · DECISIONS.md를 이 새 플랜 04.4-05(10개 파일)로 떼어 01 · 04를 15개 이하로. 옛 병합 플랜은 04.4-06으로 번호를 옮겨 모든 `depends_on`이 풀린다(UI 체커 재검 3) |
| 체커 info(04 T3 §7-5 위치) | info | Task 3 ③ · ⑤: §7-5 예외를 「Phase 4 보강」 줄이 아닌 「길이 규칙」 다음 새 줄에 두고, UI-SPEC ①에서 벗어난 위치와 이유를 DECISIONS.md 항목에 기록 |
| UI-SPEC 확정본 3(겹친 실패 행) · 5차 1(run id NULL 실패 행) | medium | Task 1: 저장된 `cleanup` 행과 URL 없는 실패 행을 브라우저에서 글자 그대로 단언, URL 없는 행에 `a` 0개 · 끝 `·` 없음 | — **2차에서 바꿈**: NULL 실패 행은 DB CHECK로 넣을 수 없어 E2E에서 빼고 URL 있는 성공 행으로 링크 생략을 증명(아래 Round 2)
| UI-SPEC 확정본 4 · UI 체커 재검 5 · UI-SPEC 5차 2(행 머리글 · 칸 접기) | high | Task 2: 이름 `th scope="row"` + 유일 id, 접힌 줄 `headers` + `.sr-only` 라벨 + `aria-hidden` 없음, PC에서 접힌 줄 숨김. 행 머리글은 셀과 같은 값(`--row-min` · `--cell-pad-y`/`--cell-pad-x` · `--fs-base` · 400 · 왼쪽 · 얇은 선) + hover. 360px 30자 이름 넘침 · 계산 `white-space` 확인 |
| UI-SPEC 확정본 6 · UI 체커 재검 4(`--lh-table`) | medium | Task 2 ②(a)·(d): 열 머리글 · 행 머리글 · 셀 · 접힌 줄 셀 네 곳에 `line-height: var(--lh-table)` 명시, E2E가 계산 비율로 확인 |
| Codex 5차 경미(공유 `.table` — 계급 화면) | low | Task 2 ②: 새 규칙은 `.peopleTable` 아래에만, 공유 변경은 `.table th` → `.table thead th` 하나(삭제 줄 하나 단언), 계급 화면 계산 스타일 불변 E2E |
| UI 체커 재검 6 · UI-SPEC 5차 3(`headers`) | medium | Task 2 behavior: 접근성 트리가 아니라 DOM `getAttribute("headers")` = 행 머리글 id, id 유일 |
| UI-SPEC 확정본 5 · UI 체커 재검 7 · UI-SPEC 5차 4(§6-8 · §7-3) | medium | Task 3: §6-8 본문 578 · LOADING 583 · EMPTY 584 세 곳의 개수 문구 제거 + 와이어프레임 줄, §7-3 P1 문장 + DECISIONS 항목, 문서 테스트가 개수 문구 0개를 단언 |
| #20 테스트가 구현 뒤에 온다 | low | Plan 05: 세 task 모두 ①이 실패 테스트 먼저 |

### Round 2 — uncommitted (UI 체커 7차 FLAG · Codex 2차 #2 · Codex 2차 §7)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| UI 체커 7차 FLAG — 실패 행은 항상 `run_url`(DB CHECK `succeeded OR run_url IS NOT NULL`, 04.4-01) | flag | Plan 05, Task 1: NULL 실패 행은 DB가 막아 E2E로 넣을 수 없으므로 그 케이스를 빼고, 링크 생략 분기는 URL 있는 성공 행(`a` 0개 · 끝 `·` 없음)으로 브라우저에서 증명. 방어적 렌더(NULL → 세그먼트 생략)는 04.4-01 순수 함수 단위 테스트가 유지 |
| Codex 2차 #2 — 실행 키 UNIQUE(04.4-01) | major | Plan 05, 가정 5: E2E가 넣는 행마다 서로 다른 `runKey`, 실패 행에는 URL |
| Codex 2차 §7 — 빈 문구 `리허설 기록 없음 — 첫 리허설 전`을 줄이자 | minor | 반려(04.4-01 가정 4의 처분): 형제 행 `백업 없음 — 첫 자동 백업 전`과 같은 조어, UI-SPEC 7차가 §7 점검에서 유지. 이 플랜의 기록 없음 단언은 그대로 |

### Eng review — 9b8694e (`/plan-eng-review` 2026-09-25, `docs/designs/plant8-erp-phase04.4-eng-review-260925.md` L13–42 · L195–240@9b8694e — BLOCKING 넷은 Fable 실측 최소 수정 원문, 참고 항목은 플래너 판단)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| E-21 `PLAN_BASE` 미설정 때 ui · roles 불변 단언이 공허 통과(E-20과 같은 원인) | P2 | Plan 05: execution_notes 리터럴 규칙 + `$PLAN_BASE` 명령 다섯(Task 1·2 acceptance · T-04.4-SC · verification) 가드 |
| E-23 `.sr-only`가 clip 방식이라 innerText 단언에 라벨이 섞여 라벨을 지우려는 위험 | P3 | Plan 05, Task 1 behavior(폰 375): 보이는 텍스트는 라벨 span을 뺀 텍스트로 잰다, 라벨을 지워 맞추지 않는다 |
### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| E-22 `overflow-wrap:anywhere` 중복 선언 | 리뷰 판정 무해 — 고칠 이유 없음 |
| E-24 `person.value` 꺼짐 때 접힌 줄이 구분자만 남는 퇴화 행 | 리뷰 5/10 · 오탐 가능 · 극단 케이스이고 고치는 모양이 UI-SPEC 결정을 건드린다 — `/design-review`에서 실제로 나오면 고친다 |
| E-26 Task 1·2 verify에 a11y · single-column · detail-link · mobile 스펙 추가 | 리뷰 원문에 구체 단언이 없고, Task 1 behavior와 04.4-06 독립 DOM 감사가 접근성 트리 · 칸 접기 · 상세 링크 44×44 · 375/360/640@2를 이미 잰다 — 더할 단언이 정해지면 반영 |
| E-27 관리자 자신 행(임시 비밀번호 1개)에 상태 칸 + 구분자 0개 단언 | E2E 관리자 계정의 `password_is_temporary` 값을 확인하지 않았다 — 틀린 픽스처 전제로 단언하면 거짓 빨강. 04.4-06 DOM 감사 (b)의 「배지 0개 행은 `—`」 · 배지 조합 실측이 그 행도 잰다 |
| E-29 700px에서 배지 위치 · 표 넘침 검사 | UI-SPEC가 정한 폭(1280 · 640@2 · 375 · 360) 밖의 새 요구 — 범위를 넓히지 않는다 |

<output>
Create `.planning/phases/04.4-restore-rehearsal-and-login-status/04.4-05-SUMMARY.md` when done
</output>
