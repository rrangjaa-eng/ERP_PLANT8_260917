---
context: phase
phase: 04.5-custom-field-admin
task: planning
total_tasks: 9 plans
status: paused
last_updated: 2026-09-24T17:18:53.959Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: STATE.md 현재 페이즈는 4로 둔다 — 04.x 스레드는 바꾸지 않는다 — STATE 기록 단계(13b)는 건너뛰고 보고한다.
- [ ] CONSTRAINT: `.planning/HANDOFF.json`은 Phase 4 것이다 — 이 인계는 이 파일에만 쓴다 — 덮으면 Phase 4 인계가 main 머지 때 사라진다.
- [ ] CONSTRAINT: 실행은 Phase 4 PR #40 머지 뒤 — 플랜 01의 「실행 순서 — Phase 4 선행」 명령이 0으로 끝나야 시작한다.
- [ ] CONSTRAINT: Codex는 매 게이트에서 실제로 돌았는지 확인한다(대신하지 않는다). 막는 지적은 파일·줄 근거 필수, 1차 대조는 Sonnet, 판단은 Opus.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Codex 통과 과장 | UI 2회차에 Codex가 보류했는데 커밋 메시지가 통과처럼 적혔다 | blocking | 판정 줄(`판정: 막는 문제 없음`)을 원문에서 grep한 뒤에만 통과라고 쓴다 |
| 백그라운드 Codex에 `&` | 안쪽 `&`를 붙이면 끝남 알림이 안 온다 | advisory | `(timeout 1200 codex exec … > OUT 2>&1; echo "exit=$?" >> OUT) </dev/null`를 run_in_background로 |
| 한 번에 쓰는 플래너 | 수정 플래너가 15분 동안 아무것도 안 썼다 | advisory | 결정마다 바로 Edit, 파일별로 나눠 띄운다 |
| `flock … CI=true pnpm` | 실행이 깨진다 | advisory | `env CI=true …` 또는 `sh -c '…'` |

<current_state>
플랜 9개(fc4fe6b) Codex 전체 최종 검토 4회로 수렴 — final-4 「판정: 막는 문제 없음」(docs/reviews/phase-04.5/codex-plan-review-final-4.md). 판단은 Fable(codex-final*-fable-judge.md).
/plan-eng-review 끝(a2585e1): 막는 문제 없음, 수용 2건(T1·T2)은 아직 플랜에 반영 안 됨. 보고서 docs/designs/plant8-erp-phase04.5-eng-review-260925.md, 게이트 기록 .claude/gates/phase-04.5.log(plan-eng-review).
</current_state>

<completed_work>
- UI-SPEC rev 3 승인(UI 검사기 VERIFIED + Codex UI 4회차 막는 문제 없음), DeleteToArchive 예외 기록(사용자 확정)
- 플랜 작성 → 검사기·Codex 1회차 → 수정(01 분리, 08 신설) → 08을 08·09로 분리 → 검사기 2회차 + Codex 2회차 → D1~D3 수정 → Codex 3회차 통과
- origin/main 머지(a65c383 #68: 소수점 페이즈 plan-ceo-review 게이트 면제 포함)
</completed_work>

<remaining_work>
1. /gsd-plan-phase 04.5 수정 모드(계획 파일 수정은 gsd-planner로): T1 — 04.5-03에 createRole 권한 행 삽입 실패 경로 테스트. T2 — 04.5-05 위협 모델에 「tx 밖 정의 읽기 TOCTOU = 허용 위험(저장 거부로 끝남)」. → 체커 + Codex(바뀐 부분만, 판정 줄 grep)
2. /plan-design-review(Codex 동시, CLAUDE.md §7 화면 사용성 원칙 점검) — OPEN 1-A·1-B·2-A·2-B·편차-04a/04b/06a·5-A·5-B 정리 → 세션 경계 인계
3. /review, PR #61 설명 갱신
4. 최종 보고: Codex 실행 목록(UI 1~4, 플랜 1~3, 최종 1~4, eng 외부 검토), 편차(STATE 기록 생략, CEO 검토 면제)
5. Phase 10 TODO(Codex 외부 #2): 4경로 동적 항목 통합 재검토 — 코디네이터에 전달
</remaining_work>

<decisions_made>
- advisory lock 관용구 `tx.select().from(sql`pg_advisory_xact_lock(...)`)` 는 로컬 probe로 잠금 획득 확인 → 유지
- 권한 행 필드별 삽입 유지(필드 수 적음), isTargetEntity 헬퍼 기각(단순함)
- Codex 외부 #1·3·4·5 기각, #6 사업 가치는 사용자 판단
- 플랜 08 → 08·09 분리: 09가 08의 `listFieldDefinitionsForAdmin`·`FIELD_DEFINITION_ADMIN_DTO_FIELDS`를 쓰므로 W3
- Phase 4 선행: 플랜 01이 먼저 origin/main 머지 후 Phase 4 플랜 18개 SUMMARY를 HEAD에서 확인, 이후 플랜은 HEAD만 확인(중간 머지 없음 — 마이그레이션 번호 재생성은 07 의식에서만)
- 새 메뉴 권한: UI-SPEC 화면 1대로 insertPermissionIfAbsent, 시스템 관리자 upsert 루프에서 그 키만 건너뜀(09)
- @testing-library/react 미승인 → 소스 검사 + E2E(편차 04b·06)
- 대상은 거래처만
</decisions_made>

<blockers>
- 실행: Phase 4 PR #40 머지 전에는 불가(설계상)
</blockers>

## Required Reading (in order)
1. docs/designs/plant8-erp-phase04.5-eng-review-260925.md — eng 검토 결과와 T1·T2
2. docs/reviews/phase-04.5/codex-plan-review-final-4.md — 마지막 Codex 판정
3. .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md — 승인된 화면 계약

## Infrastructure State
- 브랜치 claude/plan-phase-04-5-7jtm0f, PR #61 초안, CI는 문서 변경이라 돌 것 없음

<context>
CLAUDE.md §7 화면 사용성 원칙(문구·결정 최소)을 설계 검토에서 점검. 코디네이터는 일의 효과 순서로 스레드를 깨운다 — 한도가 빠듯하면 안전한 지점에서 멈춘다.
</context>

<next_action>
Start with: /gsd-plan-phase 04.5 수정 모드로 T1·T2 반영(보고서 「구현 과제」 절) → 체커 + Codex 바뀐 부분 → 통과 시 /plan-design-review
</next_action>
