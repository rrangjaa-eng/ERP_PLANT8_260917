---
context: phase
phase: 04.5-custom-field-admin
task: planning
total_tasks: 9 plans
status: paused
last_updated: 2026-09-24T17:54:37.764Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: STATE.md 현재 페이즈는 4로 둔다 — 04.x 스레드는 바꾸지 않는다 — STATE 기록 단계(13b)는 건너뛰고 보고한다.
- [ ] CONSTRAINT: `.planning/HANDOFF.json`은 Phase 4 것이다 — 이 인계는 이 파일에만 쓴다.
- [ ] CONSTRAINT: 실행은 Phase 4 PR #40 머지 뒤 — 플랜 01의 「실행 순서 — Phase 4 선행」 명령이 0으로 끝나야 시작한다.
- [ ] CONSTRAINT: Codex는 매 게이트에서 실제로 돌았는지 확인한다. 막는 지적은 파일·줄 근거 필수. 판정 줄(`판정: 막는 문제 없음`)을 원문에서 grep한 뒤에만 통과라고 쓴다.
- [ ] CONSTRAINT: 이 세션에서 게이트 리뷰를 시작하면 gsd-planner를 못 띄운다(세션 경계 훅) — 지적 반영(플래너)을 먼저, 다음 게이트는 새 세션.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 검토 전제 그대로 수용 | eng 검토 C-1 「저장 거부로만 끝남」 전제가 틀렸는데 문서화만 하려 했다 | blocking | 위협 행을 적기 전 플랜 본문에서 그 보장을 하는 줄을 찾아 인용, 없으면 REVISION_CONFLICT |
| 백그라운드 Codex에 `&` | 안쪽 `&`를 붙이면 끝남 알림이 안 온다 | advisory | `(timeout 1200 codex exec … > OUT 2>&1; echo "exit=$?" >> OUT) </dev/null`를 run_in_background로 |
| 작업자 멈춤 | 작업자가 1시간 멈춘 일(04-28) | advisory | 띄운 뒤 몇 분 안에 출력 파일이 생기고 느는지 확인, 없으면 끊고 다시 |

<current_state>
T1·T2 반영 끝(d4bd8ba): 03 createRole 권한 행 부여 1회 재시도 + warn 로그 + 두 경로 테스트, 05 저장이 거래처 행 잠금 뒤 같은 tx에서 정의·노출을 한 SELECT(readVendorFieldAccess)로 읽음(사용자 카드 「저장 때 다시 확인」). 검사기 3회 통과, Codex 델타 3회차 「판정: 막는 문제 없음」(docs/reviews/phase-04.5/codex-plan-review-t1t2-3.md).
/plan-design-review 끝(dd174e3): 보고서 docs/designs/plant8-erp-phase04.5-design-review-260925.md, Codex docs/reviews/phase-04.5/codex-design-review-1.md(막는 문제 3건 → 기각 0건, D1·D3·D6로 반영 예정), 게이트 기록 .claude/gates/phase-04.5.log(plan-design-review).
</current_state>

<completed_work>
- eng 검토 T1·T2 플랜 반영 + 검사기·Codex 델타 3회(d4bd8ba)
- /plan-design-review: Opus 1차 + Codex + Fable 대조, OPEN 1-A·1-B·2-A·2-B·5-A·5-B·편차-04a/04b/06a 처리표, 구현 과제 D1~D9(dd174e3)
</completed_work>

<remaining_work>
1. 사용자 결정 U1·U2 확인(스레드 카드). 답이 없으면 권장 A: U1 「{동사}할 수 없음 — {원인} · 」+3차 「새로 불러오기」 + 1차 비활성 / U2 칸 이름 구분자 「, 」 + SYSTEM.md 992행 예시 정정·DECISIONS 기록.
2. /gsd-plan-phase 04.5 수정 모드로 디자인 보고서 「구현 과제」 D1~D9 반영(gsd-planner) → 검사기 + Codex(바뀐 부분, 판정 줄 grep). 3회차에도 갈리면 코디네이터 거쳐 사용자 카드.
3. 최종 전체 Codex(승인 직전 1회) → /review(문서) → PR #61 설명 갱신 → 머지 준비 보고.
4. 보고에 한 줄: Codex 「사업 가치 미입증」은 사용자 판단. Phase 10 TODO(4경로 동적 항목 통합 재검토) 코디네이터 전달.
5. Codex 비차단 MINOR(t1t2-3): 한 SELECT 구조 게이트가 await 수만 봐서 Promise.all을 못 잡음 — D 반영 때 SQL 계측 단언으로 보강 검토.
</remaining_work>

<decisions_made>
- T2: 문서 수용 대신 완화 — 행 잠금 뒤 같은 tx·한 SELECT로 정의·노출 조회(공유 advisory 잠금 불필요, REPEATABLE READ 안 씀). domain은 `tx?: DbOrTx`만, `= db` 기본값은 repository에만.
- T1: 롤백 대신 1회 재시도 + warn 로그(계급 삽입이 tx 밖이라 롤백 불가, 플랜 03 설계 유지).
- 디자인: 편차-04a는 기존 임시 계급 픽스처 + 보관함 「복원」 클릭 E2E로 보완(RTL 불필요), 5-A·5-B는 현 문구 유지.
</decisions_made>

<blockers>
- 실행: Phase 4 PR #40 머지 전에는 불가(설계상)
</blockers>

## Required Reading (in order)
1. docs/designs/plant8-erp-phase04.5-design-review-260925.md — 「구현 과제」 D1~D9, 「Codex 대조」, U1·U2
2. docs/reviews/phase-04.5/codex-design-review-1.md 끝 60줄 — Codex 원 지적
3. .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md — 승인된 화면 계약

## Infrastructure State
- 브랜치 claude/plan-phase-04-5-7jtm0f, PR #61 초안(문서 변경만)

<next_action>
Start with: 스레드의 U1·U2 카드 답 확인(없으면 권장 A) → /gsd-plan-phase 04.5 수정 모드로 D1~D9 반영
</next_action>
