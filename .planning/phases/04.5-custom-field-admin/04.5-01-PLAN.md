---
phase: 04.5-custom-field-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/field-definitions.ts
  - db/migrations/NNNN_custom_field_admin.sql
  - db/migrations/meta/NNNN_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/field-definitions.ts
  - repositories/permissions.ts
  - domain/custom-fields/admin-input.ts
  - domain/custom-fields/admin.ts
  - domain/vendors/index.ts
  - app/(app)/admin/field-definitions/page.tsx
  - app/(app)/admin/field-definitions/field-definition-form.tsx
  - app/(app)/admin/field-definitions/actions.ts
  - app/(app)/admin/vendors/vendor-form.tsx
  - test/integration/field-definitions-admin.test.ts
  - test/e2e/field-definitions.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`admin.field-definitions` view·write 권한 행이 있는 시스템 관리자가 `/admin/field-definitions?new=1`에서 이름·타입(텍스트/숫자/날짜)·필수·정렬 순서를 적어 「화면 항목 추가」를 누르면 거래처 폼(`/admin/vendors?new=1`)에 그 칸이 **한글 이름**으로 보인다 — 키(`cf_` + 16진 8자)는 서버가 만들고 화면 어디에도 보이지 않는다(ROADMAP 04.5 기준 1, UI-SPEC 화면 2 등록, A1). 메뉴 등록(`MENUS` → 시드가 시스템 관리자에게 권한 행을 줌)과 관리 인덱스 링크는 08이 한 묶음으로 한다 — 그 전까지 이 플랜의 통합·E2E 테스트는 권한 행을 테스트 준비 단계에서 명시로 넣는다"
    - "칸 정의 행과 그 칸의 전 계급(보관된 계급 포함) 정보 노출표 행(`cf.vendor.<key>`, 보임)이 **한 트랜잭션**에서 생긴다 — 노출 행 몇 개를 쓴 뒤 다음 노출 행 쓰기가 실패하면 칸 정의도, 이미 쓴 노출 행도 남지 않는다(`insertVisibilityIfAbsent`의 선택 `tx`가 실제 쿼리에 쓰인다는 증명). 행은 없을 때만 쓴다 — `visible()`의 「행이 없으면 숨김」 규칙은 그대로다(D10-13, ROADMAP 기준 3, UI-SPEC E5 auto-register, 공유 결정 5)"
    - "칸 생성과 계급 생성(03의 `grantCustomFieldsToRole`)이 동시에 겹쳐도 (계급, 칸) 쌍마다 노출 행이 생긴다 — 칸 생성 트랜잭션은 **첫 문장**으로 트랜잭션 잠금 `lockCustomFieldGrants(tx)`(`repositories/field-definitions.ts`)를 잡고 그 **뒤에** 계급 목록을 읽는다. 03의 계급 쪽 경로가 같은 잠금 뒤에 칸 정의를 읽으므로 둘은 직렬화되고, 어느 쪽도 상대가 막 커밋한 행을 놓치지 않는다(D10-13 「행 없음 = 숨김」이라 놓치면 영구히 숨음 · T-04.5-07 · 경합 회귀 통합 테스트는 03 Task 1)"
    - "마이그레이션 하나가 `label`(NOT NULL — 기존 행은 `key`로 채움) · `archived_at` · `archived_by` · `archived_options`(JSONB `string[]`, 기본 `[]`) · `version`(integer NOT NULL 기본 1) · unique `(entity, label)`(label 채움 뒤)을 더하고, 이미 있는 거래처 칸 정의마다 `roles` 전 행의 노출표 행(`cf.vendor.<key>`, 보임)을 `ON CONFLICT DO NOTHING`으로 채운다 — 다시 돌려도 행이 늘지 않고, 이미 꺼 둔 행은 꺼진 채다. 번호는 `pnpm db:generate`가 정하고 계획은 번호를 적지 않는다(UI-SPEC O11·O20·O22, D10-13, 그룹 B 요구 · Codex #2)"
    - "생성 입력은 대상(entity)을 받지 않는다 — 서버가 거래처(`vendor`)를 넣고, `entity`를 실어 보내면 `.strict()` 입력 스키마가 거부한다. 타입은 등록 때만 받고 이 플랜이 받는 타입은 텍스트·숫자·날짜다(선택은 02) (ROADMAP 기준 1, UI-SPEC O7)"
    - "`admin.field-definitions` view 권한이 없는 계급(기획 PM)은 `/admin/field-definitions`와 `?new=1`에서 404를 받고, write 권한이 없으면 생성 domain 함수가 「권한 없음」 `ForbiddenError`로 끝나며 칸 정의·노출 행·로그가 늘지 않는다(ROADMAP 기준 1, UI-SPEC 화면 1 권한)"
    - "칸 생성은 커밋 뒤 행동 로그 `document_create`(entity `field_definitions`, detail에 키·대상) 한 줄을 남긴다(ROADMAP 기준 1)"
    - "`listFieldDefinitions(viewer, entity)`의 호출 모양·정렬(`sortOrder`, `key`)·행 결과는 Phase 4 호출부(`domain/projects`·`domain/quotes`)에 대해 그대로다 — 새 열이 붙을 뿐이고, `insertFieldDefinition`에서 `label`을 빼면 `key`로 채워 `test/integration/quote-lines.test.ts`·`custom-fields.test.ts` 같은 기존 호출이 그대로 돈다(CONTEXT 「분리로 생긴 조정」, UI-SPEC R7)"
    - "새 키가 기존 키와 부딪히면(`field_definitions_entity_key_key`) 새 키로 트랜잭션 전체를 다시 연다(최대 3번) — 이름 unique 위반은 재시도하지 않는다(UI-SPEC 화면 2 「충돌하면 서버가 다시 생성한다」, A1)"
    - "거래처 폼의 커스텀 칸 라벨은 `def.label`이다 — 로컬 `VendorFieldDefinition` 타입에 `label: string`이 있고, 보관된 칸 정의는 거래처 폼 입력에서 빠진다(D10-12, UI-SPEC 화면 3 변경 2, Codex #9)"
  prohibitions:
    - statement: "관리 화면이 프로젝트·견적 줄 칸을 만들 수 있어서는 안 된다. Phase 4 폼은 커스텀 칸을 보내지 않으므로 필수 칸 하나로 프로젝트·견적 줄 저장이 전부 막힌다 — 이 플랜에서 대상은 서버 쪽 상수 하나(거래처)이고 요청이 대상을 고를 수 없다(08이 그 상수를 `FIELD_DEFINITION_TARGETS = [\"vendor\"]` 등록부로 옮긴다)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/custom-fields/admin-input.ts"
      provides: "칸 정의 생성 액션의 입력 스키마(`.strict()`, 대상 없음) — DB를 import하지 않는다(08이 칸 오류 문구·이름 충돌 문구를 이 파일에 더한다)"
      exports: ["createFieldDefinitionInput"]
    - path: "domain/custom-fields/admin.ts"
      provides: "관리 화면 domain — 생성(권한 게이트·키 생성·키 충돌 재시도·한 트랜잭션·전 계급 노출 행·행동 로그)"
      exports: ["createFieldDefinition"]
    - path: "repositories/permissions.ts"
      provides: "노출표 행 없을 때만 쓰기(04-20과 같은 이름·모양 + 선택 `tx`, 쿼리가 그 `tx`를 쓴다)"
      contains: "insertVisibilityIfAbsent"
    - path: "db/migrations/NNNN_custom_field_admin.sql"
      provides: "열 다섯 · label 채움 · unique `(entity, label)` · 기존 거래처 칸의 전 계급 노출 행 채움(03 Task 2의 선행 조건 grep 대상)"
      contains: "visibility_matrix"
    - path: "app/(app)/admin/field-definitions/page.tsx"
      provides: "화면 항목 관리 화면 — 이 플랜은 인증 가드·권한 게이트·제목·`?new=1` 등록 폼(목록 표·결과 줄·칸 오류는 08)"
      contains: "admin.field-definitions"
    - path: "test/integration/field-definitions-admin.test.ts"
      provides: "한 트랜잭션(중간 실패 롤백) · 전 계급 노출 행 · 로그 · 권한 거부 · 키 충돌 재시도 · label 기본값 · 기존 칸 노출 행 채움(전 계급 · 멱등 · 꺼진 행 유지) · 마이그레이션 문장 순서"
      min_lines: 120
    - path: "test/e2e/field-definitions.spec.ts"
      provides: "추가 → 거래처 폼 한글 라벨 트레이서 + 기획 PM 404"
      min_lines: 30
  key_links:
    - from: "domain/custom-fields/admin.ts"
      to: "repositories/permissions.ts"
      via: "`withTransaction` 안에서 첫 문장 `lockCustomFieldGrants(tx)` → 계급 목록 → `insertFieldDefinition(…, tx)` 뒤 계급마다 `insertVisibilityIfAbsent(…, tx)`"
      pattern: "withTransaction"
    - from: "app/(app)/admin/vendors/vendor-form.tsx"
      to: "domain/vendors/index.ts"
      via: "`listVendorFieldDefinitions`가 `label`을 싣고 보관 칸을 뺀다 — 폼 라벨이 `def.label`"
      pattern: "def\\.label"
    - from: "db/migrations/NNNN_custom_field_admin.sql"
      to: "visibility_matrix"
      via: "`INSERT INTO \"visibility_matrix\" … SELECT … FROM \"field_definitions\" … CROSS JOIN \"roles\" … ON CONFLICT (\"role_id\", \"info_item\") DO NOTHING`"
      pattern: "ON CONFLICT"
---

<objective>
관리자가 거래처 화면 항목(텍스트·숫자·날짜)을 관리 화면에서 추가하면 거래처 폼에 한글 이름으로 보이고, 같은 트랜잭션에서 전 계급의 정보 노출표 행이 생긴다 — 이 페이즈의 끝에서 끝 트레이서(저장 구조 → 리포지토리 → domain(한 트랜잭션 + 노출 행) → 생성 액션 → 최소 관리 화면·폼 → 거래처 폼 라벨)다. 이미 있는 거래처 칸 정의의 노출 행 채움(그룹 B 요구)도 이 마이그레이션에 넣는다.

MVP 모드 메모: ROADMAP Phase 04.5의 `**Goal:**`은 사용자 스토리 형식(**As a / I want to / so that**)이 아니다 — 스토리를 지어내지 않는다. 스토리 틀이 필요하면 `/gsd mvp-phase 04.5`로 Goal을 먼저 다시 쓴다.

Purpose: 08(플랜 01 완성 — 목록·칸 오류·이름 예약·결과 줄)·09(메뉴 등록·관리 인덱스·누수 스캔)와 그 뒤 플랜 전부(선택형·수정 02, 노출 판정 03, 보관 04, 저장 보존 05, 거래처 폼 06, 여정·감사 07)가 이 플랜의 열·생성 경로·관리 화면 뼈대 위에 선다.

Output: 마이그레이션 1개(`label`·`archived_at`·`archived_by`·`archived_options`·`version`·unique `(entity, label)`·기존 거래처 칸 노출 행 채움) · `domain/custom-fields/{admin-input,admin}.ts` · `repositories/permissions.ts`의 `insertVisibilityIfAbsent`(선택 `tx`) · `/admin/field-definitions` 최소 화면(권한 게이트 + `?new=1` 등록 폼) · 거래처 폼 한글 라벨 · 통합·E2E.

**실행 순서 — Phase 4 선행:** `db/migrations/meta/_journal.json`(04-01·04-02·04-06·04-07·04-10·04-13·04-27·04-41) · `repositories/permissions.ts`(04-20) · `app/(app)/admin/vendors/vendor-form.tsx`(04-25)를 Phase 4도 고치므로 그 Phase 4 플랜이 작업 브랜치에 들어온 뒤에만 실행한다. 이후 플랜(03·04·05·06·07·09)은 도중에 main을 합치지 않고 HEAD만 보므로, 여기서 그 플랜들의 선행 Phase 4 플랜까지 합친 합집합을 확인한다 — 어떤 작업보다 먼저 `sh -c 'git fetch -q origin main && git merge --no-edit origin/main && for p in 04-01 04-02 04-06 04-07 04-08 04-10 04-12 04-13 04-14 04-18 04-20 04-25 04-27 04-29 04-32 04-41 04-42 04-46; do git ls-tree --name-only HEAD .planning/phases/04-project-quote-ledger/ | grep -q "/$p-SUMMARY.md" || exit 1; done'`를 돌려(main을 작업 브랜치에 합친 뒤 HEAD에서 확인) 종료 코드가 0이 아니면(병합 충돌 포함) 멈추고 사용자에게 알린다.

**실행 규율:** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED를 실제로 확인 → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적는다. 한 세션에 플랜 하나. E2E 판정은 `CI=true`다. **공유 테스트 환경 직렬화(공유 결정 3):** 공유 테스트 DB(`erp_test`)·통합 테스트·`db:reset:test`·마이그레이션·E2E 서버 포트 3100·`next build`를 건드리는 명령은 전부 `flock /tmp/plant8-erp-test.lock` 접두로 돌린다(여러 명령을 이으면 `sh -c '…'`로 묶어 잠금 하나 안에서 — flock은 첫 낱말을 실행 파일로 보므로 `CI=true` 같은 환경 변수 대입은 `sh -c '…'` 안이나 `env CI=true …`로 둔다). 단위 테스트·lint·typecheck는 잠그지 않는다. **E2E DB 준비 순서(공유 결정 4):** `pnpm db:reset:test`(빈 DB를 만든다) → Playwright globalSetup `test/e2e/global-setup.ts`(14–31행: 스키마 비우기 → `migrate`로 마이그레이션 → `seedMasterData`로 기본 시드) → 이 스펙의 `beforeAll` 픽스처(권한 행·기획 PM 사용자). `db:dev`는 DB 서버를 띄우고 없는 DB만 만든다 — DB를 다시 만든다고 가정하지 않는다. 화면 검증 순서는 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사(07에서 한 번에) → 수정 → 전체 게이트 한 번이다. 새 의존성 0개. `.planning/`은 GSD 도구로만 쓴다.

**병렬 페이즈 규칙(Phase 4 · 04.1~04.4와 나란히):** `domain/projects/*`·`domain/quotes/*`·프로젝트/견적 줄 폼, `domain/permissions/project.ts`·`dto-registry.ts`, `domain/seed/index.ts`는 고치지 않는다. 공유 파일(`repositories/permissions.ts`·`db/migrations/meta/_journal.json`·`app/(app)/admin/vendors/vendor-form.tsx`)에는 항목만 더한다 — 기존 줄을 고치거나 옮기지 않는다(vendor-form의 라벨 두 곳과 로컬 타입 한 줄은 예외: UI-SPEC 화면 3 변경 2 · Codex #9). `listFieldDefinitions`는 바꾸지 않는다. STATE.md의 현재 페이즈(4)는 건드리지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-custom-field-admin/04.5-CONTEXT.md
@CLAUDE.md
# 범위 Read(절 이름으로 찾는다 — 줄 번호는 UI-SPEC 개정 3(75ae245) 기준):
# .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md 「화면 1」 211–237행(권한 줄) · 「화면 2」 239–265행(키·타입·등록 폼 칸) · Assumptions O11·O20·O22
# .planning/phases/04.5-custom-field-admin/04.5-RESEARCH.md 「Common Pitfalls」 Pitfall 1(label NOT NULL) · 「Code Examples」 insert-if-absent 패턴 · Assumptions A1·A4·A5
# .planning/phases/04.5-custom-field-admin/04.5-PATTERNS.md — 파일별 가까운 선례
# docs/design/SYSTEM.md §6-1(관리자 마스터 틀) · §7-15(폼)
# docs/ARCHITECTURE.md §4-5(커스텀 필드 규약)
</context>

<probe_fallback>
⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). ROADMAP Phase 04.5의 `**Requirements**`는 TBD다(ADMN-07은 Phase 10에 남음). 수용 기준은 ROADMAP 04.5 기준 1~5, CONTEXT D10-12·D10-13과 Claude's Discretion, UI-SPEC(개정 3 승인본) `## UI Considerations`에서 끌어와 `must_haves`에 적었다.

**UI Considerations 행 배분(옛 01 → 01·08, 빠진 행 없음):** 이 플랜은 E5 auto-register(한 트랜잭션 노출 행)와 E2(등록 폼)의 **등록 경로 자체**를 덮는다. 옛 01이 덮던 E1(목록)의 populated·zero-one-many·long-text·overflow와 E2의 empty(기본값)·error·loading·success·name-reserved는 전부 08로 옮겼다 — 08의 `must_haves`가 같은 문장으로 받는다. backstop 여섯 줄은 DOM 감사를 하는 07이 옮겼다.

**옛 01의 must_have 중 08로 옮긴 것(조용히 뺀 것 없음 — 메뉴 등록·관리 인덱스·액션 등록부·누수 스캔 계약은 08에서 다시 09로 나눴다):** 관리 인덱스 「화면 항목」 링크와 SYSTEM.md §6-10·`role-menu.test.ts`·`admin-nav.spec.ts`의 10 → 11(UI-SPEC O6 — 한 계획에서 함께) · `MENUS` 등록(아래 「메뉴 등록 위치」) · 목록 표 · 정렬 기본값(`nextSortOrder`) · 칸 오류 문구 · 이름 예약(활성·보관, 「보관함에서 복원」 링크의 두 쓰기 권한) · 폼 전체 이유 자리(OPEN 1-A) · 제출 중 잠금 · 결과 줄(1-C, 「하나 더 추가」) · `FieldDefinitionAdminDto`의 누수 스캔 계약(Codex #6) · 액션 등록부(`actions.registry.ts`) · `test/e2e/fixtures.ts`의 `archiveE2EFieldDefinitions`(03·04·06·07이 쓰는 정리 픽스처 — 이 플랜의 스펙도 08에서 `afterAll`로 붙는다) · 마이그레이션 문장 순서 가드 `test/unit/custom-fields/field-definitions-migration.test.ts`(07의 재생성 뒤 손 편집 재적용을 지키는 단위 테스트 — 파일 이름·위치는 그대로, 만드는 플랜만 08) · `targets.ts` 대상 등록부. 이 플랜에 남은 것: 생성 중간 실패 롤백 테스트(`test/integration/field-definitions-admin.test.ts`)와 기존 칸 노출 행 채움의 실행 검증(같은 파일).

**계획 단계 결정(표면화):**

- **메뉴 등록 위치 — 09(파일 수 상한과 기존 테스트 결합 때문 — 08에서 나눔):** `domain/permissions/menus.ts`의 `MENUS`에 `admin.*` 키를 더하면 기존 단위 테스트 `test/unit/admin-menu-registry.test.ts`가 같은 키가 `/admin` 인덱스(`ui/shell/role-menu.ts`)에 있기를 요구하고, UI-SPEC O6은 그 인덱스 링크와 SYSTEM.md §6-10 표·순서 테스트를 **같은 계획**에서 고치라고 한다. 공유 결정 1이 인덱스 링크·SYSTEM.md 표·순서 테스트를 08로 보냈고 08의 파일 수 분할로 09가 받았으므로 `MENUS` 등록도 09에 둔다(셋을 떼면 단위 테스트가 빨개지거나 O6이 깨진다). 이 플랜의 화면·domain은 처음부터 최종 권한 판정 `can(viewer, "admin.field-definitions", …)`을 쓴다 — 바뀌는 것은 권한 행이 어디서 오느냐뿐이다. 이 플랜의 통합 테스트는 `beforeEach`에서, E2E는 `beforeAll`에서 `repositories/permissions.ts`의 `upsertPermission`으로 시스템 관리자 view·write 행을 명시로 넣는다(선례: `test/integration/revenue-entries.test.ts` 51–52행). 09가 `MENUS`에 키를 등록하면 시드가 같은 행을 주므로 09가 이 준비 줄을 지운다 — 지운 뒤에도 초록인 것이 시드 경로의 증명이다.
- **대상 상수 위치:** 이 플랜은 대상을 `domain/custom-fields/admin.ts`의 모듈 상수 하나(`"vendor"`, `domain/vendors/index.ts`의 `VENDOR_ENTITY` 선례)와 노출표 항목 키 함수(`cf.<entity>.<key>`) 하나로 둔다. 08이 순수 모듈 `domain/custom-fields/targets.ts`(`FIELD_DEFINITION_TARGETS`·`customFieldInfoItem`·상한 상수)를 만들며 이 둘을 그리로 옮긴다 — 파일 수 상한(≤15) 때문에 이 플랜은 새 모듈을 하나 덜 만든다.
- **키 형식(A1):** `cf_` + `randomUUID()`의 16진 앞 8자. 이름에서 유도하지 않는다(로마자화 의존성 없음). 이름을 바꿔도 키는 그대로다. 유니크 제약 `field_definitions_entity_key_key` 위반이면 새 키로 **트랜잭션 전체를 다시** 연다(Postgres는 제약 위반 뒤 같은 트랜잭션을 쓸 수 없다) — 최대 3번. 이름 unique(`(entity, label)`) 위반은 재시도하지 않는다(그 오류 문구는 08).
- **노출표 항목 키:** `cf.<entity>.<key>`(예 `cf.vendor.cf_1a2b3c4d`). `INFO_ITEMS`의 어떤 키도 `cf.`로 시작하지 않는다는 불변식은 08의 단위 테스트가 건다(03의 `<assumption_delta_decision>`).
- **`insertVisibilityIfAbsent`(A5 · 병렬 제약 3 · 공유 결정 5):** 「Phase 4 선행」에서 main을 합친 뒤 `git grep -n "export async function insertVisibilityIfAbsent" HEAD -- repositories/permissions.ts`를 돌린다(계획 시점 2026-09-24 main에는 없음 — 04-20이 들어오면 생긴다). **있으면** 그 함수를 재사용하고, 트랜잭션 인자가 없으면 끝에 선택 인자 `tx: DbOrTx = db` 하나만 더하고 쿼리가 전역 `db` 대신 그 `tx`로 돌게 한다(04-20 호출부는 인자 둘이라 그대로 돈다). **없으면** `repositories/permissions.ts` **파일 끝에** 04-20과 같은 이름·모양 `insertVisibilityIfAbsent(viewer, input: { roleId, infoItem, visible, updatedBy? }, tx: DbOrTx = db)`를 더한다 — `insertPermissionIfAbsent`를 거울처럼 따르되 `tx.insert(visibilityMatrix)…onConflictDoNothing({ target: [visibilityMatrix.roleId, visibilityMatrix.infoItem] })`로 **넘겨받은 `tx`에서** 실행한다. 어느 쪽이었는지 SUMMARY에 적는다. 07의 머지 직전 의식이 같은 명령으로 다시 확인하고 선택 `tx`를 지킨다. 중간 실패 롤백 통합 테스트가 `tx`가 실제로 쓰이는지 증명한다(전역 `db`로 쓰면 먼저 쓴 행이 남아 빨개진다).
- **마이그레이션 하나(병렬 제약 4 · 전 페이즈 공통 규칙):** 번호는 `pnpm db:generate --name custom_field_admin`이 journal 다음 번호로 정한다 — 손으로 붙이지 않고, 예약 번호대를 쓰지 않는다(04.1의 journal 연속성 검사가 틈 번호를 거부한다). 브랜치에서 나온 번호가 main이나 병렬 페이즈와 겹쳐도 그대로 둔다 — 07의 머지 직전 의식이 main을 합치고 이 페이즈의 SQL·스냅샷·journal 항목을 지운 뒤 다시 생성해 main 마지막 + 1로 만들고, 이 플랜의 손 편집(label 채움 세 문장 · unique 순서 · 노출 행 채움 문장)을 다시 입힌다. `archived_options`·`version`·unique `(entity, label)`을 이 한 파일에 미리 넣는다(02·04가 두 번째 마이그레이션을 만들지 않게).
- **기존 칸 노출 행 채움(그룹 B 요구 · Codex #2 · D10-13):** 이 페이즈 전에 만들어진 거래처 칸 정의에는 노출표 행이 없다(`visible()`은 행이 없으면 숨김 — 03의 칸별 판정이 켜지면 모든 계급에게서 사라진다). 마이그레이션 끝에 표시 주석 한 줄(`-- 04.5: 기존 거래처 칸 노출 행 채움`)과 함께 `INSERT INTO "visibility_matrix" ("role_id", "info_item", "visible") SELECT r."id", 'cf.vendor.' || fd."key", true FROM "field_definitions" fd CROSS JOIN "roles" r WHERE fd."entity" = 'vendor' ON CONFLICT ("role_id", "info_item") DO NOTHING;` 한 문장을 손으로 더한다(보관된 계급 포함 `roles` 전 행 — 칸 생성 경로와 같은 범위). 03 Task 2는 이 SQL에 `visibility_matrix`가 없으면 선행 조건 grep에서 멈춘다.
- **행동 로그 종류:** 마스터 표의 선례(`code_items`·`vendor`)를 따라 `document_create`(02는 `document_update`). `record.ts`는 고치지 않는다.
- **`label` 선택 인자:** `insertFieldDefinition`의 `label`이 없으면 `key`를 쓴다 — `test/integration/quote-lines.test.ts`(Phase 4)·`test/integration/custom-fields.test.ts`가 `label` 없이 부른다.
- **이 플랜의 등록 폼 범위:** 칸 넷(이름·타입·필수·정렬 순서) + 1차 「화면 항목 추가」 + 2차 「취소」. 성공하면 폼을 닫는다(`router.replace("/admin/field-definitions")`). 결과 줄·칸 오류·이유 자리·제출 중 잠금·정렬 기본값·목록 표는 08의 E2 행이다.
</probe_fallback>

<reversibility_notes>

- 마이그레이션(열 다섯, `label` 채움, unique `(entity, label)`, 기존 칸 노출 행 채움) — **costly**: 운영 DB에 적용되면 되돌리려면 역마이그레이션이 필요하다. 합의된 형태(UI-SPEC O11·O20·O22)를 따른다. 노출 행 채움은 `ON CONFLICT DO NOTHING`이라 관리자가 끈 행을 덮지 않는다.
- 노출표 항목 키 규약 `cf.<entity>.<key>` — **costly**: 관리자가 계급별로 끈 노출 행이 이 문자열을 참조한다. 바꾸면 노출 행 이관이 따라온다.
- 칸 키 형식 `cf_<hex8>` — **costly**: 거래처 행 JSONB의 키다. 바꾸면 값 이관이 따라온다.
- 메뉴 키 `admin.field-definitions` — **costly**: 권한표 행이 참조한다(등록은 09, 판정 문자열은 이 플랜부터).
- 관리 화면 경로·폼 모양 — **reversible**: 화면 코드뿐이다.

</reversibility_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 권한 행이 있는 시스템 관리자가 `/admin/field-definitions?new=1`에서 텍스트 칸을 추가하면 거래처 폼에 그 한글 이름이 보인다(마이그레이션 → 리포지토리 → domain(한 트랜잭션 + 전 계급 노출 행) → 생성 액션 → 최소 관리 화면·폼 → 거래처 폼 → E2E)</name>
  <files>db/schema/field-definitions.ts, db/migrations/NNNN_custom_field_admin.sql, db/migrations/meta/NNNN_snapshot.json, db/migrations/meta/_journal.json, repositories/field-definitions.ts, repositories/permissions.ts, domain/custom-fields/admin-input.ts, domain/custom-fields/admin.ts, domain/vendors/index.ts, app/(app)/admin/field-definitions/page.tsx, app/(app)/admin/field-definitions/field-definition-form.tsx, app/(app)/admin/field-definitions/actions.ts, app/(app)/admin/vendors/vendor-form.tsx, test/integration/field-definitions-admin.test.ts, test/e2e/field-definitions.spec.ts</files>
  <reversibility rating="costly">마이그레이션·항목 키 규약 `cf.<entity>.<key>`·칸 키 형식·메뉴 키 문자열이 운영 데이터(노출 행·JSONB 키·권한 행)에 새겨진다. 모양은 UI-SPEC O11·O20·O22·RESEARCH A1·A4를 따른 계획 재량이다.</reversibility>
  <read_first>
    - `db/schema/field-definitions.ts`(전문) · `db/schema/vendors.ts`의 `archivedAt`·`archivedBy` 열 선언(같은 모양으로) · `db/schema/permissions.ts` 27–40행(`visibilityMatrix`·unique `visibility_matrix_role_item_key`)
    - `repositories/field-definitions.ts`(전문 63행 — `insertFieldDefinition`은 호출자가 `id`를 준다) · `repositories/permissions.ts`(전문 — `insertPermissionIfAbsent` 56행~, `upsertPermission` 34행~, `upsertVisibility` 100행~) · `repositories/roles.ts`의 `listRoles(viewer, { includeArchived })`
    - `lib/db-transaction.ts`(`withTransaction`) · `db/client.ts` 41–48행(`DbOrTx`) · `lib/pg-errors.ts`의 `isUniqueViolation`
    - `domain/permissions/can.ts`(전문 — `ForbiddenError`) · `domain/vendors/index.ts` 1–70행(에러 클래스·`VENDOR_ENTITY`) · 124–160행(`FieldDefinitionDto`·`listVendorFieldDefinitions`) · 241–280행(`createVendor`의 게이트·로그 순서) · `domain/action-log/record.ts`의 `recordAction` 입력 모양
    - `app/(app)/admin/vendors/page.tsx`(전문 — 인증 가드·`notFound`·`?new=1`·필터 줄) · `app/(app)/admin/vendors/actions.ts`(전문 — `authedActionClient.schema(…)` 모양) · `app/(app)/admin/vendors/vendor-form.tsx` 14–22행(`VendorFieldDefinition`) · 205–232행(`VendorCustomField`)
    - `ui/form/Form.tsx`(전문) · `ui/select/Select.tsx` · `ui/input/TextField.tsx` · `ui/button/Button.tsx` · `app/(app)/projects/project-form.tsx`(`ui/form/Form` 사용 선례)
    - `test/integration/custom-fields.test.ts`(행 id·픽스처 관례) · `test/integration/setup.ts`(매 테스트 TRUNCATE + `seedMasterData`) · `test/integration/revenue-entries.test.ts` 45–55행(`upsertPermission`으로 권한 행을 테스트에서 준비하는 선례) · `test/e2e/fixtures.ts`(전문 — `createFixtureUser`) · `test/e2e/vendor-edit.spec.ts` 1–40행(`loginAsSysadmin`) · `test/e2e/code-tables-write-gate.spec.ts`(E2E가 `@/repositories/`를 import하는 선례·권한 없는 계급의 접근)
    - `test/e2e/global-setup.ts` 14–46행(E2E DB 준비 순서) · `.squawk.toml`(전문) · `drizzle.config.ts`
    - UI-SPEC 「화면 1」 229–231행(권한·대상) · 「화면 2」 239–255행(칸 넷·키·타입)
  </read_first>
  <behavior>
    - (통합) `test/integration/field-definitions-admin.test.ts`의 `beforeEach`가 `upsertPermission`으로 `SYSADMIN_ROLE_ID`에 `admin.field-definitions` view·write 행을 넣는다(09 전까지 `MENUS`에 키가 없어 시드가 주지 않는다). 시스템 관리자의 `createFieldDefinition`이 `entity = vendor`, 자동 키 `^cf_[0-9a-f]{8}$`, `label` = 앞뒤 공백을 자른 이름, `version` = 1, `archived_at` = null인 행을 만든다
    - (통합) `roles` 전 행(`archived_at`을 직접 채운 보관 계급 하나 포함)에 `info_item = cf.vendor.<key>`, `visible = true` 행이 정확히 하나씩 있고, 행동 로그 `document_create`(entity `field_definitions`, detail `{ key, entity: "vendor" }`) 한 줄이 남는다
    - (통합 · 공유 결정 5) 노출 행 쓰기 deps를 진짜 `insertVisibilityIfAbsent(…, tx)`를 두 번 부른 뒤 세 번째에 던지는 함수로 주입하면 `createFieldDefinition`이 그 오류로 끝나고, 그 이름의 칸 정의 행이 없으며, 그 키의 `cf.vendor.%` 노출 행이 0개이고(앞의 두 행까지 롤백), 로그가 늘지 않는다
    - (통합) 기획 PM(`DEFAULT_ROLE_ID`) viewer의 `createFieldDefinition`이 메시지 「권한 없음」의 `ForbiddenError`로 끝나고 칸 정의·노출 행·로그 수가 그대로다
    - (통합) `createFieldDefinitionInput.safeParse`가 `entity` 키가 실린 입력과 `type: "select"`를 거부하고 `{ name, type: "text"|"number"|"date", required, sortOrder }`만 받는다
    - (통합) `label` 없이 `insertFieldDefinition`을 부르면 `label = key`다 · `listFieldDefinitions(viewer, "project")`가 `sortOrder`, `key` 순으로 행을 돌려준다(새 열만 더해짐)
    - (E2E) `beforeAll`: `upsertPermission`으로 시스템 관리자 view·write 행 · `createFixtureUser({ roleId: DEFAULT_ROLE_ID })`로 기획 PM 사용자. 시스템 관리자가 `/admin/field-definitions?new=1`에서 이름(`E2E칸` + 16진 6자 — 「이름」이라는 글자를 넣지 않는다: `vendors.spec.ts` 30행 `getByLabel("이름")`이 부분 일치로 센다)·타입 「텍스트」·정렬 순서 1을 넣고 「화면 항목 추가」 → 주소가 `/admin/field-definitions`(폼 닫힘) → `/admin/vendors?new=1`에서 `getByLabel(그 이름)`이 보이고 화면에 `cf_` + 16진 8자 문자열이 없다
    - (E2E) 기획 PM이 `/admin/field-definitions`와 `/admin/field-definitions?new=1`에서 404를 받는다
  </behavior>
  <action>
① **RED 먼저.** 위 behavior의 통합·E2E를 먼저 쓰고 빨간 것을 확인한다(Skill `test-driven-development`). E2E 스펙이 만든 칸은 선택 칸(필수 아님)이라 같은 실행의 거래처 스펙을 막지 않는다 — 정리 픽스처(`archiveE2EFieldDefinitions`)와 이 스펙의 `afterAll`은 08이 붙인다. 이 스펙의 `beforeAll` 준비는 공유 결정 4 순서의 마지막 단계(픽스처 시드)다.

② **스키마·마이그레이션.** `db/schema/field-definitions.ts`에 `label`(text, NOT NULL) · `archivedAt`·`archivedBy`(거래처 표와 같은 선언) · `archivedOptions`(jsonb, NOT NULL, 기본 빈 배열, `string[]` 타입) · `version`(integer, NOT NULL, 기본 1) · unique 제약 `field_definitions_entity_label_key`(`entity`, `label`)를 더하고 표 위 주석에 한 줄(보관 선택지는 `archived_options`, `options`는 활성만 · 이름은 보관 칸까지 예약 · `version`은 조건부 갱신용 — UI-SPEC O11·O20·O22)을 더한다. `flock /tmp/plant8-erp-test.lock pnpm db:generate --name custom_field_admin`으로 SQL·스냅샷·journal 항목을 만든다(번호는 도구가 정함 — 생성 명령 출력을 SUMMARY에 인용). 생성된 SQL의 `label` 줄을 손으로 세 문장으로 바꾼다: 기본값 빈 문자열의 NOT NULL로 열 추가 → `UPDATE "field_definitions" SET "label" = "key"` → `ALTER COLUMN "label" DROP DEFAULT`. unique `(entity, label)` 제약 추가 문장이 이 채움 **뒤**에 오게 순서를 맞춘다(기존 행이 있는 DB에서 NOT NULL·unique 추가가 실패하지 않게 — RESEARCH Pitfall 1; `(entity, key)`가 이미 유일이라 채움 뒤 충돌 없음). `pnpm lint:sql`이 이 모양을 받는지 확인하고, squawk가 거부하면 규칙을 끄지 말고 squawk가 받는 동등한 채움 모양으로 바꾼다. 손 편집한 SQL 본문을 SUMMARY에 그대로 옮겨 둔다(07의 머지 직전 재생성이 이것을 다시 입힌다). 기존 거래처 칸 노출 행 채움 문장은 Task 2다.

③ **리포지토리.** `insertFieldDefinition`에 선택 `label`(없으면 `key`)과 마지막 선택 인자 `tx: DbOrTx = db`를 더하고, 쿼리를 그 `tx`로 돌린다. `listFieldDefinitions`·`findFieldDefinitionById`·`updateFieldDefinition`은 바꾸지 않는다(정렬 `orderBy(sortOrder, key)` 유지). 같은 파일 끝에 `lockCustomFieldGrants(tx: DbOrTx)`를 더한다 — 모듈 비공개 고정 정수 하나로 `pg_advisory_xact_lock`을 잡는 트랜잭션 잠금(커밋·롤백 때 저절로 풀림, T-04.5-07). 칸 생성(④)과 03의 계급 쪽 경로 `grantCustomFieldsToRole`이 **이 함수 하나**를 부르므로 잠금 번호가 갈라질 수 없다 — 잠금 번호를 domain 상수로 export해 두 곳이 각자 잠그는 방식 대신 리포지토리 함수로 둔 이유: domain은 SQL을 낼 수 없다(boundaries — `domain`은 `db` 금지, `DbOrTx`에 `execute` 없음). `DbOrTx`에 `execute`가 없으므로 `db/client.ts`를 넓히지 않고 select 빌더로 부른다(`tx.select({ … }).from(sql\`pg_advisory_xact_lock(…)\`)` 모양 — drizzle 0.45의 `from()`이 `SQL`을 받는다). 계획 시점 코드베이스에 다른 advisory lock 사용처는 없다. `repositories/permissions.ts`는 `<probe_fallback>`의 `insertVisibilityIfAbsent` 확인 명령 결과대로 재사용(+선택 `tx`)하거나 파일 끝에 더한다 — 어느 쪽이든 쿼리가 넘겨받은 `tx`에서 돈다(공유 결정 5).

④ **domain.** `domain/custom-fields/admin-input.ts`(zod만 import): `createFieldDefinitionInput` — `.strict()` `{ name: 문자열(앞뒤 공백 자름, 1자 이상), type: enum(text|number|date), required: boolean, sortOrder: 정수 }`(대상 없음). 칸 오류 문구·20자·0~999 범위·이름 충돌 문구는 08이 이 스키마에 더한다. `domain/custom-fields/admin.ts`: 모듈 상수 `FIELD_ENTITY = "vendor"`와 노출표 항목 키를 만드는 비공개 함수(`cf.${entity}.${key}`) — 08이 둘을 `targets.ts`로 옮긴다. `createFieldDefinition(viewer, { name, type, required, sortOrder }, deps?)`: `can(viewer, "admin.field-definitions", "write")`가 아니면 메시지 「권한 없음」의 `ForbiddenError`(`domain/permissions/can.ts`) → 키 생성 → `withTransaction` 안에서 **첫 문장** `lockCustomFieldGrants(tx)`(③) → 그 **뒤에** 계급 목록(`repositories/roles.ts`의 `listRoles(viewer, { includeArchived: true })` — 시그니처는 그대로, 잠금을 잡은 뒤의 조회라 먼저 커밋된 계급을 전부 본다. 트랜잭션 전에 읽으면 그 사이 만든 계급을 놓치고 03의 계급 쪽 경로도 아직 커밋 전인 이 칸을 못 봐 그 계급에게 칸이 영구히 숨는다 — T-04.5-07. 계급 목록 dep도 잠금 뒤 이 자리에서 불린다: 03의 경합 테스트와 06의 픽스처가 이 순서에 기댄다) → `insertFieldDefinition(viewer, { id: randomUUID(), entity: FIELD_ENTITY, key, label: name, type, required, sortOrder }, tx)` → 계급마다 `insertVisibilityIfAbsent(viewer, { roleId, infoItem, visible: true, updatedBy: viewer.id }, tx)`(전 계급 부여는 이 함수 안 반복문 하나 — 한 번 쓰이는 도우미 모듈을 따로 만들지 않는다; 03이 `domain/custom-fields/visibility.ts`를 만든다) → 커밋 뒤 `recordAction(viewer, { actionType: "document_create", entity: "field_definitions", entityId, detail: { key, entity: FIELD_ENTITY } })`. `can`·키 생성기·계급 목록·노출 행 쓰기·`recordAction`은 `deps`로 주입 가능하게 둔다(중간 실패·충돌 테스트용 — `domain/vendors`의 `*Deps` 선례). 이 태스크의 키 생성은 한 번이다(충돌 재시도는 Task 2). 반환은 새 행의 `{ id, key }`만(행 타입을 그대로 돌려주지 않는다 — `plant8/no-row-type-escape`). `domain/vendors/index.ts`: `FieldDefinitionDto`에 `label`을 더하고 `listVendorFieldDefinitions`가 `archivedAt`이 null인 정의만 싣는다(보관 칸은 입력에서 숨김 — D10-12). `validatedCustomFields`는 바꾸지 않는다(05).

⑤ **액션·화면.** `app/(app)/admin/field-definitions/actions.ts`: `createFieldDefinitionAction` — vendors 선례대로 `authedActionClient.schema(createFieldDefinitionInput)`, `createFieldDefinition(ctx.viewer, parsedInput)` 호출, 결과를 돌려주지 않는다(DTO 없음), `revalidatePath("/admin/field-definitions")`·`revalidatePath("/admin/vendors")`·`revalidatePath("/admin/visibility")`. 액션 등록부(`actions.registry.ts`)와 누수 스캔 import는 메뉴 등록과 한 묶음으로 08이 만든다(누수 스캔 액션 축이 `action.menu`가 `MENUS`에 있기를 요구한다). `page.tsx`: vendors 페이지와 같은 인증 가드 → `can(viewer, "admin.field-definitions", "view")` 없으면 `notFound()` → `PageHeader title="화면 항목" subtitle="거래처"` → 필터 줄(쓰기 권한이 있고 폼이 닫혀 있을 때만 3차 링크 「화면 항목 추가」 → `?new=1`) → `?new=1`이고 쓰기 권한이 있으면 등록 폼(쓰기 권한이 없으면 `?new=1`로 와도 폼을 렌더하지 않는다). 목록 표·`field-definitions.module.css`는 08이다. `field-definition-form.tsx`(클라이언트): `ui/form/Form` + `className="single-column"`, 칸 넷 — 이름(`TextField`, long) · 타입(`Select`, select 폭, 텍스트/숫자/날짜, 기본 텍스트) · 필수(체크박스 「필수」) · 정렬 순서(`TextField` short, 숫자) → 1차 「화면 항목 추가」 · 2차 링크 「취소」(`/admin/field-definitions`). `useAction(createFieldDefinitionAction)`이 성공하면 `router.replace("/admin/field-definitions")`로 폼을 닫는다 — 결과 줄·칸 오류·이유 자리·제출 중 잠금·정렬 기본값은 08의 E2 행이다. 새 색·간격·radius 없음(이 플랜은 CSS 모듈을 만들지 않는다).

⑥ **거래처 폼 라벨(UI-SPEC 화면 3 변경 2 · Codex #9).** `vendor-form.tsx` 16행 부근 로컬 타입 `VendorFieldDefinition`에 `label: string`을 더하고(domain DTO 변경만으로는 이 타입이 넓어지지 않는다), `VendorCustomField`의 라벨 두 곳(`<label>` 텍스트·`TextField`의 `label`)을 `def.key` → `def.label`로 바꾼다. 입력 이름·값 키(`cf_${def.key}`·`customFields[def.key]`)는 그대로다. 그 밖의 거래처 폼 변경은 06이다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·squawk·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 경계 규칙(domain → db 직접 import), 행 타입 유출 규칙, 마이그레이션 SQL 린트 위반 포함</fails_when>
    <automated>pnpm test:unit</automated>
    <fails_when>vitest unit의 failed 개수가 1 이상이다 — 특히 `admin-menu-registry`·`role-menu`(이 플랜은 `MENUS`를 건드리지 않으므로 그대로 초록이어야 한다)·`leak-scan-coverage`·`import-cycles`·`page-auth-guard`</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts test/integration/custom-fields.test.ts test/integration/quote-lines.test.ts test/integration/vendors.test.ts'</automated>
    <fails_when>통합 failed 개수가 1 이상이다 — 한 트랜잭션(중간 실패 롤백)·전 계급 노출 행·로그·권한 거부·`label` 기본값·Phase 4 견적 줄 저장·거래처 저장 회귀 중 하나가 깨졌다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/field-definitions.spec.ts test/e2e/vendor-edit.spec.ts test/e2e/vendors.spec.ts'</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 추가 → 거래처 폼 한글 라벨, 기획 PM 404, 기존 거래처 화면 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 마이그레이션 SQL이 하나이고, `label` 채움(`UPDATE … SET "label" = "key"`)이 NOT NULL 기본값 제거와 unique `(entity, label)` 제약보다 먼저 온다(SUMMARY에 손 편집 본문 인용)
    - 마이그레이션 번호를 손으로 붙이거나 바꾸지 않았다 — SQL 파일 이름·스냅샷·journal `idx`가 `pnpm db:generate` 출력 그대로다(SUMMARY에 생성 명령 출력 인용)
    - `grep -n "export async function insertVisibilityIfAbsent" repositories/permissions.ts`가 정확히 한 줄이고, 그 함수 본문의 쿼리가 선택 인자 `tx`로 돈다(중간 실패 롤백 통합 테스트가 초록)
    - `grep -v '^\s*//' repositories/field-definitions.ts | grep -c "pg_advisory_xact_lock"`가 1이고(주석 줄 제외), `createFieldDefinition`의 `withTransaction` 콜백에서 `lockCustomFieldGrants(tx)`가 첫 await이며 계급 목록 조회(dep)가 그 뒤다 — `db/client.ts` diff가 비어 있다(경합 실행 증명은 03 Task 1의 통합 테스트)
    - 생성 입력 스키마(`createFieldDefinitionInput`)에 `entity` 키가 없고 `.strict()`다 — 통합 테스트가 `entity`를 실은 입력의 거부를 단언한다
    - `vendor-form.tsx`의 로컬 `VendorFieldDefinition` 타입에 `label: string`이 있다
    - `domain/seed/index.ts`·`domain/permissions/menus.ts`·`domain/permissions/project.ts`·`domain/permissions/dto-registry.ts`·`domain/projects/*`·`domain/quotes/*`의 diff가 비어 있다
    - `listFieldDefinitions`의 본문(조건·정렬) diff가 비어 있다
    - 네 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>권한 행이 있는 시스템 관리자가 관리 화면에서 거래처 칸을 추가하면 거래처 폼에 한글 이름으로 보이고, 같은 트랜잭션에서 전 계급 노출 행이 생기며 중간에 실패하면 아무것도 남지 않는다 — 이 페이즈의 층 전부가 한 경로로 이어졌다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 기존 거래처 칸의 노출 행 채움(전 계급 · 멱등 · 꺼진 행 유지) · 키 충돌 재시도</name>
  <files>db/migrations/NNNN_custom_field_admin.sql, domain/custom-fields/admin.ts, test/integration/field-definitions-admin.test.ts</files>
  <read_first>
    - Task 1이 만든 마이그레이션 SQL·`domain/custom-fields/admin.ts`·통합 테스트(이 세션에서 쓴 그대로 — 다시 읽지 않아도 되면 건너뛴다)
    - `db/schema/roles.ts`(`roles` 표 이름·`archived_at`) · `db/schema/permissions.ts` 27–40행(`visibility_matrix` 열·unique)
    - `lib/pg-errors.ts`의 `isUniqueViolation(error, constraint)`
  </read_first>
  <behavior>
    - (통합) 테스트가 마이그레이션 SQL 파일(`db/migrations/*_custom_field_admin.sql`, 하나여야 한다)에서 표시 주석 `-- 04.5: 기존 거래처 칸 노출 행 채움` 뒤의 한 문장을 읽는다 — 없으면 실패한다
    - (통합) 준비: 거래처 칸 정의 하나와 프로젝트 칸 정의 하나를 `insertFieldDefinition`으로 직접 넣고(노출 행 없음), 계급 하나를 `archived_at`으로 보관하고, 기획 PM 계급의 그 거래처 칸 항목 행을 `visible = false`로 미리 넣는다 → 읽은 문장을 실행 → 그 거래처 칸 키의 `cf.vendor.<key>` 행이 `roles` 전 행(보관 계급 포함)에 하나씩 있고, 기획 PM 행은 `false` 그대로이며, 프로젝트 칸에는 행이 없다 → 같은 문장을 한 번 더 실행해도 행 수와 값이 같다
    - (통합) 키 생성기를 주입해 첫 키가 기존 거래처 칸 키와 같게 만들면 두 번째 키로 성공하고, 노출 행은 두 번째 키로만 있다 · 세 번 모두 기존 키와 같으면 오류로 끝나고 칸 정의·노출 행·로그가 늘지 않는다
  </behavior>
  <action>
① RED: behavior의 통합 케이스를 먼저 쓰고 빨간 것을 확인한다(문장이 없어 읽기 실패 · 첫 키 충돌이 unique 위반으로 끝남).

② **마이그레이션 끝에 기존 칸 노출 행 채움(그룹 B 요구 · Codex #2 · D10-13).** Task 1의 손 편집 뒤, SQL 파일 끝에 `--> statement-breakpoint`로 나눈 표시 주석 `-- 04.5: 기존 거래처 칸 노출 행 채움`과 문장 하나를 손으로 더한다: `INSERT INTO "visibility_matrix" ("role_id", "info_item", "visible") SELECT r."id", 'cf.vendor.' || fd."key", true FROM "field_definitions" fd CROSS JOIN "roles" r WHERE fd."entity" = 'vendor' ON CONFLICT ("role_id", "info_item") DO NOTHING;` — `updated_at`은 기본값, `updated_by`는 null(시드 행과 같은 결). `pnpm lint:sql`이 받는지 확인한다. 이 본문도 SUMMARY의 손 편집 인용에 더한다(07이 재생성 뒤 다시 입힌다 · 08의 `field-definitions-migration.test.ts`가 이 문장의 존재와 순서를 지킨다).

③ **키 충돌 재시도(A1).** `createFieldDefinition`에서 `isUniqueViolation(error, "field_definitions_entity_key_key")`면 새 키로 `withTransaction` 전체를 다시 연다(잠금·계급 목록 읽기도 새 트랜잭션에서 다시 — 롤백이 잠금을 푼다) — 최대 3번, 넘으면 마지막 오류를 그대로 던진다. 다른 제약 위반(이름 unique `field_definitions_entity_label_key` 포함)은 재시도하지 않고 그대로 던진다(이름 예약 문구는 08).

④ **개발 DB 확인(로컬만 — 운영 DB 명령 금지).** `flock /tmp/plant8-erp-test.lock pnpm db:dev`로 개발 DB `erp`를 띄운다. 이 페이즈 마이그레이션이 아직 적용되지 않았으면 거래처 칸 정의 행이 하나 이상 있게 하고(없으면 psql로 하나 넣는다) `flock /tmp/plant8-erp-test.lock pnpm db:migrate`를 돌린 뒤, 그 행의 `label` = `key` · `version` = 1 · 그 키의 `cf.vendor.<key>` 노출 행 수 = `roles` 행 수를 조회해 SUMMARY에 적는다. 이미 적용돼 있으면 그 사실과 같은 조회 결과를 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql</automated>
    <fails_when>eslint·tsc·squawk 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 손으로 더한 채움 문장의 SQL 린트 위반 포함</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts test/integration/custom-fields.test.ts test/integration/quote-lines.test.ts'</automated>
    <fails_when>통합 failed 개수가 1 이상이다 — 채움 문장(전 계급·보관 계급·꺼진 행 유지·프로젝트 칸 제외·두 번 실행 멱등), 키 충돌 재시도, Task 1의 트랜잭션·롤백 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'INSERT INTO "visibility_matrix"' db/migrations/*_custom_field_admin.sql`가 1이고 같은 파일에 `ON CONFLICT ("role_id", "info_item") DO NOTHING`이 있다(03 Task 2의 선행 조건 grep이 통과한다)
    - 채움 문장이 `label` 채움·unique `(entity, label)` 문장보다 뒤에 있다
    - 개발 DB 확인 조회 결과(`label` = `key` · `version` = 1 · 노출 행 수 = 계급 수)가 SUMMARY에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>이 페이즈 전에 만들어진 거래처 칸도 전 계급 노출 행을 받고(관리자가 끈 행은 그대로), 키가 부딪혀도 칸 추가가 실패하지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `createFieldDefinitionAction` | 관리자가 적은 이름·타입·필수·정렬이 서버로 들어온다 — 요청 몸통은 위조될 수 있다 |
| 관리 화면 → 거래처 폼 | 관리자가 적은 이름이 거래처 폼의 라벨로 그대로 나간다 |
| 마이그레이션 → 노출표 | 배포 때 도는 채움 문장이 관리자가 이미 정한 노출 행과 만난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-01 | Elevation of Privilege | `/admin/field-definitions` 화면 · `createFieldDefinition` | high | mitigate | page는 `can(viewer, "admin.field-definitions", "view")` 아니면 `notFound()`, `?new=1` 폼은 write일 때만, domain은 `write`를 매번 다시 판정해 「권한 없음」 `ForbiddenError`. 권한 행은 09의 `MENUS` 등록 뒤 시드의 시스템 관리자 루프만 만든다(다른 계급 행 없음 → `can()` 기본 거부) — 이 플랜의 테스트는 시스템 관리자 행만 명시로 넣는다. 통합(기획 PM 거부)·E2E(404)로 증명 — Task 1 |
| T-04.5-02 | Tampering | 생성 입력(대상·타입) | high | mitigate | 입력 스키마가 `.strict()`이고 `entity`가 없다 — 서버가 거래처(`vendor`)를 넣는다. 타입은 enum(이 플랜은 text·number·date). 프로젝트·견적 줄 칸을 만들 경로가 없다(통합 테스트가 `entity` 실린 입력의 거부를 단언) — Task 1 |
| T-04.5-07 | Tampering | 칸 정의 + 노출 행 쓰기 | medium | mitigate | 한 `withTransaction` — `insertVisibilityIfAbsent`가 넘겨받은 `tx`로 돈다. 노출 행 두 개를 쓴 뒤 세 번째가 실패하면 정의·앞 두 행까지 롤백(주입 실패 통합 테스트, 공유 결정 5). 반쪽 상태(정의는 있는데 일부 계급에만 행이 있는 칸)가 남지 않는다. 계급 생성과의 동시 실행: 트랜잭션 첫 문장 `lockCustomFieldGrants(tx)`(advisory xact lock) 뒤에 계급 목록을 읽고, 03의 `grantCustomFieldsToRole`이 같은 함수로 잠근 뒤 정의를 읽어 같은 트랜잭션에서 행을 쓴다 — 어느 순서로 겹쳐도 (계급, 칸) 쌍마다 행이 생긴다(경합 회귀 통합 테스트는 03 Task 1) — Task 1 |
| T-04.5-51 | Tampering | 마이그레이션의 기존 칸 노출 행 채움 | medium | mitigate | `ON CONFLICT ("role_id", "info_item") DO NOTHING` — 관리자가 끈 행을 덮지 않고 다시 돌려도 행이 늘지 않는다. 대상은 `entity = 'vendor'`뿐(프로젝트·견적 줄 칸 행 없음). 통합 테스트가 문장을 두 번 실행해 멱등·꺼진 행 유지·프로젝트 칸 제외를 단언 — Task 2 |
| T-04.5-08 | Repudiation | 칸 생성 | low | mitigate | 커밋 뒤 `recordAction` `document_create`(entity `field_definitions`, detail 키·대상) — 통합 테스트가 한 줄을 단언, 롤백 경로는 로그 0줄 — Task 1 |
| T-04.5-10 | Tampering | 이름 문자열 렌더(거래처 폼 라벨) | low | mitigate | React 텍스트 노드로만 렌더(HTML 삽입 경로 없음). 이름 20자 서버 상한은 08의 입력 스키마가 더한다 |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — `package.json` 의존성 객체와 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 |

`FieldDefinitionAdminDto`의 정보 노출(옛 T-04.5-09)과 이름 예약(T-04.5-15)은 그 기능과 함께 08로 옮겼다.
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과(`admin-menu-registry`·`role-menu` 포함 — 이 플랜은 `MENUS`·관리 인덱스를 건드리지 않는다)
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration …'`로 통합 네 파일(Phase 4 `quote-lines.test.ts`·거래처 `vendors.test.ts` 포함) 통과
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && CI=true pnpm playwright test …'`로 E2E 세 파일(`field-definitions` · `vendor-edit` · `vendors`) 통과 — DB 준비는 `db:reset:test` → globalSetup(마이그레이션 → 기본 시드) → 스펙 `beforeAll` 픽스처 순서(공유 결정 4)
- 공유 파일 보존: `git diff origin/main -- repositories/permissions.ts`의 삭제 줄(`^-[^-]`)은 0개(추가 경로)이거나 `insertVisibilityIfAbsent`의 시그니처·쿼리 줄뿐(재사용 경로)이다 · `_journal.json`은 기존 항목의 `idx`·`tag`가 전부 그대로이고 새 항목 하나가 끝에 붙었다(바로 앞 항목 닫는 줄의 쉼표 변경만 허용) · `vendor-form.tsx`의 변경은 로컬 타입 `label` 한 줄과 라벨 두 곳뿐이다
- `package.json` 의존성 객체·`pnpm-lock.yaml` 변경 없음
</verification>

<success_criteria>
- 권한 행이 있는 관리자가 거래처 칸(텍스트·숫자·날짜)을 추가하면 거래처 폼에 한글 이름으로 보이고, 전 계급 노출 행이 같은 트랜잭션에서 생기며 중간 실패는 아무것도 남기지 않는다
- 이 페이즈 전의 거래처 칸도 마이그레이션으로 전 계급 노출 행을 받는다(멱등 · 꺼진 행 유지)
- Phase 4 호출부의 `listFieldDefinitions` 결과·견적 줄 저장이 그대로다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `db/migrations/NNNN_custom_field_admin.sql`(번호는 `pnpm db:generate` — 계획에 고정 번호 없음) — `label`(채움) · `archived_at` · `archived_by` · `archived_options` · `version` · unique `(entity, label)` · 기존 거래처 칸 노출 행 채움 |
| 모듈(신규, domain) | `domain/custom-fields/admin-input.ts`(`createFieldDefinitionInput`) · `domain/custom-fields/admin.ts`(`createFieldDefinition`) |
| 함수(신규, repository) | `insertVisibilityIfAbsent`(`repositories/permissions.ts` — main에 있으면 재사용, 선택 `tx`) · `lockCustomFieldGrants`(`repositories/field-definitions.ts` — 칸 생성·계급 쪽 노출 행 부여가 함께 쓰는 트랜잭션 잠금) |
| 변경된 기존 심볼 | `insertFieldDefinition`(+`label` 선택 · `tx`) · `FieldDefinitionDto`(+`label`) · `listVendorFieldDefinitions`(보관 제외) · `VendorFieldDefinition`(+`label`) |
| 화면(신규) | `/admin/field-definitions`(권한 게이트 + `?new=1` 등록 폼 — 목록·결과 줄·칸 오류는 08) |
| 액션(신규) | `createFieldDefinitionAction`(등록부 등록은 09) |
| 테스트 | `test/integration/field-definitions-admin.test.ts`(한 트랜잭션·중간 실패 롤백·전 계급 노출 행·로그·권한 거부·키 충돌 재시도·label 기본값·기존 칸 노출 행 채움) · `test/e2e/field-definitions.spec.ts`(트레이서 + 기획 PM 404) |

<output>
Create `.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md` when done
</output>
