---
phase: 04.5-custom-field-admin
plan: 02
type: execute
wave: 4
depends_on: ["04.5-08", "04.5-09"]
files_modified:
  - domain/custom-fields/options.ts
  - domain/custom-fields/admin-input.ts
  - domain/custom-fields/admin.ts
  - repositories/field-definitions.ts
  - app/(app)/admin/field-definitions/actions.ts
  - app/(app)/admin/field-definitions/actions.registry.ts
  - app/(app)/admin/field-definitions/page.tsx
  - app/(app)/admin/field-definitions/field-definition-form.tsx
  - app/(app)/admin/field-definitions/field-definitions.module.css
  - test/unit/domain/custom-fields-admin.test.ts
  - test/integration/field-definitions-admin.test.ts
  - test/e2e/field-definitions.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "등록 폼에서 타입 「선택」을 고르면 구분선(`1px --line` + `--s-6`) 아래 선택지 편집기가 나온다 — 새 선택지(short, `maxLength` 40) + 3차 「선택지 추가」(또는 새 선택지 입력 안의 Enter — 폼 제출 대신)가 앞뒤 공백을 잘라 활성 목록 끝에 더한다. 빈칸은 「선택지가 비어 있습니다 · 선택지 적기」, 활성과 같으면 「이미 있는 선택지입니다 · 다른 이름 적기」, 활성 30개면 「선택지 추가」가 `disabledReason` 「선택지는 30개까지 · 쓰지 않는 선택지 삭제」로 비활성이다(UI-SPEC 화면 2, E2 overflow·long-text)"
    - "타입이 선택이고 활성 선택지가 0개면 1차가 비활성이고 이유가 「추가할 수 없음 — 선택지 0개 · 선택지 추가」(수정 모드 「수정할 수 없음 — 선택지 0개 · 선택지 추가」)다 — 서버도 선택형의 활성 선택지 1~30개 · 각 1~40자 · 중복 없음을 다시 판정한다(UI-SPEC E2 partial)"
    - "만든 선택형 칸은 거래처 폼에 그 한글 이름과 활성 선택지로 보인다 — 값 검증은 기존 `buildCustomFieldsSchema`(`z.enum(options)`) 그대로다(ROADMAP 04.5 기준 1)"
    - "`?editId=`로 연 수정 폼은 이름·필수·정렬 순서·선택지를 채워 보이고, 타입은 텍스트(예 「선택」)와 3차 「새 화면 항목 추가」(`?new=1`)로만 보인다 — 수정 입력 스키마(`.strict()`)에 `type`·`entity`가 없고 리포지토리 갱신 함수도 `type`을 받지 않는다. 이름을 바꿔도 키가 그대로라 거래처 값과 계급별 노출 설정이 따라온다(ROADMAP 기준 1, Phase 3 규약)"
    - "타입을 바꾸면서 이름을 지키는 길은 「옛 칸의 이름 바꾸기 → (필요하면 보관) → 새 칸을 원래 이름으로 추가」다 — 이름은 보관 칸까지 예약되므로 옛 칸을 그 이름 그대로 보관하면 새 칸이 그 이름을 쓸 수 없다. 화면에 설명 문구는 두지 않고(§8 규칙 5) SUMMARY 「운영 안내」에 적으며, 04가 이 순서를 통합 테스트로 증명한다(UI checker R3)"
    - "`?editId=`가 보관된 칸·없는 id·대상 상수 밖(프로젝트·견적 줄) 정의면 폼 없이 목록만 보인다(vendors `page.tsx`와 같은 조건). 서버 수정도 없는 id·대상 밖을 「화면 항목 없음」으로 거부한다(UI-SPEC 화면 1, O7)"
    - "수정 폼은 연 행의 `version`을 숨은 값으로 싣고, 서버는 행을 읽어 **보관 여부를 먼저** 보고(보관이면 「수정할 수 없음 — 보관된 화면 항목입니다 · 목록으로」) 그다음 제출 `version`과 비교해(다르면 「수정할 수 없음 — 다른 사람이 먼저 고쳤습니다 · 새로 불러오기」) 같을 때만 `UPDATE … SET …, version = version + 1, updated_at = now() WHERE id = ? AND version = ? AND archived_at IS NULL` **한 문장**으로 쓴다. 0행이면 다시 읽어 같은 두 문구 중 하나로 거부하고 칸 값은 남는다(UI-SPEC O20, E2 concurrency, UI checker R1)"
    - "「새로 불러오기」(3차)는 `router.refresh()` 뒤 폼을 `key={id}:{version}`로 다시 마운트해 최신 행(이름·필수·정렬·활성/보관 선택지)을 채우고 이름 입력으로 포커스를 보낸다 — 저장 안 한 입력은 버리고, 누르기 전까지는 입력이 남는다. 「목록으로」(3차)는 `router.replace`로 폼을 닫는다(UI-SPEC E2 stale)"
    - "수정 성공은 1차 자리 결과 줄 「화면 항목 수정 · {이름} 수정됨」(`role=status`, 포커스) + 2차 「닫기」(목록으로 `router.replace`)이고 입력은 읽기 전용이다. 그 밖의 폼 전체 서버 오류는 08의 `formReason(\"수정\", serverError)` 규칙이다(폼 전체 오류 UI는 01 분할로 08이 완성한다, UI-SPEC 화면 2 SUCCESS, Copywriting)"
    - "이름 변경도 01·08의 이름 예약을 따른다 — 자기 자신을 뺀 활성·보관 칸과 같으면 같은 두 문구(보관 쪽 「보관함에서 복원」 링크는 두 쓰기 권한일 때만 — 이름 예약 UI와 그 링크 권한은 08이 완성한다)(UI-SPEC O22)"
    - "수정 모드의 선택지: 아직 저장 안 한 선택지의 「삭제」는 목록에서 빼고, 저장된 선택지의 「삭제」는 보관 목록으로 옮긴다(「취소」하면 원래대로). 보관 선택지와 같은 문자열을 다시 추가하면 그 선택지가 활성으로 돌아온다(중복 생성 없음). 저장된 선택지 문자열은 읽기 전용이다. 두 경우 모두 포커스는 새 선택지 입력으로 간다. 선택지 행의 「삭제」 접근 이름은 「{선택지} 삭제」다(UI-SPEC 화면 2 선택지 상태, O3, R5)"
    - "보관 선택지는 네이티브 `details`(기본 접힘) + `summary` 「보관된 선택지 {N}개」(3차 모양) 안에 선택지마다 텍스트 + StatusTag 「보관됨」으로만 보이고 버튼이 없다 — N = 0이면 렌더하지 않고, 개수 상한은 활성에만 건다(UI-SPEC 화면 2, E2 overflow, **OPEN 2-A**)"
    - "`field_definitions.options`는 활성 선택지 `string[]` 모양 그대로이고, 보관 선택지는 `archived_options`에 둔다. 클라이언트는 활성 배열만 보내고 서버가 **버전 확인을 통과한 저장값**으로 `archived_options = (저장된 options ∪ 저장된 archived_options) − 제출된 options`를 파생해 같은 조건부 UPDATE에 쓴다 — 오래 열린 폼이 다른 사람의 선택지 보관·이름 변경을 되돌리지 못하고, 한 번 저장된 선택지는 활성이나 보관 중 한쪽에 반드시 남는다(UI-SPEC O11·O12·O20)"
    - "칸 수정은 행동 로그 `document_update`(entity `field_definitions`) 한 줄을 남기고, 기획 PM의 수정은 `ForbiddenError`로 거부된다(ROADMAP 기준 1)"
    - "목록의 선택지 열은 타입이 선택일 때 활성 선택지 콤마 목록(보관 선택지는 안 보임)이고, 쓰기 권한이 있으면 동작 열에 3차 링크 「수정」(`aria-label` 「{이름} 수정」)이 있다. 폼이 열리면 필터 줄 「화면 항목 추가」는 숨는다(UI-SPEC 화면 1, E1 overflow)"
  prohibitions:
    - statement: "선택지를 지우는 조작이나 늦은 저장이 저장된 선택지 문자열을 없애서는 안 된다. 거래처 행에 그 문자열이 값으로 들어 있으면 그 값의 뜻이 사라진다 — 삭제는 보관이고, 버전이 다른 저장은 거부되며, 같은 버전의 저장도 `options ∪ archived_options`에서 문자열을 빼지 못한다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/custom-fields/options.ts"
      provides: "선택지 편집 순수 함수(추가·삭제·복원·보관 파생) — 클라이언트 폼과 서버가 같이 쓴다"
      exports: ["addOption", "removeOption", "deriveArchivedOptions", "normalizeOption"]
    - path: "domain/custom-fields/admin.ts"
      provides: "칸 수정(보관 → 버전 → 이름 예약 → 보관 선택지 파생 → 버전 조건부 UPDATE 한 문장)"
      exports: ["updateFieldDefinition", "FieldDefinitionConflictError", "FieldDefinitionArchivedError", "FieldDefinitionNotFoundError"]
    - path: "repositories/field-definitions.ts"
      provides: "버전 조건부 갱신(`type` 없음, `version + 1`, 보관 행 제외) — 기존 `updateFieldDefinition`은 그대로"
      contains: "updateFieldDefinitionIfVersion"
  key_links:
    - from: "app/(app)/admin/field-definitions/field-definition-form.tsx"
      to: "domain/custom-fields/options.ts"
      via: "폼의 선택지 상태 전이가 순수 함수를 부른다 — 서버의 파생과 같은 규칙"
      pattern: "addOption|removeOption"
    - from: "domain/custom-fields/admin.ts"
      to: "repositories/field-definitions.ts"
      via: "읽은 행(같은 버전)으로 `deriveArchivedOptions` → `updateFieldDefinitionIfVersion`"
      pattern: "updateFieldDefinitionIfVersion"
    - from: "app/(app)/admin/field-definitions/page.tsx"
      to: "app/(app)/admin/field-definitions/field-definition-form.tsx"
      via: "수정 폼 `key={id}:{version}` — 새로 불러오기가 최신 버전으로 다시 마운트"
      pattern: "version"
---

<objective>
관리자가 선택형 칸을 만들고, 이미 있는 칸의 이름·필수·정렬 순서·선택지를 고친다. 타입은 바꿀 수 없고(새 칸을 만든다), 선택지를 지우면 보관된다. 오래 열린 폼은 다른 사람의 저장을 되돌리지 못한다(버전 조건부 갱신) — 저장된 선택지 문자열은 어떤 저장 경로로도 사라지지 않는다.

Purpose: ROADMAP 04.5 기준 1(수정·타입 불변)과 기준 2의 선택지 보관 절반. 05(거래처 저장이 보관 선택지 값을 통과시킴)·06(거래처 폼의 「(보관됨)」 표시)이 이 플랜의 `archived_options`를, 04(보관·복원)가 `version`을 읽는다.

Output: `domain/custom-fields/options.ts` · 수정 domain·액션(버전 조건부) · 수정 모드 폼(새로 불러오기·목록으로) · 선택지 편집기 · 테스트.

**실행 규율:** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED를 실제로 확인), 실패를 쫓기 전 `systematic-debugging`, 커밋과 「완료」 전 `verification-before-completion`을 호출한다. 서브에이전트 위임 시 세 스킬과 `model`을 적는다. 한 세션에 플랜 하나 — Task 1에서 읽은 파일은 Task 2·3에서 다시 읽지 않는다. E2E 판정은 `CI=true`. 화면 DOM 감사는 07이 별도 에이전트로 한 번에 한다. 새 의존성 0개. 병렬 페이즈 규칙은 01과 같다 — 이 플랜은 공유 파일을 건드리지 않는다.

**공유 테스트 환경:** 같은 wave(3)의 03과 공유 `erp_test` DB·E2E 포트 3100·`next build` 디렉터리를 같이 쓴다. 그래서 통합 테스트·E2E 명령은 모두 `flock /tmp/plant8-erp-test.lock `을 앞에 붙여 한 번에 하나씩 돈다(단위·lint·typecheck는 잠금 없음). 이 플랜은 따로 `pnpm build`를 돌리지 않는다. `CI=true` E2E의 webServer가 `pnpm build && pnpm start`(`playwright.config.ts` 52행)로 같은 잠금 안에서 프로덕션 빌드를 하므로, 빌드 오류는 E2E 명령에서 드러난다(예상 비용 1.1배 경고에 따른 손질 — 범위는 그대로). `pnpm db:dev`는 로컬 Postgres를 띄울 뿐 DB를 다시 만들지 않는다. 통합 DB는 vitest `test/integration/global-setup.ts`·`setup.ts`가, E2E DB는 `test/e2e/global-setup.ts`(14–46행: 스키마 비움 → 마이그레이션 → `seedMasterData`)가 준비한다. 테스트가 자기 픽스처를 넣는다.

**OPEN — /plan-design-review(이 플랜이 승인된 UI-SPEC 개정 3에서 벗어나거나 비어 있는 곳을 메운 자리):**
- **2-A** 보관 선택지 `summary`의 브라우저 기본 펼침 표시(삼각형)를 남길지 — 원문(네이티브 `details`)대로 그대로 둔다(Task 3)
- **2-B** 새 서버 문구 「화면 항목 없음」 — 수정 대상이 없거나 대상 상수 밖일 때의 원인이다. UI-SPEC Copywriting에 없는 문구다(Task 2)
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-custom-field-admin/04.5-CONTEXT.md
@.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-08-SUMMARY.md
@CLAUDE.md
# 범위 Read(UI-SPEC 개정 3, 75ae245 기준):
# .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md 「화면 2」 239–265행(262행 버전 조건부 갱신) · Copywriting 124–150행(138·139행 충돌 문구) · E2 concurrency·stale 줄(177–178행) · Assumptions O3·O11·O12·O20·O22
# .planning/phases/04.5-custom-field-admin/04.5-RESEARCH.md Pitfall 2(보관 선택지 vs z.enum)
# docs/design/SYSTEM.md §7-15(폼) · §7-5(StatusTag 어휘)
</context>

<probe_fallback>
⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). 수용 기준은 ROADMAP 04.5 기준 1·2, UI-SPEC(개정 3) 화면 2와 `## UI Considerations`의 E2 partial·overflow·long-text·concurrency·stale·name-reserved·success(수정)에서 끌어왔다.

**계획 단계 결정(표면화):**

- **버전 조건부 갱신(O20):** 트랜잭션·행 잠금을 쓰지 않는다 — 조건부 UPDATE 한 문장이 원자적이고, 파생에 쓴 저장값과 쓰는 행의 버전이 같다는 것을 `WHERE version = ?`가 보장한다. 판정 순서는 행 없음·대상 밖 → **보관** → 버전 → 이름 예약(보관을 버전보다 먼저 봐야 보관 문구에 닿는다 — 04의 보관이 버전을 올리므로, UI checker R1).
- **새 서버 문구 하나:** 수정 대상이 없거나 대상 상수 밖이면 원인이 「화면 항목 없음」이다(명사형 — 「권한 없음」과 같은 결). UI-SPEC Copywriting에 없는 문구라 SUMMARY 「새 문구」 표에 남기고 디자인 리뷰에 올린다. 보관·버전 충돌은 UI-SPEC 원문(138·139행)을 쓴다.
- **기존 `updateFieldDefinition`(repository):** 바꾸지 않는다 — 새 조건부 함수 `updateFieldDefinitionIfVersion`을 옆에 더한다(Phase 3 규약 주석·기존 호출자 보존).
- **입력 스키마 위치:** 01의 `domain/custom-fields/admin-input.ts`에 수정 스키마·충돌 원인 상수를 더한다 — 단위 테스트가 `type`·`entity` 거부를 직접 단언한다.
</probe_fallback>

<reversibility_notes>

- 버전 조건부 갱신 규칙 — **reversible**: domain 함수와 리포지토리 함수 한 쌍이다(`version` 열 자체는 01의 마이그레이션).
- 보관 선택지 파생 규칙(합집합 − 제출) — **reversible**: 서버 함수 한 곳이다.
- 수정 입력 스키마 모양 — **reversible**: 액션 하나의 계약이다.

</reversibility_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 선택형 칸 등록 — 선택지 순수 함수 · 편집기 · 서버 재판정 · 거래처 폼 반영</name>
  <files>domain/custom-fields/options.ts, domain/custom-fields/admin-input.ts, domain/custom-fields/admin.ts, app/(app)/admin/field-definitions/field-definition-form.tsx, app/(app)/admin/field-definitions/field-definitions.module.css, app/(app)/admin/field-definitions/page.tsx, test/unit/domain/custom-fields-admin.test.ts, test/integration/field-definitions-admin.test.ts, test/e2e/field-definitions.spec.ts</files>
  <read_first>
    - `.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md`·`04.5-08-SUMMARY.md` — 01·08이 만든 심볼·파일 이름(이름 예약·폼 전체 오류·「보관함에서 복원」 링크 권한은 08). 아래 이름과 다르면 SUMMARY를 따른다
    - `domain/custom-fields/targets.ts`(상한 상수) · `domain/custom-fields/admin-input.ts` · `domain/custom-fields/admin.ts`의 `createFieldDefinition`
    - `app/(app)/admin/field-definitions/field-definition-form.tsx`(전문) · `page.tsx`(목록 선택지 열)
    - `domain/custom-fields/build-schema.ts`(전문 — `z.enum(options)`)
    - `ui/status-tag/StatusTag.tsx` · `ui/button/Button.tsx`(`disabledReason`)
    - UI-SPEC 「화면 2」 도해 243–255행 · 선택지 상태 259행 · Copywriting 133–136행
  </read_first>
  <behavior>
    - (단위) `addOption({ active: ["기본"], archived: [] }, "  특약 ")` → 활성 `["기본", "특약"]`; `"기본"` → 오류 `duplicate`; `"   "` → 오류 `empty`; 활성 30개면 오류 `limit`
    - (단위) `addOption({ active: ["기본"], archived: ["특약"] }, "특약")` → 활성 `["기본", "특약"]`, 보관 `[]`(복원)
    - (단위) `removeOption(state, "특약", saved = {"기본","특약"})` → 보관으로 이동; 저장 안 한 `"MOU"` 삭제 → 목록에서 빠짐
    - (단위) `deriveArchivedOptions({ storedActive: ["a","b","c"], storedArchived: ["x"], submittedActive: ["a","d"] })` → `["b","c","x"]`(저장 순서 유지, 중복 없음, 제출에 없는 것만)
    - (단위) `createFieldDefinitionInput`이 `type: "select"`에 선택지 0개·31개·41자·공백만·중복을 거부하고, 텍스트 타입에 선택지를 실으면 거부한다
    - (통합) 선택형 생성 → 행의 `options`가 활성 배열, `archived_options`가 `[]`
    - (E2E) 타입 「선택」 → 「기본」·「특약」 추가(두 번째는 Enter) → 빈칸 추가 시 문구 → 저장 → 거래처 폼(`/admin/vendors?new=1`)의 그 칸 select에 「기본」·「특약」이 있다 → 목록 선택지 열이 「기본, 특약」
  </behavior>
  <action>
① RED 먼저(behavior 전부).

② `domain/custom-fields/options.ts`(순수 — `targets.ts`만 import): `normalizeOption(raw)`(앞뒤 공백 자르기), `addOption(state, raw)` → `{ state, error? }`(`empty` · `duplicate` · `limit` — 보관과 같은 문자열이면 보관에서 빼 활성 끝에 더함), `removeOption(state, value, savedOptions)`(저장된 것이면 보관으로, 아니면 목록에서 뺌), `deriveArchivedOptions({ storedActive, storedArchived, submittedActive })`. 오류 코드 → 문구 대응은 폼이 한다(Copywriting 원문).

③ `admin-input.ts`의 생성 스키마: 타입 enum에 `select`, `options`(문자열 배열 — 각 원소 자르기, 1~`OPTION_MAX_LENGTH`자, 중복 없음, 최대 `ACTIVE_OPTIONS_MAX`개). 선택형이면 1개 이상, 아니면 비어 있어야 한다(빈 배열 또는 없음). `createFieldDefinition`이 `options`를 저장한다(비선택형은 null, `archived_options`는 열 기본 `[]`).

④ 폼: 타입 값·활성 선택지·보관 선택지를 React 상태로 두고 상태 전이는 `options.ts` 함수로만 한다. 타입이 선택일 때만 구분선 + 선택지 목록(행마다 선택지 텍스트 + 3차 「삭제」, `aria-label` 「{선택지} 삭제」) + 새 선택지 `TextField`(short, `maxLength` 40, 오류는 이 칸의 `error`) + 3차 「선택지 추가」. 새 선택지 입력 안의 Enter는 폼 제출을 막고 추가한다. 추가·삭제 뒤 포커스는 새 선택지 입력으로. 활성 30개면 「선택지 추가」 `disabled` + `disabledReason` 「선택지는 30개까지 · 쓰지 않는 선택지 삭제」. 타입 선택 + 활성 0개면 1차 `disabled` + `disabledReason` 「추가할 수 없음 — 선택지 0개 · 선택지 추가」. 제출 때 활성 배열만 보낸다. 새 CSS는 토큰만.

⑤ `page.tsx` 목록 선택지 열: 타입이 선택이면 활성 선택지를 「, 」로 이은 텍스트, 아니면 「—」.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/domain/custom-fields-admin.test.ts &amp;&amp; pnpm db:dev &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts</automated>
    <fails_when>단위·통합 failed 개수가 1 이상이다 — 선택지 전이·파생·입력 스키마·선택형 저장 중 하나가 깨졌다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock env CI=true pnpm playwright test test/e2e/field-definitions.spec.ts</automated>
    <fails_when>webServer의 `pnpm build`가 실패하거나 playwright failed 개수가 1 이상이다 — 빌드 오류, 선택지 편집기·거래처 폼 select 반영·목록 선택지 열 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 폼의 선택지 상태 변경이 `options.ts` 함수 호출로만 일어난다(폼 파일에 배열 필터·스플라이스로 선택지를 직접 고치는 코드가 없다)
    - 선택지 편집기 문구가 Copywriting 원문 넷과 같다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>관리자가 선택형 칸을 만들면 거래처 폼에 그 선택지로 나온다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 수정 모드 — `?editId=` 폼(타입 읽기 전용 + 「새 화면 항목 추가」, 숨은 `version`) · 버전 조건부 수정(보관 먼저, 그다음 버전) · 충돌 두 문구와 「새로 불러오기」·「목록으로」 · 결과 줄 · 목록 「수정」</name>
  <files>domain/custom-fields/admin-input.ts, domain/custom-fields/admin.ts, repositories/field-definitions.ts, app/(app)/admin/field-definitions/actions.ts, app/(app)/admin/field-definitions/actions.registry.ts, app/(app)/admin/field-definitions/page.tsx, app/(app)/admin/field-definitions/field-definition-form.tsx, test/unit/domain/custom-fields-admin.test.ts, test/integration/field-definitions-admin.test.ts, test/e2e/field-definitions.spec.ts</files>
  <read_first>
    - `repositories/field-definitions.ts`(전문) — `updateFieldDefinition`이 `type`을 받지 않는 규약 주석
    - `app/(app)/admin/vendors/page.tsx` 44–75행(`editId` 조건 — 보관·없는 id면 폼 없음) · `app/(app)/admin/vendors/actions.ts`의 수정 액션
    - `lib/actions/form-reason.ts`(08) · `domain/custom-fields/admin.ts`의 이름 예약 부분(01·08 — `findFieldDefinitionByLabel`·`DuplicateFieldNameError`). `admin.ts`·`admin-input.ts`·폼·page는 Task 1에서 이미 읽었다 — 다시 읽지 않는다
    - UI-SPEC 「화면 2」 245행(수정 모드 타입) · 258행 · 262행(버전 조건부 갱신 전문) · 264행(수정 성공) · Copywriting 138·139행
  </read_first>
  <behavior>
    - (단위) `updateFieldDefinitionInput`이 `type`·`entity` 키가 실린 입력을 거부하고 `{ id, version, name, required, sortOrder, options? }`만 받는다
    - (통합) 이름을 바꾸면 `key`·`type`·그 칸의 노출 행이 그대로이고 `label`만 바뀌며 `version`이 1 오른다 · 로그 `document_update` 한 줄
    - (통합) 같은 `version`으로 두 번 저장하면 두 번째가 `FieldDefinitionConflictError`(원인 「다른 사람이 먼저 고쳤습니다 · 새로 불러오기」)로 거부되고 행이 첫 저장 그대로다 · 새 버전으로 이어서 저장하면 통과한다
    - (통합) 폼을 연 뒤(`version` = v) `archived_at`을 채우고 `version`을 올리면(04의 보관과 같은 효과) 옛 `v`로 저장할 때 **보관 문구**(「보관된 화면 항목입니다 · 목록으로」)로 거부된다 — 버전 문구가 아니다
    - (통합) 이름 중복 판정이 자기 자신을 빼고 본다(같은 이름 그대로 저장 가능) · 다른 활성 칸 이름이면 활성 문구, 보관 칸 이름이면 보관 문구
    - (통합) 없는 id·리포지토리로 직접 만든 `entity = "project"` 정의의 수정이 「화면 항목 없음」으로 거부되고 행이 바뀌지 않는다 · 기획 PM의 수정은 `ForbiddenError`
    - (E2E) 목록 「수정」(`aria-label` 「{이름} 수정」) → 수정 폼에 값이 채워져 있고 타입 칸이 select가 아니라 텍스트이며 「새 화면 항목 추가」가 `?new=1`로 간다 → 이름을 바꿔 「화면 항목 수정」 → 결과 줄 「화면 항목 수정 · {새 이름} 수정됨」 → 「닫기」로 목록 → 거래처 폼 라벨이 새 이름
    - (E2E) 같은 칸의 수정 폼을 두 탭(페이지)으로 열어 한쪽이 저장한 뒤 다른 쪽이 저장하면 이유 자리에 「수정할 수 없음 — 다른 사람이 먼저 고쳤습니다 · 」 + 3차 「새로 불러오기」 → 누르면 이름 칸이 먼저 저장된 값이고 포커스가 이름 입력에 있다
    - (E2E) `?editId=없는-id`로 오면 폼 없이 목록만 보인다
  </behavior>
  <action>
① RED 먼저.

② `admin-input.ts`에 `updateFieldDefinitionInput`(`.strict()` — `id`, `version`(양의 정수), 이름·정렬·필수는 생성과 같은 규칙과 문구, 선택지는 생성과 같은 원소 규칙). `type`·`entity` 키를 두지 않는다. 충돌 원인 상수 둘(UI-SPEC 138·139행의 「— 」 뒤 원문: 「다른 사람이 먼저 고쳤습니다 · 새로 불러오기」, 「보관된 화면 항목입니다 · 목록으로」)을 내보낸다 — 폼이 `serverError`를 이 상수와 비교해 끝의 다음 한 수를 3차 버튼으로 그린다.

③ `repositories/field-definitions.ts`: 새 함수 `updateFieldDefinitionIfVersion(viewer, id, expectedVersion, patch: { label, required, sortOrder, options?, archivedOptions? }, tx?)` — `SET …, version = version + 1, updated_at = now() WHERE id = ? AND version = expectedVersion AND archived_at IS NULL` 한 문장, 바뀐 행이 있었는지를 돌려준다. `type`은 받지 않는다. 기존 `updateFieldDefinition`·`listFieldDefinitions`는 바꾸지 않는다.

④ `domain/custom-fields/admin.ts`의 `updateFieldDefinition(viewer, input, deps?)`: `can(… "write")` 아니면 「권한 없음」 `ForbiddenError` → 행 읽기 → 없거나 `entity`가 `FIELD_DEFINITION_TARGETS` 밖이면 `FieldDefinitionNotFoundError`(UserFacingError, 「화면 항목 없음」) → **보관이면** `FieldDefinitionArchivedError`(보관 원인 상수) → 제출 `version`이 다르면 `FieldDefinitionConflictError`(충돌 원인 상수) → 이름 예약(01·08의 `findFieldDefinitionByLabel`, 자기 제외, 01·08의 `DuplicateFieldNameError`) → 선택형이 아닌 칸에 선택지가 오면 거부 → `updateFieldDefinitionIfVersion`(이 태스크에서는 선택지를 제출 그대로, 보관 파생은 Task 3) → 0행이면 다시 읽어 보관이면 보관 오류, 아니면 충돌 오류 → unique `(entity, label)` 위반은 01·08과 같이 이름 오류로 → 성공이면 `recordAction` `document_update`(entity `field_definitions`, detail 키).

⑤ `actions.ts`: `updateFieldDefinitionAction`(01·08의 생성 액션과 같은 모양 — 이름 충돌은 `returnValidationErrors`로 `name` 칸, 같은 세 경로 `revalidatePath`). `actions.registry.ts`에 등록 한 줄.

⑥ `page.tsx`: `editId`가 활성 거래처 정의 id이고 쓰기 권한이 있을 때만 수정 폼을 `key={id}:{version}`로 렌더하고, 폼이 열려 있으면(`?new=1` 또는 수정) 필터 줄 「화면 항목 추가」를 렌더하지 않는다. 동작 열: 쓰기 권한이면 3차 링크 「수정」(`?editId=<id>`, `aria-label` 「{이름} 수정」). 폼 수정 모드: 숨은 `version`, 타입 자리에 타입 이름 텍스트 + 3차 링크 「새 화면 항목 추가」(`?new=1`, 설명 문구 없음 — §8 규칙 5), 1차 「화면 항목 수정」, 성공 결과 줄 「화면 항목 수정 · {이름} 수정됨」 + 2차 「닫기」. 이유 자리: `serverError`가 충돌 원인 상수면 「수정할 수 없음 — 다른 사람이 먼저 고쳤습니다 · 」 + 3차 「새로 불러오기」(`router.refresh()` — 새 `version`의 `key`로 다시 마운트되고 이름 입력으로 포커스), 보관 원인 상수면 「수정할 수 없음 — 보관된 화면 항목입니다 · 」 + 3차 「목록으로」(`router.replace` 목록), 그 밖은 `formReason("수정", serverError)` 글자(08 OPEN 1-A의 결정이 두 모드에 같이 적용된다). 선택형 활성 0개 이유 「수정할 수 없음 — 선택지 0개 · 선택지 추가」.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/domain/custom-fields-admin.test.ts &amp;&amp; pnpm db:dev &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts test/integration/custom-fields.test.ts</automated>
    <fails_when>단위·통합 failed 개수가 1 이상이다 — 수정 스키마의 `type` 거부, 키 불변·버전 증가, 같은 버전 두 번째 거부, 보관 먼저 판정, 이름 예약, 대상 밖·권한 거부, 로그 중 하나가 깨졌다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock env CI=true pnpm playwright test test/e2e/field-definitions.spec.ts</automated>
    <fails_when>webServer의 `pnpm build`가 실패하거나 playwright failed 개수가 1 이상이다 — 빌드 오류, 수정 폼·타입 텍스트·결과 줄·충돌 문구와 새로 불러오기·목록 「수정」·없는 editId 처리 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `updateFieldDefinitionIfVersion`의 WHERE가 `id`·`version`·`archived_at IS NULL` 셋이고 SET에 `version + 1`이 있다(SUMMARY에 해당 줄 인용)
    - domain 판정 순서가 없음/대상 밖 → 보관 → 버전이다(보관 문구 통합 테스트가 증명)
    - 기존 `repositories/field-definitions.ts`의 `updateFieldDefinition` 본문 diff가 비어 있고, 새 함수 입력에도 `type`이 없다
    - 새 서버 문구 「화면 항목 없음」이 SUMMARY 「새 문구」 표에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>관리자가 칸의 이름·필수·정렬을 고칠 수 있고, 타입은 바꿀 수 없으며, 먼저 저장된 다른 사람의 변경을 오래 열린 폼이 되돌리지 못한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 수정 모드 선택지 — 저장된 선택지 「삭제」 = 보관 · 「보관된 선택지 N개」 펼침(OPEN 2-A) · 같은 버전 저장값으로 보관 파생 → 같은 조건부 UPDATE</name>
  <files>domain/custom-fields/admin.ts, app/(app)/admin/field-definitions/field-definition-form.tsx, app/(app)/admin/field-definitions/field-definitions.module.css, test/integration/field-definitions-admin.test.ts, test/e2e/field-definitions.spec.ts</files>
  <read_first>
    - UI-SPEC 「화면 2」 도해 248–253행 · 259–262행 · Assumptions O3·O11·O12·O20
    - `options.ts`(Task 1)·`updateFieldDefinition`·`updateFieldDefinitionIfVersion`(Task 2)은 같은 세션에서 이 플랜이 쓴 코드다 — 다시 읽지 않는다
  </read_first>
  <behavior>
    - (통합) 저장된 `["기본","특약"]`에서 `["기본","MOU"]`를 제출하면 `options = ["기본","MOU"]`, `archived_options = ["특약"]`
    - (통합) 다시 `["기본","MOU","특약"]`을 제출하면 `특약`이 활성으로 돌아오고 `archived_options = []`
    - (통합, 오래 열린 폼) 관리자 A·B가 같은 버전 v의 `["a","b","c"]` 화면을 연 뒤 A가 `["a","b"]`(c 보관)를 저장하고 B가 v로 `["a","b","c","d"]`를 저장하면 B는 충돌로 거부되고 행은 A의 결과 그대로(`c`는 보관된 채)다
    - (검사) `archived_options`를 쓰는 domain 경로가 `updateFieldDefinition` 한 곳이다 — 필드 전체 보관·복원(04)은 선택지 상태를 바꾸지 않는다
    - (E2E) 수정 폼에서 저장된 「특약」 「삭제」 → 「보관된 선택지 1개」가 나오고 펼치면 「특약」 + 「보관됨」, 버튼 없음 → 저장 → 거래처 새 폼 select에 「특약」이 없다 → 다시 수정 폼에서 「특약」을 새 선택지로 추가하면 활성 목록으로 돌아오고 보관 펼침이 사라진다
  </behavior>
  <action>
① RED 먼저.

② `updateFieldDefinition`의 선택형 경로: 버전 확인을 통과한 **그 읽은 행**의 `options`·`archived_options`로 `deriveArchivedOptions({ storedActive, storedArchived, submittedActive })`를 계산해 `updateFieldDefinitionIfVersion`의 `options: 제출된 활성`, `archivedOptions: 파생값`으로 같은 한 문장에 쓴다(트랜잭션·행 잠금 없음 — `WHERE version`이 파생의 전제를 지킨다). 비선택형 칸은 선택지 열을 건드리지 않는다.

③ 폼 수정 모드: 저장된 활성 선택지와 보관 선택지를 초기 상태로 받는다(page가 DTO의 `options`·`archivedOptions`를 넘김). 저장된 선택지(초기 활성 ∪ 초기 보관)는 `removeOption`의 `savedOptions`다 — 「삭제」가 보관으로 옮긴다. 보관 목록은 네이티브 `details`(기본 접힘) + `summary` 「보관된 선택지 {N}개」(3차 버튼과 같은 글자 모양) 안에 선택지마다 텍스트 + StatusTag 「보관됨」(`kind="muted"`, `variant="text"`), 버튼 없음. N = 0이면 `details`를 렌더하지 않는다. 제출은 활성 배열만 보낸다(서버가 보관을 파생). **OPEN — /plan-design-review (2-A):** `summary`의 브라우저 기본 펼침 표시(삼각형)를 남길지 — UI-SPEC 원문(네이티브 `details`)대로 기본 표시를 **그대로 두고**, 숨기거나 새 아이콘을 넣지 않는다. 결정이 오면 CSS 한 곳만 바꾼다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다</fails_when>
    <automated>pnpm db:dev &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts</automated>
    <fails_when>통합 failed 개수가 1 이상이다 — 보관 파생·재추가 복원·오래 열린 폼 거부 중 하나가 깨졌다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock env CI=true pnpm playwright test test/e2e/field-definitions.spec.ts</automated>
    <fails_when>webServer의 `pnpm build`가 실패하거나 playwright failed 개수가 1 이상이다 — 빌드 오류, 선택지 삭제 = 보관, 보관 펼침, 거래처 폼에서 보관 선택지 숨김, 재추가 복원 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `archived_options`를 쓰는 domain 경로가 `updateFieldDefinition` 한 곳이다(`grep -rn "archivedOptions:" domain/`로 확인해 SUMMARY에 인용)
    - 파생 입력이 버전 확인을 통과한 읽은 행이고, 쓰기는 같은 버전 조건의 한 문장이다
    - OPEN 2-A가 SUMMARY 「디자인 리뷰 대기」 목록에 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>관리자가 선택지를 지우면 보관되고, 같은 이름을 다시 적으면 돌아오며, 오래 열린 폼이 다른 사람의 선택지 보관을 되돌리지 못한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `updateFieldDefinitionAction` | 칸 id·버전·이름·선택지 배열이 들어온다 — id·버전은 위조될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-01 | Elevation of Privilege | `updateFieldDefinition` | high | mitigate | 매 호출 `can(viewer, "admin.field-definitions", "write")` — 기획 PM 거부 통합 테스트(Task 2) |
| T-04.5-02 | Tampering | 수정 입력(타입·대상·다른 대상의 id) | high | mitigate | 스키마 `.strict()`에 `type`·`entity` 없음(단위 테스트), 리포지토리 갱신이 `type`을 받지 않음, 대상 행의 `entity`가 `FIELD_DEFINITION_TARGETS` 밖이면 「화면 항목 없음」(프로젝트 정의 id 위조 통합 테스트) |
| T-04.5-11 | Denial of Service | 선택지 배열 | low | mitigate | 서버 스키마가 활성 30개 · 각 40자 상한을 다시 판정한다(클라이언트 비활성과 별개) |
| T-04.5-12 | Tampering | 선택지·이름 저장(겹친·늦은 저장) | medium | mitigate | 버전 조건부 UPDATE 한 문장 — 다른 버전의 저장은 거부, 같은 버전의 저장은 읽은 행에서 `archived_options`를 파생해 저장된 선택지를 지울 수 없다(오래 열린 폼 통합·E2E). 위조 버전은 거부될 뿐 다른 행을 바꾸지 못한다 |
| T-04.5-15 | Tampering | 이름 변경으로 같은 이름 두 칸 | medium | mitigate | 01·08의 이름 예약(조회 + unique `(entity, label)` 위반 매핑)을 자기 제외로 그대로 쓴다 |
| T-04.5-08 | Repudiation | 칸 수정 | low | mitigate | 성공한 수정마다 `document_update` 한 줄(통합 테스트) |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — 의존성 객체·lockfile diff 없음 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과(프로덕션 빌드는 아래 `CI=true` E2E의 webServer가 한다)
- 단위 `custom-fields-admin` · 통합 `field-definitions-admin`·`custom-fields` 통과(통합은 `flock /tmp/plant8-erp-test.lock ` 앞에 붙임)
- `flock /tmp/plant8-erp-test.lock env CI=true pnpm playwright test test/e2e/field-definitions.spec.ts` 통과(빌드 포함)
- OPEN 2-A·2-B가 SUMMARY 「디자인 리뷰 대기」에 있다
- `package.json` 의존성 객체·`pnpm-lock.yaml` 변경 없음
</verification>

<success_criteria>
- 선택형 칸을 만들고, 모든 칸의 이름·필수·정렬·선택지를 고칠 수 있으며 타입은 바꿀 수 없다
- 선택지 삭제는 보관이고, 오래 열린 폼이 먼저 저장된 변경을 되돌리지 못하며, 저장된 선택지가 어떤 경로로도 사라지지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/custom-fields/options.ts`(`addOption`·`removeOption`·`deriveArchivedOptions`·`normalizeOption`) |
| 함수(신규) | `updateFieldDefinition`(domain) · `updateFieldDefinitionIfVersion`(repository) · `updateFieldDefinitionInput`(스키마) · 충돌 원인 상수 둘 · `FieldDefinitionConflictError`·`FieldDefinitionArchivedError`·`FieldDefinitionNotFoundError` |
| 변경된 기존 심볼 | `createFieldDefinitionInput`(+`select`·`options`) — 기존 repository `updateFieldDefinition`은 그대로 |
| 액션(신규) | `updateFieldDefinitionAction`(registry 등록) |
| 화면 | 수정 모드(`?editId=`, 숨은 `version`, 새로 불러오기·목록으로) · 선택지 편집기 · 보관 선택지 펼침 · 목록 선택지 열·「수정」 |
| 새 문구 | 「화면 항목 없음」(디자인 리뷰 대기) |
| 운영 안내 | 타입을 바꾸며 이름을 지키는 순서(옛 칸 이름 바꾸기 → 보관 → 새 칸) — SUMMARY |

<output>
Create `.planning/phases/04.5-custom-field-admin/04.5-02-SUMMARY.md` when done
</output>
