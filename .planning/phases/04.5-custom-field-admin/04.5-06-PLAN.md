---
phase: 04.5-custom-field-admin
plan: 06
type: execute
wave: 4
depends_on: ["04.5-05", "04.5-02"]
files_modified:
  - app/(app)/admin/vendors/vendor-form.tsx
  - app/(app)/admin/vendors/vendors.module.css
  - lib/actions/form-reason.ts
  - test/unit/actions/form-reason.test.ts
  - test/unit/ui/vendor-form-reason.test.ts
  - test/e2e/vendor-custom-fields.spec.ts
  - test/e2e/fixtures.ts
autonomous: true
requirements: []

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "거래처 폼의 커스텀 칸 오류는 칸 아래에 보인다 — 텍스트·숫자·날짜는 `TextField`의 `error`, 네이티브 선택형은 로컬 `<p id=\"cf_{key}-error\">`(`--fs-sm --danger`)와 `aria-invalid`·`aria-describedby`(`ui/select`와 같은 배선). 오류 뒤에도 입력값이 남는다(UI-SPEC 화면 3 변경 4·6, E4 error)"
    - "1차 「거래처 등록/수정」 바로 오른쪽, 「취소」 왼쪽에 이유 자리 `<span id=\"vendor-form-reason\">`(`--fs-sm --danger`) 하나가 있고 1차의 `aria-describedby`가 그 id를 가리킨다(`role=\"alert\"` 아님). 칸 오류가 있으면 「{등록|수정}할 수 없음 — {실패 칸 이름들} {N}칸 · 」 뒤에 3차 「{첫 칸 이름} 고치기」(누르면 그 칸으로 포커스)가 온다(UI-SPEC 화면 3 변경 5, Copywriting 147행)"
    - "폼 전체 서버 오류는 폼 상단 상자가 아니라 같은 이유 자리에 「{등록|수정}할 수 없음 — {serverError 원문} · 다시 시도」로 보이고(원문에 「 · 」가 있으면 「 · 다시 시도」를 붙이지 않는다), 3차 버튼이 없다. 칸 오류와 서버 오류가 함께 오면 칸 오류 요약을 보인다. 한 번에 한 줄이다. 폼 상단 오류 상자 컴포넌트와 그 import가 이 파일에서 사라진다(UI-SPEC O23, SYSTEM.md 993행)"
    - "`<form>`에 `noValidate`가 있고 커스텀 칸에는 `required` 속성이 없다 — 브라우저 말풍선이 서버 판정(「필수 판정」 표)보다 먼저 제출을 막지 않는다. 기본 칸 「이름」의 `required`는 그대로다(UI-SPEC 화면 3 변경 1, R8)"
    - "필수 칸이 저장값 빈 상태에서 다른 칸만 고친 수정 저장이 텍스트·숫자·날짜·선택 네 타입 모두 성공한다. 등록 때 필수 칸을 비우면 막힘 문구와 이유 자리 요약이 보인다. 채워진 필수 칸을 지우면 막힌다(UI-SPEC 화면 3 E2E 필수 범위, ROADMAP 04.5 기준 2)"
    - "저장값이 활성 선택지에 없으면 그 값이 `{값} (보관됨)` 옵션으로 **활성화된 채** 목록 끝에 한 개 더해지고 그 거래처 폼에만 나타난다. 그대로 저장하면 값이 남는다. 폼이 열린 사이 보관된 선택지를 고르면 그 칸 아래 「보관된 선택지입니다 · 다른 선택지 고르기」(UI-SPEC 화면 3 「보관된 선택지가 현재 값」, O2)"
    - "기본 칸과 커스텀 칸 사이에 `1px --line` 구분선과 `--s-6` 간격(텍스트 머리 없음)이 있고, 커스텀 칸이 0개면 구분선을 포함한 묶음 전체를 그리지 않는다. 라벨은 `def.label`이다(UI-SPEC 화면 3 변경 2·3, E4 empty)"
  prohibitions:
    - statement: "거래처 폼이 폼 상단 오류 상자로 서버 오류를 보여서는 안 된다 — 오류는 원인이 있는 자리(칸 아래 또는 1차 옆 이유 자리)에만 있다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/(app)/admin/vendors/vendor-form.tsx"
      provides: "칸별 오류 배선 · 이유 자리(칸 오류 요약 / 서버 오류) · noValidate · 구분선 · 보관 선택지 현재 값 옵션"
      contains: "vendor-form-reason"
    - path: "lib/actions/form-reason.ts"
      provides: "`fieldErrorsReason(verb, names)` — 칸 오류 요약 문구(순수)"
      exports: ["formReason", "fieldErrorsReason"]
    - path: "test/e2e/vendor-custom-fields.spec.ts"
      provides: "필수 판정 네 타입 · 등록 막힘 · 지우기 막힘 · 보관 선택지 유지·거부 · 서버 오류 이유 자리 — 전용 계급에게만 보이는 칸으로 다른 스펙과 격리"
      min_lines: 120
  key_links:
    - from: "app/(app)/admin/vendors/vendor-form.tsx"
      to: "app/(app)/admin/vendors/actions.ts"
      via: "`result.validationErrors?.customFields?.[key]?._errors?.[0]` → 칸 아래 오류 · 이유 자리 요약"
      pattern: "validationErrors\\?\\.customFields"
    - from: "app/(app)/admin/vendors/vendor-form.tsx"
      to: "lib/actions/form-reason.ts"
      via: "칸 오류면 `fieldErrorsReason`, 아니면 `formReason(verb, serverError)`"
      pattern: "fieldErrorsReason|formReason"
---

<objective>
거래처 폼이 커스텀 칸의 오류를 칸 아래와 1차 버튼 옆 이유 자리에 보이고, 폼 상단 오류 상자를 없앤다. 보관된 선택지가 현재 값인 거래처는 그 값을 「(보관됨)」으로 보며 그대로 저장할 수 있다. 서버 판정(05)이 이 화면에서 사람이 읽을 수 있는 모양으로 끝난다.

Purpose: ROADMAP 04.5 기준 2(필수 판정·보관 선택지)의 화면 절반과 UI-SPEC 화면 3 변경 1~6 전부. 05가 서버 계약을 만들었고, 이 플랜이 그 계약을 사람에게 보인다.

Output: `vendor-form.tsx` 변경(칸 오류 · 이유 자리 · noValidate · 구분선 · 보관 선택지 옵션 · 폼 상단 상자 제거) · `fieldErrorsReason` · 소스 검사 단위 테스트 · E2E(전용 계급 격리).

**실행 규율:** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED를 실제로 확인 → GREEN → 리팩터), 실패를 쫓기 전 `systematic-debugging`, 커밋·「완료」 전 `verification-before-completion`. 서브에이전트에 위임하면 세 스킬과 `model`을 적는다. 한 세션에 플랜 하나. E2E는 `pnpm db:dev` 뒤 `CI=true`로 판정한다. 화면 DOM 감사는 07에서 별도 에이전트가 한 번에 한다. 새 의존성 0개.

**병렬 페이즈 규칙:** `app/(app)/admin/vendors/vendor-form.tsx`는 다른 페이즈도 고칠 수 있는 공유 파일이라 **필요한 줄만** 바꾼다. 커스텀 칸 렌더·제출·오류 자리·폼 상단 상자 한 줄과 그 import만 건드리고, 계좌번호·기본 칸·`duplicateNotice`(같은 이름 거래처 안내)·`VendorHiddenToggle`·`VendorDeleteButton`은 그대로 둔다. `test/e2e/fixtures.ts`는 끝에 더하기만 한다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-02-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-05-SUMMARY.md
@CLAUDE.md
# 범위 Read(UI-SPEC 개정 3, 75ae245 기준):
# .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md 「화면 3」 267–301행 전부 · Copywriting 140·145–148행 · E4 줄(184–189행) · Component Inventory 53–54행(폼 상단 상자·네이티브 select) · Assumptions O2·O23·R8
# docs/design/SYSTEM.md §6-3(폼 규칙) · §7-15(화면이 그대로인 폼) · 993행(폼 상단 오류 상자 금지)
</context>

<probe_fallback>
⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). 수용 기준은 ROADMAP 04.5 기준 2, UI-SPEC(개정 3) 화면 3 변경 1~6·「필수 판정」·「보관된 선택지가 현재 값」·E2E 필수 범위, `## UI Considerations`의 E4 empty·error·partial·stale·hidden·archived value, Assumptions O2·O23·R8에서 끌어왔다.

**계획 단계 결정(표면화):**

- **UI-SPEC 「단위(RTL)」 테스트 → 소스 검사 + E2E.** 이 저장소에는 React 렌더 테스트 러너가 없다(`test/unit/ui/next-turn-action.test.ts` 머리 주석 — `@testing-library/react` 미설치). 새 의존성은 이유 한 줄 + 승인 사항이라 이 플랜이 설치하지 않는다. 같은 세 속성을 이렇게 나눠 증명한다.
  - 소스 검사 단위 테스트: 이유 자리 id, 1차의 `aria-describedby`, 폼 상단 상자 부재
  - `CI=true` E2E: `serverError`가 오면 `#vendor-form-reason`에 문구가 나오고, 1차 `aria-describedby`가 그 id이며, 폼 안에 `role="alert"` 상자가 없다(실제 DOM)
  - UI-SPEC 문언과의 차이를 SUMMARY 편차로 적는다. RTL 도입은 사용자 승인 뒤 별도 작업이다.
- **칸 이름들 구분자:** UI-SPEC은 「{실패 칸 이름들} {N}칸」만 정했다. 이름 사이는 쉼표 + 공백(「, 」)으로 한다. 가운뎃점은 「원인 · 다음 행동」 구분에 쓰이므로 피한다. 예: 「수정할 수 없음 — 담당자 연락처, 계약 유형 2칸 · 담당자 연락처 고치기」.
- **E2E 격리:** Playwright 설정은 `fullyParallel: false`지만 파일은 여러 워커에서 동시에 돌고 `erp_test` 하나를 나눠 쓴다(`playwright.config.ts` 「함정 6」). 이 스펙이 만든 필수 칸이 시스템 관리자에게 보이면, 동시에 도는 `vendors.spec.ts`의 등록이 막힌다. 그래서 이 스펙이 만드는 칸은 전용 E2E 계급에게만 보이게 하고 다른 계급은 모두 끈다. 05의 입력 칸 집합이 보이는 칸만이라 다른 스펙의 저장은 영향이 없다. 테스트는 그 계급의 픽스처 사용자로 로그인한다.
- **OPEN — /plan-design-review (1-B):** `noValidate`를 켜면 기본 칸 「이름」을 비운 제출이 브라우저 말풍선 대신 서버 문구 「이름을 입력하세요.」를 이름 칸 아래에 보인다. 이 문구는 §8 「원인 · 다음 행동」 모양이 아니고, 기본 칸 문구는 이 페이즈 범위 밖이다. 또 이유 자리 요약이 기본 칸(이름) 오류도 세는지 UI-SPEC이 정하지 않았다. 이 플랜의 구현은 둘 다 그대로 둔다 — 이름 문구는 바꾸지 않고, 요약은 커스텀 칸 오류만 센다(이름만 틀리면 이유 자리는 비고 이름 칸 아래에만 오류). 디자인 리뷰가 정하면 요약 대상과 문구만 바꾼다. SUMMARY 「디자인 리뷰 대기」 목록에 적는다.
</probe_fallback>

<reversibility_notes>

- 폼 상단 오류 상자 제거 → 이유 자리 — **reversible**: 한 파일의 렌더 자리 이동이다.
- 이유 자리 요약이 커스텀 칸만 센다(OPEN 1-B의 잠정 구현) — **reversible**: 요약 입력 배열 한 곳이다.

</reversibility_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 칸 오류와 이유 자리 — 칸 아래 오류(텍스트·숫자·날짜·선택) · `#vendor-form-reason`(칸 오류 요약 + 「{첫 칸} 고치기」 / 서버 오류) · 폼 상단 상자 제거 · `noValidate` · 구분선 · 0칸이면 묶음 없음 · 전용 계급 E2E</name>
  <files>app/(app)/admin/vendors/vendor-form.tsx, app/(app)/admin/vendors/vendors.module.css, lib/actions/form-reason.ts, test/unit/actions/form-reason.test.ts, test/unit/ui/vendor-form-reason.test.ts, test/e2e/vendor-custom-fields.spec.ts, test/e2e/fixtures.ts</files>
  <read_first>
    - `app/(app)/admin/vendors/vendor-form.tsx`(전문 — 05가 바꾼 `handleSubmit`, 136행 `nameError`, 188–206행 커스텀 칸·폼 상단 상자·`formActions`, 210–232행 `VendorCustomField`)
    - `app/(app)/admin/vendors/vendors.module.css`(`.formActions`·`.hint`·`.select` — 토큰만 쓰는 관례) · `docs/design/tokens.css`(`--line`·`--s-6`·`--fs-sm`·`--danger`)
    - `ui/select/Select.tsx`(오류 `<p id="{id}-error">` + `aria-invalid`·`aria-describedby` 배선 선례) · `ui/input/TextField.tsx`(`error` prop) · `ui/button` 1차 버튼이 `aria-describedby`를 넘기는 방식
    - `lib/actions/form-reason.ts`·`test/unit/actions/form-reason.test.ts`(01 — `formReason`)
    - `test/unit/ui/next-turn-action.test.ts`(소스 검사 단위 테스트 관례) · `test/e2e/fixtures.ts`(전문 — `createFixtureUser`, 01의 `archiveE2EFieldDefinitions`) · `test/e2e/custom-field-visibility.spec.ts`(03 — 계급별 노출을 끄는 E2E 방식) · `test/e2e/vendor-edit.spec.ts`(수정 폼 여는 방식)
  </read_first>
  <behavior>
    - (단위) `fieldErrorsReason("수정", ["담당자 연락처"])` = `{ text: "수정할 수 없음 — 담당자 연락처 1칸 · ", fix: "담당자 연락처 고치기" }` · 이름 둘이면 「, 」로 잇고 「2칸」, `fix`는 첫 이름
    - (소스 검사) `vendor-form.tsx`(주석 줄 제외)에 폼 상단 오류 상자 컴포넌트 이름이 0회 · `id="vendor-form-reason"`이 1회 · 1차 버튼 props에 `aria-describedby`가 `vendor-form-reason`을 가리킨다 · `<form`에 `noValidate` · 커스텀 칸에 `required={def.required}`가 없다
    - (E2E 격리 준비) 전용 계급 + 그 계급 사용자를 만든다(거래처 보기·쓰기, `vendor.value` 보임). 이 스펙의 칸은 그 계급에게만 보이고 시스템 관리자를 포함한 다른 계급에는 꺼져 있다
    - (E2E 네 타입) 거래처를 먼저 만든 뒤 필수 칸 넷(텍스트·숫자·날짜·선택)을 추가 → 그 거래처 수정 폼에서 「사업자 번호」만 고쳐 「거래처 수정」 → 성공(이유 자리 비어 있음, 목록에 새 사업자 번호)
    - (E2E 등록 막힘) 등록 폼에서 필수 텍스트 칸과 필수 선택형 칸을 비우고 「거래처 등록」 → 두 칸 아래 「필수 칸이 비어 있습니다 · 값 입력」·「필수 칸이 비어 있습니다 · 선택지 고르기」, 이유 자리 「등록할 수 없음 — {두 이름} 2칸 · 」 + 「{첫 이름} 고치기」 → 누르면 첫 칸에 포커스 · 다른 칸에 적은 값이 남아 있다 · 거래처가 생기지 않았다
    - (E2E 지우기 막힘) 필수 텍스트 칸에 값을 넣어 저장한 뒤 그 칸을 비우고 저장 → 막힘 문구와 이유 자리 요약
    - (E2E 서버 오류) 수정 폼을 연 채 픽스처로 그 거래처를 보관 → 「거래처 수정」 → `#vendor-form-reason`에 「수정할 수 없음 — {서버 원문}」(원문 규칙대로 「 · 다시 시도」), 1차 `aria-describedby`가 `vendor-form-reason`, 폼 안에 `role="alert"` 요소 0개, 3차 「… 고치기」 없음
    - (E2E 0칸) 시스템 관리자의 거래처 폼에는 이 스펙의 칸이 없다. `vendor.value`를 끈 두 번째 전용 계급(05의 입력 칸 0개) 사용자의 거래처 폼에는 구분선 요소와 커스텀 칸이 없다
  </behavior>
  <action>
① RED 먼저. 단위·소스 검사·E2E를 쓰고 빨간 것을 확인한다.

② `lib/actions/form-reason.ts` 끝에 순수 `fieldErrorsReason(verb, names)`를 더한다. 반환은 `{ text, fix }`다. 문구는 UI-SPEC Copywriting 147행 모양이고, 이름 구분자는 「, 」다(계획 단계 결정). 01의 `formReason`은 그대로 둔다. 단위 테스트는 기존 파일 끝에 더한다.

③ `vendor-form.tsx`:
- (a) `<form>`에 `noValidate`를 더한다. `VendorCustomField`의 커스텀 칸 두 곳(선택형·입력형)에서 정의의 필수 여부를 브라우저 필수 속성으로 넘기는 부분을 뺀다. 라벨이 `def.label`인지 확인한다(01에서 바꿈).
- (b) 커스텀 칸 묶음: `fieldDefs.length > 0`일 때만 구분선 요소(`vendors.module.css`의 새 클래스 — `border-top: 1px solid var(--line)`, 위 간격 `var(--s-6)`, 토큰만)와 칸들을 그린다.
- (c) 칸별 오류: `result.validationErrors?.customFields?.[def.key]?._errors?.[0]`를 `VendorCustomField`에 `error`로 넘긴다.
  - 텍스트·숫자·날짜는 `TextField`의 `error`로 넘긴다.
  - 선택형은 `ui/select/Select.tsx`와 같게 `<p id="cf_{key}-error">`(`vendors.module.css`의 오류 클래스 — `--fs-sm`·`--danger` 토큰)와 select의 `aria-invalid`·`aria-describedby`로 그린다.
- (d) 이유 자리: `.formActions` 안에서 1차 버튼과 「취소」 링크 사이에 `<span id="vendor-form-reason">`(`--fs-sm --danger` 클래스)을 둔다. 1차 버튼에 `aria-describedby="vendor-form-reason"`을 준다.
  - 칸 오류가 하나라도 있으면: `fieldDefs` 순서로 오류가 있는 칸의 `label`을 모아 `fieldErrorsReason(isEditing ? "수정" : "등록", labels)`를 부른다. `text`를 쓰고, 그 뒤 **span 안에** 3차 버튼 `fix`를 둔다. 누르면 `document.getElementById("cf_" + 첫 키)`로 포커스한다.
  - 칸 오류가 없고 `serverError`가 있으면: `formReason(verb, serverError)` 글자만 쓴다. 3차 버튼은 없다.
  - 둘 다 없으면 span은 비어 있다.
- (e) 195행의 폼 상단 오류 상자 줄과 그 import를 지운다. 이 파일에 다른 사용이 없다.
- `duplicateNotice`·기본 칸·계좌번호 줄은 그대로다.

④ `test/e2e/fixtures.ts` **끝에** 픽스처 세 개를 더한다.
- `createE2EVendorEditor({ vendorValue })`(기본 `true`)
  - 전용 계급을 만든다. 이름은 `E2E` 접두 + 난수다. domain 계급 생성 함수를 `SYSTEM_VIEWER`로 부른다.
  - 거래처 메뉴 보기·쓰기 권한 행과 `vendor.value` 노출 행(`vendorValue` 값대로)을 넣는다. 01·03의 insert-if-absent 리포지토리 함수를 쓴다.
  - `createFixtureUser({ roleId })`를 돌려준다.
- `createE2EFieldDefinition({ label, type, required, options, onlyRoleId })`
  - 01의 domain 생성 함수를 `SYSTEM_VIEWER`로 부른다.
  - 그 칸의 `cf.vendor.<key>` 노출 행을 `onlyRoleId` 밖 모든 계급에서 `visible = false`로 바꾼다.
  - 칸 정의 행(`id`·`key`·`version`)을 돌려준다.
- `archiveE2EFieldDefinitionOption(id, option)`: 02의 domain 수정 함수로 그 선택지를 뺀 `options`를 현재 `version`으로 저장한다. 서버가 보관으로 파생한다.
- 거래처 보관은 기존 domain `archive(SYSTEM_VIEWER, "vendor", id)`로 한다.

⑤ `test/e2e/vendor-custom-fields.spec.ts`(신규):
- behavior의 E2E를 전용 계급 사용자로 쓴다.
- 칸 이름은 `E2E` 접두 + 난수 꼬리(20자 이하)다.
- `afterAll`에서 01의 `archiveE2EFieldDefinitions(접두)`를 부른다.

⑥ 싼 게이트(lint·typecheck·build)를 먼저 돌리고, 그다음 E2E를 돌린다. 이 플랜 화면의 DOM 감사는 07이 별도 에이전트로 한다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm vitest run --project unit test/unit/actions/form-reason.test.ts test/unit/ui/vendor-form-reason.test.ts</automated>
    <fails_when>eslint·stylelint·tsc·next build가 error로 끝나거나 두 단위 테스트 중 failed가 1 이상이다 — 요약 문구 · 이유 자리 id · 1차 `aria-describedby` · 폼 상단 상자 부재 · `noValidate` · 커스텀 칸 `required` 부재 중 하나라도 어긋나면 빨간색이다</fails_when>
    <automated>pnpm db:dev &amp;&amp; CI=true pnpm playwright test test/e2e/vendor-custom-fields.spec.ts test/e2e/vendors.spec.ts test/e2e/vendor-edit.spec.ts test/e2e/mobile-vendors.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 네 타입 저장값 빈 필수 칸의 수정 저장이 막히거나, 등록 막힘·지우기 막힘 문구·요약·포커스가 어긋나거나, 서버 오류가 이유 자리에 없거나, 기존 거래처 E2E가 회귀하면 빨간색이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' "app/(app)/admin/vendors/vendor-form.tsx" | grep -c "FormAlert"`가 0이다
    - `grep -c 'id="vendor-form-reason"' "app/(app)/admin/vendors/vendor-form.tsx"`가 1이다
    - `git diff origin/main -- test/e2e/fixtures.ts`에 삭제 줄(`^-[^-]`)이 없다
    - `git diff origin/main -- "app/(app)/admin/vendors/vendors.module.css"`에 새 색·px 리터럴이 없다 — 더한 줄의 값이 `var(--…)` 토큰뿐이다
    - 화면 문구가 UI-SPEC Copywriting 원문과 같다(막힘 두 변형 · 칸 오류 요약 · 서버 오류 줄)
    - OPEN 1-B가 SUMMARY 「디자인 리뷰 대기」 목록에 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>거래처 폼에서 커스텀 칸 오류가 칸 아래와 1차 옆 이유 자리에 보이고, 서버 오류도 같은 자리에 보이며, 폼 상단 상자가 없고, 저장값이 빈 필수 칸 때문에 다른 칸 수정이 막히지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 보관된 선택지가 현재 값 — `{값} (보관됨)` 활성 옵션 · 그대로 저장하면 유지 · 폼이 열린 사이 보관된 선택지를 고르면 칸 오류</name>
  <files>app/(app)/admin/vendors/vendor-form.tsx, test/e2e/vendor-custom-fields.spec.ts, test/unit/ui/vendor-form-reason.test.ts</files>
  <read_first>
    - `app/(app)/admin/vendors/vendor-form.tsx` `VendorCustomField`(Task 1 뒤 모양) · `customFieldDefaultValue`(47행 부근)
    - UI-SPEC 「화면 3」 「보관된 선택지가 현재 값」 문단 · Assumptions O2(disabled 옵션을 쓰지 않는 이유)
    - `domain/custom-fields/preserve.ts`(05 — 저장값과 같은 보관 선택지 통과 규칙)
  </read_first>
  <behavior>
    - (소스 검사) 선택형 칸에서 저장값이 활성 선택지에 없을 때 더하는 옵션이 `disabled`가 아니고 글자가 「{값} (보관됨)」이다
    - (E2E 유지) 선택형 칸 「E2E계약…」의 선택지 「구형」을 거래처에 저장 → 픽스처로 「구형」을 보관 → 그 거래처 수정 폼의 선택 칸 현재 값이 「구형 (보관됨)」이고 목록 끝에 있다 → 다른 칸만 고쳐 저장 → 성공, 다시 열어도 「구형 (보관됨)」
    - (E2E 다른 거래처) 새 거래처 등록 폼과 다른 거래처 수정 폼의 그 선택 칸에는 「구형 (보관됨)」 옵션이 없다
    - (E2E 새 지정 거부) 다른 거래처 수정 폼을 연 채 픽스처로 선택지 「신형」을 보관 → 화면에 아직 남은 「신형」을 골라 저장 → 그 칸 아래 「보관된 선택지입니다 · 다른 선택지 고르기」, 이유 자리 요약, 저장값 불변
    - (E2E 선택 없음) 「구형 (보관됨)」 거래처에서 「선택 없음」을 고르면 필수 아님 칸은 지워지고, 필수 칸이면 막힘 문구
  </behavior>
  <action>
① RED 먼저.

② `VendorCustomField`의 선택형 분기를 바꾼다.
- 조건: 저장값(문자열)이 비어 있지 않고 `def.options`에 없다.
- 그러면 `<option value={값}>{값} (보관됨)</option>`을 목록 **끝에** 하나 더한다. 활성화된 채 둔다 — UI-SPEC O2에 따르면 disabled 옵션은 FormData에서 빠져 「안 바꿈」과 「지움」을 구분할 수 없다.
- `defaultValue`는 그 저장값이다. 새 거래처(저장값 없음)에는 이 옵션이 생기지 않는다.
- 판정은 05의 서버 규칙이 한다. 클라이언트는 제출 전에 막지 않는다(§7-15 — 관문은 서버 하나).

③ E2E 케이스를 `vendor-custom-fields.spec.ts`에 더한다. Task 1의 전용 계급·픽스처를 쓴다. 소스 검사 한 줄을 `vendor-form-reason.test.ts`에 더한다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm vitest run --project unit test/unit/ui/vendor-form-reason.test.ts</automated>
    <fails_when>eslint·tsc·next build가 error로 끝나거나 소스 검사 failed가 1 이상이다 — 보관 옵션이 `disabled`이거나 글자가 「{값} (보관됨)」이 아니면 빨간색이다</fails_when>
    <automated>pnpm db:dev &amp;&amp; CI=true pnpm playwright test test/e2e/vendor-custom-fields.spec.ts test/e2e/vendor-edit.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 보관 선택지 현재 값이 그대로 저장되지 않거나, 다른 거래처 폼에 「(보관됨)」 옵션이 나타나거나, 폼이 열린 사이 보관된 선택지가 저장되면 빨간색이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 「(보관됨)」 옵션이 그 거래처 폼에만 있다(E2E 두 폼 대조)
    - 문구 「보관된 선택지입니다 · 다른 선택지 고르기」가 칸 아래에 보인다(UI-SPEC 원문)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>선택지가 보관돼도 그 값을 가진 거래처는 값을 잃지 않고 그대로 저장할 수 있으며, 보관된 선택지를 새로 고르는 저장은 칸 오류로 막힌다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 → 거래처 폼 | 칸 이름(`label`)·선택지·저장값은 관리자·사용자가 적은 문자열이다 |
| 브라우저 → 거래처 액션 | 폼이 활성화된 「(보관됨)」 옵션을 보내므로 서버 판정(05)만이 관문이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-10 | Tampering | 칸 이름·선택지·보관 값 렌더 | medium | mitigate | 모두 React 텍스트 노드·`value` 속성으로만 렌더한다. HTML 문자열 삽입 API를 쓰지 않는다(소스 검사가 새 줄에서 확인) |
| T-04.5-18 | Tampering | 활성화된 「(보관됨)」 옵션으로 보관 선택지 저장 | medium | mitigate | 클라이언트는 막지 않고 05의 서버 규칙이 판정한다. 저장값과 같을 때만 통과하고 다른 보관 선택지는 칸 오류다(E2E 「새 지정 거부」) |
| T-04.5-04 | Information Disclosure | 거래처 폼의 보이지 않는 칸 | medium | mitigate | 폼은 05의 입력 칸 집합(`listVendorFieldDefinitions`)만 그린다. 전용 계급 E2E가 시스템 관리자 화면에 그 계급 전용 칸이 없음을 실측한다 |
| T-04.5-19 | Information Disclosure | 이유 자리 서버 오류 원문 | low | accept | `serverError`는 이미 `handleServerError` 허용 목록(`UserFacingError`·ZodError 한국어 줄·일반 문구)을 거친 문자열이다. 이유 자리는 자리만 옮긴다 |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — RTL을 설치하지 않고 소스 검사 + E2E로 대신한다(도입은 사용자 승인 사항). 의존성 객체·lockfile diff 없음 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` · 단위(`form-reason`·`vendor-form-reason`) 통과
- `CI=true` E2E: `vendor-custom-fields` · `vendors` · `vendor-edit` · `mobile-vendors` 통과
- `vendor-form.tsx`에 폼 상단 오류 상자 없음(주석 제외 grep 0) · 이유 자리 id 1회
- `fixtures.ts` diff가 더하기뿐
- `package.json` 의존성 객체·`pnpm-lock.yaml` 변경 없음
</verification>

<success_criteria>
- 커스텀 칸 오류가 칸 아래와 이유 자리 요약에 보이고, 「{첫 칸} 고치기」가 그 칸으로 포커스를 보낸다
- 폼 전체 서버 오류가 이유 자리에 보이고 폼 상단 상자는 없다
- 저장값 빈 필수 칸이 다른 칸 수정을 막지 않는다(네 타입)
- 보관된 선택지 현재 값이 「(보관됨)」으로 보이고 그대로 저장된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 변경된 기존 화면 | `/admin/vendors` 거래처 폼 — 칸별 오류 · `#vendor-form-reason` · `noValidate` · 구분선 · 「{값} (보관됨)」 옵션 · 폼 상단 상자 제거 |
| 함수(신규, lib 순수) | `fieldErrorsReason(verb, names)` |
| E2E 픽스처(신규) | `createE2EVendorEditor` · `createE2EFieldDefinition` · `archiveE2EFieldDefinitionOption` |
| 테스트 | `test/unit/ui/vendor-form-reason.test.ts` · `test/e2e/vendor-custom-fields.spec.ts` · `form-reason.test.ts` 추가 케이스 |
| 편차 기록 | UI-SPEC 「단위(RTL)」 → 소스 검사 + E2E(렌더 러너 없음, 새 의존성은 승인 사항) |
| 디자인 리뷰 대기 | OPEN 1-B(이름 칸 서버 문구 · 요약 대상) |

<output>
Create `.planning/phases/04.5-custom-field-admin/04.5-06-SUMMARY.md` when done
</output>
