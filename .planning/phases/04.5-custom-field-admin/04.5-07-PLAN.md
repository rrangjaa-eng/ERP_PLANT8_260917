---
phase: 04.5-custom-field-admin
plan: 07
type: execute
wave: 7
depends_on: ["04.5-04", "04.5-06"]
files_modified:
  - test/e2e/custom-field-journey.spec.ts
  - app/(app)/admin/field-definitions/page.tsx
  - app/(app)/admin/field-definitions/field-definition-form.tsx
  - app/(app)/admin/field-definitions/field-definitions.module.css
  - app/(app)/admin/vendors/vendor-form.tsx
  - app/(app)/admin/vendors/vendors.module.css
  - db/migrations/NNNN_custom_field_admin.sql
  - db/migrations/meta/NNNN_snapshot.json
  - db/migrations/meta/_journal.json
  - repositories/permissions.ts
  - test/unit/ui/role-menu.test.ts
  - test/e2e/admin-nav.spec.ts
  - docs/design/SYSTEM.md
autonomous: true
requirements: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "거래처 입력 화면으로 끝까지 확인한다 — 관리 화면에서 칸 추가 → 시스템 관리자의 거래처 폼·노출표에 보임 → 값 저장 → 전용 계급에서 그 칸을 끔 → 그 계급 사용자의 거래처 화면 HTML에 칸 이름과 값이 없음(저장해도 값 유지) → 보관 → 모두에게 숨음 → 보관함에서 복원 → 다시 보이고 값과 계급별 끔이 그대로. 한 E2E가 이 순서를 `CI=true`로 증명한다(ROADMAP 04.5 기준 4)"
    - "머지 직전 마이그레이션 의식을 거친 뒤 이 페이즈 마이그레이션 번호는 `pnpm db:generate`가 붙인 main 마지막 + 1이다. 저널 idx가 이어지고, 손으로 넣은 채움 순서(label 기본값 → key로 채움 → 기본값 제거 → unique `(entity, label)`)와 SQL 끝의 기존 거래처 칸 노출 행 채움(표시 주석 `-- 04.5: 기존 거래처 칸 노출 행 채움` + `INSERT INTO \"visibility_matrix\" … ON CONFLICT … DO NOTHING`)이 새 SQL에 다시 들어가 있다. 어떤 파일에도 손으로 정한 번호가 없다(ROADMAP 04.5 기준 5, 병렬 페이즈 조정)"
    - "main을 합친 뒤에도 공유 파일에서 main 줄이 사라지지 않는다 — `git diff origin/main`의 `-` 줄은 파일별 승인 목록(거래처 폼의 01·05·06 교체, 저널의 쉼표 한 줄 등) 안에만 있고, 저널은 main 항목 전부 보존 · `idx` 연속 · 이 페이즈 항목이 마지막이다. 관리 인덱스 링크 수 = main의 수 + 1, 보관함 등록부 항목 수 = main의 수 + 1, `domain/archive/index.ts`의 항목별 권한 세 곳(04-07·04-12와 충돌 가능)이 남아 있다. `insertVisibilityIfAbsent`는 한 벌이고 선택 `tx`를 받아 쿼리에 쓴다"
    - "전체 게이트 `CI=true pnpm test`(단위 → 통합 → E2E)를 머지 의식 뒤 한 번 돌려 초록이다. 프로젝트·견적 줄 저장(Phase 4 코드)이 그대로 동작하고, 누수 스캔 생성기가 이 페이즈의 새 액션·DTO와 커스텀 칸 축을 포함한다(ROADMAP 04.5 기준 4·5)"
    - statement: "E3 a11y — 화면 항목 목록의 이름 셀이 `<th scope=\"row\">`라 행 머리글이 「삭제」 트리거의 문맥이 된다. 확인 진입·취소·완료 뒤 포커스 이동·복귀가 없고 진행 중 「취소」가 잠기지 않는 것은 기록된 임시 예외(SYSTEM.md 671행, DECISIONS.md 2026-09-24)와 같다 — 독립 DOM 감사가 1280·1024·375에서 실측"
      verification: backstop
    - statement: "E5 scale — 커스텀 열 20개 × 이름 20자 시드에서 `/admin/visibility`의 넘침이 가로 스크롤 컨테이너 밖으로 0이고 계급 열이 sticky로 고정된다 — 독립 DOM 감사가 1280·1024·375에서 실측"
      verification: backstop
    - statement: "E2 populated / zero-one-many — 수정 모드에 기존 값(이름·필수·정렬·활성 선택지·보관 선택지 N개)이 채워지고, 선택지 0·1·30개에서 폼이 1열을 유지하며 가로 넘침이 없다 — 독립 DOM 감사가 1280·1024·375에서 실측"
      verification: backstop
    - statement: "E3 empty / populated / partial / overflow / zero-one-many / long-text — 이름 20자 행과 보관 행을 포함한 목록에서 「삭제」 트리거와 인라인 확인 줄이 칸 밖으로 넘치지 않고, 보관 행에는 트리거가 없다 — 독립 DOM 감사가 1280·1024·375에서 실측"
      verification: backstop
    - statement: "E4 loading / populated / overflow / zero-one-many / long-text — 거래처 폼이 커스텀 칸 0·1·N개, 20자 라벨, 40자 선택지에서 1열을 유지하고 넘침이 없다. 0개면 구분선이 없다. 서버 렌더라 로딩 상태가 없다 — 독립 DOM 감사가 1280·1024·375에서 실측"
      verification: backstop
    - statement: "E5 empty / loading / populated / partial — 커스텀 칸 0개면 노출표가 기존과 같고, 보관된 칸의 열은 없으며, 커스텀 열 셀 토글이 저장되고 실패하면 원위치 + 셀 이유 「저장하지 못했습니다 · 다시 시도」 + 토스트 「권한 저장 실패 · 다시 시도」 — 독립 DOM 감사가 1280·1024·375에서 실측"
      verification: backstop
  prohibitions:
    - statement: "이 페이즈의 마이그레이션이 손으로 정한 번호나 번호 사이 빈자리를 가진 채 main에 들어가서는 안 된다 — 04.1의 저널 연속 검사가 깨지고 병렬 페이즈의 마이그레이션과 충돌한다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/e2e/custom-field-journey.spec.ts"
      provides: "ROADMAP 기준 4 여정 — 추가 → 보임 → 계급별 끔 → 보관 → 숨음 → 복원 → 다시 보임"
      min_lines: 60
    - path: "db/migrations/meta/_journal.json"
      provides: "main 마지막 + 1 번호의 이 페이즈 항목 하나(`custom_field_admin`)"
      contains: "custom_field_admin"
  key_links:
    - from: "test/e2e/custom-field-journey.spec.ts"
      to: "test/e2e/fixtures.ts"
      via: "06의 `createE2EVendorEditor`(전용 계급 사용자 — 반환 `{ email, password, roleId, roleName }`) · 01의 `archiveE2EFieldDefinitions`(정리)"
      pattern: "createE2EVendorEditor"
    - from: "db/migrations/NNNN_custom_field_admin.sql"
      to: "db/schema/field-definitions.ts"
      via: "머지 뒤 `pnpm db:generate --name custom_field_admin`이 스키마에서 다시 만든 SQL + 손으로 다시 넣은 채움 순서"
      pattern: "custom_field_admin"
---

<objective>
페이즈 전체를 거래처 입력 화면으로 끝까지 증명하고(SC-4 여정 E2E), 실행자가 아닌 별도 에이전트의 독립 DOM 감사로 backstop 여섯 개를 실측한 뒤 발견을 고친다. main을 합쳐 마이그레이션 번호를 main 마지막 + 1로 다시 만들고 공유 파일 충돌을 정리한 다음 전체 게이트를 한 번 돌린다.

Purpose: ROADMAP 04.5 기준 4(여정 E2E)와 기준 5(마이그레이션 머지 의식). CLAUDE.md 화면 검증 순서(싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번)의 마지막 세 단계.

Output: `test/e2e/custom-field-journey.spec.ts` · 독립 DOM 감사 보고서(SUMMARY에 붙임) · 감사 발견 수정 · main 병합 뒤 다시 만든 마이그레이션 · 전체 게이트 결과 요약.

**실행 순서 — Phase 4 선행:** `app/(app)/admin/vendors/vendor-form.tsx`(04-25) · `db/migrations/meta/_journal.json`(04-01·04-02·04-06·04-07·04-10·04-13·04-27·04-41) · `repositories/permissions.ts`(04-20) · `docs/design/SYSTEM.md`(04-01·04-08·04-29·04-46)를 Phase 4도 고치므로 그 Phase 4 플랜이 작업 브랜치에 들어온 뒤에만 실행한다 — 먼저 `sh -c 'for p in 04-01 04-02 04-06 04-07 04-08 04-10 04-13 04-20 04-25 04-27 04-29 04-41 04-46; do git ls-tree --name-only HEAD .planning/phases/04-project-quote-ledger/ | grep -q "/$p-SUMMARY.md" || exit 1; done'`를 돌려(01이 착수 때 main을 합쳤다 — 이 확인은 HEAD만 본다, main 병합은 Task 3 ①의 의식에서만) 종료 코드가 0이 아니면 멈추고 사용자에게 알린다.

**실행 규율:** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 커밋·「완료」 전 `verification-before-completion`을 부른다. 서브에이전트에 위임하면 세 스킬과 `model`을 적는다. 한 세션에 플랜 하나. 통합·E2E는 `pnpm db:dev`(Postgres 기동 · 없는 DB만 생성) 뒤, E2E 판정은 `CI=true`로 한다. 공유 테스트 DB(`erp_test`)·통합 테스트·`db:reset:test`·마이그레이션·3100 포트·`next build`를 쓰는 명령은 모두 `flock /tmp/plant8-erp-test.lock`로 잠근다. 단위·lint·typecheck는 잠그지 않는다. 테스트 DB를 손으로 준비할 때 순서는 `pnpm db:reset:test`(빈 DB) → 마이그레이션 → 기본 시드 → 이 플랜의 픽스처다(`test/e2e/global-setup.ts` 14–46행과 같음). 새 의존성 0개. `.planning/`은 GSD 도구로만 쓴다. `git push --force`·프로덕션 DB 명령은 금지다.

**페이즈 뒤(이 플랜 밖, 건너뛰지 않음):**
- `/gsd-verify-work`
- UI 완료 판정 `/design-review` → `/qa`
- Post-build `/review` → `/qa` → `/cso` → `/ship`. `/cso`는 이 페이즈가 권한·외부 입력(커스텀 키)을 건드렸으므로 필수다.
- 디자인 리뷰 대기 OPEN 1-A·1-B·2-A와 UI-SPEC 편차 OPEN(04의 편차-04a 테스트 (b) → 소스 검사 + domain 테스트 (a) · 편차-04b 페이지 「삭제」 렌더 조건 → 소스 검사 · 06의 편차-06a 단위(RTL) → 소스 검사 + E2E)을 `/plan-design-review`로 넘긴다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-08-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-09-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-04-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-05-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-06-SUMMARY.md
@CLAUDE.md
# 범위 Read(UI-SPEC 개정 3, 75ae245 기준):
# .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md `## UI Considerations` 표(154–201행 — backstop 여섯 줄 183·195–199행) · 「화면 4」 303–316행 · 「기존 공유 컴포넌트 — 기록된 시스템 예외」 322–334행
# .planning/ROADMAP.md Phase 04.5 기준 4·5
# playwright.config.ts(CI에서 `pnpm build && pnpm start`, `mobile-*.spec.ts` → mobile-375, 워커가 `erp_test`를 나눠 씀)
</context>

<probe_fallback>
⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). 수용 기준은 ROADMAP 04.5 기준 4·5, UI-SPEC(개정 3) `## UI Considerations`의 backstop 여섯 줄, 오케스트레이터의 병렬 페이즈 조정(마이그레이션 머지 의식 · 공유 파일 더하기만 · `insertVisibilityIfAbsent` 확인)에서 끌어왔다.

**계획 단계 결정(표면화):**

- **감사 시드는 커밋하지 않는다.** backstop 실측에는 칸 20개·이름 20자·선택지 0/1/30개·40자 선택지 같은 큰 시드가 필요하다. 이 시드가 E2E 도중 공유 `erp_test`에 있으면 다른 스펙의 폭 측정이 흔들린다(`playwright.config.ts` 「함정 6」). 그래서 감사 에이전트는 E2E와 같은 테스트 DB 준비 순서를 손으로 밟는다. 순서는 `test/e2e/global-setup.ts` 14–46행을 따른다: 스키마 비우기 → 마이그레이션 → `seedMasterData(SYSTEM_VIEWER)`. `pnpm db:reset:test`는 **빈 DB만** 만든다(`scripts/reset-test-db.sh` 7행 — DROP/CREATE뿐). 마이그레이션도 기본 시드도 하지 않는다.
  - 환경: 모든 명령에 `DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test`를 붙인다. `playwright.config.ts` 7행의 E2E DB다. `.env.local`의 개발 DB가 아니다. `pnpm db:migrate`·`pnpm db:seed`는 `--env-file-if-exists=.env.local`로 돌지만, 이미 설정된 환경 변수가 파일 값보다 우선한다.
  - 공유 환경을 쓰므로 1~6 전체를 하나의 `flock /tmp/plant8-erp-test.lock bash -c '…'`로 감싼다. 서버를 띄운 채로 실측하는 동안 다른 실행자가 `erp_test`·3100 포트를 쓰지 못하게 하기 위함이다.
  1. `pnpm db:reset:test` — 빈 `erp_test`를 만든다.
  2. 마이그레이션 — `pnpm db:migrate`. global-setup 14–17행의 `migrate()`와 같은 폴더를 쓴다.
  3. 기본 시드 — `pnpm db:seed`. global-setup 28–30행의 `seedMasterData(SYSTEM_VIEWER)`와 같다. 권한표·노출표가 비면 시스템 관리자도 모든 메뉴가 거부된다.
  4. 감사 픽스처 시드 — 01·06의 E2E 픽스처(`createE2EFieldDefinition` 등)를 부르는 임시 스크립트를 저장소 루트에서 `node --env-file-if-exists=.env.local --import tsx <스크래치패드 경로>`로 돌린다. 스크립트는 세션 스크래치패드에 두고 저장소에 넣지 않는다.
  5. `CI=true` 프로덕션 서버를 3100에 띄운다. `pnpm build && PORT=3100 BETTER_AUTH_URL=http://127.0.0.1:3100 pnpm start`에 `playwright.config.ts` 7–20행과 같은 E2E 환경 변수를 준다. 그다음 Playwright `page.evaluate`로 DOM을 실측한다(`getBoundingClientRect`·`scrollWidth`·계산된 스타일·aria 속성). 스크린샷 육안 판정은 금지다.
  6. 서버를 끄고 `pnpm db:reset:test`로 감사 시드를 지운다. 다음 E2E는 global-setup이 다시 준비한다.
- **마이그레이션 번호:** 이 플랜과 모든 플랜의 `NNNN`은 자리 표시다. 실제 번호는 `pnpm db:generate`만 붙인다.
- **files_modified 13개 — 파일별 이유(한 줄씩).** 이 플랜이 의도해서 고치는 파일만 적는다. 병합 충돌 해소로만 손대는 공유 파일(Task 3 ① 목록 — `domain/archive/index.ts`·`repositories/archive.ts` 등)은 넣지 않는다. 병합 커밋의 범위는 main이 정한다. 그 파일들은 Task 3 ⑤의 보존 검사가 대신 본다.
  - `test/e2e/custom-field-journey.spec.ts` — 새 파일. SC-4 여정(Task 1).
  - `app/(app)/admin/field-definitions/page.tsx` — 감사 E1·E3(목록·삭제 트리거·`th[scope=row]`)이 실패하면 고칠 곳(Task 2).
  - `app/(app)/admin/field-definitions/field-definition-form.tsx` — 감사 E2(수정 모드·선택지 0/1/30개 1열)가 실패하면 고칠 곳(Task 2).
  - `app/(app)/admin/field-definitions/field-definitions.module.css` — 위 두 화면의 넘침·배치 수정을 토큰으로(Task 2).
  - `app/(app)/admin/vendors/vendor-form.tsx` — 감사 E4(커스텀 칸 0·1·N · 20자 라벨 · 40자 선택지)가 실패하면 고칠 곳(Task 2).
  - `app/(app)/admin/vendors/vendors.module.css` — E4 넘침·구분선 수정을 토큰으로(Task 2).
  - `db/migrations/NNNN_custom_field_admin.sql` — 지우고 main 마지막 + 1로 다시 만들고 채움 순서를 다시 넣는다(Task 3 ③).
  - `db/migrations/meta/NNNN_snapshot.json` — 재생성 산출물(Task 3 ③).
  - `db/migrations/meta/_journal.json` — 이 페이즈 항목을 지우고 다시 생성한다. 구조 검사 대상(Task 3 ③·⑤).
  - `repositories/permissions.ts` — `insertVisibilityIfAbsent`를 한 벌로 합치고 선택 `tx`를 지킨다(Task 3 ②).
  - `test/unit/ui/role-menu.test.ts` — 관리 인덱스 링크 수를 main + 1로(Task 3 ④, 09의 수).
  - `test/e2e/admin-nav.spec.ts` — 라벨 목록·수를 main + 1로(Task 3 ④, 09의 수).
  - `docs/design/SYSTEM.md` — §6-10 표를 main 행 + 「화면 항목」으로(Task 3 ④, 09의 표).
</probe_fallback>

<reversibility_notes>

- main 병합과 마이그레이션 재생성 — **costly**: 병합 커밋은 되돌릴 수 있지만 충돌 해소 판단(공유 파일 양쪽 추가 보존)이 틀리면 다른 페이즈의 코드를 지운다. 그래서 저널은 구조로, 공유 파일은 승인 목록 대조로 확인한다(Task 3 ⑤).
- 감사 발견 수정 — **reversible**: 이 페이즈 화면 파일 안의 토큰·배치 수정이다.

</reversibility_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC-4 여정 E2E — 추가 → 보임 → 값 저장 → 계급별 끔(화면 HTML에 없음 · 저장해도 값 유지) → 보관 → 숨음 → 복원 → 다시 보임</name>
  <files>test/e2e/custom-field-journey.spec.ts</files>
  <read_first>
    - `test/e2e/fixtures.ts`(전문 — 06의 `createE2EVendorEditor`(반환 `roleId`·`roleName`)·`createE2EFieldDefinition`, 01의 `archiveE2EFieldDefinitions`)
    - `test/e2e/field-definitions.spec.ts`(01·08·09·02·04 — 관리 화면 조작) · `test/e2e/custom-field-visibility.spec.ts`(03 — 노출표 셀 끄기·HTML 부재 단언) · `test/e2e/archive.spec.ts`(보관함 복원)
    - ROADMAP Phase 04.5 기준 4
  </read_first>
  <behavior>
    - 시스템 관리자가 `/admin/field-definitions`에서 텍스트 칸 「E2E여정{난수}」를 추가한다. 이 칸은 관리 화면으로 만든다(픽스처 아님). 추가하면 `/admin/vendors?new=1`에 그 라벨이 있고 `/admin/visibility`에 그 열이 있다
    - 거래처 하나에 그 칸 값 「여정값{난수}」를 저장한다
    - 전용 계급은 06의 `createE2EVendorEditor()`로 만든다. 반환값 `{ email, password, roleId, roleName }`에서 `email`·`password`로 그 사용자로 로그인하고, `roleName`으로 셀을 찾는다. `/admin/visibility`에서 그 계급 행 × 그 칸 열의 셀을 끈다. 셀의 aria-label은 `{roleName} · [{묶음} · ]{칸 이름}`이다(`app/(app)/admin/permissions/permission-grid-client.tsx` 39–41행). 전용 계급 사용자의 거래처 목록·수정 폼 HTML(`page.content()`)에 라벨과 값 문자열이 없다. 그 사용자가 그 거래처의 사업자 번호를 고쳐 저장한 뒤에도, 시스템 관리자 수정 폼에 값이 그대로 있다
    - 시스템 관리자가 목록 행 「삭제」로 칸을 보관한다. 거래처 폼과 노출표에 칸이 없다
    - `/admin/archive`에서 「화면 항목」 행을 복원한다. 시스템 관리자의 거래처 폼에 칸과 값이 돌아오고 노출표 열도 돌아온다. 전용 계급 셀은 여전히 꺼져 있고 그 사용자의 HTML에는 여전히 없다
    - `afterAll`에서 접두 「E2E여정」 칸을 보관해 뒤 스펙에 남기지 않는다
  </behavior>
  <action>
① 스펙을 먼저 쓰고 돌린다. 01~06·08·09가 끝났으면 처음부터 초록일 수 있다. 그 경우에는 한 단계를 일부러 깨뜨려 빨간 것을 확인한다. 예: 복원 뒤 단언을 반대로 뒤집는다. 그 뒤 되돌려, 스펙이 실제 동작을 보는지 증명한다.

② 칸 이름은 20자 이하로 한다. 다른 워커와 겹치지 않도록 난수 꼬리를 붙인다.

③ 이 스펙이 드러낸 결함은 `systematic-debugging`으로 원인을 찾아 해당 플랜 파일 안에서 고친다. 고친 파일은 SUMMARY에 적는다.

④ DB 준비는 E2E globalSetup이 한다(`test/e2e/global-setup.ts` 14–30행). 순서는 스키마 비우기(빈 DB) → 마이그레이션 → 기본 시드(`seedMasterData`)이고, 그 뒤 스펙이 자기 픽스처를 만든다. `pnpm db:dev`는 Postgres를 띄우고 없는 DB만 만든다. DB를 다시 만들지 않는다. Playwright 실행은 공유 `erp_test`·3100 포트·`next build`를 쓰므로 `flock /tmp/plant8-erp-test.lock`로 잠근다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; flock /tmp/plant8-erp-test.lock env CI=true pnpm playwright test test/e2e/custom-field-journey.spec.ts</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 추가 뒤 폼·노출표에 없거나, 끈 계급 HTML에 라벨·값이 남거나, 그 계급의 저장이 값을 지우거나, 보관 뒤 보이거나, 복원 뒤 값·계급별 끔이 돌아오지 않으면 빨간색이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 여정의 여섯 단계가 한 스펙 안에서 순서대로 단언된다(SUMMARY에 단계별 결과)
    - RED 확인 방법(일부러 깨뜨린 단언)이 SUMMARY에 있다
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>관리 화면에서 만든 칸이 거래처 입력 화면에서 추가 → 보임 → 계급별 끔 → 보관 → 숨음 → 복원 → 다시 보임으로 끝까지 동작함이 E2E로 증명된다</done>
</task>

<task type="auto">
  <name>Task 2: 독립 DOM 감사(별도 에이전트, `CI=true`, 1280·1024·375) — backstop 여섯 개 실측 → 발견 수정 → 싼 게이트</name>
  <files>app/(app)/admin/field-definitions/page.tsx, app/(app)/admin/field-definitions/field-definition-form.tsx, app/(app)/admin/field-definitions/field-definitions.module.css, app/(app)/admin/vendors/vendor-form.tsx, app/(app)/admin/vendors/vendors.module.css</files>
  <read_first>
    - UI-SPEC `## UI Considerations` 표 전부(154–201행) — 특히 backstop 여섯 줄
    - UI-SPEC 「기존 공유 컴포넌트 — 기록된 시스템 예외」(322–334행 — `DeleteToArchive` 인라인 확인 X1~X4는 결함이 아니라 기록된 예외)
    - `docs/design/SYSTEM.md` §6-3 · §7-3 · §7-13 · 671행 · `docs/design/tokens.css`
    - 01~06·08 SUMMARY의 화면 변경 목록(08 — 전체 폼 오류·이름 예약 UI·복원 링크)
  </read_first>
  <action>
① 싼 게이트를 먼저 돌린다: `pnpm lint`·`pnpm typecheck`·`flock /tmp/plant8-erp-test.lock pnpm build`(`next build`는 공유 `.next`를 쓰므로 잠금).

② **독립 DOM 감사**는 실행자가 아닌 별도 서브에이전트가 한다.
- `model`을 명시한다. 판정·검토라 CLAUDE.md 모델 선택 규칙대로 Opus 5.
- 프롬프트에 Skill `verification-before-completion`을 명시한다.
- 에이전트는 계획 단계 결정의 테스트 DB 준비 순서를 그대로 밟는다. 순서는 `test/e2e/global-setup.ts` 14–46행과 같다: `pnpm db:reset:test`(빈 DB) → `pnpm db:migrate`(마이그레이션) → `pnpm db:seed`(기본 시드) → 임시 감사 픽스처 시드 → `CI=true` 프로덕션 서버(3100) → Playwright `page.evaluate` 실측. 모두 `DATABASE_URL`을 `erp_test`로 두고, 전체를 `flock /tmp/plant8-erp-test.lock` 하나로 감싼다. 프롬프트에 이 순서와 명령을 그대로 적는다. 스크린샷 육안 판정은 금지다.
- 시드: 텍스트 칸 20개(이름 20자, 공백 없는 문자열 포함), 선택형 칸 셋(활성 선택지 0·1·30개, 30개 중 40자 선택지 포함, 보관 선택지 N개), 20자 이름의 보관된 칸 하나, 커스텀 값이 든 거래처 하나.
- 폭은 1280·1024·375 세 개다.
- 판정 항목 = frontmatter의 `verification: backstop` 여섯 줄. 줄마다 실측값과 통과/실패를 적는다. 예: 넘침 px, sticky 열의 `left` 유지 여부, 1열 여부(칸들의 `left` 동일), `th[scope=row]` 존재, 셀 실패 원위치·문구, 0칸 구분선 부재.
- 노출표 셀 실패는 그 칸을 보관한 뒤 이미 열린 노출표에서 셀을 누르는 방식으로 만든다. 서버가 활성 정의 밖 항목을 거부하기 때문이다.
- 감사가 끝나면 서버를 끄고 `pnpm db:reset:test`로 감사 시드를 지운다(같은 잠금 안에서).

③ 보고서를 받아 실패 항목만 이 페이즈 화면 파일 안에서 고친다.
- 새 색·서체·radius는 만들지 않는다. 토큰은 `docs/design/tokens.css`에서만 쓴다.
- 시스템을 벗어나야 하면 고치지 말고 SUMMARY에 「디자인 리뷰 대기」로 적는다. CLAUDE.md: DECISIONS.md 기록 + SYSTEM.md 수정은 사용자 결정이다.
- 기록된 예외 X1~X4에 해당하는 관찰은 결함으로 세지 않는다.
- 고친 뒤 실패 항목만 같은 에이전트 방식으로 다시 실측한다.

④ 고친 파일이 있으면 싼 게이트를 다시 돌린다. 전체 게이트는 Task 3에서 한 번만 돌린다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error로 끝난다 — 감사 발견 수정이 토큰 밖 값·타입 오류·빌드 실패를 만들었다</fails_when>
    <human-check>독립 DOM 감사 보고서(감사 에이전트 모델명, 1280·1024·375 세 폭, backstop 여섯 줄마다 실측값과 통과/실패, 수정 뒤 재측정 결과)가 04.5-07 SUMMARY에 붙어 있다</human-check>
  </verify>
  <acceptance_criteria>
    - backstop 여섯 줄이 세 폭 모두 통과이거나, 실패 항목이 「디자인 리뷰 대기」로 이유와 함께 SUMMARY에 있다
    - 감사 에이전트가 실행자와 다른 서브에이전트이고 `model`이 명시되어 있다(SUMMARY)
    - 감사 시드 스크립트가 저장소에 없다(`git status --porcelain`에 스크래치패드 밖 새 시드 파일 없음)
    - 수정 diff의 CSS 값이 `var(--…)` 토큰뿐이다
    - 검증 명령이 초록이다
  </acceptance_criteria>
  <done>이 페이즈의 네 화면이 세 폭에서 backstop 여섯 항목을 실측으로 통과하고, 발견은 고쳐졌거나 디자인 리뷰로 넘겨졌다</done>
</task>

<task type="auto">
  <name>Task 3: 머지 직전 의식 — main 병합 · `insertVisibilityIfAbsent` 재확인 · 자기 마이그레이션 지우고 `pnpm db:generate` 다시 · 채움 순서 재적용 · 공유 파일 더하기만 확인 · 전체 게이트 한 번</name>
  <files>db/migrations/NNNN_custom_field_admin.sql, db/migrations/meta/NNNN_snapshot.json, db/migrations/meta/_journal.json, repositories/permissions.ts, test/unit/ui/role-menu.test.ts, test/e2e/admin-nav.spec.ts, docs/design/SYSTEM.md</files>
  <read_first>
    - `db/migrations/meta/_journal.json`(끝 항목들) · 이 페이즈 마이그레이션 SQL(01이 손으로 넣은 채움 순서 — 01 SUMMARY)
    - `test/unit/custom-fields/field-definitions-migration.test.ts`(01 — 채움 순서 검사) · `test/unit/db/migration-journal.test.ts`(04.1 소유 — 있을 때만)
    - `.squawk.toml` · `scripts/reset-test-db.sh`(빈 DB만) · `scripts/dev-db.sh`(`create_db_if_missing` — 있는 DB는 그대로) · `test/integration/global-setup.ts`(마이그레이션 호출) · `test/e2e/global-setup.ts` 14–46행(스키마 비우기 → 마이그레이션 → 기본 시드)
    - `repositories/permissions.ts`의 `insertVisibilityIfAbsent`(01 — 선택 `tx`) · `git show origin/claude/gsd-progress-e1nzgu:.planning/phases/04-project-quote-ledger/04-20-PLAN.md` 215행(인자 둘 계약) · 01 SUMMARY의 생성 롤백 테스트 이름
    - 04 SUMMARY의 「머지 충돌 위험」(`domain/archive/index.ts` 세 곳) · 09 SUMMARY의 관리 인덱스·SYSTEM.md·누수 스캔 변경 줄
  </read_first>
  <action>
① `git fetch origin main`, 그다음 `git merge origin/main`. 충돌은 공유 파일마다 **양쪽 추가를 모두 남기는** 방향으로 푼다. 대상은 오케스트레이터 목록의 더하기만 파일과, 이 페이즈(01·04·06·08·09)가 더한 공유 파일이다.
- `domain/action-log/record.ts`
- `domain/permissions/info-items.ts`
- `domain/permissions/menus.ts`
- `domain/seed/index.ts`
- `test/integration/leak-scan.test.ts`(09의 누수 스캔 배선 포함)
- `_journal.json`
- `vendor-form.tsx`
- `domain/archive/index.ts` · `repositories/archive.ts` · `app/(app)/admin/archive/actions.ts`(04)
- `repositories/permissions.ts`
- `test/e2e/fixtures.ts`(01·06의 픽스처)
- `ui/shell/role-menu.ts` · `docs/design/SYSTEM.md` · `test/unit/ui/role-menu.test.ts` · `test/e2e/admin-nav.spec.ts`(09의 관리 인덱스 링크·메뉴 표)

다른 페이즈의 줄을 지우지 않는다. 풀기 어려운 충돌은 멈추고 SUMMARY에 적은 뒤 사용자에게 묻는다. 병합 해소로만 손대는 파일은 files_modified에 넣지 않았다. 병합 커밋이 바꾸는 범위는 main이 정하기 때문이다. 대신 ⑤의 보존 검사가 이 목록 전부를 본다.

② **`insertVisibilityIfAbsent` 조정 — 한 벌로 합치되 선택 `tx`를 지킨다.** 먼저 `git grep -n "export async function insertVisibilityIfAbsent" -- repositories/permissions.ts`로 몇 벌인지 센다.
- 배경: Phase 4 04-20의 계약은 인자 둘 `(viewer, row)`이다(`origin/claude/gsd-progress-e1nzgu:.planning/phases/04-project-quote-ledger/04-20-PLAN.md` 215행). 기존 쓰기 패턴도 전역 `db`로 쓴다(`repositories/permissions.ts` 105행 `upsertVisibility`의 `await db`). 반면 01은 같은 함수에 마지막 선택 인자 `tx: DbOrTx = db`를 더했다. 칸 생성 트랜잭션 안에서 노출 행을 쓰기 위해서다(`grantCustomFieldToAllRoles` → `insertVisibilityIfAbsent(…, tx)`).
- **두 벌이면** 한 벌로 합친다. 결과는 main의 이름·앞 두 인자·`onConflictDoNothing` 대상에 이 페이즈의 마지막 선택 인자 `tx: DbOrTx = db`를 더한 모양이다. 쿼리는 전역 `db`가 아니라 `tx`로 시작한다(`await tx` → `.insert(visibilityMatrix)`).
- **main 쪽을 그대로 두고 이 페이즈 쪽을 지우는 해소는 금지다.** 인자 셋 호출이 타입 오류가 나거나, `tx`를 받아도 쿼리가 `db`를 써서 노출 행이 트랜잭션 밖에서 커밋된다. 그러면 노출 행 쓰기가 실패해도 칸 정의가 남아, 01이 증명한 생성 원자성이 깨진다.
- **한 벌인데 main 모양(인자 둘, `db` 사용)이면** 같은 방식으로 선택 `tx`를 더하고 쿼리가 `tx`를 쓰게 고친다. ⑤ 표의 승인 줄은 시그니처 줄과 쿼리 시작 줄이다.
- 호출부는 둘 다 그대로다. 04-20 호출부(인자 둘)는 기본값 `db`로 돈다. 이 페이즈 호출부(인자 셋)도 그대로다. 이 페이즈 호출부를 인자 둘로 줄이는 조정은 금지다.
- 확인: 함수가 한 벌이고, 시그니처에 `tx: DbOrTx`가 있고, 본문이 `tx`로 쓴다. 첫 번째 verify가 자동으로 본다.
- 01이 더한 **생성 롤백 테스트**를 다시 돌린다. 노출 행 INSERT가 중간에 실패하면 칸 정의도 노출 행도 남지 않아야 한다. 파일은 01 SUMMARY가 적은 곳이고, 계획상 `test/integration/field-definitions-admin.test.ts`다. 돌리는 때는 ③에서 마이그레이션을 다시 만들고 (g)를 마친 뒤다. 그 전에는 저널이 정리되지 않아 통합 globalSetup이 마이그레이션을 적용하지 못한다. 명령은 두 번째 verify다.

③ 마이그레이션을 다시 만든다.
- (a) 이 페이즈 SQL의 손 편집 부분을 스크래치패드에 저장한다. 손 편집 부분은 둘이다. ㉮ `label`을 빈 문자열 기본값으로 더함 → `UPDATE … SET label = key` → 기본값 제거 → unique `(entity, label)`. ㉯ SQL 끝의 기존 거래처 칸 노출 행 채움 — 표시 주석 `-- 04.5: 기존 거래처 칸 노출 행 채움`과 `INSERT INTO "visibility_matrix" … CROSS JOIN "roles" … ON CONFLICT ("role_id", "info_item") DO NOTHING;` 한 문장(01 Task 2 ②).
- (b) 이 페이즈의 SQL·snapshot·`_journal.json` 항목을 지운다. 태그에 `custom_field_admin`이 든 것만이다.
- (c) `pnpm db:generate --name custom_field_admin`을 돌린다. 번호는 drizzle-kit이 main 마지막 + 1로 붙인다. 손으로 번호를 붙이거나 바꾸지 않는다.
- (d) 새 SQL에 (a)의 두 부분을 다시 넣는다. ㉮는 unique 제약과 NOT NULL 전에 채움이 오게 한다. ㉯는 `--> statement-breakpoint`로 나눠 SQL 끝에 표시 주석과 함께 글자 그대로 붙인다 — 08의 문장 순서 가드와 03 Task 2의 선행 조건 grep이 이 주석·문장을 찾는다.
- (e) `pnpm lint:sql`
- (f) **빈 DB 적용 확인** — `flock /tmp/plant8-erp-test.lock bash -c 'pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate'`.
  - `pnpm db:reset:test`는 빈 DB만 만든다(`scripts/reset-test-db.sh` 7행). 마이그레이션은 `db:migrate`로 따로 적용한다.
  - 로컬 개발 DB(`erp`)는 이 태스크의 검증에 쓰지 않는다. 옛 번호의 적용 기록 때문에 개발용 `pnpm db:migrate`가 실패할 수 있다. 이때 `pnpm db:dev`는 답이 아니다. 없는 DB만 만들고 있는 DB는 다시 만들지 않는다(`scripts/dev-db.sh`의 `create_db_if_missing`).
  - 개발 DB가 필요하면 로컬 `erp`만 지우고 다시 준비한다: `PGPASSWORD=erp psql -h 127.0.0.1 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp WITH (FORCE)'` → `pnpm db:dev`(빈 `erp` 생성) → `pnpm db:migrate` → `pnpm db:seed`. 로컬 개발 데이터가 사라지므로 SUMMARY에 적는다. 프로덕션 DB 명령은 금지다.
- (g) `test/unit/db/migration-journal.test.ts`가 있으면 그것과 01의 마이그레이션 검사(`field-definitions-migration.test.ts` — SQL 문장 순서)를 돌린다. 저널 구조 검사(⑤-a)도 이때 돌린다.
- (h) **백필 검증 — 따로 실행하는 단계.** (f)는 빈 DB라 채움 순서가 기존 행에서 통하는지 시험하지 못한다. (g)의 단위 검사는 SQL 글자 순서만 본다. 그래서 기존 행이 있는 DB에 다시 만든 SQL을 실제로 적용한다. 전체를 `flock /tmp/plant8-erp-test.lock bash -c '…'` 하나로 감싸고, `DATABASE_URL`은 `erp_test`다.
  1. `pnpm db:reset:test` — 빈 DB를 만든다.
  2. main까지의 마이그레이션만 적용한다. `git archive origin/main db/migrations | tar -x -C <스크래치패드>/main-mig`로 main의 폴더를 스크래치패드에 푼다. `test/integration/global-setup.ts`의 `migrate(db, { migrationsFolder })` 호출과 같은 임시 스크립트를 그 폴더로 돌린다. 스크립트는 스크래치패드에 두고 저장소에 넣지 않는다.
  3. 옛 모양 행 둘을 `psql`로 넣는다: `field_definitions (id, entity, key, type)` = `('bf-1','vendor','cf_bf000001','text')`, `('bf-2','project','cf_bf000001','text')`. 대상이 다르고 key가 같아서, 채운 뒤 unique `(entity, label)`도 함께 시험한다. 관리자가 끈 행 하나도 넣는다: `visibility_matrix (role_id, info_item, visible)` = `('role-pm','cf.vendor.cf_bf000001',false)`(계급 다섯은 main 마이그레이션 0003이 이미 넣었다).
  4. `pnpm db:migrate` — 이 페이즈 마이그레이션 하나만 더 적용된다.
  5. verify의 조회로 두 행 모두 `label = key`·`version = 1`·`archived_options = []`인지 확인한다. 조회 결과와 `\d field_definitions`(label NOT NULL · 기본값 없음 · unique `(entity, label)`)를 SUMMARY에 붙인다. 이 조회는 ⑥이 DB를 다시 준비하기 **전에** 돈다.
  6. verify의 노출 행 조회로 ㉯가 적용됐는지 확인한다: 모든 계급에 `cf.vendor.cf_bf000001` 행이 있다 · 3에서 끈 `role-pm` 행은 그대로 `false`다(`ON CONFLICT DO NOTHING`) · `cf.project.%` 행은 0이다(거래처 한정). 조회 결과를 SUMMARY에 붙인다. 이것도 ⑥ 전에 돈다.

④ 공유 개수를 맞춘다(09가 만든 수 · 표를 main 기준으로 다시 센다).
- `role-menu.test.ts`의 관리 인덱스 링크 수와 `admin-nav.spec.ts`의 라벨 목록을 main의 값 + 1(「화면 항목」)로 맞춘다.
- `docs/design/SYSTEM.md` §6-10 정본 표도 main의 행에 「화면 항목」 한 행을 더한 모양으로 맞춘다.
- 보관함 등록부 항목 수가 main + 1인지, `domain/archive/index.ts`의 항목별 권한 세 곳이 병합 뒤에도 있는지 확인한다(확인만 — 고칠 곳은 ①의 충돌 해소에서 끝난다).

⑤ **보존 검사 — 「삭제 줄 0」이 아니라 저널 구조 검사 + 승인 목록 대조.** 「모든 공유 파일 삭제 줄 0」은 이 페이즈 계획과 모순이라 쓰지 않는다. JSON 배열 끝에 항목을 더하면 main 마지막 항목의 닫는 `}`에 쉼표가 붙어 그 줄이 `-`로 나온다(`db/migrations/meta/_journal.json` 81행). 또 `vendor-form.tsx`는 01(라벨)·05(제출 수집)·06(폼 변경 1~6)이 main 줄을 바꾼다(현재 100행 · 216행 등).
- (a) **저널 구조 검사** — 줄 diff가 아니라 JSON을 비교한다. verify의 node 한 줄이 확인한다: main의 항목 전부가 같은 자리에 필드까지 같게 남아 있다 · 항목 수 = main + 1 · `idx`가 0부터 빈자리 없이 이어진다 · 마지막 항목이 이 페이즈 항목이다(태그 = 네 자리 번호 + `_custom_field_admin`, 번호 = 그 `idx`). 줄 diff의 `-` 줄은 main 마지막 항목의 닫는 `}` 한 줄뿐이다.
- (b) **공유 파일 보존 검사** — 아래 표의 파일마다 `git diff origin/main -U0 -- <파일>`의 삭제 줄(`-` 한 글자로 시작하는 줄 — `---` 파일 머리는 제외)을 뽑는다. 그 줄은 main에 있는데 이 브랜치에 없는 줄이다. 모두 그 파일의 승인 목록에 있어야 한다.
  - 목록 밖 `-` 줄은 다른 페이즈의 줄을 지운 것이다. main 쪽 줄을 되살린다.
  - 이 페이즈 플랜이 의도한 교체인데 목록에 없으면, 멈추고 사용자에게 그 줄을 보여 승인을 받는다. 스스로 목록을 늘리지 않는다.
  - SUMMARY에 파일마다 `-` 줄 수와 줄별 승인 근거(플랜·항목)를 적는다.
  - 표에 없는 파일이라도 ①에서 충돌이 난 파일은 「승인 줄 없음(더하기만)」으로 같은 검사를 한다.

| 공유 파일 | 승인된 `-` 줄(이 페이즈가 바꾸는 main 줄) — 이 밖은 전부 보존 | 근거 |
|---|---|---|
| `app/(app)/admin/vendors/vendor-form.tsx` | 01: 칸 라벨 두 줄(선택형 `<label htmlFor={id}>{def.key}</label>` · 입력형 `<TextField … label={def.key} …>`) — 로컬 타입 `VendorFieldDefinition`의 `label: string`은 더하기라 `-` 줄이 없다 · 05: `handleSubmit`의 커스텀 값 수집 줄(`if (raw === "" && !def.required) continue;` 등 99–101행 범위) · 06: `<form ref={formRef} …>` 여는 줄(`noValidate`) · `required={def.required}`가 든 두 줄(`<select …>` · 입력형 `<TextField …>`) · `{fieldDefs.map(…)}` 세 줄(구분선 묶음으로 감싸며 들여쓰기 변경) · `FormAlert` import 줄과 `{result.serverError ? <FormAlert>…` 줄 · 1차 `<Button type="submit" …>` 여는 줄(`aria-describedby`) · `function VendorCustomField(…)` 시그니처 줄(`error` prop) · 선택형 분기의 선택지 렌더 줄(「(보관됨)」 옵션 · 오류 배선) · 07 Task 2: 감사 수정으로 바꾼 줄(Task 2 SUMMARY에 적힌 것만) | UI-SPEC 화면 3 변경 1~6 · 05 ④ · 06 Task 1·2 |
| `db/migrations/meta/_journal.json` | main 마지막 항목의 닫는 `}` 한 줄(쉼표가 붙음) | (a) 구조 검사가 판정 |
| `repositories/permissions.ts` | main의 `insertVisibilityIfAbsent`에 선택 `tx`가 없을 때만: 그 함수의 시그니처 줄과 쿼리 시작 줄(`await db` → `await tx`) — ②의 조정 | 공유 결정(선택 `tx` 보존) · 01 |
| `docs/design/SYSTEM.md` | §6-10 제목의 관리자 화면 수 줄 · 표 마스터 행 한 줄(끝에 「· 화면 항목」) | 09 |
| `test/unit/ui/role-menu.test.ts` · `test/e2e/admin-nav.spec.ts` | 화면 수를 말하는 단언·테스트 제목 줄(수 = main + 1) · 라벨 배열에 원소를 끼워 넣어 바뀐 줄이 있으면 그 줄 | 09 |
| `ui/shell/role-menu.ts` · `domain/permissions/menus.ts` · `domain/permissions/info-items.ts` · `domain/action-log/record.ts` · `domain/seed/index.ts` · `test/integration/leak-scan.test.ts` · `test/e2e/fixtures.ts` · `domain/archive/index.ts` · `repositories/archive.ts` · `app/(app)/admin/archive/actions.ts` | 없음 — 더하기만(verify가 `-` 줄 0을 자동 확인) | 01·03·04·06·08·09의 더하기만 규칙 |

⑥ **전체 게이트 한 번:** `flock /tmp/plant8-erp-test.lock bash -c 'pnpm db:reset:test && CI=true pnpm test'`.
- DB 준비 순서: `db:reset:test`(빈 DB) → 통합 globalSetup(`test/integration/global-setup.ts`)이 마이그레이션 → E2E globalSetup(`test/e2e/global-setup.ts` 14–30행)이 스키마 비우기 → 마이그레이션 → 기본 시드 → 각 스펙이 자기 픽스처.
- 실패하면 `systematic-debugging` → 수정 → 실패한 계층만 다시 돌린다(같은 잠금). 마지막에 전체 게이트를 한 번 더 돌린다. 이 반복은 SUMMARY에 적는다.

⑦ 이 태스크 뒤 `/ship` 전에 main이 움직였으면 ①~⑥을 다시 한다. SUMMARY 「다음 단계」에 적는다.
  </action>
  <verify>
    <automated>pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/custom-fields/field-definitions-migration.test.ts &amp;&amp; (test ! -f test/unit/db/migration-journal.test.ts || pnpm vitest run --project unit test/unit/db/migration-journal.test.ts) &amp;&amp; node -e 'const cp=require("child_process"),fs=require("fs");const m=JSON.parse(cp.execSync("git show origin/main:db/migrations/meta/_journal.json",{encoding:"utf8"})).entries;const h=JSON.parse(fs.readFileSync("db/migrations/meta/_journal.json","utf8")).entries;const last=h[h.length-1];const ok=h.length===m.length+1&amp;&amp;m.every((e,i)=>JSON.stringify(e)===JSON.stringify(h[i]))&amp;&amp;h.every((e,i)=>e.idx===i)&amp;&amp;/^[0-9]{4}_custom_field_admin$/.test(last.tag)&amp;&amp;Number(last.tag.slice(0,4))===last.idx;console.log(ok?"journal OK":"journal FAIL");process.exit(ok?0:1)' &amp;&amp; D="$(git diff origin/main -U0 -- ui/shell/role-menu.ts domain/permissions/menus.ts domain/permissions/info-items.ts domain/action-log/record.ts domain/seed/index.ts test/integration/leak-scan.test.ts test/e2e/fixtures.ts domain/archive/index.ts repositories/archive.ts "app/(app)/admin/archive/actions.ts")" &amp;&amp; test "$(printf '%s\n' "$D" | grep -c '^-[^-]')" = 0 &amp;&amp; N="$(git grep -c "export async function insertVisibilityIfAbsent" -- repositories/permissions.ts)" &amp;&amp; test "${N##*:}" = 1 &amp;&amp; F="$(git grep -A15 "export async function insertVisibilityIfAbsent" -- repositories/permissions.ts)" &amp;&amp; printf '%s\n' "$F" | grep -q "tx: DbOrTx" &amp;&amp; printf '%s\n' "$F" | grep -Eq '(await|return) tx(\.|$)'</automated>
    <fails_when>squawk가 위반을 보고하거나, 채움 순서 검사·04.1의 저널 연속 검사(있을 때) 중 failed가 1 이상이거나, `journal FAIL`이 찍히거나(main 항목이 바뀌거나 빠짐 · `idx` 빈자리 · 이 페이즈 항목이 마지막 하나가 아님 · 번호 ≠ `idx`), 더하기만 공유 파일에서 main 줄이 하나라도 사라졌거나, `insertVisibilityIfAbsent`가 한 벌이 아니거나 선택 `tx` 인자·`tx` 쿼리가 없다(② 조정이 main 모양만 남겼다)</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts</automated>
    <fails_when>failed 개수가 1 이상이다 — 특히 01의 생성 롤백 테스트(노출 행 INSERT 중간 실패 → 칸 정의·노출 행 0건)가 빨간색이면 병합 뒤 노출 행 쓰기가 트랜잭션 밖으로 나갔다(③(g) 뒤, (h) 전에 돈다)</fails_when>
    <automated>test "$(PGPASSWORD=erp flock /tmp/plant8-erp-test.lock psql -h 127.0.0.1 -U erp -d erp_test -tAc "SELECT count(*) FROM field_definitions WHERE id IN ('bf-1','bf-2') AND label = key AND version = 1 AND archived_options = '[]'::jsonb")" = 2</automated>
    <fails_when>③(h) 직후 결과가 2가 아니다 — 다시 만든 SQL이 기존 행에서 적용되지 않았거나(열 없음으로 psql 오류), `label`이 `key`로 채워지지 않았거나, `version`·`archived_options` 기본값이 틀렸다(⑥ 전에 돈다)</fails_when>
    <automated>test "$(PGPASSWORD=erp flock /tmp/plant8-erp-test.lock psql -h 127.0.0.1 -U erp -d erp_test -tAc "SELECT (SELECT count(*) FROM roles r WHERE NOT EXISTS (SELECT 1 FROM visibility_matrix v WHERE v.role_id = r.id AND v.info_item = 'cf.vendor.cf_bf000001')) || '|' || (SELECT count(*) FROM visibility_matrix WHERE role_id = 'role-pm' AND info_item = 'cf.vendor.cf_bf000001' AND visible = false) || '|' || (SELECT count(*) FROM visibility_matrix WHERE info_item LIKE 'cf.project.%')")" = "0|1|0"</automated>
    <fails_when>③(h) 직후 결과가 `0|1|0`이 아니다 — 첫 수가 1 이상이면 재생성에서 노출 행 채움(㉯)이 빠져 기존 거래처 칸이 그 계급에게 숨겨진다 · 둘째 수가 0이면 관리자가 끈 행을 채움이 덮었다(`ON CONFLICT DO NOTHING` 누락) · 셋째 수가 1 이상이면 거래처 밖 대상까지 채웠다(⑥ 전에 돈다)</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock bash -c 'pnpm db:reset:test &amp;&amp; CI=true pnpm test'</automated>
    <fails_when>종료 코드가 0이 아니거나 단위·통합·E2E 어느 계층이든 failed 개수가 1 이상이다 — 병합 뒤 이 페이즈 또는 Phase 4·병렬 페이즈 동작이 깨졌다(프로덕션 빌드 기준)</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 페이즈 마이그레이션 파일 이름의 번호가 `_journal.json`의 바로 앞 항목(main 마지막) + 1이다
    - 저장소 어디에도 이 페이즈가 손으로 정한 마이그레이션 번호가 없다. 플랜·SUMMARY·코드 모두 `pnpm db:generate` 결과만 인용한다
    - `git grep -c "export async function insertVisibilityIfAbsent" -- repositories/permissions.ts`가 1이다. 그 한 벌이 마지막 선택 인자 `tx: DbOrTx = db`를 받아 쿼리에 쓴다. 04-20 호출부(인자 둘)와 이 페이즈 호출부(인자 셋)가 모두 그대로다
    - 01의 생성 롤백 테스트가 병합·재생성 뒤 다시 돌아 초록이다(SUMMARY에 테스트 이름)
    - 저널 구조 검사가 `journal OK`다 — main 항목 전부 보존, `idx` 연속, 이 페이즈 항목이 마지막 하나
    - ③(h) 백필 검증이 따로 실행됐고, 기존 두 행의 조회 결과와 `\d field_definitions`, 노출 행 조회 결과(`0|1|0` — 전 계급 행 생성 · 끈 행 유지 · 거래처 한정)가 SUMMARY에 있다
    - 다시 만든 SQL에 표시 주석 `-- 04.5: 기존 거래처 칸 노출 행 채움`과 `INSERT INTO "visibility_matrix"` 문장이 하나씩 있다(첫 verify의 문장 순서 가드가 확인)
    - ⑤ 표의 「없음 — 더하기만」 파일들의 `git diff origin/main -U0`에 `-` 줄이 0이다
    - `vendor-form.tsx`·`repositories/permissions.ts`·`SYSTEM.md`·`role-menu.test.ts`·`admin-nav.spec.ts`의 `-` 줄이 모두 ⑤ 표의 승인 목록에 있고, 줄별 근거가 SUMMARY에 있다
    - `CI=true pnpm test`가 초록이다(SUMMARY에 계층별 통과 개수 요약)
    - 다섯 검증 명령이 전부 초록이다(순서: 정적·구조 검사 → 01 롤백 테스트 → ③(h) 백필 조회 → ③(h) 노출 행 조회 → 전체 게이트)
  </acceptance_criteria>
  <done>브랜치가 main을 품고, 이 페이즈 마이그레이션이 main 마지막 다음 번호로 다시 만들어졌으며, 공유 파일은 더하기만이고, 전체 게이트가 한 번에 초록이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 병렬 페이즈 브랜치 → 이 브랜치(병합) | 다른 페이즈의 공유 파일 변경이 충돌 해소 중 지워질 수 있다 |
| 마이그레이션 → 운영 DB 스키마 | 다시 만든 SQL이 채움 순서를 잃으면 기존 행에서 NOT NULL·unique가 실패한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-21 | Tampering | 병합 충돌 해소(공유 파일) | high | mitigate | 저널은 JSON 구조 검사(main 항목 보존 · `idx` 연속 · 이 페이즈 항목 마지막), 공유 파일은 `git diff origin/main`의 `-` 줄을 파일별 승인 목록과 대조한다 — 목록 밖 줄은 되살리거나 사용자 승인. 더하기만 파일은 `-` 줄 0을 자동 확인한다. `domain/archive/index.ts` 항목별 권한 세 곳이 남아 있는지 본다. 병합 뒤 전체 게이트를 돈다(Task 3) |
| T-04.5-22 | Denial of Service | 다시 만든 마이그레이션 | high | mitigate | 채움 순서를 다시 넣고 01의 순서 검사·squawk로 글자 순서를 본다. 빈 DB 적용(③(f))과 별도로, main까지 적용한 DB에 옛 모양 행 둘과 끈 노출 행 하나를 넣고 새 SQL을 적용하는 백필 검증(③(h))으로 기존 행이 있는 상태의 적용과 노출 행 채움(전 계급 · 끈 행 유지)을 실측한다. 04.1 저널 연속 검사(있을 때)와 저널 구조 검사를 돌린다 |
| T-04.5-07 | Tampering | 병합 뒤 `insertVisibilityIfAbsent`(칸 생성 원자성) | medium | mitigate | 04-20의 인자 둘 모양과 합칠 때 선택 `tx`와 쿼리의 `tx` 사용을 지킨다(Task 3 ②). verify가 시그니처·본문을 자동 확인하고, 01의 생성 롤백 테스트를 병합 뒤 다시 돌린다 |
| T-04.5-01 | Elevation of Privilege | 병합 뒤 권한 경로 | high | mitigate | 전체 게이트가 01·08·04의 비관리자 거부·복원 링크 권한·보관 이중 권한 (a)~(e) 테스트를 다시 돌린다 |
| T-04.5-04 | Information Disclosure | 여정의 끈 계급 화면 | medium | mitigate | 여정 E2E가 끈 계급의 거래처 HTML에서 라벨·값 문자열 부재를 실측한다(Task 1). 누수 스캔은 전체 게이트에 든다 |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — 병합이 들여온 main의 의존성 변경은 그대로 받고 이 페이즈는 더하지 않는다. `package.json` 의존성 객체의 이 페이즈 diff 없음 |
</threat_model>

<verification>
- SC-4 여정 E2E가 `CI=true`로 초록
- 독립 DOM 감사 보고서(세 폭 × backstop 여섯)가 SUMMARY에 있음
- 마이그레이션 번호 = main 마지막 + 1, 저널 연속, squawk 통과, 채움 순서 검사 통과
- `insertVisibilityIfAbsent` 한 벌(선택 `tx` 보존) · 01 생성 롤백 테스트 재실행 초록 · 저널 구조 검사 통과 · 공유 파일 `-` 줄이 승인 목록 안에만 있음
- 백필 검증(③(h)) — main까지 적용한 DB의 옛 모양 행 둘이 새 SQL 적용 뒤 `label = key`·`version = 1`, 기존 거래처 칸에 전 계급 노출 행이 생기고 끈 행은 `false`로 남음
- `flock /tmp/plant8-erp-test.lock bash -c 'pnpm db:reset:test && CI=true pnpm test'` 초록(머지 의식 뒤 한 번)
</verification>

<success_criteria>
- 관리 화면에서 만든 칸이 거래처 입력 화면에서 추가·보관·복원·계급별 끔까지 끝까지 동작한다
- 화면 backstop 여섯 항목이 세 폭에서 실측으로 통과하거나 디자인 리뷰로 넘겨졌다
- 이 페이즈 마이그레이션이 main 마지막 다음 번호이고 전체 게이트가 초록이다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 테스트 | `test/e2e/custom-field-journey.spec.ts`(ROADMAP 기준 4 여정) |
| 보고서 | 독립 DOM 감사 보고서(04.5-07 SUMMARY 안 — 별도 파일 없음) |
| 마이그레이션 | 이 페이즈 SQL·snapshot·저널 항목 — `pnpm db:generate --name custom_field_admin`이 붙인 번호(main 마지막 + 1) |
| 병합 | origin/main 병합 커밋(공유 파일 양쪽 추가 보존) |
| 디자인 리뷰 대기 | OPEN 1-A(08·02) · 2-A(02) · 1-B(06) · UI-SPEC 편차 OPEN 편차-04a·편차-04b(04)·편차-06a(06) + 감사에서 넘긴 항목 |

<output>
Create `.planning/phases/04.5-custom-field-admin/04.5-07-SUMMARY.md` when done
</output>
