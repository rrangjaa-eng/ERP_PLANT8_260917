---
phase: 04.5-custom-field-admin
plan: 08
type: execute
wave: 2
depends_on: ["04.5-01"]
files_modified:
  - domain/custom-fields/targets.ts
  - domain/custom-fields/admin-input.ts
  - domain/custom-fields/admin.ts
  - lib/actions/form-reason.ts
  - test/unit/domain/custom-fields-admin.test.ts
  - test/unit/actions/form-reason.test.ts
  - test/unit/custom-fields/field-definitions-migration.test.ts
  - test/integration/field-definitions-admin.test.ts
  - app/(app)/admin/field-definitions/actions.ts
  - app/(app)/admin/field-definitions/page.tsx
  - app/(app)/admin/field-definitions/field-definition-form.tsx
  - app/(app)/admin/field-definitions/field-definitions.module.css
  - test/e2e/field-definitions.spec.ts
  - test/e2e/fixtures.ts
autonomous: true
requirements: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "대상 등록부 `domain/custom-fields/targets.ts`(다른 모듈을 import하지 않는 순수 모듈)가 `FIELD_DEFINITION_TARGETS = [\"vendor\"]` · `customFieldInfoItem`/`parseCustomFieldInfoItem`(`cf.<entity>.<key>`) · 상한 상수 · `nextSortOrder`를 두고, 01이 `admin.ts`에 둔 모듈 상수·항목 키 함수는 이것으로 바뀐다. `INFO_ITEMS`의 어떤 키도 `cf.`로 시작하지 않는다(D10-13 노출표 두 출처의 키 공간 분리)"
    - "등록 폼의 빈 상태가 없다 — 칸이 항상 있고 타입 기본 「텍스트」, 정렬 순서 기본 = `min(활성 최대값 + 1, 999)`, 활성 정의가 없으면 1이다(UI-SPEC E2 empty, O16)"
    - "칸 오류는 서버가 판정해 해당 칸 아래 `Form.Error`로 보인다 — 이름 빈칸 「이름이 비어 있습니다 · 화면 항목 이름을 적어 주세요」, 정렬 순서 범위 밖 「0~999 사이 정수가 아닙니다 · 숫자 고치기」. 이름은 입력 `maxLength` 20 + 서버 20자 상한이다(domain도 같은 상수로 다시 판정). 오류 뒤에도 입력값이 남는다(UI-SPEC E2 error·long-text, UX-04)"
    - "이름은 보관된 칸까지 포함해 거래처 안에서 유일하다(D10-12 보관 규칙) — 활성 칸과 같으면 「같은 이름의 화면 항목이 이미 있습니다 · 이름 바꾸기」, 보관된 칸과 같으면 「보관함에 같은 이름의 화면 항목이 있습니다 · 보관함에서 복원」이고 「보관함에서 복원」은 복원에 필요한 두 쓰기 권한(`admin.archive` write · `admin.field-definitions` write)과 `admin.archive` view가 다 있을 때만 3차 링크(`/admin/archive`), 아니면 「보관함에 같은 이름의 화면 항목이 있습니다 · 이름 바꾸기」 평문이다(링크가 가리키는 `/admin/archive`는 `admin.archive` view 없이 `notFound()`다 — `app/(app)/admin/archive/page.tsx:15`, `can()`은 동작을 독립 판정한다). 서버는 `listFieldDefinitions` 조회로 두 경우를 가르고, 조회와 쓰기 사이 경합은 unique 위반을 같은 칸 오류로 바꾼다(UI-SPEC O22, E2 name-reserved, Codex 4차 MINOR = checker R2)"
    - "폼 전체 서버 오류는 `Form.Actions` 1차 옆 이유 자리에 「추가할 수 없음 — {serverError 원문} · 다시 시도」로 보이고 1차는 켜진 채다 — 원문에 이미 「 · 」가 있으면 「 · 다시 시도」를 붙이지 않는다. 폼 상단 오류 상자 컴포넌트는 쓰지 않는다. **재시도로 풀리지 않는 원인**(제출 중 권한 회수 → 원인 「권한 없음」)은 「추가할 수 없음 — 권한 없음 · 」 뒤에 3차 「새로 불러오기」(`reset()` + `router.refresh()`)가 오고 그동안 1차가 `disabled` + `disabledReason`이다 — 같은 실패를 다시 제출할 수 없다. 원인 구분은 클라이언트가 import하는 순수 모듈 `lib/actions/form-reason.ts`의 상수 비교다(UI-SPEC Copywriting 137행, 화면 1 권한 · 디자인 리뷰 U1-A)"
    - "제출 중에는 1차가 `pending`이고 입력 전체가 `fieldset disabled`로 잠기며 「취소」도 비활성이다(UI-SPEC E2 loading, O15)"
    - "등록이 성공하면 1차 버튼 자리가 결과 줄 「화면 항목 추가 · {이름} 추가됨」(`role=status`, 포커스를 받음)으로 바뀌고 입력은 읽기 전용이 되며, 3차 「정보 노출표 보기」(`/admin/visibility` — `can(viewer, \"admin.visibility\", \"view\")`일 때만, 1-C · D10-13 자동 등록 확인)·3차 「하나 더 추가」(폼을 key로 다시 마운트해 타입·정렬 기본값까지 초기화하고 이름 입력으로 포커스)·2차 「닫기」가 붙는다. 아래 목록은 서버 재검증으로 새 행을 보인다(UI-SPEC E2 success, O8)"
    - "목록 표는 흰 머리글의 읽기용 표(캡션 sr-only 「화면 항목」)이고 열은 이름(`th scope=row`)·타입·필수(「필수」/「—」 평문)·정렬·선택지·상태·동작이다. 정렬은 정렬 순서 오름차순, 같은 번호면 내부 키 순이다. 키는 어느 열에도 보이지 않는다. 셀 줄바꿈은 body 상속 `overflow-wrap: anywhere`이고 이 화면 CSS가 덮어쓰지 않는다. 이 플랜은 보관 행을 목록에서 뺀다(보관 포함 필터는 04) (UI-SPEC E1 populated·zero-one-many·long-text·overflow, R7)"
    - "마이그레이션 문장 순서 가드(`test/unit/custom-fields/field-definitions-migration.test.ts`)가 01의 손 편집을 지킨다 — `label` 채움이 NOT NULL 기본값 제거·unique `(entity, label)`보다 먼저, 기존 거래처 칸 노출 행 채움(`INSERT INTO \"visibility_matrix\"` … `ON CONFLICT (\"role_id\", \"info_item\") DO NOTHING`)이 그 뒤에 온다 · `version` NOT NULL 기본 1 · `archived_options` NOT NULL 기본 빈 배열. 07의 머지 직전 재생성에서 손 편집이 사라지면 빨개진다"
    - "E2E가 만든 칸은 끝에 항상 보관된다 — `test/e2e/fixtures.ts` 끝의 `archiveE2EFieldDefinitions(labelPrefix)`(03·04·06·07이 같은 이름으로 쓴다)를 `field-definitions.spec.ts`의 `afterAll`이 부른다"
  artifacts:
    - path: "domain/custom-fields/targets.ts"
      provides: "대상 상수 · 노출표 항목 키 규약(`cf.<entity>.<key>`) · 길이·개수 상한 · 정렬 기본값 — 다른 모듈을 import하지 않는 순수 모듈(클라이언트 폼도 import)"
      exports: ["FIELD_DEFINITION_TARGETS", "customFieldInfoItem", "parseCustomFieldInfoItem", "nextSortOrder", "FIELD_NAME_MAX", "OPTION_MAX_LENGTH", "ACTIVE_OPTIONS_MAX", "SORT_ORDER_MAX"]
    - path: "domain/custom-fields/admin-input.ts"
      provides: "생성 입력 스키마의 칸 오류 문구·상한과 이름 충돌 문구 선택 — DB를 import하지 않아 단위 테스트가 직접 검사한다"
      exports: ["createFieldDefinitionInput", "nameConflictMessage"]
    - path: "domain/custom-fields/admin.ts"
      provides: "관리 목록(권한 게이트) · 관리 DTO와 검토된 필드 목록 · 이름 예약 판정"
      exports: ["createFieldDefinition", "listFieldDefinitionsForAdmin", "FieldDefinitionAdminDto", "FIELD_DEFINITION_ADMIN_DTO_FIELDS", "DuplicateFieldNameError"]
    - path: "lib/actions/form-reason.ts"
      provides: "폼 전체 서버 오류 한 줄 규칙(순수, import 0개 — 클라이언트 폼이 import) — 재시도 불가 원인 표(원문 → 짧은 원인 · 다음 행동)에 있으면 「{동사}할 수 없음 — {짧은 원인} · 」 + `next: \"refresh\"|\"list\"` + `blocked: true`, 없으면 「{동사}할 수 없음 — {원문}」 + 원문에 「 · 」가 없을 때만 「 · 다시 시도」 + `next: \"retry\"` + `blocked: false`(02·06이 같은 표에 원인을 더한다)"
      exports: ["formReason", "PERMISSION_DENIED_CAUSE"]
    - path: "test/unit/custom-fields/field-definitions-migration.test.ts"
      provides: "마이그레이션 문장 순서 가드(07의 재생성 뒤 손 편집 재적용 확인)"
      min_lines: 20
    - path: "test/e2e/fixtures.ts"
      provides: "`archiveE2EFieldDefinitions(labelPrefix)` — 이 페이즈 E2E 정리"
      contains: "archiveE2EFieldDefinitions"
  key_links:
    - from: "app/(app)/admin/field-definitions/actions.ts"
      to: "domain/custom-fields/admin-input.ts"
      via: "`DuplicateFieldNameError` → `nameConflictMessage({ archived, canRestore })` → `returnValidationErrors(스키마, { name: { _errors: [문구] } })`"
      pattern: "returnValidationErrors"
---

<objective>
플랜 01 완성 — 01이 이은 등록 경로 위에 대상 등록부, 목록 표, 등록 폼의 모든 상태(빈 상태 기본값·칸 오류·이름 예약·폼 전체 이유 자리·제출 중 잠금·결과 줄), 관리 목록 DTO, 마이그레이션 순서 가드, E2E 정리 픽스처를 얹는다. 옛 01이 덮던 E1·E2 행과 must_have 중 01이 옮겨 보낸 것을 09와 나눠 받는다(01 `<probe_fallback>`의 목록과 같은 문장 — 메뉴 등록과 관리 인덱스 링크(정본 표·순서 테스트와 한 묶음), 액션 등록부와 `FieldDefinitionAdminDto`의 누수 스캔 계약은 09가 받는다).

Purpose: 09(메뉴·액션 등록부·누수 스캔 메뉴 게이트 DTO 축)와 02(선택형·수정)·03(노출 판정·누수 스캔 커스텀 축)이 이 플랜의 대상 등록부·입력 스키마·관리 목록 DTO·목록·폼 상태 위에 선다. D10-13(노출표 자동 등록)은 결과 줄의 「정보 노출표 보기」로, D10-12(보관)는 보관 칸까지 포함한 이름 예약으로 이 화면에 드러난다.

Output: `domain/custom-fields/targets.ts` · `admin-input.ts`·`admin.ts` 확장 · `lib/actions/form-reason.ts` · 목록 표와 완성된 등록 폼 · 단위·통합·E2E · 정리 픽스처.

**실행 규율:** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED를 실제로 확인 → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적는다. 한 세션에 플랜 하나. E2E 판정은 `CI=true`다. **공유 테스트 환경 직렬화(공유 결정 3):** 공유 테스트 DB(`erp_test`)·통합 테스트·`db:reset:test`·마이그레이션·E2E 서버 포트 3100·`next build`를 건드리는 명령은 전부 `flock /tmp/plant8-erp-test.lock` 접두로 돌린다(여러 명령을 이으면 `sh -c '…'`로 묶고, `CI=true` 같은 환경 변수 대입은 그 안이나 `env CI=true …`로 둔다 — flock은 첫 낱말을 실행 파일로 본다). 단위 테스트·lint·typecheck는 잠그지 않는다. **E2E DB 준비 순서(공유 결정 4):** `pnpm db:reset:test`(빈 DB를 만든다) → Playwright globalSetup `test/e2e/global-setup.ts`(12–30행: 스키마 비우기 → `migrate`로 마이그레이션 → `seedMasterData`로 기본 시드) → 스펙의 `beforeAll` 픽스처. `db:dev`는 DB 서버를 띄우고 없는 DB만 만든다. 화면 검증 순서는 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트의 `CI=true` 독립 DOM 감사(07에서 한 번에) → 수정 → 전체 게이트 한 번이다. 새 의존성 0개. `.planning/`은 GSD 도구로만 쓴다.

**병렬 페이즈 규칙(Phase 4 · 04.1~04.4와 나란히):** `domain/projects/*`·`domain/quotes/*`, `domain/permissions/project.ts`·`dto-registry.ts`, `domain/seed/index.ts`는 고치지 않는다. `test/e2e/fixtures.ts`에는 끝에 더하기만 한다. `listFieldDefinitions`는 바꾸지 않는다. STATE.md의 현재 페이즈(4)는 건드리지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-custom-field-admin/04.5-CONTEXT.md
@CLAUDE.md
@.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md
# 01-SUMMARY가 필요한 이유: 01이 쓴 손 편집 SQL 본문·`insertVisibilityIfAbsent` 경로(재사용/추가)를 이 플랜이 이어받는다(권한 준비 줄 위치는 09가 이어받는다).
# 범위 Read(절 이름으로 찾는다 — 줄 번호는 UI-SPEC 개정 3(75ae245) 기준):
# .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md 「Copywriting Contract」 114–150행 · 「화면 1」 211–237행 · 「화면 2」 239–265행(263–265행 SUCCESS·제출 중·이름 예약) · Assumptions O16·O22(357행~) — 「관리 인덱스」·O6은 09
# .planning/phases/04.5-custom-field-admin/04.5-PATTERNS.md — 파일별 가까운 선례
# docs/design/SYSTEM.md §6-1(관리자 마스터 목록) · §7-1(진행 바) · §7-15(폼, 「화면이 그대로인 폼」 1003행 부근) · §8(문구 규칙) — §6-10은 09
</context>

<probe_fallback>
⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). ROADMAP Phase 04.5의 `**Requirements**`는 TBD다(ADMN-07은 Phase 10에 남음). 수용 기준은 ROADMAP 04.5 기준 1·5, CONTEXT D10-12·D10-13과 Claude's Discretion, UI-SPEC(개정 3 승인본) `## UI Considerations`에서 끌어왔다.

**UI Considerations 행(옛 01에서 넘겨받음):** E1(목록)의 populated·zero-one-many·long-text·overflow(선택지 열 내용은 02가 채움) · E2(등록 폼)의 empty·error·loading·success·name-reserved. E5 auto-register와 등록 경로 자체는 01이 덮었다. backstop 여섯 줄은 DOM 감사를 하는 07이 옮겼다.

**계획 단계 결정(표면화):**

- **메뉴 등록 · 관리 인덱스 · 액션 등록부 · 권한 시드 수단 · `FieldDefinitionAdminDto`의 누수 스캔 계약 · 01의 권한 준비 줄 정리는 09(파일 수 상한 15 때문에 옛 Task 2를 그대로 옮김).** 이 플랜 동안 `admin.field-definitions`는 아직 `MENUS`에 없으므로 01의 권한 준비 줄은 그대로 둔다.
- **이름 예약 조회:** 새 리포지토리 함수를 만들지 않는다 — `listFieldDefinitions(viewer, "vendor")`(보관 포함 전 행, 거래처 칸은 수십 개 이하)에서 자른 이름과 같은 `label`을 찾는다. 경합으로 `isUniqueViolation(error, "field_definitions_entity_label_key")`가 나면 같은 조회로 다시 찾아 같은 오류로 바꾼다.
- **「다시 시도」는 글자다:** 폼 전체 서버 오류 줄의 「다시 시도」는 3차 버튼이 아니다(다음 행동은 켜져 있는 1차를 다시 누르는 것 — 화면 3 변경 5와 같은 결). 3차 버튼이 붙는 것은 UI-SPEC가 이름을 준 「새로 불러오기」·「목록으로」(02)·「보관함에서 복원」 링크와, 재시도로 풀리지 않는 원인의 「새로 불러오기」(U1-A)뿐이다.
- **재시도 불가 원인(디자인 리뷰 U1-A · D1):** 원인 구분은 `lib/actions/form-reason.ts`(import 0개)의 원인 표 하나다 — 원문과 정확히 같을 때만 짧은 원인·다음 행동으로 바꾼다. 이 플랜은 「권한 없음」(`PERMISSION_DENIED_CAUSE` — domain이 이 상수로 던진다)을 넣고, 02가 「화면 항목 없음」·보관된 화면 항목(다음 행동 「목록으로」), 06이 거래처 domain 원문 셋을 같은 표에 더한다. 이유 글자는 한 번만 보인다 — 막힌 동안에는 1차 `Button`의 `disabledReason`(`ui/button/Button.tsx` — 버튼 옆에 글자로 렌더한다, 02의 「선택지 0개」와 같은 모양)이 글자를 그리고 이유 자리 span에는 3차 버튼만 둔다.

**디자인 리뷰 해소(docs/designs/plant8-erp-phase04.5-design-review-260925.md R1~R9 + Codex 대조) — OPEN 2 권한 시드 수단은 09:**

1. **(1-A) 권한 회수 뒤 「다시 시도」 → U1-A(사용자 결정):** 「추가할 수 없음 — 권한 없음 · 」 + 3차 「새로 불러오기」 + 1차 `disabled`·`disabledReason`(위 결정, Task 1 ⑥ · Task 2 ④).
3. **「다시 시도」의 모양 → R7 수용:** 글자 그대로(위 결정).
</probe_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 대상 등록부 · 입력 규칙과 칸 오류 문구 · 이름 예약 · 관리 목록 DTO · 폼 이유 규칙 · 마이그레이션 순서 가드(domain 계약)</name>
  <files>domain/custom-fields/targets.ts, domain/custom-fields/admin-input.ts, domain/custom-fields/admin.ts, lib/actions/form-reason.ts, test/unit/domain/custom-fields-admin.test.ts, test/unit/actions/form-reason.test.ts, test/unit/custom-fields/field-definitions-migration.test.ts, test/integration/field-definitions-admin.test.ts</files>
  <read_first>
    - 01이 만든 `domain/custom-fields/admin-input.ts`·`admin.ts`·`test/integration/field-definitions-admin.test.ts` · 01이 손 편집한 `db/migrations/*_custom_field_admin.sql`(전문)
    - `domain/permissions/info-items.ts`(전문) · `domain/code-tables/tax-rule.ts`(DB 없는 순수 모듈 + 단위 테스트 선례)
    - `repositories/field-definitions.ts`(`listFieldDefinitions`·`FieldDefinitionRow`) · `lib/pg-errors.ts`의 `isUniqueViolation` · `lib/actions/user-facing-error.ts`
    - `app/(app)/admin/vendors/actions.ts` 14–40행(이름 빈칸 zod 메시지 관례) · `lib/actions/handle-server-error.ts`(UserFacingError 메시지 통과 · 일반 문구)
    - UI-SPEC 「Copywriting Contract」 화면별 카피 표(124–150행) · 「화면 2」 이름 예약(265행)
  </read_first>
  <behavior>
    - (단위) `customFieldInfoItem("vendor", "cf_1a2b3c4d")` = `"cf.vendor.cf_1a2b3c4d"`, `parseCustomFieldInfoItem`이 그 역을 돌려주고 `cf.` 접두가 아니거나 조각이 셋이 아니면 null · `FIELD_DEFINITION_TARGETS`는 `["vendor"]`
    - (단위) `INFO_ITEMS`의 어떤 키도 `cf.`로 시작하지 않는다(항목 키 공간 불변식)
    - (단위) `nextSortOrder([])` = 1, `nextSortOrder([1, 5])` = 6, `nextSortOrder([999])` = 999
    - (단위) `createFieldDefinitionInput`이 빈 이름(공백만 포함)을 「이름이 비어 있습니다 · 화면 항목 이름을 적어 주세요」로, 21자 이름을 거부하고, 정렬 -1·1000·1.5·숫자 아닌 값을 「0~999 사이 정수가 아닙니다 · 숫자 고치기」로 거부한다 · `entity` 키와 `type: "select"`는 여전히 거부한다
    - (단위) `nameConflictMessage({ archived: false })` = 「같은 이름의 화면 항목이 이미 있습니다 · 이름 바꾸기」 · `{ archived: true, canRestore: true }` = 「보관함에 같은 이름의 화면 항목이 있습니다 · 보관함에서 복원」 · `{ archived: true, canRestore: false }` = 「보관함에 같은 이름의 화면 항목이 있습니다 · 이름 바꾸기」
    - (단위, U1-A) `formReason("추가", PERMISSION_DENIED_CAUSE)` = `{ text: "추가할 수 없음 — 권한 없음 · ", next: "refresh", blocked: true }`(「다시 시도」가 없다 — 1차 비활성) · `formReason("추가", "처리 중 오류가 발생했습니다 · 잠시 후 다시 시도해 주세요")` = 그 원문 그대로 「 · 다시 시도」 없이 `next: "retry"`, `blocked: false` · 표에 없는 원문 「알 수 없는 실패」 = `{ text: "추가할 수 없음 — 알 수 없는 실패 · 다시 시도", next: "retry", blocked: false }`(1차 켜짐) · `PERMISSION_DENIED_CAUSE`는 「권한 없음」 · `lib/actions/form-reason.ts`의 import 문이 0개(클라이언트 import 가능)
    - (통합) 기획 PM의 `createFieldDefinition` 거부 메시지가 `PERMISSION_DENIED_CAUSE`와 같다(폼의 상수 비교가 실제 서버 문구와 맞는다)
    - (단위) 마이그레이션 가드: `db/migrations/*_custom_field_admin.sql`이 정확히 하나이고, `UPDATE "field_definitions" SET "label" = "key"`가 `label`의 `DROP DEFAULT`와 `field_definitions_entity_label_key` 추가보다 앞, `INSERT INTO "visibility_matrix"`가 그 둘보다 뒤이며 `ON CONFLICT ("role_id", "info_item") DO NOTHING`을 담는다 · `version`은 NOT NULL 기본 1 · `archived_options`는 NOT NULL 기본 빈 배열
    - (통합) 이름 앞뒤 공백을 자르고, 활성 칸과 같은 이름은 `DuplicateFieldNameError { archived: false }`, `archived_at`을 직접 채운 보관 칸과 같은 이름은 `{ archived: true }`로 거부한다 — 둘 다 칸 정의·노출 행·로그가 늘지 않는다
    - (통합) 이름 조회를 건너뛰게 주입해 unique `(entity, label)` 위반을 일으키면 같은 `DuplicateFieldNameError`로 바뀐다(경합 경로)
    - (통합) 액션을 거치지 않고 `createFieldDefinition`에 21자 이름이나 정렬 1000을 주면 거부된다(domain 재판정)
    - (통합) `listFieldDefinitionsForAdmin`이 시스템 관리자에게 거래처 정의 전부(보관 포함, `archived` 참/거짓)를 `sortOrder`, `key` 순으로 돌려주고, 돌려준 DTO의 키 집합이 `FIELD_DEFINITION_ADMIN_DTO_FIELDS`와 같으며, 기획 PM에게는 `ForbiddenError`다
  </behavior>
  <action>
① RED: behavior의 단위·통합을 먼저 쓰고 빨간 것을 확인한다. 마이그레이션 가드는 01의 손 편집을 지키는 회귀 가드라 처음부터 초록일 수 있다 — 그 경우 SQL 사본 문자열에서 채움 줄을 지운 입력으로 같은 판정 함수가 실패하는 케이스를 하나 두어 가드가 살아 있음을 보인다.

② **대상 등록부.** `domain/custom-fields/targets.ts`(다른 모듈을 import하지 않는다): `FIELD_DEFINITION_TARGETS = ["vendor"] as const`와 그 타입, `customFieldInfoItem(entity, key)`·`parseCustomFieldInfoItem(item)`, 상한 상수 `FIELD_NAME_MAX = 20`·`OPTION_MAX_LENGTH = 40`·`ACTIVE_OPTIONS_MAX = 30`·`SORT_ORDER_MAX = 999`, `nextSortOrder(activeSortOrders)`(= `min(최대값 + 1, SORT_ORDER_MAX)`, 빈 배열이면 1). `domain/custom-fields/admin.ts`에서 01이 둔 모듈 상수·비공개 항목 키 함수를 지우고 이것을 import한다(대상은 `FIELD_DEFINITION_TARGETS[0]`).

③ **입력 스키마 = 칸 오류의 단일 출처.** `admin-input.ts`의 `createFieldDefinitionInput`에서 `name`은 앞뒤 공백 자르기 → 빈칸이면 Copywriting 원문 「이름이 비어 있습니다 · 화면 항목 이름을 적어 주세요」 → `FIELD_NAME_MAX` 초과 거부, `sortOrder`는 `SORT_ORDER_MAX` 안의 0 이상 정수가 아니면 「0~999 사이 정수가 아닙니다 · 숫자 고치기」(숫자로 바꿀 수 없는 입력 포함). 문구는 Copywriting 표의 원문만 쓰고 새 문구를 만들지 않는다. 같은 파일에 이름 충돌 문구 상수 셋과 순수 `nameConflictMessage({ archived, canRestore })`. domain `createFieldDefinition`도 이름 자르기·길이·정렬 범위를 같은 상수로 다시 판정한다(액션 밖 호출자 방어).

④ **이름 예약(UI-SPEC O22 · D10-12).** domain은 트랜잭션 전에 `listFieldDefinitions(viewer, "vendor")`에서 자른 이름과 같은 `label` 행을 찾아 있으면 `DuplicateFieldNameError`(UserFacingError, `archived: boolean` 필드)를 던진다. 01의 키 충돌 재시도 안에서 `isUniqueViolation(error, "field_definitions_entity_label_key")`가 나면 같은 조회로 다시 찾아 같은 오류로 바꾼다(재시도하지 않는다). 이름 조회는 `deps`로 주입 가능하게 둔다(경합 테스트용).

⑤ **관리 목록 DTO.** `FIELD_DEFINITION_ADMIN_DTO_FIELDS = ["id", "key", "label", "type", "options", "archivedOptions", "required", "sortOrder", "version", "archived"] as const`와 `FieldDefinitionAdminDto`(행 타입을 그대로 돌려주지 않고 다시 매핑 — `plant8/no-row-type-escape`; `archived`는 `archivedAt !== null`). `listFieldDefinitionsForAdmin(viewer, deps?)`: `can(viewer, "admin.field-definitions", "view")` 아니면 `ForbiddenError`, 거래처 정의 전부(보관 포함, 화면이 거른다)를 `listFieldDefinitions` 순서 그대로.

⑥ **폼 이유 규칙(디자인 리뷰 U1-A · D1).** `lib/actions/form-reason.ts`(import 0개 — 클라이언트 폼이 import한다)에 원인 상수 `PERMISSION_DENIED_CAUSE = "권한 없음"`, 파일 안 재시도 불가 원인 표(원문 → `{ cause, next: "refresh" | "list" }` — 이 플랜은 `PERMISSION_DENIED_CAUSE` → `{ cause: "권한 없음", next: "refresh" }` 한 항목), 순수 `formReason(verb, serverError)` → `{ text, next, blocked }`. 원문이 표에 **정확히** 있으면 `text` = 「{verb}할 수 없음 — {cause} · 」, 표의 `next`, `blocked: true`. 없으면 `text` = 「{verb}할 수 없음 — {serverError}」 + 원문에 「 · 」가 없을 때만 「 · 다시 시도」, `next: "retry"`, `blocked: false`. `blocked`가 곧 1차 비활성이다. `domain/custom-fields/admin.ts`의 쓰기 권한 거부를 01의 리터럴 「권한 없음」 대신 `new ForbiddenError(PERMISSION_DENIED_CAUSE)`로 바꾼다(같은 글자 — 문구 변경 없음, 상수 하나가 서버 문구와 폼 비교의 출처다). 06이 같은 파일에 `fieldErrorsReason`을, 02·06이 원인 표에 항목을 더한다.

⑦ **마이그레이션 가드.** `test/unit/custom-fields/field-definitions-migration.test.ts`가 behavior의 순서·열 규칙을 SQL 문자열 위치로 판정한다(판정 함수는 테스트 파일 안에 둔다). 파일 이름·위치는 07의 머지 직전 의식이 부르는 그대로다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·tsc 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — 경계 규칙(`targets.ts`의 import 0개, domain → db 직접 import), 행 타입 유출 규칙 포함</fails_when>
    <automated>pnpm vitest run --project unit test/unit/domain/custom-fields-admin.test.ts test/unit/actions/form-reason.test.ts test/unit/custom-fields/field-definitions-migration.test.ts test/unit/import-cycles.test.ts</automated>
    <fails_when>vitest unit의 failed 개수가 1 이상이다 — 항목 키 규약·키 공간 불변식·정렬 기본값·칸 오류 문구·이름 충돌 문구·이유 자리 규칙·마이그레이션 순서 가드·순환 import 중 하나가 깨졌다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/field-definitions-admin.test.ts'</automated>
    <fails_when>통합 failed 개수가 1 이상이다 — 이름 예약(활성·보관·경합)·domain 재판정·관리 목록 게이트·DTO 키 집합·01의 트랜잭션·롤백·채움 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `domain/custom-fields/targets.ts`의 import 문이 0개다(`grep -c "^import" domain/custom-fields/targets.ts`가 0) · `grep -c "^import" lib/actions/form-reason.ts`도 0이다
    - `grep -v '^\s*//' domain/custom-fields/admin.ts | grep -c 'ForbiddenError("권한 없음")'`가 0이고 `PERMISSION_DENIED_CAUSE`를 쓴다(U1-A — 서버 문구와 폼 비교의 출처가 하나)
    - `domain/custom-fields/admin.ts`에 01이 둔 대상 모듈 상수가 없고 대상은 `FIELD_DEFINITION_TARGETS`에서 온다
    - 폼·액션에 나갈 사용자 문구가 전부 UI-SPEC Copywriting 표의 원문이다(SUMMARY에 문구 ↔ 표 행 대조표)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>칸 정의의 대상·키 규약·상한·기본값이 한 순수 모듈에 있고, 잘못된 이름·정렬과 보관된 이름까지 포함한 이름 충돌이 domain에서 정확한 문구의 오류로 갈리며, 관리 목록이 권한 뒤에서 검토된 필드만 돌려준다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 목록 표 · 등록 폼 완성 — 빈 상태 기본값 · 칸 오류 · 이름 예약(「보관함에서 복원」은 두 쓰기 권한 + `admin.archive` view가 다 있을 때만) · 폼 전체 이유 자리(재시도 불가 원인 = 「새로 불러오기」 + 1차 비활성, U1-A) · 제출 중 잠금 · 결과 줄(1-C, 「하나 더 추가」) · E2E 정리 픽스처</name>
  <files>app/(app)/admin/field-definitions/actions.ts, app/(app)/admin/field-definitions/page.tsx, app/(app)/admin/field-definitions/field-definition-form.tsx, app/(app)/admin/field-definitions/field-definitions.module.css, test/e2e/field-definitions.spec.ts, test/e2e/fixtures.ts</files>
  <read_first>
    - 01·Task 1이 만든 `actions.ts`·`page.tsx`·`field-definition-form.tsx`·`field-definitions.spec.ts`(이 세션에서 쓴 그대로 — 다시 읽지 않아도 되면 건너뛴다)
    - `node_modules/next-safe-action/dist/index.d.mts`의 `returnValidationErrors` 선언(99행) — 칸별 오류를 액션 결과 `validationErrors.<칸>._errors`로 돌려주는 방법
    - `app/(app)/admin/vendors/page.tsx`(목록 표·캡션·`th scope`·필터 줄) · `app/(app)/admin/vendors/vendors.module.css`(흰 머리글·`--cell-pad-y/x`·행 hover) · `docs/design/tokens.css`
    - `ui/form/Form.tsx`(`Form.Error`는 children을 받는다) · `ui/input/TextField.tsx`(`error`는 문자열, `aria-invalid`·`aria-describedby` 배선) · `ui/button/Button.tsx`(`pending`·`disabledReason`) · `ui/status-tag/*`(StatusTag `muted`)
    - `docs/design/SYSTEM.md` §7-1(진행 바 300ms) · §7-15 「화면이 그대로인 폼」(1003행 부근) · 672·993·1000행
    - UI-SPEC 「화면 1」 211–237행(열·필수 평문·「화면 항목 추가」 표시 조건) · 「화면 2」 263–265행(SUCCESS·제출 중·이름 예약)
    - `test/e2e/fixtures.ts`(전문) · `db/client.ts`의 `db`
  </read_first>
  <behavior>
    - (E2E) 목록: 시스템 관리자가 만든 칸이 흰 머리글 표의 이름 열(`th scope=row`)에 보이고, 캡션 「화면 항목」이 sr-only이며, 타입 「텍스트」·필수 「—」·정렬 번호·선택지 「—」·상태 「—」 칸이 있고, 키 문자열(`cf_` + 16진 8자)은 표 어디에도 없다 · 20자 이름(`E2E칸` + 16진 16자) 행의 이름 셀 computed `overflow-wrap`이 `anywhere`다(body 상속 — 이 화면 CSS가 덮어쓰지 않는다)
    - (E2E) 빈 상태: `?new=1`을 열면 타입이 「텍스트」, 정렬 순서가 활성 최대값 + 1이다
    - (E2E) 이름 빈칸으로 제출하면 이름 칸 아래 「이름이 비어 있습니다 · 화면 항목 이름을 적어 주세요」, 정렬 순서 1000이면 「0~999 사이 정수가 아닙니다 · 숫자 고치기」, 이미 있는 이름이면 「같은 이름의 화면 항목이 이미 있습니다 · 이름 바꾸기」 — 입력값은 그대로 남는다
    - (E2E) 보관된 칸(`archiveE2EFieldDefinitions`로 보관)과 같은 이름이면 시스템 관리자에게 「보관함에 같은 이름의 화면 항목이 있습니다 · 」 + 링크 「보관함에서 복원」(`/admin/archive`)
    - (E2E write-only) 임시 계급(`insertRole` + `upsertPermission`으로 `admin.field-definitions` view·write와 `admin.archive` write만 주고 `admin.archive` view는 주지 않는다 — `vendors.spec.ts` 72행 선례)의 사용자가 같은 보관 이름으로 등록하면 이름 칸 아래는 「보관함에 같은 이름의 화면 항목이 있습니다 · 이름 바꾸기」 평문이고 「보관함에서 복원」 링크가 없다(`admin.archive` view 없이는 `/admin/archive`가 `notFound()`이므로 — `app/(app)/admin/archive/page.tsx:15`)
    - (E2E 권한 회수 — U1-A) 같은 방식(`insertRole` + `upsertPermission`)으로 따로 만든 임시 계급(`admin.field-definitions` view·write)의 사용자가 `?new=1`을 연 채 픽스처가 그 계급의 `admin.field-definitions` write를 `upsertPermission(allowed: false)`로 끈 뒤 이름을 적어 「화면 항목 추가」 → 1차 옆에 「추가할 수 없음 — 권한 없음 · 」와 3차 「새로 불러오기」가 한 번씩 보이고(「추가할 수 없음」 글자 1개), 1차가 `toBeDisabled()`이며, 폼 안에 「다시 시도」 글자가 0개다 → 「새로 불러오기」 → 쓰기 권한이 없어 폼이 없는 목록이다(이름 입력 0개, 404 아님)
    - (E2E) 성공하면 결과 줄 「화면 항목 추가 · {이름} 추가됨」에 포커스가 가 있고 입력이 읽기 전용이며, 시스템 관리자에게 「정보 노출표 보기」·「하나 더 추가」·「닫기」가 보이고 아래 목록에 새 행이 있다. 「하나 더 추가」를 누르면 이름이 비고 타입이 「텍스트」로 돌아오며 정렬 순서가 새 기본값이고 이름 입력에 포커스가 있다
    - (E2E) 기획 PM이 `/admin/field-definitions`와 `?new=1`에서 404를 받는다(01 케이스 유지)
    - (E2E) 이 스펙의 칸 이름은 전부 `E2E칸` 접두(20자 이하)이고, `afterAll`이 `archiveE2EFieldDefinitions("E2E칸")`을 불러 이 스펙이 만든 칸(01의 트레이서 칸 포함)만 보관한다 — 다른 스펙은 자기 접두를 쓴다
  </behavior>
  <action>
① RED: behavior의 E2E를 먼저 쓰고 빨간 것을 확인한다. `test/e2e/fixtures.ts` **끝에** `archiveE2EFieldDefinitions(labelPrefix)`를 더한다 — DB 클라이언트(`@/db/client`의 `db`)로 `label`이 접두로 시작하고 아직 보관되지 않은 행의 `archived_at`을 지금으로, `version`을 1 올린다. 이 페이즈의 E2E는 만든 칸을 끝에 항상 보관한다(뒤 스펙 — 거래처·노출표 — 에 칸이 남지 않게).

② **액션.** `actions.ts`가 `DuplicateFieldNameError`를 잡아 `canRestore = can(viewer, "admin.archive", "view") && can(viewer, "admin.archive", "write") && can(viewer, "admin.field-definitions", "write")`로 `nameConflictMessage`를 고르고 `returnValidationErrors(createFieldDefinitionInput, { name: { _errors: [문구] } })`로 돌려준다(보관 이름 링크 조건 — Codex 4차 MINOR·checker R2, 복원에 필요한 두 쓰기 권한 + 리비전 검토: 링크가 가리키는 `/admin/archive`는 `admin.archive` view 없이 `notFound()`이므로(`app/(app)/admin/archive/page.tsx:15`, `can()`은 동작을 독립 판정한다 — `domain/permissions/can.ts:27`) view도 함께 검사한다). 그 밖의 `UserFacingError`는 기존 서버 오류 경로(`handle-server-error.ts`)로 원문이 나간다.

③ **목록(UI-SPEC 화면 1 · E1).** `page.tsx`: `listFieldDefinitionsForAdmin(viewer)`에서 보관 행을 뺀 목록(보관 포함 필터는 04)을 흰 머리글 읽기용 표로 — 캡션 sr-only 「화면 항목」, 열 일곱: 이름(`th scope="row"`) · 타입(텍스트/숫자/날짜/선택 한글) · 필수(「필수」/「—」 평문) · 정렬 · 선택지(타입이 선택이면 활성 선택지 콤마 목록, 아니면 「—」) · 상태(보관이면 「보관됨」 StatusTag `muted`, 아니면 「—」) · 동작(이 플랜에서는 비움 — 「수정」은 02, 「삭제」는 04). 등록 폼의 정렬 기본값은 활성 정의의 `sortOrder`로 `nextSortOrder`를 계산해 넘긴다. `canViewVisibility = can(viewer, "admin.visibility", "view")`를 폼에 넘긴다. `field-definitions.module.css`는 토큰만(새 색·간격·radius 금지) — 흰 머리글·`--cell-pad-y/x`·행 hover `--surface`, 셀 줄바꿈 속성을 덮어쓰지 않는다.

④ **폼(UI-SPEC 화면 2).** 이름 입력 `maxLength={FIELD_NAME_MAX}`. 칸 오류는 `validationErrors`의 칸 키로 찾아 각 칸의 `error`로 보인다. 이름 오류가 「보관함에서 복원」 변형 상수와 같으면 `TextField`의 문자열 `error` 대신 이름 칸 오류 줄을 `Form.Error`(children)로 그려 「보관함에 같은 이름의 화면 항목이 있습니다 · 」 뒤에 3차 링크 「보관함에서 복원」(`/admin/archive`)을 둔다 — `aria-invalid`·`aria-describedby` 배선은 `TextField`와 같게 맞춘다. 폼 전체 서버 오류(`serverError`)는 `formReason("추가", serverError)`로 한 번 판정한다. `blocked`가 거짓이면 `Form.Actions` 안 1차 버튼 옆 이유 자리(`--fs-sm --danger` span, 1차 `aria-describedby`가 가리킴)에 `text`를 글자로 쓰고 1차는 켜진 채다(「다시 시도」는 버튼이 아니다 — R7). **`blocked`가 참이면(U1-A — 권한 회수)** 1차에 `disabled` + `disabledReason={text}`(「추가할 수 없음 — 권한 없음 · 」 — `Button`이 버튼 옆에 글자로 그린다)를 주고, 이유 자리 span에는 글자 없이 3차 「새로 불러오기」만 둔다(누르면 `useAction`의 `reset()`으로 결과를 지우고 `router.refresh()` — 쓰기 권한이 없으면 page가 폼 없는 목록을, 보기 권한까지 없으면 404를 그린다). 같은 글자를 이유 자리와 `disabledReason`에 두 번 쓰지 않는다. 폼 상단 오류 상자 컴포넌트(`ui/form-alert`)는 import하지 않는다. 제출 중에는 1차 `pending`, 입력 전체를 감싼 `fieldset`에 `disabled`, 2차 「취소」도 비활성(링크면 비활성 표현이 되는 버튼 모양으로) — 300ms 넘으면 §7-1 진행 바(`Button`이 이미 하면 그대로 쓴다). 성공(SYSTEM.md §7-15 「화면이 그대로인 폼」): 01의 `router.replace` 닫기 대신 1차 버튼 자리가 결과 줄 「화면 항목 추가 · {이름} 추가됨」(`role="status"`, `tabIndex={-1}`, 성공 직후 포커스)으로 바뀌고 입력은 읽기 전용, 뒤에 3차 링크 「정보 노출표 보기」(`/admin/visibility` — `canViewVisibility`일 때만, **1-C 채택**, D10-13 자동 등록을 바로 확인하는 길) · 3차 「하나 더 추가」 · 2차 「닫기」(`router.replace("/admin/field-definitions")`). 「하나 더 추가」는 폼을 감싼 클라이언트 쪽에서 `key`를 바꿔 다시 마운트하고(React 상태까지 초기화) 이름 입력으로 포커스를 보낸다 — 정렬 기본값은 재검증된 page가 넘긴 새 값이다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error를 출력하고 종료 코드가 0이 아니다 — stylelint 토큰 규칙(새 색·간격·radius) 포함</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/field-definitions.spec.ts test/e2e/admin-nav.spec.ts test/e2e/vendor-edit.spec.ts test/e2e/vendors.spec.ts'</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 목록 열·키 비노출·줄바꿈, 빈 상태 기본값, 칸 오류 문구, 보관 이름 복원 링크, 권한 회수 뒤 「새로 불러오기」·1차 비활성·「다시 시도」 부재, 결과 줄 포커스·읽기 전용, 「하나 더 추가」 초기화, 기획 PM 404, 정리 픽스처, 기존 거래처 화면 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' "app/(app)/admin/field-definitions/field-definition-form.tsx" | grep -c "FormAlert"`가 0이다(주석 줄 제외)
    - 「정보 노출표 보기」 렌더가 `admin.visibility` 보기 권한 판정에 묶여 있다(1-C) · 「보관함에서 복원」 링크가 `admin.archive` view·write와 `admin.field-definitions` write 세 판정에 묶여 있다 — write만 있고 `admin.archive` view가 없는 계급에는 링크가 없음을 E2E write-only 케이스가 확인한다
    - OPEN 1·3은 SUMMARY에 「디자인 리뷰 해소(docs/designs/plant8-erp-phase04.5-design-review-260925.md R1~R9 + Codex 대조)」로 적는다 — 1-A = U1-A, 3 = R7(OPEN 2는 09) · 「디자인 리뷰 대기」 목록에 올리지 않는다
    - `grep -c "export async function archiveE2EFieldDefinitions" test/e2e/fixtures.ts`가 1이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>관리자가 목록에서 칸을 보고, 칸을 추가할 때 잘못 적거나 보관된 이름과 겹치면 그 칸 아래 이유와 다음 한 수를 보며, 성공하면 결과와 다음 한 수를 같은 자리에서 보고, 이 페이즈의 E2E는 만든 칸을 끝에 보관한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `createFieldDefinitionAction` | 이름·정렬이 서버로 들어온다 — 요청 몸통은 위조될 수 있다 |
| 관리 화면 → 목록·거래처 폼 | 관리자가 적은 이름이 표 셀·라벨로 그대로 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-09 | Information Disclosure | `FieldDefinitionAdminDto`(이름·선택지·키) | low | mitigate | 사람 단위 값이 없는 구조 메타데이터이고 `admin.field-definitions` view 뒤에서만 나간다(기획 PM `ForbiddenError` 통합). 키는 화면 어느 열에도 렌더하지 않는다(E2E) — Task 1·2. 누수 스캔의 명시적 검사 경로(검토된 필드 목록 + 계급별 게이트, Codex #6)와 `MENUS` 등록(T-04.5-01)은 09 |
| T-04.5-15 | Tampering | 이름 예약(같은 이름 두 칸) | medium | mitigate | DB unique `(entity, label)`(보관 포함) + 조회로 활성·보관 구분 + 경합 unique 위반을 같은 칸 오류로 — 뒤의 공용 `restore()`가 같은 이름 활성 칸 둘을 만들 수 없다(04가 복원 경로를 증명) — Task 1 |
| T-04.5-52 | Tampering | 액션 밖 호출자(domain 직접 호출) | medium | mitigate | domain `createFieldDefinition`이 이름 자르기·20자·정렬 0~999를 입력 스키마와 같은 상수로 다시 판정(통합 테스트) — Task 1 |
| T-04.5-53 | Elevation of Privilege | 「보관함에서 복원」 링크 | low | mitigate | 링크는 `admin.archive` view·write와 `admin.field-definitions` write가 모두 있을 때만 렌더(view가 없으면 링크가 가리키는 `/admin/archive`가 `notFound()`이므로 — `app/(app)/admin/archive/page.tsx:15`, `can()`은 동작을 독립 판정하므로 write만으로 view를 가정할 수 없다) — 링크는 이동뿐이고 복원 자체의 이중 권한은 04가 서버에서 검사한다 — Task 2 |
| T-04.5-10 | Tampering | 이름 문자열 렌더(목록·거래처 폼 라벨) | low | mitigate | React 텍스트 노드로만 렌더(HTML 삽입 경로 없음), 이름 20자 입력·서버 상한 — Task 1·2 |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — `package.json` 의존성 객체와 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과(`field-definitions-migration`·`custom-fields-admin`·`form-reason` 포함)
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration …'`로 `field-definitions-admin` 통과
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && CI=true pnpm playwright test …'`로 E2E 네 파일(`field-definitions` · `admin-nav` · `vendor-edit` · `vendors`) 통과 — DB 준비는 `db:reset:test` → globalSetup(마이그레이션 → 기본 시드) → 스펙 `beforeAll` 픽스처 순서(공유 결정 4)
- 공유 파일 diff가 「끝에 더함」뿐이다: `git diff origin/main -- test/e2e/fixtures.ts`에 삭제 줄(`^-[^-]`)이 없다
- `package.json` 의존성 객체·`pnpm-lock.yaml` 변경 없음
</verification>

<success_criteria>
- 등록 폼이 빈 상태 기본값·칸 오류·이름 예약·이유 자리·잠금·결과 줄을 UI-SPEC 문구 그대로 보이고, 목록 표가 UI-SPEC 열 그대로다
- 01의 마이그레이션 손 편집이 단위 가드로 지켜진다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 모듈(신규, domain) | `domain/custom-fields/targets.ts`(`FIELD_DEFINITION_TARGETS` · `customFieldInfoItem` · `parseCustomFieldInfoItem` · `nextSortOrder` · 상한 상수 넷) |
| 모듈(신규, lib) | `lib/actions/form-reason.ts`(`formReason` → `{ text, next, blocked }` · `PERMISSION_DENIED_CAUSE` · 재시도 불가 원인 표) |
| 함수·타입(신규, domain) | `listFieldDefinitionsForAdmin` · `FieldDefinitionAdminDto` · `FIELD_DEFINITION_ADMIN_DTO_FIELDS` · `DuplicateFieldNameError`(`admin.ts`) · `nameConflictMessage`(`admin-input.ts`) |
| 변경된 기존 심볼 | `createFieldDefinitionInput`(칸 오류 문구·상한) · `createFieldDefinition`(이름 예약·재판정·대상 등록부) |
| 화면(변경) | `/admin/field-definitions` — 목록 표 · 완성된 등록 폼(빈 상태·오류·이름 예약·이유 자리·잠금·결과 줄) · `field-definitions.module.css` |
| 테스트 | `test/unit/domain/custom-fields-admin.test.ts` · `test/unit/actions/form-reason.test.ts` · `test/unit/custom-fields/field-definitions-migration.test.ts` · `field-definitions-admin.test.ts`·`field-definitions.spec.ts` 확장 · `fixtures.ts`의 `archiveE2EFieldDefinitions` |

<output>
Create `.planning/phases/04.5-custom-field-admin/04.5-08-SUMMARY.md` when done
</output>
