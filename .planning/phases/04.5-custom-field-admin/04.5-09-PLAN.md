---
phase: 04.5-custom-field-admin
plan: 09
type: execute
wave: 3
depends_on: ["04.5-08"]
files_modified:
  - domain/permissions/menus.ts
  - ui/shell/role-menu.ts
  - docs/design/SYSTEM.md
  - test/unit/ui/role-menu.test.ts
  - test/e2e/admin-nav.spec.ts
  - app/(app)/admin/field-definitions/actions.registry.ts
  - test/integration/leak-scan.test.ts
  - test/integration/field-definitions-admin.test.ts
  - test/e2e/field-definitions.spec.ts
  - domain/seed/index.ts
  - test/integration/seed-permissions.test.ts
autonomous: true
requirements: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "시스템 관리자가 `/admin` 인덱스 마스터 그룹의 「화면 항목」(「코드표」 다음)을 눌러 `/admin/field-definitions`에 닿는다 — `domain/permissions/menus.ts`의 `MENUS` · `ui/shell/role-menu.ts` · SYSTEM.md §6-10 표와 제목의 화면 수 · `role-menu.test.ts`·`admin-nav.spec.ts`의 10 → 11이 이 플랜의 한 태스크에서 함께 바뀐다. PC 사용자 메뉴·「더보기」 시트는 「관리」 한 줄 그대로다(UI-SPEC 관리 인덱스, O6)"
    - "시드가 `admin.field-definitions`의 view·write를 시스템 관리자에게만 `insertPermissionIfAbsent`로 준다(UI-SPEC 화면 1 — 이미 있는 행은 덮지 않는다). 시스템 관리자 전 메뉴 `upsertPermission` 루프는 이 키를 건너뛴다. 다른 계급에는 행이 없다(`can()` 기본 거부). 관리자가 권한표에서 끈 행은 재시드(배포)해도 꺼진 채다. 01의 통합·E2E가 넣던 명시적 권한 준비 줄을 지워도 초록이다"
    - "새 액션·DTO가 누수 스캔 생성기에 들어간다(ROADMAP 기준 5, Codex #6): `createFieldDefinitionAction`이 `actions.registry.ts`로 등록되어 액션 축이 계급마다 케이스를 만들고, 정보 노출표 항목이 없는 관리 메타데이터 DTO `FieldDefinitionAdminDto`는 `leak-scan.test.ts` 끝에 더한 「메뉴 게이트 DTO 축」이 계급마다 케이스를 만든다 — 검토된 필드 목록과 실제 DTO 키가 같고, 계급마다 `listFieldDefinitionsForAdmin`이 `can(viewer, \"admin.field-definitions\", \"view\")`일 때만 돌려주며 아니면 `ForbiddenError`다. `dto-registry.ts`·`project.ts`는 고치지 않는다"
  artifacts:
    - path: "app/(app)/admin/field-definitions/actions.registry.ts"
      provides: "`createFieldDefinitionAction` 등록(메뉴 `admin.field-definitions`, 동작 `write`, `dtoName: null`)"
      contains: "createFieldDefinitionAction"
    - path: "test/integration/leak-scan.test.ts"
      provides: "등록부 import 한 줄 + 끝에 더한 「메뉴 게이트 DTO 축」(`FieldDefinitionAdminDto` × 계급)"
      contains: "FieldDefinitionAdminDto"
    - path: "domain/seed/index.ts"
      provides: "시스템 관리자 루프 안 `admin.field-definitions` 건너뛰기 한 줄 + 루프 뒤 `insertPermissionIfAbsent` view·write 블록(더하기만)"
      contains: "admin.field-definitions"
    - path: "test/integration/seed-permissions.test.ts"
      provides: "시스템 관리자의 `admin.field-definitions` write를 끈 뒤 재시드해도 꺼진 채인 회귀 테스트"
      contains: "admin.field-definitions"
  key_links:
    - from: "domain/seed/index.ts"
      to: "permission_matrix"
      via: "`insertPermissionIfAbsent`(`onConflictDoNothing`) — 재시드가 관리자의 회수를 되살리지 않는다(UI-SPEC 화면 1)"
      pattern: "insertPermissionIfAbsent"
    - from: "domain/permissions/menus.ts"
      to: "ui/shell/role-menu.ts"
      via: "`test/unit/admin-menu-registry.test.ts`가 `MENUS`의 `admin.*` 키 전부를 `/admin` 인덱스 href와 대조 — 둘은 한 태스크에서 함께 바뀐다"
      pattern: "admin.field-definitions"
    - from: "ui/shell/role-menu.ts"
      to: "docs/design/SYSTEM.md"
      via: "`role-menu.test.ts`가 §6-10 표와 원소 단위로 비교(O6 — 같은 계획)"
      pattern: "화면 항목"
    - from: "test/integration/leak-scan.test.ts"
      to: "domain/custom-fields/admin.ts"
      via: "메뉴 게이트 DTO 축이 `FIELD_DEFINITION_ADMIN_DTO_FIELDS`·`listFieldDefinitionsForAdmin`을 계급마다 부른다"
      pattern: "listFieldDefinitionsForAdmin"
---

<objective>
플랜 01·08 완성의 나머지 — 메뉴 등록과 관리 인덱스 링크(정본 표·순서 테스트와 한 묶음), 액션 등록부와 `FieldDefinitionAdminDto`의 누수 스캔 계약, 새 메뉴의 시드 권한(UI-SPEC 화면 1의 insert-if-absent), 01이 넣은 권한 준비 줄 정리를 얹는다. 08의 파일 수(21 > 15)를 줄이려고 옛 08 Task 2를 그대로 옮긴 플랜이다 — 누수 스캔 축이 08의 `listFieldDefinitionsForAdmin`·`FIELD_DEFINITION_ADMIN_DTO_FIELDS`를 부르므로 08 뒤에 돈다.

Purpose: 02(수정 액션 등록)·03(누수 스캔 커스텀 축)이 이 플랜의 액션 등록부·누수 스캔 파일 위에 서고, 07의 머지 직전 의식이 이 플랜의 관리 인덱스 수·§6-10 표를 main 기준으로 다시 맞춘다.

Output: 메뉴·관리 인덱스 11번째 링크 · SYSTEM.md §6-10 · 액션 등록부 · 누수 스캔 메뉴 게이트 DTO 축 · 시드 권한(시스템 관리자 view·write, 덮지 않음) · 권한 준비 줄 없이 도는 통합·E2E.

**실행 순서 — Phase 4 선행:** `domain/permissions/menus.ts`(04-13·04-20) · `docs/design/SYSTEM.md`(04-01·04-08·04-29·04-46) · `test/integration/leak-scan.test.ts`(04-07·04-14·04-18·04-32·04-42) · `domain/seed/index.ts`(04-01·04-06·04-10·04-20·04-27)를 Phase 4도 고치므로 그 Phase 4 플랜이 작업 브랜치에 들어온 뒤에만 실행한다 — 먼저 `sh -c 'for p in 04-01 04-06 04-07 04-08 04-10 04-13 04-14 04-18 04-20 04-27 04-29 04-32 04-42 04-46; do git ls-tree --name-only HEAD .planning/phases/04-project-quote-ledger/ | grep -q "/$p-SUMMARY.md" || exit 1; done'`를 돌려(01이 착수 때 main을 합쳤다 — 여기서는 합치지 않는다, 마이그레이션 번호 조정은 07 의식만) 종료 코드가 0이 아니면 멈추고 사용자에게 알린다.

**실행 규율:** 코드를 쓰는 태스크는 먼저 Skill `test-driven-development`(RED를 실제로 확인 → GREEN → 리팩터), 테스트·빌드·린트 실패는 코드를 만지기 전에 Skill `systematic-debugging`, 커밋과 「완료」 전에는 Skill `verification-before-completion`을 호출한다. 서브에이전트에 위임하면 세 스킬과 `model`을 프롬프트에 적는다. 한 세션에 플랜 하나. E2E 판정은 `CI=true`다. **공유 테스트 환경 직렬화(공유 결정 3):** 공유 테스트 DB(`erp_test`)·통합 테스트·`db:reset:test`·마이그레이션·E2E 서버 포트 3100·`next build`를 건드리는 명령은 전부 `flock /tmp/plant8-erp-test.lock` 접두로 돌린다(여러 명령을 이으면 `sh -c '…'`로 묶고, `CI=true` 같은 환경 변수 대입은 그 안이나 `env CI=true …`로 둔다 — flock은 첫 낱말을 실행 파일로 본다). 단위 테스트·lint·typecheck는 잠그지 않는다. **E2E DB 준비 순서(공유 결정 4):** `pnpm db:reset:test`(빈 DB를 만든다) → Playwright globalSetup `test/e2e/global-setup.ts`(14–31행: 스키마 비우기 → `migrate`로 마이그레이션 → `seedMasterData`로 기본 시드) → 스펙의 `beforeAll` 픽스처. `db:dev`는 DB 서버를 띄우고 없는 DB만 만든다. 새 의존성 0개. `.planning/`은 GSD 도구로만 쓴다.

**병렬 페이즈 규칙(Phase 4 · 04.1~04.4와 나란히):** `domain/permissions/project.ts`·`dto-registry.ts`는 고치지 않는다. 공유 파일(`domain/permissions/menus.ts`·`test/integration/leak-scan.test.ts`·`domain/seed/index.ts`)에는 항목만 더한다 — 기존 줄을 고치거나 옮기지 않는다(`leak-scan.test.ts`는 import 블록 끝에 줄 추가 + 파일 끝에 `describe` 추가뿐 · `domain/seed/index.ts`는 시스템 관리자 루프 안 건너뛰기 한 줄 + 루프 뒤 블록뿐 — 04-20이 바꾼 줄·루프 구조는 그대로). STATE.md의 현재 페이즈(4)는 건드리지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-custom-field-admin/04.5-CONTEXT.md
@CLAUDE.md
@.planning/phases/04.5-custom-field-admin/04.5-01-SUMMARY.md
@.planning/phases/04.5-custom-field-admin/04.5-08-SUMMARY.md
# 01-SUMMARY가 필요한 이유: 01이 넣은 권한 준비 줄 위치(통합 `beforeEach`·E2E `beforeAll`)를 이 플랜이 지운다.
# 08-SUMMARY가 필요한 이유: 누수 스캔 축이 부르는 `listFieldDefinitionsForAdmin`·`FIELD_DEFINITION_ADMIN_DTO_FIELDS`·`createFieldDefinition`의 실제 이름·모양. 아래 이름과 다르면 SUMMARY를 따른다.
# 범위 Read(절 이름으로 찾는다 — 줄 번호는 UI-SPEC 개정 3(75ae245) 기준):
# .planning/phases/04.5-custom-field-admin/04.5-UI-SPEC.md 「관리 인덱스」 318–320행 · Assumptions O6(357행~)
# docs/design/SYSTEM.md §6-10(610행~, 표 636행)
</context>

<probe_fallback>
⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). ROADMAP Phase 04.5의 `**Requirements**`는 TBD다(ADMN-07은 Phase 10에 남음). 수용 기준은 ROADMAP 04.5 기준 1·5, CONTEXT D10-13과 Claude's Discretion, UI-SPEC(개정 3 승인본) 관리 인덱스·O6에서 끌어왔다.

**계획 단계 결정(표면화 — 옛 08에서 옮김):**

- **메뉴 등록과 관리 인덱스를 한 태스크에(Task 1):** `MENUS`에 `admin.*` 키를 더하면 `test/unit/admin-menu-registry.test.ts`가 같은 키가 `/admin` 인덱스(`role-menu.ts`)에 있기를 요구하고, UI-SPEC O6은 그 인덱스 링크와 SYSTEM.md §6-10 표·순서 테스트를 같은 계획에서 고치라고 한다. 액션 등록부도 이 태스크다 — 누수 스캔 액션 축이 `action.menu`가 `MENUS`에 있기를 요구한다.
- **권한 시드 수단(UI-SPEC 화면 1 그대로 · 라운드 2 D3):** `admin.field-definitions`를 `MENUS` **끝에** 더하면 기존 시드의 시스템 관리자 전 메뉴 루프(`domain/seed/index.ts` 133행~, `upsertPermission(allowed: true)`)가 배포 시드마다 이 행을 다시 켠다 — 관리자가 권한표에서 끈 값이 되살아나 UI-SPEC 화면 1(229행 「이미 있는 행은 덮지 않는다」)과 어긋난다. 그래서 가장 작은 변경으로 승인본을 따른다: 루프 안 첫 줄에서 이 키만 건너뛰고, 루프 뒤에 `insertPermissionIfAbsent`(`repositories/permissions.ts` 56–73행 — 이미 있음)로 시스템 관리자 view·write만 준다(approve 없음). 루프를 다시 짜지 않는다. 04-20은 비관리자 기본값만 insert-if-absent로 바꾸고 시스템 관리자 루프는 upsert로 둔다 — 이 플랜은 그 파일에 줄만 더한다(07 ⑤ 표의 `-` 줄 0 검사 대상). 회귀 테스트는 기존 `test/integration/seed-permissions.test.ts`(같은 결의 회수 유지 테스트)에 `it` 하나를 더한다 — 새 파일을 만들지 않는다.
- **`FieldDefinitionAdminDto`의 누수 스캔 계약(Codex #6 · ROADMAP 기준 5):** 이 DTO는 정보 노출표 항목에 매핑되는 사람 단위 값이 없는 구조 메타데이터이고 메뉴 권한 뒤에서만 나간다(`listVendorFieldDefinitions` 주석과 같은 결). DTO 등록부 축은 필드마다 `INFO_ITEMS` 항목을 요구하므로 억지 항목을 만들지 않는다. 대신 조용히 빼지 않고 `leak-scan.test.ts` **끝에** 명시적 검사 경로 「메뉴 게이트 DTO 축」을 더한다 — 이 파일의 `NULL_DTO_EXEMPT_EXPORTS`(검토된 예외 목록) 선례와 같은 결. 목록 `MENU_GATED_DTOS`의 항목 하나 = `{ name: "FieldDefinitionAdminDto", menu: "admin.field-definitions", fields: FIELD_DEFINITION_ADMIN_DTO_FIELDS, list: listFieldDefinitionsForAdmin }`. 생성기 = 목록 × `SEED_ROLES`(기존 생성기와 같은 `flatMap`, 정렬 없음). 검사: (a) 메뉴가 `MENUS`에 있다 (b) 이름이 `DTO_REGISTRY`에 없다(두 축 중 하나에만) (c) 테스트에 적은 검토된 필드 목록과 `FIELD_DEFINITION_ADMIN_DTO_FIELDS`가 같다 — 필드를 더하려면 이 검토 목록도 고쳐야 한다 (d) 시스템 관리자가 거래처 칸 하나를 만든 뒤 `listFieldDefinitionsForAdmin`이 돌려준 DTO의 키 집합이 그 목록과 같다 (e) 계급마다 `listFieldDefinitionsForAdmin({ id: "leak-scan-probe", roleId })`가 `can(viewer, menu, "view")`가 참일 때만 배열을, 거짓이면 `ForbiddenError`를 낸다 · 시드 기준으로 배열을 받는 계급은 시스템 관리자 하나다. 파일 머리의 기존 생성기·예외 목록 줄은 고치지 않는다.
- **01의 권한 준비 줄:** 01의 통합 테스트 `beforeEach`·E2E `beforeAll`이 넣던 `admin.field-definitions` 권한 행 준비를 Task 1이 지운다 — 지운 뒤에도 초록인 것이 시드 경로의 증명이다.

**OPEN — /plan-design-review(승인된 UI-SPEC 개정 3에서 벗어난 자리 — SUMMARY에만 두지 않는다, 번호는 옛 08 그대로):**

2. **권한 시드 수단 — 해소(라운드 2 D3):** UI-SPEC 화면 1대로 `insertPermissionIfAbsent`로 준다(위 「권한 시드 수단」). 승인본과의 편차가 없어졌으므로 디자인 리뷰 대기 목록에 올리지 않는다.
</probe_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 메뉴 등록 · 관리 인덱스 11번째 링크(정본 표·순서 테스트와 한 묶음, O6) · 액션 등록부 · 누수 스캔 메뉴 게이트 DTO 축 · 시드 권한(insert-if-absent)</name>
  <files>domain/permissions/menus.ts, ui/shell/role-menu.ts, docs/design/SYSTEM.md, test/unit/ui/role-menu.test.ts, test/e2e/admin-nav.spec.ts, app/(app)/admin/field-definitions/actions.registry.ts, test/integration/leak-scan.test.ts, test/integration/field-definitions-admin.test.ts, test/e2e/field-definitions.spec.ts, domain/seed/index.ts, test/integration/seed-permissions.test.ts</files>
  <read_first>
    - `domain/permissions/menus.ts`(전문) · `domain/seed/index.ts` 120–170행(시스템 관리자 전 메뉴 루프와 PM `projects`의 `insertPermissionIfAbsent` 블록 — 이 플랜이 줄을 더한다) · `repositories/permissions.ts` 56–73행(`insertPermissionIfAbsent`) · `test/integration/seed-permissions.test.ts`(전문 19행 — 회수 유지 테스트 선례) · `test/unit/admin-menu-registry.test.ts`(전문)
    - `ui/shell/role-menu.ts` 79–95행(`ADMIN_MENUS`) · `docs/design/SYSTEM.md` 610–640행(§6-10, 표 636행) · `test/unit/ui/role-menu.test.ts` 125–270행(`ADMIN_MENU_KEYS`·10을 말하는 단언·`KEY_BY_LABEL`) · `test/e2e/admin-nav.spec.ts` 44–105행(라벨 목록 두 곳)
    - `app/(app)/admin/vendors/actions.registry.ts`(등록 선례) · `lib/actions/registry.ts`
    - `test/integration/leak-scan.test.ts`(전문 173행 — import 블록 1–36행, `NULL_DTO_EXEMPT_EXPORTS` 41행, 생성기 45–80행) · `test/unit/leak-scan-coverage.test.ts`
    - 08 SUMMARY의 `domain/custom-fields/admin.ts` 심볼(`listFieldDefinitionsForAdmin`·`FIELD_DEFINITION_ADMIN_DTO_FIELDS`·`createFieldDefinition`) · 01 SUMMARY의 권한 준비 줄 위치
  </read_first>
  <behavior>
    - (단위) `admin-menu-registry.test.ts`(기존, 고치지 않음)가 `MENUS`의 `admin.field-definitions`를 `/admin/field-definitions` 인덱스 항목과 대조해 초록이다 — `MENUS`만 먼저 고치면 빨개지는 것을 RED로 확인한다
    - (단위) `role-menu.test.ts`: `ADMIN_MENU_KEYS`에 `admin.field-definitions`가 있고 길이 11 · SYSTEM.md §6-10 표 마스터 행이 「사람 · 거래처 · 법인카드 마스터 · 코드표 · 화면 항목」이며 `adminIndexGroups`와 원소 단위로 같다 · `KEY_BY_LABEL`에 「화면 항목」 → `admin.field-definitions`
    - (단위) `leak-scan-coverage.test.ts`(기존)가 새 `actions.registry.ts`의 import를 누수 스캔에서 찾는다
    - (통합) 누수 스캔 액션 축에 `createFieldDefinitionAction·<계급>` 케이스가 계급 수만큼 생긴다 · 메뉴 게이트 DTO 축(`<probe_fallback>`의 (a)~(e))이 `FieldDefinitionAdminDto·<계급>` 케이스를 계급 수만큼 만들고 전부 초록이다 · 시드 기준으로 배열을 받는 계급은 시스템 관리자 하나다
    - (통합) `seed-permissions.test.ts`: 시드 뒤 시스템 관리자의 `admin.field-definitions` view·write가 `allowed: true`다 · write를 `upsertPermission(allowed: false)`로 끈 뒤 `seedMasterData`를 다시 돌려도 `findPermission`의 `allowed`가 `false`다 — 루프가 이 키를 건너뛰기 전에는 빨갛다
    - (통합) 01이 넣은 권한 준비 줄을 지운 `field-definitions-admin.test.ts`가 그대로 초록이다(시드가 시스템 관리자에게 view·write를 준다)
    - (E2E) `admin-nav.spec.ts`: `/admin` 인덱스에 「화면 항목」 링크가 보이고, PC 사용자 메뉴에는 「화면 항목」 menuitem이 없다
    - (E2E) `field-definitions.spec.ts`: 권한 준비 줄 없이 시스템 관리자가 `/admin` → 「화면 항목」 → 「화면 항목 추가」로 01의 트레이서 여정을 시작한다
  </behavior>
  <action>
① RED: `domain/permissions/menus.ts` **끝에** `{ key: "admin.field-definitions", label: "화면 항목" }`(04.5 주석 한 줄)만 먼저 더하고 `admin-menu-registry.test.ts`가 빨개지는 것을 확인한다. 이어 behavior의 단위·통합·E2E 단언을 쓰고 빨간 것을 확인한다.

② **관리 인덱스(UI-SPEC 관리 인덱스 · O6 — 네 파일을 같은 태스크에서).** `ui/shell/role-menu.ts`의 `ADMIN_MENUS` 마스터 그룹 「코드표」 다음에 `{ key: "admin.field-definitions", label: "화면 항목", href: "/admin/field-definitions", group: ADMIN_GROUP_MASTER }`. `docs/design/SYSTEM.md` §6-10 제목의 「관리자 화면 10개」 → 11개, 표 마스터 행 끝에 「· 화면 항목」. `test/unit/ui/role-menu.test.ts`의 `ADMIN_MENU_KEYS`(131행)에 키를 더하고 10을 11로(152행 단언과 10을 말하는 테스트 제목), `KEY_BY_LABEL`에 「화면 항목」. `test/e2e/admin-nav.spec.ts`의 라벨 목록 두 곳(사용자 메뉴에 없어야 하는 목록 · 인덱스에 보여야 하는 목록 — 「코드표」 다음)에 「화면 항목」, 10을 말하는 제목을 11로. 주석 속 「10개」는 고치지 않는다. 병렬 페이즈가 관리 화면을 먼저 더해 main의 수가 바뀌어 있으면 07의 머지 직전 의식이 「main의 수 + 1」로 맞춘다.

③ **액션 등록부.** `app/(app)/admin/field-definitions/actions.registry.ts`: vendors 선례대로 `registerAction({ name: "createFieldDefinitionAction", menu: "admin.field-definitions", action: "write", dtoName: null })`(주석 한 줄: 액션은 값을 돌려주지 않는다 — 관리 DTO는 누수 스캔의 메뉴 게이트 DTO 축이 따로 검사). `test/integration/leak-scan.test.ts`의 registry import 블록 **끝에** 이 파일 import 한 줄.

④ **메뉴 게이트 DTO 축(Codex #6 · ROADMAP 기준 5).** `leak-scan.test.ts` import 블록 끝에 `FIELD_DEFINITION_ADMIN_DTO_FIELDS`·`listFieldDefinitionsForAdmin`(`@/domain/custom-fields/admin`)·`can`·`ForbiddenError`(`@/domain/permissions/can`)·`SYSADMIN_ROLE_ID` import를 더하고, 파일 **끝에** `<probe_fallback>`의 「`FieldDefinitionAdminDto`의 누수 스캔 계약」대로 `MENU_GATED_DTOS` 목록(주석: 정보 노출표 항목이 없는 관리 메타데이터 DTO의 검토된 목록 — 여기 없는 관리 DTO는 DTO 등록부 축에 있어야 한다)과 생성기, `describe` 하나를 더한다. 검토된 필드 목록은 테스트 안의 문자열 배열 리터럴이다(생산 상수를 그대로 복사해 비교하지 않고 손으로 적는다 — 검토 게이트). (d)의 거래처 칸은 시스템 관리자 viewer로 `createFieldDefinition`을 불러 만든다. `domain/permissions/dto-registry.ts`·`project.ts`는 고치지 않는다.

⑤ **권한 준비 줄 정리.** `test/integration/field-definitions-admin.test.ts`의 `beforeEach`와 `test/e2e/field-definitions.spec.ts`의 `beforeAll`에서 01이 넣은 `admin.field-definitions` 권한 행 준비(`upsertPermission`)를 지우고, E2E 여정의 시작을 `/admin` 인덱스 클릭으로 바꾼다.

⑥ **시드 권한(UI-SPEC 화면 1 · 라운드 2 D3).** RED: `test/integration/seed-permissions.test.ts`의 기존 `describe` 끝에 behavior의 `it` 하나를 더한다(`SYSADMIN_ROLE_ID` import는 새 줄로 더한다 — 기존 줄은 고치지 않는다). 끝에 `try/finally`로 `upsertPermission(allowed: true)`를 되돌린다 — 같은 DB를 쓰는 `field-definitions-admin.test.ts`가 ⑤ 뒤로 시드 행에 기댄다. ①의 `MENUS` 등록 뒤라 기존 루프가 행을 다시 켜서 빨간 것을 확인한다. GREEN: `domain/seed/index.ts` 시스템 관리자 루프의 `for (const menu of MENUS) {` 바로 안 첫 줄에 `if (menu.key === "admin.field-definitions") continue;`(04.5 주석 한 줄 — 아래에서 insert-if-absent로 준다)를 더한다. 루프를 닫는 `}` 다음에 `for (const action of ["view", "write"] as const)`로 `insertPermissionIfAbsent(viewer, { roleId: SYSADMIN_ROLE_ID, menu: "admin.field-definitions", action, allowed: true, updatedBy: null })`와 `permissionsCount++`를 더한다(같은 파일 PM `projects` 블록 선례). 루프 구조와 04-20이 바꾼 줄은 고치지 않는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm test:unit</automated>
    <fails_when>eslint·tsc 중 하나라도 error로 끝나거나 vitest unit의 failed 개수가 1 이상이다 — 특히 `admin-menu-registry`·`role-menu`(SYSTEM.md 표 대조)·`leak-scan-coverage`·`design-system-docs`·`docs-limits`</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts test/integration/field-definitions-admin.test.ts test/integration/seed-permissions.test.ts'</automated>
    <fails_when>통합 failed 개수가 1 이상이다 — 액션 축의 새 케이스, 메뉴 게이트 DTO 축(검토 목록·DTO 키·계급별 게이트), 권한 준비 줄 없이 도는 생성 경로, 시드 권한 정책 중 하나가 깨졌다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/admin-nav.spec.ts test/e2e/field-definitions.spec.ts'</automated>
    <fails_when>playwright failed 개수가 1 이상이다 — 인덱스 「화면 항목」 링크, 사용자 메뉴에 개별 라벨 없음, 인덱스에서 시작하는 트레이서 여정 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff origin/main -- domain/permissions/menus.ts`의 삭제 줄(`^-[^-]`)이 0개다(끝에 더함뿐)
    - `git diff origin/main -- test/integration/leak-scan.test.ts`의 삭제 줄이 0개다(import 블록 끝 줄 추가 + 파일 끝 `describe` 추가뿐)
    - `domain/permissions/dto-registry.ts`·`domain/permissions/project.ts`의 diff가 비어 있다
    - `git diff origin/main -U0 -- domain/seed/index.ts`의 삭제 줄(`^-[^-]`)이 0개다(루프 안 건너뛰기 한 줄 + 루프 뒤 블록 추가뿐)
    - `grep -c "화면 항목" docs/design/SYSTEM.md`가 이 플랜 전보다 1 이상 늘었고, §6-10 제목이 「관리자 화면 11개」다
    - `seed-permissions.test.ts`의 새 `it`(시스템 관리자 `admin.field-definitions` write 회수 → 재시드 → 꺼진 채)이 초록이다 · OPEN 2는 해소로 SUMMARY에 적고 「디자인 리뷰 대기」에 올리지 않는다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>「화면 항목」이 권한표·관리 인덱스·정본 표에 한 번에 등록되어 시스템 관리자만 인덱스에서 닿고, 새 액션과 관리 DTO가 누수 스캔의 계급별 케이스로 검사된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 권한표 → 관리 화면·관리 DTO | 메뉴 등록이 누가 이 화면과 DTO에 닿는지를 정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-01 | Elevation of Privilege | `MENUS` 등록 · 관리 목록 · 관리 인덱스 · 시드 | high | mitigate | 시드가 시스템 관리자에게만 `insertPermissionIfAbsent`로 view·write 행을 만든다(다른 계급 행 없음 → `can()` 기본 거부 · 관리자가 끈 행은 재시드가 되살리지 않음 — `seed-permissions.test.ts`). 누수 스캔 메뉴 게이트 DTO 축이 계급마다 목록 게이트를 증명하고, 권한 준비 줄을 지운 통합·E2E(기획 PM 404는 08 Task 2)가 시드 경로를 증명 — Task 1 |
| T-04.5-09 | Information Disclosure | `FieldDefinitionAdminDto`(이름·선택지·키) | low | mitigate | 사람 단위 값이 없는 구조 메타데이터이고 `admin.field-definitions` view 뒤에서만 나간다. 조용히 빼지 않고 누수 스캔의 명시적 검사 경로(검토된 필드 목록 + 계급별 게이트, Codex #6)로 덮는다 — Task 1 |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — `package.json` 의존성 객체와 `pnpm-lock.yaml` diff가 비어 있는 것이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` 통과
- `pnpm test:unit` 통과(`admin-menu-registry`·`role-menu`·`leak-scan-coverage` 포함)
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration …'`로 `leak-scan` · `field-definitions-admin` · `seed-permissions`(새 메뉴 회수 유지 포함) 통과
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && CI=true pnpm playwright test …'`로 E2E 두 파일(`admin-nav` · `field-definitions`) 통과 — DB 준비는 `db:reset:test` → globalSetup(마이그레이션 → 기본 시드) → 스펙 `beforeAll` 픽스처 순서(공유 결정 4)
- 공유 파일 diff가 「끝에 더함」뿐이다: `git diff origin/main -- domain/permissions/menus.ts test/integration/leak-scan.test.ts domain/seed/index.ts`에 삭제 줄(`^-[^-]`)이 없다
- `package.json` 의존성 객체·`pnpm-lock.yaml` 변경 없음
</verification>

<success_criteria>
- 「화면 항목」이 권한표·관리 인덱스·정본 표에 한 번에 등록되고 시스템 관리자만 닿는다
- 새 액션과 관리 DTO가 누수 스캔의 계급별 케이스로 검사된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 변경된 기존 심볼 | `MENUS`(+`admin.field-definitions`) · `ADMIN_MENUS`(+화면 항목) |
| 등록부 | `app/(app)/admin/field-definitions/actions.registry.ts`(`createFieldDefinitionAction`) · 누수 스캔 「메뉴 게이트 DTO 축」(`MENU_GATED_DTOS`) |
| 문서 | SYSTEM.md §6-10 제목·마스터 행 |
| 시드 | `domain/seed/index.ts` — 시스템 관리자 루프의 `admin.field-definitions` 건너뛰기 + `insertPermissionIfAbsent` view·write |
| 테스트 | `role-menu.test.ts`·`admin-nav.spec.ts`(10 → 11) · `leak-scan.test.ts`(메뉴 게이트 DTO 축) · `field-definitions-admin.test.ts`·`field-definitions.spec.ts`(01의 권한 준비 줄 제거 · 인덱스에서 시작) · `seed-permissions.test.ts`(새 메뉴 회수 유지) |

<output>
Create `.planning/phases/04.5-custom-field-admin/04.5-09-SUMMARY.md` when done
</output>
