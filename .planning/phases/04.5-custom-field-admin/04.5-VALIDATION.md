---
phase: "04.5"
slug: "custom-field-admin"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-24"
---

# Phase 04.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (unit / integration projects) + Playwright (E2E) |
| **Config file** | `vitest.config.ts`, `playwright.config.ts` |
| **Quick run command** | `pnpm test:unit` |
| **Full suite command** | `pnpm test` (unit → integration → e2e; local DB via `pnpm db:dev`) |
| **Estimated runtime** | ~600 seconds (full), ~30 seconds (unit) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:unit` plus the touched integration file (`pnpm vitest run --project integration <file>`)
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd-verify-work`:** Full suite must be green, plus `pnpm lint` / `pnpm typecheck`, E2E with `CI=true`
- **Max feedback latency:** 60 seconds (per-task sampling)

---

## Per-Task Verification Map

Seeded from RESEARCH.md §Validation Architecture (success criteria 1–5). The planner replaces these rows with per-task IDs.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.5-SC1 | TBD | TBD | SC-1 (field CRUD, key auto-gen, type immutable, admin-only) | T-04.5-EoP | non-admin gets 404 / action rejected | unit + integration | `pnpm vitest run --project integration test/integration/custom-fields.test.ts` | ❌ W0 | ⬜ pending |
| 04.5-SC2 | TBD | TBD | SC-2 (archive keeps values, archived option values pass, required not retroactive) | T-04.5-Tamper-merge | merge fills only stored values; extra keys still rejected by `.strict()` | integration | `pnpm vitest run --project integration test/integration/custom-fields.test.ts` | ❌ W0 | ⬜ pending |
| 04.5-SC3 | TBD | TBD | SC-3 (matrix auto-register, insert-if-absent, seed does not re-enable) | T-04.5-Tamper-key | forged custom item key rejected | integration | `pnpm vitest run --project integration test/integration/custom-fields.test.ts` | ❌ W0 | ⬜ pending |
| 04.5-SC4 | TBD | TBD | SC-4 (per-field DTO visibility, hidden values kept on save, leak scan, Phase 4 saves unaffected) | T-04.5-Info | hidden field absent from DTO and page | integration + e2e | `pnpm vitest run --project integration test/integration/leak-scan.test.ts` / `CI=true pnpm exec playwright test field-definitions` | ⚠️ leak-scan exists / ❌ W0 e2e | ⬜ pending |
| 04.5-SC5 | TBD | TBD | SC-5 (registry registration, migration renumber ritual) | — | N/A | integration | `pnpm vitest run --project integration test/integration/leak-scan.test.ts` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/unit/domain/custom-fields-admin.test.ts` — key generation and option-union pure functions
- [ ] `test/integration/custom-fields.test.ts` — extend with archive / restore / visibility / value-preservation scenarios
- [ ] `test/integration/leak-scan.test.ts` — extend generator with dynamic `field_definitions` cases
- [ ] `test/e2e/field-definitions.spec.ts` — admin CRUD + vendor form reflection smoke
- Framework install: not needed (existing Vitest / Playwright)

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
