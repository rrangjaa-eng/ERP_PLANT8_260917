---
phase: 05-expense-approval-leave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/approvals.ts
  - db/migrations/meta/_journal.json
  - domain/approvals/route.ts
  - domain/approvals/kinds.ts
  - domain/approvals/index.ts
  - domain/approvals/dto.ts
  - domain/approvals/conflict-message.ts
  - repositories/approvals.ts
  - domain/leave/index.ts
  - ui/approval-route/ApprovalRoute.tsx
  - ui/approval-sheet/ApprovalSheet.tsx
  - ui/approval-sheet/ApprovalSheet.module.css
  - app/(app)/approvals/approval-sheet.tsx
  - app/(app)/approvals/inbox-table.tsx
  - test/unit/domain/approvals/next-step.test.ts
  - test/unit/domain/approvals/document-kind-registry.test.ts
  - test/integration/approvals-extensions.test.ts
autonomous: true
requirements: [EXP-02, EVID-01, UX-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "실행 착수 게이트: `origin/main`에 04.1 결재 모듈(`domain/approvals/kinds.ts` · `domain/approvals/index.ts` · `db/schema/approvals.ts` · `ui/approval-route/ApprovalRoute.tsx` · `app/(app)/document-kinds.ts`)과 04.1 SUMMARY 7개가 있고, Phase 4 플랜 수 = SUMMARY 수이며, 작업 브랜치가 `origin/main`을 포함한다. 하나라도 아니면 코드 한 줄도 바꾸지 않고 멈춘다(사용자 결정 2026-09-26 — 실행 선행 조건)"
    - "04.1 계획 이름과 실제 머지된 이름의 대응표 `name_map`이 05-01 SUMMARY frontmatter에 있고, 뒤 플랜 열두 개는 이 표의 실제 이름을 쓴다 — 대응이 없는 이름은 추측하지 않고 멈춰 보고한다(사용자 결정 2026-09-26 #4 「이름은 04.1 SUMMARY로 맞춘다」)"
    - "`nextStep(status, event)`의 기존 동작은 그대로다 — 선택 셋째 인자 없이 부르면 `withdrawn`은 모든 사건에 종결이고, 04.1의 기존 단위 사례는 한 줄도 고치지 않고 녹색이다. 셋째 인자 `{ allowResubmitFromWithdrawn: true }`일 때만 `withdrawn --resubmit--> submitted` 한 칸이 열린다(E1)"
    - "문서 종류 정의의 선택 필드 `resubmitFrom`이 없으면 기본 `[\"rejected\"]`다 — 연차는 회수 뒤 다시 신청이 여전히 거부되고, `resubmitFrom`에 `withdrawn`을 둔 종류만 기안자가 회수된 같은 문서를 다시 제출해 같은 인스턴스 · 차수 +1 · 같은 문서 id로 결재가 첫 단계부터 다시 간다(E1 · UI-SPEC 확정 #2)"
    - "최종 승인 훅: 종류의 선택 필드 `prepareFinalApproval`은 `approveDocument`가 트랜잭션을 열기 **전에** 읽고, `onFinalApprovalInTx`는 계산 결과가 `approve_final`일 때만 같은 트랜잭션 안에서 단계 기록 뒤 · 행동 로그 전에 불린다. 훅이 던지면 인스턴스 상태 · version · 단계 기록 · 로그가 전부 호출 전과 같다(E2 · D-98)"
    - "두 훅 중 하나만 있는 종류 정의는 등록이 거부된다(`registerDocumentKind`가 예외)"
    - "`bumpInstanceVersion`은 인스턴스 상태를 바꾸지 않고 `version`만 +1 올리며 `updated_by`와 `version_reason = 'evidence'`를 남긴다. 그 전에 문서를 연 결재자의 승인은 `ApprovalConflictError`로 거부되고 문구는 `{기안자 이름}이 HH:MM에 증빙을 더함 · 새로 고침`이다. 상태를 바꾸는 갱신(`updateInstanceStatus`)은 `version_reason`을 null로 되돌린다(E4 · UI-SPEC S4 · S9)"
    - "종류의 선택 필드 `approveBlockedReason`이 돌려준 이유는 `getApprovalView` · `listMyInbox` DTO의 `approveBlockedReason`(string | null)에 실리고, 결재 시트와 결재함 행의 `승인`은 그 이유가 있으면 `aria-disabled` + 이유 글자다. 서버 승인 판정은 이 필드를 쓰지 않는다 — 최종 판정은 훅(E2)이 한다(E3)"
    - "`describeDocuments` 요약 DTO에 선택 `measure`(money · days · null)가 있고, `listMyInbox` DTO의 `measureHeader`가 지금 목록에 있는 종류로 정해진다 — 금액만 `금액` · 일수만 `일수` · 섞이면 `금액 · 일수` · 둘 다 없으면 null. 연차 종류는 `measure: { kind: \"days\" }`를 실어 04.1 결재함의 일수 칸이 그대로 보인다(E5 · UI-SPEC S9)"
    - "결재선 목록의 단계 결과에 `self_approved`가 참인 단계는 `본인 승인 MM-DD HH:mm`(`success`)으로 보인다(E6 · UI-SPEC B2 · UA-607)"
    - "결재 시트는 `ui/approval-sheet/ApprovalSheet.tsx`에 있고 결재함은 그 경로를 import한다 — 옛 `app/(app)/approvals/approval-sheet.tsx`는 없고, 04.1 결재 E2E가 이동 뒤에도 `CI=true`로 녹색이다(E7 · UI-SPEC S9 · S11)"
    - "`domain/approvals/*.ts`에 문서 종류 이름 리터럴(연차 · 지출결의 · 정산 결재 키)이 없다 — 종류 차이는 전부 등록 필드로만 들어온다(ROADMAP Phase 5 기준 6 「하드코딩 없이」)"
    - "`test/integration/approvals-extensions.test.ts`의 테스트 전용 종류가 제출 → 회수 → 같은 문서 다시 제출(차수 2) → 증빙 변경(version +1) → 옛 version 승인 거부 → 새 version 최종 승인 → 훅이 같은 트랜잭션에서 한 쓰기 확인까지 한 describe에서 끝까지 녹색이다(트레이서)"
  prohibitions:
    - statement: "엔진(`domain/approvals`)에 문서 종류 이름으로 분기하는 코드를 넣어서는 안 된다 — 종류 차이는 등록 필드(E1~E5)로만"
      status: resolved
      verification: judgment
    - statement: "`prepareFinalApproval`처럼 전역 풀을 쓰는 읽기를 `withTransaction` 콜백 안에서 불러서는 안 된다 — 풀 5에서 동시 승인이 멈춘다(ARCHITECTURE §4-8 (3))"
      status: resolved
      verification: judgment
    - statement: "증빙 변경을 막기 위해 엔진에 두 번째 낙관적 잠금 토큰을 두어서는 안 된다 — 결재자는 인스턴스 `version` 하나로 막힌다(RESEARCH E4)"
      status: resolved
      verification: judgment
    - statement: "04.1 테스트 파일의 기존 사례를 고치거나 지워 이 플랜의 변경을 통과시켜서는 안 된다 — 사례 추가만 한다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/approvals/kinds.ts"
      provides: "종류 정의 선택 필드 resubmitFrom · prepareFinalApproval · onFinalApprovalInTx · approveBlockedReason, describeDocuments 요약의 선택 measure"
      contains: "resubmitFrom"
    - path: "domain/approvals/route.ts"
      provides: "nextStep 선택 셋째 인자 allowResubmitFromWithdrawn"
      contains: "allowResubmitFromWithdrawn"
    - path: "repositories/approvals.ts"
      provides: "bumpInstanceVersion(viewer, {instanceId, updatedBy, reason}, tx) — 상태 불변 version +1"
      contains: "bumpInstanceVersion"
    - path: "ui/approval-sheet/ApprovalSheet.tsx"
      provides: "결재 시트(04.1에서 이동) — 결재함과 내 차례가 같이 쓴다"
    - path: "test/integration/approvals-extensions.test.ts"
      provides: "E1 · E2 · E4 트레이서와 훅 롤백 · 등록 거부 · E3 · E5 사례"
      min_lines: 80
  key_links:
    - from: "domain/approvals/index.ts"
      to: "domain/approvals/kinds.ts"
      via: "approveDocument가 종류의 prepareFinalApproval(트랜잭션 전)과 onFinalApprovalInTx(같은 tx)를 부른다"
      pattern: "onFinalApprovalInTx"
    - from: "domain/approvals/index.ts"
      to: "domain/approvals/route.ts"
      via: "resubmitDocument가 종류의 resubmitFrom에 withdrawn이 있을 때만 allowResubmitFromWithdrawn으로 nextStep을 부른다"
      pattern: "allowResubmitFromWithdrawn"
    - from: "domain/approvals/conflict-message.ts"
      to: "repositories/approvals.ts"
      via: "version_reason = evidence이면 증빙 변경 문구 갈래"
      pattern: "version_reason|versionReason"
    - from: "app/(app)/approvals/inbox-table.tsx"
      to: "ui/approval-sheet/ApprovalSheet.tsx"
      via: "결재 시트 import 경로 이동(E7)"
      pattern: "ui/approval-sheet"
---

## Phase Goal

ROADMAP 원문 Goal: 「기획 PM이 견적 줄에서 지출결의를 한 화면에서 증빙 첨부까지 끝내 제출하면 결재자가 폰에서 승인·반려하고, 같은 결재 모듈로 연차도 승인된다」 — user story 형식이 아니다(MVP 모드). 계획은 이야기를 지어내지 않았다 — `/gsd mvp-phase 5`로 Goal을 user story로 고치기를 권한다. Goal의 「연차」 부분은 04.1 소관이고(사용자 결정 2026-09-24), 「경영관리 기안」 문구(기준 6)는 D-98로 대체되어 오케스트레이터가 `gsd_run`으로 맞춘다.

<objective>
04.1 결재 엔진에 이 페이즈가 필요한 **선택 필드 · 선택 인자 일곱 가지(E1~E7)**를 04.1 기본 동작을 바꾸지 않고 덧붙인다(사용자 결정 2026-09-26 #4 — 엔진 덧붙임은 Phase 5 Wave 1 소유). 실행 착수 게이트(04.1 머지 + Phase 4 잔여 플랜 머지)를 여기서 확인하고, 04.1 계획 이름 ↔ 실제 이름 대응표를 SUMMARY에 남겨 뒤 플랜들이 그 이름만 쓰게 한다.

Purpose: UI-SPEC 확정 #2(회수 뒤 같은 번호 다시 제출), D-98(정산 결재 최종 승인 = 프로젝트 완료, 같은 트랜잭션), S4 · S9(제출 뒤 증빙 변경이 결재자의 승인을 막음), S9(결재함 숫자 열 머리글), B2(`본인 승인` 낱말), S11(결재 시트 두 번째 사용처)이 전부 이 덧붙임 위에 선다. 04.1 그대로는 이 셋을 받을 수 없다(RESEARCH Summary).

Output: `domain/approvals` 선택 필드 넷 + `nextStep` 선택 인자 + `bumpInstanceVersion` + `approval_instances.version_reason` 열(마이그레이션 1) + 결재 시트 `ui/` 이동 + 통합 1 · 단위 사례 추가.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@.planning/phases/05-expense-approval-leave/05-RESEARCH.md
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@CLAUDE.md
</context>

## 선행 의존 (실행 착수 게이트 — 사용자 결정 2026-09-26 「04.1 머지 + Phase 4 잔여 플랜 머지」)

**Task 1 ⓪이 실행 시작 때 확인하고, 하나라도 실패하면 코드·문서를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 05-01 착수하지 않음`을 출력하고 멈춘다.** 이것은 일이 아니라 게이트다.

| id | 먼저 들어와야 하는 것 | 재확인 명령(Task 1 ⓪) |
|---|---|---|
| P-1 | 04.1 결재 모듈 코드가 main에 있다 | `git fetch origin main` 뒤 `for f in domain/approvals/kinds.ts domain/approvals/index.ts domain/approvals/route.ts db/schema/approvals.ts ui/approval-route/ApprovalRoute.tsx "app/(app)/document-kinds.ts"; do git cat-file -e "origin/main:$f" 2>/dev/null \|\| echo "MISSING $f"; done` 출력에 `MISSING`이 한 줄이라도 있으면 멈춤 |
| P-2 | 04.1 SUMMARY 일곱 개가 main에 있다 | `git ls-tree --name-only origin/main .planning/phases/04.1-approvals-leave/ \| grep -c -- '-SUMMARY.md$'`이 7 미만이면 멈춤 |
| P-3 | Phase 4 잔여 플랜이 전부 머지됐다 | `git ls-tree --name-only origin/main .planning/phases/04-project-quote-ledger/ \| grep -c -- '-PLAN.md$'`와 같은 명령의 `-SUMMARY.md$` 개수가 다르면 멈춤 |
| P-4 | 작업 브랜치가 main을 포함한다 | `git merge-base --is-ancestor origin/main HEAD`의 종료 코드가 0이 아니면 멈춤(main을 머지 커밋으로 반영한 뒤 다시 — force push 금지) |

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다 — 모든 task가 실패 테스트 먼저다. 테스트 · 마이그레이션 · CI가 하나라도 실패하면 원인을 쫓기 전에 `systematic-debugging` 스킬을 호출한다. 커밋하거나 「완료」라고 말하기 전에 `verification-before-completion` 스킬을 호출하고 아래 verify 명령의 실제 출력으로 판정한다. 서브에이전트에 위임하면 그 프롬프트에 세 스킬 이름과 모델(`model` 명시 — 구현은 `sonnet`, 판단이 갈리면 `opus`)을 적는다(CLAUDE.md §4 [Build] · §8).
- 첫 task 전에 `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-01.env"`로 기준점을 파일에 남기고, `$PLAN_BASE`를 쓰는 검사는 전부 `. "$(git rev-parse --git-dir)/gsd-05-01.env" && test -n "$PLAN_BASE" &&`로 시작한다(04.1과 같은 모양 — 빈 값이면 `HEAD..HEAD`로 공허하게 통과한다).
- **실행 순서(공유 DB)**: 이 페이즈 플랜은 로컬 DB 하나(`erp` · `erp_test`)를 쓴다. 05-02(문서만 · DB 없음)만 이 플랜과 같은 웨이브에서 나란히 돌 수 있고, 05-03부터는 웨이브마다 플랜 하나로 한 줄 체인이다. 통합 · E2E 명령은 `flock /tmp/plant8-erp-test.lock`로 감싼다(Phase 04.5에서 실제로 쓴 모양).
- 마이그레이션은 `pnpm db:generate`로만 만든다. 파일 이름 · `_journal.json` 손 편집 금지. 허용된 손 삽입은 기존 표를 건드리는 문장 앞의 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` · `--> statement-breakpoint` 블록 하나뿐이다(04.1-01 Task 2 ②와 같은 규칙). 번호는 브랜치 임시 번호이고 병합 직전 05-13이 재생성한다 — SUMMARY frontmatter `migration_manifest`에 이 플랜이 만든 tag · SQL · 스냅숏 경로를 적는다.
- **트랜잭션 규약**: `withTransaction` 콜백 안에서는 `tx`를 받는 리포지토리 호출과 종류의 `onFinalApprovalInTx`만 부른다. 설정 · 조직 스냅숏 · `can()` · 종류의 `prepareFinalApproval`은 트랜잭션 전에 읽는다(04.1-01 금지 항목 · ARCHITECTURE §4-8 (3)).
- `.planning/STATE.md`는 이 플랜의 어떤 task도 고치지 않는다(사용자 결정 2026-09-26 #3). 새 npm 의존성 0 — `package.json` · `pnpm-lock.yaml` diff가 비어 있어야 한다.
- 04.1 파일을 고칠 때는 **덧붙이기**가 원칙이다 — 기존 함수의 기본 경로 · 기존 문구 · 기존 테스트 사례를 바꾸지 않는다(CLAUDE.md §3.3).
</execution_notes>

<probe_fallback>
이 페이즈에는 `SPEC.md`가 없어 spec-less 프로브 폴백이 돌았다(`edge-probe.cjs`, 2026-09-26). 여덟 요구사항 전부가 `category: unclassified` · `status: unresolved`로 나왔다(`applicable: 8, resolved: 0`) — 요구사항 원문이 한국어라 분류기가 잡지 못한 도구 한계다. **조용히 버린 항목은 0건이다.** 여덟 건 모두 flagged 가정으로 남기고, 수용 기준은 ROADMAP Phase 5 기준 1~7 · 05-CONTEXT · UI-SPEC `## UI Considerations`에서 끌어냈다.

| # | 요구사항 | 프로브 결과 | 대신 쓰는 근거 | 수용 기준이 사는 플랜 |
|---|---|---|---|---|
| 1 | EXP-01 | unclassified → unresolved (flagged) | 기준 1 · 입력 §6 · UI 확정 #1 · #5 | 05-03 · 05-05 · 05-08 |
| 2 | EXP-02 | unclassified → unresolved (flagged) | 기준 1 · S3 · 확정 #2 | 05-01 · 05-05 · 05-07 · 05-09 |
| 3 | EXP-08 | unclassified → unresolved (flagged) | 기준 4 · 입력 §7 · 사용자 결정 #5 | 05-07 · 05-08 |
| 4 | EXP-14 | unclassified → unresolved (flagged) | 기준 1 · 게이트 ⑦ | 05-03 · 05-06 |
| 5 | EXP-15 | unclassified → unresolved (flagged) | 기준 7 · D-101 · 사용자 결정 #7 | 05-06 |
| 6 | EVID-01 | unclassified → unresolved (flagged) | ROADMAP Phase 5 비고 · S4 · RESEARCH Pattern 5 | 05-01 · 05-03 · 05-04 · 05-05 · 05-09 · 05-12 · 05-13 |
| 7 | UX-03 | unclassified → unresolved (flagged) | 기준 3 · S2 · S3 폰 · S9 | 05-01 · 05-05 · 05-08 · 05-10 · 05-11 · 05-13 |
| 8 | UX-06 | unclassified → unresolved (flagged) | 기준 3 · S6 · S11 | 05-02 · 05-06 · 05-07 · 05-10 · 05-11 |

**금지 항목 — canon 참조:** 경로 조작 · 주입 · 일반 접근 통제 같은 canon 보안 항목은 `/cso`와 기존 eslint · squawk가 소유한다(예: 「클라이언트가 정한 객체 키」는 canon 경로 조작이라 05-04 위협 모델에만 둔다). 각 플랜의 `prohibitions`는 이 제품 고유의 must-NOT이고, 기계 검사는 각 task의 acceptance_criteria에 있다.
</probe_fallback>

<!-- planner-discipline-allow: approvals/approval-sheet -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 테스트 전용 종류 한 건이 제출 → 회수 → 같은 문서 다시 제출(차수 2) → 증빙 변경(version +1) → 옛 version 승인 거부 → 최종 승인 → 같은 트랜잭션 훅까지 (E1 · E2 · E4, 스키마 열 하나 포함)</name>
  <precondition>「선행 의존」 표 P-1~P-4가 전부 통과한다 — ⓪이 확인하고 하나라도 실패하면 멈춘다.</precondition>
  <files>db/schema/approvals.ts, domain/approvals/route.ts, domain/approvals/kinds.ts, domain/approvals/index.ts, domain/approvals/conflict-message.ts, repositories/approvals.ts, test/unit/domain/approvals/next-step.test.ts, test/unit/domain/approvals/document-kind-registry.test.ts, test/integration/approvals-extensions.test.ts</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-01-SUMMARY.md` ~ `04.1-07-SUMMARY.md`(main에 머지된 것 — 실제 함수 · 열 · 파일 이름의 정본)
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 225–258행(Pattern 1 「가정할 것」 표 · 「더할 것」 E1~E7 · canResubmit · 문서 상태 파생)
    - `domain/approvals/route.ts`(전문 — `nextStep` 전이 표와 종결 판정) · `domain/approvals/kinds.ts`(전문 — `registerDocumentKind` 필드와 등록 거부 규칙) · `domain/approvals/index.ts`의 `approveDocument` · `resubmitDocument` · `withdrawDocument` · `getApprovalView`(판정 · 쓰기 순서 (1)~(7))
    - `domain/approvals/conflict-message.ts`(전문 — `isApprovalParty` · 충돌 문구 조립)
    - `repositories/approvals.ts`의 `updateInstanceStatus`(version 조건부 UPDATE 모양)
    - `db/schema/approvals.ts`(전문 — `approval_instances` 열 · CHECK 선언 모양)
    - 04.1 테스트 전용 종류 사례(04.1-01 Task 3 · 04.1-02의 `test_memo` 등록 모양 — `grep -rn "test_memo" test/`)
    - `test/integration/lock-race.ts` · `test/integration/setup.ts` — 경합 재현 도우미와 매 테스트 재시드
  </read_first>
  <behavior>
    - (단위) `nextStep("withdrawn", "resubmit")`는 셋째 인자 없이 `InvalidTransitionError`, `{ allowResubmitFromWithdrawn: true }`면 `submitted`. 셋째 인자가 참이어도 `withdrawn`의 나머지 다섯 사건은 여전히 거부된다. 04.1의 여섯 사건 × 여섯 상태 기존 사례는 그대로 녹색
    - (단위) `registerDocumentKind`에 `prepareFinalApproval`만 있고 `onFinalApprovalInTx`가 없거나 그 반대면 예외, 둘 다 있거나 둘 다 없으면 통과. `resubmitFrom`에 `rejected` · `withdrawn` 밖의 값이 오면 타입 오류(컴파일)
    - (통합, 트레이서) 테스트 안에서 등록한 테스트 전용 종류(결재선 1단 팀장 하나 · `resubmitFrom` 둘 · 훅 둘)로: 기안자 제출 → `submitted` · 차수 1 → 기안자 회수 → `withdrawn` → 같은 문서 id로 `resubmitDocument` → 같은 인스턴스 id · `current_round` 2 · 상태 `submitted` · 차수 1의 단계 기록은 그대로 남음
    - (통합, 트레이서) 팀장이 `getApprovalView`로 version V를 읽은 뒤 기안자 쪽 `bumpInstanceVersion(reason "evidence")` → 인스턴스 상태 `submitted` 그대로 · version V+1 · `version_reason` = `evidence` → 팀장이 V로 승인 → `ApprovalConflictError`이고 문구가 `{기안자 이름}이 HH:MM에 증빙을 더함 · 새로 고침`(시각은 `updated_at`의 서울 시각) · 단계 기록 0건
    - (통합, 트레이서) 팀장이 V+1로 승인 → `approved` · `version_reason` null · 훅 `onFinalApprovalInTx`가 받은 `tx`로 쓴 표식 행(행동 로그 한 행 — `appendActionLog(viewer, entry, tx)`)이 커밋돼 있고, `prepareFinalApproval`은 트랜잭션 **밖**에서 불렸다(주입한 호출 순서 기록으로 단언 — 콜백 진입 전)
    - (통합, 롤백) 같은 종류에서 `onFinalApprovalInTx`가 던지게 주입하고 최종 승인 → 오류가 나고 인스턴스 상태 · version · 단계 기록 수 · 행동 로그 수가 호출 전과 같다. 주입 없이 다시 승인하면 성공한다
    - (통합, 기본 동작 불변) `resubmitFrom`이 없는 종류(04.1 연차)는 회수 뒤 `resubmitDocument`가 거부되고 연차 E2E 문구가 바뀌지 않는다. 최종이 아닌 승인(`approve`)에서는 훅이 불리지 않는다(2단 결재선 종류로 1단 승인 사례)
  </behavior>
  <action>
⓪ **게이트와 이름 맞춤(일이 아니다).** 「선행 의존」 표 P-1~P-4 명령을 그대로 돌리고, 하나라도 실패하면 표의 문장을 출력하고 멈춘다. 통과하면 04.1 SUMMARY 일곱 개와 실제 코드(`grep -n "export" domain/approvals/*.ts repositories/approvals.ts domain/approvals/tx-log.ts`)를 대조해 SUMMARY frontmatter `name_map:`에 「RESEARCH Pattern 1 이름 → 실제 이름 · 파일」을 적는다 — 최소 항목: `registerDocumentKind` 필드(`loadRouteConfig` · `href` · `describeDocuments` · `routeSettings` · `canResubmit` · `loadDetails` · `detailDto` · `buildDetailRows`), 서비스(`prepareSubmission` · `submitDocument` · `approveDocument` · `rejectDocument` · `withdrawDocument` · `resubmitDocument` · `listMyInbox`(`withDetails`) · `getApprovalView` · `previewRoute`), 오류(`ApprovalConflictError` · `NotCurrentHolderError`), 충돌 문구(`isApprovalParty` · 문구 조립 함수), 리포지토리 `updateInstanceStatus`, 로그(`loadActionLogGate` · `recordActionInTx`), 설정 일괄 읽기(`getSimpleSettingValues` · `findSimpleValues`), `lib/dates.ts` `seoulToday`, journal 가드 테스트 경로, 결재 상태 → 낱말 매핑 파일, 결재 시트 파일, 결재함 표 파일, 종류 등록 한 곳(`app/(app)/document-kinds.ts`). 이름이 다르면 이 플랜과 뒤 플랜은 표의 실제 이름을 쓴다. 대응이 없는 항목(기능 자체가 없음)은 추측으로 만들지 않고 멈춰 보고한다.

① **스키마(E4)** `db/schema/approvals.ts`의 `approval_instances`에 선택 열 `version_reason` text null 하나를 더하고 CHECK `version_reason IS NULL OR version_reason IN ('evidence')`를 기존 CHECK 선언 모양으로 건다. 다른 열 · 표는 건드리지 않는다. `pnpm db:generate`로 마이그레이션을 만든다(적용 · 가드는 Task 2).

② **E1** `domain/approvals/route.ts` `nextStep(status, event, opts?: { allowResubmitFromWithdrawn?: boolean })` — 옵션이 참일 때만 `withdrawn` + `resubmit` → `submitted`를 허용하고 나머지 표는 그대로. `domain/approvals/kinds.ts` 종류 정의에 선택 필드 `resubmitFrom?: readonly ("rejected" | "withdrawn")[]`(없으면 `["rejected"]`로 읽는 도우미 `resubmittableStatuses(def)`를 같은 파일에 둔다). `domain/approvals/index.ts` `resubmitDocument` — 상태 판정을 `resubmittableStatuses`로 바꾸고, 지금 상태가 `withdrawn`이면 `nextStep(..., { allowResubmitFromWithdrawn: true })`로 부른다. 차수 +1 · 결재선 재고정(트랜잭션 전 `prepareSubmission` 결과로) · 기안자만 · 행동 로그 같은 tx는 04.1 동작 그대로다. `getApprovalView`의 가능 행동 계산에서 「다시 제출」 표시 조건도 같은 도우미를 쓴다(04.1의 `canResubmit` 판정은 그대로 곱한다). 종결 판정의 사건별 규칙(approve · reject · withdraw에는 `withdrawn`이 여전히 종결)은 바꾸지 않는다.

③ **E2** 종류 정의에 선택 필드 두 개 — `prepareFinalApproval?(viewer, documentId)`(Promise로 사실 묶음 P를 돌려줌)와 `onFinalApprovalInTx?(viewer, documentId, tx, prepared)`. 등록 때 둘 중 하나만 있으면 예외(04.1-05의 `loadDetails` ↔ `detailDto` 짝 규칙과 같은 모양). `approveDocument`는 트랜잭션을 열기 **전에** 종류에 `prepareFinalApproval`이 있으면 불러 결과를 들고 들어간다(최종 여부는 트랜잭션 안에서야 알므로 매 승인마다 읽는다 — 읽기 전용이라 부작용 없음). 트랜잭션 안 판정 순서 (1)~(7)에서, (4)의 계산이 `approve_final`이면 (6) 단계 기록 · 폴백 행 쓰기 **뒤** · (7) 행동 로그 **전**에 `onFinalApprovalInTx(viewer, documentId, tx, prepared)`를 부른다. 훅이 던진 오류는 잡지 않고 그대로 올려 트랜잭션 전체가 롤백된다(사용자 쪽 문구는 훅이 던진 `UserFacingError`의 문구).

④ **E4** `repositories/approvals.ts`에 `bumpInstanceVersion(viewer, { instanceId, expectedVersion?, updatedBy, reason }, tx)` — `version = version + 1`, `updated_by`, `updated_at`, `version_reason = reason`만 바꾸고(상태 · 차수 불변) `expectedVersion`이 주어지면 조건부 · 0행이면 null. `updateInstanceStatus`의 SET에 `version_reason = null` 한 칸을 더한다(상태가 바뀌면 증빙 표식이 지워진다). `domain/approvals/conflict-message.ts`의 충돌 문구 조립에 갈래 하나 — 관련자에게 보이는 상세 문구에서 인스턴스 `version_reason`이 `evidence`이면 `{updated_by 이름}이 HH:MM에 증빙을 더함 · 새로 고침`(시각은 `updated_at`의 서울 `HH:MM`, 04.1 문구와 같은 시각 서식 함수). 관련자가 아니면 04.1 규칙대로 `NotCurrentHolderError`. 엔진에 두 번째 토큰을 두지 않는다. 호출자(지출결의 증빙 추가 · 삭제 트랜잭션)는 05-04 · 05-09가 붙인다 — 이 task는 리포지토리 함수와 문구까지다.

⑤ **테스트** `test/unit/domain/approvals/next-step.test.ts`에 옵션 사례를 **덧붙이고**, `document-kind-registry.test.ts`에 훅 짝 사례를 덧붙인다. 새 파일 `test/integration/approvals-extensions.test.ts`의 첫 describe `트레이서 — 회수 뒤 다시 제출 · 증빙 변경 · 최종 훅`이 behavior 트레이서 세 줄을, 둘째 describe가 롤백 · 기본 동작 불변을 단언한다. 테스트 전용 종류는 테스트 파일 안에서 등록하고(04.1 `test_memo` 선례 — 종류 키는 이 파일에만 있는 이름), 사람 · 발령은 도메인 · 리포지토리 함수로 만든다(SQL 직접 삽입 금지).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/approvals</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 04.1의 기존 nextStep 사례 하나라도 빨개지거나(기본 동작이 바뀜), 옵션 없는 `withdrawn`/`resubmit`이 통과하거나, 훅 하나만 있는 등록이 거부되지 않으면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/approvals-extensions.test.ts test/integration/approvals-route-fixed.test.ts test/integration/approvals-concurrency.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 종료 코드가 0이 아니다 — 다시 제출이 새 인스턴스를 만들거나 차수가 2가 아니거나, 증빙 변경 뒤 옛 version 승인이 통과하거나 문구가 `증빙을 더함 · 새로 고침`이 아니거나, 훅 실패 뒤 상태 · version · 단계 · 로그가 남거나, 04.1 트레이서 · 동시성 사례가 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY frontmatter에 `name_map:`이 있고 ⓪의 최소 항목이 전부 적혀 있다
    - `grep -v '^\s*//' domain/approvals/*.ts | grep -cE '"(leave|expense|settlement)"'`이 0이다
    - `grep -c "allowResubmitFromWithdrawn" domain/approvals/route.ts domain/approvals/index.ts`가 두 파일 모두 1 이상이다
    - `grep -c "bumpInstanceVersion" repositories/approvals.ts`가 1 이상이고 `grep -c "version_reason" db/schema/approvals.ts`가 1 이상이다
    - 04.1 단위 테스트 파일의 diff가 덧붙임뿐이다 — `. "$(git rev-parse --git-dir)/gsd-05-01.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- test/unit/domain/approvals/next-step.test.ts test/unit/domain/approvals/document-kind-registry.test.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력한다(git이 실패하면 `&&`에서 멈춰 출력이 없다 — 그것도 실패다)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>테스트 전용 종류 한 건이 회수 뒤 같은 문서로 다시 제출되고, 증빙 변경이 옛 version 승인을 막으며, 최종 승인 훅이 같은 트랜잭션에서 돌고 실패하면 전부 롤백된다 — 04.1 기본 동작은 그대로다</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 스키마 확정 — `version_reason` 마이그레이션 적용 · SQL 린트 · journal 가드</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `db/migrations/meta/_journal.json`(전문)
    - Task 1이 만든 마이그레이션 SQL(`git status --porcelain db/migrations`로 찾는다)
    - `.planning/phases/04.1-approvals-leave/04.1-01-PLAN.md` Task 2(①~④ — 재생성 확인 · 빈 DB 적용 · SET LOCAL 손 삽입 · manifest)
    - 04.1이 만든 journal 가드 테스트(name_map의 경로 — 계획상 `test/unit/db/migration-journal.test.ts`)
  </read_first>
  <action>
① Task 1 스키마 위에서 `pnpm db:generate`를 다시 돌려 새 파일이 생기지 않음을 확인한다(생기면 `systematic-debugging`).
② 실제 빈 DB에 전부 적용한다 — `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`, 개발 DB `erp`는 `pnpm db:migrate`. `pnpm lint:sql`(squawk)이 기존 표 `approval_instances`를 건드리는 `ALTER TABLE` 문장에 `require-lock-timeout` · `require-statement-timeout`을 내면 그 문장 앞에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` · `--> statement-breakpoint` 블록 하나만 손으로 넣는다(스냅숏 불변 — 재생성은 여전히 "No schema changes"). 새 열은 null 허용이라 테이블 재작성 · 백필이 없다. 그 밖의 squawk 거부는 스키마 TS를 고쳐 재생성한다.
③ SUMMARY frontmatter `migration_manifest:`에 이 플랜의 journal tag · SQL 경로 · 스냅숏 경로와 손 삽입 줄 번호를 적는다(05-13이 병합 때 이 목록의 파일만 지우고 재생성한다).
  </action>
  <verify>
    <automated>G=$(pnpm db:generate 2&gt;&amp;1); RC=$?; printf '%s\n' "$G"; test "$RC" -eq 0 &amp;&amp; grep -q "No schema changes" &lt;&lt;&lt;"$G"</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 "No schema changes"가 없다 — 스키마 TS와 마이그레이션이 어긋났다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db'</automated>
    <fails_when>빈 `erp_test` 적용이 0이 아닌 코드로 끝나거나, squawk가 위반을 하나라도 출력하거나, journal 가드 단위 테스트가 실패하거나 "No test files found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 플랜이 만든 SQL 파일이 정확히 하나이고 이름이 drizzle-kit이 준 그대로다
    - `. "$(git rev-parse --git-dir)/gsd-05-01.env" && test -n "$PLAN_BASE" && git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json`의 변경이 끝에 항목 하나 추가뿐이다
    - 생성된 SQL의 손 편집은 SET LOCAL 블록 삽입뿐이고, SUMMARY에 삽입 전후 차이(추가 줄만)를 인용한다
    - SUMMARY frontmatter `migration_manifest:`의 세 경로가 실제 파일과 같다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>`version_reason` 열이 빈 DB부터 적용되고 재생성 차이 0 · squawk 0 · journal 가드 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 표시 쪽 덧붙임 — 승인 막힘 이유(E3) · 숫자 열(E5) · `본인 승인` 낱말(E6) · 결재 시트 `ui/` 이동(E7) · 04.1 E2E 회귀</name>
  <files>domain/approvals/kinds.ts, domain/approvals/index.ts, domain/approvals/dto.ts, domain/leave/index.ts, ui/approval-route/ApprovalRoute.tsx, ui/approval-sheet/ApprovalSheet.tsx, ui/approval-sheet/ApprovalSheet.module.css, app/(app)/approvals/approval-sheet.tsx, app/(app)/approvals/inbox-table.tsx, test/integration/approvals-extensions.test.ts</files>
  <read_first>
    - Task 1 산출물 `domain/approvals/kinds.ts` · `domain/approvals/index.ts`(`listMyInbox` · `getApprovalView` DTO 조립)
    - `domain/approvals/dto.ts`(결재함 항목 · 결재선 보기 명세 — `registerDto` 필드)
    - `app/(app)/approvals/approval-sheet.tsx` · `app/(app)/approvals/inbox-table.tsx`(name_map 경로) · `grep -rln "approval-sheet" app ui test`(모든 import 자리)
    - `ui/approval-route/ApprovalRoute.tsx`(단계 결과 낱말 매핑)
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S9(538–550행) · Color 매핑표(215–230행) · Component Inventory ApprovalSheet 행(121행)
    - `docs/DESIGN.md` §4 규칙 3(두 번째 사용처에서 `ui/`로 옮긴다)
    - `ui/button/Button.tsx`의 `disabledReason` · `reasonTone` prop
  </read_first>
  <behavior>
    - (통합) 테스트 종류에 `approveBlockedReason`이 `{문서 id → "진행으로 바뀜 · 반려"}`를 돌려주게 등록하면 지금 담당의 `getApprovalView` · `listMyInbox` 항목 `approveBlockedReason`이 그 글자이고, 필드가 없는 종류(연차)는 null이다. 그 상태에서도 서버 `approveDocument`는 이 필드로 거부하지 않는다(훅이 없는 종류라면 승인 성공)
    - (통합) 연차 한 건만 있는 결재함의 `measureHeader`는 `일수`, 테스트 종류가 `measure: { kind: "money", krw: 12400000, currency: "KRW", foreignAmount: null, fxRate: 1 }`을 실은 문서가 함께 있으면 `금액 · 일수`, 금액 문서만이면 `금액`, 둘 다 `measure`가 없으면 null
    - (통합) `self_approve` 설정의 테스트 종류에서 기안자가 1단을 본인 승인한 뒤 결재선 보기 단계의 결과 낱말이 `본인 승인`이다(DTO 문자열 — 컴포넌트는 그대로 그린다)
    - (E2E 회귀) 04.1 결재 · 연차 스펙이 이동 뒤에도 `CI=true`로 녹색
  </behavior>
  <action>
① **E3** 종류 정의 선택 필드 `approveBlockedReason?(viewer, documentIds: string[])` → `Map<string, string>`. `getApprovalView`와 `listMyInbox`(`withDetails` 여부와 무관하게 `mine` 항목만)가 viewer가 지금 담당인 문서 id를 모아 종류마다 **한 번** 불러 DTO 필드 `approveBlockedReason: string | null`에 싣는다(트랜잭션 없음 · 읽기 전용). `domain/approvals/dto.ts` 명세에 필드를 더하고 정보 항목은 기존 `approval.value`. 결재 시트와 결재함 PC 행의 `승인` 버튼은 이 값이 있으면 `ui/button`의 `disabledReason` + `reasonTone: "block"`(`aria-disabled`, 네이티브 `disabled` 아님)으로 그리고, `반려`는 그대로 산다. 서버 승인 판정에는 넣지 않는다 — 막는 최종 판정은 훅(E2)이다.

② **E5** `describeDocuments`가 돌려주는 요약 DTO에 선택 필드 `measure?: { kind: "money"; krw: number; currency: string; foreignAmount: string | null; fxRate: string } | { kind: "days"; quarters: number } | null`을 더하고, `listMyInbox` 결과에 `measureHeader: "금액" | "일수" | "금액 · 일수" | null`을 서버가 계산해 싣는다(그 목록의 `mine` + `processed` 항목에 있는 `measure.kind` 집합으로). 04.1 결재함 표가 일수 칸을 그리던 자리를 `measureHeader`와 항목 `measure`로 그리게 바꾸고(money = 원화 1행 + 외화면 2행 `USD 4,200.00 @1,318.4`, days = 04.1의 일수 서식 함수 그대로), 연차 종류(`domain/leave/index.ts`)의 요약에 `measure: { kind: "days", quarters }` 한 칸을 더한다 — 04.1 결재함 화면 글자는 바뀌지 않는다. 금액 서식은 `ui/` 쪽 기존 금액 서식 함수(`.num` · 쉼표)를 쓰고 산술은 하지 않는다.

③ **E6** 04.1 결재선 단계 결과 낱말 매핑(name_map의 파일 — 계획상 `ui/approval-route/ApprovalRoute.tsx` 또는 결재선 보기 DTO 조립)에 `self_approved` 단계 → `본인 승인 MM-DD HH:mm`, kind `success` 한 줄을 더한다(UI-SPEC Color 매핑표 · B2). 다른 낱말은 바꾸지 않는다.

④ **E7** `app/(app)/approvals/approval-sheet.tsx`와 그 CSS 모듈을 `git mv`로 `ui/approval-sheet/ApprovalSheet.tsx` · `ui/approval-sheet/ApprovalSheet.module.css`로 옮기고 export 이름을 `ApprovalSheet`로 맞춘다. import 자리(`grep -rln "approval-sheet" app ui test`)만 새 경로로 바꾼다. 시트는 문서 종류를 모른다 — 본문 행은 서버 DTO(04.1-05 `buildDetailRows`)를 그대로 그린다. 동작 · 마크업 · 스타일 변경 없음(이동만).

⑤ **테스트** `test/integration/approvals-extensions.test.ts`에 describe `표시 덧붙임 — E3 · E5 · E6`을 덧붙여 behavior 통합 세 줄을 단언한다. 04.1 E2E는 고치지 않고 돌려 회귀를 본다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/approvals-extensions.test.ts test/integration/approvals-inbox-projection.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — `approveBlockedReason`이 담당이 아닌 항목에 실리거나 서버 승인이 그 값으로 거부되거나, `measureHeader`가 네 경우 중 하나라도 다르거나, `본인 승인` 낱말이 없거나, 04.1 결재함 노출 조회 횟수 단언(2회 이하)이 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint · stylelint · tsc · squawk · next build 중 하나라도 error를 출력하거나 0이 아닌 코드로 끝난다 — 옛 경로 import가 남으면 build가 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/mobile-leave-approval.spec.ts test/e2e/leave-document.spec.ts'</automated>
    <fails_when>playwright failed가 1 이상이거나 "did not run"이 1 이상이다 — 결재 시트 이동이 04.1 폰 결재 · 문서 화면 흐름을 깨면 여기서 깨진다(스펙 이름은 name_map으로 확인 — 다르면 실제 04.1 결재 E2E 이름으로 바꿔 돌리고 SUMMARY에 적는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files "app/(app)/approvals/approval-sheet.tsx"`이 빈 출력이고 `git ls-files ui/approval-sheet/ApprovalSheet.tsx`가 한 줄이다
    - `grep -rn "approvals/approval-sheet" app ui test`가 0건이다
    - `grep -c "approveBlockedReason" domain/approvals/kinds.ts domain/approvals/index.ts`가 두 파일 모두 1 이상이고 `grep -c "measureHeader" domain/approvals/index.ts`가 1 이상이다
    - `grep -c "본인 승인" ui/approval-route/ApprovalRoute.tsx`가 1 이상이다(name_map이 다른 파일을 가리키면 그 파일)
    - `. "$(git rev-parse --git-dir)/gsd-05-01.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- docs/design/tokens.css package.json pnpm-lock.yaml`이 비어 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재함 · 시트가 종류가 준 막힘 이유와 숫자 열을 그리고, 본인 승인이 낱말로 보이며, 결재 시트가 `ui/`에서 두 사용처를 기다린다 — 04.1 화면은 그대로다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 종류 모듈 → 결재 엔진 | 종류가 등록한 훅 · 막힘 이유가 엔진의 트랜잭션 · DTO에 들어온다 |
| 기안자의 증빙 변경 → 결재자의 승인 | 결재자가 본 문서와 승인 순간의 문서가 같아야 한다 |
| 호출자 → `resubmitDocument` | 회수된 문서를 누가 다시 올릴 수 있는가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-101 | Tampering | 제출 뒤 증빙 변경과 승인 경합 | high | mitigate | E4 — 증빙 변경 트랜잭션이 인스턴스 version을 올려 옛 version 승인이 `ApprovalConflictError`로 막힌다. Task 1 트레이서가 옛 version 거부 · 단계 기록 0건을 단언. 실제 호출자 배선과 두 순서 통합은 05-04 · 05-09 |
| T-05-102 | Elevation of Privilege | 회수 뒤 다시 제출(E1) | medium | mitigate | 04.1 `resubmitDocument`의 기안자 판정 · `canResubmit` 곱을 그대로 두고 상태 목록만 종류 필드로 넓힌다. `resubmitFrom`이 없는 종류(연차)는 거부가 그대로라는 통합 사례 |
| T-05-103 | Tampering | 최종 승인 훅(E2) 부분 커밋 | high | mitigate | 훅은 같은 `tx`에서 돌고 오류를 삼키지 않는다 — 실패 주입 시 상태 · version · 단계 · 로그 불변 통합 사례. `prepareFinalApproval`은 트랜잭션 전(풀 교착 방지) |
| T-05-104 | Denial of Service | 트랜잭션 안 풀 읽기 | medium | mitigate | 훅의 읽기 짝을 트랜잭션 전으로 강제(호출 순서 단언) — ARCHITECTURE §4-8 (3) |
| T-05-105 | Information Disclosure | 충돌 문구의 기안자 이름 · 시각 | low | mitigate | 04.1 `isApprovalParty` 관련자에게만 상세 문구, 아니면 `NotCurrentHolderError` — 갈래를 그대로 쓴다 |
| T-05-106 | Elevation of Privilege | `approveBlockedReason`을 권한 판정으로 오용 | low | mitigate | 표시 전용 — 서버 승인은 이 필드를 보지 않고 최종 판정은 훅. 「필드가 있어도 훅 없는 종류는 승인 성공」 통합 사례 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(05-RESEARCH Package Legitimacy Audit — 설치 대상 없음). `package.json` · `pnpm-lock.yaml` diff가 비어 있다는 Task 3 수용 기준이 검증이다 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 전체 통과(04.1 결재 단위 · journal 가드 · import-cycles 포함)
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration test/integration/approvals-*.test.ts'` 통과
- 04.1 결재 · 연차 E2E `CI=true` 녹색
- `domain/approvals`에 종류 이름 리터럴 0건
</verification>

<success_criteria>
- 회수된 문서를 같은 번호 · 같은 인스턴스로 다시 올릴 수 있는 길이 종류 옵션으로 열렸다(E1)
- 최종 승인이 다른 도메인의 쓰기를 같은 트랜잭션으로 끝낼 수 있다(E2 — 05-11 정산 결재가 쓴다)
- 증빙이 바뀌면 결재자는 새로 본 뒤에만 승인한다(E4)
- 결재함 · 시트가 문서 종류를 몰라도 막힘 이유 · 숫자 열 · 본인 승인을 그린다(E3 · E5 · E6 · E7)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 열 | `approval_instances.version_reason`(text null, CHECK ∈ {evidence}) |
| 마이그레이션 | drizzle-kit 이름 SQL 1개 + 스냅숏(브랜치 임시 번호 — 05-13 재생성, SUMMARY `migration_manifest`) |
| 함수 · 필드 | `nextStep` 선택 인자 `allowResubmitFromWithdrawn` · 종류 정의 `resubmitFrom` · `prepareFinalApproval` · `onFinalApprovalInTx` · `approveBlockedReason` · 요약 `measure` · `resubmittableStatuses` · `bumpInstanceVersion` · 결재함 DTO `measureHeader` · 보기 DTO `approveBlockedReason` |
| 컴포넌트 | `ui/approval-sheet/ApprovalSheet.tsx`(04.1에서 이동) |
| 문구 | `{이름}이 HH:MM에 증빙을 더함 · 새로 고침` · 결재선 `본인 승인 MM-DD HH:mm` |
| 테스트 | `test/integration/approvals-extensions.test.ts`(새) · `next-step` · `document-kind-registry` 사례 추가 |
| 기록 | SUMMARY frontmatter `name_map`(뒤 플랜 열두 개의 이름 정본) · `migration_manifest` |

<output>
Create `.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md` when done
</output>
