---
phase: 05-expense-approval-leave
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - db/schema/expenses.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - repositories/expenses.ts
  - domain/expenses/index.ts
  - domain/expenses/dto.ts
  - domain/expenses/access.ts
  - domain/expenses/tax.ts
  - domain/expenses/line-door.ts
  - domain/expenses/gate.ts
  - domain/document-numbering/index.ts
  - domain/money/index.ts
  - domain/settings/keys.ts
  - domain/settings/registry.ts
  - domain/permissions/info-items.ts
  - domain/seed/expenses.ts
  - domain/seed/index.ts
  - app/(app)/document-kinds.ts
  - app/(app)/admin/code-tables/page.tsx
  - docs/ARCHITECTURE.md
  - test/integration/leak-scan.test.ts
  - test/integration/fixtures/expenses.ts
  - test/integration/expense-approval-lifecycle.test.ts
  - test/integration/expense-create-idempotency.test.ts
  - test/integration/expense-submit-concurrency.test.ts
  - test/integration/expense-installment-cap.test.ts
  - test/unit/domain/expenses/expense-number-format.test.ts
  - test/unit/domain/expenses/installment-cap.test.ts
  - test/unit/domain/expenses/tax.test.ts
  - test/unit/domain/money.test.ts
autonomous: true
requirements: [EXP-01, EXP-14, EVID-01]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "기획 PM이 진행 프로젝트(현재 차수 고객 승인됨)의 견적 줄 하나로 `createExpenseFromLines`를 부르면 번호 없는 작성 중 지출결의가 생기고 거래처 · 증빙 종류(거래처 기본 증빙) · 통화(줄의 실행가 통화) · 공급가액(줄의 남은 실행가) · 환율(통화별 최근 환율, D-71) · 지급 방식(코드표 `payment_method`의 첫 활성 값) · 프로젝트가 채워져 있다 — 같은 값을 다시 적지 않는다(EXP-01)"
    - "같은 줄에서 같은 기안자가 두 번 · 동시에 두 번 불러도 작성 중 문서는 하나다 — 부분 UNIQUE `(quote_line_id, drafter_id) WHERE number IS NULL AND deleted_at IS NULL`와 `INSERT … ON CONFLICT DO NOTHING` + 기존 행 조회가 최종 판정한다(EXP-01 idempotency)"
    - "여러 줄을 한 번에 넘기면 문이 열린 줄마다 작성 중 문서가 하나씩 생기고, 막힌 줄은 `{lineId, reason}`으로 돌아온다 — 문서는 줄마다 하나다(EXP-01 · UI-SPEC 확정 #5의 서버 쪽)"
    - "제출하면 문서 번호가 `{프로젝트 번호}-{순번 4자리}`(예 `26001-0001`)로 `document_counters`의 카운터 `expense` · period = 프로젝트 번호 행 잠금으로 매겨진다 — 같은 프로젝트의 제출 6건을 동시에 보내도 기본 풀(5)에서 전부 끝나고 번호가 서로 다르다(기준 1 · Issue 10 · 사용자 결정 2026-09-26 #6)"
    - "번호는 제출 때만 부여되고 반려 · 회수 뒤에도 그대로다 — 작성 중 문서에는 번호가 없다(D-42)"
    - "같은 비분할 견적 줄의 작성 중 문서 둘(기안자 둘)을 동시에 제출하면 하나만 통과하고 다른 쪽은 `이 줄에 지출결의 {번호} 있음 · 지출결의 열기`로 거부된다 — 프로젝트 행 잠금 안에서 줄의 번호 있는 문서를 다시 읽어 판정한다(ARCHITECTURE §4-8 (2) · UI Assumptions #1)"
    - "줄의 번호 있는 문서(자기 자신 제외) 공급가액 합 + 이번 공급가액 ≤ 실행가 — 넘으면 공급가액 칸 오류 `남은 실행가 {금액} 넘음 · 공급가액 고치기`로 제출이 거부된다. 문서 통화와 줄 통화가 모두 같으면 원래 통화 금액으로, 하나라도 다르면 원화로 비교한다(계획 결정 — RESEARCH Open Q4)"
    - "앞 회차(번호 있는 분할 문서)가 있는 줄에서 만든 문서는 체크박스와 무관하게 분할 문서이고 회차는 제출 때 그 줄의 번호 있는 문서 수 + 1이다. 앞 회차가 없으면 `분할 지급`을 켠 문서가 1회차다(UI-SPEC 확정 #1)"
    - "제출된 문서의 공급가액 원화는 0보다 크다 — DB CHECK `number IS NULL OR supply_amount_krw > 0`와 `supply_amount_krw IS NULL OR supply_amount_krw >= 0`가 최종 판정하고, 서버는 빈 칸이면 `공급가액 비어 있음 · 공급가액 적기`, 0이면 `공급가액이 0 · 0보다 크게`로 먼저 거부한다(EXP-14)"
    - "제출 때 세금 스냅숏(규칙 종류 · 세율 · 세율 설정 이력 행 id(없으면 null) · 회사 대납 방식 · 기준일 · 부가세 · 원천징수 · 회사 대납 세액 · 지급 총액)이 저장된다 — `computeExpenseTax`가 트랜잭션 전에 `applyTaxRule()`을 정확히 한 번 부른 결과이고, 브라우저가 보낸 금액은 저장하지 않는다(기준 7 · 05-06이 규칙 전 칸을 굳힌다)"
    - "지출결의의 화면 상태는 결재 인스턴스에서 파생된다 — 인스턴스가 없으면 `작성 중`, 있으면 그 상태다. `expenses` 표에 결재 상태 열이 없고, 작성 중 문서에는 결재 인스턴스 행이 없다(RESEARCH Pattern 1 · Anti-Patterns)"
    - "제출된 지출결의의 결재선은 문서 종류 지출결의의 결재선 설정 17키(`approval_route.expense.*`, 기본 4단 · 자기 승인 `self_approve`)로 제출 때 고정되고, 결재자가 승인해 최종 `approved`까지 간다(기준 2 · 입력 §3)"
    - "제출 트랜잭션의 잠금 순서는 프로젝트 행 → 지출결의 행(`FOR UPDATE` + version) → `document_counters`(마지막 쓰기)다. 설정 · 번호 서식 · 결재선 준비 · 세금 계산 · 조직 스냅숏은 트랜잭션 전에 읽는다(ARCHITECTURE §4-8 (2)(3))"
    - "작성 중 문서를 두 창에서 고치면 늦은 쪽 저장이 `HH:MM에 다른 곳에서 저장됨 · 새로 고침`으로 거부된다(문서 `version` — UI-SPEC 「거부 — 동시 처리」)"
    - "`project.customer_approval_gate`의 `readBy` 표시가 지워지고 이 페이즈가 그 설정을 읽는다 — 진행 · 현재 차수 고객 승인 전(설정 켜짐)이면 작성 중 문서를 만들지 않고 `quote.customer-approval` 규칙 문자열로 거부한다. 완료 프로젝트는 `완료 프로젝트 · 새 지출결의 없음`으로 거부한다(D-43 · D-47 · D-54 · Pitfall 3)"
    - "새 DTO(작성 중 · 문서)가 `registerDto`로 등록되고 필드가 정보 항목 `expense.value` · `expense.amount`에 매핑되어 누수 스캔에 들어간다(ROADMAP 기준 5 결)"
    - "계급 네 개(기획 PM · 팀장 · 본부 책임자 · 대표)에 `expenses` 보기 · 쓰기가 없을 때만 시드되고, 코드표 `payment_method`(계좌이체 · 법인카드 · 현금)가 시드되어 관리자 코드표 화면에서 고칠 수 있다(UI-SPEC UA-618 — 계획 결정: 코드표가 main에 없어 이 플랜이 만든다)"
  prohibitions:
    - statement: "브라우저가 보낸 세액 · 지급 총액 · 원화 환산액을 저장해서는 안 된다 — 서버가 다시 계산한 값만 저장한다"
      status: resolved
      verification: judgment
    - statement: "`expenses`에 결재 상태 복사 열을 두어서는 안 된다 — 정본이 둘이 되어 어긋난다"
      status: resolved
      verification: judgment
    - statement: "작성 중 문서를 결재 인스턴스 `draft` 행으로 표현해서는 안 된다 — 결재함 · 관련자 판정이 흔들린다"
      status: resolved
      verification: judgment
    - statement: "`withTransaction` 콜백 안에서 `applyTaxRule` · `getSettingValue` · `teamAtDate` · `can()` · `prepareSubmission`을 불러서는 안 된다(풀 소진 교착)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/schema/expenses.ts"
      provides: "expenses 표 — 견적 줄 · 팀 비용 한 표, 공급가액 금액 모델 네 열(원화 null 허용 — 작성 중 빈 칸), 세금 스냅숏 열, 부분 UNIQUE, CHECK"
      contains: "expenses_line_drafter_draft_uniq"
    - path: "domain/expenses/index.ts"
      provides: "지출결의 종류 등록 · createExpenseFromLines · saveExpenseDraft · submitExpense · getExpense"
      exports: ["createExpenseFromLines", "saveExpenseDraft", "submitExpense", "getExpense"]
    - path: "domain/expenses/tax.ts"
      provides: "pickTaxDates · incomeTypeFor · resolveAppliedRate · computeExpenseTax(applyTaxRule 한 번)"
      exports: ["pickTaxDates", "incomeTypeFor", "resolveAppliedRate", "computeExpenseTax"]
    - path: "domain/expenses/line-door.ts"
      provides: "expenseLineDoor — 줄마다 문 열림/닫힘/거래처 없음/행동 없음 판정(S1 · S2 · S14 · 게이트 ④ 공용)"
      exports: ["expenseLineDoor"]
    - path: "domain/document-numbering/index.ts"
      provides: "expenseNumberFormat · loadExpenseNumberFormat · allocateExpenseNumber(period = 프로젝트 번호)"
      contains: "allocateExpenseNumber"
    - path: "domain/money/index.ts"
      provides: "sumKrw · diffKrw · remainingForInstallments(06-02와 같은 이름 — Phase 5가 먼저 만든다)"
      contains: "sumKrw"
    - path: "test/integration/expense-approval-lifecycle.test.ts"
      provides: "트레이서 describe — 견적 줄 → 작성 중 → 제출(번호 · 세금 스냅숏) → 최종 승인"
      min_lines: 60
    - path: "test/integration/fixtures/expenses.ts"
      provides: "픽스처 — 진행 프로젝트 · 승인 차수 · 견적 줄(거래처 있음 · 없음 · 분할용) · 결재 담당 · submitReadyDraft 도우미(05-04가 증빙 첨부를 더한다)"
  key_links:
    - from: "domain/expenses/index.ts"
      to: "domain/approvals/kinds.ts"
      via: "모듈 적재 때 registerDocumentKind(지출결의 — resubmitFrom 둘, canResubmit, describeDocuments + measure)"
      pattern: "registerDocumentKind"
    - from: "domain/expenses/index.ts"
      to: "domain/document-numbering/index.ts"
      via: "제출 트랜잭션 마지막 쓰기로 allocateExpenseNumber(period = 프로젝트 번호)"
      pattern: "allocateExpenseNumber"
    - from: "domain/expenses/tax.ts"
      to: "domain/money/tax.ts"
      via: "applyTaxRule 한 번 + 세율 행 id는 getSettingEntry로 같은 기준일 조회"
      pattern: "applyTaxRule"
    - from: "app/(app)/document-kinds.ts"
      to: "domain/expenses/index.ts"
      via: "종류 등록 한 곳에 import 한 줄"
      pattern: "domain/expenses"
    - from: "domain/expenses/index.ts"
      to: "domain/rules/gate.ts"
      via: "작성 중 문서 생성 때 기존 규칙 quote.customer-approval 문자열을 그대로 쓴다"
      pattern: "quote.customer-approval"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」 — 계획은 이야기를 지어내지 않았다).

<objective>
지출결의의 **도메인 트레이서**: 견적 줄 하나 → 작성 중 문서(자동 채움) → 임시 저장 → 제출(프로젝트별 번호 · 세금 스냅숏 · 결재선 고정) → 결재자 승인 → 최종 승인을 표 · 리포지토리 · 도메인 · 설정 · 번호 · 결재 엔진까지 한 번에 관통시키고 통합 테스트로 끝에서 끝까지 증명한다. 이어서 EXP-01의 날카로운 가장자리(두 번 눌러도 하나 · 동시 제출 번호 · 같은 줄 경합 · 회차 상한)와 EXP-14(금액 > 0)를 DB 제약과 잠금으로 굳힌다.

Purpose: 기준 1(1줄 1문서 · 회차 · idempotency · 번호 · 금액 모델)과 기준 2(제출 시 결재선 고정)가 이 플랜에서 성립한다. 화면 트레이서(05-05)는 이 서비스 위에 선다(서버 액션은 Vitest가 import할 수 없어 화면 경로는 Playwright로만 증명된다 — 04.1과 같은 두 트레이서 나눔).

Output: 표 1 · 마이그레이션 1 · `domain/expenses`(종류 등록 · 생성 · 저장 · 제출 · 조회 · 세금 호출자 · 문 판정 · 제출 점검) · 번호 함수 셋 · `domain/money` 합 · 차이 · 회차 남은 금액 · 설정 키 20(결재선 17 · 번호 3) · 정보 항목 둘 · 시드 · 통합 4 · 단위 4.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@.planning/phases/05-expense-approval-leave/05-RESEARCH.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다(실패 테스트 먼저). 실패를 쫓기 전에 `systematic-debugging`, 커밋 · 「완료」 전에 `verification-before-completion`을 호출하고 verify 명령의 실제 출력으로 판정한다. 서브에이전트 위임 시 프롬프트에 세 스킬 이름과 `model`(구현 `sonnet`)을 적는다.
- 이름은 05-01 SUMMARY `name_map`의 실제 이름을 쓴다(아래 본문의 04.1 이름은 계획 원문).
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-03.env"` — diff 검사는 `. "$(git rev-parse --git-dir)/gsd-05-03.env" && test -n "$PLAN_BASE" &&`로 시작.
- **Task 1 중간 커밋(04.1-01 ENG-22와 같은 규칙)**: 단위 A 「스키마 + 마이그레이션 생성」(확인 `pnpm typecheck` · `pnpm db:generate` 재실행 "No schema changes") → 단위 B 「순수 함수」(번호 서식 · 문 판정 · 회차 남은 금액 · 세금 날짜 · incomeType — 단위 테스트 먼저) → 단위 C 「서비스 + 종류 등록 + 시드」(트레이서 통합 describe 먼저). 한 단위가 실패하면 마지막 녹색 단위 커밋으로 돌아가 그 단위만 다시 한다. 스키마를 고쳐야 하면 두 번째 마이그레이션을 만들지 않고 단위 A 마이그레이션을 git으로 되돌려 재생성한다(파일 이름 · journal 손 편집 금지).
- 트랜잭션 규약 · 「오늘」 = `seoulToday(deps.now)` · 금액 산술은 `domain/money` 밖 금지(린트 money-boundary) · `.planning/STATE.md` 불변 · 새 의존성 0 — 05-01 execution_notes와 같다. 통합 · E2E 명령은 `flock /tmp/plant8-erp-test.lock`.
- **증빙은 05-04가 붙인다.** 이 플랜의 제출은 증빙 수를 보지 않는다 — 05-04가 제출 점검 ⑧(증빙 1개 이상)을 더하면서 픽스처 도우미 `submitReadyDraft`에 증빙 첨부를 넣는다(이 플랜 테스트는 그 도우미로만 제출한다 — 그래서 05-04 뒤에도 고칠 필요가 없다).
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 견적 줄 하나 → 작성 중(자동 채움) → 임시 저장 → 제출(`26001-0001` · 세금 스냅숏 · 결재선 고정) → 팀장 · 대표 승인 → 최종 승인 (DB · 리포지토리 · 도메인 · 설정 · 번호 · 시드)</name>
  <reversibility rating="costly">문서 번호 꼴 `{프로젝트 번호}-{순번}`과 카운터 period = 프로젝트 번호는 발급된 번호를 다시 매길 수 없다 — 사용자가 이미 정한 꼴(2026-09-26 #6)이라 체크포인트 없이 기록만 한다.</reversibility>
  <files>db/schema/expenses.ts, db/schema/index.ts, repositories/expenses.ts, domain/expenses/index.ts, domain/expenses/dto.ts, domain/expenses/access.ts, domain/expenses/tax.ts, domain/expenses/line-door.ts, domain/expenses/gate.ts, domain/document-numbering/index.ts, domain/money/index.ts, domain/settings/keys.ts, domain/settings/registry.ts, domain/permissions/info-items.ts, domain/seed/expenses.ts, domain/seed/index.ts, app/(app)/document-kinds.ts, app/(app)/admin/code-tables/page.tsx, docs/ARCHITECTURE.md, test/integration/leak-scan.test.ts, test/integration/fixtures/expenses.ts, test/integration/expense-approval-lifecycle.test.ts, test/unit/domain/expenses/expense-number-format.test.ts, test/unit/domain/expenses/tax.test.ts, test/unit/domain/money.test.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 259–335행(Pattern 2 세금 호출자 · Pattern 3 문서 모델 · 잠금 순서 · Pattern 4 번호) · 495–563행(코드 예시 — 제출 뼈대 · 종류 등록)
    - `.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md`(`name_map`)
    - `db/schema/money-columns.ts`(전문 — 네 열 이름 · 타입 · notNull 규칙) · `db/schema/quote-lines.ts`(`check()` · 인덱스 선언 모양) · `db/schema/vendors.ts` 17–19행(코드 값 FK 없는 관례)
    - `domain/money/tax.ts`(전문 — `ApplyTaxRuleOpts` · `TaxRuleResult` · 67–73행 기준일 선택 · 88–91행 incomeType · 119–125행 회사 대납) · `domain/money/index.ts`(전문) · `domain/money/currency.ts` `recentFxRate`
    - `domain/code-tables/tax-rule.ts`(전문 — 규칙 종류 · 기준일 값) · `domain/seed/index.ts` 40–120행(증빙 종류 시드 · `basisDate`) · 140–215행(권한 시드 · `insertPermissionIfAbsent` 선례)
    - `domain/document-numbering/index.ts`(전문) · `repositories/document-counters.ts`(전문 — `allocateNumber(viewer, counterKey, period, tx)`)
    - `domain/settings/keys.ts` 56–150행(세율 · 기준일 키) · 236–310행(`PROJECT_CUSTOMER_APPROVAL_GATE`의 `readBy` · `document_number.project.*`) · `domain/settings/registry.ts`(전문) · `repositories/settings.ts` 41–54행(`findEffectiveValue`가 행 전체를 돌려준다)
    - `domain/rules/register.ts` 205–270행(`quote.customer-approval` · `quote.vendor-required` 문자열 · `CUSTOMER_APPROVAL_EXEMPT_STATUSES`) · `domain/rules/gate.ts`(전문)
    - `domain/quotes/lines.ts` 240–290행(`linkedDocumentsByLine` 자리 — 이 플랜은 고치지 않는다, 05-05) · `domain/quotes/lineage.ts`
    - `domain/projects/status.ts` 150–170행 · `repositories/projects.ts`의 프로젝트 행 잠금 읽기(`grep -n 'for("update")\|FOR UPDATE' repositories/projects.ts repositories/*.ts`) · `domain/projects/ledger.ts` `saveProjectLedger`가 쓰는 프로젝트 쓰기 권리 판정
    - `lib/db-transaction.ts` · `test/integration/projects-create-concurrency.test.ts`(풀 소진 교착 재현 모양) · `test/integration/document-counters-concurrency.test.ts`
    - 04.1 연차 종류 등록(`domain/leave/index.ts` — `registerDocumentKind` · `loadRouteConfig` 17키 · `getSimpleSettingValues`) · 04.1 결재선 17키 정의(`domain/settings/keys.ts`의 `approval_route.leave.*`) · 04.1 `domain/seed/approvals-leave.ts`
    - `test/unit/settings/registry-coverage.test.ts`(키 선언 형식 · readBy 규칙) · `test/integration/leak-scan.test.ts` 끝 · `test/unit/import-cycles.test.ts`
    - `docs/ARCHITECTURE.md` §4-6(번호 규약 — `grep -n "4-6" docs/ARCHITECTURE.md`)
  </read_first>
  <behavior>
    - (단위) `expenseNumberFormat("26001", 4, { separator: "-", seqDigits: 4, seqStart: 1 })` = `26001-0004`, `seqStart: 101`이면 `26001-0104`, 순번이 자릿수를 넘으면 자르지 않는다(`26001-10000`)
    - (단위) `incomeTypeFor("business_income")` = `business`, `incomeTypeFor("other_income")`와 그 밖 = `other`. `pickTaxDates`: 지급일 없음 · 지급 예정일 `2026-10-05` → 지급 쪽 날짜 = 지급 예정일, 지급 예정일도 없으면 서울 오늘 · 증빙일 없음 → 증빙 쪽 날짜 = 작성일(문서 `created_at`의 서울 날짜 — D-101)
    - (단위) `sumKrw([])` = 0 · `sumKrw([1, -2, 3])` = 2 · `diffKrw(10, 3)` = 7 (원 정수 · 부호 유지 — 06-02 계약)
    - (통합, 트레이서) 픽스처(기획1팀: 기획 PM 박서연 · 팀장 김도윤, 대표 최대표 · 경영관리본부 소속 없음, 진행 프로젝트 `26001` · 1차 승인 · 줄 1 거래처 스테이지원(기본 증빙 `tax_invoice`) · 실행가 KRW 12,400,000)에서 PM이 `createExpenseFromLines({ lineIds: [줄1] })` → `created` 한 건 · 번호 null · 공급가액 원화 12,400,000 · 통화 KRW · 증빙 종류 `tax_invoice` · 거래처 스테이지원 · 지급 방식 `bank_transfer` · 결재 인스턴스 없음 · `getExpense` 상태 `작성 중`
    - (통합, 트레이서) `saveExpenseDraft({ expenseId, expectedVersion: 1, fields: { note: "1차 선금" } })` → version 2
    - (통합, 트레이서) `submitReadyDraft`(픽스처 도우미 → `submitExpense`) → 번호 `26001-0001` · `submitted_at` 있음 · 세금 스냅숏 규칙 `vat_surcharge` · 세율 0.1 · 부가세 1,240,000 · 지급 총액 13,640,000 · 세율 설정 이력 행 id null(시드에 이력 행 없음) · 결재 인스턴스 `submitted` · 차수 1 단계 행 넷(팀장 · 본부 책임자 · 경영관리본부 · 대표) · `getExpense` 상태 `결재 중`
    - (통합, 트레이서) 팀장 승인 → 2 · 3단 빈 자리 건너뜀 → 대표 승인 → 인스턴스 `approved` · 번호 그대로
    - (통합) PM이 아닌 무관한 기획 PM의 `getExpense`(작성 중)는 null(→ 404)이다
  </behavior>
  <action>
① **스키마(단위 A)** 새 파일 `db/schema/expenses.ts`의 표 `expenses`(snake_case · 시각 열은 기존 관례대로 무 tz `timestamp`): `id` uuid PK 기본 랜덤 · `drafter_id` text FK users not null · `number` text null UNIQUE · `project_id` uuid null FK projects · `quote_line_id` uuid null FK quote_lines · `team_expense_kind` text null · `usage_date` date(문자열 모드) null · `content` text null · `attributed_team_id` uuid null FK teams · `vendor_id` uuid null FK vendors · `evidence_type` text null(코드 값, FK 없음) · `payment_method` text null(코드 값) · 공급가액 금액 모델 네 열 `supply_currency`(text not null 기본 KRW) · `supply_foreign_amount`(numeric 14,2 null) · `supply_fx_rate`(numeric 12,4 not null 기본 1.0000) · `supply_amount_krw`(bigint number 모드 **null 허용** — 작성 중 빈 칸을 0과 가르기 위해서다. `db/schema/money-columns.ts`와 같은 이름 · 타입이고 원화만 null 허용인 이유를 파일 머리 주석 한 줄로 적는다 — 헬퍼는 고치지 않는다) · `installment` boolean not null 기본 false · `installment_seq` int null · `scheduled_payment_date` date null · `note` text null · 세금 스냅숏 `tax_rule_kind` text null · `tax_rate` numeric(7,6) null · `tax_rate_setting_id`(settings 이력 행 id와 같은 타입, FK) null · `tax_company_borne_method` text null · `tax_basis_date` date null · `vat_krw` · `withholding_krw` · `company_borne_krw` · `payable_krw`(bigint number null) · `idempotency_key` text null UNIQUE · `submitted_at` null · `version` int not null 기본 1 · `updated_by` text FK users null · `deleted_at` null · `deleted_by` text null · `created_at` · `updated_at`. CHECK: `team_expense_kind IS NULL OR team_expense_kind IN ('lost_bid','team_overhead')` · `supply_amount_krw IS NULL OR supply_amount_krw >= 0` · `supply_foreign_amount IS NULL OR supply_foreign_amount >= 0` · `number IS NULL OR supply_amount_krw > 0`(EXP-14) · `NOT (quote_line_id IS NOT NULL AND team_expense_kind IS NOT NULL)` · `installment_seq IS NULL OR installment_seq >= 1` · `tax_rule_kind IS NULL OR tax_rule_kind IN ('none','vat_surcharge','withholding','company_borne')`. 부분 UNIQUE 인덱스 `expenses_line_drafter_draft_uniq` ON (quote_line_id, drafter_id) WHERE number IS NULL AND deleted_at IS NULL · 인덱스 (project_id) · (quote_line_id) · (drafter_id) · (attributed_team_id). 상태 열은 두지 않는다. `db/schema/index.ts` 끝에 export 한 줄. `pnpm db:generate`(적용 · 가드는 Task 2).

② **리포지토리** `repositories/expenses.ts`(viewer 첫 인자 · `DbOrTx` 선택): `insertDraftIfAbsent(viewer, values, tx?)`(`ON CONFLICT DO NOTHING` 대상 = 부분 UNIQUE → RETURNING, 비면 null) · `findDraftByLineAndDrafter` · `findExpenseById` · `lockExpenseForUpdate(viewer, id, tx)`(`FOR UPDATE`) · `updateDraftIfVersion(viewer, {id, expectedVersion, fields, updatedBy}, tx?)`(version 조건 + `version + 1` + RETURNING — 0행이면 null) · `saveSubmissionSnapshot(viewer, {id, taxSnapshot, installment, installmentSeq, submittedAt}, tx)` · `setExpenseNumber(viewer, {id, number}, tx)` · `listNumberedByLine(viewer, lineId, tx?)`(번호 있는 · 삭제 안 된 문서의 id · 번호 · 분할 · 공급가액 네 열 · 제출 시각 — 문 판정 · 회차 상한 재료) · `listExpenseSummaries(viewer, ids)`(결재함 요약용 — 프로젝트명 · 항목명 · 팀명 · 공급가액).

③ **순수 함수(단위 B)** `domain/expenses/line-door.ts` `expenseLineDoor({ line: {lineKind, cancelled, vendorId, execution}, numbered: [...], selfId? })` → `{ state: "open" | "closed" | "no_vendor" | "none", latest?: {id, number}, remaining: Money | null, nextInstallmentSeq: number | null, forcedInstallment: boolean }` — `none` = 견적 줄이 아님 · 취소 · 조정 · 견적 외 비용, `no_vendor` = 거래처 없음, `closed` = 자기 자신 밖에 번호 있는 비분할 문서가 있거나 남은 실행가가 0 이하, 그 밖 `open`(06-02의 `resolveLineDoor`와 다른 축이라 이름이 다르다). 남은 금액은 `domain/money`의 새 함수 `remainingForInstallments(execution: Money, others: Money[], current?: MoneyInput)` → `{ basis: "foreign" | "krw", remaining: Money, exceeds: boolean }`(문서 통화 = 줄 통화가 모두 같으면 원래 통화 금액으로, 하나라도 다르면 원화로 비교 — Q4 계획 결정)와 `sumKrw(values: number[])` · `diffKrw(a, b)`(원 정수, 부호 유지, 빈 배열 합 0 — 06-02가 계획한 이름 그대로, 06-02는 이 작업을 건너뛴다)를 `domain/money/index.ts`에 더해 쓴다. `domain/expenses/tax.ts`: `pickTaxDates(doc, { todayKst })` → `{ paymentDate, evidenceDate }`(지급 쪽 = 지급일(Phase 6 전에는 없음) → 지급 예정일 → 서울 오늘, 증빙 쪽 = 증빙일(이 페이즈에 없음) → 작성일 — D-101 · 06-03이 같은 파일 · 같은 이름을 확장한다) · `incomeTypeFor(evidenceType)` · `resolveAppliedRate(rule, dates, deps?)` → `{ key, rate, historizedId: string | null, effectiveFrom: string | null, method? }`(`applyTaxRule`이 그 규칙에서 읽는 세율 키 하나를 **같은 기준일**로 조회 — `domain/money/tax.ts` 67–73행과 같은 기준일 선택을 따른다. 이력 행이 없으면 `historizedId: null` + 기본값) · `computeExpenseTax(doc, deps?)` = 증빙 종류 코드 항목의 `taxRule` 읽기 + 위 셋 + `applyTaxRule()` **한 번** → `{ ruleKind, rate, historizedId, method, basisDate, vatKrw, withholdingKrw, companyBorneKrw, payableKrw }` 또는 세율이 없으면 `{ unavailable: true }`. `domain/settings/registry.ts` 끝에 덧붙이는 읽기 `getSettingEntry(def, { asOf })` → `{ value, historizedId, effectiveFrom }`(기존 `findEffectiveValue`가 행 전체를 돌려준다 — 이력형만, 행이 없으면 기본값 + null). 이 task의 단위 사례는 behavior 세 줄까지이고 규칙 전 칸 · 기준일 사슬 · 세율 바뀜은 05-06이 굳힌다.

④ **번호** `domain/document-numbering/index.ts`에 덧붙이기만: 순수 `expenseNumberFormat(projectNumber, seq, { separator, seqDigits, seqStart })`(기존 `documentNumberFormat`과 같은 표시 순번 규칙 — 오프셋 · 자르지 않음) · `loadExpenseNumberFormat(deps?)`(아래 세 키를 `getSimpleSettingValues` **한 번**으로 — 트랜잭션 전에 부른다) · `allocateExpenseNumber(viewer, { projectNumber, format }, tx)`(리포지토리 `allocateNumber(viewer, "expense", projectNumber, tx)` → `{ number, seq }`). 기존 함수 · `DOCUMENT_NUMBER_FORMAT_DEFS`의 기존 줄은 바꾸지 않는다. `docs/ARCHITECTURE.md` §4-6에 한 줄: 「지출결의 카운터 `expense`는 period = 프로젝트 번호다 — 연도 규약의 첫 예외(사용자 결정 2026-09-26 #6). 팀 비용은 `expense_team` · period = 연도(05-07)」.

⑤ **설정 키** `domain/settings/keys.ts` 끝에(04.1 결재선 17키와 같은 선언 형식 — 04.1이 도우미를 남겼으면 그것을 쓴다): `approval_route.expense.self_approval`(기본 `self_approve` — 입력 §3) + `approval_route.expense.step{1..4}.{enabled,role_id,scope,org_unit_id}`(기본값은 연차와 같은 4단, 3단 `org_unit_id`만 기본 없음 — 시드가 채운다, namespace `지출결의 결재선`, 라벨 · 힌트 · optionLabels는 04.1 S8과 같다 — UI-SPEC S13) · `document_number.expense.separator`(기본 `-`) · `.seq_digits`(4) · `.seq_start`(1)(namespace `문서 번호`, 라벨은 `document_number.project.*`의 같은 칸 라벨). `SETTING_DEFS`에 올린다. `PROJECT_CUSTOMER_APPROVAL_GATE`의 `readBy` 한 줄을 지운다(이 플랜이 읽기 시작한다 — Pitfall 3).

⑥ **도메인 서비스(단위 C)** `domain/expenses/dto.ts`: 작성 중 DTO · 문서 DTO 명세를 `registerDto` — 문서 필드는 `expense.value`, 금액 필드(공급가액 네 칸 · 세액 넷 · 지급 총액)는 `expense.amount`. `domain/permissions/info-items.ts` 끝에 `expense.value`(지출결의 정보) · `expense.amount`(지출결의 금액) 둘 다 staffDefault 참. `domain/expenses/access.ts` `canSeeExpense(viewer, row, deps?)` — 작성 중이면 기안자만, 번호 있으면 기안자 ∪ 결재 관련자(`getApprovalView`가 null이 아님). 목록 범위(팀장 · 전사)는 05-07이 더한다. `domain/expenses/gate.ts` `evaluateExpenseSubmit(facts)` → `{ allowed: true } | { allowed: false; reason; target }` — 이 task는 ③ `견적 줄이 현재 차수에 없음 · 견적 줄 바꾸기`(target `quoteLine`) · ④ `이 줄에 지출결의 {번호} 있음 · 지출결의 열기`(target `openLatest`) · ⑥ 빈 칸 묶음(`{칸 이름들} {n}칸 비어 있음 · {첫 칸} 적기/고르기`, 한 칸이면 `{칸} 비어 있음 · …`) · ⑦ `공급가액이 0 · 0보다 크게` 넷을 이 순서로 판정한다(나머지 ①②⑤⑧⑨와 규칙 등록은 05-04 · 05-06이 같은 함수에 끼운다 — 순서는 UI-SPEC S6 원문). 회차 상한 초과는 게이트가 아니라 칸 오류 `ExpenseFieldError { field: "supplyAmount", message: "남은 실행가 {금액} 넘음 · 공급가액 고치기" }`.
   `domain/expenses/index.ts`: 적재 때 `import "./dto"`와 `registerDocumentKind({ kind: 지출결의 키, label: "지출결의", loadRouteConfig: loadExpenseRouteConfig(17키를 getSimpleSettingValues 한 번), href: id → /expenses/{id}, describeDocuments: 요약(문서 칸 `지출결의 · {프로젝트명} · {항목}`, 팀 비용은 `지출결의 · {팀} · {내용}`) + measure money(공급가액), routeSettings: 17키 묶음, canResubmit: viewer → can(viewer, "expenses", "write"), resubmitFrom: [rejected, withdrawn] })`. 함수:
   - `createExpenseFromLines(viewer, { lineIds }, deps?)` → `{ created: {lineId, expenseId}[], blocked: {lineId, reason}[] }` — 트랜잭션 전에 `can(viewer, "expenses", "write")` · 프로젝트 쓰기 권리(Phase 4 판정 함수) · 현재 차수(D-54 — 최신 차수)의 줄인지 · 표 전체 게이트(완료 = `완료 프로젝트 · 새 지출결의 없음`, 진행 · 현재 차수 미승인 · 설정 켜짐 = `gate(project, "quote.customer-approval", ctx)`의 문자열 그대로 — 설정은 `getSettingValue(PROJECT_CUSTOMER_APPROVAL_GATE)`) · `expenseLineDoor`를 판정하고, 문이 열린 줄마다 자동 채움 값(거래처 · 거래처 기본 증빙 · 줄 실행가 통화 · 남은 실행가 · `recentFxRate` · `payment_method` 첫 활성 값 · 프로젝트 id)으로 `insertDraftIfAbsent` → null이면 `findDraftByLineAndDrafter`의 기존 id. 앞 회차가 있는 줄이면 `installment` 참으로 만든다(`forcedInstallment`).
   - `saveExpenseDraft(viewer, { expenseId, expectedVersion, fields }, deps?)` — 기안자 · 작성 중만(아니면 404), zod 검증된 칸만(`normalizeMoneyInput`로 금액 · 환율 정밀도 · 범위), 원화는 서버가 `moneyToColumns`로 계산, version 조건 저장(0행이면 `ExpenseConflictError` `HH:MM에 다른 곳에서 저장됨 · 새로 고침` — 시각은 행의 `updated_at` 서울 `HH:MM`).
   - `submitExpense(viewer, { expenseId, expectedVersion }, deps?)` — **트랜잭션 전**: `loadExpenseForDrafter`(기안자 아니면 404) · `computeExpenseTax` · `prepareSubmission(viewer, { kind: 지출결의 키, drafterId })` · `loadExpenseNumberFormat()` · `loadActionLogGate()`. **`withTransaction` 안(tx 호출만)**: 견적 줄 문서면 프로젝트 행 잠금(Phase 4 잠금 리포지토리 함수) → `lockExpenseForUpdate` + version 확인(불일치 → `ExpenseConflictError`) → `listNumberedByLine(tx)` · 줄 · 현재 차수를 tx로 읽어 `expenseLineDoor` + `remainingForInstallments` + `evaluateExpenseSubmit`로 다시 판정(막히면 `GateBlockedError(reason)` · 상한 초과면 `ExpenseFieldError`) → 분할 여부 · 회차(`nextInstallmentSeq`) 결정 → `saveSubmissionSnapshot` → `submitDocument(viewer, prepared, { documentId }, tx)`(결재선 고정 · `document_submit` 로그 같은 tx) → **마지막 쓰기** `allocateExpenseNumber` + `setExpenseNumber`. 번호가 이미 있는 문서의 다시 제출은 05-09가 이 함수에 `resubmitDocument` 갈래로 더한다.
   - `getExpense(viewer, { expenseId })` — `canSeeExpense`가 거짓이면 null, 맞으면 문서 DTO(상태 = 인스턴스 없음 → `작성 중`, 있으면 인스턴스 상태를 04.1 낱말 매핑으로).

⑦ **시드 · 등록 · 누수** 새 파일 `domain/seed/expenses.ts` `seedExpenses(viewer)` — `approval_route.expense.step3.org_unit_id` = `findOrgUnitByName("경영관리본부")`의 id(값이 없을 때만) · 계급 넷(`DEFAULT_ROLE_ID` · `TEAM_LEAD_ROLE_ID` · `DIVISION_HEAD_ROLE_ID` · `CEO_ROLE_ID`) × `expenses` view · write를 `insertPermissionIfAbsent` · 두 정보 항목 노출 행을 계급 다섯에 `insertVisibilityIfAbsent`(04.1 선례) · 코드표 `payment_method` 항목 셋(`bank_transfer` 계좌이체 sortOrder 0 · `corp_card` 법인카드 1 · `cash` 현금 2 — `seedCodeItem`, 있으면 두지 않음). `domain/seed/index.ts` return 직전 한 줄 호출. `app/(app)/admin/code-tables/page.tsx`의 `TABLE_OPTIONS`에 `payment_method`(지급 방식) 한 줄. `app/(app)/document-kinds.ts`에 `import "@/domain/expenses";` 한 줄. `test/integration/leak-scan.test.ts` 파일 끝에 같은 import 한 줄.

⑧ **테스트** 새 파일 `test/integration/fixtures/expenses.ts`: `setupExpenseProject()`(behavior 픽스처 — 사람 · 발령 · 프로젝트 · 차수 승인 · 줄 셋(거래처 있음 · 거래처 없음 · 분할용 실행가 10,000,000)은 도메인 · 리포지토리 함수로, SQL 직접 삽입 금지) · `submitReadyDraft(viewer, expenseId)`(지금은 version을 읽어 `submitExpense`만 — 05-04가 증빙 첨부를 더한다). `test/integration/expense-approval-lifecycle.test.ts` 첫 describe `트레이서 — 견적 줄에서 최종 승인까지`가 behavior 통합 다섯 줄을 순서대로 단언한다. 단위 사례는 `test/unit/domain/expenses/expense-number-format.test.ts` · `test/unit/domain/expenses/tax.test.ts`(새 파일) · `test/unit/domain/money.test.ts`(덧붙임).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/expenses test/unit/domain/money.test.ts test/unit/settings test/unit/import-cycles.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 번호 서식 · incomeType · 기준일 대체 · sumKrw/diffKrw 사례가 틀리거나, registry-coverage가 새 키를 「참조 없음」으로 잡거나 `readBy`가 남은 키를 잡거나, `domain/quotes` ↔ `domain/expenses` 순환이 생기면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-lifecycle.test.ts test/integration/leak-scan.test.ts test/integration/seed-permissions.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 작성 중 자동 채움 값 · 번호 `26001-0001` · 세금 스냅숏(부가세 1,240,000 · 지급 총액 13,640,000) · 단계 행 넷 · 최종 `approved` 중 하나라도 다르거나, 무관한 PM이 작성 중 문서를 보거나, 누수 스캔이 `expense.value`/`expense.amount` 미등록 필드로 실패하면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(repository-viewer-param · money-boundary · require-action-client 포함) 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - (단위 A · B · C) Task 1 커밋이 스키마 → 순수 함수 → 서비스 순서의 세 커밋 이상이고 단위 A 커밋의 diff는 `db/schema/*` · `db/migrations/*`뿐이다(`git show --name-only <A 커밋>`) — SUMMARY에 단위별 해시와 확인 결과
    - `grep -cE "text\(\"status\"" db/schema/expenses.ts`가 0이다(상태 복사 열 없음)
    - `grep -c "expenses_line_drafter_draft_uniq" db/schema/expenses.ts`가 1이고 `grep -c "check(" db/schema/expenses.ts`가 6 이상이다
    - `grep -c "applyTaxRule(" domain/expenses/tax.ts`가 1이다(호출 한 곳)
    - `grep -c "readBy" domain/settings/keys.ts`가 이 플랜 전보다 정확히 1 줄었다(기준점 파일을 읽고 `git show "$PLAN_BASE":domain/settings/keys.ts | grep -c readBy`와 비교)
    - (트랜잭션 규약) 실행자가 `submitExpense`의 `withTransaction` 콜백 안 호출 목록을 SUMMARY 표로 적고, 그 표에 `getSettingValue` · `applyTaxRule` · `prepareSubmission` · `can(` · `teamAtDate`가 없다
    - `domain/document-numbering/index.ts` diff가 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-03.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/document-numbering/index.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>견적 줄 하나에서 만든 지출결의가 자동 채움 → 저장 → 제출(프로젝트별 번호 · 세금 스냅숏 · 결재선 고정) → 최종 승인까지 도메인 한 줄로 간다</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 스키마 확정 — `expenses` 마이그레이션 적용 · SQL 린트 · journal 가드</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `db/migrations/meta/_journal.json`(전문) · Task 1이 만든 SQL
    - `.planning/phases/04.1-approvals-leave/04.1-01-PLAN.md` Task 2(①~④) · 05-01 Task 2(같은 규칙)
  </read_first>
  <action>
① `pnpm db:generate` 재실행이 새 파일을 만들지 않음을 확인한다. ② 빈 DB 적용 — `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`, 개발 DB는 `pnpm db:migrate`. 새 표가 기존 표(`users` · `projects` · `quote_lines` · `teams` · `vendors` · settings 이력 표)를 참조하는 FK 문장이 squawk `require-lock-timeout` · `require-statement-timeout`에 걸리면 그 문장들 앞에 SET LOCAL 블록 하나만 손으로 넣는다(05-01 Task 2와 같은 규칙). 부분 UNIQUE 인덱스는 새 빈 표라 `CONCURRENTLY`가 필요 없다 — squawk가 요구하면 규칙 설정(`.squawk.toml`)을 고치지 말고 SUMMARY에 판정과 함께 적고 멈춰 보고한다. ③ SUMMARY frontmatter `migration_manifest:`(tag · SQL · 스냅숏 · 손 삽입 줄 번호).
  </action>
  <verify>
    <automated>G=$(pnpm db:generate 2&gt;&amp;1); RC=$?; printf '%s\n' "$G"; test "$RC" -eq 0 &amp;&amp; grep -q "No schema changes" &lt;&lt;&lt;"$G"</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 "No schema changes"가 없다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db'</automated>
    <fails_when>빈 DB 적용이 실패하거나 squawk 위반이 하나라도 있거나 journal 가드가 실패하거나 "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 플랜의 SQL이 정확히 하나이고 이름이 drizzle-kit이 준 그대로다
    - journal diff가 끝에 항목 하나 추가뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-03.env" && test -n "$PLAN_BASE" && git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json`)
    - SQL 손 편집이 SET LOCAL 블록뿐이고 SUMMARY에 차이를 인용했다 · `migration_manifest` 세 경로가 실제 파일과 같다
    - 두 검증 명령이 초록이다
  </acceptance_criteria>
  <done>`expenses` 표가 빈 DB부터 적용되고 재생성 차이 0 · squawk 0 · journal 가드 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EXP-01 · EXP-14 가장자리를 DB와 잠금으로 굳힌다 — 두 번 눌러도 하나 · 여러 줄 · 동시 제출 번호 · 같은 줄 경합 · 회차 상한(외화 포함) · 금액 0/빈 칸 · 표 전체 게이트 · 두 창 저장</name>
  <files>domain/expenses/index.ts, domain/expenses/line-door.ts, domain/expenses/gate.ts, domain/money/index.ts, test/integration/expense-create-idempotency.test.ts, test/integration/expense-submit-concurrency.test.ts, test/integration/expense-installment-cap.test.ts, test/unit/domain/expenses/installment-cap.test.ts</files>
  <read_first>
    - Task 1 산출물 `domain/expenses/index.ts` · `line-door.ts` · `gate.ts` · `test/integration/fixtures/expenses.ts`
    - `test/integration/lock-race.ts`(sleep 없는 경합 재현 — ARCHITECTURE §4-8 (5)) · `test/integration/projects-create-concurrency.test.ts` · `test/integration/document-counters-concurrency.test.ts`
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S6 막힘 ①~⑨ 원문(264행) · Copywriting 「Error — 입력」(259행) · Assumptions #1 · #2 · #3(803–805행)
  </read_first>
  <behavior>
    - (통합) 같은 PM이 같은 줄로 `createExpenseFromLines`를 연달아 두 번 → 같은 expenseId · 작성 중 행 1개. `Promise.all`로 동시에 두 번 → 작성 중 행 1개 · 두 응답의 expenseId가 같다. 다른 PM(같은 팀, 프로젝트 쓰기 권리 있음)이 같은 줄로 만들면 그 사람 몫 작성 중 문서가 따로 하나 생긴다
    - (통합) 줄 셋(거래처 있음 · 거래처 없음 · 취소 줄)을 한 번에 → `created` 1 · `blocked` 2(`거래처 없음 · 거래처 고르기` · 취소 줄은 행동 없음 사유). 현재 차수가 아닌 이전 차수 줄 id → blocked `견적 줄이 현재 차수에 없음 · 견적 줄 바꾸기`
    - (통합) 진행 프로젝트 · 2차 미승인 · 설정 켜짐 → `blocked` 전부가 `2차 고객 승인 전 · 고객 승인 표시`(담당 PM) 문자열이고 작성 중 행 0. 설정을 끄면 생성된다. 수주중(bidding) 프로젝트는 고객 승인과 무관하게 생성된다(D-43). 완료 프로젝트 → `완료 프로젝트 · 새 지출결의 없음`
    - (통합, 기준 1) 같은 프로젝트의 서로 다른 줄 여섯의 작성 중 문서를 기안자 여섯이 `Promise.all`로 동시 제출 → 기본 풀(`DB_POOL_MAX` 5)에서 테스트 제한 시간 안에 전부 끝나고 번호 여섯 개가 `26001-0001`~`26001-0006`으로 서로 다르다(순서는 단언하지 않음). 다른 프로젝트 `26002`의 첫 제출은 `26002-0001`이다(카운터 period가 프로젝트마다 따로)
    - (통합, 같은 줄 경합) 같은 비분할 줄의 작성 중 문서 둘(기안자 둘)을 `deps.afterLock` + `lock-race.ts`로 순서를 고정해 제출 → 먼저 잠근 쪽 성공, 늦은 쪽 `GateBlockedError`이고 이유가 `이 줄에 지출결의 26001-0001 있음 · 지출결의 열기` · 늦은 쪽 문서는 번호 null 그대로 · 카운터 값 1
    - (단위, Q4) `remainingForInstallments`: KRW 줄 실행가 10,000,000 · 앞 문서 6,000,000 → 남음 4,000,000, 이번 4,000,001 → `exceeds` 참. USD 줄 실행가 USD 10,000.00 · 앞 문서 USD 6,000.00(환율 1,300) · 이번 USD 4,000.00(환율 1,400 — 원화 합은 실행가 원화를 넘음) → 원래 통화 비교라 `exceeds` 거짓 · basis `foreign`. 앞 문서 하나가 KRW면 basis `krw`로 원화 비교
    - (통합, 회차) 분할용 줄(실행가 10,000,000)에서 `분할 지급` 켠 첫 문서 6,000,000 제출 → 회차 1. 같은 줄 새 작성 중 문서는 분할 문서로 만들어지고 5,000,000으로 제출 → `ExpenseFieldError` `남은 실행가 4,000,000 넘음 · 공급가액 고치기` · 번호 null. 4,000,000으로 고쳐 제출 → 회차 2 · 이후 그 줄의 문 = `closed`(남은 0). 분할 없이 제출된 줄의 새 문서 제출 → ④ 거부
    - (통합, EXP-14) 공급가액 원화가 null인 작성 중 문서 제출 → `공급가액 비어 있음 · 공급가액 적기` · 0 → `공급가액이 0 · 0보다 크게` · 음수 입력은 `saveExpenseDraft`가 검증 오류로 거부. 리포지토리로 번호 있는 행의 공급가액을 0으로 바꾸는 UPDATE는 DB CHECK 위반(23514)으로 실패한다
    - (통합, 두 창) 같은 작성 중 문서를 version 1로 두 번 저장 → 두 번째가 `ExpenseConflictError`이고 문구가 `HH:MM에 다른 곳에서 저장됨 · 새로 고침` 꼴이다
  </behavior>
  <action>
① behavior 단위 사례를 `test/unit/domain/expenses/installment-cap.test.ts`에 먼저 적고 실패를 확인한 뒤 `remainingForInstallments` · `expenseLineDoor`를 고쳐 초록으로 만든다(`domain/money` 밖에서 금액을 더하거나 빼지 않는다 — money-boundary 린트).
② 통합 세 파일을 새로 만든다: `test/integration/expense-create-idempotency.test.ts`(생성 사례 넷 — 두 번 · 동시 · 여러 줄 · 표 전체 게이트) · `test/integration/expense-submit-concurrency.test.ts`(동시 제출 6건 · 다른 프로젝트 카운터 · 같은 줄 경합 · EXP-14 · 두 창) · `test/integration/expense-installment-cap.test.ts`(회차 · 상한 · 닫힘 · 외화 한 사례를 통합으로도). 픽스처는 Task 1의 `setupExpenseProject` · `submitReadyDraft`만 쓴다. 경합은 sleep 없이 `deps.afterLock`으로만 재현한다.
③ 사례가 드러낸 구현 틈만 고친다 — 예: 동시 생성에서 ON CONFLICT 뒤 재조회가 빠진 경우, 제출 재판정이 트랜잭션 전 값을 쓴 경우, 카운터 period가 연도로 새는 경우. 고친 곳과 이유를 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/expenses/installment-cap.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 외화 줄이 원화로 비교되거나 부분 KRW 문서가 원 통화로 비교되면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-create-idempotency.test.ts test/integration/expense-submit-concurrency.test.ts test/integration/expense-installment-cap.test.ts test/integration/expense-approval-lifecycle.test.ts test/integration/document-counters-concurrency.test.ts test/integration/projects-create-concurrency.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 제한 시간 초과가 나온다 — 작성 중 행이 2개가 되거나, 동시 제출 6건이 멈추거나 번호가 겹치거나, 같은 줄 두 제출이 둘 다 번호를 받거나, 상한 초과가 통과하거나, 금액 0 제출이 통과하거나, 기존 번호 · 프로젝트 동시성 테스트가 깨지면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "afterLock" test/integration/expense-submit-concurrency.test.ts`가 1 이상이고 `grep -cE "setTimeout|sleep\(" test/integration/expense-submit-concurrency.test.ts`가 0이다
    - 세 통합 파일이 `setupExpenseProject` 또는 `submitReadyDraft`를 import한다(`grep -l "fixtures/expenses" test/integration/expense-*.test.ts`가 넷)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>두 번 눌러도 문서 하나 · 동시 제출 번호 유일 · 같은 줄 경합 한 쪽만 · 회차 상한 · 금액 > 0이 DB 제약과 프로젝트 행 잠금으로 증명된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 액션(05-05) → `createExpenseFromLines` · `saveExpenseDraft` · `submitExpense` | 호출자가 보낸 줄 id · 문서 id · 금액 · version은 신뢰하지 않는다 |
| 브라우저 계산 → 저장 값 | 세액 · 지급 총액 · 원화는 서버가 다시 계산한다 |
| 동시 요청 → 1줄 1문서 · 번호 | 애플리케이션 판정만으로는 경합을 못 막는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-301 | Information Disclosure | 작성 중 · 남의 지출결의 조회(IDOR) | high | mitigate | `getExpense` · `saveExpenseDraft` · `submitExpense` 첫 줄이 `canSeeExpense`/기안자 판정 → null(404). 작성 중은 기안자만. Task 1 통합 사례(무관한 PM null). 목록 범위 확장은 05-07 `expense-visibility` |
| T-05-302 | Elevation of Privilege | 남의 프로젝트 줄로 지출결의 생성 | high | mitigate | `can(viewer, "expenses", "write")` + Phase 4 프로젝트 쓰기 권리 + 현재 차수 줄 판정을 트랜잭션 전에 — 권리 없는 사람의 생성 거부 사례(Task 3 통합에 한 사례 추가) |
| T-05-303 | Tampering | 세액 · 지급 총액 · 원화 변조 | high | mitigate | 액션 입력에 세액 칸이 없고 서버가 `computeExpenseTax` · `moneyToColumns`로만 채운다(금지 항목). 스냅숏 값 = 서버 계산 단언(Task 1) |
| T-05-304 | Tampering | 같은 줄 이중 문서 · 회차 초과(경합) | high | mitigate | 작성 중 = 부분 UNIQUE, 제출 = 프로젝트 행 잠금 안 재판정 + 카운터 마지막 쓰기. `afterLock` 경합 사례 · 상한 사례(Task 3) |
| T-05-305 | Tampering | 음수 · 0 금액 | medium | mitigate | DB CHECK 둘 + 서버 거부(⑥ · ⑦) + zod · `normalizeMoneyInput`. 리포지토리 UPDATE CHECK 위반 사례 |
| T-05-306 | Denial of Service | 제출 트랜잭션의 풀 점유 | medium | mitigate | 트랜잭션 전 읽기 · 안에서는 tx 호출만 · 번호가 마지막 쓰기. 풀 5에서 동시 제출 6건 사례 |
| T-05-307 | Repudiation | 제출 기록 | medium | mitigate | 04.1 `submitDocument`가 같은 tx에서 `document_submit` 로그를 쓴다(`recordActionInTx`) — 이 플랜은 그 경로를 그대로 탄다 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json` · `pnpm-lock.yaml` diff 0(verification) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 전체 통과(registry-coverage · import-cycles · journal 가드 포함)
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration'` 전체 통과(기존 스위트 회귀 없음 — 특히 `quote-*` · `project-status` · `approvals-*`)
- `pnpm db:generate` 재실행 "No schema changes"
- `. "$(git rev-parse --git-dir)/gsd-05-03.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml domain/quotes domain/projects` 비어 있음(견적 · 프로젝트 도메인은 05-05 · 05-11이 고친다)
</verification>

<success_criteria>
- 견적 줄 하나에서 만든 지출결의가 도메인 한 줄로 제출 · 최종 승인까지 간다(트레이서)
- 1줄 1문서 · 회차 상한 · 번호 유일 · 금액 > 0이 DB 제약 · 잠금으로 증명된다
- 세금은 `applyTaxRule()` 한 번 + 스냅숏이고, 브라우저 값은 저장되지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `expenses`(부분 UNIQUE `expenses_line_drafter_draft_uniq` · CHECK 7 · 상태 열 없음) |
| 마이그레이션 | drizzle-kit 이름 SQL 1개 + 스냅숏(05-12 재생성 · `migration_manifest`) |
| 함수 | `createExpenseFromLines` · `saveExpenseDraft` · `submitExpense` · `getExpense` · `canSeeExpense` · `evaluateExpenseSubmit`(③④⑥⑦) · `expenseLineDoor` · `pickTaxDates` · `incomeTypeFor` · `resolveAppliedRate` · `computeExpenseTax` · `getSettingEntry` · `expenseNumberFormat` · `loadExpenseNumberFormat` · `allocateExpenseNumber` · `sumKrw` · `diffKrw` · `remainingForInstallments` · `seedExpenses` |
| 오류 클래스 | `ExpenseConflictError` · `ExpenseFieldError`(둘 다 `UserFacingError` 하위) |
| 문서 종류 | 지출결의(`registerDocumentKind` — resubmitFrom 둘 · canResubmit · describeDocuments + measure) |
| 설정 키 | `approval_route.expense.*` 17 · `document_number.expense.*` 3 · `project.customer_approval_gate` readBy 제거 |
| 정보 항목 · 권한 · 코드표 | `expense.value` · `expense.amount` · 계급 넷 `expenses` 보기 · 쓰기 · 코드표 `payment_method` 셋 |
| 문서 | `docs/ARCHITECTURE.md` §4-6 한 줄(period = 프로젝트 번호 예외) |
| 테스트 | `test/integration/{expense-approval-lifecycle,expense-create-idempotency,expense-submit-concurrency,expense-installment-cap}.test.ts` · `test/integration/fixtures/expenses.ts` · `test/unit/domain/expenses/{expense-number-format,installment-cap,tax}.test.ts` · `test/unit/domain/money.test.ts`(추가) |

<output>
Create `.planning/phases/05-expense-approval-leave/05-03-SUMMARY.md` when done
</output>
