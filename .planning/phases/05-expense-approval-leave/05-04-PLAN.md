---
phase: 05-expense-approval-leave
plan: 04
type: execute
wave: 4
depends_on: ["05-14"]
files_modified:
  - db/schema/files.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - repositories/files.ts
  - repositories/upload-intents.ts
  - domain/evidence/index.ts
  - domain/evidence/upload-checks.ts
  - domain/evidence/dto.ts
  - domain/expenses/index.ts
  - domain/expenses/gate.ts
  - domain/settings/keys.ts
  - domain/settings/registry.ts
  - app/(app)/admin/settings/settings-form-client.tsx
  - lib/gcp/storage.ts
  - lib/env.ts
  - app/api/storage-local/[...key]/route.ts
  - .gitignore
  - .env.example
  - test/integration/fakes/memory-storage.ts
  - test/integration/fixtures/expenses.ts
  - test/integration/evidence-upload.test.ts
  - test/integration/leak-scan.test.ts
  - test/integration/tx-safety.test.ts
  - test/unit/domain/evidence/upload-checks.test.ts
  - test/unit/lib/gcp/storage-local.test.ts
  - test/unit/app/storage-local-route.test.ts
  - test/unit/env.test.ts
autonomous: true
requirements: [EVID-01, EXP-02]

estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "작성 중 지출결의의 기안자가 파일(이미지 · PDF)의 크기 · 형식 · SHA-256을 선언하면 서버가 크기 한도(`evidence.max_size_mb`, 기본 10) · 형식 허용 목록 · 같은 종류 주인 안 중복을 검사한 뒤 업로드 의도(`upload_intents`, 만료 15분)와 서명된 PUT 주소를 돌려주고, 바이트가 올라간 뒤 완료 통보를 받으면 저장소 메타데이터(크기 · 형식 · 서명에 묶인 sha256)를 다시 확인해 `files` 행을 만든다 — 파일 바이트는 서버 액션을 지나지 않는다(EVID-01 · ROADMAP Phase 5 비고 · RESEARCH Pattern 5)"
    - "제출은 증빙 파일이 1개 이상일 때만 된다 — 없으면 `증빙 없음 · 증빙 올리기 Ctrl+U`(게이트 ⑧, 대상 칸 = 첨부 영역)로 거부되고, 판정은 제출 트랜잭션 안에서 tx로 센 파일 수다(기준 3 「증빙 전에는 제출 버튼이 비활성」 · UI Assumptions #5)"
    - "크기 초과는 `{크기}MB · {한도}MB 초과 · 파일을 줄여 다시`, 형식 밖은 `이미지·PDF가 아님 · 사진이나 PDF로 다시`, 같은 문서 중복은 `같은 파일이 이미 첨부됨`, 다른 지출결의 중복은 올린 사람이 그 문서를 볼 수 있을 때만 `같은 파일이 {번호} 증빙에 있음 · 다른 파일 고르기`이고 못 보면 `이미 첨부된 파일 · 다른 파일 고르기`다 — 번호로 남의 문서 존재를 알리지 않는다(UI-SPEC Copywriting 「Error — 증빙 업로드」 · Security 「중복 오류로 다른 문서 존재 확인」)"
    - "남의 업로드 의도 · 만료된 의도 · 이미 완료한 의도로 완료 통보를 하면 `올리지 못함 · 다시 올리기`로 거부되고 `files` 행이 생기지 않는다. 메타데이터가 선언과 다르면(크기 · 형식 · sha256) 저장소 객체를 지우고 같은 문구로 거부한다"
    - "객체 키는 서버가 만든다 — 업로드(서명 PUT)는 `incoming/{의도 id}`에 받고, 완료 통보가 메타데이터를 다시 확인한 뒤 `evidence/{파일 id}`로 옮긴 다음에만 `files` 행(`object_key` = `evidence/…`)을 만든다. 버킷 수명 주기 7일 삭제는 `incoming/`에만 걸리므로(05-12) 완료된 증빙은 삭제 규칙 밖에 있고, 보존 표식(`retain`)은 그 위의 이중 방어다. 클라이언트가 보낸 이름 · 경로는 표시용 `original_name`으로만 저장된다(사용자 결정 E2: A · M1)"
    - "증빙 추가(완료 통보) · 삭제 트랜잭션은 첫 단계에서 제출과 같은 지출결의 행을 잠그고(`lockExpenseForUpdate`) 그 잠금 아래에서 주인 상태 · 살아 있는 파일을 tx로 다시 읽는다 — 제출 ∥ 삭제는 어느 순서로 겹쳐도 번호 있는 문서의 살아 있는 증빙이 1개 이상이고, 같은 파일을 두 탭에서 지우면 삭제 · 로그가 한 번만 남는다(`deps.afterLock` 두 순서 사례 — B2)"
    - "증빙 원본 보기 주소(서명 GET, 5분)는 그 지출결의를 볼 수 있는 사람에게만 만들어지고 DB에 저장되지 않는다 — 볼 수 없으면 null(404)"
    - "로컬 저장소 드라이버는 `APP_ENV=local`에서만 켜진다 — `lib/env.ts`가 `APP_ENV`가 local이 아닌데 `STORAGE_DRIVER=local`이면 부팅을 거부하고, `app/api/storage-local/[...key]/route.ts`는 드라이버가 local이 아니면 404다. 로컬 주소는 `BETTER_AUTH_SECRET`에서 파생한 HMAC · 만료 · 크기 한도 · 형식을 싣고 같은 규칙으로 거부한다 — 새 비밀 없음(Pitfall 8)"
    - "`GCS_EVIDENCE_BUCKET`은 부팅 검증으로 강제하지 않는다 — gcs 드라이버를 처음 만들 때 없으면 설정 오류를 던진다. 그래서 마이그레이션 · 계정 Job은 이 변수 없이 계속 뜬다"
    - "관리자 설정 화면 `증빙` 섹션에 `증빙 크기 한도`(숫자, 기본 10, 값 옆 정적 글자 `MB`)가 보이고, 숫자가 아니면 칸 아래 `숫자가 아닙니다 · 10처럼 적어 주세요`다(UI-SPEC S13 · UI Considerations error S13)"
    - "증빙 파일 DTO가 `registerDto`로 등록되고 필드가 `expense.value`에 매핑되어 누수 스캔에 들어간다"
    - "증빙 추가 · 삭제는 행동 로그 `document_update`(detail `{ change: \"evidence_add\" | \"evidence_remove\" | \"evidence_void\", fileId }` — `evidence_void`는 05-09가 쓰고 사유 원문 대신 `reasonLength`만 싣는다)로 파일 행 쓰기와 같은 트랜잭션에 남는다 — 새 행동 종류를 만들지 않는다(`evidence_add` · `evidence_remove`는 행동 종류가 아니라 detail의 변경 종류 값이다 — 리뷰 Round 1 F2)"
    - "운영 로그(행동 로그와 별개): 완료 통보의 메타데이터 불일치는 `log.warn(\"evidence.upload_metadata_mismatch\", { intentId, ownerKind, ownerId, reason })`(reason ∈ `size` · `content_type` · `sha256`), 로컬 저장소 라우트가 local이 아닌 드라이버에서 막힌 요청은 `log.warn(\"storage.local_route_blocked\", { method, reason: \"driver_not_local\" })` 한 줄씩 남긴다 — id와 사유 코드만, 파일명 · 사람 이름 · 금액 없음(`lib/log.ts` `log.warn` · 선례 `domain/rules/deny-write.ts`의 `write.denied` — 리뷰 Round 1 F7)"
  prohibitions:
    - statement: "로컬 저장소 드라이버 라우트가 staging · prod에서 응답해서는 안 된다 — 인증 없는 쓰기 창구가 된다"
      status: resolved
      verification: judgment
    - statement: "서명 GET 주소를 DB에 저장하거나 문서를 볼 수 없는 사람에게 만들어 주어서는 안 된다"
      status: resolved
      verification: judgment
    - statement: "파일 바이트를 서버 액션 요청 본문으로 받아서는 안 된다 — 브라우저 → 저장소 직접 PUT만"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/schema/files.ts"
      provides: "files(owner_kind · owner_id · object_key UNIQUE · sha256 · size_bytes · content_type · original_name · removed_at) + upload_intents(선언 값 · 만료 · 완료)"
      contains: "upload_intents"
    - path: "domain/evidence/index.ts"
      provides: "requestEvidenceUpload · completeEvidenceUpload · removeEvidence · createEvidenceViewUrl · listEvidence"
      exports: ["requestEvidenceUpload", "completeEvidenceUpload", "removeEvidence", "createEvidenceViewUrl", "listEvidence"]
    - path: "domain/evidence/upload-checks.ts"
      provides: "checkEvidenceUpload — 크기 · 형식 · 중복 순수 판정(06-11이 선결제 규칙을 여기에 더한다)"
      exports: ["checkEvidenceUpload"]
    - path: "repositories/files.ts"
      provides: "findActiveBySha(viewer, sha256, ownerKinds) — 06-11이 기다리는 모양 · countActiveByOwner"
      contains: "findActiveBySha"
    - path: "lib/gcp/storage.ts"
      provides: "ObjectStorage 인터페이스(createSignedPut · createSignedGet · getMetadata · move · delete · retain) + local 드라이버 + getObjectStorage(환경별 선택) — gcs 드라이버는 05-12"
      contains: "ObjectStorage"
    - path: "app/api/storage-local/[...key]/route.ts"
      provides: "로컬 드라이버 PUT · GET(STORAGE_DRIVER=local일 때만)"
    - path: "test/integration/evidence-upload.test.ts"
      provides: "요청 → PUT → 완료 → 제출 트레이서와 거부 사례"
      min_lines: 80
  key_links:
    - from: "domain/evidence/index.ts"
      to: "lib/gcp/storage.ts"
      via: "deps.storage ?? getObjectStorage() — 통합 테스트는 메모리 가짜 주입"
      pattern: "getObjectStorage"
    - from: "domain/expenses/gate.ts"
      to: "repositories/files.ts"
      via: "제출 트랜잭션 안 countActiveByOwner(tx) → 게이트 ⑧"
      pattern: "countActiveByOwner"
    - from: "domain/evidence/index.ts"
      to: "repositories/expenses.ts"
      via: "completeEvidenceUpload · removeEvidence 트랜잭션 첫 단계 lockExpenseForUpdate(05-03) — 제출과 같은 행 잠금(B2)"
      pattern: "lockExpenseForUpdate"
    - from: "domain/evidence/upload-checks.ts"
      to: "domain/settings/keys.ts"
      via: "evidence.max_size_mb(06-02가 계획한 같은 이름 — Phase 5가 등록)"
      pattern: "evidence.max_size_mb|EVIDENCE_MAX_SIZE_MB"
    - from: "app/api/storage-local/[...key]/route.ts"
      to: "lib/env.ts"
      via: "드라이버가 local이 아니면 404"
      pattern: "STORAGE_DRIVER|storageDriver"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**증빙 업로드 경로의 도메인 트레이서**: 작성 중 지출결의에 파일 한 장을 「선언 → 서명된 PUT → 완료 통보 → 메타데이터 재확인 → 파일 행」으로 붙이고, 그 파일이 있어야 제출이 되는 데까지 관통시킨다. 크기 · 형식 · 중복 · 의도 위조 · 메타데이터 불일치 거부와 로컬 저장소 드라이버(개발 · CI용)와 그 운영 차단을 함께 세운다.

Purpose: ROADMAP Phase 5 비고 「증빙 첨부의 업로드 경로(브라우저 축소 + SHA-256 + GCS 서명 URL)」를 이 페이즈가 만들고(05-CONTEXT Claude's Discretion), Phase 6은 한도 · 중복 · 선결제 규칙만 얹는다(06-11 · 06-02가 이 플랜의 이름을 기다린다). 화면(05-05)이 이 서비스 위에 선다.

Output: 표 2 · 마이그레이션 1 · `domain/evidence` · `lib/gcp/storage.ts`(인터페이스 + local 드라이버 + 팩토리) · 로컬 라우트 · env 두 변수 · 설정 키 1(+ `unitLabel` 표시 필드) · 게이트 ⑧ · 통합 1(+ `tx-safety` 덧붙임) · 단위 4. 실제 GCS 드라이버(V4 서명)와 버킷 인프라는 05-12, staging 실측은 05-13.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-RESEARCH.md
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-03-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development`(구현 전) · `systematic-debugging`(실패 추적 전) · `verification-before-completion`(커밋 · 「완료」 전)을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 프롬프트에 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-04.env"` · diff 검사는 `. "$(git rev-parse --git-dir)/gsd-05-04.env" && test -n "$PLAN_BASE" &&`로 시작.
- 트랜잭션 규약(저장소 메타데이터 조회 · 설정 · `canSeeExpense`는 트랜잭션 전) · 마이그레이션 규칙(`pnpm db:generate`만 · SET LOCAL 손 삽입만 · `migration_manifest`) · `.planning/STATE.md` 불변 · 새 의존성 0(해시 · HMAC · HKDF는 `node:crypto`) — 05-01 execution_notes와 같다. 통합 · E2E는 `flock /tmp/plant8-erp-test.lock`.
- 이 플랜의 업로드는 작성 중 · 반려 · 회수 문서에만 열린다. 제출 뒤(결재 중 · 승인) 증빙 추가 · 마지막이 아닌 파일 삭제와 그 때의 결재 인스턴스 version 올림(E4 배선)은 05-09가 같은 함수에 더한다.
- 잠금 순서(B2 · 05-09 execution_notes와 같은 표): 증빙 추가 · 삭제는 **지출결의 행**(`lockExpenseForUpdate`)만 — 프로젝트 행을 잡지 않는다. 제출은 프로젝트 행 → 지출결의 행 → 카운터라 같은 쌍을 거꾸로 잡는 경로가 없다(교착 없음). 05-09가 이 뒤에 결재 인스턴스(`bumpInstanceVersion`)를 붙인다. 저장소 호출(메타데이터 · 옮기기 · 보존 표식 · 삭제)은 트랜잭션 밖에서만 한다.
- 05-03 · 05-14의 테스트는 픽스처 도우미 `submitReadyDraft`로만 제출한다 — 이 플랜이 그 도우미에 증빙 첨부를 넣으므로 05-03 · 05-14 테스트는 고치지 않고 녹색이어야 한다. 깨지면 테스트가 아니라 게이트 순서(S6 ①~⑨)를 본다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 130k — 05-01 「웨이브 근거」의 규모 근거): Task 2 [BLOCKING]은 명령 셋 · 파일 하나로 얇다. Task 1 트레이서와 Task 3 거부 사례는 같은 `domain/evidence` · `upload_intents` · 픽스처를 이어 쓰므로 나누면 같은 파일을 두 플랜이 다시 읽는다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 작성 중 지출결의에 파일 한 장: 선언 → 서명 PUT → 완료 통보 → 메타데이터 재확인 → 파일 행 → 제출 통과 (스키마 · 리포지토리 · 도메인 · 저장소 인터페이스 · 게이트 ⑧)</name>
  <files>db/schema/files.ts, db/schema/index.ts, repositories/files.ts, repositories/upload-intents.ts, domain/evidence/index.ts, domain/evidence/upload-checks.ts, domain/evidence/dto.ts, domain/expenses/index.ts, domain/expenses/gate.ts, domain/settings/keys.ts, domain/settings/registry.ts, app/(app)/admin/settings/settings-form-client.tsx, lib/gcp/storage.ts, test/integration/fakes/memory-storage.ts, test/integration/fixtures/expenses.ts, test/integration/evidence-upload.test.ts, test/integration/leak-scan.test.ts, test/unit/domain/evidence/upload-checks.test.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 337–368행(Pattern 5 전부 — 흐름 그림 · 표 · 설정 키 · MIME · SHA-256 · 인터페이스 · 보기 · 고아) · 697–728행(Security Domain)
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S4(408–431행) · Copywriting 263행 · S13(600–608행)
    - `.planning/phases/06-payment-evidence-cards/06-11-PLAN.md` 35–45행(`findActiveBySha` · `ownerKinds` 가정) · `06-02-PLAN.md` 28–40행(`evidence.max_size_mb` 가정)
    - 05-03 산출물 `domain/expenses/index.ts` · `gate.ts` · `access.ts` · `test/integration/fixtures/expenses.ts`
    - `db/schema/expenses.ts`(FK · CHECK 모양) · `domain/settings/registry.ts`(`SettingDef` 필드 — 04.1이 더한 `optionLabels` 선례) · 04.1-04가 설정 화면에서 `optionLabels`를 그리는 칸 컴포넌트(`grep -rln "optionLabels" "app/(app)/admin/settings"` — 파일 이름이 다르면 그 파일을 쓰고 SUMMARY에 적는다)
    - `domain/action-log/record.ts`(`CORE_ACTION_TYPES` — `document_update` 재사용 — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4)) · 04.1 `domain/approvals/tx-log.ts`(`recordActionInTx`)
  </read_first>
  <behavior>
    - (단위) `checkEvidenceUpload`: 크기 0 → 거부 · 10MB 한도에서 12.4MB(13,002,342 바이트) → `12.4MB · 10MB 초과 · 파일을 줄여 다시` · `image/jpeg` · `image/png` · `image/webp` · `image/heic` · `image/heif` · `application/pdf` 통과, `text/html` → `이미지·PDF가 아님 · 사진이나 PDF로 다시` · sha256이 64자 소문자 16진이 아니면 거부 · 중복 입력 셋(같은 주인 → `같은 파일이 이미 첨부됨` / 다른 주인 · 볼 수 있음 · 번호 `26001-0004` → `같은 파일이 26001-0004 증빙에 있음 · 다른 파일 고르기` / 다른 주인 · 못 봄 → `이미 첨부된 파일 · 다른 파일 고르기`)
    - (통합, 트레이서) 05-03 픽스처의 작성 중 문서에서 기안자가 `requestEvidenceUpload({ ownerKind: 지출결의, ownerId, size: 212000, contentType: "image/jpeg", sha256, name: "세금계산서.jpg" }, { storage: 메모리 가짜 })` → `intentId` · 주소 · 헤더(Content-Type · 크기 범위 · sha256 메타)가 돌아오고 `upload_intents` 행의 `object_key`가 `incoming/{intentId}` · 만료 = 지금 + 15분
    - (통합, 트레이서) 가짜 저장소에 같은 헤더로 PUT → `completeEvidenceUpload({ intentId })` → `files` 행 1(`object_key` = `evidence/{그 파일 id}`) · 가짜 저장소에 `incoming/{intentId}` 객체가 없고 `evidence/{파일 id}` 객체가 같은 크기 · 형식 · sha256으로 있다(옮기기 기록 1) · 그 키의 `retain` 기록 1 · 의도 `completed_at` 있음 · 행동 로그 `document_update` 1건(같은 tx) · `listEvidence` 한 행(파일명 · 212KB) (Addresses review concern: M1(Round 2) · E2: A)
    - (통합, 트레이서) 증빙 없이 `submitExpense` → `증빙 없음 · 증빙 올리기 Ctrl+U` · 번호 null. 파일을 붙인 뒤 제출 → 번호 부여(05-03 트레이서와 같은 결과)
    - (통합) 기안자는 `createEvidenceViewUrl`로 5분짜리 주소를 받고 무관한 PM은 null
  </behavior>
  <action>
① **스키마** 새 파일 `db/schema/files.ts`: `files`(id uuid PK · `owner_kind` text not null CHECK ∈ {'expense'}(Phase 6이 값을 더한다) · `owner_id` uuid not null · `object_key` text not null UNIQUE · `sha256` text not null CHECK 64자 소문자 16진 · `size_bytes` int not null CHECK > 0 · `content_type` text not null · `original_name` text not null · `uploaded_by` text FK users not null · `created_at` · `removed_at` null · `removed_by` text null · `voided_at` null · `voided_by` text FK users null · `void_reason` text null(무효 처리 — 05-09가 쓴다, 이 마이그레이션에 함께 넣어 표를 두 번 고치지 않는다 · CHECK 셋이 모두 null이거나 모두 값 · `void_reason` 길이 1~500 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리), 인덱스 (owner_kind, owner_id) · (sha256)). `owner_id`에는 외래 키를 두지 않고(주인 종류가 여럿 — Phase 6이 종류를 더한다) `sha256`에는 UNIQUE를 두지 않는다 — **같은 파일을 동시에 올려 두 요청이 모두 중복 검사를 통과하면 둘 다 붙는 것을 허용한다**: 중복 검사는 사람을 돕는 안내이고, 같은 파일 두 장은 금액 · 제출 판정에 영향이 없으며 삭제로 되돌린다. UNIQUE는 삭제 · 무효 뒤 다시 올리기를 막는다(Addresses review concern: P3-11(Round 2)) · `upload_intents`(id uuid PK · `owner_kind` · `owner_id` · `object_key` UNIQUE · `declared_size` int · `declared_content_type` · `declared_sha256` · `original_name` · `created_by` text FK users · `expires_at` · `completed_at` null · `created_at`, 인덱스 (created_by)). `db/schema/index.ts` 끝 export 한 줄. `pnpm db:generate`.

② **리포지토리** `repositories/files.ts`: `insertFile(viewer, values, tx)` · `findActiveBySha(viewer, sha256, ownerKinds, tx?)`(「살아 있는 행」 = `removed_at IS NULL AND voided_at IS NULL` · 주인 id · 주인 종류 — 06-11이 기다리는 모양 그대로) · `countActiveByOwner(viewer, ownerKind, ownerId, tx?)`(같은 조건 — 무효 증빙은 게이트 ⑧ 개수 · 중복 검사에서 빠진다) · `listActiveByOwner`(삭제 안 된 행 전부 — 무효 행도 싣고 DTO에 `voidedAt` · `voidReason`을 둔다, 화면이 취소선 · 배지로 그린다) · `findFileById` · `markRemoved(viewer, {id, removedBy}, tx)`. `repositories/upload-intents.ts`: `insertIntent` · `findIntentById` · `completeIntentIfOpen(viewer, {id, createdBy, now}, tx)`(`completed_at IS NULL AND expires_at > now AND created_by = viewer` 조건 UPDATE — 0행이면 null, 한 번만).

③ **저장소 인터페이스** 새 파일 `lib/gcp/storage.ts`: 타입 `ObjectStorage`(`createSignedPut(key, { contentType, maxBytes, sha256, expiresSec })` → `{ url, method: "PUT", headers }` · `createSignedGet(key, { expiresSec, filename, disposition })` → `{ url }` · `getMetadata(key)` → `{ size, contentType, sha256 } | null` · `move(fromKey, toKey)` — 원본을 대상으로 복사(대상이 있으면 덮어씀 · 형식 · 사용자 메타 sha256 유지)한 뒤 원본을 지운다. 원본이 없으면 오류, 원본 삭제의 「없음」은 성공(두 호출이 겹쳐도 안전) — gcs는 05-12가 JSON API 재작성 + 삭제로, local은 데이터 디렉터리 안 이름 바꾸기로, 메모리 가짜는 항목 이동 + 기록으로 구현(Addresses review concern: M1(Round 2) · 사용자 결정 E2: A) · `delete(key)` · `retain(key)` — 완료된 `evidence/` 객체에 거는 보존 표식: 수명 주기 규칙은 `incoming/`에만 걸리므로(05-12) 이제 1차 보호는 접두어 분리이고 표식은 운영자 실수 · 앱 버그 삭제를 막는 이중 방어다. gcs는 05-12가 객체 보존 표식으로, local은 사이드카 메타데이터 `retained: true`로, 메모리 가짜는 기록으로 구현 — 리뷰 Round 1 F9)와 `getObjectStorage()`(환경 드라이버 선택 — 이 task는 local 분기 자리만, 드라이버 본체는 Task 3 · gcs는 05-12). 새 파일 `test/integration/fakes/memory-storage.ts`: 같은 인터페이스의 메모리 가짜(서명 주소 = 가짜 스킴 + 헤더 기록, 테스트가 `fake.put(url, { size, contentType, sha256 })`로 객체를 넣는다 — 헤더 불일치는 거부).

④ **설정 키** `domain/settings/keys.ts` 끝에 `EVIDENCE_MAX_SIZE_MB` = 키 `evidence.max_size_mb`(simple · number 정수 1~100 · 기본 10 · namespace `증빙` · 라벨 `증빙 크기 한도` · 힌트 `폰 사진은 줄여서 올림 · PDF는 원본 크기` — 06-02가 계획한 같은 이름, 06-02 목록은 넷 → 셋). `SettingDef`에 선택 표시 필드 `unitLabel?: string`(타입) — 이 키에 `MB`. 설정 화면 칸 컴포넌트(read_first로 찾은 파일)가 `unitLabel`이 있으면 입력 뒤에 정적 글자로 그린다(새 토큰 없음 — 기존 `--muted` 글자). 숫자 오류 문구는 04.1 S8과 같은 꼴 `숫자가 아닙니다 · 10처럼 적어 주세요`(스키마 오류 메시지).

⑤ **도메인** `domain/evidence/upload-checks.ts` `checkEvidenceUpload(input, { maxBytes, duplicates })` — 순수, behavior 단위 사례의 문자열을 export된 상수로 둔다(05-05 화면 · E2E가 import). 허용 MIME 목록 상수 `EVIDENCE_CONTENT_TYPES`. `domain/evidence/dto.ts` 증빙 파일 DTO(`registerDto`, 필드 → `expense.value`). `domain/evidence/index.ts`(주인 종류는 지출결의 하나 — 판정은 주인 종류별 함수 표 `OWNER_RULES`에 두어 06이 종류를 더한다):
   - `requestEvidenceUpload(viewer, input, deps?)` — 트랜잭션 없이: 주인 문서 로드 + `canSeeExpense` + 기안자 + 상태가 작성 중 · 반려 · 회수(아니면 404 또는 `UserFacingError`) → 한도 설정 → `findActiveBySha` + 다른 주인이면 그 문서를 viewer가 볼 수 있는지 `canSeeExpense`로 판정해 번호 노출 여부 결정 → `checkEvidenceUpload` → `insertIntent`(키 `incoming/{id}` — Addresses review concern: M1(Round 2) · 만료 15분 · 「오늘/지금」은 `deps.now`) → `storage.createSignedPut`(그 `incoming/` 키 · maxBytes = 한도, expiresSec 900).
   - `completeEvidenceUpload(viewer, { intentId }, deps?)` — 트랜잭션 전: 의도 로드(없음 · 남의 것 · 만료 · 완료 → `올리지 못함 · 다시 올리기`) · `storage.getMetadata(incoming/{intentId})` 재확인(없음 → 같은 문구 · 불일치 → `storage.delete` 후 같은 문구 + `log.warn("evidence.upload_metadata_mismatch", { intentId, ownerKind, ownerId, reason })` 한 줄 — Addresses review concern: F7) · 일치하면 새 파일 id를 만들고 `storage.move(incoming/{intentId}, evidence/{파일 id})`(실패 → 같은 문구 — 원본은 `incoming/`에 남아 7일 뒤 수명 주기가 지운다, Addresses review concern: M1(Round 2) · E2: A) · 행동 로그 켜짐 여부(`loadActionLogGate`). `withTransaction` 안(Addresses review concern: B2(Round 2)): **첫 단계 `lockExpenseForUpdate(viewer, ownerId, tx)`**(05-03 — 제출과 같은 행) → `deps.afterLock?.()`(경합 사례용 — 기존 `lock-race.ts` 관례) → 주인 상태 tx 재확인(허용 상태가 아니면 잠금 전 판정과 같은 거부) → `completeIntentIfOpen`(null이면 같은 문구) → `insertFile({ id: 파일 id, object_key: evidence/{파일 id}, … })` → `recordActionInTx(document_update, detail { change: "evidence_add", fileId })`. 트랜잭션이 거부되면 옮긴 `evidence/{파일 id}` 객체를 `storage.delete`로 지운 뒤 원래 거부를 올린다(파일 id가 호출마다 새것이라 같은 의도를 두 번 완료해도 먼저 붙은 객체를 지우지 않는다 — 이 삭제가 실패하면 원래 거부만 올리고 남은 객체는 Phase 6 F8 고아 정리 대상). 커밋 뒤 `storage.retain(evidence/{파일 id})` — 이중 방어라 실패해도 업로드는 성공이고 `log.warn("evidence.retain_failed", { fileId, reason })` 한 줄만 남긴다(Round 1 F9의 「retain 실패 → 거부」를 대체 — 1차 보호가 접두어 분리로 바뀌었다, Addresses review concern: M1(Round 2)).
   - `removeEvidence(viewer, { fileId }, deps?)` — 트랜잭션 전: 파일 · 주인 로드 + 기안자 판정(아니면 404). `withTransaction` 안(Addresses review concern: B2(Round 2)): **첫 단계 `lockExpenseForUpdate(viewer, ownerId, tx)`** → `deps.afterLock?.()` → 주인 상태 tx 재확인(작성 중/반려/회수가 아니면 잠금 전 판정과 같은 `UserFacingError` 거부 — 제출이 먼저 커밋된 경우) → 파일이 아직 살아 있는지 tx로 재확인(이미 지워졌으면 쓰기 · 로그 없이 성공으로 끝낸다 — 두 탭에서 같은 파일을 지운 결과가 같으므로 오류가 아니다) → `markRemoved` + `recordActionInTx(document_update, detail { change: "evidence_remove", fileId })` 같은 tx(바로 위 `completeEvidenceUpload`의 `evidence_add`와 같은 모양 — 새 행동 종류 없음, Addresses review concern: F2). 05-09가 제출 뒤 삭제의 「살아 있는 파일 > 1」 조건(`countActiveByOwner(tx)`)과 `bumpInstanceVersion`을 **이 잠금 아래** 같은 자리에 더한다. 허용 상태는 이 플랜에서 작성 중/반려/회수뿐이고 **승인된 문서의 삭제는 이 함수가 앞으로도 열지 않는다** — 제출 뒤 규칙(결재 중 삭제 허용 · 승인 뒤 추가만 · 잘못 붙인 증빙은 경영관리가 `voidEvidence`로 무효 처리)은 05-09 Task 3가 소유한다(사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리). 이 플랜은 무효 열 셋 · 「살아 있는 행」 조건 · 로그 detail 값 `evidence_void`(행동 종류 아님)의 자리만 둔다. 저장소 객체는 지우지 않는다(고아 정리는 Phase 6 F8 — `upload_intents` · `removed_at`이 그 쿼리의 재료임을 SUMMARY에 적는다).
   - `createEvidenceViewUrl(viewer, { fileId })` — 주인 문서를 볼 수 없으면 null, 보이면 `storage.createSignedGet(key, { expiresSec: 300, filename: original_name, disposition })` — `disposition`은 파일 형식이 `image/heic` · `image/heif`면 `attachment`(PC 브라우저가 못 그릴 수 있음 — 내려받기), 그 밖은 `inline`(리뷰 Round 1 P3-6 · 05-05가 화면 쪽). 주소를 저장하지 않는다.
   - `listEvidence(viewer, { ownerKind, ownerId })` — 보이는 사람에게만 DTO 목록.
   `domain/expenses/gate.ts`의 `evaluateExpenseSubmit`에 ⑧ `증빙 없음 · 증빙 올리기 Ctrl+U`(target `evidence`)를 ⑦ 다음 자리에 끼우고, `submitExpense` 트랜잭션 안 재판정 사실에 `countActiveByOwner(tx)`를 더한다. `test/integration/leak-scan.test.ts` 끝에 `import "@/domain/evidence";` 한 줄.

⑥ **테스트** `test/integration/fixtures/expenses.ts`의 `submitReadyDraft`가 제출 전에 메모리 가짜로 증빙 한 장을 붙인다(`attachEvidence(viewer, expenseId, storage)` 도우미를 같은 파일에 export — 05-09가 재사용). 새 파일 `test/integration/evidence-upload.test.ts` 첫 describe `트레이서 — 선언에서 제출까지`가 behavior 통합 네 줄을, `test/unit/domain/evidence/upload-checks.test.ts`가 단위 사례를 단언한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence test/unit/settings</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 크기 · 형식 · 중복 세 갈래 문자열이 UI-SPEC과 다르거나 registry-coverage가 `evidence.max_size_mb`를 「참조 없음」으로 잡으면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/evidence-upload.test.ts test/integration/expense-approval-lifecycle.test.ts test/integration/expense-submit-concurrency.test.ts test/integration/expense-installment-cap.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 의도 키가 `incoming/{id}`가 아니거나, 완료 뒤 객체가 `evidence/{파일 id}`로 옮겨지지 않았거나 `incoming/` 원본이 남거나, 완료 뒤 파일 행 · 로그가 없거나, 증빙 없는 제출이 통과하거나, 05-03 · 05-14 테스트가 도우미 변경 뒤 깨지거나, 누수 스캔이 증빙 DTO 미등록으로 실패하면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "incoming/" domain/evidence/index.ts`와 `grep -c "evidence/" domain/evidence/index.ts`가 둘 다 1 이상이고 객체 키를 만드는 곳이 그 파일뿐이다(실행자가 `grep -rnE '(incoming|evidence)/\$\{' domain lib app` 결과를 SUMMARY에 적는다 — M1 Round 2)
    - `grep -c "lockExpenseForUpdate" domain/evidence/index.ts`가 2 이상이다(완료 통보 · 삭제 두 트랜잭션 — B2 Round 2)
    - `grep -c "evidence.retain_failed" domain/evidence/index.ts`가 1이다(보존 표식 실패는 경고만 — M1 Round 2)
    - `grep -c "findActiveBySha" repositories/files.ts`가 1 이상이다
    - `grep -c "evidence_remove" domain/evidence/index.ts`가 1 이상이고 그 줄이 `document_update`와 같은 호출 안이다 — `grep -rn '"evidence_remove"' domain/action-log`가 0줄(행동 종류 등록부에 새 종류 없음 — 리뷰 Round 1 F2)
    - `grep -c "evidence.upload_metadata_mismatch" domain/evidence/index.ts`가 1이다(F7)
    - `domain/evidence/index.ts`에서 `createSignedGet` 결과를 리포지토리에 넘기는 호출이 없다(실행자 확인 + SUMMARY 한 줄 — 주소 비저장)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>작성 중 지출결의에 파일 한 장이 서명 PUT → 메타데이터 재확인 → 파일 행으로 붙고, 그 파일이 있어야 제출된다</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 스키마 확정 — `files` · `upload_intents` 마이그레이션 적용 · SQL 린트 · journal 가드</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `db/migrations/meta/_journal.json` · Task 1이 만든 SQL · 05-01 Task 2(같은 규칙)
  </read_first>
  <action>
① `pnpm db:generate` 재실행이 새 파일을 만들지 않음을 확인한다. ② 빈 DB 적용(`pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`, 개발 DB `pnpm db:migrate`) · `pnpm lint:sql` — 기존 표(`users`)를 참조하는 FK 문장이 squawk 잠금 규칙에 걸리면 그 앞에 SET LOCAL 블록 하나만 손으로 넣는다. ③ SUMMARY `migration_manifest`.
  </action>
  <verify>
    <automated>G=$(pnpm db:generate 2&gt;&amp;1); RC=$?; printf '%s\n' "$G"; test "$RC" -eq 0 &amp;&amp; grep -q "No schema changes" &lt;&lt;&lt;"$G"</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 "No schema changes"가 없다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db'</automated>
    <fails_when>빈 DB 적용 실패 · squawk 위반 1건 이상 · journal 가드 실패 · "No test files found" 중 하나</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 플랜의 SQL이 정확히 하나 · 이름 그대로 · journal diff가 끝 항목 하나 추가뿐(`. "$(git rev-parse --git-dir)/gsd-05-04.env" && test -n "$PLAN_BASE" && git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json`)
    - SQL 손 편집이 SET LOCAL 블록뿐 · `migration_manifest` 세 경로가 실제 파일과 같다
    - 두 검증 명령이 초록이다
  </acceptance_criteria>
  <done>증빙 두 표가 빈 DB부터 적용되고 재생성 차이 0 · squawk 0 · journal 가드 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 로컬 저장소 드라이버와 운영 차단 · 업로드 거부 사례 전부 — 남의 의도 · 만료 · 두 번 완료 · 메타데이터 불일치 · 번호 노출 규칙 · 상태별 추가 가능 · 보기 권한 · 제출↔삭제 잠금 경합 · 풀 2 증빙 추가</name>
  <files>lib/gcp/storage.ts, lib/env.ts, app/api/storage-local/[...key]/route.ts, .gitignore, .env.example, domain/evidence/index.ts, test/integration/evidence-upload.test.ts, test/integration/tx-safety.test.ts, test/unit/lib/gcp/storage-local.test.ts, test/unit/app/storage-local-route.test.ts, test/unit/env.test.ts</files>
  <read_first>
    - `lib/env.ts`(전문 — `rawSchema` · `superRefine` · 필수 키 목록 137–150행) · `test/unit/env.test.ts`(사례 모양)
    - `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`(Route Handler — `PUT` · `GET` · 동적 세그먼트 `params` 모양)
    - `app/api/health/route.ts`(기존 라우트 모양) · `test/unit/app/unsaved-edits.test.ts`(app 파일을 단위로 시험하는 선례)
    - Task 1 산출물 `lib/gcp/storage.ts` · `domain/evidence/index.ts` · `test/integration/fakes/memory-storage.ts`
    - `.gitignore`(데이터 디렉터리 줄이 없음을 확인)
    - `test/integration/lock-race.ts`(전문 — `deferred` · `waitForLockWaiter`) · 05-14 `test/integration/expense-submit-concurrency.test.ts`의 `같은 줄 경합` describe(`deps.afterLock` 쓰는 모양)
    - `test/integration/tx-safety.test.ts`의 풀 2 describe 모양(`grep -n 'describe\|DB_POOL_MAX\|resetModules' test/integration/tx-safety.test.ts` 뒤 그 범위만 Read — `process.env.DB_POOL_MAX = "2"` → `vi.resetModules()` → 격리 `import("@/db/client")`로 `pool.options.max` 2 단언 → 격리 import한 도메인 함수를 동시에 → 10초 안 · UserFacing 외 원시 풀 오류 0 → env 복원)
  </read_first>
  <behavior>
    - (단위, env) `APP_ENV=staging` + `STORAGE_DRIVER=local` → 파싱 실패(경로 `STORAGE_DRIVER`) · `APP_ENV=local` + 미설정 → 해석된 드라이버 `local` · `APP_ENV=staging` + 미설정 → `gcs` · `APP_ENV=prod` + `STORAGE_DRIVER=gcs` + `GCS_EVIDENCE_BUCKET` 미설정 → 부팅은 통과(버킷은 드라이버 생성 때 검사)
    - (단위, 로컬 드라이버) `createSignedPut`이 만든 주소를 검증 함수가 받아들이고, 서명 한 글자 변조 · 만료 지남 · 선언보다 큰 크기 · 다른 Content-Type · 다른 sha256 메타 · 허용 키 꼴 밖(`../x` · `evidence/abc` · `other/{uuid}`)은 전부 거부한다 — 허용 꼴은 `incoming/{uuid}` · `evidence/{uuid}` 둘뿐. 같은 비밀 · 같은 입력이면 같은 서명(결정적)
    - (단위, 로컬 드라이버 — M1 Round 2) `move("incoming/{uuid}", "evidence/{uuid2}")` 뒤 원본 바이트 · 사이드카가 없고 대상에 같은 바이트 · 형식 · sha256 메타가 있다 · 원본이 없으면 오류 · 허용 꼴 밖 키로 옮기기는 거부
    - (단위, 라우트) 해석된 드라이버가 gcs이면 PUT · GET 모두 404. local이면 유효한 PUT → 201 · 데이터 디렉터리에 바이트와 메타데이터, 유효한 GET → 200 · `Content-Disposition` 파일명 · `Content-Type` 선언값, 변조 주소 → 403
    - (통합) 남의 의도 id로 완료 → `올리지 못함 · 다시 올리기` · 파일 행 0 · 만료된 의도(`deps.now` + 16분) → 같은 거부 · 같은 의도 두 번 완료 → 두 번째 거부 · 파일 행 1 · 가짜 저장소의 `evidence/` 객체가 정확히 하나(파일 행의 키) — 두 번째 호출의 보상 삭제가 첫 번째 객체를 지우지 않는다 · 가짜 저장소 메타데이터 크기를 선언과 다르게 → 거부 + 가짜의 `incoming/` 객체가 지워짐 · 보존 표식 실패(가짜가 `retain`에서 던지게) → 완료는 성공 · 파일 행 1 · `evidence.retain_failed` 경고 1(M1 Round 2)
    - (통합, 잠금 경합 — B2 Round 2, `deps.afterLock` + `lock-race.ts`, 타이밍 대기 없음) 증빙 1장인 작성 중 문서에서 ⓐ `submitExpense`가 행을 잠근 채 멈춘 사이 같은 파일의 `removeEvidence`를 시작 → `waitForLockWaiter`로 대기 확인 → 제출을 풂 → 제출 성공(번호 있음) · 삭제는 잠금 뒤 상태 재확인에서 거부 · 살아 있는 증빙 1. ⓑ `removeEvidence`가 먼저 잠근 채 멈춘 사이 `submitExpense`를 시작 → 대기 확인 → 삭제를 풂 → 삭제 성공 · 제출은 tx 안 `countActiveByOwner(tx)` 0으로 `증빙 없음 · 증빙 올리기 Ctrl+U` · 번호 null. 두 순서 모두 「번호 있는 문서의 살아 있는 증빙 ≥ 1」. ⓒ 같은 파일 `removeEvidence` 둘(두 탭) — 첫 쪽이 잠근 채 멈춘 사이 둘째 시작 → 풂 → 둘 다 오류 없이 끝나고 `removed_at` 한 번 · `evidence_remove` 로그 1건
    - (통합, 풀 2 — M3 Round 2, `test/integration/tx-safety.test.ts`에 describe 하나 덧붙임) 픽스처 작성 중 문서 하나 · 의도 셋 · 메모리 가짜에 객체 셋을 기본 풀로 준비한 뒤 `DB_POOL_MAX=2` · `vi.resetModules()` · 격리 import한 `completeEvidenceUpload` 셋을 같은 가짜로 동시에 → 10초 안에 전부 끝나고 원시 풀 오류 0 · 파일 행 3(셋이 같은 지출결의 행 잠금에서 차례로 끝난다 — 트랜잭션 안 풀 읽기가 섞이면 여기서 멈춘다)
    - (통합) 반려 · 회수 상태 문서에는 업로드 요청이 열리고, 결재 중 문서에는 이 플랜에서 거부된다(05-09가 연다). 무관한 PM의 요청 → 404
    - (통합) 다른 PM이 올린 같은 sha256 파일이 그 PM의 작성 중 문서에 있을 때 → `이미 첨부된 파일 · 다른 파일 고르기`(번호 없음), 내가 볼 수 있는 제출 문서 `26001-0001`에 있을 때 → `같은 파일이 26001-0001 증빙에 있음 · 다른 파일 고르기`, 삭제된 파일의 sha256은 중복이 아니다
  </behavior>
  <action>
① **env** `lib/env.ts` `rawSchema`에 `STORAGE_DRIVER`(enum `local` · `gcs`, 선택) · `GCS_EVIDENCE_BUCKET`(선택 문자열)을 더하고, `superRefine`에 한 갈래 — `APP_ENV`가 local이 아닌데 `STORAGE_DRIVER`가 local이면 오류(경로 `STORAGE_DRIVER`). 해석 함수 `resolvedStorageDriver(env)` = 값이 있으면 그 값, 없으면 `APP_ENV` local → local · 그 밖 → gcs. 버킷은 부팅 검증에 넣지 않는다. `.env.example`에 두 변수를 주석과 함께(값은 비움 · 로컬 기본 설명 한 줄).
② **로컬 드라이버** `lib/gcp/storage.ts`에 `createLocalStorage({ secret, dataDir, baseUrl })` — 키 = `BETTER_AUTH_SECRET`에서 `node:crypto` HKDF(info `storage-local`)로 파생(값이 없으면 설정 오류 — 로컬 · CI는 이미 둔다), 주소 = `/api/storage-local/{키}?exp=…&max=…&ct=…&sha=…&op=put|get&fn=…&sig=…`(HMAC-SHA256, 비교는 `timingSafeEqual`), 순수 검증 함수 `verifyLocalSignedRequest(url, { method, contentLength, contentType, shaHeader, now })` export. 키 형식은 `^(incoming|evidence)/[0-9a-f-]{36}$`만 허용(경로 조작 차단 — 데이터 디렉터리 밖으로 나갈 수 없다 · M1 Round 2). `getMetadata` · `delete`는 데이터 디렉터리의 사이드카 메타데이터를 읽고 지우며, `move`는 같은 디렉터리 안에서 바이트 · 사이드카를 이름 바꾸기로 옮기고(두 키 모두 형식 검사 — Addresses review concern: M1(Round 2)), `retain`은 사이드카에 `retained: true`를 쓴다(로컬에는 수명 주기가 없다 — 인터페이스 일치용, F9). 데이터 디렉터리 = 저장소 루트의 `.data/uploads` — `.gitignore`에 `.data/` 한 줄.
③ **라우트** 새 파일 `app/api/storage-local/[...key]/route.ts`: `PUT` · `GET` 첫 줄에서 `resolvedStorageDriver(env)`가 local이 아니면 `log.warn("storage.local_route_blocked", { method, reason: "driver_not_local" })` 뒤 404(Addresses review concern: F7 — 키 · 쿼리 문자열은 싣지 않는다). local이면 `verifyLocalSignedRequest` 실패 → 403, PUT = 본문 바이트 길이 재확인 후 파일 · 사이드카 저장(201), GET = `Content-Type` 선언값 · `Content-Disposition: inline; filename*=UTF-8''…` · `X-Content-Type-Options: nosniff`로 바이트를 돌려준다. 세션 인증을 쓰지 않는다(서명 주소가 권한이다 — 발급은 권한 판정 뒤에만).
④ `getObjectStorage()`가 `resolvedStorageDriver`에 따라 local 드라이버를 만든다(gcs 분기는 05-12가 채울 자리 — 지금은 「gcs 드라이버 미구현」 설정 오류를 던진다. 05-12 전에는 staging에 배포하지 않는다).
⑤ **테스트** behavior 단위 셋을 `test/unit/env.test.ts`(덧붙임) · `test/unit/lib/gcp/storage-local.test.ts` · `test/unit/app/storage-local-route.test.ts`(임시 디렉터리 · env 주입)에 먼저 적고, 통합 거부 사례를 `test/integration/evidence-upload.test.ts` describe `거부 — 의도 · 메타데이터 · 상태 · 권한 · 중복`에, 잠금 경합 셋을 같은 파일 describe `잠금 — 제출과 증빙 삭제`에(Addresses review concern: B2(Round 2) — 05-14는 웨이브 3이라 `removeEvidence`가 아직 없으므로 경합 사례를 이 플랜에 둔다), 풀 2 사례를 `test/integration/tx-safety.test.ts` 끝 describe `풀 2 · 동시 증빙 추가 셋(05-04)`에(Addresses review concern: M3(Round 2) — 기존 describe · 사례는 고치지 않고 덧붙이기만) 적은 뒤 구현한다. 드러난 도메인 틈만 `domain/evidence/index.ts`에서 고친다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/env.test.ts test/unit/lib/gcp test/unit/app/storage-local-route.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — staging에서 local 드라이버가 통과하거나, 변조 · 만료 · 초과 크기 · 경로 조작 주소가 받아들여지거나, gcs 해석에서 라우트가 404가 아니면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/evidence-upload.test.ts test/integration/tx-safety.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 제한 시간 초과가 나온다 — 남의 · 만료 · 두 번째 완료가 파일 행을 만들거나, 두 번째 완료가 첫 번째 `evidence/` 객체를 지우거나, 메타데이터 불일치 객체가 남거나, 보존 표식 실패가 업로드를 막거나, 못 보는 문서의 번호가 중복 문구에 나오거나, 결재 중 문서에 업로드가 열리거나, 제출 ∥ 삭제 어느 순서에서 번호 있는 문서의 살아 있는 증빙이 0이 되거나, 같은 파일 두 삭제가 로그 둘을 남기거나, 풀 2에서 증빙 추가 셋이 멈추거나 원시 풀 오류를 내거나, 기존 tx-safety 사례가 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint · stylelint · tsc · next build 중 하나라도 error를 출력하거나 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "timingSafeEqual" lib/gcp/storage.ts`가 1 이상이다
    - `grep -c "^\.data/" .gitignore`가 1이다
    - `grep -c "storage.local_route_blocked" "app/api/storage-local/[...key]/route.ts"`가 1 이상이고 그 `log.warn` 인자에 `key` · `url`이 없다(F7 — 실행자 확인 한 줄 SUMMARY)
    - 라우트 파일의 첫 판정이 드라이버 확인이다 — `grep -n "resolvedStorageDriver" "app/api/storage-local/[...key]/route.ts"`의 첫 줄 번호가 `verifyLocalSignedRequest` 첫 줄 번호보다 작다
    - `. "$(git rev-parse --git-dir)/gsd-05-04.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml`이 비어 있다
    - (B2 Round 2) `grep -c "afterLock" test/integration/evidence-upload.test.ts`가 1 이상이고, 주석을 뺀 그 파일에 타이밍 대기가 없다(`grep -v '^ *//' test/integration/evidence-upload.test.ts | grep -cE "setTimeout|sleep\("`이 0을 출력)
    - (M3 Round 2) `grep -c "completeEvidenceUpload" test/integration/tx-safety.test.ts`가 1 이상이고 tx-safety 기존 줄은 지워지지 않았다(`. "$(git rev-parse --git-dir)/gsd-05-04.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- test/integration/tx-safety.test.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>로컬 · CI에서는 서명된 로컬 주소로, 운영에서는 로컬 창구 없이 — 업로드 거부 규칙 · 제출↔삭제 잠금 · 풀 2 증빙 추가가 전부 테스트로 고정되었다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 저장소(PUT) | 서버를 거치지 않는 바이트 — 크기 · 형식은 서명 조건과 완료 재확인으로만 통제된다 |
| 브라우저 → 완료 통보 | 의도 id는 추측 · 도용될 수 있다 |
| 로컬 라우트 → 파일 시스템 | 키 문자열이 경로가 된다 |
| 서명 GET 주소 → 제3자 | 주소가 새면 만료까지 누구나 연다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-401 | Spoofing | 남의 업로드 의도로 완료 통보 | high | mitigate | `completeIntentIfOpen`이 `created_by = viewer` · 미완료 · 미만료 조건 UPDATE로 한 번만 — Task 3 통합 사례(남의 · 만료 · 두 번째) |
| T-05-402 | Tampering / DoS | 크기 한도 우회(브라우저 검사 무시) | high | mitigate | 서명 조건에 크기 상한(로컬 `max` · GCS는 05-12 `X-Goog-Content-Length-Range`) + 완료 때 메타데이터 크기 재확인 + 불일치 객체 삭제 — Task 3 사례 |
| T-05-403 | Tampering | 형식 위장(HTML을 이미지로) | medium | mitigate | MIME 허용 목록 · Content-Type을 서명에 묶음 · GET은 선언 형식 + `nosniff` + 다른 원점(GCS). 로컬은 개발 전용 |
| T-05-404 | Tampering (경로 조작) | 로컬 라우트 키 | high | mitigate | 키는 서버가 `incoming/{uuid}`(업로드) · `evidence/{uuid}`(완료)로만 만들고 라우트 · 드라이버(`move` 포함)가 정규식으로 그 밖을 거부 — 단위 사례 `../x` · `other/{uuid}`(M1 Round 2) |
| T-05-405 | Elevation of Privilege | 로컬 저장소 라우트 운영 노출 | high | mitigate | env refine(staging/prod에서 local 거부) + 라우트 첫 줄 404 — 단위 둘(Pitfall 8) |
| T-05-406 | Information Disclosure | 중복 오류로 남의 문서 존재 확인 | low | mitigate | 볼 수 있을 때만 번호 — 해시를 알아야 하는 존재 오라클만 남음(`/cso` 확인 대상). 통합 사례 셋 |
| T-05-407 | Information Disclosure | 서명 GET 주소 | medium | mitigate | 보임 판정 뒤에만 생성 · 5분 · DB 비저장. 무관한 PM null 사례 |
| T-05-408 | Repudiation | 증빙 추가 · 삭제 | low | mitigate | `document_update` 로그(detail `change` = `evidence_add` · `evidence_remove`)를 파일 행과 같은 tx에(`recordActionInTx`) — 행동 종류 하나(F2) · 메타데이터 불일치 · 로컬 라우트 차단은 운영 로그 `log.warn`(F7) |
| T-05-410 | Tampering | 제출 ∥ 증빙 삭제 경합으로 증빙 0개 문서 제출(기준 3 위반) | high | mitigate | 완료 통보 · 삭제 트랜잭션 첫 단계 `lockExpenseForUpdate`(제출과 같은 행) + 잠금 아래 상태 · 살아 있는 파일 tx 재확인 — Task 3 `deps.afterLock` 두 순서 사례 · 같은 파일 두 삭제(B2 Round 2). 결재 중 서로 다른 파일 두 삭제는 05-09가 이 잠금 아래 「> 1」 조건으로 막는다 |
| T-05-411 | Tampering / Repudiation | 완료 증빙이 버킷 수명 주기에 지워짐 · 옮기기 중간 실패 | high | mitigate | 업로드는 `incoming/`, 완료 때 `evidence/{파일 id}`로 옮긴 뒤에만 파일 행 — 7일 규칙은 `incoming/`에만(05-12) · 보존 표식은 이중 방어(실패는 경고만). 옮기기 실패는 거부(원본은 `incoming/`에서 7일 뒤 정리), 트랜잭션 거부 뒤 옮긴 객체는 보상 삭제(E2: A · M1 Round 2) |
| T-05-409 | Spoofing | 거짓 SHA-256 선언 | low | accept | 클라이언트 선언값이라 중복 감지만 피할 수 있다 — 서버 재해시는 바이트를 내려받아야 해 하지 않는다(RESEARCH A10) |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — 해시 · HMAC · HKDF는 `node:crypto`. Task 3 수용 기준(`package.json` · lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 전체 통과
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration'` 전체 통과(05-03 · 05-14 지출결의 테스트 회귀 없음)
- `pnpm db:generate` 재실행 "No schema changes"
</verification>

<success_criteria>
- 파일이 서버를 거치지 않고 서명 주소로 올라가고, 서버는 메타데이터로 다시 확인한 뒤에만 증빙으로 인정한다
- 증빙 없이는 제출되지 않는다
- 로컬 드라이버는 로컬 · CI에서만 산다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `files` · `upload_intents` |
| 마이그레이션 | drizzle-kit 이름 SQL 1개 + 스냅숏(05-13 재생성 · `migration_manifest`) |
| 함수 | `requestEvidenceUpload` · `completeEvidenceUpload` · `removeEvidence` · `createEvidenceViewUrl` · `listEvidence` · `checkEvidenceUpload` · `findActiveBySha` · `countActiveByOwner` · `completeIntentIfOpen` · `getObjectStorage` · `createLocalStorage` · `verifyLocalSignedRequest` · `resolvedStorageDriver` · 게이트 ⑧ |
| 라우트 | `app/api/storage-local/[...key]/route.ts`(PUT · GET, local만) |
| 설정 키 · 표시 필드 | `evidence.max_size_mb`(기본 10) · `SettingDef.unitLabel` |
| 환경 변수 | `STORAGE_DRIVER`(local · gcs) · `GCS_EVIDENCE_BUCKET`(비밀 아님) |
| 테스트 | `test/integration/evidence-upload.test.ts`(잠금 경합 describe 포함) · `test/integration/tx-safety.test.ts`(풀 2 증빙 추가 describe 추가) · `test/integration/fakes/memory-storage.ts` · `test/unit/domain/evidence/upload-checks.test.ts` · `test/unit/lib/gcp/storage-local.test.ts` · `test/unit/app/storage-local-route.test.ts` · `test/unit/env.test.ts`(추가) |
| 객체 키 규칙 | 업로드 `incoming/{의도 id}` → 완료 `evidence/{파일 id}`(`ObjectStorage.move`) — E2: A |
| 기록 | SUMMARY — `upload_intents` · `removed_at`이 Phase 6 F8 고아 정리 쿼리의 재료. 미완료 업로드(`incoming/`)는 버킷 수명 주기가 지우므로 F8의 몫은 지운 파일(`removed_at`)의 `evidence/` 객체(보존 표식 해제 포함)와 트랜잭션 거부 뒤 보상 삭제가 실패한 `evidence/` 객체뿐 · 경로별 잠금 순서 표(증빙 추가 · 삭제 = 지출결의 행만) |

<output>
Create `.planning/phases/05-expense-approval-leave/05-04-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F2 — `removeEvidence` 로그 종류 `evidence_remove`가 「증빙 추가 · 삭제는 `document_update`」 규칙과 모순(L18@1108e20) | HIGH | Plan 05-04, Task 1 ⑤: `recordActionInTx(document_update, detail { change: "evidence_remove", fileId })`로 통일 · must_haves truth에 detail 모양 명시 · acceptance에 행동 종류 등록부 무변경 grep · 위협 T-05-408 갱신 |
| F7 — 구조화 로그 없음(업로드 완료 메타데이터 불일치 · 로컬 드라이버 운영 차단)(L25@1108e20) | MEDIUM | Plan 05-04, Task 1 ⑤ `evidence.upload_metadata_mismatch` · Task 3 ③ `storage.local_route_blocked` — `lib/log.ts` `log.warn`, id · 사유 코드만 · acceptance grep 둘 |
| F9 — 고아 객체(`05-04:173` 인용)(L27@1108e20) | MEDIUM | Plan 05-04 Task 1 ③ · ⑤: 저장소 인터페이스에 `retain(key)` · 완료 통보가 파일 행 전에 `retain`(실패면 거부) · 트레이서가 `retain` 기록을 단언 — 수명 주기 규칙(`evidence/` 7일 삭제, 보존 표식 객체 제외)은 Plan 05-12 Task 2 부트스트랩. Phase 6 F8은 그대로(지운 파일 정리는 표식 해제 후 삭제) |
| F4 — read_first 줄 범위가 머지 뒤 어긋남(L22@1108e20) | MEDIUM | Plan 05-04, Task 1 read_first: `domain/action-log/record.ts` 줄 범위를 이름 Grep 방식으로 교체(게이트 단계는 05-01 Task 1 ⓪-b) |
| P3-6 — HEIC 서버 처리(L35@1108e20) | LOW | Plan 05-04 Task 1 ⑤: HEIC · HEIF는 `EVIDENCE_CONTENT_TYPES`에 그대로 두고(허용) `createEvidenceViewUrl`이 그 두 형식에 `disposition: attachment` — 화면 처리는 05-05 |
| U1 — 승인 뒤 증빙 삭제(L40@1108e20) | 사용자 결정 대기(아래 「U1/U2 사용자 결정 반영」 행이 대체) | Plan 05-04 Task 1 ⑤: `removeEvidence`는 이 플랜에서 작성 중/반려/회수만 · 승인 문서 삭제를 열지 않음을 명시. 제출 뒤 규칙은 05-09 Task 3(사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리) |
| U1/U2 사용자 결정 반영 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리 · U2 Phase 6 이관 | 사용자 결정 | Plan 05-04 Task 1 ① · ② · ⑤ · truth(로그 detail): 승인 뒤 삭제 불가 유지 · `files`에 `voided_at` · `voided_by` · `void_reason`(같은 마이그레이션, 번호 미기재) · 「살아 있는 행」 = 삭제 · 무효 아님(게이트 ⑧ · 중복에서 무효 제외) · 로그 detail `evidence_void`(05-09 소유) 정합. U2는 이 플랜과 무관. UI-SPEC 가정 #8을 사용자 결정으로 대체 — `/plan-design-review`에서 UI-SPEC 보강 필요 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 해당하는 지적은 전부 반영했다 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B2 — `completeEvidenceUpload` · `removeEvidence`가 지출결의 행을 잠그지 않아 제출 ∥ 삭제로 증빙 0개 제출(L27@69fb7ea) | HIGH | Plan 05-04 Task 1 ⑤: 두 함수 트랜잭션 첫 단계 `lockExpenseForUpdate`(05-03) → `deps.afterLock` → 주인 상태 tx 재확인 → (삭제) 파일 살아 있음 tx 재확인(이미 지워졌으면 쓰기 · 로그 없이 성공). 새 truth · key_link · execution_notes 잠금 순서(지출결의 행만 — 제출의 프로젝트 → 지출결의와 교착 없음) · acceptance grep(`lockExpenseForUpdate` ≥ 2). Task 3: `deps.afterLock` 두 순서 사례(ⓐ 제출 먼저 → 삭제 거부 · 증빙 1 / ⓑ 삭제 먼저 → 제출 ⑧ 거부 · 번호 null) + 같은 파일 두 삭제(로그 1) · 타이밍 대기 0 grep · T-05-410. **사례 위치: 이 플랜(05-04)** — 05-14는 웨이브 3이라 `removeEvidence` · `files` 표가 아직 없다. 웨이브 순서(03 → 14 → 04)는 그대로 둔다 |
| M1 — `evidence/` 전체 7일 삭제 + 보존 표식 하나로 완료 증빙을 지키는 구조, `05-04:173`(L30@69fb7ea) · 사용자 결정 E2: A(L19@69fb7ea) | MEDIUM | Plan 05-04: truth(키 규칙) 갱신 · Task 1 ③ `ObjectStorage.move` 추가(local = 이름 바꾸기 · 메모리 가짜 = 이동 기록 · gcs = 05-12) · ⑤ 의도 키 `incoming/{의도 id}` → 완료 통보가 메타데이터 재확인 뒤 `evidence/{파일 id}`로 옮기고 파일 행 · 트랜잭션 거부 뒤 보상 삭제 · `retain`은 커밋 뒤 이중 방어(실패 = `evidence.retain_failed` 경고만). 파일 id를 목적지 키로 쓰는 이유: 같은 의도 두 번 완료의 보상 삭제가 먼저 붙은 객체를 지우지 않게. behavior(트레이서 · 거부 · 로컬 `move`) · fails_when · acceptance grep(`incoming/` · `evidence/` · `evidence.retain_failed`) · Task 3 ② 로컬 키 정규식(`incoming/{uuid}` · `evidence/{uuid}` 두 꼴만) · T-05-404 · T-05-411 · Artifacts. `05-04:49 · 190 · 286`(Round 1 판 줄) 전부 갱신 |
| M1이 뒤집는 Round 1 판단 — Supersedes Round 1 Addressed: F9 행(`retain` 실패 → 거부 · 보존 표식이 유일한 보호) | MEDIUM | 이제 1차 보호는 접두어 분리(`incoming/`만 수명 주기)이고 `retain`은 이중 방어다 — 실패해도 업로드를 막지 않는다(Task 1 ⑤). 인터페이스 메서드 `retain`과 트레이서의 `retain` 기록 단언은 유지 |
| M3 — 증빙 추가 경로에 풀 압력 증명(`tx-safety.test.ts`, 풀 2)이 없음(L32@69fb7ea) | MEDIUM | Plan 05-04 Task 3: `test/integration/tx-safety.test.ts` 끝에 describe `풀 2 · 동시 증빙 추가 셋(05-04)` — 기존 구조(`DB_POOL_MAX=2` · `vi.resetModules()` · 격리 import · 10초 · 원시 풀 오류 0) 그대로, 같은 문서 `completeEvidenceUpload` 셋 동시 → 파일 행 3. files_modified · Task 3 files · read_first · verify 명령 · acceptance(덧붙임만 — 삭제 줄 0) 추가. 05-09가 `bumpInstanceVersion`을 붙인 뒤에도 같은 verify로 초록이어야 한다 |
| P3-11 — `files.owner_id` FK 없음 · sha256 UNIQUE 없음, 동시 같은 파일 업로드 허용 여부(L48@69fb7ea) | LOW | Plan 05-04 Task 1 ①: 허용으로 명시 — 중복 검사는 안내이고 같은 파일 두 장은 금액 · 제출 판정에 영향이 없으며 삭제로 되돌린다 · UNIQUE는 삭제 · 무효 뒤 다시 올리기를 막는다 · `owner_id`는 주인 종류가 여럿이라 FK 불가 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| B2 삭제 ↔ 삭제 「결과 증빙 ≥ 1」 사례(결재 중 서로 다른 파일 둘 동시 삭제)(L27@69fb7ea) | 이 플랜에서는 재현할 수 없다 — 05-04의 삭제는 작성 중/반려/회수만 허용하고(그 상태에서 0개는 정상 · 제출은 ⑧이 막음) 결재 중 삭제와 「살아 있는 파일 > 1」 조건은 Plan 05-09 Task 3이 연다. 잠금과 잠금 아래 재확인 자리는 이 플랜이 만들었으므로 05-09 Task 3에 같은 `deps.afterLock` 두 순서 사례(결재 중 · 파일 2 · 서로 다른 파일 삭제 둘 → 하나 성공 · 하나 거부 · 결과 ≥ 1)를 두어야 한다 — **05-09 담당에게 넘김**(이 플랜 소유 파일 밖) |
| 상태가 바뀐 뒤(제출이 먼저 커밋) 삭제 거부 문구 | 새 문구를 만들지 않는다 — 잠금 전 상태 판정과 같은 거부를 쓴다. 전용 문구가 필요하다고 보면 design 게이트에서 UI-SPEC 보강 |

