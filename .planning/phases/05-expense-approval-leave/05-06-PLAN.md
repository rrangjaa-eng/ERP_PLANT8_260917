---
phase: 05-expense-approval-leave
plan: 06
type: execute
wave: 7
depends_on: ["05-15"]
files_modified:
  - domain/expenses/tax.ts
  - domain/expenses/gate.ts
  - domain/expenses/index.ts
  - domain/money/index.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - app/(app)/expenses/[id]/expense-form.tsx
  - app/(app)/expenses/[id]/expense-document.tsx
  - app/(app)/expenses/[id]/expense.module.css
  - test/unit/domain/expenses/tax.test.ts
  - test/unit/domain/expenses/submit-gate.test.ts
  - test/unit/domain/money.test.ts
  - test/integration/expense-tax-snapshot.test.ts
  - test/e2e/expense-form.spec.ts
autonomous: true
requirements: [EXP-15, UX-06, EXP-14, ADMN-04]  # ADMN-04는 「세율·수식」 부분만(REQUIREMENTS.md 추적표 — 결재 부분은 04.1, 알림은 Phase 7) · 리뷰 Round 1 P3-4

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "사람은 공급가액만 적는다 — 폼에서 증빙 종류 · 공급가액 · 통화 · 환율 · 지급 예정일 · 분할 지급을 바꾸면 서버 미리보기(`previewExpense` — 쓰기 없음)가 계산 한 줄과 막힘 이유를 다시 보내고, 오는 동안 이전 한 줄은 `--faint`로 남는다(빈칸 · 뼈대 없음). 종류를 바꾸면 즉시 다시 계산된다(EXP-15 · 기준 7 · UIC loading S5 · S6)"
    - "계산은 `domain/money.applyTaxRule()` 호출 하나다 — 규칙 넷의 계산 한 줄 문자열: 부가세 가산 `부가세 10% 1,240,000 · 지급 총액 13,640,000 · 세금계산서 규칙` / 없음 `지급 총액 12,400,000 · 계산서 규칙` / 원천징수 `원천징수 8.8% 264,000 · 실지급액 2,736,000 · 기타소득 규칙`(면제 기준 이하 `원천징수 0 · 면제 기준 이하 · 실지급액 100,000 · 기타소득 규칙`) / 회사 대납 `회사 대납 세금 22% {세액} · 지급 총액 {공급가액} · {종류} 규칙` / 외화면 맨 앞 `원화 {원화} · ` / 세율 없음 `계산 불가 · 세율 없음`. 숫자만 700이고 출처 꼬리 글자가 없다. 같은 서버 함수 `taxLineText`가 폼 · 문서 화면 · 결재 시트 문자열을 만든다(UI-SPEC S5 · Copywriting · B1)"
    - "사업소득 증빙은 `incomeType` `business`로 넘겨 1,000,000 → 원천징수 33,000(3.3%)이고, 기타소득은 8.8%다(Pitfall 4)"
    - "적용 기준일: 증빙 종류 코드 항목의 `taxRule.basisDate`가 있으면 그것, 없으면 규칙 종류의 설정(`tax.basis_date.withholding` — 원천징수 · 회사 대납 / `tax.basis_date.vat` — 부가세)을 따른다. 날짜 사슬은 D-101 기본값 그대로 — 지급일(Phase 6 전에는 없음) → 지급 예정일 → 서울 오늘 / 증빙일 · 발행일(이 페이즈에 없음) → 작성일. 기준일은 화면에 쓰지 않는다(D-101 · 계획 결정 — 코드표 필드가 설정보다 먼저)"
    - "제출 때 저장한 세율 스냅숏(세율 · 세율 설정 이력 행 id · 그 행의 적용일 — 값 복사, 외래 키 없음) · 기준일로 문서 화면 · 결재 시트는 **저장된 값**을 보이고, 지금 설정 · 기준일로 다시 계산한 값이 다르면 계산 한 줄 바로 아래 `--warning` 한 줄 `세율 바뀜 · 부가세 10% → 12% · 지급 총액 13,640,000 → 13,888,000`이 서고 같으면 줄이 없다 — 막지 않는다. 작성 중 폼은 늘 지금 기준이라 이 줄이 없다(기준 7 · Eng OV-5 · UIC partial S5 · B3)"
    - "`세율 바뀜` 판정은 저장 스냅숏의 세율 · 금액과 지금 재계산의 세율 · 금액을 비교한다 — 스냅숏의 이력 행 id로 설정 이력 표를 다시 읽지 않는다. 그래서 제출 문서가 참조한 예정 세율을 관리자가 취소해도(행 물리 삭제) 취소는 성공하고, 그 문서는 저장값 한 줄 + `세율 바뀜`을 보인다(사용자 결정 E1: A · B1)"
    - "회사 대납 설정 기본값이 `tax.company_borne.rate` 0.22 · `tax.company_borne.method` `gross_up`이다 — 공급가액 3,000,000 · 1원 반올림이면 회사 대납 세액 846,154 · 지급 총액 3,000,000. 5만원 이하 면제는 Phase 11(CERT-04)에 남는다(사용자 결정 2026-09-26 #7 · D-1105)"
    - "제출 막힘은 서버 규칙 `expense.submit` 하나가 판정하고(`domain/rules/register.ts` 등록 · 문맥은 `domain/expenses/gate.ts`) 첫 이유 하나만 보인다 — 순서 고정 ① `2차 고객 승인 전 · 고객 승인 표시`(담당 PM) / `2차 고객 승인 전 · 담당 PM {이름}`(그 밖) ② `완료 프로젝트 · 새 지출결의 없음` ③ `견적 줄이 현재 차수에 없음 · 견적 줄 바꾸기` ④ `이 줄에 지출결의 {번호} 있음 · 지출결의 열기` ⑤ `거래처 없음 · 거래처 고르기` ⑥ `{칸들} {n}칸 비어 있음 · {첫 칸} 적기` ⑦ `공급가액이 0 · 0보다 크게` ⑧ `증빙 없음 · 증빙 올리기 Ctrl+U` ⑨ `세금 계산 불가 · 세율은 경영관리`. ①과 ⑤는 기존 규칙 `quote.customer-approval` · `quote.vendor-required`의 문자열 그대로다(UX-06 · UI-SPEC S6 · 기준 3)"
    - "막히면 1차 `지출결의 제출`은 `aria-disabled` + `aria-describedby` → 이유 글자(네이티브 `disabled` 아님 — 탭 순서에 남아 이유가 읽힌다)이고, 비활성 1차를 눌러도 서버를 부르지 않고 다음 한 수의 대상(칸 · 첨부 영역 · 프로젝트 상세 링크 · 지출결의 열기)으로 포커스를 옮긴다. 막힘이 풀리면 이유와 3차가 사라지고 1차가 산다(S6 · UIC a11y S6의 동작 부분)"
    - "폼의 첫 포커스는 막힘 이유가 가리키는 칸이다(증빙 없음이면 첨부 영역, 빈 칸이면 그 칸, 막힘이 없으면 1차)(UIC interaction S3 첫 포커스)"
    - "제출 액션도 같은 규칙으로 다시 거부한다 — 거부 이유 문자열은 1차 옆 막힘 자리에 그대로 서고, 미리보기 호출 실패는 `제출 실패 · 네트워크 · 다시 제출` 꼴이다. 다시 받는 동안 이전 이유와 1차 상태를 그대로 둔다(UIC error S6 · loading S6)"
    - "막힘 이유는 PC에서 1차 옆에서 줄바꿈되고 폰은 제출 줄 아래 전폭 한 줄이며, 칸 이름이 일곱 개까지 늘어도 자르지 않는다. 계산 한 줄은 조각 사이에서만 줄바꿈하고 증빙 종류 이름이 길어도 `… 규칙` 조각만 꺾인다(UIC long-text S5 · S6)"
    - "세율이 그 기준일에 없으면 한 줄 `계산 불가 · 세율 없음` + 제출 막힘 ⑨(UIC error S5)"
    - "`project.customer_approval_gate`를 끄면 ①이 사라지고, 수주중 프로젝트는 ① 없이 제출된다(D-43)"
    - "Phase 6 카드 사용 경로(D-607)를 위해 `computeExpenseTax`의 입력은 공급가액(원화)으로 고정된다 — 이 페이즈는 EXP-15 문구를 고치지 않는다(RESEARCH Deferred 권고)"
  prohibitions:
    - statement: "화면이 막힘 판정 · 세금을 스스로 계산해서는 안 된다 — 서버 규칙의 첫 이유 문자열과 서버 계산 한 줄만 그린다"
      status: resolved
      verification: judgment
    - statement: "이 페이즈가 세금 계산식을 새로 써서는 안 된다 — `applyTaxRule()` 한 번(ROADMAP 기준 7 · Issue 8). 호출자는 날짜 · 소득 종류 · 세율 행 id만 고른다"
      status: resolved
      verification: judgment
    - statement: "게이트 계약(`GateDecision`)을 바꿔 다음 한 수 대상을 넣어서는 안 된다 — 대상은 문맥 조립 쪽이 같은 순서로 따로 돌려준다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/expenses/tax.ts"
      provides: "pickTaxDates(D-101 전 사슬) · taxLineText · taxDriftText — 06-03이 같은 파일을 확장"
      exports: ["pickTaxDates", "taxLineText", "taxDriftText", "computeExpenseTax"]
    - path: "domain/rules/register.ts"
      provides: "게이트 규칙 expense.submit(①~⑨ 순서)"
      contains: "expense.submit"
    - path: "domain/expenses/gate.ts"
      provides: "buildExpenseSubmitContext · nextActionTarget(같은 순서로 대상 칸)"
      exports: ["buildExpenseSubmitContext", "nextActionTarget"]
    - path: "test/integration/expense-tax-snapshot.test.ts"
      provides: "세율 스냅숏(세율 · 이력 행 id · 적용일) 저장 · 새 이력 세율 뒤 세율 바뀜 · 같으면 없음 · 예정 세율 취소 성공 뒤 세율 바뀜(B1)"
      min_lines: 40
    - path: "test/unit/domain/expenses/submit-gate.test.ts"
      provides: "①~⑨ 순서 · 첫 이유 · 대상 칸"
      min_lines: 60
  key_links:
    - from: "app/(app)/expenses/[id]/expense-form.tsx"
      to: "app/(app)/expenses/actions.ts"
      via: "칸 변경 → previewExpenseAction → 계산 한 줄 · 첫 이유 · 대상"
      pattern: "previewExpenseAction"
    - from: "domain/expenses/index.ts"
      to: "domain/rules/gate.ts"
      via: "미리보기 · 제출 트랜잭션 재판정이 같은 gate(doc, expense.submit, ctx)"
      pattern: "expense.submit"
    - from: "domain/expenses/tax.ts"
      to: "domain/money/tax.ts"
      via: "applyTaxRule 한 번 — 날짜 슬롯에 고른 기준일"
      pattern: "applyTaxRule"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**세금 한 줄과 제출 막힘 이유를 폼에서 끝까지**: 사람이 증빙 종류를 바꾸는 순간 서버가 `applyTaxRule()` 한 번으로 계산한 한 줄이 돌아오고, 제출 때 세율 버전이 저장되어 나중에 세율이 바뀌면 문서에 차이가 보이며, 제출 버튼은 `domain/rules.gate`의 첫 이유와 다음 한 수로 막힌다.

Purpose: 기준 7(EXP-15)과 기준 3의 「선행 단계 전에는 이유와 함께 비활성」(UX-06)이 이 플랜에서 성립한다. 사용자 결정 2026-09-26 #7(회사 대납 22% · gross-up)과 D-101(기준일 기본값)을 코드로 옮긴다.

Output: 세금 호출자 완성(`pickTaxDates` 전 사슬 · `taxLineText` · `taxDriftText`) · `formatRatePercent` · 게이트 규칙 `expense.submit` · 미리보기 액션 · 폼 즉시 재계산 · 막힘 표시 · 문서 화면 세율 바뀜 · 설정 기본값 둘 · 단위 2 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-RESEARCH.md
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-03-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-05-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development`(단위 사례표 먼저) · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-06.env"` · diff 검사 접두는 05-01과 같은 모양. 트랜잭션 규약(세금 계산 · 설정은 트랜잭션 전 — 제출 트랜잭션 안 재판정은 tx로 읽은 사실 + 미리 읽은 설정) · `.planning/STATE.md` 불변 · 새 의존성 0 · 금액 · 세율 산술은 `domain/money` 안에서만.
- 화면 규칙: SYSTEM.md §6-3 · §7-1 · §7-15(05-02 개정 뒤) · UI-SPEC S5 · S6. 새 토큰 0. `--warning`은 세율 바뀜 한 줄(B3)과 기존 용도에만.
- `domain/rules/register.ts`는 이 플랜이 규칙 하나를 **덧붙이는** 곳이다 — 기존 규칙의 문자열 · 순서 · 면제 상태를 바꾸지 않는다.
- 이 플랜에서 다음 한 수 3차는 지금 있는 대상(칸 포커스 · 첨부 영역 · 프로젝트 상세 링크 · `지출결의 열기`)만 연결한다. `견적 줄 바꾸기` · `거래처 고르기`의 골라내기 열기는 05-07이 같은 자리에 연결한다(대상 id는 이 플랜이 이미 보낸다).
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 폼에서 증빙 종류를 기타소득으로 바꾸면 원천징수 한 줄이 즉시 오고, 제출 때 세율 버전이 저장되며, 새 이력 세율 뒤 문서 화면에 `세율 바뀜`이 선다 (세금 호출자 완성 · 미리보기 액션 · 폼 · 문서 화면 · 기본값)</name>
  <files>domain/expenses/tax.ts, domain/expenses/index.ts, domain/money/index.ts, domain/settings/keys.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/[id]/expense-form.tsx, app/(app)/expenses/[id]/expense-document.tsx, app/(app)/expenses/[id]/expense.module.css, test/unit/domain/expenses/tax.test.ts, test/unit/domain/money.test.ts, test/integration/expense-tax-snapshot.test.ts, test/e2e/expense-form.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 259–300행(Pattern 2 · Deferred 권고)
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S5(433–443행) · Copywriting 270–271행
    - `domain/money/tax.ts`(전문 — 날짜 슬롯 규약 · 규칙별 계산 · 반올림) · `domain/code-tables/tax-rule.ts`(기준일 값 넷) · `domain/seed/index.ts`(시드 증빙 종류의 `taxRule` — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4))
    - `domain/settings/keys.ts`(세율 · 회사 대납 · 기준일 키 — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4)) · `domain/settings/registry.ts`의 `getSettingEntry`(05-03)
    - 05-03 산출물 `domain/expenses/tax.ts`(기본 경로) · 05-05 산출물 `expense-form.tsx` · `expense-document.tsx`
    - `domain/settings/registry.ts`의 `addHistorizedValue` · `cancelHistorizedValue`(테스트가 새 세율을 넣고 예정 세율을 취소하는 경로 — 취소는 **실제 시계**(`dateOnly(new Date())`)로 미래 여부를 보고 주입이 없다, `grep -n` 뒤 범위 Read) · `repositories/settings.ts` `deleteFutureHistorizedValue`(물리 삭제)
  </read_first>
  <behavior>
    - (단위) `formatRatePercent(0.1)` = `10%` · `0.088` = `8.8%` · `0.033` = `3.3%` · `0.22` = `22%`(부동소수 오차 없음)
    - (단위) `taxLineText` 규칙 넷 + 면제 + 외화(`원화 5,537,280 · 부가세 …`) + 계산 불가 문자열이 must_haves 원문과 글자 그대로 같다(숫자 700 표식은 조각 배열의 `emphasis` 필드로 — 문자열 연결 결과가 원문)
    - (단위) `taxDriftText`: 저장 10% · 지급 총액 13,640,000 vs 지금 12% · 13,888,000 → `세율 바뀜 · 부가세 10% → 12% · 지급 총액 13,640,000 → 13,888,000`, 같으면 null
    - (단위) `pickTaxDates`: 코드 항목 `basisDate: "payment_date"` · 지급 예정일 없음 · 오늘 `2026-09-26` → 기준일 `2026-09-26` · 코드 항목에 기준일이 없고 부가세 규칙 · 설정 `evidence_date` → 작성일 · 설정을 `document_date`로 바꿔도 작성일 · `scheduled_payment_date` + 지급 예정일 `2026-10-05` → `2026-10-05`
    - (단위) `computeExpenseTax`: 사업소득 1,000,000 → 원천징수 33,000 · 기타소득 1,000,000 → 88,000 · 회사 대납 종류(테스트 코드 항목) 3,000,000 · 설정 기본값 → 회사 대납 846,154 · 지급 총액 3,000,000 · 세율 설정 행이 기준일에 없고 기본값도 없으면 `unavailable`
    - (통합) 제출 때 세율 스냅숏(`tax_rate` · `tax_rate_setting_id` · `tax_rate_effective_from`)이 기준일에 유효한 이력 행의 값(픽스처로 부가세 이력 행을 먼저 넣은 경우 — 세율 0.1 · 그 행 id · 그 행 적용일)이고, 그 뒤 더 이른 적용일의 새 부가세 12% 이력 행을 넣으면 `getExpense`의 계산 한 줄은 저장값(10%) 그대로 · `taxDrift`가 `세율 바뀜 · 부가세 10% → 12% · …`. 새 행이 기준일 뒤 적용이면 `taxDrift` null (Addresses review concern: B1(Round 2) — 열 이름 · 값 스냅숏)
    - (통합, 예정 세율 취소 — Addresses review concern: B1(Round 2) · 사용자 결정 E1: A) 테스트 증빙 종류 코드 항목(부가세 가산 · `basisDate: "scheduled_payment_date"`)으로 공급가액 12,400,000 문서를 만든다. 관리자가 `addHistorizedValue`로 부가세 12% 예정 행(적용일 = **실제** 오늘 + 30일 — `cancelHistorizedValue`가 실제 시계로 미래를 판정하므로 `deps.now`로 흉내 내지 않는다)을 넣고, 지급 예정일 = 그 적용일 + 5일로 저장 · 제출 → 스냅숏 세율 0.12 · 이력 행 id = 그 예정 행 id · 적용일 = 그 날짜 · 지급 총액 13,888,000. 이어 관리자가 `cancelHistorizedValue`로 그 예정 행을 취소 → **예외 없이 성공**(23503 없음) · 이력 행이 사라짐 · 문서의 스냅숏 열 셋은 그대로 · `getExpense`의 계산 한 줄은 `부가세 12% 1,488,000 · 지급 총액 13,888,000 · …`(저장값) · `taxDrift`의 `→` 오른쪽은 예정 행을 넣기 **전에** 같은 문서로 `computeExpenseTax`가 준 세율 · 지급 총액과 같다(같은 파일 앞 사례가 넣은 과거 이력 행에 흔들리지 않는 기대값 — 새 DB에서는 `세율 바뀜 · 부가세 12% → 10% · 지급 총액 13,888,000 → 13,640,000`)
    - (E2E, desktop) 폼에서 증빙 종류 `세금계산서` → `기타소득`으로 바꾸면 계산 한 줄이 `원천징수 8.8% … · 실지급액 … · 기타소득 규칙`으로 바뀐다(바뀌는 동안 이전 줄 `--faint` 클래스) · 제출한 문서 화면에 같은 한 줄
  </behavior>
  <action>
① **세금 호출자 완성** `domain/expenses/tax.ts`: `pickTaxDates(doc, { codeBasis, basisWithholding, basisVat, todayKst })` → `{ basisKind, basisDate, applyOpts: { paymentDate, evidenceDate } }` — 기준일 종류 = 코드 항목 `taxRule.basisDate` ?? 규칙 종류 설정(원천징수 · 회사 대납 → `tax.basis_date.withholding`, 부가세 → `tax.basis_date.vat`), 날짜 사슬은 must_haves 원문, 고른 날짜를 `applyTaxRule`이 그 규칙에서 읽는 슬롯(원천징수 · 회사 대납 = `paymentDate`, 부가세 = `evidenceDate` — tax.ts 27–29행 규약)에 넣는다. `resolveAppliedRate`는 같은 `basisDate`로 세율 키를 조회한다(05-03 기본 경로를 이 사슬로 바꾼다). `taxLineText(result, money, ruleLabel)` → `{ text, parts }` · `taxDriftText(stored, recomputed)` → string | null. 세율 % 문자열은 `domain/money/index.ts`의 새 순수 함수 `formatRatePercent(rate)`(문자열 조작 · 소수 첫째 자리) — 금액 서식은 기존 서식 함수.
② **기본값** `domain/settings/keys.ts`의 `TAX_COMPANY_BORNE_RATE.default` 0.088 → 0.22, `TAX_COMPANY_BORNE_METHOD.default` `flat` → `gross_up`(사용자 결정 2026-09-26 #7). 두 키의 라벨 · 스키마 · 값 목록은 바꾸지 않는다. 5만원 이하 면제는 새 계산이라 넣지 않는다 — SUMMARY 「Phase 11로 넘김」 한 줄.
③ **미리보기 · 조회** `domain/expenses/index.ts`에 `previewExpense(viewer, { expenseId, fields })` — 기안자 · 작성 중(또는 05-09의 반려 · 회수)만, zod 검증된 저장 전 칸 값을 초안에 겹쳐 `computeExpenseTax` + `taxLineText` + 게이트 첫 이유 · 대상 칸(Task 2가 채움 — 이 task는 05-03 · 05-04 순서의 이유를 그대로) + 회차 상한 칸 오류를 돌려준다. 쓰기 없음. `getExpense`의 문서 DTO에 저장값으로 만든 `taxLine`과, 번호 있는 문서면 지금 기준 재계산으로 만든 `taxDrift`(string | null)를 싣는다 — `taxDrift`는 저장 스냅숏 값(세율 · 세액 · 지급 총액)과 재계산 값의 비교로만 만들고, 스냅숏의 `tax_rate_setting_id`로 설정 이력 행을 다시 읽거나 있는지 확인하지 않는다(예정 세율이 취소돼 행이 없어도 한 줄 · 차이가 그대로 나온다 — Addresses review concern: B1(Round 2)). `app/(app)/expenses/actions.ts`에 `previewExpenseAction`(+ 등록).
④ **폼 · 문서 화면** `expense-form.tsx`: 칸 변경(증빙 종류 · 공급가액 · 통화 · 환율 · 지급 예정일 · 분할 지급) 뒤 짧은 지연으로 `previewExpenseAction`을 부르고, 오는 동안 이전 한 줄에 `--faint` 클래스, 응답으로 한 줄 · 막힘 · 칸 오류를 바꾼다(늦게 온 응답이 새 응답을 덮지 않게 요청 순번으로 거른다). 계산 한 줄 자리는 마지막 금액 칸 바로 아래(KRW = 공급가액 아래 · 외화 = 환율 아래 — B1). `expense-document.tsx`: 계산 한 줄 아래 `taxDrift`가 있으면 `--warning` 글자 한 줄(숫자 700, 배경 없음).
⑤ **테스트** behavior 단위 사례를 `test/unit/domain/expenses/tax.test.ts`(덧붙임) · `test/unit/domain/money.test.ts`(덧붙임)에, 통합을 새 파일 `test/integration/expense-tax-snapshot.test.ts`에, E2E를 새 스펙 `test/e2e/expense-form.spec.ts`(desktop)에 먼저 적고 실패를 본 뒤 구현한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/expenses/tax.test.ts test/unit/domain/money.test.ts test/unit/domain/money-tax.test.ts test/unit/settings</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 규칙 넷 · 면제 · 외화 · 계산 불가 문자열이 원문과 한 글자라도 다르거나, 사업소득이 8.8%로 계산되거나, 회사 대납 기본값 계산이 846,154가 아니거나, 기존 `applyTaxRule` 단위 사례가 깨지면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-tax-snapshot.test.ts test/integration/expense-approval-lifecycle.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 세율 스냅숏(세율 · 이력 행 id · 적용일)이 저장되지 않거나, 새 이력 세율 뒤 문서가 새 세율로 다시 보이거나(저장값이 아님), 세율 바뀜 줄이 같을 때도 나오거나 다를 때 안 나오거나, 제출 문서가 참조한 예정 세율의 취소가 실패(23503 등)하거나 취소 뒤 문서 조회가 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-form.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 하나라도 있거나 playwright failed가 1 이상이다 — 증빙 종류를 바꿔도 한 줄이 그대로이거나 이전 줄 `--faint`가 없으면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyTaxRule(" domain/expenses/tax.ts`가 1이다
    - `grep -cE "default: 0\.22" domain/settings/keys.ts`가 1 이상이고 `TAX_COMPANY_BORNE_METHOD` 정의의 default가 `gross_up`이다(실행자가 해당 두 줄을 SUMMARY에 인용)
    - `grep -rn "from \"@/domain/money" "app/(app)/expenses"`가 0건이다(화면은 서버 문자열만)
    - `grep -c "cancelHistorizedValue" test/integration/expense-tax-snapshot.test.ts`가 1 이상이다(예정 세율 취소 사례 — B1 Round 2)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>증빙 종류를 바꾸면 한 줄이 즉시 다시 오고, 제출된 문서는 저장된 세율로 보이며 세율이 바뀌면 차이가 경고로 선다 — 회사 대납은 22% gross-up이 기본이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 제출 막힘 규칙 `expense.submit` — ①~⑨ 순서 · 첫 이유 하나 · 다음 한 수 대상 · 1차 `aria-disabled` · 첫 포커스 · 제출 액션 재거부</name>
  <files>domain/rules/register.ts, domain/expenses/gate.ts, domain/expenses/index.ts, app/(app)/expenses/[id]/expense-form.tsx, test/unit/domain/expenses/submit-gate.test.ts, test/e2e/expense-form.spec.ts</files>
  <read_first>
    - `domain/rules/gate.ts`(전문 — `registerGateRule` · `gate` · `GateDecision`) · `domain/rules/register.ts`(기존 규칙 등록 모양 · `quote.customer-approval` · `quote.vendor-required` · 면제 상태 — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4))
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S6(445–451행) · Copywriting 264행(①~⑨ 원문)
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 401–407행(Pattern 8)
    - 05-03 · 05-04 산출물 `domain/expenses/gate.ts`(③④⑥⑦⑧) · `submitExpense`의 트랜잭션 안 재판정 자리
    - `test/unit/domain/rules-gate.test.ts`(게이트 단위 사례 모양)
  </read_first>
  <behavior>
    - (단위) 사실 묶음 하나에 막힘 조건을 여럿 켜면 첫 이유는 언제나 번호가 가장 작은 것이다 — ①+⑧ → ① · ③+⑤+⑦ → ③ · ⑥+⑦ → ⑥ · ⑦+⑧ → ⑦ · ⑧+⑨ → ⑧
    - (단위) ① 문자열: 보는 사람이 담당 PM이면 `2차 고객 승인 전 · 고객 승인 표시`, 아니면 `2차 고객 승인 전 · 담당 PM 박서연` — `quote.customer-approval` 규칙 결과와 글자 그대로 같다. 설정 끔 → ① 없음 · 수주중 · 미수주 → ① 없음
    - (단위) ⑥: 공급가액 · 증빙 종류 둘 다 비면 `공급가액 · 증빙 종류 2칸 비어 있음 · 공급가액 적기`, 증빙 종류만 비면 `증빙 종류 비어 있음 · 증빙 종류 고르기`, 팀 비용 문서는 `종류` · `내용`이 묶음에 든다
    - (단위) `nextActionTarget`이 같은 순서로 `customerApproval` · none · `quoteLine` · `openLatest` · `vendor` · 첫 빈 칸 id · `supplyAmount` · `evidence` · none을 돌려준다
    - (통합 — `test/integration/expense-submit-concurrency.test.ts`에 한 사례 덧붙임) 미리보기가 통과한 문서에서 트랜잭션 전에 고객 승인이 취소되면 제출이 ① 문자열로 거부된다
    - (E2E, desktop) 증빙 없는 폼: 1차에 `aria-disabled="true"` · `aria-describedby`가 가리키는 글자 `증빙 없음 · 증빙 올리기 Ctrl+U` · 첫 포커스가 첨부 영역 · 1차를 눌러도 제출 요청 0건이고 포커스가 첨부 영역 · 파일을 올리면 이유가 사라지고 1차 `aria-disabled` 없음
  </behavior>
  <action>
① `domain/rules/register.ts`에 규칙 `expense.submit`을 **덧붙인다** — `check(doc, ctx)`는 ①~⑨를 UI-SPEC 순서로 보고 첫 막힘의 `{ allowed: false, reason }`만 돌려준다. ①은 같은 파일의 `quote.customer-approval` 판정 함수를 부른 결과 문자열, ⑤는 `quote.vendor-required` 결과 문자열을 그대로 쓴다(문자열 복제 금지). 기존 규칙은 바꾸지 않는다.
② `domain/expenses/gate.ts`: 05-03 · 05-04의 `evaluateExpenseSubmit`를 규칙 문맥 조립 `buildExpenseSubmitContext(facts)`와 대상 계산 `nextActionTarget(facts)`로 나눈다(같은 순서 표 하나를 두 함수가 읽는다 — 순서가 두 곳에 적히지 않게). `previewExpense`와 `submitExpense` 트랜잭션 안 재판정이 모두 `gate(doc, "expense.submit", ctx)`를 부르게 바꾼다. 설정(`project.customer_approval_gate`)과 담당 PM 이름은 트랜잭션 전에 읽고, 트랜잭션 안에서는 tx로 읽은 차수 · 줄 · 문 · 증빙 수 · 금액만 바꿔 넣는다.
③ `expense-form.tsx`: 미리보기 응답의 첫 이유를 1차 왼쪽에 그대로(`reasonTone: "block"`), 1차 `aria-disabled` + `aria-describedby`, 비활성 1차 누름 = 서버 호출 없이 대상으로 포커스, 다음 한 수 3차(대상 칸 `적기`/`고르기` · 첨부 영역 `증빙 올리기` · ① 담당 PM이면 `고객 승인 표시` → 프로젝트 상세 링크 · ④ `지출결의 열기` → 가장 최근 제출 문서), 첫 포커스 = 대상(없으면 1차). 제출 액션이 게이트 오류를 돌려주면 같은 자리에 그 문자열. **1차 옆 이유는 05-05의 순수 함수 `submitBlockReason`을 거쳐 그린다** — 미리보기 응답의 첫 이유와 대상(`{ reason, target }`)을 그 함수의 `server`에 넘기고 결과를 그린다: 올리는 행이 있고 서버 이유가 없거나 ⑧(대상 `evidence`)이면 `증빙 올리는 중 · 잠시 뒤 제출`(`info`)이 대신 선다(Addresses review concern: D2(Round 3) — 파일 완료 전 서버 ⑧이 틀린 안내가 되지 않게. 05-05 단위 · E2E가 회귀 기준이고, 이 task의 E2E 검증 명령이 `expense-submit-mobile-approval.spec.ts`를 함께 돌린다).
④ behavior 단위를 새 파일 `test/unit/domain/expenses/submit-gate.test.ts`에, 재거부 통합 한 사례를 `test/integration/expense-submit-concurrency.test.ts`에, E2E를 `test/e2e/expense-form.spec.ts` describe `막힘 이유`에 먼저 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/expenses/submit-gate.test.ts test/unit/domain/rules-gate.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 여러 막힘에서 첫 이유 순서가 다르거나, ① · ⑤ 문자열이 기존 규칙 결과와 다르거나, 대상 순서가 이유 순서와 어긋나면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-submit-concurrency.test.ts test/integration/expense-approval-lifecycle.test.ts test/integration/evidence-upload.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 트랜잭션 전 통과 · 안에서 고객 승인 취소된 제출이 통과하거나 기존 지출결의 · 증빙 사례가 게이트 교체로 깨지면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-form.spec.ts test/e2e/expense-submit-mobile-approval.spec.ts'</automated>
    <fails_when>playwright failed가 1 이상이다 — 막힌 1차에 `aria-disabled`가 없거나 `disabled` 속성이 있거나, 이유 글자가 원문과 다르거나, 첫 포커스가 첨부 영역이 아니거나, 비활성 1차 누름이 요청을 보내면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "expense.submit" domain/rules/register.ts`가 1 이상이다
    - `domain/rules/register.ts` diff가 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-06.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/rules/register.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - `grep -c "고객 승인 전" domain/expenses/gate.ts`가 0이다(① 문자열은 기존 규칙에서 온다 — 복제 없음)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>제출 버튼이 서버 규칙 하나의 첫 이유와 다음 한 수로 막히고, 풀리는 순간 산다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 폼 입력 → 미리보기 | 저장 전 값으로 서버가 계산 · 판정한다 — 쓰기는 없다 |
| 미리보기 결과 → 제출 | 미리보기 통과가 제출 통과를 보장하지 않는다 |
| 설정(세율 · 기준일) → 저장된 문서 | 제출 뒤 설정이 바뀌어도 결재자가 보는 숫자는 기안자가 제출한 숫자다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-601 | Tampering | 미리보기만 통과시키고 제출 때 조건이 바뀜 | high | mitigate | 제출 트랜잭션 안에서 같은 `expense.submit` 규칙으로 tx 사실을 다시 판정 — 고객 승인 취소 재거부 통합 사례 |
| T-05-602 | Tampering | 제출 뒤 세율 변경으로 결재자가 다른 숫자에 승인 | medium | mitigate | 문서 · 시트는 저장된 스냅숏을 보이고 차이는 `--warning` 한 줄로만(막지 않음) — 세율 바뀜 통합 사례 · 예정 세율 취소 뒤에도 같다(값 스냅숏 — B1 Round 2 통합 사례) |
| T-05-603 | Information Disclosure | 미리보기 액션으로 남의 초안 계산 | medium | mitigate | `previewExpense` 첫 줄이 기안자 · 편집 가능 상태 판정(아니면 404) · 쓰기 없음 |
| T-05-604 | Denial of Service | 칸 변경마다 미리보기 호출 폭주 | low | mitigate | 클라이언트 지연 호출 · 요청 순번 거름 — 서버는 읽기 전용 · 트랜잭션 없음 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(`package.json` · lockfile diff 0 — verification) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과(세금 · 게이트 · 기존 `money-tax` · registry-coverage)
- 통합: `expense-tax-snapshot` · 지출결의 · 증빙 묶음 녹색
- `CI=true` E2E: `expense-form` · `expense-submit-mobile-approval` 녹색
- `. "$(git rev-parse --git-dir)/gsd-05-06.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml domain/money/tax.ts docs/design/tokens.css` 비어 있음(계산식 불변)
</verification>

<success_criteria>
- 공급가액만 적으면 규칙별 세금 한 줄이 서버에서 오고, 종류를 바꾸면 즉시 다시 온다(EXP-15)
- 제출된 문서는 세율 버전을 기억하고 차이를 보인다(기준 7)
- 제출 버튼은 이유 하나와 다음 한 수로 막힌다(UX-06)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `pickTaxDates`(D-101 전 사슬 · 코드표 우선) · `taxLineText` · `taxDriftText` · `formatRatePercent` · `previewExpense` · `buildExpenseSubmitContext` · `nextActionTarget` |
| 게이트 규칙 | `expense.submit`(①~⑨) |
| 액션 | `previewExpenseAction` |
| 설정 기본값 | `tax.company_borne.rate` 0.22 · `tax.company_borne.method` `gross_up` |
| 문구 | 계산 한 줄 규칙 넷 · 외화 · 계산 불가 · `세율 바뀜 · …` · 막힘 ①~⑨ |
| 테스트 | `test/unit/domain/expenses/submit-gate.test.ts` · `test/unit/domain/expenses/tax.test.ts`(추가) · `test/integration/expense-tax-snapshot.test.ts` · `test/e2e/expense-form.spec.ts` |
| 기록 | SUMMARY — 5만원 이하 면제는 Phase 11(CERT-04) · 06-03 `pickTaxDates` 부가세 대체 = 작성일(D-101)로 06을 맞출 것 |

<output>
Create `.planning/phases/05-expense-approval-leave/05-06-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| P3-4 — REQUIREMENTS 추적표의 ADMN-04 「세율 · 수식은 Phase 5」를 어떤 플랜도 `requirements`에 싣지 않음(L33@1108e20) | LOW | Plan 05-06 frontmatter `requirements`에 ADMN-04(세율 · 수식 부분만 — 주석) 추가. 이 플랜의 세율 · 회사 대납 · 기준일 설정 키(관리자 설정 화면에서 바꿈)와 세율 바뀜 표시가 그 부분의 이행이다 |
| F4 — read_first 코드 줄 범위(L22@1108e20) | MEDIUM | Plan 05-06 Task 1 · Task 2 read_first: `money/tax.ts` · `seed/index.ts` · `settings/keys.ts` · `rules/register.ts` 줄 범위를 이름 `grep -n` 방식으로 교체 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 해당하는 지적은 전부 반영했다 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B1 — 세율 스냅숏 FK ↔ 예정 세율 취소, 영향 05-06 세금 한 줄 · 통합 사례 `05-06-PLAN.md:143`(L26@69fb7ea) · 사용자 결정 E1: A(L18@69fb7ea) | HIGH | Plan 05-06 Task 1: must_haves truth 갱신(스냅숏 = 세율 · 이력 행 id · 적용일 값) + 새 truth(「세율 바뀜」 판정은 스냅숏 값 대 재계산 값 — 이력 행을 다시 읽지 않음). ③ `taxDrift`가 이력 행 조회 없이 값 비교만. behavior 기존 통합 사례를 새 열 이름으로 고치고 새 통합 사례 1(예정 세율 참조 문서 제출 → `cancelHistorizedValue` 성공 · 23503 없음 → 저장값 한 줄 + `세율 바뀜 · 부가세 12% → …`) — 취소는 실제 시계로 미래를 보므로 적용일 = 실제 오늘 + 30일, 기대 오른쪽 값은 예정 행 전 `computeExpenseTax` 결과. read_first에 `cancelHistorizedValue` · `deleteFutureHistorizedValue`. fails_when · acceptance grep · T-05-602 · artifact provides 갱신. 스키마 쪽은 Plan 05-03 Task 1 ① |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 해당하는 Round 2 지적은 B1뿐이고 반영했다. 새 문구 없음 — `세율 바뀜 · …` 꼴은 UI-SPEC 기존 문자열 그대로라 UI-SPEC 보강이 필요 없다 |

### Round 3 — efd6c67

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #87 문구 결정 정렬(코디네이터 16:30Z) — DECISIONS 2026-09-26 오류 문구 명사형 통일 | 사용자 결정 | Plan 05-06 must_haves(제출 재거부 줄): 미리보기 호출 실패 `제출하지 못했습니다 · 네트워크 · 다시 제출` → `제출 실패 · 네트워크 · 다시 제출`(UI-SPEC Copywriting 「Error — 제출 실패」 같은 글자) |
| D2 파급 — 올리는 중 제출의 막힘 이유(L32@efd6c67): 이 플랜이 미리보기 첫 이유를 1차 옆에 그대로 그리면 파일 완료 전 서버 ⑧ `증빙 없음`이 05-05의 클라이언트 이유를 덮는다 | MEDIUM | Plan 05-06 Task 2 ③ 한 문장: 1차 옆 이유는 05-05 `submitBlockReason`(서버 `{ reason, target }` + 올리는 행 수)을 거쳐 그린다 — 올리는 행이 있고 서버 이유가 없거나 ⑧이면 `증빙 올리는 중 · 잠시 뒤 제출`. 회귀 기준은 05-05 단위 · E2E(`expense-submit-mobile-approval.spec.ts` — Task 2 E2E 검증 명령이 이미 돌린다). UI-SPEC S6 「올리는 중」 반영 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Round 3 지적은 D2 파급 하나이고 반영했다. REVIEWS의 영향 목록(05-05)에는 없지만, 서버 막힘 표시 소유자가 이 플랜이라 한 줄 없이는 D2 수리가 이 플랜 실행에서 되돌려진다 |
