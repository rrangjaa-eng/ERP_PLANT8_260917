---
phase: 05-expense-approval-leave
plan: 07
type: execute
wave: 8
depends_on: ["05-06"]
files_modified:
  - domain/expenses/index.ts
  - domain/expenses/pick.ts
  - domain/expenses/gate.ts
  - repositories/expenses.ts
  - domain/document-numbering/index.ts
  - domain/settings/keys.ts
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - app/(app)/expenses/new/page.tsx
  - app/(app)/expenses/[id]/expense-form.tsx
  - app/(app)/expenses/[id]/expense-document.tsx
  - app/(app)/expenses/[id]/expense.module.css
  - ui/pick-dialog/PickDialog.tsx
  - ui/pick-dialog/PickDialog.module.css
  - test/integration/expense-team-attribution.test.ts
  - test/integration/expense-pick.test.ts
  - test/e2e/expense-new.spec.ts
autonomous: true
requirements: [EXP-08, EXP-02, UX-06]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "목록의 `새 지출결의`(→ `/expenses/new`)에서 견적 줄을 고르지 않으면 팀 비용 문서다 — 칸 넷(종류 Select `미수주 비용` · `팀 관리비` · 팀(읽기) · 사용일(기본 서울 오늘) · 내용(필수))이 서고, 종류는 알 수 있는 값이 없어 `—`로 연다(사용자의 유일한 결정). 문서는 첫 저장(임시 저장 · 첫 파일 올림 · 제출)에서 폼이 열릴 때 만든 idempotency key로 생기고 주소가 `/expenses/[id]`로 바뀐다(`history.replace`) — 두 번 눌러도 하나(EXP-08 · UI-SPEC S3 두 입구 · UIC empty S3 · partial S3 팀 비용)"
    - "팀 비용 문서의 귀속 팀은 기안자의 사용일 소속 팀(`teamAtDate(viewer, drafterId, usageDate)`)이고 저장 때 계산해 `attributed_team_id`로 고정된다 — 사용일을 바꾸면 그날 소속으로 다시 오고, 소속이 없으면 사용일 칸 오류 `사용일에 소속 팀 없음 · 사용일 고치기`. 제출 뒤 기안자가 다른 팀으로 발령돼도 그 문서의 귀속 팀은 그대로다(EXP-08 · 입력 §7 · ARCHITECTURE §4-3)"
    - "팀 비용 문서의 번호는 `T{연도 2자리}-{순번 4자리}`(예 `T26-0001`)이고 카운터 `expense_team` · period = 제출일(서울)의 연도다 — 서식 키 `document_number.expense_team.*` 다섯(사용자 결정 2026-09-26 #6)"
    - "팀 비용 문서의 제목은 `지출결의 — {팀} · {내용}`(내용 전 `지출결의 — {팀}`), 문서 화면 · 결재 시트의 `프로젝트` 값은 `프로젝트 미연결 · {종류}`(`--muted`)이고 견적 줄 · 분할 지급 대신 `팀` · `사용일` · `내용`이 보인다. 결재함 문서 칸은 `지출결의 · {팀} · {내용}`(UI-SPEC S7 · S9 · Copywriting)"
    - "팀 비용 문서의 제출 막힘은 같은 규칙 `expense.submit`이고 빈 칸 묶음 ⑥에 `종류` · `내용`이 든다. ①~④(견적 줄 전용)는 팀 비용에 적용되지 않는다"
    - "`/expenses/new`에서 견적 줄을 고르면 견적 줄 문서가 되고 팀 비용 칸 넷은 사라진다(적은 값은 버림) — 그 줄에 내 작성 중 문서가 이미 있으면 새로 만들지 않고 그 문서를 연다(UI-SPEC S3 · Assumptions #1). `expenses` 쓰기 권한이 없으면 `/expenses/new`는 404다"
    - "골라내기 `ui/pick-dialog`는 변형 넷(견적 줄 바꾸기 · 견적 줄 고르기 · 거래처 바꾸기 · 거래처 고르기)을 한 컴포넌트로 그린다 — 네이티브 `<dialog>` · `--scrim` · 포커스 트랩 · 닫히면 트리거로 복귀, 검색 한 칸이 첫 포커스, 목록 `role=\"listbox\"` · 행 `role=\"option\"` · 고른 행 `aria-selected=\"true\"`, 현재 줄 700 + 왼쪽 2px `--accent`, 결과 줄 + 1차 `이 줄로 Enter` / `이 거래처로 Enter` · 2차 `취소 Esc`(UI-SPEC S14 · B5 · UIC populated S14)"
    - "키보드: 검색 칸에서 `↓` → 목록 첫 행, 목록 안 `↑↓` 로빙, `Enter` = 1차(고른 행이 없거나 고를 수 없으면 무반응), `Esc` = 2차. 행 클릭 = 고름(1차는 따로). 고를 수 없는 행은 포커스는 되고 고르지 않는다(UIC interaction S14)"
    - "견적 줄 골라내기의 고를 수 없는 행은 `aria-disabled` + 2행 이유(`지출결의 {번호} 결재 중 · {금액}` · `거래처 없음`)이고, 분할 줄은 2행 `{거래처} · N회차 · 남은 실행가 {금액}`이다. 견적 줄 고르기의 기본 목록은 내가 담당 PM인 프로젝트(완료 · 미수주 제외)의 현재 차수 줄을 프로젝트 그룹 머리글 아래 보이고, 진행 · 현재 차수 고객 승인 전 프로젝트의 줄은 그 머리글 아래 한 줄 `2차 고객 승인 전`으로 접는다(수주중은 게이트 없음 — D-43). 행 금액은 실행가다(UIC partial S14 · zero-one-many S14 · UI Assumptions #6)"
    - "골라내기 빈 상태 · 오류 · 넘침: 검색 0건 `조건에 맞는 줄이 없습니다 · 검색 지우기` / `조건에 맞는 거래처가 없습니다 · 검색 지우기`, 고를 수 있는 줄 0 `고를 수 있는 줄이 없습니다`, 담당 프로젝트 없음 `담당 프로젝트 줄이 없습니다 · 검색으로 찾기`(3차 → 검색 칸 포커스), 50행 상한 + 끝 줄 `50건 넘음 · 검색으로 좁히기`(`--fs-sm --muted`), 조회 실패 `목록을 불러오지 못했습니다 · 다시 시도`(검색 칸 · `취소 Esc`는 산다). 검색은 서버 조회(짧은 지연)이고 오는 동안 이전 목록을 유지한다(UIC empty S14 · error S14 · overflow S14 · loading S14)"
    - "행 항목은 `keep-all` 줄바꿈 · 거래처 부제는 한 줄 말줄임 · 금액 `nowrap` · 프로젝트 그룹 머리글은 줄바꿈한다(UIC long-text S14)"
    - "폼의 자동 채움 칸 `바꾸기`(견적 줄 · 거래처)가 골라내기를 연다 — 견적 줄을 바꾸면 거래처 · 증빙 종류 · 통화 · 공급가액 · 분할 지급이 새 줄 값으로 다시 채워지고(결과 줄이 미리 말한다), 거래처를 바꾸면 증빙 종류가 그 거래처 기본값으로 바뀐다(기본값이 없으면 결과 줄 없음). 막힘 ③ `견적 줄 바꾸기` · ⑤ `거래처 고르기`의 다음 한 수가 같은 골라내기를 연다(UI-SPEC S3 · S6)"
    - "새 액션(첫 저장 · 줄 바꾸기 · 거래처 바꾸기 · 줄 검색 · 거래처 검색)이 `authedActionClient` + zod이고 등록되어 누수 스캔에 들어간다"
  prohibitions:
    - statement: "발령(조직 개편)이 이미 저장된 팀 비용 문서의 귀속 팀을 바꿔서는 안 된다 — 귀속은 사용일 소속으로 저장 때 고정된다"
      status: resolved
      verification: judgment
    - statement: "골라내기에서 고를 수 없는 줄(문 닫힘 · 거래처 없음 · 게이트 막힘)을 고를 수 있게 해서는 안 된다 — 서버가 행마다 판정해 보낸다"
      status: resolved
      verification: judgment
    - statement: "골라내기를 확인 모달(`ConfirmDialog`)에 검색 · 선택 prop을 더해 만들어서는 안 된다 — 두 번째 컴포넌트다(B5)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/(app)/expenses/new/page.tsx"
      provides: "새 지출결의 입구 — 견적 줄 `—` + `견적 줄 고르기` · 팀 비용 칸 넷 · 늦은 문서 생성"
    - path: "ui/pick-dialog/PickDialog.tsx"
      provides: "골라내기(S14) — 06 S10 연결 고르기가 같은 컴포넌트를 쓴다"
    - path: "domain/expenses/pick.ts"
      provides: "searchLinesForPick · searchVendorsForPick — 행마다 고를 수 있음 · 이유 · 그룹"
      exports: ["searchLinesForPick", "searchVendorsForPick"]
    - path: "test/integration/expense-team-attribution.test.ts"
      provides: "사용일 귀속 · 소속 없음 · 발령 뒤 불변 · T26 번호 · 첫 저장 idempotency"
      min_lines: 50
    - path: "test/e2e/expense-new.spec.ts"
      provides: "/expenses/new 팀 비용 제출 · 견적 줄 고르기 · 거래처 고르기 · 골라내기 키보드"
      min_lines: 60
  key_links:
    - from: "domain/expenses/index.ts"
      to: "domain/org/index.ts"
      via: "팀 비용 저장 때 teamAtDate(viewer, drafterId, usageDate)로 귀속 팀 고정"
      pattern: "teamAtDate"
    - from: "domain/expenses/index.ts"
      to: "domain/document-numbering/index.ts"
      via: "팀 비용 제출 마지막 쓰기 allocateDocumentNumber(counterKey expense_team, 연도)"
      pattern: "expense_team"
    - from: "app/(app)/expenses/[id]/expense-form.tsx"
      to: "ui/pick-dialog/PickDialog.tsx"
      via: "바꾸기 · 고르기 · 막힘 다음 한 수가 골라내기를 연다"
      pattern: "PickDialog"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**팀 비용 지출결의와 골라내기**: 프로젝트가 없는 비용(미수주 비용 · 팀 관리비)을 `/expenses/new`에서 팀 이름으로 올리고, 귀속 팀은 사용일 소속으로 저장 때 고정되며, 번호는 `T26-0001`로 매겨진다. 같은 화면에서 견적 줄 · 거래처를 목록에서 검색해 고르는 골라내기(`ui/pick-dialog`)를 세워 폼의 `바꾸기`와 막힘 다음 한 수를 잇는다.

Purpose: 기준 4(EXP-08)의 「프로젝트 없이 팀 이름으로」 · 「사용일 기준 소속 팀 귀속」이 여기서 성립하고(팀장 목록의 `프로젝트 미연결` 표시는 05-08), S14 골라내기로 폼에서 사용자가 값을 다시 적지 않는다(CLAUDE.md §7 「검색해서 고르기」).

Output: `/expenses/new` · 팀 비용 도메인(첫 저장 · 귀속 · 번호) · 줄 · 거래처 바꾸기 · 골라내기 컴포넌트와 검색 · 설정 키 5 · 통합 2 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-03-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-06-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-07.env"` · diff 검사 접두는 05-01과 같은 모양. 트랜잭션 규약(`teamAtDate` · `can()` · 설정 · 번호 서식은 트랜잭션 전) · `.planning/STATE.md` 불변 · 새 의존성 0 · 새 토큰 0 · Next 16 문서 먼저 읽기(동적 라우트 · `history.replace` 대신 `router.replace`).
- 화면 규칙: SYSTEM.md §6-3 · §7-8 · §7-17 + 골라내기 소절(05-02 B5) · UI-SPEC S3 · S14 · 실물 `docs/design/system/form-expense.html` `#pick`(`↵`는 `Enter`로 읽는다).
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 120k — 05-01 「웨이브 근거」의 규모 근거): Task 1 트레이서가 새 화면(`/expenses/new`)과 `ui/pick-dialog`를 세우고, Task 2는 같은 두 파일에 골라내기 갈래를 채운다 — 나누면 같은 화면 파일을 두 플랜이 다시 읽는다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — `/expenses/new`에서 팀 관리비 한 건: 종류 · 사용일(오늘) · 내용 → 거래처 고르기 → 공급가액 → 첫 저장(문서 생성 · 주소 교체) → 증빙 → 제출 `T26-0001` → 팀장 승인 → 문서 화면 `프로젝트 미연결 · 팀 관리비`</name>
  <files>domain/expenses/index.ts, domain/expenses/pick.ts, domain/expenses/gate.ts, repositories/expenses.ts, domain/document-numbering/index.ts, domain/settings/keys.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/new/page.tsx, app/(app)/expenses/[id]/expense-form.tsx, app/(app)/expenses/[id]/expense-document.tsx, app/(app)/expenses/[id]/expense.module.css, ui/pick-dialog/PickDialog.tsx, ui/pick-dialog/PickDialog.module.css, test/integration/expense-team-attribution.test.ts, test/e2e/expense-new.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S3(339–406행 — 팀 비용 그림 · 두 입구 · 제어 기본값 · 팀 비용 칸) · S14(610–642행) · Copywriting 257 · 259 · 268 · 272 · 274행
    - `domain/org/index.ts` 185–215행(`teamAtDate(viewer, userId, date, deps?)` → `TeamDto | null`)
    - `domain/document-numbering/index.ts`(전문 — `DOCUMENT_NUMBER_FORMAT_DEFS` 한 줄 추가 자리 · 05-03이 더한 함수) · `domain/settings/keys.ts`의 `document_number.project.*` 다섯 키 선언
    - 05-03 · 05-06 산출물 `domain/expenses/index.ts`(`submitExpense` · `saveExpenseDraft` · `previewExpense`) · `gate.ts`(규칙 문맥) · 05-05 폼
    - `ui/confirm-dialog/ConfirmDialog.tsx`(§7-17 `<dialog>` 틀 · 포커스 트랩 · 복귀 — 골라내기가 같은 골격을 쓴다) · `docs/design/SYSTEM.md` §7-8(폰 시트) · 골라내기 소절(05-02)
  </read_first>
  <behavior>
    - (통합) 기안자(기획1팀 소속)가 `createTeamExpenseDraft({ idempotencyKey: k, fields: { teamExpenseKind: "team_overhead", usageDate: "2026-09-26", content: "팀 회식" } })`를 두 번 → 같은 expenseId · 행 1 · `attributed_team_id` = 기획1팀
    - (통합) 사용일을 기획2팀 발령일 뒤로 바꿔 저장 → 귀속 = 기획2팀. 소속 없는 날짜 → `ExpenseFieldError { field: "usageDate", message: "사용일에 소속 팀 없음 · 사용일 고치기" }`
    - (통합) 거래처 · 공급가액 440,000 · 증빙을 채워 제출 → 번호 `T26-0001` · 프로젝트 행 잠금 없음(프로젝트 id null) · 제출 뒤 기안자를 다른 팀으로 발령해도 귀속 팀 불변 · 두 번째 팀 비용 제출 → `T26-0002` · 서울 날짜 `2027-01-01`(`deps.now` = 2026-12-31T15:30Z)의 제출 → `T27-0001`
    - (통합) 팀 비용 문서에서 종류 · 내용이 비면 ⑥ `종류 · 내용 2칸 비어 있음 · 종류 고르기`
    - (E2E, desktop) PM이 `/expenses/new` → 종류 Select 첫 값 `—` · 사용일 칸 값 = 오늘 · 팀 텍스트 = 소속 팀 → 종류 `팀 관리비` · 내용 `팀 회식` → `거래처 고르기` → 골라내기에서 거래처 검색 · `Enter` → 증빙 종류가 그 거래처 기본값 → 공급가액 `440,000` → `임시 저장` → 주소가 `/expenses/{uuid}` → 사진 올림 → `Ctrl+Enter` → 번호 `T26-` 접두 · 팀장 로그인 문서 화면 `승인` → 태그 `승인` · `프로젝트` 값 `프로젝트 미연결 · 팀 관리비`
  </behavior>
  <action>
① **팀 비용 도메인** `domain/expenses/index.ts`: `createTeamExpenseDraft(viewer, { idempotencyKey, fields }, deps?)` — `can(viewer, "expenses", "write")`, 트랜잭션 전에 `teamAtDate(viewer, viewer.userId, usageDate ?? seoulToday(deps.now))`(null이면 사용일 칸 오류), `idempotency_key` UNIQUE에 `ON CONFLICT DO NOTHING` + 재조회로 한 행. `saveExpenseDraft`가 팀 비용 칸(`teamExpenseKind` · `usageDate` · `content`)을 받고, 사용일이 바뀌면 같은 방식으로 `attributed_team_id`를 다시 계산해 함께 저장한다(제출 때는 다시 계산하지 않는다 — 고정). `submitExpense`: 견적 줄이 없는 문서면 프로젝트 잠금 없이 지출결의 행 잠금 → 게이트(⑤~⑨) → 스냅숏 → 결재 제출 → 마지막 쓰기 `allocateDocumentNumber(viewer, { counterKey: "expense_team", year: 서울 오늘의 연도, format }, tx)`(서식은 트랜잭션 전 `loadDocumentNumberFormat("expense_team")`). `domain/expenses/gate.ts` 문맥: 팀 비용이면 ①~④를 건너뛰고 ⑥ 묶음에 `종류` · `내용`. 문서 DTO에 팀 이름 · 사용일 · 내용 · 종류 라벨, 제목 규칙, 결재함 요약 문서 칸 `지출결의 · {팀} · {내용}`.
② **번호 키** `domain/settings/keys.ts` 끝에 `document_number.expense_team.prefix`(기본 `T`) · `.year_digits`(2) · `.seq_digits`(4) · `.separator`(`-`) · `.seq_start`(1) — `document_number.project.*`와 같은 선언 · 라벨. `domain/document-numbering/index.ts` `DOCUMENT_NUMBER_FORMAT_DEFS`에 `expense_team` 줄 하나(기존 줄 불변).
③ **줄 · 거래처 바꾸기와 검색(이 task는 거래처 고르기 경로까지)** 새 파일 `domain/expenses/pick.ts`: `searchVendorsForPick(viewer, { query })` → 숨김 아닌 거래처 최대 50 + `truncated` · 행 = 이름 + 2행 `기본 증빙 {종류}`(없으면 없음). `domain/expenses/index.ts`에 `changeExpenseVendor(viewer, { expenseId, vendorId, expectedVersion })` — 기안자 · 편집 가능 상태만, 증빙 종류 = 그 거래처 기본값(없으면 그대로). 액션 `createTeamExpenseDraftAction` · `changeExpenseVendorAction` · `searchVendorsForPickAction`(+ 등록).
④ **골라내기 컴포넌트(거래처 변형으로 트레이서)** 새 `ui/pick-dialog/PickDialog.tsx`(+ CSS 모듈, 기존 토큰만): props = 제목 · 부제 · 검색 함수(비동기 · 주입) · 행 렌더 규칙(번호 · 제목 · 2행 · 금액 · 고를 수 있음 · 이유) · 결과 줄 · 1차 라벨 · 고른 값 콜백. 골격은 `ConfirmDialog`와 같은 네이티브 `<dialog>` 틀(포커스 트랩 · `Esc` · 트리거 복귀 · `--scrim`)이고 확인 모달을 고치지 않는다. 검색 칸 첫 포커스 · 지연 서버 조회 · 이전 목록 유지 · listbox 키보드(must_haves) · <700 폰 시트(머리 · 행동 줄 고정 · 본문만 스크롤 · 행동 줄 `--touch-min` · 1차가 2차 두 배 폭 · 결과 줄은 행동 줄 위 전폭). 이 task는 거래처 변형이 동작하면 된다 — 견적 줄 변형 · 빈 상태 전부 · 오류는 Task 2.
⑤ **`/expenses/new` 화면** 새 `app/(app)/expenses/new/page.tsx`(RSC — `can(viewer, "expenses", "write")` 아니면 `notFound()`) → 같은 폼 컴포넌트를 새 문서 모드로: 첫 칸 `견적 줄 —` + 3차 `견적 줄 고르기`(Task 2가 연결) · 팀 비용 칸 넷(종류 Select `—` 시작 · 팀 읽기 텍스트 · 사용일 280 기본 오늘 · 내용 480) · 거래처 `—` + 3차 `거래처 고르기` · 나머지 칸은 05-05 폼 그대로(분할 지급 없음). 폼이 열릴 때 `crypto.randomUUID()`로 idempotency key를 만들고 첫 저장 · 첫 파일 올림 · 제출 직전에 `createTeamExpenseDraftAction`으로 문서를 만든 뒤 `router.replace("/expenses/{id}")`. 사용일을 바꾸면 팀 텍스트가 미리보기로 다시 온다. 문서 화면(`expense-document.tsx`)은 팀 비용이면 `프로젝트` 값 `프로젝트 미연결 · {종류}`(`--muted`) · `팀` · `사용일` · `내용` 행.
⑥ **테스트** 새 파일 `test/integration/expense-team-attribution.test.ts`(behavior 통합 넷) · 새 스펙 `test/e2e/expense-new.spec.ts`(behavior E2E)를 먼저 적는다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-team-attribution.test.ts test/integration/expense-approval-lifecycle.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 첫 저장 두 번이 행 둘을 만들거나, 귀속이 사용일 소속이 아니거나 발령 뒤 바뀌거나, 번호가 `T26-0001` · `T27-0001` 꼴이 아니거나, 팀 비용 제출이 프로젝트를 잠그려다 실패하거나, 누수 스캔이 새 액션을 못 잡으면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project unit test/unit/settings &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>린트 · 타입 · registry-coverage(새 번호 키 참조) · 빌드 중 하나라도 실패한다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-new.spec.ts'</automated>
    <fails_when>playwright failed가 1 이상이다 — 종류 첫 값이 `—`가 아니거나 사용일 기본이 오늘이 아니거나, 첫 저장 뒤 주소가 바뀌지 않거나, 거래처 고르기 뒤 증빙 종류가 기본값으로 바뀌지 않거나, 번호가 `T26-`로 시작하지 않거나, 문서 화면 `프로젝트` 값이 `프로젝트 미연결 · 팀 관리비`가 아니면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "teamAtDate" domain/expenses/index.ts`가 1 이상이고 `submitExpense` 트랜잭션 콜백 안에는 없다(실행자가 콜백 안 호출 표를 SUMMARY에 갱신)
    - `grep -c "expense_team" domain/document-numbering/index.ts`가 1 이상이다
    - `grep -c "ConfirmDialog" ui/pick-dialog/PickDialog.tsx`가 0이다(확인 모달에 기대지 않는다)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>프로젝트 없는 팀 비용이 새 지출결의 화면에서 사용일 소속 팀으로 올라가 `T26-0001`로 제출 · 승인된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 골라내기 완성 — 견적 줄 바꾸기 · 견적 줄 고르기(프로젝트 그룹 · 고객 승인 전 접기) · 고를 수 없는 줄 이유 · 빈 상태 셋 · 50행 상한 · 오류 · 막힘 다음 한 수 연결</name>
  <files>domain/expenses/pick.ts, domain/expenses/index.ts, repositories/expenses.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/[id]/expense-form.tsx, ui/pick-dialog/PickDialog.tsx, test/integration/expense-pick.test.ts, test/e2e/expense-new.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S14 표(629–638행) · Copywriting 251 · 257 · 258행
    - Task 1 산출물 `ui/pick-dialog/PickDialog.tsx` · `domain/expenses/pick.ts`
    - 05-03 `expenseLineDoor` · `listNumberedByLine` · 05-05 `listLineDoors`(표 전체 게이트 판정 재사용) · 05-06 `nextActionTarget` 대상 id(`quoteLine` · `vendor`)
    - `domain/projects/index.ts`의 담당 PM 필드(`pmUserId`)와 프로젝트 목록 리포지토리(쓰기 권리 범위)
  </read_first>
  <behavior>
    - (통합) `searchLinesForPick({ mode: "change", expenseId })` → 그 문서 프로젝트의 현재 차수 견적 줄 전부(최대 50) — 문 닫힌 줄은 `selectable: false` · 이유 `지출결의 26001-0001 결재 중 · 6,900,000`, 거래처 없는 줄은 `거래처 없음`, 분할 줄은 2행 `{거래처} · 2회차 · 남은 실행가 4,000,000`, 부제 `{프로젝트명} · 견적 줄 {N} · 고를 수 있는 줄 {M}`
    - (통합) `searchLinesForPick({ mode: "pick" })`(검색어 없음) → 내가 담당 PM인 진행 · 수주중 프로젝트만 그룹으로, 진행 · 2차 미승인(설정 켜짐) 프로젝트는 줄 없이 그룹 접힘 표식 `2차 고객 승인 전`, 완료 · 미수주 프로젝트 없음. 검색어 `LED`면 내가 쓰기 권리가 있는 모든 프로젝트로 넓힌다. 결과가 51행 이상이면 50행 + `truncated: true`
    - (통합) `changeExpenseLine({ expenseId, lineId, expectedVersion })` → 거래처 · 증빙 종류 · 통화 · 공급가액(남은 실행가) · 분할 여부가 새 줄 값, 팀 비용 칸 null. 그 줄에 내 다른 작성 중 문서가 있으면 바꾸지 않고 `{ redirectTo: 그 문서 id }`
    - (E2E, desktop) 견적 줄 문서 폼에서 `바꾸기` → 골라내기 `견적 줄 바꾸기` · 첫 포커스 검색 칸 · `↓` → 첫 행 `aria-selected="true"` · 문 닫힌 행에서 `Enter` → 아무 일 없음 · 열린 행 `Enter` → 폼의 거래처 · 공급가액이 그 줄 값 · 트리거로 포커스 복귀
    - (E2E) `/expenses/new`에서 담당 프로젝트가 없는 사용자가 `견적 줄 고르기` → `담당 프로젝트 줄이 없습니다 · 검색으로 찾기` → 3차 누르면 검색 칸 포커스 · 검색 0건 → `조건에 맞는 줄이 없습니다 · 검색 지우기` · 검색 조회 실패(요청 가로채기 500) → `목록을 불러오지 못했습니다 · 다시 시도`이고 검색 칸이 살아 있다
    - (E2E) 막힘 ③(작성 중에 새 차수가 생긴 문서)의 다음 한 수 `견적 줄 바꾸기` → 골라내기가 열린다
  </behavior>
  <action>
① `domain/expenses/pick.ts`에 `searchLinesForPick(viewer, { mode: "change" | "pick", expenseId?, query? })` — 행마다 `expenseLineDoor` · 표 전체 게이트(05-05 `listLineDoors`와 같은 판정 함수 재사용 — 두 번 쓰지 않는다)로 `selectable` · 이유 · 2행을 서버가 만든다. 기본 목록 = 담당 PM(`pmUserId` = viewer) · 상태 진행 · 수주중 프로젝트, 검색 = 쓰기 권리 범위 전체, 한 번에 50행 + `truncated`. 금액 = 실행가(외화 2행). `repositories/expenses.ts`에 필요한 조회 한두 개(줄 + 프로젝트 + 번호 있는 문서 상태를 한 쿼리로 — 행마다 따로 조회하지 않는다).
② `domain/expenses/index.ts`에 `changeExpenseLine(viewer, { expenseId, lineId, expectedVersion })` — 기안자 · 편집 가능 상태, 새 줄이 고를 수 있어야 함(서버 재판정), 부분 UNIQUE 충돌이면 `{ redirectTo }`, 성공이면 줄 값으로 다시 채우고 팀 비용 칸 null. `/expenses/new`(문서 없음)에서 고르면 05-05의 `createExpenseFromLinesAction([lineId])` → 이동(팀 값 버림).
③ `PickDialog` 견적 줄 변형 둘(부제 · 그룹 머리글 행 · 접힌 그룹 한 줄 · 고를 수 없는 행 `aria-disabled` + 2행 이유 `aria-describedby` · 결과 줄 `거래처 · 증빙 종류 · 공급가액이 그 줄 값으로 바뀜`(고르기에서 팀 칸 값이 있으면 `· 팀 비용 칸 지워짐`) · 빈 상태 셋 · 50건 끝 줄 · 오류 한 줄). 액션 `searchLinesForPickAction` · `changeExpenseLineAction`(+ 등록).
④ `expense-form.tsx`: 견적 줄 · 거래처 자동 채움 칸의 3차 `바꾸기`(폰은 값 줄 오른쪽 끝 · 부제는 아래 한 줄) · `/expenses/new`의 `견적 줄 고르기` · 막힘 ③ `견적 줄 바꾸기` · ⑤ `거래처 고르기` 다음 한 수를 골라내기에 연결한다(05-06이 보낸 대상 id 그대로).
⑤ behavior 통합을 새 파일 `test/integration/expense-pick.test.ts`에, E2E를 `test/e2e/expense-new.spec.ts` describe `골라내기`에 먼저 적는다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-pick.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 닫힌 줄 · 거래처 없는 줄이 고를 수 있게 나오거나, 이유 문자열이 다르거나, 담당 PM 기본 목록에 남의 프로젝트 · 완료 프로젝트가 섞이거나, 50행 상한이 없거나, 줄 바꾸기 충돌이 새 문서를 만들면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-new.spec.ts test/e2e/expense-form.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 골라내기 첫 포커스 · `aria-selected` · 고를 수 없는 행 `Enter` 무반응 · 포커스 복귀 · 빈 상태 셋 · 오류 한 줄 · 막힘 ③ 다음 한 수 중 하나라도 다르면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="listbox"' ui/pick-dialog/PickDialog.tsx`가 1 이상이고 `grep -c "aria-selected" ui/pick-dialog/PickDialog.tsx`가 1 이상이다
    - `searchLinesForPick`이 `expenseLineDoor`를 쓴다(`grep -c "expenseLineDoor" domain/expenses/pick.ts`가 1 이상) — 문 판정이 한 함수에만 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>폼의 견적 줄 · 거래처를 목록에서 검색해 고르고, 고를 수 없는 줄은 이유와 함께 막히며, 막힘의 다음 한 수가 같은 골라내기를 연다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 골라내기 검색 → 서버 | 검색어로 볼 수 없는 프로젝트 · 줄이 새면 안 된다 |
| 폼 → 줄 바꾸기 · 첫 저장 | 호출자가 보낸 줄 id · idempotency key · 사용일은 신뢰하지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-701 | Information Disclosure | `searchLinesForPick` 검색 범위 | high | mitigate | 검색은 쓰기 권리 범위 프로젝트만 · 행 필드는 `quote.amount` · `project.value` 노출 투영을 지난다 — 통합 사례(남의 프로젝트 없음) · 누수 스캔 |
| T-05-702 | Elevation of Privilege | `changeExpenseLine`로 닫힌 줄 · 남의 줄로 옮기기 | high | mitigate | 서버가 새 줄의 문 · 게이트 · 쓰기 권리를 다시 판정 — 화면의 고를 수 없음 표시와 무관 |
| T-05-703 | Tampering | 팀 귀속 조작(사용일 · 팀 id 직접 전송) | medium | mitigate | 액션 입력에 팀 id 칸 없음 — 서버가 `teamAtDate`로만 계산해 저장 · 제출 때 재계산 없음(발령 불변 사례) |
| T-05-704 | Tampering | 첫 저장 연타로 문서 둘 | low | mitigate | `idempotency_key` UNIQUE + ON CONFLICT 재조회 — 통합 사례 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(verification의 lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과(registry-coverage)
- 통합: `expense-team-attribution` · `expense-pick` · 지출결의 묶음 · 누수 스캔 녹색
- `CI=true` E2E: `expense-new` · `expense-form` 녹색
- `. "$(git rev-parse --git-dir)/gsd-05-07.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml docs/design/tokens.css ui/confirm-dialog` 비어 있음
</verification>

<success_criteria>
- 프로젝트 없는 비용을 팀 이름으로 올리고 사용일 소속 팀에 귀속된다(EXP-08)
- 폼의 값은 목록에서 골라 채우고 다시 적지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 화면 | `/expenses/new`(견적 줄 고르기 · 팀 비용 칸 넷 · 늦은 문서 생성) · 골라내기(S14 네 변형) |
| 컴포넌트 | `ui/pick-dialog/PickDialog.tsx` |
| 함수 | `createTeamExpenseDraft` · `changeExpenseLine` · `changeExpenseVendor` · `searchLinesForPick` · `searchVendorsForPick` |
| 액션 | `createTeamExpenseDraftAction` · `changeExpenseLineAction` · `changeExpenseVendorAction` · `searchLinesForPickAction` · `searchVendorsForPickAction` |
| 설정 키 | `document_number.expense_team.*` 5(기본 `T26-0001` 꼴) |
| 테스트 | `test/integration/expense-team-attribution.test.ts` · `test/integration/expense-pick.test.ts` · `test/e2e/expense-new.spec.ts` |

<output>
Create `.planning/phases/05-expense-approval-leave/05-07-SUMMARY.md` when done
</output>
