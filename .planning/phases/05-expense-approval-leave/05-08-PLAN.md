---
phase: 05-expense-approval-leave
plan: 08
type: execute
wave: 9
depends_on: ["05-07"]
files_modified:
  - domain/permissions/menus.ts
  - domain/seed/expenses.ts
  - domain/expenses/access.ts
  - domain/expenses/list.ts
  - domain/expenses/index.ts
  - repositories/expenses.ts
  - app/(app)/expenses/page.tsx
  - app/(app)/expenses/expenses-table.tsx
  - app/(app)/expenses/status-filter.tsx
  - app/(app)/expenses/expenses.module.css
  - app/(app)/expenses/loading.tsx
  - app/(app)/expenses/error.tsx
  - app/(app)/expenses/actions.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - test/unit/domain/expenses/list-groups.test.ts
  - test/integration/expense-visibility.test.ts
  - test/integration/expense-money.test.ts
  - test/integration/seed-permissions.test.ts
  - test/e2e/expense-list.spec.ts
autonomous: true
requirements: [EXP-08, EXP-01, UX-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "팀장이 `/expenses`를 열면 자기 팀원이 올린 프로젝트 미연결 팀 비용 문서가 「프로젝트 · 항목」 칸 2행 `프로젝트 미연결 · {종류}`(`--fs-sm --muted`, 폰은 접힌 줄)로 보인다(EXP-08 「팀장 화면에 '프로젝트 미연결'」 · 기준 4 · UIC partial S8 프로젝트 미연결)"
    - "지출결의가 보이는 사람 = 기안자 ∪ 결재 관련자(04.1 규칙 — 지금 단계 후보 · 처리한 사람) ∪ (새 메뉴 `expenses.team` 보기 권한 ∧ 문서의 팀 = 내 지금 팀 — 팀 비용은 귀속 팀, 견적 줄 문서는 프로젝트 팀) ∪ (계급 업무 범위 `company` ∧ `expenses` 보기 권한). 작성 중 문서는 기안자에게만 보인다. 그 밖은 없는 문서(404) — 목록 · 문서 화면 · 증빙 목록 · 서명 GET이 같은 판정 하나를 쓴다(사용자 결정 2026-09-26 #5 · D-17 · UIC error S7 권한 밖)"
    - "`expenses.team`(라벨 `팀 지출결의 보기`)은 메뉴 레지스트리에 있고 팀장 계급에 보기가 없을 때만 시드된다 — 관리자가 권한표에서 끄면 그 팀장은 자기 문서와 결재 관련 문서만 본다. 코드에 역할 이름을 박지 않는다(사용자 결정 2026-09-26 #5)"
    - "목록은 필터 줄의 상태 select 하나(`진행 중` 기본 = 작성 중 · 반려 · 회수 · 결재 중 / `승인` / `전체`, URL `?status=`)와 오른쪽 1차 `새 지출결의`(→ `/expenses/new`, `expenses` 쓰기 권한이 있고 지금 보기가 0건이 아닐 때만)를 가진다. 월 · 팀 · 미연결 필터는 없다(UI-SPEC S8 · Assumptions #11)"
    - "그룹 머리글: `진행 중` = `작성 중` → `반려 · 회수` → `결재 중`, `승인` = 지급 예정일 기준 `예정일 지남`(`--warning`) → `이번 주 지급` → `다음 주` → `그 뒤` → `지급 예정일 없음`, `전체` = 진행 중 그룹 셋 → `승인`. 그룹 안 정렬: 작성 중 = 고친 시각 내림차순 · 반려 · 회수 = 날짜 내림차순 · 결재 중 = 제출 시각 오름차순 · 승인 = 지급 예정일 오름차순. 비는 그룹은 머리글째 없고, 쪽을 넘는 그룹은 다음 쪽 맨 위에 머리글을 다시 보인다 · 그룹이 하나뿐이어도 머리글을 그린다(UIC populated S8 · zero-one-many S8)"
    - "그룹 둘 이상이 쪽 경계를 넘어도 쪽을 차례로 이어 붙인 행 순서가 한 번에 정렬한 순서와 같다 — 그룹 순서 · 그룹 안 정렬은 리포지토리 목록 쿼리의 SQL `ORDER BY group_rank, <그룹별 CASE 키>, id` 한 곳이 정하고 쪽 나눔(50건)은 그 뒤에만 건다(리뷰 Round 2 M2)"
    - "열: 번호(P3, 작성 중 `—`) · 프로젝트 · 항목(P1, `<a>` → `/expenses/[id]`, 분할이면 끝에 ` N회차`) · 거래처(P2, 한 줄 말줄임) · 금액(P1, 원화 + 외화 2행 `USD 4,200.00 @1,318.4`) · 지급 예정(P2 `09-19`) · 기안(P3 — 보는 사람의 범위가 자기 문서뿐이면 서버가 열을 보내지 않는다) · 상태(P1 `text` — `작성 중` / `{단계} 결재 중` / `반려 09-16` / `회수 09-17` / `승인 09-18`). 금액을 볼 수 없는 계급에는 금액 열 · 합계 줄을 보내지 않는다. 행 안에 행동 버튼은 없다(UI-SPEC S8 · UIC partial S8 외화)"
    - "표 위 합계 줄 `합계 ({보기} · N건)` + 공급가액 원화 합(`--fs-lg` 700)은 페이지와 무관하게 필터 전체 기준이고 외화는 원화로만 더한다 — 합은 `domain/money.sumKrw`(UI-SPEC S8 · §6-1 ③)"
    - "좁은 폭 열 접기: 1024~1279 번호 · 기안 숨김 / 700~1023 P1 + P2 / <700 P1 + 접힌 줄(`거래처 · 지급 예정`, 외화 2행 · `프로젝트 미연결 · {종류}`도 접힌 줄). 50건 페이지(`ui/pagination` 링크 갈래, 한 쪽이면 페이지 줄 없음)(UIC overflow S8)"
    - "빈 상태: `진행 중` 0건 `진행 중인 지출결의가 없습니다 · 새 지출결의` · `승인` 0건 `승인된 지출결의가 없습니다 · 진행 중 보기` · `전체` 0건 `지출결의가 없습니다 · 새 지출결의`, 쓰기 권한이 없으면 앞 문장만. 로딩은 `loading.tsx`(PageHeader + 합계 줄 라벨 · 금액 자리 막대 없음 + 머리글 뼈대 + `--surface` 행 3개, 300ms 지연, 반짝임 · 진행 바 없음), 로드 오류는 `지출결의 불러오기 실패 · 다시 시도`(2차 · `retry`)(UIC empty S8 · loading S8 · error S8)"
    - "외화 지출결의는 저장 · 목록에 원화 환산액과 원래 통화가 함께 보인다 — USD 4,200.00 @1,318.4 → 원화 5,537,280(기준 1 「결재 대기함 · 목록에는 원화 환산액과 원래 통화」의 목록 부분)"
    - "견적 줄 표에서 범위 선택이 여러 줄일 때 `Ctrl+E`는 고른 줄 중 문이 열린 줄마다 작성 중 문서를 만들고(이미 있으면 그 문서) 지출결의 목록 `진행 중` 보기의 `작성 중` 그룹으로 이동하며 토스트 `지출결의 올리기 · 작성 중 N건`(막힌 줄이 있으면 `· 막힘 M줄`)이 뜬다. 고른 줄이 전부 닫혔거나 막혔으면 이동하지 않고 합계 행 오른쪽에 첫 막힌 줄의 이유(셀과 같은 문자열) — 표 전체 게이트가 막혀 있으면 표 위 한 줄이 이미 말하므로 다시 쓰지 않는다(UI-SPEC 확정 #5 · S1 · UIC zero-one-many S1 여러 줄 · 0줄)"
    - { statement: "같은 줄에서 `지출결의 올리기`를 두 번 · `Ctrl+E`를 두 번 눌러도 지출결의 목록 `작성 중` 그룹의 그 줄 행이 1개다(`test/e2e/expense-list.spec.ts`, `CI=true`)(UIC e2e S1 idempotency · EXP-01)", verification: backstop }
  prohibitions:
    - statement: "작성 중 문서가 기안자 말고 다른 사람의 목록 · 화면에 보여서는 안 된다"
      status: resolved
      verification: judgment
    - statement: "역할 이름(팀장 · PM)을 코드 조건으로 박아 목록 범위를 가르면 안 된다 — 메뉴 권한 `expenses.team`과 계급 업무 범위로만"
      status: resolved
      verification: judgment
    - statement: "목록 한 쪽을 그리려고 행마다 결재 상태 · 단계를 따로 조회해서는 안 된다(N+1) — 한 쪽 분을 한 번에"
      status: resolved
      verification: judgment
    - statement: "쪽을 자른 뒤 JS에서 그룹으로 다시 묶어 순서를 바꾸거나 다시 정렬해서는 안 된다 — 쪽마다 순서가 틀린다. 정렬은 SQL `ORDER BY group_rank, <그룹별 CASE 키>, id` 한 곳(리뷰 Round 2 M2)"
      status: resolved
      verification: judgment
    - statement: "결재 관련자 보임 범위를 「처리한 인스턴스 id를 모은 IN 목록」으로 넘겨서는 안 된다 — 대표 · 결재자에게 끝없이 커진다. 처리 기록은 결재 단계 표 EXISTS 서브쿼리로, 지금 단계 후보만 진행 중 인스턴스의 한정 목록으로(리뷰 Round 2 P3-6)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/expenses/list.ts"
      provides: "listExpenses(viewer, {status, page}) — 보임 범위 · 그룹 · 정렬 · 합계 · 열 여부"
      exports: ["listExpenses"]
    - path: "domain/expenses/access.ts"
      provides: "canSeeExpense 완성 — 기안자 · 결재 관련자 · expenses.team · 전사 범위"
      exports: ["canSeeExpense"]
    - path: "app/(app)/expenses/page.tsx"
      provides: "지출결의 목록(S8)"
    - path: "test/integration/expense-visibility.test.ts"
      provides: "404 · 작성 중 기안자만 · 팀장 팀 · 전사 · 결재 관련자 · 서명 GET 같은 판정"
      min_lines: 60
    - path: "test/e2e/expense-list.spec.ts"
      provides: "팀장 프로젝트 미연결 · 그룹 · 합계 · 빈 상태 · 여러 줄 Ctrl+E · idempotency"
      min_lines: 60
  key_links:
    - from: "domain/expenses/list.ts"
      to: "domain/expenses/access.ts"
      via: "목록 행 필터와 문서 404가 같은 보임 규칙"
      pattern: "canSeeExpense|visibleExpenseScope"
    - from: "domain/expenses/access.ts"
      to: "domain/permissions/menus.ts"
      via: "can(viewer, expenses.team, view) — 역할 이름 없음"
      pattern: "expenses.team"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/expenses/page.tsx"
      via: "여러 줄 Ctrl+E 뒤 /expenses?status=진행 중 이동 + 토스트"
      pattern: "/expenses"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**지출결의 목록과 보임 범위**: `/expenses`의 빈 자리를 원장(SYSTEM §6-1)으로 채우고, 누가 어떤 지출결의를 보는지를 메뉴 권한 `expenses.team`과 계급 업무 범위로 정한다(사용자 결정 2026-09-26 #5). 팀장은 팀원의 프로젝트 미연결 비용을 목록에서 본다. 견적 줄 표의 여러 줄 `Ctrl+E`가 목록 `작성 중` 그룹으로 이어진다(확정 #5).

Purpose: 기준 4의 「팀장 화면에 '프로젝트 미연결'」과 목록 범위(D-17 404 규칙)가 여기서 성립하고, 기준 1의 「목록에는 원화 환산액과 원래 통화」가 목록에 선다.

Output: 메뉴 키 1 · 시드 · 보임 판정 완성 · 목록 도메인 · 목록 화면(필터 · 그룹 · 합계 · 페이지 · 열 접기 · 빈 · 로딩 · 오류) · 여러 줄 `Ctrl+E` · 통합 2 · 단위 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-07-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-08.env"` · diff 검사 접두는 05-01과 같은 모양. `.planning/STATE.md` 불변 · 새 의존성 0 · 새 토큰 0 · 「이번 주」 계산은 `lib/dates.ts`의 서울 날짜 · 금액 합은 `domain/money`.
- `domain/permissions/menus.ts`의 `MENUS` 배열에는 **덧붙이기만** 한다(Phase 4 선례 — `projects.period` 등). 시스템 관리자는 기존 시드 루프가 모든 메뉴를 받는다.
- 화면 규칙: SYSTEM.md §6-1 · §7-3(칸 접기) · §7-7 · §7-16 · UI-SPEC S8. 셸(`role-menu.ts`)은 고치지 않는다 — 「지출결의」 메뉴는 이미 있다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 120k — 05-01 「웨이브 근거」의 규모 근거): 무게는 Task 1 트레이서(메뉴 키 · 시드 · 보기 범위)에 몰려 있고, Task 2 목록 완성 · Task 3 여러 줄 `Ctrl+E`는 Task 1 목록 화면 · 05-15 배선 위의 덧붙임이다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 팀장이 `/expenses`에서 팀원의 팀 관리비 문서를 `프로젝트 미연결 · 팀 관리비`로 보고, 다른 팀 팀장 · 무관한 PM은 못 본다 (메뉴 키 · 시드 · 보임 판정 · 목록 도메인 · 목록 화면)</name>
  <files>domain/permissions/menus.ts, domain/seed/expenses.ts, domain/expenses/access.ts, domain/expenses/list.ts, domain/expenses/index.ts, repositories/expenses.ts, app/(app)/expenses/page.tsx, app/(app)/expenses/expenses-table.tsx, app/(app)/expenses/status-filter.tsx, app/(app)/expenses/expenses.module.css, test/integration/expense-visibility.test.ts, test/integration/seed-permissions.test.ts, test/e2e/expense-list.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 605–608행(Open Question 2 — 권고) · `domain/permissions/scope-for.ts`(전문 — `rows: all | none`) · `domain/permissions/roles.ts`(계급 업무 범위 `workScope` — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4))
    - `domain/permissions/menus.ts`(전문 — 덧붙임 자리 · 세분 메뉴 선례) · `domain/seed/index.ts`(권한 시드 · `insertPermissionIfAbsent` — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4)) · 05-03 `domain/seed/expenses.ts`
    - 05-03 · 05-07 산출물 `domain/expenses/access.ts` · `index.ts` · `repositories/expenses.ts`
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S8(487–536행) · Copywriting 245 · 252–254 · 272–273행
    - `app/(app)/projects/page.tsx` · `projects-table.tsx` · `filter-bar.tsx`(원장 목록 · 필터 · 합계 · 페이지 선례) · `ui/table/Table.tsx`(그룹 머리글 · 칸 접기) · `ui/pagination/Pagination.tsx`
    - 04.1 결재 표시 계산(`name_map` — 결재함이 인스턴스 여럿의 지금 단계를 한 번에 계산하는 함수)
  </read_first>
  <behavior>
    - (통합) 픽스처: 기획1팀(PM 박서연 · 팀장 김도윤) · 기획2팀(PM 이지훈 · 팀장 정하나) · 대표 · 본부 책임자. 박서연의 팀 관리비 문서 제출(귀속 기획1팀) · 박서연의 작성 중 문서 하나 · 이지훈의 견적 줄 문서 제출(프로젝트 팀 기획2팀)
    - (통합) `listExpenses(김도윤, { status: "open" })` 행: 박서연 제출 문서 1(작성 중 없음) · 2행 표식 `프로젝트 미연결 · 팀 관리비` · `drafterColumn` 참. `정하나`: 이지훈 문서만. `이지훈`(PM): 자기 문서만 · `drafterColumn` 거짓. 대표 · 본부 책임자(업무 범위 company): 제출 문서 둘 다(작성 중 제외)
    - (통합) `getExpense` · `listEvidence` · `createEvidenceViewUrl`: 박서연 문서에 대해 김도윤 · 대표 → 보임, 정하나 · 이지훈 → null. 박서연의 작성 중 문서는 박서연 말고 전원 null. 결재 1단 담당이 다른 팀 사람이 되도록 결재선 설정을 바꾼 문서는 그 담당에게 `expenses.team` 없이도 보임(결재 관련자)
    - (통합) 관리자가 권한표에서 팀장 계급의 `expenses.team` 보기를 끄면 김도윤의 목록에서 박서연 문서가 사라진다(결재 관련자가 아닐 때)
    - (통합, 결재 관련 두 갈래 — P3-6) 결재선 설정으로 1단 담당이 된 다른 팀 사람(`expenses.team` 없음 · 업무 범위 company 아님)에게 그 문서는 승인 전에는 「지금 단계 후보」 갈래로, 승인해 지금 후보가 아니게 된 뒤에는 「처리 기록」 갈래(EXISTS)로 `listExpenses` · `getExpense` 둘 다에 보인다. 그 사람이 처리하지 않은 같은 팀의 다른 제출 문서는 보이지 않는다
    - (통합) 시드: 팀장 계급에 `expenses.team` 보기가 있고, 이미 끈 값은 재시드가 되살리지 않는다(`insertPermissionIfAbsent`)
    - (E2E, desktop) 김도윤 로그인 → `/expenses` → 박서연 행의 2행 글자 `프로젝트 미연결 · 팀 관리비` · 기안 열 `박서연` · 상태 `{단계} 결재 중` 또는 `결재 중` 꼴(04.1 낱말)
  </behavior>
  <action>
① `domain/permissions/menus.ts` `MENUS` 끝(`expenses` 뒤 자리 — 순서가 권한표 열 순서면 `expenses` 바로 뒤)에 `{ key: "expenses.team", label: "팀 지출결의 보기" }` 한 줄. `domain/seed/expenses.ts`에 `TEAM_LEAD_ROLE_ID` × `expenses.team` view를 `insertPermissionIfAbsent`(관리자가 바꾼 값을 되살리지 않는다).
② `domain/expenses/access.ts` `canSeeExpense(viewer, row, deps?)` 완성 — must_haves 판정식 그대로. 「내 지금 팀」은 `teamAtDate(viewer, viewer.userId, seoulToday())`(호출자 입구에서 한 번 — `deps.today` 주입), 「문서의 팀」은 팀 비용 = `attributed_team_id` · 견적 줄 문서 = 프로젝트 `team_id`. 목록용으로 같은 판정을 SQL 조건으로 옮긴 `visibleExpenseScope(viewer, deps)` → `{ drafterId, teamIds, company, actedByUserId, currentHolderInstanceIds }`를 같은 파일에 두고 두 함수가 같은 입력(권한 · 업무 범위 · 결재 관련)에서 나온다. 결재 관련은 두 갈래로 나눈다(Addresses review concern: P3-6(Round 2) — 처리한 문서가 쌓일수록 커지는 인스턴스 id 목록을 만들지 않는다): 「처리한 사람」은 리포지토리 조건의 결재 단계 표 EXISTS 서브쿼리(그 지출결의 결재 인스턴스의 단계 행 중 `acted_by` = viewer — 차수 표를 거쳐 인스턴스로 잇고, 04.1 `approval_steps(acted_by)` 인덱스를 탄다), 「지금 단계 후보」는 04.1 결재함 계산이 내는 진행 중(`submitted` · `in_review`) 인스턴스 id만의 목록(내 차례 문서 수로 한정). `canSeeExpense`도 같은 두 갈래(그 인스턴스에 내 처리 기록이 있음 · 04.1 지금 후보 판정)를 쓴다.
③ `domain/expenses/list.ts` `listExpenses(viewer, { status, page })` → `{ groups: [{ label, tone?, rows }], total: { count, sumKrw } | null, drafterColumn, amountColumn, page }` — 리포지토리 한 쿼리(범위 조건 · 상태 파생 조인 · 50건 · 필터 전체 합계는 같은 조건의 집계 한 번). 결재 중 행의 `{단계} 결재 중`은 한 쪽 분의 인스턴스를 04.1 표시 계산에 한 번에 넘긴다(행마다 조회 금지). 행 DTO는 등록 DTO로 투영(금액 필드 `expense.amount` — 못 보면 금액 열 · 합계 없음). 그룹 · 정렬 계약은 Task 2 ①이 전부 굳히되, 이 task도 정렬을 처음부터 SQL `ORDER BY group_rank, <그룹별 CASE 키>, id`로 시작하고 50건 쪽은 그 뒤에만 자른다(쪽을 자른 뒤 JS에서 묶거나 다시 줄 세우지 않는다 — Addresses review concern: M2(Round 2)) — 이 task는 `진행 중` 보기 그룹 셋의 CASE까지.
④ `app/(app)/expenses/page.tsx`를 원장으로 바꾼다 — `PageHeader` 제목만(기존 부제와 자리 표시 ListEmpty를 지운다), `status-filter.tsx`(상태 select · URL 파라미터), 1차 `새 지출결의`, 합계 줄, `expenses-table.tsx`(`ui/table` 읽기 표 · 그룹 머리글 · 행 링크). 쓰기 · 보기 권한 없는 사람은 기존 게이트 규칙대로(`can(viewer, "expenses", "view")` 없으면 `notFound()`).
⑤ behavior 통합을 새 파일 `test/integration/expense-visibility.test.ts`와 기존 `test/integration/seed-permissions.test.ts`(한 사례 덧붙임)에, E2E를 새 스펙 `test/e2e/expense-list.spec.ts`에 먼저 적는다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-visibility.test.ts test/integration/seed-permissions.test.ts test/integration/evidence-upload.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 다른 팀 팀장 · 무관한 PM이 문서 · 증빙 · 서명 GET을 보거나, 작성 중 문서가 기안자 밖에 보이거나, 결재 관련자가 못 보거나, 권한표에서 끈 `expenses.team`이 여전히 범위를 넓히거나, 재시드가 끈 값을 되살리면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-list.spec.ts test/e2e/permissions-grid.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 팀장 목록에 `프로젝트 미연결 · 팀 관리비` 2행이 없거나, 새 메뉴 키가 권한표 격자를 깨면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"expenses.team"' domain/permissions/menus.ts`가 1이고 `domain/permissions/menus.ts` diff가 덧붙임 한 줄뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-08.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/permissions/menus.ts) && printf '%s\n' "$D" | grep -c '^+[^+]'`이 1을 출력)
    - `grep -cE "팀장|기획 PM|role-team-lead|role-pm" domain/expenses/access.ts domain/expenses/list.ts`가 0이다(역할 이름 조건 없음)
    - `grep -cE "acted_?[bB]y" repositories/expenses.ts`가 1 이상이다(처리 기록 갈래가 리포지토리 SQL 조건 — EXISTS 서브쿼리, P3-6(Round 2))이고 `grep -c "currentHolderInstanceIds" domain/expenses/access.ts`가 1 이상이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>팀장은 팀원의 프로젝트 미연결 비용을 목록에서 보고, 볼 수 없는 사람에게 지출결의는 없는 문서다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 목록 완성 — 보기 셋의 그룹 · 정렬 · 합계 · 50건 페이지 · 열 접기 · 빈 상태 셋 · 로딩 · 오류 · 외화 2행</name>
  <files>domain/expenses/list.ts, repositories/expenses.ts, app/(app)/expenses/page.tsx, app/(app)/expenses/expenses-table.tsx, app/(app)/expenses/expenses.module.css, app/(app)/expenses/loading.tsx, app/(app)/expenses/error.tsx, test/unit/domain/expenses/list-groups.test.ts, test/integration/expense-money.test.ts, test/e2e/expense-list.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S8 · UI Considerations S8 행(701 · 708 · 714 · 722 · 729 · 730 · 736 · 784행)
    - Task 1 산출물 `domain/expenses/list.ts` · `expenses-table.tsx`
    - `lib/dates.ts`(04.1 `seoulToday` — 주 계산은 서울 날짜 문자열로) · `app/(app)/projects/loading.tsx` · `error.tsx` · 04.1 `app/(app)/approvals/loading.module.css`
    - `test/e2e/mobile-320-no-overflow.spec.ts`(칸 접기 측정 모양)
  </read_first>
  <behavior>
    - (단위) `groupExpenses(rows, { status: "approved", todayKst: "2026-09-26" })`(토요일, 행은 SQL 순서 = 지급 예정일 오름차순 · 없음 끝): 지급 예정일 `09-25` → `예정일 지남`(tone warning) · `09-27`(일) → `이번 주 지급` · `09-28`(월) → `다음 주` · `10-06` → `그 뒤` · 없음 → `지급 예정일 없음` — 주는 월요일 시작(서울). 비는 그룹 없음
    - (단위) `groupExpenses`는 받은 행 순서를 바꾸지 않는다 — 연속한 같은 그룹을 머리글 하나로 묶기만 한다(입력 순서 = 출력 행 순서 단언). 쪽의 첫 행이 앞 쪽 그룹의 이어짐이어도 첫 머리글을 그 그룹으로 다시 그린다. `진행 중` 머리글 라벨 `작성 중` · `반려 · 회수` · `결재 중` · `전체`에서 승인 행은 `승인` 한 그룹(그룹 순서 · 그룹 안 정렬은 SQL — 아래 통합)
    - (통합, 쪽 경계 × 그룹 — M2) `진행 중` 보기에 작성 중 3 · 반려 1 · 회수 1 · 결재 중 4를 두되 시각을 엇갈리게 한다(결재 중 문서의 고친 시각이 작성 중보다 늦고, 결재 중의 제출 순서와 만든 순서가 반대). 쪽 크기 4(`deps.pageSize`)로 1 · 2 · 3쪽을 읽으면 1쪽 = 작성 중 3(고친 시각 내림차순) + 반려 · 회수 1 · 2쪽 = 반려 · 회수 1(머리글 다시) + 결재 중 3(제출 시각 오름차순) · 3쪽 = 결재 중 1(머리글 다시) — 두 그룹이 쪽 경계를 넘고, 세 쪽을 이어 붙인 id 순서가 한 쪽에 전부 담은 결과(쪽 크기 50)의 id 순서와 같다
    - (통합) USD 문서(USD 4,200.00 · 환율 1,318.4) 저장 원화 5,537,280 · 목록 행 금액 DTO = 원화 5,537,280 + 외화 `USD 4,200.00 @1,318.4` · 합계 줄 = 원화 합만
    - (통합) 51건 → 1쪽 50행 · 2쪽 1행 · 합계 건수 51 · 쪽 경계에서 그룹이 이어지면 2쪽 첫 행 앞에 같은 그룹 머리글
    - (E2E, desktop) `?status=승인` 0건 → `승인된 지출결의가 없습니다 · 진행 중 보기` · 3차 → `?status=진행 중` · 쓰기 권한 없는 계급 → `진행 중인 지출결의가 없습니다`만(3차 없음) · 1차 `새 지출결의`가 0건 보기에서 없음 · 합계 줄 `합계 (진행 중 · N건)`
    - (E2E, desktop) 폭 1100에서 번호 · 기안 열 없음, 폭 800에서 P1 + P2만, 가로 스크롤 0(`scrollWidth ≤ clientWidth`)
  </behavior>
  <action>
① **정렬은 SQL 한 곳(Addresses review concern: M2(Round 2))** — 리포지토리 목록 쿼리가 `ORDER BY group_rank, <그룹별 CASE 키>, id` 계약을 갖는다: `group_rank`는 상태 파생(작성 중 1 · 반려 · 회수 2 · 결재 중 3 · 승인 4, 보기에 없는 rank는 WHERE가 거른다), 그룹별 CASE 키는 must_haves 원문(`CASE WHEN group_rank = 1 THEN 고친 시각 END DESC` · rank 2 반려 · 회수 날짜 DESC · rank 3 제출 시각 ASC · rank 4 지급 예정일 ASC NULLS LAST), 끝의 `id`가 같은 값의 순서를 고정한다. PostgreSQL은 ORDER BY 식 안에서 SELECT 별칭을 읽지 못하므로 `group_rank`는 CTE 또는 서브쿼리 열로 만든 뒤 바깥 쿼리에서 정렬한다. 승인 보기의 날짜 구간(지남 · 이번 주 · 다음 주 · 그 뒤 · 없음)은 지급 예정일 오름차순 · NULLS LAST와 같은 방향이라 이 순서에서 이미 연속한다. 50건 LIMIT · OFFSET은 이 ORDER BY 뒤에만 건다 — 쪽을 자른 뒤 JS에서 그룹으로 모으거나 배열을 다시 줄 세우지 않는다. 쪽 크기는 `listExpenses(viewer, { status, page }, deps?)`의 `deps.pageSize`(기본 50 — 쪽 경계 통합 사례만 작게 준다, `deps.today`와 같은 주입 관례). `domain/expenses/list.ts`의 순수 `groupExpenses(rows, { status, todayKst })`(export — 단위 시험 대상)는 받은 순서 그대로 연속한 같은 그룹에 머리글을 붙이고(승인 보기는 서울 날짜 구간 라벨 · tone), 쪽 첫 행 머리글 반복만 한다. 합계는 `sumKrw` · 필터 전체 합계는 같은 범위 조건의 집계 한 번.
② `expenses-table.tsx`: 열 정의(우선순위 P1 · P2 · P3 · `min-width` 긴 문자 열 · 금액 `.num` 2행) · 서버가 보낸 `drafterColumn` · `amountColumn`이 거짓이면 그 열을 그리지 않는다(클라이언트가 숨기지 않는다) · 좁은 폭 열 접기(§7-3 기존 장치) · <700 접힌 줄 · 행 = 문서 링크. `page.tsx`: 빈 상태 셋(`ListEmpty` + 3차, 권한 없으면 앞 문장만) · 0건이면 1차 없음 · `ui/pagination` 링크 갈래. `loading.tsx`(04.1 지연 CSS 모듈 재사용 · 합계 줄 라벨 · 머리글 뼈대 · `--surface` 행 3개) · `error.tsx`(`ListEmpty tone="error"` + `retry`).
③ behavior 단위를 새 파일 `test/unit/domain/expenses/list-groups.test.ts`에, 통합을 새 파일 `test/integration/expense-money.test.ts`에, E2E를 `test/e2e/expense-list.spec.ts` describe `보기 · 빈 상태 · 폭`에 먼저 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/expenses/list-groups.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 주 시작 · 날짜 구간 경계 · 머리글 라벨이 원문과 다르거나, `groupExpenses`가 받은 행 순서를 바꾸거나, 쪽 첫 행 머리글을 다시 그리지 않으면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-money.test.ts test/integration/expense-visibility.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 외화 원화 환산 · 2행 · 원화 합계 · 50건 쪽 · 쪽 경계 머리글 중 하나라도 다르거나, 그룹 둘이 쪽 경계를 넘는 사례에서 쪽을 이어 붙인 순서가 한 쪽에 담은 순서와 다르거나 그룹 순서 · 그룹 안 정렬이 원문과 다르면(M2) 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-list.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 빈 상태 문구 · 3차 유무 · 1차 유무 · 합계 줄 · 열 접기 · 가로 스크롤 0 중 하나라도 다르면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -ciE "shimmer|progress" "app/(app)/expenses/loading.tsx"`가 0이다
    - `grep -c "retry" "app/(app)/expenses/error.tsx"`가 1 이상이고 `grep -c "reset" "app/(app)/expenses/error.tsx"`가 0이다
    - `grep -c "group_rank" repositories/expenses.ts`가 1 이상이고 `grep -cE "\.sort\(|toSorted\(" domain/expenses/list.ts`가 0이다(정렬은 SQL 한 곳 — M2(Round 2))
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>지출결의 목록이 보기 셋 · 그룹 · 합계 · 페이지 · 열 접기 · 빈 · 로딩 · 오류 계약을 지키고 외화는 원화와 원래 통화로 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 여러 줄 `Ctrl+E` — 열린 줄마다 작성 중 문서 → 목록 `작성 중` 그룹 + 토스트, 전부 막힘이면 합계 행 한 줄 · 같은 줄 두 번 = 행 하나(E2E)</name>
  <files>app/(app)/projects/[id]/quote-table.tsx, app/(app)/expenses/actions.ts, domain/expenses/index.ts, test/e2e/expense-list.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S1 「`Ctrl+E`」 문단(320–322행) · 확정 #5(49행) · Copywriting 277행(토스트) · Assumptions #16
    - `app/(app)/projects/[id]/quote-table.tsx`의 범위 선택 상태(`grep -n "selection\|range" "app/(app)/projects/[id]/quote-table.tsx"`로 자리 찾기) · 05-15 Task 2의 한 줄 `Ctrl+E` 배선
    - `ui/toast/Toast.tsx`(화면 이동 뒤 토스트를 띄우는 기존 방식 — Phase 4 선례를 grep으로 찾는다)
  </read_first>
  <behavior>
    - (E2E, desktop) 견적 줄 표에서 줄 1~3(열림 · 열림 · 거래처 없음)을 범위 선택 → `Ctrl+E` → 주소 `/expenses?status=…진행 중…` · 토스트 `지출결의 올리기 · 작성 중 2건 · 막힘 1줄` · `작성 중` 그룹 행 2
    - (E2E) 같은 범위에서 다시 `Ctrl+E` → 토스트 `지출결의 올리기 · 작성 중 2건 · 막힘 1줄` · 작성 중 행은 여전히 2(새로 만들지 않음). 줄 1 셀 `지출결의 올리기`를 두 번 눌러도 작성 중 그룹의 줄 1 행은 1
    - (E2E) 거래처 없는 줄 · 닫힌 줄만 범위 선택 → `Ctrl+E` → 이동하지 않고 합계 행 오른쪽 `거래처 없음`(첫 막힌 줄 셀 문자열) · 2차 미승인 표 전체 게이트에서 `Ctrl+E` → 합계 행에 아무것도 더 쓰지 않는다(표 위 한 줄만)
  </behavior>
  <action>
① `quote-table.tsx`의 `Ctrl+E` 처리(05-15)를 범위 선택이 여러 줄이면 선택된 줄 id 전부로 `createExpenseFromLinesAction({ lineIds })`를 부르게 넓힌다. 결과 `created` ≥ 1이면 `/expenses?status=` 진행 중 보기로 이동하고 토스트(누른 1차 라벨 · 결과 — DR-21 꼴, 막힌 줄 수가 있으면 뒤 조각). `created` = 0이면 이동하지 않고 첫 막힌 줄의 이유를 합계 행 오른쪽 한 줄에(표 전체 게이트 이유는 이미 표 위에 있으므로 쓰지 않는다). 미저장 편집이 있으면 05-05와 같은 한 줄.
② 액션 결과에 `created` · `blocked`를 그대로 돌려준다(05-03 도메인 결과 — 새 판정 없음). 이미 있던 작성 중 문서도 `created`에 든다(두 번 눌러도 같은 id — 05-03 idempotency).
③ behavior E2E를 `test/e2e/expense-list.spec.ts` describe `여러 줄 Ctrl+E · 두 번 눌러도 하나`에 먼저 적는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint · tsc · next build 중 하나라도 error를 출력하거나 0이 아닌 코드로 끝난다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-list.spec.ts test/e2e/expense-submit-mobile-approval.spec.ts test/e2e/quote-table.spec.ts'</automated>
    <fails_when>playwright failed가 1 이상이다 — 여러 줄 `Ctrl+E` 뒤 이동 · 토스트 문구 · 작성 중 행 수가 다르거나, 두 번째 `Ctrl+E` · 셀 두 번 누름이 행을 늘리거나, 전부 막힌 범위에서 이동하거나, 표 전체 게이트 때 합계 행에 이유가 겹쳐 쓰이거나, Phase 4 견적 표 E2E가 깨지면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/e2e/expense-list.spec.ts`에 `작성 중 2건` 문자열과 같은 줄 두 번 누름 사례가 있다(`grep -c "작성 중 2건" test/e2e/expense-list.spec.ts`가 1 이상)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>여러 줄을 골라 한 번에 작성 중 문서를 만들고 목록에서 이어 쓰며, 몇 번 눌러도 줄마다 문서는 하나다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 목록 요청 → 범위 필터 | URL 파라미터(보기 · 쪽)는 범위를 넓힐 수 없어야 한다 |
| 권한표(관리자) → 보임 | 메뉴 권한 변경이 즉시 범위에 반영되어야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-801 | Information Disclosure | 목록 · 문서 · 증빙 · 서명 GET의 범위 불일치 | high | mitigate | `canSeeExpense`와 `visibleExpenseScope`가 같은 입력에서 나오고 네 경로가 그 판정만 쓴다 — `expense-visibility` 통합(다른 팀 · 무관 PM · 작성 중 · 결재 관련자 · 권한 끔) |
| T-05-802 | Information Disclosure | 금액 열 · 합계 | medium | mitigate | 금액 필드는 `expense.amount` 노출 투영 — 못 보면 서버가 열 · 합계를 보내지 않는다. 누수 스캔 DTO 축 |
| T-05-803 | Elevation of Privilege | 역할 이름 하드코딩 | low | mitigate | 판정은 `can(viewer, "expenses.team", "view")` · 계급 업무 범위만 — 역할 이름 grep 0 수용 기준 |
| T-05-804 | Denial of Service | 목록 N+1(결재 단계 계산) | low | mitigate | 한 쪽 분 인스턴스를 한 번에 표시 계산 — 금지 항목 · 50건 상한 |
| T-05-805 | Denial of Service | 결재 관련자 보임 조건의 누적 IN 목록(P3-6) | low | mitigate | 처리 기록은 결재 단계 표 EXISTS 서브쿼리(`acted_by` 인덱스) · 지금 후보만 진행 중 인스턴스의 한정 목록 — Task 1 ② · 금지 항목 · acceptance grep |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(verification의 lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과
- 통합: `expense-visibility` · `expense-money` · `seed-permissions` · 증빙 · 누수 스캔 녹색
- `CI=true` E2E: `expense-list` · `expense-submit-mobile-approval` · `quote-table` · `permissions-grid` 녹색
- `. "$(git rev-parse --git-dir)/gsd-05-08.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml docs/design/tokens.css ui/shell` 비어 있음
</verification>

<success_criteria>
- 팀장이 팀원의 프로젝트 미연결 비용을 목록에서 본다(기준 4)
- 볼 수 없는 지출결의는 목록 · 화면 · 증빙 어디에도 없다
- 여러 줄을 골라 줄마다 하나씩 작성 중 문서를 만든다(기준 1 · 확정 #5)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 메뉴 · 권한 | `expenses.team`(팀 지출결의 보기) · 팀장 계급 시드 |
| 함수 | `canSeeExpense`(완성) · `visibleExpenseScope` · `listExpenses` · `groupExpenses` |
| 화면 | `/expenses` 목록(S8 — 필터 · 그룹 · 합계 · 페이지 · 열 접기 · 빈 · 로딩 · 오류) · 견적 줄 표 여러 줄 `Ctrl+E` |
| 테스트 | `test/integration/expense-visibility.test.ts` · `test/integration/expense-money.test.ts` · `test/unit/domain/expenses/list-groups.test.ts` · `test/e2e/expense-list.spec.ts` · `seed-permissions`(추가) |

<output>
Create `.planning/phases/05-expense-approval-leave/05-08-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F4 — read_first의 Phase 4 코드 줄 범위가 머지 뒤 어긋남(L22@1108e20) | MEDIUM | 이 플랜 read_first의 코드 파일 줄 범위를 전부 「괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read」로 교체(계획 문서 RESEARCH · UI-SPEC 행 범위는 이 페이즈 문서라 유지) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 해당하는 다른 지적은 없다 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| M2 — 목록 페이지 나눔 × 그룹 정렬 계약 없음(`05-08:147 · 183`, L31@69fb7ea) | MEDIUM | Plan 05-08, Task 2 ①: 리포지토리 목록 쿼리 SQL `ORDER BY group_rank, <그룹별 CASE 키>, id` 계약(상태 파생 rank · 그룹별 CASE 방향 · 끝 `id` · 별칭은 CTE/서브쿼리) + LIMIT/OFFSET은 그 뒤에만 · `groupExpenses`는 순서 보존 머리글만. Task 1 ③: 트레이서부터 같은 SQL 정렬. behavior 통합 사례 1(작성 중 3 · 반려 1 · 회수 1 · 결재 중 4, 시각 엇갈림, `deps.pageSize` 4 — 반려 · 회수와 결재 중 두 그룹이 쪽 경계를 넘고 이어 붙인 순서 = 한 쪽 순서) · 단위 순서 보존 사례 · must_haves truth · prohibition · acceptance(`group_rank` grep ≥ 1 · `list.ts` 배열 정렬 호출 0) |
| P3-6 — `visibleExpenseScope.partyInstanceIds` 누적 IN 목록(`05-08:146`, L43@69fb7ea) | LOW | Plan 05-08, Task 1 ②: 결재 관련을 두 갈래로 — 「처리한 사람」은 결재 단계 표 EXISTS 서브쿼리(`acted_by` = viewer, 04.1 인덱스), 「지금 단계 후보」는 04.1 결재함 계산의 진행 중 인스턴스 한정 목록(`currentHolderInstanceIds`). `canSeeExpense`도 같은 두 갈래. behavior 통합(승인 전 = 후보 갈래 · 승인 뒤 = 처리 기록 갈래로 목록 · 문서 둘 다 보임) · prohibition · T-05-805 · acceptance(`acted_?[bB]y` grep) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| P3-13 — 목록 정렬 키(`submitted_at` · `scheduled_payment_date`) 인덱스 없음(L50@69fb7ea) | 기록만 — 30명 규모(수천 건 이하)라 정렬 키 인덱스 없이 보임 범위 조건 뒤 정렬로 충분하다(eng-review 「범위 밖」과 같음). 목록 응답이 느려지는 신호가 보이면 그때 인덱스를 더한다 |

### Round 3 — efd6c67

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #87 문구 결정 정렬(코디네이터 16:30Z) — PR #87(1dcfd6a) DECISIONS 2026-09-26 오류 문구 명사형 통일 · SYSTEM §8 규칙 3(`test/unit/error-copy-noun-style.test.ts`) | 사용자 결정 | Plan 05-08 must_haves(로드 오류 줄): `지출결의를 불러오지 못했습니다 · 다시 시도` → `지출결의 불러오기 실패 · 다시 시도`(UI-SPEC Copywriting 「Error — 로드」 같은 글자). 목록 EMPTY 문구(`진행 중인 지출결의가 없습니다 · 새 지출결의` 등)는 #87 결정이 빈 목록 문구를 명시로 제외해 그대로 둔다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Round 3 항목은 #87 문구 정렬 하나이고 반영했다(design-review D2~D11 · G1~G4는 이 플랜과 무관) |
