---
phase: 05-expense-approval-leave
plan: 09
type: execute
wave: 10
depends_on: ["05-08"]
files_modified:
  - domain/expenses/index.ts
  - domain/evidence/index.ts
  - domain/approvals/conflict-message.ts
  - repositories/expenses.ts
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - app/(app)/expenses/[id]/page.tsx
  - app/(app)/expenses/[id]/expense-form.tsx
  - app/(app)/expenses/[id]/expense-document.tsx
  - ui/attachments/Attachments.tsx
  - test/integration/fixtures/expenses.ts
  - test/integration/expense-approval-lifecycle.test.ts
  - test/integration/expense-approval-concurrency.test.ts
  - test/e2e/expense-undo-resubmit.spec.ts
autonomous: true
requirements: [EXP-02, UX-03, UX-06, EVID-01]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "제출 토스트 `지출결의 제출 · 결재 요청됨 → {담당}`의 3차 `되돌리기`는 확인 창 없이 즉시 회수한다 — 회수된 지출결의는 기안자에게 값이 채워진 고칠 수 있는 폼으로 돌아오고(맨 위 읽기 행 `회수` `MM-DD HH:mm`), 1차 `지출결의 다시 제출 Ctrl+Enter`로 **같은 번호**로 다시 제출된다(차수 +1, 결재는 첫 단계부터). 토스트는 `되돌리기 · 결재 멈춤`으로 바뀐다(UI-SPEC 확정 #2 · 사용자 결정 2026-09-26 #1 · UIC interaction S3 되돌리기)"
    - "되돌리기를 눌렀을 때 첫 단계가 이미 처리했으면 서버가 거부하고 토스트가 닫을 때까지 남는 오류 토스트로 바뀐다 — 승인됐으면 `{이름}이 HH:MM에 승인함 · 문서에서 회수`(3차 → 문서 화면, 회수는 04.1 S6 확인), 반려됐으면 `{이름}이 HH:MM에 반려함 · 새로 고침`. 네트워크 · 서버 실패는 `되돌리지 못했습니다 · 되돌리기`(같은 회수 한 번 더)(UI-SPEC Copywriting 「거부 — 늦은 되돌리기」 · 「Error — 행동 실패」)"
    - "문서 화면의 2차 `회수`는 04.1 S6 회수 확인(제목 `지출결의 회수` · 부제 `{번호} · {대상}` · 결과 줄 `결재 멈춤 · {담당}의 결재함에서 빠짐` + 승인한 단계가 있으면 `팀장 승인 기록은 남음 · 다시 제출 때 첫 단계부터`)을 거친다 — 이미 받은 승인이 다시 필요해지는 되돌릴 수 없는 일이라서다(확정 #2)"
    - "반려된 지출결의도 기안자의 고칠 수 있는 폼으로 돌아오고(맨 위 읽기 행 `반려` `{결재자} · MM-DD HH:mm` + 2행 `사유 · {원문}`), 증빙은 그대로 남으며, `지출결의 다시 제출`은 같은 번호 · 차수 +1 · 그때의 결재선 설정으로 다시 고정된다. 앞 차수의 승인 · 반려 기록은 남는다(기준 2 · 3 · 04.1 다시 신청 선례)"
    - "기안자가 결재선 단계 담당이면 자기 승인 설정(지출결의 기본 `self_approve`)을 따른다 — 그 단계 후보는 기안자 한 사람이고, 제출 결과 자리에 결과 글자 대신 1차 `승인 Ctrl+Enter`가 서며(토스트 `지출결의 제출 · 본인 승인 차례` + `되돌리기`), 문서 화면 행동 줄은 `승인` + 2차 `회수`(반려 없음)다. 승인하면 결재선 단계 결과가 `본인 승인 MM-DD HH:mm`이다(입력 §3 · UA-607 · E6)"
    - "제출된 지출결의의 결재선은 제출 뒤 결재선 설정(`approval_route.expense.*`)을 바꿔도 바뀌지 않고, 새로 제출하는 문서와 다시 제출하는 문서만 새 설정을 따른다(기준 2)"
    - "승인 ↔ 회수, 승인 ↔ 반려를 동시에 하면 두 순서 모두 하나만 성공하고 진 쪽은 04.1 「거부 — 동시 처리」 문구(누가 · 언제 · 무엇을)를 받는다 — 지출결의 문서로 다시 증명한다(기준 3 · version 조건부 UPDATE)"
    - "제출 뒤 결재 중(`submitted` · `in_review`)이면 기안자는 증빙을 더하고 **마지막이 아닌** 파일을 지울 수 있다 — 마지막 파일에는 `삭제`가 렌더되지 않고 서버도 거부한다(06 SP-6이 확인 모달과 함께 연다). **승인된 문서에는 더하기만 된다 — 모든 파일에 `삭제`가 렌더되지 않고 서버도 삭제를 거부한다**(결재자가 본 근거가 줄지 않게 — 사용자 결정 대기(U1 추천안 A 선반영 — 코디네이터 답이 B면 되돌림)). 더하거나 지우는 트랜잭션이 결재 인스턴스 version을 올린다(E4 `bumpInstanceVersion` — 상태 불변)(UI-SPEC S4 표 · UI Assumptions #8 — 승인 뒤 삭제 부분은 U1로 보류 · UIC zero-one-many S4 · populated S4 · 리뷰 Round 1 U1)"
    - "증빙 추가 ↔ 승인 두 순서: 증빙 추가가 먼저면 그 전에 문서를 연 결재자의 승인이 `{기안자}이 HH:MM에 증빙을 더함 · 새로 고침`으로 막히고, 승인이 먼저면 증빙 추가는 성공하되 결재 결과는 바뀌지 않는다(Pitfall 5 · E4)"
    - "작성 중(번호 없는) 지출결의는 PC 폼 머리 줄 오른쪽 2차 `지출결의 삭제`로 즉시 지워지고(소프트 삭제 · `/expenses`로 이동) 토스트 `지출결의 삭제` + 3차 `되돌리기`(되돌리면 폼으로 돌아온다)가 뜬다 — 확인 창 대신 되돌리기. 폰에서는 머리 줄 오른쪽 버튼이 숨는다. 삭제 실패는 `지출결의를 삭제하지 못했습니다 · 다시 시도`(CLAUDE.md §7 · UI-SPEC S3 · Assumptions #10)"
    - "파일 행 3차는 보는 사람 · 문서 상태로 서버가 정한다 — 기안자 · 작성 중/반려/회수 = 모든 파일 `삭제`, 기안자 · 결재 중 = 「하나 더」 + 마지막이 아닌 파일만 `삭제`, 기안자 · 승인 = 「하나 더」만(`삭제` 없음 — 사용자 결정 대기(U1 추천안 A 선반영 — 코디네이터 답이 B면 되돌림)), 결재선 사람 · 목록 범위로 보는 사람 = `크게 보기`만(「하나 더」 없음)(UI-SPEC S4)"
  prohibitions:
    - statement: "되돌리기 · 다시 제출이 새 번호를 매기거나 새 문서를 만들어서는 안 된다 — 같은 문서 · 같은 번호 · 같은 결재 인스턴스의 다음 차수다"
      status: resolved
      verification: judgment
    - statement: "번호를 받은 반려 · 회수 문서를 고칠 때 다른 프로젝트 줄로 옮기거나 팀 비용 ↔ 견적 줄 종류를 바꿔서는 안 된다 — 번호의 프로젝트 부분 · 원래 줄의 문(D-66)이 깨진다. 같은 프로젝트(팀 비용 문서는 팀 비용 그대로) 안에서만 줄을 바꾼다(05-07 `changeExpenseLine` 판정 · 리뷰 Round 1 F6)"
      status: resolved
      verification: judgment
    - statement: "승인된 지출결의의 증빙을 지워서는 안 된다 — 결재자가 승인한 근거가 줄어든다(사용자 결정 대기(U1 추천안 A 선반영 — 코디네이터 답이 B면 되돌림))"
      status: resolved
      verification: judgment
    - statement: "증빙이 바뀐 뒤 그 전에 문서를 연 결재자의 승인이 통과해서는 안 된다 — 결재자는 바뀐 증빙을 본 뒤에만 승인한다"
      status: resolved
      verification: judgment
    - statement: "되돌릴 수 없는 일(문서 화면 회수 — 받은 승인 무효)에 확인을 빼거나, 되돌릴 수 있는 일(토스트 되돌리기 · 작성 중 삭제)에 확인 창을 두어서는 안 된다(CLAUDE.md §7)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/expenses/index.ts"
      provides: "submitExpense 다시 제출 갈래 · withdrawExpense(undo 모드) · deleteExpenseDraft · restoreExpenseDraft"
      exports: ["withdrawExpense", "deleteExpenseDraft", "restoreExpenseDraft"]
    - path: "test/integration/expense-approval-concurrency.test.ts"
      provides: "승인↔회수 · 승인↔반려 · 증빙 추가↔승인 두 순서씩"
      min_lines: 80
    - path: "test/e2e/expense-undo-resubmit.spec.ts"
      provides: "토스트 되돌리기 → 폼 → 같은 번호 다시 제출 · 늦은 되돌리기 · 작성 중 삭제 되돌리기 · 본인 승인"
      min_lines: 60
  key_links:
    - from: "domain/expenses/index.ts"
      to: "domain/approvals/index.ts"
      via: "다시 제출 = resubmitDocument(withdrawn은 E1 옵션) · 회수 = withdrawDocument"
      pattern: "resubmitDocument"
    - from: "domain/evidence/index.ts"
      to: "repositories/approvals.ts"
      via: "제출 뒤 증빙 추가 · 삭제 트랜잭션이 bumpInstanceVersion(reason evidence)"
      pattern: "bumpInstanceVersion"
    - from: "app/(app)/expenses/[id]/expense-form.tsx"
      to: "app/(app)/expenses/actions.ts"
      via: "토스트 되돌리기 → withdrawExpenseAction(undo) · 삭제 되돌리기 → restoreExpenseDraftAction"
      pattern: "withdrawExpenseAction"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**지출결의 문서 흐름 완성**: 제출 토스트의 `되돌리기`(즉시 회수) → 고칠 수 있는 폼 → 같은 번호로 다시 제출, 반려 뒤 다시 제출, 본인 승인, 작성 중 삭제와 되돌리기, 제출 뒤 증빙 추가 · 삭제(결재자 승인을 막음)를 세우고, 결재 동시 조작 두 순서를 지출결의로 다시 증명한다.

Purpose: UI-SPEC 확정 #2가 여기서 코드가 되고, 기준 2 · 3(고정 결재선 · 낙관적 잠금 두 순서)을 지출결의 문서로 다시 증명한다. 05-01의 E1 · E4가 실제 호출자를 얻는다.

Output: 다시 제출 갈래 · 회수(되돌리기) · 작성 중 삭제/복원 · 제출 뒤 증빙 규칙 · 폼 반려/회수 모드 · 액션 셋 · 통합 2(한 파일 새로) · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-04-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-05-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-09.env"` · diff 검사 접두는 05-01과 같은 모양. 트랜잭션 규약 · `.planning/STATE.md` 불변 · 새 의존성 0 · 새 토큰 0.
- 잠금 순서(RESEARCH Pattern 7 주의): 지출결의 제출 · 다시 제출은 프로젝트 행 → 지출결의 행 → 결재 인스턴스 → 카운터. 증빙 추가 · 삭제는 지출결의 행 → 결재 인스턴스(프로젝트 잠금 없음). 04.1 승인은 인스턴스만. 같은 쌍을 거꾸로 잡는 경로를 만들지 않는다 — 실행자가 경로별 잠금 순서 표를 SUMMARY에 적는다.
- 경합 재현은 sleep 없이 `deps.afterLock` · `test/integration/lock-race.ts` · 04.1 동시성 테스트의 도우미를 쓴다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 115k — 05-01 「웨이브 근거」의 규모 근거): 세 task 모두 05-05 폼 · 문서 화면과 05-01 E1 · E4 배선 위의 덧붙임이라 새로 읽을 파일이 적다. Task 3 경합 사례는 05-14의 `lock-race.ts` 모양을 그대로 쓴다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 제출 → 토스트 `되돌리기`(즉시 회수) → 고칠 수 있는 폼(`회수` 읽기 행) → 공급가액 고침 → `지출결의 다시 제출`(같은 번호 · 차수 2) → 승인</name>
  <files>domain/expenses/index.ts, repositories/expenses.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/[id]/page.tsx, app/(app)/expenses/[id]/expense-form.tsx, domain/approvals/conflict-message.ts, test/integration/expense-approval-lifecycle.test.ts, test/e2e/expense-undo-resubmit.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` 확정 #2(46행) · S3 「반려 · 회수 뒤」(401–402행) · Copywriting 247 · 276–281행
    - 04.1 `withdrawDocument` · `resubmitDocument` · 충돌 오류 조립(`name_map`) · 05-01 E1(`resubmittableStatuses`)
    - 05-03 `submitExpense` · `saveExpenseDraft` · 05-06 `previewExpense` · 05-05 폼 · 토스트 사용
    - 04.1 연차 다시 신청 폼(읽기 행 `반려` + 사유 2행 선례 — `name_map`의 연차 폼 파일)
  </read_first>
  <behavior>
    - (통합) 제출(번호 `26001-0001` · 인스턴스 version V) → `withdrawExpense({ expenseId, expectedVersion: V, undo: true })` → 인스턴스 `withdrawn` · `getExpense` 편집 가능 · `saveExpenseDraft`로 공급가액을 바꿀 수 있다 → `submitExpense` → 같은 번호 · 같은 인스턴스 id · `current_round` 2 · 상태 `submitted` · 세금 스냅숏이 새 공급가액으로 다시 저장 · 카운터 값 불변
    - (통합, 번호 문서 줄 이동 — F6) 회수된 `26001-0001`(프로젝트 26001 견적 줄 문서)에 `changeExpenseLine`으로 프로젝트 `26002`의 줄을 주면 `GateBlockedError` `번호 있는 문서 · 같은 프로젝트 줄만` · 문서 줄 · 번호 · 원래 줄의 문(닫힘) 그대로. 같은 프로젝트의 다른 열린 줄로는 바뀐다. 회수된 팀 비용 문서 `T26-0001`에 견적 줄을 주면 같은 거부
    - (통합) 팀장이 1단을 승인한 뒤 `withdrawExpense({ undo: true, expectedVersion: V })` → 거부 · 오류 구조에 처리자 이름 · 시각 · 상태 `approved` · 인스턴스 그대로. 같은 상태에서 `undo` 없이 맞는 version으로 회수 → 성공(문서 화면 회수 경로)
    - (E2E, desktop) PM 제출 → 토스트 `되돌리기` 클릭 → 확인 창 없음 · 토스트 `되돌리기 · 결재 멈춤` · 같은 주소가 폼(읽기 행 `회수` · 1차 `지출결의 다시 제출`) · 공급가액 고침 · `Ctrl+Enter` → 번호 그대로 · 토스트 `지출결의 다시 제출 · 결재 요청됨 → {팀장}` · 팀장 승인 → 태그 진행
  </behavior>
  <action>
① `domain/expenses/index.ts`: 편집 가능 판정을 「인스턴스 없음 또는 인스턴스 상태 ∈ {rejected, withdrawn}」 + 기안자로 넓혀 `saveExpenseDraft` · `previewExpense` · 증빙 요청(05-04 상태 목록)이 같은 판정 함수 `isEditableByDrafter`를 쓰게 한다. 편집 가능이 번호 있는 문서까지 넓어지므로 05-07 `changeExpenseLine`의 번호 있는 문서 규칙(같은 프로젝트 줄만 · 팀 비용 문서 유지)이 여기서 처음 실제로 닿는다 — behavior 사례로 증명한다(Addresses review concern: F6). `submitExpense`에 다시 제출 갈래 — 번호가 있으면 트랜잭션 전 `prepareSubmission`(지금 설정으로 결재선 재고정) · 트랜잭션 안 같은 잠금 순서 · 게이트 재판정(④는 자기 자신 제외) · 스냅숏 다시 저장 · `resubmitDocument(viewer, prepared, { instanceId, expectedVersion }, tx)` · 번호 부여 없음. `withdrawExpense(viewer, { expenseId, expectedVersion, undo? })` — 04.1 `withdrawDocument` 위임, `undo`면 결과 판정은 같되 거부 때 오류 구조를 그대로 올린다(화면이 늦은 되돌리기 문구를 만든다).
② 04.1 충돌 오류가 처리자 이름 · 시각 · 상태를 구조로 싣지 않으면 `domain/approvals/conflict-message.ts`의 `ApprovalConflictError`에 선택 필드 `detail: { actorName, at, status }`를 **덧붙인다**(문구 · 기존 필드 불변 — 05-01 원칙).
③ 액션: `withdrawExpenseAction({ expenseId, expectedVersion, undo })`(+ 등록). `app/(app)/expenses/[id]/page.tsx`: 기안자 · 반려/회수면 폼. 폼: 맨 위 읽기 행(`회수 MM-DD HH:mm` 또는 `반려 {결재자} · MM-DD HH:mm` + 2행 `사유 · {원문}` — 04.1 선례 컴포넌트), 1차 라벨 `지출결의 다시 제출`, 결재선 한 줄은 지금 다시 해석한 모양. 제출 · 다시 제출 성공 토스트에 3차 `되돌리기`(→ `withdrawExpenseAction` undo · 확인 없음 · 성공 = 토스트 `되돌리기 · 결재 멈춤` + 폼으로 다시 그림 · 거부 = 오류 토스트(닫을 때까지) 문구는 must_haves, 승인 갈래 3차 `문서에서 회수` → 문서 화면 · 실패 = `되돌리지 못했습니다 · 되돌리기`).
④ behavior 통합을 `test/integration/expense-approval-lifecycle.test.ts` describe `회수 뒤 같은 번호 다시 제출`에, E2E를 새 스펙 `test/e2e/expense-undo-resubmit.spec.ts`에 먼저 적는다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-lifecycle.test.ts test/integration/approvals-extensions.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 회수 뒤 다시 제출이 새 번호 · 새 인스턴스를 만들거나 차수가 2가 아니거나, 스냅숏이 옛 금액으로 남거나, 승인 뒤 undo 회수가 통과하거나 오류에 처리자 · 시각 · 상태가 없으면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-undo-resubmit.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 되돌리기에 확인 창이 뜨거나, 폼으로 돌아오지 않거나, 다시 제출 번호가 바뀌거나, 토스트 문구가 원문과 다르면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "resubmitDocument" domain/expenses/index.ts`가 1 이상이고 `grep -c "isEditableByDrafter" domain/expenses/index.ts`가 2 이상이다
    - `domain/approvals/conflict-message.ts`를 고쳤다면 diff가 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-09.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/approvals/conflict-message.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>제출 직후 되돌려 고치고 같은 번호로 다시 올리는 길이 화면과 테스트로 선다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 반려 뒤 다시 제출 · 본인 승인 · 결재선 고정(설정 변경 뒤) · 문서 화면 회수 확인 · 작성 중 삭제와 되돌리기</name>
  <files>domain/expenses/index.ts, repositories/expenses.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/[id]/expense-form.tsx, app/(app)/expenses/[id]/expense-document.tsx, test/integration/expense-approval-lifecycle.test.ts, test/e2e/expense-undo-resubmit.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S3 머리 줄(398–400행) · S7 행동 줄 · Copywriting 280–281행(반려 · 회수 확인 문구)
    - 04.1 S6 반려/회수 확인 컴포넌트(`ui/confirm-dialog` 사용 — 04.1-05) · 04.1 결재 설정 픽스처(`upsertSimpleValue`)
    - Task 1 산출물
  </read_first>
  <behavior>
    - (통합) 팀장 반려(사유 `금액 확인`) → 기안자 폼 편집 가능 · 다시 제출 → 같은 번호 · 차수 2 · 차수 1의 팀장 `rejected` 기록 · 사유 보존
    - (통합) 팀장이 기안자면(자기 승인 기본 `self_approve`) 제출 뒤 1단 후보 = 기안자 · 기안자 승인 → 단계 `self_approved` 참 · 결재선 보기 낱말 `본인 승인` · 다음 단계로
    - (통합) 제출 뒤 `approval_route.expense.step2.role_id`를 다른 계급으로 바꿔도 그 문서의 단계 행 불변 · 새 문서는 새 계급 · 그 문서를 반려 뒤 다시 제출하면 새 설정으로 재고정
    - (통합) `deleteExpenseDraft` → `deleted_at` 있음 · 목록 · `getExpense`에서 사라짐 · `restoreExpenseDraft` → 되돌아옴 · 번호 있는 문서 삭제는 거부
    - (E2E, desktop) 작성 중 폼 머리 줄 `지출결의 삭제` → 확인 창 없음 · `/expenses`로 이동 · 토스트 `지출결의 삭제` + `되돌리기` → 누르면 폼으로. 결재 중 문서 화면 `회수` → 04.1 확인 제목 `지출결의 회수` · 결과 줄에 `팀장 승인 기록은 남음 · 다시 제출 때 첫 단계부터`(팀장 승인 뒤) → `회수 Ctrl+Enter` → 폼. 팀장 기안 문서의 결재 1단 = 문서 화면 `승인` + `회수`(반려 없음), 제출 토스트 `지출결의 제출 · 본인 승인 차례`
  </behavior>
  <action>
① `domain/expenses/index.ts`: `deleteExpenseDraft(viewer, { expenseId, expectedVersion })`(기안자 · 번호 없음 · 소프트 삭제 + `document_delete` 로그 같은 tx) · `restoreExpenseDraft(viewer, { expenseId })`(기안자 · 삭제된 작성 중만 — 같은 줄에 새 작성 중 문서가 생겨 부분 UNIQUE가 막으면 복원하지 않고 그 문서 id를 돌려 화면이 그 문서를 연다). 리포지토리 `softDeleteDraft` · `restoreDraft`. 결재 관련 판정(반려 · 본인 승인 · 결재선 고정)은 04.1 엔진 그대로 — 이 task는 지출결의로 다시 증명하는 사례와 화면 배선이다.
② 액션 `deleteExpenseDraftAction` · `restoreExpenseDraftAction`(+ 등록). 폼 머리 줄 오른쪽 2차 `지출결의 삭제`(작성 중에만 · 폰 숨김) → 즉시 삭제 → `/expenses` + 토스트 되돌리기 → 복원 → 폼. 문서 화면 행동 줄은 04.1 분기표를 그대로 쓰되 회수는 04.1 S6 확인(제목 `지출결의 회수` · 부제 · 결과 줄 — 문서 종류 이름만 이 페이즈 문구). 본인 승인 차례면 제출 결과 자리에 1차 `승인 Ctrl+Enter`(결과 글자 없음) · 토스트 `지출결의 제출 · 본인 승인 차례`.
③ behavior 통합을 `test/integration/expense-approval-lifecycle.test.ts` describe 셋(`반려 뒤 다시 제출` · `본인 승인` · `결재선 고정`)과 한 describe(`작성 중 삭제`)로, E2E를 `test/e2e/expense-undo-resubmit.spec.ts`에 덧붙인다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-lifecycle.test.ts test/integration/expense-create-idempotency.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 반려 뒤 다시 제출이 번호를 바꾸거나 이전 차수 기록을 지우거나, 본인 승인 낱말 · `self_approved`가 없거나, 설정 변경이 제출된 문서의 단계를 바꾸거나, 번호 있는 문서가 삭제되거나, 복원이 부분 UNIQUE를 깨면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-undo-resubmit.spec.ts test/e2e/expense-list.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 작성 중 삭제에 확인 창이 뜨거나 되돌리기가 없거나, 문서 화면 회수에 확인이 없거나 결과 줄이 다르거나, 본인 승인 행동 줄에 `반려`가 있으면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "deleted_at" repositories/expenses.ts`가 1 이상이고 `deleteExpenseDraft`가 번호 있는 문서를 거부하는 사례가 통합에 있다(`grep -c "deleteExpenseDraft" test/integration/expense-approval-lifecycle.test.ts`가 1 이상)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>반려 · 본인 승인 · 결재선 고정이 지출결의로 다시 증명되고, 작성 중 문서는 확인 없이 지우고 되돌린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 제출 뒤 증빙 추가 · 마지막 아닌 파일 삭제(E4 배선) · 결재 동시 조작 두 순서 셋(승인↔회수 · 승인↔반려 · 증빙 추가↔승인)</name>
  <files>domain/evidence/index.ts, ui/attachments/Attachments.tsx, app/(app)/expenses/[id]/expense-document.tsx, test/integration/fixtures/expenses.ts, test/integration/expense-approval-concurrency.test.ts, test/e2e/expense-undo-resubmit.spec.ts</files>
  <read_first>
    - 05-04 `domain/evidence/index.ts`(상태 판정 · 트랜잭션 쓰기 순서) · 05-01 `bumpInstanceVersion` · 충돌 문구 증빙 갈래
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S4 파일 행 3차 표(421–427행) · 430행 · Copywriting 267행
    - 04.1 `test/integration/approvals-concurrency.test.ts`(두 순서 경합 도우미 · 단언 모양) · `test/integration/lock-race.ts`
  </read_first>
  <behavior>
    - (통합) 결재 중 문서에 기안자가 증빙을 더하면 파일 행 +1 · 인스턴스 상태 불변 · version +1 · `version_reason` `evidence`. 파일이 둘일 때 하나 삭제 → 성공 · version +1. 파일이 하나일 때 삭제 → 거부(파일 행 · version 불변). 결재자 · 무관한 사람의 추가 · 삭제 → 404
    - (통합, 승인 뒤 — U1) 최종 승인된 문서에 기안자가 증빙 추가 → 성공 · version +1 · 상태 `approved` 그대로. 파일이 둘이어도 삭제 → 거부 · 파일 행 · version 불변. `evidenceActions.deletableFileIds`가 빈 배열
    - (통합, 두 순서 ①) 팀장이 version V를 읽은 뒤 기안자가 증빙 추가 → 팀장 V로 승인 → `ApprovalConflictError` · 문구 `{기안자}이 HH:MM에 증빙을 더함 · 새로 고침` · 단계 기록 0건. 팀장이 새로 읽고 승인 → 성공
    - (통합, 두 순서 ②) 팀장 승인이 먼저 커밋 → 기안자 증빙 추가 성공 · 인스턴스 상태 · 단계 기록은 승인 결과 그대로
    - (통합, 승인↔회수 두 순서) `afterLock`으로 순서를 고정 — 승인 먼저면 회수(옛 version)가 04.1 충돌 문구로 거부, 회수 먼저면 승인이 거부. 둘 다 원시 DB 오류가 아니다
    - (통합, 승인↔반려 두 순서) 1단 후보가 둘(팀장 둘)인 결재선에서 한 명 승인 · 한 명 반려를 두 순서로 — 먼저 커밋한 쪽만 기록되고 진 쪽은 04.1 충돌 문구
    - (E2E, desktop) 결재 중 문서 화면에서 기안자가 「하나 더」로 파일을 더함 → 파일 행 둘 · 첫 파일에 `삭제`가 있고 파일이 하나일 때는 `삭제`가 없다. 결재자의 문서 화면에는 `크게 보기`만 있고 「하나 더」가 없다. 승인된 문서 화면에서 기안자에게는 「하나 더」만 있고 어떤 파일 행에도 `삭제`가 없다(U1)
  </behavior>
  <action>
① `domain/evidence/index.ts`: 추가 허용 상태에 결재 중(`submitted` · `in_review`) · 승인을 더하고(기안자만), **삭제(`removeEvidence`) 허용 상태에는 결재 중만 더한다 — 승인(`approved`)은 추가만, 삭제는 서버가 `UserFacingError`로 거부**(Addresses review concern: U1 — 사용자 결정 대기(U1 추천안 A 선반영 — 코디네이터 답이 B면 되돌림); 05-04 작성 중/반려/회수 목록은 그대로), 그 상태의 추가 · 삭제 트랜잭션은 파일 쓰기 뒤 같은 tx에서 `bumpInstanceVersion(viewer, { instanceId, updatedBy: viewer.userId, reason: "evidence" }, tx)`(잠금 순서: 지출결의 행 → 인스턴스). 삭제는 제출 뒤이면 트랜잭션 안에서 `countActiveByOwner(tx)` > 1일 때만. 파일 행 3차 규칙(보는 사람 · 상태 — 승인 문서는 `deletableFileIds` 빈 배열)을 문서 DTO의 `evidenceActions: { canAdd, deletableFileIds }`로 서버가 보내고, `Attachments` 읽기 모드가 그 값대로 「하나 더」 · `삭제` · `크게 보기`를 그린다(클라이언트 추론 없음).
② `test/integration/fixtures/expenses.ts`의 `attachEvidence` 도우미를 재사용해 새 파일 `test/integration/expense-approval-concurrency.test.ts`에 behavior 통합 다섯 묶음을 적는다(sleep 없음 · `afterLock` 또는 04.1 경합 도우미). E2E는 `test/e2e/expense-undo-resubmit.spec.ts` describe `제출 뒤 증빙`.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-concurrency.test.ts test/integration/evidence-upload.test.ts test/integration/approvals-concurrency.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 원시 DB 오류(23505 · 40001)가 테스트 출력에 나온다 — 증빙 변경 뒤 옛 version 승인이 통과하거나, 마지막 파일 삭제 · 승인 문서의 파일 삭제가 통과하거나, 결재자가 증빙을 더하거나, 두 순서 중 하나에서 둘 다 성공하면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-undo-resubmit.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 마지막 파일에 `삭제`가 보이거나 결재자 화면에 「하나 더」가 보이면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "bumpInstanceVersion" domain/evidence/index.ts`가 1 이상이다
    - 승인 문서 삭제 거부 사례가 통합 파일에 있다(`grep -c "승인 뒤" test/integration/expense-approval-concurrency.test.ts`가 1 이상 — U1)
    - `grep -cE "setTimeout|sleep\(" test/integration/expense-approval-concurrency.test.ts`가 0이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>제출 뒤 증빙이 바뀌면 결재자는 새로 본 뒤에만 승인하고, 결재 동시 조작 두 순서가 지출결의로 증명된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 기안자 → 제출된 문서 | 제출 뒤 기안자가 바꿀 수 있는 것은 증빙뿐이다 |
| 결재자 화면(연 시점) → 승인 순간 | 결재자가 본 문서와 승인 대상이 같아야 한다 |
| 토스트 되돌리기 → 회수 | 이미 처리된 문서를 조용히 되돌리면 받은 승인이 사라진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-901 | Tampering | 증빙 변경 ↔ 승인 경합 | high | mitigate | 추가 · 삭제가 같은 tx에서 인스턴스 version을 올림(E4) — 두 순서 통합 사례 |
| T-05-902 | Tampering | 늦은 되돌리기로 받은 승인 무효화 | medium | mitigate | undo는 제출 직후 version을 조건으로 — 처리된 뒤면 거부 + `문서에서 회수`(04.1 S6 확인) |
| T-05-903 | Elevation of Privilege | 남의 문서 회수 · 삭제 · 복원 · 증빙 변경 | high | mitigate | 도메인 첫 줄 기안자 판정(04.1 회수 규칙 · `isEditableByDrafter` · 증빙 주인 판정) — 결재자 · 무관자 404 사례 |
| T-05-904 | Repudiation | 삭제 · 복원 · 증빙 변경 기록 | low | mitigate | `document_delete` · `document_update` 로그를 같은 tx에 |
| T-05-906 | Tampering | 번호 있는 반려 · 회수 문서의 프로젝트 · 종류 이동(F6) | medium | mitigate | 05-07 `changeExpenseLine` 규칙 — Task 1 거부 사례(다른 프로젝트 줄 · 팀 비용 → 줄) |
| T-05-907 | Repudiation / Tampering | 승인 뒤 증빙 삭제로 결재 근거 축소(U1) | medium | mitigate | 승인 상태 삭제 서버 거부 · `deletableFileIds` 빈 배열 — Task 3 사례(사용자 결정 대기(U1 추천안 A 선반영 — 코디네이터 답이 B면 되돌림)) |
| T-05-905 | Tampering | 다시 제출이 새 번호 · 새 인스턴스 | medium | mitigate | 번호 있는 문서는 번호 부여를 건너뛰고 `resubmitDocument`만 — 카운터 불변 단언 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(verification의 lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과
- 통합: `expense-approval-lifecycle` · `expense-approval-concurrency` · `evidence-upload` · `approvals-*` 녹색
- `CI=true` E2E: `expense-undo-resubmit` · `expense-list` 녹색
- `. "$(git rev-parse --git-dir)/gsd-05-09.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml docs/design/tokens.css` 비어 있음
</verification>

<success_criteria>
- 제출 직후 되돌려 고치고 같은 번호로 다시 올린다(확정 #2)
- 반려 · 본인 승인 · 결재선 고정 · 동시 조작 두 순서가 지출결의로 다시 증명된다(기준 2 · 3)
- 증빙이 바뀐 문서는 결재자가 다시 본 뒤에만 승인된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `submitExpense` 다시 제출 갈래 · `withdrawExpense`(undo) · `deleteExpenseDraft` · `restoreExpenseDraft` · `isEditableByDrafter` · 증빙 제출 뒤 규칙(`bumpInstanceVersion` 배선) · 문서 DTO `evidenceActions` |
| 오류 필드 | `ApprovalConflictError.detail`(필요할 때만 덧붙임) |
| 액션 | `withdrawExpenseAction` · `deleteExpenseDraftAction` · `restoreExpenseDraftAction` |
| 테스트 | `test/integration/expense-approval-concurrency.test.ts` · `expense-approval-lifecycle`(describe 다섯 추가) · `test/e2e/expense-undo-resubmit.spec.ts` |

<output>
Create `.planning/phases/05-expense-approval-leave/05-09-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F6 — 편집 가능 상태를 반려 · 회수까지 넓히면 번호 받은 문서가 다른 프로젝트 줄 · 팀 비용으로 옮겨갈 수 있음(L24@1108e20) | MEDIUM | Plan 05-09, Task 1 ① · behavior: 05-07 `changeExpenseLine` 규칙(같은 프로젝트 줄만 · 팀 비용 문서 유지)을 반려 · 회수 문서로 증명(다른 프로젝트 줄 거부 · 같은 프로젝트 성공 · `T26` 문서 → 줄 거부) · prohibition · T-05-906 |
| U1 — 승인된 지출결의에서 기안자가 증빙을 지울 수 있음(L40@1108e20) | 사용자 결정 대기 | Plan 05-09, Task 3 ①: 사용자 결정 대기(U1 추천안 A 선반영 — 코디네이터 답이 B면 되돌림). 승인 = 추가만(서버 거부 + `삭제` 렌더 안 함) · 결재 중 삭제는 현행(version 증가로 결재자 막음) · must_haves 두 줄 · prohibition · T-05-907 · 통합 · E2E 사례. **UI-SPEC UI Assumptions #8과 어긋난다 — UI-SPEC은 사용자 승인 문서라 고치지 않았고, 사용자가 B(현행 유지)를 고르면 이 행의 변경을 되돌린다** |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| U2 — 반려 · 회수된 지출결의를 끝낼(종결 · 취소) 길이 없어 번호 있는 문서가 줄 문을 닫고 회차 상한을 계속 차지함(L41@1108e20) | 사용자 결정 대기(추천안 B — Phase 6 TODO). 새 동작(종결 상태 · 상한 · 문 판정 제외)이라 자동 결정 대상이 아니다(ceo-review A6). `.planning/todos/pending/2026-09-26-phase-6-rejected-expense-close-path.md`로 기록 — 코디네이터 답이 A(이번 범위)면 이 플랜 또는 새 플랜에 task를 더하는 다음 `--reviews` 라운드가 필요하다 |

