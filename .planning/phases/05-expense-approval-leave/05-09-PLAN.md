---
phase: 05-expense-approval-leave
plan: 09
type: execute
wave: 10
depends_on: ["05-08"]
files_modified:
  - domain/expenses/index.ts
  - domain/evidence/index.ts
  - domain/evidence/signals.ts
  - domain/approvals/conflict-message.ts
  - repositories/expenses.ts
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - app/(app)/expenses/[id]/page.tsx
  - app/(app)/expenses/[id]/expense-form.tsx
  - app/(app)/expenses/[id]/expense-document.tsx
  - ui/attachments/Attachments.tsx
  - ui/attachments/Attachments.module.css
  - domain/permissions/menus.ts
  - repositories/files.ts
  - test/integration/evidence-void.test.ts
  - test/integration/fixtures/expenses.ts
  - test/integration/expense-approval-lifecycle.test.ts
  - test/integration/expense-approval-concurrency.test.ts
  - test/e2e/expense-undo-resubmit.spec.ts
autonomous: true
requirements: [EXP-02, UX-03, UX-06, EVID-01]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "제출 토스트 `지출결의 제출 · 결재 요청됨 → {담당}`의 3차 `되돌리기`는 확인 창 없이 즉시 회수한다 — 회수된 지출결의는 기안자에게 값이 채워진 고칠 수 있는 폼으로 돌아오고(맨 위 읽기 행 `회수` `MM-DD HH:mm`), 1차 `지출결의 다시 제출 Ctrl+Enter`로 **같은 번호**로 다시 제출된다(차수 +1, 결재는 첫 단계부터). 토스트는 `되돌리기 · 결재 멈춤`으로 바뀐다(UI-SPEC 확정 #2 · 사용자 결정 2026-09-26 #1 · UIC interaction S3 되돌리기)"
    - "되돌리기를 눌렀을 때 첫 단계가 이미 처리했으면 서버가 거부하고 토스트가 닫을 때까지 남는 오류 토스트로 바뀐다 — 승인됐으면 `{이름}이 HH:MM에 승인함 · 문서에서 회수`(3차 → 문서 화면, 회수는 04.1 S6 확인), 반려됐으면 `{이름}이 HH:MM에 반려함 · 새로 고침`. 네트워크 · 서버 실패는 `되돌리기 실패 · 되돌리기`(같은 회수 한 번 더)(UI-SPEC Copywriting 「거부 — 늦은 되돌리기」 · 「Error — 행동 실패」)"
    - "다시 제출 · 되돌리기는 클라이언트가 보낸 **인스턴스** version을 쓰지 않는다. 다시 제출의 `expectedVersion`은 05-03 그대로 **문서** version(`expenses.version` — 폼이 본 내용)이고, `resubmitDocument`에 넘기는 인스턴스 version은 tx 안에서 지출결의 행을 잠근 뒤 읽는다. 되돌리기는 토스트가 가진 차수(`round`)만 받고, 서버가 회수 직전에 읽은 인스턴스 상태 · 지금 차수 처리 기록으로 「이미 처리됨」을 판정해 그 읽기의 인스턴스 version으로 04.1 `withdrawDocument`를 부른다(읽기와 회수 사이의 결재는 04.1 version 조건 UPDATE가 막아 늦은 되돌리기 거부로 간다). 그래서 제출 뒤 기안자 본인이 증빙을 더해 인스턴스 version이 올라도 되돌리기 · 다시 제출이 스스로 충돌하지 않는다. 화면이 본 인스턴스 version(`expectedInstanceVersion`)을 조건으로 쓰는 것은 문서 화면 회수(04.1 S6 확인)뿐이다(리뷰 Round 2 P3-7)"
    - "문서 화면의 2차 `회수`는 04.1 S6 회수 확인(제목 `지출결의 회수` · 부제 `{번호} · {대상}` · 결과 줄 `결재 멈춤 · {담당}의 결재함에서 빠짐` + 승인한 단계가 있으면 `팀장 승인 기록은 남음 · 다시 제출 때 첫 단계부터`)을 거친다 — 이미 받은 승인이 다시 필요해지는 되돌릴 수 없는 일이라서다(확정 #2)"
    - "반려된 지출결의도 기안자의 고칠 수 있는 폼으로 돌아오고(맨 위 읽기 행 `반려` `{결재자} · MM-DD HH:mm` + 2행 `사유 · {원문}`), 증빙은 그대로 남으며, `지출결의 다시 제출`은 같은 번호 · 차수 +1 · 그때의 결재선 설정으로 다시 고정된다. 앞 차수의 승인 · 반려 기록은 남는다(기준 2 · 3 · 04.1 다시 신청 선례)"
    - "기안자가 결재선 단계 담당이면 자기 승인 설정(지출결의 기본 `self_approve`)을 따른다 — 그 단계 후보는 기안자 한 사람이고, 제출 결과 자리에 결과 글자 대신 1차 `승인 Ctrl+Enter`가 서며(토스트 `지출결의 제출 · 본인 승인 차례` + `되돌리기`), 문서 화면 행동 줄은 `승인` + 2차 `회수`(반려 없음)다. 승인하면 결재선 단계 결과가 `본인 승인 MM-DD HH:mm`이다(입력 §3 · UA-607 · E6)"
    - "제출된 지출결의의 결재선은 제출 뒤 결재선 설정(`approval_route.expense.*`)을 바꿔도 바뀌지 않고, 새로 제출하는 문서와 다시 제출하는 문서만 새 설정을 따른다(기준 2)"
    - "승인 ↔ 회수, 승인 ↔ 반려를 동시에 하면 두 순서 모두 하나만 성공하고 진 쪽은 04.1 「거부 — 동시 처리」 문구(누가 · 언제 · 무엇을)를 받는다 — 지출결의 문서로 다시 증명한다(기준 3 · version 조건부 UPDATE)"
    - "제출 뒤 결재 중(`submitted` · `in_review`)이면 기안자는 증빙을 더하고 **마지막이 아닌** 파일을 지울 수 있다 — 마지막 파일에는 `삭제`가 렌더되지 않고 서버도 거부한다(06 SP-6이 확인 모달과 함께 연다). **승인된 문서에는 더하기만 된다 — 모든 파일에 `삭제`가 렌더되지 않고 서버도 삭제를 거부한다**(결재자가 본 근거가 줄지 않게 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리). 승인 뒤 잘못 붙은 증빙은 지우지 않고 `voidEvidence`로 **무효 처리**한다(아래 줄). 더하거나 지우는 트랜잭션이 결재 인스턴스 version을 올린다(E4 `bumpInstanceVersion` — 상태 불변). 그 전에 문서를 연 결재자의 승인은 추가든 삭제든 같은 중립 문구 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침`으로 막힌다(리뷰 Round 2 P3-3 — 문구 원천은 05-01 E4)(UI-SPEC S4 표 · UI Assumptions #8 — 승인 뒤 삭제 부분은 U1로 보류 · UIC zero-one-many S4 · populated S4 · 리뷰 Round 1 U1)"
    - "증빙 추가 ↔ 승인 두 순서: 증빙 추가가 먼저면 그 전에 문서를 연 결재자의 승인이 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침`으로 막히고, 승인이 먼저면 증빙 추가는 성공하되 결재 결과는 바뀌지 않는다. 결재 중 증빙 삭제 뒤 옛 version 승인도 같은 문구로 막힌다(Pitfall 5 · E4 · 리뷰 Round 2 P3-3 — 추가 · 삭제 모두 중립 `바꿈`, UI-SPEC 267 · 548행의 `더함`은 design 게이트에서 UI-SPEC 보강)"
    - "작성 중(번호 없는) 지출결의는 PC 폼 머리 줄 오른쪽 2차 `지출결의 삭제`로 즉시 지워지고(소프트 삭제 · `/expenses`로 이동) 토스트 `지출결의 삭제` + 3차 `되돌리기`(되돌리면 폼으로 돌아온다)가 뜬다 — 확인 창 대신 되돌리기. 폰에서는 머리 줄 오른쪽 버튼이 숨는다. 삭제 실패는 `지출결의 삭제 실패 · 다시 시도`(CLAUDE.md §7 · UI-SPEC S3 · Assumptions #10)"
    - "파일 행 3차는 보는 사람 · 문서 상태로 서버가 정한다 — 기안자 · 작성 중/반려/회수 = 모든 파일 `삭제`, 기안자 · 결재 중 = 「하나 더」 + 마지막이 아닌 파일만 `삭제`, 기안자 · 승인 = 「하나 더」만(`삭제` 없음 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리), 승인 문서 · `expenses.evidence_void` 쓰기 권한자 = 살아 있는 파일마다 3차 `무효 처리`(삭제 아님), 결재선 사람 · 목록 범위로 보는 사람 = `크게 보기`만(「하나 더」 없음)(UI-SPEC S4)"
    - "승인된 지출결의의 잘못 붙은 증빙은 **무효 처리**한다(삭제 없음) — `voidEvidence(viewer, { fileId, reason })`: 사유 필수(trim 1~500자, 비면 04.1 반려 사유 빈 칸 문구 — 04.1 검증 함수 재사용이라 글자는 04.1 상수 그대로 = `사유 없음 · 사유 적기`(PR #89 R1 결정, 04.1 쪽 변경은 코디네이터가 #90에 지시) — 이 플랜은 문자열을 복제하지 않는다), 파일 행 · 저장소 객체는 그대로 두고 `voided_at` · `voided_by` · `void_reason`만 기록, 같은 tx에 행동 로그 `document_update` detail `{ change: \"evidence_void\", fileId, reasonLength }`(사유 원문은 로그에 싣지 않는다 — 04.1 `document_reject`가 사유를 단계 행에만 두는 규약과 같음) · 인스턴스 `bumpInstanceVersion`은 하지 않는다(승인 뒤라 결재 판정이 없다). 권한 = 새 메뉴 키 `expenses.evidence_void`(라벨 `증빙 무효 처리`) 쓰기 ∧ 그 지출결의를 볼 수 있음 — 기안자라도 이 권한이 없으면 거부된다. 시드는 아무 계급에도 켜지 않고 관리자가 권한표에서 실제 경영관리 계급에 준다(경영관리는 시드 계급 5종에 없음 — `projects.revenue` · `projects.adjustment` 선례, `domain/permissions/menus.ts` 주석)(사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리)"
    - "**마지막 살아 있는 파일도 무효 처리할 수 있다**(사용자 결정 G2) — `voidEvidence`는 살아 있는 파일 수를 보지 않는다(04 live 판정 · 제출 막힘 ⑧과 별개: 승인 문서라 제출 경로가 없다). 살아 있는 파일이 0이 되면 기안자 「내 차례」 [막힘] 신호(아래 G1 줄 — 05-10)와 Phase 6 점검이 잡는다"
    - "**무효 해제 동작은 없다**(사용자 결정 G3) — 무효를 되돌리는 도메인 함수 · 리포지토리 갱신 · 액션 · 버튼이 하나도 없고, 무효는 사유와 함께 영구 기록이다. 잘못 무효로 했으면 같은 파일을 다시 올린다 — 무효 파일은 「살아 있는 행」이 아니라 중복 검사에 걸리지 않으므로 같은 sha256 파일이 새 파일 행으로 붙는다"
    - "이미 무효인 파일을 무효 처리하면(두 사람이 겹침 · 오래 연 화면) `{처리자}이/가 HH:MM에 무효 처리함 · 새로 고침`으로 거부된다 — 04.1 「거부 — 동시 처리」 꼴 · 조사는 04.1 조사 함수 · 처리자 = 그 행의 `voided_by` 이름 · 시각 = `voided_at`(서울 `HH:MM`). 문구 조립은 `domain/approvals/conflict-message.ts`에 덧붙인 순수 함수 하나(design-review D9 · UI-SPEC Copywriting 「Error — 증빙 무효 처리」)"
    - "무효 증빙은 문서 화면 · 결재 시트 파일 행에 남아 `ui/status-tag` 「무효」(`--muted` — SYSTEM §7-5 B8, 견적 줄 `취소`와 같은 색) → 파일명 취소선 → 2행 `무효 · {처리자} · MM-DD HH:mm · {사유}` 순서로 보이고(DOM 순서도 같다 — 스크린리더가 「무효」를 먼저 읽는다), 파일명 요소의 `aria-describedby` = 2행 요소 id이며, `크게 보기`는 그대로 열린다. 무효 처리 결과는 기존 첨부 `aria-live=\"polite\"` 영역이 읽는다. 폰(<700) 파일 행의 3차 둘(`크게 보기` · `무효 처리`)은 각각 높이 ≥ 44(`--touch-min`)이고 둘 사이 ≥ 16(`--s-4`)이다(design-review D6 · UI-SPEC S4 「무효 행 접근성」). 무효 증빙은 「살아 있는 파일」이 아니므로 증빙 개수 게이트 ⑧ · 중복 검사 · 마지막 파일 판정에서 빠진다 — 승인 뒤라 제출 · 승인 게이트 영향은 없고 한 줄 규칙으로만 둔다. UI-SPEC 가정 #8은 이 사용자 결정으로 대체됐고 문구 · 모양의 정본은 UI-SPEC S4(99d74bd · Round 3 반영)다"
    - "무효 처리 성공은 토스트 없이 그 자리 행이 무효 행으로 바뀌는 것으로 끝난다 — 확인 창이 닫히고 결과가 행동한 자리에 보인다(§7-6 「화면 이동이 따르는 행동만 토스트」 · §7-7 SUCCESS, design-review D10). 누르는 동안 1차 `무효 처리…` · 나머지 `aria-disabled`, 네트워크 · 서버 실패는 사유 칸 아래 `무효 처리 실패 · 다시 시도`(창은 닫히지 않고 사유 보존 — design-review D5)"
    - "**무효 뒤 기안자 신호의 원천(사용자 결정 G1)**: `listEvidenceVoidSignals(viewer, { ownerKind, ownerIds })`(`domain/evidence/signals.ts`)가 넘겨받은 주인 id 가운데 「무효 파일의 가장 늦은 `voided_at` > 살아 있는 파일(`removed_at IS NULL AND voided_at IS NULL`)의 가장 늦은 `created_at`, 또는 무효 파일이 있고 살아 있는 파일이 0개」인 id만 돌려준다 — 지운 파일(`removed_at`)은 어느 쪽에도 세지 않고, 무효 뒤 새 증빙을 올리면 그 문서는 빠진다. 한 SQL(주인 id로 묶어 거르기) · 빈 입력이면 쿼리 없음. 05-10이 기안자의 승인 지출결의 id를 넘겨 「내 차례」 [막힘] `증빙 무효 · 증빙 올리기` 줄을 만든다"
  prohibitions:
    - statement: "되돌리기 · 다시 제출이 새 번호를 매기거나 새 문서를 만들어서는 안 된다 — 같은 문서 · 같은 번호 · 같은 결재 인스턴스의 다음 차수다"
      status: resolved
      verification: judgment
    - statement: "번호를 받은 반려 · 회수 문서를 고칠 때 다른 프로젝트 줄로 옮기거나 팀 비용 ↔ 견적 줄 종류를 바꿔서는 안 된다 — 번호의 프로젝트 부분 · 원래 줄의 문(D-66)이 깨진다. 같은 프로젝트(팀 비용 문서는 팀 비용 그대로) 안에서만 줄을 바꾼다(05-07 `changeExpenseLine` 판정 · 리뷰 Round 1 F6)"
      status: resolved
      verification: judgment
    - statement: "무효 처리 권한을 역할 이름(`경영관리`)이나 조직 이름으로 코드에 박아서는 안 된다 — 메뉴 권한 `expenses.evidence_void`로만 판정한다(04-02 · 04-13 선례)"
      status: resolved
      verification: judgment
    - statement: "승인된 지출결의의 증빙을 지워서는 안 된다 — 결재자가 승인한 근거가 줄어든다(사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리)"
      status: resolved
      verification: judgment
    - statement: "무효를 되돌리는(해제하는) 함수 · 액션 · 버튼 · 되돌리기 토스트를 만들어서는 안 된다 — 무효는 사유와 함께 영구 기록이다(사용자 결정 G3)"
      status: resolved
      verification: judgment
    - statement: "증빙이 바뀐 뒤 그 전에 문서를 연 결재자의 승인이 통과해서는 안 된다 — 결재자는 바뀐 증빙을 본 뒤에만 승인한다"
      status: resolved
      verification: judgment
    - statement: "되돌릴 수 없는 일(문서 화면 회수 — 받은 승인 무효)에 확인을 빼거나, 되돌릴 수 있는 일(토스트 되돌리기 · 작성 중 삭제)에 확인 창을 두어서는 안 된다(CLAUDE.md §7)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/expenses/index.ts"
      provides: "submitExpense 다시 제출 갈래 · withdrawExpense(undo 모드) · deleteExpenseDraft · restoreExpenseDraft"
      exports: ["withdrawExpense", "deleteExpenseDraft", "restoreExpenseDraft"]
    - path: "test/integration/expense-approval-concurrency.test.ts"
      provides: "승인↔회수 · 승인↔반려 · 증빙 추가↔승인 · 결재 중 삭제↔삭제 두 순서씩"
      min_lines: 80
    - path: "test/e2e/expense-undo-resubmit.spec.ts"
      provides: "토스트 되돌리기 → 폼 → 같은 번호 다시 제출 · 늦은 되돌리기 · 작성 중 삭제 되돌리기 · 본인 승인 · 무효 행 접근성(DOM 순서 · aria-describedby · 폰 터치 영역)"
      min_lines: 60
    - path: "domain/evidence/signals.ts"
      provides: "listEvidenceVoidSignals — 무효 뒤 기안자 신호의 원천(G1). `repositories/files.ts`만 import(05-10의 지출결의 종류가 순환 없이 부른다)"
      exports: ["listEvidenceVoidSignals"]
  key_links:
    - from: "domain/evidence/signals.ts"
      to: "repositories/files.ts"
      via: "findUnresolvedVoidOwnerIds — 무효 · 살아 있는 파일 시각을 주인 id로 묶어 거르는 한 SQL(G1)"
      pattern: "findUnresolvedVoidOwnerIds"
    - from: "domain/expenses/index.ts"
      to: "domain/approvals/index.ts"
      via: "다시 제출 = resubmitDocument(withdrawn은 E1 옵션) · 회수 = withdrawDocument"
      pattern: "resubmitDocument"
    - from: "domain/evidence/index.ts"
      to: "repositories/approvals.ts"
      via: "제출 뒤 증빙 추가 · 삭제 트랜잭션이 bumpInstanceVersion(reason evidence)"
      pattern: "bumpInstanceVersion"
    - from: "app/(app)/expenses/[id]/expense-form.tsx"
      to: "app/(app)/expenses/actions.ts"
      via: "토스트 되돌리기 → withdrawExpenseAction(undo) · 삭제 되돌리기 → restoreExpenseDraftAction"
      pattern: "withdrawExpenseAction"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**지출결의 문서 흐름 완성**: 제출 토스트의 `되돌리기`(즉시 회수) → 고칠 수 있는 폼 → 같은 번호로 다시 제출, 반려 뒤 다시 제출, 본인 승인, 작성 중 삭제와 되돌리기, 제출 뒤 증빙 추가 · 삭제(결재자 승인을 막음)를 세우고, 결재 동시 조작 두 순서를 지출결의로 다시 증명한다.

Purpose: UI-SPEC 확정 #2가 여기서 코드가 되고, 기준 2 · 3(고정 결재선 · 낙관적 잠금 두 순서)을 지출결의 문서로 다시 증명한다. 05-01의 E1 · E4가 실제 호출자를 얻는다.

Output: 다시 제출 갈래 · 회수(되돌리기) · 작성 중 삭제/복원 · 제출 뒤 증빙 규칙 · 폼 반려/회수 모드 · 액션 셋 · 통합 2(한 파일 새로) · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-04-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-05-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-09.env"` · diff 검사 접두는 05-01과 같은 모양. 트랜잭션 규약 · `.planning/STATE.md` 불변 · 새 의존성 0 · 새 토큰 0.
- 잠금 순서(RESEARCH Pattern 7 주의): 지출결의 제출 · 다시 제출은 프로젝트 행 → 지출결의 행 → 결재 인스턴스 → 카운터(다시 제출은 카운터 없음 · 인스턴스 version은 지출결의 행 잠금 뒤 읽는다 — 리뷰 Round 2 P3-7). 증빙 추가 · 삭제는 지출결의 행 → 결재 인스턴스(프로젝트 잠금 없음) — 지출결의 행 잠금은 05-04가 `completeEvidenceUpload` · `removeEvidence` tx 첫 단계에 둔 `lockExpenseForUpdate`(리뷰 Round 2 B2)이고, 이 플랜은 그 뒤에 상태 재판정 · 살아 있는 파일 수 · `bumpInstanceVersion`을 잇는다. 되돌리기는 04.1 `withdrawDocument`가 여는 tx에서 인스턴스만(지출결의 행 잠금 없음 — 04.1 승인과 같은 쪽). 04.1 승인은 인스턴스만. 같은 쌍을 거꾸로 잡는 경로를 만들지 않는다 — 실행자가 경로별 잠금 순서 표를 SUMMARY에 적는다.
- 경합 재현은 sleep 없이 `deps.afterLock` · `test/integration/lock-race.ts` · 04.1 동시성 테스트의 도우미를 쓴다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 115k — 05-01 「웨이브 근거」의 규모 근거): 세 task 모두 05-05 폼 · 문서 화면과 05-01 E1 · E4 배선 위의 덧붙임이라 새로 읽을 파일이 적다. Task 3 경합 사례는 05-14의 `lock-race.ts` 모양을 그대로 쓴다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 제출 → 토스트 `되돌리기`(즉시 회수) → 고칠 수 있는 폼(`회수` 읽기 행) → 공급가액 고침 → `지출결의 다시 제출`(같은 번호 · 차수 2) → 승인</name>
  <files>domain/expenses/index.ts, repositories/expenses.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/[id]/page.tsx, app/(app)/expenses/[id]/expense-form.tsx, domain/approvals/conflict-message.ts, test/integration/expense-approval-lifecycle.test.ts, test/e2e/expense-undo-resubmit.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` 확정 #2(46행) · S3 「반려 · 회수 뒤」(401–402행) · Copywriting 247 · 276–281행
    - 04.1 `withdrawDocument` · `resubmitDocument` · 충돌 오류 조립(`name_map`) · 05-01 E1(`resubmittableStatuses`)
    - 05-03 `submitExpense` · `saveExpenseDraft` · 05-06 `previewExpense` · 05-05 폼 · 토스트 사용
    - 04.1 연차 다시 신청 폼(읽기 행 `반려` + 사유 2행 선례 — `name_map`의 연차 폼 파일)
  </read_first>
  <behavior>
    - (통합) 제출(번호 `26001-0001` · 차수 1) → `withdrawExpense({ expenseId, undo: true, round: 1 })`(인스턴스 version을 보내지 않는다 — P3-7) → 인스턴스 `withdrawn` · `getExpense` 편집 가능 · `saveExpenseDraft`로 공급가액을 바꿀 수 있다(문서 version +1) → `submitExpense({ expenseId, expectedVersion: 지금 문서 version })` → 같은 번호 · 같은 인스턴스 id · `current_round` 2 · 상태 `submitted` · 세금 스냅숏이 새 공급가액으로 다시 저장 · 카운터 값 불변. 옛 문서 version으로 다시 제출 → 05-03 문서 충돌(`다른 곳에서 저장됨`) · 인스턴스 그대로
    - (통합, 번호 문서 줄 이동 — F6) 회수된 `26001-0001`(프로젝트 26001 견적 줄 문서)에 `changeExpenseLine`으로 프로젝트 `26002`의 줄을 주면 `GateBlockedError` `번호 있는 문서 · 같은 프로젝트 줄만` · 문서 줄 · 번호 · 원래 줄의 문(닫힘) 그대로. 같은 프로젝트의 다른 열린 줄로는 바뀐다. 회수된 팀 비용 문서 `T26-0001`에 견적 줄을 주면 같은 거부
    - (통합) 팀장이 1단을 승인한 뒤 `withdrawExpense({ expenseId, undo: true, round: 1 })` → 거부 · 오류 구조에 처리자 이름 · 시각 · 상태 `approved` · 인스턴스 그대로. 같은 상태에서 `undo` 없이 화면이 본 인스턴스 version으로 회수(`withdrawExpense({ expenseId, expectedInstanceVersion })`) → 성공(문서 화면 회수 경로). 문서 화면 회수를 승인 전 인스턴스 version으로 부르면 → 04.1 충돌 문구로 거부
    - (E2E, desktop) PM 제출 → 토스트 `되돌리기` 클릭 → 확인 창 없음 · 토스트 `되돌리기 · 결재 멈춤` · 같은 주소가 폼(읽기 행 `회수` · 1차 `지출결의 다시 제출`) · 공급가액 고침 · `Ctrl+Enter` → 번호 그대로 · 토스트 `지출결의 다시 제출 · 결재 요청됨 → {팀장}` · 팀장 승인 → 태그 진행. 토스트 4초 자동 소멸(`ui/toast` `AUTO_DISMISS_MS`)에 기대지 않는다 — `page.clock.install()`을 `goto` 전에 두고, 토스트가 보이면 곧바로 `page.clock.pauseAt(페이지의 지금 시각)`으로 타이머를 멈춘 뒤 `되돌리기`를 누르고 결과 토스트를 단언한 다음 폼 입력 전에 `page.clock.resume()`(P3-12)
  </behavior>
  <action>
① `domain/expenses/index.ts`: 편집 가능 판정을 「인스턴스 없음 또는 인스턴스 상태 ∈ {rejected, withdrawn}」 + 기안자로 넓혀 `saveExpenseDraft` · `previewExpense` · 증빙 요청(05-04 상태 목록)이 같은 판정 함수 `isEditableByDrafter`를 쓰게 한다. 편집 가능이 번호 있는 문서까지 넓어지므로 05-07 `changeExpenseLine`의 번호 있는 문서 규칙(같은 프로젝트 줄만 · 팀 비용 문서 유지)이 여기서 처음 실제로 닿는다 — behavior 사례로 증명한다(Addresses review concern: F6). `submitExpense`에 다시 제출 갈래 — 번호가 있으면 트랜잭션 전 `prepareSubmission`(지금 설정으로 결재선 재고정) · 트랜잭션 안 같은 잠금 순서 · 게이트 재판정(④는 자기 자신 제외) · 스냅숏 다시 저장 · `resubmitDocument(viewer, prepared, { instanceId, expectedVersion: 인스턴스 version }, tx)` · 번호 부여 없음. **version 두 가지를 섞지 않는다(Addresses review concern: P3-7(Round 2))**: `submitExpense` 입력의 `expectedVersion`은 05-03 그대로 문서 version(`expenses.version` 조건), `resubmitDocument`에 넘기는 인스턴스 version은 클라이언트에게 받지 않고 tx 안 `lockExpenseForUpdate` 뒤 인스턴스 행에서 읽은 값이다(증빙 추가 · 삭제도 지출결의 행을 먼저 잠그므로 그 사이에 끼지 못한다). `withdrawExpense(viewer, input)` 입력은 두 모양 — 되돌리기 `{ expenseId, undo: true, round }`(인스턴스 version 없음) · 문서 화면 회수 `{ expenseId, expectedInstanceVersion }`(화면이 본 값, 04.1과 같은 뜻). 되돌리기는 회수 직전에 인스턴스(상태 · `current_round` · version)와 지금 차수 단계 기록을 읽어 「차수가 `round`와 같고 · 상태 `submitted` · 처리(승인 · 반려) 기록 없음」이 아니면 거부하고 오류 구조에 가장 최근 처리 기록의 처리자 이름 · 시각 · 상태를 싣는다. 맞으면 그 읽기의 version으로 04.1 `withdrawDocument`에 위임한다(04.1 `withdrawDocument`는 tx를 받지 않고 스스로 version 조건 UPDATE — 읽기와 회수 사이에 결재자가 처리하면 04.1 충돌 오류가 나고 이것도 같은 늦은 되돌리기 거부로 올린다). 문서 화면 회수는 `expectedInstanceVersion` 그대로 04.1 위임. 거부 때 오류 구조를 그대로 올려 화면이 늦은 되돌리기 문구를 만든다.
② 04.1 충돌 오류가 처리자 이름 · 시각 · 상태를 구조로 싣지 않으면 `domain/approvals/conflict-message.ts`의 `ApprovalConflictError`에 선택 필드 `detail: { actorName, at, status }`를 **덧붙인다**(문구 · 기존 필드 불변 — 05-01 원칙).
③ 액션: `withdrawExpenseAction` — 입력 스키마는 ①의 두 모양(`{ expenseId, undo: true, round }` · `{ expenseId, expectedInstanceVersion }`)만(+ 등록). 제출 · 다시 제출 결과에 `round`를 실어 토스트가 가진다. `app/(app)/expenses/[id]/page.tsx`: 기안자 · 반려/회수면 폼. 폼: 맨 위 읽기 행(`회수 MM-DD HH:mm` 또는 `반려 {결재자} · MM-DD HH:mm` + 2행 `사유 · {원문}` — 04.1 선례 컴포넌트), 1차 라벨 `지출결의 다시 제출`, 결재선 한 줄은 지금 다시 해석한 모양. 제출 · 다시 제출 성공 토스트에 3차 `되돌리기`(→ `withdrawExpenseAction` undo · 확인 없음 · 성공 = 토스트 `되돌리기 · 결재 멈춤` + 폼으로 다시 그림 · 거부 = 오류 토스트(닫을 때까지) 문구는 must_haves, 승인 갈래 3차 `문서에서 회수` → 문서 화면 · 실패 = `되돌리기 실패 · 되돌리기`).
④ behavior 통합을 `test/integration/expense-approval-lifecycle.test.ts` describe `회수 뒤 같은 번호 다시 제출`에, E2E를 새 스펙 `test/e2e/expense-undo-resubmit.spec.ts`에 먼저 적는다. E2E의 토스트 행동은 Playwright 내장 `page.clock`(`install` → 토스트가 보이면 `pauseAt` → 누름 · 단언 → `resume`)으로 시간을 쥐고 누른다 — 고정 시간 대기 · 앱 코드나 `ui/toast`에 테스트 전용 지속 시간 설정을 넣지 않는다(새 의존성 0 · Phase 4 `revenue-section.spec.ts`의 `page.clock` 선례, Addresses review concern: P3-12(Round 2)).
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-lifecycle.test.ts test/integration/approvals-extensions.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 회수 뒤 다시 제출이 새 번호 · 새 인스턴스를 만들거나 차수가 2가 아니거나, 스냅숏이 옛 금액으로 남거나, 승인 뒤 undo 회수가 통과하거나 오류에 처리자 · 시각 · 상태가 없거나, 옛 문서 version 다시 제출 · 옛 인스턴스 version 문서 화면 회수가 통과하면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-undo-resubmit.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 되돌리기에 확인 창이 뜨거나, 폼으로 돌아오지 않거나, 다시 제출 번호가 바뀌거나, 토스트 문구가 원문과 다르면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "resubmitDocument" domain/expenses/index.ts`가 1 이상이고 `grep -c "isEditableByDrafter" domain/expenses/index.ts`가 2 이상이다
    - `domain/approvals/conflict-message.ts`를 고쳤다면 diff가 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-09.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/approvals/conflict-message.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - `grep -c "expectedInstanceVersion" domain/expenses/index.ts`가 1 이상이다(문서 version과 인스턴스 version 이름 분리 — P3-7(Round 2))
    - `grep -c "page.clock" test/e2e/expense-undo-resubmit.spec.ts`가 1 이상이고 `grep -c "waitForTimeout" test/e2e/expense-undo-resubmit.spec.ts`가 0이다(P3-12(Round 2))
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>제출 직후 되돌려 고치고 같은 번호로 다시 올리는 길이 화면과 테스트로 선다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 반려 뒤 다시 제출 · 본인 승인 · 결재선 고정(설정 변경 뒤) · 문서 화면 회수 확인 · 작성 중 삭제와 되돌리기</name>
  <files>domain/expenses/index.ts, repositories/expenses.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, app/(app)/expenses/[id]/expense-form.tsx, app/(app)/expenses/[id]/expense-document.tsx, test/integration/expense-approval-lifecycle.test.ts, test/e2e/expense-undo-resubmit.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S3 머리 줄(398–400행) · S7 행동 줄 · Copywriting 280–281행(반려 · 회수 확인 문구)
    - 04.1 S6 반려/회수 확인 컴포넌트(`ui/confirm-dialog` 사용 — 04.1-05) · 04.1 결재 설정 픽스처(`upsertSimpleValue`)
    - Task 1 산출물
  </read_first>
  <behavior>
    - (통합) 팀장 반려(사유 `금액 확인`) → 기안자 폼 편집 가능 · 다시 제출 → 같은 번호 · 차수 2 · 차수 1의 팀장 `rejected` 기록 · 사유 보존
    - (통합) 팀장이 기안자면(자기 승인 기본 `self_approve`) 제출 뒤 1단 후보 = 기안자 · 기안자 승인 → 단계 `self_approved` 참 · 결재선 보기 낱말 `본인 승인` · 다음 단계로
    - (통합) 제출 뒤 `approval_route.expense.step2.role_id`를 다른 계급으로 바꿔도 그 문서의 단계 행 불변 · 새 문서는 새 계급 · 그 문서를 반려 뒤 다시 제출하면 새 설정으로 재고정
    - (통합) `deleteExpenseDraft` → `deleted_at` 있음 · 목록 · `getExpense`에서 사라짐 · `restoreExpenseDraft` → 되돌아옴 · 번호 있는 문서 삭제는 거부
    - (E2E, desktop) 작성 중 폼 머리 줄 `지출결의 삭제` → 확인 창 없음 · `/expenses`로 이동 · 토스트 `지출결의 삭제` + `되돌리기` → 누르면 폼으로(토스트가 보이면 `page.clock.pauseAt`으로 멈추고 누른 뒤 `resume` — Task 1과 같은 방식, P3-12). 결재 중 문서 화면 `회수` → 04.1 확인 제목 `지출결의 회수` · 결과 줄에 `팀장 승인 기록은 남음 · 다시 제출 때 첫 단계부터`(팀장 승인 뒤) → `회수 Ctrl+Enter` → 폼. 팀장 기안 문서의 결재 1단 = 문서 화면 `승인` + `회수`(반려 없음), 제출 토스트 `지출결의 제출 · 본인 승인 차례`
  </behavior>
  <action>
① `domain/expenses/index.ts`: `deleteExpenseDraft(viewer, { expenseId, expectedVersion })`(기안자 · 번호 없음 · 소프트 삭제 + `document_delete` 로그 같은 tx) · `restoreExpenseDraft(viewer, { expenseId })`(기안자 · 삭제된 작성 중만 — 같은 줄에 새 작성 중 문서가 생겨 부분 UNIQUE가 막으면 복원하지 않고 그 문서 id를 돌려 화면이 그 문서를 연다). 리포지토리 `softDeleteDraft` · `restoreDraft`. 결재 관련 판정(반려 · 본인 승인 · 결재선 고정)은 04.1 엔진 그대로 — 이 task는 지출결의로 다시 증명하는 사례와 화면 배선이다.
② 액션 `deleteExpenseDraftAction` · `restoreExpenseDraftAction`(+ 등록). 폼 머리 줄 오른쪽 2차 `지출결의 삭제`(작성 중에만 · 폰 숨김) → 즉시 삭제 → `/expenses` + 토스트 되돌리기 → 복원 → 폼. 문서 화면 행동 줄은 04.1 분기표를 그대로 쓰되 회수는 04.1 S6 확인(제목 `지출결의 회수` · 부제 · 결과 줄 — 문서 종류 이름만 이 페이즈 문구). 본인 승인 차례면 제출 결과 자리에 1차 `승인 Ctrl+Enter`(결과 글자 없음) · 토스트 `지출결의 제출 · 본인 승인 차례`.
③ behavior 통합을 `test/integration/expense-approval-lifecycle.test.ts` describe 셋(`반려 뒤 다시 제출` · `본인 승인` · `결재선 고정`)과 한 describe(`작성 중 삭제`)로, E2E를 `test/e2e/expense-undo-resubmit.spec.ts`에 덧붙인다 — 토스트 되돌리기는 Task 1 ④의 `page.clock` 방식 그대로(Addresses review concern: P3-12(Round 2)).
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-lifecycle.test.ts test/integration/expense-create-idempotency.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 반려 뒤 다시 제출이 번호를 바꾸거나 이전 차수 기록을 지우거나, 본인 승인 낱말 · `self_approved`가 없거나, 설정 변경이 제출된 문서의 단계를 바꾸거나, 번호 있는 문서가 삭제되거나, 복원이 부분 UNIQUE를 깨면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-undo-resubmit.spec.ts test/e2e/expense-list.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 작성 중 삭제에 확인 창이 뜨거나 되돌리기가 없거나, 문서 화면 회수에 확인이 없거나 결과 줄이 다르거나, 본인 승인 행동 줄에 `반려`가 있으면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "deleted_at" repositories/expenses.ts`가 1 이상이고 `deleteExpenseDraft`가 번호 있는 문서를 거부하는 사례가 통합에 있다(`grep -c "deleteExpenseDraft" test/integration/expense-approval-lifecycle.test.ts`가 1 이상)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>반려 · 본인 승인 · 결재선 고정이 지출결의로 다시 증명되고, 작성 중 문서는 확인 없이 지우고 되돌린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 제출 뒤 증빙 추가 · 마지막 아닌 파일 삭제(E4 배선) · 승인 뒤 무효 처리(마지막 파일 포함 · 해제 없음 · 동시 처리 문구 · 무효 행 접근성) · 무효 뒤 기안자 신호 원천(G1) · 결재 동시 조작 두 순서 셋(승인↔회수 · 승인↔반려 · 증빙 추가↔승인)</name>
  <precondition>05-01 E4 증빙 변경 충돌 문구가 추가 · 삭제 공통 중립 문구 `증빙을 바꿈 · 새로 고침`이다 — `grep -c "증빙을 바꿈" domain/approvals/conflict-message.ts`가 1 이상. 아니면 코드 변경 없이 멈추고 오케스트레이터에 알린다(문구 원천은 05-01 — 리뷰 Round 2 P3-3)</precondition>
  <files>domain/evidence/index.ts, domain/evidence/signals.ts, domain/approvals/conflict-message.ts, repositories/files.ts, domain/permissions/menus.ts, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, ui/attachments/Attachments.tsx, ui/attachments/Attachments.module.css, app/(app)/expenses/[id]/expense-document.tsx, test/integration/fixtures/expenses.ts, test/integration/expense-approval-concurrency.test.ts, test/integration/evidence-void.test.ts, test/e2e/expense-undo-resubmit.spec.ts</files>
  <read_first>
    - 05-04 `domain/evidence/index.ts`(상태 판정 · 트랜잭션 쓰기 순서) · 05-01 `bumpInstanceVersion` · 충돌 문구 증빙 갈래
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S4 파일 행 3차 표 · 「무효 처리」 · 「무효 행 표시」 · 「무효 행 접근성」(Round 3) · Copywriting 「거부 — 동시 처리」 · 「Error — 증빙 무효 처리」 · UI Considerations 「Round 3 추가」 표 — 줄 번호 대신 절 이름으로 `grep -n` 뒤 범위 Read
    - `domain/approvals/conflict-message.ts`(05-01 E4 문구 · 04.1 조사 헬퍼 · `HH:MM` 서식 — `grep -n "function\|export"` 뒤 범위)
    - 04.1 `test/integration/approvals-concurrency.test.ts`(두 순서 경합 도우미 · 단언 모양) · `test/integration/lock-race.ts`
  </read_first>
  <behavior>
    - (통합) 결재 중 문서에 기안자가 증빙을 더하면 파일 행 +1 · 인스턴스 상태 불변 · version +1 · `version_reason` `evidence`. 파일이 둘일 때 하나 삭제 → 성공 · version +1. 파일이 하나일 때 삭제 → 거부(파일 행 · version 불변). 결재자 · 무관한 사람의 추가 · 삭제 → 404
    - (통합, 승인 뒤 — U1) 최종 승인된 문서에 기안자가 증빙 추가 → 성공 · version +1 · 상태 `approved` 그대로. 파일이 둘이어도 삭제 → 거부 · 파일 행 · version 불변. `evidenceActions.deletableFileIds`가 빈 배열
    - (통합, 무효 처리 — U1 결정) 픽스처가 테스트 계급 「경영관리」를 만들고 `expenses.evidence_void` 쓰기를 준 사람(전사 업무 범위 — 문서가 보임)이 승인된 문서의 파일에 `voidEvidence({ fileId, reason: "다른 건 영수증" })` → 성공 · `voided_at` · `voided_by` · `void_reason` 있음 · 파일 행 · 저장소 객체 그대로 · 행동 로그 `document_update` detail `{ change: "evidence_void", fileId, reasonLength: 7 }` 한 건(사유 원문 없음) · `countActiveByOwner`에서 빠짐 · `listEvidence`에 `voidedAt` 있음. 기안자(권한 없음) · 결재자(팀장 · 대표 — 권한표에 없음) → 거부 · 행 불변. 사유 빈 칸 · 공백만 → 04.1 반려 사유 검증 오류와 같은 문구(04.1이 export한 상수와 정확히 같음을 단언 — 리터럴 복제 없음) · 행 불변. 이미 무효인 파일 → 거부 · 문구가 정확히 `{처리자}이/가 HH:MM에 무효 처리함 · 새로 고침` 꼴(픽스처 처리자 이름으로 — 정규식 `^{이름}(이|가) \d{2}:\d{2}에 무효 처리함 · 새로 고침$` · design-review D9) · 행 불변. 결재 중 문서의 파일 → 거부(무효 처리는 승인 뒤만). 같은 사람의 승인 문서 `removeEvidence` → 여전히 거부. 시드 직후에는 어떤 시드 계급에도 `expenses.evidence_void`가 없다
    - (통합, 마지막 파일 — 사용자 결정 G2) 살아 있는 증빙이 **1개**인 승인 문서에서 권한자가 그 파일을 `voidEvidence` → 성공 · `countActiveByOwner` 0 · 파일 행 · 객체 그대로 · 로그 1건(마지막 파일 판정으로 막지 않는다)
    - (통합, 해제 없음 · 다시 올리기 — 사용자 결정 G3) 무효 처리한 뒤 기안자가 **같은 sha256** 파일을 같은 문서에 `requestEvidenceUpload` → 중복 거부 없이 의도가 열리고 완료하면 새 파일 행(살아 있음) · 옛 무효 행은 그대로(`voided_at` 유지). 도메인 · 리포지토리에 무효를 지우는 함수가 없다(acceptance grep)
    - (통합, 무효 뒤 신호 원천 — 사용자 결정 G1, `test/integration/evidence-void.test.ts` describe `무효 뒤 신호`) `listEvidenceVoidSignals(viewer, { ownerKind: 지출결의, ownerIds: [A, B, C] })`: A(무효 1 · 살아 있는 1이 무효보다 먼저 올림) → 포함 · 그 뒤 A에 새 증빙 추가 → 빠짐 · 그 뒤 A의 다른 파일을 또 무효 → 다시 포함(가장 늦은 무효 > 가장 늦은 살아 있는 파일) · B(마지막 파일까지 무효 — G2, 살아 있는 0) → 포함 · C(무효 없음) → 빠짐 · 지운 파일(`removed_at`)만 있는 주인 → 빠짐 · `ownerIds` 빈 배열 → 빈 결과
    - (통합, 두 순서 ①) 팀장이 version V를 읽은 뒤 기안자가 증빙 추가 → 팀장 V로 승인 → `ApprovalConflictError` · 문구 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침` · 단계 기록 0건. 팀장이 새로 읽고 승인 → 성공
    - (통합, 삭제↔삭제 두 순서 — B2 05-09 몫) 결재 중 문서 · 살아 있는 증빙 2개(A · B)에서 기안자의 A 삭제와 B 삭제를 `deps.afterLock`으로 두 순서(A 먼저 · B 먼저) 고정 → 먼저 잠근 쪽만 성공하고 다른 쪽은 잠금 뒤 센 살아 있는 파일 수가 1이라 마지막 파일 거부(`UserFacingError`) · 결과 살아 있는 증빙 1(≥ 1) · 인스턴스 version +1 한 번 · `document_update` 로그 1건 · 원시 DB 오류 없음
    - (통합, 삭제 뒤 옛 version 승인 — P3-3) 파일 둘인 결재 중 문서에서 팀장이 version V를 읽은 뒤 기안자가 하나 삭제 → 팀장 V로 승인 → `ApprovalConflictError` · 문구가 추가 때와 같은 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침` · 단계 기록 0건
    - (통합, 본인 증빙 추가 뒤 되돌리기 · 다시 제출 — P3-7) 제출(차수 1) 뒤 기안자가 증빙을 더해 인스턴스 version +1 → `withdrawExpense({ expenseId, undo: true, round: 1 })` 성공(스스로 충돌 없음) → 회수 상태에서 증빙을 하나 더 더하고 공급가액을 고친 뒤 `submitExpense({ expenseId, expectedVersion: 지금 문서 version })` → 성공 · 차수 2 · 같은 번호
    - (통합, 두 순서 ②) 팀장 승인이 먼저 커밋 → 기안자 증빙 추가 성공 · 인스턴스 상태 · 단계 기록은 승인 결과 그대로
    - (통합, 승인↔회수 두 순서) `afterLock`으로 순서를 고정 — 승인 먼저면 회수(옛 version)가 04.1 충돌 문구로 거부, 회수 먼저면 승인이 거부. 둘 다 원시 DB 오류가 아니다
    - (통합, 승인↔반려 두 순서) 1단 후보가 둘(팀장 둘)인 결재선에서 한 명 승인 · 한 명 반려를 두 순서로 — 먼저 커밋한 쪽만 기록되고 진 쪽은 04.1 충돌 문구
    - (E2E, desktop) 결재 중 문서 화면에서 기안자가 「하나 더」로 파일을 더함 → 파일 행 둘 · 첫 파일에 `삭제`가 있고 파일이 하나일 때는 `삭제`가 없다. 결재자의 문서 화면에는 `크게 보기`만 있고 「하나 더」가 없다. 승인된 문서 화면에서 기안자에게는 「하나 더」만 있고 어떤 파일 행에도 `삭제` · `무효 처리`가 없다. 경영관리 권한자가 `무효 처리` → 사유 창(사유 빈 칸이면 확인 비활성 + 이유) → 사유 입력 뒤 확인 → 그 파일 행 파일명에 `text-decoration: line-through` · 「무효」 상태 태그 · 2행 `무효 · {처리자} · …`가 보이고 `무효 처리` 버튼이 사라진다(U1 결정). 확인 뒤 토스트가 뜨지 않는다(`role="status"` 토스트 영역에 `증빙 무효` 글자 0 — D10)
    - (E2E, desktop — 무효 행 접근성 D6) 무효 행에서 「무효」 태그 요소가 파일명 요소보다 DOM 앞에 있다(`compareDocumentPosition` — `DOCUMENT_POSITION_FOLLOWING`) · 파일명 요소의 `aria-describedby`가 가리키는 요소의 글자가 `무효 · `로 시작한다 · 첨부 목록의 `aria-live="polite"` 영역 안에 무효 행이 있다
    - (E2E, 새 컨텍스트 375×800 — D6) 권한자의 승인 문서 화면에서 살아 있는 파일 행의 `크게 보기` · `무효 처리` 두 버튼 상자 높이가 각각 44 이상이고 두 상자 사이 간격(가로면 왼쪽 버튼 오른쪽 끝 → 오른쪽 버튼 왼쪽 끝, 세로면 위 버튼 아래끝 → 아래 버튼 위끝)이 16 이상이다(`boundingBox`로 잰다)
  </behavior>
  <action>
① `domain/evidence/index.ts`: 추가 허용 상태에 결재 중(`submitted` · `in_review`) · 승인을 더하고(기안자만), **삭제(`removeEvidence`) 허용 상태에는 결재 중만 더한다 — 승인(`approved`)은 추가만, 삭제는 서버가 `UserFacingError`로 거부**(Addresses review concern: U1 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리; 05-04 작성 중/반려/회수 목록은 그대로), 그 상태의 추가 · 삭제 트랜잭션은 05-04가 tx 첫 단계에 둔 `lockExpenseForUpdate` 뒤에서(리뷰 Round 2 B2 — 이 플랜은 잠금을 새로 만들지 않고 그 뒤에 잇는다) 인스턴스 상태로 허용 상태를 다시 판정하고(잠금 전 판정은 화면용), 파일 쓰기 뒤 같은 tx에서 `bumpInstanceVersion(viewer, { instanceId, updatedBy: viewer.userId, reason: "evidence" }, tx)`(잠금 순서: 지출결의 행 → 인스턴스 — 추가 · 삭제 모두 같은 reason이라 결재자 충돌 문구는 05-01 E4의 중립 문구 하나, Addresses review concern: P3-3(Round 2)). 삭제는 제출 뒤이면 그 잠금 아래에서 센 `countActiveByOwner(tx)` > 1일 때만. 파일 행 3차 규칙(보는 사람 · 상태 — 승인 문서는 `deletableFileIds` 빈 배열)을 문서 DTO의 `evidenceActions: { canAdd, deletableFileIds }`로 서버가 보내고, `Attachments` 읽기 모드가 그 값대로 「하나 더」 · `삭제` · `크게 보기`를 그린다(클라이언트 추론 없음).
② **무효 처리(Addresses review concern: U1 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리)** `domain/permissions/menus.ts` `MENUS`의 `expenses` 무리 끝에 `{ key: "expenses.evidence_void", label: "증빙 무효 처리" }` 한 줄(05-08 `expenses.team`과 같은 모양 · 시드 없음 — 주석 한 줄로 `projects.adjustment` 선례를 가리킨다). `repositories/files.ts`에 `markVoided(viewer, { id, voidedBy, reason }, tx)`(`voided_at IS NULL AND removed_at IS NULL` 조건 UPDATE — 0행이면 null). `domain/evidence/index.ts`에 `voidEvidence(viewer, { fileId, reason }, deps?)` — 트랜잭션 전: 파일 · 주인 문서 로드 · `canSeeExpense`(못 보면 404) · `can(viewer, "expenses.evidence_void", "write")`(아니면 `ForbiddenError`) · 인스턴스 상태 `approved`(아니면 `UserFacingError` `승인 뒤에만 무효 처리`) · 사유 trim 1~500(04.1 반려 사유 검증 함수 재사용). `withTransaction` 안: `markVoided`(null이면 — 이미 무효 — 같은 tx로 그 파일 행의 `voided_by` · `voided_at`을 읽어 동시 처리 문구로 거부, 아래 D9) → `recordActionInTx(document_update, detail { change: "evidence_void", fileId, reasonLength })`. **살아 있는 파일 수는 보지 않는다 — 마지막 살아 있는 파일도 무효 처리된다**(Addresses review concern: G2(Round 3) — 사용자 결정). **무효를 되돌리는 함수 · 리포지토리 갱신 · 액션 · 버튼을 만들지 않는다**(Addresses review concern: G3(Round 3) — 사용자 결정, 잘못 무효로 했으면 같은 파일을 다시 올린다: 무효 행은 05-04 「살아 있는 행」 조건 밖이라 중복 검사에 걸리지 않는다). **동시 처리 문구(Addresses review concern: D9(Round 3))**: `domain/approvals/conflict-message.ts`에 순수 함수 `buildEvidenceVoidedMessage({ actorName, at })` → `{이름}이/가 HH:MM에 무효 처리함 · 새로 고침`을 **덧붙인다**(04.1 조사 헬퍼 · 서울 `HH:MM` 서식을 같은 파일 안에서 재사용 — 기존 줄 불변, Task 1 ②와 같은 덧붙임 원칙). 문서 DTO `evidenceActions`에 `voidableFileIds`를 더한다(승인 ∧ 권한 ∧ 살아 있는 파일). 액션 `voidEvidenceAction({ fileId, reason })`(+ 등록). `Attachments` 읽기 모드(Addresses review concern: D6(Round 3) — UI-SPEC S4 「무효 행 접근성」): `voidedAt`이 있는 행은 DOM · 보이는 순서 `ui/status-tag` 「무효」(`--muted`) → 파일명(취소선 — CSS 모듈 · 기존 토큰, 요소 `aria-describedby` = 2행 id) → 2행 `무효 · {처리자} · MM-DD HH:mm · {사유}`(`--fs-sm --muted`), 결과는 기존 첨부 `aria-live="polite"` 영역 안에서 바뀐다(새 live 영역 없음). 폰(<700) 파일 행 3차 `크게 보기` · `무효 처리`는 각각 높이 `--touch-min` 영역 · 사이 `--s-4`(기존 토큰만). `voidableFileIds`의 행에 3차 `무효 처리` → 04.1 S6 반려 사유 확인 창과 같은 컴포넌트(제목 `증빙 무효 처리` · 부제 `{파일명}` · 사유 칸 · 확인 `무효 처리` — 되돌릴 수 없어 확인을 거친다, CLAUDE.md §7). 누르는 동안 1차 `무효 처리…` · 나머지 `aria-disabled`, 네트워크 · 서버 실패는 사유 칸 아래 `무효 처리 실패 · 다시 시도` · 서버 거부 문구(D9 · `승인 뒤에만 무효 처리`)도 같은 자리 · 창은 닫히지 않고 사유 보존(Addresses review concern: D5(Round 3)). 성공하면 확인 창이 닫히고 `router.refresh()`로 그 행이 무효 행으로 바뀐다 — **성공 토스트는 띄우지 않는다**(Addresses review concern: D10(Round 3) — 결과가 행동한 자리에 보이고 `aria-live`가 읽는다, §7-6 · §7-7 SUCCESS). 문구 · 모양의 정본은 UI-SPEC S4 · Copywriting 「Error — 증빙 무효 처리」(Round 3 반영 — 가정 #8 대체 완료).
③ **무효 뒤 기안자 신호 원천(Addresses review concern: G1(Round 3) — 사용자 결정)**: 새 파일 `domain/evidence/signals.ts` — `listEvidenceVoidSignals(viewer, { ownerKind, ownerIds })` → `string[]`(신호가 있는 주인 id). 이 파일은 `repositories/files.ts`만 import한다(도메인 import 없음 — 05-10의 `domain/expenses/index.ts`가 순환 없이 부른다, `test/unit/import-cycles.test.ts` 녹색). `ownerIds`가 비면 리포지토리를 부르지 않고 빈 배열. `repositories/files.ts`에 `findUnresolvedVoidOwnerIds(viewer, { ownerKind, ownerIds }, tx?)` — 한 SQL: 그 주인 종류 · id 목록의 파일을 주인 id로 묶어 `max(voided_at)`이 있고(무효 파일 ≥ 1) 그 값이 `max(created_at) FILTER (살아 있는 행 = removed_at IS NULL AND voided_at IS NULL)`보다 나중이거나 살아 있는 행이 0인 주인 id만(지운 행은 두 집계 어디에도 들지 않는다). 기안자 판정 · 문서 상태는 이 함수가 보지 않는다 — 호출자(05-10 지출결의 종류)가 기안자 = viewer인 승인 문서 id만 넘긴다.
④ `test/integration/fixtures/expenses.ts`의 `attachEvidence` 도우미를 재사용해 새 파일 `test/integration/expense-approval-concurrency.test.ts`에 behavior 통합 다섯 묶음과 삭제↔삭제 두 순서(Addresses review concern: B2(Round 2) — 결재 중 삭제는 이 플랜이 열므로 05-04가 넘긴 사례, 05-04 `removeEvidence`의 `afterLock` 주입 지점을 쓴다)를 적는다(sleep 없음 · `afterLock` 또는 04.1 경합 도우미). `bumpInstanceVersion`을 붙인 뒤에도 05-04가 만든 `test/integration/tx-safety.test.ts` describe `풀 2 · 동시 증빙 추가 셋(05-04)`이 초록인지 verify가 함께 돌린다(그 파일은 고치지 않는다 — 깨지면 tx 안 풀 읽기가 섞인 것이니 이 플랜 코드를 고친다). 무효 처리 사례(G2 마지막 파일 · G3 같은 파일 다시 올리기 · D9 문구 포함)와 describe `무효 뒤 신호`(G1)는 새 파일 `test/integration/evidence-void.test.ts`(테스트 계급 · 권한 행은 도메인 · 리포지토리 함수로 만든다 — SQL 직접 삽입 금지 · 시각 순서는 `deps.now` 주입). E2E는 `test/e2e/expense-undo-resubmit.spec.ts` describe `제출 뒤 증빙`(D6 · D10 사례 포함 — 폰 측정은 `browser.newContext({ viewport: { width: 375, height: 800 } })`).
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-concurrency.test.ts test/integration/evidence-void.test.ts test/integration/evidence-upload.test.ts test/integration/approvals-concurrency.test.ts test/integration/leak-scan.test.ts test/integration/tx-safety.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 원시 DB 오류(23505 · 40001)가 테스트 출력에 나온다 — 증빙 변경 뒤 옛 version 승인이 통과하거나, 마지막 파일 삭제 · 승인 문서의 파일 삭제가 통과하거나, 권한 없는 사람(기안자 포함) · 사유 없는 무효 처리가 통과하거나 무효 처리가 파일 행을 지우거나 로그 detail에 사유 원문이 실리거나, 결재자가 증빙을 더하거나, 두 순서 중 하나에서 둘 다 성공하거나, 삭제 뒤 옛 version 승인의 문구가 추가 때와 다르거나, 기안자 본인의 증빙 추가 뒤 되돌리기 · 다시 제출이 충돌로 거부되거나, 결재 중 서로 다른 파일 동시 삭제 두 순서 중 하나에서 둘 다 성공해 증빙이 0개가 되거나, `bumpInstanceVersion`을 붙인 뒤 `tx-safety`의 `풀 2 · 동시 증빙 추가 셋(05-04)`이 멈추거나 원시 풀 오류를 내면(B2 · M3), 마지막 살아 있는 파일의 무효 처리가 거부되거나(G2), 무효 뒤 같은 sha256 다시 올리기가 중복으로 거부되거나(G3), 이미 무효 거부 문구가 처리자 · `HH:MM` 꼴이 아니거나(D9), 무효 뒤 신호 사례(새 증빙 뒤 빠짐 · 다시 무효 뒤 포함 · 살아 있는 0 포함 · 지운 파일만 제외) 중 하나라도 다르면(G1 — Round 3) 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-undo-resubmit.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 마지막 파일에 `삭제`가 보이거나 결재자 화면에 「하나 더」가 보이거나, 무효 행에 취소선 · 「무효」 태그가 없거나 기안자에게 `무효 처리`가 보이거나, 「무효」 태그가 파일명보다 DOM 뒤에 있거나 파일명 `aria-describedby`가 2행을 가리키지 않거나, 폰 `크게 보기` · `무효 처리` 높이가 44 미만 · 사이가 16 미만이거나, 무효 처리 뒤 토스트가 뜨면(D6 · D10 — Round 3) 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "bumpInstanceVersion" domain/evidence/index.ts`가 1 이상이다
    - 승인 문서 삭제 거부 사례가 통합 파일에 있다(`grep -c "승인 뒤" test/integration/expense-approval-concurrency.test.ts`가 1 이상 — U1)
    - `grep -cE "setTimeout|sleep\(" test/integration/expense-approval-concurrency.test.ts`가 0이다
    - `grep -c "증빙을 바꿈" test/integration/expense-approval-concurrency.test.ts`가 2 이상이고 `grep -c "증빙을 더함" test/integration/expense-approval-concurrency.test.ts`가 0이다(추가 · 삭제 공통 중립 문구 — P3-3(Round 2))
    - `grep -c "round: 1" test/integration/expense-approval-concurrency.test.ts`가 1 이상이다(본인 증빙 추가 뒤 되돌리기 사례 — P3-7(Round 2))
    - `grep -c "afterLock" test/integration/expense-approval-concurrency.test.ts`가 1 이상이고 삭제↔삭제 두 순서 사례가 있다(`grep -c "삭제↔삭제" test/integration/expense-approval-concurrency.test.ts`가 1 이상 — B2(Round 2))
    - `grep -c "풀 2 · 동시 증빙 추가 셋(05-04)" test/integration/tx-safety.test.ts`가 1 이상이고 첫 검증 명령(그 파일 포함)이 초록이다 · `. "$(git rev-parse --git-dir)/gsd-05-09.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- test/integration/tx-safety.test.ts`가 비어 있다(05-04 사례를 고치지 않음)
    - `grep -c '"expenses.evidence_void"' domain/permissions/menus.ts domain/evidence/index.ts`가 두 파일 모두 1 이상이고, `grep -rn "evidence_void" domain/seed`가 0건이다(시드 없음 — 권한표에서 배정) · `grep -rnE "경영관리" domain/evidence`가 0건(역할 이름 없음)
    - `grep -c '"evidence_void"' domain/evidence/index.ts`가 1 이상이고 `grep -rn '"evidence_void"' domain/action-log`가 0건(행동 종류가 아니라 detail 값)
    - `grep -c "line-through" ui/attachments/*.css`가 1 이상이고 `grep -c "무효" test/e2e/expense-undo-resubmit.spec.ts`가 1 이상이다
    - (D9 Round 3) `grep -c "buildEvidenceVoidedMessage" domain/approvals/conflict-message.ts`와 `grep -c "buildEvidenceVoidedMessage" domain/evidence/index.ts`가 둘 다 1 이상이고 `grep -c "이미 무효 · 새로 고침" domain/evidence/index.ts`가 0이다 · conflict-message.ts diff가 덧붙임뿐이다(Task 1 acceptance의 삭제 줄 0 명령 그대로)
    - (G3 Round 3) 해제 경로가 없다 — `grep -rniE "unvoid|clearvoid|restorevoid" domain repositories app ui`가 0건이다
    - (G1 Round 3) `grep -c "export async function listEvidenceVoidSignals\|export function listEvidenceVoidSignals" domain/evidence/signals.ts`가 1이고, 런타임 import가 리포지토리뿐이다(`grep -E '^import ' domain/evidence/signals.ts | grep -v '^import type' | grep -vc '@/repositories/'`가 0) · `grep -c "무효 뒤 신호" test/integration/evidence-void.test.ts`가 1 이상이다
    - (D6 · D10 Round 3) `grep -c "aria-describedby" test/e2e/expense-undo-resubmit.spec.ts`와 `grep -c "boundingBox" test/e2e/expense-undo-resubmit.spec.ts`가 둘 다 1 이상이다 · 무효 처리 성공 토스트 코드가 없다(실행자가 `voidEvidenceAction` 성공 갈래가 `router.refresh()`만 부름을 SUMMARY에 한 줄)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>제출 뒤 증빙이 바뀌면 결재자는 새로 본 뒤에만 승인하고, 결재 동시 조작 두 순서가 지출결의로 증명된다. 승인 뒤 무효 처리는 마지막 파일까지 되고 해제가 없으며, 무효 행은 스크린리더 · 폰에서도 읽히고 눌리며, 무효 뒤 기안자 신호의 원천이 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 기안자 → 제출된 문서 | 제출 뒤 기안자가 바꿀 수 있는 것은 증빙뿐이다 |
| 결재자 화면(연 시점) → 승인 순간 | 결재자가 본 문서와 승인 대상이 같아야 한다 |
| 토스트 되돌리기 → 회수 | 이미 처리된 문서를 조용히 되돌리면 받은 승인이 사라진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-901 | Tampering | 증빙 변경 ↔ 승인 경합 | high | mitigate | 추가 · 삭제가 같은 tx에서 인스턴스 version을 올림(E4) — 두 순서 통합 사례 |
| T-05-902 | Tampering | 늦은 되돌리기로 받은 승인 무효화 | medium | mitigate | undo는 토스트의 차수 + 서버가 회수 직전에 읽은 지금 차수 처리 기록으로 판정하고 그 읽기의 인스턴스 version을 04.1 조건 UPDATE에 건다(리뷰 Round 2 P3-7 — 클라이언트 인스턴스 version 불사용) — 처리된 뒤면 거부 + `문서에서 회수`(04.1 S6 확인) |
| T-05-903 | Elevation of Privilege | 남의 문서 회수 · 삭제 · 복원 · 증빙 변경 | high | mitigate | 도메인 첫 줄 기안자 판정(04.1 회수 규칙 · `isEditableByDrafter` · 증빙 주인 판정) — 결재자 · 무관자 404 사례 |
| T-05-904 | Repudiation | 삭제 · 복원 · 증빙 변경 기록 | low | mitigate | `document_delete` · `document_update` 로그를 같은 tx에 |
| T-05-906 | Tampering | 번호 있는 반려 · 회수 문서의 프로젝트 · 종류 이동(F6) | medium | mitigate | 05-07 `changeExpenseLine` 규칙 — Task 1 거부 사례(다른 프로젝트 줄 · 팀 비용 → 줄) |
| T-05-908 | Elevation of Privilege / Repudiation | 증빙 무효 처리 권한 · 기록(U1 결정) | medium | mitigate | 메뉴 권한 `expenses.evidence_void` 쓰기 ∧ 문서 보임 · 승인 상태만 · 사유 필수 · 파일 행 보존 · `document_update` detail `evidence_void`(사유 길이만) 같은 tx — 기안자 · 결재자 거부 · 사유 없음 거부 통합 사례 · 시드 부여 0 · 해제 경로 0(G3 grep) |
| T-05-909 | Information Disclosure | 이미 무효 거부 문구의 처리자 이름 · 시각(D9) | low | accept | 문구를 받는 사람은 무효 권한 ∧ 문서 보임을 통과한 사람뿐이고, 같은 사실(처리자 · 시각 · 사유)이 그 문서의 무효 행 2행에 이미 보인다 — 새로 드러나는 정보 없음 |
| T-05-907 | Repudiation / Tampering | 승인 뒤 증빙 삭제로 결재 근거 축소(U1) | medium | mitigate | 승인 상태 삭제 서버 거부 · `deletableFileIds` 빈 배열 — Task 3 사례(사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리) |
| T-05-905 | Tampering | 다시 제출이 새 번호 · 새 인스턴스 | medium | mitigate | 번호 있는 문서는 번호 부여를 건너뛰고 `resubmitDocument`만 — 카운터 불변 단언 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(verification의 lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과
- 통합: `expense-approval-lifecycle` · `expense-approval-concurrency` · `evidence-upload` · `approvals-*` 녹색
- `CI=true` E2E: `expense-undo-resubmit` · `expense-list` 녹색
- `. "$(git rev-parse --git-dir)/gsd-05-09.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml docs/design/tokens.css` 비어 있음
</verification>

<success_criteria>
- 제출 직후 되돌려 고치고 같은 번호로 다시 올린다(확정 #2)
- 반려 · 본인 승인 · 결재선 고정 · 동시 조작 두 순서가 지출결의로 다시 증명된다(기준 2 · 3)
- 증빙이 바뀐 문서는 결재자가 다시 본 뒤에만 승인된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `submitExpense` 다시 제출 갈래 · `withdrawExpense`(undo) · `deleteExpenseDraft` · `restoreExpenseDraft` · `isEditableByDrafter` · 증빙 제출 뒤 규칙(`bumpInstanceVersion` 배선) · 문서 DTO `evidenceActions` · `voidEvidence` · `listEvidenceVoidSignals`(`domain/evidence/signals.ts` — G1) · `findUnresolvedVoidOwnerIds` · `buildEvidenceVoidedMessage`(D9) |
| 오류 필드 | `ApprovalConflictError.detail`(필요할 때만 덧붙임) |
| 액션 | `withdrawExpenseAction` · `deleteExpenseDraftAction` · `restoreExpenseDraftAction` |
| 테스트 | `test/integration/expense-approval-concurrency.test.ts` · `expense-approval-lifecycle`(describe 다섯 추가) · `test/e2e/expense-undo-resubmit.spec.ts` |

<output>
Create `.planning/phases/05-expense-approval-leave/05-09-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F6 — 편집 가능 상태를 반려 · 회수까지 넓히면 번호 받은 문서가 다른 프로젝트 줄 · 팀 비용으로 옮겨갈 수 있음(L24@1108e20) | MEDIUM | Plan 05-09, Task 1 ① · behavior: 05-07 `changeExpenseLine` 규칙(같은 프로젝트 줄만 · 팀 비용 문서 유지)을 반려 · 회수 문서로 증명(다른 프로젝트 줄 거부 · 같은 프로젝트 성공 · `T26` 문서 → 줄 거부) · prohibition · T-05-906 |
| U1 — 승인된 지출결의에서 기안자가 증빙을 지울 수 있음(L40@1108e20) | 사용자 결정 대기(아래 「U1/U2 사용자 결정 반영」 행이 대체) | Plan 05-09, Task 3 ①: 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리. 승인 = 추가만(서버 거부 + `삭제` 렌더 안 함) · 결재 중 삭제는 현행(version 증가로 결재자 막음) · must_haves 두 줄 · prohibition · T-05-907 · 통합 · E2E 사례. **UI-SPEC 가정 #8을 사용자 결정으로 대체 — UI-SPEC은 고치지 않았고 `/plan-design-review`에서 UI-SPEC 보강 필요** |
| U1/U2 사용자 결정 반영 — 사용자 결정(2026-09-26, 코디네이터 PR #89): 승인 뒤 무효 처리 · U2 Phase 6 이관 | 사용자 결정 | Plan 05-09 must_haves(무효 처리 · 표시 두 줄 · 역할 이름 금지 prohibition) · Task 3 ②(`voidEvidence` · `markVoided` · 메뉴 키 `expenses.evidence_void` 시드 없음 — `projects.revenue` · `projects.adjustment` 선례 · 로그 `document_update` detail `evidence_void` + `reasonLength` · 취소선 + `ui/status-tag` 「무효」 `--muted`) · behavior(권한자 성공 · 기안자 · 결재자 거부 · 사유 없음 거부 · 승인 뒤 삭제 여전히 거부 · E2E 취소선 · 배지) · T-05-908. 승인 뒤 삭제 불가 · 결재 중 삭제(현행)는 그대로. **UI-SPEC 가정 #8을 사용자 결정으로 대체 — `/plan-design-review`에서 UI-SPEC 보강 필요**(UI-SPEC는 고치지 않음). U2는 Deferred 행 참조 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| U2 — 반려 · 회수된 지출결의를 끝낼(종결 · 취소) 길이 없어 번호 있는 문서가 줄 문을 닫고 회차 상한을 계속 차지함(L41@1108e20) | 사용자 결정으로 Phase 6 이관(2026-09-26, 코디네이터 PR #89) — `06-CONTEXT.md` 「Phase 5에서 넘어온 것」에 한 줄 · todo 파일 상태 「사용자 결정: Phase 6 TODO 확정」. 원래 사유: 추천안 B. 새 동작(종결 상태 · 상한 · 문 판정 제외)이라 자동 결정 대상이 아니다(ceo-review A6). `.planning/todos/pending/2026-09-26-phase-6-rejected-expense-close-path.md`로 기록 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B2 — 증빙 추가 · 삭제가 지출결의 행을 잠그지 않음 vs `05-09:122` 잠금 순서 전제(L27@69fb7ea) | HIGH | 잠금은 05-04가 `completeEvidenceUpload` · `removeEvidence` tx 첫 단계에 `lockExpenseForUpdate`로 넣는다(다른 플랜). Plan 05-09 execution_notes 잠금 순서 문장과 Task 3 ①을 그에 맞춤 — 새 잠금 없이 그 잠금 뒤에 상태 재판정 · 잠금 아래 `countActiveByOwner` · `bumpInstanceVersion`(지출결의 행 → 인스턴스). 되돌리기(04.1 `withdrawDocument` — 인스턴스만)도 표에 추가 |
| B2(05-09 몫) — 결재 중 서로 다른 파일 둘 동시 삭제 → 결과 증빙 ≥ 1(L27@69fb7ea, 05-04 담당이 넘김 — 결재 중 삭제는 이 플랜이 열어 05-04에서 재현 불가) | HIGH | Plan 05-09, Task 3 behavior · ③: `test/integration/expense-approval-concurrency.test.ts`에 `deps.afterLock`(05-04 `removeEvidence` 주입 지점) 두 순서 사례 — 증빙 2개 결재 중 문서에서 A 삭제 ∥ B 삭제 → 먼저 잠근 쪽만 성공 · 다른 쪽 마지막 파일 거부 · 살아 있는 증빙 1 · version +1 한 번 · 로그 1건. `bumpInstanceVersion`을 붙인 뒤 05-04 `test/integration/tx-safety.test.ts` describe `풀 2 · 동시 증빙 추가 셋(05-04)` 초록을 Task 3 첫 verify 명령에 넣음(파일은 고치지 않음 — diff 0 acceptance) · fails_when · acceptance(`afterLock` · `삭제↔삭제` grep · tx-safety describe grep). files_modified 변경 없음(tx-safety는 실행만) |
| P3-3 — E4 충돌 문구 `증빙을 더함`이 결재 중 삭제에도 나옴(`05-09:44`, L40@69fb7ea) | LOW | 중립 문구 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침`으로 추가 · 삭제 공통(가르지 않은 이유: 가르려면 05-01 `version_reason` CHECK에 값 하나를 더하는 마이그레이션이 필요하고, 결재자의 다음 행동(새로 고침)은 같다). Plan 05-09 must_haves 두 줄 · Task 3 behavior(추가 사례 문구 교체 · 삭제 뒤 옛 version 승인 사례 새로) · acceptance(`바꿈` ≥ 2 · `더함` 0) · Task 3 precondition(05-01 문구가 중립인지 grep — 아니면 멈춤). **문구 원천은 05-01 E4(`05-01-PLAN.md`의 `증빙을 더함` 다섯 곳 — must_haves E4 줄 · Task 1 트레이서 behavior · action ④ · fails_when · 「문구」 산출물 행) — 이 라운드에서 05-01 쪽 교체가 필요(이 플랜 소유 밖, 반환 보고에 기록).** UI-SPEC에 없는 새 문구 — **UI-SPEC 267 · 548행은 design 게이트에서 UI-SPEC 보강 필요**(UI-SPEC은 고치지 않음) |
| P3-7 — 다시 제출 · 되돌리기의 `expectedVersion`이 문서 version인지 인스턴스 version인지 섞임(L44@69fb7ea) | LOW | Plan 05-09 must_haves 한 줄 · Task 1 ①: `submitExpense`의 `expectedVersion` = 문서 version(05-03 그대로), `resubmitDocument`의 인스턴스 version은 tx 안 `lockExpenseForUpdate` 뒤 읽음. `withdrawExpense` 입력을 되돌리기 `{ expenseId, undo: true, round }`(인스턴스 version 없음 — 회수 직전 읽기로 상태 · 지금 차수 처리 기록 판정 뒤 그 version으로 04.1 위임, 사이의 결재는 04.1 version 조건이 막음) · 문서 화면 회수 `{ expenseId, expectedInstanceVersion }`로 이름 분리. Task 1 ③ 액션 스키마 · behavior(옛 문서 version 다시 제출 거부 · 옛 인스턴스 version 문서 화면 회수 거부) · Task 3 behavior(기안자 본인 증빙 추가 뒤 되돌리기 · 다시 제출 성공) · T-05-902 · acceptance. 되돌리기는 04.1 `withdrawDocument`가 tx를 받지 않아 「tx 잠금 뒤 읽기」 대신 「회수 직전 읽기 + 04.1 version 조건」으로 같은 효과(스스로 충돌 없음 · 결재자 경합은 거부) |
| P3-12 — 되돌리기 토스트 E2E가 토스트 표시 시간(4초)에 기댐(L49@69fb7ea) | LOW | Plan 05-09 Task 1 ④ · behavior E2E · Task 2 ③ · behavior E2E: Playwright 내장 `page.clock`(`install` → 토스트가 보이면 `pauseAt` → 누름 · 단언 → `resume`)로 시간 주입 — 앱 코드 · `ui/toast`에 테스트 전용 지속 시간 설정 없음 · 새 의존성 0(Phase 4 `revenue-section.spec.ts` 선례). acceptance(`page.clock` ≥ 1 · 고정 대기 호출 0) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 해당하는 Round 2 지적은 전부 위에서 반영했다. E2(`incoming/` → `evidence/` 이동)는 05-04 · 05-12 몫 — 이 플랜에는 객체 키 접두어 언급이 없어 바꿀 곳이 없다(무효 처리는 파일 행 · 객체를 그대로 둔다) |

### Round 3 — efd6c67

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #87 문구 결정 정렬(코디네이터 16:30Z) — DECISIONS 2026-09-26 오류 문구 명사형 통일(Round 3 새 문구 포함) | 사용자 결정 | Plan 05-09 must_haves · action · behavior · Ledger 문구: `되돌리지 못했습니다 · 되돌리기` → `되돌리기 실패 · 되돌리기` · `지출결의를 삭제하지 못했습니다 · 다시 시도` → `지출결의 삭제 실패 · 다시 시도` · (이번 Round 3에서 새로 넣은) `무효 처리하지 못했습니다 · 다시 시도` → `무효 처리 실패 · 다시 시도`. 사유 빈 칸은 리터럴을 복제하지 않고 04.1 반려 사유 검증 상수를 쓰고 테스트도 그 상수와의 일치로 단언(아래 Deferred — 글자 자체는 사용자 결정 대기). UI-SPEC S4 · Copywriting · UI Considerations 같은 글자 |
| G1 — 증빙이 무효 처리되면 기안자 「내 차례」에 [막힘] `증빙 무효 · 증빙 올리기`, 새 증빙을 올리면 사라짐 — 이 플랜 몫은 신호 원천(L46@efd6c67 · L20@efd6c67) | 사용자 결정 | Plan 05-09 must_haves 새 줄(판정 규칙) · Task 3 ③ 새 파일 `domain/evidence/signals.ts` `listEvidenceVoidSignals(viewer, { ownerKind, ownerIds })` + `repositories/files.ts` `findUnresolvedVoidOwnerIds` 한 SQL — 「무효 파일의 가장 늦은 `voided_at` > 살아 있는 파일의 가장 늦은 `created_at`, 또는 무효 있고 살아 있는 0」 · 지운 파일 제외 · 기안자 판정은 호출자(05-10). 런타임 import는 리포지토리뿐(순환 방지). behavior 통합 describe `무효 뒤 신호`(새 증빙 뒤 빠짐 · 다시 무효 뒤 포함 · 살아 있는 0 포함 · 지운 파일만 제외 · 빈 입력) · fails_when · acceptance · artifact · key_link · files_modified. 줄 표시는 Plan 05-10 |
| G2 — 마지막 살아 있는 증빙까지 무효 허용(L47@efd6c67) | 사용자 결정 | Plan 05-09 must_haves 새 줄 · Task 3 ② 「살아 있는 파일 수를 보지 않는다」 · behavior 통합(살아 있는 1개 승인 문서 무효 → 성공 · `countActiveByOwner` 0) · fails_when |
| G3 — 무효 해제 없음 · 잘못 무효면 같은 파일을 다시 올림(L48@efd6c67) | 사용자 결정 | Plan 05-09 must_haves 새 줄 · prohibition 새 줄 · Task 3 ② 해제 함수 · 액션 · 버튼 없음 · behavior 통합(무효 뒤 같은 sha256 다시 올리기 → 중복 거부 없이 새 파일 행, 옛 무효 행 유지) · acceptance grep(해제 경로 0) · T-05-908 |
| D5 — 무효 처리 상태(loading · error · populated · a11y)가 UI Considerations에 없음 — 화면 쪽(L35@efd6c67) | MEDIUM | Plan 05-09 must_haves(성공 · 실패 줄) · Task 3 ②: 1차 `무효 처리…` · 나머지 `aria-disabled` · 실패 `무효 처리 실패 · 다시 시도`(사유 칸 아래 · 창 유지 · 사유 보존) · 서버 거부 문구 같은 자리. UI-SPEC UI Considerations 「Round 3 추가」 표 · Color(`무효`→`--muted`) · 「Accent reserved for」(`무효 처리` · `증빙 올리기`)는 이 라운드에서 고쳤다 |
| D6 — 무효 행 접근성(DOM 순서 「무효」→파일명 · `aria-describedby` = 2행 · 기존 `aria-live` · 폰 3차 둘 `--touch-min` · 사이 `--s-4`)(L36@efd6c67) | MEDIUM | Plan 05-09 must_haves(표시 줄 갱신) · Task 3 ② `Attachments` 읽기 모드 규칙 · behavior E2E 두 줄(desktop `compareDocumentPosition` · `aria-describedby` 글자 · live 영역 / 375 `boundingBox` 높이 ≥ 44 · 간격 ≥ 16) · 둘째 verify fails_when · acceptance grep. 시스템 규칙은 Plan 05-02 Task 3(B8) |
| D9 — 무효 처리 오류 `이미 무효 · 새로 고침`을 04.1 동시 처리 꼴 `{이름}이 HH:MM에 무효 처리함 · 새로 고침`으로(L41@efd6c67) | LOW | 채택. Plan 05-09 must_haves 새 줄 · Task 3 ② `markVoided` 0행이면 같은 tx로 `voided_by` · `voided_at`을 읽어 `domain/approvals/conflict-message.ts`에 덧붙인 `buildEvidenceVoidedMessage`(04.1 조사 헬퍼 · `HH:MM` 재사용)로 거부 · Task 3 files · read_first · behavior(정규식 정확 일치) · fails_when · acceptance(옛 문구 0 · 덧붙임만) · T-05-909(정보 노출 없음 — 같은 사실이 무효 행 2행에 이미 보임) |
| D10 — 무효 처리 성공 토스트가 그 자리 행 결과와 중복(L42@efd6c67) | LOW | 채택(토스트 제거). 근거: §7-6 「화면 이동이 따르는 행동만 토스트」 · §7-7 SUCCESS 「행동한 자리에서 결과가 보인다」 · 첨부 `aria-live`가 결과를 읽는다. Plan 05-09 must_haves 새 줄 · Task 3 ② 성공 = 창 닫힘 + `router.refresh()` · behavior E2E(토스트 0) · fails_when · acceptance. UI-SPEC S4 「무효 처리」 반영 |
| R1 — 확인 창 사유 빈 칸 문구 명사형(PR #89 issuecomment-5848125081, 코디네이터 대리 결정 A 17:00Z) | 사용자 결정 | 아래 Deferred 「사유 빈 칸 문구의 글자」를 대체. 글자 = `사유 없음 · 사유 적기`(04.1 · SYSTEM §7-8 · 이 화면 공통). Plan 05-09 must_haves(`voidEvidence` 사유 필수 줄)에 글자 명시, 구현은 여전히 04.1 상수 재사용(리터럴 복제 없음 — 04.1 쪽 변경은 코디네이터가 #90에 지시). UI-SPEC S4 「무효 처리」 · UI Considerations error 행, `docs/design/SYSTEM.md` §7-8 예시 · DECISIONS 2026-09-26 명사형 통일 항목에 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| #87 정렬 중 사유 빈 칸 문구의 글자(코디네이터 16:30Z) — 무효 처리 확인 창은 04.1 반려 사유 검증을 재사용하는데 그 원문(04.1-UI-SPEC · SYSTEM §7-8 예시)이 높임 꼴 `… 적어 주세요`다. #87은 §8 규칙 3을 고쳤지만 §7-8 예시와 04.1 문구는 건드리지 않았다 | **사용자 결정 대기(USER DECISION NEEDED)** — 확인 창 근거 칸의 막힘 이유가 「오류 문구」 명사형 통일에 드는지, 든다면 글자(04.1 · SYSTEM §7-8 · 이 화면 공통)를 무엇으로 할지는 이 페이즈가 정할 일이 아니다. 그동안 이 플랜은 리터럴을 복제하지 않고 04.1 상수를 쓰고 테스트도 그 상수와의 일치로 단언하므로, 결정이 04.1에 반영되면 이 플랜은 고칠 곳이 없다. 이 플랜에 걸린 나머지 Round 3 지적(G1 원천 · G2 · G3 · D5 · D6 · D9 · D10)은 전부 위에서 반영했다. 「내 차례」 줄 자체(G1 표시)는 Plan 05-10, 무효 행 표기의 디자인 정본 기록(D7 · B8)은 Plan 05-02 몫이다 |

