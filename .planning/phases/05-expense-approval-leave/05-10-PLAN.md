---
phase: 05-expense-approval-leave
plan: 10
type: execute
wave: 11
depends_on: ["05-09"]
files_modified:
  - domain/expenses/index.ts
  - domain/expenses/dto.ts
  - domain/approvals/kinds.ts
  - domain/leave/index.ts
  - domain/next-turn/index.ts
  - ui/next-turn/build-next-turn-view.ts
  - ui/next-turn/NextTurn.tsx
  - ui/approval-sheet/ApprovalSheet.tsx
  - app/(app)/page.tsx
  - app/(app)/home-approval-actions.tsx
  - app/(app)/approvals/page.tsx
  - app/(app)/approvals/inbox-table.tsx
  - app/(app)/approvals/row-actions.ts
  - test/integration/expense-inbox.test.ts
  - test/integration/next-turn.test.ts
  - test/unit/ui/next-turn.test.ts
  - test/unit/app/approval-row-actions.test.ts
  - test/e2e/expense-inbox.spec.ts
  - test/e2e/mobile-next-turn-approval.spec.ts
autonomous: true
requirements: [UX-03, UX-06, EXP-02]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "결재자가 폰 첫 화면(`/`)의 「내 차례」 `[결재]` 지출결의 행을 탭하면 결재 시트(`ui/approval-sheet`)가 열리고, 시트 본문은 지출결의 문서 화면과 같은 순서의 행(프로젝트 · 견적 줄 · 거래처 · 증빙 종류 · 공급가액 + 계산 한 줄 + 세율 바뀜 · 지급 예정일 · 지급 방식 · 증빙 썸네일 72×96 + `크게 보기` · 비고 · 기안 · 결재선)이며, `승인`을 누르면 그 행이 사라지고 다음 행이 올라온다(UX-03 · 기준 3 「다음 할 일이 한 곳에」 · UI-SPEC S9 · S11)"
    - "시트 본문 행은 종류의 `loadDetails`(구조) → 엔진의 `detailDto` 투영 → `buildDetailRows`(문자열 행) 순서로만 만들어진다 — 금액 정보 항목을 못 보는 결재자에게는 어떤 행 문자열에도 금액이 없다. 증빙 행은 파일 id · 이름 · 크기만 싣고 썸네일 · 크게 보기 주소는 시트가 열릴 때 서버가 권한 판정 뒤 만든다(주소 비저장 · 04.1 ENG-17)"
    - "결재함 문서 칸: `지출결의 · {프로젝트명} · {항목}` / `지출결의 · {팀} · {내용}` / `정산 결재 · {프로젝트명}`(05-11) / 연차는 04.1 그대로. 숫자 칸은 지출결의 공급가액(원화 + 외화 2행) · 연차 `3일` · 정산 결재 `—`이고, 머리글은 지금 표에 있는 종류로 서버가 정한다(`금액` · `일수` · `금액 · 일수`). 폰 P1 세 칸(문서 · 숫자 · 상태)은 그대로이고 외화 2행은 접힌 줄로 내려간다(UI-SPEC S9 · 기준 1 「결재 대기함에는 원화 환산액과 원래 통화」 · UIC populated S9 · partial S8·S9 외화)"
    - "PC 결재함 행 · 「내 차례」 `[결재]` 행의 3차 `승인`은 지출결의 · 정산 결재에도 확인 없이 즉시다. `반려`는 04.1 S6 반려 확인(제목 `지출결의 반려` · 부제 `{번호} · {기안자} · {대상} · {금액}` · 사유 필수 · 결과 줄 `{기안자}에게 돌아감 · 내 결재에서 빠짐`)을 거친다(UI-SPEC 확정 #3 · 사용자 결정 2026-09-26 #1)"
    - "그 문서에 종류가 준 `approveBlockedReason`(05-01 E3 — 정산 결재 `진행으로 바뀜 · 반려` 등)이 있으면 **PC 결재함 행 · 「내 차례」 PC 행**은 `승인`을 렌더하지 않고 그 자리에 이유 글자(서버 원문 그대로, `--fs-sm --danger`) + 3차 `반려`(그 문서의 가능 행동에 `반려`가 있을 때)다 — 행 셀에는 비활성 버튼 옆 이유가 설 자리가 없어서다(S1 행 행동 셀과 같은 「이유 글자 + 3차」 규칙). 결재 시트 · 문서 화면은 05-01 E3 그대로 `승인` `aria-disabled` + 이유다. 판정은 두 행이 함께 쓰는 순수 함수 `rowApprovalActions` 하나다(design-review D4 · UI-SPEC S9 「막힌 `승인`」 · S11)"
    - "결재함이 0건이면 `결재할 건이 없습니다 · 지출결의 목록 보기`(→ `/expenses`, §7-7 원문으로 되돌림)이고, `expenses` 보기 권한이 없는 사람에게는 앞 문장만 보인다(UIC empty S9)"
    - "결재 시트 · 문서 화면에서 동시 처리로 거부되면 04.1 「거부 — 동시 처리」 문구 + `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침`(추가 · 삭제 공통 — 05-01 E4, 리뷰 Round 2 P3-3)이 그 자리에 서고 시트는 닫히지 않으며 자동 재시도는 없다. 누른 버튼은 `…` · 나머지 `aria-disabled`(UIC error S9 동시 처리 · loading S9)"
    - "결재함 처리함은 최근 50건에서 자르고 페이지 이동이 없다(04.1 그대로), 시트 본문은 세로 스크롤 · 행동 줄 고정, 문서 칸은 `keep-all` 줄바꿈 · 기안 한 줄(UIC overflow S9 · long-text S9)"
    - "「내 차례」 공급 함수 `listNextTurnItems(viewer)`(`domain/next-turn/index.ts` — 06 S19가 같은 함수에 항목을 더한다)가 첫 화면 블록에 항목을 준다: `[결재]` = 지금 내가 담당인 모든 문서(지출결의 · 연차 · 정산 결재) 한 줄씩 `[결재] {대상} — {상황}  {숫자}  승인 · 반려` / `[막힘]` = 내가 기안한 문서 중 ① 반려된 것 `[막힘] {대상} — {종류} 반려, {반려자}  {숫자}  {종류} 열기`(→ 고칠 수 있는 폼) ② 종류가 「승인 뒤 막힘」을 알려 주는 승인 문서 — 이 페이즈에는 지출결의의 증빙 무효 `[막힘] {대상} — 증빙 무효  {숫자}  증빙 올리기`(→ `/expenses/{id}#evidence`) 하나(사용자 결정 G1 · 판정 규칙 = 05-09 `listEvidenceVoidSignals`: 무효가 살아 있는 증빙보다 나중이거나 살아 있는 증빙 0). 새 증빙을 올리면 그 줄이 사라진다. 순서는 반려 줄 다음 증빙 무효 줄. 작성 중 · 회수 문서는 넣지 않는다(UI-SPEC S11 · Copywriting 「표시 — 「내 차례」」 · UIC populated S11 · partial S11)"
    - "태그 순서 고정 막힘 → 오늘 → 결재 → 대기, 최대 6줄 + `더 보기 N건`, 항목 0이면 블록째 없다. 폰은 두 줄(1행 태그 · 대상 · 행동, 2행 숫자 · 이유)이고 하단 탭 `내 차례 N`의 N = 항목 수다(§7-4 · UIC empty S11 · overflow S11 · zero-one-many S11 · long-text S11)"
    - "「내 차례」 행 `승인` · `반려`의 `aria-describedby`는 그 행 대상 글자를 가리킨다. 블록 로드 실패는 블록 자리 `불러오기 실패 · 다시 시도`, 행 행동 거부는 04.1 동시 처리 문구를 그 행 옆에(UIC error S11 · loading S11)"
    - "문서 종류 요약에 선택 필드 `nextTurnText: { target, situation }`이 있어 종류마다 대상 · 상황 글자를 준다 — 엔진 · 「내 차례」는 종류 이름으로 분기하지 않는다(04.1 금지 항목). 승인 뒤 막힘도 종류 정의의 선택 필드 `blockedAfterApproval?(viewer, documentIds)`(→ 문서 id별 `{ situation, actionLabel, href }`)가 주고, 「내 차례」는 그 필드가 있는 종류에만 기안자의 승인 문서 id를 한 번 넘긴다(G1)"
  prohibitions:
    - statement: "결재 시트 · 결재함이 종류 이름으로 분기해 본문 · 숫자 칸을 만들어서는 안 된다 — 종류가 등록 필드로 준다"
      status: resolved
      verification: judgment
    - statement: "결재자가 볼 수 없는 정보 항목(금액 등)의 값이 시트 본문 문자열 행에 들어가서는 안 된다 — 투영 뒤에만 문자열을 만든다"
      status: resolved
      verification: judgment
    - statement: "「내 차례」에 작성 중 문서를 넣어 6줄 상한을 초안이 먹게 해서는 안 된다 — 목록 `작성 중` 그룹이 그 자리다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/next-turn/index.ts"
      provides: "listNextTurnItems(viewer) — [결재] · [막힘] 공급(06 S19가 확장)"
      exports: ["listNextTurnItems"]
    - path: "app/(app)/home-approval-actions.tsx"
      provides: "첫 화면 [결재] 행 행동 — PC 승인(즉시) · 반려(04.1 확인) · 폰 행 탭 → 결재 시트"
    - path: "test/integration/next-turn.test.ts"
      provides: "[결재] 종류 셋 · [막힘] 반려 + 증빙 무효(G1 — 새 증빙 뒤 사라짐) · 작성 중 · 회수 제외 · 순서 · 상한"
      min_lines: 50
    - path: "app/(app)/approvals/row-actions.ts"
      provides: "rowApprovalActions — PC 결재함 행 · 「내 차례」 PC 행의 막힌 `승인` 셀 판정(D4)"
      exports: ["rowApprovalActions"]
    - path: "test/e2e/mobile-next-turn-approval.spec.ts"
      provides: "폰 첫 화면 [결재] 행 탭 → 시트 본문 → 승인 → 행 사라짐"
      min_lines: 40
  key_links:
    - from: "app/(app)/page.tsx"
      to: "domain/next-turn/index.ts"
      via: "buildNextTurnView(await listNextTurnItems(viewer))"
      pattern: "listNextTurnItems"
    - from: "domain/next-turn/index.ts"
      to: "domain/approvals/index.ts"
      via: "listMyInbox mine + 내 반려 문서 — 종류 요약 nextTurnText · measure"
      pattern: "listMyInbox"
    - from: "domain/expenses/index.ts"
      to: "domain/approvals/kinds.ts"
      via: "지출결의 종류 loadDetails · detailDto · buildDetailRows 등록"
      pattern: "buildDetailRows"
    - from: "domain/expenses/index.ts"
      to: "domain/evidence/signals.ts"
      via: "지출결의 종류 blockedAfterApproval → listEvidenceVoidSignals(G1)"
      pattern: "listEvidenceVoidSignals"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**결재자의 한 곳 — 결재함 · 결재 시트 · 「내 차례」**: 04.1 결재함과 결재 시트가 지출결의의 근거(금액 · 계산 한 줄 · 증빙 썸네일)를 보이고, 첫 화면 「내 차례」가 지금 내 차례인 결재와 반려된 내 문서를 한 곳에 모은다. 폰에서 첫 화면 행 탭 → 시트 → 승인으로 끝난다.

Purpose: 기준 3의 「다음 할 일이 한 곳에 보인다」와 UX-03(폰 결재)을 결재자 쪽에서 닫는다. 기준 1의 「결재 대기함에는 원화 환산액과 원래 통화」가 여기서 선다. 06 UA-611(「내 차례」 공급 함수)의 답이다.

Output: 지출결의 종류 상세(시트 본문) · 결재함 문서 칸 · 숫자 칸 · EMPTY · 「내 차례」 공급 함수 · 첫 화면 배선 · 통합 2 · 단위 1 · E2E 2.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-10.env"` · diff 검사 접두는 05-01과 같은 모양. `.planning/STATE.md` 불변 · 새 의존성 0 · 새 토큰 0.
- `ui/next-turn/*` · `domain/approvals/kinds.ts` · `ui/approval-sheet/*`는 **선택 필드 · 선택 prop 덧붙이기**만 한다 — 기존 항목 모양 · 04.1 연차 화면 글자는 바뀌지 않는다(`test/e2e/mobile-next-turn.spec.ts` · 04.1 결재 E2E가 회귀 기준).
- `ui/`는 `app/` 액션을 import하지 않는다(계층 경계) — 첫 화면 행 행동은 `app/(app)/home-approval-actions.tsx`(클라이언트)가 04.1 결재 액션과 `ui/approval-sheet`를 묶고, `NextTurn`은 서버가 넘긴 노드 슬롯만 그린다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 110k — 05-01 「웨이브 근거」의 규모 근거): Task 1 트레이서가 공급 함수 · 종류 상세 · 시트를 세우고, Task 2 결재함 · Task 3 「내 차례」는 04.1 화면 위 표시 갈래다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 폰 첫 화면 「내 차례」 `[결재]` 지출결의 행 탭 → 결재 시트(지출결의 본문 · 증빙 썸네일) → `승인` → 행이 사라진다 (공급 함수 · 종류 상세 · 첫 화면 · 모바일 E2E)</name>
  <files>domain/expenses/index.ts, domain/expenses/dto.ts, domain/approvals/kinds.ts, domain/next-turn/index.ts, ui/next-turn/build-next-turn-view.ts, ui/next-turn/NextTurn.tsx, ui/approval-sheet/ApprovalSheet.tsx, app/(app)/page.tsx, app/(app)/home-approval-actions.tsx, test/integration/next-turn.test.ts, test/e2e/mobile-next-turn-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S9(538–550행) · S11(586–594행) · Copywriting 274–275행
    - `ui/next-turn/build-next-turn-view.ts` · `NextTurn.tsx`(전문 — `NextTurnItem` 모양 · 태그 색 · 링크 행동) · `app/(app)/page.tsx`(빈 배열 자리)
    - 04.1 종류 상세 계약(`loadDetails` · `detailDto` · `buildDetailRows` — 04.1-05 · `name_map`) · 연차 구현(`domain/leave/index.ts`) · `ui/approval-sheet/ApprovalSheet.tsx`(05-01 이동본) · 04.1 결재 액션(`app/(app)/approvals/actions.ts`)
    - 05-04 `createEvidenceViewUrl` · 05-06 `taxLineText` · `taxDrift` · 05-07 팀 비용 표시
    - `test/e2e/mobile-next-turn.spec.ts`(기존 폰 블록 스펙 — 회귀 기준)
  </read_first>
  <behavior>
    - (통합) 팀장(1단 담당)의 `listNextTurnItems` → `[결재]` 지출결의 한 줄 · 대상 `{프로젝트명} · {항목}` · 상황 `지출결의, {기안자}` · 숫자 = 공급가액 원화 · 행동 참조(인스턴스 id · version). 무관한 사람 → 0줄. 연차 담당이면 연차 줄도(04.1 요약의 `nextTurnText`)
    - (통합) 지출결의 종류 `loadDetails` → `detailDto` 투영 → `buildDetailRows` 결과 행 라벨 순서가 문서 화면과 같고 증빙 행은 파일 id · 이름 · 크기만(주소 없음). `expense.amount` 노출을 끈 계급으로 부르면 어떤 행 문자열에도 금액 숫자가 없다
    - (E2E, mobile-375) PM이 제출(픽스처) → 팀장 로그인 → `/` 「내 차례」 `[결재]` 행(두 줄) · 하단 탭 `내 차례 1` → 행 탭(`button` + `aria-haspopup="dialog"`) → 시트 제목 `지출결의 — {프로젝트명} · {항목}` · 부제 `{번호} · {기안자}` · 본문에 계산 한 줄 · 증빙 썸네일 1 → `승인` → 시트 닫힘 · 그 행 없음 · 블록째 없음(항목 0)
  </behavior>
  <action>
① **종류 상세** `domain/expenses/index.ts` 종류 등록에 `loadDetails(viewer, ids)`(문서 · 파일 목록 구조 필드 — 보임 판정 `canSeeExpense`를 먼저) · `detailDto`(`domain/expenses/dto.ts`에 결재 상세 명세 — 금액 필드 `expense.amount`, 나머지 `expense.value`) · `buildDetailRows(projected)`(문서 화면 순서의 문자열 행 + 계산 한 줄 · `taxDrift` · 팀 비용 행). 04.1 상세 행 타입에 선택 갈래 `{ kind: "evidence", files: [{ id, name, sizeBytes, contentType }] }`를 **덧붙이고**(`domain/approvals/kinds.ts`), `ui/approval-sheet/ApprovalSheet.tsx`는 그 갈래를 §7-8 첨부 모양(썸네일 72×96 · 파일명 · 장수 · 용량 · 3차 `크게 보기`)으로 그린다 — 썸네일 · 크게 보기 주소는 시트가 열릴 때 주입받은 함수로 받는다(시트는 지출결의를 모른다).
② **요약 필드** 종류 요약 DTO에 선택 필드 `nextTurnText?: { target: string; situation: string }`(`domain/approvals/kinds.ts`)을 덧붙이고, 지출결의 요약(`{프로젝트명} · {항목}` 또는 `{팀} · {내용}` / `지출결의, {기안자}`)과 04.1 연차 요약(`{기안자}` / `연차 {종류} {기간}` — 04.1 결재함 글자에서 조립)에 채운다.
③ **공급 함수** 새 파일 `domain/next-turn/index.ts` `listNextTurnItems(viewer, deps?)` → `NextTurnItem[]` — `listMyInbox(viewer)`의 `mine` 항목을 `[결재]`로(대상 · 상황 = `nextTurnText`, 숫자 = `measure` 문자열 — 돈이면 원화, 일수면 `3일`, 없으면 `—`, 행동 참조). `[막힘]`은 Task 3. `ui/next-turn/build-next-turn-view.ts`의 `NextTurnItem`에 선택 필드 `key?: string` · `measureText?: string`을 덧붙이고, `NextTurn.tsx`는 선택 prop `actionSlots?: Record<string, ReactNode>`가 그 항목 key에 있으면 기본 링크 대신 그 노드를, `measureText`가 있으면 원화 서식 대신 그 글자를 그린다(기존 사용처 불변).
④ **첫 화면** `app/(app)/page.tsx`가 `buildNextTurnView(await listNextTurnItems(viewer))`를 그리고, 결재 항목마다 `app/(app)/home-approval-actions.tsx`(클라이언트 — 04.1 승인 · 반려 액션 + `ui/approval-sheet` + 04.1 S6 반려 확인) 노드를 `actionSlots`로 넘긴다. PC = 3차 `승인`(즉시) · `반려`(확인), 폰 = 행 전체가 시트를 여는 `button` + `aria-haspopup="dialog"`(04.1 폰 결재함 행과 같은 모양), 처리 뒤 `router.refresh()`. 빈 배열일 때의 기존 빈 화면 한 줄은 그대로 둔다(04.1 · Phase 2 계약).
⑤ behavior 통합을 새 파일 `test/integration/next-turn.test.ts`에(상세 투영 사례 포함), E2E를 새 스펙 `test/e2e/mobile-next-turn-approval.spec.ts`에 먼저 적는다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/next-turn.test.ts test/integration/approvals-inbox-projection.test.ts test/integration/leak-scan.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 담당이 아닌 사람에게 [결재] 줄이 생기거나, 상세 행 순서 · 증빙 행 모양이 다르거나, 금액 노출을 끈 계급의 행 문자열에 금액이 있거나, 04.1 결재함 노출 조회 횟수 단언이 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/mobile-next-turn-approval.spec.ts test/e2e/mobile-next-turn.spec.ts --project=mobile-375 --no-deps'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 첫 화면에 [결재] 행이 없거나, 행 탭이 시트를 열지 않거나, 시트 본문에 계산 한 줄 · 썸네일이 없거나, 승인 뒤 행이 남거나, 기존 폰 「내 차례」 스펙이 깨지면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from \"@/app" ui/next-turn ui/approval-sheet`가 0건이다(계층 경계)
    - `grep -v '^\s*//' domain/next-turn/index.ts | grep -cE '"(leave|expense|settlement)"'`이 0이다
    - `ui/next-turn/build-next-turn-view.ts` diff가 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-10.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- ui/next-turn/build-next-turn-view.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재자가 폰 첫 화면에서 자기 차례인 지출결의를 근거와 함께 보고 바로 승인한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 결재함 완성 — 문서 칸 · 숫자 열(원화 + 외화 2행 · 섞인 머리글) · PC 행 즉시 승인 · 막힌 `승인` 셀(D4) · EMPTY 되돌림 · 시트 동시 처리 문구</name>
  <files>domain/expenses/index.ts, app/(app)/approvals/page.tsx, app/(app)/approvals/inbox-table.tsx, app/(app)/approvals/row-actions.ts, app/(app)/home-approval-actions.tsx, test/unit/app/approval-row-actions.test.ts, test/integration/expense-inbox.test.ts, test/e2e/expense-inbox.spec.ts</files>
  <read_first>
    - 05-01 E5(`measure` · `measureHeader`) · 04.1 결재함 표(`name_map`) · `app/(app)/approvals/page.tsx`(EMPTY 자리 — 04.1이 목록 없어 바꿔 둔 문구)
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S9 · Copywriting 255 · 274행 · UI Considerations S9 행
    - 05-08 `expense-money` 픽스처(외화 문서)
  </read_first>
  <behavior>
    - (통합) 팀장 결재함에 외화 지출결의(USD 4,200.00 @1,318.4)와 연차가 함께 있으면 `measureHeader` `금액 · 일수` · 지출결의 항목 `measure` = `{ kind: "money", money: { currency: "USD", amount: 4200, fxRate: 1318.4, amountKrw: 5537280 } }`(05-01 E5 통일 모양 — 리뷰 Round 2 P3-1) · 문서 칸 `지출결의 · {프로젝트명} · {항목}` · 팀 비용 문서 칸 `지출결의 · 기획1팀 · 팀 회식`
    - (E2E, desktop) 팀장 `/approvals` → 숫자 머리글 `금액 · 일수` · 외화 행 2행 `USD 4,200.00 @1,318.4` · PC 행 `승인` 클릭 → 확인 창 없음 · 토스트 `승인 · 결재 요청됨 → {다음 담당}` · 행이 처리함으로
    - (E2E) 결재 0건인 결재자 → `결재할 건이 없습니다 · 지출결의 목록 보기` · 3차 → `/expenses`. `expenses` 보기 권한을 뺀 계급의 결재자 → `결재할 건이 없습니다`만
    - (E2E) 결재자가 시트를 연 뒤 기안자가 증빙을 더함 → 시트 `승인` → 시트가 열린 채 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침`(Round 2 P3-3)
    - (단위 — D4(Round 3)) `rowApprovalActions({ approveBlockedReason, canReject })`: (null, 참) → 승인 있음 · 이유 없음 · 반려 있음 / (`진행으로 바뀜 · 반려`, 참) → 승인 없음 · 이유 글자 그 원문 · 반려 있음 / (`지금 담당이 아님 · 새로 고침`, 거짓) → 승인 없음 · 이유 글자 · 반려 없음. 실제 종류(정산 결재)로 막힌 셀을 그리는 E2E는 05-11 Task 3(이 페이즈에서 `approveBlockedReason`을 주는 첫 종류)
  </behavior>
  <action>
① 지출결의 요약의 `measure`(공급가액 금액 모델 네 값)와 문서 칸 문자열을 05-01 E5 계약대로 채운다(팀 비용 포함). 결재함 표 · 폰 접힌 줄은 05-01이 이미 `measure`로 그린다 — 이 task는 지출결의 쪽 값과 E2E 증명이다.
② `app/(app)/approvals/page.tsx` EMPTY를 `결재할 건이 없습니다` + (`can(viewer, "expenses", "view")`일 때만) 3차 `지출결의 목록 보기` → `/expenses`로 되돌린다(§7-7 원문).
③ PC 행 · 시트 `승인` 즉시 · `반려` 확인은 04.1 동작 그대로임을 E2E로 지출결의에 대해 증명한다(종류별 확인 분기를 만들지 않는다). 토스트 문구는 04.1 문구에서 일수 조각을 뺀 꼴(`승인 · 결재 요청됨 → {다음}` · `승인 · 최종 승인` · `반려 · {기안자}에게 돌아감`).
④ **막힌 `승인` 셀(Addresses review concern: D4(Round 3))**: 새 파일 `app/(app)/approvals/row-actions.ts`에 순수 함수 `rowApprovalActions({ approveBlockedReason, canReject })` → `{ showApprove, reasonText, showReject }` — 이유가 있으면 `showApprove` 거짓 · `reasonText` = 서버 원문 그대로(자르거나 바꾸지 않는다), 없으면 기존대로. `app/(app)/approvals/inbox-table.tsx`의 PC `내 결재` 행 행동 칸과 `app/(app)/home-approval-actions.tsx`의 PC 행이 이 결과로 그린다 — `승인` 자리에 이유 글자(`--fs-sm --danger`, 기존 토큰) + 3차 `반려`(04.1 S6 반려 확인). 05-01 E3가 결재함 PC 행 `승인`에 붙인 `disabledReason` 모양은 이 셀 규칙으로 바뀐다(결재 시트 · 문서 화면의 `aria-disabled` + 이유는 05-01 그대로). 폰 행은 탭 → 시트라 바뀌지 않는다.
⑤ behavior 통합을 새 파일 `test/integration/expense-inbox.test.ts`에, E2E를 새 스펙 `test/e2e/expense-inbox.spec.ts`에, `rowApprovalActions` 단위를 새 파일 `test/unit/app/approval-row-actions.test.ts`에 먼저 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/app/approval-row-actions.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 막힌 문서에 `승인`이 남거나, 이유 글자가 서버 원문과 다르거나, 반려 가능 여부가 무시되면 여기서 깨진다(D4 Round 3)</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-inbox.test.ts test/integration/expense-money.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 섞인 머리글 · 외화 measure 네 값 · 문서 칸 문자열 중 하나라도 다르면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-inbox.spec.ts test/e2e/leave-document.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — PC 행 승인에 확인 창이 뜨거나, EMPTY 3차가 권한과 무관하게 서거나, 증빙 변경 뒤 시트 승인 거부 문구가 다르거나, 04.1 연차 문서 화면이 깨지면 여기서 깨진다(04.1 스펙 이름은 `name_map`으로 확인)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "지출결의 목록 보기" "app/(app)/approvals/page.tsx"`가 1이다
    - (D4 Round 3) `grep -c "rowApprovalActions" "app/(app)/approvals/inbox-table.tsx"`와 `grep -c "rowApprovalActions" "app/(app)/home-approval-actions.tsx"`가 둘 다 1 이상이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>결재함이 지출결의를 원화와 원래 통화로 보이고, PC 행에서 바로 승인하며, 빈 결재함은 갈 수 있는 곳만 가리킨다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 「내 차례」 완성 — `[막힘]` 반려 문서 · 증빙 무효 승인 문서(G1) · 태그 순서 · 6줄 + `더 보기` · 0이면 블록 없음 · 폰 탭 수 · 행 행동 접근성 · 오류</name>
  <files>domain/next-turn/index.ts, domain/approvals/kinds.ts, domain/expenses/index.ts, domain/leave/index.ts, app/(app)/page.tsx, app/(app)/home-approval-actions.tsx, test/integration/next-turn.test.ts, test/unit/ui/next-turn.test.ts, test/e2e/mobile-next-turn-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S11(Round 3 [막힘] ② 포함) · Copywriting 「표시 — 「내 차례」」 · UI Considerations S11 행들과 「Round 3 추가」 표 — 줄 번호 대신 절 이름으로 `grep -n` 뒤 범위 Read
    - 05-09 산출물 `domain/evidence/signals.ts`(`listEvidenceVoidSignals` 입력 · 판정 규칙) · 05-09 `test/integration/evidence-void.test.ts`의 권한자 · 무효 픽스처(재사용)
    - `docs/design/SYSTEM.md` §7-4 · §7-7(내 차례 SUCCESS · ERROR · PARTIAL)
    - Task 1 산출물 · `test/unit/` 안의 기존 `buildNextTurnView` 단위 테스트(`grep -rln "buildNextTurnView" test/unit`)
  </read_first>
  <behavior>
    - (통합) 기안자의 지출결의가 반려되면 `[막힘]` 한 줄 · 대상 · `지출결의 반려, {반려자}` · 행동 `지출결의 열기` → `/expenses/{id}`. 회수한 문서 · 작성 중 문서 → 줄 없음. 반려된 연차 → `[막힘] … 연차 열기`
    - (통합, 증빙 무효 — 사용자 결정 G1, describe `증빙 무효 [막힘]`) 기안자의 최종 승인된 지출결의에서 권한자(05-09 픽스처)가 파일 하나를 무효 처리 → 기안자의 `listNextTurnItems`에 `[막힘]` 한 줄 · 대상 = 그 문서 `nextTurnText.target` · 상황 `증빙 무효` · 숫자 = 공급가액 · 행동 `증빙 올리기` → `/expenses/{id}#evidence`. 권한자 · 결재자 · 무관한 사람 → 그 줄 없음 · 기안자가 증빙을 하나 더 올림(05-04 `attachEvidence` 도우미 — 승인 뒤 추가 허용) → 줄 없음 · 그 뒤 살아 있는 파일을 전부 무효(G2) → 줄 다시 있음 · 같은 기안자의 반려 문서가 함께 있으면 반려 줄이 먼저. 종류 함수 `blockedAfterApproval`은 한 번의 `listNextTurnItems`에서 종류당 1회만 불린다(호출 기록)
    - (단위) `[결재]` 5 + `[막힘]` 3이면 보이는 6줄 = 막힘 3 → 결재 3 · `overflowCount` 2(기존 `buildNextTurnView` 계약 그대로 — 새 사례 추가)
    - (E2E, mobile-375) 반려된 문서가 있는 기안자의 첫 화면 → `[막힘]` 행 1행 태그 · 대상 · `지출결의 열기` · 2행 숫자 · 이유 · 하단 탭 `내 차례 1`. 결재 8건인 결재자 → 6줄 + `더 보기 2건`
    - (E2E, desktop — G1) 증빙이 무효 처리된 승인 문서의 기안자 첫 화면 → `[막힘]` 행에 `증빙 무효` · 3차 `증빙 올리기`(href가 `/expenses/{id}#evidence`로 끝남) → 누르면 문서 화면 `증빙` 섹션(id `evidence`)이 보이고 「하나 더」로 파일을 올린 뒤 첫 화면으로 돌아오면 그 줄이 없다(무효 처리는 05-09 E2E의 권한자 픽스처 · 흐름을 재사용)
    - (E2E, desktop) 첫 화면 `[결재]` 행 `승인` · `반려`의 `aria-describedby`가 같은 행 대상 글자 요소를 가리키고 행마다 id가 다르다 · 공급 함수 실패(요청 가로채기) → 블록 자리 `불러오기 실패 · 다시 시도`
  </behavior>
  <action>
① `listNextTurnItems`에 `[막힘]` — 내가 기안한 문서 중 인스턴스 상태 `rejected`인 것(종류 무관 — 결재 엔진의 인스턴스 조회 한 번 + 종류 요약 `nextTurnText` · `measure` · `href`), 이유 = `{종류 라벨} 반려, {반려자 이름}`, 행동 = `{종류 라벨} 열기` → 종류 `href`. 연차 종류 요약에 `nextTurnText`가 Task 1에서 채워졌는지 확인하고 반려 줄 글자를 맞춘다.
①-b **승인 뒤 막힘(Addresses review concern: G1(Round 3) — 사용자 결정)**: `domain/approvals/kinds.ts` 종류 정의에 선택 필드 `blockedAfterApproval?: (viewer, documentIds: string[]) => Promise<Map<string, { situation: string; actionLabel: string; href: string }>>`를 **덧붙인다**(기존 필드 불변). ①의 같은 인스턴스 조회가 `rejected`에 더해 「`approved` ∧ 종류에 이 필드가 있음」도 읽고(등록부를 순회해 필드 있는 종류 키 목록을 만든다 — 04.1이 설정 화면 경고에 쓰는 등록부 순회 함수를 `grep -n "export function" domain/approvals/kinds.ts`로 찾아 쓰고, 없으면 같은 파일에 `listDocumentKinds()`를 덧붙인다), 그 종류마다 승인 문서 id를 **한 번** 넘긴다. 돌아온 문서마다 `[막힘]` 한 줄 — 대상 · 숫자는 그 종류 `describeDocuments` 요약(`nextTurnText.target` · `measure`), 이유 = `situation`, 행동 = `actionLabel` → `href`. 순서는 반려 줄 다음. `domain/expenses/index.ts` 지출결의 종류 등록에 이 필드를 채운다 — 05-09 `listEvidenceVoidSignals(viewer, { ownerKind: 지출결의 주인 종류, ownerIds: documentIds })`(`domain/evidence/signals.ts` — 런타임 import가 리포지토리뿐이라 순환 없음)가 돌려준 id마다 `{ situation: "증빙 무효", actionLabel: "증빙 올리기", href: "/expenses/{id}#evidence" }`(사용자 결정 G1 문구 `증빙 무효 · 증빙 올리기`를 §7-4 한 줄 꼴로 — UI-SPEC S11 ②). `domain/next-turn/index.ts`는 종류 이름으로 분기하지 않고 이 줄의 글자 · 주소를 만들지 않는다 — 전부 종류 필드에서 온다(04.1 금지 항목 — Task 1 acceptance의 종류 키 grep).
② 첫 화면: 공급 함수 오류를 잡아 블록 자리 한 줄(§7-7 내 차례 ERROR — 2차 `다시 시도` = `router.refresh`) · 행 행동 거부 문구는 그 행 옆. 폰 하단 탭 수는 셸이 이미 읽는 값에 항목 수를 넘긴다(셸 파일을 고쳐야 하면 수를 받는 prop 하나만 — 실행자가 기존 배선을 확인하고 SUMMARY에 적는다).
③ behavior를 `test/integration/next-turn.test.ts`(덧붙임 — G1 describe `증빙 무효 [막힘]` 포함) · `test/unit/ui/next-turn.test.ts`(기존 `buildNextTurnView` 단위 파일 — 덧붙임) · `test/e2e/mobile-next-turn-approval.spec.ts`(덧붙임) · 데스크톱 접근성 · G1 사례는 같은 스펙의 1280 컨텍스트로 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/next-turn.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 막힘이 결재 뒤로 밀리거나 넘침 수가 다르면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/next-turn.test.ts test/integration/evidence-void.test.ts' &amp;&amp; pnpm vitest run --project unit test/unit/import-cycles.test.ts</automated>
    <fails_when>failed가 1 이상이다 — 회수 · 작성 중 문서가 [막힘]에 들어가거나 반려 문서가 빠지거나 행동 주소가 다르거나, 증빙 무효 승인 문서의 [막힘] 줄이 없거나 기안자가 아닌 사람에게 서거나 새 증빙 뒤에도 남거나, `blockedAfterApproval`이 종류당 2회 이상 불리거나, `domain/expenses` → `domain/evidence/signals` import가 순환을 만들면(G1 Round 3) 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/mobile-next-turn-approval.spec.ts test/e2e/mobile-next-turn.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 폰 두 줄 · 탭 수 · 6줄 + 더 보기 · `aria-describedby` · 오류 한 줄 중 하나라도 다르거나, 증빙 무효 [막힘] 줄 · `#evidence` 이동 · 새 증빙 뒤 사라짐(G1) 중 하나라도 다르면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "rejected" domain/next-turn/index.ts`가 1 이상이고 `grep -cE "withdrawn|draft" domain/next-turn/index.ts`가 0이다(막힘은 반려 · 승인 뒤 막힘만 — 회수 · 작성 중 없음)
    - (G1 Round 3) `grep -c "blockedAfterApproval" domain/approvals/kinds.ts`와 `grep -c "blockedAfterApproval" domain/next-turn/index.ts`와 `grep -c "blockedAfterApproval" domain/expenses/index.ts`가 셋 다 1 이상 · `grep -c "listEvidenceVoidSignals" domain/expenses/index.ts`가 1 이상 · `domain/next-turn/index.ts`에 종류 키 리터럴이 여전히 0이다(Task 1 acceptance의 grep을 다시 돌린다 — 승인 뒤 막힘의 글자 · 주소는 종류가 준다) · `domain/approvals/kinds.ts` diff가 덧붙임뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-10.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/approvals/kinds.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>첫 화면 「내 차례」가 지금 내 결재와 반려된 내 문서를 규칙대로 한 곳에 모은다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 결재 엔진 → 종류 상세 | 결재자가 볼 수 없는 문서 필드가 시트로 새면 안 된다 |
| 첫 화면 행 행동 → 결재 액션 | 즉시 승인은 서버가 후보 · version을 다시 판정해야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-1001 | Information Disclosure | 결재 시트 상세 행 | high | mitigate | `loadDetails` → `detailDto` 투영 → `buildDetailRows` 순서(04.1 ENG-17) · 금액 노출 끈 계급 통합 사례 · 증빙 주소는 권한 판정 뒤 요청 때만 |
| T-05-1002 | Elevation of Privilege | 첫 화면 PC 즉시 승인 | high | mitigate | 04.1 `approveDocument`가 매번 walkRoute 후보 · version을 다시 판정 — 행 행동은 그 액션만 부른다 |
| T-05-1003 | Tampering | 증빙 변경 뒤 시트 승인 | medium | mitigate | E4 version 충돌 — E2E가 시트 거부 문구를 단언 |
| T-05-1004 | Information Disclosure | 「내 차례」 항목 | medium | mitigate | 공급 함수는 `listMyInbox`(보임 규칙) · 내 기안 문서만 — 무관한 사람 0줄 사례 |
| T-05-1005 | Information Disclosure | 승인 뒤 막힘(증빙 무효, G1) 줄 | low | mitigate | 종류 필드에 넘기는 id는 엔진 조회의 「기안자 = viewer ∧ 승인」 문서뿐이고 줄에는 대상 · 공급가액 · 고정 낱말만 — 무효 사유 · 처리자는 싣지 않는다(문서 화면에서 본다). 권한자 · 결재자 · 무관한 사람 0줄 통합 사례 |
| T-05-1006 | Elevation of Privilege | 막힌 `승인` 셀(D4) | low | mitigate | 셀은 표시만 — 서버 승인은 여전히 훅(05-01 E2)이 판정한다. 셀에서 `승인`을 빼도 직접 호출은 서버가 막는다(05-11 D-80 · F1 통합 사례) |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(verification의 lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과
- 통합: `next-turn` · `expense-inbox` · `approvals-inbox-projection` · 누수 스캔 녹색
- `CI=true` E2E: `mobile-next-turn-approval` · `mobile-next-turn` · `expense-inbox` · 04.1 연차 문서 녹색
- `. "$(git rev-parse --git-dir)/gsd-05-10.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml docs/design/tokens.css` 비어 있음
</verification>

<success_criteria>
- 결재자는 폰 첫 화면에서 자기 차례를 보고 근거와 함께 승인한다(UX-03)
- 결재함은 금액 문서를 원화와 원래 통화로 보인다(기준 1)
- 다음 할 일(결재 · 반려된 내 문서)이 한 곳에 보인다(기준 3 · UX-06)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `listNextTurnItems`(`domain/next-turn/index.ts`) · 지출결의 종류 `loadDetails` · `detailDto` · `buildDetailRows` · `blockedAfterApproval`(G1) · `rowApprovalActions`(`app/(app)/approvals/row-actions.ts` — D4) |
| 엔진 선택 필드 | 요약 `nextTurnText` · 상세 행 갈래 `evidence` · 종류 정의 `blockedAfterApproval`(G1) |
| 컴포넌트 · 화면 | `ui/next-turn`(선택 `key` · `measureText` · `actionSlots`) · `ui/approval-sheet`(증빙 갈래) · `app/(app)/home-approval-actions.tsx` · 첫 화면 「내 차례」 · 결재함 EMPTY |
| 테스트 | `test/integration/next-turn.test.ts` · `test/integration/expense-inbox.test.ts` · `test/unit/ui/next-turn.test.ts` · `test/e2e/mobile-next-turn-approval.spec.ts` · `test/e2e/expense-inbox.spec.ts` |

<output>
Create `.planning/phases/05-expense-approval-leave/05-10-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| P3-1 파급 — 05-01 E5 `measure` 금액 모양 통일(L38@69fb7ea) | LOW | P3-1 파급 반영 — Plan 05-10, Task 2 behavior(통합)의 외화 기대값을 옛 문자열 모양에서 `money: { currency: "USD", amount: 4200, fxRate: 1318.4, amountKrw: 5537280 }`(숫자 네 필드, 05-01 E5)로 교체 |
| P3-3 파급 — E4 충돌 문구 중립화(L40@69fb7ea) | LOW | Plan 05-10 must_haves truth(동시 처리 문구) · Task 2 behavior(E2E)의 기대 문구를 05-01이 원천 교체한 `{기안자}이 HH:MM에 증빙을 바꿈 · 새로 고침`으로 맞춤 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에서 05-10에 걸린 것은 05-01 계약 변경의 파급 둘뿐이고 둘 다 반영했다 |

### Round 3 — efd6c67

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #87 문구 결정 정렬(코디네이터 16:30Z) — DECISIONS 2026-09-26 오류 문구 명사형 통일 | 사용자 결정 | Plan 05-10 must_haves(「내 차례」 행 줄) · Task 3 behavior E2E: 블록 로드 실패 `불러오지 못했습니다 · 다시 시도` → `불러오기 실패 · 다시 시도`(SYSTEM §7-7 예시는 Plan 05-02 Task 2가 같은 글자로 정렬 · UI-SPEC UI Considerations error S11 같은 글자) |
| D4 — 결재함 행 · 「내 차례」 행에서 `승인`이 막힐 때 이유가 설 자리가 없음(`진행으로 바뀜 · 반려` · F1 문구) — S9 · S11(L34@efd6c67) | MEDIUM | Plan 05-10 must_haves 새 줄 · Task 2 ④: 새 순수 함수 `rowApprovalActions`(`app/(app)/approvals/row-actions.ts`) — 이유가 있으면 `승인` 없음 + 이유 글자(서버 원문 · `--fs-sm --danger`) + 3차 `반려`(가능할 때), PC 결재함 행(`inbox-table.tsx`)과 「내 차례」 PC 행(`home-approval-actions.tsx`)이 함께 쓴다. 05-01 E3의 결재함 PC 행 `disabledReason` 모양은 이 셀 규칙으로 바뀐다(시트 · 문서 화면은 05-01 그대로 — 이 플랜이 뒤 웨이브라 앞 플랜 코드를 덮는 순서가 맞다). 단위 `test/unit/app/approval-row-actions.test.ts`(세 사례) · verify · fails_when · acceptance · files_modified · T-05-1006. 실제 종류로 막힌 셀을 그리는 E2E는 Plan 05-11 Task 3(정산 결재가 첫 `approveBlockedReason` 종류). UI-SPEC S9 「막힌 `승인`」 · S11 · Copywriting 「막힘 — 정산 결재」 반영 |
| G1 — 증빙 무효 뒤 기안자 「내 차례」에 [막힘] `증빙 무효 · 증빙 올리기`, 새 증빙을 올리면 사라짐 — 「내 차례」 행(L46@efd6c67 · L20@efd6c67) | 사용자 결정 | Plan 05-10 must_haves 두 줄(`[막힘]` ② · 종류 선택 필드) · Task 3 ①-b: 종류 정의 선택 필드 `blockedAfterApproval`(덧붙임) · 같은 엔진 조회에 「승인 ∧ 필드 있는 종류」를 더해 종류당 한 번 호출 · 지출결의 종류가 05-09 `listEvidenceVoidSignals`로 채움(`{ situation: 증빙 무효, actionLabel: 증빙 올리기, href: /expenses/{id}#evidence }`) · 순서 반려 다음. behavior 통합 describe `증빙 무효 [막힘]`(기안자만 · 새 증빙 뒤 사라짐 · 전부 무효 뒤 다시 · 종류당 1회) · E2E(desktop — `#evidence` 이동 · 올린 뒤 사라짐) · verify(evidence-void · import-cycles 포함) · fails_when · acceptance(필드 셋 · 종류 키 grep 유지 · kinds.ts 덧붙임만) · artifact · key_link · T-05-1005. 판정 규칙은 Plan 05-09, `#evidence` 섹션 id는 Plan 05-05. UI-SPEC S11 ② · Copywriting 「표시 — 「내 차례」」 반영 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Round 3 지적(D4 · G1 표시)은 전부 위에서 반영했다 |
