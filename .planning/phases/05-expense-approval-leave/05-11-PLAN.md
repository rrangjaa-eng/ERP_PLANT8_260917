---
phase: 05-expense-approval-leave
plan: 11
type: execute
wave: 12
depends_on: ["05-10"]
files_modified:
  - db/schema/settlement-approvals.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - repositories/settlement-approvals.ts
  - domain/settlements/index.ts
  - domain/settlements/dto.ts
  - domain/projects/status-transitions.ts
  - domain/projects/status.ts
  - domain/settings/keys.ts
  - app/(app)/document-kinds.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/settlement-button.tsx
  - app/(app)/projects/[id]/settlement/page.tsx
  - app/(app)/projects/[id]/settlement/settlement-actions.tsx
  - app/(app)/projects/[id]/settlement/loading.tsx
  - app/(app)/projects/[id]/settlement/error.tsx
  - test/integration/leak-scan.test.ts
  - test/integration/settlement-approval.test.ts
  - test/integration/project-status.test.ts
  - test/integration/next-turn.test.ts
  - test/integration/tx-safety.test.ts
  - test/e2e/settlement-approval.spec.ts
  - test/e2e/project-lifecycle.spec.ts
autonomous: true
requirements: [UX-03, UX-06]

estimate:
  tokens: 128000
  raw_tokens: 128000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "정산 결재는 담당 PM이 올리고 대표가 승인한다 — 프로젝트 상태가 정산이고 정산 결재 문서가 없으면 담당 PM(그 프로젝트 쓰기 권리)에게만 프로젝트 상세 머리 줄 2차 `정산 결재 올리기`가 서고, 누르면 확인 창 없이 즉시 기안되며(`정산 결재 올리기…` → 토스트 `정산 결재 올리기 · 결재 요청됨 → {대표}` + 3차 `되돌리기` = 회수), 그 자리가 3차 링크 `정산 결재 중`이 된다(D-98 · D-79 · UI-SPEC 확정 #4 · 사용자 결정 2026-09-26 #1 · UIC interaction S10 (가))"
    - "대표가 정산 결재 문서 화면 `/projects/[id]/settlement`에서 `승인 Ctrl+Enter`를 누르면(또는 결재함 · 결재 시트 · 「내 차례」에서) 최종 승인과 프로젝트 `정산 → 완료` 전환이 **같은 트랜잭션**에서 커밋된다 — Phase 4 `changeProjectStatus`를 결재 종류의 최종 승인 훅(05-01 E2)이 `deps.tx` · 트랜잭션 전 `loadStatusChangeFacts`로 부르고, 행동 로그 `status_change`의 `trigger`가 `approval`이다. 토스트 `승인 · 최종 승인 · {프로젝트 번호} 완료`(ROADMAP 기준 6 · D-79 「전환 함수 그대로, 호출자만 결재 승인」 · 계획 결정 Q5 — `trigger` 선택 입력)"
    - "정산 최종 승인의 권한은 **이 결재 인스턴스의 최종 승인 단계 담당**이라는 사실 하나다 — 훅이 `changeProjectStatus`를 `trigger: \"approval\"`로 부를 때는 결재자의 `projects.complete` 메뉴 · 팀 범위 · 프로젝트 행 범위 판정을 하지 않고, 대신 훅이 같은 tx에서 읽은 「이 인스턴스 지금 차수의 마지막 단계 기록 = viewer의 승인」 사실(마지막 = 지금 차수 단계 행 중 `step_index`가 가장 큰 행 — 결재선을 전부 끄거나 담당이 없어 생긴 대표 폴백 행 `is_fallback` 참도 포함하고 `is_fallback`으로 거르지 않는다, 리뷰 Round 2 P3-4)과 **그 문서 행의 `project_id`**를 담은 `approvalAuthority`(정산 모듈 안에서만 만들 수 있는 브랜드 타입 `SettlementApprovalAuthority` — `domain/projects/status.ts`의 `ChangeProjectStatusDeps`도 구조 타입이 아니라 이 브랜드 타입을 `import type`으로 받아 요구한다)를 요구하고, `changeProjectStatus`는 `approvalAuthority.projectId`가 전환 대상 프로젝트 id와 같을 때만 통과시킨다(다른 프로젝트의 문서로 완료할 수 없다 — 리뷰 Round 1 plan-checker 후속). 관리자가 정산 결재선 마지막 단계를 대표가 아닌 계급(예: `projects.complete`가 없는 경영관리)으로 바꿔도 그 담당의 승인으로 프로젝트가 완료되고, 인스턴스 밖 사람은 승인할 수 없다. 이 판정이 거짓일 사정은 `approveBlockedReason`이 미리 `지금 담당이 아님 · 새로 고침`으로 보인다(리뷰 Round 1 F1 — 권한 교착 제거. 문구는 04.1 「거부 — 동시 처리」 · `NotCurrentHolderError`의 같은 글자를 다시 쓴다 — 새 문구 없음 · 이름 꼬리 없음, design-review D8)"
    - "풀 크기 2에서 대표가 정산 결재 셋을 동시에 최종 승인하면 10초 안에 셋 다 성공하고 세 프로젝트가 완료된다 — 훅 경로(`findFinalStepActorInTx` · `changeProjectStatus` deps.tx)가 트랜잭션 안에서 전역 풀을 부르지 않는다는 통합 증명이 `test/integration/tx-safety.test.ts`에 있다(ARCHITECTURE §4-8 (3) · (6) · 리뷰 Round 2 M3)"
    - "`SettlementApprovalAuthority`를 만드는 길은 `domain/settlements` 안의 비공개 생성 함수 하나다 — 그 밖의 운영 코드(domain · repositories · app · lib · ui · scripts)에 이 타입으로의 타입 단언(캐스트)이 0건이다(리뷰 Round 2 A1)"
    - "사람의 직접 완료 경로는 닫힌다 — 전이 표의 `정산 → 완료` 항목에 `via: \"approval\"`이 붙고, `statusDestinations`는 이 전이를 내보내지 않아 정산 프로젝트에는 「상태 바꾸기」가 렌더되지 않으며, `changeProjectStatus`는 `trigger`가 `approval`이 아니면 이 전이를 거부한다(대표 · 시스템 관리자의 액션 직접 호출 포함). 긴급 탈출구 설정 키는 만들지 않는다(Pitfall 9 · UI Assumptions #17 · CLAUDE.md §3.2)"
    - "대표가 결재 중인데 프로젝트가 기간 변경으로 진행으로 돌아가면(D-80) 문서 화면 · 결재 시트의 대표 `승인`은 `aria-disabled` + `진행으로 바뀜 · 반려`(2차 `반려`는 산다)이고, **결재함 PC 행 · 「내 차례」 PC 행**에서는 `승인`이 렌더되지 않고 그 자리에 이유 글자 `진행으로 바뀜 · 반려`(`--fs-sm --danger`) + 3차 `반려`다(05-10 `rowApprovalActions` — design-review D4). 그래도 서버에 도착한 승인은 훅의 상태 불일치로 전체가 롤백된다. 두 순서(진행 복귀 먼저 / 승인 먼저)가 통합 테스트로 증명된다(E3 · E2 · UIC state S10)"
    - "반려 · 회수된 정산 결재의 기안자에게는 프로젝트가 정산이면 1차 `정산 결재 다시 올리기 Ctrl+Enter`(같은 문서 · 다음 차수 — E1), 진행이면 1차 없이 행동 줄 자리 한 줄 `진행 중 · 정산 뒤 다시 올리기`다. 되돌리기를 눌렀을 때 대표가 이미 승인했으면 오류 토스트 `{대표}가 HH:MM에 승인함 · 새로 고침`(UI-SPEC S10 · Copywriting 266 · 278행)"
    - "정산 결재는 프로젝트마다 하나(`settlement_approvals.project_id` UNIQUE)이고 식별 번호는 프로젝트 번호다 — 새 카운터 없음. 결재선은 문서 종류 정산 결재의 설정 17키(`approval_route.settlement.*` — 기본 1~3단 꺼짐 · 4단 대표 × 전사 · 자기 승인 `self_approve`)로 제출 때 고정되고, 관리자 설정 화면 `정산 결재선` 섹션은 꺼진 단계의 세 칸이 처음부터 비활성으로 보이고(값은 보인다), `지출결의 결재선` · `정산 결재선` 두 섹션의 칸 오류 · 경고는 04.1 S8 규칙 그대로다(사용자 결정 2026-09-26 #6 · UI-SPEC S13 · UIC partial S13 · error S13)"
    - "정산 결재 제출은 지출결의 · 증빙 마감 점검으로 막지 않는다 — 그 점검은 Phase 6 미결 점검(PROJ-06)이다. 매출 세금계산서 발행 요청도 이 문서 종류에 없다(D-100 · D-99)"
    - "정산 결재 문서 화면(S10 (나)): 머리 줄 `정산 결재 — {프로젝트명}` + `{프로젝트 번호}` + 태그, 읽기 칸 프로젝트(3차 링크) · 기간 · 담당 PM · **`견적가 합` · `실행가 합`** · 기안 · 결재선 목록, 행동 줄은 04.1 분기표 + 다시 올리기 갈래. 두 합 행(사용자 결정 G4)은 현재 차수 견적 줄의 견적가(원화) 합 · 실행가(원화 환산) 합 — 견적 줄 표가 그리는 같은 줄 DTO 값(`listQuoteLines`의 `quoteAmountKrw` · `execution.amountKrw`)의 합이라 새 계산 규칙 · 새 정보 항목 · 새 설정이 없고, 정보 항목 `quote.amount`로 투영되어 볼 수 없는 사람에게는 두 행째 없다(견적 표 금액 열과 같은 규칙). 결재 시트 본문(`buildDetailRows`)도 같은 두 행을 같은 자리에 싣는다. 손익 행은 PNL-01(Phase 9)의 네 숫자 함수가 생길 때 같은 자리에 붙는다 — 이 페이즈에서는 모든 계급에 행째 없다(서버가 보내지 않음 · UI-SPEC S10 (나) 와이어의 「(손익) Phase 9부터」, design-review D11). 문서 화면은 한 열 세로 스크롤이고 제목 40자 `keep-all` 줄바꿈(태그 · 번호가 뒤에 흐른다) · 반려 사유는 자르지 않는다. 문서 화면 · 결재 시트의 동시 처리 거부는 04.1 「거부 — 동시 처리」 문구 그대로이고 다이얼로그 · 시트는 닫히지 않는다(UIC partial S10 손익 행 · overflow S10 · long-text S10 · error S10 동시 처리)"
    - "정산 결재 문서가 보이는 사람 = 기안자 ∪ 결재 관련자 ∪ (계급 업무 범위 `company` ∧ 그 프로젝트를 볼 수 있음) — 그 밖은 404. 로딩은 `loading.tsx`(머리 줄 + KvList 뼈대, 300ms 지연), 로드 오류는 `정산 결재 불러오기 실패 · 다시 시도`(UIC loading S10 · error S10 로드 · 권한 밖 — 계획 결정: 경영관리 전용 계급이 시드에 없어 업무 범위로 가른다)"
    - "결재함 문서 칸 `정산 결재 · {프로젝트명}` · 숫자 칸 `—`, 「내 차례」 `[결재] {프로젝트명} — 정산 결재, {기안자}  —  승인 · 반려` · 반려되면 기안자에게 `[막힘] {프로젝트명} — 정산 결재 반려, {대표}  —  정산 결재 열기`(UI-SPEC S9 · S11)"
    - "문서가 생긴 뒤 프로젝트 상세 머리 줄의 같은 자리는 3차 `정산 결재 중` / `정산 결재 반려` / `정산 결재 회수` / `정산 결재 승인`(→ 문서 화면)이고, 완료된 프로젝트에도 `정산 결재 승인` 링크가 남는다. 미저장 편집이 있으면 `정산 결재 올리기`는 비활성 + `저장 안 한 편집 N칸 · 먼저 일괄 저장`(DR-6 같은 함수), 실패는 `정산 결재 올리기 실패 · 다시 시도`(UI-SPEC S10 (가))"
  prohibitions:
    - statement: "정산 최종 승인 훅이 결재자의 `projects.complete` 메뉴 권한 · 팀 범위에 기대서는 안 된다 — 결재선 설정(17키)이 마지막 단계를 그 권한이 없는 계급으로 바꾸면 승인이 매번 전체 롤백되고 탈출구가 없다(리뷰 Round 1 F1). 반대로 `trigger: \"approval\"`가 `approvalAuthority` 없이 통과해서도 안 된다"
      status: resolved
      verification: judgment
    - statement: "정산 결재 없이 프로젝트가 완료되어서는 안 된다 — 사람의 직접 완료 경로가 남으면 D-79 「완료는 결재로만」이 깨진다"
      status: resolved
      verification: judgment
    - statement: "최종 승인과 프로젝트 완료가 다른 트랜잭션에서 커밋되어서는 안 된다 — 승인만 되고 완료가 안 된 문서 · 완료만 된 프로젝트가 생긴다"
      status: resolved
      verification: judgment
    - statement: "정산 결재 다시 올리기가 프로젝트 행을 `FOR UPDATE`로 잡아서는 안 된다 — 승인 경로(인스턴스 → 프로젝트)와 반대 순서로 잠가 교착이 생긴다(RESEARCH Pattern 7 잠금 순서 주의)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/schema/settlement-approvals.ts"
      provides: "settlement_approvals(project_id UNIQUE · drafter_id · timestamps)"
      contains: "settlement_approvals"
    - path: "domain/settlements/index.ts"
      provides: "정산 결재 종류 등록(최종 승인 훅 · 승인 막힘 이유 · 다시 올리기 조건) · submitSettlement · withdrawSettlement · getSettlement"
      exports: ["submitSettlement", "withdrawSettlement", "getSettlement"]
    - path: "app/(app)/projects/[id]/settlement/page.tsx"
      provides: "정산 결재 문서 화면(S10 (나))"
    - path: "test/integration/settlement-approval.test.ts"
      provides: "기안 → 대표 승인 = 완료(같은 tx) · 훅 실패 롤백 · 직접 완료 거부 · D-80 두 순서 · 다시 올리기"
      min_lines: 80
    - path: "test/e2e/settlement-approval.spec.ts"
      provides: "PM 올리기(확인 없음) → 대표 승인 → 완료 · 되돌리기"
      min_lines: 40
  key_links:
    - from: "domain/settlements/index.ts"
      to: "domain/projects/status.ts"
      via: "onFinalApprovalInTx → (tx 안 최종 단계 담당 확인) → changeProjectStatus(viewer, projectId, {from settling, to completed, trigger approval}, {tx, facts, approvalAuthority(projectId 결속)})"
      pattern: "changeProjectStatus"
    - from: "domain/settlements/index.ts"
      to: "domain/approvals/kinds.ts"
      via: "registerDocumentKind(정산 결재 — resubmitFrom 둘 · prepareFinalApproval · onFinalApprovalInTx · approveBlockedReason)"
      pattern: "onFinalApprovalInTx"
    - from: "domain/projects/status.ts"
      to: "domain/projects/status-transitions.ts"
      via: "via approval 전이는 statusDestinations에서 빠지고 trigger approval 없이 거부"
      pattern: "via"
    - from: "app/(app)/document-kinds.ts"
      to: "domain/settlements/index.ts"
      via: "종류 등록 한 곳 import 한 줄"
      pattern: "domain/settlements"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」). 기준 6의 「경영관리가 기안」 문구는 D-98로 「PM 기안 → 대표 승인」이 되며 오케스트레이터가 `gsd_run`으로 ROADMAP · REQUIREMENTS를 맞춘다 — 이 플랜은 D-98대로 구현한다.

<objective>
**정산 결재(D-98)**: 정산 상태 프로젝트의 담당 PM이 머리 줄 버튼 하나로 정산 결재를 올리고, 대표가 승인하는 순간 같은 트랜잭션에서 프로젝트가 완료된다. 사람의 직접 완료 경로를 닫고, 결재 중 진행 복귀(D-80)와 승인의 두 순서를 증명한다.

Purpose: ROADMAP 기준 6 「결재 모듈은 문서 종류 3개 — 지출결의 · 연차 · 정산 결재 — 를 하드코딩 없이」가 여기서 닫힌다(세 번째 종류가 같은 엔진 · 같은 결재함 · 같은 「내 차례」로 간다). 05-01 E1 · E2 · E3의 두 번째 사용처다.

Output: 표 1 · 마이그레이션 1 · 정산 결재 종류 · 설정 키 17 · 전이 표 `via` · `changeProjectStatus` `trigger` · 머리 줄 버튼 · 문서 화면 · 통합 1 새로 + 기존 둘 수정 · E2E 1 새로 + 기존 하나 수정.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@.planning/phases/05-expense-approval-leave/05-RESEARCH.md
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-01-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-11.env"` · diff 검사 접두는 05-01과 같은 모양. 마이그레이션 규칙(05-01) · 트랜잭션 규약 · `.planning/STATE.md` 불변 · 새 의존성 0 · 새 토큰 0.
- **잠금 순서**: 승인 = 인스턴스 UPDATE → (훅) 프로젝트 행 잠금(`changeProjectStatus` 안 `loadProjectForGate`). 기안 · 다시 올리기 = 프로젝트 상태를 tx로 **읽기만**(잠그지 않음) → 인스턴스. 지출결의 제출(프로젝트 → 지출결의 → 다른 인스턴스)과 같은 인스턴스를 공유하지 않는다. 실행자가 경로별 잠금 순서 표를 SUMMARY에 적는다.
- Phase 4 파일(`domain/projects/status.ts` · `status-transitions.ts` · `app/(app)/projects/*`)은 **외과적으로** — 전이 표 한 칸 · 선택 입력 하나 · 갈 곳 목록의 거름 한 줄 · 머리 줄 버튼 자리. 기존 문구 · 다른 전이는 바꾸지 않는다.
- 기존 테스트 `test/integration/project-status.test.ts`(대표 · 시스템 관리자 직접 완료 사례)와 `test/e2e/project-lifecycle.spec.ts`((c) 대표 「완료로 바꾸기」)는 이 플랜이 결재 경로로 **고친다** — 직접 완료가 거부됨을 단언하는 사례로 바꾸고, 완료 성공 사례는 `settlement-approval`로 옮긴다(Pitfall 9). 지운 단언마다 옮긴 자리를 SUMMARY에 적는다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 규모(추정 120k — 05-01 「웨이브 근거」의 규모 근거): Task 2 [BLOCKING]은 명령 셋 · 파일 하나로 얇다. Task 3은 Task 1이 세운 정산 종류 · 훅 위에서 직접 완료 경로를 닫고 경합 사례를 더한다. read_first는 적힌 행 범위만 읽고 테스트 출력은 요약만 본다. 컨텍스트가 50%를 넘으면 task 경계 커밋 뒤 `/gsd-pause-work`로 멈추고 이어 간다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — PM이 정산 프로젝트에서 `정산 결재 올리기`(확인 없음) → 대표가 문서 화면에서 `승인` → 같은 트랜잭션에서 프로젝트 `완료` (스키마 · 종류 · 훅 · 머리 줄 · 문서 화면 · E2E)</name>
  <files>db/schema/settlement-approvals.ts, db/schema/index.ts, repositories/settlement-approvals.ts, domain/settlements/index.ts, domain/settlements/dto.ts, domain/projects/status.ts, domain/settings/keys.ts, app/(app)/document-kinds.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/settlement-button.tsx, app/(app)/projects/[id]/settlement/page.tsx, app/(app)/projects/[id]/settlement/settlement-actions.tsx, app/(app)/projects/[id]/settlement/loading.tsx, app/(app)/projects/[id]/settlement/error.tsx, test/integration/leak-scan.test.ts, test/integration/settlement-approval.test.ts, test/integration/tx-safety.test.ts, test/e2e/settlement-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 385–399행(Pattern 7) · 546–563행(종류 등록 예시)
    - `test/integration/tx-safety.test.ts`(05-01이 덧붙인 `풀 2 · 동시 훅 최종 승인 셋(05-01 E2)` describe와 (e) `풀 2 · 동시 상태 전환 셋(04-20)` — `grep -n "describe("` 뒤 그 두 범위만, Round 2 M3)
    - 04.1 대표 폴백 규칙(`is_fallback` 행 · `step_index` = max + 1 · 켜진 단계 0이면 지금 자리 = 대표 폴백 — name_map의 `walkRoute` · 단계 기록 리포지토리를 `grep -n "is_fallback\|isFallback"` 뒤 그 범위만, Round 2 P3-4)
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S10(552–584행) · Copywriting 249 · 250 · 266 · 277 · 278행
    - `domain/projects/status.ts`(전문 — `loadStatusChangeFacts` · `changeProjectStatus` · `ChangeProjectStatusDeps.tx`/`facts`/`afterLock` · 로그 `trigger` 고정 줄 · `statusDestinations`) · `domain/projects/status-transitions.ts`(전문) · `domain/projects/period.ts`(D-80 진행 복귀 — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4))
    - `app/(app)/projects/[id]/page.tsx`(머리 줄 버튼군 · `statusDestinations` — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4)) · `app/(app)/projects/[id]/status-change.tsx` · `unsaved-edits.ts`
    - 05-01 E1 · E2 · E3 계약 · 05-03 지출결의 종류 등록(같은 모양) · 04.1 문서 화면 행동 줄 · `ui/approval-route`
    - `domain/quotes/lines.ts`의 `getCurrentQuoteRevision` · `listQuoteLines` · `QuoteLineListCtx`(읽기면 `canWrite: false`) · `QUOTE_LINE_DTO_SPEC`(`quoteAmountKrw` · `execution`이 `quote.amount`) — 이름으로 `grep -n` 뒤 범위만(G4 Round 3) · `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S10 (나) 와이어 · 「판단 근거 두 행」(Round 3)
    - 04.1 `NotCurrentHolderError` 문구(`grep -rn "지금 담당이 아님" domain/approvals`) — F1 문구 재사용(D8 Round 3)
  </read_first>
  <behavior>
    - (통합) 픽스처: 정산 상태 프로젝트 `26001`(담당 PM 박서연) · 대표. 박서연 `submitSettlement({ projectId })` → `settlement_approvals` 한 행 · 결재 인스턴스 `submitted` · 단계 행 1(4단 대표 × 전사) · `getSettlement` 상태 `결재 중`. 팀장(프로젝트 쓰기 권리 없음) 호출 → 거부. 진행 상태 프로젝트 → 거부
    - (통합) 대표 `approveDocument` → 인스턴스 `approved` · 프로젝트 `completed` · 행동 로그 `status_change` detail `{ from: "settling", to: "completed", trigger: "approval" }` 한 건 · `document_approve` 한 건
    - (통합, 결재 경로 권한 — F1) 설정 `approval_route.settlement.step4.{role_id,scope,org_unit_id}`를 경영관리 담당(시드에 경영관리 전용 계급이 없으므로 본부 책임자 계급 `DIVISION_HEAD_ROLE_ID` × `org_unit` 경영관리본부 — `projects.complete`가 없음을 픽스처가 먼저 단언)으로 바꾼 뒤 기안 → 경영관리본부 책임자 `getApprovalView`의 `approveBlockedReason` null → 승인 → 인스턴스 `approved` · 프로젝트 `completed` · 로그 `trigger: "approval"`. 인스턴스 밖 사람(`projects.complete`가 있는 대표 · 시스템 관리자 포함)의 `approveDocument` → 04.1 `NotCurrentHolderError`(또는 404) · 프로젝트 `settling` 그대로. `changeProjectStatus(대표, …, { trigger: "approval" })`를 `approvalAuthority` 없이 직접 부르면 `GateBlockedError` · 상태 그대로. 정산 프로젝트 둘(A · B)에서 A의 정산 결재 문서로 만든 `approvalAuthority`(projectId A)를 들고 B에 `changeProjectStatus(…, B, { trigger: "approval" }, { approvalAuthority })` → `GateBlockedError` · B `settling` 그대로(plan-checker 후속)
    - (통합, 대표 폴백 담당 — Round 2 P3-4) 설정 `approval_route.settlement.step4.enabled`를 거짓으로 저장(1~3단은 기본 꺼짐 → 켜진 단계 0 · 담당 없음) → 박서연 기안 → 인스턴스 `submitted` · 지금 자리 = 대표 폴백(04.1 규칙) → 대표 `getApprovalView`의 `approveBlockedReason` null → 대표 승인 → 인스턴스 `approved` · 지금 차수에 `is_fallback` 참인 단계 행 1개 · 프로젝트 `completed` · 로그 `trigger: "approval"`. 훅이 폴백 행을 「마지막 단계 기록」에서 빼면 이 사례가 `지금 담당이 아님`으로 롤백되어 깨진다(F1 교착 재발 방지)
    - (통합, 풀 2 — Round 2 M3) `tx-safety.test.ts` 새 describe `풀 2 · 동시 정산 최종 승인 셋(05-11)`: 정산 상태 프로젝트 셋에 각각 정산 결재를 올려 둔 뒤 `DB_POOL_MAX=2`에서 대표가 세 문서를 `Promise.allSettled`로 동시에 승인 → 10초 안에 셋 다 fulfilled · 세 인스턴스 `approved` · 세 프로젝트 `completed`. 훅 경로가 tx 안에서 전역 풀을 부르면(예: 커밋 뒤에 할 투영 · 사실 읽기를 deps.tx 경로에서 풀로) 두 연결이 트랜잭션에 묶인 채 셋째 연결을 기다려 5초 풀 시간 초과로 깨진다
    - (통합, 롤백) 대표 승인 직전 프로젝트를 리포지토리로 `in_progress`로 바꿔 두면 승인이 `StatusChangedError` 계열 오류로 실패하고 인스턴스 상태 · version · 단계 기록 · 프로젝트 상태 · 로그 수가 전부 그대로
    - (통합, 판단 근거 두 합 — 사용자 결정 G4 · D11) 픽스처 프로젝트 `26001`의 현재 차수 견적 줄(원화 줄 + 외화 줄 하나 이상)로 대표의 `getSettlement` DTO `quoteTotalKrw` = 그 줄들의 `quoteAmountKrw` 합 · `executionTotalKrw` = `execution.amountKrw` 합(기대값은 픽스처 줄 값에서 테스트가 더해 만든다). `quote.amount` 노출을 끈 계급의 결재 관련자(픽스처로 만든 결재선 담당)에게는 두 필드가 없고 `buildDetailRows` 결과에 `견적가 합` · `실행가 합` 행이 없다. 어떤 계급의 결과에도 `손익` 라벨 행이 없다
    - (E2E, desktop) 박서연 → 정산 프로젝트 상세 머리 줄 2차 `정산 결재 올리기` → 확인 창 없음 · 토스트 `정산 결재 올리기 · 결재 요청됨 → {대표}` · 그 자리 3차 `정산 결재 중` → 대표 로그인 → `/projects/{id}/settlement` 머리 줄 `정산 결재 — {프로젝트명}` · `{프로젝트 번호}` · 태그 `결재 중` · 읽기 칸에 `견적가 합` · `실행가 합` 행(값은 천 단위 쉼표 원화 — G4)이 있고 `손익` 행이 없다(D11) · `승인 Ctrl+Enter` → 토스트 `승인 · 최종 승인 · {프로젝트 번호} 완료` · 프로젝트 상세 태그 `완료` · 머리 줄 `정산 결재 승인` 링크
  </behavior>
  <action>
① **스키마** 새 파일 `db/schema/settlement-approvals.ts`: `settlement_approvals`(id uuid PK · `project_id` uuid FK projects not null UNIQUE · `drafter_id` text FK users not null · `created_at` · `updated_at` — 시각 열은 기존 관례의 무 tz `timestamp`). 상태 열 없음(인스턴스에서 파생). `db/schema/index.ts` 끝 export 한 줄. `pnpm db:generate`(적용 · 가드는 Task 2).
② **설정 키** `domain/settings/keys.ts` 끝에 `approval_route.settlement.*` 17키(연차 · 지출결의와 같은 선언 · 라벨 · optionLabels, namespace `정산 결재선`, 기본값 1~3단 `enabled` 거짓 · 4단 대표 계급 × `company` · 자기 승인 `self_approve`).
③ **종류** 새 파일 `domain/settlements/index.ts` — 적재 때 `registerDocumentKind({ kind: 정산 결재 키, label: "정산 결재", loadRouteConfig(17키 한 번), href: doc → /projects/{projectId}/settlement, describeDocuments(문서 칸 `정산 결재 · {프로젝트명}` · measure null · nextTurnText `{프로젝트명}` / `정산 결재, {기안자}`), routeSettings, canResubmit: 프로젝트 쓰기 권리 ∧ 프로젝트 상태 settling, resubmitFrom: [rejected, withdrawn], prepareFinalApproval: (viewer, documentId) → { projectId, facts: loadStatusChangeFacts(viewer) }(트랜잭션 전 — 문구용 라벨 · 시작일 규칙 사실만 쓰이고 메뉴 · 팀 범위 값은 결재 경로에서 쓰이지 않는다), onFinalApprovalInTx: (viewer, documentId, tx, p) → ⓐ 같은 tx에서 `repositories/settlement-approvals.ts` `findFinalStepActorInTx(viewer, { documentId }, tx)`가 **정산 결재 문서 행의 `project_id`**와 「이 문서 인스턴스 지금 차수의 **마지막** 단계 기록이 viewer의 `approved`」인지를 한 번에 읽어 `{ projectId } | null`을 돌려준다(엔진이 (6) 단계 기록 · 폴백 행 쓰기를 훅보다 먼저 한다 — 05-01 E2 순서. **마지막 단계 기록 = 지금 차수 단계 행 중 `step_index`가 가장 큰 행이고 대표 폴백 행(`is_fallback` 참 — 04.1: 켜진 단계가 없거나 담당이 없을 때 대표 계급이 승인하며 `step_index` = max + 1로 기록)을 포함한다. `is_fallback`으로 거르거나 결재선 설정의 마지막 켜진 단계 번호로 찾지 않는다** — 그러면 결재선을 전부 끈 설정에서 대표의 폴백 승인이 매번 롤백되어 F1 교착이 되살아난다, Addresses review concern: P3-4(Round 2)) — null이거나 돌려받은 `projectId`가 `p.projectId`(트랜잭션 전 값)와 다르면 `GateBlockedError("지금 담당이 아님 · 새로 고침")`(04.1 `NotCurrentHolderError`와 같은 글자 — 새 문구 없음, Addresses review concern: D8(Round 3)) ⓑ 통과하면 `domain/settlements/index.ts` 안의 비공개 생성 함수로 `approvalAuthority`(브랜드 타입 — `declare const brand: unique symbol;` 뒤 `export type SettlementApprovalAuthority = { projectId: string; documentId: string } & { readonly [brand]: true }`(타입 리터럴 안 `unique symbol`은 TS1335라 쓰지 않는다). 브랜드 심벌 · 생성 함수는 export하지 않고 타입만 export — `domain/projects/status.ts`가 `import type`으로 쓴다)를 만들어 changeProjectStatus(viewer, **tx에서 읽은 projectId**, { from: "settling", to: "completed", trigger: "approval" }, { tx, facts: p.facts, approvalAuthority }) ⓒ ⓐ · ⓑ가 던지면 `log.warn("settlement.final_approval_rolled_back", { documentId, projectId, reason })`(reason = 오류 클래스 이름 · 사람 이름 · 금액 없음 — `lib/log.ts`, 선례 `domain/rules/deny-write.ts` `write.denied`) 뒤 그대로 다시 던진다(Addresses review concern: F1 · F7), approveBlockedReason: 프로젝트가 settling이 아니면 `진행으로 바뀜 · 반려`, 지금 단계가 결재선의 마지막 단계인데 viewer가 그 단계 담당으로 해석되지 않으면 `지금 담당이 아님 · 새로 고침`(04.1 문구 재사용 — D8(Round 3) · 읽기 전용 · 메뉴 권한은 보지 않는다 — F1. 「지금 단계 · 담당」은 엔진의 지금 자리 해석을 그대로 쓴다 — 대표 폴백 자리면 담당 후보 = 대표 계급 재직자이고, 결재선 설정만 보고 따로 판정하지 않는다, Round 2 P3-4), loadDetails · detailDto · buildDetailRows(프로젝트 · 기간 · 담당 PM · `견적가 합` · `실행가 합` · 기안 · 결재선 — 손익 행 없음) })`. **판단 근거 두 합(Addresses review concern: G4 · D11(Round 3) — 사용자 결정)**: `loadDetails` · `getSettlement`가 프로젝트의 현재 차수(`getCurrentQuoteRevision`)의 `listQuoteLines(viewer, …)` 결과에서 `quoteAmountKrw` 합 → `quoteTotalKrw`, `execution.amountKrw` 합 → `executionTotalKrw`를 만든다 — 견적 줄 표 합계 행 · 차익과 같은 줄 값의 단순 합이라 새 계산 규칙 · 세금 · 환율 계산이 없다. 줄 DTO에 두 필드가 없으면(viewer가 `quote.amount`를 못 봄 — 투영이 뺐음) 합도 만들지 않는다. 트랜잭션 없음(읽기). `domain/settlements/dto.ts` 명세(`project.value` · 결재 정보 항목 · **`quoteTotalKrw` · `executionTotalKrw`는 `quote.amount`** — 견적 표 금액 열과 같은 정보 항목, 새 항목 없음). `buildDetailRows`는 투영 뒤 두 필드가 있을 때만 `견적가 합` · `실행가 합` 행을 만든다(원화 서식 · 천 단위 쉼표 — 기존 원화 서식 함수). 함수: `submitSettlement(viewer, { projectId })`(트랜잭션 전: 프로젝트 쓰기 권리 · 상태 settling · `prepareSubmission`; 트랜잭션 안: 프로젝트 상태를 tx로 읽기만 → 문서 행 삽입(`ON CONFLICT (project_id) DO NOTHING` + 재조회 — 이미 있고 인스턴스가 반려 · 회수면 `resubmitDocument`, 진행 중이면 기존 문서 그대로) → `submitDocument`) · `withdrawSettlement(viewer, { projectId, expectedVersion, undo? })`(05-09 `withdrawExpense`와 같은 모양) · `getSettlement(viewer, { projectId })`(보임 판정 must_haves → null이면 404).
④ **`changeProjectStatus` 선택 입력** `domain/projects/status.ts`: input에 선택 `trigger?: "manual" | "approval"`(기본 manual)을 더하고 로그 detail의 고정 `trigger`를 그 값으로 바꾼다(계획 결정 Q5). **결재 경로 권한(Addresses review concern: F1)**: `ChangeProjectStatusDeps`에 선택 `approvalAuthority?: SettlementApprovalAuthority`(③의 브랜드 타입)를 더한다 — `status.ts`는 `import type { SettlementApprovalAuthority } from "@/domain/settlements"`로만 가져온다(`test/unit/import-cycles.test.ts`는 `import type`을 런타임 간선으로 세지 않으므로 순환이 아니다). 구조 타입 리터럴로 받지 않는다 — 그러면 서버 쪽 호출자가 리터럴을 넘겨 rowScope · 메뉴 · 팀 판정을 건너뛸 수 있다(plan-checker 둘째 후속). **브랜드 위치(리뷰 Round 2 P3-5 검토 — 비채택, 선언 · 생성 둘 다 `domain/settlements`에 둔다)**: 브랜드를 `domain/projects` 쪽에 선언하면 정산 모듈이 값을 만드는 길은 ⓐ projects가 생성 함수를 export하거나(누구나 만들 수 있어 「tx 안 최종 단계 판정을 통과했을 때만 값이 생긴다」는 결속이 끊긴다) ⓑ settlements가 그 타입으로 캐스트하는 것뿐이다(결속이 타입이 아니라 관례 · grep에만 기대게 된다). 선언과 비공개 생성 함수가 판정 코드(`findFinalStepActorInTx` 호출)와 같은 모듈에 있어야 캐스트 없이는 만들 수 없다. 역방향 의존은 `import type` 한 줄(런타임 간선 0 · import-cycles 테스트 녹색)이라 비용이 작다. **캐스트 차단(Addresses review concern: A1(Round 2))**: 브랜드는 타입 수준 보호라 캐스트로 위조된다 — `domain/settlements` 밖 운영 코드에 이 타입으로의 캐스트가 0건임을 acceptance grep으로 확인한다. eslint `no-restricted-syntax` 대신 grep을 고른 이유: 이 타입 하나를 위해 설정 블록을 새로 두는 것보다 단순하고(CLAUDE.md §3.2), 선택자도 타입 별칭 경유 캐스트는 못 잡아 grep과 보호 범위가 같으며, 실제 경계는 런타임 판정(`approvalAuthority.projectId === projectId` · tx 안 최종 단계 확인 · 액션 입력 스키마에 필드 없음)이다. 병합된 트리에서는 05-13 전체 게이트가 같은 grep을 한 번 더 돈다. `test/`는 grep에서 뺀다 — 결속 거부 통합 사례(다른 프로젝트 문서의 권한)가 위조한 권한 값으로 런타임 결속을 증명하려면 테스트 안 캐스트가 필요하다. `trigger === "approval"`이면 ⓐ `approvalAuthority`가 없거나 `approvalAuthority.projectId !== projectId`(함수 인자)면 첫 판정으로 `GateBlockedError`(문구 `지금 담당이 아님 · 새로 고침` — 04.1 같은 글자, D8(Round 3)) — 트랜잭션 전 `rowScope` none 거부(`changeProjectStatus` 머리)를 건너뛰는 대신 이 결속이 경계다 ⓑ 통과하면 viewer의 행 범위(`rowScope` none · 보관 포함) 거부와 `TRANSITION_RULE`의 메뉴 · 팀 범위 판정을 건너뛴다 — 두 `evaluateTransition` 호출에 `menus.complete` 참 · 팀 범위 전사로 바꾼 결재 경로 사본 사실을 넘기는 방식(함수 이름은 실행자가 정하되 `status.ts` 안 한 곳). `from` 불일치(D-80 — `StatusChangedError`) · 시작일 규칙 · 행 잠금(`loadProjectForGate` — `lockProjectForWrite`는 viewer로 거르지 않음을 Grep으로 확인)은 그대로 돈다. `trigger`가 manual이면 기존 경로 한 줄도 바뀌지 않는다. `app/(app)/projects/actions.ts`의 상태 변경 액션 입력 스키마에는 `trigger` · `approvalAuthority`를 넣지 않는다(외부에서 결재 경로를 흉내 낼 수 없게). 직접 완료 차단은 Task 3.
⑤ **화면** `app/(app)/projects/actions.ts`에 `submitSettlementAction` · `withdrawSettlementAction`(+ 등록). `app/(app)/projects/[id]/page.tsx`가 정산 상태 · 문서 상태 · 쓰기 권리 · 미저장 편집 사실을 `settlement-button.tsx`(클라이언트)에 넘긴다 — 문서 없음 + 담당 PM = 2차 `정산 결재 올리기`(즉시 · pending · DR-6 비활성 · 실패 한 줄 · 성공 토스트 + `되돌리기` = `withdrawSettlementAction` undo), 문서 있음 = 3차 링크 `정산 결재 {중|반려|회수|승인}`. 새 `app/(app)/projects/[id]/settlement/page.tsx`(RSC · `getSettlement` null → `notFound()` · 읽기 칸 KvList 순서 = 프로젝트 · 기간 · 담당 PM · `견적가 합` · `실행가 합`(DTO에 그 필드가 있을 때만 — `tabular-nums` · `nowrap`, G4) · 기안 · 결재선 · 손익 행 없음(D11)) + `settlement-actions.tsx`(04.1 행동 줄 컴포넌트 재사용 · `Ctrl+Enter` · 반려/회수 04.1 S6 확인 — 제목 `정산 결재 반려` / `정산 결재 회수`) + `loading.tsx`(04.1 지연 CSS) + `error.tsx`(`정산 결재 불러오기 실패 · 다시 시도` · `retry`). `app/(app)/document-kinds.ts`에 `import "@/domain/settlements";` · `test/integration/leak-scan.test.ts` 끝에 같은 import 한 줄.
⑥ behavior 통합을 새 파일 `test/integration/settlement-approval.test.ts`에, E2E를 새 스펙 `test/e2e/settlement-approval.spec.ts`에 먼저 적는다. **풀 2 증명(Addresses review concern: M3(Round 2))**: `test/integration/tx-safety.test.ts` 끝에 describe `풀 2 · 동시 정산 최종 승인 셋(05-11)` 하나를 **덧붙인다**(기존 describe · 사례 · `setupProject` 불변). 모양은 05-01이 더한 `풀 2 · 동시 훅 최종 승인 셋`과 같다 — 픽스처(정산 상태 프로젝트 셋 · 담당 PM · 대표 · 각 `submitSettlement`)를 보통 모듈로 만들고(상태는 같은 파일 (f)처럼 리포지토리 갱신 선례 가능) → `process.env.DB_POOL_MAX = "2"` → `vi.resetModules()` → 격리 그래프에서 `@/domain/settlements`(적재가 곧 종류 등록 — 격리 레지스트리에 들어간다)와 `@/domain/approvals`를 import → 풀 `max` 2 단언 → 대표의 세 `approveDocument` `Promise.allSettled` → 10초 · 셋 다 fulfilled · 인스턴스 셋 `approved` · 프로젝트 셋 `completed` → 격리 그래프 `closeDb()` · `DB_POOL_MAX` 복원. `sleep` 없음.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/settlement-approval.test.ts test/integration/leak-scan.test.ts test/integration/approvals-extensions.test.ts test/integration/tx-safety.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 두 합(`quoteTotalKrw` · `executionTotalKrw`)이 픽스처 줄 값의 합과 다르거나 `quote.amount`를 못 보는 사람에게 실리거나 어떤 결과에 `손익` 행이 있거나(G4 · D11 Round 3), `projects.complete` 없는 계급이 최종 단계 담당일 때 승인이 롤백되거나(F1), 결재선을 전부 끈 설정에서 대표 폴백 승인이 롤백되거나(Round 2 P3-4), 풀 2에서 정산 최종 승인 셋이 10초를 넘기거나 하나라도 거부되거나 `tx-safety`의 기존 사례가 깨지거나(Round 2 M3), 정산 결재 키가 엔진 파일에 따옴표 리터럴로 나오거나(05-01 `종류 키 리터럴 없음` — Round 2 P3-2), 인스턴스 밖 사람의 승인 · `approvalAuthority` 없는 `trigger: "approval"` 호출 · 다른 프로젝트 문서의 `approvalAuthority`로 한 전환이 통과하거나, 승인 뒤 프로젝트가 완료가 아니거나, 로그 `trigger`가 approval이 아니거나, 진행 복귀 뒤 승인이 부분 커밋되거나, 담당 PM이 아닌 사람 · 진행 프로젝트의 기안이 통과하거나, 누수 스캔이 정산 결재 DTO · 액션을 못 잡으면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run --project unit test/unit/settings test/unit/import-cycles.test.ts &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/settlement-approval.spec.ts'</automated>
    <fails_when>린트 · 타입 · registry-coverage · 순환 · 빌드 중 하나라도 실패하거나 playwright failed가 1 이상이다 — 올리기에 확인 창이 뜨거나, 토스트 · 머리 줄 링크 · 완료 태그가 다르거나, 문서 화면에 `견적가 합` · `실행가 합`이 없거나 `손익` 행이 있으면(G4 · D11 Round 3) 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "onFinalApprovalInTx" domain/settlements/index.ts`가 1 이상이고 `grep -c "changeProjectStatus" domain/settlements/index.ts`가 1 이상이다
    - 엔진에 종류 이름 없음(Round 2 P3-2 — 옛 고정 문자열 grep을 대신한다): `grep -c '^import "@/domain/' "app/(app)/document-kinds.ts"`가 3 이상(연차 · 지출결의 · 정산 결재)이고, 05-01의 `test/integration/approvals-extensions.test.ts` describe `종류 키 리터럴 없음 — 등록된 종류 전부`가 첫 검증 명령에서 녹색이다 — 그 사례는 등록 모듈이 `registerDocumentKind`에 실제로 넘긴 키를 모아 엔진 파일을 검사하고 키 수 ≥ import 줄 수를 단언하므로, 정산 결재 키가 무엇이든 이 플랜의 import 한 줄로 검사 대상에 들어간다
    - 브랜드 캐스트 0건(Round 2 A1): `grep -rnE '\bas[[:space:]]+SettlementApprovalAuthority\b|(^|[^A-Za-z0-9_$])<SettlementApprovalAuthority>' --include='*.ts' --include='*.tsx' domain repositories app lib ui scripts | grep -vc '^domain/settlements/'`가 0이다(`test/`는 제외 — ④ 이유)
    - 풀 2 증명이 덧붙임뿐이다(Round 2 M3) — `grep -c "풀 2 · 동시 정산 최종 승인 셋" test/integration/tx-safety.test.ts`가 1이고, `. "$(git rev-parse --git-dir)/gsd-05-11.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- test/integration/tx-safety.test.ts) && printf '%s\n' "$D" | grep -c '^-[^-]'`이 0을 출력한다
    - 대표 폴백 사례가 있다(Round 2 P3-4) — `grep -c "is_fallback\|isFallback" test/integration/settlement-approval.test.ts`가 1 이상이다
    - `grep -c "trigger: \"manual\"" domain/projects/status.ts`가 0이다(고정값 → 입력값)
    - `grep -c "projects.complete" domain/settlements/index.ts`가 0이다(정산 훅 · 승인 막힘 이유가 메뉴 권한을 보지 않음 — F1) · `grep -rln "approvalAuthority" domain app | sort`가 `domain/projects/status.ts` · `domain/settlements/index.ts` 둘뿐이다(액션 입력에 없음)
    - `grep -c "settlement.final_approval_rolled_back" domain/settlements/index.ts`가 1이다(F7)
    - (D8 Round 3) `grep -c "지금 담당이 아님 · 새로 고침" domain/settlements/index.ts`가 1 이상이고 `grep -rn "최종 승인 담당 아님" domain app`가 0건이다(옛 F1 문구 없음 — 04.1 문구 재사용)
    - (G4 · D11 Round 3) `grep -c "quoteTotalKrw" domain/settlements/dto.ts`와 `grep -c "executionTotalKrw" domain/settlements/dto.ts`가 둘 다 1 이상이고 두 필드의 정보 항목이 `quote.amount`다(실행자가 명세 줄을 SUMMARY에 인용) · `grep -c "견적가 합" domain/settlements/index.ts`가 1 이상이다(손익 행 부재는 E2E · 통합 사례가 단언한다)
    - `domain/projects/status.ts`가 정산 모듈을 타입으로만 가져온다 — `grep -cE '^import type \{[^}]*SettlementApprovalAuthority[^}]*\} from "@/domain/settlements"' domain/projects/status.ts`가 1이고 `grep -E '^import .*"@/domain/settlements"' domain/projects/status.ts | grep -vc '^import type'`가 0이다(런타임 import 없음) · `grep -c "SettlementApprovalAuthority" domain/projects/status.ts`가 2 이상(import + deps 필드) · `pnpm vitest run --project unit test/unit/import-cycles.test.ts` 녹색(plan-checker 둘째 후속)
    - `grep -c "approvalAuthority.projectId !== projectId\|approvalAuthority?.projectId !== projectId" domain/projects/status.ts`가 1 이상이고 `grep -c "finalStepActor" domain/projects/status.ts domain/settlements/index.ts`가 두 파일 모두 0이다(자기 선언 플래그 없음 — plan-checker 후속) · 브랜드 생성 함수가 export되지 않는다(`grep -nE "^export .*(makeSettlementApprovalAuthority|approvalAuthorityBrand)" domain/settlements/index.ts`가 0건)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>PM이 버튼 하나로 정산 결재를 올리고 대표 승인과 프로젝트 완료가 한 트랜잭션으로 커밋된다</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 스키마 확정 — `settlement_approvals` 마이그레이션 적용 · SQL 린트 · journal 가드</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `db/migrations/meta/_journal.json` · Task 1이 만든 SQL · 05-01 Task 2(같은 규칙)
  </read_first>
  <action>
① `pnpm db:generate` 재실행이 새 파일을 만들지 않음을 확인한다. ② 빈 DB 적용(`pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`, 개발 DB `pnpm db:migrate`) · `pnpm lint:sql` — 기존 표(`projects` · `users`) FK 문장이 squawk 잠금 규칙에 걸리면 그 앞에 SET LOCAL 블록 하나만. ③ SUMMARY `migration_manifest`.
  </action>
  <verify>
    <automated>G=$(pnpm db:generate 2&gt;&amp;1); RC=$?; printf '%s\n' "$G"; test "$RC" -eq 0 &amp;&amp; grep -q "No schema changes" &lt;&lt;&lt;"$G"</automated>
    <fails_when>종료 코드가 0이 아니거나 출력에 "No schema changes"가 없다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db'</automated>
    <fails_when>빈 DB 적용 실패 · squawk 위반 1건 이상 · journal 가드 실패 · "No test files found" 중 하나</fails_when>
  </verify>
  <acceptance_criteria>
    - 이 플랜의 SQL이 정확히 하나 · 이름 그대로 · journal diff가 끝 항목 하나 추가뿐(`. "$(git rev-parse --git-dir)/gsd-05-11.env" && test -n "$PLAN_BASE" && git diff "$PLAN_BASE"..HEAD -- db/migrations/meta/_journal.json`)
    - SQL 손 편집이 SET LOCAL 블록뿐 · `migration_manifest` 세 경로가 실제 파일과 같다
    - 두 검증 명령이 초록이다
  </acceptance_criteria>
  <done>정산 결재 표가 빈 DB부터 적용되고 재생성 차이 0 · squawk 0 · journal 가드 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 직접 완료 경로 닫기 · D-80 두 순서 · 승인 막힘 표시 · 다시 올리기 / `진행 중 · 정산 뒤 다시 올리기` · 늦은 되돌리기 · 기존 Phase 4 완료 테스트를 결재 경로로</name>
  <files>domain/projects/status-transitions.ts, domain/projects/status.ts, domain/settlements/index.ts, app/(app)/projects/[id]/settlement/settlement-actions.tsx, app/(app)/projects/[id]/settlement-button.tsx, test/integration/settlement-approval.test.ts, test/integration/project-status.test.ts, test/integration/next-turn.test.ts, test/e2e/settlement-approval.spec.ts, test/e2e/project-lifecycle.spec.ts</files>
  <read_first>
    - `test/integration/project-status.test.ts`(직접 완료 사례 · 갈 곳 목록 사례 — `grep -n "completed\|statusDestinations"` 뒤 범위 Read) · `test/e2e/project-lifecycle.spec.ts`((c) 대표 완료 — `grep -n "완료"` 뒤 범위 Read)
    - `domain/projects/status.ts`(`statusDestinations` · `evaluateTransition` · `changeProjectStatus` — 함수 이름으로 `grep -n` 뒤 범위 Read, 리뷰 Round 1 F4) · `domain/projects/period.ts`(D-80 진행 복귀 함수 — `afterLock` 주입 가능 여부)
    - `test/integration/lock-race.ts` · 04.1 동시성 도우미
    - Task 1 산출물
  </read_first>
  <behavior>
    - (통합) `changeProjectStatus(대표, projectId, { from: "settling", to: "completed" })`(trigger 없음) → `GateBlockedError` · 프로젝트 `settling` 그대로. 시스템 관리자도 같다. `statusDestinations(대표, 정산 프로젝트)` → `settling → completed` 없음(빈 목록)
    - (통합, D-80 ① 진행 복귀 먼저) 결재 중 문서에서 PM이 기간을 바꿔 프로젝트가 `in_progress` → 대표 `getApprovalView` · `listMyInbox` 항목 `approveBlockedReason` = `진행으로 바뀜 · 반려` → 대표 승인 → 실패 · 전부 롤백 → 대표 반려(사유) → `rejected` · 기안자 `canResubmit` 거짓(진행)
    - (통합, D-80 ② 승인 먼저) `afterLock`으로 승인 트랜잭션이 프로젝트 행을 잡은 동안 기간 변경 시도 → 승인 커밋 뒤 기간 변경은 완료 프로젝트 잠금(D-47)으로 거부 · 프로젝트 `completed`
    - (통합) 반려 뒤 프로젝트가 다시 정산이면 `submitSettlement`가 같은 문서 · 차수 2로 다시 올린다(E1). 회수(undo) 뒤도 같다. 대표 승인 뒤 PM의 undo 회수 → 거부 · 오류 구조에 대표 이름 · 시각 · `approved`
    - (통합) 대표의 `listNextTurnItems` → `[결재] {프로젝트명} — 정산 결재, 박서연` · 숫자 `—`. 반려 뒤 박서연 → `[막힘] … 정산 결재 반려, {대표}` · `정산 결재 열기` → `/projects/{id}/settlement`
    - (E2E, desktop — 막힌 `승인` 셀, design-review D4) 같은 진행 복귀 상태에서 대표의 `/approvals` `내 결재` 그 문서 행과 `/` 「내 차례」 `[결재]` 그 행: 행 안에 `승인` 버튼이 0개 · 글자 `진행으로 바뀜 · 반려`가 있고 계산된 색이 `--danger` 값 · 3차 `반려`가 있고 누르면 04.1 반려 확인(제목 `정산 결재 반려`)이 열린다
    - (E2E, desktop) 진행으로 돌아간 프로젝트의 정산 결재 문서 화면에서 대표 `승인`이 `aria-disabled` + `진행으로 바뀜 · 반려` · `반려`는 살아 있음. 반려 뒤 PM 화면(진행) = 1차 없음 + `진행 중 · 정산 뒤 다시 올리기`. 정산으로 다시 온 뒤 = 1차 `정산 결재 다시 올리기 Ctrl+Enter`. PM이 올린 직후 대표가 승인한 상태에서 PM이 토스트 `되돌리기` → 오류 토스트 `{대표}가 HH:MM에 승인함 · 새로 고침`
    - (E2E, 기존 수정) `project-lifecycle.spec.ts` (c): 정산 프로젝트에 대표에게도 「상태 바꾸기」가 없다
  </behavior>
  <action>
① `domain/projects/status-transitions.ts`: `ProjectStatusTransition`에 선택 `via?: "approval"`, `settling → completed` 항목에 `via: "approval"` 한 칸(데이터 한 칸 — 다른 항목 불변). `domain/projects/status.ts`: `changeProjectStatus`가 권한 판정 전에 그 전이가 `via: "approval"`이고 입력 `trigger`가 approval이 아니면 `GateBlockedError`(문구 `정산 결재로만 완료 · 정산 결재 올리기` — 화면에는 이 경로가 렌더되지 않으므로 직접 호출 방어용), `statusDestinations`는 `via: "approval"` 전이를 내보내지 않는다. 긴급 탈출구 설정은 만들지 않는다.
② `domain/settlements/index.ts`의 `approveBlockedReason` · `canResubmit`을 behavior대로 확인하고, 문서 화면 · 머리 줄 버튼이 서버 값으로 `승인` 막힘 · 다시 올리기 1차 · 한 줄 `진행 중 · 정산 뒤 다시 올리기` · 늦은 되돌리기 오류 토스트를 그린다(05-09 지출결의 토스트와 같은 구조).
③ 기존 테스트 수정: `test/integration/project-status.test.ts`의 대표 · 시스템 관리자 직접 완료 성공 사례를 「trigger 없는 직접 완료 거부」로 바꾸고 완료 성공은 `settlement-approval.test.ts`로 옮긴다. 갈 곳 목록 사례에서 정산 → 완료 기대를 지운다. `test/e2e/project-lifecycle.spec.ts` (c)를 「정산 프로젝트는 누구에게도 「상태 바꾸기」가 없다」로 바꾼다. 지운 단언과 옮긴 자리를 SUMMARY 표로.
④ behavior 통합(D-80 두 순서 · 다시 올리기 · undo 거부 · 「내 차례」)을 `test/integration/settlement-approval.test.ts` · `test/integration/next-turn.test.ts`에, E2E를 `test/e2e/settlement-approval.spec.ts`에 덧붙인다 — 경합은 sleep 없이 `afterLock`.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/settlement-approval.test.ts test/integration/project-status.test.ts test/integration/next-turn.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — trigger 없는 직접 완료가 통과하거나, 갈 곳 목록에 완료가 남거나, D-80 두 순서 중 하나에서 승인 · 진행 복귀가 둘 다 커밋되거나, 반려 · 회수 뒤 다시 올리기가 새 문서를 만들거나, 승인 뒤 undo가 통과하면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/settlement-approval.spec.ts test/e2e/project-lifecycle.spec.ts test/e2e/project-period.spec.ts'</automated>
    <fails_when>린트 · 타입 · 빌드 오류가 있거나 playwright failed가 1 이상이다 — 정산 프로젝트에 「상태 바꾸기」가 보이거나, 승인 막힘 표시 · 다시 올리기 갈래 · 늦은 되돌리기 문구가 다르거나, 결재함 PC 행 · 「내 차례」 PC 행에 막힌 문서의 `승인`이 남거나 이유 글자 · `반려`가 없거나(D4 Round 3), 기간 변경 E2E가 깨지면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'via: "approval"' domain/projects/status-transitions.ts`가 1이다
    - `domain/projects/status-transitions.ts` diff가 타입 한 칸 + 항목 한 칸뿐이다(`. "$(git rev-parse --git-dir)/gsd-05-11.env" && test -n "$PLAN_BASE" && D=$(git diff -U0 "$PLAN_BASE"..HEAD -- domain/projects/status-transitions.ts) && printf '%s\n' "$D" | grep -c '^+[^+]'`이 3 이하를 출력)
    - `grep -c "afterLock" test/integration/settlement-approval.test.ts`가 1 이상이고 `grep -cE "setTimeout|sleep\(" test/integration/settlement-approval.test.ts`가 0이다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>완료는 정산 결재 승인으로만 일어나고, 결재 중 진행 복귀와 승인의 경합이 두 순서 모두 한쪽만 커밋된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 결재 엔진 최종 승인 → 프로젝트 상태 | 결재 결과가 다른 도메인의 되돌릴 수 없는 전이(완료 잠금)를 일으킨다 |
| 호출자 → `changeProjectStatus` | 직접 호출로 결재를 우회할 수 있다 |
| 기간 변경(D-80) ↔ 승인 | 두 쓰기가 같은 프로젝트 행에서 경합한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-1101 | Elevation of Privilege | 결재 없이 완료(직접 호출) | high | mitigate | `via: "approval"` 전이는 `trigger` approval 없이 거부 · 갈 곳 목록에서 제외 — 대표 · 시스템 관리자 거부 통합 사례 · E2E 버튼 부재 |
| T-05-1102 | Tampering | 승인 · 완료 부분 커밋 | high | mitigate | 훅이 같은 tx · 실패 전파 · 트랜잭션 전 사실 읽기(05-01 E2) — 롤백 통합 사례 |
| T-05-1103 | Tampering | D-80 진행 복귀 ↔ 승인 경합 | medium | mitigate | 훅 안 `changeProjectStatus`가 프로젝트 행 잠금 안에서 `from` 불일치를 거부 · 완료 뒤 기간 변경은 D-47 잠금 — `afterLock` 두 순서 사례 |
| T-05-1104 | Elevation of Privilege | 담당 PM이 아닌 사람의 기안 · 다시 올리기 | medium | mitigate | 프로젝트 쓰기 권리 + 상태 settling을 트랜잭션 전 판정 · `canResubmit` — 팀장 거부 사례 |
| T-05-1105 | Information Disclosure | 정산 결재 문서 조회 · 판단 근거 두 합(G4) | medium | mitigate | 보임 = 기안자 · 결재 관련자 · 전사 범위 ∧ 프로젝트 보임 — 그 밖 404. 손익 행은 이 페이즈에 없음(D11). `견적가 합` · `실행가 합`은 정보 항목 `quote.amount`로 투영(견적 표 금액 열과 같은 규칙) — 못 보는 결재 관련자에게는 두 필드 · 행 없음 통합 사례 |
| T-05-1106 | Denial of Service | 다시 올리기 ↔ 승인 잠금 순서 교착 | medium | mitigate | 다시 올리기는 프로젝트를 읽기만(금지 항목) — 잠금 순서 표 SUMMARY |
| T-05-1107 | Elevation of Privilege / DoS | 결재 경로 권한 — 메뉴 권한 없는 최종 담당의 영구 롤백(F1) · `trigger: approval` 흉내 | high | mitigate | 결재 경로는 메뉴 · 팀 범위 · rowScope 대신 훅이 tx 안에서 읽은 「마지막 단계 기록 = viewer 승인」 + 문서 행 `project_id`를 담은 `approvalAuthority`(정산 모듈 밖에서 만들 수 없는 브랜드 타입 — `ChangeProjectStatusDeps`가 구조 타입이 아니라 `import type`한 `SettlementApprovalAuthority`를 요구해 리터럴 전달은 타입 오류)만 보고, `changeProjectStatus`가 `approvalAuthority.projectId === projectId`를 요구한다(자기 선언 플래그 없음 · 트랜잭션 전 `p.projectId`는 tx 값과 대조) · 없거나 다르면 거부 · 다른 프로젝트 문서로 전환 거부 통합 사례(plan-checker 후속) · 액션 입력 스키마에 `trigger` · `approvalAuthority` 없음(grep) · 경영관리 최종 담당 성공 / 인스턴스 밖 거부 / 권한 사실 없는 직접 호출 거부 통합 사례 · 롤백은 `log.warn` `settlement.final_approval_rolled_back`(F7) · `domain/settlements` 밖 운영 코드의 브랜드 캐스트 0건 grep(Round 2 A1) · 「마지막 단계 기록」이 대표 폴백 행 포함 + 결재선 전부 끔 사례(Round 2 P3-4) |
| T-05-1108 | Denial of Service | 정산 최종 승인 훅의 tx 안 풀 읽기 | medium | mitigate | 훅 경로는 tx만 쓴다(05-01 트랜잭션 규약) — 풀 2 · 동시 정산 최종 승인 셋 통합 증명(`tx-safety.test.ts`, ARCHITECTURE §4-8 (6) · Round 2 M3) |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개(verification의 lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과(registry-coverage · import-cycles · journal 가드)
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration'` 전체 통과(Phase 4 `project-status` 수정본 포함)
- `CI=true` E2E: `settlement-approval` · `project-lifecycle` · `project-period` 녹색
- 풀 2 정산 최종 승인 셋(`tx-safety.test.ts` — Round 2 M3) · 대표 폴백 최종 승인(Round 2 P3-4) · 브랜드 캐스트 0건 grep(Round 2 A1)
- `pnpm db:generate` 재실행 "No schema changes" · lockfile diff 0
- **의식적 예외 기록(리뷰 Round 1 P3-5)**: UI 확정 #3(행 · 시트 `승인` 즉시 — 확인 창 없음)이 되돌릴 수 없는 정산 결재 최종 승인(프로젝트 완료 잠금)에도 적용된다. 사용자 결정이라 유지하되 CLAUDE.md §7 「확인은 되돌릴 수 없는 일에만」의 의식적 예외임을 05-11 SUMMARY의 `## 결정 · 예외` 절에 한 줄로 남긴다(근거: UI-SPEC 확정 #3 · 대표만 최종 단계 기본값)
</verification>

<success_criteria>
- 결재 모듈이 문서 종류 셋(지출결의 · 연차 · 정산 결재)을 하드코딩 없이 다룬다(기준 6)
- 프로젝트 완료는 정산 결재 대표 승인으로만, 한 트랜잭션에서 일어난다(D-98 · D-79)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `settlement_approvals`(project_id UNIQUE) |
| 마이그레이션 | drizzle-kit 이름 SQL 1개 + 스냅숏(05-13 재생성 · `migration_manifest`) |
| 문서 종류 | 정산 결재(`registerDocumentKind` — E1 · E2 · E3 사용) |
| 함수 | `submitSettlement` · `withdrawSettlement` · `getSettlement` · `changeProjectStatus` 선택 `trigger` · 전이 `via` |
| 설정 키 | `approval_route.settlement.*` 17(1~3단 꺼짐 · 4단 대표 × 전사) |
| 화면 | 프로젝트 상세 머리 줄 `정산 결재 올리기` / 상태 링크 · `/projects/[id]/settlement`(문서 · 로딩 · 오류) |
| 액션 | `submitSettlementAction` · `withdrawSettlementAction` |
| 테스트 | `test/integration/settlement-approval.test.ts` · `test/e2e/settlement-approval.spec.ts` · `project-status`(직접 완료 → 거부로 수정) · `project-lifecycle`((c) 수정) · `next-turn`(정산 결재 줄 추가) |

<output>
Create `.planning/phases/05-expense-approval-leave/05-11-SUMMARY.md` when done — `## 결정 · 예외`에 P3-5(UI 확정 #3 × 정산 최종 승인 = CLAUDE.md §7 의식적 예외) 한 줄, 잠금 순서 표, 결재 경로 권한(F1) 판정 위치 한 줄을 포함한다
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F1 — 정산 최종 승인 훅이 결재자 viewer로 `changeProjectStatus`를 불러 `projects.complete` 메뉴 검사에 걸리면 매번 전체 롤백 · 탈출구 없음(L17@1108e20) | HIGH | Plan 05-11, Task 1 ③ · ④: `trigger: "approval"`이면 메뉴 · 팀 범위 · 행 범위 판정 대신 훅이 같은 tx에서 읽은 「이 인스턴스 지금 차수 마지막 단계 기록 = viewer 승인」(`approvalAuthority`)만 요구 · `approveBlockedReason`에 `최종 승인 담당 아님 · 새로 고침` · must_haves truth · prohibition · T-05-1107 · 통합 사례(마지막 단계를 경영관리본부 책임자로 바꾼 설정 → 승인 = 완료 / 인스턴스 밖 사람 거부 / 권한 사실 없는 직접 호출 거부) · acceptance grep(`projects.complete` 0 · `approvalAuthority` 두 파일뿐) |
| F4 — read_first의 Phase 4 줄 범위(예: `status.ts 195–230행`)(L22@1108e20) | MEDIUM | Plan 05-11, Task 1 · Task 3 read_first: 코드 파일 줄 범위를 전부 「함수 이름으로 `grep -n` 뒤 범위 Read」로 교체. 04.1 전제 대조 단계는 05-01 Task 1 ⓪-b(다르면 05-11 재점검 요청) |
| F7 — 정산 훅 롤백 운영 로그 없음(L25@1108e20) | MEDIUM | Plan 05-11, Task 1 ③ ⓒ: `log.warn("settlement.final_approval_rolled_back", { documentId, projectId, reason })` 후 다시 던짐 · acceptance grep |
| P3-5 — UI 확정 #3(행 승인 즉시)이 되돌릴 수 없는 정산 결재 승인에도 적용(L34@1108e20) | LOW | Plan 05-11 `<verification>` · `<output>`: 사용자 결정 유지 + CLAUDE.md §7 의식적 예외로 SUMMARY `## 결정 · 예외`에 기록 |
| P3-7 — 결재 차례 알림은 Phase 7(L36@1108e20) | LOW | 기록만 — 이 플랜의 「내 차례」 줄(Task 3)이 알림 전까지의 유일한 신호다. 조치 없음 |

| F1 후속(Round 1 plan-checker follow-up, 0517e87 뒤) — `approvalAuthority = { documentId, finalStepActor: true }`가 자기 선언 플래그라 documentId ↔ projectId 대조가 없고, 결재 경로에서 rowScope · 메뉴 · 팀 판정을 건너뛰어 서버 쪽 호출자가 `true`를 그대로 넘길 수 있음 | HIGH(F1 연장) | Plan 05-11, Task 1 ③ⓐⓑ · ④: `findFinalStepActorInTx`가 같은 tx에서 문서 행 `project_id` + 마지막 단계 기록 = viewer 승인을 읽어 `projectId`를 돌려주고(트랜잭션 전 `p.projectId`와 다르면 거부), `approvalAuthority`는 정산 모듈 비공개 생성 함수만 만드는 브랜드 타입 `{ projectId, documentId }` · `changeProjectStatus`는 `approvalAuthority.projectId === projectId`를 요구. `finalStepActor` 플래그 제거 · acceptance grep · 통합 사례(다른 프로젝트 문서의 권한 → `GateBlockedError`) · T-05-1107 갱신. D-80 `from` 불일치 · 행 잠금 문구는 그대로 |
| F1 둘째 후속(Round 1 plan-checker follow-up 2, b6b0e99 뒤) — ④가 `approvalAuthority`를 구조 타입 `{projectId, documentId}`로 받아 ③의 브랜드가 힘이 없음 · 타입 리터럴 안 `unique symbol`(TS1335) | MEDIUM(F1 연장) | Plan 05-11, Task 1 ③ · ④: 브랜드 선언을 `declare const brand: unique symbol` + `{ readonly [brand]: true }`로 · `ChangeProjectStatusDeps.approvalAuthority`가 `import type { SettlementApprovalAuthority } from "@/domain/settlements"`를 요구(import-cycles 테스트는 타입 import를 세지 않음) · must_haves truth · T-05-1107 · acceptance grep(타입 전용 import · 런타임 import 0) · `projectId` 같음 판정은 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| F1 보조안 — 정산 결재선 설정 저장 때 마지막 단계 계급이 `projects.complete`를 갖는지 검증(L17@1108e20) | 채택하지 않음 — 주 수리로 최종 승인 권한이 메뉴 권한과 분리되어 그 검증이 막을 실패가 사라졌다. 오히려 관리자가 경영관리 담당을 마지막 단계로 두는 정당한 설정을 막는다. 04.1 S8 결재선 칸 규칙 그대로 둔다 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| M3 — 정산 최종 승인 경로에 풀 압력 증명 없음, `tx-safety.test.ts` 사례 없음(L32@69fb7ea) | MEDIUM | Plan 05-11, Task 1 ⑥ · behavior · verify · acceptance: `tx-safety.test.ts` 끝에 describe `풀 2 · 동시 정산 최종 승인 셋(05-11)` 덧붙임(격리 그래프에서 `@/domain/settlements` 적재 = 종류 등록 · 대표 동시 승인 셋 → 10초 안 셋 다 성공 · 프로젝트 셋 완료) · 기존 사례 diff 삭제 0 · files_modified · must_haves truth · T-05-1108 · verification |
| A1 — 브랜드 타입은 캐스트로 위조 가능, acceptance grep은 생성 함수 export만 봄(L35@69fb7ea) | MEDIUM | Plan 05-11, Task 1 ④ · acceptance · must_haves truth · T-05-1107: `domain/settlements` 밖 운영 코드(domain · repositories · app · lib · ui · scripts)에 `as` · 꺾쇠 단언 0건 grep. **eslint `no-restricted-syntax` 대신 grep 선택** — 타입 하나를 위한 설정 블록보다 단순하고(§3.2), 선택자도 별칭 경유 캐스트는 못 잡아 보호 범위가 같으며, 실제 경계는 런타임 판정(projectId 결속 · tx 안 최종 단계 확인 · 액션 스키마에 필드 없음)이다. `test/`는 제외(위조 권한으로 결속 거부를 증명하는 사례가 캐스트를 쓴다). 병합 트리 재확인은 05-13 Task 2 acceptance |
| P3-2 — 「종류 이름 리터럴 0건」 grep이 고정 문자열만 봄(L39@69fb7ea) | LOW | Plan 05-11, Task 1 acceptance · fails_when: 고정 문자열 grep을 05-01 통합 사례 `종류 키 리터럴 없음 — 등록된 종류 전부`(실제 등록 키 수집 · 키 수 ≥ import 줄 수)로 대체 + `document-kinds.ts` import 줄 3 이상 |
| P3-4 — 훅의 「지금 차수 마지막 단계 기록」이 대표 폴백 행(`is_fallback`)을 포함하는지 불명 · 4단 끔 · 담당 없음 사례 없음(L41@69fb7ea) | LOW | Plan 05-11, Task 1 ③ⓐ · approveBlockedReason · behavior · acceptance · must_haves truth: 마지막 단계 기록 = 지금 차수 `step_index` 최댓값 행, 폴백 행 포함 · `is_fallback`이나 설정의 마지막 켜진 단계로 거르지 않음 · 막힘 이유의 「지금 담당」은 엔진의 지금 자리 해석(폴백 자리 = 대표 계급) · 통합 사례 1(`step4.enabled` 거짓 → 켜진 단계 0 → 대표 폴백 승인 = 완료 · `is_fallback` 행 1) |
| M5(05-11 쪽) — 정산 결재 경로 잠금 순서(인스턴스 → 프로젝트) 예외가 SUMMARY에만(L34@69fb7ea) | MEDIUM | 05-11 변경 없음 — `<output>`의 SUMMARY 잠금 순서 표를 05-13 Task 2 ⑤가 ARCHITECTURE §4-8 예외 한 줄의 근거로 읽는다(05-13 Round 2 원장) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| P3-5 — `domain/projects/status.ts` → `domain/settlements` `import type` 역의존, 브랜드를 projects 쪽에 선언하고 생성만 settlements에서 하는 배치 검토(L42@69fb7ea) | 검토 뒤 비채택(Task 1 ④에 이유 기록) — projects 쪽 선언이면 정산 모듈이 값을 만들 길이 ⓐ projects의 공개 생성 함수(누구나 만들어 「tx 안 최종 단계 판정 통과 → 값」 결속이 끊김) 또는 ⓑ settlements 안 캐스트(결속이 관례 · grep에만 기댐)뿐이다. 선언 · 비공개 생성 함수 · 판정 호출이 한 모듈에 있어야 캐스트 없이 만들 수 없다. 역의존은 `import type` 한 줄(런타임 간선 0 · import-cycles 녹색)이라 비용이 작다. A1 grep이 남는 구멍(캐스트)을 막는다 |

### Round 3 — efd6c67

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| #87 문구 결정 정렬(코디네이터 16:30Z) — DECISIONS 2026-09-26 오류 문구 명사형 통일 | 사용자 결정 | Plan 05-11 must_haves · action ⑤: `정산 결재를 불러오지 못했습니다 · 다시 시도` → `정산 결재 불러오기 실패 · 다시 시도` · `정산 결재를 올리지 못했습니다 · 다시 시도` → `정산 결재 올리기 실패 · 다시 시도`(UI-SPEC Copywriting 「Error — 로드」 · 「Error — 행동 실패」 같은 글자) |
| D4 — 결재함 행 · 「내 차례」 행에서 막힌 `승인`(`진행으로 바뀜 · 반려` · F1 문구)의 이유가 설 자리가 없음(L34@efd6c67) — 이 플랜 몫 | MEDIUM | 셀 규칙 구현은 Plan 05-10 Task 2 ④(`rowApprovalActions`). Plan 05-11: must_haves(D-80 줄)에 행 셀 모양 · Task 3 behavior E2E(진행 복귀 뒤 대표의 `/approvals` 행 · `/` 「내 차례」 행: `승인` 버튼 0 · 이유 글자 `진행으로 바뀜 · 반려`(`--danger`) · 3차 `반려` → 04.1 반려 확인) · fails_when — 정산 결재가 이 페이즈에서 `approveBlockedReason`을 주는 첫 종류라 실제 종류 E2E가 여기 있다 |
| D8 — F1 문구는 04.1 `지금 담당이 아님 · 새로 고침`을 다시 쓰는 편이 낱말이 적다, 새 문구를 유지하면 `· 담당 {이름}` 꼬리 검토(L40@efd6c67) | LOW | **채택(04.1 문구 재사용)**. 04.1-UI-SPEC Copywriting 「거부 — 동시 처리」와 04.1-02 `NotCurrentHolderError`에 같은 글자가 있음을 확인했다. Plan 05-11 must_haves(F1 줄) · Task 1 ③ 훅 `GateBlockedError` · `approveBlockedReason` · ④ `changeProjectStatus` 결속 실패 문구 · behavior(대표 폴백 사례 설명) · read_first · acceptance(새 문구 ≥ 1 · 옛 문구 0건)를 `지금 담당이 아님 · 새로 고침`으로. 이름 꼬리는 붙이지 않는다 — 04.1 ENG-6 D1이 이 문구에 이름 · 시각을 싣지 않는 규칙(비관련자 정보 노출 방지)이라 꼬리를 달면 그 규칙과 어긋난다. 오류 클래스는 `GateBlockedError` 그대로. UI-SPEC Copywriting 「막힘 — 정산 결재」 · S9 반영. Round 1 F1 행의 옛 문구 기록은 append-only라 그대로 둔다(이 행이 문구만 대체한다) |
| D11 — S10 (나) 와이어의 `손익` 행 ↔ 이 플랜 「이 페이즈에서는 모든 계급에 행째 없다」 불일치(L43@efd6c67) | LOW | 이 플랜 쪽 규칙(행 없음)이 맞다 — UI-SPEC S10 (나) 와이어를 「(손익) Phase 9부터 — 이 페이즈에는 행 없음」으로, 본문 · UI Considerations(partial · overflow S10)를 같은 뜻으로 고쳤다. Plan 05-11 must_haves(문서 화면 줄)에 와이어 표시를 가리키고 · Task 1 ⑤ KvList 순서에 손익 행 없음 · behavior(통합 · E2E — 어떤 결과에도 `손익` 행 없음) · fails_when |
| G4 — 정산 결재 문서 화면에 견적 표에 이미 있는 합계(견적가 합 · 실행가 합) 한 행씩, 새 계산 없음 · 금액 보임 규칙 그대로(L49@efd6c67 · L23@efd6c67) | 사용자 결정 | Plan 05-11 must_haves(문서 화면 줄) · Task 1 ③: `getSettlement` · `loadDetails`가 현재 차수 `listQuoteLines` 결과의 `quoteAmountKrw` 합 · `execution.amountKrw` 합(견적 줄 표가 그리는 같은 줄 값의 단순 합 — 새 계산 규칙 · 새 정보 항목 · 새 설정 없음), `domain/settlements/dto.ts` 두 필드 = `quote.amount`, `buildDetailRows`는 투영 뒤 필드가 있을 때만 두 행(결재 시트도) · ⑤ 문서 화면 KvList `담당 PM` 아래 두 행 · read_first(`getCurrentQuoteRevision` · `listQuoteLines` · `QUOTE_LINE_DTO_SPEC`) · behavior 통합(합 = 픽스처 줄 합 · 못 보는 결재 관련자에게 없음) · E2E(두 행 있음) · fails_when · acceptance · T-05-1105 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Round 3 지적(D4 E2E · D8 · D11 · G4)은 전부 위에서 반영했다. 정산 즉시 승인의 §7 예외를 디자인 정본에 적는 일(D7 · B9)은 Plan 05-02 Task 3 — 이 플랜의 SUMMARY `## 결정 · 예외` 한 줄(Round 1 P3-5)은 그대로 둔다 |
