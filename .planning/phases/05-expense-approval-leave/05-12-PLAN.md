---
phase: 05-expense-approval-leave
plan: 12
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - lib/gcp/storage.ts
  - lib/gcp/gcs-v4.ts
  - infra/names.sh
  - scripts/bootstrap-gcp.sh
  - scripts/deploy.sh
  - scripts/verify-gcp.sh
  - docs/OPERATIONS.md
  - scripts/gcs-sign-smoke.ts
  - test/unit/lib/gcp/storage-gcs.test.ts
  - test/unit/deploy/deploy-sh.test.ts
  - test/unit/deploy/bootstrap-sh.test.ts
autonomous: false
requirements: [EVID-01]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`STORAGE_DRIVER=gcs`(staging · prod의 해석 기본값)이면 `getObjectStorage()`가 GCS 드라이버를 만든다 — 서명 PUT · GET 주소는 V4(`X-Goog-Algorithm=GOOG4-RSA-SHA256` · `X-Goog-Credential={SA}/{YYYYMMDD}/auto/storage/goog4_request` · `X-Goog-Date` · `X-Goog-Expires` · `X-Goog-SignedHeaders` · `X-Goog-Signature` 16진)이고, 서명은 이미 의존성인 `google-auth-library`의 `GoogleAuth.sign()`(Cloud Run 런타임 SA는 개인 키가 없어 IAM `signBlob`)으로 한다 — 새 패키지 없음 · 키 파일 없음(RESEARCH 권고 · Package Legitimacy Audit — `@google-cloud/storage`는 SUS라 설치하지 않는다)"
    - "서명 PUT은 `content-type` · `host` · `x-goog-content-length-range: 1,{한도 바이트}` · `x-goog-meta-sha256` 헤더를 서명에 묶는다 — 브라우저가 같은 값을 보내지 않으면 GCS가 거부한다. 메타데이터 재확인은 GCS JSON API(`storage/v1/b/{bucket}/o/{key}`)의 크기 · 형식 · 사용자 메타 sha256이고, 불일치 객체 삭제도 같은 API다(05-04 도메인 규칙의 실제 드라이버)"
    - "`GCS_EVIDENCE_BUCKET`이 없는데 gcs 드라이버를 만들면 설정 오류(부팅이 아니라 첫 사용 때)다"
    - "서명기(`GoogleAuth.sign` — IAM signBlob)가 실패하면 gcs 드라이버가 `log.warn(\"storage.sign_failed\", { op, reason })`(op ∈ `put` · `get`, reason = 오류 클래스 · 코드 — 객체 키 · 파일명 · 이메일 없음) 한 줄을 남기고 오류를 그대로 올린다(`lib/log.ts` · 선례 `domain/rules/deny-write.ts` — 리뷰 Round 1 F7)"
    - "gcs 드라이버의 `move(fromKey, toKey)`(05-04 인터페이스)는 JSON API 재작성(`POST storage/v1/b/{bucket}/o/{from}/rewriteTo/b/{bucket}/o/{to}` — 응답이 `done: false`면 받은 `rewriteToken`으로 `done: true`까지 다시)으로 복사한 뒤 원본을 `DELETE`한다(원본 삭제 404는 성공 · 재작성 실패면 원본을 지우지 않고 오류). 업로드는 `incoming/{의도 id}`에 받고 완료 통보가 `evidence/{파일 id}`로 옮긴다 — 부트스트랩의 수명 주기 규칙 하나(접두어 **`incoming/`** · 나이 7일 → 삭제)는 완료 통보 없이 남은 업로드만 지우고, 완료된 증빙이 있는 `evidence/`는 어떤 삭제 규칙에도 걸리지 않는다. 새 패키지 없음(사용자 결정 E2: A · M1)"
    - "`retain(key)`는 JSON API로 `evidence/` 객체에 임시 보존 표식(`temporaryHold: true`)을 건다 — 1차 보호는 접두어 분리이고 표식은 운영자 실수 삭제 · 앱 버그 삭제를 막는 **이중 방어**다(유지 판단: 세무 증빙은 법정 보관 대상이라 PATCH 한 번의 비용으로 사람 · 코드의 잘못된 삭제를 막을 값이 있다. 05-04가 커밋 뒤 부르고 실패는 경고만). 지운 증빙(`removed_at`)의 객체 정리는 표식 해제를 포함해 Phase 6 F8 그대로다(재작성이 형식 · 사용자 메타 sha256을 그대로 옮기는지는 [ASSUMED] — Task 3 스파이크가 옮긴 뒤 메타데이터를 확인)"
    - "실제 GCS가 이 드라이버의 V4 서명 PUT · GET을 받는지는 웨이브 13(05-13)을 기다리지 않고 이 플랜 끝의 DB 없는 스파이크(`scripts/gcs-sign-smoke.ts` — staging 버킷에 `incoming/` 서명 PUT · 메타데이터 · `evidence/`로 옮기기 · 옮긴 뒤 메타데이터 · 보존 표식 · 서명 GET · 크기 초과 PUT 거부 · 삭제 한 번)로 사람이 먼저 확인한다. 05-13 Task 3의 전체 staging 확인은 그대로다(리뷰 Round 1 F5)"
    - "증빙 버킷 `plant8-{env}-evidence`(서울 `asia-northeast3` · 균일 버킷 수준 접근 · 공개 접근 방지 강제)를 `scripts/bootstrap-gcp.sh`가 만들고, 런타임 SA에 **버킷 범위** `roles/storage.objectUser`와 **자기 자신에 대한** `roles/iam.serviceAccountTokenCreator`(signBlob), 배포 SA에 버킷 범위 `roles/storage.admin`(CORS 유지)을 준다 — 프로젝트 범위 저장소 역할을 런타임 SA에 주지 않는다(RESEARCH Pattern 5 인프라 · Security 「서명 권한 과다」)"
    - "`scripts/deploy.sh`가 배포마다 버킷이 있는지 확인하고(없으면 부트스트랩을 가리키는 오류로 멈춤) CORS(원점 = 서비스 주소 · 메서드 `PUT` · `GET` · 응답 헤더 `Content-Type` · 요청 허용 헤더 `Content-Type` · `x-goog-content-length-range` · `x-goog-meta-sha256`)를 멱등으로 맞추며, 서비스 환경 변수에 `STORAGE_DRIVER=gcs` · `GCS_EVIDENCE_BUCKET`을 넣는다. `scripts/verify-gcp.sh`가 버킷 · 균일 접근 · 공개 접근 방지 · 역할 · CORS 원점을 확인한다"
    - "`docs/OPERATIONS.md`에 「증빙 버킷」 절이 있다 — 이름 · 만드는 스크립트 · 역할 · 환경 변수 · 접두어 둘(`incoming/` 업로드 · 7일 삭제 / `evidence/` 완료 증빙 · 삭제 규칙 없음 + 보존 표식) · staging 스모크 절차(05-13 checkpoint가 그대로 따른다) · 지운 증빙과 트랜잭션 거부 뒤 남은 `evidence/` 객체 정리는 Phase 6 F8(M1 Round 2 — 의도만 있고 완료 없는 객체는 이제 수명 주기가 지운다)"
  prohibitions:
    - statement: "서비스 계정 키 파일 · 개인 키를 코드 · 저장소 · 시크릿에 두어서는 안 된다 — 서명은 IAM signBlob"
      status: resolved
      verification: judgment
    - statement: "새 npm 의존성(`@google-cloud/storage` 등)을 사용자 승인 없이 설치해서는 안 된다(CLAUDE.md §5 — 패키지 검사 SUS)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "lib/gcp/gcs-v4.ts"
      provides: "순수 V4 서명 조립 — canonicalRequest · stringToSign · signedUrl(서명 함수 주입)"
      exports: ["buildV4SignedUrl"]
    - path: "lib/gcp/storage.ts"
      provides: "createGcsStorage({ bucket, auth }) + getObjectStorage의 gcs 분기"
      contains: "createGcsStorage"
    - path: "test/unit/lib/gcp/storage-gcs.test.ts"
      provides: "로컬 생성 RSA 키로 서명 → 공개 키 검증(배관) · 쿼리 · 서명 헤더 · 메타데이터 파싱"
      min_lines: 60
    - path: "docs/OPERATIONS.md"
      provides: "증빙 버킷 절 — 역할 · 환경 변수 · staging 스모크"
      contains: "증빙 버킷"
  key_links:
    - from: "lib/gcp/storage.ts"
      to: "lib/gcp/gcs-v4.ts"
      via: "createSignedPut · createSignedGet이 buildV4SignedUrl에 GoogleAuth.sign을 주입"
      pattern: "buildV4SignedUrl"
    - from: "scripts/deploy.sh"
      to: "infra/names.sh"
      via: "evidence_bucket $ENV → GCS_EVIDENCE_BUCKET 환경 변수 · CORS 대상"
      pattern: "evidence_bucket"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**실제 GCS 드라이버와 증빙 버킷 인프라**: 05-04가 만든 `ObjectStorage` 인터페이스의 gcs 구현(V4 서명 URL · 메타데이터 재확인 · `incoming/` → `evidence/` 옮기기 · 보존 표식 · 삭제)을 새 패키지 없이 `google-auth-library`로 만들고, 회사 GCP의 증빙 버킷 · CORS · 최소 IAM을 부트스트랩 · 배포 스크립트에 넣는다. 실제 GCS가 서명을 받는지는 이 플랜 Task 3 스파이크(DB 없음 · 서명 PUT/GET 한 번)에서 사람이 먼저 확인하고, 05-13의 staging checkpoint가 앱 전체 경로로 다시 확인한다(RESEARCH A8 · A9 — 공식 문서 확인 불가 · 리뷰 Round 1 F5).

Purpose: EVID-01의 운영 경로가 선다 — 로컬 · CI는 05-04의 로컬 드라이버, staging · prod는 이 플랜의 gcs 드라이버. 호출자(도메인 · 화면)는 바뀌지 않는다.

Output: `lib/gcp/gcs-v4.ts` · gcs 드라이버(`retain` 포함) · 스크립트 넷 수정(수명 주기 규칙 포함) · 스파이크 스크립트 1 · 운영 문서 절 · 단위 1 새로 + 스크립트 단위 둘 수정 · 사람 확인 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-RESEARCH.md
@.planning/phases/05-expense-approval-leave/05-04-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development` · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(서명 조립 · IAM 판단은 `opus`, 스크립트 수정은 `sonnet`)을 적는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-12.env"` · diff 검사 접두는 05-01과 같은 모양. `.planning/STATE.md` 불변.
- **새 의존성 0** — `google-auth-library` 11.1.0은 이미 lockfile에 있다. SDK로 바꾸자는 판단이 들면 멈추고 사용자에게 묻는다(05-RESEARCH Alternatives 표 — 인터페이스가 같아 바꿔도 호출자는 불변). 식별자(프로젝트 id · 번호 · 이메일)를 스크립트 · 코드에 적지 않는다(`infra/names.sh` 머리 규칙).
- V4 알고리즘 세부는 [ASSUMED] — 단위 테스트는 배관(서명 → 검증 · 필드 모양)만 증명하고, 실제 GCS 수락은 Task 3 스파이크(사람 확인)가 먼저, 05-13 checkpoint가 앱 전체로 증명한다. 이 플랜은 staging에 **앱을** 배포하지 않는다 — 스파이크는 로컬에서 런타임 SA 가장(impersonation)으로 staging 버킷만 부른다. Task 3에서 사람을 기다리므로 `autonomous: false`다(05-05는 같은 웨이브에서 계속 돌 수 있다 — 파일 겹침 0).
- 웨이브: 05-01 「웨이브 근거」 — 이 플랜은 05-04 뒤 05-05와 같은 웨이브(5)에서 나란히 돈다. 단위 테스트만 돌리고 DB · 브라우저를 쓰지 않으며 05-05와 파일 겹침이 0이다. 읽는 것은 05-04의 `ObjectStorage` 인터페이스 · `lib/env.ts` 두 변수뿐이고, `pnpm build`는 `flock /tmp/plant8-erp-test.lock`으로 05-05 빌드와 직렬화된다(plan-checker info — 계획 수정 2026-09-26).
- **같은 작업 트리 금지(Addresses review concern: P3-8(Round 2))**: 05-05의 lint · build · E2E는 `lib/gcp/storage.ts`를 import한다. 같은 작업 트리에서 나란히 돌면 이 플랜이 반쯤 고친 그 파일을 05-05가 읽어 엉뚱한 곳에서 깨진다. execute-phase는 `workflow.use_worktrees`(`.planning/config.json`에 없음 → 기본 true)로 같은 웨이브 플랜을 각자 worktree에서 돌리므로 기본 설정이면 그대로 병렬이다. 격리가 꺼지는 환경(`use_worktrees=false` · worktree를 쓸 수 없는 호스트 — ISOLATION `none`)이면 병렬로 돌리지 말고 **순차(05-05 → 05-12)**로 돈다. 수동 실행이면 Superpowers `using-git-worktrees`로 별도 worktree를 만든다(새 worktree에서 `pnpm install --frozen-lockfile` — 새 의존성 아님).
- **사람 확인이 웨이브 5를 멈춘다(P3-8)**: Task 3 checkpoint는 사람이 staging 버킷을 부트스트랩하고 스파이크를 돌린 뒤 `approved`를 줄 때까지 이 플랜을 끝내지 않는다. 웨이브 6(05-15) 이후는 웨이브 5 전체가 끝나야 시작하므로 05-05가 먼저 끝나도 페이즈 실행이 여기서 선다 — 사람이 곧바로 응답할 수 있는 때에 웨이브 5를 시작한다. 이 checkpoint는 `workflow.human_verify_mode: end-of-phase`의 의도된 예외(Round 1 F5 — 서명 거부를 웨이브 13 전에 잡는다)이고, 자동 승인되지 않게 `gate="blocking-human"`이다(사람이 GCP 소유자 계정으로 해야 하는 일이라 자동 승인은 뜻이 없다).
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — gcs 드라이버 한 줄: `createSignedPut` → V4 서명 주소(주입 서명기) → 공개 키로 서명 검증 → 메타데이터 조회 · 삭제(주입 요청 함수) → `getObjectStorage()` gcs 분기</name>
  <files>lib/gcp/gcs-v4.ts, lib/gcp/storage.ts, test/unit/lib/gcp/storage-gcs.test.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 363–366행(gcs 드라이버 · 인프라) · 145행(SDK 대안 표)
    - `node_modules/.pnpm/google-auth-library@11.1.0/node_modules/google-auth-library/build/src/auth/googleauth.js` 878–919행(`sign()` — JWT 개인 키 / Impersonated / IAM `signBlob` 갈래 · 반환 형식)과 `getCredentials` · `request` 정의
    - 05-04 산출물 `lib/gcp/storage.ts`(인터페이스 · 로컬 드라이버 · `getObjectStorage`) · `lib/env.ts`(`resolvedStorageDriver`)
    - 기존에 `google-auth-library`를 쓰는 코드(Cloud SQL Admin 조회 — `grep -rln "google-auth-library" lib scripts`)의 생성 · 범위 지정 모양
  </read_first>
  <behavior>
    - (단위) 테스트가 `crypto.generateKeyPairSync("rsa")`로 만든 키로 서명기(`(stringToSign) => base64 서명`)를 주입하고 `createSignedPut("incoming/{uuid}", { contentType: "image/jpeg", maxBytes: 10485760, sha256, expiresSec: 900 })` → 주소 호스트 `storage.googleapis.com` · 경로 `/{bucket}/incoming/{uuid}` · 쿼리에 여섯 X-Goog 키 · `X-Goog-SignedHeaders` = `content-type;host;x-goog-content-length-range;x-goog-meta-sha256` · 돌려준 헤더 셋(Content-Type · `x-goog-content-length-range: 1,10485760` · `x-goog-meta-sha256`) · `X-Goog-Signature`(16진)를 공개 키로 검증하면 참(같은 입력으로 테스트가 다시 만든 string-to-sign)
    - (단위) `createSignedGet(key, { expiresSec: 300, filename: "세금계산서.jpg", disposition: "inline" })` → `response-content-disposition`이 서명된 쿼리에 있고 한글 파일명이 RFC 5987 꼴로 인코딩된다 · `X-Goog-Expires` 300
    - (단위) 만료가 604800초를 넘으면 오류. 서명기가 던지면 오류가 그대로 오르고(주소를 만들지 않음) 주입한 로그 함수에 `storage.sign_failed` 한 줄(`op` · `reason`만 — 키 · 이메일 없음)이 남는다(F7)
    - (단위) `retain(key)`가 주입 요청 함수로 JSON API 객체 주소에 `PATCH` 본문 `{ temporaryHold: true }`를 보낸다 · 요청이 실패하면 오류가 오른다(F9 — 경고로 낮추는 것은 호출자 05-04 몫)
    - (단위, M1 Round 2) `move("incoming/{uuid}", "evidence/{uuid2}")`가 주입 요청 함수로 `POST …/storage/v1/b/{bucket}/o/{incoming 키 인코딩}/rewriteTo/b/{bucket}/o/{evidence 키 인코딩}`을 부르고, 첫 응답이 `{ done: false, rewriteToken: "t1" }`이면 쿼리 `rewriteToken=t1`로 한 번 더 불러 `done: true`에서 멈춘 뒤 원본에 `DELETE`를 보낸다 · 원본 `DELETE`가 404면 성공 · 재작성이 실패하면 오류가 오르고 `DELETE`를 부르지 않는다(키의 `/`는 `%2F`로 인코딩)
    - (단위) 주입 요청 함수가 JSON API 응답 `{ size: "212000", contentType: "image/jpeg", metadata: { sha256 } }`을 주면 `getMetadata` = `{ size: 212000, contentType: "image/jpeg", sha256 }`, 404면 null. `delete`는 `DELETE` 메서드로 같은 주소를 부른다
    - (단위) `STORAGE_DRIVER=gcs` + 버킷 있음 → gcs 드라이버 · 버킷 없음 → 설정 오류 문구에 `GCS_EVIDENCE_BUCKET`
  </behavior>
  <action>
① 새 파일 `lib/gcp/gcs-v4.ts` — 순수 함수 `buildV4SignedUrl({ method, bucket, objectKey, headers, query, credentialEmail, now, expiresSec, sign })` → `{ url, headers }`: 경로 스타일 호스트 · 객체 키 경로 인코딩(슬래시 유지) · 서명 헤더 이름 소문자 정렬 · 정준 쿼리(키 · 값 인코딩 · 정렬) · 정준 요청(메서드 · 경로 · 쿼리 · 헤더 · 서명 헤더 목록 · `UNSIGNED-PAYLOAD`) · string-to-sign(`GOOG4-RSA-SHA256` · 날짜 · 범위 `{YYYYMMDD}/auto/storage/goog4_request` · 정준 요청 SHA-256 16진) · 주입 `sign`의 base64 결과 → 16진. 만료 상한 604800초. 날짜는 UTC(서명 규격 — 서울 날짜 규칙의 대상이 아니다).
② `lib/gcp/storage.ts`에 `createGcsStorage({ bucket, auth, now? })` — `auth`는 `GoogleAuth`(범위 `https://www.googleapis.com/auth/devstorage.read_write`), 서명 = `auth.sign(stringToSign)`, 이메일 = `auth.getCredentials()`의 `client_email`, `getMetadata` · `delete` · `retain` = `auth.request({ url: https://storage.googleapis.com/storage/v1/b/{bucket}/o/{encodeURIComponent(key)} … })`(`retain`은 `PATCH` `{ temporaryHold: true }` — Addresses review concern: F9 · Round 2부터 이중 방어). `move(fromKey, toKey)` = 같은 버킷 안 JSON API 재작성 `POST …/o/{encodeURIComponent(from)}/rewriteTo/b/{bucket}/o/{encodeURIComponent(to)}`를 `done: true`까지(`rewriteToken` 이어 부름 — 같은 위치 · 저장 등급이라 보통 한 번) → 원본 `DELETE`(404는 성공). 재작성 실패면 원본을 지우지 않고 오류를 올린다 — 원본은 `incoming/`에 남아 수명 주기가 7일 뒤 지운다(Addresses review concern: M1(Round 2) · 사용자 결정 E2: A — 새 패키지 없음). 서명 호출을 감싸 실패 때 `log.warn("storage.sign_failed", { op, reason })` 뒤 다시 던진다(로그 함수는 `lib/log.ts` `log` 기본 · 테스트 주입 — Addresses review concern: F7). `getObjectStorage()`의 gcs 분기가 버킷을 검사해 만든다(05-04가 둔 「미구현」 오류 자리를 대체). 테스트는 `auth`를 가짜로 주입한다(네트워크 없음).
③ behavior 단위를 새 파일 `test/unit/lib/gcp/storage-gcs.test.ts`에 먼저 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/lib/gcp</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 서명이 공개 키로 검증되지 않거나, 서명 헤더 목록 · 쿼리 키 · 헤더 셋이 다르거나, 만료 상한 · 버킷 없음 오류가 없거나, 05-04 로컬 드라이버 사례가 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint · tsc · next build 중 하나라도 error를 출력하거나 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `. "$(git rev-parse --git-dir)/gsd-05-12.env" && test -n "$PLAN_BASE" && S=$(git diff --stat "$PLAN_BASE"..HEAD -- package.json pnpm-lock.yaml) && test -z "$S"`이 종료 코드 0이다(새 의존성 0)
    - `grep -rnE "BEGIN (RSA )?PRIVATE KEY" lib scripts`가 0건이다(테스트는 실행 중 키를 만든다)
    - `grep -c "buildV4SignedUrl" lib/gcp/storage.ts`가 1 이상이다
    - `grep -c "storage.sign_failed" lib/gcp/storage.ts`가 1 이상이고 `grep -c "temporaryHold" lib/gcp/storage.ts`가 1 이상이다(F7 · F9)
    - `grep -c "rewriteTo" lib/gcp/storage.ts`가 1 이상이다(M1 Round 2 — gcs `move`)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>gcs 드라이버가 새 패키지 · 키 파일 없이 V4 서명 주소와 메타데이터 재확인을 만들고, 배관이 단위 테스트로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 증빙 버킷 인프라 — 부트스트랩(버킷 · 균일 접근 · 공개 접근 방지 · 버킷 범위 역할 · 자기 signBlob) · 배포(버킷 확인 · CORS 멱등 · 환경 변수) · 검증 스크립트 · 운영 문서</name>
  <files>infra/names.sh, scripts/bootstrap-gcp.sh, scripts/deploy.sh, scripts/verify-gcp.sh, docs/OPERATIONS.md, test/unit/deploy/deploy-sh.test.ts, test/unit/deploy/bootstrap-sh.test.ts</files>
  <read_first>
    - `infra/names.sh`(전문 — 이름 함수 · 식별자 금지 규칙) · `scripts/bootstrap-gcp.sh`(환경 루프 · 런타임 SA 역할 · 배포 SA 역할 — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4)) · `scripts/deploy.sh`(`SERVICE_URL` 결정 · `ensure_*` 모양 · `env_vars` — 같은 방식) · `scripts/verify-gcp.sh`(전문 — `run_check` 모양)
    - `test/unit/deploy/deploy-sh.test.ts` · `bootstrap-sh.test.ts` · `test/unit/deploy/fakebin/`(가짜 gcloud 기록 방식 — 호출 순서 단언)
    - `docs/OPERATIONS.md` §4 배포 런북 · §8 최초 1회 부트스트랩 · §9 시크릿 목록(형식) · `test/unit/docs-limits.test.ts`(문서 줄 수 한도)
  </read_first>
  <behavior>
    - (단위, 부트스트랩) 가짜 gcloud 기록에 환경마다 `storage buckets create gs://plant8-{env}-evidence --location=asia-northeast3 --uniform-bucket-level-access --public-access-prevention`(이미 있으면 describe 뒤 건너뜀) · 수명 주기 `storage buckets update gs://plant8-{env}-evidence --lifecycle-file=…`(규칙 JSON이 `{ action: Delete, condition: { age: 7, matchesPrefix: ["incoming/"] } }` 하나 — `evidence/` 접두어 규칙은 없다 · 버킷이 이미 있어도 매번 멱등 적용, F9 · M1 Round 2) · 버킷 IAM `roles/storage.objectUser`(런타임 SA) · `roles/storage.admin`(배포 SA) · 런타임 SA 자기 자신 `roles/iam.serviceAccountTokenCreator`가 있고, `projects add-iam-policy-binding`으로 저장소 역할을 주는 줄은 없다
    - (단위, 배포) 가짜 gcloud 기록에서 버킷 describe → CORS 파일로 `storage buckets update --cors-file` 호출이 서비스 배포 전에 있고, CORS 파일의 원점이 서비스 주소 한 개다. 버킷 describe가 실패하면 `deploy failed at ensure_evidence_bucket` · 부트스트랩 안내 한 줄. 서비스 배포의 `--set-env-vars`에 `STORAGE_DRIVER=gcs`와 `GCS_EVIDENCE_BUCKET=plant8-{env}-evidence`가 있다. 마이그레이션 · 계정 Job의 `common_env`에는 없어도 된다(부팅 검증 없음 — 05-04)
    - (검증 스크립트) `verify-gcp.sh`가 버킷 존재 · 균일 접근 참 · 공개 접근 방지 `enforced` · 두 역할 · CORS 원점 확인 `run_check`을 더한다
  </behavior>
  <action>
① `infra/names.sh`에 `evidence_bucket() { echo "plant8-$1-evidence"; }` 한 줄(기존 이름 함수 모양 · 식별자 없음). 버킷 이름은 전역이라 이미 쓰인 이름이면 부트스트랩이 실패로 알린다 — 그때는 05-13 checkpoint가 사용자에게 묻는다(SUMMARY 한 줄).
② `scripts/bootstrap-gcp.sh` 환경 루프에: 버킷 없으면 생성(behavior 플래그), 수명 주기 규칙 멱등 적용(`mktemp` JSON · 접두어 `incoming/` · 7일 삭제 — 완료된 증빙은 완료 통보 때 `evidence/`로 옮겨져 규칙 밖에 있다 · Addresses review concern: F9 · M1(Round 2) · 사용자 결정 E2: A, 파일 삭제), 버킷 IAM 두 바인딩(`gcloud storage buckets add-iam-policy-binding`), 런타임 SA 자기 자신 `iam.serviceAccountTokenCreator`(`gcloud iam service-accounts add-iam-policy-binding` — 대상도 주체도 같은 SA). 전부 멱등.
③ `scripts/deploy.sh`에 `ensure_evidence_bucket()`(STAGE 이름 같게 — 버킷 describe, 실패면 부트스트랩 안내 후 비정상 종료 · 임시 CORS JSON을 `mktemp`로 만들어 `storage buckets update --cors-file` · 파일 삭제)을 인프라 ensure 순서 안(서비스 배포 전)에 넣고, 서비스 `env_vars`에 두 변수를 덧붙인다. 기존 순서 · 다른 변수 불변.
④ `scripts/verify-gcp.sh`에 증빙 버킷 확인 `run_check` 묶음(수명 주기 규칙 존재 · 접두어 `incoming/` · 7일 포함 · `evidence/`를 지우는 규칙 없음 — F9 · M1 Round 2). `docs/OPERATIONS.md`에 절 「증빙 버킷(Phase 5)」 — 이름 규칙 · 부트스트랩이 하는 일 · 배포가 하는 일 · 환경 변수 둘(비밀 아님) · 로컬은 `STORAGE_DRIVER=local`(`.data/uploads`) · staging 스모크 절차(Task 3 스파이크 명령 한 줄 + 05-13 Task 3 문장과 같은 단계) · 접두어 둘(`incoming/` = 업로드 · 7일 삭제 / `evidence/` = 완료 증빙 · 삭제 규칙 없음 + 보존 표식 이중 방어 — 버킷 복사 · 이관 때 표식은 따라가지 않으므로 보호는 접두어가 한다) · 지운 증빙 객체와 트랜잭션 거부 뒤 남은 `evidence/` 객체 정리(표식 해제 포함)는 Phase 6 F8. 문서 줄 수 한도 테스트를 넘지 않게 짧게.
⑤ behavior 단위를 `test/unit/deploy/bootstrap-sh.test.ts` · `deploy-sh.test.ts`에 덧붙인다(기존 사례 불변 — 호출 순서 단언이 새 ensure 단계를 포함하도록 기대 목록만 늘린다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/deploy test/unit/docs-limits.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 버킷 생성 플래그 · 버킷 범위 역할 · 자기 signBlob · CORS 단계 순서 · 환경 변수 둘 중 하나라도 없거나, 프로젝트 범위 저장소 역할이 생기거나, 기존 배포 · 부트스트랩 사례가 깨지거나, 운영 문서가 줄 수 한도를 넘으면 여기서 깨진다</fails_when>
    <automated>bash -n scripts/bootstrap-gcp.sh &amp;&amp; bash -n scripts/deploy.sh &amp;&amp; bash -n scripts/verify-gcp.sh &amp;&amp; bash -n infra/names.sh</automated>
    <fails_when>셸 문법 검사가 하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "evidence_bucket" infra/names.sh scripts/deploy.sh scripts/bootstrap-gcp.sh`가 세 파일 모두 1 이상이다
    - `grep -nE "projects add-iam-policy-binding.*storage" scripts/bootstrap-gcp.sh`가 0건이다(런타임 SA에 프로젝트 범위 저장소 역할 없음)
    - `grep -c "STORAGE_DRIVER=gcs" scripts/deploy.sh`가 1 이상이다
    - `grep -c "증빙 버킷" docs/OPERATIONS.md`가 1 이상이다
    - `grep -c "matchesPrefix" scripts/bootstrap-gcp.sh`가 1 이상이다(F9 수명 주기 규칙)
    - (M1 Round 2) `grep -c 'incoming/' scripts/bootstrap-gcp.sh`가 1 이상이고, 주석을 뺀 부트스트랩에 `"evidence/"` 접두어 문자열이 없다(`grep -v '^ *#' scripts/bootstrap-gcp.sh | grep -c '"evidence/"'`이 0을 출력)
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>증빙 버킷 · CORS · 최소 IAM · 환경 변수가 스크립트로 재현되고, 스크립트 단위 테스트와 운영 문서가 같은 절차를 말한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 스파이크 — DB 없는 스크립트로 staging 증빙 버킷에 `incoming/` 서명 PUT · 메타데이터 · `evidence/`로 옮기기 · 보존 표식 · 서명 GET · 크기 초과 거부 · 삭제 한 번(사람 확인)</name>
  <files>scripts/gcs-sign-smoke.ts</files>
  <read_first>
    - Task 1 산출물 `lib/gcp/storage.ts`(`createGcsStorage`) · `lib/gcp/gcs-v4.ts` · Task 2 산출물 `docs/OPERATIONS.md` 「증빙 버킷(Phase 5)」 절
    - 기존 CLI 스크립트 모양(`scripts/rotate-key.ts` 등 — `pnpm tsx`로 도는 것 · env 파싱 방식)
  </read_first>
  <action>
Addresses review concern: F5 — 웨이브 13까지 기다리지 않고 가장 큰 기술 불확실성(실제 GCS가 손으로 만든 V4 서명을 받는가)을 여기서 한 번 확인한다. ① 새 파일 `scripts/gcs-sign-smoke.ts`(DB · 앱 서버 없음, `pnpm tsx scripts/gcs-sign-smoke.ts --bucket <이름>`): `createGcsStorage({ bucket, auth: new GoogleAuth(…) })`로 1KB 임의 바이트 · 계산한 sha256으로 `createSignedPut("incoming/{새 uuid}", { contentType: "image/jpeg", maxBytes: 2048, … })` → 받은 헤더 그대로 `fetch(PUT)` → 상태 코드 출력 → `getMetadata`(크기 · 형식 · sha256 일치 여부) → `move("incoming/{uuid}", "evidence/{새 uuid2}")` → 원본 `getMetadata` null · 옮긴 객체 `getMetadata`가 크기 · 형식 · sha256을 그대로 가짐(재작성의 메타 보존 [ASSUMED] 해소 — Addresses review concern: M1(Round 2)) → `retain` → JSON API로 `temporaryHold` 참 확인 → `createSignedGet`(evidence 키) → `fetch(GET)` 바이트 일치 → 같은 방식으로 4KB 바이트를 `incoming/` 키에 PUT(한도 2048) → 4xx 기대 → 보존 표식 해제 후 `delete`. 단계마다 `PASS/FAIL 단계 이름 · 상태 코드`만 출력한다(주소 · 서명 · 이메일은 출력하지 않는다). 식별자를 코드에 적지 않는다 — 버킷 이름은 인자. ② 사람에게 확인을 요청하고 멈춘다(자동 승인 없음).
  </action>
  <what-built>gcs 드라이버(V4 서명 · 메타데이터 · `incoming/` → `evidence/` 옮기기 · 보존 표식 · 삭제)와 부트스트랩 스크립트(Task 1 · 2) — 지금까지는 로컬에서 만든 RSA 키로 배관만 증명됐다</what-built>
  <how-to-verify>
    1. (소유자 계정) `bash scripts/bootstrap-gcp.sh --project <회사 프로젝트>`로 staging 증빙 버킷 · 버킷 IAM · 자기 signBlob · 수명 주기 규칙을 만들고 `bash scripts/verify-gcp.sh --project <회사 프로젝트>`의 증빙 버킷 줄(수명 주기 접두어 `incoming/` · `evidence/` 삭제 규칙 없음 포함)이 전부 통과인지 본다(버킷 이름이 이미 쓰이면 여기서 멈추고 이름을 정해 알려 준다)
    2. 런타임 SA 가장으로 기본 자격을 만든다: `gcloud auth application-default login --impersonate-service-account=<staging 런타임 SA>`(IAM signBlob 경로 — Cloud Run과 같은 서명 갈래)
    3. `pnpm tsx scripts/gcs-sign-smoke.ts --bucket plant8-staging-evidence` → 출력의 PUT · 메타데이터 · 옮기기 · 옮긴 뒤 메타데이터 · 보존 표식 · GET 줄이 `PASS`, 크기 초과 PUT 줄이 `PASS(4xx)`, 삭제 줄이 `PASS`
    4. 콘솔(또는 `gcloud storage objects list gs://plant8-staging-evidence/incoming/`와 `gs://plant8-staging-evidence/evidence/` 둘 다)에 스파이크 객체가 남지 않았다
  </how-to-verify>
  <resume-signal>`approved`(1~4 전부 통과) 또는 실패한 단계 번호와 출력 줄을 적어 주세요 — 서명 거부(403 `SignatureDoesNotMatch` 등)면 Task 1로 돌아가 `systematic-debugging`으로 정준 요청을 고친다(05-05 · 05-09 E2E까지 되돌아가기 전에 여기서 잡는 것이 이 스파이크의 목적)</resume-signal>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint 또는 tsc가 스파이크 스크립트에서 error를 출력한다</fails_when>
    <human-check>위 1~4 단계를 사람이 확인하고 `approved`를 준다 — 실행자는 단계별 PASS/FAIL · 상태 코드를 SUMMARY에 옮긴다(버킷 이름 외 식별자 없음)</human-check>
  </verify>
  <acceptance_criteria>
    - 사람이 `approved`를 줬고 SUMMARY에 스파이크 단계별 결과(PUT · 메타데이터 · 옮기기 · 옮긴 뒤 메타데이터 · 보존 표식 · GET · 크기 초과 4xx · 삭제)가 있다
    - `grep -nE "console\.log\(.*(url|signature|client_email)" scripts/gcs-sign-smoke.ts`가 0건이다(주소 · 서명 · 이메일 출력 없음)
    - 실패했다면 SUMMARY에 실패 단계 · 출력 줄 · 고친 커밋(또는 제안한 갭)이 있고 이 플랜은 완료로 표시되지 않는다
  </acceptance_criteria>
  <done>실제 GCS가 이 드라이버의 V4 서명 PUT · GET을 받고 크기 초과를 거부하며, `incoming/` → `evidence/` 옮기기가 메타데이터를 지키고 보존 표식이 걸린다는 것을 웨이브 5에서 사람이 확인했다 — 05-13 전체 확인은 그대로 남는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run 런타임 SA → GCS · IAM | 서명 · 객체 쓰기 권한의 범위 |
| 브라우저(서비스 원점) → GCS | CORS 원점 · 허용 헤더 |
| 부트스트랩 사람(소유자) → 회사 GCP | 한 번 적용되는 권한 부여 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-1201 | Elevation of Privilege | 런타임 SA 저장소 · 서명 권한 | high | mitigate | 버킷 범위 `storage.objectUser`만 · `serviceAccountTokenCreator`는 자기 자신에게만(프로젝트 범위 아님) — 부트스트랩 단위 사례 + 프로젝트 범위 grep 0 |
| T-05-1202 | Information Disclosure | 버킷 공개 노출 | high | mitigate | 균일 버킷 수준 접근 + 공개 접근 방지 강제 — `verify-gcp.sh` 확인 · 서명 GET 5분(05-04) |
| T-05-1203 | Spoofing | 다른 원점의 브라우저 업로드 | medium | mitigate | CORS 원점 = 서비스 주소 한 개 · 서명된 헤더 세트(크기 범위 · 형식 · sha256) |
| T-05-1204 | Tampering | 서명 조립 결함(규격 오해) | medium | mitigate | 배관 단위 테스트 + Task 3 스파이크(웨이브 5 — staging 버킷 서명 PUT/GET · 크기 초과 거부, 사람 확인 · F5) + 05-13 staging 앱 전체 확인 — [ASSUMED] 해소 지점 |
| T-05-1206 | Denial of Service / Tampering | 미완료 업로드 객체 누적(F9) · 수명 주기가 완료 증빙을 지움(M1) | high | mitigate | 업로드는 `incoming/`, 완료 때 `evidence/`로 옮김 · 수명 주기 7일 삭제는 `incoming/`에만 — 완료 증빙은 어떤 삭제 규칙에도 걸리지 않고 보존 표식(`retain`)이 이중 방어. 부트스트랩 단위 사례(규칙 접두어) · acceptance grep(`"evidence/"` 규칙 0) · verify-gcp 확인 · 스파이크의 옮기기 · 표식 확인(E2: A · M1 Round 2) |
| T-05-1208 | Tampering | 옮기기 중간 실패(재작성 성공 · 원본 삭제 실패) | low | accept | 원본은 `incoming/`에 남아 7일 뒤 수명 주기가 지운다 · 재작성 실패면 원본을 지우지 않는다(`move` 단위 사례) — 사람에게 보이는 영향 없음 |
| T-05-1207 | Repudiation | 서명 실패가 운영 로그에 안 남음(F7) | low | mitigate | `storage.sign_failed` `log.warn`(op · reason만) — 단위 사례 |
| T-05-1205 | Information Disclosure | 키 유출 | high | mitigate | 키 파일 없음 · IAM signBlob — 개인 키 grep 0 수용 기준 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | `@google-cloud/storage`(RESEARCH 판정 [SUS]) 설치 안 함 — 기존 `google-auth-library`만. lockfile diff 0 수용 기준. SDK로 바꾸려면 `checkpoint:human-verify`(RESEARCH 절차) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 전체 통과(`lib/gcp` · `deploy` · `docs-limits`)
- 셸 스크립트 넷 `bash -n` 통과
- `package.json` · lockfile diff 0 · 개인 키 문자열 0
- Task 3 스파이크 사람 확인 `approved`(F5)
</verification>

<success_criteria>
- staging · prod가 새 패키지 없이 GCS V4 서명 주소로 증빙을 받는다(EVID-01 운영 경로)
- 버킷 · CORS · IAM이 스크립트로 최소 권한으로 재현된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `buildV4SignedUrl`(`lib/gcp/gcs-v4.ts`) · `createGcsStorage` · `getObjectStorage` gcs 분기 |
| 인프라 | 버킷 `plant8-{env}-evidence`(균일 접근 · 공개 접근 방지 · 서울 · 수명 주기 `incoming/` 7일 삭제 — `evidence/`는 규칙 없음) · 버킷 IAM 둘 · 런타임 SA 자기 signBlob · CORS(서비스 원점) · 서비스 환경 변수 `STORAGE_DRIVER=gcs` · `GCS_EVIDENCE_BUCKET` |
| 스크립트 | `scripts/gcs-sign-smoke.ts`(스파이크 — F5) · `infra/names.sh` `evidence_bucket` · `scripts/bootstrap-gcp.sh` · `scripts/deploy.sh` `ensure_evidence_bucket` · `scripts/verify-gcp.sh` |
| 문서 | `docs/OPERATIONS.md` 「증빙 버킷(Phase 5)」 |
| 테스트 | `test/unit/lib/gcp/storage-gcs.test.ts` · `test/unit/deploy/{deploy-sh,bootstrap-sh}.test.ts`(추가) |

<output>
Create `.planning/phases/05-expense-approval-leave/05-12-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F5 — GCS V4 서명 실측이 웨이브 13에서야(L23@1108e20) | MEDIUM | Plan 05-12, Task 3(새 `checkpoint:human-verify`): DB 없는 `scripts/gcs-sign-smoke.ts`로 staging 버킷에 서명 PUT · 메타데이터 · GET · 크기 초과 거부 · 삭제 한 번, 사람 확인. `autonomous: false` · tasks 3. 05-13 Task 3 전체 확인은 그대로 |
| F7 — 서명 실패 운영 로그 없음(L25@1108e20) | MEDIUM | Plan 05-12, Task 1 ②: `log.warn("storage.sign_failed", { op, reason })` 후 다시 던짐 · 단위 사례 · acceptance grep · T-05-1207 |
| F9 — 미완료 업로드 객체 청소가 Phase 6 F8에만 있음(L27@1108e20) | MEDIUM | Plan 05-12, Task 2 ② · ④: 부트스트랩 수명 주기 규칙(`evidence/` · 7일 삭제, 멱등) + verify-gcp 확인. 완료된 증빙이 규칙에 지워지지 않도록 Task 1 ② gcs `retain` = `temporaryHold`(인터페이스 · 완료 흐름 호출은 05-04). T-05-1206. Phase 6 F8(지운 파일 정리)은 그대로 — 표식 해제 포함을 OPERATIONS에 기록 |
| F4 — read_first 코드 줄 범위(L22@1108e20) | MEDIUM | Plan 05-12 Task 2 read_first: `bootstrap-gcp.sh` · `deploy.sh` 줄 범위를 이름 `grep -n` 방식으로 교체(`node_modules` 고정 버전 파일 범위는 lockfile로 고정이라 유지) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| F9 대안 — 업로드를 별도 접두어(`incoming/`)에 받고 완료 때 복사 | 채택하지 않음 — 05-04의 객체 키 규칙(`evidence/{의도 id}`) · 로컬 드라이버 키 검사 · 서명 조건을 모두 바꿔야 한다. 보존 표식 방식은 인터페이스 메서드 하나로 같은 효과를 낸다. 표식이 수명 주기 삭제를 막는 동작은 [ASSUMED] — 스파이크가 표식 설정을, 05-13 staging 확인이 규칙 존재를 본다 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| M1 — 수명 주기가 `evidence/` 전체를 7일 뒤 지우고 보존 표식 하나로 완료 증빙을 지킴, `05-12-PLAN.md:34 · 156`(L30@69fb7ea) · 사용자 결정 E2: A(L19@69fb7ea) | MEDIUM | Plan 05-12: truth 셋 갱신(gcs `move` = JSON API `rewriteTo` 이어 부름 + 원본 DELETE · 수명 주기는 `incoming/`에만 · `retain` 이중 방어 · 스파이크 단계) · objective · Task 1 behavior(PUT 키 `incoming/` · `move` 단위 사례) · ② `move` 구현(새 패키지 없음) · acceptance `rewriteTo` grep · Task 2 behavior · ②(규칙 JSON `matchesPrefix: ["incoming/"]`) · ④ verify-gcp(`incoming/` · `evidence/` 삭제 규칙 없음) · OPERATIONS 절(접두어 둘) · acceptance(`incoming/` ≥ 1 · 주석 뺀 `"evidence/"` 0) · Task 3 스파이크(옮기기 · 옮긴 뒤 메타데이터 — 재작성 메타 보존 [ASSUMED] 해소) · how-to-verify 1 · 3 · 4 · done · T-05-1206(심각도 high로) · 새 T-05-1208 · Artifacts. `retain`(temporaryHold) **유지 판단**: 1차 보호는 접두어 분리로 옮기고 표식은 이중 방어로 둔다 — 세무 증빙은 법정 보관 대상이라 PATCH 한 번으로 운영자 실수 · 앱 버그 삭제를 막을 값이 있고, 이미 설계 · 단위 사례 · 스파이크가 있어 추가 비용이 작다. 버킷 복사 · 이관 때 표식이 따라가지 않는 약점은 접두어 규칙이 덮는다(OPERATIONS에 기록). 호출자 05-04가 커밋 뒤 부르고 실패는 경고만 |
| Supersedes Round 1 Deferred: 「F9 대안 — 업로드를 별도 접두어(`incoming/`)에 받고 완료 때 복사 — 채택하지 않음」 | MEDIUM | 사용자 결정 E2: A로 뒤집는다 — 이제 채택. Round 1이 든 비용(05-04 객체 키 규칙 · 로컬 드라이버 키 검사 · 서명 조건 변경)은 Plan 05-04 Round 2가 치른다(`incoming/{의도 id}` → `evidence/{파일 id}` · 로컬 정규식 두 꼴 · `ObjectStorage.move`). Round 1의 「표식이 수명 주기 삭제를 막는 동작 [ASSUMED]」 위험은 완료 증빙이 규칙 밖으로 나가 사라진다 |
| P3-8 — 웨이브 5 `05-05 ∥ 05-12` 같은 작업 트리 위험 · 사람 확인 checkpoint가 웨이브를 멈춤(L45@69fb7ea) | LOW | Plan 05-12 execution_notes 두 줄: ① 같은 작업 트리 금지 — execute-phase `workflow.use_worktrees` 기본 true(config에 없음)로 각자 worktree, 격리가 꺼지는 환경이면 순차(05-05 → 05-12), 수동이면 `using-git-worktrees`. ② Task 3 사람 확인이 웨이브 5를 멈추고 웨이브 6(05-15) 이후를 세운다 — 사람이 곧바로 응답할 수 있을 때 시작. 이 checkpoint는 `human_verify_mode: end-of-phase`의 의도된 예외(Round 1 F5)이고, 본문의 「자동 승인 없음」과 맞게 `gate="blocking"` → `gate="blocking-human"`(auto 모드에서도 자동 승인되지 않는 gate) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| P3-9 — `05-VALIDATION.md` Wave 0의 `test/unit/lib/gcp/storage.test.ts` 이름이 이 플랜의 `storage-gcs` · 05-04의 `storage-local`과 다름(L46@69fb7ea) | REVIEWS가 P-5(`/gsd-validate-phase 5`)에서 맞추기로 정했다 — 이 플랜 파일 이름은 그대로 둔다 |

