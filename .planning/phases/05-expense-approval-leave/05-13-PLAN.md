---
phase: 05-expense-approval-leave
plan: 13
type: execute
wave: 13
depends_on: ["05-11", "05-12"]
files_modified:
  - db/migrations/meta/_journal.json
  - docs/ARCHITECTURE.md
  - TODOS.md
  - test/e2e/expense-a11y.spec.ts
  - test/e2e/mobile-expense-320.spec.ts
autonomous: false
requirements: [EXP-01, EXP-02, EXP-08, EXP-14, EXP-15, EVID-01, UX-03, UX-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "병합 직전 `origin/main`을 병합하는 같은 충돌 해소 안에서 이 페이즈가 만든 마이그레이션 — 05-01 · 05-03 · 05-04 · 05-11 SUMMARY `migration_manifest`에 적힌 SQL과 스냅숏 **정확한 목록만** — 을 지우고 `_journal.json`은 `origin/main`의 생성물로 되돌린 뒤 병합을 커밋하고, `pnpm db:generate`로 main 마지막 마이그레이션 뒤에 Phase 5 마이그레이션 하나를 다시 만든다 — 파일 이름 · journal 손 편집 없음(04.1-07과 같은 규칙 · ROADMAP 04.1 절 규칙 개정 2026-09-24)"
    - "재생성된 SQL에는 SET LOCAL 잠금 시간 블록 손 삽입만 다시 들어가고, 그 뒤에도 `pnpm db:generate`가 \"No schema changes\" · `pnpm lint:sql` 0건 · journal 가드 녹색이며, main에 있던 마이그레이션 · journal 항목이 하나도 사라지지 않는다"
    - "병합된 트리에서 빈 DB부터 전 마이그레이션이 적용되고 지출결의 한 건이 견적 줄 → 제출 → 최종 승인까지 간다(병합 트레이서)"
    - "병합된 트리에서 `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `pnpm build` · 단위 · 통합 · `CI=true` E2E 전체가 **한 번** 녹색이다 — 싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 순서(CLAUDE.md §6)"
    - "독립 DOM 감사는 이 task의 실행자가 아닌 별도 서브에이전트(모델 명시)가 `CI=true` 프로덕션 빌드에서 DOM을 실측해 판정하고(스크린샷 육안 금지), 결과 표(표면 × 폭 × 판정)가 SUMMARY에 있고 위반 0으로 끝난다"
    - { statement: "폰 폼 제출 줄이 하단 탭 위에 고정되고 본문 마지막 칸(결재선)이 가려지지 않는다 — DOM 감사 폭 375 · 320(UIC responsive S3)", verification: backstop }
    - { statement: "지출결의 목록 · 결재함은 폭 375 · 320에서 P1 3열 + 접힌 줄, 가로 스크롤 0(`scrollWidth ≤ clientWidth`), 행 높이 ≥ 44 — DOM 감사 폭 1280 · 1024 · 700 · 375 · 320(UIC responsive S8 · S9)", verification: backstop }
    - { statement: "견적 줄 표 행 행동 열이 폭 1280 · 1024 · 700에서 숨지 않고 가로 스크롤 0 — 1024~1279 · 700~1023의 Phase 4 숨는 열이 흡수한다(UIC responsive S1)", verification: backstop }
    - { statement: "골라내기가 700 경계에서 같은 내용으로 모달 480 ↔ 폰 시트(행동 줄 44 · 1차 두 배 폭 · 결과 줄 전폭)로 바뀐다(UIC responsive S14)", verification: backstop }
    - { statement: "첨부 영역이 `<button>`(접근 이름 = 보이는 글자)이고 업로드 상태 변화가 `aria-live=\"polite\"`로 읽히며 실패 행 3차 `aria-describedby` = 파일명 · axe 위반 0(UIC a11y S4)", verification: backstop }
    - { statement: "막힌 1차 `지출결의 제출`이 `aria-disabled=\"true\"` + `aria-describedby` → 이유 글자이고 네이티브 `disabled`가 아니다(탭 순서에 남는다)(UIC a11y S6)", verification: backstop }
    - { statement: "골라내기 목록이 `role=\"listbox\"` · `option` · `aria-selected`, 포커스 트랩 · `Esc` · 닫힐 때 트리거 복귀, 고를 수 없는 행 `aria-disabled=\"true\"` + 이유 `aria-describedby`(UIC a11y S14)", verification: backstop }
    - { statement: "계산 한 줄은 조각 사이에서만 줄바꿈하고 조각 안은 `nowrap` — 13자리 금액 · 회사 대납 규칙 · 외화 조각이 모두 있어도 폭 375 · 320에서 가로 넘침 0(UIC overflow S5)", verification: backstop }
    - "실제 GCS가 이 페이즈의 V4 서명 PUT · GET을 받고 런타임 SA의 자기 signBlob · 버킷 범위 역할이 동작하며 크기 한도를 넘는 PUT은 GCS가 거부한다 — staging에서 사람이 폰으로 확인했다(05-VALIDATION Manual-Only · 사용자 결정 2026-09-26 Q7 — `checkpoint:human-verify`)"
    - "`docs/ARCHITECTURE.md`에 병합된 트리의 마지막 §4-N 다음 번호로 「지출결의 · 증빙 계약(Phase 5 → Phase 6)」 절이 20줄 이하로 있다 — 문서 모델(상태는 인스턴스에서 파생) · 1줄 1문서 판정 함수 · 번호 카운터 두 종류 · 세금 호출자(`computeExpenseTax` · `pickTaxDates` · `incomeTypeFor` · `taxLineText`) · `sumKrw`/`diffKrw` · 증빙 경로(`files` · `upload_intents` · `ObjectStorage` · `findActiveBySha`) · 게이트 `expense.submit` 순서와 06이 끼울 자리 · 「내 차례」 공급 함수 · 엔진 선택 필드 E1~E7이 Phase 6이 읽을 한 곳에 있다"
    - "`docs/ARCHITECTURE.md`의 규약 절에 이 페이즈가 만든 예외 둘이 규약 옆에 있다 — §4-8에 「정산 결재 최종 승인 경로는 잠금 순서가 결재 인스턴스 → 프로젝트이고 교착이 없는 이유」 한 항목, §4-6에 「지출결의 카운터 `expense`의 period = 프로젝트 번호(연도 규약의 예외)」 한 항목(리뷰 Round 2 M5 — SUMMARY에만 있던 예외를 규약을 읽는 사람이 보는 자리로)"
    - "이 페이즈의 어떤 task도 `.planning/STATE.md`를 고치지 않았고, 병합 때 STATE.md는 `origin/main` 판을 택한다(사용자 결정 2026-09-26 #3). `package.json` · `pnpm-lock.yaml` · `docs/design/tokens.css`는 병합 기준점 대비 바뀌지 않았다"
    - "SUMMARY에 Phase 6 정렬 메모(06 M-9 게이트용)가 있다 — 06-02 설정 키 넷 → 셋(`evidence.max_size_mb`는 Phase 5) · `sumKrw`/`diffKrw`는 Phase 5 · 06-03 `pickTaxDates` 부가세 대체 = 작성일(D-101) · 06 S4 계산 한 줄은 세율 `%`를 쓴다 · 06 S18 강행 결과 줄 → 정산 결재 문서 화면 KvList 한 행 · 05-01 `name_map` 실제 이름. Phase 11 메모 — 회사 대납 5만원 이하 면제는 CERT-04"
  prohibitions:
    - statement: "`git push`나 `git push --force`를 하지 않는다 — PR 생성 · 병합은 Post-build `/ship`의 몫이고, 병합은 사용자가 「머지해」라고 할 때만이다(CLAUDE.md)"
      status: resolved
      verification: judgment
    - statement: "병합 해소에서 마이그레이션을 넓은 탐지(`--diff-filter=A` 등)로 고르거나 journal을 병합 기준점 시점으로 되돌려서는 안 된다 — 다른 페이즈 마이그레이션이 함께 지워진다. 지우는 파일은 `migration_manifest` 목록뿐이다"
      status: resolved
      verification: judgment
    - statement: "staging 확인을 자동 승인하거나 로컬 드라이버 결과로 대신해서는 안 된다 — 사람이 실제 GCS에서 본다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "db/migrations/meta/_journal.json"
      provides: "main 항목 전부 + Phase 5 재생성 항목 하나(idx = main 마지막 + 1)"
      contains: "entries"
    - path: "docs/ARCHITECTURE.md"
      provides: "§4-N 지출결의 · 증빙 계약(Phase 5 → Phase 6) 20줄 이하"
      contains: "computeExpenseTax"
    - path: "test/e2e/expense-a11y.spec.ts"
      provides: "지출결의 목록 · 폼 · 문서 · 골라내기 · 결재함 · 정산 결재 axe 위반 0 · 포커스 규칙"
      min_lines: 40
    - path: "test/e2e/mobile-expense-320.spec.ts"
      provides: "폭 320 · 375 가로 넘침 0(목록 · 폼 · 결재함 · 계산 한 줄)"
      min_lines: 30
  key_links:
    - from: "db/migrations/meta/_journal.json"
      to: "test/unit/db"
      via: "재생성 결과가 idx · tag · when 불변식을 지킨다(04.1 journal 가드)"
      pattern: "idx"
    - from: "docs/ARCHITECTURE.md"
      to: "domain/expenses/index.ts"
      via: "Phase 6이 읽는 계약 절이 실제 export 이름을 가리킨다"
      pattern: "createExpenseFromLines|submitExpense"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
**병합 직전과 페이즈 게이트**: `origin/main`을 받아 이 페이즈 마이그레이션을 main 마지막 + 1로 다시 만들고, 병합된 트리에서 독립 DOM 감사 → 수정 → 전체 게이트를 한 번 돌리며, Phase 6이 읽을 계약을 한 곳에 적고, 마지막으로 staging에서 실제 GCS 업로드를 사람이 확인한다.

Purpose: 로컬 dev 통과는 완료가 아니다 — 완료 판정은 `CI=true` 전체 게이트다(CLAUDE.md §5). 실제 GCS V4 서명 · IAM은 로컬 · CI로 증명할 수 없어 사람이 본다(05-VALIDATION).

Output: 병합 커밋 · 재생성 마이그레이션 1 · 접근성 · 폭 E2E 둘 · DOM 감사 표 · ARCHITECTURE 절 · TODOS · SUMMARY 메모 · staging 확인 결과.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-VALIDATION.md
@.planning/phases/04.1-approvals-leave/04.1-07-PLAN.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `verification-before-completion`을 게이트 판정마다 **호출**하고, 병합 충돌 · 테스트 실패 · CI 실패를 쫓기 전에 `systematic-debugging`을 호출한다. 새 E2E 두 개는 `test-driven-development`(실패 먼저). 서브에이전트(DOM 감사)는 모델을 명시한다 — 판정 에이전트는 `sonnet`, 판정이 갈리면 `opus`.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-13.env"`와 `git fetch origin main` 뒤 받은 커밋 `MAIN_SHA`도 같은 파일에 남긴다. diff 검사 접두는 05-01과 같은 모양.
- 병합 충돌 규칙: 덧붙이기 전용 공유 파일(`domain/settings/keys.ts` · `domain/permissions/menus.ts` · `domain/permissions/info-items.ts` · `test/integration/leak-scan.test.ts` · `app/(app)/document-kinds.ts` · `docs/design/SYSTEM.md` · `docs/design/DECISIONS.md` · `docs/ARCHITECTURE.md` · `test/integration/tx-safety.test.ts` — 05-01 · 05-04 · 05-11이 describe를 덧붙였다, 리뷰 Round 2 M3)은 양쪽을 모두 남긴다. 의미 판단이 필요한 충돌은 추측하지 않고 멈춰 보고한다. `.planning/STATE.md`는 `origin/main` 판을 택한다.
- `git push` 금지. 푸시 · PR · 코디네이터 지시 확인은 Post-build `/ship` 단계다.
- human_verify_mode 설정은 `end-of-phase`지만 사용자가 이 확인을 `checkpoint:human-verify`로 정했다(2026-09-26 Q7) — Task 3만 사람을 기다린다.
- 웨이브: 05-01 「웨이브 근거」 — 마지막 웨이브(13). 05-11(마지막 기능 플랜)과 05-12(gcs 드라이버)를 둘 다 기다린다.
</execution_notes>

<tasks>

<task type="tracer">
  <name>Task 1: 병합 트레이서 — origin/main 병합 → Phase 5 마이그레이션 재생성 → 빈 DB 적용 → 지출결의 한 건 끝까지</name>
  <files>db/migrations/meta/_journal.json</files>
  <read_first>
    - `.planning/phases/04.1-approvals-leave/04.1-07-PLAN.md` Task 1(병합 · 삭제 · 복원 · 재생성 절차 · `MAIN_SHA` 조상 판정 · 「Already up to date」 갈래)
    - 05-01 · 05-03 · 05-04 · 05-11 SUMMARY frontmatter `migration_manifest`(지울 파일의 유일한 출처) · 손 삽입 줄 번호
    - `db/migrations/meta/_journal.json`(병합 전후) · `node_modules/drizzle-orm/pg-core/dialect.js` 50–70행(when 기준 건너뛰기)
  </read_first>
  <action>
① `git fetch origin main` → `MAIN_SHA` 기록 → `git merge --no-ff origin/main`(충돌 규칙은 execution_notes). 같은 해소 안에서 manifest 목록의 SQL · 스냅숏만 지우고(같은 경로가 main에도 있으면 main 판으로), `_journal.json`은 `git checkout origin/main -- db/migrations/meta/_journal.json`, STATE.md도 origin/main 판. 병합 커밋. `origin/main`이 이미 조상이면 병합 없이 같은 삭제 · 복원을 일반 커밋 하나로 하고 `MAIN_SHA`가 조상인지로 검증한다(04.1-07 CX-R1).
② `pnpm db:generate` → Phase 5 마이그레이션 하나 생성 → 네 플랜 SUMMARY의 SET LOCAL 손 삽입만 다시 한다 → `pnpm db:generate` 재실행 "No schema changes" → `pnpm lint:sql`.
③ 빈 DB 증명: `pnpm db:reset:test && DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate`, 옛 번호가 적용된 개발 DB `erp`는 `PGPASSWORD=erp psql -h 127.0.0.1 -p 5432 -U erp -d postgres -c 'DROP DATABASE IF EXISTS erp WITH (FORCE)' -c 'CREATE DATABASE erp OWNER erp' && pnpm db:migrate`.
④ 병합 트레이서: 지출결의 트레이서 통합 describe와 기준 3 E2E를 병합된 트리에서 돌린다. SUMMARY에 받은 `MAIN_SHA` · 재생성 tag · idx 연속성(main 항목 전부 보존 · Phase 5 항목 하나 · idx = main 마지막 + 1)을 적는다 — 번호 범위는 단언하지 않는다.
  </action>
  <verify>
    <automated>G=$(pnpm db:generate 2&gt;&amp;1); RC=$?; printf '%s\n' "$G"; test "$RC" -eq 0 &amp;&amp; grep -q "No schema changes" &lt;&lt;&lt;"$G"</automated>
    <fails_when>종료 코드가 0이 아니거나 "No schema changes"가 없다 — 재생성이 스키마와 어긋났다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; DATABASE_URL=postgres://erp:erp@127.0.0.1:5432/erp_test pnpm db:migrate &amp;&amp; pnpm lint:sql &amp;&amp; pnpm vitest run --project unit test/unit/db &amp;&amp; pnpm vitest run --project integration test/integration/expense-approval-lifecycle.test.ts test/integration/migration-upgrade.test.ts'</automated>
    <fails_when>빈 DB 적용 · squawk · journal 가드 · 지출결의 트레이서 · 마이그레이션 업그레이드 가드 중 하나라도 실패한다</fails_when>
    <automated>. "$(git rev-parse --git-dir)/gsd-05-13.env" &amp;&amp; test -n "$MAIN_SHA" &amp;&amp; git merge-base --is-ancestor "$MAIN_SHA" HEAD &amp;&amp; D=$(git diff --name-only --diff-filter=D "$MAIN_SHA" HEAD -- db/migrations) &amp;&amp; test -z "$D"</automated>
    <fails_when>기준점 파일에 `MAIN_SHA`가 없거나, 받은 main이 HEAD의 조상이 아니거나, main에 있던 마이그레이션 파일이 하나라도 지워졌으면 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 병합 뒤 Phase 5 SQL이 정확히 하나이고 이름이 drizzle-kit이 준 그대로다
    - `git diff "$MAIN_SHA" HEAD -- .planning/STATE.md`(기준점 파일을 먼저 읽는다)가 비어 있다
    - SUMMARY에 `MAIN_SHA` · 재생성 tag · 연속성 · 다시 넣은 손 삽입 줄 번호가 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>main을 받은 트리에서 Phase 5 마이그레이션이 main 뒤 하나로 서고, 빈 DB부터 지출결의 한 건이 끝까지 간다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 페이즈 게이트 — 접근성 · 폭 E2E 둘 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번 · 계약 문서 · 금지 파일 확인 · 메모</name>
  <files>test/e2e/expense-a11y.spec.ts, test/e2e/mobile-expense-320.spec.ts, docs/ARCHITECTURE.md, TODOS.md</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` UI Considerations backstop 행(746–755 · 780행) · S1 · S3 · S4 · S6 · S8 · S9 · S14
    - `test/e2e/a11y.spec.ts`(axe 호출 모양 — 이 파일은 고치지 않는다) · 04.1 `test/e2e/leave-a11y.spec.ts`(`name_map` — 같은 모양) · `test/e2e/mobile-320-no-overflow.spec.ts`
    - `.planning/phases/04.1-approvals-leave/04.1-05-PLAN.md` Task 3 ④(독립 DOM 감사 지시 모양) · `docs/ARCHITECTURE.md`의 마지막 §4-N · `test/unit/docs-limits.test.ts`
    - 05-01~05-15 SUMMARY(계약 이름 · 06 정렬 메모 재료)
    - 05-11 SUMMARY 「잠금 순서 표」 · 05-03 SUMMARY의 §4-6 한 줄 기록 · `docs/ARCHITECTURE.md` §4-6 · §4-8(`grep -n "^## 4-6\.\|^## 4-8\." docs/ARCHITECTURE.md` 뒤 두 절 범위만) — ⑤ 예외 두 항목의 근거(Round 2 M5)
  </read_first>
  <behavior>
    - (E2E, desktop) `/expenses` · `/expenses/new` · 작성 중 폼 · 제출된 문서 화면 · 골라내기 열린 상태 · `/approvals`(지출결의 행 포함) · 결재 시트 열린 상태 · `/projects/{id}/settlement`에서 axe 위반 0. 막힌 1차의 `aria-disabled="true"` · `aria-describedby` · `disabled` 속성 없음. 골라내기의 listbox · option · `aria-selected` · 포커스 트랩 · `Esc` 뒤 트리거 복귀. 첨부 영역 `button` 접근 이름 = 보이는 글자 · `aria-live="polite"` 존재
    - (E2E, mobile-375 + 같은 파일 320 컨텍스트) 목록 · 폼(외화 · 회사 대납 계산 한 줄 포함 문서) · 결재함 · 폰 행 시트에서 `document.documentElement.scrollWidth ≤ clientWidth` · 폰 제출 줄 고정 · 결재선 가림 없음 · 행 높이 ≥ 44
  </behavior>
  <action>
① 새 스펙 `test/e2e/expense-a11y.spec.ts`(desktop) · `test/e2e/mobile-expense-320.spec.ts`(`mobile-*` — 375 프로젝트 + 파일 안 320 컨텍스트)를 behavior대로 먼저 적는다.
② 싼 게이트: `pnpm lint && pnpm typecheck && pnpm lint:sql && flock /tmp/plant8-erp-test.lock pnpm build`.
③ **독립 DOM 감사**: 이 task의 실행자가 아닌 별도 서브에이전트(모델 `sonnet` 명시 · Superpowers `verification-before-completion` 명시)에게 `CI=true` 프로덕션 빌드를 띄워 DOM으로 실측하게 한다 — 표면: 견적 줄 표 행 행동 열(1280 · 1024 · 700) · 폰 행 시트(375 · 320) · 지출결의 폼 · 문서 화면(1280 · 375 · 320 — 제출 줄 고정 · 결재선 가림) · 계산 한 줄(13자리 · 회사 대납 · 외화, 375 · 320 넘침 0 · 조각 안 `nowrap`) · 목록(1280 · 1024 · 700 · 375 · 320 — 열 접기 · 가로 스크롤 0 · 행 ≥ 44) · 결재함 · 결재 시트(375 · 320) · 골라내기(700 경계 모달 480 ↔ 시트 · 행동 줄 44 · 1차 두 배 폭 · 결과 줄 전폭) · 정산 결재 문서(1280 · 375) · 긴 글자(프로젝트명 40자 · 항목 30자 · 긴 영문 파일명 · 반려 사유 200자 줄바꿈) · 새 토큰 0(`tokens.css` diff). 스크린샷 육안 판정 금지. 감사 표(표면 × 폭 × 판정 × 근거 수치)를 SUMMARY에 남기고 위반을 고친다.
④ **전체 게이트 한 번**(CLAUDE.md §6): `pnpm test:unit` → `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration'` → `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && CI=true pnpm playwright test'`.
⑤ `docs/ARCHITECTURE.md`에 병합된 트리의 마지막 §4-N 다음 번호로 「지출결의 · 증빙 계약(Phase 5 → Phase 6)」 20줄 이하(must_haves 항목 — 실제 export 이름). `TODOS.md`에 남은 일 항목(06 M-9 정렬 메모 · Phase 11 5만원 면제 · PNL-01 손익 행 자리 · 인쇄 뒤 페이즈) 한 줄씩.
   **규약 예외 두 항목(Addresses review concern: M5(Round 2))** — 새 절이 아니라 규약 절 자리에 덧붙인다(규약을 읽는 사람이 예외를 같은 자리에서 본다). ⓐ §4-8 끝에 번호 다음 항목 하나: 「정산 결재 최종 승인(Phase 5, 05-11)은 잠금 순서가 결재 인스턴스 → 프로젝트 행이다 — (2)의 「첫 단계에서 프로젝트 행」의 예외(엔진이 인스턴스를 갱신한 뒤 최종 승인 훅이 `changeProjectStatus`로 프로젝트를 잠근다). 교착이 없는 이유: 프로젝트 행을 먼저 잡고 같은 정산 결재 인스턴스를 잡는 경로가 없다 — 정산 기안 · 다시 올리기는 프로젝트를 tx로 읽기만 하고, 기간 변경(D-80)은 프로젝트 행만 잠그며, 지출결의 제출(프로젝트 → 지출결의 → 인스턴스)은 다른 인스턴스를 잡는다」. 05-11 SUMMARY 잠금 순서 표와 대조한다 — 경로 이름만 다르면 표의 이름을 쓰고, 표에 「프로젝트 행 → 같은 정산 결재 인스턴스」 순서로 잠그는 경로(회수 포함)가 있으면 교착 가능성이므로 이 항목을 쓰지 않고 멈춰 보고한다(05-11 금지 항목 위반 — 문서로 덮지 않는다). ⓑ §4-6: 05-03 ④가 이미 「지출결의 카운터 `expense`는 period = 프로젝트 번호 — 연도 규약의 첫 예외」 한 항목을 적었으면 그대로 두고(병합 해소에서 양쪽 보존), 병합된 트리에 없으면 같은 뜻의 한 항목을 §4-6 끝에 적는다. 두 항목은 각각 파일의 기존 줄바꿈 폭으로 5줄 이하다. docs-limits(300줄 상한)가 넘치면 ⑤의 새 절(20줄 이하)을 줄여 맞추고 예외 두 항목은 줄이지 않는다 — 그래도 넘치면 멈춰 보고한다.
⑥ SUMMARY: 06 정렬 메모 · Phase 11 메모 · 알려진 한계 · Post-build 순서(`/review` → `/qa`(+ `/design-review`) → `/cso`(파일 업로드 · 서명 URL · 결재 권한 · 금액 — 필수) → `/ship`)와 `/ship` 바로 앞에 할 확인(`git diff origin/main -- .planning/STATE.md` 비어 있음 · 코디네이터 지시 확인).
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint:sql &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; pnpm test:unit</automated>
    <fails_when>싼 게이트 또는 단위 전체에서 error · failed가 하나라도 나온다(docs-limits 포함)</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration'</automated>
    <fails_when>통합 전체에서 failed가 1 이상이다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test'</automated>
    <fails_when>`CI=true` 프로덕션 빌드 E2E 전체(desktop → mobile-375)에서 failed가 1 이상이거나 「did not run」이 1 이상이거나 종료 코드가 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - 독립 DOM 감사 표가 SUMMARY에 있고 위반 0으로 끝났다(감사 에이전트 모델 이름 포함)
    - `. "$(git rev-parse --git-dir)/gsd-05-13.env" && test -n "$MAIN_SHA" && S=$(git diff --stat "$MAIN_SHA" HEAD -- package.json pnpm-lock.yaml docs/design/tokens.css .planning/STATE.md) && test -z "$S"`이 종료 코드 0이다
    - `grep -rn "dangerouslySetInnerHTML" "app/(app)/expenses" ui/attachments ui/pick-dialog "app/(app)/projects/[id]/settlement"`이 0건이다
    - `docs/ARCHITECTURE.md`에 `computeExpenseTax` · `findActiveBySha` · `listNextTurnItems`가 있고 docs-limits 단위가 녹색이다
    - 규약 예외 두 항목이 규약 절 안에 있다(Round 2 M5) — `awk '/^## 4-8\./{f=1;print;next} /^## /{f=0} f' docs/ARCHITECTURE.md | grep -c "인스턴스 → 프로젝트"`가 1 이상이고 같은 절 출력에 `grep -c "교착"`이 1 이상 · `awk '/^## 4-6\./{f=1;print;next} /^## /{f=0} f' docs/ARCHITECTURE.md | grep -c "expense"`가 1 이상이고 같은 절 출력에 `grep -c "프로젝트 번호"`가 1 이상
    - 병합된 트리에서도 브랜드 캐스트 0건(Round 2 A1 — 05-11 acceptance와 같은 명령): `grep -rnE '\bas[[:space:]]+SettlementApprovalAuthority\b|(^|[^A-Za-z0-9_$])<SettlementApprovalAuthority>' --include='*.ts' --include='*.tsx' domain repositories app lib ui scripts | grep -vc '^domain/settlements/'`가 0이다
    - SUMMARY에 06 정렬 메모 여섯 줄 · Phase 11 메모 · Post-build 순서가 있다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>병합된 트리가 DOM 감사 위반 0 · `CI=true` 전체 게이트 녹색이고, Phase 6이 읽을 계약이 한 곳에 있다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: staging 확인 — 실제 GCS가 V4 서명 PUT · GET을 받고, 런타임 SA 자기 signBlob · 버킷 범위 역할이 동작하며, 크기 초과를 GCS가 거부한다</name>
  <read_first>
    - `docs/OPERATIONS.md` 「증빙 버킷(Phase 5)」 절(05-12) · §4 배포 런북 · §5 롤백
    - `.planning/phases/05-expense-approval-leave/05-VALIDATION.md` Manual-Only 표
  </read_first>
  <action>
사람에게 확인을 요청하고 멈춘다(자동 승인 없음). 실행자는 확인에 쓸 값(배포할 커밋 SHA · 버킷 이름 · 확인 문서 번호 칸)을 준비해 보여 주고, 결과를 SUMMARY에 옮긴다. 이 task는 코드 · 문서를 고치지 않는다 — 확인이 실패하면 원인을 적고 `systematic-debugging`으로 05-12 산출물을 고치는 새 플랜(갭)을 제안한다.
  </action>
  <what-built>gcs 드라이버(V4 서명 · 메타데이터 재확인)와 증빙 버킷 인프라(05-12), 병합 · 재생성된 트리(Task 1) — 로컬 · CI는 로컬 드라이버로만 증명됐다</what-built>
  <how-to-verify>
    1. (소유자 계정) `bash scripts/bootstrap-gcp.sh --project <회사 프로젝트>`를 다시 돌려 staging 증빙 버킷 · 버킷 IAM · 자기 signBlob이 만들어졌는지 `bash scripts/verify-gcp.sh --project <회사 프로젝트>`의 증빙 버킷 확인 줄이 전부 통과로 본다(버킷 이름이 이미 쓰이고 있으면 여기서 멈추고 이름을 정해 알려 준다)
    2. 병합 · 재생성된 트리(Task 1 커밋)로 `pnpm deploy -- --env staging --project <회사 프로젝트>`
    3. 폰으로 staging에 기획 PM 계정으로 로그인 → 진행 프로젝트 견적 줄 → `지출결의 올리기` → 사진 한 장 첨부 → 파일 행이 `올리는 중…` 뒤 크기 · 날짜로 바뀐다(콘솔 · 네트워크에 GCS 403 · CORS 오류 없음)
    4. `크게 보기`가 새 탭에서 원본을 연다(주소 호스트 `storage.googleapis.com` · 5분 뒤 같은 주소는 거부)
    5. 크기 초과: 브라우저 개발자 도구로 받은 서명 PUT 주소 · 헤더를 그대로 쓰고 한도보다 큰 파일을 `curl -X PUT`으로 올리면 GCS가 4xx로 거부한다
    6. `지출결의 제출` → 팀장 계정 폰에서 결재 시트 썸네일이 보이고 `승인`된다
    7. 주의: 확인 뒤 PR 병합 전에 `origin/main`이 다시 움직여 마이그레이션을 또 재생성해야 하면, staging DB는 옛 번호를 적용했으므로 `docs/OPERATIONS.md` 복원 절차로 staging DB를 되돌린 뒤 다시 배포한다(when 기준 건너뛰기 위험 — 04.1 journal 가드와 같은 이유)
  </how-to-verify>
  <resume-signal>`approved`(1~6 전부 통과) 또는 실패한 단계 번호와 화면 · 오류 문구를 적어 주세요</resume-signal>
  <verify>
    <human-check>위 1~6 단계를 사람이 staging에서 확인하고 `approved`를 준다 — 실행자는 결과(단계별 통과 · 오류 문구 · 배포 SHA · 버킷 이름)를 SUMMARY에 옮긴다</human-check>
  </verify>
  <acceptance_criteria>
    - 사람이 `approved`를 줬고 SUMMARY에 배포 SHA · 버킷 이름 · 단계 1~6 결과가 있다
    - 실패했다면 SUMMARY에 실패 단계 · 오류 문구 · 제안한 갭 플랜이 있고 이 플랜은 완료로 표시되지 않는다
  </acceptance_criteria>
  <done>실제 GCS에서 이 페이즈의 서명 업로드 · 보기 · 크기 거부가 사람 확인으로 증명됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 병합 해소 → main 마이그레이션 | 다른 페이즈 마이그레이션 · journal 항목을 잃으면 운영 적용이 조용히 건너뛴다 |
| 브랜치 트리 → staging(실제 GCP) | 사람이 한 번 배포 · 확인 — staging DB 마이그레이션 상태가 바뀐다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-1301 | Tampering | `_journal.json` · main 마이그레이션 손실 | high | mitigate | manifest 목록만 삭제 · journal은 origin/main 판 · 삭제 diff 0 검증 명령 · journal 가드 · `migration-upgrade` 통합 |
| T-05-1302 | Denial of Service | staging DB 마이그레이션 when 드리프트(확인 뒤 main 이동 · 재생성) | medium | mitigate | Task 3 7단계 — 재생성이 다시 필요하면 staging DB 복원 뒤 재배포(OPERATIONS 복원 절차) |
| T-05-1303 | Information Disclosure | 페이즈 전체의 새 화면 · 액션 | high | mitigate | 전체 통합(누수 스캔 · `expense-visibility`) · `CI=true` E2E 전체 · Post-build `/cso` 필수(SUMMARY 순서) |
| T-05-1304 | Tampering (XSS) | 파일명 · 비고 · 사유 렌더 | medium | mitigate | `dangerouslySetInnerHTML` 0건 수용 기준 |
| T-05-1305 | Repudiation | 무단 push · 병합 | medium | mitigate | push 금지 항목 · 병합은 사용자 「머지해」만 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 병합 기준점 대비 `package.json` · lockfile diff 0 수용 기준 — 페이즈 전체 신규 패키지 0 |
</threat_model>

<verification>
- 병합된 트리에서 `pnpm db:generate` "No schema changes" · `pnpm lint:sql` 0 · journal 가드 녹색
- 싼 게이트 → 독립 DOM 감사(위반 0) → 전체 게이트 한 번(단위 · 통합 · `CI=true` E2E) 녹색
- main 대비 `package.json` · lockfile · `tokens.css` · STATE.md diff 0
- staging 사람 확인 `approved`
</verification>

<success_criteria>
- 이 페이즈가 main 위에서 `CI=true`로 완료 판정을 받는다
- 실제 GCS 업로드가 사람 확인으로 증명된다(EVID-01 운영 경로)
- Phase 6이 읽을 계약 · 정렬 메모가 한 곳에 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | Phase 5 재생성 SQL 1개 + 스냅숏(idx = main 마지막 + 1) · `_journal.json` |
| 문서 | `docs/ARCHITECTURE.md` §4-N 지출결의 · 증빙 계약(Phase 5 → Phase 6) · `TODOS.md` 항목 |
| 테스트 | `test/e2e/expense-a11y.spec.ts` · `test/e2e/mobile-expense-320.spec.ts` |
| 기록 | SUMMARY — `MAIN_SHA` · 재생성 tag · DOM 감사 표 · 06 정렬 메모 · Phase 11 메모 · Post-build 순서 · staging 확인 결과 |

<output>
Create `.planning/phases/05-expense-approval-leave/05-13-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| M5 — 정산 결재 경로 잠금 순서(인스턴스 → 프로젝트)가 §4-8(2)와 다른데 SUMMARY에만 기록, 05-03 counter period 예외(§4-6)도 같음(L34@69fb7ea) | MEDIUM | Plan 05-13, Task 2 ⑤ · read_first · acceptance · must_haves truth: §4-8 끝에 예외 한 항목(순서 · 교착 없는 이유 — 정산 기안 · 다시 올리기는 프로젝트 읽기만, 기간 변경은 프로젝트 행만, 지출결의 제출은 다른 인스턴스) — 05-11 SUMMARY 잠금 순서 표와 대조해 「프로젝트 → 같은 인스턴스」 경로가 있으면 쓰지 않고 멈춤. §4-6 항목은 05-03 ④가 이미 적으므로 병합 트리에 있으면 보존 · 없으면 여기서 한 항목. 두 절 범위 awk + grep acceptance · 줄 상한(300) 넘치면 새 절을 줄이고 예외는 유지 |
| A1(병합 트리 재확인) — 브랜드 타입 캐스트 위조(L35@69fb7ea) | MEDIUM | Plan 05-13, Task 2 acceptance: 05-11과 같은 캐스트 0건 grep을 병합된 트리에서 한 번 더(05-11 뒤 병합 해소로 들어온 코드까지). 선택 이유(grep vs eslint)는 05-11 Round 2 원장 |
| M3(병합 쪽) — `tx-safety.test.ts`에 05-01 · 05-04 · 05-11이 describe를 덧붙임(L32@69fb7ea) | MEDIUM | Plan 05-13 execution_notes 병합 충돌 규칙: `test/integration/tx-safety.test.ts`를 덧붙이기 전용 공유 파일 목록에 넣어 main 쪽 사례와 양쪽 보존. Task 2 ④ 전체 통합 게이트가 세 사례를 한 번에 돈다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 걸린 Round 2 지적(M5 · A1 병합 재확인 · M3 병합 규칙)은 전부 반영했다. 나머지는 다른 플랜 소관 |
