---
phase: 05-expense-approval-leave
plan: 14
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - domain/expenses/index.ts
  - domain/expenses/line-door.ts
  - domain/expenses/gate.ts
  - domain/money/index.ts
  - test/integration/expense-create-idempotency.test.ts
  - test/integration/expense-submit-concurrency.test.ts
  - test/integration/expense-installment-cap.test.ts
  - test/unit/domain/expenses/installment-cap.test.ts
autonomous: true
requirements: [EXP-01, EXP-14]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "같은 줄에서 같은 기안자가 두 번 · 동시에 두 번 불러도 작성 중 문서는 하나다 — 부분 UNIQUE `(quote_line_id, drafter_id) WHERE number IS NULL AND deleted_at IS NULL`와 `INSERT … ON CONFLICT DO NOTHING` + 기존 행 조회가 최종 판정한다. 다른 기안자는 자기 몫 작성 중 문서를 따로 하나 가진다(EXP-01 idempotency)"
    - "여러 줄을 한 번에 넘기면 문이 열린 줄마다 작성 중 문서가 하나씩 생기고, 막힌 줄은 `{lineId, reason}`으로 돌아온다 — 거래처 없는 줄은 `거래처 없음 · 거래처 고르기`, 이전 차수 줄은 `견적 줄이 현재 차수에 없음 · 견적 줄 바꾸기`, 분할 없이 제출된 문서가 있는 줄은 `이 줄에 지출결의 {번호} 있음 · 지출결의 열기`다(EXP-01 · UI-SPEC 확정 #5의 서버 쪽)"
    - "진행 · 현재 차수 고객 승인 전(설정 `project.customer_approval_gate` 켜짐)이면 작성 중 문서를 만들지 않고 `quote.customer-approval` 규칙 문자열 그대로 거부한다. 설정을 끄면 생성되고, 수주중 프로젝트는 고객 승인과 무관하게 생성된다. 완료 프로젝트는 `완료 프로젝트 · 새 지출결의 없음`으로 거부한다(D-43 · D-47 · D-54 · Pitfall 3)"
    - "`expenses` 쓰기 권한이 없거나 그 프로젝트의 쓰기 권리가 없는 사람이 줄로 지출결의를 만들면 작성 중 행이 생기지 않는다(05-03 T-05-302의 증명)"
    - "같은 프로젝트의 서로 다른 줄 제출 6건을 동시에 보내도 기본 풀(`DB_POOL_MAX` 5)에서 전부 끝나고 번호가 서로 다르다. 다른 프로젝트의 첫 제출은 그 프로젝트 번호의 `-0001`이다 — 카운터 period가 프로젝트마다 따로다(기준 1 · Issue 10 · 사용자 결정 2026-09-26 #6)"
    - "같은 비분할 견적 줄의 작성 중 문서 둘(기안자 둘)을 동시에 제출하면 하나만 통과하고 다른 쪽은 `이 줄에 지출결의 {번호} 있음 · 지출결의 열기`로 거부되며 번호가 없다 — 프로젝트 행 잠금 안에서 줄의 번호 있는 문서를 다시 읽어 판정한다(ARCHITECTURE §4-8 (2) · UI Assumptions #1)"
    - "줄의 번호 있는 문서(자기 자신 제외) 공급가액 합 + 이번 공급가액 ≤ 실행가 — 넘으면 공급가액 칸 오류 `남은 실행가 {금액} 넘음 · 공급가액 고치기`로 제출이 거부된다. 문서 통화와 줄 통화가 모두 같으면 원래 통화 금액으로, 하나라도 다르면 원화로 비교한다(계획 결정 — RESEARCH Open Q4 RESOLVED)"
    - "앞 회차(번호 있는 분할 문서)가 있는 줄에서 만든 문서는 체크박스와 무관하게 분할 문서이고 회차는 제출 때 그 줄의 번호 있는 문서 수 + 1이다. 앞 회차가 없으면 `분할 지급`을 켠 문서가 1회차다. 남은 실행가가 0이 되면 그 줄의 문이 닫힌다(UI-SPEC 확정 #1)"
    - "제출된 문서의 공급가액 원화는 0보다 크다 — 빈 칸이면 `공급가액 비어 있음 · 공급가액 적기`, 0이면 `공급가액이 0 · 0보다 크게`로 제출이 거부되고, 음수 입력은 저장이 거부되며, 번호 있는 행을 0으로 바꾸는 UPDATE는 DB CHECK 위반(23514)으로 실패한다(EXP-14)"
    - "같은 문서를 같은 version으로 두 번 제출하면(폰 두 번 탭 · 응답 유실 뒤 재시도 — 차례로든 동시로든) 번호는 하나이고 카운터는 한 번만 오르며, 두 번째 요청은 오류가 아니라 결과 `already_submitted`(expenseId · 번호)를 받는다 — 화면은 `이미 제출됨 · {번호}` + 문서 화면(05-05). `다른 곳에서 저장됨` 충돌 문구를 보이지 않는다(리뷰 Round 1 F3 · 05-03 `submitExpense`의 이미 제출됨 판정)"
    - "작성 중 문서를 두 창에서 고치면 늦은 쪽 저장이 `HH:MM에 다른 곳에서 저장됨 · 새로 고침`으로 거부된다(문서 `version` — UI-SPEC 「거부 — 동시 처리」)"
  prohibitions:
    - statement: "경합 사례를 타이밍 대기로 재현해서는 안 된다 — `deps.afterLock` + `test/integration/lock-race.ts`로 순서를 고정한다(ARCHITECTURE §4-8 (5))"
      status: resolved
      verification: judgment
    - statement: "`domain/money` 밖에서 금액을 더하거나 빼서는 안 된다(money-boundary 린트)"
      status: resolved
      verification: judgment
    - statement: "이 플랜이 스키마 · 마이그레이션을 바꿔서는 안 된다 — 제약은 05-03이 만들었고 이 플랜은 그 제약을 증명한다. 제약이 틀렸으면 멈춰 보고한다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/integration/expense-create-idempotency.test.ts"
      provides: "생성 가장자리 — 두 번 · 동시 · 다른 기안자 · 여러 줄 · 이전 차수 · 표 전체 게이트 · 권리 없음 · 닫힌 줄"
      min_lines: 60
    - path: "test/integration/expense-submit-concurrency.test.ts"
      provides: "제출 가장자리 — 동시 6건 · 다른 프로젝트 카운터 · 같은 줄 경합 · 같은 문서 두 번 제출 · EXP-14 · 두 창"
      min_lines: 80
    - path: "test/integration/expense-installment-cap.test.ts"
      provides: "회차 · 상한 · 닫힘 · 외화 한 사례"
      min_lines: 50
    - path: "test/unit/domain/expenses/installment-cap.test.ts"
      provides: "remainingForInstallments Q4 — 원래 통화 비교 · 원화 비교 갈래"
      min_lines: 30
  key_links:
    - from: "test/integration/expense-create-idempotency.test.ts"
      to: "test/integration/fixtures/expenses.ts"
      via: "05-03 픽스처 setupExpenseProject · submitReadyDraft만 쓴다(05-04가 증빙 첨부를 더해도 고칠 필요가 없다)"
      pattern: "fixtures/expenses"
    - from: "domain/expenses/index.ts"
      to: "repositories/expenses.ts"
      via: "insertDraftIfAbsent가 null이면 findDraftByLineAndDrafter로 기존 id"
      pattern: "findDraftByLineAndDrafter"
    - from: "domain/expenses/index.ts"
      to: "domain/money/index.ts"
      via: "제출 트랜잭션 안 재판정에서 remainingForInstallments"
      pattern: "remainingForInstallments"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」 — 계획은 이야기를 지어내지 않았다).

<objective>
05-03이 만든 지출결의 서비스 위에서 **EXP-01 · EXP-14의 날카로운 가장자리**를 DB 제약과 잠금으로 증명한다. 먼저 생성 쪽을 끝에서 끝까지(테스트 → 도메인 → 리포지토리 → 부분 UNIQUE) 관통하는 트레이서로 두 번 눌러도 하나 · 여러 줄 · 표 전체 게이트 · 권리 없음을 굳히고, 이어서 제출 쪽(동시 제출 번호 · 다른 프로젝트 카운터 · 같은 줄 경합 · 회차 상한 · 금액 > 0 · 두 창 저장)을 굳힌다.

Purpose: 기준 1(1줄 1문서 · 회차 · idempotency · 번호)의 경계 사례가 이 플랜에서 성립한다. 05-03을 트레이서 + 마이그레이션 둘로 줄이려고 떼어 낸 플랜이다(plan-checker scope_sanity — 계획 수정 2026-09-26). 사용자 결정 2026-09-26 #6(번호 `{프로젝트 번호}-{순번 4자리}` · period = 프로젝트 번호)을 동시성으로 확인한다.

Output: 통합 3 · 단위 1(새 파일) · 사례가 드러낸 구현 틈 수정(`domain/expenses` · `domain/money` 안에서만).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@.planning/phases/05-expense-approval-leave/05-03-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- 코드를 쓰기 전에 Superpowers `test-driven-development` 스킬을 **호출**한다(실패 테스트 먼저). 실패를 쫓기 전에 `systematic-debugging`, 커밋 · 「완료」 전에 `verification-before-completion`을 호출하고 verify 명령의 실제 출력으로 판정한다. 서브에이전트 위임 시 프롬프트에 세 스킬 이름과 `model`(구현 `sonnet`)을 적는다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 이름은 05-01 SUMMARY `name_map` · 05-03 SUMMARY의 실제 이름을 쓴다(아래 본문의 이름은 계획 원문).
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-14.env"` — diff 검사는 `. "$(git rev-parse --git-dir)/gsd-05-14.env" && test -n "$PLAN_BASE" &&`로 시작.
- 트랜잭션 규약(트랜잭션 안에서는 tx 호출만) · 「오늘」 = `seoulToday(deps.now)` · 금액 산술은 `domain/money` 밖 금지 · `.planning/STATE.md` 불변 · 새 의존성 0 — 05-01 execution_notes와 같다. 통합 명령은 `flock /tmp/plant8-erp-test.lock`.
- **스키마를 고치지 않는다.** 제약(부분 UNIQUE · CHECK)은 05-03 마이그레이션에 있다. 사례가 제약 자체의 잘못을 드러내면 두 번째 마이그레이션을 만들지 말고 멈춰 보고한다(05-03 마이그레이션 재생성은 05-03 규칙대로 사람 판단).
- 픽스처는 05-03의 `setupExpenseProject` · `submitReadyDraft`만 쓴다 — 05-04가 `submitReadyDraft`에 증빙 첨부를 더해도 이 플랜 테스트는 고칠 필요가 없다.
- **증빙 경합(제출 ↔ 증빙 삭제 · 삭제 ↔ 삭제)은 이 플랜이 다루지 않는다**(리뷰 Round 2 B2): 이 플랜은 웨이브 3이라 `files` 표 · `removeEvidence`가 아직 없다(05-04 웨이브 4). 그 `deps.afterLock` 두 순서 사례는 05-04 Task 3(작성 중 — 제출 ↔ 삭제 · 같은 파일 두 삭제)과 05-09 Task 3(결재 중 — 서로 다른 파일 두 삭제, 결과 증빙 ≥ 1)이 맡는다. 이 플랜의 `lock-race.ts` · `afterLock` 사례 모양을 그 두 플랜이 따라 쓴다.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 생성 가장자리 끝에서 끝까지: 두 번 · 동시 두 번 · 다른 기안자 · 여러 줄 · 이전 차수 줄 · 닫힌 줄 · 표 전체 게이트 · 권리 없음 (테스트 → 도메인 → 리포지토리 → 부분 UNIQUE)</name>
  <files>domain/expenses/index.ts, domain/expenses/line-door.ts, domain/expenses/gate.ts, test/integration/expense-create-idempotency.test.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-03-SUMMARY.md`(실제 이름 · 픽스처 도우미 · 트랜잭션 안 호출 표)
    - 05-03 산출물 `domain/expenses/index.ts`의 `createExpenseFromLines` · `domain/expenses/line-door.ts` · `domain/expenses/gate.ts` · `repositories/expenses.ts`의 `insertDraftIfAbsent` · `findDraftByLineAndDrafter` · `test/integration/fixtures/expenses.ts`
    - `domain/rules/register.ts`(`quote.customer-approval` 문자열 · `CUSTOMER_APPROVAL_EXEMPT_STATUSES` — 줄 번호 대신 괄호 안 이름으로 `grep -n` 뒤 그 범위만 Read(리뷰 Round 1 F4))
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S6 막힘 ①~⑨ 원문(264행) · Assumptions #1 · #2 · #3(803–805행)
  </read_first>
  <behavior>
    - (통합) 같은 PM이 같은 줄로 `createExpenseFromLines`를 연달아 두 번 → 같은 expenseId · 작성 중 행 1개. `Promise.all`로 동시에 두 번 → 작성 중 행 1개 · 두 응답의 expenseId가 같다. 다른 PM(같은 팀, 프로젝트 쓰기 권리 있음)이 같은 줄로 만들면 그 사람 몫 작성 중 문서가 따로 하나 생긴다
    - (통합) 줄 셋(거래처 있음 · 거래처 없음 · 취소 줄)을 한 번에 → `created` 1 · `blocked` 2(`거래처 없음 · 거래처 고르기` · 취소 줄은 행동 없음 사유). 현재 차수가 아닌 이전 차수 줄 id → blocked `견적 줄이 현재 차수에 없음 · 견적 줄 바꾸기`. 분할 없이 제출된 문서(`submitReadyDraft`)가 있는 줄 → blocked `이 줄에 지출결의 26001-0001 있음 · 지출결의 열기`
    - (통합) 진행 프로젝트 · 2차 미승인 · 설정 켜짐 → `blocked` 전부가 `2차 고객 승인 전 · 고객 승인 표시`(담당 PM) 문자열이고 작성 중 행 0. 설정을 끄면 생성된다. 수주중(bidding) 프로젝트는 고객 승인과 무관하게 생성된다(D-43). 완료 프로젝트 → `완료 프로젝트 · 새 지출결의 없음`
    - (통합) `expenses` 쓰기가 없는 사람(권한 행을 지운 계급) · 그 프로젝트의 쓰기 권리가 없는 다른 팀 PM이 같은 줄로 부르면 권한 오류이고 작성 중 행 0
  </behavior>
  <action>
① behavior 넷을 새 파일 `test/integration/expense-create-idempotency.test.ts`에 describe 넷(`두 번 눌러도 하나` · `여러 줄` · `표 전체 게이트` · `권리 없음`)으로 먼저 적고 실행해 결과를 기록한다(05-03 구현이 이미 맞으면 첫 실행부터 초록일 수 있다 — 그때는 사례마다 한 번 일부러 기대값을 틀려 빨강을 확인한 뒤 되돌린다). 사람 · 프로젝트 · 차수 · 줄은 05-03 `setupExpenseProject`와 도메인 · 리포지토리 함수로만 만든다(SQL 직접 삽입 금지).
② 사례가 드러낸 틈만 고친다 — 예: 동시 생성에서 ON CONFLICT 뒤 재조회가 빠진 경우(`findDraftByLineAndDrafter`), 여러 줄 중 하나가 막히면 전체를 버리는 경우(줄마다 판정해야 한다), 권리 판정이 트랜잭션 안에 들어간 경우(트랜잭션 전으로 옮긴다), 닫힌 줄 문구의 번호가 최신 번호가 아닌 경우. 고친 곳 · 이유를 SUMMARY 표로 적는다.
③ 트레이서 커밋 뒤 `expense-approval-lifecycle.test.ts`(05-03 트레이서)가 여전히 초록인지 같은 명령으로 확인한다.
  </action>
  <verify>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-create-idempotency.test.ts test/integration/expense-approval-lifecycle.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 "No test files found"가 나온다 — 두 번 · 동시 두 번에 작성 중 행이 2개가 되거나, 여러 줄 중 막힌 줄이 전체를 막거나, 고객 승인 전 · 완료 프로젝트에서 행이 생기거나, 권리 없는 사람이 행을 만들거나, 05-03 트레이서가 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(repository-viewer-param · money-boundary 포함) 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "Promise.all" test/integration/expense-create-idempotency.test.ts`가 1 이상이다
    - `grep -c "fixtures/expenses" test/integration/expense-create-idempotency.test.ts`가 1이다
    - 이 task의 diff에 `db/` 아래 파일이 없다(`. "$(git rev-parse --git-dir)/gsd-05-14.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- db`가 비어 있음)
    - 두 검증 명령이 초록이다
  </acceptance_criteria>
  <done>줄에서 지출결의를 만드는 모든 가장자리(두 번 · 동시 · 여러 줄 · 게이트 · 권리)가 부분 UNIQUE와 서버 판정으로 증명된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 제출 가장자리 — 동시 제출 6건 번호 · 다른 프로젝트 카운터 · 같은 줄 경합 · 회차 상한(Q4, 외화 포함) · 금액 빈 칸/0/CHECK · 두 창 저장</name>
  <files>domain/expenses/index.ts, domain/money/index.ts, test/unit/domain/expenses/installment-cap.test.ts, test/integration/expense-submit-concurrency.test.ts, test/integration/expense-installment-cap.test.ts</files>
  <read_first>
    - `test/integration/lock-race.ts`(순서 고정 경합 재현 — ARCHITECTURE §4-8 (5)) · `test/integration/projects-create-concurrency.test.ts` · `test/integration/document-counters-concurrency.test.ts`
    - 05-03 산출물 `domain/expenses/index.ts`의 `submitExpense` · `saveExpenseDraft` · `domain/money/index.ts`의 `remainingForInstallments`
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` Copywriting 「Error — 입력」(259행) · 「거부 — 동시 처리」
  </read_first>
  <behavior>
    - (단위, Q4) `remainingForInstallments`: KRW 줄 실행가 10,000,000 · 앞 문서 6,000,000 → 남음 4,000,000, 이번 4,000,001 → `exceeds` 참. USD 줄 실행가 USD 10,000.00 · 앞 문서 USD 6,000.00(환율 1,300) · 이번 USD 4,000.00(환율 1,400 — 원화 합은 실행가 원화를 넘음) → 원래 통화 비교라 `exceeds` 거짓 · basis `foreign`. 앞 문서 하나가 KRW면 basis `krw`로 원화 비교
    - (통합, 기준 1) 같은 프로젝트의 서로 다른 줄 여섯의 작성 중 문서를 기안자 여섯이 `Promise.all`로 동시 제출 → 기본 풀(`DB_POOL_MAX` 5)에서 테스트 제한 시간 안에 전부 끝나고 번호 여섯 개가 `26001-0001`~`26001-0006`으로 서로 다르다(순서는 단언하지 않음). 다른 프로젝트 `26002`의 첫 제출은 `26002-0001`이다
    - (통합, 같은 줄 경합) 같은 비분할 줄의 작성 중 문서 둘(기안자 둘)을 `deps.afterLock` + `lock-race.ts`로 순서를 고정해 제출 → 먼저 잠근 쪽 성공, 늦은 쪽 `GateBlockedError`이고 이유가 `이 줄에 지출결의 26001-0001 있음 · 지출결의 열기` · 늦은 쪽 문서는 번호 null 그대로 · 카운터 값 1
    - (통합, 회차) 분할용 줄(실행가 10,000,000)에서 `분할 지급` 켠 첫 문서 6,000,000 제출 → 회차 1. 같은 줄 새 작성 중 문서는 분할 문서로 만들어지고 5,000,000으로 제출 → `ExpenseFieldError` `남은 실행가 4,000,000 넘음 · 공급가액 고치기` · 번호 null. 4,000,000으로 고쳐 제출 → 회차 2 · 이후 그 줄로 `createExpenseFromLines` → blocked(남은 0 — 닫힘). USD 줄 한 사례(단위 외화 사례와 같은 값)를 통합으로도
    - (통합, EXP-14) 공급가액 원화가 null인 작성 중 문서 제출 → `공급가액 비어 있음 · 공급가액 적기` · 0 → `공급가액이 0 · 0보다 크게` · 음수 입력은 `saveExpenseDraft`가 검증 오류로 거부. 리포지토리로 번호 있는 행의 공급가액을 0으로 바꾸는 UPDATE는 DB CHECK 위반(23514)으로 실패한다
    - (통합, 같은 문서 두 번 제출 — F3) 증빙이 붙은 작성 중 문서를 version V로 `submitExpense` 두 번(차례로 한 번 · `Promise.all` 동시 한 번, 각각 새 픽스처) → 첫 결과 번호 `26001-0001` · 두 번째 결과 `{ kind: "already_submitted", expenseId, number: "26001-0001" }` · 카운터 값 1 · 결재 인스턴스 1 · `document_submit` 로그 1 · `ExpenseConflictError`가 나오지 않는다
    - (통합, 두 창) 같은 작성 중 문서를 version 1로 두 번 저장 → 두 번째가 `ExpenseConflictError`이고 문구가 `HH:MM에 다른 곳에서 저장됨 · 새로 고침` 꼴이다
  </behavior>
  <action>
① behavior 단위 사례를 새 파일 `test/unit/domain/expenses/installment-cap.test.ts`에 먼저 적고 결과를 확인한 뒤 틈이 있으면 `domain/money/index.ts`의 `remainingForInstallments`를 고쳐 초록으로 만든다. `expenseLineDoor`의 남은 0 → 닫힘은 이 함수 결과만 읽으므로(05-03 ③) 틈은 `domain/money`에서 고친다.
② 통합 두 파일을 새로 만든다: `test/integration/expense-submit-concurrency.test.ts`(describe `동시 제출 번호` · `다른 프로젝트 카운터` · `같은 줄 경합` · `같은 문서 두 번 제출` · `금액 0과 빈 칸` · `두 창` — `같은 문서 두 번 제출`은 Addresses review concern: F3, 05-03이 판정을 빠뜨렸으면 `submitExpense`의 잠금 뒤 · version 확인 전 자리에 고친다) · `test/integration/expense-installment-cap.test.ts`(describe `회차와 상한` · `외화 줄`). 경합은 타이밍 대기 없이 `deps.afterLock`으로만 재현한다. 픽스처는 05-03 `setupExpenseProject` · `submitReadyDraft`만.
③ 사례가 드러낸 구현 틈만 `domain/expenses/index.ts` · `domain/money/index.ts`에서 고친다 — 예: 제출 재판정이 트랜잭션 전 값을 쓴 경우(줄의 번호 있는 문서를 tx로 다시 읽어야 한다), 카운터 period가 연도로 새는 경우, 회차가 트랜잭션 전 개수로 매겨지는 경우, 동시 제출 6건이 풀을 붙잡는 경우(트랜잭션 안 풀 읽기 — 05-03 SUMMARY의 트랜잭션 안 호출 표와 대조). 고친 곳 · 이유를 SUMMARY 표로 적는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/expenses/installment-cap.test.ts test/unit/domain/money.test.ts</automated>
    <fails_when>failed가 1 이상이거나 "No test files found" — 외화 줄이 원화로 비교되거나 부분 KRW 문서가 원 통화로 비교되거나 05-03의 sumKrw · diffKrw 사례가 깨지면 여기서 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/expense-submit-concurrency.test.ts test/integration/expense-installment-cap.test.ts test/integration/expense-create-idempotency.test.ts test/integration/expense-approval-lifecycle.test.ts test/integration/document-counters-concurrency.test.ts test/integration/projects-create-concurrency.test.ts'</automated>
    <fails_when>failed가 1 이상이거나 제한 시간 초과가 나온다 — 동시 제출 6건이 멈추거나 번호가 겹치거나, 다른 프로젝트 첫 번호가 `-0001`이 아니거나, 같은 줄 두 제출이 둘 다 번호를 받거나, 같은 문서 두 번 제출이 번호 둘 · 카운터 2 · 충돌 오류를 내거나, 상한 초과가 통과하거나, 금액 0 · 빈 칸 제출이 통과하거나, 두 번째 저장이 덮어쓰거나, 기존 번호 · 프로젝트 동시성 테스트가 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint(money-boundary 포함) 또는 tsc가 error를 하나라도 출력한다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "afterLock" test/integration/expense-submit-concurrency.test.ts`가 1 이상이다
    - 주석을 뺀 경합 파일에 타이밍 대기가 없다(`grep -v '^ *//' test/integration/expense-submit-concurrency.test.ts | grep -cE "setTimeout|sleep\("`이 0을 출력)
    - `grep -l "fixtures/expenses" test/integration/expense-*.test.ts | wc -l`이 4다(05-03 트레이서 + 이 플랜 셋)
    - `grep -c "23514" test/integration/expense-submit-concurrency.test.ts`가 1 이상이다(DB CHECK 사례)
    - `grep -c "already_submitted" test/integration/expense-submit-concurrency.test.ts domain/expenses/index.ts`가 두 파일 모두 1 이상이다(F3)
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>동시 제출 번호 유일 · 프로젝트별 카운터 · 같은 줄 경합 한 쪽만 · 회차 상한(외화 포함) · 금액 > 0 · 두 창 거부가 DB 제약과 프로젝트 행 잠금으로 증명된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 액션(05-05) → `createExpenseFromLines` · `submitExpense` | 호출자가 보낸 줄 id · 문서 id · 금액 · version은 신뢰하지 않는다 |
| 동시 요청 → 1줄 1문서 · 번호 · 회차 | 애플리케이션 판정만으로는 경합을 못 막는다 — DB 제약 · 행 잠금이 최종 판정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-1401 | Elevation of Privilege | 남의 프로젝트 줄 · 권한 없는 사람의 지출결의 생성 | high | mitigate | 05-03 트랜잭션 전 판정(`can(viewer, "expenses", "write")` + Phase 4 프로젝트 쓰기 권리)을 Task 1 `권리 없음` describe로 증명 — 작성 중 행 0 단언 |
| T-05-1402 | Tampering | 같은 줄 이중 문서 · 회차 초과(경합) | high | mitigate | 작성 중 = 부분 UNIQUE(Task 1 동시 두 번), 제출 = 프로젝트 행 잠금 안 재판정 + 카운터 마지막 쓰기(Task 2 `afterLock` 경합 · 상한 사례) |
| T-05-1403 | Tampering | 음수 · 0 · 빈 공급가액 | medium | mitigate | 서버 거부(⑥ · ⑦) + zod · `normalizeMoneyInput` + DB CHECK — Task 2 `금액 0과 빈 칸`이 UPDATE CHECK 위반(23514)까지 단언 |
| T-05-1404 | Denial of Service | 제출 트랜잭션의 풀 점유 | medium | mitigate | 풀 5에서 동시 제출 6건이 제한 시간 안에 끝나는 사례(Task 2) — 멈추면 트랜잭션 안 풀 읽기를 찾아 옮긴다 |
| T-05-1405 | Tampering | 두 창 덮어쓰기 | low | mitigate | 문서 `version` 조건 저장 — Task 2 `두 창` 사례 |
| T-05-1406 | Tampering | 같은 문서 재시도 제출로 번호 둘 | medium | mitigate | 문서 행 잠금 뒤 이미 제출됨 판정(쓰기 없음) — Task 2 `같은 문서 두 번 제출`(차례 · 동시) · 카운터 1 단언(F3) |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개. `package.json` · `pnpm-lock.yaml` diff 0(verification) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 전체 통과
- `flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev && pnpm db:reset:test && pnpm vitest run --project integration'` 전체 통과(기존 스위트 회귀 없음)
- `pnpm db:generate` 재실행 "No schema changes"(이 플랜은 마이그레이션이 없다)
- `. "$(git rev-parse --git-dir)/gsd-05-14.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- db package.json pnpm-lock.yaml domain/quotes domain/projects` 비어 있음
</verification>

<success_criteria>
- 줄에서 지출결의를 만드는 가장자리(두 번 · 동시 · 여러 줄 · 게이트 · 권리)가 증명된다(트레이서)
- 동시 제출 번호 유일 · 프로젝트별 카운터 · 같은 줄 경합 · 회차 상한 · 금액 > 0 · 두 창이 DB 제약 · 잠금으로 증명된다
- 스키마 · 의존성 변경 0
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 테스트 | `test/integration/{expense-create-idempotency,expense-submit-concurrency,expense-installment-cap}.test.ts` · `test/unit/domain/expenses/installment-cap.test.ts` |
| 수정(사례가 드러낸 틈만) | `domain/expenses/{index,line-door,gate}.ts` · `domain/money/index.ts` — SUMMARY 표 |
| 마이그레이션 | 없음 |

<output>
Create `.planning/phases/05-expense-approval-leave/05-14-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 1108e20

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F3 — 같은 문서 · 같은 version 두 번 제출 사례 없음, 두 번째가 충돌 오류(L21@1108e20) | MEDIUM | Plan 05-14, Task 2: must_haves truth · behavior `같은 문서 두 번 제출`(차례 · 동시) — 번호 하나 · 카운터 1 · 두 번째 `already_submitted` · 충돌 오류 없음 · T-05-1406 · acceptance grep. 판정 구현 자리는 05-03 Task 1 ⑥, 화면은 05-05 Task 1 ① |
| F4 — read_first 코드 줄 범위(L22@1108e20) | MEDIUM | Plan 05-14 Task 1 read_first: `domain/rules/register.ts` 줄 범위를 이름 `grep -n` 방식으로 교체 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 해당하는 지적은 전부 반영했다 |

### Round 2 — 69fb7ea

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| B2 — 「05-14에 `afterLock` 두 순서 사례 2(제출↔삭제, 삭제↔삭제 — 결과 증빙 ≥ 1)」(L27@69fb7ea) — 사례 위치 조정 | HIGH | 웨이브 순서(05-03 → 05-14 → 05-04)는 그대로 두고 사례를 뒤 플랜으로 옮긴다: 이 플랜(웨이브 3)이 돌 때 `files` 표 · `removeEvidence` · 게이트 ⑧이 없어 두 사례를 쓸 수 없다. 제출 ↔ 삭제(두 순서) · 같은 파일 두 삭제 = Plan 05-04 Task 3(잠금 구현과 같은 플랜), 결재 중 서로 다른 파일 두 삭제(결과 ≥ 1) = Plan 05-09 Task 3(결재 중 삭제를 여는 플랜 — 05-04 Round 2 Ledger Deferred에서 넘김). 이 플랜에는 execution_notes 한 줄(다루지 않는 이유와 담당 플랜)만 더했다 — 테스트 · 코드 · verify 변경 없음 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | Round 2에서 이 플랜에 해당하는 지적은 B2 사례 위치뿐이고 위에서 처리했다 |
