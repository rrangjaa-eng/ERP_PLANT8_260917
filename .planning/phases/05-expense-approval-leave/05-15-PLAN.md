---
phase: 05-expense-approval-leave
plan: 15
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - repositories/expenses.ts
  - domain/quotes/lines.ts
  - domain/expenses/index.ts
  - app/(app)/projects/status-display.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - test/integration/linked-documents-by-line.test.ts
  - test/e2e/expense-submit-mobile-approval.spec.ts
autonomous: true
requirements: [EXP-01, UX-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-66: 번호 있는(제출된 적이 있는) 지출결의가 연결된 견적 줄의 금액 셀은 읽기 전용이고 이유는 `지출결의 {번호} 연결됨 · 고치려면 새 차수`다 — 새 차수에서도 계보로 같은 줄에 따라온다. 작성 중 문서는 연결로 세지 않는다(`linkedDocumentsByLine`이 번호 있는 문서만 · UI Assumptions #3)"
    - "견적 줄 상태 열의 파생값 `지출결의 중`(`--accent` · 결재 중이거나 승인된 제출 문서가 있음)과 `반려`(`--danger` · 그 줄 문서 중 반려가 있음)가 켜진다 — 한 값 규칙 `취소 > 반려 > 지출결의 중 > 미착수`, 작성 중 · 회수 문서는 줄 상태를 바꾸지 않는다(UI-SPEC Color 매핑 · B2)"
    - "`linkedDocumentsByLine`를 채운 뒤에도 Phase 4 견적 통합 · E2E 묶음(`quote-*` · `ledger-save-flow`)이 녹색이다 — 고객 승인 끄기 막힘(`hasLinkedDocuments`)이 실데이터로 동작한다(Pitfall 7)"
    - "견적 줄 표 행 행동 셀의 나머지 갈래 — 거래처 없음 = `거래처 없음`(`--danger`) + ≥1024에서 3차 `거래처 고르기`(그 줄 거래처 셀 편집 진입) · 700~1023은 글자만 · 취소 · 조정 · 견적 외 비용 줄 = 빈 셀(글자 · `—` 없음) · 표 전체 게이트(진행 · 현재 차수 고객 승인 전 / 완료) = `지출결의 올리기`를 렌더하지 않고 이유는 표 위 한 줄 하나, `지출결의 열기`는 그대로 — 선행 단계가 끝나기 전에는 다음 동작이 없다(UX-06 · UIC populated S1 · partial S1 · empty S1)"
    - "셀의 `지출결의 올리기`를 누르면 그 버튼이 `지출결의 올리기…`이고 같은 열 나머지 버튼은 `aria-disabled`이며, 실패는 합계 행 오른쪽 한 줄 `지출결의를 만들지 못했습니다 · 다시 시도`다. 미저장 편집이 있으면 이동하지 않고 합계 행 오른쪽에 `저장 안 한 편집 N칸 · 먼저 일괄 저장`(DR-6 같은 함수)이다(UX-06 · UIC loading S1 · error S1)"
    - "편집 중이 아닐 때 `Ctrl+E`(`preventDefault`)는 활성 셀 줄의 셀 동작과 같다 — 한 줄은 토스트 없이 폼으로 간다. 셀의 3차는 격자 로빙에서 빠지고 `aria-describedby`가 그 줄 항목 칸을 가리킨다. 힌트 줄 끝에 `지출결의 올리기 Ctrl+E`(행동 열이 서는 사람에게만 · 7개를 넘으면 복사 · 붙여넣기 두 항목을 `범위 복사 Ctrl+C / 붙여넣기 Ctrl+V` 하나로)(UIC zero-one-many S1 한 줄 · UI-SPEC S1)"
    - "행동 셀은 고정 라벨이라 `nowrap`이고 `거래처 없음` · `거래처 고르기` 두 조각 사이에서만 꺾이며, Phase 4 표의 30줄 페이지 · 300줄 상한 · 숨는 열은 그대로다(UIC long-text S1 · overflow S1)"
  prohibitions:
    - statement: "작성 중 문서를 D-66 연결로 세어 견적 줄 금액 셀을 잠가서는 안 된다"
      status: resolved
      verification: judgment
    - statement: "`domain/quotes`가 `domain/expenses`를 import해서는 안 된다 — 리포지토리 한 쿼리로 읽는다(순환 금지)"
      status: resolved
      verification: judgment
    - statement: "화면이 셀 · 표 전체 게이트 판정을 스스로 추론해서는 안 된다 — `listLineDoors`가 보낸 값만 그린다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/integration/linked-documents-by-line.test.ts"
      provides: "D-66 · 계보 · 작성 중 제외 · 파생 상태 · 고객 승인 끄기 막힘"
      min_lines: 40
    - path: "repositories/expenses.ts"
      provides: "listNumberedByProject — 프로젝트의 번호 있는 지출결의 · 결재 상태 한 쿼리"
      contains: "listNumberedByProject"
    - path: "app/(app)/projects/status-display.ts"
      provides: "줄 파생 상태 `지출결의 중` · `반려`"
      contains: "지출결의 중"
  key_links:
    - from: "domain/quotes/lines.ts"
      to: "repositories/expenses.ts"
      via: "linkedDocumentsByLine이 도메인 순환 없이 리포지토리 한 쿼리로 번호 있는 문서를 읽는다"
      pattern: "listNumberedByProject"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/unsaved-edits.ts"
      via: "미저장 편집 한 줄은 DR-6 함수를 그대로 쓴다"
      pattern: "unsavedEditsReason"
---

## Phase Goal

ROADMAP 원문 Goal은 user story 형식이 아니다(05-01 「Phase Goal」).

<objective>
05-05가 세운 화면 트레이서 위에서 **견적 줄 표를 완성**한다. 먼저 D-66 연결 잠금을 리포지토리 → `linkedDocumentsByLine` → 줄 파생 상태 → 견적 줄 표 화면까지 한 번에 관통시키는 트레이서로 켜고(Phase 4 견적 통합 회귀 포함), 이어서 행 행동 셀의 나머지 갈래 · 표 위 한 줄 · 누름 중 · 미저장 편집 · 한 줄 `Ctrl+E` · 힌트 줄을 붙이고 Phase 4 견적 표 E2E 회귀를 돌린다.

Purpose: 제출된 지출결의가 견적 줄을 잠그고 줄 상태에 보이며(D-66 · B2), 선행 단계 전에는 다음 동작이 없다(UX-06). 05-05를 화면 트레이서 + 폰 둘로 줄이려고 떼어 낸 플랜이다(plan-checker scope_sanity — 계획 수정 2026-09-26).

Output: `listNumberedByProject` · `linkedDocumentsByLine` 본문 · 줄 DTO `linkedStatus` · 파생 상태 낱말 둘 · 행동 셀 나머지 갈래 · 한 줄 `Ctrl+E` · 통합 1 · E2E describe 둘(기존 스펙에 덧붙임). 여러 줄 `Ctrl+E`는 05-08이 이 배선 위에 더한다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expense-approval-leave/05-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-05-SUMMARY.md
@CLAUDE.md
</context>

<execution_notes>
- Superpowers `test-driven-development`(통합 · E2E 먼저) · `systematic-debugging` · `verification-before-completion`을 **호출**한다. 서브에이전트 위임 시 스킬 이름과 `model`(`sonnet`)을 적는다.
- 웨이브: 05-01 「웨이브 근거」 — 로컬 DB 하나(`erp`/`erp_test`)를 쓰는 플랜은 한 웨이브에 하나씩 이어 달리고, 플랜 사이에 `/gsd-pause-work`로 세션을 나눌 수 있다.
- 화면 규칙의 정본은 `docs/design/SYSTEM.md`(05-02 개정 포함)와 UI-SPEC S1이다. 새 색 · 서체 · radius · 간격 토큰 0개 — `tokens.css` diff 0. 화면 검증 순서(CLAUDE.md §6): 싼 게이트 → 이 플랜의 E2E. 독립 DOM 감사 · 전체 게이트는 05-13이 한 번 한다.
- `quote-table.tsx`는 2,338행(Phase 4) + 05-05 행동 열이다 — 범위로만 읽고 외과적으로 고친다. 기존 열 · 키 처리 · 스타일은 바꾸지 않는다.
- `printf 'PLAN_BASE=%s\n' "$(git rev-parse HEAD)" > "$(git rev-parse --git-dir)/gsd-05-15.env"` · diff 검사는 `. "$(git rev-parse --git-dir)/gsd-05-15.env" && test -n "$PLAN_BASE" &&`로 시작. `.planning/STATE.md` 불변 · 새 의존성 0. 통합 · E2E는 `flock /tmp/plant8-erp-test.lock`, E2E는 `CI=true`.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — D-66 연결 잠금 끝에서 끝까지: `listNumberedByProject` → `linkedDocumentsByLine` → 줄 파생 상태 → 견적 줄 표 금액 셀 읽기 전용 · 상태 `지출결의 중` (리포지토리 · 견적 도메인 · 상태 표시 · 통합 · E2E) + Phase 4 견적 통합 회귀</name>
  <files>repositories/expenses.ts, domain/quotes/lines.ts, app/(app)/projects/status-display.ts, test/integration/linked-documents-by-line.test.ts, test/e2e/expense-submit-mobile-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-05-SUMMARY.md`(실제 이름 · 행동 열 · E2E 픽스처 도우미)
    - `domain/quotes/lines.ts` 95–110 · 170–200 · 240–290 · 810–830행(`linkedDocumentsByLine` 자리 · `hasLinkedDocuments` · 저장 트랜잭션 안 호출) · `domain/quotes/lineage.ts`(`resolveLinkedDocumentsByLineage`) · `domain/quotes/revisions.ts` 150–210행(고객 승인 끄기 막힘) · `domain/quotes/edit-scope.ts` 40–60 · 135–140행(`linkedDocumentReason`)
    - `app/(app)/projects/status-display.ts`(Phase 4 줄 상태 매핑)
    - `.planning/phases/05-expense-approval-leave/05-RESEARCH.md` 370–383행(Pattern 6) · 479–481행(Pitfall 7)
    - `test/unit/import-cycles.test.ts`(domain/quotes → domain/expenses import 금지 판정)
    - Phase 4 통합 `test/integration/quote-approved-lock.test.ts` · `quote-lines-conflict.test.ts` 머리(연결 문서 가정)
  </read_first>
  <behavior>
    - (통합) 줄 1에 작성 중 문서만 있으면 `linkedDocumentsByLine` 결과에 줄 1이 없다. 제출(번호 `26001-0001`)하면 `[{ number: "26001-0001" }]`이고 금액 셀 편집 가능성이 읽기 전용 · 이유 `지출결의 26001-0001 연결됨 · 고치려면 새 차수`
    - (통합) 2차를 만들면 2차의 같은 계보 줄에 같은 연결이 따라온다(`byCurrentLine`)
    - (통합) 제출 문서가 결재 중 · 승인이면 줄 파생 상태 `지출결의 중`, 반려되면 `반려`, 회수면 파생 없음(미착수), 취소 줄은 `취소`가 이긴다
    - (통합) 연결 문서가 있는 차수의 고객 승인 끄기는 기존 Phase 4 막힘 문구로 거부된다
    - (E2E, desktop) describe `D-66 잠금` — PM이 줄 1로 제출한 뒤 프로젝트 상세로 돌아오면 줄 1 상태 셀 `지출결의 중` · 금액 셀이 편집되지 않고 이유 `지출결의 {번호} 연결됨 · 고치려면 새 차수`가 보인다
  </behavior>
  <action>
① behavior 통합 넷을 새 파일 `test/integration/linked-documents-by-line.test.ts`에, E2E 한 줄을 `test/e2e/expense-submit-mobile-approval.spec.ts`의 새 describe `D-66 잠금`에 먼저 적고 실패를 확인한다. 픽스처는 05-03 `test/integration/fixtures/expenses.ts`의 `setupExpenseProject` · `submitReadyDraft`와 05-05 E2E 도우미를 쓴다.
② `repositories/expenses.ts`에 `listNumberedByProject(viewer, projectId, tx?)` — 그 프로젝트의 번호 있는 · 삭제 안 된 지출결의의 `quote_line_id` · 번호 · 결재 인스턴스 상태(`approval_instances` 조인, 종류 = 지출결의 키) 한 쿼리.
③ `domain/quotes/lines.ts`의 `linkedDocumentsByLine(viewer, revisionId, tx?)` 본문만 채운다 — 그 차수의 프로젝트 · 모든 차수 줄(기존 리포지토리) + `listNumberedByProject(tx)` → `resolveLinkedDocumentsByLineage(lines, docsByLineId).byCurrentLine` → `Map<lineId, { number }[]>`. 시그니처 · 반환 타입 · 호출자는 바꾸지 않는다. `domain/quotes`는 `domain/expenses`를 import하지 않는다(리포지토리 직접 — 순환 금지). 같은 쿼리 결과로 줄 파생 상태 입력(`linkedStatus`: `rejected` 있음 / 진행 · 승인 있음 / 없음)을 줄 DTO에 한 칸 더하고, `app/(app)/projects/status-display.ts`에 `지출결의 중`(accent · `text`) · `반려`(danger)를 한 값 규칙 순서로 더한다. 금액 셀 읽기 전용 이유는 Phase 4 `linkedDocumentReason`이 이미 그리므로 화면 파일은 고치지 않는다.
④ Phase 4 견적 통합 묶음을 같은 검증에 넣어 실데이터 회귀를 본다(Pitfall 7).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/import-cycles.test.ts</automated>
    <fails_when>failed가 1 이상이다 — `domain/quotes`가 `domain/expenses`를 import하면 순환 가드가 깨진다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; pnpm vitest run --project integration test/integration/linked-documents-by-line.test.ts test/integration/quote-approved-lock.test.ts test/integration/quote-lines-conflict.test.ts test/integration/quote-lines.test.ts test/integration/quote-revisions.test.ts test/integration/quote-revision-races.test.ts test/integration/quote-line-cap.test.ts'</automated>
    <fails_when>failed가 1 이상이다 — 작성 중 문서가 연결로 세이거나, 계보가 새 차수로 따라오지 않거나, 파생 상태 순서가 틀리거나, Phase 4 견적 통합이 실데이터로 깨지면 여기서 깨진다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build &amp;&amp; flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-submit-mobile-approval.spec.ts'</automated>
    <fails_when>lint · tsc · build가 error로 끝나거나 playwright failed · "did not run"이 1 이상이다 — 제출 뒤 줄 상태 셀이 `지출결의 중`이 아니거나 금액 셀이 편집되면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "from \"@/domain/expenses" domain/quotes/*.ts`가 0이다
    - `linkedDocumentsByLine`의 export 시그니처 줄이 바뀌지 않았다(`. "$(git rev-parse --git-dir)/gsd-05-15.env" && test -n "$PLAN_BASE" && B=$(git show "$PLAN_BASE":domain/quotes/lines.ts) && printf '%s\n' "$B" | grep -F "export function linkedDocumentsByLine("`의 출력 줄이 지금 파일에도 그대로 있다)
    - `grep -c "지출결의 중" "app/(app)/projects/status-display.ts"`가 1 이상이다
    - 세 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>제출된 지출결의가 견적 줄을 D-66으로 잠그고 줄 상태에 보이는 흐름이 리포지토리에서 화면까지 통합 · E2E로 증명되고, Phase 4 견적 통합은 그대로 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 견적 줄 표 행동 셀 완성 — 나머지 갈래 · 표 위 한 줄 · 누름 중 · 미저장 편집 · 한 줄 `Ctrl+E` · 힌트 줄 · 접근성 + Phase 4 견적 표 E2E 회귀</name>
  <files>domain/expenses/index.ts, app/(app)/projects/[id]/quote-table.tsx, test/e2e/expense-submit-mobile-approval.spec.ts</files>
  <read_first>
    - `.planning/phases/05-expense-approval-leave/05-UI-SPEC.md` S1(294–329행) · Copywriting 표(240–283행)
    - `app/(app)/projects/[id]/quote-table.tsx`의 05-05 행동 열 자리(`grep -n "listLineDoors\|행동" "app/(app)/projects/[id]/quote-table.tsx"`로 찾기) · 1–200행(힌트 항목 · 활성 셀 · 키 처리) · `app/(app)/projects/[id]/unsaved-edits.ts`(DR-6 문구 함수)
    - 05-05 산출물 `domain/expenses/index.ts`의 `listLineDoors` 셀 모양
  </read_first>
  <behavior>
    - (E2E, desktop 1280) describe `행 행동 갈래` — 거래처 없는 줄 셀 `거래처 없음` + `거래처 고르기`(누르면 그 줄 거래처 셀 편집 진입), 취소 줄 셀이 비어 있음, 2차 미승인 진행 프로젝트는 표 위 한 줄 `2차 고객 승인 전 · 고객 승인 표시`이고 문 열림 줄에 `지출결의 올리기`가 없으며 `지출결의 열기`는 있음
    - (E2E) 미저장 편집 1칸이 있을 때 `지출결의 올리기`를 누르면 이동하지 않고 합계 행 오른쪽 `저장 안 한 편집 1칸 · 먼저 일괄 저장`. 만들기 실패(요청 가로채기로 500) → 합계 행 오른쪽 `지출결의를 만들지 못했습니다 · 다시 시도`. 누르는 동안 같은 열 나머지 버튼에 `aria-disabled="true"`
    - (E2E) 편집 중이 아닐 때 활성 셀 줄에서 `Ctrl+E` → 폼 이동(토스트 없음), 힌트 줄 끝 `지출결의 올리기 Ctrl+E`, 셀 3차에 `aria-describedby`가 그 줄 항목 칸 id이고 Tab 로빙에 들지 않는다
    - (E2E, desktop 800) 거래처 없는 줄 셀이 글자 `거래처 없음`만이고 `거래처 고르기`가 없다
  </behavior>
  <action>
① behavior E2E 넷을 `test/e2e/expense-submit-mobile-approval.spec.ts`의 새 describe `행 행동 갈래`에 먼저 적는다.
② `quote-table.tsx` 행동 셀 나머지 갈래(거래처 없음 + `거래처 고르기` — 그 줄 거래처 셀 편집 진입, ≥1024만 / 700~1023 글자만 · 행동 없는 줄 빈 셀 · 표 전체 게이트 = 표 위 한 줄 목록에 이유 하나 · 누른 버튼 pending + 같은 열 `aria-disabled` · 실패 한 줄 · 미저장 편집 한 줄은 DR-6 함수 `unsavedEditsReason` · 셀 3차는 격자 로빙 제외 + `aria-describedby` = 항목 칸 id) · 편집 중이 아닐 때 `Ctrl+E`(`preventDefault`, 활성 셀 줄 — 범위 선택 여러 줄은 05-08) · 힌트 줄 끝 항목(7개 규칙). 셀 값은 `listLineDoors`가 보낸 것만 그린다.
③ `listLineDoors`가 표 전체 게이트 이유 · 셀 갈래를 05-05 트레이서 모양대로 이미 보내는지 확인하고, 빠진 칸이 있을 때만 `domain/expenses/index.ts`에 더한다(판정은 05-03 `expenseLineDoor` 재사용).
④ Phase 4 견적 표 E2E 묶음을 같은 검증에 넣는다.
  </action>
  <verify>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; flock /tmp/plant8-erp-test.lock pnpm build</automated>
    <fails_when>eslint · stylelint · tsc · next build 중 하나라도 error를 출력하거나 0이 아닌 코드로 끝난다</fails_when>
    <automated>flock /tmp/plant8-erp-test.lock sh -c 'pnpm db:dev &amp;&amp; pnpm db:reset:test &amp;&amp; CI=true pnpm playwright test test/e2e/expense-submit-mobile-approval.spec.ts test/e2e/quote-table.spec.ts test/e2e/ledger-save-flow.spec.ts test/e2e/quote-revisions.spec.ts'</automated>
    <fails_when>playwright failed가 1 이상이거나 "did not run"이 1 이상이다 — 셀 갈래 · 표 위 한 줄 · 미저장 편집 · 실패 한 줄 · `Ctrl+E` · 힌트 줄 · 접근성 중 하나라도 다르거나 Phase 4 견적 표 E2E가 깨지면 여기서 깨진다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "unsavedEditsReason" "app/(app)/projects/[id]/quote-table.tsx"`가 1 이상이다(DR-6 함수 재사용)
    - `grep -c "행 행동 갈래" test/e2e/expense-submit-mobile-approval.spec.ts`가 1이다
    - `. "$(git rev-parse --git-dir)/gsd-05-15.env" && test -n "$PLAN_BASE" && git diff --stat "$PLAN_BASE"..HEAD -- docs/design/tokens.css package.json pnpm-lock.yaml`이 비어 있다
    - 두 검증 명령이 전부 초록이다
  </acceptance_criteria>
  <done>견적 줄 표 행동 셀이 다섯 갈래 · 표 위 한 줄 · 미저장 편집 · 한 줄 `Ctrl+E`를 서버 판정대로 그리고, Phase 4 견적 표 E2E는 그대로 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 판정 → 화면 | 행동 셀 · 표 전체 게이트 · 금액 셀 잠금은 서버가 정하고 화면은 그린다 |
| 견적 도메인 → 지출결의 데이터 | `domain/quotes`는 리포지토리로만 읽는다(순환 · 우회 금지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-1501 | Tampering | 제출된 지출결의가 붙은 줄의 금액 변경(D-66 우회) | high | mitigate | `linkedDocumentsByLine`이 번호 있는 문서를 계보까지 돌려주고 Phase 4 저장 트랜잭션 안 판정(`hasLinkedDocuments` · `linkedDocumentReason`)이 서버에서 막는다 — Task 1 통합이 읽기 전용 · 고객 승인 끄기 막힘을 단언 |
| T-05-1502 | Elevation of Privilege | 숨긴 `지출결의 올리기`를 되살려 액션 직접 호출 | high | mitigate | 셀은 `listLineDoors` 값만 그리고, 액션 → `createExpenseFromLines`가 권한 · 게이트를 다시 판정(05-03 · 05-14 `권리 없음` · `표 전체 게이트`) |
| T-05-1503 | Information Disclosure | 줄 파생 상태로 남의 문서 존재 노출 | low | accept | 줄 상태는 그 프로젝트 견적 표를 볼 수 있는 사람에게만 보이고 번호 · 상태 낱말뿐이다(금액 · 기안자 없음) — Phase 4 견적 표 가시성 그대로 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 외부 패키지 0개 — Task 2 수용 기준(lockfile diff 0) |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm lint:sql` · `flock /tmp/plant8-erp-test.lock pnpm build` 통과
- `pnpm test:unit` 통과(import-cycles 포함)
- 통합: `linked-documents-by-line` · Phase 4 `quote-*` 녹색
- `CI=true` E2E: `expense-submit-mobile-approval` · Phase 4 `quote-table` · `ledger-save-flow` · `quote-revisions` 녹색
- `tokens.css` · `package.json` · lockfile diff 0 · `pnpm db:generate` 재실행 "No schema changes"(이 플랜은 마이그레이션이 없다)
</verification>

<success_criteria>
- 제출된 문서가 견적 줄을 D-66으로 잠그고 줄 상태에 보인다(트레이서)
- 행 행동 셀이 다섯 갈래를 서버 판정대로 그리고, 선행 단계 전에는 `지출결의 올리기`가 없다(UX-06)
- Phase 4 견적 통합 · E2E가 그대로 녹색이다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수 | `listNumberedByProject` · `linkedDocumentsByLine`(본문 채움) · 줄 DTO `linkedStatus` |
| 낱말 | 줄 파생 `지출결의 중` · `반려` |
| 화면 | 견적 줄 표 행동 셀 나머지 갈래 · 표 위 한 줄 · 한 줄 `Ctrl+E` · 힌트 줄 항목 |
| 테스트 | `test/integration/linked-documents-by-line.test.ts` · `test/e2e/expense-submit-mobile-approval.spec.ts` describe `D-66 잠금` · `행 행동 갈래` |
| 마이그레이션 | 없음 |

<output>
Create `.planning/phases/05-expense-approval-leave/05-15-SUMMARY.md` when done
</output>
