---
phase: 06-payment-evidence-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - app/(app)/status-display.ts
  - test/unit/status-display.test.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [EXP-07, EXP-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SP-2 상태 낱말(견적 줄 파생 다섯 · 증빙 확인 넷 · 구매 요청 셋 · 발행 요청 셋 · `지급 완료`)의 `StatusTag` kind·variant 매핑이 `app/(app)/status-display.ts` 한 파일에 있고, 이 페이즈의 다른 화면은 이 파일을 읽기만 한다"
    - "`docs/design/DECISIONS.md`에 SP-1~SP-7 일곱 항목이 먼저 커밋되고, 그다음 커밋에서 `docs/design/SYSTEM.md`가 개정된다(CLAUDE.md §6 — DECISIONS 먼저)"
    - "SYSTEM.md §7-3 「일괄 처리 표」(SP-1)에 UI-SPEC rev 9 보강 네 문장이 있다 — 행 선택의 뜻은 체크박스 `checked` 하나(`aria-selected`는 활성 셀 뜻, DR-7) · `selectable` 표에는 새 줄이 없고 표 안 `Ctrl+Enter` = 화면 1차(DR-8) · 처리 뒤 서버가 막힌 행마다 「지금 고를 수 있음」을 다시 보내 게이트 막힘은 선택을 풀고 동시성·재계산 막힘은 유지(H-3) · 결과 글자는 polite `aria-live` 영역 + 막힌 행 이유 줄 id를 체크박스 `aria-describedby`에 더하고 모달이 닫히면 첫 막힌 행 체크박스로 포커스(DR-6)"
    - "SYSTEM.md §7-17에 「첨부 보기 칸」(SP-7) 슬롯 문장이 있다 — 선택 칸 0~1 · 자리 부제 아래 결과 줄 위 · §7-8 첨부 행 모양(썸네일 72×96 + 파일명·장수·용량 + 3차 `크게 보기`) · 올리기·삭제·입력 없음 · 파일이 셋을 넘으면 칸 안만 스크롤 · 쓰는 곳은 확인 대상이 파일일 때만(DR-4)"
    - "SYSTEM.md §7-5에 SP-2 낱말과 견적 줄 우선순위 주석이 있고, 「4자 이하」 규칙이 `tag` 변형에만 걸린다는 문장이 있다"
    - "`docs/design/tokens.css` · `package.json` · `pnpm-lock.yaml`은 merge-base 기준으로 바뀌지 않는다 — 커밋 뒤에도 잡히는 검사다"
    - "D-607 문구 정렬: ROADMAP Phase 6 기준 3과 REQUIREMENTS EXP-07 · EXP-15가 「카드 사용·구매 완료의 금액은 전표 결제 합계로 적고 공급가는 서버가 역산한다」로 맞춰진다 — 사용자가 고른 경로로만 바뀐다"
    - "R-9 문구 정렬: ROADMAP Phase 6 기준 1(과 같은 결의 EXP-09)이 「원천징수 지급은 공급가를 역산하지 않고 차이만 보인다(UA-619) · 부가세 역산 세율은 증빙일 기준 이력 설정(`TAX_VAT_RATE`)」으로 맞춰진다"
  prohibitions:
    - statement: "`.planning/ROADMAP.md` · `.planning/REQUIREMENTS.md`를 사용자 선택 없이 손으로 고쳐서는 안 된다(CLAUDE.md §2) — ROADMAP은 `/gsd-phase --edit 6`, REQUIREMENTS는 Task 3에서 사용자가 고른 경로로만 바뀐다"
      status: resolved
      verification: judgment
    - statement: "새 색·서체·radius·간격 토큰을 만들어서는 안 된다(CLAUDE.md §6)"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/status-display.ts"
      provides: "SP-2 상태 낱말 → StatusTag kind·variant 매핑 한 파일(이 페이즈 모든 화면이 읽는다)"
      exports: ["statusDisplay"]
    - path: "test/unit/status-display.test.ts"
      provides: "SP-2 낱말 전부가 매핑돼 있고 UI-SPEC Color 매핑표와 같은 kind를 돌려주는지"
      min_lines: 20
    - path: "docs/design/DECISIONS.md"
      provides: "SP-1~SP-7 결정 기록(내용·이유·버린 대안)"
  key_links:
    - from: "app/(app)/status-display.ts"
      to: "ui/status-tag/StatusTag.tsx"
      via: "낱말마다 StatusTag의 kind·variant 값만 돌려준다 — 새 색을 만들지 않는다"
      pattern: "StatusTagKind"
    - from: "docs/design/SYSTEM.md"
      to: "docs/design/DECISIONS.md"
      via: "SYSTEM 개정 절마다 DECISIONS SP-n을 가리킨다"
      pattern: "SP-[1-7]"
---

<objective>
이 페이즈의 모든 화면이 기대는 **디자인 기준(SP-1~SP-7)** 과 **상태 낱말 매핑 한 파일**을 먼저 세우고, 잠긴 결정 D-607과 교차 검토 R-9가 요구한 **요구사항·ROADMAP 문구 정렬**을 사용자 선택으로 끝낸다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 뒤 플랜 스무 개가 같은 낱말(SP-2)과 같은 표 규칙(SP-1)을 쓴다. 문서가 먼저 서지 않으면 화면마다 색·낱말이 갈린다(CLAUDE.md §6 「화면 하나만 예외 금지」). D-607은 「계획 단계에서 문구를 `gsd_run`으로 맞춘다」고 잠갔고, 체커 경고 3이 실행 가능한 자리를 요구했다 — Task 3이 그 자리다.

Output: DECISIONS SP-1~SP-7 → SYSTEM 개정(SP-7 = §7-17 「첨부 보기 칸」, UI-SPEC rev 9 — `/plan-design-review` DR-4), `status-display.ts` + 단위 1, ROADMAP 기준 1·3 · REQUIREMENTS EXP-07 · EXP-15(· EXP-09) 문구.

**규모 근거:** `files_modified` 6개(문서 4 · 코드 1 · 테스트 1), `estimate.tokens` 60000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다.

**R-7 메모(교차 검토 참고):** Phase 5 PLAN이 아직 0건이라 이 페이즈 플랜은 전부 가칭 위에 선다. 각 플랜 Task 1 ⓪이 멈추게 돼 있어 실행이 조용히 틀어지지는 않지만, 흩어진 작은 이름 차이는 ⓪이 다 잡지 못한다. Phase 5 계획이 나오면 `/gsd-plan-phase 6 --reviews`로 가칭 → 실제 대응을 본문에 반영해 재검증하고, 04.2 병행 실행 뒤 main 기준점도 그때 다시 잡는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 실행 시작 때 main에서 다시 확인하고, 하나라도 없으면 코드·문서를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-01 착수하지 않음`을 출력하고 멈춘다.**

| id | 먼저 들어와야 하는 것 | 재확인 명령(Task 1 ⓪) |
|---|---|---|
| UA-601 | `SYSTEM.md` Phase 4 개정(§7-15 폼 · §7-17 확인 모달)이 main에 있다 — SP-3·SP-6·SP-7이 그 절을 가리킨다 | `grep -c "^### 7-1[57]\." docs/design/SYSTEM.md` 2 미만이면 멈춤(절 머리만 센다 — 본문의 「§7-15」 같은 참조로는 통과하지 않는다. 2026-09-25 main `0341e36` = 0, Phase 4 브랜치 = 2 — UI-SPEC rev 9 UA 절 「재확인 2026-09-25」, DR-9) |
| UA-609 | `ui/status-tag/StatusTag.tsx`에 `text` 변형이 있다(견적 줄 상태 열) | `grep -c "\"text\"" ui/status-tag/StatusTag.tsx` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-14 | 견적 줄 우선순위 = 취소 > 반려 > 증빙 없음 > 지출결의 중 > 구매 요청 > 지급 완료 > 카드 사용 > 미착수(SYSTEM §7-5 주석으로만 적는다 — 판정 코드는 06-13) | SYSTEM §7-5 주석 한 줄 |
| O-2 | 증빙 확인 낱말 넷(`확인 전`·`확인됨`·`면제`·`선결제`)은 O-2 답과 무관하게 같다 | 해당 없음 |
| O-12 | 발행 요청 낱말 셋(`요청`·`발행됨`·`취소`) | `status-display.ts`의 세 줄 |
| O-9 | 구매 요청 낱말 셋(`신청`·`구매 완료`·`취소`) | `status-display.ts`의 세 줄 |
| REQ-ROUTE (Task 3에서 묻는다) | REQUIREMENTS 문구를 바꿀 경로 — gsd 도구에 요구사항 본문 편집 명령이 없다(`requirements` 하위 명령은 mark-complete · ready-ids · revert-phase뿐) | Task 3 선택 |

<assumption_delta_decision>
`gsd_run query assumption-delta`는 이 페이즈에서 `detected: false`를 돌려줬다. 이 플랜은 1차 명사를 옮기지 않는다 — 문서와 낱말 매핑을 더할 뿐이다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
이 페이즈에는 `SPEC.md`가 없어 spec-less 프로브 폴백이 돌았다. **조용히 버린 항목은 0건이다.**

`coverage.json`이 10개 요구사항 전부에 `category: unclassified` · `status: unresolved` · `probe: "unclassified — review manually"`를 돌려줬다(`applicable: 10, resolved: 0`). 요구사항 원문이 한국어라 분류기의 영어 큐 휴리스틱이 잡지 못한 것이다 — 엣지가 없는 페이즈가 아니라 도구 한계다. 10건 전부 **flagged 가정(unresolved)** 으로 남기고, 실제 수용 기준은 ROADMAP Phase 6 기준 1~5 · `06-CONTEXT.md` D-601~D-613 · `06-UI-SPEC.md` `## UI Considerations`에서 끌어냈다.

| # | 요구사항 | 프로브 결과 | 대신 쓰는 근거 | 수용 기준이 사는 플랜 |
|---|---|---|---|---|
| 1 | EXP-06 | unclassified → unresolved (flagged) | 기준 1 · D-604 · D-606 | 06-03 · 06-04 · 06-13 · 06-15 |
| 2 | EXP-07 | unclassified → unresolved (flagged) | 기준 3 · D-607 · D-609 | 06-01 · 06-05 · 06-07 · 06-08 · 06-09 · 06-13 |
| 3 | EXP-09 | unclassified → unresolved (flagged) | 기준 1 · D-604 · D-605 | 06-01~06-04 · 06-15 · 06-17 · 06-20 · 06-23 · 06-24 |
| 4 | EXP-10 | unclassified → unresolved (flagged) | 기준 3 · A-612 · O-9 · O-10 · O-13 | 06-02 · 06-08 · 06-12 · 06-14 · 06-23 · 06-24 |
| 5 | EXP-13 | unclassified → unresolved (flagged) | 기준 2 · D-603 · D-611 · O-4 · O-5 | 06-02 · 06-10 · 06-23 · 06-24 |
| 6 | EXP-16 | unclassified → unresolved (flagged) | 기준 3 · D-608 · O-11 | 06-02 · 06-09 |
| 7 | EVID-02 | unclassified → unresolved (flagged) | 기준 1 · D-603 | 06-02 · 06-04 · 06-06 · 06-10 · 06-11 · 06-24 |
| 8 | EVID-03 | unclassified → unresolved (flagged) | 기준 2 · D-602 | 06-06 · 06-11 · 06-16 |
| 9 | EVID-04 | unclassified → unresolved (flagged) | 기준 2 | 06-11 |
| 10 | PROJ-06 | unclassified → unresolved (flagged) | 기준 4 · D-610~D-613 | 06-18 · 06-19 · 06-21 · 06-22 · 06-23 · 06-24 |

**금지 항목 — canon 참조:** 주입·경로 순회 같은 canon 보안 항목은 `/cso`와 기존 eslint·squawk가 소유한다. 각 플랜의 prohibitions는 이 제품 고유의 must-NOT이다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — SP-2 낱말 하나가 DECISIONS → SYSTEM §7-5 → status-display.ts → 단위 테스트까지 한 줄로 관통한다</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, app/(app)/status-display.ts, test/unit/status-display.test.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 UA-601 · UA-609가 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md `## 시스템 변경 제안` 절(SP-1~SP-7 원문 — 줄 번호는 rev마다 바뀌므로 절 머리로 찾는다) · Color 절의 견적 줄·증빙 확인·구매 요청·발행 요청 매핑표
    - ui/status-tag/StatusTag.tsx(`StatusTagKind` · `StatusTagVariant`)
    - docs/design/DECISIONS.md 끝 두 항목(항목 모양) · docs/design/SYSTEM.md §7-5
    - test/unit/design-system-docs.test.ts(문서 구조 검사 — 개정이 깨뜨리면 안 된다)
  </read_first>
  <behavior>
    - `statusDisplay("지급 완료")` → `{ kind: "success" }`, `statusDisplay("반려")` → `{ kind: "danger" }`, `statusDisplay("미착수")` → `{ kind: "muted" }` — 값은 UI-SPEC Color 매핑표 그대로
    - SP-2 낱말 목록의 모든 낱말이 매핑을 가진다(목록을 돌며 undefined 0)
    - 목록에 없는 낱말은 타입 오류(리터럴 유니언) — 런타임 기본값으로 떨어지지 않는다
  </behavior>
  <action>
⓪ **선행 의존 재확인(문서 변경 전).** 「선행 의존(A-6xx)」 표의 명령을 돌린다. 하나라도 그 줄의 멈춤 조건에 걸리면 아무것도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-01 착수하지 않음`을 출력하고 멈춘다.

① `docs/design/DECISIONS.md`에 SP-2(상태 낱말 — 견적 줄 파생 다섯 · 증빙 확인 넷 · 구매 요청 셋 · 발행 요청 셋, 「4자 이하」 규칙은 `tag` 변형에만)를 UI-SPEC 원문의 내용·이유·버린 대안 그대로 한 항목으로 커밋한다.

② 다음 커밋에서 `docs/design/SYSTEM.md` §7-5에 SP-2 낱말 표와 견적 줄 우선순위 주석(O-14 순서), 「4자 이하는 `tag` 변형에만」 문장을 더한다. 새 색·서체·radius·간격 토큰은 만들지 않는다 — `docs/design/tokens.css`는 손대지 않는다.

③ `app/(app)/status-display.ts`: SP-2 낱말 리터럴 유니언 타입과 `statusDisplay(word)` → `{ kind, variant? }`를 둔다. kind는 `StatusTagKind` 다섯 값 안에서만, 값은 UI-SPEC Color 매핑표 그대로다. 이 페이즈의 모든 화면이 이 파일을 읽는다 — 다른 파일에 같은 매핑을 두지 않는다.

④ `test/unit/status-display.test.ts`에 behavior 세 줄을 쓴다(실패 → 구현 → 통과).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/status-display.test.ts test/unit/design-system-docs.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — design-system-docs가 빨가면 SYSTEM.md 개정이 문서 구조 검사를 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>eslint·stylelint·tsc 중 하나라도 error로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 재확인 결과가 SUMMARY에 있다
    - `git log --format=%s -- docs/design/DECISIONS.md docs/design/SYSTEM.md`에서 SP-2의 DECISIONS 커밋이 SYSTEM 커밋보다 먼저다
    - `grep -c "SP-2" docs/design/DECISIONS.md`가 1 이상
    - 단위 테스트가 SP-2 낱말 전체를 돌며 매핑 누락 0을 단언한다
  </acceptance_criteria>
  <done>SP-2 낱말 하나가 결정 기록 → 디자인 시스템 → 코드 매핑 → 테스트까지 실제로 이어지고, 뒤 플랜이 읽을 매핑 파일이 선다</done>
</task>

<task type="auto">
  <name>Task 2: SP-1 · SP-3~SP-7 문서 개정 — DECISIONS 먼저, SYSTEM 다음, 토큰·의존성 변경 0(merge-base 기준)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md `## 시스템 변경 제안` 절(SP-1 일괄 처리 표 — rev 9 보강 네 문장 포함 · SP-3 제출 뒤 문서 화면 · SP-4 하위 목록 · SP-5 용어 · SP-6 마지막 증빙 삭제 · SP-7 확인 모달 첨부 보기 칸) · S1 「제자리 증빙 확인」 절(SP-7이 쓰이는 유일한 자리)
    - docs/design/SYSTEM.md §6-1 · §6-3 · §7-3 · §7-8(시트 상세 첨부 행 — SP-7이 모양을 빌린다) · §7-10 · §7-17(확인 모달 슬롯 — SP-7이 칸 하나를 더한다) · §8 · §10(`aria-selected` = 활성 셀 — SP-1 DR-7 문장의 근거)
  </read_first>
  <action>
`docs/design/DECISIONS.md`에 SP-1 · SP-3 · SP-4 · SP-5 · SP-6 · SP-7 여섯 항목을 UI-SPEC 원문의 내용·이유·버린 대안 그대로 먼저 커밋한다(SP-1은 rev 9 보강 네 문장까지 — DR-7 · DR-8 · H-3 · DR-6). 그다음 커밋에서 `docs/design/SYSTEM.md`를 고친다: SP-1 §7-3 「일괄 처리 표」(`selectable` variant 규칙 + 보강 네 문장: 행 선택의 뜻 = 체크박스 `checked` 하나이고 `aria-selected`는 §10 활성 셀 뜻 그대로 · `selectable` 표에는 새 줄이 없고 표 안 `Ctrl+Enter` = 화면 1차 · 처리 뒤 선택은 서버가 행마다 다시 보낸 「지금 고를 수 있음」대로 · 결과 글자 polite `aria-live` 영역 + 막힌 행 이유 줄 id를 체크박스 `aria-describedby`에 + 모달이 닫히면 첫 막힌 행 체크박스, 없으면 1차 — 구현은 06-15 · 06-17) · SP-3 §6-3 「제출 뒤 문서 화면」(제자리 편집 ①②) · SP-4 §6-1 「하위 목록」 진입 규칙(필터 줄 오른쪽 3차 링크, PageHeader 부제 = 부모 메뉴) · SP-5 §8 규칙 7 용어 · SP-6 §7-10 「마지막 증빙 삭제」 확인(지출결의 문서만) · **SP-7 §7-17 슬롯 목록에 「첨부 보기 칸」**(선택 칸 0~1, 자리 = 부제 아래 · 결과 줄 위, 모양 = §7-8 첨부 행 그대로, 보기만 — 올리기·삭제·입력 없음, `크게 보기` = 새 탭 원본, 파일이 셋을 넘으면 칸 높이는 행 셋에서 멈추고 칸 안만 스크롤, 폰 시트 골격에서도 본문 안에 선다, 쓰는 곳은 확인 대상이 파일일 때만 — 이 페이즈는 S1 · S3 제자리 증빙 확인 하나. 구현은 `ui/confirm-dialog` 슬롯으로 06-17이 한다 — 이 태스크는 문서만). 각 개정 절 끝에 `(DECISIONS SP-n)`을 적는다. 토큰 파일과 패키지 파일은 손대지 않는다 — SP-7은 기존 첨부 행 모양의 재사용이라 새 색·서체·radius·간격 값이 없다(UI-SPEC SP-7 「이유」).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/design-system-docs.test.ts test/unit/docs-limits.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 문서 구조 검사나 문서 길이 상한을 깨뜨렸다</fails_when>
    <automated>git diff --stat "$(git merge-base origin/main HEAD)" -- docs/design/tokens.css package.json pnpm-lock.yaml</automated>
    <fails_when>출력이 비어 있지 않다 — 이 플랜 시작 이후(커밋 포함) 토큰이나 의존성이 바뀌었다</fails_when>
    <automated>grep -o "SP-[1-7]" docs/design/DECISIONS.md | sort -u | wc -l</automated>
    <fails_when>7보다 작다 — SP-1~SP-7 중 결정 기록에 없는 항목이 있다(SP-2는 Task 1이 넣는다)</fails_when>
    <automated>grep -c "첨부 보기 칸" docs/design/SYSTEM.md</automated>
    <fails_when>0이다 — SP-7 슬롯이 SYSTEM §7-17에 서지 않았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -o "SP-[1-7]" docs/design/DECISIONS.md | sort -u | wc -l`이 7(서로 다른 SP id 일곱 — 줄 수가 아니라 id를 센다)
    - `git log --format=%H -- docs/design/DECISIONS.md` 중 SP-1·3~7 커밋이 같은 내용의 SYSTEM.md 커밋보다 먼저다
    - SYSTEM.md §7-17 절 안에 「첨부 보기 칸」 문장과 `(DECISIONS SP-7)`이 있고, §7-3 「일괄 처리 표」 절 안에 `aria-selected` 문장(DR-7)과 `Ctrl+Enter` = 화면 1차 문장(DR-8)이 있다
    - merge-base 기준 diff가 비어 있다
  </acceptance_criteria>
  <done>SP-1~SP-7이 결정 기록과 디자인 시스템에 모두 서고(SP-1은 rev 9 보강 포함, SP-7은 §7-17 첨부 보기 칸), 토큰·의존성은 그대로다</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-607 · R-9 문구 정렬 — ROADMAP 기준 1·3과 REQUIREMENTS EXP-07 · EXP-15(· EXP-09)를 사용자가 고른 경로로 맞춘다</name>
  <decision>REQUIREMENTS 문구를 어떤 경로로 바꿀지 고른다(ROADMAP은 두 선택지 모두 `/gsd-phase --edit 6`으로 바꾼다)</decision>
  <context>
D-607(잠금)은 「카드 사용 금액은 결제 합계로 적고 공급가는 서버가 역산한다 — 계획 단계에서 REQUIREMENTS EXP-07/EXP-15 · ROADMAP 기준 3 문구를 `gsd_run`으로 맞춘다」다. 교차 검토 R-9는 ROADMAP 기준 1의 「원천징수 차감 후 이체액 → `grossFromTotal()` 역산」이 UA-619(원천징수 역산 없음 — 차이만)와 어긋난다고 짚었다. CLAUDE.md §2는 `.planning/` 수동 편집을 금지하고, gsd 도구에는 요구사항 **본문** 편집 명령이 없다(`requirements` 하위 명령 = mark-complete · ready-ids · revert-phase). ROADMAP 성공 기준은 `/gsd-phase --edit 6`(`.claude/gsd-core/workflows/edit-phase.md` — diff 확인 뒤 교체)이 GSD 경로로 바꾼다.

제안 문구(실행자가 SUMMARY에 그대로 옮기고, 사용자가 고친 것이 있으면 그것을 쓴다):
- ROADMAP 기준 3의 「사용액은 같은 금액 모델(통화·환율·원화 환산액)로 적힌다」 → 「사용액은 전표의 결제 합계(부가세 포함)로 적고 공급가·부가세는 서버가 증빙 종류 규칙으로 역산하며(D-607), 같은 금액 모델(통화·환율·원화 환산액)로 적힌다. 구매 완료 때 경영관리가 적는 카드 금액도 결제 합계다」
- ROADMAP 기준 1의 「지급 완료액은 실제 이체 금액(부가세 포함·원천징수 차감 후)으로 적고 `domain/money.grossFromTotal()`(Phase 4)로 공급가액을 역산해 계산값과 다르면 차이를 표시한다」 → 「지급 완료액은 실제 이체 금액(부가세 포함·원천징수 차감 후)으로 적고, 서버가 다시 계산한 지급 총액과 다르면 차이와 사유를 남긴다. 부가세 규칙 지급은 `grossFromTotal()`(Phase 4)로 공급가액을 역산하며 세율은 증빙일 기준 이력 설정(`TAX_VAT_RATE`)이다. 원천징수 지급은 공급가액을 역산하지 않고 차이만 보인다(UA-619)」
- REQUIREMENTS EXP-07 끝에 「사용액은 전표의 결제 합계로 적고 공급가는 서버가 역산한다(D-607)」를 더한다
- REQUIREMENTS EXP-15의 「사람은 공급가액만 적는다」 → 「사람은 공급가액만 적는다(법인카드 사용·구매 완료는 예외 — 결제 합계를 적고 서버가 공급가를 역산한다, D-607)」
- REQUIREMENTS EXP-09의 「시스템이 공급가액으로 역산해 계산값과 다르면 차이를 표시한다」 → 「부가세 규칙은 시스템이 공급가액으로 역산하고, 계산값과 다르면 차이를 표시한다(원천징수는 차이만)」 — 기준 1과 같은 결이라 함께 제안한다
  </context>
  <options>
    <option id="user-edits">
      <name>⑴ REQUIREMENTS는 사용자가 직접 고친다</name>
      <pros>CLAUDE.md §2 금지를 그대로 지킨다. 실행자는 제안 문구와 줄 번호만 넘긴다</pros>
      <cons>사용자 손이 한 번 더 든다. 사용자가 고칠 때까지 Task 3 verify가 빨갛다</cons>
    </option>
    <option id="one-time-exception">
      <name>⑵ 이번 한 번만 실행자가 REQUIREMENTS 세 줄을 고치도록 예외를 승인한다</name>
      <pros>한 번에 끝난다. diff가 세 줄뿐이라 검토가 쉽다</pros>
      <cons>`.planning/` 수동 편집 금지의 예외다 — 승인 문장을 SUMMARY에 남겨야 한다</cons>
    </option>
  </options>
  <action>
선택을 받은 뒤: ① ROADMAP 기준 1·3은 `/gsd-phase --edit 6`(edit-phase 워크플로)으로 위 제안 문구를 diff 확인 뒤 바꾼다 — ROADMAP의 다른 줄은 건드리지 않는다. ② REQUIREMENTS는 고른 경로로 바꾼다(⑴이면 사용자가 고친 뒤 재개, ⑵면 EXP-07 · EXP-15 · EXP-09 세 줄만 고치고 승인 문장을 SUMMARY에 옮긴다). ③ SUMMARY에 바뀐 전·후 문구와 경로를 남긴다.
  </action>
  <verify>
    <automated>grep -c "결제 합계" .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
    <fails_when>두 파일 중 하나라도 0이다 — 기준 3 또는 EXP-07 · EXP-15 문구가 정렬되지 않았다</fails_when>
    <automated>grep -c "UA-619\|원천징수는 차이만\|역산하지 않고 차이만" .planning/ROADMAP.md</automated>
    <fails_when>0이다 — 기준 1(R-9) 문구가 정렬되지 않았다</fails_when>
  </verify>
  <resume-signal>`user-edits`(REQUIREMENTS를 고친 뒤) 또는 `one-time-exception`을 입력한다. 제안 문구를 고치고 싶으면 고친 문구를 함께 보낸다</resume-signal>
  <done>ROADMAP 기준 1·3과 REQUIREMENTS EXP-07 · EXP-15 · EXP-09가 D-607 · UA-619와 같은 뜻이고, 어느 경로로 바꿨는지 SUMMARY에 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 계획 문서 → 실행 | 요구사항 문구가 구현과 어긋나면 `/gsd-verify-work`가 잘못된 기준으로 판정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `.planning/` 수동 편집 | medium | mitigate | ROADMAP은 `/gsd-phase --edit 6`, REQUIREMENTS는 사용자 선택 경로로만 바꾸고 SUMMARY에 전·후를 남긴다(Task 3 checkpoint) |
| T-06-02 | Tampering | 디자인 토큰 무단 추가 | low | mitigate | merge-base 기준 `tokens.css` diff 0 검사(커밋 뒤에도 잡힌다) |
| T-06-03 | Tampering | 새 의존성 무단 추가 | low | mitigate | merge-base 기준 `package.json` · `pnpm-lock.yaml` diff 0 검사 |
</threat_model>

<verification>
- `test/unit/status-display.test.ts` · `test/unit/design-system-docs.test.ts` · `test/unit/docs-limits.test.ts` 녹색, `pnpm lint` · `pnpm typecheck` 통과
- DECISIONS SP-1~SP-7이 SYSTEM 개정보다 먼저 커밋됨(SP-7 = §7-17 첨부 보기 칸)
- merge-base 기준 `tokens.css` · `package.json` · `pnpm-lock.yaml` diff 0
- ROADMAP 기준 1·3 · REQUIREMENTS EXP-07 · EXP-15 · EXP-09 문구가 D-607 · UA-619와 같은 뜻
- 화면 코드가 없는 플랜이라 DOM 감사는 없다 — `status-display.ts`의 화면 반영은 각 화면 플랜의 CI=true DOM 감사가 본다
</verification>

<success_criteria>
- 이 페이즈의 상태 낱말이 한 파일에서만 나온다
- SP-1~SP-7이 결정 기록 → 디자인 시스템 순으로 선다
- 잠긴 결정 D-607과 UA-619가 요구사항·ROADMAP 문구와 어긋나지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 문서 절 | `DECISIONS.md` SP-1~SP-7 · `SYSTEM.md` §7-3 일괄 처리 표(rev 9 보강 — 선택 뜻 · `Ctrl+Enter` · 처리 뒤 선택 · 알림·포커스) · §7-5 낱말 · §6-3 제출 뒤 문서 화면 · §6-1 하위 목록 · §8 규칙 7 용어 · §7-10 마지막 증빙 삭제 · §7-17 첨부 보기 칸(SP-7 — 문서만, 슬롯 구현은 06-17) |
| 코드 | `app/(app)/status-display.ts`(`statusDisplay`) |
| 계획 문서 | ROADMAP Phase 6 기준 1·3 · REQUIREMENTS EXP-07 · EXP-15 · EXP-09(사용자 선택 경로) |
| 테스트 | `test/unit/status-display.test.ts` |

## Review Dispositions Ledger

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 1 · 2)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 반영 지시 2 — 06-01이 SP-1~SP-6만 옮긴다(UI-SPEC rev 9에 SP-7 신설) | MEDIUM(고침에서 파생) | Plan 01 전체: must_haves.truths 「SP-1~SP-7 일곱 항목」 · artifacts DECISIONS · key_links pattern `SP-[1-7]` · objective · Output · Task 2 name · action · verify · acceptance · done · verification · success_criteria · Artifacts 표 |
| DR-4 + C-1 — 제자리 증빙 확인 모달에 파일을 볼 자리가 없다 → SP-7 「첨부 보기 칸」 | HIGH(고침, 병합) | Plan 01, Task 2 action: SP-7을 DECISIONS에 먼저, SYSTEM §7-17 슬롯 목록에 다음(0~1 · 부제 아래 · §7-8 첨부 행 모양 · 보기만 · 셋 넘으면 칸 안 스크롤 · 확인 대상이 파일일 때만). verify `grep -c "첨부 보기 칸" docs/design/SYSTEM.md` + fails_when · truths 「SYSTEM.md §7-17에 「첨부 보기 칸」」 줄. 슬롯 구현은 06-17(이 플랜은 문서만) |
| DR-7 — 행 선택 뜻에 `aria-selected`가 섞인다 | MEDIUM(고침) | Plan 01, Task 2 action SP-1 보강 첫 문장(선택 뜻 = 체크박스 `checked` 하나, `aria-selected`는 §10 활성 셀) · read_first §10 · acceptance §7-3 절 안 `aria-selected` 문장 · truths SP-1 보강 줄 |
| DR-8 — 편집 표 `Ctrl+Enter`가 §7-3(새 줄)과 S1(화면 1차)에서 뜻이 다르다 | LOW(참고) | Plan 01, Task 2 action SP-1 보강 둘째 문장(`selectable` 표에는 새 줄 없음, 표 안 `Ctrl+Enter` = 화면 1차) · acceptance · truths SP-1 보강 줄 |
| DR-6 + H-3 — 일괄 결과 알림이 스크린 리더에 닿지 않고 막힌 행 선택이 충돌한다 | HIGH(고침, 병합) | Plan 01, Task 2 action SP-1 보강 셋째 · 넷째 문장(처리 뒤 선택 = 서버가 다시 보낸 「지금 고를 수 있음」 · polite `aria-live` · `aria-describedby` · 모달 닫힌 뒤 포커스)을 시스템 규칙으로 옮긴다. 화면 구현 · E2E는 06-15 · 06-17 |
| DR-9 — UA-601 재확인 grep이 거짓 통과한다(`7-15` · `7-17`이 본문 참조에도 걸린다) | LOW(참고) | 「선행 의존(A-6xx)」 UA-601 줄: `grep -c "^### 7-1[57]\." docs/design/SYSTEM.md` 2 미만이면 멈춤(절 머리만 센다, 2026-09-25 main 0 · Phase 4 브랜치 2) · Task 1 ⓪ 문장을 「그 줄의 멈춤 조건」으로 |
| (줄 번호 표류) Task 1 · Task 2 read_first의 UI-SPEC 「854-906행」이 rev 9에서 틀어졌다 | LOW | 두 read_first를 절 머리(`## 시스템 변경 제안`)로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 06-01에 닿는 항목은 모두 반영했다. SP-7 슬롯의 `ui/confirm-dialog` 구현과 S1 · S3 제자리 확인 화면은 06-17 · 06-20 소관이고, UI-SPEC에만 닿는 항목(S21 · S22 · 스토리보드 · Copywriting · O-6 · O-21 · O-22 · M-8 버림 등)은 UI-SPEC `## GSTACK REVIEW REPORT`에 적었다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md` when done
</output>
