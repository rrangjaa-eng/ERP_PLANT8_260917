---
phase: 06-payment-evidence-cards
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - domain/evidence-reviews/index.ts
  - domain/evidence-reviews/prepaid.ts
  - domain/evidence-reviews/cost-basis.ts
  - domain/evidence-reviews/upload-checks.ts
  - domain/settings/keys.ts
  - domain/expenses/index.ts
  - domain/expenses/evidence.ts
  - app/(app)/expenses/[id]/evidence-review-section.tsx
  - app/(app)/expenses/[id]/actions.ts
  - app/(app)/expenses/[id]/actions.registry.ts
  - app/(app)/expenses/expense-form.tsx
  - ui/attachments/Attachments.tsx
  - docs/OPERATIONS.md
  - test/unit/domain/evidence-prepaid.test.ts
  - test/unit/domain/evidence-cost-basis.test.ts
  - test/unit/domain/evidence-upload-checks.test.ts
  - test/integration/evidence.test.ts
  - test/integration/evidence-upload-intent.test.ts
  - test/e2e/evidence-waive-prepaid.spec.ts
autonomous: true
requirements: [EXP-13, EVID-02, EVID-03, EVID-04]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 권한자가 증빙 0 · 면제 아닌 결재 통과 문서에서 3차 「증빙 면제」를 누르고 사유를 적어 확인하면 증빙 값만 `면제`로 바뀌고, 지급 기록·선결제 표시·선결제 사유는 그대로이며, 사유·닫은 사람·시각이 행동 로그(`document_update`)와 확인 줄 2행에 남는다(D-603, AS2)"
    - "사유 없는 증빙 면제는 서버가 거부한다 — 확인 모달의 사유 근거 칸이 비면 1차가 비활성이고, 액션을 직접 불러도 거부된다"
    - "증빙 면제 3차는 결재 통과 · 증빙 0 · 면제 아님이면 지급 전후·선결제 여부·증빙 필수 설정과 관계없이 선다(P3 · P4 선결제/필수 off 없음 · P6) — 3차라 행의 1차는 그대로다"
    - "PM이 증빙을 더하거나 떼거나 증빙 금액을 고치면 `확인됨` → `확인 전`, `면제` → `확인 전`으로 풀리고 이전 확인·면제의 행동 로그는 남는다(S4 partial)"
    - "지급 뒤 들어온 선결제 증빙은 P5 1차 `증빙 확인`만 세우고 지급 칸·지급 행동은 다시 서지 않으며, 지급 뒤 S4에는 서버 계산 한 줄(지급 총액)이 없다"
    - "PM이 지출결의 폼에서 선결제를 켜면 `선결제 사유` 480 칸과 힌트 `증빙 기한 지급일부터 14일`(숫자 = 설정 값)이 서고, 끄면 칸이 사라지며 적은 사유는 버려진다(S6 empty)"
    - "선결제 on · 사유 빔이면 제출이 「막힘 — 선결제 칸」으로 막히고, 선결제면 Phase 5의 「증빙 없음」 제출 막힘이 풀린다 — 제출 뒤에는 읽기 줄 `선결제` · `{사유 원문}`만 있고 켜고 끄는 경로가 없다(O-4)"
    - "선결제 증빙 기한 = 지급일 + `evidence.prepaid_due_days`(기본 14)이고, 기한이 지나도 지급·완료 처리는 막히지 않는다 — 기한 초과는 표시(`증빙 N일 경과`)뿐이고 알림은 Phase 7이다(EXP-13, O-5)"
    - "면제된 선결제 문서에는 기한 2행(`증빙 기한 …` · `증빙 N일 경과`)이 어디에도 없다 — `면제`가 `선결제`를 이긴다"
    - "증빙 금액(공급가)이 있으면 그 문서의 비용 기준은 `확정` · 증빙 금액(원화 환산액)이고, 증빙을 모두 떼면 `예상` · 승인액(없으면 견적 실행가)으로 돌아간다 — 확인 여부는 이 판정에 들어가지 않는다(EVID-03 · EVID-04 · D-602)"
    - "업로드 의도 요청에서 서버가 크기(`evidence.max_size_mb`, 기본 10MB) · 형식(이미지 · PDF) · 중복(SHA-256)을 검사해 걸리면 서명 URL을 내주지 않는다(기준 2)"
    - "중복 파일의 기존 문서 번호는 올린 사람이 그 문서를 읽을 수 있을 때만 보이고, 읽을 수 없으면 번호 없이 문구만 선다(O-6 — `scopeFor`로 판정)"
    - "업로드 의도만 있고 완료 통보가 없으면 파일 행이 생기지 않고, 증빙 수·비용 기준·지급 게이트 판정이 바뀌지 않는다(기준 5 · F8)"
    - "완료 통보 없는 고아 객체를 찾아 지우는 절차가 `docs/OPERATIONS.md`에 있고 문서가 300줄 상한 안이다(F8)"
    - "S7 empty: 첨부 영역 한 줄 `이미지·PDF 10MB`의 숫자가 설정 값이다"
    - "S7 loading: 업로드 행 아래 2px 진행 바(§7-10)가 서고, 폰 사진은 최대 변 2000px JPEG로 줄인 뒤 올린다 — PDF는 원본이다"
    - "S7 error: 「Error — 증빙 업로드」 넷(크기 · 형식 · 중복 · 완료 통보 실패)이 파일 행 자리에 선다"
    - "S7 populated: 파일 행이 §7-10(이름 · 크기 · 날짜 · 크게 보기)대로 선다"
    - "S7 partial: 여러 파일 중 일부만 실패하면 성공 행과 실패 행이 각각 선다(§7-7 첨부 PARTIAL)"
    - "S7 zero-one-many: 문서의 마지막 증빙 삭제는 확인 모달(「Destructive — 증빙 삭제」, 결과 줄 = 증빙 금액 지워짐 · 증빙 확인 풀림)을 열고, 마지막이 아닌 파일은 모달 없이 지워지며, 카드 사용(`card_usage`) 첨부는 마지막이어도 모달이 없다(SP-6)"
    - "S4 empty: P3 문서는 「Empty — 문서 화면 증빙」 + 같은 줄 3차 `증빙 면제`(지급 권한자), 1차 `지급 완료` 비활성 + `증빙 없음 · 담당 PM {이름}` / 면제 문서는 `면제` + 2행이고 1차가 산다 / 증빙 필수 off면 `없음`(`--muted`)이고 1차가 산다"
    - "S6 error: 「막힘 — 선결제 칸」 한 줄이 제출 버튼 옆에 선다"
    - statement: "S4 long-text: 면제 사유 원문이 2행에서 `keep-all` 줄바꿈되고 말줄임이 없으며 긴 파일명은 §7-10 파일 행 규칙을 따른다 — 200자 사유 · 80자 파일명으로 CI=true DOM 감사"
      verification: backstop
    - statement: "S6 loading: 제출 중 표시는 Phase 5 폼 규칙 그대로이고 이 플랜이 더하지 않는다 — Phase 5 폼 위에서 CI=true DOM 감사"
      verification: backstop
    - statement: "S6 long-text: 제출 뒤 읽기 줄의 선결제 사유 원문이 `keep-all`로 줄바꿈된다 — 200자 사유로 CI=true DOM 감사"
      verification: backstop
    - statement: "S7 overflow: 파일 10개 이상 문서에서 첨부 영역 행 목록이 §7-10 규칙대로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S7 long-text: 80자 한글 파일명이 파일 행에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "사유 없이, 또는 행동 로그 없이 증빙을 면제해서는 안 된다 — 면제는 미결 점검(D-611)을 닫는 길이라 흔적 없는 면제는 점검을 무력화한다"
      status: resolved
      verification: test
    - statement: "중복 검사 응답이 올린 사람이 읽을 수 없는 문서의 번호를 드러내서는 안 된다(O-6 — SHA-256 한 값으로 남의 문서 존재·번호를 캐는 경로)"
      status: resolved
      verification: test
    - statement: "선결제 증빙 기한 초과가 지급·완료 처리를 막아서는 안 된다(EXP-13 — 독촉만)"
      status: resolved
      verification: test
    - statement: "증빙 확인 여부가 비용 기준을 바꿔서는 안 된다 — 확인은 새 비용 단계가 아니다(D-602)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/evidence-reviews/prepaid.ts"
      provides: "선결제 증빙 기한·경과 일수 계산(O-5 — 지급일 기준)"
      exports: ["prepaidDueInfo"]
    - path: "domain/evidence-reviews/cost-basis.ts"
      provides: "문서 비용 기준(확정 · 증빙 금액 / 예상 · 승인액 · 실행가) 판정 — EVID-03·04"
      exports: ["expenseCostBasis"]
    - path: "domain/evidence-reviews/upload-checks.ts"
      provides: "업로드 의도 검사(크기·형식·중복) + 마지막 증빙 삭제 확인 판정"
      exports: ["checkEvidenceUpload", "lastEvidenceDeleteNeedsConfirm"]
    - path: "test/integration/evidence-upload-intent.test.ts"
      provides: "업로드 의도만 있고 완료 없는 경우의 불변식"
      min_lines: 30
  key_links:
    - from: "domain/expenses/evidence.ts"
      to: "domain/evidence-reviews/index.ts"
      via: "Phase 5 증빙 추가·삭제·금액 고침이 같은 트랜잭션에서 확인 풀림을 부른다"
      pattern: "invalidateEvidenceReview"
    - from: "domain/evidence-reviews/upload-checks.ts"
      to: "domain/permissions/scope-for.ts"
      via: "중복 문서 번호 노출을 읽기 범위로 가른다"
      pattern: "scopeFor"
    - from: "domain/rules/register.ts"
      to: "domain/evidence-reviews/index.ts"
      via: "payment.evidence-required가 면제·선결제를 같은 판정 함수로 읽는다"
      pattern: "resolveEvidenceStatus"
---

<objective>
증빙의 나머지 흐름을 06-01이 세운 증빙 확인부 위에 더한다 — 증빙 면제, 선결제 표시와 기한, 업로드 검사(크기·형식·중복), 마지막 증빙 삭제 확인, 확인 풀림, 그리고 「증빙 금액 = 확정 비용 / 떼면 예상으로」 판정.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 완료 전 점검(06-09)이 미결로 세는 「증빙 없는 문서(면제 제외, 선결제 포함)」의 모든 갈래에 닫는 길이 있어야 하고(면제), 비용 기준이 증빙 유무로만 갈려야 한다(EVID-03·04). 업로드 경로 자체(서명 URL·GCS)는 Phase 5 것이고(A-608), 이 플랜은 그 경로 앞에 검사를 세우고 뒤에 청소 절차를 적는다.

Output: 면제·선결제·비용 기준·업로드 검사 domain 4파일, Phase 5 증빙·제출 훅, S4·S6·S7 화면 보강, `docs/OPERATIONS.md` 고아 청소 절, 단위 3 · 통합 2 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-RESEARCH.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 실행 시작 때 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-02 착수하지 않음`을 출력하고 멈춘다.** 이름은 가칭이다 — 06-01 SUMMARY의 「가칭 → 실제」 표를 먼저 읽고 실제 이름을 쓴다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-607 | Phase 5 지출결의 domain — 제출 함수(가칭 `submitExpense`, 「증빙 없음」 제출 막힘 포함)와 증빙 추가·삭제·금액 고침 함수(가칭 `domain/expenses/evidence.ts`), 지출결의 폼(가칭 `app/(app)/expenses/expense-form.tsx`) | `git ls-files 'domain/expenses/*'` 비면 멈춤 · 제출 함수와 증빙 변경 함수를 grep으로 찾지 못하면 멈춤 |
| A-608 | Phase 5 업로드 경로 — 업로드 의도(서명 PUT URL 발급) 액션 · 완료 통보 액션 · 파일 표 · 첨부 영역 컴포넌트(가칭 `ui/attachments/Attachments.tsx`) | `git ls-files 'db/schema/*file*' 'ui/attach*'` 비면 멈춤 · 서명 URL 발급 함수를 grep(`signed\|putUrl\|uploadIntent`)으로 찾지 못하면 멈춤 |
| 06-01 | `expense_evidence_reviews` · `resolveEvidenceStatus` · 파일 해시 칸 · `evidence.max_size_mb`·`evidence.prepaid_due_days` 키 | `git ls-files domain/evidence-reviews/index.ts` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(면제·마지막 증빙 삭제 모달) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| UA-606 | 첨부 영역이 증빙 금액 칸 · 증빙일과 한 묶음이고, 제출 뒤에도 PM이 증빙을 더하고 뗄 수 있다 | A-608 재확인 때 컴포넌트 props를 읽어 확인, 다르면 SUMMARY에 적는다 |

**새 의존성 없음:** 브라우저 이미지 축소는 Canvas API, SHA-256은 Web Crypto(`crypto.subtle.digest`)로 한다. GCS·이미지 라이브러리는 Phase 5 소관이고(A-608) 이 플랜은 설치하지 않는다. Phase 5가 이미 축소·해시를 하면 그것을 쓴다. 만약 Canvas·Web Crypto로 안 되는 요구가 드러나면 **설치하지 말고** 멈춰 사용자 승인 체크포인트로 올린다.

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-4 | 선결제는 기안자가 제출 때만 켠다, 제출 뒤 변경 없음 | Task 2의 폼 칸 · 제출 검증 |
| O-5 | 증빙 기한 = 지급일부터 N일(설정 기본 14) | `domain/evidence-reviews/prepaid.ts` 한 함수 |
| O-6 | 중복(SHA-256)은 막는다 — 읽을 수 있으면 문서 번호, 아니면 문구만 | `domain/evidence-reviews/upload-checks.ts` 중복 갈래 |
| O-2 | (06-01 소관) 확인 = 게이트. 이 플랜의 확인 풀림은 O-2 답과 무관하게 같다 | 해당 없음 |
| AS2 | 면제 = `document_update` | `waiveEvidence`의 `recordAction` 인자 |

<probe_fallback>
coverage.json의 이 플랜 몫 네 건은 모두 `unclassified → unresolved`이며 **flagged 가정으로 남긴다**(자동 해소·기각 없음 — 전체 10건 표는 06-01 `<probe_fallback>`):

| 요구사항 | 프로브 결과 | 대신 쓰는 근거 |
|---|---|---|
| EXP-13 | unclassified → unresolved (flagged) | 기준 2 · O-4 · O-5 · D-611 |
| EVID-02 | unclassified → unresolved (flagged) | 기준 1 · D-603(면제·선결제 예외) |
| EVID-03 | unclassified → unresolved (flagged) | 기준 2 · D-602 |
| EVID-04 | unclassified → unresolved (flagged) | 기준 2 |

**추가 가정(표면화):** EVID-04의 「예상」 값은 「지출결의 승인액, 없으면 견적 실행가」로 읽었다(PNL-02 우선순위 — 증빙액 > 승인액 > 실행가). 프로젝트 합계 SQL(`effectiveCost`)은 Phase 9 소관이고 이 플랜은 문서 한 건의 판정 함수만 만든다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 증빙 면제 한 경로(P3 문서 → 3차 「증빙 면제」 → 사유 → P4 「지급 완료」 살아남)</name>
  <files>domain/evidence-reviews/index.ts, app/(app)/expenses/[id]/evidence-review-section.tsx, app/(app)/expenses/[id]/actions.ts, app/(app)/expenses/[id]/actions.registry.ts, test/integration/evidence.test.ts, test/e2e/evidence-waive-prepaid.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 06-01이 끝났다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 358-383행(「증빙 면제의 자리」) · 945행(S4 empty) · Copywriting 「Destructive — 증빙 면제」 · 「표시 — 증빙 면제 결과」
    - .planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md(가칭 → 실제 표)
    - domain/evidence-reviews/index.ts · domain/payments/action-row.ts(06-01 — 3차 `증빙 면제` 자리)
    - ui/confirm-dialog/ConfirmDialog.tsx(슬롯 이름)
  </read_first>
  <behavior>
    - `waiveEvidence`: 지급 권한 없음 → ForbiddenError / 사유 빈 문자열 → 거부 / 증빙 1개 이상 → 거부 / 결재 통과 전 → 거부 / 정상 → 확인 기록 status `waived` + 사유 + 행동 로그 `document_update`
    - 면제 뒤 `resolveEvidenceStatus` = `면제`, `payment.evidence-required` 통과, 지급 기록(있으면) 불변
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 표의 재확인을 돌린다. 없으면 멈춘다(메시지 형식은 표 위 문장). 있으면 실제 이름을 SUMMARY에 적는다.

`domain/evidence-reviews/index.ts`에 `waiveEvidence(viewer, { expenseId, reason, version })`를 더한다: `expenses.payments` write 필수(D-601) · 결재 통과 · 증빙 0 · 면제 아님일 때만 · 사유 필수 · 증빙 값만 `면제`로(지급 기록·선결제 표시·선결제 사유는 건드리지 않는다) · `recordAction` `document_update`에 사유(AS2). 06-01의 `resolveExpenseActionRow`가 내보내는 3차 `증빙 면제` 자리(P3 · P4의 선결제/필수 off `없음` · P6)에 이 행동을 붙인다 — 자리 판정을 다시 쓰지 않는다. `evidence-review-section.tsx`에서 3차를 누르면 `ConfirmDialog`(Copywriting 「Destructive — 증빙 면제」 — 결과 줄만 지급 전후·선결제로 갈린다, 사유 근거 칸 필수), 면제 뒤 확인 줄 `면제` + 2행 `{사유} · {사람} {MM-DD HH:mm}`(`keep-all`). S4 empty 세 갈래(P3 Empty + 3차 · 면제 + 1차 산다 · 필수 off `없음` muted + 1차 산다)를 이 섹션에서 맞춘다. `waiveEvidenceAction`을 06-01의 `app/(app)/expenses/[id]/actions.ts`·registry에 더한다(새 registry 파일이 아니므로 누수 스캔 import 변경 없음).

`test/integration/evidence.test.ts`에 behavior 케이스를, `test/e2e/evidence-waive-prepaid.spec.ts`에 「P3 문서 → 증빙 면제 → 1차 지급 완료가 살아난다 → 지급 완료」 한 경로를 쓴다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-waive-prepaid.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - 통합 테스트가 behavior 두 줄의 모든 갈래(권한 · 사유 · 증빙 있음 · 결재 전 · 정상 · 지급 기록 불변)를 개별 `it`으로 단언한다
    - E2E 한 경로가 CI=true로 녹색이다
  </acceptance_criteria>
  <done>P3 문서가 사유 있는 면제로 P4가 되어 지급 완료까지 가는 한 경로가 domain → 액션 → 화면 → 행동 로그를 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 선결제(S6) · 기한 · 확인 풀림 훅 · 비용 기준(EVID-03·04)</name>
  <files>domain/evidence-reviews/index.ts, domain/evidence-reviews/prepaid.ts, domain/evidence-reviews/cost-basis.ts, domain/settings/keys.ts, domain/expenses/index.ts, domain/expenses/evidence.ts, app/(app)/expenses/expense-form.tsx, app/(app)/expenses/[id]/evidence-review-section.tsx, test/unit/domain/evidence-prepaid.test.ts, test/unit/domain/evidence-cost-basis.test.ts, test/integration/evidence.test.ts, test/e2e/evidence-waive-prepaid.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 945-962행(S4 partial · S6 전부) · Copywriting 「막힘 — 선결제 칸」
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-602 · D-603 · D-611
    - test/unit/settings/registry-coverage.test.ts 99-115행(readBy 만료 — 읽기 시작한 키의 표시를 지운다)
    - A-607 재확인에서 찾은 Phase 5 제출 함수 · 증빙 변경 함수 · 폼 파일
  </read_first>
  <behavior>
    - `prepaidDueInfo`: 지급 전 → null / 지급일 2026-09-05 · 14일 → 기한 2026-09-19 / 오늘 09-21 · 증빙 0 · 면제 아님 → 경과 2 / 면제 → null / 증빙 있음 → null
    - `expenseCostBasis`: 증빙 금액 있음 → { kind: 확정, 증빙 금액 } / 증빙 0 · 승인액 있음 → { kind: 예상, 승인액 } / 둘 다 없음 → { kind: 예상, 견적 실행가 } / 확인 기록 유무로 결과가 바뀌지 않음
    - 제출 검증: 선결제 on · 사유 빔 → 막힘 / 선결제 on · 사유 · 증빙 0 → 제출 성공(Phase 5 증빙 없음 막힘 풀림) / 제출 뒤 선결제 바꾸기 → 거부
    - 증빙 추가·삭제·금액 고침 → `확인됨`/`면제`가 `확인 전`으로(같은 트랜잭션), 이전 행동 로그 보존
  </behavior>
  <action>
`domain/evidence-reviews/prepaid.ts`에 `prepaidDueInfo(expense, payment, today, dueDays)`(O-5 — 지급일 기준, 면제·증빙 있음이면 null). 기한은 **표시 전용**이다 — 이 값을 게이트·지급·완료 경로 어디서도 막는 데 쓰지 않는다(EXP-13). `domain/evidence-reviews/cost-basis.ts`에 `expenseCostBasis`(PNL-02 우선순위의 문서 한 건 판). `domain/evidence-reviews/index.ts`에 `invalidateEvidenceReview(viewer, expenseId, tx)` — 확인 기록을 지우고(행동 로그는 남는다) `확인 전`으로 되돌린다.

Phase 5 훅(A-607 실제 이름): 증빙 추가·삭제·증빙 금액 고침 함수가 같은 트랜잭션에서 `invalidateEvidenceReview`를 부르게 한 줄씩 더한다. 제출 함수에 선결제 검증을 더한다 — 선결제 on이면 사유 필수, 그러면 Phase 5의 「증빙 없음」 제출 막힘을 풀고, 제출 뒤 선결제 칸 변경은 거부한다(O-4). 폼(`expense-form.tsx`, 실제 이름)에 선결제 체크박스 + 켜면 나타나는 `선결제 사유` 480 칸 + `Form.Hint` `증빙 기한 지급일부터 {N}일`(N = `evidence.prepaid_due_days` 값, 서버가 보냄), 끄면 칸이 사라지고 사유를 버린다. 막힘 문구는 「막힘 — 선결제 칸」 그대로. 문서 화면 확인 줄에 제출 뒤 읽기 줄 `선결제` · `{사유}`(`keep-all`)와 지급 뒤 기한 2행(`증빙 기한 {MM-DD}` / `증빙 {N}일 경과`, 면제면 없음)을 더한다. S4 partial의 「지급 뒤 들어온 선결제 증빙 → P5 `증빙 확인`만, 서버 계산 한 줄 없음」은 06-01 action-row 판정 그대로임을 E2E로 확인한다.

`domain/settings/keys.ts`에서 이 태스크가 읽기 시작한 `evidence.prepaid_due_days`의 `readBy` 표시를 지운다(Task 3이 `evidence.max_size_mb` 표시를 지운다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence-prepaid.test.ts test/unit/domain/evidence-cost-basis.test.ts test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다. registry-coverage가 빨가면 readBy 표시를 안 지웠거나 잘못 지웠다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-waive-prepaid.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 ⑴ 선결제 기한 초과 문서의 지급 완료 성공(막히지 않음) ⑵ 증빙 삭제 뒤 비용 기준 = 예상 · 승인액 ⑶ 확인 전후 비용 기준 동일 ⑷ 확인됨 문서에 증빙 추가 → 확인 전 + 이전 확인 로그 행 보존 ⑸ 제출 뒤 선결제 변경 거부를 단언한다
    - `grep -n "evidence.prepaid_due_days" -A 12 domain/settings/keys.ts | grep -c readBy`가 0
    - E2E에 「선결제 켜기 → 사유 빔 막힘 → 사유 적고 제출 → 읽기 줄 `선결제`」 케이스가 녹색이다
  </acceptance_criteria>
  <done>선결제가 사유와 함께만 켜지고 처리를 막지 않으며, PM의 증빙 변경이 확인을 풀고, 비용 기준이 증빙 유무로만 갈린다는 것이 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 업로드 검사(크기·형식·중복) · 마지막 증빙 삭제 확인(SP-6) · 의도만 있는 업로드 · 고아 청소 절차</name>
  <files>domain/evidence-reviews/upload-checks.ts, domain/settings/keys.ts, domain/expenses/evidence.ts, ui/attachments/Attachments.tsx, app/(app)/expenses/[id]/evidence-review-section.tsx, docs/OPERATIONS.md, test/unit/domain/evidence-upload-checks.test.ts, test/integration/evidence-upload-intent.test.ts, test/e2e/evidence-waive-prepaid.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 963-970행(S7) · 899-906행(SP-6) · Copywriting 「Error — 증빙 업로드」 · 「Destructive — 증빙 삭제」
    - ROADMAP Phase 6 기준 2(업로드 순서 · F8)
    - domain/permissions/scope-for.ts(읽기 범위 판정)
    - docs/OPERATIONS.md 전체 목차(263줄 — 300줄 상한, test/unit/docs-limits.test.ts)
    - A-608 재확인에서 찾은 Phase 5 업로드 의도 · 완료 통보 액션 · 첨부 컴포넌트
  </read_first>
  <behavior>
    - `checkEvidenceUpload`: 크기 > 한도 MB → 크기 막힘 / MIME이 이미지·`application/pdf` 밖 → 형식 막힘 / 같은 SHA-256 파일 있음 · 올린 사람이 그 문서를 읽을 수 있음 → 중복 막힘 + 문서 번호 / 읽을 수 없음 → 중복 막힘, 번호 없음 / 통과 → ok
    - `lastEvidenceDeleteNeedsConfirm`: 지출결의 · 남는 증빙 0 → true / 지출결의 · 남는 증빙 ≥1 → false / 카드 사용 첨부 → 항상 false
    - 업로드 의도 발급 뒤 완료 통보 없음 → 파일 행 0 증가, 증빙 수·비용 기준·지급 게이트 판정 불변
  </behavior>
  <action>
`domain/evidence-reviews/upload-checks.ts`에 `checkEvidenceUpload(viewer, { sizeBytes, mimeType, sha256, expenseId })`(한도는 `evidence.max_size_mb` 설정 — 이 키의 `readBy` 표시를 지운다) · `lastEvidenceDeleteNeedsConfirm({ ownerKind, remainingCount })`. 중복 문서 번호는 `scopeFor`로 올린 사람의 읽기 범위를 확인했을 때만 응답에 싣는다(O-6). Phase 5 업로드 의도 액션(A-608 실제 이름)의 서명 URL 발급 **앞**에서 이 검사를 부르고, 걸리면 서명 URL을 발급하지 않는다. 서버는 완료 통보 때 GCS 메타데이터를 다시 확인하는 Phase 5 동작을 그대로 둔다(이 플랜은 고치지 않는다).

첨부 컴포넌트(A-608 실제 이름): Phase 5가 브라우저 축소(최대 변 2000px JPEG, PDF 원본)·SHA-256 계산을 이미 하면 그대로 쓰고, 없으면 Canvas API와 `crypto.subtle.digest("SHA-256", …)`로 더한다(새 의존성 없음). 오류 넷(크기 · 형식 · 중복 · 완료 통보 실패)은 Copywriting 「Error — 증빙 업로드」 그대로 파일 행 자리에, 여러 파일 중 일부 실패는 성공·실패 행 각각(§7-7 PARTIAL). 첨부 영역 한 줄 `이미지·PDF {N}MB`의 N은 설정 값. 지출결의 문서의 마지막 증빙 삭제는 `lastEvidenceDeleteNeedsConfirm`이 true일 때만 `ConfirmDialog`(「Destructive — 증빙 삭제」, 결과 줄 = 증빙 금액 지워짐 · 증빙 확인 풀림)를 열고, 삭제는 Phase 5 삭제 함수(Task 2 훅이 확인을 푼다)로 한다.

`docs/OPERATIONS.md`에 「증빙 업로드 고아 객체 청소(F8)」 절을 15줄 안으로 더한다: 무엇이 고아인가(서명 URL 발급 기록은 있고 파일 행이 없는 GCS 객체) · 찾는 방법(GCS 목록과 파일 표의 객체 키 대조 — 명령은 `gcloud storage ls`로, 버킷 이름은 시크릿 목록 절을 가리킨다) · 지우는 기준(발급 뒤 24시간 넘은 것) · 누가 언제(월 1회, 운영자). 전체가 300줄을 넘지 않게 한다.

`test/integration/evidence-upload-intent.test.ts`: 의도 발급 → 완료 통보 없이 끝 → 파일 행 수 · 증빙 수 · `expenseCostBasis` · `payment.evidence-required` 판정이 발급 전과 같다. 크기·형식·중복(읽기 가능/불가 두 계정) 거부도 여기서 단언한다. E2E에 이미지 업로드 한 번 · 마지막 증빙 삭제 모달 케이스를 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence-upload-checks.test.ts test/unit/docs-limits.test.ts test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다. docs-limits가 빨가면 OPERATIONS.md가 300줄을 넘었다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence-upload-intent.test.ts test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-waive-prepaid.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; git diff --stat HEAD -- package.json pnpm-lock.yaml</automated>
    <fails_when>lint·typecheck가 실패하거나 마지막 diff 출력이 비어 있지 않다(새 의존성)</fails_when>
  </verify>
  <acceptance_criteria>
    - 중복 검사 통합 테스트가 읽기 권한 없는 계정의 응답 본문에 기존 문서 번호 문자열이 없음을 단언한다
    - `grep -c "고아" docs/OPERATIONS.md`가 1 이상이고 `wc -l docs/OPERATIONS.md`가 300 이하
    - `grep -n "evidence.max_size_mb" -A 12 domain/settings/keys.ts | grep -c readBy`가 0
    - package.json·pnpm-lock.yaml diff 0
  </acceptance_criteria>
  <done>서명 URL 발급 전에 크기·형식·중복이 걸러지고, 중복 번호가 읽기 범위 밖에 새지 않으며, 의도만 있는 업로드가 아무 값도 바꾸지 않고, 청소 절차가 운영 문서에 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 업로드 의도 액션 | 크기·MIME·SHA-256은 브라우저가 보낸 값이다 — 발급 전 검사의 입력일 뿐이고, 완료 통보 때 서버가 GCS 메타데이터로 다시 확인한다(Phase 5) |
| 브라우저 → GCS | 서명 PUT URL의 크기 상한 조건이 한도를 강제한다(Phase 5) |
| 브라우저 → 면제 액션 | 사유·version이 넘어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Elevation of Privilege | `waiveEvidence` | high | mitigate | domain 첫 줄 `can(viewer, "expenses.payments", "write")` · 결재 통과 · 증빙 0 조건을 서버가 판정. 권한 없는 계정 거부 통합 테스트 |
| T-06-09 | Repudiation | 증빙 면제 | medium | mitigate | 사유 필수 + `document_update` 행동 로그(사람·시각·사유). 면제가 풀려도 로그는 남는다 |
| T-06-10 | Information Disclosure | 중복 검사 응답 | high | mitigate | 기존 문서 번호를 `scopeFor` 읽기 범위 확인 뒤에만 싣는다(O-6). 범위 밖 계정 응답에 번호 문자열 없음을 통합 테스트로 단언 |
| T-06-11 | Tampering | 업로드 크기·형식 | medium | mitigate | 브라우저 값으로 발급을 거르고, 실제 크기는 서명 URL 조건 + 완료 통보 때 GCS 메타데이터 재확인(Phase 5)으로 강제. 이 플랜은 발급 전 검사를 우회하는 경로가 없음을 테스트로 고정 |
| T-06-12 | Denial of Service | 고아 객체 누적 | low | mitigate | `docs/OPERATIONS.md` 월 1회 청소 절차(F8) |
| T-06-13 | Tampering | 선결제 칸 사후 변경 | medium | mitigate | 제출 뒤 선결제 변경을 domain이 거부(O-4). 통합 테스트 |
</threat_model>

<verification>
- 단위 3 · 통합 2 · E2E 1 녹색, `test/unit/settings/registry-coverage.test.ts` · `test/unit/docs-limits.test.ts` 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S4·S6·S7 DOM 실측 감사(backstop 5건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 미결 점검이 세는 「증빙 없는 문서」의 모든 갈래에 사유 있는 면제로 닫는 길이 있다
- 선결제가 사유와 함께만 켜지고 기한이 처리를 막지 않는다
- 비용 기준이 증빙 유무로만 갈린다(확인 여부 무관)
- 업로드 검사가 서명 URL 발급 전에 서고, 의도만 있는 업로드가 아무것도 바꾸지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `waiveEvidence` · `invalidateEvidenceReview` · `prepaidDueInfo` · `expenseCostBasis` · `checkEvidenceUpload` · `lastEvidenceDeleteNeedsConfirm` |
| Server Action | `waiveEvidenceAction` |
| 변경된 기존 심볼 | Phase 5 제출 함수(선결제 검증) · Phase 5 증빙 변경 함수(확인 풀림 훅) · Phase 5 업로드 의도 액션(발급 전 검사) · Phase 5 첨부 컴포넌트(오류 넷 · 축소 · 해시) · `evidence.max_size_mb`·`evidence.prepaid_due_days`의 readBy 표시 제거 |
| 문서 절 | `docs/OPERATIONS.md` 「증빙 업로드 고아 객체 청소(F8)」 |
| 테스트 | `test/unit/domain/{evidence-prepaid,evidence-cost-basis,evidence-upload-checks}.test.ts` · `test/integration/{evidence,evidence-upload-intent}.test.ts` · `test/e2e/evidence-waive-prepaid.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md` when done
</output>
