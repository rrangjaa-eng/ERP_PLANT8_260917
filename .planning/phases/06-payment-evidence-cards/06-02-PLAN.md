---
phase: 06-payment-evidence-cards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - domain/settings/keys.ts
  - domain/permissions/menus.ts
  - domain/money/index.ts
  - domain/document-numbering/index.ts
  - domain/quotes/line-door.ts
  - test/unit/domain/money-sum-diff.test.ts
  - test/unit/domain/document-number-format.test.ts
  - test/unit/domain/line-door.test.ts
  - test/unit/permissions/can.test.ts
  - test/integration/document-numbering.test.ts
  - test/e2e/phase6-keys.spec.ts
autonomous: true
requirements: [EVID-02, EXP-09, EXP-10, EXP-13, EXP-16]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "새 설정 키 넷(`evidence.required` 켬 · `evidence.max_size_mb` 10 · `evidence.prepaid_due_days` 14 · `purchase.online_vendor_name` 빈 값)이 `/admin/settings`에 화면 코드 없이 나온다(UA-614) — 넷 모두 이 플랜에서는 읽는 곳이 없어 `readBy: { phase: \"6\" }`을 달고, 처음 읽는 플랜이 지운다(06-04 · 06-10 · 06-11 · 06-08)"
    - "새 메뉴 키 셋(`expenses.payments` 지급 처리 · `cards.purchases` 구매 처리 · `cards.proxy` 카드 대리 등록)이 권한표에 서고, `expenses` write만 가진 역할의 `can(viewer, \"expenses.payments\", \"write\")`는 거짓이다 — 다른 메뉴 권한으로 대신 통과되지 않는다(D-601 — 대행 없음)"
    - "`sumKrw(values)` · `diffKrw(a, b)`가 `domain/money`에 있고(원 정수, 부호 유지, 빈 배열 합 0), 이 페이즈의 지급 차이·카드 부가세·구매 차이·고른 건 합·점검 합이 전부 이 둘을 쓴다(D-605)"
    - "구매 요청 번호 서식이 `{프로젝트 번호}{구분자}{접두어}{순번}`(기본 `26001-C0001`)이고 `document_number.purchase_request.*` 네 키(접두어 `C` · 순번 4자리 · 구분자 `-` · 시작 1)로 바뀐다 — 자릿수 넘침은 자르지 않고, 기존 프로젝트 번호 서식 테스트는 그대로 녹색이다(A-612)"
    - "구매 요청 번호 서식은 호출자가 트랜잭션을 열기 **전에** `loadDocumentNumberFormat(\"purchase_request\")`로 읽어 `format` 인자로 넘기고, `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)`는 설정을 읽지 않는다 — 넘긴 서식이 설정 값과 달라도 번호는 넘긴 서식을 따른다(CROSS E-2, PR #75 꼴 — PR #75 머지에 기대지 않는다)"
    - "「06-03 tx 규약」을 따른다 — `allocateScopedDocumentNumber`는 트랜잭션 안에서 `tx`를 받는 카운터 리포지토리(`allocateNumber`)와 순수 서식 함수만 부른다"
    - "견적 줄의 문(구매 요청 / 지출결의)은 `domain/quotes/line-door.ts`의 `resolveLineDoor` 한 함수가 정한다 — 설정 문자열과 거래처 이름을 앞뒤 공백 제거 · 유니코드 NFC 정규화 뒤 정확히 비교하고, 설정이 비었거나 거래처가 없거나 맞지 않으면 지출결의다(O-13 · 계획 판단 · D-611의 사람이 고르지 않는 문)"
    - "S20 empty: 설정 화면이 기본값 표 그대로(증빙 필수 켬 · 크기 한도 10 · 선결제 기한 14 · 온라인구매 협력사 빈 값) 선다"
    - "S20 loading: 새 키 넷의 저장은 §7-2 즉시 저장 흐름 그대로이고 이 플랜은 설정 화면 코드를 고치지 않는다(UA-614)"
    - statement: "S20 error: 범위 밖 숫자(0 · 음수)와 저장 실패가 §7-2 시스템 규칙대로 표시된다 — 잘못된 값을 넣어 CI=true DOM 감사"
      verification: backstop
    - statement: "S20 long-text: 긴 상호명이 480 입력 칸에서 줄바꿈 없이 스크롤된다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "새 메뉴 키 셋이 기존 메뉴(`expenses` · `cards`)의 권한으로 대신 통과되게 해서는 안 된다(D-601)"
      status: resolved
      verification: test
    - statement: "번호 서식이 순번을 자릿수에 맞춰 잘라서는 안 된다 — 잘리면 번호 유일성이 깨진다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/settings/keys.ts"
      provides: "설정 키 넷 + 구매 요청 번호 서식 키 넷"
      exports: ["EVIDENCE_REQUIRED", "EVIDENCE_MAX_SIZE_MB", "EVIDENCE_PREPAID_DUE_DAYS", "PURCHASE_ONLINE_VENDOR_NAME"]
    - path: "domain/quotes/line-door.ts"
      provides: "[O-13] 견적 줄의 문 판정 한 함수"
      exports: ["resolveLineDoor"]
    - path: "domain/money/index.ts"
      provides: "원 정수 합·차이"
      exports: ["sumKrw", "diffKrw"]
    - path: "domain/document-numbering/index.ts"
      provides: "범위(프로젝트 번호) 기간 서식·부여 함수 + 트랜잭션 밖 서식 조회(export)"
      exports: ["scopedDocumentNumberFormat", "allocateScopedDocumentNumber", "loadDocumentNumberFormat"]
    - path: "test/e2e/phase6-keys.spec.ts"
      provides: "설정 화면 네 키 · 권한표 세 메뉴가 실제로 보이는지"
      min_lines: 20
  key_links:
    - from: "domain/document-numbering/index.ts"
      to: "domain/settings/keys.ts"
      via: "`purchase_request` 서식 표 줄이 네 키를 읽는다 — readBy 없이 참조된다"
      pattern: "purchase_request"
    - from: "domain/permissions/menus.ts"
      to: "domain/permissions/can.ts"
      via: "메뉴 키 정확 일치로만 권한을 찾는다"
      pattern: "expenses.payments"
---

<objective>
이 페이즈의 뒤 플랜들이 공유하는 **작은 공용 조각**을 먼저 세운다 — 설정 키 넷 · 메뉴 키 셋 · 원 정수 합·차이 · 구매 요청 번호 서식 · 견적 줄 문 판정. 트레이서는 「설정 키와 메뉴 키가 관리자 화면에 실제로 보인다」 한 경로다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 옛 06-01(35파일)이 트레이서 안에 이 조각들을 다 품어 예산을 넘겼다(체커 BLOCKER). 이 조각들은 서로 파일이 겹치지 않고 화면 경로가 없어 한 플랜으로 먼저 세우면, 지급·카드·구매 요청 트레이서가 각자 좁게 선다. 이 플랜의 키 · 메뉴 · 함수는 읽는 쪽이 생길 때 그 플랜이 readBy를 지운다.

Output: 설정 키 넷(readBy 6) + 번호 서식 키 넷, 메뉴 키 셋, `sumKrw` · `diffKrw`, `scopedDocumentNumberFormat` · `allocateScopedDocumentNumber`(서식은 인자) · `loadDocumentNumberFormat` export, `resolveLineDoor`, 단위 4 · 통합 케이스 · E2E 1.

**규모 근거:** `files_modified` 11개(코드 5 · 테스트 6), `estimate.tokens` 70000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다. 엔지니어링 검토(CROSS E-2) 반영으로 통합 테스트 파일 하나(기존 파일 확장)가 늘었다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪ · Task 3 ⓪이 실행 시작 때 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-02 착수하지 않음`을 출력하고 멈춘다.** 아래 이름은 가칭이다 — 실제 이름이 다르면 실제 이름을 쓰고 SUMMARY에 대응표를 남긴다.

| id | 먼저 들어와야 하는 것 | 재확인(⓪) |
|---|---|---|
| A-612 | 선행 없음 — 이 플랜이 `document_number.purchase_request.*` 키와 서식을 처음 등록한다 | Task 3 ⓪ 확인만: `grep -rn "purchase_request" domain/ repositories/` 0건이어야 정상(있으면 그 이름을 쓰고 SUMMARY에 적는다) |
| A-607 | Phase 5 지출결의 번호(`{프로젝트 번호}-{순번}`)의 프로젝트 단위 카운터 경로와 **팀 비용 지출결의의 번호 규칙** | Task 3 ⓪: `grep -n "period\|scope" domain/document-numbering/index.ts`로 Phase 5 경로 유무를 SUMMARY에 적는다. 팀 비용 지출결의 번호 규칙을 Phase 5 코드 · `05-CONTEXT.md`에서 찾지 못하면 멈추고 아래 「번호 공백」을 묻는다 |

**번호 공백(계획이 스스로 정하지 않는다):** UI-SPEC은 프로젝트에 이은 요청의 번호(`26001-C0001`)만 정했다. 팀 비용 요청(프로젝트 없음)의 번호는 정본이 없다. 기본 동작: Phase 5의 팀 비용 지출결의 번호 규칙을 그대로 따른다(A-607). 없으면 Task 3 ⓪에서 멈추고 사용자 답을 받는다 — ⑴ `{팀 코드}-C{순번}`(기간 = 팀) ⑵ `{연도 2자리}T-C{순번}`(기간 = 연도, 팀 비용 공용) ⑶ 팀 비용 요청은 번호 없이 id만. 이 멈춤은 Task 3만 세운다 — Task 1 · 2는 영향이 없어 먼저 끝낸다.

**R-7 메모:** 전부 가칭 위에 선다 — 06-01 objective의 R-7 메모와 같다.

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-13 | 설정은 문자열 한 칸, 서버가 거래처 이름과 맞춘다(공백 제거 · NFC · 정확 비교, 빈 설정 = 모두 지출결의) | `domain/quotes/line-door.ts`의 `resolveLineDoor` 한 함수 |
| UA-614 | 새 키는 화면 코드 없이 §7-2 설정 화면에 나온다 | 해당 없음 |
| UA-612 | 메뉴 키 라벨 = `지급 처리` · `구매 처리` · `카드 대리 등록`(역할 이름을 코드에 박지 않는다) | `domain/permissions/menus.ts` 세 줄 |

<assumption_delta_decision>
`gsd_run query assumption-delta`는 `detected: false`. 이 플랜은 키·함수를 더할 뿐 1차 명사를 옮기지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EVID-02 · EXP-09 · EXP-10 · EXP-13 · EXP-16 — 전부 `unclassified → unresolved (flagged, 자동 해소·기각 없음)`. 전체 10건 표는 06-01 `<probe_fallback>`. 이 플랜은 그 요구사항들의 **공용 조각**만 세우고 수용 기준 판정은 각 소비 플랜이 한다.

**표면화하는 가정:** 팀 비용 구매 요청의 번호 규칙이 정본에 없다(위 「번호 공백」) — Phase 5 규칙 재사용, 없으면 멈춤.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 설정 키 넷과 메뉴 키 셋이 정의 → 등록 → 관리자 설정 화면 · 권한표까지 실제로 보인다</name>
  <files>domain/settings/keys.ts, domain/permissions/menus.ts, test/unit/permissions/can.test.ts, test/e2e/phase6-keys.spec.ts</files>
  <precondition>없음 — 기존 설정 화면(§7-2)과 권한표 화면이 main에 있다(Phase 3).</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S20. 설정 키 — 관리자 설정 화면」 절(설정 키 표 — 라벨 · 힌트 · namespace · 기본값)
    - domain/settings/keys.ts 190-230행(readBy 표시 키 모양) · `SETTING_DEFS` 배열 위치
    - test/unit/settings/registry-coverage.test.ts(readBy 만료 규칙 — 읽는 곳이 생기면 표시를 지워야 녹색)
    - domain/permissions/menus.ts · domain/permissions/can.ts(메뉴 키 정확 일치 조회)
    - test/unit/permissions/can.test.ts(기존 `deps.findPermission` 대역 모양)
    - test/e2e/settings.spec.ts · test/e2e/permissions-grid.spec.ts(E2E 선례)
  </read_first>
  <behavior>
    - `can(viewer, "expenses.payments", "write")`: 역할에 `expenses` write만 있으면 false, `expenses.payments` write가 있으면 true(`deps.findPermission` 대역)
    - `cards.purchases` · `cards.proxy`도 `cards` write로 대신 통과하지 않는다
    - E2E: 관리자가 `/admin/settings`에서 `증빙 필수`(켬) · `증빙 크기 한도`(10) · `선결제 증빙 기한`(14) · `온라인구매 협력사`(빈 값)를 본다 · 권한표에서 `지급 처리` · `구매 처리` · `카드 대리 등록` 세 줄을 본다
  </behavior>
  <action>
⓪ 없음(선행 의존이 Task 3에만 있다).

① `domain/settings/keys.ts`에 네 키를 정의하고 `SETTING_DEFS`에 넣는다: `EVIDENCE_REQUIRED`(`evidence.required`, boolean, 기본 true, namespace `증빙`, 라벨 `증빙 필수`, 힌트 `끄면 증빙 없이 지급 완료`) · `EVIDENCE_MAX_SIZE_MB`(`evidence.max_size_mb`, 양의 정수, 10, 라벨 `증빙 크기 한도`) · `EVIDENCE_PREPAID_DUE_DAYS`(`evidence.prepaid_due_days`, 양의 정수, 14, 라벨 `선결제 증빙 기한`, 힌트 `지급일부터 센 날수`) · `PURCHASE_ONLINE_VENDOR_NAME`(`purchase.online_vendor_name`, 문자열, 빈 값, namespace `구매 요청`, 라벨 `온라인구매 협력사`, 힌트 `이 거래처의 견적 줄은 구매 요청으로`). 이 플랜에서 네 키 모두 읽는 곳이 없으므로 넷 다 `readBy: { phase: "6" }`을 단다 — 처음 읽는 플랜이 지운다(`evidence.required` → 06-04, `evidence.prepaid_due_days` → 06-10, `evidence.max_size_mb` → 06-11, `purchase.online_vendor_name` → 06-08). 라벨·힌트는 UI-SPEC S20 표 그대로.

② `domain/permissions/menus.ts`에 `expenses.payments`(라벨 `지급 처리`) · `cards.purchases`(`구매 처리`) · `cards.proxy`(`카드 대리 등록`)를 기존 모양대로 더한다(UA-612). `can()`은 고치지 않는다 — 정확 일치 조회라 대신 통과가 생기지 않는다는 것을 테스트로 고정한다(D-601).

③ `test/unit/permissions/can.test.ts`에 behavior 두 줄을 더한다(기존 케이스는 그대로). `test/e2e/phase6-keys.spec.ts`를 새로 쓴다(behavior 셋째 줄 — 관리자 계정은 `test/e2e/fixtures.ts` 선례).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/permissions/can.test.ts test/unit/settings/registry-coverage.test.ts test/unit/admin-menu-registry.test.ts test/unit/ui/role-menu.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — registry-coverage가 빨가면 readBy 표시가 빠졌거나 ROADMAP Phase 6이 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/phase6-keys.spec.ts test/e2e/settings.spec.ts test/e2e/permissions-grid.spec.ts</automated>
    <fails_when>playwright 요약의 failed가 1 이상이거나 종료 코드가 0이 아니거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "evidence.required\|evidence.max_size_mb\|evidence.prepaid_due_days\|purchase.online_vendor_name" domain/settings/keys.ts`가 4 이상
    - `grep -c "expenses.payments\|cards.purchases\|cards.proxy" domain/permissions/menus.ts`가 3
    - can.test.ts에 「상위 메뉴 write만 있음 → 새 메뉴 false」 케이스가 세 키 모두 있다
    - E2E가 CI=true로 녹색이다
  </acceptance_criteria>
  <done>새 키 넷과 메뉴 셋이 정의 → 등록 → 관리자 화면까지 실제로 이어지고, 대신 통과가 없다는 것이 단위 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `sumKrw` · `diffKrw` · `resolveLineDoor` — 순수 함수 둘 묶음</name>
  <files>domain/money/index.ts, test/unit/domain/money-sum-diff.test.ts, domain/quotes/line-door.ts, test/unit/domain/line-door.test.ts</files>
  <read_first>
    - domain/money/index.ts(기존 export · `round` · 원 정수 규약) · eslint `plant8/money-boundary` 규칙(금액 셈은 금액 모듈 안)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「금액 표시 (서버 계산 값)」 절(「차이」 행 — 부호)
    - /mnt/project-files/06-prep/open-choices.md O-13 행
  </read_first>
  <behavior>
    - `diffKrw(1000, 4300)` = -3300 · `diffKrw(5200, 4000)` = 1200(부호 유지) · `sumKrw([])` = 0 · `sumKrw([1, 2, 3])` = 6 · 정수 아닌 입력은 타입 오류(원 정수 브랜드가 있으면 그것을 쓴다)
    - `resolveLineDoor({ vendorName }, setting)`: 설정 빈 값 → `expense` / `" 쿠팡 "` vs `"쿠팡"` → `purchase` / NFD로 적힌 같은 이름 → `purchase` / 다른 이름 → `expense` / 거래처 없음(null) → `expense`
  </behavior>
  <action>
`domain/money/index.ts`에 `sumKrw(values)` · `diffKrw(a, b)`(원 정수, 부호 유지)를 기존 export 모양대로 더한다 — 이 페이즈의 모든 원화 합·차이가 이 둘을 쓴다(D-605 — 차이는 표시·기록용이고 비용 기준을 바꾸지 않는다). `domain/quotes/line-door.ts`에 순수 함수 `resolveLineDoor({ vendorName }, onlineVendorSetting)` → `"purchase" | "expense"`를 둔다(O-13 계획 판단: 앞뒤 공백 제거 · `normalize("NFC")` 뒤 정확 비교, 빈 설정 = 모두 지출결의). 설정을 스스로 읽지 않는다 — 게이트(06-08 `purchase.line-door`)가 읽어 넘긴다. 두 파일에 단위 테스트(behavior 그대로, 실패 → 구현 → 통과).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/money-sum-diff.test.ts test/unit/domain/line-door.test.ts test/unit/domain/money.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — money.test.ts가 빨가면 기존 금액 함수를 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>error로 끝난다 — `plant8/money-boundary` 위반은 금액 셈이 금액 모듈 밖에 있다는 뜻이다</fails_when>
  </verify>
  <acceptance_criteria>
    - behavior의 모든 갈래가 개별 `it`이다(NFD 입력 케이스 포함)
    - `resolveLineDoor`가 설정 조회 import 없이 순수 함수다
  </acceptance_criteria>
  <done>원 정수 합·차이와 문 판정이 순수 함수로 서고 단위 테스트가 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 구매 요청 번호 서식 — `{프로젝트 번호}-C{순번 4자리}` · 설정 키 넷 · 범위 기간 부여 함수(서식은 트랜잭션 밖에서 읽어 인자로 — 팀 비용 번호 공백은 ⓪에서 묻는다)</name>
  <files>domain/settings/keys.ts, domain/document-numbering/index.ts, test/unit/domain/document-number-format.test.ts, test/integration/document-numbering.test.ts</files>
  <precondition>A-607 팀 비용 지출결의 번호 규칙이 Phase 5에 있거나 사용자가 「번호 공백」 선택지 중 하나를 골랐다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - domain/document-numbering/index.ts 전체(`DOCUMENT_NUMBER_FORMAT_DEFS` · `documentNumberFormat` · `allocateDocumentNumber`)
    - repositories/document-counters.ts(`allocateNumber`가 기간 문자열을 받는다)
    - domain/settings/keys.ts 230-290행(`document_number.project.*` 키 모양 · namespace `문서 번호`)
    - test/unit/domain/document-number-format.test.ts · test/integration/document-numbering.test.ts
    - /mnt/project-files/deadlock-fix/global-db-in-tx-audit.md 표 A행(트랜잭션 안 `loadDocumentNumberFormat` → `getSettingValue` 전역 `db` = 풀 고갈 교착)
    - PR #75 수정 꼴: main에 `export async function loadDocumentNumberFormat`이 있으면 그것을, 없으면 `git diff origin/main...origin/claude/fix-project-create-deadlock-8wybyd -- domain/document-numbering/index.ts domain/projects/index.ts`(서식을 트랜잭션 전에 읽어 `format`으로 넘기는 꼴)
  </read_first>
  <behavior>
    - `scopedDocumentNumberFormat({ scope: "26001", seq: 1 }, { prefix: "C", seqDigits: 4, separator: "-", seqStart: 1 })` = `26001-C0001`
    - 순번 12345 · 4자리 → `26001-C12345`(자르지 않음)
    - `seqStart` 101 · seq 1 → `26001-C0101`(표시 오프셋 — 카운터는 1부터)
    - 기존 `documentNumberFormat` 케이스는 그대로 녹색
    - (통합, CROSS E-2) 설정 접두어가 `C`인 상태에서 `allocateScopedDocumentNumber(viewer, { counterKey: "purchase_request", scope: "26001", format: { prefix: "Z", seqDigits: 4, separator: "-", seqStart: 1 } }, tx)` → `26001-Z0001` — 설정이 아니라 넘긴 서식을 따른다(함수 안에서 설정을 읽으면 `C`가 나와 빨갛다)
    - (통합) `loadDocumentNumberFormat("purchase_request")` → `{ prefix: "C", seqDigits: 4, separator: "-", seqStart: 1 }`(기본값) · 등록 안 된 counterKey → `UnknownDocumentNumberCounterError`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 A-612 · A-607 재확인. Phase 5가 프로젝트 단위 카운터 경로를 이미 만들었으면 그 함수에 `purchase_request` 줄만 더하고(새 부여 함수를 만들지 않는다) SUMMARY에 적는다. 팀 비용 지출결의 번호 규칙이 없으면 이 태스크의 코드를 한 줄도 바꾸지 않고 `선행 의존 A-607 미해소 — 팀 비용 번호 규칙 없음, 06-02 Task 3 착수하지 않음`과 「번호 공백」 세 선택지를 출력하고 멈춘다.

① `domain/settings/keys.ts`에 `document_number.purchase_request.prefix`(기본 `C`) · `.seq_digits`(4) · `.separator`(`-`) · `.seq_start`(1)를 `document_number.project.*`과 같은 모양 · namespace `문서 번호`로 더하고 `SETTING_DEFS`에 넣는다(A-612). ②의 서식 표가 읽으므로 readBy는 달지 않는다.

② `domain/document-numbering/index.ts`에 기간 = 범위 문자열(프로젝트 번호, 팀 비용이면 ⓪에서 정한 규칙)인 순수 서식 `scopedDocumentNumberFormat({ scope, seq }, format)` → `{scope}{separator}{prefix}{seq}`와 부여 함수 `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)`를 더한다(CROSS E-2 — PR #75 꼴). 규약: 카운터 증가는 반드시 문서 INSERT와 같은 트랜잭션(카운터는 1부터, `seqStart`는 표시 오프셋, 자릿수 넘침은 자르지 않음)이고, **서식은 호출자가 트랜잭션을 열기 전에 `loadDocumentNumberFormat("purchase_request")`로 읽어 `format`으로 넘긴다 — 이 함수는 설정을 읽지 않고 `repoAllocateNumber(…, tx)`와 `scopedDocumentNumberFormat`만 부른다**(카운터 행 잠금을 쥔 트랜잭션 안에서 전역 풀로 설정을 읽으면 풀 고갈 교착 — audit 표 A행, PR #75). 함수 머리 주석 한 줄에 「서식은 트랜잭션 전에 읽어 넘긴다」를 적는다. 서식 설정 표에 `purchase_request` 줄을 더한다(연도 자릿수 키 없음 — 범위 서식은 연도를 쓰지 않는다): 표 줄 타입의 `yearDigits`를 선택으로 두고, 지금은 모듈 안에만 있는 `loadDocumentNumberFormat`을 export한다(PR #75가 먼저 머지돼 이미 export돼 있으면 그대로 쓴다). 반환 타입은 오버로드로 가른다 — `"purchase_request"` → `ScopedDocumentNumberFormat`(`{ prefix, seqDigits, separator, seqStart }`), 그 밖 → 기존 `DocumentNumberFormat` — 기존 호출자의 타입은 바뀌지 않는다(`any` 금지). 기존 `project` 경로(`allocateDocumentNumber` · `createProject`)는 건드리지 않는다 — 그쪽 교착은 PR #75가 고친다. 호출자는 06-08(과 06-14)이다.

③ 먼저 테스트를 쓰고 실패를 확인한 뒤 ②를 구현한다(RED → GREEN). `test/unit/domain/document-number-format.test.ts`에 behavior 네 줄, `test/integration/document-numbering.test.ts`에 behavior 다섯째 · 여섯째 줄(기존 케이스는 그대로 — PR #75가 이 파일을 바꿨으면 그 모양 위에 더한다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/document-number-format.test.ts test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/document-numbering.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 기존 프로젝트 번호 경로를 깨뜨렸거나, 「넘긴 서식(`Z`)을 따른다」 케이스가 `26001-C0001`을 받아 빨갛다(부여 함수가 설정을 읽는다 — CROSS E-2 회귀)</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(Phase 5 번호 경로 유무 · 팀 비용 번호 규칙 출처 또는 사용자 선택)가 SUMMARY에 있다
    - `grep -c "document_number.purchase_request" domain/settings/keys.ts`가 4 이상
    - 단위 테스트가 자릿수 넘침·시작값 오프셋 케이스를 단언한다
    - (CROSS E-2) `awk '/export async function allocateScopedDocumentNumber/,/^}$/' domain/document-numbering/index.ts | grep -cE 'getSettingValue\(|loadDocumentNumberFormat\('`가 0이고, 같은 awk 범위의 줄 수가 5 이상이다(범위가 실제로 잡혔다)
    - `grep -c "export async function loadDocumentNumberFormat" domain/document-numbering/index.ts`가 1 이상
    - 통합 테스트에 「넘긴 서식을 따른다(`26001-Z0001`)」 케이스가 있고 녹색이다
  </acceptance_criteria>
  <done>구매 요청 번호가 설정으로 바뀌는 서식(트랜잭션 밖에서 읽어 인자로)과 설정을 읽지 않는 트랜잭션 안 부여 함수로 서고, 기존 프로젝트 번호 경로가 그대로 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 권한표 → `can()` | 새 메뉴 키가 기존 메뉴 권한으로 대신 통과되면 경영관리 전용 동작이 열린다 |
| 설정 화면 → 설정 값 | 관리자가 넣는 문자열(온라인구매 협력사)이 문 판정에 쓰인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | 새 메뉴 키 | high | mitigate | `can()` 정확 일치 조회를 유지하고, 상위 메뉴 write만 있는 역할이 새 세 키에서 거짓인 단위 테스트(D-601) |
| T-06-05 | Tampering | 온라인구매 협력사 설정 문자열 | low | mitigate | 공백 제거 · NFC 정규화 뒤 정확 비교만 — 부분 일치·정규식 없음. 설정 쓰기는 기존 `admin.settings` 권한 |
| T-06-06 | Tampering | 번호 유일성 | medium | mitigate | 서식이 순번을 자르지 않고, 부여는 문서 INSERT와 같은 트랜잭션(호출자 06-08 · 06-14가 지킨다). 기존 동시성 통합 테스트 녹색 유지 |
| T-06-151 | Denial of Service | 번호 부여 트랜잭션의 풀 고갈 교착 | high | mitigate | `allocateScopedDocumentNumber`는 서식을 인자로만 받고 설정을 읽지 않는다(CROSS E-2 · audit A행 · PR #75 꼴). 통합 「넘긴 서식을 따른다」 케이스 + region grep 0건. 동시 6건 경합은 호출자 06-08 통합 테스트가 증명한다 |
</threat_model>

<verification>
- 단위 4(can · money-sum-diff · line-door · document-number-format) + registry-coverage · admin-menu-registry · role-menu 녹색
- 통합 document-numbering · document-counters-concurrency 녹색(기존 경로 불변 + 「넘긴 서식을 따른다」 — CROSS E-2)
- E2E phase6-keys · settings · permissions-grid가 CI=true로 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 이 플랜은 화면 코드를 고치지 않는다(UA-614). 싼 게이트 → 별도 에이전트가 `CI=true`로 S20 DOM 실측 감사(backstop 2건) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 설정 키 넷과 메뉴 키 셋이 관리자 화면에 보이고 대신 통과가 없다
- 원 정수 합·차이 · 문 판정 · 구매 요청 번호 서식이 순수 함수로 서서 뒤 플랜이 그대로 부른다
- 팀 비용 번호 공백이 조용히 정해지지 않았다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 설정 키 | `evidence.required` · `evidence.max_size_mb` · `evidence.prepaid_due_days` · `purchase.online_vendor_name`(readBy 6) · `document_number.purchase_request.prefix` · `.seq_digits` · `.separator` · `.seq_start` |
| 메뉴 키 | `expenses.payments` · `cards.purchases` · `cards.proxy` |
| 함수 | `sumKrw` · `diffKrw` · `resolveLineDoor` · `scopedDocumentNumberFormat` · `allocateScopedDocumentNumber`(`format` 인자) · `loadDocumentNumberFormat`(export — 트랜잭션 밖 조회용) |
| 테스트 | `test/unit/domain/{money-sum-diff,line-door}.test.ts` · `test/unit/domain/document-number-format.test.ts`(확장) · `test/unit/permissions/can.test.ts`(확장) · `test/integration/document-numbering.test.ts`(확장 — 넘긴 서식) · `test/e2e/phase6-keys.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-2 — `allocateScopedDocumentNumber`가 트랜잭션 안에서 서식 설정을 읽어 PR #75 교착을 옮긴다(eng-cross-opus.md L16-18@333f7bf; 코드 확인: `domain/document-numbering/index.ts` 68-83 `loadDocumentNumberFormat`이 `getSettingValue` 5번, 87-105 `allocateDocumentNumber`가 트랜잭션 안에서 그것을 부른다 · PR #75는 origin/main에 없음) | 막음 | Plan 02, Task 3 ②: 서식은 호출자가 트랜잭션 전에 `loadDocumentNumberFormat("purchase_request")`로 읽어 `format`으로 넘기고 부여 함수는 설정을 읽지 않는다(PR #75 꼴 · export는 PR #75 머지 여부와 무관하게 이 플랜이 한다). behavior 「넘긴 서식 `Z`를 따른다」 통합 케이스(RED 가능) · acceptance region grep · T-06-151 · must_haves.truths 한 줄 |
| ENG E-3 — tx 규약을 한 곳에 못박고 다른 플랜은 참조(eng-review.md L72-74@333f7bf) | 고침 | must_haves.truths에 「06-03 tx 규약」 참조 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| ENG 리뷰 Section 2 「06-02:242는 서식을 트랜잭션 밖에서 읽는 PR#75 방식을 따른다 — 문제 없음」(eng-review.md L78@333f7bf) | 기각 — 코드와 플랜 원문(옛 Task 3 ② 「기존 `allocateDocumentNumber`와 같은 규약」)을 확인한 결과 CROSS E-2가 맞다. 위 Addressed 행으로 대체 |
| 번호 경합 동시 6건 테스트 | 이 플랜이 아니라 호출자 06-08 Task 2(`test/integration/purchase-requests.test.ts` 「동시 6건 번호 경합」)가 진다 — 부여 함수 단독으로는 교착 조건(줄 잠금 + 설정 읽기)이 서지 않는다 |

### Round 2 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-6 — read_first의 UI-SPEC rev 8 줄 번호가 남았다(06-02:141 「776-781행(S20)」은 rev 9에서 S13 · S14를 가리킨다 · :184 — 체커 W4 잔여) | 고침 | Task 1 read_first → 「S20. 설정 키 — 관리자 설정 화면」 절 · Task 2 read_first → 「금액 표시 (서버 계산 값)」 절 「차이」 행. 줄 번호 없음 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |
