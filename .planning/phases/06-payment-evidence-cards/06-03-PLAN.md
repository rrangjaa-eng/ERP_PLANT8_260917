---
phase: 06-payment-evidence-cards
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - db/schema/corp-card-usages.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/amounts.ts
  - domain/corp-card-usages/link-targets.ts
  - domain/quotes/lines.ts
  - domain/rules/register.ts
  - repositories/corp-card-usages.ts
  - repositories/quote-line-links.ts
  - app/(app)/cards/page.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/link-picker.tsx
  - app/(app)/cards/actions.ts
  - app/(app)/cards/actions.registry.ts
  - app/(app)/cards/cards.module.css
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/card-usage-section.tsx
  - test/unit/domain/card-usage-amounts.test.ts
  - test/unit/domain/rules-card-dual-link.test.ts
  - test/integration/corp-card-usages.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/card-usage.spec.ts
autonomous: true
requirements: [EXP-07]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 `/cards?new=1`에서 자기 카드(또는 자기 팀 카드)를 고르고 사용일·결제 합계를 적고 연결(견적 줄 / 견적 외 비용 / 팀 비용) 하나를 골라 1차 「카드 사용 등록」을 누르면 저장되고 `/cards`로 돌아가 그 카드 그룹에 행이 선다(EXP-07)"
    - "연결을 고르지 않은 카드 사용은 화면 1차가 막히고(`연결 없음 · 연결 고르기`), 액션을 직접 불러도 서버가 거부한다 — DB CHECK가 견적 줄 · 팀 중 정확히 하나를 강제한다(EXP-07 「연결 없는 카드 사용은 서버가 막는다」)"
    - "사람은 결제 합계(부가세 포함)를 적고 공급가·부가세는 서버가 증빙 종류 규칙으로 역산해 저장한다 — `vat_surcharge`는 `grossFromTotal()`, `none`은 공급가 = 결제 합계, 반올림 잔차가 0이 아니면 서버 계산 한 줄 끝에 `· 반올림 차이 N`(D-607)"
    - "외화 카드는 외화 금액 × 환율(기본값 = 설정 최근 환율)로 원화 결제 합계를 서버가 계산한 뒤 역산한다 — 저장 행에 통화·외화 금액·환율·원화 환산액이 모두 있다(O-7, FX-01)"
    - "카드 폼의 증빙 종류 옵션은 서버가 `vat_surcharge`·`none` 규칙의 종류만 보내고, 가맹점(거래처)을 고르면 거래처 기본 증빙 종류로 채워지되 그것이 옵션 밖이면 `카드 전표`로 떨어지며(옵션 안에 있을 때만) 가맹점 줄 아래 `기본 증빙 {종류} · 카드에 없음`이 선다 — 서버도 옵션 밖 종류의 저장을 거부한다"
    - "`팀 비용`을 고르면 팀을 고르는 칸이 없고 사용한 사람의 사용일 소속(`teamAtDate`)이 텍스트로 채워진다 — 소속이 없으면 임의 팀으로 떨어지지 않고 1차가 「막힘 — 카드 사용 폼」 소속 없음 갈래로 막힌다(EXP-07)"
    - "`견적 외 비용`을 고르고 저장하면 같은 트랜잭션에서 그 프로젝트에 견적가 0인 견적 외 비용 줄이 새로 생기고(항목 기본값 = 가맹점 이름) 카드 사용이 그 줄에 연결된다(O-8, UA-615)"
    - "지출결의가 연결된 견적 줄(법인카드 지급 방식 지출결의 포함)에는 카드 사용을 이을 수 없다 — `rules.gate`의 `card.dual-link-block`이 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄` 꼴 이유로 거부하고, 연결 고르기 목록에서 그 줄은 `aria-disabled` + 2행 이유다(D-609)"
    - "같은 쪽(카드 사용) 여러 건은 한 줄에 이을 수 있고, 견적 줄 아래 힌트와 목록 2행에 `카드 사용 N건 {합}`이 보인다(D-609)"
    - "완료(정산) 프로젝트는 직원의 프로젝트 고르기에서 고를 수 없다(행 2행 `완료 · 견적 줄 잠김`) — 대리 등록 권한자의 견적 외 비용 갈래는 06-04가 연다"
    - "카드 사용 등록은 결재가 없고 행동 로그에 `document_create` 한 줄을 남긴다(D-608)"
    - "S8 empty: 「Empty — 카드 사용 목록」 세 갈래(이번 달 0건 · 필터 0건 · 쓸 카드 0장 — 1차 렌더 안 함)"
    - "S8 loading: 첫 로드는 §7-7 LOADING 뼈대이고 월·카드·연결·등록 필터 이동은 GET 이동이다"
    - "S8 error: 「Error — 목록 로드」 카드 사용 문구가 선다"
    - "S8 populated: 카드 그룹 머리글 · 사용일 오름차순 · 열(사용일 · 가맹점 · 연결 · 결제 합계 · 증빙 · 등록) · 결제 합계 2행 공급가/외화 묶음 · 등록 칸(「표시 — 경영관리 등록」) · 합계 줄 · 오른쪽 3차 `구매 요청 N` + 1차가 와이어프레임대로 선다"
    - "S8 partial: 가맹점 없음 = `—` · 증빙은 `있음`/`—` 글자(상태 색 없음) · 외화는 2행 두 묶음(「표시 — 카드 사용 결제 합계 2행」)"
    - "S8 overflow: 50건 페이지(§7-16, UA-603)이고 카드 그룹 머리글이 쪽 첫 줄에 다시 선다, 좁은 PC는 P3 증빙부터 숨긴다"
    - "S8 zero-one-many: 0건 = Empty · 건수는 합계 줄 `N건` · 하위 목록 링크는 0이면 `구매 요청`만(SP-4 — 링크 자체는 06-06의 구매 요청 목록으로 간다)"
    - "S10 empty: 「Empty — 연결 고르기 목록」 세 갈래(줄 0 · 검색 0건 · 고를 수 있는 줄 0)가 카드 폼에서 다음 한 수 `견적 외 비용으로`를 말한다 — 구매 요청 폼 갈래(`팀 비용으로`)는 06-06이 같은 컴포넌트에 더한다"
    - "S10 error: 「Error — 연결 고르기 목록 로드(S10)」 — 목록 자리 한 줄 `프로젝트를 불러오지 못했습니다 · 다시 시도` / `견적 줄을 불러오지 못했습니다 · 다시 시도`, 1차 `이 줄로` 비활성, 2차 `취소 Esc`"
    - "S10 populated: 프로젝트 행 = 번호 · 이름 + 2행 `{상태} · 담당 {PM}` / 견적 줄 행 = 번호 · 항목(거래처) · 실행가, 현재 줄 700 + 왼쪽 2px `--accent`, 같은 쪽 기존 연결 2행 `카드 사용 1건 1,200,000`"
    - "S10 partial: 반대쪽 줄은 보이되 `aria-disabled` + 2행 이유(「막힘 — 연결 고르기 목록의 반대쪽 줄」) — 구매 요청 폼의 온라인구매 줄만 고르기는 06-06"
    - "S10 zero-one-many: 부제 `… · 카드로 이을 수 있는 줄 N`, 고를 수 있는 줄 0이면 1차 `이 줄로` 비활성 + `이을 수 있는 줄 없음 · 견적 외 비용으로`"
    - "S15 empty: 「Empty — 상세 「법인카드 사용」 섹션」 — 카드가 있는 사람에게만 3차, 없으면 사실만"
    - "S15 error: 「Error — 섹션 로드」 — 이 섹션만 실패하고 상세의 다른 섹션은 선다(§7-7)"
    - "S15 populated: 열(사용일 · 견적 줄 · 가맹점 · 결제 합계 2행 공급가 · 등록)이고 경영관리 등록 건은 `경영관리 등록`(600) — 팀 비용 연결 건은 없다"
    - "S15 partial: `quote.amount`가 없는 사람은 금액 열이 없고 부제가 `{N}건`만이다 — 서버가 DTO에서 칸을 뺀다"
    - "S15 zero-one-many: 부제 `{N}건 · 결제 합계 {합}`, 0건 = Empty 한 줄"
    - "S9 empty: 새 건에서 연결 미선택 = `연결 없음 · 연결 고르기`, 빈 칸 = `카드 · 결제 합계 2칸 비어 있음 · 카드 고르기`(「막힘 — 카드 사용 폼」). 증빙 종류는 `카드 전표`가 옵션에 있을 때만 채우고, 없으면 `—` + 「막힘 — 카드 사용 폼 · 증빙 종류 자동 채움 없음」"
    - "S9 loading: 결제 합계·통화·환율이 바뀌면 서버 계산 한 줄이 이전 값 `--faint` → 새 값으로 바뀌고, 1차 처리 중에는 `…`다 — 사용일 변경 때의 사용한 사람 후보 재전송은 대리 등록(06-04)"
    - "S9 populated: 카드 · 사용일 · 가맹점 · 결제 합계 + 서버 계산 한 줄 · 증빙 종류 · 연결 셋 · 메모 · 증빙이 와이어프레임대로 서고 카드 옵션 글자는 `{카드 이름} · {발급사} {뒤 4자리}`다"
    - statement: "S8 long-text: 가맹점·연결이 한 줄 말줄임 + `title`로 선다 — 긴 해외 가맹점 이름 · 긴 프로젝트 이름으로 CI=true DOM 감사"
      verification: backstop
    - statement: "S10 loading: 모달·시트 목록 로드와 검색 중 표시가 §7-7 LOADING 꼴이다 — 느린 응답을 강제해 CI=true DOM 감사"
      verification: backstop
    - statement: "S10 overflow: 견적 줄 100줄 이상 프로젝트에서 480 모달·폰 시트 안 목록이 스크롤로 서고 행동 줄이 가려지지 않는다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S10 long-text: 긴 항목·거래처·프로젝트 이름이 480 폭에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S15 loading: 섹션 로드가 §7-7 LOADING 꼴(섹션 단위)이다 — 느린 응답을 강제해 CI=true DOM 감사"
      verification: backstop
    - statement: "S15 overflow: 카드 사용 50건 이상 프로젝트에서 섹션 표가 §7-3 (자) 규칙대로 선다(이 섹션의 페이지 나눔은 본문에 없다 — 계획은 나누지 않는다) — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "한 견적 줄의 같은 돈이 지출결의 쪽과 카드 사용 쪽에 동시에 잡혀서는 안 된다(D-609 — 이중 계산). 화면 비활성만으로 막지 않고 서버 게이트가 거부한다"
      status: resolved
      verification: test
    - statement: "브라우저가 보낸 공급가·부가세·원화 환산액을 저장해서는 안 된다 — 사람은 결제 합계만 적고 나머지는 서버가 계산한다(D-607)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/corp-card-usages.ts"
      provides: "카드 사용 표 — 카드 · 사용일 · 가맹점 · 결제 합계 Money 묶음 · 공급가 · 부가세 · 증빙 종류 · 연결(견적 줄 XOR 팀) · 사용한 사람 · 등록 경로(self·proxy·purchase) · version"
      exports: ["corpCardUsages"]
    - path: "domain/corp-card-usages/index.ts"
      provides: "카드 사용 등록 · 목록 · 프로젝트 섹션 DTO"
      exports: ["createCardUsage", "listCardUsages", "listProjectCardUsages"]
    - path: "domain/corp-card-usages/amounts.ts"
      provides: "결제 합계 → 원화 · 공급가 · 부가세 · 반올림 잔차(D-607, O-7)"
      exports: ["splitCardTotal"]
    - path: "repositories/quote-line-links.ts"
      provides: "견적 줄마다 연결 문서(지출결의 · 카드 사용) 조회 — 이중 연결 판정과 줄 상태 파생의 공용 입력"
      exports: ["findLineLinks"]
  key_links:
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/rules/gate.ts"
      via: "저장 전 이중 연결 판정을 게이트 한 지점에서 받는다"
      pattern: "card.dual-link-block"
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/quotes/lines.ts"
      via: "견적 외 비용 줄 생성을 견적 줄 모듈에 맡기고 같은 트랜잭션에서 부른다"
      pattern: "createOutOfQuoteLine"
    - from: "domain/corp-card-usages/amounts.ts"
      to: "domain/money/index.ts"
      via: "역산과 부가세 차가 금액 모듈 함수로만 이루어진다"
      pattern: "grossFromTotal"
---

<objective>
법인카드 사용 등록의 한 경로를 새 표(A-611, 선행 설계 없음)부터 목록·폼·프로젝트 섹션까지 세운다 — 직원의 **본인 등록**, 세 연결 대상(견적 줄 · 견적 외 비용 · 팀 비용), 결제 합계 → 공급가 역산(D-607), 외화(O-7), 이중 연결 서버 차단(D-609). 대리 등록·수정·삭제는 06-04, 구매 완료 모드는 06-06이 이 위에 더한다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 「모든 나가는 돈이 견적 줄에 한 번만」은 카드 쪽에서 가장 쉽게 깨진다 — 카드는 결재가 없고(D-608) 여러 사람이 등록한다. 그래서 연결 필수(DB CHECK)와 반대쪽 차단(`rules.gate`)을 첫 경로에 넣는다.

Output: `corp_card_usages` 표 + 마이그레이션, `domain/corp-card-usages`(AS3 — 마스터 `domain/corp-cards`와 별도 모듈), 게이트 `card.dual-link-block`, `/cards` 목록·폼·연결 고르기, 프로젝트 상세 카드 섹션, 단위 2 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-RESEARCH.md
@.planning/phases/06-payment-evidence-cards/06-PATTERNS.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-03 착수하지 않음`을 출력하고 멈춘다.** 이름은 가칭 — 06-01 SUMMARY의 「가칭 → 실제」 표를 먼저 읽는다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-603 | Phase 4 04-13 — 견적 줄 종류(`quote` · `out_of_quote` · `adjustment`) 컬럼(마이그레이션 `0014_quote_line_kind` 계열). 견적 외 비용 줄 생성(O-8)이 이 종류에 기댄다. **이 플랜의 판단:** 조정 줄(`adjustment`)은 카드 연결 대상이 아니다(연결 고르기 목록에서 뺀다 — D-612가 조정 줄을 미매칭 점검에서 빼는 것과 같은 결) | `grep -n "out_of_quote" db/schema/quote-lines.ts` 0건이면 멈춤 |
| A-607 | Phase 5 지출결의의 견적 줄 연결 칸(가칭 `expenses.quote_line_id`)과 지급 방식(UA-618) — 반대쪽(지출결의 쪽) 판정 입력 | `grep -rn "quote_line_id\|quoteLineId" db/schema/` 에 지출결의 표 줄이 없으면 멈춤 |
| UA-603 | `ui/pagination/Pagination.tsx`(50건 페이지) | `git ls-files ui/pagination/Pagination.tsx` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(「확인 — 입력 버리기」) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| A-611 | 선행 없음 — 이 플랜이 `corp_card_usages`를 처음 설계한다 | 확인만: `git ls-files 'db/schema/*card-usage*'` 비어 있어야 정상(있으면 멈추고 SUMMARY에 적는다) |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-7 | 외화 카드 = 외화 금액 + 환율(기본 = 설정 최근 환율) → 서버가 원화 → 역산 | `domain/corp-card-usages/amounts.ts` · 폼 결제 합계 칸 |
| O-8 | 「견적 외 비용」 연결은 저장 때 견적 외 비용 줄을 새로 만든다 | `createCardUsage`의 견적 외 비용 갈래 한 곳 |
| AS3 | 카드 사용은 `domain/corp-card-usages` 별도 모듈 | 모듈 경계(바뀌면 파일 이동) |
| O-18 | 금액 셈은 서버 — 서버 계산 한 줄은 액션 응답 | 해당 없음 |
| O-11 | (06-04 소관) 이 플랜은 새 건만 — 수정·삭제 권리는 06-04 | 해당 없음 |

<probe_fallback>
coverage.json의 이 플랜 몫: **EXP-07 — unclassified → unresolved (flagged, 자동 해소·기각 없음)**. 대신 쓰는 근거: 기준 3 · D-607 · D-608 · D-609 · O-7 · O-8. 전체 10건 표는 06-01 `<probe_fallback>`.

**표면화하는 가정(조율 필요):** D-607(「카드 금액 = 결제 합계, 공급가는 서버 역산」)은 REQUIREMENTS의 EXP-15 「사람은 공급가액만 적는다」 문구·ROADMAP 기준 3과 낱말이 어긋난다. 이 플랜은 **잠긴 결정 D-607을 따른다.** 요구사항·ROADMAP 문구 정렬은 계획이 `.planning/`을 손으로 고치지 않으므로 오케스트레이터(`/gsd-phase edit`) 몫이다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 직원이 자기 카드 사용 한 건을 「팀 비용」에 이어 등록하고 목록에서 본다</name>
  <files>db/schema/corp-card-usages.ts, db/schema/index.ts, db/migrations/meta/_journal.json, domain/corp-card-usages/index.ts, domain/corp-card-usages/amounts.ts, repositories/corp-card-usages.ts, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-list.tsx, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, app/(app)/cards/cards.module.css, test/unit/domain/card-usage-amounts.test.ts, test/integration/leak-scan.test.ts, test/e2e/card-usage.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 06-01이 끝났다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 551-645행(S8 · S9 정본) · 832-833행(카드 공급가·부가세 · 외화) · Copywriting 「막힘 — 카드 사용 폼」 · 「표시 — 카드(목록 · 폼)」 · 「표시 — 카드 사용 결제 합계 2행」 · 「표시 — 카드 폼 연결 「팀 비용」」 · 「Empty — 카드 사용 목록」
    - .planning/phases/06-payment-evidence-cards/06-PATTERNS.md(corp-cards 선례 — `cardOwnerKind` XOR · `isUniqueViolation` · 오류 클래스)
    - domain/corp-cards/index.ts · repositories/corp-cards.ts · app/(app)/admin/corp-cards/{page.tsx,card-form.tsx,actions.ts,actions.registry.ts}
    - domain/revenue/index.ts 80-100행(`computeGrossFromPayment` · `recomputeDeltaKrw` 선례)
    - domain/money/index.ts(`grossFromTotal` · `toKrw`) · domain/money/currency.ts(`recentFxRate`) · db/schema/money-columns.ts
    - domain/org/index.ts의 `teamAtDate` · domain/permissions/scope-for.ts
    - app/(app)/cards/page.tsx(현재 19줄 ListEmpty 자리)
    - test/unit/leak-scan-coverage.test.ts(새 `actions.registry.ts`는 누수 스캔 import 필수)
  </read_first>
  <behavior>
    - `splitCardTotal({ currency: KRW, amount: 1240000 }, vat_surcharge)` → 원화 1,240,000 · 공급가 = grossFromTotal 값 · 부가세 = 합계 − 공급가 · 잔차 = 재계산 합 − 합계
    - `none` 규칙 → 공급가 = 합계, 부가세 0, 잔차 0
    - USD 900.00 @1,474.89 → 원화 = `toKrw` 값, 그 원화로 역산
    - `createCardUsage`: 연결 없음 → 거부 / 팀 비용 · 사용일 소속 없음 → 거부 / 옵션 밖 증빙 종류 → 거부 / 남의 카드(본인 등록) → 거부 / 정상 → 저장 + `document_create`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. 있으면 실제 이름을 SUMMARY에 적는다.

① **스키마(A-611 — 최초 설계).** `db/schema/corp-card-usages.ts`: `corp_card_usages`(id · corp_card_id FK → 카드 마스터 · used_on date · merchant_vendor_id FK null(가맹점 = 거래처, 비울 수 있음) · `moneyColumns("total")` · supply_krw bigint · vat_krw bigint · evidence_type_code text · link_kind text CHECK in (quote_line, team_cost) · quote_line_id FK null · team_id FK null · CHECK(견적 줄 연결이면 quote_line_id만, 팀 비용이면 team_id만 — XOR, corp-cards의 `cardOwnerKind` 선례) · used_by_user_id FK · registered_by FK · registered_via text CHECK in (self, proxy, purchase) · memo text null · version · source 'demo' · 생성·수정 시각). 인덱스: (corp_card_id, used_on) · (quote_line_id). `pnpm db:generate --name corp_card_usages` → `pnpm lint:sql` → `pnpm db:migrate` → 두 번째 `pnpm db:generate`가 `No schema changes`.

② **금액.** `domain/corp-card-usages/amounts.ts`의 `splitCardTotal(total, rule, settings)`: 외화면 `toKrw`로 원화 결제 합계 → `vat_surcharge`는 `grossFromTotal(원화, 부가세율, 단위, 방식)`으로 공급가, 부가세 = `diffKrw(원화, 공급가)`, 잔차는 revenue 선례처럼 재계산 합 − 원화 / `none`은 공급가 = 원화. 세율·반올림은 기존 설정 키(`tax.*`)에서 읽는다 — 새 키 없음.

③ **domain · 액션.** `domain/corp-card-usages/index.ts`의 `createCardUsage(viewer, input)` — 이 태스크는 **본인 등록 · 팀 비용 연결** 한 경로: 카드 자격(자기 카드 = 소지자 본인, 자기 팀 카드 = 사용일 소속 팀의 카드) · 사용한 사람 = viewer · 팀 = `teamAtDate(viewer, viewer.userId, usedOn)`(null이면 소속 없음 거부) · 증빙 종류 옵션 판정(카드에 맞는 규칙만 — 코드표 규칙 값으로) · `splitCardTotal` · INSERT · `recordAction` `document_create`. 브라우저가 보낸 공급가·부가세·원화 필드는 zod 입력에 없다. `listCardUsages(viewer, filters)` — 범위: 직원 = 자기 카드·자기 팀 카드의 사용 + 자기 등록 / `cards.proxy`·`expenses.payments` write 권한자와 `scopeFor`가 전사 범위인 사람(대표) = 전부(UA-612). DTO `registerDto`. `app/(app)/cards/actions.ts`(`createCardUsageAction` · 서버 계산 한 줄용 `previewCardAmountsAction`) · `actions.registry.ts` 신규 → `test/integration/leak-scan.test.ts`에 이 registry와 `@/domain/corp-card-usages` import 두 줄.

④ **화면.** `app/(app)/cards/page.tsx`의 ListEmpty 자리를 S8 목록으로 바꾸고(`?new=1`이면 S9 폼), 목록은 카드 그룹 머리글 · 사용일 오름차순 · 열 · 합계 줄 · 필터(월 · 카드 · 연결 · 등록 — 등록 필터는 권한자에게만) · 50건 `Pagination`. 폼은 카드 `Select`(한 장이면 텍스트, 0장이면 폼 대신 Empty 카드 0장 갈래) · 사용일(기본 오늘) · 결제 합계(통화 `Select` + 금액 280, 외화면 환율 칸 기본 최근 환율) · 서버 계산 한 줄(액션 응답, 이전 값 `--faint`) · 증빙 종류 · 연결 라디오 셋(이 태스크는 `팀 비용` 갈래만 동작, 나머지 둘은 Task 2) · 메모. 1차 막힘 이유는 Copywriting 행 그대로. 숫자 `.num`, 토큰은 `tokens.css`만, 상태 글자는 `status-display.ts`.

⑤ **E2E.** `test/e2e/card-usage.spec.ts`: 카드 소지 직원 → `/cards` → 「카드 사용 등록」 → 팀 비용 → 저장 → 목록 그룹에 행 + 연결 `팀 비용 · {팀}`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/card-usage-amounts.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 개수를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - 생성된 마이그레이션 SQL에 XOR CHECK(견적 줄 · 팀 중 하나)가 있다 — `grep -c "CHECK" db/migrations/*corp_card_usages*.sql` 1 이상
    - `createCardUsageAction`의 zod 입력에 공급가·부가세·원화 환산액 필드가 없다
    - `grep -c "corp-card-usages\|cards/actions.registry" test/integration/leak-scan.test.ts`가 2 이상
    - E2E 한 경로 CI=true 녹색
  </acceptance_criteria>
  <done>직원의 카드 사용 한 건이 스키마 → 역산 → domain → 액션 → 목록 화면 → 행동 로그를 실제로 통과하고, 연결 없는 저장이 DB에서도 불가능하다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 견적 줄 · 견적 외 비용 연결 — 연결 고르기(S10) · 이중 연결 게이트 · 견적 외 비용 줄 생성</name>
  <files>domain/corp-card-usages/index.ts, domain/corp-card-usages/link-targets.ts, domain/quotes/lines.ts, domain/rules/register.ts, repositories/quote-line-links.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/link-picker.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/unit/domain/rules-card-dual-link.test.ts, test/integration/corp-card-usages.test.ts, test/e2e/card-usage.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 625-653행(연결 · S10) · 987-994행(S10 considerations) · Copywriting 「막힘 — 연결 고르기 목록의 반대쪽 줄」 · 「Empty — 연결 고르기 목록」 · 「Error — 연결 고르기 목록 로드(S10)」
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-609 · D-612
    - domain/quotes/lines.ts(`saveQuoteLines`·`QUOTE_LINE_DTO_SPEC`) · domain/rules/register.ts
    - A-603 재확인에서 찾은 견적 줄 종류 컬럼 이름 · A-607의 지출결의 견적 줄 칸 이름
  </read_first>
  <behavior>
    - `card.dual-link-block` 카드 쪽: 줄에 살아 있는 지출결의(반려·취소 제외, 법인카드 지급 방식 포함) 있음 → 막힘 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄` / 카드 사용만 있음 → 통과(같은 쪽 여러 건) / 연결 0 → 통과
    - 지출결의 쪽 판정(같은 규칙, 06-07이 부른다): 줄에 카드 사용 있음 → 막힘 `카드 사용 {N}건 연결됨 · 지출결의는 다른 줄`
    - 조정 줄 · 취소 줄은 고르기 목록에 없다, 완료 프로젝트는 직원에게 고를 수 없음
    - 견적 외 비용 갈래: 저장 한 번에 줄 1개 + 카드 사용 1개, 카드 사용 저장이 실패하면 줄도 생기지 않는다(한 트랜잭션)
  </behavior>
  <action>
`repositories/quote-line-links.ts`의 `findLineLinks(viewer, lineIds, tx)` — 줄마다 살아 있는 지출결의(A-607 칸) · 카드 사용 목록. `domain/rules/register.ts`에 `card.dual-link-block`을 등록한다: doc = { side: card 또는 expense, links }, 반대쪽 연결이 있으면 막힘(이유 문자열은 Copywriting 「막힘 — 연결 고르기 목록의 반대쪽 줄」·S14 문구 꼴). 같은 쪽 여러 건은 통과(D-609). 지출결의 쪽 호출은 06-07이 Phase 5 제출 함수에 붙인다 — 이 태스크는 규칙과 카드 쪽 호출만.

`domain/quotes/lines.ts`에 `createOutOfQuoteLine(viewer, { projectId, itemName }, tx)` — 04-13(A-603)의 견적 외 비용 종류, 견적가 0 · 실행가는 카드 공급가(UI-SPEC 628행 `실행가 {공급가}`) · 현재 차수의 끝 순서, 기존 저장 경로의 게이트(`project.completed-lock`)를 그대로 지난다. `createCardUsage`에 견적 줄 갈래(`gate(…, "card.dual-link-block")` 뒤 INSERT)와 견적 외 비용 갈래(한 트랜잭션 안에서 줄 생성 → 카드 사용 INSERT)를 더한다.

`domain/corp-card-usages/link-targets.ts`: 프로젝트 고르기 목록(검색 번호·이름·클라이언트, 미수주는 끝, 완료는 직원에게 고를 수 없음 + 2행 `완료 · 견적 줄 잠김`)과 견적 줄 고르기 목록(취소·조정 줄 제외, 반대쪽 줄은 `selectable: false` + 이유, 같은 쪽 기존 연결 2행 `카드 사용 N건 {합}`, 부제 `카드로 이을 수 있는 줄 N`) DTO. 고르기 가능 여부는 서버가 줄마다 보낸다. `link-picker.tsx`(S10 — PC 480 모달 · 폰 시트, 1차 `이 줄로 Enter` · 2차 `취소 Esc`, 세 Empty 갈래, 로드 오류 한 줄 + `다시 시도`)는 `mode` 속성을 받되 이 플랜은 `card`만 구현한다(`purchase`는 06-06). 폼의 `견적 줄`·`견적 외 비용` 라디오 갈래를 켜고, `aria-label`로 `프로젝트 바꾸기`·`견적 줄 바꾸기`·`가맹점 바꾸기`를 가른다. 입력이 있는데 `취소 Esc`면 「확인 — 입력 버리기」(`ConfirmDialog`).

통합 테스트 `test/integration/corp-card-usages.test.ts`: 연결 없음 거부(액션 · DB CHECK 둘 다) · 반대쪽 줄 거부 · 같은 쪽 여러 건 허용 · 견적 외 비용 원자성 · 조작된 공급가 무시 · 옵션 밖 증빙 종류 거부 · 팀 비용 소속 없음 거부 · 외화 저장값 네 칸.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-card-dual-link.test.ts test/unit/domain/card-usage-amounts.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. quote-lines가 빨가면 `createOutOfQuoteLine`이 기존 저장 경로를 깨뜨렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 네 줄의 각 갈래를 개별 `it`으로 단언하고, 원자성 케이스는 카드 사용 INSERT를 실패시켰을 때 견적 줄 수가 늘지 않음을 확인한다
    - E2E에 「견적 줄 고르기에서 반대쪽 줄이 `aria-disabled` + 이유」와 「견적 외 비용 저장 → 상세 견적 표에 새 줄」 케이스가 녹색이다
  </acceptance_criteria>
  <done>세 연결 대상이 모두 동작하고, 반대쪽 줄 연결이 화면과 서버 둘 다에서 막히며, 견적 외 비용 줄이 카드 사용과 원자적으로 생긴다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 프로젝트 상세 「법인카드 사용」 섹션(S15) · 목록 마감(S8 필터·외화 2행·등록 칸)</name>
  <files>domain/corp-card-usages/index.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/card-usage-section.tsx, app/(app)/cards/card-usage-list.tsx, test/integration/corp-card-usages.test.ts, test/e2e/card-usage.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 705-712행(S15) · 1027-1033행(S15 considerations) · 971-978행(S8 considerations) · Copywriting 「표시 — 경영관리 등록」 · 「Empty — 상세 「법인카드 사용」 섹션」 · 「Error — 섹션 로드」
    - app/(app)/projects/[id]/page.tsx · app/(app)/projects/[id]/revenue-section.tsx(섹션 모양 선례)
    - domain/permissions/info-items.ts(`quote.amount`)
  </read_first>
  <behavior>
    - `listProjectCardUsages`: 그 프로젝트의 견적 줄(견적 외 비용 포함)에 이어진 카드 사용만, 팀 비용 건 없음
    - `quote.amount` 없는 계급 → DTO에 결제 합계·공급가 칸 없음, 부제는 건수만
    - 등록 칸: registered_via = proxy → `경영관리 등록` + 2행 등록자 · 날짜
  </behavior>
  <action>
`domain/corp-card-usages/index.ts`에 `listProjectCardUsages(viewer, projectId)` — 금액 칸은 `quote.amount` 정보 항목으로 가르고(서버가 칸을 뺀다), 부제 `{N}건 · 결제 합계 {합}`의 합은 서버가 `sumKrw`로 낸다. `card-usage-section.tsx`(§6-2 섹션, 읽기 표): 열 사용일 · 견적 줄 · 가맹점 · 결제 합계 2행 공급가 · 등록(`경영관리 등록` 600), Empty 한 줄(카드가 있는 사람에게만 3차 `카드 사용 등록`), 섹션 로드 실패는 섹션만 한 줄 + `다시 시도`. 페이지 나눔은 하지 않는다(§7-3 (자)). 프로젝트 상세 `page.tsx`에 매출 섹션 뒤에 붙인다.

S8 목록 마감: 가맹점 없음 `—`, 증빙 `있음`/`—` 글자(색 없음), 외화 2행 두 묶음(`USD 900.00 · 1,327.40`), 등록 칸(proxy면 `경영관리 등록` + 2행), 필터 `연결` 네 값, 좁은 PC P3 증빙 숨김, 쪽마다 카드 그룹 머리글 반복, 오른쪽 3차 `구매 요청` 링크 자리(건수는 06-06이 채운다 — 이 플랜에서는 0이라 이름만).

E2E에 「상세에서 카드 섹션 행 + 부제 합」, 「`quote.amount` 없는 계정은 금액 열 없음」 케이스를 더한다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. leak-scan이 빨가면 새 DTO가 계급별 노출 규칙을 어겼다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. revenue-section이 빨가면 상세 화면 섹션 배치를 깨뜨렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 `quote.amount` 없는 계급의 `listProjectCardUsages` 결과 객체에 금액 키가 없음을 단언한다
    - E2E 두 케이스 녹색, 기존 revenue-section 스펙 녹색
  </acceptance_criteria>
  <done>카드 사용이 목록·폼·연결 고르기·프로젝트 상세 네 자리에서 계약대로 보이고, 금액 노출이 서버 DTO에서 갈린다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 카드 사용 액션 | 카드·사용일·결제 합계·통화·환율·증빙 종류·연결이 넘어온다. 공급가·부가세·원화 환산액은 넘어와도 저장되지 않는다 |
| domain → quote_lines | 카드 모듈이 견적 줄 모듈에 줄 생성을 부탁한다(O-8) — 같은 트랜잭션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14 | Tampering | 이중 연결 우회 | high | mitigate | `card.dual-link-block`을 domain 저장 함수 안에서 `gate()`로 부른다. 화면 비활성은 보조일 뿐이다. 반대쪽 줄 거부 통합 테스트 |
| T-06-15 | Tampering | 공급가·부가세 조작 | high | mitigate | zod 입력에 계산 필드 없음 · 서버 `splitCardTotal` 재계산 · 조작 페이로드 통합 테스트 |
| T-06-16 | Elevation of Privilege | 남의 카드로 본인 등록 | high | mitigate | 카드 자격(소지자 본인 · 사용일 소속 팀의 카드)을 서버가 판정. 남의 카드 거부 통합 테스트(대리 등록은 06-04의 별도 권한) |
| T-06-17 | Information Disclosure | 카드 사용 목록·상세 섹션 DTO | medium | mitigate | 범위(자기 카드·팀 카드·자기 등록 / 권한자·전사 범위)를 리포지토리 쿼리에서 거르고, 금액 칸은 `quote.amount`로 가른다. DTO 등록 → 누수 스캔 |
| T-06-18 | Tampering | 팀 비용 귀속 | medium | mitigate | 팀은 입력이 아니라 `teamAtDate`로 서버가 정한다. 소속 없음이면 저장 거부 |
| T-06-19 | Denial of Service | 마이그레이션 | low | mitigate | 새 표 · 락 타임아웃 규약 · `pnpm lint:sql` 0건 |
</threat_model>

<verification>
- 단위 2 · 통합 1(+ 기존 quote-lines · leak-scan) · E2E 1 녹색, `test/unit/leak-scan-coverage.test.ts` 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate`가 `No schema changes`
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml·tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S8·S9·S10·S15 DOM 실측 감사(backstop 6건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 카드 사용이 세 연결 대상 중 하나 없이는 저장될 수 없다(액션 · DB 둘 다)
- 사람은 결제 합계만 적고 공급가·부가세·원화는 서버가 계산한다
- 한 견적 줄이 지출결의 쪽과 카드 쪽에 동시에 잡히지 않는다(카드 쪽 입구)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `corp_card_usages` |
| 마이그레이션 | `db/migrations/NNNN_corp_card_usages.sql`(번호는 `pnpm db:generate`) |
| 게이트 규칙 | `card.dual-link-block`(카드 쪽 · 지출결의 쪽 두 방향 판정) |
| 함수(domain) | `createCardUsage` · `listCardUsages` · `listProjectCardUsages` · `splitCardTotal` · 연결 고르기 목록 DTO 함수 · `createOutOfQuoteLine` |
| 리포지토리 | `repositories/corp-card-usages.ts` · `repositories/quote-line-links.ts`(`findLineLinks`) |
| Server Action | `createCardUsageAction` · `previewCardAmountsAction` · 연결 고르기 목록 액션 |
| 화면 | `/cards` 목록·폼(`card-usage-list.tsx` · `card-usage-form.tsx` · `link-picker.tsx`) · 프로젝트 상세 `card-usage-section.tsx` |
| 테스트 | `test/unit/domain/{card-usage-amounts,rules-card-dual-link}.test.ts` · `test/integration/corp-card-usages.test.ts` · `test/e2e/card-usage.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md` when done
</output>
