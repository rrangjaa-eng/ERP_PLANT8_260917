---
phase: 06-payment-evidence-cards
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - db/schema/expense-payments.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - domain/rules/register.ts
  - domain/money/tax.ts
  - domain/payments/index.ts
  - domain/payments/action-row.ts
  - repositories/expense-payments.ts
  - app/(app)/expenses/[id]/page.tsx
  - app/(app)/expenses/[id]/payment-action-row.tsx
  - app/(app)/expenses/[id]/actions.ts
  - app/(app)/expenses/[id]/actions.registry.ts
  - test/unit/domain/money-tax.test.ts
  - test/unit/domain/payments.test.ts
  - test/integration/leak-scan.test.ts
  - test/integration/expense-payments-concurrency.test.ts
  - test/e2e/payment-single.spec.ts
autonomous: true
requirements: [EXP-06, EXP-09]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "지급 권한(`expenses.payments` write)이 있는 사람이 결재 통과 · 지급 전 지출결의 문서 화면에서 1차 「지급 완료」를 누르면 이체액 = 서버가 계산한 지급 총액으로 지급 기록이 저장되고, 같은 화면이 응답 상태로 「지출결의 상태 → 1차」 표를 다시 읽어 P6(`지급 완료` 태그, 1차 없음)으로 바뀐다(D-604)"
    - "결재 통과 전(결재 중·반려·회수) 문서는 `gate(doc, \"payment.approval-required\", { approvalState, stepName })`가 막고 이유는 `결재 통과 전 · {단계} 결재 중` 꼴이다 — 액션을 직접 불러도 같은 이유로 거부된다(EXP-06, 기준 1). 지출결의 자기 승인은 `approved`와 같은 통과다(UA-607)"
    - "지급 총액은 서버가 `applyTaxRule()`에 증빙 금액(없으면 승인액)과 두 기준일(`pickTaxDates` — 증빙일 또는 결재 통과일 · 지급일 > 지급 예정일 > 오늘 KST)을 넣어 계산하고, 브라우저가 보낸 지급 총액·공급가 역산 필드는 저장 경로에 들어가지 않는다 — `expectedPayableKrw`는 화면이 본 값이 낡았는지 가리는 비교값으로만 쓰인다"
    - "tx 규약: 트랜잭션 안에서는 `tx`를 받는 리포지토리와 순수 함수만 부른다. `can`·`visible`·`getSettingValue`(세율·반올림·문서 번호 서식 포함)·`recordAction`은 트랜잭션을 열기 전에 읽어 인자로 넘기거나(`recordAction`은 `deferRecord`/tx 인자 판) 트랜잭션 뒤로 뺀다. 근거: global-db-in-tx-audit, PR #75·#77."
    - "세율은 트랜잭션을 열기 전에 읽는다(ENG E-2 · CROSS E-1): `loadPaymentInputs`가 잠금 없는 사전 조회로 두 기준일 · 증빙 종류 세금 규칙을 정하고 `loadTaxRates(증빙일)` · `loadTaxRates(지급일)`을 읽는다 — `applyTaxRule`이 부가세는 증빙일(`domain/money/tax.ts:82-83`), 원천징수·회사 대납은 지급일(91-93 · 115-117)로 읽으므로 두 날짜 몫이고, 같은 날이면 한 번이다. 트랜잭션 안 재계산은 `decidePayable(input, rates)`(DB를 읽지 않는다)만 쓴다. 잠금 뒤 두 기준일 · 증빙 종류가 사전 조회와 다르면 처리하지 않고 `PayableChangedError`다"
    - "「지급 완료 동시 6건」 통합 테스트: 서로 다른 결재 통과 문서 `pool.options.max`+1건(기본 `DB_POOL_MAX` 5 → 6건, `lib/env.ts:54`)을 `Promise.all`로 지급 완료하면 타임아웃 없이 모두 끝나고 살아 있는 지급 기록이 6건이다 — 세율 읽기를 트랜잭션 안으로 되돌리면 이 테스트가 멈춰(타임아웃) 빨갛다"
    - "서버가 다시 계산한 지급 총액이 `expectedPayableKrw`와 다르면 처리하지 않고 새 지급 총액과 `지급일 {MM-DD} 기준 지급 총액 바뀜 · 이체액 확인`을 돌려준다(`PayableChangedError`)"
    - "부가세 규칙(`vat_surcharge`) 지급만 이체액에서 `grossFromTotal()`로 역산한 공급가를 저장하고 그 세율은 **증빙일 기준** 이력 설정 `TAX_VAT_RATE`다. 원천징수·규칙 없음·회사 대납은 역산 값을 저장하지도 보내지도 않는다(UA-619 · R-9 — 원천징수 역산 함수를 더하지 않는다)"
    - "원천징수 규칙 지급의 `applyTaxRule()` 호출은 증빙 종류 코드 `business_income`이면 `incomeType: \"business\"`, 그 밖이면 `\"other\"`다(UA-620)"
    - "지급 완료는 권한 판정 · 사전 조회(`loadPaymentInputs`)를 트랜잭션 전에 끝내고, 한 트랜잭션에서 ⑴ 지출결의 행을 `FOR UPDATE`로 잠그고(R-1) ⑵ 문서 version을 비교하고 ⑶ 결재 상태를 같은 `tx`로 읽어 게이트(뒤 플랜의 증빙 게이트 포함) ⑶' 기준 재판정 · 재계산(게이트보다 뒤 — CROSS-R1 F-3) ⑷ INSERT ⑸ 문서 version +1(R-2) 순서로 돌며, 행동 로그는 커밋 뒤에 남긴다 — 살아 있는 지급 기록은 부분 유니크 인덱스 `(expense_id) WHERE cancelled_at IS NULL`로 하나뿐이다"
    - "**전역 잠금 순서(N-3):** 프로젝트 행 → 견적 줄(id 오름차순, `lockQuoteLines`) → 지출결의 · 카드 사용 · 구매 요청 문서 행. 둘 이상을 잡는 경로는 이 순서로만 잡는다(Phase 4 `loadProjectForGate`가 프로젝트 → 줄 순서다). 이 플랜의 지급 완료는 지출결의 행 하나만 잡고, 그 뒤에 프로젝트 행 · 견적 줄을 잠그지 않는다. **N-2:** 지급 완료는 문서에 이은 견적 줄도 같은 `lockQuoteLines`로 `lockExpenseRow` **앞에서** 잡는다 — 그 함수는 06-07(W4)에서 생기고 막아야 할 `expense.line-paid-lock`은 06-13(W7)에서 생기므로, 이 호출은 06-13이 `completeExpensePayment` ⑴ 앞에 넣는다(06-13 Task 2)"
    - "문서 화면 행동 줄의 1차는 `resolveExpenseActionRow()` 한 함수가 정하고(이 플랜은 P0 · P4 · P6) 한 상태에 하나뿐이다 — 지급 권한이 없는 사람(대표 포함)에게는 이 페이즈의 1차·2차·3차가 비활성으로도 렌더되지 않고 담당 표기 `지급은 경영관리` 한 줄만 선다(D-601)"
    - "행동 로그: 지급 완료 = `payment_process` — 문서 화면을 열어 보기만 하는 것은 아무것도 남기지 않는다"
  prohibitions:
    - statement: "경영관리 권한이 없는 사람이 지급 완료를 대신할 수 있는 경로(다른 메뉴 권한으로 통과, 관리자 우회, 대표 버튼)를 만들어서는 안 된다(D-601)"
      status: resolved
      verification: test
    - statement: "브라우저가 보낸 지급 총액·공급가 역산 값이 저장돼서는 안 된다 — 서버 재계산값만 저장한다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/expense-payments.ts"
      provides: "지급 기록 표 — 지급일·이체액·지급 총액·차이·차이 사유·공급가 역산·방식·처리한 사람·취소 표시·version, 살아 있는 기록 하나 부분 유니크"
      exports: ["expensePayments"]
    - path: "domain/money/tax.ts"
      provides: "세율 사전 조회 — `applyTaxRule`이 읽는 설정 키 전부를 한 기준일로 읽은 평범한 객체 + 그 객체만 읽는 `deps.getSettingValue` 대용(06-05 카드가 같은 `TaxRates`를 쓴다)"
      exports: ["loadTaxRates", "TaxRates", "taxRatesReader"]
    - path: "domain/payments/index.ts"
      provides: "지급 총액 계산 · 두 기준일 · incomeType · 트랜잭션 전 사전 조회 · 지급 완료 · 지급 섹션 DTO"
      exports: ["completeExpensePayment", "getPaymentView", "pickTaxDates", "incomeTypeFor", "decidePayable", "loadPaymentInputs", "PayableChangedError"]
    - path: "test/integration/expense-payments-concurrency.test.ts"
      provides: "「지급 완료 동시 6건」 — 풀 소진 교착 회귀 가드(PR #75 결정적 재현 꼴). 06-04 · 06-06 · 06-10 · 06-15가 다시 돌린다"
      min_lines: 40
    - path: "domain/payments/action-row.ts"
      provides: "「지출결의 상태 → 1차」 판정(P0 · P4 · P6) + 결재 게이트 판정 순수 함수"
      exports: ["resolveExpenseActionRow", "approvalGateDecision"]
    - path: "repositories/expense-payments.ts"
      provides: "지급 기록 쓰기 · 지출결의 행 잠금(`FOR UPDATE` — 이 저장소의 첫 행 잠금 선례)"
      exports: ["lockExpenseRow"]
    - path: "test/e2e/payment-single.spec.ts"
      provides: "결재 통과 건 → 지급 완료 한 경로(06-04 · 06-06이 케이스를 더한다)"
      min_lines: 30
  key_links:
    - from: "app/(app)/expenses/[id]/actions.ts"
      to: "domain/payments/index.ts"
      via: "지급 액션이 domain 함수만 부르고 리포지토리를 직접 부르지 않는다"
      pattern: "completeExpensePayment"
    - from: "domain/rules/register.ts"
      to: "domain/payments/action-row.ts"
      via: "`payment.approval-required` 규칙이 화면 1차와 같은 판정 함수를 부른다"
      pattern: "approvalGateDecision"
    - from: "domain/payments/index.ts"
      to: "domain/money/tax.ts"
      via: "지급 총액이 applyTaxRule 한 함수에서만 나온다"
      pattern: "applyTaxRule"
    - from: "domain/payments/index.ts"
      to: "domain/money/tax.ts"
      via: "세율은 트랜잭션 전 `loadTaxRates`로 읽고, 트랜잭션 안 `decidePayable`은 `taxRatesReader`로 그 값만 읽는다(06-03 tx 규약)"
      pattern: "loadTaxRates"
    - from: "domain/payments/index.ts"
      to: "repositories/expense-payments.ts"
      via: "게이트 전에 지출결의 행을 잠근다(R-1)"
      pattern: "lockExpenseRow"
---

<objective>
경영관리(지급 권한자)가 결재를 통과한 지출결의 한 건을 **지급 완료**하는 단건 경로를 스키마 → 리포지토리(행 잠금) → 게이트 → 지급 총액 계산 → Server Action → 문서 화면 → 행동 로그까지 한 번에 꿴다. 이 플랜은 **지급 흐름의 트레이서**다. 증빙 게이트·지급 섹션 칸·취소는 06-04, 증빙 확인은 06-06이 붙인다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 뒤 플랜(일괄 지급 · 증빙 · 점검)이 같은 세 지점을 쓴다 — `rules.gate`의 지급 게이트, `applyTaxRule()` 기반 지급 총액, 「지출결의 상태 → 1차」 판정 함수. 이 셋을 층별로 쌓으면 "화면 1차와 서버 게이트가 서로 다른 표를 읽는다"가 마지막에야 드러난다. 옛 06-01(35파일 · 130k)이 체커 BLOCKER였다 — 이번에는 트레이서를 **결재 게이트 하나 · 기본 이체액 하나**로 좁혔다.

Output: `expense_payments` 표(신규 마이그레이션 1), 게이트 `payment.approval-required`, `domain/money/tax.ts`의 세율 사전 조회(`loadTaxRates` · `TaxRates` · `taxRatesReader`), `domain/payments`(지급 총액 · 기준일 · 사전 조회 · 지급 완료 · DTO), `action-row.ts`(P0 · P4 · P6), 문서 화면 행동 줄, 단위 1(+ money-tax 케이스) · 통합 1(「지급 완료 동시 6건」) · E2E 1.

**엔지니어링 검토 반영(ENG E-2 · E-3 · CROSS E-1):** 이 플랜이 페이즈 전체의 **tx 규약**(must_haves 첫머리)을 못 박는다 — 다른 플랜은 「06-03 tx 규약」으로 참조한다. 지급 완료가 트랜잭션 안에서 전역 `db`로 세율을 읽으면(`getSettingValue` → `repositories/settings.ts` `findEffectiveValue`, tx 인자 없음) 풀 크기(`DB_POOL_MAX` 기본 5)만큼 동시 지급이 서로의 커넥션을 기다리며 멈춘다 — PR #75(프로젝트 등록) · PR #77(환율)과 같은 꼴이다.

**규모 근거:** `files_modified` 17개 — 체커 Files/plan 표(`.claude/agents/gsd-plan-checker.md` Thresholds — 목표 5-8 · 경고 10 · 막음 15+)를 넘는다. 쪼개지 않는다(r3-2 오케스트레이터 결정 — 계획 게이트 막바지에 나누면 페이즈 전체의 wave · depends_on이 다시 섞인다). 17개의 무게: **테스트 5**(새 3 — `payments.test.ts` · `expense-payments-concurrency.test.ts` · `payment-single.spec.ts`, 기존 파일에 케이스 · import 한 줄 2 — `money-tax.test.ts` · `leak-scan.test.ts`), **등록 · 생성 4**(`db/schema/index.ts` re-export 한 줄 · `_journal.json` drizzle 생성 · `domain/rules/register.ts` 규칙 등록 하나 · `actions.registry.ts` 등록 하나), **실제 코드 8**(새 5 — 스키마 · `domain/payments` 둘 · 리포지토리 · `payment-action-row.tsx`, 기존 파일에 더함 3 — `domain/money/tax.ts` `loadTaxRates` · Phase 5 문서 화면 `page.tsx` 섹션 붙이기 · `actions.ts` 액션 하나). `estimate.tokens` 100000(`confidence: low`, 표본 0 — 보정 계수 1) — 예산 100k 경계다(세율 사전 조회 · 동시 6건 통합 테스트가 더해졌다). Task 1(트레이서)이 16개를 만진다 — 스키마부터 문서 화면까지 한 경로라 나누면 트레이서가 끝까지 닿지 않는다. **멈춤 방아쇠(보고하고 멈춘다 — 분할 제안과 함께, 실행자가 새 플랜을 스스로 만들지 않는다):** ⑴ ⓪ 또는 실행 중에 17개 밖 파일이 필요해지면(18번째 — 예: Phase 5 문서 화면의 섹션 자리가 `page.tsx` 밖 컴포넌트, Phase 5 액션 파일이 다른 경로) 그 파일을 고치기 전에 멈추고 `06-03 파일 한도 — {그 파일} 필요`와 분할 제안(⒜ 화면 몫 — ④ 액션 · 화면 · ⑥ E2E: `page.tsx` · `payment-action-row.tsx` · `actions.ts` · `actions.registry.ts` · `payment-single.spec.ts` — 을 바로 뒤 플랜으로 떼고 이 플랜은 통합 테스트로 경로를 닫는다 ⒝ Task 2를 떼어 낸다)을 보고한다 ⑵ Task 1 ⑥ E2E가 녹색이 되기 전에 컨텍스트가 60%를 넘으면 멈추고 「Task 2(단위 고정 · 「지급 완료 동시 6건」)를 새 플랜으로」 분할 제안을 SUMMARY와 보고에 적는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-RESEARCH.md
@.planning/phases/06-payment-evidence-cards/06-PATTERNS.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

이 플랜은 main에 아직 없는 코드 위에 선다. **Task 1 ⓪이 실행 시작 때 main에서 다시 확인하고, 하나라도 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-03 착수하지 않음`을 출력하고 멈춘다.** 아래 이름은 전부 가칭이다 — 실제 이름이 다르면 실제 이름을 쓰고 SUMMARY에 「가칭 → 실제」 표를 남긴다. 존재하지 않는 코드의 시그니처를 이 플랜이 지어내지 않는다.

| id | 먼저 들어와야 하는 것 | 재확인 명령(Task 1 ⓪) |
|---|---|---|
| A-606 | 04.1 결재 모듈 — `db/schema/approvals.ts`와 문서의 결재 상태를 읽는 조회 함수(가칭 `getApprovalView`). 게이트가 이 함수로 「통과 / 통과 전」과 현재 단계 이름을 얻는다 | `git ls-files db/schema/approvals.ts 'domain/approvals/*'` 비어 있으면 멈춤 |
| A-607 | Phase 5 지출결의 — `db/schema/expenses.ts`(가칭)의 상태·승인액·증빙 종류·지급 방식(UA-618)·지급 예정일(UA-617)·**`version`** 칸, `domain/expenses/`, 문서 화면 `app/(app)/expenses/[id]/page.tsx` | `git ls-files 'db/schema/*expense*' 'domain/expenses/*' 'app/(app)/expenses/[[]id]/page.tsx'` 셋 중 하나라도 비면 멈춤 · `grep -n "version" db/schema/expenses.ts` 0이면 멈춤(R-2가 문서 version을 쓴다) |
| A-608 | Phase 5 증빙 첨부 — 파일 표(가칭 `db/schema/files.ts`)와 증빙 금액(공급가)·증빙일 칸(UA-606) | `git ls-files 'db/schema/*file*'` 비면 멈춤 · `grep -rn "evidence" db/schema/` 0건이면 멈춤 |
| UA-607 | 지출결의 자기 승인이 `approved`와 같은 「통과」로 읽힌다 | A-606 조회 함수의 상태 열거를 읽어 확인, 다르면 SUMMARY에 적고 `approvalGateDecision` 표에 반영 |
| 06-01 · 06-02 | `app/(app)/status-display.ts` · 메뉴 키 `expenses.payments` · `diffKrw` | `git ls-files 'app/(app)/status-display.ts'` 비면 멈춤 · `grep -c "expenses.payments" domain/permissions/menus.ts` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-18 (닫힘) | 클라이언트는 금액 셈을 하지 않는다 — 지급 총액은 서버 응답 | 해당 없음 |
| UA-619 (계획 판단 · R-9) | 원천징수 역산 함수를 `domain/money`에 더하지 않는다. 부가세 역산 세율 = 증빙일 기준 `TAX_VAT_RATE` | `domain/payments/index.ts`의 역산 분기 하나 |
| UA-620 (계획 판단) | `business_income` → `"business"`, 그 밖 → `"other"` | `domain/payments/index.ts`의 `incomeTypeFor` |
| R-2 (교차 검토) | 지급 완료·취소가 지출결의 문서 version을 +1 한다 — 일괄 지급 스냅숏(06-15)이 이 version으로 낡음을 가린다 | `completeExpensePayment` · `cancelExpensePayment`(06-04)의 version 갱신 한 줄씩 |
| ENG E-2 · CROSS E-1 (계획 판단) | CROSS E-1의 두 방법 중 ⒜ 사전 조회를 쓴다(⒝ tx로 묶은 `getSettingValue`는 `findEffectiveValue`에 tx 인자를 새로 뚫어야 하고, 브리프의 공통 이름 `loadTaxRates`와 어긋난다). 잠금 뒤 기준이 바뀌면 다시 시도하지 않고 `PayableChangedError`로 사람이 이체액을 다시 보게 한다. `decidePayable`의 `rates`는 `{ atEvidence: TaxRates; atPayment: TaxRates }` 두 몫이다 — 공통 이름의 `TaxRates`는 한 기준일 값 그대로다(06-05 카드는 사용일 하나) | `domain/payments/index.ts`의 `loadPaymentInputs` · `completeExpensePayment` |

<assumption_delta_decision>
`gsd_run query assumption-delta`는 `detected: false`. 이 플랜은 지출결의·결재(Phase 5 · 04.1 명사) 위에 지급 기록 표를 **더한다** — 1차 명사를 옮기지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-06 · EXP-09 — `unclassified → unresolved (flagged, 자동 해소·기각 없음)`. 대신 쓰는 근거: 기준 1 · D-601 · D-604 · D-605. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 결재 통과 건 하나가 행 잠금 → 결재 게이트 → 지급 총액 → 저장 → 문서 화면 P6까지 한 경로로 관통한다</name>
  <files>db/schema/expense-payments.ts, db/schema/index.ts, db/migrations/meta/_journal.json, domain/rules/register.ts, domain/money/tax.ts, test/unit/domain/money-tax.test.ts, domain/payments/index.ts, domain/payments/action-row.ts, repositories/expense-payments.ts, app/(app)/expenses/[id]/page.tsx, app/(app)/expenses/[id]/payment-action-row.tsx, app/(app)/expenses/[id]/actions.ts, app/(app)/expenses/[id]/actions.registry.ts, test/unit/domain/payments.test.ts, test/integration/leak-scan.test.ts, test/e2e/payment-single.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 A-606 · A-607 · A-608 · 06-01 · 06-02가 main에 있다 — ⓪이 확인하고, 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「지출결의 상태 → 1차 (정본 …)」 절(P0~P6 정본) · 「금액 표시 (서버 계산 값)」 절(「기준일 고르기」)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「Copywriting Contract」 절 중 「Primary CTA — 지출결의 문서 화면」 · 「거부 — 문서 화면 동시성」 · 「표시 — 지급 방식 · 계좌」
    - .planning/phases/06-payment-evidence-cards/06-RESEARCH.md(Pattern 2 지급 총액 · Pitfall 1~4)
    - .planning/phases/06-payment-evidence-cards/06-PATTERNS.md(domain/revenue 역산 선례 · corp-cards 오류 클래스 선례)
    - domain/money/tax.ts(`applyTaxRule` 옵션 · 규칙 분기 — 설정을 읽는 줄 72-117과 `deps.getSettingValue` 주입 자리 65) · domain/money/index.ts(`grossFromTotal`) · domain/settings/keys.ts의 `TAX_VAT_RATE`(이력 설정 · 날짜 조회) · test/unit/domain/money-tax.test.ts(주입 선례)
    - domain/settings/registry.ts 68-85(`getSettingValue` — tx 인자 없음) · repositories/settings.ts 10-50(`findSimpleValue` · `findEffectiveValue`가 전역 `db`) · lib/db-transaction.ts(`withTransaction`) · db/client.ts(`DbOrTx`)
    - /mnt/project-files/deadlock-fix/global-db-in-tx-audit.md(트랜잭션 안 전역 `db` 목록 · `tx` 없는 리포지토리 목록) · PR #75 수정 커밋 `git show 70a39d4 -- domain/document-numbering/index.ts domain/projects/index.ts`(트랜잭션 전에 설정을 읽어 인자로 넘기는 꼴)
    - domain/rules/gate.ts(`gate(doc, ruleName, ctx)` 세 인자) · domain/rules/register.ts(`project.completed-lock` 등록 모양)
    - domain/permissions/can.ts · domain/revenue/index.ts 300-330행(`can(viewer, 메뉴, "write")` 호출 선례)
    - domain/action-log/record.ts(`recordAction` · `CORE_ACTION_TYPES`)
    - test/unit/leak-scan-coverage.test.ts · test/integration/leak-scan.test.ts 1-40행
    - app/(app)/projects/actions.ts · app/(app)/projects/actions.registry.ts(`authedActionClient` + 등록 선례)
    - db/migrations/0010_revenue_entries.sql 1-5행(SET LOCAL 한 쌍 선례)
    - test/e2e/fixtures.ts · test/e2e/revenue-section.spec.ts(E2E 데이터 준비 선례)
    - ⓪에서 확인한 Phase 5 · 04.1 실제 파일
  </read_first>
  <behavior>
    - `pickTaxDates`: 증빙 있음 → evidenceDate = 증빙일 / 증빙 없음 → 결재 통과일. paymentDate = 처리 때 고른 지급일 > 지급 예정일 > 오늘(KST — 인자로 받은 값)
    - `incomeTypeFor("business_income")` = "business", 그 밖의 원천징수 코드 = "other"
    - `loadTaxRates(asOf)`(money-tax 단위, `getSettingValue` 주입): `applyTaxRule`이 읽는 이력 키마다 `asOf`를 넘겨 한 번씩 읽고 평범한 객체를 돌려준다 / `taxRatesReader([rates])`로 주입한 `applyTaxRule`이 실제 설정 읽기와 같은 결과를 낸다 / 사전 조회에 없는 기준일을 물으면 던진다(조용히 전역 `db`로 새지 않는다)
    - 지급 총액 = `decidePayable(input, rates)` = `applyTaxRule(증빙 금액 ?? 승인액, 규칙, 기준일, { getSettingValue: taxRatesReader(...) })`의 payable — 증빙 금액이 있으면 승인액을 쓰지 않는다
    - `vat_surcharge`만 `grossFromTotal(이체액, 증빙일의 TAX_VAT_RATE)` 역산 값이 있고, 그 밖의 규칙은 null(원천징수 포함 — UA-619)
    - `approvalGateDecision({ approvalState, stepName })`: `approved`·자기 승인 → allowed / 결재 중 → `결재 통과 전 · {단계} 결재 중` / 반려·회수 → 막힘 + 이유
    - `resolveExpenseActionRow`: 결재 통과 전 → P0(이 페이즈 버튼 0) / 통과 · 지급 전 → P4(1차 `지급 완료`) / 지급 뒤 → P6(1차 없음) / 지급 권한 없음 → 버튼 0 + 담당 표기
  </behavior>
  <action>
⓪ **선행 의존 재확인(코드 변경 전).** 「선행 의존(A-6xx)」 표의 명령을 순서대로 돌린다. 하나라도 비면 `선행 의존 {id} 미해소 — {무엇} 없음, 06-03 착수하지 않음`을 출력하고 멈춘다. 모두 있으면 Phase 5 · 04.1의 실제 이름을 읽고 SUMMARY 「가칭 → 실제」 표에 적은 뒤 실제 이름으로 진행한다.

① **스키마(신규 표).** `db/schema/expense-payments.ts`: `expense_payments`(id · expense_id FK → Phase 5 지출결의 · pay_date date · transfer_krw bigint · payable_krw bigint(처리 때 지급 총액) · diff_krw bigint · diff_reason text null · gross_supply_krw bigint null(부가세 규칙만) · payment_method text(처리 때 방식 복사, UA-618) · processed_by FK · processed_at · cancelled_at null · cancelled_by null · cancel_reason null · version int · source 'demo'). 살아 있는 기록 하나는 `uniqueIndex().on(expense_id).where(cancelled_at IS NULL)`(`.unique()`가 아니라 `uniqueIndex()` — B-2). 차이가 0이 아니면 사유가 비지 않는다는 CHECK(신규 표라 NOT VALID 불필요). `db/schema/index.ts`에 export.
   **마이그레이션 절차(B-2, 신규 표):** `pnpm db:generate --name phase6_payments`(번호를 손으로 정하지 않는다) → 생성된 SQL 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` 한 쌍을 첫 락 요구 문장 앞에 둔다(0010 선례, 이유 주석 한 줄) → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 `pnpm db:generate`가 `No schema changes`.

② **리포지토리.** `repositories/expense-payments.ts`(viewer 첫 인자 규약): `lockExpenseRow(viewer, expenseId, tx)` — Drizzle `.for("update")`로 지출결의 행을 잠그고 게이트 입력(상태 · 승인액 · 증빙 금액·증빙일 · 증빙 종류 · 지급 방식 · 지급 예정일 · version)을 돌려준다(R-1 — 이 저장소의 첫 행 잠금 선례라 함수 머리 주석 한 줄에 이유를 적는다). `insertPayment` · `bumpExpenseVersion(expenseId, expectedVersion, tx)`(갱신 행 0이면 동시성 오류). 셋 다 `tx: DbOrTx`를 받는다. **결재 상태(06-03 tx 규약):** 게이트가 읽는 결재 상태 · 현재 단계 이름 · 결재 통과일은 잠금 뒤 같은 `tx`로 읽는다 — ⓪에서 A-606 조회 함수가 `tx` 인자를 받는지 보고, 받으면 그것을 `tx`와 함께 부르고, 받지 않으면 이 파일에 `tx`를 받는 좁은 조회(결재 표에서 그 세 값만, 가칭 `findApprovalGateInputs(viewer, expenseId, tx)`)를 둔다. 어느 쪽인지 SUMMARY에 적는다. `tx` 없는 A-606 함수를 트랜잭션 안에서 부르지 않는다.

③ **domain(06-03 tx 규약 — ENG E-2 · E-3 · CROSS E-1).**
   `domain/money/tax.ts`: `loadTaxRates(asOf)` — `applyTaxRule`이 읽는 설정 키 전부(기준일 종류 둘 · 부가세 세율 · 부가세 절사 단위 · 원천징수 기타/사업 세율 · 원천징수 절사 단위 · 최소 징수액 · 기타소득 면세 기준 · 회사 대납 세율 · 회사 대납 방식 — tax.ts 72-117)를 `getSettingValue(def, { asOf })`로 읽어 평범한 객체 `TaxRates`(`asOf` 날짜 문자열 포함)로 돌려준다. 전역 `db`를 읽는 함수라 트랜잭션 **밖**에서만 부른다. 절사 방식은 설정이 아니라 코드표 `rule.roundingMethod`(tax.ts:79)라 `TaxRates`에 넣지 않는다. `taxRatesReader(list: TaxRates[])` — `applyTaxRule`의 `deps.getSettingValue` 자리에 넣는 읽기 함수: 이력 키는 `asOf` 날짜가 같은 항목에서, 단순 키는 첫 항목에서 값을 돌려주고, 없으면 던진다. `applyTaxRule`의 시그니처 · 기본 동작은 바꾸지 않는다(revenue가 그대로 쓴다). 단위 테스트는 `test/unit/domain/money-tax.test.ts`에 behavior 셋째 줄(실패 → 구현 → 통과).
   `domain/payments/index.ts`: `pickTaxDates(doc, payDate, today)` · `incomeTypeFor`(UA-620) · `decidePayable(input, rates)` — input = 증빙 금액 ?? 승인액 · 세금 규칙 · 두 기준일 · incomeType · 이체액, rates = `{ atEvidence: TaxRates; atPayment: TaxRates }`(부가세는 증빙일, 원천징수·회사 대납은 지급일 — tax.ts 82-83 · 91-93 · 115-117). 지급 총액 = `applyTaxRule(…, { getSettingValue: taxRatesReader([rates.atEvidence, rates.atPayment]) })`의 payable, 공급가 역산은 `vat_surcharge`만 `grossFromTotal(이체액, rates.atEvidence.vatRate, rates.atEvidence.vatRoundingUnit, rule.roundingMethod)`(R-9 — 원천징수 역산은 만들지 않는다, UA-619). `decidePayable`은 DB를 읽지 않는다.
   `loadPaymentInputs(viewer, { expenseId, payDate? })` — **잠금 없는 사전 조회**: 문서(증빙 금액·증빙일·증빙 종류·지급 예정일 · 결재 통과일)를 읽고, 오늘(KST)을 여기서 한 번 정해 `pickTaxDates`로 두 기준일을 정하고, 증빙 종류의 세금 규칙(코드표 — `tx` 없는 리포지토리)과 `loadTaxRates(증빙일)` · `loadTaxRates(지급일)`(같은 날이면 한 번)을 읽어 `{ today, taxDates, evidenceTypeCode, taxRule, rates }`로 돌려준다.
   `completeExpensePayment(viewer, { expenseId, payDate?, expectedPayableKrw, version })`: ⑴ `can(viewer, "expenses.payments", "write")` 거짓이면 `ForbiddenError`(D-601) ⑴' `loadPaymentInputs` — 트랜잭션을 열기 **전** ⑵ `withTransaction` 한 트랜잭션(전역 잠금 순서 N-3 — `lockExpenseRow` 앞은 06-13이 견적 줄 잠금을 넣는 자리이고, 문서 행 뒤에는 프로젝트 행 · 견적 줄을 잠그지 않는다): `lockExpenseRow` → version 비교 → 결재 상태 `tx` 조회(②) → `gate(doc, "payment.approval-required", { approvalState, stepName })` → (06-04가 여기에 증빙 게이트를 더한다) → 잠근 행과 사전 조회의 `today`로 `pickTaxDates` · 증빙 종류를 다시 정해 사전 조회와 하나라도 다르면 `PayableChangedError` → `decidePayable(lockedInput, pre.rates)`. **판정 순서 고정(CROSS-R1 F-3):** 게이트(결재 · 증빙)는 기준 재판정보다 **앞**이다 — 증빙이 사라지면 증빙일이 결재 통과일로 바뀌어 기준 재판정도 걸리는데, 사람에게는 `증빙 없음 · 담당 PM {이름}`(게이트 문구)이 먼저 가야 한다. `PayableChangedError`는 게이트를 모두 통과한 문서에만 나온다, `expectedPayableKrw`와 다르면 `PayableChangedError`(새 값 포함) → 이체액 = 지급 총액(이 플랜에는 이체액 칸이 없다 — 칸과 차이 사유는 06-04) → INSERT → `bumpExpenseVersion`(R-2) ⑶ 커밋 뒤 `recordAction` `payment_process`. 기준이 바뀌어 되돌린 경우의 새 지급 총액은 트랜잭션 **밖에서** `loadPaymentInputs`를 다시 읽고 `decidePayable`로 셈해 오류에 싣는다(다시 시도하지 않는다). 트랜잭션 콜백 안에서는 `tx`를 받는 리포지토리와 `gate` · `decidePayable` · `pickTaxDates` · `diffKrw` 같은 DB 없는 함수만 부른다. `getPaymentView(viewer, expenseId)` DTO는 `registerDto`로 등록하고 지급 총액은 `loadPaymentInputs` → `decidePayable`로 셈한다(읽기 경로, 트랜잭션 없음).
   `domain/payments/action-row.ts`: 순수 함수 `approvalGateDecision({ approvalState, stepName })`와 `resolveExpenseActionRow(state, perms)`(P0 · P4 · P6만 — P1 · P2 · P3 · P5는 06-04 · 06-06). `domain/rules/register.ts`에 `payment.approval-required`를 등록하고 규칙 몸통은 `approvalGateDecision`을 부르기만 한다(R-8 ctx = `{ approvalState, stepName }`, 화면 1차와 같은 판정 — 조건을 두 곳에 쓰지 않는다).

④ **액션 · 화면.** `app/(app)/expenses/[id]/actions.ts`(Phase 5 파일이 있으면 확장, 없으면 신규)에 `completeExpensePaymentAction`(zod 입력 = expenseId · payDate? · expectedPayableKrw · version — 지급 총액·역산 필드 없음). `actions.registry.ts`에 등록하고, 새 파일이면 `test/integration/leak-scan.test.ts`에 그 registry와 `@/domain/payments` import 한 줄씩. `payment-action-row.tsx`: `resolveExpenseActionRow` 결과만 렌더(1차 하나 · 처리 중 `…`), 권한 없는 사람은 담당 표기 `지급은 경영관리`. 상태 태그 `지급 완료`는 `statusDisplay`로만. Phase 5 문서 화면(`page.tsx`)에 결재 통과 문서일 때만 행동 줄을 붙인다(P0 = 이 페이즈 버튼 없음). 숫자는 `.num`, 토큰은 `tokens.css`의 것만.

⑤ **단위.** `test/unit/domain/payments.test.ts`에 behavior 중 `loadTaxRates` 줄을 뺀 여섯 줄, `test/unit/domain/money-tax.test.ts`에 그 한 줄(실패 → 구현 → 통과).

⑥ **E2E 한 경로.** `test/e2e/payment-single.spec.ts`: `expenses.payments` write 계정 · 결재 통과 지출결의(증빙 0, `evidence.required` = false로 준비 — 06-04가 증빙 게이트를 붙여도 이 케이스가 녹색이게)를 Phase 5 · 04.1의 **domain 함수로** 만든다 → 문서 화면 → 1차 `지급 완료` → `지급 완료` 태그 · 1차 없음 → 행동 로그에 `payment_process` 한 줄. 대표 계정 케이스: 같은 문서에 버튼 요소 0 + `지급은 경영관리`.

**중간 검증 지점:** ⑥이 `CI=true`로 녹색이 된 뒤에만 Task 2로 넘어간다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments.test.ts test/unit/domain/money-tax.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — leak-scan-coverage가 빨가면 새 registry가 누수 스캔에 import되지 않았고, money-tax가 빨가면 `loadTaxRates` · `taxRatesReader`가 `applyTaxRule`의 실제 설정 읽기와 어긋나거나 기존 `applyTaxRule` 동작이 바뀌었다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나, squawk 출력에 0이 아닌 `Found N issues`가 있거나, 마지막 `db:generate` 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>eslint·stylelint·tsc·next build 중 하나라도 error로 끝난다. `plant8/money-boundary` 위반은 금액 셈이 금액 모듈 밖에 있다는 뜻이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>playwright 요약의 failed가 1 이상이거나 종료 코드가 0이 아니거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪의 재확인 결과(있음/없음, 가칭 → 실제)가 SUMMARY에 표로 있다
    - `grep -c "payment.approval-required" domain/rules/register.ts`가 1 이상
    - `grep -c "SET LOCAL lock_timeout" db/migrations/*phase6_payments*.sql`이 1
    - `completeExpensePaymentAction`의 zod 입력 스키마에 지급 총액·역산 필드 이름이 없다(`expectedPayableKrw`는 비교값으로만 있다)
    - E2E에 대표 계정 「버튼 0 + 담당 표기」 케이스가 있다(`getByRole("button")` 개수로 단언)
    - 06-03 tx 규약: `completeExpensePayment`의 `withTransaction` 콜백 줄 범위를 SUMMARY에 적고, `sed -n '{시작},{끝}p' domain/payments/index.ts | grep -v '^\s*//' | grep -cE '\b(can|visible|getSettingValue|loadTaxRates|loadPaymentInputs|recordAction|applyTaxRule)\('` 결과 0을 함께 적는다(콜백 밖 같은 파일의 정상 호출은 세지 않는다 — 범위 한정)
    - 결재 상태를 `tx`로 읽는 방법(A-606 함수에 `tx` 전달 / 좁은 조회 신설)이 SUMMARY에 있다
  </acceptance_criteria>
  <done>결재 통과 문서 하나가 스키마 → 권한 · 세율 사전 조회(트랜잭션 전) → 행 잠금 → 결재 게이트 → 지급 총액 계산 → Server Action → 문서 화면 → 행동 로그(커밋 뒤)를 실제로 통과해 `지급 완료`가 되고, 단위 1 · E2E 1이 CI=true로 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 조작 페이로드 · 낡은 지급 총액 · 부가세 역산 세율 · 잠금 뒤 기준 바뀜을 단위로, 「지급 완료 동시 6건」을 통합으로 못 박는다</name>
  <files>domain/payments/index.ts, test/unit/domain/payments.test.ts, test/integration/leak-scan.test.ts, test/integration/expense-payments-concurrency.test.ts</files>
  <read_first>
    - Task 1이 만든 domain/payments/index.ts · domain/money/tax.ts(`loadTaxRates` · `taxRatesReader`)
    - domain/settings/keys.ts의 `TAX_VAT_RATE` 이력 조회 함수(날짜 인자)
    - test/integration/leak-scan.test.ts(DTO 계급별 검사 방식)
    - PR #75 회귀 테스트 `git show origin/claude/fix-project-create-deadlock-8wybyd:test/integration/projects-create-concurrency.test.ts`(main에 들어왔으면 그 파일) — 풀 밖 `pg` Client로 행을 먼저 잠그고 `pg_blocking_pids` 재귀로 풀 커넥션이 전부 막힌 것을 확인한 뒤 풀어 주는 결정적 재현 꼴, 10초 `Promise.race`, `finally`의 `Promise.allSettled`
    - test/integration/document-counters-concurrency.test.ts(`pool.options.max` 실측 단언 선례) · lib/env.ts 54행(`DB_POOL_MAX` 기본 5)
  </read_first>
  <behavior>
    - 부가세 역산 세율: 증빙일이 세율 변경 전이면 변경 전 세율, 뒤면 뒤 세율(이력 설정 대역 두 값)
    - 원천징수 규칙(`business_income` · 그 밖)은 역산 값 null이고 `incomeType`만 다르다
    - `decidePayable`에 지급 총액·역산 필드를 담은 입력 객체를 넘겨도 결과가 재계산값과 같다(필드를 읽지 않는다)
    - `expectedPayableKrw`가 재계산값과 1원이라도 다르면 `PayableChangedError`에 새 값이 실린다
    - 증빙일과 지급일이 세율 변경일을 사이에 두면 부가세는 증빙일 몫, 원천징수는 지급일 몫 세율로 셈한다(`rates.atEvidence` · `rates.atPayment`)
    - 잠금 뒤 판정: 사전 조회의 증빙일(또는 증빙 종류)과 잠근 행의 값이 다르면 `PayableChangedError`(주입한 잠근 행으로 단위 확인) — 단, 결재 게이트가 막는 문서는 기준이 달라도 게이트 이유가 나온다(게이트가 기준 재판정보다 앞 — CROSS-R1 F-3)
    - 「지급 완료 동시 6건」(통합): 서로 다른 결재 통과 문서 `pool.options.max`+1건을 `Promise.all`로 지급 완료 → 10초 안에 모두 끝나고 살아 있는 `expense_payments` 6건 · 문서 version 각 +1
  </behavior>
  <action>
behavior 앞 여섯 줄을 `test/unit/domain/payments.test.ts`에 단위 테스트로 고정한다(`decidePayable`은 Task 1에서 섰다 — 테스트가 드러내는 결함만 고치고 요청받지 않은 리팩터는 하지 않는다). 부가세 역산 세율은 증빙일의 `TAX_VAT_RATE` 이력 값이라는 것(R-9)을 테스트 이름에 적는다. `test/integration/leak-scan.test.ts`가 `getPaymentView` DTO를 계급별로 검사하는 케이스(지급 권한 없는 계급에 이체액·계좌 칸이 없는지)를 더한다 — 계좌 노출 규칙(O-17)은 06-20이 채우므로 이 케이스는 금액 칸만 본다.

**「지급 완료 동시 6건」(ENG E-2 · CROSS E-1 — 06-04 통합이 아니라 여기 한 곳).** `test/integration/expense-payments-concurrency.test.ts`(신규)를 PR #75 회귀 테스트 꼴로 쓴다: ⑴ `pool.options.max`가 2 이상임을 먼저 단언하고 N = `pool.options.max` + 1(기본 6) ⑵ 결재 통과 · 지급 전 지출결의 N건을 Phase 5 · 04.1 **domain 함수로** 만든다(`evidence.required` = false, 증빙 0 — 06-04가 증빙 게이트를 붙여도 녹색이게, 06-03 E2E 준비와 같은 방식) ⑶ 풀 밖 `pg` Client가 `BEGIN` 뒤 그 N개 지출결의 행을 `FOR UPDATE`로 잡는다 ⑷ `completeExpensePayment` N건을 `Promise.all`로 띄우고, `pg_blocking_pids` 재귀 조회로 이 Client에 막힌 풀 커넥션 수가 `pool.options.max`에 이를 때까지 폴링한다(약 4초 상한 — 못 이르면 「교착 조건을 만들지 못했다」로 실패) ⑸ Client `COMMIT` → `Promise.race([Promise.all(calls), 10초 타임아웃])` ⑹ N건 모두 성공 · 살아 있는 지급 기록 N건 · version 각 +1을 DB로 단언 ⑺ `finally`에서 Client `ROLLBACK`/`end` · `Promise.allSettled(calls)`. 테스트 이름에 「지급 완료 동시 6건」을 넣는다. 타이밍에만 기대는 「쏘고 기다리기」는 쓰지 않는다(PR #75 주석의 이 프로젝트 규약).
**RED 확인(순서 고정):** 테스트를 먼저 쓰고, `loadPaymentInputs`의 두 `loadTaxRates` 호출을 잠시 트랜잭션 콜백 안(`lockExpenseRow` 뒤)으로 옮겨 이 테스트가 10초 타임아웃으로 빨간 것을 본다 → 그 출력 한 줄을 SUMMARY에 적는다 → 되돌려 녹색을 본다. 되돌린 뒤 `git diff`에 그 임시 이동이 남지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/expense-payments-concurrency.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 「지급 완료 동시 6건」이 10초 타임아웃으로 빨가면 트랜잭션 안에서 전역 `db`(세율 · 권한 · 설정 · 결재 조회)를 다시 읽는다(06-03 tx 규약 위반), 「교착 조건을 만들지 못했다」로 빨가면 풀 커넥션이 잠금 대기에 들어가지 못했다(준비 오류), leak-scan이 빨가면 지급 DTO가 권한 없는 계급에 금액을 흘렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 세율 변경 전후 두 케이스 · 원천징수 두 코드 케이스 · 조작 필드 무시 케이스 · 낡은 값 케이스 · 두 기준일 세율 케이스 · 잠금 뒤 기준 바뀜 케이스가 개별 `it`이다
    - leak-scan에 지급 DTO 케이스가 있고 녹색이다
    - `grep -c "동시 6건" test/integration/expense-payments-concurrency.test.ts`가 1 이상이고, 테스트 본문이 `pool.options.max`를 읽는다(6을 숫자로 박지 않는다)
    - SUMMARY에 RED 확인 출력 한 줄(세율 읽기를 트랜잭션 안으로 옮겼을 때의 타임아웃)과 되돌린 뒤 녹색 결과가 있다
  </acceptance_criteria>
  <done>지급 총액이 서버 재계산값뿐이고 부가세 역산 세율이 증빙일 기준이며 원천징수는 역산하지 않고, 세율이 트랜잭션 밖에서 읽혀 동시 지급이 풀 크기를 넘어도 멈추지 않는다는 것이 테스트로 고정된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Server Action | 지급일 · `expectedPayableKrw` · version이 넘어온다. 지급 총액·공급가 역산은 이 경계를 넘어와도 저장 경로에 들어가지 않는다 |
| app/ → domain/ | 권한(`expenses.payments`)·게이트 판정이 domain 함수 안에 있어 액션을 직접 불러도 우회되지 않는다 |
| domain → DB(트랜잭션) | 행 잠금 · 게이트 · 재계산 · INSERT · version 갱신이 한 트랜잭션이다. 권한 · 세율 · 세금 규칙 읽기는 그 앞, 행동 로그는 그 뒤다(06-03 tx 규약) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Elevation of Privilege | `completeExpensePayment` | high | mitigate | domain 첫 줄 `can(viewer, "expenses.payments", "write")`, 다른 메뉴 권한 대신 통과 없음(D-601). 대표 계정 E2E 버튼 0 |
| T-06-09 | Tampering | 지급 액션 입력 | high | mitigate | zod 입력에 지급 총액·역산 필드가 없고 서버가 `applyTaxRule`로 재계산. `expectedPayableKrw` 불일치면 처리 안 함. 조작 필드 무시 단위 테스트 |
| T-06-10 | Tampering | 이중 지급(경합) | high | mitigate | 지출결의 행 `FOR UPDATE`(R-1) + 부분 유니크 인덱스 + 문서 version(R-2). 병렬 두 요청 통합 테스트는 06-04 |
| T-06-11 | Elevation of Privilege | 게이트 우회 | high | mitigate | 게이트 판정이 domain 함수 안 `gate()` 한 지점. 규칙 몸통은 화면 1차와 같은 순수 함수를 부른다. 등록 안 된 규칙 이름은 `UnknownGateRuleError`(기존 동작) |
| T-06-12 | Repudiation | 지급 완료 | medium | mitigate | `payment_process` 행동 로그에 처리한 사람·이체액·지급 총액 |
| T-06-13 | Information Disclosure | 지급 DTO | medium | mitigate | `registerDto` 등록 + 누수 스캔 계급별 케이스(Task 2) |
| T-06-14 | Denial of Service | 신규 표 마이그레이션 | low | mitigate | 신규 표라 기존 행 스캔이 없지만 SET LOCAL lock_timeout · statement_timeout 한 쌍을 둔다(0010 선례), `pnpm lint:sql` 0건 |
| T-06-14a | Denial of Service | 지급 완료 트랜잭션의 커넥션 풀 소진(ENG E-2 · CROSS E-1) | high | mitigate | 세율 · 세금 규칙 · 권한을 트랜잭션 전 `loadPaymentInputs` · `can`으로 읽고, 트랜잭션 안은 `tx` 리포지토리와 `decidePayable`만(06-03 tx 규약). 결재 상태도 `tx`로 읽는다. 「지급 완료 동시 6건」 통합 테스트(PR #75 결정적 재현 꼴, RED 확인 포함) |
| T-06-14b | Tampering | 사전 조회와 잠금 사이 기준 바뀜 | medium | mitigate | 잠금 뒤 두 기준일 · 증빙 종류를 다시 정해 사전 조회와 다르면 처리하지 않고 `PayableChangedError`(새 값은 트랜잭션 밖에서 셈) — 낡은 세율로 저장되지 않는다. 단위 케이스 |
</threat_model>

<verification>
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- 단위 payments · money-tax · leak-scan-coverage, 통합 leak-scan · expense-payments-concurrency(「지급 완료 동시 6건」, RED 확인 기록 포함), E2E payment-single이 CI=true로 녹색
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트(lint·typecheck·build) → 실행자가 아닌 별도 에이전트가 `CI=true`로 문서 화면 행동 줄 DOM 실측 감사(권한별 버튼 수 · 담당 표기) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 결재 통과 문서 하나가 지급 완료까지 한 경로로 실제로 돈다
- 결재 미통과 막힘이 `rules.gate` 한 지점에서 나오고 화면 1차 판정과 같은 함수를 읽는다
- 지급 총액이 서버 재계산값뿐이고, 부가세 역산만 있고 원천징수 역산은 없다
- 트랜잭션 안에서 전역 `db`를 읽지 않아 풀 크기를 넘는 동시 지급이 멈추지 않는다(06-03 tx 규약)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `expense_payments`(부분 유니크 `(expense_id) WHERE cancelled_at IS NULL`) |
| 마이그레이션 | `db/migrations/NNNN_phase6_payments.sql`(번호는 `pnpm db:generate`, SET LOCAL 한 쌍) |
| 게이트 규칙 | `payment.approval-required`(ctx `{ approvalState, stepName }`) |
| 함수(domain) | `completeExpensePayment` · `getPaymentView` · `pickTaxDates` · `incomeTypeFor` · `decidePayable` · `loadPaymentInputs` · `resolveExpenseActionRow`(P0 · P4 · P6) · `approvalGateDecision` |
| 함수(money) | `loadTaxRates(asOf)` · `taxRatesReader` · 타입 `TaxRates`(`domain/money/tax.ts` — 06-05 `splitCardTotal(total, rates)`가 같은 타입을 쓴다) |
| 함수(repo) | `lockExpenseRow`(`FOR UPDATE`) · `bumpExpenseVersion` · (A-606 함수가 `tx`를 받지 않으면) 결재 상태 `tx` 조회 |
| 오류 클래스 | `PayableChangedError` |
| Server Action | `completeExpensePaymentAction` |
| 화면 | `app/(app)/expenses/[id]/payment-action-row.tsx` |
| 규약 | 06-03 tx 규약(must_haves) — 페이즈 전체가 「06-03 tx 규약」으로 참조 |
| 테스트 | `test/unit/domain/payments.test.ts` · `test/unit/domain/money-tax.test.ts` 케이스 · `test/integration/expense-payments-concurrency.test.ts`(「지급 완료 동시 6건」) · `test/e2e/payment-single.spec.ts` · leak-scan 케이스 |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG E-2 지급 완료 재계산이 트랜잭션 안에서 세율을 읽는다(eng-review.md L31@333f7bf) | 고침 | Plan 06-03, Task 1 ③: `loadPaymentInputs`(잠금 없는 사전 조회 — 두 기준일 · 세금 규칙 · `loadTaxRates`)를 트랜잭션 전 ⑴'에 두고, 트랜잭션 안 재계산은 `decidePayable(input, rates)`만. 잠금 뒤 기준 바뀜 → `PayableChangedError`. must_haves 「세율은 트랜잭션을 열기 전에 읽는다」 |
| ENG E-3 전역 규약을 한 곳에 못박기(eng-review.md L35@333f7bf) | 고침 | must_haves.truths에 공통 문장 「tx 규약: …」을 그대로 넣었다. 다른 플랜은 「06-03 tx 규약」으로 참조한다. Task 1 acceptance에 콜백 범위 한정 grep |
| CROSS E-1 지급 완료 트랜잭션 안 전역 `db` 세율 읽기 → 풀 고갈 교착(eng-cross-opus.md L12@333f7bf) | 막음 | Task 1 ③(⒜ 방법 — 열린 선택 표에 이유) · T-06-14a · Task 2 「지급 완료 동시 6건」 통합 테스트(PR #75 결정적 재현 꼴, RED = 세율 읽기를 트랜잭션 안으로 옮기면 타임아웃). 06-04가 아니라 여기 한 곳 — RED가 순서상 먼저다. 06-04 · 06-06 · 06-10 · 06-15가 이 파일을 다시 돌린다 |
| (코드 확인 중 발견) 결재 상태를 A-606 조회 함수로 트랜잭션 안에서 읽으면 같은 교착 꼴 | 고침 | Task 1 ②: 결재 상태 · 단계 이름 · 결재 통과일은 잠금 뒤 같은 `tx`로 — A-606 함수가 `tx`를 안 받으면 `tx` 받는 좁은 조회. 증빙 종류 세금 규칙(코드표, `tx` 없는 리포지토리)은 사전 조회로 |
| 브리프 공통 이름 `decidePayable(input, rates: TaxRates)` | 조정 | 이름은 그대로, `rates`는 `{ atEvidence: TaxRates; atPayment: TaxRates }` 두 몫 — `applyTaxRule`이 부가세는 증빙일(tax.ts:82-83), 원천징수 · 회사 대납은 지급일(91-93 · 115-117)로 읽기 때문. `loadTaxRates(asOf)` · `TaxRates`는 한 기준일 그대로라 06-05 `splitCardTotal(total, rates)`와 맞다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| CROSS E-1 방법 ⒝(`applyTaxRule` deps에 tx로 묶은 `getSettingValue`) | 채택하지 않음 — `repositories/settings.ts` `findEffectiveValue`에 tx 인자를 새로 뚫어야 하고, 페이즈 공통 이름(`loadTaxRates` 사전 조회)과 두 갈래가 된다 |

### Round 2 — 333f7bf(+미커밋 반영분 · /mnt/project-files/06-gates/eng-apply-cross-r1.md · eng-apply-checker-r1.md)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS-R1 F-3 잠금 뒤 판정 순서 미고정 — 증빙이 사라지면 기준 재판정(`PayableChangedError`)이 게이트 문구보다 먼저 나올 수 있다 | 고침 | must_haves ⑶ · ⑶′ 줄과 Task 1 ③ `completeExpensePayment`에 「게이트(결재 · 06-04 증빙)는 기준 재판정보다 앞」을 고정. Task 2 behavior 「잠금 뒤 판정」에 게이트 우선 단서 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| CROSS-R1 R-3 · R-4(참고 — `taxRatesReader` 날짜 비교 `dateOnly` · `loadTaxRates` 순차 읽기) | 이번 라운드 배정(F-3 · F-4 · W1) 밖이라 넣지 않았다 — 구현 메모 수준, 다음 라운드에서 받으면 Task 1 ③ 한 줄 |

### Round 3 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-6 — read_first의 UI-SPEC rev 8 줄 번호가 남았다(06-03:170-171 「354-383행」 · 「819-850행」 · 「238-350행」 — 체커 W4 잔여) | 고침 | Task 1 read_first 두 줄을 절 이름으로: 「지출결의 상태 → 1차 (정본 …)」 절 P0~P6 · 「금액 표시 (서버 계산 값)」 절 「기준일 고르기」 · 「Copywriting Contract」 절 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 4 — 체커 r3 경고 반영(r3-2)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 r3 scope_sanity — `files_modified` 17개가 Files/plan 한도(15 미만)를 넘는데 넘는 이유가 플랜에 없다(06-11이 「17개인 06-03도 경고 0」이라 적고 넘어감) | 경고 | 쪼개지 않음(오케스트레이터 r3-2 결정 — 늦은 분할은 페이즈 wave · depends_on을 다시 섞는다). 「규모 근거」를 고쳐 체커 표를 인용하고 17개의 무게(테스트 5 · 등록 · 생성 4 · 실제 코드 8)와 estimate, 트레이서를 나누지 않는 이유, 멈춤 방아쇠 둘(⑴ 18번째 파일 → 고치기 전에 멈추고 `06-03 파일 한도 — {그 파일} 필요` + 분할 제안 ⒜ 화면 몫 / ⒝ Task 2 ⑵ 컨텍스트 60% → 멈추고 Task 2 분할 제안)을 적었다. 옛 방아쇠 「Task 2를 새 플랜으로 넘기고」는 「멈추고 제안」으로 바꿨다 |
| (재확인 중 발견) Task 1 action ② · ⑤가 `domain/money/tax.ts` · `test/unit/domain/money-tax.test.ts`를 고치는데 Task 1 `<files>`에 없었다 — frontmatter에는 있다 | 참고 | Task 1 `<files>`에 두 파일을 더했다(16개). `files_modified`는 그대로 17 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 체커 fix_hint 「나누거나」 | 나누지 않음 — 오케스트레이터 r3-2 결정. 방아쇠가 걸리면 그때 분할 제안으로 돌아온다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md` when done
</output>
