---
phase: 06-payment-evidence-cards
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/rights.ts
  - domain/corp-card-usages/link-targets.ts
  - app/(app)/cards/page.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/link-picker.tsx
  - app/(app)/cards/actions.ts
  - app/(app)/cards/actions.registry.ts
  - test/unit/domain/card-usage-rights.test.ts
  - test/integration/corp-card-usages-proxy.test.ts
  - test/e2e/card-proxy.spec.ts
autonomous: true
requirements: [EXP-16, EXP-07]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "대리 등록 권한(`cards.proxy` write)이 있는 사람이 `/cards?new=1`에서 활성 카드 전부 중 남의 카드(또는 팀 카드)를 고르면 카드 아래 `경영관리 등록 · 카드 소지자 {이름}`이 서고, 저장된 건은 목록·프로젝트 상세의 등록 칸에 `경영관리 등록` + 2행 등록자·날짜로 보인다(EXP-16)"
    - "대리 등록 권한이 없는 사람이 남의 카드로 저장을 요청하면 서버가 거부한다 — 화면 옵션을 조작해도 같다(EXP-16, D-601 결)"
    - "대리 등록은 결재가 없고 행동 로그에 `document_create`(등록 경로 proxy)로 남는다(D-608) — 담당 PM 알림은 Phase 7이다"
    - "대리 등록으로 팀 카드를 고르고 `팀 비용`에 이으면 `사용한 사람` 칸이 서고, 후보는 **사용일에** 그 카드 팀 소속이던 사람 전부(`teamAtDate` = 카드 팀)다 — 사용일 뒤 옮긴 사람은 `{이름} · 지금 {팀}`, 퇴사자는 `{이름} · 퇴사`로 들고, 사용일 뒤 들어온 사람은 없다"
    - "개인 카드의 대리 등록은 사용한 사람 후보가 소지자 한 사람이라 입력이 아니라 텍스트다"
    - "팀 비용의 팀은 사용한 사람의 사용일 소속으로 서버가 정한다 — 소속이 없으면 1차가 막히고 임의 팀으로 떨어지지 않는다(EXP-07)"
    - "완료(정산) 프로젝트는 대리 등록 권한자에게만 `견적 외 비용` 연결로 고를 수 있고(D-47), 견적 줄 연결은 여전히 고를 수 없다"
    - "[O-11 기본값] 카드 사용 건의 수정·삭제는 등록한 사람과 대리 등록 권한자만 할 수 있고, 완료 프로젝트 줄에 이어진 건은 대리 등록 권한자만, 구매 완료로 생긴 건(`registered_via = purchase`)은 연결을 바꿀 수 없고 삭제가 없다 — 판정은 `domain/corp-card-usages/rights.ts` 한 함수에서 나온다"
    - "수정 모드(`?editId=`)의 카드는 그 건에 저장된 카드의 읽기 텍스트다 — 여는 사람의 카드 옵션을 계산하지 않으므로 옵션이 0장이어도 폼이 서고, 카드 값을 바꿔 보낸 수정 요청은 서버가 거부한다(구매 완료 건의 카드 고치기는 06-06)"
    - "수정 저장은 결제 합계·증빙 종류가 바뀌면 공급가·부가세를 서버가 다시 역산하고, 연결을 바꾸면 `card.dual-link-block`을 다시 지난다 — `version`이 어긋나면 거부된다"
    - "S9 error: 「Error — 카드 사용 폼 칸」 · 「막힘 — 카드 사용 폼」(서버 이중 연결 거부 · 소속 없음 · 수정 모드 증빙 종류 포함) · 「막힘 — 카드 사용 폼 · 증빙 종류 자동 채움 없음」이 각 자리에 선다"
    - "S9 partial: 입력이 있는데 `취소 Esc`면 「확인 — 입력 버리기」가 열리고, 대리 등록 팀 카드에서 사용일을 바꿔 고른 사람이 새 후보에 없으면 칸이 빈다(빈 칸 막힘), 수정 모드에서 저장된 증빙 종류가 지금 옵션에 없으면 `—` + 막힘이다"
    - "S9 zero-one-many: 새 건 카드 0장 = 폼 대신 「Empty — 카드 사용 목록」 카드 0장 갈래 · 1장 = 텍스트 · 여러 장 = `Select` / 수정 모드 카드 = 저장된 카드 읽기 텍스트(0장 갈래로 바뀌지 않음) / 사용한 사람 후보 1명 = 텍스트 · 여럿 = `Select` 기본값 없음 / 증빙 종류 옵션 0개 = `카드에 쓸 증빙 종류 없음 · 코드표 세금 규칙은 관리자`"
    - "수정 모드 2차 `카드 사용 삭제`는 확인 모달을 거쳐 지우고 행동 로그 `document_delete`를 남긴다 — 구매 완료로 생긴 건에는 이 버튼이 렌더되지 않는다"
    - "목록 행을 누르면 등록한 사람·대리 등록 권한자는 `?editId=`로, 그 밖의 사람은 폰 행 시트(보기 전용)로 간다 — 등록 필터(`경영관리 등록`)는 권한자에게만 보인다"
    - statement: "S9 overflow: 활성 카드 30장 이상인 대리 등록 권한자의 카드 `Select`(200)와 긴 카드 이름이 말줄임으로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S9 long-text: 메모·`견적 외 비용` 항목 480 칸과 긴 가맹점 이름이 가맹점 줄의 3차 `바꾸기`를 밀지 않는다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "대리 등록 권한(`cards.proxy`) 없이 남의 카드 사용을 등록·수정·삭제할 수 있는 경로를 만들어서는 안 된다 — 다른 메뉴 권한(지급·구매)으로 대신 통과하지 않는다"
      status: resolved
      verification: test
    - statement: "사용한 사람을 사용일 기준 후보 밖의 사람으로 저장해서는 안 된다 — 팀 비용 귀속이 사람 선택으로 조작되는 경로다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/corp-card-usages/rights.ts"
      provides: "[O-11] 카드 사용 수정·삭제 권리 판정 한 함수"
      exports: ["cardUsageRights"]
    - path: "test/integration/corp-card-usages-proxy.test.ts"
      provides: "대리 등록 권한·사용한 사람 후보·수정·삭제 권리 증명"
      min_lines: 40
  key_links:
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/corp-card-usages/rights.ts"
      via: "수정·삭제가 권리 판정 한 함수를 지난다"
      pattern: "cardUsageRights"
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/org/index.ts"
      via: "사용한 사람 후보와 팀 비용 팀이 사용일 소속 판정에서 나온다"
      pattern: "teamAtDate"
---

<objective>
06-03의 카드 사용 경로 위에 경영관리의 **대리 등록**(EXP-16)과 카드 사용 건의 **수정·삭제**(O-11)를 더한다 — 남의 카드 선택, `경영관리 등록` 표시, 사용일 기준 사용한 사람 후보, 완료 프로젝트의 견적 외 비용, 수정 모드, 삭제.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 자동 결제·정기 결제처럼 PM이 모르는 카드 사용이 원장에 들어와야 「모든 나가는 돈」이 성립한다. 대신 권한 경계가 넓어지는 곳이라 권리 판정을 한 함수로 모은다.

Output: 대리 등록 갈래 · 수정·삭제 · 권리 판정 함수, 폼 수정 모드, 단위 1 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-04 착수하지 않음`을 출력하고 멈춘다.**

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-03 | `corp_card_usages` · `createCardUsage` · `/cards` 폼 · `link-picker.tsx` | `git ls-files domain/corp-card-usages/index.ts 'app/(app)/cards/card-usage-form.tsx'` 둘 중 하나라도 비면 멈춤 |
| A-603 | 견적 외 비용 줄 종류(06-03이 이미 확인) | `grep -n "createOutOfQuoteLine" domain/quotes/lines.ts` 0건이면 멈춤 |
| A-601 | 프로젝트 완료 상태 값(`settled` — main 4값) | `grep -n "settled" db/schema/projects.ts` 0건이면 멈춤, 값이 바뀌었으면 실제 값으로 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(삭제 · 입력 버리기) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-11 | 등록한 사람·대리 등록 권한자만 수정·삭제, 완료 프로젝트 줄은 대리 등록 권한자만, 구매 완료 건은 연결 변경·삭제 없음 | `domain/corp-card-usages/rights.ts`의 `cardUsageRights` 한 함수 + 단위 테스트 |
| AS3 | 별도 모듈(06-03과 같음) | 해당 없음 |
| O-19 · O-20 | (06-06 소관) 이 플랜은 구매 완료 건을 읽기만 한다 | 해당 없음 |

**계획 판단(Claude 재량):** 카드 사용 건을 지워도 그 건이 만든 견적 외 비용 줄은 지우지 않는다 — 줄은 견적 줄 모듈의 것이고(04 규칙), 남은 줄은 완료 전 점검(06-09)의 미매칭 견적 줄로 드러나는 것이 맞는 신호다.

<probe_fallback>
coverage.json의 이 플랜 몫: **EXP-16 — unclassified → unresolved (flagged)**, **EXP-07 — unclassified → unresolved (flagged)**. 대신 쓰는 근거: 기준 3 · D-608 · D-47 · O-11. 자동 해소·기각 없음 — 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 경영관리가 남의 개인 카드 자동 결제 한 건을 대리 등록하고, 담당 PM의 프로젝트 상세에 「경영관리 등록」으로 보인다</name>
  <files>domain/corp-card-usages/index.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, test/integration/corp-card-usages-proxy.test.ts, test/e2e/card-proxy.spec.ts</files>
  <precondition>06-03이 끝났고 「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 599-600행(카드 — 새 건 자격) · 630-637행(사용한 사람 · 완료 프로젝트) · 801행(S9 권한 행) · Copywriting 「표시 — 경영관리 등록」
    - .planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
    - domain/corp-card-usages/index.ts(06-03 — `createCardUsage` 카드 자격 판정 자리)
  </read_first>
  <behavior>
    - `cards.proxy` write 있음 · 남의 개인 카드 · 견적 줄 연결 → 저장, `registered_via = proxy`, 사용한 사람 = 소지자, `document_create`
    - `cards.proxy` 없음 · 남의 카드 → ForbiddenError
    - `expenses.payments`·`cards.purchases`만 있는 사람 · 남의 카드 → ForbiddenError(대신 통과 없음)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

`createCardUsage`의 카드 자격 판정에 대리 등록 갈래를 더한다: `can(viewer, "cards.proxy", "write")`면 활성 카드 전부가 자격이고, 고른 카드가 본인 자격(자기 카드·자기 팀 카드) 밖이면 `registered_via = proxy`로 저장한다. 개인 카드면 사용한 사람 = 소지자(입력 없음). 폼은 대리 등록 권한자에게 카드 옵션으로 활성 카드 전부를 보내고, 남의 카드를 고르면 카드 아래 `Form.Hint` `경영관리 등록 · 카드 소지자 {이름}`을 세운다(서버가 판정한 값을 받아 그린다 — 클라이언트가 소지자를 비교하지 않는다). 06-03이 만든 목록·상세 섹션의 등록 칸 표시는 그대로 쓴다.

통합 테스트에 behavior 세 줄, E2E `test/e2e/card-proxy.spec.ts`에 「권한자 → 남의 카드 → 견적 줄 → 저장 → 담당 PM 계정으로 프로젝트 상세 카드 섹션에 `경영관리 등록`」 한 경로.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages-proxy.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - 통합 테스트가 「지급·구매 권한만 있는 계정의 남의 카드 등록 거부」를 개별 `it`으로 단언한다
    - E2E 한 경로가 담당 PM 계정 화면에서 `경영관리 등록` 글자를 확인하고 녹색이다
  </acceptance_criteria>
  <done>대리 등록 한 건이 권한 판정 → 저장 → 담당 PM의 프로젝트 비용 표시까지 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 팀 카드 대리 등록의 사용한 사람(사용일 후보) · 완료 프로젝트의 견적 외 비용</name>
  <files>domain/corp-card-usages/index.ts, domain/corp-card-usages/link-targets.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/link-picker.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/integration/corp-card-usages-proxy.test.ts, test/e2e/card-proxy.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 629-637행 · 979-986행(S9 considerations) · Copywriting 「막힘 — 카드 사용 폼」 소속 없음 갈래
    - domain/org/index.ts의 `teamAtDate`(188행 부근) · 사람 목록(퇴사 표시) 조회 함수
  </read_first>
  <behavior>
    - `usedByCandidates(cardId, usedOn)`: 개인 카드 → [소지자] / 팀 카드 → 사용일에 그 팀 소속이던 사람 전부(지금 다른 팀 · 퇴사 포함, 사용일 뒤 입사 제외), 각 후보에 `지금 {팀}` 또는 `퇴사` 부가 글자
    - 저장: 사용한 사람이 후보 밖 → 거부 / 팀 비용 · 사용한 사람 사용일 소속 없음 → 거부 / 정상 → 팀 = 사용한 사람의 사용일 소속
    - 완료 프로젝트: 권한자 · 견적 외 비용 → 저장 / 권한자 · 견적 줄 → 거부 / 비권한자 → 프로젝트 목록에서 고를 수 없음
  </behavior>
  <action>
`domain/corp-card-usages/index.ts`에 `usedByCandidates(viewer, { cardId, usedOn })`(대리 등록 권한 필요)를 더하고, 저장 때 같은 함수로 사용한 사람을 다시 확인한다(후보 밖이면 거부). 팀 비용이면 팀 = `teamAtDate(사용한 사람, 사용일)`, null이면 거부. `link-targets.ts`의 프로젝트 목록에서 완료 프로젝트를 대리 등록 권한자의 `견적 외 비용` 갈래에만 고를 수 있게 하고(D-47), 견적 줄 갈래에서는 여전히 2행 `완료 · 견적 줄 잠김`. 서버 저장도 같은 판정을 한다.

폼: 대리 등록 · `팀 비용`일 때만 `사용한 사람` 칸(후보 1명이면 텍스트, 여럿이면 `Select` 기본값 없음). 사용일을 바꾸면 후보를 다시 받아 오고 고른 사람이 새 후보에 없으면 칸을 비운다. 빈 칸은 §7-15 빈 칸 막힘. 막힘·오류 문구는 Copywriting 행 그대로(S9 error 셋). 통합 테스트·E2E에 behavior 케이스를 더한다(퇴사자 · 사용일 뒤 입사자 후보 판정 포함).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages-proxy.test.ts test/integration/corp-card-usages.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-proxy.spec.ts test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 세 줄의 모든 갈래를 개별 `it`으로 단언한다(후보 밖 사용한 사람을 보낸 조작 요청 거부 포함)
    - 06-03의 카드 사용 E2E가 여전히 녹색이다
  </acceptance_criteria>
  <done>팀 카드 대리 등록의 귀속이 사용일 기준 후보에서만 정해지고, 완료 프로젝트에는 권한자의 견적 외 비용만 열린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [O-11 기본값] 수정 모드 · 삭제 · 권리 판정 한 함수</name>
  <files>domain/corp-card-usages/rights.ts, domain/corp-card-usages/index.ts, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-list.tsx, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/unit/domain/card-usage-rights.test.ts, test/integration/corp-card-usages-proxy.test.ts, test/e2e/card-proxy.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 601-611행(수정 모드 · 구매 완료 건) · 640-643행(Form.Actions · 수정·삭제 권리) · 572행(목록 행 → 수정/행 시트)
    - /mnt/project-files/06-prep/open-choices.md 20행(O-11 기본값과 대안)
    - ui/table/RowSheet.tsx(폰 행 시트)
  </read_first>
  <behavior>
    - `cardUsageRights(usage, viewer, project)`: 등록자 → 수정·삭제 / 대리 등록 권한자 → 수정·삭제 / 그 밖 → 없음 / 완료 프로젝트 줄 · 등록자이지만 권한자 아님 → 없음 / `registered_via = purchase` → 연결 변경 없음 · 삭제 없음(수정은 등록자·권한자)
    - `updateCardUsage`: 카드 값이 저장된 것과 다름 → 거부 / 결제 합계 변경 → 재역산 / 연결 변경 → 이중 연결 게이트 재통과 / version 불일치 → 거부
    - `deleteCardUsage`: 권리 없음 → 거부 / 구매 완료 건 → 거부 / 정상 → 삭제 + `document_delete`
  </behavior>
  <action>
`domain/corp-card-usages/rights.ts`에 `cardUsageRights`를 두고 파일 머리 한 줄 주석에 "O-11 답이 바뀌면 이 함수만 고친다"를 적는다. `updateCardUsage(viewer, { id, version, … })`와 `deleteCardUsage(viewer, { id, version })`가 이 함수를 먼저 부른다. 수정 요청의 카드 값은 저장된 카드와 같아야 한다(구매 완료 건의 카드 고치기 예외는 06-06이 이 함수 옆에 더한다). 결제 합계·통화·환율·증빙 종류가 바뀌면 `splitCardTotal`로 재역산, 연결이 바뀌면 `card.dual-link-block`을 다시 부른다. 행동 로그: 수정 `document_update`, 삭제 `document_delete`.

화면: `/cards?editId={id}` — 권리가 없으면 404 꼴로 목록에 돌려보낸다. 제목 `카드 사용 저장`, 카드는 저장된 카드 읽기 텍스트(여는 사람의 옵션 계산 없음), 등록 힌트는 저장된 표시 그대로, 저장된 증빙 종류가 지금 옵션에 없으면 `Select`가 `—`에 서고 1차 막힘 `증빙 종류 {이름} 카드에 없음 · 증빙 종류 고르기`. 구매 완료 건은 연결이 텍스트이고 삭제 버튼이 렌더되지 않는다. 오른쪽 끝 2차 `카드 사용 삭제` → `ConfirmDialog`(Copywriting 「Destructive — 카드 사용 삭제」). 목록 행 클릭: 권리가 있으면 `?editId=`, 없으면 `RowSheet` 보기 전용. 등록 필터는 권한자에게만.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/card-usage-rights.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages-proxy.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "cardUsageRights" domain/ app/`가 `rights.ts` 정의 한 곳과 호출부만 보여 준다(권리 판정이 다른 파일에 중복되지 않는다)
    - 단위 테스트가 behavior 첫 줄의 다섯 갈래를, 통합 테스트가 둘째·셋째 줄의 갈래를 개별 `it`으로 단언한다
    - E2E에 「등록자가 수정 → 저장」 · 「비등록자 행 클릭 → 행 시트」 · 「삭제 모달 → 목록에서 사라짐」 케이스가 녹색이다
  </acceptance_criteria>
  <done>카드 사용 건을 누가 고치고 지울 수 있는지가 한 함수에서 정해지고, 수정 모드가 여는 사람의 카드 자격과 무관하게 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 대리 등록·수정·삭제 액션 | 카드 id · 사용한 사람 id · 연결 · version이 넘어온다. 전부 서버가 다시 판정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-20 | Elevation of Privilege | 대리 등록 | high | mitigate | 남의 카드 저장은 `can(viewer, "cards.proxy", "write")`만 허락하고 다른 메뉴 권한으로 대신 통과하지 않는다. 지급·구매 권한만 있는 계정 거부 통합 테스트 |
| T-06-21 | Spoofing | 사용한 사람 | high | mitigate | 저장 때 `usedByCandidates`로 다시 판정, 후보 밖이면 거부. 조작 요청 통합 테스트 |
| T-06-22 | Tampering | 수정 요청의 카드·연결 | medium | mitigate | 카드 값 변경 거부, 연결 변경은 이중 연결 게이트 재통과, `version` 낙관적 잠금 |
| T-06-23 | Elevation of Privilege | 수정·삭제 권리 | high | mitigate | `cardUsageRights` 한 함수를 domain 함수 첫 줄에서 부르고 화면은 그 결과만 그린다. URL 직접 접근(`?editId=`)도 서버가 거른다 |
| T-06-24 | Repudiation | 삭제 | medium | mitigate | `document_delete` 행동 로그(사람 · 시각 · 건 요약) |
</threat_model>

<verification>
- 단위 1 · 통합 1(+ 06-03 통합 · leak-scan) · E2E 1(+ 06-03 E2E) 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml·tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S9(대리 등록 · 수정 모드) DOM 실측 감사(backstop 2건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 경영관리가 PM이 모르는 카드 사용을 대신 등록하면 담당 PM의 프로젝트 비용에 `경영관리 등록`으로 보인다
- 대리 등록·수정·삭제가 권한 한 키와 권리 한 함수로만 열린다
- 팀 비용 귀속이 사용일 기준으로만 정해진다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `cardUsageRights` · `usedByCandidates` · `updateCardUsage` · `deleteCardUsage` · `createCardUsage` 대리 등록 갈래 |
| Server Action | `updateCardUsageAction` · `deleteCardUsageAction` · 사용한 사람 후보 액션 |
| 화면 | `/cards?editId=` 수정 모드 · 삭제 모달 · 목록 행 → 수정/행 시트 |
| 테스트 | `test/unit/domain/card-usage-rights.test.ts` · `test/integration/corp-card-usages-proxy.test.ts` · `test/e2e/card-proxy.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-04-SUMMARY.md` when done
</output>
