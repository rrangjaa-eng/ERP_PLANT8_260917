---
phase: 06-payment-evidence-cards
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - domain/payments/index.ts
  - domain/payments/action-row.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - repositories/expense-payments.ts
  - app/(app)/expenses/[id]/page.tsx
  - app/(app)/expenses/[id]/payment-section.tsx
  - app/(app)/expenses/[id]/payment-action-row.tsx
  - app/(app)/expenses/[id]/actions.ts
  - app/(app)/expenses/[id]/actions.registry.ts
  - test/unit/domain/payments.test.ts
  - test/unit/domain/payments-action-row.test.ts
  - test/integration/expense-payments.test.ts
  - test/e2e/payment-single.spec.ts
autonomous: true
requirements: [EXP-09, EVID-02, EXP-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 섹션(S5)에서 지급일(기본 오늘 KST)과 이체액(기본 = 서버 지급 총액)을 적고 1차 「지급 완료」를 누르면 저장되고, 지급 뒤 읽기 줄이 지급일 · 방식별 이름의 이체액 · 2행 `지급 총액 · 차이 · 공급가 역산`(서버가 보낼 때만)으로 선다(D-604)"
    - "이체액이 지급 총액과 다르면 차이 사유가 없을 때 저장이 거부되고(「Error — 차이 사유 칸」), 사유가 있으면 저장되며 비용 기준 값(증빙 금액·승인액)은 한 원도 바뀌지 않는다 — 차이는 `diffKrw`로만 계산한다(D-605)"
    - "증빙 필수 설정(`evidence.required`)이 켜져 있고 증빙 0 · 선결제 아님 · 면제 아님이면 `gate(doc, \"payment.evidence-required\", ctx)`가 `증빙 없음 · 담당 PM {이름}`으로 막고, 설정을 끄면 같은 문서가 통과한다(EVID-02, D-603). ctx = `{ evidenceRequired, evidenceCount, prepaid, waived, confirmation, pmName }`(R-8)"
    - "선결제 표시가 있거나 증빙 면제된 문서는 증빙 필수 설정이 켜져 있어도 `payment.evidence-required`를 통과한다 — 판정 함수 `evidenceGateDecision`의 단위 테스트가 두 갈래를 고정하고, 실제 값 배선은 06-06(확인) · 06-10(면제 · 선결제)이 `evidenceGateInputs`로 한다(D-603)"
    - "지급 취소는 사유 없이는 거부되고, 사유와 함께 하면 지급 기록이 취소 표시되고(행 삭제 아님) 문서 version이 +1 되며(R-2) 문서가 지급 전으로 돌아가 같은 표의 P1~P4 중 하나가 선다 — 완료(정산) 프로젝트의 문서도 취소된다(D-606, D-47)"
    - "지급 예정일 칸이 dirty인 동안 1차가 `예정일 저장 Ctrl+Enter`로 바뀌고(P1), 저장하면 예정일만 저장되고 이체액·차이 사유 칸 입력값은 남으며, `Esc`는 서버 값으로 되돌리고 상태의 1차로 돌아온다(SP-3 ②)"
    - "P3: 증빙 필수 on · 증빙 0 · 선결제·면제 아님 → `지급 완료` 비활성 + `증빙 없음 · 담당 PM {이름}` — 3차 `증빙 면제` **자리**는 판정 함수가 내보내고, 면제 행동을 붙이는 06-10 전까지 화면은 그 자리를 렌더하지 않는다(행동 없는 버튼 없음)"
    - "두 요청이 동시에 지급 완료하면 지출결의 행 잠금(R-1) · 문서 version · 부분 유니크 인덱스로 하나만 저장되고 다른 하나는 `{사람}이 {시:분}에 지급 완료함 · 새로 고침`을 받는다 — `Promise.all` 병렬 통합 테스트"
    - "「06-03 tx 규약」을 따른다 — 이 플랜이 더하는 `EVIDENCE_REQUIRED` 설정 읽기와 표시용 `pmName`은 트랜잭션 전 `loadPaymentInputs`에서, 지급 취소 · 예정일 저장의 권한 판정은 트랜잭션 전, 행동 로그는 커밋 뒤다. 06-03의 「지급 완료 동시 6건」이 이 플랜 뒤에도 녹색이다"
    - "**잠금 뒤 게이트 재판정(CROSS R-3):** 증빙 게이트 입력 중 문서 상태(증빙 수, 뒤 플랜의 확인 · 면제 · 선결제)는 `lockExpenseRow` 뒤 같은 `tx`로 읽고 사전 조회 값을 쓰지 않는다. 증빙 게이트는 06-03의 기준 재판정(`PayableChangedError`)보다 앞에서 돈다(CROSS-R1 F-3) — 사전 조회 뒤 · 잠금 전에 증빙이 사라진 문서는 `증빙 없음 · 담당 PM {이름}`으로 막힌다(통합 테스트 「잠금 뒤 게이트 재판정」)"
    - "`version`이 어긋난 저장(지급 완료 · 예정일 · 취소)은 거부되고 「거부 — 문서 화면 동시성」 문구가 칸/행동 자리에 선다"
    - "행동 로그: 지급 완료 = `payment_process`, 지급 취소 · 예정일 저장 = `document_update`(AS2 기본값)"
    - "S5 loading: 지급일·지급 예정일을 바꾸면 서버가 다시 계산한 지급 총액 힌트가 이전 값 `--faint` → 새 값으로 바뀌고, 1차 처리 중에는 `…`다"
    - "S5 error: 「Error — 이체액 칸」 · 「Error — 차이 사유 칸」 · 「Error — 지급일 · 지급 예정일 칸」 · 지급 취소 모달 「Error — 사유 근거 칸」 · 「거부 — 문서 화면 동시성」 · 「거부 — 문서 화면 응답 없음」이 각 자리에 서고, 응답 없음 뒤에도 칸 입력값이 남는다"
    - "S5 partial: 지급 예정일 칸이 dirty인 동안 칸의 `Enter`는 제출하지 않는다"
    - statement: "S5 overflow: 지급 뒤 2행 `지급 총액 · 차이 · 공급가 역산` 세 묶음이 720 한 열·폰 폭에서 숫자 중간 줄바꿈 없이 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S5 long-text: 차이 사유 읽기 줄이 `keep-all`로 줄바꿈되고 말줄임이 없다 — 200자 차이 사유로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "서버가 다시 계산한 지급 총액이 화면이 본 값과 다를 때 조용히 새 값으로 처리해서는 안 된다 — 처리하지 않고 막힘으로 돌려주어 사람이 이체액을 다시 본다"
      status: resolved
      verification: test
    - statement: "이체액과 지급 총액의 차이가 비용 기준 값(증빙 금액·승인액)을 바꿔서는 안 된다 — 차액을 비용에 넣는 길은 조정 줄(D-83)뿐이다(D-605)"
      status: resolved
      verification: test
    - statement: "경영관리 권한이 없는 사람이 지급 취소·예정일 저장을 대신할 수 있어서는 안 된다(D-601)"
      status: resolved
      verification: test
  artifacts:
    - path: "app/(app)/expenses/[id]/payment-section.tsx"
      provides: "S5 지급 섹션 — 지급일 · 이체액 · 차이 힌트 · 차이 사유 · 지급 예정일 제자리 저장 · 지급 뒤 읽기 줄"
      min_lines: 60
    - path: "domain/payments/action-row.ts"
      provides: "P0 · P1 · P3 · P4 · P6 + 결재·증빙 게이트 판정 순수 함수"
      exports: ["resolveExpenseActionRow", "approvalGateDecision", "evidenceGateDecision"]
    - path: "domain/payments/index.ts"
      provides: "지급 완료(이체액 · 차이 사유) · 지급 취소 · 예정일 저장"
      exports: ["completeExpensePayment", "cancelExpensePayment", "saveScheduledPayDate"]
    - path: "test/integration/expense-payments.test.ts"
      provides: "게이트 · 조작 · 낡은 값 · 차이 사유 · 비용 불변 · 동시 두 요청 · 잠금 뒤 게이트 재판정 · 취소 · 완료 프로젝트 · 권한"
      min_lines: 80
  key_links:
    - from: "domain/rules/register.ts"
      to: "domain/payments/action-row.ts"
      via: "`payment.evidence-required` 규칙 몸통이 `evidenceGateDecision`을 부른다 — 화면 P3와 같은 함수"
      pattern: "evidenceGateDecision"
    - from: "domain/payments/index.ts"
      to: "domain/settings/keys.ts"
      via: "증빙 필수 설정을 트랜잭션 전 `loadPaymentInputs`에서 읽는다(06-03 tx 규약) — `EVIDENCE_REQUIRED`의 readBy를 지운다"
      pattern: "EVIDENCE_REQUIRED"
    - from: "app/(app)/expenses/[id]/payment-section.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "지급 취소 모달(사유 근거 칸 필수)"
      pattern: "ConfirmDialog"
---

<objective>
06-03 트레이서 위에 **지급 섹션(S5)** 을 완성한다 — 이체액 · 차이 사유 · 지급 예정일 제자리 저장 · 증빙 필수 게이트(EVID-02) · 지급 취소 · 동시성.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 기준 1의 「실제 이체액 입력」과 D-603 증빙 게이트 · D-605 차이 사유 · D-606 취소가 한 섹션에 모인다. 증빙 게이트 ctx의 선결제·면제·확인 값은 그 표·칸을 만드는 06-06 · 06-10이 `evidenceGateInputs`로 배선한다 — 이 플랜은 판정 함수와 설정 읽기를 세운다.

Output: S5 섹션, 게이트 `payment.evidence-required`, `cancelExpensePayment` · `saveScheduledPayDate`, P1 · P3, 통합 1 · 단위 1 추가.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 100000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 경계다 — 재분할 방아쇠: Task 2 끝에서 컨텍스트 60%를 넘으면 Task 3(취소 · 동시성)을 새 플랜으로 넘기고 SUMMARY에 적는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-04 착수하지 않음`을 출력하고 멈춘다.** 가칭은 06-03 SUMMARY의 「가칭 → 실제」 표를 따른다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-03 | `completeExpensePayment` · `lockExpenseRow` · `resolveExpenseActionRow` | `grep -c "lockExpenseRow" repositories/expense-payments.ts` 0이면 멈춤 |
| UA-602 | 확인 모달 `ui/confirm-dialog/ConfirmDialog.tsx`(지급 취소 모달) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| UA-617 | Phase 5 지출결의의 지급 예정일 칸 | 06-03 SUMMARY 대응표에 칸 이름이 없으면 `grep -n "scheduled\|pay_date\|예정" db/schema/expenses.ts` 0이면 멈춤 |
| A-607 | 담당 PM 이름을 문서에서 읽는 경로(`증빙 없음 · 담당 PM {이름}`) | `grep -rln "pm\|manager" domain/expenses/` 비면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| AS2 | 지급 취소 · 예정일 저장 = 기존 `document_update`(새 행동 종류 없음) | `domain/payments/index.ts`의 `recordAction` 인자 |
| D-606 · D-47 | 지급 취소는 완료(정산) 프로젝트 문서에도 된다 — `project.completed-lock`을 부르지 않는다 | `cancelExpensePayment` |

<assumption_delta_decision>
`detected: false`. 새 명사 없음. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-09 · EVID-02 · EXP-06 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 1 · D-603 · D-605 · D-606. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 이체액을 지급 총액과 다르게 적고 차이 사유와 함께 지급 완료하면 지급 뒤 읽기 줄에 차이가 선다</name>
  <files>domain/payments/index.ts, app/(app)/expenses/[id]/payment-section.tsx, app/(app)/expenses/[id]/page.tsx, app/(app)/expenses/[id]/actions.ts, test/unit/domain/payments.test.ts, test/e2e/payment-single.spec.ts</files>
  <precondition>06-03이 끝났다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 952-957행(S5 considerations) · 819-850행(금액 표시 · 차이 부호) · Copywriting 「Error — 이체액 칸」 · 「Error — 차이 사유 칸」 · 「Error — 지급일 · 지급 예정일 칸」 · 「표시 — 지급 방식 · 계좌」
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-604 · D-605
    - 06-03이 만든 domain/payments/index.ts · payment-action-row.tsx · actions.ts
  </read_first>
  <behavior>
    - 이체액 ≠ 지급 총액 · 사유 없음 → 거부(칸 오류) / 사유 있음 → 저장, diff_krw = `diffKrw(이체액, 지급 총액)`
    - 차이 = 0 · 사유 없음 → 저장
    - 저장 전후로 지출결의의 증빙 금액·승인액이 같다
    - 지급일을 바꿔 요청한 지급 총액 힌트는 서버 응답이다(클라이언트 셈 없음)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① `completeExpensePayment` 입력에 `transferKrw` · `diffReason?`를 더한다(06-03의 기본 이체액 = 지급 총액 갈래는 칸 기본값으로 옮겨 간다). 차이 ≠ 0이면 사유 필수(D-605) — 차이는 트랜잭션 안에서 `decidePayable` 결과와 `diffKrw`(DB 없는 함수)로만 계산하고 비용 기준 값은 건드리지 않는다(06-03 tx 규약 — 트랜잭션 콜백에 설정 · 권한 읽기를 더하지 않는다). 지급일 · 지급 예정일로 지급 총액을 다시 계산해 돌려주는 조회 액션(`previewPayableAction` — 읽기 전용, 행동 로그 없음, 트랜잭션 없이 `loadPaymentInputs` → `decidePayable`)을 더한다.

② `payment-section.tsx`(S5): 지급일 칸(기본 오늘 KST) · 이체액 칸(기본값 = 서버 지급 총액, 방식별 이름은 Copywriting 「표시 — 지급 방식 · 계좌」) · 서버 계산 힌트(오는 동안 이전 값 `--faint`) · 차이 힌트(0이면 줄 없음, 부호 표시) · 차이 사유 칸(차이 있을 때만) · 지급 뒤 읽기 줄(지급일 · 이체액 · 2행 `지급 총액 · 차이 · 공급가 역산` — 역산은 서버가 보낼 때만) · 차이 사유 읽기 줄(`keep-all`). 1차 버튼은 06-03의 `payment-action-row.tsx`가 그대로 렌더하고, 섹션 칸 값을 함께 보낸다. 오류 문구는 Copywriting 행 그대로. 숫자는 `.num`, 토큰은 `tokens.css`의 것만. `page.tsx`에 결재 통과 문서일 때만 섹션을 붙인다.

③ 단위 · E2E: behavior 네 줄을 `payments.test.ts`에, E2E에 「이체액 다르게 + 사유 → 지급 완료 → 읽기 줄 2행에 차이」 케이스를 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다 — `plant8/money-boundary` 위반은 금액 셈이 화면에 있다는 뜻이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - E2E의 차이 케이스가 읽기 줄 2행의 차이 부호 문자열을 단언한다
    - 06-03의 기존 E2E 케이스가 그대로 녹색이다
  </acceptance_criteria>
  <done>이체액 · 차이 사유가 섹션에서 서버까지 이어지고, 차이가 비용 기준을 바꾸지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 증빙 필수 게이트(EVID-02) · P1 예정일 제자리 저장 · P3 증빙 없음 막힘</name>
  <files>domain/payments/action-row.ts, domain/rules/register.ts, domain/settings/keys.ts, domain/payments/index.ts, app/(app)/expenses/[id]/payment-action-row.tsx, app/(app)/expenses/[id]/payment-section.tsx, app/(app)/expenses/[id]/actions.ts, app/(app)/expenses/[id]/actions.registry.ts, test/unit/domain/payments-action-row.test.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 354-383행(P1 · P3 행) · 876-885행(SP-3 ② 제자리 편집)
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-603
    - test/unit/settings/registry-coverage.test.ts(readBy 만료)
  </read_first>
  <behavior>
    - `evidenceGateDecision({ evidenceRequired, evidenceCount, prepaid, waived, confirmation, pmName })`: 필수 on · 0 · 선결제 아님 · 면제 아님 → `증빙 없음 · 담당 PM {이름}` / 필수 off → allowed / prepaid true → allowed / waived true → allowed / 증빙 1 이상 → allowed(확인 갈래는 06-06)
    - `resolveExpenseActionRow`: 예정일 dirty → P1(1차 `예정일 저장`) / P3 → `지급 완료` 비활성 + 이유 + 3차 자리 `waive`(slot) / P0 · P4 · P6 그대로
    - `saveScheduledPayDate`: 예정일만 갱신 · 지급 권한 필요 · version 불일치 거부 · `document_update`
  </behavior>
  <action>
`domain/payments/action-row.ts`에 순수 함수 `evidenceGateDecision(ctx)`(R-8 ctx 여섯 필드)를 두고, `domain/rules/register.ts`에 `payment.evidence-required`를 등록한다 — 규칙 몸통은 이 함수를 부르기만 한다(D-603). `completeExpensePayment`는 06-03 순서대로 행 잠금 → 결재 게이트 → **증빙 게이트** → 기준 재판정(두 기준일 · 증빙 종류 — `PayableChangedError`) → 재계산으로 돈다. **증빙 게이트는 기준 재판정보다 앞이다(CROSS-R1 F-3)** — 증빙이 사라진 문서는 증빙일이 결재 통과일로 바뀌어 기준 재판정에도 걸리지만, 받는 이유는 `증빙 없음 · 담당 PM {이름}`이어야 한다. 증빙 게이트 ctx(06-03 tx 규약 · CROSS R-3): `evidenceRequired`는 `EVIDENCE_REQUIRED` 설정 값 — `getSettingValue`라 트랜잭션 **전** `loadPaymentInputs`가 읽어 사전 조회 결과에 싣는다(이 플랜이 처음 읽으므로 `domain/settings/keys.ts`의 그 키에서 readBy 표시를 지운다 — registry-coverage가 강제). `pmName`은 표시 문자열이라 같은 사전 조회에서 읽는다(사용자 표 조회는 `tx` 없는 리포지토리다). `evidenceCount`는 **잠근 문서에서 같은 `tx`로** 읽는다 — 사전 조회 값을 쓰지 않는다. `prepaid`·`waived`·`confirmation`은 그 칸과 표가 아직 없으므로 각각 false · false · null로 채운다 — 06-06이 `confirmation`을, 06-10이 `prepaid`·`waived`를 `domain/evidence-reviews`의 `evidenceGateInputs`로 옮겨 실제 값으로 바꾼다(두 플랜의 must_haves에 있다 — 둘 다 잠금 뒤 `tx`로 읽는다).

`resolveExpenseActionRow`에 P1(예정일 dirty) · P3(증빙 없음 막힘 — 3차 `증빙 면제` 자리를 판정 결과에 싣되, 화면은 행동이 붙는 06-10 전까지 그 자리를 렌더하지 않는다)을 더한다. `saveScheduledPayDate(viewer, { expenseId, scheduledPayDate, version })`(UA-617 칸만 갱신 · `document_update` — 권한 판정은 트랜잭션 전, 행동 로그는 커밋 뒤, 06-03 tx 규약). 화면: 지급 예정일 칸(SP-3 ② — dirty면 1차가 `예정일 저장 Ctrl+Enter`, 칸의 `Enter`는 제출 막음, `Esc`는 서버 값으로 되돌림, 저장은 그 칸만, 다른 칸 입력값 유지), P3 막힘 문구. 액션 `saveScheduledPayDateAction`을 등록한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments-action-row.test.ts test/unit/settings/registry-coverage.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — registry-coverage가 빨가면 `evidence.required`의 readBy를 지우지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/expense-payments-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 「지급 완료 동시 6건」이 타임아웃으로 빨가면 `EVIDENCE_REQUIRED` 읽기(또는 `pmName` 조회)가 트랜잭션 콜백 안으로 들어갔다(06-03 tx 규약 위반)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "payment.evidence-required" domain/rules/register.ts`가 1 이상
    - `EVIDENCE_REQUIRED`를 읽는 줄이 `loadPaymentInputs` 안에 있고 `completeExpensePayment`의 `withTransaction` 콜백 줄 범위 밖이다 — 두 줄 번호를 SUMMARY에 적는다
    - 단위 테스트가 evidenceGateDecision 다섯 갈래와 P1 · P3를 개별 `it`으로 단언한다
    - 단위 테스트가 P3 판정 결과에 3차 자리 `waive`가 실려 있음을 단언한다(렌더와 행동은 06-10이 증빙 섹션에 붙인다)
  </acceptance_criteria>
  <done>증빙 필수 게이트가 설정을 읽어 막고, 예정일이 제자리에서 저장되며, P0 · P1 · P3 · P4 · P6이 한 판정 함수에서 나온다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 지급 취소(D-606 · D-47) · 동시성 · 권한 — 통합 테스트로 못 박는다</name>
  <files>domain/payments/index.ts, repositories/expense-payments.ts, app/(app)/expenses/[id]/payment-action-row.tsx, app/(app)/expenses/[id]/actions.ts, app/(app)/expenses/[id]/actions.registry.ts, test/integration/expense-payments.test.ts, test/e2e/payment-single.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md Copywriting 「Destructive — 지급 취소」(결과 줄 · 사유 근거 칸) · 「Error — 사유 근거 칸」 · 「거부 — 문서 화면 동시성」 · 「거부 — 문서 화면 응답 없음」
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-601 · D-606
    - domain/rules/register.ts의 `project.completed-lock`(지급 취소가 이 잠금에 걸리지 않아야 한다 — D-47)
    - ui/confirm-dialog/ConfirmDialog.tsx(슬롯 이름)
    - test/integration/expense-payments-concurrency.test.ts(06-03 — 풀 밖 `pg` Client · `pg_blocking_pids` 폴링 꼴, 「잠금 뒤 게이트 재판정」이 같은 꼴을 쓴다)
  </read_first>
  <behavior>
    - `cancelExpensePayment`: 사유 빈 문자열 → 거부 / 사유 있음 → cancelled_at·by·reason, 행 삭제 없음, 문서 version +1, 문서가 지급 전으로(P1~P4 재판정)
    - 완료(정산) 프로젝트의 문서도 취소된다
    - 동시 두 지급 완료(`Promise.all`) → 하나 성공 · 하나 `{사람}이 {시:분}에 지급 완료함 · 새로 고침`
    - 취소 뒤 다시 지급 완료 → 새 살아 있는 기록 하나(부분 유니크가 취소 행을 세지 않는다)
    - 권한 없는 계정 · 대표 계정 → 지급 완료 · 취소 · 예정일 저장 거부
    - 잠금 뒤 게이트 재판정(CROSS R-3): `evidence.required` on · 증빙 1건 문서(증빙일 ≠ 결재 통과일로 준비 — 증빙이 사라지면 기준 재판정도 걸리는 상태) — 풀 밖 커넥션이 그 지출결의 행을 `FOR UPDATE`로 쥔 채 증빙 행을 지우고, 지급 완료 호출이 사전 조회를 마치고 행 잠금에서 막힌 것을 `pg_blocking_pids`로 확인한 뒤 커밋 → 지급 완료가 `PayableChangedError`가 아니라 `증빙 없음 · 담당 PM {이름}`으로 막히고 지급 기록 0건(증빙 게이트 → 기준 재판정 순서, CROSS-R1 F-3)
  </behavior>
  <action>
`cancelExpensePayment(viewer, { paymentId, reason, version })`: 지급 권한 필수(D-601 — `can`은 트랜잭션 전) · 사유 필수 · 한 트랜잭션에서 `lockExpenseRow`(R-1) → version 비교 → 취소 표시 → `bumpExpenseVersion`(R-2) · 커밋 뒤 `document_update`에 사유(06-03 tx 규약) · `project.completed-lock`을 부르지 않는다(D-606, D-47). 동시성: version 불일치와 부분 유니크 위반을 둘 다 「거부 — 문서 화면 동시성」 꼴 오류로 바꾼다.

**잠금 뒤 게이트 재판정(CROSS R-3).** behavior 마지막 줄을 06-03 「지급 완료 동시 6건」과 같은 풀 밖 `pg` Client 꼴(PR #75 결정적 재현 — 폴링 상한 · `finally` 정리)로 쓴다. 증빙 행은 Phase 5 domain 함수로 만들고, 지우는 쪽은 그 Client의 SQL로 한다(아직 커밋 전이라 사전 조회는 증빙 1건을 본다). 이 케이스가 빨가면 증빙 게이트 입력을 사전 조회 값에서 가져온 것이다 — `evidenceCount`를 잠근 행의 `tx` 조회로 바꾼다. **RED 확인:** Task 2가 이미 `tx`로 읽고 있으면, `evidenceCount`를 잠시 사전 조회 값으로 바꿔 이 케이스가 빨간 것을 보고 되돌린다(출력 한 줄을 SUMMARY에, 임시 변경은 `git diff`에 남기지 않는다). 06-11 무효화 훅은 확인 기록 유무와 관계없이 늘 version을 올린다(06-11 B-1, 디자인 반영 r2 — 06-15 N-4를 대체) — 이 재판정은 그 위의 두 번째 방어로 남는다.

화면: P6의 2차 `지급 취소` → `ConfirmDialog`(사유 근거 칸 필수, 결과 줄은 Copywriting 행). 권한 없는 사람·대표에게는 이 페이즈 버튼이 비활성으로도 없고 담당 표기만. 액션 `cancelExpensePaymentAction` 등록.

`test/integration/expense-payments.test.ts`(신규): 결재 통과 전 거부 · 증빙 필수 on/off · 조작된 지급 총액 필드 무시 · `expectedPayableKrw` 불일치 거부 · 차이 사유 필수 · 비용 기준 값 불변 · behavior 다섯 줄 · 행동 로그 종류. E2E에 취소 · 예정일 저장 케이스를 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments.test.ts test/unit/domain/payments-action-row.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/expense-payments.test.ts test/integration/expense-payments-concurrency.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 「잠금 뒤 게이트 재판정」이 빨가면 증빙 게이트가 사전 조회 값을 읽었고, 「지급 완료 동시 6건」이 타임아웃이면 취소 · 예정일 저장 · 증빙 게이트 배선이 트랜잭션 안에서 전역 `db`를 읽는다(06-03 tx 규약)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 각 항목을 개별 `it`으로 단언한다(동시 두 요청은 `Promise.all`로 실제 병렬)
    - 조작 페이로드 케이스: 액션 입력에 지급 총액·역산 필드를 실어도 저장 행의 값이 서버 재계산값과 같다
    - 대표 계정 E2E에서 `지급 완료`·`지급 취소` 버튼 요소가 0개다
    - CROSS R-3: `grep -c "잠금 뒤 게이트 재판정" test/integration/expense-payments.test.ts`가 1 이상이고 그 `it`이 녹색이다 — 증빙 게이트 입력(문서 상태)이 `lockExpenseRow` 뒤 `tx`로 읽힌다는 증명. 그 `it` 이름에 「증빙 게이트 → 기준 재판정」 순서를 적고(예: `잠금 뒤 게이트 재판정 — 증빙 게이트가 기준 재판정보다 먼저`), 오류가 `PayableChangedError`가 아님을 단언한다(CROSS-R1 F-3)
    - 06-03 tx 규약: `cancelExpensePayment` · `saveScheduledPayDate`의 `withTransaction` 콜백 줄 범위마다 `sed -n '{시작},{끝}p' domain/payments/index.ts | grep -v '^\s*//' | grep -cE '\b(can|getSettingValue|loadTaxRates|recordAction)\('` 결과 0을 SUMMARY에 적는다
  </acceptance_criteria>
  <done>지급 취소 · 예정일 저장 · 차이 사유 · 동시성 · 권한별 렌더가 한 판정 함수 위에서 동작하고 통합 테스트와 E2E가 CI=true로 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Server Action | 지급일 · 이체액 · 차이 사유 · 예정일 · 취소 사유 · version이 넘어온다 |
| domain → DB(트랜잭션) | 행 잠금 · 두 게이트 · 재계산 · 쓰기 · version 갱신이 한 트랜잭션. 권한 · 설정 읽기는 그 앞, 행동 로그는 그 뒤(06-03 tx 규약). 게이트의 문서 상태 입력은 잠금 뒤 `tx`로(CROSS R-3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Elevation of Privilege | `cancelExpensePayment` · `saveScheduledPayDate` | high | mitigate | domain 첫 줄 `can(viewer, "expenses.payments", "write")`. 권한 없는 계정·대표 계정 거부 통합 테스트 |
| T-06-16 | Tampering | 이체액 · 차이 | high | mitigate | 차이 ≠ 0이면 사유 필수, 차이는 서버 `diffKrw`, 비용 기준 값 불변 통합 테스트(D-605) |
| T-06-17 | Tampering | 이중 지급 · 취소 경합 | high | mitigate | `lockExpenseRow` `FOR UPDATE`(R-1) + 문서 version(R-2) + 부분 유니크. `Promise.all` 병렬 통합 테스트 |
| T-06-18 | Elevation of Privilege | 증빙 게이트 우회 | high | mitigate | `payment.evidence-required`가 domain 안 `gate()` 한 지점, 몸통은 화면 P3와 같은 `evidenceGateDecision` |
| T-06-19 | Repudiation | 취소 · 예정일 | medium | mitigate | `document_update` 로그에 사람·사유·전후 값, 지급 기록은 취소해도 행을 지우지 않는다 |
| T-06-19a | Elevation of Privilege | 사전 조회와 잠금 사이 증빙 변경(CROSS R-3) | high | mitigate | 증빙 게이트의 문서 상태 입력을 잠금 뒤 `tx`로 읽는다 — 사전 조회에서는 설정 값 · 표시 문자열만. 통합 테스트 「잠금 뒤 게이트 재판정」(풀 밖 커넥션으로 결정적 재현) |
| T-06-19b | Denial of Service | 증빙 게이트 배선 뒤 풀 소진 | high | mitigate | `EVIDENCE_REQUIRED` · `pmName`을 트랜잭션 전 `loadPaymentInputs`에서 읽는다(06-03 tx 규약). 06-03 「지급 완료 동시 6건」을 Task 2 · 3에서 다시 돌린다 |
</threat_model>

<verification>
- 단위 payments · payments-action-row · registry-coverage · leak-scan-coverage 녹색
- 통합 expense-payments(「잠금 뒤 게이트 재판정」 포함) · expense-payments-concurrency(「지급 완료 동시 6건」, 06-03) · leak-scan 녹색
- E2E payment-single이 CI=true로 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S5 DOM 실측 감사(backstop 2건 포함, 스크린샷 육안 금지) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 이체액 입력 → 차이 사유 → 지급 완료 → 취소가 문서 화면에서 실제로 돈다
- 증빙 필수 막힘이 설정을 읽고 `rules.gate` 한 지점에서 나온다
- 동시 두 요청 중 하나만 저장된다는 것이 병렬 통합 테스트로 증명된다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 게이트 규칙 | `payment.evidence-required`(ctx `{ evidenceRequired, evidenceCount, prepaid, waived, confirmation, pmName }`) |
| 함수(domain) | `cancelExpensePayment` · `saveScheduledPayDate` · `evidenceGateDecision` · `resolveExpenseActionRow`(P1 · P3 추가) |
| Server Action | `previewPayableAction` · `saveScheduledPayDateAction` · `cancelExpensePaymentAction` |
| 화면 | `app/(app)/expenses/[id]/payment-section.tsx`(S5) |
| 변경된 기존 심볼 | `EVIDENCE_REQUIRED` readBy 제거 · `completeExpensePayment`(이체액 · 차이 사유 · 증빙 게이트) |
| 테스트 | `test/unit/domain/payments-action-row.test.ts` · `test/integration/expense-payments.test.ts`(「잠금 뒤 게이트 재판정」 포함) · payment-single E2E 케이스 |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-1 지급 완료 교착 — 06-04(취소 · 이체액)가 경로를 물려받음, 06-04 통합에 `DB_POOL_MAX` 이상 병렬 케이스(eng-cross-opus.md L13@333f7bf) | 막음 | 동시 6건 테스트는 06-03 Task 2 한 곳(`expense-payments-concurrency.test.ts`, RED가 순서상 먼저)에 두고, 이 플랜은 Task 2 · 3 verify에서 다시 돌린다. Task 2: `EVIDENCE_REQUIRED` · `pmName`은 트랜잭션 전 `loadPaymentInputs`. Task 3: 취소 · 예정일 저장의 `can` 트랜잭션 전 · 행동 로그 커밋 뒤 + 콜백 범위 grep. T-06-19b |
| ENG E-3 tx 규약 참조(eng-review.md L37@333f7bf) | 고침 | must_haves에 「06-03 tx 규약」 한 줄(이 플랜 몫: 설정 · `pmName` · 권한 · 행동 로그 자리) |
| CROSS R-3 잠금 뒤 게이트 입력이 `tx`로 읽히는지 06-04 acceptance에 한 줄(eng-cross-opus.md L37@333f7bf) | 참고 | must_haves 「잠금 뒤 게이트 재판정」 · Task 2 action(`evidenceCount`는 잠근 문서 `tx`) · Task 3 behavior · 통합 `it` 「잠금 뒤 게이트 재판정」(풀 밖 커넥션 결정적 재현, RED 확인) · acceptance grep · T-06-19a |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — 333f7bf(+미커밋 반영분 · /mnt/project-files/06-gates/eng-apply-cross-r1.md · eng-apply-checker-r1.md)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS-R1 F-3 「잠금 뒤 게이트 재판정」이 기준 재판정 오류로 끝날 수 있다 | 고침 | must_haves 「잠금 뒤 게이트 재판정」 줄 · Task 2 action(증빙 게이트 → 기준 재판정 순서) · Task 3 behavior(증빙일 ≠ 결재 통과일로 준비, `PayableChangedError`가 아니라 `증빙 없음` 기대) · acceptance(`it` 이름에 순서, `PayableChangedError` 아님 단언) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 디자인 검토 반영 r2(design-apply-cross-r1.md B-1 · 06-11 Round 2 · 06-15 Round 4)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 디자인 검토 반영 r2 — 교차 B-1 정합(06-11 무효화 훅은 확인 기록 유무와 관계없이 늘 version을 올린다 — design-apply-cross-r1.md B-1, 06-15 Round 4는 이 낡은 전제를 06-04 밖으로 미뤘다) | 참고 | 「잠금 뒤 게이트 재판정」 문단 끝 문장을 06-11 B-1과 맞춰 정정 — 훅은 이제 늘 version을 올리고, 이 재판정은 두 번째 방어로 남는다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-04-SUMMARY.md` when done
</output>
