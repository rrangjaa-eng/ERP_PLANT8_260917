---
phase: 06-payment-evidence-cards
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - db/schema/corp-card-usages.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/amounts.ts
  - repositories/corp-card-usages.ts
  - app/(app)/cards/page.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/actions.ts
  - app/(app)/cards/actions.registry.ts
  - test/unit/domain/card-usage-amounts.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/card-usage.spec.ts
autonomous: true
requirements: [EXP-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 `/cards?new=1`에서 자기 카드(또는 자기 팀 카드)를 고르고 사용일·결제 합계를 적고 연결 `팀 비용`을 골라 1차 「카드 사용 등록」을 누르면 저장되고 `/cards`로 돌아가 그 카드 그룹에 행이 선다(EXP-07)"
    - "연결 없는 카드 사용은 화면 1차가 막히고(`연결 없음 · 연결 고르기`), 액션을 직접 불러도 서버가 거부한다 — DB CHECK가 견적 줄 · 팀 중 정확히 하나를 강제한다(EXP-07 「연결 없는 카드 사용은 서버가 막는다」, D-609)"
    - "사람은 결제 합계(부가세 포함)를 적고 공급가·부가세는 서버가 증빙 종류 규칙으로 역산해 저장한다 — `vat_surcharge`는 `grossFromTotal()`, `none`은 공급가 = 결제 합계, 반올림 잔차가 0이 아니면 서버 계산 한 줄 끝에 `· 반올림 차이 N`(D-607)"
    - "역산 세율·반올림의 기준일은 **사용일(`usedOn`)**이다 — 카드 전표의 증빙일이 사용일이라 06-03의 「부가세 = 증빙일 기준」과 같은 규칙이다(CROSS R-2). 잔차는 저장 부가세(`diffKrw(합계, 공급가)`)가 아니라 **따로 셈한 재계산 부가세** `round(공급가 × 세율, 단위, 방식)`으로 `공급가 + 재계산 부가세 − 합계`다(`domain/revenue/index.ts` 92-94 선례) — 합계 1,000,001 · 부가세 단위 10이면 공급가 909,090 · 재계산 부가세 90,910 · 잔차 −1(CROSS R-1)"
    - "「06-03 tx 규약」을 따른다 — 카드 사용 생성은 두 단계다: `precheckCardUsage(viewer, input)`(트랜잭션 밖 — 카드 자격 · 사용일 소속 `teamAtDate` · 증빙 종류 규칙 · `loadTaxRates(usedOn)`, 뒤 플랜의 `can` 갈래도 여기) → `createCardUsage(viewer, input, pre, tx?)`(tx가 없으면 스스로 열고 있으면 그 tx를 쓴다 — `saveQuoteLines`의 단독/외부 tx 이중 경로 꼴). 트랜잭션 몸통 `runCreate`는 `tx`를 받는 리포지토리와 순수 함수만 부른다(CROSS E-3)"
    - "외화 카드는 외화 금액 × 환율(기본값 = 설정 최근 환율)로 원화 결제 합계를 서버가 계산한 뒤 역산한다 — 저장 행에 통화·외화 금액·환율·원화 환산액이 모두 있다(O-7, FX-01)"
    - "카드 폼의 증빙 종류 옵션은 서버가 `vat_surcharge`·`none` 규칙의 종류만 보내고, 가맹점(거래처)을 고르면 거래처 기본 증빙 종류로 채워지되 그것이 옵션 밖이면 `카드 전표`로 떨어지며(옵션 안에 있을 때만) 가맹점 줄 아래 `기본 증빙 {종류} · 카드에 없음`이 선다 — 서버도 옵션 밖 종류의 저장을 거부한다"
    - "`팀 비용`을 고르면 팀을 고르는 칸이 없고 사용한 사람의 사용일 소속(`teamAtDate`)이 텍스트로 채워지고 그 아래 힌트는 없다(rev 9 M-5 — Copywriting 「표시 — 카드 폼 연결 「팀 비용」」: 팀 값과 행동 로그가 말한다) — 소속이 없으면 임의 팀으로 떨어지지 않고 1차가 「막힘 — 카드 사용 폼」 소속 없음 갈래로 막힌다(EXP-07)"
    - "카드 사용 등록은 결재가 없고 행동 로그에 `document_create` 한 줄을 남긴다(D-608)"
    - "S8 empty: 「Empty — 카드 사용 목록」 세 갈래(이번 달 0건 · 필터 0건 · 쓸 카드 0장 — 1차 렌더 안 함)"
    - "S8 loading: 첫 로드는 §7-7 LOADING 뼈대이고 월·카드·연결·등록 필터 이동은 GET 이동이다"
    - "S8 error: 「Error — 목록 로드」 카드 사용 문구가 선다"
    - "S8 populated: 카드 그룹 머리글 · 사용일 오름차순 · 열(사용일 · 가맹점 · 연결 · 결제 합계 · 증빙 · 등록) · 결제 합계 2행 공급가/외화 묶음 · 등록 칸(「표시 — 경영관리 등록」) · 합계 줄 · 오른쪽 3차 `구매 요청` + 1차가 와이어프레임대로 선다"
    - "S8 partial: 가맹점 없음 = `—` · 증빙은 `있음`/`—` 글자(상태 색 없음) · 외화는 2행 두 묶음(「표시 — 카드 사용 결제 합계 2행」)"
    - "S8 overflow: 50건 페이지(§7-16, UA-603)이고 카드 그룹 머리글이 쪽 첫 줄에 다시 선다, 좁은 PC는 P3 증빙부터 숨긴다"
    - "S8 zero-one-many: 0건 = Empty · 건수는 합계 줄 `N건` · 하위 목록 링크는 0이면 `구매 요청`만(SP-4 — 건수는 06-14가 채운다)"
    - "[M-4] 새 건(`?new=1`)은 빈 폼으로 열지 않는다 — 서버가 사용일 = 오늘(KST), 카드 = 그 사람이 마지막으로 등록한 카드 사용의 카드(지금 옵션에 있을 때만 — 없으면 옵션 한 장이면 텍스트, 여럿이면 비움), 연결 = 그 사람의 직전 등록의 연결 종류(이 플랜은 `팀 비용`뿐 — 처음 쓰는 사람만 비움, 견적 줄 · 견적 외 비용과 프로젝트는 06-07)를 채워 보내고 모두 바꿀 수 있다(UI-SPEC S9 「새 건의 기본값」, CLAUDE.md §7)"
    - "[H-4 기반] 카드 사용 표는 ADMN-12 보관 칸(`archived_at` · `archived_by` — `corp-cards` 선례)을 처음부터 가지고, 목록 · 합계 · 기본값 조회는 보관된 건을 뺀다 — 물리 삭제는 없다. 삭제(= 보관) · 토스트 `되돌리기`(= 보관 해제)는 06-09"
    - "S9 empty: 새 건은 위 기본값으로 채워져 열리고(처음 쓰는 사람만 연결이 비어 있다) 연결이 덜 채워지면 `연결 없음 · 연결 고르기`, 빈 칸 = `카드 · 결제 합계 2칸 비어 있음 · 카드 고르기`(「막힘 — 카드 사용 폼」). 증빙 종류는 `카드 전표`가 옵션에 있을 때만 채우고, 없으면 `—` + 「막힘 — 카드 사용 폼 · 증빙 종류 자동 채움 없음」"
    - "S9 loading: 결제 합계·통화·환율이 바뀌면 서버 계산 한 줄이 이전 값 `--faint` → 새 값으로 바뀌고, 1차 처리 중에는 `…`다"
    - "S9 populated: 카드 · 사용일 · 가맹점 · 결제 합계 + 서버 계산 한 줄 · 증빙 종류 · 연결 · 메모가 와이어프레임대로 서고 카드 옵션 글자는 `{카드 이름} · {발급사} {뒤 4자리}`다 — 이 플랜의 연결 라디오는 `팀 비용` 하나이고 06-07이 `견적 줄` · `견적 외 비용`을 더한다"
    - statement: "S8 long-text: 가맹점·연결이 한 줄 말줄임 + `title`로 선다 — 긴 해외 가맹점 이름 · 긴 팀 이름으로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "브라우저가 보낸 공급가·부가세·원화 환산액을 저장해서는 안 된다 — 사람은 결제 합계만 적고 나머지는 서버가 계산한다(D-607)"
      status: resolved
      verification: test
    - statement: "남의 카드로 본인 등록이 되어서는 안 된다 — 카드 자격(소지자 본인 · 사용일 소속 팀의 카드)은 서버가 판정한다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/corp-card-usages.ts"
      provides: "카드 사용 표 — 카드 · 사용일 · 가맹점 · 결제 합계 Money 묶음 · 공급가 · 부가세 · 증빙 종류 · 연결(견적 줄 XOR 팀) · 사용한 사람 · 등록 경로(self·proxy·purchase) · version"
      exports: ["corpCardUsages"]
    - path: "domain/corp-card-usages/amounts.ts"
      provides: "결제 합계 → 원화 · 공급가 · 부가세 · 반올림 잔차(D-607, O-7) — 순수 함수 `splitCardTotal(total, rates)`, 세율은 인자"
      exports: ["splitCardTotal"]
    - path: "domain/corp-card-usages/index.ts"
      provides: "카드 사용 사전 조회(트랜잭션 밖) · 등록(본인 · 팀 비용 — 단독/외부 tx 이중 경로) · 목록"
      exports: ["precheckCardUsage", "createCardUsage", "listCardUsages"]
    - path: "test/e2e/card-usage.spec.ts"
      provides: "직원 본인 등록 → 목록 한 경로(06-07이 케이스를 더한다)"
      min_lines: 30
  key_links:
    - from: "domain/corp-card-usages/amounts.ts"
      to: "domain/money/index.ts"
      via: "역산과 부가세 차가 금액 모듈 함수로만 이루어진다"
      pattern: "grossFromTotal"
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/money/tax.ts"
      via: "세율은 `precheckCardUsage`가 트랜잭션 전에 `loadTaxRates(usedOn)`로 읽어 `pre.rates`로 넘긴다(06-03 tx 규약)"
      pattern: "loadTaxRates"
    - from: "app/(app)/cards/actions.registry.ts"
      to: "test/integration/leak-scan.test.ts"
      via: "새 registry가 누수 스캔에 import된다(leak-scan-coverage 강제)"
      pattern: "cards/actions.registry"
---

<objective>
법인카드 사용 등록의 첫 경로를 새 표(A-611, 선행 설계 없음)부터 `/cards` 목록까지 세운다 — 직원의 **본인 등록 · 팀 비용 연결**, 결제 합계 → 공급가 역산(D-607), 외화(O-7), 연결 필수(DB CHECK). 견적 줄 · 견적 외 비용 연결과 이중 연결 게이트는 06-07, 대리 등록·수정·삭제는 06-09, 구매 완료 모드는 06-12가 이 위에 더한다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 카드는 결재가 없고(D-608) 여러 사람이 등록한다 — 연결 필수를 첫 경로의 DB CHECK로 넣어야 「모든 나가는 돈이 연결된다」가 카드에서 깨지지 않는다. 금액 모델(D-607)은 06-01 Task 3이 ROADMAP · REQUIREMENTS 문구와 맞춘다.

Output: `corp_card_usages` 표(신규 마이그레이션 1), `domain/corp-card-usages`(AS3 — 마스터 `domain/corp-cards`와 별도 모듈), `/cards` 목록·폼, 단위 1 · E2E 1 · 누수 스캔 케이스.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다. 옛 06-03(24파일 · 125k)을 06-05 · 06-07로 나눴다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-RESEARCH.md
@.planning/phases/06-payment-evidence-cards/06-PATTERNS.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-05 착수하지 않음`을 출력하고 멈춘다.** 이름은 가칭이다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-611 | 선행 없음 — 이 플랜이 `corp_card_usages`를 처음 설계한다 | 확인만: `git ls-files 'db/schema/*card-usage*'` 비어 있어야 정상(있으면 멈추고 SUMMARY에 적는다) |
| UA-603 | `ui/pagination/Pagination.tsx`(50건 페이지) | `git ls-files ui/pagination/Pagination.tsx` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(「확인 — 입력 버리기」) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| 06-03 | 마이그레이션 저널에 06-03의 `phase6_payments`가 먼저 있다(저널 순서) · 세율 사전 조회 `loadTaxRates(asOf)`와 타입 `TaxRates`(`domain/money/tax.ts`) | `grep -c "phase6_payments" db/migrations/meta/_journal.json` 0이면 멈춤 · `grep -c "export async function loadTaxRates" domain/money/tax.ts` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-7 | 외화 카드 = 외화 금액 + 환율(기본 = 설정 최근 환율) → 서버가 원화 → 역산 | `domain/corp-card-usages/amounts.ts` · 폼 결제 합계 칸 |
| AS3 | 카드 사용은 `domain/corp-card-usages` 별도 모듈 | 모듈 경계 |
| O-18 | 금액 셈은 서버 — 서버 계산 한 줄은 액션 응답 | 해당 없음 |
| UA-612 | 목록 범위: 직원 = 자기 카드·자기 팀 카드의 사용 + 자기 등록 / `cards.proxy`·`expenses.payments` write 권한자와 `scopeFor` 전사 범위 = 전부 | `repositories/corp-card-usages.ts`의 범위 조건 |

<assumption_delta_decision>
`detected: false`. 새 표를 더한다(A-611) — 기존 명사를 옮기지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-07 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 3 · D-607 · D-608 · D-609 · O-7. 전체 10건 표는 06-01 `<probe_fallback>`. D-607과 요구사항 문구의 어긋남은 06-01 Task 3이 사용자 경로로 정렬한다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 직원이 자기 카드 사용 한 건을 「팀 비용」에 이어 등록하고 목록 카드 그룹에서 본다</name>
  <files>db/schema/corp-card-usages.ts, db/schema/index.ts, db/migrations/meta/_journal.json, domain/corp-card-usages/index.ts, domain/corp-card-usages/amounts.ts, repositories/corp-card-usages.ts, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-list.tsx, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/integration/leak-scan.test.ts, test/e2e/card-usage.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 551-645행(S8 · S9 정본) · 832-833행(카드 공급가·부가세 · 외화) · Copywriting 「막힘 — 카드 사용 폼」 · 「표시 — 카드(목록 · 폼)」 · 「표시 — 카드 폼 연결 「팀 비용」」
    - .planning/phases/06-payment-evidence-cards/06-PATTERNS.md(corp-cards 선례 — `cardOwnerKind` XOR · `isUniqueViolation` · 오류 클래스)
    - domain/corp-cards/index.ts · repositories/corp-cards.ts · app/(app)/admin/corp-cards/{page.tsx,card-form.tsx,actions.ts,actions.registry.ts}
    - domain/money/index.ts(`grossFromTotal` · `toKrw`) · domain/money/currency.ts(`recentFxRate`) · db/schema/money-columns.ts
    - domain/org/index.ts의 `teamAtDate` · domain/permissions/scope-for.ts
    - ui/table/Table.tsx(`groupBy` · 열 `priority`) · app/(app)/cards/page.tsx(현재 ListEmpty 자리)
    - db/migrations/0010_revenue_entries.sql 1-5행(SET LOCAL 한 쌍)
    - test/unit/leak-scan-coverage.test.ts
    - 06-03-PLAN.md must_haves.truths의 「tx 규약」 줄 · domain/money/tax.ts의 `loadTaxRates` · `TaxRates`(06-03)
    - domain/quotes/lines.ts 355-383행(트랜잭션 전 사전 판정) · 481행(`runSave`) · 547행(`tx ? runSave(tx) : withTransaction(runSave)` — 단독/외부 tx 이중 경로 선례) · domain/projects/ledger.ts 45-63행(`deferRecord` — 커밋 뒤 행동 로그)
    - /mnt/project-files/deadlock-fix/global-db-in-tx-audit.md(tx 인자 없는 리포지토리 목록 — `repositories/corp-cards.ts` · `permissions.ts` · `settings.ts` · `code-tables.ts` 전부 전역 `db`)
  </read_first>
  <behavior>
    - `precheckCardUsage`(본인 · 팀 비용): 정상 → `pre`(카드 · 사용한 사람 · 팀 · 증빙 종류 규칙 · 세율) / 사용일 소속 없음 → 거부 / 남의 카드 → 거부
    - `createCardUsage(viewer, input, pre)`: 정상 → 저장 + 커밋 뒤 `document_create` / 연결 없음 → 거부
    - DB: 견적 줄 · 팀 둘 다 null이거나 둘 다 값이면 INSERT가 CHECK 위반
    - E2E: 카드 소지 직원 → `/cards` → 「카드 사용 등록」 → 팀 비용 → 저장 → 목록 그룹에 행 + 연결 `팀 비용 · {팀}`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① **스키마(A-611 — 최초 설계, 신규 표).** `db/schema/corp-card-usages.ts`: `corp_card_usages`(id · corp_card_id FK → 카드 마스터 · used_on date · merchant_vendor_id FK null · `moneyColumns("total")` · supply_krw bigint · vat_krw bigint · evidence_type_code text · link_kind text CHECK in (quote_line, team_cost) · quote_line_id FK null · team_id FK null · XOR CHECK(견적 줄이면 quote_line_id만, 팀 비용이면 team_id만 — corp-cards `cardOwnerKind` 선례) · used_by_user_id FK · registered_by FK · registered_via text CHECK in (self, proxy, purchase) · memo text null · version · source 'demo' · `archived_at` timestamp null · `archived_by` text null(ADMN-12 보관 규약 — `db/schema/corp-cards.ts` 선례, H-4의 삭제 = 보관 · 되돌리기 = 보관 해제가 이 두 칸을 쓴다 — 06-09) · 생성·수정 시각). 인덱스 `(corp_card_id, used_on)` · `(quote_line_id)`. 구매 요청 id 칸은 06-12가 더한다. `db/schema/index.ts`에 export.
   **마이그레이션(B-2, 신규 표):** `pnpm db:generate --name corp_card_usages` → SQL 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` 한 쌍(0010 선례) → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 `pnpm db:generate` `No schema changes`.

② **domain · 리포지토리(한 경로 — 시그니처 확정, CROSS E-3 · 06-03 tx 규약).** `repositories/corp-card-usages.ts`(viewer 첫 인자) INSERT(`tx: DbOrTx` 인자) · 목록 조회(범위 조건은 「열린 선택」 UA-612 · 보관된 건(`archived_at` 있음)은 늘 뺀다). `domain/corp-card-usages/index.ts`에 두 함수를 둔다:
   - `precheckCardUsage(viewer, input)` → `pre` — **트랜잭션 밖에서만** 부른다. 전역 `db`를 쓰는 판정·조회를 전부 여기서 끝낸다: 카드 자격(자기 카드 = 소지자 본인, 자기 팀 카드 = 사용일 소속 팀의 카드 — 카드 조회는 `repositories/corp-cards.ts`, tx 인자 없음) · 사용한 사람 = viewer · 팀 = `teamAtDate(viewer, viewer.userId, usedOn)`(null이면 거부) · 증빙 종류의 규칙(코드표) · 세율 `loadTaxRates(usedOn)`(06-03 — 기준일 = 사용일, CROSS R-2). 결과 `pre`는 `{ card, registeredVia, usedByUserId, teamId, evidenceRule, rates }` 꼴의 평범한 객체다. 뒤 플랜이 카드 자격 갈래를 더하는 자리도 여기다(06-09 대리 `cards.proxy` · 06-12 구매 `cards.purchases` 권한 판정과 활성 카드 전부 · 06-14 팀 비용 구매 귀속).
   - `createCardUsage(viewer, input, pre, tx?)` — 트랜잭션 몸통을 이름 있는 안쪽 함수 `const runCreate = async (innerTx: DbOrTx) => { … };`로 두고 `tx ? await runCreate(tx) : await withTransaction(runCreate)`로 부른다(`saveQuoteLines` 481 · 547행 꼴). `runCreate` 안에서는 `pre`의 값과 순수 함수(Task 2의 `splitCardTotal(total, pre.rates)` — 이 태스크에서는 KRW · `none` 규칙 갈래만 먼저)와 `innerTx`를 받는 리포지토리 INSERT만 부른다 — 카드 · 사용한 사람 · 팀 · 규칙 · 세율은 `pre`에서만 읽고 입력으로 다시 판정하지 않는다. 행동 로그 `document_create`(D-608 — 결재 없음)는 트랜잭션 안에서 남기지 않는다: 단독 경로는 `withTransaction`이 끝난 뒤 `recordAction`, 외부 tx 경로는 남길 항목을 결과로 돌려주고 호출자가 커밋 뒤에 남긴다(`domain/projects/ledger.ts` `deferRecord` 꼴).
   - `createCardUsageAction`은 `precheckCardUsage` → `createCardUsage(viewer, input, pre)`(tx 없이) 순서로 부른다. zod 입력에 공급가·부가세·원화 필드가 없다. `listCardUsages(viewer, filters)` DTO는 `registerDto`.
   TDD: 이 태스크는 트레이서라 ④의 E2E를 먼저 쓰고 빨간 것을 확인한 뒤 ①~③을 구현한다. `precheckCardUsage` 거부 갈래(남의 카드 · 소속 없음)와 외부 tx 경로의 통합 케이스는 06-07 Task 2(`test/integration/corp-card-usages.test.ts`)와 06-12 Task 2가 진다.

③ **액션 · 화면.** `app/(app)/cards/actions.ts`(`createCardUsageAction` · 서버 계산 한 줄용 `previewCardAmountsAction`) · `actions.registry.ts`(신규) → `test/integration/leak-scan.test.ts`에 이 registry와 `@/domain/corp-card-usages` import 두 줄. `app/(app)/cards/page.tsx`의 ListEmpty 자리를 S8 목록으로 바꾸고(`?new=1`이면 S9 폼), `card-usage-list.tsx`는 `ui/table`의 `groupBy`(카드 그룹 머리글) · 사용일 오름차순 · 열 여섯. `card-usage-form.tsx`: 카드 `Select`(한 장이면 텍스트) · 사용일(기본 오늘) · 결제 합계 · 서버 계산 한 줄 · 증빙 종류 · 연결 라디오 `팀 비용`(팀 텍스트) · 메모. 1차 막힘 이유는 Copywriting 행 그대로. 숫자 `.num`, 토큰은 `tokens.css`만(새 CSS 모듈 없이 `ui/table` · `ui/form` 클래스).

④ **E2E.** `test/e2e/card-usage.spec.ts`에 behavior 셋째 줄.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 새 registry가 누수 스캔에 import되지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 `Found N issues`를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - `grep -c "CHECK" db/migrations/*corp_card_usages*.sql`이 1 이상 · `grep -c "SET LOCAL lock_timeout" db/migrations/*corp_card_usages*.sql`이 1
    - `createCardUsageAction`의 zod 입력에 공급가·부가세·원화 환산액 필드가 없다
    - (CROSS E-3) `awk '/const runCreate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\(|teamAtDate\('`가 0이고, 같은 awk 범위의 줄 수가 5 이상이다(범위가 실제로 잡혔다)
    - `grep -c "withTransaction(runCreate)" domain/corp-card-usages/index.ts`가 1(단독 경로) · `createCardUsage` 시그니처가 `(viewer, input, pre, tx?)`다(SUMMARY에 줄 번호)
    - E2E 한 경로 CI=true 녹색
  </acceptance_criteria>
  <done>직원의 카드 사용 한 건이 사전 조회(트랜잭션 밖) → 트랜잭션 안 저장 → 액션 → 목록 화면 → 행동 로그를 실제로 통과하고, 연결 없는 저장이 DB에서도 불가능하다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 결제 합계 역산 · 외화(O-7) · 증빙 종류 옵션 — `splitCardTotal` 완성</name>
  <files>domain/corp-card-usages/amounts.ts, domain/corp-card-usages/index.ts, app/(app)/cards/card-usage-form.tsx, test/unit/domain/card-usage-amounts.test.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 832-833행 · Copywriting 「막힘 — 카드 사용 폼 · 증빙 종류 자동 채움 없음」
    - domain/revenue/index.ts 80-100행(`computeGrossFromPayment` — 92-94행: 재계산 부가세를 `round(gross × 세율)`로 **따로** 셈해 잔차를 낸다)
    - domain/money/tax.ts(`TaxRates` · `loadTaxRates` — 06-03) · domain/money/index.ts(`grossFromTotal` · `round` · `toKrw`)
    - domain/code-tables/(증빙 종류 코드표의 규칙 값 조회)
  </read_first>
  <behavior>
    - `splitCardTotal({ money: { currency: KRW, amount: 1240000, fxRate: 1 }, rule: vat_surcharge }, rates{부가세율 0.1 · 단위 1 · round})` → 원화 1,240,000 · 공급가 1,127,273(`grossFromTotal`) · 저장 부가세 112,727(`diffKrw(합계, 공급가)`) · 잔차 0
    - (CROSS R-1 — 잔차 ≠ 0, 단위 10) 합계 1,000,001 · 부가세율 0.1 · 단위 10 · round → 공급가 909,090 · 저장 부가세 90,911 · 재계산 부가세 `round(909,090 × 0.1, 10)` = 90,910 · 잔차 = 909,090 + 90,910 − 1,000,001 = **−1**
    - (CROSS R-1 — 잔차 ≠ 0, 단위 1) 합계 1,000,004 · 부가세율 0.1 · 단위 1 · round → 공급가 909,095 · 저장 부가세 90,909 · 재계산 부가세 90,910 · 잔차 **+1** (같은 조건의 합계 1,000,001은 잔차 0 — 단위 1에서는 0이 맞다)
    - `none` 규칙 → 공급가 = 합계, 부가세 0, 잔차 0
    - USD 900.00 @1,474.89 → 원화 = `toKrw` 값, 그 원화로 역산
    - 증빙 종류 옵션 = 코드표에서 규칙이 `vat_surcharge`·`none`인 종류만 / 옵션 밖 종류 저장 → 거부 / 가맹점 기본 종류가 옵션 밖이면 `카드 전표`(옵션 안에 있을 때만)
  </behavior>
  <action>
먼저 behavior를 `test/unit/domain/card-usage-amounts.test.ts`에 개별 `it`으로 쓰고 실패(RED)를 확인한 뒤 구현한다.

`splitCardTotal(total, rates)`을 **순수 함수**로 완성한다(설정 · DB를 읽지 않는다 — 06-03 tx 규약). `total`은 `{ money: MoneyInput, rule: TaxRule }`(증빙 종류의 규칙 — `vat_surcharge`·`none`), `rates`는 06-03의 `TaxRates`다. 외화면 `toKrw`로 원화 결제 합계 → `vat_surcharge`는 `grossFromTotal(원화, rates의 부가세율, 부가세 단위, 방식)`으로 공급가, 저장 부가세 = `diffKrw(원화, 공급가)`(공급가 + 저장 부가세 = 합계가 늘 성립), **잔차는 저장 부가세로 셈하지 않는다** — 재계산 부가세 = `round(공급가 × 세율, 단위, 방식)`을 따로 셈하고 잔차 = `diffKrw(sumKrw([공급가, 재계산 부가세]), 원화)`(revenue 92-94행 선례, CROSS R-1) / `none`은 공급가 = 원화, 잔차 0(D-607). 반올림 방식은 `TaxRates`가 방식을 담으면 그것을, 아니면 기존 `applyTaxRule`처럼 `rule.roundingMethod ?? "round"`를 쓴다 — 06-03 구현을 따르고 SUMMARY에 적는다. 세율·반올림 단위는 기존 `tax.*` 설정이 정본이다 — 새 키 없음. **기준일은 사용일(`usedOn`)**이다: `precheckCardUsage`가 `loadTaxRates(usedOn)`으로 읽어 `pre.rates`에 담는다(CROSS R-2 — 카드 전표의 증빙일 = 사용일, 06-03의 증빙일 기준과 같은 규칙). 서버 계산 한 줄용 `previewCardAmountsAction`도 트랜잭션 없이 `loadTaxRates(usedOn)` → `splitCardTotal`을 부른다.

`precheckCardUsage`에 증빙 종류 옵션 판정(옵션 밖 거부 — 코드표 조회는 트랜잭션 밖)을, `createCardUsage`의 `runCreate`에 외화 저장(통화·외화 금액·환율·원화 환산액 네 칸)을 붙인다. 폼: 통화 `Select` + 금액 280, 외화면 환율 칸(기본 최근 환율, 없으면 빈 칸 + 1차 막힘), 가맹점을 고르면 거래처 기본 증빙 종류 → 옵션 밖이면 `카드 전표` + `기본 증빙 {종류} · 카드에 없음`, 서버 계산 한 줄 끝 `· 반올림 차이 N`(잔차 ≠ 0일 때).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/card-usage-amounts.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 잔차 −1 · +1 케이스가 0을 받아 빨가면 잔차를 저장 부가세(`diffKrw`)로 셈했다(CROSS R-1)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>error로 끝난다 — `plant8/money-boundary` 위반은 금액 셈이 금액 모듈 밖에 있다는 뜻이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - behavior 여섯 줄이 개별 `it`이다(외화 · 잔차 −1 · 잔차 +1 케이스 포함)
    - (CROSS R-2) `precheckCardUsage`와 `previewCardAmountsAction`의 `loadTaxRates` 인자가 사용일(`usedOn`)이다(SUMMARY에 줄 번호) — 실제 DB 증명은 06-07 Task 2 통합 케이스 「세율 기준일 = 사용일」
    - `domain/corp-card-usages/amounts.ts`가 설정 · 리포지토리를 import하지 않는다(`grep -cE 'settings/registry|repositories/' domain/corp-card-usages/amounts.ts`가 0 — 순수 함수)
    - E2E에 「외화 카드 사용 → 목록 2행 `USD 900.00 · {원화}`」 케이스가 녹색이다
  </acceptance_criteria>
  <done>결제 합계만 적으면 서버가 사용일 기준 세율로 원화 · 공급가 · 부가세 · 잔차(따로 셈한 재계산 부가세 기준)를 계산하고, 옵션 밖 증빙 종류는 서버가 거부한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: S8 목록 완성 — 필터 · 페이지 · 합계 줄 · 등록 칸 · Empty 세 갈래 · 누수 스캔 · S9 새 건 기본값(M-4)</name>
  <files>domain/corp-card-usages/index.ts, repositories/corp-card-usages.ts, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-list.tsx, app/(app)/cards/card-usage-form.tsx, test/integration/leak-scan.test.ts, test/e2e/card-usage.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 971-978행(S8 considerations) · Copywriting 「Empty — 카드 사용 목록」 · 「Error — 목록 로드」 · 「표시 — 경영관리 등록」 · 「표시 — 카드 사용 결제 합계 2행」
    - ui/pagination/Pagination.tsx(UA-603)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S9 「새 건의 기본값(rev 9, M-4)」 · UI Considerations S9 empty 행
  </read_first>
  <behavior>
    - 범위: 직원 → 자기 카드·자기 팀 카드 사용 + 자기 등록만 / `cards.proxy`·`expenses.payments` write 권한자 · 전사 범위 → 전부
    - [M-4] `cardUsageFormDefaults(viewer, today)`: 등록한 적 없음 → 사용일 = 오늘 · 카드 = 옵션 한 장이면 그 카드, 여럿이면 없음 · 연결 없음 / 직전 등록(보관 안 된 것 중 가장 최근, 등록자 = viewer)이 팀 비용 · 카드 A → 사용일 = 오늘 · 카드 = A · 연결 = `팀 비용` / 직전 카드가 지금 옵션 밖(비활성 · 자격 잃음) → 카드 없음(한 장이면 그 카드) / 오늘 = 서울 기준 날짜
    - [H-4 기반] 보관된 건은 목록 · 합계 줄 · 기본값 조회에 없다
    - 합계 줄 = 서버 `sumKrw`(원화 환산액) · `N건`
    - 쓸 카드 0장 → Empty 갈래 · 1차 렌더 안 함
  </behavior>
  <action>
**새 건 기본값(M-4 — UI-SPEC S9 「새 건의 기본값」):** `domain/corp-card-usages/index.ts`에 `cardUsageFormDefaults(viewer, today)`를 두고 `page.tsx`가 `?new=1`일 때 불러 `card-usage-form.tsx`에 초기값으로 넘긴다 — 사용일 = 오늘(KST), 카드 = 그 사람이 마지막으로 등록한(보관 안 된) 카드 사용의 카드가 지금 옵션에 있을 때만, 연결 = 그 등록의 연결 종류(이 플랜은 `팀 비용`뿐). 옵션 판정은 폼 옵션과 같은 함수의 결과로 한다(새 판정 없음). 모두 바꿀 수 있고, 안내 문구는 두지 않는다. 목록 필터(월 · 카드 · 연결 · 등록 — 등록 필터는 권한자에게만, GET 이동), 50건 `Pagination`(쪽 첫 줄에 카드 그룹 머리글 반복), 합계 줄(서버 `sumKrw`), 가맹점 없음 `—`, 증빙 `있음`/`—` 글자(색 없음), 외화 2행 두 묶음, 등록 칸(`registered_via` = proxy면 `경영관리 등록` + 2행 등록자 · 날짜 — 대리 등록 자체는 06-09), 좁은 PC P3 증빙 숨김(열 `priority`), Empty 세 갈래, 로드 오류 한 줄, 필터 줄 오른쪽 3차 `구매 요청` 링크(SP-4 — 목적지 `/cards/purchases`는 06-08이 만들고 건수는 06-14가 채운다; 06-08 전까지 링크를 렌더하지 않는다). `test/integration/leak-scan.test.ts`에 카드 사용 목록 DTO를 직원·권한자 두 계급으로 검사하는 케이스(범위 밖 행이 없는지)를 더한다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 목록 DTO가 범위 밖 행이나 칸을 흘렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts test/e2e/corp-cards.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — corp-cards가 빨가면 카드 마스터 화면을 깨뜨렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 누수 스캔이 직원 계급의 목록 결과에 남의 카드 사용이 없음을 단언한다
    - E2E에 「쓸 카드 0장 → Empty · 1차 없음」 · 「필터 이동이 GET 쿼리」 케이스가 녹색이다
    - [M-4] E2E에 「카드 두 장 직원이 카드 B · 팀 비용으로 한 건 등록 → 다시 `카드 사용 등록` → 사용일 = 오늘 · 카드 = B · 연결 `팀 비용`이 채워져 열린다」 · 「처음 쓰는 직원 → 사용일 = 오늘 · 연결 비어 있음」 케이스가 녹색이다
    - [M-5] 같은 E2E가 `팀 비용` 연결의 팀 텍스트(`기획1팀` 꼴) 아래에 `Form.Hint` 요소가 0개임을 단언한다
  </acceptance_criteria>
  <done>S8 목록이 계약대로 서고 범위가 리포지토리 쿼리에서 갈린다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 카드 사용 액션 | 카드 · 사용일 · 결제 합계 · 통화 · 환율 · 증빙 종류 · 연결이 넘어온다. 공급가·부가세·원화 환산액은 넘어와도 저장되지 않는다 |
| 리포지토리 → 목록 | 범위 조건이 쿼리에 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-20 | Tampering | 공급가·부가세 조작 | high | mitigate | zod 입력에 계산 필드 없음 · 서버 `splitCardTotal` 재계산(D-607) |
| T-06-21 | Elevation of Privilege | 남의 카드로 본인 등록 | high | mitigate | 카드 자격(소지자 본인 · 사용일 소속 팀의 카드)을 서버가 판정 · 남의 카드 거부 케이스 |
| T-06-22 | Tampering | 연결 없는 카드 사용 | high | mitigate | XOR DB CHECK + domain 거부(EXP-07) |
| T-06-23 | Information Disclosure | 카드 사용 목록 DTO | medium | mitigate | 범위를 리포지토리 쿼리에서 거르고 `registerDto` 등록 → 누수 스캔 계급별 케이스 |
| T-06-24 | Tampering | 팀 비용 귀속 | medium | mitigate | 팀은 입력이 아니라 `teamAtDate`로 서버가 정한다 · 소속 없음이면 거부 |
| T-06-25 | Denial of Service | 신규 표 마이그레이션 | low | mitigate | SET LOCAL 한 쌍(0010 선례) · `pnpm lint:sql` 0건 |
| T-06-152 | Denial of Service | 카드 사용 트랜잭션의 풀 고갈 교착 | high | mitigate | 전역 `db` 판정·조회(카드 · 소속 · 코드표 · 세율 · 뒤 플랜의 `can`)는 `precheckCardUsage`가 트랜잭션 전에 끝내고 `runCreate`는 tx 리포지토리 · 순수 함수만(06-03 tx 규약, CROSS E-3). acceptance region grep 0건 · 외부 tx 경로의 동시 6건은 06-12 「구매 완료 동시 6건」이 증명한다 |
</threat_model>

<verification>
- 단위 card-usage-amounts(잔차 ≠ 0 두 케이스 포함) · leak-scan-coverage, 통합 leak-scan 녹색
- `createCardUsage`의 `runCreate` 범위에 전역 `db` 판정·조회 호출 0건(06-03 tx 규약 — Task 1 acceptance)
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E card-usage · corp-cards가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S8 · S9 DOM 실측 감사(backstop 1건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 카드 사용이 연결 없이 저장될 수 없다(액션 · DB 둘 다)
- 사람은 결제 합계만 적고 공급가·부가세·원화는 서버가 계산한다
- `/cards` 목록이 범위대로 보인다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `corp_card_usages`(보관 칸 `archived_at` · `archived_by` 포함 — H-4 기반) |
| 마이그레이션 | `db/migrations/NNNN_corp_card_usages.sql`(SET LOCAL 한 쌍) |
| 함수(domain) | `precheckCardUsage(viewer, input)`(트랜잭션 밖) · `createCardUsage(viewer, input, pre, tx?)`(본인 · 팀 비용 — 단독/외부 tx) · `listCardUsages`(보관 건 제외) · `splitCardTotal(total, rates)`(순수) · `cardUsageFormDefaults(viewer, today)`(새 건 기본값 — M-4) |
| 리포지토리 | `repositories/corp-card-usages.ts` |
| Server Action | `createCardUsageAction` · `previewCardAmountsAction` |
| 화면 | `/cards` 목록·폼(`card-usage-list.tsx` · `card-usage-form.tsx`) |
| 테스트 | `test/unit/domain/card-usage-amounts.test.ts` · `test/e2e/card-usage.spec.ts` · leak-scan 케이스 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-05-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-3 — `createCardUsage`가 트랜잭션을 스스로 여는지 외부 `tx`를 받는지 정하지 않았고, 권한·카드·세율 조회가 전역 `db`다(eng-cross-opus.md L22-24@333f7bf; 코드 확인: `repositories/corp-cards.ts` `findCorpCardById` tx 인자 없음 · `domain/permissions/can.ts` → `findPermission` 전역 `db` · `lib/env.ts:54` `DB_POOL_MAX` 기본 5) | 고침 | Plan 05, Task 1 ②: `precheckCardUsage(viewer, input)`(트랜잭션 밖) → `createCardUsage(viewer, input, pre, tx?)`(`runCreate` 단독/외부 tx 이중 경로 — `domain/quotes/lines.ts` 481·547 꼴) · 행동 로그는 커밋 뒤. must_haves.truths · artifacts · key_links · acceptance region grep · T-06-152 |
| CROSS R-1 — 잔차를 `diffKrw(합계, 공급가)` 부가세로 셈하면 늘 0이라 `· 반올림 차이 N` 테스트가 실패할 수 없다(L35@333f7bf) | 참고 | Plan 05, Task 2: 재계산 부가세 = `round(공급가 × 세율, 단위, 방식)`을 따로 셈한다(revenue 92-94 선례) · behavior 잔차 ≠ 0 두 케이스 — 합계 1,000,001 · 단위 10 → −1, 합계 1,000,004 · 단위 1 → +1(값은 `domain/money` 식으로 계산해 확인; 기본 단위 1에서 1,000,001은 잔차 0이라 단위 10 조건을 붙였다) · fails_when |
| CROSS R-2 — 카드 역산 세율의 기준일이 명시되지 않았다(L36@333f7bf) | 참고 | Plan 05, must_haves.truths · Task 2 action: 기준일 = 사용일(`loadTaxRates(usedOn)`), 06-03 증빙일 기준과 같은 규칙. acceptance 줄 번호 + 06-07 Task 2 통합 「세율 기준일 = 사용일」 |
| ENG E-3 — tx 규약 참조(eng-review.md L72-74@333f7bf) | 고침 | must_haves.truths에 「06-03 tx 규약」 참조 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| CROSS E-3 대안 「`applyTaxRule` deps에 tx로 묶인 `getSettingValue`를 넘긴다」(E-1 ⒝와 같은 꼴) | 채택 안 함 — 고정 규약(세율은 `loadTaxRates`로 트랜잭션 전 사전 조회, 계산기는 `rates` 인자)을 따른다. `findEffectiveValue`에 tx 인자가 없어(audit) 그 대안은 리포지토리 수정까지 번진다 |

### Round 2 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 10 · 12)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| M-4 — S9 새 건이 빈 폼으로 열려 사용일 · 카드 · 연결을 매번 고른다(design-review.md L51@f2720a8 · 반영 지시 12 L85@f2720a8) | MEDIUM(고침) | Plan 05: must_haves.truths `[M-4]` 줄. Task 3 이름 · files(`card-usage-form.tsx`) · read_first(UI-SPEC S9 「새 건의 기본값(rev 9, M-4)」) · behavior `[M-4]` 네 갈래 · action 「새 건 기본값」 문단(`cardUsageFormDefaults(viewer, today)` → `page.tsx` `?new=1` → 폼 초기값, 옵션 판정은 폼 옵션과 같은 함수) · acceptance E2E 두 케이스. 이 플랜은 연결 종류 `팀 비용`까지 — 견적 줄 · 견적 외 비용과 프로젝트 기본값은 06-07이 같은 함수를 넓힌다. Artifacts 함수 행 |
| M-5 — 카드 폼 안내 문구 중 설명형(결과 없는 것)을 지운다(design-review.md L52@f2720a8 · 반영 지시 12 L85@f2720a8) | 참고(고침) | Plan 05: must_haves.truths 「`팀 비용`」 줄에 「힌트 없음」(UI-SPEC Copywriting 「표시 — 카드 폼 연결 「팀 비용」」 rev 9) · Task 3 acceptance E2E 단언(`Form.Hint` 0개). 이 플랜에 다른 설명형 힌트는 없다 |
| H-4 기반 — 카드 사용 삭제를 모달 없는 즉시 삭제 + `되돌리기`로 바꾸려면 되돌릴 수 있는 삭제가 필요하다(design-review.md L41@f2720a8 · 반영 지시 10 L83@f2720a8) | HIGH(기반만) | Plan 05: must_haves.truths `[H-4 기반]` 줄 · 스키마에 ADMN-12 보관 칸(`archived_at` · `archived_by`, `corp-cards` 선례) · 리포지토리 목록이 보관 건을 늘 뺀다 · Task 3 behavior 한 줄 · Artifacts 표 행. 삭제(= 보관) · `되돌리기`(= 보관 해제) 동작 자체는 06-09 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 사용자 확정(2026-09-25) — 이 플랜에 해당하는 항목 없음(DR-2 · DR-3 · DR-4는 이 플랜 범위 밖) |
