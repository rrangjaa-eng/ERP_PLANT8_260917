---
phase: 06-payment-evidence-cards
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - db/schema/revenue-issue-requests.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - domain/issue-requests/index.ts
  - repositories/revenue-issue-requests.ts
  - domain/projects/ledger.ts
  - domain/revenue/index.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/page.tsx
  - app/(app)/projects/filter-bar.tsx
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - app/(app)/projects/[id]/issue-request-table.tsx
  - app/(app)/projects/issue-requests/page.tsx
  - app/(app)/projects/issue-requests/issue-requests.module.css
  - test/unit/domain/issue-requests.test.ts
  - test/integration/issue-requests.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/issue-requests.spec.ts
autonomous: true
requirements: [PROJ-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "담당 PM(프로젝트 쓰기, 1024 이상)이 상세 매출 섹션의 「발행 요청」 표에서 3차 `발행 요청 추가`로 줄을 만들고(희망 발행일 기본 오늘) 금액(공급가)·메모를 적어 1차 `일괄 저장 Ctrl+S N`을 누르면 요청이 `요청` 상태로 저장된다 — 새 1차 버튼은 없다(D-610, UA-616)"
    - "한 프로젝트에 발행 요청을 여러 개 둘 수 있고, 요청 금액의 부가세·합계는 서버가 희망 발행일 기준 세율로 계산해 2행 `부가세 · 합계`로 보낸다(`%` 글자 없음)"
    - "매출 기록 권한자(`projects.revenue` write, 1024 이상)가 `요청` 줄의 3차 `발행 줄로`를 누르면 발행 줄 표에 새 줄(발행일 = 희망 발행일, 발행액 = 요청 금액, 메모 = 요청 메모)이 생기고 그 발행일 셀에 포커스가 가며, 요청 줄 상태 2행이 `발행 줄 입력 중`(`--muted`)이 된다"
    - "그 상태로 `일괄 저장`하면 같은 트랜잭션에서 발행 줄이 저장되고 요청이 `발행됨`(2행 `발행 {MM-DD} · {발행액}`)이 되며 요청 행에 발행 줄 id가 남는다 — 저장 전에 새 발행 줄을 지우면 연결도 풀린다(D-610)"
    - "요청 금액과 발행액이 달라도 막지 않는다(선금·잔금 조정)"
    - "[O-12 기본값] 발행 요청 상태는 `요청` · `발행됨` · `취소` 셋이고 금액은 공급가다 — PM이 `Delete` → 확인 모달 `발행 요청 취소`로 `취소`로 바꾸면 줄은 남는다"
    - "`요청` 상태이고 아직 발행 줄에 이어지지 않은 줄만 PM이 고칠 수 있다 — `발행됨`·`취소` 줄 수정 요청은 서버가 거부한다"
    - "완료(정산) 프로젝트의 발행 요청 추가·수정·취소는 기존 `project.completed-lock` 판정으로 막힌다"
    - "발행액을 볼 수 없는 사람(`revenue.issued_amount` 없음, A-605)에게는 상태 2행의 발행액을 서버가 보내지 않는다"
    - "`/projects/issue-requests`(S17)는 매출 기록 권한자·대표(읽기)에게 서고, 프로젝트 목록 필터 줄 오른쪽 3차 `발행 요청 {열린 건수}`(0이면 `발행 요청`)로 들어간다 — PageHeader 부제 = 부모 메뉴 이름(SP-4)"
    - "S16 empty: 「Empty — 상세 「발행 요청」 표」 세 갈래(쓰기 PM 1024 이상 · 그 밖의 사람 · 1024 미만)가 선다"
    - "S16 loading: 저장은 상세의 `일괄 저장 Ctrl+S N` 흐름 그대로이고 저장 뒤 `저장됨 N줄 {시:분}`이다 — 발행 요청 표의 dirty도 N에 든다"
    - "S16 error: 저장 실패·동시성은 Phase 4 일괄 저장 오류 셀 흐름 + `version` 낙관적 잠금으로 표시된다"
    - "S16 populated: 열 희망 발행일 · 금액(2행 `부가세 · 합계`) · 메모 · 상태(`요청` / `발행됨` + 2행 `발행 {MM-DD} · {금액}` / `취소`)"
    - "S16 partial: `발행 줄로` 뒤 저장 전 = 요청 줄 상태 2행 `발행 줄 입력 중`(`--muted`), 새 발행 줄을 지우면 연결이 풀리고, 발행액을 볼 수 없는 사람에게는 상태 2행 금액이 없다"
    - "S16 overflow: 줄이 몇 개뿐인 섹션 표라 페이지를 나누지 않는다(§7-3 (자))"
    - "S17 empty: 「Empty — 발행 요청 목록(S17)」이 선다"
    - "S17 loading: 첫 로드 = §7-7 LOADING 뼈대, 상태 필터 이동은 GET 이동이다"
    - "S17 error: 「Error — 목록 로드」 발행 요청 문구가 선다"
    - "S17 populated: 그룹 희망 발행일(`희망일 지남`(`--warning`) → `이번 주` → `다음 주` → `그 뒤`) · 열 · 합계 줄 `합계 (요청 · N건)` · 1차 없음 · 행 3차 `요청` 행 `발행 줄 입력` / `발행됨`·`취소` 행 `요청 보기`(1024 이상 · 매출 기록 권한자) — 둘 다 `/projects/{id}?issueRequest={rid}`로 가서 그 요청 줄로 포커스한다"
    - "S17 partial: 1024 미만에는 행동 칸이 없고 행을 누르면 같은 주소의 보기 전용 상세로 간다"
    - "S17 overflow: 50건 페이지(§7-16, UA-603)"
    - "S17 zero-one-many: 프로젝트 목록 링크는 `발행 요청 N`, 0이면 `발행 요청`만 — 링크는 남는다(SP-4)"
    - statement: "S16 zero-one-many: 요청 줄 여럿이 발행 줄 표와 함께 읽힌다 — 계획 판단: 요청 줄 순서는 희망 발행일 오름차순, 같은 날은 만든 순. CI=true DOM 감사"
      verification: backstop
    - statement: "S16 long-text: 메모가 표 안 두 줄까지, 셋째 줄부터 말줄임이다 — 200자 메모로 CI=true DOM 감사"
      verification: backstop
    - statement: "S17 long-text: 프로젝트 이름·메모가 말줄임 + `title`로 선다 — 긴 이름·200자 메모로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "발행 요청을 발행 줄에 잇는 일을 매출 기록 권한(`projects.revenue` write) 밖의 사람이 할 수 있어서는 안 된다 — PM은 요청만, 발행은 경영관리(D-610)"
      status: resolved
      verification: test
    - statement: "발행액을 볼 수 없는 사람의 DTO에 발행액이 실려서는 안 된다(A-605 — 요청 상태 2행을 통한 우회 노출)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/revenue-issue-requests.ts"
      provides: "발행 요청 표 — 프로젝트 · 희망 발행일 · 금액(공급가 Money 묶음) · 메모 · 상태 3종 · 이어진 발행 줄 id(유니크) · 요청자 · version"
      exports: ["revenueIssueRequests"]
    - path: "domain/issue-requests/index.ts"
      provides: "발행 요청 저장(일괄 저장 트랜잭션 안) · 취소 · 발행 줄 잇기 · 프로젝트 표 DTO · 목록(S17) DTO"
      exports: ["listProjectIssueRequests", "listIssueRequests", "saveIssueRequestRows", "cancelIssueRequest", "linkIssueRequestToEntry"]
    - path: "test/integration/issue-requests.test.ts"
      provides: "요청 → 발행 줄 잇기 원자성 · 권한 · 노출 증명"
      min_lines: 40
  key_links:
    - from: "domain/projects/ledger.ts"
      to: "domain/issue-requests/index.ts"
      via: "일괄 저장 한 트랜잭션이 발행 요청 줄까지 저장한다"
      pattern: "saveIssueRequestRows"
    - from: "domain/revenue/index.ts"
      to: "domain/issue-requests/index.ts"
      via: "요청에서 온 새 발행 줄 INSERT 직후 같은 트랜잭션에서 요청을 발행됨으로 잇는다"
      pattern: "linkIssueRequestToEntry"
---

<objective>
PM이 경영관리에게 세금계산서 발행을 요청하고(D-610), 매출 기록 권한자가 그 요청을 발행 줄로 이어 닫는 경로를 만든다 — 상세 매출 섹션의 「발행 요청」 표(S16)와 발행 요청 목록(S17). 이것이 완료 전 점검(06-09)의 「매출 미입력」 그룹이 가리키는 다음 한 수다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: D-613의 「매출 미입력 = 발행 줄 없음」을 PM이 스스로 풀 수 없다 — 발행은 경영관리 몫이다. 그래서 PM의 요청이 원장에 남고 발행 줄로 닫혀야 점검이 사람 사이의 메신저가 아니라 화면으로 돈다.

Output: `revenue_issue_requests` 표(A-613 — 신규 표를 택했다: 기존 `revenue_entries.kind` 확장은 `REVENUE_DTO_SPEC`과 발행·입금 합계에 요청이 섞여 들어가 충돌한다), `domain/issue-requests`, 일괄 저장 확장, S16 · S17, 단위 1 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-05 착수하지 않음`을 출력하고 멈춘다.**

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-613 | 선행 없음 — 이 플랜이 발행 요청 표를 처음 설계한다 | 확인만: `git ls-files 'db/schema/*issue-request*'` 비어 있어야 정상 |
| A-605 | `revenue.issued_amount` 정보 항목의 현재 기본값(main은 `staffDefault: false`) | `grep -n "revenue.issued_amount" -A 4 domain/permissions/info-items.ts` 결과를 SUMMARY에 적는다 — 값이 무엇이든 이 플랜은 정보 항목으로 가르므로 멈추지 않는다. 항목 자체가 없으면 멈춤 |
| A-604 | 계약 금액 쓰기 경로(`saveRevenue`의 `contract`)와 `projects.contract` 컬럼 — 이 플랜은 계약 금액을 건드리지 않고 발행 줄 경로만 쓴다 | `grep -n "issuedEntries" domain/revenue/index.ts` 0건이면 멈춤 |
| UA-616 | 상세의 `일괄 저장 Ctrl+S N` 하나가 견적 줄 + 매출을 한 트랜잭션으로 저장한다(main `saveProjectLedgerAction` · `domain/projects/ledger.ts`) | `grep -n "saveProjectLedger" domain/projects/ledger.ts` 0건이면 멈춤 |
| UA-603 | `ui/pagination/Pagination.tsx`(S17 50건) | `git ls-files ui/pagination/Pagination.tsx` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(`발행 요청 취소`) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-12 | 상태 셋(`요청`·`발행됨`·`취소`) + 금액 = 공급가 | 스키마 CHECK · `domain/issue-requests`의 상태 전이 · 취소 액션(대안 「취소 = 행 삭제 + 합계 기준」이면 셋 다) |
| O-1 | 경영관리 작업 화면은 1차 메뉴에 분산 — 발행 요청 목록은 프로젝트 메뉴의 하위 목록(SP-4) | 라우트 `app/(app)/projects/issue-requests/` · 필터 줄 링크 |
| AS2 | 요청 저장·발행 잇기 = `document_create`/`document_update`, 취소 = `status_change` | `recordAction` 인자 |

**계획 판단(Claude 재량):** 요청 금액 열은 프로젝트 쓰기 권한자(요청을 적는 PM)와 `revenue.issued_amount`를 가진 사람에게 보내고, 그 밖의 읽기 사람에게는 보내지 않는다(권한 표 S16 행 「읽기(발행액 볼 수 있을 때만 금액)」). 요청 줄 순서는 희망 발행일 오름차순 · 같은 날은 만든 순.

<probe_fallback>
coverage.json의 이 플랜 몫: **PROJ-06 — unclassified → unresolved (flagged, 자동 해소·기각 없음)**. 이 플랜은 PROJ-06의 「매출 미입력」 점검이 가리키는 닫는 길을 만든다(D-610 · D-613). 점검 자체는 06-09. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — PM이 발행 요청 한 줄을 일괄 저장하고, 매출 기록 권한자가 「발행 줄로」 → 일괄 저장해 요청이 「발행됨」이 된다</name>
  <files>db/schema/revenue-issue-requests.ts, db/schema/index.ts, db/migrations/meta/_journal.json, domain/issue-requests/index.ts, repositories/revenue-issue-requests.ts, domain/projects/ledger.ts, domain/revenue/index.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/issue-request-table.tsx, test/integration/issue-requests.test.ts, test/integration/leak-scan.test.ts, test/e2e/issue-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 06-01 · 06-03이 끝났다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 713-733행(S16 · S17 정본) · 1034-1049행(considerations) · 834행(발행 요청 합계 계산) · Copywriting 「Empty — 상세 「발행 요청」 표」
    - app/(app)/projects/actions.ts 50-110행(`saveProjectLedgerAction` 스키마) · domain/projects/ledger.ts · domain/revenue/index.ts 80-175행(DTO · 역산 선례) · 232-330행(`saveRevenue` 발행 줄 경로)
    - app/(app)/projects/[id]/revenue-section.tsx(발행 줄 표 · dirty 계산 · 오류 셀) · app/(app)/projects/[id]/page.tsx
    - db/schema/revenue-entries.ts · db/schema/money-columns.ts
    - test/integration/revenue-entries.test.ts · test/e2e/revenue-section.spec.ts(선례)
  </read_first>
  <behavior>
    - `saveIssueRequestRows`: 프로젝트 쓰기 권한 없음 → 거부 / 새 줄 → `요청` + 요청자 / `발행됨`·`취소` 줄 수정 → 거부 / version 불일치 → 거부
    - 발행 줄 행에 `fromIssueRequestId`가 있으면: 매출 기록 권한 없음 → 거부 / 요청이 `요청` 아님 → 거부 / 정상 → 발행 줄 INSERT 뒤 같은 트랜잭션에서 요청 `발행됨` + 발행 줄 id
    - 발행 줄 INSERT가 실패하면 요청 상태도 바뀌지 않는다(한 트랜잭션)
    - 요청 금액의 부가세·합계는 발행 줄과 같은 서버 계산(희망 발행일 기준 세율)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. A-605 결과를 SUMMARY에 적는다.

① **스키마(A-613).** `revenue_issue_requests`(id · project_id FK · desired_issue_date date · `moneyColumns("amount")`(공급가, 발행 줄과 같은 금액 모델) · memo text null · status text CHECK in (requested, issued, cancelled) · issued_entry_id FK → revenue_entries null UNIQUE · requested_by FK · cancelled_by null · cancelled_at null · version · source 'demo' · 생성 시각). `issued`이면 issued_entry_id가 있다는 CHECK. `pnpm db:generate --name revenue_issue_requests` → `pnpm lint:sql` → `pnpm db:migrate` → 두 번째 generate `No schema changes`.

② **domain.** `domain/issue-requests/index.ts`: `listProjectIssueRequests(viewer, projectId)`(DTO — 금액 열은 계획 판단대로, 상태 2행 발행액은 `revenue.issued_amount`로 가른다, 부가세·합계는 `domain/revenue`의 발행 줄 계산 함수를 재사용 — 새 세금 식을 만들지 않는다), `saveIssueRequestRows(viewer, projectId, rows, tx)`, `linkIssueRequestToEntry(viewer, requestId, entryId, tx)`. `domain/projects/ledger.ts`의 `saveProjectLedger`가 입력 `issueRequests`를 같은 트랜잭션에서 `saveIssueRequestRows`로 넘기고, `domain/revenue/index.ts`의 발행 줄 쓰기 행 타입에 선택 필드 `fromIssueRequestId`를 더해 INSERT 직후 `linkIssueRequestToEntry`를 부른다(`projects.revenue` write 확인은 기존 발행 줄 경로의 것을 그대로 쓴다). 완료 잠금은 기존 `project.completed-lock` 판정을 지난다. DTO `registerDto` + `test/integration/leak-scan.test.ts`에 `@/domain/issue-requests` import.

③ **액션 · 화면.** `saveProjectLedgerAction`의 zod 스키마에 `issueRequests` 배열(id? · version? · desiredIssueDate · amount(moneyInputSchema) · memo)과 발행 줄 행의 `fromIssueRequestId`를 더한다 — 부가세·합계 필드는 없다. `issue-request-table.tsx`(S16 — 계약 금액 줄과 발행 줄 표 사이, 소제목 `발행 요청`): 열 희망 발행일 · 금액(2행 서버 계산) · 메모(두 줄 말줄임) · 상태. PM(프로젝트 쓰기 · 1024 이상)에게 3차 `발행 요청 추가`, `요청`·미연결 줄 편집. 매출 기록 권한자에게 `요청` 줄 상태 칸 3차 `발행 줄로` → `revenue-section.tsx`의 발행 줄 표에 미리 채운 새 줄을 넣고(클라이언트 상태에 `fromIssueRequestId`) 그 발행일 셀에 포커스, 요청 줄 2행 `발행 줄 입력 중`. 그 새 줄을 지우면 연결도 풀린다. 발행 요청 표의 dirty가 `일괄 저장 Ctrl+S N`의 N에 든다. 1024 미만은 보기 전용. Empty 세 갈래는 Copywriting 행 그대로. `page.tsx`가 요청 목록을 함께 불러 넘긴다.

④ **E2E.** `test/e2e/issue-requests.spec.ts`: PM → 발행 요청 추가 → 일괄 저장 → 매출 기록 권한자 → `발행 줄로` → 일괄 저장 → 요청 상태 `발행됨` + 2행.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 개수를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다. revenue-entries·quote-lines가 빨가면 일괄 저장 확장이 기존 경로를 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-605 값 포함)가 SUMMARY에 있다
    - 통합 테스트가 behavior 네 줄의 모든 갈래를 개별 `it`으로 단언한다(원자성 케이스는 발행 줄 INSERT를 실패시켜 요청이 `요청`으로 남는지 확인)
    - `saveProjectLedgerAction` zod 스키마의 `issueRequests` 원소에 부가세·합계 필드가 없다
    - E2E 한 경로와 기존 revenue-section 스펙이 CI=true로 녹색
  </acceptance_criteria>
  <done>발행 요청 한 줄이 PM의 일괄 저장 → 경영관리의 발행 줄 잇기 → `발행됨`까지 한 트랜잭션 규칙으로 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [O-12 기본값] 발행 요청 취소 · 편집 규칙 · 발행액 노출(A-605)</name>
  <files>domain/issue-requests/index.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/[id]/issue-request-table.tsx, test/unit/domain/issue-requests.test.ts, test/integration/issue-requests.test.ts, test/e2e/issue-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 717-722행 · 807행(S16 권한 행) · Copywriting 「Destructive — 발행 요청 취소」
    - /mnt/project-files/06-prep/open-choices.md 21행(O-12)
    - domain/permissions/info-items.ts(`revenue.issued_amount`)
  </read_first>
  <behavior>
    - 상태 전이: 요청 → 발행됨(잇기) / 요청 → 취소(PM) / 발행됨 → 무엇이든 거부 / 취소 → 무엇이든 거부
    - `cancelIssueRequest`: 프로젝트 쓰기 권한 없음 → 거부 / 이어진 요청 → 거부 / 정상 → `취소` + 사람·시각, 줄은 남음, `status_change` 로그
    - DTO: `revenue.issued_amount` 없는 읽기 계정 → 상태 2행 발행액 키 없음, 요청 금액 키 없음 / 프로젝트 쓰기 PM → 요청 금액 있음
  </behavior>
  <action>
`domain/issue-requests/index.ts`에 상태 전이 함수(순수)와 `cancelIssueRequest(viewer, { requestId, version })`를 더하고, 파일 머리 한 줄 주석에 "O-12 답이 바뀌면 상태 전이 함수 · 스키마 CHECK · 취소 액션만 고친다"를 적는다. `app/(app)/projects/actions.ts`에 `cancelIssueRequestAction`, 기존 `actions.registry.ts`에 등록(새 registry 파일 아님). 표에서 PM이 `요청`·미연결 줄에 `Delete` → `ConfirmDialog`(Copywriting 「Destructive — 발행 요청 취소」) → 취소 뒤 줄은 `취소` 상태로 남는다. DTO 노출은 계획 판단(「열린 선택」 아래)대로 서버가 칸을 뺀다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/issue-requests.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 단위 테스트가 상태 전이 표의 여섯 갈래를, 통합 테스트가 취소·노출 갈래를 개별 `it`으로 단언한다
    - 통합 테스트가 `revenue.issued_amount` 없는 계정 DTO에 발행액 값이 없음을 객체 키로 단언한다
  </acceptance_criteria>
  <done>발행 요청의 세 상태와 전이가 한 함수에서 정해지고, 취소가 줄을 남기며, 발행액이 정보 항목 밖으로 새지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 발행 요청 목록(S17) · 프로젝트 목록의 하위 목록 링크(SP-4) · `?issueRequest` 포커스</name>
  <files>domain/issue-requests/index.ts, app/(app)/projects/issue-requests/page.tsx, app/(app)/projects/issue-requests/issue-requests.module.css, app/(app)/projects/page.tsx, app/(app)/projects/filter-bar.tsx, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/issue-request-table.tsx, test/integration/issue-requests.test.ts, test/e2e/issue-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 723행(`?issueRequest` 포커스) · 726-733행(S17) · 887-891행(SP-4) · Copywriting 「Empty — 발행 요청 목록(S17)」 · 「Error — 목록 로드」
    - app/(app)/projects/page.tsx · app/(app)/projects/filter-bar.tsx · app/(app)/projects/projects-table.tsx(목록 선례 · 그룹 머리글 · 합계 줄)
    - ui/pagination/Pagination.tsx
  </read_first>
  <behavior>
    - `listIssueRequests(viewer, { status, page })`: 매출 기록 권한자·`projects.revenue` 읽기 권한자만, 그 밖 → 거부 / 그룹 희망일 지남 → 이번 주 → 다음 주 → 그 뒤(KST 기준) / 합계 줄 건수·금액(서버 합)
    - 열린 건수(`요청` 상태) → 프로젝트 목록 링크 글자 `발행 요청 N`, 0이면 `발행 요청`
  </behavior>
  <action>
`listIssueRequests`(필터 `요청` 기본 · `발행됨` · `취소` · `전체`, 50건 페이지, 그룹 판정은 서버, 합계 `sumKrw`)와 열린 건수 조회를 더한다. `app/(app)/projects/issue-requests/page.tsx`(§6-1 읽기 목록): PageHeader 제목 `발행 요청` · 부제 = 부모 메뉴 이름 `프로젝트`(SP-4) · 1차 없음 · 열 희망 발행일 · 프로젝트(번호 · 이름) · 요청자 · 금액(2행 합계) · 메모 · 상태 + 행동 3차(1024 이상 · 매출 기록 권한자만 — `요청` 행 `발행 줄 입력`, `발행됨`·`취소` 행 `요청 보기`, 둘 다 `/projects/{id}?issueRequest={rid}`). 1024 미만에는 행동 칸이 없고 행을 누르면 같은 주소. Empty · 로드 오류는 Copywriting 행 그대로. `projects/page.tsx`·`filter-bar.tsx`의 필터 줄 오른쪽에 3차 링크 `발행 요청 {N}`(링크는 `projects.revenue` 읽기 권한이 있을 때만 — 대표 포함). 상세 `page.tsx`는 `?issueRequest={id}`면 그 요청 줄로 스크롤·포커스(`발행 줄로`가 선 줄이면 그 3차, 아니면 줄 자체 `tabindex="-1"`).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. projects-list가 빨가면 목록 필터 줄 변경이 기존 목록을 깨뜨렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts test/e2e/projects-list.spec.ts test/e2e/projects-filter-reset.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 두 줄의 갈래(권한 거부 · 그룹 · 합계 · 건수 0)를 단언한다
    - E2E에 「프로젝트 목록 링크 → 목록 → `발행 줄 입력` → 상세의 `발행 줄로`에 포커스」 케이스가 녹색이다
    - 기존 projects-list · projects-filter-reset 스펙 녹색
  </acceptance_criteria>
  <done>경영관리가 프로젝트를 하나씩 열지 않고 발행 요청을 한 목록에서 보고 상세의 정확한 자리로 들어간다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `saveProjectLedgerAction` | 발행 요청 줄과 `fromIssueRequestId`가 넘어온다. 부가세·합계는 넘어와도 파싱되지 않는다 |
| domain/issue-requests ↔ domain/revenue | 발행 줄 INSERT와 요청 상태 전환이 한 트랜잭션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-25 | Elevation of Privilege | 발행 줄 잇기 | high | mitigate | `fromIssueRequestId`가 있는 발행 줄 쓰기는 기존 `projects.revenue` write 확인을 지나야 하고, 요청 상태가 `요청`일 때만 잇는다. 권한 없는 PM 거부 통합 테스트 |
| T-06-26 | Tampering | 이어진 요청 조작 | medium | mitigate | `issued_entry_id` UNIQUE + 상태 전이 함수 + `version`. `발행됨`·`취소` 줄 수정 거부 테스트 |
| T-06-27 | Information Disclosure | 발행액(A-605) | high | mitigate | 상태 2행 발행액과 요청 금액을 정보 항목·쓰기 권한으로 서버가 DTO에서 뺀다. DTO 등록 → 누수 스캔 계급별 검사 |
| T-06-28 | Repudiation | 요청 취소 | low | mitigate | `status_change` 로그 + 취소한 사람·시각 컬럼, 줄 삭제 없음 |
| T-06-29 | Denial of Service | 마이그레이션 | low | mitigate | 새 표 · `pnpm lint:sql` 0건 |
</threat_model>

<verification>
- 단위 1 · 통합 1(+ revenue-entries · quote-lines · projects-list · leak-scan) · E2E 1(+ revenue-section · projects-list · projects-filter-reset) 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml·tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S16·S17 DOM 실측 감사(backstop 3건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- PM의 발행 요청이 원장에 남고, 매출 기록 권한자가 발행 줄로 닫으면 한 트랜잭션에서 `발행됨`이 된다
- 발행 요청이 기존 일괄 저장 1차 하나로 저장된다(새 1차 없음)
- 발행액이 정보 항목 밖으로 새지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `revenue_issue_requests` |
| 마이그레이션 | `db/migrations/NNNN_revenue_issue_requests.sql`(번호는 `pnpm db:generate`) |
| 함수(domain) | `listProjectIssueRequests` · `listIssueRequests` · `saveIssueRequestRows` · `cancelIssueRequest` · `linkIssueRequestToEntry` · 상태 전이 함수 |
| 변경된 기존 심볼 | `saveProjectLedger` 입력(`issueRequests`) · `saveProjectLedgerAction` zod 스키마 · `saveRevenue` 발행 줄 행(`fromIssueRequestId`) |
| Server Action | `cancelIssueRequestAction` |
| 라우트 | `/projects/issue-requests`(S17) |
| 화면 | `app/(app)/projects/[id]/issue-request-table.tsx`(S16) · 프로젝트 목록 필터 줄 `발행 요청 N` 링크 |
| 테스트 | `test/unit/domain/issue-requests.test.ts` · `test/integration/issue-requests.test.ts` · `test/e2e/issue-requests.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-05-SUMMARY.md` when done
</output>
