---
phase: 06-payment-evidence-cards
plan: 06
type: execute
wave: 4
depends_on: ["06-04", "06-05"]
files_modified:
  - db/schema/purchase-requests.ts
  - db/schema/corp-card-usages.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - domain/purchase-requests/index.ts
  - domain/purchase-requests/policy.ts
  - domain/quotes/line-door.ts
  - domain/rules/register.ts
  - domain/document-numbering/index.ts
  - domain/settings/keys.ts
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/rights.ts
  - repositories/purchase-requests.ts
  - repositories/quote-line-links.ts
  - app/(app)/cards/purchases/page.tsx
  - app/(app)/cards/purchases/purchase-request-form.tsx
  - app/(app)/cards/purchases/actions.ts
  - app/(app)/cards/purchases/actions.registry.ts
  - app/(app)/cards/purchases/purchases.module.css
  - app/(app)/cards/page.tsx
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/link-picker.tsx
  - test/unit/domain/line-door.test.ts
  - test/unit/domain/purchase-policy.test.ts
  - test/unit/domain/document-number-format.test.ts
  - test/integration/purchase-requests.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/purchase-requests.spec.ts
autonomous: true
requirements: [EXP-10]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 `/cards/purchases?new=1`에서 품목·링크·예상 금액을 적고 연결(온라인구매 견적 줄 또는 팀 비용)을 골라 1차 「구매 요청」을 누르면 저장 순간 번호(`{프로젝트 번호}-C{순번 4자리}`, 예 `26001-C0001`)가 매겨지고 토스트 `구매 요청 · {번호}`와 목록 첫 줄에 보인다 — 결재는 없다(EXP-10, A-612)"
    - "번호 부여는 요청 INSERT와 같은 트랜잭션의 카운터 증가로 하고, 트랜잭션이 실패하면 그 번호는 결번으로 남는다(04-01 규약)"
    - "구매 요청은 견적 줄의 거래처가 설정 `purchase.online_vendor_name`과 맞는 **온라인구매 줄**에만 이을 수 있고, 그 밖의 줄에 이으려는 요청은 `rules.gate`의 `purchase.line-door`가 거부한다 — 사람이 문을 고르지 않는다(EXP-10, O-13)"
    - "온라인구매 협력사 판정은 `domain/quotes/line-door.ts`의 `resolveLineDoor` 한 함수가 한다 — 설정 문자열과 거래처 이름을 앞뒤 공백 제거 · 유니코드 NFC 정규화 뒤 정확히 비교하고, 설정이 비었거나 어느 거래처와도 맞지 않으면 모든 줄의 문은 지출결의다(계획 판단)"
    - "구매 권한(`cards.purchases` write)이 있는 사람이 `신청` 행의 3차 「구매 완료」로 S13(카드 사용 폼 구매 완료 모드)을 열고 1차 「구매 완료」를 누르면 한 트랜잭션에서 카드 사용 건(`registered_via = purchase`, 등록 칸 `구매 요청 {번호}`)이 생기고 요청이 `구매 완료`가 된다 — 행동 로그 `purchase_process`"
    - "[O-10 기본값] 예상 금액은 결제 합계(부가세 포함) 기준이고, 구매 완료 때 결제 합계가 다르면 `예상 금액 {원화} · 차이 {±원화}`(`--muted`)만 보이고 막지 않는다 — 비교는 두 금액의 원화 환산액으로 서버가 한다"
    - "[O-9 기본값] 요청 취소는 `신청` 상태에서만 된다 — 요청자는 자기 `신청` 요청을 사유 없이, 구매 권한자는 남의 `신청` 요청을 사유와 함께 취소하고, `구매 완료` 요청의 취소 요청은 서버가 거부한다"
    - "[O-19 기본값] 팀 비용 구매 요청에서 생기는 카드 사용의 사용한 사람 = 요청자, 팀 = 구매 완료 사용일의 요청자 소속(`teamAtDate(요청자, 사용일)`)이다 — 구매 완료하는 사람의 소속·카드의 팀은 팀을 정하지 않고, 사용일 소속이 없으면 1차 「구매 완료」가 막히며 서버도 거부한다. 이 판정은 `domain/purchase-requests/policy.ts`의 `purchaseTeamCostAttribution` 한 함수에서만 나온다"
    - "[O-20 기본값] 구매 요청자와 구매 완료 처리자가 같아도 막지 않는다 — 판정은 `domain/purchase-requests/policy.ts`의 상수 `PURCHASE_REQUESTER_MAY_COMPLETE = true` 한 곳에서만 나온다"
    - "구매 완료로 생긴 카드 사용 건은 연결을 바꿀 수 없고 삭제가 없다(O-11) — 그 건을 여는 사람이 지금 구매 권한을 가졌으면 카드가 `Select`(활성 카드 전부 + 저장된 카드가 현재 값)이고, 다른 카드로 저장하면 행동 로그에 `카드 고침 · {이전 카드} → {새 카드}`가 남는다"
    - "요청의 링크는 `http://`·`https://`만 저장되고, 목록의 링크 아이콘은 새 탭 `rel=\"noopener noreferrer\"` · `aria-label=\"{품목} 링크 열기\"` · `title` = URL이다"
    - "외화 예상 금액은 환율(기본 = 설정 최근 환율)과 서버 `toKrw` 원화 환산액이 늘 함께 저장된다 — 환율이 없으면 1차가 막히고 저장된 요청에 `계산 불가` 상태가 없다"
    - "S11 empty: 「Empty — 구매 요청 목록」(기본 보기 0건은 경영관리 / 요청자 두 갈래, 전체 0건 한 갈래)"
    - "S11 loading: 첫 로드 = §7-7 LOADING 뼈대, 상태·월 필터 이동은 GET 이동, 행 3차 `구매 완료`는 폼 이동이고 `요청 취소`는 확인 모달(요청 중 1차 `…`)"
    - "S11 error: 「Error — 목록 로드」 구매 요청 문구, 남의 요청 취소 사유 칸 = 「Error — 사유 근거 칸」"
    - "S11 populated: 열(번호 · 요청일 · 품목 + 링크 아이콘 · 연결 · 요청자 · 예상 금액 · 상태 + 행동 칸) · 구매 완료 행 2행 `카드 사용 {MM-DD} · {결제 합계}` · 취소 행 2행 `취소 {MM-DD} · {사람} · {사유}` · 합계 줄"
    - "S11 partial: 외화 예상 금액은 2행 `USD 1,000.00 @1,350`, 합계는 원화 환산액만, 팀 비용 요청의 취소 모달에는 결과 줄이 없고, 취소는 `신청` 상태에서만이다"
    - "S11 overflow: 50건 페이지(§7-16), `전체` 보기에서 상태 그룹 `신청` → `구매 완료` → `취소`"
    - "S11 zero-one-many: 합계 줄 `합계 (신청 · N건)`, 카드 목록의 링크는 0이면 `구매 요청`만(SP-4)"
    - "S12 empty: `?new=1`로 들어오면 연결 `팀 비용`이 골라져 있고(라디오는 남는다), `line` 쿼리로 들어오면 연결이 텍스트다. 빈 칸 = `품목 · 예상 금액 2칸 비어 있음 · 품목 적기`"
    - "S12 loading: 외화 원화 환산 `Form.Hint`는 서버 계산 — 이전 값 `--faint` → 새 값, 1차 처리 중 `…`"
    - "S12 error: 「Error — 구매 요청 폼 칸」 · 「막힘 — 구매 요청 폼」(서버 문 가르기 거부 · 팀 비용 요청자 오늘 소속 없음 포함)"
    - "S12 populated: 연결을 고른 뒤 프로젝트·견적 줄이 텍스트 + 3차 `바꾸기`(S10, 온라인구매 줄만), `팀 비용`이면 요청자의 오늘 소속 텍스트 + `Form.Hint` `구매 완료 날 요청자 소속으로 정함`(O-19)"
    - "S12 partial: 외화인데 설정에 최근 환율이 없으면 환율 칸이 비고 1차가 막힌다, 입력이 있는데 `취소 Esc` = 「확인 — 입력 버리기」"
    - "S12 zero-one-many: 온라인구매 줄 0 = 1차 `이 줄로` 비활성 + `온라인구매 줄 없음 · 팀 비용으로`, 견적 줄 0 = `이 프로젝트에 견적 줄이 없습니다 · 팀 비용으로`(「Empty — 연결 고르기 목록」)"
    - "S13 empty: 기본값으로 채워 연다 — 가맹점 = 온라인구매 협력사 · 결제 합계 = 예상 금액(통화·외화·환율 그대로) · 사용일 오늘 · 연결 = 요청의 것(텍스트), 팀 비용이면 팀 = 요청자의 사용일 소속(읽기, `사용한 사람` 칸 없음)"
    - "S13 loading: 서버 계산 한 줄 이전 값 `--faint` → 새 값, 팀 비용이면 사용일을 바꿀 때 팀도 같은 꼴로 다시 온다, 1차 `구매 완료` 처리 중 `…`"
    - "S13 error: S9 칸 오류와 같은 문구 + 그사이 상태가 바뀌면 1차 비활성 + `구매 요청 취소됨 · 새로 고침` / `이미 구매 완료 · 새로 고침`, 팀 비용인데 요청자 사용일 소속 없음 = `요청자 {이름} {MM-DD} 소속 없음 · 소속 발령은 관리자`"
    - "S13 populated: 제목 `구매 완료 — {번호} · {품목}` · 카드 옵션 = 활성 카드 전부(구매 권한자) · 등록 칸 = `구매 요청 {번호}`"
    - "S13 partial: 결제 합계 ≠ 예상 금액이면 `예상 금액 {원화} · 차이 {±원화}`(`--muted`, 막지 않음)"
    - "S13 zero-one-many: 연결은 요청의 것 하나라 텍스트(「바꾸기」 없음), 카드는 여러 장 = `Select` · 한 장 = 텍스트"
    - "S10(구매 요청 폼 갈래 — 06-03 컴포넌트에 `purchase` 모드): 온라인구매 줄만 고를 수 있고 나머지 줄은 `aria-disabled` + 2행 `거래처 {이름} · 지출결의로`, 고를 수 있는 줄 0이면 `온라인구매 줄 없음 · 팀 비용으로`"
    - statement: "S11 long-text: 품목은 한 줄 말줄임 + `title`, 링크는 아이콘 3차라 URL 길이가 칸을 밀지 않는다 — 긴 품목 이름으로 CI=true DOM 감사"
      verification: backstop
    - statement: "S12 overflow: 구매 요청 폼에서 연 S10 목록(온라인구매 줄만 고를 수 있는 긴 목록)이 스크롤로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S12 long-text: 긴 쇼핑몰 URL이 링크 칸 480에서 `overflow-wrap: anywhere`로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S13 overflow: 활성 카드 30장 이상의 카드 `Select`가 말줄임으로 선다(S9와 같은 컴포넌트) — CI=true DOM 감사"
      verification: backstop
    - statement: "S13 long-text: 긴 품목 이름이 제목 줄에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S20 partial: `온라인구매 협력사` 문자열이 어느 거래처와도 맞지 않을 때 설정 화면은 추가 표시 없이 §7-2 기본 모양 그대로이고(UA-614 — 화면 코드 없음), 그 결과는 견적 줄 표의 문이 전부 `지출결의 올리기`인 것으로 드러난다(계획 판단) — 맞지 않는 문자열을 넣어 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "사람이 견적 줄의 문(구매 요청 / 지출결의)을 고를 수 있어서는 안 된다 — 문은 거래처 설정에서만 나오고 서버 게이트가 반대 문을 거부한다(EXP-10)"
      status: resolved
      verification: test
    - statement: "구매 요청의 링크에 `http`·`https` 밖의 스킴(`javascript:` 등)이 저장되거나 새 탭이 여는 쪽 창에 접근할 수 있어서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/purchase-requests.ts"
      provides: "구매 요청 표 — 번호(유니크) · 연결(견적 줄 XOR 팀 비용) · 요청자 · 품목 · 링크 · 예상 금액 Money 묶음 · 메모 · 상태 3종 · 생긴 카드 사용 id · 취소 사유 · version"
      exports: ["purchaseRequests"]
    - path: "domain/purchase-requests/policy.ts"
      provides: "[O-19] 팀 비용 귀속 함수 · [O-20] 요청자=처리자 허용 상수 — 사용자 답이 바뀌면 이 파일만"
      exports: ["purchaseTeamCostAttribution", "PURCHASE_REQUESTER_MAY_COMPLETE"]
    - path: "domain/quotes/line-door.ts"
      provides: "[O-13] 견적 줄의 문(구매 요청 / 지출결의) 판정 한 함수"
      exports: ["resolveLineDoor"]
    - path: "domain/purchase-requests/index.ts"
      provides: "구매 요청 신청 · 목록 · 취소 · 구매 완료"
      exports: ["createPurchaseRequest", "listPurchaseRequests", "cancelPurchaseRequest", "completePurchaseRequest"]
  key_links:
    - from: "domain/purchase-requests/index.ts"
      to: "domain/document-numbering/index.ts"
      via: "번호 부여를 요청 INSERT와 같은 트랜잭션에서 부른다"
      pattern: "purchase_request"
    - from: "domain/purchase-requests/index.ts"
      to: "domain/corp-card-usages/index.ts"
      via: "구매 완료가 카드 사용 건 생성을 같은 트랜잭션에서 부른다"
      pattern: "completePurchaseRequest"
    - from: "domain/purchase-requests/index.ts"
      to: "domain/purchase-requests/policy.ts"
      via: "팀 비용 귀속·요청자=처리자 판정을 정책 파일에서만 읽는다"
      pattern: "purchaseTeamCostAttribution"
    - from: "domain/rules/register.ts"
      to: "domain/quotes/line-door.ts"
      via: "purchase.line-door 게이트가 문 판정 함수를 부른다"
      pattern: "resolveLineDoor"
---

<objective>
온라인 구매 요청(EXP-10)을 신청 → 구매 완료(카드 사용 건 생성) → 취소까지 세운다. 문 가르기(온라인구매 협력사 줄은 구매 요청만 — 사람이 고르지 않음), 번호(A-612 — `document_number.purchase_request.*` 최초 등록), 구매 완료 모드(S13 — 06-03·06-04 카드 폼 재사용).

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 온라인 구매는 결재 없이 경영관리의 카드로 나가는 돈이다. 요청 → 카드 사용이 한 트랜잭션으로 이어져야 「견적 줄에 한 번만」이 구매 경로에서도 성립한다.

Output: `purchase_requests` 표 + 카드 사용 표의 요청 id 칸, `domain/purchase-requests`(정책 파일 분리), 문 판정 · 게이트 `purchase.line-door`, 번호 서식, `/cards/purchases` 목록·폼, 카드 폼 구매 완료 모드, 단위 3 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-04-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-06 착수하지 않음`을 출력하고 멈춘다.**

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-612 | 선행 없음 — 이 플랜이 `document_number.purchase_request.*` 키와 서식을 `domain/document-numbering/`에 처음 등록한다 | 확인만: `grep -rn "purchase_request" domain/ repositories/` 0건이어야 정상 |
| A-607 | Phase 5 지출결의 번호(`{프로젝트 번호}-{순번}`)의 **프로젝트 단위 카운터 경로**와 **팀 비용 지출결의의 번호 규칙**. 프로젝트 단위 경로가 있으면 그것을 재사용하고, 없으면 이 플랜이 `allocateDocumentNumber` 옆에 기간 = 프로젝트 번호인 부여 함수를 더한다(`repositories/document-counters.ts`의 `allocateNumber`가 이미 기간 문자열을 받는다) | `grep -n "period\|scope" domain/document-numbering/index.ts` 로 Phase 5 경로 유무를 SUMMARY에 적는다. **팀 비용 지출결의 번호 규칙을 Phase 5 코드·05-CONTEXT에서 찾지 못하면 멈추고 아래 「번호 공백」 선택지를 사용자에게 묻는다** |
| 06-03 · 06-04 | 카드 사용 폼(`mode`) · 연결 고르기(`mode`) · `cardUsageRights` | `git ls-files domain/corp-card-usages/rights.ts 'app/(app)/cards/link-picker.tsx'` 비면 멈춤 |
| UA-602 · UA-603 | `ConfirmDialog` · `Pagination` | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx ui/pagination/Pagination.tsx` 둘 중 하나라도 비면 멈춤 |

**번호 공백(계획이 스스로 정하지 않는다):** UI-SPEC은 프로젝트 연결 요청의 번호(`26001-C0001` — 기간 = 프로젝트 번호)만 정했다. **팀 비용 요청(프로젝트 없음)의 번호**는 정본이 없다. 기본 동작: Phase 5의 팀 비용 지출결의 번호 규칙을 그대로 따른다(A-607). 그 규칙이 없으면 Task 1 ⓪에서 멈추고 다음 선택지로 사용자 답을 받는다 — ⑴ `{팀 코드}-C{순번}`(기간 = 팀) ⑵ `{연도 2자리}T-C{순번}`(기간 = 연도, 팀 비용 공용) ⑶ 팀 비용 요청은 번호 없이 id만.

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-19** (지금 사용자에게 묻는 중) | 사용한 사람 = 요청자, 팀 = 구매 완료 사용일의 요청자 소속, S13 팀은 읽기 | **Task 3 하나**: `domain/purchase-requests/policy.ts`의 `purchaseTeamCostAttribution` + 그 단위 테스트 + S12·S13의 힌트 한 줄씩 |
| **O-20** (지금 사용자에게 묻는 중) | 요청자 = 구매 완료 처리자 허용 | **Task 3 하나**: `domain/purchase-requests/policy.ts`의 `PURCHASE_REQUESTER_MAY_COMPLETE` + 그 단위 테스트 |
| O-9 | 취소는 `신청` 상태에서만 | `cancelPurchaseRequest`의 상태 검사 |
| O-10 | 예상 금액 = 결제 합계 기준, 차이는 표시만 | S13 차이 줄 · `completePurchaseRequest`(막지 않음) |
| O-13 | 설정은 문자열 한 칸, 서버가 거래처 이름과 맞춘다 | `domain/quotes/line-door.ts`의 `resolveLineDoor` |
| O-11 | 구매 완료 건은 연결 변경·삭제 없음(06-04 `cardUsageRights`) | 해당 없음 |
| O-1 | 구매 요청 목록 = 법인카드 메뉴의 하위 목록(SP-4) | 라우트 `app/(app)/cards/purchases/` |

<probe_fallback>
coverage.json의 이 플랜 몫: **EXP-10 — unclassified → unresolved (flagged, 자동 해소·기각 없음)**. 대신 쓰는 근거: 기준 3 · A-612 · O-9 · O-10 · O-13 · O-19 · O-20. 전체 10건 표는 06-01 `<probe_fallback>`.

**표면화하는 가정:** 팀 비용 구매 요청의 번호 규칙이 정본에 없다(위 「번호 공백」). 조용히 하나를 고르지 않고 Phase 5 규칙 재사용 → 없으면 멈춤으로 둔다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 온라인구매 줄에 이은 구매 요청 한 건이 번호를 받고, 구매 권한자가 구매 완료해 카드 사용 건이 된다</name>
  <files>db/schema/purchase-requests.ts, db/schema/corp-card-usages.ts, db/schema/index.ts, db/migrations/meta/_journal.json, domain/purchase-requests/index.ts, domain/quotes/line-door.ts, domain/rules/register.ts, domain/document-numbering/index.ts, domain/settings/keys.ts, domain/corp-card-usages/index.ts, repositories/purchase-requests.ts, app/(app)/cards/purchases/page.tsx, app/(app)/cards/purchases/purchase-request-form.tsx, app/(app)/cards/purchases/actions.ts, app/(app)/cards/purchases/actions.registry.ts, app/(app)/cards/purchases/purchases.module.css, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-form.tsx, test/unit/domain/line-door.test.ts, test/unit/domain/document-number-format.test.ts, test/integration/purchase-requests.test.ts, test/integration/leak-scan.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 06-04 · 06-05가 끝났다 — ⓪이 확인하고 없거나 번호 공백이 풀리지 않으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 655-691행(S11 · S12 · S13 정본) · 835행(예상 금액 금액 모델) · Copywriting 「막힘 — 구매 요청 폼」 · 「Error — 구매 요청 폼 칸」 · 「3차 버튼」 행(접근 이름)
    - domain/document-numbering/index.ts 전체(118줄 — `DOCUMENT_NUMBER_FORMAT_DEFS` · `documentNumberFormat` · `allocateDocumentNumber`) · repositories/document-counters.ts · test/unit/domain/document-number-format.test.ts · test/integration/document-numbering.test.ts
    - domain/settings/keys.ts 234-290행(`document_number.project.*` 키 모양)
    - domain/corp-card-usages/index.ts · rights.ts · app/(app)/cards/card-usage-form.tsx · link-picker.tsx(06-03·06-04)
    - db/schema/corp-card-usages.ts
  </read_first>
  <behavior>
    - `resolveLineDoor({ vendorName }, setting)`: 설정 빈 값 → expense / `" 쿠팡 "` vs `"쿠팡"` → purchase / 다른 이름 → expense / 거래처 없음 → expense
    - `purchase.line-door`: 구매 요청 쪽 · 문이 expense인 줄 → 막힘 `거래처 {이름} · 지출결의로` / 지출결의 쪽 · 문이 purchase인 줄 → 막힘(06-07이 부른다)
    - 번호 서식: 프로젝트 `26001` · 순번 1 · 접두어 `C` · 4자리 · 구분자 `-` → `26001-C0001`, 자릿수 넘침은 자르지 않음
    - `createPurchaseRequest`: 온라인구매 줄 → 저장 + 번호 + `document_create` / 그 밖 줄 → 게이트 거부 / 링크 `javascript:` → 거부
    - `completePurchaseRequest`: 구매 권한 없음 → 거부 / `신청` 아님 → 거부 / 정상 → 카드 사용 1 + 요청 `구매 완료` + `purchase_process`, 카드 사용 INSERT 실패 시 요청 상태 불변
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인과 번호 공백 판정. 없거나 번호 규칙이 비면 멈춘다.

① **스키마.** `purchase_requests`(id · number text UNIQUE · link_kind CHECK in (quote_line, team_cost) · project_id FK null · quote_line_id FK null · XOR CHECK · requested_by FK · item_name text · link_url text null + CHECK(스킴 http/https) · `moneyColumns("estimate")` · memo null · status CHECK in (requested, purchased, cancelled) · card_usage_id FK null UNIQUE · completed_by/at null · cancelled_by/at/reason null · version · source 'demo' · 생성 시각). `corp_card_usages`에 `purchase_request_id` FK null UNIQUE를 더하고, `registered_via = purchase`이면 값이 있다는 CHECK. `pnpm db:generate --name purchase_requests` → `pnpm lint:sql` → `pnpm db:migrate` → 두 번째 generate `No schema changes`.

② **번호(A-612).** `domain/settings/keys.ts`에 `document_number.purchase_request.prefix`(기본 `C`) · `.seq_digits`(4) · `.separator`(`-`) · `.seq_start`(1)를 `document_number.project.*`과 같은 모양·namespace `문서 번호`로 더하고 `SETTING_DEFS`에 넣는다. `domain/document-numbering/index.ts`에 프로젝트 번호를 기간으로 쓰는 서식(`{프로젝트 번호}{구분자}{접두어}{순번}`)과 부여 함수를 더한다 — Phase 5가 같은 경로를 이미 만들었으면 그것에 `purchase_request` 줄만 더한다. 부여는 반드시 요청 INSERT와 같은 트랜잭션. 기존 `project` 서식 테스트는 그대로 녹색이어야 한다.

③ **문 판정.** `domain/quotes/line-door.ts`의 `resolveLineDoor`(계획 판단: 앞뒤 공백 제거 · NFC 정규화 뒤 정확 비교, 빈 설정 → 모두 지출결의). `domain/rules/register.ts`에 `purchase.line-door`(두 방향 — 구매 요청 쪽과 지출결의 쪽). 이 플랜이 `purchase.online_vendor_name`을 읽기 시작하므로 그 키의 `readBy` 표시를 지운다.

④ **domain · 액션 · 화면(한 경로).** `createPurchaseRequest`(연결 = 견적 줄 갈래 · 게이트 · 번호 · 외화면 `toKrw` · 링크 스킴 검사 · `document_create`), `completePurchaseRequest`(`can(viewer, "cards.purchases", "write")` · `신청` 상태 · 한 트랜잭션에서 `domain/corp-card-usages`의 생성 함수를 `registered_via = purchase`로 부르고 요청 `구매 완료` + card_usage_id · `purchase_process`). `app/(app)/cards/purchases/actions.ts` + 새 `actions.registry.ts` → `test/integration/leak-scan.test.ts`에 이 registry와 `@/domain/purchase-requests` import. 화면: `/cards/purchases?new=1` 폼(이 태스크는 `line` 쿼리로 들어온 견적 줄 연결 텍스트 갈래) · `/cards/purchases` 목록 기본 보기 `신청`(번호 · 요청일 · 품목 + 링크 아이콘 · 연결 · 요청자 · 예상 금액 · 상태 + 구매 권한자 3차 `구매 완료`) · `/cards?new=1&purchase={id}` = 카드 폼 `mode = purchase`(제목 `구매 완료 — {번호} · {품목}`, 가맹점 기본 = 온라인구매 협력사, 결제 합계 기본 = 예상 금액, 연결 텍스트, 카드 옵션 = 활성 카드 전부, 등록 칸 `구매 요청 {번호}`).

⑤ **E2E.** `test/e2e/purchase-requests.spec.ts`: 설정에 온라인구매 협력사 → 그 거래처 견적 줄 → 직원이 구매 요청(`line` 쿼리) → 토스트 번호 → 구매 권한자 → 목록 `구매 완료` → 카드 폼 구매 완료 → 목록 행 `구매 완료` + 2행 `카드 사용 …` → `/cards` 목록 등록 칸 `구매 요청 {번호}`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/line-door.test.ts test/unit/domain/document-number-format.test.ts test/unit/settings/registry-coverage.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 개수를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/document-numbering.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. document-numbering·concurrency가 빨가면 기존 프로젝트 번호 경로를 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(Phase 5 번호 경로 유무 · 팀 비용 번호 규칙 출처)가 SUMMARY에 있다
    - 통합 테스트가 behavior의 모든 갈래를 개별 `it`으로 단언하고, 원자성 케이스는 카드 사용 INSERT를 실패시켜 요청이 `신청`으로 남는지 확인한다
    - 두 트랜잭션이 같은 프로젝트에 동시에 요청하면 서로 다른 번호를 받는 케이스가 있다
    - `grep -n "purchase.online_vendor_name" -A 12 domain/settings/keys.ts | grep -c readBy`가 0
  </acceptance_criteria>
  <done>구매 요청 한 건이 문 판정 → 번호 → 신청 → 구매 완료 → 카드 사용 건까지 한 트랜잭션 규칙으로 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 목록·폼 완성 — 팀 비용 연결 · 연결 고르기 purchase 모드 · 취소(O-9) · 외화 · 링크 · 카드 목록 하위 링크</name>
  <files>domain/purchase-requests/index.ts, repositories/purchase-requests.ts, repositories/quote-line-links.ts, app/(app)/cards/purchases/page.tsx, app/(app)/cards/purchases/purchase-request-form.tsx, app/(app)/cards/purchases/actions.ts, app/(app)/cards/purchases/actions.registry.ts, app/(app)/cards/link-picker.tsx, app/(app)/cards/card-usage-list.tsx, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 995-1010행(S11 · S12 considerations) · 652-653행(S10 구매 요청 폼 갈래) · Copywriting 「Empty — 구매 요청 목록」 · 「Destructive — 요청 취소」
    - /mnt/project-files/06-prep/open-choices.md 18-19행(O-9 · O-10)
  </read_first>
  <behavior>
    - 목록 범위: 구매 권한자·전사 범위(대표) → 전부 / 그 밖 → 자기 요청 + 자기가 담당 PM인 프로젝트 줄의 요청
    - `cancelPurchaseRequest`: 요청자 · 자기 `신청` → 사유 없이 취소 / 구매 권한자 · 남의 `신청` → 사유 필수 / 그 밖 → 거부 / `구매 완료`·`취소` → 거부 / `status_change` 로그
    - 합계 줄: 원화 환산액만 서버가 더함 · `전체` 보기 그룹 순서 신청 → 구매 완료 → 취소
    - `findLineLinks`가 구매 요청(`신청`·`구매 완료`)도 돌려준다 — 줄 상태 파생(06-07)의 입력
  </behavior>
  <action>
폼: 목록 1차로 들어오면(`?new=1`만) 연결 라디오 `팀 비용`이 골라져 있고 `견적 줄`로 바꿀 수 있다. `견적 줄` 갈래는 프로젝트 「바꾸기」 → 견적 줄 「바꾸기」(06-03 `link-picker`에 `mode="purchase"`를 구현 — 온라인구매 줄만 `selectable`, 나머지 2행 `거래처 {이름} · 지출결의로`, 고를 수 있는 줄 0이면 `온라인구매 줄 없음 · 팀 비용으로`, 견적 줄 0이면 `이 프로젝트에 견적 줄이 없습니다 · 팀 비용으로`). `팀 비용` 갈래의 팀 텍스트·힌트는 Task 3의 정책 함수로 채운다(이 태스크에서는 자리만 두고 정책 호출을 Task 3이 붙인다 — 정책 판정 코드를 여기 쓰지 않는다). 예상 금액: 통화 `Select` + 금액, 외화면 환율 칸(기본 최근 환율, 없으면 빈 칸 + 1차 막힘) + 서버 원화 `Form.Hint`. 링크 칸 placeholder `https://`.

목록: 범위 판정은 리포지토리 쿼리에서. 필터 `[신청 | 구매 완료 | 취소 | 전체] [월]`, 그룹(`전체`면 상태, 아니면 요청일 주), 50건 페이지, 합계 줄(원화만, 서버 `sumKrw`), 외화 2행 `USD 1,000.00 @1,350`, 구매 완료 행 2행 `카드 사용 {MM-DD} · {결제 합계}`, 취소 행 2행 `취소 {MM-DD} · {사람} · {사유}`. 행 3차: 요청자에게 자기 `신청` 행 `요청 취소`(`ConfirmDialog`, 사유 칸 없음 — 팀 비용 요청은 결과 줄 없음), 구매 권한자에게 남의 `신청` 행 `요청 취소`(사유 근거 칸 필수). 접근 이름은 `{번호} 구매 완료` · `{번호} 요청 취소` · `{품목} 링크 열기`. 링크 아이콘은 `http`·`https`일 때만 렌더, 새 탭 `rel="noopener noreferrer"`. 폰에서도 행 안 3차로 폼·모달이 선다. `/cards` 목록 필터 줄 오른쪽 3차 `구매 요청 {열린 건수}`(0이면 `구매 요청`)를 채운다(SP-4). `repositories/quote-line-links.ts`에 구매 요청 연결을 더한다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/corp-card-usages.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 네 줄의 갈래를 개별 `it`으로 단언한다(범위 밖 계정의 목록에 남의 요청이 없음 · 구매 완료 요청 취소 거부 포함)
    - E2E에 「팀 비용 요청 신청」 · 「요청자 자기 취소」 · 「연결 고르기에서 비온라인 줄 `aria-disabled`」 케이스가 녹색이다
  </acceptance_criteria>
  <done>구매 요청 목록·폼이 계약대로 서고, 취소가 `신청` 상태에서만 권한별로 되며, 줄 연결 조회가 구매 요청까지 본다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [O-19·O-20 기본값] 팀 비용 귀속 · 요청자=처리자 허용 · 구매 완료 건의 카드 고치기</name>
  <files>domain/purchase-requests/policy.ts, domain/purchase-requests/index.ts, domain/corp-card-usages/index.ts, domain/corp-card-usages/rights.ts, app/(app)/cards/purchases/purchase-request-form.tsx, app/(app)/cards/card-usage-form.tsx, test/unit/domain/purchase-policy.test.ts, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 672-675행(팀 비용의 팀 — O-19) · 686-689행(S13 팀 비용) · 606-611행(구매 완료 건의 카드 고치기) · 1011-1017행(S13 considerations) · Copywriting 「표시 — 카드 고침(구매 완료 건)」 · 「막힘 — 카드 사용 폼」 구매 완료 모드 소속 없음 갈래
    - /mnt/project-files/06-prep/open-choices.md 28-29행(O-19 · O-20 기본값과 대안)
    - domain/org/index.ts의 `teamAtDate`
  </read_first>
  <behavior>
    - `purchaseTeamCostAttribution(requesterId, usedOn)`: 사용한 사람 = 요청자 · 팀 = `teamAtDate(요청자, 사용일)` / 소속 없음 → null(호출자가 거부) / 처리자·카드의 팀은 입력에 없다
    - `PURCHASE_REQUESTER_MAY_COMPLETE = true` → 요청자가 자기 요청을 구매 완료 가능 / 상수를 false로 둔 테스트 케이스 → 거부(판정이 이 상수 한 곳에서만 나오는지 고정)
    - 신청 폼 미리보기 팀 = 요청자의 오늘 소속, 오늘 소속 없음 → 1차 막힘
    - 구매 완료 건 카드 변경: 그 건 권리 + 지금 `cards.purchases` write + 새 카드 활성 → 저장 + `document_update` `카드 고침 · {이전} → {새}` / 하나라도 아니면 거부
  </behavior>
  <action>
**이 태스크가 O-19 · O-20의 유일한 자리다.** `domain/purchase-requests/policy.ts`에 `purchaseTeamCostAttribution`과 상수 `PURCHASE_REQUESTER_MAY_COMPLETE = true`를 두고, 파일 머리 한 줄 주석에 "O-19 · O-20 답이 바뀌면 이 파일과 그 단위 테스트만 고친다"를 적는다. `completePurchaseRequest`는 팀 비용 요청이면 이 함수로 사용한 사람·팀을 정하고(null이면 거부 — 임의 팀 없음), 요청자 = 처리자 판정은 이 상수만 읽는다. `createPurchaseRequest`의 팀 비용 갈래는 오늘 소속을 같은 함수(사용일 = 오늘)로 미리 보고 없으면 거부한다. 폼: S12 팀 비용 텍스트 + `Form.Hint` `구매 완료 날 요청자 소속으로 정함`, S13 팀 읽기 텍스트 + `Form.Hint` `요청자 {이름} 사용일 소속`, 사용일을 바꾸면 서버가 팀을 다시 보냄(이전 값 `--faint`), `사용한 사람` 칸은 이 모드에 없다.

구매 완료 건의 카드 고치기(S9 rev 7): `domain/corp-card-usages/rights.ts`의 `cardUsageRights`에 「구매 완료 건 + 지금 `cards.purchases` write → 카드 변경 가능」 갈래를 더하고, `updateCardUsage`가 그때만 카드 변경을 받는다(새 카드 활성 필수). 폼은 그 경우 카드 `Select`(활성 카드 전부 + 저장된 카드가 비활성이어도 현재 값으로 맨 앞), 다른 카드를 고르면 `Form.Hint`(Copywriting 「표시 — 카드 고침(구매 완료 건)」). 등록 칸·연결·팀은 바뀌지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/purchase-policy.test.ts test/unit/domain/card-usage-rights.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/corp-card-usages-proxy.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "PURCHASE_REQUESTER_MAY_COMPLETE\|purchaseTeamCostAttribution" domain/ app/ | grep -v "^domain/purchase-requests/"` 출력이 비어 있다(O-19·O-20 판정이 정책 파일과 그 호출 모듈 밖에 없다)
    - 통합 테스트가 「구매 완료자의 소속이 요청자와 달라도 팀 = 요청자 사용일 소속」 · 「요청자 사용일 소속 없음 → 구매 완료 거부」 · 「카드 고침 로그 한 줄」을 단언한다
  </acceptance_criteria>
  <done>O-19 · O-20의 답이 바뀌어도 정책 파일 하나만 고치면 되고, 팀 비용 귀속이 요청자의 사용일 소속으로만 정해진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 구매 요청 액션 | 품목 · 링크 URL · 예상 금액 · 연결이 넘어온다. 링크는 외부 URL이고 목록에서 새 탭으로 열린다 |
| 브라우저 → 구매 완료 액션 | 카드 · 결제 합계 · 사용일이 넘어온다. 팀·사용한 사람은 넘어오지 않는다 |
| domain/purchase-requests → domain/corp-card-usages | 요청 상태 전환과 카드 사용 생성이 한 트랜잭션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-30 | Elevation of Privilege | `completePurchaseRequest` | high | mitigate | `can(viewer, "cards.purchases", "write")`를 domain 첫 줄에서 확인. 권한 없는 계정 거부 통합 테스트 |
| T-06-31 | Tampering | 문 가르기 우회 | high | mitigate | `purchase.line-door` 게이트를 요청 생성 domain에서 부른다(지출결의 쪽은 06-07). 비온라인 줄 요청 거부 통합 테스트 |
| T-06-32 | Tampering | 링크 URL(`javascript:` 등) | high | mitigate | zod + DB CHECK로 http/https만 저장, 렌더는 스킴 재확인 뒤 `rel="noopener noreferrer"` 새 탭. `javascript:` 거부 통합 테스트 |
| T-06-33 | Tampering | 번호 경합·중복 | medium | mitigate | 카운터 증가를 요청 INSERT와 같은 트랜잭션에서, `number` UNIQUE가 최후 방어선. 동시 두 요청 통합 테스트 |
| T-06-34 | Spoofing | 팀 비용 귀속 | medium | mitigate | 팀·사용한 사람을 입력으로 받지 않고 정책 함수가 요청자·사용일로 정한다 |
| T-06-35 | Information Disclosure | 구매 요청 목록 | medium | mitigate | 범위(자기 요청 · 담당 프로젝트 줄 · 권한자 전부)를 리포지토리 쿼리에서 거르고 DTO 등록 → 누수 스캔 |
| T-06-36 | Repudiation | 취소 · 카드 고침 | low | mitigate | `status_change`/`document_update` 로그에 사람·시각·사유·이전/새 카드 |
</threat_model>

<verification>
- 단위 3(+ card-usage-rights) · 통합 1(+ document-numbering · concurrency · corp-card-usages · proxy · leak-scan) · E2E 1(+ card-usage · card-proxy) 녹색
- `test/unit/settings/registry-coverage.test.ts` · `test/unit/leak-scan-coverage.test.ts` 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml·tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S10(purchase 모드)·S11·S12·S13·S20 DOM 실측 감사(backstop 6건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 온라인구매 줄은 구매 요청으로만, 그 밖은 지출결의로만 열리고 사람이 문을 고르지 않는다(구매 요청 쪽 입구)
- 구매 완료가 카드 사용 건을 한 트랜잭션으로 만들고, 번호가 저장 순간 매겨진다
- O-19 · O-20 답이 바뀌어도 정책 파일 하나만 고친다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `purchase_requests` · 컬럼 `corp_card_usages.purchase_request_id` |
| 마이그레이션 | `db/migrations/NNNN_purchase_requests.sql`(번호는 `pnpm db:generate`) |
| 설정 키 | `document_number.purchase_request.prefix` · `.seq_digits` · `.separator` · `.seq_start` |
| 게이트 규칙 | `purchase.line-door` |
| 함수(domain) | `createPurchaseRequest` · `listPurchaseRequests` · `cancelPurchaseRequest` · `completePurchaseRequest` · `resolveLineDoor` · `purchaseTeamCostAttribution` · 프로젝트 단위 번호 서식·부여 |
| 상수 | `PURCHASE_REQUESTER_MAY_COMPLETE`(O-20) |
| Server Action | `createPurchaseRequestAction` · `cancelPurchaseRequestAction` · `completePurchaseRequestAction` |
| 라우트 | `/cards/purchases`(S11 · S12) · `/cards?new=1&purchase={id}`(S13) |
| 변경된 기존 심볼 | `cardUsageRights`(구매 완료 건 카드 고치기) · `updateCardUsage` · `findLineLinks`(구매 요청 포함) · `link-picker` `purchase` 모드 · `purchase.online_vendor_name` readBy 제거 |
| 테스트 | `test/unit/domain/{line-door,purchase-policy}.test.ts` · `test/unit/domain/document-number-format.test.ts`(확장) · `test/integration/purchase-requests.test.ts` · `test/e2e/purchase-requests.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-06-SUMMARY.md` when done
</output>
