---
phase: 06-payment-evidence-cards
plan: 06
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - db/schema/expense-payments.ts
  - db/migrations/meta/_journal.json
  - domain/payments/action-row.ts
  - domain/payments/index.ts
  - domain/evidence-reviews/index.ts
  - repositories/expense-evidence-reviews.ts
  - app/(app)/expenses/[id]/evidence-review-section.tsx
  - app/(app)/expenses/[id]/page.tsx
  - app/(app)/expenses/[id]/actions.ts
  - app/(app)/expenses/[id]/actions.registry.ts
  - test/unit/domain/evidence-reviews.test.ts
  - test/unit/domain/payments-action-row.test.ts
  - test/integration/evidence-reviews.test.ts
  - test/e2e/payment-single.spec.ts
autonomous: true
requirements: [EVID-02, EVID-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[O-2 기본값] 증빙이 있고 아직 `확인 전`이면 지급 완료가 `증빙 확인 전 · 증빙 확인`으로 막힌다(증빙 필수 설정이 꺼져 있어도 — P2) — 이 판정은 `domain/payments/action-row.ts`의 상수 `EVIDENCE_CONFIRMATION_GATES_PAYMENT` 한 곳에서만 나온다(O-2 답이 바뀌면 이 상수와 P2 갈래만 고친다)"
    - "`resolveEvidenceStatus`는 다섯 값을 낸다 — 면제 기록 있음 → `면제`(선결제를 이긴다) / 증빙 있음 · 확인 기록 없음 → `확인 전` / 증빙 있음 · confirmed → `확인됨` / 증빙 0 · 선결제 → `선결제` / 증빙 0 → `없음`"
    - "지급 게이트 ctx는 `domain/evidence-reviews`의 `evidenceGateInputs(viewer, lockedDoc, pre, tx?)` 한 함수가 만든다 — `tx`는 선택 인자다(r4 F-F1): 지급 완료(`completeExpensePayment`)는 잠근 문서의 `tx`를 늘 넘기고, 트랜잭션 없는 읽기(06-15 `listPaymentTargets`)는 생략하며 그때는 안에서 부르는 06-06 리포지토리가 `tx: DbOrTx = db` 기본값(기존 `repositories/quote-lines.ts` 꼴)으로 전역 `db`를 쓴다 — domain은 `db` 값을 import하지 않는다(eslint.config.mjs domain allow).  `evidenceRequired` · `pmName`은 트랜잭션 전 사전 조회 `pre`(06-04 `loadPaymentInputs`)에서, `evidenceCount` · `waived`(확인 표의 면제 상태) · `confirmation`(`resolveEvidenceStatus`)은 잠근 문서와 같은 `tx` 조회에서 온다(CROSS R-3), `prepaid`는 06-10이 선결제 칸을 만들 때 같은 함수 안에서 실제 값으로 바뀐다(06-04의 ctx 채움을 이 함수로 옮긴다)"
    - "「06-03 tx 규약」을 따른다 — `confirmEvidence`의 권한 판정은 트랜잭션 전, 행동 로그는 커밋 뒤, 금액 고침이 따라 하는 Phase 5 부수 작업 중 설정 · 권한 · `tx` 없는 리포지토리를 읽는 것은 트랜잭션 전에 읽어 넘기고, `evidenceGateInputs`는 트랜잭션 안에서 불릴 때 받은 `tx`로만 읽는다. 06-03의 「지급 완료 동시 6건」이 이 플랜 뒤에도 녹색이다"
    - "지급 권한자가 증빙 금액을 고쳐 「증빙 확인」하면 확인 줄 2행이 `확인됨 · {사람} {MM-DD HH:mm} · {전} → {새}`로 서고 행동 로그 `document_update`에 전·후 금액이 남는다(D-602, AS2)"
    - "**B-3:** `confirmEvidence`의 금액 고침은 Phase 5 PM 증빙 편집 함수를 거치지 않고 `repositories/expense-evidence-reviews.ts`의 갱신 한 번 뒤 확인 기록 upsert(`ON CONFLICT (expense_id)`)로 끝난다 — 경영관리가 금액을 고쳐 확인한 문서는 `확인됨`을 유지한다(06-11의 증빙 변경 무효화 훅이 이 경로에 걸리지 않는다)"
    - "증빙 확인은 새 비용 단계를 만들지 않는다 — 확인 전후로 비용 기준 값은 PM이 적은(또는 경영관리가 고친) 증빙 금액 하나다(D-602)"
    - "`resolveExpenseActionRow`: 결재 통과 · 지급 전 · `확인 전` → P2(1차 `증빙 확인`, 증빙 섹션 3차 `바꾸기`) / 지급 뒤 · `확인 전` → P5(1차 `증빙 확인`, 2차 `지급 취소`, 지급 칸은 다시 서지 않음) — P0~P6 일곱 행이 한 함수에서 나온다"
    - "증빙 확인은 지급 권한(`expenses.payments` write)이 있어야 하고, 지출결의 행 잠금(R-1) 안에서 문서 version을 비교·+1 한다 — 권한 없는 사람에게는 읽기 + 2행 `확인은 경영관리`만 선다(D-601)"
    - "**B-1(디자인 반영 r2) — 보지 않은 증빙은 확인되지 않는다:** `confirmEvidence`의 판정은 이 플랜 그대로(잠금 뒤 version 비교)이고 증빙 묶음 지문을 따로 비교하지 않는다. PM의 증빙 변경(더하기 · 떼기 · 금액 고침)이 결재 통과 문서에서 확인 기록 유무와 관계없이 문서 version을 올리는 것은 06-11 훅이 맡는다(통과 전 문서는 올리지 않는다 — `증빙 확인`은 이 플랜의 P2 · P5, 곧 결재 통과 문서에서만 열리므로 이 경합이 없다, 06-11 X-1 r3) — 그래서 모달(06-17 · 06-20) · 문서 화면이 받은 version으로 부른 확인은, 그사이 증빙이 바뀌었으면 기존 version 비교에서 「거부 — 문서 화면 동시성」으로 거부된다. 이 플랜의 통합 테스트는 06-11보다 앞 웨이브라 그 경합을 돌리지 않는다 — 06-11 「PM 증빙 변경 뒤 옛 version 확인 거부」가 증명한다"
    - "**DR-4(rev 9) 응답 모양:** `confirmEvidence`와 `confirmEvidenceAction`은 새 문서 `version`(`bumpExpenseVersion`이 돌려준 값) · 증빙 상태(`resolveEvidenceStatus`) · 응답 상태의 행동 줄(`resolveExpenseActionRow`)을 돌려준다 — 판정 · 권한 · 잠금은 그대로이고 돌려주는 값만 더한다. 06-17(S1) · 06-20(S3)의 제자리 증빙 확인이 이 금액 없는 갈래를 그대로 부르고 응답의 새 `version`으로 그 행을 바꾼다(UI-SPEC 「금액 표시」 절 「낙관적 잠금」 · S1 「제자리 증빙 확인」 「서버」 줄)"
    - "S4 loading: 지급 전 문서에서 증빙 금액을 고치면 서버 계산 한 줄이 이전 값 `--faint`로 남았다가 새 값으로 바뀌고(빈칸·뼈대 없음), 1차 처리 중에는 `…`다"
    - "S4 error: 「Error — 증빙 금액 칸」 · 「거부 — 문서 화면 동시성」 · 「거부 — 문서 화면 응답 없음」이 서고, 응답 없음 뒤에도 칸 입력값이 남는다"
    - "S4 populated: 파일 행 · `증빙 금액` 값 + 2행 입력자 · 서버 계산 한 줄(지급 전만, 「표시 — 증빙 금액」) · 확인 줄 값 넷(`확인 전` · `확인됨` · `면제` · `선결제` — `statusDisplay`) · 행동 줄 1차 하나가 와이어프레임대로 선다"
    - statement: "S4 overflow: 확인 줄 2행 `확인됨 · 이과장 09-18 14:02 · 12,400,000 → 12,000,000`이 720 한 열과 폰 폭에서 숫자 중간 줄바꿈 없이(숫자 `nowrap`) 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "경영관리 권한이 없는 사람이 증빙 확인을 대신할 수 있어서는 안 된다(D-601)"
      status: resolved
      verification: test
    - statement: "경영관리의 금액 고침 확인이 증빙 변경 무효화 경로를 타서 `확인 전`으로 되돌아가서는 안 된다(B-3)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/expense-payments.ts"
      provides: "증빙 확인/면제 표 `expense_evidence_reviews` — 문서당 한 줄(`uniqueIndex`), 상태(confirmed·waived)·전후 금액·면제 사유·version"
      exports: ["expenseEvidenceReviews"]
    - path: "domain/evidence-reviews/index.ts"
      provides: "증빙 상태 다섯 값 · 증빙 확인(금액 고침 포함) · 지급 게이트 ctx"
      exports: ["resolveEvidenceStatus", "confirmEvidence", "evidenceGateInputs"]
    - path: "domain/payments/action-row.ts"
      provides: "[O-2] 증빙 확인이 지급 게이트인지 정하는 단일 상수 + P2 · P5"
      exports: ["EVIDENCE_CONFIRMATION_GATES_PAYMENT", "confirmationBlocksPayment"]
    - path: "test/integration/evidence-reviews.test.ts"
      provides: "확인 전 막힘 · 금액 고침 로그 · 권한 거부 · 비용 불변 · 고쳐 확인 → 확인됨 유지(06-11이 다시 돌린다)"
      min_lines: 60
  key_links:
    - from: "domain/payments/index.ts"
      to: "domain/evidence-reviews/index.ts"
      via: "지급 완료가 증빙 게이트 ctx를 `evidenceGateInputs`에서만 받는다"
      pattern: "evidenceGateInputs"
    - from: "domain/payments/action-row.ts"
      to: "domain/rules/register.ts"
      via: "화면 P2와 서버 게이트가 같은 O-2 상수를 읽는다(`evidenceGateDecision` 안)"
      pattern: "EVIDENCE_CONFIRMATION_GATES_PAYMENT"
    - from: "domain/evidence-reviews/index.ts"
      to: "repositories/expense-evidence-reviews.ts"
      via: "금액 고침 갱신과 확인 기록 upsert가 한 트랜잭션 — Phase 5 PM 편집 함수를 부르지 않는다(B-3)"
      pattern: "ON CONFLICT"
    - from: "app/(app)/expenses/[id]/actions.ts"
      to: "domain/evidence-reviews/index.ts"
      via: "`confirmEvidenceAction`이 `confirmEvidence`의 `{ version, evidenceStatus, actionRow }`를 그대로 돌려준다 — 06-17 · 06-20 제자리 확인이 새 `version`으로 행을 바꾼다(DR-4 rev 9)"
      pattern: "confirmEvidence"
---

<objective>
경영관리의 **증빙 확인(S4)** 을 세운다 — 확인/면제 표, 증빙 상태 다섯 값, [O-2 기본값] 「확인 전이면 지급 막힘」, 금액을 고쳐 확인하기(D-602), 지급 게이트 ctx 한 함수(`evidenceGateInputs`).

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: Goal의 첫 동사가 「증빙을 확인해」다. O-2의 답이 바뀌어도 한 상수만 고치게 판정을 한 곳에 둔다. 교차 검토 B-3이 짚은 대로, 경영관리의 금액 고침은 06-11이 붙일 「PM 증빙 변경 → 확인 무효화」 훅과 다른 길로 가야 확인이 풀리지 않는다 — 그 경로 분리를 이 플랜이 만든다.

Output: `expense_evidence_reviews` 표(신규 마이그레이션 1), `domain/evidence-reviews`, O-2 상수 · P2 · P5, S4 섹션, 단위 2 · 통합 1 · E2E 케이스.

**rev 9(`/plan-design-review` 반영):** 판정 · 권한 · 잠금은 그대로다. `confirmEvidence` · `confirmEvidenceAction`이 새 문서 `version` · 증빙 상태 · 행동 줄을 돌려주는 것만 더한다(DR-4 — 06-17 S1 · 06-20 S3 제자리 증빙 확인이 이 금액 없는 갈래를 그대로 부르고 응답 `version`으로 행을 바꾼다). Task 1 behavior · ② · ③, Task 3 「확인 응답에 새 version」.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 90000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다. 확인 표는 파일 수를 줄이려 `db/schema/expense-payments.ts`에 함께 둔다(`db/schema/index.ts`는 `export *`라 고치지 않는다). DTO는 지급 섹션 DTO에 실려 새 `registerDto`가 없지만, verify는 누수 스캔을 돌린다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-04-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-06 착수하지 않음`을 출력하고 멈춘다.** 가칭은 06-03 SUMMARY 대응표를 따른다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-608 | Phase 5 증빙 금액(공급가)·증빙일 칸과 **PM 증빙 편집 domain 함수**(가칭 `updateEvidence`) · **증빙 행을 만드는 domain 함수**(E2E 준비용, B-4 — 업로드가 아니라 함수로 만든다) | `grep -rn "evidence" domain/expenses/` 0건이면 멈춤 · 증빙 행 생성 함수 이름을 SUMMARY에 적는다(없으면 멈춤) |
| 06-04 | `evidenceGateDecision` · `payment.evidence-required` | `grep -c "evidenceGateDecision" domain/payments/action-row.ts` 0이면 멈춤 |
| 06-05 | 마이그레이션 저널에 06-05의 `corp_card_usages`가 먼저 있다 | `grep -c "corp_card_usages" db/migrations/meta/_journal.json` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-2** (지금 사용자에게 묻는 중) | 증빙 확인이 지급 게이트다 — `확인 전`이면 지급 완료 막힘 | **Task 2 하나**: `domain/payments/action-row.ts`의 `EVIDENCE_CONFIRMATION_GATES_PAYMENT` · P2 갈래 · 그 단위 테스트. must_haves의 `[O-2 기본값]` 줄 하나 |
| AS2 | 증빙 확인 = 기존 `document_update`(새 행동 종류 없음) | `domain/evidence-reviews/index.ts`의 `recordAction` 인자 |

<assumption_delta_decision>
`detected: false`. 확인 기록 표를 **더한다** — 증빙(Phase 5 명사)을 옮기지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EVID-02 · EVID-03 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 1 · 기준 2 · D-602 · O-2. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 증빙이 있는 문서를 경영관리가 「증빙 확인」하고 이어서 지급 완료한다</name>
  <files>db/schema/expense-payments.ts, db/migrations/meta/_journal.json, domain/payments/action-row.ts, domain/evidence-reviews/index.ts, repositories/expense-evidence-reviews.ts, app/(app)/expenses/[id]/evidence-review-section.tsx, app/(app)/expenses/[id]/page.tsx, app/(app)/expenses/[id]/actions.ts, app/(app)/expenses/[id]/actions.registry.ts, test/e2e/payment-single.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「지출결의 상태 → 1차 (정본 …)」 절(P2 · P5 행 · 「행동 뒤에는 늘 응답 상태로 이 표를 다시 읽는다」) · UI Considerations의 S4 행 · 「금액 표시」 절 「낙관적 잠금」 · S1 「제자리 증빙 확인 — 확인 모달 + 첨부 보기 칸(SP-7) (rev 9, DR-4 · O-21)」의 「서버」 줄 · Copywriting 「표시 — 증빙 금액」 · 「Error — 증빙 금액 칸」 · 확인 줄 2행 문구
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-602
    - 06-03 · 06-04가 만든 domain/payments/index.ts · action-row.ts · repositories/expense-payments.ts(`lockExpenseRow`)
    - A-608 재확인에서 읽은 Phase 5 증빙 금액 칸 · 증빙 행 생성 함수
    - db/migrations/0010_revenue_entries.sql 1-5행
  </read_first>
  <behavior>
    - `confirmEvidence`(금액 고침 없음) → 확인 기록 confirmed · `document_update` · 문서 version +1
    - `confirmEvidence`(금액 고침 없음)의 돌려준 값 = `{ version, evidenceStatus, actionRow }` — `version`은 +1 된 새 문서 version, `evidenceStatus`는 `확인됨`, `actionRow`는 그 상태의 `resolveExpenseActionRow` 결과(지급 전이면 P4). `confirmEvidenceAction`이 이 값을 그대로 돌려준다(DR-4 rev 9 — 06-17 · 06-20 제자리 확인이 이 값으로 행을 바꾼다)
    - E2E: 결재 통과 · 증빙 1건(Phase 5 domain 함수로 생성) 문서 → 증빙 섹션 `확인 전` → 1차 `증빙 확인` → `확인됨` → 1차 `지급 완료` → `지급 완료`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. **N-6:** A-608의 Phase 5 PM 증빙 편집 함수가 증빙 금액 칸 갱신 말고 하는 부수 작업(파생 값 재계산 · 합계 · 행동 로그 · 캐시 무효화 · 문서 version 갱신 · 그 밖 찾은 것)을 목록으로 SUMMARY에 적는다. Task 3의 금액 고침은 그 함수를 부르지 않으므로(B-3), 줄마다 ⒜ 필요 — Task 3이 같은 트랜잭션에서 따라 한다(그 함수가 부르는 하위 함수가 있으면 그것을 부르고 새로 쓰지 않는다) ⒝ 뺀다 — 이유 한 줄(확인을 풀고 문서 version을 올리는 06-11 무효화 훅은 ⒝, B-3 — 금액 고쳐 확인은 스스로 `bumpExpenseVersion`을 부른다) 중 하나로 표시한다. 「문서 version 갱신」 줄은 06-11 훅의 호출 자리를 정하는 입력이기도 하다(Phase 5 함수가 스스로 올리면 06-11이 훅을 그 뒤에 둔다 — B-1 r2). 금액이 바뀌면 틀어지는 값(파생 값 · 합계 · 캐시)은 ⒜다. **⒜ 줄마다 「전역 `db` 읽기」 칸을 더한다(06-03 tx 규약 · CROSS E-1):** 그 하위 함수가 `tx` 인자를 받는지, 안에서 `can` · `getSettingValue`(세율 포함) · `tx` 없는 리포지토리를 부르는지 적는다 — 부르면 Task 3은 그 읽기를 트랜잭션 전에 해 인자로 넘기고(세율이면 06-03 `loadTaxRates`), 그렇게 쪼갤 수 없는 하위 함수면 트랜잭션 안에서 부르지 않고 멈춰 SUMMARY에 적는다.

① **스키마(신규 표).** `db/schema/expense-payments.ts`에 `expense_evidence_reviews`(id · expense_id FK · status text CHECK in (confirmed, waived) · amount_before_krw null · amount_after_krw null · waive_reason null · reviewed_by FK · reviewed_at · version)를 더하고 문서당 한 줄은 `uniqueIndex().on(expense_id)`(B-2 — `.unique()` 아님). **마이그레이션:** `pnpm db:generate --name evidence_reviews` → SQL 머리 SET LOCAL 한 쌍(0010 선례) → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 `pnpm db:generate` `No schema changes`.

② **domain · 리포지토리.** `repositories/expense-evidence-reviews.ts`(viewer 첫 인자): 확인 기록 upsert(`ON CONFLICT (expense_id) DO UPDATE`) · 조회 · 증빙 금액 칸 갱신(Phase 5 표의 그 칸만 — B-3, Task 3이 쓴다). `domain/evidence-reviews/index.ts`: `confirmEvidence(viewer, { expenseId, version })` — 트랜잭션 전 `can(viewer, "expenses.payments", "write")`(D-601) · 한 트랜잭션에서 `lockExpenseRow`(R-1) → version 비교 → upsert → `bumpExpenseVersion` · 커밋 뒤 `recordAction` `document_update`(06-03 tx 규약). 리포지토리 함수는 모두 `tx: DbOrTx`를 받는다. **돌려준 값(DR-4 rev 9):** `{ version, evidenceStatus, actionRow }` — `version`은 `bumpExpenseVersion`이 돌려준 새 값, `evidenceStatus`는 새 확인 기록으로 `resolveEvidenceStatus`, `actionRow`는 잠근 문서와 새 증빙 상태를 DB 없는 순수 함수 `resolveExpenseActionRow(state, perms)`에 넣은 결과다 — `perms`는 트랜잭션 전 권한 판정에서 얻은 값을 넘긴다(트랜잭션 안에서 새 조회 · 새 권한 판정을 더하지 않는다 — 06-03 tx 규약). 판정 · 권한 · 잠금 순서는 바꾸지 않는다.

③ **액션 · 화면.** `confirmEvidenceAction`을 `actions.ts` · `actions.registry.ts`에 더한다 — zod 입력 = expenseId · version · correctedAmountKrw?(Task 3), 돌려준 값은 ②의 `{ version, evidenceStatus, actionRow }` 그대로(06-17 · 06-20이 이 모양을 SUMMARY에서 읽는다 — SUMMARY에 입력 · 응답 모양을 적는다). `evidence-review-section.tsx`(S4): 파일 행은 Phase 5 첨부 영역을 그대로 쓰고, `증빙 금액` 값 + 2행 입력자, 확인 줄 값(`statusDisplay`), 권한 없는 사람은 읽기 + 2행 `확인은 경영관리`. `page.tsx`에 결재 통과 문서일 때 섹션을 붙인다. 1차 `증빙 확인`은 06-03의 행동 줄이 렌더한다 — `domain/payments/action-row.ts`의 `resolveExpenseActionRow`에 P2 행(지급 전 · 증빙 있음 · 확인 기록 없음 → 1차 `증빙 확인`)을 더한다. 이 갈래를 O-2 상수로 옮기는 것과 서버 게이트의 확인 전 막힘은 Task 2다.

④ **E2E.** `payment-single.spec.ts`에 behavior 둘째 줄 케이스를 더한다. 증빙 행은 A-608의 Phase 5 **domain 함수로** 만든다 — 업로드 화면을 거치지 않는다(B-4: 업로드 테스트 모드 확인은 06-11 Task 1 ⓪에서 한다).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 `Found N issues`를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — 증빙 확인 → 지급 완료 트레이서 케이스가 빨갛다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(증빙 행 생성 함수 이름 · Phase 5 PM 편집 부수 작업 목록과 줄마다 ⒜/⒝ 표시 포함)가 SUMMARY에 있다
    - `grep -c "SET LOCAL lock_timeout" db/migrations/*evidence_reviews*.sql`이 1
    - `grep -c "uniqueIndex" db/schema/expense-payments.ts`가 2 이상(지급 기록 · 확인 기록)
    - E2E 준비 코드가 업로드 화면 조작 없이 증빙 행을 만든다
    - SUMMARY에 `confirmEvidenceAction`의 zod 입력과 응답 모양 `{ version, evidenceStatus, actionRow }`가 적혀 있다(06-17 선행 의존 「06-06」 줄이 이것을 읽는다 — DR-4 rev 9)
  </acceptance_criteria>
  <done>증빙 있는 문서 하나가 확인 표 → 확인 액션 → 문서 화면 P2 → 지급 완료까지 실제로 이어지고 E2E가 CI=true로 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [O-2 기본값] 확인 전 막힘 — 상수 한 곳 · P2 · P5 · `evidenceGateInputs`</name>
  <files>domain/payments/action-row.ts, domain/payments/index.ts, domain/evidence-reviews/index.ts, test/unit/domain/evidence-reviews.test.ts, test/unit/domain/payments-action-row.test.ts, test/e2e/payment-single.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「열린 선택 (사용자 확인 후보)」 표 O-2 행(O-2 기본값과 대안)
    - 06-04가 만든 `evidenceGateDecision` · domain/payments/index.ts의 ctx 채움
  </read_first>
  <behavior>
    - `resolveEvidenceStatus` 다섯 값(면제가 선결제를 이긴다)
    - `EVIDENCE_CONFIRMATION_GATES_PAYMENT = true`: 증빙 있음 · `확인 전` → `evidenceGateDecision`이 `증빙 확인 전 · 증빙 확인`으로 막음(증빙 필수 설정 off여도)
    - 상수를 false로 바꾼 테스트 케이스(주입) → 같은 문서 통과(정책이 한 곳에서만 오는지 고정)
    - `resolveExpenseActionRow`: 지급 전 · `확인 전` → P2 / 지급 뒤 · `확인 전` → P5
    - `evidenceGateInputs`: 확인 표 waived → `waived: true` · confirmation = `resolveEvidenceStatus` 결과
  </behavior>
  <action>
**이 태스크가 O-2의 유일한 자리다.** `domain/payments/action-row.ts`에 상수 `EVIDENCE_CONFIRMATION_GATES_PAYMENT = true`(O-2 기본값 — D-602 그대로)와 `confirmationBlocksPayment(status)`를 두고, 상수 선언 위 한 줄 주석에 "O-2 답이 바뀌면 이 상수와 P2 갈래만 고친다"를 적는다. `evidenceGateDecision`의 `confirmation` 갈래가 이 함수를 부른다 — 규칙 몸통에 같은 조건을 다시 쓰지 않는다. P2 · P5를 `resolveExpenseActionRow`에 더한다(P2 갈래는 이 상수를 읽는다).

`domain/evidence-reviews/index.ts`에 `resolveEvidenceStatus`와 `evidenceGateInputs(viewer, lockedDoc, pre, tx?)`를 두고, `completeExpensePayment`(domain/payments/index.ts)의 ctx 채움을 이 함수 호출 하나로 바꾼다 — `evidenceRequired` · `pmName`은 06-04가 트랜잭션 전 `loadPaymentInputs`로 읽은 `pre`에서 그대로, `evidenceCount`는 잠근 문서에서, `waived`는 확인 표의 면제 상태, `confirmation`은 상태 판정 — 확인 표 조회는 `repositories/expense-evidence-reviews.ts`의 `tx: DbOrTx = db` 기본값을 둔 읽기 함수로 잠금 뒤에 한다(06-03 tx 규약 · CROSS R-3). **`tx`는 선택 인자(r4 F-F1):** 지급 완료 경로는 잠근 문서의 `tx`를 반드시 넘기고, 06-15 목록처럼 트랜잭션 없는 읽기는 생략해 리포지토리 기본값(전역 `db`)을 쓴다 — domain 파일은 `db` 값을 import하지 않는다(eslint boundaries — domain은 `domain` · `repositories` · `lib`만). `DbOrTx` 타입은 기존 domain 꼴대로 리포지토리 쪽 `import type`으로 받는다. `prepaid`는 선결제 칸이 생기는 06-10이 이 함수 안에서 채운다(06-10 must_haves에 있다). 테스트는 behavior 다섯 줄(실패 → 구현 → 통과).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence-reviews.test.ts test/unit/domain/payments-action-row.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다 — boundaries 오류면 domain이 `db` 값을 import했다(r4 F-F1), 타입 오류면 `tx?` 서명과 호출자가 어긋난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rln "EVIDENCE_CONFIRMATION_GATES_PAYMENT" domain/ app/` 결과가 `domain/payments/action-row.ts` 한 파일이다(O-2 판정이 한 파일 안에만 있다 — 다른 파일은 `confirmationBlocksPayment` · `evidenceGateDecision`을 부른다)
    - 상수 주입 false 케이스가 있다
    - 트레이서 E2E(증빙 확인 → 지급 완료)가 CI=true로 녹색이다
    - [r4 F-F1] `evidenceGateInputs` 서명의 넷째 인자가 선택(`tx?`)이고, `grep -v '^\s*//' domain/evidence-reviews/index.ts | grep -c "@/db/client"` 결과가 0이다(domain이 `db` 값을 import하지 않는다 — `pnpm lint` boundaries가 같은 것을 막는다). `grep -n "evidenceGateInputs(" domain/payments/index.ts`의 지급 완료 호출 줄이 넷째 인자로 `tx`를 넘긴다(그 줄을 SUMMARY에)
  </acceptance_criteria>
  <done>O-2 기본값이 한 상수에서만 나오고, 확인 전 문서는 지급이 막히며, 게이트 ctx가 한 함수에서 만들어진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 금액 고쳐 확인(D-602 · B-3) · S4 완성 · 통합 테스트</name>
  <files>domain/evidence-reviews/index.ts, repositories/expense-evidence-reviews.ts, app/(app)/expenses/[id]/evidence-review-section.tsx, app/(app)/expenses/[id]/actions.ts, test/integration/evidence-reviews.test.ts, test/e2e/payment-single.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「SP-3. §6-3에 「제출 뒤 문서 화면」 — 1차 = 보는 사람의 다음 한 수」 ①(값과 행동을 1차 한 번에) · UI Considerations의 S4 행 · 「금액 표시」 절 「낙관적 잠금」
    - A-608의 Phase 5 PM 증빙 편집 함수(이 경로를 **부르지 않는다** — 06-11이 그 함수에 무효화 훅을 붙인다)
  </read_first>
  <behavior>
    - `confirmEvidence({ correctedAmountKrw })` → 증빙 금액 칸 = 새 값 · 확인 기록 before/after · 행동 로그 한 줄에 전·후 · 상태 `확인됨`
    - 고쳐 확인한 뒤에도 상태 `확인됨`(B-3 — "경영관리 금액 고쳐 확인 → 확인됨 유지")
    - 확인 전후로 비용 기준 값 = 증빙 금액 하나(새 비용 단계 없음)
    - `expenses.payments` 권한 없는 계정 → 거부 · version 불일치 → 거부
    - 증빙 있음 · 확인 전 문서의 지급 완료 → 거부 `증빙 확인 전 · 증빙 확인`
    - 「확인 응답에 새 version」: 금액 없는 확인의 응답 `version`이 커밋 뒤 DB 문서 version과 같고, 그 `version`으로 부른 지급 완료(`completeExpensePayment`)가 동시성 막힘 없이 통과한다 — 확인 전 `version`으로 부르면 동시성 거부(DR-4 rev 9)
  </behavior>
  <action>
`confirmEvidence`에 `correctedAmountKrw?`를 더한다 — 금액이 있으면 **`repositories/expense-evidence-reviews.ts`의 증빙 금액 갱신 한 번**(Phase 5 표의 그 칸만, 같은 트랜잭션)으로 새 값을 쓰고(D-602 — 고친 값이 새 원본), 확인 기록에 전·후를, 행동 로그 `document_update`에 전·후 금액을 싣는다. Phase 5의 PM 증빙 편집 함수를 부르지 않는다(B-3 — 06-11의 무효화 훅이 그 함수에 붙는다). 대신 Task 1 ⓪ 목록에서 ⒜로 표시한 부수 작업을 같은 트랜잭션에서 그대로 따라 한다(N-6). 그중 「전역 `db` 읽기」가 있는 줄은 그 읽기(세율이면 06-03 `loadTaxRates`, 설정이면 `getSettingValue`, 권한이면 `can`)를 트랜잭션 **전**에 끝내 인자로 넘긴다 — 트랜잭션 콜백 안에는 `tx`를 받는 리포지토리와 DB 없는 함수만 둔다(06-03 tx 규약 · CROSS E-1). 행동 로그는 커밋 뒤다.

S4 완성: 지급 전에만 서버 계산 한 줄(부가세 · 지급 총액 — 서버 액션 응답, 트랜잭션 없이 06-03 `loadPaymentInputs` → `decidePayable`에 칸의 금액을 넣어 셈한다, 오는 동안 이전 값 `--faint`), 확인됨 2행 `확인됨 · {사람} {MM-DD HH:mm} · {전} → {새}`(숫자 `nowrap`), 3차 `바꾸기`(증빙 금액 → 칸 편집, 저장은 1차 `증빙 확인`이 값과 행동을 한 번에 — SP-3 ①). 오류 문구는 Copywriting 행 그대로.

`test/integration/evidence-reviews.test.ts`(신규): behavior 여섯 줄을 개별 `it`으로(여섯째 이름 「확인 응답에 새 version」 — Task 1 ②가 만든 돌려준 값을 금액 없는 갈래와 금액 고친 갈래 둘 다 단언한다). 06-11이 무효화 훅을 붙인 뒤 이 파일을 다시 돌려 「고쳐 확인 → 확인됨 유지」가 녹색인지 본다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence-reviews.test.ts test/integration/expense-payments.test.ts test/integration/expense-payments-concurrency.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — expense-payments가 빨가면 게이트 ctx 이동이 지급 경로를 깨뜨렸고(「잠금 뒤 게이트 재판정」이 빨가면 `evidenceGateInputs`가 사전 조회 값을 읽는다), expense-payments-concurrency 「지급 완료 동시 6건」이 타임아웃이면 `evidenceGateInputs`가 트랜잭션 안에서 전역 `db`를 읽고(06-03 tx 규약), leak-scan이 빨가면 확인 DTO가 권한 없는 계급에 금액을 흘렸고, 「확인 응답에 새 version」이 빨가면 응답 `version`이 DB와 어긋나 제자리 확인 뒤 일괄 처리가 동시성에 걸린다(DR-4 rev 9)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트에 「고쳐 확인 → 확인됨 유지」 `it`이 이름 그대로 있다(06-11이 이 이름으로 다시 본다)
    - `grep -c "확인 응답에 새 version" test/integration/evidence-reviews.test.ts`가 1 이상이고 녹색이다(DR-4 rev 9 — 06-17 · 06-20 제자리 확인의 서버 쪽 전제)
    - `confirmEvidence`가 Phase 5 PM 증빙 편집 함수를 import하지 않는다(06-11 verify가 이 케이스를 다시 돌려 확인한다)
    - Task 1 ⓪ 목록의 ⒜ 부수 작업마다 금액 고쳐 확인 뒤 그 결과(파생 값 · 합계 등)가 PM 편집 뒤와 같은지 통합 테스트가 단언한다(N-6 — ⒜가 0건이면 SUMMARY에 그렇게 적는다)
    - 06-03 tx 규약: `confirmEvidence`의 `withTransaction` 콜백 줄 범위를 SUMMARY에 적고 `sed -n '{시작},{끝}p' domain/evidence-reviews/index.ts | grep -v '^\s*//' | grep -cE '\b(can|getSettingValue|loadTaxRates|applyTaxRule|recordAction)\('` 결과 0을 함께 적는다
    - E2E에 「금액 고쳐 확인 → 2행 전 → 새」 케이스가 녹색이다
  </acceptance_criteria>
  <done>경영관리가 금액을 고쳐 확인하면 전·후가 기록되고 확인이 유지되며, 확인 전 문서는 지급이 막힌다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 증빙 확인 액션 | 문서 id · 고친 금액 · version이 넘어온다 |
| domain/evidence-reviews → Phase 5 증빙 칸 | 이 모듈이 남의 페이즈 칸 하나를 갱신한다 — Phase 5 편집 함수와 다른 경로(B-3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-26 | Elevation of Privilege | `confirmEvidence` | high | mitigate | domain 첫 줄 `can(viewer, "expenses.payments", "write")`(D-601) · 권한 없는 계정 거부 통합 테스트 |
| T-06-27 | Tampering | 확인 경합 | medium | mitigate | `lockExpenseRow`(R-1) · 문서 version 비교·+1 · `uniqueIndex(expense_id)` |
| T-06-28 | Tampering | 확인 우회 지급 | high | mitigate | O-2 상수가 `evidenceGateDecision` 안에서 서버 게이트로 막는다 — 화면 P2만이 아니다 |
| T-06-29 | Repudiation | 금액 고침 | medium | mitigate | 확인 기록 before/after + `document_update` 로그 전·후 |
| T-06-30 | Denial of Service | 신규 표 마이그레이션 | low | mitigate | SET LOCAL 한 쌍 · `pnpm lint:sql` 0건 |
| T-06-30a | Denial of Service | 증빙 확인 · 금액 고침 트랜잭션의 풀 소진(CROSS E-1) | high | mitigate | 권한 · 설정 · 세율 · ⒜ 부수 작업의 전역 `db` 읽기를 트랜잭션 전에, 행동 로그는 커밋 뒤(06-03 tx 규약). 콜백 범위 grep 0 · 「지급 완료 동시 6건」 재실행 |
</threat_model>

<verification>
- 단위 evidence-reviews · payments-action-row, 통합 evidence-reviews · expense-payments · expense-payments-concurrency(「지급 완료 동시 6건」, 06-03) · leak-scan 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E payment-single이 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S4 DOM 실측 감사(backstop 1건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 증빙 있음 → 증빙 확인 → 지급 완료가 문서 화면에서 실제로 돈다
- O-2 답이 바뀌어도 한 상수와 한 갈래만 고친다
- 경영관리의 금액 고침 확인이 확인을 풀지 않는다(B-3)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `expense_evidence_reviews`(`uniqueIndex(expense_id)`) |
| 마이그레이션 | `db/migrations/NNNN_evidence_reviews.sql`(SET LOCAL 한 쌍) |
| 상수 | `EVIDENCE_CONFIRMATION_GATES_PAYMENT`(O-2) |
| 함수(domain) | `resolveEvidenceStatus` · `confirmEvidence` · `evidenceGateInputs` · `confirmationBlocksPayment` · `resolveExpenseActionRow`(P2 · P5) |
| Server Action | `confirmEvidenceAction`(응답 `{ version, evidenceStatus, actionRow }` — DR-4 rev 9, 06-17 · 06-20이 읽는다) |
| 화면 | `app/(app)/expenses/[id]/evidence-review-section.tsx`(S4) |
| 테스트 | `test/unit/domain/evidence-reviews.test.ts` · `test/integration/evidence-reviews.test.ts` · payment-single E2E 케이스 |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-1 지급 완료 교착 — 06-06(증빙 확인 금액 고침)이 경로를 물려받음(eng-cross-opus.md L13@333f7bf) | 막음 | Task 1 ② `confirmEvidence`: `can` 트랜잭션 전 · 행동 로그 커밋 뒤. Task 1 ⓪ N-6 목록에 ⒜ 줄마다 「전역 `db` 읽기」 칸 — Task 3이 그 읽기를 트랜잭션 전으로(쪼갤 수 없으면 멈춤). Task 3 서버 계산 한 줄은 트랜잭션 없이 `loadPaymentInputs` → `decidePayable`. Task 3 verify에 06-03 「지급 완료 동시 6건」 재실행 + 콜백 범위 grep. T-06-30a |
| ENG E-3 tx 규약 참조(eng-review.md L37@333f7bf) | 고침 | must_haves에 「06-03 tx 규약」 한 줄(이 플랜 몫) |
| CROSS R-3 게이트 입력이 잠금 뒤 `tx`로 읽히는지(eng-cross-opus.md L37@333f7bf) | 참고 | `evidenceGateInputs(viewer, lockedDoc, pre, tx)` — 문서 상태(증빙 수 · 면제 · 확인)는 `tx`, 사전 조회 `pre`에서는 설정 값 · 표시 문자열만(must_haves · Task 2). 06-04 「잠금 뒤 게이트 재판정」을 Task 3 verify가 다시 돌린다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — f2720a8(디자인 검토 반영 — `/plan-design-review`, 반영 지시 5 · 13)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-4 — 지급 대상 행에서 증빙 확인을 제자리(확인 모달)로, 서버는 06-06 `confirmEvidenceAction` 그대로 · 확인 뒤 저절로 골라짐(design-review.md L35@f2720a8, 사용자 카드 기본값 「예」 — O-21) | HIGH(고침) | 판정 · 권한 · 잠금은 바꾸지 않고 돌려준 값만 더함: must_haves 「DR-4(rev 9) 응답 모양」 · key_links `actions.ts` → `domain/evidence-reviews` · Task 1 behavior 둘째 줄 · ② 「돌려준 값」 · ③ zod 입력 · 응답 모양을 SUMMARY에(06-17 선행 의존 「06-06」 줄이 읽는다) · Task 1 acceptance · Task 3 behavior · 통합 `it` 「확인 응답에 새 version」 · fails_when · acceptance grep. 모달 · 행 갱신 · 저절로 골라짐은 06-17 Task 4(S1) · 06-20 Task 3(S3) |
| UI-SPEC 행 번호 표류(rev 9에서 358-383 · 945-951 · 876-885행이 다른 절을 가리킴) | 참고 | Task 1 · Task 3 read_first를 절 이름으로 바꿈(「지출결의 상태 → 1차 (정본 …)」 · UI Considerations S4 행 · 「금액 표시」 「낙관적 잠금」 · SP-3 ①) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| DR-4의 모달 화면 · 첨부 보기 칸(SP-7) · 행 갱신 | 이 플랜 범위 밖 — 06-17 Task 4(S1 · `ui/confirm-dialog` 슬롯) · 06-20 Task 3(S3). 이 플랜은 서버 응답 모양만 |

### Round 3 — 디자인 검토 반영 r2(커밋 전 · `/mnt/project-files/06-gates/design-apply-cross-r1.md` · `design-apply-r2-brief.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 B-1 — `confirmEvidence`는 version만 비교해 모달이 보여 준 증빙 묶음과 확인하는 묶음이 같은지 보지 않는다(design-apply-cross-r1.md B-1 근거 `06-06:165`) | 막음(06-11이 닫음) | 판정은 바꾸지 않는다(권고 ①). Plan 06: must_haves.truths 「B-1(디자인 반영 r2)」 줄 — version 비교가 증빙 변경까지 잡는 근거는 06-11 훅의 version 증가이고, 증명은 06-11 「PM 증빙 변경 뒤 옛 version 확인 거부」. Task 1 ⓪ N-6 ⒝ 이유 문장에 06-11 훅의 version 증가와 호출 자리 입력을 더했다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 B-1 대안 ② — 확인 입력에 증빙 지문(`evidenceStamp`)을 더해 잠금 뒤 비교 | 채택하지 않음 — ①(06-11 훅이 늘 version을 올림)로 닫히고, ②는 이 플랜의 「판정 그대로」 must_haves를 넓힌다(오케스트레이터 지시 ①) |

### Round 4 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-1 정합 — 06-11 훅이 PM 증빙 변경마다 version을 올리면 PM 자신의 저장 · 결재자 승인이 동시성으로 거부될 수 있어, 06-11이 결재 통과 문서에서만 올리고 최종 version을 돌려주게 바뀌었다(design-apply-cross-r2.md X-1, 06-11 Round 3) | 참고(파생) | must_haves.truths 「B-1(디자인 반영 r2)」 줄에 「결재 통과 문서에서 … (통과 전 문서는 올리지 않는다 — `증빙 확인`은 P2 · P5에서만 열린다)」를 더했다. 이 플랜의 판정 · 응답 모양 · 테스트는 그대로 — 올리는 범위가 확인이 열리는 상태 집합과 같아 B-1은 그대로 닫힌다. Round 3 Deferred의 「①(06-11 훅이 늘 version을 올림)」은 이 줄로 읽는다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 5 — r4 (final-review-fable, 2026-09-26)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/final-review-fable.md`(커밋 전 — 줄 번호는 그 파일 기준). 교차 검토는 Codex 대신 Fable(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F-F1 — 06-15 domain이 `evidenceGateInputs`의 `tx` 자리에 `db`를 넘기면 eslint boundaries(domain → db 금지) 위반으로 06-15 `pnpm lint`가 빨갛다. 06-06 서명이 `tx`를 필수 위치 인자로 적었다(final-review-fable.md L21-26) | 고침 | 검증: `eslint.config.mjs` domain allow = `domain` · `repositories` · `lib`, domain의 `DbOrTx`는 `@/repositories/document-counters`에서 `import type`으로만 받는다(저장소 grep). Plan 06: must_haves.truths 게이트 ctx 줄 · Task 2 action 서명을 `evidenceGateInputs(viewer, lockedDoc, pre, tx?)`로, 확인 표 읽기 리포지토리는 `tx: DbOrTx = db` 기본값(`repositories/quote-lines.ts` 꼴), 지급 완료 경로는 `tx`를 반드시 넘김. Task 2 verify에 `pnpm lint && pnpm typecheck` + fails_when, acceptance에 `@/db/client` 0 grep · 지급 완료 호출 줄의 `tx` 인자. 06-15는 `tx`를 생략한다(06-15 Round 6) |
| F-N1 — read_first가 저장소 밖 `/mnt/project-files/06-prep/open-choices.md`를 인용(L51-55) | 참고 | Task 2 read_first를 저장소 안 같은 내용 `06-UI-SPEC.md` 「열린 선택 (사용자 확인 후보)」 표 O-2 행으로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| F-N1 — Round 3 제목의 `/mnt/project-files/06-gates/...` 경로 | 고치지 않음 — 앞 라운드 제목은 그 라운드 입력의 출처 기록이고 ledger는 append-only다. read_first가 아니라 실행자가 읽을 일이 없고, 반영 내용은 그 라운드 표와 플랜 본문에 있다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-06-SUMMARY.md` when done
</output>
