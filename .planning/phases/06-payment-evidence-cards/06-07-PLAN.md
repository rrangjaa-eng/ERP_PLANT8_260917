---
phase: 06-payment-evidence-cards
plan: 07
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/link-targets.ts
  - domain/quotes/lines.ts
  - domain/rules/register.ts
  - repositories/quote-line-links.ts
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/link-picker.tsx
  - app/(app)/cards/actions.ts
  - app/(app)/cards/actions.registry.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/card-usage-section.tsx
  - test/unit/domain/rules-card-dual-link.test.ts
  - test/unit/repositories/quote-line-links-sql.test.ts
  - test/integration/corp-card-usages.test.ts
  - test/e2e/card-usage.spec.ts
autonomous: true
requirements: [EXP-07]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 카드 사용 폼에서 연결 `견적 줄`을 골라 연결 고르기(S10)에서 프로젝트 → 견적 줄을 고르고 저장하면 카드 사용이 그 줄에 이어지고 목록 연결 칸에 `{프로젝트} · {줄}`이 선다(EXP-07)"
    - "**B-1:** 카드 사용 생성은 한 트랜잭션에서 ⑴ `lockQuoteLines`(대상 견적 줄을 `ORDER BY id` 순서로 `SELECT … FOR UPDATE`) ⑵ `findLineLinks` ⑶ `gate(doc, \"card.dual-link-block\", { side: \"card\", links })` ⑷ INSERT 순서로 돈다 — 잠금 없이 판정하지 않는다. 잠금 SQL에 `order by`와 `for update`가 있다는 것은 `.toSQL()` 단위 테스트가 고정한다(CROSS E-5)"
    - "「06-03 tx 규약」을 따른다 — 견적 줄 · 견적 외 비용 갈래도 06-05의 `precheckCardUsage`(트랜잭션 밖: 프로젝트 완료 잠금 판정 · 현재 차수 · 카드 자격 · 세율) → `createCardUsage(viewer, input, pre, tx?)`의 `runCreate`(tx 리포지토리 · 순수 함수 · 게이트만) 두 단계다. 견적 외 비용 줄 생성은 `saveQuoteLines`를 트랜잭션 안에서 부르지 않는다(audit B행 — 그 안의 권한·차수·프로젝트 조회가 전역 `db`, CROSS E-3)"
    - "지출결의가 연결된 견적 줄(법인카드 지급 방식 지출결의 포함)에는 카드 사용을 이을 수 없다 — `card.dual-link-block`이 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄` 꼴 이유로 거부하고, 연결 고르기 목록에서 그 줄은 `aria-disabled` + 2행 이유다(D-609). 지출결의 쪽 판정(`side: \"expense\"`)은 같은 규칙이고 호출은 06-13이 붙인다"
    - "같은 쪽(카드 사용) 여러 건은 한 줄에 이을 수 있고, 견적 줄 아래 힌트와 목록 2행에 `카드 사용 N건 {합}`이 보인다(D-609)"
    - "`견적 외 비용`을 고르고 저장하면 같은 트랜잭션에서 그 프로젝트에 견적가 0인 견적 외 비용 줄이 새로 생기고(항목 기본값 = 가맹점 이름, 실행가 = 카드 공급가) 카드 사용이 그 줄에 연결된다 — 카드 사용 저장이 실패하면 줄도 생기지 않는다(O-8, UA-615)"
    - "조정 줄(`adjustment`)·취소 줄은 연결 고르기 목록에 없다(D-612와 같은 결). 완료(정산) 프로젝트는 직원의 프로젝트 고르기에서 고를 수 없다(행 2행 `완료 · 견적 줄 잠김`) — 대리 등록 권한자의 견적 외 비용 갈래는 06-09가 연다(D-47)"
    - "S10 empty: 「Empty — 연결 고르기 목록」 세 갈래(줄 0 · 검색 0건 · 고를 수 있는 줄 0)가 카드 폼에서 다음 한 수 `견적 외 비용으로`를 말한다 — 구매 요청 폼 갈래(`팀 비용으로`)는 06-14가 같은 컴포넌트에 더한다"
    - "S10 error: 「Error — 연결 고르기 목록 로드(S10)」 — 목록 자리 한 줄 `프로젝트를 불러오지 못했습니다 · 다시 시도` / `견적 줄을 불러오지 못했습니다 · 다시 시도`, 1차 `이 줄로` 비활성, 2차 `취소 Esc`"
    - "S10 populated: 프로젝트 행 = 번호 · 이름 + 2행 `{상태} · 담당 {PM}` / 견적 줄 행 = 번호 · 항목(거래처) · 실행가, 현재 줄 700 + 왼쪽 2px `--accent`, 같은 쪽 기존 연결 2행 `카드 사용 1건 1,200,000`"
    - "S10 partial: 반대쪽 줄은 보이되 `aria-disabled` + 2행 이유(「막힘 — 연결 고르기 목록의 반대쪽 줄」)"
    - "S10 zero-one-many: 부제 `… · 카드로 이을 수 있는 줄 N`, 고를 수 있는 줄 0이면 1차 `이 줄로` 비활성 + `이을 수 있는 줄 없음 · 견적 외 비용으로`"
    - "S15 empty: 「Empty — 상세 「법인카드 사용」 섹션」 — 카드가 있는 사람에게만 3차, 없으면 사실만"
    - "S15 error: 「Error — 섹션 로드」 — 이 섹션만 실패하고 상세의 다른 섹션은 선다(§7-7)"
    - "S15 populated: 열(사용일 · 견적 줄 · 가맹점 · 결제 합계 2행 공급가 · 등록)이고 경영관리 등록 건은 `경영관리 등록`(600) — 팀 비용 연결 건은 없다"
    - "S15 partial: `quote.amount`가 없는 사람은 금액 열이 없고 부제가 `{N}건`만이다 — 서버가 DTO에서 칸을 뺀다"
    - "S15 zero-one-many: 부제 `{N}건 · 결제 합계 {합}`, 0건 = Empty 한 줄"
    - statement: "S10 loading: 모달·시트 목록 로드와 검색 중 표시가 §7-7 LOADING 꼴이다 — 느린 응답을 강제해 CI=true DOM 감사"
      verification: backstop
    - statement: "S10 overflow: 견적 줄 100줄 이상 프로젝트에서 480 모달·폰 시트 안 목록이 스크롤로 서고 행동 줄이 가려지지 않는다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S10 long-text: 긴 항목·거래처·프로젝트 이름이 480 폭에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S15 loading: 섹션 로드가 §7-7 LOADING 꼴(섹션 단위)이다 — 느린 응답을 강제해 CI=true DOM 감사"
      verification: backstop
    - statement: "S15 overflow: 카드 사용 50건 이상 프로젝트에서 섹션 표가 §7-3 (자) 규칙대로 선다(이 섹션의 페이지 나눔은 본문에 없다 — 나누지 않는다) — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "한 견적 줄의 같은 돈이 지출결의 쪽과 카드 사용 쪽에 동시에 잡혀서는 안 된다(D-609 — 이중 계산). 화면 비활성만으로 막지 않고 잠금 뒤 서버 게이트가 거부한다 — 이 플랜은 카드 쪽 입구만 닫는다. 지출결의 쪽 입구와 동시 요청(카드 ∥ 지출결의 · 구매 요청 ∥ 지출결의) 병렬 테스트는 06-13이 닫는다"
      status: unresolved
      verification: test
  artifacts:
    - path: "repositories/quote-line-links.ts"
      provides: "견적 줄 잠금(`ORDER BY id FOR UPDATE` — 쿼리 빌더 `lockQuoteLinesQuery`를 드러내 `.toSQL()`로 검사) · 줄마다 연결 문서(지출결의 · 카드 사용) 조회 · 견적 외 비용 줄의 끝 순서 조회(tx) — 이중 연결 판정과 줄 상태 파생의 공용 입력"
      exports: ["lockQuoteLinesQuery", "lockQuoteLines", "findLineLinks"]
    - path: "test/unit/repositories/quote-line-links-sql.test.ts"
      provides: "[CROSS E-5] 잠금 SQL에 `order by \"quote_lines\".\"id\"`와 `for update`가 있는지 `.toSQL()`로 단언"
      min_lines: 10
    - path: "domain/corp-card-usages/link-targets.ts"
      provides: "연결 고르기 목록 DTO(프로젝트 · 견적 줄 — 줄마다 고를 수 있는지 서버가 보낸다)"
    - path: "app/(app)/cards/link-picker.tsx"
      provides: "S10 연결 고르기(PC 480 모달 · 폰 시트) — `mode` 속성, 이 플랜은 `card`"
      min_lines: 60
    - path: "app/(app)/projects/[id]/card-usage-section.tsx"
      provides: "S15 프로젝트 상세 「법인카드 사용」 섹션"
      min_lines: 30
  key_links:
    - from: "domain/corp-card-usages/index.ts"
      to: "repositories/quote-line-links.ts"
      via: "잠금 → 연결 조회 → 게이트 → INSERT 순서(B-1)"
      pattern: "lockQuoteLines"
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/rules/gate.ts"
      via: "저장 전 이중 연결 판정을 게이트 한 지점에서 받는다"
      pattern: "card.dual-link-block"
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/quotes/lines.ts"
      via: "견적 외 비용 줄 생성을 견적 줄 모듈에 맡기고 같은 트랜잭션에서 부른다"
      pattern: "createOutOfQuoteLine"
---

<objective>
카드 사용을 **견적 줄 · 견적 외 비용**에 잇는 경로를 세우고, 한 견적 줄에 지출결의와 카드가 동시에 잡히지 않게 하는 **카드 쪽 입구**를 잠금 뒤 게이트로 닫는다(B-1). 연결 고르기(S10)와 프로젝트 상세 「법인카드 사용」(S15)을 붙인다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 「견적 줄에 한 번만」은 카드 쪽에서 가장 쉽게 깨진다. 교차 검토 B-1이 짚은 대로 판정 전에 견적 줄을 잠그지 않으면 동시 두 요청이 둘 다 통과한다 — 이 플랜이 `lockQuoteLines`를 만들고 카드 생성 경로에 먼저 쓴다. 구매 요청(06-08) · 연결 변경(06-09) · 지출결의 제출(06-13)이 같은 함수를 쓴다.

Output: `repositories/quote-line-links.ts`(`lockQuoteLines` · `findLineLinks`), 게이트 `card.dual-link-block`, `createOutOfQuoteLine`, S10 · S15, 단위 1 · 통합 1 · E2E 케이스.

**규모 근거:** `files_modified` 15개, `estimate.tokens` 105000(`confidence: low`, 표본 0 — 보정 계수 1). 엔지니어링 검토 반영(CROSS E-5 `.toSQL()` 단위 테스트 1파일 · CROSS E-3 사전 조회 분리)으로 5k 늘었다. 예산 100k를 조금 넘는다 — 재분할 방아쇠: Task 2 끝에서 컨텍스트 60%를 넘으면 Task 3(S15)을 새 플랜으로 넘기고 SUMMARY에 적는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-05-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-07 착수하지 않음`을 출력하고 멈춘다.** 이름은 가칭이다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-603 | Phase 4 04-13 — 견적 줄 종류(`quote` · `out_of_quote` · `adjustment`) 컬럼. 견적 외 비용 줄 생성(O-8)이 이 종류에 기댄다. **계획 판단:** 조정 줄은 카드 연결 대상이 아니다 | `grep -n "out_of_quote" db/schema/quote-lines.ts` 0건이면 멈춤 |
| A-607 | Phase 5 지출결의의 견적 줄 연결 칸(가칭 `expenses.quote_line_id`)과 지급 방식(UA-618) — 반대쪽 판정 입력 | `grep -rn "quote_line_id\|quoteLineId" db/schema/` 에 지출결의 표 줄이 없으면 멈춤 |
| 06-05 | `createCardUsage` · `card-usage-form.tsx` | `git ls-files domain/corp-card-usages/index.ts` 비면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-8 | 「견적 외 비용」 연결은 저장 때 견적 외 비용 줄을 새로 만든다 | `createCardUsage`의 견적 외 비용 갈래 한 곳 |
| B-1 잠금 순서 | 대상 줄 id 오름차순 `FOR UPDATE`(교착 방지). **전역 순서(N-3):** 프로젝트 행 → 견적 줄(id 순) → 지출결의 · 카드 사용 · 구매 요청 문서 행 — Phase 4 `loadProjectForGate`가 프로젝트 → 줄 순서이므로 한 트랜잭션에서 `lockQuoteLines` 뒤에 프로젝트 행을 잠그지 않는다. 견적 줄 갈래는 줄 → 카드 INSERT, 견적 외 비용 갈래는 새 줄 INSERT → 카드 INSERT이고 프로젝트 행을 잠그지 않는다 — `project.completed-lock` 판정은 `precheckCardUsage`가 트랜잭션 전에 한다(기존 `saveQuoteLines` 단독 경로가 권한·프로젝트 판정을 `withTransaction` 앞에서 하는 것과 같은 시점, 06-03 tx 규약) | `lockQuoteLines` 한 함수 + 그 머리 주석 |

<assumption_delta_decision>
`detected: false`. 견적 줄(Phase 4 명사)에 연결 조회를 **더한다**. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-07 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 3 · D-609 · D-612 · O-8. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 카드 사용 한 건이 잠금 → 연결 조회 → 이중 연결 게이트 → 저장을 지나 견적 줄에 이어진다</name>
  <files>repositories/quote-line-links.ts, domain/rules/register.ts, domain/corp-card-usages/index.ts, domain/corp-card-usages/link-targets.ts, app/(app)/cards/link-picker.tsx, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/unit/domain/rules-card-dual-link.test.ts, test/unit/repositories/quote-line-links-sql.test.ts, test/e2e/card-usage.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 625-653행(연결 · S10) · 987-994행(S10 considerations) · Copywriting 「막힘 — 연결 고르기 목록의 반대쪽 줄」 · 「Empty — 연결 고르기 목록」 · 「Error — 연결 고르기 목록 로드(S10)」
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-609 · D-612
    - domain/rules/gate.ts(`gate(doc, ruleName, ctx)`) · domain/rules/register.ts
    - repositories/expense-payments.ts(06-03 `lockExpenseRow` — `.for("update")` 선례)
    - A-603 · A-607 재확인에서 찾은 칸 이름
    - domain/corp-card-usages/index.ts(06-05 — `precheckCardUsage` · `createCardUsage(viewer, input, pre, tx?)` · `runCreate`) · 06-03-PLAN.md must_haves.truths의 「tx 규약」 줄
    - `drizzle-orm/node-postgres`의 `drizzle.mock()`(DB 연결 없이 쿼리 빌더의 `.toSQL()`을 얻는다 — drizzle-orm 0.45.2에서 `select … order by "quote_lines"."id" asc for update` 꼴로 나오는 것을 계획 단계에서 확인했다)
  </read_first>
  <behavior>
    - `card.dual-link-block` ctx `{ side, links }`: side card · 살아 있는 지출결의(반려·취소 제외, 법인카드 지급 방식 포함) 있음 → 막힘 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄` / 카드 사용만 있음 → 통과 / 연결 0 → 통과
    - side expense · 카드 사용 있음 → 막힘 `카드 사용 {N}건 연결됨 · 지출결의는 다른 줄`
    - (CROSS E-5) `lockQuoteLinesQuery(viewer, [3, 1, 2], drizzle.mock({ schema })).toSQL().sql`이 `order by "quote_lines"."id"`(뒤에 ` asc` 허용)를 담고 `for update`로 끝난다(`for update of "quote_lines"` 허용) — 결과 행 순서가 아니라 생성 SQL을 단언하므로 `ORDER BY`나 `FOR UPDATE`를 빼면 빨갛다
    - E2E: 직원 → 카드 사용 폼 → 연결 `견적 줄` → S10에서 프로젝트 → 줄 → `이 줄로` → 저장 → 목록 연결 칸 `{프로젝트} · {줄}`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① **리포지토리(B-1 · CROSS E-5).** 먼저 `test/unit/repositories/quote-line-links-sql.test.ts`에 behavior 셋째 줄을 쓰고 실패(RED — 함수가 없어 빨갛다)를 확인한다. 그다음 `repositories/quote-line-links.ts`(viewer 첫 인자)에 쿼리 빌더 `lockQuoteLinesQuery(viewer, lineIds, tx)` — 대상 줄을 `inArray` + `orderBy(asc(quoteLines.id))` + `.for("update")`로 만든 **await하지 않은** 빌더 — 와 그것을 await하는 `lockQuoteLines(viewer, lineIds, tx)`를 둔다(잠금은 이 한 빌더로만 — 교착 방지. 함수 머리 주석 한 줄에 줄 id 순서와 전역 순서 「프로젝트 → 견적 줄 → 문서 행」을 적는다, N-3). `findLineLinks(viewer, lineIds, tx)` — 줄마다 살아 있는 지출결의(A-607 칸) · 카드 사용 목록(구매 요청은 06-13이 더한다). 두 함수 모두 `tx`로만 읽는다(06-03 tx 규약).

② **게이트.** `domain/rules/register.ts`에 `card.dual-link-block`을 등록한다 — ctx `{ side: "card" | "expense", links }`(R-8), 반대쪽 연결이 있으면 막힘, 같은 쪽 여러 건 통과(D-609). 판정은 순수 함수로 두고 규칙 몸통은 그 함수를 부른다.

③ **domain(06-03 tx 규약 · CROSS E-3).** 견적 줄 갈래를 06-05의 두 단계에 나눠 더한다. `precheckCardUsage`(트랜잭션 밖): 대상 줄의 프로젝트를 읽어 완료(정산) 프로젝트면 직원 거부(D-47 — 기존 `project.completed-lock` 판정을 여기서 부른다), 카드 자격 · 세율은 06-05 그대로. `createCardUsage`의 `runCreate`(06-05가 만든 트랜잭션 몸통): `lockQuoteLines(…, innerTx)` → `findLineLinks(…, innerTx)` → `gate(doc, "card.dual-link-block", { side: "card", links })` → INSERT(순서 고정 — B-1). `runCreate` 안에서는 권한 · 설정 · 프로젝트 조회를 부르지 않는다 — 판정 입력은 `pre`와 잠근 뒤 `innerTx`로 읽은 연결뿐이다. 먼저 behavior 첫째 · 둘째 줄 단위 테스트를 쓰고 RED를 확인한 뒤 게이트 · 갈래를 구현한다. `domain/corp-card-usages/link-targets.ts`: 프로젝트 고르기 목록(검색 번호·이름·클라이언트, 미수주는 끝, 완료는 직원에게 고를 수 없음 + 2행 `완료 · 견적 줄 잠김` — D-47)과 견적 줄 고르기 목록(취소·조정 줄 제외 — D-612와 같은 결, 반대쪽 줄은 `selectable: false` + 이유, 같은 쪽 기존 연결 2행 `카드 사용 N건 {합}`(서버 `sumKrw`), 부제 `카드로 이을 수 있는 줄 N`) DTO — 고를 수 있는지는 서버가 줄마다 보낸다.

④ **화면.** `link-picker.tsx`(S10 — PC 480 모달 · 폰 시트, 1차 `이 줄로 Enter` · 2차 `취소 Esc`, 세 Empty 갈래, 로드 오류 한 줄 + `다시 시도`)는 `mode` 속성을 받고 이 플랜은 `card`만 구현한다(`purchase`는 06-14). 폼에 연결 `견적 줄` 라디오를 더하고 `aria-label`로 `프로젝트 바꾸기` · `견적 줄 바꾸기`를 가른다. 연결 고르기 목록 액션을 등록한다.

⑤ 단위 테스트(behavior 셋째 줄까지 — ① · ③에서 먼저 쓴 것) · E2E(넷째 줄 — 트레이서라 ② 전에 먼저 쓰고 빨간 것을 확인한다).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-card-dual-link.test.ts test/unit/repositories/quote-line-links-sql.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — quote-line-links-sql이 빨가면 잠금 SQL에 `order by "quote_lines"."id"` 또는 `for update`가 없다(CROSS E-5: `lockQuoteLinesQuery`에서 `orderBy`를 지우면 이 케이스가 빨개지는 것을 한 번 확인하고 되돌린다)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - `grep -c "card.dual-link-block" domain/rules/register.ts`가 1 이상
    - `grep -c "for(\"update\")" repositories/quote-line-links.ts`가 1 이상
    - (CROSS E-5) `test/unit/repositories/quote-line-links-sql.test.ts`가 `.toSQL()` 결과에 `order by` · `for update` 두 단언을 담고, `orderBy`를 지운 임시 변경에서 빨간 것을 확인했다(SUMMARY에 한 줄)
    - `createCardUsage`의 견적 줄 갈래에서 `lockQuoteLines` 호출이 `gate(` 호출보다 앞 줄에 있다(SUMMARY에 줄 번호를 적는다)
    - (CROSS E-3) `awk '/const runCreate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\(|findProjectById\('`가 0이고 같은 범위의 줄 수가 5 이상이다
    - E2E 한 경로 CI=true 녹색
  </acceptance_criteria>
  <done>카드 사용 한 건이 잠금 → 연결 조회 → 게이트 → 저장 → 목록까지 실제로 이어지고, 반대쪽 연결이 있는 줄은 서버가 거부한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 견적 외 비용 연결(O-8) · 반대쪽 줄 표시 · 통합 테스트</name>
  <files>domain/quotes/lines.ts, repositories/quote-line-links.ts, domain/corp-card-usages/index.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/link-picker.tsx, test/integration/corp-card-usages.test.ts, test/e2e/card-usage.spec.ts</files>
  <read_first>
    - domain/quotes/lines.ts(`saveQuoteLines` · `QUOTE_LINE_DTO_SPEC` · 순서 칸 — 355-383행의 트랜잭션 전 판정과 481행 `runSave` 안의 `repoInsertQuoteLine(…, innerTx)`를 가른다)
    - /mnt/project-files/deadlock-fix/global-db-in-tx-audit.md B행(`saveQuoteLines`를 열린 tx로 부르면 `can` · `visible` · `findQuoteRevisionById` · `findProjectById` · `listFieldDefinitions`가 트랜잭션 안 전역 `db`가 된다)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 628행(`실행가 {공급가}`)
    - ui/confirm-dialog/ConfirmDialog.tsx(「확인 — 입력 버리기」)
  </read_first>
  <behavior>
    - 견적 외 비용 갈래: 저장 한 번에 줄 1개 + 카드 사용 1개, 카드 사용 INSERT가 실패하면 줄 수가 늘지 않는다(한 트랜잭션)
    - 반대쪽 줄 거부(domain) · 같은 쪽 여러 건 허용 · 조작된 공급가 무시 · 연결 없음 거부(액션 · DB CHECK 둘 다)
    - 완료 프로젝트의 견적 줄 · 견적 외 비용 → 직원 거부
    - 남의 카드(소지자도 사용일 소속 팀도 아님) → `precheckCardUsage` 거부(T-06-21)
    - (CROSS R-2) 세율 기준일 = 사용일: 오늘부터 유효한 부가세율 이력 0.2를 넣은 상태에서 사용일이 지난달인 `vat_surcharge` 카드 사용 → 공급가가 0.1 기준 역산값이다(오늘 기준으로 읽으면 0.2 값이 나와 빨갛다)
  </behavior>
  <action>
먼저 behavior를 `test/integration/corp-card-usages.test.ts`에 개별 `it`으로 쓰고 RED를 확인한 뒤 구현한다.

`domain/quotes/lines.ts`에 `createOutOfQuoteLine(viewer, { revisionId, itemName, executionKrw }, tx)` — 04-13(A-603)의 견적 외 비용 종류, 견적가 0 · 실행가 = 카드 공급가 · 현재 차수의 끝 순서. **트랜잭션 안에서 부르는 함수라 `tx`를 받는 리포지토리만 부른다(06-03 tx 규약 · CROSS E-3)**: 줄 INSERT는 `repoInsertQuoteLine(…, tx)`, 끝 순서는 `repositories/quote-line-links.ts`에 둔 tx를 받는 좁은 조회로 읽는다(`listQuoteLinesByRevision`은 tx 인자가 없다 — audit). `saveQuoteLines`를 열린 tx와 함께 부르지 않는다 — 그 앞머리의 권한 · 금액 보기 · 차수 · 프로젝트 · 사용자 정의 칸 조회가 트랜잭션 안 전역 `db`가 된다(audit B행). 그 판정들(권한 · 현재 차수 id · `project.completed-lock` · 사용자 정의 칸 기본값)은 `precheckCardUsage`의 견적 외 비용 갈래가 트랜잭션 전에 끝내 `pre.outOfQuote = { revisionId, … }`로 넘긴다(기존 `saveQuoteLines` 단독 경로가 같은 판정을 `withTransaction` 앞에서 하는 것과 같은 시점). `createCardUsage`의 `runCreate`에 견적 외 비용 갈래(같은 `innerTx`에서 `createOutOfQuoteLine` → 카드 사용 INSERT — 새 줄이라 잠글 기존 연결이 없다)를 더한다(O-8, UA-615). 폼에 `견적 외 비용` 라디오(프로젝트 고르기 → 항목 기본 = 가맹점 이름)를 더하고, 입력이 있는데 `취소 Esc`면 「확인 — 입력 버리기」(`ConfirmDialog`). 반대쪽 줄은 S10 목록에서 `aria-disabled` + 2행 이유.

`test/integration/corp-card-usages.test.ts`(신규): behavior 다섯 줄 · 옵션 밖 증빙 종류 거부 · 팀 비용 소속 없음 거부 · 외화 저장값 네 칸을 개별 `it`으로.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages.test.ts test/integration/quote-lines.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. quote-lines가 빨가면 `createOutOfQuoteLine`이 기존 저장 경로를 깨뜨렸다. 「세율 기준일 = 사용일」이 빨가면 세율을 오늘 기준으로 읽었다(CROSS R-2)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 원자성 케이스가 카드 사용 INSERT를 실패시켰을 때 견적 줄 수가 늘지 않음을 확인한다
    - (CROSS E-3) `awk '/export async function createOutOfQuoteLine/,/^}$/' domain/quotes/lines.ts | grep -cE '\bcan\(|defaultVisible\(|repoFindProjectById\(|repoFindQuoteRevisionById\(|saveQuoteLines\('`가 0이고 같은 범위의 줄 수가 5 이상이다
    - E2E에 「견적 줄 고르기에서 반대쪽 줄이 `aria-disabled` + 이유」와 「견적 외 비용 저장 → 상세 견적 표에 새 줄」 케이스가 녹색이다
  </acceptance_criteria>
  <done>세 연결 대상이 모두 동작하고, 견적 외 비용 줄이 카드 사용과 원자적으로 생긴다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 프로젝트 상세 「법인카드 사용」 섹션(S15)</name>
  <files>domain/corp-card-usages/index.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/card-usage-section.tsx, test/integration/corp-card-usages.test.ts, test/e2e/card-usage.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 705-712행(S15) · 1027-1033행(S15 considerations) · Copywriting 「표시 — 경영관리 등록」 · 「Empty — 상세 「법인카드 사용」 섹션」 · 「Error — 섹션 로드」
    - app/(app)/projects/[id]/page.tsx · app/(app)/projects/[id]/revenue-section.tsx(섹션 모양 선례)
    - domain/permissions/info-items.ts(`quote.amount`)
  </read_first>
  <behavior>
    - `listProjectCardUsages`: 그 프로젝트의 견적 줄(견적 외 비용 포함)에 이어진 카드 사용만, 팀 비용 건 없음
    - `quote.amount` 없는 계급 → DTO에 결제 합계·공급가 칸 없음, 부제는 건수만
    - 등록 칸: registered_via = proxy → `경영관리 등록` + 2행 등록자 · 날짜
  </behavior>
  <action>
`listProjectCardUsages(viewer, projectId)` — 금액 칸은 `quote.amount` 정보 항목으로 가르고(서버가 칸을 뺀다), 부제의 합은 서버 `sumKrw`. DTO는 `registerDto`(모듈 import는 06-05가 누수 스캔에 이미 넣었다 — verify가 누수 스캔을 돌린다). `card-usage-section.tsx`(§6-2 섹션, 읽기 표): 열 사용일 · 견적 줄 · 가맹점 · 결제 합계 2행 공급가 · 등록, Empty 한 줄(카드가 있는 사람에게만 3차 `카드 사용 등록`), 섹션 로드 실패는 섹션만 한 줄 + `다시 시도`, 페이지 나눔 없음(§7-3 (자)). 프로젝트 상세 `page.tsx`에 매출 섹션 뒤에 붙인다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. leak-scan이 빨가면 새 DTO가 계급별 노출 규칙을 어겼다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. revenue-section이 빨가면 상세 화면 섹션 배치를 깨뜨렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 `quote.amount` 없는 계급의 `listProjectCardUsages` 결과 객체에 금액 키가 없음을 단언한다
    - E2E 「상세에서 카드 섹션 행 + 부제 합」 · 「`quote.amount` 없는 계정은 금액 열 없음」 녹색
  </acceptance_criteria>
  <done>카드 사용이 목록·폼·연결 고르기·프로젝트 상세 네 자리에서 계약대로 보이고, 금액 노출이 서버 DTO에서 갈린다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 카드 사용 액션 | 연결(견적 줄 id · 프로젝트 id · 항목)이 넘어온다 |
| domain → quote_lines | 카드 모듈이 견적 줄을 잠그고, 견적 줄 모듈에 줄 생성을 부탁한다(O-8) — 같은 트랜잭션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-31 | Tampering | 이중 연결 우회(경합 포함) | high | mitigate | `lockQuoteLines` `FOR UPDATE` → `findLineLinks` → `card.dual-link-block` → INSERT 한 트랜잭션(B-1). 화면 비활성은 보조. 지출결의 쪽 입구 · 병렬 테스트는 06-13 |
| T-06-32 | Denial of Service | 잠금 교착 | medium | mitigate | 줄 id 오름차순으로만 잠근다 — 모든 호출자가 같은 빌더(`lockQuoteLinesQuery`)를 쓰고 그 SQL의 `order by` · `for update`를 `.toSQL()` 단위 테스트가 고정한다(CROSS E-5) · 전역 순서 프로젝트 → 견적 줄 → 문서 행(N-3 — 줄 잠금 뒤 프로젝트 행 잠금 없음) |
| T-06-153 | Denial of Service | 카드 사용 · 견적 외 비용 줄 트랜잭션의 풀 고갈 교착 | high | mitigate | 견적 줄 잠금을 쥔 `runCreate` 안에서 전역 `db`(권한 · 설정 · 프로젝트 · 차수 · 사용자 정의 칸) 호출 없음 — `precheckCardUsage`가 트랜잭션 전에 끝낸다, `createOutOfQuoteLine`은 `saveQuoteLines`를 열린 tx로 부르지 않는다(06-03 tx 규약 · audit B행 · CROSS E-3). region grep 두 개 |
| T-06-33 | Tampering | 완료 프로젝트에 견적 외 비용 줄 | medium | mitigate | `precheckCardUsage`가 트랜잭션 전에 기존 `project.completed-lock` 판정을 부른다(D-47 — 기존 `saveQuoteLines` 단독 경로와 같은 판정 시점) · 완료 프로젝트 거부 통합 케이스 |
| T-06-34 | Information Disclosure | S15 · 연결 고르기 DTO | medium | mitigate | 금액 칸을 `quote.amount`로 가르고 `registerDto` → 누수 스캔 |
</threat_model>

<verification>
- 단위 rules-card-dual-link · quote-line-links-sql(CROSS E-5 `.toSQL()`) · leak-scan-coverage, 통합 corp-card-usages · quote-lines · leak-scan 녹색
- `runCreate` · `createOutOfQuoteLine` 범위에 전역 `db` 판정·조회 호출 0건(06-03 tx 규약 — Task 1 · Task 2 acceptance)
- E2E card-usage · quote-table · revenue-section이 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S9(연결 셋) · S10 · S15 DOM 실측 감사(backstop 5건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 카드 사용이 견적 줄 · 견적 외 비용 · 팀 비용 셋 중 하나로 이어진다
- 카드 쪽 입구가 잠금 뒤 게이트로 닫힌다(지출결의 쪽 입구는 06-13)
- 프로젝트 상세에서 카드 사용이 보이고 금액 노출이 서버에서 갈린다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 게이트 규칙 | `card.dual-link-block`(ctx `{ side, links }` — 두 방향 판정) |
| 함수(repo) | `lockQuoteLinesQuery`(빌더) · `lockQuoteLines`(`ORDER BY id FOR UPDATE`) · `findLineLinks` · 견적 외 비용 끝 순서 조회(tx) |
| 함수(domain) | `createOutOfQuoteLine`(tx 리포지토리만) · `precheckCardUsage` 견적 줄 · 견적 외 비용 갈래 · `listProjectCardUsages` · 연결 고르기 목록 DTO 함수 |
| Server Action | 연결 고르기 목록 액션 |
| 화면 | `app/(app)/cards/link-picker.tsx`(S10) · `app/(app)/projects/[id]/card-usage-section.tsx`(S15) |
| 테스트 | `test/unit/domain/rules-card-dual-link.test.ts` · `test/unit/repositories/quote-line-links-sql.test.ts` · `test/integration/corp-card-usages.test.ts` · card-usage E2E 케이스 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-07-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-5 — `lockQuoteLines([3,1,2])`의 잠금 순서 단위 테스트는 결과 행으로 관찰되지 않아 RED를 만들 수 없다(eng-cross-opus.md L30-32@333f7bf) | 고침 | Plan 07, Task 1 ① · behavior 셋째 줄: 쿼리 빌더 `lockQuoteLinesQuery`를 드러내고 `drizzle.mock()`의 `.toSQL().sql`에 `order by "quote_lines"."id"` · `for update`를 단언한다(drizzle-orm 0.45.2 출력 모양은 계획 단계에서 실행해 확인). 새 파일 `test/unit/repositories/quote-line-links-sql.test.ts` · verify · fails_when(`orderBy`를 지우면 빨간 것을 확인) · acceptance · T-06-32 |
| CROSS E-3 — `createCardUsage`의 tx 경계 미정 · 트랜잭션 안 전역 `db` 조회(L22-24@333f7bf) | 고침 | Plan 07, Task 1 ③: 견적 줄 갈래를 06-05의 `precheckCardUsage`(완료 잠금 판정 포함) → `runCreate`(잠금 · 연결 · 게이트 · INSERT만)로 나눈다. Task 2: `createOutOfQuoteLine`은 `saveQuoteLines`를 열린 tx로 부르지 않고 tx 리포지토리만(audit B행이 가리키는 바로 그 꼴을 새로 만들지 않는다), 판정은 `pre.outOfQuote`. 열린 선택 B-1 잠금 순서 줄 · must_haves.truths · acceptance region grep 두 개 · T-06-153 · T-06-33 |
| CROSS R-2 — 카드 세율 기준일(L36@333f7bf; 06-05가 규칙을 정하고 여기서 DB로 증명) | 참고 | Plan 07, Task 2 behavior 「세율 기준일 = 사용일」 통합 케이스 + fails_when |
| ENG E-3 — tx 규약 참조(eng-review.md L72-74@333f7bf) | 고침 | must_haves.truths에 「06-03 tx 규약」 참조 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| CROSS E-5 대안 ⒝ — 06-13 `dual-link-concurrency`에 역순 id 두 요청 병렬 케이스 추가 | 채택 안 함 — ⒜ `.toSQL()` 단언이 이 플랜 안에서 RED를 만들 수 있어 충분하다. 06-13은 이 샤드의 편집 범위 밖이고 거기의 기존 병렬 케이스는 그대로 둔다 |
