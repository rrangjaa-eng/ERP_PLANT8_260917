---
phase: 06-payment-evidence-cards
plan: 07
type: execute
wave: 5
depends_on: ["06-02", "06-03", "06-06"]
files_modified:
  - domain/quotes/line-status.ts
  - domain/quotes/lines.ts
  - repositories/quote-line-links.ts
  - domain/rules/register.ts
  - domain/expenses/index.ts
  - app/(app)/projects/[id]/quote-table.tsx
  - test/unit/domain/quote-line-status.test.ts
  - test/unit/domain/rules-line-paid-lock.test.ts
  - test/integration/quote-line-links.test.ts
  - test/e2e/quote-line-status.spec.ts
autonomous: true
requirements: [EXP-06, EXP-07]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "견적 줄 표의 상태 열은 서버가 줄마다 파생한 한 값이다 — 한 줄에 문서가 여럿이면 우선순위 취소 > 반려 > 증빙 없음 > 지출결의 중 > 구매 요청 > 지급 완료 > 카드 사용 > 미착수의 첫 값 하나만 보낸다(SP-2, O-14)"
    - "[O-14 기본값] 결재 중인 지출결의라도 증빙이 없으면(면제 제외, 선결제 포함) `지출결의 중`이 아니라 `증빙 없음`이 선다 — 우선순위 표는 `domain/quotes/line-status.ts` 한 곳에 있다"
    - "선결제 기한이 지난 줄은 상태 2행에 `증빙 {N}일 경과`(`--fs-xs` 400 `--warning`)가 서고, 면제된 문서의 줄에는 기한 2행이 없다"
    - "지출결의 쪽 문서가 전부 지급 완료인 줄에 새 지출결의를 올리려 하면 `rules.gate`의 `expense.line-paid-lock`이 `지급 완료 {번호} · 새 지출결의 없음`으로 거부하고, 화면의 행 행동도 같은 이유로 비활성이다(EXP-06 — 이중 지급 방지)"
    - "지급 완료된 지출결의가 이어진 줄의 금액 셀 읽기 전용 이유가 `지출결의 {번호} 지급 완료 · 고치려면 새 차수`(D-66 꼴)로 바뀐다"
    - "카드 사용이 이어진 줄에 지출결의를 제출하면 Phase 5 제출 경로에서 `card.dual-link-block`(지출결의 쪽)이 `카드 사용 {N}건 연결됨 · 지출결의는 다른 줄` 꼴로 거부한다 — 06-03의 카드 쪽 입구와 합쳐 한 줄이 양쪽에 동시에 잡히지 않는다(EXP-07, D-609)"
    - "온라인구매 줄에 지출결의를 제출하면 Phase 5 제출 경로에서 `purchase.line-door`(지출결의 쪽)가 거부한다 — 문은 사람이 고르지 않는다(EXP-10 입구의 반대편)"
    - "견적 줄 표의 행 행동(Phase 5가 「지출결의 올리기」를 둔 자리, UA-608)은 온라인구매 줄이면 `구매 요청`(→ `/cards/purchases?new=1` + 그 줄), 그 밖이면 `지출결의 올리기`만 렌더되고, `Ctrl+E`는 현재 줄의 열린 문이다"
    - "힌트 줄은 그 프로젝트에 온라인구매 줄이 하나라도 있으면 `지출결의·구매 요청 Ctrl+E`, 없으면 Phase 5 그대로다"
    - "S14 empty: 연결 문서 0인 줄의 상태 = `미착수`(`muted`)"
    - "S14 error: 서버 게이트 거부는 화면 막힘과 같은 문구다(「막힘 — 견적 줄 표 행 행동」, RESEARCH Pitfall 3)"
    - "S14 populated: 상태 열 = Color 매핑표 견적 줄 값(`text` 변형 `--fs-xs` 600) + 2행 `증빙 {N}일 경과`, 온라인구매 줄의 행 행동 = `구매 요청`, 그 밖 = `지출결의 올리기`, 힌트 줄 `지출결의·구매 요청 Ctrl+E`"
    - "S14 partial: 한쪽 연결(D-609)이면 반대쪽 행 행동은 렌더 + 비활성 + 이유 + 다음 한 수 3차(`카드 사용 등록`)이고, 지급 완료 줄은 새 지출결의 막힘 + D-66 읽기 전용 이유다"
    - "S14 zero-one-many: 한 줄에 문서가 여럿이면 우선순위 첫 값 하나만(서버 파생)"
    - statement: "S14 loading: 행 행동(`구매 요청` / `지출결의 올리기`)은 폼 이동이라 진행 표시가 §7-1 규칙 그대로다 — Phase 5 행 행동 자리(UA-608) 확정 뒤 CI=true DOM 감사"
      verification: backstop
    - statement: "S14 overflow: `지출결의 중`(5자) 등 긴 상태 낱말이 좁은 PC·폰 P1 상태 칸에서 잘리지 않는다(SP-2 — 4자 규칙은 `tag`만) — CI=true DOM 감사"
      verification: backstop
    - statement: "S14 long-text: 막힘 이유 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄` + 3차가 행 행동 자리에서 넘치지 않는다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "지급 완료된 견적 줄에 새 지출결의가 올라갈 수 있어서는 안 된다 — 화면 비활성만이 아니라 Phase 5 제출 경로의 서버 게이트가 거부한다(EXP-06)"
      status: resolved
      verification: test
    - statement: "견적 줄 상태를 클라이언트가 문서 목록을 보고 추론해서는 안 된다 — 파생은 서버 한 함수이고 화면은 받은 값만 그린다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/quotes/line-status.ts"
      provides: "[O-14] 견적 줄 상태 파생 한 함수 + 우선순위 표"
      exports: ["deriveQuoteLineStatus", "QUOTE_LINE_STATUS_PRIORITY"]
    - path: "test/integration/quote-line-links.test.ts"
      provides: "지출결의 쪽 세 게이트(지급 잠금 · 이중 연결 · 문) 증명"
      min_lines: 40
  key_links:
    - from: "domain/expenses/index.ts"
      to: "domain/rules/gate.ts"
      via: "Phase 5 제출이 지급 잠금·이중 연결·문 세 게이트를 한 지점에서 받는다"
      pattern: "expense.line-paid-lock"
    - from: "domain/quotes/lines.ts"
      to: "domain/quotes/line-status.ts"
      via: "견적 줄 DTO의 상태가 파생 함수 한 곳에서 나온다"
      pattern: "deriveQuoteLineStatus"
    - from: "domain/quotes/line-status.ts"
      to: "repositories/quote-line-links.ts"
      via: "줄마다 연결 문서(지출결의·지급·증빙·카드·구매 요청)를 한 번의 조회로 받는다"
      pattern: "findLineLinks"
---

<objective>
「한 견적 줄에 한 번만」의 **지출결의 쪽 입구**와 견적 줄 표의 표시를 닫는다 — 줄 상태 파생(SP-2 · O-14), 지급 완료 줄 잠금(EXP-06), Phase 5 제출 경로의 이중 연결·문 게이트, 견적 줄 표 행 행동의 문 가르기(S14).

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 06-03(카드 쪽) · 06-06(구매 요청 쪽)이 각자 입구에서 반대쪽을 막았다. 지출결의 쪽 입구(Phase 5 제출)가 같은 규칙을 부르지 않으면 한쪽 문이 열려 있다. 화면은 그 결과를 PM이 줄마다 한눈에 보게 한다.

Output: `domain/quotes/line-status.ts`, 게이트 `expense.line-paid-lock`, Phase 5 제출 훅, 견적 줄 표 상태 열·행 행동, 단위 2 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-06-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-07 착수하지 않음`을 출력하고 멈춘다.**

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-602 | Phase 4의 견적 줄 상태(`line_status` — main은 미착수·취소 둘, D-64)와 D-66 셀 읽기 전용 이유 판정 자리(`domain/quotes/lines.ts`). Phase 4가 파생 함수를 이미 만들었으면 그것을 확장하고, 없으면 이 플랜이 `line-status.ts`를 만든다 | `grep -n "lineStatus\|editab\|readOnlyReason" domain/quotes/lines.ts` 결과를 SUMMARY에 적는다 — `lineStatus` 자체가 없으면 멈춤 |
| A-607 | Phase 5 지출결의 제출 함수(가칭 `submitExpense`) · 지출결의 상태(결재 중·반려) · 견적 줄 연결 칸 | `grep -rn "export async function submit" domain/expenses/` 0건이면 멈춤 |
| UA-608 | Phase 5가 견적 줄 표에 둔 행 행동 「지출결의 올리기」(`Ctrl+E`)와 폰 행 시트의 같은 행동 | `grep -n "지출결의 올리기" 'app/(app)/projects/[id]/quote-table.tsx'` 0건이면 멈춤 |
| UA-609 | 견적 줄 상태 열이 `StatusTag` `text` 변형이다 | `grep -n "StatusTag" 'app/(app)/projects/[id]/quote-table.tsx'` 결과를 SUMMARY에 적는다 |
| 06-02 · 06-03 · 06-06 | `prepaidDueInfo` · `card.dual-link-block` · `purchase.line-door` · `findLineLinks`(구매 요청 포함) | `grep -c "card.dual-link-block\|purchase.line-door" domain/rules/register.ts` 2 미만이면 멈춤 |

**표면화하는 가정(⓪에서 확인):** 05-CONTEXT는 분할 지급을 회차마다 별도 지출결의로 올린다고 적었다. 「지출결의 쪽 문서가 **전부** 지급 완료인 줄은 새 지출결의 막힘」을 그대로 적용하면 첫 회차 지급 뒤에 둘째 회차를 새로 올리는 흐름이 막힌다. ⓪에서 Phase 5의 실제 분할 흐름(회차 문서를 미리 모두 올리는지)을 확인하고, 막히는 흐름이면 **코드를 바꾸지 않고 멈춰** 사용자에게 묻는다(선택지: ⑴ 그대로 막는다 — 회차는 첫 지급 전에 모두 올린다 ⑵ 회차 표시가 있는 지출결의는 잠금에서 뺀다).

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-14 | 결재 중이어도 증빙 없음이 이긴다 | `domain/quotes/line-status.ts`의 `QUOTE_LINE_STATUS_PRIORITY` 한 배열 + 그 단위 테스트 |
| O-2 | (06-01 소관) 이 플랜의 파생은 O-2와 무관하다 | 해당 없음 |

<probe_fallback>
coverage.json의 이 플랜 몫: **EXP-06 — unclassified → unresolved (flagged)**, **EXP-07 — unclassified → unresolved (flagged)**. 자동 해소·기각 없음. 대신 쓰는 근거: 기준 1(지급 줄 잠금) · 기준 3(이중 연결) · D-609 · O-14. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 지급 완료된 줄 하나가 견적 줄 표에 「지급 완료」로 서고, 그 줄에 새 지출결의 제출이 서버에서 막힌다</name>
  <files>domain/quotes/line-status.ts, domain/quotes/lines.ts, repositories/quote-line-links.ts, domain/rules/register.ts, domain/expenses/index.ts, app/(app)/projects/[id]/quote-table.tsx, test/unit/domain/rules-line-paid-lock.test.ts, test/integration/quote-line-links.test.ts, test/e2e/quote-line-status.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 분할 지급 가정이 확인됐다 — ⓪이 확인하고 아니면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 693-703행(S14 정본) · Color 절 견적 줄 매핑표 · Copywriting 「막힘 — 견적 줄 표 행 행동」
    - .planning/phases/06-payment-evidence-cards/06-RESEARCH.md Pitfall 3(화면 막힘과 서버 거부 문구 일치)
    - domain/quotes/lines.ts(`QuoteLineDto` · `QUOTE_LINE_DTO_SPEC` · 셀 편집 가능성 판정) · app/(app)/projects/[id]/quote-table.tsx
    - repositories/quote-line-links.ts(06-03·06-06) · domain/rules/register.ts
    - A-607 재확인에서 찾은 Phase 5 제출 함수
  </read_first>
  <behavior>
    - `expense.line-paid-lock`: 줄의 살아 있는 지출결의 쪽 문서가 1개 이상이고 전부 지급 완료 → 막힘 `지급 완료 {번호} · 새 지출결의 없음` / 하나라도 지급 전 → 통과 / 0개 → 통과 / 지급 취소된 문서는 지급 전으로 센다
    - Phase 5 제출: 잠긴 줄 → 거부(게이트 이유 그대로) / 정상 줄 → 기존 동작 불변
    - 파생: 지급 완료 문서만 있는 줄 → `지급 완료`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인과 분할 지급 가정 확인. 아니면 멈춘다.

`repositories/quote-line-links.ts`의 `findLineLinks`가 줄마다 지출결의(상태 · 번호 · 지급 기록 유무 · 증빙 판정 입력) · 카드 사용 · 구매 요청을 **한 번의 조회**로 돌려주게 넓힌다(줄 수만큼 쿼리를 돌지 않는다). `domain/rules/register.ts`에 `expense.line-paid-lock`을 등록한다. Phase 5 제출 함수(실제 이름)가 문서 저장 전에 같은 트랜잭션에서 `gate(…, "expense.line-paid-lock")`을 부르게 한다. `domain/quotes/line-status.ts`의 `deriveQuoteLineStatus`는 이 태스크에서 `지급 완료` · `미착수` · `취소`(Phase 4 줄 상태) 갈래만 만들고, `domain/quotes/lines.ts`의 줄 DTO에 파생 상태를 싣는다. 금액 셀 읽기 전용 이유(D-66)를 지급 완료 문서면 `지출결의 {번호} 지급 완료 · 고치려면 새 차수`로 바꾼다. `quote-table.tsx` 상태 열은 받은 값을 `status-display.ts` 매핑으로 `StatusTag` `text` 변형으로만 그린다 — 클라이언트 추론 없음. 잠긴 줄의 행 행동은 비활성 + 이유(게이트와 같은 문구).

통합 테스트 `test/integration/quote-line-links.test.ts`에 behavior 갈래, E2E `test/e2e/quote-line-status.spec.ts`에 「지급 완료 → 상세 견적 표 상태 `지급 완료` + 행 행동 비활성 이유」 한 경로.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/rules-line-paid-lock.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-links.test.ts test/integration/quote-lines.test.ts test/integration/quote-line-visibility.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. quote-lines·visibility가 빨가면 DTO 확장이 기존 노출 규칙을 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/quote-line-status.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-602 판정 자리 · 분할 지급 흐름)가 SUMMARY에 있다
    - 통합 테스트가 「잠긴 줄에 Phase 5 제출 함수를 직접 불러도 거부」와 「지급 취소 뒤 같은 줄 제출 성공」을 단언한다
    - `findLineLinks`가 줄 50개 프로젝트에서 쿼리 수가 줄 수에 비례하지 않음을 테스트가 확인한다(쿼리 수 계측 또는 단일 호출 단언)
  </acceptance_criteria>
  <done>지급 완료 줄 하나가 서버 파생 → 표 표시 → Phase 5 제출 거부까지 한 규칙으로 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [O-14 기본값] 파생 우선순위 전부 · 증빙 경과 2행 · 지출결의 쪽 이중 연결·문 게이트</name>
  <files>domain/quotes/line-status.ts, domain/quotes/lines.ts, domain/expenses/index.ts, test/unit/domain/quote-line-status.test.ts, test/integration/quote-line-links.test.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 868-874행(SP-2 우선순위) · 1019-1026행(S14 considerations)
    - /mnt/project-files/06-prep/open-choices.md 23행(O-14)
    - domain/evidence-reviews/prepaid.ts · domain/evidence-reviews/index.ts(`resolveEvidenceStatus`)
  </read_first>
  <behavior>
    - 우선순위: 취소 > 반려 > 증빙 없음 > 지출결의 중 > 구매 요청 > 지급 완료 > 카드 사용 > 미착수 — 각 쌍을 한 줄에 함께 두고 앞의 것이 이기는지
    - 결재 중 · 증빙 0 · 면제 아님 → `증빙 없음`(O-14) / 결재 중 · 면제 → `지출결의 중` / 선결제 · 증빙 0 → `증빙 없음`
    - 선결제 기한 경과 → 2행 `증빙 {N}일 경과`, 면제면 2행 없음
    - Phase 5 제출: 카드 사용 있는 줄 → `card.dual-link-block` 거부 / 온라인구매 줄 → `purchase.line-door` 거부
  </behavior>
  <action>
`domain/quotes/line-status.ts`에 배열 `QUOTE_LINE_STATUS_PRIORITY`(O-14 기본값 순서)와 파생 전 갈래를 채운다. 파일 머리 한 줄 주석에 "O-14 답이 바뀌면 이 배열만 고친다"를 적는다. 증빙 판정은 `resolveEvidenceStatus`, 기한은 `prepaidDueInfo`를 부른다(판정을 다시 쓰지 않는다). 줄 DTO에 상태 2행 글자(`증빙 {N}일 경과`)를 싣는다. Phase 5 제출 함수에 `card.dual-link-block`(지출결의 쪽) · `purchase.line-door`(지출결의 쪽)를 Task 1의 잠금 게이트 옆에 더한다 — 세 게이트는 같은 트랜잭션 안에서 문서 저장 전에 돈다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/quote-line-status.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-links.test.ts test/integration/corp-card-usages.test.ts test/integration/purchase-requests.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 단위 테스트가 우선순위 여덟 값의 인접 쌍 일곱 개와 O-14 갈래 셋을 개별 케이스로 단언한다
    - 통합 테스트가 「카드 사용 있는 줄에 지출결의 제출 거부」 · 「온라인구매 줄에 지출결의 제출 거부」를 Phase 5 제출 함수 직접 호출로 단언한다
  </acceptance_criteria>
  <done>줄 상태가 한 우선순위 배열에서 파생되고, 지출결의 쪽 입구가 카드·구매 요청 쪽과 같은 두 게이트를 부른다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 견적 줄 표 행 행동 문 가르기(S14) · 한쪽 연결 비활성 + 다음 한 수 · 힌트 줄</name>
  <files>domain/quotes/lines.ts, app/(app)/projects/[id]/quote-table.tsx, test/integration/quote-line-links.test.ts, test/e2e/quote-line-status.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 696-703행(문 가르기 · 한쪽 연결 · 카드 입구 없음)
    - app/(app)/projects/[id]/quote-table.tsx(UA-608 행 행동 자리 · 힌트 줄 · 폰 행 시트) · ui/table/RowSheet.tsx
  </read_first>
  <behavior>
    - 줄 DTO `door`: 온라인구매 줄 → purchase, 그 밖 → expense(서버 `resolveLineDoor`)
    - 줄 DTO `rowActionBlock`: 카드 쪽 줄의 지출결의 올리기 → 이유 + 다음 한 수 `카드 사용 등록` / 지출결의 쪽 줄의 구매 요청 → 이유 / 잠긴 줄 → 잠금 이유 / 없음 → null
    - 힌트 줄: 온라인구매 줄 ≥1 → `지출결의·구매 요청 Ctrl+E`, 0 → Phase 5 문구 그대로
  </behavior>
  <action>
`domain/quotes/lines.ts`의 줄 DTO에 `door`(`resolveLineDoor`)와 `rowActionBlock`(게이트 셋과 같은 판정 함수를 불러 이유 문자열을 그대로 싣는다 — 화면 문구와 서버 거부 문구가 한 출처)을 더한다. `quote-table.tsx`: Phase 5 행 행동 자리에서 `door`가 purchase면 `구매 요청`(→ `/cards/purchases?new=1` + 그 줄의 `line` 쿼리)만, 그 밖이면 `지출결의 올리기`만 렌더한다. `rowActionBlock`이 있으면 렌더 + 비활성 + 이유 + 다음 한 수 3차(`카드 사용 등록` → `/cards?new=1`). `Ctrl+E`는 현재 줄의 열린 문. 힌트 줄 문구는 서버가 보낸 「온라인구매 줄 있음」 값으로 가른다. 폰 행 시트의 같은 행동도 같은 값으로. 카드 사용 등록의 입구는 행 행동으로 두지 않는다(UI-SPEC 703행).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/quote-line-links.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/quote-line-status.spec.ts test/e2e/quote-table.spec.ts test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 ⑴ 온라인구매 줄에 `지출결의 올리기` 버튼 요소 0개 · `구매 요청` 1개 ⑵ 카드 쪽 줄의 `지출결의 올리기`가 `aria-disabled` + 이유 + `카드 사용 등록` 3차 ⑶ 힌트 줄 문구를 단언한다
    - 통합 테스트가 `rowActionBlock` 이유 문자열 = 같은 줄에 대한 서버 게이트 거부 이유 문자열임을 단언한다
  </acceptance_criteria>
  <done>견적 줄 표가 줄마다 열린 문 하나와 막힌 이유를 서버가 정한 그대로 보여 주고, 화면 막힘과 서버 거부 문구가 한 출처다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 제출 경로 → domain/rules | 지출결의 쪽 입구가 세 게이트를 부른다 — 화면 비활성과 무관하게 서버가 막는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-37 | Tampering | 지급 완료 줄에 새 지출결의(이중 지급) | high | mitigate | `expense.line-paid-lock`을 Phase 5 제출 domain 함수 안에서 부른다. 제출 함수 직접 호출 거부 통합 테스트 |
| T-06-38 | Tampering | 카드 쪽 줄에 지출결의(이중 계산) | high | mitigate | `card.dual-link-block`(지출결의 쪽) 같은 트랜잭션 호출 · 통합 테스트 |
| T-06-39 | Tampering | 온라인구매 줄에 지출결의(문 우회) | medium | mitigate | `purchase.line-door`(지출결의 쪽) · 통합 테스트 |
| T-06-40 | Information Disclosure | 줄 DTO의 이유 문자열(문서 번호) | low | mitigate | 이유 문자열은 이미 그 줄을 볼 수 있는 사람에게만 가는 줄 DTO 안이고, 기존 `quote-line-visibility` 통합 테스트가 녹색이어야 한다 |
| T-06-41 | Denial of Service | 줄마다 연결 조회(N+1) | low | mitigate | `findLineLinks` 단일 조회 + 쿼리 수 테스트 |
</threat_model>

<verification>
- 단위 2 · 통합 1(+ quote-lines · visibility · card · purchase · leak-scan) · E2E 1(+ quote-table · purchase-requests) 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml·tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S14 DOM 실측 감사(backstop 3건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 한 견적 줄이 지출결의·카드·구매 요청 중 한 쪽에만 잡히는 것이 세 입구 모두에서 서버로 보장된다
- 지급 완료된 줄에 새 지출결의가 올라가지 않는다
- 견적 줄 표의 상태·행 행동·막힘 이유가 서버 한 출처에서 나온다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 게이트 규칙 | `expense.line-paid-lock` |
| 함수(domain) | `deriveQuoteLineStatus` · `QUOTE_LINE_STATUS_PRIORITY` |
| 변경된 기존 심볼 | `QuoteLineDto`(파생 상태 · 상태 2행 · `door` · `rowActionBlock`) · D-66 읽기 전용 이유 · `findLineLinks`(단일 조회로 확장) · Phase 5 제출 함수(게이트 셋) |
| 화면 | `app/(app)/projects/[id]/quote-table.tsx` 상태 열 · 행 행동 문 가르기 · 힌트 줄 |
| 테스트 | `test/unit/domain/{quote-line-status,rules-line-paid-lock}.test.ts` · `test/integration/quote-line-links.test.ts` · `test/e2e/quote-line-status.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-07-SUMMARY.md` when done
</output>
