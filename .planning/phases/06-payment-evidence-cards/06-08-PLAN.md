---
phase: 06-payment-evidence-cards
plan: 08
type: execute
wave: 5
depends_on: ["06-07"]
files_modified:
  - db/schema/purchase-requests.ts
  - db/schema/index.ts
  - db/migrations/meta/_journal.json
  - domain/purchase-requests/index.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - repositories/purchase-requests.ts
  - app/(app)/cards/purchases/page.tsx
  - app/(app)/cards/purchases/purchase-request-form.tsx
  - app/(app)/cards/purchases/actions.ts
  - app/(app)/cards/purchases/actions.registry.ts
  - test/integration/purchase-requests.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/purchase-requests.spec.ts
autonomous: true
requirements: [EXP-10, EXP-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 온라인구매 견적 줄에서 `/cards/purchases?new=1&line={id}`로 들어와 품목·링크·예상 금액을 적고 1차 「구매 요청」을 누르면 저장 순간 번호(`{프로젝트 번호}-C{순번 4자리}`, 예 `26001-C0001`)가 매겨지고 토스트 `구매 요청 · {번호}`와 목록 첫 줄에 보인다 — 결재는 없다(EXP-10, A-612)"
    - "번호 부여는 요청 INSERT와 같은 트랜잭션의 카운터 증가(06-02 `allocateScopedDocumentNumber`)로 하고, 트랜잭션이 실패하면 카운터 증가도 요청 INSERT와 함께 롤백돼 결번이 생기지 않고 다음 요청이 그 번호를 이어받는다(D-42 — `test/integration/document-counters-concurrency.test.ts` 「실패한 트랜잭션의 증가는 문서 INSERT와 함께 롤백된다」) — 동시 두 요청은 서로 다른 번호를 받고 `number`는 `uniqueIndex`가 최후 방어선이다"
    - "번호 서식은 트랜잭션을 열기 **전에** `loadDocumentNumberFormat(\"purchase_request\")`로 읽어 `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)`에 넘긴다 — 같은 온라인구매 줄에 **동시 6건**(`Promise.all`, `DB_POOL_MAX` 기본 5 초과)을 넣어도 타임아웃 없이 모두 끝나고 서로 다른 번호 6개를 받는다(CROSS E-2, PR #75 꼴)"
    - "「06-03 tx 규약」을 따른다 — 구매 요청 생성의 트랜잭션 몸통 `runCreate`는 `tx`를 받는 리포지토리 · 순수 함수 · 게이트만 부르고, 설정(`purchase.online_vendor_name` · 번호 서식)과 권한 판정은 트랜잭션 전에 읽는다. 행동 로그는 커밋 뒤"
    - "구매 요청은 견적 줄의 거래처가 설정 `purchase.online_vendor_name`과 맞는 **온라인구매 줄**에만 이을 수 있고, 그 밖의 줄에 이으려는 요청은 `gate(doc, \"purchase.line-door\", { side: \"purchase\", door, vendorName })`가 `거래처 {이름} · 지출결의로`로 거부한다 — 사람이 문을 고르지 않는다(EXP-10, O-13, D-611의 사람이 고르지 않는 문)"
    - "**B-1:** `createPurchaseRequest`(견적 줄 갈래)는 한 트랜잭션에서 ⑴ `lockQuoteLines` ⑵ `findLineLinks` ⑶ `purchase.line-door` ⑷ `card.dual-link-block`(`side: \"card\"` — 구매 요청은 카드로 나가는 돈이다) ⑸ 번호 ⑹ INSERT 순서로 돈다 — 지출결의가 이미 이어진 온라인구매 줄에는 구매 요청이 서지 않는다(D-609)"
    - "요청의 링크는 `http://`·`https://`만 저장되고(zod + DB CHECK), 목록의 링크 아이콘은 새 탭 `rel=\"noopener noreferrer\"` · `aria-label=\"{품목} 링크 열기\"` · `title` = URL이다"
    - "구매 요청 목록 범위: 요청자 본인 요청 + 자기가 담당 PM인 프로젝트 줄의 요청 / `cards.purchases` write 권한자 · 전사 범위(대표) = 전부 — 리포지토리 쿼리에서 거른다"
    - "행동 로그: 구매 요청 = `document_create`"
    - "S11 populated: 열(번호 · 요청일 · 품목 + 링크 아이콘 · 연결 · 요청자 · 예상 금액 · 상태 + 행동 칸) · 기본 보기 `신청` — 구매 완료 · 취소 행과 합계 줄은 06-12 · 06-14"
    - "S12 empty: `line` 쿼리로 들어오면 연결이 텍스트(프로젝트 · 견적 줄)다. 빈 칸 = `품목 · 예상 금액 2칸 비어 있음 · 품목 적기` — `?new=1`만의 팀 비용 기본 선택은 06-14"
    - "S12 error: 「Error — 구매 요청 폼 칸」 · 「막힘 — 구매 요청 폼」(서버 문 가르기 거부 · 반대쪽 지출결의 연결 거부)"
    - statement: "S11 long-text: 품목은 한 줄 말줄임 + `title`, 링크는 아이콘 3차라 URL 길이가 칸을 밀지 않는다 — 긴 품목 이름으로 CI=true DOM 감사"
      verification: backstop
    - statement: "S12 long-text: 긴 쇼핑몰 URL이 링크 칸 480에서 `overflow-wrap: anywhere`로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S20 partial: `온라인구매 협력사` 문자열이 어느 거래처와도 맞지 않을 때 설정 화면은 추가 표시 없이 §7-2 기본 모양 그대로이고(UA-614), 그 결과는 견적 줄 표의 문이 전부 `지출결의 올리기`인 것으로 드러난다(계획 판단) — 맞지 않는 문자열을 넣어 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "사람이 견적 줄의 문(구매 요청 / 지출결의)을 고를 수 있어서는 안 된다 — 문은 거래처 설정에서만 나오고 서버 게이트가 반대 문을 거부한다(EXP-10). 이 플랜은 구매 요청 쪽 입구를 닫고, 지출결의 쪽 입구는 06-13이 같은 규칙으로 닫는다"
      status: resolved
      verification: test
    - statement: "구매 요청의 링크에 `http`·`https` 밖의 스킴이 저장되거나 새 탭이 여는 쪽 창에 접근할 수 있어서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/purchase-requests.ts"
      provides: "구매 요청 표 — 번호(`uniqueIndex`) · 연결(견적 줄 XOR 팀 비용) · 요청자 · 품목 · 링크(스킴 CHECK) · 예상 금액 Money 묶음 · 메모 · 상태 3종 · 완료·취소 칸 · version"
      exports: ["purchaseRequests"]
    - path: "domain/purchase-requests/index.ts"
      provides: "구매 요청 신청 · 목록"
      exports: ["createPurchaseRequest", "listPurchaseRequests"]
    - path: "test/integration/purchase-requests.test.ts"
      provides: "문 가르기 · 반대쪽 연결 · 잠금 순서 · 「동시 6건 번호 경합」(CROSS E-2) · 링크 스킴 · 범위(06-12 · 06-14가 케이스를 더한다)"
      min_lines: 60
  key_links:
    - from: "domain/purchase-requests/index.ts"
      to: "domain/document-numbering/index.ts"
      via: "번호 부여를 요청 INSERT와 같은 트랜잭션에서 부른다 — 서식은 트랜잭션 전 `loadDocumentNumberFormat`으로 읽어 넘긴다(CROSS E-2)"
      pattern: "allocateScopedDocumentNumber"
    - from: "domain/rules/register.ts"
      to: "domain/quotes/line-door.ts"
      via: "`purchase.line-door` 게이트가 문 판정 함수를 부른다"
      pattern: "resolveLineDoor"
    - from: "domain/purchase-requests/index.ts"
      to: "repositories/quote-line-links.ts"
      via: "잠금 → 연결 조회 → 두 게이트 → 번호 → INSERT(B-1)"
      pattern: "lockQuoteLines"
---

<objective>
온라인 구매 요청(EXP-10)의 **신청 경로**를 세운다 — 온라인구매 견적 줄에 이은 요청이 문 판정 · 이중 연결 판정을 잠금 뒤에 지나 번호를 받고 목록에 선다. 구매 완료(카드 사용 생성)는 06-12, 팀 비용 요청 · 취소 · 외화 · 목록 마감은 06-14가 이 위에 더한다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 온라인 구매는 결재 없이 경영관리의 카드로 나가는 돈이다. 교차 검토 B-1대로 구매 요청 생성도 견적 줄을 잠근 뒤 `card.dual-link-block`을 불러야 「한 번만」이 구매 경로에서 성립한다. 옛 06-06(29파일 · 130k — 체커 BLOCKER)을 06-08 · 06-12 · 06-14로 나눴다.

Output: `purchase_requests` 표(신규 마이그레이션 1), 게이트 `purchase.line-door`, `domain/purchase-requests`(신청 · 목록), `/cards/purchases` 목록 · 폼, 통합 1 · E2E 1 · 누수 스캔 케이스.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-07-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-08 착수하지 않음`을 출력하고 멈춘다.** 이름은 가칭이다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-02 | `resolveLineDoor` · `allocateScopedDocumentNumber` · `document_number.purchase_request.*` · 팀 비용 번호 규칙 결정(06-02 SUMMARY) | `grep -c "allocateScopedDocumentNumber" domain/document-numbering/index.ts` 0이면 멈춤 |
| 06-07 | `lockQuoteLines` · `findLineLinks` · `card.dual-link-block` | `grep -c "lockQuoteLines" repositories/quote-line-links.ts` 0이면 멈춤 |
| 06-06 | 마이그레이션 저널에 06-06의 `evidence_reviews`가 먼저 있다 | `grep -c "evidence_reviews" db/migrations/meta/_journal.json` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-13 | 설정은 문자열 한 칸, 서버가 거래처 이름과 맞춘다(06-02 `resolveLineDoor`) | 해당 없음(판정 함수는 06-02) |
| O-1 | 구매 요청 목록 = 법인카드 메뉴의 하위 목록(SP-4) | 라우트 `app/(app)/cards/purchases/` |
| O-10 | 예상 금액 = 결제 합계(부가세 포함) 기준 — 차이 표시는 06-12 | 해당 없음 |

<assumption_delta_decision>
`detected: false`. 새 명사 「구매 요청」을 더한다(EXP-10 원문의 명사). 기존 명사를 옮기지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-10 · EXP-07 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 3 · A-612 · O-13 · D-609. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 온라인구매 견적 줄의 구매 요청 한 건이 잠금 → 두 게이트 → 번호 → 저장 → 목록 첫 줄까지 간다</name>
  <files>db/schema/purchase-requests.ts, db/schema/index.ts, db/migrations/meta/_journal.json, domain/purchase-requests/index.ts, domain/rules/register.ts, domain/settings/keys.ts, repositories/purchase-requests.ts, app/(app)/cards/purchases/page.tsx, app/(app)/cards/purchases/purchase-request-form.tsx, app/(app)/cards/purchases/actions.ts, app/(app)/cards/purchases/actions.registry.ts, test/integration/leak-scan.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S11. 구매 요청 목록」 · 「S12. 구매 요청 신청 폼」 · 「S13. 구매 완료 — 카드 사용 폼의 구매 완료 모드」 절(정본) · 「금액 표시 (서버 계산 값)」 절 「구매 요청 예상 금액」 행 · Copywriting 「막힘 — 구매 요청 폼」 · 「Error — 구매 요청 폼 칸」 · 「3차 버튼」 행(접근 이름)
    - domain/document-numbering/index.ts(06-02가 더한 범위 서식 · `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)` · export된 `loadDocumentNumberFormat`)
    - domain/quotes/line-door.ts · repositories/quote-line-links.ts · domain/rules/register.ts(`card.dual-link-block`)
    - 06-03-PLAN.md must_haves.truths의 「tx 규약」 줄 · domain/projects/ledger.ts 45-63행(`deferRecord` — 커밋 뒤 행동 로그) · `grep -L "DbOrTx" repositories/*.ts` · `grep -n "export async function" repositories/settings.ts repositories/projects.ts`(tx 인자 없는 리포지토리 목록을 저장소에서 다시 뽑는다 — `repositories/vendors.ts` · `findProjectById` · `settings.ts` 읽기 함수)
    - db/schema/money-columns.ts · db/migrations/0010_revenue_entries.sql 1-5행
    - test/unit/settings/registry-coverage.test.ts
  </read_first>
  <behavior>
    - `purchase.line-door` ctx `{ side, door, vendorName }`: side purchase · 문 expense → 막힘 `거래처 {이름} · 지출결의로` / side purchase · 문 purchase → 통과 / side expense · 문 purchase → 막힘(호출은 06-13)
    - `createPurchaseRequest`(견적 줄): 온라인구매 줄 · 반대쪽 연결 없음 → 저장 + 번호 + `document_create`
    - E2E: 설정에 온라인구매 협력사 → 그 거래처 견적 줄 → 직원이 `line` 쿼리로 구매 요청 → 토스트 번호 → 목록 첫 줄
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① **스키마(신규 표).** `purchase_requests`(id · number text · link_kind CHECK in (quote_line, team_cost) · project_id FK null · quote_line_id FK null · XOR CHECK · requested_by FK · item_name text · link_url text null + CHECK(스킴 http/https) · `moneyColumns("estimate")` · memo null · status CHECK in (requested, purchased, cancelled) · completed_by/at null · cancelled_by/at/reason null · version · source 'demo' · 생성 시각). `number`는 `uniqueIndex()`(B-2 — `.unique()` 아님). 카드 사용과의 연결은 카드 사용 쪽 칸(06-12)이 가진다 — 이 표에 카드 사용 id 칸을 두지 않는다. `db/schema/index.ts`에 export. **마이그레이션:** `pnpm db:generate --name purchase_requests` → SQL 머리 SET LOCAL 한 쌍 → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 `pnpm db:generate` `No schema changes`.

② **게이트.** `domain/rules/register.ts`에 `purchase.line-door`(R-8 ctx `{ side, door, vendorName }`, 두 방향 — 구매 요청 쪽과 지출결의 쪽)를 등록한다. 문은 06-02의 `resolveLineDoor`가 정한다. 이 플랜이 `purchase.online_vendor_name`을 처음 읽으므로 `domain/settings/keys.ts`의 그 키에서 readBy 표시를 지운다(registry-coverage가 강제).

③ **domain · 리포지토리(06-03 tx 규약 · CROSS E-2).** `repositories/purchase-requests.ts`(viewer 첫 인자 — 트랜잭션 안에서 부르는 함수는 전부 `tx: DbOrTx` 인자). `createPurchaseRequest(viewer, input)` 견적 줄 갈래를 두 부분으로 쓴다:
   - **트랜잭션 전:** 요청 권한 판정(`can` — 쓰는 경우) · 온라인구매 협력사 설정 `getSettingValue(PURCHASE_ONLINE_VENDOR_NAME)` · 번호 서식 `const format = await loadDocumentNumberFormat("purchase_request")`(06-02 — PR #75 꼴)를 읽는다. 이 셋은 전역 `db`로 읽으므로 트랜잭션 안에 두지 않는다.
   - **트랜잭션 몸통 `const runCreate = async (tx: DbOrTx) => { … };` → `withTransaction(runCreate)`:** `lockQuoteLines(…, tx)` → 잠근 줄의 프로젝트 번호 · 거래처 이름을 `repositories/purchase-requests.ts`의 tx를 받는 조회 한 번으로 읽는다(`repositories/vendors.ts` · `findProjectById`는 tx 인자가 없어 트랜잭션 안에서 부르지 않는다 — audit) → `findLineLinks(…, tx)` → `door = resolveLineDoor({ vendorName }, 트랜잭션 전에 읽은 설정 값)`(순수) → `gate(…, "purchase.line-door", { side: "purchase", door, vendorName })` → `gate(…, "card.dual-link-block", { side: "card", links })` → `allocateScopedDocumentNumber(viewer, { counterKey: "purchase_request", scope: 프로젝트 번호, format }, tx)` → INSERT(B-1 순서 고정).
   - **커밋 뒤:** `recordAction` `document_create`(트랜잭션 안에서 남기지 않는다 — `deferRecord` 꼴). 전역 잠금 순서(N-3)는 프로젝트 행 → 견적 줄(id 순) → 지출결의 · 카드 사용 · 구매 요청 문서 행이다 — 이 갈래는 줄을 잡은 뒤 새 요청 행을 INSERT만 하고, 줄 잠금 뒤에 프로젝트 행이나 기존 문서 행을 잠그지 않는다. 링크는 zod에서 http/https만. `listPurchaseRequests(viewer, filters)` DTO `registerDto`.

④ **액션 · 화면.** `app/(app)/cards/purchases/actions.ts`(`createPurchaseRequestAction`) · `actions.registry.ts`(신규) → `test/integration/leak-scan.test.ts`에 이 registry와 `@/domain/purchase-requests` import 두 줄. `/cards/purchases?new=1&line={id}` 폼(연결 텍스트 · 품목 · 링크 칸 placeholder `https://` · 예상 금액 KRW · 메모) · `/cards/purchases` 목록 기본 보기 `신청`(열 일곱 + 링크 아이콘 — http/https일 때만 렌더, 새 탭 `rel="noopener noreferrer"`, `aria-label` `{품목} 링크 열기`). 토큰은 `tokens.css`만, 상태 글자는 `statusDisplay`.

⑤ **E2E.** `test/e2e/purchase-requests.spec.ts`에 behavior 셋째 줄.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/settings/registry-coverage.test.ts test/unit/leak-scan-coverage.test.ts test/unit/domain/line-door.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — registry-coverage가 빨가면 `purchase.online_vendor_name`의 readBy를 지우지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 `Found N issues`를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - `grep -c "purchase.line-door" domain/rules/register.ts`가 1 이상
    - `grep -c "SET LOCAL lock_timeout" db/migrations/*purchase_requests*.sql`이 1 · `grep -c "uniqueIndex" db/schema/purchase-requests.ts`가 1 이상
    - (CROSS E-2 · 06-03 tx 규약) `awk '/const runCreate = async/,/^  };$/' domain/purchase-requests/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadDocumentNumberFormat\(|recordAction\('`가 0이고 같은 범위의 줄 수가 5 이상이다 · 같은 범위에 `allocateScopedDocumentNumber(`가 1건 있다
    - E2E 한 경로 CI=true 녹색
  </acceptance_criteria>
  <done>구매 요청 한 건이 사전 조회(트랜잭션 밖) → 잠금 → 문 판정 → 이중 연결 판정 → 번호 → 저장 → 목록까지 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 통합 테스트 — 문 거부 · 반대쪽 지출결의 거부 · 동시 6건 번호 경합 · 링크 스킴</name>
  <files>domain/purchase-requests/index.ts, test/integration/purchase-requests.test.ts</files>
  <read_first>
    - test/integration/document-counters-concurrency.test.ts(병렬 트랜잭션 선례 — 풀 크기 실측 단언)
    - PR #75의 `test/integration/projects-create-concurrency.test.ts`(main에 있으면 그것, 없으면 `git show origin/claude/fix-project-create-deadlock-8wybyd:test/integration/projects-create-concurrency.test.ts`) — 풀 밖 `pg.Client`로 대상 행을 먼저 잠그고, 풀 연결이 전부 그 잠금을 기다리는 것을 `pg_stat_activity`로 확인한 뒤 풀어 교착 조건을 타이밍이 아니라 결정적으로 만드는 꼴. 그 파일 96-142행의 대기 판정 · 정리 꼴을 그대로 따른다(대기 판정은 `pg_blocking_pids`로 잠금 클라이언트 pid를 기다리는 연결만 센다 · 경주 타이머 `clearTimeout` · `finally`에서 `Promise.allSettled`)
    - lib/env.ts 54행(`DB_POOL_MAX` 기본 5)
    - test/integration/corp-card-usages.test.ts(06-07 — 반대쪽 연결 준비 선례)
  </read_first>
  <behavior>
    - 비온라인 줄 요청 → `purchase.line-door` 거부
    - 지출결의가 이어진 온라인구매 줄 요청 → `card.dual-link-block` 거부
    - **「동시 6건 번호 경합」**(CROSS E-2): 같은 프로젝트 · 같은 온라인구매 줄에 `createPurchaseRequest` 6건을 `Promise.all`로 동시에 부른다(`DB_POOL_MAX` 기본 5 초과 — 먼저 `pool.options.max`가 6 미만임을 단언해 풀보다 많은 호출이라는 전제를 고정한다). 풀 밖 `pg.Client`가 그 견적 줄 행을 `FOR UPDATE`로 먼저 쥐고, 풀 연결이 전부 그 잠금을 기다리는 것을 확인한 뒤 푼다(PR #75 꼴). 판정: 10초 경주 안에 6건 모두 끝남(타임아웃 없음) + 번호 6개가 서로 다르고(`new Set(numbers).size === 6`) 전부 `{프로젝트 번호}-C` 서식 + 요청 행 6개
    - 링크 `javascript:alert(1)` → zod 거부 · DB에 직접 넣으면 CHECK 위반
    - 요청 INSERT를 실패시키면 트랜잭션 전체가 롤백돼 카운터도 오르지 않는다 — 요청 행 0 · 다음 요청이 같은 번호(`…-C0001`이 실패했으면 다음도 `…-C0001`)를 받는다(결번 없음, D-42 · `test/integration/document-counters-concurrency.test.ts`와 같은 판정)
  </behavior>
  <action>
`test/integration/purchase-requests.test.ts`(신규)에 behavior 다섯 줄을 개별 `it`으로 쓴다. 테스트가 드러내는 결함만 `domain/purchase-requests/index.ts`에서 고친다(요청받지 않은 리팩터 금지). 반대쪽 지출결의는 Phase 5 domain 함수로 만든다.

「동시 6건 번호 경합」은 RED를 먼저 확인한다: Task 1의 트랜잭션 전 서식 조회를 임시로 `runCreate` 안(`allocateScopedDocumentNumber` 바로 앞)으로 옮기면 이 케이스가 10초 경주에서 빨개지는(교착 — 줄 잠금을 쥔 트랜잭션이 여섯째 커넥션을 기다린다) 것을 한 번 보고 되돌린 뒤 녹색을 확인한다. 결과(빨간 메시지 한 줄 · 되돌린 뒤 녹색)를 SUMMARY에 적는다. 이 케이스의 `it` 제한 시간은 20초로 두고, 경주 타이머는 `clearTimeout`하고, `finally`에서 잠금 클라이언트를 닫은 뒤 남은 호출을 `Promise.allSettled`로 기다린다(review.md #2).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/document-numbering.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. document-numbering · concurrency가 빨가면 기존 번호 경로를 깨뜨렸다. 「동시 6건 번호 경합」이 10초 경주 초과 · 제한 시간 초과(Test timed out)로 빨가면 트랜잭션 안에서 전역 `db`(서식 · 설정 · 권한 · 행동 로그)를 부른다 — 서식을 트랜잭션 안으로 되돌린 임시 변경에서 이 빨간 결과가 나는 것을 먼저 확인한다(CROSS E-2)</fails_when>
  </verify>
  <acceptance_criteria>
    - behavior 다섯 줄이 개별 `it`이다(동시 6건은 `Promise.all`로 실제 병렬, 테스트 이름에 「동시 6건」)
    - SUMMARY에 「동시 6건 번호 경합」의 RED(서식 조회를 트랜잭션 안으로 되돌린 임시 변경) → GREEN 확인 한 줄이 있다
  </acceptance_criteria>
  <done>문 거부 · 반대쪽 거부 · 풀보다 많은 동시 요청에서의 번호 유일성과 교착 없음 · 링크 스킴이 통합 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 목록 범위 · 누수 스캔 · 막힘 문구</name>
  <files>repositories/purchase-requests.ts, domain/purchase-requests/index.ts, app/(app)/cards/purchases/page.tsx, app/(app)/cards/purchases/purchase-request-form.tsx, test/integration/leak-scan.test.ts, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md UI Considerations `S11` · `S12` 행
    - domain/permissions/scope-for.ts · repositories/projects.ts(담당 PM 조건 선례)
  </read_first>
  <behavior>
    - 범위: 요청자 → 자기 요청 + 자기가 담당 PM인 프로젝트 줄의 요청 / `cards.purchases` write · 전사 범위 → 전부 / 그 밖 → 남의 요청 없음
    - 서버 막힘(문 · 반대쪽)이 폼 1차 자리에 「막힘 — 구매 요청 폼」 문구로 선다
  </behavior>
  <action>
범위 판정을 `repositories/purchase-requests.ts` 쿼리 조건으로 둔다(domain이 거르지 않는다). 폼은 서버 막힘을 Copywriting 「막힘 — 구매 요청 폼」 행 그대로 1차 자리에 보이고 입력값을 남긴다. `test/integration/leak-scan.test.ts`에 구매 요청 목록 DTO를 범위 밖 계정 · 권한자 두 계급으로 검사하는 케이스를, `purchase-requests.test.ts`에 behavior 첫 줄을 더한다. E2E에 「비온라인 줄로 `line` 쿼리 진입 → 막힘 문구」 케이스.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 목록 DTO가 범위 밖 요청을 흘렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 누수 스캔이 범위 밖 계정의 목록에 남의 요청이 없음을 단언한다
    - E2E 막힘 케이스 녹색
  </acceptance_criteria>
  <done>구매 요청 목록이 범위대로 보이고 서버 막힘이 폼에 그대로 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 구매 요청 액션 | 품목 · 링크 URL · 예상 금액 · 연결이 넘어온다. 링크는 외부 URL이고 목록에서 새 탭으로 열린다 |
| domain → quote_lines | 요청 생성이 견적 줄을 잠그고 두 게이트를 지난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-35 | Tampering | 문 가르기 우회 | high | mitigate | `purchase.line-door`를 요청 생성 domain에서 잠금 뒤 부른다. 비온라인 줄 거부 통합 테스트 |
| T-06-36 | Tampering | 이중 연결(구매 요청 ∥ 지출결의) | high | mitigate | `lockQuoteLines` → `card.dual-link-block`(B-1). 지출결의 쪽 입구 · 병렬 테스트는 06-13 |
| T-06-37 | Tampering | 링크 URL(`javascript:` 등) | high | mitigate | zod + DB CHECK로 http/https만, 렌더는 스킴 재확인 뒤 `rel="noopener noreferrer"` 새 탭 |
| T-06-38 | Tampering | 번호 경합·중복 | medium | mitigate | 카운터 증가를 요청 INSERT와 같은 트랜잭션, `number` `uniqueIndex`, 「동시 6건 번호 경합」 통합 테스트(서로 다른 번호 6개) |
| T-06-154 | Denial of Service | 구매 요청 트랜잭션의 풀 고갈 교착 | high | mitigate | 서식 · 온라인구매 협력사 설정 · 권한은 트랜잭션 전에 읽고 `runCreate`는 tx 리포지토리 · 순수 함수 · 게이트만(06-03 tx 규약 · CROSS E-2 · PR #75 꼴). 행동 로그는 커밋 뒤. 「동시 6건 번호 경합」(풀 밖 잠금으로 결정적 교착 조건) + region grep |
| T-06-39 | Information Disclosure | 구매 요청 목록 | medium | mitigate | 범위를 리포지토리 쿼리에서 거르고 DTO 등록 → 누수 스캔 |
| T-06-40 | Denial of Service | 신규 표 마이그레이션 | low | mitigate | SET LOCAL 한 쌍 · `pnpm lint:sql` 0건 |
</threat_model>

<verification>
- 단위 registry-coverage · leak-scan-coverage · line-door, 통합 purchase-requests(「동시 6건 번호 경합」 포함) · document-numbering · concurrency · leak-scan 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E purchase-requests가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S11 · S12 · S20 DOM 실측 감사(backstop 3건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 온라인구매 줄은 구매 요청으로만 열리고 사람이 문을 고르지 않는다(구매 요청 쪽 입구)
- 구매 요청이 잠금 뒤 이중 연결 판정을 지나 번호를 받는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `purchase_requests`(`uniqueIndex(number)`) |
| 마이그레이션 | `db/migrations/NNNN_purchase_requests.sql`(SET LOCAL 한 쌍) |
| 게이트 규칙 | `purchase.line-door`(ctx `{ side, door, vendorName }`) |
| 함수(domain) | `createPurchaseRequest`(견적 줄) · `listPurchaseRequests` |
| Server Action | `createPurchaseRequestAction` |
| 라우트 | `/cards/purchases`(S11 · S12) |
| 변경된 기존 심볼 | `PURCHASE_ONLINE_VENDOR_NAME` readBy 제거 |
| 테스트 | `test/integration/purchase-requests.test.ts`(「동시 6건 번호 경합」 포함) · `test/e2e/purchase-requests.spec.ts` · leak-scan 케이스 |

### Round 3 — r4 (final-review-fable, 2026-09-26)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/final-review-fable.md`(커밋 전 — 줄 번호는 그 파일 기준). 교차 검토는 Codex 대신 Fable(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F-N1 — read_first가 저장소 밖 `/mnt/project-files/deadlock-fix/global-db-in-tx-audit.md` · `deadlock-fix/review.md` 발견 #1 · #2를 인용(final-review-fable.md L51-55) | 참고(고침) | 사본이 저장소 · 이 환경 어디에도 없다(`find /` 0건). 감사 메모 → `grep -L "DbOrTx" repositories/*.ts`(+ 필요한 곳은 `grep -n "export async function" repositories/settings.ts`)로 저장소에서 다시 뽑는 명령(`findProjectById`는 `repositories/projects.ts` 서명으로). review.md 발견 → main에 있는 `test/integration/projects-create-concurrency.test.ts` 96-142행의 대기 판정(`pg_blocking_pids`) · 정리(`clearTimeout` · `finally` `Promise.allSettled`) 꼴(확인함) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-08-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-2 — 06-08 ③이 `lockQuoteLines` 잠금을 쥔 트랜잭션 안에서 번호 부여(서식 설정 5번 = 전역 `db`)를 부른다 · 번호 경합 테스트를 풀 크기 이상 병렬로(eng-cross-opus.md L16-18@333f7bf; `lib/env.ts:54` `DB_POOL_MAX` 기본 5 확인) | 막음 | Plan 08, Task 1 ③: 트랜잭션 전 `loadDocumentNumberFormat("purchase_request")` → `allocateScopedDocumentNumber(…, { …, format }, tx)`(06-02 시그니처). Task 2: 기존 「동시 두 요청」 케이스를 「동시 6건 번호 경합」으로 바꾸고(같은 파일 `test/integration/purchase-requests.test.ts` 유지) PR #75의 풀 밖 잠금 꼴로 결정적 교착 조건 · RED(서식을 트랜잭션 안으로 되돌림) 먼저 확인 · fails_when. must_haves.truths · key_links · acceptance region grep · T-06-38 · T-06-154 |
| ENG E-3 / 06-03 tx 규약 — 트랜잭션 안에서 `getSettingValue` · `can` · `recordAction` 금지(eng-review.md L72-74@333f7bf) | 고침 | Plan 08, Task 1 ③: `purchase.online_vendor_name` 설정과 권한 판정은 트랜잭션 전, 잠근 줄의 거래처 이름 · 프로젝트 번호는 tx 조회(`repositories/vendors.ts` · `findProjectById`는 tx 인자 없음 — audit), `document_create`는 커밋 뒤. must_haves.truths 「06-03 tx 규약」 참조 한 줄 |
| 자체 발견(검토 반영 중 플래너가 찾음) — 옛 truth 「트랜잭션이 실패하면 그 번호는 결번으로 남는다」와 Task 2 behavior 「요청 INSERT 실패 → 카운터는 올랐지만 요청 행 없음(결번)」이 D-42와 어긋났다: 카운터 증가가 요청 INSERT와 같은 트랜잭션이라 실패하면 함께 롤백된다(`test/integration/document-counters-concurrency.test.ts` 「실패한 트랜잭션의 증가는 문서 INSERT와 함께 롤백된다 — 다음 호출이 그 번호를 그대로 이어받는다(D-42)」). 그대로 두면 그 케이스는 녹색이 될 수 없다 | 고침 | Plan 08, must_haves.truths 번호 부여 줄 · Task 2 behavior 다섯째 줄을 「롤백 → 결번 없음 · 다음 요청이 같은 번호」로 바로잡음(D-42 · 테스트 파일 인용) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 번호 경합 테스트를 새 파일 `test/integration/document-numbering-concurrency.test.ts`로 | 채택 안 함 — 06-08이 이미 번호 경합 케이스를 `test/integration/purchase-requests.test.ts`에 두고 있어 그 파일을 유지한다(오케스트레이터 지시). 교착 조건은 구매 요청 경로(줄 잠금 + 번호 부여)에서만 서므로 부여 함수 단독 파일보다 이 자리가 맞다 |

### Round 2 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-6 — read_first의 UI-SPEC rev 8 줄 번호가 남았다(06-08:144 「655-691행」 · 「835행」 · :228 「995-1010행」 — 체커 W4 잔여) | 고침 | Task 1 read_first → 「S11. 구매 요청 목록」 · 「S12. 구매 요청 신청 폼」 · 「S13. 구매 완료 …」 절 · 「금액 표시 (서버 계산 값)」 절 「구매 요청 예상 금액」 행 · Task 3 read_first → UI Considerations `S11` · `S12` 행 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |
