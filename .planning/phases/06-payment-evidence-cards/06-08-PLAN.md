---
phase: 06-payment-evidence-cards
plan: 08
type: execute
wave: 5
depends_on: ["06-01", "06-02"]
files_modified:
  - domain/payments/targets.ts
  - domain/payments/batch.ts
  - repositories/payment-targets.ts
  - ui/table/Table.tsx
  - ui/table/types.ts
  - ui/table/Table.module.css
  - ui/table/use-grid-keyboard.ts
  - app/(app)/expenses/page.tsx
  - app/(app)/expenses/payment-targets-table.tsx
  - app/(app)/expenses/batch-payment-dialog.tsx
  - app/(app)/expenses/paid-list.tsx
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - app/(app)/expenses/[id]/payment-section.tsx
  - test/unit/domain/payments-targets.test.ts
  - test/unit/ui/table-selectable.test.ts
  - test/integration/payment-batch.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/payment-batch.spec.ts
autonomous: true
requirements: [EXP-09, EXP-06]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 권한자의 `/expenses` 기본 보기가 「지급 대상」(결재 통과 · 지급 전 지출결의 전사 전부)이고, 그 밖의 사람의 기본 보기는 Phase 5 그대로다"
    - "지급 권한자가 「지급 대상」에서 여러 건을 고르고 1차 `지급 완료 N Ctrl+Enter` → 확인 모달에서 지급일(기본 오늘) 한 칸을 확인하면 **건마다 따로** 처리되어, 처리된 건은 목록에서 빠지고 막힌 건은 자리·선택을 유지한 채 행 아래 이유 한 줄이 선다(D-604 부분 성공, AS1)"
    - "각 행을 고를 수 있는지는 서버가 행마다 `payment.approval-required` · `payment.evidence-required`로 판정해 보내고(「상태 → 1차」 P4만), 처리 요청 때 서버가 같은 게이트를 **다시** 돈다 — 클라이언트가 보낸 선택 가능 여부를 믿지 않는다"
    - "[O-2 기본값의 소비처] `확인 전` 행의 선택 칸은 `aria-disabled` + 이유 `확인 전 · 증빙 확인`이고, 이 판정은 06-01의 `resolveExpenseActionRow`/`payment.evidence-required`를 그대로 부른다 — 이 플랜에 O-2 조건이 새로 쓰이지 않는다"
    - "모달의 지급일로 서버가 다시 계산한 지급 총액이 화면 값과 다른 건은 처리하지 않고 막힘으로 돌아오며, 그 행의 지급 총액 셀이 새 값으로 바뀌고 이유 `지급일 {MM-DD} 기준 지급 총액 바뀜 · 이체액 확인`이 선다 — 재시도는 새 값으로 보내 같은 이유로 다시 막히지 않는다"
    - "[O-3 기본값] 한 번의 일괄 처리는 지급일 한 값을 고른 건 전부에 쓰고, 모달을 다시 열면 지급일 기본값이 방금 고른 날이다(필터 변경·새로 고침 때 잊는다)"
    - "`고른 N건 · 이체액 {합}`의 합은 서버 액션이 고른 행 id + 이체액 편집값으로 더해 돌려주고(O-18 — 클라이언트 합 없음), 응답에 합계 번호가 실려 지금 번호와 다르면 버린다"
    - "[O-17 기본값] 계좌 행·계좌 열·`번호 보기`는 지급 권한자에게만 서버가 보낸다 — 다른 역할의 DOM에는 계좌 문자열이 없다(마스킹된 꼬리 4자리도 없다)"
    - "지급 완료 보기(S3)와 Phase 5 목록의 상태 열에는 계좌 글자가 없고 지급 방식 낱말만 있다 — 대표도 이 목록을 읽는다"
    - "SP-1 `selectable` 편집 표 variant가 `ui/table`에 있다 — 선택 열(네이티브 체크박스 · `accent-color: var(--native-accent)` · 폭 44) · 머리글 체크박스 = 이 쪽의 고를 수 있는 행 전체(섞이면 `indeterminate`) · `Space` = 현재 행 고르기 · 못 고르는 행은 `aria-disabled` + `aria-describedby` → 그 행 이유 글자, 고른 행은 `--accent-weak` 면(행 배경 칠 금지는 막힌 행에만)"
    - "S1 empty: 0건 = 「Empty — 지급 대상」, 필터 0건 = 「Empty — 지급 대상 · 필터 0건」 — 둘 다 합계 줄·선택 열·1차를 그리지 않는다"
    - "S1 loading: 첫 로드 = §7-7 LOADING 뼈대, 필터·페이지 이동은 GET 이동, 합 요청 중에는 이전 합을 `--faint`로 두고, 처리 중 1차 `지급 완료 N…` + `aria-disabled`"
    - "S1 error: 로드 실패 = 「Error — 목록 로드」, 칸 오류 = 「Error — 이체액 칸」·「Error — 차이 사유 칸」, 합 실패 = 「거부 — 이체액 합계 응답 없음」, 응답 번호 ≠ 지금 합계 번호면 버린다"
    - "S1 populated: 1280 이상 10열 · 그룹 머리글(`예정일 지남` → `이번 주 지급` → `다음 주` → `그 뒤` → `지급 예정일 없음`, 주는 월요일 시작 KST) · 합계 줄 · 페이지 줄 · 힌트 줄 `이동 Tab ↑↓←→ · 고르기 Space · 취소 Esc`"
    - "S1 partial: 일괄 처리 뒤 처리된 행은 빠지고 막힌 행은 자리·선택 유지 + 행 아래 이유 + 1차 왼쪽 결과 글자(`지급 완료 N건 {시:분} · 막힘 N건`), 이유는 다음 시도·필터 변경 때 지운다 / 이체액·차이 사유 편집 칸은 P4 행에만, 고르지 않은 P4 행의 이체액을 고치면 저절로 골라진다"
    - "S1 overflow: 50건 페이지(§7-16, UA-603), 선택은 행 id로 쪽을 넘어도 남고, 합계 줄은 필터 전체 기준, 좁은 PC는 P3(번호·거래처)부터 숨기고 700~1023은 보기 전용이다"
    - "S1 zero-one-many: 고른 0건 = 1차 비활성 + `고른 건 없음`(`info`), 1건 = `지급 완료 1`, 여러 쪽 = 모든 쪽 합, 머리글 체크박스 = 이 쪽의 고를 수 있는 행 전체(섞이면 `indeterminate`)"
    - "S2 empty: 지급일 한 칸은 비어서 열리지 않는다 — 기본 오늘(KST), 직전에 막혀 돌아온 뒤에는 방금 고른 날"
    - "S2 loading: 부제 합계 자리 `…` + 1차 `…` · `aria-disabled`(`Ctrl+Enter` 무반응) — 모달을 연 순간의 합계 번호 응답이 오면 채우고 켠다, 처리 요청 중 1차 `…` + 나머지 `aria-disabled`"
    - "S2 error: 지급일 칸 = 「Error — 지급일 · 지급 예정일 칸」(모달 유지), 합 실패 = 「거부 — 이체액 합계 응답 없음」, 처리 응답 없음 = 「거부 — 일괄 지급 응답 없음」(모달을 닫지 않는다)"
    - "S2 partial: 일부만 처리 = 모달이 닫히고 S1 결과로, 지급일 세율로 지급 총액이 바뀐 건은 막힘으로 돌아와 새 지급 총액 셀 + 이유, 0건 처리면 토스트 없음"
    - "S3 empty: 「Empty — 지급 완료 보기」"
    - "S3 loading: 첫 로드 = §7-7 LOADING 뼈대, 보기·팀·월 필터 이동은 GET 이동"
    - "S3 error: 「Error — 목록 로드」 `지급 완료 목록을 불러오지 못했습니다 · 다시 시도`(「다시 시도」 = 2차)"
    - "S3 populated: 지급일 주 그룹(`이번 주` → `지난주` → `MM-DD ~ MM-DD`) · 합계 줄 `합계 (지급 완료 · {월} · N건)` · 이체액 2행 차이 · 처리한 사람 · 1차 없음, Phase 5 목록 상태 열 값 `지급 완료`(`success`) + 2행 날짜(열 신설 없음, UA-605)"
    - "S3 partial: 차이 0이면 2행 없음, 차이 사유 없으면 `—`, 지급 뒤 들어온 선결제 증빙이 `확인 전`이면 증빙 칸 3차 `증빙 확인`"
    - "S3 overflow: 50건 페이지(§7-16), 좁은 PC·폰은 P3(지급 방식 · 차이 사유 · 처리한 사람)부터 숨긴다"
    - "S3 zero-one-many: 0건 = Empty 한 줄, 건수는 합계 줄 `N건` 한 꼴"
    - "S5 empty: 값 없음은 `—`(지급 예정일이 비어도 `—`), 차이 사유 행은 차이가 있을 때만, 계좌이체인데 계좌 없음 = `계좌 없음 · 계좌 등록은 관리자`, 결재 통과 전 문서에는 섹션이 없다"
    - statement: "S1 long-text: 항목·거래처·예금주는 셀 한 줄 말줄임 + 전체 값 `title`, 표 안 차이 사유는 두 줄까지 — 20자 이상 한글 거래처 · 200자 차이 사유로 CI=true DOM 감사"
      verification: backstop
    - statement: "S2 long-text: 부제 `N건 · 이체액 합계 {13자리 금액}`과 결과 줄 둘이 `--modal-w` 480 안에서 숫자 중간 줄바꿈 없이 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S3 long-text: 항목·거래처 한 줄 말줄임 + `title`, 표 안 차이 사유 두 줄까지 — 긴 거래처·200자 사유로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "클라이언트가 금액 셈(고른 건 이체액 합 · 차이)을 해서는 안 된다 — 합과 차이는 서버 액션 응답이다(O-18)"
      status: resolved
      verification: test
    - statement: "지급 권한자가 아닌 사람의 응답·DOM에 계좌 문자열(마스킹 꼬리 포함)이 실려서는 안 된다(O-17)"
      status: resolved
      verification: test
    - statement: "일괄 처리에서 한 건의 실패가 다른 건의 지급 기록을 되돌리거나 막아서는 안 된다(D-604 부분 성공)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/payments/targets.ts"
      provides: "지급 대상·지급 완료 목록 DTO(행별 선택 가능·이유·지급 총액·계좌(권한자만)) + 고른 건 이체액 서버 합"
      exports: ["listPaymentTargets", "listPaidPayments", "sumSelectedTransfers"]
    - path: "domain/payments/batch.ts"
      provides: "건별 트랜잭션 일괄 지급(AS1) — 행마다 06-01 단건 경로 재사용"
      exports: ["completePaymentsBatch"]
    - path: "ui/table/Table.tsx"
      provides: "SP-1 `selectable` variant"
    - path: "test/integration/payment-batch.test.ts"
      provides: "부분 성공 · 게이트 재판정 · 지급일 재계산 막힘 · 계좌 노출 증명"
      min_lines: 50
  key_links:
    - from: "domain/payments/batch.ts"
      to: "domain/payments/index.ts"
      via: "행마다 단건 지급 함수를 자기 트랜잭션으로 부른다(게이트·재계산·동시성이 한 경로)"
      pattern: "completeExpensePayment"
    - from: "domain/payments/targets.ts"
      to: "domain/payments/action-row.ts"
      via: "행별 선택 가능·이유가 문서 화면과 같은 1차 판정표에서 나온다"
      pattern: "resolveExpenseActionRow"
    - from: "app/(app)/expenses/payment-targets-table.tsx"
      to: "app/(app)/expenses/actions.ts"
      via: "고른 건 합이 서버 액션 응답이고 합계 번호로 낡은 응답을 버린다"
      pattern: "sumSelectedTransfersAction"
---

<objective>
경영관리의 **일괄 지급**을 세운다 — 지출결의 목록의 「지급 대상」 보기(S1), 확인 모달과 건별 처리(S2), 「지급 완료」 보기(S3), 지급 섹션의 계좌 행(S5 empty · O-17). 06-01의 단건 경로를 행마다 재사용하고, SP-1 선택 열 variant를 `ui/table`에 더한다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 경영관리는 은행에서 스무 건을 한 번에 이체하고 기록한다. 한 건의 계좌 오류로 스무 건의 기록이 막히면 안 되고(SP-1 이유), 지급일에 따라 세율이 바뀐 건은 조용히 다른 금액으로 처리되면 안 된다.

Output: `domain/payments/targets.ts` · `batch.ts`, `ui/table` `selectable` variant, `/expenses?view=pay` · `?view=paid`, 단위 2 · 통합 1 · E2E 1.

**웨이브 5인 이유:** 의존은 06-01 · 06-02뿐이지만, Phase 5의 `app/(app)/expenses/actions.registry.ts`가 없어 새로 만들어야 하는 경우 `test/integration/leak-scan.test.ts`를 고친다 — 그 파일을 고치는 06-01·06-03·06-05·06-06과 같은 웨이브에 두지 않으려고 웨이브 5(06-07과 파일 겹침 없음)에 둔다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-RESEARCH.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-08 착수하지 않음`을 출력하고 멈춘다.**

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-607 · UA-605 | Phase 5 지출결의 목록 `/expenses`(상태 필터 · 그룹 머리글 · 합계 줄)와 그 액션 파일. **main의 `app/(app)/expenses/page.tsx`는 지금 19줄 ListEmpty 자리다** — Phase 5 목록이 들어와 있어야 한다 | `grep -c "ListEmpty" 'app/(app)/expenses/page.tsx'` 1 이상이고 목록 코드가 없으면 멈춤 · `git ls-files 'app/(app)/expenses/actions.registry.ts'` 결과를 SUMMARY에 적는다(없으면 이 플랜이 만들고 누수 스캔 import를 더한다) |
| UA-603 | `ui/pagination/Pagination.tsx` | `git ls-files ui/pagination/Pagination.tsx` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(S2 — 확인 근거 한 칸 = 지급일) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| UA-604 | `ui/table/Table`의 편집 표 · 그룹 머리글 · 칸 접기 — 선택 열은 없다(이 플랜이 더한다) | `grep -n "selectable" ui/table/Table.tsx ui/table/types.ts` 가 이미 있으면 멈추고 SUMMARY에 적는다(중복 구현 방지) |
| UA-613 | `AccountNumberCell`(main `app/(app)/admin/vendors/account-number.tsx`) · 해제 판정 = 정보 항목 `vendor.account_number_unmasked` | `git ls-files 'app/(app)/admin/vendors/account-number.tsx'` 비면 멈춤 |
| 06-01 · 06-02 | `completeExpensePayment` · `resolveExpenseActionRow` · `prepaidDueInfo` | `grep -c "completeExpensePayment\|resolveExpenseActionRow" domain/payments/index.ts domain/payments/action-row.ts` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-3 | 일괄 지급의 지급일은 한 값(고른 건 전부 같은 날) | `completePaymentsBatch` 입력 모양 · S2 모달 한 칸 |
| AS1 | 건별 트랜잭션(부분 성공) | `domain/payments/batch.ts` 한 함수 |
| O-17 | 계좌는 지급 권한자에게만, 다른 역할에는 행 자체를 보내지 않음 | `domain/payments/targets.ts`·지급 섹션 DTO의 계좌 칸 투영 |
| O-18 (닫힘) | 고른 건 합은 서버 액션 | 해당 없음 |
| O-2 | (06-01 소관 — 이 플랜은 판정 함수를 부르기만) | 해당 없음 |
| O-1 | 지급 대상은 지출결의 메뉴의 보기(6번째 메뉴 없음) | `app/(app)/expenses/page.tsx` 보기 필터 |

**계획 판단(UA-613):** `AccountNumberCell`을 `ui/`로 옮기지 않는다. `app → app` import는 eslint boundaries 규칙이 허용하고(`eslint.config.mjs`), 옮기면 컴포넌트가 쓰는 관리자 거래처 액션을 prop으로 빼는 리팩터가 따라온다(CLAUDE.md §3.3). 지급 화면은 `@/app/(app)/admin/vendors/account-number`에서 그대로 가져다 쓰고, `canReveal`은 그 해제 액션과 같은 정보 항목 판정으로 서버가 계산한다.

<probe_fallback>
coverage.json의 이 플랜 몫: **EXP-09 — unclassified → unresolved (flagged)**, **EXP-06 — unclassified → unresolved (flagged)**. 자동 해소·기각 없음. 대신 쓰는 근거: 기준 1 · D-604 · D-605 · AS1 · O-3 · O-17. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 「지급 대상」 보기에서 두 건을 골라 일괄 지급하면 한 건은 처리되고 한 건(그사이 확인 풀림)은 막혀 자리에 남는다</name>
  <files>domain/payments/targets.ts, domain/payments/batch.ts, repositories/payment-targets.ts, ui/table/Table.tsx, ui/table/types.ts, ui/table/Table.module.css, app/(app)/expenses/page.tsx, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/batch-payment-dialog.tsx, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, test/integration/payment-batch.test.ts, test/integration/leak-scan.test.ts, test/e2e/payment-batch.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 06-01 · 06-02가 끝났다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 385-463행(S1 · S2 정본) · 858-866행(SP-1) · 924-936행(S1 · S2 considerations) · Copywriting 「Destructive — 일괄 지급 완료」 · 「거부 — 일괄 지급 건별 결과」 · 「표시 — 일괄 지급 결과」 · 「Empty — 지급 대상」
    - .planning/phases/06-payment-evidence-cards/06-RESEARCH.md Pattern 1(행별 게이트) · AS1
    - ui/table/Table.tsx · ui/table/types.ts · ui/table/Table.module.css · ui/table/use-grid-keyboard.ts(편집 표 키 규약)
    - domain/payments/index.ts · action-row.ts(06-01) · 06-01·06-02 SUMMARY
    - A-607 재확인에서 찾은 Phase 5 목록 파일
  </read_first>
  <behavior>
    - `completePaymentsBatch(viewer, { payDate, rows })`: 행마다 자기 트랜잭션으로 `completeExpensePayment` → 결과 배열 `{ expenseId, ok | blocked(reason, newPayableKrw?) }`, 한 행의 예외가 다른 행을 멈추지 않는다
    - 처리 요청의 행이 그사이 P4가 아니게 됐으면(확인 풀림 등) 그 행만 막힘 + 게이트 이유
    - 지급 권한 없음 → 전체 거부(행 처리 0)
    - `listPaymentTargets`: 결재 통과 · 지급 전만, 행마다 `selectable`·이유·지급 총액, 그룹 판정 서버
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① **domain.** `repositories/payment-targets.ts`(결재 통과 · 지급 전 지출결의 + 지급 방식 · 예정일 · 증빙 판정 입력 · 거래처 계좌 참조, viewer 첫 인자). `domain/payments/targets.ts`의 `listPaymentTargets(viewer, { team, page })` — `expenses.payments` write 필수, 행마다 06-01 `resolveExpenseActionRow`로 선택 가능(P4)·이유를 정하고, 지급 총액은 06-01의 계산 함수(기준일 = 예정일, 없으면 오늘 KST), 그룹(예정일 지남 → 이번 주 지급 → 다음 주 → 그 뒤 → 지급 예정일 없음, 주 = 월요일 시작 KST)은 서버가 정한다. `domain/payments/batch.ts`의 `completePaymentsBatch` — AS1 건별 트랜잭션, 행마다 `completeExpensePayment`를 부르고(게이트 재판정 · 지급일로 재계산 · `expectedPayableKrw` 비교 · version · 동시성이 전부 그 한 경로), 결과를 모은다. DTO 등록.

② **SP-1 variant.** `ui/table`에 `selectable` 속성: 맨 왼쪽 선택 열(네이티브 체크박스, `accent-color: var(--native-accent)`, 폭 = 28 + `--cell-pad-x`×2), 행 선택 가능 여부는 행 데이터의 서버 값, 못 고르는 행은 `aria-disabled` + `aria-describedby` → 그 행의 이유 요소 id, 머리글 체크박스는 이 쪽의 고를 수 있는 행 전체(섞이면 `indeterminate`), 고른 행 면 `--accent-weak`. 선택 상태는 호출자가 행 id 집합으로 가진다(쪽을 넘어도 남음). 토큰은 `tokens.css`의 것만.

③ **화면(한 경로).** `app/(app)/expenses/page.tsx`(Phase 5 목록): 지급 권한자의 기본 보기 = `view=pay`, 보기 필터에 `지급 대상`·`지급 완료` 추가. `payment-targets-table.tsx`(S1 — 이 태스크는 선택 · 1차 `지급 완료 N` · 결과 글자 · 막힌 행 이유 줄까지), `batch-payment-dialog.tsx`(S2 — `ConfirmDialog`, 확인 근거 한 칸 = 지급일 기본 오늘, 처리 요청은 모달을 연 순간의 스냅숏(행 id · 이체액 · 차이 사유)). `app/(app)/expenses/actions.ts`(Phase 5 것 확장, 없으면 신규)에 `completePaymentsBatchAction` — 새 registry 파일이면 `test/integration/leak-scan.test.ts`에 import 한 줄.

④ **E2E.** `test/e2e/payment-batch.spec.ts`: 지급 대상 두 건(P4) → 둘 다 고름 → 한 건의 증빙을 다른 세션(domain 호출)으로 바꿔 확인을 풀기 → 1차 → 모달 → 확인 → 한 건 빠짐 · 한 건 자리 유지 + 이유 + 결과 글자 `지급 완료 1건 {시:분} · 막힘 1건`.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/expense-payments.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 registry가 누수 스캔에 import되지 않았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다. `plant8/money-boundary` 위반이면 화면이 금액 셈을 했다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(Phase 5 목록 · registry 유무)가 SUMMARY에 있다
    - 통합 테스트가 behavior 네 줄을 단언하고, 부분 성공 케이스는 막힌 행이 있어도 처리된 행의 `expense_payments`가 커밋됐음을 DB로 확인한다
    - E2E 한 경로 CI=true 녹색
  </acceptance_criteria>
  <done>일괄 지급 한 번이 행별 게이트 재판정 → 건별 트랜잭션 → 부분 성공 결과 표시까지 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 이체액 편집 · 서버 합(합계 번호) · 지급일 재계산 막힘 · 키보드 · 페이지 · 좁은 폭</name>
  <files>domain/payments/targets.ts, domain/payments/batch.ts, ui/table/Table.tsx, ui/table/use-grid-keyboard.ts, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/batch-payment-dialog.tsx, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, test/unit/domain/payments-targets.test.ts, test/unit/ui/table-selectable.test.ts, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 413-447행(열 · 우선순위 · 좁은 PC · 폰 · 편집 · 합계 번호) · 452-463행(S2 지급일 재계산)
    - ui/table/use-grid-keyboard.ts · test/e2e/keyboard-nav.spec.ts(키 규약 선례)
    - test/unit 아래 ui 컴포넌트 단위 테스트 선례(없으면 `test/unit/leak-scan-coverage.test.ts`의 정적 판정 꼴을 따르지 말고 순수 함수만 뽑아 테스트한다)
  </read_first>
  <behavior>
    - `sumSelectedTransfers({ seq, rows })` → `{ seq, totalKrw }`(서버 `sumKrw`), 선택 불가 행 id가 섞이면 그 행은 빼고 더한다
    - 지급일 재계산: 모달 지급일 기준 지급 총액 ≠ 행의 `expectedPayableKrw` → 그 행 막힘 + `newPayableKrw` + 이유 `지급일 {MM-DD} 기준 지급 총액 바뀜 · 이체액 확인` / 재시도(새 값) → 처리
    - 선택 상태 순수 함수: 머리글 토글 = 이 쪽의 고를 수 있는 행 전체, 섞이면 indeterminate, 쪽 이동 뒤에도 id 유지, 필터 변경 시 비움, 고르지 않은 P4 행 이체액 변경 → 자동 선택
  </behavior>
  <action>
`sumSelectedTransfersAction` — 입력은 고른 행 id · 이체액 편집값 · 합계 번호, 응답은 번호 + 합(서버 `sumKrw`). 화면은 고른 행 id 집합이나 고른 행 이체액이 바뀔 때만 번호를 1 올리고 300ms 모아 보내며, 응답 번호 ≠ 지금 번호면 버린다. 오는 동안 이전 합 `--faint`. 모달 부제는 모달을 연 순간 번호의 합만 쓰고, 아직이면 `…` + 1차 `aria-disabled`. 합 실패 = 「거부 — 이체액 합계 응답 없음」 + `다시 시도`(같은 번호). 클라이언트는 금액을 더하지 않는다(건수만 센다).

편집: 이체액 칸은 P4 행에만(편집 표 키 그대로 — Enter 편집 · Esc 되돌림 · Tab/방향키), 기본값 = 지급 총액, 이체액 ≠ 지급 총액이면 셀 2행 `차이 {±}`(서버가 준 차이 — 행 단위 미리보기 액션으로 받는다) + 차이 사유 칸 편집 가능·필수, 같아지면 사유 칸 `—`로 돌아가고 적은 사유는 버린다. 고르지 않은 P4 행 이체액을 고치면 그 행이 골라진다. 처리 전까지 저장되지 않으므로 떠나려 하면 `beforeunload` 경고만. 1차를 눌렀을 때 고른 행에 오류 칸이 있으면 첫 오류 칸으로 포커스(모달 없음).

S2 지급일 재계산: `completePaymentsBatch`가 지급일로 다시 계산한 지급 총액이 행의 `expectedPayableKrw`와 다르면 그 행을 처리하지 않고 새 값과 이유를 돌려준다. 화면은 그 행의 지급 총액 셀을 새 값으로 바꾸고, 모달 지급일을 기억한다(필터 변경·새로 고침 때 잊음). 응답 없음 = 모달 유지 + `결과를 받지 못함 · 새로 고침`. 1건 이상 처리면 토스트 `지급 완료 · N건`, 0건이면 토스트 없음.

`use-grid-keyboard.ts`에 `Space` = 현재 행 고르기(선택 가능 행만). 힌트 줄 `이동 Tab ↑↓←→ · 고르기 Space · 취소 Esc`. 페이지 50건(`Pagination`), 합계 줄은 필터 전체 기준(서버). 열 우선순위: 1024~1279는 번호·거래처 숨김, 700~1023은 보기 전용(선택 열·편집·1차 없음), 700 미만은 행 → 문서 화면 링크(일괄 지급 없음).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments-targets.test.ts test/unit/ui/table-selectable.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. keyboard-nav·quote-table이 빨가면 `ui/table` 변경이 기존 편집 표를 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 ⑴ `Space`로 고르기 ⑵ 이체액 수정 → 자동 선택 + 차이 2행 + 사유 필수 ⑶ 지급일을 원천징수 세율이 바뀌는 날로 골라 막힘 → 새 지급 총액 → 재시도 성공 ⑷ 700~1023 폭에서 선택 열 없음을 단언한다
    - 통합 테스트가 합 응답이 서버 `sumKrw` 값이고 선택 불가 행이 빠짐을 단언한다
    - 기존 keyboard-nav · quote-table 스펙 녹색
  </acceptance_criteria>
  <done>이체액 편집·서버 합·지급일 재계산 막힘이 계약대로 돌고, 금액 셈이 화면에 한 줄도 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [O-17 기본값] 계좌 노출 · 「지급 완료」 보기(S3) · Phase 5 목록 상태 열 · 지급 섹션 계좌 행(S5 empty)</name>
  <files>domain/payments/targets.ts, repositories/payment-targets.ts, app/(app)/expenses/page.tsx, app/(app)/expenses/paid-list.tsx, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/[id]/payment-section.tsx, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 465-471행(S3 · Phase 5 목록에 더하는 것) · 796-798행 · 813-815행(권한·계좌) · 937-944행 · 952행(S3 · S5 empty) · Copywriting 「표시 — 지급 방식 · 계좌」 · 「Empty — 지급 완료 보기」
    - app/(app)/admin/vendors/account-number.tsx · domain/vendors/index.ts 360-395행(마스킹 · 해제 판정)
  </read_first>
  <behavior>
    - 지급 권한자 DTO: 계좌이체 행에 은행 · 마스킹 번호 · 예금주 · `canReveal`(정보 항목 판정) / 지급 권한 없음(대표 포함) DTO: 계좌 키 자체가 없음
    - `listPaidPayments`: 지급 권한자·대표 = 전부 읽기, 지급일 주 그룹(이번 주 → 지난주 → 날짜 범위), 합계 줄 이체액 서버 합, 지급 방식 낱말만(계좌 없음)
    - Phase 5 목록 상태 열: 지급 기록 있음 → `지급 완료` + 2행 날짜, 선결제 기한 경과 → 2행 `증빙 {N}일 경과`
  </behavior>
  <action>
계좌 칸 투영은 `domain/payments/targets.ts`와 06-01 지급 섹션 DTO 두 곳에서 같은 판정(`can(viewer, "expenses.payments", "write")`)으로 하고, 권한이 없으면 계좌 키를 결과 객체에 싣지 않는다(O-17 — 마스킹 꼬리도 없음). S1 지급 방식 칸: 계좌이체면 1행 `{은행} ****-**-{뒤4}`, 2행 예금주 + 권한자 3차 `번호 보기`(`AccountNumberCell`을 `@/app/(app)/admin/vendors/account-number`에서 가져온다 — 계획 판단), 법인카드·현금은 낱말만. 지급 섹션(S5) 계좌 행: 권한자에게만, 계좌 없음이면 `계좌 없음 · 계좌 등록은 관리자`, 값 없음 `—`, 차이 사유 행은 차이가 있을 때만.

`paid-list.tsx`(S3, `view=paid`): 열 번호 · 프로젝트 · 항목 · 거래처 · 지급 방식(낱말만) · 지급일 · 이체액(2행 차이, 있을 때만) · 차이 사유 · 증빙(지급 뒤 들어온 선결제 증빙이 `확인 전`이면 3차 `증빙 확인` → 문서 화면) · 처리한 사람, 필터 `[지급 완료] [팀] [월]`, 합계 줄, 1차 없음, 50건 페이지, 좁은 PC·폰은 P3부터 숨김, 행 → 문서 화면. Phase 5 목록의 상태 열에 `지급 완료`(`success`) + 2행 날짜, 선결제 기한 경과면 2행 `증빙 {N}일 경과` — 열을 새로 만들지 않는다(UA-605). 상태 글자는 `status-display.ts`.

통합 테스트에 계좌 노출 계급별 단언(대표·PM·직원 계정 DTO에 계좌 키 없음), E2E에 「대표 계정 `view=paid` 페이지 DOM에 계좌 번호 문자열 없음」 · 「권한자 `번호 보기` → `mask_reveal` 로그」 케이스.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/leak-scan.test.ts test/integration/vendors.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/vendors.spec.ts test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 지급 권한 없는 세 계급의 목록·지급 섹션 DTO에 계좌 관련 키가 하나도 없음을 객체 키로 단언한다
    - E2E가 대표 계정 페이지 HTML에 거래처 계좌 뒤 4자리 문자열이 없음을 단언한다
    - 기존 vendors 통합·E2E 녹색(AccountNumberCell 재사용이 관리자 화면을 깨뜨리지 않음)
  </acceptance_criteria>
  <done>계좌가 지급 권한자에게만 가고, 지급 완료 목록과 Phase 5 목록 상태 열이 계약대로 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 지급 액션 | 행 id · 이체액 · 차이 사유 · `expectedPayableKrw` · 지급일이 넘어온다. 행의 선택 가능 여부는 넘어와도 믿지 않는다 |
| 브라우저 → 합 액션 | 행 id · 이체액 · 합계 번호 |
| 서버 → 브라우저(목록 DTO) | 계좌(민감)가 지급 권한자에게만 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-42 | Elevation of Privilege | 행별 게이트 우회 | high | mitigate | `completePaymentsBatch`가 행마다 06-01 단건 경로를 불러 게이트를 서버에서 다시 판정한다. 선택 불가 행을 실은 조작 요청 거부 통합 테스트 |
| T-06-43 | Information Disclosure | 계좌 번호(O-17) | high | mitigate | 계좌 키를 권한 판정 뒤에만 DTO에 싣는다. 해제는 기존 `vendor.account_number_unmasked` + `mask_reveal` 로그. 계급별 DTO 키 단언 + 대표 DOM 단언 |
| T-06-44 | Tampering | 지급일로 바뀐 지급 총액 | high | mitigate | 지급일로 재계산해 `expectedPayableKrw`와 다르면 처리하지 않고 막힘으로 돌려준다(조용한 처리 없음) |
| T-06-45 | Denial of Service | 일괄 처리 요청 크기 | medium | mitigate | 입력 zod에 행 수 상한(한 번에 고를 수 있는 행 = 필터 전체이므로 상한은 200으로 두고 넘으면 거부 — 계획 판단)과 건별 트랜잭션으로 한 요청의 락 범위를 행 하나로 제한 |
| T-06-46 | Repudiation | 일괄 처리 | medium | mitigate | 행마다 `payment_process` 로그(06-01 단건 경로 그대로) |
| T-06-47 | Tampering | 합 응답 재정렬 | low | mitigate | 합계 번호로 낡은 응답을 버린다 — 표시만의 문제이고 처리는 스냅숏 값으로 서버가 다시 판정 |
</threat_model>

<verification>
- 단위 2 · 통합 1(+ expense-payments · vendors · leak-scan) · E2E 1(+ keyboard-nav · quote-table · vendors · payment-single) 녹색
- `test/unit/leak-scan-coverage.test.ts` 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json·pnpm-lock.yaml·tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S1·S2·S3·S5 DOM 실측 감사(backstop 3건 포함 · 계좌 문자열 부재 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 경영관리가 여러 건을 한 번에 지급 완료하고, 막힌 건만 이유와 함께 남는다
- 지급일로 세율이 바뀐 건은 조용히 처리되지 않는다
- 계좌가 지급 권한자에게만 가고, 화면에 금액 셈이 없다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `listPaymentTargets` · `listPaidPayments` · `sumSelectedTransfers` · `completePaymentsBatch` |
| 리포지토리 | `repositories/payment-targets.ts` |
| Server Action | `completePaymentsBatchAction` · `sumSelectedTransfersAction` · 행 차이 미리보기 액션 |
| 컴포넌트 | `ui/table` `selectable` variant · `use-grid-keyboard` `Space` |
| 화면 | `/expenses?view=pay`(S1) · 일괄 지급 모달(S2) · `/expenses?view=paid`(S3) · Phase 5 목록 상태 열 · 지급 섹션 계좌 행 |
| 테스트 | `test/unit/domain/payments-targets.test.ts` · `test/unit/ui/table-selectable.test.ts` · `test/integration/payment-batch.test.ts` · `test/e2e/payment-batch.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-08-SUMMARY.md` when done
</output>
