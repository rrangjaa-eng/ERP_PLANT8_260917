---
phase: 06-payment-evidence-cards
plan: 09
type: execute
wave: 6
depends_on: ["06-02", "06-05", "06-07"]
files_modified:
  - repositories/pre-settle-check.ts
  - domain/pre-settle-check/index.ts
  - domain/pre-settle-check/summary.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - domain/approvals/settlement.ts
  - domain/approvals/index.ts
  - app/(app)/approvals/[id]/page.tsx
  - app/(app)/status-display.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/pre-settle-check.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/unit/domain/pre-settle-check.test.ts
  - test/integration/pre-settle-check.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/pre-settle-check.spec.ts
autonomous: true
requirements: [PROJ-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "완료 전 점검 섹션은 Phase 5 정산 결재를 기안할 수 있는 상태(A-601 — 다섯 값이면 `정산`)의 프로젝트 상세에만, 견적 줄 표 힌트 줄 바로 아래 첫 섹션으로 선다. 그 밖의 상태에는 섹션이 없다"
    - "점검 판정은 서버 함수 하나(`computePreSettleCheck`)가 한다. 이 함수가 세 그룹의 목록과 막힘 여부를 DTO로 돌려주고, `rules.gate`의 `project.pre-settle-check`는 그 DTO만 보고 허용·막힘을 정한다(RESEARCH Pitfall 4)"
    - "미결 지출결의(D-611)는 결재 중·반려 문서와 증빙 없는 문서다. 증빙 없는 문서에서 면제는 빠지고 선결제는 들어가며, 증빙 필수 설정과는 관계없다. 결재를 통과했지만 아직 지급하지 않은 문서는 세지 않는다"
    - "미매칭 견적 줄(D-612)은 실행가가 0이 아니고 살아 있는 지출결의·카드 사용·구매 요청 연결이 하나도 없는 줄이다. 취소 줄과 경영관리 조정 줄은 세지 않는다. 연결 금액 합이 실행가와 달라도 막지 않는다"
    - "매출 미입력(D-613)은 발행 줄이 0인 상태다. 매출 줄 `계약 금액 · 발행 합계 · 차이`는 그룹이 비어도 늘 선다. 차이가 0이 아니면 `--warning`으로 보이지만 막지 않는다. 계약 금액이 아직 승인 전이면 `계약 금액 —` 뒤에 3차 `고객 승인 전`이 선다"
    - "강행 허용 설정 셋(`project.force_complete.allow_*`, 기본 false)이 켜진 그룹은 머리글 뒤에 `· 강행 허용`(`--muted`)이 붙고 막힘 건수에 들지 않는다. 세 키의 `readBy` 표시는 이 플랜에서 지운다"
    - "막힘이 1건 이상이면 PM의 「정산 결재 올리기」가 비활성이 되고 이유 `완료 전 점검 {N}건 · 점검 보기`가 선다(3차를 누르면 섹션 제목으로 포커스). Phase 5 기안 함수를 직접 불러도 같은 이유로 거부된다(UA-610 · A-609)"
    - "강행 허용 그룹만 남으면 기안이 된다. 기안 확인 자리와 정산 결재 문서 화면에 결과 줄 `강행 · 미결 지출결의 {N}건`이 서고(항목이 있는 강행 그룹이 여럿이면 ` · `로 잇는다), 기안 시점의 점검 요약이 행동 로그 상세에 남는다"
    - "[O-16 기본값] 대표가 정산 결재 문서에서 `승인`을 누르기 전에 같은 판정이 한 번 더 돈다. 막히면 `승인` 1차가 비활성이 되고 `완료 전 점검 {N}건 · 점검 보기`(→ 프로젝트 상세 점검 섹션)가 선다. 04.1 승인 함수를 직접 불러도 거부된다"
    - "미매칭 줄의 3차는 그 줄의 문 하나다. 온라인구매 줄이면 `구매 요청`(→ `/cards/purchases?new=1&line={id}`), 그 밖이면 `지출결의 올리기`(S14 행 행동과 같은 곳)이다. 거래처 없는 줄은 S14와 같은 막힘을 보인다. 사람이 문을 고르지 않고, 카드 폼으로는 가지 않는다. 프로젝트 쓰기 권한이 없는 사람에게는 3차 대신 `담당 PM {이름}`이 보인다"
    - "매출 미입력 행의 3차는 폭 1024 이상에서만 선다. 담당 PM에게는 `발행 요청 추가`, 매출 기록 권한자에게는 `발행 줄 추가`가 선다(S16 · 04 S6과 같은 onClick). 1024 미만에는 버튼 없이 `발행 줄 없음`만 보인다(비활성 버튼도 없다)"
    - "S18 empty: 세 그룹이 다 비면 그룹 자리에 한 줄 `미결 없음`(`--success`)이 서고, 매출 줄은 그대로 남는다"
    - "S18 error: 점검 로드가 실패하면 섹션 자리에 `점검 불러오지 못함 · 다시 시도`가 서고, 「정산 결재 올리기」는 비활성 + `점검 결과 없음 · 다시 시도`다. 점검 없이 기안하지 않는다"
    - "S18 populated: 제목 `완료 전 점검`과 부제 `정산 결재 전 · 막힘 {N}건`, 그룹 셋(막힘 그룹 머리글 건수는 `--danger`), 점검 행, 매출 줄이 선다"
    - "S18 partial: 막힘이 0이어도 강행 허용 그룹의 항목은 보이고, 부제는 `정산 결재 전 · 막힘 없음`이다. 증빙 `면제` 문서는 선결제든 지급 뒤든 미결 지출결의 행에 없다"
    - "S18 zero-one-many: 그룹 머리글 건수는 `미결 지출결의 {N}건` · `미매칭 견적 줄 {N}줄`이다. 막힘 건수는 막히는 그룹의 항목 수 합이고 강행 허용 그룹은 빠진다. 비어 있는 그룹만 접는다"
    - statement: "S18 loading: 섹션 로드는 §7-7 LOADING 꼴을 섹션 단위로 따르고, 이 표면만의 규칙은 없다. 느린 응답을 강제해 CI=true DOM 감사로 확인한다"
      verification: backstop
    - statement: "S18 overflow: 섹션 표라 페이지가 없다. 미결 30건 프로젝트에서 그룹마다 항목이 전부 서는지 CI=true DOM 감사로 확인한다"
      verification: backstop
    - statement: "S18 long-text: 긴 항목 이름이 점검 행에서 말줄임 + `title`로 서는지 CI=true DOM 감사로 확인한다"
      verification: backstop
  prohibitions:
    - statement: "점검이 실패하거나 오류를 내면 정산 결재가 조용히 올라가면 안 된다. 기안 함수는 점검 계산 예외를 삼키지 않고 거부한다"
      status: resolved
      verification: test
    - statement: "막힘 건수와 기안 버튼의 막힘 이유를 화면이 세 목록을 보고 스스로 세면 안 된다. 둘 다 서버 DTO에서만 온다"
      status: resolved
      verification: judgment
    - statement: "강행 허용이 꺼진 그룹에 항목이 남아 있는데 대표 승인이 통과하면 안 된다(O-16)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/pre-settle-check/index.ts"
      provides: "완료 전 점검 DTO 계산(세 그룹 · 매출 줄 · 강행 허용 · 막힘 건수) + DTO 등록"
      exports: ["computePreSettleCheck", "PRE_SETTLE_CHECK_DTO_SPEC"]
    - path: "domain/pre-settle-check/summary.ts"
      provides: "순수 요약: 막힘 건수 · 부제 · 강행 결과 줄 · 미결 지출결의 상태 글자 순서"
      exports: ["summarizePreSettle", "forcedResultLine", "OPEN_EXPENSE_STATUS_ORDER"]
    - path: "test/integration/pre-settle-check.test.ts"
      provides: "세 그룹 × 강행 허용 × 기안 거부 · 대표 승인 거부 · 점검 실패 시 거부 증명"
      min_lines: 80
  key_links:
    - from: "domain/approvals/settlement.ts"
      to: "domain/rules/gate.ts"
      via: "Phase 5 정산 결재 기안이 같은 트랜잭션에서 점검 DTO를 계산하고 게이트를 받는다"
      pattern: "project.pre-settle-check"
    - from: "domain/approvals/index.ts"
      to: "domain/pre-settle-check/index.ts"
      via: "[O-16] 정산 결재 승인 앞 재점검"
      pattern: "computePreSettleCheck"
    - from: "domain/pre-settle-check/index.ts"
      to: "repositories/quote-line-links.ts"
      via: "미매칭 판정이 06-03·06-06·06-07의 연결 조회를 한 번 부른다(연결 정의를 다시 쓰지 않는다)"
      pattern: "findLineLinks"
    - from: "domain/pre-settle-check/index.ts"
      to: "domain/settings/keys.ts"
      via: "강행 허용 세 키 읽기"
      pattern: "PROJECT_FORCE_COMPLETE_ALLOW"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/pre-settle-check.tsx"
      via: "`QuoteLedger` 안, 힌트 줄 바로 아래에 섹션을 둔다. 매출 3차는 원장의 기존 줄 추가 함수를 그대로 받는다"
      pattern: "PreSettleCheck"
---

<objective>
정산 결재를 올리기 전에 서는 **완료 전 미결 점검**(PROJ-06, D-611~D-613)을 만든다. 점검은 세 그룹(미결 지출결의 · 미매칭 견적 줄 · 매출 미입력)을 서버 함수 하나로 계산해 화면에 보여 준다. 강행 허용 설정에 따라 PM의 「정산 결재 올리기」를 막거나 열고(UA-610), 대표 승인 앞에서 한 번 더 막는다(O-16).

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
이 Goal은 user story 형식이 아니다(MVP 모드). `/gsd mvp-phase 6`을 권하며, 계획은 이야기를 지어내지 않았다.

Purpose: ROADMAP 기준 4는 "이유와 함께 막는다"다. boolean 하나로 합치면 PM이 왜 막혔는지 볼 수 없다(Pitfall 4). 그래서 판정은 DTO(그룹별 목록)가 하고, 게이트는 그 DTO의 막힘 건수만 본다. 기안 버튼, 기안 함수, 대표 승인이 모두 같은 DTO를 읽는다.

Output: `domain/pre-settle-check`(DTO · 요약), 리포지토리 1, 게이트 `project.pre-settle-check`, 강행 허용 세 키 읽기(readBy 제거), Phase 5 기안 훅, 04.1 승인 훅(O-16), 섹션 `pre-settle-check.tsx`, 단위 1 · 통합 1 · E2E 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-05-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-07-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 실행을 시작할 때 main에서 아래를 다시 확인한다. 하나라도 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-09 착수하지 않음`을 출력하고 멈춘다.** 아래 이름은 모두 가칭이다. 실제 이름이 다르면 실제 이름을 쓰고, SUMMARY에 가칭과 실제 이름의 대응표를 남긴다. 존재하지 않는 코드의 시그니처를 이 플랜이 지어내지 않는다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-601 | 프로젝트 상태 값과 Phase 5 정산 결재 기안이 받는 상태. 다섯 값(수주중·진행·정산·완료·미수주)이면 섹션은 `정산`에서 선다. 아직 네 값이면 섹션은 「Phase 5 기안 함수가 기안을 허용하는 상태」에서 서고, 그 대응을 SUMMARY에 적는다 | `grep -n "status" db/schema/projects.ts`와 기안 함수의 상태 검사 줄을 SUMMARY에 적는다. 기안 함수가 상태를 검사하지 않으면 멈추고 묻는다 |
| A-604 | 계약 금액. Phase 4 D-84 파생값(고객 승인 차수의 견적 합계) 함수가 있으면 그것을 쓰고, 아직 `projects.contract_*` 저장 칸이면 그 칸을 쓴다 | `grep -rn "contract" domain/revenue/index.ts domain/quotes/` 결과로 둘 중 어느 갈래인지 SUMMARY에 적는다. 둘 다 없으면 멈춤 |
| A-606 | 04.1 결재 모듈의 승인 함수(가칭 `domain/approvals/index.ts`의 승인)와 결재 문서 화면(가칭 `app/(app)/approvals/[id]/page.tsx`) | `git ls-files 'domain/approvals/*' 'app/(app)/approvals/*'`가 비면 멈춤 |
| A-609 · UA-610 | Phase 5 정산 결재 기안 함수(가칭 `domain/approvals/settlement.ts`, PM이 기안하고 대표가 승인 — 05-CONTEXT D-98)와 프로젝트 상세의 「정산 결재 올리기」 버튼 자리 | `grep -rn "정산 결재 올리기" app/`가 0건이면 멈춤 · `grep -rln "settlement\|정산 결재" domain/`이 0건이면 멈춤 |
| A-603 | 경영관리 조정 줄을 가리는 표시(줄 종류 칸 등) | `grep -n "adjust\|조정" domain/quotes/lines.ts db/schema/*quote*.ts` 결과를 SUMMARY에 적는다. 조정 줄 개념이 아직 없으면 제외 조건은 빈 조건으로 두고 SUMMARY에 적는다(멈추지 않음) |
| 06-02 · 06-05 · 06-07 | `waiveEvidence` · `resolveEvidenceStatus` · 발행 줄(`revenue_entries` kind issue) · `findLineLinks`(카드·구매 요청 포함) · 줄 DTO `door`·`rowActionBlock` | `grep -c "door\|rowActionBlock" domain/quotes/lines.ts`가 2 미만이면 멈춤 · `grep -rn "export async function findLineLinks" repositories/`가 0건이면 멈춤 |

## 열린 선택 — 사용한 기본값

아직 답이 없는 선택은 현재 기본값으로 짰다. 답이 바뀌면 아래 「고칠 곳」만 바뀐다.

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-16** | 대표 승인 앞에서 한 번 더 돈다. 막히면 승인이 비활성이다 | **Task 3 하나**: `domain/approvals/index.ts`의 정산 결재 승인 분기 한 곳, 승인 화면의 막힘 표시, 통합 테스트 한 케이스. must_haves의 `[O-16 기본값]` 한 줄 |
| O-14 (관련) | 미결 지출결의 행의 상태 글자 순서는 반려 > `선결제 · 증빙 없음` / `증빙 없음` > 결재 중이다(견적 줄 파생의 O-14 순서와 같은 결) | `domain/pre-settle-check/summary.ts`의 `OPEN_EXPENSE_STATUS_ORDER` 한 배열 |
| O-2 · O-19 · O-20 | 이 플랜과 무관하다. D-611은 `확인 전`을 미결로 세지 않고, 팀 비용 구매 요청은 견적 줄이 아니며, 요청자와 결제자가 같은지는 연결 판정에 쓰이지 않는다 | 해당 없음 |
| PS-1 (계획 판단) | 미제출·회수 문서는 세지 않는다. D-611이 나열한 상태(결재 중·반려·증빙 없음)가 아니기 때문이다 | `repositories/pre-settle-check.ts`의 상태 필터 한 줄 |
| PS-2 (계획 판단) | 연결은 살아 있는 것만 센다. 취소·회수된 지출결의, 취소된 구매 요청, 지운 카드 사용은 연결이 아니다(06-04가 남긴 견적 외 비용 줄은 여기서 미매칭으로 잡힌다) | `findLineLinks` 결과를 거르는 `domain/pre-settle-check/index.ts`의 한 함수 |
| PS-3 (계획 판단) | 매출 미입력 3차는 하나만 선다. PM이면서 매출 기록 권한도 있으면 바로 닫는 길인 `발행 줄 추가`를 보인다 | `pre-settle-check.tsx`의 3차 고르기 한 곳 |
| PS-4 (계획 판단) | 강행 결과를 담는 스냅숏 칸을 만들지 않는다. 결재 문서 화면은 현재 판정으로 결과 줄을 다시 그리고, 기안 시점의 요약은 행동 로그 상세에 남긴다(스키마 변경 없음) | `domain/approvals/settlement.ts`의 `recordAction` 상세 인자 · 승인 화면 |
| PS-5 (계획 판단) | 점검 목록은 막힘 판정과 같은 범위(프로젝트 전체)로 센다. 보는 사람의 문서 범위로 줄이지 않고, 금액 칸만 정보 항목 가시성에 따라 서버가 뺀다. `지출결의 열기`는 그 문서를 열 수 있는 사람에게만 선다. 이렇게 해야 건수와 목록이 어긋나지 않는다 | `domain/pre-settle-check/index.ts`의 DTO 조립 |
| PS-6 (계획 판단) | `계약 금액 — · 3차 고객 승인 전`은 `계약 금액 —` 글자 뒤에 3차 `고객 승인 전`을 두는 것으로 읽었다. 누르면 견적 차수 머리의 고객 승인 자리로 스크롤·포커스한다(Phase 4 D-56) | `pre-settle-check.tsx` 매출 줄 |

<probe_fallback>
coverage.json에서 이 플랜이 맡은 몫: **PROJ-06 — unclassified → unresolved (flagged)**. 자동으로 해소하거나 기각한 것은 없다. 대신 쓴 근거는 ROADMAP 기준 4 · D-611 · D-612 · D-613 · O-16 · A-601 · A-604 · UA-610이다. 10건 전체 표는 06-01 `<probe_fallback>`에 있다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 정산 상태 프로젝트의 반려 지출결의 1건이 섹션에 서고, 「정산 결재 올리기」가 화면과 서버 양쪽에서 막힌다</name>
  <files>repositories/pre-settle-check.ts, domain/pre-settle-check/index.ts, domain/pre-settle-check/summary.ts, domain/rules/register.ts, domain/settings/keys.ts, domain/approvals/settlement.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/pre-settle-check.tsx, test/unit/domain/pre-settle-check.test.ts, test/integration/pre-settle-check.test.ts, test/integration/leak-scan.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다. ⓪이 확인하고, 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 735-761행(S18 정본) · 280행(「Empty — 완료 전 점검」) · 284행(「Error — 섹션 로드」) · 299-300행(「막힘 — 정산 결재 올리기」 · 「막힘 — 대표 승인」) · 328-329행(「표시 — 완료 전 점검 섹션」 · 「표시 — 점검 행」)
    - .planning/phases/06-payment-evidence-cards/06-RESEARCH.md 293-297행(Pitfall 4) · .planning/phases/06-payment-evidence-cards/06-PATTERNS.md 60-66행(게이트 규칙은 DTO를 본 뒤 판정)
    - .planning/phases/05-expense-approval-leave/05-CONTEXT.md 34-38행(D-98 · D-100)
    - domain/rules/gate.ts · domain/rules/register.ts(`project.completed-lock` 등록 모양)
    - domain/settings/keys.ts 190-223행(강행 허용 세 키) · domain/settings/registry.ts 60-92행(`getSettingValue`) · test/unit/settings/registry-coverage.test.ts 99-111행(readBy 만료 강제)
    - domain/permissions/dto-registry.ts · domain/revenue/index.ts 138-146행(`registerDto` 선례)
    - app/(app)/projects/[id]/page.tsx · app/(app)/projects/[id]/quote-table.tsx 321-360행(`QuoteLedger` props) · 955-1020행(힌트 줄 · `RevenueSection` 자리)
    - A-609 재확인에서 찾은 Phase 5 기안 함수와 버튼 파일
  </read_first>
  <behavior>
    - `summarizePreSettle`: 미결 지출결의 1건, 강행 꺼짐 → 막힘 1, 부제 `정산 결재 전 · 막힘 1건` / 같은 입력에 강행 켜짐 → 막힘 0, 부제 `정산 결재 전 · 막힘 없음`, 결과 줄 `강행 · 미결 지출결의 1건` / 전부 빔 → 막힘 0, 결과 줄 없음
    - `project.pre-settle-check`: 막힘 0 → 허용 / 막힘 N → 막힘, 이유 `완료 전 점검 {N}건 · 점검 보기`
    - Phase 5 기안: 반려 지출결의가 있는 프로젝트 → 거부(게이트 이유 그대로) / 점검 계산이 예외를 던짐 → 거부(문서 없음) / 미결 0 → 기존 동작 그대로
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」을 재확인한다. 하나라도 없으면 멈춘다. A-601 · A-604 · A-603의 판정 결과를 SUMMARY에 적는다.

① **리포지토리.** `repositories/pre-settle-check.ts`에 프로젝트 하나의 지출결의 쪽 점검 입력을 한 번에 읽는 조회를 만든다. 담는 것은 번호 · 항목(견적 줄 이름) · 금액 · 결재 상태 · 지급 여부 · 선결제 표시 · 증빙 판정 입력이다. 기존 컨벤션대로 viewer를 첫 인자로 받지만, 조회 범위는 **프로젝트 전체**다(PS-5). 막힘 판정이 보는 사람마다 달라지면 안 되기 때문이다. 상태 필터는 결재 중·반려·결재 통과(증빙 판정용)만 넣고, 미제출·회수는 넣지 않는다(PS-1).

② **순수 요약.** `domain/pre-settle-check/summary.ts`에 `summarizePreSettle`을 만든다. 입력은 그룹별 항목 수와 강행 허용 세 값이고, 출력은 막힘 건수 · 부제 글자 · 그룹별 `allowed` · `forcedResultLine`이다. 같은 파일에 `OPEN_EXPENSE_STATUS_ORDER`(O-14 결의 순서)를 두고, 파일 머리 한 줄 주석에 "O-14 답이 바뀌면 이 배열도 함께 고친다"를 적는다.

③ **DTO.** `domain/pre-settle-check/index.ts`의 `computePreSettleCheck(viewer, projectId, tx?)`를 만든다. 먼저 보는 사람이 그 프로젝트를 볼 수 있는지 확인한다(기존 `visible`·`can` 선례). 강행 허용 세 키는 `getSettingValue`로 모두 읽는다. 이 태스크에서는 미결 지출결의 그룹의 결재 중·반려 갈래만 채우고, 나머지 두 그룹은 Task 2가 채운다. 증빙 판정은 06-01의 `resolveEvidenceStatus`를 부른다(판정을 다시 쓰지 않는다). `PRE_SETTLE_CHECK_DTO_SPEC`에 칸마다 정보 항목을 적고 `registerDto({ name: "PreSettleCheckDto", … })`로 등록한다. 금액 칸의 정보 항목은 이렇다: 지출결의 금액은 Phase 5가 등록한 항목, 실행가와 계약 금액은 `quote.amount`, 발행 합계와 차이는 `revenue.issued_amount`. `domain/settings/keys.ts`의 강행 허용 세 키에서 `readBy: { phase: "6" }` 줄을 지운다(registry-coverage 만료 강제).

④ **게이트.** `domain/rules/register.ts`에 `project.pre-settle-check`를 등록한다. ctx는 `{ blockedCount }` 하나다. 판정은 `project.completed-lock`과 같은 모양이고, 세 조건을 `check()` 안에 다시 쓰지 않는다.

⑤ **기안 훅.** Phase 5 기안 함수(실제 이름)가 문서를 만들기 전에, 같은 트랜잭션 안에서 `computePreSettleCheck` → `gate(…, "project.pre-settle-check")`를 부르게 한다. 점검 계산이 던진 예외는 잡지 않는다. 그러면 기안이 실패하고 문서가 생기지 않는다(fail-closed, 금지 항목 1).

⑥ **화면.** `page.tsx`는 A-601에서 확인한 상태일 때만 점검을 계산하고, 결과를 `QuoteLedger`의 새 prop으로 넘긴다. 계산이 실패하면 `{ status: "error" }`를 넘긴다(Task 3가 그린다). 「정산 결재 올리기」의 비활성 여부와 이유는 DTO의 막힘 건수와 게이트 이유 문자열에서만 가져온다. 화면 문구와 서버 거부 문구가 한 출처다(RESEARCH Pitfall 3). `pre-settle-check.tsx`는 `ui/table/Table` 읽기 표로 만든다. 제목에는 `id="pre-settle-check"`를 달아 `점검 보기` 3차의 포커스 목적지로 쓴다. `quote-table.tsx`는 힌트 줄 바로 아래에 섹션을 둔다. 새 색·간격은 만들지 않고 `docs/design/tokens.css` 토큰만 쓴다.

⑦ `test/integration/leak-scan.test.ts`에 `import "@/domain/pre-settle-check";`을 더한다.

⑧ **테스트.** 단위 테스트는 behavior의 요약과 게이트 갈래를 다룬다. 통합 테스트 `test/integration/pre-settle-check.test.ts`는 세 가지를 확인한다: 반려 1건이면 기안 함수를 직접 불러도 거부된다, 점검 리포지토리에 예외를 주입하면 기안이 거부되고 문서가 0건이다, 강행 허용을 켜면 기안이 성공한다. E2E `test/e2e/pre-settle-check.spec.ts`는 한 경로를 따른다: 정산 프로젝트 상세 → 섹션 `미결 지출결의 1건` · 행 `반려` → 「정산 결재 올리기」 `aria-disabled` + `완료 전 점검 1건 · 점검 보기` → 3차를 누르면 섹션 제목에 포커스.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/pre-settle-check.test.ts test/unit/settings/registry-coverage.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다. registry-coverage가 빨가면 세 키의 readBy를 지우지 않았거나 한 키를 읽지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-601 상태 대응 · A-604 갈래 · A-603 조정 줄 판정 · 가칭과 실제 이름의 대응표)가 SUMMARY에 있다
    - 통합 테스트가 "Phase 5 기안 함수 직접 호출 거부"와 "점검 예외 주입 시 거부 + 문서 0건"을 각각 단언한다
    - `grep -c 'readBy: { phase: "6" }' domain/settings/keys.ts`가 이 플랜 전보다 3 줄었다(강행 허용 세 키)
    - E2E가 버튼의 `aria-disabled="true"`와 이유 문자열 = 서버 거부 이유 문자열을 단언한다
  </acceptance_criteria>
  <done>반려 지출결의 하나가 리포지토리 → DTO → 게이트 → Phase 5 기안 거부 → 섹션과 버튼 막힘까지 한 판정으로 실제 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 세 그룹 전부(D-611 증빙 갈래 · D-612 · D-613) · 강행 허용 셋 · 매출 줄 · 강행 결과 줄과 행동 로그</name>
  <files>repositories/pre-settle-check.ts, domain/pre-settle-check/index.ts, domain/pre-settle-check/summary.ts, domain/approvals/settlement.ts, app/(app)/projects/[id]/pre-settle-check.tsx, test/unit/domain/pre-settle-check.test.ts, test/integration/pre-settle-check.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md 52-55행(D-611~D-613 원문)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 374-383행(증빙 면제의 자리 · 면제가 선결제를 이긴다) · 752-758행(막힘과 미결의 구분 · 강행 결과 줄)
    - repositories/quote-line-links.ts(`findLineLinks` — 06-03 · 06-06 · 06-07) · domain/quotes/lines.ts(줄 DTO `door` · `rowActionBlock` · 실행가 · 줄 상태)
    - domain/evidence-reviews/index.ts(`resolveEvidenceStatus`) · domain/evidence-reviews/prepaid.ts(06-02)
    - A-604 재확인에서 고른 계약 금액 출처 · domain/revenue/index.ts(발행 줄 합계)
    - domain/money/index.ts(`sumKrw` · `diffKrw` — 06-01)
    - domain/action-log/record.ts(`recordAction` 상세 인자)
  </read_first>
  <behavior>
    - D-611: 결재 통과 · 증빙 0 · 면제 아님 · 지급 전 → `증빙 없음` / 같은데 선결제 · 지급 뒤 → `선결제 · 증빙 없음` / 면제 → 행 없음(선결제든 지급 뒤든) / 결재 통과 · 증빙 있음 · 지급 전 → 행 없음 / 증빙 필수 off · 증빙 0 · 면제 아님 → `증빙 없음`(설정과 무관)
    - D-611 상태 글자: 반려이면서 증빙 0 → `반려`(`OPEN_EXPENSE_STATUS_ORDER`) / 결재 중이면서 증빙 0 → `증빙 없음`
    - D-612: 실행가 0 → 제외 / 취소 줄 → 제외 / 조정 줄 → 제외 / 취소된 지출결의만 걸린 줄 → 미매칭(PS-2) / 카드 사용 1건 연결 → 제외 / 구매 요청 1건 연결 → 제외 / 연결 합 ≠ 실행가 → 제외(막지 않음)
    - D-613: 발행 줄 0 → 항목 1 / 발행 줄 ≥ 1 → 그룹 빔 / 매출 줄 차이 = `diffKrw(계약, 발행 합계)`, 차이 ≠ 0이어도 막힘 아님 / 계약 금액 없음 → 계약 `null`
    - 강행: 세 키 조합 8가지 중 대표 넷(전부 꺼짐 · 전부 켜짐 · 하나씩)에서 막힘 건수와 결과 줄
  </behavior>
  <action>
`repositories/pre-settle-check.ts`에 조회 둘을 더한다. 하나는 미매칭 판정용 줄 목록(실행가 ≠ 0 · 취소 아님 · 조정 아님)이고, 다른 하나는 발행 줄 합계와 건수다. 줄의 연결은 `findLineLinks`를 **한 번** 불러 받고, 연결 정의를 새로 쓰지 않는다. 살아 있는 연결만 세는 거르기는 `domain/pre-settle-check/index.ts`의 함수 하나에 둔다(PS-2).

`computePreSettleCheck`에 D-611 증빙 갈래를 채운다. 면제는 `resolveEvidenceStatus`의 결과로 판정하고, 선결제 표시는 Phase 5 칸에서 읽는다. 미매칭 그룹과 매출 미입력 그룹도 채운다. 미매칭 행에는 줄 DTO와 같은 함수로 만든 `door`와 `rowActionBlock`을 싣는다. S14 행 행동과 한 출처여야 하므로, 06-07이 만든 판정 함수를 부르고 새로 쓰지 않는다. 매출 줄은 계약 금액(A-604 갈래) · 발행 합계(`sumKrw`) · 차이(`diffKrw`)로 만들고, 금액 셈은 `domain/money` 밖에서 하지 않는다(lint `plant8/money-boundary`). 계약 금액이 없으면 `null`이다.

`summarizePreSettle`에 세 그룹과 강행 셋의 모든 조합을 반영하고, `forcedResultLine`은 항목이 있는 강행 그룹을 ` · `로 잇는다. Phase 5 기안 함수는 강행으로 통과할 때 `recordAction` 상세에 기안 시점 요약(그룹별 건수 · 강행 그룹)을 남긴다(PS-4, AS2 결 — 새 행동 종류 없이 Phase 5 기안의 기존 행동 종류). 기안 확인 자리(Phase 5 확인 모달 또는 기안 화면, UA-610에서 확인)에는 결과 줄을 보인다.

`pre-settle-check.tsx`에 그룹 셋 · 행 · 매출 줄을 그린다. 막힘 그룹 머리글 건수는 `--danger`, 강행 허용 머리글 꼬리는 `--muted`, 차이 ≠ 0은 `--warning`이다. 상태 글자 색은 `app/(app)/status-display.ts` 매핑을 거친다(Task 3에서 값을 더한다). 금액 표시는 기존 금액 셀 포맷을 쓴다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/pre-settle-check.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/quote-line-links.test.ts test/integration/issue-requests.test.ts test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts test/e2e/issue-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 단위 테스트가 behavior의 D-611 다섯 갈래, D-612 일곱 갈래, D-613 네 갈래, 강행 넷을 각각 개별 케이스로 단언한다
    - 통합 테스트가 두 흐름을 단언한다. 하나는 "증빙 면제 뒤 점검을 다시 읽으면 그 행이 빠진다(06-02 `waiveEvidence` 호출)", 다른 하나는 "발행 줄 1건 저장 뒤 매출 미입력 그룹이 빈다"이다
    - 통합 테스트가 강행 기안의 행동 로그 상세에 그룹별 건수가 있음을 단언한다
    - `grep -rn "findLineLinks" domain/pre-settle-check/`가 1건 이상이다. `repositories/pre-settle-check.ts`에는 카드 사용·구매 요청 표 조인이 없다(연결은 `findLineLinks` 한 곳이 맡는다 — `grep -v '^\s*//' repositories/pre-settle-check.ts | grep -c "corpCardUsages\|purchaseRequests"`가 0 — 주석 줄은 세지 않는다)
  </acceptance_criteria>
  <done>세 그룹이 D-611~D-613 그대로 계산되고, 강행 허용 셋이 막힘 건수와 결과 줄을 가르며, 강행 기안이 로그에 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [O-16 기본값] 대표 승인 앞 재점검 · S18 표시 상태(권한별 3차 · 1024 규칙 · 오류 · 빈 · 긴 이름)</name>
  <files>domain/approvals/index.ts, app/(app)/approvals/[id]/page.tsx, app/(app)/status-display.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/pre-settle-check.tsx, app/(app)/projects/[id]/project-detail.module.css, test/integration/pre-settle-check.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 759행(O-16) · 761행(폰 · 1024 미만) · 809행(S18 권한 행) · 1050-1057행(S18 considerations) · 1095행(O-16 선택지)
    - A-606 재확인에서 찾은 04.1 승인 함수와 결재 문서 화면
    - app/(app)/projects/[id]/quote-table.tsx의 `addIssued`(04 S6 3차) · 06-05가 더한 발행 요청 줄 추가 함수 · 1024 미만 보기 전용 판정(DR-36)
    - app/(app)/status-display.ts(06-01 매핑) · ui/status-tag/StatusTag.tsx(`text` 변형)
  </read_first>
  <behavior>
    - 04.1 승인: 정산 결재 문서 · 막힘 1 → 거부 `완료 전 점검 1건 · 점검 보기` / 막힘 0 · 강행 그룹 있음 → 승인 / 다른 문서 종류 → 기존 동작 그대로(점검을 부르지 않는다)
    - 3차 고르기: 프로젝트 쓰기 권한 있음 · 온라인구매 줄 → `구매 요청` / 쓰기 권한 있음 · 그 밖 → `지출결의 올리기` / 거래처 없음 → S14 막힘 / 쓰기 권한 없음 → `담당 PM {이름}`
    - 매출 3차: PM만 → `발행 요청 추가` / 매출 기록 권한만 → `발행 줄 추가` / 둘 다 → `발행 줄 추가`(PS-3) / 1024 미만 → 없음
  </behavior>
  <action>
**[O-16 기본값]** 04.1 승인 함수(실제 이름)의 정산 결재 문서 분기에서, 승인 기록 전에 같은 트랜잭션 안에서 `computePreSettleCheck` → `gate(…, "project.pre-settle-check")`를 부른다. 다른 문서 종류에는 손대지 않는다. 결재 문서 화면(실제 파일)에서는 정산 결재일 때 서버가 같은 DTO로 `승인` 1차의 비활성 여부와 이유를 정하고, 3차 `점검 보기`가 `/projects/{id}#pre-settle-check`로 가게 한다. 강행 그룹이 있으면 문서에 결과 줄 `강행 · …`을 보인다(PS-4). 대표 = 전부 읽기(D-601)이므로, 대표 화면의 행 3차는 담당 표기로 바뀐다.

**표시 상태.** `pre-settle-check.tsx`를 아래처럼 채운다.
- 세 그룹이 비면 `미결 없음`(`--success`) 한 줄을 보인다. 매출 줄은 남는다.
- 오류 prop이면 `점검 불러오지 못함 · 다시 시도`를 보인다(「다시 시도」 = 2차, 누르면 다시 읽는다). 이때 「정산 결재 올리기」는 비활성 + `점검 결과 없음 · 다시 시도`다.
- 막힘 0이면 부제 `막힘 없음`을 보인다.
- 비어 있는 그룹만 접는다.
- 행 3차는 서버가 권한별로 보낸 값만 그리고, 클라이언트는 권한을 추론하지 않는다.
- 매출 3차는 원장이 이미 가진 줄 추가 함수(`addIssued` · 06-05의 요청 줄 추가)를 prop으로 받아 **같은 onClick**을 부른다.
- 1024 미만 판정은 Phase 4 편집 표가 보기 전용으로 바뀌는 판정을 그대로 쓴다(새 판정을 만들지 않는다).
- 긴 항목 이름은 말줄임 + `title`로 보인다.

`status-display.ts`에 점검 행 상태 네 값(`결재 중` `--accent` · `반려` `--danger` · `증빙 없음` `--danger` · `선결제 · 증빙 없음` `--danger`)을 더한다. 색은 UI-SPEC Color 매핑표와 대조한다. `project-detail.module.css`에는 섹션 간격과 말줄임만 넣고, 값은 `docs/design/tokens.css` 토큰만 쓴다. 폰에서는 읽기 표 P1 = 항목 · 금액 · 상태이고, 행동 3차는 P1 셀 2행이다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 두 가지를 단언한다. 하나는 "막힘 1인 정산 결재를 04.1 승인 함수로 직접 승인하면 거부된다", 다른 하나는 "지출결의 승인은 점검을 부르지 않는다(점검 리포지토리 호출 0회)"이다
    - E2E가 다섯 가지를 단언한다: ⑴ 세 그룹이 비면 `미결 없음` + 매출 줄 ⑵ 폭 1023에서 `발행 요청 추가`·`발행 줄 추가` 요소 0개, 1280에서 1개 ⑶ 쓰기 권한 없는 계정의 미매칭 행에 `담당 PM` 글자가 있고 버튼 요소는 0개 ⑷ 점검 로드 실패를 강제하면 버튼 `aria-disabled` + `점검 결과 없음 · 다시 시도` ⑸ 대표 계정의 정산 결재 문서에서 `승인` 비활성 + `완료 전 점검 {N}건 · 점검 보기`
    - Phase 5·04.1의 기존 결재 통합 테스트(`git ls-files 'test/integration/*approv*' 'test/integration/*settle*'`로 찾은 것)가 녹색이다
  </acceptance_criteria>
  <done>대표 승인 앞에서도 같은 판정이 막고, S18 섹션이 빈·오류·강행·권한·폭 갈래를 UI-SPEC 문구 그대로 보인다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PM 브라우저 → Phase 5 정산 결재 기안 | 버튼 비활성과 관계없이 기안 함수가 점검 게이트를 부른다 |
| 대표 브라우저 → 04.1 승인 | 정산 결재 승인 앞에서 같은 판정을 한 번 더 한다(O-16) |
| domain/pre-settle-check → 화면 DTO | 프로젝트 전체 범위로 센 목록이 보는 사람에게 갈 때 금액 칸은 정보 항목으로 걸러진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-48 | Elevation of Privilege | 기안 버튼을 우회한 Phase 5 기안 함수 직접 호출 | high | mitigate | `project.pre-settle-check`를 기안 domain 함수 안, 같은 트랜잭션에서 부른다. 직접 호출 거부 통합 테스트 |
| T-06-49 | Elevation of Privilege | 대표 승인 경로 우회(O-16) | high | mitigate | 04.1 승인 함수의 정산 결재 분기에서 같은 게이트를 부른다. 직접 승인 거부 통합 테스트 |
| T-06-50 | Denial of Service → 무결성 | 점검 계산 실패가 기안을 통과시킴(fail-open) | high | mitigate | 점검 예외를 삼키지 않는다(fail-closed). 예외 주입 통합 테스트로 문서 0건을 단언한다. 화면은 `점검 결과 없음 · 다시 시도`로 비활성 |
| T-06-51 | Information Disclosure | 점검 행의 금액 · 발행 합계가 볼 수 없는 사람에게 감 | medium | mitigate | `PreSettleCheckDto`를 `registerDto`로 칸별 정보 항목과 함께 등록한다. `leak-scan.test.ts`가 계급별 케이스를 자동으로 만든다 |
| T-06-52 | Repudiation | 강행 기안 | medium | mitigate | 기안 시점의 그룹별 건수와 강행 그룹을 기존 행동 종류의 상세에 남긴다(PS-4). 통합 테스트로 확인한다 |
| T-06-53 | Tampering | 점검과 기안 사이에 문서가 끼어듦(경합) | low | mitigate | 점검과 기안이 같은 트랜잭션이고, 대표 승인 앞에서 다시 점검한다(O-16) |
| T-06-54 | Tampering | 강행 허용 설정 변경 | low | accept | 설정 화면은 관리자 전용이고 기존 설정 저장이 이미 행동 로그를 남긴다(§7-2). 이 플랜은 읽기만 한다 |
</threat_model>

<verification>
- 단위 1(+ registry-coverage · leak-scan-coverage) · 통합 1(+ quote-line-links · issue-requests · evidence · leak-scan · 기존 결재 통합) · E2E 1(+ issue-requests · quote-table · revenue-section) 녹색
- `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 S18 DOM을 실측 감사(backstop 3건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 정산 결재는 막힘 0이거나 남은 그룹이 모두 강행 허용일 때만 올라가고, 대표 승인도 같은 규칙을 따른다
- PM이 섹션만 보고 무엇이 몇 건 남았는지, 그 줄에서 다음에 무엇을 누를지 안다
- 점검이 실패하면 기안이 막힌다(fail-closed)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 게이트 규칙 | `project.pre-settle-check` |
| 함수(domain) | `computePreSettleCheck` · `summarizePreSettle` · `forcedResultLine` |
| 상수 | `OPEN_EXPENSE_STATUS_ORDER` |
| DTO | `PreSettleCheckDto`(`PRE_SETTLE_CHECK_DTO_SPEC`, `registerDto`) |
| 리포지토리 | `repositories/pre-settle-check.ts` |
| 변경된 기존 심볼 | 강행 허용 세 키의 readBy 표시 제거 · Phase 5 정산 결재 기안 함수(점검 게이트 · 강행 로그) · 04.1 승인 함수(정산 결재 분기 재점검) · `QuoteLedger` props(점검 DTO · 줄 추가 함수 전달) · `status-display.ts`(점검 행 네 값) |
| 화면 | `app/(app)/projects/[id]/pre-settle-check.tsx`(S18) · 「정산 결재 올리기」 막힘 · 결재 문서 화면의 승인 막힘과 강행 결과 줄 |
| 테스트 | `test/unit/domain/pre-settle-check.test.ts` · `test/integration/pre-settle-check.test.ts` · `test/e2e/pre-settle-check.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-09-SUMMARY.md` when done
</output>
