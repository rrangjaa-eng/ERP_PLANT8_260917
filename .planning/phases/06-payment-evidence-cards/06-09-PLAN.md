---
phase: 06-payment-evidence-cards
plan: 09
type: execute
wave: 5
depends_on: ["06-07"]
files_modified:
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/rights.ts
  - domain/corp-card-usages/link-targets.ts
  - repositories/corp-card-usages.ts
  - app/(app)/cards/page.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/link-picker.tsx
  - app/(app)/cards/actions.ts
  - app/(app)/cards/actions.registry.ts
  - test/unit/domain/card-usage-rights.test.ts
  - test/integration/corp-card-usages-proxy.test.ts
  - test/e2e/card-proxy.spec.ts
autonomous: true
requirements: [EXP-16, EXP-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "대리 등록 권한(`cards.proxy` write)이 있는 사람이 `/cards?new=1`에서 활성 카드 전부 중 남의 카드(또는 팀 카드)를 고르면 카드 아래 `경영관리 등록 · 카드 소지자 {이름}`이 서고, 저장된 건은 목록·프로젝트 상세의 등록 칸에 `경영관리 등록` + 2행 등록자·날짜로 보인다(EXP-16)"
    - "대리 등록 권한이 없는 사람이 남의 카드로 저장을 요청하면 서버가 거부한다 — `expenses.payments` · `cards.purchases`만 있는 계정도 대신 통과하지 않는다(EXP-16, D-601 결)"
    - "대리 등록은 결재가 없고 행동 로그에 `document_create`(등록 경로 proxy)로 남는다(D-608) — 담당 PM 알림은 Phase 7이다"
    - "대리 등록으로 팀 카드를 고르고 `팀 비용`에 이으면 `사용한 사람` 칸이 서고, 후보는 **사용일에** 그 카드 팀 소속이던 사람 전부(`teamAtDate` = 카드 팀)다 — 사용일 뒤 옮긴 사람은 `{이름} · 지금 {팀}`, 퇴사자는 `{이름} · 퇴사`로 들고, 사용일 뒤 들어온 사람은 없다. 개인 카드는 후보가 소지자 한 사람이라 텍스트다"
    - "팀 비용의 팀은 사용한 사람의 사용일 소속으로 서버가 정한다 — 소속이 없으면 1차가 막히고 임의 팀으로 떨어지지 않는다(EXP-07)"
    - "완료(정산) 프로젝트는 대리 등록 권한자에게만 `견적 외 비용` 연결로 고를 수 있고(D-47), 견적 줄 연결은 여전히 고를 수 없다"
    - "[O-11 기본값] 카드 사용 건의 수정·삭제는 등록한 사람과 대리 등록 권한자만 할 수 있고, 완료 프로젝트 줄에 이어진 건은 대리 등록 권한자만, 구매 완료로 생긴 건(`registered_via = purchase`)은 연결을 바꿀 수 없고 삭제가 없다 — 판정은 `domain/corp-card-usages/rights.ts` 한 함수에서 나온다"
    - "수정 모드(`?editId=`)의 카드는 그 건에 저장된 카드의 읽기 텍스트다 — 여는 사람의 카드 옵션을 계산하지 않으므로 옵션이 0장이어도 폼이 서고, 카드 값을 바꿔 보낸 수정 요청은 서버가 거부한다(구매 완료 건의 카드 고치기는 06-12)"
    - "**B-1:** 수정 저장에서 연결이 바뀌면 한 트랜잭션에서 `lockQuoteLines`(새 줄) → `findLineLinks` → `card.dual-link-block` → UPDATE 순서로 돈다. 결제 합계·증빙 종류가 바뀌면 공급가·부가세를 서버가 다시 역산한다 — `version`이 어긋나면 거부된다(D-609)"
    - "「06-03 tx 규약」을 따른다 — 대리 등록 판정(`can(viewer, \"cards.proxy\", \"write\")` · 활성 카드 전부 · 사용한 사람 후보 `usedByCandidates` · `teamAtDate` · 완료 프로젝트 판정)은 06-05의 `precheckCardUsage` 대리 갈래가 트랜잭션 전에 하고 `createCardUsage`의 `runCreate`는 `pre`만 읽는다. 수정 · 삭제도 권리 판정(`cardUsageRights` · `can`) · 세율 `loadTaxRates(usedOn)` · 카드 조회를 트랜잭션 전에 끝내고 트랜잭션 몸통(`runUpdate` · `runDelete`)은 tx 리포지토리 · 순수 함수 · 게이트만, 행동 로그는 몸통 안 `recordAction(…, { tx })`(같은 tx — r5 S-F1, CROSS E-3 follow-on)"
    - "S9 error: 「Error — 카드 사용 폼 칸」 · 「막힘 — 카드 사용 폼」(서버 이중 연결 거부 · 소속 없음 · 수정 모드 증빙 종류 포함) · 「막힘 — 카드 사용 폼 · 증빙 종류 자동 채움 없음」이 각 자리에 선다"
    - "S9 partial: 입력이 있는데 `취소 Esc`면 「확인 — 입력 버리기」가 열리고, 대리 등록 팀 카드에서 사용일을 바꿔 고른 사람이 새 후보에 없으면 칸이 빈다(빈 칸 막힘), 수정 모드에서 저장된 증빙 종류가 지금 옵션에 없으면 `—` + 막힘이다"
    - "S9 zero-one-many: 새 건 카드 0장 = 폼 대신 「Empty — 카드 사용 목록」 카드 0장 갈래 · 1장 = 텍스트 · 여러 장 = `Select` / 수정 모드 카드 = 저장된 카드 읽기 텍스트 / 사용한 사람 후보 1명 = 텍스트 · 여럿 = `Select` 기본값 없음 / 증빙 종류 옵션 0개 = `카드에 쓸 증빙 종류 없음 · 코드표 세금 규칙은 관리자`"
    - "[H-4] 수정 모드 2차 `카드 사용 삭제`(행동 줄 오른쪽 끝 — 1차와 떨어진 자리)는 모달 없이 그 건을 보관하고(`archived_at` · `archived_by` — 물리 삭제 없음, ADMN-12) `/cards`로 이동 + 토스트 `카드 사용 삭제 · 1,240,000` + 3차 `되돌리기`(보관 해제 — 같은 건이 목록에 돌아온다)다. 행동 로그는 삭제 `document_delete` · 되돌리기 `restore`. 구매 완료로 생긴 건에는 이 버튼이 렌더되지 않는다(UI-SPEC Copywriting 「즉시 — 카드 사용 삭제(S9, rev 9 H-4 — 모달 없음)」)"
    - "[H-4] 새 건 저장 토스트 `카드 사용 등록 · 1,240,000`에 3차 `되돌리기`(= 그 건 삭제 → 보관함)가 붙는다 — 그 건의 삭제 권리(`cardUsageRights`, O-11)가 있을 때만. 수정 저장 토스트 `카드 사용 저장 · 1,240,000`에는 되돌리기가 없다(다시 고치면 된다). 되돌리기도 저장과 같은 서버 판정을 지나고, 막히면 토스트가 오류 모양(닫을 때까지)으로 바뀌어 그 판정의 막힘 문구를 그대로 보인다(Copywriting 「SUCCESS — 토스트」 3차 `되돌리기`)"
    - "[H-4] 보관 해제(`restoreCardUsage`)는 삭제와 같은 권리 판정(트랜잭션 전)과 B-1 순서(`lockQuoteLines` → `findLineLinks` → `card.dual-link-block` → UPDATE)를 지난다 — 지운 사이 그 줄에 지출결의가 이어졌으면 거부되고 토스트가 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄`을 보인다. 카드 사용 표는 관리자 보관함(`ARCHIVABLE_TABLES`)에 등록하지 않는다 — 그 복원 경로는 이중 연결 게이트를 지나지 않는다(D-609)"
    - "[M-4] 대리 등록 권한자의 새 건 기본값 — 카드 옵션이 활성 카드 전부라 직전에 남의 카드로 등록했으면 그 카드로 채워지고 카드 아래 `경영관리 등록 · 카드 소지자 {이름}` 힌트가 그대로 선다. 직전 연결이 완료 프로젝트의 `견적 외 비용`이면 프로젝트도 남는다 — 06-07 `cardUsageFormDefaults`의 프로젝트 판정이 Task 2의 권한자 예외를 그대로 쓴다(UI-SPEC S9 「새 건의 기본값(rev 9, M-4)」)"
    - "목록 행을 누르면 등록한 사람·대리 등록 권한자는 `?editId=`로, 그 밖의 사람은 폰 행 시트(보기 전용)로 간다 — 등록 필터(`경영관리 등록`)는 권한자에게만 보인다"
    - statement: "S9 overflow: 활성 카드 30장 이상인 대리 등록 권한자의 카드 `Select`(200)와 긴 카드 이름이 말줄임으로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S9 long-text: 메모·`견적 외 비용` 항목 480 칸과 긴 가맹점 이름이 가맹점 줄의 3차 `바꾸기`를 밀지 않는다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "대리 등록 권한(`cards.proxy`) 없이 남의 카드 사용을 등록·수정·삭제할 수 있는 경로를 만들어서는 안 된다 — 다른 메뉴 권한(지급·구매)으로 대신 통과하지 않는다"
      status: resolved
      verification: test
    - statement: "사용한 사람을 사용일 기준 후보 밖의 사람으로 저장해서는 안 된다 — 팀 비용 귀속이 사람 선택으로 조작되는 경로다"
      status: resolved
      verification: test
    - statement: "[H-4] 카드 사용 행을 물리 삭제해서는 안 되고, 보관된 건을 이중 연결 게이트 없이 되살려서는 안 된다(ADMN-12 · D-609)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/corp-card-usages/rights.ts"
      provides: "[O-11] 카드 사용 수정·삭제 권리 판정 한 함수"
      exports: ["cardUsageRights"]
    - path: "domain/corp-card-usages/index.ts"
      provides: "`precheckCardUsage` 대리 등록 갈래(트랜잭션 전) · 사용한 사람 후보 · 수정 · 삭제(= 보관) · 보관 해제(토스트 `되돌리기` — 게이트 재통과, H-4)(판정은 트랜잭션 전)"
      exports: ["usedByCandidates", "updateCardUsage", "deleteCardUsage", "restoreCardUsage"]
    - path: "test/integration/corp-card-usages-proxy.test.ts"
      provides: "대리 등록 권한 · 사용한 사람 후보 · 수정·삭제 권리 · 연결 변경 잠금"
      min_lines: 40
  key_links:
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/corp-card-usages/rights.ts"
      via: "수정·삭제가 권리 판정 한 함수를 지난다"
      pattern: "cardUsageRights"
    - from: "domain/corp-card-usages/index.ts"
      to: "domain/org/index.ts"
      via: "사용한 사람 후보와 팀 비용 팀이 사용일 소속 판정에서 나온다"
      pattern: "teamAtDate"
    - from: "domain/corp-card-usages/index.ts"
      to: "repositories/quote-line-links.ts"
      via: "연결 변경도 잠금 → 게이트 순서(B-1)"
      pattern: "lockQuoteLines"
    - from: "app/(app)/cards/actions.ts"
      to: "domain/corp-card-usages/index.ts"
      via: "토스트 3차 `되돌리기` → 보관 해제(삭제 토스트) · 보관(등록 토스트) — 저장과 같은 서버 판정(H-4)"
      pattern: "restoreCardUsage"
---

<objective>
06-05 · 06-07의 카드 사용 경로 위에 경영관리의 **대리 등록**(EXP-16)과 카드 사용 건의 **수정·삭제**(O-11)를 더한다 — 남의 카드 선택, `경영관리 등록` 표시, 사용일 기준 사용한 사람 후보, 완료 프로젝트의 견적 외 비용, 수정 모드(연결 변경도 잠금 뒤 게이트), 삭제(= 보관 — 모달 없이 즉시 + 토스트 `되돌리기`, H-4), 대리 등록 권한자의 새 건 기본값(M-4).

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 자동 결제·정기 결제처럼 PM이 모르는 카드 사용이 원장에 들어와야 「모든 나가는 돈」이 성립한다. 권한 경계가 넓어지는 곳이라 권리 판정을 한 함수로 모은다.

Output: 대리 등록 갈래 · 수정·삭제(보관) · 보관 해제 · 권리 판정 함수, 폼 수정 모드, 카드 사용 등록 · 저장 · 삭제 토스트(등록 · 삭제에 `되돌리기`), 단위 1 · 통합 1 · E2E 1.

**규모 근거:** `files_modified` 13개, `estimate.tokens` 110000(`confidence: low`, 표본 0 — 보정 계수 1). 디자인 검토 반영(H-4 보관 · 보관 해제 · 토스트 두 개 · `repositories/corp-card-usages.ts` · M-4 대리 갈래 케이스)으로 10k 늘었다. 예산 100k를 넘는다 — 재분할 방아쇠: Task 2 끝에서 컨텍스트 60%를 넘으면 Task 3을 새 플랜으로 넘기고 SUMMARY에 적는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-05-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-07-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-09 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-07 | `createOutOfQuoteLine` · `lockQuoteLines` · `link-picker.tsx` | `grep -c "createOutOfQuoteLine" domain/quotes/lines.ts` 0이면 멈춤 · `git ls-files 'app/(app)/cards/link-picker.tsx'` 비면 멈춤 |
| A-601 | 프로젝트 완료 상태 값(`settled` — main 4값) | `grep -n "settled" db/schema/projects.ts` 0건이면 멈춤, 값이 바뀌었으면 실제 값으로 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(입력 버리기 — 삭제는 모달 없음, H-4) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| 06-05 · 06-07 | 보관 칸 `archived_at` · `archived_by`(H-4 기반) · `cardUsageFormDefaults(viewer, today, entry)`(M-4) | `grep -c "archived_at" db/schema/corp-card-usages.ts` 0이면 멈춤 · `grep -c "cardUsageFormDefaults" domain/corp-card-usages/index.ts` 0이면 멈춤 |
| Toast | `ui/toast/Toast.tsx`의 `actionLabel` · `onAction` · `tone="error"`(되돌리기 · 막힘 오류 모양) | `grep -c "actionLabel" ui/toast/Toast.tsx` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-11 | 등록한 사람·대리 등록 권한자만 수정·삭제, 완료 프로젝트 줄은 대리 등록 권한자만, 구매 완료 건은 연결 변경·삭제 없음 | `domain/corp-card-usages/rights.ts`의 `cardUsageRights` 한 함수 + 단위 테스트 |
| 계획 판단 | 카드 사용 건을 지워도(보관 — 등록 토스트 `되돌리기` 포함) 그 건이 만든 견적 외 비용 줄은 지우지 않는다 — 남은 줄은 완료 전 점검(06-19)의 미매칭 견적 줄로 드러나는 것이 맞는 신호다 | `deleteCardUsage` |
| 계획 판단(H-4) | 카드 사용 보관은 `domain/archive` · `ARCHIVABLE_TABLES`(관리자 보관함)에 등록하지 않고 `deleteCardUsage` · `restoreCardUsage`만 쓴다 — 관리자 보관함의 `restore`는 대상 행의 보관 칸만 비우고 이중 연결 게이트(D-609)를 지나지 않는다. 토스트가 사라진 뒤 지운 건을 되살리는 길은 없다(새로 등록) | `restoreCardUsage` · `repositories/archive.ts`(등록 안 함) |

<assumption_delta_decision>
`detected: false`. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-16 · EXP-07 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 3 · D-608 · D-47 · O-11. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 경영관리가 남의 개인 카드 자동 결제 한 건을 대리 등록하고, 담당 PM의 프로젝트 상세에 「경영관리 등록」으로 보인다</name>
  <files>domain/corp-card-usages/index.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, test/integration/corp-card-usages-proxy.test.ts, test/e2e/card-proxy.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S9의 카드(새 건 자격) · 「새 건의 기본값(rev 9, M-4)」 · 사용한 사람 · 완료 프로젝트 문단, 권한 표 S9 행(rev 9 — rev 8 줄 번호는 낡았다) · Copywriting 「표시 — 경영관리 등록」
    - domain/corp-card-usages/index.ts(06-05 · 06-07 — 카드 자격 판정 자리는 `precheckCardUsage`, 트랜잭션 몸통은 `createCardUsage`의 `runCreate`)
    - 06-03-PLAN.md must_haves.truths의 「tx 규약」 줄 · `grep -L "DbOrTx" repositories/*.ts`(tx 인자 없는 리포지토리 파일을 저장소에서 다시 뽑는다 — main 4347517 기준 `repositories/permissions.ts` · `corp-cards.ts` · `users.ts`는 파일 전체 `tx` 없음. `team-memberships.ts`는 `teamLeadCandidatesAtDate` ~88만 `tx?`를 받아 `grep -L` 목록에서 빠지지만 `teamAtDate`가 읽는 `findMembershipAtDate` · `findMembershipsAtDate`는 `tx` 인자가 없다 — `grep -n "export async function" repositories/team-memberships.ts`로 함수 단위 확인, r5 R4-F3)
  </read_first>
  <behavior>
    - `cards.proxy` write 있음 · 남의 개인 카드 · 견적 줄 연결 → 저장, `registered_via = proxy`, 사용한 사람 = 소지자, `document_create`
    - `cards.proxy` 없음 · 남의 카드 → ForbiddenError
    - `expenses.payments` · `cards.purchases`만 있는 사람 · 남의 카드 → ForbiddenError(대신 통과 없음)
    - [M-4] 권한자가 직전에 남의 개인 카드 C로 등록 → `cardUsageFormDefaults`의 카드 = C(권한자 옵션 = 활성 카드 전부) · 폼 카드 아래 `경영관리 등록 · 카드 소지자 {이름}` / 같은 C가 비활성이 됨 → 카드 비움(옵션 여럿)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

`precheckCardUsage`(06-05 — 트랜잭션 밖)의 카드 자격 판정에 대리 등록 갈래를 더한다: `can(viewer, "cards.proxy", "write")`면 활성 카드 전부가 자격이고, 고른 카드가 본인 자격 밖이면 `pre.registeredVia = "proxy"`(D-608 — 결재 없음). 개인 카드면 `pre.usedByUserId` = 소지자(입력 없음). 권한 · 카드 조회는 전역 `db`라 여기서만 하고, `createCardUsage`의 `runCreate`는 `pre`의 등록 경로 · 사용한 사람을 그대로 INSERT한다 — `runCreate` 안에 권한 판정을 두지 않는다(06-03 tx 규약, CROSS E-3 follow-on). 먼저 behavior 세 줄 통합 테스트를 쓰고 RED를 확인한 뒤 구현한다. 폼은 대리 등록 권한자에게 카드 옵션으로 활성 카드 전부를 보내고, 남의 카드를 고르면 카드 아래 `Form.Hint` `경영관리 등록 · 카드 소지자 {이름}`을 세운다(서버가 판정한 값을 받아 그린다). 06-05 · 06-07이 만든 목록·상세 섹션의 등록 칸 표시는 그대로 쓴다. **새 건 기본값(M-4):** 06-05 · 06-07의 `cardUsageFormDefaults`는 「지금 옵션에 있을 때만」을 폼 옵션과 같은 함수로 판정하므로, 권한자의 옵션을 활성 카드 전부로 넓히면 남의 카드 기본값이 저절로 들어온다 — 따로 판정하지 않는다. 그 카드가 남의 카드면 위 힌트가 그대로 선다(S9 「새 건의 기본값」).

통합 테스트(신규 `corp-card-usages-proxy.test.ts`)에 behavior 넷 줄(`[M-4]`는 두 갈래를 따로), E2E `test/e2e/card-proxy.spec.ts`에 「권한자 → 남의 카드 → 견적 줄 → 저장 → 담당 PM 계정으로 프로젝트 상세 카드 섹션에 `경영관리 등록`」 한 경로.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages-proxy.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - 통합 테스트가 「지급·구매 권한만 있는 계정의 남의 카드 등록 거부」를 개별 `it`으로 단언한다
    - [M-4] 통합에 「권한자 직전 남의 카드 → 기본 카드 = 그 카드」 · 「그 카드 비활성 → 비움」 두 `it`이 녹색이다
    - (CROSS E-3 follow-on) `awk '/const runCreate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\(|usedByCandidates\(|teamAtDate\('`가 0이고 같은 범위의 줄 수가 5 이상이다(대리 갈래를 더한 뒤에도)
    - E2E 한 경로가 담당 PM 계정 화면에서 `경영관리 등록` 글자를 확인하고 녹색이다
  </acceptance_criteria>
  <done>대리 등록 한 건이 권한 판정 → 저장 → 담당 PM의 프로젝트 비용 표시까지 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 팀 카드 대리 등록의 사용한 사람(사용일 후보) · 완료 프로젝트의 견적 외 비용</name>
  <files>domain/corp-card-usages/index.ts, domain/corp-card-usages/link-targets.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/link-picker.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/integration/corp-card-usages-proxy.test.ts, test/e2e/card-proxy.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S9 사용한 사람 · 완료 프로젝트 문단 · 「새 건의 기본값(rev 9, M-4)」 연결 갈래 · UI Considerations S9 행(rev 9 — rev 8 줄 번호는 낡았다) · Copywriting 「막힘 — 카드 사용 폼」 소속 없음 갈래
    - domain/org/index.ts의 `teamAtDate` · 사람 목록(퇴사 표시) 조회 함수
  </read_first>
  <behavior>
    - `usedByCandidates(cardId, usedOn)`: 개인 카드 → [소지자] / 팀 카드 → 사용일에 그 팀 소속이던 사람 전부(지금 다른 팀 · 퇴사 포함, 사용일 뒤 입사 제외), 각 후보에 `지금 {팀}` 또는 `퇴사` 부가 글자
    - 저장: 사용한 사람이 후보 밖 → 거부 / 팀 비용 · 사용한 사람 사용일 소속 없음 → 거부 / 정상 → 팀 = 사용한 사람의 사용일 소속
    - 완료 프로젝트: 권한자 · 견적 외 비용 → 저장 / 권한자 · 견적 줄 → 거부 / 비권한자 → 프로젝트 목록에서 고를 수 없음
    - [M-4] 직전 연결 = 완료 프로젝트 P의 `견적 외 비용`: 권한자 → 새 건 연결 `견적 외 비용` · P 남음 / 비권한자(P가 등록 뒤 완료) → 종류만, 프로젝트 빔
  </behavior>
  <action>
`usedByCandidates(viewer, { cardId, usedOn })`(대리 등록 권한 필요)를 더하고, 저장 때 `precheckCardUsage` 대리 갈래가 트랜잭션 전에 같은 함수로 사용한 사람을 다시 확인한다(후보 밖이면 거부 — 사람 · 소속 조회가 전역 `db`라 트랜잭션 안에 두지 않는다, 06-03 tx 규약). 팀 비용이면 팀 = `teamAtDate(사용한 사람, 사용일)`(역시 precheck), null이면 거부. `link-targets.ts`의 프로젝트 목록에서 완료 프로젝트를 대리 등록 권한자의 `견적 외 비용` 갈래에만 고를 수 있게 하고(D-47), 견적 줄 갈래에서는 여전히 2행 `완료 · 견적 줄 잠김`. 서버 저장도 같은 판정을 `precheckCardUsage`의 완료 프로젝트 판정(06-07)에 권한자 예외로 더해 트랜잭션 전에 한다. 06-07 `cardUsageFormDefaults`의 「직전 프로젝트를 지금 고를 수 있나」도 같은 프로젝트 목록 판정이라 권한자의 완료 프로젝트 `견적 외 비용`이 저절로 남는다(M-4 — UI-SPEC S9 「새 건의 기본값」의 괄호 예외). 먼저 behavior 통합 케이스를 쓰고 RED를 확인한 뒤 구현한다. `link-picker.tsx`는 서버가 보낸 `selectable`만 그린다.

폼: 대리 등록 · `팀 비용`일 때만 `사용한 사람` 칸(후보 1명이면 텍스트, 여럿이면 `Select` 기본값 없음). 사용일을 바꾸면 후보를 다시 받아 오고 고른 사람이 새 후보에 없으면 칸을 비운다. 빈 칸은 §7-15 빈 칸 막힘. 후보 액션을 등록한다. 통합 테스트 · E2E에 behavior 케이스(퇴사자 · 사용일 뒤 입사자 판정 포함).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages-proxy.test.ts test/integration/corp-card-usages.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-proxy.spec.ts test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 넷 줄의 모든 갈래를 개별 `it`으로 단언한다(후보 밖 사용한 사람을 보낸 조작 요청 거부 · `[M-4]` 두 갈래 포함)
    - 06-05 · 06-07의 카드 사용 E2E가 여전히 녹색이다
  </acceptance_criteria>
  <done>팀 카드 대리 등록의 귀속이 사용일 기준 후보에서만 정해지고, 완료 프로젝트에는 권한자의 견적 외 비용만 열린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [O-11 기본값] 수정 모드 · 삭제(= 보관) · 토스트 되돌리기(H-4) · 권리 판정 한 함수 · 연결 변경 잠금(B-1)</name>
  <files>domain/corp-card-usages/rights.ts, domain/corp-card-usages/index.ts, repositories/corp-card-usages.ts, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-list.tsx, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, test/unit/domain/card-usage-rights.test.ts, test/integration/corp-card-usages-proxy.test.ts, test/e2e/card-proxy.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S9 「카드 — 수정 모드(`?editId=`)」 · `Form.Actions` · 「수정·삭제 권리(O-11)」 · S8 목록 행 → 수정/행 시트(rev 9 — rev 8 줄 번호는 낡았다) · Copywriting 「즉시 — 카드 사용 삭제(S9, rev 9 H-4 — 모달 없음)」 · 「SUCCESS — 토스트」와 그 아래 3차 `되돌리기`(rev 9, H-4) 문단 · Component Inventory Toast 행
    - ui/toast/Toast.tsx(`actionLabel` · `onAction` · `tone="error"` — 오류는 닫을 때까지) · app/(app)/admin/archive/archive-table.tsx(같은 화면 안 토스트 상태 선례)
    - domain/archive/index.ts · repositories/archive.ts `ARCHIVABLE_TABLES`(ADMN-12 보관 규약 — 이 표를 여기에 등록하지 않는 이유: `restore`가 보관 칸만 비운다)
    - `.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md` 「열린 선택 (사용자 확인 후보)」 표 O-11 행(기본값과 대안)
    - ui/table/RowSheet.tsx(폰 행 시트)
  </read_first>
  <behavior>
    - `cardUsageRights(usage, viewer, project)`: 등록자 → 수정·삭제 / 대리 등록 권한자 → 수정·삭제 / 그 밖 → 없음 / 완료 프로젝트 줄 · 등록자이지만 권한자 아님 → 없음 / `registered_via = purchase` → 연결 변경 없음 · 삭제 없음(수정은 등록자·권한자)
    - `updateCardUsage`: 카드 값이 저장된 것과 다름 → 거부 / 결제 합계 변경 → 재역산 / 연결 변경 → 잠금 뒤 이중 연결 게이트 재통과 / version 불일치 → 거부
    - `deleteCardUsage`: 권리 없음 → 거부 / 구매 완료 건 → 거부 / 정상 → 보관(`archived_at` · `archived_by` 채움 — 표의 행 수는 그대로, H-4) + `document_delete` · 목록 · 합계에서 빠짐
    - [H-4] `restoreCardUsage`: 권리 없음 → 거부 / `version` 불일치 → 거부 / 지운 사이 그 줄에 지출결의(A-607 칸 fixture)가 이어짐 → `card.dual-link-block` 거부(막힘 문구 `지출결의 {번호} 연결됨 · 카드 사용은 다른 줄` 그대로) / 팀 비용 건 · 정상 → 보관 칸 비움 + `restore` 로그 · 목록에 돌아옴
    - [H-4] 새 건 저장 응답: 그 건의 삭제 권리 있음 → 토스트용 되돌리기 값(`id` · `version`) 있음 / 없음 → 없음 · 수정 저장 응답에는 되돌리기 값이 없다
    - (E2E, H-4) 수정 모드 `카드 사용 삭제` → 대화상자 없음 → `/cards` 토스트 `카드 사용 삭제 · {결제 합계}` → `되돌리기` → 같은 건이 목록에 다시 / 새 건 저장 → 토스트 `카드 사용 등록 · {결제 합계}` → `되돌리기` → 목록에서 빠짐
  </behavior>
  <action>
`domain/corp-card-usages/rights.ts`에 `cardUsageRights`를 두고 함수 선언 위 한 줄 주석에 "O-11 답이 바뀌면 이 함수만 고친다"를 적는다. `updateCardUsage(viewer, { id, version, … })`와 `deleteCardUsage(viewer, { id, version })`가 이 함수를 먼저 부른다 — **트랜잭션을 열기 전에**(06-03 tx 규약, CROSS E-3 follow-on): 건을 잠금 없이 읽고 → `cardUsageRights`(그 안의 `can(viewer, "cards.proxy", "write")` · 완료 프로젝트 판정 포함) → 증빙 종류 옵션(코드표) → 결제 합계·통화·환율·증빙 종류·사용일이 바뀌면 `loadTaxRates(usedOn)`(06-03 — 기준일 = 사용일)까지 끝낸다. 수정 요청의 카드 값은 저장된 카드와 같아야 한다(구매 완료 건의 카드 고치기 예외는 06-12가 이 함수 옆에 더한다). 트랜잭션 몸통은 이름 있는 안쪽 함수 `const runUpdate = async (tx: DbOrTx) => { … };` · `const runDelete = async (tx: DbOrTx) => { … };`로 두고 `withTransaction`으로 부른다: `runUpdate`는 건 행 `version` 확인(tx — 사전 조회 뒤 바뀌었으면 여기서 거부) → 연결이 바뀌면 `lockQuoteLines(…, tx)`(새 줄) → `findLineLinks(…, tx)` → `gate(…, "card.dual-link-block", { side: "card", links })`(B-1) → `splitCardTotal(total, rates)`(순수)로 재역산 → UPDATE(tx). `runDelete`는 `version` 확인 → 보관 UPDATE(`archived_at = now()` · `archived_by = viewer` — tx, H-4: 물리 삭제 없음, ADMN-12). **보관 해제(H-4):** `restoreCardUsage(viewer, { id, version })` — 트랜잭션 전에 보관된 건을 잠금 없이 읽고(`repositories/corp-card-usages.ts`에 보관 건도 읽는 좁은 조회 — 목록 조회는 계속 보관 건을 뺀다) `cardUsageRights`의 삭제 권리 갈래를 지난다. 몸통 `const runRestore = async (tx: DbOrTx) => { … };`은 `version` 확인 → 줄 연결이면 `lockQuoteLines(…, tx)` → `findLineLinks(…, tx)` → `gate(…, "card.dual-link-block", { side: "card", links })`(B-1 — 지운 사이 지출결의가 이어졌을 수 있다) → 보관 칸 비움(tx). 카드 사용 표는 `ARCHIVABLE_TABLES`에 등록하지 않는다 — 관리자 보관함 `restore`는 게이트를 지나지 않는다(D-609). 행동 로그 수정 `document_update` · 삭제 `document_delete` · 보관 해제 `restore`는 각 몸통(`runUpdate` · `runDelete` · `runRestore`) 안에서 `recordAction(…, { tx })`로 같은 tx에 남긴다(06-03 tx 규약, r5 S-F1). 새 건 저장(06-05 `createCardUsage`)의 액션 응답에 그 건의 `cardUsageRights` 삭제 권리가 있을 때만 되돌리기 값(`id` · `version`)을 싣는다(권리 판정은 커밋 뒤 — 트랜잭션 몸통 밖). 먼저 behavior 단위 · 통합 테스트를 쓰고 RED를 확인한 뒤 구현한다.

화면: `/cards?editId={id}` — 권리가 없으면 404 꼴로 목록에 돌려보낸다. 제목 `카드 사용 저장`, 카드는 저장된 카드 읽기 텍스트, 저장된 증빙 종류가 지금 옵션에 없으면 `Select`가 `—`에 서고 1차 막힘 `증빙 종류 {이름} 카드에 없음 · 증빙 종류 고르기`. 구매 완료 건은 연결이 텍스트이고 삭제 버튼이 렌더되지 않는다. 오른쪽 끝 2차 `카드 사용 삭제`(1차와 떨어진 자리) → **모달 없이**(H-4 — 되돌릴 수 있어 확인 창을 쓰지 않는다, CLAUDE.md §7) `deleteCardUsageAction` → `/cards` 이동 + `Toast` `카드 사용 삭제 · {결제 합계}` + `actionLabel` `되돌리기`(→ `restoreCardUsageAction`). 새 건 저장 → `/cards` + 토스트 `카드 사용 등록 · {결제 합계}` + 응답에 되돌리기 값이 있을 때만 `되돌리기`(→ `deleteCardUsageAction`). 수정 저장 → 토스트 `카드 사용 저장 · {결제 합계}`(되돌리기 없음). 되돌리기가 거부되면 같은 자리 토스트를 `tone="error"`로 바꿔 서버 막힘 문구를 그대로 보인다(닫을 때까지). 폼에서 `/cards`로 옮겨 가며 토스트를 넘기는 방식은 기존 선례가 없다(`archive-table.tsx`는 같은 화면 안 상태) — 가장 작은 방식을 골라 SUMMARY에 적는다. `ConfirmDialog`는 입력 버리기에만 쓴다. 되돌리기 액션 둘을 등록한다. 목록 행 클릭: 권리가 있으면 `?editId=`, 없으면 `RowSheet` 보기 전용. 등록 필터는 권한자에게만.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/card-usage-rights.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/corp-card-usages-proxy.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rln "function cardUsageRights\|const cardUsageRights" domain/ app/` 결과가 `domain/corp-card-usages/rights.ts` 한 파일이다(권리 판정 정의가 중복되지 않는다)
    - 단위 테스트가 behavior 첫 줄의 다섯 갈래를, 통합 테스트가 둘째·셋째 줄의 갈래를 개별 `it`으로 단언한다(연결 변경 → 반대쪽 줄 거부 포함)
    - (CROSS E-3 follow-on) `awk '/const runUpdate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|cardUsageRights\('`가 0이고 같은 범위의 줄 수가 5 이상이다 · 같은 검사를 `runDelete` 범위에도 한다(줄 수 조건 없이 0건) · 두 범위 각각 06-03 「같은 tx 기록 검사」(`awk '{그 범위}' domain/corp-card-usages/index.ts | grep -v '^\s*//' | tr '\n' ' ' | grep -oE 'recordAction\([^;]*;' | grep -vcE '\{ *tx\b'`)가 0이고 `recordAction(`이 1건 이상이다(r5 S-F1)
    - E2E에 「등록자가 수정 → 저장」 · 「비등록자 행 클릭 → 행 시트」 케이스가 녹색이다
    - [H-4] E2E 「`카드 사용 삭제` → `role=dialog` 요소 0개 · 목록에서 사라짐 · 토스트 `카드 사용 삭제 · {결제 합계}` → `되돌리기` → 같은 건이 목록에 다시」 · 「새 건 저장 토스트 `되돌리기` → 목록에서 빠짐」 녹색
    - [H-4] 통합이 behavior `deleteCardUsage`(보관 뒤 표의 행 수 그대로 · `archived_by` = 요청자) · `restoreCardUsage` 네 갈래 · 새 건 저장 응답 두 갈래를 개별 `it`으로 단언한다
    - [H-4] `awk '/const runRestore = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|cardUsageRights\('`가 0이고 같은 범위의 줄 수가 5 이상이며, 같은 범위의 06-03 「같은 tx 기록 검사」가 0 · `recordAction(` 1건 이상이다(r5 S-F1) · `grep -v '^\s*//' repositories/archive.ts | grep -cE 'corp_card_usages|corpCardUsages'`가 0이다(관리자 보관함 미등록)
  </acceptance_criteria>
  <done>카드 사용 건을 누가 고치고 지울 수 있는지가 한 함수에서 정해지고, 연결 변경도 잠금 뒤 게이트를 지난다. 삭제는 모달 없는 보관이고, 등록 · 삭제 토스트의 `되돌리기`가 같은 서버 판정을 지난다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 대리 등록·수정·삭제 액션 | 카드 id · 사용한 사람 id · 연결 · version이 넘어온다. 전부 서버가 다시 판정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-41 | Elevation of Privilege | 대리 등록 | high | mitigate | 남의 카드 저장은 `can(viewer, "cards.proxy", "write")`만 허락하고 다른 메뉴 권한으로 대신 통과하지 않는다. 지급·구매 권한만 있는 계정 거부 통합 테스트 |
| T-06-42 | Spoofing | 사용한 사람 | high | mitigate | 저장 때 `usedByCandidates`로 다시 판정, 후보 밖이면 거부. 조작 요청 통합 테스트 |
| T-06-43 | Tampering | 수정 요청의 카드·연결 | medium | mitigate | 카드 값 변경 거부, 연결 변경은 잠금 뒤 이중 연결 게이트 재통과(B-1), `version` 낙관적 잠금 |
| T-06-44 | Elevation of Privilege | 수정·삭제 권리 | high | mitigate | `cardUsageRights` 한 함수를 domain 함수 첫 줄에서 부르고 화면은 그 결과만 그린다. URL 직접 접근(`?editId=`)도 서버가 거른다 |
| T-06-45 | Repudiation | 삭제(보관) · 보관 해제 | medium | mitigate | `document_delete` · `restore` 행동 로그(사람 · 시각 · 건 요약) + 행의 `archived_by`(H-4) |
| T-06-192 | Tampering | 보관 해제로 이중 연결 우회(H-4) | high | mitigate | `restoreCardUsage`가 삭제 권리 판정(트랜잭션 전) → `lockQuoteLines` → `findLineLinks` → `card.dual-link-block`을 지난다. 관리자 보관함(`ARCHIVABLE_TABLES`)에 등록하지 않아 게이트 없는 복원 경로가 없다. 「지운 사이 지출결의 연결 → 거부」 통합 케이스 · 미등록 grep |
| T-06-156 | Denial of Service | 대리 등록 · 수정 · 삭제 트랜잭션의 풀 고갈 교착 | high | mitigate | 대리 판정(`can` · 활성 카드 · `usedByCandidates` · `teamAtDate`)은 `precheckCardUsage`, 수정 · 삭제의 권리 · 세율 · 코드표 판정은 트랜잭션 전 — `runCreate` · `runUpdate` · `runDelete`는 tx 리포지토리 · 순수 함수 · 게이트 · `{ tx }` 기록만, 행동 로그는 같은 `tx`(06-03 tx 규약, CROSS E-3 follow-on, r5 S-F1). region grep 셋 |
</threat_model>

<verification>
- 단위 card-usage-rights, 통합 corp-card-usages-proxy · corp-card-usages · leak-scan 녹색
- E2E card-proxy · card-usage가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S9(대리 등록 · 수정 모드 · 삭제 뒤 토스트 `되돌리기`) DOM 실측 감사(backstop 2건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 경영관리가 PM이 모르는 카드 사용을 대신 등록하면 담당 PM의 프로젝트 비용에 `경영관리 등록`으로 보인다
- 대리 등록·수정·삭제가 권한 한 키와 권리 한 함수로만 열린다
- 팀 비용 귀속이 사용일 기준으로만 정해지고, 연결 변경이 잠금 뒤 게이트를 지난다
- 카드 사용 삭제는 모달 없는 보관이고, 등록 · 삭제 토스트의 `되돌리기`가 저장과 같은 서버 판정(보관 해제는 이중 연결 게이트 포함)을 지난다(H-4)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `cardUsageRights` · `usedByCandidates` · `updateCardUsage`(`runUpdate`) · `deleteCardUsage`(`runDelete` — 보관) · `restoreCardUsage`(`runRestore` — 보관 해제, 게이트 재통과) · `precheckCardUsage` 대리 등록 갈래 |
| 리포지토리 | `repositories/corp-card-usages.ts` 보관 · 보관 해제 UPDATE(tx) · 보관 건도 읽는 좁은 조회 |
| Server Action | `updateCardUsageAction` · `deleteCardUsageAction` · `restoreCardUsageAction` · 사용한 사람 후보 액션 |
| 화면 | `/cards?editId=` 수정 모드 · 삭제(모달 없음) · 토스트 `카드 사용 등록` · `카드 사용 저장` · `카드 사용 삭제`(등록 · 삭제에 3차 `되돌리기`) · 목록 행 → 수정/행 시트 |
| 테스트 | `test/unit/domain/card-usage-rights.test.ts` · `test/integration/corp-card-usages-proxy.test.ts` · `test/e2e/card-proxy.spec.ts` |

### Round 3 — r4 (final-review-fable, 2026-09-26)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/final-review-fable.md`(커밋 전 — 줄 번호는 그 파일 기준). 교차 검토는 Codex 대신 Fable(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F-N1 — read_first가 저장소 밖 `/mnt/project-files/deadlock-fix/global-db-in-tx-audit.md` · `06-prep/open-choices.md` 20행(O-11)을 인용(final-review-fable.md L51-55) | 참고(고침) | 사본이 저장소 · 이 환경 어디에도 없다(`find /` 0건). 감사 메모 → `grep -L "DbOrTx" repositories/*.ts`(+ 필요한 곳은 `grep -n "export async function" repositories/settings.ts`)로 저장소에서 다시 뽑는 명령, O-11 → `06-UI-SPEC.md` 「열린 선택 (사용자 확인 후보)」 표 O-11 행 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 4 — r5 (pre-merge staleness + final-review-fable-r4, 2026-09-26)

> 입력: `.planning/phases/06-payment-evidence-cards/final-review-fable-r4.md`(R4-F1~F3 · R4-N1~N3 — 저장소 안) + Opus pre-merge staleness 검토(`/review` 적대 패스, 2026-09-26 — 파일 없음, 지적은 오케스트레이터 지시 본문 S-F1~S-F5 · S-N1). 기준 코드: main 4347517(PR #40 머지). 줄 번호는 모두 `grep -n`으로 다시 쟀고 심볼 이름을 먼저, 줄은 「~N」으로 적었다

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| S-F1 / R4-F2 — 「06-03 tx 규약」을 따르는 이 플랜의 「행동 로그는 커밋 뒤」가 main 관행과 어긋난다. Phase 4의 커밋 뒤 지연 기록(`deferRecord`)은 e13d0e4(04-12)에서 지워졌고 main은 트랜잭션 안 `recordAction(…, { tx })`다(`domain/projects/ledger.ts` `saveProjectLedger` · `domain/revenue/index.ts` `saveRevenueInTx` ~424 · `domain/action-log/record.ts` `RecordActionDeps.tx` ~97, ARCHITECTURE §4-8 (3)) | 고침(오케스트레이터 결정 — main을 따른다, 06-03 Round 6이 규약을 다시 썼다) | 검증: `grep -rn "deferRecord" domain repositories app lib` 0건. Plan 09: must_haves 「06-03 tx 규약」 줄 · Task 3 action(수정 · 삭제 · 보관 해제 로그를 각 몸통 안 `{ tx }`로) · Task 3 acceptance `runUpdate` · `runDelete` · `runRestore` 범위 grep(`recordAction` 금지를 빼고 06-03 「같은 tx 기록 검사」) · T-06-156 |
| R4-F3 — read_first 「`team-memberships.ts`는 tx 인자 없음」이 머지로 낡음(`teamLeadCandidatesAtDate` ~88 `tx?: DbOrTx`, `grep -L` 목록에서 빠짐) | 고침 | 검증: `grep -n "export async function\|tx?" repositories/team-memberships.ts` — `teamLeadCandidatesAtDate`만 `tx?`, `teamAtDate`(`domain/org/index.ts` ~190)가 부르는 `findMembershipAtDate`는 `tx` 없음. 괄호를 함수 단위 사실로 고치고 함수 서명 grep을 따르라는 문장으로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (앞 라운드 표의 「커밋 뒤」 · `deferRecord` 언급) | 고치지 않음 — ledger는 append-only다. 이 라운드의 S-F1 줄이 그 규약을 대체한다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-09-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-3 follow-on — 06-05가 카드 사용 생성을 `precheckCardUsage`(트랜잭션 밖) → `createCardUsage(viewer, input, pre, tx?)`로 확정한 뒤, 이 플랜의 대리 등록 갈래가 여전히 「`createCardUsage`의 카드 자격 판정」에 `can(viewer, "cards.proxy", "write")`를 두고 있었다(eng-cross-opus.md L22-24@333f7bf의 같은 꼴) | 고침 | Plan 09, Task 1: 대리 갈래를 `precheckCardUsage`로 옮김(`runCreate`는 `pre`만). Task 2: `usedByCandidates` 재확인 · `teamAtDate` · 완료 프로젝트 권한자 예외도 precheck. Task 3: `updateCardUsage` · `deleteCardUsage`의 권리 · 세율 · 코드표 판정을 트랜잭션 전으로, 몸통 `runUpdate` · `runDelete`, 행동 로그는 커밋 뒤. acceptance region grep 셋 · T-06-156 |
| ENG E-3 — tx 규약 참조(eng-review.md L72-74@333f7bf) | 고침 | must_haves.truths에 「06-03 tx 규약」 참조 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 대리 등록 경로의 「동시 6건」 통합 테스트 | 추가하지 않음 — 같은 `runCreate` 외부/단독 경로의 교착 회귀는 06-12 「구매 완료 동시 6건」이 증명하고, 이 플랜은 region grep으로 트랜잭션 몸통을 지킨다. 브리프의 동시 6건 배정 목록에 06-09가 없다 |

### Round 2 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 10 · 12)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| H-4 — 되돌릴 수 있는 카드 사용 삭제에 확인 모달을 쓰고, 등록 토스트에 `되돌리기`가 없다(design-review.md L41@f2720a8 · 반영 지시 10 L83@f2720a8) | HIGH(고침) | Plan 09: must_haves.truths `[H-4]` 셋(삭제 = 모달 없는 보관 + 토스트 `되돌리기` · 등록 토스트 `되돌리기`(삭제 권리 있을 때만) · 보관 해제의 게이트 재통과) · prohibitions 한 줄(물리 삭제 · 게이트 없는 복원 금지) · artifacts `restoreCardUsage` · key_links(`actions.ts` → `restoreCardUsage`) · objective · 규모 근거 · 선행 의존(보관 칸 · Toast) · 열린 선택 「계획 판단(H-4)」. Task 3 이름 · files(`repositories/corp-card-usages.ts`) · read_first(Copywriting 「즉시 — 카드 사용 삭제」 · 「SUCCESS — 토스트」 3차 `되돌리기` · Toast · ADMN-12) · behavior(보관 · 보관 해제 네 갈래 · 등록 응답 · E2E) · action(`runDelete` = 보관 UPDATE · `restoreCardUsage`/`runRestore` · 토스트 셋 · 오류 모양 · `ConfirmDialog`는 입력 버리기만) · acceptance(E2E 둘 · 통합 · region grep · 미등록 grep). T-06-45 · T-06-192 · verification · success_criteria · Artifacts |
| M-4 — 새 건 기본값(대리 등록 권한자 갈래)(design-review.md L51@f2720a8 · 반영 지시 12 L85@f2720a8) | MEDIUM(고침) | Plan 09: must_haves.truths `[M-4]` 줄. Task 1 behavior · action 「새 건 기본값」 문장 · acceptance(남의 카드 기본값 + 힌트 그대로). Task 2 behavior · action(완료 프로젝트 `견적 외 비용` 예외가 06-07 `cardUsageFormDefaults`에 저절로 든다) · acceptance. 새 판정을 만들지 않는다 |
| (부수) Task 1 · 2 · 3 read_first의 UI-SPEC rev 8 줄 번호가 rev 9에서 낡았다 | 참고 | 절 이름으로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 카드 사용을 관리자 보관함(ADMN-12 `ARCHIVABLE_TABLES`)에 등록해 토스트가 사라진 뒤에도 되살리기 | 등록하지 않음 — `domain/archive` `restore`는 보관 칸만 비우고 이중 연결 게이트(D-609)를 지나지 않는다. 필요하면 게이트를 거치는 복원 경로와 함께 따로 계획한다(오케스트레이터에 넘김) |
