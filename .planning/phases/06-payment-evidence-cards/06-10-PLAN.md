---
phase: 06-payment-evidence-cards
plan: 10
type: execute
wave: 7
depends_on: ["06-04", "06-08", "06-09"]
files_modified:
  - domain/next-turn/index.ts
  - domain/next-turn/phase6-items.ts
  - ui/next-turn/build-next-turn-view.ts
  - ui/next-turn/NextTurn.tsx
  - app/(app)/page.tsx
  - test/unit/domain/next-turn-phase6.test.ts
  - test/unit/ui/next-turn.test.ts
  - test/integration/next-turn.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/next-turn-phase6.spec.ts
  - test/e2e/management-flow.spec.ts
autonomous: true
requirements: [EXP-09, EXP-10, EXP-13, EVID-02, PROJ-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 권한자(`expenses.payments` write)가 홈 `/`을 열면, 예정일이 오늘이거나 지난 지급 대상이 있을 때 한 줄 `[오늘] 지급 — 예정일 지남·오늘 {N}건 · 확인 전 {M}건  {합계}  지급 대상 보기`가 선다. 3차를 누르면 `/expenses?view=pay`로 간다. 오늘 예정 건도 이 `[오늘]` 줄에 든다"
    - "예정일이 오늘이거나 그 전인 지급 대상이 하나도 없고 이번 주(서울 기준 월~일)에 예정된 대상이 있을 때만 `[대기] 지급 — 이번 주 {K}건  {합계}  지급 대상 보기`가 선다"
    - "구매 권한자(`cards.purchases` write)에게는 `[대기] 구매 요청 — 신청 {N}건 · 가장 오래된 {MM-DD}  {합계}  구매 요청 보기`(→ `/cards/purchases`)가 선다. 매출 기록 권한자(`projects.revenue` write)에게는 `[대기] 매출 발행 요청 — {N}건 · 희망 {MM-DD}  {합계}  발행 요청 보기`(→ `/projects/issue-requests`)가 서고, 가장 이른 희망일이 오늘이거나 그 전이면 태그가 `[오늘]`이다"
    - "담당 PM에게는 두 종류의 줄이 건마다 선다. 하나는 선결제 증빙 기한이 지난 문서마다 `[막힘] {프로젝트} · {항목} — 선결제 증빙 {N}일 경과  {금액}  증빙 올리기`(→ 그 지출결의)이고, 다른 하나는 완료 전 점검 막힘이 있는 정산 프로젝트마다 `[막힘] {프로젝트} — 완료 전 점검 {N}건  —  점검 보기`(→ `/projects/{id}#pre-settle-check`)이다. 직원은 자기가 기안한 선결제 기한 초과 줄만 받는다. 대표에게는 이 페이즈 항목이 없다"
    - "증빙 `면제`된 선결제 문서에는 「내 차례」 PM 항목이 서지 않는다. 면제가 선결제를 이긴다(UI-SPEC 382행)"
    - "[O-15 기본값] 「내 차례」 공급 함수는 main에 이미 있으면 거기에 Phase 6 항목을 더하고, 없으면(홈이 여전히 `buildNextTurnView([])`) 이 플랜이 `domain/next-turn`에 처음 만든다. 어느 갈래였는지 SUMMARY에 남는다"
    - "S19 empty: 이 페이즈 항목도 다른 항목도 0건이면 블록이 없다(§7-4)"
    - "S19 error: 공급이 실패하면 블록 자리에 한 줄 `내 차례를 불러오지 못했습니다 · 다시 시도`(`--danger`, 「다시 시도」 = 2차)가 서고, 홈의 나머지는 그대로 선다. 0건(블록 없음)과 구분된다"
    - "S19 partial: 경영관리 항목은 묶음 한 줄(지급 · 구매 요청 · 발행 요청 각각)이고, PM 항목은 건마다 한 줄이다. 지급 묶음은 예정일이 오늘이거나 그 전인 건이 없을 때만 `[대기]`다"
    - "S19 overflow: 최대 6줄이고 태그 순서는 막힘 → 오늘 → 결재 → 대기다. 경영관리 묶음 한 줄이 상한을 다 먹지 않는다"
    - "S19 zero-one-many: 묶음 줄은 건수를 글자로 쓴다(`신청 3건`). 0건인 묶음은 줄이 없고, 지급 묶음의 `확인 전 0건` 조각은 쓰지 않는다"
    - "경영관리 흐름(결재 통과 건 → 증빙 확인 → 지급 완료)이 이미지 증빙 한 경로와 PDF 증빙 한 경로로 Playwright E2E에 있고 CI=true로 녹색이다(ROADMAP 기준 5)"
    - "이 페이즈의 새 액션 레지스트리와 새 DTO가 모두 누수 스캔에 등록되어 있어, 계급별 검사가 따라온다(ROADMAP 기준 5)"
    - statement: "S19 loading: 홈 로드 규칙은 §7-4 · §7-7 그대로이고, 공급 경로는 이 플랜이 정했다. 느린 응답을 강제해 CI=true DOM 감사로 확인한다"
      verification: backstop
    - statement: "S19 populated: 역할별 문구·태그 순서·6줄 상한이 공급 함수 결과로 서는지 CI=true DOM 감사로 확인한다(경영관리 · PM · 직원 · 대표 계정)"
      verification: backstop
    - statement: "S19 long-text: 긴 프로젝트·항목 이름이 NextTurn 한 줄에서 말줄임 + `title`로 서는지 CI=true DOM 감사로 확인한다"
      verification: backstop
  prohibitions:
    - statement: "「내 차례」 공급이 실패했는데 빈 블록이나 일부 항목만 보여서는 안 된다. 공급원 하나라도 실패하면 블록 전체가 오류 한 줄이다"
      status: resolved
      verification: test
    - statement: "권한이 없는 역할의 홈에 지급·구매 요청·발행 요청 묶음 줄이 서거나, 볼 수 없는 금액이 실려서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/next-turn/phase6-items.ts"
      provides: "Phase 6 「내 차례」 항목 — 묶음 셋 · PM 항목 둘 · 순수 빌더와 수집 함수"
      exports: ["listPhase6NextTurnItems", "buildPaymentBundle", "buildPurchaseBundle", "buildIssueBundle", "buildPrepaidOverdueItems", "buildPreSettleBlockedItems"]
    - path: "domain/next-turn/index.ts"
      provides: "[O-15] 홈이 부르는 공급 함수(이미 있으면 그 함수에 더한다)"
      exports: ["listNextTurnItems"]
    - path: "test/e2e/management-flow.spec.ts"
      provides: "ROADMAP 기준 5 — 경영관리 흐름 이미지 · PDF 두 경로"
      min_lines: 60
  key_links:
    - from: "app/(app)/page.tsx"
      to: "domain/next-turn/index.ts"
      via: "홈이 빈 배열 대신 공급 함수 결과를 `buildNextTurnView`에 넘긴다"
      pattern: "listNextTurnItems"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/payments/targets.ts"
      via: "지급 묶음이 06-08의 지급 대상 정의를 그대로 쓴다(사본 없음)"
      pattern: "listPaymentTargets"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/pre-settle-check/index.ts"
      via: "PM 점검 막힘 항목이 06-09 판정 그대로를 읽는다"
      pattern: "computePreSettleCheck"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/evidence-reviews/prepaid.ts"
      via: "선결제 기한 경과 일수는 06-02 한 함수에서 온다(O-5)"
      pattern: "prepaidDueInfo"
---

<objective>
경영관리와 PM이 홈 「내 차례」(S19, UA-611)에서 이 페이즈의 할 일을 다음 한 수와 함께 보게 한다. 경영관리 몫은 지급 · 구매 요청 · 발행 요청 묶음이고, PM 몫은 선결제 증빙 기한 초과와 완료 전 점검 막힘이다. ROADMAP 기준 5의 **경영관리 전체 E2E**(이미지 · PDF 두 업로드 경로)와 누수 스캔 완전성, 페이즈 전체 CI=true 게이트로 Phase 6를 닫는다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
이 Goal은 user story 형식이 아니다(MVP 모드). `/gsd mvp-phase 6`을 권하며, 계획은 이야기를 지어내지 않았다.

Purpose: O-1 기본값에 따라 경영관리 전용 화면은 없다. 그러니 흩어진 목록(지급 대상 · 구매 요청 · 발행 요청)을 모아 보여 주는 자리는 「내 차례」뿐이다. 지금 홈은 늘 빈 배열을 넘긴다(main 실측). 독촉 **알림**(이메일·알림함)은 Phase 7 몫이고, 이 줄은 알림이 아니라 할 일 표시다.

Output: `domain/next-turn`(공급 함수 · Phase 6 항목), `ui/next-turn`의 `reason`·`amount` 빈 값 허용, 홈 연결과 오류 줄, 단위 2 · 통합 1 · E2E 2, 누수 스캔 완전성 단언, 페이즈 전체 게이트.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-08-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-09-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 실행을 시작할 때 main에서 아래를 다시 확인한다. 하나라도 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-10 착수하지 않음`을 출력하고 멈춘다.** 아래 이름은 모두 가칭이다. 실제 이름이 다르면 실제 이름을 쓰고, SUMMARY에 가칭과 실제 이름의 대응표를 남긴다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| UA-611 · O-15 | 홈의 「내 차례」 공급 경로. 홈이 여전히 `buildNextTurnView([])`이면 이 플랜이 처음 만든다. Phase 5나 04.1이 공급 함수(결재 항목 등)를 이미 만들었으면 거기에 더한다 | `grep -n "buildNextTurnView" 'app/(app)/page.tsx'`와 `git ls-files 'domain/next-turn/*'`의 결과로 갈래를 정하고 SUMMARY에 적는다(멈추지 않음) |
| A-608 | Phase 5 증빙 업로드 경로(축소 → 서버 검사 → 서명 PUT → 완료 통보)가 **테스트 환경에서** 도는 방법(저장소 테스트 어댑터 등) | `grep -rln "upload\|signed" test/e2e/ test/integration/`로 Phase 5 업로드 테스트를 찾는다. 테스트 환경 업로드 경로가 없으면 **멈추고 묻는다**. 선택지는 ⑴ Phase 5 저장소 어댑터에 테스트 모드를 더한다 ⑵ 에뮬레이터 컨테이너를 쓴다(새 인프라 의존 — 사용자 승인 필요)이다. 새 패키지는 조용히 더하지 않는다 |
| 06-02 · 06-05 · 06-06 · 06-08 · 06-09 | `prepaidDueInfo` · `listIssueRequests` · `listPurchaseRequests` · `listPaymentTargets` · `computePreSettleCheck`와 06-09 SUMMARY의 A-601 상태 대응 | `grep -rn "prepaidDueInfo\|listIssueRequests\|listPurchaseRequests\|listPaymentTargets\|computePreSettleCheck" domain/ | grep -c "export"`가 5 미만이면 멈춤 |
| A-606 | 04.1 결재 항목이 「내 차례」에 `[결재]`로 이미 들어오는지 여부(태그 순서 정렬이 함께 돈다) | ⓪의 O-15 갈래 확인에 포함한다. 결과를 SUMMARY에 적는다 |

## 열린 선택 — 사용한 기본값

아직 답이 없는 선택은 현재 기본값으로 짰다. 답이 바뀌면 아래 「고칠 곳」만 바뀐다.

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-15** | Phase 5 산출물을 보고 정한다. 없으면 이 페이즈가 처음 만든다 | `domain/next-turn/index.ts` 한 곳(⓪ 갈래). must_haves의 `[O-15 기본값]` 한 줄 |
| O-1 | 경영관리 전용 화면 없이 「내 차례」가 모음이다 | 해당 없음(이 플랜의 묶음 셋이 그 모음이다) |
| O-2 | 지급 묶음의 `확인 전 {M}건`은 O-2 답과 관계없이 센다. 확인이 지급 게이트가 아니어도 확인은 할 일이기 때문이다 | `buildPaymentBundle`의 한 조각 |
| O-19 · O-20 | 구매 요청 묶음은 팀 비용 요청도 똑같이 센다. 귀속(O-19)과 결제자(O-20)는 묶음 판정에 쓰이지 않는다 | 해당 없음 |
| O-5 | 선결제 기한의 기준(지급일 + N일)은 06-02 `prepaidDueInfo` 한 곳에 있다. 이 플랜은 부르기만 한다 | 해당 없음 |
| PN-1 (계획 판단) | 예정일 없는 대상과 다음 주 이후 대상은 지급 묶음에 세지 않는다(지급 대상 목록에는 있다). 다른 선택지는 `[대기] 지급 — 예정일 없음 {N}건`을 더하는 것이다 | `buildPaymentBundle`의 날짜 가르기 |
| PN-2 (계획 판단) | UI-SPEC 문구를 글자 그대로 내기 위해 `NextTurnItem.reason`은 비울 수 있고(`null`이면 ` · 이유` 조각이 없다), `amount`는 `null`이면 `—`다(SYSTEM §2 163행 빈 값 규칙). 대상과 이유를 잇는 ` · `는 그대로 둔다 | `ui/next-turn/build-next-turn-view.ts` 타입 · `NextTurn.tsx` 두 칸 |
| PN-3 (계획 판단) | 공급원 하나라도 실패하면 블록 전체가 오류 한 줄이다. 일부만 보이면 막힘 항목이 조용히 빠지기 때문이다 | `app/(app)/page.tsx`의 catch 한 곳 |
| PN-4 (계획 판단) | 같은 지출결의가 PM 항목과 기안자 항목으로 겹치면 한 줄만 둔다 | `buildPrepaidOverdueItems` |

**항목 문자열 대응(PN-2 — 현재 렌더러는 `{label} · {reason}`):**

| 항목 | label | reason |
|---|---|---|
| 지급 `[오늘]` | `지급 — 예정일 지남·오늘 {N}건` | `확인 전 {M}건`(M = 0이면 `null`) |
| 지급 `[대기]` | `지급 — 이번 주 {K}건` | `null` |
| 구매 요청 | `구매 요청 — 신청 {N}건` | `가장 오래된 {MM-DD}` |
| 매출 발행 요청 | `매출 발행 요청 — {N}건` | `희망 {MM-DD}` |
| 선결제 기한 초과 | `{프로젝트} · {항목} — 선결제 증빙 {N}일 경과` | `null` |
| 완료 전 점검 막힘 | `{프로젝트} — 완료 전 점검 {N}건` | `null`, amount `null` |

<probe_fallback>
coverage.json에서 이 플랜이 맡은 몫: **EXP-09 · EXP-10 · EXP-13 · EVID-02 · PROJ-06 — 모두 unclassified → unresolved (flagged)**. 자동으로 해소하거나 기각한 것은 없다. 대신 쓴 근거는 ROADMAP 기준 5 · UI-SPEC S19 · O-15 · UA-611이다. 10건 전체 표는 06-01 `<probe_fallback>`에 있다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 지급 권한자 홈에 「[오늘] 지급」 묶음 한 줄이 서고, 누르면 지급 대상으로 간다</name>
  <files>domain/next-turn/index.ts, domain/next-turn/phase6-items.ts, ui/next-turn/build-next-turn-view.ts, ui/next-turn/NextTurn.tsx, app/(app)/page.tsx, test/unit/domain/next-turn-phase6.test.ts, test/unit/ui/next-turn.test.ts, test/integration/next-turn.test.ts, test/e2e/next-turn-phase6.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다. ⓪이 확인하고, 없으면 멈춘다(A-608 테스트 업로드 경로는 Task 3 전에 멈추고 묻는다).</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 763-769행(S19 정본) · 283행(「Error — 「내 차례」 로드」) · 330행(「표시 — 「내 차례」」) · 810행(S19 권한 행) · 1058-1065행(S19 considerations)
    - docs/design/SYSTEM.md 783-789행(§7-4) · 163행(빈 값 `—`)
    - ui/next-turn/build-next-turn-view.ts · ui/next-turn/NextTurn.tsx · test/unit/ui/next-turn.test.ts · test/e2e/mobile-next-turn.spec.ts(폰 두 줄 배치 — 깨지면 안 된다)
    - app/(app)/page.tsx · ui/list-empty/ListEmpty.tsx(`tone="error"` = 2차 행동)
    - eslint.config.mjs 40-72행(domain은 ui를 import할 수 없다 — domain 항목 타입은 `NextTurnItem`과 구조가 같은 자체 타입으로 둔다)
    - domain/payments/targets.ts(06-08 `listPaymentTargets`) · domain/evidence-reviews/index.ts(`resolveEvidenceStatus` — `확인 전` 판정) · domain/money/index.ts(`sumKrw`)
  </read_first>
  <behavior>
    - `buildPaymentBundle(targets, today)`: 예정일 ≤ 오늘 3건(그중 확인 전 1) → `[오늘]` · label `지급 — 예정일 지남·오늘 3건` · reason `확인 전 1건` · amount = 세 건 지급 총액 합 / 확인 전 0 → reason `null` / 오늘 이전 0 · 이번 주 2 → `[대기]` · `지급 — 이번 주 2건` / 예정일 없음만 → 줄 없음 / 대상 0 → 줄 없음 / 일요일 경계(오늘 = 일요일, 다음 월요일 예정) → 이번 주 아님
    - `buildNextTurnView`: `amount: null` · `reason: null` 항목을 받아도 정렬·절단이 그대로다
    - `NextTurn`: amount `null` → `—`, reason `null` → ` · ` 조각 없음
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」을 재확인한다. O-15 갈래(처음 만든다 / 더한다)와 04.1 결재 항목 여부를 SUMMARY에 적는다.

① **ui 타입(PN-2).** `ui/next-turn/build-next-turn-view.ts`의 `NextTurnItem`에서 `amount`를 `number | null`로, `reason`을 `string | null`로 바꾼다. `NextTurn.tsx`는 금액이 `null`이면 `—`를 그리고, 이유가 `null`이면 ` · ` 조각을 그리지 않는다. 클래스 이름과 배치는 그대로 둔다(`mobile-next-turn.spec.ts` 두 줄 배치 유지). 단위 테스트 `test/unit/ui/next-turn.test.ts`에 빈 값 케이스를 더한다.

② **domain.** `domain/next-turn/phase6-items.ts`를 만든다. domain은 ui를 import하지 않으므로, 항목 타입은 `NextTurnItem`과 구조가 같은 자체 타입(태그 넷 리터럴 유니언 · label · reason · amount · action)으로 둔다. 홈에서 그대로 넘기고 typecheck가 호환을 증명한다. 이 태스크에서는 `buildPaymentBundle`(순수)과 수집 함수 `listPhase6NextTurnItems(viewer, today)`의 지급 갈래만 만든다. 수집 함수는 먼저 `can(viewer, "expenses.payments", "write")`를 확인하고, 없으면 지급 대상 조회를 부르지 않는다. 지급 대상 정의는 06-08의 `listPaymentTargets`를 그대로 부른다(사본 없음). 합계는 `sumKrw`로 낸다. `확인 전`은 `resolveEvidenceStatus`로 판정한다. 날짜는 서울 기준 `YYYY-MM-DD` 문자열이고, `today`는 인자로 받는다(테스트 주입). `domain/next-turn/index.ts`의 `listNextTurnItems(viewer, today)`가 Phase 6 항목을 모은다. ⓪에서 기존 공급 함수가 있었으면, 그 함수에 Phase 6 항목을 이어 붙이는 것으로 대신한다.

③ **홈.** `app/(app)/page.tsx`에서 빈 배열을 넘기던 호출을 `listNextTurnItems(viewer, today)`의 결과를 넘기는 호출로 바꾼다. D-24 주석은 바뀐 사실(이제 도메인이 항목을 공급한다)에 맞게 한 줄로 고친다.

④ **테스트.** 통합 테스트 `test/integration/next-turn.test.ts`는 두 가지를 확인한다: 지급 권한자는 지급 줄 1개를 받고, 대표·직원·PM은 지급 줄을 받지 않는다. E2E `test/e2e/next-turn-phase6.spec.ts`는 한 경로를 따른다: 오늘 예정 지급 대상 1건 시드 → 경영관리 계정 `/` → `[오늘]` 태그와 `지급 — 예정일 지남·오늘 1건` → `지급 대상 보기` → `/expenses?view=pay`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/next-turn-phase6.test.ts test/unit/ui/next-turn.test.ts test/unit/ui/next-turn-action.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/next-turn.test.ts test/integration/payment-batch.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다. typecheck가 page.tsx에서 빨가면 domain 항목 타입과 `NextTurnItem`의 구조가 어긋났다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/next-turn-phase6.spec.ts test/e2e/mobile-next-turn.spec.ts test/e2e/wordmark-home.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(O-15 갈래 · 04.1 결재 항목 여부 · 가칭과 실제 이름의 대응표)가 SUMMARY에 있다
    - `grep -v '^\s*//' 'app/(app)/page.tsx' | grep -c "buildNextTurnView(\[\])"`가 0이다(주석 줄은 세지 않는다)
    - `pnpm lint`가 녹색이다 — boundaries 규칙이 domain → ui import를 막으므로, 녹색이면 층 경계가 지켜졌다
    - 단위 테스트가 behavior의 지급 갈래 여섯 개를 개별 케이스로 단언한다
  </acceptance_criteria>
  <done>지급 대상 하나가 06-08 정의 → domain 묶음 → 홈 NextTurn → 지급 대상 목록까지 실제로 이어진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 나머지 묶음(구매 요청 · 발행 요청) · PM 항목(선결제 기한 초과 · 점검 막힘) · 역할 범위 · 오류 줄 · 6줄 상한</name>
  <files>domain/next-turn/phase6-items.ts, domain/next-turn/index.ts, app/(app)/page.tsx, test/unit/domain/next-turn-phase6.test.ts, test/integration/next-turn.test.ts, test/e2e/next-turn-phase6.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 382행(면제가 선결제를 이긴다 — PM 항목 없음)
    - domain/purchase-requests/index.ts(06-06 `listPurchaseRequests` 상태 필터) · domain/issue-requests/index.ts(06-05 `listIssueRequests`)
    - domain/evidence-reviews/prepaid.ts(06-02 `prepaidDueInfo`) · domain/pre-settle-check/index.ts(06-09 `computePreSettleCheck`) · 06-09 SUMMARY의 A-601 상태 대응
    - db/schema/projects.ts(`pmUserId`) · app/(app)/error.tsx(오류 메시지를 화면에 노출하지 않는 선례, T-02-18)
  </read_first>
  <behavior>
    - 구매: 신청 3건(가장 오래된 09-18) → `[대기]` · `구매 요청 — 신청 3건` · `가장 오래된 09-18` / 신청 0 → 줄 없음 / 구매 완료·취소만 → 줄 없음
    - 발행: 요청 2건 · 가장 이른 희망 09-30 · 오늘 09-24 → `[대기]` · `희망 09-30` / 가장 이른 희망 ≤ 오늘 → `[오늘]` / 발행됨·취소만 → 줄 없음
    - 선결제: 선결제 · 증빙 0 · 면제 아님 · 기한 16일 경과 → `[막힘]` 한 줄 / 면제 → 없음 / 기한 전 → 없음 / 같은 문서가 PM 범위와 기안자 범위에 둘 다 → 한 줄(PN-4)
    - 점검: 정산 프로젝트 · 막힘 3 · 보는 사람이 PM → `[막힘]` · amount `null` / 막힘 0(강행만) → 없음 / PM 아님 → 없음
    - 역할: 대표 → Phase 6 항목 0 / 직원 → 자기 선결제 초과만 / 금액 정보 항목을 볼 수 없는 사람 → 그 항목 amount `null`
    - 공급원 하나가 던짐 → `listNextTurnItems`가 던진다(삼키지 않음)
  </behavior>
  <action>
`buildPurchaseBundle` · `buildIssueBundle` · `buildPrepaidOverdueItems` · `buildPreSettleBlockedItems`를 순수 함수로 만들고, 문자열은 위 「항목 문자열 대응」 표 그대로 쓴다. 수집 함수는 원천마다 권한을 먼저 확인한다: 구매는 `cards.purchases` write, 발행은 `projects.revenue` write다. PM 항목은 `projects.pmUserId = viewer` 프로젝트 범위이고, 직원 항목은 자기가 기안한 문서 범위다. 권한이 없는 원천은 부르지 않는다. 각 목록은 06-05 · 06-06의 목록 함수를 상태 필터와 함께 부르고, 정의를 다시 쓰지 않는다. 선결제 경과 일수는 `prepaidDueInfo`, 점검 막힘은 A-601 대응 상태의 PM 프로젝트마다 `computePreSettleCheck`로 얻는다(PM의 정산 프로젝트 수만큼). PM 항목의 금액은 지출결의 금액 정보 항목을 볼 수 있을 때만 싣고, 아니면 `null`이다.

`app/(app)/page.tsx`에서 공급 호출을 try/catch로 감싼다(PN-3). 실패하면 서버 로그에 오류를 남기고, 블록 자리에 `ListEmpty` `tone="error"`로 `내 차례를 불러오지 못했습니다` + 2차 `다시 시도`(→ `/`)를 그린다. 예외 메시지는 화면에 내지 않는다. 이때 빈 목록 줄(`표시할 항목이 없습니다`)은 그리지 않는다. 0건일 때의 기존 동작은 그대로 둔다. 태그 정렬과 6줄 절단은 `buildNextTurnView`가 이미 한다 — 새로 정렬하지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/next-turn-phase6.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/next-turn.test.ts test/integration/purchase-requests.test.ts test/integration/issue-requests.test.ts test/integration/pre-settle-check.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/next-turn-phase6.spec.ts test/e2e/mobile-next-turn.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 역할 넷(경영관리 · PM · 직원 · 대표)마다 받는 Phase 6 항목의 종류 집합을 단언한다(대표 = 빈 집합)
    - 통합 테스트가 "면제된 선결제 문서 → PM 항목 없음"과 "금액 정보 항목을 볼 수 없는 PM → amount `null`"을 단언한다
    - E2E가 세 가지를 단언한다: ⑴ 공급 실패를 강제하면 `내 차례를 불러오지 못했습니다`와 `다시 시도` 링크가 있고 홈의 `PageHeader`가 선다 ⑵ 항목 8개(막힘 2 · 오늘 2 · 대기 4)를 시드하면 보이는 줄이 6개이고 첫 두 줄이 `[막힘]`이며 `더 보기 2건`이 있다 ⑶ PM 계정의 점검 줄 금액 칸이 `—`다
  </acceptance_criteria>
  <done>역할마다 자기 할 일만 한 줄씩 서고, 실패는 오류 한 줄로 드러나며, 태그 순서와 6줄 상한이 지켜진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 경영관리 전체 E2E(이미지 · PDF 두 업로드 경로) · 누수 스캔 완전성 · 페이즈 전체 CI=true 게이트</name>
  <files>test/e2e/management-flow.spec.ts, test/integration/leak-scan.test.ts</files>
  <precondition>A-608 재확인에서 Phase 5 업로드 경로가 테스트 환경에서 돈다는 것이 확인됐다. 아니면 새 의존성을 더하지 않고 멈춰 묻는다.</precondition>
  <read_first>
    - .planning/ROADMAP.md 501-515행(Phase 6 기준 1~5 원문 — 이 태스크는 기준 5)
    - A-608 재확인에서 찾은 Phase 5 업로드 E2E와 저장소 테스트 모드
    - test/e2e/payment-single.spec.ts(06-01) · test/e2e/evidence-waive-prepaid.spec.ts(06-02) · test/e2e/fixtures.ts(로그인 · 시드 도우미)
    - test/integration/leak-scan.test.ts 1-40행(side-effect import 목록) · test/unit/leak-scan-coverage.test.ts · 06-01 ~ 06-09 SUMMARY의 「Artifacts」(새 DTO 이름 · 새 `actions.registry.ts`)
  </read_first>
  <behavior>
    - 이미지 경로: PM이 지출결의에 PNG 증빙을 첨부해 제출 → 결재자 승인 → 경영관리 홈 `[오늘] 지급` → 지급 대상 → 문서 `증빙 확인` → `지급 완료`(이체액 입력) → 지급 완료 목록에 번호
    - PDF 경로: 같은 흐름을 PDF 증빙으로(서버가 원본 그대로 받는다) → 지급 완료
    - 누수 스캔: Phase 6 DTO 이름이 모두 `DTO_REGISTRY`에 있고, Phase 6 새 `actions.registry.ts`가 모두 import돼 있다
  </behavior>
  <action>
`test/e2e/management-flow.spec.ts`를 만든다. 두 경로는 하나의 `describe` 안의 두 `test`다. 파일은 저장소에 바이너리로 두지 않는다. 테스트 안에서 작은 PNG 바이트 상수와 최소 PDF 바이트를 만들어 `setInputFiles({ name, mimeType, buffer })`로 넣는다. 업로드는 Phase 5 경로(A-608 테스트 모드)를 그대로 탄다. 각 경로는 한 문서가 PM 제출에서 경영관리 지급 완료까지 가는 흐름이다. 지급 완료 뒤에는 그 문서 번호가 `/expenses?view=paid`에 있고, 홈의 지급 묶음 건수가 1 줄어드는 것을 단언한다. 이 페이즈의 증빙 필수 설정은 켜 둔 상태로 돈다(EVID-02 — 증빙 확인 전에는 `지급 완료`가 비활성임을 한 번 단언한다).

`test/integration/leak-scan.test.ts`에 Phase 6 import가 빠짐없이 있는지 06-01 ~ 06-09 SUMMARY의 「Artifacts」와 대조한다. 빠진 것이 있으면 더한다. `it("Phase 6 DTO가 모두 등록돼 있다")`를 하나 더해, SUMMARY에서 모은 Phase 6 DTO 이름 목록이 `DTO_REGISTRY`의 이름에 모두 있음을 단언한다(ROADMAP 기준 5 — 지급·카드·구매 요청·증빙·발행 요청·점검).

페이즈 전체 게이트를 돌린다. 스키마 드리프트는 `pnpm db:generate`가 `No schema changes`를 출력하는지로 확인한다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/management-flow.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm db:dev &amp;&amp; pnpm test:integration</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝나거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:generate</automated>
    <fails_when>출력에 "No schema changes"가 없다(마이그레이션과 스키마가 어긋났다)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm lint:sql &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm test:e2e:ci</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝나거나, E2E failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - `management-flow.spec.ts`에 `image/png`와 `application/pdf` 두 mimeType 업로드가 각각 있고, 두 test가 모두 CI=true로 녹색이다
    - `leak-scan.test.ts`의 새 `it`이 Phase 6 DTO 이름을 SUMMARY 대조로 6개 이상 단언하고 녹색이다
    - `git diff --stat -- package.json pnpm-lock.yaml docs/design/tokens.css`가 비어 있다(새 의존성 · 새 토큰 없음)
    - 페이즈 전체 게이트(`pnpm test:e2e:ci` 포함) 결과가 SUMMARY에 요약돼 있다
  </acceptance_criteria>
  <done>ROADMAP 기준 5의 E2E · 통합 · 누수 스캔 항목이 CI=true로 녹색이고, Phase 6 전체 게이트가 한 번에 통과한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 홈 RSC → domain/next-turn | 보는 사람의 권한에 따라 원천마다 부를지 말지를 정한다 |
| domain/next-turn → 06-05 · 06-06 · 06-08 · 06-09 목록 함수 | 각 목록 함수의 기존 범위·가림을 그대로 물려받는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-55 | Information Disclosure | 권한 없는 역할에 지급·구매·발행 묶음(건수·합계) 노출 | medium | mitigate | 원천마다 `can(viewer, 메뉴, "write")`를 먼저 확인하고, 없으면 조회하지 않는다. 역할 넷 통합 테스트(대표 = 빈 집합) |
| T-06-56 | Information Disclosure | PM 항목에 볼 수 없는 지출결의 금액 | medium | mitigate | 금액 정보 항목 가시성이 없으면 `amount: null` → `—`. 통합 테스트로 확인한다 |
| T-06-57 | Information Disclosure | 오류 줄에 예외 메시지 노출 | low | mitigate | 고정 문구만 그리고 예외는 서버 로그에만 남긴다(`app/(app)/error.tsx` 선례) |
| T-06-58 | Denial of Service | 홈 로드마다 PM 정산 프로젝트 수만큼 점검 계산 | low | mitigate | 점검은 보는 사람이 PM인 정산 상태 프로젝트에만 돈다. 원천은 각각 목록 함수 한 번씩이다. 실패는 홈 전체가 아니라 블록 한 줄로 끝난다 |
| T-06-59 | Tampering | E2E 업로드 경로 | low | accept | 새 업로드 면을 만들지 않는다. Phase 5 경로를 그대로 타고, 테스트 파일은 코드 안의 바이트 상수다 |
</threat_model>

<verification>
- 단위 2(+ next-turn-action) · 통합 1(+ payment-batch · purchase-requests · issue-requests · pre-settle-check · leak-scan · 통합 전체) · E2E 2(+ mobile-next-turn · wordmark-home · `pnpm test:e2e:ci` 전체) 녹색
- `pnpm db:generate` → `No schema changes` · `pnpm lint:sql` 통과
- package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 별도 에이전트가 `CI=true`로 DOM을 실측 감사 → 수정 → 전체 게이트 한 번. 감사 범위는 S19와, 06-01 ~ 06-10 must_haves의 `verification: backstop` 항목 전부(페이즈 합계 39건)다. 전체 게이트(`pnpm test:e2e:ci`)는 감사 뒤 수정까지 끝난 다음 한 번만 돈다
</verification>

<success_criteria>
- 경영관리가 홈에서 지급 · 구매 요청 · 발행 요청의 할 일을 묶음 한 줄씩 보고, 한 번 눌러 해당 목록으로 간다
- PM이 선결제 증빙 기한 초과와 완료 전 점검 막힘을 홈에서 건마다 본다
- ROADMAP 기준 5(E2E 두 경로 · 통합 · 누수 스캔)가 CI=true로 녹색이다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `listNextTurnItems` · `listPhase6NextTurnItems` · `buildPaymentBundle` · `buildPurchaseBundle` · `buildIssueBundle` · `buildPrepaidOverdueItems` · `buildPreSettleBlockedItems` |
| 변경된 기존 심볼 | `NextTurnItem`(`amount: number \| null` · `reason: string \| null`) · `NextTurn`(빈 값 렌더) · 홈 `app/(app)/page.tsx`(공급 연결 · 오류 줄) |
| 테스트 | `test/unit/domain/next-turn-phase6.test.ts` · `test/integration/next-turn.test.ts` · `test/e2e/next-turn-phase6.spec.ts` · `test/e2e/management-flow.spec.ts` · `leak-scan.test.ts`(Phase 6 DTO 완전성 단언) |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-10-SUMMARY.md` when done
</output>
