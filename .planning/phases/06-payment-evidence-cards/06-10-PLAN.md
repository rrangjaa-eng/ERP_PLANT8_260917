---
phase: 06-payment-evidence-cards
plan: 10
type: execute
wave: 6
depends_on: ["06-06", "06-08"]
files_modified:
  - db/schema/expenses.ts
  - db/schema/files.ts
  - db/migrations/meta/_journal.json
  - domain/evidence-reviews/index.ts
  - domain/evidence-reviews/prepaid.ts
  - domain/expenses/index.ts
  - domain/settings/keys.ts
  - app/(app)/expenses/expense-form.tsx
  - app/(app)/expenses/[id]/evidence-review-section.tsx
  - app/(app)/expenses/[id]/actions.ts
  - app/(app)/expenses/[id]/actions.registry.ts
  - test/unit/domain/evidence-prepaid.test.ts
  - test/integration/evidence.test.ts
  - test/e2e/evidence-waive-prepaid.spec.ts
autonomous: true
requirements: [EXP-13, EVID-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 권한자가 증빙 0 · 면제 아닌 결재 통과 문서에서 3차 「증빙 면제」를 누르고 사유를 적어 확인하면 증빙 값만 `면제`로 바뀌고, 지급 기록·선결제 표시·선결제 사유는 그대로이며, 사유·닫은 사람·시각이 행동 로그(`document_update`)와 확인 줄 2행에 남는다(D-603, AS2)"
    - "사유 없는 증빙 면제는 서버가 거부한다 — 확인 모달의 사유 근거 칸이 비면 1차가 비활성이고, 액션을 직접 불러도 거부된다. 면제는 지출결의 행 잠금(R-1) 안에서 문서 version을 비교·+1 한다"
    - "증빙 면제 3차는 결재 통과 · 증빙 0 · 면제 아님이면 지급 전후·선결제 여부·증빙 필수 설정과 관계없이 선다(P3 · P4 선결제/필수 off `없음` · P6) — 06-04 판정 함수가 내보낸 자리를 증빙 섹션이 렌더한다. 3차라 행의 1차는 그대로다"
    - "지급 게이트 ctx의 `prepaid`는 `evidenceGateInputs`가 지출결의의 선결제 칸에서 읽는다 — 선결제 표시가 있거나 면제된 문서는 증빙 필수 설정이 켜져 있어도 `payment.evidence-required`를 통과한다(D-603, 06-04 ctx 배선 완료)"
    - "PM이 지출결의 폼에서 선결제를 켜면 `선결제 사유` 480 칸과 힌트 `증빙 기한 지급일부터 14일`(숫자 = 설정 값)이 서고, 끄면 칸이 사라지며 적은 사유는 버려진다(S6 empty)"
    - "선결제 on · 사유 빔이면 제출이 「막힘 — 선결제 칸」으로 막히고, 선결제면 Phase 5의 「증빙 없음」 제출 막힘이 풀린다 — 제출 뒤에는 읽기 줄 `선결제` · `{사유 원문}`만 있고 켜고 끄는 경로가 없다(O-4)"
    - "선결제 증빙 기한 = 지급일 + `evidence.prepaid_due_days`(기본 14)이고, 기한이 지나도 지급·완료 처리는 막히지 않는다 — 기한 초과는 표시(`증빙 N일 경과`)뿐이고 알림은 Phase 7이다(EXP-13, O-5, D-611)"
    - "면제된 선결제 문서에는 기한 2행(`증빙 기한 …` · `증빙 N일 경과`)이 어디에도 없다 — `면제`가 `선결제`를 이긴다"
    - "**B-2:** Phase 5 표(지출결의 · 파일)에 칸을 더하는 마이그레이션은 머리에 `SET LOCAL lock_timeout = '1s'` · `statement_timeout = '5s'` 한 쌍을 두고, 선결제 CHECK를 `NOT VALID`로 걸며, VALIDATE는 `pnpm db:generate --custom`으로 만든 별도 마이그레이션(SET LOCAL 한 쌍 포함)에서 한다 — `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate`가 `No schema changes`"
    - "S4 empty: P3 문서는 「Empty — 문서 화면 증빙」 + 같은 줄 3차 `증빙 면제`(지급 권한자), 1차 `지급 완료` 비활성 + `증빙 없음 · 담당 PM {이름}` / 면제 문서는 `면제` + 2행이고 1차가 산다 / 증빙 필수 off면 `없음`(`--muted`)이고 1차가 산다"
    - "S6 error: 「막힘 — 선결제 칸」 한 줄이 제출 버튼 옆에 선다"
    - statement: "S4 long-text: 면제 사유 원문이 2행에서 `keep-all` 줄바꿈되고 말줄임이 없다 — 200자 사유로 CI=true DOM 감사"
      verification: backstop
    - statement: "S6 loading: 제출 중 표시는 Phase 5 폼 규칙 그대로이고 이 플랜이 더하지 않는다 — Phase 5 폼 위에서 CI=true DOM 감사"
      verification: backstop
    - statement: "S6 long-text: 제출 뒤 읽기 줄의 선결제 사유 원문이 `keep-all`로 줄바꿈된다 — 200자 사유로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "사유 없이, 또는 행동 로그 없이 증빙을 면제해서는 안 된다 — 면제는 미결 점검(D-611)을 닫는 길이라 흔적 없는 면제는 점검을 무력화한다"
      status: resolved
      verification: test
    - statement: "선결제 증빙 기한 초과가 지급·완료 처리를 막아서는 안 된다(EXP-13 — 독촉만)"
      status: resolved
      verification: test
    - statement: "제출 뒤 선결제 표시를 바꿀 수 있어서는 안 된다(O-4)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/evidence-reviews/prepaid.ts"
      provides: "선결제 증빙 기한·경과 일수 계산(O-5 — 지급일 기준, 표시 전용)"
      exports: ["prepaidDueInfo"]
    - path: "domain/evidence-reviews/index.ts"
      provides: "증빙 면제 · 게이트 ctx의 선결제 배선"
      exports: ["waiveEvidence", "evidenceGateInputs"]
    - path: "db/schema/expenses.ts"
      provides: "Phase 5 지출결의 표의 `prepaid` · `prepaid_reason`(CHECK NOT VALID → 별도 VALIDATE)"
    - path: "db/schema/files.ts"
      provides: "Phase 5 파일 표의 `sha256` 칸 + 비유니크 인덱스(중복 검사는 06-11)"
    - path: "test/e2e/evidence-waive-prepaid.spec.ts"
      provides: "P3 → 면제 → 지급 완료 · 선결제 제출 두 경로"
      min_lines: 30
  key_links:
    - from: "domain/evidence-reviews/index.ts"
      to: "domain/payments/action-row.ts"
      via: "면제 행동이 06-04 판정 함수가 내보낸 3차 자리에 붙는다 — 자리 판정을 다시 쓰지 않는다"
      pattern: "waive"
    - from: "domain/expenses/index.ts"
      to: "domain/evidence-reviews/prepaid.ts"
      via: "Phase 5 제출 검증이 선결제 규칙을 읽는다"
      pattern: "prepaid"
---

<objective>
증빙을 **면제**로 닫는 길과 **선결제**(S6 · 기한 표시)를 세운다. 선결제·파일 해시는 Phase 5 표에 칸을 더해야 하므로 교차 검토 B-2의 기존 표 마이그레이션 절차(SET LOCAL 한 쌍 · CHECK NOT VALID · `--custom` VALIDATE)를 이 플랜이 처음 쓴다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 완료 전 점검(06-19)이 미결로 세는 「증빙 없는 문서(면제 제외, 선결제 포함)」의 모든 갈래에 닫는 길이 있어야 한다(면제). 선결제는 기안자가 제출 때 켜고(O-4) 처리를 막지 않는다(EXP-13). 06-04가 남긴 게이트 ctx의 `prepaid` · `waived`가 여기서 실제 값이 된다.

Output: Phase 5 표 칸 추가 마이그레이션 2개(생성 1 + `--custom` VALIDATE 1), `waiveEvidence` · `prepaidDueInfo`, 제출 검증, S4 면제 · S6 선결제, 단위 1 · 통합 1 · E2E 1.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다. 옛 06-02(19파일 · 115k)를 06-10 · 06-11로 나눴다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-06-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-10 착수하지 않음`을 출력하고 멈춘다.** 가칭은 06-03 SUMMARY 대응표를 따른다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-607 | Phase 5 지출결의 domain — 제출 함수(가칭 `submitExpense`, 「증빙 없음」 제출 막힘 포함)와 지출결의 폼(가칭 `app/(app)/expenses/expense-form.tsx`) | `git ls-files 'domain/expenses/*' 'app/(app)/expenses/expense-form.tsx'` 비면 멈춤 · 제출 함수를 grep으로 찾지 못하면 멈춤 |
| A-608 | Phase 5 파일 표(가칭 `db/schema/files.ts`) | `git ls-files 'db/schema/*file*'` 비면 멈춤. 이미 내용 해시 칸이 있으면 그 칸을 쓰고 더하지 않는다 |
| 06-06 | `expense_evidence_reviews` · `evidenceGateInputs` · `resolveEvidenceStatus` | `grep -c "evidenceGateInputs" domain/evidence-reviews/index.ts` 0이면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(면제 모달) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| 06-08 | 마이그레이션 저널에 06-08의 `purchase_requests`가 먼저 있다 | `grep -c "purchase_requests" db/migrations/meta/_journal.json` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-4 | 선결제는 기안자가 제출 때만 켠다, 제출 뒤 변경 없음 | Task 3의 폼 칸 · 제출 검증 |
| O-5 | 증빙 기한 = 지급일부터 N일(설정 기본 14) | `domain/evidence-reviews/prepaid.ts` 한 함수 |
| AS2 | 면제 = `document_update` | `waiveEvidence`의 `recordAction` 인자 |

<assumption_delta_decision>
`detected: false`. Phase 5 표에 칸을 **더한다** — 명사를 옮기지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-13 · EVID-02 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 1 · 기준 2 · D-603 · D-611 · O-4 · O-5. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 증빙 면제 한 경로(P3 문서 → 3차 「증빙 면제」 → 사유 → 1차 「지급 완료」 살아남 → 지급 완료)</name>
  <files>domain/evidence-reviews/index.ts, app/(app)/expenses/[id]/evidence-review-section.tsx, app/(app)/expenses/[id]/actions.ts, app/(app)/expenses/[id]/actions.registry.ts, test/integration/evidence.test.ts, test/e2e/evidence-waive-prepaid.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 358-383행(「증빙 면제의 자리」) · 945행(S4 empty) · Copywriting 「Destructive — 증빙 면제」 · 「표시 — 증빙 면제 결과」
    - domain/evidence-reviews/index.ts · domain/payments/action-row.ts(06-04 · 06-06 — 3차 `waive` 자리)
    - repositories/expense-payments.ts(`lockExpenseRow` · `bumpExpenseVersion`) · repositories/expense-evidence-reviews.ts(upsert)
    - ui/confirm-dialog/ConfirmDialog.tsx(슬롯 이름)
  </read_first>
  <behavior>
    - `waiveEvidence`: 지급 권한 없음 → ForbiddenError / 사유 빈 문자열 → 거부 / 증빙 1개 이상 → 거부 / 결재 통과 전 → 거부 / 정상 → 확인 기록 status `waived` + 사유 + `document_update` + 문서 version +1
    - 면제 뒤 `resolveEvidenceStatus` = `면제`, `payment.evidence-required` 통과, 지급 기록(있으면) 불변
    - E2E: `evidence.required` on · 증빙 0 문서(Phase 5 domain 함수로 준비) → 3차 `증빙 면제` → 사유 → 1차 `지급 완료` 활성 → 지급 완료
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

`waiveEvidence(viewer, { expenseId, reason, version })`: `expenses.payments` write 필수(D-601) · 한 트랜잭션에서 `lockExpenseRow`(R-1) → version 비교 → 결재 통과 · 증빙 0 · 면제 아님 확인 → 확인 기록 upsert(status `waived`, 사유) → `bumpExpenseVersion` · `recordAction` `document_update`에 사유(AS2). 증빙 값만 `면제`로 바뀌고 지급 기록·선결제 칸은 건드리지 않는다(D-603). 06-04 판정 함수가 내보내는 3차 `waive` 자리(P3 · P4의 선결제/필수 off `없음` · P6)에 이 행동을 붙인다 — 자리 판정을 다시 쓰지 않는다.

`evidence-review-section.tsx`: 3차 `증빙 면제`(판정 결과에 자리가 있을 때만 렌더) → `ConfirmDialog`(Copywriting 「Destructive — 증빙 면제」 — 결과 줄만 지급 전후·선결제로 갈린다, 사유 근거 칸 필수), 면제 뒤 확인 줄 `면제` + 2행 `{사유} · {사람} {MM-DD HH:mm}`(`keep-all`). S4 empty 세 갈래를 이 섹션에서 맞춘다. `waiveEvidenceAction`을 `actions.ts` · registry에 더한다(기존 registry 파일이라 누수 스캔 import 변경 없음).

`test/integration/evidence.test.ts`(신규)에 behavior 두 줄, E2E `evidence-waive-prepaid.spec.ts`(신규)에 셋째 줄. 증빙 행이 필요한 준비는 업로드가 아니라 Phase 5 domain 함수로 한다(B-4).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-waive-prepaid.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - 통합 테스트가 behavior 두 줄의 모든 갈래(권한 · 사유 · 증빙 있음 · 결재 전 · 정상 · 지급 기록 불변)를 개별 `it`으로 단언한다
    - E2E 한 경로가 CI=true로 녹색이다
  </acceptance_criteria>
  <done>P3 문서가 사유 있는 면제로 지급 완료까지 가는 한 경로가 domain → 액션 → 화면 → 행동 로그를 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 5 표 칸 추가(B-2 절차) · 게이트 ctx 선결제 배선 · 기한 계산</name>
  <files>db/schema/expenses.ts, db/schema/files.ts, db/migrations/meta/_journal.json, domain/evidence-reviews/index.ts, domain/evidence-reviews/prepaid.ts, test/unit/domain/evidence-prepaid.test.ts, test/integration/evidence.test.ts</files>
  <read_first>
    - db/migrations/0003_permissions_masters_spine.sql 80-90행(기존 표 제약 `NOT VALID` + 주석 선례) · db/migrations/0004_users_role_id_validate.sql 전체(별도 VALIDATE 선례) · db/migrations/0010_revenue_entries.sql 1-5행
    - .squawk.toml(`assume_in_transaction` · 예외 규칙)
    - drizzle-kit `generate --custom` 사용법(`pnpm db:generate --help`)
  </read_first>
  <behavior>
    - `prepaidDueInfo`: 지급 전 → null / 지급일 2026-09-05 · 14일 → 기한 2026-09-19 / 오늘 09-21 · 증빙 0 · 면제 아님 → 경과 2 / 면제 → null / 증빙 있음 → null
    - `evidenceGateInputs`: 선결제 칸 true → `prepaid: true` → 증빙 필수 on · 증빙 0이어도 게이트 통과
    - 선결제 기한이 지난 문서의 지급 완료 → 성공(막히지 않음)
  </behavior>
  <action>
**마이그레이션(B-2 — 기존 표).** Phase 5 지출결의 표에 `prepaid boolean not null default false` · `prepaid_reason text null` · CHECK(선결제면 사유 있음), 파일 표에 내용 해시 칸이 없으면 `sha256 text null` + 비유니크 인덱스를 Drizzle 스키마에 더한다. 절차: ⑴ `pnpm db:generate --name evidence_prepaid_sha256` ⑵ 생성 SQL 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` 한 쌍 ⑶ 선결제 CHECK 문장 끝에 `NOT VALID`를 붙이고 0003처럼 이유 주석(기존 행 스캔 없이 새 행부터 강제, 검증은 다음 파일) ⑷ `pnpm db:generate --custom --name evidence_prepaid_validate`로 빈 마이그레이션을 만들고 SET LOCAL 한 쌍 + `ALTER TABLE … VALIDATE CONSTRAINT …`를 적는다(0004 선례) ⑸ 인덱스는 트랜잭션 안 일반 `CREATE INDEX`(`.squawk.toml`이 CONCURRENTLY 규칙을 뺀 이유와 같다) ⑹ `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 `pnpm db:generate`가 `No schema changes`. 유니크 제약이 필요하면 `uniqueIndex()`만 쓴다(이 플랜에는 없다).

**domain.** `domain/evidence-reviews/prepaid.ts`에 `prepaidDueInfo(expense, payment, today, dueDays)`(O-5 — 지급일 기준, 면제·증빙 있음이면 null). 기한은 **표시 전용**이다 — 게이트·지급·완료 경로 어디서도 막는 데 쓰지 않는다(EXP-13). `evidenceGateInputs`가 잠근 문서의 선결제 칸을 `prepaid`로 넘기게 한다(06-06이 남긴 false 채움을 실제 값으로 — D-603). 단위 테스트는 behavior 첫 줄, 통합 테스트에 둘째·셋째 줄.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence-prepaid.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 `Found N issues`를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence.test.ts test/integration/expense-payments.test.ts test/integration/migrate-runner.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — migrate-runner가 빨가면 `--custom` 마이그레이션이 러너 규약을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "NOT VALID" db/migrations/*evidence_prepaid_sha256*.sql`이 1 이상 · `grep -c "VALIDATE CONSTRAINT" db/migrations/*evidence_prepaid_validate*.sql`이 1
    - 두 SQL 파일 모두 `grep -c "SET LOCAL lock_timeout"`이 1
    - 통합 테스트가 기한 초과 문서의 지급 완료 성공을 단언한다
  </acceptance_criteria>
  <done>선결제 · 해시 칸이 기존 표 마이그레이션 규약대로 들어가고, 게이트가 선결제를 실제 값으로 읽으며, 기한은 표시로만 쓰인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 선결제 폼(S6) · 제출 검증(O-4) · 기한 2행 · readBy 제거</name>
  <files>domain/expenses/index.ts, domain/settings/keys.ts, app/(app)/expenses/expense-form.tsx, app/(app)/expenses/[id]/evidence-review-section.tsx, test/integration/evidence.test.ts, test/e2e/evidence-waive-prepaid.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 945-962행(S4 partial · S6 전부) · Copywriting 「막힘 — 선결제 칸」
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-603 · D-611
    - test/unit/settings/registry-coverage.test.ts(readBy 만료)
    - A-607 재확인에서 찾은 Phase 5 제출 함수 · 폼 파일
  </read_first>
  <behavior>
    - 제출 검증: 선결제 on · 사유 빔 → 막힘 / 선결제 on · 사유 · 증빙 0 → 제출 성공(Phase 5 증빙 없음 막힘 풀림) / 제출 뒤 선결제 바꾸기 → 거부
    - 폼 힌트의 N = `evidence.prepaid_due_days` 설정 값(서버가 보냄)
    - 면제된 선결제 문서 → 기한 2행 없음
  </behavior>
  <action>
Phase 5 제출 함수(A-607 실제 이름, `domain/expenses/index.ts`)에 선결제 검증을 더한다 — 선결제 on이면 사유 필수, 그러면 Phase 5의 「증빙 없음」 제출 막힘을 풀고, 제출 뒤 선결제 칸 변경은 거부한다(O-4). 폼(`expense-form.tsx`)에 선결제 체크박스 + 켜면 나타나는 `선결제 사유` 480 칸 + `Form.Hint` `증빙 기한 지급일부터 {N}일`(N은 서버가 설정에서 읽어 보냄 — 이 플랜이 `evidence.prepaid_due_days`를 처음 읽으므로 `domain/settings/keys.ts`의 그 키에서 readBy 표시를 지운다), 끄면 칸이 사라지고 사유를 버린다. 막힘 문구는 「막힘 — 선결제 칸」 그대로. 문서 화면 확인 줄에 제출 뒤 읽기 줄 `선결제` · `{사유}`(`keep-all`)와 지급 뒤 기한 2행(`증빙 기한 {MM-DD}` / `증빙 {N}일 경과`, 면제면 없음 — `prepaidDueInfo`)을 더한다. 통합 테스트 · E2E(「선결제 켜기 → 사유 빔 막힘 → 사유 적고 제출 → 읽기 줄 `선결제`」)를 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/settings/registry-coverage.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — registry-coverage가 빨가면 `evidence.prepaid_due_days`의 readBy를 지우지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-waive-prepaid.spec.ts test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 세 줄의 갈래를 개별 `it`으로 단언한다(제출 뒤 선결제 변경 거부 포함)
    - E2E 선결제 케이스와 06-03~06-06의 payment-single E2E가 CI=true로 녹색이다
  </acceptance_criteria>
  <done>선결제가 사유와 함께 제출 때만 켜지고 처리를 막지 않으며, 기한이 표시로만 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 면제 액션 | 사유 · version이 넘어온다 |
| 브라우저 → 제출(Phase 5) | 선결제 · 사유가 넘어온다 |
| 마이그레이션 → Phase 5 표 | 이 플랜이 남의 페이즈 표(지출결의 · 파일)에 칸·제약·인덱스를 더한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Denial of Service | Phase 5 표 ALTER 마이그레이션 | medium | mitigate | 기본값 있는 NOT NULL · nullable 칸만 더하고, 머리에 `SET LOCAL lock_timeout = '1s'` · `statement_timeout = '5s'` 한 쌍(0010 선례)을 둔다. 선결제 CHECK는 `NOT VALID`로 걸고 VALIDATE는 `pnpm db:generate --custom`으로 만든 별도 마이그레이션(SET LOCAL 한 쌍 포함)에서 한다 — 이 분리가 주는 것은 저장소 선례(0003/0004)를 따르는 것과 squawk 파일 단위 검사(`constraint-missing-not-valid`) 통과뿐이다. drizzle `migrate()`는 대기 중인 마이그레이션 전부를 한 트랜잭션으로 감싸므로(`.squawk.toml` 1-3행), 두 파일이 한 배포에서 함께 적용되면 잠금 시간은 줄지 않는다(N-5 — 표가 작아 위험은 낮다). 인덱스는 트랜잭션 안 일반 CREATE INDEX(squawk 예외 규약). `pnpm lint:sql` 0건 · 두 번째 generate `No schema changes`(B-2) |
| T-06-46 | Elevation of Privilege | `waiveEvidence` | high | mitigate | domain 첫 줄 `can(viewer, "expenses.payments", "write")` · 결재 통과 · 증빙 0 조건을 서버가 판정. 권한 없는 계정 거부 통합 테스트 |
| T-06-47 | Repudiation | 증빙 면제 | medium | mitigate | 사유 필수 + `document_update` 행동 로그(사람·시각·사유). 면제가 풀려도 로그는 남는다 |
| T-06-48 | Tampering | 선결제 칸 사후 변경 | medium | mitigate | 제출 뒤 선결제 변경을 domain이 거부(O-4). 통합 테스트 |
| T-06-49 | Tampering | 면제 경합 | medium | mitigate | `lockExpenseRow`(R-1) · 문서 version 비교·+1 |
</threat_model>

<verification>
- 단위 evidence-prepaid · registry-coverage · leak-scan-coverage, 통합 evidence · expense-payments · migrate-runner · leak-scan 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E evidence-waive-prepaid · payment-single이 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S4(면제) · S6 DOM 실측 감사(backstop 3건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 미결 점검이 세는 「증빙 없는 문서」에 사유 있는 면제로 닫는 길이 있다
- 선결제가 사유와 함께만 켜지고 기한이 처리를 막지 않는다
- 기존 표 마이그레이션이 B-2 규약(SET LOCAL · NOT VALID · 별도 VALIDATE)대로 들어간다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컬럼(Phase 5 표) | `prepaid` · `prepaid_reason`(지출결의) · `sha256`(파일, 없을 때만) |
| 마이그레이션 | `NNNN_evidence_prepaid_sha256.sql`(SET LOCAL · CHECK NOT VALID) · `NNNN_evidence_prepaid_validate.sql`(`--custom`, VALIDATE) |
| 함수(domain) | `waiveEvidence` · `prepaidDueInfo` · `evidenceGateInputs`(선결제 배선) · Phase 5 제출 함수 선결제 검증 |
| Server Action | `waiveEvidenceAction` |
| 변경된 기존 심볼 | `EVIDENCE_PREPAID_DUE_DAYS` readBy 제거 · Phase 5 지출결의 폼 선결제 칸 |
| 테스트 | `test/unit/domain/evidence-prepaid.test.ts` · `test/integration/evidence.test.ts` · `test/e2e/evidence-waive-prepaid.spec.ts` |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-10-SUMMARY.md` when done
</output>
