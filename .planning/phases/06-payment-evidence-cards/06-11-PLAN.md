---
phase: 06-payment-evidence-cards
plan: 11
type: execute
wave: 7
depends_on: ["06-10"]
files_modified:
  - domain/evidence-reviews/index.ts
  - domain/evidence-reviews/cost-basis.ts
  - domain/evidence-reviews/upload-checks.ts
  - domain/expenses/evidence.ts
  - domain/settings/keys.ts
  - ui/attachments/Attachments.tsx
  - docs/OPERATIONS.md
  - test/unit/domain/evidence-cost-basis.test.ts
  - test/unit/domain/evidence-upload-checks.test.ts
  - test/integration/evidence-invalidation.test.ts
  - test/integration/evidence-upload-intent.test.ts
  - test/e2e/evidence-lifecycle.spec.ts
autonomous: true
requirements: [EVID-03, EVID-04, EVID-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PM이 증빙을 더하거나 떼거나 증빙 금액을 고치면(Phase 5 PM 편집 경로) 같은 트랜잭션에서 지출결의 행을 `FOR UPDATE`로 잠근 뒤(R-1) `확인됨` → `확인 전`, `면제` → `확인 전`으로 풀리고 이전 확인·면제의 행동 로그는 남는다(S4 partial)"
    - "**B-3:** 경영관리가 금액을 고쳐 확인하는 `confirmEvidence`는 이 훅을 타지 않는다 — 훅을 붙인 뒤에도 06-06의 `test/integration/evidence-reviews.test.ts` 「고쳐 확인 → 확인됨 유지」가 녹색이다"
    - "**B-1(디자인 반영 r2):** 이 훅은 확인 기록이 있든 없든(`확인 전` 문서여도) 같은 트랜잭션에서 문서 version을 +1 한다(06-03 `bumpExpenseVersion`) — PM이 증빙을 바꾼 뒤 옛 version으로 부른 `confirmEvidence`는 동시성으로 거부되어, 경영관리가 보지 않은 증빙 묶음이 `확인됨`이 되지 않는다(UI-SPEC 금액 표시 절 「낙관적 잠금」 · S1 「제자리 증빙 확인」 「서버」). 통합 `it` 「PM 증빙 변경은 문서 version을 올린다」 · 「PM 증빙 변경 뒤 옛 version 확인 거부」"
    - "지급 뒤 들어온 선결제 증빙은 P5 1차 `증빙 확인`만 세우고 지급 칸·지급 행동은 다시 서지 않으며, 지급 뒤 S4에는 서버 계산 한 줄(지급 총액)이 없다"
    - "증빙 금액(공급가)이 있으면 그 문서의 비용 기준은 `확정` · 증빙 금액(원화 환산액)이고, 증빙을 모두 떼면 `예상` · 승인액(없으면 견적 실행가)으로 돌아간다 — 확인 여부는 이 판정에 들어가지 않는다(EVID-03 · EVID-04 · D-602)"
    - "업로드 의도 요청에서 서버가 크기(`evidence.max_size_mb`, 기본 10MB) · 형식(이미지 · PDF) · 중복(SHA-256)을 검사해 걸리면 서명 URL을 내주지 않는다(기준 2) — 검사 함수는 첨부 주인 종류(`expense` 등)를 인자로 받는 한 함수다(06-16이 다른 주인에 그대로 쓴다). **중복은 같은 주인 종류 안에서만 막는다(DR-3 · UI-SPEC S7 · O-6 rev 9)** — 지출결의↔지출결의 · 차수 승인↔차수 승인 · 리저브↔리저브, 다른 종류 주인에 같은 파일은 막지 않는다. 이 플랜 때 주인은 지출결의뿐이라 보이는 동작은 같다"
    - "**R-5:** 중복 검사의 SHA-256은 브라우저가 보낸 **참고 값**이다 — 같은 파일을 두 번 올리는 실수를 막는 편의이고, 어떤 권한·금액·게이트 판정도 이 값에 기대지 않는다. 실제 크기·형식은 서명 URL 조건과 완료 통보 때 Phase 5의 GCS 메타데이터 재확인이 강제한다"
    - "중복 파일의 기존 문서 번호는 올린 사람이 그 문서를 읽을 수 있고 그 주인이 지출결의일 때만 보이고, 읽을 수 없으면 번호 없이 문구만 선다(O-6 — `scopeFor`로 판정)"
    - "업로드 의도만 있고 완료 통보가 없으면 파일 행이 생기지 않고, 증빙 수·비용 기준·지급 게이트 판정이 바뀌지 않는다(기준 5 · F8)"
    - "완료 통보 없는 고아 객체를 찾아 지우는 절차가 `docs/OPERATIONS.md`에 있고 문서가 300줄 상한 안이다(F8)"
    - "**B-4:** 실제 파일 업로드를 쓰는 E2E는 이 플랜이 처음이다 — Task 1 ⓪이 Phase 5 저장소의 테스트 모드(CI=true E2E에서 GCS 없이 업로드가 끝나는 경로)를 확인하고, 없으면 코드를 바꾸지 않고 멈춰 사용자에게 묻는다(⑴ Phase 5 저장소 어댑터 테스트 모드 ⑵ 에뮬레이터 컨테이너 — 새 기반 시설, 승인 필요). 06-24는 재확인만 한다. 「증빙 있음」이 필요한 다른 E2E는 업로드가 아니라 Phase 5 domain 함수로 행을 만든다"
    - "S7 empty: 첨부 영역 한 줄 `이미지·PDF 10MB`의 숫자가 설정 값이다"
    - "S7 loading: 업로드 행 아래 2px 진행 바(§7-10)가 서고, 폰 사진은 최대 변 2000px JPEG로 줄인 뒤 올린다 — PDF는 원본이다"
    - "S7 error: 「Error — 증빙 업로드」 넷(크기 · 형식 · 중복 · 완료 통보 실패)이 파일 행 자리에 선다"
    - "S7 populated: 파일 행이 §7-10(이름 · 크기 · 날짜 · 크게 보기)대로 선다"
    - "S7 partial: 여러 파일 중 일부만 실패하면 성공 행과 실패 행이 각각 선다(§7-7 첨부 PARTIAL)"
    - "S7 zero-one-many: 문서의 마지막 증빙 삭제는 확인 모달(「Destructive — 증빙 삭제」, 결과 줄 = 증빙 금액 지워짐 · 증빙 확인 풀림)을 열고, 마지막이 아닌 파일은 모달 없이 지워지며, 지출결의가 아닌 주인(카드 사용 등)의 첨부는 마지막이어도 모달이 없다(SP-6)"
    - statement: "S7 overflow: 파일 10개 이상 문서에서 첨부 영역 행 목록이 §7-10 규칙대로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S7 long-text: 80자 한글 파일명이 파일 행에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "중복 검사 응답이 올린 사람이 읽을 수 없는 문서의 번호를 드러내서는 안 된다(O-6 — SHA-256 한 값으로 남의 문서 존재·번호를 캐는 경로)"
      status: resolved
      verification: test
    - statement: "증빙 확인 여부가 비용 기준을 바꿔서는 안 된다 — 확인은 새 비용 단계가 아니다(D-602)"
      status: resolved
      verification: test
    - statement: "업로드 테스트 모드가 없는데 GCS 에뮬레이터 컨테이너 같은 새 기반 시설을 승인 없이 더해서는 안 된다(CLAUDE.md §5 새 의존성)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/evidence-reviews/cost-basis.ts"
      provides: "문서 비용 기준(확정 · 증빙 금액 / 예상 · 승인액 · 실행가) 판정 — EVID-03·04"
      exports: ["expenseCostBasis"]
    - path: "domain/evidence-reviews/upload-checks.ts"
      provides: "업로드 의도 검사(크기·형식·중복 — 중복은 같은 주인 종류 안, 주인 종류 인자) + 마지막 증빙 삭제 확인 판정"
      exports: ["checkEvidenceUpload", "lastEvidenceDeleteNeedsConfirm"]
    - path: "domain/evidence-reviews/index.ts"
      provides: "PM 증빙 변경 → 확인 풀림 + 문서 version +1(행 잠금 뒤, 확인 기록 유무와 관계없이 — B-1 r2)"
      exports: ["invalidateEvidenceReview"]
    - path: "test/integration/evidence-upload-intent.test.ts"
      provides: "업로드 의도만 있고 완료 없는 경우의 불변식 · 검사 거부"
      min_lines: 30
  key_links:
    - from: "domain/expenses/evidence.ts"
      to: "domain/evidence-reviews/index.ts"
      via: "Phase 5 PM 증빙 추가·삭제·금액 고침이 같은 트랜잭션에서 확인 풀림을 부른다"
      pattern: "invalidateEvidenceReview"
    - from: "domain/evidence-reviews/index.ts"
      to: "repositories/expense-payments.ts"
      via: "훅이 행 잠금 뒤 확인 기록 유무와 관계없이 문서 version을 올린다(B-1 r2 — 보지 않은 증빙이 옛 version으로 확인되지 않는다)"
      pattern: "bumpExpenseVersion"
    - from: "domain/evidence-reviews/upload-checks.ts"
      to: "domain/permissions/scope-for.ts"
      via: "중복 문서 번호 노출을 읽기 범위로 가른다"
      pattern: "scopeFor"
    - from: "domain/expenses/evidence.ts"
      to: "domain/evidence-reviews/upload-checks.ts"
      via: "서명 URL 발급 전에 검사한다"
      pattern: "checkEvidenceUpload"
---

<objective>
증빙의 나머지 수명 주기를 닫는다 — PM 증빙 변경이 확인을 풀고 문서 version을 올리는 훅(행 잠금 뒤, B-3과 겹치지 않게 — version은 디자인 반영 r2 B-1), 비용 기준 판정(EVID-03 · EVID-04), 업로드 검사(크기·형식·중복, 주인 종류 일반), 마지막 증빙 삭제 확인(SP-6), 의도만 있는 업로드의 불변식, 고아 청소 절차.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 기준 2(증빙 = 확정 비용 · 업로드 순서 · F8)가 이 플랜에 모인다. 업로드 경로 자체(서명 URL · GCS)는 Phase 5 것이고(A-608), 이 플랜은 그 앞에 검사를 세우고 뒤에 청소 절차를 적는다. 교차 검토 B-4대로 실제 업로드를 쓰는 첫 E2E라 테스트 모드 확인을 Task 1 ⓪에 둔다.

Output: `invalidateEvidenceReview` 훅 · `expenseCostBasis` · `checkEvidenceUpload` · `lastEvidenceDeleteNeedsConfirm`, 첨부 컴포넌트 오류·모달, `docs/OPERATIONS.md` 고아 청소 절, 단위 2 · 통합 2 · E2E 1.

**규모 근거:** `files_modified` 12개, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-06-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-10-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-11 착수하지 않음`을 출력하고 멈춘다.** 가칭은 06-03 SUMMARY 대응표를 따른다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-607 | Phase 5 PM 증빙 추가·삭제·금액 고침 domain 함수(가칭 `domain/expenses/evidence.ts`) | `git ls-files domain/expenses/evidence.ts` 비면(실제 이름을 grep으로도 못 찾으면) 멈춤 |
| A-608 | Phase 5 업로드 경로 — 업로드 의도(서명 PUT URL 발급) domain 함수 · 완료 통보 · 파일 표 · 첨부 영역 컴포넌트(가칭 `ui/attachments/Attachments.tsx`) | `git ls-files 'db/schema/*file*' 'ui/attach*'` 비면 멈춤 · 서명 URL 발급 함수를 grep(`signed\|putUrl\|uploadIntent`)으로 찾지 못하면 멈춤 |
| A-608-T (B-4) | **Phase 5 저장소의 테스트 모드** — `CI=true` E2E에서 실제 GCS 없이 업로드 의도 → 업로드 → 완료 통보가 끝나는 경로(가칭 저장소 어댑터 · 환경 변수) | `grep -rln "STORAGE\|storage.*test\|fake.*gcs\|memory.*storage" lib/ domain/ scripts/ playwright.config.ts` 로 찾고 SUMMARY에 적는다. **찾지 못하면 멈추고 아래 「업로드 테스트 모드 공백」 선택지를 묻는다** |
| 06-10 | `waiveEvidence` · 파일 해시 칸 | `grep -c "waiveEvidence" domain/evidence-reviews/index.ts` 0이면 멈춤 |
| UA-602 · UA-606 | `ConfirmDialog` · 첨부 영역이 증빙 금액 칸·증빙일과 한 묶음이고 제출 뒤에도 PM이 증빙을 더하고 뗄 수 있다 | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 · 첨부 컴포넌트 props를 읽어 확인, 다르면 SUMMARY에 적는다 |

**업로드 테스트 모드 공백(B-4 — 계획이 스스로 정하지 않는다):** 테스트 모드가 없으면 Task 1 ⓪에서 `선행 의존 A-608-T 미해소 — 업로드 테스트 모드 없음, 06-11 착수하지 않음`과 함께 묻는다 — ⑴ Phase 5 저장소 어댑터에 테스트 모드를 Phase 5 쪽 작업으로 더한 뒤 재개(새 의존성 없음, 경로의 주인이 고친다) ⑵ GCS 에뮬레이터 컨테이너를 로컬 DB처럼 테스트 기반에 더함(새 기반 시설 — CLAUDE.md §5 승인 필요, 승인 문장을 SUMMARY에 옮긴다). 즉석으로 GCS를 흉내 내지 않는다.

**새 의존성 없음:** 브라우저 이미지 축소는 Canvas API, SHA-256은 Web Crypto(`crypto.subtle.digest`)로 한다. Phase 5가 이미 축소·해시를 하면 그것을 쓴다. Canvas · Web Crypto로 안 되는 요구가 드러나면 설치하지 말고 멈춰 사용자에게 묻는다.

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-6 | 중복(SHA-256)은 **같은 주인 종류 안에서만** 막는다(rev 9, DR-3 = 허용 — 사용자 확정(2026-09-25)) — 읽을 수 있는 지출결의면 문서 번호, 아니면 문구만. 답이 「종류와 상관없이 모두 막기」로 오면 범위 조건 하나를 뺀다 | `upload-checks.ts` 중복 갈래 · 중복 조회의 주인 종류 범위 조건 하나 |
| O-2 | (06-06 소관) 확인 풀림은 O-2 답과 무관하게 같다 | 해당 없음 |
| B-4 테스트 모드 | Phase 5 저장소 어댑터의 테스트 모드를 쓴다 — 없으면 Task 1 ⓪에서 묻는다 | Task 1 ⓪ · Task 3 E2E 준비 |

<assumption_delta_decision>
`detected: false`. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EVID-03 · EVID-04 · EVID-02 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 2 · D-602 · O-6. 전체 10건 표는 06-01 `<probe_fallback>`.

**추가 가정(표면화):** EVID-04의 「예상」 값은 「지출결의 승인액, 없으면 견적 실행가」로 읽었다(PNL-02 우선순위 — 증빙액 > 승인액 > 실행가). 프로젝트 합계 SQL은 Phase 9 소관이고 이 플랜은 문서 한 건의 판정 함수만 만든다.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — `확인됨` 문서에서 PM이 증빙 금액을 고치면 잠금 뒤 확인이 풀려 경영관리 화면이 P2로 돌아간다</name>
  <files>domain/evidence-reviews/index.ts, domain/expenses/evidence.ts, test/integration/evidence-invalidation.test.ts, test/e2e/evidence-lifecycle.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 A-607 · A-608 · A-608-T · 06-10 · UA-602가 main에 있다 — ⓪이 확인하고 없으면 멈춘다(A-608-T가 없으면 선택지를 묻는다).</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md UI Considerations `S4` partial 행 · 「금액 표시」 절 「낙관적 잠금」(PM 증빙 변경도 version을 올린다 — r2 B-1) · S1 「제자리 증빙 확인」 「서버」 줄
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md D-602
    - domain/evidence-reviews/index.ts(06-06 · 06-10) · repositories/expense-payments.ts(`lockExpenseRow` · `bumpExpenseVersion` — 06-03, 실제 시그니처는 06-03 SUMMARY)
    - 06-06 SUMMARY ⓪의 N-6 목록(Phase 5 PM 증빙 편집 함수가 스스로 문서 version을 올리는지)
    - test/integration/evidence-reviews.test.ts(06-06 — 「고쳐 확인 → 확인됨 유지」)
    - A-607 재확인에서 찾은 Phase 5 PM 증빙 편집 함수
  </read_first>
  <behavior>
    - PM 증빙 추가·삭제·금액 고침 → 같은 트랜잭션에서 확인 기록 삭제, 이전 확인·면제 행동 로그 보존, 상태 `확인 전`
    - 훅이 지출결의 행을 잠근 뒤 확인을 푼다(동시 지급 완료와 직렬화 — `Promise.all`로 PM 금액 고침 ∥ 지급 완료 → 둘 다 끝난 뒤 상태가 일관: 지급 완료면 P5 또는 P6, 아니면 P2)
    - 경영관리 `confirmEvidence`(금액 고침)는 이 훅을 타지 않는다 — 06-06 테스트 녹색 유지
    - [B-1 r2] 「PM 증빙 변경은 문서 version을 올린다」: `확인 전`(확인 기록 없음) 문서에 PM이 파일을 더함 → 문서 version +1 · PM 저장 성공 / `확인됨` 문서에서 PM이 증빙 금액을 고침 → 확인 풀림 + version +1 / 파일을 뗌 → version +1
    - [B-1 r2] 「PM 증빙 변경 뒤 옛 version 확인 거부」: `확인 전` 문서의 version v를 읽음 → PM이 파일을 더함 → `confirmEvidence({ expenseId, version: v })` → 동시성 거부 · 확인 기록 없음 → 새 version으로 다시 → `확인됨`
    - E2E: `확인됨` 문서(증빙 행은 Phase 5 domain 함수로 준비) → PM이 문서 화면에서 증빙 금액을 고쳐 저장 → 경영관리 계정으로 열면 확인 줄 `확인 전` · 1차 `증빙 확인`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. A-608-T(업로드 테스트 모드)를 찾아 결과(경로)를 SUMMARY에 적는다 — 없으면 코드를 한 줄도 바꾸지 않고 「업로드 테스트 모드 공백」의 두 선택지를 출력하고 멈춘다(B-4 — 업로드가 처음 필요한 플랜의 첫 자리에서 묻는다).

`domain/evidence-reviews/index.ts`에 `invalidateEvidenceReview(viewer, expenseId, tx)` — `lockExpenseRow`(R-1)로 지출결의 행을 잠근 뒤 확인 기록을 지우고(행동 로그는 남는다) `확인 전`으로 되돌린다. Phase 5 PM 증빙 추가·삭제·증빙 금액 고침 함수(A-607 실제 이름, `domain/expenses/evidence.ts`)가 같은 트랜잭션에서 이 함수를 부르게 한 줄씩 더한다. 06-06의 `confirmEvidence`는 자기 리포지토리 갱신으로 금액을 쓰므로 이 경로에 걸리지 않는다(B-3) — 이 태스크 verify가 06-06의 통합 테스트를 다시 돌려 그것을 확인한다.

**B-1(디자인 반영 r2) — 훅은 늘 version을 올린다.** `invalidateEvidenceReview`는 확인 기록이 있든 없든 같은 트랜잭션에서 `lockExpenseRow`가 돌려준 version으로 06-03의 `bumpExpenseVersion`(실제 이름 · 시그니처는 06-03 SUMMARY — 다른 이름이면 그 함수를 쓰고 SUMMARY에 적는다)을 부른다. 확인 기록이 없는 `확인 전` 문서에서 PM이 파일을 더해도 version이 오르므로, 모달 · 문서 화면이 받은 옛 version으로 부른 06-06 `confirmEvidence`는 기존 version 비교에서 거부된다 — 06-06에 새 판정을 더하지 않는다. ⓪에서 06-06 SUMMARY의 N-6 목록을 읽어 Phase 5 PM 편집 함수가 같은 트랜잭션에서 스스로 version을 비교 · 증가하는지 확인한다: 그렇다면 훅 호출을 그 증가 **뒤**에 둔다(앞에 두면 Phase 5의 기대 version 비교가 훅이 올린 값과 어긋나 PM 저장이 동시성 오류로 떨어진다 — 이 경우 version은 두 번 오르고, 비교는 같음만 보므로 문제없다). 어느 경우인지와 호출 줄 번호를 SUMMARY에 적는다. 06-15 N-4(「훅은 version을 올리지 않는다」)는 이 줄로 대체된다 — 06-04 「잠금 뒤 게이트 재판정」은 두 번째 방어로 그대로 둔다.

`test/integration/evidence-invalidation.test.ts`(신규)에 behavior 첫째~둘째 줄과 `[B-1 r2]` 두 줄(`it` 이름 「PM 증빙 변경은 문서 version을 올린다」 · 「PM 증빙 변경 뒤 옛 version 확인 거부」 — **RED 먼저:** 훅에 version 증가를 넣기 전에 이 둘이 빨간 것을 보고 SUMMARY에 한 줄), E2E `evidence-lifecycle.spec.ts`(신규)에 마지막 줄.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence-invalidation.test.ts test/integration/evidence-reviews.test.ts test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — evidence-reviews의 「고쳐 확인 → 확인됨 유지」가 빨가면 훅이 경영관리 확인 경로에 걸렸다(B-3). 「PM 증빙 변경은 문서 version을 올린다」 · 「PM 증빙 변경 뒤 옛 version 확인 거부」가 빨가면 훅이 확인 기록 없는 문서에서 version을 올리지 않아 보지 않은 증빙이 `확인됨`이 될 수 있다(B-1 r2). 「PM 증빙 변경은 문서 version을 올린다」의 PM 저장 단언이 동시성 오류로 빨가면 훅 호출이 Phase 5의 자체 version 증가보다 앞에 있다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-lifecycle.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-608-T 테스트 모드 경로 또는 사용자 선택 · 승인 문장)가 SUMMARY에 있다
    - 통합 테스트에 PM 금액 고침 ∥ 지급 완료 `Promise.all` 케이스가 있다
    - 06-06의 「고쳐 확인 → 확인됨 유지」 `it`이 이 플랜 verify에서 녹색이다
    - [B-1 r2] `grep -c "PM 증빙 변경은 문서 version을 올린다\|PM 증빙 변경 뒤 옛 version 확인 거부" test/integration/evidence-invalidation.test.ts`가 2 이상이고 녹색이다. `grep -v '^\s*//' domain/evidence-reviews/index.ts | grep -c "bumpExpenseVersion"`이 1 이상이다(06-03 실제 이름이 다르면 그 이름으로 세고 SUMMARY에 적는다). RED 한 줄 · Phase 5 자체 version 증가 유무 · 훅 호출 줄 번호가 SUMMARY에 있다
  </acceptance_criteria>
  <done>PM의 증빙 변경이 잠금 뒤 확인을 풀고 문서 version을 올려 옛 version의 확인이 거부되며, 경영관리의 금액 고침 확인은 풀리지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 비용 기준(EVID-03 · 04) · 업로드 검사(주인 일반 · R-5) · 의도만 있는 업로드의 불변식</name>
  <files>domain/evidence-reviews/cost-basis.ts, domain/evidence-reviews/upload-checks.ts, domain/expenses/evidence.ts, domain/settings/keys.ts, test/unit/domain/evidence-cost-basis.test.ts, test/unit/domain/evidence-upload-checks.test.ts, test/integration/evidence-upload-intent.test.ts</files>
  <read_first>
    - ROADMAP Phase 6 기준 2(업로드 순서 · F8)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S7 「중복(SHA-256) — 같은 주인 종류 안에서만」 · 열린 선택 O-6(rev 9) · Copywriting 「Error — 증빙 업로드(S7 · S21 · S22, §7-10 행 오류 모양)」
    - domain/permissions/scope-for.ts(읽기 범위 판정)
    - A-608 재확인에서 찾은 Phase 5 업로드 의도 · 완료 통보 함수
  </read_first>
  <behavior>
    - `expenseCostBasis`: 증빙 금액 있음 → { kind: 확정, 증빙 금액 } / 증빙 0 · 승인액 있음 → { kind: 예상, 승인액 } / 둘 다 없음 → { kind: 예상, 견적 실행가 } / 확인 기록 유무로 결과가 바뀌지 않음
    - `checkEvidenceUpload({ ownerKind, ownerId, sizeBytes, mimeType, sha256 })`: 크기 > 한도 → 크기 막힘 / MIME 이미지·`application/pdf` 밖 → 형식 막힘 / 같은 SHA-256 · 주인 `expense` · 읽을 수 있음 → 중복 막힘 + 문서 번호 / 읽을 수 없음 또는 주인이 지출결의가 아님 → 중복 막힘, 번호 없음 / [DR-3] 같은 SHA-256 · 같은 주인 종류 → 중복 막힘 · 중복 조회는 요청의 `ownerKind` 범위로만 불린다(다른 종류 주인의 같은 해시는 막지 않는다) / 통과 → ok
    - `lastEvidenceDeleteNeedsConfirm({ ownerKind, remainingCount })`: `expense` · 남는 0 → true / `expense` · 남는 ≥1 → false / 그 밖 주인 → 항상 false
    - 업로드 의도 발급 뒤 완료 통보 없음 → 파일 행 0 증가, 증빙 수·비용 기준·지급 게이트 판정 불변
  </behavior>
  <action>
`domain/evidence-reviews/cost-basis.ts`에 `expenseCostBasis`(PNL-02 우선순위의 문서 한 건 판, D-602 — 확인 여부 무관). `domain/evidence-reviews/upload-checks.ts`에 `checkEvidenceUpload`(한도는 `evidence.max_size_mb` — 이 플랜이 처음 읽으므로 `domain/settings/keys.ts`의 그 키에서 readBy 표시를 지운다) · `lastEvidenceDeleteNeedsConfirm`. 중복 문서 번호는 주인이 `expense`이고 `scopeFor`로 올린 사람의 읽기 범위를 확인했을 때만 응답에 싣는다(O-6). **중복 조회는 요청의 `ownerKind`와 같은 주인 종류의 파일만 본다(DR-3 · S7 · O-6 rev 9)** — 파일 표 주인 모양이 ⒜ `owner_kind` 칸이면 그 칸 = 요청 `ownerKind`, ⒝ 주인별 FK 칸이면 그 종류의 FK 칸 `IS NOT NULL`을 조건 하나로 건다(06-16 Task 1 ①과 같은 두 갈래 — 고른 갈래를 SUMMARY에 적는다). 이 플랜 때 주인은 지출결의뿐이라 06-16이 두 주인 종류를 더해도 이 함수는 고치지 않는다. 카드 사용의 카드 전표를 지출결의와 한 종류로 세는 규칙(S7)은 카드 전표를 파일 표 주인으로 넓히는 06-25(「카드 사용 증빙 첨부」, 디자인 반영 r2 오케스트레이터 결정)가 이 조건에 더한다 — 이 플랜은 그 자리(주인 종류 범위 조건 하나)만 남긴다. SHA-256은 브라우저가 보낸 참고 값이다(R-5) — 함수 머리 한 줄에 그 뜻을 적고, 어떤 권한·금액 판정에도 쓰지 않는다. Phase 5 업로드 의도 domain 함수(A-608 실제 이름, `domain/expenses/evidence.ts`)의 서명 URL 발급 **앞**에서 이 검사를 부르고, 걸리면 발급하지 않는다. 완료 통보 때 GCS 메타데이터를 다시 확인하는 Phase 5 동작은 그대로 둔다.

`test/integration/evidence-upload-intent.test.ts`(신규): behavior 넷째 줄 + 크기·형식·중복(읽기 가능/불가 두 계정) 거부. 업로드는 Task 1 ⓪에서 확인한 테스트 모드 경로로 탄다. 단위 테스트는 behavior 첫째~셋째 줄.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence-cost-basis.test.ts test/unit/domain/evidence-upload-checks.test.ts test/unit/settings/registry-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — registry-coverage가 빨가면 `evidence.max_size_mb`의 readBy를 지우지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/evidence-upload-intent.test.ts test/integration/evidence-invalidation.test.ts test/integration/evidence-reviews.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 중복 검사 통합 테스트가 읽기 권한 없는 계정의 응답 본문에 기존 문서 번호 문자열이 없음을 단언한다
    - 단위 테스트가 `lastEvidenceDeleteNeedsConfirm`의 비지출결의 주인 갈래와 `expenseCostBasis`의 확인 기록 무관 갈래를 단언한다
    - [DR-3] 단위 테스트가 「같은 SHA-256 · 같은 주인 종류 → 중복 막힘」과 「중복 조회가 요청의 `ownerKind`를 범위로 받는다(조회 가짜의 인자 단언)」를 개별 케이스로 단언한다 — 다른 종류 주인이 실제로 막히지 않는 것은 06-16 Task 2 통합이 증명한다
  </acceptance_criteria>
  <done>서명 URL 발급 전에 크기·형식·중복이 걸러지고, 중복 번호가 읽기 범위 밖에 새지 않으며, 의도만 있는 업로드가 아무 값도 바꾸지 않고, 비용 기준이 증빙 유무로만 갈린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 첨부 영역 오류 넷 · 마지막 증빙 삭제 확인(SP-6) · 업로드 E2E · 고아 청소 절차</name>
  <files>ui/attachments/Attachments.tsx, docs/OPERATIONS.md, test/e2e/evidence-lifecycle.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S7. 증빙 첨부 규칙」 절 · 「SP-6. §7-10에 「마지막 증빙 삭제」 확인」 절 · UI Considerations `S7` 행 · Copywriting 「Error — 증빙 업로드」 · 「Destructive — 증빙 삭제」(줄 번호는 rev 9에서 밀려 절 이름으로 찾는다)
    - docs/OPERATIONS.md 목차(300줄 상한 — test/unit/docs-limits.test.ts)
    - A-608 재확인에서 찾은 Phase 5 첨부 컴포넌트
  </read_first>
  <behavior>
    - 크기·형식·중복·완료 통보 실패가 파일 행 자리에 각각의 문구로 선다 · 여러 파일 중 일부 실패는 성공·실패 행 각각
    - 지출결의의 마지막 증빙 삭제 → 확인 모달 / 마지막이 아님 → 모달 없이 삭제
    - E2E: 이미지 한 장 업로드 → 파일 행 · 마지막 증빙 삭제 → 모달 결과 줄 → 삭제 뒤 확인 줄 `확인 전` 또는 `없음`
  </behavior>
  <action>
첨부 컴포넌트: Phase 5가 브라우저 축소(최대 변 2000px JPEG, PDF 원본)·SHA-256 계산을 이미 하면 그대로 쓰고, 없으면 Canvas API와 `crypto.subtle.digest("SHA-256", …)`로 더한다(새 의존성 없음). 오류 넷은 Copywriting 「Error — 증빙 업로드」 그대로 파일 행 자리에(§7-7 PARTIAL). 첨부 영역 한 줄 `이미지·PDF {N}MB`의 N은 서버가 보낸 설정 값. 마지막 증빙 삭제는 `lastEvidenceDeleteNeedsConfirm`이 true일 때만 `ConfirmDialog`(결과 줄 = 증빙 금액 지워짐 · 증빙 확인 풀림)를 연다 — 삭제 자체는 Phase 5 삭제 함수(Task 1 훅이 확인을 푼다).

`docs/OPERATIONS.md`에 「증빙 업로드 고아 객체 청소(F8)」 절을 15줄 안으로 더한다: 무엇이 고아인가(서명 URL 발급 기록은 있고 파일 행이 없는 GCS 객체) · 찾는 방법(GCS 목록과 파일 표의 객체 키 대조, `gcloud storage ls`, 버킷 이름은 시크릿 목록 절을 가리킨다) · 지우는 기준(발급 뒤 24시간) · 누가 언제(월 1회, 운영자). 300줄을 넘지 않게 한다.

E2E `evidence-lifecycle.spec.ts`에 behavior 셋째 줄을 Task 1 ⓪에서 확인한 테스트 모드 경로로 더한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/docs-limits.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — OPERATIONS.md가 300줄을 넘었다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/evidence-lifecycle.spec.ts test/e2e/evidence-waive-prepaid.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; git diff --stat "$(git merge-base origin/main HEAD)" -- package.json pnpm-lock.yaml</automated>
    <fails_when>앞의 명령 중 하나라도 실패하거나 마지막 diff 출력이 비어 있지 않다(이 브랜치에서 새 의존성이 들어왔다 — 커밋 뒤에도 잡힌다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "고아" docs/OPERATIONS.md`가 1 이상이고 `wc -l docs/OPERATIONS.md`가 300 이하
    - E2E 업로드 · 마지막 삭제 모달 케이스가 CI=true로 녹색이다
    - merge-base 기준 package.json · pnpm-lock.yaml diff 0
  </acceptance_criteria>
  <done>첨부 영역이 오류 넷과 마지막 삭제 확인을 계약대로 보이고, 고아 청소 절차가 운영 문서에 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 업로드 의도 | 크기·MIME·SHA-256은 브라우저가 보낸 값이다 — 발급 전 검사의 입력일 뿐이다(R-5) |
| 브라우저 → GCS | 서명 PUT URL의 크기 상한 조건이 한도를 강제한다(Phase 5) |
| Phase 5 증빙 변경 → 확인 기록 | PM 편집이 경영관리의 확인을 푼다 — 행 잠금 뒤 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-50 | Information Disclosure | 중복 검사 응답 | high | mitigate | 기존 문서 번호를 주인 `expense` · `scopeFor` 읽기 범위 확인 뒤에만 싣는다(O-6). 범위 밖 계정 응답에 번호 문자열 없음 통합 테스트 |
| T-06-51 | Tampering | 업로드 크기·형식 | medium | mitigate | 브라우저 값으로 발급을 거르고, 실제 크기는 서명 URL 조건 + 완료 통보 때 GCS 메타데이터 재확인(Phase 5)으로 강제 |
| T-06-52 | Tampering | 브라우저 SHA-256 위조 | low | accept | 참고 값(R-5) — 위조해도 중복 파일이 한 번 더 올라갈 뿐이고, 어떤 권한·금액·게이트 판정도 이 값에 기대지 않는다 |
| T-06-53 | Tampering | PM 증빙 변경 ∥ 지급 완료 | medium | mitigate | 훅이 `lockExpenseRow`(R-1) 뒤에 확인을 푼다 · 병렬 통합 테스트 |
| T-06-231 | Repudiation | 보지 않은 증빙의 확인(모달 · 문서 화면을 연 뒤 PM이 증빙을 바꿈 — B-1 r2) | high | mitigate | 훅이 확인 기록 유무와 관계없이 `bumpExpenseVersion` → 06-06 `confirmEvidence`의 기존 version 비교가 옛 version을 거부. 통합 「PM 증빙 변경 뒤 옛 version 확인 거부」(RED 먼저) · 06-17 E2E 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」 |
| T-06-54 | Denial of Service | 고아 객체 누적 | low | mitigate | `docs/OPERATIONS.md` 월 1회 청소 절차(F8) |
| T-06-55 | Tampering | 새 기반 시설 무단 추가 | low | mitigate | 테스트 모드가 없으면 Task 1 ⓪에서 멈춰 사용자 선택(B-4), merge-base 기준 의존성 diff 0 검사 |
</threat_model>

<verification>
- 단위 evidence-cost-basis · evidence-upload-checks · docs-limits · registry-coverage 녹색
- 통합 evidence-invalidation · evidence-upload-intent · evidence-reviews(06-06 재실행 — B-3) · evidence · leak-scan 녹색
- E2E evidence-lifecycle · evidence-waive-prepaid가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, merge-base 기준 의존성 diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S4(partial) · S7 DOM 실측 감사(backstop 2건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- PM의 증빙 변경이 확인을 풀고, 경영관리의 금액 고침 확인은 유지된다(B-3)
- PM의 증빙 변경이 확인 기록 유무와 관계없이 문서 version을 올려, 경영관리가 보지 않은 증빙 묶음이 옛 version으로 `확인됨`이 되지 않는다(B-1 r2)
- 비용 기준이 증빙 유무로만 갈린다(확인 여부 무관)
- 업로드 검사가 서명 URL 발급 전에 서고, 의도만 있는 업로드가 아무것도 바꾸지 않는다
- 업로드 E2E가 승인된 경로로만 돈다(B-4)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `invalidateEvidenceReview` · `expenseCostBasis` · `checkEvidenceUpload` · `lastEvidenceDeleteNeedsConfirm` |
| 변경된 기존 심볼 | Phase 5 증빙 변경 함수(확인 풀림 + 문서 version 증가 훅 — B-1 r2) · Phase 5 업로드 의도 함수(발급 전 검사) · Phase 5 첨부 컴포넌트(오류 넷 · 축소 · 해시 · 마지막 삭제 모달) · `EVIDENCE_MAX_SIZE_MB` readBy 제거 |
| 문서 절 | `docs/OPERATIONS.md` 「증빙 업로드 고아 객체 청소(F8)」 |
| 테스트 | `test/unit/domain/{evidence-cost-basis,evidence-upload-checks}.test.ts` · `test/integration/{evidence-invalidation,evidence-upload-intent}.test.ts` · `test/e2e/evidence-lifecycle.spec.ts` |

## Review Dispositions Ledger

### Round 1 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 3)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-3 — 중복(SHA-256) 막힘이 주인 종류를 넘어 걸린다(N-7, design-review.md L36@f2720a8 · 반영 지시 3 L76@f2720a8). 사용자 카드 DR-3 = 허용(추천안 적용, 답 대기) | MEDIUM(고침) | Plan 11, Task 2: behavior `[DR-3]` 갈래 · action 「중복 조회는 요청의 `ownerKind`와 같은 주인 종류의 파일만」(파일 표 모양 ⒜/⒝별 조건 하나) · read_first UI-SPEC S7 · O-6 · acceptance `[DR-3]` 단위 케이스 둘. must_haves.truths 업로드 검사 줄 · artifacts `upload-checks.ts` provides · 열린 선택 O-6 행. 다른 종류 주인이 실제로 막히지 않는 증명은 06-16 Task 2 통합 |
| (Shard A 전달) 카드 사용의 카드 전표는 지출결의와 한 종류로 센다(UI-SPEC S7 · O-6 rev 9) | MEDIUM | Plan 11, Task 2 action 마지막 문장 — 규칙을 적고, 범위 조건에 카드 전표 주인을 더하는 일은 카드 전표를 파일 표에 저장하는 플랜의 몫으로 넘겼다(아래 보류 줄) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 카드 전표 첨부 저장의 소유 플랜 없음 — UI-SPEC S7 「카드 사용 폼·구매 완료 폼의 첨부」 · S8 목록 `증빙` 열 · S9 `증빙` 첨부 영역 · S13 「증빙 첨부 선택」, REQUIREMENTS EVID-01 「법인카드 건에 첨부」 | 오케스트레이터 결정 필요(검토 밖에서 드러난 범위 공백). EVID-01은 Phase 5 매핑인데 `corp_card_usages`는 06-05가 처음 만들어 Phase 5가 카드 사용을 파일 표 주인으로 둘 수 없고, 06-05 · 06-07 · 06-09 · 06-12 · 06-14 · 06-16 어느 플랜도 카드 사용을 주인으로 넓히지 않는다. 06-16에 더하면 재분할 방아쇠(15파일)를 넘고, 카드 플랜(06-05, 3파)에 더하면 06-16의 주인 확장(8파)보다 앞서 의존이 뒤집힌다. 권고: 06-16 다음 파에 작은 플랜 하나(파일 표 주인 `corp_card_usage` · 카드 폼 · 구매 완료 폼 첨부 영역 · 목록 `증빙` 열 · 이 플랜의 중복 조건에 지출결의와 한 묶음). 결정 전까지 이 플랜의 중복 조건은 같은 종류만 본다 |

### Round 2 — 디자인 검토 반영 r2(커밋 전 · `/mnt/project-files/06-gates/design-apply-cross-r1.md` · `design-apply-checker-r1.md` · `design-apply-r2-brief.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 B-1 — 모달 · 문서 화면을 연 뒤 PM이 증빙을 바꾸면 보지 않은 증빙이 `확인됨`이 된다: 이 플랜의 훅이 version을 올리지 않았다(design-apply-cross-r1.md B-1, 권고 ①) | 막음 | Plan 11, must_haves.truths 「B-1(디자인 반영 r2)」 줄 · artifacts `index.ts` provides · key_links `bumpExpenseVersion` · objective. Task 1 read_first(UI-SPEC 「낙관적 잠금」 · 06-03 `bumpExpenseVersion` · 06-06 N-6 목록) · behavior `[B-1 r2]` 두 줄 · action 「B-1 — 훅은 늘 version을 올린다」(확인 기록 유무 무관 · Phase 5 자체 증가가 있으면 그 뒤에 호출 · RED 먼저) · fails_when · acceptance · done. T-06-231 · success_criteria. 06-15 N-4를 대체한다(06-15 Round 4) — 06-04 「잠금 뒤 게이트 재판정」은 두 번째 방어로 둔다 |
| Supersedes Round 1 Deferred: 카드 전표 첨부 저장의 소유 플랜 없음 | 참고(결정됨) | 오케스트레이터 결정 — 새 플랜 06-25 「카드 사용 증빙 첨부」(R2 작성). Task 2 action 마지막 문장을 「→ 06-25가 이 범위 조건에 카드 전표를 지출결의와 한 묶음으로 더한다」로 바꿨다. 이 플랜의 함수 모양은 그대로 |
| 체커 W4 — read_first의 UI-SPEC 줄 번호가 rev 9에서 밀렸다(06-11:158 · :231) | 경고 | Task 1 · Task 3 read_first를 절 이름(UI Considerations `S4` partial · 「S7. 증빙 첨부 규칙」 · 「SP-6」)으로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 B-1 대안 ②(보기 액션의 증빙 지문 + `confirmEvidenceAction`의 `evidenceStamp` 비교) | 채택하지 않음 — 권고 ①로 닫힌다. ②는 06-06 판정을 넓힌다(오케스트레이터 지시 ①) |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md` when done
</output>
