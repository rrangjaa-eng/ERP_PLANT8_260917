---
phase: 06-payment-evidence-cards
plan: 12
type: execute
wave: 7
depends_on: ["06-08", "06-09", "06-10"]
files_modified:
  - db/schema/corp-card-usages.ts
  - db/migrations/meta/_journal.json
  - domain/purchase-requests/index.ts
  - domain/purchase-requests/policy.ts
  - domain/corp-card-usages/index.ts
  - domain/corp-card-usages/rights.ts
  - app/(app)/cards/page.tsx
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/purchases/page.tsx
  - app/(app)/cards/purchases/actions.ts
  - app/(app)/cards/purchases/actions.registry.ts
  - test/unit/domain/purchase-policy.test.ts
  - test/integration/purchase-requests.test.ts
  - test/e2e/purchase-requests.spec.ts
autonomous: true
requirements: [EXP-10]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "구매 권한(`cards.purchases` write)이 있는 사람이 `신청` 행의 3차 「구매 완료」로 S13(카드 사용 폼 구매 완료 모드)을 열고 1차 「구매 완료」를 누르면 한 트랜잭션에서 카드 사용 건(`registered_via = purchase`, `purchase_request_id` = 요청, 등록 칸 `구매 요청 {번호}`)이 생기고 요청이 `구매 완료`가 된다 — 행동 로그 `purchase_process`. 카드 사용 INSERT가 실패하면 요청은 `신청`으로 남는다"
    - "[H-4] 구매 완료 토스트 `구매 완료 · 26001-C0001`에는 3차 `되돌리기`가 없다 — 구매 완료로 생긴 건은 삭제가 없다(O-11). 잘못 고른 카드는 Task 3의 카드 고치기로만 고친다(UI-SPEC Copywriting 「SUCCESS — 토스트」 3차 `되돌리기` 문단의 되돌리기 없음 목록)"
    - "**R-3:** 카드 사용 생성에는 `registered_via = purchase` 전용 갈래가 있다 — 카드 자격은 `can(viewer, \"cards.purchases\", \"write\")`(활성 카드 전부)이고 이 판정은 `precheckCardUsage`의 purchase 갈래가 **트랜잭션 전에** 한다. 본인 카드·대리 등록 갈래를 타지 않는다. 견적 줄에 잇는 경우 `createCardUsage`의 `runCreate`가 06-07과 같은 `lockQuoteLines` → `findLineLinks` → `card.dual-link-block` 순서를 다시 지난다"
    - "「06-03 tx 규약」을 따른다 — 구매 완료는 트랜잭션 전에 권한(`can`) · 요청 사전 조회 · `precheckCardUsage`(활성 카드 · `loadTaxRates(usedOn)`)를 끝내고, 트랜잭션 몸통 `runComplete`에서는 `tx`를 받는 리포지토리와 `createCardUsage(viewer, input, pre, tx)`(외부 tx 경로)만 부른다. 행동 로그는 커밋 뒤(CROSS E-3 · ENG E-2)"
    - "같은 요청에 **구매 완료 동시 6건**(`Promise.all`, `DB_POOL_MAX` 기본 5 초과)을 넣어도 타임아웃 없이 모두 끝나고 하나만 성공 · 다섯은 `이미 구매 완료 · 새로 고침`, 카드 사용 1건이다"
    - "**전역 잠금 순서(N-3):** 프로젝트 행 → 견적 줄(id 오름차순, `lockQuoteLines`) → 지출결의 · 카드 사용 · 구매 요청 문서 행. 구매 완료는 요청의 견적 줄을 먼저 잡고 그다음 요청 행을 `FOR UPDATE`로 잡는다 — 요청 행을 먼저 잡고 줄을 뒤에 잡지 않는다"
    - "[O-10 기본값] 예상 금액은 결제 합계(부가세 포함) 기준이고, 구매 완료 때 결제 합계가 다르면 `예상 금액 {원화} · 차이 {±원화}`(`--muted`)만 보이고 막지 않는다 — 비교는 두 금액의 원화 환산액으로 서버가 `diffKrw`로 한다"
    - "[O-20 기본값] 구매 요청자와 구매 완료 처리자가 같아도 막지 않는다 — 판정은 `domain/purchase-requests/policy.ts`의 상수 `PURCHASE_REQUESTER_MAY_COMPLETE = true` 한 곳에서만 나온다"
    - "구매 완료로 생긴 카드 사용 건은 연결을 바꿀 수 없고 삭제가 없다(O-11) — 그 건을 여는 사람이 지금 구매 권한을 가졌으면 카드가 `Select`(활성 카드 전부 + 저장된 카드가 현재 값)이고, 다른 카드를 고르면 카드 아래 `Form.Hint` `구매 완료 때 카드 {이전 카드}`(`--muted` — 바뀌기 전 값만, 설명형 뒷말 없음, rev 9 M-5)가 서고 저장된 카드로 되돌리면 사라진다. 다른 카드로 저장하면 행동 로그에 `카드 고침 · {이전 카드} → {새 카드}`가 남는다"
    - "**B-2:** 카드 사용 표에 `purchase_request_id`(FK) · 유니크 · CHECK(`registered_via = purchase`이면 값 있음)를 더하는 마이그레이션은 SET LOCAL 한 쌍을 머리에 두고, FK · CHECK를 `NOT VALID`로 걸며, VALIDATE는 `pnpm db:generate --custom`으로 만든 별도 마이그레이션에서 한다. 유니크는 `uniqueIndex()`다"
    - "S11: 구매 완료 행 2행 `카드 사용 {MM-DD} · {결제 합계}` · `/cards` 목록 등록 칸 `구매 요청 {번호}`"
    - "S13 empty: 기본값으로 채워 연다 — 가맹점 = 온라인구매 협력사 · 결제 합계 = 예상 금액(통화·외화·환율 그대로) · 사용일 오늘 · 연결 = 요청의 것(텍스트) — 팀 비용 요청의 팀 읽기는 06-14"
    - "S13 loading: 서버 계산 한 줄 이전 값 `--faint` → 새 값, 1차 `구매 완료` 처리 중 `…`"
    - "S13 error: S9 칸 오류와 같은 문구 + 그사이 상태가 바뀌면 1차 비활성 + `구매 요청 취소됨 · 새로 고침` / `이미 구매 완료 · 새로 고침`"
    - "S13 populated: 제목 `구매 완료 — {번호} · {품목}` · 카드 옵션 = 활성 카드 전부(구매 권한자) · 등록 칸 = `구매 요청 {번호}`"
    - "S13 partial: 결제 합계 ≠ 예상 금액이면 `예상 금액 {원화} · 차이 {±원화}`(`--muted`, 막지 않음)"
    - "S13 zero-one-many: 연결은 요청의 것 하나라 텍스트(「바꾸기」 없음), 카드는 여러 장 = `Select` · 한 장 = 텍스트"
    - statement: "S13 overflow: 활성 카드 30장 이상의 카드 `Select`가 말줄임으로 선다(S9와 같은 컴포넌트) — CI=true DOM 감사"
      verification: backstop
    - statement: "S13 long-text: 긴 품목 이름이 제목 줄에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "구매 권한 없이 구매 완료를 처리하거나, 구매 완료 건의 연결을 바꾸거나 지울 수 있어서는 안 된다"
      status: resolved
      verification: test
    - statement: "한 구매 요청에서 카드 사용 건이 두 번 생겨서는 안 된다 — 구매 완료 동시 6건 중 하나만 성공한다(요청 행 잠금 + `uniqueIndex(purchase_request_id)`)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/purchase-requests/policy.ts"
      provides: "[O-20] 요청자=처리자 허용 상수 — 06-14가 [O-19] 팀 비용 귀속 함수를 같은 파일에 더한다"
      exports: ["PURCHASE_REQUESTER_MAY_COMPLETE"]
    - path: "domain/purchase-requests/index.ts"
      provides: "구매 완료"
      exports: ["completePurchaseRequest"]
    - path: "db/schema/corp-card-usages.ts"
      provides: "`purchase_request_id` 칸(FK · uniqueIndex · CHECK)"
  key_links:
    - from: "domain/purchase-requests/index.ts"
      to: "domain/corp-card-usages/index.ts"
      via: "구매 완료가 카드 사용 건 생성(purchase 갈래)을 같은 트랜잭션에서 부른다"
      pattern: "completePurchaseRequest"
    - from: "domain/purchase-requests/index.ts"
      to: "domain/purchase-requests/policy.ts"
      via: "요청자=처리자 판정을 정책 파일에서만 읽는다"
      pattern: "PURCHASE_REQUESTER_MAY_COMPLETE"
    - from: "domain/corp-card-usages/rights.ts"
      to: "domain/permissions/can.ts"
      via: "구매 완료 건 카드 고치기는 지금 구매 권한이 있을 때만"
      pattern: "cards.purchases"
---

<objective>
구매 요청을 **구매 완료**로 닫는다 — 구매 권한자가 S13(카드 폼 구매 완료 모드)에서 한 트랜잭션으로 카드 사용 건을 만들고 요청을 `구매 완료`로 바꾼다. 카드 사용 쪽에 구매 전용 갈래(R-3)를 두고, 구매 완료 건의 카드 고치기와 [O-20] 요청자=처리자 허용을 한 파일에 둔다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 요청 → 카드 사용이 한 트랜잭션이어야 「견적 줄에 한 번만」이 구매 경로에서도 성립한다. 교차 검토 R-3은 구매 완료가 본인 카드 자격 판정에 섞여 들어가지 않게 명시 갈래를 요구했다.

Output: 카드 사용 표 칸 추가(마이그레이션 2 — 생성 + `--custom` VALIDATE), `completePurchaseRequest`, `policy.ts`(O-20), 카드 폼 `mode = purchase`, 구매 완료 건 카드 고치기, 단위 1 · 통합 · E2E 케이스.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-08-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-09-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-12 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-08 | `purchase_requests` · `createPurchaseRequest` · `/cards/purchases` | `git ls-files db/schema/purchase-requests.ts` 비면 멈춤 |
| 06-09 | `cardUsageRights` · `updateCardUsage` · 카드 폼 수정 모드 | `git ls-files domain/corp-card-usages/rights.ts` 비면 멈춤 |
| 06-10 | 마이그레이션 저널에 06-10의 `evidence_prepaid_validate`가 먼저 있다 | `grep -c "evidence_prepaid_validate" db/migrations/meta/_journal.json` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-20** (지금 사용자에게 묻는 중) | 요청자 = 구매 완료 처리자 허용 | **Task 3 하나**: `domain/purchase-requests/policy.ts`의 `PURCHASE_REQUESTER_MAY_COMPLETE` + 그 단위 테스트 |
| O-10 | 예상 금액 = 결제 합계 기준, 차이는 표시만 | S13 차이 줄 · `completePurchaseRequest`(막지 않음) |
| O-11 | 구매 완료 건은 연결 변경·삭제 없음(06-09 `cardUsageRights`) — 카드 고치기는 이 플랜이 더한다 | `rights.ts` 구매 완료 갈래 |

<assumption_delta_decision>
`detected: false`. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-10 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 3 · O-10 · O-11 · O-20 · D-607(구매 완료 금액 = 결제 합계). 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 구매 권한자가 `신청` 요청 한 건을 구매 완료하면 카드 사용 건이 한 트랜잭션으로 생기고 두 목록에 보인다</name>
  <files>db/schema/corp-card-usages.ts, db/migrations/meta/_journal.json, domain/purchase-requests/index.ts, domain/corp-card-usages/index.ts, app/(app)/cards/page.tsx, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/purchases/page.tsx, app/(app)/cards/purchases/actions.ts, app/(app)/cards/purchases/actions.registry.ts, test/e2e/purchase-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S13 · UI Considerations S13 행(rev 9 — rev 8 줄 번호는 낡았다) · Copywriting 「막힘 — 카드 사용 폼」 구매 완료 모드 갈래 · 「SUCCESS — 토스트」와 3차 `되돌리기` 문단(rev 9 H-4 — 구매 완료는 되돌리기 없음)
    - db/migrations/0003_permissions_masters_spine.sql 80-90행 · 0004_users_role_id_validate.sql(NOT VALID → 별도 VALIDATE 선례)
    - domain/corp-card-usages/index.ts(06-05 · 06-07 · 06-09 — `precheckCardUsage`의 카드 자격 판정 갈래 · `createCardUsage(viewer, input, pre, tx?)`의 외부 tx 경로 · `runCreate`)
    - domain/purchase-requests/index.ts(06-08 — 트랜잭션 전 조회 · `runCreate` 꼴)
    - 06-03-PLAN.md must_haves.truths의 「tx 규약」 줄 · domain/projects/ledger.ts 45-63행(`deferRecord` — 커밋 뒤 행동 로그) · /mnt/project-files/deadlock-fix/global-db-in-tx-audit.md(`repositories/permissions.ts` · `corp-cards.ts` · `settings.ts`는 tx 인자 없음)
  </read_first>
  <behavior>
    - `completePurchaseRequest`: 구매 권한 없음 → 거부 / `신청` 아님 → 거부 / 정상 → 카드 사용 1(purchase 갈래) + 요청 `구매 완료`(completed_by/at) + `purchase_process`
    - 카드 사용 INSERT를 실패시키면 요청 상태 불변
    - E2E: 구매 권한자 → `/cards/purchases` 목록 `신청` 행 3차 `구매 완료` → `/cards?new=1&purchase={id}` 폼 기본값 → 1차 `구매 완료` → 목록 행 `구매 완료` + 2행 `카드 사용 …` → `/cards` 목록 등록 칸 `구매 요청 {번호}` · 구매 완료 직후 토스트 `구매 완료 · {번호}`에 `되돌리기` 버튼이 없다(H-4)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① **스키마 · 마이그레이션(B-2 — 기존 표).** `corp_card_usages`에 `purchase_request_id` FK null + `uniqueIndex()`(한 요청에서 카드 사용 하나) + CHECK(`registered_via = purchase`이면 값 있음)를 더한다. 절차: `pnpm db:generate --name card_usage_purchase_link` → SQL 머리 SET LOCAL 한 쌍 → FK · CHECK 문장 끝에 `NOT VALID` + 이유 주석(0003 선례) → `pnpm db:generate --custom --name card_usage_purchase_link_validate`에 SET LOCAL 한 쌍 + 두 `VALIDATE CONSTRAINT`(0004 선례) → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 `pnpm db:generate` `No schema changes`.

② **domain(R-3 · 06-03 tx 규약 · CROSS E-3 · ENG E-2).** 카드 사용 쪽: `precheckCardUsage`에 `registered_via = purchase` 전용 갈래를 명시로 둔다 — 카드 자격 = `can(viewer, "cards.purchases", "write")` + 활성 카드(카드 조회 포함), 사용한 사람 = 요청자, 세율 `loadTaxRates(usedOn)`, 본인 카드·대리 등록 판정을 거치지 않는다. 이 판정은 전부 트랜잭션 전이다. `createCardUsage`의 `runCreate`는 purchase 갈래에서도 `pre`만 읽고, 견적 줄 연결이면 06-07 순서(`lockQuoteLines` → `findLineLinks` → `card.dual-link-block`)를 다시 지난다.
   `completePurchaseRequest(viewer, { requestId, version, card, usedOn, total })`를 세 부분으로 쓴다:
   - **트랜잭션 전:** 요청을 잠금 없이 읽어 견적 줄 id · 요청자 · 연결을 얻는다 → O-20 요청자=처리자 판정(Task 3 상수) → `const pre = await precheckCardUsage(viewer, { registeredVia: "purchase", usedByUserId: 요청자, card, usedOn, total, link: 요청의 연결 })`(권한 `can` · 활성 카드 · 세율이 여기서 끝난다 — 권한 없으면 트랜잭션을 열지 않고 거부).
   - **트랜잭션 몸통 `const runComplete = async (tx: DbOrTx) => { … };` → `withTransaction(runComplete)`:** 전역 잠금 순서(N-3 — 프로젝트 행 → 견적 줄 → 문서 행)대로 사전 조회한 견적 줄을 `lockQuoteLines(…, tx)`로 먼저 잡고(견적 줄이 없는 요청이면 건너뜀) → 요청 행을 `FOR UPDATE`로 잠그고 `신청` · version 확인(그사이 요청이 바뀌었으면 — 사전 조회 뒤의 변경 포함 — 이 version 비교가 거부한다, 따로 확인을 더하지 않는다) → `createCardUsage(viewer, { …, purchaseRequestId }, pre, tx)`(외부 tx 경로 — 그 안의 `lockQuoteLines`는 같은 트랜잭션이 이미 잡은 줄이라 기다리지 않는다) → 요청 `구매 완료`(tx).
   - **커밋 뒤:** `purchase_process`와 `createCardUsage`가 돌려준 행동 로그 항목을 남긴다(`deferRecord` 꼴 — 트랜잭션 안에서 남기지 않는다).

③ **액션 · 화면.** `completePurchaseRequestAction` 등록. `/cards?new=1&purchase={id}` = 카드 폼 `mode = purchase`(제목 `구매 완료 — {번호} · {품목}`, 가맹점 기본 = 온라인구매 협력사, 결제 합계 기본 = 예상 금액, 사용일 오늘, 연결 텍스트, 카드 옵션 = 활성 카드 전부, 등록 칸 `구매 요청 {번호}`). 구매 요청 목록 `신청` 행에 구매 권한자 3차 `구매 완료`(접근 이름 `{번호} 구매 완료`), 구매 완료 행 2행 `카드 사용 {MM-DD} · {결제 합계}`. `/cards` 목록 등록 칸 `구매 요청 {번호}`. 1차 `구매 완료` 성공 → 구매 요청 목록 + `Toast` `구매 완료 · {번호}` — `actionLabel`을 넘기지 않는다(rev 9 H-4: 구매 완료로 생긴 건은 삭제가 없어 되돌리기가 없다, O-11).

④ **E2E.** behavior 셋째 줄.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 `Found N issues`를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과가 SUMMARY에 있다
    - `grep -c "NOT VALID" db/migrations/*card_usage_purchase_link.sql`이 2 이상 · `grep -c "VALIDATE CONSTRAINT" db/migrations/*card_usage_purchase_link_validate*.sql`이 2 · 두 파일 모두 SET LOCAL 한 쌍
    - `createCardUsage`에 `registered_via`가 purchase인 갈래가 따로 있다(SUMMARY에 줄 번호)
    - `completePurchaseRequest`에서 `lockQuoteLines` 호출이 요청 행 `for("update")`보다 앞 줄에 있다(N-3 — SUMMARY에 줄 번호)
    - (CROSS E-3 · ENG E-2) `awk '/const runComplete = async/,/^  };$/' domain/purchase-requests/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\(|recordAction\('`가 0이고 같은 범위의 줄 수가 5 이상이다 · 같은 범위에 `createCardUsage(`가 1건 있다
    - (CROSS E-3) `awk '/const runCreate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\('`가 0이다(purchase 갈래를 더한 뒤에도)
    - E2E 한 경로 CI=true 녹색(토스트 `구매 완료 · {번호}` 안에 `되돌리기` 버튼 0개 단언 포함 — H-4)
  </acceptance_criteria>
  <done>구매 요청 한 건이 구매 완료 → 카드 사용 건까지 한 트랜잭션으로 실제로 이어진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 구매 완료 경합 · 원자성 · 상태 변경 오류 · 차이 표시(O-10)</name>
  <files>domain/purchase-requests/index.ts, app/(app)/cards/card-usage-form.tsx, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S13. 구매 완료 — 카드 사용 폼의 구매 완료 모드」 절 첫 줄(결제 합계 기본값 · `예상 금액 … · 차이 …`) · 끝 줄(그사이 취소·완료 → `구매 요청 취소됨 · 새로 고침` / `이미 구매 완료 · 새로 고침`) · UI Considerations S13 error · partial 행
    - /mnt/project-files/06-prep/open-choices.md 18-19행(O-10)
    - test/integration/purchase-requests.test.ts의 「동시 6건 번호 경합」(06-08 — 풀 밖 `pg.Client` 선점 · `pg_blocking_pids` 대기 확인 · 경주 타이머 · `Promise.allSettled` 꼴, PR #75 `projects-create-concurrency.test.ts`에서 온 것) · lib/env.ts 54행(`DB_POOL_MAX` 기본 5)
  </read_first>
  <behavior>
    - **「구매 완료 동시 6건」**: 같은 `신청` 요청(견적 줄 연결)에 구매 권한자의 `completePurchaseRequest` 6건을 `Promise.all`로 동시에 부른다(`DB_POOL_MAX` 기본 5 초과 — `pool.options.max`가 6 미만임을 먼저 단언). 풀 밖 `pg.Client`가 그 견적 줄 행을 `FOR UPDATE`로 먼저 쥐고, 풀 연결이 전부 그 잠금을 기다리는 것을 확인한 뒤 푼다(06-08 꼴). 판정: 10초 경주 안에 6건 모두 끝남(타임아웃 없음) + 성공 1 · `이미 구매 완료 · 새로 고침` 5 + 그 요청의 카드 사용 1건 + 요청 `구매 완료`
    - 카드 사용 INSERT 실패 → 요청 `신청` 유지(외부 tx 경로 — `createCardUsage`가 넘겨받은 tx에서 실패하면 요청 상태 변경도 함께 롤백)
    - 취소된 요청의 구매 완료 → `구매 요청 취소됨 · 새로 고침`
    - 결제 합계 ≠ 예상 금액 → 저장 성공 + 응답에 차이(원화, `diffKrw`)
  </behavior>
  <action>
behavior 네 줄을 `test/integration/purchase-requests.test.ts`에 개별 `it`으로 더하고(먼저 쓰고 RED를 확인한다), 드러나는 결함만 고친다. 「구매 완료 동시 6건」은 RED를 먼저 확인한다: Task 1의 트랜잭션 전 `precheckCardUsage`(권한 `can` · 활성 카드 · `loadTaxRates`)를 임시로 `runComplete` 안(요청 행 잠금 뒤)으로 옮기면 이 케이스가 10초 경주에서 빨개지는(줄 · 요청 행 잠금을 쥔 트랜잭션이 여섯째 커넥션을 기다리는 교착) 것을 한 번 보고 되돌린 뒤 녹색을 확인하고, 결과를 SUMMARY에 적는다. `it` 제한 시간 20초 · 경주 타이머 `clearTimeout` · `finally`에서 잠금 클라이언트를 닫은 뒤 `Promise.allSettled`. 폼: 결제 합계를 바꾸면 서버가 차이를 다시 보내고(`예상 금액 {원화} · 차이 {±원화}` `--muted`, 막지 않음 — O-10), 그사이 요청 상태가 바뀌었으면 1차 비활성 + 해당 문구. E2E에 「예상 금액과 다른 결제 합계 → 차이 줄 → 구매 완료 성공」 케이스.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/corp-card-usages.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. 「구매 완료 동시 6건」이 10초 경주 초과 · 제한 시간 초과(Test timed out)로 빨가면 트랜잭션 안에서 전역 `db`(권한 · 카드 · 세율 · 행동 로그)를 부른다 — 사전 조회를 트랜잭션 안으로 되돌린 임시 변경에서 이 빨간 결과가 나는 것을 먼저 확인한다(CROSS E-3). 성공이 2건 이상이거나 카드 사용이 2건 이상이면 요청 행 잠금 · version · `uniqueIndex`가 빠졌다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 「구매 완료 동시 6건」 케이스가 `Promise.all` 6건으로 실제 병렬이고(테스트 이름에 「동시 6건」) 성공 1 · 카드 사용 건 수 1을 단언한다
    - SUMMARY에 「구매 완료 동시 6건」의 RED(사전 조회를 트랜잭션 안으로 되돌린 임시 변경) → GREEN 확인 한 줄이 있다
  </acceptance_criteria>
  <done>구매 완료가 풀보다 많은 동시 요청 · 실패에도 교착 없이 한 요청에 카드 사용 하나만 만든다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [O-20 기본값] 요청자=처리자 허용 · 구매 완료 건의 카드 고치기</name>
  <files>domain/purchase-requests/policy.ts, domain/purchase-requests/index.ts, domain/corp-card-usages/rights.ts, domain/corp-card-usages/index.ts, app/(app)/cards/card-usage-form.tsx, test/unit/domain/purchase-policy.test.ts, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S9 「카드 — 구매 완료로 생긴 건의 카드 고치기(rev 7)」(rev 9 — rev 8 줄 번호는 낡았다) · Copywriting 「표시 — 카드 고침(구매 완료 건, S9 수정 모드)」(rev 9 M-5 — 설명형 뒷말 없음)
    - /mnt/project-files/06-prep/open-choices.md 29행(O-20 기본값과 대안)
  </read_first>
  <behavior>
    - `PURCHASE_REQUESTER_MAY_COMPLETE = true` → 요청자가 자기 요청을 구매 완료 가능 / 상수를 false로 둔 테스트 케이스(주입) → 거부(판정이 이 상수 한 곳에서만 나오는지 고정)
    - 구매 완료 건 카드 변경: 그 건 권리 + 지금 `cards.purchases` write + 새 카드 활성 → 저장 + `document_update` `카드 고침 · {이전} → {새}` / 하나라도 아니면 거부
    - 구매 완료 건의 연결 변경 · 삭제 → 거부(06-09 권리 판정 그대로)
    - (E2E, M-5) 구매 완료 건에서 다른 카드 고르기 → 카드 아래 힌트 `구매 완료 때 카드 {이전 카드}` 한 줄 / 저장된 카드로 되돌리기 → 힌트 없음
  </behavior>
  <action>
**이 태스크가 O-20의 유일한 자리다.** `domain/purchase-requests/policy.ts`(신규)에 상수 `PURCHASE_REQUESTER_MAY_COMPLETE = true`를 두고 선언 위 한 줄 주석에 "O-20 답이 바뀌면 이 상수와 그 단위 테스트만 고친다"를 적는다. `completePurchaseRequest`의 요청자 = 처리자 판정은 이 상수만 읽는다. 06-14가 같은 파일에 O-19 귀속 함수를 더한다.

구매 완료 건의 카드 고치기(S9 rev 7): `cardUsageRights`에 「구매 완료 건 + 지금 `cards.purchases` write → 카드 변경 가능」 갈래를 더하고, `updateCardUsage`가 그때만 카드 변경을 받는다(새 카드 활성 필수). 권한 판정(`can`)과 새 카드 활성 조회는 `updateCardUsage`가 트랜잭션을 열기 **전에** 한다(06-03 tx 규약 — `repositories/permissions.ts` · `corp-cards.ts`는 tx 인자 없음). 폼은 그 경우 카드 `Select`(활성 카드 전부 + 저장된 카드가 비활성이어도 현재 값으로 맨 앞), 다른 카드를 고르면 `Form.Hint`(Copywriting 「표시 — 카드 고침(구매 완료 건, S9 수정 모드)」 — 문구는 `구매 완료 때 카드 {이전 카드}`, `--muted`, 바뀌기 전 값만이고 설명형 뒷말은 두지 않는다 — rev 9 M-5). 저장된 카드로 되돌리면 힌트가 사라진다. 등록 칸·연결·팀은 바뀌지 않는다. 저장 토스트는 06-09의 `카드 사용 저장 · {결제 합계}`(되돌리기 없음) 그대로다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/purchase-policy.test.ts test/unit/domain/card-usage-rights.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/corp-card-usages-proxy.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rln "PURCHASE_REQUESTER_MAY_COMPLETE" domain/ app/` 결과가 `domain/purchase-requests/` 안의 파일뿐이다(O-20 판정이 정책 파일과 그 호출 모듈 밖에 없다)
    - 상수 주입 false 케이스가 있다
    - 통합 테스트가 「카드 고침 로그 한 줄」 · 「구매 권한 없는 사람의 카드 고침 거부」를 단언한다
    - [M-5] E2E 「다른 카드 → 힌트 `구매 완료 때 카드 {이전 카드}` 한 줄 · 되돌리면 힌트 없음」이 녹색이다
  </acceptance_criteria>
  <done>O-20의 답이 바뀌어도 정책 파일 하나만 고치면 되고, 구매 완료 건의 카드는 구매 권한자만 고친다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 구매 완료 액션 | 카드 · 결제 합계 · 사용일 · version이 넘어온다. 사용한 사람 · 연결은 넘어오지 않는다(요청에서 온다) |
| domain/purchase-requests → domain/corp-card-usages | 요청 상태 전환과 카드 사용 생성이 한 트랜잭션 |
| 마이그레이션 → 카드 사용 표 | 이 페이즈가 앞서 만든 표에 FK · CHECK를 더한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-56 | Elevation of Privilege | `completePurchaseRequest` · 구매 완료 건 카드 고치기 | high | mitigate | `can(viewer, "cards.purchases", "write")`를 트랜잭션 전 `precheckCardUsage` purchase 갈래 · 권리 함수에서 확인(R-3 — 본인 카드 갈래를 타지 않는다, 06-03 tx 규약). 권한 없는 계정 거부 통합 테스트 |
| T-06-57 | Tampering | 이중 구매 완료 | high | mitigate | 요청 행 `FOR UPDATE` + `신청` · version 확인 + `uniqueIndex(purchase_request_id)`. 「구매 완료 동시 6건」 `Promise.all` 병렬 테스트(성공 1 · 카드 사용 1) |
| T-06-155 | Denial of Service | 구매 완료 트랜잭션의 풀 고갈 교착 | high | mitigate | 권한 · 활성 카드 · 세율(`precheckCardUsage`)과 요청 사전 조회는 트랜잭션 전, `runComplete`는 tx 리포지토리와 `createCardUsage(…, pre, tx)`만, 행동 로그는 커밋 뒤(06-03 tx 규약 · CROSS E-3 · ENG E-2). 「구매 완료 동시 6건」(풀 밖 잠금으로 결정적 교착 조건, RED 먼저) + region grep 두 개 |
| T-06-58 | Tampering | 구매 완료 시점의 이중 연결 | high | mitigate | 견적 줄 연결이면 `lockQuoteLines` → `card.dual-link-block`을 다시 지난다 · 줄 잠금이 요청 행 잠금보다 앞(전역 순서 프로젝트 → 견적 줄 → 문서 행, N-3 — 교착 없음) |
| T-06-59 | Repudiation | 카드 고침 | low | mitigate | `document_update` `카드 고침 · {이전} → {새}` |
| T-06-60 | Denial of Service | 카드 사용 표 ALTER | low | mitigate | SET LOCAL 한 쌍 · FK/CHECK `NOT VALID` · `--custom` VALIDATE 별도 파일(B-2) · `pnpm lint:sql` 0건 |
</threat_model>

<verification>
- 단위 purchase-policy · card-usage-rights, 통합 purchase-requests(「구매 완료 동시 6건」 포함) · corp-card-usages · corp-card-usages-proxy · leak-scan 녹색
- `runComplete` · `runCreate` 범위에 전역 `db` 판정·조회 호출 0건(06-03 tx 규약 — Task 1 acceptance)
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E purchase-requests · card-proxy가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S11(구매 완료 행) · S13 DOM 실측 감사(backstop 2건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 구매 완료가 카드 사용 건을 한 트랜잭션으로 하나만 만든다
- 구매 완료 경로가 본인 카드 자격 판정과 섞이지 않는다(R-3)
- O-20 답이 바뀌어도 정책 파일 하나만 고친다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 컬럼 | `corp_card_usages.purchase_request_id`(FK NOT VALID → VALIDATE · `uniqueIndex`) · CHECK(purchase → 값 있음) |
| 마이그레이션 | `NNNN_card_usage_purchase_link.sql` · `NNNN_card_usage_purchase_link_validate.sql`(`--custom`) |
| 함수(domain) | `completePurchaseRequest` · `createCardUsage` purchase 갈래 · `cardUsageRights` 구매 완료 카드 고치기 갈래 |
| 상수 | `PURCHASE_REQUESTER_MAY_COMPLETE`(O-20) |
| Server Action | `completePurchaseRequestAction` |
| 라우트 | `/cards?new=1&purchase={id}`(S13) · 구매 완료 토스트 `구매 완료 · {번호}`(되돌리기 없음 — H-4) |
| 테스트 | `test/unit/domain/purchase-policy.test.ts` · purchase-requests 통합(「구매 완료 동시 6건」 포함) · E2E 케이스 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-12-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-3 — 구매 완료 트랜잭션 안의 `createCardUsage`가 권한(`can` → `findPermission`) · 활성 카드(`repositories/corp-cards.ts`) · 세율(`tax.*`)을 전역 `db`로 조회한다(06-12:158 · eng-cross-opus.md L22-24@333f7bf; 코드 확인: 세 리포지토리 모두 tx 인자 없음, `lib/env.ts:54` 풀 5) | 고침 | Plan 12, Task 1 ②: `precheckCardUsage` purchase 갈래(권한 · 활성 카드 · `loadTaxRates(usedOn)`)와 요청 사전 조회를 트랜잭션 전으로, `runComplete`는 tx 리포지토리와 `createCardUsage(viewer, input, pre, tx)`만, 행동 로그는 커밋 뒤. acceptance: `runComplete` 안 `\bcan\(`·`getSettingValue\(`·`loadTaxRates\(`·`precheckCardUsage\(`·`recordAction\(` 0건 + `runCreate` 범위 0건. Task 2 「구매 완료 동시 6건」(RED 먼저). must_haves.truths · T-06-155 |
| ENG E-2 뒷부분 — 06-12의 `can(viewer,"cards.purchases","write")`(06-12:158)도 「판정·설정 읽기는 트랜잭션 앞」(eng-review.md L70@333f7bf) | 고침 | Plan 12, Task 1 ② 트랜잭션 전 부분 · Task 3(구매 완료 건 카드 고치기의 권한 · 새 카드 활성 판정도 `updateCardUsage` 트랜잭션 전) · R-3 truth · T-06-56 |
| ENG E-3 — tx 규약 참조(eng-review.md L72-74@333f7bf) | 고침 | must_haves.truths에 「06-03 tx 규약」 참조 한 줄 |
| 동시성 통합 테스트 규약(동시 6건 = `Promise.all` 6건 > `DB_POOL_MAX` 5, RED 먼저) | 고침 | Plan 12, Task 2: 기존 「동시 두 구매 완료」를 「구매 완료 동시 6건」으로(같은 파일 `test/integration/purchase-requests.test.ts`) · fails_when · prohibitions 문구 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 배정된 항목은 모두 반영했다 |

### Round 2 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 10 · 12)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| H-4 — 되돌릴 수 있는 등록 · 요청 토스트에 `되돌리기`를 붙이되, 되돌릴 수 없는 것은 뺀다(design-review.md L41@f2720a8 · 반영 지시 10 L83@f2720a8) | HIGH(이 플랜 몫 — 되돌리기 없음 명시) | Plan 12: must_haves.truths `[H-4]` 줄(구매 완료 토스트에 되돌리기 없음 — UI-SPEC 「SUCCESS — 토스트」 되돌리기 없음 목록 · O-11). Task 1 read_first · behavior E2E 끝 · action ③(토스트 `구매 완료 · {번호}`, `actionLabel` 없음) · acceptance(버튼 0개 단언). Task 3 action(저장 토스트는 06-09 그대로). Artifacts. 모달을 없앨 곳은 이 플랜에 없다 |
| M-5 — 카드 폼 설명형 힌트를 줄인다(design-review.md L52@f2720a8 · 반영 지시 12 L85@f2720a8) | 참고(고침) | Plan 12: must_haves.truths 카드 고치기 줄에 힌트 문구 `구매 완료 때 카드 {이전 카드}`(바뀌기 전 값만). Task 3 read_first(rev 9 Copywriting 이름) · behavior E2E · action(문구 · 되돌리면 사라짐) · acceptance |
| (부수) Task 1 · 3 read_first의 UI-SPEC rev 8 줄 번호가 rev 9에서 낡았다 | 참고 | 절 이름으로 바꿨다. Task 2 read_first(`686-691행`)는 이 라운드에서 건드리지 않은 태스크라 그대로 — 실행자는 S13 차이 줄 · 상태 변경 문구 절로 읽는다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 배정된 H-4 · M-5 몫은 반영했다 |

### Round 3 — e2c8922(디자인 검토 반영 r2 — 미커밋 1회차 반영분 점검 `design-apply-checker-r1.md` · 교차 `design-apply-cross-r1.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W4 — Task 2 read_first `686-691행(S13 차이 줄 · 상태 변경 문구)`이 rev 9에서 S9 문단을 가리킨다(design-apply-checker-r1.md 경고 4 · 교차 R-7). Round 2가 「건드리지 않은 태스크라 그대로」로 남긴 줄 | 경고(고침) | Supersedes Round 2 Addressed 「(부수) … Task 2 read_first(`686-691행`)는 … 그대로」. Plan 12, Task 2 read_first → 「S13. 구매 완료 — 카드 사용 폼의 구매 완료 모드」 절 첫 줄(차이 줄) · 끝 줄(상태 변경 문구) · UI Considerations S13 error · partial 행. 줄 번호를 남기지 않았다 |
| 교차 B-2 — 카드 전표 첨부 주인 플랜 없음(S13 「증빙 첨부 선택」 포함) | 막음(다른 플랜에서 해소) | 이 플랜은 바꾸지 않는다. S13 폼의 첨부 영역과 구매 완료 트랜잭션의 전표 결합은 새 플랜 06-25(Task 2)가 이 플랜의 `completePurchaseRequest` · `completePurchaseRequestAction` 위에 더한다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 W4는 반영했다 |
