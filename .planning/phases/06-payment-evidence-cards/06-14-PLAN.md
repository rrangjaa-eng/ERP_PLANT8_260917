---
phase: 06-payment-evidence-cards
plan: 14
type: execute
wave: 8
depends_on: ["06-12"]
files_modified:
  - domain/purchase-requests/index.ts
  - domain/purchase-requests/policy.ts
  - domain/corp-card-usages/index.ts
  - repositories/purchase-requests.ts
  - app/(app)/cards/purchases/page.tsx
  - app/(app)/cards/purchases/purchase-request-form.tsx
  - app/(app)/cards/purchases/actions.ts
  - app/(app)/cards/purchases/actions.registry.ts
  - app/(app)/cards/link-picker.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/card-usage-form.tsx
  - test/unit/domain/purchase-policy.test.ts
  - test/integration/purchase-requests.test.ts
  - test/e2e/purchase-requests.spec.ts
autonomous: true
requirements: [EXP-10]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "직원이 `/cards/purchases?new=1`로 들어오면 연결 라디오 `팀 비용`이 골라져 있고(라디오는 남는다), 품목·예상 금액만 적고 1차 「구매 요청」을 누르면 팀 비용 요청이 06-02에서 정한 팀 비용 번호 규칙으로 번호를 받아 목록 첫 줄에 선다(EXP-10)"
    - "[O-19 기본값] 팀 비용 구매 요청에서 생기는 카드 사용의 사용한 사람 = 요청자, 팀 = 구매 완료 사용일의 요청자 소속(`teamAtDate(요청자, 사용일)`)이다 — 구매 완료하는 사람의 소속·카드의 팀은 팀을 정하지 않고, 사용일 소속이 없으면 1차 「구매 완료」가 막히며 서버도 거부한다. 이 판정은 `domain/purchase-requests/policy.ts`의 `purchaseTeamCostAttribution` 한 함수에서만 나온다"
    - "팀 비용 요청은 견적 줄을 잡지 않으므로 `lockQuoteLines` · `card.dual-link-block` · `purchase.line-door`를 타지 않는다 — B-1 잠금 순서는 06-08의 견적 줄 갈래에만 있고, 이 플랜은 그 갈래를 바꾸지 않는다"
    - "「06-03 tx 규약」을 따른다 — 팀 비용 요청의 번호 서식은 트랜잭션 전에 `loadDocumentNumberFormat`(06-02)으로 읽어 `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)`에 넘기고, 소속 판정(`purchaseTeamCostAttribution` → `teamAtDate`) · 권한(`can`)도 트랜잭션 전이다. 구매 완료 팀 비용 갈래의 사용한 사람 · 팀은 `precheckCardUsage` purchase 갈래가 트랜잭션 전에 받는다. 트랜잭션 몸통은 tx 리포지토리 · 순수 함수만, 행동 로그는 커밋 뒤(CROSS E-3 follow-on)"
    - "[O-9 기본값] 요청 취소는 `신청` 상태에서만 된다 — 요청자는 자기 `신청` 요청을 사유 없이, 구매 권한자는 남의 `신청` 요청을 사유와 함께 취소하고, `구매 완료`·`취소` 요청의 취소는 서버가 요청 행을 잠근 뒤 거부한다. 행동 로그 `status_change`(사람 · 시각 · 사유)"
    - "외화 예상 금액은 환율(기본 = 설정 최근 환율)과 서버 `toKrw` 원화 환산액이 늘 함께 저장된다 — 환율이 없으면 1차가 막히고 저장된 요청에 `계산 불가` 상태가 없다"
    - "S10(구매 요청 폼 갈래 — 06-07 `link-picker`의 `purchase` 모드): 온라인구매 줄만 고를 수 있고 나머지 줄은 `aria-disabled` + 2행 `거래처 {이름} · 지출결의로`, 고를 수 있는 줄 0이면 `온라인구매 줄 없음 · 팀 비용으로`"
    - "S11 empty: 「Empty — 구매 요청 목록」(기본 보기 0건은 경영관리 / 요청자 두 갈래, 전체 0건 한 갈래)"
    - "S11 loading: 첫 로드 = §7-7 LOADING 뼈대, 상태·월 필터 이동은 GET 이동, `요청 취소`는 확인 모달(요청 중 1차 `…`)"
    - "S11 error: 「Error — 목록 로드」 구매 요청 문구, 남의 요청 취소 사유 칸 = 「Error — 사유 근거 칸」"
    - "S11 populated: 취소 행 2행 `취소 {MM-DD} · {사람} · {사유}` · 합계 줄(원화 환산액만, 서버 `sumKrw`)"
    - "S11 partial: 외화 예상 금액은 2행 `USD 1,000.00 @1,350`, 팀 비용 요청의 취소 모달에는 결과 줄이 없고, 취소 행동은 `신청` 행에만 렌더된다"
    - "S11 overflow: 50건 페이지(§7-16), `전체` 보기에서 상태 그룹 `신청` → `구매 완료` → `취소`"
    - "S11 zero-one-many: 합계 줄 `합계 (신청 · N건)`, 카드 목록의 하위 링크는 열린 요청 0이면 `구매 요청`만, 1 이상이면 `구매 요청 {N}`(SP-4)"
    - "S12 loading: 외화 원화 환산 `Form.Hint`는 서버 계산 — 이전 값 `--faint` → 새 값, 1차 처리 중 `…`"
    - "S12 populated: `팀 비용`이면 요청자의 오늘 소속 텍스트 + `Form.Hint` `구매 완료 날 요청자 소속으로 정함`(O-19), `견적 줄`이면 프로젝트·견적 줄 텍스트 + 3차 `바꾸기`(S10 purchase 모드)"
    - "S12 partial: 외화인데 설정에 최근 환율이 없으면 환율 칸이 비고 1차가 막힌다, 입력이 있는데 `취소 Esc` = 「확인 — 입력 버리기」"
    - "S12 zero-one-many: 견적 줄 0 = `이 프로젝트에 견적 줄이 없습니다 · 팀 비용으로`(「Empty — 연결 고르기 목록」)"
    - "S13(팀 비용 갈래): 팀 = 요청자의 사용일 소속(읽기 텍스트 + `Form.Hint` `요청자 {이름} 사용일 소속`), `사용한 사람` 칸 없음, 사용일을 바꾸면 서버가 팀을 다시 보낸다(이전 값 `--faint`), 소속 없음 = `요청자 {이름} {MM-DD} 소속 없음 · 소속 발령은 관리자`"
    - statement: "S12 overflow: 구매 요청 폼에서 연 S10 목록(온라인구매 줄만 고를 수 있는 긴 목록)이 스크롤로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S11 overflow(폰): 행 3차 `요청 취소`와 2행 취소 사유가 폰 P1에서 행 밖으로 넘치지 않는다 — 긴 사유로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "팀 비용 구매 요청의 팀·사용한 사람을 입력(폼 값)으로 받아서는 안 된다 — 정책 함수가 요청자와 사용일로만 정한다(O-19)"
      status: resolved
      verification: test
    - statement: "`구매 완료`된 요청을 취소해 이미 생긴 카드 사용 건이 연결 없이 남아서는 안 된다(O-9)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/purchase-requests/policy.ts"
      provides: "[O-19] 팀 비용 귀속 함수 — 06-12의 [O-20] 상수와 같은 파일"
      exports: ["purchaseTeamCostAttribution", "PURCHASE_REQUESTER_MAY_COMPLETE"]
    - path: "domain/purchase-requests/index.ts"
      provides: "팀 비용 갈래 신청 · 취소 · 목록 마감(필터·그룹·페이지·합계)"
      exports: ["createPurchaseRequest", "cancelPurchaseRequest", "listPurchaseRequests"]
    - path: "test/unit/domain/purchase-policy.test.ts"
      provides: "O-19 귀속 · O-20 상수 단위 고정"
      min_lines: 30
  key_links:
    - from: "domain/purchase-requests/index.ts"
      to: "domain/purchase-requests/policy.ts"
      via: "팀 비용 귀속을 정책 파일에서만 읽는다"
      pattern: "purchaseTeamCostAttribution"
    - from: "domain/purchase-requests/index.ts"
      to: "domain/org/index.ts"
      via: "사용일 소속을 `teamAtDate`로 본다"
      pattern: "teamAtDate"
    - from: "app/(app)/cards/card-usage-list.tsx"
      to: "domain/purchase-requests/index.ts"
      via: "카드 목록 하위 링크의 열린 요청 수(SP-4)"
      pattern: "구매 요청"
---

<objective>
구매 요청(EXP-10)을 마감한다 — 팀 비용 요청(O-19 귀속), 연결 고르기 purchase 모드, 취소(O-9), 외화 예상 금액, 목록 필터·그룹·페이지·합계, 카드 목록 하위 링크(SP-4). 06-08(신청 경로) · 06-12(구매 완료) 위에 얹는다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 「팀 비용까지 모든 나가는 돈」의 구매 경로다. 팀 비용 요청이 서지 않으면 온라인 구매 중 프로젝트 밖 비용이 카드 사용 직접 등록으로 새어 나가고, 귀속 팀이 사람마다 달라진다.

Output: `purchaseTeamCostAttribution`, 팀 비용 갈래 신청 · 구매 완료, `cancelPurchaseRequest`, 목록 마감, `link-picker` purchase 모드, SP-4 링크, 단위 1 · 통합 1 · E2E 1(확장).

**규모 근거:** `files_modified` 14개, `estimate.tokens` 90000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-06(29파일 · 130k — 체커 BLOCKER)을 06-08 · 06-12 · 이 플랜으로 나눈 셋째 몫이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-08-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-12-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-14 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-02 번호 공백 | 팀 비용 요청 번호 규칙(Phase 5 규칙 재사용 또는 사용자 선택) — 06-02 SUMMARY | `grep -n "팀 비용" .planning/phases/06-payment-evidence-cards/06-02-SUMMARY.md` 0건이면 멈춤 |
| 06-12 | `completePurchaseRequest` · `policy.ts`의 `PURCHASE_REQUESTER_MAY_COMPLETE` · `createCardUsage` purchase 갈래(R-3) | `grep -c "PURCHASE_REQUESTER_MAY_COMPLETE" domain/purchase-requests/policy.ts` 0이면 멈춤 |
| A-606 | `teamAtDate`(`domain/org/index.ts`) | `grep -n "export async function teamAtDate" domain/org/index.ts` 0건이면 멈춤 |
| UA-602 · UA-603 | `ConfirmDialog` · `Pagination` | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx ui/pagination/Pagination.tsx` 둘 중 하나라도 비면 멈춤 |
| 06-05 | 최근 환율 설정 · 카드 목록 필터 줄 | `grep -n "fx\|환율" domain/corp-card-usages/index.ts` 결과를 SUMMARY에 적는다 — 최근 환율 조회가 없으면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-19** | 사용한 사람 = 요청자, 팀 = 구매 완료 사용일의 요청자 소속, S13 팀은 읽기 | **Task 1 하나**: `domain/purchase-requests/policy.ts`의 `purchaseTeamCostAttribution` + 그 단위 테스트 + S12·S13의 힌트 한 줄씩 |
| O-9 | 취소는 `신청` 상태에서만 | `cancelPurchaseRequest`의 상태 검사 |
| O-1 | 구매 요청 목록 = 법인카드 메뉴의 하위 목록(SP-4) | `card-usage-list.tsx` 필터 줄 3차 |

<assumption_delta_decision>
`detected: false`. 06-08 · 06-12의 구매 요청 계약에 팀 비용 갈래·취소·목록 마감을 **더한다**. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-10 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 3 · O-9 · O-19 · SP-4. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — [O-19 기본값] 팀 비용 구매 요청 한 건이 신청 → 구매 완료되어 요청자의 사용일 소속 팀 비용 카드 사용이 된다</name>
  <files>domain/purchase-requests/policy.ts, domain/purchase-requests/index.ts, domain/corp-card-usages/index.ts, app/(app)/cards/purchases/purchase-request-form.tsx, app/(app)/cards/purchases/actions.ts, app/(app)/cards/card-usage-form.tsx, test/unit/domain/purchase-policy.test.ts, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 672-675행(팀 비용의 팀 — O-19) · 686-689행(S13 팀 비용) · 1011-1017행(S13 considerations) · Copywriting 「막힘 — 카드 사용 폼」 구매 완료 모드 소속 없음 갈래
    - /mnt/project-files/06-prep/open-choices.md 28행(O-19 기본값과 대안)
    - domain/purchase-requests/index.ts(06-08 `createPurchaseRequest`의 트랜잭션 전 조회 · `runCreate`, 06-12 `completePurchaseRequest`의 `runComplete`) · policy.ts(06-12) · domain/corp-card-usages/index.ts의 `precheckCardUsage` purchase 갈래(06-12 R-3)
    - domain/org/index.ts 190행 근처 `teamAtDate`
    - 06-02 SUMMARY의 팀 비용 번호 규칙 · domain/document-numbering/index.ts의 `loadDocumentNumberFormat` · `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)`
    - 06-03-PLAN.md must_haves.truths의 「tx 규약」 줄 · /mnt/project-files/deadlock-fix/global-db-in-tx-audit.md(`team-memberships.ts` · `permissions.ts` · `settings.ts`는 tx 인자 없음)
  </read_first>
  <behavior>
    - `purchaseTeamCostAttribution(requesterId, usedOn)`: 사용한 사람 = 요청자 · 팀 = `teamAtDate(요청자, 사용일)` / 소속 없음 → null(호출자가 거부) / 처리자·카드의 팀은 입력에 없다
    - `createPurchaseRequest` 팀 비용 갈래: 요청자 오늘 소속 있음 → 저장 + 번호(06-02 팀 비용 규칙) + `document_create` / 오늘 소속 없음 → 거부 / 폼이 팀 id를 보내도 무시
    - `completePurchaseRequest` 팀 비용 갈래: 카드 사용의 `used_by` = 요청자 · 팀 = 사용일 소속 / 처리자의 소속이 달라도 팀은 요청자 쪽 / 사용일 소속 없음 → 거부, 요청은 `신청` 유지
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

**이 태스크가 O-19의 유일한 자리다.** `domain/purchase-requests/policy.ts`(06-12가 만든 파일)에 `purchaseTeamCostAttribution`을 더하고, 파일 머리 주석을 "O-19 · O-20 답이 바뀌면 이 파일과 그 단위 테스트만 고친다"로 바꾼다. `createPurchaseRequest`에 연결 `team_cost` 갈래를 더한다 — 견적 줄을 잡지 않으므로 잠금 · 줄 게이트가 없다. **트랜잭션 전(06-03 tx 규약 · CROSS E-3 follow-on):** 오늘 소속을 같은 함수(사용일 = 오늘 — `teamAtDate`는 전역 `db`)로 미리 보고 없으면 거부, 번호 서식을 `const format = await loadDocumentNumberFormat(팀 비용 counterKey)`로 읽는다(06-02 SUMMARY의 팀 비용 규칙 — Phase 5 규칙 재사용이면 그 counterKey와 서식 로더를 쓰고, 그 로더가 트랜잭션 안에서 설정을 읽는 꼴이면 06-02 ②처럼 서식을 인자로 받게 고쳐 쓴다). **트랜잭션 몸통(06-08의 `runCreate` 꼴 — 팀 비용 갈래도 같은 안쪽 함수 안):** `allocateScopedDocumentNumber(viewer, { counterKey, scope, format }, tx)` → 요청 INSERT(tx). **커밋 뒤:** `document_create`. 06-08의 견적 줄 갈래(B-1 잠금 순서)는 건드리지 않는다. `completePurchaseRequest`는 팀 비용 요청이면 **트랜잭션 전에** 이 정책 함수로 사용한 사람 · 팀을 정해(null이면 거부 — 임의 팀 없음) `precheckCardUsage` purchase 갈래에 넘기고(`pre.usedByUserId` · `pre.teamId`), `createCardUsage`의 `runCreate`는 `pre`의 사용한 사람 · 팀을 그대로 쓴다(폼 값이 아니다 — `runComplete` · `runCreate` 안에서 `teamAtDate`를 부르지 않는다). 액션 zod 스키마에는 팀 · 사용한 사람 칸이 없다. 먼저 behavior 테스트를 쓰고 RED를 확인한 뒤 구현한다.

화면: `purchase-request-form.tsx`는 `?new=1`만으로 들어오면 연결 라디오 `팀 비용`이 골라져 있고, 요청자 오늘 소속 텍스트 + `Form.Hint` `구매 완료 날 요청자 소속으로 정함`. `card-usage-form.tsx` purchase 모드의 팀 비용 갈래는 팀 읽기 텍스트 + `Form.Hint` `요청자 {이름} 사용일 소속`, 사용일 변경 시 서버가 팀을 다시 보내고(이전 값 `--faint`), 소속 없음이면 1차 비활성 + 막힘 문구, `사용한 사람` 칸 없음.

단위 `purchase-policy.test.ts`(06-12 파일)에 behavior 첫 줄, 통합 `purchase-requests.test.ts`에 둘째·셋째 줄, E2E `purchase-requests.spec.ts`에 「팀 비용 요청 신청 → 구매 권한자 구매 완료 → `/cards` 목록에 요청자 · 요청자 팀으로 한 줄」 한 경로.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/purchase-policy.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/corp-card-usages.test.ts test/integration/document-counters-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rln "purchaseTeamCostAttribution" domain/ app/` 결과가 `domain/purchase-requests/policy.ts` · `domain/purchase-requests/index.ts` 두 파일뿐이다
    - 통합 테스트가 「처리자 소속이 요청자와 달라도 팀 = 요청자 사용일 소속」 · 「요청자 사용일 소속 없음 → 구매 완료 거부 + 요청 `신청` 유지」 · 「폼이 보낸 팀 id 무시」를 개별 `it`으로 단언한다
    - (CROSS E-3 follow-on) 세 트랜잭션 몸통에 전역 `db` 판정·조회 호출 0건: `awk '/const runCreate = async/,/^  };$/' domain/purchase-requests/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadDocumentNumberFormat\(|teamAtDate\(|purchaseTeamCostAttribution\(|recordAction\('`가 0 · 같은 정규식으로 `const runComplete = async` 범위가 0 · `awk '/const runCreate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|loadTaxRates\(|teamAtDate\(|purchaseTeamCostAttribution\('`가 0(세 범위 모두 줄 수 5 이상)
    - ⓪ 결과(팀 비용 번호 규칙 출처)가 SUMMARY에 있다
  </acceptance_criteria>
  <done>팀 비용 요청 한 건이 신청 → 구매 완료 → 요청자 사용일 소속의 카드 사용 건까지 정책 파일 한 곳의 판정으로 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [O-9 기본값] 요청 취소 · 연결 고르기 purchase 모드(S10) · 외화 예상 금액</name>
  <files>domain/purchase-requests/index.ts, repositories/purchase-requests.ts, app/(app)/cards/purchases/page.tsx, app/(app)/cards/purchases/purchase-request-form.tsx, app/(app)/cards/purchases/actions.ts, app/(app)/cards/purchases/actions.registry.ts, app/(app)/cards/link-picker.tsx, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 652-653행(S10 구매 요청 폼 갈래) · 995-1010행(S11 · S12 considerations) · 835행(예상 금액 금액 모델) · Copywriting 「Destructive — 요청 취소」 · 「Error — 사유 근거 칸」
    - /mnt/project-files/06-prep/open-choices.md 18행(O-9)
    - app/(app)/cards/link-picker.tsx(06-07 card 모드) · app/(app)/cards/card-usage-form.tsx의 외화 · 환율 칸(06-05)
  </read_first>
  <behavior>
    - `cancelPurchaseRequest`: 요청자 · 자기 `신청` → 사유 없이 취소 / 구매 권한자 · 남의 `신청` → 사유 필수(빈 사유 거부) / 그 밖 → 거부 / `구매 완료`·`취소` → 거부 / 요청 행 `FOR UPDATE` 뒤 판정 / `status_change` 로그
    - 취소 ∥ 구매 완료 동시(`Promise.all`) → 정확히 하나 성공
    - 외화 예상 금액: 통화 USD · 1,000.00 · 환율 1,350 → 원화 환산 1,350,000 저장 / 환율 없음 → 거부
    - `link-picker` purchase 모드: 온라인구매 줄만 `selectable` · 나머지 `aria-disabled` + 2행 이유
  </behavior>
  <action>
`cancelPurchaseRequest`를 `domain/purchase-requests/index.ts`에 더한다 — 06-12의 `completePurchaseRequest`와 같은 요청 행 `FOR UPDATE` 뒤 상태·권한을 판정한다(취소와 구매 완료가 같은 행을 두고 경합하면 하나만 이긴다). 구매 권한 `can(viewer, "cards.purchases", "write")`은 **트랜잭션 전에** 읽어 두고(06-03 tx 규약 — `findPermission`은 전역 `db`), 잠근 뒤에는 그 값과 잠근 행의 요청자 · 상태로만 판정한다. `status_change` 행동 로그는 커밋 뒤. 액션 `cancelPurchaseRequestAction`을 `actions.ts`와 `actions.registry.ts`에 더한다(06-08이 registry를 leak-scan에 이미 import했다 — 재확인만). 목록 행 3차: 요청자에게 자기 `신청` 행 `요청 취소`(`ConfirmDialog`, 사유 칸 없음 — 팀 비용 요청은 결과 줄 없음), 구매 권한자에게 남의 `신청` 행 `요청 취소`(사유 근거 칸 필수). 접근 이름 `{번호} 요청 취소`. 취소 행 2행 `취소 {MM-DD} · {사람} · {사유}`.

폼 `견적 줄` 갈래: 프로젝트 「바꾸기」 → 견적 줄 「바꾸기」로 `link-picker`의 `mode="purchase"`를 연다 — 온라인구매 줄 판정은 서버 DTO의 `door` 값(06-02 `resolveLineDoor`)만 쓰고 클라이언트가 거래처 이름을 비교하지 않는다. 고를 수 있는 줄 0이면 1차 `이 줄로` 비활성 + `온라인구매 줄 없음 · 팀 비용으로`, 견적 줄 0이면 「Empty — 연결 고르기 목록」.

예상 금액: 통화 `Select` + 금액, 외화면 환율 칸(기본 = 06-05의 최근 환율 조회, 없으면 빈 칸 + 1차 막힘) + 서버 원화 `Form.Hint`(이전 값 `--faint`). 서버가 `toKrw`로 원화 환산액을 계산해 함께 저장한다 — 클라이언트가 보낸 환산액은 받지 않는다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 첫 줄 갈래 다섯과 「취소 ∥ 구매 완료 `Promise.all` → 성공 1」을 개별 `it`으로 단언한다
    - E2E에 「요청자 자기 취소」 · 「연결 고르기에서 비온라인 줄 `aria-disabled` + 2행 이유」 · 「외화 요청 2행 `USD 1,000.00 @1,350`」 케이스가 녹색이다
  </acceptance_criteria>
  <done>요청 취소가 `신청` 상태에서만 권한별로 되고 구매 완료와 경합해도 한쪽만 이기며, 연결 고르기와 외화 금액이 계약대로 선다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 구매 요청 목록 마감(S11 필터 · 그룹 · 50건 페이지 · 합계 줄) · 카드 목록 하위 링크(SP-4)</name>
  <files>domain/purchase-requests/index.ts, repositories/purchase-requests.ts, app/(app)/cards/purchases/page.tsx, app/(app)/cards/card-usage-list.tsx, test/integration/purchase-requests.test.ts, test/e2e/purchase-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 655-668행(S11 정본) · 868행 근처 SP-4 · Copywriting 「Empty — 구매 요청 목록」 · 「Error — 목록 로드」
    - domain/money/index.ts(06-02 `sumKrw`) · app/(app)/cards/card-usage-list.tsx(06-05 필터 줄)
  </read_first>
  <behavior>
    - 필터 `[신청 | 구매 완료 | 취소 | 전체] [월]` → 서버 쿼리 / 기본 보기 `신청`
    - 그룹: `전체`면 상태(신청 → 구매 완료 → 취소), 그 밖이면 요청일 주
    - 50건 페이지 · 합계 줄 `합계 ({보기} · N건)` = 원화 환산액만 `sumKrw`(범위 안 행만)
    - 카드 목록 하위 링크 열린 요청 수 = 보는 사람 범위 안 `신청` 건수(0이면 숫자 없음)
  </behavior>
  <action>
`listPurchaseRequests`에 필터 · 그룹 · 페이지 · 합계를 더한다 — 범위(06-08: 자기 요청 + 담당 PM 프로젝트 줄 요청 / 구매 권한자·전사 범위 = 전부)는 리포지토리 쿼리에서 그대로 거르고, 합계와 건수도 같은 범위 쿼리에서 서버가 계산한다(클라이언트 합산 없음). 필터 이동은 GET 쿼리(`?status=&month=&page=`). `page.tsx`: 필터 줄, 그룹 머리, `Pagination`, 합계 줄, 빈 화면(「Empty — 구매 요청 목록」 — 기본 보기 0건은 경영관리 / 요청자 두 갈래, 전체 0건 한 갈래, 빈 화면에는 1차 `구매 요청`), 첫 로드 §7-7 LOADING 뼈대. 폰에서도 행 안 3차로 폼·모달이 선다.

`card-usage-list.tsx`: 필터 줄 오른쪽 3차 `구매 요청 {열린 건수}`(0이면 `구매 요청`) → `/cards/purchases`. 건수는 서버가 목록 범위와 같은 쿼리로 계산한 값을 페이지가 넘긴다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/purchase-requests.test.ts test/integration/corp-card-usages.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/purchase-requests.spec.ts test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 「범위 밖 계정의 합계·건수에 남의 요청이 들지 않음」 · 「51건 → 둘째 페이지 1건」 · 「`전체` 보기 그룹 순서」를 단언한다
    - E2E가 카드 목록 하위 링크 문구 `구매 요청` → 요청 1건 신청 뒤 `구매 요청 1`을 단언한다
  </acceptance_criteria>
  <done>구매 요청 목록이 필터·그룹·페이지·합계까지 서버 범위 안에서 서고, 카드 목록에서 열린 요청 수로 들어갈 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 구매 요청 액션 | 연결 종류 · 품목 · 예상 금액 · 통화 · 환율이 넘어온다. 팀 · 사용한 사람 · 원화 환산액은 넘어오지 않는다 |
| 브라우저 → 취소 액션 | 요청 id · 사유가 넘어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-66 | Spoofing | 팀 비용 귀속 | medium | mitigate | 액션 zod 스키마에 팀 · 사용한 사람 칸이 없고 `purchaseTeamCostAttribution`이 요청자 · 사용일로 정한다. 「폼이 보낸 팀 id 무시」 통합 테스트 |
| T-06-67 | Elevation of Privilege | `cancelPurchaseRequest` | medium | mitigate | 요청자 본인 또는 `cards.purchases` write만, 요청 행 `FOR UPDATE` 뒤 판정. 권한 밖 계정 거부 통합 테스트 |
| T-06-68 | Tampering | 취소 ∥ 구매 완료 경합 | medium | mitigate | 두 함수가 상태 판정 전에 같은 요청 행 잠금을 잡는다(구매 완료는 그 앞에 견적 줄을 잡는다 — 06-12 전역 순서 N-3, 취소는 줄을 잡지 않으므로 교착 없음). `Promise.all` 통합 테스트 |
| T-06-69 | Tampering | 외화 원화 환산액 | low | mitigate | 서버 `toKrw`로만 계산해 저장, 클라이언트 값은 받지 않는다 |
| T-06-70 | Information Disclosure | 목록 합계 · 카드 목록 건수 | medium | mitigate | 합계 · 건수를 목록과 같은 범위 쿼리에서 계산. 범위 밖 계정 통합 테스트 · leak-scan 녹색 |
| T-06-71 | Repudiation | 요청 취소 | low | mitigate | `status_change` 로그에 사람 · 시각 · 사유 |
| T-06-157 | Denial of Service | 팀 비용 요청 · 구매 완료 · 취소 트랜잭션의 풀 고갈 교착 | high | mitigate | 번호 서식(`loadDocumentNumberFormat`) · 소속(`teamAtDate`) · 권한(`can`)은 트랜잭션 전, 구매 완료 팀 비용 귀속은 `precheckCardUsage` purchase 갈래로, 트랜잭션 몸통은 tx 리포지토리 · 순수 함수만, 행동 로그는 커밋 뒤(06-03 tx 규약 · CROSS E-3 follow-on · PR #75 꼴). Task 1 region grep 셋 · 06-08/06-12의 동시 6건 케이스가 같은 파일에서 계속 녹색 |
</threat_model>

<verification>
- 단위 purchase-policy · leak-scan-coverage, 통합 purchase-requests · corp-card-usages · document-counters-concurrency · leak-scan 녹색
- E2E purchase-requests · card-usage가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S10(purchase 모드) · S11 · S12 · S13(팀 비용 갈래) DOM 실측 감사(backstop 2건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 팀 비용 구매 요청의 귀속 팀이 요청자의 사용일 소속으로만 정해지고, O-19 답이 바뀌어도 정책 파일 하나만 고친다
- 요청 취소가 `신청` 상태에서만 권한별로 되고, 구매 완료와 경합해도 한쪽만 이긴다
- 구매 요청 목록이 계약대로 마감되고 카드 목록에서 들어갈 수 있다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `purchaseTeamCostAttribution` · `cancelPurchaseRequest` · `listPurchaseRequests`(필터 · 그룹 · 페이지 · 합계) |
| Server Action | `cancelPurchaseRequestAction` |
| 변경된 기존 심볼 | `createPurchaseRequest`(팀 비용 갈래 — 서식은 트랜잭션 전) · `completePurchaseRequest`(팀 비용 귀속 — 트랜잭션 전) · `precheckCardUsage` purchase 갈래(호출자 귀속 사용) · `link-picker` `purchase` 모드 · 카드 목록 필터 줄 |
| 화면 | `/cards/purchases`(S11 마감 · S12 팀 비용 · 외화) · `/cards?new=1&purchase={id}`(S13 팀 비용 갈래) · `/cards` 하위 링크 |
| 테스트 | `test/unit/domain/purchase-policy.test.ts`(확장) · `test/integration/purchase-requests.test.ts`(확장) · `test/e2e/purchase-requests.spec.ts`(확장) |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-14-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-3 follow-on — 06-05/06-12가 카드 사용 생성을 `precheckCardUsage`(트랜잭션 밖) → `createCardUsage(…, pre, tx)`로 확정한 뒤, 이 플랜은 팀 비용 귀속을 「`createCardUsage` purchase 갈래」에 넘기고 있었다(eng-cross-opus.md L22-24@333f7bf의 같은 꼴) | 고침 | Plan 14, Task 1: `purchaseTeamCostAttribution`(→ `teamAtDate`)을 `completePurchaseRequest` 트랜잭션 전에 부르고 결과를 `precheckCardUsage` purchase 갈래(`pre.usedByUserId` · `pre.teamId`)로 넘긴다. Task 2: 취소의 `can`도 트랜잭션 전. acceptance region grep 셋 · T-06-157 |
| CROSS E-2 follow-on — 팀 비용 요청 번호도 서식을 트랜잭션 안에서 읽으면 PR #75 교착이 된다(eng-cross-opus.md L16-18@333f7bf) | 막음 | Plan 14, Task 1: 트랜잭션 전 `loadDocumentNumberFormat` → `allocateScopedDocumentNumber(…, { …, format }, tx)`(06-02 시그니처). Phase 5 규칙 재사용이면 그 로더도 같은 꼴로 쓴다 |
| ENG E-3 — tx 규약 참조(eng-review.md L72-74@333f7bf) | 고침 | must_haves.truths에 「06-03 tx 규약」 참조 한 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 팀 비용 요청 경로의 별도 「동시 6건」 테스트 | 추가하지 않음 — 같은 파일의 06-08 「동시 6건 번호 경합」 · 06-12 「구매 완료 동시 6건」이 같은 트랜잭션 꼴을 지키고, 이 플랜 verify가 그 파일을 다시 돌린다. 브리프의 동시 6건 배정 목록에 06-14가 없다 |
