---
phase: 06-payment-evidence-cards
plan: 15
type: execute
wave: 8
depends_on: ["06-11"]
files_modified:
  - domain/payments/targets.ts
  - domain/payments/batch.ts
  - repositories/payment-targets.ts
  - ui/table/Table.tsx
  - ui/table/types.ts
  - ui/table/Table.module.css
  - app/(app)/expenses/page.tsx
  - app/(app)/expenses/payment-targets-table.tsx
  - app/(app)/expenses/batch-payment-dialog.tsx
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - test/integration/payment-batch.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/payment-batch.spec.ts
autonomous: true
requirements: [EXP-09, EXP-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 권한자의 `/expenses` 기본 보기가 「지급 대상」(결재 통과 · 지급 전 지출결의 전사 전부)이고, 그 밖의 사람의 기본 보기는 Phase 5 그대로다"
    - "지급 권한자가 「지급 대상」에서 여러 건을 고르고 1차 `지급 완료 N` → 확인 모달에서 지급일(기본 오늘) 한 칸을 확인하면 **건마다 따로** 처리되어, 처리된 건은 목록에서 빠지고 막힌 건은 자리·선택을 유지한 채 행 아래 이유 한 줄이 선다(D-604 부분 성공, AS1)"
    - "각 행을 고를 수 있는지는 서버가 행마다 `resolveExpenseActionRow`(P4)로 판정해 보내고, 처리 요청 때 `completePaymentsBatch`가 행마다 06-03 · 06-04의 `completeExpensePayment`를 자기 트랜잭션으로 불러 `lockExpenseRow`(R-1) 뒤 게이트를 **다시** 돈다 — 클라이언트가 보낸 선택 가능 여부를 믿지 않는다"
    - "**R-2:** 처리 요청은 모달을 연 순간의 스냅숏 `{ expenseId, expenseVersion, transferKrw, diffReason, expectedPayableKrw }` 배열이다. 그사이 문서 version이 바뀐 행(지급 · 취소 · 증빙 확인 · 면제로 +1)은 처리하지 않고 막힘 + 「거부 — 일괄 지급 건별 결과」 동시성 문구(`다른 사람이 {시:분}에 바꿈 · 새로 고침` 꼴, 사람·시각은 서버 값)로 돌아온다. **N-4:** 증빙 무효화(06-11 `invalidateEvidenceReview`)는 version을 올리지 않는다(06-11 훅은 그대로 둔다) — 확인이 풀린 행은 `completeExpensePayment`가 행 잠금 뒤 게이트를 다시 돌 때 게이트 문구로 막힌다(Task 1 behavior 「P4가 아니게 된 행」 갈래). Phase 5 PM 증빙 편집 함수가 스스로 version을 올리면 동시성 문구로 막힌다 — 어느 쪽인지는 06-06 ⓪ SUMMARY의 부수 작업 목록(N-6)에 있다"
    - "「06-03 tx 규약」을 따른다 — `completePaymentsBatch`는 권한 판정을 행 루프 전에 한 번 하고 스스로 트랜잭션을 열지 않으며, 행마다 단건 경로(사전 조회 `loadPaymentInputs` → 트랜잭션)를 그대로 부른다. `listPaymentTargets`는 트랜잭션 없는 읽기 경로에서 단건과 같은 두 몫 세율 `{ atEvidence, atPayment }`로 `decidePayable`을 부른다(행마다 `pickTaxDates` — 부가세는 증빙일, 원천징수 · 회사 대납은 지급일 = 예정일 > 오늘 KST). 세율은 요청 안에서 서로 다른 날짜마다 `loadTaxRates`를 한 번만 읽어 캐시하고, 세금 규칙 · `EVIDENCE_REQUIRED`도 요청마다 한 번 읽는다 — 행마다 설정을 다시 읽지 않는다(CROSS-R1 F-4 · CHK-R1 W1). 06-03의 「지급 완료 동시 6건」이 이 플랜 뒤에도 녹색이다"
    - "목록이 보여 주는 지급 총액(= 일괄 요청의 `expectedPayableKrw`)은 같은 지급일이면 `completeExpensePayment`의 잠금 뒤 재계산과 같다 — 증빙일과 예정일 사이에 부가세율이 바뀐 행도 일괄 지급이 `PayableChangedError` 없이 처리된다(통합 테스트 「목록 지급 총액 = 단건 재계산」)"
    - "**ENG E-5 멱등성:** 같은 일괄 처리 요청(같은 스냅숏)을 다시 보내면 첫 요청이 올린 문서 version과 어긋나 행마다 동시성 문구로 막히고 살아 있는 지급 기록이 한 건도 늘지 않는다 — 통합 테스트 「같은 요청 재전송 → 0건 추가 지급」"
    - "[O-2 기본값의 소비처] `확인 전` 행의 선택 칸은 `aria-disabled` + 이유 `확인 전 · 증빙 확인`이고, 이 판정은 06-04 · 06-06의 `resolveExpenseActionRow` · `payment.evidence-required`를 그대로 부른다 — 이 플랜에 O-2 조건이 새로 쓰이지 않는다"
    - "[O-3 기본값] 한 번의 일괄 처리는 지급일 한 값을 고른 건 전부에 쓴다"
    - "SP-1 `selectable` 편집 표 variant가 `ui/table`에 있다 — 선택 열(네이티브 체크박스 · `accent-color: var(--native-accent)` · 폭 44) · 머리글 체크박스 = 이 쪽의 고를 수 있는 행 전체(섞이면 `indeterminate`) · 못 고르는 행은 `aria-disabled` + `aria-describedby` → 그 행 이유 글자, 고른 행은 `--accent-weak` 면(행 배경 칠 금지는 막힌 행에만)"
    - "S1 empty: 0건 = 「Empty — 지급 대상」, 필터 0건 = 「Empty — 지급 대상 · 필터 0건」 — 둘 다 합계 줄·선택 열·1차를 그리지 않는다"
    - "S1 loading: 첫 로드 = §7-7 LOADING 뼈대, 필터 이동은 GET 이동, 처리 중 1차 `지급 완료 N…` + `aria-disabled`"
    - "S1 error: 로드 실패 = 「Error — 목록 로드」"
    - "S1 populated: 1280 이상 10열 · 그룹 머리글(`예정일 지남` → `이번 주 지급` → `다음 주` → `그 뒤` → `지급 예정일 없음`, 주는 월요일 시작 KST — 서버 판정) · 합계 줄(필터 전체, 서버 `sumKrw`)"
    - "S1 partial: 일괄 처리 뒤 처리된 행은 빠지고 막힌 행은 자리·선택 유지 + 행 아래 이유 + 1차 왼쪽 결과 글자(`지급 완료 N건 {시:분} · 막힘 N건`), 이유는 다음 시도·필터 변경 때 지운다"
    - "S1 zero-one-many: 고른 0건 = 1차 비활성 + `고른 건 없음`(`info`), 1건 = `지급 완료 1`, 머리글 체크박스 = 이 쪽의 고를 수 있는 행 전체(섞이면 `indeterminate`)"
    - "S2 empty: 지급일 한 칸은 비어서 열리지 않는다 — 기본 오늘(KST)"
    - "S2 loading: 처리 요청 중 1차 `…` + 나머지 `aria-disabled`"
    - "S2 error: 지급일 칸 = 「Error — 지급일 · 지급 예정일 칸」(모달 유지), 처리 응답 없음 = 「거부 — 일괄 지급 응답 없음」(모달을 닫지 않는다)"
    - "S2 partial: 일부만 처리 = 모달이 닫히고 S1 결과로, 1건 이상 처리면 토스트 `지급 완료 · N건`, 0건 처리면 토스트 없음"
  prohibitions:
    - statement: "일괄 처리에서 한 건의 실패가 다른 건의 지급 기록을 되돌리거나 막아서는 안 된다(D-604 부분 성공)"
      status: resolved
      verification: test
    - statement: "일괄 처리가 단건 경로를 거치지 않고 지급 기록을 직접 INSERT해서는 안 된다 — 게이트 · 잠금 · version · 재계산이 한 경로다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/payments/targets.ts"
      provides: "지급 대상 목록 DTO(행별 선택 가능 · 이유 · 지급 총액 · version · 그룹)"
      exports: ["listPaymentTargets"]
    - path: "domain/payments/batch.ts"
      provides: "건별 트랜잭션 일괄 지급(AS1) — 행마다 단건 경로 재사용, R-2 스냅숏"
      exports: ["completePaymentsBatch"]
    - path: "ui/table/Table.tsx"
      provides: "SP-1 `selectable` variant"
    - path: "test/integration/payment-batch.test.ts"
      provides: "부분 성공 · 게이트 재판정 · version 낡음 막힘 · 권한 증명"
      min_lines: 50
  key_links:
    - from: "domain/payments/batch.ts"
      to: "domain/payments/index.ts"
      via: "행마다 단건 지급 함수를 자기 트랜잭션으로 부른다(잠금 · 게이트 · 재계산 · version이 한 경로)"
      pattern: "completeExpensePayment"
    - from: "domain/payments/targets.ts"
      to: "domain/payments/action-row.ts"
      via: "행별 선택 가능 · 이유가 문서 화면과 같은 1차 판정표에서 나온다"
      pattern: "resolveExpenseActionRow"
    - from: "app/(app)/expenses/actions.registry.ts"
      to: "test/integration/leak-scan.test.ts"
      via: "목록 액션 DTO가 누수 스캔에 든다"
      pattern: "expenses/actions.registry"
---

<objective>
경영관리의 **일괄 지급** 뼈대를 세운다 — 지출결의 목록의 「지급 대상」 보기(S1), 확인 모달과 건별 처리(S2), SP-1 선택 열 variant. 06-03 · 06-04의 단건 경로를 행마다 재사용하고 R-2 스냅숏으로 낡은 행을 막는다. 이체액 편집 · 서버 합 · 지급일 재계산 · 키보드 · 페이지는 06-17, 계좌 · 지급 완료 보기는 06-20.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 경영관리는 은행에서 스무 건을 한 번에 이체하고 기록한다. 한 건의 계좌 오류로 스무 건의 기록이 막히면 안 되고(SP-1 이유), 모달을 연 뒤 바뀐 문서가 조용히 옛 값으로 처리되면 안 된다(R-2).

Output: `domain/payments/targets.ts` · `batch.ts`, `repositories/payment-targets.ts`, `ui/table` `selectable` variant, `/expenses?view=pay` · 일괄 지급 모달, 통합 1 · E2E 1.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-08(19파일 · 130k)을 06-15 · 06-17 · 06-20으로 나눈 첫 몫이다. 웨이브 8인 이유: 증빙 게이트 입력(확인 06-06 · 선결제 06-10 · 무효화 06-11)이 모두 들어온 뒤에 행별 판정을 붙인다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-RESEARCH.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-03-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-04-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-15 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-607 · UA-605 | Phase 5 지출결의 목록 `/expenses`(상태 필터 · 그룹 머리글 · 합계 줄)와 그 액션 파일. **main의 `app/(app)/expenses/page.tsx`는 ListEmpty 자리다** — Phase 5 목록이 들어와 있어야 한다 | `grep -c "ListEmpty" 'app/(app)/expenses/page.tsx'` 1 이상이고 목록 코드가 없으면 멈춤 · `git ls-files 'app/(app)/expenses/actions.registry.ts'` 결과를 SUMMARY에 적는다(없으면 이 플랜이 만들고 누수 스캔 import를 더한다) |
| UA-603 | `ui/pagination/Pagination.tsx` | `git ls-files ui/pagination/Pagination.tsx` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(S2 — 확인 근거 한 칸 = 지급일) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| UA-604 | `ui/table/Table`의 편집 표 · 그룹 머리글 · 칸 접기 — 선택 열은 없다(이 플랜이 더한다) | `grep -n "selectable" ui/table/Table.tsx ui/table/types.ts`가 이미 있으면 멈추고 SUMMARY에 적는다(중복 구현 방지) |
| 06-03 · 06-04 | `completeExpensePayment`(입력에 `version` · `transferKrw` · `diffReason` · `expectedPayableKrw`) · `resolveExpenseActionRow` · `lockExpenseRow` | `grep -c "completeExpensePayment\|resolveExpenseActionRow" domain/payments/index.ts domain/payments/action-row.ts` 0이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-3 | 일괄 지급의 지급일은 한 값(고른 건 전부 같은 날) | `completePaymentsBatch` 입력 모양 · S2 모달 한 칸 |
| AS1 | 건별 트랜잭션(부분 성공) | `domain/payments/batch.ts` 한 함수 |
| O-2 | (06-04 · 06-06 소관 — 이 플랜은 판정 함수를 부르기만) | 해당 없음 |
| O-1 | 지급 대상은 지출결의 메뉴의 보기(6번째 메뉴 없음) | `app/(app)/expenses/page.tsx` 보기 필터 |
| 행 수 상한 | 한 번에 처리하는 행 200 — 넘으면 거부(계획 판단) | `completePaymentsBatchAction` zod 스키마 |

<assumption_delta_decision>
`detected: false`. 06-03 · 06-04의 단건 계약을 **재사용**한다 — 단건 경로의 입력 · 판정을 바꾸지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-09 · EXP-06 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 1 · D-604 · AS1 · O-3 · R-2. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 「지급 대상」 보기에서 두 건을 골라 일괄 지급하면 한 건은 처리되고 한 건(그사이 확인 풀림)은 막혀 자리에 남는다</name>
  <files>domain/payments/targets.ts, domain/payments/batch.ts, repositories/payment-targets.ts, ui/table/Table.tsx, ui/table/types.ts, ui/table/Table.module.css, app/(app)/expenses/page.tsx, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/batch-payment-dialog.tsx, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, test/integration/payment-batch.test.ts, test/integration/leak-scan.test.ts, test/e2e/payment-batch.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 385-463행(S1 · S2 정본) · 858-866행(SP-1) · 924-936행(S1 · S2 considerations) · Copywriting 「Destructive — 일괄 지급 완료」 · 「거부 — 일괄 지급 건별 결과」 · 「표시 — 일괄 지급 결과」 · 「Empty — 지급 대상」
    - .planning/phases/06-payment-evidence-cards/06-RESEARCH.md Pattern 1(행별 게이트) · AS1
    - ui/table/Table.tsx · ui/table/types.ts · ui/table/Table.module.css(편집 표 규약)
    - domain/payments/index.ts · action-row.ts · repositories/expense-payments.ts(06-03 · 06-04)
    - A-607 재확인에서 찾은 Phase 5 목록 파일
  </read_first>
  <behavior>
    - `completePaymentsBatch(viewer, { payDate, rows })`: 행마다 자기 트랜잭션으로 `completeExpensePayment` → 결과 배열 `{ expenseId, ok | blocked(reason) }`, 한 행의 예외가 다른 행을 멈추지 않는다
    - 처리 요청의 행이 그사이 P4가 아니게 됐으면(확인 풀림 등) 그 행만 막힘 + 게이트 이유
    - 지급 권한 없음 → 전체 거부(행 처리 0)
    - `listPaymentTargets`: 결재 통과 · 지급 전만, 행마다 `selectable` · 이유 · 지급 총액 · `version`, 그룹 판정 서버
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

① **domain.** `repositories/payment-targets.ts`(결재 통과 · 지급 전 지출결의 + 지급 방식 · 예정일 · 증빙 판정 입력 · version, viewer 첫 인자). `domain/payments/targets.ts`의 `listPaymentTargets(viewer, { team })` — `expenses.payments` write 필수, 행마다 `resolveExpenseActionRow`로 선택 가능(P4) · 이유를 정하고(증빙 판정 입력은 06-06 · 06-10의 `evidenceGateInputs` — 트랜잭션 없는 읽기라 `tx` 자리에 `db`, `pre`의 `evidenceRequired`는 요청마다 한 번 읽는다), 지급 총액은 단건 경로(06-03 `loadPaymentInputs` → `decidePayable`)와 **같은 입력 · 같은 두 기준일**로 셈한다(CROSS-R1 F-4 · CHK-R1 W1): 행마다 `pickTaxDates(행, payDate 없음, today)`(today는 요청마다 한 번 정한 KST 오늘 — 부가세 기준 = 증빙일(없으면 결재 통과일), 원천징수 · 회사 대납 기준 = 예정일 > 오늘) → `decidePayable(input, { atEvidence: 캐시(증빙일), atPayment: 캐시(지급일) })`. 캐시는 요청 안에서만 사는 `Map<날짜, TaxRates>`이고 서로 다른 날짜마다 `loadTaxRates`를 한 번만 부른다. 증빙 종류 세금 규칙(코드표)과 `EVIDENCE_REQUIRED`도 요청마다 한 번 읽는다. 그래서 `repositories/payment-targets.ts` 행에 증빙 금액 · 증빙일 · 증빙 종류 · 결재 통과일을 싣는다. `listPaymentTargets(viewer, { team }, deps?)`의 `deps.loadTaxRates`는 테스트가 호출 횟수를 세는 주입 자리다(기존 `deps` 꼴 — `applyTaxRule` · `getSettingValue`와 같다). 트랜잭션 없는 읽기 경로다. 그룹(예정일 지남 → 이번 주 지급 → 다음 주 → 그 뒤 → 지급 예정일 없음, 주 = 월요일 시작 KST)은 서버가 정한다. `domain/payments/batch.ts`의 `completePaymentsBatch` — 권한 판정은 행 루프 전에 한 번(트랜잭션 없음), AS1 건별 트랜잭션, 행마다 `completeExpensePayment`를 스냅숏 값으로 부르고(사전 조회 · 잠금 · version 비교 · 게이트 재판정 · 지급일로 재계산 · `expectedPayableKrw` 비교가 전부 그 한 경로), 결과를 모은다. 이 파일은 스스로 트랜잭션을 열지 않고(행 트랜잭션 안에서 다음 행을 부르면 06-03 tx 규약 위반 · 부분 성공 붕괴), 지급 기록 INSERT를 직접 하지 않는다. DTO 등록.

② **SP-1 variant.** `ui/table`에 `selectable` 속성: 맨 왼쪽 선택 열(네이티브 체크박스, `accent-color: var(--native-accent)`, 폭 = 28 + `--cell-pad-x`×2), 행 선택 가능 여부는 행 데이터의 서버 값, 못 고르는 행은 `aria-disabled` + `aria-describedby` → 그 행의 이유 요소 id, 고른 행 면 `--accent-weak`. 선택 상태는 호출자가 행 id 집합으로 가진다. 토큰은 `tokens.css`의 것만.

③ **화면(한 경로).** `app/(app)/expenses/page.tsx`(Phase 5 목록): 지급 권한자의 기본 보기 = `view=pay`, 보기 필터에 `지급 대상` 추가. `payment-targets-table.tsx`(S1 — 이 태스크는 선택 · 1차 `지급 완료 N` · 결과 글자 · 막힌 행 이유 줄까지), `batch-payment-dialog.tsx`(S2 — `ConfirmDialog`, 확인 근거 한 칸 = 지급일 기본 오늘, 처리 요청은 모달을 연 순간의 스냅숏(R-2 — 행 id · version · 이체액 · 차이 사유 · `expectedPayableKrw`)). 이 플랜에는 이체액 편집 칸이 없으므로 스냅숏의 이체액 = 행의 지급 총액, 차이 사유 = null(편집은 06-17). `app/(app)/expenses/actions.ts`(Phase 5 것 확장, 없으면 신규)에 `completePaymentsBatchAction`(zod: 행 수 1~200) — 새 registry 파일이면 `test/integration/leak-scan.test.ts`에 import 한 줄.

④ **E2E.** `test/e2e/payment-batch.spec.ts`: 지급 대상 두 건(P4) → 둘 다 고름 → 한 건의 증빙을 domain 호출로 바꿔 확인을 풀기(06-11 무효화 — 훅은 version을 올리지 않으므로 행 잠금 뒤 게이트 재판정이 막는다, N-4. 그 재판정이 잠금 뒤 `tx`로 읽는다는 것은 06-04 통합 「잠금 뒤 게이트 재판정」이 증명하고 이 플랜의 verify가 다시 돌린다 — CROSS R-3) → 1차 → 모달 → 확인 → 한 건 빠짐 · 한 건 자리 유지 + 이유(06-06 SUMMARY의 Phase 5 PM 편집 version 동작대로 단언 — 안 올리면 게이트 문구 `증빙 확인 전 · 증빙 확인`, 올리면 동시성 문구) + 결과 글자 `지급 완료 1건 {시:분} · 막힘 1건`.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/expense-payments.test.ts test/integration/expense-payments-concurrency.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — 「지급 완료 동시 6건」(06-03)이 타임아웃이면 단건 경로가 트랜잭션 안에서 전역 `db`를 읽게 바뀌었다(06-03 tx 규약)</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 registry가 누수 스캔에 import되지 않았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다. `plant8/money-boundary` 위반이면 화면이 금액 셈을 했다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(Phase 5 목록 · registry 유무)가 SUMMARY에 있다
    - 통합 테스트가 behavior 네 줄을 단언하고, 부분 성공 케이스는 막힌 행이 있어도 처리된 행의 `expense_payments`가 커밋됐음을 DB로 확인한다
    - `grep -c "completeExpensePayment" domain/payments/batch.ts`가 1 이상이고, `domain/payments/batch.ts`가 `repositories/expense-payments.ts`의 INSERT 함수를 import하지 않음을 SUMMARY에 import 줄로 적는다
    - 06-03 tx 규약: `grep -v '^\s*//' domain/payments/batch.ts | grep -cE '\b(withTransaction|transaction)\('` 결과가 0(배치는 트랜잭션을 열지 않는다)임을 SUMMARY에 적는다
  </acceptance_criteria>
  <done>일괄 지급 한 번이 행별 잠금 · 게이트 재판정 → 건별 트랜잭션 → 부분 성공 결과 표시까지 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R-2 스냅숏 낡음 · 행 격리 · 권한 · 행 수 상한 · 그룹 판정</name>
  <files>domain/payments/targets.ts, domain/payments/batch.ts, app/(app)/expenses/actions.ts, test/integration/payment-batch.test.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 301행(「거부 — 일괄 지급 건별 결과」 동시성 문구) · 405-411행(그룹 머리글)
    - test/integration/expense-payments.test.ts(06-04 — 단건 동시성 선례)
    - domain/payments/index.ts의 `loadPaymentInputs` · `pickTaxDates` · `decidePayable`(06-03 — 목록이 같은 두 몫 rates를 쓴다) · domain/money/tax.ts의 `loadTaxRates`
  </read_first>
  <behavior>
    - 스냅숏 version ≠ 지금 version(그사이 다른 사람이 지급 · 취소 · 증빙 확인 · 면제 — 증빙 무효화는 version을 올리지 않아 Task 1 behavior 「P4가 아니게 된 행」 갈래로 막힌다, N-4) → 그 행만 막힘 + 동시성 문구(사람 · 시각은 서버 값), 다른 행은 처리
    - 한 행에서 예외(예: DB 제약 위반) → 그 행만 막힘 `처리 실패 · 새로 고침`, 앞뒤 행 커밋 유지
    - 같은 행 id가 요청에 두 번 → 한 번만 처리, 둘째는 막힘(부분 유니크 인덱스가 최후 방어선)
    - 같은 요청 재전송(ENG E-5): 두 행 일괄 요청이 성공한 뒤 **같은 요청 객체**를 다시 보내면 두 행 모두 동시성 문구로 막히고 살아 있는 `expense_payments` 수 · 문서 version이 첫 요청 뒤와 같다 — 0건 추가 지급
    - 목록 지급 총액 = 단건 재계산(CROSS-R1 F-4 · CHK-R1 W1): 부가세율 이력을 두 대역으로 두고 증빙일은 변경 전 · 예정일은 변경 뒤인 `vat_surcharge` 행 → 목록의 지급 총액이 증빙일 세율로 셈한 값이고, 그 값을 스냅숏으로 지급일 = 예정일 일괄 요청을 보내면 `PayableChangedError` 없이 처리된다
    - 세율 읽기 횟수(CHK-R1 참고 I-3): 같은 증빙일 · 예정일을 가진 행 5개 목록 → 주입한 `deps.loadTaxRates` 호출이 서로 다른 날짜 수(2)와 같고 행 수에 비례하지 않는다
    - 지급 권한 없음 → 전체 거부 / 행 201 → zod 거부
    - 그룹: 예정일 < 오늘 → `예정일 지남` / 이번 주(월요일 시작 KST) → `이번 주 지급` / 다음 주 / 그 뒤 / 예정일 없음 — KST 자정 경계 케이스 포함
  </behavior>
  <action>
`completePaymentsBatch`의 행별 결과를 06-03 · 06-04가 던지는 오류 종류(version 불일치 · 게이트 거부 · `PayableChangedError` · 부분 유니크 위반)마다 「거부 — 일괄 지급 건별 결과」 문구로 바꾸는 한 표를 둔다 — 문구 자체는 단건 경로가 만든 것을 그대로 싣고 새로 쓰지 않는다. 알 수 없는 예외는 그 행만 `처리 실패 · 새로 고침`으로 두고 서버 로그에 남긴다(다른 행은 계속). 요청 안 중복 행 id는 첫 것만 처리한다. 그룹 판정은 `domain/payments/targets.ts`의 순수 함수로 두고 KST 날짜 규약(06-03 `pickTaxDates`가 쓰는 오늘 KST 판정 — main 선례는 `domain/quotes/lines.ts`의 `timeZone: "Asia/Seoul"` `Intl` 포맷)을 쓴다 — 새 날짜 라이브러리를 들이지 않는다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/expense-payments.test.ts test/integration/evidence-invalidation.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 「목록 지급 총액 = 단건 재계산」이 빨가면 목록이 부가세를 증빙일이 아닌 날짜의 세율로 셈했고(두 몫 rates 어긋남), 「세율 읽기 횟수」가 빨가면 행마다 설정을 다시 읽는다. 「같은 요청 재전송 → 0건 추가 지급」이 빨가면 재전송이 version 비교를 지나 지급 기록을 더 만들었고(R-2 스냅숏 무력), expense-payments의 「잠금 뒤 게이트 재판정」이 빨가면 무효화된 행이 옛 게이트 입력으로 처리된다(N-4 · CROSS R-3)</fails_when>
  </verify>
  <acceptance_criteria>
    - CROSS-R1 F-4 · CHK-R1 W1: `grep -c "목록 지급 총액 = 단건 재계산" test/integration/payment-batch.test.ts`가 1이고, 세율 변경일을 사이에 둔 행의 일괄 지급이 성공함을 DB로 단언한다. 「세율 읽기 횟수」 `it`이 `deps.loadTaxRates` 호출 수 = 서로 다른 날짜 수를 단언한다
    - 통합 테스트가 behavior 여덟 줄을 개별 `it`으로 단언하고, version 낡음 케이스는 다른 커넥션에서 먼저 지급한 뒤 일괄 요청을 보내 막힘 문구에 먼저 지급한 사람 이름이 실림을 확인한다
    - ENG E-5: `grep -c "같은 요청 재전송 → 0건 추가 지급" test/integration/payment-batch.test.ts`가 1이고, 그 `it`이 재전송 전후 살아 있는 `expense_payments` 수를 DB로 세어 같음을 단언한다
    - package.json diff 0(날짜 라이브러리 추가 없음)
  </acceptance_criteria>
  <done>낡은 스냅숏 · 한 행 예외 · 중복 행이 다른 행의 처리를 막지 않고, 막힌 행마다 단건 경로와 같은 이유가 돌아온다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SP-1 선택 열 마감 · S1 · S2 상태(빈 · 로딩 · 오류 · 부분 · 0-1-다수)</name>
  <files>ui/table/Table.tsx, ui/table/types.ts, ui/table/Table.module.css, app/(app)/expenses/page.tsx, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/batch-payment-dialog.tsx, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 858-866행(SP-1 머리글 체크박스 · indeterminate) · 924-936행(S1 · S2 considerations) · Copywriting 「Empty — 지급 대상 · 필터 0건」 · 「거부 — 일괄 지급 응답 없음」 · 「Error — 지급일 · 지급 예정일 칸」
    - test/e2e/quote-table.spec.ts(편집 표 E2E 선례)
  </read_first>
  <behavior>
    - 머리글 체크박스: 이 쪽의 고를 수 있는 행 전체 토글 · 일부만 골랐으면 `indeterminate` · 못 고르는 행은 건드리지 않음
    - 0건 · 필터 0건 → Empty 두 갈래(합계 줄 · 선택 열 · 1차 없음)
    - 고른 0건 → 1차 비활성 + `고른 건 없음`
    - 모달: 지급일 칸 오류 → 모달 유지 / 처리 응답 없음 → 모달 유지 + 문구 / 0건 처리 → 토스트 없음
  </behavior>
  <action>
`ui/table` `selectable`에 머리글 체크박스(이 쪽의 고를 수 있는 행 전체, 섞이면 `indeterminate` — DOM 속성으로), 막힌 행의 이유 요소 id 연결을 마감한다. 기존 편집 표 · 그룹 머리글 · 칸 접기 동작은 그대로 둔다(`selectable`이 없으면 이전과 같은 DOM). S1: 빈 화면 두 갈래(빈 화면에는 설명 대신 필터 초기화 3차만 — 「Empty — 지급 대상 · 필터 0건」 문구), 첫 로드 §7-7 LOADING 뼈대, 처리 중 1차 `지급 완료 N…` + `aria-disabled`, 로드 실패 「Error — 목록 로드」. S2: 지급일 칸 검증 오류는 모달 안 칸 오류, 처리 응답 없음은 모달 유지 + 「거부 — 일괄 지급 응답 없음」, 1건 이상 처리면 토스트 `지급 완료 · N건`. 1차 왼쪽 결과 글자와 막힌 행 이유는 다음 처리 시도 · 필터 변경 때 지운다. 토큰은 `tokens.css`의 것만.

E2E에 behavior 네 줄 케이스와 「`selectable` 없는 기존 편집 표(견적 줄 표) DOM에 선택 열이 없음」을 더한다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/quote-table.spec.ts test/e2e/keyboard-nav.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. quote-table · keyboard-nav가 빨가면 `ui/table` 변경이 기존 편집 표를 깨뜨렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 머리글 체크박스의 `indeterminate` DOM 속성과 막힌 행 체크박스의 `aria-describedby` → 이유 요소 텍스트를 단언한다
    - E2E가 Empty 두 갈래에서 선택 열 · 합계 줄 · 1차 요소 수 0을 단언한다
    - 기존 quote-table · keyboard-nav 스펙 녹색
  </acceptance_criteria>
  <done>선택 열과 S1 · S2의 모든 상태가 계약대로 서고, 기존 편집 표는 바뀌지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 일괄 지급 액션 | 행 id · version · 이체액 · 차이 사유 · `expectedPayableKrw` · 지급일이 넘어온다. 행의 선택 가능 여부는 넘어와도 믿지 않는다 |
| domain/payments/batch → 단건 경로 | 행마다 별도 트랜잭션 — 잠금 · 게이트 · version이 단건 경로 안에 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-72 | Elevation of Privilege | 행별 게이트 우회 | high | mitigate | `completePaymentsBatch`가 행마다 단건 경로를 불러 `lockExpenseRow` 뒤 게이트를 서버에서 다시 판정한다. 선택 불가 행을 실은 조작 요청 거부 통합 테스트 |
| T-06-73 | Tampering | 낡은 스냅숏(R-2) | high | mitigate | 스냅숏 version을 잠근 행의 version과 비교해 다르면 처리하지 않는다. 다른 커넥션 선지급 통합 테스트 |
| T-06-74 | Tampering | 한 요청 안 중복 행 · 동시 두 요청 · 같은 요청 재전송 | high | mitigate | 요청 안 중복 제거 + 행 잠금 + 부분 유니크 인덱스(06-03) + 스냅숏 version(R-2). 통합 테스트(「같은 요청 재전송 → 0건 추가 지급」 포함, ENG E-5) |
| T-06-74a | Denial of Service | 일괄 지급의 풀 소진(ENG E-2 · CROSS E-1) | high | mitigate | 배치는 트랜잭션을 열지 않고 권한은 루프 전 한 번, 행마다 단건 경로의 사전 조회 → 트랜잭션(06-03 tx 규약). 06-03 「지급 완료 동시 6건」 재실행 · batch.ts 트랜잭션 0건 grep |
| T-06-75 | Denial of Service | 일괄 처리 요청 크기 | medium | mitigate | zod 행 수 상한 200 + 건별 트랜잭션으로 한 요청의 잠금 범위를 행 하나로 제한 |
| T-06-76 | Repudiation | 일괄 처리 | medium | mitigate | 행마다 `payment_process` 로그(단건 경로 그대로) |
| T-06-77 | Information Disclosure | 지급 대상 목록 DTO | medium | mitigate | `expenses.payments` write 필수 · DTO 등록 → 누수 스캔 녹색. 계좌 칸은 이 플랜에 없다(06-20) |
</threat_model>

<verification>
- 통합 payment-batch(「같은 요청 재전송 → 0건 추가 지급」 포함) · expense-payments(「잠금 뒤 게이트 재판정」 포함) · expense-payments-concurrency(「지급 완료 동시 6건」, 06-03) · evidence-invalidation · leak-scan, 단위 leak-scan-coverage 녹색
- E2E payment-batch · quote-table · keyboard-nav가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S1 · S2 DOM 실측 감사 → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 경영관리가 여러 건을 한 번에 지급 완료하고, 막힌 건만 이유와 함께 남는다
- 모달을 연 뒤 바뀐 문서는 조용히 처리되지 않는다(R-2)
- 일괄 처리가 단건 경로 하나만 지나므로 게이트 · 잠금 · 재계산이 두 벌이 아니다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `listPaymentTargets` · `completePaymentsBatch` |
| 리포지토리 | `repositories/payment-targets.ts` |
| Server Action | `completePaymentsBatchAction` |
| 컴포넌트 | `ui/table` `selectable` variant |
| 화면 | `/expenses?view=pay`(S1) · 일괄 지급 모달(S2) |
| 테스트 | `test/integration/payment-batch.test.ts`(「같은 요청 재전송 → 0건 추가 지급」 포함) · `test/e2e/payment-batch.spec.ts` |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG E-2 일괄 지급(AS1 건별 트랜잭션)이 세율 재계산 경로를 반복한다(eng-review.md L32@333f7bf) | 고침 | must_haves 「06-03 tx 규약」 줄 · Task 1 ①: 배치는 트랜잭션을 열지 않고 권한은 루프 전 한 번, 행마다 단건 경로(사전 조회 → 트랜잭션), 목록 지급 총액은 트랜잭션 없는 `loadTaxRates` → `decidePayable`. acceptance에 batch.ts 트랜잭션 0건 grep · T-06-74a |
| CROSS E-1 지급 완료 교착 — 06-15(일괄 → 단건 재사용)가 경로를 물려받음(eng-cross-opus.md L13@333f7bf) | 막음 | Task 1 verify에 06-03 「지급 완료 동시 6건」 재실행 |
| ENG E-3 tx 규약 참조(eng-review.md L37@333f7bf) | 고침 | must_haves 「06-03 tx 규약」 한 줄 |
| ENG E-5 일괄 지급 멱등성 — 테스트 이름 명시(eng-review.md L55@333f7bf) | 참고 | must_haves 「ENG E-5 멱등성」 · Task 2 behavior · 통합 `it` 「같은 요청 재전송 → 0건 추가 지급」(acceptance grep = 1, DB로 살아 있는 기록 수 비교) · T-06-74 |
| CROSS R-3 스냅숏과 무효화 — 무효화 훅이 version을 올리지 않아 잠금 뒤 게이트 재판정에만 기댐(eng-cross-opus.md L37@333f7bf, 대상 06-15:171) | 참고 | 확인 한 줄은 검토 권고대로 06-04 acceptance(「잠금 뒤 게이트 재판정」)에 두었고, 이 플랜은 Task 1 ④에 그 근거를 적고 Task 1 · 2 verify가 expense-payments를 다시 돌린다. 06-11 훅은 그대로(N-4) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 2 — 333f7bf(+미커밋 반영분 · /mnt/project-files/06-gates/eng-apply-cross-r1.md · eng-apply-checker-r1.md)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS-R1 F-4 · CHK-R1 W1 `listPaymentTargets`가 두 몫 rates를 채울 방법 미정 — 예정일 하나로 읽으면 부가세 기준일 어긋남 → 일괄 지급 `PayableChangedError` | 고침(경고) | must_haves 두 줄(tx 규약 줄 갱신 + 「목록 지급 총액 = 단건 재계산」) · Task 1 ①: 행마다 `pickTaxDates` → `decidePayable(input, { atEvidence, atPayment })`, 행에 증빙 금액 · 증빙일 · 증빙 종류 · 결재 통과일 · Task 2 behavior · acceptance · fails_when |
| CHK-R1 참고 I-3 목록이 행마다 세율을 다시 읽는다(N+1 설정 읽기) | 참고 | Task 1 ①: 요청 안 `Map<날짜, TaxRates>` 캐시(서로 다른 날짜마다 `loadTaxRates` 한 번), 세금 규칙 · `EVIDENCE_REQUIRED`도 요청마다 한 번. `deps.loadTaxRates` 주입으로 「세율 읽기 횟수」 `it` |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-15-SUMMARY.md` when done
</output>
