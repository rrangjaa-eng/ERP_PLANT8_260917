---
phase: 06-payment-evidence-cards
plan: 16
type: execute
wave: 8
depends_on: ["06-11", "06-12"]
files_modified:
  - db/schema/files.ts
  - db/migrations/meta/_journal.json
  - domain/evidence-attachments/index.ts
  - domain/expenses/evidence.ts
  - domain/quotes/revisions.ts
  - domain/reserves/index.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/[id]/revision-section.tsx
  - app/(app)/pnl/reserves/actions.ts
  - app/(app)/pnl/reserves/actions.registry.ts
  - app/(app)/pnl/reserves/reserves-table.tsx
  - test/integration/approval-reserve-evidence.test.ts
  - test/e2e/approval-reserve-evidence.spec.ts
  - test/e2e/quote-revisions.spec.ts
autonomous: true
requirements: [EVID-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**D-56:** 상세 견적 차수의 고객 승인 표시가 켜져 있으면, 그 표시를 켤 수 있는 사람(담당 PM + `projects` 쓰기 — Phase 4 `quote.approval-toggle`과 같은 판정)이 차수 섹션에서 승인 증빙 파일(이미지 · PDF)을 붙이고 뗄 수 있다. 첨부는 선택이고 어떤 게이트도 이 첨부 유무를 보지 않는다"
    - "[D-56 계획 판단] 승인 표시가 꺼지면(연결 문서 없을 때만 가능 — Phase 4) 그 차수의 승인 증빙 파일은 지우지 않고 읽기 전용 목록으로 남으며, 다시 켜면 그대로 편집할 수 있다. 완료 프로젝트에서는 읽기 전용이다"
    - "[D-56 계획 판단] 승인 증빙 파일 목록 · 내려받기는 그 차수의 견적 합계를 볼 수 있는 사람(`quote.amount` 노출)에게만 실린다 — 승인 버튼과 같은 노출 기준"
    - "**D-60:** 리저브 대장 줄마다 증빙 파일을 붙이고 뗄 수 있다 — 쓰기는 리저브 쓰기(`pnl` 쓰기 + `reserve.amount` 노출, Phase 4 04-07과 같은 판정), 목록 · 내려받기는 리저브 읽기(`pnl` 보기 + `reserve.amount`)에게만. 보관된 줄은 읽기 전용이고 파일은 줄과 함께 남는다(복원하면 그대로)"
    - "두 새 주인의 업로드는 Phase 5 업로드 경로(서명 URL · 완료 통보 · 파일 표)와 06-11의 `checkEvidenceUpload`(크기 · 형식 · 중복)를 그대로 지난다 — 새 저장 경로를 만들지 않는다. 중복 막힘은 **같은 주인 종류 안에서만** 걸리고(차수 승인 증빙끼리 · 리저브 줄 증빙끼리 — DR-3 · UI-SPEC S7 · O-6 rev 9, 지출결의 증빙이나 다른 종류 주인에 같은 파일은 막지 않는다) 주인이 지출결의가 아니므로 문서 번호 없이 `이미 첨부된 파일 · 다른 파일 고르기`이며, 마지막 파일 삭제에 모달이 없다(`lastEvidenceDeleteNeedsConfirm` = false)"
    - "두 새 주인의 증빙에는 금액 칸이 없고 비용 · 지급 · 잔액 계산을 바꾸지 않는다(EVID-03 비용 규칙 불변 — 이 플랜은 첨부 주인만 넓힌다)"
    - "주인 권리는 서버가 `ownerKind` · `ownerId`로 주인 행을 다시 읽어 판정한다 — 브라우저가 보낸 권리 · 프로젝트 id를 믿지 않는다"
    - "**B-2:** Phase 5 파일 표의 주인 제약을 넓히는 마이그레이션은 머리에 SET LOCAL 한 쌍, 새 CHECK · FK를 `NOT VALID`로 걸고 VALIDATE는 `pnpm db:generate --custom`으로 만든 별도 마이그레이션에서 한다 — `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate`가 `No schema changes`"
    - "**UI-SPEC S21 · S22(rev 9 — DR-1 · DR-2):** 화면은 06-UI-SPEC S21 「차수 승인 증빙(D-56)」 · S22 「리저브 줄 증빙(D-60)」 두 절대로만 선다 — 04 S5 차수 섹션 동작 칸의 3차 · 「차수 열기」 펼침 규칙, 04 S9 편집 표의 읽기 셀 열 하나 · 표 아래 펼침, §7-10 파일 행 · 첨부 영역(Phase 5 첨부 컴포넌트 — A-608 · UA-606), 폰 행 시트의 §7-8 첨부 행만 쓰고 새 시각 패턴(색 · 서체 · radius · 새 컴포넌트 · 새 레이아웃 꼴)을 만들지 않는다. 리저브 행 시트(§7-3 (바) 보기 전용)에는 업로드를 두지 않는다. 실행 시작 때 Task 1 ⓪이 `grep -cw -e \"D-56\" -e \"D-60\"`(rev 9 = 12)로 두 절이 있는지 확인한다"
    - "06-03 tx 규약을 따른다 — 이 플랜의 트랜잭션은 첨부 권리 판정이 도는 차수 · 리저브 쓰기 경로다"
    - "**04 S5 개정(디자인 반영 r2 F-3 — UI-SPEC S21 「04 S5 개정」 ⒜ ⒝ ⒞):** ⒜ 승인된 최신 차수 행의 동작 칸에는 `quote.amount` 노출자에게 3차 `승인 증빙 {N}` 하나가 서고, 04 「현재 차수 행 동작 없음」 · 「차수 하나면 동작 칸 빈 칸」은 승인 안 된 최신 차수에만 남는다. ⒝ 이전 차수 읽기 섹션(「차수 열기」 펼침)에서 쓰기는 승인 증빙 블록에만 있고, 캡션 `상세 견적 {n}차 견적 줄` 표는 04 그대로 편집 · 저장 · 구조 컨트롤이 없다. ⒞ 「차수 열기」는 04 그대로 `차수 닫기`로 토글되고, `승인 증빙 {N}`은 라벨이 그대로이며 열림은 `aria-expanded`로만 말한다. 이 세 문장에 걸리는 Phase 4 E2E(`test/e2e/quote-revisions.spec.ts`) 단언은 이 플랜이 세 문장대로 고치고, 04 계약의 나머지 단언은 바꾸지 않는다 — 그 스펙이 CI=true로 녹색"
    - "S21 empty: 「Empty — 차수 승인 증빙(S21, 펼침 섹션)」 — 쓰기 권리면 첨부 영역만, 읽기 · 파일 0이면 `승인 증빙 없음 · 올리기는 담당 PM {이름}`. 승인 표시 취소 뒤 파일 0이면 3차 자체가 없다"
    - "S21 loading: 펼침은 04 S5 「차수 열기」와 같은 규칙(이 페이즈 새 진행 표시 없음). 업로드 = §7-10 행 아래 2px 진행 바, 삭제 = 누른 3차 `…`. 즉시 저장이라 상세의 `일괄 저장 Ctrl+S N`에 들지 않는다"
    - "S21 error: 「Error — 증빙 업로드」 — 중복은 늘 번호 없는 문구(같은 주인 종류 = 차수 승인 증빙 안에서만 막힘, O-6). 권리 밖 직접 호출은 서버가 거부한다"
    - "S21 populated: 승인된 최신 차수 행 동작 칸 3차 `승인 증빙 {N}` → 차수 섹션 아래 펼침 제목 `상세 견적 {n}차 승인 증빙` + 부제 `승인 {승인일}` · §7-10 파일 행(쓰기면 3차 `삭제`) + 첨부 영역. 성공 토스트 없음. 승인 직후 3차가 저절로 서므로 안내 문구 · 자동 펼침 없음(DR-12)"
    - "S21 partial: 이전 차수 = 「차수 열기」 펼침 제목 아래 같은 블록(동작 칸 3차 하나). 승인 표시 취소 뒤 = 파일이 있을 때만 3차가 남고 펼치면 읽기. 읽기 권리 = 파일 행만(`크게 보기`). `quote.amount` 없는 사람 = 3차 · 블록 · 개수가 DOM에 없다"
    - "S21 zero-one-many: 3차 라벨 0 = `승인 증빙`(숫자 없음) · 1+ = `승인 증빙 {N}`, 접근 이름 `{n}차 승인 증빙 {N}`(예 `2차 승인 증빙 1`). 펼침은 한 번에 하나(「차수 열기」와 서로 바뀐다), 열리면 포커스 = 섹션 제목(`tabindex=\"-1\"`), 닫히면 그 3차"
    - "S22 empty: 「Empty — 리저브 줄 증빙(S22, 펼침 섹션)」 — 쓰기면 첨부 영역만, 읽기 · 파일 0이면 `증빙 없음`(`--muted`, 담당 표기 없음). 열 값 0 = 3차 `증빙`(숫자 없음), 저장 전 새 줄 = `—`(3차 없음)"
    - "S22 loading: 업로드 = §7-10 행 아래 2px 진행 바, 삭제 = 누른 3차 `…`. 즉시 저장이라 표의 dirty `N`에 들지 않고 잔액 · 금액을 바꾸지 않는다"
    - "S22 error: 「Error — 증빙 업로드」(번호 없는 중복 문구). 권리 밖 직접 호출 = 그 행 오류 자리 `리저브 쓰기 권한 없음`(04-07 `denyWrite`)"
    - "S22 populated: 세금계산서 번호 다음 읽기 셀 열 `증빙`(P2) 3차 `증빙 {N}` → 표 블록 아래(페이지 줄 · 힌트 줄 다음) 펼침 제목 `{날짜} {클라이언트} {구분} {금액} 증빙`(예 `09-18 현대자동차 출금 3,300,000 증빙`) · 파일 행 + 첨부 영역. 성공 토스트 없음. 쪽을 넘기면 펼침이 닫힌다"
    - "S22 partial: 폭 1024~1279 쓰기 · 700~1023 파일 행만(첨부 영역 없음) · 폰(<700) 읽기만 — 접힌 줄 `증빙 2` 글자(버튼 아님) + 행 시트 접힌 값 목록의 §7-8 첨부 행(썸네일 72×96 · 파일명 · 장수 · 용량 · 3차 `크게 보기`)(O-22 — 사용자 확정(2026-09-25)). 보관된 줄 = 읽기. 줄 삭제 = 04 S16 모달 결과 줄 `증빙 {N}개 함께 보관`(N ≥ 1, 「Destructive — 리저브 줄 삭제」)"
    - "S22 zero-one-many: 0 = `증빙` · 1+ = `증빙 {N}`, 접근 이름 `{날짜} {클라이언트} 증빙 {N}`(예 `09-18 현대자동차 증빙 2`). 펼침은 한 번에 하나(다른 줄 3차를 누르면 바뀐다). 편집 표 키에서 이 셀 `Enter` = 펼치기(링크 셀과 같은 규칙)"
    - statement: "S21 overflow: 파일 10개 차수에서 파일 행이 펼침 섹션 안에 페이지 없이 전부 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S21 long-text: 80자 파일명이 §7-10 파일 행에서 말줄임 + `title`로 서고 `삭제` 3차를 밀어내지 않는다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S22 overflow: 파일 10개 줄에서 표 아래 펼침 섹션의 파일 행이 전부 서고, 쪽을 넘기면 펼침이 닫힌다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S22 long-text: 80자 파일명 · 긴 클라이언트 이름이 펼침 제목 줄 · 파일 행에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "승인 표시를 켤 수 없는 사람이나 리저브를 쓸 수 없는 사람이 서버 액션을 직접 불러 파일을 붙이거나 뗄 수 있어서는 안 된다"
      status: resolved
      verification: test
    - statement: "두 새 주인의 증빙 파일이 그 주인을 읽을 수 없는 사람(견적 금액 · 리저브 금액 노출이 꺼진 계급)의 응답 · DOM에 실려서는 안 된다"
      status: resolved
      verification: test
    - statement: "D-56 · D-60 화면이 UI-SPEC에 없는 새 시각 패턴을 만들어서는 안 된다"
      status: resolved
      verification: judgment
    - statement: "리저브 행 시트(§7-3 (바) 보기 전용)나 1024 미만 폭에 리저브 증빙 업로드 · 삭제 요소가 있어서는 안 된다(S22 · O-22 기본값)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/evidence-attachments/index.ts"
      provides: "첨부 주인 종류(`expense` · `quote_revision` · `reserve_entry`)별 권리 판정 한 곳 + Phase 5 업로드 경로 호출"
      exports: ["attachmentOwnerRights", "ATTACHMENT_OWNER_KINDS"]
    - path: "test/integration/approval-reserve-evidence.test.ts"
      provides: "두 새 주인의 권리 · 표시 꺼짐 · 보관 · 노출 · 중복 증명"
      min_lines: 50
  key_links:
    - from: "domain/evidence-attachments/index.ts"
      to: "domain/quotes/revisions.ts"
      via: "승인 증빙 쓰기 권리가 Phase 4 승인 표시 권리와 같은 판정을 부른다"
      pattern: "attachmentOwnerRights"
    - from: "domain/evidence-attachments/index.ts"
      to: "domain/reserves/index.ts"
      via: "리저브 증빙 권리가 Phase 4 리저브 쓰기 · 읽기 판정을 부른다"
      pattern: "reserve.amount"
    - from: "domain/expenses/evidence.ts"
      to: "domain/evidence-attachments/index.ts"
      via: "Phase 5 업로드 의도가 주인 종류별 권리를 한 곳에서 받는다"
      pattern: "ATTACHMENT_OWNER_KINDS"
---

<objective>
Phase 4에서 넘어온 두 증빙 첨부를 세운다 — **D-56** 차수 고객 승인 증빙, **D-60** 리저브 줄 증빙(06-CONTEXT.md:11). Phase 5 업로드 경로 · 06-11 업로드 검사 · 기존 첨부 컴포넌트를 재사용하고 주인 종류만 넓힌다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 체커 경고 · 교차 검토 B-5 — D-56 · D-60이 어느 플랜에도 없었다. 오케스트레이터 결정대로 이 페이즈에서 계획한다.

**요구사항 표기:** D-56 · D-60은 요구사항 ID가 없는 Phase 4 이월 결정(PROJ-07 · RSV-01은 Phase 4 소관)이다. frontmatter `requirements`는 비울 수 없어 가장 가까운 EVID-03(증빙이 기록되는 규약)을 적었다 — 이 플랜은 EVID-03의 비용 규칙을 바꾸지 않는다.

Output: 파일 표 주인 제약 확장 마이그레이션 2개(생성 1 + `--custom` VALIDATE 1), `domain/evidence-attachments`, UI-SPEC S21 차수 승인 증빙(동작 칸 3차 `승인 증빙 {N}` + 펼침), UI-SPEC S22 리저브 줄 증빙(`증빙` 열 P2 + 표 아래 펼침 + 폰 행 시트 첨부 행 + 줄 삭제 모달 결과 줄), 통합 1 · E2E 1.

**규모 근거:** `files_modified` 15개(디자인 반영 r2 F-3이 Phase 4 E2E `test/e2e/quote-revisions.spec.ts` 단언 조정을 더했다 — 04 S5 개정 ⒜ ⒝가 그 단언을 깨므로 같은 플랜에서 고친다. 체커 15개 문턱에 닿는다 — 06-03 · 06-07 선례처럼 보고하고 쪼개지 않는다), `estimate.tokens` 90000(`confidence: low`, 표본 0 — 보정 계수 1). 재분할 방아쇠: ⓪에서 차수 섹션이 차수 DTO를 통째로 받지 않아 `app/(app)/projects/[id]/page.tsx` 전달이 필요하면(16번째 파일) 코드를 바꾸지 않고 멈추고 `06-16 파일 한도 — page.tsx 전달 필요`를 보고한다(오케스트레이터가 D-60을 따로 떼거나 page.tsx를 더한다). 같은 방식으로, S22가 고치는 04 S16 `리저브 줄 삭제` 확인 모달이나 리저브 폰 행 시트가 `reserves-table.tsx` 밖 파일에 있으면 멈추고 `06-16 파일 한도 — {그 파일} 필요`를 보고한다. ⓪에서 파일 표가 ⒝(주인별 FK 칸)이면 06-11 중복 조회 리포지토리의 종류 → FK 칸 대응도 고쳐야 해 16번째 파일이다 — 같은 방식으로 멈추고 보고하며, 이 경우 재분할을 예상한다(r3-2).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/04-project-quote-ledger/04-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-16 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

**실행 전 선행 조건(N-1) — rev 9로 채워짐(DR-1 · DR-2):** `/plan-design-review`가 06-UI-SPEC rev 9에 S21 「차수 승인 증빙(D-56)」 · S22 「리저브 줄 증빙(D-60)」을 더했다(`grep -cw -e "D-56" -e "D-60"` 결과가 rev 8에서 0건, rev 9에서 12건). 이 플랜은 두 절을 그대로 짓고 화면을 지어내지 않는다. 아래 표의 「UI-SPEC D-56 · D-60 표면」 줄은 실행 시작 때 확인만 한다(되돌려진 판을 받았을 때의 멈춤).

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| **A-614** (Phase 4) | 차수 고객 승인 표시 — `domain/quotes/revisions.ts`의 승인 표시 · 취소(`quote.approval-toggle`, 04-14)와 차수 섹션 화면 `revision-section.tsx` · 그 E2E `test/e2e/quote-revisions.spec.ts`(04-24) | `grep -c "approval-toggle" domain/quotes/revisions.ts domain/rules/register.ts` 합이 0이거나 `git ls-files 'app/(app)/projects/[[]id]/revision-section.tsx' test/e2e/quote-revisions.spec.ts` 출력에 두 경로 중 하나라도 없으면 멈춤(r2 F-3 — 04 S5 개정 단언을 고칠 스펙) |
| **A-615** (Phase 4) | 리저브 대장 RSV-01 — `domain/reserves/index.ts`(04-07, 쓰기 = `pnl` 쓰기 + `reserve.amount`)와 `/pnl/reserves` 표(04-42) | `git ls-files domain/reserves/index.ts 'app/(app)/pnl/reserves/reserves-table.tsx' 'app/(app)/pnl/reserves/actions.registry.ts'` 셋 중 하나라도 비면 멈춤 |
| **UI-SPEC D-56 · D-60 표면** | 06-UI-SPEC rev 9 S21 · S22(DR-1 · DR-2 — 위 실행 전 선행 조건) | 설계 검토로 고쳐진 06-UI-SPEC.md가 채운다 — 실행 시작 때 `grep -cw -e "D-56" -e "D-60" .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md`가 0이면 멈추고 `선행 의존 UI-SPEC D-56·D-60 미해소 — 설계 표면 없음, 06-16 착수하지 않음`을 출력한다(이 플랜은 UI-SPEC을 고치지 않는다) |
| A-608 · A-608-T | Phase 5 업로드 경로 · 테스트 모드(06-11 ⓪이 찾은 경로) | `grep -n "A-608-T" .planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md` 0건이면 멈춤 |
| UA-606 · A-608 | Phase 5 첨부 컴포넌트(가칭 `ui/attachments/Attachments.tsx` — Phase 5가 만들고 06-11 Task 3이 고친다, 이 플랜은 만들지 않는다)가 업로드 · 삭제 동작을 prop으로 받고, 그 prop 없이 넘기면 파일 행만 서는 읽기로 그려진다(`ui/`는 `app/` 액션을 import할 수 없다) | 06-11 SUMMARY ⓪이 적은 실제 경로를 `git ls-files`로 찾아 **비면 멈춤**(파일이 없으면 아래 grep이 0건으로 조용히 지나가므로 먼저 본다) · 그 파일에서 `grep -n "@/app/\|@/domain/expenses"`가 1건 이상이면(지출결의에 묶임) 멈춤 · 업로드 · 삭제 prop 없이 읽기로 그려지지 않으면 멈추고 SUMMARY에 적는다(06-11 Task 3 소관 보강 — 이 플랜이 컴포넌트를 고치지 않는다) |
| 06-11 | `checkEvidenceUpload`(주인 일반) · `lastEvidenceDeleteNeedsConfirm` | `grep -c "ownerKind" domain/evidence-reviews/upload-checks.ts` 0이면 멈춤 |
| 차수 DTO 전달 | `revision-section.tsx`가 차수 요약 DTO 객체를 prop으로 받는다 | 칸별 prop이면 「규모 근거」의 재분할 방아쇠 |
| 리저브 표 자리(S22) | `reserves-table.tsx`가 04 S16 `리저브 줄 삭제` 확인 모달과 폰 행 시트를 그 파일 안에서 연다 | 다른 파일이면 「규모 근거」의 재분할 방아쇠 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| D-56 첨부 시점 | 승인 표시가 켜져 있을 때만 첨부 · 삭제, 꺼지면 파일은 남고 읽기 전용(계획 판단) | `attachmentOwnerRights`의 `quote_revision` 갈래 |
| D-56 노출 | 승인 증빙 목록 · 내려받기는 `quote.amount` 노출자에게만(계획 판단 — 승인 버튼과 같은 기준) | `domain/quotes/revisions.ts` 차수 DTO 투영 |
| D-60 보관 | 보관된 리저브 줄의 파일은 줄과 함께 남고 읽기 전용(계획 판단) | `attachmentOwnerRights`의 `reserve_entry` 갈래 |
| D-56 · D-60 중복 범위(N-7 → DR-3 · O-6 rev 9) | 같은 파일(SHA-256 같음)은 **같은 주인 종류 안에서만** 막는다 — 차수 승인 증빙끼리 · 리저브 줄 증빙끼리(지출결의 증빙끼리는 06-11 그대로). 다른 종류 주인(지출결의 · 차수 승인 · 리저브)에는 같은 파일을 붙일 수 있다 — 한 세금계산서가 지출결의 증빙이면서 리저브 출금 증빙인 것은 정상 업무이고, 두 새 주인에는 금액 칸이 없어 비용이 두 번 들지 않는다. **사용자 확정(2026-09-25, DR-3 = 허용)** | 06-11 `checkEvidenceUpload`의 주인 종류 범위 조건(06-11 Task 2 소관 — 이 플랜 `files_modified`에 더하지 않는다) · Task 2 · Task 3 behavior `[DR-3]` 줄 |
| O-22 리저브 증빙 폰 업로드(DR-2) | 올리지 않는다 — 쓰기는 ≥1024(1024~1279 포함), 700~1023은 펼침에 파일 행만, 폰(<700)은 행 시트의 §7-8 첨부 행으로 읽기만(리저브 표가 1024 미만 보기 전용이고 행 시트가 보기 전용 계약). **사용자 확정(2026-09-25, DR-2 = 아니오)** | `reserves-table.tsx`의 폭 갈래 · Task 3 behavior 폭 줄 · E2E 폭 케이스 |

<assumption_delta_decision>
`detected: false`. Phase 5 첨부 계약에 주인 종류 둘을 **더한다** — 지출결의 주인의 동작은 바꾸지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EVID-03(표기만 — 위 「요구사항 표기」) — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 06-CONTEXT.md:11 · D-56 · D-60(04-CONTEXT). 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 승인된 차수에 담당 PM이 승인 증빙 PDF 한 장을 붙이면 차수 섹션에 선다</name>
  <files>db/schema/files.ts, db/migrations/meta/_journal.json, domain/evidence-attachments/index.ts, domain/expenses/evidence.ts, domain/quotes/revisions.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/[id]/revision-section.tsx, test/integration/approval-reserve-evidence.test.ts, test/e2e/approval-reserve-evidence.spec.ts, test/e2e/quote-revisions.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있고 UI-SPEC에 D-56 · D-60 표면이 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S21 「차수 승인 증빙(D-56)」(와이어프레임 · 자리 · 펼침 · 내용 · 저장 · 오류 · 노출 · 폭) · UI Considerations S21 행 · Copywriting 「3차 버튼」(`승인 증빙 {N}`) · 「Empty — 차수 승인 증빙(S21, 펼침 섹션)」 · 「Error — 증빙 업로드(S7 · S21 · S22, §7-10 행 오류 모양)」
    - .planning/phases/04-project-quote-ledger/04-UI-SPEC.md S5 차수 섹션(동작 칸 · 「차수 열기」 펼침 — 한 번에 하나 · 제목 포커스 · 닫히면 버튼으로)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S21 머리 「04 S5 개정」 ⒜ ⒝ ⒞(r2 F-3 — 이 플랜이 Phase 4 S5 계약을 바꾸는 곳 셋과 04 E2E 단언 조정)
    - test/e2e/quote-revisions.spec.ts(04-24 — 승인된 최신 차수 행의 동작 칸이 비었다는 단언과 이전 차수 읽기 섹션에 편집 입력 · 저장 버튼이 없다는 단언을 `grep -n "동작\|차수 열기\|input\|저장"`으로 찾는다. 행 번호에 기대지 않는다)
    - 06-11 SUMMARY ⓪의 Phase 5 첨부 컴포넌트 실제 경로와 prop(업로드 · 삭제 동작 prop, 읽기)
    - .planning/phases/04-project-quote-ledger/04-CONTEXT.md D-56(45행) · D-60(52행)
    - domain/quotes/revisions.ts(승인 표시 · 취소 · `loadProjectForGate` 잠금) · app/(app)/projects/[id]/revision-section.tsx
    - db/schema/files.ts(Phase 5 — 주인 칸 모양) · 06-11 ⓪이 찾은 업로드 의도 · 완료 통보 · 삭제 함수(`domain/expenses/evidence.ts`)
    - db/migrations/0003_permissions_masters_spine.sql 80-90행 · db/migrations/0004_users_role_id_validate.sql · 06-10 SUMMARY(B-2 첫 적용)
  </read_first>
  <behavior>
    - `attachmentOwnerRights(viewer, { ownerKind: "quote_revision", ownerId }, tx)`: 담당 PM + `projects` 쓰기 + 표시 켜짐 + 완료 아님 → `{ read: true, write: true }` / 표시 꺼짐 → write false / `quote.amount` 노출 없음 → read false · write false
    - `ownerKind: "expense"` → Phase 5 판정 그대로(회귀 없음)
    - 업로드 의도(`quote_revision`) → 권리 → `checkEvidenceUpload` → 서명 URL / 권리 없음 → 거부, 서명 URL 없음
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. UI-SPEC 표면 조건은 실행 전 `/plan-design-review`가 고친 06-UI-SPEC.md로 채워져 있어야 한다(N-1 — 「선행 의존」의 실행 전 선행 조건). 실행 시작 때 `grep -cw -e "D-56" -e "D-60" .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md`가 여전히 0이면 코드를 한 줄도 바꾸지 않고 멈추고, `선행 의존 UI-SPEC D-56·D-60 미해소 — 설계 표면 없음, 06-16 착수하지 않음`을 오케스트레이터에 보고한다 — 자리를 스스로 지어 UI-SPEC이나 화면을 채우지 않는다. 결과(파일 표 주인 칸 모양 · 업로드 함수 이름 · UI-SPEC 줄 번호 · `grep -c "\.skip\|\.fixme" test/e2e/quote-revisions.spec.ts` 값 — r2 F-3)를 SUMMARY에 적는다.

① **마이그레이션(B-2 — Phase 5 표).** 파일 표 주인 모양에 따라 한 갈래만: ⒜ `owner_kind` 칸 + CHECK면 CHECK 값 목록에 `quote_revision` · `reserve_entry`를 더한 새 CHECK로 바꾼다(옛 CHECK 삭제 + 새 CHECK `NOT VALID`) ⒝ 주인별 FK 칸이면 `quote_revision_id` → `quote_revisions` · `reserve_entry_id` → `reserve_entries` nullable FK를 `NOT VALID`로 더하고 「주인 하나」 CHECK를 새 칸 포함으로 바꾼다(`NOT VALID`) + 새 FK 칸 인덱스. 절차는 06-10과 같다: `pnpm db:generate --name attachment_owner_kinds` → 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` → 제약 끝 `NOT VALID` + 0003처럼 이유 주석 → `pnpm db:generate --custom --name attachment_owner_kinds_validate`에 SET LOCAL 한 쌍 + `VALIDATE CONSTRAINT` → 인덱스는 트랜잭션 안 일반 `CREATE INDEX` → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 generate `No schema changes`.

② **domain.** `domain/evidence-attachments/index.ts`(신규): `ATTACHMENT_OWNER_KINDS`와 `attachmentOwnerRights` — `expense`는 Phase 5 판정 함수를 그대로 부르고, `quote_revision`은 Phase 4 승인 표시 권리(04-14의 실제 판정 함수 — 새로 쓰지 않는다) + 표시 켜짐 + 완료 아님 + `quote.amount` 노출로 정한다. Phase 5 업로드 의도 · 완료 통보 · 삭제(`domain/expenses/evidence.ts` 실제 함수)의 주인 타입을 `ATTACHMENT_OWNER_KINDS`로 넓히고 권리 판정을 이 함수로 받는다 — 지출결의 주인의 흐름(06-11 검사 · 06-11 무효화 훅)은 그대로다. 넓힌 함수 안의 06-11 무효화 훅 호출(`invalidateEvidenceReview`)은 **`ownerKind === "expense"` 긍정 조건 안에만** 둔다 — 차수 승인 · 리저브(그리고 06-25의 카드 전표) 주인은 지출결의 행을 잠그지 않고 version도 바꾸지 않는다(r3 X-2 — 부정 조건 「차수 승인 · 리저브가 아니면」으로 쓰면 주인 종류가 늘 때 새 종류가 훅을 탄다). 주인 행은 서버가 `ownerId`로 다시 읽는다. `domain/quotes/revisions.ts` 차수 요약 DTO에 승인 증빙 파일 목록과 쓰기 가능 여부를 `quote.amount` 노출자에게만 싣는다(DTO 명세 갱신 — 누수 스캔은 기존 등록).

③ **액션 · 화면(한 경로).** `app/(app)/projects/actions.ts`에 승인 증빙 업로드 의도 · 완료 통보 · 삭제 액션 셋(Phase 5 업로드 액션과 같은 입력 모양 + `ownerKind` 고정 `quote_revision`)을 더하고 `actions.registry.ts`에 등록한다(이 registry는 Phase 4가 누수 스캔에 이미 import했다). `revision-section.tsx`: UI-SPEC S21 대로(DR-1) — **승인된 최신 차수** 행의 동작 칸에 3차 `승인 증빙 {N}`(파일 0이면 `승인 증빙`, 접근 이름 `{n}차 승인 증빙 {N}`, `<button aria-expanded aria-controls>`)을 두고, 누르면 차수 섹션 바로 아래 펼침 섹션(04 S5 「차수 열기」와 같은 자리 · 같은 규칙 — 한 번에 하나, 열리면 제목 `tabindex="-1"`로 포커스, 닫히면 그 3차로)이 선다. 제목 `상세 견적 {n}차 승인 증빙`(`--fs-sm` 600, 2px `--line-strong`으로 시작) + 부제 `승인 {승인일}`, 안에 Phase 5 첨부 컴포넌트(§7-10 파일 행 + 첨부 영역)를 그 prop 규약대로 붙인다. 승인 직후 3차가 저절로 서므로 안내 문구를 두지 않고 자동으로 펼치지도 않는다(DR-12). 업로드 · 삭제는 즉시 저장 — 상세의 `일괄 저장 Ctrl+S N`에 들지 않는다. 새 스타일 · 새 컴포넌트 없음, 토큰은 `tokens.css`의 것만. 이 트레이서는 최신 차수 · 쓰기 권리 한 경로만, 나머지 갈래(이전 차수 · 표시 취소 · 읽기)는 Task 2.

④ **테스트.** 통합 `approval-reserve-evidence.test.ts`(신규)에 behavior 세 줄, E2E `approval-reserve-evidence.spec.ts`(신규)에 「담당 PM → 차수 승인 표시 → 동작 칸 3차 `승인 증빙` → 펼침 제목 `상세 견적 {n}차 승인 증빙`에 포커스 → 승인 증빙 PDF 업로드(06-11 테스트 모드) → 파일 행에 파일 이름 · 3차 글자 `승인 증빙 1`」 한 경로.

⑤ **04 S5 개정 ⒜ — Phase 4 E2E 단언 조정(r2 F-3, UI-SPEC S21 「04 S5 개정」).** ③이 승인된 최신 차수 행에 3차를 세우므로 `test/e2e/quote-revisions.spec.ts`의 「차수 둘(1차 미승인 · 2차 최신이면서 고객 승인) → 2차 행 동작 칸 빈 칸」 단언(04 UI-SPEC U-2 ⓐ 케이스)이 깨진다. 먼저 ③까지 한 상태에서 그 스펙을 한 번 돌려 빨간 케이스를 SUMMARY에 적는다 — U-2 ⓐ 케이스의 동작 칸 단언 하나만 빨가야 하고, 다른 케이스가 빨가면 04 계약을 깨뜨린 것이니 스펙이 아니라 ③을 고친다. 그다음 그 단언만 개정 ⒜ 문장대로 바꾼다: 그 케이스의 사용자가 `quote.amount` 노출자(담당 PM)면 2차 행 동작 칸에 버튼이 하나이고 라벨이 `승인 증빙`(파일 0)이며 「차수 열기」가 없다. 승인 안 된 최신 차수 행의 동작 칸이 비었다는 단언(차수 셋 케이스의 3차 행)과 차수가 하나면 「차수 열기」가 없다는 단언은 04 그대로 둔다. 케이스를 지우거나 `skip` · `fixme`로 돌리지 않는다. 고친 단언의 전후 문장을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 개수를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/approval-reserve-evidence.test.ts test/integration/evidence-upload-intent.test.ts test/integration/quote-revisions.test.ts test/integration/migrate-runner.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. evidence-upload-intent가 빨가면 지출결의 주인 흐름을 깨뜨렸고, migrate-runner가 빨가면 `--custom` 마이그레이션이 러너 규약을 깨뜨렸다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/approval-reserve-evidence.spec.ts test/e2e/quote-revisions.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. quote-revisions가 빨가면 ⒜ 단언을 고치지 않았거나 04 S5 개정 ⒜ 밖의 04 계약을 깨뜨렸다(r2 F-3)</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(주인 칸 모양 ⒜/⒝ · 업로드 함수 이름 · 첨부 컴포넌트 실제 경로 · UI-SPEC S21 · S22 절 줄 번호)가 SUMMARY에 있다
    - E2E 트레이서가 3차 `승인 증빙` → 펼침 제목 포커스 → 업로드 → 3차 글자 `승인 증빙 1`을 단언한다(S21)
    - `grep -c "NOT VALID" db/migrations/*attachment_owner_kinds.sql`이 1 이상 · `grep -c "VALIDATE CONSTRAINT" db/migrations/*attachment_owner_kinds_validate.sql`이 1 이상
    - 통합 테스트가 behavior 세 줄을 개별 `it`으로 단언한다
    - [r3 X-2] `grep -n "invalidateEvidenceReview" domain/expenses/evidence.ts`의 호출 줄마다 `ownerKind === "expense"` 조건 안에 있다 — 호출 줄 번호와 조건 줄을 SUMMARY에 적는다(06-25 ⓪이 이 줄을 읽는다)
    - [r2 F-3] 04 S5 기존 E2E `test/e2e/quote-revisions.spec.ts`가 CI=true로 녹색이다 — 개정 ⒜ 문장으로 고친 단언 포함. SUMMARY에 ⑤의 첫 실행에서 빨간 케이스가 U-2 ⓐ 동작 칸 단언 하나뿐이었다는 줄과 고친 단언의 전후 문장이 있다
    - [r2 F-3] `grep -c "\.skip\|\.fixme" test/e2e/quote-revisions.spec.ts`가 착수 전과 같다(⓪에서 센 값을 SUMMARY에 적는다) — 단언을 지우거나 건너뛰어 녹색으로 만들지 않았다
  </acceptance_criteria>
  <done>승인 증빙 한 장이 마이그레이션 → 권리 판정 → Phase 5 업로드 경로 → 차수 섹션 표시까지 실제로 통과하고, Phase 4 차수 섹션 E2E가 04 S5 개정 ⒜대로 녹색이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-56 규칙 마감 — 표시 꺼짐 읽기 전용 · 완료 읽기 전용 · 노출 · 권리 밖 직접 호출 · 중복 · 마지막 삭제</name>
  <files>domain/evidence-attachments/index.ts, domain/quotes/revisions.ts, app/(app)/projects/[id]/revision-section.tsx, test/integration/approval-reserve-evidence.test.ts, test/e2e/approval-reserve-evidence.spec.ts, test/e2e/quote-revisions.spec.ts</files>
  <read_first>
    - domain/evidence-reviews/upload-checks.ts(06-11 — 주인이 지출결의가 아닐 때의 중복 문구 · 같은 주인 종류 범위 조건(DR-3) · 마지막 삭제 판정)
    - app/(app)/projects/[id]/revision-section.tsx(Task 1 결과)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S21 「자리」 · 「승인 표시 취소 뒤」 · 「내용」 · 「노출」 · UI Considerations S21 partial · zero-one-many 행 · S7 「중복(SHA-256) — 같은 주인 종류 안에서만」 · 열린 선택 O-6
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S21 머리 「04 S5 개정」 ⒝ · ⒞(r2 F-3) · test/e2e/quote-revisions.spec.ts의 「차수 열기」 케이스(이전 차수 읽기 섹션에 편집 입력 · 저장 버튼 · 구조 컨트롤이 없다는 단언 — 내용으로 찾는다)
  </read_first>
  <behavior>
    - 표시 켬 → 파일 1 → 표시 취소(연결 문서 없음) → 파일 남음 · 쓰기 거부 · 목록 읽기 가능 → 다시 켬 → 쓰기 가능
    - 완료 프로젝트 → 쓰기 거부, 목록 읽기 가능
    - 담당 아님 · `projects` 쓰기 없음 → 서버 액션 직접 호출 거부
    - `quote.amount` 노출 없는 계급 → 차수 DTO에 파일 목록 키 없음
    - [DR-3 · O-6 rev 9 — 「열린 선택」 중복 범위] 같은 파일을 다른 차수의 승인 증빙에 → 중복 막힘, 문서 번호 없음 / 같은 파일이 지출결의 증빙(또는 리저브 줄 증빙)에만 있음 → 막지 않음(업로드 통과)
    - (E2E) 이전 차수(승인된 채 새 차수가 생김) → 그 행 동작 칸에 3차 없음, 「차수 열기」 펼침 제목 아래 같은 블록 / 표시 취소 뒤 파일 0 → 3차 없음 · 파일 ≥ 1 → 3차가 남고 펼치면 파일 행만 / 쓰기 권리 없는 읽기 계정 · 파일 0 → `승인 증빙 없음 · 올리기는 담당 PM {이름}`(버튼 없음)
    - (E2E) 승인 증빙 펼침과 「차수 열기」는 한 번에 하나(서로 바뀐다) · 열리면 포커스 = 펼침 제목 · 닫히면 그 3차 / 저장 안 한 견적 편집이 있어도 업로드가 되고 상세의 `일괄 저장` N이 그대로다
    - 마지막 파일 삭제 → 모달 없음(`lastEvidenceDeleteNeedsConfirm` false)
    - (E2E, 04 S5 개정 ⒝ — r2 F-3) 승인된 이전 차수의 「차수 열기」 섹션, 쓰기 권리 → 첨부 영역 · 파일 행 `삭제`는 승인 증빙 블록 안에만 있고, 캡션 `상세 견적 {n}차 견적 줄` 표 안의 편집 입력 · 저장 버튼 · 구조 컨트롤 수 0
    - (E2E, 04 S5 개정 ⒞ — r2 F-3) `승인 증빙 {N}`을 누르면 라벨이 그대로 `승인 증빙 {N}`이고 `aria-expanded="true"`, 다시 누르면 `"false"` / 「차수 열기」는 04 그대로 `차수 닫기`로 바뀐다
  </behavior>
  <action>
`attachmentOwnerRights`의 `quote_revision` 갈래를 behavior대로 마감한다 — 판정은 잠근 프로젝트 행(Phase 4 `loadProjectForGate`) 뒤 같은 트랜잭션의 읽기로 한다(Phase 4 규약: 잠금 안 전역 db 호출 없음 — 06-03 tx 규약: 권한 · `quote.amount` 노출 판정은 트랜잭션을 열기 전에 읽어 넘기고, 잠근 뒤에는 표시 켜짐 · 완료 아님만 `tx`로 읽는다). 표시 취소 경로(Phase 4)는 파일을 지우지 않는다 — 취소 코드를 고치지 않고, 통합 테스트로 파일이 남는 것만 고정한다. `revision-section.tsx`는 서버가 보낸 쓰기 가능 여부로만 첨부 영역을 편집 / 읽기 전용으로 가른다(클라이언트가 표시 상태로 추론하지 않는다). UI-SPEC S21 갈래를 마감한다: 이전 차수(승인된 채 새 차수가 생긴 차수)는 동작 칸에 3차를 두지 않고 「차수 열기」 펼침 제목 바로 아래 같은 블록을 세우고, 승인 표시 취소 뒤에는 파일이 있을 때만 3차가 남아 펼치면 파일 행만(3차 `크게 보기`), 읽기 · 파일 0이면 Copywriting 「Empty — 차수 승인 증빙(S21, 펼침 섹션)」(`--muted`, 버튼 없음 — §8 규칙 3). 승인 증빙 펼침은 04 S5 「차수 열기」와 같은 한 개 상태를 나눠 써서 서로 바뀐다. 중복 · 삭제 문구는 06-11 판정을 그대로 쓴다 — 중복은 같은 주인 종류 안에서만 걸린다(DR-3, 06-11 Task 2의 범위 조건).

**04 S5 개정 ⒝ · ⒞ — Phase 4 E2E 단언 조정(r2 F-3, UI-SPEC S21 「04 S5 개정」).** 이전 차수 블록을 세우면 `test/e2e/quote-revisions.spec.ts` 「차수 열기」 케이스의 「편집 입력 · 저장 버튼 · 구조 컨트롤 · 복원 줄 · 힌트 줄 · `조정` 그룹 없음」 단언이, 그 케이스의 이전 차수가 승인되어 있고 사용자가 쓰기 권리면 승인 증빙 블록의 첨부 영역 · `삭제` 때문에 깨질 수 있다. Task 1 ⑤와 같은 순서로 한다 — 블록을 세운 뒤 스펙을 먼저 돌려 빨간 단언을 SUMMARY에 적고(그 단언 말고 다른 것이 빨가면 스펙이 아니라 블록을 고친다), 그 단언의 범위만 캡션 `상세 견적 {n}차 견적 줄` 표(그리고 승인 증빙 블록 밖의 섹션 영역)로 좁힌다(⒝). 단언을 지우거나 약하게(수 0 → 존재 여부 생략) 하지 않는다. 라벨 토글 단언(`차수 닫기`)은 04 그대로 둔다 — `승인 증빙 {N}`의 라벨 그대로 · `aria-expanded`(⒞)는 이 플랜의 `approval-reserve-evidence.spec.ts`가 단언한다. 두 버튼이 같은 펼침 자리 · 한 번에 하나 상태를 나눠 쓰는 구현(behavior `(E2E)` 번갈아 열림)은 04 「차수 열기」 상태를 그대로 쓴다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/approval-reserve-evidence.test.ts test/integration/quote-revisions.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/approval-reserve-evidence.spec.ts test/e2e/quote-revisions.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. quote-revisions가 빨가면 이전 차수 블록이 04 읽기 섹션 계약(견적 줄 표 읽기 전용)을 깨뜨렸거나 ⒝ 단언 범위를 좁히지 않았다(r2 F-3)</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior의 domain 줄 여섯(표시 켬 · 끔 · 다시 켬 / 완료 / 직접 호출 거부 / 노출 없는 계급 / `[DR-3]` 두 갈래 / 마지막 삭제)을 개별 `it`으로 단언하고, 노출 케이스는 차수 DTO 객체 키로, `[DR-3]`은 같은 파일(같은 SHA-256)을 지출결의 증빙으로 먼저 붙인 뒤 차수 승인 증빙 업로드 의도가 서명 URL을 받는 것과 다른 차수 승인 증빙에는 거부되는 것으로 확인한다
    - E2E가 표시 취소 뒤 첨부 영역의 업로드 · 삭제 요소 수 0과 파일 이름 표시를 단언한다
    - E2E가 behavior `(E2E)` 두 줄 — 이전 차수 블록 자리 · 표시 취소 뒤 3차 유무 · 읽기 빈 문구 · 「차수 열기」와 번갈아 열림 · 포커스 이동과 복귀 · 업로드 전후 `일괄 저장` N 같음 — 을 단언한다
    - [r2 F-3] E2E가 behavior `(E2E, 04 S5 개정 ⒝)` · `(E2E, 04 S5 개정 ⒞)` 두 줄을 단언한다 — 캡션 `상세 견적 {n}차 견적 줄` 표 안 `input` · `저장` 버튼 수 0과 승인 증빙 블록 안 첨부 영역 존재, `승인 증빙 {N}` 펼친 뒤 라벨 불변 · `aria-expanded="true"`
    - [r2 F-3] 04 S5 기존 E2E `test/e2e/quote-revisions.spec.ts`가 CI=true로 녹색이다(개정 ⒝ 문장으로 범위를 좁힌 단언 포함) · `grep -c "\.skip\|\.fixme" test/e2e/quote-revisions.spec.ts`가 ⓪ 값과 같다 · SUMMARY에 좁힌 단언의 전후 문장이 있다
  </acceptance_criteria>
  <done>승인 증빙이 표시 상태 · 완료 · 노출 · 권리를 서버 한 판정으로 지키고, 표시가 꺼져도 파일이 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-60 리저브 줄 증빙 — 첨부 · 권리 · 보관 줄 읽기 전용 · 노출</name>
  <files>domain/evidence-attachments/index.ts, domain/reserves/index.ts, app/(app)/pnl/reserves/actions.ts, app/(app)/pnl/reserves/actions.registry.ts, app/(app)/pnl/reserves/reserves-table.tsx, test/integration/approval-reserve-evidence.test.ts, test/e2e/approval-reserve-evidence.spec.ts</files>
  <read_first>
    - .planning/phases/04-project-quote-ledger/04-CONTEXT.md D-60(52행)
    - domain/reserves/index.ts(04-07 — 쓰기 판정 `can(viewer, "pnl", "write")` + `visible(viewer, "reserve.amount")` · 트랜잭션 앞 판정 규약 · `denyWrite`) · app/(app)/pnl/reserves/reserves-table.tsx(04-42 — 행 시트 · 행 행동 자리)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S22 「리저브 줄 증빙(D-60)」(와이어프레임 · 열 · 펼침 · 내용 · 저장 · 줄 삭제 · 오류 · 폭) · UI Considerations S22 행 · Copywriting 「Empty — 리저브 줄 증빙(S22, 펼침 섹션)」 · 「Destructive — 리저브 줄 삭제(04 S16 모달에 결과 줄 하나, S22)」 · 열린 선택 O-22
    - .planning/phases/04-project-quote-ledger/04-UI-SPEC.md S9 리저브 대장(열 우선순위 · 1024 미만 보기 전용 · 행 시트) · S16 「리저브 줄 삭제」 모달
  </read_first>
  <behavior>
    - `attachmentOwnerRights(viewer, { ownerKind: "reserve_entry", ownerId })`: `pnl` 쓰기 + `reserve.amount` + 줄 보관 아님 → write / 보관된 줄 → write false · read true / `pnl` 보기 + `reserve.amount` → read / 노출 없음 → read false · write false
    - 권리 없는 계급이 리저브 증빙 액션을 직접 부르면 거부 — 거부 메시지 · 로그에 금액 · 잔액이 없다(04-07 `denyWrite` 규약)
    - 줄 보관 → 복원 → 파일 그대로 · 쓰기 가능
    - 리저브 증빙 업로드 · 삭제가 잔액 · 금액을 바꾸지 않는다
    - [DR-3] 같은 파일을 다른 리저브 줄에 → 중복 막힘, 번호 없음 / 같은 파일이 지출결의 증빙 · 차수 승인 증빙에만 있음 → 막지 않음
    - (E2E, S22) ≥1280: 세금계산서 번호 다음 `증빙` 열 3차(0 = `증빙`, 1+ = `증빙 {N}`, 접근 이름 `{날짜} {클라이언트} 증빙 {N}`) → 표 블록 아래 펼침(제목 `{날짜} {클라이언트} {구분} {금액} 증빙`, 한 번에 하나, 제목 포커스, 쪽을 넘기면 닫힘) → 업로드 → 열 `증빙 1` · 표 dirty `N` 그대로 / 저장 전 새 줄 = `—` / 편집 표 키로 그 셀에서 `Enter` = 펼치기
    - (E2E, S22 폭 — O-22) 1024~1279: 열이 서고 업로드가 된다 / 700~1023: 펼침에 파일 행만(첨부 영역 · 삭제 요소 0) / <700: 접힌 줄 `증빙 {N}` 글자, 행 시트에 §7-8 첨부 행이 서고 업로드 · 삭제 요소 0
    - (E2E, S22) 증빙 1개 이상인 줄의 삭제 → 04 S16 모달에 결과 줄 `증빙 {N}개 함께 보관` / 증빙 0인 줄은 그 결과 줄 없음
  </behavior>
  <action>
`attachmentOwnerRights`에 `reserve_entry` 갈래를 더한다 — 판정은 Phase 4 `domain/reserves`의 쓰기 · 읽기 판정 함수를 그대로 부른다(새로 쓰지 않는다). 권리 판정은 트랜잭션 앞(04-07 규약), 거부는 `denyWrite`(허용 필드만 — 금액 없음). `domain/reserves/index.ts`의 줄 DTO에 증빙 파일 수 · 목록 · 쓰기 가능 여부를 리저브 읽기 권리자에게만 싣는다. `app/(app)/pnl/reserves/actions.ts`에 업로드 의도 · 완료 통보 · 삭제 액션 셋(`ownerKind` 고정 `reserve_entry`)을 더하고 `actions.registry.ts`에 등록한다(Phase 4가 누수 스캔에 이미 import했다). `reserves-table.tsx`: UI-SPEC S22 대로(DR-2) — 04 S9 편집 표의 세금계산서 번호 다음에 읽기 셀 열 `증빙`(편집 칸 아님 — §7-3 (가) 읽기 전용 단계, 우선순위 **P2** — 1024~1279에서 숨는 P3에 두지 않는다)을 더하고 값은 3차 `증빙 {N}` / `증빙`(0), 저장 전 새 줄(id 없음)은 `—`. 누르면 **표 블록 아래**(페이지 줄 · 힌트 줄 다음) 펼침 섹션(S21과 같은 규칙 — 한 번에 하나 · 제목 포커스 · 닫히면 3차로 · `aria-expanded`, 쪽을 넘기면 닫힘)에 Phase 5 첨부 컴포넌트를 붙인다 — 쓰기 권리면 파일 행 + 첨부 영역, 읽기면 파일 행만, 읽기 · 파일 0이면 Copywriting 「Empty — 리저브 줄 증빙(S22, 펼침 섹션)」. 폭: 1024~1279 쓰기, 700~1023 펼침에 파일 행만(업로드 · 삭제 prop을 넘기지 않는다), 폰(<700)은 열이 P2 접힌 줄의 `증빙 {N}` 글자이고 행 시트(§7-3 (바) 보기 전용)의 접힌 값 목록에 §7-8 첨부 행(썸네일 72×96 · 파일명 · 장수 · 용량 · 3차 `크게 보기`) — 행 시트에 업로드를 두지 않는다(O-22 기본값). 04 S16 `리저브 줄 삭제` 확인 모달에 결과 줄 하나 `증빙 {N}개 함께 보관`(N ≥ 1일 때만 — 줄 DTO의 증빙 수). 권리 밖 직접 호출의 거부는 그 행 오류 자리 `리저브 쓰기 권한 없음`. 업로드 · 삭제는 즉시 저장이라 표의 dirty `N`에 들지 않는다. 새 스타일 · 새 컴포넌트 없음. 삭제 모달이나 행 시트가 이 파일 밖이면 「규모 근거」의 재분할 방아쇠로 멈춘다.

E2E에 「경영관리(≥1280) → 리저브 줄 `증빙` 3차 → 표 아래 펼침 → 증빙 이미지 업로드 → 열 `증빙 1`」 · 「보관 → 복원 → 파일 그대로」와 behavior `(E2E, S22 …)` 세 줄(폭 1280 · 1100 · 900 · 390, 줄 삭제 모달 결과 줄)을 더한다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/approval-reserve-evidence.test.ts test/integration/reserve-entries.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. reserve-entries가 빨가면 Phase 4 리저브 동작을 깨뜨렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/approval-reserve-evidence.spec.ts test/e2e/reserves.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior의 domain 줄 다섯(권리 넷 갈래 · 직접 호출 거부 · 보관 → 복원 · 잔액 불변 · `[DR-3]` 두 갈래)을 개별 `it`으로 단언하고, 거부 케이스는 오류 메시지 · 경고 로그 객체에 숫자 금액 키가 없음을 확인한다
    - E2E가 behavior `(E2E, S22 …)` 세 줄을 단언한다 — 폭 390에서 행 시트 안 `input[type=file]` · `삭제` 버튼 수 0, 폭 900에서 펼침 안 같은 요소 수 0, 폭 1100에서 `증빙` 열이 보이고 업로드가 되며, 증빙 1개 줄 삭제 모달에 `증빙 1개 함께 보관`
    - 기존 reserve-entries 통합 · reserves E2E 녹색
  </acceptance_criteria>
  <done>리저브 줄마다 증빙을 붙이고 뗄 수 있고, 권리 · 보관 · 노출이 Phase 4 리저브 규칙과 같은 판정을 따른다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 첨부 액션 | `ownerKind`는 액션마다 고정, `ownerId` · 파일 메타가 넘어온다. 권리 · 프로젝트 id는 넘어와도 믿지 않는다 |
| 서버 → 브라우저(차수 · 리저브 DTO) | 승인 증빙 · 리저브 증빙 파일 목록이 노출 기준을 지난 사람에게만 나간다 |
| 마이그레이션 → Phase 5 파일 표 | 기존 표 제약 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-78 | Elevation of Privilege | 승인 증빙 · 리저브 증빙 액션 | high | mitigate | `attachmentOwnerRights`가 주인 행을 서버에서 다시 읽어 Phase 4 판정(승인 표시 권리 · 리저브 쓰기)을 부른다. 권리 밖 계급 직접 호출 거부 통합 테스트 |
| T-06-79 | Information Disclosure | 차수 · 리저브 DTO의 파일 목록 | high | mitigate | `quote.amount` · `reserve.amount` 노출자에게만 싣고 DTO 명세 갱신 → 누수 스캔. 노출 없는 계급 DTO 키 단언 |
| T-06-80 | Information Disclosure | 리저브 거부 메시지 · 로그 | medium | mitigate | 04-07 `denyWrite` 허용 필드만 — 금액 · 잔액 없음. 통합 테스트 |
| T-06-81 | Tampering | `ownerKind` · `ownerId` 바꿔치기 | medium | mitigate | 액션마다 `ownerKind` 고정, 파일 삭제는 파일 행의 저장된 주인으로 권리 판정(요청 값 아님) |
| T-06-82 | Denial of Service | Phase 5 파일 표 제약 변경 마이그레이션 | medium | mitigate | B-2: SET LOCAL 한 쌍 · 제약 `NOT VALID` · `--custom` VALIDATE · 일반 CREATE INDEX · `pnpm lint:sql` 0건 · 두 번째 generate `No schema changes` |
| T-06-83 | Tampering | 업로드 파일(크기 · 형식 · 중복) | low | mitigate | 06-11 `checkEvidenceUpload`를 서명 URL 발급 앞에서 그대로 부른다 · Phase 5 완료 통보의 메타데이터 재확인 유지 |
</threat_model>

<verification>
- 통합 approval-reserve-evidence · evidence-upload-intent · quote-revisions · reserve-entries · migrate-runner · leak-scan, 단위 leak-scan-coverage 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E approval-reserve-evidence · quote-revisions · reserves가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 UI-SPEC S21(차수 동작 칸 3차 · 펼침) · S22(`증빙` 열 · 표 아래 펼침 · 폭 1280 · 1100 · 900 · 390 · 폰 행 시트 첨부 행) DOM 실측 감사(backstop 4건 · UI-SPEC 밖 새 시각 패턴 부재 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- D-56 · D-60이 이 페이즈 안에서 계획대로 끝나고, UI-SPEC S21 · S22가 정한 자리에만 기존 컴포넌트로 선다(리저브 폰 · 700~1023은 읽기만 — O-22)
- 두 새 주인이 Phase 5 업로드 경로 · 06-11 검사를 그대로 지나고, 권리 · 노출이 Phase 4 판정과 같다
- 기존 표 마이그레이션이 B-2 규약대로 들어간다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `NNNN_attachment_owner_kinds.sql`(SET LOCAL · 제약 NOT VALID) · `NNNN_attachment_owner_kinds_validate.sql`(`--custom`, VALIDATE) |
| 함수(domain) | `attachmentOwnerRights` · `ATTACHMENT_OWNER_KINDS` |
| 변경된 기존 심볼 | Phase 5 업로드 의도 · 완료 통보 · 삭제(주인 타입 확장) · 차수 요약 DTO(승인 증빙) · 리저브 줄 DTO(증빙) |
| Server Action | 승인 증빙 업로드 의도 · 완료 통보 · 삭제 · 리저브 증빙 업로드 의도 · 완료 통보 · 삭제 |
| 화면 | UI-SPEC S21 — 차수 섹션 동작 칸 3차 `승인 증빙 {N}` + 펼침 섹션(이전 차수는 「차수 열기」 펼침 안 블록) · UI-SPEC S22 — 리저브 대장 `증빙` 열(P2) + 표 아래 펼침 · 폰 행 시트 §7-8 첨부 행 · 04 S16 `리저브 줄 삭제` 모달 결과 줄 `증빙 {N}개 함께 보관` |
| 테스트 | `test/integration/approval-reserve-evidence.test.ts` · `test/e2e/approval-reserve-evidence.spec.ts` · `test/e2e/quote-revisions.spec.ts`(Phase 4 — 04 S5 개정 ⒜ ⒝ 단언만 조정, r2 F-3) |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG E-4 — 첨부 주인 CHECK 갈래 ⒜/⒝(06-16:160)가 실행 중 분기 선택이다 | LOW(참고) | 계획 변경 없음 — 검토 판정 「수용」. Task 1 ⓪이 Phase 5 파일 표 주인 칸 모양을 읽어 ① 두 갈래 중 하나만 고르고, 고른 갈래를 SUMMARY에 남긴다(Task 1 acceptance 첫 줄 「주인 칸 모양 ⒜/⒝」) |
| ENG E-3 참조 — 06-03 tx 규약 | MEDIUM(고침, 06-03 소관) | must_haves.truths 한 줄 「06-03 tx 규약을 따른다」 · Task 2 action의 판정 문구에 규약 괄호 한 줄(권한 · 노출은 트랜잭션 전, 잠금 뒤에는 표시 · 완료 상태만 `tx`) — 기존 「잠금 안 전역 db 호출 없음」과 같은 뜻을 규약 이름으로 맞췄다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 배정된 검토 항목은 ENG E-4 하나이고 수용으로 끝났다 |

### Round 2 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 4)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-1 — D-56 차수 승인 증빙 자리가 UI-SPEC에 없어 06-16이 착수할 수 없다(design-review.md L33@f2720a8) | HIGH(막음) | UI-SPEC rev 9 S21(Shard A). Plan 16: must_haves.truths 「UI-SPEC S21 · S22」 줄 · S21 UI Considerations 여섯 explicit + backstop 둘 lift · 「선행 의존」 N-1 문단 · 표 줄 갱신 · Task 1 read_first · action ③(동작 칸 3차 → 「차수 열기」 규칙 펼침 · 제목 · 부제 · 즉시 저장) · ④ E2E · acceptance · Task 2 read_first · behavior `(E2E)` 두 줄 · action(이전 차수 · 표시 취소 뒤 · 읽기 빈 문구 · 번갈아 열림) · acceptance · verification · success_criteria · Artifacts 화면 줄. 태스크 본문에 자리를 비워 둔 곳이 남지 않았다 |
| DR-2 — D-60 리저브 줄 증빙 자리가 없고 06-16 Task 3이 자리를 「기존 행 시트 또는 행 행동」 중 하나로 비워 두었다(L34@f2720a8). 사용자 카드 DR-2 = 아니오(폰 업로드 없음 — 추천안 적용, 답 대기) | HIGH(막음) | UI-SPEC rev 9 S22 · O-22(Shard A). Plan 16: must_haves.truths S22 여섯 explicit + backstop 둘 · prohibitions 「행 시트 · 1024 미만 업로드 요소 없음」 · 열린 선택 O-22 행 · 「선행 의존」 리저브 표 자리 줄 · 「규모 근거」 재분할 방아쇠(삭제 모달 · 행 시트) · Task 3 read_first(S22 · 04 S9 · S16) · behavior `(E2E, S22 …)` 세 줄 · action(`증빙` 열 P2 · 표 아래 펼침 · 폭 갈래 · 행 시트 §7-8 첨부 행 · 줄 삭제 모달 결과 줄) · E2E · acceptance(폭 390 · 900 · 1100 요소 수). `files_modified` 14개 그대로 |
| DR-3 — 중복 막힘을 같은 주인 종류 안으로(N-7 :130 · Task 2 behavior :201, L36@f2720a8). 사용자 카드 DR-3 = 허용(추천안 적용, 답 대기) | MEDIUM(고침) | Plan 16: 열린 선택 중복 범위 행(N-7 → DR-3 · O-6 rev 9) · must_haves.truths 업로드 줄 · Task 2 behavior `[DR-3]` · Task 3 behavior `[DR-3]` · 두 태스크 acceptance(지출결의 증빙으로 먼저 붙인 같은 파일이 통과). 범위 조건은 06-11 Task 2 소관 — 이 플랜 `files_modified` 그대로 |
| DR-12 — 승인 표시 뒤 다음 한 수(승인 증빙)가 보이는가(L47@f2720a8) | LOW(참고) | Plan 16, Task 1 action ③: 3차가 승인 직후 저절로 서므로 안내 문구 · 자동 펼침 없음 · must_haves S21 populated 줄 |
| DR-9 — `ui/attachments`가 main · Phase 4 브랜치 어디에도 없다(L44@f2720a8) · Shard A 전달 「어느 플랜이 `Attachments.tsx`를 만드나」 | LOW(참고) | 확인 결과: Phase 5가 만든다(A-608 · UA-606 — ROADMAP Phase 5 「증빙 첨부 포함」, CEO 검토 재사용 표 「첨부 = Phase 5 업로드 · 06-11 · 06-16」), 06-11 Task 3이 고치고 이 플랜은 쓰기만 한다. 「선행 의존」 UA-606 · A-608 줄에 **파일 존재 확인을 먼저**(없으면 grep이 0건으로 조용히 지나가던 구멍) · 06-11 SUMMARY의 실제 경로 · 읽기 그리기 확인을 더했다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 카드 전표 첨부 저장(파일 표 주인 `corp_card_usage`)의 소유 플랜 없음 — 이 플랜이 주인 종류를 넓히는 자리라 검토함 | 이 플랜에 더하지 않음 — 카드 폼 · 구매 완료 폼 · 목록 증빙 열까지 더하면 재분할 방아쇠(15파일)를 넘는다. 오케스트레이터 결정 필요(06-11 ledger Round 1 보류 줄과 같은 권고) |

### Round 3 — 디자인 검토 반영 r2(커밋 전 · `/mnt/project-files/06-gates/design-apply-cross-r1.md` · `design-apply-r2-brief.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 F-3 — S21이 Phase 4 S5 계약 셋(현재 차수 행 동작 없음 · 이전 차수 섹션 읽기 전용 · 「차수 열기」 라벨 토글)을 바꾸면서 바꾼다고 적지 않아, 06-16 실행자가 04 E2E를 깨뜨리거나 두 계약 중 하나를 골라야 한다(design-apply-cross-r1.md F-3) | 경고 | UI-SPEC S21 머리 「04 S5 개정」 ⒜ ⒝ ⒞(같은 라운드 · R1). Plan 16: `files_modified`에 `test/e2e/quote-revisions.spec.ts`(15개) · must_haves.truths 「04 S5 개정」 줄 · 「규모 근거」(15개 · 방아쇠는 16번째) · 「선행 의존」 A-614 줄(스펙 존재 확인). Task 1 files · read_first · action ⑤(③ 뒤 먼저 돌려 빨간 단언이 U-2 ⓐ 하나뿐인지 확인 → ⒜ 문장대로 그 단언만 고침) · E2E fails_when · acceptance(스펙 녹색 · `skip`/`fixme` 수 불변) · done. Task 2 files · read_first · behavior ⒝ ⒞ 두 줄 · action(⒝ 범위를 견적 줄 표로 좁힘 · ⒞는 이 플랜 스펙이 단언) · verify에 quote-revisions · fails_when · acceptance. Artifacts 테스트 줄 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| Supersedes Round 2 — 「카드 전표 첨부 저장(`corp_card_usage`)의 소유 플랜 없음」 보류 줄 | 오케스트레이터 결정(B-2)으로 새 플랜 → 06-25 「카드 사용 증빙 첨부」(R2 작성, 06-16 뒤 파). 이 플랜에는 더하지 않는다 — 이 플랜의 `ATTACHMENT_OWNER_KINDS` · `attachmentOwnerRights`가 06-25가 주인 종류를 하나 더 넓히는 자리다 |
| 06-16 `files_modified` 15개(체커 15개 문턱) | 쪼개지 않음 — 더한 파일은 이 플랜의 변경이 깨뜨리는 Phase 4 스펙이라 다른 플랜으로 떼면 이 플랜의 verify가 빨간 채 끝난다. 06-03 · 06-07(17개) 선례처럼 오케스트레이터에 보고한다. Task 1이 파일 11개(기존 10 + 스펙)인 것도 같은 이유 |

### Round 4 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-2 — 06-11 훅이 `ownerKind === "expense"`에서만 불린다는 조건이 어디에도 없다. 06-16:192 「지출결의 주인의 흐름(06-11 무효화 훅)은 그대로」는 조건을 적지 않았다(design-apply-cross-r2.md X-2 근거 06-16:192) | 고침(06-25와 나눔) | Plan 16, Task 1 action ②: 넓힌 Phase 5 함수 안의 훅 호출을 `ownerKind === "expense"` **긍정** 조건 안에만 둔다(부정 조건이면 06-25의 새 종류가 훅을 탄다) · acceptance `[r3 X-2]`(호출 줄마다 조건 안 — 줄 번호를 SUMMARY에, 06-25 ⓪이 읽는다). 조건을 두는 자리를 `domain/expenses/evidence.ts`를 이미 가진 이 플랜으로 잡아 06-25 파일 수를 15에 묶는다. 카드 전표 쪽 불변 테스트는 06-25 Task 3 |
| X-4 · 파일 수 문턱 한 규칙 — 06-16은 15개를 받아들이고 16번째에서 멈추는데 06-25는 15번째에서 멈춰 기준이 달랐다(design-apply-cross-r2.md X-4 ⑵) | 참고 | 이 플랜의 「규모 근거」 규칙(15개 보고 · 16번째 파일에서 멈춤 — `.claude/agents/gsd-plan-checker.md` Thresholds 「Files/plan … 15+」)을 한 규칙으로 두고 06-25 · 06-11이 이 규칙을 따른다. 이 플랜 문구는 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (관찰) 파일 표가 ⒝ 주인별 FK 칸 모양이면 06-11 중복 조회 리포지토리의 종류 → FK 칸 대응에 `quote_revision` · `reserve_entry`가 더해져야 하는데 그 리포지토리가 이 플랜 files_modified에 없다 | 이 라운드 지적 밖이라 고치지 않는다(06-11 Round 3 Deferred와 같은 줄). ⒜(`owner_kind IN`)이면 해당 없음. ⓪에서 ⒝을 고르고 그 대응이 필요하면 「규모 근거」와 같은 방식으로 `06-16 파일 한도 — {리포지토리} 필요`를 보고한다 |

### Round 5 — 체커 r3 참고 반영(r3-2)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 r3 key_links_planned(info) — Round 4 보류 줄(파일 표 ⒝면 중복 조회 리포지토리 대응 필요)과 ⓪ 멈춤 경로를 받아들임, ⒝이면 실행 때 06-16 재분할을 예상 | 참고 | 「규모 근거」 끝에 한 문장: ⓪에서 ⒝이면 그 리포지토리가 16번째 파일이라 멈추고 보고하며 재분할을 예상한다. 코드 · 파일 목록 변경 없음 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 6 — r4 (final-review-fable, 2026-09-26)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/final-review-fable.md`(커밋 전 — 줄 번호는 그 파일 기준). 교차 검토는 Codex 대신 Fable(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| (이 플랜 본문에 해당 지적 없음) | — | final-review-fable.md가 이 플랜을 F-N1 목록에만 올렸고, 이 플랜의 저장소 밖 경로는 아래 ledger 제목뿐이다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| F-N1 — Round 3 제목의 `/mnt/project-files/06-gates/...` 경로 | 고치지 않음 — 앞 라운드 제목은 그 라운드 입력의 출처 기록이고 ledger는 append-only다. read_first · action이 아니라 실행자가 읽을 일이 없다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-16-SUMMARY.md` when done
</output>
