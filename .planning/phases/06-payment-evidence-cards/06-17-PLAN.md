---
phase: 06-payment-evidence-cards
plan: 17
type: execute
wave: 9
depends_on: ["06-15"]
files_modified:
  - domain/payments/targets.ts
  - domain/payments/batch.ts
  - ui/table/Table.tsx
  - ui/table/use-grid-keyboard.ts
  - ui/confirm-dialog/ConfirmDialog.tsx
  - ui/confirm-dialog/ConfirmDialog.module.css
  - app/(app)/expenses/payment-targets-table.tsx
  - app/(app)/expenses/batch-payment-dialog.tsx
  - app/(app)/expenses/actions.ts
  - app/(app)/expenses/actions.registry.ts
  - test/unit/domain/payments-targets.test.ts
  - test/unit/ui/table-selectable.test.ts
  - test/integration/payment-batch.test.ts
  - test/e2e/payment-batch.spec.ts
autonomous: true
requirements: [EXP-09]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "「지급 대상」의 P4 행은 이체액 칸을 편집할 수 있고(기본값 = 지급 총액, D-604), 이체액 ≠ 지급 총액이면 셀 2행 `차이 {±}`(`--warning`, 서버가 준 값)와 차이 사유 칸(편집 가능 · 필수)이 선다 — 같아지면 사유 칸은 `—`로 돌아가고 적은 사유는 버린다(D-605)"
    - "고르지 않은 P4 행의 이체액을 고치면 그 행이 저절로 골라진다. 막힌 행에는 편집 칸이 없으므로 저절로 골라지는 일도 없다"
    - "`고른 N건 · 이체액 {합}`의 합과 행별 차이는 서버 액션 `sumSelectedTransfersAction`이 고른 행 id + 이체액 편집값으로 `sumKrw` · `diffKrw`로 계산해 돌려주고(O-18 — 클라이언트 금액 셈 없음), 응답에 실린 합계 번호가 지금 번호와 다르면 버린다"
    - "모달의 지급일로 서버가 다시 계산한 지급 총액이 스냅숏의 `expectedPayableKrw`와 다른 건은 처리하지 않고 막힘으로 돌아오며, 그 행의 지급 총액 셀이 새 값으로 바뀌고 이유 `지급일 {MM-DD} 기준 지급 총액 바뀜 · 이체액 확인`이 선다 — 재시도는 새 값으로 보내 같은 이유로 다시 막히지 않는다(06-03 `PayableChangedError`)"
    - "[O-3 기본값] 모달을 다시 열면 지급일 기본값이 방금 고른 날이다(필터 변경 · 새로 고침 때 잊는다)"
    - "`Space` = 현재 행 고르기(선택 가능 행만), 힌트 줄 `이동 Tab ↑↓←→ · 고르기 Space · 취소 Esc`"
    - "S1 loading: 합 요청 중에는 이전 합을 `--faint`로 둔다"
    - "S1 error: 칸 오류 = 「Error — 이체액 칸」 · 「Error — 차이 사유 칸」, 합 실패 = 「거부 — 이체액 합계 응답 없음」 + `다시 시도`(같은 번호)"
    - "S1 overflow: 50건 페이지(§7-16, UA-603), 선택은 행 id로 쪽을 넘어도 남고 필터 변경 때 비며, 합계 줄은 필터 전체 기준, 1024~1279는 P3(번호 · 거래처)부터 숨기고 700~1023은 보기 전용(선택 열 · 편집 · 1차 없음), 700 미만은 행 → 문서 화면 링크"
    - "**H-2(rev 9) 링크 셀:** ≥1280 번호 셀은 식별자형 링크(3차) → `/expenses/{id}?from=pay&{지금 보기 · 필터 · 쪽}`(접근 이름 `26001-0004 지출결의 열기` 꼴), 번호가 숨는 1024~1279와 700~1023에서는 「프로젝트 · 항목」 셀 1행이 같은 링크다. 링크 셀 · 증빙 칸 3차는 편집 칸이 아니라 방향키 · `Tab`으로 닿고 `Enter` = 열기이며, 가운데 클릭 · `Ctrl`+클릭(새 탭)을 막지 않는다"
    - "**DR-8(rev 9):** `selectable` 표에는 새 줄이 없다 — 표 안에서 누른 `Ctrl+Enter`는 §7-3 「새 줄」이 아니라 화면 1차 `지급 완료 N`이다"
    - "S1 zero-one-many: 여러 쪽에 걸쳐 고르면 `고른 N건 · 이체액 {합}`이 모든 쪽 합이다"
    - "S2 loading: 부제 합계 자리 `…` + 1차 `…` · `aria-disabled` — 모달을 연 순간의 합계 번호 응답이 오면 채우고 켠다"
    - "S2 error: 합 실패 = 「거부 — 이체액 합계 응답 없음」"
    - "S2 partial: 지급일 세율로 지급 총액이 바뀐 건은 막힘으로 돌아와 새 지급 총액 셀 + 이유"
    - "처리 전까지 편집값은 서버에 저장되지 않고 탭 안 `sessionStorage`(키 = 보기 + 필터, 값 = 고른 행 id · 행마다 이체액 · 차이 사유 편집값 · 지금 쪽 — 계좌 · 이름 · 계산 금액은 싣지 않는다)에 보관된다. 같은 탭에서 같은 키로 S1이 다시 열리면 묻지 않고 되살린다 — **보관한 행 id 전부(지금 쪽 밖 포함)를 합 요청 한 번에 실어**(합계 번호 +1) 서버가 행마다 돌려준 `selectable` · `version` · 지급 총액(`rowStates`)으로 지금 고를 수 있는(P4) 행의 선택 · 편집만 되살린다. 다른 쪽에서 고른 행도 살아남고, 지금 쪽 밖 행의 `version` · 지급 총액은 이 응답 값으로 처리 요청에 든다(r2 F-2 — 보관은 `version`을 싣지 않는다). 처리 응답 뒤 처리된 행 · 필터 변경 때 그 키 통째로 · 탭을 닫을 때 지운다(C-1). `beforeunload` 경고는 탭 닫기 · 다른 사이트로 떠날 때만이고 앱 안 이동(`Link`)은 가로채지 않는다. 1차를 눌렀을 때 고른 행에 오류 칸이 있으면 첫 오류 칸으로 포커스(모달 없음)"
    - "**DR-4(rev 9, O-21 「예」 — 사용자 확정(2026-09-25)):** ≥1024 S1의 `확인 전` 행 증빙 칸 3차 `증빙 확인`(접근 이름 `26001-0007 증빙 확인` 꼴, 증빙이 있는 `확인 전` 행에만)은 목록을 떠나지 않고 확인 모달을 연다 — 제목 `증빙 확인` · 부제 `{번호} · {프로젝트} · {항목} · 증빙 금액 {금액}` · 첨부 보기 칸(SP-7) · 결과 줄 = 서버 계산 한 줄(`부가세 … · 지급 총액 … · {규칙}`) · 1차 `증빙 확인` + `Ctrl+Enter` · 2차 `취소 Esc`, 입력 칸 없음. 700~1023의 3차는 모달이 아니라 문서 화면 `?from=pay` 링크다"
    - "제자리 확인은 06-06 `confirmEvidenceAction`의 금액 없는 갈래를 **모달을 열 때 보기(`getEvidenceConfirmViewAction`)와 함께 받은 `version`**으로 그대로 부르고(새 판정 없음 — r2 B-1, 행의 `version`이 아니다), 응답의 새 `version` · 상태로 그 S1 행을 바꾼다 — 그 행을 포함한 뒤이은 일괄 처리가 동시성 막힘 없이 처리된다. 06-11 훅이 결재 통과 문서(모달이 열리는 `확인 전` 행은 모두 결재 통과 — X-1 r3)의 PM 증빙 변경마다 문서 `version`을 올리므로 모달을 연 뒤 바뀐 증빙은 확인이 거부되고, 보기의 `version`이 그 행의 `version`과 다르면(목록을 받은 뒤 바뀜) 1차 `aria-disabled` + 「거부 — 문서 화면 동시성」 문구(사람 · 시각은 서버 값)"
    - "`ui/confirm-dialog`에 선택 슬롯 「첨부 보기 칸」(SP-7) — 부제 아래 · 결과 줄 위, 파일마다 한 행(썸네일 72×96 + 파일명 · 장수 · 용량 + 3차 `크게 보기` = 새 탭 원본), 보기만(올리기 · 삭제 · 입력 없음). 슬롯을 넘기지 않은 기존 사용처(S2 · 지급 취소 · 면제 등)는 이전과 같은 DOM"
    - "S1 제자리 증빙 확인 empty: 빈 모달이 열릴 길이 없다 — 3차 `증빙 확인`은 증빙이 있는 `확인 전` 행에만 선다. 1024 미만은 3차가 문서 화면으로 간다(모달 없음)"
    - "S1 제자리 증빙 확인 loading: 여는 동안 첨부 보기 칸 · 부제 금액 · 결과 줄 자리 `…` + 1차 `aria-disabled`(`Ctrl+Enter` 무반응), 확인 처리 중 1차 `증빙 확인…`"
    - "S1 제자리 증빙 확인 error: 「거부 — 증빙 확인 모달(S1 · S3)」 — 불러오기 실패 `증빙을 불러오지 못함 · 다시 시도`, 확인 거부 · 열 때 받은 `version` ≠ 행 `version` = 「거부 — 문서 화면 동시성」 · 「거부 — 문서 화면 응답 없음」 문구. 모달은 닫히지 않고, `새로 고침`은 목록과 모달 보기를 다시 받아 그 문서가 아직 `확인 전`이면 모달이 새 파일 · 증빙 금액 · 결과 줄 · 새 `version`으로 다시 선다(오는 동안 `…` + 1차 `aria-disabled`). `확인 전`이 아니게 됐으면 모달을 닫고 그 행은 새 상태대로(저절로 골라지지 않음), 포커스 = 그 행 링크. 다른 행의 선택 · 편집은 그대로(r2 B-1)"
    - "S1 제자리 증빙 확인 populated: 성공 = 모달이 닫히고 그 행이 P4 → 선택 칸이 켜지고 저절로 골라짐 · 증빙 칸 `확인됨`(2행 없음) · 이체액 칸 편집 가능 · 합계 번호 +1 → 서버 합 다시 · 포커스 = 그 행 이체액 셀 · 토스트 없음"
    - "S1 제자리 증빙 확인 partial: 금액을 고칠 건은 모달에 칸이 없어 번호 링크로 문서 화면(`from=pay`)에서 한다. 응답의 새 `version`으로 행을 바꾼다. 닫힌 직후 `event.repeat`인 `Ctrl+Enter`는 무시한다(M-3 — 연타로 S2 모달이 열리지 않는다)"
    - statement: "S1 제자리 증빙 확인 overflow: 파일이 셋을 넘으면 첨부 보기 칸이 행 셋 높이에서 멈추고 칸 안만 스크롤(제목 · 부제 · 행동 줄 고정, SP-7) — 파일 6개 건으로 480 모달 · 폰 시트에서 CI=true DOM 감사"
      verification: backstop
    - statement: "S1 제자리 증빙 확인 long-text: 긴 프로젝트 · 항목 이름의 부제와 80자 파일명이 480 모달에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
    - "S1 일괄 결과 알림 error: 막힌 행 이유 줄(「거부 — 일괄 지급 건별 결과」)의 id를 그 행 체크박스 `aria-describedby`에 더한다(증빙 칸 글자 id와 함께 — 누르기 전에 읽힌다, DR-6)"
    - "S1 일괄 결과 알림 populated: 결과 글자 `14:02 지급 완료 5건 · 막힘 2건` 꼴을 처음부터 DOM에 있는 빈 `aria-live=\"polite\"` 영역에 쓴다. 자리 = 필터 줄 왼쪽 묶음 끝(1차와 떨어짐, M-2 · DR-6)"
    - "S1 일괄 결과 알림 partial: 게이트 막힘(P2 · P3 · 지급 뒤) = 선택 풀림 + 체크박스 `aria-disabled` + 편집값 버림, 동시성 · 지급일 세율 막힘(P4 그대로) = 선택 유지. 1차 N = 고를 수 있는 고른 행만(H-3 — 판정은 06-15 `selectable`). 처리 뒤 같은 쪽을 다시 받고, 비었으면 행이 있는 마지막 쪽으로 가며 처리된 id는 다른 쪽 선택과 보관에서도 지운다(M-1)"
    - "S1 일괄 결과 알림 zero-one-many: 막힘 0 = `14:02 지급 완료 5건`만. 포커스 = 막힌 행이 있으면 첫 막힌 행 체크박스(`aria-disabled`여도 포커스 — SYSTEM §10), 없으면 1차 버튼"
    - "S2 partial(확정 전, H-5): 지금 쪽 밖에서 고른 건 · 차이 있는 건이 섞이면 결과 줄 ③ `다른 쪽 N건 포함 · 차이 있음 N건`(해당 묶음만, 둘 다 없으면 이 줄 없음 — 둘 다 개수라 화면이 센다)"
    - statement: "S1 long-text: 항목 · 거래처 · 예금주는 셀 한 줄 말줄임 + 전체 값 `title`, 표 안 차이 사유는 두 줄까지 — 20자 이상 한글 거래처 · 200자 차이 사유로 CI=true DOM 감사"
      verification: backstop
    - statement: "S2 long-text: 부제 `N건 · 이체액 합계 {13자리 금액}`과 결과 줄 둘이 `--modal-w` 480 안에서 숫자 중간 줄바꿈 없이 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "클라이언트가 금액 셈(고른 건 이체액 합 · 차이)을 해서는 안 된다 — 합과 차이는 서버 액션 응답이다(O-18)"
      status: resolved
      verification: test
    - statement: "지급일로 바뀐 지급 총액이 조용히 옛 이체액으로 처리되어서는 안 된다"
      status: resolved
      verification: test
    - statement: "제자리 증빙 확인이 새 판정 · 새 확인 경로를 만들어서는 안 된다 — 06-06 `confirmEvidenceAction` 금액 없는 갈래만 부른다(DR-4)"
      status: resolved
      verification: test
    - statement: "게이트로 막힌 행이 체크된 채 남아 1차 N에 들어서는 안 된다(H-3)"
      status: resolved
      verification: test
    - statement: "모달이 보여 준 증빙과 다른 증빙(모달을 연 뒤 PM이 더하거나 떼거나 금액을 고친 묶음)이 `확인됨`이 되어서는 안 된다(r2 B-1)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/payments/targets.ts"
      provides: "고른 건 이체액 서버 합 · 행별 차이(합계 번호) · 행 상태 `rowStates`(`{ id, selectable, version, payableKrw }` — 되살리기 · 지금 쪽 밖 행, r2 F-2) · 그룹 · 페이지 · 제자리 증빙 확인 모달 보기(DR-4 · 보기 `version`과 행 `version` 비교, r2 B-1)"
      exports: ["listPaymentTargets", "sumSelectedTransfers", "getEvidenceConfirmView"]
    - path: "ui/confirm-dialog/ConfirmDialog.tsx"
      provides: "SP-7 「첨부 보기 칸」 슬롯 `attachments`(읽기 전용 · 부제 아래 · 셋 넘으면 칸 안 스크롤)"
    - path: "test/unit/ui/table-selectable.test.ts"
      provides: "선택 상태 순수 함수(머리글 토글 · indeterminate · 쪽 이동 유지 · 필터 초기화 · 편집 자동 선택 · 처리 결과 적용 · 되살리기)"
      min_lines: 30
  key_links:
    - from: "app/(app)/expenses/payment-targets-table.tsx"
      to: "app/(app)/expenses/actions.ts"
      via: "고른 건 합 · 차이가 서버 액션 응답이고 합계 번호로 낡은 응답을 버린다"
      pattern: "sumSelectedTransfersAction"
    - from: "domain/payments/batch.ts"
      to: "domain/payments/index.ts"
      via: "지급일 재계산 막힘이 단건 경로의 `PayableChangedError`에서 온다"
      pattern: "PayableChangedError"
    - from: "app/(app)/expenses/payment-targets-table.tsx"
      to: "app/(app)/expenses/[id]/actions.ts"
      via: "제자리 확인은 06-06 `confirmEvidenceAction` 금액 없는 갈래를 모달 보기가 준 version으로 그대로 부르고(r2 B-1) 응답 version으로 행을 바꾼다(DR-4)"
      pattern: "confirmEvidenceAction"
    - from: "app/(app)/expenses/payment-targets-table.tsx"
      to: "ui/confirm-dialog/ConfirmDialog.tsx"
      via: "제자리 확인 모달이 첨부 보기 칸(SP-7)에 파일 행을 넘긴다"
      pattern: "attachments"
---

<objective>
일괄 지급(06-15)을 마감한다 — 이체액 편집 · 차이 사유, 서버 합(합계 번호), 지급일 재계산 막힘, `Space` · 50건 페이지 · 좁은 폭. **rev 9(`/plan-design-review` 반영):** 목록을 떠나지 않는 제자리 증빙 확인(DR-4 — 확인 모달 + 첨부 보기 칸 SP-7, 06-06 `confirmEvidenceAction` 그대로 · 모달이 받은 version으로 확인(r2 B-1) · 응답 version으로 행 갱신 · 저절로 골라짐)과 편집 · 선택 보관(`sessionStorage`, C-1 — 되살리기는 보관한 id 전부를 합 요청 한 번으로, r2 F-2), 일괄 결과 알림 · 막힌 행 선택(DR-6 · H-3), 모달 결과 줄 `다른 쪽 N건 포함 · 차이 있음 N건`(H-5), 링크 셀(H-2) · 표 안 `Ctrl+Enter`(DR-8) · 처리 뒤 쪽(M-1) · 확인 직후 `Ctrl+Enter` 반복 무시(M-3). 문서 화면 쪽 왕복(3차 `지급 대상으로` · `다음 확인 전 {번호}`)과 `[증빙 전체 ▾]` 필터, S3 제자리 확인은 06-20.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 실제 이체액이 계산값과 다른 건(이체 수수료 등)을 일괄 흐름 안에서 적어야 D-604 · D-605가 성립하고, 화면의 합이 서버 값이어야 은행 합계와 대조할 수 있다.

Output: `sumSelectedTransfers` · 이체액 편집 · 지급일 재계산 막힘 · `Space` · 페이지 · 좁은 폭, `getEvidenceConfirmView` · `getEvidenceConfirmViewAction` · `ui/confirm-dialog` `attachments` 슬롯(SP-7) · `EvidenceConfirmDialog` · `sessionStorage` 보관, 단위 2 · 통합 1(확장) · E2E 1(확장).

**규모 근거:** `files_modified` 14개, 태스크 4, `estimate.tokens` 115000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-08의 둘째 몫. rev 9에서 Task 4(제자리 증빙 확인 · 보관)와 `ui/confirm-dialog` 두 파일이 더해져 80000 → 115000, 태스크 3 → 4(plan-checker 기준 경고 — 차단 아님). 쪼개지 않은 이유: 제자리 확인 · 보관 · 결과 알림이 모두 `payment-targets-table.tsx` 한 파일의 선택 · 합계 번호 상태를 고치고, 반영 지시 5가 이 플랜에 두라고 했다. 파일 수를 14에 묶으려고 모달 컴포넌트를 새 파일로 만들지 않고(`payment-targets-table.tsx`의 이름 있는 export), 문서 화면 쪽 왕복 · 증빙 필터는 06-20으로 보냈다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-15-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-06-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-17 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-15 | `completePaymentsBatch` · `listPaymentTargets` · `selectable` variant | `grep -c "selectable" ui/table/Table.tsx` 0이면 멈춤 |
| 06-02 | `sumKrw` · `diffKrw` | `grep -c "export function sumKrw\|export function diffKrw" domain/money/index.ts` 2 미만이면 멈춤 |
| UA-604 | `ui/table/use-grid-keyboard.ts` 편집 표 키 규약 | `git ls-files ui/table/use-grid-keyboard.ts` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx` · `ConfirmDialog.module.css`(Task 4가 슬롯을 더한다) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx ui/confirm-dialog/ConfirmDialog.module.css` 결과가 두 줄이 아니면 멈춤 |
| SP-7 · 06-01 | SYSTEM.md §7-17에 「첨부 보기 칸」(06-01 Task 2가 옮긴다) | `grep -c "첨부 보기 칸" docs/design/SYSTEM.md` 0이면 멈춤 |
| 06-11 (r2 B-1 · r3 X-1) | PM 증빙 변경 훅이 결재 통과 문서에서 확인 기록 유무와 관계없이 문서 `version`을 올린다(`bumpExpenseVersion` — 통과 전 문서는 올리지 않는데, 모달은 결재 통과 `확인 전` 행에서만 열린다) — Task 4의 모달 version 확인이 여기에 기댄다 | `grep -c "bumpExpenseVersion" domain/evidence-reviews/index.ts` 0이면 멈춤 · 06-11 SUMMARY에 통합 「PM 증빙 변경은 문서 version을 올린다」 녹색 줄이 없으면 멈춤 |
| 06-06 | `confirmEvidenceAction`(금액 없는 갈래)이 새 문서 `version`과 응답 상태를 돌려준다(06-06 Task 1 behavior) | `grep -c "confirmEvidenceAction" 'app/(app)/expenses/[id]/actions.ts'` 0이면 멈춤 · 06-06 SUMMARY의 응답 모양에 `version`이 없으면 멈춤 |
| A-608 | Phase 5 첨부 파일 읽기 경로(파일 행 · 썸네일 · 원본 열기 — 제자리 확인 모달의 첨부 보기 칸) | 06-06 SUMMARY ⓪이 적은 증빙 함수 이름을 읽는다 · `grep -rln "evidence" domain/expenses/` 0건이면 멈춤 |
| M-9 · UA-605 · UA-606 | **실행 착수 게이트(rev 9):** Phase 5 UI-SPEC이 있고 06-UI-SPEC 「Phase 4·5 의존 가정 (UI)」의 UA-605(지출결의 목록 · 문서 화면) · UA-606(첨부 영역)과 대조를 마쳤다 — S1 표 · 50건 페이지 · 번호 링크 `from=pay`와 제자리 확인 모달의 첨부 보기 칸(SP-7 — 첨부 영역의 파일 행 모양을 읽기 전용으로 쓴다)이 이 줄들에 기댄다 | `git ls-files .planning/phases/05-expense-approval-leave/05-UI-SPEC.md` 비면 멈춤 · 대조 표(UA-605 · UA-606 줄마다 Phase 5 UI-SPEC의 라우트 · 필터 이름 · 문서 화면 섹션 순서 · 첨부 영역 파일 행 모양가 06-UI-SPEC과 같은지 · 다르면 어디)를 SUMMARY에 적고, 어긋난 줄이 하나라도 있으면 코드를 바꾸지 않고 `선행 의존 M-9 미해소 — 06-UI-SPEC {UA-id} 먼저 고침, 06-17 착수하지 않음`을 출력하고 멈춘다 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-3 | 모달 지급일은 방금 고른 날을 기억(필터 변경 · 새로 고침 때 잊음) | `batch-payment-dialog.tsx` 상태 한 곳 |
| O-18 (닫힘) | 고른 건 합 · 차이는 서버 액션 | 해당 없음 |
| 합 요청 모음 | 300ms 모아 보냄(계획 판단) | `payment-targets-table.tsx` 상수 한 줄 |
| **O-21** (사용자 확정(2026-09-25) — DR-4 「예」) | ≥1024 `확인 전` 행 증빙 칸 3차 = 제자리 확인 모달 | **Task 4 ③ 한 갈래**: 답이 「아니오」면 3차를 문서 화면 `?from=pay` 링크로만 두고(DR-5 대안 — Task 4 ④ 보관이 편집 · 선택을 받는다) `EvidenceConfirmDialog` · `getEvidenceConfirmView` · `attachments` 슬롯을 만들지 않는다 |
| 보관 키 | 보기 + 필터(`pay` · 팀) — 06-20이 증빙 필터 값을 더한다 | `payment-targets-table.tsx` 키 함수 한 곳 |

<assumption_delta_decision>
`detected: false`. 06-15 계약에 편집 · 합 · 키 · 페이지를 **더한다**. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-09 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 1 · D-604 · D-605 · O-3 · O-18. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 이체액을 고친 한 행이 저절로 골라지고 서버가 준 차이 · 합이 서며, 사유와 함께 일괄 처리되어 지급 기록에 이체액 · 차이 · 사유가 남는다</name>
  <files>domain/payments/targets.ts, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/batch-payment-dialog.tsx, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, test/unit/domain/payments-targets.test.ts, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S1 「열(≥1280, 10열)」 · 「이체액·차이 사유 편집」 · 「합계 줄」(합계 번호) · Copywriting 「Error — 이체액 칸」 · 「Error — 차이 사유 칸」 · 「거부 — 이체액 합계 응답 없음」 (rev 9에서 줄 번호가 밀려 절 이름으로 찾는다)
    - app/(app)/expenses/payment-targets-table.tsx · batch-payment-dialog.tsx(06-15)
    - domain/money/index.ts(`sumKrw` · `diffKrw`)
  </read_first>
  <behavior>
    - `sumSelectedTransfers(viewer, { seq, rows })` → `{ seq, totalKrw, diffs, rowStates }` — 합은 `sumKrw`, 행별 차이는 `diffKrw(이체액, 지급 총액)` / 선택 불가 행 id가 섞이면 그 행은 빼고 더한다 / 지급 권한 없음 → 거부
    - [r2 F-2] 「고른 행 상태 일괄 조회」: 두 쪽에 걸친 id(51번째 행 포함) + 처리된 id + 게이트로 막힌 id를 한 번에 보내면 `rowStates`에 행마다 `{ id, selectable, version, payableKrw }` — P4 행만 `selectable: true`, `version` · `payableKrw`는 DB 값 / 지급 대상 조회 범위 밖 id(없는 id · 지급 대기에 들 수 없는 문서)는 `rowStates`에 싣지 않는다
    - 이체액 ≠ 지급 총액 · 사유 빔 → 처리 요청 zod 거부(그 행 칸 오류)
    - 이체액 · 사유가 있는 스냅숏 → 지급 기록에 이체액 · 차이 · 사유(06-04 단건 경로)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

`domain/payments/targets.ts`에 `sumSelectedTransfers`(지급 권한 필수 · 행 id로 서버가 지급 총액을 다시 읽고 편집 이체액과 `diffKrw` · 합 `sumKrw`, 받은 id마다 행 상태 `rowStates` — `selectable`은 합에서 선택 불가 행을 빼는 판정과 같은 것(06-15 `selectable` 판정 그대로, 새 판정 없음) · `version` · 지급 총액. 지급 대상 조회 범위 밖 id는 싣지 않는다 — r2 F-2, Task 4 되살리기가 쓴다)와 액션 `sumSelectedTransfersAction`(`actions.ts` · `actions.registry.ts`)을 더한다. `payment-targets-table.tsx`: P4 행에만 이체액 칸(편집 표 키 그대로 — Enter 편집 · Esc 되돌림 · Tab/방향키, 숫자 쉼표 자동), 기본값 = 지급 총액, 고르지 않은 P4 행을 고치면 선택. 고른 행 id 집합이나 고른 행 이체액이 바뀔 때만 합계 번호를 1 올리고 300ms 모아 보내며, 응답 번호 ≠ 지금 번호면 버린다. 응답의 행별 차이로 셀 2행 `차이 {±}`와 사유 칸 편집 가능 · 필수를 켠다 — 화면은 금액을 더하거나 빼지 않는다(건수만 센다). 모달 스냅숏(06-15 R-2)에 편집 이체액 · 사유를 싣는다.

단위 `payments-targets.test.ts`(신규)에 합 · 차이 · 선택 불가 제외, 통합에 behavior 둘째 줄 「고른 행 상태 일괄 조회」(r2 F-2)와 넷째 줄, E2E에 「이체액 수정 → 자동 선택 + 차이 2행 + 사유 필수 → 사유 → 합 → 처리 → 문서 화면 S5 이체액 · 차이 · 사유」 한 경로.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/payments-targets.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. 「고른 행 상태 일괄 조회」가 빨가면 되살리기가 다른 쪽 선택을 살릴 값(`selectable` · `version` · 지급 총액)을 받지 못한다(r2 F-2)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다. `plant8/money-boundary` 위반이면 화면이 금액 셈을 했다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 합 응답이 서버 `sumKrw` 값이고 선택 불가 행이 빠짐을 단언한다
    - [r2 F-2] `grep -c "고른 행 상태 일괄 조회" test/integration/payment-batch.test.ts`가 1이고 녹색이다 — 51번째 행 id의 `selectable` · `version` · `payableKrw`와 범위 밖 id가 `rowStates`에 없음을 단언한다
    - `grep -n "reduce\|+=" 'app/(app)/expenses/payment-targets-table.tsx'` 결과가 금액 변수에 쓰이지 않음을 SUMMARY에 줄마다 적는다(건수 셈만)
    - ⓪ 결과에 M-9 대조 표(UA-605 · UA-606 줄마다 같음/다름)가 SUMMARY에 있다(rev 9)
  </acceptance_criteria>
  <done>이체액 편집 한 건이 서버 차이 · 합 → 사유 → 일괄 처리 → 지급 기록까지 실제로 통과하고, 화면에 금액 셈이 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 지급일 재계산 막힘 · 지급일 기억(O-3) · 합 실패 · 모달 부제 합 · 결과 줄 `다른 쪽 · 차이 있음`(H-5) · 일괄 결과 알림 · 막힌 행 선택(DR-6 · H-3)</name>
  <files>domain/payments/batch.ts, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/batch-payment-dialog.tsx, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S2. 일괄 지급 완료」 절(「모달을 두는 이유(H-5)」 · 지급일 재계산) · 「S1 일괄 결과 알림 · 막힌 행 선택」 · Copywriting 「거부 — 일괄 지급 건별 결과」 · 「Destructive — 일괄 지급 완료」(결과 줄 ③) · 「표시 — 일괄 지급 결과」 · UI Considerations `S1 일괄 결과 알림` 네 행 · S2 partial 행
    - domain/payments/index.ts의 `PayableChangedError`(06-03) · 06-15 SUMMARY(`completePaymentsBatch` 결과의 막힌 행 `selectable` — H-3)
  </read_first>
  <behavior>
    - 모달 지급일이 원천징수 세율이 바뀌는 날 → 그 행 `blocked` + `newPayableKrw` + 이유 / 다른 행 처리
    - 막힘 행을 새 값으로 재시도 → 처리
    - 모달을 닫고 다시 열면 지급일 = 방금 고른 날 / 필터 변경 뒤 → 오늘
    - 합 액션 실패 → 「거부 — 이체액 합계 응답 없음」 + `다시 시도`(같은 번호) · 모달 1차 `aria-disabled`
    - 결과 줄 ③(H-5, 테스트 이름 「다른 쪽 N건 포함」): 지금 쪽 밖에서 고른 건이 있으면 `다른 쪽 N건 포함`, 차이 2행이 선 고른 행이 있으면 `차이 있음 N건`, 둘 다면 ` · `로 한 줄, 둘 다 없으면 줄 없음
    - 일괄 결과 알림 · 막힌 행 선택(DR-6 · H-3, 테스트 이름 「일괄 결과 알림 · 막힌 행 선택」): 처리 전부터 S1에 빈 `aria-live="polite"` 영역이 있고 처리 뒤 결과 글자가 그 안에 쓰인다 / 막힌 행 체크박스 `aria-describedby`가 증빙 칸 글자 id와 이유 줄 id 둘을 가리킨다 / 모달이 닫히면 포커스 = 첫 막힌 행 체크박스, 막힘 0이면 1차 버튼 / 게이트로 막힌 행(`selectable: false`) → 체크 풀림 · 편집 칸 사라짐 · N에서 빠짐, 지급일 세율로 막힌 행(`selectable: true`) → 체크 유지 · N에 듦
  </behavior>
  <action>
`completePaymentsBatch` 결과에 `PayableChangedError`의 새 지급 총액을 `newPayableKrw`로 싣는다. 화면은 그 행의 지급 총액 셀과 이체액 기본값(사람이 고치지 않았을 때만)을 새 값으로 바꾸고 이유를 행 아래에 둔다 — 재시도 스냅숏의 `expectedPayableKrw`는 새 값이다. 모달 지급일은 모달 밖 상태 한 곳에 기억하고 필터 변경 · 새로 고침 때 잊는다(O-3). 모달 부제는 모달을 연 순간 번호의 합만 쓰고, 아직이면 `…` + 1차 `aria-disabled`(`Ctrl+Enter` 무반응).

**H-5.** 모달 결과 줄 ③은 모달을 연 스냅숏으로 화면이 센다 — 지금 쪽에 없는 고른 id 수 = `다른 쪽 N건 포함`, 서버가 준 차이가 0이 아닌 고른 행 수 = `차이 있음 N건`. 둘 다 개수라 화면이 세며(O-18 금액 셈 아님), 해당 묶음만 쓰고 없으면 줄을 두지 않는다(Copywriting 「Destructive — 일괄 지급 완료」 결과 줄 ③ 문구 그대로).

**DR-6 · H-3.** 결과 글자 영역은 S1 첫 렌더부터 DOM에 있는 빈 `aria-live="polite"` 요소이고 처리 응답 뒤 그 안에 글자를 쓴다(조건부 렌더로 새로 만들면 스크린 리더가 읽지 않는다 — SYSTEM §10 「저장 결과 `aria-live`」와 같은 결). 막힌 행 이유 줄에 id를 주고 그 행 체크박스 `aria-describedby`에 증빙 칸 글자 id와 함께 넣는다. 모달이 닫히면 첫 막힌 행 체크박스(`aria-disabled`여도)로, 막힘 0이면 1차 버튼으로 포커스를 옮긴다. 선택을 풀거나 두는 것은 06-15가 만든 응답의 `selectable`을 그대로 따른다 — 이 태스크가 새로 판정하지 않고, 풀린 행의 편집값은 버린다.

E2E: 지급일을 세율 이력이 바뀌는 날로 골라 막힘 → 새 지급 총액 → 재시도 성공 · 지급일 기억. 「다른 쪽 N건 포함」: 51건 이상에서 첫 쪽 한 건 + 둘째 쪽 한 건을 고른 뒤 모달 → `다른 쪽 1건 포함`, 이체액을 고친 행을 섞으면 `차이 있음 1건`, 한 쪽 안에서 차이 없이 고르면 결과 줄 ③ 없음. 「일괄 결과 알림 · 막힌 행 선택」: 게이트 막힘 한 건(domain 호출로 확인을 풂) + 지급일 세율 막힘 한 건 + 처리될 한 건을 함께 처리.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/expense-payments.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — 「일괄 결과 알림 · 막힌 행 선택」이 빨가면 결과 글자 영역이 처리 뒤 새로 생겼거나(읽히지 않음) 게이트로 막힌 행이 체크된 채 남았고, 「다른 쪽 N건 포함」이 빨가면 모달이 보이지 않는 선택을 말하지 않는다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 「세율 경계 지급일 → 막힘 + 새 값」 · 「새 값 재시도 → 처리」를 개별 `it`으로 단언한다
    - E2E가 behavior 여섯 줄을 단언한다
    - E2E 「다른 쪽 N건 포함」이 세 갈래(다른 쪽만 · 차이만 · 둘 다 없음 → 결과 줄 ③ 요소 0)를 단언한다(H-5)
    - E2E 「일괄 결과 알림 · 막힌 행 선택」이 ⑴ 처리 전 S1에 빈 `[aria-live="polite"]` 요소 1개 ⑵ 처리 뒤 같은 요소의 글자가 `{시:분} 지급 완료 1건 · 막힘 2건` 꼴 ⑶ 막힌 행 체크박스 `aria-describedby`의 id 둘이 증빙 칸 글자 · 이유 줄을 가리킴 ⑷ 모달 닫힌 뒤 `document.activeElement` = 첫 막힌 행 체크박스 ⑸ 게이트 막힘 행 `checked` 거짓 · 지급일 세율 막힘 행 `checked` 참 · 1차 라벨 `지급 완료 1`을 단언하고, 막힘 0 케이스에서 포커스 = 1차 버튼을 단언한다(DR-6 · H-3)
    - `grep -c "일괄 결과 알림 · 막힌 행 선택" test/e2e/payment-batch.spec.ts`와 `grep -c "다른 쪽 N건 포함" test/e2e/payment-batch.spec.ts`가 각 1 이상
  </acceptance_criteria>
  <done>지급일로 바뀐 지급 총액이 조용히 처리되지 않고, 재시도와 지급일 기억이 계약대로 돌며, 모달이 보이지 않는 선택 · 차이를 말하고, 일괄 결과가 스크린 리더에 읽히며 게이트로 막힌 행은 선택이 풀린다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `Space` · 50건 페이지 · 쪽 넘는 선택 · 처리 뒤 쪽(M-1) · 좁은 폭 · 링크 셀(H-2) · 표 안 `Ctrl+Enter`(DR-8) · 선택 상태 순수 함수</name>
  <files>ui/table/Table.tsx, ui/table/use-grid-keyboard.ts, domain/payments/targets.ts, app/(app)/expenses/payment-targets-table.tsx, test/unit/ui/table-selectable.test.ts, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S1 「열(≥1280, 10열)」(번호 = 식별자형 링크) · 「열 우선순위」 · 「좁은 PC」 · 「링크 셀과 편집 표 키(H-2)」 · 「폰」 · 「S1 일괄 결과 알림 · 막힌 행 선택」의 「쪽(M-1)」 · Copywriting 「단축키(D-94)」(DR-8 · 링크 셀 `Enter`) · 「3차 버튼」(번호 링크 · 접근 이름) · §7-16 페이지
    - ui/table/use-grid-keyboard.ts · test/e2e/keyboard-nav.spec.ts(키 규약 선례)
  </read_first>
  <behavior>
    - 선택 상태 순수 함수: 머리글 토글 = 이 쪽의 고를 수 있는 행 전체 · 섞이면 indeterminate · 쪽 이동 뒤에도 id 유지 · 필터 변경 시 비움 · 고르지 않은 P4 행 이체액 변경 → 자동 선택 · **처리 결과 적용**(처리된 id를 모든 쪽 선택 · 편집에서 지우고 `selectable: false`인 막힌 행의 선택 · 편집을 푼다 — H-3 · M-1) · **되살리기**(보관한 선택 · 편집과 합 응답 `rowStates`를 받아 `selectable: true`인 행 것만 남긴다 — 지금 쪽 밖 id도 남고, `rowStates`에 없거나 `selectable: false`인 행의 보관값은 버린다. C-1 · r2 F-2, Task 4가 쓴다) · **「다른 쪽 id 유지」**(r2 F-2): 지금 쪽에 없는 id가 `rowStates`에서 P4면 선택 · 편집이 그대로 남는다
    - 처리 뒤 쪽(M-1, 테스트 이름 「처리 뒤 쪽 다시 받기」): 같은 쪽을 다시 받고, 그 쪽이 비었으면 행이 있는 마지막 쪽으로 / 모든 쪽이 비면 「Empty — 지급 대상」 / 이유 줄은 행 id로 기억해 다시 받은 쪽에 그 행이 있으면 붙인다
    - 링크 셀(H-2, 테스트 이름 「링크 셀 Enter 열기」): ≥1280 번호 셀 = `<a>` 식별자형 링크 `/expenses/{id}?from=pay&{보기 · 팀 · 쪽}` · 접근 이름 `{번호} 지출결의 열기` / 1024~1279 · 700~1023 = 「프로젝트 · 항목」 1행이 같은 링크 / 방향키로 링크 셀에 닿아 `Enter` → 문서 화면(편집 칸이 아님)
    - 표 안 `Ctrl+Enter`(DR-8, 테스트 이름 「표 안 Ctrl+Enter는 화면 1차」) → 새 줄이 생기지 않고 S2 모달이 열린다
    - `Space` → 현재 행 선택 토글(선택 불가 행 무반응)
    - `listPaymentTargets({ page })` 50건 · 합계 줄 = 필터 전체(서버)
    - 1024~1279: 번호 · 거래처 열 없음 / 700~1023: 선택 열 · 편집 · 1차 없음 / 700 미만: 행 → 문서 화면 링크
  </behavior>
  <action>
선택 상태 계산을 `ui/table`의 순수 함수로 뽑아(`Table.tsx` 옆 export — 새 파일을 만들지 않는다) 단위 테스트한다. 선택 상태 순수 함수에 「처리 결과 적용」(처리된 id 제거 · `selectable: false` 선택 · 편집 해제 — 06-15 응답 그대로, H-3)과 「되살리기」(보관값 + 합 응답 `rowStates` → `selectable`인 행 것만, 지금 쪽 밖 id 포함 — C-1 · r2 F-2. 지금 쪽 목록으로 거르지 않는다)를 더한다. `use-grid-keyboard.ts`: ⑴ `Space` = 현재 행 고르기(`selectable` 표에서만 — 다른 편집 표는 이전 동작 그대로). ⑵ 링크 셀(번호 셀 · 「프로젝트 · 항목」 1행 링크 · 증빙 칸 3차 — 편집 칸 판정과 따로 받는 인자, 이름은 기존 훅 인자 꼴을 따른다)에서 `Enter` = 그 셀의 링크 · 버튼 누르기(H-2 — 편집 칸의 `Enter 편집`은 그대로). ⑶ `selectable` 표에서는 `Ctrl+Enter`를 새 줄(`onNewRow`)로 쓰지 않고 화면 1차 콜백으로 넘긴다 — `selectable`이 없는 표(견적 줄 표)의 새 줄 동작은 그대로(DR-8). 힌트 줄 `이동 Tab ↑↓←→ · 고르기 Space · 취소 Esc`. 번호 셀 · 「프로젝트 · 항목」 1행 링크는 `<a href>`라 가운데 클릭 · `Ctrl`+클릭이 브라우저 기본대로 새 탭을 연다(막지 않는다). 링크 쿼리 `from=pay`에는 지금 보기 · 팀 · 쪽만 싣는다(증빙 필터 값은 06-20이 더한다). **M-1:** 처리(S2) 응답 뒤 같은 쪽을 다시 받고(서버가 보낸 전체 건수로 마지막 쪽을 셈 — 개수 셈), 지금 쪽이 마지막 쪽보다 뒤면 마지막 쪽으로 옮긴다. 처리된 id는 「처리 결과 적용」으로 다른 쪽 선택에서도 지운다. `listPaymentTargets`에 `page`(50건, `Pagination`)와 필터 전체 합계. 열 우선순위와 폭 갈래는 UI-SPEC 표대로 — 700~1023에서는 `selectable`을 켜지 않는다. 토큰은 `tokens.css`의 것만.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/ui/table-selectable.test.ts test/unit/domain/payments-targets.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다. 「다른 쪽 id 유지」가 빨가면 되살리기가 지금 쪽 목록으로 걸러 다른 쪽 선택을 조용히 버린다(r2 F-2)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/keyboard-nav.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다. keyboard-nav · quote-table이 빨가면 `ui/table` 변경이 기존 편집 표를 깨뜨렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 ⑴ `Space`로 고르기 ⑵ 51건에서 둘째 쪽으로 가도 첫 쪽 선택 유지 · 합이 두 쪽 합 ⑶ 800px 폭에서 선택 열 요소 0개 ⑷ 600px 폭에서 행 링크를 단언한다
    - E2E 「링크 셀 Enter 열기」가 1280에서 번호 셀 링크의 접근 이름(`{번호} 지출결의 열기`) · href(`from=pay` 포함), 방향키로 닿아 `Enter` → 문서 화면 URL, 1100px에서 번호 열 없음 · 「프로젝트 · 항목」 1행 링크 존재를 단언한다(H-2)
    - E2E 「표 안 Ctrl+Enter는 화면 1차」가 표 셀 포커스에서 `Ctrl+Enter` → 행 수 그대로 · S2 모달 열림을 단언한다(DR-8)
    - E2E 「처리 뒤 쪽 다시 받기」가 둘째 쪽(51~)의 행을 전부 골라 처리 → 첫 쪽으로 옮겨 가고 「Empty — 지급 대상」이 서지 않음 · 처리된 id가 선택 · 1차 N에서 빠짐을 단언한다(M-1)
    - 단위 table-selectable에 「처리 결과 적용」 · 「되살리기」 · 「다른 쪽 id 유지」(r2 F-2) 케이스가 있다 — `grep -c "다른 쪽 id 유지" test/unit/ui/table-selectable.test.ts`가 1 이상
    - 기존 keyboard-nav · quote-table 스펙 녹색
  </acceptance_criteria>
  <done>키보드만으로 고르고 문서를 열며(링크 셀 `Enter`), 표 안 `Ctrl+Enter`가 화면 1차이고, 쪽을 넘어도 선택이 남고 처리 뒤 빈 쪽에 머물지 않으며, 좁은 폭에서 일괄 지급이 안전하게 빠진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 제자리 증빙 확인(DR-4 · O-21) — 확인 모달 + 첨부 보기 칸(SP-7) · 응답 version 갱신 · 저절로 골라짐, 편집 · 선택 보관(`sessionStorage`, C-1) · 확인 직후 `Ctrl+Enter` 반복 무시(M-3)</name>
  <files>ui/confirm-dialog/ConfirmDialog.tsx, ui/confirm-dialog/ConfirmDialog.module.css, domain/payments/targets.ts, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/actions.ts, app/(app)/expenses/actions.registry.ts, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」의 UA-602 · SP-7 · 06-11(r2 B-1) · 06-06 · A-608 줄이 main에 있다 — SYSTEM.md §7-17에 「첨부 보기 칸」이 있고, 06-11 훅이 결재 통과 문서의 PM 증빙 변경마다 문서 `version`을 올리며, 06-06 `confirmEvidenceAction` 응답에 새 문서 `version`이 실린다</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S1 제자리 증빙 확인 — 확인 모달 + 첨부 보기 칸(SP-7)」 · 「S1 문서 화면 왕복 — `from=pay` · 편집·선택 보관(보조 경로)」의 「보관」 · 「되살리기」 · 「지우는 때」 · S1 머리 스토리보드 3~4단계 · 「SP-7」 절 · Copywriting 「확인 — 증빙 확인(S1 · S3 제자리)」 · 「거부 — 증빙 확인 모달(S1 · S3)」 · 「SUCCESS — 제자리 결과(토스트 없음)」(M-3 문장) · UI Considerations `S1 제자리 증빙 확인` 일곱 행 · 열린 선택 O-21
    - ui/confirm-dialog/ConfirmDialog.tsx · ConfirmDialog.module.css(슬롯 자리 · `initialFocusTarget`) · docs/design/SYSTEM.md §7-17(06-01이 옮긴 SP-7 문장) · §7-8 시트 상세 첨부 행
    - 06-06 SUMMARY(`confirmEvidenceAction` 입력 · 응답 모양, S4 서버 계산 한 줄을 만드는 함수 이름, ⓪이 적은 Phase 5 증빙 · 첨부 함수 이름 — A-608)
    - app/(app)/expenses/payment-targets-table.tsx(Task 1~3 결과 — 선택 · 합계 번호 · 편집 · 순수 함수 「되살리기」 · 합 응답 `rowStates`)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S1 제자리 증빙 확인」의 「서버(r2 B-1)」 · 「실패」 줄과 「S1 문서 화면 왕복」의 「되살리기」(r2 F-2) 줄 · 금액 표시 절 「낙관적 잠금」
    - 06-11 SUMMARY(r2 B-1 — PM 증빙 변경 훅의 `bumpExpenseVersion` 호출 자리, RED에서 잠시 뺄 줄) · 06-06 SUMMARY(「거부 — 문서 화면 동시성」의 사람 · 시각 출처)
  </read_first>
  <behavior>
    - `getEvidenceConfirmView(viewer, { expenseId, rowVersion })`(통합 `it` 「증빙 확인 보기 — 권한 · 확인 전만」): 지급 권한 + 증빙 있는 `확인 전`(P2 · P5) 문서 → 파일 행(파일명 · 장수 · 용량 · 썸네일 · 원본 열기 경로) · 증빙 금액 · 서버 계산 한 줄(지급 전 문서만) · 지금 `version` / `rowVersion` ≠ 지금 `version` → 같은 보기 + 동시성 이유(바꾼 사람 · 시각 — 06-06 동시성 거부와 같은 출처, r2 B-1) / 지급 권한 없음 → 거부 / `확인 전`이 아닌 문서 → 거부
    - E2E 「제자리 증빙 확인」: ≥1280 `확인 전` 행 증빙 칸 3차 `증빙 확인` → 모달(제목 `증빙 확인` · 부제 · 첨부 보기 칸 파일 행 · 결과 줄 · 1차 `증빙 확인` `Ctrl+Enter`) → `Ctrl+Enter` → 모달 닫힘 · 그 행 증빙 칸 `확인됨` · 체크박스 `checked` · 포커스 = 그 행 이체액 셀 · 토스트 없음 · 1차 `지급 완료 N+1` → 1차 → S2 → 확인 → 그 행이 처리됨(동시성 막힘 없음 = 응답 version으로 행을 바꿨다)
    - E2E 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」(r2 B-1): ⑴ 모달이 파일 A 한 행으로 선 뒤 domain 호출(Phase 5 PM 증빙 추가 함수)로 파일 B를 더함 → `Ctrl+Enter` → 확인 거부(version 불일치) · 모달 유지 · 막힘 이유 `{사람}이 {시:분}에 바꿈 · 새로 고침` · 그 행 증빙 칸 그대로 `확인 전` → `새로 고침` → 모달이 파일 A · B 두 행 · 새 결과 줄로 다시 섬 · 다른 행의 선택 · 이체액 편집 그대로 → `Ctrl+Enter` → 확인 성공 · 그 행 `확인됨` · 저절로 골라짐 ⑵ 목록을 받은 뒤 domain 호출로 PM이 증빙 금액을 고침 → 3차 `증빙 확인` → 모달이 새 금액으로 서고 1차 `aria-disabled` + 동시성 문구(`Ctrl+Enter` 무반응) → `새로 고침` → 1차 켜짐 ⑶ 모달을 연 뒤 domain 호출로 다른 경영관리가 먼저 확인 → `Ctrl+Enter` → 거부 → `새로 고침` → 모달 닫힘 · 그 행 `확인됨` · 체크박스 `checked` 거짓 · 포커스 = 그 행 링크
    - 불러오기 실패(보기 액션 오류 주입) → `증빙을 불러오지 못함 · 다시 시도` + 1차 `aria-disabled`
    - E2E 「확인 직후 Ctrl+Enter 반복 무시」(M-3 · r2 F-4 순서): 모달에서 `keyboard.down('Control')` · `keyboard.down('Enter')`(첫 누름 — 확인) → **모달이 닫히고 포커스 = 그 행 이체액 셀이 될 때까지 기다린다**(키는 떼지 않는다) → `Control`을 누른 채 `keyboard.down('Enter')` 한 번 더(Playwright가 이미 눌린 키라 `repeat: true`로 보낸다 — 표 안 `Ctrl+Enter` = 화면 1차 자리) → S2 `role=dialog` 0개 · 1차 라벨 `지급 완료 N+1` 그대로 · 지급 기록 0 → 키를 떼고 새로 `Control+Enter` → S2 모달이 열린다(가드가 새 누름까지 막지 않음)
    - E2E 「보관 되살리기」(C-1): 행 A 이체액 편집 + 행 B 선택 → 번호 링크로 문서 화면 → 같은 탭에서 S1로 돌아옴 → A 편집값 · B 선택이 묻지 않고 되살아나고 합이 다시 불린다 / [r2 F-2 두 쪽 갈래] 51건 이상에서 첫 쪽 행 A 이체액 · 차이 사유 편집 + 둘째 쪽 행 C 선택 → 둘째 쪽에서 번호 링크로 문서 화면 → 돌아옴 → A · C 둘 다 살아 있고 1차 `지급 완료 2` · S2 결과 줄 ③ `다른 쪽 1건 포함` → 그대로 처리하면 A · C 모두 막힘 0으로 처리(지금 쪽 밖 행의 `version` · 지급 총액이 합 응답 값) / 보관 뒤 domain 호출로 처리된 행은 되살아나지 않는다 / 필터를 바꾸면 그 키가 비어 되살릴 것이 없다 / 처리 뒤 처리된 행 · 게이트로 막혀 풀린 행의 보관값은 없다
    - 700~1023: 증빙 칸 3차가 모달이 아니라 문서 화면 `?from=pay` 링크 / 슬롯을 넘기지 않은 S2 모달에 첨부 보기 칸 요소 0
  </behavior>
  <action>
DR-4(O-21 「예」 — 사용자 확정(2026-09-25)) · C-1 ② · M-3을 반영한다. 모든 화면 문구는 UI-SPEC rev 9 Copywriting 행 그대로 쓴다.

① **SP-7 슬롯.** `ui/confirm-dialog/ConfirmDialog.tsx`에 선택 prop `attachments`(파일 행 배열 — 행마다 파일명 · 장수 · 용량 표시 글자 · 썸네일 src · 원본 열기 href)를 더한다. 자리 = 부제 아래 · 결과 줄 위. 행 모양은 SYSTEM §7-8 시트 상세 첨부 행 그대로(썸네일 72×96 + 파일명 · 장수 · 용량 + 3차 `크게 보기` — 새 탭(`target="_blank"` · `rel="noopener"`)으로 원본, 모달과 그 뒤 화면의 상태는 그대로). 보기만 — 올리기 · 삭제 · 입력 요소 없음. 파일이 셋을 넘으면 칸 높이를 행 셋에서 멈추고 칸 안만 스크롤(제목 · 부제 · 행동 줄 고정), 폰 시트 골격(§7-17)에서도 본문 안. 불러오는 동안은 호출자가 `…` 한 줄을 넘긴다. 긴 파일명은 한 줄 말줄임 + `title`. 스타일은 `ConfirmDialog.module.css`에 `tokens.css` 변수만(새 색 · 서체 · radius · 그림자 0). `ui/`는 `app/` · `domain/`을 import하지 않는다 — 행 데이터만 받는다. `attachments`가 없으면 이전과 같은 DOM(S2 · 지급 취소 · 증빙 면제 등 기존 사용처 불변).

② **서버.** `domain/payments/targets.ts`에 `getEvidenceConfirmView(viewer, { expenseId, rowVersion })` — 트랜잭션 없는 읽기, `can(viewer, "expenses.payments", "write")` 필수(D-601), `resolveExpenseActionRow`가 P2 · P5가 아니면 거부(모달을 열 이유가 없는 문서). 파일 행은 Phase 5 첨부 읽기 경로(A-608 — 06-06 SUMMARY ⓪이 적은 함수)로, 서버 계산 한 줄은 06-06 Task 3 S4와 같은 경로(`loadPaymentInputs` → `decidePayable`, 지급 전 문서만 — 새 계산 없음, O-18)로 만든다. 액션 `getEvidenceConfirmViewAction`(`app/(app)/expenses/actions.ts`)과 DTO 등록(`actions.registry.ts` → 누수 스캔). 보기는 지금 `version`을 싣고, `rowVersion`(화면이 가진 그 S1 행의 `version`)과 다르면 동시성 이유(바꾼 사람 · 시각 — 06-06 동시성 거부가 쓰는 출처 그대로, 새 조회 경로를 만들지 않는다)를 함께 싣는다(r2 B-1). 확인 자체는 새 액션을 만들지 않는다 — 06-06 `confirmEvidenceAction`(`app/(app)/expenses/[id]/actions.ts`)의 금액 없는 갈래를 **보기가 준 `version`**으로 그대로 부른다(새 판정 없음, D-602). 행의 `version`을 보내지 않는다 — 06-11 훅이 PM 증빙 변경마다 `version`을 올리므로(r2 B-1) 보기의 `version`을 보내야 모달을 연 뒤 바뀐 증빙이 06-06의 기존 version 비교에서 거부된다.

③ **화면(S1).** `payment-targets-table.tsx`에 모달 부분을 이름 있는 export `EvidenceConfirmDialog`로 둔다 — 06-20 S3가 같은 것을 import한다(이 플랜 파일 한도 14 때문에 새 파일을 만들지 않는다). ≥1024 `확인 전` 행의 증빙 칸 3차 `증빙 확인`(접근 이름 `{번호} 증빙 확인`, 증빙이 있는 `확인 전` 행에만 — 빈 모달이 열릴 길이 없다)이 모달을 연다 → 열 때 `getEvidenceConfirmViewAction` → 제목 `증빙 확인` · 부제 `{번호} · {프로젝트} · {항목} · 증빙 금액 {금액}` · `attachments` · 결과 줄 = 서버 계산 한 줄 · 1차 `증빙 확인` + `Ctrl+Enter` · 2차 `취소 Esc`. 성공 응답의 새 `version` · 상태로 그 행 데이터를 바꾸고 「상태 → 1차」 표를 다시 읽는다 → P4 → 체크박스 켜짐 + 저절로 선택 + 합계 번호 +1(Task 1 규칙) + 증빙 칸 `확인됨`(다른 확인됨 행과 같은 모양, 2행 없음) + 이체액 칸 편집 가능 + 포커스 그 행 이체액 셀, 토스트 없음. 보기에 동시성 이유가 실려 오면(열 때 받은 `version` ≠ 행 `version`) 1차 `aria-disabled`(`Ctrl+Enter` 무반응) + 막힘 이유 자리에 「거부 — 문서 화면 동시성」 문구. 확인 실패도 모달을 닫지 않고 막힘 이유 자리에 「거부 — 증빙 확인 모달(S1 · S3)」. 두 경우 모두 `새로 고침`은 목록(지금 쪽)과 모달 보기를 다시 받는다(r2 B-1) — 그 문서가 아직 `확인 전`이면 모달이 새 파일 · 증빙 금액 · 결과 줄 · 새 `version`으로 다시 서고(오는 동안 「여는 동안」과 같은 `…` + 1차 `aria-disabled`), `확인 전`이 아니게 됐으면 모달을 닫고 그 행은 새로 받은 상태대로 서며(저절로 골라지지 않는다 — 이 사람이 확인한 것이 아니다) 포커스는 그 행 링크로 간다. 다른 행의 선택 · 편집은 화면 상태 그대로다(목록을 다시 받아도 선택은 행 id로 남는다). 모달이 닫힌 직후의 `Ctrl+Enter`는 `event.repeat`면 무시하고 새로 누른 것만 화면 1차로 받는다(M-3). 700~1023(보기 전용)의 증빙 칸 3차는 모달이 아니라 문서 화면 `/expenses/{id}?from=pay&…` 링크다. `Esc` = 닫기, 포커스는 그 3차로 돌아온다(§7-17). DR-5(3차를 만드는 플랜이 없음)는 이 ③이 닫는다.

④ **보관(C-1 ②).** 선택 · 이체액 · 차이 사유 편집 · 지금 쪽을 `sessionStorage`(탭 수명)에 키 = 보기 + 필터(`pay` · 팀)로, 바뀔 때마다 쓴다 — 행 id와 사람이 적은 값만(계좌 · 이름 · 계산 금액은 싣지 않는다). S1이 같은 탭에서 같은 키로 다시 열리면 묻지 않고, 보관한 행 id 전부(지금 쪽 밖 포함)와 편집 이체액을 합 요청 한 번(합계 번호 +1 — Task 1 규칙)에 실어 보내고, 응답 `rowStates`를 Task 3 순수 함수 「되살리기」에 넘겨 `selectable`인 행 것만 되살린다(r2 F-2 — 지금 쪽 목록으로 거르지 않는다. `복원 / 버림` 줄 없음 — 스스로 떠났다 돌아온 이어 하기, CLAUDE.md §7). 보관은 `version`을 싣지 않으므로 지금 쪽 밖 행의 `version` · 지급 총액은 이 응답 값으로 들고 있다가 S2 스냅숏에 싣는다(S2 1차는 모달을 연 합계 번호의 응답을 기다리므로 늘 채워져 나간다). 처리(S2) 응답 뒤 선택이 남은 지금 쪽 밖 행은 다음 합 응답의 `rowStates`로 `version` · 지급 총액을 바꾼다 — 동시성으로 막혀 선택을 유지한 다른 쪽 행이 옛 `version`으로 다시 막히지 않게(지금 쪽은 Task 3 M-1 재조회가 바꾼다). 지우는 때: 처리(S2) 응답 뒤 처리된 행과 게이트로 막혀 선택이 풀린 행(H-3 — Task 3 「처리 결과 적용」) · 필터를 바꿀 때(그 키 통째로) · 탭을 닫을 때. `beforeunload`는 탭 닫기 · 다른 사이트로 떠날 때만 — 앱 안 이동(`Link`)은 가로채지 않는다(보관이 받는다).

⑤ **테스트.** 통합(`payment-batch.test.ts`)에 behavior 첫 줄 `it` 「증빙 확인 보기 — 권한 · 확인 전만」(`rowVersion` 불일치 갈래 포함). E2E에 behavior 둘째~일곱째 줄(이름: 「제자리 증빙 확인」 · 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」 · 「보관 되살리기」 · 「확인 직후 Ctrl+Enter 반복 무시」). 증빙 행 · PM 증빙 변경은 06-06처럼 Phase 5 domain 함수로 만든다(업로드 화면을 거치지 않는다). RED를 먼저 본다 — 셋, SUMMARY에 RED · GREEN 한 줄씩: ⒜ 모달 성공 뒤 행 version을 바꾸지 않은 채로 「제자리 증빙 확인」의 일괄 처리 단계가 동시성 문구로 빨갛다. ⒝(r2 B-1) 06-11 훅의 `bumpExpenseVersion` 호출 한 줄을 잠시 빼면 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」 ⑴의 첫 `Ctrl+Enter`가 통과해 보지 않은 파일 B가 든 묶음이 `확인됨`이 되어 빨갛다 — 확인하고 그 줄을 되돌린다(이 플랜 파일이 아니므로 `git diff --exit-code domain/evidence-reviews/index.ts`가 0이어야 한다). ⑴은 열 때 보기 `version`과 행 `version`이 같아 둘을 가르지 못한다 — 열 때 비교는 ⑵의 1차 `aria-disabled` 단언이 지킨다. ⒞(r2 F-4) `event.repeat` 가드를 잠시 빼면 「확인 직후 Ctrl+Enter 반복 무시」가 S2 모달이 열려 빨갛다 — 확인하고 되돌린다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/evidence-reviews.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 「증빙 확인 보기 — 권한 · 확인 전만」이 빨가면 모달 보기 액션이 지급 권한 없는 사람이나 `확인 전`이 아닌 문서에 파일 · 금액을 보냈거나 `rowVersion` 불일치에 동시성 이유를 싣지 않았고(r2 B-1), evidence-reviews가 빨가면 제자리 확인이 06-06 경로를 바꿨으며, leak-scan이 빨가면 모달 보기 DTO가 등록되지 않았다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다 — `ui/confirm-dialog`의 기존 단위 테스트가 빨가면 슬롯 추가가 초점 · 라벨 규칙을 바꿨고, boundaries 규칙 위반이면 `ui/confirm-dialog`가 `app/` · `domain/`을 import했다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — 「제자리 증빙 확인」의 일괄 처리 단계가 동시성 문구로 막히면 응답 version으로 행을 바꾸지 않았고, 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」 ⑴의 첫 확인이 통과하면 06-11 훅이 version을 올리지 않거나 모달이 보기 `version`이 아닌 값을 보냈으며(RED 방법: 훅의 `bumpExpenseVersion` 한 줄을 잠시 빼면 이 단계가 빨갛다 — r2 B-1), 「확인 직후 Ctrl+Enter 반복 무시」에서 S2 모달이 열리면 `event.repeat` 가드가 없다(RED 방법: 가드를 잠시 빼면 이 단계가 빨갛다 — r2 F-4), 「보관 되살리기」 두 쪽 갈래가 빨가면 되살리기가 지금 쪽 목록으로 걸렀다(r2 F-2), payment-single이 빨가면 `ConfirmDialog` 변경이 문서 화면 모달(지급 취소 · 증빙 면제)을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "제자리 증빙 확인\|모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시\|보관 되살리기\|확인 직후 Ctrl+Enter 반복 무시" test/e2e/payment-batch.spec.ts`가 4 이상이고, `grep -c "증빙 확인 보기 — 권한 · 확인 전만" test/integration/payment-batch.test.ts`가 1이다
    - [r2 B-1] E2E 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」가 behavior ⑴ ⑵ ⑶ 세 갈래를 단언한다 — ⑴ 첫 확인 거부 · 그 행 `확인 전` 그대로 · `새로 고침` 뒤 모달 파일 행 수 2 · 두 번째 확인 성공, ⑵ 열 때 1차 `aria-disabled="true"` · `새로 고침` 뒤 거짓, ⑶ 모달 닫힘 · 체크박스 `checked` 거짓 · `document.activeElement`가 그 행 링크
    - [r2 B-1] SUMMARY에 `payment-targets-table.tsx`의 `confirmEvidenceAction` 호출 줄과 그 `version` 인자가 보기 응답 값에서 온다는 줄이 있다
    - [r2 F-4] E2E 「확인 직후 Ctrl+Enter 반복 무시」의 둘째 `keyboard.down('Enter')`가 모달이 닫히고 포커스가 이체액 셀로 간 것을 기다린 **뒤**에 있다(스펙에서 두 줄의 순서를 SUMMARY에 적는다)
    - [r2 F-2] E2E 「보관 되살리기」가 두 쪽 갈래(둘째 쪽 선택이 되살아남 · 1차 `지급 완료 2` · 결과 줄 ③ `다른 쪽 1건 포함` · 처리 막힘 0)를 단언한다
    - E2E 「제자리 증빙 확인」이 확인한 행을 포함한 일괄 처리가 막힘 0으로 처리됨을 단언한다(응답 version 갱신)
    - `grep -c "confirmEvidenceAction" 'app/(app)/expenses/payment-targets-table.tsx'`가 1 이상이고, SUMMARY에 `app/(app)/expenses/actions.ts`의 export 목록을 적어 이 플랜이 더한 증빙 쪽 액션이 보기 액션 `getEvidenceConfirmViewAction` 하나뿐임을 보인다(확인 쓰기는 06-06 액션 그대로 — 새 판정 없음)
    - `grep -v '^\s*//' ui/confirm-dialog/ConfirmDialog.tsx | grep -cE "@/(app|domain)/"`가 0(ui는 행 데이터만 받는다)
    - `git diff --exit-code docs/design/tokens.css`가 0으로 끝난다(토큰 추가 0)
    - `grep -vE '^\s*(/\*|\*)' ui/confirm-dialog/ConfirmDialog.module.css | grep -cE "#[0-9a-fA-F]{3,6}\b|rgb\("`가 0이다(주석 줄 제외 — 새 색 리터럴 없음)
    - E2E가 슬롯을 넘기지 않은 S2 모달에서 첨부 보기 칸 요소 수 0과, 800px 폭에서 증빙 칸 3차가 `href`에 `from=pay`를 가진 링크임을 단언한다
    - RED · GREEN 한 줄씩이 SUMMARY에 있다 — ⑤의 ⒜ · ⒝(r2 B-1 — 훅 호출을 뺐을 때) · ⒞(r2 F-4 — 가드를 뺐을 때) 셋, 그리고 되돌린 뒤 `git diff --exit-code domain/evidence-reviews/index.ts`가 0
  </acceptance_criteria>
  <done>경영관리가 목록을 떠나지 않고 파일을 보며 증빙을 확인하고(모달이 보여 준 증빙만 확인된다 — r2 B-1), 그 행이 저절로 골라져 같은 흐름에서 일괄 지급되며, 문서 화면에 다녀와도 다른 쪽 것까지 선택 · 편집이 남는다(r2 F-2)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 합 액션 | 행 id · 편집 이체액 · 합계 번호 |
| 브라우저 → 일괄 지급 액션 | 편집 이체액 · 차이 사유 · 지급일 · `expectedPayableKrw` |
| 브라우저 → 제자리 확인(보기 액션 · 06-06 확인 액션) | 문서 id · `version`이 넘어온다 — 파일 목록 · 증빙 금액이 돌아간다 |
| 브라우저 `sessionStorage` | 행 id · 사람이 적은 이체액 · 차이 사유 · 쪽(탭 수명, 같은 출처) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-84 | Tampering | 지급일로 바뀐 지급 총액 | high | mitigate | 단건 경로가 지급일로 재계산해 `expectedPayableKrw`와 다르면 처리하지 않는다. 세율 경계 통합 테스트 |
| T-06-85 | Tampering | 차이 사유 누락 | medium | mitigate | 이체액 ≠ 지급 총액이면 사유 필수 — zod + 06-03 DB CHECK |
| T-06-86 | Information Disclosure | 합 액션 | medium | mitigate | 지급 권한 필수 · 지급 총액은 서버가 행 id로 다시 읽는다(클라이언트 값 아님) |
| T-06-87 | Tampering | 합 응답 재정렬 | low | accept | 표시만의 문제이고 합계 번호로 낡은 응답을 버린다 — 처리는 스냅숏 값으로 서버가 다시 판정 |
| T-06-170 | Information Disclosure | `getEvidenceConfirmViewAction`(파일 목록 · 증빙 금액) | medium | mitigate | `can(viewer, "expenses.payments", "write")` 필수 · `확인 전`(P2 · P5) 문서만 · DTO 등록 → 누수 스캔. 통합 「증빙 확인 보기 — 권한 · 확인 전만」 |
| T-06-171 | Tampering | 제자리 확인의 낡은 version · 확인 우회 | medium | mitigate | 06-06 `confirmEvidence`의 `lockExpenseRow` + version 비교 · 권한 판정을 그대로 쓴다(새 판정 · 새 쓰기 경로 없음). 응답 version으로 행을 바꾸고, 일괄 처리는 행마다 다시 판정(T-06-72). E2E 「제자리 증빙 확인」 |
| T-06-233 | Repudiation | 보지 않은 증빙의 확인 — 모달을 연 뒤 PM이 증빙을 바꿈(r2 B-1) | high | mitigate | 확인은 보기가 준 `version`으로(행 `version` 아님) · 06-11 훅이 결재 통과 문서의 PM 증빙 변경마다 `bumpExpenseVersion`(06-11 T-06-231 — 모달이 열리는 행은 모두 결재 통과, X-1 r3) · 열 때 보기 `version` ≠ 행 `version`이면 1차 `aria-disabled`. E2E 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」(RED: 훅 호출을 빼면 빨감) |
| T-06-234 | Information Disclosure | `sumSelectedTransfersAction` `rowStates`(브라우저가 보낸 임의 id의 지급 총액 · `version` — r2 F-2) | medium | mitigate | 지급 권한 필수(T-06-86) · 서버가 id로 다시 읽고 지급 대상 조회 범위 밖 id는 싣지 않는다 · DTO 등록 → 누수 스캔. 통합 「고른 행 상태 일괄 조회」 |
| T-06-172 | Information Disclosure | `sessionStorage` 보관 | low | accept | 행 id · 사람이 적은 이체액 · 차이 사유 · 쪽만(계좌 · 이름 · 계산 금액 없음), 탭 수명 · 같은 출처. 처리 권한은 서버가 처리 때 다시 판정한다(R-2) |
| T-06-173 | Elevation of Privilege | 보관에서 되살린 선택 | low | mitigate | 되살리기는 서버 합 응답 `rowStates`에서 `selectable`인 행만(Task 3 순수 함수 — 지금 쪽 밖 포함, r2 F-2) · 처리 때 서버가 행마다 게이트 재판정(06-15 T-06-72). 단위 「되살리기」 · 「다른 쪽 id 유지」 |
| T-06-174 | Information Disclosure | 첨부 원본 열기(`크게 보기`) | medium | transfer | Phase 5 첨부 읽기 경로의 권한 판정을 그대로 쓴다(A-608) — 이 플랜은 파일 경로를 새로 만들지 않고 보기 액션이 돌려준 href만 쓴다 |
</threat_model>

<verification>
- 단위 payments-targets · table-selectable, 통합 payment-batch · expense-payments · evidence-reviews · leak-scan 녹색
- E2E payment-batch · payment-single · keyboard-nav · quote-table가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S1 · S2 · S1 제자리 증빙 확인 모달 DOM 실측 감사(backstop 4건 — S1 long-text · S2 long-text · 제자리 확인 overflow(파일 6개) · 제자리 확인 long-text 포함, 결과 글자 `aria-live` · 링크 셀 접근 이름 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 실제 이체액이 다른 건을 일괄 흐름 안에서 사유와 함께 적는다
- 화면의 합 · 차이가 서버 값이고, 지급일로 바뀐 금액이 조용히 처리되지 않는다
- 키보드 · 페이지 · 좁은 폭이 계약대로 선다
- 경영관리가 목록을 떠나지 않고 증빙을 확인해 그 행을 같은 흐름에서 지급하고(DR-4), 문서 화면에 다녀와도 선택 · 편집이 남는다(C-1)
- 일괄 결과가 스크린 리더에 읽히고, 게이트로 막힌 행은 선택이 풀리며, 모달이 보이지 않는 선택을 말한다(DR-6 · H-3 · H-5)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `sumSelectedTransfers`(행 상태 `rowStates` — r2 F-2) · `listPaymentTargets`(페이지 · 필터 전체 합계) · `getEvidenceConfirmView`(제자리 확인 모달 보기, DR-4 · 입력 `rowVersion` — r2 B-1) |
| Server Action | `sumSelectedTransfersAction` · `getEvidenceConfirmViewAction` |
| 컴포넌트 | `ui/confirm-dialog` `ConfirmDialog`의 `attachments` 슬롯(SP-7 「첨부 보기 칸」) · `EvidenceConfirmDialog`(`app/(app)/expenses/payment-targets-table.tsx`의 이름 있는 export — 06-20 S3가 import) |
| 변경된 기존 심볼 | `ui/table` 선택 상태 순수 함수(「처리 결과 적용」 · 「되살리기」 포함) · `use-grid-keyboard`(`Space` · 링크 셀 `Enter` · `selectable` 표 `Ctrl+Enter` = 화면 1차) · `completePaymentsBatch`(`newPayableKrw`) · S1 `sessionStorage` 보관(키 = 보기 + 필터) |
| 테스트 | `test/unit/domain/payments-targets.test.ts` · `test/unit/ui/table-selectable.test.ts`(「다른 쪽 id 유지」 — r2 F-2) · `test/integration/payment-batch.test.ts`(확장 — 「증빙 확인 보기 — 권한 · 확인 전만」 · 「고른 행 상태 일괄 조회」 — r2 F-2) · `test/e2e/payment-batch.spec.ts`(확장 — 「다른 쪽 N건 포함」 · 「일괄 결과 알림 · 막힌 행 선택」 · 「링크 셀 Enter 열기」 · 「표 안 Ctrl+Enter는 화면 1차」 · 「처리 뒤 쪽 다시 받기」 · 「제자리 증빙 확인」 · 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」(r2 B-1) · 「보관 되살리기」 · 「확인 직후 Ctrl+Enter 반복 무시」) |

## Review Dispositions Ledger

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 5 · 8 · 11 · 13)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| DR-4 + C-1 — 확인 전 행을 고르려면 문서 화면을 오가야 하고 목록 편집 · 선택이 사라진다(design-review.md L35@f2720a8) | HIGH(고침, 병합) | 새 Task 4: ① `ui/confirm-dialog` `attachments` 슬롯(SP-7 — 06-01은 문서만, 구현은 여기) ② `getEvidenceConfirmView` · `getEvidenceConfirmViewAction`(지급 권한 · 확인 전만) ③ S1 3차 → 모달, 06-06 `confirmEvidenceAction` 금액 없는 갈래 그대로 · 응답 version으로 행 갱신 · 저절로 골라짐 · 포커스 이체액 셀 · 700~1023은 `?from=pay` 링크 ④ `sessionStorage` 보관 · 되살리기(C-1 ②) · `beforeunload`만. must_haves DR-4 · 제자리 확인 · SP-7 · 보관 줄, prohibitions 둘, key_links 둘, 선행 의존 UA-602 · SP-7 · 06-06 · A-608, 열린 선택 O-21 · 보관 키, T-06-170~174. version 갱신 단언 = E2E 「제자리 증빙 확인」의 뒤이은 일괄 처리 막힘 0(+ RED 먼저). C-1의 문서 화면 쪽(3차 `지급 대상으로` · `다음 확인 전 {번호}`)과 `[증빙 전체 ▾]`은 06-20 Task 4 — 아래 Deferred |
| DR-4 UI Considerations 일곱 행(`S1 제자리 증빙 확인`) | HIGH | must_haves.truths로 옮김(empty · loading · error · populated · partial + backstop overflow · long-text), verification DOM 감사 backstop 4건 |
| DR-6 + H-3 — 일괄 결과가 스크린 리더에 닿지 않고, 게이트로 막힌 행이 체크된 채 남는다(L37@f2720a8) | HIGH(고침, 병합) | Task 2 name · read_first · behavior 「일괄 결과 알림 · 막힌 행 선택」 · action(빈 `aria-live="polite"` 영역을 첫 렌더부터 · 이유 줄 id를 `aria-describedby`에 · 모달 닫힘 뒤 포커스 · 06-15 `selectable` 그대로) · acceptance ⑴~⑸ · fails_when · must_haves `S1 일괄 결과 알림` 네 줄 · prohibitions 「게이트로 막힌 행이 체크된 채」. 판정 로직은 06-15 |
| DR-5 — S1 증빙 칸 3차를 만드는 플랜이 없다(design-review-opus.md DR-5) | MEDIUM | Task 4 ③이 3차를 만든다(DR-4 채택). O-21 답이 「아니오」로 바뀌면 열린 선택 O-21 행대로 3차만 `?from=pay` 링크로(보관 ④가 편집을 받는다) |
| H-2 — PC S1에서 문서를 여는 자리가 정해지지 않았다(L40@f2720a8) | MEDIUM(고침) | must_haves 「H-2(rev 9) 링크 셀」 · Task 3 name · read_first · behavior 「링크 셀 Enter 열기」 · action ⑵(링크 셀 `Enter` = 열기, `<a href>` 가운데 클릭 · `Ctrl`+클릭 그대로, 쿼리 = 보기 · 팀 · 쪽) · acceptance. 06-13은 S1을 갖지 않아(견적 줄 표 S14) 이 플랜이 맡는다 |
| H-5 — 일괄 지급 모달이 보이지 않는 선택을 확정한다(L42@f2720a8) | MEDIUM(고침) | Task 2 behavior 「다른 쪽 N건 포함」 · action(스냅숏으로 개수를 셈, 해당 묶음만) · acceptance 세 갈래 · must_haves S2 partial 줄 |
| DR-8 — `selectable` 표 안 `Ctrl+Enter`가 새 줄과 화면 1차 사이에서 갈린다(L43@f2720a8) | LOW(참고) | must_haves 「DR-8(rev 9)」 · Task 3 behavior 「표 안 Ctrl+Enter는 화면 1차」 · action ⑶(`selectable` 표는 `onNewRow` 대신 화면 1차 콜백, 견적 줄 표 불변) · acceptance. 06-15가 여기로 보냈다(`use-grid-keyboard.ts` 소유) |
| DR-10 — 경영관리 주간 지급 스토리보드(L45@f2720a8) | LOW(참고) | UI-SPEC S1 머리(Shard A). Task 4 read_first가 3~4단계를 읽고 E2E 「제자리 증빙 확인」이 그 두 단계를 한 경로로 돈다 |
| M-1 — 일괄 처리 뒤 쪽 번호 · 빈 쪽(L48@f2720a8) | LOW(참고) | Task 3 behavior 「처리 뒤 쪽 다시 받기」 · action M-1(같은 쪽 재조회 · 마지막 쪽으로) · 순수 함수 「처리 결과 적용」 · acceptance · must_haves 일괄 결과 알림 partial 줄 |
| M-3 — 확인 직후 `Ctrl+Enter` 연타로 곧바로 지급(L50@f2720a8) | LOW(참고) | S1 몫: Task 4 behavior 「확인 직후 Ctrl+Enter 반복 무시」 · action ③(`event.repeat` 무시 · 포커스 이체액 셀) · must_haves 제자리 확인 partial 줄. 문서 화면(S4) 몫은 06-20 Task 4 |
| M-9 — 핵심 표면이 아직 없는 Phase 5 UI-SPEC에 기댄다(L56@f2720a8, 반영 지시 13 「06-15/06-17/06-19 착수 게이트」) | LOW(참고) | 「선행 의존(A-6xx)」 새 줄 「M-9 · UA-605 · UA-606」(05-UI-SPEC 없으면 멈춤 · 대조 표를 SUMMARY에 · 어긋나면 06-UI-SPEC 먼저) · Task 1 acceptance ⓪ 대조 표. 페이즈 전체 게이트는 06-VALIDATION 「실행 착수 게이트(M-9)」 |
| (줄 번호 표류) Task 1~3 read_first의 UI-SPEC 줄 번호가 rev 9에서 밀렸다 | LOW | 절 이름으로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| C-1 ① 문서 화면 쪽 — `from=pay`로 연 문서 화면의 3차 `지급 대상으로` · `다음 확인 전 {번호}`(반영 지시 5가 이 플랜에 둠) | 06-20 Task 4로 보냄(연기 아님 — 같은 페이즈 웨이브 10). 이유: 이 플랜 `files_modified`가 14에서 멈춰야 한다(plan-checker 15 이상 차단) — 문서 화면 두 파일(`app/(app)/expenses/[id]/page.tsx` · `payment-action-row.tsx`)을 더하면 16. 목록 쪽 절반(번호 링크 `from=pay` · 보관 · 되살리기)은 이 플랜에 있다 |
| C-1 ④ `[증빙 전체 ▾]` 필터 | 06-20 Task 4로 보냄 — 목록 조회(`repositories/payment-targets.ts`)와 필터 줄(`app/(app)/expenses/page.tsx`)이 이 플랜 파일이 아니고 06-20에 이미 있다. 보관 키에 증빙 값을 더하는 것도 그때 |
| DR-4 모달 컴포넌트를 새 파일 `evidence-confirm-dialog.tsx`로(검토 DR-4 ⑧ 제안) | 받지 않음 — 같은 파일 한도 이유로 `payment-targets-table.tsx`의 이름 있는 export `EvidenceConfirmDialog`로 두고 06-20 S3가 import한다. 동작 차이 없음 |
| 이 플랜이 태스크 4 · 115000 토큰으로 커졌다 | 경고로 둔다 — plan-checker 기준 태스크 4는 경고(차단 아님). 새 플랜으로 쪼개려면 ROADMAP · 웨이브를 오케스트레이터가 바꿔야 해서 이 샤드 권한 밖이다 — 보고에 적었다 |

### Round 2 — 디자인 검토 반영 r2(커밋 전 · `/mnt/project-files/06-gates/design-apply-cross-r1.md` · `design-apply-checker-r1.md` · `design-apply-r2-brief.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 B-1 — 모달을 연 뒤 PM이 증빙을 바꾸면 보지 않은 증빙이 `확인됨`이 된다. 보기는 「지금 `version`」을 주는데 확인은 「행의 `version`」으로 불러 두 줄이 다르다(design-apply-cross-r1.md B-1, 권고 ①) | 막음 | 06-11이 훅에서 늘 `bumpExpenseVersion`(06-11 Round 2). Plan 17: must_haves.truths 제자리 확인 줄(보기가 준 `version`으로 확인 · 열 때 불일치 = 1차 `aria-disabled` + 동시성 문구) · error 줄(`새로 고침` = 목록 + 보기 다시, 모달이 새 증빙으로 다시 섬 · `확인 전` 아님이면 닫고 포커스 행 링크) · prohibitions 「모달이 보여 준 증빙과 다른 증빙이 `확인됨`이 되어서는 안 된다」 · artifacts · key_links · objective. 「선행 의존」 06-11(r2 B-1) 줄. Task 4 precondition · read_first · behavior 첫 줄(`rowVersion`) · 새 E2E 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」 ⑴ ⑵ ⑶ · action ②(보기 `version` · 동시성 이유) · ③(열 때 불일치 · `새로 고침` 흐름) · ⑤ RED ⒝(훅 호출을 빼면 ⑴이 빨감) · fails_when · acceptance · done. T-06-233 |
| 교차 F-2 — 되살리기가 지금 쪽 행만 살려 다른 쪽 선택을 조용히 버린다(F-2, UI-SPEC 「되살리기」 · 이 플랜 Task 3 · Task 4) | 고침 | UI-SPEC 「되살리기」(같은 라운드 · R1). Plan 17: must_haves.truths 보관 줄 · artifacts `rowStates`. Task 1 behavior 「고른 행 상태 일괄 조회」 · action(`rowStates` — 06-15 `selectable` 판정 그대로 · 범위 밖 id 제외) · fails_when · acceptance. Task 3 순수 함수 「되살리기」(`rowStates`로 거름 · 지금 쪽 목록으로 거르지 않음) · 단위 「다른 쪽 id 유지」 · fails_when · acceptance. Task 4 action ④(보관 id 전부를 합 요청 한 번 · 지금 쪽 밖 행 `version` · 지급 총액은 응답 값 · 처리 뒤 다음 합 응답으로 갱신) · E2E 「보관 되살리기」 두 쪽 갈래 · acceptance. T-06-234 |
| 교차 F-4 — M-3 E2E가 `event.repeat` 가드 없이도 초록이 된다(둘째 keydown이 제출 중 모달에 떨어짐, F-4) | 고침 | Plan 17, Task 4 behavior 「확인 직후 Ctrl+Enter 반복 무시」 순서: 첫 누름 → 모달 닫힘 · 포커스 = 이체액 셀을 기다림 → `Control`을 누른 채 `keyboard.down('Enter')` 한 번 더(`repeat: true`) → S2 `role=dialog` 0 · 지급 기록 0 → 떼고 새로 누르면 S2가 열림. action ⑤ RED ⒞(가드를 빼면 빨감) · fails_when · acceptance(두 줄 순서를 SUMMARY에). 테스트 이름은 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 B-1 대안 ② — 보기가 증빙 지문(파일 id 목록 + 증빙 금액)을 주고 `confirmEvidenceAction`이 `evidenceStamp`를 비교 | 채택하지 않음 — 권고 ①(훅이 늘 version을 올림)이 06-06 「판정 그대로」를 바꾸지 않고 같은 경합을 막는다(06-11 Round 2와 같은 판단) |
| 체커 W3 — 이 플랜이 태스크 4 | 쪼개지 않음 — 오케스트레이터 판단(design-apply-r2-brief.md 「오케스트레이터 판단」)으로 경고 유지 |
| 06-VALIDATION.md 새 행(「고른 행 상태 일괄 조회」 · 「다른 쪽 id 유지」 · 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」) | R2 · 오케스트레이터 소관 — 이 파일 밖. 반환에 적었다 |

### Round 3 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-1 정합 — 06-11 훅이 결재 통과 문서에서만 version을 올리게 바뀌었다(design-apply-cross-r2.md X-1, 06-11 Round 3) | 참고(파생) | Supersedes Round 2 How Addressed의 「06-11이 훅에서 늘 `bumpExpenseVersion`」: must_haves.truths 제자리 확인 줄 · 「선행 의존」 06-11 줄 · Task 4 precondition · T-06-233에 「결재 통과 문서의」를 더했다. 모달은 결재 통과 `확인 전` 행(P2 · P5)에서만 열리므로 이 플랜의 동작 · E2E 「모달 연 뒤 증빙 바뀜 → 새 증빙으로 다시」 · 멈춤 조건(`bumpExpenseVersion` grep · 06-11 SUMMARY 「PM 증빙 변경은 문서 version을 올린다」 녹색 줄)은 그대로다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 06-17 · 06-20 분할 안 함은 사용자 · 오케스트레이터 결정 그대로(Round 2) |

### Round 4 — r4 (final-review-fable, 2026-09-26)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/final-review-fable.md`(커밋 전 — 줄 번호는 그 파일 기준). 교차 검토는 Codex 대신 Fable(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| F-F2 — `targets.ts`(06-15 · 06-17) → `index.ts` 간선과 06-20의 반대 간선이 순환(final-review-fable.md L28-33, 근거에 06-17:323 `getEvidenceConfirmView` → `loadPaymentInputs` → `decidePayable`) | 참고(06-20이 닫음) | 이 플랜은 그대로 — targets → index 한 방향이 유지해야 할 간선이다. 06-20이 계좌 투영을 `index.ts`로 옮기고 `import-cycles` verify를 더했다(06-20 Round 3) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| F-N1 — Round 2 제목의 `/mnt/project-files/06-gates/...` 경로 | 고치지 않음 — 앞 라운드 제목은 그 라운드 입력의 출처 기록이고 ledger는 append-only다. read_first · action이 아니라 실행자가 읽을 일이 없다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-17-SUMMARY.md` when done
</output>
