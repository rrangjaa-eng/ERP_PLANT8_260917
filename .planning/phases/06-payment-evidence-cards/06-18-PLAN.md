---
phase: 06-payment-evidence-cards
plan: 18
type: execute
wave: 9
depends_on: ["06-01", "06-02", "06-13", "06-16"]
files_modified:
  - db/schema/revenue-entries.ts
  - db/migrations/meta/_journal.json
  - domain/issue-requests/index.ts
  - repositories/revenue-issue-requests.ts
  - domain/projects/ledger.ts
  - domain/revenue/index.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/revenue-section.tsx
  - app/(app)/projects/[id]/issue-request-table.tsx
  - test/integration/issue-requests.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/issue-requests.spec.ts
autonomous: true
requirements: [PROJ-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "담당 PM(프로젝트 쓰기, 1024 이상)이 상세 매출 섹션의 「발행 요청」 표에서 3차 `발행 요청 추가`로 줄을 만들고(희망 발행일 기본 오늘) 금액(공급가) · 메모를 적어 1차 `일괄 저장 Ctrl+S N`을 누르면 요청이 `요청` 상태로 저장된다 — 새 1차 버튼은 없다(D-610, UA-616)"
    - "한 프로젝트에 발행 요청을 여러 개 둘 수 있고, 요청 금액의 부가세 · 합계는 서버가 희망 발행일 기준 세율로 `domain/revenue`의 발행 줄 계산 함수로 계산해 2행 `부가세 · 합계`로 보낸다(`%` 글자 없음)"
    - "매출 기록 권한자(`projects.revenue` write, 1024 이상)가 `요청` 줄의 3차 `발행 줄로`를 누르면 발행 줄 표에 새 줄(발행일 = 희망 발행일, 발행액 = 요청 금액, 메모 = 요청 메모)이 생기고 그 발행일 셀에 포커스가 가며, 요청 줄 상태 2행이 `발행 줄 입력 중`(`--muted`)이 된다"
    - "그 상태로 `일괄 저장`하면 같은 트랜잭션에서 발행 줄이 저장되고 요청 행을 `FOR UPDATE`로 잠근 뒤 `발행됨`(2행 `발행 {MM-DD} · {발행액}`)이 되며 요청 행에 발행 줄 id가 남는다 — 저장 전에 새 발행 줄을 지우면 연결도 풀린다(D-610). 발행 줄 INSERT가 실패하면 요청은 `요청`으로 남는다"
    - "요청 금액과 발행액이 달라도 막지 않는다(선금 · 잔금 조정)"
    - "`요청` 상태이고 아직 발행 줄에 이어지지 않은 줄만 PM이 고칠 수 있다 — `발행됨` 줄 수정 요청은 서버가 거부한다. 완료(정산) 프로젝트의 발행 요청 추가 · 수정은 기존 `project.completed-lock` 판정으로 막힌다"
    - "[A-605 계획 판단] 요청 금액 열은 프로젝트 쓰기 권한자(요청을 적는 PM)와 `revenue.issued_amount`를 가진 사람에게만, 상태 2행의 발행액은 `revenue.issued_amount`를 가진 사람에게만 서버가 싣는다 — 그 밖 계정의 DTO에는 키가 없다"
    - "**B-2:** 발행 요청 표는 새 표라 마이그레이션 머리에 SET LOCAL 한 쌍(0010 선례)을 두고, `issued_entry_id` 유일성은 `uniqueIndex()`다 — `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate`가 `No schema changes`"
    - "S16 empty: 「Empty — 상세 「발행 요청」 표」 세 갈래(쓰기 PM 1024 이상 · 그 밖의 사람 · 1024 미만)가 선다"
    - "S16 loading: 저장은 상세의 `일괄 저장 Ctrl+S N` 흐름 그대로이고 저장 뒤 `저장됨 N줄 {시:분}`이다 — 발행 요청 표의 dirty도 N에 든다"
    - "S16 error: 저장 실패 · 동시성은 Phase 4 일괄 저장 오류 셀 흐름 + `version` 낙관적 잠금으로 표시된다"
    - "S16 populated: 열 희망 발행일 · 금액(2행 `부가세 · 합계`) · 메모 · 상태(`요청` / `발행됨` + 2행 `발행 {MM-DD} · {금액}` / `취소`)"
    - "S16 partial: `발행 줄로` 뒤 저장 전 = 요청 줄 상태 2행 `발행 줄 입력 중`(`--muted`), 새 발행 줄을 지우면 연결이 풀리고, 발행액을 볼 수 없는 사람에게는 상태 2행 금액이 없다"
    - "S16 overflow: 줄이 몇 개뿐인 섹션 표라 페이지를 나누지 않는다(§7-3 (자))"
    - "06-03 tx 규약을 따른다 — 이 플랜의 트랜잭션은 원장 일괄 저장(`saveProjectLedger`)과 그 안의 발행 요청 저장 · 잇기다"
    - "**CROSS E-4:** `saveIssueRequestRows` · `linkIssueRequestToEntry` · `lockIssueRequest`는 원장 트랜잭션 안에서 `tx`를 받는 리포지토리만 부르고, 권한 판정은 트랜잭션 앞 · 행동 기록은 커밋 뒤(`deferRecord`)에 둔다. 원장 트랜잭션 안 전역 `db` 호출(global-db-in-tx-audit 표 B)의 수정이 main에 없으면 이 플랜은 착수하지 않는다(Task 1 ⓪ `audit-B`)"
    - statement: "S16 zero-one-many: 요청 줄 여럿이 발행 줄 표와 함께 읽힌다 — 계획 판단: 요청 줄 순서는 희망 발행일 오름차순, 같은 날은 만든 순. CI=true DOM 감사"
      verification: backstop
    - statement: "S16 long-text: 메모가 표 안 두 줄까지, 셋째 줄부터 말줄임이다 — 200자 메모로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "발행 요청을 발행 줄에 잇는 일을 매출 기록 권한(`projects.revenue` write) 밖의 사람이 할 수 있어서는 안 된다 — PM은 요청만, 발행은 경영관리(D-610)"
      status: resolved
      verification: test
    - statement: "한 요청이 두 발행 줄에 이어져서는 안 된다 — 동시 두 잇기 중 하나만 성공한다(요청 행 잠금 + `uniqueIndex(issued_entry_id)`)"
      status: resolved
      verification: test
    - statement: "발행액을 볼 수 없는 사람의 DTO에 발행액이 실려서는 안 된다(A-605 — 요청 상태 2행을 통한 우회 노출)"
      status: resolved
      verification: test
  artifacts:
    - path: "db/schema/revenue-entries.ts"
      provides: "`revenueIssueRequests` 표 — 프로젝트 · 희망 발행일 · 금액(공급가 Money 묶음) · 메모 · 상태 3종 · 이어진 발행 줄 id(`uniqueIndex`) · 요청자 · 취소 칸 · version"
      exports: ["revenueIssueRequests"]
    - path: "domain/issue-requests/index.ts"
      provides: "발행 요청 저장(일괄 저장 트랜잭션 안) · 발행 줄 잇기 · 프로젝트 표 DTO"
      exports: ["listProjectIssueRequests", "saveIssueRequestRows", "linkIssueRequestToEntry"]
    - path: "test/integration/issue-requests.test.ts"
      provides: "요청 → 발행 줄 잇기 원자성 · 권한 · 동시성 · 노출 증명"
      min_lines: 40
  key_links:
    - from: "domain/projects/ledger.ts"
      to: "domain/issue-requests/index.ts"
      via: "일괄 저장 한 트랜잭션이 발행 요청 줄까지 저장한다"
      pattern: "saveIssueRequestRows"
    - from: "domain/revenue/index.ts"
      to: "domain/issue-requests/index.ts"
      via: "요청에서 온 새 발행 줄 INSERT 직후 같은 트랜잭션에서 요청을 발행됨으로 잇는다"
      pattern: "linkIssueRequestToEntry"
    - from: "test/integration/leak-scan.test.ts"
      to: "domain/issue-requests/index.ts"
      via: "발행 요청 DTO가 누수 스캔에 든다"
      pattern: "@/domain/issue-requests"
---

<objective>
PM이 경영관리에게 세금계산서 발행을 요청하고(D-610), 매출 기록 권한자가 그 요청을 발행 줄로 이어 닫는 경로를 만든다 — 상세 매출 섹션의 「발행 요청」 표(S16). 이것이 완료 전 점검(06-19)의 「매출 미입력」 그룹이 가리키는 다음 한 수다. 취소(O-12) · 목록(S17) · 목록 링크는 06-21.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: D-613의 「매출 미입력 = 발행 줄 없음」을 PM이 스스로 풀 수 없다 — 발행은 경영관리 몫이다. PM의 요청이 원장에 남고 발행 줄로 닫혀야 점검이 메신저가 아니라 화면으로 돈다.

Output: `revenue_issue_requests` 표(A-613 — 신규 표를 택했다: 기존 `revenue_entries.kind` 확장은 `REVENUE_DTO_SPEC`과 발행 · 입금 합계에 요청이 섞여 충돌한다. 스키마는 같은 `db/schema/revenue-entries.ts` 파일에 두어 `db/schema/index.ts`를 고치지 않는다), `domain/issue-requests`, 일괄 저장 확장, S16, 통합 1 · E2E 1.

**규모 근거:** `files_modified` 14개, `estimate.tokens` 100000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-05(20파일 · 115k)를 06-18 · 06-21로 나눈 첫 몫이다. 재분할 방아쇠: Task 2 끝에서 컨텍스트 60%를 넘으면 Task 3(S16 상태 마감)을 새 플랜으로 넘기고 SUMMARY에 적는다. 웨이브 9인 이유: `quote-table.tsx`(06-13, W7) · `projects/actions.ts`와 `_journal.json`(06-16, W8)을 같은 웨이브에서 겹치지 않게 한다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-01-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-18 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-613 | 선행 없음 — 이 플랜이 발행 요청 표를 처음 설계한다 | 확인만: `grep -c "revenueIssueRequests\|issue_requests" db/schema/*.ts` 0이어야 정상 |
| A-605 | `revenue.issued_amount` 정보 항목(main은 `staffDefault: false`) | `grep -n "revenue.issued_amount" -A 4 domain/permissions/info-items.ts` 결과를 SUMMARY에 적는다 — 값과 무관하게 멈추지 않는다. 항목 자체가 없으면 멈춤 |
| A-604 | 발행 줄 쓰기 경로(`domain/revenue`의 `issuedEntries`) | `grep -n "issuedEntries" domain/revenue/index.ts` 0건이면 멈춤 |
| UA-616 | 상세의 `일괄 저장 Ctrl+S N` 하나가 견적 줄 + 매출을 한 트랜잭션으로 저장한다(`saveProjectLedgerAction` · `domain/projects/ledger.ts`) | `grep -n "saveProjectLedger" domain/projects/ledger.ts` 0건이면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx` | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |
| **audit-B** (CROSS E-4) | global-db-in-tx-audit 표 B행 수정 — `saveProjectLedger`의 트랜잭션(`domain/projects/ledger.ts` `withTransaction`) 안에서 `saveQuoteLines` · `saveRevenue`가 전역 `db`로 부르는 판정 · 조회가 트랜잭션 전 사전 조회나 `tx` 인자로 바뀌어 main에 있다. main 0341e36 기준 자리: `domain/quotes/lines.ts` 363(`can`) · 368(`visible`) · 372(`findQuoteRevisionById`) · 375(`findProjectById`) · 383(`quoteLineCustomFieldsSchema` → `listFieldDefinitions`), `domain/revenue/index.ts` 319 · 324(`can`). 이 플랜은 그 트랜잭션에 요청 행 `FOR UPDATE`를 더해 잠금을 쥔 시간을 늘리므로 수정 전에는 들어가지 않는다(계획 시점 2026-09-25 — origin/main 0341e36에 없음, 「Review Dispositions Ledger」) | `awk '/export async function saveQuoteLines/,/^}/' domain/quotes/lines.ts`와 `awk '/export async function saveRevenue/,/^}/' domain/revenue/index.ts`에서 위 호출을 **이름으로** 찾아(줄 번호는 바뀐다) 원장 경로에서 트랜잭션 전에 도는지 · `tx`를 받는지를 SUMMARY에 적는다. 하나라도 원장 트랜잭션 안에서 전역 `db`로 돌면 코드를 바꾸지 않고 `선행 의존 audit-B 미해소 — saveProjectLedger 트랜잭션 안 전역 db, 06-18 착수하지 않음`을 출력하고 멈춘다. 수정과 함께 들어온 원장 저장 동시성 회귀 테스트(`git grep -l "saveProjectLedger" -- 'test/integration/*concurrency*'`)가 있으면 돌려 결과를 SUMMARY에 적는다. **소유 · 추적(CHK-R1 W2 · CROSS-R1 F-6):** audit B 수정은 이 프로젝트의 후속 스레드 「원장 일괄 저장 교착」이 맡는다 — PR #40이 main에 머지된 뒤 main에서 시작하고 코디네이터가 추적한다. 멈추면 이 결과를 코디네이터에 넘긴다 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-12 | 상태 셋(`요청` · `발행됨` · `취소`) + 금액 = 공급가 — 이 플랜은 스키마 CHECK와 `요청 → 발행됨`만, 취소는 06-21 | 스키마 CHECK · 06-21의 상태 전이 함수 |
| AS2 | 요청 저장 · 발행 잇기 = `document_create` / `document_update` | `recordAction` 인자 |
| A-605 노출 | 위 must_have의 계획 판단 | `listProjectIssueRequests` DTO 투영 |

<assumption_delta_decision>
`detected: false`. 일괄 저장 입력에 `issueRequests`를, 발행 줄 행에 선택 필드 `fromIssueRequestId`를 **더한다** — 기존 칸의 뜻은 바뀌지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: PROJ-06 — `unclassified → unresolved (flagged)`. 이 플랜은 PROJ-06의 「매출 미입력」 점검이 가리키는 닫는 길을 만든다(D-610 · D-613). 점검 자체는 06-19. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — PM이 발행 요청 한 줄을 일괄 저장하고, 매출 기록 권한자가 「발행 줄로」 → 일괄 저장해 요청이 「발행됨」이 된다</name>
  <files>db/schema/revenue-entries.ts, db/migrations/meta/_journal.json, domain/issue-requests/index.ts, repositories/revenue-issue-requests.ts, domain/projects/ledger.ts, domain/revenue/index.ts, app/(app)/projects/actions.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/issue-request-table.tsx, test/integration/issue-requests.test.ts, test/integration/leak-scan.test.ts, test/e2e/issue-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 713-725행(S16 정본) · 1034-1041행(S16 considerations) · 834행(발행 요청 합계 계산) · Copywriting 「Empty — 상세 「발행 요청」 표」
    - app/(app)/projects/actions.ts 50-110행(`saveProjectLedgerAction` 스키마) · domain/projects/ledger.ts · domain/revenue/index.ts 80-175행(DTO · 계산 선례) · 232-330행(`saveRevenue` 발행 줄 경로)
    - app/(app)/projects/[id]/quote-table.tsx(일괄 저장 dirty 합산 자리) · revenue-section.tsx(발행 줄 표 · 오류 셀) · page.tsx
    - db/schema/revenue-entries.ts · db/schema/money-columns.ts · db/migrations/0010_revenue_entries.sql 1-5행(새 표 SET LOCAL 선례)
    - test/integration/revenue-entries.test.ts · test/e2e/revenue-section.spec.ts(선례)
  </read_first>
  <behavior>
    - `saveIssueRequestRows`: 프로젝트 쓰기 권한 없음 → 거부 / 새 줄 → `요청` + 요청자 / `발행됨` 줄 수정 → 거부 / version 불일치 → 거부 / 완료 프로젝트 → `project.completed-lock` 거부
    - 발행 줄 행에 `fromIssueRequestId`: 매출 기록 권한 없음 → 거부 / 요청이 `요청` 아님 → 거부 / 정상 → 발행 줄 INSERT 뒤 같은 트랜잭션에서 요청 행 잠금 → `발행됨` + 발행 줄 id
    - 발행 줄 INSERT가 실패하면 요청 상태도 바뀌지 않는다(한 트랜잭션)
    - 요청 금액의 부가세 · 합계는 발행 줄과 같은 서버 계산(희망 발행일 기준 세율)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. A-605 결과와 `audit-B` 판정(호출 이름별 트랜잭션 앞 · `tx` 여부)을 SUMMARY에 적는다.

① **스키마(A-613 · B-2).** `db/schema/revenue-entries.ts`에 `revenueIssueRequests`(id · project_id FK · desired_issue_date date · `moneyColumns("amount")`(공급가, 발행 줄과 같은 금액 모델) · memo text null · status text CHECK in (requested, issued, cancelled) · issued_entry_id FK → revenue_entries null · `uniqueIndex().on(issued_entry_id)` · requested_by FK · cancelled_by null · cancelled_at null · version · source 'demo' · 생성 시각)와 CHECK(`issued`이면 issued_entry_id 있음 — 새 표라 NOT VALID 불필요). `pnpm db:generate --name revenue_issue_requests` → 생성 SQL 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';`(0010 선례) → `pnpm lint:sql` → `pnpm db:migrate` → 두 번째 generate `No schema changes`.

② **domain.** `repositories/revenue-issue-requests.ts`(viewer 첫 인자 · 요청 행 `.for("update")` 잠금 함수 `lockIssueRequest`). `domain/issue-requests/index.ts`: `listProjectIssueRequests(viewer, projectId)`(DTO — 부가세 · 합계는 `domain/revenue`의 발행 줄 계산 함수를 재사용, 새 세금 식 없음), `saveIssueRequestRows(viewer, projectId, rows, tx)`, `linkIssueRequestToEntry(viewer, requestId, entryId, tx)`. `domain/projects/ledger.ts`의 `saveProjectLedger`가 입력 `issueRequests`를 같은 트랜잭션에서 `saveIssueRequestRows`로 넘기고, `domain/revenue/index.ts`의 발행 줄 쓰기 행 타입에 선택 필드 `fromIssueRequestId`를 더해 INSERT 직후 `linkIssueRequestToEntry`를 부른다(`projects.revenue` write 확인은 기존 발행 줄 경로 그대로). 두 함수와 `lockIssueRequest`는 원장 트랜잭션 안에서 돈다 — 06-03 tx 규약(CROSS E-4): 발행 요청 줄의 프로젝트 쓰기 권한 판정은 `saveProjectLedger`가 트랜잭션을 열기 전에 한다(audit B 수정이 `saveQuoteLines` · `saveRevenue`의 판정을 옮긴 사전 조회 자리 — 결과를 인자로 넘기든 그 자리에서 거부하든 그 수정의 꼴을 그대로 따른다). 안에서는 `tx`를 받는 리포지토리만 부른다(`repositories/revenue-issue-requests.ts`의 잠금 · 쓰기 함수는 `tx` 필수 — 잠금 함수에 풀 기본값을 두지 않는다). `project.completed-lock`의 프로젝트 행도 audit B 수정과 같은 방법(사전 조회 또는 `tx`)으로 읽는다. 행동 기록(AS2)은 원장의 `deferRecord`로 커밋 뒤에 남긴다. DTO `registerDto` + `test/integration/leak-scan.test.ts`에 `@/domain/issue-requests` import 한 줄.

③ **액션 · 화면.** `saveProjectLedgerAction`의 zod 스키마에 `issueRequests` 배열(id? · version? · desiredIssueDate · amount(`moneyInputSchema`) · memo)과 발행 줄 행의 `fromIssueRequestId`를 더한다 — 부가세 · 합계 필드는 없다. `issue-request-table.tsx`(S16 — 계약 금액 줄과 발행 줄 표 사이, 소제목 `발행 요청`): 열 희망 발행일 · 금액(2행 서버 계산) · 메모 · 상태. PM(프로젝트 쓰기 · 1024 이상)에게 3차 `발행 요청 추가`와 `요청` · 미연결 줄 편집, 매출 기록 권한자에게 `요청` 줄 상태 칸 3차 `발행 줄로` → `revenue-section.tsx` 발행 줄 표에 미리 채운 새 줄(클라이언트 상태에 `fromIssueRequestId`) + 그 발행일 셀 포커스. 발행 요청 표의 dirty를 `quote-table.tsx`의 `일괄 저장 Ctrl+S N`의 N에 더한다. `page.tsx`가 요청 목록을 함께 불러 넘긴다.

④ **테스트.** 통합 `issue-requests.test.ts`(신규)에 behavior 네 줄, E2E `issue-requests.spec.ts`(신규)에 「PM → 발행 요청 추가 → 일괄 저장 → 매출 기록 권한자 → `발행 줄로` → 일괄 저장 → 요청 `발행됨` + 2행」 한 경로.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 개수를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/revenue-entries.test.ts test/integration/quote-lines.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다. revenue-entries · quote-lines가 빨가면 일괄 저장 확장이 기존 경로를 깨뜨렸다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts test/e2e/revenue-section.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-605 값 포함)가 SUMMARY에 있다
    - 통합 테스트가 behavior 네 줄의 갈래를 개별 `it`으로 단언한다(원자성 케이스는 발행 줄 INSERT를 실패시켜 요청이 `요청`으로 남는지 확인)
    - `saveProjectLedgerAction` zod 스키마의 `issueRequests` 원소에 부가세 · 합계 필드 이름이 없다
    - 생성 마이그레이션 머리 두 줄이 SET LOCAL 한 쌍이다(`head -2`로 SUMMARY에)
  </acceptance_criteria>
  <done>발행 요청 한 줄이 PM의 일괄 저장 → 경영관리의 발행 줄 잇기 → `발행됨`까지 한 트랜잭션 규칙으로 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 권리 · 동시 잇기 · 발행액 노출(A-605)</name>
  <files>domain/issue-requests/index.ts, repositories/revenue-issue-requests.ts, domain/revenue/index.ts, test/integration/issue-requests.test.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 807행(S16 권한 행)
    - domain/permissions/info-items.ts(`revenue.issued_amount`) · test/integration/document-counters-concurrency.test.ts(병렬 선례)
  </read_first>
  <behavior>
    - 같은 요청을 두 매출 기록 권한자가 동시에 잇기(`Promise.all`, 서로 다른 커넥션) → 하나 성공, 다른 하나 거부 + 그 발행 줄 INSERT도 되돌림
    - PM(프로젝트 쓰기 · 매출 기록 권한 없음)이 `fromIssueRequestId` 발행 줄을 직접 보냄 → 거부
    - DTO: `revenue.issued_amount` 없는 읽기 계정 → 상태 2행 발행액 키 없음 · 요청 금액 키 없음 / 프로젝트 쓰기 PM → 요청 금액 있음, 발행액은 정보 항목대로
  </behavior>
  <action>
`linkIssueRequestToEntry`가 요청 행을 `FOR UPDATE`로 잠근 뒤 상태를 판정하게 하고(잠금 뒤 판정), `uniqueIndex(issued_entry_id)` 위반과 상태 불일치를 같은 일괄 저장 오류 셀 꼴의 거부로 바꾼다 — 거부되면 같은 트랜잭션의 발행 줄 INSERT도 되돌아간다. `listProjectIssueRequests` DTO 투영을 must_have의 A-605 계획 판단대로 서버에서 가른다(키 자체를 뺀다). 병렬 케이스가 빨가면 잠금 순서를 고친다 — 재시도 · 타임아웃으로 덮지 않는다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/leak-scan.test.ts test/integration/revenue-entries.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 세 줄을 개별 `it`으로 단언하고, 병렬 케이스는 `Promise.all`로 성공 수 1과 발행 줄 수 1을 단언한다
    - 노출 케이스가 DTO 객체 키로 확인한다
    - [CROSS E-4] 잠금 · 잇기 안에 전역 `db`가 없다: `awk '/export async function lockIssueRequest/,/^}/' repositories/revenue-issue-requests.ts | grep -v '^\s*//' | grep -cE '\bdb\b'`가 0이고, `awk '/export async function (linkIssueRequestToEntry|saveIssueRequestRows)/,/^}/' domain/issue-requests/index.ts | grep -v '^\s*//' | grep -cE '\bdb\b|\b(can|visible|scopeFor|getSettingValue|recordAction)\('`가 0이다(두 결과를 SUMMARY에)
  </acceptance_criteria>
  <done>잇기가 매출 기록 권한자에게만, 동시에도 한 번만 되고, 발행액이 정보 항목 밖으로 새지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: S16 상태 마감 — 빈 화면 세 갈래 · 입력 중 · 연결 풀기 · 1024 미만 보기 전용 · 동시성 오류 셀</name>
  <files>app/(app)/projects/[id]/issue-request-table.tsx, app/(app)/projects/[id]/revenue-section.tsx, app/(app)/projects/[id]/quote-table.tsx, test/e2e/issue-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 713-725행 · 1034-1041행 · Copywriting 「Empty — 상세 「발행 요청」 표」
    - app/(app)/projects/[id]/revenue-section.tsx(Phase 4 오류 셀 · 동시성 흐름)
  </read_first>
  <behavior>
    - 요청 0줄: 쓰기 PM 1024 이상 → 3차 `발행 요청 추가`만 / 그 밖 → 한 줄 / 1024 미만 → 보기 전용 한 줄
    - `발행 줄로` 뒤 저장 전 → 2행 `발행 줄 입력 중` / 새 발행 줄 삭제 → 연결 풀림 · 2행 사라짐
    - 다른 사람이 그사이 같은 요청을 이음 → 저장 오류 셀(Phase 4 흐름) + version 낙관적 잠금 문구
    - 요청 줄 순서: 희망 발행일 오름차순 · 같은 날 만든 순
  </behavior>
  <action>
`issue-request-table.tsx`에 Empty 세 갈래(Copywriting 행 그대로 — 빈 화면은 첫 행동 버튼), 1024 미만 보기 전용, 메모 두 줄 말줄임, 줄 순서를 마감한다. `revenue-section.tsx`에서 `fromIssueRequestId`가 있는 새 발행 줄을 지우면 클라이언트 연결 상태를 지우고 요청 줄 2행을 되돌린다. 동시성 거부는 Phase 4 일괄 저장 오류 셀 흐름으로 요청 줄에 표시한다. 토큰은 `tokens.css`의 것만, 새 컴포넌트 없음.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts test/e2e/revenue-section.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 behavior 네 줄을 개별 케이스로 단언한다
    - 기존 revenue-section · quote-table 스펙 녹색
  </acceptance_criteria>
  <done>S16의 빈 · 입력 중 · 연결 풀기 · 오류 상태가 계약대로 서고 기존 매출 섹션 동작이 바뀌지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `saveProjectLedgerAction` | 발행 요청 줄과 `fromIssueRequestId`가 넘어온다. 부가세 · 합계는 넘어와도 파싱되지 않는다 |
| domain/issue-requests ↔ domain/revenue | 발행 줄 INSERT와 요청 상태 전환이 한 트랜잭션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-88 | Elevation of Privilege | 발행 줄 잇기 | high | mitigate | `fromIssueRequestId`가 있는 발행 줄 쓰기는 기존 `projects.revenue` write 확인을 지나야 하고, 요청 행 잠금 뒤 `요청`일 때만 잇는다. 권한 없는 PM 거부 통합 테스트 |
| T-06-89 | Tampering | 동시 잇기 · 이어진 요청 조작 | medium | mitigate | 요청 행 `FOR UPDATE` + `uniqueIndex(issued_entry_id)` + `version`. `Promise.all` 통합 테스트 · `발행됨` 줄 수정 거부 테스트 |
| T-06-90 | Information Disclosure | 발행액 · 요청 금액(A-605) | high | mitigate | 정보 항목 · 쓰기 권한으로 서버가 DTO에서 키를 뺀다. DTO 등록 → 누수 스캔 계급별 검사 |
| T-06-91 | Denial of Service | 마이그레이션 | low | mitigate | 새 표 · SET LOCAL 한 쌍 · `pnpm lint:sql` 0건 |
| T-06-143 | Denial of Service | 원장 일괄 저장 트랜잭션 안 전역 풀 호출(audit B) + 새 요청 행 잠금 → 커넥션 풀 고갈 교착(CROSS E-4) | high | mitigate | Task 1 ⓪ `audit-B` — 수정이 main에 없으면 착수하지 않음 · 06-03 tx 규약(권한 판정은 트랜잭션 앞, 행동 기록은 `deferRecord`) · Task 2 구조 grep(잠금 · 잇기 · 저장 함수 안 전역 `db` 0건) |
</threat_model>

<verification>
- 통합 issue-requests · revenue-entries · quote-lines · leak-scan 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- E2E issue-requests · revenue-section · quote-table가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S16 DOM 실측 감사(backstop 2건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- PM의 발행 요청이 원장에 남고, 매출 기록 권한자가 발행 줄로 닫으면 한 트랜잭션에서 `발행됨`이 된다
- 발행 요청이 기존 일괄 저장 1차 하나로 저장된다(새 1차 없음)
- 발행액이 정보 항목 밖으로 새지 않고, 한 요청이 두 번 이어지지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 표 | `revenue_issue_requests`(`db/schema/revenue-entries.ts`의 `revenueIssueRequests`) |
| 마이그레이션 | `db/migrations/NNNN_revenue_issue_requests.sql`(번호는 `pnpm db:generate`) |
| 함수(domain) | `listProjectIssueRequests` · `saveIssueRequestRows` · `linkIssueRequestToEntry` |
| 리포지토리 | `repositories/revenue-issue-requests.ts` |
| 변경된 기존 심볼 | `saveProjectLedger` 입력(`issueRequests`) · `saveProjectLedgerAction` zod 스키마 · `saveRevenue` 발행 줄 행(`fromIssueRequestId`) · 일괄 저장 dirty 합산 |
| 화면 | `app/(app)/projects/[id]/issue-request-table.tsx`(S16) |
| 테스트 | `test/integration/issue-requests.test.ts` · `test/e2e/issue-requests.spec.ts` |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS E-4 — 06-18이 이미 교착 버그가 있는 원장 경로(audit B)에 요청 행 `FOR UPDATE`를 더한다(원문 06-18:122 · 203) | MEDIUM(고침) | Plan 18, 「선행 의존」 표 `audit-B` 줄 + Task 1 ⓪: 수정이 main에 없으면 멈춤(검토가 준 두 방법 중 멈춤과 grep을 **둘 다** 넣었다). Task 1 ②: 발행 요청 저장 · 잇기 · 잠금을 06-03 tx 규약대로(권한 판정은 원장 트랜잭션 앞, `tx` 리포지토리만, 행동 기록 `deferRecord`). Task 2 acceptance: `lockIssueRequest` · `linkIssueRequestToEntry` · `saveIssueRequestRows` 본문 안 전역 `db` · 판정 · 설정 호출 0건 grep. must_haves.truths `**CROSS E-4:**` 줄 · T-06-143. **main 확인 결과(2026-09-25, `git fetch origin main`):** origin/main = 0341e36(audit 기준 커밋 그대로)이고 `domain/quotes/lines.ts` 363 · 368 · 372 · 375 · 383, `domain/revenue/index.ts` 319 · 324가 그대로 원장 트랜잭션 안에서 전역 `db`로 돈다 — **수정 없음**. PR #40(origin/claude/gsd-progress-e1nzgu @ f0592ce)도 같은 호출이 415 · 420 · 424 · 427 · 435 / 319 · 324에 남아 있고, PR #75는 문서 번호만 고친다. 그래서 지금 실행하면 ⓪에서 멈춘다 — audit B 수정이 먼저 main에 들어가야 한다 |
| ENG E-3 참조 — 06-03 tx 규약 | MEDIUM(고침, 06-03 소관) | must_haves.truths 한 줄 「06-03 tx 규약을 따른다」 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| audit B 수정 자체(원장 경로의 `saveQuoteLines` · `saveRevenue` 사전 조회를 트랜잭션 앞으로) | 이 플랜 범위 밖 — Phase 4 원장 코드의 선행 수정(PR #75 꼴)이다. 이 플랜은 멈춤 조건으로만 기다리고 스스로 고치지 않는다. 동시 6건 원장 테스트도 그 수정이 가져온다(있으면 ⓪이 돌린다) |

### Round 2 — 333f7bf(미커밋 반영분 교차 검토 · 점검: eng-apply-cross-r1 · eng-apply-checker-r1)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS-R1 F-6 · CHK-R1 W2 — 멈춤 조건 audit-B를 풀 주체 · 추적이 없어 W9 이후가 실행 중에 멈춘다 | MEDIUM(고침 · 경고) | 「선행 의존」 `audit-B` 줄에 소유 · 추적을 적었다: 후속 스레드 「원장 일괄 저장 교착」이 맡고(PR #40 머지 뒤 main에서 시작), 코디네이터가 추적한다. 멈춤 조건과 Task 2 구조 grep은 그대로 둔다(CROSS-R1 F-6 선택지 ⒜ — 06-18에 수정을 넣는 ⒝는 고르지 않음) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 F-6 · W2는 반영했다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-18-SUMMARY.md` when done
</output>
