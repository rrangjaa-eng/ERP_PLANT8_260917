---
phase: 06-payment-evidence-cards
plan: 19
type: execute
wave: 10
depends_on: ["06-13", "06-18"]
files_modified:
  - repositories/pre-settle-check.ts
  - domain/pre-settle-check/index.ts
  - domain/pre-settle-check/summary.ts
  - domain/rules/register.ts
  - domain/settings/keys.ts
  - domain/approvals/settlement.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/pre-settle-check.tsx
  - test/unit/domain/pre-settle-check.test.ts
  - test/integration/pre-settle-check.test.ts
  - test/integration/leak-scan.test.ts
  - test/e2e/pre-settle-check.spec.ts
autonomous: true
requirements: [PROJ-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "완료 전 점검 섹션은 Phase 5 정산 결재를 기안할 수 있는 상태(A-601 — 다섯 값이면 `정산`)의 프로젝트 상세에만, 견적 줄 표 힌트 줄 바로 아래 첫 섹션으로 선다. 그 밖의 상태에는 섹션이 없다"
    - "점검 판정은 서버 함수 하나(`computePreSettleCheck`)가 한다. 이 함수가 세 그룹의 목록과 막힘 건수를 DTO로 돌려주고, `gate(doc, \"project.pre-settle-check\", { blockedCount })`는 그 건수만 보고 허용 · 막힘을 정한다(R-8, RESEARCH Pitfall 4)"
    - "미결 지출결의(D-611)는 결재 중 · 반려 문서와 증빙 없는 문서다. 증빙 없는 문서에서 면제는 빠지고 선결제는 들어가며, 증빙 필수 설정과는 관계없다. 결재를 통과했지만 아직 지급하지 않은 문서는 세지 않는다"
    - "미매칭 견적 줄(D-612)은 실행가가 0이 아니고 살아 있는 지출결의 · 카드 사용 · 구매 요청 연결이 하나도 없는 줄이다. 취소 줄과 경영관리 조정 줄은 세지 않는다. 연결 금액 합이 실행가와 달라도 막지 않는다"
    - "매출 미입력(D-613)은 발행 줄이 0인 상태다. 매출 줄 `계약 금액 · 발행 합계 · 차이`는 그룹이 비어도 늘 선다. 차이가 0이 아니면 `--warning`으로 보이지만 막지 않는다"
    - "강행 허용 설정 셋(`project.force_complete.allow_*`, 기본 false)이 켜진 그룹은 머리글 뒤에 `· 강행 허용`(`--muted`)이 붙고 막힘 건수에 들지 않는다. 세 키의 `readBy` 표시는 이 플랜에서 지운다"
    - "막힘이 1건 이상이면 PM의 「정산 결재 올리기」가 비활성이 되고 이유 `완료 전 점검 {N}건 · 점검 보기`가 선다(3차를 누르면 섹션 제목으로 포커스). Phase 5 기안 함수를 직접 불러도 같은 이유로 거부된다(UA-610 · A-609). 점검 계산이 예외를 내면 기안도 거부된다(fail-closed)"
    - "강행 허용 그룹만 남으면 기안이 된다. 기안 버튼 옆 섹션 부제 자리와 정산 결재 문서 화면(06-22)에 결과 줄 `강행 · 미결 지출결의 {N}건`이 서고(항목이 있는 강행 그룹이 여럿이면 ` · `로 잇는다), 기안 시점의 점검 요약이 행동 로그 상세에 남는다"
    - "**R-6:** 점검 행 목록은 보는 사람의 문서 범위(`scopeFor`) 안의 행만 이름 · 금액과 함께 싣고, 범위 밖 행은 그룹마다 `그 밖 {N}건` 한 줄(건수만 — 번호 · 이름 · 금액 없음)로 모은다. 막힘 건수는 범위와 무관하게 **프로젝트 전체**로 센다 — 그래서 막힘 건수 = 범위 안 행 수 + `그 밖` 건수다(옛 PS-5를 대신한다)"
    - "점검 행의 금액 칸은 정보 항목(지출결의 금액 = Phase 5 항목 · 실행가 · 계약 금액 = `quote.amount` · 발행 합계 · 차이 = `revenue.issued_amount`)이 없는 사람에게 서버가 싣지 않는다 — `PreSettleCheckDto`가 `registerDto`로 칸별 항목과 함께 등록되어 누수 스캔을 지난다"
    - "미매칭 줄의 3차는 그 줄의 문 하나다(06-13 줄 DTO의 `door` · `rowActionBlock`을 그대로 싣는다). 온라인구매 줄이면 `구매 요청`, 그 밖이면 `지출결의 올리기`이고, 카드 폼으로는 가지 않는다"
    - "S18 populated: 제목 `완료 전 점검`과 부제 `정산 결재 전 · 막힘 {N}건`, 그룹 셋(막힘 그룹 머리글 건수는 `--danger`), 점검 행, `그 밖 {N}건` 줄, 매출 줄이 선다"
    - "S18 zero-one-many: 그룹 머리글 건수는 `미결 지출결의 {N}건` · `미매칭 견적 줄 {N}줄`(범위 밖 포함 프로젝트 전체)이다. 막힘 건수는 막히는 그룹의 항목 수 합이고 강행 허용 그룹은 빠진다"
    - "06-03 tx 규약을 따른다 — 이 플랜의 트랜잭션은 Phase 5 정산 결재 기안이다"
    - "**ENG E-1:** 점검의 프로젝트 읽기 권한 판정과 강행 허용 세 키는 `loadPreSettleInputs(viewer, projectId)`가 트랜잭션을 열기 **전에** 읽는다(Task 2의 온라인구매 협력사 설정 `onlineVendorName`, Task 3의 범위 · 금액 노출 판정도 여기서 읽는다). `computePreSettleCheck(viewer, projectId, inputs, tx)`는 권한 · 설정을 다시 읽지 않고 `tx`를 받는 리포지토리와 순수 함수만 부른다 — 「기안 동시 6건」(`Promise.all` 6건, `DB_POOL_MAX` 기본 5 초과)이 제한 시간 안에 모두 끝나고 정산 결재 문서가 6건 생긴다"
  prohibitions:
    - statement: "점검이 실패하거나 오류를 내면 정산 결재가 조용히 올라가면 안 된다. 기안 함수는 점검 계산 예외를 삼키지 않고 거부한다"
      status: resolved
      verification: test
    - statement: "막힘 건수와 기안 버튼의 막힘 이유를 화면이 세 목록을 보고 스스로 세면 안 된다. 둘 다 서버 DTO에서만 온다"
      status: resolved
      verification: judgment
    - statement: "보는 사람의 문서 범위 밖 지출결의의 번호 · 항목 · 금액이 점검 목록으로 새어 나가면 안 된다(R-6) — 범위 밖은 건수만"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/pre-settle-check/index.ts"
      provides: "트랜잭션 전 사전 조회(`loadPreSettleInputs` — 권한 · 강행 허용 세 키 · 온라인구매 협력사 설정 · 범위 · 금액 노출) + 완료 전 점검 DTO 계산(세 그룹 · 범위 안 행 · `그 밖` 건수 · 매출 줄 · 강행 허용 · 막힘 건수) + DTO 등록"
      exports: ["loadPreSettleInputs", "computePreSettleCheck", "PRE_SETTLE_CHECK_DTO_SPEC"]
    - path: "domain/pre-settle-check/summary.ts"
      provides: "순수 요약: 막힘 건수 · 부제 · 강행 결과 줄 · 미결 지출결의 상태 글자 순서"
      exports: ["summarizePreSettle", "forcedResultLine", "OPEN_EXPENSE_STATUS_ORDER"]
    - path: "test/integration/pre-settle-check.test.ts"
      provides: "세 그룹 × 강행 허용 × 기안 거부 · 점검 실패 시 거부 · 범위(R-6) · 「기안 동시 6건」(ENG E-1) 증명"
      min_lines: 80
  key_links:
    - from: "domain/approvals/settlement.ts"
      to: "domain/rules/gate.ts"
      via: "Phase 5 정산 결재 기안이 트랜잭션 전에 `loadPreSettleInputs`를 읽고, 같은 트랜잭션에서 점검 DTO를 계산하고 게이트를 받는다"
      pattern: "project.pre-settle-check"
    - from: "domain/approvals/settlement.ts"
      to: "domain/pre-settle-check/index.ts"
      via: "ENG E-1 — 권한 · 설정 사전 조회는 기안 트랜잭션을 열기 전(06-03 tx 규약)"
      pattern: "loadPreSettleInputs"
    - from: "domain/pre-settle-check/index.ts"
      to: "repositories/quote-line-links.ts"
      via: "미매칭 판정이 06-07 · 06-13의 연결 조회를 한 번 부른다(연결 정의를 다시 쓰지 않는다)"
      pattern: "findLineLinks"
    - from: "domain/pre-settle-check/index.ts"
      to: "domain/settings/keys.ts"
      via: "강행 허용 세 키 읽기(`loadPreSettleInputs` 안에서만 — 트랜잭션 밖)"
      pattern: "PROJECT_FORCE_COMPLETE_ALLOW"
    - from: "app/(app)/projects/[id]/quote-table.tsx"
      to: "app/(app)/projects/[id]/pre-settle-check.tsx"
      via: "`QuoteLedger` 안, 힌트 줄 바로 아래에 섹션을 둔다"
      pattern: "PreSettleCheck"
---

<objective>
정산 결재를 올리기 전에 서는 **완료 전 미결 점검**(PROJ-06, D-611~D-613)의 판정과 기안 막힘을 만든다 — 세 그룹을 서버 함수 하나로 계산하고, 강행 허용 설정에 따라 PM의 「정산 결재 올리기」를 막거나 연다(UA-610). 범위(R-6)로 목록을 거르되 막힘 건수는 프로젝트 전체로 센다. 대표 승인 앞 재점검(O-16)과 S18 표시 상태 마감은 06-22.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: ROADMAP 기준 4는 "이유와 함께 막는다"다. boolean 하나로 합치면 PM이 왜 막혔는지 볼 수 없다(Pitfall 4). 판정은 DTO(그룹별 목록)가 하고, 게이트는 그 DTO의 막힘 건수만 본다. 기안 버튼 · 기안 함수 · 대표 승인(06-22)이 모두 같은 DTO를 읽는다.

Output: `domain/pre-settle-check`(트랜잭션 전 사전 조회 · DTO · 요약), 리포지토리 1, 게이트 `project.pre-settle-check`, 강행 허용 세 키 읽기(readBy 제거), Phase 5 기안 훅, 섹션 `pre-settle-check.tsx`, 단위 1 · 통합 1(「기안 동시 6건」 포함) · E2E 1.

**규모 근거:** `files_modified` 13개, `estimate.tokens` 100000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-09(17파일 · 115k)를 06-19 · 06-22로 나눈 첫 몫이다. 재분할 방아쇠: Task 2 끝에서 컨텍스트 60%를 넘으면 Task 3(R-6 범위)을 새 플랜으로 넘기고 SUMMARY에 적는다 — 그 경우 Task 2 끝의 목록은 프로젝트 전체 범위이므로 그 새 플랜이 끝나기 전에는 이 브랜치를 PR로 내지 않는다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-13-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-18-SUMMARY.md
@.planning/phases/05-expense-approval-leave/05-CONTEXT.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-19 착수하지 않음`을 출력하고 멈춘다.** 아래 이름은 가칭이다 — 실제 이름이 다르면 실제 이름을 쓰고 SUMMARY에 대응표를 남긴다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-601 | 프로젝트 상태 값과 Phase 5 정산 결재 기안이 받는 상태. 다섯 값이면 섹션은 `정산`에서 선다. 네 값이면 「기안 함수가 허용하는 상태」에서 서고 그 대응을 SUMMARY에 적는다 | `grep -n "status" db/schema/projects.ts`와 기안 함수의 상태 검사 줄을 SUMMARY에 적는다. 기안 함수가 상태를 검사하지 않으면 멈추고 묻는다 |
| A-604 | 계약 금액 — Phase 4 D-84 파생값 함수 또는 `projects.contract_*` 칸 | `grep -rn "contract" domain/revenue/index.ts domain/quotes/` 결과로 갈래를 SUMMARY에 적는다. 둘 다 없으면 멈춤 |
| A-609 · UA-610 | Phase 5 정산 결재 기안 함수(가칭 `domain/approvals/settlement.ts`)와 프로젝트 상세의 「정산 결재 올리기」 버튼 | `grep -rln "정산 결재 올리기" app/` 결과가 `app/(app)/projects/[id]/page.tsx` · `quote-table.tsx` 밖이면 멈춤(이 플랜의 파일 한도) · `grep -rln "settlement\|정산 결재" domain/`이 0건이면 멈춤 |
| **A-603** | 경영관리 조정 줄을 가리는 표시(줄 종류 칸 등) | `grep -n "adjust\|조정" domain/quotes/lines.ts db/schema/*quote*.ts` 0건이면 **멈추고 묻는다** — ⑴ 조정 줄 제외 조건 없이 진행(조정 줄 개념이 생기는 페이즈가 이 조건을 더한다) ⑵ 조정 줄이 들어온 뒤 재개. 계획이 조용히 빈 조건을 고르지 않는다 |
| R-6 기반 | 문서 범위 함수 `scopeFor`(main `domain/permissions/scope-for.ts`)가 Phase 5 지출결의에도 쓰인다 | `grep -rln "scopeFor" domain/expenses/`가 0건이면 Phase 5 지출결의 목록이 범위를 정하는 함수를 찾아 SUMMARY에 적고 그것을 쓴다 — 어느 쪽도 없으면 멈춤 |
| 06-13 · 06-18 | `findLineLinks`(구매 요청 포함 단일 조회) · 줄 DTO `door` · `rowActionBlock` · 발행 요청 | `grep -c "door\|rowActionBlock" domain/quotes/lines.ts` 2 미만이면 멈춤 |
| ENG E-1 기반 | Phase 5 기안 함수 **자신의** 트랜잭션 콜백 안에 전역 `db` 판정 · 설정 읽기(권한 · 정보 항목 노출 · 설정 값 · 바로 쓰는 행동 기록)가 없다(06-03 tx 규약) — 있으면 이 플랜과 무관하게 「기안 동시 6건」이 멈춘다 | 기안 함수를 읽어 트랜잭션 앞 · 안의 호출과, 트랜잭션이 처음 잠그거나 쓰는 행(「기안 동시 6건」이 풀 밖 Client로 막아 세울 행 — Task 1 ⑧)을 SUMMARY에 적는다. 안에 하나라도 있으면 코드를 바꾸지 않고 `선행 의존 ENG-E1 미해소 — Phase 5 기안 트랜잭션 안 전역 db, 06-19 착수하지 않음`을 출력하고 멈춘다(다른 페이즈 함수의 구조를 이 플랜이 고치지 않는다 — 06-18의 audit B 멈춤과 같은 꼴). **소유 · 추적(CHK-R1 W2 · CROSS-R1 F-6):** 이 조건은 Phase 5 실행 때 그 페이즈가 확인하고 코디네이터가 추적한다 — 멈추면 그 결과를 코디네이터에 넘긴다 |
| M-9 · UA-610 | **실행 착수 게이트(rev 9):** Phase 5 UI-SPEC이 있고 06-UI-SPEC 「Phase 4·5 의존 가정 (UI)」의 UA-610(정산 상태 프로젝트 상세의 「정산 결재 올리기」 자리 · 라벨)과 대조를 마쳤다 — S18 「완료 전 점검」 섹션과 기안 막힘 이유 `완료 전 점검 {N}건 · 점검 보기`가 이 버튼 자리에 기댄다 | `git ls-files .planning/phases/05-expense-approval-leave/05-UI-SPEC.md` 비면 멈춤 · 대조 표(UA-610 줄마다 Phase 5 UI-SPEC의 「정산 결재 올리기」 자리 · 라벨 · 1차/2차 여부가 06-UI-SPEC과 같은지 · 다르면 어디)를 SUMMARY에 적고, 어긋난 줄이 하나라도 있으면 코드를 바꾸지 않고 `선행 의존 M-9 미해소 — 06-UI-SPEC {UA-id} 먼저 고침, 06-19 착수하지 않음`을 출력하고 멈춘다 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-14 (관련) | 미결 지출결의 행의 상태 글자 순서는 반려 > `선결제 · 증빙 없음` / `증빙 없음` > 결재 중 | `domain/pre-settle-check/summary.ts`의 `OPEN_EXPENSE_STATUS_ORDER` 한 배열 |
| PS-1 (계획 판단) | 미제출 · 회수 문서는 세지 않는다(D-611이 나열한 상태가 아니다) | `repositories/pre-settle-check.ts`의 상태 필터 한 줄 |
| PS-2 (계획 판단) | 연결은 살아 있는 것만 센다 — 취소 · 회수된 지출결의, 취소된 구매 요청, 지운 카드 사용은 연결이 아니다 | `domain/pre-settle-check/index.ts`의 거르기 함수 하나 |
| PS-4 (계획 판단) | 강행 결과 스냅숏 칸을 만들지 않는다 — 문서 화면은 현재 판정으로 결과 줄을 다시 그리고, 기안 시점 요약은 행동 로그 상세에 남긴다 | `domain/approvals/settlement.ts`의 `recordAction` 상세 인자 |
| **R-6** (교차 검토 — PS-5 대체) | 목록 = 범위 안 행 + `그 밖 {N}건`, 막힘 건수 = 프로젝트 전체 | `domain/pre-settle-check/index.ts`의 DTO 조립 한 함수 |

<assumption_delta_decision>
`detected: false`. Phase 5 기안 · 06-13 연결 조회 계약을 **소비**한다 — 바꾸지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: PROJ-06 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: ROADMAP 기준 4 · D-611 · D-612 · D-613 · A-601 · A-604 · UA-610 · R-6. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 정산 상태 프로젝트의 반려 지출결의 1건이 섹션에 서고, 「정산 결재 올리기」가 화면과 서버 양쪽에서 막힌다</name>
  <files>repositories/pre-settle-check.ts, domain/pre-settle-check/index.ts, domain/pre-settle-check/summary.ts, domain/rules/register.ts, domain/settings/keys.ts, domain/approvals/settlement.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/pre-settle-check.tsx, test/unit/domain/pre-settle-check.test.ts, test/integration/pre-settle-check.test.ts, test/integration/leak-scan.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다(A-603이 없으면 묻는다).</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S18. 완료 전 점검 — 상세 「완료 전 점검」 섹션 + 정산 결재 올리기 막힘」 절 전체(S18 정본) · Copywriting 「Empty — 완료 전 점검(S18)」 · 「Error — 섹션 로드」 · 「막힘 — 정산 결재 올리기(S18, Phase 5 버튼)」 · 「막힘 — 대표 승인(S18, O-16)」 · 「표시 — 완료 전 점검 섹션(S18)」 · 「표시 — 점검 행」
    - .planning/phases/06-payment-evidence-cards/06-RESEARCH.md 293-297행(Pitfall 4) · 06-PATTERNS.md 60-66행(게이트 규칙은 DTO를 본 뒤 판정)
    - .planning/phases/05-expense-approval-leave/05-CONTEXT.md 34-38행(D-98 · D-100)
    - domain/rules/gate.ts · domain/rules/register.ts(`project.completed-lock` 등록 모양)
    - domain/settings/keys.ts 190-223행(강행 허용 세 키) · domain/settings/registry.ts 60-92행(`getSettingValue`) · test/unit/settings/registry-coverage.test.ts 99-111행(readBy 만료 강제)
    - domain/permissions/dto-registry.ts · domain/revenue/index.ts 138-146행(`registerDto` 선례)
    - app/(app)/projects/[id]/page.tsx · quote-table.tsx 321-360행(`QuoteLedger` props) · 955-1020행(힌트 줄 · `RevenueSection` 자리)
    - A-609 재확인에서 찾은 Phase 5 기안 함수와 버튼 파일
    - ENG E-1 근거: domain/permissions/can.ts(`findPermission` — 전역 `db`) · repositories/settings.ts 10-50행(`findSimpleValue` · `findEffectiveValue` — 전역 `db`) · lib/env.ts 54행(`DB_POOL_MAX` 기본 5) · domain/projects/ledger.ts 48-58행(`deferRecord` 꼴)
    - 동시성 테스트 선례: PR #75 `test/integration/projects-create-concurrency.test.ts`(main에 없으면 `git show origin/claude/fix-project-create-deadlock-8wybyd:test/integration/projects-create-concurrency.test.ts`) — 풀 밖 `pg.Client`로 행을 먼저 잠그고 `pg_blocking_pids` 재귀 조회로 풀 커넥션이 전부 그 잠금에 막힌 것을 확인한 뒤 풀어 교착 조건을 결정적으로 만드는 꼴 · 10초 `Promise.race`(이긴 뒤 `clearTimeout`) · `finally`의 `Promise.allSettled`. 같은 꼴을 쓰는 06-03 「지급 완료 동시 6건」 · 06-08 「동시 6건 번호 경합」
  </read_first>
  <behavior>
    - `summarizePreSettle`: 미결 지출결의 1건 · 강행 꺼짐 → 막힘 1, 부제 `정산 결재 전 · 막힘 1건` / 강행 켜짐 → 막힘 0, 부제 `정산 결재 전 · 막힘 없음`, 결과 줄 `강행 · 미결 지출결의 1건` / 전부 빔 → 막힘 0, 결과 줄 없음
    - `project.pre-settle-check` ctx `{ blockedCount }`: 0 → 허용 / N → 막힘 `완료 전 점검 {N}건 · 점검 보기`
    - Phase 5 기안: 반려 지출결의가 있는 프로젝트 → 거부(게이트 이유 그대로) / 점검 계산 예외 → 거부(문서 없음) / 미결 0 → 기존 동작 그대로
    - `loadPreSettleInputs`: 프로젝트 읽기 권한 없음 → 거부 / 강행 허용 세 키 값을 그대로 담는다 / 예외 → 기안 거부, 트랜잭션을 열지 않음(문서 0건)
    - 「기안 동시 6건」(ENG E-1 · CROSS-R1 F-1): 막힘 0인 `정산` 프로젝트 6개 · 풀 밖 `pg.Client`가 기안 트랜잭션이 막힐 행을 먼저 잡음 → Phase 5 기안 `Promise.all` 6건 → 풀 커넥션 `pool.options.max`개가 그 잠금에 막힌 것을 확인 → 잠금 해제 → 10초 제한 안에 모두 끝나고 정산 결재 문서가 프로젝트마다 1건씩 6건
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 하나라도 없으면 멈춘다. A-601 · A-604 · A-603 판정 결과, 가칭 대응표, `ENG E-1 기반` 줄(기안 트랜잭션 안 호출 · 막아 세울 행)을 SUMMARY에 적는다.

① **리포지토리.** `repositories/pre-settle-check.ts`에 프로젝트 하나의 지출결의 쪽 점검 입력(번호 · 항목(견적 줄 이름) · 금액 · 결재 상태 · 지급 여부 · 선결제 표시 · 증빙 판정 입력 · 작성자 · 팀)을 한 번에 읽는 조회를 만든다. viewer를 첫 인자로 받지만 조회 범위는 **프로젝트 전체**다 — 막힘 건수가 보는 사람마다 달라지면 안 된다(범위 거르기는 Task 3이 DTO 조립에서). 상태 필터는 결재 중 · 반려 · 결재 통과만(PS-1).

② **순수 요약.** `domain/pre-settle-check/summary.ts`에 `summarizePreSettle`(입력: 그룹별 항목 수 · 강행 허용 세 값 → 출력: 막힘 건수 · 부제 · 그룹별 `allowed` · `forcedResultLine`)과 `OPEN_EXPENSE_STATUS_ORDER`, 파일 머리 한 줄 주석 "O-14 답이 바뀌면 이 배열도 함께 고친다".

③ **사전 조회 · DTO(ENG E-1 — 06-03 tx 규약).** `domain/pre-settle-check/index.ts`에 둘을 나눈다. ⑴ `loadPreSettleInputs(viewer, projectId)` — 트랜잭션 **밖**에서 부른다: 프로젝트 읽기 권한(`can`) 확인, 강행 허용 세 키를 `getSettingValue`로 모두 읽어 `PreSettleInputs`(`allowances` 셋)로 돌려준다. ⑵ `computePreSettleCheck(viewer, projectId, inputs, tx?)` — 권한 · 설정을 다시 읽지 않고 `inputs`만 쓴다. 안에서는 `tx`를 받는 리포지토리와 순수 함수만 부른다(①의 조회는 `tx: DbOrTx = db` 인자 — 화면 경로는 `tx` 생략, 기안 · 승인 경로는 그 트랜잭션의 `tx`). 권한 · 설정 읽기를 `computePreSettleCheck` 안에 두면 기안 트랜잭션이 커넥션 하나를 쥔 채 전역 풀의 둘째 커넥션을 기다린다 — `DB_POOL_MAX` 5에서 동시 기안 5건이면 전부 멈춘다(PR #75 · #77과 같은 꼴). 이 태스크는 미결 지출결의 그룹의 결재 중 · 반려 갈래만 채운다. `PRE_SETTLE_CHECK_DTO_SPEC`에 칸마다 정보 항목을 적고 `registerDto({ name: "PreSettleCheckDto", … })`. `domain/settings/keys.ts`의 강행 허용 세 키에서 `readBy: { phase: "6" }` 줄을 지운다.

④ **게이트.** `domain/rules/register.ts`에 `project.pre-settle-check`(R-8 ctx `{ blockedCount }`) — `project.completed-lock`과 같은 모양, 세 조건을 `check()` 안에 다시 쓰지 않는다.

⑤ **기안 훅.** Phase 5 기안 함수(실제 이름)가 트랜잭션을 열기 **전에** `loadPreSettleInputs(viewer, projectId)`를 읽고(ENG E-1 — 예외면 트랜잭션을 열지 않고 거부), 문서를 만들기 전에 같은 트랜잭션에서 `computePreSettleCheck(viewer, projectId, inputs, tx)` → `gate(…, "project.pre-settle-check", { blockedCount })`. 점검 예외를 잡지 않는다(fail-closed).

⑥ **화면.** `page.tsx`는 A-601 상태일 때만 점검을 계산해 `QuoteLedger`의 새 prop으로 넘기고, 계산 실패면 `{ status: "error" }`(06-22가 그린다). 「정산 결재 올리기」의 비활성 여부와 이유는 DTO 막힘 건수와 게이트 이유 문자열에서만 — 화면 문구와 서버 거부 문구가 한 출처(Pitfall 3). `pre-settle-check.tsx`는 `ui/table/Table` 읽기 표, 제목 `id="pre-settle-check"`(`점검 보기` 포커스 목적지). `quote-table.tsx`는 힌트 줄 바로 아래에 섹션. 토큰은 `tokens.css`의 것만.

⑦ `test/integration/leak-scan.test.ts`에 `import "@/domain/pre-settle-check";` 한 줄.

⑧ **테스트(TDD — ①~⑤보다 먼저 쓰고 RED를 본다).** 단위 behavior 요약 · 게이트 갈래, 통합 「반려 1건 → 기안 직접 호출 거부」 · 「점검 리포지토리 예외 주입 → 기안 거부 + 문서 0건」 · 「`loadPreSettleInputs` 예외 주입 → 기안 거부 + 문서 0건」 · 「강행 허용 켬 → 기안 성공」 · 「기안 동시 6건」, E2E 「정산 프로젝트 상세 → 섹션 `미결 지출결의 1건` · 행 `반려` → 버튼 `aria-disabled` + `완료 전 점검 1건 · 점검 보기` → 3차로 섹션 제목 포커스」. 「기안 동시 6건」은 `test/integration/pre-settle-check.test.ts` 안의 `it` 이름에 이 글자를 그대로 넣고 PR #75 꼴로 교착 조건을 **결정적으로** 만든다(CROSS-R1 F-1 — 타이밍에만 기대는 쏘고 기다리기는 쓰지 않는다, 06-03 · 06-08과 같은 꼴): ⑴ `pool.options.max`가 2 이상 6 미만임을 먼저 단언한다(호출 6건이 풀보다 많다는 전제) ⑵ 막힘 0인 `정산` 프로젝트 6개를 domain 함수로 만든다 ⑶ 풀 밖 `pg.Client`가 `BEGIN` 뒤 ⓪에서 찾은 「기안 트랜잭션이 처음 잠그거나 쓰는 행」을 `FOR UPDATE`로 쥔다 — 기본은 그 6개 프로젝트 행(기안이 프로젝트 행을 잠그든, 프로젝트 FK를 가진 문서를 INSERT하든 — FK 검사의 `FOR KEY SHARE`도 이 잠금에 막힌다), 그 어느 쪽도 아니면 기안의 문서 번호 카운터 행(PR #75와 같은 행) ⑷ 기안 6건을 `Promise.all`로 띄우고 `pg_blocking_pids` 재귀 조회로 이 Client에 막힌 풀 커넥션이 `pool.options.max`개가 될 때까지 폴링한다(약 4초 상한 — 못 이르면 「교착 조건을 만들지 못했다」로 실패) ⑸ Client `COMMIT` → `Promise.race([Promise.all(calls), 10초 타임아웃])`, 이긴 뒤 `clearTimeout` ⑹ 6건 모두 성공 · 정산 결재 문서 6건을 DB로 단언 ⑺ `finally`에서 Client `ROLLBACK` · `end`와 `Promise.allSettled(calls)`. 기안 트랜잭션이 막아 세울 행을 하나도 잡지 않으면(프로젝트 행 잠금 · 프로젝트 FK INSERT · 카운터 행 모두 없음) 코드를 바꾸지 않고 멈추고 보고한다. 이 케이스는 훅이 없어도 초록이므로 RED는 변이로 본다 — ⑤를 단 뒤 `loadPreSettleInputs` 호출을 트랜잭션 콜백 안, 막아 세우는 그 문 **바로 뒤**로 잠시 옮기면(점검 · 게이트가 그보다 앞이면 함께 뒤로) 잠금이 풀린 트랜잭션 다섯이 커넥션을 쥔 채 여섯째 커넥션을 기다려 제한 시간 초과로 반드시 빨개진다. 한 번 보고(SUMMARY에 결과 한 줄) 되돌려 초록을 본다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/pre-settle-check.test.ts test/unit/settings/registry-coverage.test.ts test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다. registry-coverage가 빨가면 세 키의 readBy를 지우지 않았거나 한 키를 읽지 않았다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts -t "기안 동시 6건"</automated>
    <fails_when>종료 코드가 0이 아니거나, passed가 1이 아니거나(케이스 이름 불일치 · 건너뜀 포함), 풀 커넥션 `pool.options.max`개가 풀 밖 Client 잠금에 막힌 상태에 이르지 못하거나(교착 조건 미성립), 10초 제한 경주에서 지거나(교착), 정산 결재 문서가 6건이 아니다. RED 확인: `loadPreSettleInputs` 호출을 트랜잭션 콜백 안, 막아 세우는 문 바로 뒤로 옮기면 이 명령이 제한 시간 초과로 실패해야 한다 — 실패하지 않으면 케이스가 교착을 재현하지 못하는 것이다(SUMMARY에 RED · GREEN 결과 한 줄씩)</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-601 상태 대응 · A-604 갈래 · A-603 판정 또는 사용자 선택 · 가칭 대응표 · ENG E-1 기반 줄 — Phase 5 기안 함수의 트랜잭션 앞 · 안 호출 · M-9 대조 표(UA-610 같음/다름, rev 9))가 SUMMARY에 있다
    - 통합 테스트가 「기안 함수 직접 호출 거부」와 「점검 예외 주입 시 거부 + 문서 0건」을 각각 단언한다
    - [ENG E-1] 전역 `db` 판정 · 설정 읽기는 `loadPreSettleInputs` 안에만 있다: `grep -v '^\s*//' domain/pre-settle-check/index.ts | grep -cE '\b(can|visible|scopeFor|getSettingValue|recordAction)\('`의 값이 `awk '/export async function loadPreSettleInputs/,/^}/' domain/pre-settle-check/index.ts | grep -v '^\s*//' | grep -cE '\b(can|visible|scopeFor|getSettingValue|recordAction)\('`의 값과 같고, `awk '/export async function computePreSettleCheck/,/^}/' domain/pre-settle-check/index.ts | grep -v '^\s*//' | grep -cE '\b(can|visible|scopeFor|getSettingValue|recordAction)\('`가 0이다
    - [ENG E-1] 기안 함수 안에서 `loadPreSettleInputs(` 줄이 트랜잭션을 여는 줄보다 앞이다 — `grep -n "loadPreSettleInputs(\|withTransaction(\|\.transaction(" domain/approvals/settlement.ts`(실제 파일) 결과를 SUMMARY에 적는다
    - `grep -c 'readBy: { phase: "6" }' domain/settings/keys.ts`가 이 플랜 전보다 3 줄었다
    - E2E가 버튼의 `aria-disabled="true"`와 이유 문자열 = 서버 거부 이유 문자열을 단언한다
  </acceptance_criteria>
  <done>반려 지출결의 하나가 리포지토리 → DTO → 게이트 → Phase 5 기안 거부 → 섹션과 버튼 막힘까지 한 판정으로 실제 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 세 그룹 전부(D-611 증빙 갈래 · D-612 · D-613) · 강행 허용 셋 · 매출 줄 · 강행 결과 줄과 행동 로그</name>
  <files>repositories/pre-settle-check.ts, domain/pre-settle-check/index.ts, domain/pre-settle-check/summary.ts, domain/approvals/settlement.ts, app/(app)/projects/[id]/pre-settle-check.tsx, test/unit/domain/pre-settle-check.test.ts, test/integration/pre-settle-check.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-CONTEXT.md 52-55행(D-611~D-613 원문)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「적용 규칙」 「지출결의 상태 → 1차 (정본 …)」 절 표 아래 문단(「행동 뒤에는 늘 응답 상태로 이 표를 다시 읽는다」 ~ 「`면제`가 `선결제`를 이기므로」 — 면제가 선결제를 이긴다) · S18 절 「행 모양·행동」 줄 ~ 「정산 결재 올리기(UA-610)」 줄(막힘과 미결 구분 · 강행 결과 줄 `강행 · 미결 지출결의 2건`)
    - repositories/quote-line-links.ts(`findLineLinks`) · domain/quotes/lines.ts(줄 DTO `door` · `rowActionBlock` · 실행가 · 줄 상태)
    - domain/evidence-reviews/index.ts(`resolveEvidenceStatus`) · domain/evidence-reviews/prepaid.ts
    - A-604 재확인에서 고른 계약 금액 출처 · domain/revenue/index.ts(발행 줄 합계) · domain/money/index.ts(`sumKrw` · `diffKrw`)
    - domain/action-log/record.ts(`recordAction` 상세 인자)
    - domain/quotes/line-door.ts(06-02 `resolveLineDoor` — 순수) · domain/settings/keys.ts의 `PURCHASE_ONLINE_VENDOR_NAME`(06-02) · 06-13 SUMMARY의 줄 DTO `door` · `rowActionBlock` 도우미 이름과 인자
  </read_first>
  <behavior>
    - D-611: 결재 통과 · 증빙 0 · 면제 아님 · 지급 전 → `증빙 없음` / 같은데 선결제 · 지급 뒤 → `선결제 · 증빙 없음` / 면제 → 행 없음 / 결재 통과 · 증빙 있음 · 지급 전 → 행 없음 / 증빙 필수 off · 증빙 0 · 면제 아님 → `증빙 없음`
    - D-611 상태 글자: 반려 · 증빙 0 → `반려` / 결재 중 · 증빙 0 → `증빙 없음`
    - D-612: 실행가 0 → 제외 / 취소 줄 → 제외 / 조정 줄 → 제외(A-603 선택대로) / 취소된 지출결의만 걸린 줄 → 미매칭(PS-2) / 카드 사용 1건 → 제외 / 구매 요청 1건 → 제외 / 연결 합 ≠ 실행가 → 제외
    - D-613: 발행 줄 0 → 항목 1 / 발행 줄 ≥ 1 → 그룹 빔 / 차이 = `diffKrw(계약, 발행 합계)`, ≠ 0이어도 막힘 아님 / 계약 없음 → `null`
    - 강행: 세 키 조합 대표 넷(전부 꺼짐 · 전부 켜짐 · 하나씩)에서 막힘 건수와 결과 줄
  </behavior>
  <action>
`repositories/pre-settle-check.ts`에 조회 둘(미매칭 판정용 줄 목록 — 실행가 ≠ 0 · 취소 아님 · 조정 아님, 발행 줄 합계와 건수)을 더한다. 줄의 연결은 `findLineLinks`를 **한 번** 불러 받고 연결 정의를 새로 쓰지 않는다. 살아 있는 연결만 세는 거르기는 `domain/pre-settle-check/index.ts`의 함수 하나(PS-2).

`computePreSettleCheck`에 D-611 증빙 갈래(면제는 `resolveEvidenceStatus`, 선결제는 Phase 5 칸), 미매칭 그룹(행마다 06-13 줄 DTO와 같은 함수로 만든 `door` · `rowActionBlock`), 매출 미입력 그룹과 매출 줄(계약 금액 · `sumKrw` 발행 합계 · `diffKrw` 차이 — 금액 셈은 `domain/money` 밖에서 하지 않는다)을 채운다. `summarizePreSettle`에 세 그룹과 강행 셋 조합을 반영하고, `forcedResultLine`은 항목이 있는 강행 그룹을 ` · `로 잇는다. Phase 5 기안 함수는 강행으로 통과할 때 `recordAction` 상세에 기안 시점 요약(그룹별 건수 · 강행 그룹)을 남긴다(PS-4 — 기존 행동 종류). 이 기록은 06-03 tx 규약대로 기안 트랜잭션 안에서 전역 풀로 바로 쓰지 않는다 — Phase 5 기안 함수가 쓰는 꼴(`deferRecord` 커밋 뒤 기록 또는 tx 인자 판)을 따른다. 이 태스크가 `computePreSettleCheck`에 더하는 판정(`resolveEvidenceStatus` · 06-13 줄 DTO의 `door` · `rowActionBlock` 함수)도 트랜잭션 안에서 도므로 권한 · 설정을 읽지 않는 판만 부른다(ENG E-1). 특히 미매칭 줄의 `door`에 드는 온라인구매 협력사 설정은(CROSS-R1 F-2) `loadPreSettleInputs`가 트랜잭션 전에 `PURCHASE_ONLINE_VENDOR_NAME`을 읽어 `PreSettleInputs.onlineVendorName`으로 싣고, `computePreSettleCheck`는 `resolveLineDoor({ vendorName }, inputs.onlineVendorName)`(06-02 `domain/quotes/line-door.ts` — 순수)로 문을 정한다. 거래처 이름은 이 태스크의 미매칭 줄 조회가 `tx`로 함께 읽는다(`repositories/vendors.ts`는 `tx` 인자가 없다 — audit). `rowActionBlock`은 06-13이 `domain/quotes/lines.ts`에 둔 도우미(실제 이름 — SUMMARY에)가 문 · 설정 값을 **인자로 받는** 판일 때만 그것을 부른다. 그 도우미가 설정을 스스로 읽는 판뿐이면 부르지 않고 `resolveLineDoor`와 06-13의 게이트 셋(순수)을 직접 불러 같은 이유 문자열을 싣는다 — `domain/quotes/lines.ts`는 이 플랜 파일이 아니므로 고치지 않는다(SUMMARY에 「도우미 미사용」). `pre-settle-check.tsx`에 그룹 셋 · 행 · 매출 줄 · 부제 자리의 강행 결과 줄을 그린다 — 막힘 그룹 머리글 건수 `--danger`, 강행 허용 꼬리 `--muted`, 차이 ≠ 0 `--warning`, 금액은 기존 금액 셀 포맷.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/pre-settle-check.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/quote-line-links.test.ts test/integration/issue-requests.test.ts test/integration/evidence.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. 「기안 동시 6건」만 빨가면 이 태스크가 더한 판정 · 행동 기록이 기안 트랜잭션 안에서 전역 풀을 부른다(ENG E-1)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts test/e2e/issue-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 단위 테스트가 D-611 다섯 갈래, D-612 일곱 갈래, D-613 네 갈래, 강행 넷을 개별 케이스로 단언한다
    - 통합 테스트가 「증빙 면제 뒤 점검을 다시 읽으면 그 행이 빠진다(06-10 `waiveEvidence`)」 · 「발행 줄 1건 저장 뒤 매출 미입력 그룹이 빈다」 · 「강행 기안의 행동 로그 상세에 그룹별 건수」를 단언한다
    - `grep -rn "findLineLinks" domain/pre-settle-check/`가 1건 이상이고, `grep -v '^\s*//' repositories/pre-settle-check.ts | grep -c "corpCardUsages\|purchaseRequests"`가 0이다(주석 줄은 세지 않는다 — 연결은 `findLineLinks` 한 곳)
    - [CROSS-R1 F-2] `grep -n "onlineVendorName" domain/pre-settle-check/index.ts`가 `loadPreSettleInputs` 안의 설정 읽기와 `computePreSettleCheck` 안의 `resolveLineDoor` 인자 양쪽에서 잡힌다. 구조 grep을 도우미 파일까지 넓힌다: `grep -v '^\s*//' domain/quotes/line-door.ts | grep -cE '\b(can|visible|scopeFor|getSettingValue|recordAction)\('`가 0이고, `computePreSettleCheck`가 부르는 06-13 `rowActionBlock` 도우미가 있으면 그 본문 `awk '/function {도우미 실제 이름}/,/^}/' domain/quotes/lines.ts | grep -v '^\s*//' | grep -cE '\b(can|visible|scopeFor|getSettingValue|recordAction)\('`도 0이다(두 결과 · 도우미 이름 또는 「도우미 미사용」을 SUMMARY에). Task 1의 `domain/pre-settle-check/index.ts` 구조 grep 두 줄도 그대로 성립한다
  </acceptance_criteria>
  <done>세 그룹이 D-611~D-613 그대로 계산되고, 강행 허용 셋이 막힘 건수와 결과 줄을 가르며, 강행 기안이 로그에 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: R-6 범위 — 범위 안 행만 · `그 밖 {N}건` · 막힘 건수는 프로젝트 전체 · 금액 노출 · 누수 스캔 케이스</name>
  <files>domain/pre-settle-check/index.ts, app/(app)/projects/[id]/pre-settle-check.tsx, test/integration/pre-settle-check.test.ts, test/integration/leak-scan.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <read_first>
    - domain/permissions/scope-for.ts(`scopeFor`) · Task 1 ⓪에서 찾은 Phase 5 지출결의의 범위 적용 자리
    - test/integration/leak-scan.test.ts(계급별 자동 케이스 · 범위 케이스를 더하는 자리)
    - domain/permissions/dto-registry.ts
  </read_first>
  <behavior>
    - 같은 프로젝트에 PM A의 반려 1건 · PM B의 반려 1건: A로 보면 행 1(A 문서) + `그 밖 1건`, 막힘 건수 2 / 대표(전사 범위)로 보면 행 2, `그 밖` 없음, 막힘 건수 2
    - `그 밖` 줄 객체에 번호 · 항목 · 금액 키가 없다(건수만)
    - 미매칭 줄 · 매출 줄은 프로젝트 읽기 권한 안의 값이라 범위 거르기 대상이 아니다 — 금액 칸만 정보 항목으로 걸러진다
    - 정보 항목 없는 계급: 점검 행 금액 키 없음 · 매출 줄 발행 합계 · 차이 키 없음
  </behavior>
  <action>
범위 판정(`scopeFor` 또는 ⓪이 찾은 Phase 5 범위 함수)과 금액 칸의 정보 항목 노출(`visible`)은 둘 다 전역 풀로 권한표를 읽으므로 `loadPreSettleInputs`가 트랜잭션 전에 읽어 `inputs`에 더한다(ENG E-1 · 06-03 tx 규약 — 기안 트랜잭션 안에서 돌지 않게). `computePreSettleCheck`의 DTO 조립 한 함수는 그 값으로만 미결 지출결의 행을 가른다 — 범위 안 행은 그대로, 범위 밖 행은 그룹마다 `{ outOfScopeCount }` 하나로만 싣는다. 막힘 건수 · 그룹 머리글 건수는 가르기 **전** 전체 목록으로 센다(R-6 — 건수 = 범위 안 행 + `그 밖`). 금액 칸은 `PRE_SETTLE_CHECK_DTO_SPEC`의 정보 항목으로 서버가 뺀다 — 투영 함수 `project`의 노출 판정 주입 자리에 `inputs`의 노출 값을 넘겨 트랜잭션 안에서 권한표를 다시 읽지 않는다. `pre-settle-check.tsx`는 `그 밖 {N}건`(`--muted`, 행동 없음) 한 줄을 그룹 끝에 그린다. `test/integration/leak-scan.test.ts`에 「범위가 좁은 계정의 점검 DTO에 범위 밖 문서 번호 · 금액 문자열이 없음」 케이스 하나를 더한다(자동 계급 케이스와 별도 — 범위는 정보 항목이 아니라 자동 스캔이 잡지 못한다).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — leak-scan이 빨가면 범위 밖 문서가 점검 DTO로 샜다. 「기안 동시 6건」이 빨가면 범위 · 노출 판정이 기안 트랜잭션 안으로 들어갔다(ENG E-1)</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 네 줄을 개별 `it`으로 단언하고, 막힘 건수가 보는 사람과 무관하게 같음을 두 계정으로 확인한다
    - E2E가 PM A 화면에 `그 밖 1건` 글자와 B 문서 번호 부재를 단언한다
    - [ENG E-1] Task 1의 구조 grep 두 줄(판정 · 설정 읽기는 `loadPreSettleInputs` 안에만, `computePreSettleCheck` 안 0건)이 범위 · 노출 판정을 더한 뒤에도 성립한다
  </acceptance_criteria>
  <done>점검 목록이 보는 사람의 범위 밖 문서를 드러내지 않으면서, 막힘 건수는 누구에게나 같은 프로젝트 전체 값이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PM 브라우저 → Phase 5 정산 결재 기안 | 버튼 비활성과 관계없이 기안 함수가 점검 게이트를 부른다 |
| domain/pre-settle-check → 화면 DTO | 프로젝트 전체로 센 건수와 범위 안 행만 보는 사람에게 간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-92 | Elevation of Privilege | 기안 버튼을 우회한 Phase 5 기안 함수 직접 호출 | high | mitigate | `project.pre-settle-check`를 기안 domain 함수 안, 같은 트랜잭션에서 부른다. 직접 호출 거부 통합 테스트 |
| T-06-93 | Denial of Service → 무결성 | 점검 계산 실패가 기안을 통과시킴(fail-open) | high | mitigate | 점검 예외를 삼키지 않는다. 예외 주입 통합 테스트로 문서 0건 단언 |
| T-06-94 | Information Disclosure | 범위 밖 지출결의 번호 · 항목 · 금액(R-6) | high | mitigate | `scopeFor`로 행을 가르고 범위 밖은 건수만. 누수 스캔 범위 케이스 + 통합 테스트 |
| T-06-95 | Information Disclosure | 점검 행 금액 · 발행 합계 | medium | mitigate | `PreSettleCheckDto` 칸별 정보 항목 등록 → 누수 스캔 계급별 자동 케이스 |
| T-06-96 | Repudiation | 강행 기안 | medium | mitigate | 기안 시점 그룹별 건수 · 강행 그룹을 기존 행동 종류 상세에 남긴다(PS-4). 통합 테스트 |
| T-06-97 | Tampering | 점검과 기안 사이 끼어듦 | low | mitigate | 점검과 기안이 같은 트랜잭션, 대표 승인 앞 재점검(06-22 O-16) |
| T-06-98 | Tampering | 강행 허용 설정 변경 | low | accept | 설정 화면은 관리자 전용이고 기존 설정 저장이 행동 로그를 남긴다(§7-2). 이 플랜은 읽기만 한다 |
| T-06-141 | Denial of Service | 기안 트랜잭션 안 전역 풀 읽기(권한 · 설정 · 범위 · 노출)로 커넥션 풀 고갈 교착(ENG E-1) | high | mitigate | `loadPreSettleInputs`가 트랜잭션 전에 읽고 `computePreSettleCheck`는 `tx` 리포지토리 · 순수 함수만(06-03 tx 규약). 「기안 동시 6건」 통합 테스트(PR #75 꼴 풀 밖 잠금으로 교착 조건을 결정적으로 만들고 RED 확인) · 구조 grep(`domain/pre-settle-check/index.ts` + 줄 문 도우미) |
</threat_model>

<verification>
- 단위 pre-settle-check · registry-coverage · leak-scan-coverage, 통합 pre-settle-check(「기안 동시 6건」 포함) · quote-line-links · issue-requests · evidence · leak-scan 녹색
- E2E pre-settle-check · issue-requests가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S18 DOM 실측 감사 → 수정 → 전체 게이트 한 번(S18 backstop 3건은 06-22)
</verification>

<success_criteria>
- 정산 결재는 막힘 0이거나 남은 그룹이 모두 강행 허용일 때만 올라간다
- PM이 섹션만 보고 무엇이 몇 건 남았는지, 그 줄에서 다음에 무엇을 누를지 안다
- 점검이 실패하면 기안이 막히고, 범위 밖 문서는 건수로만 보인다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 게이트 규칙 | `project.pre-settle-check`(ctx `{ blockedCount }`) |
| 함수(domain) | `loadPreSettleInputs`(트랜잭션 전 사전 조회) · `computePreSettleCheck(viewer, projectId, inputs, tx?)` · `summarizePreSettle` · `forcedResultLine` |
| 상수 | `OPEN_EXPENSE_STATUS_ORDER` |
| DTO | `PreSettleCheckDto`(`PRE_SETTLE_CHECK_DTO_SPEC`, `registerDto`) |
| 리포지토리 | `repositories/pre-settle-check.ts` |
| 변경된 기존 심볼 | 강행 허용 세 키의 readBy 제거 · Phase 5 정산 결재 기안 함수(점검 게이트 · 강행 로그) · `QuoteLedger` props(점검 DTO) |
| 화면 | `app/(app)/projects/[id]/pre-settle-check.tsx`(S18 — 그룹 · 행 · `그 밖` · 매출 줄) · 「정산 결재 올리기」 막힘 |
| 테스트 | `test/unit/domain/pre-settle-check.test.ts` · `test/integration/pre-settle-check.test.ts`(「기안 동시 6건」 포함) · `test/integration/leak-scan.test.ts`(범위 케이스) · `test/e2e/pre-settle-check.spec.ts` |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — 333f7bf

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| ENG E-1 — 정산 점검이 기안 트랜잭션 안에서 `can` · `getSettingValue`(전역 `db`)를 불러 커넥션 풀 교착 | HIGH(막음) | Plan 19, Task 1 ③: `loadPreSettleInputs(viewer, projectId)`(트랜잭션 밖 — 권한 · 강행 허용 세 키)와 `computePreSettleCheck(viewer, projectId, inputs, tx)`로 나눔 · ⑤ 기안 훅이 트랜잭션 전에 사전 조회 · ⑧ 「기안 동시 6건」(RED 확인 방식) · verify에 `-t "기안 동시 6건"` 명령 + fails_when · acceptance 구조 grep 두 줄. 검토 원문의 「Task 3 ③ · ⑤」는 이 플랜의 Task 1 ③ · ⑤다(③ · ⑤는 Task 1 action 단계). Task 2(행동 기록 `deferRecord` · 판정 함수 순수성) · Task 3(범위 · 노출 판정도 사전 조회로)도 같은 규약으로 막았다. must_haves.truths `**ENG E-1:**` 줄 · key_links `loadPreSettleInputs` · T-06-141 |
| ENG E-3 참조 — 06-03 tx 규약 | MEDIUM(고침, 06-03 소관) | must_haves.truths 한 줄 「06-03 tx 규약을 따른다」 |
| ENG E-1 선행 조건 — Phase 5 기안 함수 자신의 트랜잭션 안 전역 `db` | HIGH(막음에서 파생) | 「선행 의존」 표 `ENG E-1 기반` 줄: 있으면 코드를 바꾸지 않고 멈춘다(동시 6건이 이 플랜과 무관하게 멈추므로 — 06-18 audit B 멈춤과 같은 꼴) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 배정된 검토 항목은 모두 반영했다. 통합 테스트 파일은 새로 만들지 않고 기존 `test/integration/pre-settle-check.test.ts` 안에 「기안 동시 6건」 케이스를 둔다 |

### Round 2 — 333f7bf(미커밋 반영분 교차 검토 · 점검: eng-apply-cross-r1 · eng-apply-checker-r1)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS-R1 F-1 — 「기안 동시 6건」이 타이밍에 기대 RED가 결정적이지 않다 | MEDIUM(고침) | Plan 19, Task 1 ⑧ · behavior · verify fails_when · read_first: PR #75 꼴(06-03 · 06-08과 같은 꼴)로 바꿨다 — `pool.options.max` 2 이상 6 미만 단언 → 풀 밖 `pg.Client`가 기안 트랜잭션이 처음 잠그거나 쓰는 행(기본 6개 프로젝트 행 — 프로젝트 FK INSERT의 `FOR KEY SHARE`도 막힌다, 아니면 문서 번호 카운터 행)을 `FOR UPDATE` → `pg_blocking_pids` 재귀로 풀 커넥션 전부가 막힌 것을 확인 → 해제 → 10초 경주. RED 변이는 `loadPreSettleInputs`를 막아 세우는 문 바로 뒤로 옮긴다. 막을 행이 없으면 멈춤. ⓪ · `ENG E-1 기반` 줄에 막을 행 기록 · T-06-141 |
| CROSS-R1 F-2 — `door`의 온라인구매 협력사 설정이 `PreSettleInputs`에 없고 구조 grep이 도우미를 못 본다 | MEDIUM(고침) | Plan 19, Task 2 action: `loadPreSettleInputs`가 `PURCHASE_ONLINE_VENDOR_NAME`을 읽어 `PreSettleInputs.onlineVendorName` → `resolveLineDoor({ vendorName }, inputs.onlineVendorName)`, 거래처 이름은 `tx` 조회. 06-13 `rowActionBlock` 도우미는 인자를 받는 판만 부르고, 아니면 `resolveLineDoor` · 게이트를 직접 부른다(`domain/quotes/lines.ts`는 고치지 않음). Task 2 acceptance: 구조 grep을 `domain/quotes/line-door.ts`와 도우미 본문까지 넓힘. truths `**ENG E-1:**` 줄 · artifacts · read_first 보강 |
| CROSS-R1 F-6 · CHK-R1 W2 — 멈춤 조건(`ENG E-1 기반`, Phase 5 기안 트랜잭션)의 소유 · 추적이 없다 | MEDIUM(경고) | 「선행 의존」 `ENG E-1 기반` 줄에 소유 · 추적을 적었다: Phase 5 실행 때 그 페이즈가 확인하고 코디네이터가 추적한다. 멈춤 조건은 그대로 둔다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 F-1 · F-2 · F-6 · W2는 모두 반영했다 |

### Round 3 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 13)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| M-9 — S18(「정산 결재 올리기」 자리)이 아직 없는 Phase 5 UI-SPEC(UA-610)에 기댄다(design-review.md L56@f2720a8, 반영 지시 13 「06-15/06-17/06-19 착수 게이트」) | LOW(참고) | 「선행 의존(A-6xx)」 새 줄 「M-9 · UA-610」(05-UI-SPEC 없으면 멈춤 · 대조 표를 SUMMARY에 · 어긋나면 06-UI-SPEC 먼저, `06-19 착수하지 않음`) · Task 1 acceptance ⓪ 결과에 M-9 대조 표. 페이즈 전체 게이트는 06-VALIDATION 「실행 착수 게이트(M-9)」 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 것은 M-9 한 줄뿐이다(반영 지시 13의 M-6 대표 승인 막힘 화면은 이 플랜 몫이 아니다 — Shard B) |

### Round 4 — e2c8922(디자인 검토 반영 r2 — 미커밋 1회차 반영분 점검 `design-apply-checker-r1.md` · 교차 `design-apply-cross-r1.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W4 — read_first의 UI-SPEC 줄 번호(`735-761행` · `280행` · `284행` · `299-300행` · `328-329행` · `374-383행` · `752-758행`)가 rev 9에서 다른 절을 가리킨다(design-apply-checker-r1.md 경고 4 · 교차 R-7) | 경고(고침) | Plan 19: Task 1 read_first → 「S18. 완료 전 점검 — …」 절 전체 · Copywriting 행 이름 여섯(「Empty — 완료 전 점검(S18)」 · 「Error — 섹션 로드」 · 「막힘 — 정산 결재 올리기(S18, Phase 5 버튼)」 · 「막힘 — 대표 승인(S18, O-16)」 · 「표시 — 완료 전 점검 섹션(S18)」 · 「표시 — 점검 행」). Task 2 read_first → 「지출결의 상태 → 1차 (정본 …)」 절 표 아래 문단(「행동 뒤에는 늘 응답 상태로 이 표를 다시 읽는다」 ~ 「`면제`가 `선결제`를 이기므로」) · S18 절 「행 모양·행동」 줄 ~ 「정산 결재 올리기(UA-610)」 줄(막힘과 미결 구분 · 강행 결과 줄). 줄 번호를 남기지 않았다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 것은 W4 한 줄뿐이고 반영했다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-19-SUMMARY.md` when done
</output>
