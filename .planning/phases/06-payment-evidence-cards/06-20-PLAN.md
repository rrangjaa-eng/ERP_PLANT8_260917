---
phase: 06-payment-evidence-cards
plan: 20
type: execute
wave: 10
depends_on: ["06-17"]
files_modified:
  - domain/payments/targets.ts
  - domain/payments/index.ts
  - repositories/payment-targets.ts
  - app/(app)/expenses/page.tsx
  - app/(app)/expenses/paid-list.tsx
  - app/(app)/expenses/payment-targets-table.tsx
  - app/(app)/expenses/[id]/payment-section.tsx
  - app/(app)/expenses/[id]/page.tsx
  - app/(app)/expenses/[id]/payment-action-row.tsx
  - test/integration/payment-batch.test.ts
  - test/e2e/payment-batch.spec.ts
autonomous: true
requirements: [EXP-09]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "[O-17 기본값] 계좌 행 · 계좌 열 · `번호 보기`는 지급 권한자에게만 서버가 보낸다 — 다른 역할(대표 포함)의 응답 · DOM에는 계좌 문자열이 없다(마스킹된 꼬리 4자리도 없다)"
    - "지급 완료 보기(S3)와 Phase 5 목록의 상태 열에는 계좌 글자가 없고 지급 방식 낱말만 있다 — 대표도 이 목록을 읽는다"
    - "S1 지급 방식 칸: 계좌이체면 1행 `{은행} ****-**-{뒤4}`, 2행 예금주 + 권한자 3차 `번호 보기`(main `AccountNumberCell` 재사용 — `mask_reveal` 로그), 법인카드 · 현금은 낱말만"
    - "S3 empty: 「Empty — 지급 완료 보기」"
    - "S3 loading: 첫 로드 = §7-7 LOADING 뼈대, 보기 · 팀 · 월 필터 이동은 GET 이동"
    - "S3 error: 「Error — 목록 로드」 `지급 완료 목록을 불러오지 못했습니다 · 다시 시도`(「다시 시도」 = 2차)"
    - "S3 populated: 지급일 주 그룹(`이번 주` → `지난주` → `MM-DD ~ MM-DD`) · 합계 줄 `합계 (지급 완료 · {월} · N건)`(서버 `sumKrw`) · 이체액 2행 차이 · 처리한 사람 · 1차 없음, Phase 5 목록 상태 열 값 `지급 완료`(`success`) + 2행 날짜(열 신설 없음, UA-605)"
    - "S3 partial: 차이 0이면 2행 없음, 차이 사유 없으면 `—`, 지급 뒤 들어온 선결제 증빙이 `확인 전`이면 증빙 칸 3차 `증빙 확인` — ≥1024는 제자리 확인 모달(06-17 `EvidenceConfirmDialog`, 결과 줄 없음 · 선택 없음, 성공하면 그 행 증빙 칸 `확인됨` + 포커스 그 행 링크 · 지급 기록 그대로 — DR-4. S3 행 `version`을 모달의 `rowVersion`으로 넘겨 06-17과 같이 열 때 비교하고 보기가 준 `version`으로 확인한다 — r2 B-1), 1024 미만은 문서 화면 S4"
    - "**C-1(rev 9) 문서 화면 왕복:** `from=pay`로 연 문서 화면(S4 · S5)에만 행동 줄 오른쪽 3차 `지급 대상으로`(받은 쿼리 중 허용 키 `view` · `team` · `evidence` · `page`만 들고 `/expenses`로 — 경로 고정)가 서고, 그 문서가 `확인됨`이 되면 같은 자리에 3차 `다음 확인 전 26001-0009` 꼴(서버가 같은 필터 · 목록 순서의 다음 `확인 전` 번호를 보낸다, 없으면 렌더하지 않음)이 하나 더 선다. 1차는 「상태 → 1차」 표 그대로 하나다"
    - "**C-1 ④(rev 9) `[증빙 전체 ▾]`:** S1 필터 값 `증빙 전체` · `확인 전`(P2) · `없음`(P3) · `지급 가능`(P4) — 쿼리 `evidence=unreviewed` · `missing` · `payable`, 합계 줄 · 페이지는 그 필터 전체, 필터를 바꾸면 선택과 보관 키가 빈다"
    - "**M-3(rev 9) 문서 화면:** `증빙 확인` 응답으로 1차가 `지급 완료`로 바뀐 직후의 `Ctrl+Enter`는 `event.repeat`면 무시하고 새 1차가 그려진 뒤 새로 누른 것만 받으며, 포커스는 이체액 칸으로 옮긴다 — 연타로 이체액 · 지급일을 보지 않고 지급되지 않는다"
    - "**M-7(rev 9):** 계좌이체인데 거래처 계좌가 없는 행도 S1에서 P4 그대로 고를 수 있다(`selectable` = true) — 표시는 `계좌 없음 · 계좌 등록은 관리자`, 게이트 규칙 없음"
    - "S3 overflow: 50건 페이지(§7-16), 좁은 PC · 폰은 P3(지급 방식 · 차이 사유 · 처리한 사람)부터 숨긴다"
    - "S3 zero-one-many: 0건 = Empty 한 줄, 건수는 합계 줄 `N건` 한 꼴"
    - "S5 empty: 값 없음은 `—`(지급 예정일이 비어도 `—`), 차이 사유 행은 차이가 있을 때만, 계좌이체인데 계좌 없음 = `계좌 없음 · 계좌 등록은 관리자`, 결재 통과 전 문서에는 섹션이 없다"
    - "Phase 5 목록 상태 열: 지급 기록 있음 → `지급 완료` + 2행 날짜, 선결제 기한 경과 → 2행 `증빙 {N}일 경과`(06-10 `prepaidDueInfo`) — 열을 새로 만들지 않는다"
    - statement: "S3 long-text: 항목 · 거래처 한 줄 말줄임 + `title`, 표 안 차이 사유 두 줄까지 — 긴 거래처 · 200자 사유로 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "지급 권한자가 아닌 사람의 응답 · DOM에 계좌 문자열(마스킹 꼬리 포함)이 실려서는 안 된다(O-17)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/payments/targets.ts"
      provides: "지급 완료 목록 DTO + 계좌 칸 투영(권한자만) + 증빙 필터 · 다음 확인 전 번호(C-1)"
      exports: ["listPaidPayments", "listPaymentTargets", "nextUnreviewedTarget"]
    - path: "app/(app)/expenses/[id]/payment-action-row.tsx"
      provides: "`from=pay` 3차 `지급 대상으로` · `다음 확인 전 {번호}` · 확인 직후 `Ctrl+Enter` 반복 무시(M-3)"
    - path: "app/(app)/expenses/paid-list.tsx"
      provides: "S3 지급 완료 보기"
      min_lines: 40
  key_links:
    - from: "domain/payments/targets.ts"
      to: "domain/payments/index.ts"
      via: "목록과 지급 섹션이 같은 계좌 투영 판정을 쓴다"
      pattern: "expenses.payments"
    - from: "app/(app)/expenses/payment-targets-table.tsx"
      to: "app/(app)/admin/vendors/account-number.tsx"
      via: "계좌 해제는 기존 `AccountNumberCell`과 정보 항목 판정을 그대로 쓴다"
      pattern: "AccountNumberCell"
    - from: "app/(app)/expenses/paid-list.tsx"
      to: "app/(app)/expenses/payment-targets-table.tsx"
      via: "S3 제자리 증빙 확인이 06-17의 같은 모달을 쓴다(DR-4)"
      pattern: "EvidenceConfirmDialog"
    - from: "app/(app)/expenses/[id]/page.tsx"
      to: "domain/payments/targets.ts"
      via: "`다음 확인 전 {번호}`가 목록과 같은 조회 · 순서에서 온다(C-1)"
      pattern: "nextUnreviewedTarget"
---

<objective>
지급의 **읽기 쪽**을 마감한다 — 계좌 노출(O-17), 「지급 완료」 보기(S3), Phase 5 목록 상태 열, 지급 섹션 계좌 행(S5 empty). **rev 9(`/plan-design-review` 반영):** S3 `확인 전` 행의 제자리 증빙 확인(DR-4 — 06-17 모달 재사용), 문서 화면 왕복의 문서 화면 쪽(C-1 — `from=pay` 3차 `지급 대상으로` · `다음 확인 전 {번호}`)과 S1 `[증빙 전체 ▾]` 필터(C-1 ④), 문서 화면 확인 직후 `Ctrl+Enter` 반복 무시(M-3), 계좌 없는 행도 고를 수 있음(M-7). 목록 쪽 왕복(번호 링크 · 보관 · 되살리기)은 06-17.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 대표는 무엇이 언제 얼마 나갔는지 읽어야 하지만 계좌를 볼 이유는 없다. 계좌는 은행에 이체하는 경영관리에게만 간다.

Output: `listPaidPayments`, 계좌 칸 투영(목록 · 지급 섹션), `/expenses?view=paid`, Phase 5 목록 상태 열 값, S3 제자리 확인, `nextUnreviewedTarget` · 증빙 필터 · 문서 화면 3차 둘, 통합 1(확장) · E2E 1(확장).

**규모 근거:** `files_modified` 11개, 태스크 4, `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-08의 셋째 몫. rev 9에서 Task 4(C-1 문서 화면 쪽 · 증빙 필터 · M-3)와 문서 화면 두 파일이 더해져 70000 → 95000, 태스크 3 → 4(plan-checker 기준 경고 — 차단 아님). 06-17이 파일 한도(14)로 받지 못한 C-1 절반을 여기서 받는다 — 목록 조회 · 필터 줄 파일이 이 플랜에 이미 있다.

**계획 판단(UA-613):** `AccountNumberCell`을 `ui/`로 옮기지 않는다. `app → app` import는 eslint boundaries 규칙이 허용하고, 옮기면 관리자 거래처 액션을 prop으로 빼는 리팩터가 따라온다(CLAUDE.md §3.3). 지급 화면은 `@/app/(app)/admin/vendors/account-number`에서 그대로 가져다 쓰고, `canReveal`은 그 해제 액션과 같은 정보 항목 판정으로 서버가 계산한다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-15-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-17-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-20 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| UA-613 | `AccountNumberCell`(main `app/(app)/admin/vendors/account-number.tsx`) · 해제 판정 = 정보 항목 `vendor.account_number_unmasked` | `git ls-files 'app/(app)/admin/vendors/account-number.tsx'` 비면 멈춤 |
| A-607 · UA-605 | Phase 5 지출결의 목록의 상태 열 자리(06-15 ⓪이 찾은 파일) | `grep -n "status\|상태" 'app/(app)/expenses/page.tsx'` 결과를 SUMMARY에 적는다 — 상태 열이 이 플랜 파일 밖 컴포넌트에 있으면 멈추고 그 파일 이름을 보고한다(파일 한도) |
| UA-618 | 지출결의의 지급 방식 칸(계좌이체 · 법인카드 · 현금)과 거래처 계좌 참조 | `grep -rn "payment_method\|paymentMethod" db/schema/` 0건이면 멈춤 |
| 06-15 · 06-17 | `listPaymentTargets` · `payment-targets-table.tsx` · 보기 필터 | `grep -c "view=pay\|\"pay\"" 'app/(app)/expenses/page.tsx'` 0이면 멈춤 |
| 06-17(rev 9) | `EvidenceConfirmDialog`(제자리 확인 모달 — S3가 import) · `getEvidenceConfirmViewAction` · S1 보관 키 함수 · 번호 링크 `from=pay` | `grep -c "EvidenceConfirmDialog" 'app/(app)/expenses/payment-targets-table.tsx'` 0이면 멈춤 · `grep -c "sessionStorage" 'app/(app)/expenses/payment-targets-table.tsx'` 0이면 멈춤 |
| 06-04 · 06-06 | 문서 화면 행동 줄 `app/(app)/expenses/[id]/payment-action-row.tsx`(1차 · `Ctrl+Enter`)와 증빙 확인 응답 상태 | `git ls-files 'app/(app)/expenses/[id]/payment-action-row.tsx'` 비면 멈춤 |
| M-9 · UA-605 | **실행 착수 게이트(rev 9):** Phase 5 UI-SPEC이 있고 06-UI-SPEC 「Phase 4·5 의존 가정 (UI)」의 UA-605(지출결의 목록 · 문서 화면)와 대조를 마쳤다 — S3 「지급 완료」 보기와 S1 「문서 화면 왕복」의 문서 화면 쪽(3차 `지급 대상으로` · `다음 확인 전 {번호}`)이 이 줄에 기댄다 | `git ls-files .planning/phases/05-expense-approval-leave/05-UI-SPEC.md` 비면 멈춤 · 대조 표(UA-605 줄마다 Phase 5 UI-SPEC의 라우트 · 필터 이름 · 문서 화면 섹션 순서 · 1차 규칙가 06-UI-SPEC과 같은지 · 다르면 어디)를 SUMMARY에 적고, 어긋난 줄이 하나라도 있으면 코드를 바꾸지 않고 `선행 의존 M-9 미해소 — 06-UI-SPEC {UA-id} 먼저 고침, 06-20 착수하지 않음`을 출력하고 멈춘다 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-17 | 계좌는 지급 권한자에게만, 다른 역할에는 키 자체를 보내지 않음 | `domain/payments/targets.ts` · 지급 섹션 DTO의 계좌 칸 투영 한 함수 |
| UA-613 | `AccountNumberCell` 재사용(옮기지 않음) | 해당 없음 |
| **O-21** (사용자 확정(2026-09-25) — DR-4 「예」) | S3(≥1024) `확인 전` 행 3차 = 06-17 제자리 확인 모달 | **Task 3 한 갈래**: 답이 「아니오」면 3차를 문서 화면 링크로만 둔다(06-17 O-21 행과 함께) |
| 되돌림 쿼리 허용 키 | `view` · `team` · `evidence` · `page`(계획 판단 — UI-SPEC 「지금 보기 · 필터 · 쪽」) | `app/(app)/expenses/[id]/page.tsx` zod 스키마 한 곳 |

<assumption_delta_decision>
`detected: false`. 목록 · 지급 섹션 DTO에 계좌 칸을 권한 판정 뒤에만 **더한다**. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-09 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: 기준 1 · O-17 · UA-605 · UA-613. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 대표가 「지급 완료」 보기에서 지급된 건을 읽고, 그 페이지 DOM에 계좌 문자열이 없다</name>
  <files>domain/payments/targets.ts, repositories/payment-targets.ts, app/(app)/expenses/page.tsx, app/(app)/expenses/paid-list.tsx, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S3. 「지급 완료」 보기」 절(「Phase 5 목록에 더하는 것」 포함) · UI Considerations S3 행 · Copywriting 「표시 — 지급 방식 · 계좌」 · 「Empty — 지급 완료 보기」 (rev 9에서 줄 번호가 밀려 절 이름으로 찾는다)
    - domain/payments/targets.ts · repositories/payment-targets.ts(06-15 · 06-17)
    - app/(app)/expenses/page.tsx(보기 필터)
  </read_first>
  <behavior>
    - `listPaidPayments(viewer, { team, month, page })`: 지급 권한자 · 대표 → 전부 읽기 / 그 밖 → Phase 5 목록 범위 그대로 / 행에 지급 방식 낱말만(계좌 키 없음) / 지급 취소된 기록 제외
    - 합계 줄 이체액 = 서버 `sumKrw`
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

`repositories/payment-targets.ts`에 지급 완료 조회(살아 있는 지급 기록 + 지출결의 · 프로젝트 · 거래처 이름 · 지급 방식 · 처리한 사람, 계좌 칸 없음)를 더한다. `domain/payments/targets.ts`의 `listPaidPayments` — 지급일 주 그룹(이번 주 → 지난주 → 날짜 범위, KST 월요일 시작)은 서버가 정하고, 합계는 `sumKrw`. DTO 등록(계좌 칸 없음). `page.tsx` 보기 필터에 `지급 완료`(`view=paid`)를 더하고 `paid-list.tsx`(S3 — 이 태스크는 열 · 그룹 · 합계 줄까지): 번호 · 프로젝트 · 항목 · 거래처 · 지급 방식(낱말만) · 지급일 · 이체액(2행 차이, 있을 때만) · 차이 사유 · 증빙 · 처리한 사람, 1차 없음, 행 → 문서 화면. 상태 글자는 `status-display.ts`의 기존 값만.

통합에 behavior 두 줄, E2E에 「대표 계정 `view=paid` → 지급된 건 한 줄 · 페이지 HTML에 거래처 계좌 뒤 4자리 문자열 없음」.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 대표 계정 페이지 HTML에 거래처 계좌 뒤 4자리 문자열이 없음을 단언한다
    - 통합 테스트가 지급 취소된 기록이 목록에 없음을 단언한다
    - ⓪ 결과에 M-9 대조 표(UA-605 줄 같음/다름)가 SUMMARY에 있다(rev 9)
  </acceptance_criteria>
  <done>지급 완료 목록이 대표에게 계좌 없이 읽히고, 합계가 서버 값이다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [O-17 기본값] 계좌 투영 — 지급 대상 지급 방식 칸 · 지급 섹션 계좌 행(S5 empty) · `번호 보기`</name>
  <files>domain/payments/targets.ts, domain/payments/index.ts, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/[id]/payment-section.tsx, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「권한·노출 (역할별)」 절(계좌) · Copywriting 「표시 — 지급 방식 · 계좌」(계좌 없음 = P4 그대로, M-7) · S1 「선택 열(SP-1)」 · UI Considerations S5 empty 행
    - app/(app)/admin/vendors/account-number.tsx(`AccountNumberCell` props) · domain/vendors/index.ts 360-395행(마스킹 · 해제 판정)
    - domain/payments/index.ts(`getPaymentView` — 06-03)
  </read_first>
  <behavior>
    - 지급 권한자 DTO: 계좌이체 행에 은행 · 마스킹 번호 · 예금주 · `canReveal`(정보 항목 판정) / 지급 권한 없음(대표 · PM · 직원) DTO: 계좌 키 자체가 없음
    - 계좌이체인데 거래처 계좌 없음 → `계좌 없음` 표시 값(권한자에게만) + 그 행 `selectable` = true(P4 그대로 — M-7, 통합 `it` 「계좌 없음 행 고를 수 있음」)
    - 권한자 `번호 보기` → 기존 해제 액션 → `mask_reveal` 로그
  </behavior>
  <action>
계좌 칸 투영을 `domain/payments/targets.ts`에 한 함수로 두고 `listPaymentTargets`와 `getPaymentView`(06-03, `domain/payments/index.ts`)가 같은 함수를 부른다 — 판정은 `can(viewer, "expenses.payments", "write")`, 거짓이면 계좌 키를 결과 객체에 싣지 않는다(O-17). `payment-targets-table.tsx` 지급 방식 칸: 계좌이체면 1행 `{은행} ****-**-{뒤4}`, 2행 예금주 + 권한자 3차 `번호 보기`(`AccountNumberCell`을 `@/app/(app)/admin/vendors/account-number`에서), 법인카드 · 현금은 낱말만. 계좌 유무는 선택 판정에 넣지 않는다 — 계좌 없는 계좌이체 행도 `resolveExpenseActionRow`가 P4면 고를 수 있다(M-7, 06-15 must_haves). `payment-section.tsx`(S5) 계좌 행: 권한자에게만, 계좌 없음이면 `계좌 없음 · 계좌 등록은 관리자`, 값 없음 `—`, 차이 사유 행은 차이가 있을 때만, 결재 통과 전 문서에는 섹션 없음.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/expense-payments.test.ts test/integration/vendors.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/payment-single.spec.ts test/e2e/vendors.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 지급 권한 없는 세 계급의 목록 · 지급 섹션 DTO에 계좌 관련 키가 하나도 없음을 객체 키로 단언한다
    - 통합 `it` 「계좌 없음 행 고를 수 있음」이 계좌 없는 계좌이체 P4 행의 `selectable` = true를 단언한다(M-7) — `grep -c "계좌 없음 행 고를 수 있음" test/integration/payment-batch.test.ts`가 1
    - E2E가 「권한자 `번호 보기` → 전체 번호 · `mask_reveal` 로그 1줄」을 단언한다
    - 기존 vendors 통합 · E2E 녹색(AccountNumberCell 재사용이 관리자 화면을 깨뜨리지 않음)
  </acceptance_criteria>
  <done>계좌가 지급 권한자에게만 가고, 목록과 지급 섹션이 같은 판정 한 곳을 쓴다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: S3 마감(빈 · 오류 · 부분 · 페이지 · 좁은 폭 · 제자리 증빙 확인 DR-4) · Phase 5 목록 상태 열</name>
  <files>domain/payments/targets.ts, app/(app)/expenses/page.tsx, app/(app)/expenses/paid-list.tsx, test/e2e/payment-batch.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S3. 「지급 완료」 보기」 절 · 「S1 제자리 증빙 확인」의 「성공(S3)」 · Copywriting 「확인 — 증빙 확인(S1 · S3 제자리)」(S3은 결과 줄 없음) · UI Considerations S3 행 · Copywriting 「Error — 목록 로드」
    - app/(app)/expenses/payment-targets-table.tsx의 `EvidenceConfirmDialog`(06-17 — `rowVersion` prop · 보기 `version`으로 확인 · 열 때 불일치 = 1차 `aria-disabled`, r2 B-1) · 06-UI-SPEC 「S1 제자리 증빙 확인」의 「서버(r2 B-1)」 · 「실패」 줄
    - domain/evidence-reviews/prepaid.ts(`prepaidDueInfo` — 06-10)
  </read_first>
  <behavior>
    - 0건 → 「Empty — 지급 완료 보기」 / 로드 실패 → 「Error — 목록 로드」 + 2차 `다시 시도`
    - 차이 0 → 2행 없음 / 사유 없음 → `—` / 지급 뒤 선결제 증빙 `확인 전` → 증빙 칸 3차 `증빙 확인`
    - S3 제자리 증빙 확인(DR-4, E2E 「S3 제자리 증빙 확인」): ≥1280에서 그 3차 → 모달(제목 `증빙 확인` · 부제 · 첨부 보기 칸 · 결과 줄 없음 · 1차 `증빙 확인` `Ctrl+Enter`) → 확인 → 모달 닫힘 · 그 행 증빙 칸 `확인됨` · 포커스 = 그 행 링크 · 그 문서 지급 기록 그대로 / 800px에서는 같은 3차가 문서 화면 링크 / [r2 B-1] 모달이 열리면 1차가 켜져 있다(S3 행 `version` = 보기 `version` — 행 DTO에 `version`이 없거나 넘기지 않으면 1차가 `aria-disabled`로 선다)
    - 51건 → 둘째 쪽 1건 · 합계 줄은 필터 전체
    - Phase 5 목록 상태 열: 지급 기록 있음 → `지급 완료` + 2행 날짜 / 선결제 기한 경과 → 2행 `증빙 {N}일 경과`
  </behavior>
  <action>
`paid-list.tsx`에 필터 `[지급 완료] [팀] [월]`(GET 이동), Empty · 오류, 50건 `Pagination`, 좁은 PC · 폰에서 P3(지급 방식 · 차이 사유 · 처리한 사람)부터 숨김, 첫 로드 §7-7 LOADING 뼈대를 마감한다. 증빙 칸 3차 `증빙 확인`은 ≥1024에서 06-17의 `EvidenceConfirmDialog`를 S3 갈래로 연다(결과 줄 · 선택 없음 — 지급 뒤 문서라 서버 계산 한 줄이 없다, S4 「지급 뒤 들어온 증빙」). S3 행 DTO(`domain/payments/targets.ts`의 지급 완료 목록)에 문서 `version`을 싣고 모달에 `rowVersion`으로 넘긴다 — 모달이 06-17과 같이 열 때 비교하고 보기가 준 `version`으로 확인하며, 실패 · 동시성의 `새로 고침`은 S3 목록과 모달 보기를 다시 받는다(`확인 전`이 아니게 됐으면 모달을 닫고 포커스 그 행 링크 — r2 B-1, 06-17 ③과 같은 흐름. 새 판정 없음). 성공하면 그 행 증빙 칸을 응답 상태(`확인됨`)로 바꾸고, 3차가 사라지므로 포커스를 그 행 링크로 옮긴다(§7-17 「트리거가 사라졌으면」의 이 표면 적용). 1024 미만은 문서 화면 S4 링크 그대로(DR-4 · O-21). Phase 5 목록의 상태 열(⓪이 찾은 자리 — `page.tsx`)에 서버가 보낸 지급 상태 값을 `statusDisplay`로 그리고 2행(지급 날짜 또는 `증빙 {N}일 경과` — 기한 계산은 `prepaidDueInfo`)을 싣는다. 열을 새로 만들지 않는다(UA-605). 토큰은 `tokens.css`의 것만.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/evidence-waive-prepaid.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — 「S3 제자리 증빙 확인」에서 1차가 `aria-disabled`로 서면 S3 행 DTO가 `version`을 싣지 않았거나 모달에 넘기지 않았다(r2 B-1)</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 behavior 다섯 줄을 개별 케이스로 단언한다 — `grep -c "S3 제자리 증빙 확인" test/e2e/payment-batch.spec.ts`가 1
    - E2E 「S3 제자리 증빙 확인」이 확인 뒤 그 문서의 살아 있는 지급 기록 수가 1 그대로(DB)이고 `document.activeElement`가 그 행 링크임을 단언한다
    - E2E가 Phase 5 목록의 열 머리글 수가 이 플랜 전과 같음을 단언한다(열 신설 없음)
    - [r2 B-1] `grep -c "rowVersion" 'app/(app)/expenses/paid-list.tsx'`가 1 이상이고, E2E 「S3 제자리 증빙 확인」이 모달이 열린 뒤 1차 `aria-disabled`가 아님을 단언한다
  </acceptance_criteria>
  <done>지급 완료 보기와 Phase 5 목록 상태 열이 계약대로 서고, 열이 늘지 않으며, 지급 뒤 들어온 증빙을 목록을 떠나지 않고 확인한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 문서 화면 왕복(C-1 보조 경로) — `from=pay` 3차 `지급 대상으로` · `다음 확인 전 {번호}` · `[증빙 전체 ▾]` 필터 · 문서 화면 확인 직후 `Ctrl+Enter` 반복 무시(M-3)</name>
  <files>domain/payments/targets.ts, repositories/payment-targets.ts, app/(app)/expenses/page.tsx, app/(app)/expenses/payment-targets-table.tsx, app/(app)/expenses/[id]/page.tsx, app/(app)/expenses/[id]/payment-action-row.tsx, test/integration/payment-batch.test.ts, test/e2e/payment-batch.spec.ts</files>
  <precondition>06-17이 S1 목록 쪽 왕복(번호 링크 `from=pay` · `sessionStorage` 보관 · 되살리기)을 세웠다 — 「선행 의존」 06-17(rev 9) 줄</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S1 문서 화면 왕복 — `from=pay` · 편집·선택 보관(보조 경로)」 · S1 「보기 필터」(`[증빙 전체 ▾]`) · S4 「확인 직후 1차 바뀜(M-3)」 · 「`from=pay`로 열렸을 때」 · Copywriting 「3차 버튼」(`지급 대상으로` · `다음 확인 전 26001-0009`) · 「SUCCESS — 제자리 결과(토스트 없음)」의 M-3 문장 · S1 머리 스토리보드 2 · 6단계
    - app/(app)/expenses/[id]/page.tsx · payment-action-row.tsx(06-04 · 06-06 — 행동 줄 1차 · `Ctrl+Enter`) · app/(app)/expenses/payment-targets-table.tsx(06-17 — 보관 키 · 번호 링크 쿼리)
    - domain/payments/targets.ts · repositories/payment-targets.ts(06-15 · 06-17 — 목록 순서 · 그룹 · 행의 증빙 판정 입력)
  </read_first>
  <behavior>
    - `listPaymentTargets({ evidence })`(통합 `it` 「증빙 필터」): `unreviewed` → P2 행만 · `missing` → P3만 · `payable` → P4만 · 없음 → 전부, 합계 줄 · 페이지는 그 필터 전체 / 모르는 값 → 필터 없음
    - `nextUnreviewedTarget(viewer, { afterExpenseId, team, evidence })`(통합 `it` 「다음 확인 전 번호」): 같은 필터 · 목록 순서(예정일 그룹 → 번호)에서 그 문서 다음의 `확인 전`(P2) 번호 · id / 없으면 null / 지급 권한 없음 → null
    - E2E 「문서 화면 왕복 — 지급 대상으로」: S1에서 행 A 이체액 편집 + 행 B 선택 → 행 C(`확인 전`) 번호 링크 → 문서 화면 행동 줄 오른쪽 3차 `지급 대상으로` → 금액을 고쳐 `증빙 확인` → 같은 자리 3차 `다음 확인 전 {번호}`가 선다 → `지급 대상으로` → A 편집값 · B 선택 그대로 + C가 P4(선택 칸 켜짐) / `다음 확인 전` → 다음 확인 전 문서 화면 · `from=pay` 쿼리 유지
    - 조작 쿼리: 허용 밖 키 · 바깥 URL 값을 붙여 연 문서 화면의 `지급 대상으로` href는 `/expenses?`로 시작하고 허용 키(`view` · `team` · `evidence` · `page`)만 가진다 / `from=pay` 없이 연 문서 화면에는 두 3차가 없다
    - 필터(E2E): `[증빙 전체 ▾]` → `확인 전` → URL `evidence=unreviewed` · P2 행만 · 그 전에 고른 선택과 보관 키가 비어 있다
    - E2E 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」(M-3 · r2 F-4 순서): P2 문서에서 `keyboard.down('Control')` · `keyboard.down('Enter')`(첫 누름 — 1차 `증빙 확인`) → **1차 라벨이 `지급 완료`로 바뀌고 포커스 = 이체액 칸이 될 때까지 기다린다**(키는 떼지 않는다) → `Control`을 누른 채 `keyboard.down('Enter')` 한 번 더(Playwright가 이미 눌린 키라 `repeat: true`로 보낸다 — 새 1차 `지급 완료` 자리) → 증빙 확인 한 번 · 지급 기록 0건(DB) · 1차 `지급 완료` 그대로 · 포커스 = 이체액 칸 → 키를 떼고 새로 `Control+Enter` → 06-04 단건 지급 1차 동작이 시작된다(06-04 단건 지급 E2E의 첫 반응과 같은 단언 — 가드가 새 누름까지 막지 않음)
  </behavior>
  <action>
C-1 ① · ④와 M-3의 문서 화면 몫을 반영한다(06-17이 목록 쪽 — 번호 링크 `from=pay` · 보관 · 되살리기 — 을 세웠다). 화면 문구는 UI-SPEC rev 9 Copywriting 행 그대로.

① **필터.** `repositories/payment-targets.ts` 목록 조회에 증빙 값 조건(`unreviewed` P2 · `missing` P3 · `payable` P4)을 더한다 — 판정 입력은 06-15가 행에 실은 증빙 판정 입력 그대로이고 판정표를 새로 쓰지 않는다. SQL로 거를 수 없는 갈래면 domain에서 `resolveExpenseActionRow` 결과로 거른 뒤 쪽을 자른다(어느 쪽인지 SUMMARY에). `listPaymentTargets`에 `evidence`. `app/(app)/expenses/page.tsx` 필터 줄에 `[증빙 전체 ▾]`(값 `증빙 전체` · `확인 전` · `없음` · `지급 가능` — 쿼리 `evidence=unreviewed` · `missing` · `payable`, GET 이동). `payment-targets-table.tsx`의 보관 키에 증빙 값을 더하고, 번호 링크 · 1024 미만 증빙 칸 3차의 `from=pay` 쿼리에 `evidence`를 싣는다. 필터를 바꾸면 선택과 그 키가 빈다(06-17 규칙 그대로).

② **다음 확인 전.** `domain/payments/targets.ts`에 `nextUnreviewedTarget(viewer, { afterExpenseId, team, evidence })` — 지급 권한이 없으면 null, `listPaymentTargets`와 같은 조회 · 순서에서 그 문서 다음의 P2 한 건(번호 · id). 트랜잭션 없는 읽기.

③ **문서 화면.** `app/(app)/expenses/[id]/page.tsx`가 `searchParams`에서 `from=pay`와 허용 키(`view` · `team` · `evidence` · `page`)만 zod로 읽고 그 밖의 키 · 값은 버린다. 되돌림 href는 늘 `/expenses` 경로에 그 허용 키로 새로 만든다 — 받은 URL을 그대로 되돌려 보내지 않는다(열린 리디렉션 없음). `payment-action-row.tsx`: `from=pay`일 때만 행동 줄 오른쪽에 3차 `지급 대상으로`, 응답 상태가 `확인됨`이면 같은 자리에 3차 `다음 확인 전 {번호}`(`nextUnreviewedTarget` 결과 — null이면 렌더하지 않음 · 같은 `from=pay` 쿼리를 이어 든다). 1차는 「상태 → 1차」 표 그대로 하나다. **M-3:** `증빙 확인` 응답으로 1차가 `지급 완료`로 바뀐 직후의 `Ctrl+Enter`는 `event.repeat`면 무시하고 새 1차가 그려진 뒤 새로 누른 것만 받는다. 포커스는 이체액 칸으로 옮긴다(입력 → 1차 흐름).

④ **테스트.** 통합(`payment-batch.test.ts`)에 「증빙 필터」 · 「다음 확인 전 번호」 `it`. E2E에 behavior 셋째~여섯째 줄(이름: 「문서 화면 왕복 — 지급 대상으로」 · 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」). 증빙 행은 Phase 5 domain 함수로 만든다. 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」는 둘째 `keyboard.down('Enter')`를 1차가 `지급 완료`로 바뀌고 포커스가 이체액 칸으로 간 **뒤**에 둔다 — 그 전에 누르면 확인 요청 중인 1차(`…`)가 키를 먹어 가드 없이도 초록이 된다(r2 F-4). RED를 먼저 본다: `payment-action-row.tsx`의 `event.repeat` 가드를 잠시 빼면 이 케이스가 지급 기록 1건 또는 지급 흐름 시작으로 빨갛다 — 확인하고 되돌린 뒤 SUMMARY에 RED · GREEN 한 줄씩 적는다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/payment-batch.test.ts test/integration/expense-payments.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 「증빙 필터」가 빨가면 필터가 판정표와 다른 조건으로 걸렀고, 「다음 확인 전 번호」가 빨가면 목록과 다른 순서를 쓰거나 지급 권한 없는 사람에게 번호를 냈다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/payment-batch.spec.ts test/e2e/payment-single.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — 「문서 화면 왕복 — 지급 대상으로」가 빨가면 돌아온 S1에서 편집 · 선택이 사라졌거나 되돌림 링크가 허용 밖 쿼리를 실었고, 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」에서 지급 기록이 생기거나 지급 흐름이 시작되면 `event.repeat` 가드가 없다(RED 방법: 가드를 잠시 빼면 이 단계가 빨갛다 — r2 F-4), payment-single이 빨가면 행동 줄 3차가 `from=pay` 없는 문서 화면에도 섰거나 1차 규칙이 바뀌었다</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "문서 화면 왕복 — 지급 대상으로\|문서 화면 확인 직후 Ctrl+Enter 반복 무시" test/e2e/payment-batch.spec.ts`가 2 이상이고, `grep -c "증빙 필터\|다음 확인 전 번호" test/integration/payment-batch.test.ts`가 2 이상이다
    - E2E가 조작 쿼리로 연 문서 화면의 `지급 대상으로` href가 `/expenses?`로 시작하고 허용 키만 가짐을 단언한다
    - E2E가 `from=pay` 없이 연 문서 화면에 `지급 대상으로` · `다음 확인 전` 요소 수 0을 단언한다
    - E2E 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」가 지급 기록 0건(DB)과 `document.activeElement` = 이체액 칸을 단언한다
    - [r2 F-4] 그 케이스의 둘째 `keyboard.down('Enter')`가 1차 라벨 `지급 완료` · 이체액 칸 포커스를 기다린 **뒤**에 있고(스펙의 두 줄 순서를 SUMMARY에 적는다), 가드를 뺀 RED · 되돌린 GREEN 한 줄씩이 SUMMARY에 있다
  </acceptance_criteria>
  <done>금액을 고쳐야 하는 건도 문서 화면에서 확인한 뒤 한 번에 목록으로 돌아오거나 다음 확인 전 건으로 이어 가고, 흩어진 확인 전 건을 필터로 모으며, 확인 직후 연타로 지급되지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 → 브라우저(목록 · 지급 섹션 DTO) | 계좌(민감)가 지급 권한자에게만 나간다 |
| 브라우저 → 계좌 해제 액션 | 기존 관리자 거래처 해제 액션 재사용 |
| 브라우저 → 문서 화면 `searchParams`(`from=pay` · 되돌림 쿼리) | 사람이 바꿀 수 있는 쿼리가 되돌림 링크로 다시 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-99 | Information Disclosure | 계좌 번호(O-17) | high | mitigate | 계좌 키를 권한 판정 뒤에만 DTO에 싣는 한 함수. 해제는 기존 `vendor.account_number_unmasked` + `mask_reveal` 로그. 계급별 DTO 키 단언 + 대표 DOM 단언 |
| T-06-100 | Information Disclosure | 지급 완료 목록 범위 | medium | mitigate | 지급 권한자 · 대표 외에는 Phase 5 목록 범위 그대로 · DTO 등록 → 누수 스캔 |
| T-06-101 | Repudiation | 계좌 번호 열람 | low | mitigate | 기존 해제 액션의 `mask_reveal` 로그를 그대로 쓴다. E2E로 로그 1줄 확인 |
| T-06-175 | Tampering | `from=pay` 되돌림 쿼리(열린 리디렉션 · 쿼리 주입) | medium | mitigate | 허용 키(`view` · `team` · `evidence` · `page`)만 zod로 읽고 경로를 `/expenses`로 고정해 새로 만든다 — 받은 URL · 경로를 되돌려 보내지 않는다. E2E 조작 쿼리 케이스 |
| T-06-176 | Information Disclosure | `다음 확인 전 {번호}` | low | mitigate | `nextUnreviewedTarget`는 지급 권한이 없으면 null(3차 없음) · 같은 필터 범위의 번호 하나만. 통합 「다음 확인 전 번호」 |
| T-06-177 | Tampering | S3 제자리 증빙 확인 | low | mitigate | 06-17 `EvidenceConfirmDialog` · 06-06 `confirmEvidenceAction` 그대로(잠금 · version · 권한 — 06-17 T-06-171). 새 판정 없음. S3 행 `version`을 `rowVersion`으로 넘기고 확인은 보기가 준 `version`으로 — 모달을 연 뒤 바뀐 증빙이 확인되지 않는다(r2 B-1 · 06-17 T-06-233) |
</threat_model>

<verification>
- 통합 payment-batch · expense-payments · vendors · leak-scan 녹색
- E2E payment-batch · payment-single · vendors · evidence-waive-prepaid가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S1 지급 방식 칸 · `[증빙 전체 ▾]` · S3(제자리 확인 모달 포함) · S5 계좌 행 · `from=pay` 문서 화면 행동 줄 3차 DOM 실측 감사(backstop 1건 · 계좌 문자열 부재 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 계좌가 지급 권한자에게만 가고, 대표도 계좌 없이 지급 완료 목록을 읽는다
- 지급 완료 보기와 Phase 5 목록 상태 열이 계약대로 선다
- 문서 화면에 다녀와도 한 번에 지급 대상으로 돌아오고(C-1), 확인 직후 연타로 지급되지 않는다(M-3)
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `listPaidPayments` · 계좌 칸 투영 함수 · `nextUnreviewedTarget`(C-1) |
| 변경된 기존 심볼 | `listPaymentTargets`(계좌 칸 · `evidence` 필터) · `getPaymentView`(계좌 행) · Phase 5 목록 상태 열 값 · `payment-action-row.tsx`(`from=pay` 3차 둘 · M-3) · S1 보관 키(+증빙 값) |
| 화면 | `/expenses?view=paid`(S3 — 제자리 증빙 확인 포함) · S1 지급 방식 칸 · S1 `[증빙 전체 ▾]` · S5 계좌 행 · `from=pay` 문서 화면 행동 줄 |
| 테스트 | `test/integration/payment-batch.test.ts`(확장 — 「계좌 없음 행 고를 수 있음」 · 「증빙 필터」 · 「다음 확인 전 번호」) · `test/e2e/payment-batch.spec.ts`(확장 — 「S3 제자리 증빙 확인」 · 「문서 화면 왕복 — 지급 대상으로」 · 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」) |

## Review Dispositions Ledger

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 1 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 5 · 7 · 13)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 반영 지시 7 · DR-4 — S3 `확인 전` 행 3차를 제자리 확인 모달로(design-review.md L81@f2720a8 · L35@f2720a8) | HIGH(고침) | must_haves S3 partial 줄 · Task 3 name · read_first · behavior 「S3 제자리 증빙 확인」 · action(06-17 `EvidenceConfirmDialog` S3 갈래 — 결과 줄 · 선택 없음, 성공 뒤 포커스 그 행 링크, 1024 미만 문서 화면) · acceptance(지급 기록 1 그대로 · 포커스) · key_links `EvidenceConfirmDialog` · 선행 의존 06-17(rev 9) · 열린 선택 O-21 · T-06-177 |
| DR-4 + C-1 ① — `from=pay` 문서 화면 쪽 3차 `지급 대상으로` · `다음 확인 전 {번호}`(L35@f2720a8) | HIGH(고침, 병합) | 새 Task 4 ② ③ · must_haves 「C-1(rev 9) 문서 화면 왕복」 · artifacts `nextUnreviewedTarget` · `payment-action-row.tsx` · key_links · 선행 의존 06-04 · 06-06 · 열린 선택 「되돌림 쿼리 허용 키」 · T-06-175 · T-06-176. 06-17이 파일 한도(14)로 이 절반을 넘겼다(06-17 ledger Deferred 첫 줄) |
| C-1 ④ — S1 `[증빙 전체 ▾]` 필터(L35@f2720a8) | MEDIUM | Task 4 ① · behavior 「증빙 필터」 · must_haves 「C-1 ④(rev 9)」. 「내 차례」 `확인 전 N건` → `evidence=unreviewed`는 UI-SPEC이 넣지 않았다(GSTACK REVIEW REPORT) — 06-23 변경 없음 |
| M-3 — 확인 직후 `Ctrl+Enter` 연타(L50@f2720a8) | LOW(참고) | 문서 화면(S4) 몫: Task 4 ③ M-3 · behavior · acceptance(지급 기록 0 · 포커스 이체액 칸) · must_haves 「M-3(rev 9) 문서 화면」. S1 모달 몫은 06-17 Task 4. 06-04 · 06-06이 만든 `payment-action-row.tsx`를 이 태스크가 고친다(웨이브 10, 같은 웨이브에 이 파일을 고치는 플랜 없음) |
| M-7 — 계좌 없는 계좌이체 행 선택(L54@f2720a8) | LOW(참고) | must_haves 「M-7(rev 9)」 · Task 2 read_first · behavior · action(계좌 유무를 선택 판정에 넣지 않음) · acceptance 통합 `it` 「계좌 없음 행 고를 수 있음」 |
| M-9 — 핵심 표면이 아직 없는 Phase 5 UI-SPEC에 기댄다(L56@f2720a8) | LOW(참고) | 반영 지시 13은 06-15 · 06-17 · 06-19를 이름 댔지만, C-1 문서 화면 쪽(06-17에서 옮겨 옴)과 S3이 이 플랜에 있어 같은 줄을 둔다: 「선행 의존(A-6xx)」 새 줄 「M-9 · UA-605」 · Task 1 acceptance ⓪ 대조 표. 페이즈 전체 게이트는 06-VALIDATION 「실행 착수 게이트(M-9)」 |
| (줄 번호 표류) Task 1~3 read_first의 UI-SPEC 줄 번호가 rev 9에서 밀렸다 | LOW | 절 이름으로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 이 플랜이 태스크 4 · 95000 토큰으로 커졌다 | 경고로 둔다 — plan-checker 기준 태스크 4는 경고(차단 아님), 파일 11. 새 플랜으로 쪼개려면 ROADMAP · 웨이브를 오케스트레이터가 바꿔야 해서 이 샤드 권한 밖이다 |

### Round 2 — 디자인 검토 반영 r2(커밋 전 · `/mnt/project-files/06-gates/design-apply-cross-r1.md` · `design-apply-r2-brief.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 F-4 — M-3 E2E의 둘째 keydown이 확인 요청 중에 떨어져 `event.repeat` 가드 없이도 초록이 된다 — 문서 화면 케이스도 같은 모양(design-apply-cross-r1.md F-4) | 고침 | Plan 20, Task 4 behavior 「문서 화면 확인 직후 Ctrl+Enter 반복 무시」 순서: 첫 누름 → 1차 `지급 완료` · 포커스 = 이체액 칸을 기다림 → `Control`을 누른 채 `keyboard.down('Enter')` 한 번 더(`repeat: true`) → 지급 기록 0 → 떼고 새로 누르면 06-04 단건 지급 1차가 시작됨. action ④(순서 이유 · 가드를 뺀 RED 먼저) · fails_when · acceptance(두 줄 순서 · RED · GREEN을 SUMMARY에). 테스트 이름은 그대로 |
| 교차 B-1 — 제자리 확인 모달이 보지 않은 증빙을 확인할 수 있다(S3도 같은 `EvidenceConfirmDialog`를 쓴다, B-1) | 막음(06-11 · 06-17이 주로 닫음) | Plan 20, must_haves.truths S3 partial 줄 · Task 3 read_first · behavior(모달이 열리면 1차 켜짐 — S3 행 `version` = 보기 `version`) · action(S3 행 DTO에 `version` · 모달 `rowVersion` · `새로 고침` = S3 목록 + 보기 다시) · fails_when · acceptance(`rowVersion` grep · 1차 켜짐 단언) · T-06-177 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 체커 W3 — 이 플랜이 태스크 4 | 쪼개지 않음 — 오케스트레이터 판단(design-apply-r2-brief.md 「오케스트레이터 판단」)으로 경고 유지 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-20-SUMMARY.md` when done
</output>
