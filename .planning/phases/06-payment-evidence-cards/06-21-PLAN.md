---
phase: 06-payment-evidence-cards
plan: 21
type: execute
wave: 10
depends_on: ["06-18"]
files_modified:
  - domain/issue-requests/index.ts
  - repositories/revenue-issue-requests.ts
  - app/(app)/projects/actions.ts
  - app/(app)/projects/actions.registry.ts
  - app/(app)/projects/[id]/issue-request-table.tsx
  - app/(app)/projects/issue-requests/page.tsx
  - app/(app)/projects/issue-requests/issue-requests.module.css
  - app/(app)/projects/page.tsx
  - app/(app)/projects/filter-bar.tsx
  - test/unit/domain/issue-requests.test.ts
  - test/integration/issue-requests.test.ts
  - test/e2e/issue-requests.spec.ts
autonomous: true
requirements: [PROJ-06]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[O-12 기본값] 발행 요청 상태는 `요청` · `발행됨` · `취소` 셋이고 금액은 공급가다 — PM이 `Delete` → 확인 모달 `발행 요청 취소`로 `취소`로 바꾸면 줄은 남는다. 상태 전이는 순수 함수 하나(`issueRequestTransition`)에서만 정해진다"
    - "`요청` 상태이고 아직 발행 줄에 이어지지 않은 줄만 PM이 고치거나 취소할 수 있다 — `발행됨` · `취소` 줄의 수정 · 취소 요청은 서버가 요청 행을 잠근 뒤 거부한다. 완료(정산) 프로젝트의 취소는 기존 `project.completed-lock`으로 막힌다"
    - "취소 ∥ 발행 줄 잇기가 같은 요청을 동시에 노리면(`Promise.all`) 정확히 하나만 성공한다(06-18의 요청 행 `FOR UPDATE`를 같이 쓴다)"
    - "`/projects/issue-requests`(S17)는 매출 기록 권한자 · 대표(읽기)에게 서고, 프로젝트 목록 필터 줄 오른쪽 3차 `발행 요청 {열린 건수}`(0이면 `발행 요청`)로 들어간다 — PageHeader 부제 = 부모 메뉴 이름(SP-4)"
    - "[A-605] S17 목록 DTO도 06-18과 같은 투영을 쓴다 — `revenue.issued_amount`가 없는 사람에게 발행액 · 요청 금액 키가 없다"
    - "`/projects/{id}?issueRequest={rid}`로 들어오면 그 요청 줄로 스크롤 · 포커스한다(`발행 줄로`가 선 줄이면 그 3차, 아니면 줄 자체 `tabindex=\"-1\"`) — 쿼리는 `issue-request-table.tsx`가 `useSearchParams`로 읽는다(상세 `page.tsx`를 고치지 않는다)"
    - "S17 empty: 「Empty — 발행 요청 목록(S17)」이 선다"
    - "S17 loading: 첫 로드 = §7-7 LOADING 뼈대, 상태 필터 이동은 GET 이동이다"
    - "S17 error: 「Error — 목록 로드」 발행 요청 문구가 선다"
    - "S17 populated: 그룹 희망 발행일(`희망일 지남`(`--warning`) → `이번 주` → `다음 주` → `그 뒤`) · 열 · 합계 줄 `합계 (요청 · N건)` · 1차 없음 · 행 3차 `요청` 행 `발행 줄 입력` / `발행됨` · `취소` 행 `요청 보기`(1024 이상 · 매출 기록 권한자) — 둘 다 `/projects/{id}?issueRequest={rid}`"
    - "S17 partial: 1024 미만에는 행동 칸이 없고 행을 누르면 같은 주소의 보기 전용 상세로 간다"
    - "S17 overflow: 50건 페이지(§7-16, UA-603)"
    - "S17 zero-one-many: 프로젝트 목록 링크는 `발행 요청 N`, 0이면 `발행 요청`만 — 링크는 남는다(SP-4)"
    - statement: "S17 long-text: 프로젝트 이름 · 메모가 말줄임 + `title`로 선다 — 긴 이름 · 200자 메모로 CI=true DOM 감사"
      verification: backstop
    - "06-03 tx 규약을 따른다 — 이 플랜의 트랜잭션은 발행 요청 취소(`cancelIssueRequest`)다"
    - "**CROSS-R1 F-5:** 취소의 프로젝트 쓰기 권한과 완료 프로젝트 판정은 트랜잭션을 열기 **전에** 하고, 트랜잭션 안에서는 06-18의 요청 행 잠금(`lockIssueRequest(…, tx)`)을 먼저 잡은 뒤 요청 상태 · 이어짐 · `version`만 `tx`로 판정한다. `status_change` 행동 기록은 커밋 뒤에 남긴다"
  prohibitions:
    - statement: "`발행됨` 요청을 취소하거나 고쳐서 이어진 발행 줄과 요청이 어긋나서는 안 된다(O-12)"
      status: resolved
      verification: test
    - statement: "발행액을 볼 수 없는 사람의 S17 목록 DTO에 발행액 · 요청 금액이 실려서는 안 된다(A-605)"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/issue-requests/index.ts"
      provides: "상태 전이 함수 · 취소 · 목록(S17) DTO · 열린 건수"
      exports: ["issueRequestTransition", "cancelIssueRequest", "listIssueRequests", "countOpenIssueRequests"]
    - path: "test/unit/domain/issue-requests.test.ts"
      provides: "상태 전이 표 여섯 갈래"
      min_lines: 20
  key_links:
    - from: "app/(app)/projects/filter-bar.tsx"
      to: "domain/issue-requests/index.ts"
      via: "프로젝트 목록 필터 줄 링크 글자의 열린 건수(SP-4)"
      pattern: "countOpenIssueRequests"
    - from: "domain/issue-requests/index.ts"
      to: "repositories/revenue-issue-requests.ts"
      via: "취소가 트랜잭션 안에서 06-18의 요청 행 잠금을 먼저 잡는다(권한 · 완료 판정은 트랜잭션 전 — CROSS-R1 F-5)"
      pattern: "lockIssueRequest"
---

<objective>
발행 요청(06-18)을 마감한다 — 취소(O-12)와 상태 전이 한 함수, 경영관리의 발행 요청 목록(S17), 프로젝트 목록의 하위 목록 링크(SP-4), `?issueRequest` 포커스.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 경영관리가 프로젝트를 하나씩 열지 않고 발행 요청을 한 목록에서 보고, 상세의 정확한 자리로 들어가 닫는다.

Output: `issueRequestTransition` · `cancelIssueRequest` · `listIssueRequests` · `countOpenIssueRequests`, `/projects/issue-requests`, 필터 줄 링크, 단위 1 · 통합 1(확장) · E2E 1(확장).

**규모 근거:** `files_modified` 12개, `estimate.tokens` 85000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-05의 둘째 몫.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-18-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-21 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-18 | `revenueIssueRequests` · `linkIssueRequestToEntry` · 요청 행 잠금 · S16 표 | `grep -c "linkIssueRequestToEntry" domain/issue-requests/index.ts` 0이면 멈춤 |
| UA-603 | `ui/pagination/Pagination.tsx`(S17 50건) | `git ls-files ui/pagination/Pagination.tsx` 비면 멈춤 |
| UA-602 | `ui/confirm-dialog/ConfirmDialog.tsx`(`발행 요청 취소`) | `git ls-files ui/confirm-dialog/ConfirmDialog.tsx` 비면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-12** | 상태 셋(`요청` · `발행됨` · `취소`) + 금액 = 공급가, 취소는 줄을 남김 | **Task 1 하나**: `issueRequestTransition` · 스키마 CHECK(06-18) · 취소 액션(대안 「취소 = 행 삭제 + 합계 기준」이면 셋 다) |
| O-1 | 발행 요청 목록 = 프로젝트 메뉴의 하위 목록(SP-4) | 라우트 `app/(app)/projects/issue-requests/` · 필터 줄 링크 |
| AS2 | 취소 = `status_change` | `recordAction` 인자 |

<assumption_delta_decision>
`detected: false`. 06-18 계약에 취소 · 목록 · 링크를 **더한다**. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: PROJ-06 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: D-610 · D-613 · O-12 · SP-4. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — [O-12 기본값] PM이 `요청` 줄을 취소하면 줄이 `취소`로 남고, `발행됨` 줄 취소는 서버가 거부한다</name>
  <files>domain/issue-requests/index.ts, repositories/revenue-issue-requests.ts, app/(app)/projects/actions.ts, app/(app)/projects/actions.registry.ts, app/(app)/projects/[id]/issue-request-table.tsx, test/unit/domain/issue-requests.test.ts, test/integration/issue-requests.test.ts, test/e2e/issue-requests.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 717-722행 · 807행(S16 권한 행) · Copywriting 「Destructive — 발행 요청 취소」
    - /mnt/project-files/06-prep/open-choices.md 21행(O-12)
    - domain/issue-requests/index.ts · repositories/revenue-issue-requests.ts(06-18 — 요청 행 잠금)
  </read_first>
  <behavior>
    - `issueRequestTransition(from, action)`: 요청 → 발행됨(잇기) / 요청 → 취소(PM) / 발행됨 → 무엇이든 거부 / 취소 → 무엇이든 거부 / 요청 → 수정 허용 / 발행됨 → 수정 거부
    - `cancelIssueRequest`: 프로젝트 쓰기 권한 없음 → 거부(트랜잭션을 열기 전) / 이어진 요청 → 거부(잠금 뒤) / 완료 프로젝트 → 거부(트랜잭션을 열기 전) / 정상 → `취소` + 사람 · 시각, 줄은 남음, `status_change` 로그(커밋 뒤)
    - 취소 ∥ 잇기 `Promise.all` → 정확히 하나 성공
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

`domain/issue-requests/index.ts`에 순수 함수 `issueRequestTransition`과 `cancelIssueRequest(viewer, { requestId, version })`를 더하고 파일 머리 한 줄 주석에 "O-12 답이 바뀌면 상태 전이 함수 · 스키마 CHECK · 취소 액션만 고친다"를 적는다. 06-18의 `saveIssueRequestRows` · `linkIssueRequestToEntry`가 상태 판정을 이 함수로 부르게 바꾼다(판정 두 벌 없음). 취소는 06-03 tx 규약대로 나눈다(CROSS-R1 F-5): **트랜잭션 전** — 요청 행을 잠금 없이 읽어 프로젝트 id를 얻고, 프로젝트 쓰기 권한(`can`)과 완료 프로젝트(`project.completed-lock` — 프로젝트 행은 잠금 없는 사전 조회)를 판정한다. **트랜잭션 몸통** `const runCancel = async (tx: DbOrTx) => { … };` → `withTransaction(runCancel)` — 06-18의 요청 행 잠금 `lockIssueRequest(…, tx)`를 먼저 잡고, 잠근 행의 프로젝트 id가 사전 조회와 다르면 거부한 뒤, 요청 상태 · 이어짐 · `version`만 `issueRequestTransition`으로 판정하고 `tx`로 쓴다. `status_change` 행동 기록은 커밋 뒤에 남긴다(`deferRecord` 꼴). 권한 · 완료 판정을 잠금 뒤에 두면 요청 행 잠금과 커넥션을 쥔 채 전역 풀(`findPermission` · `findProjectById`)을 기다린다 — PR #75와 같은 꼴. `app/(app)/projects/actions.ts`에 `cancelIssueRequestAction`, 기존 `actions.registry.ts`에 등록(새 registry 파일 아님 — 누수 스캔 import 이미 있음). 표에서 PM이 `요청` · 미연결 줄에 `Delete` → `ConfirmDialog`(Copywriting 「Destructive — 발행 요청 취소」) → 취소 뒤 줄은 `취소` 상태로 남는다.

단위 `issue-requests.test.ts`(신규)에 전이 표, 통합에 취소 갈래와 병렬 케이스, E2E에 「PM `Delete` → 모달 → 줄 `취소`」.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/issue-requests.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 단위 테스트가 전이 표 여섯 갈래를, 통합 테스트가 취소 네 갈래와 병렬 케이스(`Promise.all` · 성공 수 1)를 개별 `it`으로 단언한다
    - `grep -rln "issueRequestTransition" domain/` 결과가 `domain/issue-requests/index.ts` 한 파일이다(판정이 한 곳)
    - [CROSS-R1 F-5] 취소 트랜잭션 몸통 안에 전역 `db` 판정 · 조회가 없다: `awk '/const runCancel = async/,/^  };$/' domain/issue-requests/index.ts | grep -v '^\s*//' | grep -cE '\bdb\b|\b(can|visible|scopeFor|getSettingValue|recordAction|findProjectById|findPermission)\('`가 0이고, 같은 범위에서 `lockIssueRequest(`가 1건 이상이다(두 결과를 SUMMARY에)
  </acceptance_criteria>
  <done>발행 요청의 세 상태와 전이가 한 함수에서 정해지고, 취소가 줄을 남기며, 잇기와 경합해도 한쪽만 이긴다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 발행 요청 목록(S17) · 노출(A-605) · 50건 페이지</name>
  <files>domain/issue-requests/index.ts, repositories/revenue-issue-requests.ts, app/(app)/projects/issue-requests/page.tsx, app/(app)/projects/issue-requests/issue-requests.module.css, test/integration/issue-requests.test.ts, test/e2e/issue-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 726-733행(S17) · 1042-1049행(S17 considerations) · Copywriting 「Empty — 발행 요청 목록(S17)」 · 「Error — 목록 로드」
    - app/(app)/projects/page.tsx · app/(app)/projects/projects-table.tsx(목록 선례 · 그룹 머리글 · 합계 줄)
  </read_first>
  <behavior>
    - `listIssueRequests(viewer, { status, page })`: 매출 기록 권한자 · `projects.revenue` 읽기 권한자만, 그 밖 → 거부 / 기본 필터 `요청` / 그룹 희망일 지남 → 이번 주 → 다음 주 → 그 뒤(KST) / 합계 줄 건수 · 금액(서버 `sumKrw`) / 50건 페이지
    - `revenue.issued_amount` 없는 읽기 계정 → 행 · 합계 줄에 금액 키 없음
  </behavior>
  <action>
`listIssueRequests`(필터 `요청` 기본 · `발행됨` · `취소` · `전체`, 50건 페이지, 그룹 판정 서버, 합계 `sumKrw`)를 더하고 DTO 투영은 06-18의 A-605 함수를 그대로 쓴다. `app/(app)/projects/issue-requests/page.tsx`(§6-1 읽기 목록): PageHeader 제목 `발행 요청` · 부제 = 부모 메뉴 이름 `프로젝트`(SP-4) · 1차 없음 · 열 희망 발행일 · 프로젝트(번호 · 이름) · 요청자 · 금액(2행 합계) · 메모 · 상태 + 행동 3차(1024 이상 · 매출 기록 권한자만 — `요청` 행 `발행 줄 입력`, `발행됨` · `취소` 행 `요청 보기`, 둘 다 `/projects/{id}?issueRequest={rid}`). 1024 미만에는 행동 칸이 없고 행을 누르면 같은 주소. Empty · 로드 오류는 Copywriting 행 그대로. `issue-requests.module.css`에는 말줄임 · 간격만, 값은 `tokens.css` 토큰만.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 두 줄의 갈래(권한 거부 · 그룹 · 합계 · 노출 키 · 51건 페이지)를 단언한다
    - E2E가 Empty · 그룹 순서 · 1024 미만 행동 칸 부재를 단언한다
  </acceptance_criteria>
  <done>경영관리가 발행 요청을 한 목록에서 그룹 · 합계와 함께 보고, 발행액은 정보 항목 밖으로 새지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 프로젝트 목록 하위 링크(SP-4) · `?issueRequest` 포커스</name>
  <files>domain/issue-requests/index.ts, app/(app)/projects/page.tsx, app/(app)/projects/filter-bar.tsx, app/(app)/projects/[id]/issue-request-table.tsx, test/integration/issue-requests.test.ts, test/e2e/issue-requests.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 723행(`?issueRequest` 포커스) · 887-891행(SP-4)
    - app/(app)/projects/page.tsx · app/(app)/projects/filter-bar.tsx
    - Next.js 16 `useSearchParams` 문서(`node_modules/next/dist/docs/` — CLAUDE.md §10)
  </read_first>
  <behavior>
    - `countOpenIssueRequests(viewer)`: `요청` 상태 건수(보는 사람의 목록 권한 범위) / 권한 없음 → null(링크 없음)
    - 링크 글자: N ≥ 1 → `발행 요청 N`, 0 → `발행 요청`, 권한 없음 → 링크 없음
    - `?issueRequest={rid}`: `발행 줄로`가 선 줄 → 그 3차에 포커스 / 아니면 줄 자체 포커스 / 없는 id → 무반응
  </behavior>
  <action>
`countOpenIssueRequests`를 더한다. `projects/page.tsx`가 건수를 읽어 `filter-bar.tsx`에 넘기고, 필터 줄 오른쪽에 3차 링크 `발행 요청 {N}`(링크는 `projects.revenue` 읽기 권한이 있을 때만 — 대표 포함). `issue-request-table.tsx`가 `useSearchParams`로 `issueRequest`를 읽어 마운트 뒤 그 줄로 스크롤 · 포커스한다(`발행 줄로` 3차가 있으면 그 버튼, 없으면 줄 `tabindex="-1"`) — 상세 `page.tsx`는 고치지 않는다. `useSearchParams`를 쓰는 클라이언트 컴포넌트의 Suspense 경계 요구는 Next.js 16 문서대로 따른다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/issue-requests.test.ts test/integration/projects-list.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다. projects-list가 빨가면 목록 필터 줄 변경이 기존 목록을 깨뜨렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/issue-requests.spec.ts test/e2e/projects-list.spec.ts test/e2e/projects-filter-reset.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E에 「프로젝트 목록 링크 `발행 요청 1` → 목록 → `발행 줄 입력` → 상세의 `발행 줄로`에 포커스」 케이스가 녹색이다
    - 기존 projects-list · projects-filter-reset 스펙 녹색
  </acceptance_criteria>
  <done>프로젝트 목록에서 열린 발행 요청 수로 들어가 상세의 정확한 자리에 도착한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 취소 액션 | 요청 id · version |
| 서버 → 브라우저(S17 DTO · 링크 건수) | 발행액 · 요청 금액이 정보 항목을 지난 사람에게만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-102 | Tampering | `발행됨` 요청 취소 · 수정 | medium | mitigate | 전이 함수 한 곳 + 요청 행 잠금 뒤 판정 + `version`. 거부 통합 테스트 |
| T-06-144 | Denial of Service | 요청 행 잠금을 쥔 취소 트랜잭션이 전역 풀로 권한 · 프로젝트를 읽어 커넥션 풀 고갈 교착(CROSS-R1 F-5) | medium | mitigate | 권한 · 완료 판정은 트랜잭션 전, 몸통 `runCancel`은 요청 행 잠금 뒤 `tx` 판정만(06-03 tx 규약) · 행동 기록 커밋 뒤 · Task 1 구조 grep |
| T-06-103 | Tampering | 취소 ∥ 잇기 경합 | medium | mitigate | 같은 요청 행 `FOR UPDATE`. `Promise.all` 통합 테스트 |
| T-06-104 | Information Disclosure | S17 발행액 · 요청 금액(A-605) | high | mitigate | 06-18 투영 함수 재사용 · DTO 등록 → 누수 스캔 계급별 검사 |
| T-06-105 | Elevation of Privilege | S17 목록 접근 | medium | mitigate | `listIssueRequests`가 매출 기록 권한 · `projects.revenue` 읽기를 domain 첫 줄에서 확인 |
| T-06-106 | Repudiation | 요청 취소 | low | mitigate | `status_change` 로그 + 취소한 사람 · 시각 칸, 줄 삭제 없음 |
</threat_model>

<verification>
- 단위 issue-requests, 통합 issue-requests · projects-list · leak-scan 녹색
- E2E issue-requests · projects-list · projects-filter-reset가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S16 취소 · S17 DOM 실측 감사(backstop 1건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 발행 요청의 세 상태가 한 함수로 정해지고 취소가 줄을 남긴다
- 경영관리가 목록에서 발행 요청을 보고 상세의 정확한 자리로 들어간다
- 발행액이 정보 항목 밖으로 새지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `issueRequestTransition` · `cancelIssueRequest` · `listIssueRequests` · `countOpenIssueRequests` |
| Server Action | `cancelIssueRequestAction` |
| 라우트 | `/projects/issue-requests`(S17) |
| 변경된 기존 심볼 | `saveIssueRequestRows` · `linkIssueRequestToEntry`(상태 판정을 전이 함수로) · 프로젝트 목록 필터 줄 링크 |
| 테스트 | `test/unit/domain/issue-requests.test.ts` · `test/integration/issue-requests.test.ts`(확장) · `test/e2e/issue-requests.spec.ts`(확장) |

## Review Dispositions Ledger

> 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Round 2 — 333f7bf(미커밋 반영분 교차 검토 · 점검: eng-apply-cross-r1 · eng-apply-checker-r1)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CROSS-R1 F-5 — `cancelIssueRequest`가 요청 행 잠금 **뒤에** 권한 · 완료 프로젝트를 판정한다(원문 06-21:143 · 137 — `can` → `findPermission`, `findProjectById` 모두 전역 `db`) | MEDIUM(고침) | Plan 21, Task 1 action: 권한 · 완료 판정을 트랜잭션 전으로, 몸통 `runCancel`은 `lockIssueRequest(…, tx)` 뒤 요청 상태 · 이어짐 · `version`만 `tx`로 판정, 행동 기록은 커밋 뒤. behavior 갈래에 판정 시점 표시 · acceptance 구조 grep(몸통 안 전역 `db` · 판정 0건, 잠금 1건 이상) · must_haves.truths `**CROSS-R1 F-5:**` 줄 · key_links 문구 · T-06-144 |
| ENG E-3 참조 — 06-03 tx 규약 | MEDIUM(고침, 06-03 소관) | must_haves.truths 한 줄 「06-03 tx 규약을 따른다」(이 플랜은 Round 1 반영 범위 밖이었다 — 이번 라운드에 더함) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 F-5는 반영했다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-21-SUMMARY.md` when done
</output>
