---
phase: 06-payment-evidence-cards
plan: 22
type: execute
wave: 11
depends_on: ["06-19"]
files_modified:
  - domain/approvals/index.ts
  - app/(app)/approvals/[id]/page.tsx
  - app/(app)/status-display.ts
  - app/(app)/projects/[id]/page.tsx
  - app/(app)/projects/[id]/quote-table.tsx
  - app/(app)/projects/[id]/pre-settle-check.tsx
  - app/(app)/projects/[id]/project-detail.module.css
  - test/integration/pre-settle-check.test.ts
  - test/e2e/pre-settle-check.spec.ts
autonomous: true
requirements: [PROJ-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[O-16 기본값] 대표가 정산 결재 문서에서 `승인`을 누르기 전에 같은 판정(`computePreSettleCheck` → `project.pre-settle-check`)이 같은 트랜잭션에서 한 번 더 돈다. 막히면 `승인` 1차가 비활성이 되고 `완료 전 점검 {N}건 · 점검 보기`(→ `/projects/{id}#pre-settle-check`)가 선다. 04.1 승인 함수를 직접 불러도 거부된다. 다른 문서 종류의 승인은 점검을 부르지 않는다"
    - "강행 허용 그룹만 남은 정산 결재 문서에는 결과 줄 `강행 · 미결 지출결의 {N}건`이 현재 판정으로 선다(PS-4 — 스냅숏 칸 없음)"
    - "[D-601] 대표 = 전부 읽기이고 대행 버튼이 없다 — 대표 화면의 점검 행 3차는 담당 표기(`담당 PM {이름}`)로 바뀐다"
    - "미매칭 줄의 3차는 서버가 권한별로 보낸 값만 그린다 — 프로젝트 쓰기 권한 있음 · 온라인구매 줄 → `구매 요청`(→ `/cards/purchases?new=1&line={id}`), 그 밖 → `지출결의 올리기`, 거래처 없는 줄 → S14 막힘, 쓰기 권한 없음 → `담당 PM {이름}`(버튼 없음). 카드 폼으로는 가지 않는다"
    - "매출 미입력 행의 3차는 폭 1024 이상에서만 선다. 담당 PM에게는 `발행 요청 추가`, 매출 기록 권한자에게는 `발행 줄 추가`(둘 다 가지면 `발행 줄 추가` — PS-3)가 원장의 기존 줄 추가 함수와 **같은 onClick**으로 선다. 1024 미만에는 버튼 없이 `발행 줄 없음`만 보인다"
    - "계약 금액이 아직 승인 전이면 매출 줄에 `계약 금액 —` 뒤 3차 `고객 승인 전`이 서고, 누르면 견적 차수 머리의 고객 승인 자리로 스크롤 · 포커스한다(PS-6, Phase 4 D-56)"
    - "S18 empty: 세 그룹이 다 비면 그룹 자리에 한 줄 `미결 없음`(`--success`)이 서고, 매출 줄은 그대로 남는다"
    - "S18 error: 점검 로드가 실패하면 섹션 자리에 `점검 불러오지 못함 · 다시 시도`(「다시 시도」 = 2차)가 서고, 「정산 결재 올리기」는 비활성 + `점검 결과 없음 · 다시 시도`다. 점검 없이 기안하지 않는다"
    - "S18 partial: 막힘이 0이어도 강행 허용 그룹의 항목은 보이고, 부제는 `정산 결재 전 · 막힘 없음`이다. 증빙 `면제` 문서는 미결 지출결의 행에 없다"
    - "S18 zero-one-many: 비어 있는 그룹만 접는다"
    - "점검 행 상태 글자 네 값(`결재 중` `--accent` · `반려` `--danger` · `증빙 없음` `--danger` · `선결제 · 증빙 없음` `--danger`)은 `app/(app)/status-display.ts` 매핑을 거친다 — UI-SPEC Color 매핑표와 같다"
    - statement: "S18 loading: 섹션 로드는 §7-7 LOADING 꼴을 섹션 단위로 따른다 — 느린 응답을 강제해 CI=true DOM 감사"
      verification: backstop
    - statement: "S18 overflow: 섹션 표라 페이지가 없다 — 미결 30건 프로젝트에서 그룹마다 항목이 전부 서는지 CI=true DOM 감사"
      verification: backstop
    - statement: "S18 long-text: 긴 항목 이름이 점검 행에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "강행 허용이 꺼진 그룹에 항목이 남아 있는데 대표 승인이 통과하면 안 된다(O-16)"
      status: resolved
      verification: test
    - statement: "점검 행의 3차 버튼 종류를 화면이 권한 · 줄 속성으로 추론하면 안 된다 — 서버가 보낸 값만 그린다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/(app)/projects/[id]/pre-settle-check.tsx"
      provides: "S18 표시 상태 마감(빈 · 오류 · 강행 · 권한별 3차 · 1024 규칙 · 긴 이름)"
      min_lines: 80
    - path: "test/e2e/pre-settle-check.spec.ts"
      provides: "S18 갈래와 대표 승인 막힘 E2E"
      min_lines: 60
  key_links:
    - from: "domain/approvals/index.ts"
      to: "domain/pre-settle-check/index.ts"
      via: "[O-16] 정산 결재 승인 앞 재점검"
      pattern: "computePreSettleCheck"
    - from: "app/(app)/projects/[id]/pre-settle-check.tsx"
      to: "app/(app)/projects/[id]/quote-table.tsx"
      via: "매출 3차가 원장의 기존 줄 추가 함수를 prop으로 받아 같은 onClick을 부른다"
      pattern: "addIssued"
---

<objective>
완료 전 점검(06-19)을 대표 승인 경로까지 닫고(O-16), S18 섹션의 표시 상태를 마감한다 — 빈 · 오류 · 강행 · 권한별 3차 · 1024 규칙 · 계약 금액 미승인.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 기안 뒤 대표 승인까지 사이에 새 미결이 생기면 기안 때 점검만으로는 막지 못한다. 그리고 PM이 섹션에서 그 줄의 다음 한 수를 바로 누를 수 있어야 점검이 막힘이 아니라 안내가 된다(D-611~D-613).

Output: 04.1 승인 함수의 정산 결재 분기 재점검, 결재 문서 화면 막힘 · 결과 줄, `pre-settle-check.tsx` 마감, `status-display.ts` 네 값, 통합 1(확장) · E2E 1(확장).

**규모 근거:** `files_modified` 9개, `estimate.tokens` 70000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-09의 둘째 몫.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-19-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-22 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-606 | 04.1 결재 모듈의 승인 함수(가칭 `domain/approvals/index.ts`)와 결재 문서 화면(가칭 `app/(app)/approvals/[id]/page.tsx`) | `git ls-files 'domain/approvals/*' 'app/(app)/approvals/*'`가 비면 멈춤 · 실제 파일이 가칭과 다르면 SUMMARY에 대응표 — 이 플랜 파일 밖이면 멈추고 보고 |
| 06-19 | `computePreSettleCheck` · `project.pre-settle-check` · 섹션 · 오류 prop | `grep -c "computePreSettleCheck" domain/pre-settle-check/index.ts` 0이면 멈춤 |
| 06-18 | 원장의 발행 요청 줄 추가 함수 · Phase 4 `addIssued` | `grep -n "addIssued" 'app/(app)/projects/[id]/quote-table.tsx'` 0건이면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-16** | 대표 승인 앞에서 한 번 더 돈다. 막히면 승인이 비활성이다 | **Task 1 하나**: `domain/approvals/index.ts`의 정산 결재 승인 분기 한 곳 · 승인 화면의 막힘 표시 · 통합 테스트 한 케이스 · must_haves의 `[O-16 기본값]` 한 줄 |
| PS-3 (계획 판단) | 매출 미입력 3차는 하나만 — PM이면서 매출 기록 권한도 있으면 `발행 줄 추가` | `pre-settle-check.tsx`의 3차 고르기 한 곳 |
| PS-6 (계획 판단) | `계약 금액 — · 3차 고객 승인 전` = 글자 뒤 3차, 누르면 차수 머리 고객 승인 자리로 | `pre-settle-check.tsx` 매출 줄 |

<assumption_delta_decision>
`detected: false`. 04.1 승인 함수의 정산 결재 분기에 게이트 호출을 **더한다** — 다른 문서 종류는 바뀌지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: PROJ-06 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: ROADMAP 기준 4 · D-601 · D-611~D-613 · O-16. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — [O-16 기본값] 막힘 1건이 남은 정산 결재를 대표가 열면 `승인`이 비활성이고, 승인 함수 직접 호출도 거부된다</name>
  <files>domain/approvals/index.ts, app/(app)/approvals/[id]/page.tsx, test/integration/pre-settle-check.test.ts, test/e2e/pre-settle-check.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 759행(O-16) · 300행(「막힘 — 대표 승인」) · 1095행(O-16 선택지)
    - A-606 재확인에서 찾은 04.1 승인 함수와 결재 문서 화면
    - domain/pre-settle-check/index.ts(06-19)
  </read_first>
  <behavior>
    - 04.1 승인: 정산 결재 문서 · 막힘 1 → 거부 `완료 전 점검 1건 · 점검 보기` / 막힘 0 · 강행 그룹 있음 → 승인 / 다른 문서 종류 → 기존 동작 그대로(점검 리포지토리 호출 0회)
    - 점검 계산 예외 → 승인 거부(fail-closed)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다.

04.1 승인 함수(실제 이름)의 정산 결재 문서 분기에서, 승인 기록 전에 같은 트랜잭션 안에서 `computePreSettleCheck` → `gate(…, "project.pre-settle-check", { blockedCount })`를 부른다. 다른 문서 종류에는 손대지 않는다. 결재 문서 화면(실제 파일)에서는 정산 결재일 때 서버가 같은 DTO로 `승인` 1차의 비활성 여부와 이유를 정하고, 3차 `점검 보기`가 `/projects/{id}#pre-settle-check`로 가게 한다. 강행 그룹이 있으면 문서에 결과 줄 `강행 · …`을 현재 판정으로 보인다(PS-4).

통합에 behavior 두 줄, E2E에 「대표 계정의 정산 결재 문서에서 `승인` 비활성 + `완료 전 점검 {N}건 · 점검 보기`」.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/pre-settle-check.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 「막힘 1인 정산 결재를 04.1 승인 함수로 직접 승인하면 거부」 · 「지출결의 승인은 점검 리포지토리 호출 0회」 · 「점검 예외 → 승인 거부」를 단언한다
    - Phase 5 · 04.1의 기존 결재 통합 테스트(`git ls-files 'test/integration/*approv*' 'test/integration/*settle*'`로 찾은 것)가 녹색이다 — 찾은 목록을 SUMMARY에 적는다
  </acceptance_criteria>
  <done>대표 승인 앞에서도 같은 판정이 막고, 다른 결재 종류는 영향을 받지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: S18 권한별 3차 · 1024 규칙 · 계약 금액 미승인 3차 · 상태 글자 매핑</name>
  <files>app/(app)/status-display.ts, app/(app)/projects/[id]/page.tsx, app/(app)/projects/[id]/quote-table.tsx, app/(app)/projects/[id]/pre-settle-check.tsx, test/e2e/pre-settle-check.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 761행(폰 · 1024 미만) · 809행(S18 권한 행) · 1050-1057행(S18 considerations) · Color 절 점검 행 매핑
    - app/(app)/projects/[id]/quote-table.tsx의 `addIssued`(04 S6 3차) · 06-18의 발행 요청 줄 추가 함수 · 1024 미만 보기 전용 판정(DR-36)
    - app/(app)/status-display.ts(06-01 매핑) · ui/status-tag/StatusTag.tsx(`text` 변형)
  </read_first>
  <behavior>
    - 미매칭 3차: 쓰기 권한 · 온라인구매 줄 → `구매 요청` / 쓰기 권한 · 그 밖 → `지출결의 올리기` / 거래처 없음 → S14 막힘 / 쓰기 권한 없음 → `담당 PM {이름}`(버튼 0개)
    - 매출 3차: PM만 → `발행 요청 추가` / 매출 기록 권한만 → `발행 줄 추가` / 둘 다 → `발행 줄 추가` / 1024 미만 → 없음 + `발행 줄 없음`
    - `고객 승인 전` 3차 → 차수 머리 고객 승인 자리 포커스
    - 상태 네 값 → `statusDisplay` 색
  </behavior>
  <action>
`pre-settle-check.tsx`의 행 3차는 06-19 DTO에 실린 `door` · `rowActionBlock` · 권한 값만 그린다 — 클라이언트는 권한을 추론하지 않는다. 매출 3차는 `quote-table.tsx`가 이미 가진 줄 추가 함수(`addIssued` · 06-18의 요청 줄 추가)를 prop으로 받아 **같은 onClick**을 부른다. 1024 미만 판정은 Phase 4 편집 표가 보기 전용으로 바뀌는 판정을 그대로 쓴다(새 판정을 만들지 않는다). 매출 줄 계약 금액 미승인 갈래에 3차 `고객 승인 전`(PS-6). `page.tsx`는 점검 DTO의 권한 값을 계산해 넘기는 자리만 고친다. `status-display.ts`에 점검 행 상태 네 값을 더하고 색은 UI-SPEC Color 매핑표와 대조한다.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/status-display.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts test/e2e/quote-table.spec.ts test/e2e/revenue-section.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 ⑴ 폭 1023에서 `발행 요청 추가` · `발행 줄 추가` 요소 0개, 1280에서 1개 ⑵ 쓰기 권한 없는 계정의 미매칭 행에 `담당 PM` 글자가 있고 버튼 요소 0개 ⑶ 온라인구매 미매칭 줄의 `구매 요청` href를 단언한다
    - 단위 status-display 테스트가 네 값의 색을 단언한다
  </acceptance_criteria>
  <done>PM이 점검 행에서 그 줄의 다음 한 수를 바로 누르고, 권한 · 폭에 따라 서버가 정한 행동만 보인다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: S18 빈 · 오류 · 부분 · 접기 · 긴 이름 · 폰 표시</name>
  <files>app/(app)/projects/[id]/pre-settle-check.tsx, app/(app)/projects/[id]/project-detail.module.css, app/(app)/projects/[id]/quote-table.tsx, test/e2e/pre-settle-check.spec.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 280행(「Empty — 완료 전 점검」) · 284행(「Error — 섹션 로드」) · 1050-1057행
  </read_first>
  <behavior>
    - 세 그룹 빔 → `미결 없음` + 매출 줄
    - 오류 prop → `점검 불러오지 못함 · 다시 시도`(2차 다시 읽기) + 「정산 결재 올리기」 비활성 `점검 결과 없음 · 다시 시도`
    - 막힘 0 · 강행 그룹 항목 있음 → 부제 `정산 결재 전 · 막힘 없음` + 항목 보임
    - 비어 있는 그룹만 접힘
  </behavior>
  <action>
`pre-settle-check.tsx`를 behavior대로 마감한다 — 빈 상태, 오류 prop(06-19가 넘기는 `{ status: "error" }`) 표시와 「다시 시도」(2차, `router.refresh` 또는 기존 섹션 재조회 선례), 부제, 그룹 접기, 긴 항목 이름 말줄임 + `title`. `quote-table.tsx`는 오류 상태일 때 「정산 결재 올리기」의 비활성 이유를 `점검 결과 없음 · 다시 시도`로 바꾸는 자리만 고친다. `project-detail.module.css`에는 섹션 간격과 말줄임만 넣고 값은 `tokens.css` 토큰만. 폰에서는 읽기 표 P1 = 항목 · 금액 · 상태, 행동 3차는 P1 셀 2행.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/pre-settle-check.spec.ts test/e2e/quote-table.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 behavior 네 줄을 개별 케이스로 단언하고, 오류 케이스는 점검 로드 실패를 강제해 버튼 `aria-disabled` + `점검 결과 없음 · 다시 시도`를 확인한다
  </acceptance_criteria>
  <done>S18 섹션이 빈 · 오류 · 강행 · 접기 갈래를 UI-SPEC 문구 그대로 보이고, 점검 없이는 기안이 열리지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 대표 브라우저 → 04.1 승인 | 정산 결재 승인 앞에서 같은 판정을 한 번 더 한다(O-16) |
| 서버 → 브라우저(점검 행 3차 값) | 권한별 행동이 서버에서 정해진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-107 | Elevation of Privilege | 대표 승인 경로 우회(O-16) | high | mitigate | 04.1 승인 함수의 정산 결재 분기에서 같은 게이트를 같은 트랜잭션에서 부른다. 직접 승인 거부 통합 테스트 |
| T-06-108 | Denial of Service → 무결성 | 점검 실패가 승인을 통과시킴 | high | mitigate | 점검 예외를 삼키지 않는다. 예외 주입 통합 테스트 · 화면 `점검 결과 없음 · 다시 시도` |
| T-06-109 | Elevation of Privilege | 점검 행 3차로 권한 밖 행동 | medium | mitigate | 3차는 서버가 권한별로 보낸 값만 그리고, 누른 뒤의 폼 · 액션은 각자의 서버 게이트(06-13 · 06-08 · 06-18)를 지난다 |
| T-06-110 | Tampering | 기안 뒤 새 미결 | low | mitigate | 승인 앞 재점검(O-16)이 막는다 |
</threat_model>

<verification>
- 단위 status-display, 통합 pre-settle-check · leak-scan · 기존 결재 통합 녹색
- E2E pre-settle-check · quote-table · revenue-section가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S18 · 결재 문서 막힘 DOM 실측 감사(backstop 3건 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 정산 결재는 기안 때와 대표 승인 때 모두 같은 규칙으로 막힌다
- PM이 섹션에서 무엇이 몇 건 남았는지, 그 줄에서 다음에 무엇을 누를지 안다
- 점검이 실패하면 기안 · 승인 모두 막힌다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 변경된 기존 심볼 | 04.1 승인 함수(정산 결재 분기 재점검) · 결재 문서 화면(승인 막힘 · 강행 결과 줄) · `status-display.ts`(점검 행 네 값) · `QuoteLedger` props(줄 추가 함수 전달 · 오류 상태 이유) |
| 화면 | `app/(app)/projects/[id]/pre-settle-check.tsx`(S18 마감) |
| 테스트 | `test/integration/pre-settle-check.test.ts`(확장) · `test/e2e/pre-settle-check.spec.ts`(확장) |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-22-SUMMARY.md` when done
</output>
