---
phase: 06-payment-evidence-cards
plan: 23
type: execute
wave: 11
depends_on: ["06-14", "06-19", "06-20", "06-21"]
files_modified:
  - domain/next-turn/index.ts
  - domain/next-turn/phase6-items.ts
  - ui/next-turn/build-next-turn-view.ts
  - ui/next-turn/NextTurn.tsx
  - app/(app)/page.tsx
  - test/unit/domain/next-turn-phase6.test.ts
  - test/unit/ui/next-turn.test.ts
  - test/integration/next-turn.test.ts
  - test/e2e/next-turn-phase6.spec.ts
autonomous: true
requirements: [EXP-09, EXP-10, EXP-13, PROJ-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "지급 권한자(`expenses.payments` write)가 홈 `/`을 열면, 예정일이 오늘이거나 지난 지급 대상이 있을 때 한 줄 `[오늘] 지급 — 예정일 지남·오늘 {N}건 · 확인 전 {M}건  {합계}  지급 대상 보기`가 선다(→ `/expenses?view=pay`)"
    - "예정일이 오늘이거나 그 전인 지급 대상이 하나도 없고 이번 주(서울 기준 월~일)에 예정된 대상이 있을 때만 `[대기] 지급 — 이번 주 {K}건  {합계}  지급 대상 보기`가 선다"
    - "구매 권한자(`cards.purchases` write)에게는 `[대기] 구매 요청 — 신청 {N}건 · 가장 오래된 {MM-DD}  {합계}  구매 요청 보기`(→ `/cards/purchases`)가 선다(팀 비용 요청 포함 — O-19 귀속은 묶음 판정에 쓰이지 않는다). 매출 기록 권한자(`projects.revenue` write)에게는 `[대기] 매출 발행 요청 — {N}건 · 희망 {MM-DD}  {합계}  발행 요청 보기`(→ `/projects/issue-requests`)가 서고, 가장 이른 희망일이 오늘이거나 그 전이면 태그가 `[오늘]`이다"
    - "담당 PM에게는 세 종류의 줄이 건마다 선다 — 선결제 증빙 기한이 지난 문서마다 `[막힘] {프로젝트} · {항목} — 선결제 증빙 {N}일 경과  {금액}  증빙 올리기`(→ 그 지출결의), **결재 통과 · 증빙 없음 · 지급 대기 문서(「지출결의 상태 → 1차」 P3)마다 `[막힘] {프로젝트} · {항목} — 증빙 없음 · 지급 대기  {금액}  증빙 올리기`(→ 그 지출결의 증빙 자리, H-1)**, 완료 전 점검 막힘이 있는 정산 프로젝트마다 `[막힘] {프로젝트} — 완료 전 점검 {N}건  —  점검 보기`(→ `/projects/{id}#pre-settle-check`, D-611). 직원은 자기가 기안한 선결제 기한 초과 줄만 받는다"
    - "[H-1] 증빙 없음 · 지급 대기 줄은 경영관리가 보는 막힘 글자 `증빙 없음 · 담당 PM {이름}`의 반대편이다 — 받는 사람은 그 글자에 적힌 사람(06-04 `pmName`의 출처와 같은 사람)이고, 판정은 06-04 `evidenceGateDecision`의 P3 갈래(결재 통과 · 지급 전 · 증빙 필수 on · 증빙 0 · 선결제 아님 · 면제 아님)를 그대로 쓴다. `확인 전`(P2) · 증빙 필수 off · 면제 · 선결제 · 지급 뒤 문서에는 서지 않는다"
    - "[D-601] 대표에게는 이 페이즈 항목이 없다 — 대행 줄을 만들지 않는다"
    - "증빙 `면제`된 선결제 문서에는 「내 차례」 PM 항목이 서지 않는다(면제가 선결제를 이긴다 — UI-SPEC 「지출결의 상태 → 1차 (정본 …)」 절 표 아래 「`면제`가 `선결제`를 이기므로」 문단)"
    - "[O-15 기본값] 「내 차례」 공급 함수가 main에 이미 있으면 거기에 Phase 6 항목을 더하고, 홈이 아직 빈 배열을 넘기면 이 플랜이 `domain/next-turn`에 처음 만든다 — 어느 갈래였는지 SUMMARY에 남는다"
    - "S19 empty: 이 페이즈 항목도 다른 항목도 0건이면 블록이 없다(§7-4)"
    - "S19 error: 공급이 실패하면 블록 자리에 한 줄 `내 차례를 불러오지 못했습니다 · 다시 시도`(`--danger`, 「다시 시도」 = 2차)가 서고 홈의 나머지는 그대로 선다 — 0건(블록 없음)과 구분된다"
    - "S19 partial: 경영관리 항목은 묶음 한 줄(지급 · 구매 요청 · 발행 요청 각각)이고, PM 항목은 건마다 한 줄이다 — 선결제 기한 초과 · 결재 통과 · 증빙 없음 · 지급 대기(P3, H-1 — `확인 전`은 넣지 않는다) · 완료 전 점검 막힘"
    - "S19 overflow: 최대 6줄이고 태그 순서는 막힘 → 오늘 → 결재 → 대기다 — 정렬 · 절단은 기존 `buildNextTurnView`가 한다"
    - "S19 zero-one-many: 묶음 줄은 건수를 글자로 쓴다(`신청 3건`). 0건인 묶음은 줄이 없고, 지급 묶음의 `확인 전 0건` 조각은 쓰지 않는다"
    - statement: "S19 loading: 홈 로드 규칙은 §7-4 · §7-7 그대로 — 느린 응답을 강제해 CI=true DOM 감사"
      verification: backstop
    - statement: "S19 populated: 역할별 문구 · 태그 순서 · 6줄 상한이 공급 함수 결과로 서는지 CI=true DOM 감사(경영관리 · PM · 직원 · 대표 계정)"
      verification: backstop
    - statement: "S19 long-text: 긴 프로젝트 · 항목 이름이 NextTurn 한 줄에서 말줄임 + `title`로 선다 — CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "「내 차례」 공급이 실패했는데 빈 블록이나 일부 항목만 보여서는 안 된다. 공급원 하나라도 실패하면 블록 전체가 오류 한 줄이다"
      status: resolved
      verification: test
    - statement: "권한이 없는 역할의 홈에 지급 · 구매 요청 · 발행 요청 묶음 줄이 서거나, 볼 수 없는 금액이 실려서는 안 된다"
      status: resolved
      verification: test
  artifacts:
    - path: "domain/next-turn/phase6-items.ts"
      provides: "Phase 6 「내 차례」 항목 — 묶음 셋 · PM 항목 셋 · 순수 빌더와 수집 함수"
      exports: ["listPhase6NextTurnItems", "buildPaymentBundle", "buildPurchaseBundle", "buildIssueBundle", "buildPrepaidOverdueItems", "buildEvidenceMissingItems", "buildPreSettleBlockedItems"]
    - path: "domain/next-turn/index.ts"
      provides: "[O-15] 홈이 부르는 공급 함수(이미 있으면 그 함수에 더한다)"
      exports: ["listNextTurnItems"]
  key_links:
    - from: "app/(app)/page.tsx"
      to: "domain/next-turn/index.ts"
      via: "홈이 공급 함수 결과를 `buildNextTurnView`에 넘긴다"
      pattern: "listNextTurnItems"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/payments/targets.ts"
      via: "지급 묶음이 06-15의 지급 대상 정의를 그대로 쓴다(사본 없음)"
      pattern: "listPaymentTargets"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/pre-settle-check/index.ts"
      via: "PM 점검 막힘 항목이 06-19 판정 그대로를 읽는다"
      pattern: "computePreSettleCheck"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/payments/action-row.ts"
      via: "[H-1] 증빙 없음 · 지급 대기 항목이 06-04 증빙 게이트의 P3 판정 그대로를 쓴다(사본 없음)"
      pattern: "evidenceGateDecision"
    - from: "domain/next-turn/phase6-items.ts"
      to: "domain/evidence-reviews/prepaid.ts"
      via: "선결제 기한 경과 일수는 06-10 한 함수에서 온다(O-5)"
      pattern: "prepaidDueInfo"
---

<objective>
경영관리와 PM이 홈 「내 차례」(S19, UA-611)에서 이 페이즈의 할 일을 다음 한 수와 함께 보게 한다 — 경영관리 몫은 지급 · 구매 요청 · 발행 요청 묶음, PM 몫은 선결제 증빙 기한 초과 · 결재 통과 · 증빙 없음 · 지급 대기(H-1) · 완료 전 점검 막힘. 경영관리 전체 E2E와 페이즈 게이트는 06-24.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: O-1 기본값에 따라 경영관리 전용 화면은 없다. 흩어진 목록(지급 대상 · 구매 요청 · 발행 요청)을 모아 보여 주는 자리는 「내 차례」뿐이다. 독촉 **알림**(이메일 · 알림함)은 Phase 7 몫이고, 이 줄은 알림이 아니라 할 일 표시다.

Output: `domain/next-turn`(공급 함수 · Phase 6 항목), `ui/next-turn`의 `reason` · `amount` 빈 값 허용, 홈 연결과 오류 줄, 단위 2 · 통합 1 · E2E 1.

**규모 근거:** `files_modified` 9개, `estimate.tokens` 80000(`confidence: low`, 표본 0 — 보정 계수 1). 옛 06-10(11파일 · 110k)을 06-23 · 06-24로 나눈 첫 몫이다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-19-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-23 착수하지 않음`을 출력하고 멈춘다.** 아래 이름은 가칭이다 — 실제 이름이 다르면 실제 이름을 쓰고 SUMMARY에 대응표를 남긴다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| UA-611 · O-15 | 홈의 「내 차례」 공급 경로. 홈이 아직 빈 배열을 넘기면 이 플랜이 처음 만든다. Phase 5나 04.1이 공급 함수(결재 항목 등)를 이미 만들었으면 거기에 더한다 | `grep -n "buildNextTurnView" 'app/(app)/page.tsx'`와 `git ls-files 'domain/next-turn/*'` 결과로 갈래를 정하고 SUMMARY에 적는다(멈추지 않음) |
| 06-04 · 06-06 · 06-10 · 06-14 · 06-15 · 06-19 · 06-21 | `evidenceGateDecision` · `resolveEvidenceStatus` · `prepaidDueInfo` · `listPurchaseRequests` · `listPaymentTargets` · `computePreSettleCheck` · `listIssueRequests` | `grep -rho "function \(evidenceGateDecision\|resolveEvidenceStatus\|prepaidDueInfo\|listIssueRequests\|listPurchaseRequests\|listPaymentTargets\|computePreSettleCheck\)(" domain/` 결과의 서로 다른 이름이 7개 미만이면 멈춤 |
| A-606 | 04.1 결재 항목이 「내 차례」에 `[결재]`로 이미 들어오는지(태그 순서 정렬이 함께 돈다) | ⓪의 O-15 갈래 확인에 포함 — 결과를 SUMMARY에 적는다 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| **O-15** | Phase 5 산출물을 보고 정한다. 없으면 이 페이즈가 처음 만든다 | `domain/next-turn/index.ts` 한 곳(⓪ 갈래) · must_haves의 `[O-15 기본값]` 한 줄 |
| O-2 | 지급 묶음의 `확인 전 {M}건`은 O-2 답과 관계없이 센다(확인이 지급 게이트가 아니어도 할 일이다) | `buildPaymentBundle`의 한 조각 |
| O-5 | 선결제 기한 기준은 06-10 `prepaidDueInfo` 한 곳 | 해당 없음 |
| PN-1 (계획 판단) | 예정일 없는 대상과 다음 주 이후 대상은 지급 묶음에 세지 않는다(지급 대상 목록에는 있다) | `buildPaymentBundle`의 날짜 가르기 |
| PN-2 (계획 판단) | `NextTurnItem.reason`은 `null`이면 ` · 이유` 조각이 없고, `amount`는 `null`이면 `—`(SYSTEM §2 163행) | `ui/next-turn/build-next-turn-view.ts` 타입 · `NextTurn.tsx` 두 칸 |
| PN-3 (계획 판단) | 공급원 하나라도 실패하면 블록 전체가 오류 한 줄 | `app/(app)/page.tsx`의 catch 한 곳 |
| PN-4 (계획 판단) | 같은 지출결의가 PM 항목과 기안자 항목으로 겹치면 한 줄만 | `buildPrepaidOverdueItems` |
| PN-5 (계획 판단, H-1) | 증빙 없음 · 지급 대기 줄은 PM 항목 범위(`projects.pmUserId = viewer`)에서 모은다 — ⓪에서 06-04 SUMMARY의 `pmName` 출처가 `projects.pmUserId`가 아니면 그 출처 칸으로 범위를 맞추고 SUMMARY에 적는다. `listPaymentTargets`(지급 권한 전용 · 계좌 칸)는 부르지 않는다 | `buildEvidenceMissingItems` · 수집 함수의 PM 갈래 |

**항목 문자열 대응(PN-2 — 렌더러는 `{label} · {reason}`):**

| 항목 | label | reason |
|---|---|---|
| 지급 `[오늘]` | `지급 — 예정일 지남·오늘 {N}건` | `확인 전 {M}건`(M = 0이면 `null`) |
| 지급 `[대기]` | `지급 — 이번 주 {K}건` | `null` |
| 구매 요청 | `구매 요청 — 신청 {N}건` | `가장 오래된 {MM-DD}` |
| 매출 발행 요청 | `매출 발행 요청 — {N}건` | `희망 {MM-DD}` |
| 선결제 기한 초과 | `{프로젝트} · {항목} — 선결제 증빙 {N}일 경과` | `null` |
| 증빙 없음 · 지급 대기(H-1) | `{프로젝트} · {항목} — 증빙 없음 · 지급 대기` | `null` |
| 완료 전 점검 막힘 | `{프로젝트} — 완료 전 점검 {N}건` | `null`, amount `null` |

<assumption_delta_decision>
`detected: false`. `NextTurnItem`의 두 칸을 nullable로 **넓힌다** — 기존 항목의 렌더는 바뀌지 않는다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EXP-09 · EXP-10 · EXP-13 · PROJ-06 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: UI-SPEC S19 · O-15 · UA-611 · D-601 · D-611. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 지급 권한자 홈에 「[오늘] 지급」 묶음 한 줄이 서고, 누르면 지급 대상으로 간다</name>
  <files>domain/next-turn/index.ts, domain/next-turn/phase6-items.ts, ui/next-turn/build-next-turn-view.ts, ui/next-turn/NextTurn.tsx, app/(app)/page.tsx, test/unit/domain/next-turn-phase6.test.ts, test/unit/ui/next-turn.test.ts, test/integration/next-turn.test.ts, test/e2e/next-turn-phase6.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목이 main에 있다 — ⓪이 확인하고 없으면 멈춘다.</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md S19 「「내 차례」 항목」(정본, rev 9 — rev 8의 줄 번호는 밀렸다) · Copywriting 「Error — 「내 차례」 로드(S19)」 · 「표시 — 「내 차례」(S19)」 · 권한·노출 표 S19 행 · UI Considerations S19 행
    - docs/design/SYSTEM.md 783-789행(§7-4) · 163행(빈 값 `—`)
    - ui/next-turn/build-next-turn-view.ts · ui/next-turn/NextTurn.tsx · test/unit/ui/next-turn.test.ts · test/e2e/mobile-next-turn.spec.ts(폰 두 줄 배치 — 깨지면 안 된다)
    - app/(app)/page.tsx · ui/list-empty/ListEmpty.tsx(`tone="error"` = 2차 행동)
    - eslint.config.mjs 40-72행(domain은 ui를 import할 수 없다 — domain 항목 타입은 `NextTurnItem`과 구조가 같은 자체 타입)
    - domain/payments/targets.ts(`listPaymentTargets`) · domain/evidence-reviews/index.ts(`resolveEvidenceStatus`) · domain/money/index.ts(`sumKrw`)
  </read_first>
  <behavior>
    - `buildPaymentBundle(targets, today)`: 예정일 ≤ 오늘 3건(그중 확인 전 1) → `[오늘]` · label `지급 — 예정일 지남·오늘 3건` · reason `확인 전 1건` · amount = 세 건 지급 총액 합 / 확인 전 0 → reason `null` / 오늘 이전 0 · 이번 주 2 → `[대기]` · `지급 — 이번 주 2건` / 예정일 없음만 → 줄 없음 / 대상 0 → 줄 없음 / 일요일 경계(오늘 = 일요일, 다음 월요일 예정) → 이번 주 아님
    - `buildNextTurnView`: `amount: null` · `reason: null` 항목을 받아도 정렬 · 절단이 그대로다
    - `NextTurn`: amount `null` → `—`, reason `null` → ` · ` 조각 없음
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. O-15 갈래(처음 만든다 / 더한다)와 04.1 결재 항목 여부를 SUMMARY에 적는다.

① **ui 타입(PN-2).** `ui/next-turn/build-next-turn-view.ts`의 `NextTurnItem`에서 `amount`를 `number | null`로, `reason`을 `string | null`로 바꾼다. `NextTurn.tsx`는 금액이 `null`이면 `—`, 이유가 `null`이면 ` · ` 조각을 그리지 않는다. 클래스 이름과 배치는 그대로(`mobile-next-turn.spec.ts` 두 줄 배치 유지). `test/unit/ui/next-turn.test.ts`에 빈 값 케이스.

② **domain.** `domain/next-turn/phase6-items.ts` — 항목 타입은 `NextTurnItem`과 구조가 같은 자체 타입(태그 넷 리터럴 유니언 · label · reason · amount · action). 이 태스크는 `buildPaymentBundle`(순수)과 수집 함수 `listPhase6NextTurnItems(viewer, today)`의 지급 갈래만 — 먼저 `can(viewer, "expenses.payments", "write")`, 없으면 지급 대상 조회를 부르지 않는다. 지급 대상 정의는 `listPaymentTargets`를 그대로 부른다(사본 없음). 합계 `sumKrw`, `확인 전`은 `resolveEvidenceStatus`, 날짜는 서울 기준 `YYYY-MM-DD` 문자열이고 `today`는 인자(테스트 주입). `domain/next-turn/index.ts`의 `listNextTurnItems(viewer, today)`가 Phase 6 항목을 모은다 — ⓪에서 기존 공급 함수가 있었으면 그 함수에 이어 붙인다.

③ **홈.** `app/(app)/page.tsx`의 「내 차례」 호출이 `listNextTurnItems(viewer, today)` 결과를 `buildNextTurnView`에 넘기게 바꾼다. D-24 주석은 바뀐 사실(이제 도메인이 항목을 공급한다)에 맞게 한 줄로 고친다.

④ **테스트.** 통합 `next-turn.test.ts`(신규): 지급 권한자는 지급 줄 1개, 대표 · 직원 · PM은 지급 줄 없음. E2E `next-turn-phase6.spec.ts`(신규): 오늘 예정 지급 대상 1건 시드 → 경영관리 계정 `/` → `[오늘]` · `지급 — 예정일 지남·오늘 1건` → `지급 대상 보기` → `/expenses?view=pay`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/next-turn-phase6.test.ts test/unit/ui/next-turn.test.ts test/unit/ui/next-turn-action.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/next-turn.test.ts test/integration/payment-batch.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다. typecheck가 page.tsx에서 빨가면 domain 항목 타입과 `NextTurnItem`의 구조가 어긋났다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/next-turn-phase6.spec.ts test/e2e/mobile-next-turn.spec.ts test/e2e/wordmark-home.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(O-15 갈래 · 04.1 결재 항목 여부 · 가칭 대응표)가 SUMMARY에 있다
    - `grep -v '^\s*//' 'app/(app)/page.tsx' | grep -c "buildNextTurnView(\[\])"`가 0이다(주석 줄은 세지 않는다)
    - `pnpm lint`가 녹색이다 — boundaries 규칙이 domain → ui import를 막으므로, 녹색이면 층 경계가 지켜졌다
    - 단위 테스트가 behavior의 지급 갈래 여섯 개를 개별 케이스로 단언한다
  </acceptance_criteria>
  <done>지급 대상 하나가 06-15 정의 → domain 묶음 → 홈 NextTurn → 지급 대상 목록까지 실제로 이어진다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 나머지 묶음(구매 요청 · 발행 요청) · PM 항목(선결제 기한 초과 · 점검 막힘) · 역할 범위</name>
  <files>domain/next-turn/phase6-items.ts, domain/next-turn/index.ts, test/unit/domain/next-turn-phase6.test.ts, test/integration/next-turn.test.ts</files>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「지출결의 상태 → 1차」 표 P3 행 · 「증빙 면제의 자리」(면제가 선결제를 이긴다 — PM 항목 없음) · S19 PM 항목 문단 · Copywriting 「표시 — 「내 차례」(S19)」의 `증빙 없음 · 지급 대기` 조각(H-1)
    - domain/payments/action-row.ts(`evidenceGateDecision` — 06-04, P3 = 증빙 필수 on · 증빙 0 · 선결제 아님 · 면제 아님) · domain/evidence-reviews/index.ts(`resolveEvidenceStatus` — 06-06) · 06-04 SUMMARY(`pmName` 출처 · `EVIDENCE_REQUIRED` 읽기)
    - domain/purchase-requests/index.ts(`listPurchaseRequests` 상태 필터) · domain/issue-requests/index.ts(`listIssueRequests`)
    - domain/evidence-reviews/prepaid.ts(`prepaidDueInfo`) · domain/pre-settle-check/index.ts(`computePreSettleCheck`) · 06-19 SUMMARY의 A-601 상태 대응
    - db/schema/projects.ts(`pmUserId`)
  </read_first>
  <behavior>
    - 구매: 신청 3건(가장 오래된 09-18) → `[대기]` · `구매 요청 — 신청 3건` · `가장 오래된 09-18` / 신청 0 → 줄 없음 / 구매 완료 · 취소만 → 줄 없음
    - 발행: 요청 2건 · 가장 이른 희망 09-30 · 오늘 09-24 → `[대기]` · `희망 09-30` / 가장 이른 희망 ≤ 오늘 → `[오늘]` / 발행됨 · 취소만 → 줄 없음
    - 선결제: 선결제 · 증빙 0 · 면제 아님 · 기한 16일 경과 → `[막힘]` 한 줄 / 면제 → 없음 / 기한 전 → 없음 / 같은 문서가 PM 범위와 기안자 범위에 둘 다 → 한 줄(PN-4)
    - [H-1] 증빙 없음 · 지급 대기(`buildEvidenceMissingItems`): 결재 통과 · 지급 전 · 증빙 필수 on · 증빙 0 · 선결제 아님 · 면제 아님 → `[막힘]` · label `{프로젝트} · {항목} — 증빙 없음 · 지급 대기` · reason `null` · 3차 `증빙 올리기`(→ `/expenses/{id}` 증빙 자리) / `확인 전`(P2) → 없음 / 증빙 필수 off → 없음 / 면제 → 없음 / 선결제 → 없음(선결제 줄 몫) / 지급 뒤 → 없음 / 결재 중 · 반려 → 없음 / 보는 사람이 그 문서의 담당 PM 아님 → 없음
    - 점검: 정산 프로젝트 · 막힘 3 · 보는 사람이 PM → `[막힘]` · amount `null` / 막힘 0(강행만) → 없음 / PM 아님 → 없음
    - 역할: 대표 → Phase 6 항목 0 / 직원 → 자기 선결제 초과만(증빙 없음 · 지급 대기 줄 없음 — PM 몫) / 금액 정보 항목을 볼 수 없는 사람 → 그 항목 amount `null`
  </behavior>
  <action>
`buildPurchaseBundle` · `buildIssueBundle` · `buildPrepaidOverdueItems` · `buildEvidenceMissingItems`(H-1) · `buildPreSettleBlockedItems`를 순수 함수로 만들고, 문자열은 위 「항목 문자열 대응」 표 그대로 쓴다. 수집 함수는 원천마다 권한을 먼저 확인한다(구매 = `cards.purchases` write, 발행 = `projects.revenue` write, PM 항목 = `projects.pmUserId = viewer` 프로젝트 범위, 직원 항목 = 자기가 기안한 문서 범위) — 권한이 없는 원천은 부르지 않는다. 각 목록은 06-14 · 06-21의 목록 함수를 상태 필터와 함께 부르고 정의를 다시 쓰지 않는다. 선결제 경과 일수는 `prepaidDueInfo`, **증빙 없음 · 지급 대기(H-1)는 PM 항목 범위의 결재 통과 · 지급 전 문서를 `resolveEvidenceStatus`(06-06) · `EVIDENCE_REQUIRED` 설정 값(06-04 `loadPaymentInputs`와 같은 읽기)으로 채운 ctx를 06-04 `evidenceGateDecision`에 넣어 P3 막힘(`증빙 없음 · 담당 PM {이름}`)이 나오는 문서만 고른다 — 조건을 다시 쓰지 않고, 받는 사람은 그 막힘 글자의 사람(PN-5). 지급 권한 전용 `listPaymentTargets`는 부르지 않는다(계좌 칸 — O-17)**, 점검 막힘은 A-601 대응 상태의 PM 프로젝트마다 `computePreSettleCheck`의 막힘 건수(프로젝트 전체 — 06-19 R-6). PM 항목의 금액은 지출결의 금액 정보 항목을 볼 수 있을 때만 싣고 아니면 `null`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/next-turn-phase6.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나, failed가 1 이상이거나, "No test files found"가 나온다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/next-turn.test.ts test/integration/purchase-requests.test.ts test/integration/issue-requests.test.ts test/integration/pre-settle-check.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 역할 넷(경영관리 · PM · 직원 · 대표)마다 받는 Phase 6 항목의 종류 집합을 단언한다(대표 = 빈 집합)
    - 통합 테스트가 「면제된 선결제 문서 → PM 항목 없음」과 「금액 정보 항목을 볼 수 없는 PM → amount `null`」을 단언한다
    - [H-1] 통합 테스트가 「P3 문서 → 담당 PM에게 `증빙 없음 · 지급 대기` 한 줄」 · 「`확인 전` 문서 → 그 줄 없음」 · 「증빙 필수 off → 그 줄 없음」 · 「같은 문서의 기안 직원(PM 아님) → 그 줄 없음」을 개별 `it`으로 단언하고, 단위 테스트가 behavior `[H-1]` 줄의 갈래 여덟을 개별 케이스로 단언한다
    - [H-1] `grep -v '^\s*//' domain/next-turn/phase6-items.ts | grep -c "listPaymentTargets("`의 결과가 1(지급 묶음 호출 한 곳 — import 줄 · 주석 줄은 세지 않는다)이다 — PM 갈래가 지급 권한 전용 함수를 부르지 않는다
  </acceptance_criteria>
  <done>역할마다 자기 할 일만 한 줄씩 서고, 권한 없는 원천은 조회조차 하지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 오류 줄(PN-3) · 6줄 상한 · 태그 순서 · 빈 값 표시</name>
  <files>app/(app)/page.tsx, test/e2e/next-turn-phase6.spec.ts</files>
  <read_first>
    - app/(app)/error.tsx(오류 메시지를 화면에 노출하지 않는 선례, T-02-18) · ui/list-empty/ListEmpty.tsx
    - ui/next-turn/build-next-turn-view.ts(태그 정렬 · 6줄 절단 · `더 보기`)
  </read_first>
  <behavior>
    - 공급원 하나가 던짐 → 블록 자리 `내 차례를 불러오지 못했습니다` + 2차 `다시 시도`(→ `/`), 홈의 나머지 선다, 빈 목록 줄 없음
    - 항목 8개(막힘 2 · 오늘 2 · 대기 4) → 보이는 줄 6, 첫 두 줄 `[막힘]`, `더 보기 2건`
    - [H-1] P3 문서 하나를 시드한 담당 PM 계정 `/` → `[막힘] {프로젝트} · {항목} — 증빙 없음 · 지급 대기` 한 줄 → 3차 `증빙 올리기` → `/expenses/{id}`
    - PM 점검 줄 금액 칸 `—`
  </behavior>
  <action>
`app/(app)/page.tsx`에서 공급 호출을 try/catch로 감싼다(PN-3). 실패하면 서버 로그에 오류를 남기고 블록 자리에 `ListEmpty` `tone="error"`로 `내 차례를 불러오지 못했습니다` + 2차 `다시 시도`(→ `/`)를 그린다 — 예외 메시지는 화면에 내지 않는다. 이때 빈 목록 줄(`표시할 항목이 없습니다`)은 그리지 않는다. 0건일 때의 기존 동작은 그대로. 태그 정렬과 6줄 절단은 `buildNextTurnView`가 이미 한다 — 새로 정렬하지 않는다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/next-turn-phase6.spec.ts test/e2e/mobile-next-turn.spec.ts test/e2e/wordmark-home.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - E2E가 behavior 네 줄을 단언하고, 오류 케이스에서 홈의 `PageHeader`가 서 있음을 확인한다
    - 기존 mobile-next-turn · wordmark-home 스펙 녹색
  </acceptance_criteria>
  <done>실패는 오류 한 줄로 드러나고, 태그 순서와 6줄 상한이 지켜지며, 빈 값이 `—`로 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 홈 RSC → domain/next-turn | 보는 사람의 권한에 따라 원천마다 부를지 말지를 정한다 |
| domain/next-turn → 06-14 · 06-15 · 06-19 · 06-21 목록 함수 | 각 목록 함수의 기존 범위 · 가림을 그대로 물려받는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-111 | Information Disclosure | 권한 없는 역할에 지급 · 구매 · 발행 묶음(건수 · 합계) | medium | mitigate | 원천마다 `can(viewer, 메뉴, "write")`를 먼저 확인하고, 없으면 조회하지 않는다. 역할 넷 통합 테스트(대표 = 빈 집합) |
| T-06-112 | Information Disclosure | PM 항목에 볼 수 없는 지출결의 금액 | medium | mitigate | 금액 정보 항목 가시성이 없으면 `amount: null` → `—`. 통합 테스트 |
| T-06-113 | Information Disclosure | 오류 줄에 예외 메시지 | low | mitigate | 고정 문구만 그리고 예외는 서버 로그에만(`app/(app)/error.tsx` 선례) |
| T-06-114 | Denial of Service | 홈 로드마다 PM 정산 프로젝트 수만큼 점검 계산 | low | mitigate | 점검은 보는 사람이 PM인 정산 상태 프로젝트에만. 실패는 홈 전체가 아니라 블록 한 줄로 끝난다 |
| T-06-190 | Information Disclosure | [H-1] 증빙 없음 · 지급 대기 항목 수집이 지급 권한 전용 목록(계좌 칸)을 부르거나 남의 프로젝트 문서를 읽음 | medium | mitigate | PM 항목 범위(`projects.pmUserId = viewer`)에서만 모으고 `listPaymentTargets`를 부르지 않는다(O-17 — 계좌는 지급 권한자만). acceptance의 `listPaymentTargets` 1건 grep · 「기안 직원 → 그 줄 없음」 통합 테스트 |
</threat_model>

<verification>
- 단위 next-turn-phase6 · next-turn · next-turn-action, 통합 next-turn · payment-batch · purchase-requests · issue-requests · pre-settle-check 녹색
- E2E next-turn-phase6 · mobile-next-turn · wordmark-home가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 S19 DOM 실측 감사(backstop 3건 · 역할 넷) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- 경영관리가 홈에서 지급 · 구매 요청 · 발행 요청의 할 일을 묶음 한 줄씩 보고, 한 번 눌러 해당 목록으로 간다
- PM이 선결제 증빙 기한 초과 · 결재 통과 · 증빙 없음 · 지급 대기(H-1) · 완료 전 점검 막힘을 홈에서 건마다 본다 — 경영관리가 `증빙 없음 · 담당 PM {이름}`으로 멈춘 건이 PM 홈에 선다
- 공급 실패가 조용히 빈 블록으로 숨지 않는다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 함수(domain) | `listNextTurnItems` · `listPhase6NextTurnItems` · `buildPaymentBundle` · `buildPurchaseBundle` · `buildIssueBundle` · `buildPrepaidOverdueItems` · `buildEvidenceMissingItems`(H-1) · `buildPreSettleBlockedItems` |
| 변경된 기존 심볼 | `NextTurnItem`(`amount: number \| null` · `reason: string \| null`) · `NextTurn`(빈 값 렌더) · 홈 `app/(app)/page.tsx`(공급 연결 · 오류 줄) |
| 테스트 | `test/unit/domain/next-turn-phase6.test.ts` · `test/unit/ui/next-turn.test.ts`(확장) · `test/integration/next-turn.test.ts` · `test/e2e/next-turn-phase6.spec.ts` |

## Review Dispositions Ledger

### Round 1 — f2720a8(디자인 검토 반영 — `/plan-design-review` `design-review.md` 반영 지시 9)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| H-1 — PM 「내 차례」에 「결재 통과 · 증빙 없음 · 지급 대기」(P3)가 없어 P3 건이 정산 점검까지 조용히 멈춘다(design-review.md L39@f2720a8 · 반영 지시 9 L82@f2720a8) | HIGH(고침) | Plan 23: must_haves.truths PM 줄(세 종류) · `[H-1]` 줄 · S19 partial 줄 · artifacts `buildEvidenceMissingItems` · key_links `evidenceGateDecision` · objective · 「선행 의존」 이름 일곱 grep · 열린 선택 PN-5 · 항목 문자열 표 새 행. Task 2 read_first(P3 행 · S19 · 06-04 · 06-06) · behavior `[H-1]` 여덟 갈래 · 역할 줄 · action(`evidenceGateDecision` P3 판정 재사용 · PM 범위 · `listPaymentTargets` 안 부름) · acceptance(통합 넷 · 단위 여덟 · grep 1건). Task 3 behavior E2E 한 줄 · acceptance. T-06-190 · success_criteria · Artifacts. Task 1 read_first의 rev 8 줄 번호를 절 이름으로 바꿨다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 플랜에 배정된 항목은 H-1 하나이고 반영했다. 독촉 알림(이메일 · 알림함)은 Phase 7 그대로 |

### Round 2 — e2c8922(디자인 검토 반영 r2 — 미커밋 1회차 반영분 점검 `design-apply-checker-r1.md` · 교차 `design-apply-cross-r1.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 W4 — must_haves.truths 「면제가 선결제를 이긴다 — UI-SPEC 382행」의 줄 번호가 rev 9에서 다른 곳을 가리킨다(design-apply-checker-r1.md 경고 4 — 06-23:34) | 경고(고침) | Plan 23: must_haves.truths 그 줄 → UI-SPEC 「지출결의 상태 → 1차 (정본 …)」 절 표 아래 「`면제`가 `선결제`를 이기므로」 문단. 줄 번호를 남기지 않았다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | 이 라운드에 이 플랜으로 배정된 것은 W4 한 줄뿐이고 반영했다 |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-23-SUMMARY.md` when done
</output>
