---
phase: 06-payment-evidence-cards
plan: 24
type: execute
wave: 12
depends_on: ["06-16", "06-22", "06-23"]
files_modified:
  - test/e2e/management-flow.spec.ts
  - test/integration/leak-scan.test.ts
  - db/migrations/meta/_journal.json
autonomous: true
requirements: [EXP-06, EXP-07, EXP-09, EXP-10, EXP-13, EXP-16, EVID-02, EVID-03, EVID-04, PROJ-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "경영관리 흐름(결재 통과 건 → 증빙 확인 → 지급 완료)이 이미지 증빙 한 경로와 PDF 증빙 한 경로로 Playwright E2E에 있고 CI=true로 녹색이다(ROADMAP 기준 5 · D-602 · D-604)"
    - "증빙 필수 설정은 켠 상태로 돈다 — 증빙 확인 전에는 `지급 완료`가 비활성임을 한 번 단언한다(EVID-02 · D-603)"
    - "이 페이즈의 새 액션 레지스트리와 새 DTO가 모두 누수 스캔에 등록되어 있어 계급별 검사가 따라온다 — `it(\"Phase 6 DTO가 모두 등록돼 있다\")`가 SUMMARY 대조 목록을 `DTO_REGISTRY`에 대고 단언한다(ROADMAP 기준 5)"
    - "**B-4:** 업로드 테스트 모드는 06-11 Task 1 ⓪에서 이미 정해졌다 — 이 플랜은 06-11 SUMMARY의 A-608-T 결과를 **다시 확인만** 하고, 새로 묻거나 새 방법을 만들지 않는다"
    - "**R-4:** PR을 올리기 전에 origin/main을 합치고, 이 페이즈가 만든 마이그레이션 · 스냅숏을 지운 뒤 `pnpm db:generate`로 다시 만든다. 페이즈 6 이전 표(Phase 5 지출결의 · 파일)에 거는 제약만 B-2 손편집(SET LOCAL 한 쌍 · `NOT VALID`)을 다시 하고 `--custom` VALIDATE 마이그레이션을 다시 만든다. `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate`가 `No schema changes`. 저널 가드 테스트가 있으면 돌리고, 없으면 `GUARD-ABSENT`를 SUMMARY에 적는다"
    - "새 의존성 · 새 토큰이 없다 — `git diff --stat \"$(git merge-base origin/main HEAD)\" -- package.json pnpm-lock.yaml docs/design/tokens.css`가 비어 있다(커밋된 변경까지 본다)"
    - "페이즈 전체 게이트는 CLAUDE.md §6 순서로 한 번만 돈다 — 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 페이즈의 `verification: backstop` 41건 전부를 DOM 실측 감사 → 수정 → 전체 게이트(`pnpm test:e2e:ci` 포함) 한 번"
  prohibitions:
    - statement: "페이즈 마감에서 업로드 테스트 모드를 즉석으로 흉내 내거나 새 의존성으로 대체해서는 안 된다(B-4 · CLAUDE.md §5)"
      status: resolved
      verification: judgment
    - statement: "main과 합친 뒤 마이그레이션 번호 · 저널이 어긋난 채로 PR이 올라가서는 안 된다(R-4)"
      status: resolved
      verification: test
  artifacts:
    - path: "test/e2e/management-flow.spec.ts"
      provides: "ROADMAP 기준 5 — 경영관리 흐름 이미지 · PDF 두 경로"
      min_lines: 60
    - path: "test/integration/leak-scan.test.ts"
      provides: "Phase 6 DTO · 액션 레지스트리 완전성 단언"
      contains: "Phase 6 DTO가 모두 등록돼 있다"
  key_links:
    - from: "test/e2e/management-flow.spec.ts"
      to: "domain/payments/index.ts"
      via: "E2E가 증빙 확인 → 지급 완료를 화면으로 통과한다"
      pattern: "지급 완료"
    - from: "test/integration/leak-scan.test.ts"
      to: "domain/permissions/dto-registry.ts"
      via: "Phase 6 DTO 이름 목록을 `DTO_REGISTRY`에 대고 단언한다"
      pattern: "DTO_REGISTRY"
---

<objective>
Phase 6를 닫는다 — ROADMAP 기준 5의 **경영관리 전체 E2E**(이미지 · PDF 두 업로드 경로), 누수 스캔 완전성, PR 전 마이그레이션 재생성(R-4), 페이즈 전체 CI=true 게이트.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 각 플랜은 자기 경로를 증명했다. 이 플랜은 한 문서가 PM 제출에서 경영관리 지급 완료까지 실제 두 증빙 형식으로 끝나는지, 그리고 main과 합친 상태에서 스키마 · 누수 스캔 · 전체 게이트가 한 번에 녹색인지를 증명한다. 이 플랜은 D-601~D-613 전부의 페이즈 단위 회귀를 한 번 돈다.

Output: `test/e2e/management-flow.spec.ts`, 누수 스캔 완전성 `it`, 다시 만든 Phase 6 마이그레이션(번호 · 이름은 `pnpm db:generate`가 정한다 — 파일 목록은 SUMMARY에), 페이즈 게이트 요약.

**규모 근거:** `files_modified` 3개(+ R-4가 다시 만드는 `db/migrations/NNNN_*.sql` · `meta/NNNN_snapshot.json` — 이름은 실행 때 정해진다), `estimate.tokens` 75000(`confidence: low`, 표본 0 — 보정 계수 1). 전체 게이트 출력은 요약만 읽는다(CLAUDE.md §8).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-24 착수하지 않음`을 출력하고 멈춘다.** R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| A-608-T (B-4 — 재확인만) | 06-11 Task 1 ⓪이 정한 업로드 테스트 모드(경로 또는 사용자 선택 · 승인 문장) | `grep -n "A-608-T" .planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md` 0건이면 멈춘다 — 여기서 새 선택지를 묻지 않고 06-11로 돌려보낸다 |
| 06-01 ~ 06-23 | 모든 플랜의 SUMMARY | `ls .planning/phases/06-payment-evidence-cards/06-*-SUMMARY.md \| wc -l`이 23 미만이면 멈춤 |
| 06-15 · 06-20 · 06-23 | 지급 대상 · 지급 완료 보기 · 홈 지급 묶음 | E2E 경로가 지나는 화면 — ⓪에서 `git ls-files 'app/(app)/expenses/paid-list.tsx' domain/next-turn/phase6-items.ts` 비면 멈춤 |

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| R-4 저널 가드 | `test/unit/db/migration-journal.test.ts`가 있으면 돌리고, 없으면 `GUARD-ABSENT`로 기록(이 플랜이 새로 만들지 않는다 — 없는 가드를 만드는 것은 요청 밖) | Task 3 verify 한 줄 |
| 테스트 파일 바이트 | 증빙 파일은 저장소에 바이너리로 두지 않고 테스트 안 바이트 상수로 만든다(계획 판단) | `management-flow.spec.ts` 머리 상수 둘 |

<assumption_delta_decision>
`detected: false`. 새 계약 없음 — 앞 플랜들의 계약을 페이즈 단위로 **검증**한다. `no-change`.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: 10건 전부 — `unclassified → unresolved (flagged)`. 대신 쓰는 근거: ROADMAP 기준 1~5 · D-601~D-613. 전체 10건 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — PNG 증빙 한 건이 PM 제출 → 결재 → 홈 「[오늘] 지급」 → 증빙 확인 → 지급 완료 → 지급 완료 목록까지 간다</name>
  <files>test/e2e/management-flow.spec.ts</files>
  <precondition>06-11 SUMMARY에 A-608-T 결과가 있다 — ⓪이 확인하고 없으면 멈춘다(여기서 새로 묻지 않는다).</precondition>
  <read_first>
    - .planning/ROADMAP.md 501-515행(Phase 6 기준 1~5 원문 — 이 태스크는 기준 5)
    - .planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md(A-608-T 테스트 모드 경로)
    - test/e2e/payment-single.spec.ts(06-03 · 06-04) · test/e2e/evidence-lifecycle.spec.ts(06-11 — 업로드 선례) · test/e2e/fixtures.ts(로그인 · 시드 도우미)
  </read_first>
  <behavior>
    - 이미지 경로: PM이 지출결의에 PNG 증빙을 첨부해 제출 → 결재자 승인 → 경영관리 홈 `[오늘] 지급` → 지급 대상 → 문서 → `지급 완료` 비활성(`확인 전 · 증빙 확인`) → `증빙 확인` → `지급 완료`(이체액 입력) → `/expenses?view=paid`에 번호 → 홈 지급 묶음 건수 1 감소
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인(B-4 — 06-11 결과 확인만). 없으면 멈춘다.

`test/e2e/management-flow.spec.ts`(신규)에 `describe` 하나와 이미지 경로 `test` 하나를 만든다. 증빙 파일은 저장소에 두지 않는다 — 테스트 안에서 작은 PNG 바이트 상수를 만들어 `setInputFiles({ name, mimeType, buffer })`로 넣는다. 업로드는 06-11이 확인한 Phase 5 경로의 테스트 모드를 그대로 탄다(새 흉내 없음). 증빙 필수 설정은 켠 상태 — 확인 전 `지급 완료` 비활성을 한 번 단언한다(EVID-02). 지급 완료 뒤 그 문서 번호가 `/expenses?view=paid`에 있고 홈 지급 묶음 건수가 1 줄어드는 것을 단언한다.
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/management-flow.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(06-11 A-608-T 줄 인용)가 SUMMARY에 있다
    - `management-flow.spec.ts`에 `image/png` mimeType 업로드가 있고 CI=true로 녹색이다
    - `git status --porcelain -- 'test/e2e/*.png'` 출력이 비어 있고 `git ls-files -- 'test/e2e/*.png'` 목록이 이 플랜 전과 같다(바이너리 픽스처 추가 없음 — 파이프 없이 git 종료 코드가 그대로 드러난다)
  </acceptance_criteria>
  <done>이미지 증빙 한 건이 PM 제출에서 경영관리 지급 완료까지 화면으로 실제 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF 경로 · 누수 스캔 완전성 · 의존성 · 토큰 diff(merge-base)</name>
  <files>test/e2e/management-flow.spec.ts, test/integration/leak-scan.test.ts</files>
  <read_first>
    - test/integration/leak-scan.test.ts 1-40행(side-effect import 목록) · test/unit/leak-scan-coverage.test.ts · domain/permissions/dto-registry.ts
    - 06-01 ~ 06-23 SUMMARY의 「Artifacts」(새 DTO 이름 · 새 `actions.registry.ts`)
  </read_first>
  <behavior>
    - PDF 경로: 같은 흐름을 PDF 증빙으로(서버가 원본 그대로 받는다) → 지급 완료
    - 누수 스캔: Phase 6 DTO 이름이 모두 `DTO_REGISTRY`에 있고, Phase 6 새 `actions.registry.ts`(`expenses/[id]` · `expenses` · `cards` · `cards/purchases`)가 모두 import돼 있다
  </behavior>
  <action>
`management-flow.spec.ts`에 PDF 경로 `test`를 더한다(최소 PDF 바이트 상수, `application/pdf`). `test/integration/leak-scan.test.ts`의 Phase 6 import를 06-01 ~ 06-23 SUMMARY의 「Artifacts」와 대조해 빠진 것을 더하고, `it("Phase 6 DTO가 모두 등록돼 있다")`를 하나 더해 SUMMARY에서 모은 Phase 6 DTO 이름 목록이 `DTO_REGISTRY`의 이름에 모두 있음을 단언한다(지급 · 카드 · 구매 요청 · 증빙 · 발행 요청 · 점검). 목록은 테스트 안 상수 배열이다 — 레지스트리에서 거꾸로 만들지 않는다(그러면 빠진 등록을 잡지 못한다).
  </action>
  <verify>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/management-flow.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts &amp;&amp; pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다</fails_when>
    <automated>git diff --stat "$(git merge-base origin/main HEAD)" -- package.json pnpm-lock.yaml docs/design/tokens.css</automated>
    <fails_when>출력이 한 줄이라도 있다(커밋된 변경까지 포함해 새 의존성 · 새 토큰이 생겼다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `management-flow.spec.ts`에 `image/png`와 `application/pdf` 두 mimeType 업로드가 각각 있고 두 test가 CI=true로 녹색이다
    - 새 `it`이 Phase 6 DTO 이름을 6개 이상 단언하고 녹색이다
    - merge-base diff 출력이 비어 있다
  </acceptance_criteria>
  <done>ROADMAP 기준 5의 E2E 두 경로와 누수 스캔 완전성이 녹색이고, 새 의존성 · 토큰이 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: R-4 PR 전 마이그레이션 재생성 · 저널 가드 · 페이즈 전체 게이트(§6 순서)</name>
  <files>db/migrations/meta/_journal.json, test/integration/leak-scan.test.ts</files>
  <read_first>
    - 06-03 · 06-05 · 06-06 · 06-08 · 06-10 · 06-12 · 06-16 · 06-18 SUMMARY의 마이그레이션 이름과 B-2 손편집 목록
    - db/migrations/0003_permissions_masters_spine.sql 80-90행 · db/migrations/0004_users_role_id_validate.sql · db/migrations/0010_revenue_entries.sql 1-5행
    - `.squawk.toml`(CONCURRENTLY 규칙 예외 이유)
  </read_first>
  <behavior>
    - origin/main 합친 뒤 Phase 6 마이그레이션 재생성 → `pnpm lint:sql` 0건 → `pnpm db:migrate` 성공 → 두 번째 generate `No schema changes`
    - Phase 5 표 제약(지출결의 선결제 CHECK · 파일 주인 제약)은 `NOT VALID` + 별도 `--custom` VALIDATE로 다시 선다
    - 페이즈 전체 게이트 녹색
  </behavior>
  <action>
**R-4 절차(순서 고정 — 한 단계라도 예상과 다르면 멈추고 SUMMARY에 적는다):** ⑴ `git fetch origin` 뒤 `git merge origin/main`(push 하지 않는다 — CLAUDE.md §2). 마이그레이션 밖 파일에 충돌이 나면 해결하지 말고 멈춰 보고한다. ⑵ 이 페이즈가 만든 `db/migrations/NNNN_*.sql`과 `meta/NNNN_snapshot.json`을 지우고 `_journal.json`에서 그 항목을 뺀다(SUMMARY의 이름 목록과 `git log --diff-filter=A --name-only "$(git merge-base origin/main HEAD)"..HEAD -- db/migrations/`로 대조 — main에서 온 파일은 건드리지 않는다). ⑶ `pnpm db:generate --name phase6`. ⑷ 생성 SQL 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` 한 쌍을 두고, **페이즈 6 이전 표**(Phase 5 지출결의 · 파일)에 거는 CHECK · FK에만 `NOT VALID`와 0003식 이유 주석을 다시 단다 — 같은 파일 안에서 새로 만드는 표의 제약은 그대로 둔다. ⑸ `pnpm db:generate --custom --name phase6_validate`에 SET LOCAL 한 쌍 + ⑷에서 `NOT VALID`로 건 제약마다 `VALIDATE CONSTRAINT`. ⑹ `pnpm lint:sql` 0건 → `pnpm db:dev` 초기화 뒤 `pnpm db:migrate` → 두 번째 `pnpm db:generate`가 `No schema changes`. ⑺ 저널 가드(아래 verify 셋째 줄).

**페이즈 게이트(CLAUDE.md §6 순서):** 싼 게이트(`pnpm lint` · `pnpm typecheck` · `pnpm build`) → 실행자가 아닌 별도 에이전트가 `CI=true`로 06-01 ~ 06-23 must_haves의 `verification: backstop` 41건 전부를 DOM 실측 감사(스크린샷 육안 금지) → 감사가 찾은 것 수정 → 전체 게이트(`pnpm test:unit` · `pnpm test:integration` · `pnpm test:e2e:ci`) 한 번. 출력은 요약만 SUMMARY에 적고 실패는 실패 부분만 인용한다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 개수를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/migrate-runner.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — migrate-runner가 빨가면 재생성 · `--custom` 마이그레이션이 러너 규약을 깨뜨렸다</fails_when>
    <automated>if [ -f test/unit/db/migration-journal.test.ts ]; then pnpm vitest run --project unit test/unit/db/migration-journal.test.ts; else echo GUARD-ABSENT; fi</automated>
    <fails_when>종료 코드가 0이 아니다(가드가 있고 빨갛다). `GUARD-ABSENT` 출력은 실패가 아니고 SUMMARY에 적는다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm test:unit &amp;&amp; pnpm db:dev &amp;&amp; pnpm test:integration &amp;&amp; pnpm test:e2e:ci</automated>
    <fails_when>하나라도 0이 아닌 코드로 끝나거나 failed가 1 이상이다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY에 R-4 ⑴~⑺ 각 단계 결과와 다시 만든 마이그레이션 파일 이름 목록이 있다
    - `grep -c "NOT VALID" db/migrations/*phase6.sql`이 1 이상 · `grep -c "VALIDATE CONSTRAINT" db/migrations/*phase6_validate.sql`이 1 이상
    - 별도 에이전트의 DOM 감사 결과(backstop 41건 판정)와 전체 게이트 요약이 SUMMARY에 있다 — 전체 게이트는 감사 · 수정 뒤 한 번만 돌았다
  </acceptance_criteria>
  <done>main과 합친 상태에서 스키마가 한 벌로 다시 서고, 페이즈 전체 게이트가 §6 순서로 한 번에 녹색이다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브랜치 → main 합치기 | main의 새 마이그레이션과 이 페이즈 마이그레이션의 번호 · 저널이 만난다 |
| E2E → Phase 5 업로드 경로 | 테스트 모드(06-11 확인)를 그대로 탄다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-115 | Tampering | 합친 뒤 마이그레이션 번호 · 저널 충돌(R-4) | high | mitigate | Phase 6 마이그레이션을 지우고 main 위에서 다시 만든다 · 두 번째 generate `No schema changes` · migrate-runner 통합 테스트 · 가드 테스트(있으면) |
| T-06-116 | Denial of Service | 다시 만든 마이그레이션의 기존 표 잠금 | medium | mitigate | B-2 손편집을 Phase 5 표 제약에 다시 적용(SET LOCAL · NOT VALID · 별도 VALIDATE) · `pnpm lint:sql` 0건 |
| T-06-117 | Information Disclosure | Phase 6 DTO 누락으로 누수 스캔 밖 | medium | mitigate | 상수 목록 대조 `it` — 레지스트리에서 거꾸로 만들지 않는다 |
| T-06-118 | Tampering | E2E 업로드 경로 | low | accept | 새 업로드 면을 만들지 않는다 — Phase 5 경로의 테스트 모드를 그대로 타고, 테스트 파일은 코드 안 바이트 상수다 |
</threat_model>

<verification>
- E2E management-flow(두 경로) CI=true 녹색, 통합 leak-scan · migrate-runner, 단위 leak-scan-coverage 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`, 저널 가드 녹색 또는 `GUARD-ABSENT` 기록
- `git diff --stat "$(git merge-base origin/main HEAD)" -- package.json pnpm-lock.yaml docs/design/tokens.css` 빈 출력
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 페이즈 backstop 41건 DOM 실측 감사 → 수정 → 전체 게이트(`pnpm test:e2e:ci` 포함) 한 번
- 이 플랜 뒤 Post-build(`/review` → `/qa` → `/cso` → `/ship`)는 CLAUDE.md §4대로 건너뛰지 않는다 — 이 페이즈는 권한 · 외부 입력(업로드 · 링크 URL)을 건드렸으므로 `/cso`는 필수다
</verification>

<success_criteria>
- ROADMAP 기준 5(E2E 두 경로 · 통합 · 누수 스캔)가 CI=true로 녹색이다
- main과 합친 상태에서 스키마 · 저널이 한 벌이고 `No schema changes`다
- 페이즈 전체 게이트가 §6 순서로 한 번에 통과한다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | 다시 만든 `NNNN_phase6.sql`(SET LOCAL · Phase 5 표 제약 NOT VALID) · `NNNN_phase6_validate.sql`(`--custom`, VALIDATE) — 앞 플랜들의 Phase 6 마이그레이션을 대신한다 |
| 테스트 | `test/e2e/management-flow.spec.ts` · `test/integration/leak-scan.test.ts`(Phase 6 DTO 완전성 단언) |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-24-SUMMARY.md` when done
</output>
