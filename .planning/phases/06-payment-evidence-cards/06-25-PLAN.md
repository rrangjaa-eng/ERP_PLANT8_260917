---
phase: 06-payment-evidence-cards
plan: 25
type: execute
wave: 10
depends_on: ["06-11", "06-14", "06-16", "06-18"]
files_modified:
  - db/schema/files.ts
  - db/migrations/meta/_journal.json
  - domain/evidence-attachments/index.ts
  - domain/evidence-reviews/upload-checks.ts
  - domain/corp-card-usages/index.ts
  - repositories/corp-card-usages.ts
  - repositories/files.ts
  - domain/purchase-requests/index.ts
  - app/(app)/cards/actions.ts
  - app/(app)/cards/actions.registry.ts
  - app/(app)/cards/card-usage-form.tsx
  - app/(app)/cards/card-usage-list.tsx
  - app/(app)/cards/purchases/actions.ts
  - test/integration/card-usage-evidence.test.ts
  - test/e2e/card-usage-evidence.spec.ts
autonomous: true
requirements: [EVID-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**[EVID-01 카드 몫 · 교차 B-2]** 카드 사용 수정 폼(`/cards?editId=`)에서 그 건의 수정 권리(06-09 `cardUsageRights` — 등록한 사람 · 대리 등록 권한자, 구매 완료로 생긴 건은 구매 완료한 사람)가 있는 사람이 카드 전표 파일(이미지 · PDF)을 붙이고 뗄 수 있고, 붙이면 `/cards` 목록 그 행의 증빙 열이 `—` → `있음`이 된다(UI-SPEC S7 · S8 · S9)"
    - "새 건(`/cards?new=1`)과 구매 완료(`/cards?new=1&purchase={id}` — UI-SPEC S13 「증빙 첨부 선택」)에도 같은 첨부 영역이 서고, 저장 전에 올린 전표는 카드 사용 저장(`createCardUsage` · `completePurchaseRequest`)과 **같은 트랜잭션**에서 그 건에 묶인다 — 저장이 실패하거나 거부되면 하나도 묶이지 않는다. 저장 전에 올린 전표가 있으면 `취소 Esc`는 입력이 있는 것으로 본다(Copywriting 「확인 — 입력 버리기」)"
    - "카드 전표는 선택이다 — 없어도 등록 · 수정 · 구매 완료가 막히지 않고, 붙이거나 떼도 카드 사용의 결제 합계 · 공급가 · 부가세 · 연결 · `version`이 바뀌지 않는다(UI-SPEC S7 「전표를 떼도 바뀌는 값이 없다」 — 비용은 결제 합계에서 역산, D-607 · D-608)"
    - "업로드는 Phase 5 업로드 경로(서명 URL · 완료 통보 · 파일 표)와 06-11 `checkEvidenceUpload`(크기 · 형식 · 중복)를 그대로 지난다 — 새 저장 경로를 만들지 않는다(06-16과 같은 꼴, 주인 종류 `corp_card_usage` 하나를 더한다)"
    - "**[DR-3 · O-6 rev 9 — UI-SPEC S7 「중복」]** 카드 전표는 중복(SHA-256) 판정에서 **지출결의 증빙과 한 종류**다 — 같은 해시가 지출결의 증빙이나 다른 카드 사용의 전표에 있으면 막고, 차수 승인 증빙 · 리저브 줄 증빙에만 있으면 막지 않는다. 지출결의 증빙을 올릴 때도 같은 파일이 카드 전표에 있으면 막힌다(같은 영수증이 두 번 비용이 되는 길을 막는다)"
    - "중복 행 오류의 문서 번호는 기존 주인이 지출결의이고 올린 사람이 그 문서를 읽을 수 있을 때만 싣는다(`같은 파일이 26001-0004 증빙에 있음 · 다른 파일 고르기`) — 기존 주인이 카드 사용이거나 읽을 수 없으면 `이미 첨부된 파일 · 다른 파일 고르기`, 같은 카드 사용이면 `같은 파일이 이미 첨부됨`(Copywriting 「Error — 증빙 업로드(S7 · S21 · S22, §7-10 행 오류 모양)」 문구 그대로)"
    - "[계획 판단] 보관된(삭제된) 카드 사용의 전표는 중복 판정에 들지 않는다 — UI-SPEC S9 「카드를 잘못 고른 건은 삭제 뒤 새로 등록한다」 경로에서 같은 전표를 다시 붙일 수 있다. 보관된 건에는 붙이거나 뗄 수 없고, 되돌리기(보관 해제, 06-09)하면 파일이 그대로 돌아온다"
    - "주인 권리는 서버가 `ownerKind` · `ownerId`로 카드 사용 행을 다시 읽어 판정한다 — 쓰기 = `cardUsageRights`의 수정 권리 + 보관 아님, 읽기(파일 목록 · 크게 보기) = UI-SPEC S8 목록 범위(06-05 리포지토리 범위 조건)와 같은 판정. 저장 전 업로드는 새 건이면 폼 카드 옵션이 1장 이상, 구매 완료면 `cards.purchases` write + 요청 `신청`일 때만 받는다. 브라우저가 보낸 권리 · 카드 사용 id를 믿지 않는다"
    - "[계획 판단] 수정 모드의 첨부 · 삭제는 즉시 저장이다(UI-SPEC S21 · S22와 같은 꼴) — 폼의 `카드 사용 저장` · `취소 Esc`에 들지 않고 카드 사용 `version`을 올리지 않아, 전표를 붙인 뒤 같은 폼을 저장해도 동시성 거부가 나지 않는다"
    - "**[X-2 r3]** 카드 전표 업로드 · 완료 통보 · 삭제 · 대기 결합은 06-11 무효화 훅(`invalidateEvidenceReview`)을 부르지 않는다 — 훅 호출은 Phase 5 증빙 함수 안의 `ownerKind === \"expense\"` 긍정 조건 안에만 있어(06-16이 세운 조건 — Task 1 ⓪이 확인), 카드 전표가 지출결의 행을 잠그거나 어느 지출결의의 version · 확인 기록을 바꾸지 않는다(06-03 N-3 전역 잠금 순서 그대로). 통합 `it` 「카드 전표 첨부 · 삭제 · 대기 결합 전후 모든 지출결의 version 불변」"
    - "06-03 tx 규약을 따른다 — 권리 판정(`cardUsageRights` · `can` · 목록 범위)은 트랜잭션 전, 트랜잭션 안에서는 보관 아님 확인과 대기 전표 결합만 `tx`로 한다. 대기 전표 결합은 `올린 사람 = 저장하는 사람` · `주인 없음` 조건으로만 걸리고, 바뀐 행 수가 넘긴 참조 수와 다르면 저장 전체가 롤백된다"
    - "**마이그레이션 규약(06-10 첫 적용 B-2):** 파일 표 주인 제약에 `corp_card_usage`를 더하는 마이그레이션은 머리에 SET LOCAL 한 쌍, 새 CHECK · FK를 `NOT VALID`로 걸고 VALIDATE는 `pnpm db:generate --custom`으로 만든 별도 마이그레이션에서 한다 — `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate`가 `No schema changes`"
    - "S7 empty(카드 폼): 첨부 영역 한 줄 `이미지·PDF 10MB`(숫자 = 설정 값 `evidence.max_size_mb`) — Phase 5 첨부 컴포넌트(§7-10, UA-606)가 카드 폼 · 구매 완료 폼의 `증빙` 줄(메모 아래)에 선다"
    - "S7 loading(카드 폼): 업로드 행 아래 2px 진행 바(§7-10). 폰 사진은 최대 변 2000px JPEG로 줄인 뒤 올리고 PDF는 원본이다 — 카드 폼은 폰에서도 그대로 서는 입력 표면이라(UI-SPEC S9 「폰: 폼이 그대로 선다(UX-03)」) 폰(<700)에서도 첨부 영역이 선다(리저브 O-22와 다르다)"
    - "S7 error(카드 폼): 「Error — 증빙 업로드」 넷(크기 · 형식 · 중복 · 완료 통보 실패)이 파일 행 자리에 선다 — 중복 번호 규칙은 위 줄"
    - "S7 populated(카드): 파일 행(§7-10 이름 · 크기 · 날짜 · 크게 보기, 쓰기 권리면 3차 `삭제`). 카드 사용 목록 증빙 열은 `있음`/`—` 글자"
    - "S7 partial(카드 폼): 여러 파일 중 일부만 실패하면 성공 행 + 실패 행 각각(§7-7 첨부 PARTIAL)"
    - "S7 zero-one-many(카드): 카드 전표는 마지막이어도 삭제에 확인 모달이 없다(`lastEvidenceDeleteNeedsConfirm` = false — §7-10 보통 삭제, SP-6 대상 아님). 지우면 목록 증빙 열만 `있음` → `—`"
    - "S8 partial: 증빙 열 `있음`/`—`는 서버가 보낸 값(보관 안 된 그 카드 사용의 파일 행 1개 이상)이고 상태 색이 없다 — 06-05가 세운 열에 실제 값을 댄다. 폰 P3 증빙 값은 행 시트에서 같은 글자로 보인다(S8 「폰」)"
    - "S9 populated: 와이어프레임대로 메모 아래 `증빙` 첨부 영역(S7, 선택)이 새 건 · 수정 · 구매 완료 세 모드에 선다"
    - "S13(「증빙 첨부 선택」): 구매 완료 모드에도 같은 첨부 영역이 서고, 붙인 전표는 구매 완료로 생긴 카드 사용 건에 묶인다 — 등록 칸 `구매 요청 26001-C0001`과 구매 완료 트랜잭션(06-12)은 그대로다"
    - statement: "S7 overflow(카드 폼): 파일 10개 이상 카드 사용에서 첨부 영역 행 목록이 §7-10 규칙대로 선다 — CI=true DOM 감사"
      verification: backstop
    - statement: "S7 long-text(카드 폼): 80자 한글 파일명이 파일 행에서 말줄임 + `title`로 서고 `삭제` 3차를 밀지 않는다 — 폭 1280 · 390에서 CI=true DOM 감사"
      verification: backstop
  prohibitions:
    - statement: "카드 사용을 읽을 수 없는 사람(S8 범위 밖)의 응답 · DOM에 그 카드 전표의 파일 목록 · 내려받기 URL이 실려서는 안 된다"
      status: resolved
      verification: test
    - statement: "권리 없는 사람이나 보관된 카드 사용에 서버 액션을 직접 불러 전표를 붙이거나 뗄 수 있어서는 안 된다"
      status: resolved
      verification: test
    - statement: "남이 올린 대기 전표를 자기 카드 사용 저장에 묶을 수 있어서는 안 된다"
      status: resolved
      verification: test
    - statement: "카드 전표 첨부 · 삭제가 카드 사용의 금액 · 연결 · `version`을 바꿔서는 안 된다"
      status: resolved
      verification: test
    - statement: "카드 전표 첨부 · 삭제 · 대기 결합이 지출결의 행을 잠그거나 지출결의의 version · 확인 기록을 바꿔서는 안 된다(X-2 r3 — 06-11 훅은 지출결의 주인 전용)"
      status: resolved
      verification: test
    - statement: "카드 폼 첨부가 UI-SPEC에 없는 새 시각 패턴(색 · 서체 · radius · 새 컴포넌트 · 새 레이아웃 꼴)을 만들어서는 안 된다"
      status: resolved
      verification: judgment
  artifacts:
    - path: "domain/evidence-attachments/index.ts"
      provides: "첨부 주인 종류 `corp_card_usage` 갈래 — 수정 권리 · 보관 · 목록 범위 · 저장 전 업로드(새 건 · 구매 완료) 권리 판정"
      exports: ["attachmentOwnerRights", "ATTACHMENT_OWNER_KINDS"]
    - path: "domain/evidence-reviews/upload-checks.ts"
      provides: "중복 판정의 주인 종류 묶음 — 카드 전표 = 지출결의 한 종류(DR-3 · S7), 조회에 `ownerKinds`로 넘긴다(리포지토리는 목록만 받는다 — X-4 r3), 보관된 카드 사용 전표 제외"
      exports: ["checkEvidenceUpload", "duplicateScopeKinds"]
    - path: "test/integration/card-usage-evidence.test.ts"
      provides: "카드 전표 권리 · 대기 결합 원자성 · 중복 묶음 · 보관 · 값 불변 · 노출 증명"
      min_lines: 60
    - path: "test/e2e/card-usage-evidence.spec.ts"
      provides: "수정 · 새 건 · 구매 완료 전표 첨부 → 목록 `있음`, 폰 사진 축소, 마지막 전표 삭제 모달 없음, 중복 번호"
      min_lines: 40
  key_links:
    - from: "app/(app)/cards/actions.ts"
      to: "domain/evidence-attachments/index.ts"
      via: "카드 전표 업로드 의도 · 완료 통보 · 삭제 액션이 `ownerKind` 고정 `corp_card_usage`로 주인 권리를 한 곳에서 받는다"
      pattern: "corp_card_usage"
    - from: "domain/purchase-requests/index.ts"
      to: "domain/corp-card-usages/index.ts"
      via: "구매 완료 트랜잭션이 대기 전표 참조(`evidenceRefs`)를 `createCardUsage` 외부 tx 경로로 넘겨 같은 트랜잭션에서 묶는다"
      pattern: "evidenceRefs"
    - from: "repositories/corp-card-usages.ts"
      to: "db/schema/files.ts"
      via: "목록 `hasEvidence`가 보관 안 된 그 건의 파일 행 존재로 정해진다"
      pattern: "hasEvidence"
    - from: "domain/evidence-reviews/upload-checks.ts"
      to: "domain/evidence-attachments/index.ts"
      via: "중복 조회 범위가 주인 종류 묶음(`duplicateScopeKinds`)을 쓴다 — 카드 전표 ↔ 지출결의"
      pattern: "duplicateScopeKinds"
    - from: "domain/evidence-reviews/upload-checks.ts"
      to: "repositories/files.ts"
      via: "`checkEvidenceUpload`가 `duplicateScopeKinds(ownerKind)`를 `ownerKinds`로 넘긴다 — 리포지토리는 목록만 받고 domain을 import하지 않는다(X-4 r3 — 순환 없음, 가칭 경로는 06-11 SUMMARY의 실제 이름)"
      pattern: "ownerKinds"
---

<objective>
카드 사용 건에 카드 전표 파일을 붙인다 — EVID-01 「세금계산서·영수증·카드 전표 파일을 지출결의·**법인카드 건**에 첨부한다(폰 사진 포함)」의 카드 몫이다. UI-SPEC rev 9가 약속한 네 표면(S7 첨부 규칙 · S8 목록 `증빙` 열 · S9 폼 `증빙` 첨부 영역 · S13 구매 완료 「증빙 첨부 선택」)을 이 플랜이 짓는다. 06-11의 업로드 검사와 06-16의 첨부 주인 확장 꼴을 그대로 재사용하고, 주인 종류 `corp_card_usage` 하나를 더한다.

**Phase Goal(ROADMAP 원문):** 경영관리가 결재 통과 건의 증빙을 확인해 지급 완료하며(실제 이체액 입력 → 공급가 역산), 법인카드·구매 요청·팀 비용까지 모든 나가는 돈이 견적 줄에 한 번만 연결되고, 완료(정산) 전 미결 점검이 선다. 지급일 자동 계산·마감·알림은 Phase 7이다.
user story 형식이 아니다(MVP 모드) — `/gsd mvp-phase 6` 권고, 계획은 이야기를 지어내지 않았다.

Purpose: 교차 검토 B-2(design-apply-cross-r1) — 카드 전표 첨부를 짓는 플랜이 없었다(06-11 Round 1 · 06-16 Round 2 보류 줄). `corp_card_usages`는 06-05가 처음 만들어 Phase 5가 카드 사용을 파일 주인으로 둘 수 없다. 오케스트레이터 결정대로 범위 밖으로 빼지 않고 이 페이즈 마지막 쪽 웨이브에 작은 플랜으로 둔다. DR-3 주인 종류 규칙(카드 전표 = 지출결의와 한 종류, UI-SPEC S7 · O-6)을 06-11이 넘긴 자리(`checkEvidenceUpload`의 중복 조건)에서 닫는다.

**요구사항 표기:** EVID-01은 ROADMAP · REQUIREMENTS 추적표에서 Phase 5 매핑이다. 이 플랜은 그중 「법인카드 건에 첨부」 몫만 진다(지출결의 첨부 · 업로드 경로는 Phase 5). REQUIREMENTS 추적표 `| EVID-01 | Phase 5 | Pending |`를 「Phase 5(지출결의) + Phase 6(카드 사용)」으로 나누는 일은 06-01 Task 3(REQ-ROUTE — 사용자가 고른 경로로 REQUIREMENTS 문구를 바꾸는 체크포인트)이 EXP-07 · EXP-15 · EXP-09와 함께 한다(r3 X-8 — gsd 도구에 요구사항 본문 편집 명령이 없어 손 편집은 사용자 경로로만). ROADMAP Phase 6 계획 목록 · 계획 수(25)는 r3 계획 반영 때 이미 고쳤다. 둘 다 이 플랜 `files_modified` 밖이다.

Output: 파일 표 주인 제약 확장 마이그레이션 2개(생성 1 + `--custom` VALIDATE 1), `attachmentOwnerRights` `corp_card_usage` 갈래, `duplicateScopeKinds`, 카드 전표 업로드 의도 · 완료 통보 · 삭제 액션 셋, 카드 폼 세 모드의 첨부 영역, 목록 `hasEvidence`, 통합 1 · E2E 1.

**규모 근거:** `files_modified` 15개(r3 X-4가 Phase 5 파일 리포지토리 — 중복 조회 · 대기 결합 자리, 가칭 `repositories/files.ts` — 를 더했다), `estimate.tokens` 95000(`confidence: low`, 표본 0 — 보정 계수 1). 예산 100k 안이다. 체커 Files/plan 표(`.claude/agents/gsd-plan-checker.md` Thresholds — 목표 5-8 · 경고 10 · 막음 15+)의 15에 닿는다 — 받아들이는 근거는 구성이다: 테스트 2(통합 · E2E) · 스키마 · 저널 2(마이그레이션 규약 B-2) · 등록 1(`actions.registry.ts`) · 실제 코드 10(06-16 · 06-11 · 06-05가 만든 파일에 갈래 · 칸을 더하는 것이 대부분), 태스크별 11 · 9 · 7개. **06-16과 같은 한 규칙**(15개는 이 근거와 함께 보고하고 쪼개지 않는다 · 16번째 파일에서 멈춘다)을 따른다. 재분할 방아쇠: Task 1 ⓪에서 ⑴ 첨부 영역에 넘길 값(설정 한도 · 파일 목록 · 쓰기 가능 여부)이 `app/(app)/cards/page.tsx`의 칸별 전달을 거쳐야만 폼에 닿거나 ⑵ 06-11의 중복 조회가 06-11 SUMMARY의 리포지토리(이 플랜의 `repositories/files.ts` 자리)가 아닌 파일에 있거나 ⑶ A-608-P 결합 함수가 주인 종류를 받지 않는데 그 함수가 그 리포지토리 밖(예: `domain/expenses/evidence.ts`)에 있거나 ⑷ Phase 5 증빙 함수의 06-11 훅 호출이 `ownerKind === "expense"` 긍정 조건 안에 있지 않으면(X-2 — `domain/expenses/evidence.ts` 필요) — 16번째 파일이다 — 코드를 바꾸지 않고 멈추고 `06-25 파일 한도 — {그 파일} 필요`를 보고한다(오케스트레이터가 그 파일을 더하거나 Task 3을 떼어 낸다).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-payment-evidence-cards/06-CONTEXT.md
@.planning/phases/06-payment-evidence-cards/06-UI-SPEC.md
@.planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md
@.planning/phases/06-payment-evidence-cards/06-16-SUMMARY.md
@CLAUDE.md
</context>

## 선행 의존(A-6xx)

**Task 1 ⓪이 main에서 다시 확인하고, 없으면 코드를 한 줄도 바꾸지 않고 `선행 의존 {id} 미해소 — {무엇} 없음, 06-25 착수하지 않음`을 출력하고 멈춘다.** 이름은 앞 플랜 SUMMARY의 실제 이름을 따른다. R-7 메모는 06-01 objective와 같다.

| id | 먼저 들어와야 하는 것 | 재확인(Task 1 ⓪) |
|---|---|---|
| 06-16 | 첨부 주인 확장 — `ATTACHMENT_OWNER_KINDS` · `attachmentOwnerRights`, 파일 표 주인 칸 모양(⒜ `owner_kind` 칸 + CHECK / ⒝ 주인별 FK 칸 + 「주인 하나」 CHECK — 06-16 SUMMARY), Phase 5 업로드 의도 · 완료 통보 · 삭제의 주인 타입이 `ATTACHMENT_OWNER_KINDS`, **06-11 훅 호출이 `ownerKind === "expense"` 긍정 조건 안(r3 X-2 — 06-16 SUMMARY `[r3 X-2]` 줄)** | `grep -c "ATTACHMENT_OWNER_KINDS" domain/evidence-attachments/index.ts domain/expenses/evidence.ts` 합이 2 미만이면 멈춤 · `grep -n "⒜\|⒝" .planning/phases/06-payment-evidence-cards/06-16-SUMMARY.md` 0건이면 멈춤 · `grep -n "invalidateEvidenceReview" domain/expenses/evidence.ts`의 호출 줄마다 `ownerKind === "expense"` 조건 안인지 읽어 SUMMARY에 적는다 — 아니면(조건 없음 · 부정 꼴) 「규모 근거」 방아쇠 ⑷ |
| 06-11 | `checkEvidenceUpload`(주인 종류 인자) · `lastEvidenceDeleteNeedsConfirm` · 중복 조회 리포지토리(`ownerKinds` 목록을 받는 함수 — r3 X-4) · 업로드 테스트 모드(A-608-T) | `grep -c "ownerKinds" domain/evidence-reviews/upload-checks.ts` 0이면 멈춤 · `grep -n "A-608-T" .planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md` 0건이면 멈춤 · 중복 조회 리포지토리 경로를 06-11 SUMMARY에서 찾아 이 플랜의 `repositories/files.ts` 자리와 다르면 「규모 근거」 방아쇠 ⑵ |
| 06-05 · 06-09 · 06-12 · 06-14 | `corp_card_usages`(보관 칸 `archived_at`) · `createCardUsage(viewer, input, pre, tx?)`(`runCreate`) · `cardUsageRights`(`domain/corp-card-usages/rights.ts`) · `updateCardUsage` · `restoreCardUsage` · `completePurchaseRequest`(`runComplete`) · 카드 폼 세 모드(새 건 · `?editId=` · `mode = purchase`) · 목록 증빙 열 | `git ls-files domain/corp-card-usages/rights.ts` 비면 멈춤 · `grep -c "archived_at" db/schema/corp-card-usages.ts` 0이면 멈춤 · `grep -c "runComplete" domain/purchase-requests/index.ts` 0이면 멈춤 · `grep -c "restoreCardUsage" domain/corp-card-usages/index.ts` 0이면 멈춤 |
| 06-18 | 마이그레이션 저널 순서 — 06-18의 `revenue_issue_requests`가 저널에 먼저 있다(같은 `_journal.json`, 06-05 「저널 순서」 선례) | `grep -c "revenue_issue_requests" db/migrations/meta/_journal.json` 0이면 멈춤 |
| A-608 · UA-606 | Phase 5 첨부 컴포넌트(06-11 SUMMARY의 실제 경로)가 업로드 · 삭제 동작을 prop으로 받고 prop 없이 넘기면 읽기로 그려진다(06-16 ⓪이 확인한 것) | 그 경로를 `git ls-files`로 찾아 비면 멈춤 |
| **A-608-P (저장 전 첨부 규약)** | Phase 5 지출결의 폼(새 문서)이 **문서 저장 전에 올린 파일을 문서에 묶는 방식** — ⒜ 대기 결합: 저장 전 업로드가 주인 없는 대기 파일(행 또는 토큰)로 남고, 문서 저장 트랜잭션이 결합 함수(`tx` 인자)로 주인을 붙이며, 대기 파일에 올린 사람 칸이 있다 / ⒝ 저장 뒤 첨부: 문서가 먼저 있어야 올릴 수 있다 | Phase 5 폼 첨부 코드 · 파일 표를 읽어 ⒜/⒝와 결합 함수 이름 · 파일 경로 · 올린 사람 칸 이름, 그리고 **⒞ 결합 함수가 주인 종류를 인자로 받는가**(파일 표 ⒜ 모양이면 `owner_kind` 값 / ⒝ 모양이면 채울 주인 FK 칸 — r3 X-3)를 SUMMARY에 적는다. **⒝이거나, ⒜인데 결합 함수가 `tx`를 받지 않거나 올린 사람 칸이 없으면** 코드를 한 줄도 바꾸지 않고 아래 「저장 전 첨부 규약 공백」 선택지를 출력하고 멈춘다. ⒞이 「받지 않음」이면 Task 2가 그 함수에 주인 종류 인자를 더한다 — 함수가 Phase 5 파일 리포지토리(이 플랜의 `repositories/files.ts` 자리)에 있으면 그 파일에서, 밖에 있으면 「규모 근거」 방아쇠 ⑶ |

**저장 전 첨부 규약 공백(계획이 스스로 정하지 않는다):** `선행 의존 A-608-P 미해소 — {무엇}, 06-25 착수하지 않음`과 함께 묻는다 — ⑴ Phase 5 첨부 경로에 대기 결합(주인 없는 대기 파일 + 올린 사람 칸 + `tx`를 받는 결합 함수)을 Phase 5 쪽 작업으로 더한 뒤 재개(경로의 주인이 고친다) ⑵ 새 건 · 구매 완료 모드에서는 첨부 영역을 세우지 않고 저장 뒤 수정 모드에서만 붙인다 — UI-SPEC S9 와이어프레임 · S13 「증빙 첨부 선택」을 고쳐야 하므로 UI-SPEC 개정과 사용자 승인이 먼저다. 즉석으로 대기 표나 임시 저장을 만들지 않는다.

**새 의존성 없음:** 폰 사진 축소 · SHA-256은 Phase 5 · 06-11 첨부 컴포넌트의 것을 그대로 쓴다. E2E의 큰 이미지는 브라우저 canvas로 만든다(새 픽스처 파일 · 패키지 없음).

## 열린 선택 — 사용한 기본값

| id | 기본값 | 고칠 곳 |
|---|---|---|
| O-6 (DR-3) | 중복은 같은 주인 종류 안에서만 막고, **카드 전표는 지출결의와 한 종류**로 센다(UI-SPEC S7 · O-6 rev 9). **사용자 확정(2026-09-25, DR-3 = 허용).** 답이 「종류와 상관없이 모두 막기」로 오면 `duplicateScopeKinds`가 모든 종류를 돌려주게 한 줄만 바꾼다 | `domain/evidence-reviews/upload-checks.ts` `duplicateScopeKinds` |
| 수정 모드 첨부 시점(계획 판단) | 즉시 저장 — 폼 저장 · `취소 Esc`에 들지 않고 카드 사용 `version`을 올리지 않는다(S21 · S22와 같은 꼴, 전표는 카드 사용의 값을 바꾸지 않는다 — S7) | `card-usage-form.tsx` 수정 모드 · 카드 전표 액션 |
| 새 건 · 구매 완료 첨부 시점 | Phase 5 저장 전 첨부 규약(A-608-P ⒜) 그대로 — 저장 트랜잭션에서 결합 | `createCardUsage` `runCreate` · `completePurchaseRequest` `runComplete` |
| 보관된 카드 사용의 전표(계획 판단) | 중복 판정에서 뺀다(S9 「삭제 뒤 새로 등록」 경로) · 쓰기 거부 · 되돌리면 그대로 | 중복 조회 조건 · `attachmentOwnerRights` |
| 되돌리기 뒤 중복(계획 판단) | 보관 해제(06-09 `restoreCardUsage`)는 중복을 다시 보지 않는다 — 되돌리기는 토스트 수명 안의 동작이라 그사이 같은 전표가 다른 건에 붙는 경우가 드물다(T-06-201 수용). 토스트 수명 규칙이 바뀌면(교차 R-1) 다시 본다 | `restoreCardUsage`(이 플랜은 고치지 않는다) |
| 읽기 범위 | 파일 목록 · 크게 보기 = S8 목록 범위(06-05 리포지토리 범위 조건). 파일 목록은 수정 DTO에만 싣고 목록 DTO에는 `hasEvidence` 불리언만 | `attachmentOwnerRights` 읽기 갈래 · `repositories/corp-card-usages.ts` |

<assumption_delta_decision>
`detected: false`(검출기 결과 — 이 플랜이 바꾸지 않는다). 06-16이 넓힌 첨부 주인 종류에 `corp_card_usage` 하나를 **더한다** — 주인 식별 모델(주인 종류 + 주인 id)은 그대로라 `no-change`. 차수 승인 · 리저브 주인의 동작은 바꾸지 않는다. **지출결의 주인은 중복 판정 범위만 넓어진다** — 같은 파일이 카드 전표에 있으면 지출결의 증빙 업로드도 막힌다(DR-3 · UI-SPEC S7, 번호 없는 문구 — 의도된 규칙 확장이고 S7이 정본이다, r3 X-7). 지출결의의 권리 · 업로드 경로 · 삭제 · 06-11 무효화 훅은 그대로다.
</assumption_delta_decision>

<probe_fallback>
coverage.json의 이 플랜 몫: EVID-01(카드 몫) — coverage.json 생성 때 이 페이즈 요구사항이 아니어서 행이 없다 → `unresolved (flagged)`. 대신 쓰는 근거: REQUIREMENTS EVID-01 · UI-SPEC S7 · S8 · S9 · S13 · O-6 rev 9 · 교차 B-2. 전체 표는 06-01 `<probe_fallback>`.
</probe_fallback>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 트레이서 — 등록자가 수정 폼에서 카드 전표 한 장을 붙이면 파일 행이 서고 `/cards` 목록 증빙 열이 `있음`이 된다</name>
  <files>db/schema/files.ts, db/migrations/meta/_journal.json, domain/evidence-attachments/index.ts, domain/corp-card-usages/index.ts, repositories/corp-card-usages.ts, app/(app)/cards/actions.ts, app/(app)/cards/actions.registry.ts, app/(app)/cards/card-usage-form.tsx, app/(app)/cards/card-usage-list.tsx, test/integration/card-usage-evidence.test.ts, test/e2e/card-usage-evidence.spec.ts</files>
  <precondition>「선행 의존(A-6xx)」 표의 항목(A-608-P 포함)이 main에 있다 — ⓪이 확인하고 없으면 멈춘다(A-608-P가 ⒝이거나 조건을 못 채우면 선택지를 묻는다).</precondition>
  <read_first>
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S7. 증빙 첨부 규칙」 절 전체(「중복(SHA-256) — 같은 주인 종류 안에서만」 · 카드 전표 문단 · 「마지막 카드 전표 삭제는 모달이 없다」) · 「S8. 법인카드 사용 목록 `/cards`」 절(열 · 폰) · 「S9. 법인카드 사용 등록·수정 폼」 절(와이어프레임 `증빙` 줄 · 「카드 — 수정 모드」 · 「수정·삭제 권리(O-11)」 · 「폰」) · UI Considerations S7 · S8 · S9 행 · Copywriting 「Error — 증빙 업로드(S7 · S21 · S22, §7-10 행 오류 모양)」
    - .planning/phases/06-payment-evidence-cards/06-16-SUMMARY.md(주인 칸 모양 ⒜/⒝ · 마이그레이션 이름 · 업로드 함수 이름 · 첨부 컴포넌트 prop) · domain/evidence-attachments/index.ts(06-16 — `quote_revision` · `reserve_entry` 갈래 꼴)
    - .planning/phases/06-payment-evidence-cards/06-11-SUMMARY.md(A-608-T 테스트 모드 · 중복 조회 자리) · domain/evidence-reviews/upload-checks.ts
    - domain/corp-card-usages/rights.ts(`cardUsageRights` — 06-09 · 06-12) · domain/corp-card-usages/index.ts(수정 DTO · 목록 DTO · `runCreate` · `runUpdate`) · repositories/corp-card-usages.ts(목록 범위 조건 · 보관 제외)
    - app/(app)/cards/card-usage-form.tsx(세 모드) · app/(app)/cards/card-usage-list.tsx(증빙 열 렌더) · app/(app)/cards/actions.ts · actions.registry.ts
    - app/(app)/projects/actions.ts(06-16 — 승인 증빙 업로드 의도 · 완료 통보 · 삭제 액션 셋, `ownerKind` 고정 꼴)
    - db/schema/files.ts · 06-16의 `attachment_owner_kinds` · `attachment_owner_kinds_validate` 마이그레이션(B-2 절차 선례)
  </read_first>
  <behavior>
    - `attachmentOwnerRights(viewer, { ownerKind: "corp_card_usage", ownerId })`: 등록한 사람(보관 아님) → `{ read: true, write: true }` / 대리 등록 권한자 → write / 구매 완료로 생긴 건의 구매 완료한 사람 → write(`cardUsageRights` 그대로) / S8 범위 안이지만 수정 권리 없음(같은 팀 카드의 다른 직원) → read true · write false / 범위 밖 → read false · write false
    - 업로드 의도(`corp_card_usage`, 권리 있음) → `checkEvidenceUpload` → 서명 URL / 권리 없음 → 거부, 서명 URL 없음
    - 완료 통보 뒤 파일 행 1 → 목록 DTO 그 행 `hasEvidence: true` · 그 카드 사용의 결제 합계 · 공급가 · 부가세 · 연결 · `version` 그대로
    - E2E(트레이서): 카드 사용을 등록한 직원 → `/cards` 그 행 → `?editId=` → `증빙` 첨부 영역에 이미지 한 장(06-11 테스트 모드) → 파일 행에 파일 이름 · 3차 `삭제` → `/cards` 목록 그 행 증빙 열 `있음`(업로드 전 `—`)
  </behavior>
  <action>
⓪ 「선행 의존(A-6xx)」 재확인. 없으면 멈춘다. A-608-P(저장 전 첨부 규약)를 이 자리에서 읽어 ⒜/⒝ · 결합 함수 이름 · `tx` 인자 · 올린 사람 칸 이름을 SUMMARY에 적는다 — 조건을 못 채우면 코드를 한 줄도 바꾸지 않고 「저장 전 첨부 규약 공백」 두 선택지를 출력하고 멈춘다(Task 2가 기대는 규약이라 트레이서 전에 묻는다). 「규모 근거」의 재분할 방아쇠 넷(⑴ page.tsx 전달 · ⑵ 중복 조회 리포지토리 자리 · ⑶ 결합 함수의 주인 종류 · ⑷ 훅 호출 조건)도 여기서 본다. 결과(주인 칸 모양 · 업로드 · 삭제 함수 이름 · 첨부 컴포넌트 경로 · 중복 조회 리포지토리 경로 · 결합 함수 ⒞ · 훅 호출 줄과 조건)를 SUMMARY에 적는다.

TDD: 트레이서라 ④의 E2E와 통합 behavior 첫 줄을 먼저 쓰고 빨간 것(첨부 영역 없음 · 권리 갈래 없음)을 확인한 뒤 ①~③을 구현한다(RED 한 줄을 SUMMARY에).

① **마이그레이션(규약 B-2 — Phase 5 표, 06-16과 같은 절차).** 06-16이 고른 주인 칸 모양 그대로 한 갈래만: ⒜ `owner_kind` CHECK 값 목록에 `corp_card_usage`를 더한 새 CHECK로 바꾼다(옛 CHECK 삭제 + 새 CHECK `NOT VALID`) ⒝ `corp_card_usage_id` → `corp_card_usages` nullable FK를 `NOT VALID`로 더하고 「주인 하나」 CHECK를 새 칸 포함으로 바꾼다(`NOT VALID`) + 새 FK 칸 인덱스. `pnpm db:generate --name card_usage_evidence_owner` → 머리에 `SET LOCAL lock_timeout = '1s';` · `SET LOCAL statement_timeout = '5s';` → 제약 끝 `NOT VALID` + 0003처럼 이유 주석 → `pnpm db:generate --custom --name card_usage_evidence_owner_validate`에 SET LOCAL 한 쌍 + `VALIDATE CONSTRAINT` → 인덱스는 트랜잭션 안 일반 `CREATE INDEX` → `pnpm lint:sql` 0건 → `pnpm db:migrate` → 두 번째 generate `No schema changes`. 대기 파일(A-608-P ⒜)이 주인 없는 행이면 새 CHECK도 Phase 5의 대기 행 조건을 그대로 허용한다(06-16 CHECK를 옮겨 적고 값만 더한다).

② **domain · 리포지토리.** `domain/evidence-attachments/index.ts`의 `ATTACHMENT_OWNER_KINDS`에 `corp_card_usage`를 더하고 `attachmentOwnerRights`에 갈래를 둔다 — **트랜잭션 전에** 카드 사용 행을 잠금 없이 읽고, 쓰기 = 06-09 `cardUsageRights`의 수정 권리 + `archived_at` 없음, 읽기 = 06-05 목록 범위 조건과 같은 판정(리포지토리의 범위 함수를 부른다 — 새 판정을 쓰지 않는다). 트랜잭션 안(완료 통보 · 삭제)에서는 `tx`로 보관 아님만 다시 읽는다(06-03 tx 규약 — 권한 조회는 전역 `db`라 트랜잭션 안에서 부르지 않는다). **06-11 무효화 훅은 주인이 `expense`일 때만 불린다(r3 X-2)** — 카드 전표 업로드 · 완료 통보 · 삭제는 지출결의 행을 잠그지 않고 어느 지출결의의 version · 확인 기록도 바꾸지 않는다. 조건은 06-16이 Phase 5 증빙 함수에 세운 `ownerKind === "expense"` 긍정 조건이고 이 플랜은 그 파일을 고치지 않는다 — ⓪에서 조건이 없거나 부정 꼴이면 「규모 근거」 방아쇠 ⑷로 멈춘다. `repositories/corp-card-usages.ts` 목록 조회에 `hasEvidence`(그 카드 사용의 파일 행이 1개 이상 — 주인 칸 모양대로 `EXISTS` 한 조건)를 더하고, `domain/corp-card-usages/index.ts`의 목록 DTO에 `hasEvidence`, 수정 DTO에 전표 파일 목록(Phase 5 파일 행 DTO) · `evidenceWritable` · 설정 한도(`evidence.max_size_mb`)를 싣는다 — 파일 목록은 수정 DTO(수정 권리가 있어야 열린다)에만, 목록 DTO에는 불리언만(DTO 명세 갱신 — 누수 스캔은 06-05가 이미 등록).

③ **액션 · 화면(한 경로 — 수정 모드).** `app/(app)/cards/actions.ts`에 카드 전표 업로드 의도 · 완료 통보 · 삭제 액션 셋(Phase 5 업로드 액션과 같은 입력 모양 + `ownerKind` 고정 `corp_card_usage` — 06-16 `app/(app)/projects/actions.ts` 꼴)을 더하고 `actions.registry.ts`에 등록한다. 삭제는 파일 행에 저장된 주인으로 권리를 판정한다(요청 값이 아니다). `card-usage-form.tsx` 수정 모드: 와이어프레임대로 메모 아래 `증빙` 줄에 Phase 5 첨부 컴포넌트를 그 prop 규약대로 붙인다 — 서버가 보낸 `evidenceWritable`이면 업로드 · 삭제 prop을 넘기고 아니면 파일 행만. 첨부 · 삭제는 즉시 저장이라 폼의 `카드 사용 저장` · dirty 판정에 들지 않는다(「열린 선택」 수정 모드 첨부 시점). `card-usage-list.tsx` 증빙 열은 `hasEvidence`로 `있음`/`—`(06-05가 세운 열 · 색 없음 — 값만 바꾼다). 새 스타일 · 새 컴포넌트 없음, 토큰은 `tokens.css`의 것만.

④ **테스트.** 통합 `test/integration/card-usage-evidence.test.ts`(신규)에 `describe("카드 전표 주인 권리")`로 behavior 첫 세 줄, E2E `test/e2e/card-usage-evidence.spec.ts`(신규)에 `test("수정 모드 전표 첨부 → 목록 있음")`로 넷째 줄(06-11 테스트 모드 경로).
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm lint:sql &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:generate</automated>
    <fails_when>종료 코드가 0이 아니거나 squawk가 0이 아닌 `Found N issues`를 내거나 마지막 출력에 `No schema changes`가 없다</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/card-usage-evidence.test.ts test/integration/approval-reserve-evidence.test.ts test/integration/evidence-upload-intent.test.ts test/integration/migrate-runner.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — approval-reserve-evidence가 빨가면 06-16 두 주인을 깨뜨렸고, evidence-upload-intent가 빨가면 지출결의 주인 흐름을 깨뜨렸고, migrate-runner가 빨가면 `--custom` 마이그레이션이 러너 규약을 깨뜨렸다</fails_when>
    <automated>pnpm vitest run --project unit test/unit/leak-scan-coverage.test.ts</automated>
    <fails_when>종료 코드가 0이 아니다 — 새 카드 전표 액션이 registry에 등록되지 않았다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage-evidence.spec.ts test/e2e/card-usage.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — card-usage가 빨가면 06-05 목록 · 폼을 깨뜨렸다</fails_when>
  </verify>
  <acceptance_criteria>
    - ⓪ 결과(A-608-P ⒜/⒝ · 결합 함수 이름 · 올린 사람 칸 · 주인 칸 모양 · 중복 조회 자리 · 첨부 컴포넌트 경로)와 트레이서 RED 한 줄이 SUMMARY에 있다
    - `grep -c "NOT VALID" db/migrations/*card_usage_evidence_owner.sql`이 1 이상 · `grep -c "VALIDATE CONSTRAINT" db/migrations/*card_usage_evidence_owner_validate.sql`이 1 이상 · 두 파일 모두 `grep -c "SET LOCAL lock_timeout"`이 1
    - `grep -c "corp_card_usage" domain/evidence-attachments/index.ts`가 2 이상(종류 목록 + 권리 갈래)
    - 통합 `카드 전표 주인 권리`가 behavior 첫 세 줄을 개별 `it`으로 단언하고, 값 불변은 업로드 전후 카드 사용 행의 결제 합계 · 공급가 · 부가세 · 연결 · `version` 비교로 확인한다
    - E2E 트레이서가 업로드 전 증빙 열 `—` → 업로드 뒤 `있음`을 단언한다
  </acceptance_criteria>
  <done>카드 전표 한 장이 마이그레이션 → 권리 판정 → Phase 5 업로드 경로 → 수정 폼 파일 행 → 목록 증빙 열 `있음`까지 실제로 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 새 건 · 구매 완료(S13)에서 저장 전 전표 첨부 — 저장 트랜잭션에서 묶기 · 폰 사진</name>
  <files>domain/evidence-attachments/index.ts, domain/corp-card-usages/index.ts, domain/purchase-requests/index.ts, repositories/files.ts, app/(app)/cards/actions.ts, app/(app)/cards/purchases/actions.ts, app/(app)/cards/card-usage-form.tsx, test/integration/card-usage-evidence.test.ts, test/e2e/card-usage-evidence.spec.ts</files>
  <read_first>
    - Task 1 ⓪이 SUMMARY에 적은 A-608-P(저장 전 첨부 규약 ⒜ — 결합 함수 · 파일 경로 · 올린 사람 칸 · ⒞ 주인 종류 인자 유무)와 Phase 5 지출결의 폼의 그 호출 자리
    - domain/corp-card-usages/index.ts(`precheckCardUsage` · `createCardUsage`의 `runCreate` — 06-05 · 06-09 · 06-12) · domain/purchase-requests/index.ts(`completePurchaseRequest`의 `runComplete` — 06-12 · 06-14)
    - app/(app)/cards/actions.ts(`createCardUsageAction` zod 입력) · app/(app)/cards/purchases/actions.ts(`completePurchaseRequestAction` zod 입력) · app/(app)/cards/card-usage-form.tsx(새 건 · `mode = purchase`)
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S13. 구매 완료 — 카드 사용 폼의 구매 완료 모드」 절(「증빙 첨부 선택」) · 「S9 …」 절 「폰」 줄 · UI Considerations S7 loading 행 · S9 partial 행(「확인 — 입력 버리기」)
    - 06-05-PLAN.md · 06-12-PLAN.md acceptance의 `runCreate` · `runComplete` 범위 grep(06-03 tx 규약 — 이 태스크 뒤에도 0건이어야 한다)
  </read_first>
  <behavior>
    - 저장 전 업로드 권리: 새 건 — 폼 카드 옵션 1장 이상(06-05 · 06-09 옵션 판정 함수 결과) → 서명 URL / 0장 → 거부 · 구매 완료 — `cards.purchases` write + 요청 `신청` → 서명 URL / 권한 없음 · 요청 `취소`·`구매 완료` → 거부
    - 새 건 저장(`createCardUsage`, 대기 전표 참조 `evidenceRefs` 둘) → 한 트랜잭션에서 카드 사용 1건 + 그 건에 묶인 파일 행 2 / 참조 없음 → 지금처럼 저장(첨부는 선택)
    - 저장이 거부되면(예: 06-07 이중 연결 막힘) → 카드 사용 0건 · 대기 전표는 묶이지 않은 채 그대로
    - 남이 올린 대기 전표 참조를 섞어 보냄 → 저장 전체 거부 · 카드 사용 0건 · 어느 파일도 묶이지 않음
    - 구매 완료(`completePurchaseRequest`, `evidenceRefs` 하나) → 한 트랜잭션에서 카드 사용 1건(`registered_via = purchase`) + 파일 행 1 + 요청 `구매 완료`
    - E2E: 직원 `/cards?new=1` → 전표 이미지 첨부 → `카드 사용 등록` → 목록 새 행 증빙 `있음` / 구매 권한자 `/cards?new=1&purchase={id}` → 전표 첨부 → `구매 완료` → `/cards` 그 행 증빙 `있음` · 등록 칸 `구매 요청 {번호}` / 전표만 올리고 `취소 Esc` → 「확인 — 입력 버리기」 모달이 선다
    - E2E(폰, 폭 390): `/cards?new=1` 폼에 첨부 영역이 서고, 4000×3000 이미지를 올려 저장한 뒤 수정 폼 파일 행 `크게 보기`의 이미지 `naturalWidth` · `naturalHeight`가 둘 다 2000 이하다(S7 loading — 폰 사진 축소)
  </behavior>
  <action>
먼저 behavior를 통합 `describe("대기 전표 저장 결합")`와 E2E `test("새 건 · 구매 완료 전표 첨부")` · `test("폰 전표 사진 축소")`에 쓰고 빨간 것을 확인한 뒤 구현한다.

**저장 전 업로드 권리(`attachmentOwnerRights` — 주인 id 없음).** `corp_card_usage` 갈래에 주인 id가 없는 요청 두 꼴을 더한다: 새 건 = 폼 카드 옵션 판정 함수(06-05 · 06-09 — 새 판정을 쓰지 않는다)의 결과가 1장 이상, 구매 완료 = 요청 id를 받아 `cards.purchases` write + 요청 `신청`(06-12와 같은 판정 — 트랜잭션 전). 업로드 의도 · 완료 통보는 A-608-P ⒜ 규약대로 대기 전표를 만든다(주인 없음 · 올린 사람 = viewer). 이 두 꼴은 Task 1의 카드 전표 액션 셋이 입력 하나(`purchaseRequestId` 선택)로 받는다.

**저장 트랜잭션에서 묶기(06-03 tx 규약).** `createCardUsage`의 입력에 대기 전표 참조 `evidenceRefs`(선택, 배열)를 더하고 `runCreate` 끝에서 A-608-P 결합 함수를 `innerTx`로 부른다 — 주인은 `corp_card_usage` · 새 카드 사용 id이고, 결합 조건은 `올린 사람 = viewer` · `주인 없음`이며, 바뀐 행 수가 `evidenceRefs` 수와 다르면 던져 저장 전체를 롤백한다(남의 대기 전표 · 이미 묶인 전표를 막는 자리). **결합 함수의 주인 종류(r3 X-3):** ⓪의 ⒞이 「받음」이면 그대로 부른다. 「받지 않음」(지출결의 전용 — 예: `expense_id` 칸만 채움 · `owner_kind` 값을 `expense`로 고정)이면 Phase 5 파일 리포지토리(`repositories/files.ts` 자리)의 그 함수 입력에 주인 종류 칸 하나(`ATTACHMENT_OWNER_KINDS` 타입 — 06-16이 업로드 의도 · 완료 통보 · 삭제를 넓힌 꼴)를 더하고, 파일 표 모양대로 ⒜ `owner_kind` 값 / ⒝ 그 종류의 FK 칸을 채운다. 기존 지출결의 호출자는 고치지 않는다 — 그 칸이 없으면 `expense`로 읽는다(주인 종류 기본값 한 곳, 파일 한도 15 안). 함수가 리포지토리 밖에 있으면 「규모 근거」 방아쇠 ⑶(이미 ⓪에서 멈췄다). **대기 결합은 06-11 무효화 훅을 부르지 않는다(r3 X-2)** — 카드 사용 저장 · 구매 완료 트랜잭션은 지출결의 행을 잠그지 않고(06-03 N-3 전역 잠금 순서 그대로) 어느 지출결의의 version도 바꾸지 않는다. `runCreate` 안에 권한 · 설정 · 세율 조회를 새로 넣지 않는다 — 06-05 · 06-12 acceptance의 범위 grep이 이 태스크 뒤에도 0건이다. `completePurchaseRequest`는 입력에 `evidenceRefs`를 받아 `createCardUsage` 외부 tx 경로로 그대로 넘긴다(06-12가 입력을 칸별로 골라 넘기므로 한 칸을 더한다). `createCardUsageAction` · `completePurchaseRequestAction`의 zod 입력에 `evidenceRefs`(참조 id 배열, 길이 상한 = Phase 5 폼 규약)를 더한다 — 금액 칸은 여전히 없다.

**폼.** `card-usage-form.tsx` 새 건 · `mode = purchase`: 메모 아래 `증빙` 줄에 같은 첨부 컴포넌트를 대기 규약 prop으로 붙이고, 올린 대기 전표 참조를 1차 제출 입력 `evidenceRefs`에 싣는다. 대기 전표가 있으면 `취소 Esc`는 입력이 있는 것으로 본다(Copywriting 「확인 — 입력 버리기」 — 기존 판정에 조건 하나). 폰(<700)에서도 폼과 첨부 영역이 그대로 선다(S9 「폰」 · UX-03) — 축소 · 해시는 첨부 컴포넌트의 것이다. 새 문구 없음.

**E2E 큰 이미지.** 브라우저 안 canvas로 4000×3000 PNG를 만들어 base64로 받아 `setInputFiles`에 버퍼로 넘긴다(새 픽스처 파일 · 의존성 없음). 구매 완료 데이터는 06-12 E2E 준비 꼴(구매 권한자 · `신청` 요청)을 쓴다.
  </action>
  <verify>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/card-usage-evidence.test.ts test/integration/purchase-requests.test.ts test/integration/corp-card-usages.test.ts test/integration/corp-card-usages-proxy.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이거나 "No test files found"가 나온다 — purchase-requests가 빨가면(「구매 완료 동시 6건」 포함) 구매 완료 트랜잭션을 깨뜨렸고, corp-card-usages가 빨가면 06-07 · 06-09 등록 · 수정 흐름을 깨뜨렸다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage-evidence.spec.ts test/e2e/purchase-requests.spec.ts test/e2e/card-proxy.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다</fails_when>
    <automated>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
    <fails_when>하나라도 error로 끝난다</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 `대기 전표 저장 결합`이 behavior 도메인 줄 다섯(저장 전 업로드 권리 두 꼴 · 새 건 결합 · 거부 시 미결합 · 남의 대기 전표 섞임 거부 · 구매 완료 결합)을 개별 `it`으로 단언하고, 거부 케이스는 카드 사용 행 수 0과 대기 전표의 주인이 여전히 없음을 확인한다
    - (06-03 tx 규약) `awk '/const runCreate = async/,/^  };$/' domain/corp-card-usages/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\(|teamAtDate\('`가 0 · `awk '/const runComplete = async/,/^  };$/' domain/purchase-requests/index.ts | grep -cE '\bcan\(|getSettingValue\(|loadTaxRates\(|precheckCardUsage\(|recordAction\('`가 0
    - `grep -c "evidenceRefs" domain/purchase-requests/index.ts`가 1 이상 · `grep -c "evidenceRefs" 'app/(app)/cards/purchases/actions.ts'`가 1 이상
    - [r3 X-3] 결합 함수 ⒞(주인 종류 인자 유무)와, 더했다면 더한 칸 이름이 SUMMARY에 있다 · 기존 지출결의 호출자 파일(⓪이 적은 경로)의 `git diff --stat "$(git merge-base origin/main HEAD)" -- {그 파일}` 출력이 비어 있다(커밋 뒤에도 잡힌다 — 이 플랜이 호출자를 고치지 않았다) · 통합 「새 건 결합」 `it`이 묶인 파일 행의 주인이 `corp_card_usage` · 그 카드 사용 id임을 단언한다(지출결의 주인으로 묶이지 않음)
    - E2E `새 건 · 구매 완료 전표 첨부` · `폰 전표 사진 축소`가 CI=true로 녹색(폰 케이스는 `naturalWidth` · `naturalHeight` ≤ 2000 단언 포함)
  </acceptance_criteria>
  <done>새 건 · 구매 완료에서 올린 전표가 저장과 한 트랜잭션으로 묶이고, 실패 · 남의 전표는 하나도 묶이지 않으며, 폰에서도 줄인 사진으로 붙는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 규칙 마감 — 중복 한 종류(카드 전표 ↔ 지출결의) · 번호 노출 · 보관 · 권리 밖 직접 호출 · 마지막 전표 삭제 모달 없음 · 값 불변</name>
  <files>domain/evidence-reviews/upload-checks.ts, domain/evidence-attachments/index.ts, domain/corp-card-usages/index.ts, repositories/corp-card-usages.ts, repositories/files.ts, test/integration/card-usage-evidence.test.ts, test/e2e/card-usage-evidence.spec.ts</files>
  <read_first>
    - domain/evidence-reviews/upload-checks.ts(06-11 — `ownerKinds`를 넘기는 자리 · 번호 노출 `scopeFor` · `lastEvidenceDeleteNeedsConfirm`) · 06-11 SUMMARY의 중복 조회 리포지토리 경로(이 플랜의 `repositories/files.ts` 자리 — ⒜/⒝ 조건) · test/unit/domain/evidence-upload-checks.test.ts(06-11 — 「조회 가짜의 인자 단언」, 포함 단언) · test/unit/import-cycles.test.ts
    - .planning/phases/06-payment-evidence-cards/06-UI-SPEC.md 「S7. 증빙 첨부 규칙」 「중복(SHA-256) — 같은 주인 종류 안에서만」 문단(카드 전표 = 지출결의 한 종류 · 번호 노출) · 열린 선택 O-6 · Copywriting 「Error — 증빙 업로드(S7 · S21 · S22, §7-10 행 오류 모양)」 · UI Considerations S7 zero-one-many 행
    - domain/corp-card-usages/index.ts(06-09 `deleteCardUsage` · `restoreCardUsage` — 보관 · 보관 해제)
    - test/integration/approval-reserve-evidence.test.ts(06-16 `[DR-3]` 케이스 — 지출결의 증빙으로 먼저 붙인 같은 파일이 차수 승인 · 리저브에는 통과)
  </read_first>
  <behavior>
    - `duplicateScopeKinds`: `expense` → [`expense`, `corp_card_usage`] / `corp_card_usage` → [`expense`, `corp_card_usage`] / `quote_revision` → [`quote_revision`] / `reserve_entry` → [`reserve_entry`]
    - 같은 SHA-256이 지출결의 증빙에 있음 → 카드 전표 업로드 의도 거부(중복) — 올린 사람이 그 지출결의를 읽을 수 있으면 응답에 번호, 없으면 번호 문자열 없음
    - 같은 SHA-256이 다른 카드 사용의 전표에 있음 → 거부, 번호 없음 · 같은 카드 사용 → `같은 파일이 이미 첨부됨` 갈래
    - 같은 SHA-256이 카드 전표에 있음 → 지출결의 증빙 업로드 의도 거부, 번호 없음
    - 같은 SHA-256이 차수 승인 증빙 · 리저브 줄 증빙에만 있음 → 카드 전표 업로드 통과(06-16 `[DR-3]` 케이스도 그대로 녹색)
    - 전표가 있는 카드 사용을 보관(삭제) → 같은 SHA-256으로 새 카드 사용에 업로드 통과 / 보관된 건에 업로드 · 삭제 직접 호출 → 거부 / 되돌리기(보관 해제) → 파일 그대로 · 목록 `있음`
    - 권리 없는 계정(S8 범위 안 · 수정 권리 없음 / 범위 밖)이 카드 전표 액션을 직접 부름 → 거부 · 범위 밖 계정의 응답에 파일 목록 키 없음
    - 전표 첨부 · 삭제 전후 카드 사용의 결제 합계 · 공급가 · 부가세 · 연결 · `version` 같음 · 첨부 뒤 같은 폼 저장(`updateCardUsage`, 첨부 전에 받은 `version`) → 성공
    - [r3 X-2] 「카드 전표 첨부 · 삭제 · 대기 결합 전후 모든 지출결의 version 불변」: 결재 통과 · 결재 중 지출결의(증빙 · 확인 기록 있음)를 둔 채 카드 전표 첨부 · 삭제 · 새 건 저장(대기 결합) → 모든 지출결의의 `version` · 확인 기록 수 · 확인 상태 같음
    - E2E: 전표 1장인 카드 사용 수정 폼 → 파일 행 3차 `삭제` → 확인 모달 없음(`role=dialog` 0개) → 파일 행 0 → 목록 증빙 `—` / 담당 지출결의에 증빙이 있는 PM이 같은 파일을 카드 폼에 올림 → 파일 행 자리 `같은 파일이 {번호} 증빙에 있음 · 다른 파일 고르기`
  </behavior>
  <action>
먼저 통합 `describe("카드 전표 중복 — 지출결의와 한 종류")` · `describe("보관된 카드 사용 전표")` · `describe("카드 전표는 값을 바꾸지 않는다")`와 E2E `test("마지막 전표 삭제 모달 없음")` · `test("카드 폼 중복 번호")`를 쓰고, 중복 묶음을 넣기 전에 「지출결의 증빙과 같은 파일 → 카드 전표 거부」가 빨간 것을 확인한다(RED 한 줄을 SUMMARY에).

**중복 묶음(DR-3 · S7 — 06-11이 넘긴 자리).** `domain/evidence-reviews/upload-checks.ts`에 `duplicateScopeKinds(ownerKind)`를 두고(behavior 첫 줄 — 카드 전표와 지출결의를 한 묶음, 나머지는 자기 종류만), `checkEvidenceUpload`가 조회에 넘기는 `ownerKinds`를 06-11의 `[ownerKind]`에서 `duplicateScopeKinds(ownerKind)`로 바꾼다(r3 X-4). 리포지토리(`repositories/files.ts` 자리 — 06-11 SUMMARY의 실제 경로)는 목록만 받아 ⒜ `owner_kind IN (목록)` / ⒝ 목록의 FK 칸 중 하나라도 `IS NOT NULL` 조건을 그대로 걸고 **domain을 import하지 않는다** — 리포지토리가 `duplicateScopeKinds`를 부르면 upload-checks ↔ 리포지토리 순환 import가 되어 `test/unit/import-cycles.test.ts`가 빨갛다. ⒝이면 리포지토리의 종류 → FK 칸 대응에 `corp_card_usage` → `corp_card_usage_id` 한 줄을 더한다. 보관된 카드 사용의 파일은 조회에서 뺀다(`corp_card_usages.archived_at` 없음 조건 — 카드 사용 주인일 때만, 리포지토리 조건 하나). 06-11 단위 테스트 「조회 가짜의 인자 단언」은 넘긴 `ownerKinds`에 요청 `ownerKind`가 들어 있음을 보는 포함 단언이라 그대로 녹색이다. 번호 노출은 06-11 규칙 그대로(기존 주인이 지출결의 · `scopeFor` 읽기 가능일 때만) — 카드 사용 주인은 번호 없는 문구다. 함수 선언 위 한 줄 주석에 「O-6 답이 바뀌면 이 함수만 고친다」를 적는다.

**권리 마감.** `attachmentOwnerRights` `corp_card_usage` 갈래가 보관된 건을 쓰기 false · 읽기는 범위대로로 두는지, 삭제 액션이 파일 행에 저장된 주인으로 판정하는지 behavior대로 마감한다. 되돌리기(06-09 `restoreCardUsage`)는 고치지 않는다 — 파일은 주인 행과 함께 남으므로 통합 테스트로 그것만 고정한다. `repositories/corp-card-usages.ts` · `domain/corp-card-usages/index.ts`의 `hasEvidence` · 수정 DTO 파일 목록이 보관 해제 뒤 그대로 서는지 확인한다(이미 Task 1 조건이면 코드 변경 없음 — 테스트만).

**마지막 전표 삭제.** 첨부 컴포넌트의 마지막 삭제 판정은 06-11 `lastEvidenceDeleteNeedsConfirm`(`corp_card_usage` → false)을 그대로 쓴다 — 카드 폼에서 모달을 새로 막는 코드를 쓰지 않는다. E2E가 모달 0개를 단언한다.

**지출결의 불변(r3 X-2).** 통합 `describe("카드 전표는 값을 바꾸지 않는다")`에 `it` 「카드 전표 첨부 · 삭제 · 대기 결합 전후 모든 지출결의 version 불변」(behavior `[r3 X-2]` 줄)을 더한다. **RED 확인:** Phase 5 증빙 함수의 훅 조건(06-16이 세운 `ownerKind === "expense"`)을 잠시 빼서 이 `it`이 빨간 것을 보고 되돌린다(출력 한 줄을 SUMMARY에, 임시 변경은 `git diff`에 남기지 않는다 — 06-04 「잠금 뒤 게이트 재판정」 RED 확인과 같은 꼴).
  </action>
  <verify>
    <automated>pnpm vitest run --project unit test/unit/domain/evidence-upload-checks.test.ts test/unit/import-cycles.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — 「조회 가짜의 인자 단언」이 빨가면 넘기는 `ownerKinds`에서 요청 `ownerKind`가 빠졌고, import-cycles가 빨가면 중복 조회 리포지토리가 domain(`duplicateScopeKinds`)을 import했다(r3 X-4)</fails_when>
    <automated>pnpm db:dev &amp;&amp; pnpm vitest run --project integration test/integration/card-usage-evidence.test.ts test/integration/evidence-upload-intent.test.ts test/integration/approval-reserve-evidence.test.ts test/integration/leak-scan.test.ts</automated>
    <fails_when>종료 코드가 0이 아니거나 failed가 1 이상이다 — approval-reserve-evidence `[DR-3]`가 빨가면 중복 묶음이 차수 승인 · 리저브까지 번졌다</fails_when>
    <automated>pnpm db:reset:test &amp;&amp; CI=true pnpm exec playwright test test/e2e/card-usage-evidence.spec.ts test/e2e/evidence-lifecycle.spec.ts</automated>
    <fails_when>failed가 1 이상이거나 "No tests found"가 나온다 — evidence-lifecycle가 빨가면 지출결의 마지막 증빙 삭제 모달(SP-6)을 깨뜨렸다</fails_when>
    <automated>pnpm test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; git diff --stat "$(git merge-base origin/main HEAD)" -- package.json pnpm-lock.yaml docs/design/tokens.css</automated>
    <fails_when>앞의 명령 중 하나라도 실패하거나 마지막 diff 출력이 비어 있지 않다(새 의존성 · 새 토큰이 들어왔다)</fails_when>
  </verify>
  <acceptance_criteria>
    - 통합 테스트가 behavior 도메인 줄 아홉(r3 X-2 줄 포함)을 개별 `it`으로 단언하고, 번호 노출은 읽기 권한 없는 계정의 응답 본문에 기존 문서 번호 문자열이 없음으로, 노출은 범위 밖 계정 응답 객체의 파일 목록 키 부재로 확인한다
    - 중복 묶음 RED(묶음 전 「지출결의 증빙과 같은 파일 → 카드 전표 거부」 빨강) 한 줄이 SUMMARY에 있다
    - `grep -c "duplicateScopeKinds" domain/evidence-reviews/upload-checks.ts`가 2 이상(선언 + 조회 사용)
    - [r3 X-4] 중복 조회 리포지토리 파일의 import 줄에 evidence-reviews가 없다 — `grep -cE '^import .*domain/evidence-reviews' {그 파일}`이 0(import 줄만 센다 — 주석은 걸리지 않는다. 리포지토리 관례인 `@/domain/viewer` 타입 import는 괜찮다) · `test/unit/import-cycles.test.ts` 녹색
    - [r3 X-2] `it` 「카드 전표 첨부 · 삭제 · 대기 결합 전후 모든 지출결의 version 불변」이 녹색이고 RED 확인 한 줄이 SUMMARY에 있다
    - E2E `마지막 전표 삭제 모달 없음`이 `role=dialog` 0개와 삭제 뒤 목록 증빙 `—`를, `카드 폼 중복 번호`가 문구 `같은 파일이 {번호} 증빙에 있음 · 다른 파일 고르기`를 단언한다
    - merge-base 기준 package.json · pnpm-lock.yaml · tokens.css diff 0
  </acceptance_criteria>
  <done>카드 전표가 지출결의 증빙과 한 종류로 중복을 막고, 번호 · 파일 목록이 읽기 범위 밖에 새지 않으며, 보관 · 삭제 · 되돌리기가 카드 사용 값과 무관하게 계약대로 선다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 카드 전표 액션 | `ownerKind`는 액션마다 고정, `ownerId` · 요청 id · 파일 메타(크기 · MIME · SHA-256 참고 값)가 넘어온다. 권리 · 카드 사용 값은 넘어와도 믿지 않는다 |
| 브라우저 → 카드 사용 저장 · 구매 완료 | 대기 전표 참조 `evidenceRefs`가 넘어온다 — 올린 사람 · 주인 없음 조건으로만 묶인다 |
| 서버 → 브라우저(목록 · 수정 DTO) | 목록은 `hasEvidence` 불리언만, 파일 목록은 수정 DTO에만 |
| 마이그레이션 → Phase 5 파일 표 | 기존 표 제약 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-194 | Elevation of Privilege | 카드 전표 업로드 · 삭제 액션 직접 호출(권리 밖 · 보관된 건) | high | mitigate | `attachmentOwnerRights`가 카드 사용 행을 서버에서 다시 읽어 `cardUsageRights` 수정 권리 + 보관 아님으로 판정, 트랜잭션 안에서 보관 아님을 `tx`로 다시 확인. 삭제는 파일 행에 저장된 주인으로 판정. 권리 밖 · 보관 건 직접 호출 거부 통합 테스트 |
| T-06-195 | Information Disclosure | 카드 전표 파일 목록 · 내려받기 URL | high | mitigate | 파일 목록은 수정 DTO(수정 권리가 있어야 열림)에만 싣고 읽기 판정 = S8 목록 범위. 목록 DTO는 불리언만(DTO 명세 갱신 → 06-05 누수 스캔 등록). 범위 밖 계정 응답 키 부재 단언 |
| T-06-196 | Information Disclosure | 중복 응답의 문서 번호(해시로 남의 문서 캐기) | high | mitigate | 06-11 규칙 그대로 — 기존 주인이 지출결의이고 `scopeFor` 읽기 가능일 때만 번호, 카드 사용 주인은 늘 번호 없음. 읽기 권한 없는 계정 응답 본문에 번호 문자열 없음 통합 테스트 |
| T-06-197 | Tampering | 남이 올린 대기 전표를 자기 카드 사용에 묶기 | high | mitigate | 결합은 `올린 사람 = viewer` · `주인 없음` 조건으로만, 바뀐 행 수 ≠ 참조 수면 던져 저장 전체 롤백. 섞인 참조 거부 통합 테스트(카드 사용 0 · 대기 전표 주인 없음) |
| T-06-198 | Tampering | `ownerKind` · `ownerId` · 요청 id 바꿔치기 | medium | mitigate | 액션마다 `ownerKind` 고정 `corp_card_usage`, 주인 · 요청 행을 서버가 다시 읽는다, 구매 완료 저장 전 업로드는 요청 `신청` + `cards.purchases` write |
| T-06-199 | Denial of Service | Phase 5 파일 표 제약 변경 마이그레이션 | medium | mitigate | 규약 B-2: SET LOCAL 한 쌍 · 제약 `NOT VALID` · `--custom` VALIDATE · 일반 CREATE INDEX · `pnpm lint:sql` 0건 · 두 번째 generate `No schema changes` |
| T-06-200 | Denial of Service | 카드 사용 · 구매 완료 트랜잭션 안 전역 `db` 조회(풀 교착) | high | mitigate | 06-03 tx 규약 — 권리 · 옵션 · 권한 판정은 트랜잭션 전, `runCreate` · `runComplete` 안에는 `tx` 결합만. 06-05 · 06-12 범위 grep 0건 재확인 · 06-12 「구매 완료 동시 6건」 재실행(Task 2 verify) |
| T-06-201 | Tampering | 되돌리기(보관 해제) 뒤 같은 전표가 두 주인에 남음 | low | accept | 보관 해제는 중복을 다시 보지 않는다 — 토스트 수명 안의 되돌리기라 그사이 같은 전표가 다른 건에 붙는 경우가 드물고, 붙어도 파일 중복일 뿐 금액 판정이 해시에 기대지 않는다(06-11 R-5). 토스트 수명 규칙이 바뀌면(교차 R-1) 다시 본다 |
| T-06-236 | Denial of Service | 카드 전표 경로가 06-11 훅을 타 지출결의 행을 잠그고 version을 올림(r3 X-2 — 무관한 지출결의 저장 · 결재가 동시성으로 거부, 잠금 순서 N-3 어긋남) | medium | mitigate | 훅 호출은 `ownerKind === "expense"` 긍정 조건 안에만(06-16이 세움 — ⓪이 확인, 없으면 방아쇠 ⑷), 대기 결합은 훅을 부르지 않는다. 통합 「카드 전표 첨부 · 삭제 · 대기 결합 전후 모든 지출결의 version 불변」(RED 확인) |
| T-06-202 | Tampering | 업로드 크기 · 형식 · SHA-256(브라우저 값) | low | mitigate | 06-11 `checkEvidenceUpload`를 서명 URL 발급 앞에서 그대로 부르고, 실제 크기 · 형식은 서명 URL 조건 + Phase 5 완료 통보의 메타데이터 재확인이 강제 |
</threat_model>

<verification>
- 통합 card-usage-evidence · approval-reserve-evidence · evidence-upload-intent · purchase-requests · corp-card-usages · corp-card-usages-proxy · migrate-runner · leak-scan, 단위 evidence-upload-checks · import-cycles · leak-scan-coverage 녹색
- `pnpm lint:sql` 0건, 두 번째 `pnpm db:generate` `No schema changes`
- `runCreate` · `runComplete` 범위에 전역 `db` 판정 · 조회 호출 0건(06-03 tx 규약 — Task 2 acceptance)
- E2E card-usage-evidence · card-usage · purchase-requests · card-proxy · evidence-lifecycle가 CI=true로 녹색, `pnpm lint` · `pnpm typecheck` · `pnpm build` 통과, merge-base 기준 package.json · pnpm-lock.yaml · tokens.css diff 0
- **화면 검증 순서(CLAUDE.md §6):** 싼 게이트 → 실행자가 아닌 별도 에이전트가 `CI=true`로 UI-SPEC S7(카드 폼 첨부 영역 · 파일 행 · 오류 넷) · S8(증빙 열 `있음`/`—` · 폰 행 시트 P3) · S9(세 모드의 `증빙` 줄 · 폭 1280 · 390) · S13(구매 완료 폼 첨부) DOM 실측 감사(backstop 2건 · UI-SPEC 밖 새 시각 패턴 부재 포함) → 수정 → 전체 게이트 한 번
</verification>

<success_criteria>
- EVID-01의 카드 몫(「법인카드 건에 첨부」, 폰 사진 포함)이 이 페이즈 안에서 끝나고, UI-SPEC S7 · S8 · S9 · S13이 약속한 카드 전표 표면에 주인 플랜이 생긴다(교차 B-2 해소)
- 카드 전표가 Phase 5 업로드 경로 · 06-11 검사를 그대로 지나고, 중복에서 지출결의 증빙과 한 종류로 센다(DR-3)
- 새 건 · 구매 완료의 전표가 저장과 한 트랜잭션으로 묶이고, 전표가 카드 사용 값을 바꾸지 않는다
- 기존 표 마이그레이션이 규약 B-2대로 들어간다
</success_criteria>

## Artifacts this phase produces

| 종류 | 이름 / 경로 |
|---|---|
| 마이그레이션 | `NNNN_card_usage_evidence_owner.sql`(SET LOCAL · 제약 NOT VALID) · `NNNN_card_usage_evidence_owner_validate.sql`(`--custom`, VALIDATE) |
| 함수(domain) | `duplicateScopeKinds`(중복 주인 종류 묶음) · `attachmentOwnerRights` `corp_card_usage` 갈래(수정 권리 · 보관 · 목록 범위 · 저장 전 업로드 두 꼴) · `ATTACHMENT_OWNER_KINDS`에 `corp_card_usage` |
| 변경된 기존 심볼 | `createCardUsage`(`evidenceRefs` · `runCreate` 끝 결합) · `completePurchaseRequest`(`evidenceRefs` 전달) · `checkEvidenceUpload`가 넘기는 `ownerKinds`(= `duplicateScopeKinds(ownerKind)`) · Phase 5 파일 리포지토리의 중복 조회(보관 카드 사용 제외 · ⒝면 FK 칸 대응 한 줄 — 목록만 받는다) · (⒞ 「받지 않음」이면) Phase 5 대기 결합 함수의 주인 종류 칸 · 카드 사용 목록 DTO `hasEvidence` · 수정 DTO 전표 파일 목록 · `evidenceWritable` · 설정 한도 |
| Server Action | 카드 전표 업로드 의도 · 완료 통보 · 삭제(`ownerKind` 고정 `corp_card_usage`) · `createCardUsageAction` · `completePurchaseRequestAction` 입력 `evidenceRefs` |
| 화면 | UI-SPEC S9 — 카드 폼 새 건 · 수정 · 구매 완료(S13) 세 모드의 `증빙` 첨부 영역(폰 포함) · UI-SPEC S8 — 목록 증빙 열 `있음`/`—` 실제 값 |
| 테스트 | `test/integration/card-usage-evidence.test.ts` · `test/e2e/card-usage-evidence.spec.ts` |

## Review Dispositions Ledger

### Round 1 — e2c8922(디자인 검토 반영 r2 — 미커밋 1회차 반영분 점검 `design-apply-checker-r1.md` · 교차 `design-apply-cross-r1.md`)

> 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 교차 B-2 — 카드 전표 첨부(EVID-01 「법인카드 건에 첨부」)를 짓는 플랜이 없다 — UI-SPEC S7(카드 전표 = 지출결의 한 종류 · 카드 폼 · 구매 완료 폼 첨부 · 마지막 전표 삭제 모달 없음) · S8 `증빙` 열 · S9 `증빙` 첨부 영역 · S13 「증빙 첨부 선택」이 주인 없이 남았다(design-apply-cross-r1.md 「막음」 B-2 · 권고 ①) | HIGH(막음) | 새 플랜 06-25(오케스트레이터 결정 — 범위 밖으로 빼지 않음). Task 1(트레이서 — 주인 종류 `corp_card_usage` 마이그레이션 · 권리 · 수정 모드 첨부 · 목록 `있음`) · Task 2(새 건 · 구매 완료 저장 트랜잭션 결합 · 폰 사진) · Task 3(중복 한 종류 · 번호 노출 · 보관 · 권리 밖 호출 · 마지막 전표 삭제 모달 없음 · 값 불변). must_haves에 UI-SPEC S7 · S8 · S9 · S13 해당 UI Considerations 행 lift. 06-11 Round 1 · 06-16 Round 2의 보류 줄(「카드 전표 첨부 저장의 소유 플랜 없음」)을 이 플랜이 넘겨받는다 — 두 플랜의 줄 고침(「→ 06-25」)은 R1 몫 |
| 체커 참고 1 — 카드 전표 첨부 S7 · S8 · S9 · S13 주인 없음(design-apply-checker-r1.md 참고 1 · yaml `requirement_coverage` info) | 참고 | 위 줄과 같은 반영. frontmatter `requirements: [EVID-01]` |
| (06-11 action 이관) 카드 전표를 지출결의와 한 종류로 세는 중복 조건은 카드 전표가 파일 표 주인이 될 때 그 플랜이 더한다 | MEDIUM | Plan 25, Task 3: `duplicateScopeKinds` · 06-11 중복 조회 범위 조건을 묶음으로(인자 모양 그대로 — 06-11 단위 테스트 녹색 유지) · 보관된 카드 사용 전표 제외 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 교차 B-2 권고 ① 끝 — REQUIREMENTS 추적표 EVID-01 줄을 「Phase 5(지출결의) + Phase 6(카드 사용)」으로 나누고 ROADMAP Phase 6 계획 목록 · 계획 수(24 → 25)를 고친다 | 이 플랜 `files_modified` 밖 — 계획 단계의 `.planning/` 상태 파일(ROADMAP · REQUIREMENTS)은 오케스트레이터가 계획 커밋 때 `gsd_run`으로 고친다(이번 라운드 플래너 지시 「ROADMAP · STATE 편집 금지」). objective 「요구사항 표기」에 적었다 |
| 교차 R-1 — 카드 사용 삭제의 되돌리기 창이 토스트 수명뿐이다 | 그대로 둔다(오케스트레이터 판단 — 06-09 소관 · ledger 기록 유지). 이 플랜의 「되돌리기 뒤 중복」 수용(T-06-201)은 그 규칙이 바뀌면 다시 본다 |

### Round 2 — 디자인 검토 반영 r3(교차 r2 · design-apply-cross-r2.md)

> 지적 원본은 `.planning/phases/06-payment-evidence-cards/design-apply-cross-r2.md`(저장소 안 사본 — r1 보고서는 저장소 밖이라 잃었다). 디자인 교차 검토는 Codex 대신 Opus(한도 2026-09-29까지) — 한도 풀리면 Codex 재확인 필요

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| X-2 — 카드 전표 업로드 · 삭제 · 대기 결합이 06-11 훅(지출결의 행 `FOR UPDATE` + version 증가)을 타지 않는다는 조건과 테스트가 없다(design-apply-cross-r2.md X-2) | 고침 | Plan 25: must_haves.truths `[X-2 r3]` 줄 · prohibitions 「지출결의 행 잠금 · version · 확인 기록」 · Task 1 action ② 「06-11 무효화 훅은 주인이 `expense`일 때만」 · Task 2 「저장 트랜잭션에서 묶기」 끝 문장 · Task 3 behavior `[r3 X-2]` `it` 「카드 전표 첨부 · 삭제 · 대기 결합 전후 모든 지출결의 version 불변」(RED 확인) · acceptance(아홉 줄 · `[r3 X-2]`) · T-06-236. 조건 코드 자체는 `domain/expenses/evidence.ts`를 이미 가진 06-16이 긍정 꼴로 세우고(06-16 Round 4) 이 플랜 ⓪이 읽어 확인한다 — 없거나 부정 꼴이면 「규모 근거」 방아쇠 ⑷(16번째 파일)로 멈춘다. 수정안의 「Phase 5 호출 자리에 조건 하나를 더한다」와 같은 성질을 파일 한도 15 안에서 얻는 길이다 |
| X-3 — A-608-P 재확인이 결합 함수가 주인 종류를 받는지 보지 않는다(design-apply-cross-r2.md X-3) | 고침 | 「선행 의존」 A-608-P 줄에 ⒞(주인 종류 인자 유무 · 함수 파일 경로). 「받지 않음」이면 Task 2가 Phase 5 파일 리포지토리(`repositories/files.ts` 자리 — X-4로 files_modified에 들어옴)의 그 함수 입력에 주인 종류 칸 하나를 더한다(기존 지출결의 호출자는 그대로 — 칸이 없으면 `expense`). 함수가 리포지토리 밖이면 방아쇠 ⑶. Task 2 files · read_first · action · acceptance `[r3 X-3]` · Artifacts |
| X-4 — 중복 조회 리포지토리가 files_modified에 없고, 「인자 모양 `ownerKind` 그대로」면 리포지토리가 `duplicateScopeKinds`를 import해 순환이 생긴다. 파일 수 방아쇠가 06-16(16번째)과 달리 15번째였다(design-apply-cross-r2.md X-4) | 고침 | Supersedes Round 1 Addressed 「(06-11 action 이관) … 인자 모양 그대로」: files_modified에 `repositories/files.ts`(가칭 — 06-11 SUMMARY의 실제 경로, 15개). Task 3 action: `checkEvidenceUpload`가 `duplicateScopeKinds(ownerKind)`를 `ownerKinds`로 넘기고 리포지토리는 목록만 받는다(domain import 없음 — 06-11 Round 3과 한 짝). 06-11 단위 「조회 가짜의 인자 단언」은 포함 단언이라 녹색. Task 3 verify에 `import-cycles` · acceptance `[r3 X-4]`(import 줄 grep) · key_link `ownerKinds` · artifacts provides. 「규모 근거」를 06-16과 한 규칙(15개 보고 · 16번째에서 멈춤 — `.claude/agents/gsd-plan-checker.md` Thresholds 「Files/plan … 15+」, `verify.plan-structure`에는 파일 수 규칙 없음)으로 고치고 방아쇠를 넷으로 적었다 |
| X-7 — `<assumption_delta_decision>`이 「지출결의 동작은 바꾸지 않는다」라 적어 must_haves · Task 3 behavior(지출결의 증빙도 카드 전표와 같은 파일이면 막힘)와 어긋난다(design-apply-cross-r2.md X-7) | 참고 | 블록을 고쳐 「지출결의 주인은 중복 판정 범위만 넓어진다 — 같은 파일이 카드 전표에 있으면 지출결의 증빙 업로드도 막힌다(DR-3 · S7 정본)」로 적었다. 수정안의 `detected: true`는 쓰지 않았다 — `detected`는 assumption-delta 검출기의 결과 값이고(플래너가 판정을 바꾸는 칸이 아니다) 주인 식별 모델(종류 + id)이 그대로라 결정은 `no-change`다. 규칙 확장은 문장으로 드러냈다 |
| X-8 — ROADMAP Phase 6 목록에 06-25가 없고 계획 수 24, REQUIREMENTS EVID-01 추적표가 「Phase 5」뿐이다(design-apply-cross-r2.md X-8) | 참고 | Supersedes Round 1 Deferred 첫 줄(「오케스트레이터가 계획 커밋 때 `gsd_run`으로」): ROADMAP은 r3 계획 반영 때 플래너 `update_roadmap` 단계가 Phase 6 절만 고쳤다(`**Plans**: 25 plans` · Wave 10 줄). REQUIREMENTS 추적표 EVID-01 줄은 06-01 Task 3(REQ-ROUTE 체크포인트 — 사용자가 고른 경로)으로 보냈다(06-01 Round 2). objective 「요구사항 표기」를 06-01 Task 3으로 가리키게 고쳤다 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

### Round 3 — 체커 r3 경고 반영(r3-2)

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 체커 r3 scope_sanity 파생 — 파일 수 근거가 도구 경고 유무에 기대면 안 된다(06-11:127과 같은 결) | 참고 | 「규모 근거」의 「`verify.plan-structure`에는 파일 수 규칙이 없다」를 지우고 체커 Files/plan 표 인용 + 15개의 구성(테스트 2 · 스키마 · 저널 2 · 등록 1 · 실제 코드 10)과 태스크별 11 · 9 · 7개로 바꿨다. 규칙 · 방아쇠 넷은 그대로 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (없음) | — |

<output>
Create `.planning/phases/06-payment-evidence-cards/06-25-SUMMARY.md` when done
</output>
