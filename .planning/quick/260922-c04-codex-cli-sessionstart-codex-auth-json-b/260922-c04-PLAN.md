---
phase: quick-260922-c04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/session-hooks.test.ts
  - scripts/install-codex.sh
  - .claude/settings.json
  - docs/OPERATIONS.md
autonomous: true
requirements: [QUICK-260922-c04]
user_setup:
  - service: chatgpt-codex
    why: "구독 계정 Codex 자격은 사용자 PC 브라우저 로그인으로만 발급된다 — 훅은 주입만 한다"
    env_vars:
      - name: CODEX_AUTH_JSON_B64
        source: "사용자 PC에서 `codex login` 후 생긴 ~/.codex/auth.json을 base64 한 줄로 만들어 claude.ai Environment 변수에 붙여넣기"
    dashboard_config:
      - task: "네트워크 허용 목록에 api.openai.com · chatgpt.com · auth.openai.com 추가"
        location: "claude.ai > Settings > Environment > Network access"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "클라우드 세션(startup|resume)에서 codex CLI 0.155.1이 PATH에 있다"
    - "CODEX_AUTH_JSON_B64가 설정된 클라우드 세션은 ${CODEX_HOME:-$HOME/.codex}/auth.json(mode 600)을 갖는다"
    - "깨진/부족한 자격 입력은 auth.json을 만들지 않고 경고만 남기며 세션은 계속된다"
    - "로컬(PC) 세션은 이 훅의 영향을 전혀 받지 않는다"
    - "토큰 값이 세션 로그(stdout/stderr)에 절대 나타나지 않는다"
    - "훅 출력이 네트워크 허용 목록 3개 호스트를 안내한다"
  artifacts:
    - "scripts/install-codex.sh (mode 755)"
    - "test/unit/session-hooks.test.ts"
    - "docs/OPERATIONS.md의 Codex 교차 리뷰 절"
  key_links:
    - ".claude/settings.json SessionStart 배열 → scripts/install-codex.sh (install_pkgs.sh 뒤, dev-db.sh 앞)"
    - "scripts/install-codex.sh → ${CODEX_HOME:-$HOME/.codex}/auth.json → codex CLI 자체 refresh_token 갱신"
    - "test/unit/session-hooks.test.ts → settings.json 훅 순서 + 스크립트 누수 금지 계약"
---

<objective>
클라우드 세션(Claude Code on the web)에서 `/gsd-review N --codex`가 동작하도록, SessionStart
훅으로 Codex CLI를 설치하고 환경 변수 `CODEX_AUTH_JSON_B64`의 ChatGPT 자격을
`~/.codex/auth.json`으로 복원한다.

Purpose: GPT 교차 리뷰를 모바일 포함 클라우드 세션에서 쓸 수 있게 한다. 구독 계정은
`--with-access-token`으로 로그인할 수 없으므로(agent identity JWT 요구) PC에서 만든
auth.json을 통째로 주입하는 것이 유일한 경로다.
Output: scripts/install-codex.sh, .claude/settings.json 훅 항목, test/unit/session-hooks.test.ts,
docs/OPERATIONS.md 절 1개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@.claude/settings.json
@scripts/install_pkgs.sh
@test/unit/ci-guard.test.ts
@test/unit/docs-limits.test.ts
</context>

<research_facts>
계획 시점(2026-09-22) 실측으로 확정된 사실 — 실행 중 재조사 금지.

- `npm install -g @openai/codex@0.155.1`은 이 클라우드 컨테이너에서 5초에 성공한다
  (registry.npmjs.org는 프록시 우회 허용). 설치 경로 `/opt/node22/bin/codex`.
- `codex --version` 출력은 `codex-cli 0.155.1` (마지막 필드가 버전).
- `codex login status`는 미인증 시 stdout `Not logged in`, exit 1.
- `codex login --with-access-token`은 ChatGPT 세션 토큰이 아니라 agent identity JWT를
  요구해 구독 계정에 쓸 수 없다(더미 입력 시 exit 1, 파일 미생성).
  `--with-api-key`는 OPENAI_API_KEY(API 과금)용이며 stdin으로 키를 읽는다.
- auth.json 필드: `OPENAI_API_KEY`(null), `tokens{id_token, access_token, refresh_token,
  account_id}`, `last_refresh`. Codex는 refresh_token으로 스스로 갱신한다.
- 이 환경의 네트워크 정책이 api.openai.com · chatgpt.com · auth.openai.com CONNECT를
  403으로 막는다. 훅은 설치·주입까지만 책임진다.
- 현재 컨테이너에는 이미 codex-cli 0.155.1이 설치되어 있다(`/opt/node22/bin/codex`).
- `docs/OPERATIONS.md`는 현재 233줄, 상한 300줄(test/unit/docs-limits.test.ts가 고정).
- 기존 SessionStart 훅 순서: install_pkgs.sh → dev-db.sh. cloud-only 가드는
  `if [ "$CLAUDE_CODE_REMOTE" != "true" ]; then exit 0; fi` 형태.
- `pnpm test:unit`은 `vitest run --project unit`이고 `test/unit/**/*.test.ts`를 수집한다.
  파일 필터는 `pnpm test:unit -- <이름조각>`로 넘긴다.

## Package Legitimacy (인라인 — RESEARCH.md 없음, quick 모드)

| 패키지 | 버전 | 상태 | 근거 |
|--------|------|------|------|
| @openai/codex | 0.155.1 (고정) | [VERIFIED] | OpenAI 공식 스코프(`@openai`), 계획 시점에 이 컨테이너에 이미 설치·실행 확인(`codex-cli 0.155.1`). 프로젝트 의존성이 아니다 — package.json·lockfile 변경 없음, 전역 CLI 한 개. |

[ASSUMED]/[SUS]가 아니므로 blocking human checkpoint를 넣지 않는다. 버전을 고정하는 것이
공급망 완화책이다(T-c04-SC).

## 스펙 충돌 1건 — 해소 방식 (실행자는 이 해소안을 따른다)

원 스펙의 누수 금지 정규식 `/echo.*CODEX_AUTH_JSON_B64/`는, 같은 스펙이 요구하는 안내
문구 `echo "install-codex: CODEX_AUTH_JSON_B64 not set — ..."`(변수 **이름**만 쓰는 줄)과
충돌해 항상 실패한다. 의도는 "값을 출력하지 말라"이므로, 테스트는 **변수 전개**가 있는
줄만 금지한다: `echo`/`cat`과 같은 줄에 `$CODEX_AUTH_JSON_B64` 또는
`${CODEX_AUTH_JSON_B64}`(OPENAI_API_KEY 동일)가 오면 실패. 이름만 있는 안내 문구는 통과.
`printf '%s' "$CODEX_AUTH_JSON_B64" | base64 -d > <임시파일>`은 stdout이 아니라 파이프로
가므로 금지 대상이 아니다(스펙이 명시한 구현 형태).
</research_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — session-hooks 계약 테스트를 먼저 쓰고 실패를 확인한다</name>
  <precondition>워크트리에 node_modules가 있다 — 없으면 먼저 `pnpm install --frozen-lockfile`을 실행한다.</precondition>
  <files>test/unit/session-hooks.test.ts</files>
  <behavior>
    - settings.json: JSON으로 파싱되고, matcher가 `startup|resume`인 SessionStart 항목의
      hooks[].command 목록에서 `scripts/install_pkgs.sh` 인덱스 < `scripts/install-codex.sh`
      인덱스 < `scripts/dev-db.sh` 인덱스다(셋 다 -1이 아니다).
    - settings.json: install-codex 훅 command가 `bash "$CLAUDE_PROJECT_DIR"/scripts/install-codex.sh`
      문자열을 포함하고, type이 `command`다.
    - install-codex.sh 존재·양성 토큰: `CLAUDE_CODE_REMOTE`, `CODEX_VERSION=0.155.1`,
      `CODEX_AUTH_JSON_B64`, `umask 077`, `codex login status` 다섯 개를 모두 포함한다.
    - install-codex.sh 누수 금지(줄 단위 검사, 주석 줄 포함):
      어떤 줄도 `/\becho\b[^\n]*\$\{?CODEX_AUTH_JSON_B64/`에 매치하지 않는다.
      어떤 줄도 `/\becho\b[^\n]*\$\{?OPENAI_API_KEY/`에 매치하지 않는다.
      어떤 줄도 `/\bcat\b[^\n]*auth\.json/`에 매치하지 않는다.
      어떤 줄도 xtrace(`/^\s*set\s+-[a-z]*x/`)를 켜지 않는다 — 켜면 bash가 전개된 토큰 값을
      stderr로 그대로 찍는다.
  </behavior>
  <action>
test/unit/session-hooks.test.ts를 새로 만든다. 스타일·구조는 test/unit/ci-guard.test.ts를
그대로 따른다: node:fs의 readFileSync + node:path의 resolve + vitest의 describe/expect/it,
`process.cwd()` 기준 경로, 파서 의존성 없이 텍스트/JSON.parse로만 검사, 한국어 주석으로
"왜 이 계약을 고정하는가"를 상단에 3~6줄로 남긴다(누수 벡터와 훅 순서 두 가지).

검사 대상 두 파일을 상수로 읽는다: `.claude/settings.json`(JSON.parse 결과와 원문 둘 다),
`scripts/install-codex.sh`(원문). 훅 순서 판정은 문자열 indexOf가 아니라 파싱한 배열의
인덱스로 한다 — matcher가 `startup|resume`인 SessionStart 항목을 찾아 hooks 배열의
command 문자열들에서 `findIndex(c => c.includes("scripts/<파일명>"))`으로 세 인덱스를 얻고
`expect(indexes).toEqual([...indexes].sort((a,b)=>a-b))` + 각 인덱스가 -1이 아님을 단언한다.
누수 금지 단언은 `<behavior>`의 네 정규식을 `source.split("\n")`의 각 줄에 돌려 매치한 줄
번호 목록이 빈 배열임을 단언한다(실패 메시지에 줄 번호만 넣고 줄 내용은 넣지 않는다 —
실패 출력 자체가 누수 통로가 되면 안 된다).

타입: `any` 금지. settings.json 파싱 결과는 좁은 로컬 타입(예: `{ hooks?: { SessionStart?:
Array<{ matcher?: string; hooks?: Array<{ type?: string; command?: string }> }> } }`)으로
선언하고 옵셔널 체이닝으로 접근한다. 파일이 아직 없어 readFileSync가 던지는 것도 정상적인
RED다 — try/catch로 감싸 초록으로 만들지 않는다.
  </action>
  <verify>
    <automated>pnpm test:unit -- session-hooks > /tmp/c04-red.log 2>&1; RC=$?; tail -30 /tmp/c04-red.log; grep -q "session-hooks" /tmp/c04-red.log && ! grep -q "No test files found" /tmp/c04-red.log && [ "$RC" -ne 0 ] && echo "RED-OK"</automated>
  </verify>
  <done>테스트 파일이 수집되고 실패한다(RED-OK 출력). `test(quick-c04): SessionStart 훅·누수 금지 계약 테스트 추가` 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — install-codex.sh 작성 + settings.json 훅 등록</name>
  <precondition>`codex --version`이 codex-cli 0.155.1을 출력하거나(설치 생략 경로) registry.npmjs.org로 `npm install -g`가 가능하다.</precondition>
  <files>scripts/install-codex.sh, .claude/settings.json</files>
  <action>
**scripts/install-codex.sh** (신규, `chmod 755`). 형태·톤은 scripts/install_pkgs.sh를 따른다:
`#!/bin/bash`, 한국어 주석 헤더, `set -e`/`set -u`/xtrace 사용 금지, 마지막은 `exit 0`.

1) 클라우드 가드: `CLAUDE_CODE_REMOTE`가 `true`가 아니면 즉시 `exit 0`(install_pkgs.sh와
   동일한 형태). 로컬 PC 세션은 건드리지 않는다.
2) 버전 고정: `CODEX_VERSION=0.155.1`.
3) 멱등 설치: `codex --version`의 마지막 필드를 awk로 뽑아 `$CODEX_VERSION`과 같으면
   설치를 건너뛰고 건너뛴다는 한 줄만 남긴다. 다르거나 codex가 없으면
   `npm install -g "@openai/codex@$CODEX_VERSION"`. 실패하면 stderr에 경고 한 줄을 남기고
   `exit 0` — SessionStart는 절대 세션을 막지 않는다.
4) 자격 복원(`CODEX_AUTH_JSON_B64`가 비어있지 않을 때):
   - `umask 077`을 먼저 건다.
   - `CODEX_DIR="${CODEX_HOME:-$HOME/.codex}"`, `mkdir -p "$CODEX_DIR"`(실패 시 경고 후 exit 0).
   - `TMP="$(mktemp "$CODEX_DIR/auth.json.XXXXXX")"` — 같은 파일시스템이라야 mv가 원자적이다.
   - `printf '%s' "$CODEX_AUTH_JSON_B64" | base64 -d > "$TMP" 2>/dev/null`.
   - 검증: `node -e`로 `$TMP`를 읽어 JSON.parse하고 `tokens.refresh_token`이 길이 1 이상의
     문자열인지만 본다. **반드시 try/catch로 감싸고 catch에서는 아무것도 출력하지 않고
     exit 1** — V8의 JSON.parse 오류 메시지는 입력 일부를 그대로 담아 토큰을 흘린다.
     성공 시에도 아무것도 출력하지 않는다.
   - 통과: `chmod 600 "$TMP"` 후 `mv -f "$TMP" "$CODEX_DIR/auth.json"`, 복원했다는 한 줄을
     **경로만** 넣어 출력한다.
   - 실패: `rm -f "$TMP"` 후 stderr에 "유효한 base64 JSON(tokens.refresh_token)이 아니라
     auth.json을 쓰지 않았다"는 경고 한 줄. auth.json은 만들지 않는다. 종료 코드는 0.
5) 대체 경로: `CODEX_AUTH_JSON_B64`가 없고 `OPENAI_API_KEY`만 있으면
   `printenv OPENAI_API_KEY | codex login --with-api-key`를 stdout·stderr 버린 채 실행하고
   실패 시 경고 한 줄. 둘 다 없으면
   `install-codex: CODEX_AUTH_JSON_B64 not set — codex installed but not logged in`
   한 줄을 남긴다(변수 **이름**만 — `$` 전개를 같은 줄에 쓰지 않는다).
6) 마지막에 `codex login status`를 돌린다(미인증이면 exit 1이므로 `|| true`로 삼킨다).
7) 네트워크 안내 한 줄: api.openai.com · chatgpt.com · auth.openai.com를 환경의 허용
   목록에 넣어야 실제 리뷰 호출이 동작한다.
8) 누수 금지 자기검열: 토큰 값·auth.json 내용을 출력하는 줄을 만들지 않는다. 주석 줄에도
   출력 명령과 두 비밀 변수의 달러-전개 형태를 한 줄에 같이 쓰지 않는다 — Task 1의 계약이
   주석을 구분하지 않고 줄 단위로 거부한다. 변수 이름만 쓰는 안내 문구는 허용된다.

**.claude/settings.json**: `hooks.SessionStart`의 matcher `startup|resume` 항목 hooks 배열에
`{"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR\"/scripts/install-codex.sh"}`를
install_pkgs.sh **뒤**, dev-db.sh **앞**에 끼워 넣는다. 동기 모드(다른 키 추가 없음).
다른 훅·키·마켓플레이스 설정은 한 글자도 건드리지 않는다. Edit로 삽입한다(파일 전체 재작성 금지).
  </action>
  <verify>
    <automated>chmod 755 scripts/install-codex.sh; [ "$(stat -c %a scripts/install-codex.sh)" = "755" ] && bash -n scripts/install-codex.sh && node -e 'JSON.parse(require("fs").readFileSync(".claude/settings.json","utf8"))' && echo SYNTAX-OK</automated>
    <automated>env -u CODEX_AUTH_JSON_B64 -u OPENAI_API_KEY CLAUDE_CODE_REMOTE=true bash scripts/install-codex.sh > /tmp/c04-a.log 2>&1; RC=$?; cat /tmp/c04-a.log; [ "$RC" -eq 0 ] && grep -q "not set" /tmp/c04-a.log && grep -q "Not logged in" /tmp/c04-a.log && echo VERIFY-A-OK</automated>
    <automated>rm -rf /tmp/codex-quick-test; CODEX_HOME=/tmp/codex-quick-test CLAUDE_CODE_REMOTE=true CODEX_AUTH_JSON_B64=$(printf '%s' '{"OPENAI_API_KEY":null,"tokens":{"id_token":"x","access_token":"x","refresh_token":"x","account_id":"x"},"last_refresh":"2026-01-01T00:00:00Z"}' | base64 -w0) bash scripts/install-codex.sh; RC=$?; [ "$RC" -eq 0 ] && [ -f /tmp/codex-quick-test/auth.json ] && [ "$(stat -c %a /tmp/codex-quick-test/auth.json)" = "600" ] && echo VERIFY-B-OK; rm -rf /tmp/codex-quick-test</automated>
    <automated>rm -rf /tmp/codex-quick-bad; CODEX_HOME=/tmp/codex-quick-bad CLAUDE_CODE_REMOTE=true CODEX_AUTH_JSON_B64=abc bash scripts/install-codex.sh > /tmp/c04-c.log 2>&1; RC=$?; cat /tmp/c04-c.log; [ "$RC" -eq 0 ] && [ ! -f /tmp/codex-quick-bad/auth.json ] && [ -z "$(ls -A /tmp/codex-quick-bad 2>/dev/null)" ] && echo VERIFY-C-OK; rm -rf /tmp/codex-quick-bad</automated>
    <automated>CLAUDE_CODE_REMOTE= bash scripts/install-codex.sh > /tmp/c04-local.log 2>&1; RC=$?; [ "$RC" -eq 0 ] && [ ! -s /tmp/c04-local.log ] && echo LOCAL-NOOP-OK</automated>
    <automated>pnpm test:unit -- session-hooks</automated>
  </verify>
  <done>VERIFY-A-OK · VERIFY-B-OK · VERIFY-C-OK · LOCAL-NOOP-OK가 모두 출력되고 session-hooks 테스트가 GREEN이다. `chore(quick-c04): SessionStart에 Codex CLI 설치·자격 주입 훅 추가` 커밋 1개(토큰 값 포함 금지).</done>
</task>

<task type="auto">
  <name>Task 3: docs/OPERATIONS.md에 Codex 교차 리뷰 절 추가 + 전체 게이트</name>
  <files>docs/OPERATIONS.md</files>
  <action>
docs/OPERATIONS.md 끝(§12 뒤)에 `## 13. Codex 교차 리뷰` 절을 추가한다. 기존 문서 톤(한국어
산문, 80자 내외 줄바꿈)을 따르고 다음 7가지를 빠짐없이 담는다:

1. 사용자 PC에서 `codex login`(브라우저)으로 로그인하면 `~/.codex/auth.json`이 생긴다.
2. 그 파일을 base64 한 줄로 만든다 — Linux `base64 -w0 ~/.codex/auth.json`,
   macOS `base64 -i ~/.codex/auth.json | tr -d '\n'`, Windows PowerShell
   `[Convert]::ToBase64String([IO.File]::ReadAllBytes("$env:USERPROFILE\.codex\auth.json"))`.
   세 명령은 코드 펜스 한 개에 세 줄로 넣는다.
3. 출력을 claude.ai 환경(Environment) 변수 `CODEX_AUTH_JSON_B64`에 붙여넣는다.
4. 같은 환경의 네트워크 허용 목록에 `api.openai.com` · `chatgpt.com` · `auth.openai.com`을
   추가한다 — 빠지면 설치·주입은 되지만 리뷰 호출이 403으로 막힌다.
5. 새 세션에서 `/gsd-review N --codex`로 쓴다(훅은 `scripts/install-codex.sh`, SessionStart).
6. 갱신에 실패하면 PC에서 2번을 다시 해 값을 교체한다.
7. 토큰은 리포·커밋·문서에 절대 넣지 않는다.

제약: 절 전체 15줄 이내, 파일 최종 줄 수 300 이하(test/unit/docs-limits.test.ts가 고정 —
현재 233줄이라 여유가 있다). 12자리 연속 숫자를 쓰지 않는다(같은 테스트의 D-03 단언).
실제 토큰·base64 값은 한 글자도 쓰지 않는다 — 명령과 변수 이름만 쓴다.
기존 §1~§12는 한 글자도 고치지 않는다.
  </action>
  <verify>
    <automated>[ "$(awk 'END{print NR}' docs/OPERATIONS.md)" -le 300 ] && grep -q "CODEX_AUTH_JSON_B64" docs/OPERATIONS.md && grep -q "auth.openai.com" docs/OPERATIONS.md && echo DOCS-OK</automated>
    <automated>pnpm test:unit -- session-hooks docs-limits ci-guard</automated>
    <automated>pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>DOCS-OK 출력, 세 테스트 파일 GREEN, lint·typecheck 통과. `docs(quick-c04): Codex 교차 리뷰 자격 주입 절차 추가` 커밋 1개.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 환경 변수 → 파일시스템 | `CODEX_AUTH_JSON_B64`는 훅이 통제하지 않는 외부 입력이며, 복호화 결과가 자격 파일이 된다 |
| 훅 → 세션 로그(stdout/stderr) | 훅 출력은 세션 트랜스크립트에 남는다 — 여기 찍힌 값은 회수 불가 |
| npm registry → 전역 PATH | `npm install -g`가 실행 가능한 코드를 컨테이너에 들인다 |
| 워크트리 → git 원격 | 커밋된 파일은 되돌려도 히스토리에 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-c04-01 | Information Disclosure | install-codex.sh 출력 | high | mitigate | 값 출력 줄 금지 + xtrace 금지를 test/unit/session-hooks.test.ts가 줄 단위로 고정(Task 1). node 검증기는 try/catch로 JSON.parse 오류 메시지(입력 일부 포함)를 삼킨다 |
| T-c04-02 | Information Disclosure | ~/.codex/auth.json 권한 | high | mitigate | `umask 077` 선행 + mktemp를 같은 디렉터리에 + `chmod 600` 후 `mv` — 다른 사용자가 읽을 수 있는 순간이 없다 |
| T-c04-03 | Tampering | 잘못된/악의적 env 내용 | medium | mitigate | base64 디코드 + JSON 파싱 + `tokens.refresh_token` 비어있지 않은 문자열 검증을 통과해야만 `mv`. 실패 시 임시파일 삭제, auth.json 미생성(Verify C) |
| T-c04-04 | Information Disclosure | 리포 커밋 | high | mitigate | 커밋 파일에는 실제 토큰·base64 값을 넣지 않는다. 검증용 더미 JSON은 `"x"` 자리표시자로 ephemeral bash에서만 쓴다 |
| T-c04-05 | Denial of Service | SessionStart 체인 | medium | mitigate | 모든 실패 경로가 경고 후 `exit 0` — dev-db.sh와 세션 자체를 막지 않는다. 로컬 세션은 가드로 즉시 종료(LOCAL-NOOP-OK) |
| T-c04-06 | Elevation of Privilege | `codex login --with-api-key` | low | accept | 대체 경로는 사용자가 스스로 넣은 `OPENAI_API_KEY`만 쓰고 stdin으로 전달한다(인자 노출 없음). 키가 없으면 실행되지 않는다 |
| T-c04-SC | Tampering | npm 전역 설치 | high | mitigate | `@openai/codex@0.155.1` 버전 고정(범위·latest 금지), 공식 `@openai` 스코프, 계획 시점 설치·실행 확인([VERIFIED]). package.json/lockfile 변경 없음 |

## 외부 API 통합 없음

이 작업은 CLI를 설치하고 로컬 자격 파일을 복원할 뿐, 어떤 API 표면도 코드로 통합하지 않는다
(API 호출은 별도 명령인 `/gsd-review --codex`가 codex CLI를 통해 한다). API coverage 매트릭스
대상이 아니다.
</threat_model>

<verification>
1. `bash -n scripts/install-codex.sh` — 문법.
2. Verify A(변수 없음) / B(정상 주입, 600) / C(깨진 입력, 미생성) / LOCAL-NOOP(로컬 무동작).
3. `pnpm test:unit -- session-hooks docs-limits ci-guard` GREEN.
4. `pnpm lint` · `pnpm typecheck` 통과.
5. `git diff`에 실제 토큰·base64 값이 없다. 변경 파일은 정확히 4개.
6. package.json · pnpm-lock.yaml 미변경(`git status`로 확인).
</verification>

<success_criteria>
- 클라우드 세션 SessionStart가 install_pkgs.sh → install-codex.sh → dev-db.sh 순서로 돈다.
- `CODEX_AUTH_JSON_B64`가 있는 세션은 mode 600 auth.json을 얻고, 없거나 깨진 세션도 exit 0.
- 토큰 값이 로그·커밋·문서 어디에도 없다.
- docs/OPERATIONS.md ≤ 300줄이고 PC→클라우드 자격 전달 절차가 문서화됐다.
- 커밋 3개(test:/chore:/docs:), 각 커밋 한 의도, 본문 한국어.
</success_criteria>

<output>
Create `.planning/quick/260922-c04-codex-cli-sessionstart-codex-auth-json-b/260922-c04-SUMMARY.md` when done
</output>
