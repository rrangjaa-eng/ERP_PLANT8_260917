---
phase: quick-260922-c04
plan: 01
subsystem: infra
tags: [codex-cli, session-hooks, claude-code-web, ci]

requires: []
provides:
  - "scripts/install-codex.sh — SessionStart 훅으로 Codex CLI 0.155.1 멱등 설치 + CODEX_AUTH_JSON_B64 자격 주입"
  - "test/unit/session-hooks.test.ts — SessionStart 훅 순서·누수 금지 계약 테스트"
  - "docs/OPERATIONS.md §13 — Codex 교차 리뷰 자격 전달 절차"
affects: ["gsd-review --codex 클라우드 세션 사용"]

actuals:
  tokens: 6000
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "SessionStart 훅 체인(install_pkgs.sh -> install-codex.sh -> dev-db.sh)에 실패해도 exit 0으로 세션을 막지 않는 신규 훅 추가 패턴"

key-files:
  created:
    - scripts/install-codex.sh
    - test/unit/session-hooks.test.ts
  modified:
    - .claude/settings.json
    - docs/OPERATIONS.md

key-decisions:
  - "누수 금지 정규식은 값(달러 전개)이 있는 줄만 거부하고 변수 이름만 쓰는 안내 문구는 허용 — PLAN의 스펙 충돌 해소안을 그대로 따름"
  - "자격 파일 검증은 node -e를 try/catch로 감싸 JSON.parse 오류 메시지(입력 일부 포함)가 stdout/stderr로 새지 않게 함"

patterns-established:
  - "임시파일은 대상 디렉터리 안에 mktemp -> chmod 600 -> mv로 원자적 배치, umask 077을 먼저 걸어 생성 순간부터 소유자 전용"

requirements-completed: [QUICK-260922-c04]

coverage:
  - id: D1
    description: "클라우드 세션 SessionStart가 install_pkgs.sh -> install-codex.sh -> dev-db.sh 순서로 등록됨"
    requirement: QUICK-260922-c04
    verification:
      - kind: unit
        ref: "test/unit/session-hooks.test.ts#install_pkgs.sh < install-codex.sh < dev-db.sh 순서로 등록된다"
        status: pass
    human_judgment: false
  - id: D2
    description: "CODEX_AUTH_JSON_B64가 유효하면 mode 600 auth.json이 생성되고, 없거나 깨진 입력·로컬 세션은 exit 0으로 무해하게 끝남"
    requirement: QUICK-260922-c04
    verification:
      - kind: other
        ref: "bash scripts/install-codex.sh 4종 수동 실행(VERIFY-A/B/C/LOCAL-NOOP), 본 SUMMARY 검증 증거 절 참고"
        status: pass
    human_judgment: false
  - id: D3
    description: "install-codex.sh의 어떤 줄도 토큰 값을 echo/cat/xtrace로 노출하지 않음"
    requirement: QUICK-260922-c04
    verification:
      - kind: unit
        ref: "test/unit/session-hooks.test.ts#scripts/install-codex.sh 누수 금지 (줄 단위) (4개 it)"
        status: pass
    human_judgment: false
  - id: D4
    description: "docs/OPERATIONS.md에 PC->클라우드 Codex 자격 전달 절차가 300줄 상한 안에서 추가됨"
    requirement: QUICK-260922-c04
    verification:
      - kind: unit
        ref: "test/unit/docs-limits.test.ts (전체)"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-09-22
status: complete
---

# Quick Task 260922-c04: Codex CLI SessionStart 훅 Summary

**클라우드 세션에서 `/gsd-review --codex`가 동작하도록 SessionStart 훅으로 codex-cli
0.155.1을 멱등 설치하고, `CODEX_AUTH_JSON_B64` 환경 변수의 base64 JSON을 검증 후
mode 600 `~/.codex/auth.json`으로 원자적으로 복원한다.**

## Performance

- **Duration:** ~5min (RED 커밋부터 최종 docs 커밋까지 08:47:24Z~08:51:40Z)
- **Tasks:** 3/3
- **Files modified:** 4 (test/unit/session-hooks.test.ts, scripts/install-codex.sh,
  .claude/settings.json, docs/OPERATIONS.md — plan `files_modified`와 정확히 일치)

## Accomplishments

- `test/unit/session-hooks.test.ts` — 훅 순서(인덱스 정렬) + 4가지 누수 금지 정규식을
  줄 단위로 고정하는 계약 테스트. RED(파일 없음, ENOENT)를 먼저 확인
- `scripts/install-codex.sh`(mode 755) — 클라우드 가드 → 버전 고정 멱등 설치 →
  CODEX_AUTH_JSON_B64 검증·주입(umask 077 + mktemp 같은 디렉터리 + node try/catch
  검증 + chmod 600 + mv) → OPENAI_API_KEY 대체 경로 → `codex login status` 확인 →
  네트워크 허용 목록 안내. 모든 실패 경로가 경고 후 exit 0
- `.claude/settings.json` — SessionStart `startup|resume` hooks 배열에
  install_pkgs.sh 뒤 dev-db.sh 앞으로 삽입(Edit, 다른 키 무변경)
- `docs/OPERATIONS.md` §13(14줄, 233→248줄) — PC 로그인 → base64 변환(3개 OS 명령) →
  Environment 변수 붙여넣기 → 네트워크 허용 목록 → 사용 → 갱신 → 토큰 비보관 원칙

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — session-hooks 계약 테스트** - `7167a76` (test)
2. **Task 2: GREEN — install-codex.sh + settings.json 훅 등록** - `070c3c5` (chore)
3. **Task 3: docs/OPERATIONS.md Codex 교차 리뷰 절 추가** - `4b5f8ea` (docs)

_TDD: Task 1은 RED만(구현 없음), Task 2가 구현으로 session-hooks 테스트를 GREEN으로
전환. 별도 리팩터 커밋 없음(최소 구현이 이미 단순)._

## Verification Evidence

**Task 1 RED (`pnpm test:unit -- session-hooks`):**
```
FAIL |unit| test/unit/session-hooks.test.ts [ test/unit/session-hooks.test.ts ]
Error: ENOENT: no such file or directory, open '.../scripts/install-codex.sh'
Test Files  1 failed | 63 passed (64)
```

**Task 2 GREEN 4종 수동 실행:**
- `env -u CODEX_AUTH_JSON_B64 -u OPENAI_API_KEY CLAUDE_CODE_REMOTE=true bash scripts/install-codex.sh`
  → exit 0, stdout에 "not set" + `codex login status`의 "Not logged in" 포함 → VERIFY-A-OK
- `CODEX_HOME=/tmp/... CODEX_AUTH_JSON_B64=<더미 JSON base64, refresh_token="x">` →
  exit 0, `/tmp/.../auth.json` mode 600 생성 → VERIFY-B-OK
- `CODEX_AUTH_JSON_B64=abc`(깨진 입력) → exit 0, auth.json 미생성, 디렉터리 비어있음 →
  VERIFY-C-OK
- `CLAUDE_CODE_REMOTE=`(로컬) → exit 0, stdout/stderr 완전히 빈 로그 → LOCAL-NOOP-OK
- `pnpm test:unit -- session-hooks` → `Test Files 64 passed (64)`, `Tests 607 passed (607)`

**Task 3 (`DOCS-OK` + 3파일 테스트 + lint/typecheck):**
- `awk 'END{print NR}' docs/OPERATIONS.md` → 248(≤300) + grep 두 토큰 통과 → DOCS-OK
- `pnpm test:unit -- session-hooks docs-limits ci-guard` → `Test Files 64 passed (64)`,
  `Tests 607 passed (607)`
- `pnpm lint` → eslint+stylelint 통과(기존 boundaries 플러그인 deprecation 경고만,
  본 작업과 무관한 사전 존재 경고)
- `pnpm typecheck` → 출력 없이 통과

**최종 전체 확인:**
- `pnpm test:unit`(전체) → `Test Files 64 passed (64)`, `Tests 607 passed (607)`
- `git status --short` → 세 커밋 후 clean, package.json/pnpm-lock.yaml 미변경
- `git diff` 토큰 값 grep(`refresh_token|access_token|id_token|account_id`, `"x"` 자리
  표시자 제외) → 매치 없음

## Decisions Made

- 누수 금지 테스트는 PLAN이 지시한 스펙 충돌 해소안(달러 전개가 있는 줄만 거부, 변수
  이름만 쓰는 안내 문구는 허용)을 그대로 구현했다 — 다른 해석 없음
- `install-codex.sh`는 install_pkgs.sh와 동일한 무주석 `set -e/-u` 미사용 스타일(경고
  후 exit 0)을 따랐다 — 스크립트 전체가 최선노력형이라 즉시 종료보다 개별 단계 방어가
  일관성 있다고 판단(기존 install_pkgs.sh 패턴과 동일)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. `codex login status`가 더미 토큰("x")에 대해 stderr에 "invalid ID token format"을
출력했지만, 이는 `|| true`로 삼켜져 스크립트 exit code에 영향 없고 VERIFY-B/C 판정
기준(파일 존재·권한)과 무관해 계획대로 통과했다.

## User Setup Required

None for this quick task's automated verification — 실제 클라우드 세션에서 Codex
교차 리뷰를 쓰려면 `docs/OPERATIONS.md` §13 절차대로 사용자가 PC에서 `codex login` 후
`CODEX_AUTH_JSON_B64`를 claude.ai Environment 변수로 설정하고 네트워크 허용 목록
3개 호스트를 추가해야 한다(PLAN frontmatter의 `user_setup` 그대로, 이 실행자는 코드만
전달했다).

## Next Phase Readiness

`/gsd-review --codex`는 클라우드 세션에서 사용자가 `CODEX_AUTH_JSON_B64`를 설정하는
즉시 쓸 수 있다. 블로커 없음.

## Self-Check: PASSED

- FOUND: scripts/install-codex.sh
- FOUND: test/unit/session-hooks.test.ts
- FOUND: docs/OPERATIONS.md §13 (line 235)
- FOUND commit 7167a76, 070c3c5, 4b5f8ea in `git log --oneline --all`

---
*Quick task: 260922-c04*
*Completed: 2026-09-22*
