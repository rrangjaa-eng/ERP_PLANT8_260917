---
phase: quick-260922-i3k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/SYSTEM.md
  - docs/design/DECISIONS.md
  - test/unit/design-system-docs.test.ts
  - ui/shell/role-menu.ts
  - ui/shell/MoreSheet.tsx
  - ui/shell/TopBar.tsx
  - app/(app)/admin/page.tsx
  - app/(app)/admin/admin-index.module.css
  - test/unit/ui/role-menu.test.ts
  - test/e2e/admin-nav.spec.ts
  - test/unit/ui/admin-table-caption.test.ts
  - app/(app)/admin/code-tables/page.tsx
  - app/(app)/admin/corp-cards/page.tsx
  - app/(app)/admin/people/page.tsx
  - app/(app)/admin/people/roles/roles-client.tsx
  - app/(app)/admin/vendors/page.tsx
  - ui/history-list/HistoryList.tsx
  - app/(app)/admin/settings/settings-form-client.tsx
  - app/(app)/admin/people/[id]/person-detail-client.tsx
autonomous: true
requirements: [QUICK-260922-i3k, A-M3]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "admin.* 메뉴를 하나라도 볼 수 있는 계급의 PC 사용자 메뉴·「더보기」 시트에는 관리자 항목이 「관리」 한 줄뿐이고, 개별 관리자 화면 이름은 그 두 표면에 없다"
    - "admin.* 메뉴를 하나도 볼 수 없는 계급에게는 「관리」 줄 자체가 없고 /admin은 404다"
    - "/admin 인덱스가 마스터·설정·권한·운영 기록 세 그룹으로 관리자 화면 10개를 보여주고, 볼 항목이 0개인 그룹은 머리글도 렌더하지 않는다"
    - "/admin 인덱스의 그룹·항목 순서가 SYSTEM.md §6-10 표와 원소 단위로 같다(문서가 정본)"
    - "관리자 읽기용 표 6종에 시각적으로 숨긴 <caption>이 있고, 코드표의 모든 th에 scope가 있다"
  artifacts:
    - "docs/design/SYSTEM.md §6-10 신설 + §6-0 (a)·§6-8·§7-8 개정"
    - "docs/design/DECISIONS.md 2026-09-22 항목"
    - "app/(app)/admin/page.tsx + app/(app)/admin/admin-index.module.css"
    - "ui/shell/role-menu.ts의 adminIndexGroups 내보내기"
    - "test/unit/ui/admin-table-caption.test.ts"
  key_links:
    - "SYSTEM.md §6-10 표 → role-menu.test.ts가 그 표를 읽어 adminIndexGroups와 대조(문서가 바뀌면 테스트가 먼저 깨진다)"
    - "app/(app)/admin/page.tsx → MENUS × can()으로 계산한 allowedMenus → adminIndexGroups(서버가 거른다, 클라이언트에서 숨기지 않는다)"
    - "role-menu.ts adminMenu → Shell → TopBar(PC 메뉴)·BottomTabs → MoreSheet(폰 시트)"
---

<objective>
관리자 진입점을 「관리」 한 줄로 접고 그 뒤에 /admin 인덱스(3그룹)를 새로 만든다(옵션 B, 사용자 확정). 같은 커밋 묶음에서 Phase 3 이월 항목 A-M3(읽기용 표 6종의 `<caption>`, 코드표 `th scope`)을 닫는다.

Purpose: 지금 관리자는 드롭다운 한 곳에서 13줄(관리자 10 + 계정 3)을 본다. Phase 7이 공휴일·지급일·알림·SMTP 설정을 더 얹으면 셸이 무너진다. 「관리」 한 줄은 관리자 화면이 늘어도 셸의 줄 수를 고정한다. Phase 4 실행 전에 SYSTEM.md를 정착시켜 새 화면(프로젝트·견적·리저브)이 흔들린 계약 위에 지어지지 않게 한다.

Output: SYSTEM.md §6-10 신설 + 세 절 개정, DECISIONS.md 근거 기록, /admin 인덱스 화면, 접힌 셸 메뉴, 표 6종 caption.

**작업 순서는 문서 → 테스트 → 구현이다.** `test/unit/ui/role-menu.test.ts`와 `test/unit/design-system-docs.test.ts`는 `docs/design/SYSTEM.md`의 문장을 읽어 통과/실패가 갈린다 — 문서가 계약의 정본이다.

**커밋**: 태스크당 정확히 한 커밋(한 커밋 한 의도). 제목은 영어 접두어 + 짧은 요약, 본문은 한국어. 각 커밋 메시지 끝에 다음 두 줄을 붙인다:
`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
`Claude-Session: https://claude.ai/code/session_01VHtmvgYrwfb9K8iLXmHD2e`

**브랜치**: 이미 `claude/gsd-progress-joto9s`에 있다. 브랜치를 만들거나 바꾸거나 리베이스하지 않는다.
</objective>

<planning_findings>
계획 시점에 실제 파일을 읽고 확인한 사실이다. 실행 중 이와 다르면 멈추고 보고한다.

1. **테스트의 문서 결합은 부분적이다.** `test/unit/ui/role-menu.test.ts`가 SYSTEM.md에서 읽는 것은 (a) 「계정」 그룹 라벨(정규식 `「계정」\s*그룹\(([^)]+)\)`, 문서 전체에서 **첫 번째** 일치 — 현재 §7-8 754행 하나뿐이다), (b) 폰 하단 탭 역할 표 행, (c) §6-0 구간이 `PC 상단 바 우측 사용자 진입점`·`시스템 상태` 두 문자열을 담는지다. **관리자 메뉴 계약(ADMIN_MENU_KEYS 10개, adminMenu 길이 단언)은 문서가 아니라 테스트 파일 안의 상수로 하드코딩되어 있다.** 즉 문서만 고쳐도 관리자 메뉴 테스트는 RED가 되지 않는다 — 그래서 태스크 2가 테스트를 먼저 고치고 RED를 눈으로 확인한 뒤 구현한다. 새 §6-10 표는 (a)·(b)와 같은 방식(문서를 읽어 대조)으로 묶는다.
2. **문서 결합 테스트가 하나 더 있다.** `test/unit/design-system-docs.test.ts` 63~90행이 §6-0 구간에 `시스템 상태`·`소속`·계급 5종·탭 표 5행이 있는지 검사한다. 그래서 §6-0 (a) 개정문은 「관리」를 도입하면서도 `시스템 상태`라는 낱말을 그 구간에 남긴다(관리 화면 예시로). 이건 테스트를 피하려는 꼼수가 아니라 사실대로 쓰는 문장이다.
3. **A-M3의 "6종"은 실측과 정확히 맞는다.** caption이 없는 표는 정확히 여섯이다 — `admin/code-tables/page.tsx`(118행, `th scope`도 없음), `admin/corp-cards/page.tsx`(145), `admin/people/page.tsx`(70), `admin/people/roles/roles-client.tsx`(126), `admin/vendors/page.tsx`(110), `ui/history-list/HistoryList.tsx`(219). 행동 로그(107행)·보관함(48행)·권한 그리드는 **이미 caption이 있다** — 손대지 않는다.
4. **시각적으로 숨기는 유틸리티는 이미 있다.** `app/globals.css:60` `.sr-only`(주석이 "표 caption·페이지 제목" 용도라고 명시). `action-log.module.css`·`PermissionGrid.module.css`에 같은 규칙의 모듈 지역 복제본이 두 개 더 있다. **새로 만들지 않고 전역 `.sr-only`를 쓴다**(새 CSS 0줄). 기존 복제본 둘은 그대로 둔다 — 요청받지 않은 리팩터 금지.
5. **TopBar.tsx는 코드 변경이 필요 없다.** `pcMenuItems`가 `adminMenu`를 그대로 펼치므로 adminMenu가 한 줄로 접히면 PC 메뉴도 자동으로 「관리 · 내 정보 · 로그아웃」이 된다. 이 파일에서 고치는 것은 우리 변경으로 거짓이 된 주석 한 문단뿐이다.
6. **E2E 하나가 반드시 깨진다.** `test/e2e/admin-nav.spec.ts`가 "사용자 메뉴에 admin.* 10개가 전부 보인다"를 단언한다 — 같은 태스크에서 새 계약으로 고친다. `test/e2e/user-menu.spec.ts`는 `menuitem >= 3`만 보므로(관리·내 정보·로그아웃 = 3) 고칠 필요가 없다.
7. **새 파일이 기존 메타 테스트를 건드리지 않는다.** `admin-master-list-first.test.ts`·`leak-scan.test.ts`는 경로를 명시 열거하고, `no-admin-boolean.test.ts`는 `isAdmin` 문자열만 금지한다 — `app/(app)/admin/page.tsx` 신설은 셋 중 어디에도 걸리지 않는다(액션·폼이 없다).
8. **관련 없는 발견(고치지 않는다)**: `ui/shell/MoreSheet.tsx`의 `firstRowHref` 계산은 `moreMenu[0]`가 있을 때 adminMenu 쪽 ref를 영원히 쓰지 않는다 — 동작상 문제는 없다(첫 행에 포커스가 간다). 이번 범위 밖이므로 손대지 않고 여기 적어만 둔다.
</planning_findings>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@docs/design/SYSTEM.md
@docs/design/DECISIONS.md
@ui/shell/role-menu.ts
@test/unit/ui/role-menu.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYSTEM.md 개정 + §6-10 신설 + DECISIONS.md 기록</name>
  <files>docs/design/SYSTEM.md, docs/design/DECISIONS.md, test/unit/design-system-docs.test.ts</files>
  <read_first>docs/design/SYSTEM.md 260~310행(§6-0), 559~581행(§6-8), 746~756행(§7-8), 582~605행(§6-9 끝~§7 시작); docs/design/DECISIONS.md 365행 이후(최근 항목 서식); test/unit/design-system-docs.test.ts 30~60행(절 경계 표)</read_first>
  <action>
docs/design/SYSTEM.md를 다섯 곳 고친다. 기존 문장 구조·어투를 그대로 따르고, 아래 열거한 곳 말고는 한 글자도 바꾸지 않는다.

(1) §6-0 **(a) PC 상단 바 우측 사용자 진입점** 항목의 괄호 안을 바꾼다. 지금은 "권한표의 시스템 상태 보기 권한이 있는 계급만 그 위에 시스템 상태(§6-8) 항목이 하나 더 보인다, D-17"이다. 이것을 "권한표에서 view 권한이 있는 관리자 화면이 하나라도 있는 계급만 그 위에 「관리」(§6-10) 항목이 하나 더 보인다 — 개별 관리자 화면(시스템 상태 · 코드표 · 권한표 …)의 이름은 이 메뉴에 나오지 않고 「관리」 인덱스에서 세 그룹으로 고른다, D-17" 취지로 고친다. **`시스템 상태`라는 낱말이 §6-0 구간에 남아야 한다**(위 예시 나열이 그 역할을 한다) — design-system-docs.test.ts와 role-menu.test.ts가 이 구간에서 그 문자열을 찾는다. 「관리」는 낫표를 포함해 정확히 `「관리」`로 적는다(테스트가 이 형태를 찾는다).

(2) §6-0 마지막 항목 "「더보기」 시트(§7-8): 첫 항목이 검색…, 그 아래 나머지 1차 메뉴 · 설정 · 내 정보 · 로그아웃"에 「관리」를 넣는다 — "나머지 1차 메뉴 · 관리(권한이 있을 때만) · 설정 · 내 정보 · 로그아웃" 형태. 이 문장에 `「계정」 그룹(`으로 시작하는 새 괄호 표현을 만들지 않는다(4번 주의사항과 같은 이유).

(3) §6-8의 항목 "PC 사용자 메뉴(§6-0 (a))와 관리자용 「더보기」 시트가 이 화면의 진입점이다"를 "「관리」 인덱스(§6-10)의 「운영 기록」 그룹이 이 화면의 진입점이고, PC 사용자 메뉴(§6-0 (a))·「더보기」 시트(§7-8)의 「관리」 한 줄이 그 인덱스로 간다"로 바꾼다.

(4) §7-8의 항목 "「더보기」 시트(§6-0): 첫 항목 검색… , 「계정」 그룹(내 정보 · 설정 · 로그아웃). 목록 행 44, 아이콘 없음…"에서, 1차 메뉴 건수 괄호와 「계정」 그룹 사이에 관리 줄을 끼운다: "…오른쪽 건수(…), 권한표에서 view 권한이 있는 관리자 화면이 하나라도 있으면 「관리」 한 줄(§6-10 — 그룹 머리글 없이 목록 행 하나), 「계정」 그룹(내 정보 · 설정 · 로그아웃). 목록 행 44…". **`「계정」 그룹(내 정보 · 설정 · 로그아웃)` 이 문자열은 한 글자도 바꾸지 않는다.** role-menu.test.ts가 문서 전체에서 `「계정」 그룹(` 첫 일치를 읽어 계정 그룹 계약을 만든다 — 앞쪽에 같은 형태의 괄호 표현을 새로 만들면 그 테스트가 엉뚱한 목록을 읽는다.

(5) §6-9 끝(오류 페이지 다섯 상태 목록 다음, `---`와 `## 7. 컴포넌트 규칙` 앞)에 새 절을 만든다. 머리글은 정확히 `### 6-10. 「관리」 인덱스 화면 (관리자 화면 10개의 단일 진입점)`. 내용은 §6-8과 같은 골격으로 쓴다 — 화면 얼개 스케치(§6-8처럼 세로 막대 줄로 그린다: 화면 제목 「관리」 + 그룹 머리글 + 항목 행), 그다음 규칙 항목들, 마지막에 다섯 상태. 담아야 할 계약:
  - 그룹과 순서가 정본인 표. 머리글 행은 `| 그룹 | 항목 |`, 구분선 한 줄, 그리고 세 행: 마스터 = 사람 · 거래처 · 법인카드 마스터 · 코드표 / 설정·권한 = 권한표 · 정보 노출표 · 시스템 설정 / 운영 기록 = 시스템 상태 · 행동 로그 · 보관함. 항목은 가운뎃점 `·`로 구분하고 항목 이름은 role-menu.ts의 라벨과 글자 단위로 같아야 한다.
  - 진입점은 PC 사용자 메뉴(§6-0 (a))와 「더보기」 시트(§7-8)의 「관리」 한 줄뿐이고, 개별 관리자 화면 이름은 그 두 표면에 나오지 않는다. 관리자 화면이 늘어도 셸의 줄 수는 그대로다.
  - 행 모양: 그룹 머리글은 §7-3 그룹 머리글 행과 같은 결(`--fs-sm --muted` 600), 항목은 §7-8 시트 목록 행과 같은 높이(`--touch-min`)의 링크. 카드 없음, 아이콘 없음, 설명 문구 없음(§8).
  - 서버가 거른다: 권한표에서 view 권한이 있는 항목만 서버가 보낸다(§7-3 「칸 수 가변」과 같은 원칙 — 클라이언트에서 숨기지 않는다). 항목이 하나도 없는 그룹은 머리글도 렌더하지 않는다.
  - 볼 수 있는 항목이 0개인 계급에게는 이 화면이 404이고(D-17) 「관리」 줄도 없다.
  - 다섯 상태: LOADING 해당 없음(서버 렌더 한 번에 목록이 확정, 추가 조회 없음) / EMPTY 해당 없음(볼 항목 0개면 빈 화면이 아니라 404) / ERROR 해당 없음(조회할 데이터가 없다 — 세션·권한 실패는 §6-9가 받는다) / SUCCESS 해당 없음(이동 전용 화면) / PARTIAL 계급에 따라 일부 그룹·항목만 보인다, 빠진 자리를 `—`로 채우지 않는다. 다섯 낱말(LOADING·EMPTY·ERROR·SUCCESS·PARTIAL)을 전부 적는다.

test/unit/design-system-docs.test.ts를 문서에 맞춘다(이 파일이 docs 전용 커밋을 CI에 태우는 장치라 같은 커밋에 들어가야 한다): 머리글 목록에 `### 6-10`을 더하고, `sectionBounds`의 6-9 행 끝 경계를 `## 7. 컴포넌트 규칙`에서 `### 6-10`으로 바꾸고, `["6-10 「관리」 인덱스 화면", "### 6-10", "## 7. 컴포넌트 규칙"]` 행을 더한다.

docs/design/DECISIONS.md 맨 끝에 항목 하나를 덧붙인다. 기존 항목 서식 그대로: `## 2026-09-22 — 관리자 진입점을 「관리」 한 줄 + /admin 인덱스 3그룹으로 (§6-0 (a)·§6-8·§7-8 개정, §6-10 신설)`, 그 아래 **결정**(무엇을) · **결정자**(사용자 결정 2026-09-22, 옵션 B) · **왜 이 해법인가**(관리자 10줄 + 계정 3줄 = 드롭다운 13줄이 이미 한 화면의 메뉴로 과하고, Phase 7이 공휴일·지급일·알림·SMTP 설정을 더 얹으면 선형으로 늘어난다 / 셸은 줄 수가 고정되어야 하고 인덱스 화면은 그룹으로 늘어난다 / 권한 필터를 한 곳(서버)에 둔 채 표면만 접는 변경이라 노출 계약이 바뀌지 않는다) · **버린 대안**(드롭다운 안 접이식 그룹 — §7-8이 시트·모달의 쓰임을 한정하고 드롭다운 안 2단 메뉴는 이 시스템에 없는 새 장치다 / 1차 메뉴에 「관리」 추가 — §6-0이 1차 메뉴 5개를 D-22로 고정했다) · **같이 고친 것**(§6-0 (a)·§6-0 시트 항목·§6-8 진입점 문장·§7-8 시트 항목) · **범위**(문서 + `ui/shell/role-menu.ts`·`MoreSheet.tsx`·`app/(app)/admin/page.tsx`) 문단을 쓴다. 이 결정이 D-17(권한 없는 리소스는 404)을 바꾸지 않는다는 점을 한 줄로 못 박는다.
  </action>
  <verify>
    <automated>cd /home/user/ERP_PLANT8_260917 && pnpm exec vitest run --project unit test/unit/design-system-docs.test.ts test/unit/ui/role-menu.test.ts</automated>
  </verify>
  <done>두 테스트 파일이 전부 통과한다(문서만 고친 단계라 role-menu.test.ts는 여전히 GREEN이어야 한다 — RED면 §6-0/§7-8에서 건드리지 말아야 할 문장을 건드린 것이다). SYSTEM.md에 `### 6-10` 머리글과 세 그룹 표가 있고, DECISIONS.md에 2026-09-22 항목이 있다. 커밋 하나.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 「관리」 한 줄로 접기 + /admin 인덱스 3그룹 (RED 먼저)</name>
  <files>test/unit/ui/role-menu.test.ts, ui/shell/role-menu.ts, ui/shell/MoreSheet.tsx, ui/shell/TopBar.tsx, app/(app)/admin/page.tsx, app/(app)/admin/admin-index.module.css, test/e2e/admin-nav.spec.ts</files>
  <read_first>ui/shell/role-menu.ts 전체; test/unit/ui/role-menu.test.ts 전체; ui/shell/MoreSheet.tsx 100~135행; app/(app)/layout.tsx(allowedMenus 계산 방식); app/(app)/admin/system-status/page.tsx 1~25행(세션·권한·404 패턴); ui/shell/MoreSheet.module.css 70~117행(.list/.link/.group 규칙); test/e2e/admin-nav.spec.ts 전체</read_first>
  <behavior>
    - adminMenu: allowedMenus에 admin.* 키가 하나라도 있으면 정확히 `[{ label: "관리", href: "/admin" }]`
    - adminMenu: allowedMenus가 비면 `[]` (계급과 무관 — 시스템 관리자라도 허용 목록이 비면 없다)
    - adminIndexGroups: 10개 전부 허용이면 그룹 3개, 라벨과 항목 순서가 SYSTEM.md §6-10 표와 원소 단위로 같다
    - adminIndexGroups: 한 그룹의 항목만 허용되면 그 그룹 하나만 돌아온다(빈 그룹은 배열에 없다)
    - adminIndexGroups: 빈 allowedMenus면 `[]`
    - adminIndexGroups: allowedMenus의 순서를 뒤집어도 결과가 같다(순수 함수)
    - adminIndexGroups: 모든 항목 href가 `/admin/<키 뒤쪽 이름>`이고, 세 그룹의 합집합이 admin.* 키 10개를 빠짐없이 덮는다
    - 전제 확인: admin.* 키 10개의 라우트 디렉터리와 `app/(app)/admin/page.tsx`가 실재한다
  </behavior>
  <action>
**먼저 테스트를 고치고 RED를 눈으로 본다. 구현 코드를 한 줄도 먼저 쓰지 않는다.**

1단계 — `test/unit/ui/role-menu.test.ts`를 새 계약으로 고친다. 기존 구조(문서를 읽어 대조하는 방식)를 그대로 따른다:
  - `section(SYSTEM, "### 6-10", "## 7. 컴포넌트 규칙")`으로 §6-10 구간을 잘라, 그 안의 표 행에서 그룹 라벨과 항목 목록을 읽는 헬퍼를 만든다(`expectedBottomTabRow`와 같은 결). 세로 막대로 시작하는 줄 중 구분선(`---` 포함)과 머리글 행(첫 칸이 `그룹`)을 걸러내고, 남은 세 행의 첫 칸을 그룹 라벨로, 둘째 칸을 `·`로 쪼개 항목 이름 목록으로 삼는다. 문서에서 읽지 못하면 명확한 메시지로 던진다.
  - `describe("roleMenu — 관리자 메뉴 진입점 …")` 블록을 새 계약으로 교체한다: admin.* 키 10개 각각 하나만 허용해도 adminMenu가 길이 1이고 `{ label: "관리", href: "/admin" }`이라는 것, 빈 allowedMenus면 `[]`라는 것, 시스템 관리자라도 허용 목록이 비면 `[]`라는 것. 기존의 "10개면 10개" · "admin.settings 라벨 구분" 단언은 adminMenu가 아니라 새 `adminIndexGroups`에 대한 단언으로 옮긴다.
  - `describe("adminIndexGroups — 「관리」 인덱스 3그룹 (SYSTEM.md §6-10 표가 정본)")`를 새로 쓴다. 위 `<behavior>` 여덟 줄을 전부 단언으로 옮긴다. 그룹 라벨·항목 순서는 하드코딩하지 말고 §6-10 표에서 읽은 값과 대조한다.
  - 라우트 전제 확인에 `app/(app)/admin/page.tsx` 존재 단언을 더한다.
  - 마지막 `describe`의 §6-0 전제 확인에 `expect(SHELL_SECTION).toContain("「관리」")`를 더한다(기존 `시스템 상태` 단언은 남긴다).

2단계 — RED 확인. `pnpm exec vitest run --project unit test/unit/ui/role-menu.test.ts`를 실행하고 **실패 출력을 그대로 인용해 기록한다**(요약본이 아니라 실제 실패 줄). `adminIndexGroups`가 없어 import가 깨지는 것도 RED의 일부다. 실패를 보지 못했다면 구현으로 넘어가지 않는다.

3단계 — `ui/shell/role-menu.ts` 구현:
  - `ADMIN_MENUS`의 각 원소에 `group` 필드를 더하고 배열 순서를 §6-10 표 순서로 바꾼다: 사람·거래처·법인카드 마스터·코드표(마스터), 권한표·정보 노출표·시스템 설정(설정·권한), 시스템 상태·행동 로그·보관함(운영 기록). key·label·href 문자열은 지금 값 그대로 옮긴다(오타 유입 금지).
  - 그룹 라벨 상수 세 개를 §6-10 표와 같은 문자열로 둔다: `마스터`, `설정·권한`, `운영 기록`.
  - `export type AdminMenuGroup = { label: string; items: MenuLink[] }`를 더하고, `export function adminIndexGroups(viewer: RoleMenuViewer): AdminMenuGroup[]`를 만든다 — allowedMenus에 있는 항목만 ADMIN_MENUS 순서대로 골라 그룹별로 묶고, 항목이 0개인 그룹은 배열에 넣지 않는다.
  - `buildAdminMenu`를 "허용된 admin.* 메뉴가 하나라도 있으면 `[{ label: "관리", href: "/admin" }]`, 없으면 `[]`"로 바꾼다. `adminMenu` 필드 이름과 `MenuLink[]` 타입은 유지한다 — Shell·TopBar·BottomTabs·layout.tsx의 배선을 건드리지 않기 위한 선택이다.
  - 우리 변경으로 거짓이 된 주석(파일 머리, `adminMenu` 필드 설명, ADMIN_MENUS 위 문단)을 사실로 고친다. 진입점이 「관리」 한 줄이고 개별 화면은 §6-10 인덱스가 담당한다는 것, 그룹 순서의 정본이 SYSTEM.md §6-10 표라는 것을 적는다. 그 밖의 주석은 손대지 않는다.

4단계 — `ui/shell/MoreSheet.tsx`: adminMenu 블록에서 `관리자` 그룹 머리글 `<li>`를 없애고, adminMenu 항목을 1차 메뉴 행과 같은 모양의 평범한 목록 행으로 렌더한다(§7-8 개정문: 그룹 머리글 없이 목록 행 하나). `<li className={styles.group}>계정</li>`은 그대로 둔다. 그 외 로직(포커스·로그아웃·검색 행)은 건드리지 않는다.

5단계 — `ui/shell/TopBar.tsx`: **코드는 바꾸지 않는다.** `isSettingsEntry` 위 주석이 "adminMenu(admin.* 전체)로 일반화했다"고 말해 이제 거짓이므로, 그 문단만 "관리자 진입점은 「관리」 한 줄이고 개별 화면은 /admin 인덱스(§6-10)에서 고른다"는 사실로 고친다.

6단계 — `app/(app)/admin/page.tsx` 신설. `app/(app)/admin/system-status/page.tsx`의 앞 세 줄 패턴을 그대로 따른다: `getSession()`이 없으면 `redirect("/login")`. 그다음 `app/(app)/layout.tsx`와 같은 방식으로 `MENUS`를 `can(viewer, key, "view")`로 걸러 allowedMenus를 만들고(같은 계산을 두 곳에서 한다는 주석 한 줄 — 사용자 10~30명이라 최적화하지 않는다), `adminIndexGroups({ roleId: viewer.roleId ?? "", allowedMenus })`를 부른다. 결과가 빈 배열이면 `notFound()`(D-17 — 권한 없는 리소스는 404). 렌더는 `<PageHeader title="관리" />` 다음에 그룹마다 `<h2>` 그룹 라벨 + `<ul>` 안의 `<a href>` 항목 행. 새 토큰·색·서체·radius를 만들지 않는다.

7단계 — `app/(app)/admin/admin-index.module.css` 신설. `ui/shell/MoreSheet.module.css`의 `.list`·`.list li`·`.link`(+ hover·focus-visible)·`.group` 규칙을 그 토큰 그대로 옮겨 쓴다(`--touch-min`·`--line-w`·`--line`·`--fs-base`·`--fs-xs`·`--fw-medium`·`--faint`·`--pad-page`·`--s-*`·`--focus`). 값을 새로 지어내지 않는다 — 토큰 이름이 기억나지 않으면 그 파일을 열어 복사한다.

8단계 — `test/e2e/admin-nav.spec.ts`를 새 계약으로 고친다(파일을 새로 만들지 않고 이 파일만 고친다). 세 테스트: ① 사용자 메뉴의 「관리」를 눌러 `/admin`에 닿고(주소 직접 입력 금지), 거기서 「코드표」를 눌러 `/admin/code-tables`에 닿는다(기존 화면 제목·「코드표 선택」 nav 단언 유지) ② PC 사용자 메뉴에는 「관리」 항목이 보이고, 관리자 화면 개별 라벨 10개는 메뉴 안에 하나도 없다(각 라벨 `exact: true`로 count 0; 기존 「설정」 count 0 단언 유지) ③ `/admin` 인덱스에 그룹 머리글 셋(마스터·설정·권한·운영 기록)과 항목 링크 10개가 전부 보인다. 파일 머리 주석도 새 계약을 설명하도록 고친다.

9단계 — GREEN 확인: `pnpm exec vitest run --project unit test/unit/ui/role-menu.test.ts` 통과, 이어서 `pnpm lint && pnpm typecheck`.
  </action>
  <verify>
    <automated>cd /home/user/ERP_PLANT8_260917 && pnpm exec vitest run --project unit test/unit/ui/role-menu.test.ts && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>RED 출력을 인용해 기록했고, 이후 role-menu 단위 테스트·typecheck·lint가 전부 통과한다. `app/(app)/admin/page.tsx`와 `admin-index.module.css`가 존재하고, role-menu.ts가 `adminIndexGroups`를 내보낸다. MoreSheet에 「관리자」 그룹 머리글이 없다. 커밋 하나.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A-M3 — 읽기용 표 6종 caption + 코드표 th scope</name>
  <files>test/unit/ui/admin-table-caption.test.ts, app/(app)/admin/code-tables/page.tsx, app/(app)/admin/corp-cards/page.tsx, app/(app)/admin/people/page.tsx, app/(app)/admin/people/roles/roles-client.tsx, app/(app)/admin/vendors/page.tsx, ui/history-list/HistoryList.tsx, app/(app)/admin/settings/settings-form-client.tsx, app/(app)/admin/people/[id]/person-detail-client.tsx</files>
  <read_first>test/unit/ui/admin-master-list-first.test.ts 1~45행(소스 단언 테스트 관례); app/(app)/admin/action-log/page.tsx 104~118행(이미 있는 caption 선례); app/globals.css 55~70행(.sr-only); ui/history-list/HistoryList.tsx 28~48행(props)·215~228행(table 머리); app/(app)/admin/settings/settings-form-client.tsx 193~205행; app/(app)/admin/people/[id]/person-detail-client.tsx 50~58행</read_first>
  <behavior>
    - 여섯 파일 각각의 `<table>` 첫 자식이 `<caption>`이고, 그 caption이 전역 유틸리티로 시각적으로 숨겨져 있다
    - caption 문자열: 코드표 / 법인카드 / 사람 / 계급 / 거래처 (각 화면 PageHeader 제목과 같다), HistoryList는 호출부가 넘긴 값
    - 코드표 화면의 모든 `<th`에 `scope="col"`이 있다(머리글 다섯 칸 전부)
    - HistoryList는 `caption` prop을 필수로 받는다(호출부마다 다른 표라 컴포넌트가 이름을 지어낼 수 없다)
  </behavior>
  <action>
**테스트 먼저, RED 확인 후 구현.**

1단계 — `test/unit/ui/admin-table-caption.test.ts`를 만든다. `test/unit/ui/admin-master-list-first.test.ts`와 같은 방식(소스 문자열 단언 — vitest unit은 node 환경이라 RTL이 없다). 대상 여섯 파일과 기대 caption 문자열을 표로 두고: ① 각 소스에 `<caption` 이 있다 ② caption 요소에 시각 숨김 클래스가 붙어 있다 ③ 기대 문자열이 caption 안에 있다(HistoryList는 prop 표현식이므로 caption 값이 아니라 `{caption}` 렌더와 `caption:` prop 선언을 단언한다) ④ 코드표 소스에서 `<th`로 시작하는 태그 수와 `<th scope="col"` 수가 같다(`<thead`는 단어 경계로 걸리지 않게 `<th[\s>]` 꼴로 센다) ⑤ HistoryList 호출부 두 곳이 caption prop을 넘긴다. 파일 머리에 A-M3 출처(`.planning/phases/03-permissions-settings-masters/03-OPEN-ITEMS.md`)와 §10 접근성 계약을 적는다.

2단계 — RED 확인: `pnpm exec vitest run --project unit test/unit/ui/admin-table-caption.test.ts`. **실패 출력을 그대로 인용해 기록한다.**

3단계 — 구현. 시각 숨김은 `app/globals.css`에 이미 있는 전역 `.sr-only`를 쓴다 — 새 CSS 규칙을 만들지 않고, 모듈 지역 복제본도 새로 만들지 않는다.
  - `app/(app)/admin/code-tables/page.tsx`: `<table>` 바로 안에 `<caption className="sr-only">코드표</caption>`를 넣고, 머리글 `<th>` 다섯 개에 전부 `scope="col"`을 더한다(조건부로 렌더되는 「동작」 칸 포함).
  - `app/(app)/admin/corp-cards/page.tsx`: caption `법인카드`.
  - `app/(app)/admin/people/page.tsx`: caption `사람`.
  - `app/(app)/admin/people/roles/roles-client.tsx`: caption `계급`.
  - `app/(app)/admin/vendors/page.tsx`: caption `거래처`.
  - `ui/history-list/HistoryList.tsx`: `HistoryListProps`에 필수 `caption: string`을 더하고(짧은 주석 — 한 화면에 여러 개가 렌더되므로 호출부가 이름을 준다, `idPrefix`와 같은 이유), `<table>` 첫 자식으로 `<caption className="sr-only">{caption}</caption>`를 렌더한다. `entries.length === 0` 조기 반환(ListEmpty) 경로는 건드리지 않는다.
  - 호출부 둘: `app/(app)/admin/settings/settings-form-client.tsx`는 그 필드의 `label`을 써서 이력 표 이름을 만든다(예: 라벨 + ` 이력`), `app/(app)/admin/people/[id]/person-detail-client.tsx`는 `소속 발령 이력`.
  이미 caption이 있는 표(행동 로그·보관함·권한 그리드)는 손대지 않는다. 각 파일에서 caption·scope 말고는 아무것도 바꾸지 않는다.

4단계 — GREEN: 새 테스트 통과.
  </action>
  <verify>
    <automated>cd /home/user/ERP_PLANT8_260917 && pnpm exec vitest run --project unit test/unit/ui/admin-table-caption.test.ts && pnpm typecheck</automated>
  </verify>
  <done>RED 출력을 인용해 기록했고, 이후 새 단위 테스트와 typecheck가 통과한다. 여섯 표에 시각적으로 숨긴 caption이 있고 코드표의 모든 th에 scope가 있다. 커밋 하나.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 서버 컴포넌트(`/admin`) | 인증된 사용자가 관리자 인덱스를 요청한다. 계급별로 보여 줄 항목이 다르다 |
| 셸 렌더 데이터(allowedMenus) → 클라이언트 번들 | `roleMenu()` 결과가 클라이언트 컴포넌트(TopBar·MoreSheet) props로 직렬화되어 브라우저에 간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-i3k-01 | Information Disclosure | `app/(app)/admin/page.tsx` | medium | mitigate | 볼 권한이 있는 항목만 서버가 만든다 — `MENUS`를 `can(viewer, key, "view")`로 걸러 얻은 allowedMenus로 `adminIndexGroups`를 부르고, CSS·클라이언트 분기로 숨기지 않는다(§6-10·§7-3 「칸 수 가변」과 같은 원칙). 단위 테스트가 "빈 그룹은 배열에 없다"를 고정한다 |
| T-i3k-02 | Elevation of Privilege | `/admin` 라우트 | medium | mitigate | 인덱스는 링크만 준다 — 각 관리자 화면의 자체 `can()`·`notFound()` 게이트는 그대로다. 볼 항목이 0개면 인덱스 자체가 `notFound()`(D-17: 권한 없는 리소스는 404, 존재를 알리지 않는다) |
| T-i3k-03 | Information Disclosure | `ui/shell/role-menu.ts` → 클라이언트 props | low | mitigate | adminMenu가 `{label:"관리", href:"/admin"}` 한 줄로 접히면서 브라우저로 나가는 관리자 화면 이름·경로 목록이 오히려 10개에서 0개로 줄어든다(노출면 축소). 인덱스 항목 계산은 서버 컴포넌트 안에서만 일어난다 |
| T-i3k-04 | Tampering | 패키지 설치 | n/a | accept | 이 작업은 의존성을 추가·변경하지 않는다(새 의존성 금지). 설치 태스크가 없으므로 패키지 적법성 게이트 대상이 아니다 |

ASVS 레벨 1, 차단 임계값 high — 위 항목 중 high/critical 없음.
</threat_model>

<verification>
전체 게이트는 **마지막에 한 번만** 돌린다(CLAUDE.md 화면 검증 순서 — 싼 게이트를 태스크마다 좁게 돌리고, 전체 게이트는 한 번). 태스크 3까지 끝난 뒤:

1. `pnpm lint` · `pnpm typecheck` · `pnpm test:unit` — 전부 통과.
2. `CI=true pnpm build` — 프로덕션 빌드 통과.
3. E2E: **로컬 DB가 필요하다**(`pnpm db:dev`). DB가 있으면 `pnpm test:e2e:ci`(= `pnpm db:reset:test && CI=true pnpm test:e2e`)를 돌려 `admin-nav.spec.ts`·`user-menu.spec.ts`·`system-status.spec.ts`가 통과하는지 본다. DB를 띄울 수 없으면 **통과했다고 쓰지 않는다** — SUMMARY에 "E2E 미실행(로컬 DB 없음)"과 실행해야 할 정확한 명령을 남기고 사용자에게 보고한다.
4. 통합 테스트(`pnpm test:integration`)도 DB가 필요하다 — 이 작업은 액션·리포지토리·스키마를 건드리지 않으므로 영향이 없다고 보지만, DB가 있으면 같은 실행에서 함께 돌린다.

DOM 감사·시각 검증은 이 계획의 일이 아니다 — Post-build의 `/review` → `/qa` → `/design-review`가 받는다.
</verification>

<success_criteria>
- 관리자 10개를 볼 수 있는 계급의 PC 사용자 메뉴가 「관리 · 내 정보 · 로그아웃」 세 줄이다(전에는 13줄).
- 「더보기」 시트에 「관리」 한 줄이 있고 「관리자」 그룹 머리글이 없다.
- `/admin`이 세 그룹으로 10개를 보여주고, 클릭만으로 각 관리자 화면에 닿는다(E2E가 증명).
- 관리자 메뉴를 하나도 볼 수 없는 계급에게 「관리」 줄이 없고 `/admin`이 404다.
- SYSTEM.md §6-10이 그룹·순서·필터·다섯 상태를 규정하고, 단위 테스트가 그 표를 읽어 코드와 대조한다.
- DECISIONS.md에 왜 시스템을 이 방향으로 고쳤는지가 남아 있다.
- 관리자 읽기용 표 6종에 caption이 있고 코드표 th에 scope가 있다(A-M3 종료).
- 커밋 3개, 각각 한 의도. 새 의존성 0, 새 토큰·색·서체·radius 0.
</success_criteria>

<output>
`.planning/quick/260922-i3k-b-admin-3-a-m3/260922-i3k-SUMMARY.md`를 쓴다. 두 RED 출력 인용(태스크 2·3), E2E 실행 여부, `.planning/phases/03-permissions-settings-masters/03-OPEN-ITEMS.md`의 A-M3와 `.planning/phases/04-project-quote-ledger/04-OPEN-ITEMS.md`의 A-M3 행을 닫을 수 있게 된 사실을 적는다(파일 수정은 하지 않는다 — `.planning/` 수동 편집 금지).
</output>
