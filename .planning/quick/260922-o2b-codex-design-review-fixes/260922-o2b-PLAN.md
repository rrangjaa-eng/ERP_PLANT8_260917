---
phase: quick-260922-o2b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/DECISIONS.md
  - docs/design/SYSTEM.md
  - docs/design/tokens.css
  - app/globals.css
  - app/(app)/account/page.tsx
  - test/unit/ui/single-column.test.ts
  - test/e2e/single-column.spec.ts
  - app/(app)/admin/page.tsx
  - app/(app)/admin/settings/page.tsx
  - app/(app)/admin/people/[id]/page.tsx
  - app/(app)/admin/people/org/page.tsx
  - app/(app)/admin/system-status/page.tsx
  - app/(app)/admin/vendors/vendor-form.tsx
  - app/(app)/admin/corp-cards/card-form.tsx
  - app/(app)/admin/code-tables/code-item-form.tsx
  - app/(app)/admin/people/person-form.tsx
  - app/(app)/admin/people/roles/roles-client.tsx
  - app/(app)/admin/code-tables/page.tsx
  - app/(app)/admin/code-tables/code-tables.module.css
  - app/(app)/admin/people/people.module.css
  - ui/history-list/HistoryList.tsx
  - ui/history-list/HistoryList.module.css
  - app/(app)/admin/vendors/page.tsx
  - app/(app)/admin/corp-cards/page.tsx
  - app/(app)/admin/people/page.tsx
  - test/unit/ui/system-md-compliance.test.ts
  - test/e2e/code-tables.spec.ts
  - test/e2e/master-edit.spec.ts
  - ui/shell/MoreSheet.module.css
  - ui/shell/TopBar.module.css
  - app/(app)/admin/settings/settings.module.css
  - ui/auth-frame/AuthFrame.module.css
  - app/(app)/account/change-password-form.tsx
  - app/(app)/account/account.module.css
  - test/e2e/mobile-shell.spec.ts
  - test/e2e/mobile-page-chrome.spec.ts
  - test/e2e/page-chrome.spec.ts
autonomous: true
requirements: [QUICK-260922-o2b, F-02, F-04, F-05, F-07, F-08, F-09, F-10]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "1280px에서 /account·/admin·/admin/settings·/admin/people/[id]·/admin/people/org·/admin/system-status의 한 열 콘텐츠와 관리자 마스터 등록·수정 폼(거래처·법인카드·코드표·사람·계급·조직)은 폭 720px 이하이고 화면 제목(main h1)과 같은 x에서 시작한다"
    - "같은 1280px에서 데이터 표(/admin/code-tables 목록 표)는 여전히 720px보다 넓다 — 규칙이 표로 번지지 않는다"
    - "SYSTEM.md §2-3·§3이 단일 기둥 최대 폭 규칙(적용 목록·제외 목록·폰 동작·행동 줄 폭·구현 클래스)을 다른 절에 「같은 결」로 떠넘기지 않고 그 자리에 전문으로 적는다. DECISIONS.md에 이유와 버린 대안이 있고, 새 토큰 없이 --form-max를 쓴다"
    - "375px에서 「더보기」 시트의 아래 끝이 뷰포트 아래 끝(800)에 붙고, 위 끝은 0보다 아래에 있다"
    - "375px에서 상단 바 사용자 메뉴 트리거가 44×44 이상이고 상단 바 밖으로 넘치지 않는다. /admin/settings의 체크박스 라벨은 높이 44 이상이고 체크박스 glyph는 20×20 그대로다"
    - "/login 폼 틀이 360px 이하로 가운데 정렬되고, 그 폭이 --auth-max 토큰에서 온다(DECISIONS.md 기록 · SYSTEM.md §6-7 참조 · tokens.css 정의)"
    - "/account 「비밀번호 변경」 h2가 font-size 18px · font-weight 700으로 계산된다(브라우저 기본값 21px이 아니다)"
    - "코드표·계급의 정렬 칸(머리글 포함)과 숫자형 이력 값 칸이 우측 정렬·tabular-nums·nowrap이고, 거래처·법인카드·코드표·사람 목록의 정상 상태 칸과 계급의 비시드 칸이 빈칸 대신 —를 보인다"
  artifacts:
    - "docs/design/DECISIONS.md — 2026-09-22 항목 2건(단일 기둥 최대 폭 · 로그인 틀 --auth-max)"
    - "docs/design/SYSTEM.md — §2-3 줄 길이 줄 교체, §3 「단일 기둥 최대 폭」 신설, §6-7 틀 폭 줄 추가"
    - "docs/design/tokens.css — --form-max 주석 확장, --auth-max: 360px 신설"
    - "app/globals.css — 전역 유틸리티 .single-column"
    - "app/(app)/account/account.module.css — .sectionTitle"
    - "test/unit/ui/single-column.test.ts · test/e2e/single-column.spec.ts (신설)"
  key_links:
    - "app/globals.css .single-column → 적용 화면·폼의 className=\"single-column\" (single-column.test.ts의 TARGETS가 파일 단위로 대조, single-column.spec.ts가 DOM 폭으로 대조)"
    - "SYSTEM.md §6-7의 --auth-max ↔ tokens.css 정의 (design-system-docs.test.ts 「SYSTEM.md가 참조하는 커스텀 속성이 전부 실재한다」가 대조)"
    - "ui/auth-frame/AuthFrame.module.css .frame → max-width: var(--auth-max)"
    - "test/e2e/master-edit.spec.ts의 소유 칸 단언 → 행 전체가 아니라 소유 열 하나로 한정(정상 상태 칸의 —와 분리)"
---

<objective>
독립(Codex) 전체 앱 디자인 리뷰(`260922-o2b-CODEX-REVIEW.md`) 가운데 **사용자 결정이 필요 없는 부분**만 반영한다. 모두 이미 SYSTEM.md에 적힌 규칙이거나(F-04·F-05·F-07·F-08·F-09·F-10), 적힌 규칙 사이의 공백을 기존 토큰으로 메우는 것이다(F-02).

목적: Phase 4(프로젝트·견적 원장)가 새 화면을 만들기 전에 전역 규칙의 공백(단일 기둥 최대 폭)을 닫고, 지금 관리자 화면에 있는 위반을 고친다. 그래야 새 화면이 이 습관을 따라 하지 않는다. `.planning/todos/pending/2026-09-22-single-column-max-width.md`를 해소한다.

산출물: SYSTEM.md·DECISIONS.md·tokens.css 개정, 전역 `.single-column` 유틸리티와 11개 화면·폼 적용, 셸·로그인·내 계정·관리자 표의 규칙 위반 6종 수정, 각 수정의 단위·E2E 회귀 테스트.
</objective>

<planning_findings>
계획 전에 리뷰의 주장을 소스와 SYSTEM.md에 하나씩 대조했다. 결과:

| ID | 판정 | 근거 |
|---|---|---|
| F-02 | **유지·확장** | 확인됨. `--form-max`를 쓰는 곳이 한 군데도 없다(`grep`: max-width는 셸 main과 AuthFrame뿐). §2-3 「표·폼은 컨테이너 폭」과 §6-3 「한 열, max 720」이 서로 모순이었고, 폼은 이미 720이 정답이었다. **규칙이 폼·`dl`을 덮으므로** 리뷰가 적은 다섯 화면 말고 `/admin/system-status`(`dl`)와 관리자 마스터 등록·수정 폼 6종(거래처·법인카드 등록/소유자 변경·코드표·사람과 그 결과 패널·계급)에도 적용한다. 규칙을 세워 놓고 곧바로 어긴 화면을 남기는 것은 CLAUDE.md의 「화면 하나만 예외 금지」에 걸린다 |
| F-04 | 유지 | 확인됨. `.sheet`가 `inset-block-end:0`만 준다. 브라우저가 `dialog:modal`에 기본으로 주는 `inset-block: 0` 가운데 위쪽 값이 남아 있고, `max-height:85vh`·`margin:0`에서 값이 겹치면 top이 이긴다. 그래서 시트가 y=0에 붙는다 |
| F-05 부분 | 유지 | 트리거가 `padding:0`·`--fs-sm`이라 약 19px이다. 설정 체크박스 라벨에는 최소 높이가 없다. §3·§10(폰 44×44)과 §7-2 boolean(「라벨 자체가 클릭 영역」)이 근거다. 전역 `--control-h` 40→44는 **범위 밖**이다 |
| F-09 | 유지(새 토큰) | 확인됨. §6-7은 360인데 AuthFrame이 `--modal-w`(480)를 재사용하고, DECISIONS 기록이 없다. `tokens.css` 머리말에 「컴포넌트는 이 변수만 참조한다. 새 값이 필요하면 DECISIONS.md → SYSTEM.md → 여기」라고 적혀 있고, 레이아웃 폭(`--container-max`·`--label-w`·`--form-max`·`--modal-w`)도 전부 토큰이다. 그래서 360에도 토큰이 필요하다. 값 자체는 새것이 아니다 — SYSTEM.md가 이미 확정한 값에 이름을 붙이는 것이다(`--on-accent-weak` 선례) |
| F-10 | 유지 | 확인됨. `change-password-form.tsx`의 `<h2>`에 클래스가 없고 globals.css에도 h2 규칙이 없다. 그래서 브라우저 기본값 1.5em이 적용된다 |
| F-07 부분 | 유지 | 코드표·계급의 `sortOrder` `<td>`에 클래스가 없다. §2-4 「모든 숫자 칸은 우측 정렬, tabular-nums, nowrap」이 근거다. 숫자형 이력 값(부가세율 등 historized number)도 §7-14 「값은 §7-2 매핑으로」와 §7-2 number 「우측 정렬 tabular-nums」가 명시하므로 포함한다. 세 모듈 모두 `.table th { text-align: left }`(0,1,1)가 있어 **`.table .num`(0,2,0) 선택자여야 머리글까지 이긴다** |
| F-07 행동 로그 날짜 | **제외** | §2-4가 같은 해 날짜는 `09-18`, 시각은 `14:00`으로 규정하는 것은 맞다. 그런데 셀이 `toISOString()`으로 **UTC**를 찍고 있고, 프로젝트에 표시 시간대 정책이 없다(`Asia/Seoul`·KST가 코드·문서 어디에도 없다). 형식만 바꾸면 UTC가 굳고, KST로 바꾸는 것과 감사 로그에서 초를 버리는 것은 둘 다 사용자 결정이다. UTC 표시는 별도 결함으로 보고한다 |
| F-08 | 유지(`—`) | §2-4 「값이 없으면 `—` 하나. 빈칸을 두지 않는다」가 빈 상태 칸을 금지한다. SYSTEM.md가 「예외만 보인다」고 정한 곳은 없다. 채울 값은 리뷰가 제안한 `활성`/`사용 중`이 아니라 **`—`**다 — 새 상태 낱말은 카피 결정이고 UI-SPEC의 상태 낱말(`비활성`·`숨김`·`보관됨`)에도 없다. 같은 파일(roles-client)의 계급 「시드 여부」 빈칸(`""`)도 같은 규칙이라 함께 고친다 |
| F-01·F-03·F-06·폰 `--control-h` | 범위 밖 | 사용자 결정 대기(지시대로 건드리지 않는다) |

**부수 발견 — 결함 하나가 확정적이다**: `test/e2e/master-edit.spec.ts:91`은 법인카드 행에서 `name: "—"` 칸이 0개라고 단언한다(소유 칸이 `—`로 남는 결함 3의 회귀 방지). F-08 뒤에는 정상 상태 칸이 `—`이 되므로 이 단언이 깨진다. 테스트의 의도(소유 칸)에 맞게 소유 열 하나로 좁힌다(태스크 2).

**관찰만 하고 고치지 않는 것**(보고만): ① §6-3 칸 폭(select 200·짧은 칸 280·긴 칸 480)이 어디에도 적용되지 않았다. F-02 뒤에 입력칸은 1136px에서 720px 안쪽으로 줄지만 칸 폭 규칙은 여전히 어긴다. 공용 TextField를 건드리는 별도 작업이다. ② 비율 이력 값이 `0.1`로 보인다(§2-4 비율은 `10.0%`). 표시 형식 결정이 필요하다. ③ 이력 목록의 과거 행 상태 칸이 비어 있다. §7-14는 `적용 중`·`예정`만 정의한다. ④ 투두 곁가지 FINDING-003(`/admin` `.link`의 죽은 `gap`)은 그대로 둔다.
</planning_findings>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260922-o2b-codex-design-review-fixes/260922-o2b-CODEX-REVIEW.md
@.planning/todos/pending/2026-09-22-single-column-max-width.md

실행 규율(CLAUDE.md):
- 각 태스크의 구현 전에 Superpowers `test-driven-development` 스킬을 호출한다. 예상하지 못한 테스트 실패는 `systematic-debugging` 스킬로 쫓는다. SUMMARY에 「완료」를 쓰기 전에 `verification-before-completion` 스킬을 호출한다.
- 디자인 변경 순서는 **DECISIONS.md → SYSTEM.md → tokens.css → 적용**이다. 새 색·서체·radius·그림자는 0개다. 새 토큰은 `--auth-max` 하나이고 폭 토큰이다(F-09, DECISIONS에 기록).
- Next.js 16 API는 쓰지 않는다 — className·CSS·JSX 텍스트만 바꾼다.
- 커밋 하나에 의도 하나. 제목은 영어 접두어(`docs:`/`feat:`/`fix:`)와 짧은 요약, 본문은 한국어로 쓴다. 새 의존성은 0개다.
- E2E를 로컬에서 돌리려면 `pnpm db:dev`가 떠 있어야 한다. 태스크 안의 E2E는 dev 서버로 도는 **좁은 확인**이다. 로컬 dev에서 통과해도 완료 신호가 아니다 — 완료 판정은 아래 `<verification>`의 CI=true 전체 게이트가 한다.
- SYSTEM.md는 991줄이다. 범위를 지정해 읽는다: §2-2~§3은 134–188행, §6-3은 374–407행, §6-7은 527–557행, §7-2는 667–700행, §7-3은 701–736행, §7-8은 787–797행, §7-14는 888–920행.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 단일 기둥 최대 폭 — DECISIONS → SYSTEM → 토큰 주석 → 전역 클래스 → /account 한 줄 관통 (F-02 트레이서)</name>
  <files>docs/design/DECISIONS.md, docs/design/SYSTEM.md, docs/design/tokens.css, app/globals.css, app/(app)/account/page.tsx, test/unit/ui/single-column.test.ts, test/e2e/single-column.spec.ts</files>
  <precondition>로컬 Postgres가 떠 있다 — `pnpm db:dev`가 0으로 끝난다(이 트레이서의 end-to-end 검증은 E2E DB erp_test를 쓴다)</precondition>
  <read_first>docs/design/SYSTEM.md 149–188행(§2-3·§3), 374–400행(§6-3 「한 열, max 720」 · 「머리 줄도 폼 열 720」). docs/design/DECISIONS.md 396–437행(형식 예: `---` 구분선, `## 날짜 — 제목`, **결정**/**왜 …**/**버린 대안**/**범위**). docs/design/tokens.css 110–120행. app/globals.css 55–70행(`.sr-only` — 전역 유틸리티 선례). test/unit/ui/system-md-compliance.test.ts 1–25행(소스 단언 방식). test/e2e/page-chrome.spec.ts 1–25행(loginAs 도우미).</read_first>
  <behavior>
    - 단위(single-column.test.ts, node 환경 소스 단언): SYSTEM.md에 「단일 기둥 최대 폭」 절이 있고 그 안에 `max-width: var(--form-max)`와 `.single-column`이 있다. SYSTEM.md의 「- 줄 길이:」 줄이 「체크박스 매트릭스」와 `--form-max`를 말한다. DECISIONS.md에 `## 2026-09-22 — 단일 기둥 최대 폭` 머리글이 있다. tokens.css의 `--form-max:` 줄 주석에 「단일 기둥」이 있다. globals.css의 `.single-column { … }` 블록에 `max-width: var(--form-max)`가 있다. TARGETS 배열(이 태스크에서는 `app/(app)/account/page.tsx` 하나)의 각 파일에 `single-column`이 있다
    - E2E(single-column.spec.ts, desktop 프로젝트): 뷰포트 폭이 1280임을 먼저 단언한다(기본값이 바뀌면 테스트가 헛돌지 않게). 직원 픽스처로 로그인해 /account에서 「현재 비밀번호」 입력을 가진 form의 폭 ≤ 720(그리고 > 0), 「현재 비밀번호」 입력 폭 ≤ 720, form의 x가 `main h1`의 x와 1px 안에서 같다(왼쪽 정렬)
  </behavior>
  <action>
1. RED. 위 behavior대로 `test/unit/ui/single-column.test.ts`와 `test/e2e/single-column.spec.ts`를 새로 만든다. 단위 테스트는 system-md-compliance.test.ts처럼 `readFileSync`로 소스를 읽는다. TARGETS는 태스크 2가 늘릴 배열 상수로 둔다. E2E의 로그인 도우미는 page-chrome.spec.ts의 loginAs(`createFixtureUser` + 로그인 폼)와 같은 모양으로 파일 안에 두고, 폭·x 비교는 `expectSingleColumn(page, locator)` 도우미 하나로 묶는다(태스크 2가 재사용한다). 두 테스트를 돌려 **실패를 확인하고 그 출력을 SUMMARY에 인용한다**(E2E는 form 폭 약 1240에서 실패해야 한다).
2. `docs/design/DECISIONS.md` 끝에 기존 형식대로(`---` 뒤에) `## 2026-09-22 — 단일 기둥 최대 폭: 기존 --form-max(720)를 폼 밖 한 열 콘텐츠까지 넓힌다(새 토큰 없음)`을 추가한다. **결정**: 가로 비교가 목적이 아닌 한 열 콘텐츠는 `--form-max`에서 멈추고 왼쪽에 붙는다. 전역 유틸리티 `.single-column` 하나로 건다. **왜**: ① 1280에서 `/admin` 링크 행의 92%가 빈 들판이었고, `/account` 입력은 1136px, 설정 섹션은 1240px로 늘어났다(/design-review 260922-i3k FINDING-002, Codex 전체 리뷰 F-02). ② §2-3 「표·폼은 컨테이너 폭」과 §6-3 「한 열, max 720」이 서로 어긋나 있었다. 폼은 이미 720이 정답이었고, §2-3이 틀린 쪽이다. ③ 한 칸짜리 링크 목록·`dl`·설정 묶음은 산문도 표도 폼도 아니어서 어느 조항도 덮지 않았다. **버린 대안**: 목록 전용 새 토큰(`--list-max` 등) — 같은 일을 하는 폭 토큰이 둘이 된다. `/admin`만 좁히기 — 화면 단위 예외 금지. 가운데 정렬 — §3 「왼쪽 정렬(원장은 왼쪽에서 시작)」. 화면별 모듈 CSS마다 `max-width` — 규칙이 흩어져 다음 화면이 빠뜨린다. 새 컴포넌트 — JS가 필요 없는 폭 하나라 `.sr-only`처럼 전역 유틸리티가 가장 싸다. **범위**: SYSTEM.md §2-3·§3, tokens.css(주석만), app/globals.css, 적용 화면 목록(태스크 1·2의 파일).
3. `docs/design/SYSTEM.md` §2-3의 「- 줄 길이:」 줄(155행)을 다음으로 바꾼다: 「- 줄 길이: 산문(안내·설정 설명)은 `max-width: 64ch`. 데이터 표·체크박스 매트릭스·대시보드는 컨테이너 폭을 따른다. 폼·설정 필드 묶음·`dl` 라벨·값 목록·계층 목록·한 칸짜리 링크 목록처럼 가로 비교가 목적이 아닌 한 열 콘텐츠는 `max-width: var(--form-max)`(720px)이고 컨테이너 왼쪽에 붙는다 — 적용·제외 목록 전문은 §3 「단일 기둥 최대 폭」」. §3의 「- 컨테이너:」 줄(182행) 바로 아래에 새 항목을 넣는다. 첫 줄: 「- **단일 기둥 최대 폭**(2026-09-22 `DECISIONS.md`): 가로 비교가 목적이 아닌 한 열 콘텐츠는 `width: 100%; max-width: var(--form-max)`(720px)이고 컨테이너 **왼쪽**에 붙는다(가운데 정렬하지 않는다). 폰(<700px)은 가용 폭이 720보다 좁아 자연히 전폭이 된다. 새 토큰 없이 `--form-max` 하나를 폼 밖까지 넓혀 쓴다」. 그 아래에 하위 항목 다섯 개를 둔다. (a) 「적용: 폼(§6-3 폼 화면, 내 계정, 관리자 마스터 화면의 등록·수정 폼과 그 결과 패널) · 설정 필드 묶음(§7-2 자동 생성 설정 화면의 섹션 전체 — 2px 섹션 선과 그 안의 §7-14 이력 목록 포함) · `dl` 라벨·값 목록(§6-8 시스템 상태, 사람 상세) · 계층 목록(조직의 본부·팀) · 한 칸짜리 링크 목록(§6-10 「관리」 인덱스)」 (b) 「제외 — 컨테이너 폭 그대로: 데이터 표(§7-3 — 원장·관리자 마스터 목록), 체크박스 매트릭스(§7-13), 대시보드(§6-4), 표 자리의 EMPTY 한 줄(§7-7), 배너(§7-11), 화면 제목 줄(`PageHeader` — 제목·부제만 있는 줄은 폭이 보이지 않는다). 한 화면에 표와 폼이 함께 있으면 폼만 720이고 표는 컨테이너 폭이다」 (c) 「목록 머리글의 필터·등록 행동 줄(§6-1)은 그 아래 콘텐츠와 같은 폭이다 — 표 위면 컨테이너 폭, 한 열 목록(조직) 위면 720」 (d) 「제목 줄 오른쪽에 요소(상태·버튼)가 붙는 폼 화면은 §6-3 「머리 줄도 폼 열 720에 맞춘다」를 따른다」 (e) 「구현: `app/globals.css`의 전역 유틸리티 `.single-column` 하나로 건다(`.sr-only`와 같은 자리). 적용 대상 묶음의 바깥 요소 — 폼이면 `<form>` 자신 — 에 `className="single-column"`을 붙인다. 화면별 모듈 CSS에 같은 `max-width`를 다시 쓰지 않는다」. 이 규칙은 다른 절에 「같은 결」로 떠넘기지 않고 그 자리에 전부 적는다(지시 사항 — 위임 문장은 새어 나간다).
4. `docs/design/tokens.css`는 **주석만** 바꾼다. `--form-max: 720px;` 줄 끝에 「단일 기둥 최대 폭: 폼·설정 필드 묶음·dl·계층/링크 목록(SYSTEM.md §3). 표·매트릭스·대시보드 제외」 주석을 붙인다. 값과 다른 줄은 그대로 둔다.
5. `app/globals.css`에서 `.sr-only` 블록 뒤에 `.single-column { width: 100%; max-width: var(--form-max); }`를 추가한다. 머리 주석에는 SYSTEM.md §3 「단일 기둥 최대 폭」과 「표·매트릭스·대시보드에는 붙이지 않는다」를 적는다.
6. `app/(app)/account/page.tsx`에서 Banner와 PageHeader는 밖에 두고, 그 아래의 이름 문단·`ChangePasswordForm`·`LogoutButton`을 `<div className="single-column">` 하나로 감싼다.
7. GREEN. 단위 테스트, design-system-docs.test.ts(토큰 실재 대조), lint, typecheck, E2E를 돌려 통과를 확인한다.
8. 커밋 둘. `docs: record single-column max-width rule`(DECISIONS·SYSTEM·tokens)과 `feat: add single-column utility and apply to account`(globals·account·테스트 둘). 본문은 한국어로, Codex F-02와 투두 해소를 적는다.
  </action>
  <verify>
    <automated>cd /home/user/ERP_PLANT8_260917 && pnpm exec vitest run --project unit test/unit/ui/single-column.test.ts test/unit/design-system-docs.test.ts && pnpm lint && pnpm typecheck && pnpm exec playwright test test/e2e/single-column.spec.ts --project=desktop</automated>
  </verify>
  <done>1280에서 /account 비밀번호 폼이 720px 이하로 main h1과 같은 x에서 시작한다(E2E 통과). SYSTEM.md §2-3·§3에 규칙 전문이 있고 DECISIONS.md에 이유·버린 대안이 있으며 tokens.css는 주석만 바뀌었다(단위 테스트 통과). lint·typecheck 통과. RED 출력 둘을 SUMMARY에 인용했다. 커밋 2개.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 관리자 화면 — 단일 기둥 규칙 전면 적용(F-02) + 숫자 칸 정렬(F-07) + 빈 상태 칸 `—`(F-08)</name>
  <files>app/(app)/admin/page.tsx, app/(app)/admin/settings/page.tsx, app/(app)/admin/people/[id]/page.tsx, app/(app)/admin/people/org/page.tsx, app/(app)/admin/system-status/page.tsx, app/(app)/admin/vendors/vendor-form.tsx, app/(app)/admin/corp-cards/card-form.tsx, app/(app)/admin/code-tables/code-item-form.tsx, app/(app)/admin/people/person-form.tsx, app/(app)/admin/people/roles/roles-client.tsx, app/(app)/admin/code-tables/page.tsx, app/(app)/admin/code-tables/code-tables.module.css, app/(app)/admin/people/people.module.css, ui/history-list/HistoryList.tsx, ui/history-list/HistoryList.module.css, app/(app)/admin/vendors/page.tsx, app/(app)/admin/corp-cards/page.tsx, app/(app)/admin/people/page.tsx, test/unit/ui/single-column.test.ts, test/unit/ui/system-md-compliance.test.ts, test/e2e/single-column.spec.ts, test/e2e/code-tables.spec.ts, test/e2e/master-edit.spec.ts</files>
  <read_first>각 대상 파일은 Grep으로 자리를 찾고 그 범위만 읽는다: admin/page.tsx 34–52행(groups.map), settings/page.tsx 88–93행, people/[id]/page.tsx 63–84행, org/page.tsx 34–39행, system-status/page.tsx 28–73행, 각 폼의 루트 `<form … id="…">` 줄(vendor-form 140, card-form 60·171, code-item-form 47, person-form 61·76, roles-client 101), code-tables/page.tsx 118–158행, roles-client.tsx 34–60·126–142행, HistoryList.tsx 226–260행, 세 모듈의 `.table th` 블록(code-tables.module.css 40행대, people.module.css 46행대, HistoryList.module.css 14행대), corp-cards.module.css 60–65행(`.last4` — 숫자 칸 선례), master-edit.spec.ts 60–92행, code-tables.spec.ts 1–28행.</read_first>
  <behavior>
    - 단위 single-column.test.ts: TARGETS에 파일 10개(위 files 목록의 앞 10개)를 더한다. 각 파일에 `single-column`이 있다
    - E2E single-column.spec.ts(시스템 관리자 픽스처, 1280): /admin의 `main ul` 전부 · /admin/settings의 `main section` 전부 · /admin/system-status의 `main dl` · 사람 상세(/admin/people에서 첫 「상세」 링크로 이동)의 `main dl`과 `#person-role-change` · /admin/people/org의 `main ul` 전부와 「본부 추가」로 연 `#org-unit-form` · 등록 링크로 연 `#vendor-form`(「거래처 등록」)·`#corp-card-form`(「법인카드 등록」)·`#code-item-form`(「코드 추가」)·`#person-form`(「사람 등록」)·`#role-form`(`/admin/people/roles?new=1`)이 모두 폭 ≤ 720이고 x가 `main h1`의 x와 1px 안에서 같다. 반례로 /admin/code-tables 목록의 `main table`은 폭 > 720이다
    - 단위 system-md-compliance.test.ts(F-07): code-tables/page.tsx와 roles-client.tsx에서 「정렬」 `<th>`와 `sortOrder` `<td>`가 `styles.num`을 쓴다. HistoryList.tsx가 `valueKind.kind === "number"`일 때 값 머리글·값 칸에 `styles.num`을 건다. 세 모듈 CSS에 `.table .num` 블록이 있고 그 안에 `text-align: right`·`tabular-nums`·`white-space: nowrap`·`letter-spacing: var(--ls-num)`가 있다
    - 단위 system-md-compliance.test.ts(F-08): vendors·corp-cards·code-tables·people의 page.tsx 각각이 정규식 `/<\/StatusTag>\s*\)\s*:\s*"—"\s*\}/`에 맞는다(상태 태그가 없을 때 `—`). roles-client.tsx가 `role.isSeed ? "시드" : "—"`를 쓴다
    - E2E code-tables.spec.ts: 시스템 관리자로 /admin/code-tables에 들어가 첫 `main table`의 머리글에서 「정렬」·「상태」 열 번호를 이름으로 찾는다. 「정렬」 th와 첫 행 정렬 td의 계산값이 `text-align: right` · `white-space: nowrap` · `font-variant-numeric`에 `tabular-nums`를 포함한다. 「상태」 열에서 정확히 `—`인 칸이 하나 이상 있다(시드 코드는 정상 상태다)
  </behavior>
  <action>
**A. F-02 전면 적용** (Task 1 `.single-column` 재사용, per SYSTEM.md §3 적용 목록). RED: behavior의 단위 TARGETS 확장과 single-column.spec.ts 케이스를 먼저 쓰고, 실패(폭 약 1240)를 확인해 인용한다. 그 다음 적용한다. 화면 단위 감싸기 — 모두 PageHeader·Banner는 밖에 둔다: `/admin`은 `groups.map(...)` 전체를 `<div className="single-column">`로 감싼다. `/admin/settings`는 `<SettingsFormClient …/>`를 감싼다(내보내기 줄·섹션·이력 목록이 함께 들어간다). 사람 상세는 `<KvList …/>`와 `<PersonDetailClient …/>`를 한 div로 감싼다. 조직은 `<OrgClient …/>`를 감싼다(등록 행동 줄도 들어가 720 오른쪽 끝에 붙는다 — §3 (c)). 시스템 상태는 `<KvList …/>`만 감싼다. 폼 단위 — 루트 `<form>`에 `className="single-column"`을 직접 붙인다: `#vendor-form`, `#corp-card-form`, `#corp-card-owner-form`, `#code-item-form`, `#person-form`, `#role-form`. 사람 등록의 결과 패널(`styles.registeredPanel`, 배경·테두리가 있어 폭이 보인다)은 기존 클래스 뒤에 `single-column`을 이어 붙인다. code-item-form처럼 배열 `join(" ")` 또는 템플릿 문자열을 쓴다. 표·필터 줄·EMPTY 줄에는 붙이지 않는다. GREEN 확인 뒤 커밋: `feat: apply single-column max-width to admin screens and forms`.

**B. F-07 숫자 칸** (SYSTEM.md §2-4 「모든 숫자 칸은 우측 정렬, tabular-nums, nowrap」, §2-3 「숫자: letter-spacing: 0」, §7-14 → §7-2 number 매핑). RED부터 쓴다(behavior의 단위 F-07·E2E code-tables). `code-tables.module.css`·`people.module.css`·`HistoryList.module.css` 각각에 `.table .num { text-align: right; font-variant-numeric: tabular-nums; white-space: nowrap; letter-spacing: var(--ls-num); }`을 추가한다. 선택자는 반드시 `.table .num`이다 — 세 모듈의 `.table th { text-align: left }`가 단일 클래스 `.num`보다 명시도가 높아 머리글이 왼쪽에 남는다. 주석에 §2-4를 적는다(corp-cards `.last4` 선례와 같은 결). 적용: code-tables/page.tsx의 「정렬」 th와 `{item.sortOrder}` td, roles-client.tsx의 「정렬」 th와 `{role.sortOrder}` td(roles는 `../people.module.css`를 쓴다), HistoryList.tsx의 「값」 th와 `{entry.displayValue}` td — `valueKind.kind === "number"`일 때만 `styles.num`, 아니면 `undefined`. 날짜 열(적용 시작일·발생 시각)은 건드리지 않는다.

**C. F-08 빈 상태 칸** (SYSTEM.md §2-4 「값이 없으면 `—` 하나. 빈칸을 두지 않는다」, §10 「상태는 항상 글자」). 새 상태 낱말(`활성`·`사용 중`)은 만들지 않는다 — 카피 결정이라 범위 밖이다. vendors/page.tsx·corp-cards/page.tsx·code-tables/page.tsx·people/page.tsx의 상태 `<td>` 삼항에서 마지막 분기(태그가 없는 정상 상태)가 `"—"`를 렌더하게 바꾼다. roles-client.tsx의 「시드 여부」 칸 거짓 분기(지금은 빈 문자열)도 `"—"`로 바꾼다. 같은 규칙이고 같은 파일이다. 동작 칸·다른 칸은 건드리지 않는다. 이어서 `test/e2e/master-edit.spec.ts` 91행을 고친다. 행 전체에서 `—` 칸이 0개라는 단언은 이제 정상 상태 칸 때문에 깨진다. 테스트 의도(결함 3: 소유 칸이 `—`로 남지 않는다)대로 **소유 열 하나**만 본다. corp-cards/page.tsx의 열 순서(발급사·뒤 4자리·별칭·종류·소유·상태)를 확인하고 `updatedRow.getByRole("cell").nth(4)`가 `—`가 아님을 단언한다. 주석은 「상태 칸의 —와 분리하려고 소유 열로 한정」으로 바꾼다. GREEN 확인 뒤 커밋: `fix: right-align numeric cells and show em dash in empty status cells`(본문에 F-07·F-08, 행동 로그 날짜를 뺀 이유 한 줄).
  </action>
  <verify>
    <automated>cd /home/user/ERP_PLANT8_260917 && pnpm exec vitest run --project unit test/unit/ui/single-column.test.ts test/unit/ui/system-md-compliance.test.ts test/unit/ui/history-list-id-prefix.test.ts && pnpm lint && pnpm typecheck && pnpm exec playwright test test/e2e/single-column.spec.ts test/e2e/code-tables.spec.ts test/e2e/master-edit.spec.ts --project=desktop</automated>
  </verify>
  <done>1280에서 관리자 한 열 화면 5개와 등록·수정 폼 6종이 720px 이하로 왼쪽 정렬되고, 코드표 목록 표는 720보다 넓다(E2E 통과). 정렬·숫자 이력 칸이 우측·tabular·nowrap이고 정상 상태 칸과 비시드 칸이 `—`다(단위·E2E 통과). master-edit.spec.ts가 소유 열만 단언하며 통과한다. lint·typecheck 통과. RED 출력을 인용했다. 커밋 2개.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 셸·로그인·내 계정 — 시트 하단 고정(F-04) · 폰 터치 목표(F-05) · 로그인 틀 360(F-09) · 섹션 제목 타입 스케일(F-10)</name>
  <files>ui/shell/MoreSheet.module.css, ui/shell/TopBar.module.css, app/(app)/admin/settings/settings.module.css, docs/design/DECISIONS.md, docs/design/SYSTEM.md, docs/design/tokens.css, ui/auth-frame/AuthFrame.module.css, app/(app)/account/change-password-form.tsx, app/(app)/account/account.module.css, test/e2e/mobile-shell.spec.ts, test/e2e/mobile-page-chrome.spec.ts, test/e2e/page-chrome.spec.ts</files>
  <read_first>ui/shell/MoreSheet.module.css 1–27행. ui/shell/TopBar.module.css 75–91·129–139행. app/(app)/admin/settings/settings.module.css 31–42행. ui/auth-frame/AuthFrame.module.css 전체(26행). docs/design/SYSTEM.md 527–557행(§6-7). docs/design/tokens.css 110–120행. app/(app)/account/change-password-form.tsx 30–40행. test/e2e/mobile-shell.spec.ts 1–70행(loginAsEmployee, 「더보기」 여는 방법). test/e2e/mobile-page-chrome.spec.ts 1–40행(loginAs(page, roleId), SYSADMIN_ROLE_ID). test/e2e/page-chrome.spec.ts 1–25행.</read_first>
  <behavior>
    - mobile-shell.spec.ts(375×800): 「더보기」 시트(`getByRole("dialog", { name: "더보기" })`)의 `y + height`가 800과 1px 안에서 같고 `y > 0`이다
    - mobile-shell.spec.ts: `header button[aria-haspopup="menu"]`(사용자 메뉴 트리거)의 박스가 폭·높이 ≥ 44이고, 아래 끝이 `header` 아래 끝을 넘지 않는다
    - mobile-page-chrome.spec.ts(시스템 관리자): /admin/settings의 `main label` 가운데 체크박스를 가진 것이 1개 이상이고, 전부 높이 ≥ 44다. 그 안 체크박스 박스는 20×20이다
    - page-chrome.spec.ts(1280): /login의 form 폭이 360 이하이고 300보다 크며, 가로 중심 `x + width/2`가 640과 1px 안에서 같다
    - page-chrome.spec.ts: 직원으로 로그인한 /account에서 `getByRole("heading", { level: 2, name: "비밀번호 변경" })`의 계산값이 font-size `18px`, font-weight `700`이다
  </behavior>
  <action>
먼저 RED. behavior의 E2E 다섯 케이스를 각 스펙에 추가하고 돌려서 실패를 확인한 뒤 인용한다. 폰 스펙은 `--project=mobile-375 --no-deps`로 돌린다. 이어서 아래 순서대로 고치고, 고칠 때마다 해당 케이스를 GREEN으로 만든 뒤 따로 커밋한다.

1. F-04 (SYSTEM.md §7-8 「시트(폰): 하단에서 올라옴」). `MoreSheet.module.css`의 `.sheet`에서 `inset-block-end: 0;` 바로 위에 `inset-block-start: auto;`를 넣는다. 한 줄 주석으로 이유를 적는다: 네이티브 `dialog:modal`의 브라우저 기본값이 위아래 inset을 모두 0으로 주므로 위쪽을 풀어야 아래에 붙는다. 열림 모션은 이 범위가 아니다. 커밋: `fix: anchor more sheet to the bottom edge`.
2. F-05 부분 (SYSTEM.md §3·§10 폰 44×44, §7-2 boolean 「라벨 자체가 클릭 영역」). `TopBar.module.css`의 기존 폰 미디어 쿼리(`max-width: 699.98px`) 안에 `.userTrigger { min-height: var(--touch-min); min-width: var(--touch-min); }`를 추가한다. 폰 상단 바 높이 44 안에 맞는다. `settings.module.css` 끝에 같은 폰 미디어 쿼리를 두고, 그 안에 `.checkboxLabel { min-height: var(--touch-min); }`를 넣는다. glyph(`--icon-lg` 20px)와 전역 `--control-h`는 건드리지 않는다. 커밋: `fix: give phone user menu and settings checkboxes 44px touch targets`.
3. F-09 (SYSTEM.md §6-7 「최대 폭 360」). 순서는 DECISIONS → SYSTEM → tokens → 적용이다. (a) `DECISIONS.md` 끝에 `## 2026-09-22 — 로그인 틀 폭 360: --auth-max 신설(480 모달 폭 재사용 철회)`를 추가한다. **결정**: §6-7의 360을 `--auth-max: 360px`로 이름 붙여 AuthFrame이 쓴다. **왜**: ① SYSTEM.md가 확정한 값은 360인데, 구현은 기록 없이 480 모달 폭 토큰을 재사용했다(Codex F-09, 미기록 이탈). ② tokens.css 머리말이 「컴포넌트는 이 변수만 참조한다」이고 레이아웃 폭은 모두 토큰이다. ③ 화면 값은 SYSTEM.md 그대로라 새 디자인 결정이 아니다(`--on-accent-weak` 선례). **버린 대안**: AuthFrame에 360px 리터럴(토큰 규칙 위반), `calc(var(--form-max) / 2)`(관계없는 폼 폭에 로그인 폭이 묶인다), 480 유지 + SYSTEM.md 개정(확정 디자인을 이유 없이 넓힌다). **범위**: SYSTEM.md §6-7, tokens.css, AuthFrame.module.css. (b) `SYSTEM.md` §6-7의 불릿 목록 맨 앞에 「- 틀 폭: `max-width: var(--auth-max)`(360px), 가운데 정렬, PC·폰 공통(폰은 가용 폭이 더 좁으면 그 폭)」을 추가한다. 이 순간 design-system-docs.test.ts가 RED가 되는 것을 확인한다(참조 토큰 미정의). (c) `tokens.css`에서 `--modal-w` 줄 바로 아래에 `--auth-max: 360px;`를 추가하고 주석 「로그인(§6-7) 틀 최대 폭 — 셸 없는 인증 폼」을 붙인다. (d) `AuthFrame.module.css`의 `.frame` max-width를 `var(--auth-max)`로 바꾼다. 머리 주석 2–4행은 옛 재사용 이유를 설명하므로 우리 변경과 함께 고친다: 「최대 폭은 §6-7의 360 = --auth-max(2026-09-22 DECISIONS.md)」. 커밋: `fix: narrow login frame to 360px via --auth-max`.
4. F-10 (SYSTEM.md §2-2 `--fs-lg` 18/1.4/700, §2-3 제목 자간 -0.02em, §3 섹션 사이 위 `--s-6`·아래 `--s-3`). `app/(app)/account/account.module.css`를 새로 만든다. `.sectionTitle`에 `margin: var(--s-6) 0 var(--s-3)`, `font-size: var(--fs-lg)`, `font-weight: var(--fw-bold)`, `line-height: var(--lh-head)`, `letter-spacing: var(--ls-head)`를 준다. 머리 주석에 근거 절을 적는다. `change-password-form.tsx`에서 이 모듈을 import하고 `<h2>`에 `className={styles.sectionTitle}`를 준다. 선은 추가하지 않는다. 화면 크기 단계는 lg·sm·base 세 단계로 §2-2 한도 안이다. 커밋: `fix: apply type scale to account section heading`.
  </action>
  <verify>
    <automated>cd /home/user/ERP_PLANT8_260917 && pnpm exec vitest run --project unit test/unit/design-system-docs.test.ts && pnpm lint && pnpm typecheck && pnpm exec playwright test test/e2e/page-chrome.spec.ts --project=desktop && pnpm exec playwright test test/e2e/mobile-shell.spec.ts test/e2e/mobile-page-chrome.spec.ts --project=mobile-375 --no-deps</automated>
  </verify>
  <done>375에서 시트가 화면 아래에 붙고, 트리거가 44×44 이상이며, 설정 체크박스 라벨이 높이 44 이상이다(glyph 20). 1280에서 로그인 form이 360 이하로 가운데에 있다. /account h2가 18px·700이다. `--auth-max`가 DECISIONS→SYSTEM→tokens 순서로 들어갔다(design-system-docs 통과). lint·typecheck·해당 E2E 통과. RED 출력을 인용했다. 커밋 4개.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 서버 컴포넌트(관리자 화면) | 이 계획은 경계를 새로 만들지 않는다. 바꾸는 것은 className·CSS·표시 문자열(`—`)뿐이고, 서버가 보내는 필드와 권한 필터(`can(viewer, key, "view")`)는 그대로다 |
| 브라우저 → /login | 틀 폭만 바뀐다. 로그인 액션·실패 문구·잠금 로직은 건드리지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o2b-01 | I | 관리자 표·폼 마크업(태스크 2) | low | accept | 새 데이터를 노출하지 않는다 — 감싸는 div·className·빈 칸의 `—`만 추가한다. 열 구성은 서버가 정하는 대로다(§7-3 「칸 수 가변」 불변) |
| T-o2b-02 | S | /login AuthFrame(태스크 3) | low | accept | 폭 토큰만 바뀐다. 인증 흐름·오류 문구(`test/e2e/change-password.spec.ts` 계약)는 그대로다 |
| T-o2b-03 | T | test/e2e/master-edit.spec.ts 단언 완화 | low | mitigate | 결함 3의 회귀 감시가 약해지지 않게 소유 열 하나를 정확히 겨눈다(`nth(4)`가 `—`가 아님). 행 전체 단언을 없애지 않고 대상을 좁힌다 |
| T-o2b-SC | T | 패키지 설치 | low | accept | 해당 없음 — 새 의존성 0, 설치 작업 없음 |
</threat_model>

<verification>
CLAUDE.md의 화면 검증 순서를 따른다: **싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번**. 전체 게이트는 두 번 돌리지 않는다.

1. (실행자) 싼 게이트: `pnpm lint` · `pnpm typecheck` · `pnpm test:unit` · `pnpm build` — 전부 통과해야 한다. 태스크 안의 E2E는 dev 서버로 도는 좁은 확인일 뿐 완료 신호가 아니다. SUMMARY에 「CI=true 전체 게이트 대기」라고 적는다.
2. (오케스트레이터 — 실행자가 아니다) 독립 DOM 감사: 별도 에이전트가 `CI=true`(프로덕션 빌드)로 must_haves.truths 8개를 DOM 계산값·박스로 실측 판정한다. 스크린샷을 눈으로 보고 판정하지 않는다. 항목은 1280 폭 11곳과 표 반례, 375 시트 위치·트리거·체크박스 라벨, 로그인 폼 360, h2 18px, 숫자 칸 계산값, 상태 칸 `—`다.
3. 감사가 찾은 것을 고친다.
4. 전체 게이트 한 번: `pnpm test:e2e:ci`(= `pnpm db:reset:test && CI=true pnpm test:e2e`, desktop 다음 mobile-375). DB가 있으면 `pnpm test:integration`도 함께 돌린다. DB를 띄울 수 없으면 통과했다고 쓰지 않고, 실행해야 할 명령을 남긴다.
5. Post-build(CLAUDE.md, 건너뛰지 않는다): `/review` → `/qa` → `/design-review`(UI 변경). `/cso`는 이 작업이 인증·권한·암호화·외부 입력 **로직**을 바꾸지 않아 해당 없음으로 본다. 다만 건너뛰기 전에 사용자에게 먼저 말하고 승인을 받는다.
</verification>

<success_criteria>
- 1280에서 한 열 콘텐츠가 전부 720에서 멈추고 왼쪽에 붙는다. 표는 그대로 컨테이너 폭이다. 규칙은 SYSTEM.md 한 자리에 전문으로 있고, 새 토큰이 없다(F-02). 투두 `2026-09-22-single-column-max-width.md`의 해결 조건 1·2·3을 모두 충족한다.
- 폰 「더보기」 시트가 아래에서 올라오는 자리에 있다(F-04).
- 폰 사용자 트리거와 설정 체크박스 라벨이 44 이상이다(F-05 부분). 전역 `--control-h`는 그대로다.
- 로그인 틀이 SYSTEM.md의 360이고, 이탈이 DECISIONS.md에 정리되어 있다(F-09).
- /account 섹션 제목이 타입 스케일 안에 있다(F-10).
- 숫자 칸이 우측·tabular·nowrap이고 빈 상태 칸이 없다(F-07·F-08 부분). 행동 로그 날짜는 제외 사유(UTC 표시·시간대 정책 부재)와 함께 보고된다.
- 커밋 8개, 각각 한 의도. 새 의존성 0, 새 색·서체·radius·그림자 0, 새 토큰은 `--auth-max` 하나.
</success_criteria>

<output>
`.planning/quick/260922-o2b-codex-design-review-fixes/260922-o2b-SUMMARY.md`를 쓴다. 담을 것: 태스크별 RED 출력 인용, 실행한 좁은 E2E와 「CI=true 전체 게이트·독립 DOM 감사 대기」 상태, `<planning_findings>`의 제외 항목(행동 로그 날짜 — UTC 표시 결함을 따로 보고)과 관찰 항목 네 개. 투두 `.planning/todos/pending/2026-09-22-single-column-max-width.md`를 닫을 수 있게 되었다는 사실도 적는다. 투두 이동은 오케스트레이터가 GSD 도구로 한다 — `.planning/` 수동 편집 금지. Phase 4 계획 파일은 건드리지 않는다.
</output>
