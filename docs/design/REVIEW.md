# REVIEW.md — 디자인 리뷰 기록

`docs/DESIGN.md` §3 마지막 단계 `/plan-design-review`의 결과. 대상은 `SYSTEM.md`(+ `tokens.css`, `system/preview.html`).
gstack 절차는 패스마다 개별 승인 질문을 요구하지만, 이 세션은 사용자가 실시간으로 답하지 않는 조건이어서 **기존 결정을 바꾸지 않는 보강은 바로 넣고 여기 기록**했고, 사용자 판단이 필요한 것만 `DECISIONS.md` 「미확정」에 남겼다. 다음 리뷰는 8점 미만 패스만 전체 검토, 8점 이상은 빠르게 확인한다.

---

## 2026-09-18 — 1차 리뷰 (SYSTEM.md 초판)

**사전 감사**: 대상은 앱 UI(OPERATE). `DESIGN.md`(절차) 있음, `SYSTEM.md`(시스템) 이 PR에서 신규. 260907 화면은 참고 금지라 「기존 패턴」 없음. 이전 리뷰 없음.
**초기 종합 점수: 7/10** — 토큰·컴포넌트·템플릿은 구체적이나, 상태가 컴포넌트별로 펼쳐지지 않았고, 사용자 동선(시간 축)과 접근성 계약, 태블릿 구간이 비어 있었다.

| 패스 | 전 | 후 | 발견 → 조치 |
|---|---|---|---|
| 1 정보 구조 | 8 | 9 | 첫·둘째·셋째가 템플릿마다 정해져 있음(내 차례 → 표 → 힌트). **빈 곳**: 폰 하단 탭 4개의 역할별 구성, 「더보기」 시트 내용, 폰에서 검색(`⌘K`) 진입점, 700–1023 태블릿 구간 → §6-0에 표와 규칙 추가 |
| 2 상호작용 상태 | 7 | 9 | 다섯 상태 정의는 있으나 컴포넌트마다 「사용자가 보는 것」이 없었음 → §7-7에 컴포넌트 7종 × 5상태 표 추가. 서버 액션 **진행 중** 버튼 상태 누락 → §7-1 추가 |
| 3 사용자 동선·감정 | 6 | 9 | 브리프 3문은 있지만 시간 축 스토리보드가 없었음 → §11에 핵심 동선 3개(막힘 해소·표 20줄 입력·폰 승인) 단계별 「하는 것·느끼는 것·화면이 보장하는 것」 표 추가 |
| 4 AI 슬롭 위험 | 8 | 9 | 하드 리젝션 7항목 해당 없음(카드 0, 첫 화면이 원장). 리트머스: 브랜드 식별 Y(바+워드마크) · 시각 앵커 Y(내 차례) · 제목만으로 스캔 Y · 섹션 한 일 Y · 카드 필요성 N/A · 모션이 위계를 돕는가 Y(응답형만) · 그림자 없이 고급인가 Y(그림자 0). 블랙리스트 11항목 해당 없음. **빈 곳**: 브라우저 기본 표면(선택색·캐럿·스크롤바·accent-color) 미정 → §4-4 + 토큰 6개. **의도적 이탈 기록**: 표 셀 14px(폰 15px)는 「본문 16px 미만 금지」 규칙과 어긋나지만 업무 표 밀도를 위한 선택 — §10에 명시 |
| 5 시스템 정합 | 9 | 10 | `DESIGN.md` §3-2 항목 전부 존재, `tokens.css`와 1:1. 문구 모순 1건(틴트 「세 개」라 쓰고 네 개 나열) 수정 |
| 6 반응형·접근성 | 7 | 9 | 대비값·터치 44·키보드 표 편집·focus-visible·reduce-motion은 있었음. **빈 곳**: 랜드마크·스킵 링크·표 편집 `role=grid`·`aria-live`·오류 셀 `aria-invalid`·포커스 복귀·200% 확대 → §10 「접근성 계약」 신설 |
| 7 미결 결정 | — | — | U1 로고 반전 · U2 인쇄 결재 칸 · U3 Pretendard 파일 추가(의존성 승인) · U4 손익 인라인 막대 → `DECISIONS.md` 「미확정」 |

**종합 점수 후: 9/10.** 남은 1점은 미확정 4건과, 실제 앱 코드에서 확인해야 할 것(웹폰트 로딩 체감, 표 편집 엔진 선택은 Phase 4).

**범위 밖으로 둔 것**: 표 편집 자체 구현 vs 라이브러리(UX-05가 Phase 4 계획으로 명시) · 손익 차트 종류(Phase 9) · 알림(마감 알림) 채널 UI(Phase 별도).
**이미 있는 것으로 해결한 것**: 인쇄 요구(D의 태도 → §6-6) · 오류 셀 고정과 힌트 줄(C → §7-3·§7-9) · 막힘 행 구조(B → §7-4).

## GSTACK REVIEW REPORT

| Runs | Status | Findings |
|---|---|---|
| 1 (2026-09-18) | issues_open | 7 passes evaluated · 6 gaps fixed in `SYSTEM.md`/`tokens.css` · 1 deliberate deviation recorded · 4 decisions pending user |

VERDICT: SYSTEM.md is implementation-ready for Phase 2 screens once U1–U3 are answered; U4 defers to Phase 9. Initial 7/10 → 9/10. OUTSIDE COVERAGE: none (no external model run in this session).

**UNRESOLVED DECISIONS:**
- U1 로고 워드마크 반전 사용 허용 여부 (`DECISIONS.md` 미확정 표)
- U2 인쇄물 결재 칸: 텍스트 vs 서명 이미지
- U3 Pretendard Variable 웹폰트 파일 추가 승인
- U4 손익 원장 인라인 막대의 v1 포함 여부
