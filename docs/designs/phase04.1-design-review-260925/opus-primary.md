# 완료

# 04.1 디자인 계획 검토 — Opus 1차 판단 (2026-09-25, 기준 커밋 d7372af)

대상: `04.1-UI-SPEC.md`(S1~S10, A1~A4) + 04.1-02/04/05/06 UI 태스크. 기준: `docs/design/SYSTEM.md` §6-1·§6-3·§7-1·§7-7·§7-8·§7-15·§8·§10, 실물 `sheet-modal.html`, Phase 4 개정 P0(원격 SYSTEM.md 1086행).

## 1. 점수표 (패스 1~6)

| 패스 | 점수 | 10이 되려면 |
|---|---|---|
| 1 정보 구조 | 7 | 같은 숫자·같은 상태가 두 자리에 나오는 곳 셋(일수 힌트↔잔고 행, KvList 일수↔잔고 행, 그룹 머리글 `내 결재`↔상태 칸 `내 결재`)을 한 자리로 줄인다 (OP-02 · OP-07) |
| 2 상태 | 7 | 종류·시간 칸이 빈(`—`) 상태의 막힘 문구가 없고, 담당이 사라져 문서가 막힌 상태에 할 일이 없다. UI-SPEC 473행은 "대표가 받는다"라고 적었는데 플랜은 "막힘"이라 서로 다르다 (OP-01 · OP-09) |
| 3 사용자 여정 | 7 | 폰 처리함 행을 누르면 어디로 가는지, 기안자가 지금 담당이기도 할 때 무엇을 누를 수 있는지를 UI-SPEC과 플랜이 다르게 말한다. 계약을 하나로 맞춘다 (OP-03) |
| 4 AI 슬롭·구체성 | 9 | 문구·숫자 형식·상태 매핑이 구체적이다. 남은 것은 `· 결재 규칙`처럼 행동이 없는 꼬리 글자뿐이다 (OP-19, 참고) |
| 5 SYSTEM.md·토큰 정렬 | 7 | 새 토큰·색·radius는 0이다. P0 「같은 사실 두 자리 금지」 위반을 없앤다. `ui/select`가 늘 붙이는 빈 옵션 `—`를 §7-15 계약(「고를 것이 없으면」)에 맞춘다. 실물 결재함(PC `승인` → 근거 시트)과 다른 점을 결정으로 남긴다 (OP-01 · OP-02 · OP-12) |
| 6 반응형·접근성 | 7 | 결재함 행마다 되풀이되는 `승인`·`반려`에 행 문맥을 담은 접근 이름을 준다. 태블릿 터치 폭에서 행 3차 버튼이 44px에 못 미친다 (OP-10 · OP-15) |

## 2. 지적 목록

| ID | 등급 | 화면 | 문제 | 근거 파일:줄 | 추천 고치기 (A 추천 / B 대안) | 원칙 (CLAUDE.md §7) |
|---|---|---|---|---|---|---|
| OP-01 | 막음 | S2 · S1 · S9 | 공용 `ui/select`는 옵션이 있어도 늘 첫 옵션 `—`를 그린다. 그래서 늘 값이 있어야 하는 칸 넷에서 사용자가 `—`를 고를 수 있다: S2 `종류`(넷 고정)·`시간`(오전/오후), S1 연도, S9 조정 `잔고`. 이것은 컴포넌트 주석과 §7-15 계약(「고를 것이 없으면 첫 옵션 `—`」)과도 어긋난다. UI-SPEC은 종류·시간의 기본값을 정하지 않았고, 이 칸이 빌 때의 막힘 문구도 없다(날짜만 있다). 06 플랜은 S9에서 `—` 옵션을 그대로 두고, 그걸 고르면 서버가 거부하는 흐름을 테스트로 굳힌다 | `ui/select/Select.tsx:4-5` vs `:29` · `docs/design/SYSTEM.md:985` · UI-SPEC `:203` `:262` `:277` · `04.1-06-PLAN.md:343-344` `:349` | **A** Phase 4(`ui/select` 소유)에 `—`를 옵션 0개일 때만 그리도록 계약 정렬을 요청하고 DECISIONS에 적는다. 04.1은 기본값을 둔다: 종류 `종일`, 시간은 최근 입력값(없으면 `오전`), 연도는 올해, 잔고는 `연차`. `—` 거부 E2E(06:344)는 지운다. **B** `ui/select`를 못 고치면 기본값은 A와 같게 두고, 빈 값의 막힘 문구 `종류 비어 있음 · 종류 고르기` · `시간 비어 있음 · 시간 고르기`를 Copywriting에 더한다 | 할 수 없는 선택은 숨김 · 알 수 있는 값은 기본값 |
| OP-02 | 막음 | S2 · S3 · S5 | 같은 숫자를 두 자리에 쓴다. S2에서는 일수 힌트 `이번 신청 3일`과 잔고 행 `… · 이번 신청 3일`이 몇 줄 사이로 두 번 나온다. S3·S5에서는 KvList `일수 3일`과 잔고 행 `이번 신청 3일`이 겹친다. UI-SPEC이 스스로 적용하겠다고 한 P0(「같은 사실을 두 자리에 쓰지 않는다」)를 어긴다 | UI-SPEC `:181` `:207-208` `:265` `:268` `:293` `:295` · 원격 SYSTEM.md `:1086`(P0) | **A** 잔고 행(입력 §5 정본 숫자)은 그대로 둔다. S2 힌트는 `주말 2일 제외`만 남기고(없으면 힌트 없음, 재택은 `재택 · 차감 없음`), S3·S5의 `일수` 행은 잔고 행이 없을 때(재택·종결 문서)만 그린다. **B** 잔고 행에서 `이번 신청` 조각을 뺀다. 다만 입력 §5의 「이번 신청」 정본 숫자와 닿으니 사용자 확인이 필요하다 | 안내 문구 최소 · P0 |
| OP-03 | 막음 | S3 · S4 · S5 · S8 · S9 | 엔지니어링 2회차에서 플랜은 바뀌었는데 UI-SPEC(정본 화면 계약)에 반영되지 않은 화면 동작이 넷이다. `/design-review`와 DOM 감사는 UI-SPEC을 기준으로 판정하므로 실행 뒤에 서로 어긋난다. ① 폰에서 처리함 행을 누르면 UI-SPEC은 「행동 줄 없는 시트」, 05 플랜은 「`/leave/[id]`로 이동」이다(ENG-16). ② 기안자가 지금 담당이기도 할 때 행동이 `[승인, 회수]`(반려 없음)인데, S3 표 네 갈래와 S4 「승인·반려만」에 이 경우가 없다(CXF2-B-RF01). 결재함 PC 행 3차 `회수`와 시트 2차 `회수`도 계약에 없다. ③ S8에서 지금 효과가 없는 칸을 비활성으로 두는 것(CX-W2)이 S8에 없다. ④ S9의 `{Y-1} 회계연도` · `올해 보기` 링크와 연도 부제(S9-FY·CX-R6)가 S9에 없다. S9는 「연도 select 없음」만 적었다 | UI-SPEC `:193-194` `:307-312` `:342` `:351-352` `:370-378` `:380-388` · `04.1-05-PLAN.md:197` `:218` `:244` `:283` · `04.1-04-PLAN.md:35` `:192` · `04.1-06-PLAN.md:64` | **A** 실행 전에 UI-SPEC S3 표에 「기안자이면서 지금 담당 → 1차 `승인` + 2차 `회수`」 한 줄, S4·S5에 그 행동을 더한다. S5 351-352행을 「처리함 행 = 문서 화면 링크」로 바꾸고 「행동 줄이 없으면 닫기 x」 조항은 지운다. S8에 「효과 없는 칸은 비활성(값 보존)」을, S9에 연도 부제·두 링크를 적는다(조정자 판단 메모 = cross-notes-B 방식). **B** 플랜을 UI-SPEC 원문으로 되돌린다(①은 처리함 상세 읽기가 늘어난다) | 단계·주 버튼 하나(계약이 하나여야 판정 가능) |
| OP-04 | 경고 | S2 | 종일·재택 신청에서 종료일을 따로 적어야 한다. UI-SPEC은 「하루면 같은 날」이라고만 적었고, 막힘 문구 `시작일 · 종료일 2칸 비어 있음`은 두 칸을 따로따로 요구한다. 시스템이 알 수 있는 값(하루짜리면 종료일 = 시작일)을 사용자에게 묻는 셈이다 | UI-SPEC `:203` `:263-264` `:277` · `04.1-06-PLAN.md:62`(truth: 막힘 문구 그대로) | **A** 시작일을 고르면 종료일이 비어 있거나 시작일보다 빠를 때 시작일로 채운다. 막힘은 `시작일 비어 있음 · 시작일 적기` 한 가지가 된다. **B** 채우지는 않고 날짜 선택기의 `min`을 시작일로 둔다 | 알 수 있는 값은 기본값 · 틀린 선택 불가 |
| OP-05 | 참고 | S1 | 이번 연도가 0건이면 머리의 1차 `연차 신청`을 치우고 설명 한 줄 + 3차 링크를 둔다. §7 「빈 화면엔 설명 대신 첫 행동 버튼」과 결이 반대다. 다만 Phase 4 프로젝트 목록의 선례를 따른 것이다 | UI-SPEC `:188` `:196` · SYSTEM.md `:810` | 사용자 결정·Phase 4 선례와 충돌할 수 있어 참고로만 둔다. 바꾼다면 A: 1차는 남기고 EMPTY는 `신청한 연차 없음` 한 줄 |
| OP-06 | 경고 | S1 | 새 직원이나 첫해 사용자는 연도 select에 고를 것이 올해 하나뿐이다(+ `—`). 효과 없는 선택 컨트롤이 필터 줄에 선다 | UI-SPEC `:237` `:253` · `04.1-06-PLAN.md:176` `:219` | **A** 옵션이 올해 하나면 select를 그리지 않는다(부제 `{연도} 회계연도`가 이미 말한다). **B** 비활성 select | 할 수 없는 선택은 숨김 |
| OP-07 | 경고 | S4 | 그룹 머리글 `내 결재` 아래 모든 행의 상태 칸이 다시 `내 결재`다. 같은 사실을 두 자리에 쓴다. 실물 결재함 표는 그룹 머리글 없이 상태 칸 하나로 말한다. 폰에서는 P1 세 칸 중 하나를 이 중복이 차지한다 | UI-SPEC `:168` `:327-328` `:335` · `docs/design/system/sheet-modal.html:217` | **A** `내 결재` 그룹 행의 상태 칸은 비운다(폰은 문서 칸이 그 폭을 쓴다). 처리함 행은 그대로(`승인 09-03`·`{단계} 결재 중`). **B** 그룹 머리글을 없애고 실물처럼 상태 칸으로만 구분한다 | P0 · 배치로 뜻 전달 |
| OP-08 | 경고 | S8 | ① 한 화면에서 `—`가 두 뜻이다. `N단 담당 계급` = `—`는 「계급 조건 없음(전원)」이고(엔진 null = 무조건), `N단 특정 부서` = `—`는 「부서 없음 → 빈 자리로 건너뜀」 경고다. ② UI-SPEC 안에서도 3단 기본값이 「경영관리 담당(계급)」과 「계급 `—`」로 서로 다르다 | UI-SPEC `:219` `:374` `:474` · `04.1-04-PLAN.md:190` | **A** 계급 select의 빈 값 라벨을 `계급 무관`으로 두고(관리자 화면이라 §7-2 라벨 허용), 374행을 474행(결정 1)에 맞춘다. **B** 라벨은 두고 `N단 담당 계급` 힌트에 `— = 계급 무관` | 알아보기 쉬운 이름 |
| OP-09 | 경고 | S3 · S7 | 담당이 사라져 결재가 멈춘 문서(고아 최종 W10·W13 = 막힘)는 결재선에 `— 경영관리 · 담당 없음`만 붉게 나온다. 기안자에게는 `회수`만 있고 무엇을 하면 되는지가 없다. 잠김 상태인데 할 일 한 줄이 빠졌다. 게다가 UI-SPEC 473행은 「뒤가 전부 비면 대표가 받는다」라고 적어 플랜(막힘)과 다르다 | UI-SPEC `:210` `:473` · `04.1-01-PLAN.md:465` `:468` `:470` · `04.1-05-PLAN.md`(Task 2 ② 「담당 없음 … 결재자 쪽 행동 줄 없음」) | **A** 기안자에게만 결재선 끝 줄 `결재할 사람 없음 · 회수 후 새로 신청`(`--danger`, 회수 = 같은 행동 줄의 2차)을 둔다. 473행은 「대표 1단 승인 뒤 남은 담당이 전부 사라지면 막힘」으로 고친다. **B** 문구 없이 473행만 고친다(사용자가 스스로 알아내야 한다) | 잠김엔 한 줄로 할 일 |
| OP-10 | 경고 | S4 | 결재함 PC 행마다 이름이 `승인`·`반려`(·`회수`)인 버튼이 되풀이된다. 스크린리더와 키보드 사용자는 어느 문서의 승인인지 모른다. UI-SPEC의 a11y 줄은 caption·상태 글자만 다룬다 | UI-SPEC `:193` `:342` `:471` · SYSTEM.md §10 `:1031-1042` | **A** 버튼에 `aria-describedby`로 그 행의 문서 칸을 잇는다(보이는 글자는 그대로). **B** `aria-label="승인 — 연차 · 종일 09-18 ~ 09-20"` | 키보드 흐름 · 접근성 |
| OP-11 | 참고 | S3 · S5 | 제목 `연차 — 종일 09-18 ~ 09-20`과 KvList `종류`·`기간`이 같은 값을 다시 쓴다. 문서의 이름표라 허용할 여지는 있다 | UI-SPEC `:289-292` `:350` | 제목을 번호·기안자 중심으로 바꾸는 것은 범위 밖이라 둔다. 줄인다면 KvList `종류` 행을 뺀다(기간 행에 `종일`을 붙인다) |
| OP-12 | 경고 | S4 | PC 결재함 행의 3차 `승인`은 확인 없이 바로 실행되고 되돌릴 수 없다(Assumptions #11, 오케스트레이터 가정이지 사용자 결정이 아니다). 실물 `sheet-modal.html`의 PC 행 `승인`은 근거 시트를 연다. 잔고·결재선을 보지 않고 목록에서 한 번에 확정되고, 3차 `반려`와는 8px 떨어져 있을 뿐이다 | UI-SPEC `:193` `:342` `:506` · `sheet-modal.html:218` | 패스 7로 넘긴다. **A** 가정을 유지하되 DECISIONS에 「실물과 다름: PC 행 승인은 즉시」를 적는다(§6 화면 하나만 예외 금지를 피한다). **B** 실물대로 PC 행 `승인` → 문서 화면(S3)에서 `Ctrl+Enter` | 위험 동작 떨어뜨림 · 확인은 되돌릴 수 없는 일에 |
| OP-13 | 참고 | S2 | 신청 토스트에 되돌리기가 없다. §6-3 선례(제출 토스트 + 3차 `되돌리기`)와 다르다. 착지 화면(S3)에 `회수`가 있으니 실해는 작다 | UI-SPEC `:214` · SYSTEM.md `:410` | 이유(「되돌리기는 회수」) 한 줄을 A4 개정 문장에 같이 적어 선례 차이를 남긴다 |
| OP-14 | 참고 | S1 · S3 · S4 | 300ms 지연 뼈대 CSS를 `app/(app)/approvals/loading.module.css`에 두고 `/leave`가 가져다 쓴다. 쓰는 곳이 셋이라 DESIGN.md §4 규칙 3(두 곳 이상이면 `ui/`) 대상이다 | `04.1-05-PLAN.md` Task 3 ① · `04.1-06-PLAN.md:196` | Phase 4 `ui/` 충돌을 피하려는 선택이면 Assumptions에 한 줄로 남긴다 |
| OP-15 | 참고 | S4 | 행 3차 버튼이 「PC·태블릿」 폭(700~1023)에도 그려진다. 태블릿은 터치 기기인데 행 높이가 36이고 3차 버튼은 글자 높이라 44에 못 미친다. SYSTEM은 32px를 「마우스·키보드 전용」 전제로만 허용한다 | UI-SPEC `:341` · SYSTEM.md `:193` `:1039` | 태블릿(<1024)은 폰처럼 행을 누르면 시트가 열리게 하거나, 3차 버튼 칸에 44 터치 영역을 둔다. DOM 감사 1024 폭에서 확인한다 |
| OP-16 | 참고 | S8 | 비활성 칸(CX-W2)에 이유가 없다. §7-1 「이유 없는 비활성 금지」는 버튼 규칙이지만 관리자는 칸이 왜 잠겼는지 알기 어렵다. 옆 칸(`N단 사용`·`조직 범위`)이 원인이라 배치로 통할 여지는 있다 | `04.1-04-PLAN.md:192`(「새 힌트 문구는 더하지 않는다」) · SYSTEM.md `:669` | 관리자 화면이라 §7-2 힌트가 허용된다. 필요하면 `특정 부서` 칸 힌트 `조직 범위가 특정 부서일 때만`(이미 UI-SPEC 219행에 있음)을 비활성일 때도 그대로 보인다 |
| OP-17 | 경고 | S4 · S5 | 기안자이면서 담당인 행에서는 결재함 PC 행에 3차 `승인`과 3차 `회수`가 나란히 같은 무게로 선다. 회수는 확인 모달이 있어 오조작 피해는 작지만, 위험 동작이 주 행동 바로 옆에 붙는다 | `04.1-05-PLAN.md:283` | **A** 행 안 순서를 `승인` … (오른쪽 끝) `회수`로 떨어뜨린다. **B** 결재함 행에는 `승인`만 두고 `회수`는 문서 화면에서만 | 위험 동작 떨어뜨림 |
| OP-18 | 참고 | S9 | 조정 폼 `월차` 옵션이 빠질 때 Select 아래 문구로 이유를 보인다. 할 수 없는 선택을 숨기고 이유를 한 줄로 주는 좋은 처리다. 다만 OP-01의 `—`가 남으면 그 효과가 반감된다 | `04.1-06-PLAN.md:349` | OP-01과 함께 처리한다 |
| OP-19 | 참고 | S2 · S7 | 결재선 한 줄 끝의 `· 결재 규칙`은 링크가 아닌 글자라 누를 곳도 뜻도 없는 꼬리다. 다만 §6-3 원문이다 | UI-SPEC `:209` `:364` · SYSTEM.md §6-3 결재선 문단 | SYSTEM 원문이라 참고로만 둔다. Phase 5에서 규칙 화면이 생기면 링크로 |

## 3. 패스 7 — 미결 결정

| 결정 필요 | 미루면 생기는 일 |
|---|---|
| `ui/select`의 빈 `—` 옵션을 「옵션 0개일 때만」으로 맞출지. 소유자가 Phase 4라 04.1이 요청만 할 수 있다 (OP-01) | 04.1 폼 넷이 `—`를 고를 수 있는 채로 나가고, 서버 거부 문구와 E2E가 그 상태를 굳힌다. Phase 5 지출결의 폼에도 같은 문제가 번진다 |
| UI-SPEC을 2회차 플랜 변경 넷(처리함 링크 · `[승인, 회수]` · S8 비활성 · S9 연도 링크)에 맞춰 고칠 주체와 시점. 조정자 판단 메모 방식 (OP-03) | `/design-review`와 DOM 감사가 옛 계약으로 판정해 거짓 실패가 나거나, 실행자가 두 문서 사이에서 임의로 고른다 |
| 잔고 행의 `이번 신청`과 일수 표시 중 어느 쪽을 남길지. 입력 §5 정본 숫자와 닿는다 (OP-02) | P0 위반이 신청 창·문서 화면·결재 시트 세 곳에 그대로 구현된다 |
| PC 결재함 행 `승인`을 즉시 실행할지(가정 #11), 실물처럼 근거를 거칠지 (OP-12) | 실물과 다른 동작이 결정 기록 없이 들어가 「화면 하나만 예외」가 된다. 목록에서 한 번에 되돌릴 수 없는 승인이 생긴다 |
| 고아 최종(막힘) 문서에서 기안자에게 줄 한 줄 문구와 UI-SPEC 473행 정정 (OP-09) | 드문 경우지만 문서가 붉은 `담당 없음`인 채 멈추고, 기안자는 무엇을 할지 모른다 |
| 태블릿(700~1023)에서 결재함 행 행동을 PC 방식으로 둘지 폰 방식으로 둘지 (OP-15) | 터치 기기에서 44px 미만 버튼이 나간다. DOM 감사 1024 폭 판정 기준이 모호하다 |

## 4. 결론

막는 문제 3 · 경고 8 · 참고 8
