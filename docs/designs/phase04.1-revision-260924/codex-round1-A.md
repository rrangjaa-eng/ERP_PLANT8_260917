Shard A scope: 04.1-01-PLAN.md, 04.1-03-PLAN.md (route/decision table engine; leave balance & first-year accrual & D5 proration)
Run: 2026-09-25 00:56 KST

지정된 두 계획의 must_haves와 D1–D6, 기존 리뷰 항목을 대조하고, 인터페이스와 검증 명령은 저장소 코드로 확인하겠습니다. 금지한 경로는 읽지 않겠습니다.


전사 범위·중복 본인 승인·담당 소멸 처리와 D5 비례 계산은 수정된 결정표와 테스트 사례에 반영되어 있습니다. 현재는 잔고 배분의 연도 경계와 서비스 연결, 테스트가 수정 후에도 성립하는지 확인하고 있습니다.


- [BLOCKER] R01 -- 04.1-01-PLAN.md:424 -- 기안자의 옛 version 승인 오류에 이름·시각이 없어야 하고, 이 테스트가 02 이후에도 그대로 통과한다고 명시한다. 그러나 04.1-02-PLAN.md:349,358은 동일 호출에 처리자 이름·시각을 반드시 포함한다(D1). 두 계획을 그대로 실행하면 회귀 테스트가 실패한다. -- 01에서는 기안자의 오류 종류·상태 불변만 단언하고, 이름·시각 부재는 무관한 사용자에게만 단언하도록 수정한다.

- [WARNING] R02 -- 04.1-03-PLAN.md:207 -- 연차 부여가 0이면 grant를 생성하지 않으면서, 초과 사용은 “해당 회계연도 연차 부여를 음수로” 넘기도록 한다. 입사 첫해에는 넘길 grant가 없어 같은 계획 :283의 `연차 남음 -1` 처리 계약이 비어 있다. -- grant가 없는 연도에도 초과분을 기록하는 연도별 배분 결과를 정의하고, 첫해·연차 설정 0일 사례에서 남음과 `over`를 검증한다.

- [WARNING] R03 -- 04.1-03-PLAN.md:309 -- 조정 로그 인자를 `{type: "document_create", …}`로 지정했지만, 01의 래퍼는 인자를 그대로 `recordAction`에 전달한다(04.1-01-PLAN.md:312). 기존 함수는 `entry.actionType`을 검사한다(`domain/action-log/record.ts:123`). 그대로 구현하면 타입 검사 또는 로그 기록이 실패한다. -- `actionType: "document_create"`로 통일한다.

- [WARNING] R04 -- 04.1-03-PLAN.md:171 -- 음수 월차 조정이 “같은 창의 부여”에서 차감된다는 정의가 불명확하다. 조정 시작일은 입력일이지만 기존 월차 grant들의 시작일은 각 적립일이므로 동일한 유효 창이 아니다. 소멸일이 같다는 사실만으로 차감 시점의 자격이 같아지지는 않는다. -- 조정일 현재 유효한 같은 버킷 grant에서 차감하는지 명시하고, 조정일 이전 사용·이후 적립이 함께 있는 사례로 과거 배분 보존과 음수 잔고 처리를 고정한다.

VERDICT: 1 blockers