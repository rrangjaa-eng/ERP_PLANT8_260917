Shard B scope: 04.1-02-PLAN.md, 04.1-04-PLAN.md, 04.1-05-PLAN.md (approval transitions/conflicts/registry; approval-line settings screen; approval screens)
Run: 2026-09-25 00:56 KST

지정된 세 계획과 공통 정의, 기존 리뷰 및 관련 코드를 대조해 실행 전 결함을 확인하겠습니다. 금지된 경로는 읽지 않겠습니다.


이전 리뷰의 충돌 정보 노출과 상세 DTO 투영 문제는 수정 지시와 테스트까지 추가되어 있습니다. 지금은 설정 가져오기의 실제 저장 방식, 권한 변경 후 버튼 노출, 검증 명령이 기존 코드와 맞는지 확인하고 있습니다.


실행 순서를 그대로 따르면 재신청이 종결 상태 검사에 막히는 모순과, 상세 잔고 조회에 테스트 시계가 전달되지 않는 계약 누락이 보입니다. 권한을 회수한 기안자에게 재신청 버튼이 계속 나오는 경우도 함께 정리하겠습니다.


- [BLOCKER] B1 -- 04.1-02-PLAN.md:292 -- 공용 전이가 재신청까지 `(2) 종결 상태면 거부`를 적용한다. 선행 04.1-01-PLAN.md:302는 `rejected`도 종결 상태로 명시하므로, 같은 줄의 `rejected에서만 재신청`과 충돌한다. -- 종결 검사를 사건별 허용표로 바꾸고 `rejected + resubmit` 예외를 명시한다. 정상 재신청·중복 재신청 테스트 모두 이 공용 경로를 검증해야 한다.

- [BLOCKER] B2 -- 04.1-05-PLAN.md:211 -- `loadDetails` 계약과 실제 호출(:212)은 `{visible}`만 전달하지만, :201–202 테스트는 `deps.now`로 2027·2028년 월차를 검증한다. 04.1-03-PLAN.md:144는 적립 기준일을 `seoulToday(deps.now)`로 정하므로 상세 경로에서 주입이 끊기면 벽시계로 계산된다. -- `listMyInbox → loadDetails → getLeaveBalancesForRequests` 전체에 동일한 `now` 전달 계약을 추가하고, 실제 시계와 다른 주입 날짜로 상세 문자열·DTO를 단언한다.

- [WARNING] W1 -- 04.1-02-PLAN.md:300 -- 반려된 기안자에게 권한과 무관하게 `다시 신청`을 제공하지만, :296은 `leave:write`가 없으면 재신청을 거부한다. 04.1-05-PLAN.md:271은 이 목록대로 편집 폼을 노출해 실행 불가능한 선택을 만든다. -- 서버의 가능 행동 계산에도 종류별 재신청 권한을 반영하고, 권한 회수 후 폼·버튼 부재를 검증한다. 회수 권한은 유지한다.

- [WARNING] W2 -- 04.1-04-PLAN.md:188 -- 모든 설정 칸을 계속 조작 가능하게 두고 힌트로 적용 조건을 설명한다. 꺼진 단계의 담당·범위·부서, 특정 부서 범위가 아닌 단계의 부서 선택은 현재 효과가 없다(:34, :149). -- 기존 값을 보존하면서 해당 컨트롤을 비활성화하고, 조건 설명용 상시 힌트를 줄인다. 단계·범위 변경에 따른 활성화 테스트를 추가한다.

- [WARNING] W3 -- 04.1-04-PLAN.md:201 -- `! pnpm … | grep …`는 첫 Playwright 열거가 실패해도 “스펙 없음”으로 통과할 수 있다. 두 번째 파이프도 Playwright 종료 코드를 직접 확인하지 않는다. -- 각 열거 명령의 성공을 먼저 확인한 뒤 저장한 출력에서 포함·제외를 검사한다.

- [WARNING] W4 -- 04.1-02-PLAN.md:348 -- D1 회귀 테스트는 승인·반려·회수만 호출한다. 네 전이 모두 보호한다는 :58의 재신청 요구는 실제 공개 진입점 `resubmitLeave`로 검증되지 않는다. -- 무관하지만 `leave:write`는 있는 사용자의 유효한 재신청 입력·옛 version 사례를 추가해 일반 오류, 상세 정보 부재, 신청 필드·차수 불변을 단언한다.

- [NOTE] N1 -- 04.1-04-PLAN.md:248 -- 범위 내 이전 리뷰 #5·6·11·12·16·17·18·19는 공통 검증, 관련자 판정, 잔고 정의, 처리함 링크, 투영 후 문자열 생성, 계수 범위, 액션 파일 열거로 계획에 반영됐다(02:236,357; 05:52,60,211,214). -- 위 잔여 결함을 고친 뒤 실행 결과로 확정한다. 나머지 리뷰 항목은 이번 지정 범위 밖이다.

VERDICT: 2 blockers