# Codex Round 1 Triage — 3 shards (A/B/C)

Method: for each finding, opened the cited file:line(s), quoted the key text, classified against D1–D6 / X-1..X-4 / actual repo code where cited.

## Shard A (04.1-01, 04.1-03)

| id | file:line | quote | classification | fix (if CONFIRMED) |
|---|---|---|---|---|
| R01 BLOCKER | 01:424 | "무관한 기획 PM · 기안자 둘 다 거부되고 오류 문구에 처리자 이름 · HH:MM이 없으며 … 이 사례는 오류 종류를 무관한 사용자 쪽에서 단언하지 않는다 … 그래서 04.1-02 뒤에도 이 사례는 그대로 녹색이다" | **CONFIRMED** — the exemption at 01:424 only covers the *error class* for the unrelated user; it does not exempt the "no name/time" wording assertion for the **drafter**. 02:349 (same fixture) explicitly makes the drafter's stale-version `approveDocument` return `{팀장}이/가 HH:MM에 승인함` (name+time), because drafter is 관련자 (D1). So 01:424's blanket "이름·시각이 없다" claim (covering both PM and drafter) breaks once 02 lands. | Narrow 01:424's "이름·시각 없음" assertion to the unrelated PM only; drop it (or move to 02) for the drafter case. |
| R02 WARNING | 03:207,283 | 207: "0이면 그 해 연차 부여를 목록에 넣지 않는다" / 283: "2026 연차 부여 0이라 연차 남음 -1" | **CONFIRMED** — `allocateLeave`'s only defined negative-overflow path is "모자라면 해당 회계연도 연차 부여를 음수로 넘긴다", which needs an existing grant entry for that fiscal year to negate. First-year hires have **no** annual grant entry (0 → omitted from list), so the contract for producing "남음 -1" in that case is undefined. | Define explicitly: `allocateLeave` synthesizes an implicit 0-quarter annual grant for years absent from the grants list when overflow needs somewhere to go negative; add a first-year-overflow unit case. |
| R03 WARNING | 03:309 vs 01:312, `domain/action-log/record.ts` | 03:309: `recordActionInTx(viewer, {type: "document_create", entity: "leave_adjustment", …}, tx, gate, deps)` / code: `recordAction` throws unless `isCoreActionType(entry.actionType)` | **CONFIRMED** — verified in repo: `recordAction` checks `entry.actionType`, not `entry.type`. `recordActionInTx` (01:312) forwards `entry` unchanged. 03's call uses key `type`, so `entry.actionType` is `undefined` → `UnknownActionTypeError`. | Change 03:309's call to `{actionType: "document_create", entity: "leave_adjustment", …}`. |
| R04 WARNING | 03:171 | "음수 조정은 같은 버킷 · 같은 창의 부여 합에서만 빠지는 차감 기록(**창 안의 개별 부여는 소멸일이 같아 어느 것에서 빠지든 결과가 같다**)" | **EVIDENCE-MISMATCH** — the same cited line already states and justifies the reason individual-grant accrual timing doesn't matter: all monthly grants in a hire's window share one dissolution date, so the aggregate (부여합 − 사용 − 결재중) is well-defined regardless of which individual grant is notionally debited. Codex's "differing eligibility at adjustment time" concern doesn't apply because the model is an aggregate-bucket subtraction, not a per-grant chronological allocation. | n/a (no fix needed) |

Duplicates: none within shard A.

## Shard B (04.1-02, 04.1-04, 04.1-05)

| id | file:line | quote | classification | fix (if CONFIRMED) |
|---|---|---|---|---|
| B1 BLOCKER | 02:292 vs 01:296 (truth) | 02:292: "→ (1) version 불일치… → (2) 종결 상태면 거부 → (3) 후보(승인·반려) 또는 기안자(회수·다시 신청) 판정 → (4) … nextStep" / 01 truth: `…approved/rejected/withdrawn` (terminal states incl. `rejected`) | **CONFIRMED** — the shared transition function used by `resubmitDocument` applies an unconditional "(2) 종결 상태면 거부" step before reaching `nextStep`. Since `resubmitDocument` is only valid **from** `rejected`, a terminal state, step (2) as written would reject every resubmit before step (4)'s `nextStep(…, "resubmit")` table lookup could allow it. No event-specific exception is stated. | Make step (2) event-aware: skip/allow the terminal check for the `(status=rejected, event=resubmit)` pair specifically, or move the terminal check after an event-table lookup. |
| B2 BLOCKER | 05:211-212 vs 05:201-202, 03:144 | 05:211: `loadDetails(viewer, documentIds, deps?: { visible })` and `getLeaveBalancesForRequests(viewer, leaveIds, deps?: { visible, … })` (no `now` in signature) / 03:144: "「오늘」…은 `seoulToday(deps.now)`에서만 얻는다" / 05:201-202 tests assert `withDetails` balance strings/DTOs for fixed injected `deps.now` dates | **CONFIRMED** — neither `loadDetails` nor `getLeaveBalancesForRequests`'s documented signature threads `now`/`deps.now` from `listMyInbox`'s caller. Per CEO-11 (03:144) all "오늘" reads must come from injected `deps.now`; without an explicit propagation contract, the balance calc inside the detail path would fall back to real wall-clock, contradicting the fixed-date test expectations at 05:201-202. | Add `now` to the `withDetails`/`loadDetails`/`getLeaveBalancesForRequests` deps contract explicitly, threaded end-to-end from `listMyInbox`'s `deps.now`. |
| W1 WARNING | 02:300 vs 02:296, 05:271 | 02:300 (④-b): "viewer가 기안자이고 상태가 … `rejected`면 `다시 신청`" (no write-permission check) / 02:296: `assertLeaveWrite` guards `resubmitLeave` at the domain layer / 05:271 renders the resubmit form directly from that action list | **CONFIRMED** — `getApprovalView`'s possible-actions list (02:300) is computed purely from drafter/state, with no `leave:write` check, while the actual `resubmitLeave` call is rejected server-side without it (02:296). A drafter whose `leave:write` was revoked after a rejection would still see/attempt the resubmit form (05:271) and hit a late `ForbiddenError`. | Fold a `leave:write` check into the action-list branch in `getApprovalView` (or the leave-kind's action decorator) so the possible-actions list matches what the server will actually allow. |
| W2 WARNING | 04:188, :34, :149 | 188: "기술자 종류(…)와 무관하게 select를 그리고 … 저장 액션 호출·즉시 저장 동작은 그대로" (no disabling logic anywhere in the task) | **CONFIRMED** — no control in the plan is disabled based on step-enabled/scope; per truth :34/:149 several cells (담당 계급 for a disabled step, 부서 for a non-특정 부서 scope) have no effect but remain freely editable, contradicting CLAUDE.md §7 ("할 수 없는 선택지는 … 비활성화"). | Disable dependent selects (role for a disabled step, org-unit select when scope ≠ 특정 부서) while preserving their stored value; add an activation test per step/scope toggle. |
| W3 WARNING | 04:201 | `! pnpm playwright test --list --project desktop … \| grep -q "…" && pnpm playwright test --list --project desktop-settings … \| grep -q "…"` | **CONFIRMED** — without `pipefail`, each pipeline's exit status is `grep`'s, not `playwright test --list`'s; if the first `playwright --list` itself errors/produces no output, `grep -q` still exits non-matching (1) and `! …` (0) reads as "correctly absent" even though enumeration failed silently. | Capture each `playwright --list` exit code separately (or `set -o pipefail`) before grepping its saved output. |
| W4 WARNING | 02:348 vs 02:58 (truth) | 02:58 (truth): "승인 · 반려 · 회수 · 다시 신청은 공용 전이 함수 하나를 지나고 …" (all four transitions equally protected) / 02:348's D1 regression case calls only `approveDocument` · `rejectDocument` · `withdrawDocument` | **CONFIRMED** — the truth claims parity across all four transitions, but the concrete D1 stale-version/unrelated-user regression test (348) never exercises `resubmitLeave` (the public entry point), so that transition's D1 behavior is untested by this case. | Add a `leave:write`-holding drafter + old-version `resubmitLeave` case to the same describe, asserting generic error / no leak / unchanged round. |
| N1 NOTE | 04:248 | "범위 내 이전 리뷰 #5·6·11·12·16·17·18·19는 … 계획에 반영됐다" | **ALREADY-HANDLED** — self-reported roundup note confirming prior review items were folded into 02/05; consistent with what's visible in those plans (D1/X-4 wiring, DTO projection, inbox links). No new action. | n/a |

Duplicates: W1 (shard B) and Shard C's R5 (below) are related (both about a possible-action/CTA surfacing without a matching write-permission check) but touch different files/screens — not exact duplicates.

## Shard C (04.1-06, 04.1-07, cross-plan)

| id | file:line | quote | classification | fix (if CONFIRMED) |
|---|---|---|---|---|
| R1 BLOCKER | 07:138,147 | "`git merge --no-ff --no-commit origin/main`" then verify assumes `HEAD^` is a merge commit with 2 parents | **CONFIRMED** (verified live in repo) — `git merge-base --is-ancestor origin/main HEAD` returns true on the current branch (`origin/main` is already an ancestor of HEAD). With `--no-ff` but nothing to merge, git reports "Already up to date." and creates **no commit at all**, so ①'s two-parent verify script has no merge commit to inspect. | Add an explicit "already up to date" branch: detect via `git merge-base --is-ancestor origin/main HEAD` before merging; if true, skip the merge step and verify migrations against a recorded `origin/main` SHA directly instead of a synthetic second parent. |
| R2 BLOCKER | 06:320, 03:205/211, eslint.config.mjs:45-47 | 06:320: "`[id]/page.tsx`: … 잔고·조정 목록을 서버에서 읽어 넘긴다" / 03:205: `repositories/leave-adjustments.ts` — `listLeaveAdjustments(viewer, userId)`만 / eslint: `{ from: "app", allow: ["app", "domain", "lib", "ui"] }` (no `repositories`) | **CONFIRMED** — 06's read_first (line 299) and action (320) point the page directly at `repositories/leave-adjustments.ts`'s `listLeaveAdjustments`; no domain-level, permission-checked, DTO-projected wrapper for adjustment history exists anywhere in 01/03/05/06. Calling the repository from `app/` violates the cited eslint boundary rule, and raw rows would bypass the `approval.value`/`leave.value` visibility-projection pattern the rest of the phase uses. | Add a thin `domain/leave/balance-service.ts` (or `index.ts`) function e.g. `listLeaveAdjustmentsForUser(viewer, userId)` that checks people-management read access and `project()`s rows through a new `LEAVE_ADJUSTMENT_DTO_SPEC`; have 06's page call that instead of the repository. |
| R3 BLOCKER | 06:261,263,362 vs 01:84 (truth) | 06:261: `previewRoute` "결재선을 해석하되 … 단계 사람 · 단계 이름 … 돌려준다" (raw names) / 06:362 (STRIDE T-04.1-33): "첫 문장 `assertLeaveWrite` — … 결재선 미리보기는 사람 이름만 싣는다" (no `approval.value` check) | **CONFIRMED** — the phase-wide DTO/visibility rule (01:84: "새 DTO … 모든 필드가 `approval.value`·`leave.value` 정보 항목에 매핑") doesn't cover `previewRoute`'s output; its own threat-model entry (362) confirms the only guard is the `leave:write` permission gate, not the `approval.value` info-item. A user with `leave:write` but `approval.value` hidden still sees approver names via preview. | Register a small preview DTO (approver display name field → `approval.value`) and `project()` `previewRoute`'s output before it reaches `previewLeaveAction`; add a `leave:write=true, approval.value=false` denial test. |
| R4 WARNING | 06:265 | "날짜·종류 change마다 `previewLeaveAction` 한 번(받는 동안 **이전 값 유지** — 계획 가정 2)" | **CONFIRMED** — the plan only specifies keeping the stale value while a request is in flight; it never specifies discarding an out-of-order (slow, earlier) response that resolves after a newer one, so a race can overwrite the latest inputs' preview with a stale result. | Tag each `previewLeaveAction` call with the input revision it was issued for and drop responses whose revision isn't current; add a reversed-completion-order test. |
| R5 WARNING | 06:204 (list CTA), 06:318 (adjustment form) vs 03:171 | 204: 1차 `연차 신청` CTA rendering has no write-permission condition stated (only "올해 0건이면 렌더하지 않음") / 318: "잔고 Select 200 `연차`/`월차`" with only a blanket "쓰기 권한이 없으면 입력·폼을 렌더하지 않는다" gate — no per-bucket disabling tied to hire date / expiry | **CONFIRMED** — (a) the list-page CTA isn't explicitly conditioned on `leave:write` (only on 0-applications), so a view-only user could see it; (b) the admin adjustment bucket Select isn't filtered/disabled when the target has no hire date or is past monthly expiry — 03:171's `checkLeaveAdjustment` only rejects at submit time, so an impossible choice stays selectable, contradicting CLAUDE.md §7. | Gate the list `연차 신청` CTA on `leave:write` explicitly; default/disable the `월차` option in the adjustment Select when hire date is missing or past its expiry, with a one-line reason. |
| R6 WARNING | 06:318 | "연도 select는 두지 않는다(지난 연도 정정 = 그 연도를 `?year=`로 연 뒤 조정)" | **CONFIRMED** — the admin person-detail leave section explicitly forgoes a year selector, requiring the operator to hand-edit the URL query string to correct a past year; no in-page link/control into a prior year is specified there (note: `/leave`'s own list page *does* have a year select per 06:204 — this gap is specific to the admin section). | Add a compact year control (or a "지난 연도 보기" link) to `leave-section.tsx` mirroring `/leave`'s year select, backed by the same `resolveLeaveYear`. |
| R7 WARNING | 07 ⓪ (~line 184) vs 05:211 | 07 ⓪: `registerDocumentKind({kind, label, loadRouteConfig, href, describeDocuments, routeSettings?, loadDetails?, detailDto})` (no `buildDetailRows`) / 05:211: "`loadDetails`가 있으면 **둘 다 필수**인 `detailDto` … `buildDetailRows`" | **CONFIRMED** — the Phase 5 handoff contract documented in ARCHITECTURE §4-N omits `buildDetailRows` and the projection-before-formatting order (ENG-17), even though 05 makes both mandatory companions to `loadDetails`. A Phase 5 reader following only this contract would under-register or reimplement string-building ahead of projection. | Add `buildDetailRows` (and the "project then format" ordering note) to the ⓪ contract summary. |
| R8 WARNING | 06:155,192,193,305 | Relative-date E2E dates (`leaveWeekdayRange(seoulToday(), …)`) computed independently in spec and server, both via real wall-clock `seoulToday()` (no shared injected clock in E2E) | **CONFIRMED, low severity** — spec-side and server-side `seoulToday()` calls are independent wall-clock reads; a Seoul midnight/year-end/month-end crossing between the two reads (rare, narrow window) can shift an accrual/fiscal-year boundary and desync the test's expected string from the server's computed one. This is an accepted trade-off elsewhere in the phase (E2E doesn't inject `deps.now`), but it is a real, uncontrolled flake source specifically at day/month/year boundaries. | Either accept as a known flake window (document it) or have the E2E spec read the server's resolved date/fiscal-year from the rendered page instead of recomputing independently, for boundary-adjacent scenarios. |
| R9 WARNING | 06:60 (truth), 294/320 (Task 3 action) | 60 (truth): "연차 목록·사람 상세 연차 섹션 로딩은 뼈대 행 …, 로드 실패는 `연차를 불러오지 못했습니다 · 다시 시도`" / Task 3's action (316–325) has no `loading.tsx`/`error.tsx` (or equivalent) for `admin/people/[id]`'s leave section, unlike Task 1's explicit `loading.tsx`/`error.tsx` for `/leave` | **CONFIRMED** — Task 1 (list page) explicitly wires `loading.tsx`/`error.tsx` (204: "`loading.tsx` · `error.tsx`는 04.1-05의 두 파일과 같은 모양"); Task 3's action list for the admin section has no equivalent step or verify coverage, though the phase-wide truth (60) promises the same skeleton/error behavior there too. | Add a loading/error boundary for the admin leave section (or explicitly scope it out of Task 3 with a truth amendment) plus a failure/retry test. |
| R10 WARNING | 07:151 (verify) | `pnpm db:generate 2>&1 \| tee /dev/stderr \| grep -q "No schema changes" && …` (no `pipefail`, no captured exit code for `db:generate`) | **CONFIRMED** — without `pipefail`, the pipeline's exit status is `grep`'s. In the common failure mode (generate errors and never prints the phrase) the check still correctly fails since grep finds no match — but if a future `db:generate` failure path happens to echo text containing the substring "No schema changes" (e.g. inside a longer diagnostic dump) before erroring, the pipeline would pass despite the generator failing. The check does not independently assert `db:generate`'s own exit code. | Capture `db:generate`'s exit code explicitly (`set -o pipefail`, or `... ; rc=$?`) and require it to be 0 in addition to the grep match. |

Duplicates: none within shard C. R2(C) and R3(C) are both "missing DTO/visibility projection" issues but on different data paths (adjustment history vs. route preview) — related in kind, not duplicates.

## Summary counts

- BLOCKER: 6 total (shard A: R01; shard B: B1, B2; shard C: R1, R2, R3) — all 6 CONFIRMED, none rejected.
- WARNING: 13 total — 12 CONFIRMED (R02, R03 [shard A]; W1, W2, W3, W4 [shard B]; R4, R5, R6, R7, R8, R9, R10 [shard C]), 1 EVIDENCE-MISMATCH (R04 shard A).
- NOTE: 1 total (N1, shard B) — ALREADY-HANDLED.
