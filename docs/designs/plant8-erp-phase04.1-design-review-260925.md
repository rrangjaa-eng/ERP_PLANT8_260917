# Phase 04.1(결재·연차) 디자인 계획 리뷰 (plan-design-review) — 2026-09-25

대상: `.planning/phases/04.1-approvals-leave/04.1-UI-SPEC.md`(S1~S10, 개정 제안 A1~A4) + `04.1-01`~`07-PLAN.md`의 UI 태스크 @ `d7372af`
기준: `docs/design/SYSTEM.md` · `docs/design/tokens.css` · 실물 `docs/design/system/sheet-modal.html` · Phase 4 개정본(`origin/claude/gsd-progress-e1nzgu:docs/design/SYSTEM.md` §7-17·P0) · **CLAUDE.md §7 화면 사용성 원칙**(최우선 렌즈)
분석: Opus 1차 판단(`phase04.1-design-review-260925/opus-primary.md`) + Opus 독립 교차 검토(`opus-cross.md`, 서로 결과를 보지 않음). 지시문은 `prompt-common.md`.
- 교차 검토를 Codex 대신 Opus로 한 것은 사용자 결정 「Opus」다. Codex는 한도(9/29까지)라 호출하지 않았다. **한도가 풀리면 Codex 재확인이 필요하다.**
- 막음 근거와 주요 경고 근거는 원문 파일을 직접 열어 대조했다. 틀린 줄 번호 넷은 고쳤다(아래 「근거 정정」).

목업: 만들지 않았다. OpenAI 이미지 API는 유료이고 Codex도 한도다. 무엇보다 이번 쟁점은 레이아웃이 아니라 **계약(UI-SPEC↔PLAN)·상태·문구**다.
보고서 위치: CEO·엔지니어링 리뷰와 같은 `docs/designs/`에 둔다. `.planning/`은 도구로만 고친다 — 반영은 다음 세션의 `/gsd-ui-phase 04.1`(UI-SPEC 개정)과 `/gsd-plan-phase 04.1` 수정 회차가 한다.

약어: UI-SPEC = `04.1-UI-SPEC.md`, 0N-PLAN = `04.1-0N-PLAN.md`, SYSTEM = `docs/design/SYSTEM.md`(로컬), 원격 SYSTEM = Phase 4 개정본.

## 요약

- 분류: OPERATE(업무 앱). HARD REJECTION 0건(교차 검토 판정, 1차 동의). 새 색·서체·radius 0개. 시각 방향은 적합하다.
- 핵심 문제: **엔지니어링 2회차에서 플랜은 바뀌었는데 UI-SPEC(정본)은 그대로다.** 그래서 실행자와 `/design-review`·DOM 감사가 다른 기준으로 판정하게 된다. 그리고 공용 `ui/select`가 늘 `—`를 그려 **틀린 선택이 가능**하다.
- 쟁점 28건(1차 19 + 교차 23, 중복 병합). 막음 5 · 경고 13 · 참고 10. 새 기능 추가 없음.
- 종합 점수 **6 → 8**(반영 회차가 끝나야 실제 8).

## 점수

처음 점수는 두 검토 중 낮은 쪽을 쓴다.

| 패스 | 1차 | 교차 | 처음(채택) | 결정 반영 후 | 남는 것 |
|---|---|---|---|---|---|
| 1 정보 구조 | 7 | 7 | 7 | 9 | 제목·KvList 중복(참고 20)은 둔다 |
| 2 상태 | 7 | 6 | 6 | 9 | 고아 최종 문구는 사용자 확인(11) |
| 3 사용자 여정 | 7 | 6 | 6 | 9 | 반려 문서 종착은 사용자 확인(17) |
| 4 AI 슬롭·구체성 | 9 | 8 | 8 | 9 | S4 부제는 범위 밖(참고 27) |
| 5 SYSTEM·토큰 정렬 | 7 | 7 | 7 | 9 | `ui/select` 계약 정렬은 Phase 4 소유 |
| 6 반응형·접근성 | 7 | 6 | 6 | 8 | 1024·태블릿 터치는 DOM 감사로 실측 |
| 7 미결 결정 | 6건 | 8건 | 28건 | 28 결정 · 0 이월 | 사용자 확인 권장 5건 |
| **종합(최저값)** | | | **6/10** | **8/10** | |

## 결정 원장 (28건)

**사용자는 자는 중이라 모든 결정은 추천 기본값(각 검토의 A안 또는 병합안)으로 정했다.** 취향·정책 성격인 항목은 「사용자 확인 권장」으로 표시했다 — 아침에 뒤집어도 반영 회차 전이면 비용이 없다. 사용자가 이미 정한 결정(05-CONTEXT D-96·D-97, CEO 리뷰 IA, Phase 4 개정 P0·`Ctrl+`·진행 바 없음)은 뒤집지 않았다.

| # | 등급 | 원 ID | 화면 | 문제 | 근거 파일:줄 | 결정 |
|---|---|---|---|---|---|---|
| 1 | 막음 | OP-01 · X-04 · OP-18 | S2 · S1 · S9 | `ui/select`가 옵션이 있어도 늘 첫 옵션 `—`를 그린다. 자기 주석·§7-15(「고를 것이 없으면」)와 다르다. 종류·시간·연도·조정 잔고에서 `—`를 고를 수 있고, S2 종류·시간 `—`의 막힘 문구가 없다 | `ui/select/Select.tsx:4-5` vs `:29` · SYSTEM:985 · UI-SPEC:203 · :262 · :277 · 06-PLAN:343-344 · :508 | 04.1은 **제어 기본값**을 규칙으로 적는다: 종류 `종일`, 시간 `오전`, 연도 = 올해, 조정 잔고 `연차`. `—`를 고르면 막힘 `종류 비어 있음 · 종류 고르기` / `시간 비어 있음 · 시간 고르기`. `ui/select` 계약 정렬(옵션 0개일 때만 `—`)은 Phase 4 소유라 DECISIONS에 요청으로 남긴다. 06-PLAN:344의 `—` 선택 E2E는 서버 거부 통합 테스트로 옮긴다(UI의 `—`에 기대지 않게) |
| 2 | 막음 | OP-03① · X-01 · X-17 | S4 · S5 | 폰 `처리함` 행 탭: UI-SPEC은 「행동 줄 없는 시트」, 플랜은 「`/leave/[id]` 이동」(ENG-16). 폰 `내 결재` 행은 「행 링크」라 불리지만 실제로는 시트 트리거다 | UI-SPEC:343 · :351-352 · 05-PLAN:197 · :218 · :244 | UI-SPEC을 플랜에 맞춘다. 351행 = 「처리함 행 = 문서 링크(`<a>`)」, 352행의 「행동 줄이 없으면 닫기 x」 삭제. 폰 `내 결재` 행 = `button` + `aria-haspopup="dialog"`, `처리함` 행 = 링크로 명시 |
| 3 | 막음 | OP-03② · X-02 · OP-17 | S3 · S4 · S5 | 「기안자이면서 지금 담당」(본인 승인·대표 폴백)의 행동 `승인` + `회수`(반려 없음)가 UI-SPEC S3 표·Copywriting·S5에 없다. 플랜은 PC 결재함 행에 3차 `승인`·`회수`를 나란히 둔다 | UI-SPEC:193 · :307-312 · 05-PLAN:50 · :283 | UI-SPEC S3 표에 다섯째 행 「기안자 = 지금 담당 → 1차 `승인 Ctrl+Enter` + 2차 `회수`, 반려 없음」, Copywriting 193행·S5에 같은 갈래. **결재함 PC 행에는 3차 `승인`만** 두고 `회수`는 문서 화면·시트에서만(05-PLAN:283 수정) |
| 4 | 막음 | X-03 | S2 · S3 · S5 · S6 | 제출 중 같은 줄의 다른 버튼을 플랜은 실제 `disabled`로, UI-SPEC과 Phase 4 §7-17은 `aria-disabled`로 정한다. 실제 `disabled`는 포커스를 `body`로 떨어뜨리고 공용 ConfirmDialog 계약과 갈린다. 엔지니어링 결정(L160)이지 사용자 결정이 아니다 | 05-PLAN:49 · :364 · :411 · UI-SPEC:284 · :360 · :436 · 원격 SYSTEM:1073 | §7-17대로: 누른 버튼은 `pending`(라벨 `…`), **나머지는 `aria-disabled`**. 두 번 누름은 동기 ref 가드(06-PLAN:408과 같은 방식)로 막는다. E2E 단언은 「`disabled` 속성」에서 「요청 한 번·나머지 `aria-disabled`」로 바꾼다 |
| 5 | 막음 | OP-02 | S2 · S3 · S5 | 같은 숫자를 두 자리에 쓴다(P0 위반). S2 일수 힌트 `이번 신청 3일`과 잔고 행 `… · 이번 신청 3일`, S3·S5 KvList `일수 3일`과 잔고 행 | UI-SPEC:181 · :208 · :265 · :268 · :293 · :295 · 원격 SYSTEM:1086 | 잔고 행(입력 §5 정본 숫자)은 그대로. S2 힌트는 `주말 2일 제외`만(없으면 힌트 없음, 재택은 `재택 · 차감 없음`). S3·S5 `일수` 행은 잔고 행이 없을 때(재택·종결 문서)만 그린다 |
| 6 | 경고 | X-09 · OP-06 | S1 | 연도를 부제·필터·EMPTY 세 자리에 쓴다. 첫해 사용자는 연도 select에 고를 것이 올해 하나뿐이다 | UI-SPEC:196-197 · :234 · :237 · 06-PLAN:176 | 연도는 한 자리에만: 옵션이 둘 이상이면 select만(부제 없음), 하나면 select를 그리지 않고 부제 `{연도} 회계연도`만. EMPTY에서 연도를 뺀다(`신청한 연차가 없습니다 · …`) |
| 7 | 경고 | OP-04 · X-10 | S2 | 하루짜리 종일·재택도 종료일을 따로 적어야 1차가 켜진다. 시스템이 알 수 있는 값을 묻는다 | UI-SPEC:203 · :263-264 · :277 · 06-PLAN:62 | 시작일을 고르면 종료일이 비었거나 시작일보다 빠를 때 시작일로 채운다. 막힘은 `시작일 비어 있음 · 시작일 적기` 하나 |
| 8 | 경고 | OP-07 | S4 | 그룹 머리글 `내 결재` 아래 모든 행의 상태 칸이 또 `내 결재`다(P0). 폰 P1 세 칸 중 하나를 중복이 차지한다 | UI-SPEC:327-328 · :335 · `sheet-modal.html:217` | `내 결재` 그룹 행의 상태 칸은 비운다(폰은 문서 칸이 그 폭을 쓴다). 처리함 행은 그대로 |
| 9 | 경고 | OP-08 · X-15 | S8 | 한 화면에서 `—`가 두 뜻이다(계급 `—` = 조건 없음, 부서 `—` = 빈 자리). 3단 기본값 기술이 UI-SPEC 안에서 374행과 474행이 다르다 | UI-SPEC:374 · :474 · 04-PLAN:190 · SYSTEM:985 | 계급 select의 빈 값 라벨을 `계급 무관`으로(관리자 화면이라 §7-2 라벨 허용). 374행을 474행(결정 1: 계급 `—` · 부서 = 경영관리본부)에 맞춘다 |
| 10 | 경고 | OP-03③④ · X-08 · OP-16 | S8 · S9 | 2회차 플랜의 화면 요소가 UI-SPEC에 없다: S8 효과 없는 칸 비활성(CX-W2), S9 `{Y-1} 회계연도`·`올해 보기` 링크·연도 부제, `월차` 옵션 빠짐 + 한 줄, 조정이 「보는 연도」로 기록됨. 지난 연도를 보는 채로 조정하면 버튼이 그 사실을 말하지 않는다 | 04-PLAN:35 · :192 · 06-PLAN:64 · :349 · UI-SPEC:380-388 | UI-SPEC S8·S9에 플랜 요소를 확정한다. S8 비활성 칸은 기존 힌트(UI-SPEC:219 `조직 범위가 특정 부서일 때만`)를 그대로 보인다. 지난 연도를 볼 때 S9 1차 라벨에 연도를 넣는다(`2025 연차 조정 추가`) |
| 11 | 경고 | OP-09 | S3 · S7 | 담당이 사라져 멈춘 문서(고아 최종 W10·W13 = 막힘)에서 기안자에게 할 일이 없다. 결재선에 붉은 `담당 없음`뿐이다. UI-SPEC 473행은 일반 규칙(「뒤가 전부 비면 대표가 받는다」)만 적고 막힘 예외를 빠뜨렸다 | UI-SPEC:210 · :473 · 01-PLAN:68 · :465 · :468 · :470 | 기안자에게만 결재선 끝에 `결재할 사람 없음 · 회수 후 새로 신청`(`--danger`, 회수 = 같은 행동 줄 2차). 473행에 「대표 1단 승인 뒤 남은 담당이 전부 사라지면 막힘(W10·W13)」을 더한다. **사용자 확인 권장**(문구 정책 — 「관리자가 담당을 정하면 이어짐」 쪽도 가능) |
| 12 | 경고 | OP-12 · X-11 · X-23 · OP-15 | S4 | PC·태블릿 결재함 행 3차 `승인`은 확인·되돌리기 없이 즉시 실행되고, 3차 `반려`와 붙어 있다. 실물은 PC 행 `승인`이 근거 시트를 연다. Assumptions #11은 「confirm later」이지 사용자 결정이 아니다. 태블릿(700~1023) 터치에서 32px 버튼 | UI-SPEC:193 · :341-342 · :464 · :506 · `sheet-modal.html:218` · SYSTEM:193 · :1039 | 즉시 승인은 유지하고 DECISIONS에 「실물과 다름: PC 행 승인은 즉시」를 적는다(화면 하나만 예외 방지). 행 안에서 `승인`과 `반려` 사이를 `--s-4` 이상 띄운다. `pointer: coarse`에서 행 행동 버튼에 `--touch-min` 터치 영역. **사용자 확인 권장**(#11 — 대안: PC 행 `승인` → 문서 화면에서 `Ctrl+Enter`) |
| 13 | 경고 | OP-10 | S4 | 행마다 `승인`·`반려`가 되풀이돼 스크린리더·키보드 사용자는 어느 문서의 승인인지 모른다 | UI-SPEC:342 · :471 · SYSTEM:1031-1042 | 버튼에 `aria-describedby`로 그 행의 문서 칸을 잇는다(보이는 글자는 그대로) |
| 14 | 경고 | X-05 | S1 · S2 · S3 · S5 | 월차 표시 조건이 다르다. UI-SPEC은 「부여가 있을 때만」·「0이고 적립 없으면 둘째 줄 없음」, 플랜은 「근속 첫 1년 안이면 0이어도 보임」(D4) | UI-SPEC:207 · :236 · :452 · 06-PLAN:56 · :61 | UI-SPEC 207·236·452행을 플랜(D4) 규칙으로 정정 |
| 15 | 경고 | X-06 | S3 | UI-SPEC은 S3 로딩 「해당 없음」, 플랜은 `leave/[id]/loading.tsx` 뼈대를 만든다 | UI-SPEC:435 · :437 · 05-PLAN:313 | UI-SPEC 435행 로딩 행에 S3(PageHeader + KvList 뼈대)을 더하고 437행에서 S3를 뺀다 |
| 16 | 경고 | X-07 | S6 | 반려 사유 칸 「긴 칸 480」은 480 모달(좌우 패딩 포함)에 들어가지 않는다. 실물은 라벨 + `1fr`, `width:100%` | UI-SPEC:216 · `sheet-modal.html:134-136` · `:142` | 「사유 = 라벨 + 남은 폭(실물 `.reason`), 폰 전폭」으로 정정 |
| 17 | 경고 | X-12 | S1 · S3 | 반려 문서는 다시 신청 말고 끝낼 길이 없다(회수는 결재 중만). 다시 내지 않을 문서가 그해 내내 목록 맨 위 `반려` 그룹에 남는다 | UI-SPEC:250 · :311 · 02-PLAN:67 | 그룹 순서를 `결재 중 → 반려 → 승인 → 회수`로 바꾼다(상태 전이는 그대로 — 새 기능 없음). **사용자 확인 권장**(정책 — 대안: 반려 문서에도 기안자 `회수` 허용, 상태 전이 변경이라 범위 확인 필요) |
| 18 | 경고 | X-13 | S6 | 회수 결과 줄 `결재 멈춤 · 김팀장의 결재함에서 빠짐`은 담당이 한 명일 때만 성립한다 | UI-SPEC:217 · 05-PLAN:267 | 이름 조각은 S7 후보 표기 규칙 그대로(`이수아 외 2명의 결재함에서 빠짐`). 담당이 없으면 `결재 멈춤`만 |
| 19 | 참고 | OP-05 · X-21 | S1 | 0건이면 머리 1차를 치우고 설명 한 줄 + 3차(§7 「빈 화면엔 첫 행동 버튼」과 결이 반대지만 Phase 4 선례). 쓰기 권한 없는 사람의 EMPTY는 플랜에만 있다 | UI-SPEC:188 · :196 · SYSTEM:810 · 06-PLAN:58 | 선례를 따르므로 구조는 둔다. 권한 없는 사람 EMPTY(「앞 문장만」)를 UI-SPEC 196행에 반영 |
| 20 | 참고 | OP-11 · X-16 | S3 · S4 · S5 | 제목과 KvList `종류`·`기간`이 같은 값. 제목 구분자가 문서 화면 `—`, 시트 `·`로 다르다 | UI-SPEC:289 · :350 | 제목 자리(S3 머리·S5 시트 머리)는 §6-3 원형대로 `—`로 맞춘다(350행 수정). 표 셀은 `·` 유지 — 표에서 `—`는 「비어 있음」이다. KvList 중복은 둔다 |
| 21 | 참고 | OP-13 | S2 | 신청 토스트에 되돌리기가 없다(§6-3 선례와 다름). 착지 화면에 `회수`가 있다 | UI-SPEC:214 · SYSTEM:410 | A4 개정 문장에 「되돌리기 = 회수」 한 줄로 차이를 남긴다 |
| 22 | 참고 | OP-14 | S1 · S3 · S4 | 300ms 지연 뼈대 CSS를 `approvals/loading.module.css`에 두고 셋이 쓴다(DESIGN.md §4 규칙 3 대상) | 05-PLAN:313 · :321 | Phase 4 `ui/` 충돌 회피가 이유면 Assumptions에 한 줄 |
| 23 | 참고 | OP-19 | S2 · S7 | 결재선 끝 `· 결재 규칙`은 누를 곳 없는 꼬리 글자 | UI-SPEC:209 | SYSTEM §6-3 원문이라 둔다 |
| 24 | 참고 | X-14 | S2 | `신청하지 못했습니다 · 네트워크`에 다음 행동이 없다 | UI-SPEC:202 · SYSTEM §7-7 ERROR 예 | `신청하지 못했습니다 · 네트워크 · 다시 신청`(SYSTEM 예의 틀) |
| 25 | 참고 | X-18 | S9 | 연차 섹션 읽기 실패가 사람 상세 전체를 오류 화면으로 바꾼다 | 06-PLAN:68 · :351 · UI-SPEC:437 · :444 | 받아들이고 UI-SPEC 오류 행에 적는다(새 경계 없음) |
| 26 | 참고 | X-19 | S5 → S6 | 폰 반려 확인 `취소`가 결재 시트가 아닌 목록 행으로 돌아간다 | UI-SPEC:354 · :462 | 확정 설계(시트 중첩 없음)라 유지 |
| 27 | 참고 | X-20 | S4 | 부제 `내가 처리할 결재 문서`는 설명형(P0) | UI-SPEC:324 | 「현재 그대로」 범위 밖. Phase 5에서 |
| 28 | 참고 | X-22 | 문서 정합 | 02-PLAN은 `{기안자}이/가 HH:MM에 다시 신청함`을 「UI-SPEC에 없는 새 문구」라 적었지만 UI-SPEC 205행에 이미 있다 | 02-PLAN:402 · :504 · UI-SPEC:205 | 수정 회차에서 메모 표현만 정리 |

### 근거 정정 (원 보고서 대조)

- OP-09: 「UI-SPEC 473행이 플랜과 반대」는 과하다. 473행은 01-PLAN:63·68의 일반 규칙과 같고, **W10·W13 막힘 예외를 빠뜨린 것**이다(#11에 반영).
- X-03: 05-PLAN:48 → **:49**(실제 `disabled` 문장). 동기 가드 선례는 06-PLAN:289가 아니라 **:408**(T-04.1-48).
- X-12: 회수 조건 근거 02-PLAN:66 → **:67**.
- X-22: 02-PLAN:375 → **:402 · :504**.

## 두 검토의 불일치와 판정

| 항목 | 1차 | 교차 | 판정 · 이유 |
|---|---|---|---|
| 같은 숫자 두 자리(#5) | 막음 | 없음 | **막음.** 원격 SYSTEM:1086 P0 위반이 세 화면에 구현된다 |
| 제출 중 `disabled`(#4) | 없음 | 막음 | **막음.** §7-17 계약 위반이고 원격 SYSTEM:1073으로 확인됨. 엔지니어링 결정이라 사용자 결정 보호 대상이 아니다 |
| select `—` 해법(#1) | Phase 4에 계약 정렬 요청 | 04.1 기본값 + 막힘 문구 | **둘 다.** 04.1이 혼자 할 수 있는 것(기본값·문구)을 먼저, 공용 파일은 요청만(병렬 충돌 회피) |
| S8·S9 동기화(#10) | 막음(OP-03 묶음) | 경고 | **경고.** 관리자 화면이고 오조작 피해가 좁다. 처리함·본인 승인(#2·#3)만 막음으로 뗐다 |
| 본인 승인 행의 `회수`(#3) | 행 안에서 떨어뜨림 | 행에서 뺌 | **행에서 뺌.** 행 안에 되돌릴 수 없는 두 행동을 붙이지 않는 쪽이 단순하다 |
| S1 연도(#6) | select 숨김 | 부제 없앰 | **병합.** 옵션 수에 따라 한 자리만 |
| PC 즉시 승인(#12) | 유지 + DECISIONS | 떨어뜨림 + 사용자 확인 | **병합.** 셋 다 적용하고 사용자 확인 권장 |
| 제목 구분자(#20) | 언급 없음 | `·` 추천 | **`—`(제목만).** SYSTEM §6-3 원형 우선, 표 셀의 `—`는 「비어 있음」이라 셀은 `·` |

## 리트머스 · 하드 리젝션 (OPERATE)

하드 리젝션 7개 — 모두 NO(통과).

| # | 기준 | 교차 판정 | 1차 의견 |
|---|---|---|---|
| 1 | 첫인상이 일반 SaaS 카드 그리드 | NO — 원장 표 + 그룹 머리글(UI-SPEC:232-245 · 322-337) | 동의 |
| 2 | 예쁜 이미지에 약한 브랜드 | NO — 이미지 없음 | 동의 |
| 3 | 강한 제목에 분명한 행동 없음 | NO — 화면마다 1차 하나(기안자 결재 중만 의도적으로 2차 `회수`, UI-SPEC:194) | 동의. 본인 승인 갈래(#3)가 정본에 올라가야 완결 |
| 4 | 글 뒤에 복잡한 이미지 | NO | 동의 |
| 5 | 같은 분위기 문장 반복 | NO(경계) — S1 연도 세 번(#6) | 동의. 숫자 두 자리(#5)도 같은 결 |
| 6 | 서사 없는 캐러셀 | NO | 동의 |
| 7 | 레이아웃 대신 카드 쌓기 | NO — KvList·표·한 열 폼 | 동의 |

리트머스 7개.

| # | 질문 | 교차 판정 | 1차 의견 |
|---|---|---|---|
| 1 | 첫 화면에서 제품이 분명한가 | YES — `--accent` 다섯 자리만 | 동의 |
| 2 | 강한 시각 닻이 하나인가 | YES — S1 잔고 줄 · S4 `내 결재` · S2·S3 1차 | 동의. #8 반영 뒤 S4 닻이 더 선명 |
| 3 | 머리글만 훑어도 이해되는가 | 부분 YES — 폰 처리함 탭 결과가 그룹마다 다름 | 동의. #2(행 요소 구분)로 해소 |
| 4 | 섹션마다 일이 하나인가 | YES | 동의 |
| 5 | 카드가 꼭 필요한가 | 카드 없음(통과) | 동의 |
| 6 | 모션이 위계를 돕는가 | 모션 없음(적절) — 300ms 지연 불투명도 한 번 | 동의 |
| 7 | 그림자를 다 빼도 고급스러운가 | YES — 그림자 0, 2px 선과 여백 | 동의 |

## 반영 지시 (다음 세션)

순서: **T1(UI-SPEC 개정) → T2(플랜 수정 회차) → 최종 전체 검토.** UI-SPEC이 정본이므로 먼저 고친다.

| T | 할 일 | 도구 · 파일 | 결정 | 확인 방법 |
|---|---|---|---|---|
| T1 | S2 제어 기본값(종류 `종일`·시간 `오전`)과 `—` 막힘 문구 둘, S1 연도 기본 = 올해, S9 잔고 기본 `연차` | `/gsd-ui-phase 04.1` 개정 · UI-SPEC Copywriting 203행 근처 · S2 · S1 · S9 | #1 | UI-SPEC에서 `종류 비어 있음 · 종류 고르기` Grep 1건 이상 |
| T2 | DECISIONS에 「`ui/select` `—`는 옵션 0개일 때만 — Phase 4에 정렬 요청」 기록(Phase 4 머지 뒤 처리) | `/gsd-ui-phase 04.1` · UI-SPEC 개정 제안 A-계열 + `docs/design/DECISIONS.md` 반영 태스크 | #1 | A-계열 목록에 항목 1개 |
| T3 | 06-PLAN:344 `—` 선택 E2E를 서버 거부 통합 테스트로 옮기고, 06-PLAN:508 수락 기준 문구 정리 | `/gsd-plan-phase 04.1` 수정 회차 · 06-PLAN Task 3 | #1 | 06-PLAN에서 E2E의 `—` 선택 단계 0건 |
| T4 | S5 351-352행을 「처리함 행 = 문서 링크」로, 폰 `내 결재` 행 = `button` + `aria-haspopup="dialog"` | `/gsd-ui-phase 04.1` · UI-SPEC S4 343행 · S5 | #2 | UI-SPEC과 05-PLAN:197·244가 같은 말(교차 Grep `처리함`) |
| T5 | S3 행동 표 다섯째 행(기안자 = 지금 담당), Copywriting 193행·S5에 같은 갈래 | `/gsd-ui-phase 04.1` · UI-SPEC:193 · :307-312 · S5 | #3 | S3 표 행 5개 |
| T6 | 05-PLAN:283 PC `내 결재` 행에서 3차 `회수` 제거(문서 화면·시트만) + 해당 E2E 기대 수정 | `/gsd-plan-phase 04.1` · 05-PLAN Task 2 ⑤ | #3 | 05-PLAN:283에 행 `회수` 렌더 문장 0건 |
| T7 | 제출 중 규칙을 §7-17대로: 누른 버튼 `…`, 나머지 `aria-disabled`, 동기 ref 가드. E2E 단언 교체. 05-PLAN:411 표 행 개정 | `/gsd-plan-phase 04.1` · 05-PLAN:49 · :364 · :411 · 관련 E2E | #4 | 05-PLAN에서 「같은 줄의 버튼이 실제 `disabled`」 0건 |
| T8 | S2 힌트는 `주말 2일 제외`만, S3·S5 `일수` 행은 잔고 행 없을 때만 | `/gsd-ui-phase 04.1` · UI-SPEC:208 · :265 · :293 · 06-PLAN:61 · 05-PLAN KvList 태스크 | #5 | 화면 한 곳에 `이번 신청` 숫자 1회(DOM 감사) |
| T9 | S1 연도 한 자리 규칙 + EMPTY에서 연도 삭제, 권한 없는 EMPTY 반영 | `/gsd-ui-phase 04.1` · UI-SPEC:196-197 · :234 · :237 → 06-PLAN:58 · :176 · :219 | #6 · #19 | UI-SPEC S1에 연도 표기 자리 1곳 |
| T10 | 시작일 선택 시 종료일 채우기, 막힘 문구 하나로 | UI-SPEC:203 · :277 → 06-PLAN:62 · :289 | #7 | 단위/E2E: 시작일만 고르면 1차 켜짐 |
| T11 | `내 결재` 그룹 행 상태 칸 비움 | UI-SPEC S4 · 05-PLAN Task 2 ④ | #8 | DOM: `내 결재` 글자가 그룹 머리글 1회 |
| T12 | S8 계급 빈 값 라벨 `계급 무관`, 374행을 474행에 맞춤, 비활성 칸 힌트 유지 | UI-SPEC:374 · S8 → 04-PLAN:190 · :192 | #9 · #10 | UI-SPEC 안 3단 기본값 기술 일치 |
| T13 | S9 플랜 요소 확정(연도 부제·두 링크·월차 옵션 빠짐 + 한 줄), 지난 연도 1차 라벨 `{연도} 연차 조정 추가` | UI-SPEC S9 → 06-PLAN:64 · :349 | #10 | 지난 연도 E2E에서 1차 라벨에 연도 |
| T14 | 기안자용 고아 최종 한 줄 + 473행 예외 추가 | UI-SPEC:210 · :473 → 05-PLAN Task 2 ② | #11 | 상태 표 473행에 W10·W13 언급 |
| T15 | PC 즉시 승인 DECISIONS 기록, 행 `승인`·`반려` 간격 `--s-4`, `pointer: coarse` 터치 영역, `aria-describedby` | UI-SPEC:341-342 · :506 · a11y 줄 → 05-PLAN Task 2 ⑤ | #12 · #13 | DOM 감사 1024 폭 + coarse 에뮬레이션에서 터치 영역 ≥ 44 |
| T16 | 월차 표시 조건(D4), S3 로딩 행, 사유 칸 폭, 회수 결과 줄 담당 여럿·없음 | UI-SPEC:207 · :236 · :452 · :435 · :437 · :216 · :217 | #14 · #15 · #16 · #18 | UI-SPEC과 05·06-PLAN 문자열 교차 Grep 일치 |
| T17 | 반려 그룹 순서 `결재 중 → 반려 → 승인 → 회수` | UI-SPEC:250 → 06-PLAN 목록 태스크 | #17 | E2E 그룹 순서 단언 갱신 |
| T18 | 참고 반영: 제목 `—`(350행), A4에 「되돌리기 = 회수」, 로딩 CSS 위치 Assumptions, 네트워크 오류 `· 다시 신청`, S9 로드 오류 행, 02-PLAN:402 메모 정리 | UI-SPEC · 02/05/06-PLAN | #20 · #21 · #22 · #24 · #25 · #28 | 각 1건 Grep |
| T19 | 반영 뒤 UI-SPEC 체커 재실행 → 최종 전체 검토(Fable 1회) | `/gsd-ui-phase` 체커 · 최종 검토 | 전체 | 체커 PASS, 교차 Grep 불일치 0 |

## NOT in scope

- 새 기능: 반려 문서 기안자 회수(#17 대안), 최근 입력값 기억(시간 기본값은 `오전` 고정), 결재 규칙 화면 링크(#23).
- `ui/select` 코드 수정 — Phase 4 소유 파일. 04.1은 요청만(#1·T2).
- S4 부제 정리(#27) — Phase 5.
- 이미지 목업 — 이유는 머리 참고.
- 사용자 결정 변경: D-96·D-97, CEO 리뷰 IA(더보기 → 계정 → 연차), Phase 4 P0·`Ctrl+`·진행 바 없음, 시트 중첩 없음.

## What already exists

- 공용 컴포넌트: `ui/select`, `ui/button` `pending`, `ui/confirm-dialog`(Phase 4 S16 · §7-17 계약), `Form.Hint`, KvList.
- 실물: `docs/design/system/sheet-modal.html`(결재함 행·근거 시트·반려 모달 `.reason`).
- 규칙: SYSTEM §6-3(결재선 한 줄·제출 토스트), §7-7(다섯 상태), §7-15(Select), §10(접근성), 원격 SYSTEM §7-17·P0(1073·1086행).
- 선행 검토: CEO 리뷰(`plant8-erp-phase04.1-ceo-review-260924.md`), 엔지니어링 2회차(`phase04.1-eng-review2-260925/` cross-notes·fable-final-full).
- 04.1 UI-SPEC 토큰 29개 전부 `tokens.css`에 있음(교차 검토 확인).

## 미결 결정

없음. 28건 모두 추천값으로 정했다. 사용자 확인 권장: **#11 · #12 · #17**(정책), 그리고 기본값 성격인 **#1**(시간 기본 `오전`)·**#20**(제목 구분자)는 취향이라 원하면 바꿔도 된다.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|---|---|---|---|---|---|
| CEO Review | `/plan-ceo-review` | 범위·전략 | 1 | CLEAR(PLAN) | 결정 반영 완료(`plant8-erp-phase04.1-ceo-review-260924.md`) |
| Eng Review | `/plan-eng-review` | 아키텍처·테스트 | 2 | CLEAR | 2회차 반영 완료. Fable 최종 전체 검토 대신 2회차로 마감 |
| Design Review | `/plan-design-review` | UI·UX 갭 | 1 | issues_open | 점수 6 → 8, 결정 28(막음 5 · 경고 13 · 참고 10) |
| Outside Review | `/codex` | 독립 2차 의견 | 1 | Codex unavailable(한도) → Opus 독립 교차 in-host | 교차 23건, 1차와 병합 |

OUTSIDE COVERAGE: Design = Opus 독립 교차(Codex 대체, 한도 9/29까지) — 한도가 풀리면 Codex 재확인 필요.
VERDICT: CEO + ENG CLEARED, DESIGN 지적 반영 필요 — 반영 회차 뒤 최종 전체 검토(Fable 1회)

NO UNRESOLVED DECISIONS
