# Phase 04.5 화면 항목 관리 — /plan-eng-review

- 대상: `.planning/phases/04.5-custom-field-admin/04.5-01~09-PLAN.md` (브랜치 `claude/plan-phase-04-5-7jtm0f`, 커밋 fc4fe6b)
- 날짜: 2026-09-25 (KST)
- 방식: 1차 검토 3개(Sonnet, 영역 A 아키텍처·흐름 / B 데이터·성능 / C 엣지·테스트) + Codex 외부 검토 동시 실행, 판단은 Opus. 사용자 부재 → 모든 결정은 권장안 자동 선택(표시: "사용자 부재, 권장").
- 선행: Codex 전체 최종 검토 4회(final 1~4) 수렴 — final-4 「판정: 막는 문제 없음」 (`docs/reviews/phase-04.5/codex-plan-review-final-4.md`).

## Step 0 — 범위 점검
- 9개 플랜, 거래처(vendor) 한 엔티티만. 새 의존성 없음(@testing-library/react 는 승인 대기, 이 게이트에서 추가 요구 없음).
- 실행 조건: PR #40(Phase 4 묶음 ②) 머지 후. 04-32 선행.
- 범위 축소 여지: 없음. 9개 플랜 모두 D10-13 가시성 행렬·관리 화면·잠금 경쟁 방지에 직접 연결.

## 1. 아키텍처
| # | 발견 | 심각도 | 처리 |
|---|------|--------|------|
| A-1 | `lockCustomFieldGrants`의 `tx.select().from(sql\`pg_advisory_xact_lock(...)\`)` 관용구가 미검증 | P1 | **해소** — 로컬 probe로 `select 1 from pg_advisory_xact_lock($1)` 생성·잠금 획득 확인 |
| A-2 | 09가 main을 다시 머지하지 않음 | P2 | 수용 — 07의 최종 머지 단계가 담당 |
| A-3 | 키 생성 ×3 재시도에 주석 없음 | P3 | 실행 시 한 줄 주석(플랜 수정 불필요) |

## 2. 코드 품질
| # | 발견 | 심각도 | 처리 |
|---|------|--------|------|
| B-3 | `isTargetEntity` 공용 헬퍼 제안 | P2 | 기각 — 한 엔티티뿐, 단순함 우선(CLAUDE.md §3.2) |

## 3. 테스트
| # | 발견 | 심각도 | 처리 |
|---|------|--------|------|
| B-2 | `createRole`이 권한 행 삽입 실패를 삼켜, 일시 오류 시 역할이 가시성 행 없이 남음(=전부 숨김) | P2 | **T1 수용** — 03에 실패 경로 테스트 추가 |
| C-2 | 필수 선택형 필드의 옵션이 모두 보관되면 입력 불가 | P2 | 해소 — 02의 "옵션 0개 거부"가 막음 |
| C-3 | 기타 사소한 경계값 | P3 | 실행 시 판단 |

커버리지(요약): 잠금 경쟁(01/03 동시성 테스트, pg_locks 폴링) · 숨김 키 보존(05 knownKeys) · 누출 스캔(03 present→absent) · 역할 전용 필드 E2E(06) · 편집자 흐름 E2E(07) · 복원 링크 권한(08 write+admin.archive, write-only 테스트). 빈 곳: B-2 한 곳 → T1.

## 4. 성능
| # | 발견 | 심각도 | 처리 |
|---|------|--------|------|
| B-1 | 잠금 안에서 필드마다 권한 행 1개씩 삽입 | P2 | 유지 — 필드 수가 적음(수십 개). 풀 크기 5(lib/env.ts:54) 동시 테스트로 교착 없음 확인 계획 |
| C-1 | tx 밖 정의 읽기 → tx 사이 TOCTOU | P2 | **T2 수용** — 05 위협 모델에 "허용 위험"으로 문서화(결과가 저장 거부로 끝나 무해) |

## Codex 외부 검토 (outside voice)
| # | 주장 | 처리 |
|---|------|------|
| 1 | Phase 4 선행 조건이 병렬성을 죽임 | 기각 — 로드맵·코디네이터 결정 사항 |
| 2 | 04-32 선행이면 4경로 동적 항목·Phase 10 통합 논의가 무의미 | 보류 — Phase 10 TODO로 남김 |
| 3 | 파일 수 제한이 인계를 유발 | 기각 — 프로세스 규칙 |
| 4 | E2E 격리를 우회로 처리 | 기각 — 이미 최소 수정 반영(06 insertRole, 선례 vendors.spec.ts:72) |
| 5 | main 이동마다 머지 의식 반복 | 기각 — 프로젝트 마이그레이션 규칙 |
| 6 | 사업 가치 미입증 | 사용자 판단 사항 — 보고에 한 줄로 전달 |

## 결정 기록 (사용자 부재, 권장)
- D1 A-1: 관용구 유지(probe 근거) — 권장
- D2 B-2: 테스트 추가(T1) — 권장
- D3 C-1: 문서화로 수용(T2) — 권장
- D4 B-1: 현행 유지 — 권장
- D5 B-3: 헬퍼 기각 — 권장
- D6 Codex #2: Phase 10 TODO — 권장

## 구현 과제 (다음 세션, /gsd-plan-phase 수정 + 체커 + Codex)
- T1 — 04.5-03: `createRole` 권한 행 삽입 실패 시 동작 테스트(오류를 삼키지 않거나, 삼킨다면 로그+재시도 경로를 검증)
- T2 — 04.5-05: 위협 모델에 "정의 읽기 TOCTOU = 허용 위험(저장 거부로 끝남)" 추가
- TODO — Phase 10: 4경로 동적 항목 통합 재검토(Codex #2)

## 범위 밖 (NOT in scope)
- 거래처 외 엔티티, 필드 순서 드래그, Phase 10 손익 양식 통합.

## 이미 있는 것
- repositories viewer 첫 인자 규칙(eslint/rules/repository-viewer-param.mjs), DbOrTx 선택 인자 패턴, 마이그레이션 가드 테스트.

## 실패 모드
- 잠금 미획득 → 동시 생성 시 가시성 행 누락: 01/03 경쟁 테스트가 잡음.
- 권한 행 삽입 일시 실패 → 역할에서 필드 전부 숨김: T1이 잡음.

## 병렬화
- 웨이브 구조 변경 없음. 01 → 02/03 → 05/06 → 07/08 → 09.

## 완료 요약
막는 문제 없음. 수용 2건(T1, T2)은 다음 세션에서 플랜에 반영, 이어서 /plan-design-review.

## GSTACK REVIEW REPORT
| 항목 | 결과 |
|------|------|
| Step 0 범위 | 유지 |
| 아키텍처 | P1 1건 해소(probe), P2 1 수용, P3 1 |
| 코드 품질 | P2 1 기각 |
| 테스트 | P2 1 수용(T1), P2 1 해소 |
| 성능 | P2 1 유지, P2 1 수용(T2) |
| Codex 외부 | 6건: 기각 4, 보류 1, 사용자 판단 1 |
| VERDICT | 통과(issues_open: T1·T2 반영 대기) |

NO UNRESOLVED DECISIONS
