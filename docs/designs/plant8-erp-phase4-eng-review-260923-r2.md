# Phase 4 엔지니어링 리뷰 r2 (plan-eng-review) — 2026-09-23

- 대상: `.planning/phases/04-project-quote-ledger/` 미실행 플랜 34개(04-06~04-47, 04-03 철회) · 브랜치 `claude/gsd-progress-e1nzgu` · HEAD `b1e9645`
- 모드: FULL_REVIEW · 자동 결정(사용자 부재, 「이전 세션 결정 적용」 지시)
- 이전 결정 존중: 엔지 리뷰 1차 R0~R11(R11 = Phase 4 한 PR), CEO 1차(HOLD SCOPE), CEO r2 반영 지시 8개(수용), UI-SPEC rev5 + R1~R4, U-2(UI-SPEC:529 사용자 확정), C2-02(직렬 웨이브 유지), 인트라넷 이전 없음(04-03 철회)
- Outside Voice: `codex exec`(read-only) 실행 성공 — 결과는 각 항목에 반영, 원문 요지는 부록

## 요약

플랜 구조는 1차 리뷰 뒤 대부분 정리됐다. 의존 그래프는 사실상 직렬(29웨이브)이고, 같은 웨이브에서 파일이 겹치는 곳은 이미 실행된 04-05와 04-32가 함께 쓰는 `docs/ARCHITECTURE.md` 하나뿐이다. 04-11의 조건부 UPDATE(`FOR UPDATE SKIP LOCKED` 하위 선택 + 바깥 `AND status = $from` + `RETURNING` 행만 로그)는 멱등성 요구를 이미 충족한다. 남은 일은 네 가지다. (1) 04-17·04-30 분할과 그에 따른 웨이브 이동, (2) CEO r2 지시 5의 대상 오기(최종 게이트는 04-47이 아니라 04-31, 계약 컬럼 삭제는 0014가 아니라 **0015**), (3) 트래픽 롤백이 스키마 호환성을 보지 않는 문제(치명 공백 1), (4) E2E 플레이크 판정 기준을 숫자로 정하는 것. 범위를 바꾸는 항목은 없다.

## 발견 (E2-xx)

| ID | 심각도 | 영역 | 발견 | 자동 결정 |
|---|---|---|---|---|
| E2-01 | 높음 | 플랜 구조 | 04-17(95KB·3태스크, 태스크 75/41/63행)이 체커 상한을 넘는다. Task 1 트레이서 하나에 파일 13개 이상이 걸려 있다 | 2분할. 아래 「분할안」 |
| E2-02 | 높음 | 플랜 구조 | 04-30(77KB·3태스크)도 상한을 넘는다. Task 3(저장 중 잠금·폭별 보기 전용·복원 줄 + 화면 검증)은 따로 떼도 된다 | 2분할. 04-26 의존 이동, 웨이브 15 이후 +1 |
| E2-03 | 높음 | 문서 정합 | CEO r2 지시 5가 「04-47(최종 게이트)」「0014 가드」라고 적었다. 실제 마지막 플랜은 04-31(웨이브 29, Task 3 = 페이즈 최종 화면 검증)이고, 가드가 있는 계약 컬럼 삭제는 04-41의 `0015_drop_project_contract_columns`다. 0014는 04-13의 `quote_line_kind`다 | 지시 5를 04-31·0015로 바로잡아 병합 |
| E2-04 | 치명(공백) | 배포·롤백 | `scripts/rollback.sh`는 서빙 중인 SHA와 **다른** 이전 배포 SHA를 고를 뿐 스키마 호환성을 보지 않는다. 0012(상태 다섯 값 재매핑)나 0015(컬럼 삭제) 적용 뒤 Phase 3 리비전으로 트래픽을 되돌리면, 옛 코드가 다섯 값 DB 위에서 돌거나 없는 컬럼을 읽는다. 테스트·처리·알림이 모두 없고 조용히 틀린다 | 04-31 Task 2(배포 main 한정)에 「스키마 하한 SHA」 가드를 넣는다. 하한보다 오래된 후보를 거부하고, 복구는 전진 수정 + 역 SQL(쓰기 중지 상태에서 리허설한 절차)로 한다 |
| E2-05 | 중간 | 마이그레이션 | 0015 RAISE 가드가 업무 행(`source <> 'demo'`)의 `contract_amount_krw <> 0`만 본다. 원화 환산액은 0인데 `contract_foreign_amount`만 채워진 행은 그대로 삭제된다 | 04-41 가드 조건에 `OR contract_foreign_amount IS NOT NULL`을 더하고, 업그레이드 테스트에 해당 픽스처를 추가한다 |
| E2-06 | 중간 | 테스트 | `playwright.config.ts`가 `retries: 0`인데, 플레이크를 무엇으로 판정할지 수치가 없다. 초록 한 번은 약한 증거다 | 04-31 최종 게이트에서 `CI=true` 전체 E2E를 새 DB로 **연속 3회** 돌려 전부 초록이면 통과. 첫 실패 trace는 보존한다. 원인이 설명되지 않는 실패는 `systematic-debugging`으로 조사한 뒤에만 통과시킨다. `retries`는 0으로 둔다 |
| E2-07 | 중간 | 동시성 | 04-11 읽기 경로 자동 정산에서 쓰기 요청이 stale 거부로 롤백되면 그 자동 정산도 함께 롤백된다. SKIP LOCKED로 건너뛴 행은 그 요청에서 정산되지 않는다 | 결과적 일관성으로 수용한다(다음 읽기가 따라잡는다). 04-11 통합 테스트에 「쓰기 거부 뒤 다음 목록 읽기에서 정산됨」 한 줄을 더한다. 「로그 한 줄」 단언은 **전환 1회당**으로 표현한다 |
| E2-08 | 낮음 | 동시성 | 04-17에서 목록과 합계를 `Promise.all`로 따로 읽으므로, 사이에 다른 요청의 정산이 커밋되면 기준(견적/발행)이 순간적으로 어긋날 수 있다 | 수용한다(30명 규모이고 새로 고침하면 맞는다). 04-17 threat 표에 accept로 적는다. 같은 스냅숏 트랜잭션은 도입하지 않는다(04-32 풀 규약과 병렬 읽기 이득이 충돌) |
| E2-09 | 낮음 | 성능 | 04-11 자동 정산이 대상 전체를 한 트랜잭션에서 UPDATE하고 로그를 남긴다. 배치 상한이 없다 | 수용한다. 대상은 종료일이 지난 진행 건뿐이라 규모상 수십 행이다. 배치 상한은 넣지 않는다(Simplicity). `project.auto_settle` count 로그로 관측한다 |
| E2-10 | 정보 | 의존 그래프 | 같은 웨이브 파일 겹침은 w3 04-05·04-32의 `docs/ARCHITECTURE.md`뿐이고 04-05는 실행을 마쳤다. w24 04-17·04-19, w7 04-09·04-27, w5 04-25·28·29는 겹치지 않는다. 마이그레이션 번호(0011 04-10 w4 → 0012 04-06 w6 → 0013 04-27 w7 → 0014 04-13 w16 → 0015 04-41 w23 → 0016 04-07 w27)가 웨이브 순서와 맞는다. 04-40이 잠금 안에서 읽을 때 `tx`를 쓰는 규칙이 04-32 풀 규약을 따른다 | 조치 없음. 분할 뒤 겹침 스크립트를 한 번 더 돌린다 |

### 분할안 (같은 PR 유지 — R11)

- **04-17 → 04-17 + 04-48(신규)**
  - 04-17(웨이브 24 유지): Task 1 트레이서(올해 기본 보기·겹침·표 위 합계 줄·bigint 경계·`profitBasisFor`) + Task 2(50건 페이지·OFFSET·범위 밖 보정·요청 순서). 04-17 truths 가운데 필터 UI·빈 목록·폰·로딩에 해당하는 것은 04-48로 옮긴다.
  - 04-48: 기존 Task 3(기간 필터·연도 자동 전환·파라미터 정규화·필터 지우기·빈 목록 세 갈래·폰 첫 화면·loading + 화면 검증). `depends_on: ["04-17"]`, 웨이브 25. 04-47과 같은 웨이브지만 파일이 겹치지 않는다(04-48은 `app/(app)/projects/*`와 `list-view`, 04-47은 `ui/table/*`와 `quote-table`).
  - 04-18: `depends_on`을 `["04-17","04-47"]`에서 `["04-48","04-47"]`로 바꾼다(`projects-table.tsx`·`list-view.ts`가 겹친다). 웨이브 26은 그대로라 뒤 웨이브에 영향이 없다.
  - 파라미터 정규화가 페이지 쿼리 계약에 필요하면 04-17 Task 2로 당긴다(Codex 지적).
- **04-30 → 04-30 + 04-49(신규)**
  - 04-30(웨이브 14): Task 1 트레이서(DTO → 셀 권한 → 저장) + Task 2(구조 컨트롤·새 줄·삭제·취소 모달·순서 전송). 편집기 커밋과 저장 페이로드 수집은 한 플랜에 둔다. 「활성 셀의 마지막 값이 전송 페이로드에 든다」는 단언을 추가한다.
  - 04-49: 기존 Task 3(저장 중 잠금 `save-lock`·1024 미만 보기 전용·좁은 PC 열 접기·복원 줄 overflow + 화면 검증 1280/1024/375). `depends_on: ["04-30"]`, 웨이브 15.
  - 04-26: `depends_on`을 `["04-49"]`로 바꾼다(`quote-table.tsx`가 겹친다). 웨이브 15 이후 모든 플랜의 웨이브를 +1 한다(04-26 w16 … 04-31 w30).
- Codex는 4분할을 제안했다. 자동 결정은 2분할이다. 태스크 경계를 그대로 쓰므로 재작성 위험이 가장 작고, 체커 상한만 넘기면 된다. 분할 뒤에도 체커가 상한 초과로 판정하면 그때 04-17 Task 1을 합계 계약과 합계 줄 렌더로 한 번 더 나눈다.

## 테스트 도식 (계획된 테스트 기준, 변경분)

```
[목록 GET /projects]
  applyAutoSettlement ── unit(auto-transition) ✔ ── integ(KST 경계·멱등·SKIP LOCKED·actor=system) ✔
        │                                         └─ integ(쓰기 거부 뒤 다음 읽기에서 정산) ✚ E2-07
  loadProjectList ── integ(합계 bigint·귀속·profitBasisFor 5상태×발행) ✔ (04-17)
        │         └─ integ(페이지·범위 밖 보정) ✔ (04-17)
  filter-bar/빈 목록/폰 ── e2e projects-list ✔ (04-48로 이동)
[견적 표 편집]
  셀 권한·저장 ── e2e quote-edit-scope ✔ (04-30) + 활성 셀 마지막 값 페이로드 단언 ✚
  save-lock·폭 게이트 ── unit save-lock ✔ + 화면 검증 3폭 ✔ (04-49로 이동)
[마이그레이션]
  0012 재매핑·RAISE ── integ migration-upgrade ✔ (04-06)
  0015 가드 ── integ migration-upgrade(c) ✔ + foreign_amount 픽스처 ✚ E2-05
[배포]
  rollback.sh 스키마 하한 거부 ── 스크립트 테스트 ✚ E2-04 (04-31)
[최종 게이트 04-31]
  CI=true 전체 E2E ×3 연속, 새 DB ✚ E2-06
```
✔ 계획에 있음 · ✚ 이번 리뷰로 추가

## 실패 모드

| 경로 | 실패 | 테스트 | 처리 | 드러남 | 판정 |
|---|---|---|---|---|---|
| 트래픽 롤백 → Phase 3 SHA | 옛 코드가 다섯 값 상태나 삭제된 컬럼 위에서 동작 | 없음 | 없음 | 조용함 | **치명 → E2-04로 해소** |
| 0015 외화 계약 값 손실 | 원화 0·외화 값 행 삭제 | 없음 | 없음 | 조용함 | E2-05로 해소 |
| 자동 정산 쓰기 롤백 | 정산 지연 | ✚ | 다음 읽기 | 로그 | 수용 |
| 목록·합계 스냅숏 차이 | 순간 불일치 | 없음 | 새로 고침 | 보임 | 수용(E2-08) |
| E2E 플레이크 | 거짓 초록/빨강 | ✚ 3회 | trace | 보임 | E2-06 |

## Decision ledger (자동 결정 + 근거)

| # | 결정 | 근거 |
|---|---|---|
| D2-1 | 04-17·04-30은 2분할하고 같은 PR에 둔다 | CEO r2 지시 1, R11. 태스크 경계를 그대로 쓰는 것이 가장 단순하다 |
| D2-2 | 04-49를 넣어 웨이브 15 이후를 +1 한다(04-48은 이동 없음) | 파일 겹침은 직렬로 둔다(C2-02) |
| D2-3 | 지시 5의 대상을 04-31·0015로 바로잡는다 | 플랜 frontmatter 사실 확인(04-31 w29, 04-41 0015) |
| D2-4 | rollback.sh에 스키마 하한 SHA 가드를 둔다 | 1차 엔지 A-28과 CEO B-09 롤백 자세의 공백을 메운다. 범위 변경이 아니라 배포 안전 보강이다 |
| D2-5 | 0015 가드에 foreign_amount 조건을 넣는다 | 사용자 D9 「값 있으면 RAISE」의 문자 그대로의 의미 |
| D2-6 | retries 0 유지, 3회 연속 판정 | CLAUDE.md 「CI=true로 판정」, CEO r2 지시 5 |
| D2-7 | 자동 정산은 결과적 일관성, 배치 상한과 스냅숏 트랜잭션은 넣지 않는다 | Simplicity, 30명 규모, 04-32 풀 규약 |
| D2-8 | 04-11 조건부 UPDATE는 이미 충족 → CEO 지시 4는 「목록 일괄 1회·KST·actor 테스트 확인 + E2-07 테스트 추가」로 좁힌다 | 04-11:37, :187, :390 |

## /gsd-plan-phase 4 반영 지시 (CEO r2 8개 + 엔지 r2 병합, 중복 제거)

1. **04-17**: Task 3을 신규 **04-48**(w25, deps 04-17)로 분리한다. 04-17 threat 표에 E2-08 accept를 추가한다. (CEO 1 · E2-01)
2. **04-18**: deps를 `["04-48","04-47"]`로 바꾼다. (E2-01)
3. **04-30**: Task 3을 신규 **04-49**(w15, deps 04-30)로 분리한다. 04-30에 「활성 셀 마지막 값 → 페이로드」 단언을 추가한다. (CEO 1 · E2-02)
4. **04-26 및 w15 이후 전 플랜**: 04-26 deps를 `["04-49"]`로 바꾸고 웨이브를 +1 한다. 분할 뒤 같은 웨이브 파일 겹침을 다시 검사한다. (E2-02 · E2-10)
5. **04-14(:362)·04-16(:360)**: 04-03 의존과 언급을 지운다. (CEO 2)
6. **T-04-SC 행이 있는 모든 플랜**: 「04-03이 재개되어…」 괄호 문구를 지운다. (CEO 3)
7. **04-11**: 목록 일괄 1회·KST 00:00 경계·actor=system 테스트가 있는지 확인한다(조건부 UPDATE·멱등은 이미 충족). 「쓰기 거부 뒤 다음 읽기 정산」 통합 테스트를 더하고, 로그 단언을 「전환 1회당 한 줄」로 표현한다. (CEO 4 · E2-07 · E2-09)
8. **04-31(실제 최종 게이트)**: E2E 플레이크 조사 태스크를 둔다(`systematic-debugging`). `CI=true` 전체 E2E를 새 DB로 연속 3회 돌리고 trace를 보존한다. 마이그레이션 순서 0011→0016과 **0015** 가드를 검증한다. (CEO 5 정정 · E2-03 · E2-06)
9. **04-31 Task 2**: `scripts/rollback.sh`에 스키마 하한 SHA 가드를 넣고 스크립트 테스트를 더한다. DECISIONS에 「Phase 4 머지 이전 SHA로 트래픽 롤백 금지, 복구는 전진 수정 또는 쓰기 중지 후 역 SQL」을 적는다. (E2-04)
10. **04-41**: 0015 가드에 `OR contract_foreign_amount IS NOT NULL`(업무 행)을 넣고 업그레이드 테스트 픽스처를 추가한다. 역 SQL은 「복구 절차(쓰기 중지)」로 표기한다. (E2-05)
11. **S5 담당 플랜**: U-2 확정 규칙(UI-SPEC:529)이 렌더와 E2E 단언에 반영됐는지 확인하고, OPEN-ITEMS U-2를 해소로 표기한다. (CEO 6)
12. **UI 플랜 전반(04-48·04-49 포함)**: UI-SPEC은 S번호와 「적용 규칙」 절(1006~)을 정본으로 인용한다. (CEO 7)
13. **RESEARCH/ROADMAP/OPEN-ITEMS**: Q1·Q3은 해소, Q2는 무의미로 표기한다. ROADMAP 분모는 신규 2개를 반영해 미실행 36 / 전체 40으로 통일한다. OPEN-ITEMS 첫 줄을 갱신한다. (CEO 8 · 분할 반영)

## 사용자 확인 필요(아침)

- 없음. 범위를 바꾸는 발견이 없다. U-2(UI-SPEC:529), C2-02(직렬 유지), R11(한 PR)은 이미 기록된 결정으로 덮인다.

## 이미 있는 것 (재사용)

- `lockProjectForWrite`(04-20), `tx-safety.test.ts` 경합 도우미(04-32), `createScratchDb` 업그레이드 테스트(04-06), `rollback.sh`의 APP_GIT_SHA 기반 배포 단위 선택. E2-04 가드는 이 스크립트를 확장한다.

## NOT in scope

- 인트라넷 데이터 이전(MIG-01~03), 04-03
- 자동 정산 Cloud Scheduler(Phase 7)
- PR 분할(R11)
- 자동 정산 배치 상한, 목록·합계 단일 스냅숏(E2-08·E2-09 수용)
- Playwright retries 도입

## 부록 — Outside Voice (codex exec, read-only) 요지

- 04-11 SQL에 중복 로그 구멍은 없다. 「로그 한 줄」은 전환 1회당으로 한정해야 한다 → E2-07 반영
- 목록·합계 스냅숏 불일치, 자동 정산 무상한 → E2-08·E2-09 수용
- 04-17·04-30 4분할 제안 → 2분할로 자동 결정(D2-1). 편집기 커밋과 페이로드를 함께 두라는 지적은 반영
- 0012 롤백 보호는 가정에 기댄다. rollback.sh가 스키마 호환을 보지 않는다 → E2-04
- 0015 가드가 한 컬럼만 본다 → E2-05
- retries 0 유지 + 연속 다회 CI 판정 → E2-06

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|---|---|---|---|---|---|
| plan-eng-review r2 | CEO r2 뒤 재검토 | 분할 경계·멱등·마이그레이션·플레이크·의존 그래프 | 2 | issues_open | 10건(치명 1 · 높음 3 · 중간 3 · 낮음 2 · 정보 1), 모두 자동 결정, 반영 지시 13개 |
| plan-ceo-review r2 | 이전 실행 | HOLD SCOPE | 2 | issues_open | 지시 8개를 위 목록에 병합(지시 5 대상 정정) |
| Outside Voice (codex) | 기본 | 독립 도전 | 1 | done | 9개 요지, 7개 반영·2개 수용 |

**VERDICT:** 조건부 통과. 반영 지시 1~13을 /gsd-plan-phase 4에 적용한 뒤 체커로 재검증한다. 치명 공백 1건(E2-04)은 지시 9로 해소된다.

NO UNRESOLVED DECISIONS
