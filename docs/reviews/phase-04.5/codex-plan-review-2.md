아래 플랜 파일은 모두 `.planning/phases/04.5-custom-field-admin/` 기준입니다. 파일은 수정하지 않았습니다.

| # | 라운드 1 지적 | 상태 | 근거 |
|---|---|---|---|
| 1 | 동시 저장의 숨은 값 덮어쓰기 | RESOLVED | `04.5-05-PLAN.md` Task 2④: 행 잠금 조회·병합·UPDATE를 같은 트랜잭션으로 묶고 두 연결 경합 테스트 명시. |
| 2 | 기존 정의·테스트의 노출 행 누락 | PARTIAL | `04.5-01-PLAN.md` Task 2②에 백필, `04.5-05-PLAN.md` Task 2⑤에 픽스처 보완. 다만 07 재생성 절차와 불일치(D1). |
| 3 | 병합 후 생성 원자성 상실 | RESOLVED | `04.5-07-PLAN.md` Task 3②: 선택 `tx`와 실제 쿼리 사용 보존, 부분 INSERT 실패 롤백 재검증. |
| 4 | DOM 감사 DB 준비 순서 | RESOLVED | `04.5-07-PLAN.md` Task 2②: 테스트 DB 지정 → 마이그레이션 → 기본 시드 → 감사 픽스처. Task 3③(h)에 기존 행 적용 검증 별도 추가. |
| 5 | 같은 wave의 공유 환경 충돌 | RESOLVED | 02~05의 실행 규율·`verify`: 통합/E2E/빌드를 공통 `flock`으로 직렬화. |
| 6 | 관리 DTO 누수 검사 제외 | RESOLVED | `04.5-09-PLAN.md` Task 1④: 검토 필드 목록·실제 DTO 키·계급별 메뉴 게이트 검사 경로 추가. |
| 7 | 삭제 줄 0 게이트의 모순 | RESOLVED | `04.5-07-PLAN.md` Task 3⑤: 저널 구조 검사와 파일별 승인 변경 목록으로 교체. |
| 8 | 픽스처 `roleId` 반환 누락 | RESOLVED | `04.5-06-PLAN.md` Task 1④: `{ ...credentials, roleId, roleName }` 반환 계약 명시. |
| 9 | 클라이언트 `label` 타입 누락 | RESOLVED | `04.5-01-PLAN.md` Task 1⑥: 로컬 `VendorFieldDefinition`에 `label: string` 추가. |
| 10 | 첫 플랜 과대 범위 | RESOLVED | 01은 생성 트레이서·백필, 08은 목록·폼 완성, 09는 메뉴·누수 검사로 순차 분리. |

- **[MAJOR] D1. 재생성 절차가 새 노출 백필을 누락한다** — **근거:** `04.5-01-PLAN.md` Task 2②는 노출 행 INSERT도 재생성 후 복원하도록 요구하지만, `04.5-07-PLAN.md` Task 3③(a)·(d)의 보존·재적용 목록에는 `label` 채움만 있다. 그대로 실행하면 08 Task 1의 SQL 가드가 실패한다. 누락 상태로 적용하면 노출 행 없는 기존 칸은 숨겨진다(`domain/permissions/visible.ts:22`). — **수정:** 재적용 목록에 노출 백필 SQL·표시 주석을 넣고, ③(h)에서 전 계급 행 생성과 기존 `false` 보존까지 확인한다. 라운드 1 #2의 잔여 문제다.

- **[MAJOR] D2. Phase 4 선행 확인은 작업 브랜치 반영을 보장하지 않는다** — **근거:** `04.5-01-PLAN.md`·`04.5-09-PLAN.md`의 「실행 순서 — Phase 4 선행」는 fetch 후 `origin/main`의 SUMMARY 존재만 검사한다. 실제 `git merge origin/main`은 `04.5-07-PLAN.md` Task 3①까지 미뤄져 있다. 따라서 원격 확인은 통과하면서 01~09 구현·검증은 이전 코드 위에서 진행할 수 있다. — **수정:** 01 착수 전에 Phase 4가 반영된 main을 작업 브랜치에 병합하고, 해당 병합 커밋이 HEAD의 조상인지 검사한다.

- **[MAJOR] D3. Plan 09가 회수한 관리 권한을 배포 시 복구한다** — **근거:** `04.5-09-PLAN.md` Task 1①·⑤ 및 「권한 시드 수단」은 기존 `upsertPermission` 루프에 의존한다. 실제 루프는 `allowed: true`를 쓴다(`domain/seed/index.ts:133`). 승인된 `04.5-UI-SPEC.md` 화면 1(:229)의 “이미 있는 행은 덮지 않는다”와 동작이 다르다. OPEN 기록만으로 이 계약이 충족되지는 않는다. — **수정:** 새 메뉴를 덮어쓰기 루프에서 제외하고 view·write를 insert-if-absent로 부여하도록 계획의 시드 수정 금지를 좁혀 조정한다. 권한을 끈 뒤 재시드해도 유지되는 테스트를 추가한다.

- **[NOTE] 승인본 밖 오류 문구는 최소화 필요** — **근거:** `04.5-02-PLAN.md` OPEN 2-B의 「화면 항목 없음」, `04.5-05-PLAN.md` OPEN 5-A·5-B의 타입·위조 키 문구는 승인본 밖 보완이다. 추가 사용자 선택은 발견하지 못했다. — **수정:** 기존 오류 자리에서만 표시하고 별도 도움말·확인 단계를 추가하지 않는다. 거래처 한정과 새 의존성 금지는 유지한다.

판정: 막는 문제 있음 (3건)
