codex
계획 파일명은 `.planning/phases/04.5-custom-field-admin/` 기준입니다. 파일은 수정하지 않았습니다.

- **[MAJOR] 칸·계급 동시 생성 시 노출 행이 영구 누락됨** — `04.5-01-PLAN.md` Task 1④는 트랜잭션 전에 계급을 읽고, `04.5-03-PLAN.md` Task 1⑤는 계급 INSERT 후 정의를 읽는다. 새 계급 INSERT가 앞선 계급 조회보다 늦고, 정의 조회가 칸 커밋보다 빠르면 양쪽 모두 상대를 놓친다. 실제 조회·삽입도 별도 DB 호출이다(`repositories/roles.ts:10`, `:34`, `repositories/field-definitions.ts:9`). 시드는 누락을 복구하지 않는다 — **두 생성 경로가 같은 잠금 규칙 아래 조회·삽입·노출 행 생성을 수행하도록 하고, 이 교차 순서를 재현하는 경합 테스트를 추가한다.**

- **[MAJOR] 선택형 수정의 서버 최소 선택지 검증이 빠짐** — `04.5-02-PLAN.md` Task 2②·④는 `options?`와 원소 규칙만 명시하고, Task 3②는 제출 배열을 그대로 보관 파생에 사용한다. 생성과 달리 수정에서 `[]`·생략을 어떻게 거부하는지 없다. 빈 배열을 저장하면 기존 검증기는 빈 `z.enum`을 만든다(`domain/custom-fields/build-schema.ts:29`). 클라이언트 버튼 비활성으로는 위조 요청을 막지 못한다 — **조회한 타입이 선택형이면 활성 선택지 1~30개를 domain에서 검증하고, 빈 배열·생략 요청의 행·버전·로그 불변 테스트를 추가한다.**

- **[MAJOR] 수정 성공 직후 폼 재마운트가 성공 상태를 지움** — `04.5-02-PLAN.md` Task 2③·⑤·⑥은 저장 시 `version + 1`, 페이지 재검증, 폼 `key={id}:{version}`을 동시에 요구한다. 재검증으로 새 버전이 내려오면 성공 결과 줄을 가진 폼 자체가 교체된다. 따르는 기존 액션도 저장 후 즉시 재검증한다(`app/(app)/admin/vendors/actions.ts:28`). 이는 같은 태스크의 결과 줄·읽기 전용 유지 계약과 충돌한다 — **성공 상태를 유지하는 바깥 컴포넌트를 두고, 명시적 「새로 불러오기」에서만 편집 상태를 초기화한다. 재검증 완료 후에도 결과 줄이 유지되는 E2E를 명시한다.**

- **[MAJOR] 전용 계급 E2E 격리에 공개되는 시간 틈이 있음** — `04.5-06-PLAN.md` Task 1④는 필수 칸을 전 계급 보임으로 생성·커밋한 다음 다른 계급에서 끈다. 그 사이 다른 워커의 거래처 등록이 필수 오류로 실패할 수 있다. 워커는 같은 DB를 공유하며(`playwright.config.ts:78`), 기존 등록 테스트는 커스텀 필수값을 보내지 않는다(`test/e2e/vendors.spec.ts:36`). 실행 전체의 `flock`은 내부 워커를 직렬화하지 않는다 — **테스트 정의와 제한된 노출 행을 한 트랜잭션으로 준비하거나 해당 검증을 워커 하나로 격리한다.**

- **[MINOR] 오래 열린 거래처 폼의 복구 행동이 없음** — `04.5-05-PLAN.md` Task 1은 입력 집합 밖 키를 무조건 거부하지만, UI-SPEC E4 stale은 폼을 연 뒤 보관·노출 해제된 칸의 저장값 보존을 요구한다. 기존 폼은 열 때 받은 정의의 키를 계속 제출한다(`app/(app)/admin/vendors/vendor-form.tsx:98`). 따라서 정상 사용자가 일반 오류만 반복해서 받는다 — **위조 키 거부는 유지하되, 오래된 폼에서 재조회하는 복구 경로와 입력 보존 E2E를 명시한다.**

- **[MINOR] 플랜 간 생산·소비 심볼이 불일치함** — `04.5-02-PLAN.md` Task 2는 `findFieldDefinitionByLabel`을 소비하지만, `04.5-08-PLAN.md` 「이름 예약 조회」는 그 함수를 만들지 않기로 했다. 또한 06 Task 1④는 생성 결과에 `version`을 요구하지만 01 Task 1④의 반환은 `{ id, key }`뿐이다 — **이름 조회는 08의 목록 조회 계약으로 통일하고, E2E 픽스처의 version 재조회 단계를 명시한다.**

- **[NOTE] Phase 4 브랜치 대조는 완료하지 못함** — 지정한 `origin/claude/gsd-progress-e1nzgu`가 로컬 원격 추적 목록에 없고 `git show`가 `invalid object name`으로 실패했다. 따라서 03의 04-32 호환성 및 07의 병합 전제를 해당 브랜치 코드로 확인하지 못했다 — **해당 ref를 제공한 환경에서 공유 파일과 호출 계약을 다시 대조해야 한다.**

판정: 막는 문제 있음 (4건)
tokens used
174,426
계획 파일명은 `.planning/phases/04.5-custom-field-admin/` 기준입니다. 파일은 수정하지 않았습니다.

- **[MAJOR] 칸·계급 동시 생성 시 노출 행이 영구 누락됨** — `04.5-01-PLAN.md` Task 1④는 트랜잭션 전에 계급을 읽고, `04.5-03-PLAN.md` Task 1⑤는 계급 INSERT 후 정의를 읽는다. 새 계급 INSERT가 앞선 계급 조회보다 늦고, 정의 조회가 칸 커밋보다 빠르면 양쪽 모두 상대를 놓친다. 실제 조회·삽입도 별도 DB 호출이다(`repositories/roles.ts:10`, `:34`, `repositories/field-definitions.ts:9`). 시드는 누락을 복구하지 않는다 — **두 생성 경로가 같은 잠금 규칙 아래 조회·삽입·노출 행 생성을 수행하도록 하고, 이 교차 순서를 재현하는 경합 테스트를 추가한다.**

- **[MAJOR] 선택형 수정의 서버 최소 선택지 검증이 빠짐** — `04.5-02-PLAN.md` Task 2②·④는 `options?`와 원소 규칙만 명시하고, Task 3②는 제출 배열을 그대로 보관 파생에 사용한다. 생성과 달리 수정에서 `[]`·생략을 어떻게 거부하는지 없다. 빈 배열을 저장하면 기존 검증기는 빈 `z.enum`을 만든다(`domain/custom-fields/build-schema.ts:29`). 클라이언트 버튼 비활성으로는 위조 요청을 막지 못한다 — **조회한 타입이 선택형이면 활성 선택지 1~30개를 domain에서 검증하고, 빈 배열·생략 요청의 행·버전·로그 불변 테스트를 추가한다.**

- **[MAJOR] 수정 성공 직후 폼 재마운트가 성공 상태를 지움** — `04.5-02-PLAN.md` Task 2③·⑤·⑥은 저장 시 `version + 1`, 페이지 재검증, 폼 `key={id}:{version}`을 동시에 요구한다. 재검증으로 새 버전이 내려오면 성공 결과 줄을 가진 폼 자체가 교체된다. 따르는 기존 액션도 저장 후 즉시 재검증한다(`app/(app)/admin/vendors/actions.ts:28`). 이는 같은 태스크의 결과 줄·읽기 전용 유지 계약과 충돌한다 — **성공 상태를 유지하는 바깥 컴포넌트를 두고, 명시적 「새로 불러오기」에서만 편집 상태를 초기화한다. 재검증 완료 후에도 결과 줄이 유지되는 E2E를 명시한다.**

- **[MAJOR] 전용 계급 E2E 격리에 공개되는 시간 틈이 있음** — `04.5-06-PLAN.md` Task 1④는 필수 칸을 전 계급 보임으로 생성·커밋한 다음 다른 계급에서 끈다. 그 사이 다른 워커의 거래처 등록이 필수 오류로 실패할 수 있다. 워커는 같은 DB를 공유하며(`playwright.config.ts:78`), 기존 등록 테스트는 커스텀 필수값을 보내지 않는다(`test/e2e/vendors.spec.ts:36`). 실행 전체의 `flock`은 내부 워커를 직렬화하지 않는다 — **테스트 정의와 제한된 노출 행을 한 트랜잭션으로 준비하거나 해당 검증을 워커 하나로 격리한다.**

- **[MINOR] 오래 열린 거래처 폼의 복구 행동이 없음** — `04.5-05-PLAN.md` Task 1은 입력 집합 밖 키를 무조건 거부하지만, UI-SPEC E4 stale은 폼을 연 뒤 보관·노출 해제된 칸의 저장값 보존을 요구한다. 기존 폼은 열 때 받은 정의의 키를 계속 제출한다(`app/(app)/admin/vendors/vendor-form.tsx:98`). 따라서 정상 사용자가 일반 오류만 반복해서 받는다 — **위조 키 거부는 유지하되, 오래된 폼에서 재조회하는 복구 경로와 입력 보존 E2E를 명시한다.**

- **[MINOR] 플랜 간 생산·소비 심볼이 불일치함** — `04.5-02-PLAN.md` Task 2는 `findFieldDefinitionByLabel`을 소비하지만, `04.5-08-PLAN.md` 「이름 예약 조회」는 그 함수를 만들지 않기로 했다. 또한 06 Task 1④는 생성 결과에 `version`을 요구하지만 01 Task 1④의 반환은 `{ id, key }`뿐이다 — **이름 조회는 08의 목록 조회 계약으로 통일하고, E2E 픽스처의 version 재조회 단계를 명시한다.**

- **[NOTE] Phase 4 브랜치 대조는 완료하지 못함** — 지정한 `origin/claude/gsd-progress-e1nzgu`가 로컬 원격 추적 목록에 없고 `git show`가 `invalid object name`으로 실패했다. 따라서 03의 04-32 호환성 및 07의 병합 전제를 해당 브랜치 코드로 확인하지 못했다 — **해당 ref를 제공한 환경에서 공유 파일과 호출 계약을 다시 대조해야 한다.**

판정: 막는 문제 있음 (4건)
exit=0
