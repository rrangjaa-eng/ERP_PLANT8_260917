1. **[BLOCKER] UI Considerations E4 / 화면 3 — 기존 빈 필수값 문제를 해결하지 못했다.**  
   **근거:** `04.5-UI-SPEC.md:155`는 다른 칸만 수정하면 저장을 막지 않는다고 선언하지만, `:225`는 입력 렌더를 바꾸지 않는다. 실제 `app/(app)/admin/vendors/vendor-form.tsx:140`에는 `noValidate`가 없고, `:217`의 select와 `:231`의 text/number/date에 `required={def.required}`가 있다. 브라우저가 `handleSubmit` 실행 전에 제출을 막으므로 서버의 예외 판정까지 도달하지 못한다.  
   **수정:** 거래처 폼의 브라우저 검증 제거와 서버 검증·오류 연결을 변경 범위에 명시한다. `.planning/ROADMAP.md:424`의 “고칠 때”가 **행 수정 전체인지 해당 칸 수정인지**도 확정해야 한다. 현재 명세가 선택한 “무관한 수정 허용”을 유지한다면 신규 등록·기존 빈값 유지·기존 값 삭제·선택→필수 전환을 각각 정의하고, 네 타입 모두 무관한 수정 저장을 E2E로 검증한다.

2. **[BLOCKER] 화면 3 — 보관된 선택지를 표시해도 제출 과정에서 값이 사라진다.**  
   **근거:** `04.5-UI-SPEC.md:240`은 현재 보관값을 `disabled` 옵션으로 표시한다. 하지만 `vendor-form.tsx:95`는 `FormData`로 값을 수집하고, `:39`와 `:99`는 수집되지 않은 값을 빈 문자열로 바꾼다. 선택된 disabled 옵션은 제출 데이터에서 빠지므로 필수 칸은 `""`, 선택 칸은 키 누락으로 전달될 수 있다. 이는 `.planning/ROADMAP.md:424`의 기존 보관값 보존 보장을 충족하지 못한다.  
   **수정:** 변경하지 않은 보관값과 사용자가 명시적으로 비운 값을 구분하는 제출 계약을 정한다. 서버는 기존 값 유지와 활성 옵션으로의 변경만 허용하고, 보관값 신규 지정은 거부한다.

3. **[MAJOR] 화면 2 / Assumptions 6·7 — 선택지 ‘보관’의 저장 계약과 복원 설명이 성립하지 않는다.**  
   **근거:** `04.5-UI-SPEC.md:209`는 보관 선택지를 표시하지만 `:217`은 활성 배열만 제출한다. `:282`의 “필드 전체 보관·복원으로 선택지 복원을 우회”한다는 주장에도 근거가 없다. 현재 옵션은 `db/schema/field-definitions.ts:21`의 단일 JSONB이며, 연구 문서조차 `04.5-RESEARCH.md:281`에서 active/archived 분리를 요구한다.  
   **수정:** 저장된 옵션 보관과 미저장 옵션 제거를 분리한다. 보관 옵션을 유지하는 요청·응답 형태, 저장 전 취소, 같은 문자열 재추가 규칙을 명시한다. 전체 필드 복원이 옵션까지 복원한다는 문구는 삭제하고, 필요하면 개별 복원 동작을 정의한다.

4. **[MAJOR] Component Inventory / 화면 1 — 보관 버튼·확인 방식이 재사용 컴포넌트 및 SYSTEM과 다르다.**  
   **근거:** `04.5-UI-SPEC.md:47`은 `DeleteToArchive`를 그대로 사용하면서 `:192`는 버튼을 「보관」으로 지정한다. 실제 `app/(app)/admin/archive/delete-to-archive.tsx:29`와 `:51`은 「삭제」이며 라벨 변경 prop도 없다. 확인은 `:48`의 인라인 행인데, `docs/design/SYSTEM.md:670`은 확인 모달을 요구한다.  
   **수정:** 사용자에게 보이는 용어와 확인 표면을 하나로 확정한다. 기존 인라인 방식이 승인된 예외라면 해당 결정 근거를 연결하고 시스템을 정합화한다. 재사용한다는 이유만으로 SYSTEM 준수를 주장하지 않는다.

5. **[MAJOR] UI Considerations E3 / 화면 2 — 제출 중 상태와 확인 포커스가 불완전하다.**  
   **근거:** `04.5-UI-SPEC.md:151`은 확인 버튼 pending만 다룬다. 실제 `delete-to-archive.tsx:53`의 취소 버튼은 처리 중에도 활성이고, `:26`과 `:47` 사이 전환에 포커스 이동·복귀 처리가 없다. 새 폼의 선택지 추가·보관도 제출 중 잠금 계약이 없다. `SYSTEM.md:671`은 같은 폼의 다른 버튼 비활성 및 300ms 이후 진행 표시를 요구한다.  
   **수정:** 제출 중 변경 가능한 컨트롤을 잠그고 진행 표시를 지정한다. 확인 진입·취소·완료 및 옵션 제거 후 포커스 목적지, 반복 「보관」 버튼의 대상 포함 접근 이름을 명시한다.

6. **[MAJOR] 화면 2 SUCCESS — SYSTEM을 인용하면서 반대 동작을 지정한다.**  
   **근거:** `04.5-UI-SPEC.md:220`은 화면이 그대로인 폼의 성공 처리를 입력 초기화로 정의한다. `SYSTEM.md:1002`는 버튼 자리에 결과 텍스트를 표시하고 입력을 읽기 전용으로 바꾸도록 한다. 기존 `vendor-form.tsx:79`의 reset은 실제 선례지만 시스템 준수 근거는 아니다. 또한 새 폼의 타입·선택지는 React 상태(`UI-SPEC.md:216`)라 `form.reset()`만으로 초기화되지 않는다.  
   **수정:** 성공 표시·목록 갱신·포커스·추가 등록 진입을 확정한다. 연속 등록을 택한다면 시스템 예외를 정식 반영하고 React 상태까지 초기화하는 계약을 적는다.

7. **[MAJOR] Copywriting Contract / 오류 상태 — 오류 위치·접근성에 관한 기존 코드 주장이 틀렸다.**  
   **근거:** `04.5-UI-SPEC.md:147`은 폼 전체 오류를 `FormAlert`로 처리한다. 이는 `SYSTEM.md:992`의 `Form.Actions` 이유 자리 계약과 다르며, 실제 `ui/form-alert/FormAlert.tsx:15`는 `role="alert"`다. `UI-SPEC.md:154`의 거래처 칸별 오류가 “새 로직 없음”이라는 주장도 틀렸다. `vendor-form.tsx:136`은 이름 오류만 추출하고, `:189`와 `:231`은 커스텀 칸 오류를 전달하지 않는다.  
   **수정:** 커스텀 키별 오류 매핑, `aria-invalid`·`aria-describedby`, 값 보존을 구현 계약에 포함한다. 폼 전체 오류는 제출 영역에 연결한다. 「같은 이름… · 확인」(`UI-SPEC.md:118`)도 「이름 바꾸기」처럼 실제 다음 행동으로 바꾼다.

8. **[MAJOR] Spacing Scale / Color — 토큰은 존재하지만 사용 규칙이 틀렸다.**  
   **근거:** `04.5-UI-SPEC.md:61`~`:63`은 셀 패딩을 PC 8/12px, 폰 16px로 지정한다. `SYSTEM.md:190`과 `docs/design/tokens.css:112`, `:162`의 계약은 PC 6/8px, 폰 10/8px다. `UI-SPEC.md:91`의 표 머리글 `--g-100`은 같은 문서 `:141`의 읽기용 흰 머리글과 충돌한다. `:99`는 행동 링크에 accent가 해당 없다고 하지만 `SYSTEM.md:663`의 3차 버튼은 accent다.  
   **수정:** 셀 패딩은 `--cell-pad-y/x`로 통일하고, 읽기용 목록과 매트릭스 머리글 색을 분리한다. accent 사용 목록에 실제 3차 행동을 반영한다.

9. **[MAJOR] 화면 4 / E5 long-text — ‘기존 그대로’는 긴 열 제목의 줄바꿈을 제공하지 않는다.**  
   **근거:** `04.5-UI-SPEC.md:246`, `:255`는 기존 컴포넌트를 유지하면서 다단 줄바꿈을 보장한다. 실제 `ui/permission-grid/PermissionGrid.module.css:60`은 `white-space: nowrap`이다. `UI-SPEC.md:158`은 폭 상한을 미해결로 남긴다.  
   **수정:** 래퍼 props 유지와 스타일 변경을 구분한다. 열 폭·줄바꿈·공백 없는 긴 문자열 처리 및 폰의 계급 선택 목록에서의 표시를 확정하고, 긴 이름·다수 열로 검수한다.

10. **[MAJOR] 화면 1·2 — 폰의 순서 변경 계약이 불충분하다.**  
    **근거:** `04.5-UI-SPEC.md:193`은 P1에 이름·타입·행동을 배치하지만 `SYSTEM.md:733`의 문자+숫자+상태/행동 배정과 다르다. 순서는 `UI-SPEC.md:204`의 숫자 입력뿐이며 중복 순서 처리와 저장 후 위치 확인이 없다. 기존 정렬은 `repositories/field-definitions.ts:14`에서 순서가 같으면 자동 생성 키로 결정된다.  
    **수정:** P1 배정을 시스템과 맞추거나 예외를 정식 반영한다. 숫자 순서 편집을 유지한다면 중복·음수·동률 규칙, 키보드 조작과 저장 후 위치를 명시한다. 드래그는 필수가 아니지만 폰에서 여러 항목을 재배치할 수 있는 구체적 흐름은 필요하다.

11. **[MAJOR] UI Considerations / 화면 2·3 — 동시 편집과 정의 변경 중인 폼의 상태가 없다.**  
    **근거:** `04.5-UI-SPEC.md:217`은 이름·필수·순서·옵션을 한 번에 저장하지만 충돌 상태가 없다. `repositories/field-definitions.ts:55`와 `:65`도 읽은 버전 조건 없이 갱신한다. 두 관리자의 옵션 변경이 서로 덮이거나, 열린 거래처 폼에서 선택한 옵션이 저장 직전 보관될 수 있다.  
    **수정:** 정의 버전 충돌 시 저장 거부·입력 유지·최신 정의 확인 흐름을 정한다. 열린 거래처 폼에 대한 필드 보관·필수 전환·노출 권한 변경도 별도 오류 상태로 다룬다.

12. **[MAJOR] 화면 1 권한 게이트 — 버튼 숨김만 있고 직접 진입·저장 거부 계약이 빠졌다.**  
    **근거:** `04.5-UI-SPEC.md:189`은 view/write에 따른 버튼 표시만 규정한다. 실제 vendors는 `page.tsx:65`에서 보관 행 편집을 제외하고, `:80`에서 폼 전체를 쓰기 권한으로 감싼다. 보관 권한도 `:53`, `:160`의 `admin.archive.write`로 별도 판정하므로 “vendors와 동일”이라는 설명이 부정확하다.  
    **수정:** 권한 없는 `?new=1`·`?editId=`, 보관/없는 ID, 제출 중 권한 회수의 결과를 명시한다. `.planning/ROADMAP.md:423`의 관리자 전용 조건을 화면과 액션 모두에 적용하고 보관함 권한과 정합성을 맞춘다.

13. **[MAJOR] 화면 1 / 화면 4 — 거래처 한정·자동 노출의 화면과 저장 계약 연결이 약하다.**  
    **근거:** `04.5-UI-SPEC.md:190`의 대상 선택 UI 제거는 맞다. 그러나 `.planning/04.5` 범위의 서버 요청도 거래처만 받아야 한다는 연결 계약은 없다. 또한 `:246`은 열 추가만 설명하며 `.planning/ROADMAP.md:425`의 생성 즉시 전 계급 보임과 등록 실패 처리를 명시하지 않는다. 현재 `repositories/field-definitions.ts:26`은 entity를 일반 문자열로 받는다.  
    **수정:** 생성·수정 대상은 서버에서 거래처로 제한하고 프로젝트/견적 줄 ID·entity 요청을 거부하도록 적는다. 필드 생성과 노출표 초기화의 성공을 하나로 정의하고, 성공 후 「정보 노출표」로 이동할 행동을 제공한다. UI에 대상 선택이 없다는 사실만으로 서버 제한까지 충족했다고 볼 수 없다.

14. **[MINOR] 화면 2 — 타입 불변성과 키 자동 생성이 사용자 계약에서 사라졌다.**  
    **근거:** `04.5-UI-SPEC.md:215`는 타입을 못 바꾼다는 사실을 인터랙션 부재만으로 전달한다. `:201`의 이름 입력에는 키 자동 생성·이름 변경 후 키 유지 여부가 없다. `.planning/ROADMAP.md:423`은 키 자동 생성과 타입 변경 대신 새 칸 만들기를 요구한다.  
    **수정:** 타입 옆에 「변경 불가」와 「새 화면 항목 추가」 행동을 둔다. 키는 숨겨도 되지만 자동 생성·불변·충돌 처리 정책은 명세에 적는다. 기술 키를 사용자에게 입력시키거나 이름 변경으로 재생성해서는 안 된다.

15. **[MAJOR] UI Considerations — 빈 상태·많은 항목을 ‘covered’로 처리하기엔 결정이 부족하다.**  
    **근거:** `04.5-UI-SPEC.md:138`은 모든 0건에 「등록된 화면 항목이 없습니다」를 쓴다. 전부 보관되어 현재 필터에서만 0건인 경우와 다르다. `:141`은 5~15건만 가정하고, `:145`, `:149`, `:158`은 긴 이름·많은 선택지·열 제목을 육안 확인이나 미해결로 넘긴다.  
    **수정:** 전체 0건과 활성 0건을 구분하고 후자에는 「보관 포함」 행동을 제공한다. 이름·옵션 길이와 개수의 허용 범위, 빈 문자열·중복 옵션 오류, 다수 필드에서 탐색·제출 위치, 긴 목록의 펼침 방식을 확정한다. 검수 데이터와 통과 조건도 적는다.

16. **[MINOR] Copywriting Contract / 화면 4 — 인용한 서버 오류는 현재 UI에 그대로 나타나지 않는다.**  
    **근거:** `04.5-UI-SPEC.md:126`은 「알 수 없는 정보 항목입니다」를 기존 화면 오류로 재사용한다. 실제 `app/(app)/admin/visibility/actions.ts:22`에는 그 검증 문구가 있지만, `app/(app)/admin/permissions/permission-grid-client.tsx:74`~`:76`은 validationErrors를 「저장하지 못했습니다.」로 바꾼다.  
    **수정:** 서버 검증 문구와 사용자 표시 문구를 구분한다. 래퍼를 유지한다면 명세를 실제 표시와 맞추고, 원인을 노출하려면 오류 매핑 변경을 범위에 포함한다. 어느 쪽이든 다음 행동을 붙인다.

17. **[MAJOR] 관리 인덱스·내비게이션 — 셸 노출 위치가 SYSTEM과 충돌한다.**  
    **근거:** `04.5-UI-SPEC.md:124`는 「더보기」 시트·상단 사용자 메뉴에도 「화면 항목」 항목을 지정한다. `SYSTEM.md:640`은 두 표면에는 「관리」 한 줄만 두고 개별 관리자 화면 이름은 표시하지 않도록 한다. `UI-SPEC.md:268`은 시스템의 정본 메뉴 표 대신 자체 표를 테스트 기준으로 삼는다.  
    **수정:** 「화면 항목」은 관리 인덱스의 마스터 그룹에만 추가한다. 정본인 `SYSTEM.md:636`과 순서 테스트를 함께 갱신하는 절차를 계약에 명시한다(`docs/DESIGN.md:92`).

**판정: 승인 불가 — 기존 빈 필수값과 보관 선택지의 저장 보존을 해결하고, 시스템·실제 컴포넌트와 충돌하는 계약을 수정한 뒤 재검토해야 한다.**
